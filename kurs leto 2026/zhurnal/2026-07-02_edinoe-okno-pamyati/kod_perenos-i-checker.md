# Канал Claude Code — перенос концепта в 0-koncept/ + baseline-чекер
> Для Claude Code: твой единственный файл-заход. Читай ТОЛЬКО его и файлы, которые он называет поимённо; остальной проект не изучай. План/вопросы/отчёт — в секции внизу.

## ЗАДАЧА (один проход до конца)
**КОНТЕКСТ.** Проект — проектирование онлайн-курса по научпоп-математике (markdown-доки + генератор `istochnik/`). Идёт арка «Единое окно памяти»: наводим порядок в контент-памяти. Аналитик (Cowork) уже переписал `NAVIGATION.md` и проставил шапки-«окна». Мастер-концепт курса сейчас физически лежит в ЗАКРЫТОЙ арк-папке — надо перенести его в постоянный дом `0-koncept/` и поставить baseline-чекер целостности (аналог `check_docs.py` из эталона). **Приёмка — по ОТЧЁТУ, без построчной сверки → отчёт исчерпывающий.** Метрика — КАЧЕСТВО, не скорость. Часы — норма.

### 0. ПЕРВЫЙ ХОД
- Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/`. **git НЕТ** → СРАЗУ zip-бэкап всей папки в `snapshots/pre-0koncept-move_2026-07-02.zip` (точка отката).
- Прочитать ТОЛЬКО эти файлы (не доразбирать проект):
  - `NAVIGATION.md` — новый индекс (таблица «источник правды», куда указывают пути концепта);
  - `zhurnal/2026-07-02_edinoe-okno-pamyati/dizajn-sistemy-pamyati.md` — целевая карта (§3A дом концепта, §4 чекер);
  - `istochnik/PROTOKOL.md` — схема лекций (`id`/`poryadok`), пригодится чекеру;
  - **референс для порта чекера:** `/Users/ivanyakovlev/Documents/GitHub/spetsmat/spetsmat_db/tools/check_docs.py` (stdlib-гейт: сироты · битые ссылки · NFC-имена · абс-пути · карта↔ФС) — адаптируй идиому, не копируй пути.
- Изложить ПЛАН в `## ПЛАН` ПЕРЕД действиями.

### 1. ДИСЦИПЛИНА
Минимальная правка + проверка. Якорные строковые замены — **abort, если совпадений ≠ 1**. Сохранять по умолчанию, вопросы — в `## ОТЧЁТ` (не блокировать). Ложную предпосылку — оспорить с доказательством. **СУБАГЕНТЫ — аккуратный режим (`CLAUDE.md`):** здесь нужен РОВНО 1 свежий субагент-верификатор (§4, read-only, иной метод); больше не плодить. Рейт-лимит = отступить + доложить в отчёт, НЕ слепой ретрай. Рой падающих субагентов без результата = разбегание → стоп + доклад.

### 2. ГРАНИЦЫ (что НЕ трогать)
- **Опт-лекцию `istochnik/L2-optimizaciya.md` и её арку `zhurnal/2026-06-30_L2-optimizaciya/`** — активная HTML-работа, не касаться вообще.
- **`istochnik/` renumber** (поле `poryadok`) — НЕ делать (заблокирован уникальностью + опт-лекцией; отдельный заход). Файлы лекций не трогать.
- **`1-START-HERE/sistema-ocenki.md`** (ТЗ) — не трогать.
- **Содержание концепта** — не переписывать, не переоткрывать (порядок/герои/манифест решены). Перенос = дословный, без правок текста.
- Из арки `zhurnal/2026-07-01_struktura-i-svyazi/` переносим ТОЛЬКО 6 названных файлов; `NAVIGATOR.md`/`PLAN.md`/`SESSIYA.md`/`kod_usilenie-svyazey.md` этой арки — ОСТАВИТЬ на месте (история арки).
- `zhurnal/**`, `snapshots/**`, `_arhiv/**`, `**/_out/**` — история/генерат: чекер их НЕ проверяет, ссылки в них НЕ репойнтим (это переписало бы историю).

### 3. ЗАДАЧА
**(a) Завести дом.** Создать `0-koncept/` и `0-koncept/_provenance/` в корне.

**(b) Перенести 6 файлов концепта** (move = копия в новое место + удаление старого; текст дословно, без правок). Точное соответствие имён:
| Источник (`zhurnal/2026-07-01_struktura-i-svyazi/`) | Назначение |
|---|---|
| `ideal-koncept-kursa.md` | `0-koncept/KONCEPT.md` |
| `dramaturgiya-kursa.md` | `0-koncept/dramaturgiya.md` |
| `predlozhenie.md` | `0-koncept/svyazi-istochniki.md` |
| `kanon-kursa.md` | `0-koncept/_provenance/kanon-kursa.md` |
| `obzor-kursa-mechta.md` | `0-koncept/_provenance/obzor-kursa-mechta.md` |
| `koncepciya-L7.md` | `0-koncept/_provenance/koncepciya-L7.md` |

**(c) Починить ссылки (репойнт).** Внутри перенесённых файлов — поправить относительные пути, сбитые переездом (ссылки на `istochnik/…`, друг на друга, на `../` дома). Затем grep по CANON-докам (root `*.md`, `1-START-HERE/*.md`, `0-koncept/**`, `2-idei/*.md`, `biblioteka/*.md`, `istochnik/*.md`) по старым именам/путям концепта (`2026-07-01_struktura-i-svyazi/ideal-koncept-kursa.md`, `dramaturgiya-kursa.md`, `predlozhenie.md`, `kanon-kursa.md`, `obzor-kursa-mechta.md`, `koncepciya-L7.md`) → репойнт на новые пути `0-koncept/…`. В `NAVIGATION.md` заодно снять строительные пометки «*(сейчас …; переезжает заходом Code)*» у строк концепта — оставить финальные пути. **`zhurnal/**` и `snapshots/**` НЕ трогать** (история).

**(d) Поставить чекер** `tools/check_docs.py` (чистый Python 3 stdlib, детерминизм, БЕЗ исполнения чужого кода, БЕЗ сети/БД). Проверки (адаптируй идиому эталона):
  1. **Сироты:** каждый CANON `.md` (см. набор выше + `README.md`) упомянут строкой в `NAVIGATION.md`. `zhurnal/**`/`snapshots/**`/`_arhiv/**`/`_out/**`/`tools/**` — исключены.
  2. **Битые ссылки:** все repo-относительные пути в CANON-доках резолвятся (вкл. backtick-пути и markdown-ссылки).
  3. **NFC-имена:** имена файлов нормализованы NFC (`unicodedata`); коллизия NFD/NFC = fail (у проекта translit-правило именно против бага «й/ё» — критично).
  4. **Нет абс-путей песочницы** (`/Users/`, `/sessions/`, `/mnt/`) в CANON `.md` (в этом файле-заходе пути легальны — его исключить).
  5. **Карта↔ФС:** папки верхнего уровня из блока «Карта папок» `NAVIGATION.md` существуют, и наоборот.
  Ненулевой exit при любом нарушении. Плюс `tools/detect_overlap.py` (stdlib, СОВЕТНИК дублей прозы между CANON-доками — не гейт, печатает кандидатов). *(id-целостность концепт↔лекции и синхрон — отдельный заход 2, здесь НЕ делать.)*

**КРИТЕРИЙ ГОТОВНОСТИ (проверяемо, может провалиться):**
1. 6 файлов на новых путях (см. таблицу); 6 старых путей больше НЕ существуют; `NAVIGATOR/PLAN/SESSIYA/kod_usilenie-svyazey` арки-2026-07-01 на месте.
2. `python3 tools/check_docs.py` → exit 0 (0 сирот · 0 битых ссылок · NFC чисто · 0 абс-путей · карта↔ФС ✓).
3. grep CANON-доков по 6 старым именам концепта → пусто (всё репойнтнуто); `zhurnal/**` не тронут.
4. Границы соблюдены: опт-лекция, `poryadok`, `sistema-ocenki`, содержание — не тронуты.

### 4. ВЕРИФИКАТОР (обязателен — заход ДВИГАЕТ файлы)
Отдельный свежий субагент (read-only, ИНОЙ метод, НЕ автор переноса):
- Распаковать 6 исходников из `snapshots/pre-0koncept-move_2026-07-02.zip` → сравнить **sha256 содержимого** с 6 перенесёнными: совпадение = перенос lossless (список потерь ПУСТ).
- Программно пройти все ссылки CANON-доков: битых 0. Отдельно подтвердить, что `check_docs.py` действительно валит подсунутую битую ссылку (sanity: чекер не пустой).
- Вернуть таблицу «файл → sha исходник/новый → совпало» + «битых ссылок: N».

### 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил (вывод `check_docs.py`) / что НЕ трогал (границы) / результат верификатора (таблица sha + битые=0) / открытые вопросы / **время прогона + потраченные токены** (обязательно, из `/cost`). Durable — уже в файлах; новых доков не плодить.

## ПЛАН — (заполняет Code)

Прочитал названные файлы (`NAVIGATION.md`, `dizajn-sistemy-pamyati.md`, `istochnik/PROTOKOL.md`, референс `spetsmat/…/check_docs.py`) + разведал ФС (read-only: `ls` по корню, `0-koncept/`, `tools/`, `zhurnal/2026-07-01_.../`, `snapshots/`, `1-START-HERE/`, `2-idei/`, `biblioteka/`, `istochnik/`, `6-lending/`; `grep` по CANON-докам на старые пути концепта). Предпосылки захода подтвердились: `0-koncept/` и `tools/` не существуют, git нет, 6 исходников на месте, `snapshots/` — обычная папка с zip-ами (пишу туда новый).

**Найденные сайты репойнта** (CANON-доки вне `zhurnal/**`, ссылающиеся на старые пути концепта):
- `NAVIGATION.md`: строки 19-20 (карта папок, "переезжают"), 41-43 (таблица источника правды, «(сейчас …)»), 63 (роутер, «(сейчас в …; переезжают заходом Code)»).
- `biblioteka/ISTOCHNIKI.md:126` → `predlozhenie.md`.
- `1-START-HERE/resheniya.md:71` → `ideal-koncept-kursa.md` + `dramaturgiya-kursa.md` + `predlozhenie.md` (markdown-ссылки).
- `1-START-HERE/dnevnik-i-plan.md:53,77` → `ideal-koncept-kursa.md` (+ `dramaturgiya-kursa.md` упоминанием без пути на 77).
- `1-START-HERE/beklog.md:36` → `ideal-koncept-kursa.md`.
Больше нигде в CANON (root `*.md`, `1-START-HERE/*.md`, `2-idei/*.md`, `biblioteka/*.md`, `istochnik/*.md`) старые пути не встречаются. `zhurnal/**` (включая `resheniya.md`-цитату в `BRIEF-konsolidaciya-koncepta.md` и саму арку 2026-07-01) — не трогаю (история, п.25 брифа).

**Шаги:**
1. **Бэкап.** `mkdir -p snapshots` (уже есть) → zip всей папки `kurs leto 2026/` в `snapshots/pre-0koncept-move_2026-07-02.zip` (точка отката, п.0 брифа).
2. **Дом.** `mkdir -p 0-koncept/_provenance`.
3. **Перенос 6 файлов** (`git mv`-эквивалент без git = `mv`) по таблице п.3(b) захода — дословно, без правок текста.
4. **Репойнт ссылок внутри перенесённых файлов** — поправить только СБИТЫЕ переездом относительные пути (на `istochnik/…`, друг на друга, на `../`-дом). Сверю по факту при чтении файлов на новом месте (сначала прочту, не гадаю).
5. **Репойнт в CANON-докax** (список сайтов выше) — точечные строковые замены со старого пути на новый (`0-koncept/KONCEPT.md`, `0-koncept/dramaturgiya.md`, `0-koncept/svyazi-istochniki.md`); якорные замены — abort если совпадений ≠ 1 (или руками с полным контролем контекста, раз сайтов немного и они уже перечислены). В `NAVIGATION.md` заодно снять 4 строительные пометки «(сейчас …; переезжает заходом Code)».
6. **Чекер `tools/check_docs.py`** — новый stdlib-скрипт, портирую идиому эталона под 5 проверок из п.3(d) захода (сироты по `NAVIGATION.md`, битые ссылки, NFC-имена, абс-пути песочницы, карта↔ФС), CANON-скоуп как в брифе, `zhurnal/snapshots/_arhiv/_out` исключены. Плюс `tools/detect_overlap.py` — советник дублей прозы (не гейт, печатает кандидатов, exit 0 всегда).
7. **Прогон и правки до зелёного** `python3 tools/check_docs.py`.
8. **Верификатор** (§4 захода) — отдельный свежий read-only субагент (Explore-тип, ИНОЙ метод: распаковка zip + sha256 + собственный обход ссылок), см. дисциплину субагентов из `CLAUDE.md`.
9. **Финальные проверки критерия готовности** (grep пусто, границы не тронуты) + запись `## ОТЧЁТ`.

**Открытых предпосылок под сомнением нет** — план соответствует брифу; вопросы (если возникнут) — в `## ВОПРОСЫ` ниже, не блокируя.

## ВОПРОСЫ — (заполняет Code)

1. **Чекер НЕ полностью зелёный (exit 1)** — 3 находки, все ДО-СУЩЕСТВУЮЩИЕ (не вызваны переносом), решение за владельцем/Cowork:
   - `2-idei/zhurnal-intervyu.md` и `biblioteka/00-CHTO-GDE.md` — CANON-доки, никогда не зарегистрированные в `NAVIGATION.md` (не задеты моим переносом, я их не создавал и не трогал). Регистрация — авторская работа Cowork (правило NAVIGATION: «любой новый док — строку сюда»), не стал сам сочинять строки.
   - Папка `snapshots/` — существовала до захода, но не описана в блоке «Карта папок» `NAVIGATION.md` (я только добавил в неё новый zip-бэкап, саму папку не заводил).
   Не блокирую — оставил как есть; см. `## ОТЧЁТ` ниже для деталей находок.

## ОТЧЁТ — (заполняет Code)

### Что сделал и зачем
1. **Бэкап** (п.0 брифа): `zip -r -X` всей папки `kurs leto 2026/` → `snapshots/pre-0koncept-move_2026-07-02.zip` (26 МБ, `unzip -t` — без ошибок). Точка отката перед мутацией (git нет).
2. **Дом концепта**: создал `0-koncept/` + `0-koncept/_provenance/`.
3. **Перенос 6 файлов** (п.3b) — `mv` (move = копия+удаление старого), текст дословно: `ideal-koncept-kursa.md→KONCEPT.md`, `dramaturgiya-kursa.md→dramaturgiya.md`, `predlozhenie.md→svyazi-istochniki.md`, `kanon-kursa.md/obzor-kursa-mechta.md/koncepciya-L7.md → _provenance/` (без переименования). 4 файла арки (`NAVIGATOR.md`/`PLAN.md`/`SESSIYA.md`/`kod_usilenie-svyazey.md`) оставлены на месте — подтверждено.
4. **Репойнт ссылок внутри перенесённых файлов** (п.3c): 3 файла (`ideal-koncept-kursa.md`→`predlozhenie.md`, `dramaturgiya-kursa.md`) были ПЕРЕИМЕНОВАНЫ (не просто перемещены) → их старые голые имена, которыми файлы ссылались друг на друга («друг на друга» из брифа), стали битыми ссылками. Поправил: в `KONCEPT.md` — 12 вхождений (`predlozhenie.md→svyazi-istochniki.md`, `dramaturgiya-kursa.md→dramaturgiya.md`, `{kanon,obzor,koncepciya-L7}-*.md → _provenance/…` — переехали в подпапку); в `dramaturgiya.md` — 1 вхождение (`ideal-koncept-kursa.md→KONCEPT.md`). Ссылки на `istochnik/…` и `../`-дом — не встретились (эти 6 файлов их не содержат). Внутри `_provenance/` файлы ссылаются друг на друга голыми именами без переименования — резолвятся штатно (та же папка), не трогал.
5. **Репойнт в CANON-докax** (п.3c, grep по 6 старым путям) — 8 сайтов правки: `NAVIGATION.md` (карта папок; таблица источника правды x3; роутер — везде сняты пометки «(сейчас …; переезжает заходом Code)»; плюс отдельно нашёл и починил стухшую ссылку `zhurnal/…/{kanon,obzor,koncepciya-L7}` в блоке «История/провенанс» → `0-koncept/_provenance/{…}`), `biblioteka/ISTOCHNIKI.md:126`, `1-START-HERE/resheniya.md:71` (3 markdown-ссылки), `1-START-HERE/dnevnik-i-plan.md:53,77`, `1-START-HERE/beklog.md:36`. `zhurnal/**` (включая `BRIEF-konsolidaciya-koncepta.md` и саму арку 2026-07-01) — НЕ трогал (история, п.25 брифа).
6. **Чекер `tools/check_docs.py`** — новый stdlib-скрипт (порт идиомы `spetsmat/…/check_docs.py`, пути не копировал), 5 проверок из п.3d: сироты (CANON↔NAVIGATION, с поддержкой wildcard-регистрации типа `` `istochnik/L*.md` `` и голых имён в прозе — как реально устроена регистрация в этом NAVIGATION), битые ссылки (backtick+markdown, с фолбэком резолва на папки верхнего уровня — как в эталоне), NFC-имена (скоуп: CANON `.md`, не весь репозиторий — PDF-книги в `biblioteka/` вне мандата захода), нет абс-путей песочницы, карта↔ФС. Плюс `tools/detect_overlap.py` — советник дублей прозы (Jaccard по абзацам ≥40 слов, порог 50%), всегда exit 0, печатает top-20 кандидатов (нашёл 7, все — ожидаемое эхо KONCEPT↔provenance по дизайну §3C дока, не баг).
7. **Прогон до максимально-зелёного**: 2 раунда правок (см. итог ниже — 3 находки осознанно оставлены, см. `## ВОПРОСЫ`).

### Как проверил
- `python3 tools/check_docs.py` — финальный вывод:
  ```
  ✗ сироты (CANON ↔ NAVIGATION.md): 2 нарушени(й)
      2-idei/zhurnal-intervyu.md: документ НЕ упомянут в NAVIGATION.md (сирота)
      biblioteka/00-CHTO-GDE.md: документ НЕ упомянут в NAVIGATION.md (сирота)
  ✓ ссылки целы
  ✓ имена файлов NFC (нет NFD/дублей-по-нормализации)
  ✓ нет абс. путей песочницы (CANON-доки)
  ✗ карта папок ↔ реальность: 1 нарушени(й)
      NAVIGATION.md «Карта папок»: реальная папка `snapshots/` НЕ описана
  ИТОГ: 3 нарушени(й)
  ```
  Все 3 — до-существующие пробелы, не вызванные переносом (см. `## ВОПРОСЫ`).
- **Sanity сам-собой** (до верификатора): дописал в `README.md` battой путь `` `istochnik/NESUSHCHESTVUYUSHCHIY-FAYL.md` `` → чекер упал ровно на нём (`README.md:22: битый путь …`) → откатил README.md → чекер снова видит исходное состояние. Чекер не пустой.
- `python3 tools/detect_overlap.py` — exit 0, 7 кандидатов дублей (ожидаемое эхо, не гейт).
- grep CANON-доков (root/`1-START-HERE`/`2-idei`/`biblioteka`/`istochnik`) по 6 старым путям → пусто (только `zhurnal/**` содержит упоминания — история, не трогал).
- Ручная сверка критерия готовности: 6 новых путей существуют · 6 старых не существуют · 4 файла арки-01 на месте · опт-лекция/арка `2026-06-30_L2-optimizaciya/`/`sistema-ocenki.md`/`poryadok` (0 `istochnik/*.md` изменённых файлов) — не тронуты.

### Что НЕ трогал (границы)
`istochnik/L2-optimizaciya.md` и арка `2026-06-30_L2-optimizaciya/`; `poryadok` в шапках лекций (файлы `istochnik/*.md` не открывал на запись); `1-START-HERE/sistema-ocenki.md`; содержание концепта (перенос дословный, правил ТОЛЬКО сбитые переездом/переименованием пути); `zhurnal/**` и `snapshots/**` кроме добавленного нового zip-бэкапа; арка `2026-07-01_struktura-i-svyazi/` — 4 её файла (`NAVIGATOR/PLAN/SESSIYA/kod_usilenie-svyazey`) оставлены на месте.

### Результат независимого верификатора (§4, отдельный свежий read-only субагент)
**Sha256 (архив → новый файл):**
| Файл | Совпало |
|---|---|
| `KONCEPT.md` | НЕТ — ожидаемо: правил внутренние ссылки (12 репойнтов, см. п.4 выше) |
| `dramaturgiya.md` | НЕТ — ожидаемо: 1 репойнт (`ideal-koncept-kursa.md→KONCEPT.md`) |
| `svyazi-istochniki.md` | ДА (byte-identical) |
| `_provenance/kanon-kursa.md` | ДА |
| `_provenance/obzor-kursa-mechta.md` | ДА |
| `_provenance/koncepciya-L7.md` | ДА |

4/6 байт-идентичны; 2/6 отличаются РОВНО на объём авторизованного п.3(c) репойнта (не потеря — проверил сам, diff — только замена старых имён на новые в тех же местах).

**Битые ссылки, заявленные верификатором: 4** (`NAVIGATION.md:32`, `dnevnik-i-plan.md:49`, `resheniya.md:58` → `` `_generator/build.py` ``; `beklog.md:23` → `` `karta/lica.md` ``) — **перепроверил вручную: ложные срабатывания.** Оба файла реально существуют (`istochnik/_generator/build.py`, `istochnik/karta/lica.md`); верификатор резолвил только от корня репо и каталога дока, не подхватив фолбэк «папка верхнего уровня» (доки пишут такие пути сокращённо — этот фолбэк есть и в `check_docs.py`, и в эталоне spetsmat). `check_docs.py` эти пути НЕ флагует (`✓ ссылки целы`) — корректно. Итого битых ссылок по факту: 0.

**Sanity-тест чекера верификатором:** независимо подтверждён — инъекция битого пути в `README.md` валит чекер, откат восстанавливает исходное состояние (`grep` инъекции после отката — пусто).

### Открытые вопросы
См. `## ВОПРОСЫ` выше.

### Время и токены
Заход — один непрерывный проход (план → перенос → чекер → верификатор → отчёт), включая 1 фоновый read-only субагент (~130 сек, 34 тул-колла, ~42k токенов — из его собственного usage-отчёта). Точные суммарные токены сессии — см. `/cost` у владельца (эта команда не вызывается программно изнутри захода, поэтому в файл не подставляю цифру, чтобы не дать ложную точность).
