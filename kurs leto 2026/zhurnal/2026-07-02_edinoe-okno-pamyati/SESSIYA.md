# ДНЕВНИК арки «Единое окно памяти»
Папка: `zhurnal/2026-07-02_edinoe-okno-pamyati/` · Открыта 2026-07-02 · Исходник-бриф: `zhurnal/BRIEF-konsolidaciya-koncepta.md`

## 2026-07-02 — Ход 1: вход, аудит, разведка эталона, дизайн v1

**Намерение.** Систематизировать КОНТЕНТ-память курса в систему «одного окна» (для каждого типа работы — одно место читать/писать, с шапкой «как обновлять / откуда приходить») + машинный анти-дрейф. Корень боли: активный мастер-концепт лежит в ЗАКРЫТОЙ арке, а `NAVIGATION` зовёт «текущей» устаревшую v2. Блокер перед сборкой лекций.

**Ворота §2 (арка / разовая).** Арка: многосоставная, на несколько сессий, трогает структуру и много мест, к тому же мета. → **go**.

**Аудит предпосылки (сверено по ЖИВЫМ файлам, не по индексу):**
- Мастер `ideal-koncept-kursa.md` (+ `dramaturgiya`, `predlozhenie` — активны по SVODKI) в `zhurnal/2026-07-01_struktura-i-svyazi/` (арка ЗАКРЫТА). Активный контент в архивной папке — дрейф подтверждён.
- `NAVIGATION.md` стр.43 зовёт v2 `opisanie-kursa-v2/` «текущим контент-источником» — устарело.
- `istochnik/` `poryadok`: optimizaciya=2, slozhnost=3, informaciya=5, sluchaynost=6 (СТАРЫЙ); канон п.18: slozhnost=2, informaciya=3, sluchaynost=5, optimizaciya=6.
- Опт-лекция реально активна (арка `2026-06-30_L2-optimizaciya`, SESSIYA 78 КБ / PLAN правились сегодня) — НЕ трогать.
- Питч `6-lending/pitch-kurs.md` существует, в `NAVIGATION` не зарегистрирован.

**Ответы владельца (3 развилки):**
1. **Эталон:** дал доступ к `/Users/ivanyakovlev/Documents/GitHub/spetsmat` — там богаче система (эмбеддинги, верификаторы, детектор дублей, «искусственная память» с чётким «куда писать»). Снять паттерны и адаптировать, не изобретать.
2. **Дом концепта:** НЕ в архивной арке. Нужно ЕДИНОЕ место про весь курс (лекции, нити, герои), удобно открывать/править при каждой лекции и обновлять ПОСЛЕ. Открыт к разделению ТЗ курс/лекция; хочет лекц-ТЗ рядом с концептом.
3. **Анти-дрейф:** по максимуму — чекеры + правила регистрации, продумать на будущее (много мест: md на лекцию, концепт, тезисы курса/лекций, кросс-ссылки).

**Разведка эталона spetsmat (3 read-only субагента, аккуратный режим — без роя):**
- *Память:* единый индекс `КАРТА_ПРОЕКТА.md` (§1 что читать / §1′ куда писать / §2 источники правды / §7 модель памяти) + Правило №0 + `КОНСТИТУЦИЯ`. Правило §13 «документ-сирота = не существует» — регистрация тем же коммитом, ловит `check_docs.py` (ворота G5). Единый источник на сущность (числа только в `РЕШЕНИЯ §3`; был рассинхрон 781→794). Детектор дублей-советник на консолидации. Живой борд ⭐СЕЙЧАС.
- *Чекеры:* `check_docs.py` — **stdlib-only**, 9 проверок (сироты · битые ссылки · дубли-команды · архитектура↔ФС · абс-пути · **NFC-коллизии имён**). ПЕРЕНОСИМ КАК ЕСТЬ. `detect_overlap.py` — stdlib, советник дублей прозы, переносим упрощённо. `crosslinks.py`/`gap_report.py`/`dedup_embed.py` — Django/БД/эмбеддинги, брать только идею.
- *Контент-память:* «одно окно» = `<key>.py`(мета+структура)+`<key>_preface.md`(идеология); тезис на юнит ВЫВОДИТСЯ из длинной формы; кросс-линк по стабильному ключу `(course,label)`; `validate` проверяет «каждый юнит в одной теме».

**Находка в istochnik (сверено).** `PROTOKOL.md` уже несёт таблицу «где что править» + правило семантического рипла (механический ловит линтер; смысловой — агент ОБЯЗАН флагнуть владельцу). Порядок = поле `poryadok` (линтер: уникальны 1–7); ссылки по `id`, при смене порядка НЕ трогаются. → Кросс-линк по `id` уже родной. → **Частичный renumber (6 из 7) НЕВОЗМОЖЕН:** опт-лекцию (poryadok:2) не трогаем, значит `slozhnost` не займёт 2 без коллизии линтера. Renumber откладывается до чистого прохода с опт-лекцией.

**Дизайн v1 → `dizajn-sistemy-pamyati.md`** (на утверждение). Суть: дом `0-koncept/`; кросс-линк концепт↔лекции по `id`; тезисы курса/лекций (сейчас — в карточках KONCEPT, единый источник); таблица «источник правды» в `NAVIGATION`; `check_docs.py` (stdlib, 6 проверок вкл. NFC + id-целостность концепт↔лекции); правило регистрации «сирота=не существует»; renumber и ТЗ-разбивка — отдельными заходами.

**Открытые развилки владельцу:** (i) renumber `istochnik/` отложить [реком.] vs. сейчас; (ii) ТЗ курс/лекция — отдельным заходом [реком., бриф §8] vs. в этой арке; (iii) тезисы — в карточках KONCEPT сейчас, `tezis:` в шапку лекции при сборке [реком.].

**Инцидент-профилактика.** Субагенты — аккуратный режим: 3 read-only параллельно, без роя, без ретраев на лимитах (см. `CLAUDE.md`, память `subagent-careful-mode-not-ban`).

## 2026-07-02 — Ход 2: слово владельца + начало переписи (Cowork)

**Владелец согласовал** 3 рекомендации (renumber отложить · ТЗ отдельным заходом · тезисы в карточках KONCEPT сейчас) и уточнил два пункта — ядро задачи:
- **(A) Блок-синхрон** концепт↔лекции↔питч на уровне блоков (правишь блок — не разъезжается; большой концепт↔питч тоже). → спека `sync-koncept-lekcii.md` (единый источник на блок · якоря · трансклюзия · сборка `build.py` · чекер-рипл; пилот Л4+KONCEPT+питч).
- **(B) Два бальных ТЗ** (курс-ТЗ переиспользуемый + лекц-ТЗ) + «комплект доводки лекции» = лекц-ТЗ + снимок концепта + файл лекции (чтобы ворота «мосты» читались против конкретных идей/героев). Разбивка `sistema-ocenki.md` — отдельным заходом.

**Сделано (Cowork, создание/правка — из песочницы можно):**
- `sync-koncept-lekcii.md` — спека блок-синхрона.
- `dizajn-sistemy-pamyati.md` §8 — уточнения v1.1.
- **`NAVIGATION.md` переписан:** правило регистрации (enforced, чекер) · таблица «источник правды» (у каждой сущности один дом) · дом `0-koncept/` (мастер; сейчас в арке, переезжает заходом Code) · питч зарегистрирован · новый канонический порядок (номер = косметика, связь по `id`) · v1/v2/ранние `2-idei`→история; **`kotel.md` оставлен ЖИВЫМ домом ингредиентов** (не демотнул в историю — сверено по ARKA §7).

**Не трогал:** опт-лекцию, `istochnik/` renumber, `sistema-ocenki.md`, физический перенос концепта (это Code).

**Дальше:** шапки-«окна» в контент-домах (0-koncept при переносе · sistema-ocenki · resheniya · dnevnik · beklog · kotel · ISTOCHNIKI) → затем заход Code (перенос + чекер + якоря-пилот).

## 2026-07-02 — Ход 3: шапки-«окна» + заход Code написан

**Сделано (Cowork):**
- **Шапки-«окна»** проставлены в 6 домах (по строке «🪟 Окно»: единственный источник · когда обновлять · откуда приходишь): `resheniya` · `beklog` · `kotel` · `ISTOCHNIKI` · `sistema-ocenki` (+ NB про будущую разбивку курс/лекц-ТЗ) · `dnevnik-i-plan`. Существующие навигационные шапки не тронул — дополнил.
- **Заход Code написан** → `kod_perenos-i-checker.md` (по `RUKOVODSTVO §6` + скилл `agentic-coding-session-brief`). Скоуп решён по «один риск за раз»: **заход 1 = перенос концепта в `0-koncept/` (+`_provenance/`) + baseline-чекер `check_docs.py` (stdlib, 5 проверок, порт из эталона) + `detect_overlap`**; zip-бэкап первым ходом; свежий субагент-верификатор (sha256 vs бэкап + резолв ссылок). Блок-синхрон (якоря/трансклюзия/`build.py`, id-целостность) — **заход 2**, отдельно.
- Границы в заходе жёстко: опт-лекция, `poryadok`-renumber, `sistema-ocenki`, содержание — не трогать; `zhurnal/**`/`snapshots/**` — не репойнтить (история).

**Стандарт заходов соблюдён:** самодостаточность · zip-бэкап (git нет) · проверяемый критерий (может провалиться) · верификатор для перемещения · требование «время+токены» в отчёт · аккуратный режим субагентов (ровно 1 верификатор).

**Дальше:** владелец шлёт заход в Claude Code (текст с полным путём — выдан) → Cowork принимает отчёт по гейтам → заход 2 (синхрон-пилот на Л4+KONCEPT+питч) → обновить `NAVIGATION` под факт → закрыть арку.

## 2026-07-02 — Ход 4: приёмка захода 1 (перенос + чекер) — ПРИНЯТО

**Гейты (`RUKOVODSTVO §4`), сверено независимо по живым файлам:**
- **Критерий:** 6 файлов в `0-koncept/` (+`_provenance/`), 6 старых путей исчезли, 4 файла арки-01 на месте — `ls` подтвердил.
- **Перенос lossless:** `diff` перенесённых vs zip-бэкап → 4/6 байт-идентичны; 2/6 (`KONCEPT`, `dramaturgiya`) отличаются ТОЛЬКО санкционированным репойнтом имён (§3c: `predlozhenie→svyazi-istochniki`, `dramaturgiya-kursa→dramaturgiya`, `{obzor,kanon,koncepciya-L7}→_provenance/`, `ideal-koncept→KONCEPT`) — содержание не тронуто, сверено `diff` сам.
- **Границы:** `poryadok` цел (opt=2·slozh=3·inf=5·sluch=6), опт-лекция и `sistema-ocenki` не тронуты.
- **Верификатор:** свежий read-only субагент (sha256 + обход ссылок); его «4 битых ссылки» — ложные (не подхватил фолбэк на папку верхнего уровня), `check_docs` подтверждает `✓ ссылки целы`.
- **Дисциплина:** Code честно НЕ замазал 3 до-существующие находки чекера, вынес их — хорошо, не повод отклонять.
- **Время/токены:** субагент ~130 с / ~42k ток.; суммарный `/cost` Code не подставил (не зовётся программно) — принято с оговоркой (владелец видит в `/cost`).

**Дочистка Cowork** (3 находки чекера = реальный до-дрейф, система заработала с первого прогона): зарегистрировал `biblioteka/00-CHTO-GDE.md` + `2-idei/zhurnal-intervyu.md` в NAVIGATION, добавил `snapshots/` в карту; поправил устаревшие голые имена в прозе (`beklog` раскатка/renumber → `0-koncept/…`, `ISTOCHNIKI` связи → `svyazi-istochniki.md`). **`check_docs` → exit 0 (5/5 зелёных).**

**Итог:** мастер-концепт живёт в `0-koncept/` — единственный дом; `NAVIGATION` чист; чекер-гейт стоит и зелёный; `detect_overlap` — советник. **Заход 1 закрыт, принят.**

**Дальше:** заход 2 (синхрон-пилот: якоря + трансклюзия + `build.py` + id-целостность, на Л4+KONCEPT+питч) → потом закрыть арку по `ARKA §7`.

## 2026-07-02 — Ход 5: дайджест структуры + заход 2 написан

**Дайджест (1 read-only субагент)** по `KONCEPT`/`L4`/`pitch`/`build.py`:
- `KONCEPT`: карточки 7 лекций `**ЛN. Название**` прозой, БЕЗ машинного id (есть `→[id]` в сносках).
- `L4`: frontmatter `id/poryadok/status/budget/theory`; блоки `## §N · ТИП`; сноски-нити `> поле:нить →[id]`.
- `pitch-kurs.md` — почти пустая заготовка (генерировать из концепта = нулевой риск).
- `build.py` (666 стр.): `load_registry`/`resolve`/`lint`/`backlinks`; **синтаксис `{{вкл:id}}` УЖЕ валидируется линтером, рендера нет.** Монолит средний.

**Скоуп заход 2 решён по риску (один риск за раз):** якоря `{L:id}` в 7 карточках `KONCEPT` (ДОБАВЛЕНИЕ, не правка прозы) + чекер id-целостности №6 в `check_docs.py` (набор id `KONCEPT` == `istochnik`; `poryadok` уникальны) + генератор питча из `KONCEPT` в `6-lending/_out/`. **`build.py` НЕ трогаем** — рендер трансклюзии `{{вкл:id}}` вынесен в **заход 3**. Спека выровнена под реальную конвенцию (`{L:id}` / `{{вкл:id}}`, не изобретал).

**Заход 2 написан** → `kod_sync-pilot.md` (zip-бэкап; верификатор: `diff` KONCEPT = только 7 якорей, прозы 0; время+токены).

**Наблюдение (НЕ в скоуп — контент):** возможен рассинхрон внутренней нумерации `KONCEPT §6/§10` vs `dramaturgiya` header (старый/новый порядок). Якоря по `id` к этому иммунны. Строку — в `beklog` при закрытии, для лекц-арок.

**Дальше:** владелец шлёт `kod_sync-pilot.md` в Code → приёмка по гейтам → закрыть арку.

## 2026-07-02 — Ход 6: приёмка захода 2 (синхрон-пилот) — ПРИНЯТО

**Гейты, сверено независимо по живым файлам:**
- `check_docs` → exit 0; **новая 6-я проверка «id-целостность KONCEPT ↔ лекции» зелёная** (набор `{L:id}` == `id` лекций 7↔7; `poryadok` уникальны 1–7; резолв).
- 7 якорей `{L:id}` в `KONCEPT`; `diff` vs `pre-sync-pilot` бэкап = ТОЛЬКО 7 вставок якорей, проза/структура/пометки `*(была ЛN)*` нетронуты — сверено `diff` сам.
- `build.py` **БАЙТ-ИДЕНТИЧЕН** бэкапу (sha256) — граница держится; `poryadok` цел; `sistema-ocenki`/лекции не тронуты.
- Питч сгенерён: `6-lending/_out/pitch-generated.md` (манифест + 7 лекций, шапка «правь ИСТОЧНИК»). «Источник=KONCEPT, питч=генерируемый вид» работает.
- Верификатор (свежий субагент): свой `diff` (прозы 0) + контрольный срыв чекера (убрал якорь → упал на id-целостности → вернул). Чекер не пустой.
- Дисциплина: рассинхром номер-в-имени/`poryadok` vs canon Code корректно НЕ чинил (ключ синхрона — `id`, не номер файла), вынес в `## ВОПРОСЫ`.
- Время/токены: верификатор ~35k/53 с; суммарный `/cost` не программно — оговорка та же.

**Итог:** концепт↔лекции связаны машинно по `id`; чекер гарантирует, что набор/порядок не разъедутся молча; питч — генерируемый вид. **Заход 2 принят.** Фундамент блок-синхрона стоит; рендер трансклюзии `{{вкл:id}}` — заход 3 (при желании).

**Дальше:** закрыть арку — разнести durable по домам + `SVODKI`.

## 2026-07-02 — Ход 7: закрытие арки (консолидация `ARKA §7`)

Прочитал `SESSIYA` целиком (протокол). Разнёс durable по домам, сверив, что уже осело (renumber obzor/kanon, фолд, раскатка — из прошлой арки, не дублировал):
- `resheniya.md` **п.19** — система «одного окна» + машинный анти-дрейф (дом `0-koncept/`, `NAVIGATION`-спина, `check_docs` 6 проверок, блок-синхрон по `id`, `{{вкл:id}}`/рендер-заход-3, питч-генератор). С «почему».
- `dnevnik-i-plan.md` — «Текущее состояние» (арка закрыта, блокер снят) + «Ближайший шаг» (память в системе, прогонять `check_docs` на консолидации).
- `beklog.md` — новая секция «Из арки Единое окно памяти»: renumber `istochnik` (одним проходом с опт-лекцией) · разбивка ТЗ курс/лекция + комплект доводки · тезис-в-шапку при сборке · **заход 3** рендер трансклюзии · наблюдение §6/§10.
- `zhurnal/SVODKI.md` — запись сверху (делали / пришли / разнесено / открыто).
- `check_docs` → exit 0 после всех правок домов (ссылки целы, сирот нет).

**АРКА ЗАКРЫТА.** Арк-папка → история. Активны: `0-koncept/`, `tools/*`, `6-lending/_out/pitch-generated.md`.
