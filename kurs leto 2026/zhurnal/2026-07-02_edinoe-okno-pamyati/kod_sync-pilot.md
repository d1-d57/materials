# Канал Claude Code — синхрон-пилот: якоря концепта + id-целостность + генератор питча
> Для Claude Code: твой единственный файл-заход. Читай ТОЛЬКО его и файлы, которые он называет поимённо; остальной проект не изучай. План/вопросы/отчёт — в секции внизу.

## ЗАДАЧА (один проход до конца)
**КОНТЕКСТ.** Проект — онлайн-курс научпоп-математики (markdown + генератор `istochnik/`). Арка «Единое окно памяти»: заход 1 (перенос концепта в `0-koncept/` + baseline-чекер `tools/check_docs.py`) ПРИНЯТ. Теперь — фундамент блок-синхрона концепт↔лекции↔питч: сделать связь МАШИННОЙ, низким риском, **БЕЗ рендера трансклюзии** (он — заход 3). **Приёмка — по ОТЧЁТУ.** Качество, не скорость. Часы — норма.

### 0. ПЕРВЫЙ ХОД
- Рабочая папка `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/`. **git НЕТ** → СРАЗУ zip-бэкап всей папки в `snapshots/pre-sync-pilot_2026-07-02.zip`.
- Прочитать ТОЛЬКО эти файлы (проект не доразбирать):
  - `zhurnal/2026-07-02_edinoe-okno-pamyati/sync-koncept-lekcii.md` — спека синхрона (конвенции `{L:id}`, `{{вкл:id}}`);
  - `0-koncept/KONCEPT.md` — мастер; карточки 7 лекций (заголовки вида `**ЛN. Название**`, раздел «5» / «карточки»);
  - `istochnik/L1-granicy.md` … `istochnik/L7-myshlenie.md` — 7 лекций; нужны ТОЛЬКО `id:` и `poryadok:` из frontmatter (НЕ править);
  - `tools/check_docs.py` — сюда добавляем проверку №6;
  - `6-lending/pitch-kurs.md` — цель генерации (сейчас заготовка);
  - `istochnik/PROTOKOL.md` — схема (`id`/`poryadok`, правило рипла).
- Изложить ПЛАН в `## ПЛАН` ПЕРЕД действиями.

### 1. ДИСЦИПЛИНА
Минимальная правка + проверка. Якорные строковые замены — **abort при совпадениях ≠ 1**. Сохранять по умолчанию, вопросы — в отчёт. Ложную предпосылку — оспорить с доказательством. **СУБАГЕНТЫ — аккуратный режим (`CLAUDE.md`):** максимум 1 свежий read-only верификатор (§4); рейт-лимит = отступить + доложить, НЕ слепой ретрай; рой падающих = стоп + доклад.

### 2. ГРАНИЦЫ (что НЕ трогать)
- **`istochnik/_generator/build.py` — НЕ трогать.** Рендер трансклюзии `{{вкл:id}}` — это заход 3. Здесь только `check_docs.py` + новый скрипт.
- **Содержание `KONCEPT.md`** — прозу не переписывать. В `KONCEPT` ТОЛЬКО добавить якорь `{L:id}` к 7 заголовкам-карточкам лекций (больше ничего).
- **`istochnik/L*.md`** — только ЧИТАТЬ (`id`/`poryadok`); не править. **Опт-лекцию `L2-optimizaciya.md` не касаться**, `poryadok` нигде не менять.
- `1-START-HERE/sistema-ocenki.md`, `resheniya.md`, прочие дома — не трогать.
- `zhurnal/**`, `snapshots/**` — история, не трогать.

### 3. ЗАДАЧА
**(A) Якоря лекций в `KONCEPT.md`.** К каждому из 7 заголовков-карточек лекции добавить машинный якорь `{L:<id>}` с id из соответствующей `istochnik/L*.md`. Соответствие ЛN↔id — по canon-порядку (`resheniya` п.18) и frontmatter лекций: Л1 `granicy` · Л2 `slozhnost` · Л3 `informaciya` · Л4 `kriptografiya` · Л5 `sluchaynost` · Л6 `optimizaciya` · Л7 `myshlenie`. Пример: `**Л2. Сложность** {L:slozhnost}`. **ТОЛЬКО добавление якоря; прозу карточки не трогать.** *(Прим.: если внутренняя нумерация карточек `KONCEPT` где-то разошлась с canon — НЕ чинить содержание, только повесить якорь по id; рассинхром вынести в `## ВОПРОСЫ`.)*

**(B) Чекер id-целостности** — новая проверка №6 в `tools/check_docs.py` (stdlib, идиома существующих проверок):
  1. множество `{L:id}` в `KONCEPT.md` == множество `id:` из frontmatter `istochnik/L*.md` — точное совпадение 7↔7 (ни лишних, ни недостающих);
  2. `poryadok` во всех `istochnik/L*.md` уникальны в диапазоне 1..7;
  3. каждый `{L:id}` резолвится в реально существующий файл лекции.
  Нарушение → ненулевой exit (в общий итог чекера). Прочие 5 проверок не ломать.

**(C) Генератор питча** — новый `tools/render-pitch.py` (stdlib): читает `KONCEPT.md` (манифест + 7 карточек по якорям `{L:id}`), собирает короткий питч-черновик в `6-lending/_out/pitch-generated.md` (создать `_out/` при нужде). Демонстрация «источник = `KONCEPT`, питч — генерируемый вид». **`pitch-kurs.md` НЕ перезаписывать** (оставить заготовкой). В шапке генерата — строка: «сгенерировано из `0-koncept/KONCEPT.md`; правь ИСТОЧНИК, не этот файл».

**КРИТЕРИЙ ГОТОВНОСТИ (проверяемо, может провалиться):**
1. 7 якорей `{L:id}` в `KONCEPT.md`; `diff` KONCEPT vs zip-бэкап = ТОЛЬКО вставки якорей (проза/структура не тронуты).
2. `python3 tools/check_docs.py` → exit 0, включая новую проверку id-целостности (7↔7 · poryadok уникальны · резолв).
3. `python3 tools/render-pitch.py` → создаёт непустой `6-lending/_out/pitch-generated.md` (манифест + 7 лекций).
4. Границы: `build.py`, лекции, `poryadok`, `sistema-ocenki` — не тронуты.

### 4. ВЕРИФИКАТОР (обязателен — заход правит МАСТЕР `KONCEPT`)
Отдельный свежий read-only субагент (ИНОЙ метод, не автор правок):
- `diff` `0-koncept/KONCEPT.md` (новый) против распакованного из `snapshots/pre-sync-pilot_2026-07-02.zip` → подтвердить: изменения = ТОЛЬКО 7 вставок `{L:id}`, ни одна строка прозы/структуры не изменена (список изменений прозы ПУСТ).
- Независимо подтвердить, что новая проверка чекера НЕ пустая: убрать один `{L:id}` → `check_docs.py` падает на id-целостности → вернуть на место.
- Вернуть: список 7 вставок + «прозы изменено: 0» + «чекер ловит рассинхрон: да».

### 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил (вывод `check_docs.py` + факт генерации питча) / что НЕ трогал (границы: `build.py`, лекции, `poryadok`) / результат верификатора (прозы изменено 0 · чекер ловит) / открытые вопросы / **время прогона + потраченные токены** (обязательно, из `/cost`). Durable — в файлы; новых доков не плодить.

## ПЛАН — (заполняет Code)

**0. Прочитано** (только названные файлы): `sync-koncept-lekcii.md`, `0-koncept/KONCEPT.md` (целиком — карточки в разделе «4. СЕМЬ ЛЕКЦИЙ», не «5», см. ВОПРОСЫ), `tools/check_docs.py`, `6-lending/pitch-kurs.md`, `istochnik/PROTOKOL.md`, frontmatter всех 7 `istochnik/L*.md`. Бэкап `snapshots/pre-sync-pilot_2026-07-02.zip` создан ДО правок (~9 МБ, вся папка кроме `snapshots/`).

**Важная сверка (см. ВОПРОСЫ):** реальные файлы — `L1-granicy.md`(poryadok1) `L2-optimizaciya.md`(poryadok2) `L3-slozhnost.md`(poryadok3) `L4-kriptografiya.md`(poryadok4) `L5-informaciya.md`(poryadok5) `L6-sluchaynost.md`(poryadok6) `L7-myshlenie.md`(poryadok7). Номер в ИМЕНИ ФАЙЛА и `poryadok` — это СТАРЫЙ порядок (Опт была 3-й, тут видна по файлу L2/poryadok2 — примерно старая позиция), а не новый канон из KONCEPT.md (Опт — 6-я). Задание даёт явную таблицу ЛN↔id — она СОВПАДАЕТ с ярлыками карточек в самом KONCEPT.md (§4: Л1 Границы·Л2 Сложность·Л3 Информация·Л4 Криптография·Л5 Случайность·Л6 Оптимизация·Л7 Мышление) — беру эту таблицу, игнорируя номер в имени файла/poryadok. `poryadok` НЕ трогаю и на чекер это не влияет (проверка №6.2 — просто уникальность 1..7, они уже уникальны).

**A. Якоря в KONCEPT.md.** 7 строк-заголовков карточек (grep `^\*\*Л[1-7]\.`, строки 44/47/50/53/56/59/62). Вставляю `{L:<id>}` сразу после закрывающих `**` заголовка, ПЕРЕД любой хвостовой курсивной пометкой типа `*(была Л4)*` — эту пометку не трогаю. Пример: `**Л3. Информация: измерить, сжать, защитить.** {L:informaciya} *(была Л4)*`. Ничего больше в этих строках и во всём файле не меняю.

**B. Чекер (`tools/check_docs.py`, проверка №6).** Добавляю `check_id_sync()`: читает `{L:id}` из `0-koncept/KONCEPT.md` regex'ом, `id:`/`poryadok:` из frontmatter каждого `istochnik/L*.md`; (1) множества id совпадают 7↔7 (симметрич. разность пуста), (2) `poryadok` уникальны в 1..7, (3) каждый `{L:id}` резолвится в существующий `istochnik/L<N>-*.md` (по id, не по числу в имени). Добавляю в список `CHECKS`, не трогая существующие 5.

**C. `tools/render-pitch.py`** (новый, stdlib). Читает манифест (раздел «1. МАНИФЕСТ», абзац «В одну фразу») + карточки по `{L:id}` из KONCEPT.md, пишет `6-lending/_out/pitch-generated.md` с шапкой-предупреждением «сгенерировано из `0-koncept/KONCEPT.md`; правь ИСТОЧНИК, не этот файл». `_out/` создаю при необходимости. `pitch-kurs.md` не трогаю.

**Проверка:** `python3 tools/check_docs.py` → exit 0; `python3 tools/render-pitch.py` → файл создан и непуст; затем — порча одного якоря → чекер должен упасть на новой проверке → откат. Верификатор (§4 захода) — отдельный read-only субагент на diff KONCEPT vs zip и на срыв чекера.


## ВОПРОСЫ — (заполняет Code)

**Рассинхрон имя-файла/`poryadok` vs canon-порядок — не чинил, только фиксирую (ожидаемо, уже описано в шапке `KONCEPT.md`).** Реальные `id`/`poryadok` в `istochnik/L*.md`: `L1-granicy.md`(poryadok1) `L2-optimizaciya.md`(poryadok2) `L3-slozhnost.md`(poryadok3) `L4-kriptografiya.md`(poryadok4) `L5-informaciya.md`(poryadok5) `L6-sluchaynost.md`(poryadok6) `L7-myshlenie.md`(poryadok7). Это СТАРАЯ нумерация (Оптимизация была ближе к началу). Заход дал явную таблицу ЛN↔id, которая совпадает с ярлыками карточек в самом `KONCEPT.md` §4 (Л1 Границы·Л2 Сложность·Л3 Информация·Л4 Криптография·Л5 Случайность·Л6 Оптимизация·Л7 Мышление) — использовал её, ИГНОРИРУЯ номер в имени файла и `poryadok`. Итог: якорь `{L:optimizaciya}` стоит на карточке «Л6», хотя физический файл называется `L2-optimizaciya.md` — это ожидаемо и корректно (id, не номер файла, это ключ синхрона), но подтверждаю явно, раз KONCEPT сам предупреждал о рассинхроне номеров. Переименование файлов/обновление `poryadok` — вне границ этого захода (явно запрещено трогать `poryadok`), и новый чекер это не требует: проверка №6.2 — просто уникальность `poryadok` в 1..7, а они и так уникальны независимо от порядка.

## ОТЧЁТ — (заполняет Code)

**Что сделал и зачем.**
1. **Бэкап.** `snapshots/pre-sync-pilot_2026-07-02.zip` (~9 МБ, вся папка кроме `snapshots/`) — до любых правок, единственная точка отката (git нет).
2. **(A) Якоря в `0-koncept/KONCEPT.md`.** К каждому из 7 заголовков-карточек лекций (`**ЛN. …**`, строки 44/47/50/53/56/59/62) добавлен машинный якорь `{L:<id>}` сразу после закрывающих `**`, перед любой хвостовой курсивной пометкой (`*(была ЛN)*` не тронута). Соответствие ЛN↔id — по явной таблице захода (совпадает с ярлыками самих карточек KONCEPT, см. `## ВОПРОСЫ`): Л1 granicy · Л2 slozhnost · Л3 informaciya · Л4 kriptografiya · Л5 sluchaynost · Л6 optimizaciya · Л7 myshlenie. Проза карточек не тронута — ниже подтверждено дифом и независимым верификатором.
3. **(B) Чекер id-целостности** — новая проверка №6 `check_id_sync()` в `tools/check_docs.py`: (1) множество `{L:id}` в `KONCEPT.md` == множество `id:` из frontmatter `istochnik/L*.md`, точное 7↔7; (2) `poryadok` во всех лекциях уникальны и покрывают 1..7 без пропусков; (3) каждый `{L:id}` резолвится в реально существующий файл лекции. Зарегистрирована в общем списке `CHECKS`, остальные 5 проверок не менялись (кроме перестановки: новая вставлена перед существующей секцией 5, чтобы не рвать нумерацию комментариев — сам список проверок не переупорядочен, добавлена последней записью).
4. **(C) Генератор питча `tools/render-pitch.py`** (новый, только stdlib). Читает `KONCEPT.md`: абзац «В одну фразу» (манифест) + 7 карточек по регэкспу заголовка+тела; пишет `6-lending/_out/pitch-generated.md` с шапкой «сгенерировано из `0-koncept/KONCEPT.md`; правь ИСТОЧНИК, не этот файл» + манифест + список из 7 лекций (заголовок + короткая затравка из «Ось: …» до первого « · »). `6-lending/pitch-kurs.md` не тронут (остаётся заготовкой) — подтверждено MD5-сверкой до/после.

**Как проверил.**
- `python3 tools/check_docs.py` → exit 0, все 6 проверок зелёные (включая новую «id-целостность KONCEPT ↔ лекции»).
- `python3 tools/render-pitch.py` → создал непустой `6-lending/_out/pitch-generated.md` (манифест + ровно 7 карточек; скрипт сам падает, если карточек не 7).
- Дифф `0-koncept/KONCEPT.md` против распакованного бэкапа (сам, до вызова верификатора) — ровно 7 однострочных изменений, каждое — только вставка токена `{L:id}`.
- Диффы `istochnik/_generator/build.py`, всех 7 `istochnik/L*.md`, `1-START-HERE/sistema-ocenki.md` против бэкапа — идентичны (границы не задеты).

**Что НЕ трогал (границы).** `istochnik/_generator/build.py` (рендер трансклюзии — заход 3); проза/структура `KONCEPT.md` (только якоря); `istochnik/L*.md` (только читал `id`/`poryadok`, файлы не менял, включая `L2-optimizaciya.md`); `poryadok` — нигде; `1-START-HERE/sistema-ocenki.md`, `resheniya.md`; `zhurnal/**`, `snapshots/**`; `6-lending/pitch-kurs.md`.

**Результат независимого верификатора (§4, отдельный свежий read-only субагент, метод — свой diff + свой контрольный срыв чекера, не полагался на мой вывод):**
- Дифф KONCEPT (свой распаковка + свой `diff`) — те же 7 строк, «прозы изменено: 0».
- `check_docs.py` сейчас: exit 0, все 6 проверок перечислены и зелёные.
- Контрольный тест: удалил `{L:granicy}` → `check_docs.py` упал (exit 1) ИМЕННО на «id-целостность KONCEPT ↔ лекции» → вернул якорь на место → чекер снова exit 0. **Чекер ловит рассинхрон: да.**

**Открытые вопросы** — см. `## ВОПРОСЫ` выше (рассинхрон номер-в-имени-файла/`poryadok` vs canon-порядок — уже известный факт, не блокер, ничего не чинил).

**Время и токены.** Инструмента `/cost` из-под агента у меня нет (это интерактивная команда CLI) — не могу вписать точную цифру; посмотри в интерфейсе Claude Code (`/cost` или статус-бар) и впиши сюда при необходимости. Из наблюдаемого: 1 фоновый read-only верификатор (§4, ~35к токенов, 14 тул-коллов, ~53 сек по его собственному отчёту) + основной проход (чтение 5 названных файлов + frontmatter 7 лекций, 7 точечных Edit-правок KONCEPT, один новый файл в check_docs.py, один новый скрипт render-pitch.py, несколько прогонов `check_docs.py`/`render-pitch.py`/`diff`) — в пределах одного захода, без рой-субагентов и без ретраев на рейт-лимитах.

**Критерий готовности — все 4 пункта выполнены:**
1. ✓ 7 якорей `{L:id}` в `KONCEPT.md`; diff vs бэкап = только вставки якорей.
2. ✓ `python3 tools/check_docs.py` → exit 0, включая новую проверку.
3. ✓ `python3 tools/render-pitch.py` → непустой `6-lending/_out/pitch-generated.md` (манифест + 7 лекций).
4. ✓ Границы (`build.py`, лекции, `poryadok`, `sistema-ocenki`) не тронуты.
