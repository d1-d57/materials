# СЕССИЯ 2026-06-27 — Настройка архитектуры памяти курса (Cowork)

> Живой конспект сессии (пишется по ходу, заморожен в конце). НЕ дом durable — осевшее поднято в дома (`resheniya` / `dnevnik` / `NAVIGATION` / `SVODKI`). Что осталось бы только здесь — не существует. Первый экземпляр новой структуры журнала — пилот на самих себе.

## Задача
Аудит папки курса + архитектурные правки под систему памяти: чтобы соображений можно было копить много и ничего не терять. Опора — канон большого проекта (спецмат).

## Диагноз (аудит)
- Память плоская: нет границы «живой черновик ↔ durable», нет журнала сессий → протухнет как DIARY в большом проекте.
- Нет входного контракта Cowork (среда / роли / дисциплина).
- Три списка файлов (README / NAVIGATION / dnevnik) → риск расхождения индексов.
- Нет плана производственных фаз (лекции → HTML → лендинг).

## Решения владельца (развилки)
- Память — средняя модель, упрощённо. · Арки — лёгкие папки. · Будущие папки — стабы + карта фаз сейчас.

## Сделано
- Новые: `COWORK.md`, `1-START-HERE/resheniya.md`, `zhurnal/{PRO-ETU-PAPKU,SVODKI}.md`, эта `SESSIYA.md`, стабы `4-lekcii` / `5-html` / `6-lending`.
- Перестроено: `NAVIGATION.md` (единственный индекс), `README.md` (ужат), `dnevnik-i-plan.md` (живой борд; решения → `resheniya`; фазы сборки Ф6–Ф8).
- Содержание идей не трогали.

## Осталось / открыто
- Прогнать новые доки через `russian-editor` (output-gate).
- Следующий содержательный шаг — Ф2 (гейты-и-баллы из `sloi-arhitektura`).
- При появлении кода — git-репо и граница с Code.

## Добавлено: полная арк-дисциплина (под ключ)
По запросу владельца — переключение с лёгких арок на полную систему большого проекта. Подцеплено из большого (read-only): `АРКА_DRAFT`, `архитектура-входа_решение`, `CLAUDE.md`. Сделано: `ARKA.md` (канон арок), `CLAUDE.md` (вход Code), `COWORK` → прокладка-роутер; эта папка приведена к шаблону арки (`NAVIGATOR` + `PLAN` + этот дневник + Code-канал). Решения: durable арки в дома НЕ переносим до закрытия; Code общается только через файл-заход в папке арки.

## Принят отчёт Code — перенос библиотеки (2026-06-27)
Отчёт пришёл в `kod_perenos-biblioteki.md`. Сверил гейты независимо (grep + tree): 6 файлов в `biblioteka/{kniga-donor,fakty}/`, `3-istochniki-kniga/` удалена, активных битых ссылок 0, верификатор Code (свежий субагент, sha256) зелёный. **Принято.** Code сам поймал 7-ю ссылку, которой не было в §3c (`4-lekcii/PRO-ETU-PAPKU` → `../biblioteka/`), и починил — ратифицировано. Чистку хвостов `source-pass` корректно не делал (вне код-захода) — остаётся редакторской работой.

## Стандартизация работы с Code
Правка владельца: короткий текст для Code должен встраивать полный адрес ВНУТРЬ (не папка+текст отдельно). Завёл проектный `RUKOVODSTVO-zahodami.md` — адаптация `РУКОВОДСТВО_заходами` большого проекта, skill-независимо: канал `kod_*.md`, структура захода (no-git → zip-бэкап), СТАНДАРТНЫЙ короткий текст с встроенным путём, приёмка отчёта. Ссылки в `ARKA §5/§8`, `COWORK`-роутере и `NAVIGATION` переведены со скилла на этот док.

## Закрытие арки (2026-06-27)
Навигатор арки доведён до ХАБА: блок «Процедуры — куда идти» (вход → `ARKA §3`, план → `§4`, хэндофф → `§6`, закрытие → `§7`, Code → `RUKOVODSTVO`) — пойнтеры, не процедуры; впечатываются при создании папки (`ARKA §2/§9`). Хвосты `source-pass` подрезаны (факты сохранены). Durable арки — в каноническом доме (сам канон); открытые хвосты — в `../../biblioteka/00-CHTO-GDE.md`. Сводка — `../SVODKI.md`. **Арка закрыта.** Папка готова к выносу в отдельный Cowork-проект.
