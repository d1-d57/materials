# ПЛАН арки «единый источник правды»

## ⭐ СЕЙЧАС
**Шаг 3 — влить полную Л4 lossless (Code-заход).** Генератор ПРИНЯТ (приёмка с.3: stdlib-генератор + seed + линтер 2 яруса + бэклинки + `data-source`; критерий перепроверен независимо). Теперь вычерпать ВСЕ входы Л4 в `istochnik/L4-kriptografiya.md` без потерь: пилот + `opisanie-kursa-v2/` + глубокий банк (`graf-pautina`, `dosye`, `harvest`, `almazy`, `bazovyy-pul`, `kotel §10`) + существующий HTML (`L4-v4.html`/`v3`). **Учесть разбор Л4 при влитии** (не вливать заведомо переделываемое: крючок «как сохранить секрет»+Энигма; RSA→Ферма→Эйлер; блок Эйлера; биткоин-финал; Logjam; Шеннон во введение). Заход по `RUKOVODSTVO`+`agentic-coding-session-brief`; karpathy; **тяжёлый верификатор полноты** (свежий субагент, чек-лист из всех входов, список потерь пуст). `status: polno` → линтер жёсткий (≥20 матблоков, квоты графа). Эталон — `L4-v4.html`.

**Визуал-доводка — сделана и принята (с.3).** ⭐ Дальше: **консолидация арки + закрытие** (Конец АРКИ: durable по домам · SVODKI · пометить закрытой), затем **новая контент-арка** — влитие Л4 lossless (с разбором) + раскатка на 7. Генератор доведён до логичного конца.

## Шаги
- [x] Консолидация арки «научпоп-черновик» (durable по домам + SVODKI + закрыта; верификатор GREEN)
- [x] Завести арку (NAVIGATOR / PLAN / SESSIYA) после go (кикофф + развилки уже решены — `resheniya.md` п.15)
- [x] **Шаг 1 — схема источника** v1 (черновик) → полный вход + интервью + ресёрч + аудит → **переписана v2** (`SHEMA-istochnika.md`: карта дроблена по типу · адресация стабильным id · слой целостности · привязка HTML↔текст · протокол правок · контракт генератора); **утверждена владельцем**
- [x] **Шаг 2 — генератор MD→HTML** (Code-заход `kod_generator.md`; stdlib-генератор + seed `istochnik/` + линтер 2 яруса + бэклинки + `data-source`) — **принято с.3**: критерий из 6 пунктов перепроверен независимо в песочнице; границы чисты
- [x] **Визуал-доводка генератора** (заход `kod_generator-visual.md`; чипы `.lref` / люди `.pers` / короткая подпись / наезды `clear`) — **принято с.3**; формулы-нотация (`$$`) и портрет-гейт (`ядро`/`подмигивание`) → контент-арка
- [ ] **Шаг 3 — влить полную Л4 lossless** (весь научпоп + математика + все связи графа + детали глубокого банка + связующие тексты; верификатор «ничего не потеряно»). _Учесть правки разбора Л4 при влитии — не вливать заведомо переделываемое._
- [ ] Проверить петлю: правлю MD → одной командой полный HTML → смотрю в HTML (`visual-design-review`)
- [ ] **Шаг 4 — раскатка на остальные 6 лекций**
- [ ] Параллельный трек: разбор-embedding (общие критерии in-place в ТЗ) + пересборка Л4 по разбору

## Развилки — РЕШЕНЫ (аудит сессии 2, утв. владельцем)
- **Карта:** ~~один `karta.md`~~ → **дроблена по типу** (`graf/lica/teorii/svyazujushchee`) — 4 разные схемы, изоляция правки, чистый парс. (Сознательно против прежнего «один файл».)
- **Место `istochnik/`:** **корень проекта** (durable-контент + сам deliverable; перенос из арки — лишний риск).
- **Целостность строгости:** **два яруса** — структурное жёстко, редакционные квоты по полю `status` (жёстко при `polno`).
- **Переиспользование:** ссылка + фраза-роль локально; дословный повтор — трансклюзия `{{вкл:id}}` (один дом).
- **Адресация:** стабильный id, номер = поле `poryadok` (показ). Поля-сноски — typed-блок `> поле:<тип>`.
- **Генератор:** свой тонкий Python (Pandoc baseline) — деталь Code-захода.
- **ТЗ-канон** — отдельно (решено, п.15); в источник не вливаем.
