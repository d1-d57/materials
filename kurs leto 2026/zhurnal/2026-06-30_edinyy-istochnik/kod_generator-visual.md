# Канал Claude Code — визуал-доводка генератора (один заход до конца)

> Для Claude Code: единственный файл-заход. Читай только его и названное ниже. План/вопросы/отчёт — в секции внизу. Автономно до конца; сохранять не блокируя (вопросы — в ## ВОПРОСЫ).
> МОДЕЛЬ: оркестратор Sonnet (`/model opusplan`). Ты ВИДИШЬ результат — **рендери страницы и смотри глазами**, это главный инструмент захода.

КОНТЕКСТ. Генератор `istochnik/_generator/build.py` (чистая stdlib) собирает HTML-страницу лекции из MD-источника и уже ПРИНЯТ (работает, критерий пройден). Но при сравнении вывода `istochnik/_out/L4-kriptografiya.html` с эталоном `L4-v4.html` владелец увидел визуальные расхождения. **CSS у обоих ИДЕНТИЧЕН** (Code снял стиль с эталона целиком) — значит расхождения в **РАЗМЕТКЕ, которую эмитит генератор**: тот же стиль на другой HTML → другой вид. Задача: небольшими правками **генератора / шаблона / CSS** довести вёрстку вывода до уровня эталона.

**ГЛАВНОЕ:** правки кладёшь в генератор/`_dizajn/`, НЕ в одну страницу, — чтобы они автоматически распространялись на все будущие лекции (в этом смысл инструмента). **КОНТЕНТ НЕ ТРОГАЕМ** — полноту текста вливаем отдельным шагом; здесь правим только то, как уже существующее РИСУЕТСЯ.

## 0. ПЕРВЫЙ ХОД
- Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026`. Папка не под git → СРАЗУ zip-бэкап: `zip -r snapshots/backup-visual-$(date +%Y%m%d-%H%M%S).zip . -x 'snapshots/*'`.
- Прочитать ТОЛЬКО: `istochnik/_generator/build.py`, `istochnik/_dizajn/stil.css`, `istochnik/_dizajn/shablon.html`, `istochnik/L4-kriptografiya.md`, и эталон `zhurnal/2026-06-28_nauchpop-chernovik/L4-v4.html` (как дизайн-референс). Остальной проект не изучать.
- **Отрендерить ОБЕ страницы** (свой вывод `istochnik/_out/L4-kriptografiya.html` и эталон `L4-v4.html`) в PNG и СМОТРЕТЬ глазами (playwright / chromium-headless / wkhtmltoimage — поставь, если нет; ты в своём окружении, не ограничен). Сверяй «до» и «после» каждой правки. Если рендер у тебя совсем недоступен — поставь что можешь; в крайнем случае правь по разметке и ЯВНО отметь в отчёте, что финальную визуальную сверку делает владелец.
- Изложить ПЛАН в `## ПЛАН` перед правками.

## 1. ДИСЦИПЛИНА (karpathy-guidelines)
Минимум правок, каждая → пересборка + рендер + взгляд. Якорные строковые замены — abort при совпадениях ≠1. Не ломать принятый функционал (линтер 2 яруса / бэклинки / `data-source` / self-contained офлайн). Сохранять по умолчанию; неоднозначное — в `## ВОПРОСЫ`. Обобщай (правка для всех лекций), не хардкодь под Л4.

## 2. ГРАНИЦЫ (НЕ трогать)
- НЕ влитие контента, НЕ разбор Л4, НЕ менять текст seed по смыслу (объём текста — отдельный шаг; здесь не наша забота).
- НЕ трогать durable-доки, ТЗ (`1-START-HERE/`), банки, арк-доки. Эталон `L4-v4.html` — только чтение.
- Правки — в `build.py` / `_dizajn/` (генерализованно). Не плодить новых доков.

## 3. ЗАДАЧА — диагностированные расхождения (стартовый список; добавляй, что глаз увидит хуже эталона)
1. **Чипы-ссылки на лекции.** Сейчас ссылки на лекции несут полные названия (`→Случайность`, `→Сложность`) в той же крошечной «таблетке», что была под короткое `Л6` → тесно и коряво. Появление названий — это правильно (так и нужно); оформи лекции-чипам аккуратный, чуть просторнее стиль: название читается, не ломается посреди фразы (`white-space:nowrap` или разумный перенос), есть воздух. По желанию — лёгкий значок-стрелка как в эталоне.
2. **Люди ≠ кнопки.** `→[turing]` / `→[shannon]` сейчас рисуются той же громкой «таблеткой», что и лекции. В эталоне люди идут **портретом сбоку** (`figure.port`) + имя в тексте, НЕ кнопкой. Сделай: первое упоминание лица → портрет; в прозе — спокойное имя (обычный/слегка выделенный текст), без loud-pill. Убери дублирующую таблетку-персону.
3. **Подпись портрета.** Сейчас в `figcaption` валится весь био (длинно, грузно). Сделай коротко: даты + одна роль-фраза (как в эталоне: «1916–2001 · ключ не короче сообщения»). Полный био остаётся в `karta/lica.md`; в подпись не льём.
4. **Наезды текста.** Вероятная причина — второй плавающий портрет (монограмма «Тьюринг») на короткой странице: врезки сталкиваются с заголовками/абзацами. Поправь обтекание / `clear` / расстановку врезок, чтобы ничего не наезжало. Подтверди рендером.
5. **Формулы в центр.** Эталон выносит ~6 формул в центрированный блок `.eq`; вывод — 3, остальные остались в строке (`.m`). Чаще выноси display-формулы в центр `.eq`: выносные `$$…$$` и формулу раздела `{теорема-заголовок}` / ключевые именованные. (Пояснительные слова ВОКРУГ формул — это контент, придёт на шаге влития; здесь только вынос в центр.)

Если глаз увидит ещё расхождения с эталоном (отступы, типографика, цвета, ритм) — мелкие поправь, крупные/неоднозначные → `## ВОПРОСЫ`. Держи изменения небольшими: цель — «заметно лучше и ближе к эталону», не редизайн.

### КРИТЕРИЙ ГОТОВНОСТИ (проверяемый — может провалиться)
1. `python3 istochnik/_generator/build.py` зелёный (структурный линтер pass, сборка ок); вывод self-contained (`grep -nE 'https?://|src="//"'` по выходу = только `data:`/якоря).
2. Рендеры «до / после» вывода + рендер эталона (приложи/опиши): видно, что лекции-чипы читаемы, люди — портретом, а не кнопкой; подпись портрета короткая; наездов нет; display-формул в центре стало ≈ как в эталоне.
3. Правки генерализованы: на stub-лекции или второй сущности стиль тот же (правил генератор/CSS, не одну страницу).
4. Принятый функционал цел: `--backlinks turing` работает, `data-source` на блоках на месте, линтер по-прежнему валит на порче (быстрый спот-чек: временно битый `→[id]` → `exit≠0` → откат).

## 4. ВЕРИФИКАТОР
Заход правит ВЁРСТКУ, ничего durable не двигает/не теряет → тяжёлый верификатор не нужен. Гейт = рендер-сравнение с эталоном (глазами) + критерий выше.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что менял в генераторе/CSS и зачем / рендеры «до-после» (опиши, что изменилось визуально) / что вынес в `## ВОПРОСЫ` / что НЕ трогал / **время прогона + потраченные токены** (`/cost`).

## ПЛАН — (заполняет Code)

Бэкап сделан (`snapshots/backup-visual-20260630-140335.zip`). Прочитал только названное + `karta/lica.md`/`teorii.md`/`graf.md` (данные, которые потребляет генератор для item 1–3). Рендерил обе страницы Chrome-headless, смотрел глазами (см. ОТЧЁТ).

Диагноз подтверждён рендером (вывод vs эталон):
- **item 1** — лекции-чипы: сейчас полные названия (`→ Случайность/Сложность/...`) в той же крошечной таблетке `.ln`, что и под `Л6` → тесно. Чинить — отдельный класс лекции-чипа с воздухом, nowrap, лёгкой стрелкой.
- **item 2** — лица: `→[shannon]`/`→[turing]` рисуются громкой таблеткой `.ln` (как у лекций). В эталоне имя в прозе — спокойный жирноватый текст, без pill. Чинить — отдельный класс `.pers` (серив, 600), pill убрать; портрет на первом упоминании остаётся.
- **item 3** — подпись портрета: льётся весь `био:` из `lica.md` (многострочно). Эталон: даты + одна роль-фраза. Чинить — короткая подпись `<даты> · <роль>` из полей `био` (1-й сегмент до `;`) и `роль`. Полный `био` в `lica.md` не трогаю.
- **item 4** — наезды: в §9 два плавающих портрета (Шеннон-фото + Тьюринг-монограмма) на короткой секции; флоат вытекает за `.techblock` и сталкивается с §10. Чинить — `clear:left` на `h3` (следующий заголовок встаёт ниже плавающих врезок). Портреты НЕ убираю (правило «первое упоминание → портрет»), чиню только обтекание.
- **item 5** — формулы в центр: генератор уже выносит ВСЕ `$$…$$` в `.eq` (это §2/§4/§9 = 3 шт.). Лишние 3 `.eq` эталона — это §8 (мини-RSA + ошибка), где в MD-ИСТОЧНИКЕ формулы записаны как строчные `$…$` вперемешку с прозой; чтобы их центрировать, надо править ИСТОЧНИК (`$…$`→`$$…$$`) — это контент-шаг, он за границами захода («КОНТЕНТ НЕ ТРОГАЕМ»). Угадывающую авто-центровку строчных формул не ввожу (хрупко, может центрировать формулы внутри фраз). → в ВОПРОСЫ.

Правки: `build.py` (render_ref для lecture/person, portrait_html подпись, парс `роль`), `_dizajn/stil.css` (`.lref`, `.pers`, `h3 clear`). Каждая → пересборка + рендер + взгляд.

## ВОПРОСЫ — (заполняет Code)

1. **Формулы в центр (item 5) упёрлись в контент, не в вёрстку.** Генератор уже выносит в `.eq` ВСЕ выносные `$$…$$` — это §2/§4/§9 = 3 шт. Недостающие 3 `.eq` эталона живут в §8 (мини-RSA построчно + формула ошибки), но в MD-источнике они записаны как **строчные** `$…$` вперемешку с русской прозой («$p=3,\ q=11 \to n=33$, $\varphi=20$…»). Центрировать их без правки источника нельзя, а источник — контент (заход прямо запрещает трогать). Что выбрать: **(а)** на контент-шаге переписать эти формулы в источнике как `$$…$$` (тогда центрируются сами), или **(б)** ввести в генератор авто-центровку «лид-формулы матблока»? Я склоняюсь к (а): (б) — эвристика, легко начнёт центрировать формулы, которые автор задумал строчными внутри фразы.
2. **Подпись портрета — роль-фраза из данных vs лекционная.** Сделал короткую подпись `<даты> · <роль>` из полей `lica.md` → для Шеннона выходит «1916–2001 · информация/секретность». В эталоне стояла «1916–2001 · ключ не короче сообщения» — это фраза **под конкретную лекцию** (тема §9), из данных карточки не выводится. Оставляем роль-фразу из `lica.md` (генерализуемо, едина по курсу), или завести отдельное поле «подпись-на-лекции» (в `lica.md` per-появление или в секции источника)?
3. **Два портрета в §9 vs один в эталоне.** Правило «первое упоминание лица → портрет» даёт портрет и Тьюрингу (монограмма «Т»), хотя в `lica.md` его появление в §9 помечено `(подмигивание)`, а Шеннона — `(ядро техблока)`. Наезды я убрал (clear), но если нужна именно эталонная плотность — можно давать портрет только «ядро»-появлениям, а «подмигивание» оставлять просто именем. Ввести это правило по аннотации в `появления:[…]`? (Сейчас НЕ ввёл — это меняет поведение по всем лекциям, нужно ваше «да».)

## ОТЧЁТ — (заполняет Code)

**Что и зачем менял** (всё — в генераторе/CSS, генерализуемо на все лекции; контент и одну страницу не трогал):

- **`build.py` · `render_ref`, лекции (item 1).** Лекция-ссылка теперь эмитит класс `.lref` вместо общей таблетки `.ln`. Причина: `.ln` был рассчитан на короткое `Л6`, а сейчас в чип попадает полное название («→ Случайность») — было тесно и коряво.
- **`build.py` · `render_ref`, лица (item 2).** Лицо в прозе теперь спокойное имя `<span class="pers">Имя</span>` вместо громкой таблетки `.ln`. Портрет на первом упоминании сохранён (логика `pending_portraits` нетронута).
- **`build.py` · `portrait_html` + парс `роль` (item 3).** Подпись портрета — короткая `<даты> · <роль>` (даты = 1-й сегмент `био:` до `;`, роль = новое разобранное поле `роль:`), вместо вливания всего `био:`. Полный `био:` в `lica.md` не тронут.
- **`_dizajn/stil.css`.** Добавлены `.lref` (просторный чип лекции: белый фон, акцентный текст, рамка, `white-space:nowrap`, лёгкая стрелка «→», воздух в паддинге) и `.pers` (серив, `font-weight:600` — выделено, но не кнопка). В `h3` добавлен `clear:left` (item 4): следующий заголовок встаёт ниже плавающих портретов, флоат больше не вытекает за `.techblock` в §10.

**Рендеры «до → после» (Chrome headless, 1120px, смотрел глазами):**
- Лекция-чипы: были крошечные тесные пилюли с полным названием → стали читаемые просторные чипы с стрелкой, не ломаются посреди фразы (§1 «→ Случайность», §4 «→ Сложность», §7 ряд из трёх).
- Лица: были громкие таблетки `→[shannon]`/`→[turing]` → стали спокойные жирноватые имена в прозе («Шеннон», «Тьюринг»).
- Подпись портрета: была многострочная свалка всего био → стала короткая строка «1916–2001 · информация/секретность».
- Наезды §9→§10: два плавающих портрета вытекали на домашку → после `clear:left` §10 «Домашка» и список встают чисто ниже портретов, пересечений нет.
- Формулы: 3 центрированных `.eq` (все `$$`), как и было корректно; разрыв с 6 у эталона — контентный (см. ВОПРОС 1).

**Критерий готовности:**
1. `python3 istochnik/_generator/build.py` — зелёный (структурный линтер pass, 5 редакц. предупреждений как и раньше; сборка ✓ 78 КБ). Self-contained: `grep -nE 'https?://|src="//'` по выходу — пусто (только `data:`-картинки и якоря). ✓
2. Рендеры до/после/эталон сняты и просмотрены (выше). ✓
3. Генерализация: правил генератор+CSS, не страницу. На странице это видно на нескольких сущностях сразу — 12 лекция-чипов `.lref`, 2 лица `.pers` (Шеннон и Тьюринг — стиль одинаков), 0 старых `.ln`-пилюль. ✓
4. Принятый функционал цел: `--backlinks turing` → находит 1 место ✓; `data-source=` в выходе 29 шт. ✓; спот-чек линтера — временно битый `→[shannonXX]` даёт `exit=1` с сообщением «не резолвится», после отката `exit=0` ✓; MD-источник побайтово не изменён (`diff -q` чисто) ✓.

**Что НЕ трогал:** контент/текст seed, объём текста, разбор Л4, durable-доки/ТЗ/банки, эталон `L4-v4.html` (только чтение), `karta/lica.md` (полные био на месте), логику линтера/бэклинков/трансклюзий, шаблон `shablon.html`.

**Бэкап:** `snapshots/backup-visual-20260630-140335.zip` (до правок).

**Время/токены:** заход выполнен за один автономный прогон (~неск. минут wall-clock на рендеры Chrome headless). Точные токены — команда `/cost` интерактивная (терминал Claude Code), из этого harness не вызывается; цифру может снять владелец в своей сессии.
