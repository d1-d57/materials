# Канал Claude Code — генератор источник→HTML (один заход до конца)

> Для Claude Code: это твой ЕДИНСТВЕННЫЙ файл-заход. Читай только его и файлы, названные в §0 поимённо; остальной проект НЕ изучай. Прикладывать ничего не нужно — весь контекст внутри. Часы работы — норма; метрика — КАЧЕСТВО.
> РЕЖИМ: автономно до конца; вкусовое/рискованное/неоднозначное — пиши в `## ВОПРОСЫ` внизу и иди дальше, сохранение НЕ блокируй. План/вопросы/отчёт — в секции внизу этого файла.
> МОДЕЛЬ/БЮДЖЕТ: оркестратор — Sonnet (паттерн `/model opusplan`: план на Opus, исполнение на Sonnet); механическую транскрипцию и снятие CSS — дешёвым субагентам; тяжёлое суждение (архитектура генератора) — сильной.

КОНТЕКСТ. Проект — платный онлайн-курс по научпоп-математике. Цель арки: свести ВСЮ правду о курсе в один MD-источник (папка `istochnik/`), из которого **программно** генерится офлайн-HTML в дизайне v3/v4 — «правлю MD, смотрю HTML, без расхождений». Схема источника спроектирована и утверждена владельцем: **`SHEMA-istochnika.md` v2** — это контракт, его исполняешь. Сейчас `istochnik/` ещё НЕ существует. Прошлый этап дал готовую пилотную Л4 «Криптография» (карта разделов в `L4-pilot-v4.md`) и её свёрстанную версию `L4-v4.html` — она будет ЭТАЛОНОМ вывода.

ЦЕЛЬ захода (шаг 2 арки — «генератор»): поставить пайплайн source→HTML на ногах на реальной Л4 и доказать его на эталоне. Три части одним вертикальным срезом: **(А)** создать минимальный РЕАЛЬНЫЙ seed `istochnik/` из пилотной Л4; **(Б)** написать генератор MD→HTML; **(В)** слой целостности (линтер двух ярусов + бэклинк-индекс + привязка `data-source`). Приёмка — по `## ОТЧЁТ`, поэтому отчёт исчерпывающий.

---

## 0. ПЕРВЫЙ ХОД
- Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026`. Папка **НЕ под git** — единственная точка отката это снапшот. СРАЗУ, первым действием: `zip -r snapshots/backup-generator-$(date +%Y%m%d-%H%M%S).zip . -x 'snapshots/*'` (в `snapshots/`, не в Downloads; исключить сам snapshots, чтобы не зациклить).
- Прочитать ТОЛЬКО эти файлы (поимённо; остальное не открывать):
  - `zhurnal/2026-06-30_edinyy-istochnik/SHEMA-istochnika.md` — **главный контракт**. Существенны: §2 раскладка папки, §3 адресация id, §4 формат файла-лекции, §5 формат `karta/`, §6 правило полноты, §7 слой целостности (линтер два яруса), §9 контракт генератора. ВЕРБАТИМ-примеры из §5 и §11 — твой материал для seed.
  - `zhurnal/2026-06-28_nauchpop-chernovik/L4-pilot-v4.md` — контент для seed Л4 (карта разделов §1–§10 со всеми матблоками и домашкой).
  - `zhurnal/2026-06-28_nauchpop-chernovik/L4-v4.html` — **ЭТАЛОН** дизайна и вывода: снять `<style>`/CSS, набор классов-компонентов, base64-портрет; в конце визуально/структурно сверять свой Л4-HTML с ним. (Подтверждено: содержит классы `.claimbox/.eq/.cue/aside.note/figure.port/.ln/.h3tag/.techblock`, `<style>`, base64-jpeg, формулы БЕЗ MathJax.)
  - `zhurnal/2026-06-28_nauchpop-chernovik/chernovik-kursa-v3.html` — НЕ обязателен; открой ТОЛЬКО если в `L4-v4.html` не хватает какого-то класса/компонента (это 7-лекционный курс, 1.1 МБ — не вычитывай целиком, точечный grep).
- Изложить ПЛАН в `## ПЛАН` ПЕРЕД действиями (что в каком порядке, где риски).

## 1. ДИСЦИПЛИНА (karpathy-guidelines)
Минимум кода, детерминизм, никаких спекулятивных абстракций «на вырост» — пиши ровно то, что нужно для критерия. Меняешь только названное. Якорные строковые замены — abort при числе совпадений ≠ 1. Сохранять по умолчанию, вопросы — в `## ВОПРОСЫ`. **Оспаривай предпосылку:** если по живым файлам что-то не так, как сказано в заходе, — стоп, напиши в отчёт с доказательством, не строй на ложном.

## 2. ГРАНИЦЫ (НЕ трогать)
- НЕ править/двигать/удалять durable-доки, ТЗ (`1-START-HERE/`), банки контента, существующие HTML и арк-доки. `L4-v4.html` / `chernovik-kursa-v3.html` — **только чтение** (источник дизайна).
- **НЕ делать полное влитие Л4** из глубоких банков и **НЕ применять разбор Л4** (переупорядочивание RSA→Ферма→Эйлер, Энигма-крючок, биткоин-финал и пр.) — это СЛЕДУЮЩИЙ заход (шаг 3, lossless + полнотный верификатор). Seed Л4 здесь = **верная транскрипция пилота, провизорная** (доказывает машину, не финальный контент).
- НЕ тащить внешние сетевые зависимости в рантайм HTML — итог самодостаточный, открывается офлайн.
- `istochnik/` кладёшь в **КОРЕНЬ** проекта (durable-контент и сам deliverable).
- Никаких датированных копий рабочей папки; работаешь в одной папке, версии — снапшот + коммиты по блокам.

## 3. ЗАДАЧА

### (А) Seed `istochnik/` — раскладка строго по `SHEMA §2`
**`istochnik/L4-kriptografiya.md`** — транскрипция `L4-pilot-v4.md` в формат `SHEMA §4`. Перенос механический по правилам ниже — НЕ интерпретируй и НЕ обогащай сверх пилота:
- **YAML-шапка** (§4.1): `id: kriptografiya` · `poryadok: 4` · `title: Криптография` · `role: ядро` · `theory: [teoriya-chisel, rand-alg, informaciya-t]` · `cel: Как сохранить секрет` · `teorema-zagolovok: Малая теорема Ферма` · `budget: { math_blokov: 20, vsego_blokov: 38 }` · `status: chernovik` (важно: chernovik — линтер печатает недобор, но НЕ валит; см. критерий).
- **Заголовки разделов:** `**§N · [ТИП] Назв.**` → `## §N · ТИП · Назв.`. Маппинг типов: `[КРЮЧОК]`→`КРЮЧОК`, `[МАТЕМАТИКА·руками]`→`МАТЕМАТИКА`, `[РАССКАЗ]`→`РАССКАЗ`, `[ТЕХБЛОК]`→`ТЕХБЛОК`, `[ЧЁРНЫЙ ЯЩИК]`→`ЧЁРНЫЙ-ЯЩИК`. Флаги: §2 Ферма → `{теорема-заголовок}`; §6 ZKP → `{в-парах}`.
- **4 поля матблока** (§4.2): `*Утверждение / пример:*`→`**[утверждение]**`, `*Логика:*`→`**[логика]**`, `*Подробность:*`→`**[подробность]**`, `*Чёрный ящик:*`→`**[чёрный-ящик]**` (каждое — абзац-тег). Сохрани все 4 у каждого матблока.
- **Ссылки на лекции — фиксированный мап Л→id** (из `SHEMA §2`; пилот в нумерации v2, поэтому ложится чисто — в этом и смысл адресации id): Л1=`granicy`, Л2=`optimizaciya`, Л3=`slozhnost`, Л4=`kriptografiya`, Л5=`informaciya`, Л6=`sluchaynost`, Л7=`myshlenie`. Любую `→[Л6: …]`/`→[Л3, Л1]`/`→[Л5]` замени на чип-ссылку `→[sluchaynost]` / `→[slozhnost] →[granicy]` / `→[informaciya]`. Описательные `→[мотив «спецслужба уже умела»]`, `→[Принстон кочует: …]` — это НЕ id-ссылки: оставь смысл прозой без `→[]` (или, если по смыслу это сквозная нить, — полем-сноской `> поле:нить`; неоднозначно → в `## ВОПРОСЫ`, выбери минимальный вариант).
- **Лица:** Тьюринг → `→[turing]`, Шеннон → `→[shannon]` (их карточки кладёшь в `karta/lica.md`). Ривест/Шамир/Адлеман, Эллис/Кокс, Хенингер/Ленстра, Гарднер — Л4-локальные: оставь прозой, без `{id}` и без карты.
- **Поля-сноски** (§4.3) typed-блоком `> поле:тип …`: сквозная рифма из шапки пилота «случайность как доказательство» → `> поле:нить →[N:sluchaynost-instrument] случайность как доказательство`; мост «факторизация НЕ NP-полна (NP∩co-NP); если P=NP — RSA падает →[granicy]» (из §5) → `> поле:честно …`; крючок §1 (роторы Энигмы / визуал) → `> поле:cue-визуал …`; §6 раунд ZKP в парах → `> поле:cue-интерактив …`.
- **Формулы:** инлайн `$…$`, выносные `$$…$$` (пример: `$a^{p-1}\equiv 1 \pmod p$`, `$H(K)\ge H(M)$`, `$c=m^e \bmod n$`). Рендер — см. (Б).
- **Домашка:** §10 → `## §10 · ДОМАШКА`, список из 8 задач пилота, `*`-звёздочки сохранить.

**6 stub-лекций** `L1-granicy.md`, `L2-optimizaciya.md`, `L3-slozhnost.md`, `L5-informaciya.md`, `L6-sluchaynost.md`, `L7-myshlenie.md` — только YAML-шапка (`id`, `poryadok`, `title`, `role`, `status: skelet`) + одна строка-заглушка прозой. Нужны, чтобы `→[L:id]` и рёбра графа резолвились. Данные: L1 `granicy`/1/Границы, L2 `optimizaciya`/2/Оптимизация, L3 `slozhnost`/3/Сложность, L5 `informaciya`/5/Информация, L6 `sluchaynost`/6/Случайность, L7 `myshlenie`/7/Мышление (названия рабочие).

**`istochnik/karta/`** (4 файла, формат `SHEMA §5`) — инстанцируй **ВЕРБАТИМ примеры из `SHEMA §5`**: `graf.md` (нити `{predel}`, `{os-vremeni}` + рёбра), `lica.md` (`{turing}`, `{shannon}` с именами фото), `teorii.md` (`{teoriya-chisel}`, `{sluchaynost-t}`), `svyazujushchee.md` (`{manifest}`, `{timeline}`, `{karta-idey}`, `{gde-kogda}`, `{kazhdyy-den}`). Это seed карты — он ссылается на id шести stub-лекций + `kriptografiya`, поэтому всё резолвится.

**`istochnik/_dizajn/`** (машинерия, не правда курса): `stil.css` — `<style>` из `L4-v4.html`; `shablon.html` — HTML-каркас эталона (head, обёртки, порядок компонентов) как Jinja2-шаблон с плейсхолдерами; `foto/` — вынуть base64-портреты (turing/shannon). Если в `L4-v4.html` фото только одно — вынь его, остальные оставь плейсхолдером-рамкой (как монограмма Хэмминга), НЕ выдумывай портреты.

**`istochnik/README.md`** (как регенерить: одна команда + «правь источник, не HTML») и **`istochnik/PROTOKOL.md`** (скопируй таблицу «где что править» из `SHEMA §8`).

### (Б) Генератор — тонкий Python, одна команда
`python istochnik/_generator/build.py` → один самодостаточный HTML (для seed достаточно страницы Л4: `istochnik/_out/L4-kriptografiya.html`; общий индекс — по желанию, не требуется). Стек: `markdown-it-py` + `mdit-py-plugins` (контейнеры под typed-блоки) + Jinja2 (шаблон из `_dizajn/shablon.html`).
- **Парсит:** YAML-шапки; разделы `## §N · ТИП · …{флаги}`; 4 поля матблока по тегам `**[…]**`; поля-сноски `> поле:тип`; домашку; `karta/*.md` по `|`/`-`-строкам.
- **Резолвит двупроходно** (сначала собрать ВСЕ объявленные id, потом рендер): `→[id-лекции]`/`→[L:id]` → чип `.ln` (подпись = `title` лекции, НЕ номер); `→[id-лица]` → портрет `figure.port` у первого упоминания + чип (данные из `lica.md`); `→[T:id]`/`→[N:id]` → стиль нити; раздел `{теорема-заголовок}` → его `[утверждение]` в `.claimbox`; матполя → `.techblock`/`.eq`; `graf.md` → SVG-инфографика (дуги/«метро»); `svyazujushchee.md` → SVG; `{{вкл:id}}` → инлайн канонического блока (если встретится).
- **Формулы:** эталон рендерит без MathJax (plain styled HTML/unicode в `.eq`). Сделай детерминированно и офлайн — РЕШИ сам: лёгкий маппинг простых выражений в HTML/unicode под эталон, ЛИБО встроенный KaTeX (если берёшь — шрифты/CSS заинлайнить, остаться self-contained). По умолчанию — соответствие эталону при минимуме зависимостей. Решение и причину — в `## ОТЧЁТ`.
- **Привязка `data-source`:** на каждый сгенерённый блок — атрибут `data-source="L4-kriptografiya.md §N"` + HTML-коммент перед блоком, однозначно ведущий к MD-блоку (паттерн md-redline — чтобы правку из HTML можно было вернуть в MD). 
- **Дизайн = эталон:** классы `.claimbox/.eq/.cue/aside.note/figure.port/.ln/.h3tag/.techblock`, CSS из `_dizajn/stil.css`. Итог визуально/структурно ≈ `L4-v4.html`; связей (чипов/портретов) может быть БОЛЬШЕ — это нормально (новый формат вытаскивает граф и лица).

### (В) Слой целостности (`SHEMA §7`) — гоняется в сборке
- **Бэклинк-индекс:** инверсия всех `→[id]` → `задето[id] = [(файл, §, цитата)]`; доступно командой (напр. `build.py --backlinks turing`).
- **Линтер-падающий-тест, ДВА ЯРУСА** (строгость по полю `status`):
  - **Структурные** (жёсткий гейт ВСЕГДА, exit≠0 — это баги): каждый `→[id]` резолвится в объявленный id · нет дубля id · матблок несёт ВСЕ 4 поля · заголовок раздела валиден · нет битой `{{вкл:id}}`.
  - **Редакционные** (мягко; жёстко ТОЛЬКО при `status: polno`): ≥~20 матблоков · квоты графа (`SHEMA §7`) · лицо в 2–5 лекциях · домашка 5–10. При `chernovik` печатать «L4: N/20 матблоков» и т.п., но сборку НЕ валить.
- Счётчики/квоты пересчитываются из живых файлов — **ничего не хранить и не дублировать.**

### КРИТЕРИЙ ГОТОВНОСТИ (проверяемый — каждый пункт может ПРОВАЛИТЬСЯ; пройди по списку в отчёте)
1. `python istochnik/_generator/build.py` одной командой → один self-contained HTML. Офлайн-чек: в выходе нет внешних сетевых ссылок (`grep -nE 'https?://|src="//"' istochnik/_out/*.html` даёт только `data:`/якоря/безопасное) — иначе провал.
2. Правлю одно поле в `L4-kriptografiya.md` (напр. `[утверждение]` Ферма) → пересборка → HTML меняется РОВНО в том блоке, и его `data-source` указывает на `L4-kriptografiya.md §…`.
3. Линтер ВАЛИТ (exit≠0) на каждом из: временно вставленный битый `→[nesushchestvuyushchiy]`; дубль id; матблок с 3 полями. (Внеси порчу временно, покажи падение, откати.)
4. При `status: chernovik` и <20 матблоков сборка НЕ валится — печатает «L4: N/20 матблоков».
5. `build.py --backlinks turing` выдаёт ВСЕ места `→[turing]` (файл, §).
6. Глазами: итоговый `L4-kriptografiya.html` структурно/визуально соответствует `L4-v4.html` (те же компоненты, связей не меньше). В отчёт — короткое описание сверки (что совпало, что отличается и почему).

## 4. ВЕРИФИКАТОР
Заход **созидающий** — `istochnik/` создаётся, существующие файлы только читаются; ничего не двигается/не удаляется/не сжимается. Поэтому тяжёлый lossless-верификатор от снапшота здесь НЕ нужен; гейт — КРИТЕРИЙ выше (он может провалиться) + офлайн-self-contained-чек (п.1). Если по ходу обнаружишь, что задача требует что-то ДВИГАТЬ/УДАЛЯТЬ (не должна) — стоп, в `## ВОПРОСЫ`. (Полнотный верификатор «ничего не потеряно» — на шаге влития Л4, не здесь.)

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что увидел / что сделал + ЗАЧЕМ / как проверил (по КАЖДОМУ пункту критерия: pass/fail) / решение по рендеру формул и ПОЧЕМУ / какие описательные `→[]` снял прозой vs полем / что НЕ трогал / **время прогона + потраченные токены** (`/cost`). Открытые развилки и всё вынесенное на ревью → `## ВОПРОСЫ`. Durable новых доков не плодить — артефакты в `istochnik/`.

---

## ПЛАН — (Code)

**Разведка (сделано):** бэкап `snapshots/backup-generator-20260630-041410.zip` (исключён сам `snapshots/`). Прочитаны 4 названных файла + CLAUDE.md. Снят `<style>` эталона (2 блока, 15.5 КБ), разобрана разметка тела `L4-v4.html`. Классы-компоненты подтверждены: `.masthead .taglegend .kicker .lec-num .role .budget .h3tag(.t-hook/t-story/t-math/t-tech/t-box/t-hw) .claimbox .eq .m aside.note(.thread/.honest) .ln figure.port(.pi/.pn) .actdiv .techblock(.plaque) .cue .hw(.star/.hint)`. Фото в эталоне ОДНО — `--p-shannon` (Тьюринг → монограмма-плейсхолдер). `data-source` в эталоне НЕТ (его добавляю я — это новое). Формулы в эталоне — plain unicode/HTML, без MathJax. `istochnik/` отсутствует.

**Развилка по стеку (решена, см. ВОПРОСЫ #1):** в окружении НЕТ ни `markdown-it-py`, ни `mdit-py-plugins`, ни `Jinja2`, ни `PyYAML` (Python 3.9.6). Формат источника — bespoke (строчно-ориентированный: `## §N · ТИП`, теги `**[поле]**`, `> поле:тип`, `|`-таблицы), не CommonMark — markdown-it дал бы только рендер инлайна (bold/em), что я делаю ~20 строками детерминированного regex. Решение: **генератор на чистой stdlib** (`re/html/base64/pathlib/argparse` + мини-парсер YAML-подмножества + инлайн-markdown + LaTeX→unicode). Так владелец запускает `python3 build.py` без pip/сети — ровно под петлю «правлю MD, смотрю HTML». Минимум зависимостей и детерминизм (karpathy). Отклонение от названного стека вынесено в ВОПРОСЫ.

**Порядок работ (по блокам, снапшот уже снят):**
1. **(А) Seed `istochnik/`** — раскладка по `SHEMA §2`, в КОРНЕ проекта:
   - `L4-kriptografiya.md` — транскрипция пилота §1–§10 по правилам §3(А): YAML-шапка (status: chernovik), заголовки `## §N · ТИП · Назв {флаги}`, 4 поля матблока `**[…]**`, поля-сноски `> поле:тип`, ссылки Л→id, лица turing/shannon, формулы `$…$`/`$$…$$` в LaTeX, домашка 8 задач.
   - 6 stub-лекций (`L1-granicy`…`L7-myshlenie`, кроме L4) — YAML + строка-заглушка.
   - `karta/` (graf/lica/teorii/svyazujushchee) — ВЕРБАТИМ примеры `SHEMA §5`.
   - `_dizajn/` — `stil.css` (2 `<style>` эталона), `shablon.html` (каркас как шаблон с плейсхолдерами), `foto/shannon.jpg` (раскодирован из base64; Тьюринг — монограмма).
   - `README.md` (регенерация) + `PROTOKOL.md` (таблица `SHEMA §8`).
2. **(Б) Генератор** `istochnik/_generator/build.py` → `istochnik/_out/L4-kriptografiya.html`, self-contained, дизайн = эталон, `data-source` на каждый блок, LaTeX→unicode формулы.
3. **(В) Целостность** в той же сборке: двупроходный резолв id, линтер ДВУХ ярусов (структурный — жёсткий гейт; редакционный — мягкий при chernovik), бэклинк-индекс (`--backlinks turing`).
4. **Прогон КРИТЕРИЯ** (6 пунктов, каждый pass/fail) + офлайн-self-contained-чек. Отчёт.

**Риски:** (а) `{shennon}` в `SHEMA §5` vs `→[shannon]` в §3 — противоречие, нормализую к `shannon`, иначе линтер падает (ВОПРОСЫ #2). (б) Описательный `→[Принстон: Л1,Л2,Л6]` §7 — неоднозначно проза vs нить-поле; выбран минимальный нить-вариант (ВОПРОСЫ #3). (в) SVG-инфографики из graf/svyazujushchee — в L4-эталоне ИХ НЕТ; генерить = расходиться с эталоном → отложено как способность курс-индекса (ВОПРОСЫ #4). (г) §8 в пилоте не разложен по 4 канонформулам — собираю 4 поля из живого текста §8 без выдумки.

## ВОПРОСЫ — (Code)

Всё сохранено, сборка зелёная — ни один вопрос не блокировал работу. Это развилки на ревью владельцу.

1. **Стек генератора: stdlib вместо markdown-it-py + mdit-py-plugins + Jinja2 (отклонение от §3Б).** В окружении (Python 3.9.6) НЕ установлено ни одной из названных библиотек. Формат источника bespoke (не CommonMark), markdown-it дал бы только рендер инлайна (`**bold**`/`*em*`/`$…$`), который я делаю ~30 строк детерминированного regex. Выбрал **чистую stdlib** — владелец запускает `python3 build.py` без pip/сети, ровно под петлю «правлю MD → смотрю HTML»; минимум зависимостей и детерминизм (karpathy). Шаблон — `_dizajn/shablon.html` с плейсхолдерами `{{…}}` (подстановка строками вместо Jinja2). **Если хочешь именно named-стек — переключу** (плюс шаг `pip install`, версионные риски). По-моему текущий выбор лучше под цель «самодостаточный офлайн-инструмент».

2. **`{shennon}` в `SHEMA §5` vs `→[shannon]`/`{shannon}` в §3 и в заходе — нормализовал к `shannon`.** В `SHEMA §5` пример `lica.md` объявляет `{shennon}`, но §3 и заход §3(А) предписывают ссылку `→[shannon]`. Это противоречие: при буквальном инстанцировании линтер валит сборку (`→[shannon]` не резолвится в `{shennon}`). Нормализовал всё к `shannon` (заход дважды пишет `shannon`). Минимальная правка, восстанавливающая целостность. Если каноном должен быть `shennon` — переименую в одну замену.

3. **Описательный `→[Принстон кочует: Л1,Л2,Л6]` (§7) — выбран минимальный нить-вариант.** Заход: описательные `→[]` снять прозой, но «если по смыслу сквозная нить — полем-сноской `> поле:нить`; неоднозначно → ВОПРОСЫ, минимальный вариант». Принстон проходит через 3 лекции — это нить, и эталон §7 рендерит ровно три чипа `→Л1 →Л2 →Л6`. Сделал `> поле:нить →[granicy] →[optimizaciya] →[sluchaynost] Принстон кочует по курсу` — проза сохранена, чипы как в эталоне. Если предпочитаешь голую прозу без чипов — уберу поле.

4. **SVG-инфографики из `graf.md`/`svyazujushchee.md` НЕ генерятся в L4-странице.** Эталон `L4-v4.html` (одна лекция) их НЕ содержит — это компоненты курс-индекса (`tl2/metro/inst/daily` живут в `chernovik-kursa-v3.html`). Генерить их в L4 = РАСходиться с эталоном и нарушать «минимум кода под критерий». `graf/lica/teorii` парсятся для резолва id, бэклинков и квот линтера, но в SVG для L4 не превращаются. Способность отложена в курс-индексную страницу (следующие заходы). Если нужна SVG-карта уже на L4 — отдельная задача.

5. **Мелкие осознанные адаптации seed (не блокеры, для протокола):**
   - В `lica.md` поле `фото:` поставил локальные имена `shannon.jpg`/`turing.jpg` (не Commons-имена из §5) — генератор ищет ассет в `_dizajn/foto/`; Шеннон извлечён, Тьюринг → монограмма (файла нет).
   - `появления:` Шеннона выставил `kriptografiya§9` (а не §2 как в §5-примере) — так оно соответствует живому Л4, где Шеннон в §9.
   - В `graf.md` добавил **одну** нить `{sluchaynost-instrument}` сверх двух §5-вербатим — на неё ссылается рифма-поле §1 (`→[N:sluchaynost-instrument]`, само из `SHEMA §4.3`), иначе структурный линтер падает. Это прямо реализует замысел захода «seed карты резолвит ссылки».

## ОТЧЁТ — (Code)

**Что это.** Шаг 2 арки («генератор») поставлен на ноги на реальной Л4 и доказан на эталоне. Вертикальный срез А+Б+В: реальный seed `istochnik/`, генератор source→HTML на чистой stdlib, слой целостности (линтер 2 ярусов + бэклинки + `data-source`). Всё в КОРНЕ проекта.

**Что сделал (и зачем).**
- **(А) Seed `istochnik/`** — раскладка строго по `SHEMA §2`:
  - `L4-kriptografiya.md` — транскрипция пилота §1–§10 по правилам §3(А): YAML-шапка (`status: chernovik`), заголовки `## §N · ТИП · Назв {флаги}`, по 4 поля у каждого матблока (§2/§4/§6/§8/§9), поля-сноски (`нить`/`честно`/`cue-визуал`), ссылки Л→id (фикс-мап), лица `→[turing]`/`→[shannon]`, формулы LaTeX `$…$`/`$$…$$`, домашка 8 задач. Провизорная верная транскрипция (доказывает машину, НЕ финальный контент — разбор Л4 это следующий заход, как и сказано в §2 границ).
  - 6 stub-лекций (`L1,L2,L3,L5,L6,L7`) — YAML + строка-заглушка, чтобы `→[id]` и рёбра резолвились.
  - `karta/` (graf/lica/teorii/svyazujushchee) — вербатим примеры `SHEMA §5` (+ правки из ВОПРОСЫ #2,#5).
  - `_dizajn/` — `stil.css` (2 `<style>`-блока эталона, 15.5 КБ), `shablon.html` (каркас с плейсхолдерами), `foto/shannon.jpg` (37 КБ, раскодирован из base64 эталона).
  - `README.md` (регенерация) + `PROTOKOL.md` (таблица `SHEMA §8`).
- **(Б) Генератор** `istochnik/_generator/build.py` (stdlib, ~430 строк): парсит YAML-шапки, разделы, 4 поля матблока, поля-сноски, домашку, `karta/*.md`; двупроходно резолвит `→[id]` (сначала собирает ВСЕ объявленные id, потом рендер); LaTeX→unicode/HTML формулы; Jinja-подобная подстановка в шаблон. Одна команда → `istochnik/_out/L4-kriptografiya.html`, self-contained (фото base64, без сети).
- **(В) Целостность** в той же сборке: бэклинк-индекс (`--backlinks ID`), линтер 2 ярусов (структурный — жёсткий гейт `exit≠0`; редакционный — мягкий, жёсткий только при `status: polno`), `data-source` + HTML-коммент на каждом блоке (паттерн md-redline). Счётчики/квоты деривируются из живых файлов — хранить нечего.

**Решение по рендеру формул и ПОЧЕМУ.** Выбрал **лёгкий маппинг LaTeX-подмножества → unicode/HTML** (а не KaTeX). Причины: (1) эталон рендерит формулы plain-unicode без MathJax — маппинг даёт соответствие эталону; (2) ноль рантайм-зависимостей, остаётся self-contained и офлайн; (3) детерминизм. Покрыто: `^{}`/`_{}` (→ unicode-надстрочные где можно, иначе `<sup>/<sub>`), `\equiv \not\equiv \ge \le \pmod \bmod \cdot \varphi \times \approx \to \in \cap`, тонкие пробелы. Результат в `.eq`/`.m` как в эталоне. KaTeX был бы избыточен (заинлайнить шрифты+CSS ради 3 законов — спекулятивная зависимость).

**Описательные `→[]`: что снял прозой vs полем.** §3 `→[мотив «спецслужба уже умела»]` → проза без `→[]` (как предписано). §5 мост «факторизация НЕ NP-полна… P=NP → RSA падает →[Л3,Л1]» → `> поле:честно …→[slozhnost] →[granicy]`. §7 `→[Принстон: Л1,Л2,Л6]` → `> поле:нить →[granicy] →[optimizaciya] →[sluchaynost]` (минимальный нить-вариант, ВОПРОСЫ #3). Рифма-шапка пилота → `> поле:нить →[N:sluchaynost-instrument] →[sluchaynost]` в §1.

**Как проверил — по КАЖДОМУ пункту КРИТЕРИЯ (pass/fail):**
1. **PASS.** `python3 istochnik/_generator/build.py` одной командой → `istochnik/_out/L4-kriptografiya.html` (77 КБ), self-contained. Офлайн-чек: `grep -nE 'https?://|src="//"'` = **0 совпадений**; единственный внешний ресурс — `data:image/...base64` (фото Шеннона). Без сети открывается.
2. **PASS.** Правка `[утверждение]` §2 (`для простого`→маркер) → пересборка → diff показал изменение **ровно в одной строке** блока `<div class="claimbox" data-source="L4-kriptografiya.md §2 [утверждение]">`; откат → HTML побайтово идентичен. `data-source` ведёт точно к MD-блоку.
3. **PASS.** Линтер валит `exit=1` на каждом: битый `→[xxx]` (§3) → «ссылка не резолвится»; дубль id `kriptografiya` (внёс в L7) → «дубль id [lecture]»; матблок §2 без `[подробность]` → «матблок без полей ['подробность']». Все три порчи внесены временно, падение показано, откат подтверждён (чистая сборка `exit=0`).
4. **PASS.** При `status: chernovik` и 5<20 матблоков сборка НЕ валится: печатает `⚠ редакц.: kriptografiya: 5/20 матблоков`, `exit=0`, HTML собран. (Так же мягко печатаются недоборы квот графа.)
5. **PASS.** `build.py --backlinks turing` → `→[turing] — задето в 1 месте: L4-kriptografiya.md §9 | …`. Проверены и `shannon` (§9), `granicy` (5 мест: §5/§7/§9×2 + stub L1).
6. **PASS (с описанными ожидаемыми отличиями).** Структурная сверка состава компонентов эталон↔мой: **разделов §1–§10 — 10 vs 10**, совпали `techblock/actdiv/cue/hw = 1/1/1/1`. Отличия — все объяснимые и в духе захода:
   - **связей БОЛЬШЕ** (ln-чипов 14 vs 7): новый формат вытащил граф (хребет §9, Принстон §7, честно §5) — заход прямо это допускает.
   - **портретов 2 vs 1**: добавлена монограмма Тьюринга (в эталоне только фото Шеннона — фото одно, Тьюринг → плейсхолдер-рамка, как монограмма Хэмминга).
   - **claimbox 10 vs 6**: каждое из 4 полей матблока — свой блок с `data-source` (точечный рипл), эталон группировал иначе.
   - **eq 3 vs 6 / m-инлайн 36 vs 55 / h3tag 10 vs 11**: seed провизорный и терсе пилота (эталон — полная v3-проза); часть законов осталась инлайн `.m` (промоутил в выносные `.eq` 3 ключевых: Ферма, RSA, Шеннон); §9 двойной тег `техблок+чёрный-ящик` свёл к одному ТИПу (чёрный-ящик несёт поле). Текстовый рендер страницы прогнан целиком — поток (крючок→матблоки→рассказы→АКТ II→техблок→домашка) читается верно.

**Что НЕ трогал.** Durable-доки, ТЗ (`1-START-HERE/`), банки контента, арк-доки, существующие HTML — не открывал ничего сверх 4 названных файлов. `L4-v4.html`/`chernovik-v3.html` — только чтение (источник дизайна; v3 не понадобился — все классы нашлись в L4-v4). Полное влитие Л4 и разбор (RSA→Ферма→Эйлер, Энигма-крючок, биткоин-финал) НЕ делал — это шаг 3. Ничего не двигал/не удалял/не сжимал → тяжёлый lossless-верификатор не нужен (заход §4); гейт — КРИТЕРИЙ + офлайн-чек, оба пройдены.

**Снапшот / откат.** Бэкап до работы: `snapshots/backup-generator-20260630-041410.zip` (4.7 МБ, исключён сам `snapshots/`). Папка не под git — это единственная точка отката.

**Время / токены.** Один заход в один проход, без субагентов: срез вертикальный и тесно связный — разрезать на параллельные ветки = потерять когерентность (seed↔генератор↔линтер — один контракт); калибровка под подзадачу тут не выигрывает. `/cost` из этого контекста программно не снять (это интерактивная CLI-команда) — точные токены смотри в `/cost` на своей стороне. Прогон по wall-clock — порядка десятков минут чистой работы (разведка → seed → генератор → 3 цикла отладки → прогон 6 критериев).

**Артефакт для петли.** `python3 istochnik/_generator/build.py` — правь `istochnik/L4-kriptografiya.md`, смотри `istochnik/_out/L4-kriptografiya.html`. Линтер ловит структурные баги до сборки; `--backlinks ID` отвечает «кого задену».
