# ДНЕВНИК арки «единый источник правды»

## Сессия 1 — 2026-06-30 (консолидация прошлой арки + завод + схема источника)

**Вход.** По инструкции проекта: COWORK.md прочитан (роли, среда, роутер). Задача владельца: закрыть арку «научпоп-черновик» (консолидация), потом завести арку «единый источник правды» (цель — `TZ-edinyy-istochnik.md`), первый шаг — схема источника. Список правок Л4 не потерять.

**Консолидация «научпоп-черновик» — сделана (ARKA §7).** Прочитал `SESSIYA.md` той арки ЦЕЛИКОМ (сессии 1–6) + дома. Durable разнесён: формат v4 + 7 финально + ТЗ v2 → `resheniya.md` п.14; архитектура единого источника → п.15; статус/Ф5✓/открытые → `dnevnik-i-plan.md`; фактчекнутый контент 10/10 + исправленные ошибки → `kotel.md §10`; оптимальный транспорт + крипто-история → `ISTOCHNIKI.md`; контент-источник v2 + формат v4 → `NAVIGATION.md`; «единый научпоп-текст Ф5» помечен сделанным → `beklog.md`. Свод — `zhurnal/SVODKI.md` (сверху), арка помечена ЗАКРЫТОЙ (+ баннер в её NAVIGATOR). **Свежий верификатор (отдельный субагент, иной метод) — GREEN:** потерь durable 0, битых ссылок 0 (новая арк-папка — ожидаемый MISS). Список правок Л4 НЕ потерян — указатель из NAVIGATOR этой арки + `dnevnik` открытые.

**Завод арки — go без нового Ф0.** Ворота/интервью/ресёрч пройдены в прошлой арке (сессия 6): кикофф `TZ-edinyy-istochnik.md` написан через `project-kickoff`, 3 развилки решены владельцем через AskUserQuestion (`resheniya.md` п.15). Поэтому Ф0 не переигрываю — создаю доки арки прямо (NAVIGATOR/PLAN/SESSIYA + схема).

**Решение (от владельца, п.15) — структура источника.** Папка: MD-файл на лекцию (формат v4 §2.5) + файл-карта (граф/лица/теории/связующее); ТЗ — отдельный канон; старт — полный Л4-образец + генератор, потом 7. Полнота жёсткая.

**Шаг 1 — схема источника спроектирована** (`SHEMA-istochnika.md`). Опирается на канон формата v4 (`sistema-ocenki.md §2.5`, 2.5.0–2.5.8) + анатомию пилота (`L4-pilot-v4.md`). Ключевые проектные решения и ПОЧЕМУ:
- **Папка `istochnik/`:** `LN-<тема>.md` на лекцию + один `karta.md` + (для генератора) `_dizajn/` с шаблонами/CSS из v3. *Почему папка, не один MD:* правка одной лекции не трогает остальные; генератор обходит файлы; растёт без раздувания.
- **Файл-лекция = YAML-шапка + именованные разделы.** Шапка: `id/title/role/theory/cel/budget`. Раздел: заголовок `## §N · ТИП · Название {флаги}`, тип из канона (`КРЮЧОК/МАТЕМАТИКА/РАССКАЗ/ТЕХБЛОК/ЧЁРНЫЙ-ЯЩИК`); матблок несёт 4 поля канона (`утверждение·пример / логика / подробность / чёрный-ящик`); РАССКАЗ — полная проза; домашка — список. *Почему так:* машиночитаемо для генератора И редактируемо руками (обычный MD); 4 поля = ровно §2.5.2 «настоящая математика, раскрытая».
- **Правило «полно и без пересечений»** (ядро полноты): контент ВНУТРИ лекции → файл лекции; всё КРОСС-лекционное (рёбра графа, кочующие лица, сквозные теории, связующие тексты) → `karta.md`; лекция ССЫЛАЕТСЯ (`→[Тьюринг]`, `→[Л1]`), не переписывает. *Почему:* у каждого факта один дом → нет дублей; генератор резолвит ссылки в HTML (вставка портрета, чип `.ln`, дуги графа). Полнота проверяема: у любого куска банка есть слот (деталь→лекция, связь→граф, лицо→лица, теория→теории, артефакт→связующее).
- **Поля-сноски** — typed-блок `> поле:<тип> …` (нить/в-кармане/отложимость/честно/боковой-сюжет) = §2.5.7.
- **Контракт генератора** прописан (что читает, что эмитит), но сам генератор — следующий шаг (Code).
- **Скелет-образец Л4** включён (реальная карта разделов пилота как шаблон с плейсхолдерами под глубокий контент) — чтобы схема была предъявлена, не абстрактна. ПОЛНОЕ влитие Л4 — отдельный шаг (Code, lossless).

**Открыто владельцу (в схеме/PLAN):** формат полей-сносок; `karta.md` один файл vs `karta/`-подпапка; куда физически кладём `istochnik/` (предложен корень проекта). _NB:_ влитие полной Л4 и пересборка Л4 по разбору пересекаются — при влитии сразу учитывать правки разбора.

**Ждём:** ревью схемы владельцем → правки → Code-заход на генератор → влитие Л4 lossless.

## Сессия 2 — 2026-06-30 (полный вход + интервью + ресёрч + аудит → схема v2 утверждена)

**Вход — полный (ARKA §3, триггер: структурная мета-задача).** Прочитано: COWORK/ARKA/NAVIGATION, арка целиком (NAVIGATOR/PLAN/SESSIYA + черновик SHEMA), `resheniya` п.14–15, формат `§2.5`, банки расползания (`opisanie-kursa-v2/`, глубокий банк `procedura-poiska/` вкл. `graf-pautina`, `2-idei/`), структура HTML-макетов (v3/L4-v4, не весь 1.1 МБ).

**Сверка диагноза по живым файлам (не индексу).**
- **Нашёл конкретное расползание:** нумерация лекций разъехалась — банк v1 (`graf-pautina`, `koncepcii-lekcij`) Л3=Криптография/Л4=Информация/Л5=Случайность; контент v2 + HTML Л3=Сложность/**Л4=Криптография**/Л5=Информация/Л6=Случайность/Л7=финал. `→Л4` в разных слоях = разные лекции. → корень-решение: адресовать **стабильным id**, не номером.
- **Поправка владельца про HTML:** «HTML не подмножество, последний заход дописал математику; проверь, не потеряем ли при сведении». Проверил `L4-v4.html` против `L4-pilot-v4.md`: все 13 матблоков (Ферма/Кармайкл-561/RSA/Эйлер/Миллер-Рабин (¼)ᵏ/мини-RSA p=3,q=11/H(K)≥H(M)/домашка) **дословно в MD-пилоте**; v3 в подвале сам объявляет источником `opisanie-kursa-v2/`. **HTML-only математики не найдено.** Но закрепил как ПРАВИЛО ПРОЦЕССА: HTML — равноправный вход при влитии (спот-чек ≠ доказательство; lossless-верификатор обязателен).

**Реврейм владельца (ключевой).** Руками не правит — работает через чат (агент/Code по протоколу), комментирует HTML. → deliverable не «редактор для человека», а: (1) единая связанная база (один дом + деривация, дубль не может разойтись); (2) писаный протокол «где что править» в традиции нашей документации; (3) верификатор. Адресация id — подтверждена владельцем (AskUserQuestion).

**Ресёрч (3 субагента, веб; дистиллят → `ISTOCHNIKI.md`).** Независимо сошлись: **не фреймворк, а тонкий слой скриптов поверх plain-MD.** Стек: генератор (свой Python `markdown-it-py`+Jinja2; Pandoc `--standalone --embed-resources` baseline) + бэклинк-индекс (инверсия `→[id]`) + линтер-падающий-тест (квоты деривируются из живых файлов) + привязка `data-source` (HTML→MD-блок) + протокол с маркерами `@canonical/@ref`. **Честный предел:** механический рипл автоматизируется, **семантический — ревью-гейт** (агент флагует, не решает).

**Аудит развилок (владелец делегировал «реши сам, поспорь с собой»; обе стороны отстоял).** Вердикты с «почему»:
1. **Карта** → ~~один `karta.md`~~ **дроблена по типу** (`graf/lica/teorii/svyazujushchee`): 4 разные схемы, изоляция правки, чистый парс, агент грузит нужный файл; «вся паутина одним свитком» лучше даёт сгенерённый граф. *Сознательно против прежнего выбора владельца — он согласился.*
2. **Переиспользование** → ссылка по умолчанию + фраза-роль локально в лекции; дословный повтор — трансклюзия `{{вкл:id}}` (один дом).
3. **Целостность** → **два яруса**: структурное (битый id, дубль, кривой блок) жёстко всегда; редакционные квоты (≥20 матблоков, квоты графа, лицо 2–5) — по полю `status` (жёстко при `polno`). Не дерётся с недописанным черновиком.
4. **Место `istochnik/`** → **корень проекта** (durable + сам deliverable; перенос из арки — риск, песочница не двигает файлы).
5. **Генератор** → свой тонкий Python (наш кейс требует Python-логики: SVG-карта, резолв id, base64); Pandoc baseline; деталь Code-захода.

**Итог.** `SHEMA-istochnika.md` переписана **v2** (4 принципа вкл. адресацию · раскладка с `karta/` по типу · реестр id деривируется · формат лекции v4 · формат 4 файлов карты · правило полноты · слой целостности §7 · протокол §8 · контракт генератора §9 · влитие с HTML-входом §10 · скелет Л4). **Утверждена владельцем** («всё нравится, погнали»). Арк-доки (NAVIGATOR/PLAN) синхронизированы.

**Ждём:** Code-заход на **генератор MD→HTML** (следующий шаг; `agentic-coding-session-brief`).

## Сессия 3 — 2026-06-30 (собран Code-заход на генератор MD→HTML)

**Вход — арк-ритуал** (NAVIGATOR → PLAN «СЕЙЧАС» → хвост SESSIYA с.2) + COWORK / SHEMA v2 / RUKOVODSTVO §6. Сверка по живым файлам: `istochnik/` ещё нет; `L4-v4.html` (91 КБ) и `chernovik-kursa-v3.html` на месте; `kod_*`-файлов в арке не было.

**Поправка предпосылки (вынес владельцу).** Владелец: «что значит источник не существует — файл со всем контентом же есть». Развёл: провалидированный КОНТЕНТ есть (`L4-pilot-v4.md` = то, из чего свёрстан `L4-v4.html`; + банки), а вот папки `istochnik/` в НОВОМ формате схемы v2 — нет; её и генерим. Реврейм владельца принят: **эталон вывода = `L4-v4.html`** («при попадании MD получается ~то же, связей может быть больше»).

**Две развилки — решены.**
1. **Объём захода (Path B).** Один заход = генератор + СОЗДАНИЕ seed `istochnik/` Кодом (внутри его бэкап-прогона — file-мутации принадлежат бэкапнутой Code-сессии, не ad-hoc Cowork). Seed Л4 = верная транскрипция пилота, **провизорная** (доказывает машину). Полное lossless-влитие Л4 (банки + разбор + полнотный верификатор) — отдельный шаг 3.
2. **Pandoc — исключён** (владелец делегировал техрешение). Аудит: реальный эталон теперь `L4-v4.html` — лучше любого Pandoc-вывода; Pandoc физически не воспроизводит наши компоненты / SVG-граф / резолв `→[id]` / `data-source` → сверять нечего, выходы несопоставимы; по karpathy — лишняя зависимость без отдачи. Оставлен заметкой-fallback, не в заходе.

**Заход написан** — `kod_generator.md` (RUKOVODSTVO §6 + `agentic-coding-session-brief`). Несёт: §0 первый ход (zip-бэкап в `snapshots/`, читать поимённо SHEMA / пилот / эталон) · §1 karpathy · §2 границы (не влитие / не разбор; HTML только чтение; не двигать) · §3 (А) seed с точным мапом переноса пилот→формат (типы, 4 поля, Л→id, лица, поля-сноски) + 6 stub-лекций + `karta/` из §5 вербатим + `_dizajn/` из эталона · (Б) генератор `markdown-it-py`+Jinja2, двупроходный резолв id, рендер формул без MathJax (решение за Code), `data-source` · (В) линтер 2 яруса + бэклинк-индекс · провальный критерий из 6 пунктов · отчёт время+токены. Тяжёлый верификатор не ставил — заход созидающий (ничего не теряет/двигает); гейт = критерий + офлайн-чек.

**Ждём:** прогон Code владельцем (стандартный текст-пересылка выдан) → приёмка по `## ОТЧЁТ` (гейты RUKOVODSTVO §4) → шаг 3 (влитие Л4).

**Приёмка (с.3, тот же чат). ПРИНЯТО.** Критерий перепроверен НЕЗАВИСИМО (генератор stdlib → запустил в песочнице на копии `istochnik/`):
- сборка `exit=0` + печать недобора `kriptografiya: 5/20 матблоков` (chernovik мягко); вывод self-contained (внешних ссылок 0, фото base64); `data-source` на 29 блоках (гранулярно по полям, напр. `§2 [утверждение]`); `--backlinks turing` → §9; счёт компонентов совпал с отчётом (claimbox 10, eq 3, ln 14, port 2, techblock 1).
- структурный линтер ВАЛИТ `exit=1` на битом `→[id]` / дубле id / матблоке без поля; чистый откат → `exit=0`. Два яруса работают.
- границы: вне `istochnik/` изменён только `kod_generator.md` (отчёт Code); HTML только читался; влития/разбора нет; ничего не двигалось/не удалялось.

**Решения Code — ратифицированы:** (1) стек — **чистая stdlib** вместо markdown-it/Jinja2 (в окружении их нет; формат bespoke, не CommonMark; лучше под офлайн-петлю без pip — Code оспорил мою предпосылку по живому окружению, верно); (2) формулы — **LaTeX→unicode** маппинг (соответствие эталону, ноль рантайм-зависимостей); (3) `{shennon}`→`{shannon}` — Code нашёл противоречие в МОЕЙ схеме §5 vs §11; нормализация верна, **канон `SHEMA §5/§11` поправлен** на `shannon`; (4) Принстон §7 → `> поле:нить` с 3 чипами (как в эталоне); (5) мелкие адаптации seed (локальные имена фото; Шеннон §9; нить `{sluchaynost-instrument}` под рифму §1) — приняты.

**Перенос/открыто:** (а) **SVG-инфографика** графа/связующего — уровень **курс-индекс-страницы**, не пер-лекция (в эталоне L4 её нет); в заход раскатки/индекса (шаг 4). (б) **Токены** прогона из интерактивного `/cost` Code программно не снял (есть только wall-clock «десятки минут») — стандарт [время+токены] закрыт частично; для точных токенов впредь — скриптовый прогон `claude -p --output-format json` (RUKOVODSTVO §8). (в) Визуальный гейт «вид ≈ эталон»: счёт компонентов сверён; полный рендер-просмотр (`visual-design-review`) — опционально перед шагом 3.

**Дальше:** шаг 3 — влитие полной Л4 lossless (с разбором; тяжёлый верификатор полноты).

**Визуал-доводка (с.3, после показа владельцу).** Владелец сравнил новый вывод с эталоном `L4-v4.html` — увидел реальные расхождения; я признал, что в приёмке сверил счёт компонентов, но не смотрел глазами. Диагноз (код-уровень, браузера в песочнице нет): **CSS байт-в-байт одинаков → дело в РАЗМЕТКЕ генератора.** Конкретно: (1) лекции-чипы несут полные названия в крошечной «таблетке» под `Л6` → тесно; (2) люди (`turing`/`shannon`) рисуются той же громкой таблеткой вместо портрета; (3) подпись портрета — весь био (длинно); (4) наезды — от 2-го плавающего портрета; (5) центр-формул 3 vs 6. Текст короче (1073 vs 1885 слов) — НЕ потеря: seed терсе (922 слова, выжимка пилота), полная проза вливается на шаге 3 (генератор из seed ничего не выкинул — проверено). Решение владельца: визуал отдать Code (рендерит и смотрит глазами), контент — потом. **Заход написан** — `kod_generator-visual.md` (правки в генераторе/`_dizajn`, чтобы распространялись на все лекции; рендер-сравнение с эталоном; критерий; не трогать контент/принятый функционал). Ждём прогона.

**Приёмка визуал-доводки (с.3). ПРИНЯТО.** Code отрендерил обе страницы Chrome-headless, правил `build.py`+`stil.css`. Перепрогнал в песочнице: сборка зелёная, self-contained; `.lref`=12 (лекции-чипы просторные), `.pers`=2 (лица — спокойное имя), `.ln`-рефов **0** (громких пилюль нет); `h3{…clear:left}` (наезды §9→§10 убраны); подпись портрета короткая; MD-источник не тронут (922 слова). Принятый функционал цел (бэклинки/`data-source`/линтер). Генерализовано (в генераторе/CSS), не в одну страницу.
**3 вопроса Code — решены (мелкие):** (1) формулы §8 в центр — через нотацию `$…$`→`$$…$$` в ИСТОЧНИКЕ на контент-шаге (генератор уже центрирует все `$$`); авто-эвристику не вводим (хрупко) — согласен с Code, вариант (а). (2) подпись портрета — оставляем общую роль-фразу из `lica.md` (генерализуемо); под-лекционная подпись («ключ не короче сообщения») — опциональный бэклог. (3) портрет на КАЖДОЕ первое упоминание → лишний портрет Тьюрингу на `(подмигивание)`; **принимаем правило «портрет — существенным появлениям (`ядро`), `подмигивание` → просто имя»**, применить в начале контент-арки (правило завязано на аннотации `появления:`, финализируемые при влитии); наезды уже убраны → не блокер.
**Итог: генератор доведён до логичного конца.** chips/люди/подпись/наезды — на месте; формулы-нотация и портрет-гейт — на контент-шаг. Дальше: консолидация арки + закрытие (Конец АРКИ), затем контент-арка (влитие Л4 + раскатка).
