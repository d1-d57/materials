# Канал Claude Code — Сборка Л2 «Оптимизация» (влитие v3 + улучшение)

> **Для Claude Code:** это твой ЕДИНСТВЕННЫЙ файл-заход. Читай ТОЛЬКО его и файлы, названные ниже поимённо; остальной проект не изучай (правило — `CLAUDE.md`). Папка НЕ под git → ПЕРВЫМ ХОДОМ zip-бэкап. План, вопросы, финальный отчёт пиши в этот же файл, в секции внизу (`## ПЛАН` / `## ВОПРОСЫ` / `## ОТЧЁТ`). Сильная модель; часы — норма; метрика — КАЧЕСТВО. Дешёвое/параллельное (grep-сверки, верификатор) — субагентам.

## ЗАДАЧА (один проход до конца)

**КОНТЕКСТ.** Проект — платный онлайн-курс по научпоп-математике; правда о курсе в `istochnik/` (Markdown, формат v4; HTML — чистая функция источника). Лекция **Л2 «Оптимизация / ЛП + оптимальный транспорт»** в `istochnik/L2-optimizaciya.md` — пока СКЕЛЕТ, НО **готовая короткая версия уже есть** в черновике курса v3 (HTML, с текстом, портретами, связями, выверенными кейсами). Аналитик сделал **композицию** (`kompoziciya-L2.md`, ред.2 — линия владельца) и **research** (`research-L2.md`, сверенная фактура+математика). **ЦЕЛЬ:** НЕ сочинять с нуля, а **влить версию v3 и улучшить** до формата v4 по композиции ред.2, вшить кросс-лекционное в `karta/`, прогнать линтер дозелёна, доказать полноту верификатором. Это метод Л4 (берём готовое, усиливаем хирургически). Приёмка — по `## ОТЧЁТ`, без построчной сверки → отчёт исчерпывающий.

### 0. ПЕРВЫЙ ХОД
- Рабочая папка `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026` (Mac). СРАЗУ zip-бэкап `istochnik/` в `snapshots/` (git нет — единственная точка отката).
- Прочитать ТОЛЬКО эти файлы (ничего сверх):
  - `zhurnal/2026-06-30_L2-optimizaciya/kompoziciya-L2.md` — **архитектура (ред.2)**: ось, ABT, лестница §4 (9 блоков), карта «v3+банк→блок», кольцо, флаги. Главный план.
  - `zhurnal/2026-06-30_L2-optimizaciya/research-L2.md` — **сверенная фактура и математика** (числа двух примеров руками, честные флаги, формулировки «для зала»). Факты — отсюда.
  - **SEED:** `zhurnal/2026-06-28_nauchpop-chernovik/chernovik-kursa-v3.html` — **только раздел «Оптимизация»** (файл 1.1 МБ с base64 — НЕ грузить целиком; вытащи `sed`/`grep` секцию `<h2>Оптимизация…` до следующего `<h2>`, ≈строки 277–305, + CSS-переменные портретов `--p-dantzig`, `--p-kantorovich`). В нём: написанный текст лекции, портреты Данцига/Канторовича (base64), связи (→Л6/Л7, таймлайн, «каждый день»→Stable Diffusion), **выверенные прикладные кейсы** (почки/Рот-2012, экипажи AA, IMRT). Это БАЗА — переноси и улучшай, не выбрасывай.
  - `istochnik/L4-kriptografiya.md` — **ОБРАЗЕЦ ФОРМАТА** v4 (шапка, `## §N · ТИП · …{флаги}`, 4 поля матблока, поля-сноски, `→[id]`, домашка). Копируй ФОРМАТ, не контент.
  - `zhurnal/2026-06-30_edinyy-istochnik/SHEMA-istochnika.md` — контракт формата (§4 лекция, §5 karta, §6 полнота, §7 линтер).
  - `istochnik/PROTOKOL.md` — куда что писать + семантический рипл.
  - `istochnik/L2-optimizaciya.md` — СКЕЛЕТ (его заполняешь).
  - `istochnik/karta/graf.md`, `lica.md`, `teorii.md` — кросс-лекционное (дополняешь L2; переиспользуй id, не дубли).
  - `istochnik/_generator/build.py` + `istochnik/README.md` — линтер/генератор (ЗАПУСКАЕШЬ, не правишь).
  - `1-START-HERE/sistema-ocenki.md` — ТОЛЬКО §2.5 (формат v4, планка плотности).
- Изложить ПЛАН в `## ПЛАН` ПЕРЕД действиями.

### 1. ДИСЦИПЛИНА
Минимальная правка + проверка после каждого блока. Якорные замены — **abort при совпадениях ≠ 1**. Сохранять по умолчанию (рискованно-неоднозначное — в `## ОТЧЁТ`). Ложную предпосылку — оспорь по живым файлам. **ВСЕГДА впиши в отчёт время прогона и токены** (`/cost`).

### 2. ГРАНИЦЫ (НЕ трогать)
- **НЕ трогать другие лекции** (L1,L3,L5–L7) — только `L2-optimizaciya.md` + L2-узлы/рёбра в `karta/` (существующие записи не переписывать, только дописывать).
- **НЕ трогать генератор/схему:** `build.py`, `SHEMA-istochnika.md`, `_dizajn/shablon.html`, `stil.css` — только пользоваться. `build.py` хардкодит HTML на `kriptografiya` (≈стр.621) → **HTML-сборку L2 НЕ требуем** (генерализация — отдельный заход, анти-скоуп). Критерий — линтер (§3).
- **SEED v3 (`chernovik-kursa-v3.html`) и входы (`kompoziciya-L2.md`, `research-L2.md`) — READ-ONLY.** Нашёл в них ошибку — флаг в `## ВОПРОСЫ`, не правь.
- НЕ трогать `1-START-HERE/sistema-ocenki.md` (ТЗ — отдельный канон).

### 3. ЗАДАЧА
Влить версию v3 §Оптимизация и улучшить до формата v4 в `istochnik/L2-optimizaciya.md`, по лестнице `kompoziciya-L2.md §4` + фактуре `research-L2.md`, образец формата — `L4-kriptografiya.md`.

**ЛИНИЯ (композиция ред.2 — соблюсти ОСЬ и ПОРЯДОК):** ось — **«как оказалось, что искать оптимум в ПРАКТИЧЕСКИХ задачах — просто»**; ведём практическими задачами (рацион·транспорт·расписание·почки·терапия — их побольше, в простом виде), абстрактное (теория игр, выпуклая геометрия, транспорт→картинки) — украшение. Порядок: **конкретное → сведе́ние → абстрактный подъём → возврат к линейным системам → кольцо**. **Данциг СНЯТ с зачина** (его байка про «домашку» — абстрактная статистика, слабый крючок) → он в блоке 6 как прикладной автор симплекса; байка — максимум короткая сноска или выброс. Зачин — конкретными практическими задачами (стена «найти лучшее» + астрономия вариантов), НЕ персоной.

**(а) Шапка (YAML).** `id: optimizaciya` (НЕ менять), `poryadok: 2`, `title: Оптимизация`, `role: тело`, `theory: [...]` (id из (в)), `cel: Как находить лучшее` (ось §2.5.5), `teorema-zagolovok: Основная теорема линейного программирования`, `budget: { math_blokov: 20, vsego_blokov: 38 }`, `status: chernovik` (флип в `polno` — отдельный шаг арки).

**(б) Тело — `## §N · ТИП · Название {флаги}`** по 9 блокам лестницы `kompoziciya-L2.md §4` (развернуть в ~13–15 §-разделов; **ДВА ядра руками** — рацион и транспорт). ТИПы: `КРЮЧОК`/`МАТЕМАТИКА`/`РАССКАЗ`/`ТЕХБЛОК`/`ЧЁРНЫЙ-ЯЩИК`. Матблоки — ровно 4 поля. Перевес в математику; целься **≥20 матблоков**. Текст научпопа — **бери из v3 и улучшай** (голос `popsci-narrative`+Ильяхов; объём не урезай). Практические кейсы v3 (почки/Рот, AA, IMRT) — **поднять в зачин/сведе́ние** (блоки 1,3), а не прятать в середину. Поля-сноски `> поле:тип`. Домашка 5–10.

**ДВА считаемых примера руками — ОБЯЗАТЕЛЬНЫ, ТОЧНЫЕ числа из `research-L2.md` (проверены scipy):**
- **Рацион (ядро 1):** min 2x₁+3x₂ при 2x₁+x₂≥8, x₁+3x₂≥9, x≥0 → вершины (0,8)=24, **(3,2)=12**, (9,0)=18; оптимум (3,2)=12. Линейку двигаем в сторону уменьшения до отрыва.
- **Транспорт (ядро 2):** запасы [20,25,15], спросы [22,19,19], стоимости [[8,6,3],[5,4,6],[4,7,5]] → оптимум **230** (единствен), план [[0,1,19],[7,18,0],[15,0,0]]; потенциалы u=[0,−2,−3]/v=[7,6,3], редуцированные ≥0 (сертификат=двойственность); северо-западный угол 345.

**ЧЕСТНЫЕ ФЛАГИ (из `research-L2.md` — дословно, НЕ переврать):**
1. Канторович: **1939 ЛП (фанерный трест) / 1942 транспортная задача / 1948 связь с Монжем** — три разные даты. (В v3 смешано «1939/транспорт» — ПОПРАВИТЬ при влитии.)
2. Двойственность ЛП и минимакс фон Неймана — **«две стороны одной медали»/частный случай**, НЕ «⇔»; строгое — Гейл–Кун–Таккер 1951.
3. Бренье: **Монж не доказал СУЩЕСТВОВАНИЯ** оптимального перемещения (стоимость |x−y|); структуру для |x−y|² (T=∇φ) раскрыл Бренье. НЕ «Монж не смог для |x−y|²».
4. Вершины транспорта: m+n−1 = **базисные** клетки (не «ненулевые»); Биркгоф — только квадратная задача о назначениях.
5. WGAN: обнуляются **расхождения** (KL/JS), не «меры»; W₁ меняется плавно.
6. Осн. теорема ЛП — формулировка «для зала» **на компакте**.
7. Данциг: **две нерешённые задачи** (не «нерешаемые»); **Абрахам Вальд** — соавтор; «Уилл Хантинг» — легенда. (Если оставляешь сноску — так.)
8. **Монж — фортификатор школы Мезьер, НЕ «инженер Наполеона».** **SABRE — БД бронирования, UPS ORION — эвристика TSP; оба НЕ ЛП** (в v3 уже заменены кейсами — сохрани). Факторизация в NP∩co-NP → к Л3.

**(в) Вшить кросс-лекционное в `karta/` — чтобы ВСЕ `→[id]` из L2 резолвились** (иначе структурный линтер валит):
- `lica.md` — добавить L2-лица (translit-id; переиспользуй существующие): `danzig`, `fon-neyman`, `kantorovich`, `monzh`, `villani`, `figalli`, `stigler`, `brenier`. Поля по образцу (роль · `фото:<имя>.jpg` · появления:[optimizaciya§N(роль),…] · био-бит). **Портреты:** Данциг/Канторович есть в v3 base64 — вытащи и сохрани в `_dizajn/foto/danzig.jpg`, `kantorovich.jpg` (best-effort; если трудно — оставь `фото:` имя как плейсхолдер, добор позже, НЕ блокируй).
- `teorii.md` — добавить `{lin-programmirovanie}` (ЛП; лекции:[optimizaciya(ядро), slozhnost(затравка NP)]) и `{opt-transport}` (Монж→Канторович→Бренье; лекции:[optimizaciya(ядро), myshlenie(мост)]); вписать их id в `theory:` шапки.
- `graf.md` — добавить L2-нити/рёбра из `kompoziciya-L2.md` (карта «v3+банк→блок», МОСТЫ): нити `универсальность` (granicy·optimizaciya·slozhnost·myshlenie), `человек→машина` (optimizaciya·granicy·slozhnost·sluchaynost·myshlenie), `спрятанная-геометрия` (optimizaciya·kriptografiya·informaciya·myshlenie); рёбра — `optimizaciya -> myshlenie` (транспорт→Stable Diffusion), `optimizaciya -> slozhnost` (NP-звоночек), `optimizaciya ~ granicy` (универсальность). `{os-vremeni}` уже включает optimizaciya — не дублировать. Связи РЕАЛЬНЫЕ (анти-фабрикация §5.2 ТЗ).

**КРИТЕРИЙ ГОТОВНОСТИ (проверяемое, может ПРОВАЛИТЬСЯ):**
- `python3 istochnik/_generator/build.py --lint` → exit 0, «✓ структурные проверки пройдены»; **0 структурных ошибок** (все `→[id]` L2 резолвятся, 4 поля у каждого матблока, типы/номера валидны).
- Редакц. отчёт линтера: `optimizaciya: ≥20/20 матблоков`, домашка 5–10.
- Полный `python3 build.py` НЕ падает (линтер всех лекций зелёный; L4-HTML собирается).
- Оба примера руками с точными числами; 8 флагов соблюдены; ось/порядок ред.2 выдержаны; Данциг не в зачине; кейсы v3 в зачине/сведе́нии; `status: chernovik`.

### 4. ВЕРИФИКАТОР ПОЛНОТЫ (свежий субагент — заход синтезирующий/лоссовый)
Отдельным свежим субагентом (не автор; другой метод — чек-лист из входов + grep + линтер):
- **Полнота композиции:** каждый из 9 блоков `kompoziciya-L2.md §4` приземлился (список потерь ПУСТ); оба ядра руками на месте; порядок = ред.2 (конкретное→абстракт→возврат→кольцо; Данциг не в §1).
- **Сохранность v3:** перенесены портреты (Данциг/Канторович), прикладные кейсы (почки/Рот, AA, IMRT), связи →Л6/Л7 — ничего из v3 не потеряно (grep по ключевым словам).
- **Честность:** grep — есть «1942» (транспорт Канторовича), числа примеров (`230`, `(3,2)`/`12`, `39.69`/`39.93`); ОТСУТСТВУЮТ «инженер Наполеона», «SABRE»/«ORION» рядом с «ЛП», «⇔» минимакс↔двойственность.
- **Ссылки:** `build.py --lint` → битых `→[id]` = 0 (программно).
Результат (таблица «вход→приземление» + потери + grep) — в `## ОТЧЁТ`.

### 5. ОТЧЁТ → секция `## ОТЧЁТ`
Что увидел / сделал и ЗАЧЕМ / как проверил (вывод линтера дословно) / что НЕ трогал / результат верификатора / вопросы в `## ВОПРОСЫ` / **время прогона + токены**.

---

## ПЛАН

**Статус первого хода:** zip-бэкап сделан (`snapshots/istochnik_pre-L2_2026-06-30_230442.zip`); все названные файлы прочитаны; базовый `--lint` зелёный (exit 0); портреты извлечены из v3 в `_dizajn/foto/` (danzig, kantorovich, fon-neyman, monzh, villani, figalli).

**Контракт линтера (вычитан из `build.py`):** матблок = §-раздел типа `МАТЕМАТИКА`/`ТЕХБЛОК`/`ЧЁРНЫЙ-ЯЩИК` со ВСЕМИ 4 полями. Структурный гейт (валит сборку): все `→[id]` резолвятся, нет дублей id, 4 поля у каждого матблока, валидный тип/номер раздела. Редакционное (≥20 матблоков, домашка 5–10, квоты графа, лицо в 2–5 лекциях) при `status: chernovik` — мягкие warnings, сборку не валят. **Тяну ≥20 матблоков по §2.5.1 (бюджет `vsego_blokov:38`/`math_blokov:20` это подтверждает — не «13–15», см. ВОПРОСЫ).**

**Шаги:**
1. **Наполнить `istochnik/L2-optimizaciya.md`** по лестнице `kompoziciya §4` (ред.2), фактуре `research`, формату `L4`. Шапка из (а). ~24 §-раздела (≈20 матблоков + крючок + 4 рассказа + домашка). Карта матблоков:
   - §1 КРЮЧОК — стена практических задач (почки/Рот·35, AA $1,4 млрд, IMRT — кейсы v3 в зачин); астрономия вариантов. НЕ персона.
   - §2–6 **ЯДРО 1 рацион руками**: ограничения=неравенства · допустимое=многоугольник(вершин конечно) · цель=линейка · скольжение→вершина (числа (0,8)=24/(3,2)=12/(9,0)=18) · осн.теорема ЛП {теорема-заголовок, на компакте}.
   - §7 сведе́ние: общая форма max cᵀx при Ax≤b; §8 стандартная форма/slack — почему «тысяча задач = одна».
   - §9 РАССКАЗ Канторович, фанерный трест 1939.
   - §10–14 **ЯДРО 2 транспорт руками**: формулировка · считаемый пример (230 vs северо-западный 345) · метод потенциалов=двойственность транспорта (сертификат u/v, ред.≥0) · релаксация Канторовича=ЛП (расщепление массы) · вершина=базисный план m+n−1.
   - §15 выпуклая геометрия (крайняя точка); §16 слабая двойственность (строка алгебры); §17 сильная=минимакс фон Неймана; §18 матричная игра=пара двойственных ЛП (фон Нейман «Oh, that!» в прозе-поле).
   - §19 симплекс: вершина=решение лин.системы (базис); §20 РАССКАЗ Данциг SCOOP 1947 + диета Стиглера (человек→машина $39.93→$39.69).
   - §21 ТЕХБЛОК Кли–Минти + сглаженный анализ {парковка} →[slozhnost].
   - §22 Бренье T=∇φ; §23 Вассерштейн/WGAN (расхождения обнуляются, W₁ плавно) →[myshlenie].
   - §24 РАССКАЗ финал-кольцо (лёд Ладоги → пиксели SD3); §25 ДОМАШКА 7–8.
2. **`karta/`:** lica.md +8 лиц (danzig, fon-neyman, kantorovich, monzh, villani, figalli, stigler, brenier); teorii.md +{lin-programmirovanie}+{opt-transport}; graf.md +3 нити (universalnost, chelovek-mashina, spryatannaya-geometriya) +3 ребра. Только дописывать.
3. **Линтер дозелёна** (`--lint` exit 0, ≥20 матблоков), затем полный `build.py` (L4-HTML собирается).
4. **Свежий субагент-верификатор** (другой метод): полнота 9 блоков, сохранность v3, честность (grep флагов), битые ссылки=0.
5. **Отчёт** в `## ОТЧЁТ` + время/токены.

## ВОПРОСЫ

1. **«~13–15 §-разделов» vs «≥20 матблоков» — противоречие в заходе.** В линтере матблок = §-раздел типа МАТЕМАТИКА/ТЕХБЛОК/ЧЁРНЫЙ-ЯЩИК с 4 полями, поэтому 20 матблоков физически не вмещаются в 13–15 разделов. Разрешил в пользу **≥20 матблоков** — это прямое требование формата §2.5.1 и бюджета (`vsego_blokov:38`, `math_blokov:20`). Итог: **26 §-разделов, ровно 20 матблоков** (+ крючок, 4 рассказа, домашка). Если владелец хотел именно компактные 13–15 разделов — это несовместимо с планкой ≥20 матблоков; нужен выбор приоритета.

2. **Семантический рипл рамки (PROTOKOL §8 — ФЛАГ на ревью).** Поставил `cel: Как находить лучшее` (ось ред.2). По композиции (флаг 1) это задевает по смыслу **манифест, переход Л1→Л2, мост Л7** в `karta/svyazujushchee.md` и соседние лекции. Эти файлы — ВНЕ скоупа захода (трогаю только L2 + L2-узлы карты; `svyazujushchee.md` мне не назван). Композиция помечает рамку как ратифицированную владельцем 2026-06-30, но фактическую сверку манифеста/таймлайна/переходов не делал — **выношу на ревью аналитику**.

3. **Single-appearance лица — мягкий warning линтера «норма 2–5».** danzig, kantorovich, monzh, villani, figalli, brenier, stigler появляются в курсе **только в Л2** (остальные лекции — скелеты). НЕ стал фабриковать кросс-появления ради метрики (анти-фабрикация §5.2): это L2-локальные лица-украшения, для них 1 появление — честная правда. `fon-neyman` оставил 3 появления (optimizaciya + slozhnost + myshlenie) — design-justified по v3 («главный кочующий герой курса, Л1/Л3/Л7»). Решение владельца: добрать появления этих лиц при наполнении соответствующих лекций.

4. **Граф: парных рёбер 5 (норма ≥10) — warning остаётся.** Заполнение всех рёбер курса вне скоупа Л2 (дописал только 3 L2-ребра + 3 L2-нити). Нити-квоты (≥3 через ≥5, ≥6 через ≥4, ≥4 типа) теперь **выполнены**. Недобор рёбер — известный общекурсовой добор, не дефект Л2.

5. **Ошибки в READ-ONLY входах (не правил, флаг).** Seed v3 содержит ровно те неточности, что ловит `research`: (а) «Бренье доказывает то, что **не смог Монж**» — research требует «Монж не доказал **существования**», не «не смог для |x−y|²»; (б) Канторович подан как «вывел ЛП из треста (1939)… расщепляя массу» без разнесения дат 1939/1942/1948. При влитии **поправил** в L2; сам v3 НЕ трогал (READ-ONLY) — флаг для возможной правки v3 отдельным заходом.

6. **Мелочь — био Стиглера.** В `lica.md` добавил «Нобель по экономике 1982» из общего знания (в `research` этого не было; дата верна, но субагентом не верифицировалась). При желании — досверить.

## ОТЧЁТ

**Итог: ГОТОВО.** Версия v3 §Оптимизация влита и улучшена до формата v4 в `istochnik/L2-optimizaciya.md` по лестнице `kompoziciya §4` (ред.2) и фактуре `research`. Линтер зелёный (exit 0, 0 структурных ошибок), полная сборка `build.py` проходит (L4-HTML собирается), верификатор — PASS. Метод Л4: брал готовое (v3), усиливал хирургически.

### Что увидел
- Скелет `L2-optimizaciya.md` — заглушка из 9 строк. Seed v3 §Оптимизация (стр.277–306) — написанный текст, 3 портрета (Данциг/фон Нейман/Канторович base64), кейсы почки-Рот/AA/IMRT, связи →Л6/Л7, но с двумя неточностями (см. ВОПРОСЫ §5).
- Контракт линтера (`build.py`): **матблок = §-раздел типа МАТЕМАТИКА/ТЕХБЛОК/ЧЁРНЫЙ-ЯЩИК с 4 полями**. Структурный гейт жёсткий; редакц. (≥20 матблоков, домашка 5–10, квоты графа, лица 2–5) при `chernovik` — мягкие warnings. Базовая линия до правок — зелёная (exit 0; L4 сам 8/20 матблоков).

### Что сделал и ЗАЧЕМ
1. **ПЕРВЫЙ ХОД — zip-бэкап** `snapshots/istochnik_pre-L2_2026-06-30_230442.zip` (git нет, единственная точка отката).
2. **Извлёк 6 портретов из v3** в `_dizajn/foto/` (danzig, kantorovich, fon-neyman, monzh, villani, figalli — реальные jpg base64; не плейсхолдеры). Stigler/Brenier — без фото в v3, в `lica.md` стоят именами-плейсхолдерами (добор позже, не блокирует).
3. **Наполнил `L2-optimizaciya.md`**: шапка по (а) (`cel: Как находить лучшее`, `teorema-zagolovok`, `theory:[lin-programmirovanie, opt-transport]`, `budget {20,38}`, `status: chernovik`); тело — **26 §-разделов, ровно 20 матблоков**. Ось ред.2 выдержана: §1 крючок практическими задачами (НЕ персона) → §2–6 ядро 1 рацион руками → §7–9 сведе́ние+трест → §10–14 ядро 2 транспорт руками → §15–17 абстрактный подъём (двойственность=минимакс) → §18–20 возврат к лин.системам (симплекс/Стиглер) → §21 парковка (Кли–Минти) → §22–25 кольцо → §26 домашка. **Данциг — в §18–20, не в зачине** (байка про «домашку» — поле-сноска §20). Кейсы v3 (почки-35/Рот, AA $1,4 млрд, IMRT) подняты в зачин (§1) и сведе́ние (§7).
4. **Два примера руками — точные числа из research:** рацион `min 2x₁+3x₂` → вершины (0,8)=24/**(3,2)=12**/(9,0)=18 (§5); транспорт → оптимум **230** vs северо-западный 345, потенциалы u=[0,−2,−3]/v=[7,6,3], редуцированные ≥0 как сертификат двойственности (§11–12).
5. **8 честных флагов вшиты** (все подтверждены верификатором): даты Канторовича 1939/1942/1948 разнесены; минимакс=частный случай, не равносильность (Гейл–Кун–Таккер 1951); Бренье — Монж не доказал *существования*; m+n−1=*базисные*, не «ненулевые»; WGAN — обнуляются *расхождения* KL/JS; осн.теорема ЛП «на компакте»; Данциг «две нерешённые» + Вальд; Монж-фортификатор школы Мезьер (1781), «инженер при Наполеоне» — анахронизм. SABRE/ORION в лекцию НЕ вносил.
6. **Карта (`karta/`, только дописывал):** `lica.md` +8 лиц (danzig, fon-neyman, kantorovich, monzh, villani, figalli, brenier, stigler); `teorii.md` +{lin-programmirovanie}+{opt-transport} (вписаны в `theory:` шапки); `graf.md` +3 нити (universalnost, chelovek-mashina, spryatannaya-geometriya) +3 ребра (optimizaciya→myshlenie, optimizaciya→slozhnost, optimizaciya~granicy). Все `→[id]` из L2 резолвятся.

### Как проверил (вывод линтера дословно)
```
── ЛИНТЕР ──
  ⚠ редакц.: kriptografiya: 8/20 матблоков
  ⚠ редакц.: граф: парных рёбер 5 (норма ≥10)
  ⚠ редакц.: лицо {danzig}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {kantorovich}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {monzh}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {villani}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {figalli}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {brenier}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {stigler}: появлений в 1 лекциях (норма 2–5)
  ✓ структурные проверки пройдены (9 редакц. замечани(й))
```
`--lint` exit **0**; полный `build.py` exit **0** → `✓ istochnik/_out/L4-kriptografiya.html (461 КБ)`. **0 структурных ошибок, 0 битых ссылок.** `optimizaciya: 20/20 матблоков` (строка-предупреждение отсутствует = норма выполнена). Все 9 warnings — мягкие и разобраны в ВОПРОСЫ (kriptografiya — чужая лекция; рёбра графа и single-appearance лица — общекурсовой добор, не дефект Л2).

### Что НЕ трогал
Другие лекции (L1,L3,L5–L7); `build.py`, `SHEMA-istochnika.md`, `_dizajn/shablon.html`, `stil.css`; READ-ONLY входы (`chernovik-kursa-v3.html`, `kompoziciya-L2.md`, `research-L2.md`); `1-START-HERE/sistema-ocenki.md`; существующие записи `karta/` (только дописывал). HTML-сборку L2 не требовал (генератор хардкодит kriptografiya — анти-скоуп).

### Результат верификатора (свежий субагент, другой метод)
**PASS.** Все 9 блоков композиции приземлились (потерь нет); порядок = ред.2; оба ядра руками на месте; Данциг вне §1; v3 сохранён полностью (портреты, кейсы, мосты); честность — числа/даты корректны, запрещённых формулировок нет. Нашёл **1 содержательный дефект:** не приземлился честный флаг о Монже (фортификатор Мезьер / «не инженер Наполеона», 1781). **Дефект устранён** — врезка + поле:честно добавлены в §13; перепроверено точечным grep (фортификатор/Мезьер/1781 — есть; буквального утверждения «инженер Наполеона» — нет; линтер остался exit 0). Остальное verifier подтвердил без потерь.

### Вопросы владельцу
6 пунктов в `## ВОПРОСЫ` — ключевые: (1) конфликт «13–15 разделов» vs «≥20 матблоков» в заходе (разрешил в пользу ≥20 по §2.5.1); (2) семантический рипл рамки `cel` на манифест/Л1-переход/Л7-мост — вне скоупа, **на ревью аналитику** (PROTOKOL §8).

### Время и токены
Прогон: ~23:04→23:24 MSK, **≈20 мин** (30 июня 2026). Токены: команда `/cost` в этом harness программно недоступна — точную цифру владелец видит в `/cost` интерактивного клиента; израсходовано в пределах одной сессии (1 субагент-верификатор ≈81k токенов + основной проход).
