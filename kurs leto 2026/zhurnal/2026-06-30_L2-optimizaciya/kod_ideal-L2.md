# Канал Claude Code — Л2: флип в `status: polno` (довести до идеала, шаг 1)

> **Для Claude Code:** твой ЕДИНСТВЕННЫЙ файл-заход. Читай ТОЛЬКО его и названное ниже; остальной проект не изучай (`CLAUDE.md`). Папка НЕ под git → ПЕРВЫМ ХОДОМ zip-бэкап. План/вопросы/отчёт — в секции внизу. Сильная модель; метрика — КАЧЕСТВО; верификатор — субагенту.

## ЗАДАЧА (один проход)

**КОНТЕКСТ.** Лекция **Л2 «Оптимизация»** (`istochnik/L2-optimizaciya.md`) собрана, добор из книги-референса вшит, и только что прошла обкатку-гейт (`obkatka-L2.md`, аналитик, 2026-07-01): **11/11 ворот, полосы A/B «силён/силён», §2.5.9/§2.5.10 без провалов, ни одна из 5 дыр L4 не повторилась**. `status: chernovik` до сих пор, потому что редакционные проверки линтера (≥20 матблоков, домашка 5–10) при `chernovik` — только МЯГКИЕ (warnings); при `status: polno` они становятся ЖЁСТКИМИ (эскалация в ошибки, см. `build.py` строки 487–524 — уже прочитано аналитиком, тебе читать не нужно). Обкатка нашла ровно ДВЕ мелкие остаточные вещи, не дыры стандарта: (1) шапка держит `budget: { math_blokov: 20, ...}`, а фактических матблоков **21** (20 секций типа МАТЕМАТИКА + 1 полный 4-польный матблок внутри §23-ТЕХБЛОК-парковка — посчитан аналитиком построчно и через `grep -c '\[утверждение\]'`); (2) статус пора флипнуть. **ЦЕЛЬ этого захода:** поправить метаданные шапки + флипнуть статус + прогнать линтер в ЖЁСТКОМ режиме и убедиться, что Л2 держит `polno` чисто. Приёмка — по `## ОТЧЁТ`.

### 0. ПЕРВЫЙ ХОД
- Папка `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026`. СРАЗУ zip-бэкап `istochnik/` в `snapshots/`.
- Прочитать ТОЛЬКО:
  - `istochnik/L2-optimizaciya.md` — правишь только шапку (YAML между `---`), тело НЕ трогаешь.
  - `istochnik/_generator/build.py` + `README.md` — линтер (запускаешь, не правишь). Обрати внимание на строки ~487–524 (`edit`/эскалация при `polno`) — уже прочитаны аналитиком, просто ориентир, что именно будет проверяться жёстко.
- ПЛАН в `## ПЛАН` перед действиями.

### 1. ДИСЦИПЛИНА
Правка **ТОЛЬКО двух полей шапки** (`math_blokov`, `status`) — якорная замена, abort при ≠1 совпадении. Тело лекции (после второго `---`) не трогать НИ СИМВОЛА. Линтер после правки. Сохранять по умолчанию; спорное — в `## ОТЧЁТ`. **Время прогона + токены** (`/cost`) — в отчёт.

### 2. ГРАНИЦЫ
- Только шапка `istochnik/L2-optimizaciya.md` (2 поля: `budget.math_blokov` 20→21, `status` `chernovik`→`polno`).
- НЕ трогать: тело Л2 (§1–§28, все матблоки/флаги/поля), другие лекции, `build.py`/SHEMA/`_dizajn`/`karta/`, `sistema-ocenki.md`, `research-L2.md`/`kompoziciya-L2.md`/`obkatka-L2.md` (read-only).
- **НЕ трогать `budget.vsego_blokov` (38)** — это поле линтером не проверяется (только `math_blokov` участвует в жёсткой проверке — см. `budget()` в `build.py`), править его не входит в задачу.
- **Граф-квоты (парных рёбер, лицо в 2–5 лекциях) НЕ в периметре.** Это общекурсовой недобор (соседние лекции — скелеты), уже разобран и ратифицирован владельцем (SESSIYA #6 арки, п.3–4); в коде эти проверки помечены статусом «—» и НЕ эскалируются флипом Л2 в `polno` (проверено аналитиком по исходнику `build.py`). Если линтер их всё равно покажет — это ожидаемые WARNINGS, не твоя задача их чинить.

### 3. ЗАДАЧА
1. В шапке `istochnik/L2-optimizaciya.md` поменять `budget: { math_blokov: 20, vsego_blokov: 38 }` → `budget: { math_blokov: 21, vsego_blokov: 38 }`.
2. Там же `status: chernovik` → `status: polno`.
3. Прогнать `python3 istochnik/_generator/build.py --lint`.

**КРИТЕРИЙ ГОТОВНОСТИ:**
- `--lint` → **exit 0**, **0 структурных ошибок**, и **0 строк `[polno]`** для `optimizaciya` (то есть матблоков/домашка Л2 держат порог и под жёстким режимом — ожидается чисто, т.к. 21≥21 и домашка 8 задач ∈ [5,10], но подтвердить фактическим прогоном, не на веру).
- `diff` шапки против старт-снапшота показывает РОВНО 2 изменённые строки (или 1, если оба поля на одной строке budget) — только `math_blokov` и `status`; тело лекции (после второго `---`) **побайтово идентично** снапшоту.
- Другие лекции и файлы не тронуты (сверка со снапшотом).

### 4. ВЕРИФИКАТОР (свежий субагент, другой метод)
- Сверить тело Л2 (всё после второго `---`) с бэкапом побайтово (sha256 или `diff`) — расхождений 0.
- Независимо прогнать `--lint` и подтвердить: exit 0, 0 структурных, 0 `[polno]`-эскалаций для `optimizaciya`.
- Подтвердить, что другие лекции/файлы (L1,L3–L7, `build.py`, `sistema-ocenki.md`, `karta/`) побайтово не тронуты.
Результат — в `## ОТЧЁТ`.

### 5. ОТЧЁТ → `## ОТЧЁТ`: что поменял (дословно старое/новое значение двух полей) / линтер дословно / diff-сверка тела / что НЕ трогал / результат верификатора / **время + токены**.

---

## ПЛАН

**Разведка (read-only) — выполнена до правки:**
1. ✅ Zip-бэкап `istochnik/` → `snapshots/istochnik_pre-L2-flip-polno_2026-07-01_235633.zip`.
2. ✅ Прочитал шапку `L2-optimizaciya.md` (поля на строках 9–10) и `build.py` целиком.
3. ✅ **Проверил предпосылку «21» по живому коду.** Линтер (`build.py:493`) считает `nmat` как число секций типа {МАТЕМАТИКА, ТЕХБЛОК, ЧЁРНЫЙ-ЯЩИК}, у которых заполнены ВСЕ 4 поля — это не то же самое, что ручной счёт или `grep`. Прогнал импортом модуля: **`nmat = 21`** (20 МАТЕМАТИКА + 1 ТЕХБЛОК §23 с полными 4 полями). Совпало с ручным счётом аналитика. Значит при `math_blokov: 21` → `nmat (21) < mb (21)` = False → без эскалации.
4. ✅ Домашка Л2: **`nhw = 8`** ∈ [5,10] → без эскалации.
5. ✅ Baseline-линтер (chernovik): exit 0, структурно чисто; для `optimizaciya` НИ ОДНОГО замечания (ни матблоки, ни домашка). Все warnings — чужие: `kriptografiya` (L4, остаётся chernovik) и граф/лица (статус «—»), они флипом Л2 не эскалируются.
6. ✅ Зафиксировал sha256 тела Л2 (после 2-го `---`) до правки: `73cc7a1b…9cfb62`.

**Мутация (по плану):**
7. Якорная замена строки 9: `math_blokov: 20` → `21` (оба поля budget на одной строке — правлю только число `20`→`21`, `vsego_blokov: 38` не трогаю).
8. Якорная замена строки 10: `status: chernovik` → `status: polno`.
9. Прогнать `python3 istochnik/_generator/build.py --lint` (жёсткий режим для polno).

**Сверка:**
10. Тело Л2 (после 2-го `---`) — sha256 идентичен baseline.
11. diff шапки — ровно 2 строки.
12. Верификатор (свежий субагент, другой метод) — независимая сверка.

## ВОПРОСЫ
Нет. Единственная нетривиальная развилка (равен ли линтерный `nmat` числу 21 из шапки) снята фактическим прогоном — см. ПЛАН п.3. Задача исполнима без решений владельца.

## ОТЧЁТ

**Итог: цель достигнута.** Л2 переведена в `status: polno`, метаданные шапки согласованы с фактом, линтер в жёстком режиме держит `polno` чисто. Тело лекции не тронуто ни на байт. Независимый верификатор (свежий субагент, другой метод) подтвердил всё четырьмя проверками.

### Что поменял (ровно 2 поля шапки `istochnik/L2-optimizaciya.md`, обе на строках 9–10)
| | было | стало |
|---|---|---|
| строка 9 | `budget: { math_blokov: 20, vsego_blokov: 38 }` | `budget: { math_blokov: 21, vsego_blokov: 38 }` |
| строка 10 | `status: chernovik` | `status: polno` |

`vsego_blokov: 38` — не тронут (вне периметра, линтером не проверяется).

### Проверка предпосылки «21» (до правки, по живому коду)
Линтер (`build.py:493`) считает `nmat` не как `grep`, а как **число секций** типа {МАТЕМАТИКА, ТЕХБЛОК, ЧЁРНЫЙ-ЯЩИК} с полными 4 полями. Прогнал импортом модуля: **`nmat = 21`** (20 секций МАТЕМАТИКА + 1 ТЕХБЛОК §23, у которого заполнены все 4 поля). Совпало со счётом аналитика. Домашка Л2: **`nhw = 8`** ∈ [5,10]. → При `math_blokov: 21` ни матблоки, ни домашка не дают эскалации. Предпосылка подтверждена ДО мутации, не на веру.

### Линтер — дословно (жёсткий режим, после правки)
```
── ЛИНТЕР ──
  ⚠ редакц.: kriptografiya: 9/20 матблоков
  ⚠ редакц.: kriptografiya: домашка 11 задач (норма 5–10)
  ⚠ редакц.: граф: парных рёбер 5 (норма ≥10)
  ⚠ редакц.: лицо {danzig}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {kantorovich}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {monzh}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {villani}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {figalli}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {brenier}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {stigler}: появлений в 1 лекциях (норма 2–5)
  ✓ структурные проверки пройдены (10 редакц. замечани(й))
exit=0
```
**Критерий готовности выполнен:** exit 0 · 0 структурных ошибок · **0 строк `[polno]`** · для `optimizaciya` — ни одной строки (ни ошибки, ни warning). Набор из 10 warnings **побайтово тот же, что на baseline (chernovik)** — флип Л2 не добавил и не эскалировал ничего. Все они чужие: `kriptografiya` (L4, остаётся `chernovik`) и граф/лица (статус «—»), эскалации не подлежат — это ожидаемо и вне периметра (см. ГРАНИЦЫ §2 захода).

### Сверка тела и diff
- **Тело Л2 (после 2-го `---`) — побайтово идентично** снапшоту: sha256 до = после = `73cc7a1b06a7d97ec4af58ff3f43c768768574b9f52d2cbd5ff7434d339cfb62` (35814 байт).
- `diff` файла против снапшота — **ровно 2 изменённые строки** (9 и 10), больше ничего.
- `diff -rq` всего дерева `istochnik/` против снапшота — **отличается только `L2-optimizaciya.md`**, ни одного «Only in …», остальные файлы идентичны.

### Что НЕ трогал
Тело Л2 (§1–§28, все матблоки/поля/флаги/сноски); `budget.vsego_blokov`; другие лекции (L1, L3–L7); `_generator/build.py`; `karta/`, `_dizajn/`; `sistema-ocenki.md`; аналитические доки (`research/kompoziciya/obkatka-L2.md`). Граф-квоты и «лицо в 2–5 лекциях» не чинил — вне периметра, ратифицировано владельцем.

### Верификатор (свежий субагент, независимый метод) — все 4 проверки PASS
1. **Тело не изменено** — sha256 бэкапа = текущего (`73cc7a1b…9cfb62`, 35814 байт). PASS.
2. **Шапка = ровно 2 задуманных изменения** (`math_blokov` 20→21, `status` chernovik→polno; `vsego_blokov` не тронут). PASS.
3. **Линтер** — exit 0, `✓ структурные проверки пройдены`, `[polno]`-строк 0, упоминаний `optimizaciya` 0. PASS.
4. **Нет побочек** — `diff -rq` дерева: отличается только `L2-optimizaciya.md`, всё прочее (`build.py`, `karta/`, `_dizajn/`, `sistema-ocenki.md`, L1, L3–L7) побайтово идентично. PASS.

### Бэкап
`snapshots/istochnik_pre-L2-flip-polno_2026-07-01_235633.zip` — сделан первым ходом до любых правок (точка отката: git в папке нет).

### Время + токены
- Верификатор-субагент: **≈59 с**, 6 tool-uses, **≈45 845 токенов** (из его usage-отчёта).
- Основной проход: старт ≈ 23:56, финал ≈ 00:05 — **≈9 мин** настенного времени (большая часть — фоновое ожидание верификатора; сама правка тривиальна).
- Полную стоимость сессии в токенах смотри командой `/cost` в CLI — программно из захода она мне недоступна, поэтому точную цифру по всему проходу привести не могу (только цифру субагента выше).
