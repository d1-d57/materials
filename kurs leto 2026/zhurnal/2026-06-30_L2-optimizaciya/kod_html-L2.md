# Канал Claude Code — генерализация build.py + сборка Л2 HTML

> **Для Claude Code:** твой ЕДИНСТВЕННЫЙ файл-заход. Читай ТОЛЬКО его и названное ниже; остальной проект не изучай (`CLAUDE.md`). Папка НЕ под git → ПЕРВЫМ ХОДОМ zip-бэкап. План/вопросы/отчёт — в секции внизу. Сильная модель; метрика — КАЧЕСТВО; верификатор — субагенту.

## ЗАДАЧА (один проход)

**КОНТЕКСТ.** Л2 «Оптимизация» (`istochnik/L2-optimizaciya.md`) прошла обкатку-гейт и переведена в `status: polno` (SESSIYA #10–#11 арки). Осталось собрать её в HTML — но `istochnik/_generator/build.py` в `main()` **хардкодит единственную лекцию** (`kriptografiya` → `_out/L4-kriptografiya.html`, строки ~618–628); это старый анти-скоуп, снятый владельцем именно для этого захода. **Аналитик уже провёл read-only аудит** (не переоткрывай): хардкод — ТОЛЬКО в `main()` (выбор `target` + имя выходного файла); `build_lecture()`, `render_section()`, `render_ref()`, `lint()` и весь остальной код **уже общие** — используют `lec.id`/`lec.header.get("poryadok")`, ни одной привязки к `kriptografiya` вне `main()`. Сейчас нескелетных лекций две: `optimizaciya` (`polno`) и `kriptografiya` (`chernovik`); L1/L3/L5–L7 — `skelet`. **ЦЕЛЬ:** добавить CLI-флаг для выбора лекции (обратная совместимость — без флага прежнее поведение байт-в-байт), собрать `L2-optimizaciya.html`, попутно починить 4 стале-номера параграфов в `karta/lica.md` (см. п.2 ниже — найдено аналитиком при аудите, не влияет на линтер, но документация должна быть верной). Приёмка — по `## ОТЧЁТ`.

### 0. ПЕРВЫЙ ХОД
- Папка `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026`. СРАЗУ zip-бэкап `istochnik/` в `snapshots/` (включая текущий `_out/L4-kriptografiya.html` — эталон регрессии).
- Прочитать ТОЛЬКО:
  - `istochnik/_generator/build.py` — весь файл, но менять только `main()` (~строки 586–628).
  - `istochnik/karta/lica.md` — только 4 строки с `{villani}`, `{figalli}`, `{brenier}`, `{monzh}` (п.2 ниже).
  - `istochnik/L2-optimizaciya.md` — НЕ править, только для сверки §-номеров, если нужно перепроверить п.2.
- ПЛАН в `## ПЛАН` перед действиями.

### 1. ДИСЦИПЛИНА
Минимальная правка + проверка; якорные замены — abort при ≠1 совпадении. Линтер и обе сборки (L4 старым путём + L2 новым) после правок. Сохранять по умолчанию; спорное — в `## ОТЧЁТ`. **Время прогона + токены** (`/cost`) — в отчёт.

### 2. ГРАНИЦЫ
- Код: только `main()` в `build.py` (добавить CLI-выбор лекции). **НЕ трогать** `build_lecture`, `render_*`, `lint`, `load_registry` — они уже общие, трогать их не нужно и НЕ входит в задачу.
- Контент: только 4 строки `karta/lica.md` (см. ниже — числа §-номеров, не текст ролей/био).
- **НЕ трогать:** тело `L2-optimizaciya.md` / `L4-kriptografiya.md` / любой другой лекции, `sistema-ocenki.md`, `_dizajn/` (CSS/шрифты/фото), `SHEMA-istochnika.md`.
- Существующий `_out/L4-kriptografiya.html` — после генерализации должен остаться **байт-в-байт идентичным** (регрессия, гейт ниже).

**(2) Попутный микро-фикс `karta/lica.md` (найдено аудитом, не блокирует основную задачу, но дёшево починить в этом же заходе):** после добора (SESSIYA #8 арки) в `L2-optimizaciya.md` сменились номера §, но аннотации «появления» 4 персон в `lica.md` остались от ДО-доборной нумерации. Линтер это не проверяет (`read_lica_appearances()` берёт только id лекции до символа «§», само число не участвует ни в одной проверке — не структурный баг), но документация должна отражать факт. Поправить ТОЛЬКО номера §, ничего больше:
  - `{villani}`: `optimizaciya§25(финал)` → `optimizaciya§27(финал)`
  - `{figalli}`: `optimizaciya§25(финал)` → `optimizaciya§27(финал)`
  - `{brenier}`: `optimizaciya§22(T=∇φ), optimizaciya§25(финал)` → `optimizaciya§24(T=∇φ), optimizaciya§27(финал)`
  - `{monzh}`: `optimizaciya§13(песок), optimizaciya§22(линейная стоимость)` → `optimizaciya§13(песок), optimizaciya§24(линейная стоимость)`
  Сверить по факту в `L2-optimizaciya.md` перед правкой (§24 — Бренье, §27 — кольцо/Виллани-Фигалли) — не переписывать вслепую.

### 3. ЗАДАЧА
**(1) Генерализация `main()`.** Добавить `ap.add_argument("--build", metavar="ID", help="собрать конкретную лекцию по id (по умолчанию — kriptografiya, для обратной совместимости)")`. Логика сборки:
```
build_id = args.build or "kriptografiya"
target = reg["lecture"].get(build_id)
if not target:
    print("нет лекции %s — нечего собирать" % build_id); return 1
lec = target["lec"]
if lec.status == "skelet":
    print("лекция %s — status: skelet, собирать нечего" % build_id); return 1
html_out = build_lecture(lec, reg)
out_path = OUT / ("L%s-%s.html" % (lec.header.get("poryadok", ""), build_id))
out_path.write_text(html_out, encoding="utf-8")
```
(Псевдокод — подгони под реальный стиль функции, не копируй дословно если не совпадёт с окружающими строками.) Обнови docstring-подсказку наверху файла (строки ~7–9) — добавь пример `--build optimizaciya`.

**(2) Прогнать сборку L2:** `python3 istochnik/_generator/build.py --build optimizaciya` → должен создать `istochnik/_out/L2-optimizaciya.html`.

**(3) Прогнать регрессию L4:** `python3 istochnik/_generator/build.py` (без флагов, старый вызов) → должен пересобрать `_out/L4-kriptografiya.html`; сверить с бэкапом (sha256) — обязан быть **идентичен**, иначе генерализация что-то сломала в общем коде (это и есть гейт «не сломал старое»).

**(4) Микро-фикс п.2** в `karta/lica.md`.

**КРИТЕРИЙ ГОТОВНОСТИ:**
- `python3 build.py --lint` → exit 0, без новых структурных/редакционных отличий от текущего baseline.
- `python3 build.py --build optimizaciya` → exit 0, создан `_out/L2-optimizaciya.html`, размер разумный (порядка сотен КБ — база64-фото), открыт как текст — содержит `Оптимизация`, `Основная теорема линейного программирования`, не содержит python-трейсбэков/пустых плейсхолдеров вместо текста.
- `python3 build.py` (без аргументов) → пересобирает `_out/L4-kriptografiya.html`, **sha256 = тому, что было ДО правки** (бэкап). Это главный регрессионный гейт.
- `diff` `build.py` против бэкапа — изменения **только внутри `main()`** (+docstring-строка), ничего в `render_*`/`lint`/`load_registry`.
- `karta/lica.md` — ровно 4 отредактированные строки (номера §), остальной файл идентичен бэкапу.

### 4. ВЕРИФИКАТОР (свежий субагент, другой метод)
- Независимо прогнать оба вызова (`--build optimizaciya`, без аргументов) и сверить с отчётом.
- **Regression-гейт:** sha256 `_out/L4-kriptografiya.html` до/после — совпадают.
- `diff -rq` дерева `istochnik/` против бэкапа: отличия ТОЛЬКО в `_generator/build.py`, `karta/lica.md`, `_out/L2-optimizaciya.html` (новый файл) — больше нигде.
- Открыть `_out/L2-optimizaciya.html` как текст: подтвердить наличие заголовка лекции, нескольких матблоков, портретов (grep по `background-image:var(--p-` для людей с фото и `class="port mono"` — ожидаются моно-плейсхолдеры для **brenier** и **stigler**, у них НЕТ файла фото в `_dizajn/foto/` — это НЕ баг генератора, а недостающий асет; зафиксировать в отчёте как честный факт, не чинить самому — рисовать/искать портреты вне периметра этого захода).
Результат — в `## ОТЧЁТ`.

### 5. ОТЧЁТ → `## ОТЧЁТ`: что изменил в `main()` (дословный diff) / что изменил в `lica.md` (4 строки, было→стало) / линтер дословно / регрессия L4 (sha256 до/после) / L2-HTML создан (размер, ключевые grep) / что НЕ трогал / результат верификатора / честный факт про отсутствующие `brenier.jpg`/`stigler.jpg` / **время + токены**.

---

## ПЛАН

**Разведка (сделано, read-only):**
- Бэкап `istochnik/` → `snapshots/istochnik_pre-L2-build_2026-07-02.zip` (711 КБ). ✓
- Эталон регрессии: sha256 `_out/L4-kriptografiya.html` = `cc51576b999ca7137c3d1c5ad9952416d534bdae737ac281fe97bdab6af05616`.
- `build.py` прочитан. Хардкод подтверждён строго в `main()` (стр. 619, 624); `build_lecture`/`render_*`/`lint`/`load_registry` — общие. Имя выходного файла в `main()` жёстко `"L4-kriptografiya.html"`.
- Заголовки лекций: kriptografiya `poryadok:4 status:chernovik`; optimizaciya `poryadok:2 status:polno`; L1/L3/L5–L7 `skelet`. → Генерализованное имя `L{poryadok}-{id}.html` даёт для kriptografiya ровно `L4-kriptografiya.html` (совпадение байт-в-байт гарантировано, т.к. `build_lecture` не трогаю).
- §-номера сверены по `L2-optimizaciya.md`: §24 = «Бренье: …градиент», §27 = «Кольцо: песок двигает пиксели» (финал: блокада + Виллани/Фигалли/Бренье). Все 4 правки захода подтверждены фактом. **Нашёл 5-й стале-номер** — см. `## ВОПРОСЫ` (не трогаю, вне периметра 4 строк).

**Мутация (по плану):**
1. `build.py` `main()`: добавить `--build ID` (после `--backlinks`); заменить блок хардкод-сборки (стр. 618–627) на общий выбор лекции с дефолтом `kriptografiya`, гейтом `skelet`→return 1 и именем `L{poryadok}-{id}.html`. Обновить docstring (стр. 7–9): пример `--build optimizaciya`.
2. `karta/lica.md`: ровно 4 строки (villani, figalli, brenier, monzh) — только числа §.
3. Прогоны: `--lint` (гейт 0), `--build optimizaciya` (создать L2), без флагов (регрессия L4 → sha256 == эталон).
4. Верификатор: свежий субагент, независимо прогоняет оба вызова, `diff -rq` дерева, grep по L2-HTML.

## ВОПРОСЫ

**Q1 — найден 5-й стале-номер §, не входящий в 4 строки захода (не тронут, жду решения владельца).**
При сверке §-номеров обнаружил, что у `{kantorovich}` (строка 7 `lica.md`) аннотация появления `optimizaciya§25(блокада — кольцо)` **тоже устарела**, ровно по той же причине, что и 4 правки захода (добор SESSIYA #8 сдвинул нумерацию).
Доказательство по факту `L2-optimizaciya.md`:
- §25 сейчас = «Расстояние Вассерштейна → нейросеть рисует» (стр. 198) — не про блокаду.
- §27 = «Кольцо: песок двигает пиксели» (стр. 216), тело стр. 218: «Зима 1941–42, блокадный Ленинград. Канторович… на льду Ладоги — „Дороги жизни“…». Это и есть «блокада — кольцо».
Т.е. корректно `optimizaciya§25(блокада — кольцо)` → `optimizaciya§27(блокада — кольцо)` (прочие два появления Канторовича — §9 фанерный трест, §13 релаксация — совпадают с фактом, менять не нужно).
Аудит этот 5-й номер, похоже, пропустил. Линтер к нему безразличен (как и к 4 заявленным — берёт только id до «§»). **Заход жёстко ограничивает контент-правку 4 строками и требует «ровно 4 отредактированные строки», поэтому kantorovich НЕ трогал.** Если владелец подтвердит — это одна строковая замена в том же стиле.

## ОТЧЁТ

**Итог: задача выполнена, все критерии готовности и независимый верификатор — PASS.**

### Бэкап
`snapshots/istochnik_pre-L2-build_2026-07-02.zip` (711 КБ, всё дерево `istochnik/` включая эталонный `_out/L4-kriptografiya.html`).

### Что изменил в `main()` (`build.py`) — дословный diff
```
--- docstring (стр. 7) ---
-  python3 istochnik/_generator/build.py              # линтер(структурный гейт) + сборка L4
+  python3 istochnik/_generator/build.py              # линтер(структурный гейт) + сборка L4 (kriptografiya, по умолчанию)
+  python3 istochnik/_generator/build.py --build optimizaciya  # собрать конкретную лекцию по id

--- argparse (после --backlinks) ---
+    ap.add_argument("--build", metavar="ID", help="собрать конкретную лекцию по id (по умолчанию — kriptografiya, для обратной совместимости)")

--- блок сборки ---
-    target = reg["lecture"].get("kriptografiya")
-    if not target:
-        print("нет лекции kriptografiya — нечего собирать")
-        return 1
-    html_out = build_lecture(target["lec"], reg)
-    out_path = OUT / "L4-kriptografiya.html"
+    build_id = args.build or "kriptografiya"
+    target = reg["lecture"].get(build_id)
+    if not target:
+        print("нет лекции %s — нечего собирать" % build_id)
+        return 1
+    lec = target["lec"]
+    if lec.status == "skelet":
+        print("лекция %s — status: skelet, собирать нечего" % build_id)
+        return 1
+    html_out = build_lecture(lec, reg)
+    out_path = OUT / ("L%s-%s.html" % (lec.header.get("poryadok", ""), build_id))
```
Обратная совместимость: без флага `build_id="kriptografiya"`, `poryadok=4` → имя `L4-kriptografiya.html`, содержимое от нетронутой `build_lecture` — отсюда байт-в-байт (гейт подтвердил). Добавлен guard `skelet` — внутри `main()`, в границах захода. `build_lecture`/`render_*`/`lint`/`load_registry` **не тронуты**.

### Что изменил в `lica.md` (ровно 4 строки, было→стало — только числа §)
- `{monzh}`: …§13(песок), **§22**(линейная стоимость)] → …§13(песок), **§24**(линейная стоимость)]
- `{villani}`: …**§25**(финал)] → …**§27**(финал)]
- `{figalli}`: …**§25**(финал)] → …**§27**(финал)]
- `{brenier}`: …**§22**(T=∇φ), **§25**(финал)] → …**§24**(T=∇φ), **§27**(финал)]

Сверено по факту `L2-optimizaciya.md`: §24 = «Бренье: …градиент», §27 = «Кольцо: песок двигает пиксели» (финал). Текст ролей/фото/био не менял.

### Линтер (дословно, exit 0)
```
── ЛИНТЕР ──
  ⚠ редакц.: kriptografiya: 9/20 матблоков
  ⚠ редакц.: kriptografiya: домашка 11 задач (норма 5–10)
  ⚠ редакц.: граф: парных рёбер 5 (норма ≥10)
  ⚠ редакц.: лицо {danzig}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {kantorovich}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {monzh}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {villani}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {figalli}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {brenier}: появлений в 1 лекциях (норма 2–5)
  ⚠ редакц.: лицо {stigler}: появлений в 1 лекциях (норма 2–5)
  ✓ структурные проверки пройдены (10 редакц. замечани(й))
```
Все 10 — мягкие `⚠` (не эскалированы: у kriptografiya `chernovik`, а нити/лица считаются глобально). Вывод линтера **идентичен во всех трёх прогонах** — новых структурных/редакционных расхождений от baseline мои правки не внесли. Правка §-номеров линтер не задевает (он берёт id до «§»).

### Регрессия L4 (главный гейт) — PASS
- sha256 ДО: `cc51576b999ca7137c3d1c5ad9952416d534bdae737ac281fe97bdab6af05616`
- sha256 ПОСЛЕ (`python3 build.py` без флагов): `cc51576b999ca7137c3d1c5ad9952416d534bdae737ac281fe97bdab6af05616`
- **байт-в-байт совпадает** → генерализация не сломала общий код.

### L2-HTML создан
- `istochnik/_out/L2-optimizaciya.html`, 537 796 байт (~525 КБ).
- grep: «Оптимизация» ×3, «Основная теорема линейного программирования» ×2, `<title>Лекция 2 «Оптимизация»`.
- python-Traceback — 0; неразвёрнутых плейсхолдеров `{{…}}` — 0.
- Портреты: 7 персон по `→[id]` в теле → 6 карточек `class="port"` с вшитым фото (danzig, figalli, fon-neyman, kantorovich, monzh, villani) + 1 `class="port mono"` (brenier, буквица «Б»).

### Что НЕ трогал
Тело `L2/L4/*.md`, `sistema-ocenki.md`, `_dizajn/` (CSS/шрифты/фото), `SHEMA-istochnika.md`, `build_lecture`/`render_*`/`lint`/`load_registry`. `diff -rq` дерева против бэкапа: отличия **только** в `_generator/build.py`, `karta/lica.md` и новом `_out/L2-optimizaciya.html` — больше нигде.

### Результат верификатора (свежий субагент, независимые прогоны)
Все пункты A–E — **PASS**: регрессия L4 sha256 совпал; `diff -rq` = ровно 3 ожидаемых отличия; `build.py` изменён только в docstring+`main()`; `lica.md` = 4 строки, только §; L2-HTML чист (0 Traceback, 0 висячих плейсхолдеров, 1 `port mono`, 7 фото-var). Вердикт: «ВСЁ СОШЛОСЬ». (Субагент субагентов не порождал — глубина делегирования = 1, как требует обновлённый CLAUDE.md.)

### Честный факт про отсутствующие фото (не баг генератора)
- `_dizajn/foto/brenier.jpg` — **отсутствует** → brenier рендерится как моно-плейсхолдер (`port mono`, буквица). Ожидаемо.
- `_dizajn/foto/stigler.jpg` — **отсутствует**. Но stigler в теле L2 упомянут только прозой (§20 «диета Стиглера»), **без** `→[stigler]`, поэтому портретной карточки не получает вовсе — ни `port`, ни `port mono`. Т.е. заявленное в заходе ожидание «2 моно-плейсхолдера (brenier + stigler)» на деле даёт **1** (brenier); stigler портрета в L2 не имеет. Это данные карты, не код.
- Косметика: генератор вшивает `--p-<id>` для ВСЕХ персон с существующим файлом фото (не только упомянутых) → в `:root` L2 попал `--p-shannon` без карточки (shannon в L2 не фигурирует). Так же было и в L4; поведение `build_lecture`, которое трогать не просили. Не дефект, отмечаю для полноты.
- Портреты искать/рисовать не стал — вне периметра захода.

### ВОПРОС владельцу (см. `## ВОПРОСЫ` Q1)
Найден **5-й** стале-§ вне 4 строк захода: `{kantorovich}` `optimizaciya§25(блокада — кольцо)` устарел так же — блокада/Дорога жизни теперь в §27, не §25. Не тронул (заход требует «ровно 4 строки»), жду решения — это одна замена в том же стиле.

### Время + токены
- **Верификатор-субагент** (точно, из телеметрии): 53 114 токенов, 13 вызовов инструментов, длительность 243 437 мс (~4 мин 3 с).
- **Корневой сеанс**: `/cost` — интерактивная команда, недоступна в этом headless-режиме, поэтому точную цифру по корню дать не могу. Оценочно основной проход — порядка сотен тыс. токенов (чтение `build.py` ~630 строк + L2 ~230 строк + прогоны/grep), заведомо на порядки ниже аварийного порога из CLAUDE.md (9 млн). Ровный ход, без лавины субагентов (спавн один).
