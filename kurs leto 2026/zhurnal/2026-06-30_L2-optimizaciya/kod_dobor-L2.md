# Канал Claude Code — Добор Л2 из книги-референса (только ДОБАВИТЬ)

> **Для Claude Code:** твой ЕДИНСТВЕННЫЙ файл-заход. Читай ТОЛЬКО его и названное ниже; остальной проект не изучай (`CLAUDE.md`). Папка НЕ под git → ПЕРВЫМ ХОДОМ zip-бэкап. План/вопросы/отчёт — в секции внизу. Сильная модель; метрика — КАЧЕСТВО; верификатор — субагенту.

## ЗАДАЧА (один проход)

**КОНТЕКСТ.** Лекция **Л2 «Оптимизация»** уже собрана и принята (`istochnik/L2-optimizaciya.md`, формат v4, `status: chernovik`, 26 §/20 матблоков). Владелец прочитал парную главу золотого референса (Литвак–Райгородский, гл.2 «Менеджмент и многогранники» + приложение) и просит **ДОБАВИТЬ новый интересный материал, НЕ урезая существующее** — только расширить. Харвест уже сделан аналитиком и лежит в `research-L2.md §ДОБОР`. **ЦЕЛЬ:** хирургически вшить добор в `L2-optimizaciya.md`, линтер дозелёна. Приёмка — по `## ОТЧЁТ`.

### 0. ПЕРВЫЙ ХОД
- Папка `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026`. СРАЗУ zip-бэкап `istochnik/` в `snapshots/`.
- Прочитать ТОЛЬКО:
  - `istochnik/L2-optimizaciya.md` — собранная лекция (её РАСШИРЯЕШЬ, не переписываешь).
  - `zhurnal/2026-06-30_L2-optimizaciya/research-L2.md` — **раздел «ДОБОР из Литвака–Райгородского»** (что добавить + слоты). Источник материала.
  - `zhurnal/2026-06-30_L2-optimizaciya/kompoziciya-L2.md` — ось/порядок (ред.2), чтобы добор лёг в линию.
  - `istochnik/L4-kriptografiya.md` — образец формата v4.
  - `1-START-HERE/sistema-ocenki.md` — ТОЛЬКО §2.5 (формат) и **§2.5.10 (подача текста)** — соблюсти.
  - `istochnik/_generator/build.py` + `README.md` — линтер (запускаешь, не правишь).
- ПЛАН в `## ПЛАН` перед действиями.

### 1. ДИСЦИПЛИНА
**Только ДОБАВЛЯТЬ** — существующие блоки/числа/флаги не урезать и не переписывать без нужды (добор = прирост). Якорные замены — abort при ≠1. Линтер после правок. Сохранять по умолчанию; спорное — в `## ОТЧЁТ`. **Время прогона + токены** (`/cost`) — в отчёт.

### 2. ГРАНИЦЫ
- Только `istochnik/L2-optimizaciya.md` (+ при крайней нужде L2-узлы `karta/`, но см. ниже — новые имена идут winks без `→[id]`, карта скорее не нужна).
- НЕ трогать: другие лекции, `build.py`/SHEMA/`_dizajn`, `sistema-ocenki.md`, `research-L2.md`/`kompoziciya-L2.md` (read-only входы). HTML-сборку L2 не требуем (генератор хардкодит kriptografiya).
- **НЕ урезать** существующий текст Л2 (владелец: «не уменьшая рассказ, а увеличивая»).

### 3. ЗАДАЧА — вшить 4 добора из `research-L2.md §ДОБОР` (формат v4, соблюсти §2.5.10):

**(1) ★ НОВЫЙ блок «Математика, обогнавшая компьютер»** — в зоне «возврат к линейным системам / человек→машина» (рядом с симплексом/Стиглером, ≈§18–20), РАССКАЗ + матблок. Это количественный климакс оси «искать оптимум стало просто». Числа (по Биксби-2012, через книгу-референс):
- 1953: вариант диеты (71 перем., 26 огр.) на перфокартной машине — **8 часов**, бо́льшая часть — вручную вставлять перфокарты.
- Эксперимент Биксби (2007): версии CPLEX 1991→2006 на 1892 задачах целочисл. ЛП, на ОДНОМ компьютере → **за 15 лет ×29 000** (только алгоритмы); скачок ~×10 в 1998 (CPLEX 6.5 — 30 лет теории внедрили разом). +Gurobi → алгоритмы суммарно **×469 800** (1991–2012).
- Закон Мура: компьютеры ~×8000 за тот же срок. Афоризм: «лучше старый компьютер + современные методы, чем новейший + методы 90-х».
- **Совместно ×4 миллиарда: задача на 126 лет в 1991-м → 1 секунда в 2012-м.** Не предел: Бертсимас–Кинг (MIT, 2015) → **×450 миллиардов**.
- Имена (Биксби, Бертсимас, Кинг; пакеты CPLEX/Gurobi) — **inline winks, БЕЗ `→[id]`** (не заводить лица в карте). Мостик прозой к человек→машина (Стиглер руками → секунды → ×4 млрд).

**(2) Целочисленное ЛП = NP-трудное** — добор к блоку-парковке (NP-звоночек, ≈§21, →[slozhnost]). Расписания → переменные 0/1 (целочисл. ЛП); симплекс не даёт целого (углы дробные) → **NP-трудно**; методы — **ветвей и границ** + **отсекающие плоскости (Гомори)**. Конкретизирует «не всегда быстро»: дробное ЛП легко, целочисленное — NP-трудно.

**(3) Прикладные кейсы в довесок** (нить практических задач, блок 1 или 3 — НЕ заменяя почки/AA/IMRT): **ж/д Нидерландов** (премия Эдельмана 2008, расписание с 2006); по желанию — **фестиваль хоров** (студенты Univ Twente; считали днями → программа за минуты; певцы спели благодарственную арию) как лёгкий человеческий эпизод (поле:боковой или короткий абзац).

**(4) Формальное «оптимум в углу» на компакте** — в [подробность] или [чёрный-ящик] матблока осн. теоремы ЛП (≈§5–6): книга доказывает для 2 перем./m огр. на **ограниченной** области (оптимум не внутри → на стороне → вдоль стороны цель меняется монотонно → в угол). Подтверждает наш флаг «на компакте». Уровень школы — можно дать кратко.

**КРИТЕРИЙ ГОТОВНОСТИ:**
- `python3 istochnik/_generator/build.py --lint` → exit 0, **0 структурных ошибок, 0 битых ссылок**; `optimizaciya` матблоков **≥20** (после добора — не меньше).
- Добор присутствует (grep): `29 000`/`29000`, `469 800`/`469800`, `4 миллиард`, `126 лет`, `Биксби`, `Эдельман`, `ветвей и границ`/`Гомори`.
- **Существующее НЕ урезано:** оба примера руками (рацион (3,2)=12; транспорт 230) и 8 честных флагов на месте; число строк L2 ВЫРОСЛО против старт-снапшота.
- §2.5.10 соблюдён (мотивация перед формулой, центрированные `$$…$$`, проговорённые мостики); `status: chernovik`.

### 4. ВЕРИФИКАТОР (свежий субагент, другой метод)
- Добор приземлился (grep ключевых чисел/имён выше).
- **Ничего не потеряно:** все §-разделы старта на месте, оба примера руками и 8 флагов целы, строк стало БОЛЬШЕ (сверка со старт-снапшотом).
- `--lint` → битых `→[id]` = 0.
Результат — в `## ОТЧЁТ`.

### 5. ОТЧЁТ → `## ОТЧЁТ`: что добавил и куда / как проверил (линтер дословно) / что НЕ трогал / верификатор / **время + токены**.

---

## ПЛАН

**Разведка (готово).** Прочитаны только названные файлы. Старт-снапшот L2: **217 строк, 20 матблоков**, линтер `--lint` → **exit 0** (только редакц. варнинги, ни одной структурной ошибки). Бэкап: `snapshots/istochnik_pre-L2-dobor_2026-07-01_231823.zip` (sha L2 внутри = live, совпал).

**Карта внутренних §-ссылок (риск реордера).** В прозе L2 по §-номерам ссылается только **§22 (Бренье)** — строки 108 («…финала в §22») и 192 («у Бренье (§22)»). Разделы §21, §23–§26 в прозе не адресуются (только заголовки). Значит при вставке новых секций после §20 достаточно поправить 2 прозаических «§22». Линтер §-прозу не проверяет — правлю руками.

**Хирургия (только ДОБАВИТЬ, ничего не резать):**

1. **Добор ★ (1) — 2 новые секции после §20** (зона «человек→машина», перед парковкой): продолжают «решается за секунды» количественно.
   - `§21 · РАССКАЗ · Математика, обогнавшая компьютер` — 1953 диета 8 ч на перфокартах; эксперимент Биксби 2007 (1892 задачи, один компьютер) → ×**29 000** алгоритмами, скачок ~×10 в 1998; +Gurobi → ×**469 800**; закон Мура ×8000; афоризм; Бертсимас–Кинг (MIT 2015) ×450 млрд. Имена (Биксби, Бертсимас, Кинг, CPLEX, Gurobi) — inline winks, **без `→[id]`**.
   - `§22 · МАТЕМАТИКА · Считаем ускорение: 126 лет в одну секунду` — матблок: `алгоритмы × железо ≈ 4·10⁹`; **126 лет → 1 секунда**. Все 4 поля.
   - Реордер: старые §21→§23 … §26→§28; 2 прозаических «§22»→«§24».
2. **Добор (2) — в §21-парковку (после реордера §23):** в [логика] дописать целочисл. ЛП = NP-трудное (расписания→0/1; углы дробны; **ветвей и границ** + Гомори), →[slozhnost]. Расширение поля, не новая секция.
3. **Добор (3) — прикладные кейсы, в довесок:** в §1 `поле:в-кармане` дописать ж/д Нидерландов (**премия Эдельмана 2008**, с 2006); хоры Univ Twente — `поле:боковой` у §7. Почки/AA/IMRT не трогать.
4. **Добор (4) — в §6 [подробность]:** школьный разбор «оптимум в углу» на **ограниченной** области (внутри улучшимо → на стороне → монотонно → в угол); подтверждает флаг «на компакте». Расширение поля.

**Проверка:** `build.py --lint` → exit 0, 0 структурных/0 битых ссылок; матблоков ≥20; grep ключей; строк стало больше; оба примера руками ((3,2)=12, транспорт 230) и 8 «честно»-флагов целы. Затем свежий верификатор-субагент другим методом. Отчёт — ниже.

## ВОПРОСЫ

Блокирующих нет. Одно интерпретационное решение (принято мной, не блокирую владельца): «★ НОВЫЙ блок … РАССКАЗ + матблок» реализован как **две** секции (РАССКАЗ §21 + МАТЕМАТИКА §22), а не одна — так честнее к формулировке захода, к «увеличивая, не уменьшая» и к §2.5.1 (каждый нарративный узел несёт свой матблок), и повторяет пару §19(матем.)+§20(рассказ). Если владелец хотел один слитный блок — тривиально свести.

## ОТЧЁТ

**Итог: добор вшит, старое не тронуто, линтер зелёный, независимый верификатор — PASS по всем пунктам.** L2 вырос 217 → **236 строк**, матблоков 20 → **21** (≥20). Бэкап до правки: `snapshots/istochnik_pre-L2-dobor_2026-07-01_231823.zip` (sha L2 внутри совпал с live).

### Что добавил и куда (все 4 добора)
1. **★ Две новые секции после §20** (зона «человек→машина», перед парковкой):
   - **§21 · РАССКАЗ · «Математика, обогнавшая компьютер»** — 1953 диета (71 перем., 26 огр.) 8 ч на перфокартах; эксперимент Биксби 2007 (1892 задачи, один компьютер) → алгоритмами ×**29 000**, скачок ~×10 в 1998 (CPLEX 6.5); +Gurobi → ×**469 800**; закон Мура ×8000; афоризм «старый компьютер + новые методы»; Бертсимас–Кинг (MIT, 2015) ×450 млрд. Имена (Биксби, Бертсимас, Кинг, CPLEX, Gurobi) — inline winks, **без `→[id]`** (в карту не заводил). +`поле:нить →[N:chelovek-mashina]`.
   - **§22 · МАТЕМАТИКА · «Считаем ускорение: 126 лет в одну секунду»** — матблок (4 поля): `алгоритмы × железо ≈ 4·10⁹`, разбор ×469 800 = ×29 000 × ×16,2; **126 лет → 1 секунда**; чёрный-ящик — методология Биксби + Бертсимас–Кинг фактом.
2. **Целочисл. ЛП = NP-трудное** — дописан хвост в `[логика]` парковки (§23): расписания → 0/1, углы дробны → NP-трудно, метод **ветвей и границ** + отсекающие плоскости (**Гомори**), →[slozhnost]. Конкретизирует NP-звоночек. Расширение поля, не новая секция.
3. **Прикладные кейсы в довесок** — в §1 `поле:в-кармане` дописано «ж/д Нидерландов (**премия Эдельмана 2008**, с 2006)» (почки/AA/IMRT сохранены); фестиваль хоров Univ Twente — новый `поле:боковой` у §7.
4. **«Оптимум в углу» на компакте** — школьный разбор дописан в `[подробность]` §6 (внутри улучшимо → на стороне → монотонно → в угол); подтверждает флаг «на компакте».

Реордер под вставку: старые §21→§23 … §26→§28; 2 прозаические ссылки на Бренье-секцию «§22»→«§24» (стр. в §13 и §26).

### Как проверил — линтер дословно
`python3 istochnik/_generator/build.py --lint` → **exit 0**:
```
── ЛИНТЕР ──
  ⚠ редакц.: kriptografiya: 9/20 матблоков
  ⚠ редакц.: kriptografiya: домашка 11 задач (норма 5–10)
  ⚠ редакц.: граф: парных рёбер 5 (норма ≥10)
  ⚠ редакц.: лицо {danzig}: появлений в 1 лекциях (норма 2–5)
  … (лица kantorovich/monzh/villani/figalli/brenier/stigler — те же)
  ✓ структурные проверки пройдены (10 редакц. замечани(й))
```
Ни одной строки `✗ структ.`; **0 битых `→[id]`**; все замечания — редакционные и **по L4/графу, не по L2** (были и до правки, к задаче не относятся). Grep ключей — все на месте: `29 000`[1], `469 800`[2], `4 миллиард`[2], `126 лет`[3], `Биксби`[2], `Эдельман`[1], `ветвей и границ`[1], `Гомори`[1].

### Что НЕ трогал
Другие лекции, `build.py`/SHEMA/`_dizajn`, `sistema-ocenki.md`, `research-L2.md`/`kompoziciya-L2.md` (read-only входы). HTML L2 не собирал (генератор хардкодит kriptografiya). Существующий текст Л2 не урезан: `diff` против бэкапа показал, что каждая изменённая строка — либо реордер заголовка (тот же текст, другой §), либо расширение (старый текст целиком внутри нового). Оба примера руками — рацион **(3,2)=12** и транспорт **230** (план `[[0,1,19],[7,18,0],[15,0,0]]`, потенциалы `u=[0,−2,−3]`/`v=[7,6,3]`) — целы; `поле:честно`-флагов 9 = 9 (не уменьшилось; в захо́де ориентир «8» — фактически в старте было 9, ни один не потерян). `status: chernovik` сохранён.

### Верификатор (свежий субагент, другой метод) — PASS по всем 5 пунктам
Независимо: (1) линтер exit 0, матблоков 21≥20, битых `→[id]` нет; (2) добор приземлился (grep ключей + обе новые секции); (3) `diff` старт→новый — каждая `<`-строка сопоставлена с надмножеством/реордером, потерь нет; (4) инварианты целы (рацион 12, транспорт 230, флаги 9=9, строк 217→236 больше); (5) §-кросс-ссылки непрерывны §1…§28, ссылки на Бренье указывают на актуальный §24. Расхождений не нашёл.

### Время + токены
`/cost` — интерактивная CLI-команда, программно из этого окружения недоступна, точную цифру главного потока не выгружу. По телеметрии: верификатор-субагент — **53 945 токенов, 13 tool-calls, ~82 с**. Главный проход — один линейный сеанс (чтение 8 названных файлов → бэкап → ~14 правок → линтер/grep/diff → субагент): грубо ~10–12 мин активной работы. Точный `/cost` владелец может снять сам в интерактивном `claude`.
