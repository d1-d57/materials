# ПЛАН арки «Л2 Оптимизация/ЛП — сборка до идеала»

## ⭐ СЕЙЧАС
> **✅ АРКА ЗАКРЫТА (2026-07-03, SESSIYA #21).** Л6 собрана и принята владельцем как рабочая версия (ворота ✓, баланс математика/байки/приложения ✓, объём 1.5 ч ✓). Durable разнесён по домам: `resheniya.md` п.22 · `beklog.md` «Из арки Л2 Оптимизация» · `dnevnik-i-plan.md` · `biblioteka/ISTOCHNIKI.md §Л2` · `zhurnal/SVODKI.md` (сверху). **Открытый долг** — художественная смысловая ось лекций (Л6 = набор блоков, не сценарий) на ФИНАЛЬНОЙ фазе, после сборки всех лекций. Арк-папка → история.
>
> **НОВАЯ ФАЗА — аддитивные правки Л6 под дрейфнувший концепт (2026-07-03, SESSIYA #18).** Концепт дрейфовал (имя «Может ли машина мыслить?», строка «у каждого своя граница», мотив «Побочный плод», Опт=Л6). Аддитивно: Винер (2-й сквозной, полный РАССКАЗ-блок; ⚠️ не ЛП), Тьюринг-1948 wink, Блэкетт/OR, нить «Побочный плод», герои-биты, сверка cross-ref под новый порядок + поиск новых мостов. Развилки решены владельцем (полный блок Винера; §23 перешить под порядок). Полный ТЗ-список + текст вставок → `pravki-L6.md`; заход → `kod_pravki-L6.md`. **СЕЙЧАС:** авторинг+гейт закрыты, `kod_pravki-L6.md` НАПИСАН и ждёт прогона Code. Владелец шлёт заход в Code → приёмка (отчёт + независимая сверка) → пересборка HTML (`build.py --build optimizaciya`) → SCORECARD §6.2 дельта → флаг риплов → ревью → закрытие арки. Границы: не трогать `poryadok`; эталонную ось не менять; семантич.риплы (§23, «Побочный плод») — владельцу.

> **Довести до идеала ПРИНЯТО (2026-07-01, SESSIYA #11):** `kod_ideal-L2.md` → Code. Метаданные согласованы (`budget.math_blokov` 20→21), **`status: chernovik → polno`**, линтер в жёстком режиме чист для `optimizaciya` (exit 0, 0 структурных, 0 `[polno]`-эскалаций); тело лекции побайтово не тронуто (sha256 подтверждён независимо аналитиком). §2.5.10 (диаграммы протоколов) уточнён — правило неприменимо, если в лекции нет сторон-с-подслушивающим (первый пример — Л2). Финальный SCORECARD §6.2 = тот же, что в `obkatka-L2.md` (содержание не менялось, менялись только метаданные) — считается финальным.

> **HTML Л2 ПРИНЯТО (2026-07-02, SESSIYA #13):** `kod_html-L2.md` — `build.py` генерализован (`--build ID`, обратная совместимость без флага байт-в-байт — regression sha256 L4 совпал), собран `istochnik/_out/L2-optimizaciya.html` (537 796 байт, чисто: 0 traceback, 0 «висячих» `{{…}}`, 7 портретных карточек — 6 фото + 1 моно-плейсхолдер brenier). `karta/lica.md` — 4 стале-номера § починены Code + 5-й найденный Code, но оставленный вне жёстких границ захода (kantorovich §25→§27), починен аналитиком напрямую (однострочный, верифицированный факт). Всё сверено НЕЗАВИСИМО (не на слово): diff -rq дерева = ровно 3 отличия, как заявлено.

> **visual-design-review Л2 HTML ПРОВЕДЕНО (2026-07-02, SESSIYA #14).** Рендер средствами аналитика (playwright/chromium скачать не удалось — сеть блокирует storage.googleapis.com; поставлен WeasyPrint+pypdfium2), 8 срезов, весь документ просмотрен глазами. **Найдены 2 БАГА ГЕНЕРАТОРА (не рендера — подтверждено на уровне сырого HTML-текста, будут одинаковы в любом браузере), общие для build.py, не только Л2:**
> 1. **Утечка сырого LaTeX** — конвертер формул не знает много макросов (`\min \max \text{} \sum \mathsf{} \begin{pmatrix} \nabla \mu \nu \lambda \theta \tfrac \Big \iff \sup \log \qquad \textstyle \mathrm{}`) → ~60 вхождений на Л2 (грязные матрицы §10–12, почти нечитаемая цепочка слабой двойственности §16, худший случай — определение Вассерштейна §25, там же баг замены `\int`→«∈» вместо «∫»). На L4 таких утечек 0 — просто повезло с более простой математикой, баг тот же, дремлющий.
> 2. **Дубли честных флагов** — `> поле:честно/нить/боковой …`, стоящее последней строкой поля матблока, утекает СЫРЫМ текстом в конец предыдущего абзаца И ЕЩЁ РАЗ рендерится корректным блоком ниже. Подтверждено и на Л2 (7-8 мест), и на L4 (5-6 мест) — старый, ранее не пойманный баг (HTML L4 тоже никогда не смотрели глазами до сегодня).
> **Неопределённость (не баг, вероятно ограничение WeasyPrint):** портреты-фото (6 из 7) не показались в моём рендере — похоже, WeasyPrint слабо держит `aspect-ratio` на пустом `<div>` (у brenier-моно с текстом внутри всё ок). Тест `var()`-фона в изоляции прошёл чисто → вероятно, в реальном браузере фото на месте. **Проверить, когда откроешь файл.**
> Полный разбор — просьба владельца прочитать HTML сам; отчёт не плодил отдельным файлом (устно/в чате).

> **Генератор ПОЧИНЕН (2026-07-02, SESSIYA #16): `kod_fix-latex.md` ПРИНЯТО.** Оба бага закрыты в `build.py` (только `_CMD`+`_matrix_to_text`+`fields_of`, ничего больше). Сверено независимо и глубже критерия захода — полный дословный diff обеих лекций (Л2 145 строк/24 хунка, Л4 46/6) прочитан целиком глазами: каждый хунк либо чистка формулы, либо снятие дубль-хвоста, ни одной правки прозы/факта/числа. Grep-счётчики (латех-утечки/`поле:`/`∈t`) — 0 везде на свежих файлах, `∫` в Вассерштейне — ровно 2. `diff -rq` дерева — ровно 3 файла отличаются. Попутно Code почистил 2 доп. орфан-бэкслеш артефакта той же природы (§175, §11 `X^*`) — не просили явно, но по духу задачи, проверено — реальны и в бэкапе, оба исправлены.

> **Дешёвый повторный визуальный обзор ПРОВЕДЁН (2026-07-02, SESSIYA #17).** Весь документ (6 срезов, переиспользован WeasyPrint из SESSIYA #14) просмотрен глазами целиком, не только точечно. Все прежние баги закрыты по-настоящему: матрицы §10–12/§28 читаемы, цепочка двойственности §16 — одна чистая строка алгебры, Вассерштейн §25 — настоящие «∫» (не «∈t»), дублей footnote нигде. Два открытых момента — НЕ блокируют: портреты (открытый вопрос, но дальше копать в WeasyPrint бессмысленно — решится за 5 сек, когда владелец откроет файл в браузере) и плоский текст матриц вместо 2D-таблицы (осознанный минимум, опциональный будущий апгрейд, не дефект). **Вердикт аналитика: генераторный «идеал» достигнут, дальше вкладываться в рендер смысла нет.**

**Дальше — файл передан владельцу на чтение содержания.** По итогам его комментариев — либо шаг content-правок, либо прямиком закрытие арки (ARKA §7). При выходе на публичный сайт (не сейчас) — `site-cross-device-audit`.

> **Сборка ПРИНЯТА (2026-06-30, SESSIYA #6):** Code влил v3 + улучшил; линтер exit 0 / 20 матблоков / 26 §; оба примера руками точны; 8 флагов, запрещённое 0; ось ред.2 (Данциг не в зачине); карта вшита; границы соблюдены (дифф против снапшота). Правки L4 + ТЗ §2.5.10 + ISTOCHNIKI (книга-референс) — **по отдельному слову владельца, НЕ откатывать**. HTML-сборка L2 вне границ (генератор хардкодит kriptografiya — отдельный микро-заход).
> **Беречь при сборке (флаги research):** даты Канторовича **1939 ЛП / 1942 транспорт / 1948 Монж**; минимакс = частный случай (не «⇔»); Бренье — Монж не доказал *существования*; вершины транспорта m+n−1 = базисные (не «ненулевые»); WGAN — обнуляются *расхождения*, не «меры»; §145 (Монж-фортификатор, SABRE/ORION не ЛП). PROTOKOL §13: рамка-цель → ревью манифеста/перехода Л1/моста Л7.

> **Композиция — РЕД.2 (2026-06-30, линия владельца):** ред.1 (рамка «что значит лучшее», зачин байкой Данцига) отклонена. Новая ось — **«как искать оптимум в ПРАКТИЧЕСКИХ задачах — просто»**; ведём практическими задачами, абстрактное (теория игр, выпуклая геометрия) — украшение; конкретное→сведе́ние→абстракт→возврат к лин. системам→кольцо (песок→Stable Diffusion). Данциг снят с зачина → в «возврат» (прикладной симплекс). Seed — **v3** (не с нуля). Research закрыт: числа сверены (рацион (3,2)=12; транспорт 230). Детали — `kompoziciya-L2.md` ред.2 · `research-L2.md` · SESSIYA #2–#5.

## Шаги
- [x] **Ф0**: вход по канону (COWORK→ARKA §2), сверка предпосылки по живым файлам (STUB подтверждён; банки богатые; P=NP→Л3 ✓), go аргументирован, 2 развилки решены: опт.транспорт = **равноправное второе ядро**; стандарт = **полный цикл по реальной дыре**. Доки арки заведены. → SESSIYA #1.
- [x] **Композиция** (`kompoziciya-L2.md`): **РЕД.2 (линия владельца)** — ось «искать оптимум в практических задачах — просто»; конкретное→абстракт→возврат к лин. системам→кольцо; Данциг снят с зачина; seed — v3. (Ред.1 «что значит лучшее»/Данциг-зачин отклонена.) Лестница 9 блоков, 2 ядра руками, ≥20 матблоков, self-check ✅. → SESSIYA #2, #5.
- [x] **Research** (`research-L2.md`): фактура сверена (фактчек-субагент) + математика через proof-self-review субагента и scipy (рацион (3,2)=12; транспорт 230, единствен, потенциалы/сертификат). Поправка: даты Канторовича 1939/1942/1948; оговорки формулировок (минимакс, Бренье, вершины, WGAN). Источники → `biblioteka/ISTOCHNIKI.md §Л2`. → SESSIYA #3.
- [x] **Сборка** (`kod_sborka-L2.md` → Code): влил v3 + улучшил до формата v4; `L2-optimizaciya.md` = 217 строк, 26 §/20 матблоков; линтер зелёный; карта вшита (8 лиц, 2 теории, 6 связей); seed-v3 сохранён. ПРИНЯТА по гейтам (SESSIYA #6). HTML-сборка L2 — отдельный микро-заход (генератор хардкодит kriptografiya).
- [x] **Добор из книги-референса** (`kod_dobor-L2.md` → Code): вшиты §21 «Математика, обогнавшая компьютер» + §22 матблок «126 лет→секунда» (Биксби ×4 млрд), целочисл. ЛП=NP в §23-парковку, кейсы ж/д/хоры, «угол на компакте». L2 236 строк/21 матблок; только ADD, старое цело; §-реордер чист. ПРИНЯТ (SESSIYA #8).
- [x] **Обкатка-гейт** (`obkatka-L2.md`) — валидация метода: прогон собранной Л2 через усиленный стандарт. Л2 проходит целиком (11/11 ворот, A/B силён/силён); 5 дыр L4 не повторились. Найдена дыра H6 (шарнир для двух-ядерных лекций — §2.5.5 не различало «цель» от «приём» при равноправных ядрах, §2.4 требовало «одно ядро»); закрыта в `sistema-ocenki.md` (§2.4 + §2.5.5 + provenance + таблица). ПРИНЯТА (SESSIYA #10).
- [x] **Довести до идеала** (владелец: «хочется, чтобы было очень круто»): `kod_ideal-L2.md` → Code — метаданные (21 матблок) + `status: polno`; жёсткий линтер чист (0 `[polno]`-эскалаций); тело не тронуто (sha256 сверен независимо); §2.5.10 уточнён (диаграммы протоколов — не применимо без сторон-с-подслушивающим). Финальный SCORECARD = SCORECARD из `obkatka-L2.md`. ПРИНЯТО (SESSIYA #11).
- [x] **HTML Л2**: `kod_html-L2.md` → Code — `build.py` генерализован (`--build ID`), regression L4 sha256 совпал байт-в-байт, собран `_out/L2-optimizaciya.html` (537 796 байт, чист). `lica.md`: 4 стале-номера § Code + 5-й (kantorovich) — аналитик. Верификатор + независимая сверка аналитика — оба зелёные. ПРИНЯТО (SESSIYA #13).
- [x] **visual-design-review Л2 HTML**: весь документ (8 срезов) просмотрен глазами (рендер — WeasyPrint, playwright недоступен). Найдены 2 бага генератора (не рендера): утечка сырого LaTeX в матблоках (~60 вхождений, худшее — §16/§25) + дубли честных флагов (`> поле:…` в конце матблочного поля). Оба подтверждены и на L4 — общий, ранее не пойманный дефект build.py. Портреты-фото — под вопросом (вероятно ограничение WeasyPrint, не баг). SESSIYA #14.
- [x] **Починить генератор** (`kod_fix-latex.md` → Code → ПРИНЯТО, SESSIYA #16): таблица LaTeX→текст расширена (греч. буквы, `\min\max\sup\log\sum\prod\nabla`, `\text\mathsf\mathrm`, `\tfrac13`-форма, скобки Big/left/right, `\iff`, `\qquad`), `\int`/`\infty` подняты выше `\in` (чинит «∈t»-баг), матрицы → отдельная `_matrix_to_text()`, дубль-footnote — стоп в `fields_of()` перед `> поле:`. Обе лекции пересобраны, регрессия чиста (независимая сверка: полный diff обеих лекций прочитан построчно, только формулы+дубли изменились).
- [x] **Повторный визуальный обзор** (SESSIYA #17, дешёвый — 6 срезов, весь документ) — все баги закрыты по-настоящему, идеал с т.з. аналитика достигнут. Портреты/матрицы-таблица — открытые опциональные моменты, не блокируют.
- [x] **Владелец читает Л2 HTML и комментирует содержание** → итог: концепт дрейфнул, нужен **аддитивный content-раунд** (не закрытие). → новая фаза ниже.

### Аддитивные правки Л6 под дрейфнувший концепт (SESSIYA #18, 2026-07-03)
- [x] Вход + изучение (дрейф концепта, лекция, карта, ратифицированный банк `svyazi-istochniki.md`); собран полный ТЗ-список; развилки решены владельцем (полный блок Винера; §23 перешить под порядок + поиск новых мостов). → SESSIYA #18, `pravki-L6.md`.
- [x] **Авторинг вставок** (`pravki-L6.md`): текст A1–A5, B1–B4, C + новые мосты; гейт math-russian-terminology (4 замены) + russian-editor (тире/фрагменты). Развилки делегированы владельцем → полные варианты.
- [x] **`kod_pravki-L6.md` написан** (RUKOVODSTVO §6, E1–E13 дословно + renumber §27→§28, §28→§29 + budget + линтер-гейт + верификатор + zip-бэкап). Ждёт прогона Code. *(content-ревью свёрнут в заход — владелец видит текст там.)*
- [ ] **Приёмка Code + пересборка HTML** (`build.py --build optimizaciya`) + **SCORECARD §6.2 дельта** + флаг семантич.риплов (§23, «Побочный плод»-рамка) владельцу.

- [x] **Закрытие арки** (ARKA §7): ✅ СДЕЛАНО 03.07 — SESSIYA прочитан целиком, durable разнесён (`resheniya.md` п.22 · `beklog.md` · `dnevnik-i-plan.md` · `ISTOCHNIKI.md §Л2`) + запись в `zhurnal/SVODKI.md` + арка помечена закрытой.
