# ДНЕВНИК арки «Л2 Оптимизация/ЛП — сборка до идеала»

> Дистиллят: деталь + решения с «почему» + история. Один док, ведётся с первого хода (переживает обрыв окна).

---

## #1 — 2026-06-30 · Ф0 (вход, ресёрч на живых файлах, go, доки арки)

**Что сделал.** Вошёл по канону (COWORK §4 роутер → ARKA §2 Ф0). Сверил предпосылку захода по живым файлам, снял 2 развилки, аргументировал go, завёл доки арки. Файлы до слова владельца не писал.

**Ресёрч (живые файлы).**
- **Контур метода (образец Л4, resheniya п.17):** 3 артефакта + петля. `obkatka` (SCORECARD §6.2 → таблица «замечание→ловит ли стандарт» → дыры → усиление ТЗ → перепрогон → §8 S/H) · `kompoziciya` (лестница §2.5.9: throughline→ABT→карта переноса→блоки[сцена-вход·вклад·«из-за этого»·S/H]→кольцо→парковка) · `research-handgap` (фактура+proof-self-review субагента) → хирургическая сборка в `istochnik/`, build.py ✓.
- **Состояние Л2:** `istochnik/L2-optimizaciya.md` = STUB (`status: skelet`) — подтверждает resheniya п.16. **Сырых замечаний владельца по Л2 НЕТ** (у Л4 были `L4-syrye-kommentarii`+`razbor`; у Л2 нет — лекцию не строили). → метод адаптируется: не пересборка-по-критике, а **сборка из банков под стандарт; обкатка = гейт**.
- **Банки богатые:** `koncepcii-lekcij-v2.md §Л2` (плотно: байка Данцига · ядро рацион→многогранник→линейка под углом цены→оптимум в вершине · два козыря Стиглер[человек→машина] + опт.транспорт[мост в финал] · слои/поля/мосты) · `dokazatelstvo-v2.md` · `klubok/`(v1) · `kotel.md`(трест, двойственность, спрятанная геометрия, арка транспорта, §145 исправления).
- **Стандарт** (что Л2 обязана пройти): ворота Г1–Г11 · баллы A1–A6/B1–B6 (0–4) · движки · анатомия §2.4 · формат v4 §2.5 (≥20 матблоков, чёрный ящик≠вещь-в-себе §2.5.4, одна цель-мысль §2.5.5, арка §2.5.9) · К1–К11 (К11 низковисящие плоды) · клубок §3.4 · SCORECARD §6.2 · §7 hand-gap.

**Связи (сверены).**
- **P=NP-хвост → Л3** ✓: в v2 P=NP — отдельная Л3; Л2 оставляет «NP-звоночек» (`koncepcii §Л2 поля/мосты`), полно в Л3. (В v1 6-лекц. P=NP жил В лекции про ЛП — сплит v1→v2 его вынес; resheniya п.12/14.)
- **Канторович — шарнир:** его трест родил ЛП, его релаксация транспорта *есть* ЛП. → два козыря не несведённые, сходятся в одну цель-мысль. Снимает риск §2.5.5 при двух ядрах.

**РЕШЕНИЕ Ф0: GO** (оспоримо, не оспорено). *Почему арка, не разовая:* многосоставная (композиция+research+сборка+обкатка-гейт+верификация), на неск. сессий, трогает много мест (`istochnik/L2`+`karta/`+`biblioteka/`+потенц. `sistema-ocenki`), открытой арки про Л2 нет. Контр-«влезет в чат» не проходит: сборка с нуля (seed нет), один только research (опт.транспорт+симплекс/двойственность+proof-self-review) многосессионный.

**Развилки (владелец, AskUserQuestion):**
1. **Опт.транспорт = РАВНОПРАВНОЕ второе ядро руками** (не лёгкое второе движение — мой best-guess отклонён). *Почему важно:* два ядра нагружают §2.5.5 «одна цель-мысль»; держим через шарнир Канторович/ЛП — оба ядра обязаны сойтись в одну цель, иначе провал. *Следствие:* +B2/матёмкость, но длиннее; throughline должен явно нести оба ядра; research тяжелее (нужен считаемый транспортный пример руками, как F₁₇ в L4).
2. **Стандарт — полный цикл как на Л4:** дыра обкатки → усиливаем `sistema-ocenki` §7, **только по реальной дыре, в нужный раздел**. *Почему:* это и есть «метод подтверждён на втором, отличном по типу материале» — полная цель арки.

**Staleness-флаги (учтены в NAVIGATOR).**
- Бриф «мост Л2↔Л6», но живой v2 уводит опт.транспорт → **Л7** (диффузия/нейросети `koncepcii §Л2 мосты`, `graf.md`), а Л6-случайность цепляется к Л2 через фон Неймана/СССР. Похоже, «Л6» в брифе = старая 6-лекц. нумерация финала (тогда финал = Л6, теперь Л7). **На композиции уточнить терминальный мост.** Best-guess: Л7 (тек. нумерация).
- Беречь §145-исправления `kotel.md`: Монж — военный инженер-фортификатор (НЕ «инженер Наполеона», анахронизм); SABRE/UPS-ORION НЕ ЛП; факторизация в NP∩co-NP.

**Доки арки заведены:** NAVIGATOR · PLAN · SESSIYA (этот). Папка `zhurnal/2026-06-30_L2-optimizaciya/`.

**Дальше (СЕЙЧАС в PLAN):** композиция Л2 — throughline 2-ядра-в-1-цель через Канторович/ЛП, ≥2 кандидата рамки (К10), лестница §2.5.9. Свежим чатом по арк-ритуалу (ARKA §3).

---

## #2 — 2026-06-30 · Композиция Л2 (`kompoziciya-L2.md`)

**Что сделал.** Вошёл по арк-ритуалу (NAVIGATOR→PLAN→SESSIYA). Прочитал стандарт (§2.5.5/§2.5.9/К10/§3.4), образец метода `kompoziciya-L4.md`, банки (`koncepcii §Л2` — плотный, `kotel.md`), STUB+PROTOKOL. Скиллы: `popsci-narrative`. По «разговору первым» (COWORK §2) вынес рамку владельцу, после выбора собрал композицию. Файл `kompoziciya-L2.md` заведён.

**Throughline (зафиксирован).** «„Оптимально" — не середина, а угол возможного; рацион/транспорт/распил фанеры — одна задача (ЛП), которую Канторович нашёл на тресте, и та же релаксация решает задачу Монжа; у „лучшего" есть вычислимый адрес — и поиск перешёл от человека к машине.» Объединение **целью**, не приёмом; шарнир Канторович/ЛП + явное сведение (блок 6) держат §2.5.5 при двух ядрах.

**Рамка (К10) — РЕШЕНИЕ владельца (AskUserQuestion).** Из 3 кандидатов выбрана **«Что значит лучшее»** (обыденное «оптимально» с глубиной). Невыбранные едут лейтмотивами: **универсальность** (B) — «ого» в блоке 3; **человек→машина** (C) — «поэтому» + кольцо. *Почему важно:* рамка задаёт вход/разворот/кольцо/название; PROTOKOL §13 — правка рамки-цели не ловится линтером → флаг на ревью при сборке (манифест Л2, переход Л1, мост).

**Лестница (10 блоков).** Крючок (Данциг-домашка) → ЯДРО 1 руками (рацион→многогранник→линейка→вершина) → универсальность (симплекс/SCOOP) → ШАРНИР (Канторович: трест→ЛП; двойственность фон Неймана подмигиванием) → ЯДРО 2 руками (Монж→транспортный пример→релаксация=ЛП) → сведение двух ядер → человек→машина (Стиглер) → парковка (строгость + NP-звоночек→Л3) → финал-кольцо (Бренье→Виллани/Фигалли→Stable Diffusion, мост Л7) → домашка. **21 матблок** (M1–M21), перевес в математику, два ядра руками.

**Снятые/новые флаги.** Терминальный мост = **Л7** (живой банк, не Л6) — staleness-флаг #1 снят. Двойственность Ч2 — по умолчанию подмигивание + ящик §2.5.4 (опция: поднять до раскрытого матблока — владельцу). §145-исправления внесены как guardrails (Монж-фортификатор; SABRE/UPS-ORION не ЛП; факторизация NP∩co-NP→Л3).

**Дальше (СЕЙЧАС в PLAN):** Research Л2 (`research-L2.md`) — фактура+hand-gap, математика через proof-self-review субагента, источники в `biblioteka/ISTOCHNIKI.md`. Очередь — `kompoziciya-L2.md §7`. Свежим чатом.

---

## #3 — 2026-06-30 · Research Л2 (`research-L2.md`)

**Что сделал.** Прошёл шаг Research по `popsci-research`. Математику считал сам + **проверил программно** (scipy в песочнице) + прогнал **proof-self-review субагента** (адверсариально). Факты сверил **фактчек-субагентом** (web). Дистиллят — `research-L2.md`; источники строкой+URL — `biblioteka/ISTOCHNIKI.md §Л2`.

**Математика — проверена, «на доску без опаски».**
- Рацион (ядро 1): min 2x₁+3x₂ при 2x₁+x₂≥8, x₁+3x₂≥9 — вершины (0,8)=24/(3,2)=12/(9,0)=18, оптимум вершина **(3,2)=12**.
- Транспорт (ядро 2, аналог F₁₇): запасы [20,25,15], спросы [22,19,19], C=[[8,6,3],[5,4,6],[4,7,5]] → оптимум **230** (единствен, 5 ячеек = невырожденная вершина), потенциалы u=[0,−2,−3]/v=[7,6,3], редуцированные ≥0 — сертификат; NW-угол 345 (контраст). Метод потенциалов = двойственность транспорта.

**Поправки от субагентов (внесены в research-L2.md, беречь при сборке).**
- ⚠️ **Факт:** даты Канторовича разнести — **1939 ЛП / 1942 транспортная задача / 1948 Монж** (банк смешивал 1939/1940).
- + **Абрахам Вальд** — соавтор 2-й задачи Данцига; «Уилл Хантинг» — городская легенда.
- **Формулировки (proof-self-review):** осн. теорема ЛП — на компакте (иначе «либо вершина, либо неограничено»); **минимакс = частный случай** двойственности, не «⇔» (строго — Гейл–Кун–Таккер 1951); **Бренье** — Монж не доказал *существования* (стоимость |x−y|), структуру для |x−y|² раскрыл Бренье; вершины транспорта m+n−1 = **базисные** (не «ненулевые»); Биркгоф — только квадратная задача о назначениях; **WGAN** — обнуляются *расхождения* KL/JS, не «меры».
- **Источник-якорь подтверждён:** Quanta «Researchers Discover the Optimal Way To Optimize» (Nadis, 13.10.2025; Bach–Huiberts) — вся линия Данциг→Кли–Минти→Спилман–Тенг.
- §145 guardrail: SABRE — БД бронирования; UPS ORION — эвристика TSP; оба **не ЛП**.

**Дальше (СЕЙЧАС в PLAN):** Сборка Л2 — заполнить `istochnik/L2-optimizaciya.md` (формат v4, ≥20 матблоков, 2 ядра руками) из композиции+research; `build.py`; карта. Вероятно тяжёлый Code-заход (`agentic-coding-session-brief`). Беречь флаги выше.

---

## #4 — 2026-06-30 · Code-заход на сборку написан (`kod_sborka-L2.md`)

**Что сделал.** По `agentic-coding-session-brief` + `RUKOVODSTVO §6` написал заход для Claude Code на сборку Л2. Перед написанием — read-only аудит механики сборки на живых файлах (SHEMA, `L4-kriptografiya.md` как образец формата, `build.py`, `karta/`, STUB).

**Аудит — что выяснил (сверх NAVIGATOR).**
- Формат v4: шапка YAML (`cel`, `teorema-zagolovok`, `budget`, `status`), разделы `## §N · ТИП · …{флаги}`, матблок = 4 поля, поля-сноски `> поле:тип`, `→[id]`. Образец — `L4-kriptografiya.md` (status `chernovik`, ~12 §, 8 матблоков).
- **`build.py`:** `--lint` гоняет структурные (битый `→[id]`, дубль id, 4 поля матблока, тип/номер раздела — валят сборку) на ВСЕХ лекциях; редакционные (≥20 матблоков, квоты) мягко, жёстко лишь при `status: polno`. **HTML-вывод хардкодит на `kriptografiya`** (≈стр.621) → L2-HTML требует генерализации генератора = отдельный микро-заход, вне границ (анти-скоуп «не трогаем build.py»).
- **`karta/` почти пуста:** объявлены только turing/shannon (lica), predel/os-vremeni/sluchaynost-instrument (graf), teoriya-chisel/sluchaynost-t (teorii). → L2-лица (Данциг, фон Нейман, Канторович, Монж, Виллани, Фигалли, Стиглер, Бренье) и теории (ЛП, опт.транспорт) надо ДОБАВИТЬ, иначе `→[id]` не резолвятся (структурный провал). Это вшито в заход.

**Заход — границы.** Автор L2-источник + вшить karta (чтобы ссылки резолвились) + `--lint` зелёный + **верификатор полноты** (свежий субагент: каждый блок композиции и факт research приземлился; запрещённые формулировки отсутствуют — grep; битых ссылок 0). НЕ трогать другие лекции, генератор, SHEMA, ТЗ. `status: chernovik` (флип в `polno` — отдельный шаг Верификации). 8 честных флагов выписаны в заход дословно.

**Дальше:** владелец шлёт `kod_sborka-L2.md` в Code → Code исполняет → **приёмка по отчёту** (`RUKOVODSTVO §4`), ратификация в SESSIYA #5. Затем — Обкатка-гейт.

---

## #5 — 2026-06-30 · Ревью владельца ПЕРЕД записью → композиция РЕД.2 + перезаход

**Контекст.** Владелец остановил перед отправкой захода в Code, два замечания (ревью-гейт PROTOKOL §13 сработал).

**Замечание 1 — опираться на готовую версию, не с нуля.** Есть короткий черновик курса `…/2026-06-28_nauchpop-chernovik/chernovik-kursa-v3.html` (1.1 МБ) с разделом «Оптимизация»: написанный текст, портреты Данцига/Канторовича (base64), связи (→Л6/Л7, таймлайн, «каждый день»→Stable Diffusion) и **уже вычищенные прикладные кейсы** (почки/Рот-2012, экипажи AA $1,4 млрд, IMRT — заменили ошибочные SABRE/UPS). → Я зря трактовал Л2 как чистый STUB; настоящий seed — v3. Это ровно метод Л4 (влить готовое + усилить). **Принято.**

**Замечание 2 — реверс линии, прочь Данциг-зачин.** Байка Данцига про «домашку» = абстрактная статистика без приложения, слабый крючок (владелец прав). Уточнил по существу: сам Данциг не лишний — симплекс прикладной → перенёс его в «возврат к линейным системам», не в зачин. Владелец задал ось сам: **лекция о том, как оказалось, что искать оптимум в ПРАКТИЧЕСКИХ задачах — просто**; ведём практическими задачами (их побольше, в простом виде), абстрактное (теория игр, выпуклая геометрия, транспорт→картинки) — украшение. Делегировал дизайн линии мне («изобрети и протестируй»).

**Что сделал.** Изобрёл и протестировал линию; переписал **`kompoziciya-L2.md` → РЕД.2**: ось «как находить лучшее (практически)»; ABT (астрономия вариантов → одна задача, лучшее в вершине → поиск стал машинным); лестница 9 блоков (стена практических задач → рацион руками → сведе́ние/кейсы → транспорт руками → абстрактный подъём двойственность=игры → возврат к лин. системам/симплекс/Стиглер → парковка NP → кольцо песок→Stable Diffusion). Self-check §2.5.9 ✅; §2.5.5 держится осью. Карта «v3+банк→блок» доказывает: v3 не теряется (реордер, не потеря). Числа/факты не трогал — research-L2.md валиден, меняется только порядок.

**Перезаход.** `kod_sborka-L2.md` переписан: режим **«влить v3 + улучшить»** (seed-раздел v3 в read-list, перенос портретов/кейсов/связей), порядок ред.2, Данциг снят с зачина, верификатор дополнен сохранностью v3. Границы/флаги/критерий сохранены.

**Дальше:** владелец шлёт перезаход в Code → исполнение → приёмка (#6).

---

## #6 — 2026-06-30 · Приёмка сборки Л2 → ПРИНЯТО

**Вердикт: Л2 ПРИНЯТА.** Code влил v3 и улучшил до формата v4. Сверил по гейтам (`RUKOVODSTVO §4`) — не на веру, независимым прогоном на живых файлах.

**Гейты (моя сверка, не отчёт Code):**
- Линтер `--lint` exit 0, **0 структурных ошибок, 0 битых ссылок**; `optimizaciya: 20/20 матблоков`, 26 §-разделов; полный `build.py` зелёный (L4-HTML 461 КБ).
- Два примера руками — точные числа: рацион (0,8)=24/(3,2)=12/(9,0)=18; транспорт 230 vs северо-западный 345, потенциалы u=[0,−2,−3]/v=[7,6,3], 63+107+60=230.
- 8 честных флагов присутствуют (grep: 1942/1948, Мезьер, фортификатор, Вальд, Гейл, «на компакте», расхождения); запрещённое — 0 («инженер Наполеона», SABRE, ORION, ⇔, «эквивалентн»).
- Ось ред.2 выдержана: §1 — стена практических задач (почки/35, развозка, экипажи, рацион) + явная ось, НЕ персона; Данциг в §18–20, байка «домашка» → поле-сноска. Кейсы v3 (почки/Рот, AA, IMRT) в зачине/сведе́нии.
- Карта дописана (8 лиц, 2 теории, 3 нити + 3 ребра); seed-v3 сохранён (6 портретов в `_dizajn/foto/`). Верификатор Code сам поймал и починил недосевший флаг про Монжа.
- **Границы:** дифф против старт-снапшота — L1/L3/L5/L6/L7 и `build.py`/SHEMA идентичны (не тронуты). Изменён L4 — **по отдельному заданию владельца** (см. ниже), не нарушение.

**Вопросы Code (6) — разобраны/ратифицированы:**
1. «20 матблоков vs ~13–15 разделов» — противоречие было в МОЁМ заходе; Code верно выбрал ≥20 по §2.5.1. Ратифицирую 26 §/20 матблоков.
2. Рипл рамки `cel` на манифест/Л1-переход/Л7-мост — верно вынес на ревью (вне скоупа). **Отложено** до наполнения соседних лекций и курсового манифеста (`svyazujushchee.md`).
3–4. Одиночные появления лиц + недобор рёбер графа — честный общекурсовой недобор (соседи — скелеты), НЕ фабриковал (анти-фабрикация §5.2). Добор при наполнении других лекций.
5. Ошибки seed-v3 (Бренье «не смог Монж»; даты Канторовича слиты) — починил в L2, сам v3 не трогал (READ-ONLY). v3 — архивный черновик, правка опц.
6. Нобель Стиглера 1982 — проверил, дата верна (отдельно от Канторовича-1975).

**Правки вне L2-захода — по отдельному слову владельца (НЕ откатывать):**
- **L4 §1–§2 отредактирован** под новый §2.5.10: проговорённый мостик «Тьюринга видели в Л1 →[turing]», перехватчик Ева (диаграмма Алиса/Боб/Ева), «четыре стены предела» читаемой прозой.
- **ТЗ: добавлен §2.5.10 «Подача текста»** (мотивация перед формулой · клубок проговорён в тексте · переиспользование старого · центрированные display-формулы · диаграмма многосторонних протоколов · мета-нити прозой). + роутер. **Новое требование — учитывать на обкатке Л2 и впредь.**
- **ISTOCHNIKI:** Литвак–Райгородский — золотой референс подачи (К9) с картой глав; **Л2 ← Гл.2 «Менеджмент и многогранники»** (ЛП·симплекс·расписания).

**Моя ошибка (owned):** в ходе приёмки назвал «н��до» в выводе diff порчей кодировки — оказалось **артефакт отображения**, а не порча. Прямая проверка: «надо» цело, все затронутые файлы — валидный UTF-8 (strict), линтер зелёный. Чинить/откатывать нечего.

**HTML-сборка L2** — по-прежнему вне границ (генератор хардкодит kriptografiya); отдельный микро-заход на генерализацию.

**Дальше (СЕЙЧАС в PLAN):** Обкатка-гейт (`obkatka-L2.md`) — прогон собранной Л2 через усиленный стандарт, **включая новый §2.5.10**; особый фокус — §2.5.5 «одна ось» при двух ядрах. Дыра → усилить ТЗ §7.

---

## #7 — 2026-07-01 · Добор из книги-референса (Литвак–Райгородский гл.2)

**Контекст.** Владелец: перед обкаткой прочитать парную главу золотого референса (гл.2 «Менеджмент и многогранники» + ЛП-приложение) и **добавить новое, не урезая**.

**Что сделал.** Извлёк PDF (`pdftotext`), прочитал главу 2 + приложение к гл.2. Харвест → `research-L2.md §ДОБОР`; источники → `ISTOCHNIKI §Л2`. Написал хирургический Code-заход `kod_dobor-L2.md` (только ADD).

**Главная находка (то, что владелец просил — «как выросла эффективность ЛП»):** раздел «Математика, обогнавшая компьютер» по Биксби-2012. Диета 1953 на перфокартах — 8 ч. CPLEX 1991→2006 ×29 000 (только алгоритмы; скачок ×10 в 1998 — 30 лет теории разом); +Gurobi → ×469 800; железо (Мур) ×8000; **совместно ×4 миллиарда — «126 лет в 1991 → 1 секунда в 2012»**; Бертсимас–Кинг 2015 → ×450 млрд. Количественный климакс оси «искать оптимум стало просто».

**Ещё добор:** целочисленное ЛП = NP-трудно (0/1-расписания, ветви-границы, Гомори) → конкретизирует NP-звоночек к Л3; кейсы ж/д Нидерландов (Эдельман-2008) + хоры Twente; формальное «оптимум в углу» на **ограниченной** области (приложение к гл.2) — подтверждает наш флаг «на компакте».

**Дальше:** владелец шлёт `kod_dobor-L2.md` в Code → приёмка → обкатка-гейт.

---

## #8 — 2026-07-01 · Приёмка добора → ПРИНЯТО

**Вердикт: добор ПРИНЯТ.** Сверил независимо (не на веру), все гейты зелёные.
- Линтер `--lint` exit 0, 0 структурных / 0 битых ссылок (10 редакц. варнингов — все по L4/графу, доборо́м не порождены).
- L2 **217→236 строк**, §-разделов 28, матблоков **21** (было 20).
- **Реордер чист:** §-последовательность 1..28 без дыр; новые §21 «Математика, обогнавшая компьютер» + §22 матблок «126 лет→секунда»; парковка стала §23 (+целочисл. ЛП=NP), Бренье §24; обе прозаические ссылки на Бренье ведут в §24 (поправлены). Линтер §-прозу не ловит — сверил руками.
- **Добор на месте:** grep — 29 000, 469 800, ×4 миллиарда, 126 лет, Биксби, Эдельман, ветвей и границ, Гомори, «обогнавшая».
- **Старое цело:** оба примера ((3,2)=12, транспорт 230), 9 «честно»-флагов, кейсы почки/AA/IMRT, «Мезьер», запрещённое (инженер Наполеона/SABRE/ORION) = 0.
- **Границы:** дифф против снапшота `pre-L2-dobor` — L1/L3/L4/L5/L6/L7 и build.py байт-в-байт не тронуты; в istochnik изменён только L2 (+19 строк). Read-only входы (research/kompoziciya) Code не правил.
- Верификатор Code (свежий субагент) — PASS по 5 пунктам; сошёлся с моим.
- Решение Code: ★-блок разнесён на РАССКАЗ §21 + МАТЕМАТИКА §22 (а не один) — ратифицирую (следует §2.5.1 «каждый узел несёт матблок»).

**Дальше (СЕЙЧАС в PLAN):** Обкатка-гейт (`obkatka-L2.md`) — прогон Л2 через усиленный стандарт (SCORECARD §6.2 + §2.5.9 + §2.5.10 + перепрогон дыр L4), фокус — §2.5.5 «одна ось при двух ядрах». Вторая половина цели арки: подтвердить метод и стандарт на двухъядерной лекции.

---

## #9 — 2026-07-01 · ХЭНДОФФ (арка продолжается, свежий чат)

**Состояние арки.** Ф0 ✅ · Композиция ред.2 ✅ · Research ✅ · Сборка ✅ · Добор ✅ — всё принято по гейтам. **Л2 собрана:** `istochnik/L2-optimizaciya.md` = 236 строк, 28 §, 21 матблок, `status: chernovik`, линтер зелёный (0 структурных). Ось: «искать оптимум в практических задачах оказалось просто». Два ядра руками (рацион (3,2)=12; транспорт 230). Добор из книги-референса вшит (§21–22 «обогнавшая компьютер» ×4 млрд; целочисл. ЛП=NP; кейсы ж/д/хоры; «угол на компакте»).

**СЕЙЧАС (для след. чата).** **Обкатка-гейт** (`obkatka-L2.md`) — вторая половина цели арки: прогон готовой Л2 через усиленный стандарт (SCORECARD §6.2 + §2.5.9 + **§2.5.10 подача текста** + перепрогон дыр L4), главный фокус — **§2.5.5 «одна ось при ДВУХ ядрах»** (нагрузка, которой не было у односпинной L4). Дыра в стандарте → усилить `sistema-ocenki` §7 (в нужный раздел). Образец метода — `2026-06-30_L4-ideal-standart/obkatka-L4.md`.

**Потом (forward-цели владельца, 2026-07-01).**
1. **Довести до идеала** («очень круто»): закрыть дыры обкатки → `status: polno` + жёсткий линтер + финальный SCORECARD.
2. **HTML Л2** (в конце арки): генератор хардкодит `kriptografiya` → отдельный Code-заход на **обобщение build.py** → собрать Л2 HTML → `visual-design-review`. Планка — очень круто, не «прошло».
3. **Закрытие арки** (ARKA §7): durable по домам + `SVODKI.md` + пометить закрытой.

**Вход в след. чат:** арк-ритуал (COWORK §4 → `NAVIGATOR` → `PLAN §СЕЙЧАС` → эта запись SESSIYA). **Беречь:** честные флаги (`research-L2.md §Честность`), §2.5.10; при правке ТЗ — правило §7 (в нужный раздел, обновить роутер/таблицу). Ключевые файлы: `kompoziciya-L2.md` (ред.2) · `research-L2.md` (+§ДОБОР) · `istochnik/L2-optimizaciya.md` · `1-START-HERE/sistema-ocenki.md` (Г4-ось, §2.5.9 эталоны, §2.5.10).

---

## #10 — 2026-07-01 · Обкатка-гейт (`obkatka-L2.md`) → ПРИНЯТА, найдена и закрыта H6

**Что сделал.** Вошёл по арк-ритуалу (NAVIGATOR→PLAN→SESSIYA #9). Прочитал живые файлы: `istochnik/L2-optimizaciya.md` (built, 236 строк/28 §/21 матблок), `kompoziciya-L2.md` (ред.2, self-check §8), `research-L2.md` (§Честность + §ДОБОР), `1-START-HERE/sistema-ocenki.md` целиком, `obkatka-L4.md` (образец метода). Прогнал `--lint` (0 структурных, 10 редакц. — все по L4/графу, не Л2). Написал `obkatka-L2.md`.

**SCORECARD Л2:** 11/11 ворот ✅ без провалов (Г4 — ось уже дословно эталонирована в §2.5.9 до этой обкатки, добор её не расшатал). Баллы A=силён (A1=3 A2=4 A3=4 A4=3 A5=3 A6=4), B=силён (B1=4 B2=4 B3=3 B4=3 B5=4 B6=4) — ровный профиль без провалов (в отличие от baseline-Л4 с просевшими B2/B5): ожидаемо, Л2 не диагностика-по-критике, а сборка сразу под усиленный стандарт. §2.5.9 (лестница/ABT/сцена/кольцо/парковка) и §2.5.10 (6 правил подачи) — сверены на BUILT-тексте (не только на композиции) и держатся после добора (§-реордер SESSIYA #8 арку не сломал). Правило §2.5.10 «диаграммы протоколов» — первый пример легитимной **неприменимости** (нет сторон-с-подслушивающим в оптимизации), не провал.

**Перепрогон 5 дыр L4 (H1–H5) на Л2 — ни одна не повторилась:** чёрные ящики все именованы (H1✓); К11+B5=4 дают явный выход-пейофф (H2✓); лестница держится built-текстом (H3✓); каждый рассказ-узел смежен с матблоком (H4✓); все блоки в упаковке/парковке, ни одного «не при делах» (H5✓). Подтверждает метод на втором материале — вторая половина цели арки выполнена.

**Главный результат — особый фокус §2.5.5 при ДВУХ равноправных ядрах.** Применил тест рукой: «убери общее „оба суть ЛП" — держится ли причина держать оба ядра?» На Л2 — держится: связку несёт не приём (ЛП), а конкретный шарнир — **Канторович**, нашедший общую форму ЛП на фанерном тресте (1939) РАНЬШЕ транспортной релаксации (1942) и РАНЬШЕ, чем узнал о задаче Монжа (1948). Убери этот факт — лекция распадается на два примера одной техники. Тест прошёл, но заметил: **сам стандарт (§2.5.5) не давал этого теста явно** — я применил его по аналогии с методом L4 (H1–H5), не потому что §2.5.5 его формулировал. **Плюс нашёл смежную дыру:** §2.4 (слой ТЕЛО) буквально требовал «**одно** ядро в зале» — стале-формулировка, прямо противоречащая решению владельца на Ф0 арки («опт.транспорт = равноправное ВТОРОЕ ядро», форк №1).

**H6 закрыта — правки в `sistema-ocenki.md` (не спорны по форме, аналог H1/H4/H5 у L4 — внёс сразу):**
1. §2.4 ТЕЛО: «одно ядро» → «одно ядро **или несколько равноправных, если держит явный ШАРНИР** (→§2.5.5)».
2. §2.5.5: добавлен проваливаемый тест шарнира — «убери общее „оба суть X" — держится ли причина?»; эталон-калибровка — Канторович/Л2.
3. §7 (provenance): запись «H6, обкатка Л2».
4. Таблица соответствий: строка H6 → §2.4 + §2.5.5.
Роутер §0 правки не потребовал (§2.5.5 уже маршрутизируется через «ФОРМАТ конспекта → §2.5»).

**Остаточные наблюдения (НЕ дыры ТЗ, work-gap на «Довести до идеала»):** расхождение метаданных `budget: math_blokov: 20` в шапке L2 vs фактических 21 (20 секций МАТЕМАТИКА + 1 матблок внутри §23-ТЕХБЛОК-парковка) — линтер не ловит (мягкий порог), поправить при флипе в `status: polno`; одиночные появления лиц/недобор рёбер графа — уже разобранный общекурсовой недобор (SESSIYA #6 п.3–4), добор его численно не менял.

**Дальше (СЕЙЧАС в PLAN).** **Довести до идеала**: закрыть остаточные наблюдения (метаданные budget; уточнить объём правила §2.5.10 про диаграммы) → `status: chernovik → polno` (жёсткий линтер, квоты) → финальный SCORECARD §6.2 → верификатор полноты свежим субагентом. Потом — HTML Л2 (генерализация build.py, отдельный Code-заход) → закрытие арки.

---

## #11 — 2026-07-01 · «Довести до идеала» шаг 1 (`kod_ideal-L2.md`) → ПРИНЯТО

**Что сделал.** Написал узкий Code-заход `kod_ideal-L2.md` (по `RUKOVODSTVO-zahodami.md §6`, не по общему скиллу — проект самодостаточен): 2 поля шапки (`budget.math_blokov` 20→21, `status` chernovik→polno) + жёсткий линтер + верификатор. Заход явно исключил из периметра граф-квоты (парные рёбра, лицо 2–5 лекций) — сверил заранее по исходнику `build.py`, что они помечены статусом «—» и флипом Л2 не эскалируются (не додумывал, проверил).

**Отчёт Code принят по гейтам (RUKOVODSTVO §4), сверка НЕ на слово — независимо:**
- Шапка: `sed` живого файла → `math_blokov: 21`, `status: polno` — совпадает с отчётом дословно.
- Линтер (`build.py --lint`) прогнан мной independently → **exit 0**, те же 10 warnings (только `kriptografiya`+граф/лица), **0 строк `[polno]`**, ни одного упоминания `optimizaciya` — совпадает с отчётом Code байт-в-байт.
- Тело Л2 (после 2-го `---`) — sha256 сам посчитал: `73cc7a1b…9cfb62` — совпал с заявленным Code (и с его верификатором).
- Снапшот `snapshots/istochnik_pre-L2-flip-polno_2026-07-01_235633.zip` — существует, сделан до правки.
- Вопросов от Code нет (единственная развилка — совпадает ли линтерный `nmat` с ручным счётом 21 — Code снял её сам прогоном ДО мутации, не на веру; я это одобряю как хорошую дисциплину).

**Ратифицирую: принято без замечаний.** Заодно закрыл мелкое наблюдение обкатки: **§2.5.10** (диаграммы многосторонних протоколов) дополнен уточнением объёма — правило не в силе, если в лекции нет сторон-с-подслушивающим (первый пример — Л2, зафиксирован явно, чтобы не путать «н/п» с провалом на будущих обкатках).

**Итог: Л2 = `status: polno`, финальный SCORECARD** — тот же, что в `obkatka-L2.md` (11/11 ворот, A/B силён/силён), так как содержание лекции не менялось, менялась только метаданные/статус. Верификатор полноты (свежий субагент внутри Code-захода) — 4/4 PASS.

**Дальше (СЕЙЧАС в PLAN).** **HTML Л2**: генератор (`build.py`) хардкодит `kriptografiya` → отдельный Code-заход на генерализацию (вывод любой лекции по `id`) → собрать Л2 HTML → `visual-design-review` (смотрим глазами); при выходе на сайт — `site-cross-device-audit`. Потом — закрытие арки (ARKA §7).

---

## #12 — 2026-07-02 · Заход на генерализацию build.py + сборку Л2 HTML написан (`kod_html-L2.md`)

**Что сделал.** Read-only аудит `istochnik/_generator/build.py` (весь файл) ПЕРЕД написанием захода. Подтвердил: хардкод лекции `kriptografiya` — **только** в `main()` (строки ~618–628: выбор `target` + имя выходного файла); `build_lecture`/`render_section`/`render_ref`/`lint`/`load_registry` уже используют `lec.id`/`lec.header.get("poryadok")` — общие, трогать не нужно. Проверил состояние: только `optimizaciya` (`polno`) и `kriptografiya` (`chernovik`) не скелеты; L1/L3/L5–L7 — `skelet`.

**Побочная находка аудита №1 (не блокирует, зафиксирована в заходе).** `karta/lica.md` держит §-номера появлений Виллани/Фигалли/Бренье/Монжа **от ДО-доборной нумерации** (SESSIYA #8 сдвинула §; прозаические ссылки внутри `L2-optimizaciya.md` тогда поправили, а аннотации в `lica.md` — нет). Проверил по `read_lica_appearances()` в коде: число после «§» нигде не участвует в проверках линтера (используется только id лекции до «§») — НЕ структурный баг, чисто документация. Вписал микро-фикс (4 строки, только номера) в тот же заход — дёшево, раз уж открываем контекст.

**Побочная находка аудита №2 (честный факт, не блокирует).** В `_dizajn/foto/` есть 7 портретов (danzig, figalli, fon-neyman, kantorovich, monzh, shannon, villani), но **нет `brenier.jpg` и `stigler.jpg`** — хотя `lica.md` их объявляет. Проверил код рендера (`portrait_html`): отсутствие фото не роняет сборку — деградирует в моно-плейсхолдер (буква на цветном фоне). Не чиню сам (рисовать/искать портреты — вне периметра генерализации), явно вынес в критерий захода и в отчёт — чтобы всплыло на `visual-design-review`, а не потерялось.

**Заход `kod_html-L2.md`:** генерализация `main()` через `--build ID` (без флага — прежнее поведение байт-в-байт, это и есть регрессионный гейт: sha256 `_out/L4-kriptografiya.html` до/после ОБЯЗАН совпасть); сборка `--build optimizaciya` → `_out/L2-optimizaciya.html`; микро-фикс `lica.md` (4 числа). Границы: только `main()` в коде + 4 строки в `lica.md`; тело лекций/`_dizajn`/`sistema-ocenki.md`/`SHEMA` не трогать. Верификатор — независимый прогон обоих вызовов + regression-sha256 + `diff -rq` дерева + grep портретов (ожидаемые моно-плейсхолдеры у brenier/stigler).

**Дальше:** владелец шлёт `kod_html-L2.md` в Code → исполнение → приёмка по гейтам (следующая запись) → `visual-design-review` смотрим Л2 HTML глазами → при выходе на сайт `site-cross-device-audit` → закрытие арки.

---

## #13 — 2026-07-02 · HTML Л2 → ПРИНЯТО (генерализация build.py + сборка)

**Отчёт Code принят по гейтам (RUKOVODSTVO §4) — сверил НЕЗАВИСИМО, не на слово.**

- **Генерализация `main()`:** `--build ID` добавлен, дефолт без флага = `kriptografiya` (обратная совместимость). Сам прогнал `python3 build.py` (без флагов) → **sha256 `L4-kriptografiya.html` = `cc51576b…6af05616`** — совпал и с отчётом Code, и с эталоном из бэкапа (`unzip -p` бэкапа → тот же хэш). Регрессия чиста.
- **Сборка Л2:** сам прогнал `--build optimizaciya` → `_out/L2-optimizaciya.html`, **537 796 байт** (`wc -c`, совпало с отчётом Code день-в-день; расхождение с промежуточным «495 КБ» в стдауте — это `len()` по символам Python-строки против байт UTF-8 на кириллице, не баг). Grep: «Оптимизация»×3, «Основная теорема линейного программирования»×2, `<title>Лекция 2 «Оптимизация»`; 0 Traceback; 0 висячих `{{…}}`. Портреты: `grep -o 'class="port[^"]*"'` → 6× `port` + 1× `port mono` = 7 карточек, совпадает с отчётом.
- **Границы:** `diff -rq` дерева `istochnik/` против бэкапа (`snapshots/istochnik_pre-L2-build_2026-07-02.zip`) → **ровно 3 отличия**: `_generator/build.py` (изменён), `karta/lica.md` (изменён), `_out/L2-optimizaciya.html` (новый) — ничего сверх заявленного.
- **Линтер:** прогнал сам — идентичен baseline и отчёту (10 warnings, все чужие/глобальные, exit 0).
- **`lica.md`:** прочитал файл целиком — все 4 заявленные строки (monzh/villani/figalli/brenier) содержат ровно то, что в отчёте; текст ролей/био/фото не тронут.

**Вопрос Code (Q1) разобран, не отложен.** Code честно нашёл **5-й** стале-номер (`{kantorovich}` `optimizaciya§25(блокада — кольцо)` — устарел так же, как остальные 4, но остался ВНЕ строгих границ захода в 4 строки) и не тронул его сам, дисциплинированно — это хорошо, не повод возвращать на дочистку. Проверил сам по факту (`L2-optimizaciya.md`: §25 = «Расстояние Вассерштейна», §27 = «Кольцо…» с блокадой/Ладогой) — Code прав. Тривиальная однострочная правка (RUKOVODSTVO §7 — без церемоний), поэтому не стал гонять ещё один Code-заход ради одной строки: **починил сам** (`optimizaciya§25` → `optimizaciya§27` в `lica.md`, якорная замена, совпадение ровно 1), перепрогнал линтер — exit 0, без изменений в предупреждениях.

**Честный факт про фото (не баг, зафиксирован).** `brenier.jpg` отсутствует → моно-плейсхолдер «Б». `stigler.jpg` тоже отсутствует, но Стиглер упомянут в L2 только прозой без `→[stigler]` — портретной карточки не получает вовсе (не 2 моно-плейсхолдера, как ожидалось в заходе, а 1). Косметика: `--p-shannon` попадает в CSS `:root` L2 без карточки (генератор вшивает переменные для всех персон с фото-файлом, не только упомянутых) — поведение `build_lecture`, не regression, не трогали.

**Ратифицирую: принято.** Верификатор Code (свежий субагент, глубина делегирования = 1 — новый CLAUDE.md-лимит соблюдён) — 4/4 PASS, совпал с моей независимой сверкой.

**Дальше (СЕЙЧАС в PLAN).** `visual-design-review` — отрендерить `_out/L2-optimizaciya.html` и посмотреть глазами (ожидаемо: brenier без фото, stigler без карточки вовсе — не чинить как баг, если только владелец не захочет добыть фото). Потом при выходе на сайт — `site-cross-device-audit`. Затем — закрытие арки (ARKA §7): SESSIYA целиком → durable по домам + `SVODKI.md`.

---

## #14 — 2026-07-02 · visual-design-review Л2 HTML → найдены 2 бага генератора (общие с L4)

**Контекст.** Владелец: провести визуальный обзор + вывести файл, чтобы наконец прочитать содержание самому (маркдаун ему тяжело читать).

**Инструментальная преграда (решена).** `playwright`/Chromium — недоступны: `npm`-скачивание Chromium стучится в `storage.googleapis.com`, который блокирует сетевой allowlist песочницы (DNS `EAI_AGAIN`), а pip-колесо `playwright` (47 МБ) не успевало докачаться за 45-секундное окно инструмента. Решение — **WeasyPrint + pypdfium2** (лёгкие чистопитоньи пакеты, докачались после пары попыток): HTML→PDF (принудительный один «лист» 1200×60000px через инжект `@page` в копию файла, т.к. в оригинале `@page` нет — документ веб-continuous, не для печати) → PNG через pypdfium2 → нарезка на 8 срезов по ~3000px (реальный контент оказался ~24000px, остальное — запас) → каждый срез посмотрен глазами (`Read`/vision).

**Метод верификации находок — НЕ доверял одному рендеру.** Раз WeasyPrint — не настоящий браузер (без JS, слабее CSS3), для каждой находки проверял: это баг СЫРОГО HTML-текста (одинаков в любом браузере) или артефакт МОЕГО рендерера?
- Утечка LaTeX и дубли footnote — подтверждены **на уровне сырого текста файла** (`grep`/`re.findall` по самому HTML, без всякого рендера) → 100% совпадут в реальном браузере, не мой артефакт.
- Отсутствие фото-портретов — проверил изолированным тестом (`background-image:var(--custom-prop)` с data-URI в мини-HTML) → CSS-переменные в фоне WeasyPrint **поддерживает** (тест прошёл, красный квадрат отрисовался). Тогда откуда пустота у реальных портретов? Сравнил `.pi` (пустой `<div>`, только `aspect-ratio:1/1`, без контента) с `.pi.mono` (есть `<span>Б</span>` внутри) — только mono-вариант показался. Похоже, WeasyPrint не считает высоту `aspect-ratio` для ПУСТОГО элемента без контента/явной высоты — известный класс проблем у нестандартных рендереров. **Не утверждаю как баг реальной страницы** — пометил как «проверить в браузере».

**НАХОДКА 1 — утечка сырого LaTeX (генератор, не контент).** `build.py` конвертирует `$$…$$` в юникод-псевдоматематику (подстрочные/надстрочные индексы, ≥/≤ — работает), но таблица макросов **неполная**: `\min \max \text{} \sum \mathsf{} \begin{pmatrix}/\end{pmatrix} \nabla \mu \nu \lambda \theta \tfrac \Big \iff \sup \log \qquad \textstyle \mathrm{Lip}` — ни один не конвертируется, вылезают на экран сырыми backslash-командами. Грязнее всего: транспортная матрица §10 (`C=\beginpmatrix8&6&3\\5&4&6\\4&7&5\endpmatrix`), план и потенциалы §11–12 (та же беда), слабая двойственность §16 (`c\mathsf T x≥(A\mathsf T y)\mathsf T x=…` — цепочка неравенств практически нечитаема), и худший случай — определение Вассерштейна §25: помимо утечек `\mu,\nu,\mathrmLip,\Big,\textstyle`, там ЕЩЁ и **баг замены** — второй интеграл `\int` превратился в «∈t» (знак «принадлежит», а не «интеграл» — тихая подмена символа, хуже сырой утечки, т.к. выглядит правдоподобно и может быть прочитана неверно, а не просто «сломано»).
- **Проверил L4 для контекста:** 0 утечек — но НЕ потому что там нет бага, а потому что математика L4 проще (не использует ни один из недостающих макросов). Дремлющий баг, впервые проявленный содержанием Л2.

**НАХОДКА 2 — дубли честных флагов (генератор, общий с L4).** Когда `> поле:честно/нить/боковой …` — последняя строка внутри поля матблока (обычно `[чёрный-ящик]`), она попадает как сырой текст в КОНЕЦ абзаца ЭТОГО поля, а НИЖЕ ещё раз корректно рендерится отдельным стилизованным блоком. Подтвердил на Л2 (7–8 мест: §6,7,12,13×2,17,23,24,25) И на L4 (5–6 мест, тем же способом — `grep 'поле:'` по сырому HTML) — старый баг, который просто никогда не ловили, потому что HTML L4 тоже никто не смотрел глазами до сегодняшнего дня.

**Что хорошо (не только баги).** Композиция/иерархия — чистая (шапка/легенда типов §/цель/бюджет-статус). Типографика — комфортный serif (Georgia, 18px/1.72), понятная цветовая кодировка типов разделов. Цвет — связный тёплый крем/охра/белый, честные флаги — отдельным акцентом (пусть и дублированы багом №2). Вёрстка/отступы — ровные, переполнений не заметил. Там, где математика простая (рацион §2–8), эффект «теорема доступна руками» реально работает — именно это делает контраст с поломанными формулами дальше ещё заметнее.

**Оценка серьёзности.** Оба найденных бага — не «полировка», а дефекты ДОСТОВЕРНОСТИ конспекта (§2.5.0 ТЗ: «конспект = доказательство читаемости»); хуже всего страдают самые «жемчужные» формулы (двойственность, Вассерштейн) — ровно те места, что должны были держать балл B2=4. НЕ блокирует чтение владельцем СЕЙЧАС (смысл/аргументация везде на месте, ломается только ОТОБРАЖЕНИЕ части формул) — но блокирует показ кому-то ещё до фикса генератора.

**Файл выведен владельцу напрямую** (`istochnik/_out/L2-optimizaciya.html`) — просил прочитать сам, возможны содержательные замечания.

**Дальше (СЕЙЧАС в PLAN).** Чинить генератор (`build.py`): расширить таблицу LaTeX-макросов (список выше) + починить утечку `> поле:` в хвосте матблочного поля. Затрагивает ОБЕ лекции (L2 и L4) — регрессия по обеим. После фикса — пересобрать HTML, повторно посмотреть глазами (в идеале уже в реальном браузере — заодно снимет неопределённость с портретами). Потом — при выходе на сайт `site-cross-device-audit`, затем закрытие арки.

---

## #15 — 2026-07-02 · Владелец: чинить генератор ОБЯЗАТЕЛЬНО, сначала фикс — потом чтение

**Контекст.** Владелец посмотрел L4 глазами сам («ничего не видел») и твёрдо сказал: генератор чинить, читать текст в текущем виде отказывается («не могу смотреть на этот текст в таком плохом состоянии») — сначала фикс полностью, потом уже смотреть. Это отменяет план «читать сейчас, чинить потом» — фикс ставится первым.

**Что сделал — полный построчный аудит `build.py` (631 строка) до причины, не только списка симптомов.**
- **Баг «∈t» — root cause найден.** Таблица `_CMD` содержит `(r"\\in", "∈")` БЕЗ границы слова. `\int` начинается с тех же 3 символов `\in` → регэксп частично поглощает `\int`, оставляя хвост «t»: `\int` → «∈»+«t» = «∈t». Не утечка — тихая подмена символа. Тот же класс риска молча грозит и `\infty` (тоже начинается на `\in`).
- **Баг дублей footnote — root cause найден.** `fields_of()` (строка 262) вырезает текст поля матблока регэкспом, который останавливается только перед следующим `**[поле]**` ИЛИ в конце секции — но НЕ перед строкой-сноской `> поле:…`. Если сноска идёт последней строкой поля (типично — после `[чёрный-ящик]`), она попадает В ЗАХВАТ как сырой текст, а `footnotes_of()` (строка 266) НЕЗАВИСИМО находит ту же строку и рендерит её ЕЩЁ РАЗ корректно. Красивое механическое доказательство после фикса: рендер-функция footnote НИКОГДА не выводит слово «поле:» — только исходник его содержит. Значит `grep -c "поле:"` на готовом HTML ОБЯЗАН быть 0; сейчас ~8 (Л2) и ~11 (Л4) — это и есть точный счётчик утечек.
- **Полный список пропущенных LaTeX-макросов** размечен по каждому вхождению (не просто «расширить таблицу») — включая частный случай `\tfrac13` (короткая форма без скобок, existing-правило ловит только `\tfrac{}{}` форму) и матрицы `\begin{pmatrix}…\end{pmatrix}` (нужна отдельная функция с разбором строк/столбцов, не просто строка в `_CMD`).

**Написан Code-заход `kod_fix-latex.md`** (по `RUKOVODSTVO §6`, самодостаточен): §0 zip-бэкап + чтение только `build.py`+`L2-optimizaciya.md`(read-only) → §1 дисциплина (менять только `latex_to_html`/`_CMD`/`fields_of`, прогонять `--lint`+обе сборки после каждого блока) → §2 границы (только эти 2 функции в build.py; НЕ трогать содержание лекций/дизайн/ТЗ) → §3 задача с ДОСЛОВНЫМИ regex-правками (порядок важен: `\int`/`\infty` строкой ВЫШЕ `\in`; новые макросы; функция `_matrix_to_text` — минимум плоский текст «(8  6  3; …)», красивая HTML-таблица — на усмотрение Code, если без риска) → §4 критерий готовности (механически проверяемый: ноль `\[a-zA-Zа-яА-Я]+` в обоих HTML, ноль «поле:» в обоих, ноль «∈t», матрицы читаемы, diff против бэкапа — изменения ТОЛЬКО внутри формул/хвостов полей) → §5 верификатор (свежий субагент, независимый grep + сверка 3 конкретных снимков «было/стало») → §6 отчёт.

**Дальше (СЕЙЧАС в PLAN).** Владелец отправляет `kod_fix-latex.md` в Code → исполнение → приёмка НЕЗАВИСИМОЙ сверкой (как всегда — grep-счётчики, sha256/diff-регрессия по ОБЕИМ лекциям, ручная проверка конкретных формул) → пересборка обеих HTML → повторный визуальный обзор (по возможности в реальном браузере — заодно снимет неопределённость с портретами) → **только тогда** предъявить файл владельцу на чтение. Раньше не показывать.

---

## #16 — 2026-07-02 · `kod_fix-latex.md` → ПРИНЯТО (генератор починен, оба бага закрыты)

**Вердикт: ПРИНЯТО.** Сверил НЕ на слово — по всем пунктам независимо, где возможно — глубже, чем просил заход (полный дословный diff обеих лекций прочитан глазами целиком, не только grep-счётчики).

**Что проверил сам (независимо от отчёта Code и от его верификатора-субагента):**
- **`diff` бэкапа `build.py` против текущего** — ровно 3 зоны правки: таблица `_CMD` (добавления + критичный порядок `\int`/`\infty` ДО `\in` с комментарием-объяснением), новая функция `_matrix_to_text()` + её вызов сразу после `html.escape`, лукахед `fields_of()` (добавлен стоп перед `> поле:`). Больше нигде ни строки — `footnotes_of`/`render_section`/`build_lecture`/`lint`/CLI байт-в-байт нетронуты.
- **Линтер** (`--lint`, оба прогона) — exit 0, те же 10 редакционных предупреждений (все дореформенные, чужие для этого захода), 0 новых структурных.
- **Grep по СВЕЖИМ файлам** (сам считал, не копировал цифры отчёта): Л2 — утечки `\`+буквы 0 (было ~69), `поле:` 0 (было 10), `∈t` 0 (было 1), `∫` **2** (было 0), `begin/pmatrix` 0; Л4 — всё 0/0/0/0, честных флагов 7 (не менялось). Числа: `(3,2)=12`, `230`, `345` — на месте.
- **`diff -rq` дерева против бэкапа** — ровно 3 файла отличаются (`build.py` + оба `_out/*.html`), больше нигде, подтверждено самостоятельно.
- **Полный dословный diff ОБОИХ HTML прочитан целиком глазами** (не только счётчики) — Л2 145 строк/24 хунка, Л4 46 строк/6 хунков. **Каждый без исключения хунк** — либо чистка формулы, либо снятие дубль-хвоста `> поле:…`; ни одной правки прозы/факта/числа не нашёл нигде.
- **Точечная проверка конкретных формул** (по дословному diff): §10–12 матрицы → чистый плоский вид `(8  6  3; 5  4  6; 4  7  5)`; §16 цепочка двойственности → `c^T x≥ (A^T y)^T x = y^T(Ax)≥ y^T b = b^T y` (полностью читаема); §25 Вассерштейн → `W₁(μ,ν)=sup_{f:Lip(f)≤1}(∫ f dμ-∫ f dν)` — **баг «∈t» подтверждён исправленным по существу** (не просто скрыт, второй интеграл теперь настоящий «∫», ровно 2 на формулу); §25 подробность (двойной-escape `\0\×U`) → чисто `0×U`, `θ×U`; домашка §28 `\tfrac13`/`\tfrac23` → `1/3`/`2/3`.
- **Бэкап** `snapshots/istochnik_pre-fix-latex_2026-07-02.zip` — `unzip -t` прошёл без ошибок, точка отката цела.

**Решение Code по матрицам — ратифицирую.** Плоский текст `(a  b  c; …)`, не HTML-таблица — разумно (заход разрешал минимум, Code не полез в рискованный CSS-эксперимент вне границ).

**Три расхождения захода с реальностью, которые Code поймал и скорректировал сам (не на моё слово — проверил все три по итоговому diff'у, все три корректны):** безскобочная форма `\mathsf T` (не `\mathsf{T}`) в §16; `\sum` тёк, хоть и не был в списке захода — добавлен; матрицы разбираются по `&amp;`, т.к. вызов после `html.escape` — учтено верно.

**Попутно (не просил явно, но по духу задачи — «читатель не должен видеть бэкслеш») Code почистил 2 доп. артефакта той же природы, БЫВШИЕ в бэкапе:** §175 осиротевший `\` перед `×`, §11 `X^\*`. Проверил — да, оба присутствовали в старом файле, оба исправлены, новых не внесено.

**Ратифицирую: принято без замечаний, статус — заметно лучше, чем просил критерий готовности** (сам заход просил «ноль утечек», Code дополнительно закрыл 2 смежных бага той же природы и явно об этом отчитался).

**Дальше (СЕЙЧАС в PLAN).** Повторный визуальный обзор обеих HTML (в идеале — уже в реальном браузере, заодно снимет неопределённость с портретами из SESSIYA #14) → **только тогда** предъявить `L2-optimizaciya.html` владельцу на чтение (его исходное условие — сначала фикс, потом смотреть). Потом — при выходе на сайт `site-cross-device-audit`, затем закрытие арки (ARKA §7).

---

## #17 — 2026-07-02 · Дешёвый повторный визуальный обзор → вердикт «готово к чтению»

**Контекст.** Владелец: сам сделай короткий дешёвый визуальный обзор, посмотри как стало, реши, что дальше и когда это дойдёт до идеала с моей стороны — и уже тогда он сам сядет читать и комментировать содержание.

**Метод (дёшево — переиспользовал готовую инфраструктуру, не тратил время на новую).** Тот же WeasyPrint+pypdfium2 из SESSIYA #14 (пакеты уже стояли в песочнице). НЕ 8 срезов как в прошлый раз — переоценил все 28 §, но так, чтобы влезло в 6 срезов по ~4000px (весь контент после обрезки пустого хвоста — 24090px). Просмотрел ВСЕ 6 глазами (не только точечно баги) — это дало заодно и общий взгляд на композицию, не только проверку фикса.

**Результат — все прежние баги закрыты НАСТОЯЩИМ образом, не только по grep.** Своими глазами увидел: §2/§7 (`min`/`max`, `при`, `c^T x` — читается идеально), §8 (`⇔`), §10–12 (матрицы — чистый плоский вид `(8 6 3; 5 4 6; 4 7 5)`, полностью читаемо), §16 (цепочка двойственности `c^Tx≥(A^Ty)^Tx=y^T(Ax)≥y^Tb=b^Ty` — раньше нечитаемая каша, теперь одна строка алгебры без сучка), §22 («методы»/«закон Мура» — плейн текст, не `\textметоды`), §24 (`T=∇φ`), **§25 Вассерштейн — главный прежний провал** (`W₁(μ,ν)=sup_{f:Lip(f)≤1}(∫f dμ-∫f dν)` — оба интеграла настоящие «∫», не «∈t»; подробность `μ на 0×U, ν на θ×U` без двойного-escape), §28 (`1/3`/`2/3`). Дублей footnote нигде не увидел — «честно»/«на полях»/«нить»-рамки идут по одному разу, чисто.

**Структурная критика (6 измерений, кратко — по делу, не стена нитпиков).** Композиция/иерархия — ровный повторяющийся ритм секций (тип-чип→утверждение→логика→подробность→чёрный ящик→честный флаг), читается предсказуемо. Отступы/выравнивание — без замечаний, боксы (claimbox/подробность/честно/на полях/нить) равномерны. Типографика — serif комфортен, иерархия ясна; **математика внутри `eq`-боксов теперь полностью корректна**. Цвет — тёплая кремовая палитра + цветные §-чипы и цветные типы footnote-рамок держат навигацию по типу контента, не режет глаз. Деталь/отделка — два открытых момента (ниже). Соответствие цели — да: конспект прошёл бы собственный тест «§2.5.0 — доказательство читаемости», ни разу не споткнулся о нечитаемый символ на всём проходе.

**Два открытых момента — НЕ блокируют показ, разного типа:**
1. **Портреты (Данциг/Канторович/фон Нейман/Виллани/Фигалли) — вопрос всё ещё открыт, но дальше копать в WeasyPrint бессмысленно.** Тот же рендерер, что и в SESSIYA #14 — этот прогон НЕ добавляет новой информации о том, что покажет настоящий браузер (принципиально не может её дать). Дешевле всего закрыть — владелец сам глянет за 5 секунд, когда откроет файл: если фото на месте — это подтверждает старую гипотезу «ограничение WeasyPrint, не баг реальной страницы»; если пусто и в браузере — тогда это настоящий баг, отдельный маленький Code-заход на `aspect-ratio`/`.pi`. Не стал жечь ещё дешёвый-бюджет на переисследование одного и того же инструмента.
2. **Матрицы — плоский текст, не 2D-таблица (осознанный минимум из `kod_fix-latex.md`).** Полностью корректно и читаемо сейчас; красивая табличная вёрстка со скобкой-рамкой — опциональный будущий апгрейд эстетики, не исправление дефекта. Не стал запускать ради этого новый Code-заход — риск/выгода не оправдывает ещё один проход прямо сейчас (доходность полировки уже падает, `visual-design-review` сам предупреждает про 2–3 итерации и всё).

**Моё решение: генераторный «идеал» — с точки зрения аналитика — достигнут.** Дальше вкладываться в рендер/генератор смысла нет — контент теперь полностью доверенно читаем. Передаю файл владельцу на чтение содержания сейчас, без доп. раунда.

**Дальше (СЕЙЧАС в PLAN).** Владелец читает `L2-optimizaciya.html` и комментирует содержание. По итогам — либо новый шаг правок (content), либо прямиком закрытие арки (ARKA §7). При выходе на публичный сайт (не сейчас) — `site-cross-device-audit`.

---

## #18 — 2026-07-03 · Новый заход: АДДИТИВНЫЕ правки Л6 под дрейфнувший концепт (изучение + ТЗ + развилки)

**Контекст.** Концепт дрейфовал с момента сборки лекции (staleness-флаг ARKA §3.4 сработал закономерно, это и есть повод захода): имя курса «Может ли машина мыслить?»; строка героев «у каждого своя граница»; новый сквозной мотив «Побочный плод» (`formulirovki §6`); порядок Оптимизация = Л6 (`KONCEPT`). Задача — **аддитивно** (добавить связи/людей/сюжеты, НЕ пересобирать зрелую `status:polno` лекцию).

**Вошёл по арк-ритуалу** (NAVIGATOR→PLAN→хвост #17→staleness). Прочитал: `KONCEPT.md` (карточка Л6 §4 + линии §5.1/§5.2 + решения §9), `formulirovki.md §5–6`, `beklog.md` (карточка «ТЗ на правки Оптимизация», стр.45), `istochnik/L2-optimizaciya.md` целиком, `karta/*` (lica/graf/teorii/PROTOKOL), `svyazi-istochniki.md` (ратифицированные связи), арочные `research-L2`/`kompoziciya-L2`.

**Ключевое про ресёрч.** Арочные `research-L2`/`kompoziciya-L2` написаны ДО дрейфа — Винера/Блэкетта/Тьюринга-1948 там НЕТ. Но вся эта фактура уже собрана и **ратифицирована судьёй** (фабрикации нет) в `0-koncept/svyazi-istochniki.md` (разд. C досье Винера, разд. D война-OR). То есть Фаза-2 «ДОБРАТЬ» опирается на готовый ратифицированный банк; в авторинге останется сверить конкретные числа перед финалом (снаряды Блэкетта ~20 000→~4 000; фильтр Винера «Yellow Peril» 1942→моногр.1949; Тьюринг-1948 число обусловленности; отказ Винера — «A Scientist Rebels», Atlantic, янв.1947).

**Собран полный ТЗ-список правок** (таблица правка→куда→тип) — вынесен отдельным доком `pravki-L6.md` (deliverable шага 1). Кратко: **A** новые связи/люди — A1 Винер (2-й сквозной, ПИК; фильтр=опт.предсказание; ⚠️ честно НЕ ЛП, вариационно-квадратичная; шов из Л5), A2 Тьюринг-1948 wink (число обусловленности), A3 Блэкетт/OR («цирк Блэкетта» 1940), A4 нить «Побочный плод» (рамка→флаг), A5 герои-биты «своя граница» (Винер-отказ, фон Нейман-строитель); **B** сверка cross-ref под новый порядок — B1 §23 «следующая лекция→slozhnost» (семантич.рипл→флаг), B2 §7 «Л1 и Л3»→«Л1 и Л2», B3 §18 перечень фон Неймана стале, B4 graf.md стале-§ в рёбрах; **C** karta-производные (нов.лицо {wiener}, нить {pobochnyy-plod}, ребро «оптимальное предсказание» Л5↔Л6). **Уже в лекции — не дублировать:** «Oh, that!» §18 ✓, экономический исток Канторовича §9 ✓ (два пункта из брифа — сверка, на месте).

**Две развилки — вынесены владельцу (AskUserQuestion), решены:**
- **Форма Винера:** ПОЛНОЦЕННЫЙ РАССКАЗ-блок. Владелец: «наша лекция не про линейное программирование, а про **идею оптимизации**» → Винер (опт.предсказание, не ЛП) не нарушает ось, а расширяет её; надо найти правильное место.
- **Рипл §23:** перешить под новый порядок. Владелец: «новый порядок определяет другие зависимости — можно ссылаться на то, что было раньше (Сложность Л2, обратный callback — связь остаётся), и подводить к тому, что будет позже (Мышление Л7)». **+ Директива:** кроме уже найденных мостов — **поискать новые**, согласованные с изменениями концепции.

**Границы захода (беречь):** НЕ трогать `poryadok` (заморожен на 2; renumber — отдельный проход); эталонную ось §1/§7 не менять (Винер/Блэкетт вторым слоем поверх, `KONCEPT §9` п.2); честные флаги (даты Канторовича 1939/1942/1948, минимакс=частный случай, Бренье, вершины m+n−1, WGAN, Монж-фортификатор, §145 SABRE/ORION не ЛП) — не вносить ошибки обратно; Винер честно НЕ ЛП и НЕ автор нейросетей; тяжёлое (война/отказ) — лёгким штрихом. Новый § → внутренний renumber с якорной дисциплиной (как добор #8) — отдать Code.

**Дальше (СЕЙЧАС в PLAN).** Фаза-4 авторинг: написать точный текст всех вставок в `pravki-L6.md` (место Винер-блока + новые мосты) → math-russian-terminology + russian-editor гейт → предъявить владельцу на content-ревью → Фаза-5 `kod_pravki-L6.md` в Code (критерий+линтер-гейт+верификатор).

**Апдейт (тот же ход).** Фаза-4 ЗАКРЫТА — весь текст вставок написан (`pravki-L6.md`) и прогнан через гейт. `math-russian-terminology`: 4 замены (операционные исследования→**исследование операций**; исключение Гаусса→**метод Гаусса/LU-разложение**; средняя ошибка→**среднеквадратичная**; зенитный прицел→**упреждение зенитного огня**); Блэкетт/Тьюринг-1948/число обусловленности/Винер–Хопф подтверждены web-сверкой (рус. Википедия «Исследование операций» и «Блэкетт»; Oxford QJMAM 1948). `russian-editor`: разбил длинный абзац Винер-блока (рябь тире #27), убрал фрагменты-пунчлайны без сказуемого (#16), развёл повтор «это» (#33). **Развилки владелец делегировал** («твоё решение; место не экономить — режем на HTML») → взял полные варианты: блок §26→§27 (хэндофф «лёд→лёд»), «Побочный плод» нитью в §9 И §27, Блэкетт полноценным лицом {blackett}, градиент-рифма §26 внесена. **Фаза-5: `kod_pravki-L6.md` НАПИСАН** (RUKOVODSTVO §6, 13 правок E1–E13 дословно + якоря + порядок renumber §27→§28, §28→§29 + budget + линтер-гейт + верификатор свежим субагентом + zip-бэкап). Ждёт прогона Code.

**Дальше (СЕЙЧАС).** Владелец шлёт `kod_pravki-L6.md` в Code → приёмка по отчёту + независимая сверка → пересборка HTML → SCORECARD §6.2 дельта → флаг риплов (§23-переход; нить «Побочный плод» пока не сфолжена в `KONCEPT §5.2` — отдельный заход владельца) → ревью → закрытие арки.

---

## #19 — 2026-07-03 · `kod_pravki-L6.md` → ПРИНЯТО (Code на Sonnet 5; аддитивные правки Л6)

**Вердикт: ПРИНЯТО.** Прогон Code на **Sonnet 5** (по моей рекомендации). Судил по отчёту + сверил НЕЗАВИСИМО (RUKOVODSTVO §4), где мог — глубже критерия.

**Что проверил сам (не на слово):**
- **Линтер** — прогнал: exit 0, 0 структурных, 11 редакц. (все безобидные: 8× «лицо в 1 лекции» — старый паттерн danzig/kantorovich/monzh/villani/figalli/brenier/stigler + новый {blackett} того же класса; 3× глоб. крипто/граф). 0 `[polno]`-эскалаций.
- **Границы** — `diff -rq` бэкапа (`istochnik_pre-pravki-L6_2026-07-03.zip`) vs текущего: отличаются РОВНО 4 файла (`L2-optimizaciya.md`, `karta/lica.md`, `karta/graf.md`, `_out/L2-optimizaciya.html`). Ничего сверх.
- **Секции** §1…§29 подряд без дыр (§27 Побочный плод / §28 Кольцо / §29 Домашка); budget `vsego_blokov` 38→39, `math_blokov` 21 не тронут.
- **§27-блок** — точный отредактированный текст (Винер/фильтр/Блэкетт/цирк/«A Scientist Rebels»); карта: `{wiener}`+`{blackett}`, нить `{pobochnyy-plod}`, ребро sluchaynost→optimizaciya §27, turing+§19, стале-§ рёбер §23→§25 и §21→§23 — все на месте.
- **HTML** пересобран, 0 сырых LaTeX-утечек (безопасный ASCII-grep), 0 `поле:`, 0 `∈t`.
- **Верификатор Code** (свежий субагент, 123.5 с / 71 643 ток.) — подтвердил все 13 правок + 6 критериев независимо.

**Единственный хвост — разобран, не отложен.** Code честно поймал: линтер выдавал ложное `{wiener}: появлений в 6 лекциях` (реально 4). Root cause (подтвердил сам): `read_lica_appearances()` режет `появления:[...]` наивным `split(',')`, а в моём (дословно вставленном) тексте Винера были запятые ВНУТРИ скобок — `(…фильтр, отказ)`, `(…энтропии, wink)`. Это **мой авторский баг** (нарушил конвенцию — у всех прочих лиц иннер-запятых нет), не ошибка Code. Code дисциплинированно НЕ тронул `build.py` (запрещён §2) и НЕ отступил от «дословно», вынес в `## ВОПРОСЫ` с двумя вариантами — образцовое НЕ-действие (§4). **Починил сам** (тривиально, RUKOVODSTVO §7): запятые → «и»/«—», перелинтил — ложное предупреждение ушло (4 лекции, в норме), HTML пересобрал. Опция на будущее (beklog): закалить регэксп `read_lica_appearances` против запятых в скобках — но конвенция «без иннер-запятых» и так держит.

**Мелкий процесс-недобор Code:** «время+токены» отдал только по верификатору-субагенту; общий расход прогона не инструментировал (сослался на биллинг). Не блок; на будущее — в заход добавлять «в конце вставь вывод `/cost`».

**Эффективность Sonnet 5 — осмысленно (подтверждено практикой):** заход был ровно «исполнительный, полностью расписанный» (якоря, дословный текст, renumber, линтер-гейт, 1 верификатор). Sonnet исполнил всё с ПЕРВОГО прохода — без ретраев (а именно ретраи запускают токен-парадокс), без путаницы, и даже проявил суждение (поймал хрупкость линтера). Глубина Opus тут не покупала ничего: единственная заминка была моим авторским багом, который Opus не «исполнил бы вернее». Opus стоил бы ~2.5× за токен и тянул бы дефицитный ОБЩИЙ недельный кап — за нулевую выгоду. Вывод в дисциплину: **тугой заход держит Sonnet на рельсах, и парадокс не срабатывает** — подтверждает §8.1.

**Дальше (СЕЙЧАС).** Прогнать обновлённую Л6 по SCORECARD §6.2 (дельта против прежнего) → показать владельцу на ревью → флаг риплов (§23; фолд «Побочного плода» в KONCEPT §5.2 — его заход) → при приёмке закрыть арку (ARKA §7).

---

## #20 — 2026-07-03 · SCORECARD §6.2 дельта + HTML владельцу на первый живой просмотр

**SCORECARD-дельта против обкатки (`obkatka-L2.md §1`).** Аддитивный заход сдвинул ровно то, ради чего затевался — интеграцию в курс, не ядро:
- **A5 (вписанность в имя-центр) 3→4** ↑: «Побочный плод» связал Л6 с Л1·Л3·Л4·Л5·Л7; шов Л5→Л6 (Винер) + мост Л6→Л7 (градиент→обучение) делают лекцию явно обязательной частью пакета.
- **B6 (кочующие люди) — цифра та же (4), но прочнее в разы:** «люди реально кочуют» держится теперь не на одном фон Неймане — Винер второй сквозной (Л5·Л6·Л7), Тьюринг явлен в оптимизации (1948), фон Нейман назван сквозным. Закрыта дыра «один сквозной» (KONCEPT §7).
- **A3/B1 глубже (не в цифре):** драма-скамейка (отказ Винера, цирк Блэкетта); мировоззренческий сдвиг «вычисление выпало из чужих вопросов».
- **Не двигалось:** матядро B2=4 (Винер честно не ЛП, вошёл рассказом), B5=4. Ни одна ось не просела.
- **Честные слабые оси (НЕ от этого захода, на будущее):** A1=3 (заголовок), A4=3 (нет промо-картинки) — A-рычаги дизайн-фазы; B3=3/B4=3 (домашка/интерактив «крепкие», не «вау»). Профиль как был — СИЛЁН/СИЛЁН, вердикт ядро/тело.

**HTML предъявлен владельцу** (`istochnik/_out/L2-optimizaciya.html`, 540 КБ, пересобран после фикса Винера) — это его ПЕРВЫЙ живой просмотр лекции (раньше только пересказ/слух). Портреты: 6 с фото (danzig/figalli/fon-neyman/kantorovich/monzh/villani), 3 моно-плейсхолдера без фото (wiener/blackett/brenier — фото добыть позже). Ждём его содержательных впечатлений против его вкуса → доводка → потом двигаться к другим лекциям.

**Дальше.** Впечатления владельца → content-доводка (если будет) → при приёмке закрыть арку (ARKA §7: durable по домам — в т.ч. beklog «закалить `read_lica_appearances` от запятых в скобках» + «добыть фото wiener/blackett/brenier»; фолд «Побочного плода» в KONCEPT §5.2 — заход владельца — + строка в SVODKI).

---

## #21 — 2026-07-03 · Первый живой просмотр владельца → рабочая версия ПРИНЯТА, АРКА ЗАКРЫТА

**Обратная связь владельца (первый раз смотрел HTML глазами, раньше только пересказ).** Доволен: баланс математика/байки/приложения соблюдён, теоремы где доказаны / где рассказаны, объём с лихвой на 1.5 ч, у человека создастся представление и много запомнит. Ворота пройдены — **неплохой результат, принято как рабочая версия.** НО по структуре (записать на будущее, НЕ доделывать сейчас):
- **Перегружено, нет единой истории / считываемого «оглавления».** Пока это набор блоков, не сценарий лекции.
- **Вау не считывается.** Вау оптимизации = «очень непохожие задачи приводят к одной технике». Сейчас два ручных ядра (рацион/транспорт) выглядят математически ПОХОЖЕ → сюрприз «непохожие→одно» не срабатывает.
- **«Ничего в конец» нарушено.** Части выносятся в конец (поля, парковка) — а договаривались единый текст; в этих «приписках» есть самостоятельные куски, которым место в основном сюжете.
- **Вывод владельца:** из этих блоков можно построить интересное, сохранив всё содержание, — добавив художественную линию/понятную арку (откуда куда идём, на что нанизано). Но такие вещи решаются В КОНЦЕ, когда всё собрано, а потом сочиняем/переписываем конкретный текст. Сюжетные линии — позже.

**Закрытие арки (ARKA §7) — выполнено.** Прочитал SESSIYA целиком (#1–#20), разнёс durable по домам:
- Решение (Л6 собрана/принята + метод 2× + H6 + генератор + модель-дисциплина) → `resheniya.md` **п.22**.
- Отложенное (★ художественная ось Л6 + «дискретность двух ядер» + «ничего в конец» · фото портретов · закалить `read_lica_appearances` · матрицы 2D · renumber `poryadok` разблокирован) → `beklog.md` «Из арки Л2 Оптимизация».
- Статус + метод-инсайт «сборка-блоков и сценарий-нарратив = разные фазы, сценарий поздней фазой» → `dnevnik-i-plan.md` (Текущее состояние + Ближайший шаг).
- Источники (Блэкетт/Винер-фильтр сверены; Тьюринг-1948/Винер уже были) → `biblioteka/ISTOCHNIKI.md §Л2`.
- Свод арки (сверху) → `zhurnal/SVODKI.md`; очередь заходов помечена → `zahody-ocheredi.md`.

**АРКА ЗАКРЫТА.** Арк-папка → история. Активны: `istochnik/L2-optimizaciya.md` (`status: polno`) · `_out/L2-optimizaciya.html` · `karta/` · `build.py`. Следующее (новая арка/сессия) — Криптография Л4 (аддитив как у Л6) либо раскатка на несобранные лекции; художественную ось — на финальной фазе.
