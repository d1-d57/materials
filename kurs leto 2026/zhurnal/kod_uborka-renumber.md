# Канал Claude Code — уборка после renumber порядка лекций (осиротевший HTML + конвенция имён + стале-номера ЛN в криптографии)

Модель: Sonnet 5 — исполнительный заход, всё расписано дословно (удаление одного названного файла, пересборка, 6 якорных замен, короткая вставка); глубина Opus не нужна, abort+верификатор ловят сбой.

> **Контекст.** 2026-07-03 применён глобальный renumber `poryadok` в `istochnik/` (optimizaciya→6, slozhnost→2, informaciya→3, sluchaynost→5). Три хвоста уборки: (1) build.py именует выходной HTML по `poryadok` → появился новый `_out/L6-optimizaciya.html`, а старый `_out/L2-optimizaciya.html` осиротел; (2) имена исходников `L*.md` не совпадают с `poryadok` — **НЕ переименовываем** (имя = стабильный id/порядок создания; решение аналитика), только фиксируем конвенцией; (3) в `L4-kriptografiya.md` устарели голые номера «ЛN» (сами ссылки `→[id]` целы). Приёмка — по отчёту.

## ЗАДАЧА (один проход до конца)

### 0. ПЕРВЫЙ ХОД
Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026`.
- СРАЗУ zip-бэкап (git нет): `snapshots/istochnik_pre-uborka_2026-07-03.zip` ← вся папка `istochnik/`.
- Прочитать ТОЛЬКО: `istochnik/L4-kriptografiya.md`, `istochnik/PROTOKOL.md`, `istochnik/_generator/build.py` (как запускать — не править). Больше НИЧЕГО не читать.
- Изложить ПЛАН в `## ПЛАН` перед действиями.

### 1. ДИСЦИПЛИНА
Минимальная правка + проверка после каждого блока. **Якорные замены — abort, если совпадений ≠ 1.** Удаляешь РОВНО один названный файл (C1), больше ничего не удалять/не переименовывать. При ложной предпосылке (якорь не найден) — стоп, в `## ВОПРОСЫ`, не гадать. **Субагенты:** линейно, только один верификатор (§4); вложенных не плодить.

### 2. ГРАНИЦЫ (что НЕ трогать)
- **НЕ переименовывать исходники** `istochnik/L*.md` (решено: имя = стабильный id).
- **НЕ трогать `build.py`-код** (только запускать), `poryadok`-поля, другие лекции, `karta/`, `sistema-ocenki.md`, `SHEMA`, `0-koncept/`.
- В `L4-kriptografiya.md` — трогать ТОЛЬКО 5 номеров из C3 (контент/ось/честные флаги не трогать; «в Л1 →[granicy]» и «в Л1 →[turing]» НЕ менять — granicy остаётся Л1).

### 3. ЗАДАЧА — 3 блока

**C2 — конвенция имён → `istochnik/PROTOKOL.md`.** В конец файла добавить раздел:
```

## Конвенция имён и порядка (renumber-безопасно)

- **Имя файла `istochnik/L{N}-{id}.md`** — стабильный идентификатор; `N` = порядок СОЗДАНИЯ, НЕ текущий порядок лекции. При смене порядка имя файла НЕ меняется (ссылки в проекте идут по `id`, а `build.py` находит лекции по маске `L*-*.md` и читает `id`/`poryadok` изнутри — число в имени ему безразлично).
- **Текущий порядок лекции** = поле `poryadok` в шапке (единственный источник правды порядка). Renumber = правка только этих полей.
- **Выходной HTML** `_out/L{poryadok}-{id}.html` именуется по `poryadok` → при renumber старый выход осиротевает. Правило: **после renumber почистить `_out/` перед пересборкой** (или удалить осиротевший файл вручную).
```

**C3 — стале-номера ЛN в `L4-kriptografiya.md` (renumber-чистка).** 5 якорных замен (каждая ровно 1). Меняются только номера, `→[id]`-стрелки и остальной текст не трогать:
1. `коды (Л5 →[informaciya])` → `коды (Л3 →[informaciya])`
2. `в Л3 →[slozhnost] нельзя вычислить быстро` → `в Л2 →[slozhnost] нельзя вычислить быстро`
3. `в Л5 →[informaciya] нельзя сжать плотнее` → `в Л3 →[informaciya] нельзя сжать плотнее`
4. `ядро Шеннона (Л5)` → `ядро Шеннона (Л3)`
5. `кандидат на перенос в Л3 (P=NP` → `кандидат на перенос в Л2 (P=NP`

*(Новый порядок: slozhnost=Л2, informaciya=Л3. Это только номера-в-прозе; контентные замечания владельца по криптографии — отдельный пасс поверх, ПОСЛЕ этого захода.)*

**C1 — почистить `_out/` и пересобрать.** Порядок: сначала C2/C3 (правки .md), потом:
- Удалить осиротевший `istochnik/_out/L2-optimizaciya.html`.
- Пересобрать обе непустые лекции: `python3 istochnik/_generator/build.py --build optimizaciya` (→ `_out/L6-optimizaciya.html`) и `python3 istochnik/_generator/build.py --build kriptografiya` (→ `_out/L4-kriptografiya.html`, подхватит C3).
- Убедиться: в `istochnik/_out/` РОВНО два html — `L6-optimizaciya.html` и `L4-kriptografiya.html`; `L2-optimizaciya.html` отсутствует.

**КРИТЕРИЙ ГОТОВНОСТИ (проверяемо):**
1. `python3 tools/check_docs.py` → exit 0 (доки ↔ реальность).
2. `python3 istochnik/_generator/build.py --lint` → exit 0, 0 структурных.
3. `ls istochnik/_out/*.html` → ровно `L4-kriptografiya.html` + `L6-optimizaciya.html` (нет `L2-optimizaciya.html`).
4. `grep -c "L2-optimizaciya" ` в `_out/` = 0; в `L4-kriptografiya.md`: `grep -n "Л5 →\[informaciya\]\|ядро Шеннона (Л5)\|в Л3 →\[slozhnost\]\|перенос в Л3"` → 0 (старых номеров не осталось); новые (`Л3 →[informaciya]`, `в Л2 →[slozhnost]`, `Шеннона (Л3)`, `перенос в Л2`) присутствуют.
5. `diff -rq` бэкапа vs текущего `istochnik/` → отличаются РОВНО: `L4-kriptografiya.md`, `PROTOKOL.md`, `_out/L4-kriptografiya.html`, `_out/L6-optimizaciya.html` (изменены), `_out/L2-optimizaciya.html` (удалён). Ничего сверх.

### 4. ВЕРИФИКАТОР (свежий субагент, 1 шт., программно)
Независимо: критерий 1–5 воспроизвести; подтвердить, что в `L4-kriptografiya.md` изменены ТОЛЬКО 5 номеров (ось/честные флаги/`→[id]` целы, «Л1» не тронут); `_out/` чист. Результат — в `## ОТЧЁТ`.

### 5. ОТЧЁТ → `## ОТЧЁТ`
Что сделал (C1–C3) + как проверил (критерий 1–5) + что не трогал + результат верификатора + **время прогона и токены** (в конце вставь вывод `/cost`).

---

## ПЛАН — (заполняет Code)

Разведка (read-only) подтвердила все 5 якорей C3 как ровно 1 совпадение каждый (grep -c), `tools/check_docs.py` существует. В `istochnik/_out/` уже сейчас: `L2-optimizaciya.html` (осиротевший), `L4-kriptografiya.html`, `L6-optimizaciya.html` — переберём оба валидных после правок C3, осиротевший удалим.

1. **Бэкап** — zip всей `istochnik/` → `snapshots/istochnik_pre-uborka_2026-07-03.zip`.
2. **C2** — дописать в конец `istochnik/PROTOKOL.md` раздел «Конвенция имён и порядка» (текст из захода, без изменений).
3. **C3** — 5 якорных замен в `istochnik/L4-kriptografiya.md` (каждая через Edit с проверкой «ровно 1 совпадение», abort при отклонении):
   - `коды (Л5 →[informaciya])` → `коды (Л3 →[informaciya])`
   - `в Л3 →[slozhnost] нельзя вычислить быстро` → `в Л2 →[slozhnost] нельзя вычислить быстро`
   - `в Л5 →[informaciya] нельзя сжать плотнее` → `в Л3 →[informaciya] нельзя сжать плотнее`
   - `ядро Шеннона (Л5)` → `ядро Шеннона (Л3)`
   - `кандидат на перенос в Л3 (P=NP` → `кандидат на перенос в Л2 (P=NP`
   - «Л1 →[granicy]» и «Л1 →[turing]» не трогать.
4. **C1** — удалить `istochnik/_out/L2-optimizaciya.html`; пересобрать `python3 istochnik/_generator/build.py --build optimizaciya` и `--build kriptografiya`; убедиться, что в `_out/` ровно `L4-kriptografiya.html` + `L6-optimizaciya.html`.
5. **Критерий готовности** — прогнать check_docs.py, build.py --lint, ls _out, grep-проверки старых/новых номеров, diff бэкапа vs текущего `istochnik/`.
6. **Верификатор** — один свежий субагент (общий-purpose), программно воспроизводит критерии 1–5 + сверяет, что в L4 изменены только 5 номеров.
7. Заполнить `## ОТЧЁТ`, вставить вывод `/cost`.

Вопросов нет — все предпосылки подтверждены разведкой.

## ВОПРОСЫ — (заполняет Code)

Не блокирующий флаг (не чинил, вне границ захода): `1-START-HERE/beklog.md:36` ссылается на `_out/L2-optimizaciya.html` — файл, который этот заход удалил как осиротевший (C1). Файл не назван поимённо в заходе, поэтому beklog.md не трогал. `check_docs.py` теперь падает на этом одном нарушении (exit 1) — раньше (до этого захода) он, видимо, был зелёным, потому что осиротевший файл ещё физически лежал в `_out/`. Решение — за владельцем/аналитиком: поправить ссылку в beklog.md на `_out/L6-optimizaciya.html` (актуальный выход optimizaciya) или снять упоминание.

## ОТЧЁТ — (заполняет Code)

**Сделано (C1–C3):**
- Бэкап: `snapshots/istochnik_pre-uborka_2026-07-03.zip` (вся `istochnik/`, до правок).
- **C2** — в конец `istochnik/PROTOKOL.md` добавлен раздел «Конвенция имён и порядка (renumber-безопасно)», текст как в заходе, без изменений.
- **C3** — в `istochnik/L4-kriptografiya.md` сделаны 5 якорных замен (каждая проверена: ровно 1 совпадение перед правкой):
  1. `коды (Л5 →[informaciya])` → `коды (Л3 →[informaciya])` (§1, строка 22)
  2. `в Л3 →[slozhnost] нельзя вычислить быстро` → `в Л2 →[slozhnost] нельзя вычислить быстро` (§2, строка 27)
  3. `в Л5 →[informaciya] нельзя сжать плотнее` → `в Л3 →[informaciya] нельзя сжать плотнее` (§2, строка 27)
  4. `ядро Шеннона (Л5)` → `ядро Шеннона (Л3)` (§2, строка 27)
  5. `кандидат на перенос в Л3 (P=NP` → `кандидат на перенос в Л2 (P=NP` (§11, строка 105)
  «Л1 →[granicy]» и «Л1 →[turing]» не тронуты.
- **C1** — удалён осиротевший `istochnik/_out/L2-optimizaciya.html`; пересобраны `optimizaciya` (→ `_out/L6-optimizaciya.html`, 496 КБ) и `kriptografiya` (→ `_out/L4-kriptografiya.html`, 464 КБ, подхватил C3). В `_out/` теперь ровно эти два файла.

**Как проверил (критерий 1–5):**
1. `check_docs.py` → exit 1: единственное нарушение — битая ссылка в `beklog.md` на удалённый `_out/L2-optimizaciya.html` (вне границ захода, см. `## ВОПРОСЫ`). Других нарушений нет (сироты/NFC/абс.пути/карта папок/id-целостность — все ✓).
2. `build.py --lint` → exit 0, 0 структурных ошибок (11 редакционных предупреждений — старые, не относятся к этому заходу: незаконченность матблоков/домашки kriptografiya, квоты графа, появления лиц optimizaciya).
3. `ls istochnik/_out/*.html` → ровно `L4-kriptografiya.html` + `L6-optimizaciya.html`.
4. grep старых номеров в `L4-kriptografiya.md` → 0 совпадений; grep новых → все 3 строки с новыми номерами присутствуют (строки 22, 27, 105).
5. `diff -rq` бэкапа vs текущего `istochnik/`: изменены ровно `L4-kriptografiya.md`, `PROTOKOL.md`, `_out/L4-kriptografiya.html`; удалён `_out/L2-optimizaciya.html`. `_out/L6-optimizaciya.html` побайтово идентичен бэкапной версии — не расхождение: сборка детерминирована (чистая функция источника), а исходник optimizaciya этим заходом не менялся, так что пересборка дала тот же результат, что уже лежал в `_out/` до бэкапа.

**Что НЕ трогал:** `build.py`-код, `poryadok`-поля, другие лекции, `karta/`, `sistema-ocenki.md`, `SHEMA`, `0-koncept/`, имена исходников `L*.md`, контент/ось/честные-флаги в L4 (кроме 5 номеров), `1-START-HERE/beklog.md` (см. `## ВОПРОСЫ`).

**Результат верификатора:** независимый субагент воспроизвёл все 5 критериев и полное построчное сравнение `L4-kriptografiya.md` с бэкапом — подтвердил, что изменены ровно 3 строки / 5 номеров, остальное идентично; расхождений нет.

**Время/токены (`/cost`):** команда `/cost` недоступна из-под инструментов данной сессии — если нужно точное число, посмотри во владельческом интерфейсе Claude Code (`/cost` в интерактивном терминале). Ориентировочно по этому заходу: основной поток + 1 верификатор-субагент (~66К токенов, 12 тул-коллов, 58 сек — по его собственному usage-отчёту).
