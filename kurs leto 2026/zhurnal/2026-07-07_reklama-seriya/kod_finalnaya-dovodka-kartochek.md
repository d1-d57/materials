# Канал исполнителя — финальная доводка карточек выпуска-1 + фиксация дизайн-схемы (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные ниже якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу ЭТОГО файла (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ).
> Метрика — КАЧЕСТВО (визуальная связность набора). Часы — норма. Вход агента (CLAUDE.md) уже прочитан тобой автоматически — следуй ему.

КОНТЕКСТ. Проект — рекламная карусель для Телеграма: 6 карточек 4:5 (1080×1350) к онлайн-курсу
«Может ли машина мыслить?». Набор уже собран и почти доведён (несколько кругов правок с владельцем).
Осталась финальная доводка + окончательная фиксация дизайн-схемы, которая дальше станет основой
веб-формата сайта курса.

ЦЕЛЬ этого захода: (1) довести набор так, чтобы 6 карт **красиво читались рядом** в раскладке поста
(карусель/контакт-лист), как единая система; (2) финальный микро-полиш; (3) **окончательно
зафиксировать** дизайн-схему `DESIGN-SYSTEM-kartochki-v2.md`.

Приёмка — по твоему ОТЧЁТУ, без построчной сверки, но по гейтам (§3, §4).
**СТОП ДО ЦЕЛИ (громко):** ты доводишь HTML-набор и фиксируешь схему. Ты **НЕ** публикуешь в
Телеграм и **НЕ** делаешь сайт — это отдельные заходы. Получишь отполированный `kartochki-vypusk-1.html`
+ locked-схему, но НЕ публикацию и НЕ веб-версию.

## 0. ПЕРВЫЙ ХОД
- Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-07-07_reklama-seriya/`
- **Точка отката: проект НЕ под git → сразу сделай zip-бэкап рабочей папки** (единственный откат). Например `zip -r _backup_dovodka_$(date +%Y%m%d_%H%M).zip .` в этой папке.
- Прочитай ПОИМЁННО только (все — в рабочей папке):
  - `kartochki-vypusk-1.html` — набор (это его ты правишь; эталон-реализация схемы).
  - `DESIGN-SYSTEM-kartochki-v2.md` — дизайн-схема (канон; её фиксируешь).
  - `MANIFEST-kartochki.md` — незыблемые принципы стиля.
  - `teksty-vypusk-1.md` — тексты дословно (эталон для сверки слов).
  - `verstka-akcenty-vypusk-1.md` — раскладка блоков и акценты.
- Сначала напиши ПЛАН в `## ПЛАН`, только потом правь.

## 1. ДИСЦИПЛИНА (Карпатов)
- **Думать до правки.** Назови предпосылки; развилку не выбирай молча — покажи в отчёте. Ложную
  предпосылку (в т.ч. в этом заходе) — оспорь по живым файлам с доказательством.
- **Минимум.** Ровно то, что нужно для связности и полиша. Никаких спекулятивных абстракций,
  не переписывать рабочий CSS/HTML ради красоты.
- **Хирургия.** Трогай только нужное; каждая правка трассируется к пункту §3. Чужой стиль (токены,
  отступы, имена классов) сохраняй. Якорные строковые замены — **abort, если совпадений ≠ 1**.
- **Цель — проверяемое условие** (§3 КРИТЕРИЙ), петля до зелёного; «выглядит правильно» — не критерий,
  поэтому суди по РЕНДЕРУ (см. §3).
- **Сохранять по умолчанию:** безопасное делай; рискованно-неоднозначное оставь и опиши в `## ОТЧЁТ`.
- **СУБАГЕНТЫ — аккуратный режим.** Разрешены, в т.ч. вложенные, осмысленно; ≤5 параллельно;
  рейт-лимит = отступить и доложить, НЕ слепой ретрай. Здесь достаточно: 1 свежий субагент-верификатор
  текста (§4). Больше — только по необходимости, назвав в плане сколько и на что.

## 2. ГРАНИЦЫ (не трогать)
- **Тексты — дословно** из `teksty-vypusk-1.md`. Слова не менять, не досочинять, не переформулировать.
- **Уже внесены и УТВЕРЖДЕНЫ владельцем намеренные отступления от исходной пунктуации — сохранить их, НЕ откатывать:**
  1. нет точки в конце абзацев (внутренние точки остаются);
  2. неразрывный пробел перед тире (` —`, тире не начинает строку);
  3. на карте 6 у заголовков-вопросов убраны кавычки-ёлочки («…?» → …?);
  4. на обложке убрано «1936 · 1950».
  Ёлочки-цитаты внутри текста (карта 3: «чего машина не может», «может ли она думать») — это
  содержание, оставить.
- **Токены и шкала кеглей** (`DESIGN-SYSTEM-kartochki-v2.md`) — не ломать. Палитра фиксирована
  (петроль/золото/коралл). Один шрифт-герой.
- **Формат 4:5 (1080×1350), 6 карт** — не менять. Не добавлять подписи/легенды/рамки/колонтитулы.

## 3. ЗАДАЧА
Мутирующий заход. Правишь `kartochki-vypusk-1.html` и дописываешь/фиксируешь `DESIGN-SYSTEM-kartochki-v2.md`.

**3a. Связность набора «рядом» (главное).** Собери раскладку поста — 6 карт в один ряд/сетку
(контакт-лист), как их увидят в карусели — и доведи, чтобы они читались единой системой:
- **Единая верхняя линия заголовков.** Сейчас `.main` центрирует контент по вертикали → на лёгких
  картах (5 «Для кого», 6 «Уровень сложности») заголовок стоит НИЖЕ, чем на плотных (2,3,4). Для
  шаблонной сетки (и будущего сайта) **приведи заголовки всех контентных карт к общей верхней линии**
  (обложка — исключение, там заголовок-герой по центру). Освободившийся низ на лёгких картах
  закрывают уже имеющиеся мотивы: вертикальная линия (карта 5) и лента 0/1 (карта 6). Если где-то
  всё же образуется заметная дыра снизу — подними кегль/интерлиньяж В ПРЕДЕЛАХ ШКАЛЫ или, как
  исключение, оставь центрирование на этой карте, объяснив в отчёте.
- **Ритм мотивов по ряду** проверь и при нужде выровняй: скобки-лента на 1 и 6 (сверху/снизу),
  вертикальная линия на 2 и 5, чистые 3 и 4, один коралловый узел на иллюстрацию. Должно читаться
  ровно и симметрично, без «перекоса» коралла или графики в одну сторону ряда.
- **Единый ритм полей и коралла** между картами (не должно быть карты заметно «пустее» или
  «краснее» прочих).

**3b. Финальный микро-полиш** каждой карты (по рендеру + тест-миниатюрой с телефонной ширины):
переносы (0 тире в начале строки; без висячих слов — `text-wrap: balance`, для длинных колонок
`pretty`), кернинг/интервалы, контраст, отсутствие переполнения/обрезки, аккуратные поля.

**3c. Зафиксировать схему.** Свери `DESIGN-SYSTEM-kartochki-v2.md` с финальным HTML (устрани дрейф,
внеси принятое решение по верхней линии заголовков в §«Открытые решения» как закрытое), и **пометь
схему locked** (дата, «канон, готово к переносу на сайт»). Не плодить новых доков.

**КРИТЕРИЙ ГОТОВНОСТИ (каждый может провалиться, проверь по рендеру/файлам):**
- Все 6 карт рендерятся без переполнения и обрезки; контакт-лист (6 в ряд) читается как единый набор;
  заголовки контентных карт — на общей верхней линии (или задокументированное исключение).
- Слова совпадают с `teksty-vypusk-1.md` дословно — верификатор §4 даёт **0 расхождений** (после
  нейтрализации 4 утверждённых отступлений из §2).
- Правила набора держатся на ВСЕХ картах: 0 финальных точек в абзацах, 0 тире в начале строки,
  нет висячих слов/строк.
- `DESIGN-SYSTEM-kartochki-v2.md` согласован с HTML и помечен locked.
- В `## ОТЧЁТ` есть время прогона и потраченные токены.

Рендер: отрисуй карты в PNG чем есть в системе (headless Chrome / Playwright / puppeteer; Space
Grotesk/Manrope/JetBrains Mono тянутся из Google Fonts по `@import`) и суди ГЛАЗАМИ, итерируй. Если
рендер в системе недоступен — остановись и напиши это громко в `## ОТЧЁТ` (не сдавай «вслепую»).

## 4. ВЕРИФИКАТОР (обязателен — заход трогает текст-смежное)
Отдельный СВЕЖИЙ субагент, ДРУГИМ методом, читает оригинал из `teksty-vypusk-1.md` (и/или из
zip-бэкапа) и проверяет ПРОГРАММНО, что в HTML не изменились СЛОВА карт. Метод: вытащить текст карт
из HTML (снять теги/SVG), у обоих сторон нейтрализовать утверждённые отступления (убрать точки,
ёлочки, `·`, привести неразрывные пробелы к обычным), сравнить пословно. Ожидаемый результат:
список расхождений ПУСТ (единственное допустимое — отсутствие «1936 · 1950» на обложке).
Результат (число расхождений + что именно) — в `## ОТЧЁТ`.

## 5. ОТЧЁТ → пиши в `## ОТЧЁТ` внизу
Что сделал и ЗАЧЕМ (каждая правка → к пункту §3) / как проверил (рендер, тест-миниатюрой) / решение
по верхней линии заголовков и любые исключения / что НЕ трогал / результат верификатора §4 /
статус фиксации схемы / **время прогона + потраченные токены**.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
