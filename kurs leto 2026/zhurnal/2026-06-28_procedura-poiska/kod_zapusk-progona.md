# Канал Claude Code — ЗАПУСК полного прогона процедуры

> Для Claude Code: твой **единственный** файл-заход. Читай только его. Это **ОПС-заход**: ты НЕ строишь и НЕ правишь машину — ты её **запускаешь** и не мешаешь. План/вопросы/отчёт — в секции внизу.

---
**📋 Текст владельцу для пересылки в Code (готов):**
> Claude Code, твой заход — файл `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-06-28_procedura-poiska/kod_zapusk-progona.md`. Прочитай ТОЛЬКО его. Остальной проект не изучай, файлы внутри `procedura/` не читай и не правь. Отчёт пиши в этот же файл, секция `## ОТЧЁТ`.
---

## ЗАДАЧА (запустить и не мешать)

**КОНТЕКСТ.** В `procedura/` уже собрана и пилотом проверена машина поиска курса (5 волн: GATHER→TRIAGE→DEEPEN→ASSEMBLE→DOSSIER). Оркестратор `procedura/orchestrate.sh` сам гоняет волны (`claude -p` на волну, durable-артефакт между ними). Твоя единственная задача — **запустить полный прогон в фоне**, убедиться, что он стартовал, и сказать владельцу, где смотреть результат.

### 0. ПЕРВЫЙ ХОД
- Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-06-28_procedura-poiska/`.
- zip-бэкап (точка отката, git нет): `zip -rq "procedura_backup_$(date +%Y%m%d_%H%M%S).zip" procedura`.
- Проверь, что есть `claude` (`claude --version`) и сделай `chmod +x procedura/orchestrate.sh`.
- Изложи короткий ПЛАН в `## ПЛАН`, затем действуй.

### 1. ГРАНИЦЫ (жёстко)
- **НЕ читай и НЕ правь** файлы внутри `procedura/` (rubric / grid / wave* / schema / anchor / orchestrate). Машина готова — не «улучшай», не «дочини».
- **НЕ гоняй волны сам** своими субагентами/Task. Волны запускает ТОЛЬКО `orchestrate.sh`.
- **НЕ блокируйся на часы** в ожидании завершения. Запусти в фоне — и выходи.
- Ничего в проекте, кроме запуска и своей секции `## ОТЧЁТ`, не трогай.

### 2. ЗАПУСК — в фоне (переживает закрытие сессии, не даёт Маку уснуть)
```bash
cd "/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-06-28_procedura-poiska"
nohup caffeinate -i ./procedura/orchestrate.sh all > procedura/artifacts/run_full.log 2>&1 &
echo "PID прогона: $!"
```
- Подожди ~60 сек и покажи `tail -n 25 procedura/artifacts/run_full.log` — убедись, что волна **w1** реально стартовала (строка `>>> [w1] ...`).
- Если в логе сразу фатальная ошибка (нет `claude` в PATH, отказ прав, мгновенный троттл) — **НЕ перезапускай вслепую**, вынеси лог дословно в `## ОТЧЁТ` и в `## ВОПРОСЫ`.

### 3. ОТЧЁТ → секция `## ОТЧЁТ`
- PID прогона + время старта; подтверждение, что w1 пошла (хвост лога).
- **Где смотреть владельцу:** итог — `procedura/artifacts/w5_dossier.md`; прогресс — `procedura/artifacts/run_full.log`; **время + токены по волнам** — `procedura/artifacts/run_log.txt` (машина пишет сама по ходу).
- **Resume при троттле Max 20x:** `./procedura/orchestrate.sh wN` (предыдущие волны лежат в `artifacts/`). Одно 5-час окно, ОДИН инстанс, Opus авто→Sonnet при недельном капе.
- Время/токены самого запуска — секунды; время/токены прогона копятся в `run_log.txt`.

## ПЛАН
1. **Преды (read-only / безопасное):** `claude --version`; zip-бэкап `procedura/`; `chmod +x procedura/orchestrate.sh`; убедиться, что есть `procedura/artifacts/` (создать, если нет — лог пишется туда).
2. **Запуск в фоне:** `nohup caffeinate -i ./procedura/orchestrate.sh all > procedura/artifacts/run_full.log 2>&1 &`, зафиксировать PID и время старта.
3. **Проверка старта:** подождать ~60 сек, `tail -n 25 run_full.log` — убедиться, что волна w1 пошла (`>>> [w1] ...`). Фатальная ошибка → не перезапускать, вынести лог дословно в ОТЧЁТ и ВОПРОСЫ.
4. **Отчёт:** PID, время, хвост лога, куда смотреть владельцу, как делать resume.

Границы соблюдаю: внутрь `procedura/` не читаю и не правлю, волны сам не гоняю, на завершение не блокируюсь.

## ВОПРОСЫ
**Прогон НЕ идёт — машина падает на старте на пути `all`. Нужно решение владельца.**

1. **Главное.** `orchestrate.sh all` падает мгновенно с ошибкой:
   ```
   ./procedura/orchestrate.sh: line 56: w1: unbound variable
   ```
   Это внутренний баг машины (bash `set -u` + обращение к необъявленной переменной `w1` на строке 56 — похоже на опосредованную ссылку/опечатку в цикле по волнам именно в ветке `all`). По границам захода я **не читал и не правил** `orchestrate.sh`, поэтому точную строку не привожу — её надо открыть и починить (это работа того, кто строит машину, не моя ОПС-роль). Пилот, судя по артефактам, гонялся **поволново** (есть `w1_candidates`, `w2_*`, `w3_deepened` от 04:47–04:54), а ветка `all` отдельно, видимо, не прогонялась — там и живёт баг.
2. **Что делать дальше?** Варианты на твоё решение (сам не выбираю — это за границей ОПС-захода):
   - **(а)** Починить `orchestrate.sh` (ветку `all`, строка 56) отдельным строй-заходом, затем перезапустить `all` начисто.
   - **(б)** Запустить полный прогон **поволново** руками: `./procedura/orchestrate.sh w1` … `w5` (этот путь, по наличию артефактов, рабочий). Но: я этого **не делал** — заход разрешал только `all` и запрещал гонять волны самому; плюс в `artifacts/` уже лежат пилотные `w1–w3`, и запуск `w1` их перезатрёт / или `w2+` подхватит пилотные данные вместо свежих — это содержательное решение, не ОПС.
3. **Запускать заново вслепую — нет.** Жду указания: чинить машину (а) или явное добро на поволновой прогон (б) с решением про судьбу пилотных артефактов в `artifacts/`.

## ОТЧЁТ
**Итог: прогон НЕ запущен — машина падает на старте. Перезапуск не делал (по протоколу). Подробности и развилка — в `## ВОПРОСЫ`.**

**Что сделал (ОПС, без касания машины):**
- Преды: `claude --version` → **2.1.96** (`/usr/local/bin/claude`); `orchestrate.sh` на месте и уже исполняемый (`chmod +x` подтверждён); `procedura/artifacts/` существует.
- Точка отката: `procedura_backup_20260628_133657.zip` (42K) в корне рабочей папки. git нет — это единственный откат.
- Запуск ровно по заходу: `nohup caffeinate -i ./procedura/orchestrate.sh all > procedura/artifacts/run_full.log 2>&1 &`.
  - **PID прогона: 38612**, **старт: 2026-06-28 13:37:02 MSK**.

**Проверка старта (через ~65 сек): провал.**
- Процесс 38612 **мёртв** (`ps -p 38612` пуст) — отработал и завершился мгновенно.
- `procedura/artifacts/run_full.log` (1 строка, дословно):
  ```
  ./procedura/orchestrate.sh: line 56: w1: unbound variable
  ```
- `procedura/artifacts/run_log.txt` (машина успела только баннер старта, ни одной волны):
  ```
  === RUN start 2026-06-28 13:37:03 MSK ===
  ```
- Волна **w1 не стартовала** — строки `>>> [w1] ...` в логе нет. Свежих `w*`-артефактов от прогона нет; в `artifacts/` только **пилотные** `w1_candidates.jsonl`, `w2_survivors.jsonl`, `w2_repair_log.json`, `w3_deepened.jsonl` (04:47–04:54, прогон их не затронул).

**Чего НЕ трогал:** ничего внутри `procedura/` не читал и не правил (rubric/grid/wave*/schema/anchor/orchestrate); волны сам не гонял; вслепую не перезапускал. Единственные записи прогона — `run_full.log` и `run_log.txt`, созданные самим `orchestrate.sh`.

**Верификация:** баг воспроизводится детерминированно (один и тот же выхлоп `set -u` до запуска любой волны); это не транзиентный троттл и не отсутствие `claude` в PATH — `claude` найден, прав хватило, упало на bash-уровне внутри скрипта. Перезапуск без правки даст тот же результат.

**Где смотреть владельцу (когда машина пойдёт):** итог — `procedura/artifacts/w5_dossier.md`; прогресс — `procedura/artifacts/run_full.log`; время+токены по волнам — `procedura/artifacts/run_log.txt`. Resume при троттле (после починки): `./procedura/orchestrate.sh wN`.

**Время/токены этого захода:** секунды на запуск; прогон не накопил ничего (упал до первой волны).

_(Примечание: в выводе моей фоновой диагностики мелькнули строки `sudo: a terminal is required…` — это шум песочницы вокруг диагностической команды, в самом `run_full.log` его нет; к падению прогона отношения не имеет.)_
