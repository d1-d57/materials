# Канал Claude Code — ПРОГОН процедуры субагентами (ты — оркестратор)

> Для Claude Code: твой **единственный** файл-заход. Читай его + названные контракты в `procedura/`. План/вопросы/отчёт — в секциях внизу.

---
**📋 Текст владельцу для пересылки в Code (готов):**
> Claude Code, твой заход — файл `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-06-28_procedura-poiska/kod_progon-subagentami.md`. Прочитай его и названные в нём файлы `procedura/`. Остального проекта не касайся. План/вопросы/отчёт пиши в этот же файл, в секции внизу.
---

## ЗАДАЧА — прогнать процедуру ТВОИМИ субагентами (как в пилоте)

**КОНТЕКСТ.** Машина `procedura/` собрана и проверена пилотом. Headless `claude -p` (через `orchestrate.sh`) в этом окружении **НЕ работает** — дочерний процесс не авторизован (401). НО пилот успешно гонял волны w1→w3 **ТВОИМИ субагентами** (Task/Agent) внутри этой авторизованной сессии, с тирингом моделей и python-валидацией — рабочие артефакты лежат в `procedura/artifacts/_pilot/`. **Сделай так же — на всю сетку, до досье.** `orchestrate.sh` НЕ используй.

**Ты — оркестратор.** Гонишь 5 волн по очереди, durable-артефакт на диск ПОСЛЕ каждой; внутри волны работу делают твои субагенты по контракту волны.

### 0. ПЕРВЫЙ ХОД
- Папка машины: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-06-28_procedura-poiska/procedura/`.
- Прочитай контракты: `grid.md` (сетка+засев), `rubric.md` (судья), `scorecard.schema.json` (контракт), `anchor.md` (якорь Монте-Карло), и `wave1.md … wave5.md` (что делает каждая волна).
- zip-бэкап: `zip -rq "procedura_backup_$(date +%Y%m%d_%H%M%S).zip" procedura`.
- Подчисти мусор прошлой неудачи: удали 0-байтные `procedura/artifacts/w*_run.json` (если есть). Пилотные артефакты уже отложены в `artifacts/_pilot/` — стартуем чисто.
- Короткий ПЛАН → `## ПЛАН`, затем действуй.

### 1. ЦИКЛ ВОЛН (durable ПОСЛЕ каждой; внутри — твои субагенты по `waveN.md`)
- **w1 GATHER — субагенты `model: haiku`.** По `grid.md` (~60 ячеек область×мотив) спавни субагентов **батчами 3–5** (rate-limits Max 20x). Каждый по `wave1.md`: 1–3 темы-кандидата в своей ячейке + источники-на-факт + ЧЕРНОВОЙ scorecard строго по `scorecard.schema.json`, инлайн. Аудиторный гард (аудитория — НЕ-айтишники) — в `wave1.md`. Склей → `artifacts/w1_candidates.jsonl`.
- **w2 TRIAGE — `model: sonnet`.** Мёрж, дедуп (хэш+смысл), ворота-отвал (провал любого Г1–Г8 → выбыл/доработать), ранг к якорю; **валидация+ремонт** невалидных scorecard (Haiku слипает типы — чини по `wave2.md`). → `artifacts/w2_survivors.jsonl`.
- **w3 DEEPEN — `model: sonnet`, ИЗОЛИРОВАННЫЙ субагент на КАЖДУЮ тему.** По `wave3.md`: discovery-fiction-крафт по дуге сложности + факт-чек ОТДЕЛЬНЫМ проходом (пер-факт, право «не нашёл») + ПОЛНЫЙ scorecard НАНОВО (не наследовать баллы) + крючки-параллели. → `artifacts/w3_deepened.jsonl`.
- **w4 ASSEMBLE — ядро `model: opus` СКУПО; судья `model: sonnet` (ИНОЙ тир).** По `wave4.md`: чемпион-6 + внутренние развилки, MMR-покрытие, изолированный судья когерентности, **анти-хак-tripwire** (сверка с `anchor.md` каждое поколение; счёт растёт, а ранг к якорю нет → СТОП+флаг), кап 3–5 поколений. Если Opus недоступен (недельный Opus-кап Max 20x) — бери `sonnet`, пометь в логе. → `artifacts/w4_assembly.json`.
- **w5 DOSSIER — `model: sonnet`.** По `wave5.md`: досье — чемпион (6 лекций, порядок, роли) + вау-питч + внутренние развилки + карта мостов + scorecard'ы + открытые развилки владельцу + честные флаги. → `artifacts/w5_dossier.md`.

### 2. ВАЛИДАЦИЯ + АНТИ-ХОЛОСТОЙ-УСПЕХ (критично — прошлый прогон «прошёл» ВХОЛОСТУЮ)
- После каждой scorecard-волны валидируй ПРОГРАММНО: `python3` + `jsonschema` против `procedura/scorecard.schema.json`. Невалид → почини (стиль w2) или выкинь с логом.
- **Волна НЕ выполнена, если её durable-артефакт ПУСТ, 0-байтный или невалиден.** Пусто/битьё → **СТОП, отчитайся, дальше НЕ иди.** (Прошлый `orchestrate.sh` молча счёл пустой 401-вывод успехом и «прошёл» все волны за 3 сек без досье — так НЕ делай: проверяй контент, а не код возврата.)
- Веди `artifacts/run_log.txt`: на волну — число записей, валидных/починенных, время, токены субагентов.

### 3. КОНТЕКСТ / RESUME (durable — твоя страховка от авто-компакта)
Если к концу волны контекст тяжелеет или поймал троттл Max 20x: **допиши durable, отчитайся «волна N готова, артефакт на диске», и ОСТАНОВИСЬ.** Владелец запустит свежую сессию с этим же заходом и припиской «продолжи с волны N+1, читай `artifacts/wN`». Чекпойнть агрессивно — лучше durable+resume, чем потерять прогон.

### 4. ГРАНИЦЫ
- Контракты `procedura/` (`waveN.md`/`rubric`/`schema`/`grid`/`anchor`) — **читай и исполняй, не переписывай**.
- `sistema-ocenki.md` и остальной проект — не трогай. Пиши только в `procedura/artifacts/` + свои секции внизу.
- `orchestrate.sh` / `claude -p` не используй (headless не авторизован).
- Дыра в рубрике (канон) → флаг в `## ОТЧЁТ`, не правка втихую.

### 5. ОТЧЁТ → секция `## ОТЧЁТ`
До какой волны дошёл; по каждой — число записей + валидных/починенных + время + токены; пути к durable; если остановился — **точка resume**; вскрытые дыры/флаги; **общее время + токены прогона**.

## ПЛАН

**Прочитано:** заход + `grid.md`, `rubric.md`, `scorecard.schema.json`, `anchor.md`, `wave1–5.md`, пилотные `_pilot/{w1,w2,w3}`. Формат, который реально отработал, понятен.

**Подготовка (сделано):** zip-бэкап папки; удалены 0-байтные `w*_run.json` + пустой `run_full.log` прошлой 401-неудачи; проверен валидатор `python3+jsonschema 4.25.1` — гоняет на пилотных артефактах (6/6 и 2/2 валидны). Свежий `run_log.txt` начат. `_pilot/` не тронут.

**Модель прогона — я оркестратор, работу делают мои субагенты (Agent/Task), `orchestrate.sh` не используется:**
- **w1 GATHER (haiku):** 10 субагентов — по одному на **область** (вся строка из 6 мотивов сетки §2), батчами 3–4 (rate-limits Max 20x). Решение оркестратора: область-на-субагента вместо ячейка-на-субагента — даёт целевые ~80–150 черновых scorecard'ов при вменяемом числе вызовов и не топит мой контекст 60 tool-результатами. Контракт `wave1.md` (что делает агент на ячейке) исполняется как есть. Контракты `rubric/anchor/schema` инлайнятся в промпт (субагент НЕ читает файлы проекта — анти-CC-рамка). Склейка → `w1_candidates.jsonl`.
- **w2 TRIAGE (sonnet):** 1 субагент — валидатор/ремонт → дедуп → ворота-отвал → пере-оценка наново → ранг к якорю. → `w2_survivors.jsonl` + `w2_repair_log.json`.
- **w3 DEEPEN (sonnet):** изолированный субагент на КАЖДУЮ выжившую тему, батчами 3–5. DF-крафт + факт-чек отдельным проходом + полный scorecard наново. → `w3_deepened.jsonl`.
- **w4 ASSEMBLE (opus скупо + sonnet-судья иного тира):** чемпион-6, MMR-покрытие, изолированный судья когерентности, анти-хак-tripwire против `anchor.md`, кап 3–5 поколений. Opus недоступен → sonnet с пометкой. → `w4_assembly.json`.
- **w5 DOSSIER (sonnet):** досье владельцу. → `w5_dossier.md`.

**Анти-холостой-успех (жёстко):** после каждой scorecard-волны — программная валидация против схемы; durable пуст/0-байт/невалид → СТОП, отчёт, дальше не иду. `run_log.txt` веду по волнам.

**Чекпойнт/resume:** durable пишу ПОСЛЕ каждой волны; тяжелеет контекст / троттл → дописываю, отчитываюсь «волна N готова», останавливаюсь.

## ВОПРОСЫ

Блокирующих нет — заход самодостаточен, отработал до конца. Решения владельца, вскрытые прогоном (вынесены в досье §7 «Открытые развилки»):
1. **Слот 5: «сложность» vs «языки».** Размен Коммивояжёр (P vs NP) → Марков/LLM сделан по рекомендации изолированного судьи (закрывает ветку-2 и финал «в кармане»=ChatGPT, убирает единственную дорогую тему с завышенным рангом). Цена — из покрытия Н4 уходит область «сложность/оптимизация». Коммивояжёр оставлен сильнейшим сменным слотом. **Выбор характера курса за вами.**
2. **Область «обучение/нейросети» отсеяна целиком** на w2 (все темы n1=false «дорогие» + слабое матядро для не-айтишников). В чемпионе ИИ присутствует только как финал ветки-2 через Марков→LLM. Если хочется отдельной НС-лекции — это сознательное добавление 2-й дорогой темы (Н1 позволяет ≤2), нужен ваш сигнал.
3. **lecturer-fit по умолчанию** (демонстративно / ранний формализм / историчный вход) — развилки заготовлены пер-слот в досье §3, дефолт нужно зафиксировать под конкретного лектора.

## ОТЧЁТ

**Докуда дошёл:** все **5 волн до досье** (w1→w5), durable после каждой, прогон чистый. `orchestrate.sh` не использовался — работа сделана моими субагентами (Agent/Task) с тирингом моделей внутри этой авторизованной сессии, как в пилоте.

**По волнам** (число записей / валидных-починенных / durable):
- **w1 GATHER** (haiku×10 по областям, батчи 3–4): **74** черновых scorecard, JSON-валидны 74/74, схемно 62/74 (12 — штатный haiku-брак обл. «логика»: `dvizhki` массивом / `vs_yakor` строкой, чинит w2). Покрыты все 10 областей сетки. → `artifacts/w1_candidates.jsonl`.
- **w2 TRIAGE** (sonnet×1): 74→**17** выживших, **17/17 схемно-валидны**. 6 дедуп-слияний (PageRank ×6→1, Тьюринг/остановка ×5→1, Хэмминг+Вояджер ×4→1, Марков ×4→1, Игра жизни ×4→1, P vs NP ×4→1); починено 9 кривых записей; де-инфляция ядра́ ~33→8; repair_log 53 записи. → `w2_survivors.jsonl`, `w2_repair_log.json`.
- **w3 DEEPEN** (sonnet×17, изолированный на тему, батчи по 5): **17** углублённых, **17/17 схемно-валидны** (3 механические починки: `vs_yakor` «–»→«хуже», вычистка протёкших полей факт-чека из `sources`). Факт-чек отдельным проходом поймал **фабрикации**: «Нобель Шеннона» (FALSE), «QR/CD на коде Хэмминга» (FALSE→Рида-Соломона); imprecise-уточнения (Бенфорд/Ньюком, маршрутов (n−1)!/2, «LLM=Марков» как аналогия). → `w3_deepened.jsonl` (+ `_w3parts/`).
- **w4 ASSEMBLE** (opus×2 + sonnet-судья×1 иного тира): чемпион-6 **Остановка(вход)→RSA→Эйлер→Хэмминг+Вояджер→Марков→LLM→Монте-Карло(финал)**; Н1=0 дорогих, Н2/Н3/Н4 ✓, 6 разных областей. **Анти-хак сработал как задумано:** изолированный судья поймал ложное «0 подозрительных» автора (3 завышения Pugh) + дыру ветки-2 → **tripwire СРАБОТАЛ**, разрешён в gen2 (размен + честное понижение Эйлер/Хэмминг до «хуже/хуже», финал 0 подозрительных). Opus был доступен (в недельный кап не упёрлись). → `w4_assembly.json` (+ `_w4parts/`).
- **w5 DOSSIER** (sonnet×1): `w5_dossier.md` (257 строк/36 КБ), 8 разделов + цитационный проход на 25 утверждений с пометкой осиротевших. → `w5_dossier.md`.

**Валидация (анти-холостой-успех).** После каждой scorecard-волны — программная проверка `python3 + jsonschema 4.25.1` против `scorecard.schema.json`. Финальная сверка durable: **все артефакты непусты и валидны (ЗЕЛЁНО)** — w2 17/17, w3 17/17 схемно-валидны, w4 чемпион-6 + ворота, w5 36 КБ. Проверял **контент, не код возврата** — прошлый пустой 401-прогон так бы не «прошёл».

**Точка resume:** не потребовалась — дошёл до досье в одной сессии.

**Вскрытые дыры / флаги:**
- Все факт-чек-флаги и tripwire-трейс вынесены в досье §8 (не скрыты).
- **Дыр в рубрике/каноне не нашёл** — рубрика отработала на всём диапазоне (от якоря до отсева λ-уровня тем). Контракты `procedura/` не правил.
- Орк-решение (зафиксировано в ПЛАНе): w1 — субагент **на область** (10), а не на ячейку (60), ради вменяемого числа вызовов и моего контекста; контракт `wave1.md` исполнен по сути. Субагентам w2–w5 разрешал читать **ровно их входной артефакт** по абс. пути (анти-CC-рамка «не блуждать по проекту/CLAUDE.md» соблюдена), данные текли через durable — это плюмбинг ввода, не правка контрактов.

**Затронутые файлы:** только `procedura/artifacts/**` (новые durable + `_w3parts/`, `_w4parts/`, `run_log.txt`) и эти секции. `_pilot/`, контракты, `sistema-ocenki.md`, остальной проект — не тронуты. Сделан zip-бэкап папки перед стартом; удалён 0-байтный мусор прошлой неудачи.

**Общее время / токены:** прогон ~1 ч (14:06→15:04 MSK по логам); субагентов-волн ≈36; токены субагентов **≈1.56 млн** (w1≈350k, w2≈156k, w3≈697k, w4≈265k, w5≈91k). Детали по волнам — `artifacts/run_log.txt`.

**Что читать владельцу:** `procedura/artifacts/w5_dossier.md`.
