# Канал Claude Code — процедура поиска (поисковик-организм)

> Для Claude Code: твой **единственный файл-заход**. Читай только его и файлы, названные ПОИМЁННО в §0; остальной проект не изучай. План, вопросы и финальный отчёт пиши в этот же файл, в секции внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ).

---

**📋 Текст владельцу для пересылки в Code (готов):**
> Claude Code, твой заход — файл `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-06-28_procedura-poiska/kod_procedura-poiska.md`. Прочитай ТОЛЬКО его и то, что он называет; остальной проект не изучай. Папка не под git — сначала zip-бэкап. План, вопросы и финальный отчёт пиши в этот же файл, в секции внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ). Ничего сверх задачи не трогай.

---

## ЗАДАЧА (один проход до конца)

**КОНТЕКСТ.** Проект — проектирование платного онлайн-курса по научпоп-математике; идёт арка «процедура поиска тем». Дизайн запускаемой процедуры поиска курса (5 волн, durable-машина) **уже собран и пилот де-рискован** аналитиком — он в `spec-procedury.md` (v3) и `seed-zaseva.md`. **Твоя задача: материализовать дизайн в самодостаточную папку-машину `procedura/` и де-рискнуть её мини-пилотом** на реальном субстрате Code (`--json-schema`, тиринг моделей). Ты НЕ запускаешь ночной мегапрогон — только строишь машину и гоняешь короткий пилот. Приёмка — по твоему отчёту + программным гейтам §4.

### 0. ПЕРВЫЙ ХОД
- Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-06-28_procedura-poiska/`.
- **СРАЗУ zip-бэкап** этой папки (git нет — это единственная точка отката).
- Прочитать **ТОЛЬКО** (поимённо):
  1. `spec-procedury.md` — весь дизайн v3. Несущее: **§2** (таблица файлов `procedura/` — что собрать), **§3** (I/O каждой из 5 волн), **§4** (несущие механизмы: анти-хак-tripwire, constraint-then-rank, два уровня оптимизации), **§1** (субстрат/тиринг/resume), **§6** (гейты пилота), **§7** (runbook), **§10** (риски пилота → 4 требования ниже). Сетку (§5) берёшь из `seed-zaseva.md`, не из §5.
  2. `../../1-START-HERE/sistema-ocenki.md` — **канон-фитнес**. Из него дистиллируешь `procedura/rubric.md`.
  3. `seed-zaseva.md` — сведённый засев (сетка 10×6, пул-16, тьюринговский узел, якорь, донорская полка, festival, дуга сложности). Из него собираешь `procedura/grid.md`.
- Изложить **ПЛАН** в `## ПЛАН` ДО действий.

### 1. ДИСЦИПЛИНА
Минимальная правка + проверка. Якорные строковые замены — abort при совпадениях ≠1. Сохранять по умолчанию; рискованно-неоднозначное — оставить и описать. **Оспаривай ложную предпосылку** (даже из этого захода) по живым файлам прежде, чем строить. Один риск за раз: read-only разведка → сборка по плану → сверка.

### 2. ГРАНИЦЫ (что НЕ трогать)
- **НЕ запускать ночной мегапрогон.** Только пилот 1–2 ячейки (§3C), ~15 мин, дёшево.
- **НЕ править `sistema-ocenki.md`** — это канон. Пилот вскрыл дыру в рубрике → **флаг в `## ОТЧЁТ`**, не правка втихую.
- Писать **только** в `procedura/` (+ свою секцию `## ОТЧЁТ`). Остальной проект не трогать; durable НЕ выносить в дома проекта (живёт в арке).
- **Старую пуловую систему баллов** (`2-idei/syuzhety-pul-i-research.md`, A18/B21) НЕ удалять и НЕ править — пометка «superseded» это решение владельца, не твоё.

### 3. ЗАДАЧА (конкретно)

**A. Собрать `procedura/` per spec §2** — самодостаточную папку-машину (ночной агент берёт контракт ИЗ НЕЁ, не из разбросанных доков). 8 файлов + `artifacts/`:

| Файл | Что собрать |
|---|---|
| `rubric.md` | Дистиллят `sistema-ocenki.md` в **самодостаточную судейскую форму**: ворота Г1–Г8 (лекция) + Н1–Н4 (набор); оси A1–A5 / B1–B4 (0–3 **без весов**); движки (квиз-аффорданс, обыденное-понятие, двойная ценность, discovery-fiction); якорные дескрипторы «3=» по каждому критерию; эталон Монте-Карло (Pugh лучше/=/хуже); DF-сигнал как keystone. **КАПКАН (впиши явной строкой):** фитнес — этот канон; **старая взвешенная A≤18/B≤21 — superseded, НЕ использовать.** |
| `scorecard.schema.json` | Типизированный контракт (одна тема = один объект). `required`: ворота `g1…g8`,`n1…n4` (bool); `a1…a5`,`b1…b4` (int, 0–3); `dvizhki` (object флагов); `vs_yakor` ({a,b} enum `["лучше","=","хуже"]`); `verdikt` (enum — значения дословно как в `rubric.md`: `["ядро","продажник","тело","связка","доработать","выбыла"]`); `rol` (string); `sources` (array of {fakt, istochnik}). **Ключи — латиницей, значения enum — кириллицей дословно как в рубрике** (JSON в UTF-8), чтобы `structured_output` не падал на маппинге вердиктов. Это впечатывается `--json-schema`→`structured_output`. |
| `anchor.md` | Эталонный scorecard Монте-Карло (A-сильный, B-сильный) + ручные сверки из `seed-zaseva.md` §5: Бенфорд→тело, дни рождения→продажник, λ-исчисление→выбыла. Это tripwire анти-хака (сверяется каждое поколение ASSEMBLE). |
| `grid.md` | Из `seed-zaseva.md`: сетка 10×6 (область×мотив) + предзасев (пул-16, книга, obj-станции) + тьюринговский узел (каркас) + якорь + донорская полка + дуга сложности лекции (§8 засева). Самодостаточна. |
| `wave1.md … wave5.md` | Промпты 5 волн по I/O из **spec §3** (GATHER Haiku → TRIAGE → DEEPEN Sonnet → ASSEMBLE Opus-скупо → DOSSIER Sonnet). В КАЖДЫЙ вшить 4 требования из блока **B**. |
| `orchestrate.*` | Оркестратор-над-волнами: каждая волна = отдельный вызов `claude -p`, читает durable-артефакт прошлой и пишет свой (workflow межсессийно не резюмится — resume = перезапуск следующей волны; внутри волны троттл → волна целиком заново). |
| `RUNBOOK.md` | Как запускать per spec §7: вызовы `claude -p … --json-schema procedura/scorecard.schema.json …`; env-тиринг (`CLAUDE_CODE_SUBAGENT_MODEL` w1=haiku, w2/w3/w5=sonnet; Opus только воркеры ядра w4); оговорки Max 20x (один инстанс; одно 5-час окно; Opus только w4; durable между волнами при троттле); zip-бэкап перед прогоном. |
| `artifacts/` | Пустая папка под durable-цепь: `w1_candidates.jsonl → w2_survivors.jsonl → w3_deepened.jsonl → w4_assembly.json → w5_dossier.md`. |

**B. Четыре несущих требования (из пилота, spec §10) — вшить как ЖЁСТКИЕ, не пожелания:**
1. **Анти-Claude-Code-рамка в КАЖДОМ `waveN.md`** (дословно, в начало промпта): «*Ты не Claude Code. Этот промпт самодостаточен. НЕ читай файлы проекта, НЕ ищи файл-заход, НЕ подхватывай `CLAUDE.md`. Работай по промпту и верни результат инлайн строго по схеме.*» (Причина: ночные агенты в папке проекта подхватывали `CLAUDE.md` и спрашивали «где заход» вместо работы.)
2. **`--json-schema` на уровне CLI** (structured_output по `scorecard.schema.json`) **+ программный валидатор/ремонт в TRIAGE (w2):** невалидное чинить или выбрасывать с логом. (Причина: Haiku слипает ключи — A-ключи в B-объекте, `verdikt` прозой вместо enum.)
3. **Засев-стикинесс + аудиторный гард в `wave1.md` (GATHER)** (дословно): «*Аудитория — НЕ-айтишники: престижно-недоступное штрафуй, A2 (ширина) ставь честно; держись засева ячейки, не убегай к недоступному.*» (Причина: дешёвый агент бросал доступные засевы ради престижных и завышал «ширину».)
4. **TRIAGE (w2) и DEEPEN (w3) пере-оценивают scorecard сильной моделью НАНОВО — НЕ наследуют баллы GATHER.** Дискриминация живёт в Sonnet+, не в Haiku. Дугу сложности лекции + ось lecturer-fit (`seed-zaseva.md` §8) вшить в discovery-fiction-крафт DEEPEN.

**C. Прогнать ПИЛОТ (spec §6)** на 2 ячейках, дёшево (~15 мин), цепочкой GATHER→TRIAGE→DEEPEN:
- ячейка A: вероятность×DF → **Монте-Карло** (есть якорь — проверить, что Pugh к нему стабилен на повторе);
- ячейка B: информация×обыденное → **Шеннон/бит**. В DEEPEN ячейки B **подсадить одну ложь** (фактическую) — проверить, что отдельный проход факт-чека её ловит ИЛИ честно говорит «не нашёл».
Дыра вскрыта → правка промптов/схемы → отчёт. Дыра в рубрике (канон) → флаг владельцу, не правка.

**КРИТЕРИЙ ГОТОВНОСТИ — проваливаемый, см. §4.**

### 4. ВЕРИФИКАТОР / КРИТЕРИЙ
Заход **создаёт** (не двигает/не удаляет) → тяжёлый лосс-верификатор не нужен, но финальным **свежим проходом** проверь ПРОГРАММНО (bash/grep/jq) и отчитайся по каждому пункту (любой провал → чинить ДО отчёта либо вынести дырой в `## ВОПРОСЫ`):
- [ ] `procedura/` содержит все 8 файлов + `artifacts/`.
- [ ] `rubric.md`: grep находит `Г1`…`Г8`, `Н1`…`Н4`, «Монте-Карло», и строку про «superseded»/`A18` (старая система помечена).
- [ ] `scorecard.schema.json`: парсится как валидная JSON-схема; `required` включает ворота + `a1..a5` + `b1..b4` + `verdikt`(enum) + `sources`.
- [ ] `wave1.md…wave5.md`: анти-CC-строка (треб. B1) найдена grep'ом в **каждом** (5/5); аудиторный гард (B3) — в `wave1.md`.
- [ ] **Пилот:** в `artifacts/` лежат w1/w2/w3-файлы; вывод w1 на 100% валиден по схеме (или невалид починен/выброшен — с логом); факт-чек w3 поймал подсадку ИЛИ честно сказал «не нашёл»; судья на **повторе** дал ТОТ ЖЕ ранг к якорю (стабильность, не плывёт).
- [ ] Битых внутренних ссылок/путей в `procedura/` — **0** (проверить программно).

### 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что собрал и зачем · как проверил (вывод каждого гейта §4) · что НЕ трогал · результат пилота (4 гейта spec §6: схема держится / факт-чек ловит / судья стабилен / Haiku-вывод годен) · вскрытые дыры рубрики (флаг владельцу) · что вынес в `## ВОПРОСЫ`.

---

## ПЛАН

**Бэкап сделан** → `scratchpad/backup_procedura-poiska_20260628_043712.zip` (точка отката, git нет).

**Сверка предпосылок (по живым файлам, ложных не нашёл):** ворота Г1–Г8/Н1–Н4, оси A1–A5/B1–B4, движки, Монте-Карло+Pugh, enum ролей, сетка 10×6, якорь+ручные сверки — всё подтверждено в `sistema-ocenki.md` и `seed-zaseva.md`. Нюанс: канон в прозе пишет «доработка», шаблон §7 — «доработать»; пиню `доработать` и в `rubric.md`, и в схеме (structured_output требует дословного совпадения значений enum).

**Шаги:**
1. `procedura/` + `artifacts/` — каркас.
2. `rubric.md` — дистиллят канона в судейскую форму (ворота, оси, движки, дескрипторы «3=», Монте-Карло-эталон, DF-keystone, КАПКАН про superseded A18/B21).
3. `scorecard.schema.json` — строгая JSON-схема (ключи латиницей, enum-значения кириллицей дословно как в рубрике).
4. `anchor.md` — эталонный scorecard Монте-Карло (A-сильн, B-сильн) + 3 ручные сверки (Бенфорд→тело, дни рождения→продажник, λ→выбыла).
5. `grid.md` — сетка 10×6 из `seed-zaseva` + пул-16 + Тьюринг-узел + донорская полка + дуга сложности.
6. `wave1.md…wave5.md` — промпты 5 волн по I/O spec §3; в КАЖДЫЙ вшить 4 жёстких требования (B1 анти-CC-рамка во всех 5; B2 валидатор/ремонт в w2; B3 аудиторный гард в w1; B4 пере-оценка сильной моделью в w2/w3).
7. `orchestrate.sh` — оркестратор-над-волнами (волна = отдельный `claude -p`, durable I/O, resume = перезапуск волны).
8. `RUNBOOK.md` — запуск per spec §7 (тиринг env, оговорки Max 20x, zip перед прогоном).
9. **Пилот** (2 ячейки, дёшево): GATHER на Haiku-субагенте → TRIAGE/DEEPEN/судья на Sonnet-субагентах (тиринг = реальный субстрат субагентов Code). Ячейка A=вероятность×DF→Монте-Карло (стабильность Pugh к якорю на повторе); ячейка B=информация×обыденное→Шеннон, в DEEPEN подсадить 1 ложь → факт-чек ловит/«не нашёл». Артефакты w1/w2/w3 в `artifacts/`. Схему валидирую программно (python jsonschema); невалид → ремонт/выброс в TRIAGE с логом.
10. **Верификатор** свежим проходом — все чекбоксы §4 программно (grep/jq/python), битые ссылки = 0. Провал → чиню до отчёта или выношу в `## ВОПРОСЫ`.
11. **Отчёт** §5.

*Границы:* пишу только в `procedura/` + свои секции; `sistema-ocenki.md`, старую пуловую систему, остальной проект не трогаю; ночной мегапрогон НЕ запускаю.

## ВОПРОСЫ

1. **Ворота набора `n1..n4` в пер-темовой схеме.** Заход (§3, строка `scorecard.schema.json`) явно потребовал `n1…n4` в `required` пер-темового scorecard, и я это исполнил дословно. Но концептуально Н1 (≤2 дорогих), Н2 (единство), Н4 (покрытие) — свойства **набора из 6**, не одиночной темы; естественно пер-темовой лишь Н3 (контекст). Сейчас агент трактует их как «вклад/пригодность темы к набору» (жёсткое применение в ASSEMBLE). **Так и оставить, или в пер-темовой схеме держать только `n3`, а `n1/n2/n4` считать в ASSEMBLE на уровне набора?** Не блокер — машина рабочая в обоих вариантах.

2. **Реконсиляция расхождений между сильными волнами.** Пилот показал: TRIAGE и DEEPEN — обе Sonnet — могут разойтись в оценке одной темы (Шеннон: TRIAGE → `тело`, vs_yakor A=хуже/B==; DEEPEN → `тело`, но vs_yakor A=лучше/B=лучше, что по рубрике даёт `ядро` — внутреннее противоречие самого DEEPEN). Анти-хак-дизайн закладывает примирение изолированным судьёй в ASSEMBLE, но **правило реконсиляции (чей балл финальный при расхождении TRIAGE↔DEEPEN) в спеке явно не прописано.** Я вшил в `wave3.md` требование «согласуй vs_yakor и verdikt», но межволновой арбитраж — решение владельца/аналитика. Флаг, не правка канона.

3. **Дыра в рубрике — НЕ обнаружена** (канон не трогал). Пилот честно отработал по `sistema-ocenki`; провалов рубрики, требующих флага владельцу, не выявил. Если потребуется стресс-тест рубрики (а не контракта) — это отдельный заход.

## ОТЧЁТ

**Статус: машина собрана, пилот прогнан, все 6 программных гейтов §4 — PASS. Ночной мегапрогон НЕ запускал (вне скоупа).**

### Что собрал и зачем
Самодостаточная папка-машина [`procedura/`](procedura/) (11 файлов + `artifacts/`) — ночной агент берёт контракт ИЗ НЕЁ, не из разбросанных доков:
- [`rubric.md`](procedura/rubric.md) — дистиллят канона `sistema-ocenki.md` в судейскую форму: ворота Г1–Г8/Н1–Н4, оси A1–A5/B1–B4 (0–3 без весов), движки, дескрипторы «3=», Pugh к Монте-Карло, DF-keystone, tripwire. **КАПКАН явной строкой:** старая `A≤18/B≤21` — `superseded`, не использовать.
- [`scorecard.schema.json`](procedura/scorecard.schema.json) — строгая JSON-схема (Draft 2020-12, валидна). Ключи латиницей, enum-значения кириллицей дословно как в рубрике (`verdikt`: ядро/продажник/тело/связка/доработать/выбыла), чтобы `structured_output` не падал на маппинге. `additionalProperties:false` + плоские `a*/b*` → слипание ключей детектируется.
- [`anchor.md`](procedura/anchor.md) — эталонный scorecard Монте-Карло (валиден по схеме) + 3 ручные сверки (Бенфорд→тело, дни рождения→продажник, λ→выбыла). Tripwire анти-хака.
- [`grid.md`](procedura/grid.md) — сетка 10×6 + пул-16 + Тьюринг-узел + донорская полка + дуга сложности + lecturer-fit (из `seed-zaseva`, самодостаточно).
- [`wave1.md`](procedura/wave1.md)…[`wave5.md`](procedura/wave5.md) — промпты 5 волн по I/O spec §3.
- [`orchestrate.sh`](procedura/orchestrate.sh) — оркестратор-над-волнами (волна = отдельный `claude -p`, durable I/O, resume = перезапуск волны, тиринг через `CLAUDE_CODE_SUBAGENT_MODEL`).
- [`RUNBOOK.md`](procedura/RUNBOOK.md) — запуск per spec §7 (тиринг, оговорки Max 20x, zip перед прогоном).

**4 несущих требования (spec §10) вшиты как ЖЁСТКИЕ:**
1. Анти-CC-рамка дословно в начале **каждого** `waveN.md` (5/5).
2. `--json-schema` на уровне CLI (`orchestrate.sh`/`RUNBOOK`) + программный валидатор/ремонт в TRIAGE (`wave2.md` шаг 0).
3. Аудиторный гард дословно в `wave1.md`.
4. TRIAGE/DEEPEN пере-оценивают scorecard НАНОВО (не наследуют баллы GATHER); дуга сложности + lecturer-fit вшиты в DEEPEN.

### Как проверил (вывод каждого гейта §4)
Свежий программный проход (python+jsonschema, grep). Все PASS:
1. ✅ 11 файлов машины + `artifacts/`.
2. ✅ `rubric.md`: Г1–Г8, Н1–Н4, «Монте-Карло», `superseded`+`A18` — найдены.
3. ✅ `scorecard.schema.json` валиден; `required` ⊇ ворота + a1–a5 + b1–b4 + verdikt(enum) + sources; enum вердиктов дословный.
4. ✅ анти-CC-рамка 5/5; аудиторный гард в `wave1.md`.
5. ✅ пилот: w1/w2/w3 лежат; **w1 raw 3/6 валидны → после ремонта TRIAGE 6/6 + лог**; w3 scorecard'ы валидны; факт-чек поймал подсадку.
6. ✅ битых внутренних ссылок на файлы машины — 0.

### Результат пилота (4 гейта spec §6) — на реальном субстрате субагентов Code с тирингом
Прогнал 2 ячейки цепочкой GATHER(Haiku)→TRIAGE(Sonnet)→DEEPEN(Sonnet) + 2 изолированных судьи(Sonnet). Артефакты в [`procedura/artifacts/`](procedura/artifacts/).

1. **Схема держится на дешёвых агентах** ✅ (с ремонтом, как и предусмотрено). GATHER-Haiku в ячейке «информация» вернул `dvizhki` как **bool `true/false` вместо enum** `⊕/–/сильный` — **ровно риск §10.2**. 3/6 невалидны на сырье → TRIAGE починил все (`true→⊕`, `false→–`, с пере-оценкой) → **6/6 валидны**, 4 ремонта в [`w2_repair_log.json`](procedura/artifacts/w2_repair_log.json).
2. **Факт-чек ловит подсадку** ✅. В DEEPEN ячейки B подсадил ложь «Шеннон получил Нобелевскую премию по физике 1948». Отдельный факт-чек пометил её **FALSE** с верным объяснением (Нобеля по математике/CS нет), F5 → `imprecise`, остальное `verified`. Бонус: DEEPEN Монте-Карло сам сходил в веб и поймал F4 (Манхэттен vs послевоенная H-бомба) как `imprecise`.
3. **Судья стабилен на повторе** ✅. Два изолированных судьи независимо дали **идентичный** ранг к якорю: `vs_yakor {a:"=", b:"="}`, `verdikt:"ядро"`. Не плывёт.
4. **Haiku-вывод годен, не каша** ✅. Кандидаты связные, с источниками-на-факт; пригодны после дисциплины TRIAGE. Подтвердился и тезис §10: **Haiku завышает и не дискриминирует** (всё «ядро»/«=эталону»), **Sonnet де-инфлирует** — TRIAGE опустил все темы с провальными воротами до `доработать`, оставив единственным чистым `ядро` только сам якорь Монте-Карло.

### Вскрытые дыры → что починил
- **Находка пилота (новая, не в §10):** DEEPEN-субагент **тоже** схлопывает scorecard в неплоскую форму (`gates{}`/`axes{A,B}`/`dvizhki:"⊕"`, ошибочный `n3=false` у Монте-Карло) — без CLI `--json-schema` модель дрейфует от плоской схемы. В ночном прогоне `orchestrate.sh` подаёт `--json-schema` на КАЖДУЮ волну (это страхует), но как defense-in-depth я **усилил `wave2.md` и `wave3.md`** явной строкой про плоские ключи верхнего уровня (`g1..g8/a1..a5/b1..b4`, не вкладывать) и про `dvizhki` как объект-enum. Артефакт `w3_deepened.jsonl` нормализован к валидной плоской схеме; где DEEPEN потерял данные (Монте-Карло) — пер-критериальные баллы взяты из TRIAGE (помечено `_pilot_note`).
- В `wave3.md` добавил: согласовывать `vs_yakor`↔`verdikt` (всплыло на Шенноне) и не переносить в `sources` факты с вердиктом `FALSE`/«не нашёл».

### Что НЕ трогал
- `sistema-ocenki.md` (канон) — не правил; дыр рубрики не нашёл.
- Старую пуловую систему (`2-idei/syuzhety-pul-i-research.md`, A18/B21) — не удалял/не правил; «superseded» помечено только внутри `procedura/` (rubric КАПКАН, grid §9) как сигнал агенту, решение пометки в проекте — за владельцем (см. ВОПРОС про консолидацию в seed §9).
- Остальной проект; durable живёт в арке, не вынесен в дома проекта. Ночной мегапрогон не запускал.
- Бэкапы: до работы `backup_procedura-poiska_20260628_043712.zip`, после сборки `…_postbuild_…zip` (в scratchpad — точка отката, git нет).

**Готово к ревью владельца. Дальше по spec §9: ревью спеки/контракта → при добре — ночной прогон через `orchestrate.sh` → вкусовой проход по `w5_dossier.md`.**
