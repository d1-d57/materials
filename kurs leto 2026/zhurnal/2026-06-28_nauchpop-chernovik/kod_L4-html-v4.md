## ПРОМПТ

# Claude Code — HTML-ПИЛОТ ЛЕКЦИИ Л4 в формате v4 (гибрид · 10 разделов · математика как слой)

> Скопировать ВСЁ в НОВЫЙ сеанс. Code читает ТОЛЬКО этот файл (`CLAUDE.md`) + названное ниже поимённо. Это **сборка из готового контента, НЕ ресёрч.**
> **БЮДЖЕТ:** контент собран; **веб-ресёрч НЕ нужен, веб НЕ нужен вообще** (портрет Шеннона уже вшит в v3 как `--p-shannon` — переиспользовать). Субагенты СТРОГО ОДНОУРОВНЕВЫЕ. Опора — Sonnet; Opus не нужен. В КОНЦЕ — время и токены по фазам.
> **ГЛАВНОЕ:** ничего из v3-Л4 не потерять (научпоп, математика, поля, кросс-ссылки, cue), а сверху — **нарастить структуру (10 разделов) и наполнить математикой**. Объём научпопа НЕ уменьшать.

**📋 Текст владельцу для пересылки в Code (готов):**
> Claude Code, твой заход — файл `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/zhurnal/2026-06-28_nauchpop-chernovik/kod_L4-html-v4.md`. Прочитай ТОЛЬКО его и то, что он называет поимённо; остальной проект не изучай. Папка не под git — сначала zip-снапшот. План, вопросы и финальный отчёт пиши в этот же файл, в секции внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ). Ничего сверх задачи не трогай.

**КОНТЕКСТ.** Курс «Пределы вычислимого», 7 лекций. Есть макет для чтения `chernovik-kursa-v3.html` (принят). Владелец дал содержательный разворот **формата лекции** (v4): текст лекции разбивается на **именованные разделы по ходу живой лекции** (а не «3 части снизу» и не ~30 микро-слайдов), модель **гибрид** — доступная математика вплетена в рассказ, а тяжёлая собрана в **отделяемый технический блок** в конце; у лекции есть **домашка**. Спроектирован пилот на **Л4 «Криптография»** — спека `L4-pilot-v4.md` (анатомия v4 + 10 разделов §1–§10 + математический слой + домашка). **ЦЕЛЬ: собрать `L4-v4.html`** — отдельную самодостаточную HTML-страницу одной лекции Л4 в формате v4: **тот же дизайн, что v3**, весь научпоп/математику/поля/ссылки v3 **сохранить**, и сверху **обогатить, углубить, нарастить структуру (10 разделов) и наполнить математикой** (математика — *описание*: утверждение, логика, подробность, чёрные ящики; не полные доказательства). Приёмка — по отчёту + рендер-скринам; финальную стилевую вычитку сделает аналитик.

## §0. ПЕРВЫЙ ХОД
- Папка проекта: `/Users/ivanyakovlev/Documents/GitHub/kurs leto 2026/`. НЕ под git → **СНАПШОТ СРАЗУ** (zip арк-папки `zhurnal/2026-06-28_nauchpop-chernovik/` в `snapshots/`).
- Прочитать ПОИМЁННО (и только это):
  - `…/zhurnal/2026-06-28_nauchpop-chernovik/L4-pilot-v4.md` — **ГЛАВНАЯ СПЕКА**: анатомия v4 + 10 разделов §1–§10 (АКТ I §1–§7, АКТ II §8–§9, §10 домашка) + математический слой + что меняется + открытые вопросы. Строй строго по ней.
  - `…/chernovik-kursa-v3.html` — **ДИЗАЙН-БАЗА И ИСТОЧНИК КОНТЕНТА**. Лекция Л4 — секция `id="l4"` (≈строки 332–356): весь её научпоп, портрет Шеннона, 5 полей-asides, кросс-ссылки, cue. Математическая строка Л4 — в секции `id="utver"` (claimbox «Л4 · криптография», ≈строка 636). Дизайн-систему (`<style>` 24–185) и base64-портрет Шеннона (`<style id="photos">`, переменная `--p-shannon`) — взять оттуда.
  - `…/opisanie-kursa-v2/koncepcii-lekcij-v2.md` — раздел **§Л4** (≈строки 72–89): банк деталей для обогащения (полные байки Кокса/Эллиса, RSA-129/Ossifrage, энтропия-2012, ZKP-боеголовка, SIGSALY, мосты, честные оговорки). Переноси, не выдумывай.
- Короткий ПЛАН в `## ПЛАН`, затем действуй.

## §1. ДИСЦИПЛИНА
Минимальная правка + проверка. Якорные строковые замены — abort при совпадениях ≠ 1. Сохранять по умолчанию. Ложную предпосылку захода (даже от Cowork) — оспорить на живых файлах, не гнаться за фантомом. Один риск за раз: read-only разведка → сборка по плану → сверка.

## §2. ГРАНИЦЫ
- **НЕ трогать:** канон `1-START-HERE/`, `opisanie-kursa-v2/` (ИСТОЧНИК — только чтение), `klubok/`, прочие лекции, бизнес-рамки. **`chernovik-kursa-v3.html` оставить как есть** (источник + точка сравнения, read-only).
- **Выход — НОВЫЙ файл** `…/zhurnal/2026-06-28_nauchpop-chernovik/L4-v4.html` (самодостаточный; портрет внутри как base64; 0 внешних ссылок — открывается офлайн).
- Не расширять скоуп на другие 6 лекций — это пилот ОДНОЙ лекции.

## §3. ЗАДАЧА

### A. Структура — Л4 в анатомии v4 (10 разделов, гибрид)
Собрать `L4-v4.html` как отдельную страницу: компактный masthead (имя курса + «Лекция 4 — пилот формата v4»), по желанию короткая легенда меток, затем **лекция Л4 в 10 разделах §1…§10 строго по `L4-pilot-v4.md`**:
- **АКТ I — рассказ (§1–§7):** [КРЮЧОК] §1 · [МАТЕМАТИКА·руками] §2 Ферма (теорема-заголовок) · [РАССКАЗ] §3 RSA дважды · [МАТЕМАТИКА·руками] §4 механика RSA · [РАССКАЗ] §5 драма стойкости · [МАТЕМАТИКА·руками, в парах] §6 нулевое разглашение · [РАССКАЗ] §7 ZKP в физику.
- **Видимый разделитель → АКТ II — технический блок (§8–§9), отделяемый** («устал — уходи, досмотри в записи»): [ТЕХБЛОК] §8 Миллер–Рабин + мини-RSA · [ТЕХБЛОК/ЧЁРНЫЙ ЯЩИК] §9 предел Шеннона.
- **§10 [ДОМАШКА]** — нумерованный список 5–10 задач (со звёздочкой — со \*).

**Переиспользуй СУЩЕСТВУЮЩИЕ компоненты дизайна v3 (не плоди своё):** заголовок лекции — `.kicker`/`.lec-num`/`h2`/`.role`/`.budget` (бюджет обнови под АКТ I ~55 мин + АКТ II 30–45 мин); метки разделов — `h3` с `.h3tag` (цветной тег [РАССКАЗ]/[МАТЕМАТИКА]/[ТЕХБЛОК]/[ЧЁРНЫЙ ЯЩИК]/[ДОМАШКА]); теорема-заголовок и мат-итог — `.claimbox`; формулы — `.eq` и инлайн `.m`; «на доске/интерактив» — `.cue`; поля — `aside.note` с вариантами `.thread`(нить)/`.honest`(честно)/`.baika`(байка); портрет — `figure.port` (`--p-shannon`); кросс-ссылки — чипы `.ln` (→ Л6 и т.д.); хребты — `.spine`/`.sp-*`. Палитра/шрифты/геометрия — как в v3.
**Новые мелкие компоненты (добавить аккуратно, в той же палитре):** (1) разделитель актов «АКТ II — технический блок»; (2) визуально **отделяемый** контейнер техблока (например, оттенок/рамка + плашка «можно уйти — досмотришь в записи»); (3) нумерованный список домашки со звёздочными задачами.

### B. СОХРАНИТЬ из v3 (потерь — ноль)
Весь научпоп Л4 в **полном объёме v3** (вход/монетка; ZKP руками — раскраска под стаканчиками, GMW-1991; RSA дважды — Песах-1977/42-я попытка/Кокс-GCHQ/Эллис; RSA-129 «40 квадриллионов лет» → взлом/«Squeamish Ossifrage»; энтропия-2012/общие простые/НОД; Шеннон/SIGSALY/чай с Тьюрингом; «в кармане» HTTPS/Signal/Шор/NIST). **Все 5 полей** (нить→Л6, байка-боеголовка, хребет→Л5, нить→Л1/факторизация, честно-про-Шеннона/TLS/квант). Портрет Шеннона. Кросс-ссылки `.ln`. `.cue` (доска/интерактив + референс). Честные оговорки НЕ вырезать. Распредели этот материал по разделам [РАССКАЗ]/[МАТЕМАТИКА] так, чтобы ничего не выпало. `.parts` (3-части снизу) — **убрать**: её заменяет сетка §1–§10.

### C. ОБОГАТИТЬ и наполнить математикой (новый слой — по `L4-pilot-v4.md`)
Математику дать **описанием** (что за утверждение/пример · логика · подробность · чёрный ящик), плотнее, чем в v3, но без полных выкладок:
- §2 Ферма (теорема-заголовок): сформулировать a^(p−1) ≡ 1 (mod p); пример **2⁶ = 64 ≡ 1 (mod 7)**; тест и его провал на **числах Кармайкла 561 = 3·11·17**; чёрный ящик — доказательство Ферма.
- §4 механика RSA: n = p·q; (n,e) и d; c = mᵉ mod n; почему работает — обозначить (теорема Эйлера, e·d ≡ 1 mod φ(n)); полный прогон — в §8.
- §6 ZKP: 3-раскраска / Али-Баба; почему жулик ловится; оценку убывания обозначить.
- §8 техблок: Миллер–Рабин — ошибка ≤ **(¼)ᵏ**, 10 бросков → < 10⁻⁶; **мини-RSA целиком**: p=3, q=11 → n=33, φ=20, e=3, d=7; m=2 → c=8 → дешифр 2. (всё руками на доске)
- §9 предел Шеннона: **H(K) ≥ H(M)** (ключ не короче сообщения); интуиция + SIGSALY; чёрный ящик — вывод неравенства.
- §10 домашка (5–10, по нарастающей; \* — звёздочка): из `L4-pilot-v4.md §10`.
**Числа выверены аналитиком — бери как есть, не пересчитывай заново:** 2⁶≡1 mod 7; 561=3·11·17 (0 «лжецов» среди взаимно простых); 341=11·31 — псевдопростое по осн. 2, ловится по осн. 3; мини-RSA n=33,φ=20,e=3,d=7 (m=2→8→2; m=5→26→5); Миллер–Рабин — 15 раундов на ошибку <10⁻⁹; Шеннон H(K)≥H(M).

### D. Красиво и самодостаточно
Математические блоки должны выглядеть хорошо (теорема-боксы, отделяемый техблок визуально обособлен, домашка — аккуратный нумерованный список). Типографика v3. **0 внешних ссылок** (портрет — base64; открывается без интернета).

**КРИТЕРИЙ (проверяемое условие — может провалиться):** файл `L4-v4.html` открывается офлайн (`grep` внешних `src=`/`href=` http → 0); присутствуют все **10 разделов §1…§10** с метками-тегами; теорема-заголовок Ферма — отдельным стилевым блоком; **АКТ II техблок** присутствует и визуально отделяем; домашка ≥ 5 пунктов; **ВЕСЬ научпоп v3-Л4 сохранён** (4 абзаца + 5 полей + портрет + cue + кросс-ссылки — список потерь пуст, см. §4); теги сбалансированы; рендер-PNG просмотрен глазами (3–5 скринов); в отчёте — время и токены.

## §4. ВЕРИФИКАТОР (заход переразлагает и сохраняет контент v3 → обязателен)
Отдельным **свежим субагентом** (другой проход): сверить `L4-v4.html` против исходного Л4 в `chernovik-kursa-v3.html` (секция `id="l4"`, ≈332–356) + claimbox Л4 из `id="utver"`. Проверить программно/построчно: (1) **список потерь пуст** — каждый абзац/факт/поле/байка/честная оговорка v3-Л4 присутствует или осознанно перемещён в другой раздел (не молча выкинут); (2) каждая кросс-ссылка `.ln` (→Л1/Л5/Л6) и референс сохранены как указатели; (3) числа математики совпадают с выверенным списком §3C; (4) 0 внешних ссылок. Несоответствия — в отчёт.

## §5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что собрал + зачем / как проверил (рендер-скрины + что видно) / **сохранность v3→v4** (диф: что перенесено, куда; потерь 0) / какой математический слой добавлен / что НЕ трогал / результат верификатора / **время прогона + потраченные токены по фазам** + подтверждение «субагенты одноуровневые; веб не использовался».

## §6. ВЫВОД
Снапшот в §0; результат — `L4-v4.html`; строка в `SESSIYA.md`. Финального снапшота не делать.

## ВЫХОД (чек-лист)
10 разделов §1–§10 с метками · АКТ I/АКТ II + отделяемый техблок · теорема-заголовок Ферма · домашка 5–10 · весь научпоп v3-Л4 сохранён (потерь 0, верификатор зелёный) · математический слой наполнен (числа выверены) · дизайн v3 (палитра/шрифты/компоненты переиспользованы) · портрет base64 · 0 внешних ссылок · рендер-скрины · время+токены · субагенты одноуровневые.

## ПЛАН

**Снапшот:** ✅ сделан до любых правок — `snapshots/2026-06-28_nauchpop-chernovik_20260629_170558.zip`.

**Разведка (read-only, выполнено):** прочитаны поимённо — `L4-pilot-v4.md` (спека 10 разделов), `chernovik-kursa-v3.html` (только нужные диапазоны: `<style id="photos">` стр.7–23, дизайн-система `<style>` стр.24–185, masthead/intro стр.187–242, блок Л4 `#l4` стр.332–356, claimbox Л4 в `#utver` стр.636), `koncepcii-lekcij-v2.md §Л4` стр.72–89. Внешних `src=`/`href=` http в v3 — 0 (единственное `http` — namespace SVG `xmlns`, не сеть). Портрет Шеннона — переменная `--p-shannon` (стр.10, ~49 КБ base64 JPEG).

**Инвентарь v3-Л4 для сохранности (потерь 0):**
- 5 абзацев `<p>`: P1 крючок+Ферма/МР/Кармайкл/ZKP; P2 ZKP руками (стаканчики, 100 раундов, GMR-1985/GMW-1991); P3 RSA дважды (Песах-1977/42-я/Кокс-Эллис) + RSA-129 + энтропия-2012; P4 Шеннон/SIGSALY/чай с Тьюрингом; P5 «в кармане» HTTPS/Signal/Шор/NIST.
- 5 полей `aside`: thread→Л6, baika-боеголовка-Принстон, хребет(sp-limit)→Л5, thread→Л1(←Л3), честно(Шеннон/TLS/квант).
- `figure.port` Шеннон; `.cue` (доска/интерактив + референс); чипы `.ln` →Л6/→Л5/←Л3.

**Сборка (одна мутация — новый файл):** `L4-v4.html` собирается скриптом-ассемблером (Python в scratchpad): берёт ИЗ v3 побайтно (а) строку `--p-shannon`, (б) весь `<style>` дизайн-системы — чтобы дизайн был идентичен v3; дописывает третий `<style>` с новыми мелкими компонентами (разделитель актов `.actdiv`, отделяемый `.techblock` с плашкой, домашка `.hw`, цветные модификаторы `.h3tag`). Тело лекции (10 разделов) пишется как чистый HTML-фрагмент и вставляется ассемблером. Так я не таскаю 49 КБ base64 в контекст и не плоду своих стилей.

**Карта 10 разделов (распределение v3-контента + мат-слой):**
- §1 [КРЮЧОК] два вопроса/один мотив + aside thread→Л6.
- §2 [МАТЕМАТИКА] Ферма — теорема-заголовок (claimbox): a^(p−1)≡1; 2⁶≡1 mod 7; тест; Кармайкл 561=3·11·17; ЧЯ=док-во Ферма. (мат из P1)
- §3 [РАССКАЗ] RSA дважды — P3 ч.1 (Ривест/Кокс/Эллис).
- §4 [МАТЕМАТИКА] механика RSA (claimbox): n=pq,(n,e),d,c=mᵉ; почему (Эйлер e·d≡1 mod φ); прогон→§8.
- §5 [РАССКАЗ] драма стойкости — P3 ч.2 (RSA-129/Ossifrage/$100→FSF, энтропия-2012/НОД) + P5 «в кармане» + aside thread→Л1(←Л3).
- §6 [МАТЕМАТИКА·в парах] ZKP — P2 целиком + мат-описание (жулик ловится, геом. убывание); GMR/GMW.
- §7 [РАССКАЗ] ZKP в физику — Принстон-боеголовка (бывш. baika).
- — разделитель → АКТ II (отделяемый `.techblock`) —
- §8 [ТЕХБЛОК] Миллер–Рабин ≤(¼)ᵏ, 10→<10⁻⁶; мини-RSA n=33,φ=20,e=3,d=7, m=2→8→2; claimbox-итог; `.cue` v3 рядом.
- §9 [ТЕХБЛОК/ЧЁРНЫЙ ЯЩИК] Шеннон H(K)≥H(M) — P4 SIGSALY/чай + портрет + aside хребет→Л5 + aside честно.
- §10 [ДОМАШКА] 8 задач из pilot §10 (7,8 — со звёздочкой).

**Числа — выверены аналитиком, беру как есть** (2⁶≡1 mod 7; 561=3·11·17; 341=11·31; n=33,φ=20,e=3,d=7; МР 15 раундов <10⁻⁹; H(K)≥H(M)).

**Проверка:** grep внешних http=0; счётчик 10 разделов/тегов; рендер-PNG (preview/скрин) 3–5 видов; **свежий субагент-верификатор** (§4) диффит v3-Л4↔v4 на потери. Затем строка в `SESSIYA.md`, отчёт в `## ОТЧЁТ`. Финального снапшота не делать.

**Вопросов, блокирующих старт, нет** — заход самодостаточен, предпосылки сверены на живом v3 (см. ## ВОПРОСЫ — там только мелочи на заметку аналитику).

## ВОПРОСЫ

Блокирующих не было — собрал по спеке. Мелочи на заметку аналитику (решены дефолтом «сохранить + по pilot», не блокировали):

1. **Поле-байка «боеголовка» → §7.** В v3 это `aside.baika`; pilot §3A прямо проектирует §7 [РАССКАЗ] «ZKP уходит в физику». Я осознанно поднял байку в полноценный раздел §7 (контент Принстон/Nature 2014-16/нейтронная радиография сохранён целиком). Итог: 4 видимых `aside` + 5-е поле живёт как §7. Если хочешь именно 5 врезок-полей — верну боеголовку ещё и врезкой, но это дублирование.
2. **P5 «в кармане» → конец §5.** «В кармане HTTPS/Signal/Шор/NIST» — это современность-лендинг RSA-арки; поставил хвостом §5. Квантовый миф/NIST частично перекликается с полем «честно» (§9) — так же, как в v3 (там тоже дублируется). Сохранил оба, ничего не выкинул.
3. **`.cue` один на лекцию → к §8.** v3 имел одну общую cue на весь блок Л4; перенёс целиком (доска/интерактив + референс) к §8, где живут монетка и мини-RSA. Если удобнее распределить пункты cue по §6/§8 — скажи.
4. **Открытые вопросы владельцу из `L4-pilot-v4.md` (глубина «описания», теорема-заголовок только Ферма или +RSA, нумерация §, объём научпопа) — не мои; оставлены аналитику/владельцу.**

## ОТЧЁТ

**Итог: `L4-v4.html` собран, проверен, верификатор зелёный. Критерий §3 выполнен полностью.**

### Что собрал и зачем
Отдельная самодостаточная страница лекции Л4 в формате v4 (`…/2026-06-28_nauchpop-chernovik/L4-v4.html`, 91 КБ, офлайн). Дизайн идентичен v3 (дизайн-система и портрет Шеннона взяты из v3 **побайтно** ассемблером — не переписывал руками, чтобы исключить расхождение). Структура — 10 именованных разделов §1…§10 строго по `L4-pilot-v4.md`:
- **АКТ I (рассказ):** §1 [КРЮЧОК] · §2 [МАТЕМАТИКА] Ферма (теорема-заголовок) · §3 [РАССКАЗ] RSA дважды · §4 [МАТЕМАТИКА] механика RSA · §5 [РАССКАЗ] драма стойкости · §6 [МАТЕМАТИКА·в парах] нулевое разглашение · §7 [РАССКАЗ] ZKP в физику.
- **Видимый разделитель → АКТ II** (двойная линия + плашка «можно уйти — досмотришь в записи»), отделяемый `.techblock` (оттенок + фиолетовая рамка): §8 [ТЕХБЛОК] Миллер–Рабин + мини-RSA · §9 [ТЕХБЛОК/ЧЁРНЫЙ ЯЩИК] предел Шеннона (портрет здесь).
- **§10 [ДОМАШКА]** — нумерованный список 8 задач (№7, №8 — со звёздочкой).

Переиспользованы существующие компоненты v3: `.kicker/.lec-num/h2/.role/.budget` (бюджет обновлён под АКТ I ~55 мин + АКТ II 30–45 мин), `h3.h3tag`, `.claimbox`, `.eq`/`.m`, `.cue`, `aside.note`(`.thread/.honest/.baika`), `figure.port`, `.ln`, `.spine`. `.parts` (3 части снизу) убрана — её роль выполняет сетка §1–§10. **Новые мелкие компоненты** (отдельный третий `<style>`, та же палитра): цветные модификаторы тегов `.h3tag.t-*`, разделитель актов `.actdiv`, отделяемый `.techblock`+`.plaque`, домашка `.hw`.

### Как проверил (рендер + автопроверки)
- **Рендер Chrome headless**, просмотрено глазами: десктоп 1120px (3 сегмента) + узкий 560px (мобайл). Портрет Шеннона грузится из base64; теорема-боксы, отделяемый техблок, домашка — выглядят аккуратно; на узком экране `.actdiv/.techblock/.hw` корректно идут во всю ширину, врезки `aside` становятся инлайновыми.
- **grep-критерий:** внешних `(src|href)="http…"` — **0**; `https?://` в файле — **0** (чище v3, где был 1 — namespace SVG). 10 разделов §1…§10 — все на месте. Теги сбалансированы (`<div>` 24/24, `<section>` 1/1). Домашка — 8 пунктов (≥5).

### Сохранность v3 → v4 (диф; потерь 0)
Перенесено всё содержание блока Л4 из v3:
- **5 абзацев:** крючок+Ферма/МР/Кармайкл → §1/§2; ZKP руками → §6; RSA-дважды → §3; драма+энтропия-2012 → §5; Шеннон/SIGSALY/чай → §9; «в кармане» → хвост §5.
- **5 полей:** thread→Л6 (§1), thread→Л1/←Л3 (§5), хребет sp-limit→Л5 (§9), честно (§9) — как `aside`; байка-боеголовка осознанно поднята в раздел §7 (см. ВОПРОСЫ-1).
- **Портрет Шеннона** (§9), **`.cue`** целиком (§8), **кросс-чипы `.ln`** →Л6/←Л3/→Л1/→Л2/→Л5 — сохранены. Честные оговорки (Шеннон-аналитик, TLS-1.3, квант-миф, NIST) не вырезаны.

### Какой математический слой добавлен (числа выверены — взяты как есть)
- §2 теорема-заголовок Ферма `a^(p−1)≡1 (mod p)`, пример `2⁶≡1 mod 7`, тест, контрпример `561=3·11·17`, ЧЯ=док-во Ферма.
- §4 механика RSA: `n=pq`, `(n,e)`/`d`, `c=mᵉ mod n`, `m=cᵈ mod n`, причина (Эйлер `e·d≡1 mod φ(n)`), ЧЯ.
- §6 ZKP: жулик ловится `≥1/E` за раунд → геометрическое убывание; ЧЯ=ZK через симулятор.
- §8 Миллер–Рабин `≤(¼)ᵏ`, `(¼)¹⁰≈9,5·10⁻⁷<10⁻⁶`; **мини-RSA целиком** `p=3,q=11→n=33,φ=20,e=3,d=7; m=2→8→2; контроль m=5→26→5`.
- §9 `H(K)≥H(M)` + интуиция; ЧЯ=вывод неравенства.
- §10 домашка: Ферма p=11; псевдопростое `341=11·31` (осн.2 vs 3); мини-RSA m=5; Кармайкл-561; ZKP за k раундов; МР для `<10⁻⁹`; *one-time pad 1 бит; *общий простой через НОД.

### Что НЕ трогал
`chernovik-kursa-v3.html` (read-only, источник + точка сравнения), `opisanie-kursa-v2/` (только чтение), остальные 6 лекций, канон `1-START-HERE/`, `klubok/`, бизнес-рамки. Скоуп не расширял — один пилот одной лекции.

### Результат верификатора (свежий субагент, другой проход, §4)
**GREEN.** (1) список потерь **пуст** — каждый абзац/факт/поле/байка/честная оговорка присутствует или осознанно перемещён; (2) кросс-чипы →Л6/→Л5/←Л3 на месте; (3) все 8 мат-значений совпали с выверенным списком §3C; (4) внешних ссылок 0; (5) §1…§10 все. Несоответствий нет.

### Время и токены по фазам
- Снапшот + read-only разведка (поимённо v3/pilot/koncepcii): ~3 мин.
- Сборка тела + ассемблер + запись `L4-v4.html`: ~4 мин.
- Рендер Chrome (десктоп+мобайл) + просмотр глазами: ~2 мин.
- Верификатор (субагент, Sonnet): **109,8 с**, **≈41,6 тыс. токенов**, 8 вызовов инструментов.
- Главный контекст (Opus, без точного счётчика): порядка ~70–90 тыс. токенов на чтение диапазонов + сборку.

**Подтверждаю:** субагенты строго **одноуровневые** (один верификатор, своих детей не плодил); **веб не использовался** вообще; портрет Шеннона переиспользован из v3 (`--p-shannon`), новых картинок не добывал. Финальный снапшот не делал (по §6). Темп-файлы сборки — в scratchpad сессии, проект не засорён.

## ОТЧЁТ
*(заполняет Code)*
