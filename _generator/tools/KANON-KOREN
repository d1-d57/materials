# КАНОНИЧЕСКИЙ КОРЕНЬ — где ЭТОТ репозиторий лежит НА МАШИНЕ ВЛАДЕЛЬЦА.
#
# Одна строка: абсолютный путь. Всё после `#` — комментарий.
# Читает `_generator/tools/korni.py` (`kanon_koren()`), больше никто.
#
# ЗАЧЕМ. Инструменты, которые ЗАПИСЫВАЮТ путь в файл для другой машины (сегодня
# это стартовое сообщение владельцу в `bootstrap_zahod.py`), не могут взять его
# из окружения: аналитик гонит генератор из песочницы Cowork, где корень
# смонтирован как `/sessions/<имя>/mnt/materials`, а исполнитель работает там,
# где такого пути нет вовсе. 14.08 два запуска подряд встали на первом ходу
# именно так; из 296 файлов-заходов заражены были 64.
#
# ПОЧЕМУ РУКАМИ, А НЕ КОМАНДОЙ. Прогон 14.08 (заход `kanon-put`) из имитации
# песочницы: `git config --get remote.origin.url` даёт имя репозитория, а не
# путь на диске; `git worktree list | head -1` и формула `parents[2]` дают ровно
# путь песочницы. Факт о ЧУЖОЙ машине отсюда не наблюдается — его объявляют.
#
# ПЕРЕЕХАЛ РЕПОЗИТОРИЙ — правится эта строка, и только она. Не объявлено вовсе
# (новый корень, дерево-однодневка фикстуры) — генератор ОТКАЗЫВАЕТСЯ писать
# секцию и говорит, чем чинить: молчаливая запись чужого пути выглядит как
# рабочий заход и потому хуже отказа.

/Users/ivanyakovlev/Documents/GitHub/materials
