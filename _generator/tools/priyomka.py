#!/usr/bin/env python3
"""priyomka.py — тонкий диспетчер: ОДНА дверь, а не вторая копия.

ОТКЛЮЧЕНО 2026-08-21, заход `otkljuchit-dubli`. Сверка копий (`disciplina`
`_generator/tools/sverka_kopij.py`) назвала этот файл разошедшимся с живым
оригиналом в `disciplina/_generator/tools/priyomka.py` молча: в `disciplina`
он переписан (добавлен `gate_g16`), а в `materials` осталась прежняя
редакция. Проверено перед отключением: строк, уникальных для редакции
`materials` (по множеству `def`/`class`), — 0; всё, что здесь было, есть и
там, только в переписанном виде. Ведущая сторона — `disciplina`. Настоящий
инструмент теперь живёт по адресу, вычисленному ниже (сосед `materials` по
имени `disciplina`), путь остаётся рабочим — вызывающие 485 документов не
меняются.

Путь до полной двери вычисляется ОТ ЭТОГО ФАЙЛА (`__file__`), а не хардкод-
литералом: репозитории `materials` и `disciplina` лежат рядом друг с другом
у ЭТОГО владельца, но это не гарантия для всех — вычисление по имени соседа
переживёт переезд там, где захардкоженная абсолютная строка умрёт молча.

🔴 ЭТОТ ФАЙЛ — И CLI, И БИБЛИОТЕКА, И ОБРАЗЕЦ (`tools/git_zona.py`, только
`if __name__ == "__main__":`) ЭТОГО НЕ ПОКРЫВАЕТ. `check_faza_priyomki.py`
делает `import priyomka` и читает `priyomka.REPO`, `priyomka.gate_g13/14/15`
— живой прод-хук `.githooks/pre-commit`, а не тест. Наивный диспетчер под
`__main__`-гвардом ничего не определяет при обычном импорте: `AttributeError`
на КАЖДОМ коммите в `materials`. Найдено живым прогоном при попытке
закоммитить эту же зону — не гипотеза, а факт с первого прогона.

Приём: `runpy.run_path(ДВЕРЬ, run_name=__name__)` БЕЗ `if __name__ ==
"__main__":`-гварда вокруг вызова. `__name__` этого файла Python выставляет
сам — `"__main__"`, когда файл ЗАПУЩЕН (`python3 priyomka.py …`), и
`"priyomka"`, когда он ИМПОРТИРОВАН (`import priyomka`). Передавая его
дальше дверью, получаем: при запуске дверь получает `__name__ == "__main__"`
и её собственный `if __name__ == "__main__": sys.exit(main())` срабатывает
как обычно — то же поведение, что дал бы `python3 <дверь>` напрямую, с тем
же кодом возврата. При импорте дверь получает `__name__ == "priyomka"`, её
`__main__`-блок НЕ срабатывает (не запускает `main()`, не делает `sys.exit`),
а все её имена — `REPO`, `gate_g13`, `gate_g14`, `gate_g15`, `main` и
остальные — достаются в `_ns` и переносятся в этот модуль: `import priyomka;
priyomka.REPO` продолжает работать, потому что после этой строки `priyomka`
(этот файл) обладает тем же `REPO`, что и дверь.

Дверь не найдена — ВНЯТНЫЙ отказ, а не `ModuleNotFoundError` без объяснений:
при запуске — печать и `sys.exit(1)`; при импорте — то же сообщение в stderr
и `ModuleNotFoundError` с вычисленным путём, чтобы читающий трейсбек видел
ПРИЧИНУ, а не голое имя модуля.
"""

import runpy
import sys
from pathlib import Path

КОРЕНЬ_MATERIALS = Path(__file__).resolve().parents[2]
ДВЕРЬ = КОРЕНЬ_MATERIALS.parent / "disciplina" / "_generator" / "tools" / "priyomka.py"

if not ДВЕРЬ.is_file():
    print(f"⛔ Полная дверь не найдена по вычисленному пути: {ДВЕРЬ}", file=sys.stderr)
    print("   Ожидается репозиторий `disciplina` рядом с `materials` "
          f"(сосед {КОРЕНЬ_MATERIALS.parent}). Если разложено иначе — "
          "поправь вычисление ДВЕРЬ в этом файле.", file=sys.stderr)
    if __name__ == "__main__":
        sys.exit(1)
    raise ModuleNotFoundError(f"дверь диспетчера priyomka.py не найдена: {ДВЕРЬ}")

globals().update(runpy.run_path(str(ДВЕРЬ), run_name=__name__))
