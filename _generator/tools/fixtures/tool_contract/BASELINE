# BASELINE контракта инструмента — инструменты, жившие ДО гейта.
#
# ЧТО ЭТА СТРОКА ДАЁТ. Только одно послабление: с инструмента не требуют
# спутник-фикстуру («кривой вход отвергается»). Все остальные проверки —
# переносимость, `--no-optional-locks`, shell владельцу, запрет записи из
# песочницы — судят его в ПОЛНУЮ силу.
#
# ЗАЧЕМ ОНА ВООБЩЕ ЕСТЬ. Без неё первая же правка любого из этих инструментов
# останавливала бы здоровый коммит требованием фикстуры, которую правящему в
# тот момент негде взять. Гейт, бьющий по своим, отключат — и это будет конец
# затеи (Р31). Список делает долг ВИДИМЫМ вместо того, чтобы прятать его в
# смягчённой проверке.
#
# КАК ЭТОТ ФАЙЛ ТАЕТ. Завёл инструменту фикстуру — объяви охват строкой
# `# TOOL-CONTRACT-COVERS: <имя>.py` в её PROGNAT.sh и убери имя отсюда.
# НОВЫЙ инструмент сюда не дописывают: у него фикстура или пометка
# `# TOOL-CONTRACT: no-input` — в этом весь смысл гейта.
#
# Дата данных: 2026-07-28. Покрыты фикстурами и здесь НЕ значатся:
# git_zona.py, bootstrap_arka.py, bootstrap_zahod.py, bootstrap_lekcia.py
# (fixtures/git_zona), check_uroki.py (fixtures/uroki),
# check_tool_contract.py (fixtures/tool_contract).

check_kartoteka.py
check_kurs.py
check_view.py
sostoyanie.py
sostoyanie_lekcii.py
