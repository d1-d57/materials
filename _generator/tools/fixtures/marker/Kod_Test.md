# Просто текст, не заход по содержимому

Фикстура Ш1 (заход `kod_ohvat.md`): судится по НОВОЙ форме имени —
регистронезависимость (`Kod_Test.md` с заглавных, не `kod_*.md`).
Содержимое нейтральное, маркера нет — обязана покраснеть.
