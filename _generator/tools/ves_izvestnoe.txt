# ИЗВЕСТНОЕ ТЯЖЁЛОЕ ВНЕ GIT — список для check_ves.py
#
# Формат: путь от корня репо, после `#` — ПРИЧИНА. Причина обязательна:
# строка без неё через месяц читается как «кто-то зачем-то разрешил».
#
# Попасть сюда — осознанное решение «пусть лежит», а не способ погасить
# красноту. Гейт краснеет ровно на том, чего тут нет; вписывая строку, ты
# берёшь на себя, что файл нужен. Книги и статьи сюда НЕ пишутся — источники
# (`istochniki/`, `biblioteka/`) законны по определению и учтены реестром
# `_studio/zhurnal/_INFRA-git/REESTR-istochnikov.md`.
#
# Заведён 2026-08-01 заходом `kod_chistka-diska.md` — по факту прогона на
# папке ПОСЛЕ уборки трёх куч (2,9 ГБ модель Whisper + 224 МБ wav + 337 МБ
# tar-снапшотов).
#
# СПИСОК ПУСТ, и это не «его забыли наполнить» — это здоровое состояние.
# Четыре строки, стоявшие тут в первой редакции, прожили один день: владелец
# 01.08 разобрал каждую и решил удалить все четыре. Что ушло тем же днём:
#   · teorkat-vvedenie/repeticia/audio/* — 8 файлов, 60 МБ, оригиналы записей
#     репетиции. Индекс 0, история 0 → удаление ОКОНЧАТЕЛЬНОЕ. Владелец:
#     «это аудиозапись, которую я разобрал, полностью расшифровал, её можно
#     удалить» — расшифровки лежат текстом в teorkat-vvedenie/repeticia/.
#   · kurs leto 2026/snapshots/ — 23 файла, 56 МБ, zip-снапшоты дерева. Индекс 0,
#     история 2 у КАЖДОГО файла (проверено пофайлово) → обратимо через
#     `git --no-optional-locks show <коммит>:<путь>`.
#   · _studio.zip — 12 МБ в корне репо. Индекс 0, история 0 → ОКОНЧАТЕЛЬНО.
#     Владелец: «это просто я пересылал в студию».
#
# Пустой список — это утверждение: тяжёлых файлов вне git, которые мы согласились
# терпеть, сейчас нет вообще. Появится строка — значит кто-то принял решение
# «пусть лежит», и оно записано здесь с причиной, а не растворилось в папке.
