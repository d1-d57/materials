# ИЗВЕСТНОЕ ТЯЖЁЛОЕ ВНЕ GIT — список для check_ves.py
#
# Формат: путь от корня репо, после `#` — ПРИЧИНА. Причина обязательна:
# строка без неё через месяц читается как «кто-то зачем-то разрешил».
#
# Попасть сюда — осознанное решение «пусть лежит», а не способ погасить
# красноту. Гейт краснеет ровно на том, чего тут нет; вписывая строку, ты
# берёшь на себя, что файл нужен. Книги и статьи сюда НЕ пишутся — источники
# (`istochniki/`, `biblioteka/`) законны по определению и учтены реестром
# `_studio/zhurnal/_INFRA-git/REESTR-istochnikov.md`.
#
# Заведён 2026-08-01 заходом `kod_chistka-diska.md` — по факту прогона на
# папке ПОСЛЕ уборки трёх куч (2,9 ГБ модель Whisper + 224 МБ wav + 337 МБ
# tar-снапшотов). Всё, что ниже, — остаток того же класса: сложено «на время»
# и пережило свой повод. Каждая строка — кандидат следующего круга, а не норма.

teorkat-vvedenie/repeticia/audio/akt-1.m4a   # исходник записи репетиции, 13 МБ. Расшифровки готовы (akt-1-syroj.md рядом), но m4a — единственный оригинал: wav из .rabota были производными и удалены 01.08. Заход их не называл, решение владельца нужно отдельно
teorkat-vvedenie/repeticia/audio/akt-2.m4a   # то же, 20 МБ. Вся папка audio/ — 60 МБ за 6 актов
kurs leto 2026/snapshots/pre-0koncept-move_2026-07-02.zip   # zip-снапшот дерева, 25 МБ, вся папка 56 МБ. Тот же класс, что удалённые catalan/snapshots: решение 21.07 «git САМ и есть механизм снимков» на этот проект не распространили. В истории git есть (2 коммита) — удаление было бы обратимым
_studio.zip   # 12 МБ, архив папки _studio в корне репо. Происхождение не установлено, дата не проверялась; вне индекса и вне истории — удаление было бы ОКОНЧАТЕЛЬНЫМ, поэтому не тронут
