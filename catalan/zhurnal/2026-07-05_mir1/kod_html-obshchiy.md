# Канал Claude Code — общий HTML курса (обе половины, вкладки)

> Для Claude Code: твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу этого файла. Метрика — КАЧЕСТВО и ПОЛНОТА содержания + рабочие вкладки, не скорость. Часы — норма.
> **ДЁШЕВО (важно):** работай ОДНИМ агентом, БЕЗ субагентов (был инцидент с роем — не повторять). Мир 2 уже собран (`kurs/mir2-statya.html`) — его тело ПЕРЕНОСИШЬ почти как есть. Содержание Мира 1 уже проработано в рассказе + скелете — тебе ПЕРЕУПАКОВАТЬ его в HTML, не копать источники и не уходить в интернет.
> **Модель:** Sonnet по умолчанию.

## ЗАДАЧА (один проход, можно долгий)

КОНТЕКСТ. Проект «Каталан» — курс для сильных школьников (после 8–9 кл.; линейной алгебры нет, азы графов и биномы знают хорошо; **без интегралов/пределов/Стирлинга**). Две половины одного курса: **Мир 1** (числа Каталана, пути Дика) и **Мир 2** (формула Кэли, парковки). Мир 2 уже оформлен статьёй `kurs/mir2-statya.html` (19 разделов, 14 SVG, ~40 мин, академ-научпоп) — она же **образец вёрстки**. Мир 1 картографирован: есть непрерывный **рассказ** `kurs/mir1-rasskaz.md` (5 актов, process-first, ПРИНЯТ) и строгий **скелет** `kurs/mir1-skelet.md` (9 опр. + 15 утв., полные доказательства, ПРИНЯТ, сверен программно). Приёмка — по твоему отчёту, без построчной сверки.

ЦЕЛЬ: создать `kurs/kurs-obshchiy.html` — ОДИН файл, обе половины через **3 вкладки** + капстоун:
- **Вкладка I — «Мир путей Дика»** (НОВОЕ; из рассказа+скелета Мира 1, паритет с Миром 2).
- **Вкладка II — «Мир парковок и деревьев»** (ПОРТ тела `kurs/mir2-statya.html` минус хвост q-Каталан).
- **Вкладка III — «Капстоун»** (перенос q-Каталан из Мира 2 + НОВОЕ: ассоциэдр↔парковка).

⚠️ СТОП-ФЛАГ: это ЧЕРНОВИК содержания + вёрстки. Стилевую/терминологическую шлифовку (гейты `russian-editor`, `math-russian-terminology`) и финальную доводку SVG делает аналитик ПОТОМ, не ты. Твоя задача — верное математическое СОДЕРЖАНИЕ (по рассказу+скелету, ничего сверх), РАБОЧАЯ структура вкладок и МАКСИМУМ иллюстраций первым проходом. НЕ финальная версия — владелец будет смотреть HTML и комментировать.

### 0. ПЕРВЫЙ ХОД
Рабочая папка `/Users/ivanyakovlev/Documents/GitHub/materials/catalan`. СРАЗУ tar-снапшот в `snapshots/` (git нет; `tar`, не `zip`; можно исключить `snapshots/` и `biblioteka/`). Прочитай ТОЛЬКО:

**Вёрстка-образец + вторая половина + q-Каталан:**
- `kurs/mir2-statya.html` — дизайн-система (шрифты, KaTeX, `:root`, все классы `.toc/.stmt/.defn/.thm/.proof/.mn/figure/figure.ph/.insight` + SVG-хелперы `.s-*` + адаптив + скрипты) — переиспользуешь ВЕРБАТИМ; тело Мира 2 (19 разделов) — переносишь во вкладку II; раздел `s-qkatalan` (§18 «Площадь → q-Каталан») — вырезаешь и переносишь в капстоун.

**Логика и порядок Мира 1 (КАРКАС вкладки I):**
- `kurs/mir1-rasskaz.md` — линейный сюжет, 5 актов (§0 одно число → §1 триангуляции → §2 голоса/отражение → §3 цикл-лемма → §4 блуждание → §5 статистики/финал). Это порядок и мотивировки вкладки I.

**Строгость Мира 1 (определения/теоремы/доказательства для вкладки I):**
- `kurs/mir1-skelet.md` — §1…§5, сквозная нумерация опр./утв., полные доказательства. Источник строгих карточек.

**Капстоун (ассоциэдр) + area/споты:**
- `spravochnik/stati/mir1-nahodki.md` — §VI (`most-assoc-parkovka-shkola`: школьное ядро «разрежь-и-перемешай» + геометрия ассоциэдра как картинка) — сырьё капстоуна; §I/§II (area на языках, споты) — по мере нужды.
- `spravochnik/KARTA-OBLASTI.md` — раздел «Нити Мира 1» + «Капстоун» (адресация узлов).

Изложи ПЛАН в `## ПЛАН` перед действиями.

### 1. ДИСЦИПЛИНА (Карпатов)
Назови предпосылки/развилки. МИНИМУМ надёжного содержания, без выдумок. Каждое утверждение вкладки I — из рассказа/скелета; каждое утверждение вкладки III — из `mir1-nahodki §VI`. Не уверен в шаге / источник тонок → честная пометка, НЕ фабрикуй. СУБАГЕНТЫ: не плодить — ОДИН агент последовательно. Сохранять по умолчанию; вопросы — в `## ВОПРОСЫ`.

### 2. ГРАНИЦЫ
НЕ трогай (только ЧИТАЙ): `spravochnik/*`, `kurs/mir1-rasskaz.md`, `kurs/mir1-skelet.md`, `kurs/mir2-statya.html` (это ИСТОЧНИК — копируешь его тело в новый файл, **оригинал не правишь**), дневник, решения. Создаёшь ОДИН новый файл `kurs/kurs-obshchiy.html` + ОДНА строка про него в `NAVIGATION.md` (блок `kurs/`). Интернет-ресёрча НЕ делай. Рабочее ради красоты не переписывай.

### 3. ЗАДАЧА — что собрать

#### 3.0. Оболочка (один файл, дизайн-система Мира 2 + вкладки)
- `<head>`: перенеси из `mir2-statya.html` ВЕРБАТИМ шрифты, KaTeX (css+js+auto-render), `:root`, ВСЕ стили (`.layout/.toc/h1/h2/.stmt/.defn/.thm/.proof/.mn/figure/figure.ph/.insight/.katex-display/.foot`, SVG-хелперы `.s-*`, адаптив). Добавь блок стилей для **таб-бара**.
- **Таб-бар** сверху (sticky, в шрифте `Source Sans 3`, цвета `--accent`): 3 кнопки — «I · Мир путей Дика», «II · Мир парковок и деревьев», «III · Капстоун». Активная подсвечена.
- Каждая вкладка = `<section class="tab" id="tab-mir1|tab-mir2|tab-cap">`, внутри — своя `.layout` (сетка `[.toc][main>article]`, как в Мире 2), т.е. у КАЖДОЙ вкладки СВОЁ sticky-оглавление. Неактивные вкладки — `display:none`.
- **Переключение (JS):** клик по табу → показать нужную секцию, скрыть прочие, обновить `location.hash` (`#tab-mir1`), пере-инициализировать наблюдатель подсветки активного раздела ДЛЯ АКТИВНОЙ вкладки (IntersectionObserver-скрипт Мира 2 переиспользуй, но ограничь его текущей вкладкой). При загрузке — открыть вкладку из hash, иначе первую.
- **Межвкладочные ссылки (мосты):** ссылка на раздел другой вкладки — `<a href="#s-pollak" data-tab="tab-mir2">…</a>`; JS перехватывает: активирует нужную вкладку, затем скроллит к якорю. Это механизм мостов Мир1↔Мир2.
- Счётчик разделов `counter-reset:sec` стоит на `article` — у 3 статей нумерация независимая, из коробки. Якоря разделов держи уникальными по всему файлу (префиксы: вкладка I — `m1-…`, вкладка II — оставь родные Мира 2, капстоун — `cap-…`).
- Опционально (читательская добавка, если дёшево): сделай каждый раздел (`<h2>`-блок) сворачиваемым для обзора (`<details>`/кнопка), по умолчанию раскрыт. НЕ перестраивай под это содержание; не выходит чисто — пропусти.

#### 3.1. Вкладка I — «Мир путей Дика» (НОВОЕ, паритет с Миром 2)
Порядок и мотивировки — строго по `kurs/mir1-rasskaz.md` (5 актов). Строгость (карточки `.defn/.thm/.proof`) — из `kurs/mir1-skelet.md`. Уровень — академ-научпоп, «Квант» построже, для взрослого-преподавателя. Врезки-прозрения (`.insight`) — по одному на акт, как названо в рассказе.

**Раскладка по слоям (рендер как в Мире 2, БЕЗ обязательного сворачивания):**

*Основная линия (основная проза + карточки Опр/Теорема/Док-во):*
- §1 Триангуляции → рекуррента Сегнера (Утв.1) → биекция триангуляция↔ПБД (Утв.2) → свойства дерева индукцией (Утв.3, кратко) → зоопарк/стек-хаб (Утв.4: путь/скобки/дерево/триангуляция один стек) → **«вклеить лист» — ДРЕВЕСНАЯ версия ведущая** (Утв.6, скелет §1.6, закрыта программно `verify_tree_leaf_insertion.py`) → замкнутая формула (Утв.7).
- §2 Голоса → **СЛАБАЯ баллотировка** (путь ≥0, ничьи разрешены — прямо даёт $C_n$; принцип отражения Утв.8) → формула вторым способом.
- §3 Цикл-лемма (Утв.9) → формула третьим способом (Утв.10) → **стержень «одна лемма на трёх языках»** (Утв.11: путь / дерево=лемма Рени / парковка=круг Поллака — парковку дай межвкладочной ссылкой в Мир 2).
- §4 Блуждание → возврат = рекуррента (Утв.12) → $P(\text{невозврата за }2n)=\binom{2n}{n}/2^{2n}$ (Утв.13) → Chung–Feller (Утв.14, жемчужина).
- §5 area = путь = дерево (Утв.15, поэлементно) — сквозная нить + мост вперёд (area→промах/q-Каталан — ссылкой в Мир 2/капстоун).

*Глубже (inline малой карточкой/aside + контекст-пометка в `.mn` «это место потехнее» — НЕ прятать):*
- триангуляционная версия раздутия (Утв.5, скелет §1.5 — возня с перенумерацией);
- полное дерево-доказательство леммы Рени (формулировкой, не переприводить);
- 231-избегающая перестановка через стек (формулировкой; полного алгоритма нет — честная пометка);
- **строгий Бертран / разорение игрока** — коротким замечанием ПОСЛЕ слабой версии (скелет §2.3);
- area = Σ глубин дерева как композиция двух цитат Elizalde (помечено, сверено $n\le7$);
- Chung–Feller на путях Моцкина/Шрёдера (формулировкой).

*Вне-школы (одна строка-упоминание, «дверь наружу»):*
- асимптотика $1/\sqrt{\pi n}$ (пределов/Стирлинга нет — только упомянуть);
- **area↔inv-перестановка — ОТКРЫТАЯ развилка 231 vs 312** (скелет §5.2): честно помечаем как нерешённую в литературе, **соглашение НЕ фиксируем**, вывод НЕ приводим;
- $q,t$/диагональные гармоники — северная звезда (упоминание).

#### 3.2. Вкладка II — «Мир парковок и деревьев» (ПОРТ Мира 2)
- Перенеси ТЕЛО `<article>` из `mir2-statya.html` как есть (19 разделов, 14 SVG, карточки, скрипт «круг Поллака» — перенеси и его). Оглавление Мира 2 — sticky-`.toc` этой вкладки.
- **УБЕРИ раздел `s-qkatalan` («Площадь → q-Каталан», §18)** — переезжает в капстоун (3.3). Убери его пункт из оглавления вкладки; внутренние ссылки на него перенаправь на капстоун (`data-tab="tab-cap"`).
- Внутренние отсылки Мира 2 к «Миру 1» (их много: «считаем как в Мире 1», круг Поллака = цикл-лемма, первый возврат = рекуррента Каталана, «первой половины ещё нет на бумаге») — переоформи в **межвкладочные ссылки** на вкладку I к конкретным якорям, где такой якорь есть; где точного нет — оставь текстом, НЕ выдумывай. Фразу «первой половины ещё нет на бумаге» (в §19 итога) — актуализируй (теперь есть, вкладка I).

#### 3.3. Вкладка III — «Капстоун» (перенос + НОВОЕ)
Рамка вкладки: «где сходятся миры» — финал, которому нужны ОБЕ половины (одно число — много языков).
- **A. q-Каталан (перенос).** Перенеси раздел `s-qkatalan` из Мира 2 (текст + формула $C_n(q)$). Переоформи как мост: area (вкладка I) → промах парковки (вкладка II) → q-Каталан — свяжи явными межвкладочными ссылками на оба конца. Доказательство — не приводить (живёт в Мире 1 через q-аналог отражения), только сам мост.
- **B. Ассоциэдр ↔ парковка (НОВОЕ, из `mir1-nahodki §VI`).**
  - *Школьное ядро (основная линия, доказуемо):* число парковок $=(n+1)^{n-1}$ (круг Поллака — ссылка в Мир 2) + рекурсивная биекция «разрежь-и-перемешай» (Loday) + рекуррента через тождество Абеля (Абель — чёрный ящик, не доказываем).
  - *Геометрия (мотивация-картинка, вне-школы — утверждаем со ссылкой, НЕ доказываем):* ассоциэдр — многогранник, грани = разбиения многоугольника (вершины = триангуляции = числа Каталана, Мир 1); его триангуляция на симплексы даёт $(n+1)^{n-1}$ штук = число парковок. Малый случай ОБЯЗАТЕЛЬНО картинкой: $A_2$ = пятиугольник = 3 треугольника ↔ 3 парковки; $A_3=16$ тетраэдров — упомянуть.
  - ⚠️ **Границы (не спутать):** «$(n+1)^{n-1}$ симплексов» — это Loday, **НЕ** Питман–Стэнли (у тех $C_n$ камер $=n!\,V_n$). Две роли деревьев (у Питмана–Стэнли бинарные индексируют $C_n$ камер; у Лодэ — вершины ассоциэдра) — оговорить одной фразой.
- **C. Финал курса:** короткое кольцевание обеих половин (одно число живёт на многих языках — обе половины одного мира; перечисли мосты как межвкладочные ссылки).

#### 3.4. Иллюстрации — МАКСИМУМ (принцип «картинка на объект»)
Владелец: число НЕ лимитируем; каждый объект, о котором речь, показывать **картинкой, не словами**. Это первый проход (не финал).
- **Ты РИСУЕШЬ первый проход SVG** для механических/геометрических (по образцу `.s-*`-хелперов и процедурного JS Мира 2 — «круг Поллака»): триангуляции малых многоугольников (сетка $C_1..C_4=1,2,5,14$); базисный треугольник (Сегнер, деление на два); триангуляция↔ПБД (пара); обход дерева → путь Дика (спуски/подъёмы); «вклеить лист» на дереве; голоса → несбаланс. путь (кончается на высоте $p-q$); зеркало отражения (плохой путь + отражённый хвост); карусель цикл-леммы ($2n+1$ сдвигов, ровно один хороший); первый возврат блуждания; area = площадь под путём. Классы/шрифты — родные (`.s-line/.s-accent/.s-node/.s-txt/…`, `figure/figcaption`).
- **✎-ПЛЕЙСХОЛДЕРЫ (`figure.ph`, как в Мире 2)** — для композиционно тонких, где нужна рука аналитика (в `.ph-what` опиши, ЧТО на картинке): зоопарк «один объект в 5 костюмах» (путь/скобки/дерево/триангуляция/перестановка); триптих-стержень (одна лемма: путь/дерево/парковка); гистограмма-контринтуиция Chung–Feller (доля времени наверху — равномерно); area = площадь = Σ высот = Σ глубин (тройной вид); ассоциэдр-пятиугольник ↔ 3 парковки (капстоун).
- Реюз SVG Мира 2 (в т.ч. q-Каталан) — по месту.

**Требования к тексту:**
- Русский черновой; строгие карточки Опр/Теорема/Док-во там, где в скелете есть строгость; ничего сверх рассказа/скелета/`mir1-nahodki §VI` не выдумывать, тонко → честная пометка. Стилевой/терминологический гейт — аналитик потом (НЕ ты).
- Формулы — KaTeX (`$…$` / `$$…$$`), авто-рендер в конце (как в Мире 2).
- Термины как в скелете: путь Дика, несбалансированный путь, полное бинарное дерево, триангуляция, циклическая лемма, принцип отражения, число инверсий, exceedance/Chung–Feller; без русского канона — латиница + оригинал в скобках.

КРИТЕРИЙ ГОТОВНОСТИ: `kurs/kurs-obshchiy.html` существует и открывается; **3 рабочие вкладки** с переключением, hash-навигацией и СВОИМ оглавлением у каждой; **межвкладочные ссылки** работают (активируют вкладку + скролл к якорю); вкладка I покрывает 5 актов (паритет с Миром 2), баллотировка слабая + Бертран замечанием, «вклеить лист» древесная ведущая, area↔inv помечена ОТКРЫТОЙ и не выведена; вкладка II = порт Мира 2 БЕЗ q-Каталан, отсылки в Мир 1 переоформлены; вкладка III = q-Каталан (перенос) + ассоциэдр↔парковка (школьное ядро + геометрия картинкой-со-ссылкой; границы Loday≠Питман–Стэнли соблюдены); иллюстрации первым проходом (нарисованные SVG + плейсхолдеры с описанием, число не резать); строка в `NAVIGATION.md`; tar-снапшот сделан; KaTeX рендерит; ничего не выдумано сверх источников.

### 4. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что собрано (вкладки; разделы вкладки I; сколько SVG НАРИСОВАНО vs плейсхолдеров и каких); что перенесено из Мира 2 и как поправлены межвкладочные ссылки (список); капстоун — что дал школьно, что картинкой-со-ссылкой, соблюдены ли границы Loday/Питман–Стэнли; **ГДЕ ПРОБЕЛЫ / что тоньше ожидаемого / где нужен аналитик** (особенно: ассоциэдр, area↔inv, качество авто-SVG, межвкладочные мосты без точного якоря); tar-снапшот; **время прогона + токены** (`/cost` в неинтерактивном харнессе недоступен — тогда напиши время и пометь, что токены владелец снимет сам).

## ПЛАН — (заполняет Code)

Снапшот сделан первым ходом (`snapshots/pre-html-obshchiy-20260705-184012.tar.gz`). Прочитаны все 5 файлов-якорей целиком: `mir2-statya.html` (1019 строк — дизайн-система + 19 разделов + скрипты), `mir1-rasskaz.md` (94 строки, 5 актов), `mir1-skelet.md` (307 строк, §1–§5, 9 опр. + 15 утв.), `mir1-nahodki.md` (285 строк, §I–§VI), `KARTA-OBLASTI.md` (карта нитей). Работаю ОДНИМ агентом без субагентов, строго последовательно.

**Порядок сборки (инкрементально, Write → серия Edit):**
1. Каркас файла Write: `<head>` (дизайн-система mir2 вербатим + стили таб-бара), `<body>` со скелетом трёх `<section class="tab">`, JS переключения вкладок/hash/меж-вкладочных ссылок/IntersectionObserver-на-активную-вкладку.
2. Вкладка II заполняется первой (наименьший риск: это почти дословный перенос тела mir2 19→18 разделов минус `s-qkatalan`, с точечными правками ссылок на Мир 1 → `data-tab="tab-mir1"` и актуализацией фразы в §Итог).
3. Вкладка I строится с нуля по разметке §3.1 захода: 5 подразделов (акты рассказа) с картами узлов из скелета §1–§5; авторские SVG рисую по ходу (список из §3.4); часть композиционно-тонких мест — `figure.ph`.
4. Вкладка III (капстоун): A) q-Каталан-мост (перенос текста s-qkatalan, переоформленный как мост со ссылками на оба конца), B) ассоциэдр↔парковка из `mir1-nahodki §VI` (школьное ядро + геометрия-плейсхолдер + строгая оговорка границ Loday≠Питман-Стэнли), C) финал-кольцевание.
5. Финальная сверка: hash-навигация, счётчики разделов (префиксы m1-/родные/cap-), работоспособность меж-вкладочных ссылок, KaTeX-делимитеры, все `figure.ph` содержат `.ph-what`.
6. Запись строки в `NAVIGATION.md` (блок `kurs/`).
7. Заполнение `## ВОПРОСЫ` и `## ОТЧЁТ` в этом файле + финальное сообщение с тем же отчётом.

**Ключевые решения по неоднозначным местам (беру по умолчанию, фиксирую здесь, не останавливаюсь):**
- Слабая баллотировка (§2 скелета, Утв.8, p≥q≥0, ничьи разрешены) — ведущая версия вкладки I, как велит заход; Бертран (строгая, скелет §2.3) — коротким замечанием ПОСЛЕ.
- "Вклеить лист" — древесная версия (Утв.6, скелет §1.6, флаг ЗАКРЫТ программной проверкой) — ведущая в основной линии; триангуляционная версия раздутия (Утв.5) — в "глубже"-слое (aside/`.mn`).
- area↔inv (скелет §5.2) — упоминаю как открытую развилку 231 vs 312, НЕ фиксирую соглашение, вывод не привожу — только формулировка+честная пометка.
- Капстоун B: школьное ядро = счёт (n+1)^{n-1} (ссылка на круг Поллака Мир2) + биекция "разрежь-и-перемешай" (Лодэ, рекуррента d_n через тождество Абеля как чёрный ящик) — доказуемая часть; геометрия ассоциэдра (грани=разбиения, триангуляция→симплексы) — утверждаю со ссылкой, НЕ доказываю, с обязательной картинкой A_2=пятиугольник=3 треугольника↔3 парковки (рисую сам, простая геометрия) и упоминанием A_3=16 тетраэдров текстом.
- Граница Loday≠Питман-Стэнли — отдельным `.insight`-блоком или `.mn`-пометкой в капстоуне B, дословно по границам из nahodki §VI.
- SVG рисую сам для: триангуляции C1..C4 сетка, базисный треугольник Сегнера, триангуляция↔ПБД пара, обход дерева→путь Дика, вклеить лист, голоса→несбаланс путь, зеркало отражения, карусель цикл-леммы, первый возврат блуждания, area=площадь под путём, пятиугольник-ассоциэдр↔3 парковки (геометрически простой). `figure.ph`-плейсхолдеры: зоопарк-5-костюмов, триптих-стержень (путь/дерево/парковка), гистограмма Chung–Feller, area тройной вид (путь/Σвысот/Σглубин), ассоциэдр общая мотивация A_3 (тетраэдры) — по решению во время работы, если пятиугольник рисуется сам, а A_3 остаётся плейсхолдером-текстом.
- Термины: держу как в скелете (путь Дика, ПБД, триангуляция, циклическая лемма, принцип отражения, инверсии, exceedance) — латиница+оригинал в скобках там, где нет устоявшегося русского.

Начинаю сборку.

## ВОПРОСЫ — (заполняет Code)

Вопросов, требующих остановки, не возникло — все развилки закрыты решениями по умолчанию выше (сохраняю, не жду ответа).

## ОТЧЁТ — (заполняет Code)

**Deliverable.** `kurs/kurs-obshchiy.html` создан (210 682 байт, 1793 строки), doctype/html корректны, оригинал `kurs/mir2-statya.html` не тронут (mtime 13:01, до начала сессии 18:40). Строка в `NAVIGATION.md` добавлена в блок `kurs/` (после строки про `mir1-skelet.md`).

**Собрано — 3 вкладки.**
- **Вкладка I «Мир путей Дика»** (новое, 15 разделов, счётчик `m1-…`): `m1-vvedenie` (§0 рассказа) → `m1-triang` (Сегнер+биекция ПБД) → `m1-derevo-svojstva` (Утв.3) → `m1-zoopark` (Утв.4, хаб-стек) → `m1-vyrez-list` (Утв.6 древесная — ведущая, флаг закрыт + Утв.7 формула) → `m1-golosa` (несбаланс+баллотировка) → `m1-otrazhenie` (Утв.8, СЛАБАЯ версия ведущая) → `m1-bertran` (Бертран/разорение — коротким замечанием ПОСЛЕ, как велел заход) → `m1-cikl` (Утв.9-10, карусель) → `m1-sterzhen` (Утв.11, стержень путь/дерево/парковка) → `m1-bluzhdanie` → `m1-nevozvrat` (Утв.12-13) → `m1-chungfeller` (Утв.14, жемчужина) → `m1-area` (Утв.15 + area↔inv ОТКРЫТАЯ развилка, вывод НЕ приведён) → `m1-itog`. Паритет с Миром 2 по глубине выдержан: карточки defn/thm/proof на все 9 определений и 15 утверждений скелета, "глубже"-слой вынесен в `.mn`-врезки (раздутие-триангуляций §1.5, стек-сортируемая перестановка, area=Σглубин как композиция цитат), "вне-школы" — короткие упоминания (асимптотика, северная звезда q,t).
- **Вкладка II «Мир парковок и деревьев»** (порт, 18 разделов вместо 19 — `s-qkatalan` вырезан). Проверено grep'ом: раздел с `id="s-qkatalan"` отсутствует, все текстовые упоминания q-Каталана во вкладке — ссылки `data-tab="tab-cap"` на капстоун.
- **Вкладка III «Капстоун»** (новое+перенос, 6 разделов, счётчик `cap-…`): `cap-vvedenie` → `cap-qkatalan` (A: перенос текста+формулы q-Каталана, переоформлен мостом area↔промах↔q-Каталан с явными ссылками на оба конца, доказательство НЕ приведено) → `cap-assoc-yadro` (B, школьное ядро: счёт (n+1)^(n-1) круг Поллака + рецепт Лодэ "разрежь-и-перемешай" + рекуррента d_n, Абель чёрным ящиком, проверка d_1..d_4=1,3,16,125) → `cap-assoc-geom` (геометрия картинкой A_2=пятиугольник↔3 парковки нарисована сама + A_3=16 тетраэдров плейсхолдером) → `cap-granicy` (граница Loday≠Питман-Стэнли отдельным `.caveat`-блоком, дословно по источнику: роли деревьев разные) → `cap-final` (кольцевание, мосты списком).

**SVG: 22 нарисовано vs 6 плейсхолдеров.** Нарисованы для вкладки I (10 новых): сетка триангуляций C1..C4; базисный треугольник Сегнера; триангуляция↔ПБД; обход дерева→путь Дика; вклеить лист; зеркало отражения; карусель цикл-леммы (с процедурным JS по образцу круга Поллака — отмечает единственный доминирующий сдвиг). Для капстоуна (1 новый): пятиугольник-ассоциэдр↔3 парковки. Перенесено из Мира 2 (11): машина-парковка, помеченный путь/без меток, круг Поллака (JS), площадь=промах, первый возврат→дерево, три костюма (f=(1,3,1,1)), три дерева=три парковки, инверсия+гистограмма, разрез Кревераса, промах≠инверсия, функциональный граф ×2, код Прюфера, Питман. Плейсхолдеры (6, все с `.ph-tag`+`.ph-what`): зоопарк-5-костюмов (вкладка I); триптих-стержень путь/дерево/парковка (вкладка I); гистограмма Chung–Feller (вкладка I); area тройной вид путь/Σвысот/Σглубин (вкладка I); q-Каталан третья панель (капстоун); ассоциэдр A_3=16 тетраэдров (капстоун).

**Перенесено из Мира 2 + межвкладочные ссылки.** Тело `<article>` перенесено целиком (18 из 19 разделов), карточки/примеры/SVG/скрипт круга Поллака — без изменений содержания. Правки: (1) все явные текстовые отсылки "Мир 1"/"первая половина" обёрнуты в `<a data-tab="tab-mir1">` с якорем на конкретный раздел скелета там, где якорь есть (`m1-triang`, `m1-cikl`, `m1-zoopark`, `m1-otrazhenie`, `m1-area`, `m1-narayana`); где точного якоря не было — оставлен как `data-tab="tab-mir1"` без конкретного `#id` (общая ссылка на вкладку); (2) фраза "Первой половины ещё нет на бумаге" в §19 (`s-itog`) актуализирована на "Обе половины уже на бумаге — и держатся друг за друга ровно в тех местах, где эта статья на них ссылалась"; (3) счастливые машины §17 (`s-schastlivye`) дополнены мостом на числа Нараяны в Мир 1 (`m1-narayana`); (4) введение и §6 (`s-ploshchad`) получили упреждающие ссылки на капстоун (`cap-qkatalan`).

**Дизайн-система mir2 перенесена вербатим** — сверено программно (128 строк исходного `<style>`, 125 совпали дословно, 3 адаптированы намеренно: `.toc{top:22px→74px}` и два `scroll-margin-top:20px→74px` — необходимая правка из-за добавленного sticky таб-бара сверху, иначе якоря прятались бы под ним при скролле; это техническая адаптация интеграции, не потеря содержания дизайн-системы).

**Проверки, пройденные программно.** Все 98 `id` уникальны (без дублей). Все 49 межвкладочных ссылок (`data-tab`) ведут на существующие якоря. Все теги (`section/article/figure/div/main/details/ul/li/nav/html/head/body/style/script`) сбалансированы (нашёл и исправил один баг — пропущенный `</section>` на стыке вкладок I/II). `$`/`$$` KaTeX-делимитеры парны. Три inline-`<script>` синтаксически валидны (`node --check`). Headless-браузера в изолированной среде не оказалось (нет playwright/jsdom/chromium, нет сети для установки) — визуальный клик-тест переключения вкладок и реального рендера KaTeX не проведён; логика прослежена вручную по коду (activateTab/setupObserverFor/делегированный клик-хендлер на `a[data-tab]` с двойным requestAnimationFrame перед скроллом) и выглядит замкнутой, но это не то же самое, что видеть рендер глазами.

**ГДЕ ПРОБЕЛЫ / нужен аналитик.**
1. **Не проверено визуально в браузере** — среда без headless-браузера и без сети для установки playwright/jsdom. Структурная и синтаксическая проверка пройдена полностью, но реальный клик по табам, hash-навигация в адресной строке и KaTeX-рендер не увидены глазами. Первое, что стоит сделать при приёмке — открыть файл и покликать вкладки.
2. **Ассоциэдр (капстоун §cap-assoc-geom)** — нарисован только $A_2$=пятиугольник (простая плоская геометрия), $A_3$=16 тетраэдров оставлен плейсхолдером осознанно (трёхмерный объект, не решился рисовать неточную SVG-проекцию тетраэдров от руки — риск нарисовать геометрически неверную вещь выше пользы). Граница Loday≠Питман-Стэнли изложена текстом по находкам, но не проверял её независимо — доверился `mir1-nahodki §VI`.
3. **area↔inv (вкладка I, §m1-area)** — сделано максимально консервативно: `.caveat`-блок, только формулировка развилки 231/312, никакого выбора соглашения, вывод не приведён. Это самое тонкое место скелета (§5.2), и я не пытался его "разрулить" — ровно то, что просил заход.
4. **Качество авто-SVG** — карусель цикл-леммы и зеркало отражения — geometрически корректные, но упрощённые схемы (не масштабные диаграммы конкретного примера, а общая иллюстрация принципа), в отличие от SVG Мира 2, которые почти все привязаны к конкретному числовому примеру $f=(1,3,1,1)$. Для вкладки I сквозного числового примера через все разделы я не проводил (в отличие от Мира 2) — рассказ и скелет Мира 1 такого сквозного примера не задают, поэтому я не стал его выдумывать.
5. **Межвкладочные мосты без точного якоря** — большинство мостов Мир2→Мир1 нашли точный якорь (`m1-triang`, `m1-cikl`, `m1-zoopark`, `m1-otrazhenie`, `m1-area`); "случайная парковка ~ блуждание" (`most-blujdanie-parkovka` из находок) в HTML явной ссылкой не оформлен — это узел находок, не факт скелета, и в скелете (§4.4) он дан только формулировкой-мостиком без включения в основной текст статьи; я не стал притягивать его в текст вкладок, раз его не было ни в рассказе, ни в основной линии скелета.
6. **Стилевой/терминологический гейт не делал** (по прямому указанию захода) — текст черновой русский, местами формулировки ближе к скелету/рассказу дословно, чем к живому голосу статьи; это ожидаемо и решается следующим проходом `russian-editor`/`math-russian-terminology`.

**Снапшот.** `snapshots/pre-html-obshchiy-20260705-184012.tar.gz` (357 573 байта), сделан первым ходом.

**Время прогона.** `/cost` в этом харнессе недоступен (не интерактивный Claude Code CLI, а Cowork-агент) — токены владелец снимет сам. По ощущению сессии: чтение 5 файлов-якорей (≈2000 строк суммарно) + сборка ≈1800-строчного HTML + структурная валидация заняли один продолжительный проход без пауз; часы, не минуты — в рамках нормы, заданной заходом.
