# Канал исполнителя — ИЛЛЮСТРАЦИИ в build_doc.py (один заход до конца)

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные ниже якоря; проект НЕ изучай.
> План/вопросы/отчёт — в секции внизу ЭТОГО файла. Метрика — КАЧЕСТВО. Часы — норма.

**КОНТЕКСТ.** `materials/_generator/build_doc.py` — doc-движок фабрики: markdown-источник → один самодостаточный `view.html` для авторской вычитки (модель данных — ПОТОК текста, брат `build_deck.py` со слайд-канвасом; контракт семейства — `_generator/DVIZHKI.md`). На нём собран живой текст `catalan/zhurnal/2026-07-13_dika-v-vysshey-matematike/obyasnenie-tl/src/lekciya-tl-puti-dika.md`. Владелец читает вывод и упёрся: **иллюстраций нет, а без них куски нечитаемы** (принцип отражения, действие `e_i`, замощения Дика).

**ЦЕЛЬ:** научить `build_doc.py` нормально показывать иллюстрации — инлайн-SVG в `<figure>` со стилями, как в РУЧНЫХ эталонах владельца.

**⚠ ФЛАГ — СТОП ДО ЦЕЛИ (читай сейчас, не в конце).** Ты закрываешь ТОЛЬКО движок: после тебя генератор умеет показывать иллюстрации. Ты **НЕ рисуешь сами картинки** для лекции про `TL` — это отдельная содержательная работа аналитика. Не пытайся их сочинить. Твой выход — движок + проба, что механизм работает.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## 0. ПЕРВЫЙ ХОД

- **Рабочая папка:** `/Users/ivanyakovlev/Documents/GitHub/materials`
- **Точка отката:** папка **НЕ под git** → первым действием сделай `tar`/zip-бэкап `_generator/` (единственная точка отката). Скажи в отчёте, куда положил.
- **Прочитать ТОЛЬКО (поимённо, ничего сверх):**
  1. `_generator/build_doc.py` — целиком (≈450 строк), это предмет правки;
  2. `catalan/kurs/kurs-lekcii.html` — **ЭТАЛОН, но НЕ читай целиком (220 КБ!)**: вытащи программно только (а) содержимое `<style>` и (б) первые 2 блока `<figure>…</figure>`. Например: `python3 -c "import re,pathlib; t=pathlib.Path('catalan/kurs/kurs-lekcii.html').read_text(); print(re.search(r'<style.*?</style>', t, re.S).group(0))"`.
- **ПЛАН пиши в `## ПЛАН` ДО правок.**

## 1. ДИСЦИПЛИНА (Карпатов)

- **Назови предпосылки и развилки**; развилку не выбирай молча — в `## ВОПРОСЫ`.
- **Оспорь ложную предпосылку — включая написанную в этом заходе** (см. §3: предпосылки уже проверены аналитиком на живых файлах, но проверь сам; ложна → скажи с доказательством, не гоняйся за фантомом).
- **Минимум.** Ровно то, что решает задачу. Никаких абстракций «на будущее», никакого «заодно отрефакторю».
- **Хирургия.** Каждая изменённая строка трассируется к фразе задания. Чужой стиль (кавычки, отступы, имена, язык комментариев — русский) сохранять: код написан в определённой манере, подражай ей.
- **Якорные замены — abort при ≠1 совпадении.**
- **Субагенты:** заход маленький, fan-out не нужен. Если всё же зовёшь — ≤5 параллельно, рейт-лимит = отступить и доложить, не слепой ретрай.

## 2. ГРАНИЦЫ (что НЕ трогать)

- **НЕ трогать `build_deck.py`**, `render.py`, `audit.py`, `harvest_katex.py` — другой движок/инструменты.
- **НЕ менять модель данных** `build_doc.py` (поток блоков), не переписывать рабочий рендер ради красоты.
- **НЕ трогать содержимое** `catalan/**` и `kurs leto 2026/**` — это тексты владельца. `kurs-lekcii.html` — **read-only эталон**.
- **НЕ писать новый генератор** (правило фабрики: брать готовый, дорабатывать существующий).
- **НЕ делать оглавление (`.toc`)** — отложено сознательно, отдельная задача. Один риск за раз.

## 3. ЗАДАЧА

**Предпосылки (проверены аналитиком на живых файлах 2026-07-16 — перепроверь, но не переоткрывай):**

- `render_inline()` пропускает HTML автора КАК ЕСТЬ («доверенный источник, традиция семьи build_deck») → **инлайн-SVG уже доходит до выхода**. Проверено пробой: `<figure><svg…></svg><figcaption>…</figcaption></figure>` в markdown → в `view.html` SVG на месте.
- **Дефект 1:** блок сырого HTML попадает в ветку «абзац» `render_stream()` и оборачивается в `<p>` → на выходе `<p><figure>…` — невалидно (`<figure>` внутри `<p>`), браузер рвёт абзац, стили едут.
- **Дефект 2:** в CSS `build_doc.py` **нет SVG-палитры**. Ручные эталоны владельца рисуют классами `.s-line .s-dash .s-node .s-node-r .s-accent .s-thin .s-fillw .s-fillsh .s-ar-a .s-ar-m .s-txt-*` — в генераторе их нет, поэтому SVG выйдет голым.
- **Дефект 3:** нет стилей `<figure>/<figcaption>` (у `.ill-ph` они есть, у реальной картинки — нет).
- `> поле:mn`, `.insight`, `.foot`, `.flag`, KaTeX, гиперссылки — **уже работают, не трогай**.

**Сделать ровно три вещи:**

1. **`render_stream()`: блок, начинающийся с сырого HTML-тега верхнего уровня** (`<figure`, `<svg`, `<div`, `<table`, `<details`), отдавать в выход КАК ЕСТЬ, без обёртки `<p>`. Ветку поставить рядом с существующими (после ветки «иллюстрация-плейсхолдер `🖼`», в той же манере `if first.startswith(...)`: continue). Плейсхолдер `🖼` и `render_ill()` **оставить рабочими** — они ещё используются.
2. **Портировать SVG-палитру из эталона** `catalan/kurs/kurs-lekcii.html`: взять из его `<style>` правила для `.s-*`-классов и перенести в CSS `build_doc.py`, **со значениями оттуда** (цвета/толщины НЕ выдумывать — палитра владельца уже устоялась; если в эталоне класс завязан на переменные, которых в `build_doc.py` нет, — либо возьми ближайшую существующую переменную движка, либо захардкодь значение эталона; выбор назови в отчёте).
3. **Добавить стили `figure`/`figcaption`** для реальной картинки: центрирование, отступы, подпись мелким приглушённым (в манере существующего `.ill-ph figcaption`). `svg{max-width:100%;height:auto}` — чтобы не вылезала.

**КРИТЕРИЙ ГОТОВНОСТИ (все пункты, каждый может ПРОВАЛИТЬСЯ):**

- [ ] На пробном источнике (сделай сам, положи во ВРЕМЕННУЮ папку вне `catalan/**`, например `/tmp/proba/`) с блоком `<figure><svg …><figcaption>…</figcaption></figure>`: в выходном `view.html` **`grep -c "<p><figure"` = 0**, при этом `grep -c "<figure"` ≥ 1.
- [ ] В выходном CSS присутствуют ВСЕ классы, реально встречающиеся в эталоне (`grep -oE "\.s-[a-z-]+" catalan/kurs/kurs-lekcii.html | sort -u` — список сверь; каждый обязан быть в `build_doc.py`).
- [ ] **Регрессия:** `python3 _generator/build_doc.py catalan/zhurnal/2026-07-13_dika-v-vysshey-matematike/obyasnenie-tl/src --title "Темперли — Либ и пути Дика"` — линтер зелёный, сборка проходит, **число `<figure class="ill-ph"` в выходе = 4** (плейсхолдеры не сломаны), `<p>`-абзацев не стало меньше, чем было до правки (посчитай ДО и ПОСЛЕ).
- [ ] `python3 _generator/build_doc.py _generator/doc-demo` (если папка есть) — собирается без падения.
- [ ] **Визуальный гейт (важно — у тебя он, скорее всего, ЕСТЬ, в отличие от аналитика).** Ты работаешь на машине владельца: если доступны Chrome + playwright (см. рецепт в `_generator/render.py`), **сними скриншот** пробного `view.html` и **посмотри на него**: `<figure>` не разорвана абзацем, SVG видна, подпись под ней, линии не чёрные-по-умолчанию, а в палитре эталона. Приложи вывод в отчёт. Если Chrome/playwright недоступны — **скажи это явно в отчёте**, не молчи (тихий недобор = провал гейта).

## 4. ВЕРИФИКАТОР

Заход не двигает и не удаляет данные — полноценный субагент-верификатор не нужен. Вместо него: **сверка ДО/ПОСЛЕ по числам** (см. регрессию в критерии) + `diff` бэкапа `_generator/build_doc.py` с новым — в отчёте назови число изменённых строк и что каждая делает. Если правка вышла больше ~40 строк — **остановись и спроси в `## ВОПРОСЫ`**: значит задача понята шире, чем ставилась.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу

Что сделал + **ЗАЧЕМ** / как проверил (все пункты критерия — с числами) / что НЕ трогал / вопросы / результат сверки ДО/ПОСЛЕ / доступен ли был визуальный гейт и что увидел / **время прогона + потраченные токены**.

---

## ПЛАН
*(заполняет исполнитель — до правок)*

**Бэкап:** `/tmp/backup_generator_20260716_103848.tar.gz` (весь `_generator/`, 4.4 МБ).

**Предпосылки — перепроверены, все подтвердились:**
- `render_inline()` действительно пропускает HTML как есть — источник доверенный.
- Дефект 1 подтверждён: в `render_stream()` нет ветки для сырого HTML-блока → падает в п.7 «абзац» → `<p><figure>…</p>`.
- Дефект 2 подтверждён: в CSS `build_doc.py` нет ни одного `.s-*` класса; в эталоне (`grep -oE '\.s-[a-z-]+' catalan/kurs/kurs-lekcii.html | sort -u`) их 13: `.s-accent .s-ar-a .s-ar-m .s-dash .s-fillsh .s-fillw .s-line .s-node .s-node-r .s-thin .s-txt .s-txt-m .s-txt-w`.
- Дефект 3 подтверждён: в `build_doc.py` есть только `.ill-ph figcaption`, для «настоящего» `figure`/`figcaption` (без класса `ill-ph`) стилей нет.

**Эталон вытащен программно (без полного чтения 220 КБ файла):**
- `<style>` — 9750 символов, взят целиком regex'ом.
- Первые 2 `<figure>…</figure>` — структура `<figure><svg viewBox="…" …>…</svg><figcaption>…</figcaption></figure>`, БЕЗ class-атрибута (в отличие от `figure.ph`, который в эталоне — плейсхолдер аналитика, аналог нашего `.ill-ph`, трогать не буду).

**Три правки:**

1. **`render_stream()`** — новая ветка ПОСЛЕ ветки `🖼` (п.2), ДО ветки «закрытый мат-долг» (п.3): если `first` начинается с `<figure`, `<svg`, `<div`, `<table`, `<details` → `out.append(joined)` как есть, `continue`. Один `if`, ~4 строки, в манере `first.startswith(...)`.

2. **CSS-палитра `.s-*`** — переношу блок «SVG helpers» из эталона ДОСЛОВНО (13 классов), в `:root` `build_doc.py` уже есть `--text` и `--accent` (собственные значения генератора, не эталона — так и должно быть, палитра логическая, не числовая), но НЕТ `--muted`-эквивалента для варианта эталона (в build_doc `--muted` уже есть, значения близкие) и НЕТ `--warm`/`--shade` — этих двух переменных в `build_doc.py` нет вовсе.
   - Решение по `--warm`/`--shade` (называю в отчёте по правилу §3 задачи): добавлю их в `:root` build_doc.py со значениями ИЗ ЭТАЛОНА дословно (`--warm:#c9743a; --shade:#dfeaf0;`) — это не «выдумывание», а точный перенос палитры владельца, как и требует пункт 2 задачи («значения оттуда»). Не выбираю «ближайшую существующую переменную», потому что в build_doc.py нет визуально близкого аналога (там нет ни оранжевого акцента, ни голубоватой заливки) — искажение палитры хуже двух новых custom-properties.
   - `.s-line/.s-thin/.s-dash/.s-accent` используют `var(--text)`, `var(--muted)`, `var(--accent)` — эти три переменные в build_doc.py уже есть под теми же именами, беру их (движковые значения, не эталонные — так и предписано пунктом 2: «ближайшая существующая переменная»).

3. **`figure`/`figcaption` (без `.ill-ph`)** — добавляю правило в манере `.ill-ph figcaption` (шрифт `--sans`, приглушённый цвет, чуть мельче), плюс центрирование и отступы как в эталоне, плюс `svg{max-width:100%;height:auto}`.

**Проба:** временный markdown в `/tmp/proba/` с одним блоком `<figure><svg…><figcaption>…</figcaption></figure>`, соберу через `build_doc.py`, проверю `grep -c "<p><figure"` = 0 и `grep -c "<figure"` ≥ 1.

**Регрессия (baseline СНЯТ до правки движка):** `<p>` = 118, `<figure class="ill-ph"` = 4 — в `lekciya-tl-puti-dika.md` (пересобрано текущим, ещё не тронутым движком). После правки жду те же числа (в этом файле нет сырых HTML-блоков `<figure>/<svg>` — правка 1 их не касается).

Смету по объёму правки: ветка в `render_stream()` (~4 строки) + CSS-блок `.s-*` (~13 строк) + 2 новые переменные в `:root` (2 строки) + `figure/figcaption` (~6 строк) ≈ 25 строк — в пределах лимита §4 (~40 строк), останавливаться не буду.

## ВОПРОСЫ
*(заполняет исполнитель)*

Развилок не возникло — все три предпосылки аналитика подтвердились без исправлений. Единственный выбор (§2 задачи, «палитра завязана на переменных, которых нет») сделан и обоснован в ПЛАНЕ: `--warm`/`--shade` добавлены новыми custom-properties со значениями эталона дословно, а не «ближайшим существующим» приближением — в build_doc.py нет визуально близких аналогов (нет оранжевого и голубоватого тонов вообще).

## ОТЧЁТ
*(заполняет исполнитель)*

**Бэкап:** `/tmp/backup_generator_20260716_103848.tar.gz` (весь `_generator/`, 4.4 МБ, сделан первым действием, до правок).

### Что сделал и зачем

Три правки в `_generator/build_doc.py`, ровно по пунктам задачи:

1. **`render_stream()`** — новая ветка 2b (4 строки) сразу после ветки `🖼`-плейсхолдера: блок, начинающийся с `<figure`, `<svg`, `<div`, `<table`, `<details`, уходит в выход как есть, без обёртки `<p>`. Чинит дефект 1 — раньше такой блок падал в ветку «абзац» и HTML выходил `<p><figure>…</p>` (невалидно, браузер рвёт абзац).
2. **`:root` + SVG-палитра** — добавил `--warm:#c9743a; --shade:#dfeaf0;` в `:root` (в build_doc.py их не было вовсе) и перенёс 13 классов `.s-line/.s-thin/.s-dash/.s-accent/.s-fillw/.s-fillsh/.s-node/.s-node-r/.s-txt/.s-txt-w/.s-txt-m/.s-ar-a/.s-ar-m` дословно из `catalan/kurs/kurs-lekcii.html`, с одной заменой: `font-family:"Source Sans 3",sans-serif` → `font-family:var(--sans)` (движковая переменная шрифта, у build_doc.py свой `--sans`, отличный от эталонного — это и есть «ближайшая существующая переменная» из формулировки задачи). `--text`, `--muted`, `--accent` брал движковые (уже существовали под теми же именами) — их эталонные значения НЕ переносил, чтобы не рассинхронизировать с остальной палитрой генератора. Чинит дефект 2.
3. **`figure`/`figcaption`** (без `.ill-ph`) — центрирование, отступы `2.1em 0`, `svg{max-width:100%;height:auto}`, подпись в манере `.ill-ph figcaption` (шрифт `--sans`, `.88rem`, приглушённый `--muted`). Чинит дефект 3.

### Проверка (все пункты критерия)

- **Проба:** `/tmp/proba/src/test.md` с блоком `<figure><svg…><figcaption>…</figcaption></figure>` + окружающими абзацами + плейсхолдером `🖼`. После сборки: `grep -c "<p><figure"` = **0**, `grep -c "<figure"` = **2** (реальная + плейсхолдер `.ill-ph`). ✓
- **Полнота CSS-палитры:** сверил `grep -oE '\.s-[a-z-]+' catalan/kurs/kurs-lekcii.html | sort -u` (13 классов) против `build_doc.py` — расхождений нет, все 13 на месте. ✓
- **Регрессия на живой лекции** (`catalan/…/obyasnenie-tl/src/lekciya-tl-puti-dika.md`): ДО правки движка (baseline, снят отдельным прогоном текущего на тот момент build_doc.py) — `<p>` = 118, `<figure class="ill-ph"` = 4. ПОСЛЕ правки — `<p>` = 118, `<figure class="ill-ph"` = 4. Числа совпали (в этом markdown-источнике нет сырых HTML-блоков `<figure>/<svg>`, поэтому ветка 2b на нём не сработала — ожидаемо). Линтер зелёный, сборка проходит. ✓
- **`_generator/doc-demo`:** папка есть (`matematika.md`, `naupop.md`), собирается без падения (1 мягкое предупреждение про открытый мат-долг в chernovik-статусе — это норма линтера, не ошибка). ✓
- **Визуальный гейт:** Chrome-браузер (Claude Browser pane) доступен на машине владельца. Поднял `python3 -m http.server` на `/tmp/proba/src`, открыл `view.html`, снял скриншот. Увидел: `<figure>` НЕ разорвана абзацем текста (окружает её `<p>Обычный абзац…</p>` сверху и `<p>Ещё один абзац…</p>` снизу — оба целые), SVG-окружность отрисована классом `.s-line` (тёмный контур, а не браузерный дефолт), подпись «Тестовая подпись» под иллюстрацией мелким приглушённым шрифтом — визуально совпадает с манерой `.ill-ph`. Плейсхолдер `🖼` (dashed-рамка) ниже тоже цел. Гейт пройден.

### Что НЕ трогал

`build_deck.py`, `render.py`, `audit.py`, `harvest_katex.py` — не открывал. Модель данных потока (список блоков) не менял. `catalan/**` и `kurs leto 2026/**` не менял (кроме файла-захода самого себя — план/отчёт). Оглавление (`.toc`) не делал. `render_ill()` и плейсхолдер `🖼` оставлены рабочими как есть — не трогал их код, только добавил соседнюю ветку.

### Объём правки

`diff` бэкапа с новым файлом: **22 добавленные строки + 1 изменённая** (расширение строки `:root` двумя переменными) = в пределах лимита §4 (~40 строк), останавливаться и спрашивать не потребовалось.

### Время и токены

Заход занял один проход без субагентов (задача заявлена как маленькая, fan-out не понадобился). Токены/время — не логирую отдельно, оценка «на глаз»: чтение файла-захода + `build_doc.py` (450 строк) + программная выжимка эталона + 3 точечные правки + проба + регрессия + визуальный гейт — около 20–25 тыс. токенов заявленного контекста, без параллельных агентов.
