# Нить 2 — Голоса и отражение (Л2: выход в несбалансированный мир)

> Сюжет Л2: процесс-first вход — задача о голосах; решая, приходим к произвольным (несбалансированным) путям ±1 — осевому объекту Мира 1; принцип отражения даёт формулу и для Каталана, и в общем; одна идея = формула Бертрана = разорение игрока. Оставляет открытым: отражение «случайно» → Н3 ищет единый счёт. Узлы в порядке нити. Объекты — `mir1-obiekty.md`. Формат — `STANDART-uzla.md`.

```
id: zatravka-ballot · затравка · задача о подсчёте голосов (баллотировка)
находка: A набрал a голосов, B — b < a; сколькими способами при последовательном подсчёте A вёл ВСЮ
  дорогу? Конкретная процесс-first задача, из которой сам собой рождается путь ±1 (шаг = бюллетень).
  Вход в несбаланс — как парковка в Мире 2.
связи: [obj-ballot — герой; obj-put-nesbalans-vhod → объект-ответ; bij-otrazhenie → метод; most-otrazhenie-bertran → костюмы идеи]
уровень: школьно · источник: catalan-dyck-ideas (B); Bertrand 1887 · статус: черновик
```

```
id: obj-put-nesbalans-vhod · объект · несбалансированный путь как осевой объект
находка: подсчёт голосов = путь ±1 от оси, вообще говоря НЕ возвращающийся (конец на высоте a−b).
  Число ВСЕХ путей = биномиальный коэффициент (дети знают — не тратим). Условие «A всегда впереди» =
  «путь не касается оси» — вот где нужна биекция. Сбаланс. путь Дика = частный случай (возврат в 0).
связи: [zatravka-ballot ← откуда; obj-put-nesbalans; obj-put-dika ← частный случай; bij-otrazhenie → счёт условия]
уровень: школьно · источник: mir1-nahodki §V (obj-put-nesbalans); _seed-kb §2 · статус: черновик
```

```
id: bij-otrazhenie · биекция · принцип отражения (André) → формула разностью биномов
находка: «плохие» пути (коснулись оси) ↔ пути с отражённым началом ⇒ число хороших = разность двух
  биномов; частный случай возврата даёт Cₙ = C(2n,n)−C(2n,n−1) = C(2n,n)/(n+1). Первое доказательство
  «широкого контекста»: работает и в несбалансе, и для Каталана.
связи: [obj-put-nesbalans-vhod ← на чём; formula-factorial (нить1) ↔ та же Cₙ иным путём; most-otrazhenie-bertran → костюмы]
уровень: школьно · источник: _seed-kb §2 (André, 1887) · статус: черновик
```

```
id: most-otrazhenie-bertran · мостик · отражение = формула Бертрана = разорение игрока
находка: один зеркальный приём носит три костюма — баллотировка (Бертран), подсчёт путей, разорение
  игрока (вероятность проигрыша блуждания с барьером). Готовый throughline-мостик; связывает
  комбинаторный и вероятностный углы уже в Л2, готовит Н4.
связи: [bij-otrazhenie ← приём; obj-random-walk (нить4) → разорение; ruin (catalan-dyck-ideas C)]
уровень: школьно · источник: catalan-dyck-ideas часть C (most-otrazhenie) · статус: черновик
```

```
id: vyhod-k-ciklu · выход · отражение красиво, но «случайно» — нужен единый счёт (мост в Н3)
находка: зеркало появляется «ниоткуда»; естественный вопрос — нет ли ОДНОГО счёта, объясняющего
  формулу без трюка. Это двигает в Н3 (цикл-лемма: та же формула прокруткой, и она обобщается + сшивает миры).
связи: [bij-otrazhenie ← что оставили открытым; nit-3:zatravka-cikl → ответ]
уровень: школьно · источник: — (нарративная связка) · статус: черновик
```
