# CLAUDE.md — вход Claude Code (стаб; полный порт — в очереди)

> Работаешь в этой папке как Claude Code — читаешь **ТОЛЬКО** свой файл-заход, названный владельцем (в `zhurnal/<арка>/`), и то, что он называет поимённо. НЕ изучаешь весь проект, заход самодостаточен.
> Полная версия (дисциплина, субагенты, формат отчёта) портируется Code-заходом из `../kurs leto 2026/CLAUDE.md`. Канон процесса — скилл `agentic-coding-session-brief`.

| Ситуация | Дом |
|---|---|
| **🖼 работаешь с ИЗОБРАЖЕНИЕМ** (схема, диаграмма, график — слайды И тексты) | `../_illustracii/` — отдельная фабрика; ядро `../_illustracii/DISCIPLINA.md`, читать ПЕРЕД рисованием |

Дисциплина коротко: не ломать; минимальная правка + проверка; якорные замены — abort при ≠1 совпадении; двигаешь / удаляешь / сжимаешь → snapshot + свежий верификатор другим проходом; один риск за раз (аудит → мутация → сверка). `## ВОПРОСЫ` и `## ОТЧЁТ` дописываешь в свой файл-заход.

**⚙️ HTML — только готовым генератором `materials/_generator/`** (`build_doc.py` — документ-вид, `build_deck.py` — слайды); новый генератор НЕ писать, правь источник (`content/*.md`, `tokens.css`), HTML — выход.

Среда: удаление гейтится (`allow_cowork_file_delete`); снапшоты — `tar`; папка не git.
