# Перебор Э0.3/Э0.5 (T_BODY_MARGIN_TOP_PX / T_BODY_MARGIN_SIDE_PX)

Полностью автоматический перебор (`podbor_margin.py`) не удалось довести до конца:
солвер (`vmeshchenie.podobrat_slide`, устройство — не тронуто) на карточке `retrakt`
(`polosa_gorizontalnaya`, `liniya=75.4`) стал НЕДЕТЕРМИНИРОВАННО падать с «ни одна
проба не влезла внутри жёстких зон» — иногда находит решение, иногда нет, на
БУКВАЛЬНО одном и том же входе (проверено: тот же T_BODY_MARGIN_TOP_PX=0/SIDE=0,
то есть БЕЗ моей правки Э0.3/Э0.5 вовсе — падает тоже, значит причина не в моей
правке отступов, а в стоимости самой блок-схемы Э2/Э3 на этой карточке, упирающейся
в границу солверных «жёстких зон». Это не входит в мою зону (солвер — устройство).

## Что удалось снять ручным перебором (top, при side=12 фикс)

Каждая точка — полная сборка `deck.py … --zanovo` (с ретраями на редкую сборку из-за
той же нестабильности retrakt) + `progon.py`. Числа НЕ включают retrakt отдельно,
только суммарный охват — сравнивать между собой корректно, retrakt в каждой точке
даёт свои единицы px, роли не меняющие.

| top, px | переполнено (слайдов) | суммарно px | цена ср. % | цена худш. % |
|---|---|---|---|---|
| 20 | 7 | 184.1 | 2.13 | 26.32 (kategoriya) |
| 26 | 6 | 171.2 | 2.26 | 26.32 (kategoriya) |
| 32 | 6 | 159.2 | 2.26 | 26.32 (kategoriya) |

top=32 численно чуть лучше (159.2 против 171.2 px), но именно на нём `--zanovo`
ни разу не собрался за 8 попыток (retrakt) — а на top=26 сборка успешна и
воспроизводима (проверено повторным прогонщиком: `progon.py` после отката к
top=26 даёт РОВНО те же 171.2 px / 6 слайдов, что и первый успешный прогон).
**Выбрано top=26** — не по максимуму числа, а по критерию «собирается» (клауза 5:
артефакт обязан существовать).

## side (лево/право)

Проверен ТОЛЬКО side=0 (выключено) и side=12 — side=0 тоже не чинит нестабильность
retrakt (см. выше), поэтому полноценная сетка по side не пройдена по той же причине.
side=12 оставлен как рабочее, консервативное значение (равно top/2, без отдельного
обоснования числом — честно назвать это НЕ полным подбором, а тем, что успело
пройти до того, как нестабильность солвера сделала дальнейший перебор
нецелесообразным).

## Итог

**T_BODY_MARGIN_TOP_PX = 26, T_BODY_MARGIN_SIDE_PX = 12** — рабочее значение,
воспроизводимо даёт 6 переполненных слайдов из 21 (171.2 px суммарно), из них
**3 — карточки, запертые автором `verstka_reshena: da`** (`funktor`, `kategoriya`,
`kategoriya-vect-iso`, +30…+69 px каждая — новая схема стоит дороже старой
авторской калибровки, чинить не мой мандат), **3 — малый остаточный шум сметы**
(`retrakt` +3.8px, `teorema-brauera` +6.7px, `vektornye-prostranstva` +9.0px,
то есть <0.2 строки каждая).
