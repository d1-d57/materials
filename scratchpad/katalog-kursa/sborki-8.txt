── _studio/zhurnal/2026-08-24_obzor-funkciya-putey/kod_arhitektura-punkta.md
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/plany.py --proverit; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/plany.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `grep -c '^<!--id: p-' kurs-puti-i-volny/plan/src/punkty.md` НЕ ПРОВЕРЕНА (метасимвол(ы) < — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `cd kurs-puti-i-volny && python3 tools/indeks.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ & ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/graf.py | grep -A3 '^\[c\]'` НЕ ПРОВЕРЕНА (метасимвол(ы) [ \ ] | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С5: `git_zona.py check`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С3: путь `scratchpad/otchet.md` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: пути, названные в прозе и не найденные (2): `../docs/kak-delat/GIT-disciplina.md`, `../docs/kak-delat/STANDART-teksta.md` — адресами работы они не являются, красным не считаю
── _studio/zhurnal/2026-08-24_obzor-funkciya-putey/kod_graf-korpusa.md
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/graf.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/graf.py | grep -cE '^\[(1|2|3|4|5|6|c)\]'` НЕ ПРОВЕРЕНА (метасимвол(ы) ( ) [ \ ] | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `cd kurs-puti-i-volny && python3 tools/indeks.py --proverit` НЕ ПРОВЕРЕНА (метасимвол(ы) & — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С5: `git_zona.py check`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С5: `register_doc.py kurs-puti-i-volny`: справку получить не удалось (инструмент `_generator/tools/register_doc.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
❌ С3: путь `/Users/ivanyakovlev/Documents/GitHub/materials-wt/graf-korpusa` НЕ СУЩЕСТВУЕТ ни от одной из 2 баз (`test -e` → ложь), а назван АДРЕСОМ работы — урок 45
⚠ С3: путь `scratchpad/graf-korpusa/` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: путь `scratchpad/otchet.md` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: пути, названные в прозе и не найденные (4): `../docs/kak-delat/GIT-disciplina.md`, `../docs/kak-delat/STANDART-teksta.md`, `sys.path`, `topsort_karty.py` — адресами работы они не являются, красным не считаю
── _studio/zhurnal/2026-08-24_obzor-funkciya-putey/kod_konsolidaciya-korpusa.md
⚠ С4: команда критерия `grep -cE '^- Р([1-9]|1[0-6]) · СТАТУС: ' kurs-puti-i-volny/SBORKA/KARTA-rashozhdeniy.md` НЕ ПРОВЕРЕНА (метасимвол(ы) ( ) [ ] | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `grep -cE '^- Д-пара [1-7] · ' kurs-puti-i-volny/SBORKA/KARTA-rashozhdeniy.md` НЕ ПРОВЕРЕНА (метасимвол(ы) [ ] — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/dubli.py --porog 0.45` НЕ ПРОВЕРЕНА (`python3` разрешён ТОЛЬКО в форме `python3 <инструмент из _generator/tools/> [подкоманда] --help`) — выполните вручную
⚠ С4: команда критерия `cd kurs-puti-i-volny && python3 tools/indeks.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ & ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `ls catalan/kartoteka/kart-*.md | wc -l` НЕ ПРОВЕРЕНА (метасимвол(ы) * | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `grep -c '^| .kart-' catalan/kartoteka/KARTA-OBLASTI.md` НЕ ПРОВЕРЕНА (метасимвол(ы) | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С5: `git_zona.py check`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
❌ С3: путь `/Users/ivanyakovlev/Documents/GitHub/materials-wt/konsolidaciya-korpusa` НЕ СУЩЕСТВУЕТ ни от одной из 2 баз (`test -e` → ложь), а назван АДРЕСОМ работы — урок 45
⚠ С3: путь `scratchpad/konsolidaciya-korpusa/` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: путь `scratchpad/otchet.md` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: пути, названные в прозе и не найденные (5): `../docs/kak-delat/GIT-disciplina.md`, `../docs/kak-delat/STANDART-teksta.md`, `obzory/…/LEKCIYA-v2/00-arhitektura.md`, `obzory/…/LEKCIYA/00-obshchee.md`, `obzory/…/SKELET.md` — адресами работы они не являются, красным не считаю
── _studio/zhurnal/2026-08-24_obzor-funkciya-putey/kod_napolnenie-chetverti.md
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/plany.py --proverit; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/plany.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `cd kurs-puti-i-volny && python3 tools/indeks.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ & ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/graf.py | grep -A2 '^\[c\]'` НЕ ПРОВЕРЕНА (метасимвол(ы) [ \ ] | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С5: `git_zona.py check`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С3: путь `scratchpad/napolnenie-chetverti/` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: путь `scratchpad/otchet.md` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: пути, названные в прозе и не найденные (3): `../docs/kak-delat/GIT-disciplina.md`, `../docs/kak-delat/STANDART-teksta.md`, `…/kotly/matematika.md` — адресами работы они не являются, красным не считаю
── _studio/zhurnal/2026-08-24_obzor-funkciya-putey/kod_otrisovka-vidov.md
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/plany.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/plany.py --proverit; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 kurs-puti-i-volny/tools/graf.py | grep '^\[c\]'` НЕ ПРОВЕРЕНА (метасимвол(ы) [ \ ] | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `cd kurs-puti-i-volny && python3 tools/indeks.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ & ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С5: `git_zona.py commit`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С5: `git_zona.py check`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С3: путь `scratchpad/otrisovka-vidov/` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: путь `scratchpad/otchet.md` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: пути, названные в прозе и не найденные (3): `../docs/kak-delat/GIT-disciplina.md`, `../docs/kak-delat/STANDART-teksta.md`, `chetvert-N.md` — адресами работы они не являются, красным не считаю
── _studio/zhurnal/2026-08-24_obzor-funkciya-putey/kod_pasporta-korpusa.md
⚠ С4: команда критерия `cd kurs-puti-i-volny && python3 tools/indeks.py` НЕ ПРОВЕРЕНА (метасимвол(ы) & — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `grep -c '^| .*⚠' kurs-puti-i-volny/INDEKS.md` НЕ ПРОВЕРЕНА (метасимвол(ы) * | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `grep -c '^| \` НЕ ПРОВЕРЕНА (метасимвол(ы) \ | — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С5: `git_zona.py zayavki`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С5: `git_zona.py check`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С5: `git_zona.py commit`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С3: путь `scratchpad/pasporta-korpusa/` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: путь `scratchpad/otchet.md` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: пути, названные в прозе и не найденные (2): `../docs/kak-delat/GIT-disciplina.md`, `../docs/kak-delat/STANDART-teksta.md` — адресами работы они не являются, красным не считаю
── _studio/zhurnal/2026-08-24_obzor-funkciya-putey/kod_rasskaz-god.md
⚠ С4: команда критерия `grep -c '<!--id: rasskaz-g' kurs-puti-i-volny/RASSKAZ-god.md` НЕ ПРОВЕРЕНА (метасимвол(ы) < — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `grep -c '\.svg' kurs-puti-i-volny/RASSKAZ-god.md` НЕ ПРОВЕРЕНА (метасимвол(ы) \ — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `python3 ../disciplina/_generator/tools/check_termin.py kurs-puti-i-volny/RASSKAZ-god.md; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С4: команда критерия `cd kurs-puti-i-volny && python3 tools/indeks.py; echo $?` НЕ ПРОВЕРЕНА (метасимвол(ы) $ & ; ? — строка писана для шелла, к токенам не приводится) — выполните вручную
⚠ С5: `register_doc.py kurs-puti-i-volny`: справку получить не удалось (инструмент `_generator/tools/register_doc.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С5: `check_termin.py kurs-puti-i-volny`: справку получить не удалось (инструмент `_generator/tools/check_termin.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С5: `git_zona.py check`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
⚠ С3: путь `scratchpad/rasskaz-god/` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: путь `scratchpad/otchet.md` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: пути, названные в прозе и не найденные (2): `../docs/kak-delat/GIT-disciplina.md`, `../docs/kak-delat/STANDART-teksta.md` — адресами работы они не являются, красным не считаю
── _studio/zhurnal/2026-08-24_obzor-funkciya-putey/kod_zakon-zakrytiya-noch-puti-i-volny.md
⚠ С4: секция «КРИТЕРИЙ ГОТОВНОСТИ» не найдена — сверять нечего
⚠ С5: `git_zona.py check`: справку получить не удалось (инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — отказ) — форма вызова не проверена
❌ С8: нет раздела «## 4.1 ГИГИЕНА» — заход не говорит, чем исполнитель проверит СОСТОЯНИЕ репозитория перед сдачей (зона доехала в git, второй репозиторий, невлитые ветки, точка вызова нового инструмента, регистрация нового `.md`, чужие пути в коммите). Раздел вшит в `bootstrap_zahod.py` и появляется сам — его отсутствие означает заход, собранный или правленный руками
⚠ С3: путь `scratchpad/zakon-zakrytiya-noch-puti-i-volny/` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)
⚠ С3: путь `scratchpad/otchet.md` вписан ШАБЛОНОМ генератора (`bootstrap_zahod.py`, боилерплейт КОНТРАКТА ЗОНЫ), не аналитиком — в этом репозитории может не существовать физически, адресом работы не считаю (заявка 2026-08-29T0106)

═══ СЛЕПЫЕ ЗОНЫ И ОХВАТ — печать всегда, не гейт ═══
   судимо файлов: 8 из 8
   белый список: 15 вход(ов) · таймаут 20 с · потолок 120 команд на файл
   команд найдено в текстах: 84 · ВЫПОЛНЕНО: 25 · отказано: 59
   НЕ ВЫПОЛНЕНО (проверьте вручную), по причинам:
     · инструмент `_generator/tools/git_zona.py` вне `_generator/tools/` того репозитория, где лежит гейт — 22 шт.: `python3 _generator/tools/git_zona.py --help`; `python3 _generator/tools/git_zona.py check --help`; `python3 _generator/tools/git_zona.py commit --help`
     · метасимвол(ы) $ ; ? — 8 шт.: `python3 ../disciplina/_generator/tools/check_termin.py kurs-puti-i-vol`; `python3 kurs-puti-i-volny/tools/graf.py; echo $?`; `python3 kurs-puti-i-volny/tools/plany.py --proverit; echo $?`
     · метасимвол(ы) $ & ; ? — 5 шт.: `cd kurs-puti-i-volny && python3 tools/indeks.py; echo $?`
     · инструмент `_generator/tools/register_doc.py` вне `_generator/tools/` того репозитория, где лежит гейт — 4 шт.: `python3 _generator/tools/register_doc.py --help`; `python3 _generator/tools/register_doc.py kurs-puti-i-volny --help`
     · метасимвол(ы) [ \ ] | — 3 шт.: `python3 kurs-puti-i-volny/tools/graf.py | grep '^\[c\]'`; `python3 kurs-puti-i-volny/tools/graf.py | grep -A2 '^\[c\]'`; `python3 kurs-puti-i-volny/tools/graf.py | grep -A3 '^\[c\]'`
     · метасимвол(ы) < — 2 шт.: `grep -c '<!--id: rasskaz-g' kurs-puti-i-volny/RASSKAZ-god.md`; `grep -c '^<!--id: p-' kurs-puti-i-volny/plan/src/punkty.md`
     · метасимвол(ы) & — 2 шт.: `cd kurs-puti-i-volny && python3 tools/indeks.py`; `cd kurs-puti-i-volny && python3 tools/indeks.py --proverit`
     · метасимвол(ы) $ ? — 2 шт.: `echo $?`
     · метасимвол(ы) * | — 2 шт.: `grep -c '^| .*⚠' kurs-puti-i-volny/INDEKS.md`; `ls catalan/kartoteka/kart-*.md | wc -l`
     · инструмент `_generator/tools/check_termin.py` вне `_generator/tools/` того репозитория, где лежит гейт — 2 шт.: `python3 _generator/tools/check_termin.py --help`; `python3 _generator/tools/check_termin.py kurs-puti-i-volny --help`
     · метасимвол(ы) ( ) [ \ ] | — 1 шт.: `python3 kurs-puti-i-volny/tools/graf.py | grep -cE '^\[(1|2|3|4|5|6|c)`
     · метасимвол(ы) ( ) [ ] | — 1 шт.: `grep -cE '^- Р([1-9]|1[0-6]) · СТАТУС: ' kurs-puti-i-volny/SBORKA/KART`
     · метасимвол(ы) [ ] — 1 шт.: `grep -cE '^- Д-пара [1-7] · ' kurs-puti-i-volny/SBORKA/KARTA-rashozhde`
     · `python3` разрешён ТОЛЬКО в форме `python3 <инструмент из _generator/tools/> [подкоманда] --help` — 1 шт.: `python3 kurs-puti-i-volny/tools/dubli.py --porog 0.45`
     · метасимвол(ы) | — 1 шт.: `grep -c '^| .kart-' catalan/kartoteka/KARTA-OBLASTI.md`
     · метасимвол(ы) \ | — 1 шт.: `grep -c '^| \`
     · метасимвол(ы) \ — 1 шт.: `grep -c '\.svg' kurs-puti-i-volny/RASSKAZ-god.md`
   структурно не проверяется:
     · правдивость «этого нет» (С6) не проверяется — только наличие ОБЪЕКТА и команды про него рядом; «нет нигде» неопровержимо без указания, где искать. Словарь отрицаний из 12 словоформ неполон по построению
     · имя ветки распознаётся по пяти формам записи (`--branch X`, «обязано быть `X`», `zahod/…`, «ветка `X`»); шестая форма пройдёт мимо С1 и С7
     · С6 не судит строки markdown-таблиц, текст в «ёлочках» и врезки в круглых скобках — заход, который ЦИТИРУЕТ или ПОЯСНЯЕТ, ничего не утверждает о мире
     · утверждение прозой, без адреса и без команды, невидимо этому гейту целиком
     · число сверяется только со СЧИТАЮЩЕЙ командой (вывод — одно целое); `git show --stat` рядом с «ровно два пути» не сверяется
     · С3 краснеет ТОЛЬКО на путях в адресных местах (якоря §0, КОНТРАКТ ЗОНЫ, КРИТЕРИЙ); путь, названный в прозе, печатается ⚠ одной строкой и красным не считается
     · С3 резолвит путь от трёх баз (корень репо гейта, корень репо захода, папка арки) плюс индекс `git ls-files` — по голому имени и по хвосту пути; четвёртая база даст ложное «не существует»
     · С3 не судит `.git/…`, слэш-команды (`/usage`) и голые расширения (`.md`)
     · пути ЗОНЫ в С3 не краснеют: зона перечисляет и те файлы, которые исполнитель только создаст
     · С3b (виден ли якорь с ветки исполнителя) судит только якоря секции «Прочитать ТОЛЬКО это»
     · команда вне белого списка НЕ ВЫПОЛНЯЕТСЯ и потому не проверена — список отказов выше поимённо, молчать про них нельзя (урок 42)
     · С8 судит только НАЛИЧИЕ раздела «ГИГИЕНА» и НЕПУСТОТУ слота зоны в нём. Выполнил ли исполнитель Г1–Г6 и что они ответили, гейт не знает и знать не может: он живёт ДО прогона, а они — после. Это проверяет приёмка по отчёту
     · С8 краснеет на любом заходе, написанном ДО 2026-08-09 (раздела в них нет) — красное истинное, но живого шума не даёт: `check_sborki` зовётся только на свежесобранном файле из `bootstrap_zahod.py`, старые заходы не перелинтуются
     · С9 сверяет только АБСОЛЮТНЫЙ путь к файлам ОБЪЯВЛЕННОЙ зоны, и только в worktree-заходах; абсолютный путь к любому ДРУГОМУ файлу основного дерева (вне зоны) гейт не видит — это защита адреса зоны, а не общий запрет на абсолютные пути

❌ ГЕЙТ СБОРКИ: красных 3 в 3 файл(ах) из 8.
