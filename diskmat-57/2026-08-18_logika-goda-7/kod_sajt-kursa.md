# Канал исполнителя — sajt-kursa (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — дизайн календаря придумывается заново, план рождается по ходу.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — дизайн календаря придумывается заново, план рождается по ходу; механику вёрстки владелец разрешил уводить на Sonnet (режим opusplan).

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-kursa.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/2026-08-18_logika-goda-7/kod_sajt-kursa.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add sajt-kursa --branch zahod/sajt-kursa-diskmat-57, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-19, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py diskmat-57/2026-08-18_logika-goda-7`
Область: «diskmat-57/2026-08-18_logika-goda-7» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/compassionate-zen-bell/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Владелец строит четырёхлетний спецмат в 57 школе; сейчас собран год 7 класса — блоки-мини-курсы, кружок третьим уроком (состав и размер года — снимок при сборке 2026-08-19, проверь: `python3 diskmat-57/_sajt/build.py --proverit`). Прошлый этап: раскладка года переехала из вёрстки в данные `diskmat-57/_sajt/god.json`, и это единственный дом фактов о годе. Тогда же аналитик собрал страницу `_sajt/index.html` **без ТЗ и мимо дисциплины заходов** — она забракована владельцем целиком, и ты собираешь сайт заново по `TZ-sajt.md`.

ЦЕЛЬ: владелец должен увидеть программу года сверху за 3–10 минут и показать её любому человеку. Сейчас он этого сделать не может: в прошлой версии самым заметным на странице оказался контроль, а математику стало не видно.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ.** Ты получишь календарь года, страницы блоков и страницы занятий — но НЕ материалы в них. Листков, домашек и конспектов на диске пока нет: занятия начинаются 1 сентября, и папка `diskmat-57/_sajt` их не содержит — проверь сам, `ls diskmat-57/_sajt`. Страницы занятий делаются пустыми каркасами под будущее наполнение, и это правильный исход, а не недоделка.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-19) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Сайт — один адрес-журнал; сегодня в нём один курс, иерархия класс → предмет → курс закладывается пустой под будущих преподавателей
2. Клетка календаря = неделя = одно занятие = пара (33 пары в году, по одной в неделю); на клетке стоит НОМЕР недели, дат пока нет
3. Единица показа на главной — блок-мини-курс большим прямоугольником; в четверти обычно два блока, иногда три
4. Контрольные внутри блока видны, отдельно стоящие идут узкой деликатной полоской: в прошлой версии контроль был самым жирным на странице, и математику стало не видно
5. Тумблер основной курс / кружок: два календаря по тем же неделям, свои цвета у кружка
6. Опорные точки блока берутся из уже собранной карты содержания в god.json (принята владельцем 19 августа, собрана по книжкам); новых формулировок никто не сочиняет
7. Весь текст на страницах происходит из `diskmat-57/_sajt/god.json` либо из белого списка интерфейсных слов; обоснования, ссылки на источники, оценки, решения и длинные абзацы на сайт не идут. Проверяется гейтом текста (§2, шаг 5)
8. Канал app, параллельный worktree, режим opusplan — выбор владельца 2026-08-20

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/sajt-kursa`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/sajt-kursa-diskmat-57`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add sajt-kursa --branch zahod/sajt-kursa-diskmat-57
  cd ../materials-wt/sajt-kursa
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `diskmat-57/_sajt`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone diskmat-57/_sajt --kommitit 'Свою зону diskmat-57/_sajt целиком: файл данных, сборщик, собранные страницы. Коммитить по вехам — данные отдельно, сборщик отдельно, вид отдельно' --kommitit 'Файл-заход kod_sajt-kursa.md НЕ коммитить: он лежит в основной папке и его забирает приёмка' --zakryt 'Ветку zahod/sajt-kursa-diskmat-57 и папку materials-wt/sajt-kursa не гасить и не вливать самому — влитие есть ход приёмки' --zakryt 'Чужие ветки не трогать вовсе; невлитая zahod/chistka-hvostov к этой работе отношения не имеет'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

🔴 **Вливать чужие ветки этот заход НЕ должен.** Невлитых `zahod/*` при сборке была одна — `zahod/chistka-hvostov`, с пятью непереносёнными файлами; она относится к чистке хвостов арки и к сайту отношения не имеет. Проверь число первым ходом (`git --no-optional-locks branch --no-merged | grep -c 'zahod/'`) и назови его в снимке входа; выросло — назови, чьи ветки, и оставь их в покое.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/sajt-kursa-diskmat-57`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `diskmat-57/_sajt`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти файлы, в этом порядке. Проект не изучай, по сторонам не смотри.**
  1. `/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/2026-08-18_logika-goda-7/TZ-sajt.md` — **ТЗ, оно главнее этого захода в любом расхождении**: устройство трёх экранов, список «чего на сайте НЕТ», критерий готовности владельца;
  2. `diskmat-57/_sajt/god.json` — данные года: четверти, блоки, часы, домены, карта содержания, опорные задачи, дорожка кружка, контрольные, уток методов. **Единственный дом фактов о годе**;
  3. `diskmat-57/_sajt/build.py` и `index.html` — забракованная версия. Читай её как **список того, чего делать не надо**: узкая колонка вместо широкой раскладки, блоки-строки вместо прямоугольников, жирный контроль, отсебятина в прозе. Код переиспользуй только если он честно годится;
  4. `/Users/ivanyakovlev/Documents/GitHub/spetsmat/materials/дизайн-черновики/13_МАКЕТ-страница-темы-v3.html` и рядом лежащие `10_`, `12_`, `17_` — **чужой репозиторий, READ-ONLY, ничего там не трогать**. Оттуда берётся формат страницы занятия. 🔴 Смотреть их **в браузере на весь экран**, а не читать разметку: типовая ошибка — сверстать под узкую колонку в полэкрана.
- **Не читать:** `MIRY-goda.md`, `KONTURY-i-geyty.md`, `ISTOCHNIKI.md`, `KARTA-7.md`, `SESSIYA.md`, выгрузки. Всё, что оттуда нужно сайту, уже лежит в `god.json`; чтение остального даст соблазн пересказать прозу на страницу, а это прямо запрещено ТЗ.
- `diskmat-57/_sajt/rasskaz.json` — **текст, написанный ассистентом от себя. На сайт он НЕ идёт.** Файл оставь на месте, не удаляй и не выводи.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Собрать сайт курса по `TZ-sajt.md`. Шесть шагов.

**Шаг 1. Нарезать год на недели — этого в данных ещё нет.** В `diskmat-57/_sajt/god.json` элементы четверти лежат потоком с часами (блок 6 ч, контрольная 1 ч и так далее). Неделя = два подряд идущих урока этого потока, поэтому одна неделя может делиться между контрольной и началом блока — это законно и должно быть видно.

Проверка счётом: часы четвертей складываются в 66 уроков, то есть 33 недели — **снимок при сборке захода 2026-08-19, проверь сам первым ходом:**
```
python3 -c "import json; g=json.load(open('diskmat-57/_sajt/god.json')); print([c['chasy'] for c in g['chetverti']], sum(c['chasy'] for c in g['chetverti']))"
```
Сошлось не так — останавливайся и пиши в `## ВОПРОСЫ`, данные не подгоняй.

**Шаг 2. Экран года.** По `TZ-sajt.md §3`. Четыре четверти сверху вниз; внутри четверти блоки-прямоугольники (обычно два в ряд); внутри блока — клетки недель с НОМЕРОМ недели. Цвет по домену, стыковые блоки многоцветные. Отдельно стоящие контрольные — узкой полоской, уже блока. Тумблер «основной курс / кружок». Плашка «демо-версия». Раскладка широкая, под весь экран.

**Шаг 3. Экран блока.** Опорные точки нумерованным списком плюс список недель-занятий со ссылками. **Опорные точки берутся из `karta` соответствующего блока в `god.json` — это уже утверждённый владельцем материал, собранный по книжкам. Своих формулировок не пиши.** Длинно — сокращай отбрасыванием, не пересказом.

**Шаг 4. Экран занятия.** Пустой каркас: номер недели, блок, к которому она относится, и места под «что было на уроке» и «домашнее задание». Формат материалов не изобретай — его придумают позже.

**Шаг 5. Гейт текста — напиши его сам, он и есть главная защита задания.** Скрипт в зоне, который берёт собранные страницы, вынимает весь видимый текст и проверяет, что каждая строка происходит **либо из `god.json`, либо из короткого белого списка интерфейсных слов** (условно «четверть», «неделя», «кружок», «контрольная», «демо-версия»). Белый список лежит отдельным полем в данных и **перечисляется в отчёте целиком, слово в слово** — это единственное место, где владелец увидит, что именно на сайте написано не его рукой. Строка не из данных и не из списка — красный.

**Шаг 6. Посмотреть глазами.** Ты работаешь host-side: отрендери экран года на ширине 1440 и на 390, открой картинки и **посмотри**. В отчёте назови три вещи, которые видно первым делом. Если первым в глаза бросается контроль, а не математика — задача не сделана, чини и рендери снова.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — три машинных условия и одно глазное:**

0. **Живой прогон на реальном объекте репозитория, а не на фикстуре:** сборка гоняется на настоящем `diskmat-57/_sajt/god.json` со всеми одиннадцатью блоками года, и её вывод уходит в отчёт дословно. Зелёная фикстура при красном живом прогоне — не сдача.
1. Сборка одной командой из зоны, `rc=0`, и она **печатает охват числами**: блоков X из Y, недель X из Y, страниц занятий X из Y. **Y считается по `god.json` ДО работы той же командой, а не берётся из этого захода** — на 2026-08-20 блоков там 11 (`python3 -c "import json;print(sum(1 for c in json.load(open('diskmat-57/_sajt/god.json'))['chetverti'] for e in c['elementy'] if e['tip']=='blok'))"`), но число могло измениться, считай сам. `X = 0` при непустом источнике — красный, а не зелёный.
2. Гейт текста печатает `строк проверено N, сирот 0`. Хоть одна сирота — красный. **Гейт честно печатает, чего он НЕ проверяет** (например, текст, собранный в JS на лету, если такой есть).
3. Битых внутренних ссылок 0, считается командой: с года на блок, с блока на занятие, с занятия обратно.
4. Глазное: две картинки рендера лежат по названным в отчёте путям, и в отчёте есть строка «первым в глаза бросается: …». Это условие проваливается словами, а не скриптом, и потому идёт последним — но без него первые три ничего не гарантируют.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР — нужен, операция лоссовая

Ты сжимаешь карту содержания блоков (45 пунктов «тема → что внутри») до коротких опорных точек. Это потеря по построению, и судить её сам ты не можешь: сокращал ты.

**Свежий субагент, ДРУГОЙ метод.** Он читает `god.json` (оригинал) и собранные страницы блоков, и отвечает на два вопроса: (1) какие темы из `karta` не доехали на страницу вовсе — список; (2) появилось ли на странице хоть одно утверждение, которого в `god.json` нет — список. **Оба списка обязаны быть пустыми.** Охват внутри вердикта: «проверено X блоков из Y».

Второй верификатор — **на текст, и он обязан НЕ ВИДЕТЬ `god.json`**: читает только собранные страницы и отвечает, понятно ли из них устройство года человеку, который курса не знает. Автор страницы достраивает недостающее из головы, поэтому проверять понятность самому бесполезно.

🔴 Ответ каждого субагента кончается строкой «выдано N позиций из M найденных». Нет строки — ответ усечён, перезапроси.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- diskmat-57/_sajt                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- diskmat-57/_sajt   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `diskmat-57/_sajt`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone diskmat-57/_sajt` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** ⚠ **Проверено при сборке захода: у проекта `diskmat-57/` карты-индекса нет вовсе** — `korni.карта_для('diskmat-57/…')` возвращает `None`, а дверь `register_doc.py` судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/` и на пути проекта отказывает. Заводить `.md` в своей зоне тебе не требуется; если всё же завёл — регистрировать некуда, так и напиши строкой. Не изобретай индекс и не клади документ в чужой корень ради регистрации.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: сначала все коммиты → потом все ветки → потом вывоз.**
Обратный порядок не работает технически: влитие отказывает на грязном дереве, вывоз — на невлитом.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВСЕ ВЕТКИ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged <основная>`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main` там,
где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**3 · ВЫВОЗ.** `git --no-optional-locks log --oneline @{u}.. | wc -l` → 0 по каждому репозиторию.
Ненулевое означает, что работа существует только на этом диске.

**4 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Три числа по каждому репозиторию — вне git · невлитых ·
невывезенных — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число, оставленное без
объяснения, приёмка читает как несделанную работу: она гоняет те же команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.


### Рабочая папка захода заводится от ветки, в которой объявленной зоны нет

`git_zona.py worktree add … --branch …` отпочковывает ветку от основной и разворачивает её в новую папку. Если Cowork готовил материалы проекта в ГЛАВНОЙ папке, его `git add` остался индексом: из песочницы он в `.git` не пишет (`GIT-disciplina §2`). Индекс главной папки в новый worktree не переносится — и папка приезжает без зоны, хотя `git ls-files` в главной папке показывает все её файлы как отслеживаемые. Снаружи это выглядит как «зоны не существует», изнутри — как «всё на месте».

ЦЕНА: заход `kod_sajt-kursa.md` (2026-08-20) оказался технически неисполним первым же ходом: контракт зоны велит работать в `../materials-wt/sajt-kursa`, а `diskmat-57/_sajt` там не было вовсе. Ушло четыре хода на диагностику (`ls` → `ls-files` → `ls-tree HEAD` → `worktree list`), прежде чем стало видно, что файлы застейджены и не закоммичены; зона переносилась копированием руками. Ловушка тихая: `git ls-files` отвечает из индекса и подтверждает, что файлы «в git», а `ls-tree HEAD` — что их нет. Исполнитель, который проверит только первой командой, соберёт пустоту и не поймёт, почему. Чинится в самом `worktree add`: проверять, что путь зоны существует в HEAD заводимой ветки, и краснеть строкой «зона объявлена, но её нет в HEAD: она застейджена в главной папке и не закоммичена» — иначе следующий заход по любому подготовленному Cowork'ом проекту наступит сюда же.

### `--window-size` у headless Chrome не задаёт viewport, и рендер врёт про мобильную вёрстку

`chrome --headless --screenshot --window-size=390,844` даёт картинку шириной 390, но страница внутри верстается по ДРУГОЙ ширине: медиазапросы срабатывают не те, а результат обрезается по краю. Картинка при этом выглядит абсолютно правдоподобно — как страница с горизонтальным переполнением.

ЦЕНА: в этом заходе рендер на 390 показал переполнение и обрез правого края, которого на настоящем viewport нет (`document.scrollWidth == clientWidth == 390`). Начал чинить несуществующий баг вёрстки и остановился только потому, что решил перемерить программно. Полтора хода впустую; при менее осторожном исполнителе правка «под ложный баг» уехала бы в сборку и испортила настоящую раскладку. Правило: глазная проверка вёрстки (шаг «посмотреть глазами» есть почти в каждом заходе на HTML) гонится через playwright с явным `viewport`, а не через `--window-size`; рабочий образец на 20 строк — `diskmat-57/_sajt/_render.py`.

### Гейт, который не проверили на подсадке, не проверили вообще

Оба написанных здесь гейта (текста и ссылок) с первого прогона были зелёными. Зелёный гейт неотличим от гейта, который ничего не смотрит: оба печатают «0».

ЦЕНА: прямой цены в этом заходе нет — я подсадил в копию `dist` заведомую сироту («Этот курс учит думать, а не считать») и битую ссылку, получил `rc=1` на обоих и только после этого поверил своим `сирот 0`. Но цена измерена соседним классом ошибок в этом же репозитории: правило `KONSTITUCIYA §11` («правило заводится вместе с тем, что покраснеет») существует ровно потому, что зелень без доказанной способности краснеть уже стоила репозиторию пяти неверных цифр. Предлагаю в канон: заход, который ПИШЕТ гейт, обязан отчитаться не только зелёным прогоном, но и красным — на нарочно испорченной копии, с указанием, что именно подсаживалось.

## ПЛАН — (заполняет исполнитель)

> Модель Opus 5. Ниже — план ДО действий; всё, что сделано до него, — только чтение и гит-контур §0.1.

### Р0. Оспариваю предпосылки входа (Карпатов: ложную предпосылку назвать ДО работы)

**П1. Зоны `diskmat-57/_sajt` нет ни в одной ветке — она только в ИНДЕКСЕ главной папки.**
Проверено: `git ls-tree HEAD --name-only` в worktree — каталога `diskmat-57` нет вовсе; `git ls-files diskmat-57/_sajt | wc -l` в главной папке — 6. То есть файлы застейджены Cowork'ом и не закоммичены. Следствие: worktree, заведённый по §0.1, приезжает БЕЗ зоны, и заход технически неисполним «как написано».
**Что делаю:** переношу шесть файлов зоны (`god.json`, `build.py`, `index.html`, `rasskaz.json`, `sajt.json`, `izvlech_iz_infografiki.py`) копированием из главной папки в свою — чтение вне зоны, запись в зону. Байт-в-байт (`cmp` на `god.json` — identical). Коммитит их моя ветка, и этим зона впервые доезжает в git.

**П2. ТЗ §3 требует дату на клетке недели и в списке занятий блока; в `god.json` подельных дат НЕТ.**
Есть только границы четвертей (`chetverti[].daty`, например «1 сентября — 25 октября»), а `snoska` прямо говорит, что и они не сверены («Даты по календарю Минпросвещения на 2026/2027, сверить с графиком 57-й»). Раскладывать 33 недели по календарю значит сочинить 33 факта, которых владелец не утверждал, — это ровно то, что запрещает ТЗ §4 и что покраснеет на гейте текста.
**Что делаю:** следую финализированному на интервью п.2 («на клетке стоит НОМЕР недели, дат пока нет»); границы четверти показываю в шапке четверти — они из данных. Расхождение с ТЗ выношу в `## ВОПРОСЫ` пунктом очереди, а не решаю молча.

**П3. Поле `opornye` в данных — это опорные ЗАДАЧИ, а не «опорные точки мини-курса» из ТЗ §3.**
`opornye` — одна строка примеров («уксусная эссенция 70% и разбавление · …»). «Опорные точки: 2–5 самых важных фактов, техник и идей» — это `karta` (3–5 пунктов «тема → что внутри», всего 45 на 11 блоков), как и говорит §2 шаг 3 захода.
**Что делаю:** опорные точки строю из `karta`; опорные задачи показываю отдельной тихой полосой на странице блока, разрезав строку по `·` на короткие фразы. Это владельческий текст и самая конкретная математика на странице — она работает на «математика заметнее контроля». Судейское решение, называю его здесь явно.

**П4. Критерий готовности п.1 требует «страниц занятий X из Y» — Y считаю сам.**
Считаю командой до работы, число в заход не вписываю.

### Р1. Что собираю

`build.py` переписывается заново (забракованную версию не переиспользую: там узкая колонка и ассистентская проза из `rasskaz.json` — я её видел в браузере). Одна команда `python3 build.py` из зоны собирает всё дерево:

- `dist/index.html` — экран года,
- `dist/blok-<slug>.html` — 11 страниц блоков + 4 страницы кружка,
- `dist/nedelya-<NN>.html` — 33 страницы занятий (пустые каркасы).

**Нарезка на недели (шаг 1).** Поток четверти = элементы `tip ∈ {blok, kontrolnaya}` по порядку, каждый со своими `chasy` уроками. Неделя = два подряд идущих урока. `kruzhok` в поток НЕ входит (это параллельная дорожка), `vne-setki` тоже. Счёт, проверенный командой: 16+16+20+14 = 66 уроков = 33 недели; часы четверти чётны, поэтому неделя не переползает через границу четверти. Неделя, поделённая между двумя элементами, — законна и рисуется расщеплённой клеткой (проверка: в I четверти таких две — недели 2 и 5).

**Экран года (шаг 2).** Широкая раскладка во весь экран, четыре четверти сверху вниз. Внутри четверти — блоки прямоугольниками в ряд, ширина пропорциональна числу недель. Внутри блока — клетки недель с номером. Цвет по `domen` (A/K/V/D/O); `na-styke: true` (Двойной счёт, Кодировка) — двухцветная заливка, нарочная. Отдельно стоящие контрольные — узкой низкой полоской между блоками, приглушённо. Тумблер «основной курс / кружок» — второй календарь по тем же 33 неделям, свои цвета. Плашка «демо-версия» сверху.

**Экран блока (шаг 3).** Опорные точки нумерованным списком из `karta` (`tema` — заголовок пункта, `chto` разрезан по `·` на короткие фразы). Опорные задачи — тихой полосой. Список недель блока со ссылками. `istochniki` и `sluzhebnoe` на сайт НЕ идут (ТЗ §4).

**Экран занятия (шаг 4).** Пустой каркас: номер недели, четверть, блок и ссылка на него, места под «что было на уроке» и «домашнее задание», заглушка из `sajt.json`. Формат материалов не изобретаю.

**Формат.** Палитра и типографика — из образца `13_МАКЕТ-страница-темы-v3.html`, смотрел в браузере на 1440: бумага `#f6f1e7`, карточка `#fcfaf5`, чернила `#2b2724`, акцент-терракота `#b3503c`, Georgia для содержания, sans для интерфейса, серые прописные метки, ноль боксов. Календарь придумывается заново — его в образце нет.

### Р2. Гейт текста (шаг 5)

Свой скрипт в зоне `gejt_teksta.py`: вынимает весь видимый текст из собранных страниц (снимает теги, `<script>`/`<style>` целиком, `title`), режет на строки и проверяет, что каждая происходит либо из `god.json`/`sajt.json`, либо из белого списка интерфейсных слов. Белый список — новым полем `interfejs` в `god.json` (как требует §2 шаг 5), в отчёте перечисляется целиком слово в слово. Гейт печатает `строк проверено N, сирот 0` и **честно печатает, чего не проверяет.**

### Р3. Проверки (критерий готовности)

0. живой прогон на настоящем `god.json` со всеми 11 блоками, вывод в отчёт дословно;
1. `build.py` печатает охват числами, Y считается из `god.json` той же командой;
2. гейт текста → `сирот 0`;
3. свой `check_ssylki.py`: битых внутренних ссылок 0, три направления (год→блок, блок→занятие, занятие→блок/год);
4. глазное: рендер экрана года на 1440 и на 390, смотрю картинки, называю три вещи, которые видно первым делом.

### Р4. Верификаторы (§3) — два свежих субагента

- **В1** (видит `god.json` + собранные страницы блоков, метод другой — сверка списков): какие `tema` из `karta` не доехали; какое утверждение на странице отсутствует в `god.json`. Оба списка обязаны быть пусты, охват «X блоков из Y».
- **В2** (НЕ видит `god.json`, только собранные страницы): понятно ли устройство года человеку, курса не знающему.
- Ответ каждого кончается строкой «выдано N позиций из M найденных»; нет строки — перезапрашиваю.

### Р5. Порядок ходов

1. ✅ гит-контур §0.1 субагентом (запущен первым ходом), снимок входа снят;
2. ✅ чтение: ТЗ → `god.json` → забракованные `build.py`/`index.html` → макеты в браузере на 1440;
3. нарезка недель + белый список в данные → коммит «данные»;
4. `build.py` (три экрана) → коммит «сборщик»;
5. `gejt_teksta.py` + `check_ssylki.py`, прогон, починка → коммит «гейты»;
6. рендер 1440 и 390, глазами, починка → коммит «вид»;
7. верификаторы В1 и В2, починка по их спискам;
8. полная гит-гигиена (три числа) → `## ОТЧЁТ`.

**Механику вёрстки (пункт 6 — рендер и вычитка картинок) увожу на субагента, решения по дизайну оставляю себе.** Субагентов ≤5.


## ВОПРОСЫ — (заполняет исполнитель)

Пункты очереди — по формату из шапки секции.

```
1. ТЗ §3 требует дату на клетке недели и в списке занятий блока, а в god.json
   подельных дат НЕТ — только границы четвертей, и сама snoska говорит, что и они
   не сверены («Даты по календарю Минпросвещения на 2026/2027, сверить с графиком
   57-й»). Разложить 33 недели по календарю значило бы сочинить 33 факта, которых
   владелец не утверждал. Сделано по финализированному на интервью п.2: на клетке
   НОМЕР недели, границы четверти — в её подписи. Чтобы даты появились, в god.json
   должно приехать поле с датой недели (или дата первого понедельника + пропуски
   каникул), сверенное с графиком 57-й школы.
   ДОМ: владелец
   ДОСТАВЛЕНО: приёмка 2026-08-20 — «оставить номера», решение владельца в SESSIYA.md

2. ТЗ §3 требует, чтобы блок «на стыке двух разделов» красился МНОГОЦВЕТНО, но
   второго раздела в данных нет: у элемента одно поле domen и булев флаг na-styke.
   На стыке стоят два блока — «Двойной счёт» (domen D) и «Кодировка» (domen K).
   Первая редакция красила такой блок цветом СОСЕДНЕГО блока четверти; я это снял,
   потому что это утверждение о разделе, которого владелец не давал. Сейчас блок
   двухтонен в двух оттенках СВОЕГО раздела плюс ярлык «на стыке» — нарочно, но
   без выдуманного факта. Чтобы стало по ТЗ, в god.json нужен второй раздел,
   например поле "domen-2".
   ДОМ: владелец
   ДОСТАВЛЕНО: приёмка 2026-08-20 — «назвать второй раздел», решение владельца в SESSIYA.md

3. Верификатор понятности (не видевший данных) споткнулся ровно там, где нужен
   текст, которого в god.json нет: (а) почему страница занятия пуста и когда
   заполнится; (б) что означает плашка «демо-версия». ТЗ §3 сам даёт формулировку
   для (б) — «программа ниже — план, заполняться будет по ходу года, занятие за
   занятием», — но её нет в данных, а интервью п.7 запрещает брать текст откуда-то
   ещё. Для (а) готовая фраза лежит в diskmat-57/_sajt/sajt.json
   ("materialy_zaglushka"), и я её НЕ взял: sajt.json происходит из забракованной
   версии, авторство фразы не подтверждено, а весь смысл этого сайта — что на нём
   нет ни строки, которой владелец не писал. Обе фразы закрываются одним ходом:
   перенести их в god.json, если они владельческие.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет — фразы владелец ещё не написал, вопрос открыт

4. sajt.json на сайт не выводится вовсе: titul дублирует kurs.nazvanie, а god,
   shkola, podval и materialy_zaglushka — второй источник истины, что противоречит
   ТЗ §5 «один файл данных на весь сайт». Решить, живёт ли он дальше: либо его
   поля переезжают в god.json, либо файл удаляется как остаток забракованной
   версии. Я его не трогал — удаление необратимо и в задание не входило.
   ДОМ: владелец
   ДОСТАВЛЕНО: приёмка 2026-08-20 — «удалить всё трое», решение владельца в SESSIYA.md

5. Забракованные index.html и rasskaz.json остались лежать в зоне рядом с новой
   сборкой (dist/index.html). Заход прямо велел rasskaz.json не удалять, про
   index.html не сказал ничего, и я его не удалял. Соседство путает: в корне зоны
   лежит старая страница, которую легко открыть вместо новой.
   ДОМ: владелец
   ДОСТАВЛЕНО: приёмка 2026-08-20 — «удалить всё трое», решение владельца в SESSIYA.md

6. Положение тумблера «основной курс / кружок» не переживает переход на страницу
   блока и обратно: состояние держится на :checked у радиокнопки, а JS на страницах
   запрещён собственным гейтом текста (текст, рождённый в JS, гейту не виден, и
   тегов <script> на страницах ровно 0 — это проверяется). Починка без JS возможна
   через :target и якорь в каждой ссылке «к году», но это удваивает разметку
   календаря; делать это без спроса я не стал.
   ДОМ: владелец
   ДОСТАВЛЕНО: приёмка 2026-08-20 — «разрешить JS», решение владельца в SESSIYA.md

7. Зоны diskmat-57/_sajt не было ни в одной ветке — она лежала ТОЛЬКО в индексе
   главной папки (застейджена, не закоммичена). Из-за этого worktree захода
   приехал без зоны, и заход был технически неисполним «как написано». Я перенёс
   шесть файлов копированием и закоммитил их на своей ветке — зона впервые доехала
   в git. Но в ИНДЕКСЕ главной папки лежит прежняя, доработочная версия тех же
   путей: при влитии моей ветки в arka/mat-kostyak это даст пересечение «путь
   одновременно грязный и участвует в слиянии», на котором vlit-v-osnovnuyu
   отказывает по построению. Приёмке это надо развести ДО влития.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

8. Гейт §1 про порядок терминов (check_termin.py) к этой работе неприменим:
   содержательного текста я не писал ни строки — весь текст страниц происходит из
   god.json или из белого списка. Пункт назван, чтобы молчание не читалось как
   «забыл».
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
```

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(снят ПЕРВЫМ ходом, до всякой работы; вывод дословный)*
```
$ git --no-optional-locks branch --no-merged arka/mat-kostyak     # невлитые
+ zahod/chistka-hvostov
$ git --no-optional-locks branch --no-merged | grep -c 'zahod/'
1
$ git --no-optional-locks status --porcelain | wc -l      # рабочая папка захода
0
$ git --no-optional-locks log --oneline @{u}.. | wc -l    # рабочая папка захода
fatal: no upstream configured for branch 'zahod/sajt-kursa-diskmat-57'

$ python3 _generator/tools/schet_nezakrytogo.py diskmat-57/2026-08-18_logika-goda-7
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : 39
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0
```
Заявок на входе — 7 (снято субагентом контура командой `git_zona.py zayavki`, его строки ниже).
Невлитых веток на входе — **1** (`zahod/chistka-hvostov`), ровно как в снимке сборки захода: не выросло, к сайту отношения не имеет, не тронута.

**ЧТО СДЕЛАНО** *(шесть строк субагента гит-контура, вставлены ДОСЛОВНО)*

1. заявок было 7, закрыто 1 (2026-08-18T0216-2026-08-13-skill-mat-teksta — снесены шесть пробных заходов сборщика kod_prZ/kod_pr_app/kod_pr_terminal/kod_probaX/Y/Z и их шесть строк из `_studio/docs/KARTA.md`, коммит d8e359c), застряло 6: 2026-08-18T0216-git-17-18-08-disciplina-materials (половина materials исполнена коммитом 8368c17, половина disciplina — чужой репозиторий, вне рабочей папки контура, полного `git_zona.py` там нет), 2026-08-19T2204-otkaz-diskmat-57, 2026-08-19T2206-diskmat-57-43-2026-08-20, 2026-08-19T2336-2026-08-19t2206-diskmat-57-43 (все три — зона живого захода diskmat-57, контуру коммитить содержательную работу запрещено), 2026-08-20T0010-otkaz-generator-tools-bootstrap-zahod-py, 2026-08-20T0013-generator-tools-bootstrap-zahod-py-pravki (авторская правка чужой арки 2026-08-19_vynos-fabriki-arok, не индекс и не автолог); сторож молчит — заявок с 3+ отказами подряд нет.
2. влито в основную 0 веток из 0 названных — этот заход чужих веток не вливает; основная ветка (снято `osnovnaya_vetka`/doctor в главной папке) — `arka/mat-kostyak`; невлитых веток фактически 1 (`git branch --no-merged arka/mat-kostyak`): `zahod/chistka-hvostov`, к сайту отношения не имеет, не тронута.
3. хвост Cowork ЗАБРАН тремя коммитами: e469880 — `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, `_studio/zhurnal/_INFRA-git/VERDIKTY.md`; 8368c17 — `_studio/zhurnal/2026-08-13_skill-mat-teksta/UROKI-FABRIKE.md`; d8e359c — `_studio/docs/KARTA.md` + удаление шести пробных заходов; `_studio/docs/KARTA.md` иных правок не имел. Вне git осталось 50 путей (`git_zona.py check`): зона захода diskmat-57 — 45 путей (`diskmat-57/_sajt/{build.py,god.json,index.html,izvlech_iz_infografiki.py,rasskaz.json,sajt.json}`, `diskmat-57/2026-08-18_logika-goda-7/*` включая новые `TZ-sajt.md` и `kod_sajt-kursa.md`, `diskmat-57/docs/*`, `diskmat-57/_istochnik/handoff-2026-08-18/*`, `diskmat-57/START-HERE.md`) и не мои 5: `.gitignore`, `README.md`, `_generator/tools/bootstrap_zahod.py`, `_fond/biblioteka/README.md`, `_fond/chuzhoe/README.md`; все три коммита только на этой машине — origin не тронут, вывоз в задании не значился.
4. погашено ноль: ветка `zahod/sajt-kursa-diskmat-57` и папка `materials-wt/sajt-kursa` НЕ погашены по заданию (влитие — ход приёмки), `poteri --branch zahod/sajt-kursa-diskmat-57` rc=0 «потерь нет»; `zahod/chistka-hvostov` НЕ погашена — `poteri` показывает 5 файлов, которых нет нигде больше (`_generator/tools/git_zona.py`, `bootstrap_zahod.py`, две фикстуры PROGNAT.sh, `_studio/docs/kak-delat/DOLG-ISKLYUCHENIA.md`) и живую рабочую папку `materials-wt/chistka-hvostov`; чужие ветки не тронуты вовсе.
5. необратимое: удалены шесть файлов `_studio/zhurnal/2026-08-13_skill-mat-teksta/kod_prZ.md`, `kod_pr_app.md`, `kod_pr_terminal.md`, `kod_probaX.md`, `kod_probaY.md`, `kod_probaZ.md` и шесть строк их регистрации в `_studio/docs/KARTA.md` · главная папка репозитория materials · восстанавливается из git: `git show d8e359c^:<путь>` (все шесть были в git до удаления).

выдано 5 позиций из 5 найденных

**ДОБИВКА ВЫВОЗА** *(тот же субагент, отдельным ходом: вывоз в его объявленных правах, не в моих)*

1. `python3 _generator/tools/git_zona.py vyvezti --yes` — вывод: «вывоз: 1 ветк(а/и/) — суммарно 3 коммит(ов) только на этой машине · arka/mat-kostyak: впереди origin/arka/mat-kostyak на 3 · ✅ arch push прошёл · ✅ Вывезено — локально-только коммитов не осталось ни на одной из перечисленных веток»; ветка `zahod/sajt-kursa-diskmat-57` и папка `materials-wt/sajt-kursa` не тронуты (без `--zahody`).
2. контрольный замер после: `git --no-optional-locks log --oneline @{u}.. | wc -l` в /Users/ivanyakovlev/Documents/GitHub/materials → 0

выдано 2 позиций из 2 найденных

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`

Поимённо, что осталось и почему непроходимо правами этого захода:

- **6 застрявших заявок** (id выше). Три — на зону живого захода `diskmat-57`: контуру запрещено коммитить содержательную работу, а мне — коммитить что-либо вне своей зоны `diskmat-57/_sajt`. Одна — на репозиторий `disciplina`: это ДРУГОЙ git-репозиторий, `git_zona.py` в нём нет. Две — авторская правка чужой арки `2026-08-19_vynos-fabriki-arok`, не индекс и не автолог: нужно решение владельца, чья это работа.
- **50 путей вне git в главной папке.** 45 — работа аналитика по арке `2026-08-18_logika-goda-7` (в том числе `TZ-sajt.md` и сам файл-заход, который забирает приёмка по контракту зоны) плюс застейдженная прежняя версия моей зоны; 5 — чужое (`.gitignore`, `README.md`, `_generator/tools/bootstrap_zahod.py`, два README в `_fond/`). Ни то, ни другое не мои пути.
- **`zahod/chistka-hvostov` невлита.** Ровно как на входе; у неё живая рабочая папка `materials-wt/chistka-hvostov` и 5 файлов, которых нет больше нигде — гасить нельзя, вливать этот заход не должен.
- **39 долгов СТАТУС: ЖИВ** — счёт глобальный по репозиторию, к этой записи не размечен; приоритет владельца (инциденты важнее долгов) соблюдён: инцидентов без вердикта 0.

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-kursa/diskmat-57/_sajt/dist/index.html` — открывать браузером, двойным щелчком; это экран года, с него кликом уходят на блок и на занятие.
Рендеры для быстрого взгляда без браузера:
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-kursa/diskmat-57/_sajt/_render/god-1440.png`
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-kursa/diskmat-57/_sajt/_render/god-kruzhok.png`
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-kursa/diskmat-57/_sajt/_render/god-390.png`
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-kursa/diskmat-57/_sajt/_render/blok-1440.png`
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-kursa/diskmat-57/_sajt/_render/blok-390.png`
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-kursa/diskmat-57/_sajt/_render/nedelya-1440.png`

**КОММИТ:** `1562cb1` — «diskmat-57/_sajt: вид доведён по двум верификаторам…» · `git_zona.py check --zone diskmat-57/_sajt` → ✅ «работа доехала в git, вне git ничего нет»
Пять коммитов по вехам: `bcc0e77` данные · `844a4f2` сборщик · `12e217c` гейты · `3857795` вид · `1562cb1` доводка по верификаторам.

---

### ЧТО СДЕЛАНО И ЗАЧЕМ

**Сломанный вход, найден первым ходом.** Зоны `diskmat-57/_sajt` не было ни в одной ветке: шесть её файлов лежали ТОЛЬКО в индексе главной папки, застейдженные и незакоммиченные (`git ls-tree HEAD` в worktree каталога `diskmat-57` не знает вовсе). Worktree захода приехал без зоны — заход был технически неисполним «как написано». Перенёс шесть файлов копированием из главной папки в свою (чтение вне зоны, запись в зону), `cmp` на `god.json` — байт в байт. Этим коммитом зона впервые доехала в git. Подробности и последствие для влития — `## ВОПРОСЫ` п. 7.

**Шаг 1, нарезка на недели.** Поток четверти = элементы `tip ∈ {blok, kontrolnaya}` по порядку; `kruzhok` — параллельная дорожка (третий урок), `vne-setki` вне сетки. Неделя = два подряд идущих урока потока. Счёт проверен командой, а не взят из захода: 16+16+20+14 = 66 уроков = 33 недели, часы каждой четверти чётны, поэтому неделя не переползает границу четверти. Сборщик проверяет это сам и падает с внятной строкой, если сумма разойдётся, — данные не подгоняются. **Расщеплённых недель 8**, не 7: недели 2, 5, 9, 12, 17, 20, 23, 29. Каждая нарисована двухцветной клеткой — видно, что на этой неделе одно кончается и начинается другое.

**Шаг 2, экран года.** Четыре четверти сверху вниз; подпись четверти (римская цифра, слово, даты) уехала в левый жёлоб, а «N недель» встало вплотную к пронумерованной линейке — иначе связь «эти клетки и есть недели» приходится угадывать (находка второго верификатора). Лента — сетка в УРОКАХ, 20 колонок на все четверти: уроки выровнены по вертикали, а короткая четверть честно не достаёт до правого края, и разница 8/8/10/7 недель видна глазом. Блок — крупный прямоугольник шириной ровно в свои часы, с именем, подзаголовком, разделом и часами; внутри белые волоски по границам пар — клетки недель. Отдельно стоящие контрольные — полоска высотой 24 px, прижатая к низу ленты, кеглем 8 px; четвертные ещё тише — 16 px, прозрачная, пунктиром. Тумблер «основной курс / кружок» — две радиокнопки и CSS, **ни строки JS** (это же и позволяет гейту текста видеть весь текст: `<script>` на страницах ровно 0). Плашка «демо-версия» — в шапке каждой страницы.

**Шаг 3, экран блока.** Опорные точки — нумерованный список из `karta`: `tema` заголовком пункта, `chto` разрезано на короткие фразы. Ниже — опорные задачи из `opornye`, тоже фразами. Список недель блока со ссылками. `istochniki` и `sluzhebnoe` на сайт не идут (ТЗ §4), и гейт это проверяет отдельным счётчиком.

**Шаг 4, экран занятия.** Пустой каркас: номер недели, четверть, ярлыки блока и кружка со ссылками, два пустых места — «что было на уроке» и «домашнее задание». Формат материалов не изобретал.

**Шаг 5, гейт текста.** `gejt_teksta.py` вынимает видимый текст из всех 49 страниц (текстовые узлы + атрибуты `title`/`alt`/`aria-label`/`placeholder`, `<script>` и `<style>` выброшены) и проверяет, что каждый фрагмент происходит либо из `god.json`, либо из белого списка `god.json["interfejs"]`. Дополнительно ловит `<script>`, `content:` с буквами в CSS и просачивание служебных полей. **Гейт умеет краснеть — проверено подсадкой:** в копию `index.html` вписана фраза «Этот курс учит думать, а не считать» → `сирот 1`, `rc=1`; на настоящей сборке `rc=0`.

**Шаг 6, глазами.** Смотрел рендеры на 1440 и 390 сам, плюс прогнал измеритель наездов на **17 ширинах от 320 до 1920**: наездов 0, горизонтального переполнения нет ни на одной.

**Что чинилось по ходу — настоящие дефекты, не косметика:**
- разрезание перечислений шло по голой `·` и рвало формулы: «8!, 8·7, 8·7/2, 2⁸» превращалось в три обломка. Режу только по разделителю с пробелами; проверено, что все 6 кусков с формульной точкой целы;
- `-webkit-line-clamp` внутри flex-колонки не работает (display блокифицируется) — подзаголовок наезжал на подпись раздела. Заменено обрезом по высоте, кратной строке;
- на мобильном жёлоб четверти не схлопывался и лента ужималась до 226 px;
- ярлык «на стыке» в углу отнимал у имени 126 px, и «Кодировка» лезла под него.

### КАК ПРОВЕРЯЛ

**Живой прогон на настоящем `diskmat-57/_sajt/god.json` со всеми 11 блоками, `dist` предварительно снесён. Вывод дословный:**

```
$ rm -rf dist && python3 build.py
источник         : …/materials-wt/sajt-kursa/diskmat-57/_sajt/god.json
собрано в        : …/materials-wt/sajt-kursa/diskmat-57/_sajt/dist
страница года    : 1 из 1
страниц блоков   : 11 из 11
страниц кружка   : 4 из 4
страниц занятий  : 33 из 33
недель в году    : 33 из 33, расщеплённых между двумя элементами 8
пунктов karta    : 56 из 56
файлов записано  : 49
охват полон
rc сборки = 0

$ python3 gejt_teksta.py
источник данных  : …/diskmat-57/_sajt/god.json
страниц проверено: 49
строк проверено 1103, сирот 0
тегов <script> на страницах: 0 (текста, рождённого в JS, нет только при 0)
CSS content: с буквами: 0
служебных полей данных (sluzhebnoe, snoska, istochniki, utok) на страницах: 0
rc гейта = 0

$ python3 check_ssylki.py
страниц в dist   : 49 (блоков 11 из 11, кружка 4 из 4, занятий 33 из 33)
ссылок всего     : 344
   blok → god             30
   blok → nedelya         63
   god → blok             15
   god → nedelya          77
   nedelya → blok         93
   nedelya → god          66
битых внутренних ссылок: 0
внешних ссылок   : 0
страниц, до которых нельзя дойти ни по одной ссылке: 0
rc = 0
```

**Y считается из `god.json` той же командой** (`build.py --proverit`), а не берётся из захода: блоков 11, дорожек кружка 4, недель 33, пунктов `karta` 56 (45 в блоках + 11 в кружке — заход называл 45, потому что считал только блоки).

**Критерий готовности — по пунктам:**
0. живой прогон на реальном объекте — ✅, вывод выше дословно;
1. сборка одной командой, `rc=0`, охват числами, Y посчитан командой — ✅;
2. гейт текста `строк проверено 1103, сирот 0` — ✅, и он честно печатает, чего не проверяет (смысл, а не происхождение; текст, дорисованный CSS не через `content`; SVG; внешние ресурсы — последних трёх на страницах нет, и это тоже проверено);
3. битых внутренних ссылок 0, три направления посчитаны раздельно — ✅;
4. глазное — ✅, шесть картинок по путям выше.

**Первым в глаза бросается:**
1. **строй крупных цветных прямоугольников** — одиннадцать мини-курсов, около 90 % визуальной массы страницы; читаешь их имена раньше всего остального;
2. **что год делится на четыре разной длины полосы** — III четверть заметно длиннее, IV короче всех, и это видно по тому, докуда каждая лента доходит вправо;
3. **два полосатых блока «на стыке»** — «Двойной счёт» и «Кодировка» выделяются как нарочно другие.
Контроль не бросается вообще: чтобы его найти, пришлось искать специально. Это и требовалось — в забракованной версии было наоборот.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРОВ

**В1, карта содержания** (видел `god.json` и собранные страницы, метод другой — машинная сверка множеств, `build.py` не открывал):
СПИСОК 1 (темы `karta`, не доехавшие на страницу): **пусто**.
СПИСОК 2 (утверждения на странице, которых нет в данных): **пусто**.
ОХВАТ: проверено 15 блоков из 15 (11 `blok` + 4 `kruzhok`), пунктов `karta` 56 из 56, в них 168 из 168 фрагментов `chto`, текстовых узлов 539 из 539. Сверх задания проверил перекрёстное протекание пунктов между блоками — 0 случаев.
Строка охвата на месте: «выдано 0 позиций из 0 найденных».

**В2, понятность** (не видел `god.json` и вообще никаких `.py`/`.md`/`.json` — только собранные страницы и рендеры):
ВЕРДИКТ: **да с оговорками** — таймлайн из тем, четвертей и недель читается с первого взгляда, но цена колонки, роль контроля и смысл тумблера восстанавливались арифметикой и кликами.
Строка охвата на месте: «выдано 24 позиций из 24 найденных».

**Из 24 его находок починено 9:**
подпись «N недель» переехала вплотную к линейке недель (ключ к единицам) · подпись раздела больше не дублирует имя блока («Разгон» / «разгон») · блок «на стыке» перекрашен в два тона СВОЕГО раздела, выдуманный цвет соседа снят · у кружка появились клетки недель, и параллельность дорожек стала видна · контрольная стала кликаться, как всё остальное · на узком экране контрольная больше не раздувается до массы шестичасового блока · половина недели, занятая контрольной, различима, а не «пусто» · «контрольная» и её охват на странице занятия слились в одну плашку, и «II четверть · контрольная · I четверть» перестало читаться как противоречие · подзаголовки блоков перестали пропадать в окнах 900–1100 px.

**Не починено, потому что требует текста, которого в данных нет** (п. 1, 2, 3 в `## ВОПРОСЫ`): даты недель · второй раздел у блока «на стыке» · объяснение, почему пуста страница занятия и что значит «демо-версия». Сочинять эти фразы было бы ровно тем, что запрещает ТЗ §4.
**Не починено сознательно:** сброс тумблера при возврате «к году» — без JS чинится только через `:target` и якорь в каждой ссылке, это удвоение разметки календаря (`## ВОПРОСЫ` п. 6).

**Измеренный остаток, который я не прячу:** у самого узкого блока («Игры», 3 часа) подпись раздела «дискретная математика» упирается в край в окнах примерно 1301–1360 px. Ниже 1300 раскладка становится вертикальной и вопрос снимается, выше 1360 подпись помещается. Дальше ужимать кегль вреднее, чем оставить многоточие: раздел там же читается цветом и полностью написан на странице блока.

### ЧТО НЕ ТРОГАЛ

- Ничего вне зоны `diskmat-57/_sajt`. `git show --stat` по всем пяти коммитам — только пути зоны, чужого нет.
- `god.json`: добавлено ровно одно новое поле `interfejs` (белый список, его требует §2 шаг 5). Проверено программно: если убрать это поле, файл побайтово равен исходному — данные владельца не тронуты.
- `rasskaz.json` — оставлен на месте, на сайт не выводится (в сборщике 0 упоминаний).
- `sajt.json`, забракованные `index.html` и `izvlech_iz_infografiki.py` — не тронуты, не выводятся; про их судьбу — `## ВОПРОСЫ` п. 4 и 5.
- Забракованный `build.py` переписан заново, а не подправлен: смотрел его результат в браузере — узкая колонка ассистентской прозы из `rasskaz.json`. Переиспользовать там было нечего.
- Макеты в `spetsmat/materials/дизайн-черновики/` — только читал и смотрел в браузере на 1440. Оттуда взяты палитра, типографика и грамматика подписей; дизайн календаря придуман заново, его в макетах нет.
- Чужие ветки, чужие рабочие папки, файл-заход (его коммитит приёмка).

### ВРЕМЯ ПРОГОНА И ТОКЕНЫ

Неприменимо: канал `app`, лог прогона существует только на канале `terminal`, где его кладёт `tee`.

### НЕОБРАТИМОЕ

- **Со стороны захода — необратимого нет.** Ни одного удаления, переименования, перемещения, `git reset`/`checkout` поверх несохранённого. Единственное перезаписывание — `rm -rf dist` перед живым прогоном: `dist` целиком порождается `build.py` из `god.json`, восстанавливается одной командой, и в git лежит (`1562cb1`).
- **Со стороны субагента гит-контура — было, и вот оно строкой:** удалены шесть файлов `_studio/zhurnal/2026-08-13_skill-mat-teksta/kod_prZ.md`, `kod_pr_app.md`, `kod_pr_terminal.md`, `kod_probaX.md`, `kod_probaY.md`, `kod_probaZ.md` и шесть строк их регистрации в `_studio/docs/KARTA.md` · главная папка репозитория `materials` · восстанавливается `git show d8e359c^:<путь>` (все шесть были в git до удаления). Это исполнение открытой заявки Cowork, а не моё решение.
- **Вывоз на origin:** три коммита контура (`e469880`, `8368c17`, `d8e359c`) отправлены в `origin/arka/mat-kostyak` — это требование §ГИГИЕНА п. 3 «невывезенных 0». Отменяется обычным способом, но код уже на GitHub.

### ПОЛНАЯ ГИТ-ГИГИЕНА ПОСЛЕ РАБОТЫ — три числа по каждому репозиторию

Порядок соблюдён: сначала все коммиты, потом ветки, потом вывоз.

```
репозиторий                                          вне git  невлитых  невывезено
materials-wt/sajt-kursa (рабочая папка захода)             0         2           0
materials              (главная папка)                    50         2           0

$ python3 git_zona.py check --zone diskmat-57/_sajt   # из рабочей папки захода
✅ зона diskmat-57/_sajt: работа доехала в git, вне git ничего нет.

$ git log --format= --name-only bcc0e77~1..HEAD | grep -v '^diskmat-57/_sajt' | wc -l
0                                    # чужих путей в моих пяти коммитах — ноль
$ git log --format='%s' bcc0e77~1..HEAD | grep -ci 'no-verify'
0                                    # обходов хука — ноль
```

**Что означает каждое ненулевое число:**

- **вне git 0 в рабочей папке захода** — вся моя работа доехала.
- **вне git 50 в главной папке** — не мои пути: 45 это работа аналитика по арке `2026-08-18_logika-goda-7` (включая `TZ-sajt.md` и сам файл-заход, который забирает приёмка по контракту зоны) плюс застейдженная прежняя версия зоны, и 5 чужих (`.gitignore`, `README.md`, `_generator/tools/bootstrap_zahod.py`, два README в `_fond/`). Поимённо разобраны в `## ГИГИЕНА ВХОДА`.
- **невлитых 2** — `zahod/chistka-hvostov` (была на входе, к сайту отношения не имеет, живая рабочая папка, гасить нельзя) и **моя собственная** `zahod/sajt-kursa-diskmat-57`: заход вливать её не должен, влитие есть ход приёмки, и она названа в открытой заявке `2026-08-20T0055` с инструкцией «как вливать». На входе невлитых `zahod/*` было 1 — прибавилась ровно моя, и это законно.
- **невывезено 0 в обоих.** В главной папке было 3 (коммиты субагента гит-контура) — закрыто отдельным ходом того же субагента: `vyvezti --yes`, контрольный замер после → 0. У ветки захода upstream не настроен: её вывоз, как и влитие, есть ход приёмки.

### ГЕЙТЫ ПРИЁМКИ НА ВЫХОДЕ — чтобы красное не оказалось сюрпризом

`python3 _generator/tools/priyomka.py diskmat-57/2026-08-18_logika-goda-7/kod_sajt-kursa.md` → **13 из 17 зелёных, 4 красных.** Ни один из четырёх не про качество работы, и вот кто чей:

- **Г0 «зона доехала в git» — красный, и это ВХОД, а не моя работа.** Гейт гоняет `git_zona.py check --zone diskmat-57/_sajt` из ГЛАВНОЙ папки, где шесть путей зоны застейджены Cowork'ом и не закоммичены («правлено и не закоммичено — 6»). Из рабочей папки захода тот же гейт зелёный: «✅ работа доехала в git, вне git ничего нет». Закрыть это своими правами нельзя: коммит застейдженной копии в `arka/mat-kostyak` дал бы конфликт added-by-both на всех шести файлах при влитии моей ветки. Как разводить — в заявке `2026-08-20T0055` пунктом «ЧЬЕЙ СТОРОНОЙ» и в `## ВОПРОСЫ` п. 7.
- **Г13 «фаза приёмки заполнена» — красный по построению:** раздел `## ФАЗА ПРИЁМКИ` заполняет аналитик, исполнителю туда писать нечего.
- **Г14 «ветка названа и её влитие проверено фактом» — БЫЛ красный, закрыт:** поставлена заявка `2026-08-20T0055-zahod-sajt-kursa-diskmat-57-arka` с полной инструкцией «как вливать». Гейт теперь зелёный: «долг виден и адресован».
- **Г15 «отказ git-операции разобран» — красный на ДВУХ чужих отказах,** закрывать которые этот заход не вправе: `2026-08-19T2204-otkaz-diskmat-57` — отказ на `commit --zone diskmat-57` ЦЕЛИКОМ (не `_sajt`), блокер назван в парной заявке: `check_marker.py` краснеет на архивном `diskmat-57/_istochnik/handoff-2026-08-18/00_HANDOFF.md`, красное чужое и законное; `2026-08-20T0010-otkaz-...bootstrap_zahod.py` — правка чужой арки `2026-08-19_vynos-fabriki-arok`. Закрыть первый как «разобрано» было бы неправдой: сделана только половина `_sajt`, остальные пути арки по-прежнему вне git. **Новый факт для того, кто будет его закрывать:** зона `diskmat-57/_sajt` коммитится начисто двумя ходами `add` + `commit -- <пути>` (хук судит только застейдженные пути и на архивный `00_HANDOFF.md` не смотрит) — пять моих коммитов прошли без единого `--no-verify`. То есть блокер снимается сужением pathspec, а не обходом.
- **Г10 «накопление git-долга» — красный, и гейт сам пишет, что судит РЕПОЗИТОРИЙ, а не исполнителя:** `materials` — 50 путей вне git при пороге 15. Все 50 разобраны поимённо в `## ГИГИЕНА ВХОДА`; моих среди них нет.

**Обходов хука не было ни одного.** `--no-verify` не применялся, долгов по этой строке заход не оставляет.

### ПОВТОРЯЕМОСТЬ НАХОДОК

**Повторятся на следующей единице работы — значит это заход ДО следующего прогона, а не запись в очередь:**

1. **Зона захода не в git, а только в индексе главной папки.** Повторится на КАЖДОМ следующем заходе по любому проекту, который Cowork готовил в главной папке: `bootstrap_zahod.py` заводит worktree от основной ветки, а Cowork из песочницы в `.git` не пишет, поэтому его `git add` остаётся индексом и в worktree не попадает. Сегодня это стоило первых ходов и переноса шести файлов руками; в следующий раз исполнитель может этого не заметить и собрать пустоту. Чинится в `git_zona.py worktree add`: проверять, что объявленная зона существует в HEAD новой ветки, и краснеть с внятной строкой, если нет.
2. **`--window-size` у headless Chrome не задаёт viewport.** Рендер на 390 показал ложное горизонтальное переполнение, которого на настоящем viewport нет; я едва не начал чинить несуществующий баг. Повторится у любого захода, который проверяет вёрстку картинками. Чинится тем, что рендер идёт через playwright с явным `viewport` (`_render.py` в зоне — рабочий образец).

**Не повторятся — законно уходят в очередь пунктами `## ВОПРОСЫ`:** всё, что связано с содержимым `god.json` (даты, второй раздел на стыке, судьба `sajt.json`) — это свойства одного конкретного файла данных, а не фабрики.

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 _generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — критерий выполнен по всем четырём пунктам, и проверено это перегоном на живом артефакте, а не чтением отчёта: аналитик снёс dist, пересобрал из настоящего god.json (11 блоков, 4 дорожки кружка, 33 недели, 56 пунктов karta, 49 файлов, rc=0), прогнал гейт текста (строк 1103, сирот 0) и проверку ссылок (344 ссылки, битых 0), **подсадил в собранный собранную страницу года чужую фразу и получил `сирот 1`** — гейт умеет краснеть, а не только печатать нули. Утверждение «данные владельца не тронуты» тоже перегнано: god.json без ключа interfejs равен исходному, ключей не убыло. Экран года и страница блока просмотрены глазами: год читается с одного взгляда, контроль тихий, весь текст страницы происходит из данных.

**Охват проверки аналитиком, честно:** три скрипта прогнаны целиком; из 49 собранных страниц глазами просмотрены 2 (экран года и один блок) плюс рендер кружка — **46 страниц занятий и блоков не видел никто, кроме гейтов и верификатора В1**. Белый список интерфейсных слов прочитан целиком, все 18 позиций нейтральны.

**Пять красных гейтов priyomka.py разобраны, ни один не про качество работы:**
Г0 — зона грязная в ГЛАВНОЙ папке, потому что там лежит застейдженная аналитиком прежняя версия тех же шести файлов. Из рабочей папки захода тот же гейт зелёный. Это дефект подготовки, а не исполнения (см. секцию вопросов исполнителя, п. 7, и урок фабрике).
Г3 — «артефакт не найден»: гейт ищет абсолютный путь рабочей папки захода, недоступный из песочницы, а сам файл существует и **свежее источника** (god.json 00:22 → dist/index.html 01:02, проверено командой ls). Законная часть замечания: путь артефакта положено давать **от корня репозитория**, а не абсолютным путём worktree — он протухнет при первом переносе.
Г13 — заполняется здесь, этой строкой.
Г15 — два ЧУЖИХ отказа git-операций (id полностью: 2026-08-19T2204-otkaz-diskmat-57 и 2026-08-20T0010-otkaz-generator-tools-bootstrap-zahod-py), закрыть которые заход был не вправе; первый — мой собственный, от 19 августа. Оба остаются открытыми сознательно: коммит зоны diskmat-57 целиком по-прежнему не прошёл, и закрыть отказ как «разобрано» было бы неправдой.
Г10 — долг репозитория (50 путей вне git при пороге 15), накоплен не этим заходом.

**Что заход сделал сверх задания и это правильно:** нашёл сломанный вход (зоны не было ни в одной ветке, worktree приехал пустым) и не стал делать вид, что задание исполнимо как написано; отказался красить блок «на стыке» цветом соседнего раздела, потому что второго раздела в данных нет и это было бы выдуманным фактом; не взял готовую фразу из sajt.json, потому что её авторство не подтверждено. Три отказа сочинять — ровно то, чего требовало ТЗ.

**ВЕТКА РАБОТЫ:** `zahod/sajt-kursa-diskmat-57`
Не влита в основную ветку — влитие есть ход приёмки, а из песочницы запись в .git запрещена. Названа в открытой заявке (ниже), Г14 зелёный. Перенос adopt пофайлово тоже отказал по той же причине — перенос кода к себе остаётся за владельцем или за гит-контуром следующего захода.

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**

- `2026-08-20T0055-zahod-sajt-kursa-diskmat-57-arka` — `git-operaciya` — влитие ветки захода; поставлена самим исполнителем, приёмка её подтверждает и не дублирует
- `2026-08-19T2206-diskmat-57-43-2026-08-20` — `git-operaciya` — коммит остальной зоны
- `2026-08-19T2336-2026-08-19t2206-diskmat-57-43` — `git-operaciya` — дополнение к предыдущей: ТЗ и файл-заход `diskmat-57` (ТЗ, файл-заход, доки арки), поставлены аналитиком до захода и по-прежнему открыты

*Своих новых заявок эта приёмка не ставила: влитие уже адресовано заявкой исполнителя, а коммит прочих путей арки — двумя прежними; четвёртая заявка на то же самое была бы шумом в очереди.*
