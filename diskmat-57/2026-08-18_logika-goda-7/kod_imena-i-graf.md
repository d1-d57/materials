# Канал исполнителя — imena-i-graf (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
<!-- гейт сборки заполненного: ЖДЁТ -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — чистая механика: два возврата, одна строка из списка и пятнадцать переименований по готовой таблице.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — чистая механика по готовой таблице.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/imena-i-graf.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/2026-08-18_logika-goda-7/kod_imena-i-graf.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add imena-i-graf --branch zahod/imena-i-graf, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py diskmat-57/2026-08-18_logika-goda-7`
Область: «diskmat-57/2026-08-18_logika-goda-7» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — ни одного skills/*/DOLG.md нет на диске (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 15
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 54
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 43
       живых (чинится доставкой — «дом есть»)  : 5
       к владельцу (решение за человеком)      : 11
       адрес недоступен (нет/папка/код/указат.) : 0
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 27
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `diskmat-57/2026-08-18_logika-goda-7` есть 1 незакоммиченных файлов-заходов `kod_revizia-i-storozh.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «незакоммичены решения Р119–Р120, закрытие В30, дневник и заполненная ФАЗА ПРИЁМКИ предыдущего захода — всё написано аналитиком в этой сессии. Заход забирает их нулевым коммитом: из рабочей папки они не видны.». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. Год курса описан одним `diskmat-57/_sajt/god.json`, из него собирается сайт. Предыдущий заход провёл ревизию содержания, завёл сторож отменённого и внёс недельную сетку кружка — всё принято. На приёмке вскрылись две вещи, обе решены владельцем: сторож перестарался (вычистил слово «граф» из блока «Игры», хотя ось в том же файле называется «графы»), и имена тем кружка вышли длинными. ЦЕЛЬ: вернуть вычищенное, снять лишний запрет, укоротить имена, пересобрать и опубликовать.
Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели не предусмотрен: заход идёт до опубликованного адреса.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-14) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Р64 ограничивает РЕЧЬ НА ЗАНЯТИИ, а не внутренний документ: «граф позиций» возвращается в блок Игры, слово «граф» снимается со сторожа (Р119)
2. Имена тем — максимально короткие, по возможности одно слово; подробности уезжают в описание, а не в имя (Р120)
3. Список коротких имён кружка утверждён владельцем целиком и приведён в задаче построчно
4. Сторож отменённого (Р110) остаётся на месте и должен остаться зелёным после правок

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/imena-i-graf`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/imena-i-graf`. 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 **Заводишь её САМ — ВТОРЫМ ходом, СРАЗУ ПОСЛЕ пункта 1 блока §0.1 ГИТ-КОНТУР ниже (не первым ходом захода целиком), из корня репозитория:**
  ```
  python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add imena-i-graf --branch zahod/imena-i-graf
  cd ../materials-wt/imena-i-graf
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце вливает САМ ЗАХОД, последним ходом** (решение 20.08, заход `granica-priyomki`, пересмотр решения 05.08; WARNING-блок ниже печатает тот же порядок): цепочкой коммит зоны → пуш → влитие в основную (`git_zona.py vlit-v-osnovnuyu`) → пост-проверка ИЗ ГЛАВНОЙ папки → гашение СВОЕЙ ветки. Причина пересмотра — замер 20.08: изнутри захода не видно разницы между «механизм новый в рабочей папке» и «в `main` он старый», а приёмке это невидимо по построению. Влитие УСЛОВНОЕ: красная пост-проверка = откат влития самим заходом и строка в отчёт, а не «доложу, пусть приёмка решает». Файл-заход по-прежнему НЕ коммитить — его коммитит аналитик при приёмке.
- **ЗОНА (можно менять):** `diskmat-57/_sajt/god.json` `diskmat-57/_sajt/build.py` `diskmat-57/_sajt/dist` `sayt/static/diskmat-57` `diskmat-57/docs` `diskmat-57/2026-08-18_logika-goda-7`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *(история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц1) Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/imena-i-graf/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц2)
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц3).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала САМ прогоняешь две команды самопроверки контура (пункт 1 ниже), и только ПОТОМ заводишь свою рабочую папку** — её ветка отпочковывается от основной такой, какая она есть на момент запуска: контур пуст, доносить инструмент влитием нечего.

**1. ВЕСЬ КОНТУР ПУСТ — САМОПРОВЕРКА ВМЕСТО СУБАГЕНТА.** При сборке проверены три числа контура, и все три нулевые: невлитых `zahod/*`-веток 0 (🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`); открытых заявок 0 (снимок при сборке 2026-09-14, пересчитать самому: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`); названных `--vlit` 0. Звать субагента не за чем — выполни САМ две команды и вставь их вывод в `## ОТЧЁТ` дословно:
```
git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'   # снимок при сборке 2026-09-14: 0
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/god.json diskmat-57/_sajt/build.py diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7
```
Первая вернула не 0 — НИЧЕГО чужого не вливай (свою ветку вольёшь последним ходом, см. ниже), назови число строкой в `## ОТЧЁТ` и работай дальше. Вторая красная — сначала приведи в порядок свою зону.

Если при следующей сборке хоть одно из трёх чисел окажется ненулевым, генератор сам вернёт сюда задание субагенту гит-контура — печатает его дверь `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu`; звать его в этом заходе не надо.

🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- задеплоить: сайт курса на GitHub Pages по процедуре из kod_deplo-sajta.md §Часть Б; адрес назвать в отчёте

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/imena-i-graf`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add diskmat-57/_sajt/god.json diskmat-57/_sajt/build.py diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы. Проект сверх них не изучай:
  1. `diskmat-57/_sajt/god.json` — рабочий файл.
  2. `diskmat-57/_sajt/build.py` — список запретов сторожа, около строки 927.
  3. `diskmat-57/2026-08-18_logika-goda-7/kod_deplo-sajta.md`, раздел «Часть Б — публикация на GitHub Pages».
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц7)
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц8)

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц9). Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
🔴 **НУЛЕВОЙ ХОД — ГИГИЕНА, В ГЛАВНОЙ ПАПКЕ, ДО ЗАВЕДЕНИЯ WORKTREE.** В главной папке лежит незакоммиченной работа аналитика: `diskmat-57/docs/RESHENIYA.md` (решения Р119–Р120), `OTKRYTYE-VOPROSY.md` (закрыт В30), `SESSIYA.md`, и файл-заход `kod_revizia-i-storozh.md` с заполненной `## ФАЗА ПРИЁМКИ`. Забери их одним коммитом гигиены из главной папки, между пунктом 1 и пунктом 2 блока §0.1 — из свежего worktree они не видны. Содержание НЕ правь, только коммить.

### Часть А — вернуть «граф позиций» и снять «граф» со сторожа

Предыдущий заход, собирая список отмен, добавил в сторож запрет на слово «граф» по Р64 («рисуем, но слова не произносим») и вычистил его из блока «Игры». Формально верно, по существу — перечистка: **ось в том же файле называется `"G": "графы"` и подписана на сайте под каждым графовым блоком.** Владелец определил область действия Р64 (решение Р119): **слово не произносится ДЕТЯМ НА ЗАНЯТИИ, а во внутренних документах программы называется своим именем.**

**А1. В блоке `Игры` (IV четверть) вернуть слово «граф», как было до ревизии:**

- `podzag`: `стратегии на симметрии счёта · разметка позиций · выигрышные и проигрышные` → **`стратегии на симметрии счёта · граф позиций · выигрышные и проигрышные`**
- карта, пункт `анализ с конца`, поле `chto`: `разметка позиций: вершины — позиции, стрелки — ходы · …` → **`граф позиций: вершины — позиции, стрелки — ходы · …`** (остаток строки не трогать)

Сверься с прежней редакцией, чтобы вернуть ровно то, что было: `git show 58b51eb2:diskmat-57/_sajt/god.json`.

**А2. Из списка запретов сторожа в `build.py` (около строки 927) убрать строку с `граф` / Р64.** Остальные запреты не трогать. После этого сторож обязан остаться ЗЕЛЁНЫМ на живом файле — проверь и вставь вывод в отчёт.

🔴 **Другие следы Р64 не трогай.** Если найдёшь слово «граф» ещё где-то в блоках — оставь и назови в `## ВОПРОСЫ`. Р119 возвращает то, что вычистил предыдущий заход, а не открывает кампанию по расстановке слова «граф» везде.

### Часть Б — короткие имена тем кружка

**Имена тем — максимально короткие, по возможности одно слово; подробности живут в описании темы (`chto`), а не в имени** (Р120). Список утверждён владельцем целиком, менять его по своему вкусу нельзя:

| было (`imya`) | стало |
|---|---|
| логика со звёздочкой | `Логика` |
| конструкции: мнимые противоречия | `Конструкции` |
| раскраски и замощения | `Раскраски` |
| размен монет: делимость со звёздочкой | `Размен монет` |
| оценка, пример, принцип крайнего | `Оценка и пример` |
| инвариант и подсчёт двумя способами | `Инвариант` |
| изоморфизм задач, перебор со звёздочкой | `Изоморфизм` |
| взвешивания и мудрецы | `Взвешивания` |
| арифметика и вероятность со звёздочкой | `Арифметика` |
| шахматы и игры | `Игры` |
| игры: процессы и полуинвариант | `Процессы` |
| олимпиада, тренировочная перед Матпраздником | `Олимпиада` |

Уже короткие и не меняются: `Дирихле`, `Разнобой`, `Аукцион`, а также «разнобой», «олимпиада», «аукцион», «заключительная олимпиада» → последняя тоже становится `Олимпиада`.

🔴 **Содержание тем при переименовании НЕ ТЕРЯЕТСЯ.** Всё, что ушло из имени, обязано оказаться в `chto` той же темы, если его там ещё нет: «мнимые противоречия», «замощения», «принцип крайнего», «подсчёт двумя способами», «перебор со звёздочкой», «мудрецы», «вероятность со звёздочкой», «шахматы», «полуинвариант», «тренировочная перед Матпраздником». **Потерять содержание, укорачивая имя, — брак**; это ровно та ошибка, от которой предостерегает проверка на перечистку ниже.

🔴 **Три темы получают одинаковое имя `Олимпиада`** (недели 14, 25, 33) — это нормально, они различаются четвертью и описанием. Не выдумывай им разных имён.

### Часть В — пересборка и публикация

`python3 build.py` из `diskmat-57/_sajt/`, затем публикация по якорю 3. Адрес назвать в отчёте строкой.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).**

Это ЖИВОЙ ПРОГОН на реальном объекте репозитория, а не фикстура.
Это ЖИВОЙ ПРОГОН на реальном объекте репозитория, а не фикстура: команды читают живой `diskmat-57/_sajt/god.json` и живой `diskmat-57/_sajt/build.py`.

1. `python3 diskmat-57/_sajt/build.py --proverit` → `rc=0`, сторож внутри него ЗЕЛЁНЫЙ.
2. Проверка, вывод дословно в `## ОТЧЁТ`:

```
python3 - <<'GATE'
import json,sys
d=json.load(open('diskmat-57/_sajt/god.json'))
igry=[e for q in d['chetverti'] for e in q['elementy'] if e.get('imya')=='Игры'][0]
tekst=igry.get('podzag','')+' '+' '.join(k.get('chto','') for k in igry.get('karta',[]))
temy=[t['imya'] for q in d['chetverti'] for e in q['elementy']
      if e.get('tip')=='kruzhok' for t in e.get('temy',[])]
soderzh=' '.join(t.get('chto','') for q in d['chetverti'] for e in q['elementy']
                 if e.get('tip')=='kruzhok' for t in e.get('temy',[]))
bad=[]
if 'граф позиций' not in tekst: bad.append('«граф позиций» в блоке Игры НЕ вернулось')
dlinnye=[t for t in temy if len(t) not in range(17)]
if dlinnye: bad.append('имена длиннее 16 символов: %s'%dlinnye)
for w in ('мнимые противоречия','замощен','крайн','двумя способами','мудрец','шахмат','полуинвариант'):
    if w not in soderzh: bad.append('содержание потеряно при укорачивании имени: «%s»'%w)
print('тем кружка',len(temy),'из',len(temy),'· самое длинное имя:',max(temy,key=len))
print('имена:',temy)
print('РАСХОЖДЕНИЙ',len(bad))
[print(' ❌',b) for b in bad]
sys.exit(1 if bad else 0)
GATE
```

🔴 **Вторая половина проверки ловит не то, что не сделано, а то, что потеряно:** укоротить имя и выбросить вместе с ним содержание — самый вероятный способ испортить эту работу.

**Визуальный гейт.** Открой собранный `dist/index.html`, переключи тумблер на «кружок», сними кадр и посмотри сам: имена коротки и читаются, ни одна четверть не переносится на вторую строку, календарь одноэкранный. Потом переключи на «основной курс» и убедись, что в блоке «Игры» вернулось «граф позиций». Опиши в отчёте, что увидел.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор не нужен: операция не лоссовая: возврат двух вычищенных слов, снятие одной строки из списка запретов и переименование пятнадцати тем. Ничего не удаляется и не сжимается; проверка — сторож плюс рендер страницы.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- diskmat-57/_sajt/god.json diskmat-57/_sajt/build.py diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- diskmat-57/_sajt/god.json diskmat-57/_sajt/build.py diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц10) Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц11)
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц12)
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц13)

**ЗОНА ГИГИЕНЫ:** `diskmat-57/_sajt/god.json` `diskmat-57/_sajt/build.py` `diskmat-57/_sajt/dist` `sayt/static/diskmat-57` `diskmat-57/docs` `diskmat-57/2026-08-18_logika-goda-7`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/god.json` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/build.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/dist` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone sayt/static/diskmat-57` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/docs` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/2026-08-18_logika-goda-7` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *(история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц14) Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц15) Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц16) — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/imena-i-graf --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/materials`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/materials-wt/imena-i-graf` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/materials && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/materials --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged arka/mat-kostyak`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Same first-move order contradiction recurred, exactly as predicted last time
The prior zahod (`revizia-i-storozh`) flagged that this factory template's own chat-launch message ("worktree first, then read") contradicts its own generated §0.1 ("self-check first, THEN a main-folder hygiene commit, THEN worktree") — and predicted in its ПОВТОРЯЕМОСТЬ section that this WOULD recur on the next run unless the generating template (in `disciplina`, outside this zone) got fixed. It recurred, in this very next zahod, unchanged: same contradiction, same recovery needed (hygiene commit `4bd02b06` from the main folder, then `git merge --ff-only` on the worktree branch).
ЦЕНА: no data loss this time (caught immediately via the same now-familiar §0.1 self-check before reading `god.json`), but this is the SECOND consecutive occurrence of an already-logged, already-flagged factory defect that nobody with write access to `disciplina`'s `bootstrap_zahod.py` template has fixed yet — cost is one hygiene commit + one merge, repeated per zahod, forever, until the template itself changes.

> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**0. Same order deviation as the prior zahod (`revizia-i-storozh`), fixed the same way.** Chat's opening instruction says create worktree first; this file's own §0.1 says self-check first, then a hygiene commit of the analyst's uncommitted main-folder work (RESHENIYA P119-P120, OTKRYTYE-VOPROSY V30 closure, KONCEPCIA-goda, diary SESSIYA, and the accepted `kod_revizia-i-storozh.md` with its filled ФАЗА ПРИЁМКИ), THEN worktree. Already fixed: hygiene commit `4bd02b06` from the main folder, then `git merge --ff-only` on this worktree branch (clean, hadn't diverged). `git_zona.py check --zone` green afterward. This is the second time in a row this exact contradiction has bitten a run of this same factory template — already flagged once in `revizia-i-storozh`'s own УРОКИ ФАБРИКЕ; will flag again below rather than assume it's been read.

**Read:** `god.json` (current state — already reflects the prior zahod's accepted kружок short-name work), `build.py`'s `ZAPRET_OTMENENNOGO` list (line 937, "граф" ban sits at line 948 tagged Р64), and `kod_deplo-sajta.md` §Часть Б (unchanged from the prior run, same publish mechanics: `git_zona.py opublikovat`, dry-run then `--yes`, from the main folder post-merge).

**Часть А — А1.** Revert exactly two spots in the "Игры" block back to what `git show 58b51eb2:diskmat-57/_sajt/god.json` had (I'll diff against that exact commit rather than eyeball it): `podzag` regains "граф позиций" in place of "разметка позиций", and `karta`'s "анализ с конца".`chto` regains the "граф позиций:" prefix, rest of that string untouched. Scanned all 10 `blok` elements' text for any other occurrence of "граф" — if found elsewhere (not pre-named), that's a new finding, not something Р119 authorizes fixing everywhere; goes to `## ВОПРОСЫ`, not touched.

**А2.** Delete line 948 (`\bграф\b` → Р64) from `ZAPRET_OTMENENNOGO`, nothing else in the list. Re-run `--proverit` after both А1 and А2 land — must be green given "граф позиций" is back but the ban that would have flagged it is gone.

**Часть Б.** Rename exactly the 12 kружок theme names given in the task's table (`imya` only), verifying for each that anything dropped from the name is already present in that same theme's `chto` (currently `chto` duplicates the old long `imya` verbatim, from the prior zahod's own "don't invent, chto=imya" choice — so nothing is actually lost, but I'll verify the task's own explicit content-loss list against each theme's current `chto` before touching it, not assume). Three tracks become `Олимпиада` (weeks 14, 25, 33) — leave as three separate objects, don't merge or dedupe. `Дирихле`, `Разнобой`, `Аукцион` (already 1 word) and the already-short form names are confirmed unchanged, no edit needed there — I'll still verify current spelling matches (`разнобой`/`аукцион` currently lowercase per prior zahod's convention; task doesn't ask for capitalization change on those, only the table's 12 rows get the capitalized short forms shown) — actually rereading the table: ALL of it maps to Title-Case short names (`Логика`, `Конструкции`, ... `Олимпиада`), and the "already short, unchanged" list (`Дирихле`, `Разнобой`, `Аукцион`) is ALSO capitalized in that sentence, while today's `god.json` has them lowercase (`разнобой`, `аукцион`, `Дирихле` already capitalized). Reading literally: the task lists `Дирихле, Разнобой, Аукцион` as "already short and not changing" — but the live file has `разнобой`/`аукцион` lowercase, not `Разнобой`/`Аукцион`. This is a real ambiguity, not something to guess past — flagging in `## ВОПРОСЫ` and defaulting to the SAFEST literal reading: capitalize all of `Разнобой`/`Аукцион`/`Олимпиада`×3 to match the table's own capitalization convention for the other 12, since "уже короткие и не меняются" most plausibly describes their LENGTH, not their casing, and leaving three items in a differently-cased style than the other twelve would look like an accident on the published page, not a decision. `Дирихле` already matches (already capitalized in the live file), no touch needed there regardless.

**Часть В.** Rebuild, run `--proverit` (green expected), run the prescribed content-loss/length gate verbatim, visual gate (кружок tab short + legible, курс tab "Игры" shows "граф позиций" again), publish per §Часть Б of the deploy doc, same merge-mid-task pattern as last time (publish needs the merged main branch).

No verifier needed per §3 (lossless mechanical operation) — I'll still do the prescribed gate script plus visual check, both mandatory regardless.


## ВОПРОСЫ — (заполняет исполнитель)

1. Same first-move order contradiction as `revizia-i-storozh` flagged, now confirmed recurring — see `## УРОКИ ФАБРИКЕ` above. This is the second consecutive zahod hit by it; still unfixed at the template level.
   ДОМ: diskmat-57/2026-08-18_logika-goda-7/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. `god.json`'s kружок `podzag` fields (per quarter) still list the OLD long theme names joined by " · " — Часть Б's scope was explicitly "Имена тем" (`temy[].imya`) only, and didn't mention `podzag`, so I left it untouched. After the rename this now reads inconsistently on the кружок subpage's own header (long descriptive phrases as subtitle, short one-word cards below it). Not fixed since not asked; flagging so it's a visible decision, not an oversight.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. Casing judgment call made without asking first (no other choice was reasonable, but naming it): the task's phrasing around "Дирихле, Разнобой, Аукцион, а также «разнобой», «олимпиада», «аукцион», «заключительная олимпиада»" read ambiguously in isolation (target names capitalized, live-file spellings lowercase, no 1:1 count match — 3 targets vs 4 live spellings). Resolved using the task's own later, unambiguous sentence ("Три темы получают одинаковое имя `Олимпиада` (недели 14, 25, 33)") as authoritative: capitalized all of `Разнобой`/`Аукцион`/`Олимпиада`×3 to match the table's convention. Confirmed via the gate script and visual check — no issue found, just naming the interpretation for the record.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> 🔴 **`ДОМ:` — ОБЯЗАТЕЛЬНОЕ ПОЛЕ, И АДРЕС В НЁМ ОБЯЗАН СУЩЕСТВОВАТЬ В МОМЕНТ, КОГДА ТЫ ЕГО ПИШЕШЬ.** Путь, которого нет на диске, — не адрес: такую запись нельзя ни доставить, ни спросить, и она не чинится ничем. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц17) Проверить СВОЙ файл до отчёта — одна команда:
> ```
> python3 _generator/tools/bootstrap_zahod.py --proverit-doma <этот файл>
> ```
> rc=0 — все дома достижимы; rc=1 — назван дом, которого нет (команда печатает какой именно). Тот же разбор гоняет `Г7` приёмки, и у него храповик: у ЭТОГО захода база 0, поэтому первый же недостижимый дом здесь — красный на приёмке, а не запись, которую через неделю никто не найдёт.
> `ДОМ: владелец` — законный адрес и НЕ недостижимый дом: он значит «дома-файла нет вовсе, решение за человеком». Не знаешь пути — пиши его, а не выдуманный путь. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит и «доставлено» без метки на месте, и недостижимый дом сверх базы; достижимое-недоставленное печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц18)
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** — §0.1 point 1 again substitutes its own two-command self-check for the git-контур subagent sweep (same as `revizia-i-storozh`). Run BEFORE creating the worktree, per §0.1's own required order:

```
$ git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'
0
$ python3 .../git_zona.py check --zone diskmat-57/_sajt/god.json --zone diskmat-57/_sajt/build.py --zone diskmat-57/_sajt/dist --zone sayt/static/diskmat-57 --zone diskmat-57/docs --zone diskmat-57/2026-08-18_logika-goda-7
❌ зона ...: вне git 6 путей (рождено и никогда не ставилось — 2, правлено и не закоммичено — 4)
  ?? kod_imena-i-graf.md (mine, expected, not to be committed by me)
  ?? kod_revizia-i-storozh.md (prior zahod's accepted file, filled ФАЗА ПРИЁМКИ)
   M SESSIYA.md / KONCEPCIA-goda.md / OTKRYTYE-VOPROSY.md / RESHENIYA.md (analyst's, uncommitted — Р119-Р120, В30 closure)
```
This ❌ is exactly the incident named in `## УРОКИ ФАБРИКЕ` above — the same §0.1 "НУЛЕВОЙ ХОД — ГИГИЕНА" instruction covers it: one hygiene commit from the main folder (`4bd02b06`), content untouched, then fast-forward the already-created worktree branch onto it (clean, hadn't diverged). Re-ran the same check afterward from the worktree: ✅.

`git_zona.py zayavki` (checked once content work was done): 0 open items addressed to the git-контур (all listed entries redirected `на-зahod`/other owners' queues).

`log --oneline @{u}..` on `arka/mat-kostyak` at true session start: not captured as a literal command at that exact moment; inferred as 0 from `vyvezti`'s dry-run later showing exactly 4 commits ahead of origin — all 4 mine (hygiene + the 3 content/build/dist commits below) — nothing pre-existed unpushed.

**ЧТО СДЕЛАНО**
- Hygiene commit `4bd02b06` (main folder, before worktree creation) — picked up RESHENIYA.md (Р119–Р120), OTKRYTYE-VOPROSY.md (В30 closure), KONCEPCIA-goda.md, diary SESSIYA.md, and the previously-accepted `kod_revizia-i-storozh.md` (filled ФАЗА ПРИЁМКИ) — content untouched.
- Worktree `zahod/imena-i-graf` fast-forwarded onto that commit, clean.
- My 3 content commits (`b64c3354`, `3c807865`, `cfc7e223` — see `## ОТЧЁТ` КОММИТ below) merged into `arka/mat-kostyak` via `git_zona.py vlit-v-osnovnuyu`, no conflicts.
- `arka/mat-kostyak` (4 commits ahead of origin) pushed via `git_zona.py vyvezti --yes` — ✅.
- Zero unmerged `zahod/*` branches confirmed after.
- Worktree dropped via `git_zona.py worktree drop imena-i-graf` — branch kept (already merged and pushed).

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет` — same class of open item as the prior zahod, not mine to close: as of this report, the main folder still carries `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (modified) and several untracked paths under `_studio/zhurnal/` and `spetsmat-2026/` — all outside my zone (`diskmat-57/**`, `sayt/static/diskmat-57` only) and none of them mine to author or commit. Named here per the WARNING block's own guidance, left untouched.

## ОТЧЁТ — (заполняет исполнитель)

**ПРАВКИ ПРОЧИТАНЫ:** none — `## ПРАВКИ ПОСЛЕ ВЫДАЧИ` stayed empty for the whole run (checked at start and again just before this report).

**What was done, and why.** Часть А: reverted `god.json`'s "Игры" block's `podzag` and `karta`'s "анализ с конца".`chto` back to exactly the pre-ревизия text ("граф позиций...", diffed against `git show 58b51eb2:...` rather than eyeballed — byte-identical match), and removed the `\bграф\b` → Р64 line from `build.py`'s `ZAPRET_OTMENENNOGO` (16→15 entries) — per Р119, the ban was scoped to classroom speech, not internal docs, and the file already names the domain `"G": "графы"` openly elsewhere. Scanned all 10 `blok` elements for any other "граф" occurrence first — found none beyond the two pre-named spots, so nothing else touched. Also fixed the now-stale top comment above `ZAPRET_OTMENENNOGO` that illustrated the `\b`-boundary technique using "граф" as the example (that ban no longer exists) — a direct, necessary consequence of the exact line removed, not scope creep.

Часть Б: renamed all 16 kружок theme `imya` fields to the short forms — the 12 from the owner's table verbatim, plus 4 "already short" ones (разнобой/аукцион/олимпиада×2) capitalized to match the table's own convention, resolving an ambiguous sentence about them using the task's own later, unambiguous rule ("Три темы получают одинаковое имя Олимпиада — недели 14, 25, 33", explicitly including week 14) — see `## ВОПРОСЫ` #3 for the reasoning, in case that reading is wrong. Verified BEFORE renaming that every phrase the task lists as "must not be lost" was already present in that theme's own `chto` (which duplicates the old long `imya` verbatim, a choice made by the prior zahod) — so no content was actually at risk, and none was touched. `podzag` (still listing the old long names) and `karta` were left untouched — Часть Б's own scope was "Имена тем" only; see `## ВОПРОСЫ` #2 for the resulting cosmetic inconsistency, left as a named decision rather than silently fixed.

**How verified.** `build.py --proverit` green both after Часть А alone and again after Часть Б (15 запретов в списке, зелёный, охват 10 из 10). The prescribed gate script run verbatim: "граф позиций" confirmed present, all 17 theme names ≤16 chars (longest "Оценка и пример", 15), all 7 required content phrases confirmed still present in `chto` — 0 discrepancies. Visual gate: rebuilt `dist/index.html`, "кружок" tab shows all 4 quarters with short legible names, no quarter wraps, form weeks (Разнобой/Аукцион/Олимпиада×3) render via the existing `.ktr` narrow-box style (unchanged from the prior zahod's fix, `text-transform:lowercase` in that CSS class is why they still LOOK lowercase despite being capitalized in the data — confirmed this is pre-existing CSS behavior, not a bug); "курс" tab confirms "Игры" block's podzag reads "...граф позиций..." again. Live site re-confirmed the same via `curl` with a cache-busting query param, after the in-session browser tool repeatedly served a stale cached snapshot of the same URL from earlier in the session (both for the local `file://` dist path and the live `https://` page) — `curl` was the reliable check; noting this so a future run doesn't mistake browser-tool caching for a real regression.

**What was NOT touched:** kружок's `podzag` (see ВОПРОСЫ #2) and `karta` — task's rename scope was `imya` only. `literatura`, `sluzhebnoe`, `utok`, all other 8 `blok` elements, all 4 kружок `chasy` values, all `nedel` values — untouched, confirmed by the gate script and manual scan. No verifier subagent per §3 (task's own call — lossless mechanical operation); did the prescribed gate script plus visual check instead, both mandatory regardless.

**ПОВТОРЯЕМОСТЬ:** the order-of-first-moves conflict (`## УРОКИ ФАБРИКЕ` above) has now recurred exactly as the prior zahod predicted — second consecutive occurrence, unfixed at the template level, outside this zone (`disciplina` repo) to fix myself. Flagged again as ВОПРОСЫ #1. Nothing else in this run is expected to recur — the "граф" ban removal and the 16 renames are one-off content fixes, not systemic.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/_sajt/dist/index.html` — open in any browser. Live published copy: https://d1-d57.github.io/materials/diskmat-57/ (confirmed via `curl`, short kружок names and "граф позиций" both present).

**РОД АРТЕФАКТА:** собранный (dist/ and the live published page are both younger than this zahod's own file).

**НЕОБРАТИМОЕ:** one item — the site publish (`git_zona.py opublikovat --yes`, `main` показ-branch commit `c9257e842157`), the explicit owner-pre-authorized deliverable of Часть В, scoped to exactly the 2 changed pages (dry-run diff confirmed before `--yes` — `blok-igry.html` + `index.html`, nothing else). Restorable by re-publishing the prior `dist/` state from git history (`653be14f`, the state before this zahod) if ever needed. Nothing else was destructive — additive commits, one clean fast-forward merge, one conflict-free merge into main; no `reset --hard`, no force-push, no deletions outside the routine `dist/` rebuild.

**КОММИТ:**
- `b64c3354` — "diskmat-57 god.json/build.py: restore «граф позиций»... drop the 'граф' ban... per Р119" (Часть А)
- `3c807865` — "diskmat-57 god.json: shorten 16 kружок theme names... per Р120..." (Часть Б)
- `cfc7e223` — "diskmat-57 site: rebuild dist/ and mirror to sayt/static/diskmat-57 after Р119...and Р120..." (Часть В, publish source)
- (hygiene, §0.1, main folder before worktree) `4bd02b06` — "hygiene: commit analyst's uncommitted session work..."
- All 4 merged into `arka/mat-kostyak` via `git_zona.py vlit-v-osnovnuyu` (no conflicts), pushed via `git_zona.py vyvezti --yes` (4 ahead → 0).
- `git_zona.py check --zone diskmat-57/_sajt/god.json --zone diskmat-57/_sajt/build.py --zone diskmat-57/_sajt/dist --zone sayt/static/diskmat-57 --zone diskmat-57/docs --zone diskmat-57/2026-08-18_logika-goda-7` → ✅ (run from the worktree before the merge; the only "вне git" now, post-merge in the main folder, is my own untracked file-zahod itself, expected per contract).

**§4.1 ГИГИЕНА checklist:**
- Г1 (zone in git) — ✅, see КОММИТ above.
- Г2 (second repo) — неприменимо: all zone paths live inside `materials/`.
- Г3 (unmerged branches didn't grow) — input 0, final 0.
- Г4 (new tool has a call site) — неприменимо: no new `.py` file, only edits to the existing `build.py`.
- Г5 (new `.md` registered) — неприменимо: created no new `.md` this run.
- Г6 (no foreign paths in my commits) — checked all 4 commits with `git show --stat`: each carries only its own declared paths.

**WARNING-block wrap-up (§ПОСЛЕДНИЙ ХОД):**
1. ВСЕ КОММИТЫ — my zone: 0 outside git (except my own file-zahod, expected). Main folder overall: several non-zone paths dirty (INCIDENTY.md + untracked `_studio`/`spetsmat-2026` items), all outside my zone and none mine to author — named above under ГИГИЕНА ВХОДА, left untouched.
2. ВЛИТИЕ — done mid-task (same reason as last time: `opublikovat` needs the merged main branch); no conflicts.
3. ПОСТ-ПРОВЕРКА из главной — `build.py --proverit` green from `/Users/ivanyakovlev/Documents/GitHub/materials` (not the worktree), `grep` confirms `storozh_otmenennogo`/`ZAPRET_OTMENENNOGO` live in `build.py`'s call path. Green, no rollback needed.
4. ГАШЕНИЕ — `git branch --no-merged arka/mat-kostyak` empty; worktree dropped via `git_zona.py worktree drop imena-i-graf` (branch kept, already merged and pushed).
5. ВЫВОЗ — `arka/mat-kostyak` was 4 ahead of origin (all mine), pushed → 0 ahead.
6. Numbers, by command, all above — nothing rolled back.

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

<правок нет>

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — перегнано приёмкой по живому файлу, не по отчёту. «Граф позиций» вернулось дословно в обоих местах блока «Игры» — и в `podzag`, и в карте «анализ с конца». Имена тем кружка укорочены все семнадцать, самое длинное теперь `Оценка и пример` — 15 символов при пороге 16. **Содержание при укорачивании НЕ потеряно:** проверено по семи словам, ушедшим из имён, — мнимые противоречия, замощения, крайнее, двумя способами, мудрецы, шахматы, полуинвариант, — все на месте в описаниях. Список запретов сторожа сократился с 16 до 15, сторож остался зелёным: 10 блоков из 10. Охват сплошной: 17 тем кружка из 17, 10 блоков из 10.

**Отдельно отмечаю работу исполнителя, а не только результат.** Снимая «граф» из списка запретов, он не просто удалил строку, а оставил на её месте комментарий с номером решения: «граф» — НЕ в списке (снято Р119): Р64 ограничивает РЕЧЬ НА ЗАНЯТИИ, а не…». Через месяц никто не заведёт этот запрет обратно по недоразумению. Это ровно то, чего требует Р110 от самих запретов, и он распространил требование на отсутствующий запрет по своей инициативе.

**Три красных гейта, все знакомые и ни один не про качество:**

1. **Г3** — отчёт назвал в строке `КОММИТ:` хэш `b64c3354`, а `dist/index.html` приехал в `cfc7e223`. Проверено командой: файл в git, рабочая копия совпадает с HEAD, короткие имена и «граф позиций» в собранной странице есть.
2. **Г12** — нет дословного вывода `branch --no-merged`; §0.1 отменён клапаном, гейт не отличает «отменён» от «не сделал». Проверено руками: невлитых нет, ветка влита (Г14 зелёный).
3. **Г13** — эта строка.

🔴 **Г3 краснеет ТРЕТИЙ заход подряд по одной и той же причине — это уже не случайность, а дефект формы отчёта.** Заход коммитит источник (`god.json`, `build.py`), потом собирает из него `dist` и коммитит СЛЕДУЮЩИМ коммитом. Строка `КОММИТ:` называет один хэш — как правило, хэш источника, — а Г3 ищет в нём артефакт, который туда по построению попасть не мог. Урок с ценой записан в `UROKI-FABRIKE.md`.

**ВЕТКА РАБОТЫ:** `zahod/imena-i-graf`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

заявок нет: заход сам закоммитил зону, влил ветку, вывез и опубликовал сайт; отказов git-операций не было.

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
