# Канал исполнителя — revizia-i-storozh (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
<!-- гейт сборки заполненного: ЖДЁТ -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — ревизия по названному списку и сторож по названному списку запретов; решений по ходу не рождается.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — ревизия и сторож по названным спискам, решений по ходу не рождается.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/revizia-i-storozh.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/2026-08-18_logika-goda-7/kod_revizia-i-storozh.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add revizia-i-storozh --branch zahod/revizia-i-storozh, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py diskmat-57/2026-08-18_logika-goda-7`
Область: «diskmat-57/2026-08-18_logika-goda-7» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — ни одного skills/*/DOLG.md нет на диске (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 15
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 48
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 40
       живых (чинится доставкой — «дом есть»)  : 4
       к владельцу (решение за человеком)      : 9
       адрес недоступен (нет/папка/код/указат.) : 0
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 27
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `diskmat-57/2026-08-18_logika-goda-7` есть 1 незакоммиченных файлов-заходов `kod_sajt-cveta-i-setka.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «незакоммичены версия 2 KONCEPCIA-goda.md, дневник, уроки и заполненные приёмки двух заходов — всё написано аналитиком в этой сессии параллельно прогонам. Заход забирает их НУЛЕВЫМ коммитом: из рабочей папки они не видны, и прошлый исполнитель уже об это споткнулся.». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. Год курса описан одним файлом `diskmat-57/_sajt/god.json`, из него собирается сайт. Устройство верное и менять его не надо. Сломано другое: файл ни разу не сверялся с решениями ЦЕЛИКОМ. Два предыдущих захода правили его точечно — по списку названных правок, — и поэтому не тронули то, что в список не попало. Владелец открыл опубликованную страницу и увидел в блоке «Разгон» проценты и доли, отменённые решением Р50 ещё 13 сентября. ЦЕЛЬ: вычистить отменённое из блоков основной программы и завести сторож, который не даст этому повториться.
Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели не предусмотрен: заход идёт до опубликованного адреса.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-14) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Устройство менять НЕ надо: источник правды — god.json, сайт собирается из него. Сломано содержание, а не система
2. Разгон: карта «доли и части» и подзаг «проценты и доли» отменены Р50 ещё 13.09 и должны уйти; по Р74 и Р80 в блоке обратный ход, логика и разумный перебор
3. Служебные заметки sluzhebnoe устарели в четырёх местах: вероятность как блок, «арифметики 10 часов и расти некуда», «Дирихле ушло на кружок», «Кодировка после Подсчётов 2»
4. Вероятность на КРУЖКЕ остаётся и называется своим именем — Р49 ограничивает основную программу, а не кружок (Р108)
5. Список литературы вмещает всё, чем потенциально пользуемся; книгу под отменённый блок не удалять (Р109)
6. Заводится сторож отменённого (Р110): краснеет на запрещённом решением слове в блоках основной программы, каждый запрет несёт номер Р. Область — только tip == blok, его imya, podzag и karta. Кружок, литература и sluzhebnoe под сторож не попадают
7. Сторож гоняется вместе с build.py --proverit, а не отдельной командой, которую забудут позвать

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/revizia-i-storozh`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/revizia-i-storozh`. 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 **Заводишь её САМ — ВТОРЫМ ходом, СРАЗУ ПОСЛЕ пункта 1 блока §0.1 ГИТ-КОНТУР ниже (не первым ходом захода целиком), из корня репозитория:**
  ```
  python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add revizia-i-storozh --branch zahod/revizia-i-storozh
  cd ../materials-wt/revizia-i-storozh
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце вливает САМ ЗАХОД, последним ходом** (решение 20.08, заход `granica-priyomki`, пересмотр решения 05.08; WARNING-блок ниже печатает тот же порядок): цепочкой коммит зоны → пуш → влитие в основную (`git_zona.py vlit-v-osnovnuyu`) → пост-проверка ИЗ ГЛАВНОЙ папки → гашение СВОЕЙ ветки. Причина пересмотра — замер 20.08: изнутри захода не видно разницы между «механизм новый в рабочей папке» и «в `main` он старый», а приёмке это невидимо по построению. Влитие УСЛОВНОЕ: красная пост-проверка = откат влития самим заходом и строка в отчёт, а не «доложу, пусть приёмка решает». Файл-заход по-прежнему НЕ коммитить — его коммитит аналитик при приёмке.
- **ЗОНА (можно менять):** `diskmat-57/_sajt/god.json` `diskmat-57/_sajt/build.py` `diskmat-57/_sajt/dist` `sayt/static/diskmat-57` `diskmat-57/docs` `diskmat-57/2026-08-18_logika-goda-7`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *(история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц1) Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/revizia-i-storozh/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц2)
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц3).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала САМ прогоняешь две команды самопроверки контура (пункт 1 ниже), и только ПОТОМ заводишь свою рабочую папку** — её ветка отпочковывается от основной такой, какая она есть на момент запуска: контур пуст, доносить инструмент влитием нечего.

**1. ВЕСЬ КОНТУР ПУСТ — САМОПРОВЕРКА ВМЕСТО СУБАГЕНТА.** При сборке проверены три числа контура, и все три нулевые: невлитых `zahod/*`-веток 0 (🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`); открытых заявок 0 (снимок при сборке 2026-09-14, пересчитать самому: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`); названных `--vlit` 0. Звать субагента не за чем — выполни САМ две команды и вставь их вывод в `## ОТЧЁТ` дословно:
```
git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'   # снимок при сборке 2026-09-14: 0
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/god.json diskmat-57/_sajt/build.py diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7
```
Первая вернула не 0 — НИЧЕГО чужого не вливай (свою ветку вольёшь последним ходом, см. ниже), назови число строкой в `## ОТЧЁТ` и работай дальше. Вторая красная — сначала приведи в порядок свою зону.

Если при следующей сборке хоть одно из трёх чисел окажется ненулевым, генератор сам вернёт сюда задание субагенту гит-контура — печатает его дверь `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu`; звать его в этом заходе не надо.

🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- задеплоить: сайт курса на GitHub Pages по процедуре из kod_deplo-sajta.md §Часть Б; адрес назвать в отчёте

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/revizia-i-storozh`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add diskmat-57/_sajt/god.json diskmat-57/_sajt/build.py diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы. Проект сверх них не изучай:
  1. `diskmat-57/docs/RESHENIYA.md` — таблица решений, 110 строк. Это источник списка отмен; читай её целиком, отмены рассыпаны по всей таблице.
  2. `diskmat-57/_sajt/god.json` — рабочий файл ревизии.
  3. `diskmat-57/_sajt/build.py` — куда встраивается сторож: функция, обслуживающая `--proverit`.
  4. `diskmat-57/2026-08-18_logika-goda-7/kod_deplo-sajta.md`, раздел «Часть Б — публикация на GitHub Pages».
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц7)
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц8)

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц9). Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
🔴 **НУЛЕВОЙ ХОД — ГИГИЕНА, В ГЛАВНОЙ ПАПКЕ, ДО ЗАВЕДЕНИЯ WORKTREE.** В главной папке лежит незакоммиченной работа аналитика этой сессии: `diskmat-57/docs/KONCEPCIA-goda.md` (версия 2), `RESHENIYA.md`, `OTKRYTYE-VOPROSY.md`, дневник `SESSIYA.md`, `UROKI-FABRIKE.md` и два файла-захода с заполненной `## ФАЗА ПРИЁМКИ`. Забери их ОДНИМ коммитом гигиены из главной папки, между пунктом 1 и пунктом 2 блока §0.1: из свежего worktree они не видны, там своя копия дерева. Содержание НЕ правь, только коммить.

Дальше две работы: ревизия содержания и сторож. **Ревизия первая** — сторож обязан покраснеть на невычищенном файле и позеленеть на вычищенном, и это твоя же проверка, что он работает.

### Часть А — ревизия содержания `god.json`

**А1. Собери список отмен САМ, по `RESHENIYA.md`.** Не бери его из этого захода на веру: заход называет то, что владелец заметил глазами, а не всё, что отменено. Пройди таблицу решений целиком и выпиши каждое, которое что-то УБИРАЕТ из основной программы. Опорные, которые там точно есть: Р50 (проценты и дроби), Р49 (вероятность как блок и как слово), Р25 (ОТА, НОД и НОК, разложение на простые, алгоритм Евклида), Р95 (уравнения в целых числах, взаимно простые), Р103 (текстовые задачи: движение, возраст, работа, составление уравнений), Р15 (сочетания, бином, Паскаль — 8 класс), Р64 (слово «граф» вслух), Р70 (термин «индукция»), Р71 (формальная логика и кванторы), Р87 и Р89 (оценка с примером и конструкции как ТЕМА основной программы). **Нашёл отмену, которой в этом списке нет, — это находка, а не повод молчать: назови её в `## ВОПРОСЫ`.**

**А2. Пройди все элементы `tip == "blok"` всех четырёх четвертей** и сверь с этим списком их `imya`, `podzag` и каждый пункт `karta` (поля `tema` и `chto`).

Одно место известно заранее и чинится точно:

- **`Разгон`, I четверть.** Подзаг сейчас `проценты и доли · обратный ход · рыцари, лжецы и отрицание`; карта несёт пункт `доли и части` → `процент как доля · задачи на смеси и разбавление · «каждый седьмой шахматист»`. Всё это отменено Р50 полностью ещё 13.09. Пункт карты убрать, подзаг привести к тому, что в блоке осталось. По Р74 блок занимает 2 часа и называется в раскладке «логика и перебор», по Р80 именно здесь вводится разумный перебор, по Р73 15 сентября идут два листка — обратный ход и логика с упором в аккуратный перебор. Карта после правки обязана этому соответствовать.

🔴 **Границы ревизии, нарушать нельзя:**

- **Кружок НЕ трогать.** Вероятность на кружке остаётся и называется своим именем: Р49 ограничивает основную программу, а не кружок (Р108, Р84).
- **`literatura` НЕ трогать.** Список вмещает всё, чем мы потенциально пользуемся, включая книги под снятые блоки (Р109).
- **Ничего НЕ добавлять.** Ревизия только убирает отменённое и приводит подзаг в соответствие с оставшейся картой. Нового содержания не сочиняй: пустое место — находка для владельца, а не приглашение его заполнить.

**А2б. Домен блока «Двойной счёт» — составной.** Сейчас стоит `"domen": "K"`. Именно в этом блоке вводится уток «Граф — молча»: карта несёт `подсчёт рёбер → сумма степеней вершин считается с двух сторон · число людей с нечётным числом рукопожатий чётно`, то есть лемму о рукопожатиях. Поставить `"domeny": ["K","G"]` тем же приёмом, каким у блока «Информация» стоит `["K","M","A"]`; поле `domen` оставить `K` как основное. Владелец 14.09: «в двойной счёт надо добавить цвет про графы тоже».

**А3б. 🔴 В `sluzhebnoe` есть ОТМЕНЁННОЕ ПРАВИЛО — его надо снять, а не просто поправить числа рядом.** Там стоит: «Правило: если тему кто-то потом будет проходить систематически, кружку её отдавать нельзя». Владелец 14.09 отменил его прямо (Р111) и заменил обратным: **тема курса, которая развивается на кружке — особенно одновременно с парой, — это хорошо**; причина, по которой так не везде, не запрет, а нехватка места в программе курса. Перепиши это место по Р111. Абзац «Чего на кружке быть не может» со списком из одиннадцати пунктов при этом ОСТАЁТСЯ — он опирается на Р25 и Р15, а не на отменённое правило.

**А3. Служебные заметки `sluzhebnoe`.** Это проза о порядке блоков, и четыре места в ней устарели: про вероятность после Подсчётов 2 (блока нет, Р49), про «арифметики всего 10 часов за год, и расти ей некуда» (стало 17 — Чётность 4, Делимость 6, Остатки 7), про «Дирихле ушло на кружок» (Р105 вернул его в основную программу отдельным блоком), про «Кодировка после Подсчётов 2» (блок называется «Информация», Р102). Перепиши эти четыре места по живому состоянию; остальное в `sluzhebnoe` не трогай — там история порядка, она ценна.

### Часть Б — сторож отменённого

**Б1. Заведи проверку, которая краснеет, если в блоках основной программы появилось отменённое** (Р110). Она гоняется **вместе с `build.py --proverit`**, а не отдельной командой: отдельную забудут позвать, и именно так мы сюда и приехали.

Требования к сторожу, каждое обязательно:

- **Каждый запрет несёт НОМЕР РЕШЕНИЯ.** Сообщение вида `🔴 «процент» в блоке Разгон, karta[0] — отменено Р50`. Запрет без номера запрещён: через месяц по нему не восстановить, кто и зачем это убрал.
- **Область — только `tip == "blok"`, поля `imya`, `podzag`, `karta`.** Кружок, `literatura`, `sluzhebnoe` и `utok` под сторож НЕ попадают — это прямое следствие Р108 и Р109, а не упрощение.
- **Сторож печатает ОХВАТ вместе с вердиктом:** сколько блоков проверено из скольких и сколько запретов в списке. Зелёный без охвата неотличим от «не нашёл, что проверять».
- **Сторож честно печатает, чего он НЕ проверяет.** Как минимум: судит подстроки, а не смысл; не видит отменённого, названного другими словами; не судит кружок и литературу — по решению, а не по недосмотру.
- **Список запретов лежит РЯДОМ С КОДОМ сторожа, одним местом, и читается глазами.** Не рассыпан по функциям.

**Б2. Проверь сторож ОБЕИМИ сторонами — иначе он не проверен.** Гейт, который никогда не краснел, не доказал, что умеет краснеть. Прогони его на состоянии ДО ревизии (прежняя версия `god.json` из git) — обязан покраснеть на Разгоне. Прогони после — обязан позеленеть. **Оба прогона с кодом возврата, в `## ОТЧЁТ` дословно.**

### Часть В — КРУЖОК В ЭТОМ ЗАХОДЕ НЕ ТРОГАТЬ

🔴 **Дорожки кружка (`tip == "kruzhok"`) — READ-ONLY, включая их часы, темы, порядок и домены.** Причина названа, чтобы ты не «починил заодно» то, что выглядит сломанным: кружок действительно разъехался, и это известно. Снято командой 14.09:

```
четверть      I    II   III   IV    сумма
недель        6     8    11    8      33
кружок        8     8    10    7      33   ← расходится в I, III и IV
```

Кружок идёт один урок в неделю (Р83), значит его часов в четверти обязано быть ровно столько, сколько вторников. Сумма 33 сходится случайно — тот же класс ошибки, что дал раньше «70 часов при годе в 66». Плюс кружковый Дирихле стоит на неделях 1–3 третьей четверти, а основной блок «Дирихле» — на 5–6.5, и домен кружкового Дирихле стоит `A` вместо `M`.

**Всё это чинится ОТДЕЛЬНЫМ заходом**, потому что владелец 14.09 решил переделывать программу кружка целиком, с привязкой к КАЖДОЙ неделе, а не блоками по 2–3. Половинчатая правка здесь означала бы двойную работу. Увидел в кружке ещё что-то — строка в `## ВОПРОСЫ`, руки прочь.

### Часть Г — пересборка и публикация

`python3 build.py` из `diskmat-57/_sajt/`, затем публикация по якорю 4 (`kod_deplo-sajta.md`, «Часть Б»). Адрес назвать в отчёте строкой.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).**
Это ЖИВОЙ ПРОГОН на реальном объекте репозитория, а не фикстура: команды читают живой `diskmat-57/_sajt/god.json` и живой `diskmat-57/_sajt/build.py`.

1. `python3 diskmat-57/_sajt/build.py --proverit` → `rc=0`, и сторож внутри него зелёный.
2. Сторож на прежней версии `god.json` из git → `rc≠0`, и в выводе назван Разгон.
3. Проверка границы — вставь в отчёт вывод дословно:

```
python3 - <<'GATE'
import json,re,sys
d=json.load(open('diskmat-57/_sajt/god.json'))
bloki=[e for q in d['chetverti'] for e in q['elementy'] if e.get('tip')=='blok']
def tekst(e):
    return ' '.join([e.get('imya',''),e.get('podzag','')]+
                    [k.get('tema','')+' '+k.get('chto','') for k in e.get('karta',[])])
zapret={'процент':'Р50','дроб':'Р50','доли и части':'Р50','Евклид':'Р25',
        'взаимно прост':'Р95','разложение на прост':'Р25'}
bad=[]
for e in bloki:
    t=tekst(e)
    for w,r in zapret.items():
        if re.search(w,t,re.I): bad.append('«%s» в блоке %s — отменено %s'%(w,e.get('imya'),r))
kruzhok=[e for q in d['chetverti'] for e in q['elementy'] if e.get('tip')=='kruzhok']
ver=sum('вероятност' in json.dumps(e,ensure_ascii=False).lower() for e in kruzhok)
print('блоков проверено',len(bloki),'из',len(bloki),'· запретов в списке',len(zapret))
print('кружок: дорожек',len(kruzhok),'· из них с вероятностью',ver,'— обязано быть больше нуля (Р108)')
if ver==0: bad.append('вероятность вычищена с КРУЖКА — нарушение Р108, ревизия вышла за границу')
print('РАСХОЖДЕНИЙ',len(bad))
[print(' ❌',b) for b in bad]
sys.exit(1 if bad else 0)
GATE
```

🔴 **Вторая половина этой проверки важна не меньше первой:** она ловит не недочистку, а ПЕРЕчистку — ревизию, вышедшую за границу и снёсшую законное вместе с отменённым. Ноль вероятности на кружке — красное, а не победа.

**Визуальный гейт.** Открой собранный `dist/index.html`, сними кадр и посмотри сам: в блоке «Разгон» нет процентов и долей, подзаг соответствует карте, календарь по-прежнему одноэкранный и ни одна четверть не переносится. Опиши в отчёте, что увидел.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ПОСЛЕ-типа** — судит результат, стоит в конце, после задачи. Свежий субагент, ДРУГИМ методом (свежий субагент СВОИМ кодом проходит блоки основной программы god.json и сверяет каждое текстовое поле со списком отмен из RESHENIYA.md; отдельно проверяет, что сторож НЕ краснеет на кружке, литературе и sluzhebnoe — то есть что область сужена верно), не перечитывает свою же правку. Доля сплошной выборки: сплошная выборка 100 процентов: все 10 элементов tip == blok всех 4 четвертей плюс 4 дорожки кружка и 7 записей литературы как контроль ложного срабатывания — охват назвать числом. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- diskmat-57/_sajt/god.json diskmat-57/_sajt/build.py diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- diskmat-57/_sajt/god.json diskmat-57/_sajt/build.py diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц10) Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц11)
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц12)
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц13)

**ЗОНА ГИГИЕНЫ:** `diskmat-57/_sajt/god.json` `diskmat-57/_sajt/build.py` `diskmat-57/_sajt/dist` `sayt/static/diskmat-57` `diskmat-57/docs` `diskmat-57/2026-08-18_logika-goda-7`

- **В1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/god.json` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/build.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/dist` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone sayt/static/diskmat-57` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/docs` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/2026-08-18_logika-goda-7` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **В2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **В3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **В4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *(история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц14) Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц15) Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц16) — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/revizia-i-storozh --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/materials`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/materials-wt/revizia-i-storozh` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/materials && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/materials --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged arka/mat-kostyak`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### The zahod's own opening chat message and its own §0.1 disagree on step order, and this is not the first time
The chat message that launched this run ("СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ" block) says: "Первым ходом: cd .../materials, затем ... worktree add ..., затем чтение захода." §0.1 of the same generated file says the opposite: run the two self-check commands FIRST, and — critically — do a hygiene commit of the analyst's uncommitted main-folder edits BETWEEN §0.1's point 1 and point 2, THEN create the worktree. I followed the chat message and created the worktree before reading §0.1, so it forked before `diskmat-57/docs/RESHENIYA.md` (v2, with rows Р84–Р111 — 27 of 122 rows) and `KONCEPCIA-goda.md` (v2) were committed to the main folder; my worktree could not see them. This exact failure mode is already documented as a factory lesson inside `RESHENIYA.md` itself (line 123, a `<!-- ДОСТАВЛЕНО: god-json-v2#2 ... -->` comment): a prior zahod hit the identical trap and it was already "closed" as a lesson — yet the generating template still produces a chat message that reintroduces it.
ЦЕНА: caught only because §0.1's own point-1 self-check (`git_zona.py check --zone`) came back ❌ before any content work started — cost one extra `git_zona.py commit` (hygiene) plus one `git merge --ff-only` to bring the worktree branch up to date, both harmless once caught. Had this run not paused to run the self-check literally before reading RESHENIYA.md, the entire content revision (Часть А) would have been built against a stale decisions table missing 27 rows (Р84–Р111) — a second recurrence of the exact incident this arc's own lesson file already names as closed.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**0. Order deviation, already fixed.** The chat's opening instruction told me to create the worktree as the very first move; §0.1 of this zahod instead requires: self-check first, THEN (between its point 1 and point 2) a hygiene commit of the analyst's uncommitted main-folder work, THEN create the worktree. I had already created the worktree before reading this. Consequence: `diskmat-57/docs/RESHENIYA.md` and `KONCEPCIA-goda.md` had uncommitted v2 edits in the main folder that my worktree — forked earlier — could not see (this exact trap is already documented as a factory lesson at the tail of RESHENIYA.md, from a prior zahod `god-json-v2`). Fix applied: committed the hygiene set from the main folder (`5f1f96e3`: KONCEPCIA-goda v2, RESHENIYA v2, diary SESSIYA, UROKI-FABRIKE, and the `kod_sajt-cveta-i-setka.md` + `.otmenen` pair — content untouched, commit only), then fast-forwarded my worktree branch onto it (`git merge --ff-only`, clean since my branch had not diverged yet). Verified `git_zona.py check --zone` green afterward. `OTKRYTYE-VOPROSY.md` was already clean, no action needed there.

**А1. My own removal list from RESHENIYA.md (read whole, 122 rows).** Beyond the zahod's named anchors (Р50, Р49, Р25, Р95, Р103, Р15, Р64, Р70, Р71, Р87, Р89), I found no additional decision that removes math *content* from the main-program blocks. (Р36 — dropping named reference problems before research, 20.08 — is an editorial-process rule about task authoring, not curriculum content; excluded.) I will use exactly the named 11-decision list against `tip=='blok'` fields.

**А2. Audit of all 10 `blok` elements' `imya`/`podzag`/`karta`** (Разгон, Перебор и правило суммы, Чётность / Делимость, Двойной счёт / Правило произведения, Дирихле, Информация / Остатки, Игры):
- **Разгон (I)** — the pre-named fix: drop karta item "доли и части" (Р50: "процент как доля", "смеси и разбавление", "каждый седьмой шахматист"); podzag `проценты и доли · обратный ход · рыцари, лжецы и отрицание` → `обратный ход · рыцари, лжецы и отрицание`. `imya` stays "Разгон" (Р74's "логика и перебор" is the raskladka's shorthand label for the slot, not an instruction to rename the block — task text only asks to drop the karta item and fix podzag).
- **Игры (IV) — NEW finding, not pre-named.** podzag and karta[`анализ с конца`].chto both say **«граф позиций»** — the literal word banned by Р64 ("графы — имя внутреннее... слова не произносим"). Fix: reword to drop the word while keeping the substance (vertices=positions, arrows=moves), reusing this same block's own vocabulary ("три правила разметки"): `граф позиций` → `разметка позиций` in both spots. (Compare: "Двойной счёт" already handles this correctly — talks of "рукопожатий", "вершин", "рёбер" without ever saying "граф"; that's the existing good pattern, not something to touch.)
- All other 8 blocks — checked against all 11 decisions (процент/дробь, Евклид/разложение на простые, взаимно простые, текстовые задачи, сочетания/бином/Паскаль, граф, индукция, кванторы, оценка-с-примером, конструкция/узкое место): clean, no changes.

**А2б.** "Двойной счёт" domen: add `"domeny": ["K","G"]` alongside the existing `"domen":"K"`, same mechanism as "Информация"'s `["K","M","A"]` — owner's 14.09 instruction, the karta already carries the handshake-lemma content that motivates it.

**А3 / А3б. `sluzhebnoe` — 5 edits, nothing else touched:**
1. Delete the "Вероятность после Подсчётов 2" paragraph outright (no live referent — no probability block exists, Р49).
2. "Арифметика: всего 10 часов за год, и расти ей некуда" → "всего 17 часов за год — Чётность 4, Делимость 6, Остатки 7" (rest of that paragraph — Shen's листки 01-04, Euclid/OTA/Fermat deferred to 8th grade — stays, still accurate).
3. "Что ушло на кружок: ним ..., раскраски ..., Дирихле (мало простых задач...), симметрия..." → drop the Дирихле clause (Р105 returned it to the main program too, so "ушло" is no longer accurate).
4. "Кодировка после Подсчётов 2" → "Информация после Подсчётов 2" (Р102 rename only; I won't also touch the stale "Подсчётов 2" reference itself since the task only asked for the name swap — flagging that separately below).
5. (А3б) Drop the trailing sentence "Правило: если тему кто-то потом будет проходить систематически, кружку её отдавать нельзя." and replace per Р111 with something reflecting the reversal (the preceding 11-item "чего на кружке быть не может" list stays untouched, per task).

**Out of scope, flagged not fixed:** `Разгон.opornye` still carries "уксусная эссенция 70%...", "смешать 10% и 15% растворы..." — genuinely Р50-banned content, but the task's own scope for both the revision (А2) and the guard (Б1) is explicitly limited to `imya`/`podzag`/`karta`. Leaving it untouched and naming it in `## ВОПРОСЫ`.

**Б. Guard.** Add to `build.py`: a `ZAPRET_OTMENENNOGO` list of (regex, Р-number) pairs living next to a `storozh_otmenennogo(g)` function (one place, readable), covering all 11 decisions from А1 (word-boundary-safe for short tokens like `граф`/`бином` so it doesn't false-positive inside "график"/"биография"-style words). Wire it into `sobrat_klass()`'s existing `if tolko_schet:` branch (i.e. runs automatically under `build.py --proverit`, not a separate command) — prints coverage ("блоков проверено X из Y · запретов в списке N"), prints what it does NOT catch (substrings not meaning; renamed-around bans; кружок/literatura/sluzhebnoe/utok out of scope by Р108/Р109, not oversight), and returns rc=1 through the existing `sobrat_klass`→`main` rc chain if anything is found.

**Б2.** Prove both directions with exit codes pasted verbatim: guard on the git HEAD version of `god.json` (before my edits) → rc≠0, Разгон named; guard after my edits → rc=0.

**Г.** `python3 build.py` (rebuild dist), then the prescribed border-check heredoc pasted verbatim into `## ОТЧЁТ`, then the visual gate (screenshot of dist/index.html — Разгон block, calendar still one screen), then publish per `kod_deplo-sajta.md` §Б: `git_zona.py opublikovat` (dry-run first, i.e. without `--yes`, to see what would move, then `--yes`) from the main folder — the command publishes `sayt/` of this same repo; I'll confirm in the dry-run that nothing outside the course site's own path is touched before running `--yes` (Б3's stop condition). I will disregard kod_deplo-sajta.md's own criterion "3" ("git diff по god.json пуст") — that line was written for a *different* zahod where god.json must NOT change; here changing god.json is the entire point, and my own zahod's criteria (§КРИТЕРИЙ ГОТОВНОСТИ above) govern instead.

**§3 Verifier.** Fresh subagent, separate code path: reads `RESHENIYA.md` itself, independently walks all 10 blocks + 7 literatura entries (control against false positives) + 4 kружок tracks (confirm probability NOT wiped, Р108), reports coverage as "N of M".

**Commits.** Per §4, as each part lands (content revision, guard, dist rebuild/publish), not one final commit — two-step `add` then `commit -- <paths>` each time, `check --zone` after each.

Flagging in `## ВОПРОСЫ`: (a) the `opornye` leftover above; (b) the stale "Подсчётов 2" reference inside the "Информация" sluzhebnoe paragraph (not touched, outside the four named fixes).


## ВОПРОСЫ — (заполняет исполнитель)

1. This zahod's own opening chat message and its own §0.1 disagree on the order of the first moves — see `## УРОКИ ФАБРИКЕ` above for the full incident. Factory-level recurrence, годная другим заходам.
   ДОМ: diskmat-57/2026-08-18_logika-goda-7/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. `god.json`'s "Разгон" block still carries Р50-banned content in its `opornye` field ("уксусная эссенция 70% и разбавление · смешать 10% и 15% растворы до 12% · блондины и голубоглазые") — this is the exact percent/mixture content the owner originally spotted on the published page. My task's revision (А2) and the new guard (Б1) are both explicitly scoped by this zahod to `imya`/`podzag`/`karta` only, so I left `opornye` untouched; a fresh subagent verifier independently found the same thing without being told about it. `opornye` is shown to readers on the site ("опорные точки" section of the block page), so this is real, live drift — just outside the boundary this zahod drew. Owner's call whether to widen the revision/guard scope to `opornye` too.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. `god.json`'s `sluzhebnoe` field still says "Кодировка после Подсчётов 2" (now fixed to "Информация после Подсчётов 2" per this task's instruction) — but "Подсчётов 2" itself is a stale block name (no block called "Подсчёты 2" exists; the block right before "Информация" in the III-quarter raskladka is "Дирихле"). Task instruction only asked for the Кодировка→Информация rename, so I left the "Подсчётов 2" reference as-is. Separately, the verifier flagged (informational, not a violation): `utok` entry "Пошаговое построение" has `"dom": "Информация"`, which reads oddly against Р70 ("пошаговое конструирование выделяется отдельно — на КРУЖКЕ") — but the "Информация" block's own karta text never says "пошаговое"/"конструирование", so nothing is visibly wrong on the site; `utok` is outside this task's guard scope by design (Р108/Р109 boundary covers kружок/literatura/sluzhebnoe/utok).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> 🔴 **`ДОМ:` — ОБЯЗАТЕЛЬНОЕ ПОЛЕ, И АДРЕС В НЁМ ОБЯЗАН СУЩЕСТВОВАТЬ В МОМЕНТ, КОГДА ТЫ ЕГО ПИШЕШЬ.** Путь, которого нет на диске, — не адрес: такую запись нельзя ни доставить, ни спросить, и она не чинится ничем. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц17) Проверить СВОЙ файл до отчёта — одна команда:
> ```
> python3 _generator/tools/bootstrap_zahod.py --proverit-doma <этот файл>
> ```
> rc=0 — все дома достижимы; rc=1 — назван дом, которого нет (команда печатает какой именно). Тот же разбор гоняет `Г7` приёмки, и у него храповик: у ЭТОГО захода база 0, поэтому первый же недостижимый дом здесь — красный на приёмке, а не запись, которую через неделю никто не найдёт.
> `ДОМ: владелец` — законный адрес и НЕ недостижимый дом: он значит «дома-файла нет вовсе, решение за человеком». Не знаешь пути — пиши его, а не выдуманный путь. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит и «доставлено» без метки на месте, и недостижимый дом сверх базы; достижимое-недоставленное печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц18)
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** — §0.1 point 1 explicitly told me: "Звать субагента не за чем — выполни САМ две команды", i.e. this zahod substitutes its own two-command self-check for the usual git-контур subagent sweep. Ran BEFORE creating the worktree (per §0.1's own required order — see the `## УРОКИ ФАБРИКЕ` incident above for why this had to be redone once):

```
$ git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'
0
$ (first attempt, wrong flag syntax — see below)
```

Second command, first attempt used a single `--zone` with a space-separated path list (the syntax literally written in §0.1's own code block) — that form is rejected by `git_zona.py`'s actual argparse (`--zone` must repeat, one path each); re-ran with the correct repeated form:
```
$ python3 .../git_zona.py check --zone diskmat-57/_sajt/god.json --zone diskmat-57/_sajt/build.py --zone diskmat-57/_sajt/dist --zone sayt/static/diskmat-57 --zone diskmat-57/docs --zone diskmat-57/2026-08-18_logika-goda-7
❌ зона ...: вне git 7 путей (рождено и никогда не ставилось — 3, правлено и не закоммичено — 4)
  ?? kod_revizia-i-storozh.md (mine, expected, not to be committed by me)
  ?? kod_sajt-cveta-i-setka.md / .otmenen (analyst's, uncommitted)
   M SESSIYA.md / UROKI-FABRIKE.md / KONCEPCIA-goda.md / RESHENIYA.md (analyst's, uncommitted)
```
This ❌ is exactly the incident in `## УРОКИ ФАБРИКЕ`: §0.1's own "НУЛЕВОЙ ХОД — ГИГИЕНА" instruction (task section, not this hygiene block) covers precisely this — one hygiene commit from the main folder (`5f1f96e3`), content untouched, then fast-forward my already-created worktree branch onto it. Re-ran the same check afterward from the worktree: ✅ (pasted under `## ОТЧЁТ` commit trail below).

`git_zona.py zayavki` (run once content work was done, to check the queue before wrap-up): `✅ заявок нет` (0 open items addressed to the git-контур; the 12 listed entries are all redirected `на-зahod`/other zahod owners' queues, unrelated to this zone).

`log --oneline @{u}..` on the branch that existed at true session start (`arka/mat-kostyak`, since the worktree branch didn't exist yet) wasn't captured as a literal command at that exact moment — inferred as 0 from the fact that `vyvezti`'s later dry-run showed local `arka/mat-kostyak` exactly 4 commits ahead of `origin/arka/mat-kostyak`, and all 4 are mine (the hygiene commit plus my three content/guard/dist commits) — nothing pre-existed unpushed.

**ЧТО СДЕЛАНО**
- Hygiene commit `5f1f96e3` (main folder, before worktree creation) — picked up analyst's uncommitted session work (KONCEPCIA-goda v2, RESHENIYA v2, diary SESSIYA, UROKI-FABRIKE, `kod_sajt-cveta-i-setka.md` + `.otmenen` pair), content untouched.
- Worktree `zahod/revizia-i-storozh` fast-forwarded onto that commit (`git merge --ff-only`), clean (branch hadn't diverged).
- My branch (3 content/guard/dist commits, see `## ОТЧЁТ` КОММИТ below) merged into `arka/mat-kostyak` via `git_zona.py vlit-v-osnovnuyu` (no conflicts) — done mid-task, earlier than the WARNING block's literal last-move framing, because `git_zona.py opublikovat` (Часть Г) reads `sayt/` off whatever the main folder has checked out, which only works post-merge.
- `arka/mat-kostyak` (4 commits ahead of origin) pushed via `git_zona.py vyvezti --yes` — ✅.
- Zero unmerged `zahod/*` branches confirmed after (`git branch --no-merged arka/mat-kostyak` empty).
- Worktree dropped (`git_zona.py worktree drop revizia-i-storozh`) — branch itself kept (its work is already merged and pushed).

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет` — one item stays open, and it is not mine to close:
- 4 paths still uncommitted in the main folder as of this report (`_studio/zhurnal/_INFRA-git/INCIDENTY.md`, and three more `M`/`??` paths under `diskmat-57/docs`/`diskmat-57/2026-08-18_logika-goda-7` — SESSIYA.md, KONCEPCIA-goda.md, RESHENIYA.md carry NEW analyst edits that appeared *after* my hygiene commit, made live during this same session; `kod_revizia-i-storozh.md` itself stays uncommitted by this zahod's own contract). This is the analyst's own in-progress content, authored while I was mid-task — not something I can commit blindly without knowing whether it's finished. Named here per the WARNING block's own guidance ("чужая содержательная работа — НЕ твоя: называешь строкой в отчёте и оставляешь"), left untouched.

## ОТЧЁТ — (заполняет исполнитель)

**ПРАВКИ ПРОЧИТАНЫ:** 1 — `## ПРАВКИ ПОСЛЕ ВЫДАЧИ` was empty at start and re-checked empty right before I first drafted this report; ПРАВКА 1 appeared on a subsequent re-read (triggered by the file being modified on disk mid-session), and the owner separately confirmed it in chat. It reverses "Часть В — КРУЖОК В ЭТОМ ЗАХОДЕ НЕ ТРОГАТЬ" entirely and adds the kружок weekly-grid entry as new, additive work — done below, nothing rolled back.

**What was done, and why.** Часть А (content): removed Р50-banned "доли и части" from `god.json`'s "Разгон" block (karta item + podzag), and — a finding of my own, not pre-named by the zahod — removed the literal word "граф" (banned by Р64) from the "Игры" block's podzag and karta, reworded to "разметка позиций" (reusing the block's own existing vocabulary, no new content invented). Added `"domeny": ["K","G"]` to "Двойной счёт" per the owner's 14.09 instruction. Rewrote 4 stale `sluzhebnoe` passages (probability-block paragraph deleted, arithmetic-hours corrected 10→17, "Кодировка"→"Информация" rename, "Дирихле ушло на кружок" claim dropped since Р105 returned it to the main program too) plus the separate Р111 rule-flip replacement. Audited all 10 `blok` elements against an 11-decision removal list I built myself from reading all 122 rows of `RESHENIYA.md` (not taken on faith from the zahod's own opornye list) — the other 8 blocks were already clean. Часть Б (guard): added `storozh_otmenennogo()` + a readable `ZAPRET_OTMENENNOGO` list (16 patterns covering all 11 decisions, word-boundary-safe so "граф"/"бином" don't false-positive inside "график"/"биография") to `build.py`, wired into the existing `--proverit` path (not a separate command). Часть Г: rebuilt `dist/`, ran the mandatory border-check script verbatim, mirrored `dist/` → `sayt/static/diskmat-57/` (the pre-existing convention — confirmed by diffing the two trees, only the changed pages differed), and published via `git_zona.py opublikovat --yes`.

**ПРАВКА 1 (post-report discovery):** entered the kружок weekly grid into `god.json`'s 4 `kruzhok` elements, per the tables pasted in ПРАВКА 1 itself — cross-checked those tables against the live (uncommitted, per §0.1's own hygiene note) `diskmat-57/docs/KONCEPCIA-goda.md` §4 "Недельная сетка кружка" and found them byte-identical, no discrepancy to report. For each quarter: `chasy` fixed to the calendar-correct 6/8/11/8 (was 8/8/10/7); `temy` replaced with the new theme rows (`imya`=table's theme name, `chto`=same text — no invented elaboration beyond the table, per ПРАВКА 1's own "ничего не сочиняй сверх таблиц"; `domen` set from the "цвет" column where a color is given, omitted entirely for the 5 "form" weeks — разнобой/олимпиада×2/аукцион/заключительная — per ПРАВКА 1's explicit instruction "оставь поле пустым или не ставь вовсе"); `podzag` rewritten as the new theme names joined by " · ". One `literatura` fix, exactly as instructed and nothing more: `shap-primer`'s `nazvanie` → "Математические конструкции: от хижин к дворцам" (`ssylka`/`gde`/`chto` left as-is, per ПРАВКА 1's own "больше в literatura ничего"). `karta` on the kружок elements was not touched — ПРАВКА 1 only names `chasy`/`temy`/`podzag`.

**A real bug, found by the live rebuild, not invented:** the first rebuild attempt crashed — `build.py`'s `lenta_kruzhka()` unconditionally read `tema['domen']`, which the 5 domain-less "form" weeks don't have (by ПРАВКА 1's own design). Fixed minimally: `tema.get('domen')`, falling back to the existing neutral `--ktr` grey (already used elsewhere in this file for content-less slots — контрольная/пусто) and omitting the domain-label span when there's no domain, rather than inventing new visual language. Confirmed on the rebuilt `dist/` and the live site: form weeks render as a plain grey card, no domain label, no crash.

**Owner feedback round (after seeing the published кружок tab, screenshots attached in chat):** the domain-less form weeks (разнобой/олимпиада/аукцион/заключительная) were still using the full-size colored `blok kr-chast` card markup (just recolored grey), and their titles were getting clipped/wrapped illegibly in the narrow 1-week columns — the owner asked for the same narrow-box "on its side" treatment already used for контрольная. Fixed by rendering these specific entries via the existing `.ktr` class (same markup contрольная itself uses) instead of `.blok kr-chast` — one conditional branch in `lenta_kruzhka()`, nothing else touched. Verified on rebuild and the republished live site: all 5 form weeks (including the long "олимпиада, тренировочная перед Матпраздником") now render fully legible, matching контрольная's own visual language exactly; colored theme weeks are untouched.

**How verified.** Guard tested both directions with exit codes (§Б2): rc=1 on the pre-revision `god.json` from git history (Разгон AND Игры both named), rc=0 on the fixed version. The zahod's own prescribed border-check heredoc run verbatim (§Г item 3) — 0 discrepancies, and its second half (probability-on-kружок check) confirms I did not over-clean: 2 of 4 kружок tracks still carry "вероятность" (Р108 intact). Visual gate: screenshotted the rebuilt `dist/index.html` locally, then the live published page — Разгон podzag reads "обратный ход · рыцари, лжецы и отрицание" (no percent/fractions), Игры podzag reads "...разметка позиций..." (no "граф"), calendar still one screen per quarter, week numbering runs 1–33 unbroken. Independent verifier (fresh subagent, own code, own re-read of `RESHENIYA.md`, not shown my git history) confirms: 10/10 blocks clean, 7/7 literatura entries intact, 4/4 kружок tracks checked with probability confirmed present — "выдано 8 позиций из 8 найденных". Post-merge mechanism check from the main folder (not the worktree): `build.py --proverit` green, `grep` confirms `storozh_otmenennogo` is live in the call path.

**ПРАВКА 1 verification (§5 in the правка itself):**
```
часы по четвертям: [6, 8, 11, 8]
вероятность на кружке: 2 (обязано быть >0, Р108)
РАСХОЖДЕНИЙ 0
```
(script: per-quarter `chasy` compared against `[6,8,11,8]`, and per-track `sum(nedel)` compared against that track's own `chasy` — both computed from the live `god.json`, not asserted.) The original §Г border-check script (verbatim, unchanged) and `build.py --proverit` were both re-run after this change too — same 0-discrepancy, green result as before (Разгон/Игры still clean, storozh_otmenennogo still green, kружок probability still present). Visual gate (правка §6): switched the tumbler to "кружок" — all 4 quarters render, week numbering continues 1–33 with no wrap, colors match the pair's domain wherever a "параллель" is named in the table (checked all 9: Разгон/Чётность/Делимость/Двойной счёт/Правило произведения/Дирихле/Информация/Остатки/Игры), and the 5 domain-less form weeks (разнобой/олимпиада×2/аукцион/заключительная) render as plain neutral-grey cards with no domain label — confirmed on both the local rebuild and, after republishing, the live site.

**What was NOT touched:** kружок's `chasy`/`temy`/`podzag` WERE touched, per ПРАВКА 1 (which explicitly cancels Часть В's original read-only boundary) — but its `karta` field was left exactly as before, since ПРАВКА 1 only names `chasy`/`temy`/`podzag`. `literatura` — untouched except the ONE explicit exception ПРАВКА 1 names (`shap-primer`'s `nazvanie` only; `ssylka`/`gde`/`chto` left as-is). `sluzhebnoe` — only the 4 named passages + the Р111 rule sentence changed; the rest (Чётность-ordering rationale, Двойной-счёт-after-чётность, "Соответствие подаётся молча", "Игры: тема алгоритмизуема...", "Дыры без источника") is byte-for-byte as before. `utok` — untouched entirely (out of guard scope by design, and ПРАВКА 1 doesn't mention it). `Разгон.opornye` and the "Подсчётов 2" reference in sluzhebnoe — deliberately left, see `## ВОПРОСЫ` #2 and #3 (outside this task's literal `imya`/`podzag`/`karta` scope; ПРАВКА 1 doesn't widen this). *(Note: the independent verifier ran BEFORE ПРАВКА 1 landed, so its "kружок untouched / literatura 7/7 intact" finding is a correct snapshot of the pre-правка state, not a stale claim about the final state.)*

**ПОВТОРЯЕМОСТЬ:** the order-of-first-moves conflict (`## УРОКИ ФАБРИКЕ` above) WILL recur on the next zahod launched the same way — it already recurred once before mine (the prior `god-json-v2` incident, already logged in `RESHENIYA.md` itself, did not fix the generating template). I cannot fix it myself: the contradiction is baked into `bootstrap_zahod.py`'s chat-message template and §0.1's own generated body, both inside the `disciplina` repository, entirely outside this zahod's zone (`diskmat-57/**` only). Flagged as ВОПРОСЫ #1 with `ДОМ: UROKI-FABRIKE.md` rather than treated as a queue item alone, since per `RUKOVODSTVO-zahodami.md`'s "класс НЕМЕДЛЕННОЕ" rule a recurring finding should be a fix-zahod, not a queue line — but I have no zone to run that fix myself; naming it clearly is what's in my power. The `opornye`/"Подсчётов 2" leftovers (ВОПРОСЫ #2, #3) are one-off content gaps, not expected to recur mechanically.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/_sajt/dist/index.html` — open in any browser (double-click or `file://` in an address bar). Live published copy: https://d1-d57.github.io/materials/diskmat-57/ (confirmed open and correct — screenshotted above).

**РОД АРТЕФАКТА:** собранный (dist/ + the live published page are both younger than this zahod's own file).

**НЕОБРАТИМОЕ:** three items, all site publishes — (1) `git_zona.py opublikovat --yes`, `main` показ-branch commit `5bce8b45d26f` (Часть А/Б/Г, first pass); (2) same command after ПРАВКА 1's kружок grid, `main` commit `52ad01828c80`; (3) same command again after the owner's form-week visual feedback, `main` commit `022b0292ccc0`. All three are the explicit, owner-pre-authorized deliverable of Часть Г (kod_deplo-sajta.md §Б3: "Публикация — необратимое действие... Владелец дал на неё явное согласие"), each scoped exactly to the course site's own changed pages (dry-run diff checked before every `--yes` — 4 files, then 6, then 1, nothing else touched, so Б3's stop condition never triggered). Restorable by re-publishing a prior `dist/` state from git history (`2d1a22ad` pre-run, `ad4dc87b` pre-ПРАВКА-1, or `de1993c9` pre-visual-fix) if ever needed. Nothing else in this run was destructive — all git operations were additive commits, one clean fast-forward merge, and three conflict-free merges into main; no `reset --hard`, no force-push, no file deletions outside the routine `dist/` rebuilds (which regenerate deterministically from `god.json`).

**КОММИТ:**
- `ef4ec6ae` — "diskmat-57 god.json: revise Разгон/Игры against RESHENIYA.md removals..." (Часть А)
- `30182169` — "diskmat-57 build.py: add storozh_otmenennogo (P110)..." (Часть Б)
- `ad4dc87b` — "diskmat-57 site: rebuild dist/ and mirror to sayt/static/diskmat-57..." (Часть Г, first publish)
- `fb551ff9` — "diskmat-57 god.json/build.py: enter kружок weekly grid per PRAVKA 1..., fix lenta_kruzhka() crash..." (ПРАВКА 1)
- `de1993c9` — "diskmat-57 site: rebuild dist/ and mirror to sayt/static/diskmat-57 after kружок weekly grid (PRAVKA 1)" (second publish's source commit)
- `68ae78b6` — "diskmat-57 build.py: kружок form weeks... render as narrow .ktr boxes like контрольная..." (owner feedback on published кружок tab)
- `653be14f` — "diskmat-57 site: rebuild dist/ and mirror to sayt/static/diskmat-57 after kружок form-week visual fix" (third publish's source commit)
- (hygiene, §0.1, main folder before worktree) `5f1f96e3` — "hygiene: commit analyst's uncommitted session work..."
- All 8 merged into `arka/mat-kostyak` via `git_zona.py vlit-v-osnovnuyu` (three merge operations, no conflicts any time), pushed via `git_zona.py vyvezti --yes` (three times: 4-ahead→0, 2-ahead→0, 2-ahead→0).
- `git_zona.py check --zone diskmat-57/_sajt/god.json --zone diskmat-57/_sajt/build.py --zone diskmat-57/_sajt/dist --zone sayt/static/diskmat-57 --zone diskmat-57/docs --zone diskmat-57/2026-08-18_logika-goda-7` → ✅ (all three times, run from the worktree before each merge).

**§4.1 ГИГИЕНА checklist:**
- В1 (zone in git) — ✅, see КОММИТ above.
- В2 (second repo) — неприменимо: all zone paths live inside `materials/`.
- В3 (unmerged branches didn't grow) — input 0, final 0 (`git branch --no-merged arka/mat-kostyak` empty both times).
- В4 (new tool has a call site) — неприменимо: no new `.py` file, only edits to the existing `build.py`.
- Г5 (new `.md` registered) — неприменимо: I created no new `.md` this run.
- Г6 (no foreign paths in my commits) — checked all commits with `git show --stat`: each carries only its own declared paths.

**WARNING-block wrap-up (§ПОСЛЕДНИЙ ХОД) — done THREE times, once per publish (first pass, after ПРАВКА 1, after the owner's visual feedback):**
1. ВСЕ КОММИТЫ — my zone: 0 outside git, all three times. Main folder overall: 4 non-zone paths still dirty (as of this report), all analyst's own live work (named above under ГИГИЕНА ВХОДА / "ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ: нет"), left untouched.
2. ВЛИТИЕ — done mid-task all three times (required for Часть Г's publish step to work at all — see ЧТО СДЕЛАНО above for why the order deviates from the block's literal "last move" framing); `vlit-v-osnovnuyu` reported no conflicts any time.
3. ПОСТ-ПРОВЕРКА из главной — `build.py --proverit` green from `/Users/ivanyakovlev/Documents/GitHub/materials` (not the worktree) all three times + `grep` confirms `storozh_otmenennogo` live in `build.py`'s call path. Green every time, no rollback needed.
4. ГАШЕНИЕ — `git branch --no-merged arka/mat-kostyak` empty every time; worktree dropped and recreated twice more (idempotent, existing branch) for the extra rounds of work, dropped for good at the very end via `git_zona.py worktree drop revizia-i-storozh` (branch itself kept, per the tool's own message: "её работа никуда не делась" — already merged and pushed, safe to leave named).
5. ВЫВОЗ — pass 1: `arka/mat-kostyak` 4 ahead of origin, pushed → 0. Pass 2 (ПРАВКА 1): 2 ahead, pushed → 0. Pass 3 (visual fix): 2 ahead, pushed → 0.
6. Numbers, by command, all above — nothing rolled back at any point.

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

### ПРАВКА 1 · 2026-09-14 17:30 · кружок ПЕРЕСТАЁТ быть read-only — вносишь его недельную сетку

🔴 **Эта правка ОТМЕНЯЕТ «Часть В — КРУЖОК В ЭТОМ ЗАХОДЕ НЕ ТРОГАТЬ» целиком.** Там написано, что кружок правится отдельным заходом; это больше не так. Причина: сетка кружка за это время согласована с владельцем полностью, она лежит в том же `god.json`, что ты уже правишь, и отдельный заход означал бы вторую пересборку и вторую публикацию того же сайта. Владельцу нужна промежуточная версия программы, чтобы разослать причастным.

**Ничего откатывать не надо.** Правка только ДОБАВЛЯЕТ работу; всё, что ты уже сделал по частям А, Б и Г, остаётся в силе. Если ты ещё не дошёл до пересборки — тем лучше, внеси кружок до неё, одной сборкой.

🔴 **Сетка вложена сюда целиком, и это не дублирование, а необходимость.** Её дом — `diskmat-57/docs/KONCEPCIA-goda.md`, раздел 4, подраздел «Недельная сетка кружка». Но на момент этой правки раздел лежит в главной папке НЕЗАКОММИЧЕННЫМ: `git show HEAD:diskmat-57/docs/KONCEPCIA-goda.md | grep -c 'Недельная сетка кружка'` даёт **0**, а в рабочем дереве главной папки — **1**. Если твой нулевой коммит гигиены уже прошёл, раздел у тебя есть и он совпадает с таблицами ниже; если нет — работай по таблицам отсюда. **Разошлись — доверяй `KONCEPCIA-goda.md` и назови расхождение в `## ОТЧЁТ`.**

Колонка «параллель» в таблицу `god.json` не идёт — это пояснение, что в тот же вторник у класса на паре. Колонка «цвет» идёт полем домена.

**I четверть — 6 недель** · пара: Разгон · Перебор и правило суммы · Чётность

| нед | кружок | цвет | параллель |
|---|---|---|---|
| 1–2 | логика со звёздочкой | методы | Разгон, Перебор |
| 3 | конструкции: мнимые противоречия | методы | — |
| 4–5 | раскраски и замощения | числа | Чётность |
| 6 | разнобой | — | конец четверти |

**II четверть — 8 недель** · пара: Делимость · Двойной счёт

| нед | кружок | цвет | параллель |
|---|---|---|---|
| 7–8 | размен монет: делимость со звёздочкой | числа | Делимость |
| 9–10 | оценка, пример, принцип крайнего | методы | — |
| 11–13 | инвариант и подсчёт двумя способами | подсчёты | Двойной счёт |
| 14 | олимпиада | — | конец четверти |

**III четверть — 11 недель** · пара: Правило произведения · Дирихле · Информация

| нед | кружок | цвет | параллель |
|---|---|---|---|
| 15–18 | изоморфизм задач, перебор со звёздочкой | подсчёты | Правило произведения |
| 19–21 | Дирихле | методы | Дирихле |
| 22–23 | взвешивания и мудрецы | подсчёты | Информация |
| 24 | аукцион | — | конец четверти |
| 25 | олимпиада, тренировочная перед Матпраздником | — | конец четверти |

**IV четверть — 8 недель** · пара: Остатки · Игры

| нед | кружок | цвет | параллель |
|---|---|---|---|
| 26–28 | арифметика и вероятность со звёздочкой | числа | Остатки |
| 29–30 | шахматы и игры | графы | Игры |
| 31–32 | игры: процессы и полуинвариант | графы | Игры |
| 33 | заключительная олимпиада | — | конец года |

Ниже — то, что важно не упустить при переносе таблиц в файл.

**1. Часы дорожек кружка привести к календарю: I — 6, II — 8, III — 11, IV — 8.** Сейчас стоит 8 · 8 · 10 · 7. Сумма 33 в обоих случаях, но по четвертям расходится: кружок идёт один урок в неделю (Р83), значит его часов в четверти обязано быть ровно столько, сколько вторников. Сумма `nedel` по темам каждой дорожки обязана сойтись с её `chasy`.

**2. Состав тем — по таблицам раздела 4.** Каждая строка таблицы это одна тема с полем `nedel`, равным числу недель в строке. Формы занятия (разнобой, олимпиада, аукцион) — тоже темы, по одной неделе каждая, и стоят они в КОНЦЕ четверти (Р114).

**3. Домены кружковых тем — по колонке «цвет» тех же таблиц** (Р115): там, где идёт параллель с парой, цвет совпадает с цветом блока пары. `методы` → `M`, `числа` → `A`, `подсчёты` → `K`, `графы` → `G`. У форм занятия домена нет — оставь поле пустым или не ставь вовсе, как принято в файле для элементов без оси.

**4. Подзаголовки дорожек (`podzag`) переписать** под новый состав: сейчас они перечисляют старые темы.

🔴 **Границы, нарушать нельзя:**

- **Конкретные игры НЕ фиксируй как обязательства** (Р116). В сетке стоят «шахматы» и «мудрецы» — это примеры, они могут смениться при отборе задач. Пиши их как есть, но НЕ дописывай к ним названий игр от себя и не раскрывай «игры» в конкретный список.
- **Ничего не сочиняй сверх таблиц.** Если в таблице неделя названа крупно — так и оставь крупно. Дробность это уровень листка, а не программы (Р113).
- **`literatura` по-прежнему не трогать** (Р109).
- Одно исключение в `literatura`, и только оно: **запись `shap-primer` называет книгу «Как построить пример?», которой у нас нет** (по реестру 404), **а на диске лежит другая книга того же автора и жанра — «Математические конструкции: от хижин к дворцам», 177 страниц** (`_fond/biblioteka/shapovalov-konstrukcii.pdf`), и карта кружка ссылается именно на неё. Поправь `nazvanie` этой записи на «Математические конструкции: от хижин к дворцам». Больше в `literatura` ничего.

**5. Критерий готовности дополняется.** К тому, что уже есть, добавь проверку и вставь её вывод в `## ОТЧЁТ`: часы дорожек кружка по четвертям равны `[6, 8, 11, 8]`; у каждой дорожки сумма `nedel` по темам равна её `chasy`; вероятность на кружке НЕ исчезла (проверка на перечистку из прежнего критерия остаётся в силе — Р108).

**6. Визуальный гейт дополняется.** На собранной странице переключи тумблер на вкладку «кружок» и посмотри: четверти не переносятся на вторую строку, цвета кружковых недель совпадают с цветами пары там, где параллель заявлена. Опиши в отчёте, что увидел.

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — приёмка перегнала всё САМА, по живому файлу и по живой странице. Часы кружка `[6,8,11,8]`, у каждой дорожки сумма `nedel` равна `chasy`; основная программа не поехала — `[12,16,22,16]`; вероятность на кружке НА МЕСТЕ (проверка на перечистку зелёная, Р108); отменённого в блоках ноль; правило «кружку её отдавать нельзя» из `sluzhebnoe` снято (Р111); `shap-primer` теперь «Математические конструкции: от хижин к дворцам» — та книга, что лежит на диске. Охват сплошной: 10 блоков из 10, 4 дорожки кружка из 4, 4 четверти из 4.

🔴 **Сторож проверен приёмкой ОБЕИМИ сторонами, а не по отчёту.** Я импортировал `storozh_otmenennogo` из живого `build.py` и прогнал его сам на двух состояниях: на версии `58b51eb2` (до ревизии) — **3 находки**, все верные; на живой — **0**. Сторож умеет краснеть, а не только зеленеть. Он печатает охват («блоков проверено 10 из 10 · запретов в списке 16») и честно печатает свои слепые зоны («подстроки, не смысл · отменённое другими словами не увидит · кружок, literatura, sluzhebnoe, utok вне области — решение, не недосмотр»). Обе клаузы критерия выполнены буквально.

**Визуальный гейт проверен приёмкой отдельно**, на опубликованной странице при 1800×1000: вкладка «кружок» — 17 элементов, все четыре четверти по 22 колонки в ОДНУ строку, горизонтального скролла нет, высота 1047. Цвета кружковых недель совпадают с цветом пары: методы `#7d4f78`, числа `#b25f3c`, подсчёты `#5f7f4a`, графы `#4a7292`, формы серые.

**Три красных гейта, все разобраны, ни один не про качество работы:**

1. **Г0** краснеет на `diskmat-57/docs` и папке арки — там незакоммиченная работа АНАЛИТИКА, написанная уже после нулевого коммита гигиены этого захода (раздел «Недельная сетка кружка», решения Р113–Р118, дневник). Исполнитель их видеть не мог.
2. **Г3** — отчёт назвал в строке `КОММИТ:` хэш `ef4ec6ae`, а `dist/index.html` приехал в `de1993c9`. Проверено командой: файл в git, рабочая копия совпадает с HEAD, сборка свежая, сетка кружка в собранной странице есть. Дефект формы отчёта, не работы.
3. **Г12** — в отчёте нет дословного вывода `branch --no-merged`. §0.1 отменён клапаном, гейт честно говорит, что отличить «отменён» от «не сделал» по тексту не может. Проверено руками: невлитых `zahod/*` нет, ветка влита (Г14 зелёный).

**ПРАВКА 1 прочитана и исполнена** — строка `ПРАВКИ ПРОЧИТАНЫ: 1` на месте, кружок внесён той же сборкой, откатывать ничего не пришлось.

🔴 **ОДНА НАХОДКА ПРИЁМКИ, КОТОРУЮ РЕШАЕТ ВЛАДЕЛЕЦ — ПЕРЕЧИСТКА.** Собирая список отмен сам (как и велел заход), исполнитель добавил в сторож запрет на слово «граф» по Р64 — и вычистил его из блока «Игры»: подзаг `граф позиций` стал `разметка позиций`, карта `граф позиций: вершины — позиции, стрелки — ходы` стала `разметка позиций: вершины — позиции…`. Формально Р64 это и говорит: «рисуем, но слова не произносим».

Но тогда несогласованность: **ось в `kurs.domeny` называется `"G": "графы"`, и слово «графы» подписано на сайте под каждым графовым блоком.** Одно слово вычищено, другое стоит рядом крупным шрифтом. Похоже, Р64 про РЕЧЬ НА ЗАНЯТИИ — что говорят детям, — а не про внутренний документ программы, который читают владелец и ассистенты. Если так, «граф позиций» надо вернуть, а «граф» убрать из списка сторожа. **Исполнитель тут не виноват: он исполнил инструкцию буквально и правильно; неопределённой оказалась область действия Р64.** Вопрос владельцу, отдельной строкой.

**ВЕТКА РАБОТЫ:** `zahod/revizia-i-storozh`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

заявок нет: заход сам закоммитил зону, влил ветку, вывез на origin и опубликовал сайт; отказов git-операций не было.

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
