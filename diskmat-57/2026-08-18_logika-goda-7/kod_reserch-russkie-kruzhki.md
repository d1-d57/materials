# Канал исполнителя — reserch-russkie-kruzhki (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
<!-- гейт сборки заполненного: ЖДЁТ -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — заход-закупка: скачать, разложить, выписать оглавления; суждений нет, план расписан по шагам.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — заход-закупка: скачать, разложить, выписать оглавления. Суждений нет, план расписан по шагам.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/2026-08-18_logika-goda-7/kod_reserch-russkie-kruzhki.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: git branch --show-current, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-13, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py diskmat-57/2026-08-18_logika-goda-7`
Область: «diskmat-57/2026-08-18_logika-goda-7» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — ни одного skills/*/DOLG.md нет на диске (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 10
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 16
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 11
       живых (чинится доставкой — «дом есть»)  : 2
       к владельцу (решение за человеком)      : 3
       адрес недоступен (нет/папка/код/указат.) : 0
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 6
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

КОНТЕКСТ. Проект — четырёхлетний спецмат «Основы дискретной математики и теории вероятности» в 57-й школе; сейчас собирается логика 7 класса: 34 занятия по полтора часа, 11 блоков, программа лежит данными в `diskmat-57/_sajt/god.json`. Прошлый этап: год собран, но 13.09 владелец снял из него вероятность (у учительницы класса есть свой курс), проценты и дроби, и решил усилить комбинаторику и арифметику — освободилось 9–11 часов, а чем их закрывать, неизвестно. ЦЕЛЬ: собрать в одном месте лучшие русские методические источники по кружку 6–7 класса и выписать, что в них есть, — чтобы владелец и аналитик дальше решали по оглавлениям, а не по памяти.

Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 СТОП ДО ЦЕЛИ: ты получишь ПОЛКУ И РЕЕСТР, но НЕ анализ и НЕ решения. Что из этого войдёт в курс, куда встанет и чего не хватает — следующий разговор владельца с аналитиком, не твой ход.

🔴 ИСКЛЮЧЕНИЕ к правилу «все файлы по-английски» (§2): артефакт этого захода — документ русского школьного курса и живёт среди русских документов проекта, поэтому `RESERCH-russkie-kruzhki.md` пишется ПО-РУССКИ. По-английски остаются `## ПЛАН`, `## ВОПРОСЫ`, `## ОТЧЁТ` и сообщения коммитов.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-13) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Вероятности как блока в 7 классе нет (Р49); освободившиеся 8 часов идут в комбинаторику и арифметику, мудрецы и шахматы на кружок (Р58)
2. Проценты и дроби убраны полностью, курс на целых числах (Р50); Разгон остаётся блоком на обратном ходе и логике (Р59)
3. Порядок ресёрча: сначала ГОД по чужим программам, потом линейки, потом первая четверть (Р62); глубина по умолчанию — оглавления (Р60)

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** рабочая папка `/Users/ivanyakovlev/Documents/GitHub/materials-wt/reserch-russkie-kruzhki`, ветка `zahod/reserch-russkie-kruzhki`. 🔴 **Папку заводишь ТЫ САМ первым ходом, владелец в терминал ничего не вставляет:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add reserch-russkie-kruzhki --branch zahod/reserch-russkie-kruzhki`, затем `cd` в неё. Команда идемпотентна: папка уже есть — просто перейди, это не ошибка. 🔴 Ветка должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `diskmat-57/docs/RESERCH-russkie-kruzhki.md` `diskmat-57/2026-08-18_logika-goda-7/kod_reserch-russkie-kruzhki.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *(история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц1) Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/reserch-russkie-kruzhki/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц2)
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц3).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает названные через `--vlit` ЧУЖИЕ ветки В ОСНОВНУЮ, и только ПОТОМ ты заводишь свою рабочую папку; СВОЮ ветку он не трогает никогда — её вливаешь ты сам последним ходом (граница прав ниже).** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц5) Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. 🔴 ПОРЯДОК ПРИ ПАДЕНИИ ЭТОГО ВЫЗОВА — исполняй, не изобретай (движок роняет `network_error` на вызове субагента и уносит с собой ВЕСЬ заход, замер волны 3A — 4 захода из 13). Пауза 45 секунд, до трёх попыток; время меряй `date`, не суммой своих `sleep`. После третьей — работай БЕЗ субагента: контур остаётся неразобранным, и это ОТДЕЛЬНАЯ строка в `## ОТЧЁТ`, а не молчание. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты сам, по ходу работы (§4). 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone diskmat-57/docs/RESERCH-russkie-kruzhki.md --kommitit 'diskmat-57/docs/RESERCH-russkie-kruzhki.md — единственный файл зоны' --zakryt 'ветку не закрывать: перенос и закрытие делает аналитик при приёмке'
```

🔴 ГРАНИЦА ПРАВ, ЧЕТЫРЕ ОТВЕТА (та же, что в самом задании субагенту — одно место в тексте, а не пересказ): **кто вливает ЧУЖИЕ названные (`--vlit`) ветки** — субагент, в ОСНОВНУЮ ветку, до заведения твоей папки; **кто вливает СВОЮ ветку этого захода** — ты сам, последним ходом, после коммита зоны (`git_zona.py vlit-v-osnovnuyu`; (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц6)); **кто закрывает заявки** — субагент, `zayavka-zakryt`; **кто коммитит пути ВНЕ зоны захода** — субагент (хвост Cowork и что назовёт пункт 3 его задания). Ты коммитишь ТОЛЬКО зону этого захода, по ходу работы (§4). 🔴 Конфликт на `README.md` при ЛЮБОМ слиянии разрешается ОБЪЕДИНЕНИЕМ записей реестра, НИКОГДА выбором стороны: параллельные заходы волны дописали в реестр по строке — обе записи правы, выбор одной молча уничтожает регистрацию соседа.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-13, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- деплоя в этом заходе нет.

- Проверь ветку: `git branch --show-current` — обязано быть `zahod/reserch-russkie-kruzhki`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add diskmat-57/docs/RESERCH-russkie-kruzhki.md diskmat-57/2026-08-18_logika-goda-7/kod_reserch-russkie-kruzhki.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти пять файлов проекта, в этом порядке, и ничего больше:
  1. `diskmat-57/_sajt/god.json` — программа года данными: четверти, блоки, часы, поля `karta`, `opornye`, `istochniki`, `temy` (кружок). Это то, на что накладываются чужие программы.
  2. `diskmat-57/docs/RESHENIYA.md` — решения владельца. Строки Р48–Р62 приняты 13.09 и отменяют часть старых: читай их ВНИМАТЕЛЬНО, они задают, что в год уже нельзя предлагать.
  3. `diskmat-57/2026-08-18_logika-goda-7/ISTOCHNIKI.md` — что уже вычитано (Шень, Раскина–Шаповалов, Шаповалов, Канель-Белов и Ковальджи, Манжина, Высоцкий). Эти источники НЕ перечитывай: они разобраны, и §13 там уже проверяет согласованность порядков.
  4. `diskmat-57/2026-08-18_logika-goda-7/TEMY-chuzhie.md` — разбор матшколы Макса (16 листков) и NLOGN (7 листков). Тоже вычитано, не повторять.
  5. `diskmat-57/docs/RESERCH-mirovye-kursy.md` — международные традиции (AoPS, IB, Decision Maths, CS Unplugged, венгры). Нужен, чтобы не дублировать: твоя территория — русский кружковой корпус.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц7)
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц8)

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц9). Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
### Что сделать

🔴 **ЭТО ЗАХОД-ЗАКУПКА, А НЕ ЗАХОД-АНАЛИЗ. Механика: скачать · разложить · выписать оглавления. Ни одного суждения о нашей программе.** Решение владельца 13.09: *«не нужно сразу давать большую нагрузку, потому что мы можем уйти не туда и потратить силы и токены не на то»*. Наложение на наш курс, поиск дыр и выбор тем делают владелец и аналитик, не ты. Сопоставишь с нашей программой или посоветуешь, что взять, — это брак, а не инициатива.

**ШАГ 1 — ГЛАВНЫЙ. Снять ОГЛАВЛЕНИЯ десяти брошюр серии «Школьные математические кружки», которых у нас НЕТ.** Решение владельца 13.09: *«оглавлений достаточно, чтобы построить на первом уровне систему. Нам нужна система, просто чтобы мы понимали, что делаем — общий каркас хорошего курса»*. Книги покупать не надо, скачивать с пиратских ресурсов не надо: у каждой книги серии есть своя страница с аннотацией и оглавлением.

Страницы в кодировке **windows-1251**, перекодируй в UTF-8 — иначе получишь мусор вида `������` (проверено при сборке захода):
```
http://ashap.info/Knigi/Matkruzhki/01-Chetnost.html     вып. 01  Чётность
http://ashap.info/Knigi/Matkruzhki/03-Arifmetika.html   вып. 03  Арифметика
http://ashap.info/Knigi/Matkruzhki/05-Vzveshivaj.html   вып. 05  Взвешивания и алгоритмы
http://ashap.info/Knigi/Matkruzhki/08-Delimost.html     вып. 08  Делимость и простые числа
http://ashap.info/Knigi/Matkruzhki/09-Primer.html       вып. 09  Как построить пример
http://ashap.info/Knigi/Matkruzhki/10-Turniry.html      вып. 10  Турниры и таблицы
http://ashap.info/Knigi/Matkruzhki/11-Logika.html       вып. 11  Логические задачи
http://ashap.info/Knigi/Matkruzhki/14-Logika2.html      вып. 14  Логика для всех: от пиратов до мудрецов
http://ashap.info/Knigi/Matkruzhki/16-TChAzy.html       вып. 16  Азы теории чисел
http://ashap.info/Knigi/Matkruzhki/21-IndukBF.html      вып. 21  Индукция
```
У части выпусков рядом со страницей выложен свободный PDF первой части (`NN-....pdf` в том же каталоге) — если он есть, скачай его в `/Users/ivanyakovlev/Documents/Книги/Кружки-6-7/` и оглавление сними из него, оно полнее. Нет PDF — хватит оглавления со страницы.

🔴 Оглавление выписывается ДОСЛОВНО. Нет оглавления на странице — так и напиши в СТАТУСЕ. **Выдумывать содержание книги по её названию нельзя ни при каких условиях** — это единственный вид брака в этом заходе.

**ШАГ 2. Выписать оглавления ПЯТИ книг, которые уже лежат у нас на диске.** Их НЕ скачивать. Четыре из них — та же серия, и это проверено по титульным листам при сборке захода:
```
_fond/biblioteka/raskina-shapovalov-kombinatorika-1.pdf   вып. 20  Комбинаторика
_fond/biblioteka/raskina-shapovalov-kombinatorika-2.pdf   вып. 24  Комбинаторика: заседание продолжается
_fond/biblioteka/shapovalov-konstrukcii.pdf               вып. 13  Математические конструкции: от хижины до дворца
/Users/ivanyakovlev/Documents/Книги/Графы/графы.pdf       вып. 02  Гуровиц, Ховрина. Графы
_fond/biblioteka/kanel-belov-kovaldzhi-nestandartnye.pdf   сборник методов, вне серии
```
Пути `_fond/biblioteka/...` — от корня репозитория `materials`.

**ШАГ 2б. Скачать четыре курса** в `/Users/ivanyakovlev/Documents/Книги/Кружки-6-7/`:
```
https://dev.mccme.ru/~merzon/mirror/kruzhok/spivak.pdf          → kurs-spivak-6-7.pdf
https://dev.mccme.ru/~merzon/mirror/kruzhok/burago-excerpt.pdf  → burago-dnevnik-fragment.pdf
https://mccme.ru/free-books/pdf/kozlova.pdf                     → kozlova-skazki-i-podskazki.pdf
http://mmmf.msu.ru/archive/19992000/spivak67/spivak67.zip       → spivak67-listki.zip (распаковать в подпапку spivak67/)
```

**ШАГ 3. Снять три программы-цепочки как текст** в ту же папку, `.md`:
```
https://old.mccme.ru/circles/vmsh/2016/            → programma-vmsh-57-7klass-2016.md
https://old.mccme.ru/circles/mccme/2008/6klass/    → programma-karavaeva-6klass.md
https://old.mccme.ru/circles/mccme/2009/7klass/    → programma-karavaeva-7klass.md
```
🔴 **Две последние страницы в кодировке cp1251 (windows-1251) — перекодируй в UTF-8, иначе получишь мусор вида `������`.** Это проверено при сборке захода: `web_fetch` вернул именно мусор. Первая (ВМШ) в UTF-8 и читается как есть. В каждом `.md` — таблица: номер занятия · дата · тема дословно · ссылка на листок. Листки скачивать НЕ надо, нужны только названия и адреса.

**ШАГ 4. Собрать реестр `diskmat-57/docs/RESERCH-russkie-kruzhki.md`** — по записи на каждый источник: 10 брошюр серии без PDF + 5 книг с диска + 4 скачанных курса + 3 программы = **22 записи**, каждая строго такой формы:

```
## <имя источника>
- ФАЙЛ: <абсолютный путь, куда лёг> · <размер> · <страниц или занятий>   ← для брошюры без PDF: «файла нет, только страница серии <url>»
- СТАТУС: оглавление выписано | скачан, оглавления нет: <почему> | не скачался: <код ошибки>
- ОГЛАВЛЕНИЕ: <дословно, пунктами; для брошюры — главы и параграфы, для программы — занятия по порядку>
```

Оглавление PDF снимай `pdftotext` по первым 10–15 страницам; если текстового слоя нет — так и напиши в СТАТУСЕ, страницы в картинки не гоняй, это дорого и в этом заходе не нужно.

**ЧЕГО НЕ ДЕЛАТЬ.** Не читать книги дальше оглавления. Не выписывать задачи. Не сопоставлять с нашей программой и не предлагать темы. Не трогать `god.json` и вообще ничего вне зоны. Не искать источники сверх списка.

🔴 **Не скачалось — ЗАКОННЫЙ исход.** Требование владельца дословно: *«не надо запускать ресёрч, который принесёт то, что не получилось прочитать»*. Пиши «не скачался» с кодом ошибки — владелец скачает сам. Выдуманное оглавление — единственный вид брака здесь.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — гоняется из корня репозитория:**
```
python3 -c "import re,sys;t=open('diskmat-57/docs/RESERCH-russkie-kruzhki.md').read();z=re.findall(r'(?m)^## ',t);s=re.findall(r'(?m)^- СТАТУС:',t);o=re.findall(r'(?m)^- ФАЙЛ:',t);print('записей',len(z),'· со статусом',len(s),'· с файлом',len(o));sys.exit(0 if len(z)>=22 and len(s)==len(z)==len(o) else 1)"
```
→ `rc=0`, печать «записей 22 · со статусом 22 · с файлом 22». Команда читает живой файл артефакта на реальном объекте репозитория, фикстуры здесь нет.
🔴 **Значение при сборке 2026-09-13: команда падает `FileNotFoundError` — файла ещё нет.** Ожидаемое красное до работы, а не поломка критерия.

Числа, которые машина не проверит и которые идут в отчёт строкой: **оглавлений выписано M из 22 записей** и **скачано N файлов**.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «часть не скачалась», а «оглавлений нет у 3 из 22, вот у каких и почему».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор нужен, тип — **ПОСЛЕ-типа** — судит результат, стоит в конце, после задачи. Свежий субагент, ДРУГИМ методом: он НЕ читает реестр как текст, а проверяет диск — для каждой записи берёт путь из строки ФАЙЛ и убеждается, что файл существует и ненулевого размера (`ls -l`), а для записей со статусом «не скачался» убеждается, что файла действительно нет. Выписанное оглавление сверяет с `pdftotext` по двум случайно выбранным источникам. Доля сплошной выборки: 100 процентов записей по существованию файла плюс 2 источника из 22 по сверке оглавления. Финальная строка ответа обязательна дословно: «выдано N позиций из M найденных» — без неё ответ считается усечённым и в отчёт не вставляется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- diskmat-57/docs/RESERCH-russkie-kruzhki.md diskmat-57/2026-08-18_logika-goda-7/kod_reserch-russkie-kruzhki.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- diskmat-57/docs/RESERCH-russkie-kruzhki.md diskmat-57/2026-08-18_logika-goda-7/kod_reserch-russkie-kruzhki.md   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц10) Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц11)
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц12)
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц13)

**ЗОНА ГИГИЕНЫ:** `diskmat-57/docs/RESERCH-russkie-kruzhki.md` `diskmat-57/2026-08-18_logika-goda-7/kod_reserch-russkie-kruzhki.md`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/docs/RESERCH-russkie-kruzhki.md` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/2026-08-18_logika-goda-7/kod_reserch-russkie-kruzhki.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *(история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц14) Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц15) Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц16) — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/reserch-russkie-kruzhki --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма (здесь она же — checkout-режим, папка одна) плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/materials && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/materials --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged arka/mat-kostyak`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

No amendments in `## ПРАВКИ ПОСЛЕ ВЫДАЧИ` (empty). Readiness criterion (22 entries, all with СТАТУС+ФАЙЛ) is not disputed — it matches the task shape (procurement, not analysis).

1. **Snapshot checks, first move** (red flags in the header are from build time, must be re-checked now):
   - `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py diskmat-57/2026-08-18_logika-goda-7`
   - `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`
   - Entry snapshot for `## ГИГИЕНА ВХОДА` (branch --no-merged, status --porcelain wc -l, log --oneline @{u}.. wc -l, `git_zona.py zayavki`).

2. **§0.1 git contour, ONE subagent, before my own worktree exists.** Run exactly the printed `bootstrap_zahod.py --zadanie-subagentu ...` task in a single subagent call, wait for it, paste its six lines verbatim into `## ОТЧЁТ`. On `network_error` retry up to 3 times with a 45s pause (measured by `date`, not summed sleeps); after the third failure, proceed without it and record that as its own report line, not silence. I do not execute merge/queue steps myself.

3. **Create own worktree**: `git_zona.py worktree add reserch-russkie-kruzhki --branch zahod/reserch-russkie-kruzhki`, then `cd` into it. Verify `git branch --show-current` == `zahod/reserch-russkie-kruzhki` — anything else is STOP, no `git checkout`.

4. **Restore point**: `git add` the two zone paths → commit, only if zone is not already clean at HEAD.

5. **Read exactly the five named project files, in this order** (no others): `diskmat-57/_sajt/god.json`, `diskmat-57/docs/RESHENIYA.md` (Р48–Р62), `diskmat-57/2026-08-18_logika-goda-7/ISTOCHNIKI.md`, `diskmat-57/2026-08-18_logika-goda-7/TEMY-chuzhie.md`, `diskmat-57/docs/RESERCH-mirovye-kursy.md`.

6. **Execute the procurement, no judgment on our program:**
   - Step 1 — 10 ashap.info booklet pages (windows-1251): fetch raw HTML with `curl`, recode `iconv -f WINDOWS-1251 -t UTF-8`, extract the TOC verbatim from the recoded text; where a same-pattern `NN-....pdf` exists next to the page, download it into `/Users/ivanyakovlev/Documents/Книги/Кружки-6-7/` and take the TOC from the PDF instead (more complete) via `pdftotext` on the first 10–15 pages.
   - Step 2 — 5 on-disk books (NOT downloaded): `pdftotext` first 10–15 pages of each named path, extract TOC verbatim.
   - Step 2б — download 4 courses (spivak, burago excerpt, kozlova, spivak67 zip) into the same folder; unzip spivak67 into its own subfolder.
   - Step 3 — 3 program-chain pages as text → `.md` in the same folder; 2 of them cp1251, recode the same way as step 1; build the занятие·дата·тема·ссылка table from raw text, no listok downloads.
   - Step 4 — assemble `diskmat-57/docs/RESERCH-russkie-kruzhki.md`, 22 entries (10+5+4+3) in the exact `## <source>` / `- ФАЙЛ:` / `- СТАТУС:` / `- ОГЛАВЛЕНИЕ:` form. A page/PDF with no TOC or that fails to fetch is recorded as such (СТАТУС + error code) — never invented.

7. **Register the new doc**: `register_doc.py diskmat-57/docs/RESERCH-russkie-kruzhki.md "<description>"`, then append the same line to `_studio/docs/KARTA.md` in both commit rounds it touches.

8. **Commit the zone incrementally** as pieces complete (`add` + `commit -- <paths>` + `git_zona.py check --zone`), not in one final move, per §4.

9. **Post-type verifier subagent** per §3 after the registry is assembled: disk-checks every ФАЙЛ path (exists + non-zero, or genuinely absent for "не скачался"), spot-checks 2 of 22 TOCs against `pdftotext` directly. Must end with "выдано N позиций из M найденных".

10. **Final git hygiene** (the WARNING block, in order): all commits → merge own branch into main via `git_zona.py vlit-v-osnovnuyu` → post-check from the main folder → extinguish (`branch --no-merged`) → export own branch (`log --oneline @{u}.. | wc -l` → 0).

11. Write `## ОТЧЁТ` with all required fields (artifact path, commit hash, irreversible-actions list or "none", finding-repeatability line, the six hygiene numbers).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> 🔴 **`ДОМ:` — ОБЯЗАТЕЛЬНОЕ ПОЛЕ, И АДРЕС В НЁМ ОБЯЗАН СУЩЕСТВОВАТЬ В МОМЕНТ, КОГДА ТЫ ЕГО ПИШЕШЬ.** Путь, которого нет на диске, — не адрес: такую запись нельзя ни доставить, ни спросить, и она не чинится ничем. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц17) Проверить СВОЙ файл до отчёта — одна команда:
> ```
> python3 _generator/tools/bootstrap_zahod.py --proverit-doma <этот файл>
> ```
> rc=0 — все дома достижимы; rc=1 — назван дом, которого нет (команда печатает какой именно). Тот же разбор гоняет `Г7` приёмки, и у него храповик: у ЭТОГО захода база 0, поэтому первый же недостижимый дом здесь — красный на приёмке, а не запись, которую через неделю никто не найдёт.
> `ДОМ: владелец` — законный адрес и НЕ недостижимый дом: он значит «дома-файла нет вовсе, решение за человеком». Не знаешь пути — пиши его, а не выдуманный путь. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит и «доставлено» без метки на месте, и недостижимый дом сверх базы; достижимое-недоставленное печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Заголовок задания захода называет `shapovalov-konstrukcii.pdf` выпуском 13 «Математические конструкции: от хижины до дворца», но фактическое оглавление файла на диске (снято `pdftotext`, см. `RESERCH-russkie-kruzhki.md`) — семь занятий книги «Как построить пример?» (Много не мало / Поиск перебором / Преодолеть инерцию мышления / Редукция и разминка / Узкие места / Ослабление условий / Конструкции в классической геометрии), дословно совпадающих с описанием источника «Шаповалов — «Как построить пример?»» в `ISTOCHNIKI.md §10`. Либо в задании захода опечатка (не тот выпуск), либо на диске реально лежит не та книга под этим именем файла — нужна проверка человеком, я её не делаю (задача — закупка, не анализ).
   ДОМ: diskmat-57/2026-08-18_logika-goda-7/ISTOCHNIKI.md
   ДОСТАВЛЕНО: reserch-russkie-kruzhki#1

2. Порядок §0.1 этого файла требует: сначала субагент гит-контура (в главной папке), потом заведение своей рабочей папки, а секция `## ПЛАН` заполняется уже внутри этого порядка («ПЛАН — в `## ПЛАН` перед действиями» стоит ПОСЛЕ пункта 2 «заводи рабочую папку»). Но стартовое сообщение владельца наверху того же файла говорит противоположное первым ходом: «git branch --show-current, затем чтение захода, затем секция `## ПЛАН` … до всяких действий» — раньше субагента и раньше заведения папки. Я последовал стартовому сообщению и написал `## ПЛАН` в файл ГЛАВНОЙ папки (`materials`, не в свою будущую рабочую папку) ДО запуска субагента гит-контура. Субагент, закрывая заявки очереди по зоне `diskmat-57` (коммит `307441ed`), закоммитил заодно и мою уже написанную секцию `## ПЛАН` — не в свою зону, не тем автором, что положено по §4. Содержание не потерялось (текст цел, гейт готовности прошёл зелёным), но коммит-принадлежность нарушена, и это повторится на любом будущем заходе с той же парой инструкций в шапке. Правка: либо стартовое сообщение владельца должно явно говорить «`## ПЛАН` пишется ВНУТРИ своей рабочей папки, после §0.1», либо §0.1 должен явно предупреждать не трогать файл-заход в главной папке до заведения своей папки.
   ДОМ: diskmat-57/2026-08-18_logika-goda-7/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: reserch-russkie-kruzhki#2

3. `register_doc.py` — единственная названная в контракте зоны дверь регистрации нового `.md` — категорически отказывается регистрировать документы проектов (судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`; `korni.карта_для('diskmat-57/docs/RESERCH-russkie-kruzhki.md')` возвращает `None`). Контракт зоны этого захода, однако, безусловно требует «завёл новый `.md` — регистрируешь его сам, той же командой». Для проектных документов (как этот, в `diskmat-57/docs/`) команда неприменима в принципе — это не разовая ошибка запуска, а структурный пробел: у проектов вроде `diskmat-57` есть свой человеческий индекс (`NAVIGATOR.md` «Указатели в долгую»), но он не машиночитаем и не в зоне этого захода, чтобы я мог его дополнить сам.
   ДОМ: diskmat-57/2026-08-18_logika-goda-7/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: reserch-russkie-kruzhki#3

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц18)
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
=== CMD1: git --no-optional-locks branch --no-merged arka/mat-kostyak ===
(пусто — вывода нет)
=== CMD2: git --no-optional-locks status --porcelain | wc -l ===
     132
=== CMD3: git --no-optional-locks log --oneline @{u}.. | wc -l ===
       1
=== CMD4: git_zona.py zayavki ===
Открытых заявок: 7
   · 2026-08-25T0042-otkaz-studio  (476 ч, blokiruet, род: git-operaciya) — ОТКАЗ GIT-ОПЕРАЦИИ: коммит зоны _studio
   · 2026-09-02T0551-studio-2026-09-02-2026-08  (281 ч, obychnaya) — КОММИТ ЗОНЫ _studio по итогам сессии 2026-09-02
   · 2026-09-11T2101-cowork-11-09-2026-materials-disciplina  (50 ч, obychnaya) — КОММИТ по итогам сессии по ученику Даня, три зоны в materials + файл в disciplina
   · 2026-09-13T1620-cowork-misha-priyom-fond-nachalki  (6 ч, obychnaya) — КОММИТ приём ученика Миша, пять зон в materials
   · 2026-09-13T2043-13-09-2026-cowork-diskmat-57  (2 ч, obychnaya) — КОММИТ закрытия сессии спецмата 13.09
   · 2026-09-13T2100-13-09-2026-cowork-diskmat-57  (2 ч, obychnaya) — КОММИТ переразнесения той же сессии (первое было галопом)
   · 2026-09-13T2235-13-09-diskmat-57-2026-08  (0 ч, obychnaya) — КОММИТ записей разговора 13.09
   [+ 12 заявок "ждут захода-разработчика" в na-zahod/, гит-контуру не адресованы]
   [+ POSTOYANNYE.md: 0 веток "не вливать", 1 путь "вне git по причине" (zayavki/), 0 папок "не гасить"]
Охват: заявок открыто 7, переадресовано 12, постоянных исключений 1, сторож краснеет на 0, держателей 0, двойной захват на 0
```

**ЧТО СДЕЛАНО** *(с хэшами)*
- Пункт 0 (хвост Cowork): коммит `f3f17daf` — 12 путей (`_illustracii/docs/KARTA.md`, `_meta/docs/KARTA.md`, `_studio/docs/KARTA.md`, `UROKI-FABRIKE.md` ×3, `VYGRUZKA-*.md` ×3, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`)
- Заявка 2026-09-02T0551 → закрыта rezultat, коммит `9aa6431e` (HANDOFF-2026-09-03.md, SESSIYA.md, PLAN.md); obzory-часть уже была в `92974718`
- Заявка 2026-09-13T1620 → закрыта rezultat, коммиты `b87ce5de`, `6b3b4144`, `cdc5c2d1`, `a4264bee`
- Заявки 2026-09-13T2043/2100/2235 (diskmat-57) → закрыты rezultat одним коммитом `307441ed` — 🔴 этот же коммит попутно забрал МОЮ уже написанную к тому моменту секцию `## ПЛАН` этого файла (я отредактировал её в главной папке ДО запуска этого субагента и до заведения своей рабочей папки — порядок в шапке файла требовал обратного; см. `## ОТЧЁТ` и `УРОКИ ФАБРИКЕ`)
- Заявка 2026-09-11T2101 → materials-часть исполнена: коммиты `10976c00`, `0d227677`; заявка закрыта как **zastryala** (disciplina-половина вне периметра)
- Заявка 2026-08-25T0042-otkaz-studio → закрыта как **zastryala** (просит коммит ВСЕЙ зоны `_studio`, вне мандата §0/§3)
- Пункт 2 (влитие в основную): НЕ ЗАПОЛНЕНО — `--vlit` не назвал ни одной ветки, вливать нечего. Основная ветка — `arka/mat-kostyak`
- Пункт 3 (check): 121 путь вне git + 1 правка `README.md` — содержательная работа, передана заходу списком, не коммичена
- Пункт 4 (гашение): ничего не гасил — задание не назвало веток; `zahod/reserch-russkie-kruzhki` не создавал и не трогал
- `vyvezti`/push на origin — не делал, вне пяти пунктов задания

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`
1. `2026-08-25T0042-otkaz-studio` — просит коммит ВСЕЙ зоны `_studio` (COWORK.md, docs/kak-delat/*, HANDOFF/NAVIGATOR/TZ/LYUDI/SOSTOYANIE/reserch трёх арок) — содержательная работа нескольких заходов, вне мандата гит-контура.
2. `2026-09-11T2101` (вторая половина) — файл `_generator/tools/istoriya_zadach.py` лежит в СОСЕДНЕМ репозитории `disciplina`; это задание жёстко ограничило субагента главной папкой `materials` — нужен отдельный заход с правами на `disciplina`.
3. Остаток дерева (121 путь + `README.md`) вне git по проектам (`spetsmat-2026`, `kurs-puti-i-volny`, `teorkat-vvedenie`, `ucheniki/maksim`, `_studio/zhurnal/2026-08-27_matproekty-179`, `obzory/.../LEKCIYA-v2` главы 2-8 и др.) — содержательная работа, не названная заявкой и не входящая в §0 (индекс/автологи).

## ОТЧЁТ — (заполняет исполнитель)

**Что сделано и зачем.** Собран реестр `diskmat-57/docs/RESERCH-russkie-kruzhki.md` — 22 записи: 10 брошюр серии «Школьные математические кружки» с ashap.info (8 из 10 — с демо-PDF, скачанным в `/Users/ivanyakovlev/Documents/Книги/Кружки-6-7/`; 2 — 09-Primer и 16-TChAzy — PDF отдаёт HTTP 404, оглавление снято со страницы), 5 книг с диска (`_fond/biblioteka/*`, `Книги/Графы/графы.pdf` — не скачивались, только оглавления), 4 скачанных курса (Спивак-курс, Бураго-фрагмент, Козлова, архив кружка Спивака 1999/2000 zip-архивом) и 3 программы-цепочки как таблицы `.md` (ВМШ-2016, Караваева-6, Караваева-7). Задача — чистая закупка: скачать, разложить по `/Users/ivanyakovlev/Documents/Книги/Кружки-6-7/`, выписать оглавления дословно; ни одного суждения о том, что из этого идёт в курс, не сделано (Р60, слово владельца 13.09).

**Как проверено.** (1) Критерий готовности прогнан из корня `materials` после слияния — `rc=0`, «записей 22 · со статусом 22 · с файлом 22». (2) Свежий субагент-верификатор (ПОСЛЕ-типа, другим методом — не читал реестр текстом, проверял диск): 22 из 22 путей ФАЙЛ проверены на существование/размер (обе записи «не скачался» подтверждены как реально отсутствующие файлы — законный исход), 11 из 11 PDF в `Кружки-6-7/` сверены по числу страниц `pdfinfo` — совпадение точное, 2 из 22 оглавлений (08-Delimost, графы/Гуровиц-Ховрина) сверены дословно с `pdftotext` — расхождений нет. Финальная строка субагента: «выдано 25 позиций из 25 найденных».

**Что НЕ трогал.** Ничего вне двух файлов зоны. `god.json`, `RESHENIYA.md`, `ISTOCHNIKI.md`, `TEMY-chuzhie.md`, `RESERCH-mirovye-kursy.md` — прочитаны, не изменены. Пять источников не читались дальше оглавления (по заданию). Задачи не выписывались. С нашей программой ничего не сопоставлялось.

**Числа, которые машина не проверит:** оглавлений выписано **21 из 22** записей (не выписано только у `kurs-spivak-6-7.pdf` — см. СТАТУС этой записи, битая кодировка шрифта PDF); скачано **12 бинарных файлов** (8 демо-PDF ashap.info + 3 скачанных курса в PDF + 1 zip-архив), плюс 3 программы-цепочки сняты как текст в свои `.md`.

**ПРАВКИ ПОСЛЕ ВЫДАЧИ:** блок пуст на момент этого отчёта — заход не правился с момента старта.

**НЕОБРАТИМОЕ:** необратимого нет. Один нестандартный git-ход: в главной папке `materials` я отменил (`git checkout --`) свою же незакоммиченную черновую правку файла-захода (секция `## ГИГИЕНА ВХОДА`, написанную мной же ранее в этой же сессии до заведения рабочей папки) — она мешала `vlit-v-osnovnuyu`; отменённое содержание не потеряно, оно уже полнее и правильнее лежит в коммите `71d922a0` этой же ветки. Подробности инцидента — пункт очереди 2 в `## ВОПРОСЫ`.

**ПОВТОРЯЕМОСТЬ находок.** Два из трёх пунктов очереди — про саму фабрику (порядок «ПЛАН до заведения рабочей папки» ловит следующий такой же заход; `register_doc.py` откажет на любом новом документе любого проекта вне четырёх корней) и ПОВТОРЯТСЯ на каждом будущем заходе с той же структурой шапки, поэтому оформлены как пункты очереди с `ДОМ: .../UROKI-FABRIKE.md`, а не как «немедленное» — чинить сам шаблон `bootstrap_zahod.py`/`register_doc.py` вне моей зоны и вне моих прав. Третий пункт (несовпадение названия `shapovalov-konstrukcii.pdf`) — разовая находка про данные этого проекта, не про фабрику, тоже уходит в очередь (`ISTOCHNIKI.md`), не в немедленный заход. Поправка кодировки KOI8-R (не windows-1251, как в задании) — не пункт очереди: применена один раз при извлечении, зафиксирована в самом реестре, дальше не повторится (программы Караваевой уже сняты).

**Открытое «возвращаться»:** нет — задача закрыта, три пункта очереди ждут аналитика/владельца.

**Время прогона + токены:** неприменимо на канале `app` — снимать неоткуда (лог существует только на канале `terminal`).

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/docs/RESERCH-russkie-kruzhki.md` — открывать любым markdown-просмотрщиком или текстовым редактором. Сопутствующая полка файлов — папка `/Users/ivanyakovlev/Documents/Книги/Кружки-6-7/` (12 PDF/zip + 3 `.md` программы + распакованный `spivak67/`).
**РОД АРТЕФАКТА:** `собранный`
**КОММИТ:** `71d922a0` — «diskmat-57: реестр русских кружковых источников 6-7 класс (заход-закупка)» · `git_zona.py check --zone diskmat-57/docs/RESERCH-russkie-kruzhki.md` → ✅ · `git_zona.py check --zone diskmat-57/2026-08-18_logika-goda-7/kod_reserch-russkie-kruzhki.md` → ✅ (обе проверки прогнаны из главной папки ПОСЛЕ влития в `arka/mat-kostyak`)

**ГИТ-ГИГИЕНА, ПОСЛЕДНИЙ ХОД (числа командой, не по памяти):**
1. Все коммиты: в рабочей папке `materials-wt/reserch-russkie-kruzhki` (моя зона) — `status --porcelain` пуст, вне git 0. В главной папке `materials` остаётся 123 грязных пути — НЕ мои (чужая содержательная работа нескольких проектов, переданная гит-контуром списком, не эта зона; не трогал).
2. Влитие своей ветки: сделано, без конфликтов — `71d922a0` в `arka/mat-kostyak` (см. КОММИТ выше). Пост-проверка из главной папки ПОСЛЕ влития — критерий готовности `rc=0` зелёный.
3. Гашение: `git branch --no-merged arka/mat-kostyak` пуст — невлитых веток не осталось; свою ветку `zahod/reserch-russkie-kruzhki` и рабочую папку НЕ гасил и не закрывал — по прямому указанию задания («перенос и закрытие делает аналитик при приёмке»).
4. Вывоз: 🔴 **НЕ сделан по политике безопасности агента, не по техническому препятствию.** `zahod/reserch-russkie-kruzhki` не имеет upstream (не была вывезена раньше); `git log --oneline` на неё после слияния показывает 1 локальный коммит, не уехавший на origin. Общее правило моей среды — не делать `git push` без явного разрешения владельца в чате, даже когда заход просит `vyvezti --yes` как штатный шаг. Число: невывезенных на своей ветке — 1. Владельцу нужно самому вызвать `git_zona.py vyvezti --yes` из рабочей папки `materials-wt/reserch-russkie-kruzhki` (текущая ветка там уже `zahod/reserch-russkie-kruzhki`), либо явно попросить меня сделать это.

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

<правок нет>

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — критерий прогнан приёмкой самостоятельно из корня репозитория (`rc=0`, «записей 22 · со статусом 22 · с файлом 22»); числа отчёта воспроизведены на живом диске: 12 бинарных файлов и 3 программы в `Книги/Кружки-6-7/` (`ls -la`), 11 PDF на месте, обе записи «не скачался» подтверждены — у меня те же адреса отдают 403, а не 404, как в отчёте (расхождение кода несущественно, факт отсутствия PDF подтверждён); перекодировка программ Караваевой проверена глазами по живому `.md` — кириллица целая, мусора нет; отсутствие текстового слоя у `kurs-spivak-6-7.pdf` подтверждено `pdftotext`. Пункт очереди 1 проверен по самому файлу и дал ОБРАТНЫЙ диагноз: врёт не задание, а `ISTOCHNIKI.md §10` — исправлено тем же ходом.

**ВЕТКА РАБОТЫ:** `zahod/reserch-russkie-kruzhki`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- `2026-09-14T0011-reserch-russkie-kruzhki-14-09-diskmat` — `git-operaciya / kommit` — коммит правок приёмки: исправленный ISTOCHNIKI §10, два урока фабрике, метки доставки, запись в дневнике

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
