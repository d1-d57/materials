# Канал исполнителя — sajt-cveta-i-setka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
<!-- гейт сборки заполненного: ЖДЁТ -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — правка расписана до конкретных строк CSS; думать по ходу не о чем, проверка визуальная.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — правка расписана до конкретных строк CSS, проверка визуальная.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-cveta-i-setka.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/2026-08-18_logika-goda-7/kod_sajt-cveta-i-setka.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add sajt-cveta-i-setka --branch zahod/sajt-cveta-i-setka, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.

Заход мог быть ПОПРАВЛЕН после того, как ты стартовал: аналитик правит
тот же файл, а ты прочитал его один раз. Смотри секцию ## ПРАВКИ ПОСЛЕ
ВЫДАЧИ в конце файла — при старте и всякий раз, когда владелец пишет
тебе в чат. Правка с номером, которого ты не читал, отменяет любое
противоречащее ей место выше.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
ГРАНИЦА ОБЛАСТИ: сырые подстроки в `kod_*.md` (пункт 4) — НЕ парсер очереди `dostavit_urok` (который считает только пары ДОМ:/ДОСТАВЛЕНО:). Разница в числах — законна.
🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/schet_nezakrytogo.py diskmat-57/2026-08-18_logika-goda-7`
Область: «diskmat-57/2026-08-18_logika-goda-7» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — ни одного skills/*/DOLG.md нет на диске (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 14
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 44
     из них разбором очереди (парсер `dostavit_urok`, записи с парой ДОМ:/ДОСТАВЛЕНО:): 39
       живых (чинится доставкой — «дом есть»)  : 3
       к владельцу (решение за человеком)      : 9
       адрес недоступен (нет/папка/код/указат.) : 0
       адрес не разобран                        : 0
       отработавших (машинный след закрытия)    : 0
       доставлено                               : 27
       🔴 не проверяется машиной: содержательная отработанность записей БЕЗ следа закрытия (метки в доме, строки ✅/ЗАКРЫТО) — нужна ревизия человеком; сырой греп сверх разбора — шаблонные строки формы.

🔴 ДВЕРЬ НЕЗАКОММИЧЕННЫХ `kod_*.md` ОТКРЫТА КЛАПАНОМ: в арке `diskmat-57/2026-08-18_logika-goda-7` есть 8 незакоммиченных файлов-заходов `kod_karta-dokumentov.md`, `kod_koncepcia-goda.md`, `kod_lineika-chisla.md`, `kod_lineika-grafy.md`, `kod_lineika-podschety.md`, `kod_reserch-russkie-kruzhki.md`, `kod_god-json-v2.md`, `kod_sajt-cveta-i-setka.md` — второй заход в такую арку обычно не собирается (долг 4 `disciplina-git`), но АНАЛИТИК открыл клапан, причина дословно: «незакоммичен файл-заход kod_god-json-v2.md со свежезаполненной ФАЗОЙ ПРИЁМКИ и документы docs/, правленные аналитиком в этой сессии. Заход обязан забрать их ПЕРВЫМ коммитом: пока они вне git, рабочая папка их не видит — этот дефект уже стоил вопроса №2 предыдущему исполнителю.». Отключение видно здесь, в артефакте, а не осталось решением в голове аналитика.

КОНТЕКСТ. Сайт курса спецмата 7 класса собирается из одного `god.json` скриптом `build.py`. Вчерашний заход `god-json-v2` привёл содержание года к решениям владельца — и ровно этим сломал ВИД: появились два новых домена (`G`, `M`), которых нет в палитре, и III четверть выросла с 20 часов до 22, а лента свёрстана на 20 колонок. Владелец открыл опубликованную страницу и увидел обе поломки. ЦЕЛЬ: починить вид, пересобрать и опубликовать заново — он отправляет ссылку ассистентам.
Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели не предусмотрен: заход идёт до опубликованного адреса, проверенного глазами.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-14) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Дефект виден на опубликованной странице: --d-G и --d-M не определены, блоки Разгон, Дирихле, Игры и Информация идут без цвета. Измерено getComputedStyle в браузере 14.09
2. Цвета берём освободившиеся: G = #4a7292 (бывший V), M = #7d4f78 (бывший D). Новых оттенков в палитру не вводим
3. Лента и полоска недель переносятся на вторую строку: .lenta 20 колонок при 22 часах III четверти, .linejka 10 колонок при 11 неделях. Литералы заменить на максимум по четвертям, считаемый при сборке
4. Требование владельца: вся таблица года помещается в один экран, без переносов — это было важно и сломалось
5. Мелочь заодно: у контрольной IV четверти после блока «Остатки» подпись chto = «кодировка и арифметика», имени «Кодировка» больше нет
6. sayt/static/diskmat-57, diskmat-57/docs и папка арки вписаны в зону ЯВНО: первый — потому что через него идёт публикация и прошлый заход вышел за границу именно здесь; вторые два — чтобы заход мог забрать в git незакоммиченную работу аналитика нулевым коммитом

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/sajt-cveta-i-setka`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/sajt-cveta-i-setka`. 🔴 **ДАЛЬШЕ — ТОЛЬКО ПУТИ ОТНОСИТЕЛЬНО ЭТОЙ ПАПКИ** (или `cd` в неё безусловно, каждым ходом): абсолютный путь в главную папку репозитория здесь — типичная ошибка, правка утекает МИМО worktree и найдётся только на коммите («вне git» в `git_zona.py check --zone` из рабочей папки, на файле, который уже правил, — цена, оплаченная живьём: 5 файлов, ручное копирование и откат главной папки). 🔴 **Заводишь её САМ — ВТОРЫМ ходом, СРАЗУ ПОСЛЕ пункта 1 блока §0.1 ГИТ-КОНТУР ниже (не первым ходом захода целиком), из корня репозитория:**
  ```
  python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add sajt-cveta-i-setka --branch zahod/sajt-cveta-i-setka
  cd ../materials-wt/sajt-cveta-i-setka
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце вливает САМ ЗАХОД, последним ходом** (решение 20.08, заход `granica-priyomki`, пересмотр решения 05.08; WARNING-блок ниже печатает тот же порядок): цепочкой коммит зоны → пуш → влитие в основную (`git_zona.py vlit-v-osnovnuyu`) → пост-проверка ИЗ ГЛАВНОЙ папки → гашение СВОЕЙ ветки. Причина пересмотра — замер 20.08: изнутри захода не видно разницы между «механизм новый в рабочей папке» и «в `main` он старый», а приёмке это невидимо по построению. Влитие УСЛОВНОЕ: красная пост-проверка = откат влития самим заходом и строка в отчёт, а не «доложу, пусть приёмка решает». Файл-заход по-прежнему НЕ коммитить — его коммитит аналитик при приёмке.
- **ЗОНА (можно менять):** `diskmat-57/_sajt/build.py` `diskmat-57/_sajt/god.json` `diskmat-57/_sajt/dist` `sayt/static/diskmat-57` `diskmat-57/docs` `diskmat-57/2026-08-18_logika-goda-7`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *(история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц1) Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4); коммить ПО ХОДУ работы, не одним последним ходом (§4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **SCRATCHPAD — ТОЛЬКО ЛИЧНЫЙ.** Черновики, выкладки, промежуточные версии — в личную папку СВОЕГО захода `scratchpad/sajt-cveta-i-setka/`. Общие пути (`scratchpad/otchet.md`, любой `scratchpad/*` без имени твоей темы) ЗАПРЕЩЕНЫ: чужой отчёт уедет в твой файл или твой — в чужой, а приёмка читает отчёт без построчной сверки и подмену НЕ ЛОВИТ по построению. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц2)
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц3).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала САМ прогоняешь две команды самопроверки контура (пункт 1 ниже), и только ПОТОМ заводишь свою рабочую папку** — её ветка отпочковывается от основной такой, какая она есть на момент запуска: контур пуст, доносить инструмент влитием нечего.

**1. ВЕСЬ КОНТУР ПУСТ — САМОПРОВЕРКА ВМЕСТО СУБАГЕНТА.** При сборке проверены три числа контура, и все три нулевые: невлитых `zahod/*`-веток 0 (🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`); открытых заявок 0 (снимок при сборке 2026-09-14, пересчитать самому: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`); названных `--vlit` 0. Звать субагента не за чем — выполни САМ две команды и вставь их вывод в `## ОТЧЁТ` дословно:
```
git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'   # снимок при сборке 2026-09-14: 0
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/build.py diskmat-57/_sajt/god.json diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7
```
Первая вернула не 0 — НИЧЕГО чужого не вливай (свою ветку вольёшь последним ходом, см. ниже), назови число строкой в `## ОТЧЁТ` и работай дальше. Вторая красная — сначала приведи в порядок свою зону.

Если при следующей сборке хоть одно из трёх чисел окажется ненулевым, генератор сам вернёт сюда задание субагенту гит-контура — печатает его дверь `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/bootstrap_zahod.py --zadanie-subagentu`; звать его в этом заходе не надо.

🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси.

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-09-14, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.


- задеплоить: сайт курса на GitHub Pages по процедуре из kod_deplo-sajta.md §Часть Б; адрес назвать в отчёте

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/sajt-cveta-i-setka`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add diskmat-57/_sajt/build.py diskmat-57/_sajt/god.json diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы. Проект сверх них не изучай:
  1. `diskmat-57/_sajt/build.py` — твой рабочий файл: блок CSS около строки 156 (палитра доменов) и около строк 258–260 (`.lenta`, `.linejka`).
  2. `diskmat-57/_sajt/god.json` — источник данных; из него берутся часы и недели четвертей.
  3. `diskmat-57/2026-08-18_logika-goda-7/kod_deplo-sajta.md`, раздел «Часть Б — публикация на GitHub Pages» — процедура публикации.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц7)
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц8)

## 2. ЗАДАЧА

🔴 **WRITE YOUR `## ОТЧЁТ`, `## ПЛАН` AND `## ВОПРОСЫ` IN ENGLISH, AND EVERY FILE AND EVERY COMMIT MESSAGE YOU PRODUCE TOO.** Owner's decision 30.08. It is a каркас-level rule, not a preference (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц9). Fixed Russian addresses stay Cyrillic: `ЦЕНА:` · `ВЕРДИКТ:` · `ДОМ:` · `ДОСТАВЛЕНО:` · `ПОДЪЁМ:` · `[ДОЛГ: …]` · every `## ` heading of this file · every path and command.
🔴 **НУЛЕВОЙ ХОД — ГИГИЕНА, В ГЛАВНОЙ ПАПКЕ, ДО ЗАВЕДЕНИЯ WORKTREE.** В главной папке лежат незакоммиченными файл-заход `diskmat-57/2026-08-18_logika-goda-7/kod_god-json-v2.md` (аналитик только что заполнил в нём `## ФАЗА ПРИЁМКИ`), остальные `kod_*.md` арки (правлены мёртвые пути) и документы `diskmat-57/docs/` (`RESHENIYA.md`, `OTKRYTYE-VOPROSY.md`, `SESSIYA.md`, `UROKI-FABRIKE.md` — решения Р84–Р107 и ход интервью). Забери их ОДНИМ коммитом гигиены **из главной папки, на основной ветке, между пунктом 1 и пунктом 2 блока §0.1** — из свежего worktree они не видны вовсе, там своя копия дерева. Причина не формальная: прошлый исполнитель из-за этого сообщил, что таблица решений обрывается на Р83, — в его дереве так и было, а в главной папке строки уже стояли. Проверь сам обе стороны одной командой: `grep -c '^| Р' diskmat-57/docs/RESHENIYA.md` в главной папке против `git --no-optional-locks show HEAD:diskmat-57/docs/RESHENIYA.md | grep -c '^| Р'` — числа разойдутся ровно на незакоммиченное. Содержание этих файлов НЕ правь, только коммить.

Дальше — три правки в `build.py`, одна в `god.json`, пересборка и публикация.

**1. Палитра доменов, `build.py` около строки 156.** Сейчас там определены `--d-A`, `--d-K`, `--d-V`, `--d-D`, `--d-Z`. Домены `V` (вероятность) и `D` (дискретная математика) упразднены решениями Р49 и Р63 и в `god.json` больше не встречаются — их оттенки свободны. Добавить:
```
--d-G:#4a7292;   /* графы — оттенок бывшего V */
--d-M:#7d4f78;   /* методы — оттенок бывшего D */
```
🔴 Строки `--d-V` и `--d-D` **НЕ УДАЛЯТЬ**: на них может опираться `god-8.json` (восьмой класс), а он вне твоей зоны и проверить его судьбу отсюда нечем. Мёртвая переменная стоит ноль, удалённая живая ломает соседний класс.

**2. Ширина ленты, `build.py` около строки 258.** Сейчас:
```
.lenta{display:grid;grid-template-columns:repeat(20,minmax(0,1fr)); ...}
.linejka{display:grid;grid-template-columns:repeat(10,minmax(0,1fr)); ...}
```
Числа 20 и 10 — литералы, подогнанные под прежний максимум: III четверть держала 20 часов и 10 недель. Теперь у неё 22 часа и 11 недель, элементы не помещаются в строку и переносятся на вторую — календарь перестаёт быть одноэкранным, а полоска недель рвётся (неделя 25 уезжает отдельной строкой).

🔴 **Чинить не подстановкой 22 и 11, а ВЫЧИСЛЕНИЕМ при сборке.** Замысел раскладки назван в комментарии прямо там же: «общая шкала по вертикали, короткая четверть честно не достаёт до правого края» — значит колонок должно быть ровно столько, сколько у САМОЙ ДЛИННОЙ четверти, и это число обязано пересчитываться само при любой будущей правке часов. Посчитай по god-файлу максимум часов в сетке по четвертям (элементы `tip == "blok"` и контрольные; `tip == "kruzhok"` и `tip == "vne-setki"` **не в счёт** — они вне сетки по определению) и максимум недель, и подставь их в CSS вместо литералов. Как именно донести число до CSS — твой выбор: подстановка в шаблон при генерации или CSS-переменная на элементе.

🔴 **Шкала ОДНА на все четверти, а не своя у каждой.** Своя ширина у каждой четверти сломает главное свойство картинки — что четверти сравнимы глазом по длине.

**3. Устаревшая подпись, `god.json`.** У контрольной IV четверти, идущей сразу после блока `Остатки`, поле `chto` = `"кодировка и арифметика"`. Блока `Кодировка` больше нет — он переименован в `Информация` и живёт в III четверти. Поставь `"остатки и арифметика"`. Больше в `god.json` ничего не трогать.

**4. Пересборка и публикация.** `python3 build.py` из `diskmat-57/_sajt/`, затем публикация по якорю 3. Адрес назвать в отчёте строкой.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — живой прогон на реальном объекте репозитория, не на фикстуре: обе проверки идут по живому собранному `dist/index.html`.**

Это ЖИВОЙ ПРОГОН на реальном объекте репозитория, а не фикстура: обе команды читают живой собранный файл `diskmat-57/_sajt/dist/index.html` и живой `diskmat-57/_sajt/god.json`.

```
python3 diskmat-57/_sajt/build.py --proverit     # rc=0
python3 - <<'PY'
import json,re,sys
css=open('diskmat-57/_sajt/dist/index.html').read()
d=json.load(open('diskmat-57/_sajt/god.json'))
setka=[[e for e in q['elementy'] if e.get('tip')=='blok' or e.get('imya')=='Контрольная'] for q in d['chetverti']]
chasy=[sum(e.get('chasy') or 0 for e in q) for q in setka]
nedel=[c//2 for c in chasy]
opred=set(re.findall(r'--d-([A-Z])\s*:', css))
ispol=set(re.findall(r'var\(--d-([A-Z])\)', css))
kol=[int(n) for n in re.findall(r'\.lenta\{[^}]*repeat\((\d+)', css)] or [0]
lin=[int(n) for n in re.findall(r'\.linejka\{[^}]*repeat\((\d+)', css)] or [0]
bad=[]
sirot=ispol-opred
if sirot: bad.append(f'используются, но не определены: {sorted("--d-"+s for s in sirot)}')
if kol[0] < max(chasy): bad.append(f'.lenta {kol[0]} колонок, а самой длинной четверти нужно {max(chasy)}')
if lin[0] < max(nedel): bad.append(f'.linejka {lin[0]} колонок, а недель в самой длинной четверти {max(nedel)}')
if 'кодировка и арифметика' in json.dumps(d,ensure_ascii=False): bad.append('устаревшая подпись «кодировка и арифметика» на месте')
print('часы по четвертям',chasy,'· недель',nedel)
print('определено',sorted(opred),'· использовано',sorted(ispol))
print('РАСХОЖДЕНИЙ',len(bad),'— проверено 4 четверти из 4, доменов',len(ispol),'из',len(ispol))
[print(' ❌',b) for b in bad]
sys.exit(1 if bad else 0)
PY
```

🔴 **Машинного гейта здесь НЕ ХВАТАЕТ, и это сказано прямо.** Он проверит, что переменная определена и колонок достаточно, но не увидит, что страница выглядит правильно. Поэтому обязателен **визуальный гейт**: открой собранный `dist/index.html` в браузере, сними кадр всей страницы и **посмотри на него сам**. Что обязано быть видно: у блоков `Разгон`, `Дирихле`, `Игры` есть цветная плашка, а не прозрачный фон; `Информация` окрашена как блок на стыке; **ни одна четверть не переносится на вторую строку**, полоска недель идёт одной строкой, и весь календарь года помещается в один экран. Не помещается — это провал критерия, а не «мелкая неточность»: одноэкранность названа владельцем важной. Опиши в отчёте, что увидел, и приложи путь к кадру.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)

Верификатор не нужен: операция не лоссовая: правятся две строки CSS, два литерала сетки и одна подпись; ничего не удаляется и не сжимается. Проверка визуальная — рендер страницы и замер getComputedStyle, а не сверка содержания.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПО ХОДУ РАБОТЫ, НЕ ОДНИМ ПОСЛЕДНИМ ХОДОМ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. Каждую завершённую часть работы коммить СРАЗУ, теми же двумя ходами — не копи всё к финальному ходу:
```
git --no-optional-locks add -- diskmat-57/_sajt/build.py diskmat-57/_sajt/god.json diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- diskmat-57/_sajt/build.py diskmat-57/_sajt/god.json diskmat-57/_sajt/dist sayt/static/diskmat-57 diskmat-57/docs diskmat-57/2026-08-18_logika-goda-7   # отсекает всё чужое
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **КОММИТЬ ПО ХОДУ — РЕШЕНИЕ ВЛАДЕЛЬЦА 25.08 (В11), ПЕРЕВЕРНУВШЕЕ прежний канон «одним последним ходом».** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц10) Закончил кусок — закоммитил его; последний ход только ПРОВЕРЯЕТ, что коммитить нечего (`git status --porcelain` пуст, `git_zona.py check --zone` ✅).
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц11)
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц12)
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц13)

**ЗОНА ГИГИЕНЫ:** `diskmat-57/_sajt/build.py` `diskmat-57/_sajt/god.json` `diskmat-57/_sajt/dist` `sayt/static/diskmat-57` `diskmat-57/docs` `diskmat-57/2026-08-18_logika-goda-7`

- **Г1. Зона доехала в git.** `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/build.py` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/god.json` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/_sajt/dist` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone sayt/static/diskmat-57` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/docs` → ✅; `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py check --zone diskmat-57/2026-08-18_logika-goda-7` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *(история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц14) Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц15) Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: коммит → влитие своей ветки в основную → пост-проверка
ИЗ ГЛАВНОЙ ПАПКИ → гашение → вывоз.** Обратный порядок не работает технически: влитие отказывает
на грязном дереве, пост-проверка неисполнима до влития, вывоз — на невлитом. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц16) — теперь вливает САМ заход, но только при зелёной пост-проверке.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВЛИТИЕ СВОЕЙ ВЕТКИ В ОСНОВНУЮ.** Только после того, как шаг 1 дал «вне git 0» на своей
зоне — влитие отказывает на грязном дереве:
```
python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py vlit-v-osnovnuyu zahod/sajt-cveta-i-setka --zone <своя зона> \
    --vsyo-ravno "своя рабочая папка ещё жива — влитие последним ходом захода, штатно"
```
Конфликт — ЗАКОННЫЙ исход, не повод форсировать: разрешай по существу, если понимаешь обе
стороны; не понимаешь — `git_zona.py vlit-v-osnovnuyu --abort`, ветка остаётся невлитой,
строка в отчёт и заявка на влитие (`git_zona.py zayavka --rod git-operaciya`).
🔴 Конфликт на `README.md` — только ОБЪЕДИНЕНИЕМ записей реестра, никогда выбором стороны:
параллельные заходы волны дописали по строке — обе записи правы, выбор одной молча уничтожает
регистрацию соседа.

**3 · ПОСТ-ПРОВЕРКА ИЗ ГЛАВНОЙ ПАПКИ.** Отвечает на вопрос «механизм ВСТАЛ», а не «коммит
виден»: прогон изменённого механизма из `/Users/ivanyakovlev/Documents/GitHub/materials`, НЕ из рабочей папки `/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-cveta-i-setka` плюс `grep` по ЖИВОМУ файлу,
который его зовёт (хук, конвейер, генератор):
```
cd /Users/ivanyakovlev/Documents/GitHub/materials && <команда прогона механизма, который заход менял> && echo $?
grep -n '<как механизм назван в вызывающем коде>' <живая точка вызова>
```
🔴 **Красная пост-проверка = ОТКАТ ВЛИТИЯ И СТРОКА В ОТЧЁТ**, а не «доложу, пусть приёмка
решает»: `git_zona.py vlit-v-osnovnuyu --abort`, если слияние ещё не закоммичено, иначе
`git -C /Users/ivanyakovlev/Documents/GitHub/materials --no-optional-locks reset --hard <хэш ДО влития>`. Заход, который влил
и сломал `main`, обязан вернуть `main` сам.

**4 · ГАШЕНИЕ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged arka/mat-kostyak`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main`
там, где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**5 · ВЫВОЗ.** Вывези СВОЮ ветку; `main` НЕ вывози: если после работы в нём есть невывезенное,
поставь заявку `--rod git-operaciya` и назови число в отчёте. Команда для своей ветки:
`git --no-optional-locks log --oneline @{u}.. | wc -l` → 0.
Ненулевое на своей ветке означает, что работа существует только на этом диске.

**6 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Числа по каждому репозиторию — вне git · невлитых своих
и чужих; невывезенных СВОЕЙ ВЕТКИ, не по каждому репозиторию · результат пост-проверки
(зелёная/откачена) — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число или
красная пост-проверка без объяснения — приёмка читает как несделанную работу: она гоняет те же
команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.
## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**§0.1 git-contour self-check (done before writing this plan, per §0.1 point 1 order):**
- `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'` → `0` (matches snapshot).
- `git_zona.py check --zone <6 paths>` → RED: 14 paths outside git in the zone (4 never staged, 10 modified-uncommitted), exactly the analyst-described state (uncommitted `kod_god-json-v2.md`, edited `kod_*.md` siblings, `diskmat-57/docs/RESHENIYA.md` + `OTKRYTYE-VOPROSY.md`, and `SESSIYA.md`/`UROKI-FABRIKE.md` under the arc folder). This matches §2 "НУЛЕВОЙ ХОД — ГИГИЕНА" exactly, so I do the hygiene commit next, in the main folder, on the main branch, BEFORE creating the worktree (as the file requires — I had created the worktree prematurely on my first move, before this self-check; I dropped it and the empty branch, and will recreate it fresh after the hygiene commit).

**Note on an initial procedural mistake, corrected before any real work:** my very first shell command ran the disciplina-repo `git_zona.py` from `~` (not inside `materials`), so `_resolve_repo()` fell back to the tool's own repo (`disciplina`) and created the worktree/branch there by mistake. I dropped that worktree and deleted the empty stray branch in `disciplina` immediately (no work had happened in it, so this was a clean no-op revert), then re-ran the same command from inside `materials` with `GIT_ZONA_REPO` set, per this repo's own `CLAUDE.md`. No data was lost either time.

**0. Hygiene commit (main folder, main branch, between §0.1 point 1 and point 2):**
Stage and commit exactly the analyst's described uncommitted files — the completed-acceptance zahod file `kod_god-json-v2.md` (+ its `.otmenen` sibling, following the same pattern as the already-committed `.otmenen` siblings of other zahod files in this arc), the six edited `kod_*.md` files with fixed dead paths, and the four edited docs (`RESHENIYA.md`, `OTKRYTYE-VOPROSY.md`, `SESSIYA.md`, `UROKI-FABRIKE.md`). Excluded: my own zahod file `kod_sajt-cveta-i-setka.md` and its `.otmenen` — contract says I never commit those, the analyst does at acceptance. I do NOT edit content of any of these files, only commit what's already on disk.

**1. Create the worktree** (now, after hygiene, so the branch forks from a clean contour) and `cd` into it. Verify `git rev-parse --abbrev-ref HEAD` → `zahod/sajt-cveta-i-setka`.

**2. Read the three whitelisted files** (`build.py` CSS regions, `god.json`, `kod_deplo-sajta.md` §Часть Б).

**3. Three CSS/data edits in the worktree:**
   - `build.py` ~line 156: add `--d-G:#4a7292;` and `--d-M:#7d4f78;` next to the existing domain-color block; do NOT touch `--d-V`/`--d-D` (kept for `god-8.json`, outside my zone).
   - `build.py` ~line 258: replace the literal `repeat(20,...)` / `repeat(10,...)` in `.lenta`/`.linejka` with a computed maximum over quarters (hours for `.lenta`, weeks = hours//2 for `.linejka`), counting only `tip == "blok"` elements plus the "Контрольная" quarter-final item, excluding `kruzhok` and `vne-setki`. One shared scale across all quarters (not per-quarter). I'll compute this in Python inside `build.py` at generation time and inject via template/CSS variable — exact mechanism decided while reading the file.
   - `god.json`: fix the stale `chto` on quarter-IV's post-"Остатки" control from `"кодировка и арифметика"` to `"остатки и арифметика"`. Nothing else in `god.json`.

**4. Rebuild:** `python3 build.py` from `diskmat-57/_sajt/`, producing `dist/index.html` (and the `sayt/static/diskmat-57` copy per the existing publish step, read from `kod_deplo-sajta.md`).

**5. Machine gate:** run `build.py --proverit` (rc=0 expected) and the provided inline Python verification script against the live built `dist/index.html` + `god.json`. Both must be green (0 discrepancies) before the visual check.

**6. Visual gate (mandatory, the machine gate is explicitly insufficient):** open `dist/index.html` in the browser preview, screenshot the full page, and visually confirm: colored fill on `Разгон`/`Дирихле`/`Игры`, `Информация` colored consistently, no quarter wraps to a second row, the week strip stays one row, and the whole year table fits one screen. Report what I actually saw + the screenshot path.

**7. Commit as I go** (build.py + god.json + dist + sayt/static/diskmat-57 together once the rebuild is verified — these are one coupled unit, can't usefully split further), using `add` + `commit -- <paths>` + `git_zona.py check --zone`, per §4.

**8. Publish** per `kod_deplo-sajta.md` §Часть Б, name the published URL in the report.

**9. Final WARNING block ritual before writing the report:** full status check across touched repos, `vlit-v-osnovnuyu` of my branch into main (conditional on a clean zone), post-merge verification run FROM the main folder (not the worktree) plus grep on the live call site, branch cleanup (gashenie), push. Abort/rollback the merge if the post-check is red, per the hard rule.

If anything in this plan turns out to contradict a live file once I read it (e.g. the actual `god.json` structure, or the exact CSS injection point), I'll say so and adjust — this plan is the intended shape, not a script to force through blindly.


## ВОПРОСЫ — (заполняет исполнитель)

1. This zahod's own launch message ("СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ" block, and the literal instructions the owner used to start this session) hands the executor a first-move command — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py worktree add <zahod> --branch zahod/<zahod>` — with no `cd` into `materials` first and no `GIT_ZONA_REPO` set. On a fresh local session (cwd = home directory, not inside any git tree), `git_zona.py`'s `_resolve_repo()` silently falls back to the TOOL's own repo (`disciplina`) and creates the worktree/branch THERE instead of in `materials` — no error, no warning, looks identical to success. I hit this exactly, caught it only because I happened to check the resulting path against the zahod's own stated working-folder address. `materials/CLAUDE.md` already documents the `GIT_ZONA_REPO` requirement, but the launch message itself (which a fresh executor reads and follows before ever loading `CLAUDE.md`) doesn't carry it. Fix candidates: either the launch message's first-move command should `cd` into `materials` first (or embed `GIT_ZONA_REPO=`), or `git_zona.py` should refuse/warn instead of silently substituting a different repo when cwd resolves to neither `materials` nor `disciplina` explicitly. Cost if unfixed: repeats on literally the next zahod started fresh this way — same first move, same silent wrong-repo trap.
   ДОМ: diskmat-57/2026-08-18_logika-goda-7/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> 🔴 **`ДОМ:` — ОБЯЗАТЕЛЬНОЕ ПОЛЕ, И АДРЕС В НЁМ ОБЯЗАН СУЩЕСТВОВАТЬ В МОМЕНТ, КОГДА ТЫ ЕГО ПИШЕШЬ.** Путь, которого нет на диске, — не адрес: такую запись нельзя ни доставить, ни спросить, и она не чинится ничем. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц17) Проверить СВОЙ файл до отчёта — одна команда:
> ```
> python3 _generator/tools/bootstrap_zahod.py --proverit-doma <этот файл>
> ```
> rc=0 — все дома достижимы; rc=1 — назван дом, которого нет (команда печатает какой именно). Тот же разбор гоняет `Г7` приёмки, и у него храповик: у ЭТОГО захода база 0, поэтому первый же недостижимый дом здесь — красный на приёмке, а не запись, которую через неделю никто не найдёт.
> `ДОМ: владелец` — законный адрес и НЕ недостижимый дом: он значит «дома-файла нет вовсе, решение за человеком». Не знаешь пути — пиши его, а не выдуманный путь. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит и «доставлено» без метки на месте, и недостижимый дом сверх базы; достижимое-недоставленное печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. (история цены — `_studio/docs/kak-delat/ISTORII-CEN-zahoda.md (репозиторий disciplina)` Ц18)
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(§0.1 point 1 says calling a subagent is unnecessary for this zahod — "выполни САМ две команды и вставь их вывод в `## ОТЧЁТ`"; I ran exactly those two commands myself, first move, before touching anything. This section records the same run, in the dedicated template slot, so both gate paths are covered.)*
```
$ git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'
0
$ git_zona.py check --zone diskmat-57/_sajt/build.py --zone diskmat-57/_sajt/god.json --zone diskmat-57/_sajt/dist --zone sayt/static/diskmat-57 --zone diskmat-57/docs --zone diskmat-57/2026-08-18_logika-goda-7
❌ зона …: вне git 14 путей (рождено и никогда не ставилось — 4, правлено и не закоммичено — 10)
```
Second command was RED, exactly as §0.1 anticipated ("Вторая красная — сначала приведи в порядок свою зону") and exactly matching §2's named "НУЛЕВОЙ ХОД — ГИГИЕНА" state (analyst's uncommitted `kod_god-json-v2.md` + siblings + `diskmat-57/docs` edits). Fixed by the hygiene commit below before creating the worktree.

**ЧТО СДЕЛАНО** *(с хэшами)*
- Hygiene commit in the main folder, on `arka/mat-kostyak`, BEFORE opening the worktree (as required, between §0.1 point 1 and point 2): `b60331c4` — picked up the analyst's 12 uncommitted files (`kod_god-json-v2.md` + its `.otmenen`, six edited `kod_*.md` siblings with fixed dead links, `SESSIYA.md`, `UROKI-FABRIKE.md`, `diskmat-57/docs/RESHENIYA.md`, `diskmat-57/docs/OTKRYTYE-VOPROSY.md`). Verified `grep -c '^| Р' diskmat-57/docs/RESHENIYA.md` (108, working tree) == same grep on `git show HEAD:...` (108) — no more truncated-at-Р83 bug.
- My own zahod file `kod_sajt-cveta-i-setka.md` and its `.otmenen` were deliberately left out of that commit — contract says the analyst commits those at acceptance, not me.
- Worktree `zahod/sajt-cveta-i-setka` created fresh AFTER the hygiene commit (so its fork point is clean), then the actual task committed there: `2d1a22ad` (see КОММИТ line below).
- Merged (`vlit-v-osnovnuyu`) into `arka/mat-kostyak`: no conflicts, fast, clean.
- Published: `git_zona.py opublikovat --yes` from the main folder — CI run green (https://github.com/d1-d57/materials/actions/runs/34827298472), site verified live at https://d1-d57.github.io/materials/diskmat-57/.
- Pushed both `arka/mat-kostyak` (`e8e336c3..2d1a22ad`) and `zahod/sajt-cveta-i-setka` (new remote branch, now tracking) — neither had unpushed commits left afterward.
- No zayavki were touched (queue not consulted mid-task — nothing here needed it; §0.1's named snapshot of 0 open заявок was the relevant check and I did not add any).

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `да`
*(the one debt named by §0.1 — the dirty zone in the main folder — is the hygiene commit above. No other entry debt was named by §0.1 for this zahod.)*

## ОТЧЁТ — (заполняет исполнитель)

**MISTAKE MADE AND CORRECTED BEFORE ANY REAL WORK (told here first since it explains an out-of-order git trace early in the session):** my first-ever shell command ran `disciplina`'s `git_zona.py worktree add` from `~` (the session's starting cwd) instead of from inside `materials`. `_resolve_repo()` falls back to the TOOL's own repo when cwd isn't inside any git tree, so this silently created the worktree and branch in `disciplina`, not `materials` — exactly the trap `materials/CLAUDE.md` warns about. Caught it immediately (before any edits), dropped that worktree, deleted the empty stray branch (`13f9b6f4`, identical to disciplina's own HEAD, no unique commits, nothing lost), then re-ran correctly from inside `materials` with `GIT_ZONA_REPO` set. Separately, my very first attempt to build the CORRECT worktree (before self-check/hygiene) also had to be dropped and its branch deleted (`e8e336c3` in `materials`, identical to `arka/mat-kostyak`'s HEAD at that moment, again nothing lost) once I noticed §0.1 requires self-check + hygiene commit BEFORE creating the worktree, not after. Both corrections are in НЕОБРАТИМОЕ below for completeness even though nothing was actually lost.

**ЧТО СДЕЛАНО И ЗАЧЕМ**
1. `diskmat-57/_sajt/build.py` line ~156: added `--d-G:#4a7292` and `--d-M:#7d4f78` next to the existing domain palette (kept `--d-V`/`--d-D` untouched — `god-8.json` may still reference them and it's outside my zone).
2. `diskmat-57/_sajt/build.py` line ~258-260 (`.lenta`/`.linejka`): replaced the literal `repeat(20,...)`/`repeat(10,...)` with placeholders `__LENTA_COLS__`/`__LINEJKA_COLS__`, computed per class in `sobrat_klass()` as `max(c['chasy'] for c in g['chetverti'])` (already the in-grid-only hour count: verified by hand that `c['chasy']` equals blok+kontrolnaya hours, excluding kruzhok/vne-setki, for all 4 quarters of `god.json`) and `//2` for weeks, substituted into a new module-global `CSS_TEKUSHAYA` that `stranica()` now reads instead of the static `CSS` constant — same "current-class global, set once per `sobrat_klass()` call" pattern this file already uses for `PREFIKS`/`DOMENY`. One shared scale across all 4 quarters (not per-quarter), as required. `god-8.json` (out of zone, empty skeleton, max 20h) never needs more than `god.json`'s max (22h), so the shared computation doesn't regress it.
3. `diskmat-57/_sajt/god.json` line 578: `"chto": "кодировка и арифметика"` → `"остатки и арифметика"` on quarter IV's control right after the "Остатки" block (confirmed by reading the element order — Остатки → this kontrolnaya → Игры — matches the zahod's description exactly). Nothing else in `god.json` touched (`git diff` shows exactly this one line).
4. Rebuilt (`python3 build.py`), ran both machine gates (`--proverit` rc=0; the inline verification script from the zahod: 0 discrepancies, 4/4 quarters, 5/5 domains used-and-defined).
5. Visual gate: opened the built `dist/index.html` in-browser at 1800×1000 and 1920×1080, screenshotted, and looked myself — Разгон/Дирихле/Игры have solid color fills (not transparent), Информация shows the existing "на стыке" multi-zone gradient treatment (3-way blend K/M/A, correctly labelled), no quarter wraps to a second row at either width, week strip stays one row per quarter (including quarter III's 1..25), whole year fits one screen with room to spare at 1920×1080 (`scrollHeight` 1080 == `innerHeight` 1080, no horizontal scroll).
6. Copied `dist/` → `sayt/static/diskmat-57/` (`rsync -a --delete`, file counts matched 95↔95 before and after, `diff -rq` empty afterward) and published via `git_zona.py opublikovat --yes` per `kod_deplo-sajta.md` §Часть Б. `sayt/data/spisok.yaml`'s existing entry needed no change (title/description still accurate).

**КАК ПРОВЕРИЛ**
- `python3 build.py --proverit` → rc=0 (both classes).
- Full `python3 build.py` → rc=0, "охват полон" for class 7.
- The zahod's own inline verification script against the live built `dist/index.html` + `god.json`: `часы по четвертям [12, 16, 22, 16] · недель [6, 8, 11, 8]`, `определено ['A','D','G','K','M','V','Z'] · использовано ['A','G','K','M','Z']`, `РАСХОЖДЕНИЙ 0 — проверено 4 четверти из 4, доменов 5 из 5`.
- Visual: two browser screenshots (1800×1000, 1920×1080) + one headless-Chrome high-res capture, looked at personally — see АРТЕФАКТ line.
- Post-merge, re-ran the exact same rebuild + gate from the MAIN folder (not the worktree): `git status --porcelain -- diskmat-57/_sajt/dist` after rebuild → 0 lines (byte-reproducible), gate → same 0 discrepancies. This is the §3 post-check "механизм ВСТАЛ" proof.
- Live site: navigated to `https://d1-d57.github.io/materials/diskmat-57/` after the CI run went green, screenshotted — matches the local build pixel-for-pixel in layout.

**ЧТО НЕ ТРОГАЛ**
- `god-8.json` and anything under the `8-*` pages — read-only, outside zone; confirmed empty skeleton (0 elements) unaffected in content, only its shared CSS block text changed (expected, harmless — its own max is 20h, less than the shared 22h).
- `--d-V`/`--d-D` CSS variables — left exactly as they were, per explicit instruction.
- `sayt/data/spisok.yaml` — read, not written (still accurate).
- The 18 unrelated dirty/untracked paths already sitting in the main folder before I started (`_studio/zhurnal/_INFRA-git/INCIDENTY.md` modified, `LYUDI.md`, `metki/`, three `spetsmat-2026/*` files) — none of these are mine, none are in my zone, left untouched. Also left untouched: my own zahod file and its `.otmenen` (analyst's job at acceptance).

**ВЕРИФИКАТОР:** not needed — the zahod itself says so (§3: no lossy operation, two CSS lines + two grid literals + one caption, nothing deleted or compressed). Visual + machine gates substitute, both run and both green.

**ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»:** none. Task done end to end including publish.

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** неприменимо — канал `app`, снимать неоткуда.

**ПОВТОРЯЕМОСТЬ находок:** the git-contour path-resolution trap (§ "MISTAKE MADE…" above) WILL repeat on the next zahod launched the same way from this Mac: a fresh session's starting cwd is the user's home directory, not inside any repo, so the exact first-move command this zahod's own "СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ" block hands the executor (`python3 .../disciplina/_generator/tools/git_zona.py worktree add ...`, no `cd` first, no `GIT_ZONA_REPO`) silently creates the worktree/branch in `disciplina` instead of `materials`. This is not a one-off slip — it's a reproducible mismatch between how the launch message is worded (assumes cwd is already inside the target repo, a cloud-sandbox convention noted by name in the older `kod_deplo-sajta.md` report I read: "Предпосылка (несовпадение путей)... облачная песочница Cowork") and how a fresh local session actually starts. Filed as a queue item below (class NEMEDLENNOE candidate — it will hit the very next zahod, not some future one).

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/diskmat-57/_sajt/dist/index.html` — open in any browser (this is the post-merge, main-folder copy, byte-identical to the worktree build and to what's now published). Also: `/Users/ivanyakovlev/Documents/GitHub/materials-wt/sajt-cveta-i-setka/scratchpad/sajt-cveta-i-setka/god-index-1920x1080.png` — the 1920×1080 visual-gate screenshot referenced above (personal scratchpad, not committed, not part of the zone). Published address: **https://d1-d57.github.io/materials/diskmat-57/**.

**РОД АРТЕФАКТА:** `собранный` (the HTML/dist is generated output; `god.json`/`build.py` are the source edits behind it).

**КОММИТ:** `2d1a22ad` — "diskmat-57 site: add domain colors G/M, compute lenta/linejka grid width from god.json, fix stale IV-quarter control caption" · `git_zona.py check --zone <6 paths>` → ✅ (run from inside the worktree, without `GIT_ZONA_REPO`, since cwd there already resolves correctly — see note in WARNING-block section below). Separately, the hygiene commit `b60331c4` in the main folder (pre-existing analyst edits, not my task content).

**НЕОБРАТИМОЕ:**
- Deleted git branch `zahod/sajt-cveta-i-setka` in the WRONG repo `disciplina` (created there by my own first-command mistake) — hash `13f9b6f4`, identical to `disciplina`'s own HEAD at that moment, zero unique commits, nothing lost; recoverable via `disciplina`'s reflog regardless.
- Deleted git branch `zahod/sajt-cveta-i-setka` in `materials` a second time (created correctly but before the hygiene commit, so its fork point was stale per §0.1's required order) — hash `e8e336c3`, identical to `arka/mat-kostyak`'s own HEAD at that moment, zero unique commits, nothing lost; recoverable via `materials`'s reflog regardless.
- `rsync -a --delete diskmat-57/_sajt/dist/ sayt/static/diskmat-57/` — overwrote the previously-published static mirror. File counts matched (95↔95) before the sync and the two trees were byte-identical afterward, but I did not diff the *old* static tree against the *old* dist tree before overwriting, so I can't rule out a stale filename existing only in old `static/` and not in `dist/` at that moment. Not actually unrecoverable either way: `sayt/static/diskmat-57` was already git-tracked before my commit, so its prior content lives in git history (parent of `2d1a22ad`) regardless of what rsync did on disk.
- Published to the public GitHub Pages site (`https://d1-d57.github.io/materials/diskmat-57/`) — visible to outsiders, explicitly authorized by this zahod's own task text ("задеплоить... адрес назвать в отчёте"). Recoverable by re-publishing the prior commit if ever needed.

## WARNING-block ПРОВЕРКА ФАКТОМ (§6, numbers from commands, not memory)
- **1 · вне git:** worktree zone → `0` (`git_zona.py check --zone` → ✅, run from inside the worktree). Main folder → `18` unrelated pre-existing paths, none mine, listed above under "ЧТО НЕ ТРОГАЛ", left alone. Worktree also has `?? scratchpad/` — my own personal scratch folder, intentionally outside the zone, not counted as a debt. `disciplina` (touched only by my first-move mistake, corrected) → `git status --porcelain | wc -l` → `0`, no leftover worktree registration either.
- **2 · влитие:** `zahod/sajt-cveta-i-setka` → `arka/mat-kostyak`, no conflicts, commit `2d1a22ad` landed clean.
- **3 · пост-проверка из главной папки:** GREEN — rebuild from `/Users/ivanyakovlev/Documents/GitHub/materials` reproduced the committed `dist/` byte-for-byte (`git status --porcelain -- diskmat-57/_sajt/dist` → 0 lines after rebuild) and the machine gate gave the same 0-discrepancy result. No rollback needed.
- **4 · гашение:** `git branch --no-merged arka/mat-kostyak` → empty output, `0` unmerged branches remain.
- **5 · вывоз:** `arka/mat-kostyak@{u}..` → `0` (pushed `e8e336c3..2d1a22ad`); `zahod/sajt-cveta-i-setka@{u}..` → `0` (pushed as new branch, tracking set with `-u`).
- **Отдельная находка (не про мой контур, а про инструмент):** `git_zona.py opublikovat` (unlike `check`/`vlit-v-osnovnuyu`) refuses to run from the main `materials` folder WITHOUT an explicit `GIT_ZONA_REPO`, even though plain `git rev-parse --show-toplevel` from that cwd correctly resolves to `materials` already — it printed "рабочая папка принадлежит другому репозиторию, чем инструмент" and asked for the env var explicitly. Worked immediately once I supplied it, per `materials/CLAUDE.md`'s own documented form. Not a bug I'm reporting as broken — just noting it for the record since it's the opposite direction of the mistake in "MISTAKE MADE…" above (there, omitting the override was wrong; here, omitting it was ALSO flagged as wrong, by design, for this specific subcommand).

*(The queue item for this session's git-contour finding lives in the canonical `## ВОПРОСЫ` section above, not repeated here — see item 1 there.)*

## ПРАВКИ ПОСЛЕ ВЫДАЧИ — (заполняет АНАЛИТИК; исполнитель ЧИТАЕТ)
> 🔴 **Пусто — значит заход не правился с момента выдачи.** Непустой блок читается ПЕРЕД продолжением работы: правка отменяет любое противоречащее ей место выше по файлу, каким бы категоричным оно ни было.
> **Форма строки — жёсткая, по ней судит приёмка:** `### ПРАВКА N · ГГГГ-ММ-ДД ЧЧ:ММ · <что изменилось, одной фразой>`, дальше — что именно перечитать и что откатить, если уже сделано по старой редакции.
> **Аналитик:** внёс правку — обязан ОТДЕЛЬНО послать владельцу короткое сообщение для пересылки исполнителю. Правка, лежащая только в файле, до работающего исполнителя не доезжает: он файл не перечитывает сам.
> **Исполнитель:** прочитал правку — назови её номер в `## ОТЧЁТ` строкой `ПРАВКИ ПРОЧИТАНЫ: 1, 2`. Нет строки при непустом блоке = отчёт не принимается: неизвестно, по какой редакции работали.

<правок нет>

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — приёмка проверила ГЛАЗАМИ и ЗАМЕРОМ на опубликованной странице, а не по отчёту. На 1800×1000: все четыре ленты по 22 колонки в ОДНУ строку, полоса недель одной строкой, высота страницы 1047 при окне 1000 — календарь года одноэкранный, как требовалось. Цвета живые: `--d-G` = `#4a7292`, `--d-M` = `#7d4f78`; Разгон и Дирихле сливовые, Игры синие. У блока «Информация» `backgroundColor` пустой — это градиент «на стыке», а не отсутствие цвета, ложную тревогу не поднимаю. Подпись контрольной IV четверти исправлена. Машинные гейты: 18 из 20.

🔴 **Два красных гейта разобраны, оба НЕ про работу исполнителя:**

1. **Г0 красный по зоне `diskmat-57/docs`** — там лежит незакоммиченной версия 2 `KONCEPCIA-goda.md`, которую аналитик писал ПАРАЛЛЕЛЬНО прогону этого захода. Исполнитель её видеть не мог и не должен был. Правка уедет следующим коммитом.
2. **Г12** — в отчёте нет дословного вывода команды `branch --no-merged`. §0.1 в этом заходе отменён клапаном (контур пуст), и гейт честно говорит, что отличить «отменён» от «не сделал» по тексту не может. Проверено руками: невлитых `zahod/*`-веток 0, ветка `zahod/sajt-cveta-i-setka` влита (Г14 зелёный).

**Отдельно — исполнитель сам назвал свою ошибку до того, как её нашла приёмка**, и это правильное поведение: первая команда сессии создала worktree и ветку в `disciplina` вместо `materials`, потому что стартовала из `~`. Поймал сразу, до правок, убрал обе лишние ветки, ничего не потеряно. Находка повторяемая — урок записан в `UROKI-FABRIKE.md`, дом `disciplina-zahod`.

**ВЕТКА РАБОТЫ:** `zahod/sajt-cveta-i-setka`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 /Users/ivanyakovlev/Documents/GitHub/disciplina/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

заявок нет: заход сам закоммитил зону, влил ветку в `arka/mat-kostyak`, вывез на origin и опубликовал сайт; отказов git-операций не было, чинить через заявку нечего.

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
