# Файлы без поля `opisanie:` в шапке — унаследованный долг.
# Гейт их не валит, только считает. Новый файл в этом списке
# появляться НЕ должен: если появился, значит долг растёт.
kurs-puti-i-volny/INDEKS.md
