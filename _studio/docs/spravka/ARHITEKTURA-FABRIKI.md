# ARHITEKTURA-FABRIKI — три архитектуры + карта конвейера

> **КОГДА читать:** нужен полный дизайн фабрики или карта шагов. · **ЧТО дописывать сюда:** архитектурные детали (решения-однострочники → `../pochemu-i-videnie/RESHENIYA.md`). · **КУДА дальше:** `../KARTA.md`. Прайор-арт-обоснования — `PRAJOR-ART.md`.

## Три сцепленные архитектуры
1. **Презентация** — из чего состоит финальный дек как файлы.
2. **Фабрика** — папка-конвейер: стадии создания как арки, по которым агент идёт через Cowork.
3. **Процесс-память (ARC)** — история проекта/стадия (дома) + задача-локальные арки. Механика — `../kak-delat/ARKA.md`.

**Замок:** каждая стадия (арх. 2) исполняется КАК заход (арх. 3) и производит файлы источника презентации (арх. 1), поверх замороженного канон-скелета и генератора.

## Арх. 1 — презентация: три слоя файлов
- **Канон-скелет (общий стандарт):** `shablon.html` (4 блока), `engine.js` (~70% общий core + пер-дек патчи), палитра-ядро в `tokens.css` (идентична у dandelin/buffon), `fonts/` (Forum + Glacial Indifference + Noto). Заморожен.
- **Пер-дек контент:** `content/*.md`, `slides/*.html`, `illustrations/*`, свои type-токены.
- **Опц. расширения:** 3D (`chapters/ drivers/ lib/three`) · математика (`math/katex.json + overlay.css`) · симуляции (`sims/lab.js`).

Forward-path нового дека: копируешь канон-скелет → пишешь `content/ + illustrations/` → подключаешь расширения → генератор собирает. Новые формулы — math-арк (Р7).

## Арх. 2 — конвейер: 2 фазы, ~10 шагов

**Фаза I — ИНДИВИДУАЛЬНОСТЬ** (интерактив; растёт карточный граф → нить → гибрид-источник; вкус и гейты автора; модель — `FAZA-1-REDESIGN.md` + Р19):

| # | Шаг | Выход | Гейт |
|---|-----|-------|------|
| 1 | Бриф (лёгкая разведка) | 8 полей: тема·аудитория·длит.→бюджет слайдов·throughline-кандидат·что резать·материал/референс·уровень строгости·формат события+лекция/курс | бриф полон? |
| 2 | Ресёрч = карточный граф (котлы математика‖научпоп, doc-движок) | узлы-карточки + `KARTA-OBLASTI` + нити | фактура+разворот «но» есть? |
| 3 | Строгая матбаза = слой `skelet` | доказательства в полном объёме, якоря, `⚑ Флаг`→`Флаг закрыт` | математика верна? |
| 4 | **Нарратив + гибрид-источник** (линчпин) | `rasskaz` (нить, 5 актов) ‖ `skelet`, сшивка цитатой-сноской + описания илл. `🖼` | источник закрыт? |
| 5 | Раскадровка | слайды+бюджет; на каждый archetype+дистилляция+сцены+илл (содержание, не финальный текст); регистр+бюджет слов деки | что на каждом — ясно? |

← конец Фазы I, СТОП-точка. Дальше автомат.

**Фаза II — МЕХАНИКА** (автомат; fresh-context; локально):

| # | Шаг | Чем | Внешний скилл? |
|---|-----|-----|----------------|
| 6 | Плакатный текст | локальный text-инструмент из корпуса | нет (термины → `math-russian-terminology`) |
| 7 | Вёрстка (archetype→грид, fit) | канон-ядро html-slides-studio | нет |
| 8 | Сцены (`{@N}/{fill}/{blur}`) | локально | нет |
| 9 | Иллюстрации (по описаниям) | inline-SVG + SVGO + verify | 3D → `threejs`, код |
| 10 | Сборка + QA | math-арк→кэш; `build_deck.py` (render-identity); audit + headless-verify + глаз | нет |

Параллельно: 8↔9, частично 6↔9. Внешние скиллы — только шаг 9 и точечно термины.

## Три ресёрч-потока (врезка)
Карта шагов несёт ДВА ресёрч-потока: шаг 2 (ресёрч = карточный граф, котёл научпоп, единица=сюжет) и шаг 3 (строгая матбаза, единица=факт/доказательство). **Третий поток — ресёрч задач** (единица=ЗАДАЧА) идёт ПАРАЛЛЕЛЬНО, не отдельным номером-шагом: как циферблаты, он ортогонален карте. Он наполняет курс-масштабный выход «Ф7 · Задачи» (листок) и картотеку зоны курса; включается, когда циферблат «лекция↔курс» сдвинут к «курс» или в профиле «мини-курс вокруг одной идеи» (`PROFILI-CIFERBLATY.md`). Чейнится: задача-вход отдаёт утверждение в `skelet` (шаг 3) и историю в котёл popsci (шаг 2). Метод — `../kak-delat/RESERCH-ZADACH.md`; карта источников — `BAZY-ZADACH.md`; завершается запуском корпусов в `istochniki` курса (`MANIFEST`/`VYCHITANO`/`kartoteka`), не каталогом.

## Форматы и нюансы
- **Формат-инвариант:** источник — HTML(+MathJax рантайм для авторской вычитки) с шага 3; финальный дек — генератор + статический KaTeX-кэш (не рантайм). Два контекста рендера.
- **Нюанс:** стиль источника (шаг 4, ближе к журнальной прозе) vs слайда (шаг 6, плакатный) — разные; решить при сборке арки.
- **Оркестровка:** не полагаться на кастомные `subagent_type` — general-purpose + инлайн-роль + явный `Skill` + `model`-оверрайд.

## Диагноз, из которого всё (прогон «платоновы тела»)
Не скиллы по отдельности, а (A) фаза 1 не была единым алгоритмом; (B) нужен ОДИН гибрид-документ, которого нет ни в одном скилле (`popsci-narrative` срезал математику `popsci-research`); (C) метания MD↔HTML. Полный отчёт — у владельца (`process-report.md`).

## Следующий заход
Собрать `_studio`-конвейер: 10 арк-файлов (SKILL.md-паттерн) с контрактами вход→выход и гейтами; старт — арка «гибрид-источник» + сбор text-корпуса.

---
*Дизайн фабрики. Прайор-арт — `PRAJOR-ART.md`; решения — `../pochemu-i-videnie/RESHENIYA.md`; арк-механика — `../kak-delat/ARKA.md`.*

## Гейты картотеки и дисциплина коммита — что построено арками 26–28.07

**Пять ворот** (`_generator/tools/check_kartoteka.py --gates`, вшиты в `.githooks/pre-commit` с `--staged`): карточка заведена мимо картотеки · карточка не в индексе по роду · ссылка на несуществующий `id` · ссылка на несуществующую секцию рамы · `.md` в `_studio/` без строки в `docs/KARTA.md §6`. Каждое ворото заведено по УЖЕ случившейся поломке; «на всякий случай» нет ни одного.

**Два греп-стойких послабления, и они названы лазейками вслух:** `🔒 АРКА ЗАКРЫТА` в шапке `PLAN.md` снимает ворота 4 и 5 с файлов закрытой арки · `🗄 АРХИВ — ГЕЙТ НЕ СУДИТ` в шапке файла снимает все пять. Держатся не запретом, а **видимостью**: гейт печатает, сколько файлов и арок он не судил, ВСЕГДА — в том числе когда зелено. Зелёный чекер с необъявленной слепой зоной опаснее красного.

**Почему послабления понадобились:** ворота судили всё подряд и дали 219 ❌, из которых настоящим долгом было **пять**. Остальное — рабочие потроха закрытых арок и архивы мёртвых рамок. Число такого порядка обесценивает красный цвет, и обход из исключения становится рутиной.

**`git_zona.py commit --no-verify "<причина>"`** — законный обход при ЧУЖОМ красном. Причина обязательна (голый флаг отклоняется человеческим отказом) и сама уезжает в `INCIDENTY.md`. Флаг встроен в общее ядро обеих троп коммита, чтобы они не разъехались по поведению. Три половины держит ловушка 15 фикстуры, каждая проверена мутацией.

**Известные дефекты ворот, не починенные:**
- ворота 4 не различают мёртвую ссылку · имя строки таблицы · **текст ПРО мёртвую ссылку**. Последнее ловит записи разбора и учит прятать токены вместо починки. Лечение: искать адреса только в полях `источник:`/`связи:`;
- ворота 5 матчат по **голому имени файла** — документ проходит, если такое имя есть у любого другого зарегистрированного файла;
- разделения на **блокирующие** и **предупреждающие** нарушения нет. Пока долг мал — не мешает; при накоплении красный обесценится снова.

## Сеть песочницы: российские площадки недоступны

`mccme.ru`, `ium.mccme.ru`, `math.msu.su`, `mi-ras.ru`, `vyshka.math.ru`, `gorod.bogomolov-lab.ru` дают **TCP-таймаут** из песочницы — не 404 и не блокировку контента, соединение не устанавливается. Доступны `verbit.ru`, `math.hse.ru`, `arxiv.org`, `mathnet.ru` (частично), `web.archive.org`.

⇒ маршрут к российским материалам — через `web.archive.org`, со скоростью 7–10 с на страницу и рейт-лимитом (за один ночной прогон архив отбил соединение пять раз). **Ссылки в выходах приводить в исходном виде с пометкой «через архив»**, а не архивным URL: иначе адрес протухнет вместе со снимком.

## Четыре узла плагина `disciplina` — что выяснилось при подключении (арка 2026-08-05, дата данных 2026-08-07)

Плагин `disciplina` — семь скиллов, вынесенных из `_studio/` в отдельный git-репозиторий рядом с `materials/`. Ниже — четыре места, где устройство неочевидно и где на этом уже спотыкались.

### Узел 1 — `GIT_ZONA_REPO` единственная дверь в чужой репозиторий

`korni.py:42`. Через неё ходят **все** инструменты `_generator/tools/`, когда работают в `disciplina`. 07.08 выяснилось, что она переключает корень (`REPO`), но **не таблицу корней** (`КОРНИ`): дверь регистрации внутри `disciplina` принимала ровно один путь, `README.md`, потому что таблица отдавала корни `materials` плюс несуществующий там префикс `disciplina/`. Починено заходом `dver-urokov`.

🔴 **Расхождение того же рода — не разовое, а класс:** инструмент фабрики знает один репозиторий, а работа идёт в двух. За сутки трижды (уроки 53, 57, 67). Заводя инструмент, который резолвит пути, спрашивай, откуда он берёт базу, — константа `REPO` там почти всегда неверна.

### Узел 2 — гейтов петли три, а не два

| гейт | что судит | как |
|---|---|---|
| `check_zahod.py` | **текст захода** (З1–З9) | лексически: есть ли секции, единственны ли, не противоречит ли критерий запретам того же файла |
| `priyomka.py` | **отчёт исполнителя** (Г0–Г8) | Г0 — чистота зоны первым ходом; отчёт с грязной зоной не читается |
| `check_sborki.py` | **утверждения захода о мире** (С1–С7) | не читает, а **исполняет**: существует ли названная ветка, разрешается ли путь-якорь, лежит ли инструмент в git |

Третий появился 07.08. Он работает только на свежесобранном заходе: на корпусе 91 файл из 176 красный и полтора часа прогона против 0.8 с на файл — то есть это гейт сборки, а не ревизии.

### Узел 3 — дом инцидентов ≠ дом скилла ≠ дом долга

Три разных файла, три разных предмета, и путать их дорого:

- `disciplina/doma/<дом>/POKRYTIE.md` — **инциденты**: они ссылаются на корпус в `materials`, а не содержат его;
- `disciplina/skills/<имя>/DOLG.md` — **долги** нашего скилла;
- `disciplina/skills/<имя>/NEPOKRYTOE.md` — **непокрытое** у погашенного ВНЕШНЕГО скилла: что было в нём и чего нет у нас.

Домов инцидентов четыре: `meta`, `slajdy`, `reserch-cikl`, `illustracii`. Разобран один — МЕТА (775 из 775).

### Узел 4 — гашение внешнего скилла обратимо, и знать это надо ДО, а не после

`disciplina/arhiv-skillov/` держит **побайтовые копии** внешних скиллов (`html-slides-studio` — 125 файлов, 7.7 МБ), и они под git. Поэтому отключение скилла в облачном реестре ничего не стирает: содержание остаётся на диске и в истории.

🔴 **Почему это узел, а не удобство.** Обратимость меняет цену решения на противоположную: вопрос «гасить ли» перестаёт быть рискованным и становится дешёвым, а вот вопрос «перенесли ли содержание» остаётся дорогим — и его легко спутать с первым. 07.08 приёмка рекомендовала не гасить, владелец погасил вопреки и оказался прав ([Р69](../pochemu-i-videnie/RESHENIYA.md)): непокрытое было добрано двумя часами позже, чем снят вердикт гейта.
