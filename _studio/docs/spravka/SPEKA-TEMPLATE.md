# SPEKA — {{ПРОЕКТ}} (единый источник истины: ЧТО строим)

> **ЧИТАЕТСЯ ПЕРВОЙ** каждым заходом и каждой волной сборки — до `kotly/matematika.md`, `raskadrovka/TEKSTY*`, самого захода. Намерение проекта живёт ЗДЕСЬ и только здесь.
> **Один дом охвата.** «Что входит / что строим» — не в matematika, не в STANDART, не в заходе, не в памяти. Если охват противоречит другому доку — прав SPEKA.
> **Дистиллят, не дубль фабрики.** Канон (движок, формат, гейты) не переписываем — ссылаемся и называем ТОЛЬКО отклонения. Числа и доказательства — в `matematika`; тексты — в `TEKSTY`; процесс — в арке.

---

## Слой 1 — СТАБИЛЬНЫЙ (правится редко)

### Проект — один абзац
<Что за материал, для кого, профиль/масштаб (см. `PROFILI-CIFERBLATY.md`), throughline одной фразой.>

### Метод — как доказываем/строим
<Ведущий приём (напр. двойной счёт / биекция / инвариант). Что разрешено и что запрещено. Ссылка на строгий слой: `kotly/matematika.md`.>

### Охват — ЧТО входит (ПО ПОРЯДКУ, полностью)
<**Базовое правило проекта** одной фразой (напр. «идём по первоисточнику, закрываем ВСЕ утверждения, ничего не паркуем молча»). Затем — полный упорядоченный список единиц (объекты / утверждения / задачи). Если что-то НЕ входит — это **named-исключение здесь** (с уровнем «сильной группе» / «вне-школы»), а не тихий срез в заходе.>

### Наследование фабрики — «дефолт, кроме…»
<Что берём как есть из канона (перечислить домами: движок-примитивы, формат вёрстки, гейты). Затем — список ТОЛЬКО отклонений этого проекта от дефолта, каждое одной строкой.>

### Формат-отклонения (визуальный контракт проекта)
<Палитра опорных точек (Задача/Определение/Утверждение/Теорема — какие цвета), раскладки, шрифт-акценты, правила reveal — только то, что уточняет/отменяет канон `konvejer/07-verstka/DOK.md`. Точные токены — из `src/tokens.css`.>

---

## Слой 2 — ЖИВОЙ (пер-единица «здесь хочу то-то»)

> Роутер + намерение на каждую единицу (слайд/сцену). Растёт по ходу; сюда садятся правки владельца «на слайде X хочу…». Единственный дом финального порядка и ретайра.

| # | единица | намерение (что показать / как) | текст | математика |
|---|---------|-------------------------------|-------|------------|
| 1 | <id> | <что здесь, сцены, формат> | `TEKSTY §..` | `matematika Утв..` |

**Ретайр (что убрано из порядка):** <файлы → `_zadel/`, вне slide_order.>

---
*Намерение проекта. Истина — `kotly/matematika.md`; тексты — `raskadrovka/TEKSTY*`; канон/дефолт — `_studio/`; процесс — арка `zhurnal/<арка>/`.*
