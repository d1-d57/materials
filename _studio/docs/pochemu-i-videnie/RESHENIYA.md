# RESHENIYA — принятые решения (что / почему / что отвергнуто)

> **КОГДА читать:** нужно «почему так решено» или свериться перед новой работой. · **ЧТО дописывать сюда:** осевшие решения (живой статус и шаг — в `../sostoyanie/OTKRYTYE-ZADACHI.md`; идеи на будущее — беклог там же). · **КУДА дальше:** `../KARTA.md`. **Числа/факты — сверять по живым файлам, не по памяти.**

## Р1. Источник → генератор → самодостаточный HTML, гейт render-identity
Папка-источник (`content/*.md` + `illustrations/*` + токены + `shablon` + `engine`) собирается `../../../_generator/build_deck.py` в один HTML; правка гейтится нормализованной сверкой с оригиналом. **Почему:** правка = дешёвое изменение markdown, не кода; выход самодостаточен.

## Р2. Формат текста слайда — шорткаты
`{@N}` сцены, `{fill@N|…|…}`, `{blur@N|…}`, `**acc**`, `$tex$` из кэша; `accent_tag` в `brief.md` (`b` Dandelin / `span` Buffon). **Почему:** любое слово/формула/акцент — правка одной строки md.

## Р3. Фабрика самодостаточна, НЕ дирижёр
Внешний скилл — только где способность медиум-независима и инструментальна: 3D → `threejs`, код, маттермины → `math-russian-terminology`. Всё специфичное для презентаций — локально. **Почему:** generic-скилл кодирует чужой формат и активно мешает. **Отвергнуто:** дирижировать цепочкой скиллов — это и было причиной провала прогона «платоновы тела» (`popsci-narrative` срезал математику `popsci-research`).

## Р4. Стиль текста слайдов — локальный инструмент
Плакатный баланс (законченные фразы, минимум символов) — собрать из корпуса старых презентаций. **Почему:** это отдельный стиль; `russian-editor` (литературный) для него неверен.

## Р5. Линчпин — гибрид-источник
ОДИН документ: живая история + математика финального объёма (доказательства в тексте) + свёрнутые `<details>` + описания иллюстраций. **Почему:** он один кормит слайды, поэтому математику не срезают; такого режима нет ни в одном скилле.

**Пересмотрено (Р19, 2026-07-10):** режим «один гибрид-файл» устарел → заменён карточным графом (ресёрч) + двумя слоями `rasskaz` (нарратив) ‖ `skelet` (строгая матбаза), сшитыми цитатой-сноской; `<details>` — только оглавление. Линчпин-идея (математику не срезают) сохранена: `skelet` держит полный объём доказательств.

## Р6. Конвейер — 2 фазы, ~10 шагов
Фаза I «индивидуальность» (бриф→ресёрч→строгая матбаза→гибрид-источник→раскадровка; гейты автора) → Фаза II «механика» (плакатный текст→вёрстка→сцены→иллюстрации→сборка+QA; автомат). **Почему:** индивидуальность автора — только в фазе I; дальше автоматизируется. Детали — `../spravka/ARHITEKTURA-FABRIKI.md`.

## Р7. Math-арк — изолированный Node+katex
Новые формулы рендерит `katex.renderToString`, версия pinned, батчем в тот же `{tex→html}` кэш. **Почему:** чистого stdlib-пути нет; единственный способ совпасть с классом разметки кэша. Ядро-генератор остаётся stdlib — Node ТОЛЬКО в math-арке. **Отвергнуто:** рантайм KaTeX/MathJax (ломает самодостаточность/детерминизм), SVG-движки (несовместимый markup).

## Р8. Дизайн-система — токены-tiers, свой эмиттер
`canon.tokens.json` (primitive: палитра/тип/шрифты) + semantic-алиасы (деки читают ТОЛЬКО их) + пер-дек оверрайд; свой ~50-строчный stdlib-эмиттер инлайнит merged `:root` в каждый дек. **Почему:** «определить раз, наследовать много», без внешнего tokens.css. **Отвергнуто:** Style Dictionary / npm как зависимость.

## Р9. Арк-файлы — паттерн SKILL.md
Триггер-фронтматтер / короткое тело / якоря-по-требованию; REPORT = дистиллят 1–2k токенов (сырой контекст вниз не передавать); явный `next`+checkpoint. **Почему:** конвергенция зрелых агентных фреймворков; чистый контекст на стадию.

## Р10. Иллюстрации — живой inline-SVG
Свой `.svg` на илл., инлайнить как живой `<svg>` (не base64/`<img>`); SVGO-нормализация (pinned + per-file id-prefix); headless-верификатор (пустая/одноцветная зона ловит чёрную заливку). **Почему:** до `<svg>` дотягиваются `var()`/`currentColor`; в `<img>` — нет (изоляция → чёрная заливка).

## Р11. Проект фабрики — в `materials/_studio`, ARC-дисциплина
Живёт в `materials/_studio/`, git-откат из `materials`; дисциплина воспроизведена с `spetsmat`/`kurs leto` (дома, арки, заходы, single-index). **Почему:** фабрика развивается — нужна память и дисциплина, чтобы наработанное не терялось.

## Р12. Старт сборки — с арки гибрид-источника + сбор корпуса
**Почему:** гибрид-источник — линчпин; локальному text-инструменту нужен корпус.

## Р13. Интервью-арка — под матлекцию, не общий скилл
Арка 1 (интервью/бриф) — специально под математическую лекцию (у неё заранее понятная арка: что спрашивать и зачем), НЕ общий `the-interviewer`/`brainstorming`. **Почему:** общий интервью-скилл даёт не то — как `popsci-research`/`popsci-narrative` давали не то в «платоновых телах».

## Р14. Сборка арок — по трём классам
Каждая из 10 арок собирается по своему классу: **ПЕРЕНОС** (вычленить из `html-slides-studio`: вёрстка/сцены/сборка+QA/архетипы) · **ОРКЕСТРАЦИЯ** (тонкая обвязка внешних: 3D→`threejs`, код, термины) · **GREENFIELD** (придумать заново: интервью, ресёрч+матбаза-поток, гибрид-источник, раскадровка, text-стиль). **Почему:** разный способ сборки → разный подход и приоритет. Перенос/harvest — bounded, распараллеливаются агентами; greenfield — интерактивно с вкусом владельца. Разбивка задач — `../sostoyanie/OTKRYTYE-ZADACHI.md`.

## Р15. Семейство движков markdown→HTML — `_generator` дом движков, `_studio` мозг
markdown — источник истины (дёшево править), HTML — вид, пересборка — чистый Python БЕЗ токенов, линтер-гейт. `_generator/` держит ДВА движка одного семейства: **дек** (`build_deck.py`, фикс-канвас 1440×810+сцены, ГОТОВ) и **документ** (doc-движок для растущих markdown-источников Фазы I с полями/`status`, портировать из `build.py` проекта `kurs leto 2026` — СПРОЕКТИРОВАН, не реализован). `_studio` ими пользуется, не содержит. Принцип распространяется на всю Фазу I (один источник istины на всех стадиях). Детали — `../../_generator/DVIZHKI.md`. **Почему:** правка HTML вручную дорога; markdown+пересборка дёшева; у дека и документа общая философия, но разные модели данных. **Отвергнуто:** сливать движки в один код (модели данных различны); слить `_generator` в `_studio` (движок шире фабрики — лекции/питчи `kurs leto`/`catalan` того же семейства). **На будущее:** doc-движок как общий скилл для всей работы; правка «поверх HTML» (in-place, à la Claude Design).

## Р16. База канона = buffon (не dandelin)
Скелет greenfield-`src/` и пер-дековый `base.css` берут за дефолт канон **buffon**. **Почему:** buffon совпадает с Canva-шаблоном автора и большинством референсов (вёрстка, цвета) — это фактический стандарт; dandelin был ПЕРВЫМ пробным прогоном фабрики, собран быстро и «разъехался» (byte-shared общий base на оба эталона невозможен — канон реально расходится, доказано `verifikacia-infra.md`). Отдельные удачные элементы dandelin (обложка, картинки) — свести/доработать позже отдельным заходом дизайн-системы. **Отвергнуто:** dandelin-дефолт (текущий в скелете — переключить на buffon); единый общий base на оба (= рестайл, KONSTITUCIYA §1).

## Р17. Автономность фабрики — скиллы ВНУТРЬ, не в прод (ужесточение Р3)
Фабрика — самодостаточный продукт; внешние скиллы в проде НЕ вызываются, кроме чисто технических (реальный код; иллюстрации/3D → `threejs`). Содержимое полезных скиллов (`popsci-research`, `popsci-narrative`, `the-interviewer`, `math-russian-terminology`…) ПЕРЕНОСИТСЯ внутрь — разбивается на куски и раскладывается короткими конкретными шагами процедуры. Пример: `math-russian-terminology` → заменяется на **per-project документ-список терминов** (заполняется заранее) + шаг «сверься с ним»; сам скилл триггерился после каждого куска и заново гуглил написание — это баг. Метод сборки: перенести ВСЁ многословно → выкинуть лишнее, где неадекватно → свести к чётким коротким шагам, полупустой контекст. **Почему:** generic-скилл кодирует чужую цель/формат (статья в интернете, «сам разобраться») и мешает; нужна адаптация под конкретную задачу. **Ужесточает Р3** (убирает маттермины из списка допущенных внешних — теперь внешние только код+3D).

## Р18. Одна фабрика — и лекция, и курс (нет принципиальной разницы)
Лекция и курс делаются ОДНИМ механизмом; курс = запуск фабрики многократно в разных контекстах (сначала концепт всего курса — набор сюжетов, порядок лекций, мосты; потом каждая лекция, уточняя/наполняя тот же большой документ). Костяк-текст может быть костяком одной лекции ИЛИ целого курса (как `catalan`: документ выглядит как лекция, а это весь курс). Мосты/пересечения между лекциями — фиксируем. Фабрика производит и КОНТЕНТ (история+математика), и презентацию/сайт-материалы поверх (текст+слайды выкладываются). Раньше — два скилла (создание истории; создание презентации из истории), объединены в одну фабрику; при нужде легко разделить. **Почему:** механизм универсален, разделение искусственно. Разворачивается в «фабрику курсов» (масштаб держим в уме, пока не расширяем). Циферблат масштаба «лекция↔курс», реализующий этот принцип операционально, — `../spravka/PROFILI-CIFERBLATY.md`.

## Р19. Верхняя часть Фазы I — редизайн (карточный граф → котлы-теги → нить→rasskaz)
Ресёрч и нарратив — РАЗНЫЕ стадии/сессии. Ресёрч растит **карточный граф** (узлы-стикеры формат Каталана `id·род·находка·связи·уровень·источник·статус` + карта нитей + дисциплина «находка без карточки=потеряна»); карточки тегируются котлом **математика|научпоп** — ОДИН граф, «два котла» = два тег-фильтра, показанные двухвкладочным doc-движковым видом (D1). Критерии: математика — «лекцию можно заменить набором вопросов»; научпоп — «каждый факт = мини-история». Строгая матбаза = слой `skelet`; мат-долг `⚑ Флаг`→`Флаг закрыт` (грепается, снимает арка 3). Нарратив — отдельная стадия: вытащить НИТЬ → `rasskaz` (5 актов). Сборка источника = `rasskaz`+`skelet`, сшивка цитатой-сноской (НЕ переусложнять). Раскадровка = дешёвый doc-движковый вид; контракт 5→6 несёт `register`+`word_budget`+`accent_tag`+`slide_order`. Пакеты `konvejer/01–05` переписаны. **Ratified развилки (владелец 2026-07-10):** proof-self-review — метод ВНУТРЬ (не внешний вызов); карточки — тег котла над одним графом (не два набора); нарратив — под-стадия `4a` (без renumber, 06–10 целы); сборка — цитата-сноска (не `<details>`). **Почему:** grounded в живой практике Каталана + Р15/Р17/Р18. **Пересматривает Р5.**

## Р20. Автономность внесена; kurs-fabrika абсорбирована и удалена
(1) Содержимое скиллов внесено ВНУТРЬ пакетов Фазы I (Р17): the-interviewer→01, popsci-research→02, popsci-narrative→04a, proof-self-review→03 (шагами), math-russian-terminology → per-project `TERMINY.md` + шаг «сверься». Живых внешних вызовов в 01–05 нет. *Остаток:* `06-tekst`/`09-illustracii` ещё зовут внешние термины — правка Фазы II (беклог).
(2) Старт-кит `kurs-fabrika` (v1) абсорбирован: курс-уровневая методология (профили ПОЛНЫЙ/ЛЁГКИЙ-научпоп/ОТ-КНИГИ + циферблаты масштаба, вкл. новый **лекция↔курс** + Ф7-Задачи/Ф8-Анонс) → `../spravka/PROFILI-CIFERBLATY.md`; заход «программа курса» → `../kak-delat/PROGRAMMA-KURSA.md`; таблица тип-захода→модель + текст запуска → `../kak-delat/RUKOVODSTVO-zahodami.md`. Свежий верификатор: список потерь ПУСТ. Репо **УДАЛЕНО** (бэкап `../../zhurnal/2026-07-10_sborka-konvejera/backup_kurs-fabrika_20260710.tar.gz`). Процесс-каркас `kurs-fabrika` — дубль (наши ARKA/RUKOVODSTVO/COWORK эволюционнее), не тащили. Анонс (Ф8) — связный текст, `russian-editor` уместен (Р4 — только для слайдов). **Почему:** Р18 (лекция=курс) требует курс-масштабную методологию; дубли и заброшенный репо убраны.

## Р21. Оркестрация фабрики — обёртка «идея→публикация» (трекер · единый алгоритм · каталог · папка лекции)
Достроена ОБЁРТКА поверх готового конвейера (арка «оркестрация фабрики» 2026-07-10; грунтовка — прогон многогранников `process-report.md` + арки Каталана). Пять частей:
1. **Трекер = журнал вызовов + read-only аудит диска** (`../../_generator/tools/sostoyanie.py`, в духе `audit.py`): читает папку лекции, по реестру гейтов печатает `PASS/FAIL/WARN` + «⏺ ты здесь» (первый незакрытый жёсткий гейт) + exit-код; **гейты-НЕ-рельсы** (возврат-волна — норма; ловим МОЛЧАЛИВЫЙ прыжок вперёд сверкой журнал↔диск). Диск = правда, «где мы» пересчитывается, не держится руками.
2. **Единый спинной алгоритм на ОБЕ фазы** (`../../konvejer/ALGORITM.md`: волновой драйвер Фазы I 1–5 + детерминированный 6–10 + преамбул «сессия=вызов, состояние на диске, гейты-не-рельсы») + **реестр гейтов** `../../konvejer/GEJTY.md` (16: G1–G11 + H1–H5, каждый с грепаемым признаком-на-диске — данные для трекера). Таблица Арх.2 приведена к Р19.
3. **Функции = внутренние процедуры + КАТАЛОГ, НЕ skill-файлы** (`../../konvejer/KATALOG.md`: станции 1–10 + под-функции Фазы I + микрофункции, у каждой «инструмент-под-задачу»). **Усиливает Р17:** skill-файлы на микрофункцию отвергнуты (авто-триггер по фразе вредит в управляемом конвейере; драйвер маршрутизирует явно). Скилл на всю фабрику — возможен потом одним скиллом (PLAYBOOK §7).
4. **Бутстрап папки лекции** (`../../_generator/tools/bootstrap_lekcia.py` + канон `../spravka/PAPKA-LEKCII.md`): одна папка по ролям — `brief.md` · `kartoteka/` · `istochniki/` · `kotly/` (matematika=слой skelet ‖ naupop) · `chernovik/rasskaz.md` · `raskadrovka/` · опц. `src/`+`dist/`. **Слайды опциональны** (`слайды: да|нет` в брифе; лекция «на доске» — без `src/`, Фаза II N/A). Модель — трёхдомная Каталана под лекцию.
5. **Место журнала** — `<лекция>/dnevnik/zhurnal.md` (append-only, отдельно от прозы `SESSIYA`), формат — `../../konvejer/ZHURNAL.md`.

**Провизорно (подтвердит первый реальный дек #14):** слой `skelet` физически = `kotly/matematika.md`; ряд диск-признаков гейтов Фазы I — эвристики (беклог — `../sostoyanie/OTKRYTYE-ZADACHI.md`). **Почему:** цель владельца — «фабрика ведёт по шагам сама, я только отвечаю/верифицирую»; обёртка делает состояние машинно-проверяемым (закрывает болячку Каталана «дисциплина держалась на человеке»). **Отвергнуто:** skill-файлы на микрофункцию (Р17); отдельный мутируемый файл-состояние (рассинхрон с диском).

## Р22. Автоматический жизненный цикл арки + гид — «Cowork ведёт сам, владелец на бегу»
Финальный (⑤) заход арки «оркестрация» достроил ПОВЕРХ обёртки (Р21) слой, который делает работу «самоведущей» (цель владельца: «я не держу план в голове — Cowork, читая доки, сам понимает стадию, зовёт хэндофф при переполнении окна, при конце арки закрывает И ТУТ ЖЕ открывает следующую»). Три части:
1. **Раздел `../kak-delat/ARKA.md §10`** — 3 явных триггера с конкретикой «что Cowork ДЕЛАЕТ»: **A гид-по-стадиям** (после каждого хода сам называет стадию · закрыт ли гейт · следующий конкретный шаг+чем; для лекции сверяется прогоном `sostoyanie.py`); **B конец-сессии** (проактивно на первой закрытой гейт-границе, до молчаливого авто-компакта → `PLAN`/`SESSIYA`/хэндофф); **C конец-арки** (при достижении цели `PLAN` предлагает закрыть по §7 И тем же ходом открыть следующую `bootstrap_arka`, вписав концепцию/задачи/хэндофф уже в НОВУЮ арку). **Инвариант:** пока работа идёт — арка не остаётся без преемника (закрыть+открыть = один ход) → эффект памяти проекта.
2. **Шаблон-арка `../../zhurnal/_TEMPLATE-arka/`** (`NAVIGATOR`+`PLAN`+`SESSIYA` по формам ARKA §9/§4 + `TZ` по форме `../spravka/PROFILI-CIFERBLATY.md §TZ`) — стандартный скелет, копируемый на КАЖДУЮ новую арку (и «первая арка-пример»). Авто-плейсхолдеры `{{ИМЯ}}`/`{{ДАТА}}`/`{{КОНЦЕПЦИЯ}}` подставляет скрипт; ручные `<...>` (границы/go-no-go) дозаполняет Cowork с владельцем на Ф0.
3. **Скрипт `../../../_generator/tools/bootstrap_arka.py`** (stdlib, идемпотентен) — КОПИРУЕТ `_TEMPLATE-arka/` в `zhurnal/<дата_тема>/` с подстановкой. Операционализация правила «перекопировать начальное состояние с новыми задачами». **НЕ дублирует `bootstrap_lekcia.py`** (тот генерит дерево ЛЕКЦИИ из строк — папка контента; этот копирует шаблон АРКИ — процесс-память). Роутер `../../COWORK.md §4-6` указывает на `ARKA §10`.

**Почему:** обёртка (Р21) ЗНАЛА состояние, но НАПРАВЛЯЛА через Cowork вручную (аудит арки, `SESSIYA` 2026-07-10); §10 делает направление процедурным, а жизненный цикл арки — скриптуемым, замыкая цель «запустил → ведёт само». **Отвергнуто:** отдельный скрипт-стейт-машина цикла (дисциплина живёт в доках-как-скрипте `ARKA §10`, не в коде — код только копирует шаблон); откладывать в отдельную арку после #14 (владелец решил доделать ⑤ тем же вечером). **Развилки** (порог «контекст переполнен» B; точный признак «конец арки» C) — рабочие дефолты в §10, подтвердит первый живой цикл.

## Р23. Опорные точки (Задача/Утверждение/Теорема) — выделенный формат
Прецедент — Фибоначчи #14. РАНЬШЕ в лекциях опорные точки (условие задачи, утверждение, теорема, определение) не выделялись — научпоп-нить текла непрерывно, без явных якорей. Владелец: их нужно выделять единым узнаваемым форматом. **Канон (редакция «цветное слово», #14 v3):** «Задача N» / «Утверждение» / «Теорема» / «Определение» — единый визуальный ярлык = **цветное СЛОВО-надзаголовок** (Forum caps): «Задача» — `--brick` + крупный номер по листку; «Определение» — `--cgreen`, без номера; «Утверждение» и «Теорема» — общий тон `--steel`, между собой НЕ различаются (решение владельца). Слово у ЛЕВОГО поля; формулировка (Noto) — заметно ниже, на всю ширину слайда, БЕЗ точки в конце; из первоисточника ДОСЛОВНО; картинка/доказательство ПОЯВЛЯЕТСЯ ПОСЛЕ формулировки (порядок всегда ЧТО→как), не одновременно. Дом «как»: архетип в `../../konvejer/06-tekst/DOK.md` (текст) + визуальный контракт в `../../konvejer/07-verstka/DOK.md`; палитра — `src/tokens.css`. **Почему:** якоря дают лекции скелет, по которому школьник ориентируется; без них дек читается сплошным потоком. **Отвергнуто:** буквица / крупная Forum-цифра-номер на поле (ранняя #14-реализация — владелец развернул в «цветное слово»); центрирование как обязательное («не обязательно по центру»).

## Р24. Один доминирующий переиспользуемый визуальный архетип на лекцию + вариативность вёрстки
Прецедент — Данделен (три конических сечения = одна история трижды, канвасы/сцены по одной логике), Буффон (похожие канвасы по шагам), Фибоначчи #14 (биекция-полоска `fib-strip`, размноженная по моделям). **Наблюдение владельца:** в лекции ОБЫЧНО есть ОДИН доминирующий тип визуализации/интерактива (вероятностный / 3D / биекция / …), размножаемый с вариациями много раз; редко — набор очень разных. **Канон:** распознать доминирующий тип РАНО (раскадровка/иллюстрации), собрать УНИВЕРСАЛЬНЫЙ переиспользуемый формат (движок-примитивы + конфиг на слайд), переиспользовать по всей лекции — не bespoke на каждый слайд. Расширяет «4 ветки» `../../konvejer/09-illustracii/DOK.md` (обычно доминирует одна ветка — её и универсализируем). **Обратная сторона:** единый ДВИЖОК ≠ единая ВЁРСТКА. Прецедент #14: все слайды вышли одной раскладкой (текст сверху / илл. снизу) — монотонно. Канон: варьировать раскладку (архетип-таблица `../../konvejer/07-verstka/DOK.md`: горизонтальная/вертикальная полоса, слева/справа, без полос) при едином визуальном языке. **Почему:** один движок = дешёвые правки и узнаваемость; разная вёрстка = ритм, не скука. **Отвергнуто:** bespoke-визуализация на каждый слайд (дорого, теряется единство); и обратное — одинаковая раскладка всех слайдов (монотонно).

## Р25. Курс «Наглядная геометрия» 6 кл сужен до 4-лекционного мини-курса «Характеристика Эйлера»
Комбинаторная геометрия без углов: граф+χ → раскраски карт → Пик → многогранники. Убраны оптика (эллипсы сложны для 6 кл), оригами (отдельное мероприятие), закон квадрата-куба (масштаб/измерение — чужд нити). **Почему:** 4-лекционный мини-курс должен быть максимально консистентным — одна нить наращивается, не набор тем. Широкая ресёрч-база (8 тем) сохранена как источник, курс курируется сверху. Операциональный дом — профиль «мини-курс вокруг одной идеи» (`../spravka/PROFILI-CIFERBLATY.md`) + стандарт лекции (`../kak-delat/PROGRAMMA-KURSA.md`); инстанс — `../../../nagliadnaya-geometriya/mini-kurs-euler/STANDARD-lekcii.md`.

## Р26. Направление Пик↔Эйлер — порядок графы→Пик→многогранники верен
Стандартно Пик выводится ИЗ Эйлера (планарного); в решётке они взаимовыводимы, но общий Эйлер (любой многогранник) шире. ⇒ порядок графы→Пик→многогранники верен, переигрывать не надо. **Проверено:** Эппштейн «Proof 10», PBS Infinite Series.

## Р27. Ресёрч задач — третий ресёрч-поток фабрики (единица = ЗАДАЧА); семя — graph-course
У фабрики теперь ТРИ ресёрч-потока с разными единицами: матбаза (факт/доказательство, шаг 3) · научпоп (сюжет, шаг 2 котёл popsci) · **ресёрч задач** (ЗАДАЧА). Единица — карточка `источник · возраст/уровень · идея решения · чем «бриллиант»`. Две оси источников: задачные корпуса (по темам) × методические тексты (по идее решения). Ключевой приём — **задачи прячутся под чужими ярлыками** (раскраска в «Инварианте», переправа = скрытый граф; problems.ru: 390 «Графов» + 500+ скрытых); метод про то, как их выследить, через кросс-традиционную триангуляцию + статусы источников и гейт доступности к возрасту. Выход — картотека/листок нашего формата (Ф7), кормит матбазу (доказательство задачи) и научпоп (её история). **Процесс завершается ЗАПУСКОМ, не каталогом:** скачать корпуса в `istochniki` курса → `MANIFEST` → вычитать в `VYCHITANO` → размечать в `kartoteka`. Доки: `../kak-delat/RESERCH-ZADACH.md` (метод) + `../spravka/BAZY-ZADACH.md` (карта). **Развилки владельца (2026-07-11):** `BAZY-ZADACH` = общая КАРТА для поиска, не макс-список; конкретика (списки, дайджесты, файлы) — в папках курсов; скачивание — per-course (общий физический фонд НЕ заводим). graph-course остаётся спящим семенем-заготовкой. **Почему:** листок уже канонизирован как выход (Ф7 · Задачи, `../spravka/PROFILI-CIFERBLATY.md`), но дисциплинированного процесса-наполнителя не было; семя graph-course (source-first, 30 источников, 7+ традиций, 15+ кругов) даёт готовую кросс-традиционную карту и метод. **Отвергнуто:** общий физический фонд источников (копии — под курс, базы всё равно кросс-курсовые — общей делаем КАРТУ); перенос геосписка из `PRAJOR-ART` (перенос/удаление — не в правах песочницы; взаимные ссылки дешевле и обратимы); максимально полный каталог (карта для поиска, не свалка). Опирается на Р23 (опорные точки «Задача N», формулировка дословно), Р25 (широкая база сверху), Р18 (лекция=курс). **Уточнено (2026-07-11, после dry-run):** метод оформлен СТРОГИМ пронумерованным алгоритмом (`../kak-delat/RESERCH-ZADACH.md §2`, шаги 0–8 с гейтами) — цель владельца «искать задачи максимально просто». Скачивание/вычитка (шаги 3–4 на живом корпусе) — применение к РЕАЛЬНОМУ курсу, НЕ часть постройки механизма; незачем гонять на курсе-примере. Прогон на семени graph-course переклассифицирован в разовый **dry-run** (корпус оставлен спящим по слову владельца), его ценность — закалка алгоритма тремя граблями: тир доступности (free/платно/print), гейт полноты PDF (бесплатные зеркала отдают демо-огрызки), сверка фактов вживую (счётчики источников протухают). **Северная звезда карты (владелец):** сохранить ОБЩИЙ слой источников (сайты с задачами, задачные базы, книжные серии) из игрушечного архива graph-course, чтобы он не пропал и по нему искали будущие реальные курсы при наполнении листками — не конкретные графовые находки, а переиспользуемую инфраструктуру поиска. Перед закрытием досверена полнота: добраны AoPS/AMC–AIME, Турнир городов, mathraining.be, elementy, Малый мехмат/ЦПМ, Ore/Trudeau/Болтянский. Арка `reserch-zadach` 2026-07-11.

## Р28. Курс «Информация и коды» — архитектура 4 занятий + принцип отбора входной лекции; Л1 финализирован
**Нить — «передать информацию»**; шифрование (секретность) ≠ кодирование (сколько информации / защита) → шифры = ДВЕРЬ (Л1), не весь курс. **4 занятия:** Л1 шифры / числовые ребусы (мягкий вход, декод-разнобой) · Л2 «сколько информации» (магические карточки Клепцына = ЦЕНТР Л2 + угадай-число «да/нет» + двоичная запись + счёт + оценки/деревья) · Л3 «защита от ошибок» (чётность → Хэмминг, мудрецы/колпаки) · Л4 кульминация («коды в жизни» + наглядное сжатие + гранд-мудрецы) — **Л4 пока НЕ определён, нужен ресёрч**. Формат — кружок (листок с большой подборкой, самостоятельно + устная сдача, мало теории). Карта — `../../../informacia-i-kody/kartoteka/00-KARTA-kursa.html`.
**Принцип отбора входной/разнобойной лекции (переиспользуемый):** (1) берём только задачи, которые ДАЛЬШЕ не всплывут (чистая секретность/декод); задачи-мосты к последующим темам (двоичное, чётность/ошибки, оценки) **БЕРЕЖЁМ** для лекций, где задач мало — не жечь скудный будущий материал во входной. (2) Задача = шифр как ТОНКАЯ ОБОЛОЧКА над математическим перебором/дедукцией (ребусы-столбики, коды переменной длины, дедукция), не над лингвистикой/частотами; частотный анализ и мутные механизмы — вон. (3) Печатный листок детям — минималистичный ч/б: шапка (класс·пара·дата) + только номер+условие; версия препода — отдельно, с решениями.
**Л1 финализирован:** 12 задач (problems.ru «Криптография»/«Ребусы» + Матпраздник + Козлова + авторский Цезарь), по нарастанию, шифры/ребусы вперемешку; `../../../informacia-i-kody/kartoteka/L1-listok-shifry.html` (препод) + `L1-print.html` (дети). **Почему:** курс держит нить информации, а не расплывается в криптографию; входная лекция не тратит редкие будущие задачи и учит математике, а не языку. Опирается на Р25 (мини-курс вокруг одной идеи), Р27 (ресёрч задач). Арка `2026-07-11_informacia-i-kody` (закрыта 2026-07-12). **Отвергнуто:** разнобой со взвешиваниями/колпаками/карточками в Л1 (это ядро Л2/Л3); частотно-лингвистические шифры как «математика»; крипто-олимпиада Верченко (`cryptolymp.ru` — архив мёртв).

## Р29. Курс «Теория категорий» — профиль ПОЛНЫЙ + рекурсивный запуск; три арки со швом «ресёрч+картотека | программа | тексты»
Первый **платный заказной** курс-проект фабрики и первый **ПОЛНЫЙ**-профиль после Каталана: 9 лекций от нуля до элементарного топоса для программистов 35+ **без математического образования** (ТЗ 🔒: CCC · LCCC · топосы · жанр «изобретаем вместе с залом»). Вход — `../../../teoriya-kategoriy/START-HERE.md`; что строим — `SPEKA.md`; намерение — `TZ-zakazchika.md`. Арка 1 — `../../zhurnal/2026-07-16_teorkat-landshaft/`.

**Циферблаты (решение владельца 2026-07-16).** Глубина — **ПОЛНЫЙ** (библиотека с дайджестами + картотека): «максимальный пул экспертизы» — прямое требование, своей экспертизы по теоркату у владельца нет. Строгость — **строгая матбаза ВНУТРИ, научпоп СНАРУЖИ**: критерий ТЗ «знаю, зачем нужно каждое определение» пересказом не берётся. Масштаб — **тяжёлый полюс, рекурсивный запуск (Р18)**: арка-концепт курса → фабрика заново на каждую лекцию. **Отвергнут распил** (один растущий документ на весь курс): на 9 лекциях он станет неподъёмным — циферблат «лекция↔курс» впервые выставлен на тяжёлый полюс на реальном проекте, и это же его первая проверка.

**Три арки, шов «ресёрч+картотека | программа | тексты».** Владелец предлагал шов «сбор | база знаний | программа+тексты» — переставлено, аргумент принят. **Почему:** (1) картотека НЕ отдельная стадия — карточка пишется тем же ходом, что и ресёрч (`STANDART-uzla` авто-хук «находка без карточки = потеряна»); отдельной аркой пришлось бы перечитывать прочитанное задним числом; (2) нарратив обязан быть отдельной сессией — `../spravka/FAZA-1-REDESIGN.md`: «ресёрч идёт БЕЗ навязанной истории; нарратив — отдельной сессией из собранного», это разные режимы работы. **Отвергнуто:** две арки (ресёрч+картотека+программа одной) — линия начнёт навязываться материалу, пока он ещё собирается.

**Отклонения проекта от эталонов — сознательные.** (а) **Роды карточек `мотивация` и `сцена` — новые**, `биекция` убран: мотивация здесь несущий слой (ТЗ: «сначала мотивация, потом примеры, потом результат» — жёсткое ограничение; главный актив в том, что у лектора она ещё НЕ интернализована), визуал — несущая конструкция, а не украшение. (б) **Формат полей карточки сразу «одно поле = одна строка»** — в `fibonacci` спека и живая карта разъехались, известный дефект не наследуем. (в) **Глоссарий — приоритет ВЫСОКИЙ**, в отличие от `catalan` («терминология не критична, адресат — владелец-математик»): здесь адресат зал из ста человек, калька слышна сразу. (г) **4 входа зрелого проекта не заводим** — только `START-HERE` + `SPEKA` (KONSTITUCIYA §14).

**Ворота проекта живы: владелец ещё не взялся.** Предварительный ответ заказчику — вечер 17 июля; старт **~26 июля**, не 23-е (оценка владельца, не договорено). Основание решения — **не разбор лекций (ТЗ §Последовательность п.1), а программа + конспект ещё не существующего курса**: прочитал → понял → захотел. Этим ТЗ п.1 переписан; `TZ-zakazchika.md` не правим — это документ намерения на дату, а не живой план.

**Долг на входе (не потерять):** предпосылка ТЗ «научпопа по теоркату не существует» **не проверена** — арка 1 обязана проверить, а не принять (KONSTITUCIYA §13; владелец подтвердил сомнение 2026-07-16) · `Lawvere` по-русски намеренно не записан (гадать запрещено) · порядок композиции `g∘f` vs `f;g` — решить один на весь курс.

**Уточнено тем же днём (2026-07-16), в два хода — второй отменяет главный вывод первого.**

**Ход 1 (18:45) — получены ранние источники, сделан НЕВЕРНЫЙ вывод.** Владелец прислал телеграм Куклева (через В. Рогова) и письмо Куклева, «полностью согласованное с оргами» → `../../../teoriya-kategoriy/ZAPROS-zakazchika.md`. Письмо противоречит ТЗ по аудитории («старшеклассников-первокурсников **математиков в основном**» + EPFL/MIT против «ядро — программисты 35+, не математики»). Агент построил лестницу `ZAPROS > TZ > SPEKA` и объявил ТЗ дефектным пересказом, а аудиторию — открытым вопросом к заказчику.

**Ход 2 (19:00) — владелец поправил; выведен КАНОН.** Прислана расшифровка звонка с Куклевым → `../../../teoriya-kategoriy/ZVONOK-2026-07-16.md`. **`TZ-zakazchika.md` — не пересказ письма, а ПРОТОКОЛ ЗВОНКА, который был ПОЗЖЕ письма.** Владелец сверил прочтение вживую: «моё представление в целом тут очень точно ложится на реальность».

> **⛔ Канон (переиспользуемый, не про этот курс).** **Лестница источников правды заказчика строится ПО ВРЕМЕНИ, а не по формальности.** Заказчик меняет позицию по ходу переговоров; прав **последний**, а не самый официальный. Документ со словами «полностью согласовано с теми, кто платит» звучит авторитетнее пересказа телефонного разговора — **и именно поэтому обманывает**. Правило: заметив расхождение внутреннего документа с внешним «официальным», **не чинить внутренний — сперва установить хронологию**. *(Прецедент: `teoriya-kategoriy` 2026-07-16, агент перевернул лестницу и завёл ложную тревогу на несущем решении проекта.)*
>
> **Второй урок, дешёвый и вредный:** имя `TZ-zakazchika.md` **выглядит первоисточником**, хотя внутри — протокол владельца. Имя обмануло агента на весь Ф0. Кандидат в канон: **документ-пересказ обязан именем отличаться от документа-цитаты.**

**Что устояло по существу (аудитория — ЗАКРЫТА, не открывать):** изначальный запрос Куклева целился в продвинутых, но на звонке пришли к другому — **ядро вводного курса — 35-летние айтишники**, которые всё забыли и хотят восстановить; продвинутые (аспиранты, MIT, EPFL) «наверное, не будут ходить на вводный курс — **и это нормально**». Концепция сдвинута в сторону **больше научпопа**: не «без доказательств» (доказательства должны быть), а максимум понятности и наглядности. Планка едина во всех источниках, включая звонок: **«на уровне матшкольников»**. Телеграм (ранний) и звонок (поздний) сходятся; письмо — выброс посередине: его согласовывали с оргами абстрактно, а звонок был про то, **кто реально придёт**.

**Что письмо всё же дало — сверх звонка (ради этого разбор и не выброшен):**
1. **Закрыт вопрос, который ТЗ звал самым важным из всех** — «зачем им топосы из соображений, связанных с их программированием»: «закончим тем, что потребуется для **унивалентных теорий типов**» + второй курс про ∞-группоиды и (∞,1)-категории (звонок подтверждает: «точно будет»). Догадка ТЗ про HoTT верна; **топос — ступень, не финал**. Заодно объясняется единственное «обязательно» письма — фундаментальный группоид в лекции 1: зерно ∞-группоида, мост во второй курс. **NB:** ТЗ писалось ПОСЛЕ письма и вопрос всё равно оставило — значит либо владельцу нужна бóльшая конкретика («какой именно кусок топосов нужен под их прувер»), либо связь не была замечена. Вопрос **сужен**, а не снят.
2. **Закрыт «кто те двое»** — Куклев (JetBrains, 2015–2022 JetBrains Research, лаба Arend, модальные теории типов) + **Саша Грызлов**.
3. **Якорь-кандидат №1 — Ловер–Шнауэль «Conceptual Mathematics»** (Куклев зовёт её «Concrete Mathematics» — описка, та книга Грэхема–Кнута–Паташника): ровно «категории с нуля не-математикам», прямое попадание в профиль.
4. **Формат-ориентир — школа «Современная математика»**: в ТЗ отсутствует вовсе, хотя это единственный конкретный якорь жанра. Звонок сдвинул аудиторию, но планку оставил — поднять в арке 1.
5. **Доводы в три развилки** (монады · фундаментальный группоид · темп LCCC 5 vs 8). **Решение владельца: все три решает арка 2 разом** — сцеплены между собой и с аудиторией, поодиночке не развязываются. `TZ-zakazchika.md` не правим (документ на дату); расхождения живут в `ZAPROS-zakazchika.md`, охват решает SPEKA.

## Р30. Курс «Кривая дракона» — второй блок той же группы; линия вместо разнобоя; разбор для владельца ПЕРЕД листками
Новый курс-проект `../../../krivaya-drakona/` (вход — `START-HERE.md`, арка `../../zhurnal/2026-07-16_krivaya-drakona/`): 4 занятия ≈6 ч для той же группы школьников после 6 класса, что прошла «Информацию и коды» (Р28). Профиль — **«мини-курс вокруг одной идеи» + циферблат «якорная литература»**: содержание целиком держат два якоря — статья Васильева–Гутенмахера «Кривые дракона» («Квант» 2020 №1, перепечатка из №2/1970) и листок собеседования в 8 класс школы 179; ресёрч-волн не гоняем.

**Тема выбрана ПОД ФОРМУ, а не наоборот.** Явный запрос владельца: предыдущий блок был разнобоем сюжетов, здесь нужна **одна линия**, где задачи цепляются друг за друга и наращиваются. Кривая дракона это даёт: один объект — полоска бумаги — разворачивается в длинную нить. **Throughline — «один объект, три языка»:** полоска (руками) ↔ геометрия (теоремы 1 и 2 об удвоении) ↔ слово из L/R (рекурсия, позиции, степени двойки); каждая задача = перевод с языка на язык, **сложность = длина цепочки переводов** — это же и градация листка.

**Ворота go/no-go — GO, и риск обратный обычному.** Прецедент: ту же статью с тем же листком давали на собеседовании в 8 класс школы 179 (3 часа, дети **после 7** класса, статья на руках) — группа постарше не прошла листок целиком. У нас 6 часов, но группа младше и слабее. Значит вопрос не «хватит ли материала», а **«что срезать»** → потолок глубины становится явным шагом плана, а не самотёком.

**Порядок работ: разбор ДЛЯ ВЛАДЕЛЬЦА → потолок → раскладка → листки.** Владелец тему знает плохо («сталкивался много раз, но до конца никогда не видел», не хватает визуала) и на вопрос о потолке ответил «решить после разбора». **Почему так:** листки, написанные раньше, чем автор курса увидел объект, придётся переписывать — а потолок без понимания темы назначается наугад. Это отличие от Р28, где владелец тему знал и ресёрч сразу шёл под листок.

**Границы:** фрактальная теория (размерность, комплексные числа, база i − 1 из рукописи Дэвиса–Кнута) — за скобки; **задача со с. 19 (теорема Кнута о четырёх ломаных) в листок как задача НЕ идёт** — только финальный факт с картинкой: статья сама зовёт её «трудной теоремой», проверка численно подтверждает утверждение, но доказательство вне кружка.

**Гейт полноты якоря сработал сразу:** в присланном скане статьи **нет с. 18** (страницы 13–17, 19) — потеряны упражнения 6–11, включая задачу 10, на которую ссылается текст со с. 19. Долг владельцу: с. 18 · есть ли часть 2 (файл звался `кривая-дракона-1.pdf`) · картинка «Мама, папа и сын» к задаче №7 листка · сроки блока.

## Р31. Механизм доставки уроков в фабрику перестроен: три хука → один дешёвый + одна отдельная сессия
Стресс-тест 16–17.07 (курс «Теория категорий») дал измеримый факт: **у фабрики было ТРИ хука «→ фабрика» (после отчёта · хэндофф · закрытие арки), и за трое суток они доставили ~1 урок из ~12 — написано 17 секций «→ фабрика», до канона дошла ОДНА** (`git log _studio/docs/`: один коммит за трое суток, и не про уроки).

**Диагноз — не «хуки недостаточно жёсткие».** Три хука доставили меньше, чем один: диффузия ответственности («поймает следующий») плюс дорогое действие (консолидирующая правка канона) в момент, когда занят другим. **Дорогое пожелание не исполняется никогда.** Ещё одно «теперь точно заносим» было бы тем же правилом-пожеланием в четвёртый раз.

**Решено — разнести по ЦЕНЕ** (`kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»` — единственный дом; `ARKA §4/§7`, `COWORK §5` только ссылаются):
- после каждого отчёта — **одна строка в `UROKI-FABRIKE.md` арки: факт + ЦЕНА**, ~минута; не формулировка закона и не правка канона;
- хук «конец сессии» — **удалён** как дублирующий (иллюзия второго шанса на том же дешёвом действии);
- при закрытии арки — **отдельная сессия с ПУСТЫМ контекстом**, вердикт на каждый урок, правка канона консолидируя.

**Почему вердикт выносит не автор уроков:** он лицо заинтересованное, красивая формулировка от него подозрительна by design. Из механизмов, реально ловивших аналитика за арку, надёжнее всего — вынос задачи в пустой контекст (поймал дважды подряд). **Отбраковка слабого урока — результат сессии, а не провал.**

**Гейты — грепом** (`^### ` vs `^ЦЕНА: ` vs `^ВЕРДИКТ: `): урок без ЦЕНЫ не идёт в канон (наблюдение, не закон); урок без ВЕРДИКТА держит арку открытой. Файл рождается вместе с аркой из `../../zhurnal/_TEMPLATE-arka/` — завести забыть нельзя. *Греп считает строки, не правду: что цена настоящая, а вердикт честен, не проверяет никто, кроме владельца — названо честно (клауза 3 гейта).*

**Отвергнуто:** писать `check_dolg.py` на все четыре дома долга. Скрипту нужна разметка, которой ещё никто не пишет ⇒ новое правило-пожелание с лишним слоем. Сперва разметка приживётся практикой — тогда скрипт (беклог).

**Дополнено тем же днём — два уровня ниже прозы (по вопросу владельца «можно ли жёстче?»).**

*Диагноз уточнён по git:* канон правили 13 коммитов, из них **11 — арки ПРО фабрику**, где правка канона была deliverable. Хук как хук сработал **дважды за всю историю**. «Раньше работало» — это июль 10–12, когда фабрика и была проектом; как только арка стала про КУРС, доставка упала с 19% коммитов до 2%. Причина структурная, а не ленивая: **контракт зоны запрещает исполнителю писать в `_studio/docs/`**, значит канон для курс-арки — чужой дом, как глоссарий.

1. **Исполнитель пишет уроки САМ** — секция `## УРОКИ ФАБРИКЕ` в его `kod_<тема>.md` (вшита `bootstrap_zahod.py`). Звено «отчёт → аналитик переносит» убрано: это и был хенд-офф-долг. *Ложная предпосылка по дороге: «зона исполнителя включает папку арки» — НЕТ, живые заходы дают зону вида `<курс>/файл.md, _studio/zhurnal/<арка>/kod_<тема>.md`, то есть только свой kod-файл. Проверено по живым файлам, взято из прозы канона ошибочно. Фикс от этого стал проще: kod-файл уже в зоне ⇒ ни расширения зоны, ни гонок между параллельными заходами, ни нового файла.*
2. **`check_uroki.py` в pre-commit** (`.githooks/`, версионирован; `git config core.hooksPath .githooks`). Красит ❌ на урок без `ЦЕНА:`. **Обход `--no-verify` оставлен намеренно:** §11 запрещает нарушать МОЛЧА, а не нарушать. *Чекер соврал на первом запуске — объявил браком все 11 уроков арки teorkat (цена там прозой, не строкой). Соблазн ослабить регексп до «есть слово цена» убил бы гейт; вместо этого — урок с `ВЕРДИКТ:` не пересматривается, его уже отсудили.*

**Хуки Claude Code — ПРОВЕРЕНЫ, результат отрицательный (17.07).** `SessionStart`/`Stop`/`PreCompact` — детерминированные shell-команды (`Stop` с exit 2 не даёт сессии завершиться); класс сильнее всего вышеперечисленного. Тест: `.claude/settings.json` с `PostToolUse` (matcher `*`) и `UserPromptSubmit`, пишущими в два абсолютных пути — хостовый и песочницы. **Ни одного срабатывания** при живом контроле (песочница в тот путь пишет ⇒ тест валиден).
**Вывод формулируется СЛАБО, по охвату:** доказано не «хуки в Cowork не работают», а «с `.claude/settings.json` в рабочей папке они не стреляют в ЖИВОЙ сессии». Открыты две гипотезы: не поддерживаются вовсе · нет live-reload, нужен рестарт. Различает их `SessionStart` на новой сессии — **до этого на хуки не закладываться.** *(Первая версия теста была негейтом: `2>/dev/null || true` глушил «не сработало» и «сработало, но не смогло записать» одинаково. Переписан — клауза 1 гейта.)*

**Что установлено ТВЁРДО и без теста:** `materials/CLAUDE.md` **автозагружается** — лежит в контексте Cowork-сессии, которая его не читала (прямое наблюдение, не документация). Это ЕДИНСТВЕННЫЙ проверенный рычаг, доезжающий до каждой сессии сам, — и потому единственное место, где правило не зависит от того, вспомнил ли аналитик прочитать доку. `_studio/COWORK.md` (дом дисциплины аналитика) автозагрузки НЕ имеет: читается по договорённости, то есть может не читаться. Держать CLAUDE.md < 200 строк — иначе контекст съедается и adherence падает.

---
*Решения. Живой статус — `../sostoyanie/OTKRYTYE-ZADACHI.md`; детали архитектуры — `../spravka/ARHITEKTURA-FABRIKI.md`.*
