# DOLG-ISKLYUCHENIA — что вынесено из счёта git-долга, с причиной и датой

> **КОГДА читать:** когда `dolg_repozitoriev.py` печатает «вынесено» и хочется понять,
> почему чужой долг не краснеет. · **ЧТО дописывать сюда:** ТОЛЬКО строку выноса по
> формату ниже. · **КУДА дальше:** сам счётчик — `_generator/tools/dolg_repozitoriev.py`,
> канон git — `disciplina/skills/disciplina-git/GIT-disciplina.md` (репозиторий `disciplina`; копия в `materials/_meta/disciplina/` снесена 2026-08-25).

## Зачем этот файл

Счётчик долга считает все репозитории рядом. Среди них есть завершённые и чужие
проекты, где некоммиченное лежит месяцами и лежать будет: `matema-fest` — живой сайт
фестиваля, куда не пишут; «спецмат» — отдельная база. Владелец назвал их прямо: «там
много чего не закоммичено, их надо как-то, может быть, вынести сейчас».

Без выноса счётчик красный ВСЕГДА, а всегда красный счётчик перестают читать — и он
молчит в тот единственный день, когда покраснел по делу (`Р31`: красное на
унаследованном отключат вместе со всем гейтом). Вынос — это не «долг исчез», а «долг
назван, отложен и виден отдельной строкой»: `dolg_repozitoriev.py` печатает вынесенные
репозитории ВСЕГДА, включая полностью зелёный прогон.

## Формат — жёсткий, по нему разбирает инструмент

```
- `<имя папки репозитория>` · причина: <почему отложено> · дата: <YYYY-MM-DD>
```

Причина и дата обязательны обе. Без причины вынос через месяц неотличим от забытого;
без даты — от вечного (`CLAUDE.md` правило 6: отметка «закрыто» без даты данных доверия
не заслуживает). Строка не по формату НЕ выносится: инструмент печатает её отдельным
предупреждением и судит репозиторий наравне со всеми — молчаливое «не разобрал строку»
означало бы тихо снятый гейт.

Вернуть репозиторий в счёт — удалить его строку. Проверить, что вынос действует:
`python3 _generator/tools/dolg_repozitoriev.py` (вынесенные печатаются под чертой
«вынесено из счёта»), а разово судить всех — `--vse`.

## Список

- `matema-fest` · причина: завершённый сайт фестиваля, правки не ведутся; накопленное вне git — архивное, разбирается отдельным заходом · дата: 2026-08-16
- `matemdigest-map` · причина: владелец 2026-08-16 решил закрыть долг полностью (не «чужой проект» — прежняя причина устарела и снята); заход `chistka-hvostov` (Ч2+Ч5, 2026-08-16) починил баг `git_zona.py commit` (`add` на уже застейдженном пути) и зафиксировал коммитом `2710ecd` 23 давно удалённых с диска мусорных пути (`.DS_Store`/`.fuse_hidden*`/`materials.db-shm`/`-wal`) — эта часть причины СНЯТА, прежний вывод «блокировано известным багом» больше не в силе. Остаток НЕ закрывается по трём узким причинам, ни одна не по силам git-контуру: ветка `main` — несвязанная история старой витрины исходного `digest` до объединения репозиториев (`merge-base` не находит общего предка), сливать нечего и решать, что с ней, — владельцу; 14 путей вне git на 2026-08-16 — содержательная работа параллельного захода (`docs/`, `vypuski/`), трогать запрещено правилом зоны; коммит `2710ecd` довывезен тем же ходом после временной сетевой недоступности — колонка «не вывезено» снята · дата: 2026-08-16
