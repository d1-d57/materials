# GIT-disciplina.md → ПЕРЕЕХАЛ

> 🔴 **Единственный дом git теперь `_meta/disciplina/GIT-disciplina.md`** (от корня репозитория). Здесь остался только указатель.

🔴 **Ссылка вида `GIT-disciplina.md §<раздел>` теперь ведёт СЮДА, а раздела здесь нет.** Все разделы переехали целиком и под теми же номерами в `../../../_meta/disciplina/GIT-disciplina.md`:

| раздел | о чём |
|---|---|
| `§0` · `§0б` · `§0в` | закон, из которого следует всё остальное · инцидент → след в `INCIDENTY.md` · кривой вход и `purge` |
| `§1` | **владельцу** — одна тропа на частый случай |
| `§2` | **аналитику (Cowork)** — план файлом, не блок в чат |
| `§3` | **исполнителю (Claude Code)** — коммитишь САМ, последним ходом, по явным путям |
| `§4` · `§4а` · `§4б` · `§4в` | параллельные заходы через worktree · что живёт вне git · слияние веток · публикация сайта |
| `§5` | инструмент и его защита от регрессии (сторож соседних веток, названные долги) |
| `§6` · `§7` · `§8` | почему нет разреза монорепо · развенчанный фантом «в репозитории живёт авто-коммит» · что не проверено боем |

Ищи раздел там — номера не менялись.

**Как мы вообще работаем — идёшь в `_meta/`.** Заход, коммит, арка, дневник — всё оттуда и одним языком.

| Что нужно | Куда |
|---|---|
| любая работа с git: коммит · «не коммитится» · worktree · слияние · публикация | `../../../_meta/disciplina/GIT-disciplina.md` |
| как писать заход исполнителю и как принимать отчёт | `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md` |
| канон арок | `../../../_meta/disciplina/ARKA.md` |
| где что живёт и что краснеет, если разъехалось | `../../../_meta/disciplina/ISTOCHNIK-PRAVDY.md` |
| что этот дом вообще есть и чего он НЕ делает | `../../../_meta/README.md` |

⚠ **Сам инструмент никуда не ехал.** `git_zona.py` как лежал, так и лежит в `../../../_generator/tools/git_zona.py`; переехали ПРАВИЛА, а не код. Автолог срывов коммита (`../../zhurnal/_INFRA-git/INCIDENTY.md`) тоже остался при `_studio/` — индекс git у репозитория один, и лог у него один (`_generator/tools/korni.py`, константа `ИНФРА`).

**Почему файл не удалён, а стал указателем.** На старый адрес ссылались 110 живых мест по всему репозиторию (дата данных 2026-08-06; пересчёт — `grep -rlF 'GIT-disciplina.md' --include='*.md' --include='*.py' --include='*.sh' . | grep -v '^_meta/'`), и большинство лежит вне зоны того захода, который делал перенос (`../../zhurnal/2026-08-05_faza-lenty/kod_vynos-meta.md`). Мёртвая ссылка обнаружилась бы не у автора переноса, а у следующего, кому канон понадобится. Указатель снимается вместе с последней ссылкой на него, не раньше.

*История файла целиком уехала вместе с содержанием: `git log --follow _meta/disciplina/GIT-disciplina.md`.*
