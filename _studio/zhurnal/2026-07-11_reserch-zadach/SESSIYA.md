# СЕССИЯ — дневник арки «2026-07-11_reserch-zadach»

> ДЕТАЛЬ + РЕШЕНИЯ + ИСТОРИЯ (ARKA §4). Дистиллят, не свалка; один док; у решения — «почему». С первого хода. Новая запись — снизу, с датой.

## 2026-07-11 — старт
Арка заведена скаффолдом `bootstrap_arka.py` из `_TEMPLATE-arka/`. Концепция и границы — в `NAVIGATOR.md`, ТЗ — в `TZ.md`. Дальше — по `PLAN.md` («⭐ СЕЙЧАС»).

## 2026-07-11 — Ф0: концепт, разведка канона, go
**Ориентировка.** Прочитал канон (COWORK, KARTA, ARKA §2/§10, ARHITEKTURA-FABRIKI, PROGRAMMA-KURSA, RESHENIYA, PRAJOR-ART, PROFILI §Ф7) + семя прицельно (concept_note, research_journal_v1 §3, v2 карта, project_text_v4 §5–6).

**Ключевое открытие при разведке.** Листок уже канонизирован как ВЫХОД — «Ф7 · Задачи» (PROFILI-CIFERBLATY) + «Стандарт лекции · листок · метод ресёрча» (PROGRAMMA-KURSA). Чего нет — дисциплинированного ПРОЦЕССА, который его наполняет. Арка заводит именно процесс, третьим ресёрч-потоком рядом с матбазой (шаг 3) и научпопом (шаг 2).

**Концепт (принят).** Единица = ЗАДАЧА (`источник · возраст/уровень · идея решения · бриллиант`). Две оси источников — задачные корпуса (по темам) × методические тексты (по идее решения). Приём «скрытых ярлыков»: задача живёт под чужим разделом (раскраска → «Инвариант», переправа → скрытый граф; problems.ru: 390 «Графов» + 500+ скрытых). Кросс-традиционная триангуляция + статусы. Гейт доступности. Завершение — ЗАПУСКОМ, не каталогом.

**Развилки (владелец, 2026-07-11):**
- *Фонд баз.* Скачивание — per-course (istochniki у каждого курса свой, как сейчас). Общий слой — не физический фонд, а **карта источников для поиска** (`BAZY-ZADACH`), переиспользуемая, НЕ макс-полный список. **Почему:** базы кросс-курсовые, но копии — под курс; ориентироваться удобно по одной карте.
- *Карта vs список в PRAJOR-ART.* Конкретику держим в папках курсов; общая карта — для ориентации. Геосписок в PRAJOR-ART (из geometria-6) НЕ переношу (перенос/удаление — не в правах песочницы, только владелец/Code) → взаимные ссылки. **Почему:** KARTA §2 «один дом», но перенос требует слова; ссылки дешевле и обратимы.
- *Первая волна запуска.* Кладу в `graph-course/istochniki` (семя как носитель корпуса), сам курс спит. Тяжёлую вычитку — Claude Code.

**go получен** («давай, конечно»). Арка заведена, доки дозаполнены. Дальше — писать `RESERCH-ZADACH.md`.

## 2026-07-11 — dry-run, разворот на строгий алгоритм
**Прогон запуска (Claude Code, Sonnet).** Скачал 7 задачников в `graph-course/istochniki/` + MANIFEST/VYCHITANO. Как ЦЕЛЬ — впустую (graph-course не настоящий курс, провизионить его незачем). **Владелец:** сконцентрироваться ТОЛЬКО на механизме — «искать задачи максимально просто, очень чёткий алгоритм».
**Развороты (решения):**
- Метод переписан из эссе в **строгий пронумерованный алгоритм** (`RESERCH-ZADACH §2`, шаги 0–8, вход/выход/гейт + пайплайн одним взглядом). Форму выбрал владелец.
- Граница «механизм vs прогон» проведена явно (`§5`): скачивание/вычитка — применение к РЕАЛЬНОМУ курсу, не постройка механизма. Эта арка алгоритм не гоняет. **Почему:** гонять на курсе-примере — busywork.
- Прогон graph-course = разовый **dry-run**; корпус (7 PDF) оставлен спящим (слово владельца).
**Dry-run как стресс-тест — 3 грабли, вшиты в механизм** (`RESERCH-ZADACH §3` + `BAZY-ZADACH`): тир доступности (полные ШМК — платно; free-зеркала дают демо-огрызки, ловится постраничной сверкой); счётчики протухают (problems.ru 161→150/199→48, разделы стали дочерними); мосты подтверждены ДОСЛОВНО в первоисточнике (Медников «чётность… графы, игры»; Шаповалов «Засада на переправе»).
**Моя ошибка (→ фабрика).** Нацелил «запуск» на мёртвый пример по букве брифа, не флагнув громко, что на курсе-примере гонять незачем. Урок: флаг «стоп до цели» проверять не только на объём захода, но и на ОСМЫСЛЕННОСТЬ его цели. (Уже отражено границей `RESERCH-ZADACH §5`; при закрытии решить — тянет ли на строку в RUKOVODSTVO.)
**Осталось:** верификация → предложить закрытие арки.
