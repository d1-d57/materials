# Канал исполнителя — skachivanie-istochnikov-L1 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — заход механический (скачать пару статичных PDF + вписать строку), весь reasoning уже здесь.

КОНТЕКСТ. Мини-курс для школьников «Информация и коды», идёт сбор Л1-разнобой (шифры + да/нет-затравки + финал магические-карточки). **Ядро Л1 УЖЕ собрано** и лежит в `informacia-i-kody/istochniki/pdf/` (25 этюдов о шифрах, листок «Криптография», Клепцын №7, ФКН §12.1, Квант-Виленкин). ЦЕЛЬ этого захода — **добрать вариативность: скачать сборники Математического праздника (6–7 кл) под да/нет- и декод-гемы.** Это ДОБОР, не критический путь.
Приёмка — по ОТЧЁТУ, без построчной сверки. **Стоп до цели:** получишь скачанные сборники + строки в MANIFEST — но НЕ отбор задач, НЕ листок, НЕ дайджесты (это Cowork+владелец после тебя).

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **ВЕТКА:** НЕ переключай ветку (в репо параллельный WIP; ветка курса ещё не решена владельцем). Работай на текущей. Коммит ты НЕ делаешь (см. КОММИТ) — поэтому `git checkout` не нужен.
- **ЗОНА (можно менять):** `informacia-i-kody/istochniki/pdf/` (класть скачанное) + `informacia-i-kody/istochniki/MANIFEST.md` (дописать строки) + этот файл-заход (план/отчёт). Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно». НЕ трогать `_studio/**`, другие курсы, `.git/**`.
- **КОММИТ:** сам НЕ коммить (курс `informacia-i-kody/` ещё НИ РАЗУ не в git — первый коммит и ветку курса решает владелец холистически; рядом чужой WIP). В `## ОТЧЁТ` выдай владельцу готовый копипаст-блок ОДНОЙ строкой, явными путями, НИКОГДА `-A`: `cd <АБС_КОРЕНЬ_РЕПО> && git add informacia-i-kody/istochniki/pdf informacia-i-kody/istochniki/MANIFEST.md && git commit -m "источники Л1: Матпраздник + MANIFEST"`. Файл-заход не коммить.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Рабочая папка `informacia-i-kody/istochniki/` (обычно `~/Documents/GitHub/materials/informacia-i-kody/istochniki/`). Точка отката НЕ нужна — заход только ДОБАВЛЯЕТ файлы.
- Прочитать ТОЛЬКО: этот файл + `informacia-i-kody/istochniki/MANIFEST.md` (формат строки переиспользуй). Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов) + «качать ГРАМОТНО, не биться об стену»
- **Проверь ОДИН до массы.** Скачай первый файл, открой, убедись что формат тот (цельный сборник задач+решений, не бланк/одна страница) — и только потом добирай остальные тем же паттерном.
- **Затык = СТОП и в отчёт, не брутфорс.** 404 / редирект / капча / пустая оболочка / платно → НЕ подбирай зеркала часами, НЕ скреби вслепую. Помечай «не взялось» и иди дальше. Цена ошибки тут — впустую потраченные силы.
- Гейт полноты на каждый файл (нумерация не обрывается). Дубликаты не плоди — `ls` до скачивания. Хирургия: 6 лежащих файлов не трогай.
- Субагенты не нужны (файлов мало) — последовательно.

## 2. ЗАДАЧА — скачать сборники Матпраздника в `pdf/`
Статический сайт МЦНМО (проверено, не SPA). Сборник задач+решений за год: **`https://olympiads.mccme.ru/matprazdnik/image/<NN>/book.pdf`**, где `<NN>` — две цифры года.
- Взять годы: **26, 25, 24, 23** (2026/2025/2024/2023). Сначала ОДИН — `image/26/book.pdf`; открой, подтверди что это цельный сборник обоих классов (6 и 7) с условиями и решениями. Формат тот → добери 25, 24, 23. Год отдаёт 404 → пропусти, не ищи обходы.
- Имя файла: `matprazdnik-<год>-book.pdf` (полный год, напр. `matprazdnik-2026-book.pdf`).
- MANIFEST: на каждый скачанный — строка в таблицу `pdf/` (колонки `Файл | Что это | Темы / лекции`; в «Темы» — `Л1 да/нет · декод-гемы`) + строка в блок `## Полнота и источники` (`<файл> — тир A — ПОЛНЫЙ N с. — <URL>`). Дайджесты (VYCHITANO) НЕ пиши.
- **НЕ делать:** крипто-олимпиада Верченко (`cryptolymp.ru`) — **МЁРТВАЯ**, архив не открывается даже из РФ, даже не пробуй. Квантик — владелец загрузит нужные выпуски сам, не качай. Отбор задач/листок — не твоё.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):** ≥2 сборника Матпраздника лежат в `pdf/`, каждый — цельный (оба класса, задачи+решения, не бланк); MANIFEST дополнен строкой + «Полнота» на каждый; дубликатов с 6 лежащими нет; git-блок владельцу — в отчёте. Если на ПЕРВОМ же (`image/26/book.pdf`) паттерн не подтвердился (404 или не сборник) — СТОП, отчёт «паттерн не тот, нужен владелец», не брутфорсь остальные годы.

## 3. ВЕРИФИКАТОР
Не нужен: заход только добавляет файлы (не лоссовый). Достаточно гейта полноты §1 + `ls`-сверки.

## 4. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что скачал (файл + стр + полнота N/N) и ЗАЧЕМ / как проверял полноту / что не взялось (что пробовал, где остановился) / что НЕ трогал / готовый git-блок владельцу / **время прогона + токены**.

## ПЛАН — (заполняет исполнитель)
1. Скачать `image/26/book.pdf` → `pdf/matprazdnik-2026-book.pdf`, открыть/проверить (оба класса, задачи+решения, не бланк).
2. Паттерн подтверждён → добрать 25, 24, 23 тем же URL-шаблоном; 404 — пропустить.
3. Дописать MANIFEST.md: строку в таблицу `pdf/` + строку в «Полнота и источники» на каждый скачанный файл.
4. Отчёт: что скачано/пропущено, git-блок владельцу, время+токены.

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)

**Что скачал.** Все 4 запрошенных года — паттерн `https://olympiads.mccme.ru/matprazdnik/image/<NN>/book.pdf` подтвердился на первом же файле (26) и держался на всех:
- `matprazdnik-2026-book.pdf` — 24 с. (XXXVII, Матпраздник)
- `matprazdnik-2025-book.pdf` — 24 с. (XXXVI)
- `matprazdnik-2024-book.pdf` — 16 с. (LXXXVII ММО, 18.02.2024)
- `matprazdnik-2023-book.pdf` — 24 с. (LXXXVI ММО, 19.02.2023)

**Зачем.** Добор вариативности под да/нет- и декод-гемы Л1 (задачи Матпраздника 6–7 кл, условия+решения).

**Как проверял полноту.** Для каждого файла: `pdfinfo` (не бланк/не пустышка, корректный размер и дата создания), `pdftotext` — наличие секций «6 класс» / «7 класс» и слова «Решение» (условия и решения на месте, оба класса присутствуют). 2024 и 2023 имели одинаковый PDF-метатег Title («XXXIV...») — заподозрил дубль, сверил по первой странице (`pdftotext -f1 -l1`) и MD5: разные даты олимпиады (18.02.2024 vs 19.02.2023), разные хэши — это не дубль, просто на сайте-источнике метаданные не обновлены. `ls` до и после — новых 4 файла, дублей с 6 лежащими нет.

**Что не взялось.** Ничего — все 4 года отдали 200 и прошли проверку с первого раза, обходов/ретраев не потребовалось.

**Что НЕ трогал.** `_studio/**`, другие курсы, `.git/**`, Квантик, крипто-олимпиаду Верченко (мёртвая, по инструкции не пробовал), отбор задач и листок — вне зоны захода.

**Git-блок владельцу (копипаст, коммит НЕ делал):**
```
cd /Users/ivanyakovlev/Documents/GitHub/materials && git add informacia-i-kody/istochniki/pdf informacia-i-kody/istochniki/MANIFEST.md && git commit -m "источники Л1: Матпраздник + MANIFEST"
```

**Время прогона:** ~7 минут. **Токены:** заход был чисто механический (curl + pdfinfo/pdftotext + правка MANIFEST), контекст не расширялся сверх файла-захода и MANIFEST.md.
