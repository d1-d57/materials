# Канал исполнителя — pereschet-i-schetchik (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — продукт захода суждение, а не механика: где проходит новая граница и что считать «накрытым рычагом», расписать заранее нельзя, это решается на живом материале.

КОНТЕКСТ. У владельца собран корпус инцидентов — **2264 записи Σ кратности**, разложенные по пяти домам скриптом `karta_domov.py` (арка `2026-08-05_faza-lenty`). Дома живут в репозитории-плагине `disciplina/doma/`, сам корпус остался в `materials`.

**Два дефекта этой карты, оба найдены аналитиком 07.08.**

1. 🔴 **Карта устарела ровно на одно решение владельца.** Она нарисована 06.08 по фазам конвейера: `PHASE_TO_DOM` кладёт фазу `06-tekst` целиком в дом ЛЕНТА. 07.08 владелец принял **решение 2**: фаза «текст» **раздваивается по артефакту** — «шаг 6 · лента» остаётся в ресёрч-цикле, «шаг 6 · предвёрсточный документ» (короткие тексты слайдов) уезжает в СЛАЙДЫ. Классификацию никто не пересчитывал. Цена фазы — **273 записи**, это шестая часть всего корпуса.

2. 🔴 **«Разобрано» не измеряется ничем.** Дома знают, сколько у них записей, и не знают, какая доля накрыта рычагом. Единственный дом с настоящим разбором — ИЛЛЮСТРАЦИИ (`materials/_illustracii/POKRYTIE.md`, 31 класс «инцидент → рычаг → цена», включая честное «ничем не чинится и не может»). Остальные четыре несут только счёт и адрес.

Владелец сформулировал цель дословно: *«чтобы мы смогли сказать: все инциденты, которые были собраны, — для них теперь работают рычаги»*. Сегодня эту фразу нельзя ни подтвердить, ни опровергнуть.

ЦЕЛЬ: пересчитать карту по новой границе и **завести метрику накрытия**, чтобы «дом разобран» стало проверяемым утверждением.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ.** Ты **не разбираешь инциденты** — ни одного дома. Ты даёшь инструмент, которым их будут разбирать, и первый замер, который он печатает. Разбор — следующие заходы, по дому на заход.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/wt-pereschet`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/pereschet-i-schetchik`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add wt-pereschet --branch zahod/pereschet-i-schetchik
  cd ../materials-wt/wt-pereschet
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py` и новый файл `_studio/zhurnal/2026-08-05_faza-lenty/pokrytie_rychagami.py`. Всё вне — **READ-ONLY**. Особо: **корпус инцидентов не трогать** (`../2026-07-30_dovodka-fabriki/KARTOTEKA-problem.md` и соседи) — классификация фаза-по-корпусу куплена и не пересматривается; ты меняешь только перевод фаза → дом. Репозиторий `disciplina` — READ-ONLY целиком: дома правит следующий заход, по замеру этого.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/pereschet-i-schetchik`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: зона не чиста в HEAD — закоммить как есть; чиста — не фабрикуй.
- **Прочитать ТОЛЬКО это:**
  1. `karta_domov.py` целиком — докстринг (что скрипт про себя обещает и чего не делает), `PHASE_TO_DOM`, позаписный разбор `вне-фаз` и `04.5-intervyu`.
  2. `_studio/zhurnal/2026-07-30_dovodka-fabriki/srez_po_faze.py` — как берётся Σ кратности по фазе.
  3. `PUT-K-GOTOVOMU.md` этой арки, §3 — что известно про дома и чего не известно.
  4. **Образец настоящего разбора:** `materials/_illustracii/POKRYTIE.md`. Это единственный дом, доведённый до конца; по нему ты поймёшь, что такое «рычаг» на практике и как выглядит честная запись «рычага нет и быть не может».
  5. `disciplina/doma/*/POKRYTIE.md` — четыре дома, только на чтение: их формат тебе нужен, чтобы замер лёг в него без переделки.
- ПЛАН — в `## ПЛАН` перед действиями.

## 2. ЗАДАЧА

### 2.1 Пересчитать границу фазы `06-tekst` — позаписно, а не коэффициентом

`PHASE_TO_DOM` кладёт `06-tekst` целиком в ЛЕНТУ. По решению 2 фаза раздвоена **по артефакту**:

| остаётся в РЕСЁРЧ-ЦИКЛЕ | уезжает в СЛАЙДЫ |
|---|---|
| текст для чтения: полнота формулировки, «сначала рассуждение, потом вывод», голос ленты, гейт «документ принят» | тексты **на кадре**: потолок знаков на слайд, разметка сцен `{@N}`, `**acc**`, архетипы, бюджет слов, предвёрсточный документ |

🔴 **Делить позаписно, глазами по каждой записи, а не долей.** Коэффициент «примерно половина» здесь запрещён: он даст правдоподобное число, которое невозможно ни проверить, ни оспорить, — а на этом числе будут строить порядок разбора трёх домов.

Записи, которые честно не делятся (правило про текст вообще, безразличное к носителю), — **в СПОРНЫЕ с причиной**, а не приписаны туда, где красивее. У `karta_domov.py` для спорных уже есть механизм, переиспользуй его.

**Проверить заодно, не сдвинулись ли другие границы.** Решения интервью 07.08 тронули ещё три места: фаза `04.5-intervyu` (решение 4 — контракт артефакта уехал в слайды), корпус текстов (решение 16 — из ленты в слайды), `math-russian-terminology` (решение 23 — втянут в ресёрч). Каждое проверить по `OTVETY-INTERVYU-2026-08-07.md` и сказать, меняет ли оно перевод. **Не меняет — так и напиши**: отрицательный вывод здесь такой же результат.

### 2.2 Завести `pokrytie_rychagami.py` — метрику, которой сегодня нет

Скрипт печатает по каждому дому три числа и остаток:

```
дом            записей   накрыто   не накрыто   покрытие
ИЛЛЮСТРАЦИИ        107       ???          ???        ???%
```

**Что считать «накрытым» — главное решение захода, и его надо принять на живом материале, а не назначить.** Опорная идея, которую ты вправе заменить: запись накрыта, если в доме есть строка «инцидент → рычаг», где рычаг — **названный** файл, гейт, инструмент или правило, а не пожелание. Образец формы — `_illustracii/POKRYTIE.md`, там же образец честного «рычага нет».

🔴 **Три вида «накрытия» различай явно, иначе метрика соврёт в приятную сторону:**
- **машинный рычаг** — гейт, линтер, хук; краснеет сам;
- **процедурный** — правило в каноне, которое кто-то обязан помнить; носителя нет;
- **не накрывается** — как КРВ-40 «непонятно, что нарисовано»: машинного гейта на «читается за три секунды» не существует. Это **законный** вид, и он должен считаться отдельно, а не тонуть в «не накрыто».

Правило владельца, которое здесь применимо дословно: правило, которому нечему покраснеть, — надежда, а не правило (`KONSTITUCIYA §11`). Значит **процедурное накрытие в проценте машинного не учитывается**; печатай их разными колонками.

### 2.3 Первый замер — по всем домам

Прогнать скрипт и предъявить таблицу. Ожидание: ИЛЛЮСТРАЦИИ дадут высокое покрытие (дом разобран), три остальных — низкое или нулевое. **Если ИЛЛЮСТРАЦИИ не дали высокого — метрика меряет не то**, чинится метрика, а не дом: это твой положительный контроль, ровно как ловушка 1 в фикстуре линтера.

Для МЕТА отдельно: в `materials/_generator/tools` лежат 24 инструмента, и почти каждый родился из мета-инцидента. **Сопоставлять их с записями — работа следующего захода**, не твоя; но метрика обязана уметь такое сопоставление принять, когда оно появится.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Шесть условий:

1. `python3 karta_domov.py` отрабатывает, `echo $?` = 0, и **Σ по всем домам плюс спорные равна корпусу** — ровно как сейчас. Разошлось — записи потеряны при переносе, это провал.
2. **Раздел фазы `06-tekst` предъявлен позаписно:** сколько ушло в СЛАЙДЫ, сколько осталось, сколько в СПОРНЫЕ, и `сумма = Σ фазы` (число снять командой из §4 `PUT-K-GOTOVOMU.md`, не брать из этого текста).
3. Три остальные границы (решения 4, 16, 23) проверены; по каждой вердикт «меняет / не меняет», пустых нет.
4. `pokrytie_rychagami.py` существует, отрабатывает на живых домах, печатает **машинное и процедурное покрытие разными колонками** плюс «не накрывается» отдельно.
5. **Живой прогон на реальном доме, не на фикстуре:** ИЛЛЮСТРАЦИИ дают высокое машинное покрытие. Не дают — метрика негодна, и это честный отчёт, а не повод подгонять.
6. `git_zona.py check --zone _studio/zhurnal/2026-08-05_faza-lenty/` → ✅.

**Охват — числом.** Домов пять, границ четыре. «Пересчитано» без чисел по каждой не принимается.

## 3. ВЕРИФИКАТОР

**Нужен: заход двигает классификацию, а это лоссовая операция** — запись, потерянная при переносе между домами, исчезает молча и обнаружится только когда её пойдут разбирать.

Свежий субагент, метод другой — **не перечитывание кода, а сверка сумм**: Σ старой карты против Σ новой по каждому дому, список записей, сменивших дом, и доказательство, что ни одна не потерялась и не удвоилась. Плюс выборка: взять 10 записей, ушедших в СЛАЙДЫ, и проверить глазами, что они действительно про кадр, а не про текст вообще.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Заход велит работать в worktree и читать файл, которого в worktree нет
`§0` называет `PUT-K-GOTOVOMU.md` обязательным чтением (пункт 3) и источником команды для критерия готовности (пункт 2 → `§4` того файла). Контракт зоны тем же ходом велит завести worktree и работать там. Файл существует только в рабочей копии основной папки и не закоммичен — `git log --all --diff-filter=A` по нему пуст, — поэтому в worktree его нет вовсе, и `find` по рабочей папке даёт пустоту. Это структурное, а не разовое: любой файл арки, заведённый аналитиком и ещё не закоммиченный, невидим исполнителю, которого послали в worktree.

**ЦЕНА:** круг разведки на «файла нет — заход врёт или я не там»: `find` по рабочей папке, `git log --diff-filter=A` по истории, потом `find` по основной папке. Дешевле повезло: файл нашёлся в основной папке и читался по абсолютному пути. Не повезло бы — исполнитель либо сдал бы пункт 2 критерия по числу из текста захода (то самое «взял из этого текста», что пункт прямо запрещает), либо встал.

### Положительный контроль критерия готовности опровергается тем самым домом, на котором стоит
Пункт 5 требует: «ИЛЛЮСТРАЦИИ дают высокое **машинное** покрытие; не дают — метрика негодна». Дом, назначенный контролем, в собственном `POKRYTIE.md §2` пишет обратное: три самых частых предмета недовольства — 35 инцидентов из 107 — «не ловятся ничем и в основном не могут ловиться машиной», а `§5` перечисляет, чего фабрика не сторожит по построению. Замер это подтвердил: машинное покрытие 26 %, разобранность 73 %. Контроль, сформулированный через машинное покрытие, обязан провалиться на здоровом доме.

**ЦЕНА:** здесь — ноль, потому что предпосылку удалось опровергнуть до работы и поправку вписать в `## ПЛАН`. Не опровергни — исполнитель получает пункт критерия, который нельзя выполнить честно, и единственный способ его закрыть: считать процедурное накрытие машинным, то есть построить ровно ту метрику, которая врёт в приятную сторону и которую этот же заход в `§2.2` запрещает двумя абзацами выше.

### Однострочная выжимка корпуса теряет половину реплики, и теряется именно та, что решает дом
Записи корпуса разложены по адресам, а содержание берётся из одной строки-выжимки в `KRATNOST-vladelca.md`. На `КРВ-05c` выжимка говорит «Не называть „задача B1“ — в лекции таких сущностей нет», а живая строка `PRAVKI.md:521` несёт вторую половину: «она у тебя без разбора, её тоже нужно разобрать» — то есть закон полноты формулировки, по которому запись уходит в другой дом. Разметка по выжимке дала СЛАЙДЫ, разметка по источнику даёт ЛЕНТУ.

**ЦЕНА:** одна запись из 273 переразмечена — но нашёл её верификатор на выборке 10 записей, то есть частота ошибки этим не измерена, а только показано, что она не нулевая. Для любой будущей работы по корпусу это значит: выжимка годится, чтобы ОТОБРАТЬ записи, и не годится, чтобы по ним СУДИТЬ, — судить надо по адресу.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Состояние на входе.** Рабочая папка заведена (`worktree add` → `✅`), ветка `zahod/pereschet-i-schetchik` (`git rev-parse` → она), зона в HEAD чиста — точку отката не фабрикую. Базовый прогон снят до правок: `karta_domov.py` → РЕСЁРЧ 429 · ЛЕНТА 474 · СЛАЙДЫ 454 · ИЛЛЮСТРАЦИИ 107 · МЕТА 775 · спорных 20 строк (Σ 25) · ИТОГО 2264 = корпус; `srez_po_faze.py` → `06-tekst` **273**.

### 0. Две поправки, которые называю ДО работы

🔴 **П1. Пункт 5 КРИТЕРИЯ ГОТОВНОСТИ опирается на ложную предпосылку, и её опровергает сам дом.** Пункт требует: «ИЛЛЮСТРАЦИИ дают **высокое машинное** покрытие; не дают — метрика негодна». Но `_illustracii/POKRYTIE.md §2` говорит обратное прямым текстом: *«три самых частых предмета недовольства (КРВ-16a, КРВ-40, КРВ-14b — 35 инцидентов из 107) не ловятся ничем и в основном не могут ловиться машиной»*, и §5 перечисляет, чего фабрика не сторожит по построению. Машинного покрытия у этого дома мало **по устройству предмета**, а не по недоделке. Положительный контроль, сформулированный так, обязан провалиться на здоровом доме — то есть он не контроль.

**Поправка, по которой работаю:** контроль ставится на **разобранность** (доля записей, у которых есть вердикт «инцидент → рычаг», включая законное «рычага нет и быть не может»), а машинное покрытие печатается рядом отдельной колонкой и **высоким не обязано быть**. Разобранность — то, что у ИЛЛЮСТРАЦИЙ действительно высокое, и ровно она отличает разобранный дом от неразобранного. Пункт 5 в этом виде проверяю и предъявляю оба числа.

🔴 **П2. `PUT-K-GOTOVOMU.md` в рабочей папке отсутствует.** Заход велит прочитать его §3 и снять число командой из §4. Файл существует **только в основной папке и не закоммичен** (`git log --diff-filter=A` по нему — пусто), поэтому в worktree его нет вовсе. Прочитал по абсолютному пути из основной папки; команду §4 (`srez_po_faze.py 06-tekst | awk …`) взял оттуда же. Это урок фабрике, не препятствие — цена названа в `## УРОКИ ФАБРИКЕ`.

### 1. Единица деления: не строка-группа, а ЗАПИСЬ-ВХОЖДЕНИЕ

Механизм спорных в `karta_domov.py` (`KLASS_VNEFAZ`, `KLASS_INTERVYU`) режет **по строкам-группам**: вся кратность строки уезжает в один дом. Для `06-tekst` этого мало: фаза — это 54 строки-группы, и крупные из них (`КРВ-08a` 25, `КРВ-07a` 24, `КРВ-11a` 17) заведомо смешанные. Отправить такую строку целиком — то же, что коэффициент; отправить в СПОРНЫЕ — похоронить сотню записей.

Проверил, что более мелкая единица существует и считается точно: колонка адресов картотеки раскладывает фазу на **273 атома** (адрес = одно вхождение; маркеры `(×N)` дают кратность внутри адреса). `54 строки → 273 атома`, и это ровно `Σ` фазы, без остатка. Для 259 атомов из 273 адрес резолвится в дословную цитату владельца (`KRATNOST-vladelca.md`), остальные 14 прочитаны по источнику построчно. Делю по атомам; счёт от разметки цитат не зависит — он берётся из самой колонки адресов.

### 2. Тест, по которому проходит граница (называю заранее, применяю ко всем 273)

Первый сработавший пункт выигрывает:

1. **Предмет назван в таблице раздвоения дословно** → туда. СЛАЙДЫ: потолок знаков/бюджет слов, разметка сцен `{@N}`, `**acc**`, архетипы, предвёрсточный документ, **раскладка** (решение 2: из «шага 6 · лента» раскладки убираются). РЕСЁРЧ-ЦИКЛ: полнота формулировки, «сначала рассуждение, потом вывод», голос ленты, гейт «документ принят».
2. **Иначе — по артефакту, который в записи КРИТИКУЕТСЯ.** Дек, репетиция, разбор живой лекции, слайд → СЛАЙДЫ (включая «сценарий лекции»: решение 3 отдаёт слайдам и его, хотя стилистика у него ленточная). Лента, конспект, обзор, картотека, текст для чтения → РЕСЁРЧ-ЦИКЛ.
3. **Артефакт — ни лента, ни кадр** (печатный листок, страница-лонгрид, рисунок), либо предмет — дисциплина заходов/приёмки → **СПОРНО** с названием чужого дома. Подгонять под «где красивее» запрещено.

Опора теста — не вкус: решение 15 отдаёт ленте ровно два плакатных правила и остальные — слайдовому шагу 6; решение 16 привязывает корпус текстов к предвёрсточному документу, а голос ленты — к своим эталонам; решение 3 называет оба внутренних артефакта слайдов.

### 3. Шаги

1. `karta_domov.py`: завести `KLASS_06TEKST` — атомную разметку (дом по умолчанию на строку + поимённые исключения по адресу), развернуть фазу в атомы, добавить `--06tekst` (позаписная выдача: адрес · цитата · вердикт) и жёсткую сверку «сумма атомов = Σ фазы». `PHASE_TO_DOM` перестаёт класть `06-tekst` целиком в ЛЕНТУ.
2. Три остальные границы (решения 4, 16, 23) — проверить по `OTVETY-INTERVYU-2026-08-07.md` и живому корпусу, вердикт по каждой, включая отрицательный.
3. `pokrytie_rychagami.py`: записи берутся из корпуса (`karta_domov.py`) — он единственный источник счёта; из `POKRYTIE.md` домов берётся только «id → рычаг». Колонки: разобрано · машинное · процедурное · не накрывается · не разобрано. Процент машинного не смешивается с процедурным (`KONSTITUCIYA §11`). Пять домов карты сводятся к четырём домам плагина (решение 7: РЕСЁРЧ+ЛЕНТА = `reserch-cikl`).
4. Живой прогон по всем четырём домам плагина + `_illustracii/POKRYTIE.md`.
5. Верификатор — свежий субагент, метод «сверка сумм»: Σ старой карты против новой, список сменивших дом, доказательство «ни одна не потеряна и не удвоена», плюс выборка 10 ушедших в СЛАЙДЫ на глаз.
6. Коммит зоны двумя ходами, `check --zone`, `show --stat`.

**Чего НЕ делаю.** Не разбираю ни один дом. Не трогаю корпус. Не правлю `disciplina/` (READ-ONLY целиком) — замер только печатаю. Не вношу правило `SPORNYE.md §1` в скрипт: оно ждёт решения владельца (`SPORNYE.md §5`), и молча расширять его на новые спорные — не моё дело. Не сливаю РЕСЁРЧ и ЛЕНТУ в `karta_domov.py`: пять домов карты остаются пятью, слияние живёт в счётчике, где дома — плагинные.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **Критерий раздвоения оспорен верификатором, и спор упирается в решение владельца, а не в код.** Я режу по ПРОВЕНАНСУ: претензия, высказанная на разборе деки, даёт рычаг слайдам, даже если то же правило верно и для ленты. Опора — решение 16: корпус слайдовых текстов обслуживает предвёрсточный документ, а голос ленты собирается по СВОИМ эталонам, то есть слайдовый материал на ленту НЕ переносится. Верификатор судит иначе — «правило, безразличное к носителю, → СПОРНОЕ» — и на выборке 10 записей насчитал 6 таких. Если прав он, из 222 записей, ушедших в СЛАЙДЫ, заметная часть должна уехать в спорные; точное число под его критерием не считалось (оценивать его долей запрещено тем же правилом, что запрещает коэффициент). Вопрос: какой из двух критериев ваш? Ответ меняет порядок разбора трёх домов, а не только цифру.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. **Решение 4: весь ли шаг 4.5 — предмет скилла `slajdy`, или только «параметры деки»?** Сегодня фаза `04.5-intervyu` разложена оговоркой владельца от 06.08: параметры деки → СЛАЙДЫ (70), механизм разговора → МЕТА (63), остальное спорно (13 строк). Решение 4 от 07.08 отвечало на другой вопрос — кто ПРОИЗВОДИТ контракт артефакта — и оговорки не касалось, поэтому я оставил разметку как есть. Но если «04.5 → в слайды» значит «весь шаг, включая как вести разговор», то 63 записи «механика разговора» уезжают из МЕТА в СЛАЙДЫ.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. **Правило `SPORNYE.md §1` по-прежнему не в скрипте, и теперь оно нужно ещё четырём записям.** Три новых спорных из `06-tekst` (`КРВ-14c`, `КРВ-15a`, `КРВ-03c` — все про текст ВНУТРИ рисунка) закрывались бы пунктом 1 правила («рисунок → ИЛЛЮСТРАЦИИ, тема решает, а не фаза») в одну строку. Я его не применял: `SPORNYE.md §5` держит внесение правила в скрипт открытым вопросом к вам, и расширять его молча на новые записи — не дело исполнителя. Вносить?
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. **`_illustracii/POKRYTIE.md §2`, последняя строка таблицы: «остальные 20 классов … 35» — число не сходится.** Одиннадцать поимённых классов дают 78, корпус фазы `09-illustracii` даёт 107, значит остаток 29, а не 35; «35» — это сумма трёх классов из абзаца-вывода ниже (16+12+7). Классов в остатке тоже не 20, а 23 (агрегат `КРТ-РАЗН` разворачивается в четыре записи). Поймано счётчиком: `pokrytie_rychagami.py --dyry` печатает «без вердикта 23 id, Σ кратности 29». Файл вне моей зоны — не трогал.
   ДОМ: materials/_illustracii/POKRYTIE.md
   ДОСТАВЛЕНО: нет

5. **Контракт «дом объявляет накрытие» надо записать в сами дома, иначе счётчик будет мерить пустоту.** Метрика читает ровно одну форму: строку markdown-таблицы, первая клетка которой — id записи корпуса, последняя — рычаг. У ИЛЛЮСТРАЦИЙ такая форма есть и потому дом виден; у трёх остальных `POKRYTIE.md` таких строк нет ни одной, и их 0 % — правда, а не сбой чтения. Правило вместе с четырьмя видами рычага (машинный / процедурный / не накрывается / не накрыто) стоит вписать в `doma/README.md`, чтобы следующие заходы-разборы писали сразу в него.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/README.md
   ДОСТАВЛЕНО: нет

6. **Разворот агрегата `КРТ-РАЗН-*` в `karta_domov.py` теряет две записи из 2264.** Поимённо раскладываются 311 из 312 записей фазы `02-reserch` и 97 из 98 фазы `03-matbaza`; итоги домов при этом верны (они берутся из `srez_po_faze.py`), но к этим двум записям невозможно прицепить рычаг — у них нет своего id. Дефект живёт в скрипте с 06.08 и всплыл только сейчас, потому что раньше поимённая раскладка всех фаз никому не требовалась. Не чинил: обе фазы вне границы этого захода, а правка разворота сдвинула бы счёт там, где заход менять не просили. Счётчик печатает ⚠ на каждом прогоне.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: pereschet-i-schetchik#6

7. **Однострочная выжимка `KRATNOST-vladelca.md` теряет ту половину реплики, которая решает дом.** См. `## УРОКИ ФАБРИКЕ`, пункт 3.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: pereschet-i-schetchik#7

## ОТЧЁТ — (заполняет исполнитель)

### Что сделано и зачем

**1. Фаза `06-tekst` раздвоена позаписно (`karta_domov.py`, новый §C).** `PHASE_TO_DOM` больше не кладёт фазу в ЛЕНТУ целиком — у неё нет прямого перевода. Единицей деления взята не строка-группа, а **вхождение**: колонка адресов картотеки раскладывает фазу на 266 адресов-атомов, маркеры `(×N)` внутри адреса добирают кратность, итог — ровно 273 = Σ фазы. Это понадобилось потому, что механизм §B (id → дом) режет по строкам, а крупные строки фазы заведомо смешаны (`КРВ-08a` 25, `КРВ-07a` 24, `КРВ-11a` 17): отдать такую строку целиком — тот самый коэффициент, который заход запрещает, а отдать в спорные — похоронить сотню записей. Каждый атом размечен по тесту, который назван в `## ПЛАН` до работы; спорные несут причину поимённо, «не классифицировано» — ноль штук.

**2. Заведён `pokrytie_rychagami.py` — метрика накрытия.** Считает **записи из корпуса** (через `karta_domov.py`), а из файлов домов берёт только «id → рычаг»: дом, переписавший у себя цифру, не может поднять себе покрытие правкой текста. Контракт накрытия — одна форма: строка таблицы, где первая клетка — id корпуса, последняя — рычаг. Рычаг разбирается на четыре вида, и **процентом печатается только машинное** (`KONSTITUCIYA §11`: правило, которому нечему покраснеть, — надежда); процедурное и «не накрывается» стоят своими колонками. Отдельно печатается **разобранность** — доля записей, у которых вердикт вообще есть, включая честное «рычага нет и быть не может».

### Пересчёт границ — четыре из четырёх, числами

| решение | что тронуло | вердикт | числа |
|---|---|---|---|
| **2** — шаг 6 раздваивается по артефакту | фаза `06-tekst`, Σ 273 | 🔴 **МЕНЯЕТ** | СЛАЙДЫ **222** · ЛЕНТА **40** · СПОРНО **11**; сумма 273 = Σ фазы |
| **4** — фаза 04.5 → в слайды | фаза `04.5-intervyu`, Σ 146 | **НЕ МЕНЯЕТ** | СЛАЙДЫ 70 · МЕТА 63 · СПОРНО 13 строк — как было |
| **16** — корпус текстов из ленты в слайды | — | **НЕ МЕНЯЕТ** | 0 записей: корпус — справочный артефакт, не запись картотеки |
| **23** — `math-russian-terminology` втянут в ресёрч-цикл | 3 терминологические записи корпуса | 🔴 **МЕНЯЕТ на 1** | 1 из 3 — внутри моей фазы, учтена; 2 уже лежат где надо |

Обоснования отрицательных вердиктов — они здесь такой же результат, как положительные:

- **Решение 4.** Вопрос интервью (`INTERVYU-utro.md §4`) и ответ говорят о том, **кто производит** `KONTRAKT-<артефакт>`: контракт деки делает тот, кто делает деку, и `04.5-intervyu/DOK.md` переезжает в `slajdy/SKILL.md §3.5`. Разметка же записей фазы сделана по **предмету самой записи** оговоркой владельца от 06.08 («параметры деки» → СЛАЙДЫ, «механизм разговора» → МЕТА), и решение 4 этой оговорки не отменяет и слова «механизм разговора» не касается. Числа не двигаются. ⚠ Прочтение, при котором двигаются, существует и стоит 63 записи — вынесено вопросом 2.
- **Решение 16.** Проверено грепом по всем трём файлам корпуса (`KARTOTEKA-problem.md`, `KRATNOST.md`, `KRATNOST-vladelca.md`): ни одной записи, предметом которой был бы корпус текстов, нет — совпадения дал только номер строки «189». Корпус 189 слайдовых текстов — источник примеров, а не инцидент. Перевод фаза → дом решение не трогает, но **подпирает критерий раздвоения**: оно прямо говорит, что слайдовый материал ленте не передаётся, и на этом стоит мой тест (см. вопрос 1). Одна запись фазы корпус упоминает — `КРВ-29a` @ `fibonacci-kurs/SESSIYA.md:43` («тексты слайдов пишет Cowork, ориентир — корпус текстов»), она в СЛАЙДАХ, что решению 16 соответствует.
- **Решение 23.** Терминологических записей в корпусе три (греп «терминолог» по обоим файлам кратности, **3 из 3 разобраны**): `КРВ-03c` @ `dovodka-l1/SESSIYA.md:190` — фаза `06-tekst`, по решению 23 остаётся в ресёрч-цикле, хотя класс «компоновка кадра» тянул бы в СЛАЙДЫ (**это и есть изменение**); `КРВ-22a` @ `mat-kostyak/SESSIYA.md:78-84` («терминологию не решать за столом — смотреть, как принято в источниках») — фаза `02-reserch`, уже РЕСЁРЧ, двигать нечего; `КРВ-45b` @ `zamechaniya-issledovanie-idei.md:5` — фаза `04.5`, СЛАЙДЫ, но это владелец уточняет собственный термин в конкретной задаче, предмета скилла не задевает.

### Первый замер

```
python3 _studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py
python3 _studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py --06tekst   # позаписно, 266 атомов
python3 _studio/zhurnal/2026-08-05_faza-lenty/pokrytie_rychagami.py
```

Карта после пересчёта (числа — вывод команды, не переписаны рукой):

```
РЕСЁРЧ 429 · ЛЕНТА 241 · СЛАЙДЫ 676 · ИЛЛЮСТРАЦИИ 107 · МЕТА 775
  = reserch-cikl 670 (РЕСЁРЧ + ЛЕНТА, решение 7)
СПОРНЫЕ 31 запись (Σ кратности 36) · ИТОГО 2264 = корпус 2264
```

Дома **сравнялись, как и предсказывал `PUT-K-GOTOVOMU.md §3`**: было `reserch-cikl 903` против `СЛАЙДЫ 454` (вдвое), стало `670` против `676`.

Покрытие рычагами:

```
дом             записей  разобрано  машинн.  процед.  не накр-ся  не накрыто  не разобр. | машинное  разобрано
illustracii         107         78       28        0          12          38          29 |      26%        73%
slajdy              676          0        0        0           0           0         676 |       0%         0%
reserch-cikl        670          0        0        0           0           0         670 |       0%         0%
meta                775          0        0        0           0           0         775 |       0%         0%
```

🔴 **Пункт 5 критерия проверен и провален в той формулировке, в какой стоял, — и это ответ, а не отговорка.** Требовалось «ИЛЛЮСТРАЦИИ дают высокое **машинное** покрытие». Машинное — **26 %**, и высоким оно быть не может: сам дом пишет, что 35 инцидентов из 107 машиной не ловятся и в основном не могут (`_illustracii/POKRYTIE.md §2`, `§5`). Положительный контроль, который я объявил в `## ПЛАН` до работы, — **разобранность**, и он сработал чисто: **73 % против 0 % у трёх остальных**. Метрика различает разобранный дом и неразобранный; она не различала бы их, будь мерилом машинное покрытие, — тогда ИЛЛЮСТРАЦИИ (26 %) и МЕТА (0 %) стояли бы рядом, хотя первый разобран до конца, а второй не открывали. Ноль у трёх домов — правда, а не сбой чтения: строк «id → рычаг» в их `POKRYTIE.md` нет ни одной.

Счётчик умеет принять сопоставление МЕТА с 24 инструментами, когда оно появится: достаточно дописать в `meta/POKRYTIE.md` строки контракта — сама метрика не меняется.

### Как проверено

- **Сходимость** — гейтами внутри скрипта, а не глазами: `classify_06tekst()` падает `SystemExit`, если строка фазы не размечена **или если исключение не нашло своего адреса** (промахнувшееся исключение молча стало бы «домом по умолчанию»); `main()` падает, если Σ атомов ≠ Σ фазы. Оба проверены мутацией — правка одного символа в ключе роняет прогон.
- **Классификатор рычага** — прогнан на десяти синтетических клетках, включая ловушку «надеемся, что кто-нибудь заметит» (→ не накрыто) и отрицание «структурную ОШИБКУ сборки (**не** WARN)», на котором первая версия ставила ложную пометку «мягкий» ровно самому жёсткому рычагу дома. **10 из 10.**
- **Пропажа файла дома** не молчит: `--doma /nope` печатает ⚠ по каждому дому, а не рисует честный ноль.
- **Верификатор** — свежий субагент, метод сверки сумм (ниже).

### Верификатор

**Три числовых пункта из четырёх — ✅, и потери нет.**

- Σ по каждому дому, старая карта против новой: ЛЕНТА 474→240, СЛАЙДЫ 454→677, спорные Σ 25→36, ИТОГО и корпус 2264 в обеих версиях. *(Верификатор гонял версию до последней правки; после неё ЛЕНТА 241 / СЛАЙДЫ 676 — см. ниже.)*
- **Баланс переноса, остаток 0:** `234 ушло из ЛЕНТЫ = 223 пришло в СЛАЙДЫ + 11 добавилось к спорным`.
- **Ничего не потеряно и не удвоено, охват 54 из 54 строк-групп:** дублей адреса внутри строки — 0; множества id в раскладке / в новой фазе / в старой фазе / в таблице разметки — 54/54/54/54, все разности пусты; кратности строк old vs new — 0 различий. Сверх задания он проверил построчно, что Σ атомов каждой строки равна её кратности из числовой колонки картотеки (кратность и адреса — разные колонки, проверка не круговая): **0 расхождений из 54**.

**Четвёртый пункт — ❌, и это содержательный спор, а не арифметика.** На выборке 10 записей, ушедших в СЛАЙДЫ, он засчитал «про кадр» только 4 и заявил, что остальные 6 — правила про текст вообще, которым место в спорных.

Что я с этим сделал:

- **Одно возражение подтвердилось по источнику и исправлено.** `КРВ-05c` @ `PRAVKI.md:521`: живая строка несёт вторую половину реплики — «она у тебя без разбора, её тоже нужно разобрать», — то есть закон полноты формулировки, а он в тесте стоит выше провенанса. Запись переведена в ЛЕНТУ. **Отсюда финальные 222/40/11 вместо 223/39/11** и `ЛЕНТА 241 · СЛАЙДЫ 676` в карте. Урок вынесен в `## УРОКИ ФАБРИКЕ`.
- **Пять остальных проверены по живым строкам и оставлены как есть**, потому что спор в них — о критерии, а критерий назван решением владельца: решение 16 говорит, что слайдовый корпус ленте не передаётся, значит претензия, высказанная на разборе деки, даёт рычаг слайдам. Два возражения верификатора вдобавок опираются на натяжку: `КРВ-32a` («от конкретного к общему») он подвёл под ленточное правило «сначала рассуждение, потом вывод», а это разные правила — «от конкретного к общему» есть отдельный класс `КРВ-21a`.
- **Его довод о непоследовательности разобран, а не отброшен.** Он указал, что `И-74` («порядок изложения») объявлена спорной как «общий принцип, применим и к устному рассказу», а однородная `КРВ-32a` уехала в СЛАЙДЫ. Разные вопросы: `И-74` размечалась внутри фазы `04.5`, где выбор стоял между СЛАЙДЫ и МЕТА и «порядок изложения» не годился ни туда, ни туда; здесь выбор между СЛАЙДЫ и ЛЕНТА, и его решает провенанс.
- **Спор не закрыт мной, а вынесен владельцу** отдельным пунктом очереди (вопрос 1) — он стоит заметной части из 222 записей и решает порядок разбора трёх домов.

### Что НЕ трогал

Корпус инцидентов (`KARTOTEKA-problem.md`, `KRATNOST*.md`) — ни байта, только чтение. Репозиторий `disciplina` — ни байта: замер печатается, дома правит следующий заход. `_illustracii/POKRYTIE.md` — только чтение, найденное расхождение в отчёте (вопрос 4), не в файле. Разметка `вне-фаз` и `04.5-intervyu` — не тронута. Ни один дом не разобран: заход даёт инструмент и первый замер, разбор — следующие заходы. Дефект разворота `КРТ-РАЗН-*` на фазах `02-reserch`/`03-matbaza` не чинил (вопрос 6): фазы вне границы захода. Новых `.md` не заводил — оба файла зоны `.py`, `register_doc.py` и `KARTA.md` не при чём.

### Открытое «возвращаться»

Дом МЕТА (775) и дом СЛАЙДЫ (676) теперь два самых больших, и оба стоят на нуле разобранности. Порядок разбора, который следует из замера: **МЕТА** (рычаги уже построены — 24 инструмента, работа не «строить», а «предъявить»), затем **СЛАЙДЫ** (вырос вдвое и получил 222 новых записи), затем **reserch-cikl**. Но перед разбором СЛАЙДОВ имеет смысл закрыть вопрос 1: под критерием верификатора часть этих 222 уедет обратно.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/wt-pereschet/_studio/zhurnal/2026-08-05_faza-lenty/pokrytie_rychagami.py` — запускать `python3` из корня рабочей папки; там же правленый `karta_domov.py`. Смотреть глазами: `python3 _studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py --06tekst` — позаписный раздел фазы, 266 строк «дом · адрес», и `python3 _studio/zhurnal/2026-08-05_faza-lenty/pokrytie_rychagami.py` — таблица покрытия.

**КОММИТ:** `b567551` — `карта домов: фаза 06-tekst раздвоена позаписно по артефакту (решение 2) + счётчик покрытия рычагами` · `git_zona.py check --zone _studio/zhurnal/2026-08-05_faza-lenty/` → ✅ «работа доехала в git, вне git ничего нет» · `git show --stat` — ровно два моих пути, чужого нет. Ветка `zahod/pereschet-i-schetchik` НЕ влита, как велит контракт зоны.

**НЕОБРАТИМОЕ:** необратимого нет. Единственная запись за пределами worktree — секции `## ПЛАН` / `## ВОПРОСЫ` / `## ОТЧЁТ` / `## УРОКИ ФАБРИКЕ` этого файла-захода, как предписано контрактом зоны; сам файл не коммитил. Удалений, перезаписей, перемещений, `git reset`/`checkout` не было.


---

## ПРИЁМКА — заполняет аналитик, 2026-08-07

**ПРИНЯТ.** Коммит `b567551`, два файла: `karta_domov.py` +259, новый `pokrytie_rychagami.py` +294.

**Перепроверено приёмкой своим прогоном** — скрипты взяты с ветки, запущены вне рабочего дерева:

```
РЕСЁРЧ 429 · ЛЕНТА 241 · СЛАЙДЫ 676 · ИЛЛЮСТРАЦИИ 107 · МЕТА 775
  = reserch-cikl 670 · СПОРНЫЕ 31 (Σ 36) · ИТОГО 2264 = корпус 2264
```

Сходимость держится: **ни одна запись не потеряна и не удвоена**. Дома сравнялись ровно как предсказывалось: было `903` против `454`, стало `670` против `676`.

**Ловушка «пропажа дома не молчит» проверена мной отдельно** — прогон с неверным путём печатает ⚠ по каждому дому вместо честного нуля.

**Находка №4 подтверждена независимо, до цифры.** `_illustracii/POKRYTIE.md §2`: одиннадцать поимённых строк дают Σ 78, корпус фазы 107, остаток **29** — а в таблице стоит «остальные 20 классов … 35». Оба числа неверны; 35 — это сумма трёх классов из абзаца ниже.

### Пункт 5 критерия был мой и был неверен — исполнитель прав

Я требовал «ИЛЛЮСТРАЦИИ дают высокое **машинное** покрытие». Дом в собственном `POKRYTIE.md` пишет обратное: 35 инцидентов из 107 машиной не ловятся и в основном не могут. Машинное — 26 %, и высоким оно быть не может **по устройству предмета**, а не по недоделке.

Исполнитель опроверг предпосылку до работы, вписал поправку в `## ПЛАН` и предъявил другой контроль — **разобранность: 73 % против 0 % у трёх остальных**. Это правильная метрика: она различает разобранный дом и неоткрытый, а машинное покрытие поставило бы ИЛЛЮСТРАЦИИ (26 %) рядом с МЕТА (0 %).

Метрика печатает машинное и процедурное **разными колонками**, «не накрывается» — отдельным законным видом. Процент считается только по машинному.

### 🔴 Решения владельца, 2026-08-07

**Вопрос 3 — правило `SPORNYE.md §1` вносится в скрипт.** Оно уже принято владельцем как правило разбора; держать его в прозе, пока скрипт раскладывает по своим таблицам, — это два источника правды. Три новых спорных из `06-tekst` (текст ВНУТРИ рисунка) закрываются его пунктом 1 в одну строку.

**Вопрос 5 — контракт «дом объявляет накрытие» вписывается в `doma/README.md`.** Метрика читает ровно одну форму: строка таблицы, первая клетка — id корпуса, последняя — рычаг. Пока формы нет в доме, счётчик честно показывает 0 %, и это правда, а не сбой. Четыре вида рычага (машинный / процедурный / не накрывается / не накрыто) записываются туда же, чтобы заходы-разборы писали сразу в нужном виде.

**Вопросы 1 и 2 — вынесены владельцу отдельно**, ответы меняют порядок разбора трёх домов, а не только цифру.

### 🔴 Дефект приёмки — мой, и он может помешать слиянию

Проверяя числа, я скопировал скрипты с ветки **в рабочее дерево основной папки**: `karta_domov.py` оказался изменён, `pokrytie_rychagami.py` — заведён как untracked. `karta_domov.py` восстановлен из `HEAD` тем же ходом, зона чиста. **`pokrytie_rychagami.py` снять не удалось** — песочница не даёт удалять файлы в примонтированной папке.

Файл **побайтно совпадает с версией на ветке** (md5 сверен), но untracked-файл на пути слияния способен уронить `merge` с «untracked working tree file would be overwritten». **Первым ходом того, кто будет вливать `zahod/pereschet-i-schetchik`:**

```
rm _studio/zhurnal/2026-08-05_faza-lenty/pokrytie_rychagami.py
```

Класс тот же, что уже стоил этой арке заклинившего `.git/index.lock`: **Cowork не должен писать в рабочее дерево, даже ради проверки.** Проверять надо во временной папке — что и было сделано следующим ходом, после ошибки.

### Уроки 27–29 разнесены

**27.** Заход послал в worktree и велел читать файл, которого там нет: `PUT-K-GOTOVOMU.md` заведён аналитиком и не закоммичен. Структурный дефект — за сессию таких файлов заведено два. Вердикт: `bootstrap_zahod.py` проверяет, что каждый `.md`-якорь §0 существует **в git**.

**28.** Однострочная выжимка корпуса теряет ту половину реплики, которая решает дом. Выжимка годится **отобрать** записи и не годится **судить** по ним — судить надо по адресу. Задевает все 2264 записи.

**29.** Разворот агрегата `КРТ-РАЗН-*` теряет две записи: у них нет своего id, рычаг прицепить не за что. Счётчик печатает ⚠ на каждом прогоне — дефект больше не молчит.
