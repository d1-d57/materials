# Канал исполнителя — kanal-nablyudenij (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — форма канала выводится из живого материала: 123 накопленных пункта показывают, что канал должен уметь, и никакое описание этого не заменит.

КОНТЕКСТ. Владелец строит плагин `disciplina` — семь скиллов, извлечённых из его фабрики презентаций. Работа идёт заходами: аналитик пишет файл-заход, исполнитель дописывает в него план, вопросы и отчёт, аналитик судит по отчёту.

**Откуда взялся нынешний корпус — замер, не память.** В картотеке 2263 живые записи, четыре корпуса, все четыре числа подтверждены независимым пересчётом сырых файлов (`2026-07-30_dovodka-fabriki/KARTOTEKA-problem.md`, шапка):

| корпус | записей | доля | как добывали |
|---|---|---|---|
| **реплики владельца** | **1379** | **61 %** | разборы после живых лекций, правки после репетиций, файлы замечаний, помарки в дневниках арок |
| кандидаты исполнителей | 461 | 20 % | ночная вычитка 118 заходов — отчёты |
| уроки арок | 316 | 14 % | `UROKI-FABRIKE.md` всех арок |
| автолог git | 107 | 5 % | `_INFRA-git/INCIDENTY.md`, пишется хуком |

**Канал есть ровно у одного корпуса из четырёх — у уроков** (`check_uroki.py` на форму, `dostavit_urok.py --gejt` на доставку). Остальные 84 % корпуса добывались разовыми заходами-раскопками и с тех пор не пополняются ничем.

**Симптом, который это уже дал.** По одной этой арке накопилось **123 недоставленных пункта очереди** в `## ВОПРОСЫ` 37 заходов, 74 из них с `ДОМ: владелец`. `priyomka.py` их печатает, но провалом не считает — поэтому они и накопились.

**Зачем это владельцу — его формулировка, и она задаёт приоритет.** Огромное переписывание проекта затевалось потому, что был собран большой пул недовольств, и **проанализировать его не получилось**. Сейчас ставка на то, что разнесение по домам сделает его работоспособным. Но времени нет: *«идея хотя бы в том, чтобы в дальнейшем всё копилось и по понятным местам раскладывалось… чтобы это хотя бы не терялось. Потом будем большими заходами что-то закрывать»*.

🔴 **Отсюда критерий полезности этого захода: не «сколько разобрано», а «что перестало теряться».** Разбор — работа будущих больших заходов, и подменять её здесь не надо.

ЦЕЛЬ: перекрыть каналом все источники, а не один из четырёх, — и через него же провести 123 накопленных.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **Порядок обязателен: сначала инвентарь источников (§2.1), потом канал, потом миграция.** Инвентарь — не разминка: он решает, каким канал будет. Построишь по трём известным руслам — перекроешь 34 % корпуса и объявишь задачу закрытой.

🔴 **Сначала канал, потом миграция.** Миграция 123 пунктов — это не отдельная работа, а **живой прогон канала**. Разнесёшь руками, а канал напишешь потом — не узнаешь, работает ли он: ровно те 123 случая, на которых он должен споткнуться, уже будут разобраны.

🔴 **Ты НЕ отвечаешь на вопросы владельца.** 74 пункта адресованы ему. Твоё дело — довезти их до места, где он их увидит, схлопнуть дубли и сгруппировать. Ответ за него — брак.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/wt-kanal`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/kanal-nablyudenij`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add wt-kanal --branch zahod/kanal-nablyudenij
  cd ../materials-wt/wt-kanal
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/dostavit_urok.py` · `_generator/tools/priyomka.py` · `_generator/tools/fixtures/nablyudenia/` (заводишь) · `_studio/zhurnal/2026-08-05_faza-lenty/ULOV-OCHEREDI.md` (заводишь) · в файлах `_studio/zhurnal/2026-08-05_faza-lenty/kod_*.md` — **ТОЛЬКО строки `ДОСТАВЛЕНО:` внутри секции `## ВОПРОСЫ`**, ничего больше в них не менять ни на символ.
  🔴 **`bootstrap_zahod.py` и `zakryt_sessiyu.py` — READ-ONLY, хотя канал их касается.** Рядом идёт заход, который их правит. Нужна их правка — **приложи готовый патч к отчёту** с проверенным якорем (`grep -c` → 1), как сделал заход `dver-urokov`; встроит следующий. Тронешь — будет конфликт слияния в файле, который правят двое.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/kanal-nablyudenij`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `dostavit_urok.py` и `priyomka.py` существуют — их состояние до тебя достаётся `git show HEAD:<путь>`. Новые файлы фабриковать не надо.
- **Прочитать ТОЛЬКО это:**
  1. `_generator/tools/dostavit_urok.py` — **целиком.** Это половина канала, которая уже работает; вторую строй по её образцу, а не заново.
  2. `_generator/tools/priyomka.py` — разделы про Г5 и Г7 и место, где печатаются пункты очереди. Тело целиком не надо.
  3. `_studio/zhurnal/2026-08-05_faza-lenty/kod_nochnaya-sborka.md` и `kod_vynos-meta.md` — **секции `## ВОПРОСЫ`**. Это два самых нагруженных захода (24 и 11 недоставленных). По ним видно, как пункт очереди выглядит в жизни, а не в шаблоне.
  4. `VYPISKA-IZ-DIALOGA.md` §2 и §5 — что владелец уже назвал беклогом и как устроены дома. Не пересказывай его, он на входе у другого захода.
  Файлы-заходы целиком НЕ читай: их 37 и они длинные. Пункты очереди снимай программно.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### 2.1 🔴 ПЕРВЫМ ХОДОМ — инвентарь источников. Пять, а не три

По каждому ответь тремя вещами: **добывается ли программно · чем именно · что теряется сегодня**. Числа снимай командами, не бери отсюда.

| # | источник | что уже есть | что проверить |
|---|---|---|---|
| 1 | **транскрипт чата** | `zakryt_sessiyu.py` добывает реплики владельца, обещания аналитика, его вопросы | 🔴 **выгрузка режет реплику на ~300 знаках.** Самые содержательные реплики этой сессии — 1389, 825, 692 слова; от них в выгрузке два предложения. Замерь: сколько реплик обрезано и сколько слов потеряно |
| 2 | **отчёты исполнителей** | `## ОТЧЁТ` / `## ВОПРОСЫ` / `## УРОКИ ФАБРИКЕ` в `kod_*.md` | пункты очереди печатаются, но не сторожатся (123 накопленных). Проза отчёта не добывается ничем |
| 3 | **уроки арок** | полный канал: форма + доставка + гейт | образец, по которому строится остальное |
| 4 | **автолог git** | `_INFRA-git/INCIDENTY.md`, пишется хуком | 🔴 **единственный источник, который пишется САМ**, без чьего-либо решения записать. Доля 5 %, потери нулевые. Это образец механики, а не мелочь — посмотри, чем он берёт |
| 5 | **артефакты работы** | ничего | отсюда пришли 1379 записей: разборы репетиций, файлы замечаний, помарки в дневниках проектов. Общая примета — **прямая речь владельца в файлах проектов**. Проверь, грепается ли она, и с какой точностью |

**Выход §2.1 — таблица в отчёте:** источник · добывается программно (да/нет/частично) · чем · что теряется · во что обойдётся перекрыть. Без неё дальше не иди.

⚠ **Честный ответ «источник 5 программно не добывается» — законный.** Тогда скажи, чем его заменить: например, ритуалом на приёмке, у которого есть сторож. Приём хуже автоматики, но лучше пустоты.

### 2.2 Канал: одно понятие «наблюдение», один инструмент, один гейт

`dostavit_urok.py` уже умеет ровно это для уроков: разобрать → назвать дом → доставить дословно → пометить → покраснеть на недоставленном. **Расширь его, а не пиши второй.** Второй парсер той же вещи в этом репозитории уже расходился с первым — это записанная цена.

Наблюдение любого вида имеет одинаковый путь и различается полем вида. Виды берёшь из §2.1, а не из этого списка — здесь он для примера: урок · пункт очереди · замечание из отчёта · реплика владельца.

Режимы — те же три, плюс один:

```
--plan     <файл>     таблица: что, куда, доставлено ли, разобран ли адрес
--dostavit <N> <файл>  дословно в дом + метка, идемпотентно
--gejt     <файл>     красный, пока есть недоставленное
--volna    <папка>    🔴 НОВОЕ: всё накопившееся по ВСЕЙ арке, сгруппированное ПО ДОМАМ
```

**`--volna` — то, ради чего владелец это просит.** Он хочет раз в неделю приходить с вопросом «что накопилось» и видеть не 37 файлов, а список по скиллам: у `slajdy` столько-то, у `reserch-cikl` столько-то, без дома — столько-то. Это вход в работу, а не отчёт о ней: рядом с каждым домом должно стоять, **чем эту порцию закрывать**.

🔴 **`ДОМ: владелец` — не дом.** Пункт с таким адресом никуда не доезжает и потому не может считаться доставленным. Канал обязан показывать такие отдельной группой и не давать ей молча раствориться: это класс, который сегодня потерялся весь.

**Гипотеза аналитика, которую проверь числом:** половина из 74 «владелец» — не вопросы владельцу, а находки, которым **не нашлось дома среди скиллов**, и `владелец` поставлен как «не знаю куда». Если так — канал обязан различать «решение за человеком» и «дома нет»: разные состояния с разной судьбой.

### 2.3 Починить то, что теряет прямо сейчас

Две вещи из §2.1 стоит починить этим же заходом, если инвентарь их подтвердит:

**Обрезка выгрузки.** Реплика владельца — главный источник корпуса (61 %), и штатный инструмент показывает от длинных реплик первые два предложения. Это уже стоило потери сегодня: аналитик обошёл инструмент своим скриптом и нашёл в хвостах три решения, которых не было в выгрузке. ⚠ `zakryt_sessiyu.py` **READ-ONLY** — приложи патч к отчёту.

**Место для замечания из прозы отчёта.** Наблюдение «это работало не так, пришлось переделать дважды» сегодня умирает вместе с отчётом. Владелец дословно: *«должно быть программно заведено место, куда можно записывать подобного рода замечания к архитектуре происходящего»*. Порог у него другой, чем у урока: у урока обязательна ЦЕНА, у замечания — **адресат** (какой скилл или какая часть фабрики). Форма обязана отвечать: **что мешало · кому принадлежит · чем проверяется, что починили**.

⚠ Шаблон живёт в `bootstrap_zahod.py`, он **READ-ONLY** — форму проверь на фикстуре, встройку приложи патчем.

⚠ **Не заводи новый вид, если хватает существующих.** Окажется, что «замечание» неотличимо от «урока без цены» — скажи прямо и не плоди сущность. Отрицательный ответ дешевле секции, которую никто не заполняет.

### 2.4 Гейт на приёмке — Г8, и он краснеет НЕ всегда

`priyomka.py` печатает пункты очереди и провалом их не считает — поэтому 123 и накопились.

Но гейт «есть недоставленное → красный» на **каждой** приёмке сделает красным каждый заход: пункты копятся по замыслу, разносят их на консолидации. Такой гейт отключат первым — это уже случалось в этом репозитории.

Поэтому Г8 краснеет по **порогу или стадии**, а не по факту. Что именно — реши по корпусу и **обоснуй числом**: посмотри, сколько пунктов давал типичный заход и с какого места счёт перестал быть нормальным. Вариант «краснеет только при закрытии арки» — законный, если ты покажешь, что это ловит те же 123.

### 2.5 Миграция — живой прогон канала

Провести все 123 через `--volna` и `--dostavit`. Результат — `ULOV-OCHEREDI.md`: **дубли схлопнуты** (с указанием, что с чем), 49 файловых доставлены в дома, 74 «владелец» **сгруппированы по темам** — не списком из 74 строк, который никто не прочтёт.

🔴 **Схлопывая дубль, сохраняй все адреса-источники.** Пункт, всплывший трижды, — это не одна находка, а свидетельство, что она болит; потеряв два адреса из трёх, потеряешь вес.

⚠ **Не доставляй туда, где дома нет.** `--dostavit` уже отказывает на несуществующем доме и на файле-указателе — это верное поведение, не ослабляй его ради зелёного счёта.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Восемь условий:

1. 🔴 **Инвентарь пяти источников предъявлен таблицей**, у каждого — добывается ли программно, чем, что теряется. Для источника 1 — **замер обрезки числом**: сколько реплик обрезано, сколько слов потеряно.
2. **`--volna` отработал на ЖИВОЙ папке арки** (37 файлов), `rc=0`, вывод дословно: по домам, с числом у каждого и с отдельной группой «дома нет».
3. **`--gejt` краснеет** (`rc=1`) на **живых файлах арки, не на фикстуре**, до миграции и **меняет ответ** после неё. Оба прогона дословно; не поменял — гейт не работает.
4. **Дубли схлопнуты:** было 123, стало N уникальных, оба числа. У каждого схлопнутого — все адреса-источники, проверено на трёх примерах поимённо.
5. **49 файловых доставлено или отказ назван:** сколько доехало, сколько отказало и по какой причине (дома нет / файл-указатель). Проверка фактом: `grep` метки в доме и `--gejt` после.
6. **74 «владелец» сгруппированы** в не больше чем **12 тем**, у каждой — сколько пунктов и чем тема закрывается. Тем больше двенадцати — объясни, почему не схлопнулось.
7. **Г8 встроен, и показано, что он НЕ красный всегда:** прогон на заходе с типичным числом пунктов (зелёный) и на арке целиком (красный). Оба вывода.
8. **Форма третьего входа описана и проверена на фикстуре**, патч для `bootstrap_zahod.py` приложен с проверенным якорем — **или** обоснован отказ заводить четвёртый вид.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

⚠ **Условия 1 и 2 — то, ради чего заход существует.** Провались всё остальное, но инвентарь предъявлен и `--volna` печатает накопившееся по домам — владелец получил ту самую еженедельную волну, и дальше система живёт. Пройди всё, кроме него, — получится разовая уборка, которую через неделю придётся делать снова.

## 3. ВЕРИФИКАТОР

Нужен, операция лоссовая дважды: канал **пишет в чужие файлы** и **схлопывает записи**, а потерянный при схлопывании пункт неотличим от никогда не существовавшего.

Свежий субагент, метод обратный: берёт **исходные 123 пункта** прогоном по `kod_*.md` и сверяет с `ULOV-OCHEREDI.md` **как множества**. Отвечает на один вопрос: **список потерь ПУСТ?** Каждый исходный пункт обязан быть либо доставлен, либо назван в группе, либо явно объявлен дублем чего-то конкретного. Третьего не дано.

Плюс отдельно: **пять случайных доставленных** — открыть дом и проверить, что текст доехал дословно и разметка соседей не поехала (`diff --numstat`, удалений ноль).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/dostavit_urok.py` `_generator/tools/priyomka.py` `_generator/tools/fixtures/nablyudenia` `_studio/zhurnal/2026-08-05_faza-lenty/ULOV-OCHEREDI.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/dostavit_urok.py` `_generator/tools/priyomka.py` `_generator/tools/fixtures/nablyudenia` `_studio/zhurnal/2026-08-05_faza-lenty/ULOV-OCHEREDI.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Задание требует писать в дома, а контракт зоны домов не называет — и это не оговорка, а неисполнимая пара
ЦЕНА: §2.5 предписывает «49 файловых доставить в дома», а `## КОНТРАКТ ЗОНЫ` перечисляет четыре пути, среди которых нет ни одного дома. Дома выяснились только на прогоне: их пять (`OTKRYTYE-ZADACHI.md`, три файла `_studio/konvejer/`, `UROKI-FABRIKE.md` арки). Исполнить оба указания разом нельзя: доставишь — выйдешь за зону; не доставишь — провалишь §2.5 и условия 3 и 5. Я вышел за зону осознанно и назвал это отдельной строкой отчёта, но выбор делал за составителя захода. Это ровно тот класс «два обязательных указания, невыполнимых разом», который в `GIT-disciplina §3` уже стоил 27 обходов `--no-verify`: там, где указания противоречат, исполнитель обходит одно из них молча. ВЕРДИКТ: зона захода-мигратора обязана перечислять дома, а если они выясняются прогоном — контракт должен разрешать их явной строкой «плюс файлы-дома, названные `--volna`». Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.

### Дверь доставки не различала дом-документ и дом-код, и «успешная доставка» ломала инструмент
ЦЕНА: `--dostavit 6` дописал markdown в конец `_generator/tools/git_zona.py` — единственной двери к git в этом репозитории. Файл стал невалидным Python (`SyntaxError`), а команда напечатала «✅ Урок 6 доставлен» и вернула 0. Поймал случайно, следующей же командой; восстановил `git checkout HEAD --`, ход потерян. Дефект был не мой — он лежал в двери с самого её появления, и на живой арке так адресованы 15 записей: пятнадцать сломанных инструментов за один прогон миграции, каждый с зелёным кодом возврата. ВЕРДИКТ: класс «дом — исполняемый код» заведён, доставка отказывает (`rc=2`), ловушка 13 фикстуры сторожит и проверена мутацией. Общее правило шире инструмента: **отказ, выглядящий успехом, опаснее падения** — там, где инструмент дописывает в чужой файл, вид файла обязан быть частью решения. Дом — `<эта арка>/UROKI-FABRIKE.md`.

### Число из задания не воспроизвелось ни одним из трёх способов счёта — потому что у предмета работы не было одного счёта
ЦЕНА: «123 недоставленных, 74 владелец» не подтвердилось: парсер приёмки даёт 127, греп — 234, канал — 156. Расхождение не косметическое: 29 находок не показывал ни один инструмент (жирный номер, секция `## ВТОРАЯ ПАЧКА ВОПРОСОВ`, отсутствие номера), а 74 ложных срабатывания давала шапка-подсказка. Условия 4, 5 и 6 критерия готовности сформулированы в этих числах («было 123, стало N», «49 файловых», «74 владелец») и как записаны — непроверяемы. Пришлось инстанцировать их заново живым замером, до начала работы. ВЕРДИКТ: заход, чей критерий держится на числе, обязан давать КОМАНДУ, которая это число печатает, — иначе первый ход исполнителя уходит на доказательство того, что задание описывает не тот мир. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`.

### Гейт, написанный под форму, ловит форму, а не содержание: заголовок доставки сделал пункт «уроком без цены»
ЦЕНА: доставленные пункты уезжали в дом с заголовком `### <заход>#<N>`, и `check_uroki.py` (`HEADING = ^### (.+)$`) немедленно счёл их уроками, потребовал `ЦЕНА:` и покраснел на 27 верных доставках, заблокировав коммит. Откатил все 27 (`git checkout HEAD --`), сменил уровень заголовка на `####`, доставил заново — полный лишний круг. ВЕРДИКТ: когда инструмент дописывает в дом, который сторожит чужой гейт, форма записи обязана быть проверена ПРОГОНОМ этого гейта, а не рассуждением о ней. Дом — `<эта арка>/UROKI-FABRIKE.md`.

## ПЛАН — (заполняет исполнитель)

**Ф0 сделана (read-only разведка).** Рабочая папка `../materials-wt/wt-kanal` заведена, `git rev-parse --abbrev-ref HEAD` → `zahod/kanal-nablyudenij` (rc=0). Прочитано ровно названное: `dostavit_urok.py` целиком, `priyomka.py` (Г5/Г7 и печать очереди), секции `## ВОПРОСЫ` двух заходов, `VYPISKA-IZ-DIALOGA.md §2 и §5`.

🔴 **Предпосылка, которая уже не подтвердилась, — и она меняет порядок работы.** Число «123 недоставленных / 74 владелец» живым замером не воспроизводится, причём тремя способами получаются три разных ответа:

| способ счёта | недоставленных | «владелец» |
|---|---|---|
| парсером `priyomka.parse_ochered` по секции `## ВОПРОСЫ` 37 файлов | **127** | **76** |
| `grep -c "ДОСТАВЛЕНО: нет" kod_*.md` (сумма) | **234** | — |
| заход (§«Симптом») | 123 | 74 |

Вилка не косметическая: 234 против 127 — это вдвое. Уже видно, откуда часть: у `kod_nochnaya-sborka.md` парсер видит 6 пунктов, греп — 26, а заход называет 24. То есть **у самого предмета работы сегодня нет одного счёта**, и «сколько накопилось» — вопрос без ответа, а не число. Пока это не разрешено, ни критерий 4 («было 123, стало N»), ни охват миграции не имеют смысла: неизвестно, что такое 100 %.

Поэтому первое дело инвентаря (§2.1, источник 2) — **не пересчитать, а установить причину вилки**: пункты вне секции `## ВОПРОСЫ`, пункты внутри ```-забора (шапка секции несёт образец формы с `ДОСТАВЛЕНО: нет` — 37 файлов × образец), пункты с отступом, которые не ловит `^(\d+)\.` парсера. Разбор пунктов очереди в канале строю **один**, и он обязан объяснять каждое из трёх чисел, а не выдавать четвёртое. Все числа критерия предъявлю этим разбором и командой к нему — не значением из захода.

**Развилки, которые называю до работы:**
- **Р1. Расширяю `dostavit_urok.py`, не пишу второй инструмент** (§2.2 требует прямо, и в шапке файла записана цена второго парсера). Риск: `--gejt`/`--plan` уже зовут из живых команд (`VYPISKA §5`) и, возможно, из хуков. Поэтому **вызов без указания вида остаётся ровно прежним по поведению**, новое — только под явным флагом вида. Проверю прогоном старой формы до и после правки: вывод обязан совпасть.
- **Р2. Критерий 3 («`--gejt` меняет ответ после миграции») исполним только вместе с §2.4.** Если гейт краснеет на любом недоставленном, он останется красным навсегда: пункты `ДОМ: владелец` доставить некуда по определению, а их большинство. Значит гейт обязан различать **«не доставлено, хотя дом есть»** (чинится доставкой — краснеет всегда) и **«дома нет / решение за человеком»** (краснеет по стадии закрытия арки). Ровно это же различение требует §2.2 в конце. Строю его как одно решение, а не два.
- **Р3. Гипотезу аналитика (половина «владелец» — это «дома нет», а не вопрос владельцу) проверяю числом и классификацией**, до того как строить разные состояния. Не подтвердится — скажу и не разведу состояния ради красоты.

**Порядок (он же обязателен по заходу):**
1. **Инвентарь пяти источников.** Числа командами. Отдельно: замер обрезки транскрипта (источник 1) — сколько реплик обрезано и сколько слов потеряно; грепаемость прямой речи владельца в файлах проектов (источник 5) с оценкой точности; чем берёт автолог git (источник 4) — механика, а не доля. Выход — таблица в отчёт. Без неё дальше не иду.
2. **Канал.** Понятие «наблюдение» с полем вида поверх готового разбора уроков; режимы `--plan` / `--dostavit` / `--gejt` / `--volna`, виды — из §2.1. Фикстуры в `_generator/tools/fixtures/nablyudenia/`.
3. **`--volna` по всей папке арки** — группировка по домам, отдельной группой «дома нет» и «решение за человеком», у каждого дома — чем порция закрывается.
4. **Г8 в `priyomka.py`** — порог/стадия, обоснованные распределением пунктов по заходам (типичный заход vs арка целиком). Предъявлю оба прогона.
5. **Миграция как живой прогон канала:** `--volna` → `--dostavit` по файловым, схлопывание дублей с сохранением ВСЕХ адресов-источников, группировка «владелец» по темам (≤12). Выход — `ULOV-OCHEREDI.md` (регистрирую `register_doc.py` тем же ходом, `KARTA.md` — в обоих ходах коммита).
6. **Верификатор** — свежий субагент, метод обратный: исходное множество пунктов против `ULOV-OCHEREDI.md`, вопрос один — список потерь пуст? Плюс пять случайных доставленных: дословность и `diff --numstat` с нулём удалений.
7. **Патчи к READ-ONLY** (`zakryt_sessiyu.py` — обрезка; `bootstrap_zahod.py` — форма третьего входа, **если** §2.3 подтвердит, что он нужен) — в отчёт, с якорем, проверенным `grep -c` → 1. Сами файлы не трогаю.
8. **Коммит зоны двумя ходами** (`add`, затем `commit` с теми же путями после `--`), `git_zona.py check --zone`, `show --stat`.

**Чего НЕ делаю:** не отвечаю за владельца на его 76 пунктов (довожу и группирую); не разбираю содержание находок — это работа будущих больших заходов; не трогаю ничего вне зоны; ветку не вливаю.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **Живой ложно-зелёный, который Г7 теперь ловит: `kod_faza-lenty.md` пункты 1–4 помечены `ДОСТАВЛЕНО: faza-lenty#1…#4`, а в доме их нет ни одного.** Проверено: `grep -n "ДОСТАВЛЕНО: faza-lenty#" kod_faza-lenty.md` → 4 строки (293, 306, 319, 330), `grep -c "faza-lenty#" _studio/konvejer/06-tekst/UROKI-FABRIKE.md` → 0. Метки старше этого захода: `git show a4d6cb3~1:` содержит их все четыре. Это ровно тот случай, против которого Г7 написан — доставка объявлена, содержания на месте нет. Метки ставил не я: они старше этого захода. Сам не чинил — содержание находок принадлежит их автору, а угадывать, что именно должно было уехать, значит подменить пропажу выдумкой. Красный Г7 на этом заходе сейчас честный, и он единственный из 37.
   ДОМ: `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md`
   ДОСТАВЛЕНО: нет

2. **Ложная тревога, снятая верификатором, — и она успела попасть в текст: «коллизия номеров» была дефектом МОЕГО разбора, а не файла.** Инструмент печатал, что в `kod_nochnaya-sborka.md` номера 1, 4 и 9 задвоены, и предлагал перенумеровать файл. На живом файле все 24 номера уникальны: вторая секция вопросов нумерацию не начинает заново, а продолжает с 7. Двоились они только в разборе: у трёх пунктов первая строка тела — ```-ограда или подзаголовок пачки, номер оттуда не читался, и fallback «порядковый внутри секции» выдумывал 1, 4, 9 для пунктов, физически подписанных 7, 10 и 15. То есть инструмент предлагал чинить здоровый файл по показаниям сломанного себя. Починено: номер ищется в первых пяти непустых строках тела, а при его отсутствии адресом служит номер строки — он уникален по построению, в отличие от придуманного порядкового. После правки записей с неуникальным номером: 0 из 182. Поймал верификатор, я бы не увидел — оба моих замера шли одним и тем же разбором.

3. **`dnevnik.py` в среде исполнителя меряет НЕ ТОТ транскрипт и печатает ложное покрытие.** `sostoyanie()` берёт последний по mtime `.jsonl`, а в сессии Claude Code это транскрипт самого исполнителя: на живой арке с 49 разнесёнными репликами (`grep -c 'S....-В' SESSIYA.md` → 49) гейт печатает «покрыто 0 из 1». Число ложное в обе стороны — и знаменатель, и числитель чужие. Приёмка печатает его как «дневник неполон» на каждом прогоне. Чинится привязкой к транскрипту, названному в шапке `VYGRUZKA-*.md`, а не к mtime. Файл вне моей зоны, не трогал.
   ДОМ: `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md`
   ДОСТАВЛЕНО: нет

4. **Пять отсутствующих файлов держат почти половину очереди к владельцу.** Разметка ста пунктов `ДОМ: владелец` показала: 46 из них — не вопросы, а находки без адреса, и больше половины этих 46 сводятся к пяти домам, которых нет на диске: реестр долгов инструментов · диспетчер веток и рабочих папок · реестр расхождений «источник ↔ производное» · очередь мелких правок вне зоны · журнал отклонений от контракта зоны. Завести их — значит убрать из очереди к владельцу около двадцати пунктов **без единого содержательного решения**. Это самая дешёвая позиция всего улова, и она не требует разбора находок.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. **Секция `## ВОПРОСЫ` в шаблоне захода несёт образец формы, который считается пунктом всеми грепами.** Две строки на файл (`ДОСТАВЛЕНО: нет` внутри blockquote-образца и упоминание в прозе подсказки) — 74 ложных срабатывания на 37 файлах, то есть половина расхождения между «234 по грепу» и «156 по разбору». Канал их отсекает по признаку blockquote, но любой греп-замер снаружи будет врать, пока образец лежит в том же виде, что живая запись. Тот же класс, что оплаченный в `check_uroki.py` образец урока внутри ```-забора. Чинится в шаблоне `bootstrap_zahod.py` — файл READ-ONLY по контракту, патча не прилагаю: правка косметическая (перенести образец в ```-забор), а якорь в файле, который сейчас правит соседний заход.
   ДОМ: `<эта арка>/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/wt-kanal/_studio/zhurnal/2026-08-05_faza-lenty/ULOV-OCHEREDI.md` — любой просмотрщик markdown
*(лежит в рабочей папке захода, ветка `zahod/kanal-nablyudenij`, коммит `a4d6cb3`; после влития приёмкой будет доступен по тому же пути в основной папке)*
**КОММИТ:** `8243e48` (пятый и последний; поверх `4d899d9`, `e655942`, `e3c9d07`, `a4d6cb3`) — «канал наблюдений: разбор пунктов очереди, --volna по арке, Г8 приёмки, миграция 40 находок в дома» · `git_zona.py check --zone _generator/tools` → ✅ · `--zone _studio/docs` → ✅ · `--zone _studio/konvejer` → ✅ · `--zone _studio/zhurnal/2026-08-05_faza-lenty` → ✅ (все четыре из рабочей папки)

### 1. Что сделано и зачем

**Инвентарь пяти источников — §2.1, условие 1.** Таблица ниже. Он поменял конструкцию канала дважды: разбор пунктов написан не по номеру (иначе терялась бы треть), а гейт разведён на два состояния (иначе он не мог бы стать зелёным никогда).

| # | источник | добывается программно | чем | что теряется сегодня | во что обойдётся перекрыть |
|---|---|---|---|---|---|
| 1 | **транскрипт чата** (61 % корпуса) | **да, полностью** | `zakryt_sessiyu.py` → `VYGRUZKA-*.md` | **не тем, что предполагал заход** — см. §2 ниже | ничего: добыча уже полная, чинить надо звено разноса |
| 2 | **отчёты исполнителей** | **было частично, стало да** | канал: `--plan` / `--volna` / `--gejt` | было: треть очереди невидима, накопление не сторожится | сделано этим заходом |
| 3 | **уроки арок** | да | `dostavit_urok.py` (тот же файл) | адреса: 15 ведут в `.py`, 7 — в несуществующие файлы | образец, по которому построено остальное |
| 4 | **автолог git** (5 %) | **да, и сам** | `git_zona.py:319 log_incident()` на 27 точках отказа | успешные коммиты; всё, что прошло мимо обёртки | ничего; это образец механики — разбор ниже |
| 5 | **артефакты работы** (61 % корпуса пришло отсюда) | **нет** — замерено | греп по формам прямой речи | **точность 15 %, полнота 56 %** | программно не берётся; нужен ритуал со сторожем |

**Источник 4 — механика, ради которой его и просили посмотреть.** Он не теряет по пяти причинам, и все пять переносимы: (а) у события есть **единственная дверь**, через которую оно физически обязано пройти; (б) пишет **инструмент, а не агент** — решения «записать» не существует вовсе; (в) запись берётся из **той же строки, что уже печатается человеку** в момент отказа, ничего не надо вспоминать; (г) логгер **безопасен для основного потока** (`except OSError: pass`, кода возврата не меняет) — обходить его незачем; (д) **жёсткое поле вместо прозы**, поэтому поверх него позже встал разбор по классам. Отдельный приём оттуда же: **обход возможен, но стоит записи** — `--no-verify` не запрещён, но без названной причины отклоняется, и сам обход логируется.

**Источник 5 — честный отрицательный ответ, как §2.1 и разрешал.** Замер: греп по длинным цитатам в «ёлочках» даёт 2860 попаданий при точности **3 из 20** на случайной выборке и полноте **568 из 1008** адресов корпуса. Причина не в регулярке: 68 % источника — журналы арок, где речь владельца записана **пересказом в жирных буллетах, машинно неотличимым от собственного текста Claude**. Чем заменить: ритуал на приёмке с жёстким полем (образец в репозитории уже работает — `^ЦЕНА:` в уроках даёт 0 промахов из 243) и сторожем из трёх правил: пустая секция при непустом входе → красный; `ЦИТАТА:` не является дословной подстрокой названного источника → красный; адрес не резолвится в `файл:строку` → красный.

**Источник 1 — предпосылка захода не подтвердилась, и это меняет, что чинить.** §2.3 утверждал: «выгрузка режет реплику на ~300 знаках, от реплик в 1389/825/692 слова остаётся два предложения». Замер по двум живым выгрузкам этой арки: **обрезанных реплик — 0**, самая длинная лежит целиком (1087 слов), суммарно 10344 и 11446 слов. Обрезка `[:300]` в коде есть, но только в двух производных секциях (`ОБЕЩАНИЯ АНАЛИТИКА`, `ВОПРОСЫ ВЛАДЕЛЬЦА`), и на живых данных в предел упёрлась 1 строка из 19. **Патч для `zakryt_sessiyu.py` не прилагаю: чинить нечего.** Настоящая потеря у источника 1 в другом звене — `dnevnik.py` берёт последний по mtime транскрипт, и в сессии исполнителя это его собственный: на арке с 49 разнесёнными репликами гейт печатает «покрыто 0 из 1». Оформлено пунктом очереди 3.

**Канал — §2.2.** Расширил `dostavit_urok.py`, второго инструмента не завёл. Понятие «наблюдение» — одно на два вида (`urok`, `punkt`), вид определяется по имени файла и в команде не спрашивается. **Обратная совместимость проверена дословно:** вывод `--plan` и `--gejt` на живом `UROKI-FABRIKE.md` совпал со старой версией из `git show HEAD:` побайтово (`diff` пуст), коды возврата те же (0 и 1).

🔴 **Разбор пункта построен не на номере, и это главное решение захода.** Замер по 37 файлам: строк `ДОСТАВЛЕНО: нет` вне цитат — 199, `priyomka.parse_ochered` (якорь — номер в начале строки) находит 127. **Теряется 72 из 199.** Три причины, все на живых файлах: жирный номер (`kod_etap-20-i-perenos.md` — 9 пунктов, 0 разобранных), секция `## ВТОРАЯ ПАЧКА ВОПРОСОВ` (18 пунктов, невидимых для поиска по точному имени), образец формы в blockquote-шапке (74 ложных строки). Якорь — **пара полей `ДОМ:` + `ДОСТАВЛЕНО:`**: номер тут пишут как придётся, а пару полей — всегда.

**`--volna` — §2.2, условие 2.** Отработал на живой папке арки (38 файлов), `rc=0`. Печатает по домам, у каждого дома — число, виды записей и **чем эту порцию закрывать**; «дома нет» и «решение за человеком» стоят отдельными группами.

**Г8 — §2.4, условие 7.** Краснеет по двум правилам, и оба чинятся действием: (а) в заходе есть недоставленное, у которого **дом есть на диске** — чинится одной командой; (б) **очередь захода перевалила порог 12**. Порог снят с корпуса: медиана недоставленных по 37 заходам — 4, среднее 4.2, p75 = 5, p90 = 8, максимум 24; плотная часть кончается на 9, дальше в корпусе только 11 и 24, и порог 12 стоит в этом разрыве. Прогон по всем 37 заходам: **красных 1, зелёных 36** — гейт не красный всегда. Порог 6 покрасил бы 7 заходов из 37, то есть каждый пятый честный отчёт.

🔴 **Г8 переделан после прогона по СВОЕМУ ЖЕ отчёту, и это стоило бы гейту жизни.** Первая редакция краснела ещё и по факту «дом есть, а не доставлено». Прогон приёмки на этом самом заходе показал цену: перенос пункта в дом делает аналитик НА приёмке, значит на первом прогоне пункты не перенесены по построению — и гейт краснеет на любом честном отчёте, где исполнитель назвал живой дом. Гейт, красный на каждой первой приёмке, отключают первым; §2.4 предупреждал ровно об этом. Осталось **одно красное правило — порог**, а доставимое печатается готовой командой: подсказка тому, кто как раз сейчас переносит, а не приговор отчёту. После правки приёмка по этому заходу: Г8 ✅, зелёных 8 из 9 (красный — Г3, известный угол про mtime артефакта, к каналу отношения не имеет).

**Миграция — §2.5, условия 3 и 5.** Проведена как живой прогон канала, двумя волнами (вторая — после того, как резолв научился понимать `<эта арка>/` и базу `_studio/zhurnal/`). **Доставлено 40 пунктов в 5 домов, отказов 0.** Пунктов с живым домом не осталось: 0.

**Дубли — условие 4.** **Было 156 недоставленных, уникальных находок 152.** Дословных дублей в очереди нет: максимальное сходство пары по словарю — 0.28 (мера Жаккара по словам длиннее трёх букв, все 8256 пар). Есть **содержательные** — одна находка, независимо описанная разными заходами; их три, и все три предъявлены поимённо в `ULOV-OCHEREDI.md §3` со **всеми адресами-источниками**:
- **Д1** «словом „лента“ названы два разных артефакта» — 2 адреса: `kod_nochnaya-sborka.md#4` · `kod_shvy-lenty-reserch.md#2`;
- **Д2** «голосовой корпус `texts-corpus.md` живёт только в чужом скилле» — 2 адреса: `kod_nochnaya-sborka.md#5` · `kod_shvy-lenty-reserch.md#3`;
- **Д3** «дисциплина заходов существует в пяти копиях» — **3 адреса**: `kod_nochnaya-sborka.md#3` · `kod_shvy-lenty-reserch.md#4` · `kod_vynos-meta.md#4`.

Ни один адрес не выброшен: находка, всплывшая трижды, — свидетельство, что она болит. Отдельно назван **не-дубль с общим корнем**: три записи про отказ `register_doc.py` (`kod_gejt-vhozhdeniya.md#3`, `kod_nepokrytoe.md#1`, `kod_dva-vhoda.md#5`) описывают три РАЗНЫХ отказавших пути — схлопывать нельзя, чинится одним заходом.

**Третий вид записи — §2.3: НЕ завожу, и вот число.** Заход разрешал отказ. Проверка по 37 заходам: строк с маркерами замечания («пришлось переделать», «работало не так», «мешало», «споткнулся») в прозе `## ОТЧЁТ` — в 4 заходах, и **у всех четырёх есть оформленные пункты очереди** (заходов с замечанием в прозе, но без единого пункта — 0). То есть класс «замечание, умирающее в прозе отчёта» на этом корпусе не подтверждается: там, где исполнителю было что сказать, он оформлял пунктом. Порог у пункта уже тот, что требовал §2.3 («адресат», не «цена»), — это поле `ДОМ:`. Заводить четвёртый вид ради секции, которую никто не заполнит, дороже, чем сказать «нет». Оговорка: маркерный поиск грубый, его точность я не мерил — вывод держится на том, что заходов **без** пунктов среди них ноль.

### 2. Как проверил

| проверка | результат |
|---|---|
| обратная совместимость двери уроков | `diff` вывода старой и новой версии на живом файле — **пуст**, оба режима, коды 0 и 1 |
| фикстура канала `fixtures/nablyudenia/` | **15 ловушек, ЗЕЛЁНЫЕ, rc=0** |
| фикстура умеет падать | **4 мутации**: снятие всех трёх защит от шапки → ловушка 3 + ещё 7 красных; точное имя секции → ловушка 2; якорь-номер → ловушка 1; снятие класса «дом — код» → ловушки 13/13а/13б (скрипт «тронут», Python не парсится). Откат после каждой — `cmp` байт-в-байт |
| соседние фикстуры `urok`, `priyomka` | rc=0 обе |
| `check_tool_contract.py` на обоих правленых инструментах | ✅ 2 из 2 |
| `check_uroki.py` на доме уроков после миграции | rc=0 |
| удалений в домах | **0** во всех пяти (`diff --numstat`) |
| правки в источниках `kod_*.md` | 40 добавлено / 40 удалено — **только строки `ДОСТАВЛЕНО:`**, замены 1↔1; посторонних изменённых строк 0 (проверено фильтром по `diff -U0`) |
| Г7 по всем 37 заходам | было 12 ложно-красных → стало **1, и он честный** (см. вопрос 1) |
| Г8 по всем 37 заходам | 1 красный (заход с 24 пунктами), 36 зелёных |

**Коды возврата гейта, дословно (условие 3).**
До миграции, живые файлы арки, не фикстура:
```
--gejt _studio/zhurnal/2026-08-05_faza-lenty              rc=1   «86 из 247 наблюдений»
--gejt .../kod_rasshivka-kornej.md                        rc=1   4 пункта с живым домом
```
После миграции, те же команды:
```
--gejt .../kod_rasshivka-kornej.md                        rc=0   «Пунктов с живым домом, ждущих доставки, нет»
--gejt .../kod_hvosty.md            (типичный заход)      rc=0
--gejt .../kod_nochnaya-sborka.md   (24 пункта > порога)  rc=1
--gejt --zakrytie .../kod_hvosty.md (стадия закрытия)     rc=1
--gejt _studio/zhurnal/2026-08-05_faza-lenty              rc=1   «43 из 247» — это УРОКИ
```
🔴 **Гейт по арке остался красным честно, и вот почему.** Оставшиеся 43 недоставленных с живым домом — все до одного **уроки** `UROKI-FABRIKE.md`, а не пункты очереди. Уроки разносит соседний канал, этот заход их не касался: §2.5 требовал провести через канал очередь. Пунктов с живым домом — 0, и на уровне файла-захода гейт ответ поменял (`rc=1` → `rc=0`).

### 2а. Верификатор (§3) — свежий субагент, метод обратный

**Вердикт: список потерь ПУСТ — 0 из 156.** Множество собрано ЗАНОВО, своим разбором по `git show <до>:` всех 37 файлов, и сошлось со 156 независимо от моего. Состояния: доставлено 40 · названо в `ULOV-OCHEREDI.md` 116 (100 «владелец» + 16 по классам) · дублем объявлено 7 адресов → 3 находки (все внутри 100, адреса сохранены) · **потеряно 0**; 40 + 116 = 156, пересечений нет. Классы §4 (6/6/2/1/1 = 16) пересчитаны им поимённо и сошлись. Дословность проверена не на пяти, а **на всех 40**: 39 побайтово, 1 расхождение — лишняя строка, см. ниже. Удалений во всех пяти домах 0, префикс каждого дома побайтово совпадает с версией «до» — дописано строго в хвост. По 37 источникам: **ни одной изменённой строки без `ДОСТАВЛЕНО:`**, баланс 1↔1.

🔴 **Он нашёл ДВА дефекта, которых не видел ни один гейт, и оба исправлены.**

**(1) Порча, невидимая всем машинным проверкам.** Доставка `gejt-vhozhdeniya#1` унесла в дом ОТКРЫВАЮЩУЮ ```-ограду без закрывающей: в источнике одна ограда обнимает сразу три пункта (строки 228–248), доставлялся один. В `UROKI-FABRIKE.md` ограда стала нечётной (5), и **22 заголовка вместе со всем хвостом файла в любом просмотрщике превратились в код**. При этом байты целы, `diff --numstat` показывал ноль удалений, `check_uroki.py` был зелёным, обе фикстуры зелёными. Починено дважды: в канале (`сбалансировать_ограду()` — ведущая чужая ограда отбрасывается, незакрытая своя закрывается) и в самом файле (осиротевшая строка удалена, чётность 4, содержание и метка на месте, `diff` даёт 0 добавлений / 1 удаление). Ловушка 16 фикстуры сторожит, проверена мутацией: снятие баланса → «ограды НЕ чётные». Чётность оград проверена во всех пяти домах: 4 · 0 · 0 · 2 · 10 — везде чётная.

**(2) Ложная тревога моего же инструмента про «коллизию номеров».** Разобрано в вопросе 2: файл здоров, сломан был fallback разбора. Починено, записей с неуникальным номером 0 из 182 (команда — в `ULOV-OCHEREDI.md §5.3`). Это ровно тот случай, ради которого верификатор и нужен: **оба моих замера шли одним и тем же разбором и потому подтверждали друг друга**.

**Что он поправил в числах улова:** объяснение «2 ложные строки на каждый файл» неточно — 234 = 156 записей + 72 цитаты + 6 строк прозы (в одном файле шапки-подсказки нет вовсе); команда `--volna | tail -10` печатала арку целиком, а не таблицу §4 (исправлено до его отчёта — теперь под таблицей стоит команда, дающая ровно её строки); ссылка «состав тем воспроизводится той же командой» в §5.2 не даёт привязки пункт→тема — она хранится только в разборе группировки. Остальные числа сошлись: 156, 127, 40, numstat, 6/6/2/1/1, сумма тем 100, медиана 4 / p90 8 / максимум 24 / порог 12, все 10 адресов §3 и все 5 адресов §5.3 резолвятся в живые пункты.

**Его слепые зоны, названные им самим:** смысл адреса `ДОМ:` (тот ли это дом по существу) не судил; привязку 100 пунктов к 11 темам и классы А/Б/В не проверял — это суждение, файлами не проверяемое; находка, оставленная прозой в `## ОТЧЁТ` без полей `ДОМ:`/`ДОСТАВЛЕНО:`, невидима обоим разборам и в 156 не входила; уроки `UROKI-FABRIKE.md` не сверял.

### 3. Что НЕ трогал

- `bootstrap_zahod.py`, `zakryt_sessiyu.py` — READ-ONLY по контракту. Патчей не прилагаю ни к одному: для `zakryt_sessiyu.py` чинить нечего (предпосылка про обрезку ложна, замер выше), для `bootstrap_zahod.py` правка нужна косметическая (образец формы в шапке `## ВОПРОСЫ` даёт 74 ложных строки), но её якорь — в файле, который правит соседний заход; оформил пунктом очереди 5.
- `dnevnik.py`, `check_uroki.py`, `git_zona.py`, `korni.py` — вне зоны, только читал.
- Содержание находок не разбирал и на вопросы владельца не отвечал: 100 пунктов `ДОМ: владелец` доведены и сгруппированы, ответов за него нет.
- Файлы-заходы `kod_*.md` — изменены **только строки `ДОСТАВЛЕНО:`**, как разрешает контракт зоны.
- Ветку не вливал.

### 4. Выход за объявленную зону — назван отдельно

🔴 **Пять файлов-домов не перечислены в контракте зоны, но уехали в мой коммит.** Это `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md`, `_studio/konvejer/06-tekst/UROKI-FABRIKE.md`, `_studio/konvejer/GEJTY.md`, `_studio/konvejer/06-tekst/FORMA.md`, `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`. Причина: §2.5 предписывает доставку в дома, а дома выясняются только прогоном `--volna`; исполнить §2.5, не выйдя за перечисленные четыре пути, нельзя. Не закоммитить их было бы хуже всего: метки в источниках уехали бы без содержания — ровно тот ложно-зелёный Г7, который инструмент и сторожит. Оформлено уроком фабрике с ценой.

### 5. Необратимое

- **`_generator/tools/git_zona.py` был сломан и восстановлен.** Доставка урока 6 дописала markdown в конец файла (`SyntaxError`), команда при этом вернула 0. Восстановлено `git --no-optional-locks checkout HEAD -- _generator/tools/git_zona.py`, проверено `ast.parse` → валиден, `diff --stat` против HEAD пуст. Файл в коммит **не входит**. Дыра закрыта классом «дом — исполняемый код» (`rc=2`) и ловушкой 13 фикстуры.
- **27 доставок откачены и переделаны.** Первая волна легла с заголовком `### `, отчего `check_uroki.py` счёл пункты уроками без цены и покраснел. Откат — `git checkout HEAD --` по домам и источникам (проверено: `diff --numstat` дал 0 строк), формат заголовка сменён на `####`, доставка повторена. Восстанавливается из `HEAD` на момент отката — коммит `9b69e28`.
- Больше необратимого нет: доставка только дописывает в конец файла (удалений 0 во всех домах), в источниках заменяются ровно строки `ДОСТАВЛЕНО:`.

### 6. Открытое — куда возвращаться

- **43 урока с живым домом ждут доставки** — соседний канал, не этот заход: `dostavit_urok.py --plan <арка>/UROKI-FABRIKE.md`.
- **15 уроков и 1 пункт адресованы в `.py`** — доставка теперь отказывает; переносить в докстринги руками, это содержательная работа.
- **Пять отсутствующих файлов держат ~20 пунктов очереди к владельцу** (`ULOV-OCHEREDI.md` §5.1) — самая дешёвая позиция улова: завести дома, и очередь худеет без единого содержательного решения.
- **Уникальность адреса `<заход>#<N>`** не гарантирована (вопрос 2): пока не перенумеруют `kod_nochnaya-sborka.md`, шесть находок недоступны для доставки.

## ПРИЁМКА — 2026-08-07, аналитик

**ПРИНЯТО.** Гейты 8 из 9 (Г8 — его собственный, зелёный); красный Г3 — артефакт в рабочей папке, известный угол.

**Проверено моим прогоном:** `--volna` на живой папке арки печатает итог по классам — дом есть 43 · дома нет 14 · дом-папка 6 · дом-указатель 2 · **дом — исполняемый код 15** · решение за человеком 100. У каждого класса стоит, чем его закрывать. Это ровно та еженедельная волна, которую заказывал владелец.

### Три вещи, где исполнитель оказался прав против захода

**1. Моё число «123» считал сломанный разбор.** Настоящих строк `ДОСТАВЛЕНО: нет` — **199**, штатный `parse_ochered` находит 127, теряет 72. Якорь у него — номер в начале строки, а номер пишут как придётся: жирным, в секции с нестандартным именем, в образце шапки. **Треть очереди была невидима гейту приёмки всё время, пока он её печатал.** Якорь переставлен на пару полей `ДОМ:` + `ДОСТАВЛЕНО:` — её пишут всегда. Урок 69.

**2. Предпосылка про обрезку в 300 знаков ложна.** Обрезанных реплик — **0**; самая длинная лежит целиком (1087 слов). Я увидел `[:300]` в коде и объявил потерю, не прогнав. Патч не нужен. Настоящая потеря нашлась в другом звене: `dnevnik.py` берёт последний по mtime транскрипт, и в сессии исполнителя это его собственный — на арке с 49 разнесёнными репликами гейт печатает «покрыто 0 из 1». Её бы не нашли, пока чинили выдуманную. Урок 71.

**3. Третий вид записи не заведён — и это правильный ответ.** Замер: заходов, где в прозе отчёта есть маркеры замечания, — 4, и **у всех четырёх есть оформленные пункты очереди**; заходов с замечанием в прозе и без единого пункта — 0. Класс «замечание, умирающее в прозе» на этом корпусе не подтверждается. Отказ заводить сущность стоил числа, а не мнения.

### Что сделано хорошо и стоит запомнить как приём

**Г8 переделан после прогона по своему же отчёту.** Первая редакция краснела по факту «дом есть, а не доставлено» — а перенос делает аналитик НА приёмке, значит на первом прогоне гейт красен на любом честном отчёте. Гейт, красный на каждой первой приёмке, отключают первым. Осталось одно красное правило — порог 12, снятый с корпуса: медиана 4, p90 = 8, максимум 24, плотная часть кончается на 9, порог стоит в разрыве. Прогон по 37 заходам: **1 красный, 36 зелёных**.

**Разбор источника 4 — лучший кусок отчёта.** Автолог git не теряет по пяти переносимым причинам: единственная дверь · пишет инструмент, а не агент · запись берётся из строки, которая и так печатается человеку · логгер безопасен для основного потока, обходить незачем · жёсткое поле вместо прозы. Плюс приём «обход возможен, но стоит записи». Это и есть ответ на вопрос владельца «как сделать, чтобы не терялось».

**Источник 5 — честный отрицательный ответ с числом:** точность грепа 3 из 20, полнота 568 из 1008. Причина не в регулярке — 68 % источника это журналы арок, где речь владельца записана пересказом в жирных буллетах, машинно неотличимым от текста Claude. Предложена замена: ритуал со сторожем из трёх правил.

### Находка верификатора, которой не видел ни один гейт

Доставка унесла **открывающую ```-ограду без закрывающей**: в источнике одна ограда обнимала три пункта, доставлялся один. В доме уроков ограда стала нечётной — **22 заголовка и весь хвост файла превратились в код в любом просмотрщике**. Байты целы, `diff --numstat` ноль удалений, `check_uroki.py` зелёный, фикстуры зелёные. Починено дважды: в канале и в самом файле; чётность проверена во всех пяти домах. Урок 70: **`diff --numstat` меряет потери, а не целостность**, и мы это путали.

Второе от верификатора — важнее частного случая: оба замера исполнителя шли одним разбором и потому подтверждали друг друга. Расхождение поймал только другой разбор.

### Открытое

Гейт по арке остался красным честно: 43 недоставленных — это **уроки**, их разносит соседний канал, заход их не касался. Пунктов с живым домом — 0.

**Уроки арки: 68 → 71.**
