# Канал исполнителя — nepokrytoe (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — работа целиком в суждении: чтобы сказать «вот этого у нас нет и вот чего будет стоить это добрать», надо понять оба текста, а не сопоставить строки.

🔴 **ТЫ РАБОТАЕШЬ В РЕПОЗИТОРИИ `disciplina`.** Инструменты лежат в `materials/_generator/tools/` и наводятся `GIT_ZONA_REPO` — форма в блоке «МЕСТО РАБОТЫ». Голым путём не зови: `_generator/` в `disciplina` нет (урок 53).

КОНТЕКСТ. Владелец строит плагин из семи скиллов взамен россыпи внешних. Гейт вхождения 06.08 проверил восемь внешних кандидатов на вопрос «доехало ли их уникальное содержание в наши скиллы»: **390 утверждений, проверено 390 из 390, непокрытыми остались 72**. Вывод гейта был — гасить нельзя ни одного.

**07.08 владелец погасил два из них сознательно: `html-slides-studio` (14 непокрытых) и `popsci-research` (9).** Это его решение и оно принято. Следствие: их содержание больше не работает, а живёт только в двух местах — в перечне гейта и в побайтовой копии `arhiv-skillov/`.

**И перечень гейта не годится как рабочий документ.** Он устроен как отчёт о проверке — по кандидатам, с вердиктами. Владельцу сегодня вечером нужно другое: сесть за скилл `slajdy` и увидеть, **чего этому скиллу не хватает**, откуда это взять и сколько это стоит. Перечень по кандидатам на такой вопрос не отвечает: непокрытое одного скилла размазано по трём кандидатам.

ЦЕЛЬ: перевернуть 72 непокрытых с оси «кандидат» на ось **«наш скилл»** и превратить каждое в работу, которую можно начать не разбираясь.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **Ты НЕ переносишь содержание и НЕ правишь ни одного `SKILL.md`.** Ты пишешь **карту недостающего**: что отсутствует, где лежит исходный текст, зачем он нужен и во что обойдётся перенос. Перенос — отдельная работа, её владелец делает на живой лекции. Начнёшь переносить — получится плохая копия вместо хорошей карты.

🔴 **«Не покрыто» ≠ «надо добрать».** Часть непокрытого — законно чужое: другой предмет, другой вкус, устаревший приём. **Такой вердикт для нас ценнее», чем перенос**, потому что снимает работу навсегда. Пиши его прямо и с доводом.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **`~/Documents/GitHub/disciplina-wt/wt-nepokrytoe`** — worktree на ветке `zahod/nepokrytoe` **репозитория `disciplina`**. 🔴 **Заводишь САМ, первым ходом:**
  ```
  cd ~/Documents/GitHub/disciplina
  GIT_ZONA_REPO=~/Documents/GitHub/disciplina \
    python3 ~/Documents/GitHub/materials/_generator/tools/git_zona.py worktree add wt-nepokrytoe --branch zahod/nepokrytoe
  cd ~/Documents/GitHub/disciplina-wt/wt-nepokrytoe
  ```
  Отказала — **СТОП и в `## ВОПРОСЫ`**, руками `git worktree add` не делай.
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `skills/slajdy/NEPOKRYTOE.md` `skills/reserch-cikl/NEPOKRYTOE.md` `skills/disciplina-arka/NEPOKRYTOE.md` `skills/disciplina-zahody/NEPOKRYTOE.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
  🔴 **ДОПИСАНО АНАЛИТИКОМ по ходу, 07.08 — зона расширена на §2.5:** `skills/slajdy/references/dok-07-verstka.md` (и другие файлы `skills/*/`, если §2.5 найдёт в них тот же дефект) — **только правка битого адреса, одной строкой на файл**. Ничего другого в этих файлах не меняешь.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/nepokrytoe`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката не нужна: ты только заводишь четыре новых файла, существующего не меняешь.
- **Прочитать ТОЛЬКО это:**
  1. `GEJT-VHOZHDENIYA.md` — **целиком.** Это твой источник: итоговая таблица, восемь разделов по кандидатам, таблица «где сосредоточено непокрытое» и раздел верификаторов. 417 строк.
  2. `arhiv-skillov/README.md` — зачем архив и что в нём.
  3. `arhiv-skillov/<кандидат>/` — **исходные тексты погашенных и остальных кандидатов.** Ходи туда адресно, за конкретным непокрытым, а не читай подряд: в одном `html-slides-studio` 125 файлов и 7.7 МБ.
  4. `skills/slajdy/SKILL.md`, `skills/reserch-cikl/SKILL.md`, `skills/disciplina-arka/SKILL.md`, `skills/disciplina-zahody/SKILL.md` — **шапку и оглавление**, чтобы понимать, куда именно недостающее встанет. Тела читай выборочно.
  ⚠ `skills/*/DOLG.md` может появиться под руками — его заводит параллельный заход в `materials`. **Не трогай его ни в каком виде**, у тебя свой файл.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### 2.1 Разнести 72 по домам — сначала счёт, потом текст

Гейт группирует по кандидатам. Дома известны из его же итоговой таблицы:

| кандидат | непокрыто | наш дом |
|---|---|---|
| `html-slides-studio` 🔴 погашен | 14 | `slajdy` |
| `content-studio` | 2 | `slajdy` |
| `the-interviewer` | 1 | `disciplina-zahody` + `slajdy` |
| `popsci-research` 🔴 погашен | 9 | `reserch-cikl` |
| `popsci-narrative` | 26 | `reserch-cikl` |
| `math-russian-terminology` | 4 | `reserch-cikl` |
| `session-handoff` | 8 | `disciplina-arka` |
| `agentic-coding-session-brief` | 8 | `disciplina-zahody` |

🔴 **Числа выше — из гейта, а не мои. Пересчитай их своим прогоном первым делом** и назови расхождение, если оно есть. Сумма обязана дать 72; не дала — это находка, а не повод подогнать.

Дальше — четыре файла `skills/<дом>/NEPOKRYTOE.md`. Одна запись на одно непокрытое утверждение, **пять полей**:

```
### <что отсутствует — своими словами, одной строкой>
ОТКУДА: <кандидат> · <путь в arhiv-skillov, где лежит исходный текст>
ЗАЧЕМ: <что сломается или чего не будет, если не добрать — конкретно, не «улучшит качество»>
ВЕС: <мелкий | средний | крупный> — <почему именно такой>
ВЕРДИКТ: <добрать | добрать позже | НЕ добирать> — <довод>
```

**Поле `ОТКУДА` — самое важное и единственное машинно проверяемое.** Путь обязан существовать: `test -e` проверяется на каждом. Утверждение, чей исходный текст не находится в архиве, помечается явно и **считается отдельно** — это значит, что содержание не сохранено нигде, и владелец должен знать про такие немедленно.

### 2.2 Приоритет — два погашенных скилла

`html-slides-studio` (14) и `popsci-research` (9) погашены 07.08. Их 23 записи разбери **первыми и подробнее остальных**: у них есть срок — владелец садится за `slajdy` сегодня вечером.

Для этих 23 добавь шестое поле:

```
С ЧЕГО НАЧАТЬ: <первое действие, которое можно сделать, не разбираясь заново>
```

Не план и не оценка сроков — одно конкретное первое действие. Например: «прочитать `arhiv-skillov/html-slides-studio/references/typography.md` §2 и сверить с нашим `references/typography.md` — там расходится обработка вдовы и висячей строки».

### 2.3 🔴 Вердикт «НЕ добирать» — полноправный и желанный исход

Гейт вхождения считал непокрытым всё, что не нашлось дословно. Часть этого не нужна:
- приём под другой предмет (лендинг вместо лекции);
- вкусовое решение, с которым владелец не согласен;
- механизм, который у нас решён иначе и лучше.

Такой вердикт **снимает работу навсегда** и потому ценнее переноса. Но он обязан нести **довод из текста**, а не «кажется лишним»: назови, что именно в нашем скилле уже решает эту задачу, или чем предмет отличается.

⚠ **Соблазн, который назову прямо:** написать «НЕ добирать» на всём тяжёлом и получить короткий красивый файл. Это испортит ровно ту базу, ради которой заход существует. Держи долю: если «НЕ добирать» вышло больше трети, перепроверь и скажи в отчёте, почему их столько.

### 2.4 Шапка каждого файла

Три строки по канону (`КОГДА читать` / `ЧТО дописывать` / `КУДА дальше`) плюс **дата данных** и **счёт**: сколько записей, сколько «добрать», сколько «НЕ добирать», сколько без исходника в архиве.

🔴 Числа в шапке пиши **командой, которая их считает**, а не результатом (`KONSTITUCIYA §10`): файл будет жить и дописываться, а вписанное руками число протухнет молча.

Четыре новых `.md` — регистрируй дверью:
```
GIT_ZONA_REPO=~/Documents/GitHub/disciplina \
  python3 ~/Documents/GitHub/materials/_generator/tools/register_doc.py <путь> "<описание>"
```
⚠ **Дверь для `disciplina` из worktree СЕГОДНЯ НЕ РАБОТАЕТ** — два дефекта, воспроизведены 07.08 (урок 59), чинит их параллельный заход. Отказала — **не обходи, не пиши в `README.md` руками**: назови в `## ВОПРОСЫ` и оставь файлы незарегистрированными. Это ожидаемый исход, а не твой промах.

### 2.5 🔴 ДОПИСАНО АНАЛИТИКОМ по ходу — битые адреса в погашенные скиллы

**Повод.** 07.08 владелец погасил `html-slides-studio` и `popsci-research`. Приёмка сразу после этого нашла в `skills/slajdy/references/dok-07-verstka.md:68` живой адрес `html-slides-studio/references/…` — папки с таким именем у нас нет, и читатель по ней не попадает никуда. Соседний `dok-08-sceny.md` был поправлен 06.08, а этот пропустили.

Это записанный урок **32**: гейт вхождения **односторонний** — он проверяет, что доехало ОТ кандидата К нам, и не смотрит, что осталось ОТ нас К кандидату. Пока гасить было нечего, цена была нулевой. Теперь она не нулевая: файл читается скиллом по контракту, и владелец садится за вёрстку сегодня.

**Что сделать — два хода.**

**Ход 1, обязательный: прочесать и предъявить.** Найди все адреса из наших скиллов наружу — в любой из восьми кандидатов гейта, не только в два погашенных. Отдели три разных вещи, их легко спутать:

| вид упоминания | пример | что с ним делать |
|---|---|---|
| **провенанс** | «перенесено из `popsci-research` 07.08», `RASHOZHDENIYA.md` | **не трогать.** Это объяснение, откуда правило; оно ценнее чистоты списка (решение владельца 07.08 п.8) |
| **адрес в архив** | `arhiv-skillov/popsci-research/SKILL.md` | **не трогать**, путь живой и проверяемый |
| **живой адрес наружу** | `html-slides-studio/references/style-core.md` | **чинить**: заменить на наш путь, если содержание доехало; на путь в `arhiv-skillov/`, если нет |

Результат хода 1 — таблица в отчёте: файл · строка · вид · вердикт. **Числа приведи по каждому виду**, иначе «нашёл 30 упоминаний» ничего не значит.

**Ход 2, точечный: починить только третий вид.** Одна строка на файл, ровно адрес. Дословный текст рядом не трогай — часть этих файлов объявлена побайтовой копией из монорепо, и правка сверх адреса эту копию ломает. `dok-08-sceny.md` уже поправлен 06.08 — посмотри, **как именно там оформлена правка** (строка `⚠ Адрес переставлен 2026-08-06: стояло …`), и сделай так же: замена + видимая пометка, что и когда переставлено.

⚠ **Не нашёл ничего, кроме `dok-07-verstka.md` — законный и хороший исход.** Скажи это с охватом: «проверено N файлов из N».

⚠ **Сомневаешься, куда должен вести адрес** — не угадывай: почини на путь в `arhiv-skillov/` (он точно существует) и назови сомнение в отчёте. Адрес в архив хуже правильного, но лучше несуществующего.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Шесть условий:

1. **Сумма разнесённого = 72**, и это показано числом по каждому из четырёх файлов плюс итог. Разошлось с гейтом — расхождение названо, а не сглажено.
2. **`ОТКУДА` проверен ЖИВЫМ ПРОГОНОМ на реальных файлах архива:** `test -e` по каждому пути, оба числа (сколько существует из скольких). Ненайденные — списком, поимённо.
3. **23 записи двух погашенных скиллов несут `С ЧЕГО НАЧАТЬ`**, все 23 из 23.
4. **Пять полей заполнены у каждой записи** — ни одного пустого и ни одной заглушки. Проверяется командой, число дословно.
5. **Доля «НЕ добирать»** названа числом и долей; больше трети — объяснено.
6. Четыре файла заведены; регистрация прошла **или** отказ двери назван в `## ВОПРОСЫ`.
7. 🔴 **(§2.5, дописано аналитиком) Прочёс проведён с охватом:** сколько файлов проверено из скольких, и три числа по видам упоминаний. Живые адреса наружу — **починены все или названо, почему нет**. Проверка фактом: после правки ни один адрес третьего вида не остаётся, и это показано той же командой, что искала.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

⚠ **Условие 3 — то, что нужно владельцу сегодня.** Провались всё кроме него — заход окупился: вечером можно садиться и работать. Пройди всё кроме него — база есть, а начать по ней нельзя.

## 3. ВЕРИФИКАТОР

Нужен, и метод обратный твоему. Ты идёшь от гейта к архиву — верификатор идёт **от архива к записи**: берёт **5 случайных записей** и на каждой отвечает на два вопроса, читая исходный текст по адресу `ОТКУДА`:

1. **правда ли, что этого у нас нет** — то есть не выдумано ли отсутствие (проверяется чтением нашего `SKILL.md` и `references/`);
2. **выдерживает ли вердикт** — особенно «НЕ добирать»: не отброшено ли нужное ради краткости.

Свежий субагент, без твоего плана и без этого захода. Результат — 5 из 5 либо список расхождений. Расхождение → **чини запись, а не вердикт верификатора**.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `skills/slajdy/NEPOKRYTOE.md` `skills/reserch-cikl/NEPOKRYTOE.md` `skills/disciplina-arka/NEPOKRYTOE.md` `skills/disciplina-zahody/NEPOKRYTOE.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `skills/slajdy/NEPOKRYTOE.md` `skills/reserch-cikl/NEPOKRYTOE.md` `skills/disciplina-arka/NEPOKRYTOE.md` `skills/disciplina-zahody/NEPOKRYTOE.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Заход собран по документу-отчёту, состояние которого не сверено с git-логом того же репозитория — и разошёлся с ним дважды в одном задании
Оба раза источником была не ложь, а **возраст снимка**: аналитик читал документ и приёмочную находку, но не спрашивал git, что случилось после них.
- Гейт вхождения (`a9137c0`, 18:22) объявил 72 непокрытых. Перенос этих же 72 лёг через **2 часа 16 минут** (`0c797ac`, 20:38) и был влит в `main` (`26b5ef6`, 21:18). Заход, написанный на следующий день, предъявил перечень гейта как актуальное состояние — «содержание живёт только в перечне гейта и в архиве».
- §2.5 дописан по находке приёмки «в `dok-07-verstka.md:68` остался живой адрес». Этот адрес был починен коммитом `d4848b1` того же 06.08, то есть **до** написания §2.5.

ЦЕНА: первая треть прогона ушла не на задачу, а на доказательство, что предпосылка ложна (git-лог, шесть файлов переноса целиком, сверка 72 позиций с живыми файлами вместо архива). Форму задания пришлось расширять на ходу — вводить четвёртое значение вердикта, которого в задании нет, иначе 63 записи из 72 отвечали бы «возьми из архива» на то, что уже перенесено, и владелец вечером переносил бы перенесённое по срочному. По §2.5 ход 2 на названном файле оказался пустым; три реальных живых адреса нашлись побочно, в другом файле (`plakatnyj-stil.md`), то есть находка получена не благодаря указанному адресу, а вопреки ему. Дешёвая профилактика — одна команда `git log --format='%h %ad %s' -- <файл-источник>` при сборке захода: она печатает, что случилось с источником после того, как его прочитали.

### Охват «выписано N, проверено N из N» не объявляет ЕДИНИЦУ счёта, и в одном документе их оказалось две
`GEJT-VHOZHDENIYA.md` считает непокрытое по-разному в разных разделах: у `popsci-narrative` итог 26 совпадает с числом номеров утверждений, у `popsci-research` итог 9 — это число ПУНКТОВ перечня, а номеров в них десять (один пункт несёт «№ 73, № 75» сразу). По номерам утверждений сумма даёт 73, по пунктам — 72.

ЦЕНА: критерий готовности требовал «сумма обязана дать 72», и честный пересчёт по номерам его проваливал — не по ошибке исполнителя, а потому что критерий унаследовал необъявленную единицу. Ушёл отдельный прогон на установление того, какая единица авторитетна, и запись в карту пришлось делать по пунктам, оговорив это вслух в двух местах. Формула охвата обязана называть единицу: «выписано 49 **утверждений**, проверено 49 из 49, непокрыто 26 **утверждений**» — иначе следующий заход, которому дадут это число как вход, воспроизведёт ту же развилку.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 🔴 Предпосылка захода ложна — оспариваю ДО работы (§1 «оспорить ложную предпосылку, включая КРИТЕРИЙ ГОТОВНОСТИ»)

Заход говорит: содержание 72 непокрытых «больше не работает, а живёт только в двух местах — в перечне гейта и в побайтовой копии `arhiv-skillov/`». **На живом репозитории это не так.** Проверено git-логом, не предположено:

```
git log --format='%h %ad %s' --date=iso   →
  a9137c0  2026-08-06 18:22  гейт вхождения: снята двусмысленность охвата у кандидата 3   ← ГЕЙТ лёг здесь
  0c797ac  2026-08-06 20:38  этап 20 вторая половина + перенос непокрытого: семь скиллов   ← и через 2ч16м ПЕРЕНЕСЁН
  26b5ef6  2026-08-06 21:18  Merge branch 'zahod/etap-20-i-perenos' into main              ← и влит в main
```

Тело сообщения `0c797ac` дословно: *«Перенос 72 позиций гейта вхождения, у каждой вердикт (пустых клеток ноль): перенесено 68, осознанно не берём 3 (оценка 0-100, порог 70, интервью вне конвейера — решение владельца), требует решения владельца 3 (в `DYRY.md`)»* — и он же принёс материал: 42 эталона, 23 кадра, 10 контактных листов, 5 фонт-спеков, `evals.json`.

Физически на диске под этот перенос заведено пять новых файлов, названия ложатся ровно на непокрытые узлы гейта:

| файл | строк | закрывает узел гейта |
|---|---|---|
| `skills/disciplina-arka/references/gigiena-peredachi.md` | 112 | гигиена документа хэндоффа (кандидат 1) |
| `skills/reserch-cikl/references/kompoziciya-narrativa.md` | 503 | композиционная техника (кандидат 7) |
| `skills/reserch-cikl/references/gigiena-teksta.md` | 145 | двухуровневый материал + AI-эстетика (кандидат 6) |
| `skills/reserch-cikl/references/terminologiya-rabota.md` | 89 | четыре механизма терминологии (кандидат 8) |
| `skills/slajdy/references/archetipy-geometriya.md` | 81 | геометрия архетипов (кандидат 5) |
| `skills/slajdy/references/protokol-pravok.md` | 122 | нормы из `SKILL.md` кандидата 4 |

**Что из этого следует для задания.** Цель захода («перевернуть 72 с оси кандидата на ось нашего скилла и превратить в работу, которую можно начать не разбираясь») остаётся в силе и остаётся нужной: карты по домам не существует ни в каком виде, а перечень гейта устроен по кандидатам — тут заход прав. **Ломается ровно одно: вопрос «чего не хватает» у большинства позиций имеет ответ «уже есть, вот адрес», а не «взять из архива».** Писать по такой позиции «ОТКУДА: архив / ЗАЧЕМ: иначе сломается» — значит послать владельца переносить то, что перенесено, вечером и по срочному.

**Поправка, которую вношу (форму захода НЕ ломает).** Пять полей и шестое для 23 — как задано; счёт 72 — как задано. Меняется словарь поля `ВЕРДИКТ`: к трём значениям захода добавляется четвёртое, **`уже добрано`** — с адресом у нас и коммитом, которым добрано. Это сильнейшая форма того, что заход сам называет желанным исходом («снимает работу навсегда»): не «нам это не нужно», а «это сделано, вот где лежит». Поле `ОТКУДА` сохраняет смысл и машинную проверку для всех 72 без исключения: исходный текст в архиве проверяется `test -e` независимо от вердикта.

**Чего я НЕ делаю:** не верю сообщению коммита на слово. Ни одна позиция не получает `уже добрано` по факту существования файла — только по факту, что нужное содержание читается в нём глазами. Где перенос формален (имя правила есть, тела нет; процедура предписана, материал недостаточен) — вердикт остаётся `добрать`, и это ровно то, что владельцу нужно видеть вечером.

**Критерий готовности от этого не мягчает, а становится острее:** условие 1 (сумма 72) и условие 2 (`test -e` по `ОТКУДА`) не меняются; условие 5 (доля «НЕ добирать») считаю отдельно от доли «уже добрано», чтобы порог трети мерился по тому, что заход имел в виду.

### Шаги

1. ✅ Рабочая папка `disciplina-wt/wt-nepokrytoe`, ветка `zahod/nepokrytoe`.
2. ✅ `GEJT-VHOZHDENIYA.md` целиком; `arhiv-skillov/README.md`; инвентаризация архива и наших `skills/`.
3. Пересчёт 72 своим прогоном по разделам гейта — до текста (расхождение назвать, не подгонять).
4. Прочитать адресно: шапки/оглавления четырёх `SKILL.md`; шесть новых файлов переноса целиком; `RASHOZHDENIYA.md` и `DYRY.md` четырёх домов — они держат вердикты «не берём» и «требует решения владельца».
5. По каждой из 72 позиций: исходник в архиве → адрес у нас (или его отсутствие) → пять полей; для 23 позиций двух погашенных — шестое поле.
6. Четыре файла `skills/<дом>/NEPOKRYTOE.md`, шапка с числами командами (`KONSTITUCIYA §10`).
7. Живой прогон: `test -e` по каждому `ОТКУДА`; счёт полей; доли вердиктов.
8. Свежий субагент-верификатор, 5 случайных записей, метод от архива к записи (§3).
9. Регистрация дверью (ожидаемо откажет — урок 59, тогда в `## ВОПРОСЫ`), коммит зоны двумя ходами, отчёт.

## ВОПРОСЫ — (заполняет исполнитель)

1. **Дверь `register_doc.py` отказала на всех четырёх файлах — четыре файла остались НЕзарегистрированными.** Не обходил, в `README.md` руками не писал (§2.4). Причина видна из третьей попытки и она конкретнее урока 59: дверь судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`, `disciplina/`, а скиллы этого репозитория лежат в `skills/`. Три попытки, все с кодом возврата 0 и отказом в выводе: относительный путь при `GIT_ZONA_REPO=~/Documents/GitHub/disciplina` → «❌ Путь вне репозитория»; абсолютный путь → то же; `GIT_ZONA_REPO=<worktree>` → «❌ Не регистрирую: дверь судит только `_studio/`…». То есть для репозитория `disciplina` дверь не знает его раскладки вовсе — не «сломана в worktree», а не имеет правила на `skills/`.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. **Повод §2.5 устарел до того, как был записан: `dok-07-verstka.md:68` починен коммитом `d4848b1` (06.08), а не остался живым.** Ход 1 §2.5 всё равно окупился — три живых адреса наружу нашлись в другом файле, `plakatnyj-stil.md`. Урок про сборку заходов вынесен в `## УРОКИ ФАБРИКЕ` отдельным пунктом.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

3. **Формула охвата «выписано N, проверено N из N» не объявляет единицу счёта, и в `GEJT-VHOZHDENIYA.md` их две.** По номерам утверждений сумма непокрытого = 73, по пунктам перечня = 72. Расхождение — ровно один пункт (`popsci-research`, «№ 73, № 75 — те же два требования в форме чек-листа»). Гейт я не правил: он вне зоны.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

4. **Два дефекта в файлах ВНЕ зоны, найденные по ходу; не трогал.** (а) `skills/disciplina-arka/SKILL.md §6` — предложение «**Названный долг инструмента:** пока `sdelat_handoff.py` этих трёх вещей не печатает…» стоит **дважды подряд**, дословным дублем. (б) `skills/reserch-cikl/references/kompoziciya-narrativa.md:116` — адрес нашего чекпоинта владельцу назван неверно: «контракт возврата throughline» (B14) лежит в `SKILL.md:133`, в §«Шаг 1 · бриф», а не в §4a; в §4a чекпоинта владельцу нет. Там же: перечисление содержимого этого файла в `SKILL.md` (строки 284–289) называет все параграфы, кроме §2, — то есть на шаге 4a пошаговый обмен с владельцем не объявлен.
   ДОМ: skills/reserch-cikl/references/kompoziciya-narrativa.md · skills/disciplina-arka/SKILL.md
   ДОСТАВЛЕНО: нет

5. **Гейт занижает число контактных листов:** говорит «7 контактных листов», в архиве и у нас их **10**. Число внутри `GEJT-VHOZHDENIYA.md`, правка вне зоны.
   ДОМ: GEJT-VHOZHDENIYA.md
   ДОСТАВЛЕНО: нет

6. **Открытые вопросы владельцу, которые карты только предъявляют, а решить их может он один** (шесть штук, `grep -cE '^(ОТКУДА|ВЕРДИКТ|⚠).*ВОПРОС ВЛАДЕЛЬЦУ' skills/*/NEPOKRYTOE.md`): действует ли `karpathy-guidelines` как наша зависимость · распоряжаемся ли мы `frontend-slides` (кандидат объявлял, что его «можно удалить») · какой словарь архетипов ведущий (Р-04, два словаря под одним словом) · откуда брать полный список AI-оборотов, раз `humanizer` не сохранён нигде · заводить ли гейт документа передачи в pre-commit · куда адресовать под-дом «гигиена сессии» целиком (101 запись).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** четыре файла, все в worktree `zahod/nepokrytoe`. Первым открывать первый — владелец садится за `slajdy`:
- `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/wt-nepokrytoe/skills/slajdy/NEPOKRYTOE.md` — 16 записей, 163 строки
- `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/wt-nepokrytoe/skills/reserch-cikl/NEPOKRYTOE.md` — 39 записей, 287 строк
- `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/wt-nepokrytoe/skills/disciplina-arka/NEPOKRYTOE.md` — 8 записей, 97 строк
- `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/wt-nepokrytoe/skills/disciplina-zahody/NEPOKRYTOE.md` — 9 записей, 107 строк

Чем открывать — любой просмотрщик markdown или редактор. HTML не собирался: заход просил карту в `.md`, генератор здесь не при чём.

**КОММИТ:** `8d762ef` — «nepokrytoe: 72 позиции гейта разнесены по четырём домам + прочёс битых адресов» · `git_zona.py check` → ✅ «всё дерево: работа доехала в git, вне git ничего нет» · `git show --stat` → ровно 5 путей, все мои, чужого не подмело. Ветка НЕ влита (влитие делает приёмка).

### Что сделал и ЗАЧЕМ

**1. Оспорил предпосылку захода ДО работы** (§1, `## ПЛАН`). Заход утверждал, что содержание 72 непокрытых живёт только в перечне гейта и в архиве. По git-логу это неверно: гейт лёг в 18:22 (`a9137c0`), перенос этих же 72 позиций — в 20:38 того же дня (`0c797ac`), влит в `main` в 21:18 (`26b5ef6`). Физически перенос принёс шесть новых файлов (`gigiena-peredachi.md` 112 стр · `kompoziciya-narrativa.md` 503 · `gigiena-teksta.md` 145 · `terminologiya-rabota.md` 89 · `archetipy-geometriya.md` 81 · `protokol-pravok.md` 122) плюс материал: 42 эталона, 23 кадра, 10 контактных листов, 5 фонт-спеков, `evals.json`.

**Зачем это меняло задание.** Цель («перевернуть 72 с оси кандидата на ось нашего скилла») осталась верной — карты по домам не существовало. Сломался вопрос «чего не хватает»: у большинства позиций ответ «уже есть, вот адрес». Поэтому в словарь вердиктов добавлено четвёртое значение — **`уже добрано`**, с адресом у нас и коммитом, которым добрано. Форму захода это не ломает: пять полей и шестое для 23 — как задано, счёт 72 — как задано, `ОТКУДА` проверяем `test -e` у всех 72 независимо от вердикта.

**Чего я при этом НЕ делал:** не верил сообщению коммита на слово. Ни одна позиция не получила `уже добрано` по факту существования файла — только по прочитанному содержанию. Прочитаны целиком все шесть файлов переноса, соответствующие разделы четырёх `SKILL.md`, оба `DYRY.md` и оба `RASHOZHDENIYA.md`. Там, где перенос формален, вердикт остался рабочим: `evals.json` перенесён физически, но написан под чужой вход и поощряет запуск скилла без входа (`добрать`); норма снимка git-состояния доехала, автоматизация — нет (`добрать позже`); валидатор документа передачи есть прозой, носителя нет (`добрать`).

**2. Разнёс 72 по четырём домам.** slajdy 16 (`html-slides-studio` 14 + `content-studio` 2) · reserch-cikl 39 (`popsci-narrative` 26 + `popsci-research` 9 + `math-russian-terminology` 4) · disciplina-arka 8 · disciplina-zahody 9 (`agentic-coding-session-brief` 8 + `the-interviewer` 1). Единственное непокрытое `the-interviewer` гейт адресовал двум домам сразу — записал одним экземпляром в `disciplina-zahody` (предмет — дисциплина входа, не конвейер слайдов), в `slajdy` его нет намеренно, иначе счёт 72 сломался бы двойной записью; оговорено в шапке файла.

**3. Прочёс §2.5** — ход 1 и ход 2, ниже отдельным блоком.

### Как проверил — семь условий критерия, живым прогоном

| # | условие | результат |
|---|---|---|
| 1 | сумма разнесённого = 72 | ✅ 16 + 39 + 8 + 9 = **72**; сумма вердиктов тоже 72 (пустых нет) |
| 2 | `ОТКУДА` проверен `test -e` | ✅ **23 из 23** путей существуют, ненайденных нет |
| 3 | 23 записи погашенных несут `С ЧЕГО НАЧАТЬ` | ✅ `html-slides-studio` 14/14 + `popsci-research` 9/9 = **23 из 23**; сверх нормы дописал 6 в `session-handoff` |
| 4 | пять полей у каждой записи, ни одной заглушки | ✅ записей без обязательного поля — **0** из 72 (прогон по `ОТКУДА:`/`ЗАЧЕМ:`/`ВЕС:`/`ВЕРДИКТ:`) |
| 5 | доля «НЕ добирать» | ✅ **3 из 72 = 4,2 %**; треть — это 24, порог не достигнут, объяснять нечего |
| 6 | четыре файла заведены; регистрация или отказ в `## ВОПРОСЫ` | ✅ заведены · ❌ дверь отказала трижды, назван пункт 1 `## ВОПРОСЫ` |
| 7 | прочёс с охватом и три числа по видам | ✅ ниже |

**Расхождение с гейтом по счёту — есть, и оно не сглажено.** Сумма 72 держится на **единице «пункт перечня»**. По номерам утверждений сумма даёт **73**: у `popsci-research` гейт объявляет 9, а номеров десять (15, 16, 21, 45, 48, 49, 63, 71, 73, 75) — один пункт несёт «№ 73, № 75» сразу, назвав их «те же два требования в форме чек-листа». Записал одной записью, как в гейте, и оговорил вслух в самой карте. Второе, мелкое: гейт говорит «7 контактных листов», фактически их **10** (пункт 5 `## ВОПРОСЫ`).

**Вердикты числом:** `уже добрано` 63 · `добрать` 4 · `добрать позже` 2 · `НЕ добирать` 3. Без исходника в архиве — **1** (полный список AI-оборотов жил в скилле `humanizer`, которого нет ни в `arhiv-skillov/`, ни в каноне; в архиве есть только четыре пункта, названные самим `popsci-research`). С открытым вопросом владельцу — **6**. Все числа в шапках карт пишутся командами, а не значениями; команды подчищены отдельно, чтобы не матчить сами себя — иначе они печатали бы 5 вместо 1 и протухали молча.

### §2.5 — прочёс битых адресов

**Охват: проверено 156 файлов из 156** в `skills/` (не только `.md`); кандидатов упоминают **42** файла, упоминаний всего **105**.

| вид | число | что сделал |
|---|---|---|
| провенанс («перенесено из…», `RASHOZHDENIYA.md` «У них:», пометки «⚠ Адрес переставлен») | **94** | не трогал |
| адрес в архив (`arhiv-skillov/<кандидат>/…`) | **11** | не трогал, пути живые |
| **живой адрес наружу** | **3** | **починены все три** |

Все три живых адреса — в одном файле, `skills/slajdy/references/plakatnyj-stil.md`, шаги 6, 8 и 9 порядка действий: `content-studio/references/slide-voice.md`, `content-studio/references/archetypes.md`, `content-studio/SKILL.md §Процесс, шаг 5`. Починка выбрана не на угад: **в этом же файле шаг 2 уже ссылался правильно** — `arhiv-skillov/content-studio/SKILL.md §Процесс, шаг 2`, — то есть внутри одного файла одна и та же ссылка была написана двумя способами. Переставил три адреса на префикс `arhiv-skillov/` и добавил одну видимую пометку по образцу `dok-08-sceny.md`. Дословный текст рядом не тронут: правка ровно адресов.

**Проверка фактом, той же командой, что искала:** после правки адресов третьего вида — **0**. Единственное остаточное попадание регекса (`style-core.md:147`) — это перенос строки внутри пометки «⚠ Адрес переставлен», начатой на строке 146; показано выводом обеих строк подряд.

🔴 **Повод §2.5 к моменту работы уже не существовал.** `dok-07-verstka.md:68` был починен коммитом `d4848b1` от 06.08 — там стоит пометка «⚠ Адрес переставлен 2026-08-06: стояло `html-slides-studio/references/…`». То же у `dok-08-sceny.md:62` и `style-core.md:35,146`. Ход 1 всё равно окупился: три реальных адреса нашлись в файле, которого §2.5 не называл.

### Верификатор (§3)

Свежий субагент, метод обратный моему — от архива к записи, без моего плана и без файла-захода. Выборка 5 записей случайная и воспроизводимая (`random.seed(20260806)` по списку всех 72), не рукой: секреты 13 паттернов (arka) · пошаговый обмен с владельцем · семь традиций композиции · Narrative Index · форма отчёта терминологии.

**Результат: 4 из 5 подтверждены.** Расхождение и две побочные находки я перепроверил сам по живым файлам — все три подтвердились — и починил **записи, а не вердикт верификатора**:
1. **Расхождение (запись «Пошаговый обмен с владельцем»).** Мой вердикт утверждал стыковку на «`SKILL.md §4a`, контракт возврата throughline». Контракт (B14) стоит в `SKILL.md:133`, в §«Шаг 1 · бриф»; в §4a чекпоинта владельцу нет вовсе. Хуже адреса — стыковка не проведена: перечисление содержимого `kompoziciya-narrativa.md` в `SKILL.md:284–289` называет все параграфы, **кроме §2**. Вердикт понижен с `уже добрано` на **`добрать позже`** с названной починкой в одну строку.
2. **Побочно, вне выборки: из семи не-источников перенесено шесть.** Выпал `sources.md:183` — «„Уроки английского“ Линор Горалик и других — русская традиция эссеистики», то есть **единственная русскоязычная традиция в списке**, при том что лента пишется по-русски. Вердикт понижен с `уже добрано` на **`добрать`**.
3. **Мелкие неточности адресов**, вердиктов не меняли, поправлены: диапазон `SECRET_PATTERNS` — 23–37, а не 23–52 (константа кончается на 37); `Narrative Index` получил якорь §1; в запись про метрику дописано наблюдение верификатора, что §16 обещает применение в §4, а в §4 метрика не упомянута ни разу.

Верификатор отметил ещё, что замер «0 попаданий 13 паттернов по всем `.md` живой арки» из этого репозитория проверить нельзя — арка живёт в `materials/`. Это честная оговорка, и она в записи учтена полем `С ЧЕГО НАЧАТЬ`: перегнать те же паттерны по двум `VYGRUZKA-*.md`, потому что выгрузка и есть главный носитель.

### Что НЕ трогал

`GEJT-VHOZHDENIYA.md` и `arhiv-skillov/` — только на чтение. Ни одного `SKILL.md` не правил, содержание не переносил (§18 захода): карты называют адреса, а не копируют текст. `skills/*/DOLG.md` под руками не появился. Провенанс и адреса в архив (105 упоминаний минус 3) не трогал. Два дефекта вне зоны нашёл и оставил — пункт 4 `## ВОПРОСЫ`. Точка отката не заводилась: существующего не менял, кроме трёх адресов §2.5, которые обратимы одним `git revert`.

### НЕОБРАТИМОЕ

**Необратимого нет.** Ни одного удаления, переименования, перемещения, `git reset` или `checkout` поверх несохранённого. Четыре файла созданы с нуля. Единственная правка существующего файла — три адреса и одна строка-пометка в `skills/slajdy/references/plakatnyj-stil.md`, всё в коммите `8d762ef` и откатывается `git revert 8d762ef`.

### Открытое «возвращаться»

- **Вечером по `slajdy` работать можно** — условие 3 выполнено, 23 из 23. Но честный итог по этому дому: 15 из 16 позиций уже перенесены, и вечер стоит тратить не на перенос, а на три вещи, которые перенос не закрыл: прогнать процедуру сверки с 42 эталонами (её ни раз не гоняли после переноса), сверить кегли `buffon-fontspec.txt` с нашим `typography.md`, переписать сценарий 1 `evals.json` под наш вход.
- Шесть вопросов владельцу из пункта 6 `## ВОПРОСЫ` — каждый снимает работу дешевле, чем любой перенос.
- Дверь `register_doc.py` не знает раскладки `disciplina` (`skills/`); четыре карты не зарегистрированы.
- Две правки вне зоны из пункта 4 — обе в одну строку.

**Время прогона + токены** — снимает приёмка из лога прогона; исполнителю счётчик недоступен.

## ПРИЁМКА — 2026-08-07, аналитик

**ПРИНЯТО, и это лучший заход дня.** Гейты 6 из 8; оба красных — межрепозиторный класс (`priyomka.py` ищет хэш и артефакт в `materials`, работа в `disciplina`), плюс АРТЕФАКТ дан прозой вместо одного пути.

### Оспоренная предпосылка — проверил сам, исполнитель прав

`a9137c0` (гейт) — 06.08 **18:22**. `0c797ac` (перенос тех же 72 позиций) — **20:38** того же дня, влит в `main`. Проверено `merge-base --is-ancestor`: оба в `main`.

**Вердикт гейта «гасить нельзя ни одного» устарел через два часа шестнадцать минут после написания и прожил сутки как действующий.** Утром 07.08 я предъявил владельцу «14 непокрытых» как текущее состояние и рекомендовал не гасить. Владелец погасил вопреки — и **был прав**.

Живой разбор: **уже добрано 63 · добрать 4 · добрать позже 2 · НЕ добирать 3.** Реальной работы — девять позиций из семидесяти двух.

Отдельно ценно, что вердикт `уже добрано` не ставился по факту существования файла: прочитаны все шесть файлов переноса, разделы четырёх `SKILL.md`, оба `DYRY.md` и оба `RASHOZHDENIYA.md`. Там, где перенос формален, вердикт остался рабочим — `evals.json` перенесён физически, но написан под чужой вход.

### Проверено моим прогоном

16 + 39 + 8 + 9 = **72**; поля на месте (`ОТКУДА`/`ЗАЧЕМ`/`ВЕС`/`ВЕРДИКТ` по 16 из 16 в `slajdy`), `С ЧЕГО НАЧАТЬ` — 14, ровно по числу позиций `html-slides-studio`. Перенесённые файлы существуют и не пусты (503 · 145 · 81 строк).

**Расхождение со счётом гейта названо, а не сглажено:** по номерам утверждений выходит 73, потому что один пункт `popsci-research` несёт два требования. Плюс «7 контактных листов» в гейте против фактических 10.

### §2.5 — прочёс окупился, хотя повод был ложным

**Повода не существовало: `dok-07-verstka.md:68` — не живой адрес, а пометка о его починке** («⚠ Адрес переставлен 2026-08-06»), сделанной за сутки до. Я грепнул, увидел совпадение и объявил владельцу срочную находку, не прочитав строку целиком. Урок 66 — это дословно тот класс, который я сам записал в слепые зоны `check_sborki.py`.

Прочёс всё равно окупился: 156 файлов из 156, 105 упоминаний, из них провенанс 94 · адрес в архив 11 · **живых битых 3** — и все три в файле, которого §2.5 не называл (`plakatnyj-stil.md`). Починка выбрана не наугад: в том же файле шаг 2 уже ссылался правильно. После правки — 0, показано той же командой, что искала.

### Верификатор сработал по назначению

4 из 5, и расхождение настоящее: вердикт `уже добрано` стоял на записи, где стыковка не проведена — перечисление содержимого `kompoziciya-narrativa.md` называет все параграфы, **кроме §2**. Понижен до `добрать позже`. Плюс побочная находка вне выборки: из семи не-источников перенесено шесть, выпала **единственная русскоязычная традиция** — при том что лента пишется по-русски. Понижен до `добрать`.

**Уроки арки: 64 → 66.**
