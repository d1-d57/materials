# УРОКИ ФАБРИКЕ — арка «2026-08-05_faza-lenty»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c '^### '      UROKI-FABRIKE.md   # уроков
grep -c '^ЦЕНА: '    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -c '^ВЕРДИКТ: ' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---

### 1. Аналитик построил проверку того, что легко проверить, вместо того, что заказано
ЦЕНА: полный заход (Opus, ночной прогон, 20 файлов) построил спецификацию формы и машинный гейт формы, а владелец просил дисциплину СОДЕРЖАНИЯ — краткость, уровень подробности, идейное доказательство. Гейт не знал, что доказательство обязано сворачиваться, и пропустил документ с нулём катов при 22 врезках; не знал про унификацию шагов — `Шаг 1:` рядом с `шаг 3.` прошли зелёными; про сырой TeX (43 формулы) не знал вовсе. Владелец при первом взгляде: «я не знаю, зачем ты мне его показываешь». Круг объяснений плюс переформулировка задачи; поймал владелец, ни один гейт.
ВЕРДИКТ: вписан → `_studio/zhurnal/2026-08-05_faza-lenty/DISCIPLINA-teksta.md` (ядро будущей фабрики лент). В анкету фазы интервью нужен вопрос «что именно должно стать лучше — форма или содержание»: сегодня там есть J3 про ширину/глубину, а этого нет. Внести отдельной сессией вместе с разнесением.

### 2. Число-цена в заходе оказалось замером ПРОШЛОЙ редакции, и критерий на нём провалиться не мог
ЦЕНА: заход требовал починить два дефекта движка и приводил цену — «склеились 7 списков из 8», «21 таблица в собранном виде». Оба верны как замер первой сборки того же дня; к запуску склеек было 0, таблиц 1, потому что тогда починили ИСТОЧНИК, а движок остался сломан. Ход исполнителя ушёл на доказательство ложности предпосылки; хуже — критерий, построенный на таком числе, сверяется с состоянием, которого нет, то есть покраснеть не может.
ВЕРДИКТ: вписан → `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, клауза 5½.

### 3. `KARTA.md` правят обе стороны, и при `adopt` регистрация исчезает молча
ЦЕНА: исполнитель зарегистрировал три файла в своей ветке, Cowork в это же время правил `KARTA.md` у себя. После `adopt` файлы приехали, строки регистрации — нет. Коммит владельца упал воротами 5 («документ-сирота `POKRYTIE-lenty.md`»), понадобился круг диагностики и три повторных вызова двери. Правило «KARTA уезжает в тот же коммит» существует, но случай прихода файла через `adopt` из чужой ветки оно не покрывает.
ВЕРДИКТ: вписан → `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md` — в ритуал приёмки нужен шаг «после `adopt` прогнать регистрацию приехавших `.md`».

### 4. Гейт формы поймал съеденную строку там, где `check_view.py` зелёный
ЦЕНА: в эталоне фазы стояло `<адрес>` в угловых скобках, движок отдал его сырым, браузер прочитал как тег и съел строку. `check_view.py` зелёный: поломка в СЕРЕДИНЕ блока, а он сверяет хвост. Дефект прошёл в ЭТАЛОН — документ, который будут копировать. До этого тот же дефект был записан со словами «пока ноль: поймано до выдачи», то есть цены не имел и в канон не шёл.
ВЕРДИКТ: вписан → `_studio/konvejer/06-tekst/UROKI-FABRIKE.md` (дом уроков фазы, заведён этой аркой).

### 5. Код возврата прочитан от `tail`, а не от команды — на приёмке, которая это правило и несёт
ЦЕНА: два прогона гейта объявлены зелёными, будучи красными; поймано в тот же ход перепроверкой, дальше отчёта не ушло. Значимость не в цене, а в том, КТО нарушил: правило стоит первой строкой §1 каждого захода, и нарушил его тот, кто проверяет чужое соблюдение.
ВЕРДИКТ: отбракован — правило есть и достаточно, повторять нечего. Настоящий долг иной: у правила нет носителя, оно держится на внимании. Записан в `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.

### 6. Зона слияния в заходе описана по `poteri`, а `poteri` структурно не видит половину ветки
ЦЕНА: `poteri` отвечает на вопрос «что ПРОПАДЁТ, если удалить ветку», то есть показывает только пути, которых на этой стороне нет ВОВСЕ; файл, изменённый обеими сторонами, в его вывод не попадает по построению. Составитель захода взял по нему картину ветки и объявил зону `_studio/`, перечислив 7 файлов. Ветка трогала 20 путей, 7 из них в `_generator/`. `merge --zone "_studio/"` отказал целиком — один потерянный прогон и две строки в `INCIDENTY.md`. Дороже другое: **заход, исполненный буквально по своей же зоне, не выполнил бы собственную цель**, потому что частичного слияния не существует (`--zone` — сторож, `git merge` берёт ветку целиком). Исполнитель обязан был выйти за объявленную зону, чтобы задание стало исполнимым, — тот же класс «два обязательных указания, невыполнимых разом», который в `GIT-disciplina §3` уже стоил 27 обходов `--no-verify`. Потерь не случилось: измерено, что все 7 путей уже лежали в дереве байт в байт.
ВЕРДИКТ: чинить ИНСТРУМЕНТ, не правило. `poteri` печатает два числа — «пропадёт файлов» и «изменено с обеих сторон»; `merge` при отказе по зоне предлагает готовую строку со всеми нужными `--zone` (автолог такую подсказку уже пишет, значит инструмент их знает). Дом — `_generator/tools/git_zona.py`: лежит в зоне захода `kod_rasshivka-kornej.md`, вписано туда пунктом.

### 7. Ворота 5 не отличают НОВОГО сироту от унаследованного долга, а критерий требует «нового»
ЦЕНА: живой прогон `check_kartoteka.py --gates` дал `rc=1` и 7 сирот — все из чужой арки, к заходу отношения не имеющие. Один лишний ход на сверку каждой против точки отката, чтобы доказать «новых 0 из 7». Риск дороже хода: `rc=1` читается как провал критерия, а предложенная самим гейтом починка толкает исполнителя регистрировать 7 ЧУЖИХ документов, то есть править `KARTA.md` вне своей зоны под своим сообщением.
ВЕРДИКТ: ворота принимают базу сравнения (`--baseline <ref>`) и делят вывод на «новые» и «унаследованные» — ровно так, как это уже умеет сторож веток в `doctor`. Записано в `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.
ДОСТАВЛЕНО-В-ДОМ: 2026-08-06 → ../../docs/sostoyanie/OTKRYTYE-ZADACHI.md

### 8. Гейт Г3 писан под сборочные заходы и на переносных краснеет по построению
ЦЕНА: заход-сведение ничего не собирает — он переносит уже собранное. Артефакт приехал в 15:13, файл-заход дописывался отчётом до 15:31, и условие «артефакт не старше источника» не выполняется при полностью верном отчёте. Приёмка потратила ход на диагностику, причём первая версия диагноза («два пути в одной строке») оказалась неверной и была снята перепроверкой. Второй слой: из песочницы Cowork абсолютный путь машины владельца не разрешается вообще, поэтому Г3 там не может позеленеть ни при каком отчёте.
ВЕРДИКТ: у Г3 нужен вид захода (сборочный / переносной) либо сверка со временем КОММИТА, а не файла-захода. Записано в `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.

### 9. Предупреждение «хук покраснеет — обходи законной формой», вписанное в заход ЗАРАНЕЕ, работает как разрешение на обход
ЦЕНА: заход `kod_standart-skilla` нёс готовую строку обхода с уже сформулированной причиной — аналитик предвидел, что дверь регистрации в репозитории `disciplina` не заведена. Хука `pre-commit` в `disciplina` нет вовсе; коммит прошёл обычной формой с `rc=0`. Исполнитель, читающий §4 буквально, ушёл бы на `--no-verify` и записал в `INCIDENTY.md` срыв, которого не было, — то есть заход загрязнил бы автолог обходов, по которому меряется вся боль с коммитами (48 % записей). Поймал исполнитель, а не гейт. Класс шире случая: **предсказание аналитика «здесь будет красное», поданное вместе с готовым обходом, снимает проверку с того единственного, кто мог бы её сделать** — ровно та же асимметрия, что в гейте входа (у составителя проверка стоит одну команду, у исполнителя её нет по построению).
ВЕРДИКТ: предвиденное красное подаётся как **условие**, а не как готовый обход: «прогони хук; покраснел на чужом долге — вот форма обхода; не покраснел — обычная форма». Вписать в `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md §4` рядом с блоком про `--no-verify`.
ДОСТАВЛЕНО: standart-skilla#5

### 10. Домен документации Anthropic разошёлся на три, и все ссылки фабрики живут на редиректе
ЦЕНА: пока нулевая — оба адреса, названные в заходе, разрешились через редирект, исполнитель прошёл их и прочитал конечные документы, назвав это в отчёте. Замер 2026-08-06: `docs.claude.com/.../agent-skills/best-practices` → **302** на `platform.claude.com`, `docs.claude.com/en/docs/claude-code/skills` → **301** на `code.claude.com/docs/en/skills`. Цена отложенная и того же класса, что уже оплачен четырьмя неверными адресами за арку: редирект — не гарантия, он снимается молча, и тогда заход, названный «проверенным аналитиком», приведёт исполнителя в пустоту за час до работы.
ВЕРДИКТ: в заходах, ссылающихся на документацию Anthropic, писать КОНЕЧНЫЙ адрес, а не исторический; при сборке захода адрес проверять кодом ответа, а не открытием в браузере. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, гейт входа, вопрос 6 («каждый адрес проверен»): он про пути в репозитории и внешних адресов не покрывает.
ДОСТАВЛЕНО: standart-skilla#4

### 11. Адрес правки в заходе назван по памяти, и построенный на нём критерий провалиться не мог
ЦЕНА: заход `kod_reshenia-intervyu` §2.8 послал закрыть шесть развилок в `skills/slajdy/SKILL.md §8`. Маркер «не выбрано» там не жил никогда: живой адрес — `skills/slajdy/DYRY.md §8`, и встречался маркер ОДИН раз, в заголовке раздела, а не построчно (`git grep "не выбрано" f6fc2e1 -- skills/` → одна строка). Следствие дороже промаха: критерий готовности №4 был сформулирован как `grep -c "не выбрано" skills/slajdy/SKILL.md` → 0 — то есть давал **0 и до работы, и после**, независимо от того, сделана ли она. Гейт был зелёным по построению. Спас исполнитель: нашёл настоящий адрес грепом по репозиторию, исполнил по факту и назвал расхождение в `## ВОПРОСЫ`. Составитель мог сделать тот же греп при сборке — это ровно вопрос 6 гейта входа («каждый адрес проверен грепом»), и он не был прогнан. Класс не новый: за арку 28–30.07 он стоил четырёх неверных адресов подряд.
ВЕРДИКТ: греп-критерий прогоняется на живом файле ДО выдачи захода, и текущее значение пишется в заход (`RUKOVODSTVO §Критерий, клауза 5`) — правило есть и было нарушено составителем. Настоящий долг: у клаузы 5 нет носителя, `check_zahod.py` греп-критерии не исполняет. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`: гейт входа должен уметь ПРОГНАТЬ греп из критерия и покраснеть, если тот даёт целевое значение уже сейчас.
ДОСТАВЛЕНО: reshenia-intervyu#1

### 12. Критерий готовности требовал нуля там, где тот же заход запрещал править файл
🔴 **ФОРМУЛИРОВКА ИСПРАВЛЕНА 07.08 ПО ЖИВОМУ ФАЙЛУ — см. урок 17.** Первая редакция этого урока утверждала, что «§0 захода трогать `INTERVYU-utro.md` ЗАПРЕЩАЛ». Это неверно: `grep -n "INTERVYU-utro" kod_reshenia-intervyu.md` даёт 8 совпадений, **все до одного в секциях `## ВОПРОСЫ` / `## ОТЧЁТ` / `## ПРИЁМКА`**, написанных ПОСЛЕ работы; в задании файл не назван ни разу, а ЗОНА сказана как «всё в репозитории `disciplina`, кроме двух `SKILL.md`» — то есть формально файл в зону ВХОДИЛ. Запрет жил в голове исполнителя и в приёмке, а не в тексте. Настоящий дефект ниже.

ЦЕНА: критерий №2 захода `kod_reshenia-intervyu` требовал `grep "doma/lenta\|doma/reserch/"` → **0 строк** по ВСЕМУ репозиторию, при том что зона захода уже репозитория: из неё явной оговоркой «кроме» вырезаны пути, вхождения в которых исполнителю не починить. Две живые строки лежали как раз в файле, правка которого была нежелательна по внешней причине (владелец держал его открытым), но нигде не запрещена. Исполнитель оказался перед требованием, выполнимым только выходом за назначенную ему рамку — тот самый класс, который в `GIT-disciplina §3` уже стоил 27 обходов `--no-verify` из 28. Здесь обошлось дёшево: файл не тронут, критерий закрыт двумя задокументированными строками вместо нуля, расхождение названо в `## ВОПРОСЫ`. Но исход зависел от того, что исполнитель выбрал доложить, а не подчиниться букве: подчинившись, он правил бы файл, открытый владельцем.
ВЕРДИКТ: критерий, считающий по ВСЕМУ репозиторию, обязан исключать пути, которые заход объявил неприкосновенными, — либо нести явную строку «эти N вхождений законны, причина такая-то». Совместимость критерия с собственными запретами захода проверяется арифметикой ДО выдачи, как и совместимость критериев между собой. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, клауза 5, шестым подпунктом.

### 13. Число долга в принятом решении устарело между его написанием и исполнением
ЦЕНА: решение 9 интервью и сам `INTERVYU-utro.md` называют долг «7 уроков без дома»; живой прогон `check_uroki_doma.py` на той же арке даёт **9**. Исполнитель проверил стэшем, что расхождение существовало ДО его правок, и не стал чинить — верно. Настоящая причина: число снято в ночь на 06.08, а арка с тех пор росла, и растёт она в том числе от приёмок, которые в неё же и пишут уроки. Цена пока в один ход сверки, но класс дорогой: на этом числе стоит порядок работ («перенести 7 уроков → rc=0 → включить хук»), и заход, написанный по нему буквально, объявит себя выполненным, перенеся семь из девяти.
ВЕРДИКТ: в решениях, принятых по замеру, число заменяется командой, которая его считает, — `KONSTITUCIYA §10` про критерии и про прозу, но не про ПРИНЯТЫЕ РЕШЕНИЯ, а они живут дольше всего. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.
ДОСТАВЛЕНО: reshenia-intervyu#3

### 14. Критерий готовности требовал действия, которое §2 того же захода запрещал — ВТОРОЙ РАЗ ЗА ДЕНЬ
ЦЕНА: заход `kod_marketplace-github` условием 5 требовал «сделай правку, закоммить, **запушь**» — живой прогон механизма обновления; §2.2 того же файла говорил «ветки заходов НЕ пушить», а §4 — «ветку в конце НЕ вливай». Место, где тестовый коммит физически разрешён (`main`), не названо нигде. Исполнитель **дважды останавливал работу и спрашивал владельца в чате** — то есть свойство «заход самоценен, исполнитель работает с пустым контекстом и не дёргает владельца» было потеряно ровно там, ради чего вся дисциплина заходов и существует. Класс — тот же, что урок 12, записанный ЭТИМ ЖЕ ДНЁМ на предыдущем заходе: два обязательных указания, невыполнимых разом. Повтор внутри одних суток означает, что дело не в невнимательности составителя, а в отсутствии носителя.
ВЕРДИКТ: у клаузы «ОДИН заход = ОДИН критерий» нужен парный гейт — **критерий проверяется на непротиворечивость СО СВОИМИ ЖЕ запретами**, механически. `check_zahod.py` уже читает секцию критерия и секцию задачи; добавить проверку: глагол действия в критерии (`запушь`, `влей`, `удали`, `отключи`), присутствующий в тексте захода под отрицанием, — красное на сборке. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`, и это второй за день долг, указывающий на один и тот же ненаписанный гейт.
ДОСТАВЛЕНО: marketplace-github#1

### 15. Заход пересказал ЧУЖОЙ заход по приёмке, а не по его тексту, и поставил задачу про фантом
ЦЕНА: `kod_gejt-protivorechiya` в КОНТЕКСТЕ утверждал, что в `kod_reshenia-intervyu` «§0 того же файла ЗАПРЕЩАЛ править файл, где эти строки лежат». Живой греп: 8 совпадений, **все в секциях, написанных исполнителем ПОСЛЕ работы**; в задании файла нет, зона его формально включала. Составитель взял факт из СВОЕЙ ЖЕ приёмки, где сам этот запрет и сформулировал, — и подал как свойство чужого текста. Последствие не косметическое: исполнитель, поверивший предпосылке, искал бы лексический запрет, которого нет, и правило распознавания провалилось бы **не поняв почему**. Спасло то, что он проверил предпосылку грепом до работы и оспорил в `## ПЛАН`, как велит канон. Класс: **пересказ по памяти о собственном разборе неотличим от цитаты, и в заходе он выглядит как проверенный факт.**
ВЕРДИКТ: гейт входа, вопрос 4 («предпосылка жива?») требует сверять по живому файлу — правило есть. Не хватает частного случая: **предпосылка О ЧУЖОМ ЗАХОДЕ проверяется по его ЗАДАНИЮ (текст до `## ПЛАН`), а не по отчёту, вопросам и приёмке** — они написаны позже и другим лицом. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, гейт входа, вопрос 4.
ДОСТАВЛЕНО: gejt-protivorechiya#1

### 16. `git_zona.py check --zone`, позванный по абсолютному пути из worktree, судит ОСНОВНУЮ папку — молча
ЦЕНА: заходы этой арки предписывают форму `python3 $MAT/_generator/tools/git_zona.py check --zone …` — она верна для чужого репозитория, но в worktree наводит инструмент на ОСНОВНУЮ рабочую папку. Исполнитель получил «Правлено и не закоммичено: M `_generator/build_listok.py`» — чужой файл, которого в его worktree нет вовсе (`git status --porcelain` там пуст), и минуту читал это как красное на своей зоне. `GIT_ZONA_REPO` тут не спасает: репозиторий тот же, разъезжаются РАБОЧИЕ ПАПКИ. Цена пока минута, но класс дорогой: гейт приёмки 0 — первый ход разбора отчёта, и он способен показать чужую грязь как свою.
ВЕРДИКТ: `check --zone` печатает первой строкой **какую рабочую папку судит** (`git rev-parse --show-toplevel`), либо отказывается, когда его зовут из одной рабочей папки по пути другой. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.
ДОСТАВЛЕНО: gejt-protivorechiya#3

### 17. Урок фабрике, записанный приёмкой, содержал неверный факт и месяц бы жил в каноне
ЦЕНА: урок 12 этой же арки, записанный аналитиком 07.08, утверждал факт о чужом файле, которого в нём нет (см. урок 15). Урок — вход сессии, которая правит КАНОН; неверный факт в нём попал бы в правило, которое читают все будущие проекты, и обнаружился бы только при попытке применить. Поймал исполнитель следующего захода, потому что заход велел ему прочитать эти уроки как источник. Стоило: одна правка урока в тот же день. Механизм спас случайно — исполнителя послали читать уроки не ради их проверки, а ради материала.
ВЕРДИКТ: у секции «Как отсюда читать» есть три формы вердикта, и **нет формы «факт урока не подтвердился»**. Завести четвёртую: `ВЕРДИКТ: факт исправлен → <чем>`, с обязательной ссылкой на то, что показало ошибку. Плюс правило: урок, записанный ПРИЁМКОЙ (а не исполнителем), сверяется с живым файлом тем же ходом — у приёмки, в отличие от исполнителя, соблазн описать не текст, а своё понимание текста. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

### 18. Заход перенёс РЕШЕНИЕ владельца, но не перенёс его ЦЕНУ в критерий — и критерий стал неисполнимым
ЦЕНА: `kod_etap-20` §2.5 велел поставить `disable-model-invocation` (решение владельца 1), а критерий 5 того же захода требовал от валидатора `rc=0` — при том что ровно это поле валидатор и роняет. Обе стороны конфликта были записаны в `STANDART-SKILLA §3 К-1` **за день до сборки захода**, то есть составитель их знал и не свёл. Исполнитель оказался обязан либо нарушить §2.5, либо провалить критерий 5; выбор между ними ему не принадлежит. Обошлось одним ходом на доказательство запуском обеих веток и поправкой 5′ в `## ПЛАН` — но при молчаливом выборе стоило бы всей приёмки: отчёт с `rc=1` заворачивается гейтом 0, не читая. Третий за сутки случай того же корня, что уроки 12 и 14: критерий не сверен с остальным телом захода.
ВЕРДИКТ: правило шире гейта З8, который ловит только глагольную и охватную формы. **Решение владельца, у которого в источнике названа ЦЕНА, переносится в заход ВМЕСТЕ с ценой, и критерий пишется с её учётом**; перенос одной половины — брак сборки. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, клауза 6 («ОДИН заход = ОДИН критерий»), рядом с вердиктом урока 14.
ДОСТАВЛЕНО: etap-20#6

### 19. Решение «удалить как мёртвое» принято по замеру, смотревшему в один файл, — а файлы были живыми
ЦЕНА: позиция Ф-4 этапа 19 мерила «упомянут ли файл в `SKILL.md`» и назвала `dok-01`…`dok-04` мёртвыми дублями; владелец по этому замеру решил удалить (решение 4). Живой `references/vhod-gejty.md`, названный в `§0` того же скилла, ссылался на них **в семи местах** — оттуда цитируются критерии гейтов G1–G4. Удаление создало бы семь висящих ссылок в файле, который скилл читает по контракту. Поймано только потому, что исполнитель прогрепал зону ПЕРЕД `rm`, а не после; верификатор нашёл ещё одну такую же ссылку от второго удаления. Ссылки перенаправлены на оригиналы в `materials/_studio/konvejer/`, повторный греп — 0.
ВЕРДИКТ: **решение владельца наследует слепую зону замера, на котором принято**, и это свойство самого решения, а не небрежность. Перед исполнением решения «удалить» охват замера пересчитывается по ВСЕЙ зоне, а не по файлу, где мерили. Плюс формулировка вывода в отчёте этапа-19-типа обязана нести охват: не «не упомянут», а «не упомянут в `SKILL.md`; в остальных файлах зоны не проверялось». Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, клауза 2 (охват внутри вердикта).
ДОСТАВЛЕНО: etap-20#7

### 20. Число, «снятое командой», всё равно потребовало глаза: греп считал объяснение конвенции наравне с меткой
ЦЕНА: критерий этапа 20 назвал охват «31 метка `⚠ ДЫРА`». Живой `grep -c "⚠ ДЫРА"` после слияния дал 33, из которых **3 — не метки, а прозаические упоминания самой конвенции** в шапках тел («места нехватки помечены `⚠ ДЫРА:`»). Разница мала, но она ломает главный приём канона: `KONSTITUCIYA §10` требует писать команду вместо числа именно потому, что команда не врёт, — а здесь команда врёт на 10 %, и молча. Исполнитель развёл метки и упоминания глазами и предъявил обе колонки; без этого «закрыто 16 из 30» читалось бы как «из 33».
ВЕРДИКТ: у счётной команды должен быть якорь, который нельзя произнести в прозе, — например метка в начале строки (`^⚠ ДЫРА:`) вместо вхождения где угодно. Правило шире случая: **команда-счётчик проверяется на ложные срабатывания ТАМ ЖЕ, где вводится**, иначе она наследует доверие, которого не заслужила. Дом — `../../docs/KONSTITUCIYA.md §10`.
ДОСТАВЛЕНО: etap-20#2

### 22. Правка файла-захода через `replace()` не совпала и молча ничего не сделала — потерян весь контракт зоны
ЦЕНА: заход `kod_gejt-vhozhdeniya` собирался Cowork так же, как три соседних: генератор печатает скелет, аналитик дозаполняет секции строковой заменой. Две замены в блоке «КОНТРАКТ ЗОНЫ» были написаны по образцу ДРУГОГО захода и в этом файле не совпали ни с чем. `str.replace()` при несовпадении возвращает исходную строку и **не сообщает об этом** — файл записался, линтер входа прошёл зелёным, заход ушёл владельцу. Потеряно ровно то, без чего §2.4 неисполним: исключение «отключение скиллов меняет настройки вне обоих репозиториев». То есть заход велел гасить скиллы и тем же текстом объявлял всё вне `arhiv-skillov/` READ-ONLY — **четвёртый за двое суток случай двух обязательных указаний, невыполнимых разом** (уроки 12, 14, 18). Поймал владелец, заметив, что исполнитель встал на отсутствующем worktree; сам worktree — вторая, независимая потеря того же прогона (флаг `--worktree` не передан генератору).
ВЕРДИКТ: **аналитик правит файл-заход только проверяемой заменой** — `assert` на присутствие нового текста ПОСЛЕ записи, либо anchor-replace с abort при ≠1 совпадении. Правило уже существует для исполнителя («якорные замены — abort при ≠1», `§1 ДИСЦИПЛИНА` каждого захода) и не было распространено на того, кто заходы собирает. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, рядом с гейтом входа: это его восьмой вопрос — «то, что ты вписал, действительно вписалось?».

### 23. Гейт З8 не поймал противоречие, потому что сузили словарь — ровно на том глаголе
ЦЕНА: 07.08 заход `gejt-protivorechiya` сузил З8 с шести глаголов до двух, сняв `удали`, `отключи`, `перепиши`, `тронь` как полисемичные: у них объект бывает разного масштаба, и верификатор предъявил ложное красное по каждому. Решение верное и принято приёмкой. **В тот же день следующий заход собрал противоречие ровно на глаголе `отключи`** — §2.4 требует отключить скиллы, контракт зоны это запрещает, — и З8 промолчала законно. Цена пока в один ход владельца; но класс дорогой: сужение гейта не оставило следа там, где перестало ловить, и составитель об этом при сборке не вспомнил.
ВЕРДИКТ: снятое из гейта обязано оставаться **видимым в момент сборки**, а не только в докстринге инструмента. Дешёвая форма: `bootstrap_zahod.py` печатает при сборке строку «З8 судит глаголы `пуш`/`влить`; `удали`, `отключи`, `перепиши`, `тронь` она НЕ судит — сверь их глазами». Тот же приём, что со стартовым сообщением: убрать место для ошибки, а не завести ещё одно правило. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`, вместе с вердиктом урока 22.

### 24. Пять заходов подряд ушли владельцу с ДВУМЯ критериями готовности — вторым был пустой шаблон
ЦЕНА: аналитик дозаполняет скелет двумя способами: заменой шаблонного §2 на свой (так собраны `reshenia-intervyu`, `standart-skilla`, `etap-20` — чисто) и **вставкой своего текста перед `## 1. ДИСЦИПЛИНА`** (так собраны пять следующих). Второй способ оставляет шаблонный `## 2. ЗАДАЧА` ниже по файлу — вместе с плейсхолдером `<конкретно, по шагам>` и **вторым «КРИТЕРИЕМ ГОТОВНОСТИ»**, пустым. Проверено грепом по восьми заходам: у пяти `§2` и `§3` встречаются ДВАЖДЫ. Четыре из этих пяти в момент обнаружения **уже работали у исполнителей**. Прямое нарушение клаузы 6 канона («ОДИН заход = ОДИН критерий готовности»), и это тот самый класс, который уже оплачен: заход нёс вторую задачу под заголовком «СПРАВОЧНО» со вторым критерием на 7 пунктов, два пункта которого исполнять было надо — «отличить в нём живое от мёртвого может только тот, кто уже прочёл весь заход». Не выстрелило по случайности: плейсхолдер выглядит незаполненным шаблоном, и исполнители его, судя по отчётам, игнорировали. Поймано при сборке шестого захода, аналитиком, случайно — искал остаток плейсхолдера после отдельной правки.
ВЕРДИКТ: **линтер входа проверяет наличие секций (З7), но не их ЕДИНСТВЕННОСТЬ.** Дописать в `check_zahod.py`: `## 2. ЗАДАЧА`, `## 3. ВЕРИФИКАТОР`, `КРИТЕРИЙ ГОТОВНОСТИ`, `## ОТЧЁТ` — каждая ровно один раз; больше одного — красное. Проверка стоит трёх строк и ловит весь класс «дозаполнил вставкой, а не заменой», к какому бы способу сборки ни перешли дальше. Файл в зоне работающего сейчас захода `kod_vlitie-v-generator` — вписать туда пунктом либо отдельным заходом после него. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.

### 25. Критерий потребовал того, чего инструмент не умеет — ПЯТЫЙ случай невыполнимого критерия за двое суток
ЦЕНА: критерий п.3 захода `kod_vlitie-v-generator` велел «собери пробный заход в `/tmp`», а `bootstrap_zahod.py` отказывает на `arka_dir` вне корня репозитория («папка арки вне репозитория» — строка 365). Составитель читал этот файл при сборке захода и всё равно потребовал невозможного. Исполнитель разобрался сам, обошёл через `GIT_ZONA_REPO` на одноразовом репозитории и назвал расхождение в `## ВОПРОСЫ`, не переписывая задачу; стоило времени на разбор. **Подкласс НОВЫЙ и гейтом З8 не ловится:** З8 судит «глагол в критерии против запрета в теле захода», а здесь запрета в теле нет — требование невыполнимо **технически**, и знание об этом лежит в чужом файле. Счёт по классу «критерий, который нельзя исполнить»: уроки 12, 14, 18, 22 и этот — пять за двое суток, и каждый раз причина новая.
ВЕРДИКТ: у класса нет одного лечения, и попытка накрыть его одним гейтом уже дала сужение до двух глаголов (урок 23). Настоящее лечение процедурное и дешёвое: **команда, названная в критерии, прогоняется составителем при сборке захода — ровно как греп-критерий по клаузе 5.** Клауза 5 требует этого только для грепов; распространить на любую команду, чей результат критерий предсказывает. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, клауза 5, расширить формулировку с «греп» на «любая команда критерия».
ДОСТАВЛЕНО: vlitie-v-generator#1

### 26. `check_kurs.py --help` падает трейсбеком вместо справки — и это единственный способ узнать, что инструмент не тот
ЦЕНА: заход `etap-21-ssylki` §2.4 велел «инструмент выбери сам, посмотри `--help`, не угадывай». Исполнитель проверил пять кандидатов; на `check_kurs.py` `--help` не распознаётся как флаг argparse — парсер принимает его за путь и падает `FileNotFoundError: '--help/kartoteka/KARTA-OBLASTI.md'`. Воспроизведено приёмкой дословно. Не заблокировало (заход предусмотрел запасной путь), но стоило разбора трейсбека вместо чтения одной строки. Класс: **инструмент, который нельзя спросить, что он делает, приходится читать кодом** — а канон в каждом заходе требует именно «сверь `--help`, не угадывай», то есть предписывает то, что у части инструментов не работает.
ВЕРДИКТ: `--help` обязан отвечать у КАЖДОГО инструмента `_generator/tools/` — это контракт, а не удобство. Дешёвый носитель: `check_tool_contract.py` уже судит инструменты по контракту; добавить ему проверку «`<tool> --help` даёт `rc=0` и непустой вывод», прогон по всем файлам папки. Ловит весь класс разом и не требует помнить. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.
ДОСТАВЛЕНО: etap-21-ssylki#1

### 27. Заход послал в worktree и велел читать файл, которого в worktree нет по построению
ЦЕНА: `kod_pereschet-i-schetchik` §0 назвал `PUT-K-GOTOVOMU.md` обязательным чтением и источником команды для критерия. Файл заведён аналитиком в рабочей копии ОСНОВНОЙ папки и не закоммичен (`git log --all --diff-filter=A` по нему пуст) — значит в worktree его нет вовсе. Исполнитель потратил круг разведки: `find` по рабочей папке, история по git, снова `find` по основной. Повезло, что файл читался по абсолютному пути; не повезло бы — он либо сдал бы пункт критерия по числу из текста захода (ровно то, что пункт запрещает), либо встал. **Дефект структурный:** любой файл арки, заведённый аналитиком и ещё не закоммиченный, невидим исполнителю, которого послали в worktree. За эту сессию таких файлов аналитик завёл два.
ВЕРДИКТ: аналитик, называющий в заходе СВОЙ свежий файл, обязан либо закоммитить его до выдачи захода, либо дать абсолютный путь основной папки и сказать, что в worktree файла нет. Механический носитель дешевле правила: `bootstrap_zahod.py` при сборке проверяет, что каждый `.md`-якорь, названный в §0, существует **в git**, и печатает предупреждение по тем, что только в рабочей копии. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, гейт входа, вопрос 6 (адреса), новым подпунктом «и виден ли адрес оттуда, куда послан исполнитель».
ДОСТАВЛЕНО: pereschet-i-schetchik

### 28. Однострочная выжимка корпуса теряет ту половину реплики, которая решает дом
ЦЕНА: записи корпуса разложены по адресам, а содержание берётся из строки-выжимки в `KRATNOST-vladelca.md`. На `КРВ-05c` выжимка говорит «не называть „задача B1“ — в лекции таких сущностей нет», а живая строка `PRAVKI.md:521` несёт вторую половину: «она у тебя без разбора, её тоже нужно разобрать» — то есть закон полноты формулировки, по которому запись принадлежит другому дому. По выжимке — СЛАЙДЫ, по источнику — ЛЕНТА. Одна запись из 273 переразмечена; нашёл верификатор на выборке 10, то есть частота ошибки не измерена, показано лишь, что она не нулевая. Класс задевает весь корпус в 2264 записи, а не одну фазу.
ВЕРДИКТ: **выжимка годится, чтобы ОТОБРАТЬ записи, и не годится, чтобы по ним СУДИТЬ** — судить надо по адресу. Вписать это строкой в шапку `KRATNOST-vladelca.md` и в задание каждого будущего захода-разбора дома; без неё разборщик по умолчанию поверит выжимке, потому что она под рукой, а адрес надо открывать. Дом — `_studio/zhurnal/2026-07-30_dovodka-fabriki/KRATNOST-vladelca.md`, шапка.
ДОСТАВЛЕНО: pereschet-i-schetchik

### 29. Разворот агрегата `КРТ-РАЗН-*` теряет две записи, и к ним нельзя прицепить рычаг
ЦЕНА: `karta_domov.py` раскладывает поимённо 311 записей из 312 фазы `02-reserch` и 97 из 98 фазы `03-matbaza`. Итоги домов при этом верны — они берутся из `srez_po_faze.py`, — поэтому дефект жил с 06.08 незамеченным. Всплыл, когда появился счётчик накрытия: у двух записей нет своего id, значит рычаг к ним прицепить не за что, и в метрике они навсегда «не разобраны». Пока цена — две записи из 2264; но она растёт с каждым домом, который начнут разбирать по id.
ВЕРДИКТ: чинить разворот агрегата в `karta_domov.py` — отдельным заходом, вместе с разбором тех фаз, где эти записи живут (сдвигать счёт в чужой фазе попутно нельзя). Счётчик уже печатает ⚠ на каждом прогоне, то есть дефект больше не молчит — это условие достаточное, чтобы не чинить его срочно. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.
ДОСТАВЛЕНО: pereschet-i-schetchik

### 30. Заход назвал зону путями чужого репозитория, не назвав сам репозиторий
ЦЕНА: контракт зоны `kod_gejt-vhozhdeniya` перечислял `disciplina/arhiv-skillov/` рядом с путём файла-захода в `materials` — читается как одно дерево. На деле `disciplina` отдельный репозиторий со своими worktree, и `git_zona.py` там нет вовсе. Проверка «в какой ветке лежит `disciplina/`» дала **0 файлов во всех 14 ветках `materials`**: предпосылка ложна не в детали, а в адресе работы целиком. Стоило **холостого запуска Opus-захода** — исполнитель верно встал на §0 по правилу «ветки нет — СТОП», выдал отчёт о непроходимости, разблокировка потребовала хода владельца и второй редакции контракта. Побочно в `materials` осталась пустая ветка-мусор. Ключевое: **«ветки нет» и «репозитория нет» на первом ходе выглядят одинаково**, а исполнитель с пустым контекстом различить два дерева по именам путей не может по построению.
ВЕРДИКТ: если заход работает НЕ в том репозитории, где лежит его файл, это стоит **первой строкой контракта зоны**, вместе с `GIT_ZONA_REPO`. Во второй редакции аналитик так и вписал; урок — про то, что в первой строки не было, хотя тремя заходами ранее он же её писал. Механический носитель: `bootstrap_zahod.py` видит в `--zone` путь, начинающийся с имени чужого репозитория, и требует явного флага репозитория работы. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, контракт зоны.
ДОСТАВЛЕНО: gejt-vhozhdeniya#1

### 31. Покрытие, которое держится на ИМЕНИ правила: греп находит, а дела за ним нет
ЦЕНА: гейт вхождения показал, что четыре наших адреса для `popsci-narrative` — это строки 286–287 `reserch-cikl/SKILL.md`, где перечислены «Story Spine», «small-big-small», «imperfect-puzzle», «закольцовка» **без содержания**. Воспроизведено приёмкой дословно. Погаси внешний скилл — и в каноне останется список имён, за которыми ничего нет: читатель встанет ровно там, где правило должно было сработать. Это худший вид покрытия, потому что **любая машинная проверка его засчитает**: греп по имени находит, а проверить наличие смысла нечем. Из 26 непокрытых утверждений этого кандидата четыре выглядели покрытыми именно так.
ВЕРДИКТ: при переносе содержания «упомянуто по имени» не равно «перенесено». Гейт вхождения обязан требовать не адрес, а **адрес плюс объём**: сколько строк содержания стоит по этому адресу. Ссылка на правило без его тела — вид «НЕ покрыто», и это надо назвать в самом методе гейта, иначе следующий разбор повторит ошибку. Дом — `disciplina/GEJT-VHOZHDENIYA.md`, раздел метода.
ДОСТАВЛЕНО: gejt-vhozhdeniya

### 32. Внутри нашего скилла живёт ссылка на скилл, который собираются гасить
ЦЕНА: `disciplina/skills/slajdy/references/style-core.md` в трёх местах (строки 13, 33, 141) адресует наружу — в `content-studio/references/slide-voice.md` и `content-studio/assets/corpus/`. Проверено приёмкой грепом. `content-studio` — кандидат 5 на отключение; погасив его, канон получит адрес в никуда, причём в файле, который скилл читает по контракту. Цена пока нулевая: гейт не прошёл никто, гасить нечего. Но если бы гейт прошёл, отключение сломало бы наш собственный скилл — а проверка вхождения смотрит, что доехало ОТ кандидата К нам, и не смотрит, что осталось от нас К кандидату.
ВЕРДИКТ: гейт вхождения двусторонний. Перед отключением — греп по нашим скиллам на имя кандидата; найдено — чинится ДО гашения, а не после. Одна строка в методе гейта, ловит весь класс. Дом — `disciplina/GEJT-VHOZHDENIYA.md`, раздел метода, рядом с вердиктом урока 31.
ДОСТАВЛЕНО: gejt-vhozhdeniya#2

### 33. Заход объявил «этого не существует нигде» — и обе фактические предпосылки оказались ложны, обе проверялись одной командой
ЦЕНА: `kod_dva-vhoda` утверждал, что знание «как устроено всё вместе» сегодня не живёт нигде, и что репозиториев два. Живой замер: `~/Documents/GitHub/README.md` **существует** — 63 строки, роутер «ситуация → дом» плюс таблица репозиториев, три из пяти пунктов задания он уже покрывал; репозиториев **пять** (`digest`, `disciplina`, `matema-fest`, `matemdigest-map`, `materials`). Обе проверки — по одной команде (`ls`, цикл по `*/.git`), составитель не сделал ни одной. Спасло то, что исполнитель проверил сам и **не стал заводить вторую копию таблицы репозиториев**: два списка разъехались бы молча, а не-версионируемый разъехался бы первым. Заход при этом остался осмысленным по другой причине (папка не под git — у знания есть текст, но нет дома), то есть цель уцелела случайно, а не по замыслу.
ВЕРДИКТ: **утверждение «этого нет» — самое дорогое в заходе и единственное, которое нельзя писать по памяти.** Оно снимает с исполнителя обязанность искать: поверив, он не посмотрит. Гейт входа, вопрос 7 («задача уже сделана?») ловит соседний случай — «работа выполнена», — и не ловит «предмет уже существует в другом виде». Дописать восьмым вопросом: **каждое «нет / не существует / не живёт нигде» в тексте захода подтверждается командой, и команда пишется рядом.** Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, гейт входа.
ДОСТАВЛЕНО: dva-vhoda

### 34. Потолок объёма и требуемое содержание в одном заходе оказались несовместимы
ЦЕНА: заход требовал ответить на пять вопросов в одном документе и на пять в другом — и тут же ставил ориентир «не длиннее 35 строк, и это потолок, а не цель». Вышло 51 и 47. Исполнитель сжимал двумя полными проходами, дальше начиналось вырезание ответов, которых требуют §2.1/§2.2, и он остановился, назвав требование несовместимым с заданием, а не собой невыполненным. Это верный ход: подчинившись потолку, он выбросил бы часть ответов молча, и приёмка увидела бы короткий документ, а не пропажу. Класс тот же, что уроки 12/14/18/22/25, но в новой одежде: **не запрет против требования, а два требования, конкурирующих за один ресурс** — объём.
ВЕРДИКТ: потолок объёма в заходе законен только вместе с указанием, **чем жертвовать при нехватке места**. Нет такого указания — потолок не пишется вовсе, потому что исполнитель обязан выбрать, а выбор ему не принадлежит. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, клауза 6, рядом с вердиктом урока 14.
ДОСТАВЛЕНО: dva-vhoda

### 35. `disable-model-invocation: true` закрывает скилл и для явного вызова — описание обещает дверь, которой нет
ЦЕНА: `disciplina/README.md:35` и оба черновика описывают флаг так: «активируются **только по явному вызову по имени**, на общие запросы не срабатывают». Верификатор проверил прогоном: прямой вызов `reserch-cikl` и `slajdy` даёт ошибку «cannot be used due to disable-model-invocation», и в списке доступных скиллов сессии их **нет вовсе**. То есть для исполнителя и для агента скилл не полу-доступен, а недоступен целиком; поднять может только владелец из чата. Поймано вторым проходом верификатора, который пошёл по документу буквально и встал ровно там, где документ обещал дверь. Цена сегодня — один круг правки документа; цена завтра выше: **вся вторая половина этапа 20 писалась в предположении, что черновики можно звать по имени**.
ВЕРДИКТ: флаг ставился решением владельца К-1 07.08 «временно, до выхода из черновиков», и цена его тогда была названа неполно — знали про потерю переносимости на claude.ai, не знали про потерю вызова по имени. Решение подлежит пересмотру владельцем с полной ценой. Описание в трёх местах (`README.md` и два `SKILL.md`) исправляется независимо от решения: оно ложно уже сейчас. Дом — `disciplina/README.md` + оба `SKILL.md`.
ДОСТАВЛЕНО: dva-vhoda#4

### 36. Ручной замер аналитика по корпусу нашёл 4 дефекта из 17 — и был подан заходу как факт
ЦЕНА: собирая `kod_edinstvennost-sekcij`, аналитик прошёл глазами по 167 файлам-заходам и объявил: дубли секций у четырёх, поимённо. Живой изолированный прогон готовой проверки нашёл **17**. Не пойманы **76 %**, причём это не редкий класс: у десяти файлов задвоена одна и та же `## УРОКИ ФАБРИКЕ`. Заход при этом строился на «четырёх» как на калибровочной опоре — если бы правило подгонялось под этот замер, оно вышло бы слишком узким и молчало на большинстве настоящих дефектов. Спасло, что заход требовал **живого прогона по всему корпусу** отдельным условием критерия: исполнитель прогнал и увидел разницу.
ВЕРДИКТ: `KONSTITUCIYA §10` («не число, а команда, которая его считает») записана про отчёты исполнителей и прозу канона — и **не распространена на замеры самого аналитика при сборке захода**. Между тем аналитик считает глазами по корпусу в 167 файлов, то есть в условиях, где глаз заведомо не справляется. Распространить дословно: **число в заходе, снятое аналитиком вручную, пишется вместе с командой или не пишется вовсе**; калибровочная опора без команды — не опора. Дом — `../../docs/KONSTITUCIYA.md §10`, дополнить адресатом.
ДОСТАВЛЕНО: edinstvennost-sekcij#2

### 37. Новая проверка покраснела на собственном отчёте исполнителя — потому что он цитировал то, что она ищет
ЦЕНА: дописав `## ОТЧЁТ`, исполнитель прогнал на нём же свежую З9 и получил красное: «КРИТЕРИЙ ГОТОВНОСТИ» встречается 3 раза. Причина — отчёт **дважды цитировал строку в backtick**, обсуждая саму проверку. Заголовки `##` он заякорил на начало строки сразу, а `KRITERIJ_RE` заякорить нельзя (шаблон ставит эту строку то первой фразой абзаца, то серединой) — и цитата считалась наравне с настоящей. Починил тем же ходом: `_v_backtike()` исключает backtick-цитаты из счёта, второй коммит. **Это третий случай одного класса за два дня** (уроки 23 и 31): проверка считает присутствие слова, а не его роль, и первым на неё натыкается документ, который эту проверку объясняет.
ВЕРДИКТ: класс устойчивый и заслуживает не правила, а привычки: **любая новая текстовая проверка калибруется на документе, который её описывает** — он по построению содержит все её маркеры в роли цитаты. Дешёвая форма: в фикстуру каждой такой проверки — ловушка «файл-заход, вводящий эту проверку, обязан молчать». У З8 такая ловушка появилась, у З9 её вписал исполнитель по ходу; сделать это обязательным пунктом при заведении проверки. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, рядом с клаузой про гейты.

### 38. Отсечка по дате в заходе была снята с хэндоффа, а не с шапки корпуса — и оказалась неверной на неделю
ЦЕНА: `kod_dobor-incidentov` велел отсекать записи «по 30.07, дате сборки корпуса». Живая шапка `KARTOTEKA-problem.md` говорит **дата данных 2026-08-05**: 05.08 заход `kod_razbros-po-fazam` пересобрал корпус заново по живым файлам. Работай исполнитель по букве — улов удвоился бы **заведомо известным**: в корпусе уже лежат уроки арок `sayt-drakon` (7 из 7), `teorver-plan` (10 из 10), `puti-i-volny` (6 из 10), то есть работа ПОСЛЕ названной даты. Спасло, что заход рядом требовал отсекать «по содержанию, а не по mtime», и исполнитель взял покрытие вместо даты — но это его решение, а не указание.
ВЕРДИКТ: дата в заходе — такое же число, как любое другое, и подпадает под `KONSTITUCIYA §10`: **пишется командой, читающей шапку источника**, а не переносится из хэндоффа. Здесь достаточно было `grep -i "дата данных" KARTOTEKA-problem.md`. Класс шире даты: **любой параметр, который источник объявляет о себе сам, берётся из источника, а не из документа о нём.** Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, гейт входа, рядом с вопросом 4 («предпосылка жива?»).
ДОСТАВЛЕНО: dobor-incidentov#1

### 39. Сборщик корпуса не индексирует собственную арку — и это свойство процедуры, а не пропуск
ЦЕНА: корпус 30.07 не открыл ни одного файла-захода арки `2026-07-30_dovodka-fabriki` — той самой, в которой собирался (`grep -c dovodka-fabriki` по индексу → 0). При этом **87 из 201 записи нового улова лежат именно там**, 80 из них в жёстком формате с готовой строкой `ЦЕНА:`. То есть почти половина добора — не «новое за неделю», а дыра метода: сборщик читает прошлое, своя арка в момент сборки не закончена и потому невидима.
ВЕРДИКТ: у сбора корпуса нужен второй проход — **по собственной арке, после её закрытия**. Иначе каждая сборка систематически теряет самую свежую и самую плотную часть материала: именно в арке сборки идёт разбор, и именно там уроки пишутся чаще всего. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.
ДОСТАВЛЕНО: dobor-incidentov#2

### 40. Греп-гейт уроков ищет `^ЦЕНА:` и не видит `**ЦЕНА:**` — «секций N, цен N» может сойтись при непокрытых
ЦЕНА: шаблон `^ЦЕНА: ` стоит в греп-гейтах шапки каждого `UROKI-FABRIKE.md` и в `check_uroki.py`. Живой замер по файлам-заходам: `ЦЕНА:` — 12, с отступом — 7, **жирным — 6**, в бэктиках — 1. У исполнителя это стоило **10 пропусков из 50**, найденных верификатором. Приёмка проверила по живым аркам, и там хуже: `2026-07-25_lekcia-1` — 19 секций, гейт видит **6** цен, а с жирными их **18**; `2026-07-16_teorkat-programma-dizajn` — 19 и 8 против 8. То есть гейт, которым арка объявляет себя закрытой, **на трёх арках из семнадцати судит не то, что думает**.
ВЕРДИКТ: расширить шаблон до `^\**ЦЕНА:` (и то же для `ВЕРДИКТ:`) в трёх местах разом — шапка шаблона `UROKI-FABRIKE.md`, `check_uroki.py`, докстринги. Правка на один символ в регулярке, но без неё гейт закрытия арки — украшение: он сходится ровно потому, что не видит части записей. Дом — `_generator/tools/check_uroki.py` + `_studio/zhurnal/_TEMPLATE-arka/UROKI-FABRIKE.md`.
ДОСТАВЛЕНО: dobor-incidentov#3

### 41. Заход выдан на ветку, которой не существует — второй холостой прогон за день по одной причине
ЦЕНА: контракт зоны `kod_etap-20-i-perenos` утверждал «ветка должна УЖЕ стоять»; `git rev-parse --verify` → `rc=1`, ветки нет вовсе, основная папка на `main`. Исполнитель встал на §0, сессия открыта впустую, семь скиллов не тронуты, потребовалась разблокировка владельцем. **Это второй такой прогон 07.08** — первый у `kod_gejt-vhozhdeniya` (урок 30). Причина одна: **заход описывает git-состояние по памяти составителя, а не по замеру**, притом что команда, дающая точный ответ, у составителя под рукой и стоит секунду.
ВЕРДИКТ: лечится инструментом, не правилом — правило «проверь адрес» есть и нарушено дважды за сутки. `bootstrap_zahod.py` перед выдачей прогоняет `git rev-parse --verify <ветка>` и `git show --name-only --format= <ветка-донор>` и **вклеивает ВЫВОД, а не утверждение**: несуществующая ветка тогда видна в момент сборки, а не на первом ходе исполнителя. Дом — `_generator/tools/bootstrap_zahod.py`, тем же ходом, что вердикт урока 30.
ДОСТАВЛЕНО: etap-20-i-perenos#5

### 42. Критерий назвал и число, и команду — но команду не прогнали, и она дала вчетверо меньше
ЦЕНА: условие 2 требовало снять охват командой `grep -c "НЕ покрыто"` при задании в **72** позиции; живой замер даёт **18** — документ устроен двумя форматами, и в пяти разделах из восьми одна строка шапки покрывает десятки позиций. Исполнитель, выполнивший условие буквально и честно, сдал бы **«охват 19 из 19, пустых клеток ноль» зелёным, пропустив 53 позиции из 72**. Это ровно та натяжка, которую сам заход объявил главной ловушкой и назвал дороже пропуска: она выглядит готовностью и закрывает вопрос, который останется открытым. Не выстрелило только потому, что исполнитель прогнал команду до работы и оспорил в `## ПЛАН`.
ВЕРДИКТ: `KONSTITUCIYA §10` требует писать команду вместо числа — этого мало. **Команда из критерия готовности прогоняется составителем на живом файле, и её вывод вклеивается в заход.** Непрогнанная команда опаснее голого числа: число вызывает сомнение, команда — доверие. Клауза 5 требует этого для греп-критериев; распространить на любую команду критерия, вместе с вердиктом урока 25. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, клауза 5.
ДОСТАВЛЕНО: etap-20-i-perenos#6

### 43. Гейт, который все считают рычагом, оказался мягким — и завышал покрытие дома
ЦЕНА: разбор МЕТЫ засчитал З6 линтера входа («числовое утверждение без соседней команды») машинным рычагом на 12 Σ кратности. Проверка живёт в `SOFT_CHECKS`; `main()` складывает только жёсткие (`bad += len(hard)`), прогон печатает ⚠ и отдаёт **rc=0**. То есть она ничему не мешает и ничего не останавливает. Покрытие дома завышалось **с 31 % до 36 %**, и поймал это верификатор, а не разбор. Рядом второй случай: З3 на зоне-папке зелёная — то есть ровно тот вход, ради которого её заводили, она пропускает. **Гейт, о котором говорят как о рычаге, а он мягкий, опаснее отсутствующего: он создаёт ощущение накрытия.**
ВЕРДИКТ: в инвентаре рычагов «мягкая проверка» — отдельный вид, наравне с процедурным, и в процент машинного не идёт. Механически: `pokrytie_rychagami.py` при рычаге вида «гейт» обязан спросить у инструмента, жёсткий ли он (`HARD_CHECKS` против `SOFT_CHECKS`), а не верить формулировке в доме. Плюс отдельная ревизия: какие ещё проверки объявлены гейтами, но не роняют `rc`. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.
ДОСТАВЛЕНО: razbor-meta#7

### 44. Два разных инцидента живут под одним id — и рычаг был приписан чужому смыслу
ЦЕНА: Раздел 5 картотеки не несёт колонки id; `karta_domov.py` нумерует его строки сквозным счётчиком **независимо** в срезе `вне-фаз` и в срезе `04.5-intervyu`, а счётчик покрытия складывает оба в один `Counter`. Проверено прогоном: `Р5-1` — это «число в шапке вписано руками» (3) **и** «аналитик сужает ответ владельца» (1); `Р5-2` — «критерий не может провалиться» (2) **и** «движок расшифровки теряет контент» (1). **5 Σ кратности из 7 были накрыты рычагом от чужого смысла.** По выжимке различить нельзя — id один; поймал верификатор. Любой следующий разбор напорется на то же место.
ВЕРДИКТ: чинить в источнике счёта, а не в разборе: либо колонка id в Разделе 5 картотеки, либо префикс среза в `karta_domov.py` (`Р5-внефаз-1` против `Р5-045-1`). Пока не починено — **всякое покрытие записей `Р5-*` считается непроверенным**, и это надо написать в самом доме, чтобы следующий разбор не унаследовал ошибку молча. Дом — `_studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py`.
ДОСТАВЛЕНО: razbor-meta#6

### 45. Критерий требовал прогона инструмента, который в названном месте не запускается
ЦЕНА: условие 5 захода `kod_razbor-meta` требовало «живой прогон на реальном счётчике». `pokrytie_rychagami.py` лежит в основной папке `materials` **untracked** (его туда уронила приёмка при проверке чисел), а нужная ему `classify_06tekst()` живёт в `karta_domov.py` **невлитой** ветки — прогон даёт `AttributeError`. Единственный обход: гонять счётчик из ЧУЖОГО worktree `materials-wt/wt-pereschet`, то есть **исполнитель вынужден лезть в зону другого захода, чтобы закрыть свой критерий**. Аналитик, писавший заход, снял замер «МЕТА — 0 %» именно оттуда и этого не заметил.
ВЕРДИКТ: тот же корень, что уроки 25 и 42, но новая грань: **команда критерия прогоняется составителем не где угодно, а в том месте, куда послан исполнитель.** Прогон из своей папки доказывает, что команда существует, и не доказывает, что она доступна ему. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, клауза 5, тем же ходом что вердикты 25 и 42.
ДОСТАВЛЕНО: razbor-meta#5

### 46. `git_zona.py check --zone .` молчит на корневых файлах — а это гейт 0 каждой приёмки
ЦЕНА: префикс `.` сравнивается со строкой пути, и имя вроде `OTVETY-INTERVYU-2026-08-07.md` с точки не начинается — фильтр его не видит. Живой прогон исполнителя: при незакоммиченном файле в корне `--zone .` отдал **✅**, а тот же `check` **без** `--zone` честно покраснел. То есть форма, которую составитель пишет в заход чаще всего («зона — весь репозиторий»), **тише формы без зоны**. Цена по автологу не измерена — найдено тестом, а не инцидентом; но класс дорогой по построению: `check --zone` стоит первым ходом КАЖДОЙ приёмки, и «зона чиста» — то, на чём приёмка решает читать отчёт дальше. Приёмка сегодня приняла на этом гейте девять отчётов.
ВЕРДИКТ: `--zone .` чинится как синоним «весь корень» — тем более что `korni.py` теперь умеет корневые файлы репозитория (правка того же захода). Пока не починено, **приёмка гоняет `check` без `--zone` вторым ходом**, иначе гейт 0 слеп ровно к тем файлам, которые чаще всего забывают закоммитить. Дом — `_generator/tools/git_zona.py`.
ДОСТАВЛЕНО: vhod-i-dver#3

### 47. Заход назвал команду по устаревшему CLI — синтаксис разошёлся с живым инструментом
ЦЕНА: §2.4 предписывал `git_zona.py zakryt-vetku zahod/gejt-vhozhdeniya` позиционным аргументом; живой инструмент требует `--branch`. Исполнитель посмотрел `--help`, поправил на месте и назвал это в отчёте — верно, это синтаксис, а не решение по существу. Стоило одного разбора; спасло то, что канон в каждом заходе велит сверять `--help`. Но у этого правила есть известная дыра: у части инструментов `--help` падает трейсбеком (урок 26), и там исполнителю сверять нечем.
ВЕРДИКТ: **команда, вписанная составителем в заход, прогоняется им же с `--help` при сборке** — это тот же вердикт, что у уроков 25, 42 и 45, и он повторяется четвёртый раз. Правило не приживается, потому что живёт как призыв; носитель — проверка в `check_zahod.py`: вытащить из текста захода вызовы `<инструмент> <подкоманда>` и сверить с его `--help`. Дом — `_generator/tools/check_zahod.py`, вместе с вердиктом урока 41.

### 48. Заход назвал дефект в файле, который сам же называл источником, — и не открыл его
ЦЕНА: `kod_gejty-kotorye-vrut` утверждал, что узкий шаблон `^ЦЕНА:` стоит «в греп-гейтах шапки И в `check_uroki.py`». Первое верно, второе — нет: `check_uroki.py` **bold-tolerant с 21.07**, и это записано в его собственном комментарии, строка 56 («Цена засчитывается и с жирным выделением»). Файл был назван якорем §0 того же захода — то есть составитель послал исполнителя его читать и сам не прочёл. Исполнитель проверил, скорректировал в `## ПЛАН` и правил только шаблон. **Второе завышение в том же заходе:** «ожидание — вырастут три арки»; выросла **одна** (`lekcia-1`, 6→18), и мой же замер, вклеенный в тот же заход, это показывал — у двух других жирной формы нет вовсе. Цена: разбор исполнителя вместо работы; ущерба нет, потому что он не поверил.
ВЕРДИКТ: это подкласс урока 33 («утверждение „этого нет“ — единственное, что нельзя писать по памяти»), и он показывает границу точнее: **опаснее всего утверждение о ФАЙЛЕ, который заход тут же велит открыть.** Исполнитель прочтёт и увидит расхождение — то есть заход теряет доверие целиком на первом же ходу. Правило узкое и дешёвое: **назвал файл якорем — открой его при сборке.** Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, гейт входа, вопрос 6.
ДОСТАВЛЕНО: gejty-kotorye-vrut

### 49. Восемь инструментов из двадцати двух не отвечают на `--help`, а канон требует его сверять в каждом заходе
ЦЕНА: замер приёмки командой по всей папке — инструментов **22**, падают или молчат на `--help` **9** (после правки этого захода — 8). Среди них `korni.py` — отдаёт `rc=0` и **пустой вывод**, то есть худший случай: выглядит успешным. При этом канон в каждом заходе велит «сверь `--help`, не угадывай», и на этих восьми сверять нечем. Уже оплачено дважды: урок 26 (разбор трейсбека вместо чтения строки) и урок 47 (заход назвал команду по устаревшему CLI, потому что справки не было).
ВЕРДИКТ: контракт `--help` заведён этим заходом в `check_tool_contract.py` — теперь дефект **не молчит**. Чинить сами восемь инструментов — отдельная работа, дешёвая и механическая (у большинства достаточно трёх строк в парсере), но менять поведение чужого кода попутно нельзя. Записать очередью: восемь имён названы поимённо в отчёте захода. Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`.
ДОСТАВЛЕНО: gejty-kotorye-vrut

### 50. Замер «11 файлов зовут движок» смешал вызов с упоминанием — работы там было вчетверо меньше
ЦЕНА: заход `kod_generator-po-imeni` объявил «одиннадцать скриптов зовут движок кодом», сняв число командой `grep -rln "_generator" --include="*.py"`. Команда честная, вывод честный, **вопрос неверный**: она считает файлы, где слово встречается, а не файлы, где движок вызывается. Живой разбор: вызовов по пути — **4 файла, 8 мест**; в остальных семи `_generator` живёт в докстрингах и комментариях вида «почему сборщик свой, а не генератор». Проверено приёмкой выборочно на трёх из семи — подтвердилось. Следствие было не косметическим: **условие 4 критерия требовало «прогнать до и после каждый из одиннадцати»**, то есть прогонять семь файлов, которых заход не касается. Исполнитель оспорил до работы и поправил охват на два числа — «переписано 4 из 4» и «путь не встречается в 7 из 11». Прогон нетронутого файла доказывает стабильность окружения, а не безвредность правки.
ВЕРДИКТ: **команда, снимающая охват, обязана отвечать на тот вопрос, который задаёт критерий.** `grep -l` по слову отвечает «где упоминается», а критерий спрашивал «где вызывается» — между ними вчетверо. Это третье завышение аналитика за сутки (уроки 36, 48 и это), и все три одного корня: команда выполнена, вопрос не сформулирован. Практическое правило: **прежде чем вклеить число в заход, произнести вслух, на какой вопрос отвечает команда, и сверить с формулировкой критерия.** Дом — `../../docs/KONSTITUCIYA.md §10`, вместе с вердиктом урока 36.
ДОСТАВЛЕНО: generator-po-imeni

### 51. Инструкция, которая сама себя размножает: скелет копируется в каждый новый дек вместе с устаревающей командой
ЦЕНА: `_generator/skeleton/brief.md:37` велит `python3 _generator/build_deck.py <дек>/src`, а `bootstrap_lekcia.py:274` копирует этот скелет в `src/` **каждого нового дека**. Сегодня инструкция верна; после выноса `_generator` в свой репозиторий она станет ложной — и к тому моменту разъедется по всем проектам, заведённым в промежутке. Из 27 живых `.md`-инструкций «запусти `python3 _generator/…`» эта **единственная самораспространяющаяся**: остальные 26 чинятся разово, эта размножается со скоростью заведения лекций. Найдено исполнителем попутно, цена пока нулевая — но она растёт сама, без чьего-либо участия.
ВЕРДИКТ: чинить **до** переноса папки, и первой из двадцати семи: скелет — не документ, а станок, печатающий документы. Общее правило, которого в каноне нет: **инструкция внутри шаблона, который копируется, стареет в N экземплярах**, и её адресация обязана быть косвенной (имя команды, а не путь). Дом — `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md`, пунктом «расшивка перед выносом `_generator`».
ДОСТАВЛЕНО: generator-po-imeni

### 52. Правило «проверь, что файл из указателя существует» неисполнимо: документы не объявляют базу пути
ЦЕНА: живой прогон гигиены передачи по `disciplina-arka §6в` на арке из 51 файла. Два паттерна отработали чисто (13 масок секретов → 0, заглушки → 0), третий — «файл из указателей существует» — **дал 22 «пропажи» от папки арки и 24 от корня, оба списка почти целиком ложные**. Причина: в одном `PLAN.md` соседствуют `06-tekst/DOK.md` (от папки конвейера), `_studio/docs/KARTA.md` (от корня) и голое `RESHENIYA.md` (от папки арки) — база не объявлена нигде. Единственная НАСТОЯЩАЯ битая ссылка (`KARTA-ZNANIYA.md` из чужой арки) утонула в обоих прогонах. Формулировка исполнителя точна: **чекер, написанный по этому правилу в лоб, был бы хуже отсутствия чекера — он приучает игнорировать собственный вывод.**
ВЕРДИКТ: правило исполнимо только вместе с конвенцией базы. Дешёвая форма: **относительный путь в наших документах всегда от корня репозитория**, а всё остальное пишется с явным `./` или не пишется. До введения конвенции пункт остаётся `⚠ ДЫРА` в теле скилла — так и сделано. Дом — `../../docs/kak-delat/STANDART-oformlenia.md` плюс `disciplina/skills/disciplina-arka/SKILL.md §6в`.
ДОСТАВЛЕНО: etap-20-i-perenos

### 53. Процедура коммита в скилле написана под `materials` и вне него не исполняется дословно
ЦЕНА: прогон 2 живой проверки — коммит зоны по `disciplina-git §3`, исполнено дословно в репозитории `disciplina`. Первые два хода отработали (`add` rc=0, 122 пути, вне зоны ноль; `commit` → `0c797ac`), **третья строка процедуры упала**: она зовёт `python3 _generator/tools/git_zona.py`, а `_generator/` в `disciplina` нет вовсе — «can't open file». Та же ловушка в прощальной подсказке `worktree add`. Скилл переносимый по замыслу — он и вынесен в плагин ради переносимости, — а его центральная процедура работает ровно в одном репозитории. Поймано только потому, что заход требовал **исполнить** дисциплину, а не прочитать: текст читается гладко.
ВЕРДИКТ: в переносимом скилле команда, зовущая инструмент чужого репозитория, пишется **только в форме с `GIT_ZONA_REPO`/`MAT`** — исполнитель дописал рабочую форму, замену (`doctor`) проверил запуском. Общее правило, которого не хватало: **скилл, объявленный переносимым, обязан быть прогнан хотя бы в одном чужом репозитории до того, как назван готовым.** Это и есть содержательный смысл «живого прогона» для скиллов. Дом — `disciplina/skills/disciplina-git/SKILL.md` + `../../docs/kak-delat/GIT-disciplina.md`.
ДОСТАВЛЕНО: etap-20-i-perenos

### 54. Команда владельцу дана относительным путём и выполнена из домашней папки
ЦЕНА: приёмка вложила в чат строку `python3 -m pip install --user -e _generator/ --no-build-isolation` без `cd`. Владелец выполнил её из `~` и получил `ERROR: _generator/ is not a valid editable requirement` — папки там нет. Один круг на диагностику, работа не пострадала. Класс известен и записан в каноне дословно: *«команда владельцу пишется абсолютным путём»*, цена названа трижды («три случая за две сессии, когда команда владельцу оказалась нерабочей»). Нарушила его приёмка — то есть тот, кто это правило цитирует исполнителям.
ВЕРДИКТ: правило есть, носителя нет — и это четвёртый его отказ. Дешёвый носитель: **любая команда, уходящая владельцу в чат, начинается с `cd <абсолютный путь>`** — не «пиши абсолютный путь», а «первой строкой всегда `cd`». Формулировка, которую нельзя выполнить наполовину. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md §Два канала запуска`.

### 55. Гейт зоны требует префикс, а бывает зона «всё, что вне git» — и приёмка краснеет на полностью сделанной работе
ЦЕНА: заход `svesti-den` выполнен целиком (54 урока введены в git, 11 веток из 12 влиты, пакет поставлен, 15 папок сняты), а `priyomka.py` дала ❌: Г0 не нашёл строки `git_zona.py check --zone <префикс>`, а печать C1 объявила «вне зоны» двадцать файлов, которые слияния трогают по определению. Исполнитель честно написал, что `check --zone` не гонял, — гонять было нечего: зона этого захода не префикс пути, а предикат «незакоммиченное на момент старта». Аналитик проверял вручную шестью командами, круг приёмки удвоился.
ВЕРДИКТ: класс заходов «свести/подмести/влить» имеет зону, которую нынешний контракт выразить не может, и гейт на них систематически ложно-красный. Дешёвая форма: **в контракте зоны разрешить значение `--zone СВОДКА`** — Г0 тогда требует не префикс, а другого доказательства: `git status --short` до и после, приложенные дословно, и перечень не взятых файлов с причиной. Пока формы нет — такой заход принимается вручную, и это записывается в ПРИЁМКУ явно, а не молча. Дом — `_generator/tools/priyomka.py` + `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`.
ДОСТАВЛЕНО: нет

### 56. Заход велел подмести оба репозитория, а инструмент подметания живёт в одном
ЦЕНА: §2.4 требовала снять рабочие папки «в обоих репозиториях» командами `git_zona.py poteri` / `zakryt-vetku`. В `materials` снялось 15 из 21, в `disciplina` — ноль из семи: инструмента там нет вовсе, а голый `git worktree remove` без страховки `poteri` исполнитель делать отказался и был прав. Семь папок остались, вопрос ушёл владельцу. Это уже второй отказ той же природы за день — урок 53 поймал то же самое на процедуре коммита.
ВЕРДИКТ: `GIT_ZONA_REPO` сделал инструмент способным смотреть в чужой репозиторий, но заходы этого до сих пор не используют — пишут команду так, будто репозиторий один. Проверяемая форма: **шаг захода, названный «в обоих репозиториях», обязан привести КОМАНДУ для каждого из них отдельно**; одна команда на два репозитория — брак сборки, и линтеру входа это по силам (лексически: «обоих» рядом с единственным блоком кода). Дом — `_generator/tools/check_zahod.py` + `../../docs/kak-delat/GIT-disciplina.md`.
ДОСТАВЛЕНО: нет

### 57. Гейт сборки резолвит пути от своего репозитория, а заход всё чаще работает в чужом
ЦЕНА: первый живой прогон `check_sborki.py` на свежесобранном `kod_evals.md` дал **6 красных С3, из них 4 ложных**: пути `skills/slajdy/evals/evals.json`, `zhurnal/STANDART-SKILLA.md`, `KOLLIZII-opisanij.md` объявлены несуществующими. Они существуют — в `disciplina`, где заход и работает; гейт резолвил от трёх баз, и ни одна из них не была репозиторием из блока «МЕСТО РАБОТЫ». Проверка С2 при этом молчала правильно (`GIT_ZONA_REPO` в заходе назван) — то есть гейт **знал** про чужой репозиторий одной проверкой и не знал другой. Ещё 2 красных пришли из шаблонного абзаца про `register_doc.py`, а не из авторского текста.
ВЕРДИКТ: у С3 базы фиксированы, а `GIT_ZONA_REPO` сделал место работы переменной — это расхождение будет расти, заходов в `disciplina` уже больше, чем в `materials`. Дешёвая форма: **четвёртая база С3 — репозиторий, названный в «МЕСТО РАБОТЫ»**, добывается тем же разбором, что уже кормит С2. Третья находка того же прогона: **С6 краснеет на УСЛОВНОМ наклонении** — «дом не существует → откажи с причиной» это инструкция исполнителю, а не утверждение о мире; лечится тем же способом, что уже применён к цитатам и таблицам. Отдельно: **пути из шаблонной части не судить вовсе** — они одинаковы во всех 176 файлах корпуса и красят каждый. Дом — `_generator/tools/check_sborki.py`.
ДОСТАВЛЕНО: нет

### 58. Образец формы врал про самого себя, и это заметил только исполнитель с пустым контекстом
ЦЕНА: `skills/slajdy/evals/evals.json` — единственный существующий комплект оценок, на который заход `evals` прямо послал как на образец формы. Поле `skill_name` в нём — `html-slides-studio`, то есть **чужой внешний скилл**, а промпт первого сценария («Сделай презентацию на 5 слайдов…») дословно совпадает с тем, на что `slajdy` в своём `description` запрещает себе срабатывать. Файл пережил сборку скилла, приёмку и мою сборку захода: я послал на него как на эталон, не открыв. Поймано на первом же чтении исполнителем, который не знал истории и потому прочёл буквально.
ВЕРДИКТ: **«образец» — это утверждение о мире, и оно подлежит той же проверке, что остальные.** Заход, который посылает на эталон, обязан назвать его КОМАНДОЙ, дающей проверяемое свойство (здесь хватило бы `grep skill_name`), а не словом «как в соседнем файле». Это ровно предмет `check_sborki.py` и его случая С3 — расширяется до «якорь, названный эталоном, сверяется по содержанию, а не по существованию». Дом — `_generator/tools/check_sborki.py` + `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`.
ДОСТАВЛЕНО: нет

### 59. Дверь регистрации не работает ни в одном worktree — а контракт зоны worktree предписывает
ЦЕНА: заход `evals` не смог зарегистрировать свой документ, `rc=2`, два независимых дефекта. **Д1:** `отн_путь()` считает путь от ОСНОВНОЙ папки репозитория, файл захода лежит в `disciplina-wt/wt-evals/…` — физически вне, `relative_to()` бросает, дверь отказывает. То есть требование «завёл `.md` — зарегистрируй тем же ходом» **неисполнимо штатно для любого worktree-захода**, а worktree предписан контрактом зоны каждому параллельному заходу. **Д2:** `GIT_ZONA_REPO` переключает `REPO`, но НЕ таблицу корней: `префиксы_двери()` отдаёт корни `materials` плюс `disciplina/`, а внутри `disciplina` такой папки нет — дверь принимает там ровно один путь, `README.md`. Оба воспроизведены вызовами, а не предположены.
ВЕРДИКТ: это ровно тот класс, ради которого дверь заводилась (27 обходов `--no-verify`, 48 % всей боли с коммитами) — и он вернулся с другой стороны. **Дверь обязана принимать путь внутри worktree того же репозитория** (резолвить через `git rev-parse --show-toplevel`, а не через константу `REPO`), и **таблица корней обязана переключаться вместе с `GIT_ZONA_REPO`**. До починки заход, работающий в `disciplina`, регистрировать документы не может — это надо писать в заходе прямо, а не обещать обратное. Дом — `_generator/tools/korni.py` + `register_doc.py`.
ДОСТАВЛЕНО: нет

### 60. Ни одно описание скилла не написано в грамматике возражения — а владелец возражает постоянно
ЦЕНА: холодный прогон 18 живых фраз владельца: две (**11** «накапливали эту картотеку, не хотим её вот так вот разрезать» и **17** «почему мы должны выбирать именно Riehl и Фетисова?») не подняли **ни одного скилла из 68** — ни нашего, ни установленного. Довод субагента дословно: «фраза — не запрос на действие, а возражение; ни один скилл не описывает триггер вида „пользователь защищает картотеку“». Все триггер-листы написаны императивами заказа («сделай», «заведи», «проверь»). При этом граничные случаи — те, где скилл обязан ОТКАЗАТЬ и поднять вопрос, — владелец формулирует именно возражением. То есть дисциплина молчит ровно там, где она нужнее всего.
ВЕРДИКТ: класс, а не два случая. **Триггер-лист скилла обязан содержать хотя бы одну форму возражения/сомнения**, а не только форму заказа. Цена правки названа честно: возражательные триггеры усилят коллизии внутри нашей же группы, где общих слов и так много, — поэтому правится не россыпью, а один раз, вместе с разведением омонимов (урок 61). Дом — `disciplina/skills/*/SKILL.md` через `zhurnal/STANDART-SKILLA.md`.
ДОСТАВЛЕНО: нет

### 61. Разбор коллизий видит общий ТРИГГЕР и не видит общий ТЕРМИН при разных денотатах
ЦЕНА: слово «источник» значит разное в двух наших скиллах: у `disciplina-istochnik-pravdy` это источник ПРАВДЫ (где живёт документ), у `reserch-cikl` — учебный источник (книга). На фразе 17 субагент отверг первый дословно за это: «совпадает лишь словом „источник“». `KOLLIZII-opisanij.md` строит разбор на пересечении триггерных ФРАЗ и такую пару не показывает вовсе — метод её не покрывает по построению. Омонимы внутри группы из семи скиллов, написанных одним автором за неделю, — не редкость, а норма.
ВЕРДИКТ: в метод разбора коллизий добавляется третий класс — **«омоним»: общий термин при разных денотатах**, наравне с жёсткой и смысловой коллизией. Проверяется тем же способом, что уже есть: пересечение существительных описаний с последующей проверкой «об одном ли предмете». Дом — `disciplina/KOLLIZII-opisanij.md`.
ДОСТАВЛЕНО: нет

### 62. Заход послан пользоваться инструментом, который лежит в невлитой ветке
ЦЕНА: §2.2 захода `hvosty` («встрой `check_sborki.py` в `bootstrap_zahod.py`») **не выполнен вовсе**: инструмента нет ни в базе, ни в ветке исполнителя — он живёт только в `zahod/gejt-sborki`, которую приёмка приняла и не влила. Исполнитель поступил правильно (слить вторую ветку контракт зоны не разрешал, копировать в обход — тем более) и потерял на этом ход. Виновата сборка: я принял ветку утром, назвал инструмент готовым и через час послал на него другой заход, ни разу не спросив, доехал ли он до базы. Ровно тот же класс, что уроки 30 и 41 — «заход опирается на состояние мира, которого нет», — но с новой поверхностью: не ветка и не worktree, а **артефакт принятой, но невлитой работы**.
ВЕРДИКТ: **принятие ≠ доступность.** Приёмка обязана заканчиваться одним из двух: ветка влита, либо в `PUT-K-GOTOVOMU.md` появляется строка «артефакт X существует только на ветке Y». Проверяемая форма для сборщика: **заход, называющий инструмент, обязан назвать команду, которая его находит** (`git ls-files <путь>` или `test -x`), — и это ровно предмет `check_sborki.py`, случай С3, расширенный с путей-якорей на пути-инструменты. Дом — `_generator/tools/check_sborki.py` + `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`.
ДОСТАВЛЕНО: нет

### 63. Двадцать девять адресов из шестидесяти одного ведут не туда — и узнали мы это только когда завели доставку
ЦЕНА: первый же прогон `dostavit_urok.py --plan` на живом файле: адрес дома разобран у 60 из 61, **дом не существует на диске у 9**, **дом оказался файлом-указателем («переехало») у 20**. То есть у 29 уроков из 61 вердикт называет дом, куда доставка либо не пройдёт, либо ляжет мимо канона и при этом будет выглядеть успешной: обе машинные проверки (адрес разобран + файл существует) такой дом одобряют. Все эти вердикты писала приёмка — то есть я, — и каждый выглядел точным в момент написания. Проверить их было нечем, пока переезд оставался прозой.
ВЕРДИКТ: **адрес дома — такое же утверждение о мире, как ветка или путь-якорь, и протухает так же.** Дешёвая форма уже есть и уже работает: `--plan` печатает состояние каждого дома, а `--gejt` краснеет на недоставленном. Остаётся встроить: **приёмка, дописывая вердикт с адресом, гоняет `--plan` тем же ходом** — 29 протухших адресов копились неделю по одному, и каждый стоил бы секунды в момент записи. Отдельно: файл-указатель обязан отказывать, а не принимать доставку — исполнитель это уже сделал (`rc=2`). Дом — `_generator/tools/dostavit_urok.py` + `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`.
ДОСТАВЛЕНО: нет

### 64. Зона захода не включала файл, правку которого этот же заход предписывал
ЦЕНА: §2.3 захода `dver-urokov` велел встроить вызов `--plan` в `zakryt_sessiyu.py`, а контракт зоны этот файл не называл — я перечислил четыре пути и забыл пятый. Исполнитель поступил правильно: не тронул, объявил в отчёте и приложил готовый патч с проверенным якорем (`grep -c` → 1). Работа не потеряна, но встройка не сделана и уехала в следующий заход. Класс тот же, что урок 62 («принятие ≠ доступность»), только с другой стороны: там инструмент был вне мира исполнителя, здесь — вне его прав.
ВЕРДИКТ: **у сборщика есть проверка, которую он не делает: пройти по §2 и убедиться, что каждый файл, чью правку заход предписывает, назван в зоне.** Это лексически проверяемо линтером входа: путь в повелительном обороте («встрой в X», «поправь X», «допиши в X») обязан встречаться в строке ЗОНЫ. Дом — `_generator/tools/check_zahod.py`.
ДОСТАВЛЕНО: нет

### 65. Вердикт гейта устарел через два часа после написания, а прожил сутки как действующий
ЦЕНА: `GEJT-VHOZHDENIYA.md` заключил «гасить нельзя ни одного, непокрыто 72» — коммит `a9137c0`, 06.08 18:22. Перенос ровно этих 72 позиций лёг коммитом `0c797ac` в 20:38 того же дня и влит в `main` в 21:18. Гейт никто не перегонял. 07.08 я предъявил владельцу «14 непокрытых у `html-slides-studio`, 9 у `popsci-research`» как текущее состояние и **рекомендовал не гасить**; владелец погасил вопреки — и оказался прав. Живой разбор дал **63 из 72 «уже добрано»**, реальной работы осталось 9. Сутки решения принимались по числу, протухшему через два часа после рождения.
ВЕРДИКТ: это буквально Р34 («отметка „закрыто“ без ДАТЫ ДАННЫХ доверия не заслуживает»), нарушенная тем, кто её цитирует. Дешёвая форма, которой не хватало: **вердикт гейта хранит не только дату, но и КОММИТ, на котором снят**, и любое его цитирование обязано сверить, не ушла ли ветка вперёд (`git log <коммит>..main --oneline -- <области гейта>`). Одна команда; на этом файле она вернула бы шесть коммитов. Дом — `disciplina/GEJT-VHOZHDENIYA.md` + `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`.
ДОСТАВЛЕНО: нет

### 66. Приёмка объявила живым дефектом строку, которая была пометкой о его починке
ЦЕНА: 07.08 я грепнул `html-slides-studio` по нашим скиллам, увидел попадание в `slajdy/references/dok-07-verstka.md:68` и объявил владельцу срочную находку: «живой адрес в погашенный скилл, файл читается скиллом по контракту». На деле строка 68 — **пометка о починке**: «⚠ Адрес переставлен 2026-08-06: стояло `html-slides-studio/references/…`». Починено коммитом `d4848b1` за сутки до. Цена: срочное сообщение владельцу по несуществующему поводу и дописка в работающий заход. Смягчилось только тем, что прочёс, назначенный по ложному поводу, нашёл **три настоящих** битых адреса в файле, которого я не называл (`plakatnyj-stil.md`).
ВЕРДИКТ: это дословно тот класс, который я сам записал в слепые зоны `check_sborki.py` — **лексика не отличает утверждение от пояснения о нём**, — и я совершил его вручную, без инструмента, через час после того, как принял заход про это. Проверяемая форма для приёмки: **находка по грепу не уходит владельцу, пока не прочитана строка целиком и не проверено `git log -1 -- <файл>`**. Два хода, секунды. Дом — `../../../_meta/disciplina/RUKOVODSTVO-zahodami.md`, раздел про приёмку.
ДОСТАВЛЕНО: нет

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

---

## Пункты очереди заходов, доставленные `dostavit_urok.py`

> Дословные находки из секций `## ВОПРОСЫ` файлов-заходов — голос исполнителя, который об это споткнулся, не редактора. Разбирает их человек: находка описывает, что мешало, но задачей сама не становится.
> Строка `ДОСТАВЛЕНО: <заход>#<N>` внизу каждой записи — та же метка, что стоит в источнике; по ней Г7 `priyomka.py` проверяет, что доставка не объявлена ложно.

#### etap-20-i-perenos#5

**5. УРОК ФАБРИКЕ. Заход выдан на ветку, которой не существует — второй холостой прогон подряд по одной причине: состояние репозитория в заходе не сверено с живым репозиторием перед выдачей.**

Контракт зоны утверждает «ветка `zahod/etap-20-i-perenos` … должна УЖЕ стоять»; `git rev-parse --verify` → rc=1, ветки нет вовсе, основная папка на `main`. Ровно та же болезнь, что урок 30 у `kod_gejt-vhozhdeniya.md` (там зона слияния в тексте разошлась с живой веткой): заход описывает git-состояние по памяти составителя, а не по замеру.

ЦЕНА: холостой прогон исполнителя — сессия открыта, семь скиллов не тронуты, задание требует повторной выдачи; на 07.08 это уже второй такой прогон подряд. Лечится одной строкой в `bootstrap_zahod.py`/приёмке захода: перед выдачей прогнать `git rev-parse --verify <ветка>` и `git show --name-only --format= <ветка-донор>` и вклеить ВЫВОД, а не утверждение.
   ДОМ: _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: etap-20-i-perenos#5 · 2026-08-07

#### etap-20-i-perenos#6

**6. УРОК ФАБРИКЕ. Критерий готовности назвал число и назвал команду, а команда даёт другое число — и расхождение в четыре раза в сторону «зелёного».**

Условие 2: охват снять `grep -c "НЕ покрыто"`; замер даёт 18 при задании в 72 (разбор — в `## ПЛАН`). Канон требует «пиши не число, а команду, которая его считает» (`KONSTITUCIYA §10`), но команду никто не прогнал на живом файле: гейт устроен двумя форматами, и в пяти разделах из восьми одна строка шапки покрывает десятки позиций.

ЦЕНА: не реализована — поймана до работы, но её размер измерим и он крупный: исполнитель, выполнивший условие 2 буквально и честно, сдал бы «охват 19 из 19, пустых клеток ноль» зелёным, пропустив 53 позиции из 72. Это ровно та натяжка, которую заход в §2.3 объявил главной ловушкой и дороже пропуска. Реально потрачено: один прогон исполнителя на вывод правильного способа счёта. Лечится правилом «команду из критерия готовности составитель прогоняет на живом файле и вклеивает её вывод в заход» — то есть §10 распространить с текста захода на его критерий.
   ДОМ: _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: etap-20-i-perenos#6 · 2026-08-07

#### etap-20-i-perenos#7

**7. УРОК ФАБРИКЕ. Условие «вызови переписанный скилл по имени» непроверяемо по построению: скиллы грузятся из опубликованного плагина, а работа исполнителя лежит в worktree.**

Условие 6 требует живого прогона вызовом по имени. Замер: плагин `disciplina` установлен с GitHub (`d1-d57/disciplina`), кэш `~/.claude/plugins/marketplaces/disciplina/` побайтово равен `main` (`9c66c2c`); тела там 468 и 182 строки против 469 и 202 в рабочем дереве. То есть вызов по имени грузит **опубликованную** редакцию, и никакая работа исполнителя — ни в worktree, ни даже в основной папке до пуша и переустановки плагина — в него не попадает. Условие, исполненное буквально, дало бы зелёный отчёт о прогоне ЧУЖОГО кода.

ЦЕНА: реализовалась бы полностью — «живой прогон» подтвердил бы работоспособность старой редакции и был бы записан как проверка новой; поймано только потому, что исполнитель сверил размеры тел перед вызовом. Потрачено: один круг на выяснение источника плагина и на замену процедуры (прогон сделан исполнением дисциплины новых тел на настоящих объектах — и дал две реальные находки, которых вызов по имени не дал бы вовсе). Лечится строкой в шаблоне захода: **«скилл проверяется прогоном по телу из рабочего дерева; вызов по имени проверяет опубликованную редакцию и для приёмки правки не годится»** — и заодно ответом на вопрос, как вообще выкатывать правку скилла в живой Claude.
   ДОМ: _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: etap-20-i-perenos#7 · 2026-08-07

#### evals#1

1. **Дверь `register_doc.py` для `disciplina` не работает — два независимых дефекта, оба воспроизведены.** `PROGON-triggerov.md` остался НЕзарегистрированным; обхода не делал, в `README.md` руками не писал.
   **Д1 — дверь не понимает worktree.** `отн_путь()` считает путь относительно `REPO`, то есть ОСНОВНОЙ папки репозитория. Файл в рабочей папке захода лежит по `disciplina-wt/wt-evals/...`, физически вне `disciplina/` → `relative_to()` бросает `ValueError` → «❌ Путь вне репозитория», `rc=2`. Проверено: `отн_путь('/…/disciplina-wt/wt-evals/zhurnal/PROGON-triggerov.md')` → `None`, а `отн_путь('/…/disciplina/zhurnal/PROGON-triggerov.md')` → `'zhurnal/PROGON-triggerov.md'`. Цена класса: контракт зоны ПРЕДПИСЫВАЕТ worktree, и в нём дверь недоступна в принципе — то есть требование «зарегистрируй тем же ходом» для любого worktree-захода неисполнимо штатно. Это ровно тот класс дефекта, ради которого дверь и заводилась (27 обходов `--no-verify`).
   **Д2 — таблица корней не переключается вместе с `GIT_ZONA_REPO`.** `REPO` переключается верно (`korni.REPO` → `/…/disciplina`), а `КОРНИ` — нет: `korni.префиксы_двери()` возвращает `('_studio/', 'obzory/', '_illustracii/', '_meta/', 'disciplina/')`, то есть корни репозитория `materials` плюс префикс `disciplina/`. Внутри репозитория `disciplina` папки `disciplina/` нет (в корне `skills/`, `doma/`, `zhurnal/`, `tools/`, `_studio/`, `arhiv-skillov/`, `zagotovki/`). Проверено: `korni.дверь_принимает('zhurnal/PROGON-triggerov.md')` → `False`, `korni.корень_пути(...)` → `None`. Значит и из основной папки та же команда отказала бы — дверь для `disciplina` принимает сегодня ровно один путь, `README.md` в корне.
   **Почему это урок фабрике, а не мой случай:** заход утверждал «дверь для `disciplina` заведена решением владельца и должна работать». Предпосылка ложна на двух уровнях сразу, и ни один из них не виден до запуска.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: evals#1 · 2026-08-07

#### gejt-vhozhdeniya#1

```
1. Заход не назвал РЕПОЗИТОРИЙ работы, а контракт зоны писал пути так, будто он тот же, где лежит файл-заход. Все пути вида `disciplina/...` исполнитель искал внутри `materials` — там их нет ни в одной из 14 веток. Настоящий дом — отдельный репозиторий `~/Documents/GitHub/disciplina` со своими worktree.
   ДОМ: _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: gejt-vhozhdeniya#1 · 2026-08-07

#### gejty-kotorye-vrut#1

1. Мета-фикстура `_generator/tools/fixtures/tool_contract/PROGNAT.sh` («ПРОВЕРКА 8: молчит, когда чисто») подменяет `TOOL_CONTRACT_HOME` на пустой временный каталог и в этой песочнице теряет РЕАЛЬНОЕ объявление охвата (`TOOL-CONTRACT-COVERS`) любого инструмента, кроме проверяемого правкой — если он ОДНОВРЕМЕННО застейджен с `check_tool_contract.py`, тест ложно краснеет («ЛИНТЕР ШУМИТ НА ЧИСТОМ»), хотя реальный (не изолированный) хук молчит корректно. Живая цена: заход `kod_gejty-kotorye-vrut.md` (06.08) хотел одним коммитом сдать `check_uroki.py` + `check_tool_contract.py` (оба из зоны), уткнулся в это и был вынужден разбить на два коммита — единственное отклонение от критерия готовности «ровно четыре пути». Баг воспроизведён и на оригинальной (HEAD) версии `check_uroki.py` — не нов, просто раньше эту пару файлов никто не стейджил вместе.
   ДОМ: _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: gejty-kotorye-vrut#1 · 2026-08-07

#### plagin-dymovoy-test#1

1. §0 «точка отката» буквально велит `git add` по ВСЕЙ папке зоны (директория + файл-заход), но зона общая — на момент захода в ней уже лежали чужие незакоммиченные правки двух других исполнителей (`kod_karta-incidentov.md` modified, `kod_shvy-lenty-reserch.md` untracked). Исполнение буквы §0 сгребло бы их в мой чекпоинт-коммит — ровно сценарий, который §4 называет ценой 21.07 («голый `commit` подмёл чужой индекс»). §0 и §4 в этой точке противоречат друг другу так же, как в 26.07–30.07 противоречили «зарегистрируй» и «`docs/` read-only» (см. историю в §Дверь этого же захода). Я использовал разрешённую тем же пунктом альтернативу («или zip») и не тронул чужие пути; но следующему исполнителю с грязной общей зоной стоит явно указать в шаблоне захода: чекпоинт — либо zip, либо `git add` СТРОГО по своим путям, никогда по всей папке зоны.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: plagin-dymovoy-test#1 · 2026-08-07

#### rasshivka-kornej#3

3. **`sweep_dead_locks` судит лок ПО ВОЗРАСТУ, а не по наличию процесса** (`git_zona.py`, `DEAD_LOCK_SEC = 300`). Живой git-процесс отсутствовал, лок был 0 байт и брошен, но моложе пяти минут — инструмент правомерно его не снял и напечатал «рядом кто-то коммитит», хотя не коммитил никто. Стоило 90 секунд ожидания и одного ложного диагноза. Лечится проверкой, жив ли владелец лока, а не только его возрастом.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: rasshivka-kornej#3 · 2026-08-07

#### svesti-metu#2

2. ### Пункт очереди адресован захода не долетел до текста задачи
   `kod_vynos-meta.md` ВОПРОСЫ п.11 называет ДОМ ровно этого захода («заход по `_generator/tools/`» — это я, `svesti-metu`) для находки `check_tool_contract.py:51` (та же, что в п.1 выше). Но текст МОЕЙ задачи (`## 2. ЗАДАЧА`, §A/§B/§C) её не содержит вовсе — только фикстуру. Нашёл её заново независимо, через §3-верификатор, не потому что заход её назвал.
   ЦЕНА: адрес доставки был назван верно, но содержание не доехало до тела задачи — если бы верификатор не был предписан отдельным ходом, находка молча потерялась бы ещё на один заход, несмотря на то что ДОМ был указан правильно.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: svesti-metu#2 · 2026-08-07

#### vynos-illustracii#4

4. Заход класса «вынос» объявляет зону по источнику переноса, а работа лежит у ссылающихся — урок с ценой в `## УРОКИ ФАБРИКЕ`.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: vynos-illustracii#4 · 2026-08-07

#### vynos-meta#5

5. **Названный долг, унаследованный: битая ссылка внутри перенесённого канона.** `_meta/disciplina/RUKOVODSTVO-zahodami.md:13` зовёт `content-studio/assets/corpus/texts-corpus.md` — такого файла в репозитории нет и не было. Доказано, что долг НЕ мой: `git show HEAD:_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` содержит ту же ссылку до переезда. Не чинил: правильный адрес угадывать нельзя, а править чужой смысл — второй риск.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: vynos-meta#5 · 2026-08-07

#### vynos-meta#6

6. **Канон печатает команду с путём ОТ ДОКУМЕНТА и подписью «(из корня репо)» — копипаст даёт неверный путь.** `_meta/disciplina/RUKOVODSTVO-zahodami.md:71` и `:309`: `python3 ../../_generator/tools/register_doc.py …` с пометкой «(из корня репо)». Дефект был и до переезда (там стояло `../../../_generator/…` с той же пометкой) — я сохранил обе половины ровно как были, потому что чинить смысл в чужом тексте не моя зона. Цены не знаю ⇒ в `UROKI` не пишу, кладу как наблюдение.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: vynos-meta#6 · 2026-08-07

#### vynos-meta#7

7. **КРИТЕРИЙ 4 НЕПРОВЕРЯЕМ В ТОМ ПОРЯДКЕ, КОТОРЫЙ ПРЕДПИСЫВАЕТ САМ ЗАХОД — и это ловушка для следующего исполнителя.** §4 говорит «КОММИТ — ПОСЛЕДНИЙ ХОД ЗАХОДА», критерий 4 говорит «`git log --follow _meta/disciplina/ARKA.md` показывает коммиты до переезда». До коммита `--follow` по несуществующему в истории пути возвращает **ноль** — не ошибку, а пустой вывод с `rc=0`. **ЦЕНА в этом прогоне:** прогнал критерий 4 по порядку, получил `0 коммитов` на всех четырёх файлах при `rc=0` и был в одном шаге от того, чтобы записать в отчёт провал критерия. Спасло только то, что пустой вывод при `rc=0` — ровно тот случай, про который предупреждает §1 («код возврата первым»), и я полез разбираться, а не поверил. Лечение — одна строка в критерии: «пункт 4 проверяется ПОСЛЕ коммита, до коммита он пуст по построению».
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: vynos-meta#7 · 2026-08-07

#### vynos-meta#8

8. **Число из задания не воспроизводится названным инструментом.** §A говорит «мета — самый большой класс (233 записи `вне-фаз`)». Живой прогон `python3 _studio/zhurnal/2026-07-30_dovodka-fabriki/srez_po_faze.py вне-фаз` даёт **58 классов при суммарной кратности 724** (дата данных 2026-08-06); ни 233 записи, ни 233 чего-либо ещё эта команда не печатает. Вывод задания («самый большой класс») подтверждается — ближайший `02-reserch` даёт 312, — а само число 233 взято из какого-то третьего среза и в `POKRYTIE.md` я его не перенёс: вписал команду и то, что она печатает.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: vynos-meta#8 · 2026-08-07

#### dva-vhoda#5

5. **Контракт зоны требует регистрировать любой новый `.md`, а дверь отказывает на файле в корне репозитория** (`rc=2`, «дверь судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`»). Нужна строка-исключение, иначе следующий исполнитель прочитает отказ как повод к `--no-verify`. Развёрнуто с ценой — в `## УРОКИ ФАБРИКЕ`.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: dva-vhoda#5 · 2026-08-07

#### gejt-sborki#3

3. Строка 5 таблицы §2.2 («подкоманда инструмента существует | `<инструмент> --help`, поиск подкоманды в выводе | 47») недоспецифицирована: верхний `--help` доказывает лишь существование подкоманды, а урок 47 был про ФОРМУ аргумента (`zakryt-vetku <ветка>` позиционным против `--branch`). По букве таблицы урок 47 не ловится. Сделал строже — справка ПОДКОМАНДЫ плюс сверка usage.
   ДОМ: `<эта арка>/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: gejt-sborki#3 · 2026-08-07

#### gejt-sborki#4

4. Верификатор безопасности (§3) нашёл класс уязвимости, который канон фабрики нигде не называет, а он общий для любого инструмента, читающего чужой текст: **приклеенное значение короткой опции** (`grep -cf/путь`, `git log -O/путь`). Токен начинается с `-`, `=` не содержит, и любая проверка «путь-аргумент внутри репозитория», написанная по очевидной схеме, его пропускает целиком. Второй такой же общий класс — **катастрофический бэктрекинг в регулярке разбора**: 3 КБ обычной прозы вешали гейт навсегда, и объявленный таймаут не помогал, потому что висел не подпроцесс, а `re`. Оба лечения — в `check_sborki.py`, но правило общее.
   ДОМ: `<эта арка>/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: gejt-sborki#4 · 2026-08-07

#### hvosty#3

3. Адрес в моём заходе (§0 п.1: `~/Documents/GitHub/disciplina/zhurnal/PROGON-triggerov.md`) не существует — файл физически лежит только в `disciplina-wt/wt-evals/zhurnal/PROGON-triggerov.md` (ветка `zahod/evals`, не влита в `disciplina/main`). Прочитан оттуда. Когда `zahod/evals` вольётся, адрес станет верным сам; до тех пор следующему заходу, которому нужен этот файл, полезно знать, что искать в worktree, а не в каноне.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
ДОСТАВЛЕНО: hvosty#3 · 2026-08-07

#### hvosty#4

4. **Урок фабрике (Находка А):** заход §2.1 ссылался на таблицу PROGON §2 («готовым списком владельцу»), но не на его же §3 тремя абзацами ниже («отключать сейчас нельзя»). Оба абзаца — в ОДНОМ файле, прочитанном аналитиком при составлении захода (файл написан раньше, в тот же вечер). Цена, если бы заход исполнили по букве: 3 живые фразы владельца (№15, №16, №19 прогона) остались бы БЕЗ ВСЯКОГО носителя — `html-slides-studio`/`popsci-research` подхватывают их временно, пока законные получатели (`slajdy`, `reserch-cikl`) в статусе черновика.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
ДОСТАВЛЕНО: hvosty#4 · 2026-08-07

#### hvosty#5

5. **Урок фабрике (Находка Б):** заход §2.2 писал «`check_sborki.py` собран и принят 07.08» так, будто «принят приёмкой» = «доступен в моей ветке». Между ними разрыв: принятая работа лежит в невлитой ветке, а новый worktree форкается от базы, где её ещё нет. Цена при исполнении по букве: код, встраивающий вызов несуществующего файла, либо не запустился бы вовсе, либо (хуже) подтолкнул бы к копированию файла мимо `git` в обход контракта зоны — то же слияние, только без двери и без истории.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
ДОСТАВЛЕНО: hvosty#5 · 2026-08-07

#### karta-incidentov#1

1. Классификация субагентом по КОРОТКОМУ общему названию темы группы (вместо конкретной цитаты/инстанса) даёт артефактно завышенную долю «не могу решить» — не потому что записи спорные, а потому что общий ярлык темы звучит шире, чем конкретный инстанс. На `KRATNOST-vladelca.md` (группы `КРВ-NNx`, тема одна на все фазы-подгруппы) первый прогон без цитаты дал 74% СПОРНО; тот же батч с вытянутой по адресу конкретной цитатой владельца — 1–2 из 26–38 (2–8%). Общий рецепт для будущих заходов, раздающих субагентам классификацию по позаписному симптому из подобных «свёрнутых по механизму/теме» корпусов: передавать конкретный инстанс (цитату/адрес), а не только название темы группы.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: karta-incidentov#1 · 2026-08-07

#### nepokrytoe#2

2. **Повод §2.5 устарел до того, как был записан: `dok-07-verstka.md:68` починен коммитом `d4848b1` (06.08), а не остался живым.** Ход 1 §2.5 всё равно окупился — три живых адреса наружу нашлись в другом файле, `plakatnyj-stil.md`. Урок про сборку заходов вынесен в `## УРОКИ ФАБРИКЕ` отдельным пунктом.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: nepokrytoe#2 · 2026-08-07

#### nepokrytoe#3

3. **Формула охвата «выписано N, проверено N из N» не объявляет единицу счёта, и в `GEJT-VHOZHDENIYA.md` их две.** По номерам утверждений сумма непокрытого = 73, по пунктам перечня = 72. Расхождение — ровно один пункт (`popsci-research`, «№ 73, № 75 — те же два требования в форме чек-листа»). Гейт я не правил: он вне зоны.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
ДОСТАВЛЕНО: nepokrytoe#3 · 2026-08-07

#### pereschet-i-schetchik#6

6. **Разворот агрегата `КРТ-РАЗН-*` в `karta_domov.py` теряет две записи из 2264.** Поимённо раскладываются 311 из 312 записей фазы `02-reserch` и 97 из 98 фазы `03-matbaza`; итоги домов при этом верны (они берутся из `srez_po_faze.py`), но к этим двум записям невозможно прицепить рычаг — у них нет своего id. Дефект живёт в скрипте с 06.08 и всплыл только сейчас, потому что раньше поимённая раскладка всех фаз никому не требовалась. Не чинил: обе фазы вне границы этого захода, а правка разворота сдвинула бы счёт там, где заход менять не просили. Счётчик печатает ⚠ на каждом прогоне.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
ДОСТАВЛЕНО: pereschet-i-schetchik#6 · 2026-08-07

#### pereschet-i-schetchik#7

7. **Однострочная выжимка `KRATNOST-vladelca.md` теряет ту половину реплики, которая решает дом.** См. `## УРОКИ ФАБРИКЕ`, пункт 3.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
ДОСТАВЛЕНО: pereschet-i-schetchik#7 · 2026-08-07

#### svesti-i-vstroit#1

1. **Резолвер `dostavit_urok.py` структурно не умеет проверять адреса вида `disciplina/...`.** Функция `резолв()` пробует ровно две базы — папку арки и корень `materials` — и никогда не заглядывает в соседний репозиторий `disciplina`. Все 7 оставшихся «дома нет» (уроки 31, 32, 35, 53, 60, 61, 65) — это не отсутствующие дома, а именно эта слепая зона: я вручную проверил все 7 файлов прямо в `~/Documents/GitHub/disciplina` — все существуют (`GEJT-VHOZHDENIYA.md`, `README.md`, `skills/disciplina-git/SKILL.md`, `KOLLIZII-opisanij.md` на месте; урок 60 — это глоб `skills/*/SKILL.md` по семи файлам сразу, инструмент по построению не умеет адресовать «несколько домов одним уроком»). Починка — в `резолв()`/`дом_из()`, вне зоны этого захода.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
ДОСТАВЛЕНО: svesti-i-vstroit#1 · 2026-08-07

#### svesti-i-vstroit#2

2. **Патч 2 (`check_sborki.py` в `bootstrap_zahod.py`) сломал три фикстуры молча, пока не поймал собственный pre-commit хук.** `fixtures/git_zona/PROGNAT.sh` (дважды), `fixtures/register_doc/PROGNAT.sh`, `fixtures/korni/PROGNAT.sh` копируют `bootstrap_zahod.py` в изолированную песочницу ФИКСИРОВАННЫМ списком файлов — новый `import check_sborki` в новую песочницу не попадал, импорт падал, и `bootstrap_zahod.py` переставал создавать заход вовсе (симптом неотличим от «инструмент сломан»). Цена — час на диагностику и правку 4 мест. Закономерность шире случая: у инструмента появилась НОВАЯ зависимость модульного уровня — у каждой его песочницы теперь есть недостающий файл, и грепом это не найти, только красным прогоном.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
ДОСТАВЛЕНО: svesti-i-vstroit#2 · 2026-08-07
