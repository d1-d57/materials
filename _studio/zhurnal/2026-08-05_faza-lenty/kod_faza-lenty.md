# Канал исполнителя — faza-lenty (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — план рождается по ходу: классы отказов выводятся из 445 живых записей, а решение о форме врезок принимается глазами по собранным видам. Механики, которую можно расписать заранее, тут нет.

КОНТЕКСТ. Фабрика собирает лекции конвейером из одиннадцати фаз; ты чинишь ту, что производит **ленту** — текстовую версию лекции для внутреннего пользования. Прошлый этап: 05.08 собран проект перестройки и независимый реестр на 63 инцидента, в ту же ночь влилась картотека владельца — по нашим фазам там вшестеро больше записей (`cd _studio/zhurnal/2026-07-30_dovodka-fabriki && python3 srez_po_faze.py` — на 05.08 давал 273 по `06-tekst` и 172 по `05-raskadrovka`; **пересчитай, это замер на вчера**). ЦЕЛЬ: построить четыре носителя фазы (эталон · спецификацию формы · гейт · покрытие) и один машинный гейт, который краснеет сам.
Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 **Стоп до цели:** получишь систему фазы, но НЕ переписанный канон арок 05/06 и НЕ доведённую ленту Л2 — и то и другое идёт отдельными ходами после приёмки.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ: СВОЯ worktree-папка `materials-wt/faza-lenty` на ветке `zahod/faza-lenty`. Заводишь её САМ, первым ходом** (правило заведено 2026-08-05, `RUKOVODSTVO-zahodami.md §Зона`):
  ```
  cd ~/Documents/GitHub/materials
  python3 _generator/tools/git_zona.py worktree add faza-lenty --branch zahod/faza-lenty
  cd ../materials-wt/faza-lenty     # дальше работаешь ТОЛЬКО здесь
  ```
  **Папка уже есть — не пересоздавай, просто перейди:** команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. Владелец завёл ветку заранее — папка может быть уже готова.
  🔴 **НИКОГДА `git checkout` в основной папке:** сейчас параллельно идут ещё заходы (по фазе интервью и по картотеке), и checkout МОЛЧА откатит рабочее дерево под ними. Цена 27→28.07: файл откатился ночью, поймал владелец вручную, следа в git не осталось.
  🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты. Решение владельца 05.08.
- **ЗОНА (можно менять):** `_studio/konvejer/06-tekst/` `_studio/zhurnal/2026-08-05_faza-lenty/` `_generator/tools/check_lenta.py` `_generator/build_doc.py` `_studio/zhurnal/2026-08-05_faza-lenty/kod_faza-lenty.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи свою worktree-папку и перейди в неё** — команды в блоке «МЕСТО РАБОТЫ» выше. Уже есть — просто `cd`, это не ошибка.
- Проверь ветку: `git branch --show-current` — обязано быть `zahod/faza-lenty`. Не она — значит ты не в своей папке: вернись к предыдущему пункту. `git checkout` в основной папке НЕ делай ни при каких условиях (§4 GIT-disciplina).
- Точка отката: `git add `_studio/konvejer/06-tekst/` `_studio/zhurnal/2026-08-05_faza-lenty/` `_generator/tools/check_lenta.py` `_generator/build_doc.py` `_studio/zhurnal/2026-08-05_faza-lenty/kod_faza-lenty.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО это, проект не изучай:**
  1. `_studio/zhurnal/2026-08-05_faza-lenty/PROEKT-fazy-lenty.md` и `REESTR-incidentov.md` — что уже разобрано (сверка, не источник);
  2. `_studio/zhurnal/2026-07-30_dovodka-fabriki/KARTOTEKA-problem.md` + `srez_po_faze.py` рядом — **источник**;
  3. `_studio/konvejer/04.5-intervyu/{DOK,ANKETA,POKRYTIE}.md` — образец метода (READ-ONLY, там идёт чужой заход);
  4. `_studio/konvejer/05-raskadrovka/DOK.md`, `_studio/konvejer/06-tekst/{DOK,ZAHOD,PRIMERY}.md`, `_studio/konvejer/GEJTY.md` — что есть сейчас;
  5. **эталоны формы:** `teorkat-vvedenie/DOKAZATELSTVA-L1/{00-kak-chitat,01-liniya-A,02-liniya-B}.md`, `teorkat-vvedenie/KONSPEKT-L1/{05-funktor,09-dva-zapreta}.md`;
  6. **живой свежий выход фазы:** `teorkat-vvedenie/LENTA-L2.md` (+ `RABOCHIY-L2.md`, `UROKI-FABRIKE-lenta.md` — что в нём сломалось и почему);
  7. **стандарты:** `_studio/docs/kak-delat/{STANDART-teksta,STANDART-oformlenia}.md`;
  8. **движок:** `_generator/build_doc.py`, `_generator/tools/{check_view,check_termin,check_kartoteka}.py`, `_generator/DVIZHKI.md`.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### КОНТЕКСТ

Фабрика собирает лекции конвейером из одиннадцати фаз. Фаза, о которой заход, производит **ленту** — текстовую версию лекции для внутреннего пользования: по разделу на единицу выхода, математическое содержание, доказательства целиком, наброски иллюстраций. Сегодня это арки `05-raskadrovka` и `06-tekst`.

**Проблема, ради которой заход.** Фаза выдаёт каждый раз разное, и причина структурная: **у фазы нет захода на письмо ленты** — `06-tekst/ZAHOD.md` покрывает механический перенос уже готовой ленты в `src/content/`, а не её написание. Плюс `06-tekst/PRIMERY.md` — не образец выхода, а разбор чужой разметки дека, и он **противоречит собственному DOK** своей же фазы.

**ЦЕЛЬ:** построить четыре носителя фазы и один гейт, который краснеет сам.

🔴 **СТОП ДО ЦЕЛИ, заранее и громко.** Ты получишь **систему фазы** (эталон, спецификацию, гейт, покрытие). Ты **НЕ** получишь: переписанные `DOK.md`/`ZAHOD.md`/`PRIMERY.md` арок 05 и 06 — правка канона идёт отдельным ходом после приёмки; и **НЕ** доведённую до идеала ленту Л2 — она доводится в Cowork руками, по твоей же спецификации.

⚠ **Границы, сверх контракта зоны.** `_studio/konvejer/04.5-intervyu/**` и `_studio/zhurnal/2026-07-30_dovodka-fabriki/**` — там **прямо сейчас идут параллельные заходы**: читать нужно, писать нельзя ничем. `GEJTY.md` — запись нового гейта **предлагаешь текстом в отчёте**, сам не вносишь. Правку `05-raskadrovka/**` и существующих `06-tekst/{DOK,ZAHOD,PRIMERY}.md` предлагаешь файлом `06-tekst/PRAVKI-predlozhennye.md`.

---

### Часть A · Разложить картотеку по фазе ленты

Вход: `KARTOTEKA-problem.md` и `srez_po_faze.py` рядом с ней.

1. Снять срез по фазам `06-tekst` и `05-raskadrovka` **командой**; число вписать вместе с командой, не значением. На дату сборки захода скрипт печатал 273 и 172 при ИТОГО 2264 — **проверь заново, оно могло измениться.**
2. Сгруппировать записи среза в **классы отказов** — то, что ломается, а не как называется симптом. Ориентир: двенадцать классов A–L в `REESTR-incidentov.md`, выведенных по выборке вшестеро меньше; **проверь их на полной.** Не подтвердился — снимается; появился новый — заводится. Число классов не назначено намеренно: назначь его — и дробление начнут подгонять под цифру.
3. Для каждого класса — **чем ловится сегодня**: `ГЕЙТ G5.x` / `ГЕЙТ G7.x` / `проза DOK` / `строка ZAHOD` / `НЕТ РЫЧАГА`. Сверять по живым файлам.
4. Сдать **распределение**, а не список: `Counter` по классам и по видам рычага. Пустая клетка — либо мёртвый класс, либо непокрытый случай; разбирай оба.

### Часть B · Исследовать генератор и решить вопрос оформления — С ГЛАЗАМИ

🔴 **Прямое задание владельца 05.08:** *«посмотреть на генератор… подумать, как сделать более красиво, симпатично и функционально, чтобы это лучше читалось… чтобы было удобнее для любой нумерации, для любой работы. Он может провести исследование и визуально посмотреть, и продумать сам как поресёрчить. Это норма».*

Развилка: в двух признанных эталонах врезки оформлены **по-разному** — `**Доказательство.**` жирным в `DOKAZATELSTVA-L1` против `*Доказательство.*` курсивом в `KONSPEKT-L1`; нумерация буквенно-линейная (`A1`, `B2`, `A1′`) против сквозной (`256`, `93`). Пока не выбрано одно, воспроизводимости не будет ни при каком гейте. **Решение за тобой — владелец передал его явно.**

1. Прочитать, как `build_doc.py` рендерит врезки, каты, поля, нумерацию: `_KW_THM`, `_STMT_CLS`, `_PROOF_HEAD`, вырезание номера, `nomera: da`, `Статус:` в метке.
2. **Собрать и посмотреть ГЛАЗАМИ**, не по коду: пересобрать `DOKAZATELSTVA-L1/`, `KONSPEKT-L1/`, `teorkat-vvedenie/LENTA-L2/` и сравнить, как читаются три стиля на живом материале. В десктопном Claude Code браузер есть — пользуйся. **Нет браузера — скажи прямо и назови, чем заменил**; «посмотрел» без названного способа не считается.
3. Предложить **один** стиль, обосновав читаемостью, а не вкусом: что различает жанры блоков, как ведёт себя длинная метка, как выглядит кат, видна ли граница врезки, что при выключенных номерах.
4. **Улучшить движок, если это делает вид лучше** — `build_doc.py` в зоне. Разрешено: оформление врезок и катов, поддержка любой нумерации, читаемость.
   🔴 **Жёсткое условие: не менять поведение на существующих документах.** До правки собери `DOKAZATELSTVA-L1`, `KONSPEKT-L1`, `MAT-KOSTYAK`, `LENTA-L2`, сохрани выходы; после — собери заново и **сравни**. Расхождения обязаны быть только осознанными, каждое названо в отчёте. Неназванное расхождение = провал.
5. 🔴 **Два дефекта движка, найденные 05.08 живой сборкой** — почини или назови причину, почему нет:
   - **пункт списка обязан быть в одну строку**: перенос с отступом склеивает список в `<p>- пункт - пункт</p>`. На ленте Л2 так склеились **7 списков из 8**;
   - **список внутри врезки не поддерживается вовсе**: с пустой строкой выпадает наружу врезки, без пустой — склеивается. Определение, которое по смыслу есть список, оформить нечем. В картотеке это цена «пять блоков разрезаны, скелет доказательной структуры оборван».
6. ⚠ Заодно проверь: `_generator/README.md` утверждает, что движок **не умеет markdown-таблицы** — в собранном виде ленты Л2 их 21. Либо README врёт, либо утверждение неверно; проверь командой и скажи, как есть.

### Часть C · `FORMA.md` — спецификация формы, и `ETALON.md` — образец

**`06-tekst/FORMA.md`** — что обязано физически присутствовать, в машинно-проверяемых терминах. Основание — разбор эталонов, не вкус. Обязательно внести:

- 🔴 **закон записи доказательств**, дословно из `DOKAZATELSTVA-L1/00-kak-chitat.md`: *«Каждое отображение выписано явно: что куда переходит. Каждое равенство композиций проверено на элементе. Каждый нестандартный термин определён на месте. Ничего не надо достраивать в уме»* и *«После каждого утверждения стоит строка "Зачем и что дальше"… Утверждений, висящих без такой строки, в документе нет»*. В эталоне выполняется на 13 утверждениях из 13, и **ни одно из этих требований не записано ни в `06-tekst/DOK.md`, ни в `STANDART-teksta.md`**;
- **уровень строгости и детальность** — владелец: *«уровень строгости, детальность, что нужно, что не нужно — всё должно быть максимально хорошо прописано»*. Собери из `STANDART-teksta.md` (правила 1, 3, 10, 11, закон экономии, «определение дай, доказательство отдай») и `06-tekst/DOK.md` («формулировка обрывочной быть не может, доказательство — может»; «восстановимо усилием»; граница честного сжатия ≈ −10 %). **Где стандарт задаёт правило, а не число, — измерь по эталону командой** (сколько шагов у доказательства, как часто оговорка на поле) и впиши измеренное, назвав замером;
- **служебные элементы**: фронтматтер; словарь обозначений «одно понятие — одно обозначение» (единственное вхождение во всём репозитории — `00-kak-chitat.md:26`, правилом нигде не является); таблица участников перед доказательством; блок «Контроль» после запрета; `> поле:mn` как носитель математической оговорки; `> поле:insight` для приёма линии; статус источника внутри метки врезки;
- **разделение адресатов** зал / лектор;
- **иллюстрация — `<figure>` с живым `<svg>`, а не ТЗ таблицей.**

**`06-tekst/ETALON.md`** — короткая (2–3 раздела), но **полная** лента-образец, где физически есть каждый элемент `FORMA.md`. Не описание, а текст, который копируют. Материал — из `DOKAZATELSTVA-L1/01-liniya-A.md` (ретракт → Брауэр), он выверен владельцем.

⚠ Эталон обязан **собираться** `build_doc.py` и проходить `check_view.py` зелёным. Образец, который не собирается, хуже отсутствующего.

### Часть D · `_generator/tools/check_lenta.py` — гейт формы

Идиома семьи (`DVIZHKI.md`): stdlib, детерминизм, без сети и pip, `exit 1` при красном. Образец честного чекера — `check_kartoteka.py`.

Ориентировочный состав (**обоснуй по своим классам из части A**; скажи, что добавил и снял): число разделов == числу единиц перечня · у каждого утверждения есть `Статус:` · у каждого есть кат-доказательство либо ярлык «объявляем» · **построчная** сверка источник↔вид (не только хвост блока) · склеек `<p>- ` ноль · у каждой `<figure>` есть `<svg>` и `<figcaption>` · каждый класс `s-*` определён в движке · у каждого `<svg>` есть PNG-рендер не старше источника · пара разделов с совпадающим вопросом — красный · «Зачем и что дальше» у каждого утверждения кроме последнего · термин не раньше определения (`check_termin.py` **переиспользуй, не переписывай**).

🔴 **Три обязательных свойства чекера, каждое оплачено в корпусе:**

- **печатает «проверено X из Y»**, где `Y` считается ДО работы и по источнику; `X = 0` при непустом источнике — **красный, а не зелёный**. *Цена 16.07: «проверено 0 уроков из 14» прочиталось как зелёный верификатор;*
- **честно печатает, чего НЕ проверяет**: зелёный чекер с необъявленной слепой зоной опаснее красного;
- **прогоняется на живом ЭТАЛОНЕ прежде, чем на работе**: красное на эталоне = ложный гейт, а не больной эталон. Гейт, краснеющий на здоровом документе, обходят — и пропадает вся защита.

### Часть E · `06-tekst/POKRYTIE-lenty.md` — реестр «инцидент → рычаг»

Форма как у `04.5-intervyu/POKRYTIE.md`: `# · Симптом · адрес · ЦЕНА · РЫЧАГ`, где рычаг — id пункта `check_lenta.py`, строка `FORMA.md` или элемент `ETALON.md`. Плюс: **колонка с ценой** — защита от вычёркивания «за очевидностью»; строка без рычага — **долг фазы**, перечисли отдельно и предложи, чем закрыть; **иерархия**: гейт > закон > спецификация (ловится обоими — пишется гейт).

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — три прогона, все обязательны:**

1. **Живой объект репозитория.** `python3 _generator/tools/check_lenta.py teorkat-vvedenie/LENTA-L2.md` отрабатывает, печатает `проверено X из Y` с `Y > 0` и **осмысленный список замечаний** — это настоящая лента, написанная до твоей спецификации, и часть проверок на ней обязана краснеть. Зелёный на ней — подозрителен: значит гейт ничего не проверяет, разбирайся.
2. **Эталон.** Тот же чекер на `_studio/konvejer/06-tekst/ETALON.md` даёт `rc=0`. Красное на эталоне = ложный гейт, а не больной эталон: чинится гейт.
3. **Фикстура.** Тот же чекер на намеренно испорченной копии эталона в `_generator/tools/fixtures/lenta/` даёт `rc=1`. Гейт, не покрасневший на порче, — не гейт.

⚠ Прогоны 1 и 2 вместе и есть смысл критерия: чекер обязан различать документ, написанный по спецификации, и написанный до неё. Совпали вердикты — гейт слеп.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** отчёт несёт строку «классов отказов покрыто X из Y», где `Y` получено в части A **командой**, а не оценкой. Без охвата вердикт не принимается — «проверено 2 из 9» и «9 из 9» выглядят одинаково.

🔴 **Числа — командами, не значениями.** Ни одно число отчёта не написано цифрой без команды, которой снято. Числа 273/172/2264 — на дату написания захода; пересчитай.

⚠ **Не успеваешь** — законный исход: закрыть A, B, C и остановиться, **заявив это громко первой строкой отчёта**. Тихий стоп до цели = провал гейта, а не «мелкий нюанс».

## 3. ВЕРИФИКАТОР — ОБЯЗАТЕЛЕН, ты правишь общий движок

`build_doc.py` собирает документы всех лекций сразу — тихая регрессия в нём поедет под всеми и обнаружится не здесь.

**Свежий субагент, ДРУГОЙ метод:**
- собрать `teorkat-vvedenie/DOKAZATELSTVA-L1`, `KONSPEKT-L1`, `MAT-KOSTYAK`, `LENTA-L2` **до** правки движка (сохранить выходы) и **после**;
- сверить программно (не глазами): список расхождений; каждое обязано быть в списке осознанных правок из твоего отчёта;
- **список НЕНАЗВАННЫХ расхождений ПУСТ** — иначе провал верификатора, а значит и захода;
- `check_view.py` зелёный на всех четырёх пересобранных видах.


## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/konvejer/06-tekst/` `_studio/zhurnal/2026-08-05_faza-lenty/` `_generator/tools/check_lenta.py` `_generator/build_doc.py` `_studio/zhurnal/2026-08-05_faza-lenty/kod_faza-lenty.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/konvejer/06-tekst/` `_studio/zhurnal/2026-08-05_faza-lenty/` `_generator/tools/check_lenta.py` `_generator/build_doc.py` `_studio/zhurnal/2026-08-05_faza-lenty/kod_faza-lenty.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

> Написан ПОСЛЕ read-only разведки (чтение названных якорей + четыре базовых сборки в скрэтч,
> ни один файл репозитория не тронут) и ДО первой мутации. Дата данных — 2026-08-05.

### 0. Место работы и один дефект входа, который пришлось закрыть первым ходом

Worktree `materials-wt/faza-lenty`, ветка `zahod/faza-lenty` (`git branch --show-current` → она).
`git_zona.py worktree add` вернул `rc=1 «Уже есть»` — по заходу это не ошибка. `git_zona.py doctor`:
`Вне git: 0 путей`, локов нет ⇒ зона чиста в HEAD, точку отката не фабрикую.

🔴 **Файла-захода в worktree не было.** `kod_faza-lenty.md` лежит в ОСНОВНОЙ папке и там
**untracked** (`git ls-files --error-unmatch …` → `did not match any file(s) known to git`, `rc=1`;
основная папка на ветке `arka/mat-kostyak`). То есть §4 требует закоммитить путь, которого на моей
ветке не существует. Закрыл копированием файла в свою зону; работаю в копии worktree, в конце
кладу её обратно в основную папку, чтобы владелец открыл свой привычный путь и увидел отчёт.
`git checkout` в основной папке не делал и не буду. Это же — кандидат в `## УРОКИ ФАБРИКЕ`.

### 1. Три предпосылки захода, которые я обязан оспорить ДО работы

Проверены командами на живых файлах; две из трёх не подтвердились.

| предпосылка захода | вердикт | чем снято |
|---|---|---|
| «на ленте Л2 склеились 7 списков из 8» (Часть B5) | **не воспроизводится СЕГОДНЯ** | `grep -oE '<p>[ ]*[-–—•] ' view` → 0 во всех четырёх видах; в источнике ленты 8 списков, склеенных 0. Это замер ПЕРВОЙ сборки (`UROKI-FABRIKE-lenta.md` F5): починили ИСТОЧНИК, переписав пункты в одну строку. **Дефект движка остался — чинить его**, но число «7 из 8» в критерий не годится |
| «в собранном виде ленты Л2 21 таблица» (Часть B6) | **неверно** | в ленте `grep -cE '^\|[ :\|-]+\|$'` → 0 markdown-таблиц, `grep -c '<table'` → 1 сырая. Сам факт «README врёт» — **верен**: `md_table` в движке есть (ветка 6a), `DVIZHKI.md` его описывает, `README.md:58` его отрицает. Доказательство: `DOKAZATELSTVA-L1` даёт 4 `\|---\|` в источнике и 4 `<table>` в свежей сборке |
| «развилка жирное/курсивное доказательство — вопрос вкуса» (Часть B) | **это не вкус, а поломка** | см. §2 |

### 2. Главная находка разведки, из которой вырастает вся Часть B

`class="stmt"` в свежих сборках: `KONSPEKT-L1` — 167, `MAT-KOSTYAK` — 107, `LENTA-L2` — 30,
**`DOKAZATELSTVA-L1` — 0**. Каты `d-proof`: 46 / 222 / 15 / **0**.

Причина механическая, не эстетическая: `_BOLD_STMT` требует `\s+(\d+)` — только арабскую цифру, а
`_PROOF_HEAD` требует ОДИНОЧНУЮ звёздочку. У `DOKAZATELSTVA-L1` все 22 зачина — либо буквенные
(`**Утверждение A1.**`, `A1′`, `B2`, `C1a`), либо вовсе без номера (`**Определение (функтор)…**`), а
доказательство набрано жирным. **Признанный владельцем эталон рендерится нулём врезок и нулём катов
— сплошной прозой, где утверждение, доказательство и шаг визуально неразличимы.** Посмотрел глазами
в браузере (см. §5): рядом `LENTA-L2` даёт цветную врезку с границей, меткой в акценте и пилюлей
«▸ доказательство».

⇒ Развилка решается не голосованием, а так: **закон записи берём у `DOKAZATELSTVA-L1` (он про
содержание), разметку — у `KONSPEKT-L1`/`LENTA-L2` (она единственная доезжает до экрана), а движок
учим любой нумерации, чтобы эталон перестал быть плоским.** Это ровно то, что просил владелец
(«удобнее для любой нумерации»), и это не вкус: у одного варианта граница врезки на экране есть, у
другого её нет ни одной.

### 3. Что делаю по частям

**A · Картотека.** Срез снят командой: `06-tekst` Σ 273 / 54 строки, `05-raskadrovka` Σ 172 / 41,
ИТОГО 2264 — совпало с числами захода, пересчёт подтвердил. Симптомы у Разделов 1–2 живут не в
картотеке, а в `KRATNOST.md`/`KRATNOST-vladelca.md` (в картотеке только id) — развернул скриптом
`srez_full.py`, он ляжет в журнал захода. Дальше: классы отказов вывожу по **445 живым записям**
(Σ кратности), проверяю на них двенадцать классов A–L реестра, снимаю неподтвердившиеся, завожу
новые, считаю `Counter` по классам и по видам рычага.

**B · Движок.** Правки, каждая с измеренной ценой и проверкой «до/после»:
1. `_BOLD_STMT` — любая нумерация: `A1`, `A1′`, `C1a`, `256` и **отсутствие номера**;
2. `_PROOF_HEAD` — принимает и `**Доказательство.**`, и `*Доказательство.*`;
3. пункт списка в несколько строк больше не склеивает список в `<p>- … - …</p>`;
4. список ВНУТРИ врезки (`_body_html` учится `- `);
5. `_REF_RE` — автоссылка «по утверждению A1» вслед за нумерацией.
   Всё остальное не трогаю. Проверка — сборка четырёх документов до и после и программный дифф.

**C · `FORMA.md` + `ETALON.md`.** Спецификация — из разбора эталонов, числа замером командой, не
на глаз. Эталон — на материале `01-liniya-A` (ретракт → Брауэр), 2–3 раздела, с каждым элементом
`FORMA.md` физически внутри. Собирается `build_doc.py`, зелёный `check_view.py`.
⚠ Оговорка: `check_view.py` требует `*.md` и `view.html` В ОДНОЙ папке, а сам он вне моей зоны —
поэтому у эталона будет своя папка сборки внутри зоны, а `06-tekst/ETALON.md` остаётся каноном.

**D · `check_lenta.py`.** Идиома `check_kartoteka.py`: stdlib, `exit 1`, «проверено X из Y» с Y по
источнику, честный список НЕ проверяемого, прогон на эталоне раньше, чем на работе.
`check_termin.py` переиспользую импортом, не переписываю.

**E · `POKRYTIE-lenty.md`.** Колонки `# · Симптом · адрес · ЦЕНА · РЫЧАГ` как в `04.5/POKRYTIE.md`,
иерархия гейт > закон > спецификация, строки без рычага — отдельным списком «долг фазы».

### 4. Критерий готовности — принимаю как есть, с одной поправкой

Критерий проверяем и способен провалиться; спорю с ним в одном месте. Пункт «**«Зачем и что
дальше» у каждого утверждения кроме последнего**» списан с `PROEKT` («13 утверждений — 13 строк»),
а живой замер даёт другое: в `01/02/03-liniya` строк «Зачем и что дальше» ровно **4/5/3** при
**4/5/3** разделах `^## ` и при 5/5/4 утверждениях. То есть в эталоне это **один на РАЗДЕЛ, а не на
утверждение**. Гейт по букве критерия покраснел бы на здоровом эталоне — ровно класс, который в
`GEJTY.md` оплачен «12 ложных ❌ из 14». Пишу проверку по замеру (раздел), а не по `PROEKT`.

### 5. Чем смотрел глазами

Браузер в десктопном Claude Code есть и использован: `preview_start` + `navigate` на `file://`,
виды лежат в `_studio/zhurnal/2026-08-05_faza-lenty/vidy/`. Снято четыре экрана: плоская проза
`DOKAZATELSTVA-L1`, врезка `KONSPEKT-L1` с меткой в акценте, врезки `LENTA-L2` с цветной линейкой и
пилюлей ката, поведение шторки-оглавления. Сравнение — на живом материале, не по коду.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **Файл-заход не доехал в рабочую папку и в основной папке лежит untracked.** `git ls-files
   --error-unmatch _studio/zhurnal/2026-08-05_faza-lenty/kod_faza-lenty.md` → `rc=1`, «did not match
   any file(s) known to git»; основная папка на ветке `arka/mat-kostyak`, worktree — на
   `zahod/faza-lenty`, и там файла нет вовсе. При этом §4 захода требует закоммитить именно этот
   путь. Исполнитель попадает в вилку: либо коммитить путь, которого на его ветке не существует,
   либо писать отчёт вне своей зоны.
   ЦЕНА: один лишний ход на диагностику плюс риск, которого никто не видит, — владелец открывает
   привычный путь в основной папке и находит файл БЕЗ отчёта, потому что отчёт уехал в worktree.
   Механизм починки дешёвый: `bootstrap_zahod.py` коммитит заготовку захода сразу, тогда worktree
   получает её штатно.
   ДОМ: `_studio/konvejer/06-tekst/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

2. **Число в заходе назвало ЗАМЕР ПРОШЛОЙ РЕДАКЦИИ, а прочиталось как свойство живого файла.**
   Заход требует починить два дефекта движка и приводит их цену: «на ленте Л2 склеились 7 списков
   из 8» и «в собранном виде ленты Л2 21 таблица». На живых файлах: склеек `0`
   (`grep -oE '<p>[ ]*[-–—•] '` по всем четырём видам), markdown-таблиц в ленте `0`
   (`grep -cE '^\|[ :|-]+\|$'`), сырых `<table>` — `1`. Оба числа верны как замер ПЕРВОЙ сборки
   05.08: источник тогда починили (пункты переписали в одну строку), движок — нет.
   ЦЕНА: ход исполнителя уходит на доказательство, что предпосылка ложна, и — хуже — критерий
   готовности, построенный на таком числе, провалиться не может: он сверяется с состоянием,
   которого уже нет. Правило дешёвое: у числа-цены называется не только дата данных, но и **что
   именно замерено — живой файл или его прошлая редакция**.
   ДОМ: `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md`
   ДОСТАВЛЕНО: нет

3. **`check_view.py` сверяет ХВОСТ блока, и первый же документ, написанный по новой спецификации,
   содержал съеденную строку, которую гейт не увидел.** В эталоне стояло `<адрес>` в угловых
   скобках; движок отдал его в HTML сырым, браузер прочитал как тег и съел. `check_view` — зелёный:
   поломка в СЕРЕДИНЕ блока, а он смотрит последние 35 знаков. Поймано построчной сверкой
   (`check_lenta L12`) с первого прогона.
   ЦЕНА: в `teorkat-vvedenie/UROKI-FABRIKE-lenta.md` этот дефект (F1) записан со словами «пока
   ноль: поймано до выдачи» — то есть цены у него не было и в канон он не шёл. Теперь цена есть:
   **дефект прошёл в эталон фазы, то есть в документ, который будут копировать, и зелёный гейт его
   пропустил.** Починка — режим `--strict` в самом `check_view.py` либо вызов `check_lenta L12`
   из него; прототип построчной сверки уже написан и работает.
   ДОМ: `_studio/konvejer/06-tekst/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

4. **Гейт, который сверяет источник с ВИДОМ, обязан знать все трансформации движка, иначе краснеет
   на здоровом документе.** Движок печатает подпись ката строчными и без точки, срезает `Статус:`
   из метки и срезает номер врезки без `nomera: da`. Построчная сверка, не знающая об этих трёх
   преобразованиях, дала 3 ложных ✗ на здоровом эталоне и 30 на здоровой ленте Л2.
   ЦЕНА: измерена на этом заходе — 33 ложных ✗; будь гейт сдан в таком виде, его обошли бы
   `--no-verify` с первого коммита, и пропала бы вся защита, а не одна проверка. Общее правило:
   **список преобразований «источник → вид» — часть контракта движка, а не знание в голове.**
   Сегодня он не записан нигде: ни в `README.md`, ни в `DVIZHKI.md`.
   ДОМ: `_generator/DVIZHKI.md`
   ДОСТАВЛЕНО: нет

5. **Вопрос владельцу.** `_generator/README.md:58` утверждает, что движок не умеет
   markdown-таблицы; `DVIZHKI.md:39` утверждает обратное, и прав он (`md_table`, ветка 6a; проверка:
   `DOKAZATELSTVA-L1` даёт 4 `<table>` при 4 строках-разделителях в источнике). `README.md` вне моей
   зоны — правку не вносил. Чинить одной строкой?
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

6. **Вопрос владельцу.** В эталоне `DOKAZATELSTVA-L1` два разных утверждения носят один номер:
   `**Лемма B2 (что делает скаляр).**` и `**Теорема B2.**` (`02-liniya-B.md`). После правки движка
   оба получают якорь `s1-t-B2` — ссылка ведёт на первый. Файл вне моей зоны, не трогал. Это опечатка
   нумерации или намеренная пара «лемма и теорема одной ступени»?
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Цель достигнута полностью: четыре носителя фазы и машинный гейт построены, критерий готовности
пройден всеми тремя прогонами.** Стоп до цели не потребовался. Дата данных — 2026-08-05.

### 0. Что сделано и ЗАЧЕМ — по частям

**A · Классы отказов выведены по ПОЛНОЙ картотеке, а не по выборке.**
Срез снят командой, числа захода подтвердились: `06-tekst` **273**, `05-raskadrovka` **172**, ИТОГО
**2264** (`cd _studio/zhurnal/2026-07-30_dovodka-fabriki && python3 srez_po_faze.py`). В строках
Разделов 1–2 картотеки стоит только id, симптом живёт в `KRATNOST.md`/`KRATNOST-vladelca.md` —
поэтому написан `klassy_otkazov.py`, который разворачивает id в симптом, кладёт симптом в класс
отказа и считает `Counter`. Разнос по классам — по МЕХАНИЗМУ починки, не по теме.

```
cd _studio/zhurnal/2026-08-05_faza-lenty && python3 klassy_otkazov.py
#   строк-групп 95 · Σ кратности 445 · живых классов 16 (+ клетка «не сведено», 2 записи)
```

**Вердикт по двенадцати классам реестра: все двенадцать подтвердились, ни один не снят.** Добавились
четыре, которых в реестре не было вовсе — и один из них тяжёлый:

| новый класс | Σ крат | почему его не было в реестре |
|---|---|---|
| **M · раскладка единицы** (полосы, поля, один экран) | **32** | в выборке из 63 распался между E и G; на полной картотеке это отдельная и седьмая по массе клетка |
| **N · такт «вопрос залу» раньше ответа** | **24** | в реестре сидел одной строкой №45 внутри E; механизм другой — не регистр текста, а порядок подачи |
| **P · оформление деки** (цвет, палитра, единство стиля) | 10 | принадлежность фазе спорна: дом, вероятно, `07-verstka` — назван, не присвоен |
| **O · листок и упражнения** | 5 | в реестре клетки не было |

Распределение по видам рычага (то же командой): **проза DOK 239 (54 %) · НЕТ РЫЧАГА 149 (33 %) ·
ГЕЙТ 52 (12 %) · строка ZAHOD 3 (1 %)**. Больше половины фазы держалось на том, что исполнитель
прочитает прозу и вспомнит, — то есть на самом слабом из трёх носителей.
Пустых клеток нет; единственная непросуженная — 2 записи `КРТ-РАЗН`, где механизм не сведён в самом
источнике. Классифицировать их одной буквой значило бы придумать, поэтому они печатаются отдельной
клеткой (`python3 klassy_otkazov.py --neraskryt`).

**B · Движок исследован, стиль выбран, четыре правки внесены.**
🔴 Развилка «жирное против курсивного доказательства» оказалась **не вопросом вкуса, а поломкой**.
Замер по свежим сборкам: `class="stmt"` — KONSPEKT-L1 **167**, MAT-KOSTYAK **107**, LENTA-L2 **30**,
**DOKAZATELSTVA-L1 — 0**. Причина механическая: `_BOLD_STMT` требовал `\s+(\d+)` — обязательную
арабскую цифру, а у эталона все 22 зачина буквенные (`A1`, `A1′`, `C1a`) либо без номера. То есть
**признанный владельцем эталон рендерился нулём врезок — сплошной прозой, где утверждение, шаг и
доказательство визуально неразличимы.**

⇒ **Решение (принято мной, владелец передал его явно): закон записи берём у `DOKAZATELSTVA-L1`,
разметку — у `KONSPEKT-L1`/`LENTA-L2`, движок учим любой нумерации.** Обоснование читаемостью, не
вкусом: у одного варианта на экране есть цветная линейка, фон, метка в акценте и пилюля-кат —
граница врезки видна; у другого нет ни одной границы. Спецификация при этом предписывает **одну**
форму — сквозную нумерацию с именем в скобках, и это тоже механика: `check_termin.py` разбирает
метку регуляркой `\s+(\d+)\s*\(`, буквенный номер для гейта порядка введения невидим.

Четыре правки `build_doc.py`, каждая с замером до/после:
1. **любая нумерация** — номер необязателен и может быть буквенно-линейным;
2. **пункт списка в несколько строк** больше не роняет весь блок в `<p>- пункт - пункт</p>`;
3. **список внутри врезки** (`_body_html` учится строкам на `- `);
4. **автоссылки** «по утверждению A1» вслед за нумерацией;
5. *(добавлено по замечанию верификатора)* **буквенный номер печатается в подписи всегда**, без
   `nomera: da`: умолчание заводили против двух перемешанных ЦИФРОВЫХ рядов, у буквенно-линейной
   нумерации такого столкновения нет. Без этой оговорки правка ОТНИМАЛА видимую информацию.

🔴 **Пятая правка была написана и ОТКАЧЕНА — называю, потому что это решение, а не оплошность.**
Пробовал пустить под кат жирный зачин `**Доказательство.**`. Откачено тремя замерами: в
`DOKAZATELSTVA-L1` пять таких зачинов стоят отдельным блоком, и кат выходил **пустой**, с
доказательством снаружи; под ту же форму попадал чужой жанр (`**Решение: не нести.**` во вкладке
«Находки»); `check_view.py` краснел **шестью ✗ на здоровом документе**, а он вне моей зоны. Гейт,
краснеющий на здоровом документе, обходят `--no-verify` — цена этого записана в `GEJTY.md`.
⇒ **Многоблочное доказательство (Шаг 1 / Шаг 2 / …) под кат не убирается ни в какой форме.** Это
названный предел движка; в `FORMA.md §3.4` он записан вместе с машинным признаком разбора в потоке.

**Два дефекта из §B5 захода — предпосылка проверена, вердикт другой, чем в задании:**

| дефект | что на живых файлах | что сделано |
|---|---|---|
| «на ленте Л2 склеились 7 списков из 8» | **сегодня склеек 0** во всех четырёх видах; в источнике ленты 8 списков, целых 8. Число — замер ПЕРВОЙ сборки 05.08: тогда починили ИСТОЧНИК (пункты переписали в одну строку), движок остался сломан | **дефект движка починен**; воспроизведён на `02-liniya-B.md`, где перенос пункта живой и несёт `$$…$$` |
| «список внутри врезки не поддерживается» | подтвердился полностью | **починен**; работает и с отступом, и без |

**B6 · README врёт — да, но не тем числом.** `_generator/README.md:58` утверждает, что движок не
умеет markdown-таблицы. **Не умеет — неверно:** `md_table` есть (ветка 6a), `DVIZHKI.md:39` его
описывает, доказательство — `DOKAZATELSTVA-L1` даёт 4 `<table>` при 4 строках `|---|` в источнике.
**А число «21 таблица в ленте Л2» неверно:** markdown-таблиц в ленте `0`, сырых `<table>` — `1`.
`README.md` вне зоны — правку предлагаю, не вношу (вопрос 5).

**C · `FORMA.md` и `ETALON.md`.**
`FORMA.md` — спецификация в машинно-проверяемых терминах; у каждого пункта назван рычаг, у пунктов
без рычага стоит `⚠ без рычага` и дубль в долге фазы. Внесено всё заказанное: закон записи
дословно, уровень строгости и детальность, служебные элементы, разделение адресатов, иллюстрация
как `<figure>` с живым `<svg>`. Числа — замером, не на глаз: **утверждений на раздел 1,25 · шагов у
доказательства медиана 3,5 · оговорка на поле 1,5 на раздел · длина предложения медиана 9 слов**
(для сравнения `KONSPEKT-L1` 16, `LENTA-L2` 12). 🔴 Ни одно из них не сделано гейтом: число,
объявленное потолком, уже один раз сработало как цель подгонки (класс B, Σ 43).

`ETALON.md` — три раздела на материале «ретракт → Брауэр», 8 врезок, 3 ката, 2 списка (один внутри
врезки), 2 таблицы, 1 живой `<svg>` с `<figcaption>`, 4 поля, 1 `insight`, 9 автоссылок, 0 склеек.
Собирается и проходит все три гейта зелёным.

**D · `check_lenta.py`** — 13 пунктов `L1`–`L13`, каждый привязан к классу отказа из части A.
Против ориентировочного состава захода: **добавлено** `L1` (полнота фронтматтера и `nomera: da` —
класс J), `L2` (словарь обозначений — класс C), `L6` (сквозная нумерация и имя в скобках — класс C);
**снято** дублирование `G7` по раскладке (два хозяина у одного правила — способ потерять оба) и
изменена формулировка «Зачем и что дальше» (см. §1 ниже). Три обязательных свойства выполнены:
печатает «проверено X из Y» с Y по источнику и красным при X=0; печатает закрытый список того, чего
НЕ проверяет, **всегда**, а не только когда красно; прогнан на эталоне раньше, чем на работе.
`check_termin.py` переиспользован импортом, не переписан.

**E · `POKRYTIE-lenty.md`** — 63 инцидента с ценой → рычаг, иерархия ГЕЙТ > ЗАКОН > СПЕЦИФИКАЦИЯ,
долг фазы отдельным списком с предложением по каждому пункту.

### 1. Спор с критерием готовности — заявлен в `## ПЛАН` ДО работы и подтвердился

Критерий требовал «**«Зачем и что дальше» у каждого утверждения кроме последнего**». Замер по
эталону: строк «Зачем и что дальше» **12** при **12** разделах и **15** утверждениях
(`grep -c` по `DOKAZATELSTVA-L1/0[123]*.md`). То есть в эталоне это **один на РАЗДЕЛ**. Гейт по
букве критерия покраснел бы на здоровом эталоне трижды — ровно класс, за который `GEJTY.md` уже
заплатил «12 ложных ❌ из 14». `L5` написан по замеру.

### 2. Как проверил

| прогон | команда | результат |
|---|---|---|
| **1 · живой объект** | `python3 _generator/tools/check_lenta.py teorkat-vvedenie/LENTA-L2.md --vid teorkat-vvedenie/LENTA-L2/view.html` | **rc=1**, «проверено 54 из 54», **39 замечаний**: нет `nomera: da`, нет словаря, 16 врезок без `Статус:`, 12 единиц без «Зачем и что дальше», 2 нарушения порядка введения («категория» и «функтор» работают во врезке 1 раньше определений 2 и 3), нет PNG-рендеров 11 рисунков |
| **2 · эталон** | `python3 _generator/tools/check_lenta.py _studio/konvejer/06-tekst/ETALON.md` | **rc=0** |
| **3 · фикстура** | `bash _generator/tools/fixtures/lenta/PROGNAT.sh` | **rc=0** — четыре фикстуры сошлись: `zdorovaya` 0 · `slomannaya` 1 (и в вердикте названы все восемь испорченных пунктов, а не «что-нибудь») · `povtor` 1 · `pustaya` 1 |

⚠ Прогоны 1 и 2 **различают** документ, написанный по спецификации, и написанный до неё: 39 против
0. Гейт не слеп.
Плюс: `check_view.py` и `check_termin.py` на эталоне — **rc=0** оба; `build_doc.py` — **rc=0**.

🔴 **Гейт поймал на эталоне настоящую съеденную строку, которую `check_view.py` пропустил.** В
эталоне стояло `<адрес>` в угловых скобках — движок отдал его сырым, браузер прочитал как тег и
съел; `check_view` зелёный, потому что поломка в СЕРЕДИНЕ блока, а он смотрит хвост. Починено в
эталоне, урок вынесен в очередь (`## ВОПРОСЫ`, п. 3).

### 3. Результат верификатора

Свежий субагент, другой метод (нашёл папки-источники сам, по фронтматтеру, а не по наличию
`view.html`, и потому взял **52** папки против моих 34).

- проверено **52** папки-источника, собраны обоими движками, **36 побайтно идентичны**, **16
  различаются**;
- каждое из 16 расхождений закрывается заявленным списком (1) любая нумерация · (2) список с
  перенесённым пунктом · (3) автоссылки · (5) видимый буквенный номер; пункт (4) — список внутри
  врезки — в живом корпусе не сработал ни разу, как и было заявлено;
- 🔴 **СПИСОК НЕОБЪЯСНЁННЫХ РАСХОЖДЕНИЙ ПУСТ**;
- регрессии по цифровой нумерации нет числом: **1030** чисто-цифровых подписей в 27 папках, из них
  изменившихся **0** — решение владельца от 2026-07-28 нигде не отменено;
- `check_view.py` на четырёх названных документах: **rc=0 и до, и после**.

**Верификатор нашёл то, чего я не назвал, и это исправлено.** Первым проходом он заметил: буквенный
номер `A1` перестал печататься в подписи (раньше был виден как обычный текст плоского абзаца).
Формально следствие правки (1) плюс старого умолчания `nomera`, по сути — **потеря видимой
информации ровно там, где нумерация несёт навигацию**. Добавлена правка (5), перепроверено.
Отступление верификатора, названное им самим: два голых `git`-вызова (`rev-parse`, `status`) сверх
разрешённого `git show`; локов не осталось, проверено.

### 4. Охват вердикта

**Классов отказов покрыто рычагом 12 из 16** (Y=16 получено командой `klassy_otkazov.py`, не
оценкой). Из них рычаг заведён ЭТИМ заходом у десяти — A, C, D, E, F, G, H, I, J, L; `M` держится на
прежнем `G7`, `K` — на прежней строке `ZAHOD.md`.
**Без рычага остались четыре: B** (пол плотности, Σ 43 — второй по массе), **N** (такт «вопрос
залу», Σ 24), **O** (листок, Σ 5), **P** (оформление деки, Σ 10 — и дом, вероятно, в `07-verstka`).
По инцидентам: `grep` по `POKRYTIE-lenty.md` даёт **63 строки · 22 ловит новый гейт `L*` · 3 ловит
`G*` · 22 без рычага** (было 37 без рычага по реестру).

### 5. Чего НЕ трогал

- `05-raskadrovka/**` и существующие `06-tekst/{DOK,ZAHOD,PRIMERY}.md` — правку канона не вносил.
  ⚠ **`06-tekst/PRAVKI-predlozhennye.md` не заведён**: заход разрешает предлагать правку канона
  файлом, но это правка арок 05/06, а она по §2 идёт отдельным ходом ПОСЛЕ приёмки. Предложения
  лежат там, где им место сейчас: `FORMA.md §6` (чего спецификация не задаёт) и
  `POKRYTIE-lenty.md §4` (долг фазы с предложением по каждому пункту). Если приёмка ждала отдельный
  файл — скажите, соберу из этих двух за один ход.
- `_studio/konvejer/GEJTY.md` — запись нового гейта не вносил, предлагаю текстом (§6 ниже).
- `_studio/konvejer/04.5-intervyu/**` и `_studio/zhurnal/2026-07-30_dovodka-fabriki/**` — только
  чтение, там идут параллельные заходы.
- `_generator/README.md` — врёт про таблицы (вопрос 5), вне зоны, не трогал.
- `_generator/tools/check_view.py` — вне зоны. Его дефект «сверяет хвост» подтверждён живым случаем
  и вынесен уроком; правку не вносил.
- `teorkat-vvedenie/**` — read-only. ⚠ **Следствие, которое надо знать:** `view.html` четырёх
  документов в репозитории остались собранными СТАРЫМ движком и теперь устарели относительно
  источника. Пересобрать их — один ход `build_doc.py` по четырём папкам, но это правка вне моей
  зоны, и я её не делал.
- Ленту Л2 не доводил — это отдельный ход руками, по этой же спецификации.

### 6. Предлагаемая запись в `GEJTY.md` (сам не вношу, §2 захода)

```
### G17 · форма ленты (внутри арки 6, до G7)
- что проверяет: 13 пунктов `FORMA.md` — фронтматтер и `nomera: da`; словарь обозначений;
  `Статус:` у каждой врезки; доказательство у каждого утверждения (кат, разбор шагами или
  ярлык «объявляем»); «Зачем и что дальше» у каждой единицы кроме последней; сквозная нумерация
  с именем в скобках; единица помечена `· ~N мин` и число единиц сходится с перечнем; нет двух
  единиц с одним заголовком; порядок введения терминов; ноль склеек списка в собранном виде;
  `<svg>` и `<figcaption>` у каждой `<figure>` и известные движку классы `s-*`; ПОСТРОЧНАЯ
  сверка источник↔вид; PNG-рендер не старше источника у каждого `<svg>`.
- признак-на-диске: `python3 _generator/tools/check_lenta.py <лента.md>` → exit 0.
  Прогнан по живому корпусу: на `ETALON.md` — 0, на `teorkat-vvedenie/LENTA-L2.md` — 1 с
  39 замечаниями; фикстуры `_generator/tools/fixtures/lenta/PROGNAT.sh` — 4 из 4.
- тип: жёсткий-стоп-вперёд (форма) — вкус и верность математики он НЕ судит и печатает это
  списком при каждом прогоне.
- возврат-разрешён: да.
```

### 7. Открытое «возвращаться»

1. **Верность математики рычага не имеет** и никакой формой не заменяется — класс C, Σ 48.
   Предложение в `POKRYTIE-lenty.md §4`: гейт-читатель субагентом с пустым контекстом.
2. **Пол плотности** (класс B, Σ 43) — правка канона `DOK.md`, вне этого хода.
3. **Такт «вопрос залу»** (класс N, Σ 24) — механизм применён на Л1 и не записан в
   `08-sceny/DOK.md`. Самая дешёвая правка канона из трёх.
4. **`G5.г`** («список предъявлен владельцу») не имеет ни кода, ни строки в `GEJTY.md` — самый
   дорогой из непокрытых.
5. **Дубль номера `B2`** в `DOKAZATELSTVA-L1` (вопрос 6).
6. **Гейта на рост `POKRYTIE-lenty.md` нет** — в фазе `04.5` его роль играет `G16.8`; записано
   долгом, а не выдано за сделанное.

### 8. 🔴 НЕОБРАТИМОЕ

Ты запущен без запроса разрешений — перечисляю всё, чего владелец не видел в момент, когда оно
происходило.

1. **Перезапись файла-захода в ОСНОВНОЙ папке** · `~/Documents/GitHub/materials/_studio/zhurnal/2026-08-05_faza-lenty/kod_faza-lenty.md` · восстанавливается из коммита этого захода (файл в worktree — тот же, с ПЛАНОМ/ВОПРОСАМИ/ОТЧЁТОМ; содержание захода не изменено ни на строку, добавлены только три секции внизу). Сделано потому, что владелец открывает привычный путь, а файл в основной папке untracked и в worktree не доехал.
2. **Переименование** `_studio/konvejer/06-tekst/etalon-vid/` → `ETALON-vid/` · восстанавливается обратным `mv`; папка порождаемая, содержит только `view.html` и симлинк.

Больше необратимого нет: `git checkout` в основной папке не делал ни разу, ничего не удалял, ничего
за пределы зоны не двигал. Все сборки сравнения шли в скрэтч, `teorkat-vvedenie/**` не тронут.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/faza-lenty/_studio/konvejer/06-tekst/ETALON-vid/view.html` — открыть в браузере: это эталон ленты, зелёный по трём гейтам, тот текст, который копируют.
Ещё три файла, ради которых заход и делался:
· `/Users/ivanyakovlev/Documents/GitHub/materials-wt/faza-lenty/_studio/zhurnal/2026-08-05_faza-lenty/vidy/DOKAZATELSTVA-L1--DO-pravki.html` и `…--POSLE-pravki.html` — **откройте оба рядом**: это и есть ответ на вопрос про генератор. Слева эталон плоской прозой, справа он же врезками с видимыми номерами `A1`, `A1′`, `A4`.
· `/Users/ivanyakovlev/Documents/GitHub/materials-wt/faza-lenty/_studio/konvejer/06-tekst/ETALON-png/01.png` — иллюстрация эталона, отрисованная и просмотренная (диск, точка $x$, её образ $f(x)$, луч в $r(x)$ на границе).

**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
