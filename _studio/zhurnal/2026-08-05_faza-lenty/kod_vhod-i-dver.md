# Канал исполнителя — vhod-i-dver (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — четыре механические работы, каждая расписана поимённо; мест для суждения нет, а где они были бы — там стоит СТОП.

КОНТЕКСТ. У владельца два рабочих репозитория в папке `~/Documents/GitHub/`: `materials` (содержание, курсы, движки) и `disciplina` (переносимый плагин Claude Code с семью скиллами). Сама папка репозиторием **не является** — `.git` в ней нет, файл там не под версионным контролем и на второй компьютер не приезжает.

07.08 заведён `disciplina/KAK-USTROEN-GITHUB.md` — вход уровнем выше репозиториев. Тогда же выяснилось, что **знание про папку живёт и в корневом `~/Documents/GitHub/README.md`** (63 строки, роутер «ситуация → дом» плюс таблица репозиториев). Два текста про одно, и тот, что вне git, разъедется первым.

ЦЕЛЬ: свести четыре открытых хвоста, каждый из которых — одно решение владельца или один известный дефект.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ.** Ты **не трогаешь `disciplina/skills/`** — там прямо сейчас работает заход `etap-20-i-perenos`, переписывающий все семь тел. Ты **не трогаешь `doma/`** (работает `razbor-meta`) и `_generator/tools/check_zahod.py` с `fixtures/zahod/` (работает `edinstvennost-sekcij`). Заход из четырёх мелких работ, и каждая обязана остаться мелкой.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `zahod/vhod-i-dver` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `disciplina/KAK-USTROEN-GITHUB.md` `disciplina/README.md` `_generator/tools/register_doc.py` `_generator/tools/korni.py` `~/Documents/GitHub/README.md` — пять файлов в **двух репозиториях плюс один вне git**.
  🔴 **Два репозитория — два коммита**, каждый своими путями, оба хэша в отчёт. Пятый файл (`~/Documents/GitHub/README.md`) **вне любого git**: он правится, но не коммитится — так и напиши в отчёте, это не упущение.
  🔴 Инструменты лежат в `materials` и на `disciplina` наводятся дверью `GIT_ZONA_REPO` (`korni.py:42`):
  ```
  export GIT_ZONA_REPO=~/Documents/GitHub/disciplina
  export MAT=~/Documents/GitHub/materials
  ```
  Всё остальное — **READ-ONLY**, особо: `disciplina/skills/`, `disciplina/doma/`, `_generator/tools/check_zahod.py`, `_generator/tools/fixtures/` — во всех четырёх работают другие заходы.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `zahod/vhod-i-dver`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: зона не чиста в HEAD — закоммить как есть; чиста — не фабрикуй.
- **Прочитать ТОЛЬКО это:**
  1. `~/Documents/GitHub/README.md` — 63 строки, что в нём есть.
  2. `disciplina/KAK-USTROEN-GITHUB.md` — 51 строка, что уже написано; **дубли не заводить, сливать**.
  3. `disciplina/README.md` — список документов корня, он же будущая карта регистрации.
  4. `_generator/tools/korni.py` — реестр корней: как устроены `КОРНИ`, `дверь_принимает()`, `карта_для()`.
  5. `_generator/tools/register_doc.py` — как дверь пишет строку в карту.
- ПЛАН — в `## ПЛАН` перед действиями.

### 0.1 ВЛИТИЕ

```
cd ~/Documents/GitHub/disciplina
git --no-optional-locks branch --no-merged main | grep zahod
```
Пусто — напиши «невлитых 0, проверено». **Не пусто — НЕ вливай**: невлитые ветки сейчас принадлежат работающим заходам. Назови их числом и иди дальше — твоя зона с ними не пересекается.

## 2. ЗАДАЧА

### 2.1 Корневой README переезжает в плагин целиком (решение владельца 07.08)

Содержание `~/Documents/GitHub/README.md` **сливается** в `disciplina/KAK-USTROEN-GITHUB.md`; наверху остаётся заглушка в три строки: «содержание переехало в `disciplina/KAK-USTROEN-GITHUB.md`, читай там; этот файл вне git и обновляться не будет».

🔴 **Сливать, а не приклеивать.** Тексты пересекаются: у обоих есть роутер «ситуация → дом» и таблица репозиториев. Совпадающее — один раз; расходящееся — обе стороны, и **если расхождение содержательное, а не словесное, оно идёт в `## ВОПРОСЫ`**, а не разрешается тобой.

Состав репозиториев в итоговом документе — **командой, а не списком руками**: их пять, и число меняется.

### 2.2 Дверь регистрации учится принимать `disciplina` (решение владельца 07.08)

Сегодня `register_doc.py` отказывает на любом пути в корне `disciplina` (`rc=2`, «дверь судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`»), а гейта регистрации в этом репозитории нет вовсе. Документов в корне уже шесть.

Решение владельца: **`disciplina/README.md` объявляется картой регистрации корневых документов.** Научи `korni.py` знать корень репозитория `disciplina` с картой `README.md`, а `register_doc.py` — писать туда строку.

⚠ **Реестр корней читается через `GIT_ZONA_REPO`** и обслуживает оба репозитория одним кодом. Правка обязана оставить `materials` ровно таким, как был: прогони дверь на любом файле `_studio/` до и после — поведение не изменилось.

Тем же ходом зарегистрируй два корневых документа, которые сейчас сироты: `GEJT-VHOZHDENIYA.md` и `KAK-USTROEN-GITHUB.md`. **Только дверью, руками в `README.md` не дописывать** — дверь ровно одна.

### 2.3 `OTVETY-INTERVYU-2026-08-07.md` ввести в git

Файл лежит в корне `disciplina` **вне git** — родился в сессии Cowork и никогда не коммитился. На него ссылаются `KAK-USTROEN-GITHUB.md` и решения интервью; пока он вне git, ссылка живёт на честном слове диска, а `git_zona.py check` по всему дереву `disciplina` красный именно на нём.

Ввести в git, зарегистрировать дверью из §2.2, закоммитить.

### 2.4 Снять пустую ветку-мусор в `materials`

`zahod/gejt-vhozhdeniya` в репозитории `materials` — след ошибочной попытки завести worktree не в том репозитории; коммитов не несёт.

```
cd ~/Documents/GitHub/materials
python3 _generator/tools/git_zona.py poteri --branch zahod/gejt-vhozhdeniya
python3 _generator/tools/git_zona.py zakryt-vetku zahod/gejt-vhozhdeniya
```
🔴 **Сначала `poteri`, и только если он говорит «терять нечего».** Скажет иначе — **СТОП**: значит ветка не та, за которую я её принял.

⚠ Одноимённая ветка в репозитории `disciplina` **живая и нужная** — её не трогать. Проверь, в каком репозитории стоишь, до команды.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Семь условий:

1. `~/Documents/GitHub/README.md` — **не больше пяти строк** (`wc -l`), и в них есть адрес нового дома.
2. `KAK-USTROEN-GITHUB.md` содержит всё, чего не было: **список тем корневого README до и после** — какие темы были у него, какие теперь есть в плагине, пустых пересечений ноль.
3. **Живой прогон двери, не бумага:** `register_doc.py` на корневом `.md` в `disciplina` → `rc=0`, строка появилась в `README.md` (`grep`); он же на файле `_studio/` в `materials` → ведёт себя как раньше. Оба вывода дословно.
4. `GEJT-VHOZHDENIYA.md`, `KAK-USTROEN-GITHUB.md` и `OTVETY-INTERVYU-2026-08-07.md` — все три под git и в карте (`git ls-files` + `grep` по `README.md`).
5. `GIT_ZONA_REPO=… git_zona.py check --zone .` в `disciplina` → ✅ **целиком**, а не по зонам: файл-сирота был единственной причиной красного.
6. В `materials`: `git branch --list 'zahod/gejt-vhozhdeniya'` → пусто; в `disciplina` та же ветка **на месте**. Оба вывода в отчёте.
7. Два коммита, оба хэша; `git show --stat` каждого — только свои пути.

**Условие 6 читается в обе стороны** — оно доказывает и что мусор снят, и что нужное не задето.

## 3. ВЕРИФИКАТОР

Не нужен: три работы из четырёх обратимы одним `git revert`, четвёртая (снятие ветки) проверяется `poteri` до действия.

⚠ Одно исключение, где ты заменяешь верификатора сам: **слияние двух текстов в §2.1 — лоссовая операция**. Приложи в отчёт список тем корневого README (по заголовкам, командой) и напротив каждой — где она теперь. Тема, потерявшаяся при слиянии, исчезнет молча: файл-источник ты сам же и обнулишь.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
# в disciplina (свои пути)
cd ~/Documents/GitHub/disciplina
git --no-optional-locks add -- KAK-USTROEN-GITHUB.md README.md OTVETY-INTERVYU-2026-08-07.md
git --no-optional-locks commit -m "вход уровня папки GitHub + дверь регистрации: три корневых документа под git" -- KAK-USTROEN-GITHUB.md README.md OTVETY-INTERVYU-2026-08-07.md
GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 $MAT/_generator/tools/git_zona.py check --zone .

# в materials (свои пути)
cd ~/Documents/GitHub/materials
git --no-optional-locks add -- _generator/tools/register_doc.py _generator/tools/korni.py
git --no-optional-locks commit -m "дверь регистрации знает корень disciplina" -- _generator/tools/register_doc.py _generator/tools/korni.py
python3 _generator/tools/git_zona.py check --zone _generator/
git --no-optional-locks show --stat
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)
**Обновление после СТОП.** Владелец разрешил снять СТОП самостоятельно: заведён worktree дверью `git_zona.py worktree add vhod-i-dver` (rc=0) → `/Users/ivanyakovlev/Documents/GitHub/materials-wt/vhod-i-dver`, ветка `zahod/vhod-i-dver` (новая) стоит там сама, HEAD чист. Дальше работаю ИЗ этого worktree (не из основной папки). Заход `kod_vhod-i-dver.md` — файл вне git в основной папке (untracked), поэтому и дальше правлю его по абсолютному пути в основной папке, а зону — в worktree.

§0.1 ВЛИТИЕ (`disciplina`): `git branch --no-merged main | grep zahod` → 2 строки, `zahod/etap-20-i-perenos` и `zahod/gejt-vhozhdeniya` — невлитых 2, не вливаю (чужие работающие заходы), иду дальше.

Прочитаны пять файлов §0. Дальше — план по каждой из четырёх задач.

**§2.1 (слияние README → KAK-USTROEN-GITHUB.md).** Темы root README (по заголовкам): §1 роутер «ситуация→дом» (13 строк) пересекается с существующей в `KAK-USTROEN-GITHUB.md` двухстрочной сводкой домена «как работаем / что делаем» — не дословно, а по детализации (13 строк детальнее 2), поэтому не дубль на сборку, а уточнение: оставляю обе, детальную — следом за сводкой. §2 таблица репозиториев (7 строк) — в `KAK-USTROEN-GITHUB.md` аналога нет вовсе (вопреки формулировке задачи «у обоих есть таблица репозиториев» — по факту она есть только у root; предпосылка неточна, но не блокирует: переношу как чистое новое содержание, командой `ls -d ~/Documents/GitHub/*/`, как и стоит в root). §3 таблица скиллов — заново не изобретаю причину класть её именно сюда: `KAK-USTROEN-GITHUB.md` в собственном подвале уже пишет «Ниже — `README.md` каждого репозитория; там методология» — то есть эта тема штатно живёт в `disciplina/README.md`, где по факту уже есть более полная её версия (таблица «Подключено?» с 6 строками против 4 в root). Переношу §3 как есть в `KAK-USTROEN-GITHUB.md` (буквальное требование «содержание сливается целиком», ничего не теряю), но одной строкой отмечаю дубль с `README.md` как находку для владельца в `## ВОПРОСЫ`, не разрешаю сам. Раздел «⚠ Дыра — названа, а не закрыта» описывает ровно эту задачу как нерешённую — после слияния он лжёт («решение… не принято»), поэтому заменяю его на короткую пометку о закрытии (владелец решил 07.08, дыра закрыта) — это не самостоятельное решение по существу, а фиксация уже принятого. Верхний файл — трёхстрочная заглушка с адресом.

**§2.2 (дверь принимает `disciplina`).** `register_doc.py` определяет карту через `korni.карта_для(rel)`, а тот матчит корень по ПЕРВОМУ компоненту пути. У корневого файла `disciplina` (без подпапки) первый компонент — это имя самого файла, под общую схему «имя корня = имя папки» он не ложится ни при какой добавленной строке таблицы `КОРНИ` — здесь недостаточно одной строки данных, нужна одна ветка логики в `корень_пути()`: «путь без `/` и активный `REPO.name == "disciplina"` → корень с именем `disciplina`» (проверено: файл-заход №0 в §0.1 подтвердил, что реестр читается через `GIT_ZONA_REPO`, то есть `REPO` — динамический). Материалс не задет: ветка условия жёстко привязана к `REPO.name == "disciplina"`, обычная ветка (первый == имя папки) не меняется — регрессия проверяется прогоном на `_studio/README.md` до/после. Карта — `README.md`, но у него сегодня НЕТ раздела `## §6.`, а `вырезать_раздел_6()` (из `check_kartoteka.py`, только прочитан, не правится) ищет строго этот заголовок — значит раздел `## §6.` в `disciplina/README.md` нужно завести руками (это не «регистрация руками», это заведение самой секции, в которую впишется дверь; без неё дверь ответит «вписывать некуда» при rc=2, ровно как это уже происходит у новой фабрики в комментариях `check_kartoteka.py`). Регистрация — «добровольна» (симметрично `obzory`: гейта `check_kartoteka --gates` для `disciplina` сегодня нет вовсе, значит «обязательна» была бы неправдой), `ворота=False`, `журнал=None` (не прошу реестр знать про `disciplina/zhurnal/` — не входит в задачу).

🔴 **Ложная предпосылка найдена.** Задача просит «зарегистрируй GEJT-VHOZHDENIYA.md» — такого файла нет НИГДЕ в дереве `disciplina` (`find . -iname "*GEJT-VHOZHDENIYA*"` — пусто; есть только ветка `zahod/gejt-vhozhdeniya` со своим worktree — та самая, что попала в невлитые §0.1 и явно названа «работающим заходом»). Файл существует, видимо, только на этой чужой неслитой ветке — не в `main`, откуда я читаю. Регистрировать нечего: файла нет на диске, `register_doc.py` и сам откажет («Файла нет на диске»). Не выдумываю обходов — регистрирую только `KAK-USTROEN-GITHUB.md`, про второй пишу в `## ВОПРОСЫ`.

**§2.3 (OTVETY-INTERVYU под git).** Файл подтверждён untracked (`git status --short` → `?? OTVETY-INTERVYU-2026-08-07.md`). Ввести туда же дверью из §2.2 (тот же корень `disciplina`), затем в коммит §2.3 вместе с §2.2 (обе правки — один коммит в `disciplina`, как показано в шаблоне §4 файла-захода).

**§2.4 (снятие пустой ветки).** Строго по тексту: `poteri` СНАЧАЛА, `zakryt-vetku` только если «терять нечего»; команды — в основной папке `materials` (не в worktree — worktree делит ту же git-базу, ветка `zahod/gejt-vhozhdeniya` в `materials` не связана с одноимённой в `disciplina`, проверено §0.1 и текстом задачи).

**Порядок исполнения:** 2.2 (дверь+секция) → живой прогон регрессии `_studio/README.md` → 2.2 регистрация `KAK-USTROEN-GITHUB.md` → 2.3 (git add + регистрация OTVETY) → коммит зоны `disciplina` (§2.2+2.3 вместе, один коммит) → 2.1 (слияние + заглушка, второй коммит `disciplina`) → 2.4 (в `materials`, без коммита — это не файл зоны, а служебная git-операция).

## ВОПРОСЫ — (заполняет исполнитель)
1. `GEJT-VHOZHDENIYA.md` — файла нет ни в `disciplina/main`, ни где-либо в рабочем дереве репозитория `disciplina`; существует, похоже, только на неслитой ветке `zahod/gejt-vhozhdeniya` (см. `## ПЛАН` §2.2), которая по §0.1 принадлежит чужому работающему заходу. Зарегистрировать нечего — регистрирую только `KAK-USTROEN-GITHUB.md`. Нужно решение: файл потерялся, или он появится после того, как чужой заход `zahod/gejt-vhozhdeniya` (в `disciplina`) вольётся — тогда регистрация войдёт в тот заход, а не в этот.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. Таблица скиллов из root README §3 после слияния дублирует уже существующую (более полную) таблицу в `disciplina/README.md` «Что внутри». Перенёс §3 в `KAK-USTROEN-GITHUB.md` буквально (задача требует «содержание сливается целиком», ничего не терять), но по духу источника правды одной сущности — один дом; решать, оставлять ли дубль или свести к ссылке — не мне.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
3. `git_zona.py check --zone .` не ловит проблему с корневыми файлами репозитория: префикс `.` сравнивается как строка, а имя файла вроде `OTVETY-INTERVYU-2026-08-07.md` с точки не начинается — фильтр молчит там, где `check` без `--zone` честно красный. Живой прогон — в `## ОТЧЁТ`, критерий 5. Инструмент не в моей зоне, цены инцидента не знаю (нашёл тестом, не по автологу) — не оформляю уроком, только вопросом: возможно, стоит чинить `--zone .` как синоним «весь корень», раз реестр `korni.py` теперь умеет корневые файлы.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + ЗАЧЕМ:**
1. **§2.1** — содержание `~/Documents/GitHub/README.md` слито в `disciplina/KAK-USTROEN-GITHUB.md`; наверху корневого файла — 4-строчная заглушка с адресом. Раздел «⚠ Дыра — названа, а не закрыта» (который описывал ровно эту нерешённую задачу) заменён на «⚠ Дыра закрыта 2026-08-06», иначе документ лгал бы сам себе сразу после слияния.
2. **§2.2** — `korni.py`: новая строка `Корень("disciplina", "README.md", …)` + одна ветка логики в `корень_пути()` (корневой файл репозитория `disciplina` не ложится в схему «первый компонент пути = имя папки», нужна отдельная проверка `REPO.name == "disciplina"`, строго ограниченная этим именем, чтобы `materials` не задело). `disciplina/README.md`: заведён раздел `## §6.` — дверь `вырезать_раздел_6()` требует именно этот заголовок, а без раздела «вписывать некуда» (rc=2). Раздел поставлен ПОСЛЕДНИМ в файле — граница `§6` тянется до конца файла, секция посередине проглотила бы весь текст ниже (нашёл на первом же прогоне, переставил).
3. **§2.3** — `OTVETY-INTERVYU-2026-08-07.md` зарегистрирован дверью и уехал в тот же коммит, что и §2.2.
4. **§2.4** — пустая ветка `zahod/gejt-vhozhdeniya` снята в `materials` (не в `disciplina`). Команда из задачи (`zakryt-vetku zahod/gejt-vhozhdeniya` позиционным аргументом) не сработала — инструмент требует `--branch zahod/gejt-vhozhdeniya` (см. его `--help`); задача устарела относительно живого CLI, поправил на месте, это не решение по существу, а синтаксис.

**Как проверил (по критериям готовности, дословно где важно):**
1. `wc -l ~/Documents/GitHub/README.md` → `4` (≤5); первая строка после заголовка — адрес `disciplina/KAK-USTROEN-GITHUB.md`.
2. Темы root README → куда легли (по заголовкам, командой `grep -n "^#" `):
   - `§1. Ситуация → адрес` (13 строк) → раздел «Что внутри и как делится» → подраздел «Детальный роутер — ситуация → адрес» (не дубль: уточняет уже бывшую там 2-строчную сводку домена, не повторяет её дословно).
   - `§2. Где что лежит — репозитории корня` (7 строк) → новый раздел «Репозитории корня» (аналога в `KAK-USTROEN-GITHUB.md` не было вовсе).
   - `§3. Скиллы — что подключено, а что нет` → новый раздел «Скиллы — что подключено, а что нет», перенесён целиком по букве задачи; найден дубль с уже существующей более полной таблицей в `disciplina/README.md` «Что внутри» — не разрешил сам, см. `## ВОПРОСЫ` №2.
   Пустых пересечений (потерянных тем) нет — ничего не собиралось «в никуда».
3. Живой прогон двери, дословно:
   ```
   $ python3 _generator/tools/register_doc.py _studio/README.md "тест поведения до правки"
   Уже зарегистрирован — второй строки не завожу: _studio/README.md
   RC=0
   $ GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 _generator/tools/register_doc.py ~/Documents/GitHub/disciplina/KAK-USTROEN-GITHUB.md "…"
   ✅ Зарегистрирован: KAK-USTROEN-GITHUB.md
      README.md:100 — вписано: …
   RC=0
   ```
   `_studio/README.md` до и после моей правки `korni.py` — идентичный ответ («уже зарегистрирован», rc=0): регрессии в `materials` нет.
4. `git ls-files | grep -E "KAK-USTROEN-GITHUB.md|OTVETY-INTERVYU-2026-08-07.md|GEJT-VHOZHDENIYA.md"` → только первые два; `grep` по `README.md` → обе строки на месте (47, 100, 102). Третий файл — см. `## ВОПРОСЫ` №1 (не существует на диске).
5. `GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 $MAT/_generator/tools/git_zona.py check --zone .` → `✅ зона .: работа доехала в git, вне git ничего нет.` (RC=0); без `--zone` тем же путём — тоже `✅ всё дерево: работа доехала в git…`. **Находка по дороге:** `--zone .` при незакоммиченном `OTVETY-INTERVYU-2026-08-07.md` тоже отвечал ✅ — префикс `.` не матчит имя файла, не начинающееся на точку, поэтому корневые файлы этим фильтром не проверяются вовсе (без `--zone` тот же прогон честно был красным). Инструмент не в моей зоне — не правил, вынес в `## ВОПРОСЫ` №3.
6. `git branch --list 'zahod/gejt-vhozhdeniya'` в `materials` → пусто; в `disciplina` → `+ zahod/gejt-vhozhdeniya` (на месте). Оба прогнаны последним ходом, см. команды выше.
7. Два коммита:
   - `disciplina` `9c66c2c774e85ba49ef6c79fcf93ea1d5bcccd0f` — «вход уровня папки GitHub + дверь регистрации: три корневых документа под git» — `git show --stat`: только `KAK-USTROEN-GITHUB.md`, `OTVETY-INTERVYU-2026-08-07.md`, `README.md`.
   - `materials` `dcc5b8a18a79d481973c550dd6e7ed454caf9337` — «дверь регистрации знает корень disciplina» — `git show --stat`: только `_generator/tools/korni.py`. Коммит прошёл через фикстуру `PROGNAT.sh` целиком зелёной (хук `pre-commit` её прогнал сам).

**Что НЕ трогал:** `disciplina/skills/`, `disciplina/doma/`, `_generator/tools/check_zahod.py`, `_generator/tools/fixtures/` — не открывал; `_generator/tools/register_doc.py` — прочитан, не правлен (хватило правки `korni.py`, регистрация заработала без него); `_generator/tools/check_kartoteka.py` — прочитаны только две функции grep’ом (`вырезать_раздел_6`, `зарегистрирован`), чтобы понять формат раздела `§6` для `disciplina/README.md`, файл не правил; ветку `zahod/vhod-i-dver` не переключал; ветку `zahod/gejt-vhozhdeniya` в `disciplina` не трогал.

**Вопросы:** три, см. `## ВОПРОСЫ`.

**Результат верификатора:** заменён ручной сверкой тем §2.1 (см. «Как проверил» п.2) — по инструкции файла верификатор для остального не нужен.

**Открытое «возвращаться»:** одно — регистрация `GEJT-VHOZHDENIYA.md` (§2.2), заблокирована отсутствием файла на `main`, см. `## ВОПРОСЫ` №1. Остальное закрыто.

**Необратимое:** ветка `zahod/gejt-vhozhdeniya` в `materials` удалена (`git_zona.py zakryt-vetku`) — восстанавливается командой `python3 _generator/tools/git_zona.py voskresit --branch zahod/gejt-vhozhdeniya` (надгробие `mogila/zahod/gejt-vhozhdeniya → 225932d3`, инструмент напечатал это сам при удалении). Больше ничего необратимого не делал: остальное — правки под git с коммитами (откат — `git revert`).

**Время прогона + токены:** не считаю — снимается приёмкой из лога прогона (см. `result`-строку), `/cost` не существует.

**АРТЕФАКТ:** артефакта нет: заход про правку кода и документов, не про сборку. Файлы, которые владелец должен открыть: `/Users/ivanyakovlev/Documents/GitHub/disciplina/KAK-USTROEN-GITHUB.md` (слитый вход уровня папки), `/Users/ivanyakovlev/Documents/GitHub/disciplina/README.md` (новый раздел `§6`), `/Users/ivanyakovlev/Documents/GitHub/README.md` (заглушка, вне git).

**КОММИТ:** `disciplina` `9c66c2c774e85ba49ef6c79fcf93ea1d5bcccd0f` — «вход уровня папки GitHub + дверь регистрации: три корневых документа под git» · `git_zona.py check --zone .` → ✅. `materials` `dcc5b8a18a79d481973c550dd6e7ed454caf9337` — «дверь регистрации знает корень disciplina» · `git_zona.py check --zone _generator/` → ✅ (в worktree `/Users/ivanyakovlev/Documents/GitHub/materials-wt/vhod-i-dver`, ветка `zahod/vhod-i-dver`, worktree оставлен — не сносил, в файле-заходе снятие не требовалось).

---

## ПРИЁМКА — заполняет аналитик, 2026-08-07

**ПРИНЯТ.** Два коммита: `9c66c2c` в `disciplina` (три файла, +295) и `dcc5b8a` в `materials` (`korni.py`, +27).

**Перепроверено приёмкой своими командами:**

| проверка | результат |
|---|---|
| корневой `README.md` | ✅ **4 строки**, первая — адрес нового дома |
| `§6. Карта документов корня` в `disciplina/README.md` | ✅ строка 96, три записи |
| `OTVETY-INTERVYU-2026-08-07.md` под git | ✅ +228 строк в коммите |
| ветка-мусор в `materials` | ✅ снята, в `disciplina` одноимённая цела |
| регрессия двери в `materials` | ✅ нет: `_studio/README.md` отвечает как раньше |

### Что сделано тонко, и это стоит назвать

**Он поймал две ловушки формы, которых я не предвидел.** Раздел `§6` пришлось ставить **последним в файле**: дверь тянет границу раздела до конца, и секция посередине проглотила бы весь текст ниже — нашёл на первом прогоне и переставил. И в корневом README он заменил раздел «⚠ Дыра — названа, а не закрыта» на «⚠ Дыра закрыта», иначе **документ лгал бы сам себе сразу после слияния**.

**Правка `korni.py` ограничена именем репозитория** — корневой файл `disciplina` не ложится в схему «первый компонент пути = имя папки», понадобилась отдельная ветка логики, и он сузил её проверкой `REPO.name == "disciplina"`, чтобы `materials` не задело. Регрессию доказал прогоном до и после.

### 🔴 Находка про `--zone .` — задевает мой собственный гейт приёмки

`check --zone .` при незакоммиченном файле в корне отдал **✅**, а тот же `check` **без** `--zone` честно покраснел: префикс `.` не матчит имя, не начинающееся с точки. То есть **форма «зона — весь репозиторий» тише формы без зоны**.

Это гейт 0, которым я открываю каждую приёмку, и сегодня на нём принято девять отчётов. → урок 46, и до починки **приёмка гоняет `check` без `--zone` вторым ходом**.

### Ответы на вопросы

**Вопрос 1 — `GEJT-VHOZHDENIYA.md` не потерялся.** Он живёт на невлитой ветке `zahod/gejt-vhozhdeniya`, которую вливает заход `etap-20-i-perenos` — там его задание. Регистрация войдёт туда же, а не сюда; поступил верно, что не стал искать файл, которого на `main` нет.

**Вопрос 2 — таблица скиллов: свести к ссылке, но не сейчас.** Дубль настоящий: в `disciplina/README.md` «Что внутри» таблица полнее. Правило одного дома требует ссылки, но `README.md` сейчас в зоне двух заходов подряд, и трогать его третьим ходом за час — лишний риск ради косметики. Записано долгом.

**Вопрос 3** — см. урок 46 выше.

### Что закрыто этим заходом

Знание про папку `GitHub` теперь живёт **в одном доме под git** и едет на любую машину вместе с плагином. Дверь регистрации знает оба репозитория. Файл с вашими 29 решениями больше не висит вне git — на него ссылаются, и теперь ссылка обеспечена.
