# Канал исполнителя — hvosty (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — четыре хвоста, каждый расписан командой; там, где нужно суждение, стоит СТОП.

КОНТЕКСТ. Плагин `disciplina` из семи скиллов подключён и работает. За 07.08 отработали тринадцать заходов, все приняты. Осталось четыре хвоста — ни один не про содержание скиллов, все про то, чтобы система была сведена.

**Главный хвост — отключение дублирующих внешних скиллов.** 07.08 проведён холодный прогон: 18 живых фраз владельца, на каждую свежий субагент с пустым контекстом выбирал один скилл из 68 (наши семь + 62 установленных). Результат — **11 из 18** (число и вся таблица — в `disciplina/zhurnal/PROGON-triggerov.md`, читаешь его первым по §0), и четыре перехвата пришлись на чужие скиллы. Список не гипотеза, а замер.

ЦЕЛЬ: свести хвосты — подготовить отключение и проверить его, подключить третий гейт петли, разобрать конфликт, подмести папки.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП: гасить скиллы ты не можешь и не пытаешься.** Они управляются облачным реестром, доступа к нему у исполнителя нет. Твоё дело — дать владельцу список, который он выполнит за минуту, и **проверить ПОСЛЕ**, что погашенное действительно погасло. Проверка после — половина работы, не формальность.

🔴 **Параллельно идёт заход `dver-urokov`.** Он правит `korni.py`, `register_doc.py` и заводит `dostavit_urok.py`. **Эти три файла тебе READ-ONLY**, даже если покажется, что «в одну строку». Нужна их правка — СТОП и в `## ВОПРОСЫ`.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/wt-hvosty`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/hvosty`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add wt-hvosty --branch zahod/hvosty
  cd ../materials-wt/wt-hvosty
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/bootstrap_zahod.py` · `_studio/zhurnal/2026-08-05_faza-lenty/OTKLYUCHENIE.md` (заводишь) · `_studio/docs/KARTA.md` (только строкой регистрации через дверь). 🔴 **Отдельно и явно: §2.3 — СЛИЯНИЕ, и оно по построению трогает файлы вне этого списка.** Это разрешено ровно для влития принятой ветки `zahod/dobor-incidentov` и ни для чего больше: своей рукой за пределами зоны ты не правишь ничего. `check_sborki.py` — READ-ONLY, ты его только зовёшь.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/hvosty`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: зафиксируй `git rev-parse HEAD` в основной папке первым ходом и назови в отчёте — §2.3 делает слияние, оно откатывается только так.
- **Прочитать ТОЛЬКО это:**
  1. `~/Documents/GitHub/disciplina/zhurnal/PROGON-triggerov.md` — результат прогона, откуда берётся список на отключение. Таблицу и три числа, не весь файл.
  2. `_generator/tools/bootstrap_zahod.py` — целиком, ты его правишь.
  3. `_generator/tools/check_sborki.py` — только сигнатуру запуска и коды возврата (`--help` и docstring), тело читать не надо.
  4. `_studio/zhurnal/2026-08-05_faza-lenty/kod_dobor-incidentov.md` — секции `## ОТЧЁТ` и `## ПРИЁМКА`, чтобы понять, что за работа в ветке с конфликтом.
  Проект дальше не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Четыре хвоста, порядок важен: §2.1 даёт владельцу работу, которую он делает, пока ты делаешь остальное.

### 2.1 🔴 ПЕРВЫМ ХОДОМ — список на отключение, чтобы владелец начал сразу

Из прогона: **`html-slides-studio` · `popsci-research` · `consolidate-memory` · `skill-creator`.**

Заведи `_studio/zhurnal/2026-08-05_faza-lenty/OTKLYUCHENIE.md` — короткий, на один экран. По каждому скиллу три строки:

1. **что гасим** — точное имя, как оно видно владельцу в интерфейсе;
2. **чем доказано** — номер строки прогона и чей триггер он перехватил;
3. **что сломается** — честно. Гасим `html-slides-studio` → на фразах 15 и 19 не останется носителя вовсе, пока `slajdy` в статусе черновика с запретом автозапуска. Это ожидаемо и временно, но владелец должен знать до, а не после.

Ниже — **как проверить, что погасло**: команда или место в интерфейсе, где видно состояние. 🔴 Проверку **напиши так, чтобы её выполнил ты, а не владелец** — сравнение списка установленных скиллов до и после.

⚠ **Не гаси ничего сам и не предлагай гасить больше четырёх.** Прогон дал ровно этот список. `the-interviewer` и `session-handoff` проверены и **перехвата не дали** (18 из 18) — их в список не вноси, даже если по описанию кажется, что стоило бы.

Файл — **новый `.md`**, регистрируй тем же ходом; `_studio/docs/KARTA.md` допиши к путям в ОБОИХ ходах коммита.

### 2.2 Третий гейт петли — в `bootstrap_zahod.py`

`check_sborki.py` собран и принят 07.08: он **исполняет** то, что заход утверждает о мире, и краснеет на расхождении. Сейчас его надо звать руками, и потому его не зовут.

Встрой вызов в конец сборки захода: собрали файл → сразу прогнали → вывод показали. Тогда несуществующая ветка видна **в момент сборки**, а не на первом ходе исполнителя — это вердикт урока 41 дословно, и он уже стоил двух холостых прогонов Opus.

Три ограничения, каждое из живого замера:
- **режим только «свежий заход»**, никакого «весь корпус»: 91 файл из 176 корпуса красный, полтора часа прогона против 0.8 с на файл;
- **красный гейт сборку НЕ отменяет** — он печатает. Файл-заход рождается с ошибками по построению (worktree ещё не заведён, `DOLG.md` ещё не создан), и отказ сборки на этом сделает инструмент неприменимым;
- вывод отделён от остального так, чтобы было видно: это про только что собранный файл.

Проверка фактом: собери **пробный заход** этим же инструментом, покажи вывод целиком, потом **удали пробный файл и его строку регистрации** и скажи, что удалил.

### 2.3 Конфликт `zahod/dobor-incidentov` — единственная невлитая ветка

Ветка принята приёмкой 07.08, но не влилась: конфликт в `_studio/docs/KARTA.md`, обе стороны дописывали регистрации.

🔴 **Конфликт здесь механический, и только поэтому ты его разрешаешь сам:** `KARTA.md` — список регистраций, и правильное разрешение — **сохранить обе стороны**, ни одной строки не потеряв. Если после разбора окажется, что конфликт НЕ такой (задеты не только строки регистрации) — `--abort`, в `## ВОПРОСЫ`, дальше по списку.

Проверка фактом: число строк регистрации в `KARTA.md` **до слияния с каждой стороны** и **после** — сумма обязана сойтись, оба слагаемых и итог привести. Затем `git branch --no-merged | grep zahod` → пусто.

### 2.4 Подмести — 10 рабочих папок

Три в `materials-wt/`, где `zakryt-vetku` отказал с названной причиной, и семь в `disciplina-wt/`.

```
python3 _generator/tools/git_zona.py poteri
GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 _generator/tools/git_zona.py poteri
```

🔴 **Сноси только те, где `poteri` говорит «терять нечего».** Три случая уже известны и каждый требует решения, а не силы: надгробие уже существует · один незакоммиченный путь внутри · игнорируемые файлы `.egg-info`. **Ни один из трёх не обходи флагом `--vsyo-ravno`** — назови в отчёте и оставь.

🔴 **НЕ трогай папки работающих заходов:** `wt-dver` (идёт параллельно) и `linter-zahoda-wt` (не зарегистрирована как worktree, чья — не установлено).

### 2.5 После того как владелец погасил — проверить

Владелец скажет, что погасил. Тогда: сними список установленных скиллов, сравни с тем, что было до, и предъяви **разницу**, а не утверждение. Погашено меньше четырёх — назови поимённо, что осталось.

Не дождался ответа владельца за время работы — **это не повод ждать**: доделай остальное, а проверку назови непроведённой в отчёте.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Шесть условий:

1. `OTKLYUCHENIE.md` заведён, зарегистрирован, содержит **4 скилла из 4** с доказательством у каждого и последствием у каждого.
2. **Гейт зовётся из `bootstrap_zahod.py`, доказано ЖИВЫМ ПРОГОНОМ на реальном объекте:** пробный заход собран, вывод гейта показан целиком, пробный файл и его регистрация убраны — оба факта командой.
3. **Невлитых веток `zahod/*` в `materials` — 0** (кроме папок работающих заходов, у которых веток может не быть). Команда и вывод дословно.
4. **Ни одной строки регистрации не потеряно:** три числа по `KARTA.md` (сторона A, сторона B, итог), сумма сходится.
5. **Рабочих папок было / стало**, оба числа командой; папка `wt-dver` **на месте** — проверено `ls`; три отказавших случая названы поимённо, флаг обхода не применялся ни разу.
6. **Проверка отключения проведена или честно названа непроведённой.** Проведена → разница списков дословно.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

⚠ **Условие 4 — самое дорогое при провале.** Потерянная строка регистрации не видна ничем до первого `checkout`, и документ становится сиротой молча. Проверяй числами, а не глазами.

## 3. ВЕРИФИКАТОР

Нужен ровно на §2.3 — слияние лоссовое, и потеря строки в `KARTA.md` необнаружима на глаз.

Свежий субагент, другой метод: берёт `git show <сторона A>:_studio/docs/KARTA.md` и `git show <сторона B>:…`, собирает множество строк регистрации из обеих и сверяет с итогом **как множества, а не как текст**. Ответ — «список потерь ПУСТ» либо перечень потерянного.

Остальные три хвоста не лоссовые: файлы заводятся, папки сносятся только с зелёным `poteri`, гейт печатает и ничего не меняет.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/bootstrap_zahod.py` `_generator/tools/check_sborki.py` `_studio/zhurnal/2026-08-05_faza-lenty/OTKLYUCHENIE.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/bootstrap_zahod.py` `_generator/tools/check_sborki.py` `_studio/zhurnal/2026-08-05_faza-lenty/OTKLYUCHENIE.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Рабочая папка заведена (`../materials-wt/wt-hvosty`, ветка `zahod/hvosty` подтверждена `git rev-parse --abbrev-ref HEAD`). Точка отката (HEAD основной папки на момент старта): `d201f293ed12136e9d51d9a807f7b7300c34a37c`.

Прочитаны все 4 названных якоря. Два из них дали основание оспорить предпосылку по §1 («считаешь критерий/задачу кривой — скажи ДО работы, предложи поправку») — привожу с доказательством, дальше план по остатку.

### Находка А — предпосылка §2.1 («список на отключение, чтобы владелец начал сразу») устарела

`PROGON-triggerov.md` физически существует ТОЛЬКО в `disciplina-wt/wt-evals/zhurnal/` (ветка `zahod/evals`, не влита); в каноническом `disciplina/zhurnal/` его нет вовсе — сам по себе адрес в моём заходе неточен (см. пункт очереди ниже). Прочитан из рабочей папки.

Файл прямо запрещает то, что §2.1 просит подготовить как готовое к действию: **«Отключать их сейчас нельзя: решение владельца 07.08 (`OTVETY-INTERVYU-2026-08-07.md` п.5) — "можно их отключить, но только после проверки, что они стали частью новых скиллов". Этот список — вход в гейт вхождения, а не сам гейт»** (§3). Проверил решение владельца по первоисточнику — `disciplina/OTVETY-INTERVYU-2026-08-07.md` (канонический, закоммичен, дата коммита 2026-08-06 19:20): дословно та же цитата, плюс явно: «Отключению предшествует гейт вхождения. Не "отключить и посмотреть" — сначала предъявить, что уникальное содержание каждого доехало в наш скилл, и только потом гасить».

Гейт вхождения для ДРУГИХ трёх скиллов (`session-handoff`, `the-interviewer`, `agentic-coding-session-brief`) уже прошёл и влит (`zahod/gejt-vhozhdeniya`, merge 2026-08-06 21:18, `session-handoff` уже архивирован). Для НАШИХ четырёх (`html-slides-studio` · `popsci-research` · `consolidate-memory` · `skill-creator`) — не начинался: PROGON называет их только «готовым списком владельцу», не «готовыми к отключению».

**Поправка:** `OTKLYUCHENIE.md` завожу — критерий 1 требует ровно этого (4 из 4, доказательство + последствие у каждого), и эта часть предпосылки не ложна. Но файл открывается блоком «ЗАБЛОКИРОВАНО ГЕЙТОМ ВХОЖДЕНИЯ» с точной цитатой владельца и адресом обоих источников — НЕ призывом отключить сейчас. «Как проверить, что погасло» пишу как и просили: она нужна независимо от даты отключения, и снапшот для неё снимаю уже сейчас (см. ниже).

### Находка Б — §2.2 физически невыполним в этом окне

`_generator/tools/check_sborki.py` отсутствует в `arka/mat-kostyak` (это база моей ветки, не `main` — в репозитории `main` вообще нет, дефолт удалённого репо и текущая ветка основной папки разные вещи, проверено `git symbolic-ref`/`git branch --show-current`) и во всём дереве канонической `materials`. Файл существует ТОЛЬКО в невлитой ветке `zahod/gejt-sborki`, которая вдобавок вычекаучена в рабочую папку ДРУГОГО захода — `materials-wt/wt-sborki`. `git_zona.py doctor` предупреждает по этой ветке прямым текстом: «ветка вычекаучена в рабочей папке… там может идти живой заход — merge притащит недоделанное; сперва спроси того, кто в ней работает».

Мой контракт зоны разрешает слияние РОВНО одной ветки — `zahod/dobor-incidentov`, «и ни для чего больше» (§КОНТРАКТ). Влить `zahod/gejt-sborki` — нарушение контракта; скопировать файл руками мимо `git` — то же самое слияние в обход двери, только тише. Задача §2.2 («встрой вызов в конец сборки», «докажи живым прогоном») без файла на диске в моей ветке невыполнима ни тем, ни другим способом.

**Поправка:** §2.2 — СТОП, в `## ВОПРОСЫ`. Не блокирует §2.1/2.3/2.4/2.5.

### Порядок действий
1. §2.3 — конфликт `zahod/dobor-incidentov`: worktree `wt-dobor` чист (`git status --short` пусто), ветка принята приёмкой 07.08 (коммит `34f4ebc`, `kod_dobor-incidentov.md` §ПРИЁМКА). Разрешаю сам, слияние строк регистрации `KARTA.md`, верификатор — свежий субагент (§3 захода).
2. §2.4 — `poteri` в обоих репо (`materials`, `disciplina` через `GIT_ZONA_REPO`), сношу только зелёное, три известных случая — не трогаю, `wt-dver`/`linter-zahoda-wt` — не трогаю. Дополнительно НЕ трогаю `wt-dobor` (до влития) и `wt-sborki` (чужой живой заход, Находка Б).
3. §2.1 (с поправкой Находки А) — завести `OTKLYUCHENIE.md`, зарегистрировать тем же ходом.
4. §2.2 — не делаю (Находка Б), фиксирую как непройденный пункт с доказательством.
5. §2.5 — снапшот `inventar.py` «до» уже снят (`disciplina/tools/inventar.py`, вывод сохранён в scratchpad); сама сверка «после» — по факту ответа владельца в течение окна работы, иначе честно «непроведено» (её и так блокирует Находка А: гасить нечего, пока владелец не снял блокировку гейта).
6. §4 — коммит зоны (`bootstrap_zahod.py` с найденной, но не встроенной правкой №2.2 — то есть БЕЗ правки, раз она невыполнима; `OTKLYUCHENIE.md`; регистрация в `KARTA.md`).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. §2.1 заблокирован гейтом вхождения, а не готов к исполнению. `PROGON-triggerov.md` §3 и `OTVETY-INTERVYU-2026-08-07.md` п.5 (оба живых файла) прямой цитатой владельца запрещают отключать эти четыре скилла до проверки, что их уникальное содержание доехало в наши скиллы — этот гейт для данных четырёх не начинался (в отличие от `session-handoff`/`the-interviewer`/`agentic-coding-session-brief`, чей гейт уже влит `zahod/gejt-vhozhdeniya`). `OTKLYUCHENIE.md` заведён по форме критерия, но с блокирующим статусом наверху, а не приглашением отключить. Нужно решение: заводить отдельный заход «гейт вхождения для четырёх» по образцу уже пройденного, или это уже входит в чей-то план.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. §2.2 невыполним в этом окне: `check_sborki.py` существует только в невлитой ветке `zahod/gejt-sborki`, вычекаученной в рабочую папку другого захода (`materials-wt/wt-sborki`) — моя зона разрешает слияние ровно `zahod/dobor-incidentov` и ничего больше, влить вторую ветку или скопировать файл в обход было бы нарушением контракта. Нужно либо слияние `zahod/gejt-sborki` отдельным ходом (приёмкой/аналитиком), либо явное расширение моего контракта на эту ветку — сам не расширяю.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. Адрес в моём заходе (§0 п.1: `~/Documents/GitHub/disciplina/zhurnal/PROGON-triggerov.md`) не существует — файл физически лежит только в `disciplina-wt/wt-evals/zhurnal/PROGON-triggerov.md` (ветка `zahod/evals`, не влита в `disciplina/main`). Прочитан оттуда. Когда `zahod/evals` вольётся, адрес станет верным сам; до тех пор следующему заходу, которому нужен этот файл, полезно знать, что искать в worktree, а не в каноне.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

4. **Урок фабрике (Находка А):** заход §2.1 ссылался на таблицу PROGON §2 («готовым списком владельцу»), но не на его же §3 тремя абзацами ниже («отключать сейчас нельзя»). Оба абзаца — в ОДНОМ файле, прочитанном аналитиком при составлении захода (файл написан раньше, в тот же вечер). Цена, если бы заход исполнили по букве: 3 живые фразы владельца (№15, №16, №19 прогона) остались бы БЕЗ ВСЯКОГО носителя — `html-slides-studio`/`popsci-research` подхватывают их временно, пока законные получатели (`slajdy`, `reserch-cikl`) в статусе черновика.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

5. **Урок фабрике (Находка Б):** заход §2.2 писал «`check_sborki.py` собран и принят 07.08» так, будто «принят приёмкой» = «доступен в моей ветке». Между ними разрыв: принятая работа лежит в невлитой ветке, а новый worktree форкается от базы, где её ещё нет. Цена при исполнении по букве: код, встраивающий вызов несуществующего файла, либо не запустился бы вовсе, либо (хуже) подтолкнул бы к копированию файла мимо `git` в обход контракта зоны — то же слияние, только без двери и без истории.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-05_faza-lenty/OTKLYUCHENIE.md` — любой markdown-просмотрщик; в основную папку попадёт при влитии ветки, которое делает приёмка (сейчас лежит на ветке `zahod/hvosty` в рабочей папке `materials-wt/wt-hvosty`).
*(собранного HTML/документа/картинки в этом заходе нет — работа не про сборку, а про git-состояние и текстовый файл-список.)*
**КОММИТ:** `9b69e28` — «hvosty: слияние zahod/dobor-incidentov (KARTA.md без потерь) + OTKLYUCHENIE.md…» · `git_zona.py check --zone _studio/zhurnal/2026-08-05_faza-lenty/OTKLYUCHENIE.md` → ✅ `rc=0` «работа доехала в git, вне git ничего нет» · `git show --stat` → ровно 2 пути (`_studio/docs/KARTA.md`, `OTKLYUCHENIE.md`), 56 вставок. Ветка НЕ влита — это ход приёмки. Отдельно есть merge-коммит `e8b6409` (§2.3, до основного коммита зоны) — тоже на этой ветке, тоже ждёт влития приёмкой.

---

### Что сделал и ЗАЧЕМ

Четыре хвоста по порядку §2, с двумя находками, изменившими план ДО работы (см. `## ПЛАН`).

**§2.1 (с поправкой) — `OTKLYUCHENIE.md` заведён и зарегистрирован.** 4 из 4 скиллов (`html-slides-studio` · `popsci-research` · `consolidate-memory` · `skill-creator`), у каждого — номер строки прогона (`PROGON-triggerov.md` §2, строки 15/19, 16, 12, 18) и честное «что сломается». Файл открывается блоком «ЗАБЛОКИРОВАНО ГЕЙТОМ ВХОЖДЕНИЯ» — см. Находку А в `## ПЛАН`: живые файлы (`PROGON-triggerov.md` §3 и канонический `OTVETY-INTERVYU-2026-08-07.md` п.5) прямой цитатой владельца запрещают гасить эти четыре до проверки, что их содержание доехало в наши скиллы, а этот гейт для них не начинался. Список — вход в гейт, не приглашение отключить сегодня.

**§2.2 — НЕ сделано.** `check_sborki.py` не существует в ветке `zahod/hvosty` (и во всём каноне `materials`) — он живёт только в невлитой `zahod/gejt-sborki`, вычекаученной в рабочую папку другого захода (`wt-sborki`). Контракт зоны разрешает мне слияние ровно одной ветки (`zahod/dobor-incidentov`) — слить вторую или скопировать файл в обход было бы нарушением. `bootstrap_zahod.py` не тронут ни байтом. См. Находку Б в `## ПЛАН` и пункт 2 в `## ВОПРОСЫ`.

**§2.3 — конфликт `zahod/dobor-incidentov` разрешён.** Слияние в свою ветку: `git_zona.py merge --continue` (коммит `e8b6409`). Конфликт был ровно в `_studio/docs/KARTA.md §6` — обе стороны дописали новые регистрации поверх общего предка; разрешён сохранением ОБЕИХ сторон целиком (21 запись только-A + 1 запись только-B поверх 78 общих), без выбора «чья строка главнее».

**§2.4 — подметено рабочими папками.** `poteri` в обоих репозиториях, `zakryt-vetku --branch <имя>` поштучно (не `--vse-zelenye` — он не отличает «зелёно по содержимому» от «рядом идёт живая сессия», а мне нужно было явно не задеть `wt-dver`/`wt-dobor`/`wt-sborki`).

**§2.5 — снапшот «до» снят, сверка «после» не проведена** (см. критерий 6 ниже).

### Как проверил — по критерию готовности

**1. `OTKLYUCHENIE.md`:** зарегистрирован (`_studio/docs/KARTA.md:288`), 4 из 4 с доказательством (номер строки `PROGON-triggerov.md`) и последствием у каждого — см. файл.

**2. Гейт `bootstrap_zahod.py`:** НЕ встроен и НЕ доказан живым прогоном — критерий проваливается честно. Причина — Находка Б: `check_sborki.py` физически отсутствует в этой ветке, встраивать вызов несуществующего файла означало бы вписать код, который упадёт при первом прогоне на любой другой ветке без `zahod/gejt-sborki`, и я не могу проверить его живым прогоном, как требует условие 2. Пробный заход не собирал — собирать было бы нечем проверить.

**3. Невлитых `zahod/*` в `materials`:** проверено буквально командой из захода, `git branch --no-merged | grep zahod` (из ветки `zahod/hvosty`, после слияния §2.3):
```
+ zahod/gejt-sborki
```
Одна строка — рабочая папка живого захода (`wt-sborki`), исключение критерия 3 покрывает ровно этот случай. `zahod/dobor-incidentov` из списка ушла — она теперь слита в `zahod/hvosty`.

**4. Строк регистрации `KARTA.md` — числа + верификатор.** Свежий субагент (другой метод — сравнение МНОЖЕСТВ путей, не строк) подтвердил: |база (`225932d`)| = 78, |сторона A (`d201f29`)| = 99, |сторона B (`34f4ebc`)| = 79, |A∩B| = 78 (= базе — стороны не пересекались новыми путями), |ожидаемое A∪B| = 99+79−78 = **100**, |реальный итог (`e8b6409`)| = **100**. Потеряно: 0. Лишнего: 0. Дубликатов и «сломанных» строк парсер не нашёл ни в одной из четырёх версий.

**5. Рабочих папок — было/стало, командой:**
- `materials-wt`: было 8 (включая мою `wt-hvosty`), стало 8 — **0 снесено**. Три кандидата (`dovodka-fazy-intervyu`, `zahod/edinstvennost-sekcij`, `zahod/generator-po-imeni`), все три отказали по трём разным причинам — см. ниже. `wt-dver` — на месте, проверено `ls -d`: `/Users/ivanyakovlev/Documents/GitHub/materials-wt/wt-dver` ✅. `wt-dobor` и `wt-sborki` не трогал (см. `## ПЛАН`, порядок действий). `linter-zahoda-wt` не трогал.
- `disciplina-wt`: было 8 (main + 7 кандидатов), стало 3 (main + 2 отказа) — **6 снесено** (`zahod/etap-20`, `zahod/gejt-vhozhdeniya`, `zahod/marketplace-github`, `zahod/razbor-meta`, `zahod/reshenia-intervyu`, `zahod/standart-skilla`), у каждого поставлено надгробие (`mogila/<ветка>`), воскрешение — одной командой `voskresit --branch <ветка>` (см. НЕОБРАТИМОЕ). `zahod/evals` не трогал (см. `## ВОПРОСЫ` п.3 — там же и не отдающий `.md` живой файл).
- Флаг `--vsyo-ravno` не применялся НИ РАЗУ.
- **Отказавших случаев — четыре, не три** (заход предсказывал три, все в `materials-wt`; живая проверка дала ещё один в `disciplina-wt` — расхождение с предпосылкой, называю честно, а не подгоняю под «три»):
  1. `zahod/dovodka-fazy-intervyu` (materials-wt) — надгробие `mogila/zahod/dovodka-fazy-intervyu` уже существует (на `8fc9951`), кто-то закрывал эту ветку раньше и не досмотрел до конца, либо решение было отменено.
  2. `zahod/edinstvennost-sekcij` (materials-wt) — 1 незакоммиченный путь вне git в рабочей папке (`?? _studio/zhurnal/_INFRA-git/INCIDENTY.md`).
  3. `zahod/generator-po-imeni` (materials-wt) — 5 игнорируемых путей (`_generator/dvizhki.egg-info/*`).
  4. `zahod/etap-20-i-perenos` (disciplina-wt) — 1 незакоммиченный путь вне git (`?? _studio/zhurnal/_INFRA-git/INCIDENTY.md` — тот же относительный путь, что в случае 2, но в другом репозитории).

**6. Проверка отключения — НЕПРОВЕДЕНА, честно.** Владелец не отвечал в течение окна работы, и, что важнее, отвечать пока не на что: §2.1 сам заблокирован гейтом вхождения (см. критерий 1) — гасить нечего, пока блокировка не снята решением владельца. Снапшот «до» снят (`disciplina/tools/inventar.py`, вывод в scratchpad сессии, все 4 скилла на месте: строки 159-160/201-204/259-262/295-296 вывода), пригоден для сравнения, когда/если гейт будет пройден.

### Результат верификатора (§3)

Свежий субагент (fork, независимый от меня контекст), метод — множества путей `_studio/docs/KARTA.md §6`, не текстовое сравнение строк. Вердикт: **список потерь ПУСТ**. Числа приведены в п.4 выше; агент отдельно проверил на дубликаты путей и «сломанные» строки (без парной закрывающей кавычки) в каждой из четырёх версий файла — не нашёл ни одного.

### Что НЕ трогал

`_generator/tools/korni.py`, `register_doc.py`, `dostavit_urok.py` — READ-ONLY по контракту (параллельный заход `dver-urokov`), не открывал на запись ни разу. `check_sborki.py` — не существует в этой ветке, не создавал, не копировал, не трогал ветку `zahod/gejt-sborki` и её рабочую папку `wt-sborki`. `bootstrap_zahod.py` — прочитан целиком, НЕ изменён (§2.2 заблокирован). `wt-dver` (параллельный заход) и `linter-zahoda-wt` (незарегистрированная папка) — не трогал. `wt-dobor` (принятая, но ещё не влитая в базу ветка) — не сносил, оставил приёмке. `zahod/evals` и его рабочую папку `wt-evals` — не трогал (там же лежит незакоммиченный `PROGON-triggerov.md`, который я читал только на чтение). Ветку `zahod/hvosty` в конце НЕ вливал в `arka/mat-kostyak` — влитие делает приёмка.

### Открытое — куда возвращаться

1. Гейт вхождения для четырёх скиллов (`html-slides-studio`/`popsci-research`/`consolidate-memory`/`skill-creator`) не начинался — возможный следующий заход по образцу уже пройденного `gejt-vhozhdeniya` (см. `## ВОПРОСЫ` п.1).
2. `check_sborki.py` ждёт слияния `zahod/gejt-sborki` в базу отдельным ходом — тогда §2.2 станет выполним (см. `## ВОПРОСЫ` п.2).
3. Проверка «после» отключения (критерий 6) — когда владелец снимет блокировку гейта.
4. `zahod/dobor-incidentov` и `zahod/hvosty` (эта ветка) — обе ждут официального влития в `arka/mat-kostyak` руками приёмки.

### НЕОБРАТИМОЕ

Все действия ниже — через `git_zona.py` дверь, с надгробием (тег `mogila/<ветка>`) ПЕРЕД удалением — то есть закрытие ветки+рабочей папки обратимо одной командой `voskresit --branch <ветка>` (тег держит коммит вечно на этом диске). Владелец не видел ни одного из этих действий в момент, когда они происходили (сессия без запроса разрешений) — называю все шесть:

1. **Закрыта ветка `zahod/etap-20`** (disciplina) + снята рабочая папка `disciplina-wt/wt-etap-20`. Восстановить: `python3 tools/git_zona.py voskresit --branch zahod/etap-20` (вершина `531faa0`).
2. **Закрыта ветка `zahod/gejt-vhozhdeniya`** (disciplina) + снята рабочая папка `disciplina-wt/wt-gejt-vhozhdeniya`. Восстановить: `voskresit --branch zahod/gejt-vhozhdeniya` (вершина `a9137c0`).
3. **Закрыта ветка `zahod/marketplace-github`** (disciplina) + снята рабочая папка `disciplina-wt/wt-market`. Восстановить: `voskresit --branch zahod/marketplace-github` (вершина `3fe6deb`).
4. **Закрыта ветка `zahod/razbor-meta`** (disciplina) + снята рабочая папка `disciplina-wt/razbor-meta`. Восстановить: `voskresit --branch zahod/razbor-meta` (вершина `38eee1a`).
5. **Закрыта ветка `zahod/reshenia-intervyu`** (disciplina) + снята рабочая папка `disciplina-wt/wt-reshenia`. Восстановить: `voskresit --branch zahod/reshenia-intervyu` (вершина `dc718f7`).
6. **Закрыта ветка `zahod/standart-skilla`** (disciplina) + снята рабочая папка `disciplina-wt/wt-standart`. Восстановить: `voskresit --branch zahod/standart-skilla` (вершина `474c2c2`).

Все шесть — команды `git_zona.py zakryt-vetku --branch <имя>`, вызванные мной поштучно (не `--vse-zelenye`) в рамках §2.4. Флаг `--vsyo-ravno` не применялся ни разу; ни один из шести не потребовал его — все прошли по зелёной метрике сразу.

**Время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога)
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*

## ПРИЁМКА — 2026-08-07, аналитик

**ПРИНЯТО.** Гейты 7 из 8; красный Г3 — артефакт лежит на невлитой ветке в рабочей папке, это состояние, а не дефект.

**Два отказа исполнителя признаю правильными, оба ценнее выполнения.**

**§2.1 заблокирован гейтом вхождения.** Исполнитель нашёл прямую цитату владельца («можно отключить, но только после проверки, что они стали частью новых скиллов») и отказался выдавать список как разрешение. Проверил его вывод по живому `GEJT-VHOZHDENIYA.md`: `html-slides-studio` — **14 непокрытых** из 85, `popsci-research` — **9** из 78, и непокрытое лежит кучками, а не размазано (42 Canva-эталона, 23 кадра, контактные листы, фонт-спеки). Гасить их сейчас — терять названное поимённо. Блокировка по существу, не по форме.

**§2.2 не выполнен, и это моя ошибка сборки.** `check_sborki.py` существует только в `zahod/gejt-sborki`, которую я принял утром и не влил. Послал заход пользоваться инструментом, которого в его мире нет. Урок 62: **принятие ≠ доступность**.

**Работа, которая сделана — сделана чисто.**

`KARTA.md` слит без потерь, доказано множествами, а не глазами: база 78, сторона A 99, сторона B 79, пересечение 78, ожидаемое объединение 100, реальный итог **100**. Потеряно 0, лишнего 0. Верификатор проверял дубликаты и сломанные строки во всех четырёх версиях — чисто. Это самое дорогое место захода, и оно закрыто числами.

Подметено 6 папок в `disciplina-wt`, каждая с надгробием. **Флаг `--vsyo-ravno` не применялся ни разу** — четыре отказа названы поимённо, и исполнитель честно сказал, что отказов оказалось четыре, а заход предсказывал три: расхождение с предпосылкой названо, а не подогнано.

Работающие папки целы: `wt-dver` на месте, `wt-sborki`, `wt-dobor`, `wt-evals` и незарегистрированная `linter-zahoda-wt` не тронуты.

**Вынесено дальше:** гейт вхождения для четырёх кандидатов (двое его уже провалили, двое в него не входили вовсе); влитие `zahod/gejt-sborki` в базу, после чего §2.2 станет выполним; проверка «после» отключения.

**Уроки арки: 61 → 62.**
