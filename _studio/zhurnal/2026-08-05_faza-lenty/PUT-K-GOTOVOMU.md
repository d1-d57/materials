# ПУТЬ К ГОТОВОМУ — что сделано, что осталось, что запущено

> **КОГДА читать:** начинаешь сессию по этой арке или спрашиваешь «где мы». · **ЧТО дописывать сюда:** только смену статуса работы, одной строкой, с датой. · **КУДА дальше:** уроки — `UROKI-FABRIKE.md`, дневник — `SESSIYA.md`.

**Дата данных: 2026-08-07.** Числа в таблицах — снимок; живое состояние снимается командами из последнего раздела.

## Цель, сформулированная владельцем

Не «набор абстрактных скиллов», а **маленькая мощная система под конкретный рабочий процесс на конкретной машине**: под каждую задачу ровно один скилл, инциденты не теряются, рычаги накрывают их проверяемо, и всё это можно раз за разом улучшать, возвращаясь с вопросом «что накопилось».

## §1. Сделано

| работа | когда | чем доказано |
|---|---|---|
| Интервью — 29 решений владельца | 07.08 | `disciplina/OTVETY-INTERVYU-2026-08-07.md`, вердикт у каждого пункта |
| Решения внесены в файлы | 07.08 | коммит `dc718f7`, 33 файла |
| Этап 19 — стандарт формы скилла | 07.08 | `disciplina/zhurnal/STANDART-SKILLA.md`, чек-лист 22 пункта, прогон 7 из 7 |
| Маркетплейс переведён на GitHub | 07.08 | коммит `3fe6deb`; живой прогон обновления: правка → пуш → `update` → доехала |
| Гейт З8 в линтере входа | 07.08 | коммит `e174586`; 0 ложных на 161 живом заходе |
| Этап 20 — **половина**: 2 черновика из 7 скиллов | 07.08 | коммит `531faa0`, 38 файлов; закрыто 16 меток `⚠ ДЫРА` из 30 |
| Плагин подключён | 07.08 | `claude plugin details` → 7 скиллов из 7 |

## §2. Осталось — и почему именно это

Порядок внутри таблицы — по стоимости промедления, а не по размеру.

| # | работа | почему это блокирует «готово» | размер |
|---|---|---|---|
| 1 | **Пересчёт карты домов по новой границе** | классификация нарисована 06.08 по фазам, решение 2 раздвоило фазу `06-tekst` (273 записи) 07.08 — и никто не пересчитал. Разбирать дом, границы которого сдвинулись, значит разбирать чужое | средний |
| 2 | **Счётчик «накрыто рычагом»** | дома знают счёт и не знают покрытия. Пока метрики нет, «фаза закрыта» непроверяемо — а это главное, чего владелец хочет | средний |
| 3 | **Влитие — в `bootstrap_zahod.py`** | приёмку ведёт Cowork, а влить из песочницы физически не может (FUSE). Канон предписывает шаг, исполнитель которого его не сделает. Лечение: генератор печатает блок «влей эти ветки» в `§0` каждого захода | малый |
| 4 | **Сбор новых инцидентов, программно** | корпус собран 30.07 из отчётов, заходов и транскриптов. С тех пор неделя работы; одна сессия 07.08 дала 21 урок. Механизма добора нет, всё держится на приёмке вручную | средний |
| 5 | **Гейт вхождения → отключение внешних скиллов** | в системе 56 внешних скиллов, часть дублирует наши по триггерам. «Один скилл на задачу» **не достигнуто**; `session-handoff` до сих пор способен перехватить закрытие сессии | средний |
| 6 | **Этап 20, вторая половина** — 5 готовых скиллов | половина плагина написана по четырём источникам, половина — нет | большой |
| 7 | **Разбор дома МЕТА** (775) | рычаги уже построены (24 инструмента), но не сопоставлены с инцидентами. Работа не «построить», а «предъявить покрытие» | большой |
| 8 | **Разбор дома СЛАЙДЫ** (454 + часть от `06-tekst`) | образец есть: фаза `04.5-intervyu` разобрана позаписно. Остальные четыре — той же процедурой | большой |
| 9 | **Разбор дома РЕСЁРЧ** | тяжелее всех и ждёт работы №1: часть записей уедет в слайды | большой |
| 10 | **Генератор получает имя** (`pip install -e`) | `materials` — про математический контент, не про питон. Пока генератор адресуется местом, вынести его нельзя | малый |
| 11 | **Вход в `GitHub/` и дисциплина курса в `materials/`** | потерялось при разносе: нет файла «с чего начинать» ни на верхнем уровне, ни рядом с курсами | малый |
| 12 | **Этап 21** — ссылки на старый адрес иллюстраций | долг с 06.08 | малый |
| 13 | **Закрытие арки** — уроки в канон | 21 урок с ценой и вердиктом ждут консолидации | средний |

## §3. Что известно про инциденты — и чего не известно

| дом | Σ кратности на 06.08 | разбор | рычаги |
|---|---|---|---|
| ИЛЛЮСТРАЦИИ | 107 | ✅ 31 класс «инцидент → рычаг → цена», включая честное «ничем не чинится» | есть |
| МЕТА | 775 | ❌ 712 записей `вне-фаз` позаписно не читались | **есть, 24 инструмента** — но не сопоставлены |
| СЛАЙДЫ | 454 | 🟡 разобрана одна фаза из пяти (`04.5-intervyu`: 70/63/13) | частично |
| РЕСЁРЧ + ЛЕНТА | 429 + 474 | ❌ | частично |

🔴 **Число `РЕСЁРЧ 903` устарело на одно решение.** Оно складывается как 429 + 474, где 474 = `03-matbaza` 98 + `04-gibrid` 103 + **`06-tekst` 273**. Фаза `06-tekst` раздвоена решением 2: предвёрсточный документ уехал в слайды. После пересчёта дома сравняются, а не будут отличаться вдвое. Это работа №1.

🔴 **«Разобрано» не измеряется ничем.** Ни один дом не знает, какая доля его записей накрыта рычагом. Это работа №2, и без неё ни один разбор нельзя объявить законченным.

## §4. Живое состояние — командами, не памятью

```bash
# счёт по домам
python3 _studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py

# Σ по отдельной фазе (проверить пересчёт границы)
python3 _studio/zhurnal/2026-07-30_dovodka-fabriki/srez_po_faze.py 06-tekst \
  | awk -F'|' '/^\|/{s+=$4} END{print "Σ:", s}'

# уроки арки: три числа обязаны совпасть
grep -c '^### ' _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
grep -c '^ЦЕНА: ' _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
grep -c '^ВЕРДИКТ: ' _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md

# что не влито
python3 _generator/tools/git_zona.py poteri
```


## §5. Решения владельца 2026-08-07, принятые по ходу приёмок

| # | решение | почему и что из него следует |
|---|---|---|
| 1 | **`disable-model-invocation` СНЯТЬ** с `reserch-cikl` и `slajdy` | Пересмотр К-1 с полной ценой. Флаг закрывает скилл **и для явного вызова по имени**: ни заход, ни агент их не видят, доступны только владельцу из чата (проверено прогоном 07.08, урок 35). Защита от автоподхвата остаётся в `description` — так было до 07.08 и работало. Описание в трёх местах (`README.md:35` и оба `SKILL.md`) сегодня ложно и правится независимо |
| 2 | **Корневой `~/Documents/GitHub/README.md` переезжает в плагин целиком** | Содержание — в `disciplina/KAK-USTROEN-GITHUB.md`, наверху остаются три строки «переехало, читай там». Причина: папка не под git, знание в двух местах, и не-версионируемое разъедется первым. Один дом — расходиться нечему |
| 3 | **`README.md` корня `disciplina` — дом регистрации** корневых документов, под это заводится дверь | Он уже фактически ведёт этот список. `register_doc.py` / `korni.py` учатся принимать корень `disciplina`; дописывать руками нельзя, дверь ровно одна |
| 4 | **Отключение старых скиллов — вручную, владельцем, после `etap-20-i-perenos`** | Механизм локально не найден (управляемый плагин с облачными `skillId`), но это не критично: перестройка идёт в один присест. Гасить можно, когда 72 непокрытых закрыты или осознанно оставлены |
| 6 | **`the-interviewer` НЕ гасим — оставляем как есть** | Исход (в) из трёх. Владелец дословно: *«ни в коем случае не надо удалять что-то, что просто удаление чего-то сломает»*. Дыра известна и объявлена: вне конвейера математической лекции просьба «напиши пост / письмо / статью» не встретит никакого интервью, а внутри её ловит G16. Осознанная плата за то, что список скиллов не растёт. Перенести содержание в плагин — **позже и аккуратно**, отдельной работой, не гашением |
| 7 | 🔴 **Решение 3 УТОЧНЕНО: лента обязательна ДО раскадровки, а не на входе** | Владелец дословно: *«в теории сборка слайдов может стартовать вообще без всего, но тогда создание ленты должно быть первым шагом, по сути вызовом скилла ресёрча»*. Это снимает конфликт `evals.json` (сценарий 1 стартует без ленты) и делает решение 3 точнее: скилл `slajdy` **не отказывается** работать без ленты — он **первым шагом зовёт `reserch-cikl` по имени** и получает её. Отказ остаётся только на попытку идти в раскадровку без ленты. Правится `slajdy/SKILL.md §0`: «лента — обязательный вход; отсутствует → шаг 0 есть вызов `reserch-cikl`, а не остановка» |
| 8 | **`frontend-slides` НЕ удалять — удалять нечего** | Скилла нет в установленных вовсе (проверено `ls` по каталогу скиллов 07.08); в наших файлах он живёт только как **источник нормы**: `slajdy/references/animation-patterns.md` помечен «absorbed from `frontend-slides`». Гасить несуществующее не нужно; ссылку как провенанс оставить — она объясняет, откуда правило, и это ценнее чистоты списка |
| 9 | **`karpathy-guidelines` — НЕ зависимость скилла** | Его содержание уже вшито в `_TEMPLATE-zahod.md` разделом «`## 1. ДИСЦИПЛИНА (Карпатов)`», то есть доезжает до исполнителя в КАЖДОМ заходе, независимо от того, установлен внешний скилл или нет. Упоминание в `slajdy/SKILL.md:122` читается как **отсылка к норме**, а не как требование установленного скилла. Объявлять зависимостью — значит поставить работу семи скиллов в зависимость от чужого плагина ради того, что и так дублировано у нас |
| 5 | **Спорная запись дублируется в оба дома** | Провенанс — презумпция, не жёсткое правило: претензия с разбора деки по умолчанию даёт рычаг слайдам, но пересматривается по месту. Дубль живёт в обоих домах со своим рычагом, а в счёте корпуса считается **один раз** и несёт метку — иначе ломается проверка «Σ по домам = 2264» |

---
*Заводится 2026-08-07. Статус работы меняется ЗДЕСЬ и только здесь — иначе состояние живёт в чате и умирает вместе с сессией.*
