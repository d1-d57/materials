# Канал исполнителя — evals (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — здесь не механика, а суждение: «что считается успехом скилла» решается по ходу, из живого материала.

🔴 **ТЫ РАБОТАЕШЬ В РЕПОЗИТОРИИ `disciplina`, НЕ в `materials`.** Инструменты (`git_zona.py`, `register_doc.py`) физически лежат в `materials/_generator/tools/` и наводятся на твой репозиторий переменной `GIT_ZONA_REPO` — форма команд ниже. Голым путём к инструменту не зови: `_generator/` в `disciplina` нет вовсе, команда упадёт «can't open file» (цена — урок 53).

КОНТЕКСТ. `disciplina` — плагин из семи скиллов, извлечённый из фабрики презентаций владельца. Плагин подключён и работает; пять домов инцидентов посчитаны, дом МЕТА разобран позаписно (775 из 775).

**И при этом мы не знаем, работают ли скиллы.** Мы знаем, что они подключены (`claude plugin details` → 7 из 7) и что в них написано. Проверок поведения — `evals/` — **один комплект из семи**: три сценария у `slajdy`, у остальных шести ноль. Стандарт скилла требует минимум три (пункт A-26, гейт C6), и он же честно записал: *«С-4. Оценок нет ни у одного скилла»*.

Разница существенная. Дыра (`⚠ ДЫРА`) — это **известный** пробел, она объявлена и не врёт. Скилл без оценки может молча не подхватываться неделями, и заметит это владелец по тому, что работа идёт как раньше.

ЦЕЛЬ: дать каждому скиллу проверку поведения — и **измерить, срабатывают ли они на живых формулировках владельца** в присутствии 62 чужих скиллов.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **Низкая точность срабатывания — ЗАКОННЫЙ результат этого захода, а не провал.** Ты меряешь, а не добиваешься числа. Померил «4 из 18» — так и пиши; правка `description` под желаемый ответ **запрещена** (§2.4), иначе измерение станет самоисполняющимся.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `~/Documents/GitHub/disciplina-wt/wt-evals`** — worktree захода на ветке `zahod/evals` **репозитория `disciplina`**. 🔴 **Заводишь её САМ, первым ходом:**
  ```
  cd ~/Documents/GitHub/disciplina
  GIT_ZONA_REPO=~/Documents/GitHub/disciplina \
    python3 ~/Documents/GitHub/materials/_generator/tools/git_zona.py worktree add wt-evals --branch zahod/evals
  cd ~/Documents/GitHub/disciplina-wt/wt-evals
  ```
  Форма с `GIT_ZONA_REPO` обязательна и проверена прогоном (урок 53): без неё инструмент возьмёт корнем `materials` и заведёт папку не в том репозитории. Отказал — **СТОП и в `## ОТВЕТ АНАЛИТИКА — вписан по ходу, 2026-08-07 (читать перед шагом 2)

Четыре ответа на твой `## ПЛАН`. Все четыре — решения, а не мнения; работай по ним.

**1. Оспаривание клаузы 2 критерия принято, поправка твоя — верная.** Противоречие моё: §2.1 велит дописать `istochnik` «ко всем, включая существующие», и он же запрещает трогать `slajdy`. Приоритет у запрета. Считаешь **два числа: 18/18 по своей зоне и 18/21 по всем семи**, причина трёх пустых названа. Клауза 2 читается по первому числу.

**2. `skill_name` в `slajdy/evals/evals.json` — ПРАВЬ, это единственное исключение.** Поле говорит `html-slides-studio`, то есть файл в папке нашего скилла представляется чужим внешним скиллом. Оставить заведомо ложное поле в образце формы хуже, чем тронуть образец: следующий сборщик скопирует ложь. Правишь **ровно одно поле, одно слово**, `prompt`/`expected_output`/`files` не трогаешь ни в одном из трёх сценариев. В отчёте — строкой.

**3. Три промпта `slajdy` НЕ правь, но запиши долгом.** Твоя находка серьёзнее опечатки: промпт сценария 1 — дословно то, на что `slajdy` в своём `description` запрещает себе срабатывать. Значит либо описание, либо оценка описывают скилл неверно, и который из двух — решается не здесь. **Оформи пунктом очереди** (`ДОМ: владелец`), а сценарий-«чужой» для `slajdy` в прогон **включи**: если субагент на этот промпт выберет `slajdy`, мы получим измерение вместо спора.

**4. `OTVETY-INTERVYU-2026-08-07.md` как источник — разрешаю, и ты прав, что спросил.** Он в твоём репозитории, содержит дословную речь владельца и свежее корпуса на неделю. Твой довод решающий: корпус собран 30.07, скиллов тогда не существовало — требовать оттуда фраз про скиллы значит требовать сочинения. Таблица §2.1 дополняется этим файлом; **приоритет источников не меняется** — дословная речь бьёт пересказ, и `КОРПУС-VLADELCA.md` остаётся первым там, где фраза есть в обоих.

**Про «молчание как правильный ответ» (твой пункт 2) — согласен и подчёркиваю.** Для `slajdy` и `reserch-cikl` попадание засчитывается, когда субагент их НЕ выбрал. Это делает их сценарии сильнее остальных: они меряют не узнавание, а сдержанность. В таблице прогона такие строки помечай явно, чтобы три итоговых числа не смешали два разных смысла «совпало».

---

## ВОПРОСЫ`**, руками `git worktree add` не делай: страховок `poteri` для `disciplina` нет.
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `skills/*/evals/` (заводишь) и `zhurnal/PROGON-triggerov.md` (заводишь). 🔴 **Сами `SKILL.md` — READ-ONLY**, включая `description`: см. §2.4, это не осторожность, а условие честности замера. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/evals`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Проверить ветку: `git rev-parse --abbrev-ref HEAD` → `zahod/evals`.
- Точка отката не нужна: ты только ЗАВОДИШЬ файлы, ничего существующего не меняешь. Провалилось — удаляется снос ветки.
- **Прочитать ТОЛЬКО это, в таком порядке:**
  1. `skills/slajdy/evals/evals.json` — **единственный существующий образец формы**, три сценария. Форму держи его, поля не изобретай.
  2. `zhurnal/STANDART-SKILLA.md` — пункт **A-26** и гейт **C6** (что стандарт считает оценкой) плюс раздел с записью **С-4**. Целиком файл не читай, он большой: иди грепом по `eval`.
  3. `KOLLIZII-opisanij.md` — уже готовый разбор пересечений `description` наших скиллов с 62 установленными. **Это твой опорный материал для §2.3**, заново коллизии не ищи.
  4. `skills/*/SKILL.md` — только YAML-шапку (`name`, `description`) каждого; тело читай выборочно, когда пишешь сценарий именно про этот скилл.
  Проект дальше не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Три части. Первая даёт скиллам оценки, вторая — измеряет срабатывание, третья запрещает подгонку.

### 2.1 Оценки для шести скиллов — минимум по три

Шесть без оценок: `disciplina-arka` · `disciplina-git` · `disciplina-istochnik-pravdy` · `disciplina-zahody` · `illustracii` · `reserch-cikl`. Седьмой (`slajdy`) уже имеет три — **его не трогай**, он образец формы.

Форма — ровно как в `skills/slajdy/evals/evals.json`, файл `skills/<имя>/evals/evals.json`. Одно поле добавляешь ко всем, включая существующие поля образца:

```json
"istochnik": "<файл>:<строка> — <дословная фраза владельца>"
```

🔴 **Промпт сценария — РЕАЛЬНАЯ формулировка владельца, а не сочинённая тобой.** Это главное требование этой части, и оно проверяемое: у каждого сценария в `istochnik` стоит адрес, по которому фразу можно прочитать глазами.

Где брать живые формулировки — по убыванию ценности:

| источник | где | что там |
|---|---|---|
| транскрипты сессий | `~/Documents/GitHub/materials/_studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-*.md` | дословная речь владельца в работе |
| файлы-заходы арки | `~/Documents/GitHub/materials/_studio/zhurnal/2026-08-05_faza-lenty/kod_*.md`, секции `## ВОПРОСЫ` | как формулируются реальные задачи |
| дома инцидентов | `doma/*/POKRYTIE.md` | что реально ломалось — из чего выходит сценарий-ловушка |

Оба первых пути — **в чужом репозитории и READ-ONLY**: читать можно, писать туда нельзя ничем.

**Три сценария на скилл — не три любых, а три разных по роли:**

1. **прямой** — типичная задача скилла, сформулированная как обычно формулирует владелец (часто коротко и мутно: «сделай», «поправь», «собери»);
2. **граничный** — задача, где скилл обязан **отказать или поднять вопрос**, а не выполнить: негодный вход, отсутствующая предпосылка, конфликт правил. Такой сценарий ловит скилл, который «всегда что-нибудь делает»;
3. **чужой** — задача **соседнего** скилла, сформулированная похоже. Ожидаемый ответ: «этот скилл не срабатывает, работает такой-то». Ловит перехват.

Третий тип обязателен для каждого скилла: у нас семь своих скиллов и `KOLLIZII-opisanij.md` уже нашёл четыре коллизии **внутри своей же группы** — то есть перехват у нас не гипотеза.

Поле `expected_output` пиши **проверяемым**: не «скилл отработает правильно», а что именно должно оказаться на выходе или чем именно он обязан отказать.

### 2.2 🔴 Холодный прогон триггеров — главная часть захода

Оценка в файле говорит, чего мы хотим. Она **не говорит, сработает ли скилл**. Это меряется только так:

1. Собери **18 живых фраз** (по три на каждый из шести скиллов) — те же, что легли в `istochnik`.
2. На каждую фразу подними **свежего субагента с пустым контекстом**. Дай ему ровно три вещи: фразу, список `name` + `description` **всех семи наших скиллов**, и список установленных чужих скиллов с их описаниями (инвентарь готов: `~/Documents/GitHub/materials/_studio/zhurnal/2026-08-05_faza-lenty/INVENTAR-skillov.md`, 62 штуки — **не пересчитывай его**, бери как есть). Вопрос субагенту один: **какой ОДИН скилл ты бы вызвал на эту фразу и почему**.
3. 🔴 **Субагенту нельзя говорить, какой ответ правильный**, нельзя показывать этот заход и нельзя давать контекст проекта. Дал — замер испорчен, и это не чинится ничем, кроме повторного прогона на новых фразах.
4. Не больше **пяти субагентов разом** (§1). Рейт-лимит → отступить и доложить, не слепой ретрай.

Результат — таблица в новом файле `zhurnal/PROGON-triggerov.md`:

| # | фраза (сокр.) | должен | выбран | совпало | если нет — кто перехватил |
|---|---|---|---|---|---|

и под ней **три числа**: сколько попаданий из 18; сколько промахов ушло к **нашему же** скиллу (лечится нашими описаниями); сколько — к **чужому установленному** (лечится отключением, и это готовый список владельцу).

🔴 **Новый `.md` регистрируешь тем же ходом:**
```
GIT_ZONA_REPO=~/Documents/GitHub/disciplina \
  python3 ~/Documents/GitHub/materials/_generator/tools/register_doc.py zhurnal/PROGON-triggerov.md "прогон триггеров: 18 живых фраз против 7 наших и 62 установленных скиллов"
```
Дверь для `disciplina` заведена решением владельца и должна работать. **Отказала — не обходи и не пиши в `README.md` руками:** в `## ВОПРОСЫ`, а файл оставь незарегистрированным и скажи об этом в отчёте.

### 2.3 Что делать с промахами — и чего НЕ делать

Промах разбирается **письменно, в `PROGON-triggerov.md`**, одной строкой на случай: почему выбрали не то (общее слово в двух описаниях? наше описание молчит о том, что фраза называет? чужой скилл описан агрессивнее?).

Опирайся на готовый разбор `KOLLIZII-opisanij.md` — он уже отделил «жёсткие» коллизии (буквальное пересечение фраз) от «смысловых» (разные слова, одно намерение). Заново эту работу не делай.

### 2.4 🔴 ЗАПРЕТ: `description` не править ни одним символом

Соблазн будет прямой: увидел промах — дописал в `description` слово из фразы — прогнал заново — «17 из 18». Это не улучшение, это подгонка под тест, и она **уничтожает единственное честное измерение**, которое у проекта есть.

Правки описаний — отдельная работа, после того как число зафиксировано. Твоё дело — назвать, что именно править, и цену.

Проверка на выходе: `git status --short` в твоей папке не показывает **ни одного** `SKILL.md`.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Шесть условий:

1. **`evals.json` у семи скиллов из семи**, в каждом ≥3 сценария. Командой: `for d in skills/*/; do echo -n "$d "; python3 -c "import json,sys;print(len(json.load(open('$d/evals/evals.json'))['evals']))" 2>/dev/null || echo НЕТ; done` — вывод дословно.
2. **Каждый сценарий несёт `istochnik`** с адресом. Число сценариев и число непустых `istochnik` **обязаны совпасть** — оба привести. Не совпали → сколько сочинено, назвать честно.
3. **Три роли у каждого скилла**: прямой / граничный / чужой — по одному минимум. Скилл, где роли не набрались, назван с причиной.
4. **Живой прогон проведён на 18 фразах из 18** — на реальных формулировках владельца из живых файлов репозитория, не на выдуманной фикстуре. Таблица заполнена целиком, три числа посчитаны. **Само число попаданий на приёмку не влияет** — влияет полнота прогона.
5. **Ни один `SKILL.md` не изменён** — `git status --short` дословно.
6. `PROGON-triggerov.md` заведён и зарегистрирован (или отказ двери назван в `## ВОПРОСЫ`).

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «перехватов не найдено», а «не найдено, проверено X из 18».

⚠ **Условие 4 — то, ради чего заход существует.** Провалились 1–3, но прогон сделан — заход окупился: мы впервые узнаем число. Собраны все файлы, но прогон не сделан — не окупился, сколько бы файлов ни лежало.

## 3. ВЕРИФИКАТОР

Нужен, и в одном месте: **проверить, что промпты не сочинены.** Свежий субагент берёт **случайные 5 сценариев из 18**, открывает файл по адресу `istochnik` и отвечает на один вопрос: стоит ли там эта фраза и о том ли она.

Метод другой, чем у тебя: ты шёл от источника к сценарию, он идёт от сценария к источнику. Именно этот проход ловит «фраза похожа на настоящую, но написана мной» — то, чего самопроверкой не поймать.

Результат — 5 из 5 или список расхождений. Расхождение найдено → **чини сценарий, а не адрес.**

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `skills/`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `skills/`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Рабочая папка заведена первым ходом**, `rc=0`, ветка `zahod/evals` подтверждена.

### Оспаривание критерия готовности — ДО работы (§1 разрешает, §2.1 создаёт коллизию)

🔴 **Клауза 2 критерия невыполнима буквально, и это не придирка.** Она требует: «Число сценариев
и число непустых `istochnik` **обязаны совпасть**». Всего сценариев после захода будет 21 (18 моих
+ 3 у `slajdy`). У трёх сценариев `slajdy` поля `istochnik` нет, а §2.1 прямо запрещает их трогать
(«его не трогай, он образец формы»). То есть выполнить клаузу 2 можно только нарушив §2.1.

**Поправка, по которой работаю:** привожу ДВА числа — 18/18 по моей зоне (шесть скиллов) и 18/21
по всем семи, с явной причиной, откуда взялись три пустых. Ни одного `istochnik` в `slajdy` не
дописываю. Если аналитик считает иначе — это правка захода, а не моя самодеятельность.

### Что уже установлено чтением (влияет на сценарии)

1. **Образец формы `slajdy/evals/evals.json` описывает НЕ тот скилл, в чьей папке лежит.**
   Поле `"skill_name": "html-slides-studio"` — это внешний установленный скилл. Хуже: промпт
   сценария 1 («Сделай презентацию на 5 слайдов…») — дословно то, на что `slajdy/SKILL.md`
   в своём `description` запрещает себе срабатывать («НЕ срабатывает на общие просьбы вида
   „сделай презентацию“»). Форму беру как велено, содержание образца в расчёт не беру.
2. **`slajdy` и `reserch-cikl` — ЧЕРНОВИКИ с добровольным запретом автозапуска.** Оба описания
   говорят: звать ТОЛЬКО по имени, на живую формулировку владельца НЕ срабатывать. Для
   `reserch-cikl` это меняет смысл сценария-«прямого»: правильный ответ на живую ресёрч-фразу
   владельца — **молчание скилла**, и в прогоне такая фраза считается попаданием, только если
   субагент выбрал НЕ `reserch-cikl`. Пишу это в `expected_output` явно, а не подгоняю.

### Источники живых фраз — что реально нашлось

| источник | что взято |
|---|---|
| `materials/.../2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md` | 1379 записей `ЦИТАТА:` с адресом; отфильтровал по доменам: git 6, заходы 38, арка 31, картотека 29, иллюстрации 97, ресёрч 32 |
| `disciplina/OTVETY-INTERVYU-2026-08-07.md` | прямая речь владельца про сами скиллы, свежая (07.08) — то, чего в корпусе быть не может: корпус собран 30.07, скиллов тогда не существовало |
| `materials/.../2026-08-05_faza-lenty/kod_*.md` | формулировки реальных задач |

⚠ **Отклонение от таблицы §2.1, называю сразу:** `OTVETY-INTERVYU-2026-08-07.md` в таблице
источников не назван, но лежит в МОЁМ репозитории и содержит дословные кавычки владельца про
скиллы. Для `disciplina-git` в корпусе всего 6 записей, и ни одна не тянет на три роли — без
этого файла часть сценариев пришлось бы сочинить, что запрещено жёстче.

### Шаги

1. Собрать пул цитат-кандидатов с точными адресами `<файл>:<строка>` (файл + номер строки
   проверяется глазами — это то, что будет гонять верификатор §3).
2. Для каждого из шести скиллов выбрать три фразы под три роли (прямой / граничный / чужой),
   написать `expected_output` проверяемым. `skills/<имя>/evals/evals.json`.
3. Собрать фикстуру для прогона: `name` + `description` семи наших + 62 чужих с описаниями.
   Описания чужих беру из их же `SKILL.md` по путям из `INVENTAR-skillov.md` (сам инвентарь даёт
   имена и пути, но не описания — читаю их скриптом, инвентарь не пересчитываю).
4. Холодный прогон: 18 субагентов, ≤5 разом, каждому — фраза + два списка + один вопрос.
   Ни слова про заход, про правильный ответ и про проект.
5. `zhurnal/PROGON-triggerov.md`: таблица, три числа, разбор промахов со ссылкой на
   `KOLLIZII-opisanij.md`. Регистрация через `register_doc.py`.
6. Верификатор: 5 случайных сценариев из 18 → адрес → «стоит ли там эта фраза».
7. Коммит зоны двумя ходами.

### Заявленные слепые зоны прогона (в отчёт пойдут ими же)

- Субагент несёт **свой собственный реестр скиллов** в системном промпте — в том числе наши семь
  (плагин подключён) и часть 62. Инвентарь я ему даю, но «пустого контекста» в смысле «никакого
  списка скиллов вообще» не существует технически. Замер меряет выбор при ДВУХ источниках
  описаний, совпадающих по содержанию.
- Субагент наследует `CLAUDE.md` рабочей папки. Заход и цель ему не показываются, но полностью
  стерильным контекст назвать нельзя.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **Дверь `register_doc.py` для `disciplina` не работает — два независимых дефекта, оба воспроизведены.** `PROGON-triggerov.md` остался НЕзарегистрированным; обхода не делал, в `README.md` руками не писал.
   **Д1 — дверь не понимает worktree.** `отн_путь()` считает путь относительно `REPO`, то есть ОСНОВНОЙ папки репозитория. Файл в рабочей папке захода лежит по `disciplina-wt/wt-evals/...`, физически вне `disciplina/` → `relative_to()` бросает `ValueError` → «❌ Путь вне репозитория», `rc=2`. Проверено: `отн_путь('/…/disciplina-wt/wt-evals/zhurnal/PROGON-triggerov.md')` → `None`, а `отн_путь('/…/disciplina/zhurnal/PROGON-triggerov.md')` → `'zhurnal/PROGON-triggerov.md'`. Цена класса: контракт зоны ПРЕДПИСЫВАЕТ worktree, и в нём дверь недоступна в принципе — то есть требование «зарегистрируй тем же ходом» для любого worktree-захода неисполнимо штатно. Это ровно тот класс дефекта, ради которого дверь и заводилась (27 обходов `--no-verify`).
   **Д2 — таблица корней не переключается вместе с `GIT_ZONA_REPO`.** `REPO` переключается верно (`korni.REPO` → `/…/disciplina`), а `КОРНИ` — нет: `korni.префиксы_двери()` возвращает `('_studio/', 'obzory/', '_illustracii/', '_meta/', 'disciplina/')`, то есть корни репозитория `materials` плюс префикс `disciplina/`. Внутри репозитория `disciplina` папки `disciplina/` нет (в корне `skills/`, `doma/`, `zhurnal/`, `tools/`, `_studio/`, `arhiv-skillov/`, `zagotovki/`). Проверено: `korni.дверь_принимает('zhurnal/PROGON-triggerov.md')` → `False`, `korni.корень_пути(...)` → `None`. Значит и из основной папки та же команда отказала бы — дверь для `disciplina` принимает сегодня ровно один путь, `README.md` в корне.
   **Почему это урок фабрике, а не мой случай:** заход утверждал «дверь для `disciplina` заведена решением владельца и должна работать». Предпосылка ложна на двух уровнях сразу, и ни один из них не виден до запуска.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: evals#1

2. **Оценка `slajdy` описывает не тот скилл, в чьей папке лежит, — и это не опечатка в одном поле.** Поле `skill_name` я поправил по решению аналитика (п.2). Осталось несводимое: промпт сценария 1 («Сделай презентацию на 5 слайдов… Экспортируй кадры в PNG») — дословно то, на что `slajdy/SKILL.md` в своём `description` запрещает себе срабатывать. Замер это подтвердил количественно — добавочная строка 19 прогона: субагент выбрал `html-slides-studio` и отверг `slajdy` по ДВУМ причинам сразу, обе из самого описания: «явно запрещает срабатывать на общие „сделай презентацию“ **и требует на входе готовых артефактов** (бриф, карточный граф, слой skelet, закрытый гибрид-источник), которых здесь нет». То есть расхождение не в формулировке триггера, а в том, какую работу скилл делает: оценка проверяет сборку дека с нуля, описание — доводку при готовом входе `§0`. Кто из двух врёт — решается не исполнителем. Три промпта не тронуты.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. **Ни одно наше описание не написано в грамматике ВОЗРАЖЕНИЯ — а владелец возражает постоянно.** Две фразы из 18 (№11 «не хотим её вот так вот разрезать, быстро», №17 «почему мы должны выбирать именно Riehl и Фетисова?») не подняли НИ ОДНОГО скилла из 68. Довод субагента дословно: «фраза — не запрос на действие, а возражение… ни один скилл не описывает триггер вида „пользователь защищает картотеку“». Все наши триггер-листы — императивы заказа («сделай», «заведи», «проверь»), и обе фразы провалились мимо не по конкуренции, а по отсутствию носителя. Это класс, а не два случая: граничные сценарии — те, где скилл обязан отказать, — как раз и формулируются владельцем возражением. Правку описаний заход запрещает; называю, что править, и цену: добавление возражательных триггеров усилит коллизии внутри группы А, где слов и так общих много.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. **Слово «источник» — омоним, разведённый по двум скиллам, и разбор коллизий этого не поймал.** У `disciplina-istochnik-pravdy` «источник» = источник ПРАВДЫ (где живёт документ), у `reserch-cikl` «источник» = учебный источник (книга, из которой берут материал). Субагент на фразе №17 отверг первый дословно за это: «про источник правды в репозитории… а не про источник задач, и совпадает лишь словом „источник“». `KOLLIZII-opisanij.md` строит разбор на пересечении триггерных фраз и такую пару не видит: коллизия здесь не в общем триггере, а в общем ТЕРМИНЕ при разных денотатах. Метод разбора это не покрывает — стоит дописать в него класс «омоним».
   ДОМ: `disciplina/KOLLIZII-opisanij.md`
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/wt-evals/zhurnal/PROGON-triggerov.md` — открывать любым просмотрщиком markdown; это главный результат захода. Рядом — шесть новых `evals.json`: `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/wt-evals/skills/{disciplina-arka,disciplina-git,disciplina-istochnik-pravdy,disciplina-zahody,illustracii,reserch-cikl}/evals/evals.json`.

**КОММИТ:** `027fdbe` — «evals: оценки шести скиллам (18 сценариев с адресом источника) + холодный прогон триггеров 18 фраз» · `git_zona.py check --zone skills` → ✅ · `git_zona.py check --zone zhurnal` → ✅ · `git show --stat` — 8 файлов, все мои, чужого нет.

**НЕОБРАТИМОЕ:** необратимого нет. Ни одного файла не удалял, не переименовывал, не перемещал; `git reset`/`checkout` не звал. Единственная правка существующего файла — одно поле `skill_name` в `skills/slajdy/evals/evals.json` (по решению аналитика, п.2), восстанавливается из `git show 027fdbe^:skills/slajdy/evals/evals.json`.

### Что сделал и ЗАЧЕМ

**1. Оценки шести скиллам — 18 сценариев, все с адресом.** По три на скилл, роли различены и видны машинно (в поле `name` префикс `pryamoj-` / `granichnyj-` / `chuzhoj-`), потому что заводить отдельное поле заход запретил («поля не изобретай»). `expected_output` писал не «скилл отработает правильно», а тем, что можно проверить глазами: какой артефакт на выходе, каким пунктом канона он держится и **чем именно скилл обязан провалиться** — у каждого сценария есть явная строка «ПРОВАЛ: …».

**2. Холодный прогон — главная часть.** 18 фраз, 19 запусков (18 + добавочная строка `slajdy`), по пять параллельно, ни одного повтора, рейт-лимитов не было. Результат — `PROGON-triggerov.md`.

**3. Правка `skill_name` в `slajdy`.** Одно слово, `html-slides-studio` → `slajdy`. Промпты, `expected_output` и `files` во всех трёх сценариях не тронуты — их судьба в `## ВОПРОСЫ` п.2.

### Как проверил

**Критерий 1 — `evals.json` у семи из семи.** Команда захода, вывод дословно:
```
skills/disciplina-arka/ 3
skills/disciplina-git/ 3
skills/disciplina-istochnik-pravdy/ 3
skills/disciplina-zahody/ 3
skills/illustracii/ 3
skills/reserch-cikl/ 3
skills/slajdy/ 3
```

**Критерий 2 — сценарии против непустых `istochnik`.** Два числа, по поправке, принятой аналитиком:
**моя зона (шесть скиллов) — 18 сценариев, 18 непустых `istochnik`, сходится**; **все семь — 21 против 18**. Три пустых — существующие сценарии `slajdy`, трогать которые §2.1 запрещает. **Сочинённых промптов среди моих восемнадцати: 0.**

**Критерий 3 — три роли.** Прямой / граничный / чужой — по одному у каждого из шести. Скиллов, где роли не набрались, нет.

**Критерий 4 — прогон.** Проведён на **18 фразах из 18**, все — дословная речь владельца из `KORPUS-VLADELCA.md` с номером строки. Таблица заполнена целиком.

🔴 **Чисел получилось два, и они меряют разное** (по правилу аналитика про ⚑-строки):
- **Счёт A, «дошла ли работа до нужной дисциплины»: 11 из 18.** Промахов 7 — **1 к нашему же** скиллу (лечится нашими описаниями), **4 к чужому установленному** (лечится отключением), **2 ни к кому**.
- **Счёт B, «ведёт ли себя скилл, как написано»: 14 из 18.** Промахов 4 — 1 к нашему, 2 к чужому, 1 ни к кому.

🔴 **1 + 4 ≠ 7 — это не ошибка счёта.** Заход просил два ведра для промахов, прогон дал третий исход, которого в форме не было: дважды субагент отказался вызывать какой бы то ни было скилл. Спрятать эти строки в любое из двух вёдер значило бы соврать — «перехвачено чужим» и «носителя нет вообще» лечатся разным.

**Готовый список владельцу на отключение:** `consolidate-memory` · `html-slides-studio` · `popsci-research` · `skill-creator`. Гасить сейчас нельзя — решение владельца 07.08 п.5 требует сперва гейт вхождения; и два из четырёх подхватывают работу ЧЕРНОВИКОВ, так что после снятия черновикового статуса могут отпасть сами.

**Отрицательные вердикты — с охватом внутри:** перехвата `the-interviewer` (коллизия Б3, самая опасная по `KOLLIZII`) **не найдено, проверено 18 фраз из 18**. `session-handoff` (Б1) трижды назван ближайшим конкурентом и **ни разу не выбран, проверено 18 из 18**. Обе самые дорогие гипотезы разбора коллизий на этом наборе не подтвердились — на другом наборе результат может быть иным.

**Критерий 5 — ни один `SKILL.md` не изменён.** `git --no-optional-locks status --short` до коммита, дословно:
```
 M skills/slajdy/evals/evals.json
?? skills/disciplina-arka/evals/
?? skills/disciplina-git/evals/
?? skills/disciplina-istochnik-pravdy/evals/
?? skills/disciplina-zahody/evals/
?? skills/illustracii/evals/
?? skills/reserch-cikl/evals/
?? zhurnal/PROGON-triggerov.md
```
`SKILL.md` в выводе нет ни одного — ни изменённого, ни нового. Замер честен: описания, по которым субагенты выбирали, те же, что лежали в репозитории до захода.

**Критерий 6 — 🔴 ПРОВАЛЕН ЧАСТИЧНО.** `PROGON-triggerov.md` заведён, но **НЕ зарегистрирован**: дверь отказала, `rc=2`. Обхода не делал, в `README.md` руками не писал. Причина разобрана до кода и вынесена в `## ВОПРОСЫ` п.1 — дефекта два, и оба воспроизведены вызовами `korni`/`register_doc`, а не предположены.

**Верификатор (§3).** Свежий субагент, 5 случайных сценариев из 18, обратный проход «от сценария к источнику»: открыть файл по адресу `istochnik`, найти строку, ответить, та ли это фраза и о том ли она. Результат — **5 из 5 подтверждено**, расхождений нет; сверял двумя способами (глазами по полям записи и посимвольным сравнением скриптом), дополнительно вернул поле `КАСАЕТСЯ:` каждой записи — четыре «процедура», одна «визуал», то есть фразы попали в свой предмет. Чинить нечего.

### Что НЕ трогал

Ни одного `SKILL.md` (включая `description`) · три промпта `slajdy` и их `expected_output`/`files` · `KOLLIZII-opisanij.md` · `INVENTAR-skillov.md` (не пересчитывал — взял как есть) · корпуса в `materials` (только чтение) · ветку не вливал · `git checkout` в основной папке не делал.

### Открытое — куда возвращаться

- **Дверь для `disciplina` нерабочая** (`## ВОПРОСЫ` п.1). Пока не починена, ни один worktree-заход в этом репозитории не может зарегистрировать свой `.md` штатно.
- **Прогон однократный.** Разброс модели на повторе не измерен: 18 фраз — 18 запусков. Повтор на тех же фразах покажет, сколько из 11 устойчивы.
- **Английские клаузы описаний не проверены вообще** — все 18 фраз русские.
- **Набор перекошен в разговорную речь.** Все 18 взяты из `KORPUS-VLADELCA.md`; две другие названные заходом жилы (`## ВОПРОСЫ` файлов-заходов, `doma/*/POKRYTIE.md`) просмотрены, но прямой речи владельца там нет — там формулировки аналитика и счёт инцидентов. Фраз, какими владелец ПИШЕТ задание, в наборе почти нет.
- **`slajdy` в 18 участвовал только как получатель** (строка 15); его собственные сценарии сочинены и в счёт не входят.
- **Инвентарь: 62 записи, 61 уникальное имя** — `math-olympiad` лежит в двух физических копиях кэша плагинов. Не пересчитывал, беру как объявлено самим инвентарём; в прогоне это 61 описание.

### Слепые зоны замера — объявлены, а не умолчаны

1. Субагент несёт **собственный реестр скиллов** в системном промпте, включая наши семь. Контекста «совсем без списка скиллов» не существует технически: замер меряет выбор при двух источниках описаний, совпадающих по содержанию.
2. Субагент наследует `CLAUDE.md` рабочей папки. Заход, цель и правильный ответ ему не показывались, единственный прочитанный им файл — справка из двух списков ВНЕ репозитория; но стерильным контекст назвать нельзя.

**Время прогона и токены** — снимает приёмка из `result`-строки лога; исполнителю счётчик недоступен.

## ПРИЁМКА — 2026-08-07, аналитик

**ПРИНЯТО.** Гейты: 6 из 8. Оба красных — тот же межрепозиторный класс, что и урок 57: `priyomka.py` резолвит хэш и артефакт от `materials`, а работа в `disciplina`.

**Проверено своим прогоном:** коммит `027fdbe` в `disciplina` существует, 8 файлов, чужого нет; артефакт на месте (30 121 байт); `evals.json` у **7 из 7** скиллов, по 3 сценария; непустых `istochnik` — **18 из 18** в своей зоне, у `slajdy` ноль, как и договаривались; `slajdy/evals/evals.json` изменён ровно на одну строку (`2 +-`), то есть только `skill_name`.

**Критерий 6 провален частично и правильно** — дверь отказала, обхода не было, причина разобрана до кода. Это лучший исход из возможных.

### Что мы теперь знаем — впервые числом

**Счёт A (дошла ли работа до нужной дисциплины): 11 из 18. Счёт B (ведёт ли себя скилл, как написано): 14 из 18.**

Два числа вместо одного — правильное решение исполнителя: заход просил два ведра для промахов, а прогон дал третий исход (дважды не поднялся никто). Смешать их значило бы соврать: «перехвачено чужим» и «носителя нет вообще» лечатся разным.

**Твоя гипотеза про `html-slides-studio` подтверждена замером, а не ощущением:** он перехватил `slajdy` дважды из двух возможных случаев — на «канвасе пьяницы» и на добавочной строке 19.

**Список на отключение — готов и обоснован:** `html-slides-studio` · `popsci-research` · `consolidate-memory` · `skill-creator`.

**Две самые дорогие гипотезы разбора коллизий НЕ подтвердились, с охватом:** перехвата `the-interviewer` не найдено, проверено 18 из 18; `session-handoff` трижды назван ближайшим конкурентом и **ни разу не выбран**, 18 из 18. То есть гасить их сейчас не за что.

### Три находки, которых заход не заказывал

**1. Дверь регистрации не работает ни в одном worktree** (урок 59). Дефекта два: путь резолвится от основной папки, а таблица корней не переключается вместе с `GIT_ZONA_REPO`. Следствие тяжёлое: требование «завёл `.md` — зарегистрируй» неисполнимо для любого параллельного захода, а параллельность у нас норма. Это тот самый класс, ради которого дверь и заводилась.

**2. Ни одно наше описание не написано в грамматике возражения** (урок 60). Две фразы из 18 не подняли ни одного скилла из 68 — не по конкуренции, а потому что «пользователь возражает» не описан нигде. А граничные случаи владелец формулирует именно так. Дисциплина молчит там, где нужнее всего.

**3. «Источник» — омоним, и метод разбора коллизий его не видит** (урок 61): он ищет общий триггер, а здесь общий термин при разных денотатах.

**Вопрос 2 остаётся открытым и адресован тебе:** оценка `slajdy` проверяет сборку дека с нуля, описание требует готовых артефактов на входе. Расхождение не в формулировке триггера, а в том, какую работу скилл делает. Кто из двух врёт — решать при доводке скилла на живой лекции, не сейчас.

**Уроки арки: 58 → 61.** Гейты сходятся 61/61/61.
