# Канал исполнителя — karta-incidentov (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — перевод по готовой таблице плюс один разбор; классификация уже сделана, придумывать нечего. Субагенты приветствуются на под-разборе §B.

КОНТЕКСТ. `materials/` — монорепо. Фабрика `_studio/` разделяется на четыре плюс мета-дисциплина; карта — `RAZDELENIE-issledovanie.md` в этой же арке, форма домов — `RESERCH-skill-ili-papka.md` там же. Иллюстрации уже вынесены в `_illustracii/`.

Владелец объяснил, ЗАЧЕМ всё это делается, и от этого зависит твоя работа: *«я собрал кучу инцидентов, я хочу делать рычаги, которые будут их закрывать. Но единым проходом это невозможно — надо работать не со всей фабрикой вместе, а с каждым конкретным инструментом. Как только это будет разнесено, я смогу прийти и сказать: сейчас чиним всё, что связано с иллюстрациями»*.

ЦЕЛЬ: **перевести уже существующую разметку инцидентов по фазам в разметку по пяти будущим домам** — чтобы в каждый дом можно было прийти отдельно и чинить его отдельно.

🔴 **КЛАССИФИКАЦИЯ УЖЕ КУПЛЕНА И ПЕРЕДЕЛКЕ НЕ ПОДЛЕЖИТ** — см. §2, это главное свойство задачи.

🔴 **НИЧЕГО НЕ ПЕРЕНОСИТЬ И НЕ ПРАВИТЬ.** Заход производит скрипт и карту. Существующие корпусы остаются байт в байт.

Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: карта с честным охватом («разложено X из Y») ценнее полной карты с угаданными строками.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/karta`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/karta-incidentov`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add karta --branch zahod/karta-incidentov
  cd ../materials-wt/karta
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-05_faza-lenty/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/karta-incidentov`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_studio/zhurnal/2026-08-05_faza-lenty/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы. Проект сверх них не изучай:
  1. `_studio/zhurnal/2026-08-05_faza-lenty/RAZDELENIE-issledovanie.md` §8, §8-бис — какие дома бывают и по какому признаку задача относится к мета-этажу.
  2. `_studio/zhurnal/2026-07-30_dovodka-fabriki/KARTOTEKA-problem.md` — главный корпус, 1095 строк, пять разделов.
  3. `_studio/zhurnal/2026-08-05_faza-lenty/REESTR-incidentov.md` — 175 строк.
  4. `_studio/zhurnal/_INFRA-git/INCIDENTY.md` и `VERDIKTY.md` — корпус git, УЖЕ разложенный по классам: это образец того, как выглядит дом с разобранными инцидентами.
  5. `_illustracii/POKRYTIE.md` — свежий пример готового дома (107 инцидентов, 31 класс), собран заходом `vynos-illustracii`. **Форму бери отсюда.**
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **ГЛАВНОЕ, ЧТО НАДО ПОНЯТЬ ПРО ЭТУ ЗАДАЧУ: классификация УЖЕ СДЕЛАНА, и переделывать её запрещено.**

Владелец дословно: *«мы делали это много раз, даже буквально сегодня… много часов, чуть ли не пять миллионов токенов, чтобы разложить их по типам и по фазам. Поэтому оно разложено»*. У каждой записи корпуса стоит поле `фаза` — `01-brief`, `06-tekst`, `вне-фаз` и так далее. **Твоя работа — перевести фазу в дом, а не заново решать, к чему запись относится.**

Перечитывать записи и «уточнять» их отнесение — прямой убыток: это выбросит купленную работу и заменит её твоей догадкой.

### A. Перевод — СКРИПТОМ, не руками

Заведи `_studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py`. Он читает живые корпусы, применяет таблицу ниже и печатает: сколько записей в каждом доме, сколько без рычага, сколько спорных.

**Почему скриптом, а не текстом:** число записей в доме — производный факт. Вписанное руками, оно устареет от первой же новой записи (`KONSTITUCIYA §10`, цена — пять расхождений за трое суток). Карта, которую нельзя пересчитать одной командой, через неделю будет врать.

Таблица перевода — из `RAZDELENIE-issledovanie.md §8`, она уже решена владельцем:

| фаза | дом |
|---|---|
| `01-brief`, `02-reserch` | **РЕСЁРЧ** |
| `03-matbaza`, `04-gibrid-istochnik`, `06-tekst` | **ЛЕНТА** |
| `05-raskadrovka`, `07-verstka`, `08-sceny`, `10-sborka-qa` | **СЛАЙДЫ** |
| `09-illustracii` | **ИЛЛЮСТРАЦИИ** — дом уже существует, `_illustracii/POKRYTIE.md` |
| `вне-фаз` | **МЕТА** — и требует под-разбора, см. §B |
| `04.5-intervyu` | **СЛАЙДЫ** — решено владельцем 2026-08-06, см. оговорку ниже |

🔴 **Оговорка по `04.5-intervyu` — читать до разбора, там 107 записей.**

Владелец 2026-08-06: *«это интервью, которое именно вписано в создание слайдов… когда речь не идёт про слайды, мне достаточно поговорить и выстроить логику в голове — я выйду и сам буду рассказывать без бумажки. А для слайдов это принципиально»*.

Доказательство сильнее памяти: единственный живой контракт этой фазы — `teorkat-vvedenie/KONTRAKT-L2.md` — целиком про деку (12 содержательных слайдов, ~34 сцены, бюджет минут на слайд).

⚠ **Но 2026-08-05 тот же владелец сказал иначе**, и это записано в `ARHITEKTURA-fabrik.md`: *«04.5 — обе: своя версия у ленты, своя у презентации»*. Расхождение разрешено в пользу свежего и более конкретного высказывания.

⇒ **Из этого следует ровно одна дополнительная работа.** Разбирая 107 записей `04.5`, отделяй **параметры деки** (сколько слайдов, сцен, минут, габарит, раскладка) — они в СЛАЙДЫ — от **механизма разговора** (как вести интервью, как устроена анкета, как не уйти в сторону) — эти в МЕТА, они нужны каждой фабрике. Не уверен, куда запись — в `## СПОРНЫЕ`, не угадывай.

Снимок на 2026-08-06 (проверь своим прогоном, число — предпосылка): записей `вне-фаз` 233, `02-reserch` 108, `04.5-intervyu` 107, `03-matbaza` 70, `06-tekst` 61, `05-raskadrovka` 48, `01-brief` 36, `07-verstka` 35, `04-gibrid-istochnik` 35, `08-sceny` 34, `09-illustracii` 33.

### B. Единственная НОВАЯ работа — под-разбор «вне-фаз»

233 записи с тегом `вне-фаз` — самый большой класс, и он один на всю мета-дисциплину. Внутри него лежат разные вещи, и дальше их придётся чинить по отдельности. Раздели на четыре под-дома:

| под-дом | что туда |
|---|---|
| **коммиты и git** | зона, хук, обходы `--no-verify`, ветки, worktree, потери |
| **заходы и приёмка** | форма захода, критерий, охват, отчёт, гейты приёмки |
| **дневник и арки** | ведение арки, закрытие, перенос по домам, регистрация документов |
| **гигиена сессии** | контекст, хэндофф, что помнить между сессиями |

Опора, а не догадка: **раздел 4 корпуса — автолог git — уже разложен по классам**, это готовый под-дом «коммиты», сверься с ним. Остальные три размечай по симптому записи.

**Не влезает ни в один — в `## СПОРНЫЕ`.** Пятый под-дом не выдумывай: если таких записей много, это находка, и её место в отчёте.

### C. Карта

`KARTA-INCIDENTOV.md` (зарегистрируй дверью) — короткий документ, а не копия корпуса:

- команда, которой пересчитывается всё (`python3 karta_domov.py`), и её вывод на момент сборки;
- таблица «дом → сколько записей → где корпус лежит физически → что туда поедет при выносе»;
- раздел `## СПОРНЫЕ` с причиной по каждому;
- под-разбор `вне-фаз` из §B.

🔴 **Записи НЕ копируй в карту.** Они остаются в корпусах; карта говорит, куда какой кусок корпуса поедет. Копия корпуса — это второй дом одной сущности (`ISTOCHNIK-PRAVDY §2`), и он разойдётся с первым молча.

### Чего НЕ делать

- Не переклассифицировать записи и не спорить с проставленной фазой. Видишь явную ошибку разметки — **в `## ВОПРОСЫ` строкой**, не правь.
- Не заводить папки домов — это следующий заход.
- Не переносить и не править корпусы: они остаются байт в байт.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. `karta_domov.py` запускается, `rc=0`, печатает числа по домам. **Второй прогон даёт то же самое** — детерминизм.
2. 🔴 **Сумма сходится:** записи по пяти домам + спорные = общему числу записей корпуса. **Расхождение = провал**, не округление. Обе части — вывод команды.
3. 233 записи `вне-фаз` разнесены по четырём под-домам, непристроенных ноль либо они в `## СПОРНЫЕ`.
4. Корпусы не изменены: `git status` показывает ровно два новых файла (`karta_domov.py`, `KARTA-INCIDENTOV.md`) и `_studio/docs/KARTA.md`.
5. 🔴 **ЖИВОЙ ПРОГОН на реальном объекте:** `srez_po_faze.py` и `klassy_otkazov.py` запущены, вывод приложен, числа карты с ними сверены и совпали.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «все разложены», а «разложено X из Y, спорных Z».

## 3. ВЕРИФИКАТОР — проверяет ОХВАТ, а не вкус

Операция не лоссовая: ничего не двигается. Но у неё есть свой способ соврать — **тихо потерять записи**: карта выглядит полной, а треть корпуса в неё не попала, и заметит это владелец через неделю, придя чинить и не найдя своей жалобы.

Свежий субагент с пустым контекстом, метод другой — считать, а не читать:

> *«Вот корпусы инцидентов (пути) и вот карта. Для каждого корпуса посчитай число записей и проверь, что каждая упомянута в карте. Верни список непокрытых с охватом.»*

Список непокрытых обязан быть **пустым** либо каждая непокрытая названа в отчёте с причиной.

Это ровно приём `ISTOCHNIK-PRAVDY §2` — «сводный документ закрывается ОБРАТНОЙ сверкой, а не памятью автора». Там же лежит цена: пул лекции дважды собрали из неполного источника и потеряли то, что владелец считал важным.

**Субагентов можно до пяти** — работа делится по корпусам естественно.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-05_faza-lenty/`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-05_faza-lenty/`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Проверка предпосылок.** Прогнал `srez_po_faze.py` на живой `KARTOTEKA-problem.md`: ИТОГО=2264 (совпадает с оговоркой в шапке корпуса про 2263 vs 2264). Снимок §2 задачи («вне-фаз 233, 02-reserch 108, …») не совпадает с моим прогоном по группам-строкам (вне-фаз 229, 02-reserch 109, 04.5-intervyu 102 и т.д.) — расхождение на 1–7 строк по фазам, и в снимке отсутствует `10-sborka-qa`. Это ожидаемо: задача прямо предписывает «проверь своим прогоном, число — предпосылка», данные растут. Дальше считаю СВОИМ прогоном, расхождение со снимком фиксирую как факт, не подгоняю.

**Единица счёта.** Беру Σ кратности группы (как считает `srez_po_faze.py` и как определён «инцидент» в шапке `KARTOTEKA-problem.md`: 2263 живые записи), а не число строк-групп — это прямо требует критерий готовности п.5 (числа карты обязаны сойтись с выводом `srez_po_faze.py`/`klassy_otkazov.py`), и это same семантика, что «сколько раз это реально произошло».

**Механика перевода фаза→дом:**
- 9 фаз таблицы А переводятся напрямую суммированием (РЕСЁРЧ/ЛЕНТА/СЛАЙДЫ/ИЛЛЮСТРАЦИИ) — чистая арифметика по `srez_po_faze.parse()`.
- `04.5-intervyu` (102 строки-группы, Σ146: Раздел2 26/70, Раздел3 74/74, Раздел5 2/2) требует ПОЗАПИСНОГО разбора на «параметры деки» (→ СЛАЙДЫ) и «механизм разговора» (→ МЕТА) — так велит оговорка §2.
- `вне-фаз` (229 строк-групп, Σ724: Раздел1 58/250 через `KRATNOST.md`, Раздел2 38/208 через `KRATNOST-vladelca.md`, Раздел4 12/107 — уже классифицирован (`RAZBOR-povtorov.md`), весь целиком → под-дом «коммиты и git», Раздел5 121/159 симптом прямо в таблице) — тоже позаписный разбор на 4 под-дома §B.

**Источники симптома для классификации** (нужны для позаписного разбора, не для повторной классификации самой фазы — та уже куплена):
- Раздел1 id `КРТ-NNN` → `## N. <симптом> — M вхожд` в `KRATNOST.md`.
- Раздел2 id `КРВ-NN` → `### N. <симптом> — M вхожд` в `KRATNOST-vladelca.md`.
- Раздел3 (класс И) → девять подклассов `POKRYTIE.md` (`_studio/konvejer/04.5-intervyu/`, ссылка внутри Раздела 3 KARTOTEKA) — если файла с этим именем внутри `04.5-intervyu/` не найду прямым путём, беру симптом из самой строки Раздела 3 картотеки (там есть колонка подкласс/симптом).
- Раздел5 — симптом прямо в первой колонке таблицы.

**Порядок работ:**
1. Извлечь программно (свой скрипт, не руками) для каждой из 331 строки-группы (229 вне-фаз + 102 из 04.5) пару (id, кратность, симптом, адрес) — чисто механическая часть, делаю сам.
2. Классификация по симптому (единственная НОВАЯ содержательная работа) — раздаю СУБАГЕНТАМ (≤5, по корпусам: Раздел1, Раздел2-вне-фаз, Раздел2+3-intervyu, Раздел5). Каждому — чистый список (id, симптом, кратность) без доступа к остальному проекту и точные определения классов из §B/оговорки §2. Неясные — не гадают, помечают как СПОРНО с причиной.
3. Свожу ответы субагентов в `karta_domov.py` (лукап-таблицы id→класс, по образцу `klassy_otkazov.py`), считаю итог.
4. Пишу `KARTA-INCIDENTOV.md`, регистрирую дверью.
5. Верификатор (свежий субагент, §3) — охват по 4 живым корпусам.
6. Живой прогон `srez_po_faze.py`/`klassy_otkazov.py`, сверка чисел.
7. Коммит.

**Оспариваю мелочь в критерии готовности:** снимок §2 «104.5-intervyu 107» и суммарно 800 записей по домам без `10-sborka-qa` — вероятно неполный черновик снимка, не эталон для сверки. Считаю точным собственный прогон, а расхождение называю фактом в отчёте, не подгоняю.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
