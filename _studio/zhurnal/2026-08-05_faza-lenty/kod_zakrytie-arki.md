# Канал исполнителя — zakrytie-arki (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — разнос это суждение: каждая реплика, решение и урок требуют решить, чему они принадлежат, и механически это не делается.

КОНТЕКСТ. Арка `2026-08-05_faza-lenty` работала три дня и произвела больше, чем любая до неё: **семнадцать заходов за сутки 07.08**, все приняты; плагин `disciplina` из семи скиллов подключён и впервые измерен; построены три инструмента, которых не было (`check_sborki.py` — гейт сборки, `dostavit_urok.py` — канал наблюдений, `dvizhki` — генератор с именем); **72 урока с ценой и вердиктом**.

Работа кончилась. Арку надо закрыть по канону — и это не оформление: пока она открыта, всё, что в ней осело, живёт только в ней и умрёт вместе с ней.

🔴 **Первое, что ты обязан знать: баннер закрытия на этой арке УЖЕ СТОИТ, и он лжёт двое суток.** В `PLAN.md`, строка 3: `> 🔒 АРКА ЗАКРЫТА 2026-08-05 (дата данных 2026-08-05)`. После этой отметки в арке прошло 37 заходов и родился 72 урока. Канон предупреждает об этом случае дословно и с ценой (`_meta/disciplina/ARKA.md §7` п.5, прецедент арки «Пути и волны», 05.08): пока баннер стоит, `check_kartoteka.py --gates` **не судит внутренности папки** воротами 4 и 5. То есть сотня файлов арки двое суток не проверялась ничем, и никто этого не заметил.

Значит закрытие здесь — не «поставить баннер», а **снять ложный, довести работу и поставить честный**.

ЦЕЛЬ: закрыть арку так, чтобы ничего из неё не пропало и чтобы возврат к любому скиллу завтра был очевиден.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **Тебе НЕ надо ничего решать за владельца.** В улове 100 пунктов с пометкой «решение за человеком». Твоё дело — довезти их до места, где он их найдёт, а не ответить за него. Ответ за владельца — брак, который потом не отличить от его решения.

🔴 **И не надо доделывать работу арки.** Ты её закрываешь. Нашёл незакрытое — оно едет в `OTKRYTYE-ZADACHI.md` как открытое, а не чинится сейчас.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-05_faza-lenty` `_studio/docs/pochemu-i-videnie/RESHENIYA.md` `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` `_studio/docs/sostoyanie/SVODKI.md` `_studio/docs/spravka/ARHITEKTURA-FABRIKI.md` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-05_faza-lenty/kod_zakrytie-arki.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
  🔴 **Исключение, названное явно: §0.1 велит влить `zahod/kanal-nablyudenij`, и слияние по построению трогает пути вне зоны** — `_generator/tools/{dostavit_urok.py,priyomka.py,fixtures/nablyudenia/}` и `_studio/konvejer/{GEJTY.md,06-tekst/*}`. Ветка принята приёмкой 07.08. Разрешено ровно её влитие и ничего больше: **своей рукой за пределами зоны ты не правишь ни строки**.
  🔴 **`ULOV-OCHEREDI.md` появится в арке ПОСЛЕ этого слияния** — до него его на диске нет, и это не ошибка адреса.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ВЛИТИЕ — ДО ВСЕГО ОСТАЛЬНОГО

```
python3 _generator/tools/git_zona.py merge zahod/kanal-nablyudenij --zone _studio/zhurnal/2026-08-05_faza-lenty --zone _studio/docs/pochemu-i-videnie/RESHENIYA.md --zone _studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md --zone _studio/docs/sostoyanie/SVODKI.md --zone _studio/docs/spravka/ARHITEKTURA-FABRIKI.md --zone _studio/docs/KARTA.md
```

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/kanal-nablyudenij`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- 🔴 Зафиксируй `git rev-parse HEAD` первым ходом и назови в отчёте: заход правит семь живых документов канона, откат только так.
- **Прочитать ТОЛЬКО это, в таком порядке:**
  1. `../../../_meta/disciplina/ARKA.md` — **§7 целиком.** Это твой скрипт, пять шагов и петля проверки. Остальные разделы не нужны.
  2. `VYPISKA-IZ-DIALOGA.md` — **целиком.** Аналитик разобрал транскрипт программно и разнёс по домам: 12 решений, беклог из 8 пунктов, четыре архитектурных узла, шесть своих ложных утверждений, заготовка сводки, чек-лист. **Это не пересказ, а полуфабрикат — бери оттуда, а не изобретай заново.**
  3. `VYGRUZKA-2026-08-07.md` — сырьё: 63 реплики владельца дословно, 14 обещаний аналитика, 62 вопроса.
  4. `ULOV-OCHEREDI.md` — 156 находок очереди, из них 100 «решение за человеком», сгруппированные по темам.
  5. `UROKI-FABRIKE.md` — 72 урока. Целиком, это главный груз.
  6. `SESSIYA.md` — дневник арки, 188 строк.
  Файлы-заходы (`kod_*.md`, их 38) целиком **НЕ читай** — всё, что из них нужно, уже вынуто в улов и уроки.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Пять шагов канона (`ARKA.md §7`) в его порядке. Шестым — баннер, и он последний по построению.

### 2.1 Снять ложный баннер ПЕРВЫМ ходом

`PLAN.md` строка 3. Пока он стоит, гейт не судит папку — а тебе нужно, чтобы судил, иначе ты не увидишь собственных ошибок разноса.

Канон (`ARKA.md §7` п.5): *«продолжил работать в закрытой арке — сними баннер ТЕМ ЖЕ ходом, до первой правки»*. Ты именно продолжаешь.

Проверка фактом: `python3 _generator/tools/check_kartoteka.py --gates` до и после снятия — счёт судимых файлов обязан вырасти. Оба вывода дословно. **Не вырос — скажи, гейт про баннер работает не так, как написано в каноне, и это находка.**

### 2.2 Дневник: разнести 63 реплики владельца

```
python3 _generator/tools/dnevnik.py proverit _studio/zhurnal/2026-08-05_faza-lenty --transcript <путь>
python3 _generator/tools/dnevnik.py zapis  _studio/zhurnal/2026-08-05_faza-lenty --tekst "<суть>" --repliki S680b-В001,S680b-В002
```

🔴 **Флаг `--transcript` обязателен, и вот почему.** `dnevnik.py` без него берёт **последний по mtime** транскрипт, а в твоей сессии это твой собственный: гейт напечатает «покрыто 0 из N» на арке, где разнесено всё. Дефект найден 07.08 и записан пунктом очереди; здесь он тебя укусит первым. Путь бери из шапки `VYGRUZKA-2026-08-07.md` — она сделана из нужного файла.

**Не пересказывай реплики по одной.** 63 реплики дают меньше двадцати сюжетов: одна запись дневника на сюжет, с перечнем номеров. Число записей и число покрытых реплик — оба в отчёт.

### 2.3 Разнести осевшее по домам

Полуфабрикат в `VYPISKA-IZ-DIALOGA.md`, дома — из `ARKA.md §7` п.2.

| что | сколько | дом |
|---|---|---|
| решения владельца | **12** (`VYPISKA §1`) | `../../docs/pochemu-i-videnie/RESHENIYA.md`, формат `## Р<N>` |
| открытые шаги | 5 (`VYPISKA §2а`) | `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md` |
| беклог | **8** (`VYPISKA §2б`) | там же, беклог-раздел |
| архитектурные узлы | 4 (`VYPISKA §3`) | `../../docs/spravka/ARHITEKTURA-FABRIKI.md` |
| «решение за человеком» из улова | **100** | `OTKRYTYE-ZADACHI.md` — **ссылкой на темы `ULOV-OCHEREDI.md`, не переписыванием ста пунктов** |

🔴 **Два решения из двенадцати — пересмотр прежней позиции, а не подтверждение** (`VYPISKA §1` пп. 11–12: разделение репозиториев и вынос генераторов). Если в `RESHENIYA.md` уже есть решение, говорящее обратное, — **не удаляй старое**, а допиши новое с датой и доводом владельца и сошлись со старого на новое. Затёртое решение теряет историю, по которой видно, почему передумали.

⚠ **Помарки владельца durable наравне с решениями** (`ARKA.md §7` п.2, прецедент #14). Правка вкуса, формата, текста — тоже в дневник, а не «мелочь».

### 2.4 🔴 Уроки — 72 штуки, главный груз

Канон п.3: урок едет в дом **того скилла, который использовался**; использовалось несколько — **в несколько домов**.

Инструмент готов и проверен сегодня:
```
python3 _generator/tools/dostavit_urok.py --plan     UROKI-FABRIKE.md
python3 _generator/tools/dostavit_urok.py --dostavit N UROKI-FABRIKE.md
python3 _generator/tools/dostavit_urok.py --gejt     UROKI-FABRIKE.md
```

Замер аналитика 07.08 (пересчитай своим прогоном): недоставленных с живым домом — **43**; остальные упираются в четыре известных класса, и **ни один из них не чинится подстановкой похожего файла**:

| класс | что делать |
|---|---|
| **дома нет на диске** | завести дом или поправить адрес; доставка откажет (`rc=2`), и это верно |
| **дом — исполняемый код** (`.py`) | markdown в конец скрипта даёт `SyntaxError`. **Переносит человек, в докстринг.** Не автоматизируй |
| **дом — папка, а не файл** | назвать файл |
| **слепая зона резолва** | адреса в `disciplina/` инструмент не видит: он пробует две базы и в соседний репозиторий не смотрит (урок 67). **Проверь такие руками**, прежде чем объявить «дома нет» |

⚠ **Урок 60 адресован глобом `skills/*/SKILL.md` — один урок в семь домов.** Инструмент этого не умеет. Разнеси руками или назови отказ; не выбрасывай.

🔴 **После каждой партии доставок проверяй ЧЁТНОСТЬ ```-оград в доме.** 07.08 доставка унесла открывающую ограду без закрывающей, и 22 заголовка вместе с хвостом файла превратились в код — при **нулевых удалениях в `diff`, зелёном `check_uroki.py` и зелёных фикстурах**. Байты целы, файл нечитаем. `diff --numstat` меряет потери, а не целостность (урок 70).

### 2.5 Сводка

Заготовка — `VYPISKA §6`, дописывать **сверху** в `../../docs/sostoyanie/SVODKI.md` по образцу соседних записей.

🔴 **Все числа в ней пересчитай командами перед вставкой.** Заготовка писалась, когда сессия ещё шла: 72 урока мог стать другим числом, заходов было 17 на момент письма. Число, вписанное руками и протухшее, — брак, даже если сегодня оно верно.

### 2.6 Баннер — ПОСЛЕДНИЙ ход, после которого в арке не работают

Форма **дословно**: `> 🔒 АРКА ЗАКРЫТА`, в первых восьми строках `PLAN.md`, с датой данных. По этой строке гейт перестаёт судить внутренности папки — значит она ставится, когда судить больше нечего.

Папку арки **зарегистрировать в `../../docs/KARTA.md §6`** — освобождаются файлы, не сама арка.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Восемь условий, все — командами на живых файлах, не на фикстуре:

1. **Ложный баннер снят, гейт начал судить папку:** `check_kartoteka.py --gates` до и после, счёт судимых файлов вырос. Оба вывода дословно.
2. **Дневник полон:** `dnevnik.py proverit --transcript <путь>` → покрыто N из 63, **N назван**. Не 63 — назови непокрытые поимённо с причиной.
3. **12 решений в `RESHENIYA.md`**, у пересмотренных — ссылка со старого на новое, старое не удалено. Проверка: `grep -c '^## Р'` до и после.
4. **Беклог и открытое на местах:** 8 + 5 записей в `OTKRYTYE-ZADACHI.md`; 100 «решение за человеком» доступны **по одной ссылке**, а не переписаны.
5. **Уроки: `grep -c '^ВЕРДИКТ: '` = `grep -c '^### '`** (петля канона), и `dostavit_urok.py --gejt` показывает **сколько доставлено из скольких**. Не ноль недоставленных — каждый остаток назван классом и причиной.
6. **Ограды чётны во всех домах, куда доставлял** — команда и числа по каждому.
7. **`SVODKI.md` пополнен сверху**, все числа в записи получены командами; команды приведены.
8. **Баннер стоит дословно, в первых восьми строках, с датой данных; папка зарегистрирована в `KARTA.md §6`.**

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

⚠ **Условия 2 и 5 — сердце.** Реплики и уроки это то, что арка произвела; всё остальное — оформление. Провалилось только оформление — арка закрыта плохо, но ничего не потеряно. Провалились 2 или 5 — арка не закрыта, сколько бы баннеров ни стояло.

## 3. ВЕРИФИКАТОР

Нужен, операция лоссовая: разнос **пишет в семь живых документов канона**, и потерянная при переносе запись неотличима от никогда не существовавшей.

Свежий субагент, метод обратный: идёт **от домов к источникам**. Берёт `RESHENIYA.md`, `OTKRYTYE-ZADACHI.md`, `ARHITEKTURA-FABRIKI.md`, `SVODKI.md` и дома уроков **после** работы, и отвечает на три вопроса:

1. **Список потерь ПУСТ?** Каждое решение из `VYPISKA §1`, каждый пункт беклога и каждый урок — либо доехал, либо назван неразнесённым с причиной. Третьего не дано.
2. **Не поехала ли разметка** — `diff --numstat` (удалений ноль) **и** чётность ```-оград, потому что первое второго не ловит.
3. **Читается ли доставленное как часть документа**, а не как вставка чужим голосом.

⚠ Ему **не** давать твой план и этот заход: он сверяет результат с источниками, а не с намерением.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-05_faza-lenty` `_studio/docs/pochemu-i-videnie/RESHENIYA.md` `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` `_studio/docs/sostoyanie/SVODKI.md` `_studio/docs/spravka/ARHITEKTURA-FABRIKI.md` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-05_faza-lenty/kod_zakrytie-arki.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-05_faza-lenty` `_studio/docs/pochemu-i-videnie/RESHENIYA.md` `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` `_studio/docs/sostoyanie/SVODKI.md` `_studio/docs/spravka/ARHITEKTURA-FABRIKI.md` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-05_faza-lenty/kod_zakrytie-arki.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Условие критерия было построено на счётчике, который от проверяемого действия не меняется — то есть покраснеть могло только ложно
ЦЕНА: условие 1 требовало доказать снятие ложного баннера ростом «счёта судимых файлов» в `check_kartoteka.py --gates`. Живой замер: `Судим файлов: 793` до снятия и **793** после — этот счётчик считает граф картотеки, а баннер глушит не его, а ворота 4 и 5, и они печатаются отдельной строкой (`НЕ судим воротами 4 и 5: 22 закрытых арок` → **21**). То есть исполнитель, честно выполнивший условие буквально, обязан был сдать «условие 1 провалено» на полностью верной работе — либо начать искать несуществующую поломку гейта, к чему условие прямо подталкивает («не вырос — гейт про баннер работает не так, как написано в каноне, и это находка»). Стоило одного разбора вывода ворот и сверки с кодом; поймано до отчёта только потому, что рядом стояли два других счётчика, которые изменились. Класс не новый и в этой же арке оплачен пять раз (уроки 11, 42, 50): **число в критерии снято с команды, которую составитель не прогнал**, — но грань новая и хуже прежних: прогнать её было НЕ на чем, потому что до снятия баннера состояние «после» не существует. Единственная защита — назвать в критерии, какая строка вывода обязана измениться, а не «счёт».

### Доставка кладёт запись в КОНЕЦ файла, а не в свою секцию — пятнадцать уроков уехали под чужой заголовок при `rc=0` и чётных оградах
ЦЕНА: `dostavit_urok.py --dostavit N` дописал 15 уроков в конец `OTKRYTYE-ZADACHI.md`. В этом доме к тому времени было ДВЕ секции доставленного, заведённые разными механизмами: `## Уроки арок, доставленные…` (голос источника, консолидирует человек) и `## Пункты очереди заходов, доставленные…` (голос исполнителя, «находка задачей сама не становится»). Последней в файле стояла вторая — и все 15 уроков стали её подразделами, то есть получили чужую инструкцию по консолидации и чужой жанр. Все машинные проверки при этом зелёные: `rc=0` на каждой доставке, `diff --numstat` — 211 добавлений и **0 удалений**, ограды чётны, `--gejt` честно посчитал их доставленными. Ровно то же слепое пятно, что урок 70, но на уровень выше: там ломалась разметка внутри записи, здесь — принадлежность записи разделу. Стоило: обнаружено глазами при чтении дома (не гейтом), починено переносом блока из 84 строк со сверкой «множество непустых строк не изменилось».
ВЕРДИКТ-ФОРМА: доставка обязана искать в доме **свой** заголовок и вставлять под него, а при отсутствии — заводить его, а не падать в конец файла; `EOF` как адрес по умолчанию верен только для дома с одной секцией. Проверяемо тем же способом, что чётность оград: после доставки — `awk`-счёт записей внутри целевой секции против общего счёта в файле, расхождение красное.

### Проверка чётности оград зелёная на девяти документах, которых не существует: `grep -c` на пропавшем файле отдаёт 0, а ноль — чётное число
ЦЕНА: свой же прогон, шаг 6 критерия. Рабочая папка shell осталась внутри папки арки после предыдущей команды, относительные пути девяти домов перестали разрешаться, `grep -c '^```'` вернул `0` по каждому — и таблица напечатала **девять строк «ЧЁТНО»** подряд. Формально верно (0 чётно), фактически не проверено ничего. Спасло только то, что `ugrep` печатал предупреждение `No such file or directory` в stderr рядом с ответом; при чистом `grep` или при подавленном stderr вердикт «ограды чётны во всех домах» ушёл бы в отчёт как доказанный, а именно этот шаг заведён уроком 70 для ловли разметки, которую не видит `diff --numstat`. Это ровно тот же скелет, что урок 5 («код возврата прочитан от `tail`»), но на данных, а не на коде возврата: **проверка, у которой «нечего проверять» и «всё в порядке» дают один вывод, не является проверкой.**

🔴 **Второй случай того же класса в этом же заходе, через полчаса после первого.** Сторож, ждавший верификатора, мерил размер его вывода через `stat -f%z <путь>`. Путь оказался **симлинком** на транскрипт субагента, `stat` по умолчанию не следует по ссылке и отдавал длину самой ссылки — постоянные `133` байта. Сторож немедленно решил, что файл перестал расти, и отрапортовал «верификатор затих», когда тот писал 400 КБ и продолжал работать. Прими я этот рапорт — отчёт ушёл бы с результатом верификатора, которого не было. Поймано сверкой с прошлым замером (398 221 против 133), то есть случайно: рапорт сам по себе выглядел нормально. `os.path.getsize` на том же пути даёт 419 960 — разница только в том, следует ли инструмент по ссылке.

Дешёвая форма на оба случая одна: **у измеряющей команды обязан быть отдельный ответ «мерить нечего», отличимый от нуля и от штиля.** Практически — `test -f` (не `test -e`) перед счётом с явным красным; для размеров — `stat -L`/`os.path.getsize`, а не `stat`; и то же самое обязано быть внутри `check_uroki.py`, где чётность оград будут проверять инструментом.

## ПЛАН — (заполняет исполнитель)

**Точка отката: `eb8dd724cdcf7dad798087ac96c6f367bf19d983`.** Ветка `arka/mat-kostyak` подтверждена (`git branch --show-current`, rc=0).

### §0.1 исполнен, но не одной командой — три отказа подряд, все законные

1. Команда §0.1 дословно → `⛔ Слияние выходит за зоны` (rc=1). Ветка трогает ровно те шесть путей, которые контракт зоны назвал явным исключением (`dostavit_urok.py`, `priyomka.py`, `fixtures/nablyudenia/`, `konvejer/GEJTY.md`, `konvejer/06-tekst/*`) — но `--zone` инструмента про исключения в прозе не знает (это в точности урок 72). Пересобрал строку, добавив шесть точечных префиксов вместо предложенных инструментом широких `--zone _generator --zone _studio`: разрешено ровно названное и ничего сверх.
2. Второй прогон → `⛔ 4 путей ОДНОВРЕМЕННО грязные`. Это несохранённая работа предыдущей сессии в моей же зоне (уроки 67–72, запись ПРИЁМКИ в `kod_svesti-i-vstroit.md`, две строки регистрации в `KARTA.md`), только добавления, `diff --numstat` → `6/12/30/33 +, 0 −`. Закоммитил отдельным ходом → `3da7f02`.
3. Третий прогон → конфликт в `KARTA.md` и `OTKRYTYE-ZADACHI.md`. Обе стороны — чистые добавления в конец раздела, разрешил объединением (ни одна строка не потеряна), `merge --continue` → **`d83c2c6`**. Факт на диске: `git merge-base --is-ancestor zahod/kanal-nablyudenij HEAD` → rc=0; `ULOV-OCHEREDI.md` на диске, 263 строки.

### 🔴 Три расхождения КРИТЕРИЯ с живыми файлами — называю ДО работы, как велит §1

**Р-1. Условие 1 меряет не тот счётчик, а его команда не запускается как написана.**
`python3 _generator/tools/check_kartoteka.py --gates` без позиционного пути печатает докстринг и отдаёт rc=1 (`main()` требует `len(args) == 1`, строка 691). Рабочая форма — `check_kartoteka.py teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md --gates`.
Дальше существеннее: **«счёт судимых файлов» не вырос и вырасти не мог** — `Судим файлов: 793` до и 793 после, потому что это счётчик графа картотеки, а баннер глушит не его, а ворота 4 и 5. Вырос другой счёт, и он однозначен:

| | до снятия | после |
|---|---|---|
| `НЕ судим воротами 4 и 5` | **22** закрытых арок | **21** |
| ворота 4 (ссылки на секции рамы) | НЕТ ✅ | **10** ❌ — все в нашей папке |
| `ИТОГ ВОРОТ` нарушений | 8 | **18** |

Условие 1 считаю выполненным по этим числам, а формулировку «счёт судимых файлов» — ошибочной. Оба вывода дословно в отчёте. Все 10 сработавших ворот 4 — литералы-заглушки (`SPEKA §X`, `§ЗаведомоНетТакойСекции`) в примерах внутри `kod_shvy-lenty-reserch.md` и `SHVY-lenta-reserch.md`, то есть та же слепая зона «лексика не отличает утверждение от пояснения о нём», что в уроках 57 и 66. Новых сирот у арки ноль (все 7 из ворот 5 — чужой долг арок `2026-07-28_konspekt-l1` и `_SHTAB-teorkat-vvedenie`, к заходу отношения не имеют).

**Р-2. «63 реплики» протухли: живой транскрипт даёт 70.**
Транскрипт из шапки `VYGRUZKA` лежит по пути песочницы аналитика (`/sessions/epic-zen-cori/mnt/...`) и на этой машине не разрешается; настоящий файл найден по имени: `~/Library/Application Support/Claude/local-agent-mode-sessions/.../680be664-….jsonl`. `dnevnik.py proverit --transcript <он>` → **«покрыто 0 из 70»**, не из 63: сессия шла дальше после выгрузки (реплики В064–В070 — приёмки и решение «давай наконец закрывать арку»). Отчитываюсь «N из 70».

Отдельно: **у одних и тех же реплик три разных нумерации, и `VYPISKA §0` неверно утверждает, что две из них совпадают** («сопоставление с идентификаторами `S680b-В0NN` штатной выгрузки — по порядку, они совпадают»). Живая сверка по содержанию: решение «не удалять то, что удаление сломает» — `VYPISKA В032` = `VYGRUZKA В039`; «не дробить» — `В023` = `В032`; «слайды выведены» — `В001` = `В002`; а «порядок: хвосты → арка → слайды» — `В062` = `В062`. Смещение непостоянное, то есть механически пересчитать нельзя. **Каждое из 12 решений локализую в `VYGRUZKA` по содержанию, а не по номеру**, и в дневник ставлю только проверенные `S680b-*`.

**Р-3. Условие 5 и §2.4 не выражают самый крупный класс остатка — «дом вне зоны захода».**
Живой прогон `--plan` (не число из шапки): заголовков `### ` — 72, из них **1 — образец формы внутри ```-ограды**, настоящих уроков **71**; адрес разобран 71 из 71; **не доставлено 70** (доставлен один — урок 7). Шапка захода называла «43 недоставленных с живым домом» — совпадает лишь с числом «база: арка» в выводе инструмента, но принадлежность к ЗОНЕ это число не учитывает. Разбивка остатка по домам:

| класс | сколько | что делаю |
|---|---|---|
| дом **в моей зоне** | **16** (15 → `OTKRYTYE-ZADACHI.md`, 1 → `DISCIPLINA-teksta.md`) | доставляю |
| дом — **`.py`** (17 в `_generator/tools/` + `karta_domov.py`) | **18** | не автоматизирую — §2.4 сам это запрещает, переносит человек в докстринг |
| дом в репозитории **`disciplina`** | **7** (31, 32, 35, 53, 60, 61, 65) | вне репозитория; проверил руками — **все четыре файла существуют**, урок 67 подтверждён на живых файлах |
| дом в `materials`, но **ВНЕ моей зоны** | **29** (23 → `_meta/disciplina/RUKOVODSTVO-zahodami.md`, 3 → `KONSTITUCIYA.md`, 1 → `STANDART-oformlenia.md`, 1 → `konvejer/06-tekst/UROKI-FABRIKE.md`, 1 → `2026-07-30_dovodka-fabriki/KRATNOST-vladelca.md`) | **не трогаю** — контракт зоны запрещает дословно; называю классом |

Четыре класса из таблицы §2.4 не покрывают ни последний, ни самый крупный. Условие 5 при этом сформулировано так, что ненулевой остаток законен («каждый остаток назван классом и причиной»), — по нему и отчитываюсь. Поправку к заходу предлагаю ту же, что вердикт урока 72: у сводящего/закрывающего захода зона должна уметь называть **дома доставки** отдельной строкой, иначе «разнеси 71 урок по домам» и «за пределами зоны не правь ни строки» невыполнимы разом — шестой случай этого класса за арку (уроки 12, 14, 18, 22, 25, 34).

**Петля канона уже сходится:** `grep -c '^### '` = 72 = `grep -c '^ВЕРДИКТ: '` = 72 (`ЦЕНА: ` = 75, три лишних — образец формы и цитаты).

### Порядок ходов

1. ✅ снят ложный баннер, ворота замерены до/после
2. дневник: ~20 записей на 70 реплик через `dnevnik.py zapis --repliki`, повторный `proverit`
3. 12 решений → `RESHENIYA.md` (Р63…Р74); пп. 11–12 — пересмотр, старое Р59 не трогаю, ссылаюсь с него на новое
4. 5 открытых + 8 беклога + ссылка на 100 «решение за человеком» → `OTKRYTYE-ZADACHI.md`
5. 4 узла → `ARHITEKTURA-FABRIKI.md`
6. 16 уроков → доставка партиями, после каждой — чётность ```-оград (урок 70)
7. сводка → `SVODKI.md` сверху, все числа командами
8. верификатор от домов к источникам
9. баннер + регистрация папки в `KARTA.md §6`
10. коммит зоны двумя ходами

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **`check_kartoteka.py --gates` не запускается в форме, в которой его пишут заходы: нужен позиционный путь к карте.** Без него `main()` (строка 691) уходит в `sys.exit(__doc__)` — печатает докстринг и отдаёт `rc=1`, что неотличимо от «ворота прогнались и нашли нарушение». Рабочая форма: `python3 _generator/tools/check_kartoteka.py teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md --gates`. Инструмент стоит первым условием критерия у закрывающих заходов, то есть форма разойдётся снова.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

2. **Ворота 4 дают 10 срабатываний на литералах-заглушках внутри примеров** (`SPEKA §X`, `DUH §Y`, `KOTYOL §Z`, `SPEKA §ЗаведомоНетТакойСекции` в `kod_shvy-lenty-reserch.md` и `SHVY-lenta-reserch.md`). Это не битые ссылки, а нарочно-несуществующие секции в тексте, объясняющем проверку. Тот же класс, что уроки 37, 57 и 66: лексика не отличает утверждение от пояснения о нём, и первым на проверку натыкается документ, который её описывает. У ворот 4 нет исключения для backtick-цитат, хотя у З9 такое уже сделано (`_v_backtike()`).
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

3. **У одних и тех же реплик владельца три несовпадающие нумерации, и `VYPISKA §0` утверждает обратное.** Разбор аналитика насчитал 57 реплик, штатная выгрузка — 63, живой `dnevnik.py` на том же транскрипте — 70. `VYPISKA §0` пишет: «сопоставление с идентификаторами `S680b-В0NN` штатной выгрузки — по порядку, они совпадают». Сверка по содержанию: `VYPISKA В032` = `VYGRUZKA В039`, `В023` = `В032`, `В001` = `В002`, но `В062` = `В062`. Смещение непостоянное — механически пересчитать нельзя.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

4. **Регистрация ПАПКИ арки в `KARTA.md §6`, которую предписывает `ARKA.md §7` п.5, невыполнима и не является живой конвенцией.** `register_doc.py` на каталоге отказывает («Файла нет на диске»), а ни у одной из закрытых арок строки вида `` `_studio/zhurnal/<арка>/` `` в `KARTA.md` нет — регистрируются файлы поштучно. Суть требования (чтобы освобождение папки от ворот не оставило сирот) выполняется другим: все 64 `.md` арки зарегистрированы, проверено грепом. Формулировку канона стоит привести к тому, что реально проверяется.
   ДОМ: `_meta/disciplina/ARKA.md`
   ДОСТАВЛЕНО: нет

5. **Владельцу: 54 урока из 71 остались недоставленными, и 29 из них ждут одного вашего решения** — расширить ли зону закрывающего захода на живые документы канона (`_meta/disciplina/RUKOVODSTVO-zahodami.md`, `KONSTITUCIYA.md`, `STANDART-oformlenia.md`), или эти уроки консолидирует человек. Сейчас они полностью описаны и никуда не денутся (`OTKRYTYE-ZADACHI.md`, раздел арки), но в канон не попали. Ещё 18 упираются в `.py`-дома (нужен человек, в докстринг) и 7 — в репозиторий `disciplina` (нужна починка резолвера, урок 67).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

Арка закрыта. Ниже — что сделано, чем проверено и что осталось честно открытым.

### Что сделал и ЗАЧЕМ

**Влитие (§0.1).** Ветка `zahod/kanal-nablyudenij` влита — `d83c2c6`. Прошло с третьей попытки, все три отказа законные и описаны в `## ПЛАН`: зона (пересобрал точечными префиксами вместо широких `_generator`/`_studio`, которые предлагал инструмент), грязные пути (закоммитил отдельным ходом `3da7f02` — это была несохранённая работа предыдущей сессии, уроки 67–72 и запись приёмки, только добавления), конфликт в двух append-документах (разрешил объединением, ни одна строка не потеряна).

**Снял ложный баннер и вернул папку под ворота.** Это делалось первым ходом ради собственных ошибок разноса: пока баннер стоял, ворота 4 и 5 арку не судили, и я бы не увидел, что натворил. Замеры дословно — в `## ПЛАН` (условие 1 меряло не тот счётчик, разобрано там же).

**Дневник — 24 записи на 70 реплик.** Не пересказ по одной: 70 реплик дали 24 сюжета, у каждой записи перечень номеров. Реплику `S680b-В048` разнёс с явной оговоркой — это автосводка компактификации контекста, а не слово владельца.

**12 решений → Р63–Р74.** Два из них (Р73 разделение репозиториев, Р74 вынос генераторов) — пересмотр прежней позиции; старое Р59 не тронуто, в его конец дописана строка со ссылкой на новые. Р74 отдельно называет, что владелец утром того же дня принял противоположное («Да, остаётся»), и почему обе редакции оставлены.

**Открытое, беклог, архитектура, сотня к владельцу.** 5 шагов + 8 пунктов беклога + 4 узла разнесены. Сто находок `ДОМ: владелец` **не переписаны** — в борде одна ссылка на `ULOV-OCHEREDI.md §5`, где они уже сгруппированы в 11 тем с суммой ровно 100, плюс два вывода разбора, которые дешевле любого разговора (почти половина — вовсе не вопросы владельцу; больше половины класса Б закрывается заведением пяти отсутствующих файлов).

**Уроки — доставил 16, назвал 54.** Своим прогоном, а не по числу из шапки: настоящих уроков 71 (72-й `### ` — образец формы внутри ограды), доставлено суммарно 17 из 71. Остаток разнесён по четырём классам; разбивка и причины — в `## ПЛАН` и в двух домах.

**Сводка.** Дописана сверху, каждое число снято командой, и сами команды приведены в записи блоком «Как пересчитать» — чтобы следующий читатель проверял файл, а не верил ему.

**Баннер — последним ходом,** после верификатора и после починки всего, что он нашёл.

### Как проверил

| условие | чем и с каким результатом |
|---|---|
| 1 | `check_kartoteka.py … --gates` до/после: закрытых арок **22 → 21**, ворота 4 **0 → 10**, итог **8 → 18**. После финального баннера — обратно **22** и **8**. Счётчик «судим файлов» (793) к баннеру отношения не имеет — разобрано в `## ПЛАН` |
| 2 | `dnevnik.py proverit --transcript <живой>` → **rc=0**, покрыто **70 из 70**. Не 63: выгрузка сделана до конца сессии |
| 3 | `grep -c '^## Р'` → было **62**, стало **74**. Дублей номеров нет, нумерация непрерывна, Р59 на месте целиком |
| 4 | 5 открытых + 8 беклога в борде; 100 «за человеком» — **одной ссылкой** |
| 5 | `grep -c '^### '` = **72** = `grep -c '^ВЕРДИКТ: '` — петля канона сходится. `--gejt` → **rc=1, «не доставлено 54 из 71»**, красный объявленно: те же 54 расписаны классами в двух домах |
| 6 | Ограды чётны во всех домах, куда доставлял: `SVODKI` 2 · `OTKRYTYE-ZADACHI` 2 · `RESHENIYA` 0 · `ARHITEKTURA-FABRIKI` 0 · `DISCIPLINA-teksta` 0. Существование каждого файла проверено `test -f` перед счётом — см. урок фабрике ниже, ровно здесь я один раз получил ложную зелень |
| 7 | Все числа сводки сняты командами, команды вклеены в саму запись |
| 8 | Баннер `> 🔒 АРКА ЗАКРЫТА 2026-08-07` — строка 3 `PLAN.md`, с датой данных; ворота подтвердили (21 → 22). **Папку в `KARTA.md §6` зарегистрировать нельзя** — дверь отказывает на каталоге, и такой конвенции нет ни у одной закрытой арки; суть требования выполнена иначе: все **64** `.md` арки зарегистрированы, проверено грепом |

Удаления против точки отката — **0 во всех домах**; единственные 2 удаления во всём заходе это замена строки баннера в `PLAN.md`. Маркеров конфликта не осталось нигде.

### Что НЕ трогал

- **Ничего за пределами зоны своей рукой.** 29 уроков, чей дом — живой документ канона вне зоны (`_meta/disciplina/RUKOVODSTVO-zahodami.md`, `KONSTITUCIYA.md`, `STANDART-oformlenia.md`, `konvejer/06-tekst/UROKI-FABRIKE.md`, `KRATNOST-vladelca.md`), не доставлены. 18 уроков с `.py`-домом не доставлены — §2.4 сам это запрещает. 7 уроков с домом в репозитории `disciplina` не доставлены — другой репозиторий.
- **Параллельная работа владельца.** В дереве были 26 грязных путей вне слияния (`_fond/zadachi/bank/*`, `ucheniki/*`, `obzory/*`, `_generator/build_listok.py`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`). Не тронуты, в коммит не взяты.
- **Ветки не удалял** ни одной, как велит §0.1.
- **Решений за владельца не принимал.** Сто пунктов «решение за человеком» доставлены до места, где он их найдёт, и ни на один я не ответил.
- **Работу арки не доделывал.** Найденное незакрытое уехало в `OTKRYTYE-ZADACHI.md` как открытое.

### Результат верификатора

Свежий субагент, метод обратный (от домов к источникам), моего плана и текста захода не получал.

**Вопрос 1 — список потерь: ПУСТ.** Поштучно: 12 решений (адрес каждого назван), 5 открытых шагов, 8 пунктов беклога, 4 узла, сводка, все 71 урок — «🔴ПОТЕРЯН ни одной строки». Его независимый пересчёт остатка сошёлся с моим до единицы: 18 + 7 + 29 = 54, 17 + 54 = 71. Отдельно проверил оба пересмотра: Р59 не тронута, ссылка со старого на новое стоит.

**Вопрос 2 — разметка: ЧИСТО.** Удалений 0 во всех пяти домах, ограды чётны, маркеров конфликта нет. Проверил и соседей вне списка — тоже чисто.

**Вопрос 3 — читается ли как часть документа: НАЙДЕНО ПЯТЬ. Все починены:**

1. **Четыре битых внутридокументных якоря в `RESHENIYA.md` из пяти** — в slug не попал хвост `(владелец, 2026-08-07)`. Среди битых была та самая forward-ссылка Р59 → Р73/Р74, ради которой пересмотр и оформляли. Починил скриптом, который считает slug **из живых заголовков**, а не набирает его руками: 3 исправлено, 1 был верен, контрольный прогон — битых 0 из 5.
2. **Битый относительный путь** `../../../disciplina/…` в борде — нужно четыре уровня, а не три (при переносе из папки арки в дом другой глубины уровень потерялся; класс урока 63). Починил, проверив оба варианта `test -f`; затем разрешил **все** относительные пути нового раздела на диске — битых 0 из 2.
3. **«32 коммита» против 59** — я снял это число командой с ограниченным диапазоном (`eb8dd72~40..HEAD`), а в сводку вклеил команду без диапазона. То есть число и его же команда давали разное — ровно то, что канон запрещает. Исправил на **59**, значение команды из блока «Как пересчитать».
4. **«37 заходов» в борде против 39 в сводке** — два дома называли арке разный размер. 37 это заходы *после ложного баннера*. Переписал: 39 с командой рядом и оговоркой про 37.
5. **Мета-наблюдения `VYPISKA §5` не доехали никуда, и никто этого не сказал** — моё упущение, верификатор нашёл его сверх заданных вопросов. Три свойства корпуса уроков, которые меняют способ их консолидации (перекос в сторону приёмки — у исполнителей 4 из 66; уроки 55–72 не проверялись вторым проходом; урок 6 без разбираемого адреса — починен). Добавил в борд отдельной врезкой «Что знать про сам корпус этих уроков ПЕРЕД консолидацией».

Верификатор отдельно подтвердил то, что я снял молча: сквозная нумерация реплик в `VYPISKA` с выгрузкой **не совпадает**, хотя §0 выписки утверждает обратное; девять адресов, проставленных в домах, он сверил дословными цитатами — все верны.

Одно его наблюдение оставляю как есть, починки не требует: доставленный урок в `DISCIPLINA-teksta.md` сел после подписи файла (отбит `---`, читается терпимо) — это следствие того же дефекта доставки «кладу в конец файла», который записан уроком фабрике ниже.

### Открытое — куда возвращаться

**Первое и главное — 54 недоставленных урока.** Они не потеряны и полностью описаны, но в канон не попали. 29 из них ждут одного вашего решения: расширять ли зону закрывающего захода на живые документы канона, или эти уроки консолидирует человек (пункт 5 в `## ВОПРОСЫ`). 18 требуют человека в докстринг `.py`. 7 разблокируются починкой резолвера (урок 67).

**Дальше по вашему же порядку (Р72):** скилл `slajdy` — большое интервью по реорганизации; вход готов — `NEPOKRYTOE.md` (16) и `DOLG.md` (4).

**Время прогона и токены** — снимает приёмка из `result`-строки лога; исполнителю счётчик недоступен.

**АРТЕФАКТ:** артефакта нет: заход текстово-разносящий, собранного HTML/PDF/картинок не производит. Смотреть надо не файл, а результат разноса — вот абсолютные пути, по порядку чтения:
`/Users/ivanyakovlev/Documents/GitHub/materials/_studio/docs/sostoyanie/SVODKI.md` — сводка арки, сверху (что было, что измерено, чего закрытие не смогло)
`/Users/ivanyakovlev/Documents/GitHub/materials/_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` — раздел «🔴 ОТКРЫТОЕ ИЗ АРКИ «2026-08-05_faza-lenty»»: 5 шагов, 8 беклога, ссылка на сотню к вам, долги инструмента
`/Users/ivanyakovlev/Documents/GitHub/materials/_studio/docs/pochemu-i-videnie/RESHENIYA.md` — решения Р63–Р74
`/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-05_faza-lenty/ULOV-OCHEREDI.md` — §5, сто находок в 11 темах
Открывать любым просмотрщиком markdown.

### 🔴 НЕОБРАТИМОЕ — что произошло, чего вы не видели

1. **Слияние `zahod/kanal-nablyudenij` в `arka/mat-kostyak`** · merge-коммит `d83c2c6`, 5 коммитов, 30 путей · **восстанавливается:** `git reset --hard eb8dd724cdcf7dad798087ac96c6f367bf19d983`. Было предписано §0.1.
2. **Коммит `3da7f02`** — зафиксировал 4 грязных файла зоны и 3 неотслеженных, всё только добавлениями (988 строк, 0 удалений) · **восстанавливается:** тем же `reset` на `eb8dd724`. Не было предписано: слияние отказывалось идти поверх грязных путей, альтернатива — откатить чужую несохранённую работу, чего я делать не стал.
3. **Замена строки баннера в `PLAN.md`** — единственные 2 удаления за весь заход (снял ложный `🔒 АРКА ЗАКРЫТА 2026-08-05`, в конце поставил честный `2026-08-07`) · **восстанавливается:** `git show eb8dd724:_studio/zhurnal/2026-08-05_faza-lenty/PLAN.md`.
4. **Перенос блока из 84 строк внутри `OTKRYTYE-ZADACHI.md`** — 15 доставленных уроков сидели под чужим заголовком, переставил под свой · **содержание не менялось:** скрипт проверял инвариант «множество непустых строк не изменилось» (540 = 540) и отказался бы записывать при расхождении · **восстанавливается:** из git.
5. **Перезапись трёх якорей в `RESHENIYA.md`** — только внутри `](#…)`, текст не тронут · **восстанавливается:** из git.

**Ничего не удалено, не переименовано и не перемещено. Ни одной ветки не удалено. За пределы зоны своей рукой не вышел ни разу** — единственное касание чужих путей это слияние, разрешённое контрактом явным исключением.

**КОММИТ:** `773ea8e` — «zakrytie-arki: закрыть арку faza-lenty по канону — 70 реплик в дневник, 12 решений (Р63-Р74), открытое/беклог/архитектура по домам, 17 уроков доставлено и 54 названы классами, сводка, баннер» · 10 файлов, 1049 вставок, 6 удалений · `git_zona.py check --zone` → ✅ по всем шести путям зоны (`_studio/zhurnal/2026-08-05_faza-lenty`, `RESHENIYA.md`, `OTKRYTYE-ZADACHI.md`, `SVODKI.md`, `ARHITEKTURA-FABRIKI.md`, `KARTA.md`) · `git show --stat` подтверждает: посторонних путей нет.

Ещё два коммита этого захода, оба названы в НЕОБРАТИМОМ: `3da7f02` (фиксация несохранённого в зоне перед слиянием) и merge-коммит `d83c2c6` (влитие `zahod/kanal-nablyudenij` по §0.1). Хук `pre-commit` ни разу не покраснел, `--no-verify` не применялся.

*(эта строка дописана отдельным ходом поверх коммита зоны — хэш до коммита не существует; та же форма, что в отчёте `svesti-i-vstroit`)*
