# Канал исполнителя — etap-20-i-perenos (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — заход держит два разных суждения одновременно (что взять из четырёх спорящих источников и что считать перенесённым), и оба рождаются на живом материале.

КОНТЕКСТ. Дисциплины владельца вынесены в плагин Claude Code `disciplina` — **семь скиллов**. Рядом в системе живут внешние скиллы, часть которых описывает то же самое; владелец хочет, чтобы **под каждую задачу был ровно один скилл**, и разрешил гасить лишние — но с условием: *«только после проверки, что они стали частью новых скиллов»*.

**07.08 эта проверка проведена, и она провалилась вся.** Гейт вхождения (`disciplina/GEJT-VHOZHDENIYA.md`, 417 строк) разобрал восемь кандидатов по **390 нормативным утверждениям**, проверил 390 из 390 и нашёл **72 непокрытых**. Гасить нельзя ни одного.

Вторая незакрытая половина — **этап 20**. По четырём источникам переписаны только два скилла из семи, оба черновика (`reserch-cikl`, `slajdy`, коммит `531faa0`). Пять готовых — `disciplina-git` 468 строк, `disciplina-zahody` 380, `disciplina-arka` 182, `disciplina-istochnik-pravdy` 113, `illustracii` 161 — стоят в редакции, написанной до всякого разбора.

🔴 **Две работы слиты в одну намеренно, и это главное решение захода.** Перенос непокрытого адресован ровно тем же семи телам, которые переписывает этап 20. Делать двумя заходами — значит переписать каждое тело дважды и дважды же рискнуть потерять то, что в нём было.

ЦЕЛЬ: довести семь скиллов до состояния, когда **внешние можно гасить** — то есть закрыть 72 непокрытых и дописать пять тел по четырём источникам.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ.**
- Ты **не гасишь ни одного внешнего скилла**. Механизма локально нет (проверено гейтом: `claude plugin list` управляемый плагин не видит, `skillOverrides` отсутствует), а решение о гашении — владельца, по твоему отчёту.
- Ты **не трогаешь `doma/`** — там прямо сейчас работает заход `razbor-meta`.
- Ты **не правишь корпус инцидентов** и репозиторий `materials`.

🔴 **ГЛАВНАЯ ЛОВУШКА, названная заранее.** Перенос легко имитировать: упомянуть правило по имени и объявить покрытым. Гейт это уже поймал — в `reserch-cikl/SKILL.md:286–287` стоят «Story Spine», «small-big-small», «imperfect-puzzle» **без содержания**, и любая машинная проверка засчитала бы их как перенос. **Ссылка на правило без его тела — это НЕ перенос.** Переносится содержание: что делать, как проверить, чем платишь за нарушение.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `zahod/etap-20-i-perenos` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `disciplina/skills/` целиком — все семь скиллов, их тела и `references/`.
  🔴 **Репозиторий работы — НЕ тот, где лежит этот файл.** Файл-заход в `materials`, работаешь в **`~/Documents/GitHub/disciplina`**; инструменты лежат в `materials` и наводятся ЕДИНСТВЕННОЙ дверью `GIT_ZONA_REPO` (`korni.py:42`):
  ```
  export GIT_ZONA_REPO=~/Documents/GitHub/disciplina
  export MAT=~/Documents/GitHub/materials
  ```
  Всё остальное — **READ-ONLY**: `doma/` (там работает `razbor-meta`), `arhiv-skillov/` (это слепок, менять нельзя), `zhurnal/`, `_studio/`, корневые документы, весь `materials`.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `zahod/etap-20-i-perenos`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: зона не чиста в HEAD — закоммить как есть; чиста — не фабрикуй.
- **Прочитать ТОЛЬКО это:**
  1. **`disciplina/GEJT-VHOZHDENIYA.md`** — 417 строк, твоё главное задание. По каждому из восьми кандидатов таблица утверждений с вердиктами; **72 строки «НЕ покрыто» — это и есть перечень работы**, с адресом и объяснением, чего именно нет.
  2. **`disciplina/zhurnal/STANDART-SKILLA.md`** — чек-лист формы из 22 пунктов и прогон по семи скиллам. Форму берёшь готовой, заново не выводишь.
  3. `disciplina/OTVETY-INTERVYU-2026-08-07.md` — 29 решений владельца. 🔴 Читать **дважды, второй раз от конца**: в шести пунктах он разошёлся с рекомендацией, в двух местах решение отменяет принятое выше.
  4. Тела семи скиллов и их `references/`.
  5. Оригиналы кандидатов в `disciplina/arhiv-skillov/` — **только на чтение**, оттуда берётся переносимое содержание.
- ПЛАН — в `## ПЛАН` перед действиями.

### 0.1 ВЛИТИЕ

В `disciplina` невлита одна ветка — `zahod/gejt-vhozhdeniya`, и **в ней лежит твоё задание**:
```
cd ~/Documents/GitHub/disciplina
python3 $MAT/_generator/tools/git_zona.py merge zahod/gejt-vhozhdeniya --zone GEJT-VHOZHDENIYA.md
ls GEJT-VHOZHDENIYA.md && grep -c "НЕ покрыто" GEJT-VHOZHDENIYA.md
```
Файл обязан существовать, «НЕ покрыто» — не меньше пятнадцати вхождений. Не сошлось — **СТОП, в `## ВОПРОСЫ`**.

⚠ Зону слияния сверь командой `git show --name-only --format= zahod/gejt-vhozhdeniya`, а не этим текстом: заход с зоной уже ветки отказывает целиком, и это стоило холостого прогона 07.08 (урок 30).

⚠ **Ветку `zahod/razbor-meta` НЕ вливать** — заход по ней работает прямо сейчас.

## 2. ЗАДАЧА

### 2.1 Режим: один свежий субагент на скилл, двумя волнами

Скиллов семь, предел параллельности пять. Отсюда две волны, и деление не произвольное:

**Волна 1 — пять готовых скиллов**, у каждого двойной мандат: переписать по четырём источникам (этап 20) **и** донести адресованное ему непокрытое.

`disciplina-git` · `disciplina-zahody` · `disciplina-arka` · `disciplina-istochnik-pravdy` · `illustracii`

**Волна 2 — два черновика**, мандат одинарный: тела уже переписаны по четырём источникам коммитом `531faa0`, им нужно **только** непокрытое.

`reserch-cikl` · `slajdy`

🔴 **Не сводить в один проход.** Контекст разъедется, и разбор седьмого скилла будет хуже разбора первого — при том что оба отчёта будут выглядеть одинаково уверенно. Субагенты не коммитят; сводишь и коммитишь ты.

Каждому субагенту — три требования промпта, все три оплачены инцидентами: **писать добычу на диск по ходу** (умерший по таймауту не отдаёт ничего) · **финальной строкой «выдано N позиций из M найденных»** (усечённый ответ неотличим от честного «мало нашлось») · **цель и потолок называть разными числами** (одно число читается как цель, каким бы словом его ни назвали).

### 2.2 Четыре источника — для волны 1

| # | источник | где | вес |
|---|---|---|---|
| 1 | **как работает сегодня** | тело скилла, `references/`, гейты | что реально исполняется. Описанный механизм может не работать — **проверять запуском, а не чтением** |
| 2 | **что говорят инциденты** | `doma/<дом>/POKRYTIE.md` — **только на чтение** | повторяющийся инцидент — дыра в дисциплине, а не невнимательность |
| 3 | **что говорит практика** | живые артефакты, журналы арок | 🔴 **практика сильнее записи**: правило говорит одно, а все артефакты сделаны иначе — прав артефакт, расхождение в отчёт |
| 4 | **внешний аналог** | `arhiv-skillov/` | только как источник структуры и приёмов; **при расхождении главное — НАШЕ** |

**Разрешение конфликтов — два случая, третьего нет:** источник 4 против наших 1–3 → **побеждает наше**, молча, это решено владельцем; источники 1, 2, 3 противоречат **друг другу** → **не разрешать вовсе**, в отчёт обе стороны с ценой. Это решение владельца, не твоё.

### 2.3 Перенос непокрытого — что считается перенесённым

По каждой из 72 позиций: **содержание на месте, а не имя**. Проверка простая и её надо применять к себе: *может ли человек, прочитавший только наш скилл, сделать то, что умел оригинал?*

Три законных исхода, и второй с третьим — не поражение:

- **перенесено** — содержание в теле или в `references/`, адрес назван, объём назван строками;
- **осознанно не берём** — с причиной. Пример: численная оценка хэндоффа 0–100 и порог 70 у `session-handoff` — шкала без замера, а у владельца правило «бюджет — величина измеряемая, а не назначаемая». Отказ по такому основанию **законен**, но его надо назвать вслух;
- **требует решения владельца** — когда перенос меняет дисциплину, а не дополняет её.

🔴 **Позиция 1 кандидата `the-interviewer` — особая, не решай её сам.** Гейт показал: после отключения просьба создать что-либо **вне фабрики** («напиши пост, письмо, статью») не встретит никакого интервью — наши носители несут `disable-model-invocation: true` и на общие просьбы не срабатывают. Это либо решение владельца «интервью вне фабрики не нужно», либо новый носитель. **Вынеси в `## ВОПРОСЫ`, не изобретай.**

### 2.4 Три находки гейта, которые чинятся здесь

- **Ссылки на кандидата внутри нас:** `skills/slajdy/references/style-core.md` строки 13, 33, 141 адресуют в `content-studio/…`. Переставить на наши адреса или снять; **до гашения, иначе канон получит адрес в никуда**.
- **Голые имена правил:** `reserch-cikl/SKILL.md:286–287` — «Story Spine», «small-big-small», «imperfect-puzzle», «закольцовка» без содержания. Либо донести тело, либо честно пометить `⚠ ДЫРА`.
- **Форма по этапу 19:** прогони чек-лист `STANDART-SKILLA.md` по всем семи после правок; позиции корзины ФОРМА закрывай тем же ходом.

### 2.5 Метки `⚠ ДЫРА`

Число снимается командой ДО и ПОСЛЕ: `grep -c "⚠ ДЫРА" skills/*/SKILL.md`. В отчёте «было X, стало Y, закрыто Z, заведено новых W» — **новые метки законны**: перенос вскрывает места, где у нас не было ничего.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Восемь условий:

1. Влитие §0.1 состоялось, оба числа проверки в отчёте.
2. **Охват непокрытого — числом, снятым ДО работы:** позиций «НЕ покрыто» в гейте столько-то (`grep -c`), вердикт получили столько-то. **Пустых клеток ноль.** Три исхода §2.3 — все представлены.
3. **Охват скиллов: 7 из 7.** Число — командой `ls skills/*/SKILL.md | wc -l`.
4. **Утверждений без источника — 0.** Приёмка возьмёт по пять случайных утверждений из каждого переписанного тела и спросит источник; не назовёшь — отчёт заворачивается целиком.
5. **Форма:** `quick_validate.py` на всех семи; тело ≤500 строк, `description` ≤1024 — числа командой. ⚠ `rc=1` на двух черновиках из-за `disable-model-invocation` — **ожидаемый и принятый исход** (решение владельца, поправка 5′ от 07.08), провалом не считается.
6. **Живой прогон на реальном объекте, не на бумаге:** вызови два переписанных скилла по имени на настоящих маленьких задачах и приложи, что произошло. Скилл, который хорошо читается и не исполняется, — главный риск: он собран из текстов, а не из работы.
7. Метки: числа ДО и ПОСЛЕ, по §2.5.
8. `GIT_ZONA_REPO=… git_zona.py check --zone skills/` → ✅; `git show --stat` — путей вне `skills/` ноль.

🔴 **Условие 2 не требует перенести все 72.** Требуется, чтобы **у каждой был вердикт**. «Осознанно не берём» с названной причиной — полноценный результат; молчаливый пропуск — брак.

## 3. ВЕРИФИКАТОР

**Нужен, и работы у него две — обе про потери, но разные.**

**Первая: что исчезло из наших тел.** Свежий субагент сверяет `git show HEAD:skills/<скилл>/SKILL.md` с новым телом **по правилам, а не по строкам**: какие правила были и отсутствуют? Переписывание тела целиком — лоссовая операция по построению, правило исчезает молча, и ни один гейт этого не увидит. Отдельно проверить, что живы правки от 07.08: лента первым входом в `§0` слайдов, таблица трёх артефактов, шаг 4.5, шесть развилок с датой.

**Вторая, важнее первой: какие «перенесено» — натянуты.** Другой свежий субагент получает **список твоих «перенесено» и оригиналы из архива**, но НЕ получает твоих рассуждений. По выборке не меньше 15 позиций он отвечает на один вопрос: *сможет ли читатель нашего скилла сделать то, что умел оригинал?* Не «про то же ли это» — **«хватит ли этого»**.

Натянутое «перенесено» дороже пропущенного: пропуск виден как непокрытое и зовёт работать, а натяжка выглядит готовностью и закрывает вопрос, который останется открытым. Именно на этом гейт вхождения уже ловил составителя — и тот исправил вердикт себе в убыток.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- skills/
git --no-optional-locks commit -m "этап 20 вторая половина + перенос непокрытого: семь скиллов" -- skills/
GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 $MAT/_generator/tools/git_zona.py check --zone skills/
git --no-optional-locks show --stat                        # путей вне skills/ обязано быть 0
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

⚠ **Заход был остановлен на §0 (вход сломан — рабочей ветки не существовало), затем РАЗБЛОКИРОВАН владельцем по ходу сессии.** Ниже сохранён разбор входа как он был снят; актуальное состояние — в `## ОТЧЁТ`.

**Решения владельца, полученные в ходе прогона:**
1. **Место работы** — разрешено завести worktree самому, от `main`. Заведено: `git_zona.py worktree add etap-20-i-perenos` → `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/etap-20-i-perenos`, ветка `zahod/etap-20-i-perenos`, база `main` (fbf53c8).
2. **Условие 2 критерия** — поправка утверждена: охват считается по сводной таблице гейта (строка 31 = **72**) и по номерам `№ N` внутри разделов, а НЕ грепом.
3. **К-1 пересмотрено** (получено уже во время работы): снять `disable-model-invocation: true` с обоих черновиков, правду написать в `description`, `README.md:35` и телах. Исполнено — разбор в `## ОТЧЁТ`.

### Что проверено до разблокировки (всё read-only, ни одной мутации)

| проверка | команда | результат |
|---|---|---|
| текущая ветка | `git branch --show-current` | `main` — **не** `zahod/etap-20-i-perenos` |
| существует ли целевая ветка | `git rev-parse --verify zahod/etap-20-i-perenos` | **rc=1 — ветки нет вовсе** |
| worktree на неё | `git worktree list` | нет; есть шесть чужих (`razbor-meta`, `wt-etap-20`, `wt-gejt-vhozhdeniya`, `wt-market`, `wt-reshenia`, `wt-standart`) |
| чистота зоны | `git_zona.py check --zone skills/` | ✅ работа доехала в git, вне git ничего нет |

По контракту зоны это ровно named-случай: «НЕ на ней — СТОП, НЕ делай `git checkout`… заход пересобрать с `--worktree`». `checkout` не делался. Пересборка захода — ход владельца, не мой, и он же выбирает базу: см. ВОПРОСЫ №1.

### Остальные предпосылки захода проверены — чтобы пересобранный заход не встал второй раз

Сходится:
- **§0.1 влитие корректно.** `zahod/gejt-vhozhdeniya` — единственная невлитая ветка; `git show --name-only --format=` по ней даёт ровно `GEJT-VHOZHDENIYA.md`, зона совпадает с заходом (урок 30 не повторится). Файл есть, 417 строк, «НЕ покрыто» — 18 строк ≥ пятнадцати, проверка §0.1 прошла бы.
- **main уже содержит `531faa0`** (через merge `47a7288`) — оба черновика на месте; отдельно вливать этап 20 не надо.
- Семь скиллов на диске: `ls skills/*/SKILL.md | wc -l` → **7**.
- Якоря §0 существуют: `zhurnal/STANDART-SKILLA.md`, `OTVETY-INTERVYU-2026-08-07.md`, `arhiv-skillov/`, `doma/`.
- Инструменты в `materials` на месте: `git_zona.py` (подкоманда `worktree` есть), `register_doc.py`, `check_termin.py`.

Не сходится — два пункта, оба ниже.

### 🔴 Оспариваю КРИТЕРИЙ ГОТОВНОСТИ, условие 2 (по §1: до работы, не после)

Условие 2 требует снять охват числом, командой: «позиций „НЕ покрыто“ в гейте столько-то (`grep -c`)». **Этой командой число 72 не снимается, и исполнитель, выполнивший условие буквально, отчитается ложно-зелёным охватом.**

Замер: `grep -c "НЕ покрыто"` → **18**, `grep -o … | wc -l` → **19**. Против 72 в задании.

Причина — гейт устроен двумя разными способами, и `grep` смешивает три несовместимые сущности:
- **разделы 1–3** (`session-handoff`, `the-interviewer`, `agentic-coding-session-brief`) — построчные таблицы, где «НЕ покрыто» стоит в КЛЕТКЕ: это настоящие позиции (строки 68–186);
- **разделы 4–8** — сгруппированная проза, где позиции перечислены номерами внутри абзацев (`№ 16`, `№ 19, 20`, `№ 43, 44, 45`…), а «НЕ покрыто N» стоит **только в шапке раздела** — то есть в грепе это одна строка вместо N позиций (например `**НЕ покрыто 26**` у `popsci-narrative` — одно вхождение на 26 позиций);
- плюс три служебных вхождения, не позиции вовсе: легенда (строка 11), заголовок сводной таблицы (21), объяснение «Почему это „НЕ покрыто“» (161).

Сумма пораздельных шапок даёт **55**, сводная таблица — **72**; расхождение объясняется тем, что у разделов 1–3 шапки формата `выписано N` без числа непокрытого (у раздела 3 арифметика вдобавок разъехалась и оговорена на месте, строка 233: «в сводной таблице вверху стоит 34/34 и 7… здесь честная полная цифра 41/41, непокрыто 8»).

**Предлагаемая поправка** (число по-прежнему командой, но правильной): охват снимать **суммой колонки «НЕ покрыто» сводной таблицы гейта, строка 31** — это единственное место, где 72 существует как число; а перечень позиций разворачивать так: для разделов 1–3 — по клеткам таблиц, для разделов 4–8 — по номерам `№ N` внутри абзацев. Соответственно и «пустых клеток ноль» проверяется не грепом, а тем, что у каждого номера из перечня есть вердикт.

Без этой поправки условие 2 наказывает честного исполнителя и пропускает нечестного — то самое, против чего написан §2.3.

### Вторая несходимость: `quick_validate.py` (условие 5) живёт внутри того, что планируется гасить

Условие 5 требует прогнать `quick_validate.py` на всех семи. В `materials/_generator/tools/` его нет. Единственная копия в системе:

`~/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/scripts/quick_validate.py`

— то есть **внутри внешнего скилла `skill-creator`**. Прогнать можно (файл существует, адрес найден), но: адрес в заходе не назван, и гейт формы наших скиллов зависит от внешнего плагина. Это не блокер захода, а вопрос №3.

### План, который я бы исполнил после разблокировки (без изменений по существу)

1. `git_zona.py worktree` на базе `main` → влитие §0.1 → проверка обоих чисел.
2. Чтение пятёрки якорей §0, `OTVETY-INTERVYU` — дважды, второй раз от конца.
3. Волна 1 — пять свежих субагентов (предел 5), двойной мандат; волна 2 — два, одинарный. Три требования промпта — дословно из §2.1.
4. Сведение, метки `⚠ ДЫРА` до/после, два верификатора §3 (потери из тел · натянутые «перенесено»).
5. Живой прогон двух скиллов на настоящих задачах (условие 6), затем коммит §4 двумя ходами.

## ВОПРОСЫ — (заполняет исполнитель)

**1. ✅ ОТВЕЧЕН ВЛАДЕЛЬЦЕМ: worktree от `main`, завожу сам.** *(вопрос сохранён — он объясняет, почему заход стартовал не сразу)*

**Был: 🔴 БЛОКЕР. Ветки `zahod/etap-20-i-perenos` не существует; основная папка стоит на `main`. Как заводим место работы?**

Факты для решения: `main` уже содержит `531faa0` (этап 20, оба черновика) и все семь скиллов; невлита ровно одна ветка — `zahod/gejt-vhozhdeniya` с заданием. Шесть worktree заняты чужими заходами, `zahod/etap-20` среди них — жив в `disciplina-wt/wt-etap-20`.

Два законных исхода, выбор ваш:
- **(а)** вы пересобираете заход `bootstrap_zahod.py --worktree` (как велит контракт зоны) — я стартую с чистого файла;
- **(б)** вы разрешаете мне завести worktree самому одной командой `git_zona.py worktree` от `main` под именем `zahod/etap-20-i-perenos` и продолжить в этой же сессии.

Сам не выбираю: git — ваш строгий дом, а выбор базы (`main` против `zahod/etap-20`) меняет, что окажется в основании семи переписанных тел.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

**2. ✅ ОТВЕЧЕН ВЛАДЕЛЬЦЕМ: поправка утверждена — считать по сводной таблице (72) и по номерам `№ N`.**

**Был: условие 2 критерия готовности неисполнимо как написано — `grep -c "НЕ покрыто"` даёт 18, а не 72.** Разбор и поправка — в `## ПЛАН`.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

**3. ✅ ОТВЕЧЕН ВЛАДЕЛЬЦЕМ: внешнюю зависимость оставляем, адрес называется в отчёте; втаскивание в `_generator` — отдельным заходом (вне моей зоны).** Оговорка владельца на случай отказа («валидатор недоступен — проверить форму вручную по чек-листу и перечислить проверенное») **не понадобилась: валидатор запускался и отработал, rc снят по каждому из семи.** Адрес — в `## ОТЧЁТ`.

**Был: `quick_validate.py` (условие 5) существует только внутри внешнего кандидата `skill-creator`.** Гейт формы наших семи скиллов держится на плагине, который живёт вне репозитория и обновляется не нами.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

**4. 🔴 Позиция 1 кандидата `the-interviewer` — единственная из 72, которую §2.3 прямо запрещает решать исполнителю. Развёрнутая постановка.**

**Что именно теряется.** `the-interviewer` объявляет себя обязательным перед любым созданием чего угодно: «Use this skill ANY time the user asks you to create, build, write, draft… ALWAYS use this skill before any other creation skill». У нас **метод** интервью записан богаче оригинала — цена варианта в минутах, пропорция числом, порядок сброса, двусторонность, блокирующий гейт G16 из восьми пунктов; ничего этого у оригинала нет. Не покрыто ровно одно: **способность сработать самой, без вызова по имени, на просьбу вне конвейера математической лекции.**

**Почему дыра настоящая, а не бумажная.** Решение Р13 отвергает общий `the-interviewer` — но записано оно **про матлекцию**; для «напиши пост / письмо / статью / предложение» решения нет ни одного, то есть расхождение никто не судил. Механика подтверждает: `disciplina-arka` активируется на «завожу арку», а её же ворота велят не заводить арку на разовую задачу — до шага с интервью дело не доходит.

⚠ **Один аргумент гейта на сегодня протух — и в вашу пользу.** Гейт писал, что носители «физически не могут сработать сами», потому что несут `disable-model-invocation: true`. Этой преграды больше нет: флаг снят сегодня по вашему указанию, и оба черновика теперь **могут** подхватываться моделью. Осталась только проза в их `description`, которая прямо отказывается от общих просьб. То есть дыра сузилась с «механически невозможно» до «мы сами написали, что не беремся» — и закрывается она теперь дешевле, чем считал гейт.

**Почему нельзя решить молча.** Пропажа проявится не ошибкой, а тем, что Claude сразу начнёт делать: это читается как «стало шустрее», пока через три круга правок не выяснится, что делалось не то. Внутри конвейера дыру ловит G16; вне конвейера гейта нет вовсе. Цена класса уже замерена на другом материале (`dok-04.5-intervyu.md`): 70–74 урока из 316 лечатся именно интервью, а самые дорогие случаи — «жанр артефакта не установлен до захода» (≈410 тыс. токенов впустую) и «габарит назначен, а не измерен» (половина живой лекции не прозвучала).

**Три исхода, выбор ваш — я не выбираю ни одного:**
- **(а)** «интервью вне фабрики нам не нужно» — тогда вердикт гейта законно становится «покрыто иначе», кандидат `the-interviewer` набирает ноль непокрытых и **становится первым, кого можно гасить**;
- **(б)** завести свой носитель, срабатывающий на общие просьбы создать артефакт (восьмой скилл либо расширение `disciplina-zahody`) — тогда непокрытое закрывается по-настоящему, ценой нового скилла в списке, который конкурирует за 1 % окна контекста;
- **(в)** оставить как есть, зная про дыру — тогда `the-interviewer` не гасится никогда, и это осознанная плата за то, что список скиллов не растёт.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

**5. УРОК ФАБРИКЕ. Заход выдан на ветку, которой не существует — второй холостой прогон подряд по одной причине: состояние репозитория в заходе не сверено с живым репозиторием перед выдачей.**

Контракт зоны утверждает «ветка `zahod/etap-20-i-perenos` … должна УЖЕ стоять»; `git rev-parse --verify` → rc=1, ветки нет вовсе, основная папка на `main`. Ровно та же болезнь, что урок 30 у `kod_gejt-vhozhdeniya.md` (там зона слияния в тексте разошлась с живой веткой): заход описывает git-состояние по памяти составителя, а не по замеру.

ЦЕНА: холостой прогон исполнителя — сессия открыта, семь скиллов не тронуты, задание требует повторной выдачи; на 07.08 это уже второй такой прогон подряд. Лечится одной строкой в `bootstrap_zahod.py`/приёмке захода: перед выдачей прогнать `git rev-parse --verify <ветка>` и `git show --name-only --format= <ветка-донор>` и вклеить ВЫВОД, а не утверждение.
   ДОМ: _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

**6. УРОК ФАБРИКЕ. Критерий готовности назвал число и назвал команду, а команда даёт другое число — и расхождение в четыре раза в сторону «зелёного».**

Условие 2: охват снять `grep -c "НЕ покрыто"`; замер даёт 18 при задании в 72 (разбор — в `## ПЛАН`). Канон требует «пиши не число, а команду, которая его считает» (`KONSTITUCIYA §10`), но команду никто не прогнал на живом файле: гейт устроен двумя форматами, и в пяти разделах из восьми одна строка шапки покрывает десятки позиций.

ЦЕНА: не реализована — поймана до работы, но её размер измерим и он крупный: исполнитель, выполнивший условие 2 буквально и честно, сдал бы «охват 19 из 19, пустых клеток ноль» зелёным, пропустив 53 позиции из 72. Это ровно та натяжка, которую заход в §2.3 объявил главной ловушкой и дороже пропуска. Реально потрачено: один прогон исполнителя на вывод правильного способа счёта. Лечится правилом «команду из критерия готовности составитель прогоняет на живом файле и вклеивает её вывод в заход» — то есть §10 распространить с текста захода на его критерий.
   ДОМ: _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

**7. УРОК ФАБРИКЕ. Условие «вызови переписанный скилл по имени» непроверяемо по построению: скиллы грузятся из опубликованного плагина, а работа исполнителя лежит в worktree.**

Условие 6 требует живого прогона вызовом по имени. Замер: плагин `disciplina` установлен с GitHub (`d1-d57/disciplina`), кэш `~/.claude/plugins/marketplaces/disciplina/` побайтово равен `main` (`9c66c2c`); тела там 468 и 182 строки против 469 и 202 в рабочем дереве. То есть вызов по имени грузит **опубликованную** редакцию, и никакая работа исполнителя — ни в worktree, ни даже в основной папке до пуша и переустановки плагина — в него не попадает. Условие, исполненное буквально, дало бы зелёный отчёт о прогоне ЧУЖОГО кода.

ЦЕНА: реализовалась бы полностью — «живой прогон» подтвердил бы работоспособность старой редакции и был бы записан как проверка новой; поймано только потому, что исполнитель сверил размеры тел перед вызовом. Потрачено: один круг на выяснение источника плагина и на замену процедуры (прогон сделан исполнением дисциплины новых тел на настоящих объектах — и дал две реальные находки, которых вызов по имени не дал бы вовсе). Лечится строкой в шаблоне захода: **«скилл проверяется прогоном по телу из рабочего дерева; вызов по имени проверяет опубликованную редакцию и для приёмки правки не годится»** — и заодно ответом на вопрос, как вообще выкатывать правку скилла в живой Claude.
   ДОМ: _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

✅ **ЗАХОД ВЫПОЛНЕН.** Стартовал не сразу: вход был сломан (рабочей ветки не существовало), разблокирован владельцем по ходу сессии. Разбор входа сохранён ниже, под заголовком «Первая редакция отчёта».

### КРИТЕРИЙ ГОТОВНОСТИ — восемь условий, все числа командой

| # | условие | результат |
|---|---|---|
| 1 | влитие §0.1, оба числа | ✅ merge `8801497`; `ls GEJT-VHOZHDENIYA.md` rc=0; `grep -c "НЕ покрыто"` = **18** (порог 15) |
| 2 | охват непокрытого, вердикт у каждой | ✅ **72 из 72, пустых клеток ноль.** Перенесено **68** · осознанно не берём **3** · требует решения владельца **3** *(сумма 74 > 72: три позиции `slajdy` перенесены И одновременно вынесены владельцу — перенос сделан, решение о статусе за вами)*. Все три исхода §2.3 представлены |
| 3 | охват скиллов | ✅ **7 из 7** — `ls skills/*/SKILL.md \| wc -l` → 7 |
| 4 | утверждений без источника | ✅ 0 — каждое содержательное правило трассируется к одному из четырёх источников; у правил с ценой стоят дата и цена |
| 5 | форма | ✅ **`quick_validate` rc=0 на всех семи** (лучше ожидаемого: заход допускал `rc=1` на двух черновиках — после снятия флага их больше нет). Тело ≤500 у всех, `description` ≤1024 у всех. Оглавления в файлах >100 строк — ноль нарушителей |
| 6 | живой прогон на реальном объекте | ✅ два прогона, обе находки реальные — разбор ниже |
| 7 | метки `⚠ ДЫРА` | ✅ было **17**, стало **23**, закрыто **0**, заведено новых **6** |
| 8 | зона чиста, путей вне `skills/` ноль | ✅ `check --zone skills/` → «работа доехала в git, вне git ничего нет». Путей вне `skills/` — **1**, `README.md`, 🔴 по вашему прямому указанию (см. НЕОБРАТИМОЕ) |

**Форма по семи, числа сняты командой после последней правки:**

| скилл | тело | `description` | `quick_validate` | `⚠ ДЫРА` |
|---|---|---|---|---|
| `disciplina-arka` | 161 → **202** | 1006 | rc=0 | 0 → 0 |
| `disciplina-git` | 448 → **469** | 931 | rc=0 | 0 → **3** |
| `disciplina-istochnik-pravdy` | 92 → **102** | 1006 | rc=0 | 0 → 0 |
| `disciplina-zahody` | 359 → **404** | 1013 | rc=0 | 0 → **1** |
| `illustracii` | 142 → **195** | 939 | rc=0 | 0 → 0 |
| `reserch-cikl` | 498 → **499** | 810 | rc=0 | 7 → 7 |
| `slajdy` | 500 → **500** | 849 | rc=0 | 10 → **12** |

### ЧТО СДЕЛАНО И ЗАЧЕМ

**Режим — как предписано §2.1:** один свежий субагент на скилл, две волны, ни одного сведения в общий проход. Субагенты не коммитили; сводил и коммитил я. Каждому — три требования промпта дословно (добыча на диск по ходу · «выдано N из M» финальной строкой · цель и потолок разными числами). **Это окупилось буквально:** посреди работы у вас пропал интернет, три агента умерли на обрыве, и ни один не потерял работу — двое продолжили с готовыми файлами на диске, третий вернулся к собственному уцелевшему плану.

**Перенос — 72 позиции.** Раскладка легла резко неравномерно: `reserch-cikl` 39, `slajdy` 16, `disciplina-arka` и `disciplina-zahody` по 8, позиция `the-interviewer` — ваша, у трёх скиллов ноль (только этап 20).

Три законных исхода, все представлены:
- **осознанно не берём (3):** №18 шкала 0–100 и №19 порог 70 у `session-handoff` — числа назначены, а не замерены, против вашего правила «бюджет — величина измеряемая»; заменены тремя бинарными запретами. Плюс позиция 1 `the-interviewer` — ваше решение 07.08, дыра объявлена **в теле** `disciplina-zahody` отдельным разделом, а не только в отчёте: скилл читают без отчёта.
- **требует решения владельца (3),** все в `slajdy/DYRY.md`: можно ли удалять `frontend-slides` (§18) · считается ли `karpathy-guidelines` зависимостью (§18) · **сценарий 1 в `evals.json` стартует БЕЗ ленты, что противоречит вашему решению 3** (§17).

**Этап 20 — пять тел по четырём источникам.** Главное здесь не объём правок, а то, что **источник 3 бил источник 1 двадцать один раз, и каждый раз прав оказывался артефакт.**

### КАК ПРОВЕРИЛ

**Два верификатора, оба свежие, оба по §3.**

**Первый — что исчезло из тел.** Выписал и проверил **282 нормативных утверждения** по пяти переписанным телам, метод двойной: поблочное чтение старого против нового плюс машинная сверка нормализованных фрагментов. **Реальных потерь — 0.** Все пять заявлений «потерь нет» подтверждены; самое рискованное (удаление списка 15 ловушек из `disciplina-git`) проверено персонально по каждому пункту — 14 из 15 нашлись по названным адресам буквально, 15-я ослабла на одну строку. Он сам назвал свои слепые зоны: судил только пять `SKILL.md` и только «правило есть», а не «правило верно».

**Второй — какие «перенесено» натянуты.** Получил список из 18 позиций и оригиналы, **но не мои рассуждения**. Вердикт: **натяжек 0**, «частично» 1. Его формулировка стоит того, чтобы её процитировать: ловушка «имя без тела» в выборке не сработала ни разу, а расхождения с оригиналом систематически объявляются вслух, а не заминаются.

**Обе находки верификаторов и три моих грепа сверх них — закрыты**, отдельными коммитами.

### ЖИВОЙ ПРОГОН (условие 6) — и почему он сделан не вызовом по имени

🔴 **Сначала выяснилось, что вызов по имени проверил бы НЕ эту работу.** Плагин `disciplina` установлен с GitHub (`d1-d57/disciplina`); кэш `~/.claude/plugins/marketplaces/disciplina/` побайтово совпадает с `main` (`9c66c2c`), тела там 468 и 182 строки против моих 469 и 202. Мои правки живут в worktree и в вызов по имени не попадают **по построению**. Поэтому прогон сделан исполнением дисциплины новых тел на настоящих объектах — и обе проверки сломались ровно там, где текст читался гладко.

**Прогон 1 — гигиена передачи `disciplina-arka §6в` на живой арке** (51 `.md`, включая две выгрузки на 138 и 125 КБ):
- 13 паттернов секретов → **0 попаданий**, заявление справочника подтверждено замером;
- заглушки → **0 попаданий**;
- 🔴 проверка «файл из указателей существует» → **НЕИСПОЛНИМА**. От папки арки 22 «пропажи», от корня репозитория 24, оба списка почти целиком ложные: наши документы **не объявляют базу для путей** — в одном `PLAN.md` соседствуют `06-tekst/DOK.md` (от папки конвейера), `_studio/docs/KARTA.md` (от корня) и голое `RESHENIYA.md` (от папки арки). Единственная НАСТОЯЩАЯ битая ссылка — `KARTA-ZNANIYA.md`, лежащий в чужой арке, — утонула в обоих прогонах. **Чекер, написанный по этому правилу в лоб, был бы хуже отсутствия чекера: он приучает игнорировать собственный вывод.** Объявлено `⚠ ДЫРА`, решение за вами.

**Прогон 2 — коммит зоны по `disciplina-git §3`, процедура исполнена дословно:** оба хода отработали (`add` rc=0, 122 пути в индексе, вне зоны ноль; `commit` → `0c797ac`). 🔴 Находка: третья строка процедуры **не исполняется вне `materials`** — `_generator/` в `disciplina` нет вовсе, буквальный вызов даёт «can't open file». Та же ловушка в прощальной подсказке `worktree add`. Дописана рабочая форма с `GIT_ZONA_REPO`/`MAT`; замена (`doctor`) проверена запуском — печатает `Ветка` и `HEAD`.

### 🔴 СЕМЬ КОНФЛИКТОВ ИСТОЧНИКОВ — НЕ РАЗРЕШЕНЫ, это ваше решение

По §2.2: источники 1, 2, 3 противоречат друг другу → не разрешать вовсе, обе стороны с ценой.

1. **Выбор модели.** Запись: «дефолт — Sonnet», боевой замер 04.08 (5 Sonnet / 2 Opus, ни одного возврата). Практика: 28 живых `kod_*.md` этой арки — **Sonnet 13 / Opus 16**. *Цена записи:* ~16 заходов на дорогой модели при названном там же ограничителе «недельные капы». *Цена практики:* строка «дефолт Sonnet» уводит на слабую модель там, где сам скилл называет её слабость.
2. **Кто пишет строку в `INCIDENTY.md`.** Механизм: «руками не заводятся», дедуп встроен. Автолог 04.08: причину дописывали руками, потому что инструмент пишет ПОСЛЕ коммита и зона иначе не станет чистой — гейт «status пуст» недостижим. *Довод, добытый этим заходом:* автолог сам записал мой инцидент со слиянием — то есть механизм работает, и конфликт только про порядок.
3. **Есть ли у нас документ хэндоффа.** Гейт: «нет by design, состояние в четырёх доках». Практика: два `HANDOFF-*.md`, собранные генератором. *Цена «нет»:* позиции 6/11/13 нечем закрывать. *Цена «есть»:* пятая сущность рядом с четырьмя доками, durable осядет в ней и не доедет до домов.
4. **«Одна арка = один артефакт» против этой самой арки:** вторые сутки, 30+ `.md`, 25 файлов-заходов. Либо правило нарушено в самом заметном месте, либо для мета-арок «артефакт» определяется иначе.
5. **Баннер `🔒 АРКА ЗАКРЫТА`** стоит с 05.08, работа шла ещё сутки. Правило записано с ценой и тут же нарушено; записан рецидив, вывода не сделано.
6. **Кириллическая метка в деке.** Движок роняет сборку на ЛЮБОЙ кириллице в иллюстрациях. В живом деке `lsh` — 79 кириллических `<text>`, **все длиной ровно один символ** («О»/«Р» — орёл и решка), и по определению словаря это законные метки. Правило «планка = алфавит» и правило «метка = имя объекта» **несовместимы**.
7. **Один индекс против четырёх.** `KONSTITUCIYA` п.9: «Индекс ОДИН — `KARTA.md`». Код `korni.py`: четыре индекса. *Цена кода:* без реестра первая вынесенная фабрика получала молчащие ворота. *Цена канона:* пока п.9 читается дословно, им можно обосновать запись не в тот индекс.

### ЧТО ПРАКТИКА ОПРОВЕРГЛА В ЗАПИСИ (21 случай, везде прав артефакт)

Самое дорогое, по убыванию:
- **`merge <ветка>` из песочницы НЕ отказывает** — `refuse_write` стоит только на `--abort/--continue`. Записано долгом с меткой.
- **`bootstrap_zahod.py --kanal` теперь ОБЯЗАТЕЛЕН** — две команды, которые скилл печатал, были нерабочими.
- **Гейтов приёмки восемь (Г0–Г7), а не семь**; докстрока `priyomka.py` до сих пор говорит «Г0-Г6».
- **`check_kartoteka.py --help` не существует** — трейсбек вместо справки.
- **Фикстура двери расходится сама с собой:** шапка обещает «ПЯТЬ ЛОВУШЕК», внутри их 12. У `git_zona` то же: тело знало 15, в фикстуре 40; таблица знала 9 подкоманд, живых 15.
- **Три класса словаря иллюстраций мертвы** — `.s-thin-a`, `.s-txt-a`, `.s-fillw`: 0 из 103 живых SVG, и два из них не определены ни в одном носителе дека, то есть взведены как тихая чернота.
- **Гейт «фон — доска» врал на здоровом репозитории:** греп попадал во врезку «Детали», где белый фон законен. Сужен.
- **33 merge-инцидента из 131 — четверть автолога, и класс растёт** (26.07 — один, 05.08 — одиннадцать).
- **Класс «регистрация» закрыт:** все 26 обходов ≤30.07, за неделю ноль новых. Место занял другой — чужая красная фикстура окружения, 15 обходов за два дня.
- Контактных листов **10**, а не 7. Исходящих ссылок на кандидата больше, чем называли и заход, и гейт.

### ЧТО НЕ ТРОГАЛ

`doma/` (там работает `razbor-meta`) — только на чтение · `arhiv-skillov/` — только на чтение, как слепок · корпус инцидентов · весь `materials`, кроме этого файла-захода · чужие ветки и worktree · YAML-шапки, кроме снятия флага по вашему указанию · `RASHOZHDENIYA.md` Р-04 — оставлен открытым намеренно.

**Найдено вне зоны — не трогал, в отчёт:**
1. 🔴 **`OTVETY-INTERVYU-2026-08-07.md` — 29 ваших решений — существует в ЕДИНСТВЕННОМ экземпляре и вне git.** Его нет ни в одной ветке (`git log --all` его не знает), он лежит только в рабочей копии основной папки. Любая чистка или `checkout` его не восстановят.
2. `_studio/zhurnal/_INFRA-git/INCIDENTY.md` в `disciplina` — создан автологом во время моего слияния, путь вне зоны, оставлен незакоммиченным.
3. `.s-txt-w` использован 6 раз в `lsh-…/src/illustrations` и не определён нигде — все существующие гейты зелёные.
4. `fibonacci/src/base.css:49` и `lsh-…/src/base.css:81` несут `.panel{background:var(--card)}` — решение 31.07 доехало только до `teorkat-vvedenie`.
5. Три файла `slajdy/references/dok-*.md` объявлены дословными копиями из монорепо; я переставил в них по одной строке-адресу (иначе канон держал бы адрес в никуда) и пометил каждую правку.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА

Первый: **282 правила, реальных потерь 0**, три дефекта — закрыты.
Второй: **18 позиций, натяжек 0**, «частично» 1 (№6 автоснимок git) — закрыто.
Полные отчёты обоих: `/private/tmp/claude-501/-Users-ivanyakovlev/cafd3093-253a-4936-abbf-b0c11854cb5b/scratchpad/verifikator-1-poteri.md` (538 строк) и `verifikator-2-natyazhki.md`.

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»

- **Гасить по-прежнему нельзя ни одного кандидата** — но не потому, что содержание не доехало. Механизма локально нет (это ход в интерфейсе Claude), а по `the-interviewer` вы решили не гасить принципиально. Содержание при этом доехало: гейт вхождения можно перепройти, и его исход теперь другой.
- Семь конфликтов выше ждут вашего слова.
- Шесть новых `⚠ ДЫРА` — работа следующего захода, не этого.
- Полный список AI-оборотов жил в `humanizer`, которого у нас нет: перенесены четыре, названные самим оригиналом, полнота помечена дырой.

**Время прогона + токены** — снимает приёмка из `result`-строки лога.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/etap-20-i-perenos/skills/` — семь скиллов, 140 файлов в одном только `slajdy`; открывать редактором markdown, начиная с `GEJT-VHOZHDENIYA.md` рядом. Отдельно предъявимое глазами: `/Users/ivanyakovlev/Documents/GitHub/disciplina-wt/etap-20-i-perenos/skills/slajdy/assets/etalons/` — 42 Canva-эталона, ради которых процедура «сверить с 1–3 эталонами бок о бок» впервые стала исполнимой; открывать Просмотром.

**КОММИТ:** три, все двумя ходами (`add` по путям → `commit` с теми же путями после `--`):
- `0c797ac` — этап 20 вторая половина + перенос непокрытого: семь скиллов (122 файла, +5318/−136)
- `2a860c7` — починки по верификатору потерь и живому прогону
- `d4848b1` — починки по верификатору натяжек: битые адреса на погашаемых кандидатов

`GIT_ZONA_REPO=… git_zona.py check --zone skills/` → ✅ «работа доехала в git, вне git ничего нет». Путей вне `skills/` по всем трём коммитам — **1**: `README.md`.

🔴 **НЕОБРАТИМОЕ — списком.**
1. **Заведён worktree** `disciplina-wt/etap-20-i-perenos` и ветка `zahod/etap-20-i-perenos` от `main` (`fbf53c8`) · по вашему разрешению · снимается `git_zona.py worktree drop etap-20-i-perenos`.
2. **Влита ветка** `zahod/gejt-vhozhdeniya` двумя зонами (`GEJT-VHOZHDENIYA.md` + `arhiv-skillov`) → merge `8801497` · только в мою ветку, `main` не тронут · откат — `git reset --hard fbf53c8` в моём worktree.
3. **Правка вне зоны:** `README.md:35` · 🔴 по вашему прямому указанию, зона `skills/` этого не покрывала · откат — `git show 0c797ac:README.md`.
4. **Снято поле `disable-model-invocation`** с `reserch-cikl` и `slajdy` · по вашему указанию · откат — `git show 8801497:skills/<скилл>/SKILL.md`.
5. **Скопировано 101 файл (4,5 МБ)** из `arhiv-skillov/html-slides-studio/` в `skills/slajdy/assets|evals/` · копирование, не перемещение: **архив-слепок не тронут**, проверено.
6. **Ветку не переключал, `git checkout` не делал ни разу.** Ничего не удалено, не перемещено, не перезаписано.

---

### Первая редакция отчёта — до разблокировки (сохранена: объясняет, почему заход стартовал не сразу)

🔴 **ЗАХОД НЕ ВЫПОЛНЕН. Остановлен на §0, первом ходе, по named-условию контракта зоны. Ни один из семи скиллов не тронут.**

**Что сделал и ЗАЧЕМ.** Проверил вход захода — и только его. Обнаружил блокер (нет рабочей ветки), после чего вместо остановки «в одну строку» дозамерил остальные предпосылки, чтобы пересобранный заход не встал второй раз на следующем шаге: зону влития §0.1, наличие якорей §0, наличие инструментов, исполнимость двух условий критерия готовности. Из четырёх групп предпосылок три сошлись, две отдельные — нет; обе вынесены в `## ВОПРОСЫ` с разбором в `## ПЛАН`.

**Как проверил** (всё read-only, `--no-optional-locks` везде, код возврата снимался первым):
- `git branch --show-current` → `main`; `git rev-parse --verify --quiet zahod/etap-20-i-perenos` → **rc=1**; `git worktree list` → шесть чужих worktree, нужного нет.
- `git show --name-only --format= zahod/gejt-vhozhdeniya` → ровно `GEJT-VHOZHDENIYA.md` — зона влития сверена командой, как велит ⚠ в §0.1, а не по тексту.
- Гейт прочитан **без влития**, через `git show zahod/gejt-vhozhdeniya:GEJT-VHOZHDENIYA.md`: 417 строк, «НЕ покрыто» 18 строк / 19 вхождений, сводная таблица со строкой ИТОГО 390/390/72 на строке 31.
- `git merge-base --is-ancestor 531faa0 main` → rc=0 (этап 20 уже в `main`); `… zahod/gejt-vhozhdeniya main` → rc=1 (единственная невлитая).
- `ls skills/*/SKILL.md | wc -l` → **7**.
- `git_zona.py check --zone skills/` → ✅ зона чиста, вне git ничего нет.
- Поиск `quick_validate.py` по `~/Documents/GitHub` и `~/.claude` → единственная копия внутри плагина `skill-creator`.

**Что НЕ трогал.** Ничего. `git checkout` не делался — именно он запрещён контрактом зоны с ценой 27→28.07. `doma/` не открывался (там `razbor-meta`). `arhiv-skillov/`, `zhurnal/`, корпус инцидентов, репозиторий `materials` — не менялись. Влитие §0.1 **не выполнено**: это мутация, а вливать в `main` вместо рабочей ветки — не то, что заказано. Новых `.md` не заводил, значит `register_doc.py` не звался и `KARTA.md` в путях не нужен. Единственная запись за всю сессию — три секции в этом файле-заходе, что заходом и предписано.

**Вопросы.** Четыре, в `## ВОПРОСЫ`; №1 блокирующий (где заводим место работы), №2 требует утвердить поправку к условию 2 до старта. Плюс два урока фабрике — пунктами очереди 5 и 6 с `ДОМ: …/UROKI-FABRIKE.md`, как велит шапка секции УРОКИ ФАБРИКЕ (не строкой прямо в неё).

**Результат верификатора.** Не запускался: обе его работы (что исчезло из тел · какие «перенесено» натянуты) предполагают изменённые тела, а тела не менялись. `git show HEAD:skills/…` сверять не с чем.

**Открытое «возвращаться».** Всё задание целиком: 72 позиции непокрытого и пять тел по четырём источникам. Разблокировка — ответ на вопрос №1; после него заход исполним как написан, с одной поправкой (вопрос №2). План на пять шагов лежит в `## ПЛАН`, его перерабатывать не нужно.

**НЕОБРАТИМОЕ: необратимого нет.** Ни одной мутации: ни `checkout`, ни `merge`, ни `commit`, ни удаления, ни перезаписи, ни worktree. Единственное изменение на диске — добавление секций ПЛАН/ВОПРОСЫ/ОТЧЁТ в этот файл; откат — `git checkout -- <этот файл>` в `materials`, содержательного текста захода правка не касалась.

**Время прогона + токены** — снимает приёмка из `result`-строки лога.

*(Здесь в первой редакции стояли «артефакта нет» и «хэша нет» — обе строки были верны на момент остановки и отменены разблокировкой. Действующие строки АРТЕФАКТ и КОММИТ — выше, в основном отчёте.)*

---

## ПРИЁМКА — заполняет аналитик, 2026-08-07

**ПРИНЯТ. Это самая крупная работа дня и самая аккуратная.** Три собственных коммита (`0c797ac`, `2a860c7`, `d4848b1`), **125 файлов**, путей вне `skills/` — **ровно один**, `README.md`, по прямому указанию.

**Перепроверено приёмкой своими командами, тела и метки — до строки:**

| скилл | тело | ⚠ ДЫРА | флаг снят |
|---|---|---|---|
| `disciplina-arka` | 203 | 0 | — |
| `disciplina-git` | 470 | 3 | — |
| `disciplina-istochnik-pravdy` | 103 | 0 | — |
| `disciplina-zahody` | 405 | 1 | — |
| `illustracii` | 196 | 0 | — |
| `reserch-cikl` | 499 | 7 | ✅ снят (осталось объяснение в тексте) |
| `slajdy` | 500 | 12 | ✅ снят |

Расхождение на единицу с его числами — разный способ счёта шапки, не дефект. **Флаг `disable-model-invocation` снят с обоих**: в `reserch-cikl` уцелело только упоминание в прозе, и оно правильное — объясняет, что запрет теперь правило, а не механизм.

### Что делает этот отчёт исключительным

**Ни одной потери на 282 проверенных утверждениях.** Первый верификатор выписал их из пяти переписанных тел и проверил двойным методом; самое рискованное место — удаление списка 15 ловушек из `disciplina-git` — проверено персонально: 14 нашлись буквально, 15-я ослабла на строку и починена. Он сам назвал свои слепые зоны.

**Натяжек ноль на 18 позициях.** Второй верификатор получил список и оригиналы, но **не рассуждения**, и ловушка «имя без тела» — та самая, из-за которой гейт вхождения не прошёл никто, — в выборке не сработала ни разу.

**Источник 3 бил источник 1 двадцать один раз, и каждый раз прав оказывался артефакт.** Это ровно то, ради чего правило «практика сильнее записи» вводилось.

**Три агента умерли на обрыве интернета — и ни один не потерял работу.** Требование «добыча на диск по ходу» окупилось буквально в тот же прогон.

### Живой прогон сделан правильнее, чем я его задумал

🔴 Он выяснил, что **вызов по имени проверил бы не эту работу**: плагин установлен с GitHub, кэш побайтово равен `main`, а его правки живут в worktree и в вызов не попадают по построению. Поэтому прогон сделан **исполнением дисциплины новых тел на настоящих объектах** — и обе проверки сломались там, где текст читался гладко:

- правило «файл из указателя существует» **неисполнимо**: документы не объявляют базу пути, 22 и 24 ложных «пропажи», настоящая утонула → урок 52;
- процедура коммита из `disciplina-git` **не работает вне `materials`**: зовёт `_generator/`, которого в плагине нет → урок 53. Скилл, вынесенный ради переносимости, в чужом репозитории падал на третьей строке.

Обе находки стоят больше, чем зелёный вызов по имени, который я просил.

### Числа переноса

**72 из 72, пустых клеток ноль.** Перенесено 68 · осознанно не берём 3 · требует решения владельца 3. Отказы обоснованы вашим же правилом: шкала 0–100 и порог 70 — числа назначенные, а не замеренные, заменены тремя бинарными запретами.

**Дыра `the-interviewer` объявлена в ТЕЛЕ `disciplina-zahody`**, а не только в отчёте — потому что скилл читают без отчёта. Это точнее, чем я просил.

**Метки: было 17, стало 23** — заведено шесть новых, закрыто ноль. Верно: перенос вскрывает места, где у нас не было ничего.

### 🔴 Что теперь можно и чего пока нельзя

**Гасить внешние скиллы можно** — с оговоркой: перенос сделан в **ветке**, а установленный плагин смотрит на `main`. До влития ветки гашение оставит систему без обеих копий.

**Три позиции ждут вас** (все в `slajdy/DYRY.md`): удалять ли `frontend-slides` · считать ли `karpathy-guidelines` зависимостью · **сценарий 1 в `evals.json` стартует без ленты, что противоречит вашему решению 3**.

**Семь конфликтов источников не разрешены — это ваша работа, не его.** Самый острый: запись говорит «дефолт Sonnet», а живая практика этой арки — Sonnet 13 против Opus 16.

### Мой дефект этой приёмки — урок 54

Команда `pip install` ушла вам без `cd`, выполнена из домашней папки и упала. Правило «команда владельцу — абсолютным путём» в каноне есть, цена названа трижды, и нарушил его тот, кто цитирует это правило исполнителям. Установка пойдёт пунктом ближайшего захода, а не строкой в чат.

### 🔴 Три позиции закрыты решениями 07.08 — вход следующего захода

**1. Конфликт `evals.json` снят, и решение 3 стало точнее.** Владелец: *«сборка слайдов может стартовать вообще без всего, но тогда создание ленты должно быть первым шагом, по сути вызовом скилла ресёрча»*.

То есть сценарий 1 законен, а формулировка «лента — первый и обязательный вход» была слишком жёсткой: скилл **не отказывается** работать без ленты — он **первым шагом зовёт `reserch-cikl` по имени**. Отказ остаётся только на попытку идти в раскадровку без неё.

Правка: `slajdy/SKILL.md §0` — «лента обязательна ДО раскадровки; отсутствует → шаг 0 есть вызов `reserch-cikl`, а не остановка». `evals.json` при этом верен и правки не требует.

**2. `frontend-slides` не удалять — удалять нечего.** Скилла нет в установленных вовсе (проверено приёмкой `ls` по каталогу: там только `karpathy-guidelines`). В наших файлах он живёт как **провенанс**: `references/animation-patterns.md` помечен «absorbed from `frontend-slides`». Ссылка объясняет, откуда правило, — оставить.

**3. `karpathy-guidelines` — не зависимость.** Его содержание вшито в `_TEMPLATE-zahod.md` разделом «§1 ДИСЦИПЛИНА (Карпатов)» и доезжает до исполнителя в каждом заходе независимо от внешнего плагина. Упоминание в `SKILL.md:122` — отсылка к норме, а не требование установленного скилла. Объявить зависимостью значило бы подвесить семь скиллов на чужой плагин ради того, что у нас и так продублировано.
