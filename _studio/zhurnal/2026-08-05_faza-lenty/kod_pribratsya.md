# Канал исполнителя — pribratsya (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — три механических хода по готовым адресам: влить, пересобрать, проверить. Придумывать нечего.

КОНТЕКСТ. `materials/` — монорепо, идёт разделение фабрики на четыре плюс мета. Иллюстрации вынесены в `_illustracii/`, хвосты подобраны заходом `hvosty-illustracii` (принят 2026-08-06, все 8 гейтов зелёные).

ЦЕЛЬ: привести дерево в чистое состояние — влить готовое, докоммитить, пересобрать то, что устарело от правки движка, и убедиться, что ничего не сломано.

🔴 **РЯДОМ ИДУТ ДВА ЖИВЫХ ЗАХОДА** — `karta-incidentov` (worktree `karta`) и `vynos-meta` (worktree `meta`). Их ветки НЕ ТРОГАЙ, их рабочие папки НЕ ТРОГАЙ, `git checkout` не делай. Ты работаешь в основной папке на `arka/mat-kostyak` и вливаешь ровно одну названную ветку.

Приёмка — по ОТЧЁТУ. Если стоп до цели: чистое дерево важнее пересборки — сделай §A целиком, остальное назови недоделанным.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_studio/` `catalan/` `teorkat-vvedenie/` `teorver-vvedenie/` `obzory/` `kurs-puti-i-volny/` `_studio/zhurnal/2026-08-05_faza-lenty/kod_pribratsya.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_studio/` `catalan/` `teorkat-vvedenie/` `teorver-vvedenie/` `obzory/` `kurs-puti-i-volny/` `_studio/zhurnal/2026-08-05_faza-lenty/kod_pribratsya.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `_studio/zhurnal/2026-08-05_faza-lenty/kod_hvosty-illustracii.md` §ОТЧЁТ и §ВОПРОСЫ — там названы оба хвоста, которые ты закрываешь. Проект сверх этого не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### A. Влить и закоммитить

```
python3 _generator/tools/git_zona.py doctor
python3 _generator/tools/git_zona.py merge zahod/hvosty-illustracii
python3 _generator/tools/git_zona.py plan
python3 _generator/tools/git_zona.py commit --push
```

⚠ `plan` собирает черновик с плейсхолдерами `== <что и зачем — переписать>` — **перепиши их сам**, по-человечески. `commit` на неотредактированных откажет, и это правильный отказ.

⚠ Дерево движется: рядом идут два захода и, возможно, сессии владельца. Состав путей разошёлся с черновиком → `plan --force` и переписать сообщения заново. Это не глюк.

🔴 **Вливаешь ТОЛЬКО `zahod/hvosty-illustracii`.** Ветки `zahod/karta-incidentov` и `zahod/vynos-meta` — живые, их вливает приёмка после отчётов. Влить их сейчас — притащить недоделанное.

### B. Пересобрать то, что устарело от правки движка

Заход `hvosty-illustracii` поправил CSS-комментарий в `build_doc.py`. Собранные ранее `.html` несут старый текст — **их чинит пересборка источника, а не правка руками**.

Найди их сам, не по списку:
```bash
grep -rl "s-node-a" --include='*.html' . | grep -v '^./.git'
```

Для каждого найденного: **найди его ИСТОЧНИК** (папку с `.md`, из которой он собран) и пересобери `python3 _generator/build_doc.py <папка-источника>`. Известные адреса вызова, чтобы понять форму: `build_doc.py teorkat-vvedenie/MAT-KOSTYAK`, `build_doc.py teorkat-vvedenie/raskadrovka/teksty`, `build_doc.py teorkat-vvedenie/KONSPEKT-L1`.

🔴 **Источник не найден — НЕ ТРОГАЙ файл и назови его в отчёте.** Собранный `.html` без источника руками не правится: правка уйдёт при первой же настоящей пересборке, а выглядеть будет как починка. Таких, скорее всего, немного (например, снимки `vidy/` в журнале арки — они снимки и пересборке не подлежат).

⚠ После каждой пересборки — гейт вида: `python3 _generator/tools/check_view.py <источник>`. Красный — не коммить этот файл, назвать в отчёте.

### C. Проверить, что ничего не сломано

Три прогона, вывод каждого в отчёт:
```
sh .githooks/pre-commit
python3 _generator/tools/check_kartoteka.py teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md --gates
python3 _generator/build_deck.py buffon/src
```

⚠ Известное на 2026-08-06, чтобы ты не принял чужой долг за свою поломку: ворота дают **7 сирот** в арке `2026-07-28_konspekt-l1` — унаследованный долг, не твой. `dandelin/src` **не собирается и до всех этих заходов** (6 ошибок линтера про русский текст внутри рисунков) — тоже чужой долг, не чини.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. 🔴 `git_zona.py check` печатает «вне git ничего нет».
2. 🔴 `git_zona.py poteri` печатает «потерь нет» с охватом; ветки `karta-incidentov` и `vynos-meta` в списке живых и НЕ тронуты.
3. Греп `s-node-a` по `.html`: каждый оставшийся файл либо пересобран, либо назван в отчёте с причиной «источник не найден». **Вердикт с охватом: пересобрано X, оставлено Y, проверено X+Y.**
4. `sh .githooks/pre-commit` → `rc=0`.
5. 🔴 **ЖИВОЙ ПРОГОН на реальном объекте:** `build_deck.py buffon/src` собирается (`rc=0`) — правки движка ничего не сломали.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «всё пересобрано», а «пересобрано X из Y».

## 3. ВЕРИФИКАТОР — не нужен, и вот почему

Заход не двигает файлы и ничего не теряет: слияние обратимо (ветка-источник цела), пересборка воспроизводима из источника. Критерий §2 закрывается командами, каждая печатает охват.

**Одно исключение.** Если при пересборке какой-то `.html` изменился СИЛЬНО (не одна строка комментария, а куски содержания) — это значит, что источник и собранный вид давно разошлись, и пересборка не починка, а подмена. Тогда: файл не коммитить, различие показать `git diff --stat` и назвать в `## ВОПРОСЫ`.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/` `catalan/` `teorkat-vvedenie/` `teorver-vvedenie/` `obzory/` `kurs-puti-i-volny/` `_studio/zhurnal/2026-08-05_faza-lenty/kod_pribratsya.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/` `catalan/` `teorkat-vvedenie/` `teorver-vvedenie/` `obzory/` `kurs-puti-i-volny/` `_studio/zhurnal/2026-08-05_faza-lenty/kod_pribratsya.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)
1. Прочитан `kod_hvosty-illustracii.md` §ОТЧЁТ/§ВОПРОСЫ: хвост B закрыт (33 `.html` со старым CSS-комментарием `build_doc.py:1453`, список источников уже назван), хвост «долг вне зоны» — не мой (в очереди ДОМ: `OTKRYTYE-ZADACHI.md`, не трогаю).
2. §A: `git_zona.py doctor` → `merge zahod/hvosty-illustracii` → `plan` (переписать плейсхолдеры) → `commit --push`. Вливаю ТОЛЬКО этот заход, `karta-incidentov`/`vynos-meta` не трогаю.
3. §B: грепом `s-node-a` найти оставшиеся `.html`, для каждого — найти источник и пересобрать `build_doc.py <источник>`; без источника — не трогать, назвать в отчёте. После каждой пересборки — `check_view.py <источник>`.
4. §C: три прогона проверки (`pre-commit`, `check_kartoteka.py --gates`, `build_deck.py buffon/src`), вывод каждого в отчёт. Известный чужой долг (7 сирот в `2026-07-28_konspekt-l1`, `dandelin/src` не собирается) не чиню.
5. Финальный коммит зоны (§4): `add` → `commit -- <пути>` → `git_zona.py check --zone` → `show --stat`.
6. Отчёт в `## ОТЧЁТ`.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **12 `view.html` из §B-охвата разошлись с источником СИЛЬНО (исключение §3) — источник и собранный вид не синхронизировались задолго до этого захода.** Пересборка `build_doc.py <src>` дала не строку, а сотни изменённых строк; коммитить как «починку CSS-комментария» было бы подменой. Не закоммичено, откачено `git checkout --` к HEAD. Дата последнего коммита каждого `view.html` (для масштаба разрыва) и `git diff --stat` пересборки:
   - `_studio/zhurnal/2026-07-16_teorkat-programma-dizajn/annotacii/view.html` (коммит от 2026-07-17) — 509 строк
   - `_studio/zhurnal/2026-07-20_teorkat-motivacia/IZO/view.html` (2026-07-28) — 184 строки
   - `_studio/zhurnal/2026-07-20_teorkat-motivacia/VIDY/view.html` (2026-07-21) — 1020 строк
   - `_studio/zhurnal/2026-07-21_mat-kostyak/obrazec-format/view.html` (2026-07-23) — 371 строка
   - `catalan/kurs/L6-konspekt/view.html` (2026-07-20) — 430 строк
   - `catalan/kurs/L7-konspekt/view.html` (2026-07-20) — 413 строк
   - `catalan/kurs/razbor-cf-narayana/view.html` (2026-07-21) — 420 строк, **и вдобавок красный `check_view.py`** (`ХВОСТ БЛОКА НЕ ВИДЕН В HTML: razbor.md, .Шаг2:сколькословимеетровноjстыков`)
   - `teorkat-vvedenie/DOKAZATELSTVA-L1/view.html` (2026-07-29) — 204 строки
   - `teorkat-vvedenie/KONSPEKT-L1/view.html` (2026-07-29) — 140 строк
   - `teorkat-vvedenie/MAT-KOSTYAK/view.html` (2026-07-23) — 844 строки
   - `teorkat-vvedenie/raskadrovka/teksty/view.html` (2026-07-31) — 314 строк
   - `teorver-vvedenie/plan/src/view.html` (2026-08-02) — 132 строки

   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. **2 файла того же грепа (`teorkat-vvedenie/src/{view.html,dist/index.html}`) — не конвейер `build_doc.py`, а `build_deck.py` (слайды).** Их устаревание не связано с CSS-комментарием, который чинит этот заход (текст `.s-node-a — акцентный узел…` нигде, кроме `_generator/build_doc.py`, не встречается — источники этого дека его никогда не несли в таком виде). Уже названо как открытый вопрос владельцу в `kod_hvosty-illustracii.md` §ВОПРОСЫ п.2 — этот пункт только подтверждает, что вопрос остаётся открытым, не дублирую.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

### Что сделано + зачем

**§A. Влить и закоммитить.** `git_zona.py doctor` показал заход `hvosty-illustracii` завершённым (HEAD его worktree = его последний коммит `5dc10fe`, рабочая папка чиста — живой сессии там нет, вопреки родовому предупреждению doctor о вычекаученных ветках). `merge zahod/hvosty-illustracii` — без конфликтов, 16 путей. `plan` дал два черновика с плейсхолдерами (в мою зону, помимо слияния, попали ещё 13 незакоммиченных путей — 6 в `_studio/` и 7 в `kurs-puti-i-volny/`, накопленные ДО этого захода): переписал сообщения по содержанию (журнал арки faza-lenty — регистрация KARTA §6 + сами файлы-заходы; черновик плана курса kurs-puti-i-volny — авторская работа параллельной сессии владельца, не тронута по содержанию). `commit --push` → 2 коммита (`335f283`, `8f51ec1`), push прошёл.

**§B. Пересобрать устаревшее.** Греп `s-node-a` по `*.html` (весь репозиторий) дал **42** файла. Разобрал по зоне и форме источника:
- **В зоне, канонический адрес `build_doc.py` (`<src>/view.html`), источник найден — 24 файла.** Пересобрал все 24: `python3 _generator/build_doc.py <src>` → `check_view.py <src>` на каждом.
  - **11 закоммичено** (`66e7265`) — дифф ровно 1 строка (комментарий `.s-node-a` в CSS-шаблоне), `check_view` зелёный на всех: `_studio/konvejer/06-tekst/ETALON-vid`, `kurs-puti-i-volny/obrazec/src`, `obzory/{dika-v-vysshey-matematike,goodstein-ordinaly-gidra,karri-hovard-lambek,kombinatorika-okruzhnosti,monady-i-yoneda,puti-ogranichennoy-vysoty,sluchajnye-bluzhdaniya-na-gruppah,sofichnost-priblizhenie-grupp-konechnymi}/src`, `teorkat-vvedenie/LENTA-L2`.
  - **1 уже был актуален** — `kurs-puti-i-volny/plan/src/view.html`: пересборка дала нулевой дифф (владелец уже пересобрал его свежим `build_doc.py` в параллельной сессии до меня).
  - **12 НЕ закоммичено — исключение §3 (сильное расхождение, откачены `git checkout --` к HEAD).** При пересборке дифф вышел не в строку, а в сотни: источник и собранный вид разошлись задолго до этого захода (собранные `view.html` датированы 17.07–02.08, старше многих правок источника). Список с `git diff --stat` (до отката) и датой последнего коммита файла — в `## ВОПРОСЫ` п.3. Один из них (`catalan/kurs/razbor-cf-narayana`) вдобавок дал КРАСНЫЙ `check_view` (`ХВОСТ БЛОКА НЕ ВИДЕН В HTML: razbor.md`), т.е. не прошёл бы гейт §B даже без исключения §3.
- **В зоне, но НЕ в форме `build_doc.py` — 9 файлов, не тронуты.** Источник по духу есть, но вызов `build_doc.py <src>` к нему не применим:
  - `teorkat-vvedenie/src/{view.html,dist/index.html}` — это скелет **`build_deck.py`** (слайды: `content/*.md`, `slides/*.html`), не документ; сам заход называет только `build_doc.py`. Проверил происхождение строки: текста `.s-node-a — акцентный узел…` нет нигде в репозитории, КРОМЕ `_generator/build_doc.py` — значит устаревание этих двух файлов не связано с правкой, которую чинит этот заход (в `base.css`/`overlay.css` этого источника комментария никогда не было ровно в таком виде). Уже названо как открытый вопрос владельцу в `kod_hvosty-illustracii.md` §ВОПРОСЫ п.2 — не переоткрываю, только подтверждаю статус-кво.
  - `catalan/kurs/{lekciya-1,lekciya-vetvyashchiesya,perestanovki-otchet}.html` — имя не `view.html`, конвейер `build_doc.py` пишет только `<src>/view.html` по умолчанию; это не его канонический выход.
  - `_studio/zhurnal/2026-08-05_faza-lenty/vidy/{DOKAZATELSTVA-L1--DO-pravki,DOKAZATELSTVA-L1--POSLE-pravki,KONSPEKT-L1,LENTA-L2}.html` — снимки журнала арки, `.md`-источника в папке нет физически (проверено `ls`), пересборке не подлежат (заход сам это предсказал в §B).
- **Вне зоны — 9 файлов, не тронуты, не мои:** `_generator/doc-demo/view.html` · `krivaya-drakona/lekciya/build/view.html` · `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/{kotly,raskadrovka/teksty,razbor-bijekcii}/view.html` · `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/{dist/index.html,shablon.html}` · `sayt/static/teorkat/lekciya-1.html` · `teorkat/lekciya-1.html` (не `teorkat-vvedenie/` — другой проект).

**Вердикт §B (охват):** пересобрано и закоммичено X=**11**, уже было актуально=**1**, оставлено с причиной Y=**30** (12 сильно разошедшихся + 9 не в форме `build_doc.py` + 9 вне зоны), проверено X+Y = 11+1+30 = **42** = весь охват грепа.

**§C. Три прогона проверки.**
1. `sh .githooks/pre-commit` → `rc=0`, единственная строка вывода: `✅ вес: в индекс не лезет ничего тяжелее 5.0 МБ` (остальные гейты хука молчат — «чисто», по правилу хука).
2. `check_kartoteka.py teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md --gates` → `rc=1`, ворота 1–4 зелёные, ворота 5 дают ровно **7** сирот, все в `_studio/zhurnal/2026-07-28_konspekt-l1/` + один в `_SHTAB-teorkat-vvedenie/zahody/` — **дословно совпадает с названным в задаче чужим долгом**, не мой, не чинил.
3. `python3 _generator/build_deck.py buffon/src` → `rc=0`, `dist/index.html` (2755 КБ), только 2 мягких предупреждения линтера (сироты-ассеты, fillText-подпись) — не блокеры. `git status` на `buffon/` после сборки — пусто, вывод байт-в-байт совпал с уже закоммиченным (правки движка ничего не сломали в реальном объекте).

### Как проверил
Каждый гейт запущен вживую, вывод — в этом отчёте с `rc=` первым (Карпатов). `check_view.py` — по каждому из 24 пересобранных источников индивидуально (не только по 11 закоммиченным).

### Что НЕ трогал
- Ветки/рабочие папки `karta-incidentov` и `vynos-meta` — не читал, не мержил, не заходил.
- `_studio/zhurnal/2026-08-05_faza-lenty/kod_vynos-meta.md` — обнаружил его правленым (`M`, +80 строк) уже ПОСЛЕ слияния и докоммита §A, при финальной проверке зоны перед §4. `stat` показал mtime на 1 минуту раньше проверки — живая правка прямо во время этого захода (не через worktree `meta`, а в основной папке: похоже на сессию владельца/аналитика поверх этого же документа). Не входит в задачу этого захода (он не про `_meta`), содержимое не читал по существу. Исключил его из `add`/`commit` §4 точечным списком путей вместо широкого `_studio/`, чтобы не утащить чужую незавершённую правку в свой коммит — отклонение от буквальной формы команд §4 (там `add -- _studio/ …` целиком), сделано намеренно по духу контракта («рядом идут… возможно, сессии владельца», «дерево движется»). Файл остался незакоммиченным в рабочем дереве — не потерян, ждёт своего автора.
- 9 файлов вне зоны из §B (список выше) — по правилу зоны.
- Известный чужой долг ворот 5 (7 сирот `2026-07-28_konspekt-l1`) и `dandelin/src` (не проверял — задача явно сказала не чинить, не гонял отдельно).

### Открытое «возвращаться»
12 сильно разошедшихся `view.html` (список в `## ВОПРОСЫ` п.3) и 2 файла `teorkat-vvedenie/src/{view.html,dist/index.html}` (другой конвейер) — решение о пересборке/приоритете за владельцем, как и было названо в предыдущем заходе.

### Результат верификатора
§3 захода прямо разрешает не звать отдельного верификатора: слияние обратимо (ветка-источник цела), пересборка воспроизводима из источника, критерий §2 закрывается командами с печатью охвата. Не звал.

### Необратимое
- 12 файлов (список — `## ВОПРОСЫ` п.3) были пересобраны, дали дифф в сотни строк (исключение §3), и тем же ходом откачены `git checkout -- <путь>` к состоянию HEAD — восстанавливается ПОВТОРНЫМ запуском `python3 _generator/build_doc.py <src>` в любой момент (детерминированная пересборка, ничего не терялось: сгенерированный вариант не был нигде сохранён и не коммитился, только прогнан и отброшен в рамках одной проверки).
- Больше необратимого нет.

**АРТЕФАКТ:** артефакта нет: заход пересобирает документы и коммитит зону, ничего нового «на посмотреть» не производит; 11 пересобранных `.html` — служебная починка комментария в уже существующих видах, не новый продукт (адреса всех 11 — в списке §B выше, абсолютный путь к любому: `/Users/ivanyakovlev/Documents/GitHub/materials/<путь из списка>`).
**КОММИТ:** три коммита на `arka/mat-kostyak`, все запушены —
`335f283` — merge+докоммит журнала faza-lenty и черновика kurs-puti-i-volny (часть §A) ·
`8f51ec1` — докоммит kurs-puti-i-volny (часть §A) ·
`66e7265` — пересборка 11 `view.html` (§B) ·
`git_zona.py check --zone "_studio/ catalan/ teorkat-vvedenie/ teorver-vvedenie/ obzory/ kurs-puti-i-volny/"` → ✅.
Целостный `git_zona.py check` (без `--zone`, вся зона) → `rc=1`, единственная причина — незакоммиченный `kod_vynos-meta.md` (чужая живая правка, см. «Что НЕ трогал»); это ожидаемое, объяснённое отклонение от критерия готовности 1, не брак этого захода.
