# Канал исполнителя — dver-urokov (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — <одна фраза почему (Sonnet — механика расписана; Opus — план рождается по ходу)>.

КОНТЕКСТ. <проект в 1–2 фразы>. Прошлый этап: <состояние>. ЦЕЛЬ: <что закрыть>.
Приёмка — по ОТЧЁТУ, без построчной сверки. <Если стоп до цели: получишь X, но НЕ Y.>

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/wt-dver`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/dver-urokov`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add wt-dver --branch zahod/dver-urokov
  cd ../materials-wt/wt-dver
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/korni.py` `_generator/tools/register_doc.py` `_generator/tools/dostavit_urok.py` `_generator/tools/fixtures/urok`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/dver-urokov`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: зона в HEAD чиста (`dostavit_urok.py` и `fixtures/urok` ты только заводишь) — фабриковать коммит не надо. Правишь два существующих файла: их состояние до тебя достаётся `git show HEAD:<путь>`.
- **Прочитать ТОЛЬКО это:**
  1. `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md` — **живой груз, 61 урок.** Это твой материал; форму двери выводи из него, а не из головы. Целиком.
  2. `_generator/tools/korni.py` — функции `отн_путь`, `префиксы_двери`, `корень_пути`, `дверь_принимает` и константа `REPO` (строка 42).
  3. `_generator/tools/register_doc.py` — целиком, он небольшой.
  4. `_generator/tools/check_uroki.py` — **уже существующий гейт про уроки**, проверяет форму (`ЦЕНА:`). Твоя дверь обязана с ним ужиться, а не продублировать его.
  5. `_generator/tools/zakryt_sessiyu.py` — только место, где печатается `🔴 Дальше — РУКАМИ` (грепом). Это точка, куда дверь встраивается.
  6. `~/Documents/GitHub/disciplina/skills/` — только `ls`, чтобы увидеть семь папок-адресатов. Внутрь скиллов не ходи.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Одна дверь, две стороны. §2.1–2.3 — транспорт уроков. §2.4 — починка регистрации, тот же файл и та же болезнь.

### 2.1 Прочитать груз и вывести форму из него

61 урок лежит в `UROKI-FABRIKE.md`. Форма записи: `### <что произошло>` / `ЦЕНА: …` / `ВЕРДИКТ: …` / иногда `ДОСТАВЛЕНО: …`.

🔴 **Адрес дома УЖЕ есть почти в каждом вердикте** — строкой вида «Дом — `<путь>`» в конце. Замерь это первым делом, командой, и скажи оба числа: у скольких из 61 адрес разбирается автоматически и у скольких нет. **От этого числа зависит вся конструкция**, и если оно окажется низким, скажи об этом в `## ПЛАН` до того, как писать код.

Дома бывают трёх видов, и они ведут себя по-разному:

| вид дома | пример | что значит доставка |
|---|---|---|
| **канон** | `_studio/docs/kak-delat/GIT-disciplina.md` | правило дописывается в существующий документ |
| **инструмент** | `_generator/tools/check_sborki.py` | это ЗАДАЧА на будущее, а не текст: код сам себя не допишет |
| **скилл** | `disciplina/skills/slajdy/…` | другой репозиторий, и там задача на будущее |

### 2.2 🔴 `DOLG.md` в папке каждого скилла — то, ради чего владелец это просил

Дословно: *«у каждого скилла есть своя папка, где можно прописать задание на будущее, очень чёткое… если я вернусь завтра чинить какой-то из скиллов, чтобы было понятно, как это делать»*.

Заведи в каждой из семи папок `disciplina/skills/<имя>/DOLG.md` — **дом невыполненной работы по этому скиллу**. Форма — твоя, но она обязана отвечать на три вопроса, иначе долг бесполезен:

1. **что не так** — дословный урок с ЦЕНОЙ, не пересказ;
2. **чем это чинится** — вердикт, тоже дословно;
3. **откуда это взялось** — арка и дата, чтобы через месяц было видно, не устарело ли.

Пустой `DOLG.md` — законный и хороший исход для скилла, к которому претензий нет. Заводи его всё равно: пустой файл на месте говорит «проверено, чисто», а отсутствующий — «неизвестно».

⚠ **`disciplina` — ЧУЖОЙ репозиторий.** Пишешь туда только через форму с `GIT_ZONA_REPO` (цена голого пути — урок 53). Коммитить его ты **не будешь**: зона захода в `materials`. Файлы заведи, а незакоммиченность назови в отчёте — их подберёт заход закрытия арки.

### 2.3 Инструмент `dostavit_urok.py` — три режима, не больше

```
python3 _generator/tools/dostavit_urok.py --plan <UROKI-FABRIKE.md>
python3 _generator/tools/dostavit_urok.py --dostavit N <UROKI-FABRIKE.md>
python3 _generator/tools/dostavit_urok.py --gejt <UROKI-FABRIKE.md>
```

- **`--plan`** — таблица: № · урок · дом · вид дома · доставлен? · адрес разобран или нет. Ничего не меняет. Это то, что читает человек перед закрытием арки.
- **`--dostavit N`** — дописывает урок N в его дом **дословно** и проставляет `ДОСТАВЛЕНО: <дата>` в самом уроке. 🔴 **Идемпотентен:** повторный вызов дубля не заводит — проверяется наличием той же строки-метки на месте, как это уже делает Г7 в `priyomka.py`. Дом не существует → **отказ с названной причиной**, а не создание файла наугад.
- **`--gejt`** — красный, если есть уроки без `ДОСТАВЛЕНО`. Это то, что не даст закрыть арку молча.

🔴 **Метка и содержание ставятся ОДНИМ ходом, никогда порознь.** Метка без содержания рядом даёт ложно-зелёный гейт — это уже записано в каноне про Г7, и здесь ровно тот же капкан.

**Не изобретай там, где есть готовое:** `check_uroki.py` уже разбирает форму урока и терпит жирный `**ЦЕНА:**`. Пользуйся им или его разбором; второй парсер той же вещи разойдётся с первым — это в этом репозитории уже случалось.

**Встрой вызов `--plan` в `zakryt_sessiyu.py`** ровно туда, где сейчас печатается `🔴 Дальше — РУКАМИ`. Замени печать на вызов; остальное поведение файла не трогай.

### 2.4 Починить дверь регистрации — два дефекта, оба воспроизведены

**Д1 — worktree.** `отн_путь()` резолвит от константы `REPO`. Чинится тем, что корень берётся **у файла, а не у процесса**: `git rev-parse --show-toplevel` от папки файла. Тогда путь внутри worktree того же репозитория принимается.

**Д2 — чужой репозиторий.** `префиксы_двери()` отдаёт корни `materials` при любом `GIT_ZONA_REPO`. Таблица корней обязана переключаться вместе с `REPO`.

🔴 **`korni.py` — общий файл, его импортируют все инструменты.** Правь минимально и хирургически; после правки прогони по разу **каждый** инструмент, который его импортирует (список снимаешь грепом, число назови), — иначе починка одной двери сломает пять соседних. Это ровно та цена, которую платит любая правка общего корня.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Шесть условий:

1. **`--plan` на живом файле 61 урока** отрабатывает, `rc=0`, таблица печатается целиком. Два числа: адрес дома разобран у X из 61, не разобран у Y. Вывод — дословно, не пересказом.
2. **`--dostavit` доказан на живом уроке, не на фикстуре:** доставь **один** урок, чей дом — существующий файл канона, и предъяви три факта: строка появилась в доме (`grep`), метка `ДОСТАВЛЕНО` появилась в уроке, **повторный вызов дубля не завёл** (`grep -c` до и после = одно и то же число).
3. **`--gejt` краснеет на живом файле** (`rc=1`), потому что 60 уроков из 61 не доставлены. Зелёный гейт здесь означал бы, что он не работает.
4. **`DOLG.md` в 7 папках из 7.** Команда и вывод дословно. Непустых — сколько, названо числом.
5. **Дверь регистрации чинится и проверяется ЖИВЫМ ПРОГОНОМ на реальном объекте, а не фикстурой:** зарегистрировать документ **из worktree** (`rc=0`) и **из `disciplina`** через `GIT_ZONA_REPO` (`rc=0`). Оба вывода дословно. Тестовые записи после проверки убрать и сказать, что убрал.
6. **Соседи целы:** каждый инструмент, импортирующий `korni.py`, запущен минимум с `--help`; сколько запущено из скольких, оба числа. Упавший — назван поимённо, а не спрятан.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

⚠ **Условие 2 — сердце захода.** Всё остальное можно доделать позже; недоказанная доставка означает, что двери нет, сколько бы кода ни лежало.

## 3. ВЕРИФИКАТОР

Нужен, операция лоссовая: дверь **пишет в чужие файлы**, и её ошибка портит канон, который читают все будущие проекты.

Свежий субагент, другой метод — идёт **от дома к уроку**, не наоборот: открывает файл-дом после доставки и отвечает, (а) не потерялась ли ЦЕНА, (б) не поехала ли разметка соседних записей, (в) читается ли доставленное как часть документа, а не как вставка чужим голосом.

Плюс отдельно и обязательно: **список потерь по `korni.py` ПУСТ** — `git diff` на нём проверяется субагентом на предмет «не изменилось ли поведение там, где не просили».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/korni.py` `_generator/tools/register_doc.py` `_generator/tools/dostavit_urok.py` `_generator/tools/fixtures/urok`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/korni.py` `_generator/tools/register_doc.py` `_generator/tools/dostavit_urok.py` `_generator/tools/fixtures/urok`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Образец формы внутри ```-забора считается уроком всеми тремя грепами шапки, и они сходятся, не видя, что считают
ЦЕНА: шапка каждого `UROKI-FABRIKE.md` несёт греп-гейты `grep -c '^### '` / `'^ЦЕНА: '` / `'^ВЕРДИКТ: '`; на живой арке они дают 62/62/62 и выглядят сошедшимися. Один из этих 62 — ОБРАЗЕЦ формы внутри ```-забора: он несёт и цену, и вердикт, поэтому сходимость трёх чисел его присутствие не выдаёт по построению. Заход взял оттуда число «61 урок» и построил на нём условие 3 критерия. Стоило: ход исполнителя на разбор расхождения плюс поправка критерия; дороже другое — счётчик доставки на этом числе НИКОГДА не досчитает до 100 %, потому что один «урок» доставить нельзя, а гейт закрытия арки будет вечно красным по причине, которой нет. Тот же класс, что урок 20 (греп считал прозаическое упоминание конвенции наравне с меткой) и урок 37 (проверка краснеет на документе, который её объясняет). `check_uroki.py` от этого защищён — у него `PLACEHOLDER` ловит `### <…>`, — но живой образец начинается с `### N. <…>`, и под шаблон плейсхолдера не подпадает.

### Ключ `ДОСТАВЛЕНО:` уже занят ПРОТИВОПОЛОЖНЫМ смыслом, а заход заказал гейт ровно на нём
ЦЕНА: §2.3 велел проставлять `ДОСТАВЛЕНО: <дата>` как метку ухода урока в дом, а условие 3 обещало «60 уроков из 61 не доставлены». Живой замер: `grep -c '^ДОСТАВЛЕНО:'` → 44, из них 37 вида `<заход>#<N>` — и это метка ПРИХОДА, конвенция `## ВОПРОСЫ` того же шаблона захода («аналитик меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает эту же строку в файл по адресу ДОМ»). Ни один из 37 в канон не доставлен. Исполненный буквально, заход дал бы гейт, МОЛЧАЩИЙ на 37 уроках из 60, — то есть ровно тот ложно-зелёный, которым §2.3 сам же и пугает двумя абзацами выше. Стоило хода на разбор и переписи условия 3; не выстрелило только потому, что число «60 из 61» не сошлось с замером и заставило проверить. Класс: **одно слово-ключ в двух ролях внутри ОДНОГО шаблона захода** — соседство §2.3 и `## ВОПРОСЫ` в одном файле не помогло, а помешало: оба места выглядят каноном.

### Вынос `_meta` оставил адреса домов в уроках стоящими на файлах-указателях, и обе машинные проверки их одобряют
ЦЕНА: живой `--plan` по 61 уроку: у 20 адрес разбирается, файл существует — и при этом это редирект («`# … → ПЕРЕЕХАЛ`», содержание уехало в `_meta/disciplina/`). По обоим машинным признакам такой дом «живой», поэтому доставка прошла бы с `rc=0` и положила бы урок мимо канона; поймано только тем, что инструмент открыл первую строку дома. Итог сегодня: треть уроков арки нельзя разнести, не поправив адрес в вердикте. Цена уже уплачена вперёд — вынос был 06.08, уроки писались до и после него, и ни один вердикт адрес не обновил.

### Заход шестой раз за трое суток потребовал действия, которое сам же и запретил
ЦЕНА: §2.3 велит «встрой вызов `--plan` в `zakryt_sessiyu.py`», а контракт зоны того же файла объявляет всё вне четырёх путей READ-ONLY с пометкой «даже если мешает». Исполнимо только нарушением одной из двух красных строк. Стоило хода на разрешение конфликта и невыполненного пункта §2.3 (патч отдан в отчёте, применять приёмке). Отличие от уроков 12/14/18/22/25/34 — в цене выбора: правка вне зоны здесь не просто незаконна, а **бессмысленна физически** — в коммит она не попадёт (§4 требует `show --stat` только со своими путями), а незакоммиченная умрёт при `worktree drop`. То есть «сделать» означало «сделать и потерять», и это не оценочное суждение, а свойство рабочей папки, которую предписывает тот же контракт.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 0. Что уже сделано и чем меряно

Рабочая папка заведена, ветка сверена: `git rev-parse --abbrev-ref HEAD` → `zahod/dver-urokov`, `rc=0`.
Прочитаны: `UROKI-FABRIKE.md` целиком, `korni.py`, `register_doc.py`, `check_uroki.py`, точка встройки в `zakryt_sessiyu.py` (строка 456), `ls disciplina/skills/` → **7 папок**.

### 1. 🔴 ЗАМЕР АДРЕСОВ — сделан ДО кода, как требует §2.1

Живой файл — **в ОСНОВНОЙ папке**, не в worktree. Это не мелочь, а урок 27 в чистом виде:

```
grep -c '^### ' ~/Documents/GitHub/materials/…/UROKI-FABRIKE.md              → 61
grep -c '^### ' …/materials-wt/wt-dver/…/UROKI-FABRIKE.md                    → 54
```

Семь уроков (55–61) аналитик завёл и не закоммитил, поэтому в worktree их нет вовсе. **Все прогоны критерия гоню по абсолютному пути ОСНОВНОЙ папки** — иначе доказывал бы работу на устаревших данных.

**Адрес дома** (`Дом — …` / `вписан → …` / `Записан в …`, первый путь в бэктиках после маркера):

| | число |
|---|---|
| заголовков `^### ` | **61** |
| из них **образец шаблона** внутри ```-забора (`### N. <что произошло…>`) | **1** |
| настоящих уроков | **60** |
| адрес разбирается автоматически | **58 из 60** |
| не разбирается | **2 из 60** — уроки **6** и **9** |

Число высокое, конструкция на нём стоит. Оба промаха — не отсутствие дома, а другая одежда: урок 6 «Оба файла — `_generator/tools/git_zona.py` — лежат в зоне захода…», урок 9 «Вписать в `…/RUKOVODSTVO-zahodami.md §4`». Расширю словарь маркеров до `Вписать в` (регистр-независимо) и «— `<путь>` —»; что не разберётся — печатается в `--plan` строкой «адрес НЕ разобран», а не молча пропускается.

**🔴 «61 урок» в заходе — на единицу больше настоящего.** Шапка арки меряет `grep -c '^### '`, и её три грепа (`###`/`ЦЕНА`/`ВЕРДИКТ`) сходятся **61/61/61** именно потому, что образец внутри забора несёт и цену, и вердикт — то есть сходятся, не видя, что считают. Это класс уроков 20 и 37. Мой разбор забор пропускает; в `--plan` печатаю оба числа (61 заголовок · 60 уроков · 1 образец), чтобы расхождение с заходом было видно, а не спрятано.

### 2. 🔴 ОСПАРИВАЮ КРИТЕРИЙ 3 — ключ `ДОСТАВЛЕНО:` уже занят ДРУГИМ смыслом

Критерий 3: «`--gejt` краснеет, потому что **60 уроков из 61 не доставлены**». Предпосылка ложна, проверено командой:

```
grep -c '^ДОСТАВЛЕНО:'                     → 44
… из них '^ДОСТАВЛЕНО: нет'                →  7
… то есть с меткой вида '<заход>#<N>'      → 37
```

Эти 37 меток означают **приход**, а не уход: по конвенции `## ВОПРОСЫ` этого же захода аналитик при переносе находки ставит `ДОСТАВЛЕНО: <имя-захода>#<N>` **и в источнике, и в доме** — то есть метка говорит «эта запись приехала СЮДА из захода standart-skilla#5», а не «этот урок уехал в канон». Ни один из 37 в канон не доставлен.

Возьми `--dostavit` тот же ключ — и `--gejt` **промолчит на 37 уроках из 60**, объявив доставленным то, чего не было. Это ровно ложно-зелёный гейт, которым §2.3 сам же и пугает.

**Поправка:** ключ доставки в дом — `ДОСТАВЛЕНО-В-ДОМ:`, отдельный и не пересекающийся. Тогда:
- до работы не доставлено **60 из 60**, после одной доставки — **59 из 60**, `--gejt` даёт `rc=1`;
- старая метка прихода живёт нетронутой, её смысл не подменён;
- критерий остаётся способным провалиться (зелёный `--gejt` здесь означал бы поломку).

Критерий 3 читаю как «`--gejt` краснеет на живом файле, потому что подавляющее большинство уроков не доставлено», и число называю замером, а не из текста.

### 3. 🔴 §2.3 ТРЕБУЕТ ПРАВКИ ВНЕ ЗОНЫ — не делаю, отдаю патчем

«Встрой вызов `--plan` в `zakryt_sessiyu.py`» против контракта зоны: зона — `korni.py`, `register_doc.py`, `dostavit_urok.py`, `fixtures/urok`, и «**ЗАПРЕТ:** ничего за пределами зоны, даже если „мешает“». `zakryt_sessiyu.py` вне зоны.

Шестой раз тот же класс (уроки 12, 14, 18, 22, 25, 34). Разрешаю в пользу зоны, и вот почему это не отговорка:
- встройки **нет в критерии готовности** — там шесть условий, её среди них нет;
- правка вне зоны в коммит не попадёт (§4: `show --stat` обязан содержать ТОЛЬКО мои пути), а незакоммиченная правка в worktree **умрёт при `worktree drop`** — то есть «сделать» здесь означает «сделать и потерять»;
- заход прямо говорит: «Нашёл проблему вне зоны → в отчёт, не трогай».

Готовый патч (точная замена по якорю, одна строка печати → вызов) кладу в `## ОТЧЁТ` — применяется приёмкой одним ходом.

### 4. Что делаю по шагам

1. **`dostavit_urok.py`** — три режима, разбор уроков берётся у `check_uroki.lessons_in()` (второго парсера той же вещи не завожу, §2.3), поверх — только забор ```, адрес дома и метка. Дом резолвится двумя базами (папка арки → корень репо), потому что относительный путь в наших документах базы не объявляет (урок 52); какая база сработала — печатается. Дома нет → **отказ с названной причиной**, файл не создаётся.
2. **Метка и содержание — одним ходом.** Порядок: сначала дом, потом урок. Сорвётся второй ход — получится содержание без метки, гейт останется красным; обратный порядок дал бы метку без содержания, то есть ложно-зелёный.
3. **Живой прогон** `--plan` / `--dostavit` / `--gejt` по основной папке. Доставляю **один** урок, чей дом настоящий живой документ. ⚠ Половина адресов ведёт в `_studio/docs/kak-delat/*` — а эти файлы после выноса `_meta` стали **указателями** (`RUKOVODSTVO-zahodami.md` — 33 строки текста-редиректа, канон уехал в `_meta/disciplina/`). Доставлять урок в редирект бессмысленно, поэтому беру дом, который живой: `../../docs/sostoyanie/OTKRYTYE-ZADACHI.md` (516 строк). Расхождение «адрес урока ведёт в указатель» печатаю в `--plan` отдельной колонкой — это находка, а не мой обход.
4. **`DOLG.md` × 7** в `disciplina/skills/*/` — через форму с `GIT_ZONA_REPO`, без коммита (чужой репозиторий, зона не моя).
5. **Дверь: Д1 + Д2.** Д1 — корень берётся у файла (`git rev-parse --show-toplevel` от его папки) с проверкой «тот же репозиторий» по `--git-common-dir`; при отсутствии git — прежнее поведение от `REPO` (иначе умрут фикстуры, которые гоняют инструмент до `git init`). Индекс `KARTA.md` тогда тоже берётся из рабочей копии ФАЙЛА, а не процесса, — иначе строка регистрации уедет в чужую рабочую копию и не попадёт в коммит исполнителя. Д2 — таблица `КОРНИ` переключается вместе с `REPO`.
6. **Соседи.** `korni.py` импортируют **7 файлов** (`grep -rln "import korni" --include="*.py"`), считая сам `korni.py`. Прогоняю каждый + фикстуру `fixtures/korni/PROGNAT.sh` (она по своей строке `TOOL-CONTRACT-COVERS` покрывает как раз эти семь) + `fixtures/register_doc/PROGNAT.sh`.
7. **Верификатор** — свежий субагент от дома к уроку, плюс отдельный проход по `git diff korni.py`.

### 5. Предпосылки, которые называю вслух

- §0 велит читать `отн_путь` в `korni.py` — **этой функции там нет**, она в `register_doc.py:80`. Оба файла в зоне, работе не мешает; называю, чтобы адрес не переписали дальше по памяти.
- Доставка урока физически правит два файла ОСНОВНОЙ папки вне моей зоны (сам `UROKI-FABRIKE.md` и дом) — это не самовольство, а продукт инструмента, которого требует условие 2 и который §3 отдаёт верификатору на проверку. Оба файла назову в «НЕОБРАТИМОЕ» с командой отката.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Встройка `--plan` в `zakryt_sessiyu.py` (§2.3) НЕ сделана: файл вне контракта зоны. Готовый патч — в `## ОТЧЁТ`, применяется одной заменой.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. 20 уроков из 61 адресованы в файлы-указатели `_studio/docs/kak-delat/*` — содержание уехало в `_meta/disciplina/` 06.08, вердикты адрес не обновили. До правки адресов эти уроки не разносятся; поимённо они названы в выводе `--plan` (строка «дом — файл-указатель»). Решение «править вердикты или учить инструмент ходить по редиректу» — не моё.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
3. С `GIT_ZONA_REPO=disciplina` ворота `check_kartoteka --gates` теперь не судят НИЧЕГО (до правки они судили ровно один файл — `_studio/docs/KARTA.md` внутри `disciplina`, и это была утечка: чужой репозиторий судился правилом `materials`). Отказ объявлен строкой реестра `ворота=False`, но «молчащий гейт» в этом репо — отдельный класс боли (`KONSTITUCIYA §11а`), поэтому решение стоит принять вслух, а не заметить потом. Нашёл верификатор.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
4. §0 захода велит читать `отн_путь` в `korni.py` — этой функции там нет, она в `register_doc.py:80`. Оба файла в зоне, работе не помешало.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)
### Что сделал и ЗАЧЕМ

**1. `dostavit_urok.py` — три режима.** Уроки арки объявлены ephemeral и умирают вместе с ней; переезд был прозой, а по `KONSTITUCIYA §11` такое правило нарушается молча — и нарушалось. Теперь переезд это команда, а «не разнесли» — красный код возврата.
Разбор «что такое урок» взят у `check_uroki.lessons_in()` — второго парсера той же вещи не завёл. Поверх него ровно три вещи, которых гейт не знает: ```-забор, адрес дома из вердикта, метка доставки.

**2. Ключ разведён: `ДОСТАВЛЕНО-В-ДОМ:`, а не `ДОСТАВЛЕНО:`** — почему, разобрано в `## ПЛАН` п.2 и в уроке фабрике №2. Коротко: старый ключ значит ПРИХОД записи в файл арки и стоит у 37 уроков; гейт на нём промолчал бы на 37 из 61.

**3. Отказы вместо угадывания.** Дома нет на диске → `rc=2`, файл наугад не создаётся. Дом оказался файлом-указателем («→ ПЕРЕЕХАЛ») → `rc=2`: адрес разбирается и файл существует, то есть обе машинные проверки такой дом одобряют, а содержание там не живёт — доставка легла бы мимо канона и выглядела бы успешной.

**4. Дом резолвится двумя базами** (папка арки → корень репо) и печатает, какая сработала: относительный путь в наших документах базу не объявляет, и чекер на одной базе даёт списки почти целиком ложные (урок 52 это уже измерил).

**5. Дверь регистрации, Д1.** Корень берётся у ФАЙЛА (`git rev-parse --show-toplevel` от его папки), а не у процесса; тождество репозитория проверяется по `--git-common-dir`, git отсутствует вовсе → прежнее поведение от `REPO` (иначе умирают фикстуры, гоняющие инструмент до `git init`). Тем же ходом от базы файла считается и ИНДЕКС: считать файл от worktree, а `KARTA.md` от `REPO` — значит вписать строку в чужую рабочую копию, где она не попадёт в коммит исполнителя.

**6. Дверь регистрации, Д2.** Таблица корней переключается вместе с `REPO` (`КОРНИ_ПО_РЕПО`). Для `disciplina` заведён корень с именем `"*"` — «весь репозиторий»: его документы лежат без общей папки-имени, и правило «первый компонент == имя корня» давало там ровно один принимаемый путь.

**7. `DOLG.md` в семи папках скиллов** — собраны программно из живого файла уроков, ЦЕНА и ВЕРДИКТ дословно (сверено байт-в-байт), плюс «откуда взялось» с аркой и датой.

### Как проверил — шесть условий критерия

**1. `--plan` на живом файле — `rc=0`.** 🔴 Живой файл — в ОСНОВНОЙ папке: там 62 заголовка, в worktree 54 (семь уроков аналитик завёл и не закоммитил — урок 27 дословно). Все прогоны шли по абсолютному пути основной папки. Файл рос ПОКА я работал (61 → 62 заголовка), поэтому числа ниже — замер 2026-08-06 вечера. Хвост вывода дословно:
```
заголовков `### ` в файле: 62 · из них образец формы внутри ```-забора: 1 · настоящих уроков: 61
адрес дома разобран: 60 из 61 · НЕ разобран: 1 (уроки 6)
дом не существует на диске: 9 (уроки 31, 32, 35, 53, 57, 58, 60, 61, 62)
дом — файл-указатель, содержание переехало: 20 (уроки 2, 9, 10, 12, 15, 17, 18, 19, 22, 25, 27, 30, 33, 34, 37, 38, 42, 45, 48, 54)
НЕ доставлено в дом: 60 из 61
```
Единственный неразобранный — урок 6: адрес в нём есть, но написан оборотом «Оба файла — `путь` — лежат в зоне захода», а не «Дом — `путь`».

**2. `--dostavit` на ЖИВОМ уроке — три факта.** Доставлен урок 7, дом `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` (516 строк живого борда, не фикстура и не указатель).
```
✅ Урок 7 доставлен: 2026-08-05_faza-lenty#7
grep -n "ДОСТАВЛЕНО-В-ДОМ: 2026-08-05_faza-lenty#7" OTKRYTYE-ZADACHI.md → 527:…  rc=0
grep -n "^ДОСТАВЛЕНО-В-ДОМ:" UROKI-FABRIKE.md                          →  66:…  rc=0
grep -c "ДОСТАВЛЕНО-В-ДОМ" OTKRYTYE-ZADACHI.md  до повтора → 1 · после повтора → 1
повторный вызов: «Уже доставлен — второй раз не пишу: урок 7», rc=0
```

**3. `--gejt` краснеет на живом файле — `rc=1`,** «Не доставлено 60 из 61». 🔴 Первый мой замер дал `rc=0` — это был код `tail`, а не команды: перемерил без пайпа, `rc=1`. Ровно та подмена, о которой §1.

**4. `DOLG.md` — 7 из 7.** `ls -1 ~/Documents/GitHub/disciplina/skills/*/DOLG.md | wc -l` → **7**, папок скиллов `ls -1d …/skills/*/ | wc -l` → **7**. Непустых — **7 из 7**: пустых не вышло, потому что урок 60 («ни одно описание не написано в грамматике возражения») адресован всем семи. Долгов на скилл: arka 2 · git 2 · istochnik-pravdy 2 · zahody 1 · illustracii 1 · reserch-cikl 4 · slajdy 4.

**5. Дверь — живой прогон на реальном объекте, обе стороны, «до» и «после» дословно.**
*Из worktree* (та самая форма из урока 59 — инструмент по абсолютному пути, файл в worktree):
```
ДО  (код основной папки): ❌ Путь вне репозитория: …/materials-wt/wt-dver/…/PROBA-dveri-iz-worktree.md   rc=2
ПОСЛЕ (код починенный):   ✅ Зарегистрирован: _studio/zhurnal/2026-08-05_faza-lenty/PROBA-dveri-iz-worktree.md
                             _studio/docs/KARTA.md:286 — вписано: …                                     rc=0
```
Строка легла в KARTA.md **worktree**, основная папка не тронута (`grep` → rc=1).
*Из `disciplina`* через `GIT_ZONA_REPO`:
```
ДО  (код основной папки): ❌ Не регистрирую «skills/disciplina-arka/DOLG.md»: дверь судит только
                             `_studio/`, `obzory/`, `_illustracii/`, `_meta/`, `disciplina/`          rc=2
ПОСЛЕ:                    ✅ Зарегистрирован: skills/disciplina-git/DOLG.md
                             README.md:104 — вписано: …                                               rc=0
```
**Тестовые записи убраны, обе.** Файл-проба удалён; `_studio/docs/KARTA.md` восстановлен из бэкапа — `git diff --stat` по нему пуст. `disciplina/README.md` восстановлен — `diff -q` с бэкапом молчит, файл байт-в-байт как был.

**6. Соседи целы.** Импортёров `korni.py` — **7** (`grep -rln "import korni\|from korni" --include="*.py"`, считая сам реестр). Запущено **7 из 7** с `--help`: `rc=0` у **5**. Упали двое, поимённо: `bootstrap_arka.py` (rc=1, «--help похоже на флаг, а не имя арки») и `check_kartoteka.py` (rc=1, трейсбек). **Оба падают ТАК ЖЕ на непровленом коде основной папки** — прогнал там же для сверки; это унаследованный долг из урока 49 («8 инструментов из 22 не отвечают на `--help`»), не моя регрессия.
Сильнее `--help`: фикстуры. `fixtures/korni/PROGNAT.sh` (её строка `TOOL-CONTRACT-COVERS` покрывает как раз эти семь) → **ФИКСТУРЫ ЗЕЛЁНЫЕ, rc=0**; `fixtures/register_doc/PROGNAT.sh` → **ЗЕЛЁНЫЕ, rc=0**; `fixtures/uroki/PROGNAT.sh` → **ЗЕЛЁНЫЕ, rc=0**. Все три хук прогнал ещё раз на коммите — зелено.

**Своя фикстура — 8 ловушек, две проверены МУТАЦИЕЙ** (гейт, который не умеет падать, — театр):
```
мутация А: ключ обратно на общий `ДОСТАВЛЕНО:`  → ❌ ловушки 7, 3, 8 → ФИКСТУРЫ КРАСНЫЕ, rc=1
мутация Б: снят фильтр ```-забора               → ❌ ловушка 1      → ФИКСТУРЫ КРАСНЫЕ, rc=1
откат обеих → ФИКСТУРЫ ЗЕЛЁНЫЕ, rc=0, инструмент байт-в-байт восстановлен
```

### Верификаторы (§3) — два, оба свежие, метод другой

**А. От дома к уроку** — открывал сначала дом, потом источник. Вердикт 4 из 4 ✅:
- ЦЕНА не потерялась — сверено по sha256 сырых байтов строки (`7dce8dae…`, len 706), совпало; заголовок и ВЕРДИКТ тоже байт-в-байт;
- разметка соседей не поехала — `diff --numstat` → `11 0`, **ноль удалений**, один хунк `@@ -514,3 +514,14 @@`, выше точки вставки не изменено ничего; уровни заголовков целы, склейки абзацев нет;
- голос — инородно не читается, рамка прямо объявляет блок карантином. **Одно замечание принял и починил:** блок «немой» для борда — у вердикта есть невыполненная работа, а чекбокса нет, и по скану борда её не видно. Дописал в рамку строку «Задачами борда они не становятся сами: пункт `- [ ]` заводит человек». Правка в рамке, дословный текст урока не тронут;
- метка и содержание — обе на месте; опаснее была бы метка без содержания (зелёный гейт при пустом каноне), мы в правильном состоянии.

**Б. Список потерь по `korni.py`** — метод: обе версии реестра импортированы в один процесс, все 13 публичных функций прогнаны на 20 входах, в двух режимах `GIT_ZONA_REPO`. **СПИСОК ПОТЕРЬ ПУСТ.** В режиме `materials` — 129 из 135 совпало, все 6 расхождений суть одно заказанное следствие (строка `Корень("disciplina",…)` ушла из таблицы `materials`). Порядок корней не переставлен (`имена_воротами()` совпал точно). Самое сильное: боевой прогон `check_kartoteka.py --gates` со старым и новым реестром даёт **байт-в-байт идентичный вывод** (47 строк, 779 файлов, 8 ❌, rc=1), включая порядок ❌. Одно заказанное, но громкое следствие вынес в `## ВОПРОСЫ` п.3.

### Чего НЕ делал

- **`zakryt_sessiyu.py` не тронут** — вне контракта зоны (разбор в `## ПЛАН` п.3). Готовый патч, якорь единственный (`grep -c` → 1), строка 456:
```python
    # было:
    print(f"\n🔴 Дальше — РУКАМИ: разнести по дневнику и домам, ответить на вопросы, "
          f"поставить отсечку. Выгрузка сама ничего не закрывает.")
    # стало:
    import dostavit_urok
    uroki = arka / "UROKI-FABRIKE.md"
    if uroki.is_file():
        dostavit_urok.cmd_plan(uroki)
    print(f"\n🔴 Дальше — РУКАМИ: разнести по дневнику и домам, ответить на вопросы, "
          f"поставить отсечку. Выгрузка сама ничего не закрывает.")
```
- **Ничего вне зоны не рефакторил.** Два соседних инструмента без `--help` (урок 49) оставил как есть, хотя один из них — `korni.py` — в моей зоне: правка не следует из задания.
- **Ветку не вливал и не пушил.** `disciplina` не коммитил — зона в `materials`.

### 🔴 НЕОБРАТИМОЕ И НЕЗАКОММИЧЕННОЕ

| что · где | чем восстанавливается |
|---|---|
| **Доставлен урок 7** — блок дописан в конец `~/Documents/GitHub/materials/_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` (ОСНОВНАЯ папка, +11 строк, ноль удалений, НЕ закоммичено) | `git show HEAD:_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` |
| **Метка в уроке 7** — строка `ДОСТАВЛЕНО-В-ДОМ:` в `~/…/2026-08-05_faza-lenty/UROKI-FABRIKE.md:66` (файл был правлен аналитиком ещё до меня, НЕ закоммичен) | `git show HEAD:_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md` |
| **7 × `DOLG.md`** заведены в `~/Documents/GitHub/disciplina/skills/*/` — чужой репозиторий, НЕ закоммичены (зона захода в `materials`) | новые файлы; удаляются как новые, ничего не перезаписано |
| **Файл-проба и обе тестовые записи** — удалены мной же; `_studio/docs/KARTA.md` и `disciplina/README.md` восстановлены байт-в-байт (`diff -q` молчит) | восстановление уже выполнено |

🔴 **Оба файла из первых двух строк обязаны уехать ОДНИМ коммитом.** Разъедутся — при первом `checkout` получится метка без содержания, то есть ложно-зелёный гейт. Это назвал и верификатор, и сам инструмент печатает это на каждой доставке.

### Открытое «возвращаться»

Разнести остальные 60 уроков нельзя одним прогоном: у 20 дом — файл-указатель, у 9 дома нет на диске (4 из них в `disciplina`, 2 — `check_sborki.py` из невлитой ветки), у одного адрес не разобран. Инструмент их все называет поимённо; порядок работ — правка адресов в вердиктах, потом доставка.

**Время прогона + токены** — снимает приёмка из `result`-строки лога.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Downloads/plan-dostavki-urokov-2026-08-06.txt` — текстовый файл, любой редактор; это полная таблица `--plan` по всем 61 уроку (что где живёт и что не доехало), то, ради чего инструмент писался.
Пересобирается командой:
```
cd /Users/ivanyakovlev/Documents/GitHub/materials && python3 _generator/tools/dostavit_urok.py --plan _studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md
```
Второе, что стоит открыть глазами, — куда легла доставка: `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md`, конец файла.

**КОММИТ:** `9e57ffc` — `дверь уроков: транспорт урока в дом (--plan/--dostavit/--gejt) + починка двери регистрации в worktree и чужом репозитории` · `git_zona.py check --zone _generator/tools` → ✅ «работа доехала в git, вне git ничего нет»
`git show --stat` — ровно 4 пути, все мои: `dostavit_urok.py` (+376), `fixtures/urok/PROGNAT.sh` (+132), `korni.py` (75±), `register_doc.py` (73±). Хук `pre-commit` прошёл ЗЕЛЁНЫМ, обхода не было. Новых `.md` в `materials` не заводил — `register_doc.py` звать было не на что, `KARTA.md` в коммите не нужен.

## ПРИЁМКА — 2026-08-07, аналитик

**ПРИНЯТО.** Гейты 7 из 8; красный Г3 — артефакт лежит в `~/Downloads`, песочница туда не видит. Это дефект проверки, не работы.

**Дверь работает. Проверено моим прогоном, не по отчёту:**

| проверка | факт |
|---|---|
| `--plan` на живом файле 61 урока | `rc=0`, таблица целиком |
| `--gejt` | `rc=1` — краснеет, как и должен |
| `DOLG.md` в папках скиллов | **7 из 7**, непустых 7 |
| содержание `DOLG.md` | урок дословно, с ЦЕНОЙ и ВЕРДИКТОМ, плюс арка и дата |

**Главная находка захода — не инструмент, а то, что инструмент увидел.** `--plan` показал: **дом не существует у 9 уроков, дом оказался файлом-указателем у 20.** Двадцать девять адресов из шестидесяти одного ведут не туда. Все эти вердикты писал я, каждый выглядел точным в момент написания, и проверить их было нечем, пока переезд оставался прозой. Урок 63.

Особенно ценно, что исполнитель **отказался доставлять в файл-указатель** (`rc=2`): обе машинные проверки такой дом одобряют, доставка легла бы мимо канона и выглядела бы успешной.

**Решение развести ключи — верное и я бы его не нашёл.** Старый `ДОСТАВЛЕНО:` значит приход записи в файл арки и стоит у 37 уроков; гейт на нём промолчал бы на 37 из 61. Новый `ДОСТАВЛЕНО-В-ДОМ:` меряет другое событие.

**Дверь регистрации починена и доказана с обеих сторон** — из worktree и из `disciplina`, с выводом «до» и «после» дословно; тестовые записи убраны, `KARTA.md` и `README.md` восстановлены байт-в-байт.

**Соседи целы, и проверено сильнее, чем я просил.** Семь импортёров `korni.py`, 7 из 7 запущены; двое падают — **и падают так же на непровленом коде**, это унаследованный долг урока 49. Плюс три чужие фикстуры зелёные, плюс своя на 8 ловушек, **две из них проверены мутацией** — гейт, который не умеет краснеть, театр.

**Верификатор Б — эталонная работа:** обе версии реестра импортированы в один процесс, 13 функций на 20 входах в двух режимах; боевой `check_kartoteka.py --gates` со старым и новым реестром даёт **байт-в-байт идентичный вывод**, включая порядок ошибок. Список потерь пуст.

**Моя ошибка сборки: `zakryt_sessiyu.py` не был в зоне**, хотя §2.3 предписывал его правку. Исполнитель не тронул, объявил и приложил готовый патч с проверенным якорем. Урок 64 — линтер входа это ловит лексически.

**Вынесено дальше:** встройка патча в `zakryt_sessiyu.py`; 29 протухших адресов; урок 6, чей адрес написан оборотом «Оба файла — `путь`» вместо «Дом — `путь`».

**Уроки арки: 62 → 64.**
