# Канал исполнителя — marketplace-github (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — правок мало и они расписаны; риск не в сложности, а в непроверенных утверждениях о чужом инструменте, и на них расставлены явные проверки.

КОНТЕКСТ. Дисциплины владельца живут в переносимом плагине Claude Code — репозиторий `disciplina`, семь скиллов. 07.08 плагин установлен и работает: `claude plugin details` показал 7 из 7 скиллов.

**Но установлен он из ЛОКАЛЬНОГО ПУТИ** — `/plugin marketplace add ~/Documents/GitHub/disciplina`. Отсюда два дефекта, оба замерены аналитиком по документации 07.08:

1. **На другом компьютере плагина нет и не будет** — такого пути там не существует. Владелец работает с нескольких машин, и это блокирует отключение старых внешних скиллов: погасив их сейчас, он останется на второй машине и без старых, и без новых.
2. **Обновления не приезжают.** Правка на диске в установленный плагин не попадает сама. Хуже: в `.claude-plugin/plugin.json` **задано поле `version: "0.1.0"`**, а заданная версия перебивает SHA коммита — Claude Code видит ту же строку версии и оставляет кэш, сколько бы коммитов ни легло сверху.

У репозитория **уже есть remote**: `https://github.com/d1-d57/disciplina.git`. То есть чинится это не переездом, а сменой источника.

ЦЕЛЬ: перевести маркетплейс с локального пути на GitHub так, чтобы (а) плагин ставился на любой машине владельца, (б) обновления приезжали по `/plugin marketplace update` без ручного бумпа версии.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ.** Ты НЕ отключаешь ни одного внешнего скилла — это отдельный заход после гейта вхождения. Ты НЕ трогаешь `skills/` (там параллельно идёт этап 20). Ты НЕ можешь проверить второй компьютер — это ход владельца, ты только готовишь ему точную команду.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **РЕПОЗИТОРИЙ РАБОТЫ — НЕ ТОТ, В КОТОРОМ ЛЕЖИТ ЭТОТ ФАЙЛ.** Файл-заход в `materials`, работаешь в **`~/Documents/GitHub/disciplina`**. Инструменты лежат в `materials` и наводятся ЕДИНСТВЕННОЙ дверью — `GIT_ZONA_REPO` (`korni.py:42`). Забудешь — инструмент молча отработает по `materials`.
  ```
  export GIT_ZONA_REPO=~/Documents/GitHub/disciplina
  export MAT=~/Documents/GitHub/materials
  ```
- **МЕСТО РАБОТЫ:** **рабочая папка `../disciplina-wt/wt-market`** — worktree на ветке `zahod/marketplace-github`:
  ```
  cd ~/Documents/GitHub/disciplina
  python3 $MAT/_generator/tools/git_zona.py worktree add wt-market --branch zahod/marketplace-github
  cd ../disciplina-wt/wt-market
  ```
  🔴 **Worktree обязателен: параллельно в этом же репозитории идёт заход `kod_etap-20.md`** на ветке `zahod/etap-20`, он переписывает `skills/`. Ваши зоны не пересекаются, но `git checkout` в основной папке подменит файлы у него под ногами.
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `.claude-plugin/` и `README.md`. Всё остальное — **READ-ONLY**, и `skills/` особенно: там работает этап 20. Репозиторий `materials` — READ-ONLY целиком.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/marketplace-github`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: зона не чиста в HEAD — закоммить как есть; чиста — не фабрикуй.
- **Прочитать ТОЛЬКО это:**
  1. `.claude-plugin/marketplace.json` и `.claude-plugin/plugin.json` — что там сейчас.
  2. `README.md` §Установка — там сегодня написано, что маркетплейс зарегистрирован под именем `disciplina`, без указания источника.
  3. Живая документация: `https://code.claude.com/docs/en/plugin-marketplaces` и `https://code.claude.com/docs/en/discover-plugins`. 🔴 **Адреса на `docs.claude.com` отвечают редиректом на `code.claude.com`** — замер 06.08; иди по редиректу, читай конечный документ, похожие адреса не подставляй.
- ПЛАН — в `## ПЛАН` перед действиями.

🔴 **ЛОВУШКА, НАЗВАННАЯ ЗАРАНЕЕ.** Всё, что аналитик написал ниже про поведение `/plugin` — **вычитано из документации, а не проверено прогоном.** Формы команд и правила версионирования у чужого инструмента меняются. **Каждую команду проверяй `--help` ДО применения и сверяй с живой документацией; расхождение — в `## ВОПРОСЫ`, а не молчаливая подгонка.** Опровержение любого из трёх утверждений ниже — полноценный успех захода, а не его провал: заход существует именно чтобы это выяснить на живой системе.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Пять шагов. Первый — проверка предпосылок; если он не сойдётся, остальные не делаются.

### 2.1 Проверить три утверждения аналитика, ДО правок

| # | утверждение | как проверить | вердикт |
|---|---|---|---|
| 1 | версия из `plugin.json` **перебивает** SHA коммита, и потому обновления не приезжают | документация, раздел про разрешение версии | ? |
| 2 | у git-hosted маркетплейса версия берётся из SHA — то есть при снятом поле обновления идут сами по `/plugin marketplace update` | там же | ? |
| 3 | маркетплейс, добавленный локальным путём, на другой машине недоступен | документация про источники | ? |

Каждому — **подтверждено / неверно (как на самом деле) / в документации не нашёл**. Третий вердикт разрешён и провалом не считается. **Утверждение 1 неверно ⇒ шаг 2.3 не делается** — не снимай поле, которое ничего не ломает.

Отдельно проверить фактом, а не предположением: **репозиторий на GitHub доступен и актуален.**
```
git ls-remote origin 2>&1 | head -3          # отвечает ли удалённый и виден ли он тебе
git log --oneline origin/main -1             # что там лежит
git status -sb | head -1                     # насколько локальный main убежал вперёд
```
Удалённый недоступен или приватный без доступа — **СТОП, в `## ВОПРОСЫ`**: без работающего remote вся цель захода недостижима, и это надо сказать сразу, а не после правок.

### 2.2 Довезти репозиторий на GitHub

Ветка `main` (или как называется основная — узнай, не угадывай) должна быть на origin. Ветки заходов НЕ пушить и НЕ вливать — влитие делает приёмка.

⚠ Если в `main` не влиты `zahod/reshenia-intervyu` и `zahod/standart-skilla`, на GitHub уедет состояние без 29 решений владельца. **Это не твой ход** — назови в отчёте как условие: маркетплейс с GitHub отдаст ровно то, что там лежит.

### 2.3 Снять поле `version` из `plugin.json` (только если 2.1 подтвердил утверждения 1 и 2)

Тогда версия начнёт браться из SHA коммита, и каждый коммит станет новой версией автоматически — ручной бумп больше не нужен. В `marketplace.json` поле `version` у записи плагина — по тому же правилу; проверь, действует ли оно и там.

Если снять поле нельзя (документация требует его обязательным) — **оставь и впиши в `README.md` жирную строку: «правку выпускаешь — бумпни `version`, иначе она не приедет»**. Механизм, о котором никто не знает, хуже отсутствующего.

### 2.4 Переустановить маркетплейс с GitHub — и убедиться, что работает НЕ локальная копия

```
claude plugin marketplace list          # что сейчас зарегистрировано и откуда
claude plugin marketplace remove <локальный>
claude plugin marketplace add d1-d57/disciplina
claude plugin install disciplina@disciplina
claude plugin details disciplina@disciplina
```
🔴 **Формы команд проверь `--help` до применения.** Раньше могло быть `/plugin ...` внутри сессии, а не `claude plugin ...` в оболочке — обе формы существуют, и обе где-то верны.

🔴 **Локальный маркетплейс снять ОБЯЗАТЕЛЬНО, а не оставить рядом.** Иначе два источника одного плагина, и какой из них победит — не определено; хуже, что всё будет выглядеть работающим, пока владелец не сядет за второй компьютер.

### 2.5 Написать владельцу инструкцию для второго компьютера

Отдельным разделом в `README.md`: **точные команды, которые он выполнит на другой машине**, и что должен увидеть. Проверить это ты не можешь — второго компьютера у тебя нет; поэтому инструкция обязана быть проверяемой по шагам, с ожидаемым выводом каждой.

Тем же ходом обнови `README.md §Установка`: сегодня там сказано «маркетплейс зарегистрирован под именем `disciplina`» без источника, и это утверждение станет ложным.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Шесть условий:

1. Три утверждения §2.1 имеют вердикт; **пустых клеток 0**. Опровергнутое утверждение — законный результат, но названное следствие обязательно.
2. `git ls-remote origin` отвечает, `git log origin/main -1` печатает коммит — оба вывода в отчёте дословно.
3. `claude plugin marketplace list` показывает источник **GitHub**, и **не показывает** локальный путь. Вывод команды — в отчёт дословно, не пересказом.
4. `claude plugin details disciplina@disciplina` печатает **7 скиллов** — число снято командой, а не переписано отсюда: `ls skills/*/SKILL.md | wc -l` даёт эталон, они обязаны совпасть.
5. **Живой прогон обновления на реальном объекте, не на фикстуре:** сделай пустяковую правку в `README.md`, закоммить, запушь, прогони `claude plugin marketplace update` — и покажи, **доехала ли правка в установленную копию**. Не доехала — это и есть настоящий результат захода, назови причину. Именно этот шаг отличает «настроил» от «думаю, что настроил».
6. `git_zona.py check --zone .claude-plugin/` → ✅.

**Охват — числом.** Утверждений три, шагов пять. «Всё готово» без вердикта по каждому не принимается.

## 3. ВЕРИФИКАТОР

Не нужен: заход ничего не двигает и не теряет, правки — два json и README, всё в git.

**Но условие 5 заменяет верификатора и потому обязательно.** Заход целиком про то, работает ли механизм; отчёт «команды выполнены, ошибок нет» здесь ничего не доказывает — ровно этот класс уже стоил фабрике круга, когда вывод прошлого прогона был прочитан как свежий.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
cd ../disciplina-wt/wt-market
git --no-optional-locks add -- .claude-plugin/ README.md
git --no-optional-locks commit -m "маркетплейс: источник GitHub вместо локального пути; версия из SHA" -- .claude-plugin/ README.md
GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 $MAT/_generator/tools/git_zona.py check --zone .claude-plugin/
git --no-optional-locks show --stat
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

1. Развернуть worktree `wt-market` на ветке `zahod/marketplace-github`, подтвердить `HEAD` — сделано, зона в HEAD была чиста, фабриковать не стал.
2. Прочитать `.claude-plugin/marketplace.json`, `.claude-plugin/plugin.json`, `README.md §Установка` — как есть сейчас, без правок.
3. Прочитать живую документацию по обоим адресам (оба ушли редиректом на `code.claude.com`, как и предупреждено).
4. Проверить три утверждения аналитика по документации дословно + фактом (`git ls-remote` / `git log origin/main`).
5. Утверждения 1 и 2 подтвердились → снять `version` из `plugin.json` и из записи плагина в `marketplace.json` (не трогая `metadata.version` самого манифеста — это отдельное поле, не участвует в разрешении версии плагина).
6. Довезти `main` на GitHub (§2.2) — предварительно спросить владельца в чате: пуш в общий репозиторий — действие с видимыми последствиями, не покрывается контрактом зоны молчаливо.
7. Переустановить маркетплейс: `remove` локального → `add` GitHub → `install` → `details`, сверить число скиллов с `ls skills/*/SKILL.md | wc -l`.
8. Условие 5 критерия готовности требует «закоммить, запушь» для живого теста — но §2.2 этого же захода прямо запрещает пушить и вливать ветки заходов. Разрешил so: тест делается прямым тестовым коммитом в `main` (не в мою ветку), с явного разрешения владельца, и откатывается сразу после проверки. Подробнее — пункт очереди в `## ВОПРОСЫ`.
9. Дописать `README.md §Установка` + отдельный раздел для второго компьютера с проверяемыми шагами.
10. Закоммитить зону по §4 (`add` → `commit -- <пути>`), проверить `git_zona.py check --zone .claude-plugin/`.

Предпосылку про «если в main не влиты `zahod/reshenia-intervyu` и `zahod/standart-skilla`» проверил ДО пуша: `git branch --contains` показал обе ветки уже слитыми в `main` (через `bb2f185 Merge branch 'zahod/standart-skilla'`, которая сама содержит `dc718f7` — коммит `reshenia-intervyu`). Условие из предупреждения не сработало, ветка не «убегала» без 29 решений владельца.

## ВОПРОСЫ — (заполняет исполнитель)
1. Критерий готовности п.5 требует «сделай правку в README.md, закоммить, запушь» для живого теста обновления — но §2.2 этого же захода прямым текстом запрещает пушить и вливать ветки заходов. Для исполнителя с пустым контекстом неочевидно, где физически делать тестовый коммит: в своей ветке — запрещено правилом; в `main` — нигде не названо явно, хотя логически это единственное разрешённое место (§2.2 первым предложением требует `main` довезти на GitHub). Пришлось дважды останавливать работу и спрашивать владельца в чате (пуш `main` и отдельно тестовый коммит). Формулировку критерия стоит уточнить: явно называть `main` местом теста, а не оставлять исполнителя разрешать противоречие самому.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: marketplace-github
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

### 2.1 Три утверждения — вердикты (дословно из документации `code.claude.com/docs/en/plugin-marketplaces`)

| # | утверждение | вердикт | доказательство |
|---|---|---|---|
| 1 | версия из `plugin.json` перебивает SHA коммита, и потому обновления не приезжают | **подтверждено** | «Claude Code resolves a plugin's version from the first of these that is set: 1. `version` in the plugin's `plugin.json`; 2. `version` in the plugin's marketplace entry; 3. The git commit SHA of the plugin's source.» И явно: «Setting `version` pins the plugin. If `plugin.json` declares `"version": "1.0.0"`, pushing new commits without changing that string does nothing for existing users, because Claude Code sees the same version and keeps the cached copy.» |
| 2 | у git-hosted маркетплейса версия берётся из SHA при снятом поле — обновления идут сами | **подтверждено** | «For the git-based source types `github`, `url`, `git-subdir`, and relative paths inside a git-hosted marketplace, you can omit `version` entirely and every new commit is treated as a new version.» |
| 3 | маркетплейс, добавленный локальным путём, на другой машине недоступен | **подтверждено** (косвенно + фактом) | Документация определяет local-source как «directories or direct paths to `marketplace.json` files» — путь файловой системы, дословного предложения «недоступен на другой машине» в тексте нет (легитимный третий вердикт мог бы звучать так), но факт проверен напрямую: `claude plugin marketplace list --json` ДО правки показывал `"source": "directory", "path": "/Users/ivanyakovlev/Documents/GitHub/disciplina"` — абсолютный путь этой машины, не переносимая ссылка. |

Пустых клеток — 0.

Следствие утверждения 1 подтверждённого: шаг 2.3 (снятие поля) — делается, что и сделано.

### Факт: репозиторий на GitHub доступен и актуален (до правок)
```
$ git ls-remote origin | head -3
f6fc2e1a800b7ad3851fd124b2bda2642af8bb3f	HEAD
f6fc2e1a800b7ad3851fd124b2bda2642af8bb3f	refs/heads/main

$ git log --oneline origin/main -1
f6fc2e1 INTERVYU-utro.md: 29 пунктов, ...

$ git status -sb | head -1   (папка ~/Documents/GitHub/disciplina, main)
## main...origin/main [ahead 3]
```
Remote отвечает и виден. Локальный `main` был на 3 коммита впереди `origin/main`.

### Что сделал + зачем
1. **Проверил предпосылку §2.2** («если в main не влиты `zahod/reshenia-intervyu` и `zahod/standart-skilla`»): `git branch --contains zahod/reshenia-intervyu` и `--contains zahod/standart-skilla` в `main` показали обе ветки уже слитыми (через `bb2f185 Merge branch 'zahod/standart-skilla'`). Предупреждение не сработало.
2. **С явного разрешения владельца** запушил `main`: `git push origin main` → `f6fc2e1..bb2f185 main -> main`. Зачем: без этого GitHub-маркетплейс отдавал бы устаревшее состояние без 29 решений владельца.
3. **Снял `version`** из `.claude-plugin/plugin.json` (было `"0.1.0"`) и из записи плагина в `.claude-plugin/marketplace.json` (`plugins[0].version`, тоже было `"0.1.0"`). Не тронул `metadata.version` самого манифеста маркетплейса — отдельное необязательное поле, не участвует в разрешении версии плагина. Зачем: утверждения 1 и 2 подтверждены — версия теперь берётся из SHA коммита `main`, каждый коммит становится новой версией автоматически.
4. **Переустановил маркетплейс:**
   ```
   $ claude plugin marketplace remove disciplina
   ✔ Successfully removed marketplace: disciplina

   $ claude plugin marketplace add d1-d57/disciplina
   ✔ Successfully added marketplace: disciplina (declared in user settings)

   $ claude plugin install disciplina@disciplina
   ✔ Successfully installed plugin: disciplina@disciplina (scope: user)

   $ claude plugin marketplace list --json
   ...
   {
     "name": "disciplina",
     "source": "github",
     "repo": "d1-d57/disciplina",
     "installLocation": "/Users/ivanyakovlev/.claude/plugins/marketplaces/disciplina"
   }

   $ claude plugin details disciplina@disciplina
   disciplina 0.1.0
   ...
   Skills (7)  disciplina-arka, disciplina-git, disciplina-istochnik-pravdy, disciplina-zahody, illustracii, reserch-cikl, slajdy

   $ ls skills/*/SKILL.md | wc -l
   7
   ```
   `source: "github"`, локального пути нет. 7 скиллов совпадает с эталоном command'ой. (Число `0.1.0` в `claude plugin details` здесь — версия, ещё установленная ДО моей правки: маркетплейс клонировался из `main`, на котором версия ещё не снята, т.к. это моя зона, закоммиченная отдельной веткой и не влитая. После приёмки и влития — версия при переустановке пойдёт по SHA.)
5. **Живой прогон обновления (условие 5), с отдельного явного разрешения владельца:** тестовый коммит прямо в `main` (не в мою ветку — см. `## ВОПРОСЫ`, п.1):
   ```
   $ echo '<!-- test: живой прогон update-механизма ... -->' >> README.md
   $ git commit -m "test: живой прогон обновления маркетплейса ..." -- README.md   # 112fa48
   $ git push origin main                                                          # bb2f185..112fa48

   $ grep -c "живой прогон update-механизма" ~/.claude/plugins/marketplaces/disciplina/README.md
   0   # ДО update

   $ claude plugin marketplace update disciplina
   ✔ Successfully updated marketplace: disciplina

   $ grep -c "живой прогон update-механизма" ~/.claude/plugins/marketplaces/disciplina/README.md
   1   # ПОСЛЕ update — правка доехала
   ```
   Механизм подтверждён живьём, не на фикстуре: `claude plugin marketplace update` действительно подтягивает новый коммит из `origin/main` в установленную копию. Тестовый коммит сразу отменён: `git revert --no-edit 112fa48` → `c586f9f`, запушен. `main` вернулся к содержанию без тестовой строки.
6. **Дописал `README.md §Установка`** (в своей зоне, worktree `wt-market`): указан источник GitHub вместо фразы «зарегистрирован под именем disciplina» без источника; добавлены команды подключения, проверочные команды и отдельный раздел «Установка на другом компьютере» с точными командами и ожидаемым выводом каждого шага (§2.5), включая снятие старого локального маркетплейса при его наличии.
7. **Закоммитил зону** (§4, без обхода хука — в `disciplina` `pre-commit` не установлен, только `.sample`).

### Как проверил
- Live-прогон условия 5 (см. выше) — прямое до/после, не пересказ.
- `claude plugin validate .` из корня маркетплейса → `✔ Validation passed with warnings` (одно ожидаемое предупреждение: «No version specified» — это и есть цель правки, не дефект).
- `python3 -c "import json; json.load(...)"` на оба файла — валидный JSON.
- `claude plugin marketplace list --json` до и после — источник сменился с `"directory"`/локальный путь на `"github"`/`"d1-d57/disciplina"`.
- `claude plugin details disciplina@disciplina` → `Skills (7)`, сверено командой с `ls skills/*/SKILL.md | wc -l` = 7.
- `git_zona.py check --zone .claude-plugin/` → ✅ (после коммита).

### Что НЕ трогал
- `skills/` — целиком (этап 20 работает параллельно).
- Ветку `zahod/marketplace-github` **не пушил и не вливал** — коммит остался только локально в worktree, как предписано §4.
- `OTVETY-INTERVYU-2026-08-07.md` (untracked-файл в основной папке `disciplina`, обнаружен при первом `git status`) — не мой, не тронул.
- `metadata.version` верхнего уровня `marketplace.json` (версия самого манифеста маркетплейса, не плагина) — не участвует в разрешении версии плагина, оставил как есть.
- Ничего вне зоны `.claude-plugin/` + `README.md`.

### Результат верификатора
Верификатор не запускался — заходом не предусмотрен (§3), условие 5 сыграло его роль и выполнено живым прогоном выше.

### Открытое «возвращаться»
После приёмки и влития ветки `zahod/marketplace-github` в `main` — стоит один раз пересобрать локальную установку (`marketplace remove` → `add` → `install`) и убедиться, что `claude plugin details disciplina@disciplina` показывает версию как git-SHA (7-значный хэш), а не `0.1.0`: сейчас это не проверено, потому что моя правка снятия `version` живёт в невлитой ветке.

**АРТЕФАКТ:** артефакта нет — заход инфраструктурный (правка `.claude-plugin/*.json` и `README.md`), собранного файла (HTML/PDF/картинка) не предполагает.

**КОММИТ:** `3fe6deb092412929e07c5f5645f535d641ded0f7` — «маркетплейс: источник GitHub вместо локального пути; версия из SHA» · `git_zona.py check --zone .claude-plugin/` → ✅. Ветка `zahod/marketplace-github`, НЕ запушена и НЕ влита (по §4/§2.2 — влитие делает приёмка).

### НЕОБРАТИМОЕ
1. `git push origin main` (`f6fc2e1` → `bb2f185`) — довезены на GitHub 3 ранее локальных коммита (влитие `reshenia-intervyu` и `standart-skilla`). Восстановление не требуется — ожидаемый прогресс, разрешён владельцем в чате явно.
2. Тестовый коммит `112fa48` запушен в `main`, немедленно отменён ревёртом `c586f9f`, тоже запушенным. Итоговое состояние `main` = `c586f9f`, тестовой строки в содержимом нет. Восстановление не требуется, цикл закрыт тем же ходом; разрешён владельцем в чате явно.
3. Локальный маркетплейс `disciplina` снят из установки на этой машине владельца (`claude plugin marketplace remove disciplina`, вне git) и заменён на GitHub-источник. Восстановление при необходимости: `claude plugin marketplace add ~/Documents/GitHub/disciplina`.

🔴 **Хук `pre-commit` СНАЧАЛА ПРОГОНИ, потом решай про обход.** В `disciplina` его может не быть вовсе — тогда коммит идёт обычной формой, и `--no-verify` не нужен: обход, записанный в автолог без настоящего срыва, засоряет ту самую статистику, по которой меряется боль с коммитами. *Урок 9 арки, оплачен 07.08: предупреждение «здесь покраснеет, вот форма обхода», вписанное заранее, работает как разрешение на обход.*

---

## ПРИЁМКА — заполняет аналитик, 2026-08-07

**ПРИНЯТ.** Гейт 0 прогнан приёмкой: `check --zone .claude-plugin/` → ✅. Коммит `3fe6deb`, три файла: оба json по одной строке минус, `README.md` +37.

**Перепроверено своими командами:**

| проверка | результат |
|---|---|
| `version` снят из `plugin.json` | ✅ 0 вхождений на ветке захода |
| `version` снят из записи плагина в `marketplace.json` | ✅ ключей у `plugins[0]` пять, `version` среди них нет |
| `metadata.version` манифеста НЕ тронут | ✅ `0.1.0` на месте — верно, это версия манифеста, не плагина |
| `main` чист от тестовой строки | ✅ 0 вхождений; `112fa48` → ревёрт `c586f9f` |
| `origin/main` синхронен | ✅ `rev-list origin/main..main` = **0** |
| раздел «Установка на другом компьютере» | ✅ есть, с командами и ожидаемым выводом, включая снятие старого локального маркетплейса |

**Условие 5 исполнено по-настоящему.** Правка → пуш → `marketplace update` → `grep` в установленной копии: **0 до, 1 после**. Это не «команды выполнены без ошибок», а доказательство работы механизма, и ради него заход и писался.

### 🔴 Вопрос исполнителя — дефект захода, и он ПОВТОРНЫЙ

Критерий 5 требовал «закоммить, запушь», §2.2 запрещал пуш, §4 запрещал влитие, а `main` как разрешённое место теста не назван нигде. **Исполнитель дважды остановился и спросил владельца в чате** — то есть свойство «заход самоценен, владельца не дёргаем» потеряно ровно там, ради чего дисциплина заходов существует.

Класс тот же, что урок 12, записанный **этим же днём** на предыдущем заходе. Повтор внутри суток — признак отсутствия носителя, а не невнимательности: правило «ОДИН заход = ОДИН критерий» есть, а гейта, проверяющего критерий на непротиворечивость СО СВОИМИ ЖЕ запретами, нет. → урок 14, с предложением механической проверки в `check_zahod.py`.

### Что стало правдой

- Маркетплейс — **`source: github`, `repo: d1-d57/disciplina`**; локальный путь снят, не оставлен рядом.
- `claude plugin details` печатает **7 скиллов**, сверено с `ls skills/*/SKILL.md | wc -l` = 7.
- `main` доехал на GitHub вместе с обеими влитыми ветками заходов (`dc718f7`, `bb2f185`) — то есть на удалённом лежит дисциплина с 29 решениями владельца, а не позавчерашняя.
- Владелец разрешил оба необратимых действия (пуш `main`, тестовый коммит) явно, в чате; оба названы отдельным списком.

### 🔴 Долг, который эта приёмка НЕ закрывает

Ветка `zahod/marketplace-github` **не влита**. Пока её нет в `main`, GitHub раздаёт манифест со СТАРЫМ `version: 0.1.0` — то есть механизм «версия из SHA» настроен, но ещё не действует, и `claude plugin details` честно показывает `0.1.0`. Исполнитель это назвал сам в «возвращаться».

**Влитие + контрольная переустановка — первый ход следующего захода в этом репозитории**, с проверкой: `claude plugin details` обязан показать семизначный хэш вместо `0.1.0`.
