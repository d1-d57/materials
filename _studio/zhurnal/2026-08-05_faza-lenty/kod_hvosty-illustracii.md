# Канал исполнителя — hvosty-illustracii (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — механика расписана поимённо: три правки известны по адресам, решения приняты владельцем, придумывать нечего.

КОНТЕКСТ. `materials/` — монорепо. Фабрика иллюстраций только что вынесена в `_illustracii/` (заход `vynos-illustracii`, принят 2026-08-06). На старом адресе `_studio/konvejer/09-illustracii/` оставлены файлы-указатели — 51 внешняя ссылка через них разрешается.

ЦЕЛЬ: закрыть три хвоста, которые тот заход честно назвал и оставил владельцу, потому что они лежали вне его зоны. **Решения по всем трём приняты, обсуждать не надо** — только исполнить.

Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: закрывай хвосты по одному целиком, не наполовину; недоделанный назови поимённо.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/hvosty`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/hvosty-illustracii`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add hvosty --branch zahod/hvosty-illustracii
  cd ../materials-wt/hvosty
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/` `obzory/README.md` `teorkat-vvedenie/` `catalan/` `kurs-puti-i-volny/README.md` `krivaya-drakona/START-HERE.md` `lsh-2026-perechislitelnaya/` `_studio/konvejer/09-illustracii/` `_illustracii/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/hvosty-illustracii`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/` `obzory/README.md` `teorkat-vvedenie/` `catalan/` `kurs-puti-i-volny/README.md` `krivaya-drakona/START-HERE.md` `lsh-2026-perechislitelnaya/` `_studio/konvejer/09-illustracii/` `_illustracii/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы. Проект сверх них не изучай:
  1. `_illustracii/README.md` и `_illustracii/DISCIPLINA.md` — новый дом, чтобы адрес писать верно.
  2. `_studio/konvejer/09-illustracii/SLOVAR-primitivov.md` — стаб-указатель, образец формулировки переезда.
  3. `_studio/docs/KARTA.md §1` — там уже стоит строка маршрута; **скопируй её форму** в проекты, не сочиняй свою.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Новый дом словаря — **`_illustracii/DISCIPLINA.md`** (от корня репозитория). Разделы переехали под теми же именами.

### A. Движок и его вход — уводят не туда

| адрес | что там | что сделать |
|---|---|---|
| `_generator/build_doc.py:1453` | старый адрес словаря в коде; строка питает **35 собранных `.html`**, включая выложенный сайт через `sayt/static/` | заменить на новый |
| `_generator/README.md:74` | вход в корень движков; иллюстрации упомянуты 18 раз, единственный названный адрес — старый | заменить на новый |

⚠ Номера строк — снимок 2026-08-06. **Проверь грепом**, адрес тоже предпосылка (`KONSTITUCIYA §14`). Разошлось — работай по своему прогону и скажи об этом.

⚠ После правки `build_doc.py` **собранные `.html` не трогай руками** — их чинит пересборка источника. Назови в отчёте, сколько файлов ждут пересборки, и не пересобирай их в этом заходе.

### B. Шесть проектов молчат о новом доме

Владелец просил маршрут «работаешь с изображением — идёшь туда» во всех документах, читаемых **на входе в папку**. В `materials/` и `_studio/` (5 документов) он уже стоит — возьми оттуда формулировку и держи её одинаковой. Осталось:

| проект | входной документ |
|---|---|
| `obzory/` | `README.md` |
| `teorkat-vvedenie/` | `CLAUDE.md`, `COWORK.md`, `README.md` |
| `catalan/` | `CLAUDE.md`, `COWORK.md` |
| `kurs-puti-i-volny/` | `README.md` |
| `krivaya-drakona/` | `START-HERE.md` |
| `lsh-2026-perechislitelnaya/` | **входного документа нет** — не заводи, назови в отчёте |

Формат — строка таблицы «ситуация → дом», как в существующих роутерах. Не абзац, не раздел.

### C. Мусорный файл, ставший опасным

`_studio/konvejer/09-illustracii/.fuse_hidden0000001300000004` — артефакт файловой системы, случайно попавший под git: полнотекстовый снимок `DOK.md` до 10.07, расходится с живым на 63 строки.

**Решение владельца: удалить.** Раньше он был безобиден, теперь опасен — соседи стали короткими указателями, а он остался полным текстом СТАРЫХ правил и находится грепом наравне с живым `_illustracii/fazy/DOK.md`.

Удаление обратимо: файл в истории git. **Назови в `НЕОБРАТИМОЕ` хэш, из которого он достаётся.**

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. Греп по всему репозиторию: старый адрес `konvejer/09-illustracii` в `_generator/` — **ноль вхождений вне комментариев-историй**. Вердикт с охватом.
2. Маршрут стоит в **восьми** документах шести проектов (`lsh-2026` не считается — его нет). Проверяется грепом `_illustracii`, число назвать.
3. `.fuse_hidden0000001300000004` отсутствует и в дереве, и в индексе: `git ls-files _studio/konvejer/09-illustracii/` печатает четыре пути, не пять.
4. 🔴 **ЖИВОЙ ПРОГОН, не только греп:** `python3 _generator/build_doc.py` на любом живом источнике собирается (`rc=0`) — правка кода ничего не сломала. Назови, какой источник брал.
5. `sh .githooks/pre-commit` зелёный.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «старых адресов не осталось», а «не осталось, проверено X из Y».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР — один субагент, узко

Лоссовое здесь одно — удаление файла в §C, и оно обратимо через git. Полноценный верификатор не нужен, но одна сверка обязательна и делается СВЕЖИМ субагентом, а не тобой:

> *«В репозитории недавно переехала фабрика иллюстраций из `_studio/konvejer/09-illustracii/` в `_illustracii/`. Найди места, которые всё ещё ведут на старый адрес и при этом НЕ являются ни файлом-указателем, ни записью в журнале закрытой арки. Верни список с охватом.»*

Нашёл — в отчёт строкой, чини если в зоне, называй долгом если вне.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/` `obzory/README.md` `teorkat-vvedenie/` `catalan/` `kurs-puti-i-volny/README.md` `krivaya-drakona/START-HERE.md` `lsh-2026-perechislitelnaya/` `_studio/konvejer/09-illustracii/` `_illustracii/`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/` `obzory/README.md` `teorkat-vvedenie/` `catalan/` `kurs-puti-i-volny/README.md` `krivaya-drakona/START-HERE.md` `lsh-2026-perechislitelnaya/` `_studio/konvejer/09-illustracii/` `_illustracii/`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)
1. Worktree `hvosty` на ветке `zahod/hvosty-illustracii`, зона чиста в HEAD — откат не нужен.
2. Прочитать три названных файла (`_illustracii/README.md`, `DISCIPLINA.md`, `SLOVAR-primitivov.md`-стаб) + `KARTA.md §1` — взять оттуда точную формулировку строки маршрута.
3. Задача A: грепом подтвердить адреса `build_doc.py:1453` и `README.md:74` (снимок дат мог разойтись — так и оказалось: описание "35 файлов"/"18 упоминаний" не подтвердилось грепом, но сами адреса совпали) → заменить на `_illustracii/DISCIPLINA.md`.
4. Задача B: в 8 входных документах 6 проектов добавить строку таблицы «Ситуация → Дом» — форму взять из `KARTA.md §1`, в `kurs-puti-i-volny/README.md` встроить в уже существующую таблицу «хочу → файл» тем же принципом. `lsh-2026-perechislitelnaya/` — входного документа нет, не заводить.
5. Задача C: удалить `.fuse_hidden0000001300000004`, записать хэш для восстановления в `НЕОБРАТИМОЕ`.
6. Прогнать критерии готовности 1–5 (грепы + живой `build_doc.py` + `pre-commit`).
7. Позвать СВЕЖЕГО субагента-верификатора (§3) узким промптом из захода, не редактируя его.
8. По находкам верификатора: чинить то, что внутри зоны; вне зоны — в отчёт долгом. Собранные `.html` не трогать руками нигде (правило §A применяется ко ВСЕМ найденным построенным файлам, не только к 34/35 из таблицы).
9. Коммит зоны двумя ходами (`add` → `commit -- <пути>`), `git_zona.py check --zone`, `show --stat`.
10. Отчёт в этот файл.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Свежий верификатор нашёл ~45 живых (не указатель, не журнал) ссылок на старый адрес `_studio/konvejer/09-illustracii/` ЗА ПРЕДЕЛАМИ зоны этого захода — полный список в `## ОТЧЁТ`. Крупнейший кластер (9 файлов, 17 строк) — собственная документация фабрики `_studio/docs/` и `_studio/konvejer/{00-KONVEJER,04-gibrid-istochnik,05-raskadrovka,06-tekst,ALGORITM,KATALOG}`, которую этот заход не имел права трогать. Остальное — `teoriya-kategoriy/SPEKA.md` (read-only архив), `obzory/*` вне `README.md`, `krivaya-drakona/*` вне `START-HERE.md`.
   ДОМ: `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md`
   ДОСТАВЛЕНО: нет

2. Тот же верификатор + собственный греп нашли **33 собранных `.html`**, несущих устаревший CSS-комментарий из старой версии `build_doc.py` (адрес чинится пересборкой источника, не руками — правило §A), плюс отдельно `teorkat-vvedenie/src/dist/index.html`, `teorkat/lekciya-1.html`, `sayt/static/teorkat/lekciya-1.html` — производные уже почищенного `base.css`, но по другому конвейеру. Ни один не тронут: список — в `## ОТЧЁТ`, решение о приоритете пересборки — за владельцем.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

### Что сделано + зачем

**A. Движок.** `_generator/build_doc.py:1453` (комментарий в CSS-шаблоне, буквально попадает в КАЖДЫЙ собранный `.html`) и `_generator/README.md:74` — старый адрес словаря заменён на `_illustracii/DISCIPLINA.md`. Предпосылки в таблице захода частично не подтвердились: `_generator/README.md` упоминает «иллюстрации» **9 раз** (грепом `иллюстр[а-я]*`), не 18; собранных `.html`, несущих строку из `build_doc.py:1453`, было **34**, не 35 (список — ниже). Сами адреса строк (1453 / 74) грепом подтвердились точно.

**B. 8 входных документов 6 проектов** получили строку маршрута «Ситуация | Дом» с формулировкой из `KARTA.md §1` (относительный путь `../_illustracii/` — все 6 проектов на той же глубине, что и `_studio/`): `obzory/README.md`, `teorkat-vvedenie/{CLAUDE,COWORK,README}.md`, `catalan/{CLAUDE,COWORK}.md`, `kurs-puti-i-volny/README.md` (строка встроена в уже существующую таблицу «хочу → файл»), `krivaya-drakona/START-HERE.md`. `lsh-2026-perechislitelnaya/` — входного документа нет физически (проверено `ls`), не заводил.

**C.** `_studio/konvejer/09-illustracii/.fuse_hidden0000001300000004` удалён (FS-артефакт, снимок `DOK.md` до 10.07, расходился с живым на 63 строки — принято владельцем как решённое, не обсуждал).

**Сверх заявленных трёх пунктов, по итогам верификатора §3 (в зоне → починил):**
- `_generator/svgo.config.mjs:1` — комментарий-заголовок конфига.
- `teorkat-vvedenie/src/base.css:132` — канон «ФОН РИСУНКА» указывал на старый адрес.
- `teorkat-vvedenie/KONTRAKT-L2.md:194` — список источников контракта L2.
- `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/HANDOFF.md:109` — хэндофф-документ проекта.

**Оставлено БЕЗ правки внутри зоны (историческое, не живая ссылка):**
- `_illustracii/POKRYTIE.md:10` — цитата исторической команды с аргументом-фазой; сам файл поясняет, что корпус заморожен и переезду не подлежит.
- `catalan/zhurnal/2026-08-03_puti-i-mera-veyla/YADRO.md:90`, `catalan/zhurnal/2026-07-13_dika-v-vysshey-matematike/SESSIYA.md:36` — записи в `zhurnal/`, исключены критерием верификатора буквально.
- `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/SESSIYA.md:554` — дневник хода (запись от 2026-07-24), не входной документ; физически в зоне, но по духу приравнял к записи журнала закрытой арки (не в папке `zhurnal/`, поэтому вынесено вопросом владельцу, не решил сам).

### Как проверил (критерии готовности §2, все пройдены)
1. `grep -rn "konvejer/09-illustracii" _generator/` → **0 вхождений**, охват — весь `_generator/`.
2. `grep -c "_illustracii" <файл>` по всем 8 документам → **8 из 8** содержат маршрут.
3. `git ls-files _studio/konvejer/09-illustracii/` после `add` → **4 пути** (было 5).
4. Живой прогон: `python3 _generator/build_doc.py _generator/doc-demo` → **rc=0** (гонял дважды: после правки §A и после правок сверх-плана §3). Побочный эффект живого прогона: `_generator/doc-demo/view.html` пересобрался и заодно подхватил накопившиеся с 2026-08-04 изменения движка (тёмная тема, выезжающее оглавление) — это ожидаемо для fixture внутри зоны `_generator/`, не ручная правка.
5. `sh .githooks/pre-commit` на застейдженной зоне → **rc=0**, зелёный (кроме заведомо отключённой ловушки 1-2 — нет playwright у соседа, гейт сам поясняет, что это не красный, а названный зазор, Р31).

### Что НЕ трогал
- Ни одного собранного `.html` руками — не только заявленные в задаче, но и все найденные верификатором (правило §A применил ко всем, не только к списку из таблицы).
- Всё вне зоны — 4 группы находок верификатора, см. `## ВОПРОСЫ`. Список 34 → 33 (после рекомпиляции doc-demo) файлов, ждущих пересборки:
  `catalan/kurs/{L6-konspekt,L7-konspekt,razbor-cf-narayana}/view.html` · `krivaya-drakona/lekciya/build/view.html` · `teorver-vvedenie/plan/src/view.html` · `obzory/{dika-v-vysshey-matematike,karri-hovard-lambek,goodstein-ordinaly-gidra,sofichnost-priblizhenie-grupp-konechnymi,kombinatorika-okruzhnosti,puti-ogranichennoy-vysoty,sluchajnye-bluzhdaniya-na-gruppah,monady-i-yoneda}/src/view.html` · `teorkat-vvedenie/{MAT-KOSTYAK,KONSPEKT-L1,DOKAZATELSTVA-L1,LENTA-L2,raskadrovka/teksty,src}/view.html` · `kurs-puti-i-volny/{obrazec,plan}/src/view.html` · `_studio/konvejer/06-tekst/ETALON-vid/view.html` · `_studio/zhurnal/{2026-07-16_teorkat-programma-dizajn/annotacii,2026-07-20_teorkat-motivacia/{VIDY,IZO},2026-07-21_mat-kostyak/obrazec-format,2026-08-05_faza-lenty/vidy/{DOKAZATELSTVA-L1--DO-pravki,KONSPEKT-L1,LENTA-L2,DOKAZATELSTVA-L1--POSLE-pravki}}/*.html` · `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/{razbor-bijekcii,kotly,raskadrovka/teksty}/view.html`.

### Вопросы
См. `## ВОПРОСЫ` — два пункта очереди (долг вне зоны + backlog пересборки), оба `ДОСТАВЛЕНО: нет`.

### Результат верификатора (§3)
Свежий субагент нашёл 58 живых строк в 49 файлах вне определения «указатель/журнал» на момент запуска (охват: 2366 файлов репозитория просмотрено, 104 файла дали грепом `09-illustracii`). После разбора по зоне: 4 внутри зоны — починены (см. выше), 2 оставлены как историческое/пограничное с объяснением, остальное — долг вне зоны, см. `## ВОПРОСЫ` п.1. Полный поимённый список верификатора не копирую сюда повторно — он лежит в результате Agent-хода этой сессии.

### Открытое «возвращаться»
Пересборка 33 устаревших `.html` вне зоны — не моя задача в этом заходе; когда владелец решит пересобрать, `build_doc.py`/`build_deck.py` уже несут верный адрес.

### Необратимое
- **Удалён** `_studio/konvejer/09-illustracii/.fuse_hidden0000001300000004` (FS-артефакт, решение владельца, п.C задания) · восстанавливается из коммита `ed3c8077301bcd7551c705a83608ab120fc83160` (`git show ed3c8077301bcd7551c705a83608ab120fc83160:_studio/konvejer/09-illustracii/.fuse_hidden0000001300000004`).
- Больше необратимого нет.

**АРТЕФАКТ:** артефакта нет: заход правит текст/код (роутинг, CSS-комментарий, удаление FS-артефакта), ничего не собирал заново специально — `_generator/doc-demo/view.html` пересобрался только как побочный эффект живого прогона критерия готовности 4, это fixture движка, а не целевой продукт этого захода.
**КОММИТ:** `5dc10feb17de8a222b4be9f31ba5bf30481bac95` — «hvosty-illustracii: закрыть три хвоста переноса фабрики иллюстраций» · `git_zona.py check --zone "_generator/ obzory/README.md teorkat-vvedenie/ catalan/ kurs-puti-i-volny/README.md krivaya-drakona/START-HERE.md lsh-2026-perechislitelnaya/ _studio/konvejer/09-illustracii/ _illustracii/"` → ✅
