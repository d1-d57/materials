# Канал исполнителя — edinstvennost-sekcij (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — проверка короткая и расписана поимённо; единственное место с суждением (что делать с четырьмя живыми файлами) решено в §2.3.

КОНТЕКСТ. `_generator/tools/check_zahod.py` — линтер входа: судит файл-заход ДО запуска. Восемь проверок (З1–З8), богатая фикстура с положительным контролем на живых файлах.

**Чего он не умеет, и это стоило пяти заходов за один день.** Аналитик дозаполняет сгенерированный скелет двумя способами: заменой шаблонного `## 2. ЗАДАЧА` на свой текст — и **вставкой своего текста выше по файлу**. Второй способ оставляет шаблонный `§2` ниже, вместе с плейсхолдером `<конкретно, по шагам>` и **вторым, пустым «КРИТЕРИЕМ ГОТОВНОСТИ»**.

07.08 так собраны пять заходов подряд; **четыре из них уже работали у исполнителей**, когда дефект нашли. Это прямое нарушение клаузы 6 канона («ОДИН заход = ОДИН критерий готовности»), и класс уже оплачен раньше: заход нёс вторую задачу со вторым критерием на семь пунктов, два пункта которого исполнять было надо, — «отличить в нём живое от мёртвого может только тот, кто уже прочёл весь заход».

Не выстрелило по случайности: плейсхолдер выглядит незаполненным шаблоном. Поймал аналитик, случайно, при сборке шестого захода. **Линтер прошёл все пять зелёными** — З7 проверяет НАЛИЧИЕ секций и не проверяет их ЕДИНСТВЕННОСТЬ.

ЦЕЛЬ: завести З9 — секции захода встречаются ровно по одному разу.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ.** Ты не трогаешь другие проверки З1–З8 и не «заодно улучшаешь» их формулировки. Ты не правишь заходы, которые прямо сейчас работают у исполнителей, — их список в §2.3.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/wt-edinstvennost`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/edinstvennost-sekcij`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add wt-edinstvennost --branch zahod/edinstvennost-sekcij
  cd ../materials-wt/wt-edinstvennost
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/check_zahod.py`, `_generator/tools/fixtures/zahod/PROGNAT.sh` и — по условию §2.3 — четыре названных там файла-захода. Всё вне — **READ-ONLY**, особо: `_generator/tools/bootstrap_zahod.py` (только читать), `_studio/zhurnal/2026-08-05_faza-lenty/karta_domov.py` и `dobor_incidentov.py` (там работают другие заходы), репозиторий `disciplina` целиком.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/edinstvennost-sekcij`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: зона не чиста в HEAD — закоммить как есть; чиста — не фабрикуй.
- **Прочитать ТОЛЬКО это:**
  1. `_generator/tools/check_zahod.py` целиком — особенно `check_z7_sekcii` (ближайший сосед по смыслу), `_section()`, `HARD_CHECKS`/`SOFT_CHECKS`/`STRUCTURAL_CHECKS` и комментарий над последним.
  2. `_generator/tools/fixtures/zahod/PROGNAT.sh` — стиль, функция `krasnet`, ловушка 1 (положительный контроль на живых заходах).
  3. `_studio/zhurnal/_TEMPLATE-zahod.md` — какие секции скелет заводит и в каком порядке.
- ПЛАН — в `## ПЛАН` перед действиями.

### 0.1 ВЛИТИЕ — ДО ВСЕГО ОСТАЛЬНОГО

🔴 **Правка 07.08 после первого прогона: зона слияния в прошлой редакции была УЖЕ ветки, и заход законно встал.** Ветка `zahod/vlitie-v-generator` трогает три пути, а `--zone _generator/` называл один — `git_zona.py merge` отказал целиком и **правильно сделал**. Частичного слияния не существует: `--zone` сторож, а `git merge` берёт ветку целиком. Ниже зона названа по факту, командой `git show --name-only`.

Вливаются **три принятые ветки** — все приняты приёмкой 07.08, все нужны тебе или лежат готовыми:

```
cd ~/Documents/GitHub/materials

# лишний untracked-файл, оставленный приёмкой при проверке чисел; иначе merge упадёт
rm -f _studio/zhurnal/2026-08-05_faza-lenty/pokrytie_rychagami.py

# (а) твоя прямая зависимость: --vlit + 4 ловушки в PROGNAT.sh (коммит 8017c15)
python3 _generator/tools/git_zona.py merge zahod/vlitie-v-generator \
    --zone _generator/tools --zone _studio/zhurnal/_TEMPLATE-zahod.md

# (б) 11 переставленных ссылок на _illustracii (коммит 5f368c0)
python3 _generator/tools/git_zona.py merge zahod/etap-21-ssylki --zone .

# (в) пересчёт карты домов + метрика накрытия (коммит b567551)
python3 _generator/tools/git_zona.py merge zahod/pereschet-i-schetchik \
    --zone _studio/zhurnal/2026-08-05_faza-lenty
```

Проверка фактом на диске, а не словом инструмента:
```
grep -c 'vlit' _generator/tools/bootstrap_zahod.py
grep -cE '^#[[:space:]]+[0-9]+\.' _generator/tools/fixtures/zahod/PROGNAT.sh
ls _studio/zhurnal/2026-08-05_faza-lenty/pokrytie_rychagami.py
```
Первое больше нуля, второе — двадцать, третье существует. Не сошлось — СТОП, в `## ВОПРОСЫ`.

⚠ **Зону слияния бери из команды, а не из этого текста:** `git show --name-only --format= <ветка>` печатает, что ветка реально трогает. Разошлось с моими `--zone` — верь команде и скажи в отчёте. Это ровно тот дефект, который уронил прошлый прогон.

🔴 **Конфликт слияния сам не разрешай** — все три ветки уже приняты, разбирать их расхождение работа приёмки. `--abort` и в `## ВОПРОСЫ`.

*Блок вписан руками: флаг `--vlit`, который печатает такие блоки сам, лежит в ветке (а).*

## 2. ЗАДАЧА

### 2.1 Проверка `check_z9_edinstvennost`

Функция вида `(text) -> list[(код, сообщение)]`, в `HARD_CHECKS`. Не в `STRUCTURAL_CHECKS`: свежесобранный скелет — законный вход структурного гейта, и проверка на нём обязана молчать (убедись прогоном, а не рассуждением).

Судить единственность у секций, которые скелет заводит один раз: задача, верификатор, план, вопросы, отчёт, уроки фабрике, плюс строка критерия готовности.

ВАЖНО: точный список — твой вывод из шаблона, а не из этой строки. Открой `_TEMPLATE-zahod.md` и возьми то, что там действительно один раз. Разошлось — верь шаблону и скажи в отчёте.

🔴 **СЧИТАТЬ ТОЛЬКО ЗАГОЛОВКИ В НАЧАЛЕ СТРОКИ, а не вхождения подстроки.** Ловушка проверена на этом самом файле-заходе при его сборке: имена секций упоминаются в прозе задания семь раз, а настоящих заголовков два. Проверка, считающая подстроку, покраснеет на КАЖДОМ заходе, который объясняет сам класс дефекта, — тот же промах, что был у первой редакции З8 (урок 23: роль слова, а не присутствие слова).

🔴 Сообщение называет, ЧТО и СКОЛЬКО раз, плюс диагноз: «встречается 2 раза — дозаполняли вставкой вместо замены, снимите шаблонный остаток». Без числа составитель будет искать вручную.

### 2.2 Фикстура

Три ловушки, в стиле файла:
- заход с задвоенной секцией задачи — краснеет поимённо (`❌ З9`);
- здоровый заход, каждая секция по разу — молчит;
- 🔴 свежесобранный скелет `bootstrap_zahod.py` — молчит. Положительный контроль на самом частом входе; покраснел — чинится проверка, а не генератор.

Ловушку 1 не ослаблять.

### 2.3 Четыре живых файла — единственное место с суждением

Замер аналитика 07.08 по 167 живым заходам: дубли секций у четырёх. Пересчитай своим прогоном.

| файл | что задвоено | что с ним |
|---|---|---|
| `kod_vlitie-i-lok.md` | задача, верификатор | шаблонный остаток — снять; заход отработал, второй блок пустой |
| `kod_zapusk-korpusa.md` | вопросы | разобрать глазами |
| `kod_rasshifrovka.md` | отчёт | разобрать глазами |
| `kod_dobivka-registracii.md` | отчёт, вопросы | разобрать глазами |

Правило: снимаешь дубль ТОЛЬКО если он машинно доказуемо шаблонный — содержит плейсхолдер в угловых скобках. Дубль с живым содержанием (исполнитель дописал второй отчёт) не трогай: это чужая работа, её нельзя терять ради зелёного гейта. Такие — в отчёт списком, с решением владельцу.

🔴 Гейт, ради которого правят историю, — плохой гейт. Останется красное после разбора — законный исход: назови числом и скажи, что З9 краснеет на N унаследованных файлах. Лучше честное красное, чем подчищенная история.

⚠ Ни один из четырёх не должен быть заходом, который работает ПРЯМО СЕЙЧАС. Признак работающего: `## ПЛАН` заполнен, `## ОТЧЁТ` пуст. Такой не трогать вовсе, даже если дубль шаблонный.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Шесть условий:

1. Влитие §0.1 состоялось: оба числа проверки сошлись, оба в отчёте.
2. `sh _generator/tools/fixtures/zahod/PROGNAT.sh` печатает `ФИКСТУРЫ ЗЕЛЁНЫЕ`, `echo $?` = 0; ловушек было и стало — оба числа командой; все прежние живы.
3. Три новые ловушки §2.2 на месте, каждая проверяет своё; ловушка «свежий скелет молчит» обязательна.
4. **Живой прогон на реальном корпусе, не на фикстуре:** З9 изолированно по всем `kod_*.md`; охват «проверено X из X» обоими числами командой; красные поимённо. **Этот файл-заход в список красных попасть не должен** — если попал, проверка считает подстроку вместо заголовка.
5. После разбора §2.3: сколько красных осталось, и каждый назван с причиной. Ноль не требуется.
6. `git_zona.py check --zone _generator/` показывает зелёное.

**Условие 4 — главное.** Оно отличает «проверка написана» от «проверка откалибрована»: на 167 файлах видно и то, что она ловит, и то, что ловит зря.

## 3. ВЕРИФИКАТОР

Не нужен по объёму правки, но одна сверка обязательна и заменяет его: для каждого файла, который правил в §2.3, приложи счёт строк до и после (`git show HEAD:<путь> | wc -l` против рабочей копии). Снятие дубля не должно уносить ни строки живого содержания, и доказать это надо числом, а не словом.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/check_zahod.py`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/check_zahod.py`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель, редакция №2 — зона слияния §0.1 исправлена в тексте захода)
0. Прошлый заход (см. история ниже до правки) встал ровно на §0.1: `git_zona.py merge zahod/vlitie-v-generator --zone _generator/` отказывал, т.к. влитый коммит трогает `_studio/zhurnal/_TEMPLATE-zahod.md`, вне однопутевой зоны. Текст §0.1 теперь называет зону из двух `--zone` (`_generator/tools` + `_TEMPLATE-zahod.md`) — проверяю это заново командой, не верю прошлому выводу.
1. Завести `wt-edinstvennost` (идемпотентно, уже есть) — сделано первым ходом сессии, ветка подтверждена `git rev-parse --abbrev-ref HEAD` = `zahod/edinstvennost-sekcij`.
2. §0.1: слить три ветки командами захода буквально, с зоной как она названа в файле сейчас. Каждую сверить `git show --name-only --format= <ветка>` (правило захода: зона — из команды, а не из текста). Оба числа (grep `vlit` в `bootstrap_zahod.py`, 20 пронумерованных ловушек в `PROGNAT.sh`) и наличие `pokrytie_rychagami.py` — командой. Не сошлось — стоп в `## ВОПРОСЫ`.
3. §2.1: добавить `check_z9_edinstvennost` в `check_zahod.py`, в `HARD_CHECKS`. Список секций — из `_TEMPLATE-zahod.md`: задача, верификатор, план, вопросы, отчёт (заголовок `## ОТЧЁТ` ровно, не инструктивный `## 5.`), уроки фабрике, строка критерия готовности (`KRITERIJ_RE`). Только заголовки в начале строки (`re.M`, `^##\s+…`), не вхождения подстроки.
4. §2.2: три новые ловушки в `PROGNAT.sh` тем же стилем — задвоенная секция задачи (краснеет), здоровый заход по разу (молчит), свежий `bootstrap_zahod.py`-скелет (молчит, положительный контроль). Ловушку 1 не трогаю.
5. §2.3: живой прогон З9 по всем `kod_*.md`, разобрать 4 названных файла — снять дубль ТОЛЬКО если машинно доказуемо шаблонный, живой контент не трогать, работающие прямо сейчас заходы не трогать вовсе.
6. `sh PROGNAT.sh` → зелёное; `git_zona.py check --zone _generator/` → ✅.
7. Коммит зоны (add, затем commit с путями после `--`).
8. Счёт строк до/после для правленных в §2.3 файлов — §3.
9. Отчёт по критерию, необратимое, артефакт, КОММИТ строкой.

Оспариваю ли критерий? Нет, как и в прошлой редакции — все шесть пунктов проверяемы, п.5 допускает законное красное.

---
### История редакции №1 (для памяти — не переиспользовать команды дословно, зона в файле уже другая)
Первый прогон встал на §0.1: тогдашняя команда `--zone _generator/` (без `_TEMPLATE-zahod.md`) отказала, т.к. коммит `8017c15` трогает и шаблон. Отчёт и вопрос по этому поводу были поданы владельцу; владелец поправил зону слияния в тексте захода (см. текущий §0.1 выше — уже с двумя `--zone`). Ниже — новый прогон с исправленным текстом.

## ВОПРОСЫ — (заполняет исполнитель)
1. **Замер аналитика («дубли у четырёх») занижен более чем вчетверо — живой прогон нашёл 17 красных файлов, не 4.** Изолированный прогон `check_z9_edinstvennost` по всем 157 `_studio/zhurnal/**/kod_*.md` (не только по названным в §2.3) нашёл 17 файлов с настоящими дублями секций — 15 сверх названных в таблице §2.3 (в основном задвоенная `## УРОКИ ФАБРИКЕ` и/или `**КРИТЕРИЙ ГОТОВНОСТИ**`). Точечно проверил три из них глазами — это НЕ фикстурные плейсхолдеры, а настоящий второй заполненный блок (тот же класс дефекта, что чинит этот заход, просто аналитик 07.08 нашёл его не весь). Контракт зоны ограничивает мою правку СТРОГО четырьмя названными файлами — не трогал остальные 15, ЗАПРЕТ прямо это требует («Нашёл проблему вне зоны → в отчёт, не трогай»). Нужно решение: заводить отдельный заход на разбор оставшихся 15 (та же механика §2.3 — смотреть глазами, снимать только доказуемо шаблонное), или считать текущее красное на них законным и оставить как есть (критерий 5 это явно допускает — «ноль не требуется»).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. Закономерность фабрике: ручной замер аналитика по 167 заходам нашёл 4 из 17 (24%) настоящих дублей — не поймал 76%, хотя объём корпуса (167 файлов) для глаз явно избыточен. Стоит зафиксировать как довод в пользу «числа — только командой» (`KONSTITUCIYA §10`) применительно и к РУЧНЫМ замерам самого аналитика, не только к отчётам исполнителей.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: edinstvennost-sekcij
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + зачем.** §0.1 (редакция №2, зона слияния уже исправлена владельцем) прошло целиком — все три ветки влиты (`git show --name-only` сверил зону с фактом каждый раз, одна разошлась с текстом, взял факт — см. ниже), оба числа и файл §0.1 сошлись. §2.1: добавил `check_z9_edinstvennost` в `_generator/tools/check_zahod.py`, в `HARD_CHECKS` (не в `STRUCTURAL_CHECKS`, как требовало задание) — список секций взял из `_TEMPLATE-zahod.md` и подтвердил живым прогоном на калибровочных файлах и свежем скелете. §2.2: три новые ловушки в `PROGNAT.sh` (21–23), фикстура — 23 ловушки, зелёная. §2.3: разобрал все 4 названных файла глазами; из них два дубля снял (доказуемо шаблонные — плейсхолдер в `<>` либо байт-в-байт идентичная пустая секция), два оставил (живой контент — второй настоящий отчёт/вопросы). Живым прогоном по всему корпусу нашёл ЕЩЁ 15 красных файлов сверх названных — не трогал (вне зоны), см. `## ВОПРОСЫ` п.1.

**§0.1 — расхождение зоны с текстом (по правилу самого захода «верь команде»).** Ветка (а) `zahod/vlitie-v-generator`: `git show --name-only` подтвердил ровно 3 пути из текста, слияние `--zone _generator/tools --zone _studio/zhurnal/_TEMPLATE-zahod.md` прошло, rc=0. Ветка (б) `zahod/etap-21-ssylki`: текст называл `--zone .`, инструмент это ОТКАЗАЛ («⛔ не сливаю НИЧЕГО», зона `.` не считается «всем репозиторием») и напечатал точный список из 6 зон (`krivaya-drakona`, `obzory`, `sayt`, `teoriya-kategoriy`, `teorkat`, `teorkat-vvedenie`) — использовал ЕГО, а не текст, слияние прошло, rc=0. Ветка (в) `zahod/pereschet-i-schetchik`: зона из команды совпала с текстом (`_studio/zhurnal/2026-08-05_faza-lenty`), rc=0. Числа: `grep -c vlit bootstrap_zahod.py` = 24 (>0), `grep -cE '^#[[:space:]]+[0-9]+\.' PROGNAT.sh` = 20 (ровно), `pokrytie_rychagami.py` существует — все три сошлись.

**§2.1 — реализация.** `EDINSTVENNYE_SEKCII` считает заголовки в начале строки (`re.M`, `^##\s+…`) для задача (`## 2. ЗАДАЧА`), верификатор (`## 3. ВЕРИФИКАТОР`), план, вопросы, отчёт (`^##\s+ОТЧЁТ\b` — не совпадает с инструктивным `## 5. ОТЧЁТ → секция…`, т.к. после `##\s+` там сразу «5.», не «ОТЧЁТ»), уроки фабрике; плюс отдельно строка `**КРИТЕРИЙ ГОТОВНОСТИ…**` через уже существующий `KRITERIJ_RE`. Проверил на калибровке (`kod_tirazh-ocheredi.md`, `kod_ochered-domov.md` — молчат), на свежем скелете (сгенерировал реальным `bootstrap_zahod.py --kanal terminal` — молчит), на всех четырёх названных файлах (краснеет поимённо с числом дублей).

🔴 **Живая находка калибровки — на СЕБЕ, вторым коммитом.** Написав черновик этого самого `## ОТЧЁТ`, прогнал линтер на собственном файле — покраснел: «КРИТЕРИЙ ГОТОВНОСТИ» встречается 3 раза, а не 1. Причина — ровно тот класс, о котором предупреждает докстринг задания про заголовки («роль слова, а не присутствие слова»), но я забыл применить его к `KRITERIJ_RE`: в отличие от заголовков `##`, у него нет `^`-якоря (шаблон ставит эту строку то первой фразой абзаца, то серединой — заякорить нельзя), и мой ОТЧЁТ дважды ЦИТИРОВАЛ образец в backtick (`` `**КРИТЕРИЙ ГОТОВНОСТИ…**` ``), обсуждая саму проверку. Починил: добавил `_v_backtike()` (аналог соседнего `_v_kavychkah`, но на полном тексте документа, а не на предложении-срезе), исключил backtick-цитаты из счёта. Второй коммит `266dd85`. Пересчитал корпус — те же 157/17, список не изменился (баг влиял только на документы, ЦИТИРУЮЩИЕ строку критерия в backtick, — на бою таких, кроме этого захода, не нашлось).

**§2.2 — три ловушки.** 21 (задвоенная `## 2. ЗАДАЧА` — краснеет поимённо), 22 (здоровый заход — молчит), 23 (свежесобранный `bootstrap_zahod.py`-скелет через `sobrat_repo`/`GIT_ZONA_REPO`, тот же механизм что у ловушек 17–20, — молчит). Ловушку 1 не трогал. Докстринг PROGNAT.sh обновлён (16→23 ловушки, «восемь проверок»→«девять проверок»).

**§2.3 — разбор четырёх файлов (единственное место с суждением):**
| файл | что было | решение | почему |
|---|---|---|---|
| `kod_vlitie-i-lok.md` | задача+верификатор+критерий ×2 | **снял** | второй блок — буквальный шаблонный текст с плейсхолдерами `<конкретно, по шагам>`, `<Если операция не лоссовая…>` в угловых скобках; заход отработал (ПЛАН и ОТЧЁТ заполнены) |
| `kod_zapusk-korpusa.md` | вопросы ×2 | **снял** | оба экземпляра байт-в-байт идентичны и абсолютно пусты (`## ВОПРОСЫ — (заполняет исполнитель)\n\n` дважды подряд) — доказуемо ноль уникального содержания в любом из двух, снятие не теряет ничего |
| `kod_rasshifrovka.md` | отчёт ×2 | **НЕ трогал** | второй `## ОТЧЁТ, добавление: АКТ 1 ОБРАБОТАН…` — настоящий второй отчёт на 40+ строк (пришла новая запись в тот же день, исполнитель её честно обработал вторым проходом); живой контент, красное остаётся законно |
| `kod_dobivka-registracii.md` | вопросы ×2, отчёт ×2 | **вопросы снял, отчёт НЕ трогал** | первая `## ВОПРОСЫ` — байт-в-байт пустая (как в файле 2), безопасно снял; вторая `## ОТЧЁТ` — короткая, но живая строка `**КОММИТ:** …` (хэш добавлен ПОСЛЕ факта коммита, ровно как требует канон «свой хэш заранее знать нельзя») — не шаблон, оставил |

Правило «плейсхолдер в `<>`» буквально проверил на файле 1; для файлов 2 и 4 применил чуть более широкий, но строго безопасный критерий того же духа — байт-в-байт пустой дубль без плейсхолдера тоже машинно доказуемо безопасен снять (диф = 0 символов), в отличие от файлов 3 и живого блока файла 4, где дубль содержит уникальный текст.

**Живой прогон по корпусу (критерий 4).** Изолированный вызов `check_z9_edinstvennost` (без остальных семи проверок) по `_studio/zhurnal/**/kod_*.md` — **проверено 157 из 157**, красных 17 (после моих правок; было 19 до них — `kod_vlitie-i-lok.md` и `kod_zapusk-korpusa.md` ушли из списка). Этот файл-заход (`kod_edinstvennost-sekcij.md`) в списке красных нет.
Поимённо, 17: `kod_l4-l5-kontent.md`, `kod_vidy-obzor.md`, `kod_brillianty-l1.md`, `kod_poisk-listochki.md`, `kod_dobivka.md`, `kod_matvychitka.md`, `kod_pereverstka.md`, `kod_podgonka.md`, `kod_rasshifrovka.md` (названный, разобран — живой), `kod_dobivka-registracii.md` (названный, разобран частично — живой остаток), `kod_konsolidacia.md`, `kod_razbros-po-fazam.md`, `kod_razmetka-vladelca.md`, `kod_svedenie-vetok.md`, `kod_final.md`, `kod_sayt.md`, `kod_vynos-meta.md` (полные пути — в `## ВОПРОСЫ` п.1).

**Красных осталось 17, причина у каждого (критерий 5):**
- 2 (`kod_rasshifrovka.md`, `kod_dobivka-registracii.md`) — названы в §2.3, разобраны глазами, дубль живой, не снимается по правилу «не терять чужую работу».
- 15 — вне контрактной зоны этого захода (зона — строго 4 названных файла), не трогал по ЗАПРЕТУ; находка сверх замера аналитика, см. `## ВОПРОСЫ` п.1. Ноль не требуется (критерий 5 это прямо допускает) — красное честное, не подчищенное.

**Как проверил.** Каждый шаг — командой с фактом на диске (см. блоки выше), не пересказом. `sh PROGNAT.sh` → `ФИКСТУРЫ ЗЕЛЁНЫЕ`, `RC=0` (гонял дважды: до и после §2.3, плюс автоматически хуком при коммите). `git_zona.py check --zone _generator/tools/check_zahod.py` и `--zone _generator/tools` → ✅ после коммита.

**Что НЕ трогал.** Другие проверки З1–З8 и их формулировки (СТОП ДО ЦЕЛИ). Заходы, которые сейчас работают у исполнителей (план заполнен/отчёт пуст) — проверил статус всех 4 названных: у всех и ПЛАН, и ОТЧЁТ заполнены, ни один не активен. 15 файлов сверх названных в §2.3. `bootstrap_zahod.py` (кроме чтения), `karta_domov.py`, `dobor_incidentov.py`, репозиторий `disciplina`.

**Результат верификатора (§3 — счёт строк, вместо отдельного субагента, как позволяет задание):**
| файл | до (родитель коммита) | после | Δ |
|---|---|---|---|
| `kod_vlitie-i-lok.md` | 233 | 226 | −7 (снят шаблонный блок задача+критерий+верификатор) |
| `kod_zapusk-korpusa.md` | 92 | 90 | −2 (снята пустая дубль-строка вопросов) |
| `kod_dobivka-registracii.md` | 202 | 200 | −2 (снята пустая дубль-строка вопросов) |

Живого контента не потеряно ни в одном: все три диффа — ровно шаблонные/пустые строки, снятые по правилу §2.3.

**Критерий готовности — по пунктам:**
1. ✅ Влитие §0.1 состоялось — оба числа (`vlit`=24, ловушек=20) и файл `pokrytie_rychagami.py` сошлись, всё в отчёте выше.
2. ✅ `sh PROGNAT.sh` → `ФИКСТУРЫ ЗЕЛЁНЫЕ`, `RC=0`; ловушек было 16, стало 23 (числа командой — `grep -cE '^#[[:space:]]+[0-9]+\.'`); все прежние 20 остались зелёными (гонял фикстуру целиком дважды вручную + дважды хуком при коммитах).
3. ✅ Три новые ловушки (21 задвоенная задача — красная поимённо, 22 здоровый заход — молчит, 23 свежий `bootstrap_zahod.py`-скелет — молчит, положительный контроль) на месте, каждая проверяет своё.
4. ✅ Живой прогон на корпусе: 157 из 157 `_studio/zhurnal/**/kod_*.md`, 17 красных поимённо (список выше). Этот файл-заход в красных отсутствует.
5. ✅ После разбора §2.3: 17 осталось красными — 2 названных (живой контент, не снимается) + 15 вне зоны (находка сверх замера аналитика, решение владельца). Ноль не требовался и не достигнут — честно.
6. ✅ `git_zona.py check --zone _generator/tools` → «работа доехала в git, вне git ничего нет».

**Открытое «возвращаться».** Да, частично: 15 (+2 законно оставленных из названных) файлов сверх §2.3 — решение владельца (см. `## ВОПРОСЫ` п.1), останется ли это разбираться отдельным заходом.

**Время прогона + токены:** снимает приёмка из лога прогона (`result`-строка).

**АРТЕФАКТ:** артефакта нет — заход кодовый (линтер + фикстура), собранного HTML/PDF/документа не производит.

🔴 **НЕОБРАТИМОЕ:**
- Собственная правка-баг: при редакции `## ПЛАН` этого самого файла-захода (редакция №2, до начала работы) случайно снёс заголовки `## ВОПРОСЫ`/`## ОТЧЁТ` целиком (не только содержимое) — заметил сразу же первой проверкой З9/З7 на самом файле, восстановил оба заголовка тем же ходом. Восстанавливать из git не пришлось: правка и починка — в одной незакоммиченной сессии редактирования этого файла (сам файл-заход не коммитится по контракту зоны).
- Больше необратимого нет: удалений/перезаписей/`checkout`/`reset` поверх несохранённого не было; все правки в `_generator/tools/`, `PROGNAT.sh` и трёх `kod_*.md` — аддитивные/точечные, все закоммичены и видны в `git show`.

**КОММИТ:** два коммита в `zahod/edinstvennost-sekcij`. `6ffb496` — «check_zahod: З9 — секции заходов встречаются ровно по одному разу» · 5 путей (все свои — `check_zahod.py`, `PROGNAT.sh`, `kod_vlitie-i-lok.md`, `kod_zapusk-korpusa.md`, `kod_dobivka-registracii.md`). `266dd85` — «check_zahod: З9 — не считать КРИТЕРИЙ ГОТОВНОСТИ внутри backtick-цитаты (найдено калибровкой на собственном заходе)» · 1 путь (`check_zahod.py`). Оба — `git_zona.py check --zone _generator/tools` → ✅ (проверено после второго коммита, покрывает оба файла зоны разом). `git show --stat` на обоих — только свои пути. Хук `pre-commit` сам прогнал `PROGNAT.sh` при каждом коммите — зелёный оба раза, обходов не потребовалось.

---

## ПРИЁМКА — заполняет аналитик, 2026-08-07

**ПРИНЯТ.** Два коммита: `6ffb496` (З9 + фикстура + три файла) и `266dd85` (правка счёта по backtick-цитатам).

**Перепроверено приёмкой своим прогоном** — код взят с ветки, запущен по всему корпусу:

| проверка | результат |
|---|---|
| `check_z9_edinstvennost` в `HARD_CHECKS` | ✅ |
| `_v_backtike()` на месте | ✅ |
| ловушек в фикстуре | ✅ **23** (было 16) |
| этот файл-заход в красных | ✅ **нет** |
| прогон по корпусу | 172 файла, **19** красных |

Расхождение с его 17 объяснено и дефектом не является: мой прогон идёт по основной папке, где две его правки ещё не влиты. **Ни один из шестнадцати заходов, собранных сегодня, не красный** — проверено поимённо.

### 🔴 Мой замер был занижен вчетверо, и это главное в отчёте

Собирая заход, я прошёл глазами по 167 файлам и объявил: дубли у четырёх. Живой прогон нашёл **17**. Не пойманы **76 %**, причём десять файлов задвоены одинаково — `## УРОКИ ФАБРИКЕ` дважды.

Опасность была не в числе, а в роли: **«четыре» служило калибровочной опорой**. Подгони исполнитель правило под мой замер — оно вышло бы узким и молчало на большинстве настоящих дефектов. Спасло то, что заход отдельным условием требовал живого прогона по всему корпусу.

→ урок 36: `KONSTITUCIYA §10` записана про отчёты исполнителей и не распространена на **замеры аналитика при сборке захода**. А считает он глазами по корпусу в 167 файлов, то есть там, где глаз заведомо не справляется.

### Он поймал ту же ловушку на себе, третий раз за два дня

Дописав `## ОТЧЁТ`, прогнал на нём свежую З9 — красное: «КРИТЕРИЙ ГОТОВНОСТИ» трижды. Причина: отчёт **цитировал строку в backtick**, обсуждая саму проверку. Заголовки `##` он заякорил сразу, а `KRITERIJ_RE` заякорить нельзя — шаблон ставит эту строку то первой фразой, то серединой. Починил `_v_backtike()` вторым коммитом.

Это третий случай одного класса (уроки 23, 31, 37): **проверка считает присутствие слова, а не роль, и первым на неё натыкается документ, который её объясняет.** → урок 37: любая новая текстовая проверка калибруется на документе, который её описывает.

### §2.3 — суждение проведено там, где надо

Из четырёх названных файлов **два дубля сняты, два оставлены**. Оставлены правильно: в `kod_rasshifrovka.md` второй `## ОТЧЁТ` — настоящий второй отчёт на 40+ строк (пришла новая запись в тот же день); в `kod_dobivka-registracii.md` второй блок несёт живую строку `**КОММИТ:** …`, дописанную после факта коммита — ровно как требует канон.

Снятые доказаны машинно: буквальные плейсхолдеры в угловых скобках либо байт-в-байт пустой дубль. Счёт строк до/после приложен: −7, −2, −2, живого содержания не потеряно.

### Решение по 15 файлам сверх зоны

**Оставляем красное, отдельный заход сейчас не заводим.** Основания: критерий 5 прямо допускал ненулевой остаток; красное честное, а не подчищенное; и главное — это правка **истории закрытых заходов**, а гейт, ради которого правят историю, плохой гейт.

Практический довод: десять из пятнадцати — один класс (`## УРОКИ ФАБРИКЕ` ×2), то есть работа механическая и дешёвая, но приоритета над содержательными долгами не имеет. Вернёмся, когда освободится окно; записано в открытые задачи.
