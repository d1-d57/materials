# Канал исполнителя — etap-21-ssylki (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — якорные замены по проверенному списку; суждение нужно ровно в одном месте (§2.3), и оно там названо явно.

КОНТЕКСТ. 06.08 фаза иллюстраций вынесена из конвейера презентаций в отдельную фабрику `_illustracii/` — решение владельца: инструмент доводится частотой вызовов, а запертый в слайдах он их не получает. На старом месте `_studio/konvejer/09-illustracii/` остались **четыре файла-указателя** по 15–18 строк, каждый с шапкой «ПЕРЕЕХАЛ».

Ссылки на старый адрес никто не переставил. Они **не битые** — разрешаются в указатель, — но ведут в тупичок вместо дома: читатель приходит, видит «переехало», идёт дальше. Долг записан этапом 21 ночной сборки и открыт до сих пор.

🔴 **Число долга в источниках — «около 45 ссылок» — устарело.** Живой замер аналитика 07.08 (без журналов): **15 файлов, 16 вхождений**. Пересчитай своим прогоном и не бери ни то, ни другое на веру:
```
grep -rln "konvejer/09-illustracii" . --exclude-dir=.git | grep -v "/zhurnal/" | wc -l
grep -ro "konvejer/09-illustracii" . --exclude-dir=.git | grep -v "/zhurnal/" | wc -l
```

ЦЕЛЬ: переставить живые ссылки на `_illustracii/`, оставив журналы нетронутыми.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ.**
- **Журналы закрытых арок не трогать ни одной строкой** (`*/zhurnal/*`). Они описывают раму, какой она была, и переписывать историю нельзя.
- **Файлы-указатели в `09-illustracii/` не удалять.** Они и есть страховка для ссылок, до которых ты не дотянешься.
- Содержание файлов не править — только адрес в ссылке.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/wt-ssylki`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/etap-21-ssylki`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add wt-ssylki --branch zahod/etap-21-ssylki
  cd ../materials-wt/wt-ssylki
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `krivaya-drakona/` `obzory/` `sayt/` `teorkat-vvedenie/` `teorkat/` `teoriya-kategoriy/` `lsh-2026-perechislitelnaya/` `_studio/konvejer/` — и внутри них **только файлы, содержащие искомую ссылку**. Замер 07.08; список мог измениться, поэтому прогони команду из КОНТЕКСТА и **выпиши получившийся список файлов в `## ПЛАН` до первой правки**. Твой прогон дал путь вне восьми перечисленных — **в отчёт, не правь**: расширение зоны решает приёмка, а не исполнитель.
  🔴 **Вне зоны и трогать нельзя:** `*/zhurnal/*` (все журналы), `_generator/tools/` и `_studio/zhurnal/2026-08-05_faza-lenty/` — там прямо сейчас работают три других захода. Наткнулся на ссылку в этих местах — **в отчёт, не правь**.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/etap-21-ssylki`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: зона не чиста в HEAD — закоммить как есть; чиста — не фабрикуй.
- **Прочитать ТОЛЬКО это:**
  1. Четыре указателя `_studio/konvejer/09-illustracii/*.md` — по 15–18 строк. Каждый называет **свой** новый адрес: это и есть таблица перевода, а не общее правило.
  2. `ls _illustracii/` и `ls _illustracii/fazy/` — куда реально ведут указатели сегодня.
- ПЛАН — в `## ПЛАН` перед действиями, **со списком файлов зоны, снятым командой**.

## 2. ЗАДАЧА

### 2.1 Построить таблицу перевода из указателей, а не из головы

Указателей четыре, и каждый ведёт в своё место. Пример из `DOK.md`: «Дом теперь `_illustracii/fazy/DOK.md`». **Открой все четыре и выпиши таблицу «старый путь → новый»** — она короткая и она источник правды.

⚠ Не все ссылки указывают на файл: часть ведёт на папку `konvejer/09-illustracii/` целиком. Для них новый адрес — корень фабрики `_illustracii/`, а не `fazy/`. Разведи эти два случая в таблице явно.

### 2.2 Переставить — якорной заменой, с abort при ≠1

По каждому вхождению: замена по точному якорю, **abort, если якорь встретился не ровно один раз**. Замен столько же, сколько вхождений; расхождение — повод остановиться, а не подогнать.

🔴 **Правится адрес, не текст вокруг.** Фраза «см. `konvejer/09-illustracii/DOK.md`» становится «см. `_illustracii/fazy/DOK.md`» — и ничего больше. Соблазн «заодно причесать» здесь особенно велик, потому что файлы старые; ему не поддавайся, это чужая зона по смыслу, даже когда она твоя по путям.

### 2.3 🔴 Ссылки в чужих проектах — единственное место, где нужно суждение

Часть вхождений лежит в папках курсов (`krivaya-drakona/`, `teorkat-vvedenie/`, `obzory/` и других). Формально это `materials`, и путь у них тот же. Но по смыслу это **чужой материал**: там ссылка могла стоять в тексте лекции, в разборе, в конспекте — то есть в готовом артефакте, который кто-то читал и на который ссылались.

**Правило:** переставляй, если ссылка **техническая** — указывает, где взять дисциплину или инструмент. **Не переставляй и вынеси в отчёт**, если ссылка **историческая** — часть рассказа о том, как было устроено тогда.

Различить можно только глазами и по контексту; поэтому: **каждую ссылку в папке курса покажи в отчёте строкой контекста** и назови вердикт. Не уверен — не переставляй, а спроси. Ложная правка в чужой лекции дороже оставшегося тупичка.

### 2.4 Проверить, что не сломал

После всех замен — прогнать проверку битых ссылок по тронутым файлам. Инструмент выбери сам из `_generator/tools/` (там есть проверки ссылок; какой именно применим — посмотри `--help`, не угадывай). Нет подходящего — сделай проверку прогоном: каждый новый путь обязан существовать (`ls`), список проверенных путей в отчёт.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Шесть условий:

1. **Живой прогон на реальном дереве, не на фикстуре — до и после:** `grep -ro "konvejer/09-illustracii" . --exclude-dir=.git | grep -v "/zhurnal/" | wc -l` даёт число ДО и число ПОСЛЕ. Оба в отчёте.
2. Остаток после работы = число ссылок, **сознательно оставленных** по §2.3, и каждая из них названа поимённо с причиной. Остаток без поимённого списка не принимается.
3. **В журналах не тронуто ничего:** `git --no-optional-locks show --stat` не содержит ни одного пути с `/zhurnal/`. Это машинная проверка, а не обещание.
4. Каждый новый путь существует на диске (`ls`), битых — 0.
5. Файлы-указатели `09-illustracii/*.md` целы: `ls` показывает те же четыре файла, `git show --stat` их не содержит.
6. `git_zona.py check --zone .` → ✅.

**Охват — числом.** «Переставлено X, оставлено Y с причиной, проверено X+Y» — ровно эта форма, все три числа команд��й.

## 3. ВЕРИФИКАТОР

Не нужен: операция обратима целиком (`git revert` одного коммита), потерь по построению нет — заход только заменяет строки, ничего не двигает и не удаляет. Условия 1–5 проверяются машинно тем же ходом.

⚠ Одно исключение, где верификатор был бы полезен и где ты его заменяешь сам: **правки в папках курсов**. По каждой такой правке приложи в отчёт строку ДО и строку ПОСЛЕ целиком — приёмка будет читать их глазами, потому что машинно «уместность ссылки в лекции» не проверяется ничем.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `krivaya-drakona/`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `krivaya-drakona/`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Таблица перевода** (из четырёх указателей `_studio/konvejer/09-illustracii/{DOK,PRIMERY,SLOVAR-primitivov,ZAHOD}.md` + `ls _illustracii/` `ls _illustracii/fazy/`):

| Старый адрес | Новый адрес |
|---|---|
| `.../09-illustracii/DOK.md` | `_illustracii/fazy/DOK.md` |
| `.../09-illustracii/PRIMERY.md` | `_illustracii/fazy/PRIMERY.md` |
| `.../09-illustracii/ZAHOD.md` | `_illustracii/fazy/ZAHOD.md` |
| `.../09-illustracii/SLOVAR-primitivov.md` (в т.ч. `SLOVAR-primitivov.md §<раздел>` — раздел сохраняет имя) | `_illustracii/DISCIPLINA.md` (тот же `§<раздел>`) |
| `.../09-illustracii/` целиком (папка, без файла) | `_illustracii/` (корень фабрики) |

Живьём встретился только последний тип адреса из таблицы — `SLOVAR-primitivov.md` (голый или с `§`); остальные три файла и адрес папки целиком в текущем дереве не встретились ни разу (проверено тем же прогоном).

**Живой замер долга (прогон из КОНТЕКСТА, свой, не источник и не «15/16» аналитика):**
```
grep -rln "konvejer/09-illustracii" . --exclude-dir=.git | grep -v "/zhurnal/" | wc -l   → 14 файлов
grep -ro  "konvejer/09-illustracii" . --exclude-dir=.git | grep -v "/zhurnal/" | wc -l   → 15 вхождений
```
🔴 Расходится и с «~45» источника, и с «15 файлов, 16 вхождений» аналитика 07.08 — не подгонял ни под одно, посчитал заново и работаю от своего числа.

**Список файлов зоны** (снят той же командой, разложен по правилу «зона = восемь папок из контракта»):

*В зоне (11 из 12 попали в один из восьми списков, 12-й — `lsh-2026-perechislitelnaya/` тоже в списке):*
1. `teoriya-kategoriy/SPEKA.md`
2. `krivaya-drakona/L2/strelka-i-tochka.html`
3. `krivaya-drakona/razbor/gen_ill.py`
4. `krivaya-drakona/lekciya/gen_ill.py`
5. `krivaya-drakona/lekciya/_head.py`
6. `obzory/dika-v-vysshey-matematike/src/01-temperli-lib.md`
7. `obzory/dika-v-vysshey-matematike/src/view.html`
8. `obzory/sluchajnye-bluzhdaniya-na-gruppah/zahod/ZAHOD.md`
9. `teorkat/lekciya-1.html`
10. `teorkat-vvedenie/src/dist/index.html`
11. `sayt/static/teorkat/lekciya-1.html`
12. `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/SESSIYA.md`

*Вне восьми перечисленных папок зоны (путь есть, но зона его не покрывает — по контракту «в отчёт, не правь», расширение решает приёмка):*
- `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` (2 вхождения — `_studio/docs/`, а не `_studio/konvejer/`)
- `_studio/docs/pochemu-i-videnie/RESHENIYA.md` (1 вхождение — туда же)

Итого 12 файлов / 12 вхождений в зоне (единственный файл с 2 вхождениями — `OTKRYTYE-ZADACHI.md` — вне зоны), 2 файла / 3 вхождения вне зоны. 12+3=15 — сходится с живым замером.

Порядок работы: §2.2 (якорная замена, abort при ≠1 в файле) → §2.3 (для всех 12 файлов зоны — они все лежат в «папках курсов»/проектов, ни один не в `_studio/konvejer/`, так что суждение нужно по каждому) → §2.4 (проверка).

## ВОПРОСЫ — (заполняет исполнитель)

> Не про задачу этого захода — находка про инструментарий фабрики.
1. `_generator/tools/check_kurs.py --help` не печатает справку, а падает трейсбеком: аргумент `--help` не распознаётся как флаг argparse, парсер пытается открыть файл по пути `--help/kartoteka/KARTA-OBLASTI.md` и роняет `FileNotFoundError`. Обнаружено при попытке §2.4 «посмотри `--help`, не угадывай» — из пяти вероятных кандидатов (`check_kurs.py`, `sostoyanie.py`, `korni.py`, `check_view.py`, `check_ves.py`) ни один не оказался нужной проверкой битых ссылок; на `check_kurs.py` это выяснилось только через трейсбек, а не через справку. Не блокирует: заход сам предусмотрел fallback (§2.4, «нет подходящего — прогон `ls`»), им и воспользовался.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: etap-21-ssylki
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + зачем.** Переставил живые ссылки со старого адреса `_studio/konvejer/09-illustracii/...` на новый дом фабрики иллюстраций `_illustracii/...` в 11 файлах (12-й в зоне намеренно не тронут — см. §2.3 ниже). Цель — не оставлять читателя в тупичке указателя, когда дом уже переехал.

**§2.2 — якорная замена, abort при ≠1.** По каждому из 11 файлов перед правкой проверил `grep -c "konvejer/09-illustracii" <файл>` → у всех ровно 1; правил Edit-ом с точным якорем (в трёх файлах — `teorkat/lekciya-1.html`, `teorkat-vvedenie/src/dist/index.html`, `sayt/static/teorkat/lekciya-1.html` — старый адрес был перенесён по строке из-за ширины комментария, якорь взят на весь двухстрочный фрагмент, новый адрес короче и уместился в одну строку). Abort ни разу не сработал — расхождений не было.

**§2.3 — суждение по ссылкам в чужих проектах.** Все 12 файлов зоны лежат в папках проектов/курсов (`_studio/konvejer/` живых вхождений, кроме самих указателей, не дало вовсе). До/после и вердикт по каждой:

1. `teoriya-kategoriy/SPEKA.md:428` — ДО: `...словарь примитивов ⁠`../_studio/konvejer/09-illustracii/SLOVAR-primitivov.md`.` ПОСЛЕ: `...словарь примитивов ⁠`../_illustracii/DISCIPLINA.md`.` — **техническая** (список канона проекта, где взять инструмент) → переставлено.
2. `krivaya-drakona/L2/strelka-i-tochka.html:29` — ДО: `Примитивы — по _studio/konvejer/09-illustracii/SLOVAR-primitivov.md` ПОСЛЕ: `Примитивы — по _illustracii/DISCIPLINA.md` — **техническая** (комментарий-источник для генератора) → переставлено.
3. `krivaya-drakona/razbor/gen_ill.py:8` — ДО/ПОСЛЕ та же пара, что и в п.2 (докстринг генератора) — **техническая** → переставлено.
4. `krivaya-drakona/lekciya/gen_ill.py:13` — то же — **техническая** → переставлено.
5. `krivaya-drakona/lekciya/_head.py:13` — то же — **техническая** → переставлено.
6. `obzory/dika-v-vysshey-matematike/src/01-temperli-lib.md:339` — ДО: `...Иллюстрации нарисованы (4 из 4) по словарю примитивов ⁠`_studio/konvejer/09-illustracii/SLOVAR-primitivov.md`.*` ПОСЛЕ: тот же текст с `_illustracii/DISCIPLINA.md` — служебная пометка «текст закрыт», не часть нарратива лекции — **техническая** → переставлено.
7. `obzory/dika-v-vysshey-matematike/src/view.html:483` — тот же абзац, собранный HTML-вид — **техническая** → переставлено.
8. `obzory/sluchajnye-bluzhdaniya-na-gruppah/zahod/ZAHOD.md:131` — ДО: `Иллюстрации, если понадобятся, — инлайновый SVG по словарю ⁠`_studio/konvejer/09-illustracii/SLOVAR-primitivov.md`, и место...` ПОСЛЕ: адрес заменён — это ТЗ будущей работы (файл-заход), не рассказ о прошлом — **техническая** → переставлено.
9. `teorkat/lekciya-1.html:174-175` — ДО: `...канон — ⁠`_studio/konvejer/09-illustracii/\n   SLOVAR-primitivov.md §ФОН РИСУНКА`)...` ПОСЛЕ: `...канон — ⁠`_illustracii/DISCIPLINA.md §ФОН РИСУНКА`)...` — комментарий к CSS-правилу — **техническая** → переставлено.
10. `teorkat-vvedenie/src/dist/index.html:174-175` — тот же фрагмент (собранный дубль) — **техническая** → переставлено.
11. `sayt/static/teorkat/lekciya-1.html:135-136` — тот же фрагмент (опубликованный дубль) — **техническая** → переставлено.
12. `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/SESSIYA.md:554` — ДО и ПОСЛЕ одинаковы (не тронуто): `### Записано в фабрику\nПравила рисунка (критерий 0,6, «чинить рисунок, а не правило», подписи к краю — предмет по центру) внесены в ⁠`_studio/konvejer/09-illustracii/SLOVAR-primitivov.md`. Диалект движка — в ⁠`_generator/README.md`.` — это датированная запись сессионного дневника (`## 2026-07-24 (ночь, восьмой заход)` идёт следующим заголовком), фиксирует, куда правило было записано В ТОТ МОМЕНТ — **историческая**, часть рассказа о том, как было устроено тогда → **не переставлено**, оставлено сознательно.

**§2.4 — проверка.** Инструмента для проверки битых ссылок по адресу в `_generator/tools/` не нашлось (`--help` пяти кандидатов — `check_kurs.py`, `sostoyanie.py`, `korni.py`, `check_view.py`, `check_ves.py` — ни один не проверяет произвольные markdown/html-ссылки на файл; находка про `check_kurs.py --help` — в `## ВОПРОСЫ` п.1). Сделал прогоном: единственный новый адрес, встретившийся во всех 11 правках, — `_illustracii/DISCIPLINA.md`; `ls -la _illustracii/DISCIPLINA.md` → файл существует (25543 байт). Проверенные пути: `_illustracii/DISCIPLINA.md` (11/11 правок ссылаются ровно на него, сверено `grep`). Битых — 0.

**Что НЕ трогал:**
- Журналы (`*/zhurnal/*`) — не задевал ни строкой; `git diff --stat` перед коммитом это подтвердил.
- Четыре файла-указателя `_studio/konvejer/09-illustracii/*.md` — не удалял, не редактировал; `ls` после работы показывает те же четыре, `git show --stat` их не содержит.
- `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` (2 вхождения, строки 267 и 515) и `_studio/docs/pochemu-i-videnie/RESHENIYA.md` (1 вхождение, строка 90) — путь есть, но `_studio/docs/` не входит в зону (зона называет `_studio/konvejer/`, не весь `_studio/`); по контракту — в отчёт, не правил. Расширять зону не мой вызов.
- `lsh-2026-perechislitelnaya/.../SESSIYA.md:554` — см. §2.3 п.12 выше, историческая ссылка.
- Содержание файлов вокруг адреса — только сама ссылка, ничего больше (проверил `git diff --stat`: 11 файлов, 11 вставок / 14 удалений — три «двухстрочных» правки схлопнули строку, остальные восемь строго 1↔1).
- `_generator/tools/`, `_studio/zhurnal/2026-08-05_faza-lenty/` — вне зоны и запрещены явно; вхождений там и не было (не встретились в живом прогоне).

**Вопросы:** см. `## ВОПРОСЫ` п.1 (мимоходная находка про `check_kurs.py --help`, не блокирует).

**Верификатор:** не нужен по §3 (обратимо одним `git revert`), кроме ручной сверки правок в папках курсов — она сделана построчно выше (§2.3, до/после по всем 12).

**Критерий готовности — все шесть условий:**
1. Живой прогон, реальное дерево: ДО = 15, ПОСЛЕ = 4 (команда из КОНТЕКСТА, без фикстур).
2. Остаток = 4, поимённо: 1 по §2.3 (историческая, `lsh-2026-perechislitelnaya/.../SESSIYA.md:554`) + 3 вне зоны (`_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` ×2, `_studio/docs/pochemu-i-videnie/RESHENIYA.md` ×1) — оба списка поимённо выше.
3. Журналы не тронуты: `git --no-optional-locks show --stat` для коммита `5f368c0` не содержит ни одного пути с `/zhurnal/` (список путей — 11 файлов зоны, ни один не в journal-папке).
4. Новые пути существуют: `_illustracii/DISCIPLINA.md` — `ls` подтвердил, битых 0.
5. Указатели целы: `ls _studio/konvejer/09-illustracii/` → `DOK.md PRIMERY.md SLOVAR-primitivov.md ZAHOD.md` (те же четыре), в `git show --stat` коммита не встречаются.
6. `git_zona.py check --zone .` → ✅ (прогнан из воркчата после коммита).

**Охват — числом.** Переставлено 11, оставлено 4 с причиной (1 по §2.3 + 3 вне зоны), проверено 11+4=15 — сходится с живым замером ДО.

**Открытое «возвращаться»:** нет — операция целиком обратима одним `git revert 5f368c0`, повторный живой прогон (`grep -ro "konvejer/09-illustracii" ...`) в любой момент покажет актуальный остаток.

**Время прогона + токены:** снимает приёмка из лога (`/usage`, `result`-строка) — исполнителю счётчик недоступен.

**НЕОБРАТИМОЕ:** необратимого нет — только 11 строковых правок адреса, без удалений/перемещений/сброса; всё в git, откат — `git revert 5f368c0`.

**АРТЕФАКТ:** артефакта нет — заход не собирает HTML/документ/PDF, только правит адреса ссылок в исходниках.
**КОММИТ:** `5f368c0` — `etap-21-ssylki: переставить живые ссылки konvejer/09-illustracii на _illustracii (11 правок)` · `git_zona.py check --zone .` → ✅

---

## ПРИЁМКА — заполняет аналитик, 2026-08-07

**ПРИНЯТ.** Коммит `5f368c0`, 11 файлов, 11 вставок / 14 удалений (три двухстрочных фрагмента схлопнулись — новый адрес короче).

**Перепроверено приёмкой своими командами:**

| условие | результат |
|---|---|
| журналы не тронуты | ✅ путей с `/zhurnal/` в коммите — **0** |
| указатели целы | ✅ путей с `09-illustracii` в коммите — **0**, все четыре файла на месте |
| остаток | ✅ ровно **4** в трёх файлах, совпадает поимённо с заявленным |
| новый адрес существует | ✅ `_illustracii/DISCIPLINA.md`, 25543 байта |
| находка про `check_kurs.py --help` | ✅ воспроизведена дословно: `FileNotFoundError: '--help/kartoteka/KARTA-OBLASTI.md'` |

**Адрес взят из указателя, а не угадан** — и это важнее, чем кажется. `SLOVAR-primitivov.md` переехал не в `_illustracii/fazy/`, как три его соседа, а в `_illustracii/DISCIPLINA.md`; заход прямо велел строить таблицу перевода из указателей, и исполнитель это сделал. Угадывание по образцу соседей дало бы 11 битых ссылок при зелёном отчёте.

### Суждение §2.3 — сделано верно, и граница проведена там, где надо

Двенадцать вхождений в папках курсов, по каждому вердикт со строкой контекста. Одиннадцать признаны **техническими** (докстринг генератора, комментарий к CSS, служебная пометка «текст закрыт», ТЗ будущей работы) и переставлены.

Одно оставлено **историческим** — `lsh-2026-perechislitelnaya/.../SESSIYA.md:554`, запись сессионного дневника «Записано в фабрику: правила рисунка внесены в …». Она фиксирует, куда правило было записано **в тот момент**; переставить адрес значит переписать факт прошлого. Ровно тот случай, ради которого правило §2.3 и вводилось.

### Три вхождения вне зоны — не тронуты, и это верно

`_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` (×2) и `_studio/docs/pochemu-i-videnie/RESHENIYA.md` (×1). Зона называла `_studio/konvejer/`, а не весь `_studio/`. Исполнитель сформулировал точно: *«расширять зону не мой вызов»* — и это правильное поведение, а не недоработка.

**Долг остаётся открытым:** три ссылки в каноне ведут на указатель. Закрывается одной строкой в любом будущем заходе, чья зона включает `_studio/docs/` — через дверь `register_doc.py` они не идут, это правка тела.

### Урок 26

`--help` не отвечает у части инструментов `_generator/tools/`, а канон в каждом заходе требует «сверь `--help`, не угадывай» — то есть предписывает то, что местами не работает. Вердикт: `check_tool_contract.py` уже судит инструменты по контракту, добавить ему проверку «`--help` даёт `rc=0` и непустой вывод» прогоном по всей папке.
