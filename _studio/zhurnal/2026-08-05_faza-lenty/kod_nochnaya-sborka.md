# Канал исполнителя — nochnaya-sborka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — ты ОРКЕСТРАТОР, а не работник: решения о порядке, о том, что считать провалом этапа, и о том, идти ли дальше, рождаются по ходу.

КОНТЕКСТ. `materials/` — монорепо. За ночь 06.08 приняты шесть заходов: инструменты научены многим корням (реестр `_generator/tools/korni.py`), иллюстрации вынесены в `_illustracii/`, мета-дисциплина в `_meta/`, инциденты разложены по пяти домам (`KARTA-INCIDENTOV.md`, арифметика сошлась), разведаны швы ленты и ресёрча, заведён репозиторий-плагин `~/Documents/GitHub/disciplina` с одним рабочим скиллом.

ЦЕЛЬ: **довести конструкцию до состояния, в котором владелец утром сядет и за час доведёт её до рабочей.** Не «сделать всё», а «сделать всё, что можно сделать БЕЗ него, и честно оставить дыры там, где нужен он».

Владелец спит. Спросить нельзя. **Поэтому золотое правило ночи: не уверен — оставь дыру с вопросом, а не заполни собой.** Выдуманная дисциплина хуже отсутствующей: она загрузится и будет исполняться.

Приёмка — по ОТЧЁТУ. Стоп до цели штатен: этапы независимы, закрывай их по одному целиком.

## 🔴 КАК ТЫ РАБОТАЕШЬ — прочитай прежде задачи, это главное свойство захода

**Ты сам работу НЕ делаешь.** Каждый этап §2 ты отдаёшь **свежему субагенту с пустым контекстом**, забираешь у него короткий отчёт и вписываешь в этот файл. Причина не в экономии, а в качестве: канон фабрики (`00-KONVEJER.md`) — *«исполнитель читает ТОЛЬКО пакет шага, работает с пустым контекстом, отдаёт наверх дистиллят, а не сырьё»*. Прочитаешь корпусы сам — к четвёртому этапу будешь работать переполненным контекстом и глупее, чем в начале.

Цикл на каждый этап, без исключений:

1. **Составь субагенту САМОДОСТАТОЧНОЕ задание.** Он не видит ни этого файла, ни истории. В задании: что сделать · какие файлы читать поимённо · чем сдаётся · что ему ЗАПРЕЩЕНО. Текст этапа ниже — твоё сырьё для этого задания, а не готовый промпт: перепиши его так, чтобы он читался с нуля.
2. **Получи отчёт ≤40 строк:** что сделано · чем проверено (команда + `rc`) · что НЕ смог и почему · вопросы владельцу.
3. **Впиши отчёт этапа** в `## ОТЧЁТ` этого файла разделом `### Этап N — <имя>`.
4. **Закоммить** результат этапа: `python3 _generator/tools/git_zona.py commit --zone <зона этапа> -m "<что и зачем>" --push`. **Субагенты не коммитят — коммитишь ты.**
5. Этап провалился — **не чини сам и не бросай остальное**: запиши провал, переходи к следующему. Этапы независимы by design.

⚠ Сам читай только: этот файл, `KARTA-INCIDENTOV.md`, `SHVY-lenta-reserch.md`, `RESERCH-skill-ili-papka.md`. Всё прочее — руками субагентов.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_studio/` `_meta/` `_illustracii/` `_generator/` `README.md` `CLAUDE.md` `_studio/zhurnal/2026-08-05_faza-lenty/kod_nochnaya-sborka.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- 🔴 **ВТОРОЙ РЕПОЗИТОРИЙ.** Ты работаешь ещё и в `~/Documents/GitHub/disciplina` (репозиторий-скиллов) и заводишь файлы в корне `~/Documents/GitHub/`. Там **`git_zona.py` не действует** — он привязан к `materials`; пользуйся обычным `git`. Это не нарушение канона: канон запрещает голый git **в `materials`**, а инструмента для чужого репозитория не существует.
- 🔴 **ИМЯ РЕПОЗИТОРИЯ НЕ МЕНЯТЬ.** Владелец думает назвать его `skills` вместо `disciplina`. **Ночью не переименовывай:** маркетплейс уже зарегистрирован под именем `disciplina`, переименование в четыре утра добавляет отказ там, где его можно не иметь. Вынеси это решение в `## ВОПРОСЫ` — оно стоит одной команды утром.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_studio/` `_meta/` `_illustracii/` `_generator/` `README.md` `CLAUDE.md` `_studio/zhurnal/2026-08-05_faza-lenty/kod_nochnaya-sborka.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти четыре (остальное — субагентами):
  1. `_studio/zhurnal/2026-08-05_faza-lenty/RESERCH-skill-ili-papka.md` — критерий «скилл читают, папку пишут», лимиты скилла.
  2. `_studio/zhurnal/2026-08-05_faza-lenty/SHVY-lenta-reserch.md` — разведка швов: что готово, что нет, карта дублей.
  3. `_studio/zhurnal/2026-08-05_faza-lenty/KARTA-INCIDENTOV.md` — куда какой инцидент едет.
  4. `~/Documents/GitHub/disciplina/skills/illustracii/SKILL.md` — образец готового скилла.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — десять этапов

**Бюджет времени большой (до восьми часов), бюджет токенов — нет.** Считай питоном и грепом, читай точечно. Каждый этап — свежему субагенту.

Порядок обязателен для 1 (первый) и 10 (последний). Между ними — на твоё усмотрение, но зависимости названы.

---

### Этап 1 · УБОРКА И СЛИЯНИЕ

```
python3 _generator/tools/git_zona.py doctor
python3 _generator/tools/git_zona.py poteri
python3 _generator/tools/git_zona.py merge <каждая ветка с непереносённой работой>
python3 _generator/tools/git_zona.py plan       # плейсхолдеры `==` переписать
python3 _generator/tools/git_zona.py commit --push
```

В `~/Documents/GitHub/disciplina` — незакоммиченная правка `marketplace.json`. Там **обычный git**: это другой репозиторий, `git_zona` его не знает.

**Гейт:** `check` → «вне git ничего нет»; `poteri` → «потерь нет» с охватом.

---

### Этап 2 · ИНВЕНТАРИЗАЦИЯ — питоном, до всякого переноса

Заведи `<репо-скиллов>/tools/inventar.py`, который **печатает, а не решает**:

- все установленные скиллы: имя · путь · число файлов · размер · строк в `SKILL.md`;
- какие из них **авторские** (написаны владельцем) и какие внешние — признак назови сам и обоснуй; подсказка: у авторских описание по-русски;
- дубли с `_studio/`: карта уже собрана в `SHVY-lenta-reserch.md §C`, **перепроверь её командой**, а не переписывай;
- что из авторских уже лежит на диске в git, а что только в сессионном бандле.

⚠ Бандл сессионный и живёт по пути вида `~/Library/Application Support/Claude/local-agent-mode-sessions/skills-plugin/…/skills/`. **Найди его сам** (`find` по `SKILL.md`), путь не вписывай по памяти — он с идентификаторами сессии и завтра будет другим.

**Гейт:** скрипт `rc=0`, вывод в отчёт целиком, числа сходятся с `SHVY §C` либо расхождение названо.

---

### Этап 3 · АВТОРСКИЕ СКИЛЛЫ — НА ДИСК

🔴 **Заказ владельца, и он про сохранность:** *«если у меня пропадёт аккаунт в Claude, всё сохранилось бы… чтобы актуальная версия лежала не в облаке, а в папке»*.

Забрать из бандла в репозиторий-скиллов копии авторских скиллов. Кандидаты, названные владельцем: `russian-editor`, `cinematic-longread`, трёхмерная иллюстрация (`threejs`), плюс всё, на что ссылается конвейер: `html-slides-studio`, `content-studio`, `kt-channel-editor`, `math-video-studio`, `popsci-research`, `popsci-narrative`.

🔴 **Это КОПИЯ, а не подключение.** В манифест плагина их не вносить: часть дублирует `_studio/` и при подключении начнёт конфликтовать с ним. Цель этапа — чтобы файлы существовали на диске в git, а не чтобы заработали.

🔴 **Копировать байт в байт, ничего не редактируя.** Каждый — в свою папку, рядом файл `PROISHOZHDENIE.md`: откуда взят, дата, размер, зачем лежит.

⚠ `html-slides-studio` — 7,7 МБ. Проверь `check_ves.py`, не упрётся ли в гейт веса; упрётся — не обходи, назови в отчёте.

**Гейт:** `diff -r` между источником и копией пуст для каждого; число скопированных названо; репозиторий закоммичен.

---

### Этап 4 · МЕТА-СКИЛЛ — ГОТОВЫЙ, не заготовка

Источник: `_meta/disciplina/` — четыре документа, перенесены и приняты.

Размеры: `GIT-disciplina.md` 448 · `RUKOVODSTVO-zahodami.md` 349 · `ARKA.md` 161 · `ISTOCHNIK-PRAVDY.md` 92. **В один `SKILL.md` (лимит 500) не влезут** — реши: несколько скиллов или один с `references/` **на один уровень**. Обоснуй выбор.

Дописывается **только фронтматтер**: `name`, `description` (≤1024, третье лицо, триггеры RU+EN). Форма — `skills/illustracii/SKILL.md`, он уже работает.

**Гейт:** тело каждого `SKILL.md` ≤500 строк (числа назвать); JSON манифеста валиден; содержание побайтово совпадает с `_meta/` кроме фронтматтера — проверить `diff`.

---

### Этап 5 · ИЛЛЮСТРАЦИИ — довести до готового

`skills/illustracii/SKILL.md` уже есть (158 строк). Проверить, что он **полон**: словарь примитивов переехал целиком, ничего не потеряно против `_illustracii/DISCIPLINA.md`.

Плюс: у иллюстраций есть родственный внешний скилл `threejs` (трёхмерные). Владелец не помнит, переносил ли его внутрь. **Проверь и напиши, как есть.** Связывать скиллы ссылкой по имени — законный приём; если связь нужна, назови её, но не выдумывай содержание.

**Гейт:** `diff` содержания против источника пуст либо расхождение объяснено.

---

### Этап 6 · ДОМА ИНЦИДЕНТОВ — на каждый скилл

`KARTA-INCIDENTOV.md`: МЕТА 775 · ЛЕНТА 474 · СЛАЙДЫ 454 · РЕСЁРЧ 429 · ИЛЛЮСТРАЦИИ 107 · спорных 25. Счётчик — `karta_domov.py`.

У каждого дома — свой `POKRYTIE.md`. Образцы формы: `_illustracii/POKRYTIE.md`, `_studio/zhurnal/_INFRA-git/`.

🔴 **Числа в шапках не вписывать руками — писать команду** (`KONSTITUCIYA §10`).

🔴 **Где живут дома — решение владельца, исполняй его:** *«лучше не в materials, чтобы materials остались математическими проектами; структурные — в отдельной папке GitHub»*. То есть дом инцидентов скилла живёт **рядом со скиллом**, в репозитории скиллов, а не в `materials`.

⚠ Но сами корпуса (`KARTOTEKA-problem.md` и прочие) **пока остаются на месте** — их переезд связан с выносом `_studio` (этап 10) и ночью не делается. Дом ссылается на корпус, а не копирует его.

**Гейт:** сумма по домам совпадает с `karta_domov.py`; расхождение = провал.

---

### Этап 7 · АРКИ СКИЛЛОВ — где чинят сам скилл

Владелец: *«есть скилл, а есть отдельно то, как мы работаем над скиллом — арки, как маленький проект»*.

У каждого скилла заводится место для своих арок — работы по его починке. Форма арки известна: `bootstrap_arka.py`, шаблон `_TEMPLATE-arka`.

🔴 **Проверь живым прогоном, что арка в новом месте реально заводится.** Инструменты научены многим корням (реестр `korni.py`) — если новый корень не вписан, `bootstrap_arka` откажет. Вписать корень = одна строка в реестре; понадобилась вторая правка — расшивка неполна, это находка в отчёт.

**Гейт:** `bootstrap_arka.py` заводит тестовую арку в новом месте, `rc=0`; тестовую убрать и назвать в `НЕОБРАТИМОЕ`.

---

### Этап 8 · ЗАГОТОВКИ: РЕСЁРЧ-ЦИКЛ И СЛАЙДЫ

🔴 **Разведка (`SHVY-lenta-reserch.md`) показала: это СБОРКА, а не перенос.** Лента — 6 из 6 пунктов формы красные на живой `LENTA-L2`, гейт ловит 12 %. Ресёрч — 30–40 % комплекта. **Дописывать дисциплину ЗАПРЕЩЕНО.**

- **Ресёрч-цикл** — ресёрч и лента вместе. Владелец: *«они всегда работают подряд; это скилл ресёрча, который выдаёт на выход ленту как артефакт»*. Имя — про ресёрч. Источник: `_studio/konvejer/{01-brief,02-reserch,03-matbaza,04-gibrid-istochnik,06-tekst}/`, `FORMA.md` (331 строка живой дисциплины), `ETALON.md`.
- **Слайды** — остаток `_studio/konvejer/{05,07,08,10}/`. 🔴 **Входной контракт не потерять:** раньше фазы 1–4 он делал сам, теперь **требует готовые артефакты на входе**. Выписать явно, иначе при переписывании исчезнет.

🔴 **ЗАГОТОВКИ НЕ ПОПАДАЮТ В МАНИФЕСТ.** Скилл включается по `description`; заготовка с хорошим описанием начнёт срабатывать и советовать по недописанному. В шапке каждой — `status: ЗАГОТОВКА, не подключать до приёмки владельцем`.

🔴 **Расхождения с внешними скиллами — СОБРАТЬ, НЕ РАЗРЕШАТЬ.** Владелец: *«основу брать оттуда, но при расхождении следуем НАШЕМУ содержанию — мы его дорабатывали»*. Каждое расхождение — строкой в список ему на просуживание. Ни одного не решать самому.

**Гейт:** каркасы есть, в манифесте нет, пометка в шапке стоит; списки расхождений и дыр — отдельными файлами.

---

### Этап 9 · ВХОДНЫЕ ДОКУМЕНТЫ И УРОКИ

Три документа + одна правка правила.

1. **В корень `~/Documents/GitHub/`** — читается первым в любом проекте: куда идти за мета-дисциплиной, где скиллы, где математические проекты. Роутер «ситуация → дом», не эссе.
2. **В корень `materials/`** — опись проектов. **Список брать командой** (`ls -d */`), не глазами.
3. **Дисциплина заведения нового проекта.** Владелец: *«заводим новую папку — это лекция или курс? Должна заводиться карточка проекта, без которой дальше ничего не работает»*. Собрать из `01-brief/DOK.md` (8 полей) и `04.5-intervyu/DOK.md` (механизм разговора). Чего нет — дыра с вопросом, не сочинять.
4. 🔴 **УРОКИ ЕДУТ В ДОМ СВОЕГО СКИЛЛА.** Сейчас в закрытии сессии сказано «в фабрику» — одну. Стало: **урок едет в дом того скилла, который использовался; использовалось несколько — едет в несколько.** Дом правила — `_meta/disciplina/ARKA.md` и `RUKOVODSTVO-zahodami.md`, точное место найди грепом. **Правило без механизма — пожелание** (`KONSTITUCIYA §11`): назови в отчёте, что покраснеет при нарушении. Реализовать не обязан, предложить обязан.

**Гейт:** файлы есть и зарегистрированы там, где дверь обязательна; ни один список не вписан руками.

---

### Этап 10 · ПОДГОТОВКА ВЫНОСА `_studio` — ПЛАН, НЕ ПЕРЕЕЗД

🔴 **Физически `_studio` НЕ переносить.** Причина названа аналитиком владельцу: перенос в другой репозиторий **обрывает историю** — `git log --follow` перестанет отвечать, почему в дисциплине появилась строка. Сохранить историю умеет `git subtree split`, но это операция класса `filter-repo`, а он в борде висит с пометкой «не запускать без бэкапа», и бэкапа нет. Ночью, вслепую — нет.

Что делается вместо: **проверенный план на утро.**

- Прогнать `git subtree split --prefix=_studio` **на КОПИИ репозитория** (`git clone` в `/tmp`), измерить: сколько коммитов уносит, сколько весит, сохраняется ли история файла.
- Написать пошаговый план выноса с точными командами и точкой отката.
- Перечислить всё, что сломается: вшитые пути, ссылки из проектов, хук, реестр корней.

**Гейт:** план существует, прогон на копии выполнен, числа в плане — вывод команд. **В живом репозитории `_studio` не тронут: `git status` это подтверждает.**

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. 🔴 `git_zona.py check` → «вне git ничего нет»; `poteri` → «потерь нет» с охватом. **Главное: утром владелец видит целое дерево.**
2. 🔴 В `## ОТЧЁТ` — **десять разделов `### Этап N`**, включая проваленные и пропущенные. Отсутствие раздела = провал критерия.
3. 🔴 **Ни одной строки дисциплины, сочинённой тобой или субагентом.** Всё дописанное — списком «что и почему». Пустой список — лучший исход.
4. Заготовки этапа 8 в манифест не внесены — проверяется чтением JSON.
5. 🔴 `_studio/` в живом репозитории не тронут (этап 10) — `git status` чист по этому пути.
6. Оригиналы на местах: `_meta/disciplina/`, `_illustracii/`, `_studio/konvejer/` — ничего не удалено. **Копия ≠ удаление источника; удаление источника этой ночью запрещено.**
7. 🔴 **ЖИВОЙ ПРОГОН:** `sh .githooks/pre-commit` → `rc=0`, `sh _generator/tools/fixtures/korni/PROGNAT.sh` → `rc=0`. Известный чужой долг, НЕ чинить: фикстуры `sessiya` и `ves` красные с июля; `dandelin/src` не собирается; 7 сирот в арке `2026-07-28_konspekt-l1`.
8. Вопросы владельцу собраны в `## ВОПРОСЫ` — это его утренняя повестка.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

## 3. ВЕРИФИКАТОР — последним ходом, свежий, метод другой

Ночь длинная, и к утру ты будешь уверен в том, чего не проверял. Поэтому ПОСЛЕ шестого этапа — отдельный субагент с пустым контекстом, которому НЕ давать ни этот файл, ни свои отчёты:

> *«В этом репозитории за ночь переехали дисциплины: иллюстрации в `_illustracii/`, мета в `_meta/`, часть скопирована в плагин `~/Documents/GitHub/disciplina`. Найди: (1) ссылки, ведущие в никуда; (2) документы, объявленные в индексах, но отсутствующие на диске; (3) файлы, добавленные за ночь и не попавшие ни в один индекс. Верни три списка с охватом.»*

Все три списка обязаны быть пустыми либо каждая позиция объяснена в отчёте.

⚠ И отдельная проверка, которую легко забыть: **оригиналы на местах.** `_meta/disciplina/`, `_illustracii/DISCIPLINA.md`, `_studio/konvejer/` — ничего не удалено. Копия в плагин НЕ означает удаление источника; удаление источника этой ночью запрещено.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/` `_meta/` `_illustracii/` `_generator/` `README.md` `CLAUDE.md` `_studio/zhurnal/2026-08-05_faza-lenty/kod_nochnaya-sborka.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/` `_meta/` `_illustracii/` `_generator/` `README.md` `CLAUDE.md` `_studio/zhurnal/2026-08-05_faza-lenty/kod_nochnaya-sborka.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Роль: оркестратор.** Ни один этап §2 не делаю руками — каждый уходит свежему субагенту с самодостаточным заданием, я забираю дистиллят, вписываю в `## ОТЧЁТ`, коммичу сам.

### 🔴 ОСПАРИВАЮ ПРЕДПОСЫЛКУ ВХОДА (до работы, как требует §1)

**§0 ПЕРВЫЙ ХОД неисполним в написанном виде.** Мне предписано прочитать четыре якоря. Двух из них на ветке `arka/mat-kostyak` **не существует**:

```
find . -name 'SHVY*' -o -name 'KARTA-INCIDENTOV*'   # в materials → rc=1, пусто
```

Оба лежат **только в невлитых ветках**, и `git_zona.py doctor` называет их поимённо:

| файл | где физически лежит | ветка |
|---|---|---|
| `SHVY-lenta-reserch.md` | `materials-wt/shvy/…` | `zahod/shvy` |
| `KARTA-INCIDENTOV.md`, `karta_domov.py` | `materials-wt/karta/…` | `zahod/karta-incidentov` |

То есть **этап 1 (слияние) — не «уборка», а предусловие чтения моих же якорей и трёх этапов**: 6 живёт на `KARTA-INCIDENTOV.md` + `karta_domov.py`, 8 — на `SHVY §C`, 2 — на `SHVY §C` для сверки. Порядок «1 первый» в заходе стоит верно, но по другой причине, чем написано, и **читать якоря я обязан ПОСЛЕ этапа 1, а не в §0**. Правку критерия не предлагаю — предлагаю правку §0: якоря, живущие в невлитых ветках, называть вместе с веткой. Это ушло в `## УРОКИ ФАБРИКЕ` с ценой.

**Второе, мельче: §3 сам себе противоречит по времени.** Заголовок — «ВЕРИФИКАТОР — последним ходом», тело — «ПОСЛЕ шестого этапа». Этапы 7–9 заводят новые файлы и новые ссылки, то есть ровно то, что верификатор ищет; запуск после шестого проверил бы не то дерево, которое увидит владелец. **Читаю как «последним ходом» и ставлю верификатор после этапа 9** — это строго сильнее и не противоречит ни одной другой строке захода.

**Что НЕ оспариваю:** критерий готовности в остальном исполним, десять разделов отчёта дам в любом исходе, включая проваленные.

### Порядок и зависимости

Последовательно, не параллельно: этапы 3–5 пишут в один репозиторий `disciplina`, а коммит после каждого этапа — мой, и параллельные субагенты сделали бы его неатомарным. Бюджет времени (8 ч) это позволяет; метрика — качество.

| # | этап | зависит от | зона коммита |
|---|---|---|---|
| 0 | точка отката (зона не чиста в HEAD: 3 правленых + 3 новых в `_studio/`) | — | `materials` |
| 1 | **уборка и слияние** — `doctor` · `poteri` · `merge zahod/shvy` + `merge zahod/karta-incidentov` · `plan` · `commit`; отдельно `marketplace.json` в `disciplina` обычным git | — | `materials` + `disciplina` |
| — | *я читаю два приехавших якоря* | 1 | — |
| 2 | инвентаризация питоном (`inventar.py`, печатает — не решает; бандл искать `find`, путь не помнить) | 1 | `disciplina` |
| 3 | авторские скиллы на диск — **копия байт в байт**, `PROISHOZHDENIE.md` рядом, в манифест НЕ вносить | 2 | `disciplina` |
| 4 | мета-скилл из `_meta/disciplina/` — готовый, не заготовка; 448+349+161+92 в 500 не влезут → выбор формы обосновать | — | `disciplina` |
| 5 | иллюстрации — полнота против `_illustracii/DISCIPLINA.md`; про `threejs` написать **как есть**, связь не выдумывать | 3 | `disciplina` |
| 6 | дома инцидентов — `POKRYTIE.md` на скилл, **рядом со скиллом**, числа командой, корпуса не двигать | 1, 4, 5 | `disciplina` |
| 7 | арки скиллов — **живой прогон** `bootstrap_arka.py` в новом корне, тестовую убрать и назвать в НЕОБРАТИМОЕ | 4, 6 | оба |
| 8 | заготовки ресёрч-цикла и слайдов — **сборка, не перенос**; в манифест НЕ вносить; расхождения СОБРАТЬ, не разрешать | 1 | `disciplina` |
| 9 | три входных документа + правка правила «урок едет в дом своего скилла»; списки — командой | 3–8 | оба |
| — | **верификатор** — свежий, метод другой, ни этого файла, ни моих отчётов | 9 | — |
| 10 | план выноса `_studio` — прогон `subtree split` **на клоне в `/tmp`**, живой `_studio` не трогать | — | `materials` |
| — | финальный коммит зоны + `check --zone` | всё | оба |

### Правила, которые держу на каждом ходу

- **Золотое правило ночи:** не уверен — дыра с вопросом, а не заполнение собой. Каждому субагенту это пишется явным запретом.
- **`rc` первым**, до содержательного вывода — и у себя, и в требованиях к отчёту субагента.
- **Новый `.md` в `_studio/` → `register_doc.py` тем же ходом**, и `_studio/docs/KARTA.md` дописывается к путям в **обоих** ходах коммита.
- **Коммит — два хода с путями после `--`, `--no-optional-locks`.** Никогда `-A`, `.`, `-am`. Субагенты не коммитят.
- `git checkout` не делаю ни при каких обстоятельствах. Репозиторий `disciplina` **не переименовываю** — вопрос в `## ВОПРОСЫ`.
- Вне зоны — только в отчёт. `ucheniki/danya/*` правлен не мной и остаётся нетронутым.
- Этап провалился → записываю провал и иду дальше; этапы независимы.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
