# Канал исполнителя — vynos-meta (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — переносится канон, на который смотрят из четырёх проектов; ошибка молчит и всплывает у следующего исполнителя.

КОНТЕКСТ. `materials/` — монорепо. `_studio/` разделяется на четыре фабрики плюс мета-дисциплина уровнем ВЫШЕ них. Иллюстрации уже вынесены в `_illustracii/` — **это твой образец, повтори его форму**. Инструменты умеют много корней (реестр `_generator/tools/korni.py`).

ЦЕЛЬ: вынести мета-дисциплину в дом `_meta/`. Слово владельца: *«есть общая дисциплина того, как я вообще работаю с Claude — как делаю заходы, коммиты, веду дневник, как устроена каждая арка. Это мета-скилл, который используется всегда… ему совершенно нечего делать внутри студии, он к студии не относится. Это абсолютно универсальная вещь про все мои проекты»*.

Замер, подтверждающий: `RUKOVODSTVO-zahodami.md` зовут **из проектов 40 раз**, `ARKA.md` — 27, причём из `kurs leto 2026` и `catalan`, а не из фабрики. Дисциплина давно универсальна и лежит не там.

Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: переноси документы ПО ОДНОМУ целиком — документ без стаба и без правого адреса хуже непереехавшего.

🔴 **ОДИН РИСК ЗА РАЗ (`KONSTITUCIYA §13`).** Переносим четыре готовых файла целиком. `KONSTITUCIYA.md` **НЕ РЕЖЕМ** — она расщепляется внутри себя (§1–7 продукт-слайды, §8–16 мета), её зовут 38 раз, и резать конституцию тем же ходом, что переносить четыре файла, — два риска в одном заходе. По ней только предложение в отчёт.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/meta`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/vynos-meta`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add meta --branch zahod/vynos-meta
  cd ../materials-wt/meta
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_meta/` `_studio/docs/kak-delat/` `_generator/tools/korni.py`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/vynos-meta`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_meta/` `_studio/docs/kak-delat/` `_generator/tools/korni.py`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы. Проект сверх них не изучай:
  1. `_illustracii/` целиком — **образец дома**: `README.md`, `CLAUDE.md`, `DISCIPLINA.md`, `POKRYTIE.md`, `UROKI.md`, `docs/KARTA.md`, `fazy/`, `zhurnal/`. Повтори форму.
  2. `_studio/konvejer/09-illustracii/SLOVAR-primitivov.md` — **образец стаба**: как выглядит указатель на старом адресе, включая предупреждение про ссылки вида `файл.md §Раздел`.
  3. `_generator/tools/korni.py` — реестр. Новый корень = ОДНА строка.
  4. `_studio/zhurnal/2026-08-05_faza-lenty/RESERCH-skill-ili-papka.md` §6 — вердикт, что из меты позже станет скиллом, а что останется папкой. Тебе он нужен, чтобы разложить файлы по будущему назначению, а не свалить в кучу.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### A. Дом `_meta/` — по образцу `_illustracii/`

```
_meta/
  README.md          что это, когда читают, чего тут НЕ делают
  CLAUDE.md          вход исполнителя
  POKRYTIE.md        инцидент → рычаг (пока заготовка: карта инцидентов собирается соседним заходом)
  UROKI.md           дом находок
  docs/KARTA.md      СВОЙ индекс — §6 может оказаться последним разделом, это уже чинено
  disciplina/        четыре перенесённых документа, см. §B
  zhurnal/           свои арки
```

Корень вписывается **одной строкой в `_generator/tools/korni.py`**. Понадобилась вторая правка в инструментах — расшивка неполна, и это находка в отчёт, а не повод править дальше.

⚠ `POKRYTIE.md` **не сочиняй**. Соседний заход `karta-incidentov` прямо сейчас раскладывает корпус по домам, и мета — самый большой класс (233 записи `вне-фаз`). Заведи файл с шапкой, командой пересчёта и явной строкой «наполняется заходом `karta-incidentov`». Пустая честная заготовка лучше выдуманного содержания.

### B. Четыре документа — переносятся ЦЕЛИКОМ

| откуда | ссылок из проектов | почему мета |
|---|---|---|
| `_studio/docs/kak-delat/GIT-disciplina.md` | 2 | как коммитить — не про презентации |
| `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` | 40 | как писать заход — нужно всем |
| `_studio/docs/kak-delat/ARKA.md` | 27 | как вести арку — нужно всем |
| `_studio/docs/kak-delat/ISTOCHNIK-PRAVDY.md` | 0 | где что живёт — правило про репозиторий |

🔴 **`git mv`, не `cp`+`rm`** — иначе обрывается история, и `git log` перестаёт отвечать, почему в дисциплине появилась клауза.

🔴 **На каждом старом адресе остаётся СТАБ-указатель.** Это решённый приём, он уже отработал на иллюстрациях: 51 внешняя ссылка продолжила разрешаться. Стаб обязан нести **предупреждение про секции**: ссылка вида `ARKA.md §7` теперь ведёт в файл без §7 — назови, куда переехали разделы.

Почему стаб, а не правка 67 ссылок: они лежат вне твоей зоны, в чужих проектах. Выход в них — второй риск в заходе, где и так один.

### C. Маршрут во входных документах

В `materials/CLAUDE.md`, `README.md`, `_studio/COWORK.md`, `_studio/CLAUDE.md`, `_studio/docs/KARTA.md §1` уже стоит строка про иллюстрации — **скопируй её форму** и добавь рядом строку про мету: «как вообще работаем — заход, коммит, арка, дневник → `_meta/`».

⚠ Эти пять файлов лежат ВНЕ твоей зоны. **Не правь их** — выпиши готовые строки в `## ВОПРОСЫ` пунктом очереди с точным адресом вставки. Их поставит следующий заход одним ходом.

### D. `KONSTITUCIYA.md` — предложение, НЕ правка

Документ расщепляется внутри себя: §1–7 «Продукт» — про визуальный канон и деки, §8–16 «Данные и истина / Процесс / Среда» — медиум-независимы, и там все дорогие уроки. Пункт 3 — шов: говорит и про презентацию, и про арки.

**Ничего не режь.** В отчёт: какие пункты куда, где шов, и чем резать безопасно. Владелец решит отдельно.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. 🔴 **Ворота видят новый корень и краснеют на его незарегистрированном файле.** Заведи в `_meta/` `.md` мимо двери → `check_kartoteka.py <картотека> --gates` даёт по нему ❌ с указанием **своего** индекса `_meta/docs/KARTA.md`. **Молчание = провал.**
2. 🔴 **Дверь пишет в индекс СВОЕГО корня:** `register_doc.py _meta/<файл>.md "…"` → `rc=0`, строка легла в `_meta/docs/KARTA.md`, а `_studio/docs/KARTA.md` не тронут ни байтом (`cmp` до/после).
3. 🔴 **Живых битых ссылок ноль.** Каждый старый адрес разрешается — либо в стаб, либо на новое место. Проверить грепом по всему репозиторию, **вердикт с охватом**: «битых 0 из N, проверено N».
4. История цела: `git log --follow _meta/disciplina/ARKA.md` показывает коммиты до переезда.
5. `sh .githooks/pre-commit` зелёный, `sh _generator/tools/fixtures/korni/PROGNAT.sh` зелёный.
6. 🔴 **ЖИВОЙ ПРОГОН на реальном объекте:** `python3 _generator/tools/bootstrap_arka.py` заводит арку в `_meta/zhurnal/` — новый дом работает не на бумаге. Прогон и его вывод в отчёт; заведённую тестовую арку убери и назови в `НЕОБРАТИМОЕ`.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «битых нет», а «битых нет, проверено X из Y».

## 3. ВЕРИФИКАТОР — обязателен: перенос лоссовый

Свежий субагент, пустой контекст, метод другой — искать с нуля, а не перечитывать твой список:

> *«В этом репозитории недавно переехала мета-дисциплина из `_studio/docs/kak-delat/` в `_meta/`. Найди места, которые всё ещё ведут на старый адрес и при этом НЕ являются ни файлом-указателем, ни записью в журнале закрытой арки. Отдельно проверь ссылки вида `ФАЙЛ.md §Раздел` — разрешается ли раздел по новому адресу. Верни списки с охватом.»*

Плюс сверка `git show` для каждого из четырёх: содержимое до переезда против содержимого после — байт в байт.

Нашёл пропущенное — это результат захода, а не провал: строкой в отчёт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_meta/` `_studio/docs/kak-delat/` `_generator/tools/korni.py`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_meta/` `_studio/docs/kak-delat/` `_generator/tools/korni.py`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**ПРЕДПОСЫЛКИ, названные вслух до работы** (проверены по живым файлам, не приняты на слово):

- **П1. Зона чиста в HEAD** — `git status --porcelain -- _meta/ _studio/docs/kak-delat/ _generator/tools/korni.py` пуст, `_meta/` не существует. ⇒ **точку отката НЕ фабрикую** (заход прямо запрещает фабриковать на чистой зоне); откат = `git checkout` ветки захода до первого коммита.
- **П2. Перенос лоссовый не только по внешним ссылкам, но и ИЗНУТРИ файлов — и это главная работа захода.** Все четыре документа лежат на глубине `_studio/docs/kak-delat/` и калиброваны под неё: `../../../_generator/…`, `../KARTA.md`, `../KONSTITUCIYA.md`, `../../CLAUDE.md`, `../../konvejer/…`, `../sostoyanie/…`. В `_meta/disciplina/` глубина другая (`../../` = корень репо), и **каждая такая ссылка станет битой ровно в момент `git mv`**. Заход этого не называет — он говорит «переносятся ЦЕЛИКОМ» и требует критерием 3 «битых ноль». Противоречия нет: правка ссылок ВНУТРИ перенесённых файлов лежит в моей зоне (`_meta/`) и является частью переноса, а не «улучшением заодно». Тела текстов не трогаю — только адреса.
- **П3. `_studio/docs/KARTA.md` править не придётся, и это не удача, а следствие стабов.** §6 регистрирует `kak-delat/ARKA.md` и соседей по ИМЕНИ пути; на старом адресе остаётся файл с тем же именем ⇒ ворота 5 на стабах зелены без единой правки чужого индекса. Критерий 2 (`cmp` до/после) выполним не «аккуратностью», а конструкцией.
- **П4. Ворота у нового корня заводятся ОДНОЙ строкой** — `check_kartoteka.py`, `register_doc.py`, `bootstrap_arka.py`, `bootstrap_zahod.py` берут корни только из `korni.py` (прочитал: `КОРНИ = korni.имена_воротами()`, `korni.карта_для`, `--koren`). Понадобится вторая правка в инструментах — это находка в отчёт, а не повод править дальше.
- **П5. Фикстуру `fixtures/korni/PROGNAT.sh` править не надо** — она строит синтетический репозиторий в `$T` и вписывает свой второй корень сама; живой реестр ей не аргумент.

**КРИТЕРИЙ ГОТОВНОСТИ не оспариваю** — все шесть пунктов исполнимы и способны провалиться. Одно уточнение к пункту 3, которое делаю ЯВНО, а не молча: «битых ноль» считаю по ДВУМ классам сразу — (а) внешние ссылки на старый адрес (лечит стаб), (б) исходящие ссылки изнутри перенесённых файлов (лечит правка адресов, П2). Отчёт даст охват по каждому классу отдельно.

**ШАГИ**

1. **Каркас `_meta/`** по образцу `_illustracii/`: `README.md`, `CLAUDE.md`, `POKRYTIE.md` (честная заготовка, см. §A — не сочинять), `UROKI.md`, `docs/KARTA.md` (свой §6, ПОСЛЕДНИМ разделом), `disciplina/`, `zhurnal/.gitkeep`.
2. **Одна строка в `_generator/tools/korni.py`** — корень `_meta` с картой `_meta/docs/KARTA.md`, журналом `_meta/zhurnal`, воротами, обязательной регистрацией.
3. **`git mv` ×4** в `_meta/disciplina/` — по одному, история сохраняется (`--follow`).
4. **Правка адресов внутри четырёх** (П2). Хирургия: меняется только путь в ссылке, ни одно предложение не переписывается.
5. **Четыре стаба** на старых адресах по образцу `SLOVAR-primitivov.md`, каждый с предупреждением про `ФАЙЛ.md §Раздел` и перечнем, куда уехали разделы.
6. **Регистрация дверью** каждого нового `.md` в `_meta/` — `register_doc.py`, ходом создания.
7. **Гейты** — шесть пунктов критерия, каждый с `echo $?` ПЕРВЫМ.
8. **Верификатор** — свежий субагент промптом из §3 + байтовая сверка `git show` по четырём.
9. **Коммит своей зоны** двумя ходами (§4).
10. Отчёт, `## ВОПРОСЫ` (строки маршрута для пяти входных документов, §C) и предложение по `KONSTITUCIYA.md` (§D) — без единой правки этих файлов.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **§C — маршрут в пяти входных документах. Строки готовы, вставлять одним ходом; все пять файлов ВНЕ моей зоны, я их не трогал.** Форма скопирована со строки про иллюстрации в каждом файле, чтобы соседние строки читались одинаково.

   **1.1 `CLAUDE.md`, строка 12** (раздел «Три вида папок») — вставить между `_illustracii/` и `_generator/`:
   ```
   `_meta/` — **как мы вообще работаем** (заход, коммит, арка, дневник — оттуда)
   ```
   **1.2 `CLAUDE.md`, раздел «Шесть правил» — новым пунктом рядом с п.3:**
   ```
   **⚙️ Пишешь заход, коммитишь, ведёшь арку или дневник — идёшь в `_meta/`.** Как вообще работаем — заход, коммит, арка, дневник — всё оттуда и одним языком; вход — `_meta/CLAUDE.md`.
   ```
   **1.3 `README.md` §2 РОУТЕР, строкой после строки про изображение (стр. 40):**
   ```
   | **⚙️ пишешь заход · коммитишь · ведёшь арку или дневник** | `_meta/` → вход `_meta/CLAUDE.md` | как мы вообще работаем; уровнем выше фабрик — не про презентации и не про картинки |
   ```
   **1.4 `_studio/COWORK.md`, рядом со строкой 45:**
   ```
   **⚙️ ПИШЕШЬ ЗАХОД, КОММИТИШЬ, ВЕДЁШЬ АРКУ — ИДЁШЬ В `../_meta/`** (уровнем выше фабрик; канон захода и приёмки — `../_meta/disciplina/RUKOVODSTVO-zahodami.md`, git — `../_meta/disciplina/GIT-disciplina.md`, арка — `../_meta/disciplina/ARKA.md`). Дисциплина к презентациям не относится: она общая на все проекты.
   ```
   **1.5 `_studio/CLAUDE.md`, в «Дисциплину», рядом со строкой 13:**
   ```
   - **⚙️ Пишешь заход, коммитишь, ведёшь арку — идёшь в `../_meta/`** (уровнем выше фабрик; вход — `../_meta/CLAUDE.md`). Как вообще работаем — оттуда, независимо от того, что сегодня производим.
   ```
   **1.6 `_studio/docs/KARTA.md §1`, строкой после строки 26:**
   ```
   | **⚙️ пишешь заход · коммитишь · ведёшь арку или дневник** | `../../_meta/` — уровнем выше фабрик; вход `../../_meta/CLAUDE.md` |
   ```
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/` (следующий заход очереди)
   ДОСТАВЛЕНО: нет

2. **Те же пять файлов зовут канон по СТАРОМУ адресу — восемнадцать строк, все сегодня разрешаются в стаб, но адрес стухший.** Это не аварийно (стабы держат), но чинится тем же ходом, что и п.1, и дешевле сразу. Точные адреса: `CLAUDE.md:8` (`_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` → `_meta/disciplina/…`), `CLAUDE.md:31` («Весь канон git — …GIT-disciplina.md»), `README.md:41,42,50`, `_studio/COWORK.md:51,56,58`, `_studio/CLAUDE.md:28` (подвал), `_studio/docs/KARTA.md:7,19,20,21,61`. Плюс `_studio/docs/KONSTITUCIYA.md` п.12 и п.14 зовут `kak-delat/GIT-disciplina.md`, `kak-delat/ARKA.md §6`, `kak-delat/RUKOVODSTVO-zahodami.md §Заход` / `§ГЕЙТ ВХОДА`.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/` (тот же заход очереди, что и п.1)
   ДОСТАВЛЕНО: нет

3. **§D — `KONSTITUCIYA.md`: РАЗБОР И ПРЕДЛОЖЕНИЕ. Ничего не резал.** Разбор по живому файлу (54 строки, дата данных 2026-08-06):

   **Куда что.** «Продукт» (п. 1, 2, 5, 6, 7) — чистый продукт: замороженный визуальный канон, самодостаточный HTML 1440×810, плакатный стиль текста, гибрид-источник, render-identity. «Данные и истина» (8, 9, 10 со всеми под-пунктами, 10а), «Процесс» (11, 11а, 11б, 10а-бис, 10в, 10б, 12, 13, 14, 15), «Среда» (16) — медиум-независимы целиком, и там все дорогие уроки.

   **Швов ДВА, а не один.** Заход называет п.3; я нашёл ещё п.4.
   - **п.3** — «Три сцепленные архитектуры: презентация (файлы) · фабрика (арки-стадии) · процесс-память (ARC)». Первая треть — продукт, две остальные — мета.
   - **п.4** — «Фабрика самодостаточна, НЕ дирижёр». Само правило («внешний скилл зовём только там, где способность медиум-независима и инструментальна») универсально; его примеры и хвост «всё специфичное для формата презентаций — локально» — продукт.

   **Замер, который решает вопрос о цене реза** (пересчёт: `grep -rhoE 'KONSTITUCIYA[^\`]{0,4}§ ?[0-9]+[абвг]?' --include='*.md' --include='*.py' . | grep -oE '§ ?[0-9]+[абвг]?' | tr -d ' ' | sort | uniq -c | sort -rn`):

   | половина | ссылок вида `§N` | самые частые |
   |---|---|---|
   | **мета (§8–16)** | **301 из 315 — 96 %** | §10 — 144 · §11 — 66 · §9 — 34 · §14 — 16 · §13 — 13 |
   | продукт (§1–7) | 14 из 315 — 4 % | §1 — 12 · §6 — 1 · §7 — 1 |

   ⇒ Конституцию зовут почти исключительно ЗА медиум-независимую половину. Довод в пользу выноса сильнее, чем у четырёх перенесённых документов, — и ровно поэтому резать её тем же ходом было нельзя.

   **Чем резать безопасно — приём отработан дважды (иллюстрации 06.08, мета сегодня) и здесь применим с одной поправкой.** Это не переезд файла, а расщепление: §1–7 остаются в `_studio/docs/KONSTITUCIYA.md`, §8–16 уезжают в `_meta/KONSTITUCIYA-processa.md`. Поэтому:
   - **номера разделов не менять ни в одной половине** — 315 живых ссылок адресуются номером, и переномерация обрушит их все разом, а стаб от этого не спасает;
   - на старом адресе — не стаб вместо файла, а **таблица маршрутизации в шапке**: «§1–7 здесь · §8–16 → `../../_meta/KONSTITUCIYA-processa.md`, номера те же»;
   - **`git mv` тут не работает** (файл не переезжает, а делится) — историю несёт та половина, которая уедет, только если её вынести через `git mv` целого файла с последующим вырезанием остатка; иначе `git log --follow` по новой половине не ответит. Это и есть главный риск реза, и он заслуживает отдельного захода;
   - п.3 и п.4 при резе **дублируются в обе половины в усечённом виде**, а не отдаются одной: у шва обе стороны несущие.

   Решает владелец; я предлагаю, не режу.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. **Форки канона в двух проектах — дисциплина копировалась, потому что дома у неё не было.** `kurs leto 2026/ARKA.md` (135 строк против 161), `kurs leto 2026/RUKOVODSTVO-zahodami.md` (101 против 349), `catalan/RUKOVODSTVO-zahodami.md` (120 против 349) — самостоятельные редакции, разошедшиеся с каноном; `catalan/ARKA.md` — семистрочный стаб с подписью *«полный канон … портируется отдельным Code-заходом»*, и порт с тех пор не сделан. Вынос дома их НЕ снял: они вне зоны. Зафиксировано с ценой в `_meta/UROKI.md §2`. Сведение — отдельный заход, отдельный риск.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. **Названный долг, унаследованный: битая ссылка внутри перенесённого канона.** `_meta/disciplina/RUKOVODSTVO-zahodami.md:13` зовёт `content-studio/assets/corpus/texts-corpus.md` — такого файла в репозитории нет и не было. Доказано, что долг НЕ мой: `git show HEAD:_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` содержит ту же ссылку до переезда. Не чинил: правильный адрес угадывать нельзя, а править чужой смысл — второй риск.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: vynos-meta#5

6. **Канон печатает команду с путём ОТ ДОКУМЕНТА и подписью «(из корня репо)» — копипаст даёт неверный путь.** `_meta/disciplina/RUKOVODSTVO-zahodami.md:71` и `:309`: `python3 ../../_generator/tools/register_doc.py …` с пометкой «(из корня репо)». Дефект был и до переезда (там стояло `../../../_generator/…` с той же пометкой) — я сохранил обе половины ровно как были, потому что чинить смысл в чужом тексте не моя зона. Цены не знаю ⇒ в `UROKI` не пишу, кладу как наблюдение.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: vynos-meta#6

7. **КРИТЕРИЙ 4 НЕПРОВЕРЯЕМ В ТОМ ПОРЯДКЕ, КОТОРЫЙ ПРЕДПИСЫВАЕТ САМ ЗАХОД — и это ловушка для следующего исполнителя.** §4 говорит «КОММИТ — ПОСЛЕДНИЙ ХОД ЗАХОДА», критерий 4 говорит «`git log --follow _meta/disciplina/ARKA.md` показывает коммиты до переезда». До коммита `--follow` по несуществующему в истории пути возвращает **ноль** — не ошибку, а пустой вывод с `rc=0`. **ЦЕНА в этом прогоне:** прогнал критерий 4 по порядку, получил `0 коммитов` на всех четырёх файлах при `rc=0` и был в одном шаге от того, чтобы записать в отчёт провал критерия. Спасло только то, что пустой вывод при `rc=0` — ровно тот случай, про который предупреждает §1 («код возврата первым»), и я полез разбираться, а не поверил. Лечение — одна строка в критерии: «пункт 4 проверяется ПОСЛЕ коммита, до коммита он пуст по построению».
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: vynos-meta#7

8. **Число из задания не воспроизводится названным инструментом.** §A говорит «мета — самый большой класс (233 записи `вне-фаз`)». Живой прогон `python3 _studio/zhurnal/2026-07-30_dovodka-fabriki/srez_po_faze.py вне-фаз` даёт **58 классов при суммарной кратности 724** (дата данных 2026-08-06); ни 233 записи, ни 233 чего-либо ещё эта команда не печатает. Вывод задания («самый большой класс») подтверждается — ближайший `02-reserch` даёт 312, — а само число 233 взято из какого-то третьего среза и в `POKRYTIE.md` я его не перенёс: вписал команду и то, что она печатает.
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: vynos-meta#8

9. 🔴 **СРОЧНОЕ — ГЕЙТ КРАСНЫЙ ПРЯМО СЕЙЧАС, И ЭТО МОЯ РЕГРЕССИЯ.** `_generator/tools/fixtures/git_zona/PROGNAT.sh:343` держит литеральный путь к канону git; там теперь стаб, и ловушка 6 объявляет непокрытыми все 16 подкоманд. `sh _generator/tools/fixtures/git_zona/PROGNAT.sh` → **rc=1, «ФИКСТУРЫ КРАСНЫЕ»** (прогнал живьём). Следующий коммит, трогающий `git_zona.py`, встанет на ложном красном. Правка ровно одна, но путь ВНЕ моей зоны:
   ```
   DOC="$TOOLS/../../_meta/disciplina/GIT-disciplina.md"
   ```
   Критерий приёмки правки: та же команда даёт `rc=0` и `✅ все подкоманды описаны`. **Класс шире одной строки: реестр `korni.py` расшил шесть инструментов, но ФИКСТУРЫ остались с вшитыми литералами — расшивка неполна.**
   ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/` (заход по `_generator/tools/`)
   ДОСТАВЛЕНО: нет

10. **Шаблоны тиражируют старый адрес в каждую будущую арку и каждый будущий заход.** `_studio/zhurnal/_TEMPLATE-arka/NAVIGATOR.md:18,21`, `_TEMPLATE-arka/UROKI-FABRIKE.md:5`, `_TEMPLATE-zahod.md:44`. Сегодня разрешается в стаб, но `bootstrap_arka.py` копирует шаблон — и каждая новая арка рождается со стухшим адресом. По `KONSTITUCIYA §11` шаблон захода — один из двух рычагов, вообще доезжающих до исполнителя; чинить раньше домов, а не позже.
    ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/` (тот же заход очереди, что и п.1)
    ДОСТАВЛЕНО: нет

11. **Гейт называет провинившемуся адрес указателя вместо дома.** `_generator/tools/check_tool_contract.py:51`, константа `CANON = "_studio/docs/kak-delat/GIT-disciplina.md"`, печатается в текстах отказа (строки 397, 531). Функционально не ломается — адрес разрешается в стаб. Плюс прозаические комментарии на старый адрес: `check_tool_contract.py:19`, `check_ves.py:34`, `.githooks/pre-commit:67`, `.github/workflows/hugo.yml:17`.
    ДОМ: `_studio/zhurnal/2026-08-05_faza-lenty/` (заход по `_generator/tools/`)
    ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

### Что сделано и ЗАЧЕМ

**Дом `_meta/` заведён и работает не на бумаге.** Структура ровно по образцу `_illustracii/`: `README.md` (что это, почему уровнем ВЫШЕ фабрик, замер универсальности, чего дом НЕ делает) · `CLAUDE.md` (вход исполнителя) · `POKRYTIE.md` (**честная заготовка**, см. ниже) · `UROKI.md` · `docs/KARTA.md` (СВОЙ индекс, §6 последним разделом) · `disciplina/` (четыре канона) · `zhurnal/` (пустой, под `.gitkeep`).

**Корень вписан ОДНОЙ строкой в `_generator/tools/korni.py`, правок в инструментах ноль.** Это и была проверка расшивки на себе: `check_kartoteka.py`, `register_doc.py`, `bootstrap_arka.py`, `bootstrap_zahod.py` увидели новый корень, его собственный индекс и его собственный журнал без единой правки кода. Понадобилась бы вторая правка — это была бы находка о неполноте реестра; не понадобилась.

**Четыре документа переехали ЦЕЛИКОМ через `git mv`**, история цела (критерий 4 ниже). Тела текстов не переписывались: единственное, что менялось внутри, — **относительные адреса ссылок**, и вот почему это было обязательно, а не «улучшение заодно».

**Перенос лоссовый не только наружу, но и ВНУТРЬ — заход этого не называл.** Все четыре файла лежали на глубине `_studio/docs/kak-delat/` и были откалиброваны под неё: `../../../_generator/…`, `../KARTA.md`, `../KONSTITUCIYA.md`, `../../COWORK.md`, `../../konvejer/…`, `../sostoyanie/…`. В `_meta/disciplina/` глубина другая (`../../` = корень репо), и **каждая такая ссылка становилась битой ровно в момент `git mv`** — 61 адрес в четырёх файлах. Правил механически, скриптом с печатью числа замен по каждому правилу (чтобы «правило не сработало» не выглядело как «правило не понадобилось»); ни одно предложение не переписано. Одна ручная правка: `RUKOVODSTVO-zahodami.md:12` звал бывших соседей `STANDART-oformlenia.md` / `STANDART-teksta.md` голым именем — теперь с адресом, иначе имя в новом доме не разрешается ни во что.

⚠ **Осознанное решение, которое стоит назвать: ссылки вида `../KARTA.md` (индекс своего корня) я перевёл в `../../_studio/docs/KARTA.md` буквально** — то есть сохранил ТОТ ЖЕ референт, что был до переезда, а не «улучшил» смысл на «индекс своего корня». Это сохраняет ссылки живыми и не трогает смысл чужого текста, но оставляет в универсальном каноне литеральный `_studio`. В стаб `ISTOCHNIK-PRAVDY.md` вписано предупреждение читать эти формулировки как «индекс своего корня»; полноценная правка формулировок — чужой ход, не мой.

**Четыре стаба на старых адресах**, каждый — по образцу `SLOVAR-primitivov.md` и каждый несёт **таблицу разделов**: `ARKA.md §7` теперь ведёт в файл без §7, и стаб прямо говорит, что §1–§10 (включая §6б) уехали под теми же номерами. У `RUKOVODSTVO-zahodami.md` разделы названы словами, а не номерами, — перечислены словами.

**`POKRYTIE.md` не сочинён.** Заведён с шапкой, командой пересчёта и явной строкой «наполняется заходом `karta-incidentov`»; §2, §3, §4 стоят пустыми с названной формой и указателем на образец. Числа — только те, что печатает живая команда.

### Как проверил — КРИТЕРИЙ ГОТОВНОСТИ, все шесть

**1. 🟢 Ворота видят новый корень и краснеют на его незарегистрированном файле.** Завёл `_meta/SIROTA-PROBA.md` мимо двери:
```
❌ _meta/SIROTA-PROBA.md:1 — документ-сирота (KONSTITUCIYA §9): нет строки в _meta/docs/KARTA.md §6
```
Диагноз называет **свой** индекс, а не студийный. Проба убрана; после неё по `_meta/` ни одного ❌ (`grep rc=1`).

**2. 🟢 Дверь пишет в индекс СВОЕГО корня, чужой не тронут ни байтом.** Восемь вызовов `register_doc.py` на путях `_meta/…` — все `rc=0`, все строки легли в `_meta/docs/KARTA.md`. `cmp` снимка `_studio/docs/KARTA.md` до и после → **rc=0**, `git status` по нему пуст, в коммит он не попал ни разу.

**3. 🟢 Живых битых ссылок ноль — по обоим классам, с охватом.** Мерил двумя независимыми проходами, оба учитывают, что в репозитории законны ДВЕ конвенции адресации (от документа и от корня репо):
- **(а) адреса на четыре переехавших, по всему репозиторию:** **битых 14 из 252, проверено 252.** Плюс отдельно 187 голых упоминаний имени без пути — они разрешаются стабом на старом адресе.
- 🔴 **Эти 14 — НЕ мои.** Распаковал дерево `HEAD` (`git archive`) и прогнал тот же скрипт по состоянию ДО переезда: **тот же список, те же 14, файл в файл**. Переезд не добавил ни одной битой ссылки. Это ссылки, написанные от третьей базы (`kak-delat/ARKA.md` из `docs/sostoyanie/`) и указатели на давно исчезнувшую папку `shablon/` в журналах июля.
- **(б) исходящие ссылки изнутри `_meta/`:** **битых 1 из 54, проверено 54.** Единственная — `content-studio/assets/corpus/texts-corpus.md`, унаследованный долг, доказано через `git show HEAD:` (ВОПРОСЫ п.5).

**4. 🟢 История цела.** `git log --follow` по новым адресам: **ARKA.md — 13 коммитов, RUKOVODSTVO-zahodami.md — 29, GIT-disciplina.md — 23, ISTOCHNIK-PRAVDY.md — 5.** Самый старый по ARKA.md — `ed3c807 chore: чекпоинт перед заходом kod_spina`, то есть история дотягивается далеко за переезд. Git записал переименования явно: `R082`, `R096`, `R086`, `R092`.

🔴 **Критерий 4 непроверяем до коммита, и это ловушка — см. ВОПРОСЫ п.7.** Первый прогон дал `0 коммитов` на всех четырёх при `rc=0`.

**5. 🟡 Названные гейты зелёные — но НЕ все гейты репозитория.**
```
sh _generator/tools/fixtures/korni/PROGNAT.sh   → rc=0, 25 ✅, 0 ❌, «ФИКСТУРЫ ЗЕЛЁНЫЕ»
sh .githooks/pre-commit                          → rc=0, 0 ❌
python3 _generator/tools/git_zona.py check --zone _meta → rc=0, «✅ зона _meta: работа доехала в git, вне git ничего нет»
```
Фикстуру `korni` править не пришлось — она строит синтетический репозиторий и заводит свой корень сама.

🔴 **Оговорка, без которой этот пункт врёт: `fixtures/git_zona/PROGNAT.sh` — КРАСНАЯ, и красной её сделал я.** Критерий 5 назвал две команды, обе зелёные; третью он не называл, и она не поднялась при моих коммитах, потому что `git_zona.py` в них не стейджился. Нашёл верификатор, я подтвердил живьём. Разбор и одна строка починки — ВОПРОСЫ п.9. **Считать критерий 5 пройденным «по букве» и промолчать было бы ровно тем ложно-зелёным, против которого написан весь `korni.py`.**

**6. 🟢 ЖИВОЙ ПРОГОН на реальном объекте.** `python3 _generator/tools/bootstrap_arka.py --koren _meta 2026-08-06_proba-doma "…"` → **rc=0**, арка заведена в `_meta/zhurnal/`, все пять её документов (`PLAN`, `TZ`, `SESSIYA`, `NAVIGATOR`, `UROKI-FABRIKE`) автоматически зарегистрированы дверью в **`_meta/docs/KARTA.md`** — то есть новый дом работает целиком, а не только в индексе. Тестовая арка убрана вместе с её пятью строками регистрации (НЕОБРАТИМОЕ ниже).

### Коммит разделён на два — и почему именно так

Прецедент выноса иллюстраций сделал перенос отдельным коммитом, назвав причину прямо в сообщении: *«Перенос отдельным коммитом, чтобы git видел переименования и `git log --follow` отвечал»*. Повторил: коммит 1 — чистое переименование (стабы отложены в сторону, старые пути в индексе удалены), коммит 2 — стабы на освободившихся адресах.

⚠ **Честная оговорка, чтобы не завести в канон ложный урок.** Проверил гипотезу «одним коммитом `--follow` сломается» на контролируемой пробе в одноразовом репозитории — **`--follow` пережил и совмещённый коммит** (3 коммита истории). То есть разделение здесь было страховкой по прецеденту, а не спасением от доказанной поломки; критерий 4 прошёл бы, вероятно, и одним коммитом. Не пишу это уроком фабрике: цены у наблюдения нет.

### Что НЕ трогал

- **Пять входных документов** (`CLAUDE.md`, `README.md`, `_studio/COWORK.md`, `_studio/CLAUDE.md`, `_studio/docs/KARTA.md`) — вне зоны; готовые строки выписаны в ВОПРОСЫ п.1 и п.2 с точным адресом вставки.
- **`KONSTITUCIYA.md`** — ни байта. Разбор, два шва вместо одного, замер 301/315 и способ безопасного реза — ВОПРОСЫ п.3.
- **`_studio/docs/KARTA.md`** — ни байта (`cmp` rc=0). Стабы держат старую регистрацию §6 живой по построению: имена путей не изменились.
- **Форки канона в `catalan/` и `kurs leto 2026/`** — вне зоны, зафиксированы с ценой в `_meta/UROKI.md §2` и в ВОПРОСЫ п.4.
- **14 битых ссылок и `content-studio/…`** — унаследованный долг, доказанный сверкой с `HEAD`. Не чинил.

### Сверка содержания «до / после» — своя, до верификатора

`git show HEAD~2:_studio/docs/kak-delat/<имя>.md` против нового файла:

| файл | строк до | строк после | изменённых строк | различий ПОСЛЕ снятия адресов |
|---|---|---|---|---|
| `ARKA.md` | 161 | 161 | 26 | **0** |
| `RUKOVODSTVO-zahodami.md` | 349 | 349 | 30 | **0** *(единственное — ручная правка стр. 12, тоже адрес)* |
| `GIT-disciplina.md` | 448 | 448 | 12 | **0** |
| `ISTOCHNIK-PRAVDY.md` | 92 | 92 | 12 | **0** |

Метод: нормализую оба текста, вычищая из них все адресные префиксы (`../`, `../../`, `../../../`, `_studio/`, `docs/`, `_generator/`, `teoriya-kategoriy/`, `_studio/docs/kak-delat/`), и сравниваю нормализованные версии. **Ни одна содержательная строка не изменена**: число строк совпадает во всех четырёх, а после снятия адресов различий не остаётся вовсе. Разделы, на которые ссылаются чаще всего, проверены поимённо — `ARKA §6`, `GIT-disciplina §3`, `RUKOVODSTVO §Приёмка отчёта`, `ISTOCHNIK-PRAVDY §4` — все на месте под теми же номерами и именами.

### Результат верификатора

Свежий субагент, пустой контекст, метод свой (полный греп → классификация арок по баннеру закрытия → машинная проверка разрешимости → пословный diff коммита переезда). **Верификатор оправдал себя: нашёл одну вещь, которую я не нашёл, и она функциональная.**

🔴 **НАХОДКА №1 — Я СЛОМАЛ ГЕЙТ. Это РЕГРЕССИЯ ЭТОГО ЗАХОДА, не унаследованный долг.**

`_generator/tools/fixtures/git_zona/PROGNAT.sh:343` — ловушка 6 («канон не отстал от инструмента») читает канон по литеральному адресу:
```
DOC="$TOOLS/../../_studio/docs/kak-delat/GIT-disciplina.md"
```
Там теперь стаб. Проверка `[ -f "$DOC" ]` его находит — файл существует, — и ловушка грепает в нём описания подкоманд, которых в стабе нет. **Проверил сам, живьём, не поверив на слово:**
```
sh _generator/tools/fixtures/git_zona/PROGNAT.sh   → rc=1, «ФИКСТУРЫ КРАСНЫЕ»
❌ В КАНОНЕ НЕТ ПОДКОМАНД: doctor poteri zakryt-vetku voskresit mogily check plan
   commit clean untrack worktree adopt merge opublikovat purge
```
Доказательство, что это мой след, а не старая гниль: `git show 6bb560a^:_studio/docs/kak-delat/GIT-disciplina.md | grep -c` даёт **6** совпадений, стаб — **0**, `_meta/disciplina/GIT-disciplina.md` — снова **6**.

**Последствие:** хук `pre-commit` поднимает эту фикстуру, когда в индексе есть `git_zona.py` или что-то из её охвата ⇒ **следующий коммит, трогающий `git_zona.py`, встанет на ложном красном.** Мои два коммита прошли только потому, что `git_zona.py` в них не было (стейджился `korni.py`, его сторожит другая фикстура — она зелёная).

**Почему НЕ починил, хотя это одна строка.** `_generator/tools/fixtures/` не в моей зоне, а контракт зоны говорит прямым текстом: «ничего за пределами зоны, **даже если „мешает" или „чинится в одну строку"**». Правка — ровно одна:
```
DOC="$TOOLS/../../_meta/disciplina/GIT-disciplina.md"
```
Ставить её должен тот, у кого `_generator/tools/` в зоне; после правки фикстура обязана дать `rc=0` и `✅ все подкоманды описаны`.

⚠ **И это ровно тот класс, ради которого заводился реестр `korni.py`:** литеральный путь, вшитый в инструмент, переживает переезд и превращает гейт в ложный. Реестр расшил шесть инструментов, но фикстуры остались с литералами — **расшивка неполна, и это находка в отчёт, как и предписывает §A.**

**НАХОДКА №2 — шаблоны тиражируют старый адрес в каждую будущую арку.** `_studio/zhurnal/_TEMPLATE-arka/NAVIGATOR.md:18,21`, `_TEMPLATE-arka/UROKI-FABRIKE.md:5`, `_TEMPLATE-zahod.md:44` зовут `../../docs/kak-delat/{ARKA,RUKOVODSTVO-zahodami,GIT-disciplina}.md`. Сегодня разрешается в стаб, но `bootstrap_arka.py` копирует шаблон, и **каждая новая арка рождается со старым адресом**. По `KONSTITUCIYA §11` шаблон захода — один из двух рычагов, которые вообще доезжают до исполнителя; оставлять в нём стухший адрес дороже, чем в любом доме.

**НАХОДКА №3 — `_generator/tools/check_tool_contract.py:51`,** константа `CANON = "_studio/docs/kak-delat/GIT-disciplina.md"`, печатается в текстах отказа гейта (строки 397, 531). Функционально не ломается (адрес разрешается в стаб), но провинившемуся гейт называет адрес указателя вместо дома.

**Чего верификатор НЕ нашёл — и это подтверждение, а не пустота:**

| что проверял | охват | результат |
|---|---|---|
| ссылки `ФАЙЛ.md §Раздел` | **857** вхождений | разрешается 844; **сломано переездом — 0**. Все 13 неразрешимых прогнаны и по новому файлу, и по `6bb560a^` — не разрешались и до переезда (`RUKOVODSTVO §8`, `§17`, `ARKA §автоцикл` — от давно переписанной структуры) |
| исходящие ссылки изнутри четырёх | **99** токенов | **битых 0**; 56 разрешаются от папки документа, 35 от корня репо, 1 и так и так |
| сверка содержания «до / после» | 4 из 4 | **смысловых изменений 0.** Непарных вставок/удалений токенов ноль во всех четырёх (`del_tokens == ins_tokens == paired`) — ни одного слова не добавлено и не выброшено, каждый изменённый токен есть путь, заменённый на путь |
| ссылки на старый адрес | **410** вхождений | 151 в журналах закрытых арок (24 папки), 259 живых — все разрешаются в стаб; стабов среди находок нет |

Расхождение с моим замером по числам (410 против моих 252, 857 против моих 54) — разные единицы счёта: я считал **адреса-токены с `/`**, верификатор — **все вхождения имени**, включая формы без `.md`. Направление вердикта у обоих одно.

⚠ Верификатор верно поправил меня в одном месте: эталон «до переезда» — `6bb560a^`, а не `HEAD`; в `HEAD` уже лежат стабы. Мои собственные сверки шли от `HEAD~2`, что то же самое.

### Открытое «возвращаться»

- 🔴 **ПЕРВЫМ: `fixtures/git_zona/PROGNAT.sh:343`** — фикстура красная прямо сейчас по моей вине, одна строка, вне моей зоны (ВОПРОСЫ п.9).
- **Вторым: шаблоны арки и захода** (ВОПРОСЫ п.10) — иначе старый адрес поедет в каждую новую арку.
- Строки маршрута (ВОПРОСЫ п.1–2) — следующий заход, один ход.
- `POKRYTIE.md §2–§4` — заход `karta-incidentov`.
- `KONSTITUCIYA.md` — решение владельца (ВОПРОСЫ п.3).
- Сведение форков канона в двух проектах (ВОПРОСЫ п.4).

### 🔴 НЕОБРАТИМОЕ

1. **Перемещены четыре файла** `_studio/docs/kak-delat/{ARKA,RUKOVODSTVO-zahodami,GIT-disciplina,ISTOCHNIK-PRAVDY}.md` → `_meta/disciplina/` · **восстанавливается:** `git revert 6bb560a` либо `git show 6bb560a^:_studio/docs/kak-delat/<имя>.md`; история цела, содержимое до переезда достаётся `git show HEAD~2:…`.
2. **Перезаписаны четыре файла по старым адресам** `_studio/docs/kak-delat/{ARKA,RUKOVODSTVO-zahodami,GIT-disciplina,ISTOCHNIK-PRAVDY}.md` — теперь это стабы-указатели, прежнего содержания в них нет · **восстанавливается:** `git show 6bb560a^:_studio/docs/kak-delat/<имя>.md`, коммит стабов `20ebea9`.
3. **Изменены 61 относительный адрес внутри четырёх перенесённых файлов** (ARKA 26 строк, RUKOVODSTVO 30, GIT-disciplina 12, ISTOCHNIK-PRAVDY 12 по `git show --stat`) · **восстанавливается:** `git show 6bb560a^:_studio/docs/kak-delat/<имя>.md`; различия видны как `git show 6bb560a -- _meta/disciplina/<имя>.md`.
4. **Заведена и удалена тестовая арка** `_meta/zhurnal/2026-08-06_proba-doma/` (5 файлов) — требование критерия 6 · **восстанавливается:** не нужно, в git не попадала; повторяется командой из критерия 6.
5. **Удалены 5 строк регистрации** этой тестовой арки из `_meta/docs/KARTA.md §6` — руками, потому что у двери `register_doc.py` нет обратной операции, а индекс, называющий несуществующие файлы, врёт о месте · **восстанавливается:** не требуется (файлов нет); строки видны в выводе `bootstrap_arka.py`, если понадобится воспроизвести.
6. **Добавлена одна строка в `_generator/tools/korni.py`** (корень `_meta`) · **восстанавливается:** `git show 6bb560a^:_generator/tools/korni.py`.

Вне зоны не вышло ничего. `_studio/docs/KARTA.md`, `KONSTITUCIYA.md` и пять входных документов не изменены — проверено `cmp` и `git show --stat` (в обоих коммитах только мои пути).

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/meta/_meta/README.md` — открывать как текст (любой markdown-просмотрщик или редактор); это вход в новый дом, дальше `_meta/CLAUDE.md` и `_meta/docs/KARTA.md`. Собранного HTML/PDF в этом заходе нет и не предполагалось: артефакт захода — сам дом `_meta/` на ветке `zahod/vynos-meta`, папка `/Users/ivanyakovlev/Documents/GitHub/materials-wt/meta/_meta/`. **В основной папке репозитория его пока НЕТ** — ветка не влита, влитие делает приёмка.
**КОММИТ:** `6bb560a` — `_meta: мета-дисциплина вынесена отдельным корнем — четыре канона переехали в disciplina/, свой индекс docs/KARTA.md с §6 последним, README/CLAUDE/POKRYTIE/UROKI, корень одной строкой в korni.py` (11 файлов, 4 из них — `rename` с сохранённой историей)
**КОММИТ:** `20ebea9` — `_studio/docs/kak-delat: четыре стаба-указателя на старых адресах — 111/139/110/13 живых ссылок продолжают разрешаться` (4 файла)
· `python3 _generator/tools/git_zona.py check --zone _meta` → **✅** «зона `_meta`: работа доехала в git, вне git ничего нет» · `git status --porcelain` пуст · `git show --stat` в обоих коммитах — **только мои пути**, ни одного чужого.
🔴 **Ветку `zahod/vynos-meta` НЕ вливал и не пушил** — влитие делает приёмка, как велит контракт зоны.
