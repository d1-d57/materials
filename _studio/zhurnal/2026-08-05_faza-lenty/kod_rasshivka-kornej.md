# Канал исполнителя — rasshivka-kornej (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — план рождается по ходу: заход исследовательский, и заранее известно только, ГДЕ вшита константа, а не чем каждое место держится. Механика не расписана нарочно.

КОНТЕКСТ. `materials/` — монорепо; внутри `_studio/` — фабрика, задуманная под один артефакт (презентацию), а сегодня обслуживающая ленты, обзоры, конспекты и иллюстрации. Владелец разделяет её на четыре фабрики (иллюстрации · лента · ресёрч · слайды) плюс мета-дисциплина уровнем выше. Прошлый этап: карта зависимостей построена грепом — `RAZDELENIE-issledovanie.md` в этой же арке.

**Она нашла блокер, ради которого и заведён этот заход.** `_studio` — не просто папка, а **литерал, вшитый в инструменты фабрики** (семь мест, перечислены в §2). Пока это так, вынос любой фабрики даёт не «фабрику без гейтов», а **фабрику с гейтами, которые МОЛЧАТ** — а молчащий гейт неотличим от зелёного. Это в точности класс `KONSTITUCIYA §11а`, уже оплаченный месяцем: *четыре гейта и хук числились существующими и не работали, и те же дефекты дожили до живого прогона.*

ЦЕЛЬ: научить инструменты **многим корням**, не перенеся ни одного файла. После этого захода перенос фабрики становится дешёвым и проверяемым; до него — запрещён.

Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: получишь карту швов и часть расшитых инструментов, но НЕ право переносить папки — оно появляется только при зелёном критерии §2 целиком.

🔴 **ЭТОТ ЗАХОД — ИССЛЕДОВАТЕЛЬСКИЙ, И ЭТО ЕГО ГЛАВНОЕ СВОЙСТВО.** Требование владельца дословно: *«он должен быть умный и исследовательский. То есть если у него обнаружено что-то, что не работает, он должен хорошо это продумать, то есть он должен одновременно куда-то внутрь глубоко залезть и разобраться»*. Значит: наткнулся на инструмент, который делает не то, что написано в его докстринге, — **не обходи и не подпирай костылём.** Залезь, пойми, ЧТО он на самом деле делает, и напиши это в отчёт отдельным пунктом, даже если чинить не стал. Тихая подпорка здесь дороже незакрытой задачи: она уедет в четыре фабрики разом.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/rasshivka`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/rasshivka-kornej`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add rasshivka --branch zahod/rasshivka-kornej
  cd ../materials-wt/rasshivka
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/` `_generator/build_deck.py`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0-ноль. 🔴 УБОРКА — ПЕРВЫЙ ХОД, В ОСНОВНОЙ ПАПКЕ, ДО ЗАВЕДЕНИЯ WORKTREE

Дерево оставлено грязным, и это надо закрыть тебе, а не владельцу. Делается **из основной папки репозитория** (`.../GitHub/materials`), до всякого `worktree add` — потому что worktree ты заведёшь уже на чистом состоянии.

**Что известно на 2026-08-06** (проверь своим прогоном, число — предпосылка, а не задание):

- вне git 5 путей: четыре в `_studio/` (борд, уроки арки, два файла-захода) и один чужой — `lsh-2026-perechislitelnaya/programma-kursa/zahod-syuzhety-i-poryadok.md`, **рождён и ни разу не ставился в git**, то есть умрёт от `git clean` и не попадёт ни на какой бэкап;
- лежит черновик `_studio/.commit-plan` с **уже переписанными** сообщениями (аналитик переписал их 2026-08-06). Плейсхолдеров `==  <что и зачем — переписать>` в нём быть не должно.

**Ходы:**

```
python3 _generator/tools/git_zona.py check                 # что вне git прямо сейчас
python3 _generator/tools/git_zona.py plan --force          # ТОЛЬКО если check показал не те пути, что в черновике
python3 _generator/tools/git_zona.py commit --push         # исполняет черновик, обе группы, и вывозит
```

⚠ **`plan --force` затирает переписанные сообщения плейсхолдерами.** Гони его только если состав путей разошёлся с черновиком; тогда сообщения перепиши сам — по-человечески, «что и зачем», а не `==`. Если состав совпал — сразу `commit --push`, черновик готов.

**Затем прибери хвосты, каждый — командой, не на глаз:**

```
python3 _generator/tools/git_zona.py poteri                # ноль веток с непереносённой работой
python3 _generator/tools/git_zona.py zakryt-vetku --vse-zelenye --help   # СНАЧАЛА прочитай, что команда делает
```

🔴 **Ветки НЕ закрывай молча.** `poteri` считает безопасными 4 из 4 — история каждой достижима из HEAD. Но закрытие ветки заводит надгробие и сносит рабочую папку, а рабочих папок сейчас пять и в них могут идти чужие заходы. ⇒ **Напечатай список того, что закрылось бы, и оставь решение владельцу пунктом в `## ВОПРОСЫ`.** Сам не сноси ничего.

**Гейт хода:** `git_zona.py check` печатает «вне git ничего нет» И `poteri` печатает «потерь нет» с охватом. Пока обе не зелёные — к §0-бис не переходи.

## 0-бис. 🔴 БЛОКИРУЮЩЕЕ ПРЕДУСЛОВИЕ — проверить ПЕРВЫМ ДЕЙСТВИЕМ

✅ **Предусловие ЗАКРЫТО приёмкой 2026-08-06** — заход `kod_svedenie-vetok.md` принят: ветка `zahod/faza-lenty` влита (коммит `15d871a`, 5 файлов спасено), `poteri` даёт ноль веток с непереносённой работой при охвате 4 из 4, `check` — ноль путей вне git. **Проверку ниже всё равно прогони:** с момента приёмки прошли ходы, а «закрыто без даты данных доверия не заслуживает» (Р34).

**Этот заход не запускается, пока не закрыт `kod_svedenie-vetok.md` в этой же арке.** Там сводятся неслитые ветки и коммитится всё, что висит вне git.

Причина не формальная. На 2026-08-06 на ветке `zahod/faza-lenty` лежало **7 файлов, которых нет больше нигде и которые есть только на этой машине**. Ты правишь инструменты, которыми это дерево коммитят и судят. Правка поверх несведённого состояния — это правка поверх работы, которую ты не видишь и можешь молча похоронить (`KONSTITUCIYA §12`: «на диске» ≠ «в git», и это НЕ одно состояние).

Проверка — командой, не чтением чужого отчёта:

```
python3 _generator/tools/git_zona.py poteri     # веток с непереносённой работой обязано быть 0
python3 _generator/tools/git_zona.py check      # путей вне git обязано быть 0
```

**Хоть одна ненулевая — СТОП, работу не начинай**, напиши в `## ВОПРОСЫ` строкой «предусловие не закрыто: <что именно печатает команда>». Это законный исход захода.

## 0. ПЕРВЫЙ ХОД
- 🔴 **§0-ноль и §0-бис закрыты?** Нет — возвращайся туда, worktree заводится на чистом дереве.
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/rasshivka-kornej`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/` `_generator/build_deck.py`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, в этом порядке. Проект сверх них не изучай:
  1. `_studio/zhurnal/2026-08-05_faza-lenty/RAZDELENIE-issledovanie.md` — §1, §2, §6, §10. Это твоя карта: где вшита константа и чем сдаётся результат.
  2. `_generator/tools/check_kartoteka.py`, `register_doc.py`, `git_zona.py`, `bootstrap_arka.py`, `bootstrap_zahod.py`, `check_incidenty.py` — шесть инструментов зоны.
  3. `_studio/docs/kak-delat/ISTOCHNIK-PRAVDY.md` §3 — пять ворот и дверь: что именно они обещают ловить.
  4. `_studio/docs/KONSTITUCIYA.md` §10, §11, §11а, §13 — почему молчащий гейт хуже отсутствующего и почему один риск за раз.
  5. `_generator/tools/fixtures/` — посмотри, как в этом репо устроены фикстуры: свою пиши по образцу, а не по своему вкусу.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

**Ничего не переносить.** Ни одного файла из `_studio/` не двигать, не копировать, не переименовывать. Задача целиком про инструменты.

### A. Свести семь констант в ОДИН дом

Сегодня путь `_studio` вшит здесь (адреса на дату данных 2026-08-06 — **проверь их первым делом грепом, адрес тоже предпосылка**, `KONSTITUCIYA §14`):

| файл | что вшито |
|---|---|
| `check_kartoteka.py:177` | `КОРНИ = ("teoriya-kategoriy", "teorkat-vvedenie", "_studio")` |
| `check_kartoteka.py:578` | `if not p.startswith("_studio/")` — ворота 5 |
| `register_doc.py:49` | `KARTA_REL = "_studio/docs/KARTA.md"` |
| `register_doc.py:69` | `СУДИМЫЕ_КОРНИ = ("_studio/", "obzory/")` |
| `git_zona.py:69` | `PLAN = REPO / "_studio" / ".commit-plan"` |
| `git_zona.py:381` | зона = `segs[0]=="_studio" and segs[1]=="zhurnal"` |
| `bootstrap_arka.py:26–27`, `bootstrap_zahod.py:53`, `check_incidenty.py:31–33` | пути шаблонов и домов |

Заведи **один модуль-реестр корней** в `_generator/tools/` (имя предложи сам в `## ПЛАН`), из которого все шестеро берут ответ на три вопроса: *какие корни судятся · где у корня индекс `KARTA.md` · где у корня `zhurnal/`*. Реестр — данные, не логика: добавление пятой фабрики должно быть строкой в нём, а не правкой шести файлов.

🔴 **Это НЕ переименование `_studio` в переменную.** Формулировка «вынесем в константу» — самая вероятная ошибка этого захода: она даёт один корень под новым именем и не даёт многих. Проверка на себе: **если после твоей правки нельзя добавить второй корень, не тронув ни одного из шести файлов, — задача A не сделана.**

### B. Привести к одному виду определение корня репозитория

```python
# git_zona.py:68, register_doc.py:48 — с дверью
REPO = Path(os.environ.get("GIT_ZONA_REPO") or Path(__file__).resolve().parents[2])
# check_kartoteka.py:176, bootstrap_arka.py:25 — БЕЗ двери
REPO = Path(__file__).resolve().parents[2]
```

Корень репо вычисляется как «на два уровня выше файла инструмента», то есть **определён положением `_generator/`, а не наличием `.git`**. У двоих есть переменная окружения, у двоих нет. Приведи всех к одному способу.

🔴 **Прежде чем менять — разберись, ПОЧЕМУ так сделано.** Возможно, `parents[2]` держит что-то, чего не видно из докстринга (запуск из worktree, из песочницы, из подпапки). Найдёшь причину — назови её в отчёте и сохрани поведение. Не найдёшь — тоже напиши, что не нашёл, с охватом.

### C. Разведать шов иллюстраций — **отчётом, без единой правки**

Владелец: *«фаза сидит внутри слайдов, а иллюстрации нужны всегда — это инструмент, который используют совершенно не только для презентации»*. Фаза `09-illustracii` будет выноситься следующим заходом. Твоя задача — **карта, чем она приколочена**, а не вынос.

Ответь по каждому пункту, с адресом (файл:строка):
1. Что именно про иллюстрации делает `build_deck.py.lint()` — резолв `data-ill` / `data-iframe`, проверка `var(--x)` по `tokens.css`. Можно ли это выделить в отдельный проверяльщик, не ломая сборку деков?
2. Что про иллюстрации делает `audit.py` (`svgOverflow`, `svgProse`) — и нужен ли ему собранный дек или хватит одиночного SVG?
3. Что физически держит `SLOVAR-primitivov.md` — по грепу у него одна ссылка наружу (`STANDART-oformlenia.md`). Подтверди или опровергни, с охватом.
4. Гейт G10 (`_studio/konvejer/GEJTY.md`) — где он реализован в коде и реализован ли вообще. **Помни `KONSTITUCIYA §11а`: описанный в каноне механизм может не существовать.** Проверь запуском, а не чтением.

🔴 **Зона `_generator/build_deck.py` открыта тебе на ЧТЕНИЕ и на правку — но по пункту C правок НЕ делай.** Она в зоне только затем, чтобы ты мог в него залезть и разобраться. Захотел починить найденное — в `## ВОПРОСЫ` пунктом очереди, не в код.

### Д. Починить `poteri` — долг оплачен предыдущим заходом, зона твоя

`git_zona.py` уже в твоей зоне, и в нём лежит дефект с названной ценой (урок 6 арки, `UROKI-FABRIKE.md`).

**Что не так.** `poteri` отвечает на вопрос «что ПРОПАДЁТ, если удалить ветку», то есть показывает только пути, которых на этой стороне нет ВОВСЕ. Файл, изменённый обеими сторонами, в вывод не попадает по построению. Аналитик описал по нему содержимое ветки — и объявил заходу зону вдвое уже настоящей: ветка трогала 20 путей, 7 из них в `_generator/`, `merge --zone` отказал целиком.

**Две правки, обе дешёвые:**
1. `poteri` печатает **два числа**: «пропадёт файлов» и «изменено с обеих сторон». Второе сегодня не печатается вовсе, хотя вычисляется тем же обходом.
2. `merge` при отказе по зоне печатает **готовую строку со всеми нужными `--zone`**. Инструмент их уже знает: такую подсказку он пишет в автолог.

Сдаётся так: на ветке, где есть файл, изменённый с обеих сторон, `poteri` **называет его отдельным числом**, а `merge --zone <заведомо узкая>` печатает строку, которую можно скопировать и запустить без раздумий. Проверить на живой ветке репозитория, не на выдуманной.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. 🔴 **Главное — гейт КРАСНЕЕТ на втором корне.** Заводишь фикстуру: временный второй корень с незарегистрированным `.md` внутри → `check_kartoteka.py --gates` печатает по нему ❌. **МОЛЧАНИЕ = ПРОВАЛ КРИТЕРИЯ, а не успех.** Ровно это и покупается заходом: сегодня он молчит, и молчание читается как «всё чисто».
2. Тот же прогон на старом корне `_studio/` даёт **тот же результат, что до правки** — регрессии нет. Снять «до» и «после» одной командой и приложить оба вывода в отчёт.
3. `register_doc.py` на пути внутри второго корня вписывает строку **в индекс ЭТОГО корня**, а не в `_studio/docs/KARTA.md`. На несуществующем файле и пустом описании по-прежнему `rc≠0`.
4. `bootstrap_arka.py` и `bootstrap_zahod.py` заводят арку и заход во втором корне; `check_incidenty.py` находит заход по имени в обоих.
5. Добавление третьего корня — **одна строка в реестре**, ноль правок в шести инструментах. Покажи это, добавив третий и откатив.
6. Фикстура в `_generator/tools/fixtures/` по образцу существующих: **каждая ловушка проверена мутацией** — сломай проверяемое и покажи, что фикстура покраснела. Непроверенная мутацией ловушка не считается (`ISTOCHNIK-PRAVDY §3`).
7. Пункт C: четыре ответа с адресами `файл:строка` и с охватом. Правок в `build_deck.py` — ноль.
8. 🔴 **ЖИВОЙ ПРОГОН на реальном объекте репозитория, не только фикстура.** Фикстура проверяет то, что ты сам придумал проверить; живой файл проверяет то, чего ты не предусмотрел. Прогнать на реальных объектах и приложить вывод целиком:
   ```
   python3 _generator/tools/check_kartoteka.py teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md --gates
   python3 _generator/tools/register_doc.py <живой .md в _studio/> "<описание>"   # идемпотентность: дубля не заводит
   sh .githooks/pre-commit                                                        # хук целиком, не по одному гейту
   ```
   Каждый — **до и после правки**, вывод обоих в отчёт. Разошлись — объясни, чем именно, и почему это не регрессия.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР — нужен, и метод у него ДРУГОЙ

Операция не лоссовая (файлы не двигаются), но проверяемое свойство — **отрицательное**: «гейт не молчит там, где раньше молчал». Такое свойство самопроверкой не закрывается: автор правки знает, где смотреть, и смотрит именно туда.

Свежий субагент с пустым контекстом получает ровно два предложения: *«В `_generator/tools/` инструменты обязаны судить НЕСКОЛЬКО корней, а не только `_studio/`. Найди корень, который они всё ещё не видят»*. Ни карты, ни списка семи мест ему не давать — он должен искать сам.

Нашёл место, которое ты пропустил, — это не провал захода, а его результат: впиши в отчёт отдельной строкой.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/` `_generator/build_deck.py`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/` `_generator/build_deck.py`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
