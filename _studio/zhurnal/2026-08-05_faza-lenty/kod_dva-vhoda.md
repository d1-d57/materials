# Канал исполнителя — dva-vhoda (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — работа в том, чтобы отличить существующую дисциплину от её отсутствия и не написать красивого текста про то, чего нет; это суждение, а не механика.

КОНТЕКСТ. У владельца два репозитория, лежат рядом в обычной папке `~/Documents/GitHub/`:

- **`materials`** — монорепозиторий содержания: курсы, лекции, обзоры, фабрика презентаций `_studio/`, фабрика иллюстраций `_illustracii/`, движки `_generator/`;
- **`disciplina`** — переносимый плагин Claude Code, семь скиллов: как коммитить, как писать заходы, как вести арку, где что живёт, словарь примитивов иллюстраций.

🔴 **Сама папка `~/Documents/GitHub/` git-репозиторием НЕ является** — проверено 07.08, `.git` в ней нет. Файл, положенный туда, не под версионным контролем, никуда не уезжает и на втором компьютере не появится. Поэтому знание о том, **как устроено всё вместе**, жить там не может — а сегодня оно не живёт нигде.

Решение владельца дословно: *«общая информация должна идти в дисциплину — там должно быть описано, как мы вообще работаем с папкой GitHub и что там внутри лежит… а всё, что касается конкретно материалов — как мы создаём новый математический курс и лекцию — отправляется в папку materials»*.

Второй разрыв оттуда же: при разносе фабрики по домам **потерялся вход в работу с лекцией**. Инструмент есть (`_generator/tools/bootstrap_lekcia.py` заводит стаб-дерево), роутер задач есть (`materials/README.md §2`), а связного «с чего начинать новую лекцию» нет.

ЦЕЛЬ: завести два входа — по одному в каждом репозитории.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ, И ЭТО ГЛАВНОЕ ОГРАНИЧЕНИЕ.** Ты **собираешь существующее, а не сочиняешь новое**. Каждое утверждение обоих документов обязано иметь адрес: файл, инструмент или решение владельца. **Дисциплину, которой нет, придумывать нельзя** — на её месте пишется `⚠ ДЫРА: <чего не хватает>`, и это полноценный результат захода.

Причина названа заранее: вход читают с пустым контекстом и исполняют буквально. Придуманное правило, поданное авторитетно, хуже отсутствующего — оно будет исполняться.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/wt-vhoda`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/dva-vhoda`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add wt-vhoda --branch zahod/dva-vhoda
  cd ../materials-wt/wt-vhoda
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `./LEKCIYA-s-chego-nachat.md` `./README.md` `disciplina/KAK-USTROEN-GITHUB.md` `disciplina/README.md` — четыре файла, два новых и два правятся одной строкой.
  🔴 **Пути из ДВУХ репозиториев:** первые два — в `materials`, вторые два — в `disciplina` (`~/Documents/GitHub/disciplina`, инструменты наводятся на него через `GIT_ZONA_REPO`). **Два репозитория — два коммита**, каждый своими путями; оба хэша в отчёт.
  🔴 В обоих `README.md` правится **ровно одна строка** — ссылка на новый файл в уже существующей таблице. Переписывание соседних строк — выход за зону.
  Всё остальное — **READ-ONLY**: `_studio/`, `_generator/`, `_illustracii/`, папки курсов, а в `disciplina` — `skills/` и `arhiv-skillov/` (там прямо сейчас работает заход `gejt-vhozhdeniya`).
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/dva-vhoda`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: зона не чиста в HEAD — закоммить как есть; чиста — не фабрикуй.
- **Прочитать ТОЛЬКО это. Проект не изучай, по ссылкам внутри не ходи без нужды.**
  1. `materials/README.md` целиком — 151 строка. **Там уже есть §2 РОУТЕР «задача → дом» и §3 карта проектов.** Твой документ их не дублирует и не отменяет.
  2. `materials/CLAUDE.md` — 35 строк, тонкий указатель, читается автоматически.
  3. `disciplina/README.md` — что за плагин и что внутри.
  4. `python3 _generator/tools/bootstrap_lekcia.py --help` и сам файл — что именно он заводит.
  5. `python3 _generator/tools/sostoyanie_lekcii.py --help`, `check_kurs.py` — ⚠ у второго `--help` **падает трейсбеком** (известный дефект, урок 26); смотри код, а не справку.
  6. Любая **живая** папка лекции для образца — например `krivaya-drakona/lekciya/` или `teorkat-vvedenie/`: что там реально лежит.
- ПЛАН — в `## ПЛАН` перед действиями.

## 2. ЗАДАЧА

### 2.1 `disciplina/KAK-USTROEN-GITHUB.md` — вход на уровень выше репозиториев

Отвечает на вопросы, ответа на которые сегодня нет нигде:

- **что за папка `~/Documents/GitHub/`** и почему она не репозиторий — с последствием: класть туда файлы бессмысленно, они никуда не едут;
- **какие репозитории внутри, чем каждый занят, где чей вход** (`materials/README.md`, `disciplina/README.md`);
- **что где искать по задаче** — короткая таблица уровня «дисциплина работы → `disciplina`, содержание курсов → `materials`»; не переписывай роутер материалов, дай ссылку;
- **как поставить это на новом компьютере** — маркетплейс с GitHub, команды из `disciplina/README.md §Установка` (они там уже есть и проверены живым прогоном 07.08 — ссылайся, не копируй);
- **чем связаны два репозитория:** инструменты `_generator/tools/*` лежат в `materials`, а на чужой репозиторий наводятся переменной `GIT_ZONA_REPO` (`korni.py:42`). Это единственная дверь, и знать про неё надо до первой команды.

⚠ **Известное «пока не решено» назвать честно:** генератор физически живёт в `materials`, хотя по смыслу общий; вынос обсуждается. Обзоры и библиотека — кандидаты на отдельные репозитории. Это `⚠ ДЫРА`, а не описание будущего в настоящем времени.

### 2.2 `materials/LEKCIYA-s-chego-nachat.md` — вход в работу с лекцией

🔴 **Сначала выясни, что уже есть, и только потом пиши.** Есть инструмент `bootstrap_lekcia.py`, есть живые папки лекций, есть `sostoyanie_lekcii.py`. Документ должен вести по **существующему пути**, а не предлагать новый.

Что он обязан отвечать:

- **чем лекция отличается от курса и от обзора** — по факту устройства папок, не по определению из головы;
- **как заводится новая лекция** — командой, с примером; что появляется на диске и что делать дальше;
- **какие фазы проходит** — и куда идти за дисциплиной каждой: ресёрч и текст → скилл `reserch-cikl`, слайды → скилл `slajdy`, иллюстрации → `_illustracii/DISCIPLINA.md`;
- **чем меряется готовность** — какие гейты есть и что они проверяют;
- **где живёт состояние лекции** и как его спросить командой.

⚠ **Скиллы `reserch-cikl` и `slajdy` — ЧЕРНОВИКИ**, зовутся только по имени и сами не срабатывают. Сказать это прямо: читатель, отправленный к скиллу, который молчит, решит, что сломано у него.

### 2.3 По строке в каждый роутер

`materials/README.md §2` — строка на `LEKCIYA-s-chego-nachat.md`. `disciplina/README.md` — строка на `KAK-USTROEN-GITHUB.md`. **Одна строка, в существующей таблице, без переписывания соседних.**

### 2.4 Форма обоих документов

Как у соседей по репозиторию: шапка-врезка «КОГДА читать · ЧТО дописывать сюда · КУДА дальше» (образец — любой `POKRYTIE.md` или `_illustracii/DISCIPLINA.md`), числа — командой, а не значением (`KONSTITUCIYA §10`).

**Короче лучше.** Вход, который не дочитывают, входом не работает; ориентир — не длиннее `materials/CLAUDE.md` (35 строк) на документ, и это потолок, а не цель.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Семь условий:

1. Оба файла существуют, каждый в своём репозитории; **два коммита, оба хэша в отчёте**; `git_zona.py check` зелёный в обоих (в `disciplina` — с `GIT_ZONA_REPO`).
2. **Утверждений без адреса — 0.** Приёмка возьмёт пять случайных утверждений из каждого документа и спросит источник; не назовёшь — отчёт заворачивается целиком. Метки `⚠ ДЫРА` под это правило не подпадают: у них адреса нет по определению, они его отсутствие и называют.
3. **Живой прогон на реальном дереве, не на бумаге:** каждая команда, названная в обоих документах, прогнана, `echo $?` показан. Команда, которая падает, в документ не идёт — либо чинится формулировка, либо ставится `⚠ ДЫРА`.
4. Каждый путь, названный в документах, существует (`ls`); битых — **0**, число проверенных путей названо.
5. По строке добавлено в оба роутера; `git diff` по `README.md` обоих репозиториев — **не больше двух строк на файл**. Больше — ты переписал чужое.
6. Длина каждого документа названа числом (`wc -l`).
7. `⚠ ДЫРА` — сколько их, перечислены поимённо. **Ноль дыр подозрителен**: два входа собираются на месте, где раньше не было ничего, и полностью покрытым это место быть не может. Ноль — объясни, чем именно покрыто каждое из четырёх мест, названных в §2.1 и §2.2 как «пока не решено».

**Условие 2 — главное.** Оно отличает вход, собранный из фабрики, от красивого текста про фабрику.

## 3. ВЕРИФИКАТОР

**Нужен, и метод у него ровно обратный твоему.**

Свежий субагент получает **только два готовых документа** и не получает ни твоего разбора, ни доступа к твоим заметкам. Задача: по каждому утверждению попытаться **найти опору в репозиториях самостоятельно**. Что он не нашёл — либо ты не назвал адрес, либо утверждения нет в фабрике вовсе; оба случая разбираются поимённо.

Вторая половина, важнее первой: **пусть он попробует по документу §2.2 завести пробную лекцию** во временной папке и скажет, где встал. Вход, по которому нельзя пройти, — не вход; а понять это по тексту нельзя, только прогоном.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `./README.md` `./LEKCIYA-s-chego-nachat.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `./README.md` `./LEKCIYA-s-chego-nachat.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Заход продиктовал исполнителю четыре «пока не решено» — три из них были уже решены или не существовали, и они уехали бы в канон как настоящие дыры
Заход §2.1 назвал поимённо, что пометить `⚠ ДЫРА`: вынос `_generator`, обзоры и библиотека как кандидаты в репозитории, а разведка добавила четвёртую — «канон дисциплины в двух местах». Каждую я вписал в документ, потому что заход подал их как факты фабрики. Верификатор, искавший опору самостоятельно, снял три из четырёх: вынос `_generator` решён владельцем и датирован (`disciplina/OTVETY-INTERVYU-2026-08-07.md` §A.1, «остаётся в materials»); «обзоры и библиотека» не имеют опоры нигде, кроме самого текста захода; «два канона» — не дыра, а один канон и четыре стаба-указателя «→ ПЕРЕЕХАЛ» (30–37 строк против 92–448). Механика захода при этом сошлась идеально — верификатор не нашёл ни одной ошибки в путях, кодах возврата и номерах строк. Разъехалось ровно то, что пришло из ТЗ, а не с диска.
ЦЕНА: три ложные дыры в двух входных документах, дожившие до коммита и снятые только вторым, независимым проходом. Вход читают с пустым контекстом и исполняют буквально — «вынос обсуждается» в документе отправил бы следующего исполнителя обсуждать решённое, а `⚠ ДЫРА` про библиотеку заставила бы искать несуществующее. Полный переписывающий проход по обоим документам после верификации. Рычаг, который сработал: §3 требует, чтобы верификатор искал опору САМ и не получал разбора исполнителя — без этой формулировки он бы сверил документ с заходом и всё подтвердил.

### Потолок «не длиннее 35 строк» несовместим с содержанием, которое требует тот же заход
§2.4 ставит потолок 35 строк на документ; §2.1 и §2.2 требуют от них девять содержательных ответов, а условие 7 — ещё и перечень дыр поимённо. В markdown с обязательными пустыми строками, врезкой-шапкой, двумя таблицами и блоками кода это не совмещается: непустых строк у эталона `CLAUDE.md` — 24, у моих документов — 33 при уже вырезанном воздухе. Дошёл до 47 и 51 строки; дальше режется требуемое заданием.
ЦЕНА: два полных переписывающих прохода ради сжатия, оба безрезультатные по критерию — потолок всё равно превышен в 1,4 раза. Требование, которое нельзя выполнить, не дисциплинирует, а просто тратит проход: следующий исполнитель либо повторит эти два круга, либо молча выбросит часть ответов, и заметить второе будет нечем — гейта на «документ ответил на все вопросы задания» не существует.

### `register_doc.py` отказывает на `.md` в корне репозитория, а контракт зоны требует его регистрировать безусловно
Контракт зоны: «ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ». Прогон на `LEKCIYA-s-chego-nachat.md` (корень `materials/`) → `rc=2`: «дверь судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`». Отказ законный и объяснимый — файл не принадлежит ни одному индексируемому корню реестра `korni.py`, и ворота 5 его не судят, — но безусловная формулировка контракта этого класса не предусматривает.
ЦЕНА: здесь — один круг проверки, потому что я прогнал команду прежде, чем поверить. Заряжено на большее: инструкция, поданная как безусловная, при красном хуке толкает ровно к тому обходу `--no-verify`, ради искоренения которого этот пункт контракта и написан (28 обходов из 56 срывов по автологу). Достаточно одной строки-исключения: «файл вне корней реестра не регистрируется — это не долг».
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 🔴 Оспариваю предпосылку захода — ДО работы, как велит §1

**Утверждение захода (строка 11): «знание о том, как устроено всё вместе… сегодня оно не живёт нигде» — ЛОЖНО.**

Доказательство — живой файл:

```
$ wc -l ~/Documents/GitHub/README.md
      63 README.md
```

Это `~/Documents/GitHub/README.md`, заголовок дословно: «`~/Documents/GitHub/` — ТЫ ЗДЕСЬ. Первый файл в любом заходе», подзаголовок «Это РОУТЕР "ситуация → дом"», **дата данных 2026-08-06** (сегодня). Внутри — ровно то, что §2.1 просит написать заново: §1 таблица «ситуация → адрес» (13 строк), §2 таблица репозиториев корня со списком-командой, §3 состояние скиллов `disciplina/` со списком-командой.

Из пяти пунктов §2.1 он уже покрывает три: *какие репозитории внутри и где чей вход* · *что где искать по задаче* · (частично) *чем связаны репозитории* — на уровне «канон-дом такой-то».

**Заход при этом прав в главном, и именно это делает задачу осмысленной:** папка не под git (проверено `test -e ~/Documents/GitHub/.git` → НЕТ), поэтому у этого знания **есть текст, но нет дома** — оно не едет на второй компьютер и умирает от переустановки.

**Вторая неточность предпосылки:** «у владельца два репозитория». По команде их пять:

```
$ cd ~/Documents/GitHub && for d in */; do [ -e "$d/.git" ] && echo "REPO $d"; done
REPO digest/      REPO disciplina/   REPO matema-fest/
REPO matemdigest-map/                REPO materials/
```
Плюс `spetsmat/` (не под git) и две worktree-папки `materials-wt/`, `disciplina-wt/`.

### Что из этого следует для работы (поправка, а не отказ)

Задача §2.1 меняется с «написать с нуля» на **«дать существующему знанию версионируемый дом и дописать то, чего в нём нет»**. Писать в `KAK-USTROEN-GITHUB.md` вторую копию таблицы репозиториев я не буду — это прямое нарушение правила одного дома (`ISTOCHNIK-PRAVDY`): два списка репозиториев разъедутся молча, и корневой, будучи вне git, разъедется первым. Поэтому:

- **пишу то, чего в корневом README нет вовсе:** почему папка не репозиторий и что из этого следует · как поставить плагин на новом компьютере (ссылкой на `disciplina/README.md §Установка`) · `GIT_ZONA_REPO` как единственная дверь инструментов к чужому репо (`korni.py:42`) · честные `⚠ ДЫРА`;
- **состав репозиториев — командой**, а не переписанной таблицей (`KONSTITUCIYA §10` и так это требует);
- **корневой `README.md` не трогаю** — он вне зоны захода. Его судьба (перенести содержание сюда? оставить зеркалом?) — вопрос владельцу, в `## ВОПРОСЫ`.

### Что нашла разведка для §2.2 (влияет на текст документа)

1. **Канон папки лекции существует и подробен** — `_studio/docs/spravka/PAPKA-LEKCII.md` (94 строки), именно на него ссылается docstring `bootstrap_lekcia.py`. Дерево, роль каждой папки, гейт каждой стадии.
2. **`bootstrap_lekcia.py` живой**, `--help` отрабатывает `rc=0`, идемпотентен (существующую папку не затирает, `rc=1`).
3. 🔴 **`sostoyanie_lekcii.py` — НЕ универсальный инструмент.** Он жёстко зашит на один проект: `korni`-аналога нет, вместо него `P = lambda *a: os.path.join(ROOT, 'teoriya-kategoriy', *a)` (строка 31). То есть «состояние лекции» он умеет спрашивать **только у `teoriya-kategoriy/`** — а тот помечен в `README.md §3` как 🗄 **АРХИВ под мёртвую рамку**. Для новой лекции команда бесполезна. Это `⚠ ДЫРА`, и заход в §2.2 («где живёт состояние лекции и как его спросить командой») опирался на инструмент, которого в этом смысле нет.
4. 🔴 **Ни одна живая папка лекции не совпадает с тем, что заводит скаффолд.** Проверено по диску: `kotly/` есть у трёх папок (`teoriya-kategoriy`, `fibonacci`, `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal`), `chernovik/` — у двух, `NAVIGATOR.md` — у двух. Названные заходом образцы канону не следуют: `krivaya-drakona/lekciya/` — это папка **сборки** (`assemble.py`, `gen_ill.py`, `tekst/`, `ill/`, `build/`), а `teorkat-vvedenie/` живёт по своей рамке (`SPEKA.md`, `KOTYOL.md`, `LENTA-L2`). Полностью каноническая папка на диске одна — `otkrytaya-lekcia-paskal/`. Документ поведу по скаффолду + `PAPKA-LEKCII.md`, а расхождение назову дырой, а не замолчу.
5. **Скиллы-черновики подтверждены в коде, а не на слово:** у обоих во фронтматтере `disable-model-invocation: true` с комментарием «снять, когда скилл перестанет быть черновиком; решение владельца 07.08». Заход прав: они молчат.

### Порядок ходов

1. ✅ worktree заведён, ветка `zahod/dva-vhoda`, зона чиста.
2. ✅ разведка (выше).
3. Прогнать каждую команду, которая попадёт в тексты, — включая пробное заведение лекции во временной папке; `echo $?` на каждой.
4. Написать `disciplina/KAK-USTROEN-GITHUB.md` (потолок 35 строк).
5. Написать `materials/LEKCIYA-s-chego-nachat.md` (потолок 35 строк) + `register_doc.py`… — **проверить, применим ли он к файлу в корне репо:** дверь по §КОНТРАКТ отказывает на пути вне `_studio/`, а мой файл лежит в корне. Если откажет — это не повод обходить хук, а факт в отчёт.
6. По одной строке в оба роутера.
7. Проверить все пути и длины командами.
8. Верификатор — двумя проходами, как велит §3.
9. Два коммита, оба хэша в отчёт.

### Замечание к КРИТЕРИЮ ГОТОВНОСТИ

Условие 5 («`git diff` по `README.md` обоих репозиториев — не больше двух строк на файл») исполнимо, но `git diff` без путей в общем репо покажет и чужое. Читаю его как «diff по МОЕМУ `README.md`, с путём» и так и меряю.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **Судьба `~/Documents/GitHub/README.md` — решение только ваше.** Файл существует (63 строки, дата данных 2026-08-06) и несёт тот же роутер «ситуация → дом», что и заведённый мной `KAK-USTROEN-GITHUB.md`, но лежит в папке, которая репозиторием не является: не версионируется, на второй компьютер не приезжает, умирает с диском. Я ничего из него не переписал, чтобы два текста не разъехались молча, и назвал это дырой в самом документе. Развилки три: (а) перенести содержание в `disciplina/KAK-USTROEN-GITHUB.md` целиком, а наверху оставить трёхстрочную заглушку «содержание переехало, читай там»; (б) оставить наверху как одноразовое зеркало и объявить источником правды репозиторный файл; (в) оставить оба и завести гейт расхождения. Пока не решено — знание про папку дублируется.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. **`sostoyanie_lekcii.py` и `check_kurs.py` — оба зашиты на один проект, оба выглядят общими инструментами фабрики.** У первого `P = lambda *a: os.path.join(ROOT, 'teoriya-kategoriy', *a)` (строка 31), у второго в docstring прямо «Гейты КУРСА — курс „Теория категорий"». Опаснее второго первый: на чужой лекции он не падает, а выходит с `rc=0` и печатает правдоподобный вердикт «ЗАПИСИ НЕТ, карточек 0» — про чужой проект. Оба лежат в `_generator/tools/` рядом с общими инструментами и ничем от них не отличимы по имени. Расшивка через реестр корней уже сделана для `register_doc`/`git_zona` (`korni.py`) — эти двое её не прошли.
   ДОМ: _studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md
   ДОСТАВЛЕНО: нет
3. **Скаффолд лекции расходится с собственным каноном в двух местах.** `bootstrap_lekcia.py` не заводит `SPEKA.md`, хотя `PAPKA-LEKCII.md` называет её в дереве и требует читать ПЕРВОЙ; и пишет в `brief.md` девять полей, тогда как гейт G1 (`GEJTY.md`) проверяет восемь. Оба расхождения молчаливые: скаффолд отрабатывает `rc=0`, гейт краснеть не на чем. На диске это уже видно — из двух папок, следующих канону, `fibonacci/` имеет `SPEKA.md` и не имеет дневника, а `otkrytaya-lekcia-paskal/` наоборот.
   ДОМ: _studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md
   ДОСТАВЛЕНО: нет
4. **`disable-model-invocation: true` закрывает скилл и для явного вызова по имени.** `disciplina/README.md` (строка 35) и оба `SKILL.md` описывают флаг как «активируются только по явному вызову по имени, на общие запросы не срабатывают» — верификатор проверил прогоном: прямой вызов `reserch-cikl` и `slajdy` возвращает ошибку «cannot be used due to disable-model-invocation», и в списке доступных скиллов сессии их нет вовсе. Поднять их может только владелец из чата. Формулировка обещает исполнителю дверь, которой нет; я написал в своём документе то, что проверено прогоном, но исходные описания остались прежними — они вне моей зоны.
   ДОМ: disciplina/README.md
   ДОСТАВЛЕНО: нет
5. **Контракт зоны требует регистрировать любой новый `.md`, а дверь отказывает на файле в корне репозитория** (`rc=2`, «дверь судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`»). Нужна строка-исключение, иначе следующий исполнитель прочитает отказ как повод к `--no-verify`. Развёрнуто с ценой — в `## УРОКИ ФАБРИКЕ`.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina/KAK-USTROEN-GITHUB.md` и `/Users/ivanyakovlev/Documents/GitHub/materials-wt/wt-vhoda/LEKCIYA-s-chego-nachat.md` — любой markdown-просмотрщик или редактор. 🔴 Второй лежит в **worktree захода**, а не в основной папке: ветка `zahod/dva-vhoda` не влита (по указанию §КОНТРАКТ, влитие — за приёмкой). После влития он окажется по адресу `/Users/ivanyakovlev/Documents/GitHub/materials/LEKCIYA-s-chego-nachat.md`.

**КОММИТ:** два, по одному на репозиторий.
- `materials` (worktree `wt-vhoda`, ветка `zahod/dva-vhoda`): **`ee5f062`** — «dva-vhoda: вход в работу с лекцией + строка в роутер §2» · `git_zona.py check --zone .` → ✅ «работа доехала в git, вне git ничего нет»
- `disciplina` (ветка `main`): **`fbf53c8`** — «dva-vhoda: вход уровня папки GitHub + строка в README» · `check --zone KAK-USTROEN-GITHUB.md` → ✅, `check --zone README.md` → ✅

⚠ **Чужой долг, отдельной строкой.** `git_zona.py check` по ВСЕМУ дереву `disciplina` красный (`rc=1`) — но ровно на одном файле, которого я не касался: `OTVETY-INTERVYU-2026-08-07.md`, рождён и никогда не ставился в git. По зонам захода оба гейта зелёные. Обход хука не понадобился, `--no-verify` не звался ни разу.

**НЕОБРАТИМОЕ:** необратимого нет. Ничего не удалено, не переименовано и не перемещено; `git reset`/`checkout` не звались. Единственные перезаписи — мои же два новых документа, переписанные набело после верификации (обе версии не существовали до этого захода, терять было нечего). Пробные лекции заведены только в scratchpad, репозитории не тронуты.

### Что сделал и зачем

Два входа, по одному в каждом репозитории, — чтобы знание «как всё устроено вместе» и «с чего начинать лекцию» имело версионируемый дом, а не жило в голове.

1. **`disciplina/KAK-USTROEN-GITHUB.md`** (51 строка) — вход уровнем выше репозиториев: почему папка не репозиторий и что из этого следует · состав командой · граница двух домов словом владельца · `GIT_ZONA_REPO` как единственная дверь инструментов к чужому репо · установка на новой машине ссылкой, не копией · одна дыра.
2. **`materials/LEKCIYA-s-chego-nachat.md`** (47 строк) — вход в работу с лекцией: чем лекция отличается от курса и обзора по устройству папки · первый ход одной командой · фазы и куда идти за дисциплиной каждой · чем меряется готовность · три дыры.
3. **По строке в каждый роутер.** `materials/README.md` §2 — строка «завожу НОВУЮ ЛЕКЦИЮ» в существующей таблице. `disciplina/README.md` — строка в существующем списке §«Документы для решений». ⚠ Заход говорил «в существующей таблице»; в `disciplina/README.md` таблица «Что внутри» перечисляет **папки**, и строка про файл в ней была бы неверна по смыслу — положил в соседний список документов. Одна строка, как и требовалось.

### 🔴 Главное: предпосылка захода была ложной, и это изменило работу

Заход (строка 11) утверждал: знание о том, как устроено всё вместе, «сегодня не живёт нигде». **Оно живёт** — `~/Documents/GitHub/README.md`, 63 строки, дата данных 2026-08-06, роутер «ситуация → дом» плюс таблица репозиториев. Три из пяти пунктов §2.1 он уже покрывал.

Заход прав в другом, и это спасает задачу: папка не под git (`test -e ~/Documents/GitHub/.git` → нет), поэтому у знания есть текст, но **нет дома**. Поэтому я не стал заводить вторую копию таблицы репозиториев (прямое нарушение правила одного дома: два списка разъедутся молча, и не-версионируемый разъедется первым), а написал то, чего в корневом README нет вовсе, состав дал командой, и назвал дублирование дырой. Развилка «переносить ли содержание целиком» — вопрос 1 владельцу.

Вторая неточность: «два репозитория». По команде их пять (`digest`, `disciplina`, `matema-fest`, `matemdigest-map`, `materials`) плюс `spetsmat` не под git и две worktree-папки. В документе состав считается командой, поэтому число нигде не зафиксировано руками.

### Как проверил

- **Живой прогон, не бумага.** Каждая команда обоих документов прогнана, код возврата снят первым: `bootstrap_lekcia.py` → `rc=0`, повтор на существующей папке → `rc=1` («не затираю»), `--slides yes` → `src/` из skeleton; `obzory/spisok.py` → `rc=0`; `check_termin.py` на обоих моих документах → `rc=0` (зелёный); `check_view.py` до сборки → `rc=1` с подсказкой «сначала собери» — это его вердикт, и так и написано в документе; `GIT_ZONA_REPO=… git_zona.py check` → `rc=1` (вердикт «есть вне git», не сбой) — тоже оговорено в тексте. Ни одна падающая команда в документы не попала.
  ⚠ Отдельно: на первом прогоне я снял `rc` после `| head` и получил код `head`, а не инструмента. Перепрогнал начисто — `git_zona.py check` даёт `rc=1`, а не `0`. Ровно та ловушка, про которую предупреждает §1.
- **Пути:** проверено **35**, битых **0** (скриптом `test -e` по каждому, с подсчётом).
- **Длины:** `wc -l` → 47 и 51 строка.
- **Диффы роутеров:** `git diff --numstat` → `1 0 README.md` в обоих репозиториях. Ровно по одной добавленной строке, ни одной изменённой.
- **Регистрация нового `.md`:** дверь вызвана, отказала штатно — `rc=2`, «дверь судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`». Файл лежит в корне `materials/`, не в корне реестра `korni.py`, ворота 5 его не судят. Поэтому `_studio/docs/KARTA.md` в пути коммита не добавлялся — регистрации не было. Хук не краснел, обход не звался. Подробнее с ценой — в `## УРОКИ ФАБРИКЕ`.

### Верификатор — два прохода, как требует §3

**Проход 1 (опора).** Свежий субагент получил только два готовых документа, без моего разбора, и искал опору сам. Механика сошлась полностью: все пути, коды возврата, номера строк (`korni.py:42`, `sostoyanie_lekcii.py:31`, `disable-model-invocation` в обоих `SKILL.md`), цитата владельца, устройство стадий и гейтов — подтверждено поимённо. **Разъехалось ровно то, что пришло из текста захода, а не с диска:**

| Что снял верификатор | Чем оказалось на проверке | Что сделал |
|---|---|---|
| «канон дисциплины в двух местах, источник правды не объявлен» | ложно: `_studio/docs/kak-delat/*` — четыре стаба «→ ПЕРЕЕХАЛ» (30–37 строк против 92–448 в `_meta/`) | дыру снял, факт назвал явно |
| «вынос `_generator` обсуждается» | решено владельцем: `OTVETY-INTERVYU-2026-08-07.md` §A.1 — «остаётся в `materials`, движок общий для курсов» | дыру снял, решение назвал |
| «обзоры и библиотека — кандидаты в репозитории» | опоры нет нигде, кроме самого захода; папки «библиотека» в `materials/` нет | утверждение убрал |
| «инструменты живут только в `materials/`» | у `disciplina` есть свои `tools/` | сузил до инструментов, ходящих в git и индексы |
| «`korni.py:42` — реестр корней и переменная» | на строке 42 только переменная; реестр — таблица `КОРНИ` ниже | развёл на два адреса |
| ссылка на `_meta/README.md` как на границу репозиториев | раздел делит `_meta`×`_studio` внутри `materials`, про `disciplina` молчит | ссылку снял |
| «бриф — девять полей (G1)» | скаффолд пишет 9, G1 проверяет 8 | стало дырой: расхождение реальное |
| перечень гейтов Фазы I | выпал G16 | дописал |
| «обзор — один файл в `obzory/`» | папка `obzory/<slug>/src/`; в дереве лекции опущен `dist/` | оба поправил |
| дыра про живые папки | своя же команда даёт `fibonacci/` (есть `SPEKA.md`, нет дневника), а объявленный эталон `otkrytaya-lekcia-paskal/` — наоборот | переписал: полного образца нет, есть две половины |

**Проход 2 (проходимость).** Второй субагент пошёл по документу буквально и завёл пробную лекцию в scratchpad: дошёл, `rc=0`, дерево появилось ровно как обещано, повтор дал `rc=1`, бриф заполнил. **Встал на скиллах** — и это оказалось не его ошибкой: `disable-model-invocation: true` блокирует не только автоподхват, но и прямой вызов по имени (`Skill(reserch-cikl)` → ошибка; в списке скиллов сессии их нет вовсе). Формулировка «срабатывают только на явный вызов по имени» обещает дверь, которой нет. Переписал на проверенное: скиллы недоступны исполнителю, поднять может только владелец, дошёл до такой стадии — спрашивай. Он же поймал: не сказано, **куда** класть папку в монорепо; `sostoyanie_lekcii.py` не падает, а даёт `rc=0` и правдоподобный мусор; не сказано, **чем** собирать перед `check_view`; при `--slides yes` в папке два разных `brief.md`. Всё четыре — в документе.

Оба документа после правок перепроверены: `check_termin.py` → `rc=0` на обоих, все пути на месте.

### Что НЕ трогал

`_studio/`, `_generator/`, `_illustracii/`, папки курсов; в `disciplina` — `skills/` и `arhiv-skillov/` (там идёт заход `gejt-vhozhdeniya`) и чужой незакоммиченный `OTVETY-INTERVYU-2026-08-07.md`. `~/Documents/GitHub/README.md` не тронут — он вне зоны и вне git; его судьба вынесена вопросом 1. Соседние строки обоих роутеров не переписаны. Ветка `zahod/dva-vhoda` не влита.

### Открытое «возвращаться»

- **Потолок 35 строк не выдержан** — 47 и 51. Сжимал двумя полными проходами; дальше режется содержание, которого требуют §2.1/§2.2 и условие 7. Считаю требование несовместимым с заданием, а не собой невыполненным; развёрнуто с ценой — в `## УРОКИ ФАБРИКЕ`. Решение — за вами: либо потолок поднимается, либо из документов уходит часть ответов, и тогда скажите какая.
- **`OTVETY-INTERVYU-2026-08-07.md` не под git**, а `KAK-USTROEN-GITHUB.md` на него ссылается как на источник решения про `_generator`. Пока файл не закоммичен, ссылка живёт на честном слове диска.
- **Дыр в сумме четыре**, все перечислены поимённо: в `KAK-USTROEN-GITHUB.md` — одна (два текста про папку, второй вне git); в `LEKCIYA-s-chego-nachat.md` — три (состояние лекции командой не спросить · скаффолд расходится с каноном в двух местах · полного образца папки на диске нет). Плюс `⚠ ДЫРА` внутри таблицы про курс: канона папки курса и скаффолда нет. Ноль дыр был бы подозрителен — здесь их пять, и три ЛОЖНЫЕ дыры пришлось, наоборот, снять.

**Время прогона и токены** — снимает приёмка из лога.

---

## ПРИЁМКА — заполняет аналитик, 2026-08-07

**ПРИНЯТ.** Два коммита: `ee5f062` в `materials` (ветка `zahod/dva-vhoda`) и `fbf53c8` в `disciplina`.

**Перепроверено приёмкой своими командами:**

| проверка | результат |
|---|---|
| `KAK-USTROEN-GITHUB.md` | ✅ 51 строка |
| `LEKCIYA-s-chego-nachat.md` | ✅ 47 строк |
| роутеры | ✅ по **одной** добавленной строке в каждом `README.md`, изменённых — 0 |
| 🔴 корневой `~/Documents/GitHub/README.md` | ✅ **существует, 63 строки** — предпосылка захода ложна |
| 🔴 число репозиториев | ✅ **пять**, не два — вторая ложная предпосылка |

### 🔴 Обе фактические предпосылки захода были ложными, и обе проверялись одной командой

Я написал, что знание «как устроено всё вместе» сегодня не живёт нигде. **Оно живёт** — корневой `README.md`, 63 строки, роутер «ситуация → дом» плюс таблица репозиториев; три из пяти пунктов §2.1 он уже покрывал. И репозиториев не два, а пять.

Ни одной из двух проверок я при сборке не сделал, хотя каждая — одна команда.

**Спасло то, что исполнитель проверил сам** и не стал заводить вторую копию таблицы репозиториев: два списка разъехались бы молча, и не-версионируемый разъехался бы первым. Цель захода уцелела по другой причине — папка не под git, у знания есть текст, но нет дома, — то есть **случайно, а не по замыслу**. → урок 33 с вердиктом: каждое «нет / не существует» в тексте захода подтверждается командой, и команда пишется рядом.

### Верификатор снял три ЛОЖНЫЕ дыры — это редкий и ценный исход

Обычно верификатор добавляет непокрытое. Здесь он вычеркнул выдуманное: «канон в двух местах» (оказалось — четыре стаба «переехал»), «вынос `_generator` обсуждается» (решено владельцем, §A.1), «обзоры и библиотека — кандидаты в репозитории» (опоры нет нигде, кроме моего же захода). Плюс девять уточнений по механике — от `korni.py:42` до состава дерева обзора.

Второй проход — **проходимость** — сделал главное: субагент пошёл по документу буквально, завёл пробную лекцию и **встал ровно там, где документ обещал дверь**.

### 🔴 Находка, которая пересматривает решение К-1

`disable-model-invocation: true` закрывает скилл **и для явного вызова по имени**, а не только от автоподхвата. Прямой вызов `reserch-cikl` и `slajdy` → ошибка «cannot be used due to disable-model-invocation»; в списке скиллов сессии их нет вовсе.

Наше описание в трёх местах (`README.md:35` и оба `SKILL.md`) обещает обратное: «активируются только по явному вызову по имени». **Оно ложно уже сейчас и правится независимо от любого решения.**

Решение К-1 (07.08, «поставить временно») принималось с неполной ценой: знали про потерю переносимости на claude.ai, не знали про потерю вызова агентом. → урок 35; полная цена и развилка — владельцу.

### Долги, найденные попутно (в `OTKRYTYE-ZADACHI.md`)

- **`sostoyanie_lekcii.py` и `check_kurs.py` зашиты на один проект** (`P = os.path.join(ROOT, 'teoriya-kategoriy', …)`, строка 31). Опаснее первый: на чужой лекции он не падает, а даёт `rc=0` и правдоподобный вердикт про чужой проект. Оба лежат в `_generator/tools/` и от общих инструментов неотличимы; расшивку через `korni.py` не прошли.
- **Скаффолд расходится с каноном дважды:** `bootstrap_lekcia.py` не заводит `SPEKA.md`, хотя `PAPKA-LEKCII.md` требует читать её первой; пишет в `brief.md` девять полей против восьми у гейта G1. Оба расхождения молчаливые.
- **Дверь регистрации отказывает на файле в корне репозитория** — контракт зоны требует регистрировать любой новый `.md`, исключения нет. Следующий исполнитель прочитает отказ как повод к `--no-verify`.

### Что осталось незакрытым

- **Потолок 35 строк не выдержан** (47 и 51) — и это мой дефект, а не его: я потребовал ответить на десять вопросов и тут же поставил потолок, не сказав, чем жертвовать. → урок 34.
- **`OTVETY-INTERVYU-2026-08-07.md` не под git**, а новый документ на него ссылается как на источник решения. Пока файл не закоммичен, ссылка живёт на честном слове диска. Закрывается любым заходом в `disciplina`.
- **Ветка `zahod/dva-vhoda` не влита**; в `disciplina` коммит лёг прямо в `main` — я не назвал для неё ветку в контракте зоны.
