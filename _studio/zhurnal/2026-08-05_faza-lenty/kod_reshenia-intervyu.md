# Канал исполнителя — reshenia-intervyu (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — механика расписана поимённо: каждая правка названа файлом, местом и текстом, план рождаться по ходу не должен.

КОНТЕКСТ. Дисциплины владельца вынесены из фабрики `materials/_studio/` в переносимый плагин Claude Code — репозиторий `disciplina`: семь скиллов (пять готовых, два черновика), дома инцидентов, инструменты проверки. Прошлый этап: ночная сборка 06.08 закрыла 18 этапов из 22 и оставила 29 вопросов, которые нельзя было решить без владельца. **07.08 владелец прошёл интервью целиком — все 29 имеют вердикт.** ЦЕЛЬ: внести решения в файлы, чтобы они перестали быть текстом в отдельном документе и стали состоянием репозитория.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ — назван заранее.** Ты закрываешь **механические** правки в репозитории `disciplina`. Ты НЕ переписываешь содержание скиллов (это этап 20, отдельный заход, идёт после), НЕ трогаешь репозиторий `materials` (четыре правки там — отдельный заход, зона другая), НЕ отключаешь внешние скиллы (для этого сначала нужен гейт вхождения — тоже отдельный заход).

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **РЕПОЗИТОРИЙ РАБОТЫ — НЕ ТОТ, В КОТОРОМ ЛЕЖИТ ЭТОТ ФАЙЛ.** Файл-заход живёт в `materials`, а работаешь ты в **`~/Documents/GitHub/disciplina`**. Инструменты `_generator/tools/*` лежат в `materials` и на чужой репозиторий наводятся ЕДИНСТВЕННОЙ дверью — переменной `GIT_ZONA_REPO` (`korni.py:42`). Забудешь её — инструмент молча отработает по `materials` и коммитнет не туда.
  **Каждый вызов инструмента — в этой форме, без исключений:**
  ```
  export GIT_ZONA_REPO=~/Documents/GitHub/disciplina
  export MAT=~/Documents/GitHub/materials
  ```
- **МЕСТО РАБОТЫ:** **рабочая папка `../disciplina-wt/wt-reshenia`** — worktree захода на ветке `zahod/reshenia-intervyu`. 🔴 **Заводишь её САМ, первым ходом, из корня `disciplina`:**
  ```
  cd ~/Documents/GitHub/disciplina
  python3 $MAT/_generator/tools/git_zona.py worktree add wt-reshenia --branch zahod/reshenia-intervyu
  cd ../disciplina-wt/wt-reshenia
  ```
  🔴 **Worktree обязателен, а не «на всякий случай»: параллельно с тобой в ЭТОМ ЖЕ репозитории идёт второй заход** (`kod_standart-skilla.md`, ветка `zahod/standart-skilla`). Он ничего не правит и заводит один новый файл, но `git checkout` в основной папке подменит файлы у него под ногами.
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** всё в репозитории `disciplina`, **кроме** `skills/reserch-cikl/SKILL.md` и `skills/slajdy/SKILL.md` — их тела переписывает этап 20, и до него в них правятся ТОЛЬКО названные ниже строки, точечно. Репозиторий `materials` — **READ-ONLY целиком**, включая `_generator/`: там свои четыре правки и свой заход. Нашёл проблему вне зоны → в отчёт, не трогай.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/reshenia-intervyu`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: зона не чиста в HEAD — закоммить как есть перед началом; чиста — не фабрикуй.
- **Прочитать ТОЛЬКО эти файлы, в этом порядке. Проект не изучай, по ссылкам внутри них не ходи.**
  1. `~/Documents/GitHub/disciplina/OTVETY-INTERVYU-2026-08-07.md` — **главный вход.** Вердикты владельца по всем 29 пунктам, со сводкой отклонений в конце.
  2. `~/Documents/GitHub/disciplina/README.md` — что где лежит в репозитории.
  3. `~/Documents/GitHub/disciplina/doma/README.md` — устройство домов инцидентов.
  Отдельные файлы, которые правишь, открывай по ходу задачи — они названы поимённо в §2.
- ПЛАН — в `## ПЛАН` перед действиями.

🔴 **ГЛАВНАЯ ЛОВУШКА ЭТОГО ЗАХОДА.** `OTVETY-INTERVYU-2026-08-07.md` — не список «сделай так». В шести пунктах владелец **разошёлся с рекомендацией**, и в двух местах его решение **отменяет решение, принятое выше в том же файле** (п.16 отменяет адрес из п.11; пересмотр п.6 отменяет формулировку п.6). Читая сверху вниз линейно, ты возьмёшь отменённую версию: отмена всегда стоит ПОСЛЕ. **Читай файл дважды — второй раз от конца к началу**, начиная со сводки «где владелец разошёлся с рекомендацией». Расхождение, которого нет в сводке, но которое ты видишь в теле, — в `## ВОПРОСЫ`, не решай сам.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Одиннадцать правок. Каждая — самостоятельна; провал одной не отменяет остальных. Порядок задан «дёшево и заметно вперёд» (урок канона: правка на десять секунд конкурирует не за время, а за доверие).

**Адресуйся ЗАГОЛОВКАМИ, не номерами строк** — номера умирают при первой же вставке.

---

### 2.0 Подключить плагин `disciplina` — первым ходом, до правок

Плагин собран на диске, но **не подключён**: в списке скиллов сессии его нет ни под одним именем. Владелец просил, чтобы это сделал исполнитель, а не он руками.

Маркетплейс зарегистрирован под именем `disciplina`, манифесты на месте (`.claude-plugin/marketplace.json`, `.claude-plugin/plugin.json` — проверены аналитиком 07.08).

```
claude plugin marketplace add ~/Documents/GitHub/disciplina
claude plugin install disciplina@disciplina
```

🔴 **Форму команды НЕ бери отсюда на веру — она не проверена.** Сначала `claude plugin --help` и `echo $?`; подкоманды называются иначе или CLI их не имеет вовсе — **напиши это в `## ВОПРОСЫ` и иди дальше к 2.1**. Подключение — не блокирующий пункт: остальные десять правок от него не зависят. Выдуманная команда хуже отсутствующей: владелец выполнит её в zsh и получит четыре `command not found` — это уже случалось.

**Проверка, а не ощущение:** после установки скиллы обязаны быть видны по именам `disciplina:*` — назови в отчёте, **сколько их видно и сколько ожидалось (7)**. Видно 0 при успешной команде — это красное, а не зелёное.

⚠ **Отключать старые аналоги ЗАПРЕЩЕНО** — ни `session-handoff`, ни `the-interviewer`, ни `agentic-coding-session-brief`, ни `popsci-*`, ни `html-slides-studio`, ни `math-russian-terminology`. Владелец поставил условием гейт вхождения: сначала предъявить, что уникальное содержание каждого доехало в наши скиллы. Это отдельный заход.

### 2.1 Дома: слить `lenta` в `reserch`, переименовать в `reserch-cikl` (решение 7)

Сейчас `doma/reserch/` и `doma/lenta/` — два дома на один скилл, и `check_uroki_doma.py` не может судить, в верный ли дом приехал урок.

- `doma/lenta/POKRYTIE.md` слить в `doma/reserch/POKRYTIE.md`. **Слить, а не заменить:** оба непусты, сверь содержимое и сохрани обе стороны; совпадающие строки не дублируй.
- Папку `doma/reserch/` переименовать в `doma/reserch-cikl/`.
- Обновить все ссылки на оба старых адреса. Найти их командой, не памятью:
  ```
  grep -rn "doma/lenta\|doma/reserch/" . --exclude-dir=.git
  ```

### 2.2 Таблица СКИЛЛ → ДОМ в `doma/README.md` (решение 8)

Дома по скиллам НЕ дробим. Вместо этого — явная таблица, где «много скиллов → один дом» разрешено вслух:

| скилл | дом |
|---|---|
| `disciplina-git` · `disciplina-zahody` · `disciplina-arka` · `disciplina-istochnik-pravdy` · пятый, «гигиена сессии», когда появится | `meta` |
| `illustracii` | `illustracii` |
| `reserch-cikl` | `reserch-cikl` |
| `slajdy` | `slajdy` |

Тем же ходом — правило одной строкой: **имя дома совпадает с именем скилла**; исключение только то, что явно стоит в таблице.

### 2.3 Забрать всё из `html-slides-studio` (решение 12)

Внешний скилл будет отключён, но **не этим заходом** — сначала его содержание должно доехать. Владелец: *«я бы брал по максимуму»*.

Источник — `arhiv-skillov/html-slides-studio/`. Скопировать **все** reference-файлы в `skills/slajdy/references/`, включая `decks/*.md` и `rollout-map.md`. Каждому файлу — шапка первой строкой:

```
> Перенесено из внешнего скилла `html-slides-studio` 2026-08-07 (решение 12).
> При расхождении с нашим каноном главное — НАШЕ.
```

`free-style-presets.md` — забрать тоже, но шапку усилить: `> 🔴 ЧУЖИЕ стилевые пресеты. Каноном НЕ являются, справочно.`

**Охват называй числом, посчитанным ДО работы:** `ls arhiv-skillov/html-slides-studio/references/ | wc -l` → столько же файлов обязано лежать в `skills/slajdy/references/` после.

### 2.4 Голосовой корпус переезжает к СЛАЙДАМ, не к ресёрчу (решения 11 + 16)

🔴 **Здесь та самая отмена.** П.11 назвал домом корпуса ресёрч-цикл; п.16 это **отменил**: корпус — это 189 текстовых СЛАЙДОВ, он обслуживает предвёрсточный документ, а не текст для чтения. Правильный адрес — **`skills/slajdy/references/texts-corpus.md`**.

- Положить корпус по этому адресу (источник — `arhiv-skillov/content-studio/assets/corpus/texts-corpus.md`; путь проверь `ls`, не бери на веру).
- В `skills/slajdy/SKILL.md` — строка: «примеры текстов слайдов — `references/texts-corpus.md`».
- В `skills/reserch-cikl/SKILL.md` — там, где сейчас стоит ссылка на корпус, поставить: `⚠ ДЫРА: свод правил и голос ленты собираются по СВОИМ эталонам, корпус слайдов сюда не применяется (решение 16)`.

### 2.5 Лента ЕСТЬ на входе слайдов (решение 3)

`skills/slajdy/SKILL.md §0` перечисляет пять артефактов входа, и ленты среди них нет. Владелец: *«перед началом работы слайд-студио уже должен быть документ с содержанием лекции»*.

- В `§0` добавить ленту первым входом.
- «Закрытый гибрид-источник» из того же списка — **это она же, под вторым именем** (решение 3б). Второе имя убрать, оставить «лента».

🔴 **И вписать два артефакта, которых нет ни в одном файле проекта.** Это знание существует только в `OTVETY-INTERVYU-2026-08-07.md §3` — перенеси таблицу оттуда в `§0` дословно: лента на входе → **интервью** → **сценарий лекции** (разбиение на слайды, стилистика та же, что у ленты) → **предвёрсточный документ** (короткие тексты слайдов, стилистика плакатная, отсюда идёт вёрстка). Оба внутренних артефакта принадлежат слайдам и к `reserch-cikl` не привязаны.

### 2.6 Шаг 6 раздваивается (решение 2)

- `skills/reserch-cikl/SKILL.md`: раздел «Шаг 6» → «**Шаг 6 · лента**». Убрать из него раскладки и разметку `{@N}` — они про кадр, у ленты кадра нет.
- `skills/slajdy/SKILL.md`: новый раздел «**Шаг 6 · предвёрсточный документ**» + строка «стиль текста — скилл `reserch-cikl`, вызвать по имени».

### 2.7 Фаза 04.5 в слайды, фаза 09 — вызовом (решение 4)

Сегодня дек физически не собирается: `§0` требует `KONTRAKT-<артефакт>.md` (гейт G16, блокирующий), а производить его некому.

- В `skills/slajdy/SKILL.md` — новый раздел «Шаг 4.5 · контракт артефакта». Готовая копия лежит в `skills/slajdy/references/dok-04.5-intervyu.md`.
- В раздел про иллюстрации — строка: «иллюстрации — скилл `illustracii`, вызвать по имени; боксы до вызова остаются пустыми». Саму дисциплину иллюстраций НЕ копировать.

### 2.8 Шесть развилок слайдов вместо «не выбрано» (решение 21)

В `skills/slajdy/SKILL.md §8` заменить каждое «не выбрано» на решение, **с датой 2026-08-07**:

| развилка | решение |
|---|---|
| структура записи слайда | **таблица в `brief.md`** ⚠ *не прогонялось на живом деке; пересмотреть после первого дека — выбор аналитика, не владельца* |
| форма кликабельного демо | `slides/*.html` + `index.html` с prev/next |
| поле «регистр + бюджет» в `brief.md` | утверждено как предложено |
| bespoke грид vs общие классы | bespoke |
| blur-reveal CSS | в `base.css` |
| delivery vs working копия | working с тогглом; delivery не заводить |

### 2.9 Механика `fill` (решение 22)

Одна строка в разделе про шорткаты `skills/slajdy/SKILL.md`: «`{fill@N|a|b}` — подмена значения на значение; `____` не использовать». Слово «бланк» в контракте шортката **оставить** — оно называет роль, а не рисунок.

### 2.10 Заголовок слайда — поле раскадровки (решение 19)

В `skills/slajdy/SKILL.md`: поле `zagolovok` в таблице слайдов; строка «заголовок живёт в предвёрсточном документе, не слоем `<h1>` в деке — правится источник, а не артефакт». Пункт линтера («у каждого слайда заголовок непуст») здесь только ОПИСАТЬ: сам линтер живёт в `materials/_generator/`, это вне твоей зоны.

### 2.11 Уточнения помельче (решения 14, 15, 23, 28)

- **14 · лёгкий режим `reserch-cikl`.** Раздел «Лёгкий режим» перед шагом 1: три поля входа — тема · аудитория · throughline-кандидат. **Аудитория имеет значение по умолчанию: сам владелец**; спрашивается, только когда текст делается для кого-то другого. Гейты G2 (≥5 источников, ни одной находки без карточки) и G3 (математика верна) в лёгком режиме **остаются** — они про честность, а не про объём.
- **15 · плакатный стиль к ленте.** В `reserch-cikl` из правил плакатного стиля остаются ДВА: «закон полноты формулировки» и «сначала рассуждение, потом вывод». Остальные — в слайдовый шаг 6. На их месте: `⚠ ДЫРА: свод правил ленты собирается наблюдениями по эталонам`.
- **23 · терминология втягивается.** Владелец решил **втянуть `math-russian-terminology` в `reserch-cikl` разделом**, а не оставлять внешним скиллом. Источник — `arhiv-skillov/math-russian-terminology/SKILL.md`; если его там нет, скажи в `## ВОПРОСЫ` и не сочиняй. Запрет скилла на шагах 2–3 снять. **Отключение внешнего — НЕ твой ход** (нужен гейт вхождения, решение 5).
- **28 · 3D.** Одна строка в `skills/illustracii/SKILL.md`: «3D — внешний скилл `threejs`, вызывать по имени; дисциплина примитивов на 3D не распространяется». Ветку 3D НЕ переносить.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Пять условий, все машинные:

1. `python3 $MAT/_generator/tools/git_zona.py check --zone .` → ✅, зона чиста.
2. Старых адресов домов не осталось: `grep -rn "doma/lenta\|doma/reserch/" . --exclude-dir=.git` → **0 строк**. Папка `doma/reserch-cikl/` существует, `doma/lenta/` — нет.
3. Перенос из `html-slides-studio` полон: число файлов в `skills/slajdy/references/` **≥** числа в `arhiv-skillov/html-slides-studio/references/`, оба числа названы командой в отчёте. Файлов без шапки «перенесено … главное — НАШЕ» — **0**.
4. Пометок «не выбрано» в `skills/slajdy/SKILL.md` — **0**: `grep -c "не выбрано" skills/slajdy/SKILL.md`.
5. `python3 tools/polnota_perenosa.py` → `rc=0` **или** каждая названная им потеря объяснена строкой в отчёте.
6. **Живой прогон на реальных объектах репозитория, не на фикстуре:** `python3 tools/inventar.py` и `python3 tools/check_uroki_doma.py` отработали на живых файлах после правок, `echo $?` напечатан у обоих. `check_uroki_doma.py` **краснеет законно** (7 уроков без дома — известный долг, решение 9); его красное провалом не считается, но вывод обязан быть в отчёте дословно. Если он покраснел ИНАЧЕ — на путях, которых ты касался, — это твоя регрессия.

**Охват — числом, и число считается ДО работы.** Правок в этом заходе одиннадцать. В отчёте: «внесено X из 11», и по каждой невнесённой — причина. «Всё сделано» без числа не принимается.

## 3. ВЕРИФИКАТОР

Нужен: заход **двигает и сливает** файлы (2.1, 2.3, 2.4), а это лоссовые операции.

Свежий субагент, метод ДРУГОЙ — сверка с `git show HEAD:<путь>` до слияния, не перечитывание своей же работы. Что обязан проверить программно:

- в слитом `doma/reserch-cikl/POKRYTIE.md` присутствуют строки **обеих** исходных версий; список потерь ПУСТ;
- битых ссылок после переименования — 0;
- ни один файл `arhiv-skillov/` не удалён и не изменён: архив — слепок, он read-only по смыслу.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
cd ../disciplina-wt/wt-reshenia
git --no-optional-locks add -- doma/ skills/ tools/ README.md
git --no-optional-locks commit -m "решения интервью 07.08: дома, перенос html-slides-studio, развилки слайдов" -- doma/ skills/ tools/ README.md
GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 $MAT/_generator/tools/git_zona.py check --zone .
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **Пути в `add`/`commit` — те, что ты реально тронул**, а не список выше дословно: он образец формы, а не перечень. Тронул новый корень — допиши его в ОБА хода.
🔴 **`GIT_ZONA_REPO` в вызове `git_zona.py` обязательна.** Без неё инструмент судит `materials` — репозиторий, в котором ты не работал, и напечатает бодрое ✅ ни о чём.
⚠ **Регистрация нового `.md` (`register_doc.py`) в репозитории `disciplina` не работает**: у него нет `_studio/docs/KARTA.md` в том виде, какого ждёт дверь. Новых `.md` этот заход и не заводит. Заведёшь — не обходи хук молча, назови в отчёте.
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Первый ход выполнен: worktree `../disciplina-wt/wt-reshenia` на ветке `zahod/reshenia-intervyu` заведён (была новая), `git rev-parse --abbrev-ref HEAD` подтвердил ветку, зона чиста. Прочитаны `OTVETY-INTERVYU-2026-08-07.md` (дважды, включая сводку расхождений с конца), `README.md`, `doma/README.md`.

Критерий готовности кривых мест не нашёл — иду по нему как есть.

Порядок 11 правок — как в §2 файла (уже «дёшево и заметно вперёд»):

1. **2.0** — сначала `claude plugin --help`, `echo $?`; форму команд не выдумываю. Не блокирует остальное.
2. **2.1** — `doma/lenta/POKRYTIE.md` слить в `doma/reserch/POKRYTIE.md` (сверка руками, не перезапись), `git mv doma/reserch → doma/reserch-cikl`, грепом найти и поправить все ссылки на оба старых адреса.
3. **2.2** — таблица СКИЛЛ→ДОМ + правило «имя дома = имя скилла» в `doma/README.md`.
4. **2.3** — посчитать `ls arhiv-skillov/html-slides-studio/references | wc -l` ДО копирования, скопировать всё (включая `decks/*.md`, `rollout-map.md`, `free-style-presets.md`) в `skills/slajdy/references/`, каждому файлу — шапку-перенос (усиленную у `free-style-presets.md`).
5. **2.4** — 🔴 отмена п.11 п.16: корпус → `skills/slajdy/references/texts-corpus.md` (источник — `arhiv-skillov/content-studio/assets/corpus/texts-corpus.md`, путь проверю `ls`). Строка в `slajdy/SKILL.md`, строка-дыра в `reserch-cikl/SKILL.md`.
6. **2.5** — 🔴 решение 3+3б: `slajdy/SKILL.md §0` — лента первым входом, «закрытый гибрид-источник» → «лента», таблица трёх артефактов (лента → интервью → сценарий лекции → предвёрсточный документ) дословно из `OTVETY §3`.
7. **2.6** — `reserch-cikl`: «Шаг 6» → «Шаг 6 · лента», убрать раскладки/`{@N}`. `slajdy`: новый «Шаг 6 · предвёрсточный документ» + строка про вызов `reserch-cikl` по имени.
8. **2.7** — новый раздел «Шаг 4.5 · контракт артефакта» из `skills/slajdy/references/dok-04.5-intervyu.md` (готовая копия — проверю, что файл там уже есть); строка про `illustracii` в разделе иллюстраций, саму дисциплину не копировать.
9. **2.8** — в `slajdy/SKILL.md §8` заменить все «не выбрано» на 6 решений таблицы, дата 2026-08-07.
10. **2.9** — одна строка `{fill@N|a|b}` в разделе шорткатов, «бланк» не трогать.
11. **2.10** — поле `zagolovok` в таблице слайдов + строка про источник заголовка; линтер только ОПИСАТЬ, не писать (вне зоны).
12. **2.11** — 14 (лёгкий режим `reserch-cikl`, аудитория=владелец по умолчанию), 15 (два правила плакатного стиля к ленте + дыра), 23 (втянуть `math-russian-terminology` в `reserch-cikl`, проверю наличие источника перед копированием), 28 (строка про `threejs` в `illustracii`).

После правок — 6 условий критерия готовности по порядку, затем свежий субагент-верификатор (сверка с `git show HEAD:<путь>` до слияния — 2.1/2.3/2.4), затем коммит зоны (два хода `add`+`commit -- <пути>`), затем ОТЧЁТ.

Не трогаю: содержание скиллов (этап 20), репозиторий `materials` (read-only), отключение внешних скиллов (нужен гейт вхождения), `skills/reserch-cikl/SKILL.md` и `skills/slajdy/SKILL.md` целиком — только названные строки/разделы.


## ВОПРОСЫ — (заполняет исполнитель)

1. Задача 2.8 адресовала «шесть развилок, замени "не выбрано" в `skills/slajdy/SKILL.md §8`» —
   но `SKILL.md §8` («Якоря») этого текста никогда не содержал. Реальный адрес — `skills/slajdy/
   DYRY.md §8`; сам маркер «не выбрано» встречался ОДИН раз в заголовке этого раздела, а не
   построчно. Нашёл грепом по всему репозиторию, исполнил по факту (закрыл `DYRY.md §8` таблицей
   решений + вписал сами решения в operative-места `SKILL.md`, где каждая развилка реально
   обсуждается), а не по названному пути. Критерий готовности №4 из-за этого тривиально зелёный
   независимо от того, сделана ли работа — стоит поправить формулировку критерия на будущее.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: reshenia-intervyu

2. Критерий готовности №2 («`grep doma/lenta\|doma/reserch/` → 0 строк») структурно конфликтует
   с §0 того же файла-захода, который запрещает трогать `INTERVYU-utro.md` (объяснение: файл
   держит открытым владелец, правка из-под сессии рискует затереть). `INTERVYU-utro.md` содержит
   оба старых адреса в тексте вопроса №7 (ещё не сведённого в ответ — сведение туда отдельным
   ходом). Оставил файл нетронутым, критерий №2 закрыл с 2 задокументированными строками вместо 0
   строк — см. ОТЧЁТ.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: reshenia-intervyu

3. `check_uroki_doma.py` на арке `2026-08-05_faza-lenty` даёт «9 уроков без дома», а решение 9 в
   `OTVETY-INTERVYU-2026-08-07.md` и сам `INTERVYU-utro.md` называют долг «7 уроков». Проверил
   стэшем — расхождение существовало ДО моих правок этого захода, не регрессия. Число, видимо,
   устарело с момента, когда решение 9 писалось. Не поправлял: перенос уроков — предмет отдельного
   захода («перенести 7 уроков арки в дома по новому правилу»), не этого.
   ДОМ: 2026-08-05_faza-lenty/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: reshenia-intervyu

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Охват: внесено 11 из 11.** Ни одна правка не провалена целиком; расхождения с буквой задания —
задокументированы построчно ниже, а не молчаливо.

### Что сделал + зачем

- **2.0 Плагин.** `claude plugin --help`/`marketplace --help` проверены ДО использования (форма
  подтверждена, не выдумана). `claude plugin marketplace add ~/Documents/GitHub/disciplina` +
  `claude plugin install disciplina@disciplina` — оба rc=0. `claude plugin details
  disciplina@disciplina` показал **7 из 7** ожидаемых скиллов (`disciplina-arka`, `disciplina-git`,
  `disciplina-istochnik-pravdy`, `disciplina-zahody`, `illustracii`, `reserch-cikl`, `slajdy`).
- **2.1 Дома.** `doma/lenta/POKRYTIE.md` слит в `doma/reserch/POKRYTIE.md` (обе стороны сохранены,
  один найденный пробел — см. «Результат верификатора» — исправлен), папка переименована в
  `doma/reserch-cikl/`. Ссылки поправлены в `skills/reserch-cikl/DYRY.md` (пункт 18 закрыт).
  Не тронул: `INTERVYU-utro.md` — см. ВОПРОСЫ №2.
- **2.2 Таблица СКИЛЛ→ДОМ.** `doma/README.md` — новый §1а с таблицей и правилом «имя дома = имя
  скилла»; §1 переименован «Пять» → «Четыре дома» (слияние 2.1 меняет счёт).
- **2.3 html-slides-studio.** Охват ДО работы: `ls arhiv-skillov/html-slides-studio/references |
  wc -l` = **13** (top-level; рекурсивно — 21 файл, включая `decks/` и оба не-md файла в ней).
  Скопировано всё дерево в `skills/slajdy/references/`; после — **24** файла тем же `ls | wc -l`
  (24 ≥ 13). Всем 21 файлам — шапка «перенесено … главное — НАШЕ» (у `.py`/`.html` — в синтаксисе
  комментария их языка), `free-style-presets.md` — шапка усилена красным предупреждением. Архив не
  тронут (проверено верификатором).
- **2.4 Голосовой корпус.** Скопирован в `skills/slajdy/references/texts-corpus.md` (путь источника
  проверен `ls` до копирования, диф после — байт-в-байт, см. верификатор). Строка в
  `slajdy/SKILL.md` про `references/texts-corpus.md` и дыра-заглушка в `reserch-cikl/SKILL.md`
  («Голос ленты» — корпус слайдов сюда не применяется) — обе на месте; попутно закрыл секцию
  «Голос ленты», которая раньше противоречила решению 16 (говорила «голос-корпус обязателен»).
- **2.5 Лента на входе.** `slajdy/SKILL.md §0`: лента — таблица-строка №1 («первый и обязательный
  вход», цитата владельца дословно); «закрытый гибрид-источник» переименован в «лента» (решение
  3б, второе имя убрано); таблица трёх артефактов (лента → интервью → сценарий лекции →
  предвёрсточный документ) внесена дословно из `OTVETY §3`.
- **2.6 Шаг 6 раздваивается.** `reserch-cikl/SKILL.md`: «Шаг 6 · лента»; убрана раскладка (гейт
  12→11 пунктов, перенумерован) и `{@N}` из шорткатов (оставлены только `fill@`/`blur@`, они не
  были названы в задании на удаление). `slajdy/SKILL.md`: новый раздел «Шаг 6 · Предвёрсточный
  документ» + строка «стиль текста — `reserch-cikl`, вызвать по имени».
- **2.7 Фаза 4.5.** Новый раздел «Шаг 4.5 · Контракт артефакта» в `slajdy/SKILL.md`, указывает на
  готовую копию `references/dok-04.5-intervyu.md` (её провенанс-шапка тоже поправлена — раньше
  говорила «эта фаза принадлежит ресёрч-циклу», что решение 4 отменяет). Строка про `illustracii`
  вписана в §3 рядом с плейсхолдерами `illustrations/<name>`, где она по смыслу.
- **2.8 Шесть развилок.** Реальный адрес маркеров — не тот, что назван в задании; см. ВОПРОСЫ №1.
  Закрыл по факту: `DYRY.md §8` — таблица всех шести решений с датой; сами решения вписаны в
  operative-места `SKILL.md`, где каждая развилка обсуждается (структура записи — §3, форма демо —
  §3, поле регистр+бюджет — §3, bespoke-грид — §4, blur-CSS в `base.css` — §5, working-копия — §6).
  Критерий №4 (`grep -c "не выбрано"` в SKILL.md) — 0 и до, и после (маркер там не жил).
- **2.9 Fill.** Строка «`{fill@N|a|b}` — подмена значения на значение; `____` не использовать» —
  рядом с существующим определением синтаксиса. «Бланк» в контракте шортката не тронут.
- **2.10 Заголовок.** Поле `zagolovok` — пятое поле таблицы слайда в §3, со строкой «заголовок живёт
  в предвёрсточном документе, не `<h1>`»; линтер-пункт только описан (сам линтер — `materials/
  _generator/`, вне зоны).
- **2.11 Четыре уточнения.** 14 — раздел «Лёгкий режим» перед Шагом 1 (три поля, аудитория=владелец
  по умолчанию, гейты G2/G3 не меняются). 15 — в `reserch-cikl` остались два правила плакатного
  стиля (закон полноты, рассуждение→вывод), ~18 остальных физически ПЕРЕНЕСЕНЫ (не продублированы)
  в `slajdy/SKILL.md` «Шаг 6», дыра-заглушка на месте. 23 — содержание `math-russian-terminology`
  втянуто разделом в `reserch-cikl/SKILL.md` (принцип, порядок, источники, «не делать» — по
  источнику `arhiv-skillov/math-russian-terminology/SKILL.md`, он на месте); запрет вызывать скилл
  на шагах 2–3 снят (звать больше нечего). 28 — строка про `threejs` в `illustracii/SKILL.md`.
  Внешние скиллы НЕ отключал ни в одном из четырёх случаев — это отдельный ход после гейта
  вхождения (§2.0 предупреждение, решение 5).

### Как проверил

Все 6 условий критерия готовности — командами, не на глаз:
1. `git_zona.py check --zone .` → ✅ (прогнан дважды: до и после правки по находке верификатора).
2. `grep doma/lenta\|doma/reserch/` → **2 строки**, не 0 — обе в `INTERVYU-utro.md`, задокументировано
   как расхождение с критерием в ВОПРОСЫ №2. Папки: `doma/reserch-cikl/` есть, `doma/lenta/` нет.
3. Перенос html-slides-studio: 13 → 24 (≥), 0 файлов без шапки среди 21 перенесённых.
4. `grep -c "не выбрано" skills/slajdy/SKILL.md` → 0.
5. `python3 tools/polnota_perenosa.py` → **rc=0**, все 5 пар зелёные, потерь 0.
6. Живой прогон (не фикстура): `inventar.py` → rc=0. `check_uroki_doma.py` на арке
   `2026-08-05_faza-lenty` → rc=1, «9 уроков без дома» (дословно в блоке ниже). Сверил стэшем: то
   же самое ДО моих правок — не регрессия, долг числом расходится с «7» из решения 9 (ВОПРОСЫ №3).

```
❌ УРОК НИКУДА НЕ ПРИЕХАЛ — он существует только в арке, которую закроют (RUKOVODSTVO §АДРЕС УРОКА, KONSTITUCIYA §11):
  [9 уроков арки 2026-08-05_faza-lenty#1,2,3,4,6,7,8,9,10 — не найдены во всех 4 домах: illustracii, meta, reserch-cikl, slajdy]
Дома: .../wt-reshenia/doma
Проверено уроков: 9, без дома: 9.
```

### Что НЕ трогал

Содержание скиллов (этап 20) · репозиторий `materials` целиком, включая `_generator/` (read-only) ·
отключение внешних скиллов (`session-handoff`, `the-interviewer`, `agentic-coding-session-brief`,
`popsci-*`, `html-slides-studio`, `math-russian-terminology`) — все живы, только их содержание
частично втянуто внутрь по названным решениям · `skills/reserch-cikl/SKILL.md` и
`skills/slajdy/SKILL.md` — только названные строки/разделы, не тело целиком · `INTERVYU-utro.md` —
сведение ответов в исходник прямо запрещено этим же файлом-заходом (владелец мог держать файл
открытым) · `_studio/docs/KARTA.md` — новых `.md` этот заход не завёл, регистрировать нечего.

### Результат верификатора

Свежий субагент, метод — сверка с `git show HEAD:<путь>` (коммит ещё не был сделан на момент
проверки). Три лоссовые операции (2.1 слияние, 2.3 копирование, 2.4 копирование корпуса):
- Копии 2.3 и 2.4 — **PASS**, байт-в-байт/только-шапка-сверху, архив `arhiv-skillov/` не тронут ни
  байтом (`git diff --stat` пуст).
- Слияние 2.1 — нашёл **одну реальную потерю**: §3 обеих исходных `POKRYTIE.md` содержал
  перечисление пяти существовавших на 2026-08-06 скиллов и факт «скилла про ресёрч/ленту среди них
  ещё нет» — в мою переписанную версию §3 (про текущее состояние) это не попало. **Исправил**:
  вернул исходный факт с датой данных, добавил актуальное состояние отдельным абзацем — теперь оба
  присутствуют. Битых ссылок после переименования — 0.

### Открытое «возвращаться»

Три пункта в ВОПРОСЫ (адрес критерия 2.8, конфликт критерия №2 с запретом на `INTERVYU-utro.md`,
расхождение 7↔9 уроков) — не мои решения, для аналитика/владельца. Прочих открытых хвостов нет:
все 11 правок закрыты, гейт `pre-commit` прошёл зелёным без единого обхода.

**Необратимого нет** (в смысле «не восстановить») — все изменения в `git`, коммит `dc718f7`
восстановим (`git show dc718f7^:<путь>` даёт состояние ДО этого захода по каждому тронутому
файлу). Для протокола: `doma/lenta/POKRYTIE.md` и `doma/reserch/POKRYTIE.md` **удалены** (слиты в
`doma/reserch-cikl/POKRYTIE.md`, тот же коммит) — восстанавливается `git show dc718f7^:doma/lenta/
POKRYTIE.md` (или откат ветки до родителя коммита). Установка плагина `disciplina@disciplina` —
не git-операция, а состояние Claude Code CLI на этой машине; отменяется `claude plugin uninstall
disciplina@disciplina`.

**АРТЕФАКТ:** артефакта нет — заход правит файлы дисциплины (`doma/`, `skills/`), не собирает
HTML/PDF/документ. Смотреть — сами файлы по путям выше, в `~/Documents/GitHub/disciplina-wt/
wt-reshenia/`.
**КОММИТ:** `dc718f7fed475d6ceed585cf06e30d37b4d87f8a` — «решения интервью 07.08: дома, перенос
html-slides-studio, развилки слайдов» · `git_zona.py check --zone .` → ✅ (прогнан после коммита).

---

## ПРИЁМКА — заполняет аналитик, 2026-08-07

**ПРИНЯТ.** Гейт 0 прогнан приёмкой самостоятельно: `GIT_ZONA_REPO=~/Documents/GitHub/disciplina git_zona.py check --zone .` → ✅. Коммит `dc718f7`, 33 файла, 12196 вставок / 247 удалений.

**Перепроверено своими командами, не перечитыванием отчёта:**

| проверка | результат |
|---|---|
| `doma/` после слияния | ✅ `reserch-cikl` есть, `lenta` нет, домов четыре |
| старые адреса домов по всему репозиторию | ✅ ровно **2**, обе в `INTERVYU-utro.md` — совпадает с заявленным |
| `slajdy/references/` до → после | ✅ 13 → 25 (`ls-tree`), охват выполнен |
| лента в `§0` слайдов | ✅ строка 51, «первый и обязательный вход», цитата владельца дословно; таблица трёх артефактов на месте |
| `«не выбрано»` — где жило ДО захода | ✅ подтверждено: `git grep "не выбрано" f6fc2e1 -- skills/` → **одна строка, `DYRY.md:91`**, заголовок раздела |

**Расхождений между отчётом и диском не найдено. Заявленное соответствует сделанному.**

### 🔴 Три вопроса исполнителя — все три дефекты ЗАХОДА, не работы

Составитель признаёт каждый; ни один не является ошибкой исполнения.

1. **Адрес правки 2.8 назван по памяти** — послал в `SKILL.md §8` («Якоря»), маркер жил в `DYRY.md §8`. Хуже промаха следствие: критерий №4 (`grep -c "не выбрано" skills/slajdy/SKILL.md` → 0) давал ноль **и до работы, и после** — гейт был зелёным по построению. Это вопрос 6 гейта входа, и он не был прогнан. Спас исполнитель, найдя настоящий адрес грепом и исполнив по факту. → урок 11.
2. **Критерий №2 требовал нуля там, где §0 того же захода запрещал править файл.** Два обязательных указания без названного приоритета — класс, уже стоивший 27 обходов `--no-verify`. Исполнитель выбрал доложить, а не подчиниться букве; подчинившись, он правил бы файл, открытый владельцем. → урок 12.
3. **Расхождение «7 уроков» против живых 9** — число решения 9 снято в ночь на 06.08, а арка с тех пор росла, в том числе от приёмок, которые в неё же и пишут. Исполнитель проверил стэшем, что это не его регрессия, и не стал чинить — верно. **Долг «перенести 7 уроков» переформулировать командой, а не числом:** на момент приёмки уроков 14, `check_uroki_doma.py` считает живое. → урок 13.

Все три разнесены в `UROKI-FABRIKE.md` секциями 11–13, метки `ДОСТАВЛЕНО` проставлены с обеих сторон. Гейты арки: `grep -c '^### '` = `grep -c '^ЦЕНА: '` = `grep -c '^ВЕРДИКТ: '`.

### Что стало правдой после этого захода

- **Плагин подключён.** `claude plugin marketplace add` + `install` → оба `rc=0`; `plugin details` показал **7 из 7** ожидаемых скиллов. Из песочницы приёмка это проверить не может (у неё свой `HOME`) — принято по отчёту, проверяется первым же вызовом скилла по имени.
- **Верификатор нашёл настоящую потерю** при слиянии `POKRYTIE.md`: §3 обеих исходных версий нёс перечисление пяти скиллов на 06.08 и факт «скилла про ресёрч ещё нет», и в переписанную версию это не попало. Исправлено в том же ходе, с датой данных. Это ровно то, ради чего верификатор требовался.
- **Дек теперь собираем в принципе:** фаза 4.5 приписана, G16 есть кому закрыть, иллюстрации вызываются по имени.

### Чего этот заход НЕ закрыл — вход следующих

Внешние скиллы **все живы**, включая четыре, чьё содержание уже втянуто (`html-slides-studio`, `math-russian-terminology`, `content-studio`, и три из решения 5). Отключение — после гейта вхождения, отдельным заходом. До тех пор в системе два экземпляра одного содержания, и это осознанное состояние, а не недосмотр.
