# Канал исполнителя — git-kontur (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — правка git-механики в общем репозитории, где рядом идут чужие заходы; цена ошибки — чужая работа.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — правка git-механики в общем репозитории, где рядом идут чужие заходы.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/git-kontur.
Работа затрагивает ОБА репозитория: materials и /Users/ivanyakovlev/Documents/GitHub/disciplina —
в обоих надо влить накопленное и запустить push.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_git-kontur.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

⚠ Этот заход трогает ОБЩИЙ репозиторий, где рядом работают чужие заходы.
Ни одной чужой работы не потерять важнее, чем влить всё до единой ветки.

Первым ходом: python3 _generator/tools/git_zona.py worktree add git-kontur --branch zahod/git-kontur,
затем чтение захода, затем секция ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-14_rychagi-zahodov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Работа идёт так: аналитик собирает ЗАХОД (задание исполнителю с пустым контекстом), исполнитель работает в своей ветке `zahod/<тема>` и рабочей папке-worktree, потом ПРИЁМКА судит отчёт и вливает ветку в основную (`arka/mat-kostyak`). У каждого захода в §0.1 есть ГИТ-КОНТУР: слот «ВЛИТЬ ПЕРЕД РАБОТОЙ» — забрать ветки, без которых на диске не будет нужного инструмента.

🔴 **Прошлый этап — найденный дефект устройства, из-за которого система буксует.** Слот «ВЛИТЬ» вливает названную ветку **в свою рабочую ветку**, а не в основную. Влитие в основную — ход приёмки, но приёмка живёт в песочнице Cowork и в `.git` писать НЕ МОЖЕТ. Она оставляет заявку — заявку исполняет следующий заход — и он снова вливает в себя. **Получается лестница: работа копится в последней ветке цепочки, основная ветка не получает ничего.**

Замер 14.08: в `materials` **13 невлитых веток**, вся наша цепочка (`nositel → nositeli-gen → rychagi → rychagi-2 → rychagi-3`) сидит внутри последней — 11 коммитов сверх основной; **84 коммита не вывезены на GitHub**. В `disciplina` — 2 невлитых, 8 не вывезено. Ничего не потеряно, но и не действует: генератор в основной папке не знает о собственных улучшениях, и каждый следующий заход собирается СТАРЫМ инструментом.

ЦЕЛЬ: починить устройство (вариант А владельца) и тем же механизмом разгрести накопленное.

Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 **СТОП ДО ЦЕЛИ: чужие ветки ты НЕ судишь.** Ты вливаешь то, что аналитик уже просудил и назвал списком; решать, годна ли чужая работа, — не твоя роль и не твоя информация.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. вариант А, решение владельца 14.08: субагент с ОТДЕЛЬНЫМИ правами делает git-работу и вливает в основную ветку — и только потом заход заводит свою ветку от свежей основной и работает как раньше
2. тем же заходом — разовая уборка: влить ВЕСЬ список невлитого (аналитик его уже просудил), закоммитить, ЗАПУШИТЬ, погасить старые ветки
3. заход строит механизм ПЕРВЫМ, а уборку делает ИМ ЖЕ — уборка есть живой прогон механизма, а не отдельная ручная работа
4. модель Opus: правка git-механики затрагивает общий репозиторий, где рядом идут чужие заходы

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/git-kontur`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/git-kontur`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add git-kontur --branch zahod/git-kontur
  cd ../materials-wt/git-kontur
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/` `_studio/zhurnal/_TEMPLATE-zahod.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

```
python3 _generator/tools/git_zona.py merge zahod/rychagi-3 --zone _generator/tools/ --zone _studio/zhurnal/_TEMPLATE-zahod.md
```

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/rychagi-3`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

Забирать чужого нечего — но именно этот заход ЗАБИРАЕТ В ОСНОВНУЮ ВЕТКУ всё накопленное (шаг 3). Это его задача, а не побочный эффект.

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

Гашение старых веток — тоже часть задачи (шаг 3, пункт 4). Порядок строгий: сначала влито и проверено, потом `poteri`, и только при `rc=0` — `zakryt-vetku`. **`rc=1` — НЕ гасить никогда**, такая ветка уезжает строкой в отчёт.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/git-kontur`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/` `_studio/zhurnal/_TEMPLATE-zahod.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО это:
  · `_generator/tools/git_zona.py` — целиком; особенно `cmd_merge` (сейчас докстрока прямо говорит «влить ветку в ТЕКУЩУЮ — единственная законная дверь»), `vyvezti`, `zakryt-vetku`, `poteri`;
  · `_generator/tools/bootstrap_zahod.py` — блок §0.1 (`git_kontur_blok`) и промпт субагента (`--zadanie-subagentu`): механизм выноса контура в субагента уже построен 14.08, ты расширяешь его мандат, а не создаёшь заново;
  · `~/Documents/GitHub/disciplina/skills/disciplina-git/SKILL.md` — единственный дом правил про коммиты; твоя правка меняет его содержание, значит его надо и прочитать, и поправить;
  · `_generator/tools/fixtures/git_zona/PROGNAT.sh` — куда добавляются ловушки.
  Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Три шага строго по порядку: сначала механизм, потом его проверка на живом, потом уборка ИМ ЖЕ. Уборка руками, в обход построенного механизма, — брак: она и есть его живой прогон.

### Шаг 1 — СУБАГЕНТ ГИТ-КОНТУРА ПОЛУЧАЕТ ОТДЕЛЬНЫЙ МАНДАТ

Механизм уже есть: 14.08 гит-контур вынесен из окна исполнителя в отдельного субагента (`bootstrap_zahod.py --zadanie-subagentu` печатает ему задание). Ты **расширяешь его права**, а не строишь заново.

🔴 **Устройство, которое надо получить** (формулировка владельца дословно): *«мы запускаем субагента с отдельной задачей и отдельными правами, а после этого работаем ровно так же, как делали раньше — заводит ветку, туда забираем то, что нужно, там с этим работаем»*.

То есть:

1. **Субагент гит-контура вливает названные ветки в ОСНОВНУЮ ветку** (`arka/mat-kostyak` в `materials`, `main` в `disciplina` — имя брать командой, не вписывать). Не в ветку захода.
2. **Потом заход заводит свою ветку ОТ СВЕЖЕЙ основной** и работает как раньше.
3. Отсюда лестница перестаёт расти: каждая следующая ветка отпочковывается от основной, уже содержащей всё принятое.

**Что для этого нужно в коде — реши сам, но вот известные препятствия, проверенные аналитиком:**

- функция слияния в `git_zona.py` (ищи `def cmd_merge`) сегодня умеет только «влить в ТЕКУЩУЮ», и это записано в его докстроке как «единственная законная дверь». Значит либо новый флаг (`--v-osnovnuyu`), либо отдельная подкоманда. **Выбери одно и скажи в `## ПЛАН`, почему.**
- Основная папка репозитория — общая. Рядом идут чужие заходы в своих worktree; их рабочие деревья влитие в основную ветку не трогает, но **лок `.git/index.lock` общий**. Обязателен `--no-optional-locks` и честный отказ при занятом локе вместо ожидания.
- 🔴 **Права субагента ОБЪЯВЛЯЮТСЯ в его промпте явно:** он работает в основной папке, вливает в основную ветку, пушит — и **ничего сверх этого не делает**. Содержательную работу захода он не трогает. Без явной границы получится второй исполнитель без зоны.

### Шаг 2 — правило в дом и в шаблон

- `disciplina-git/SKILL.md` — единственный дом правил про коммиты: впиши новое устройство контура туда, с ценой (замер 14.08: 13 невлитых веток, лестница из пяти, 84 невывезенных коммита).
- `_TEMPLATE-zahod.md` и `bootstrap_zahod.py` — блок §0.1 теперь говорит про влитие в основную, а не в свою.
- Ловушка в `fixtures/git_zona/PROGNAT.sh`: синтетический репозиторий, две ветки, влитие через новый механизм → **основная ветка содержит обе**, а ветка захода отпочкована от основной ПОСЛЕ влития.

### Шаг 3 — РАЗОВАЯ УБОРКА накопленного, построенным механизмом

🔴 **Ты вливаешь ВЕСЬ список невлитого и НЕ судишь чужую работу.** Решение владельца дословно: *«если эта ветка была в списке на влитие, но не была влита, то её смело можно вливать в основную ветку, аналитик уже её посмотрел и принял такое решение»*.

Четыре части, по порядку:

0. 🔴 **ЗАКОММИТИТЬ ТО, ЧЕГО В GIT НЕТ ВООБЩЕ — ПЕРВЫМ ДЕЛОМ, ДО ВСЯКОГО ВЛИТИЯ.** Замер 14.08 (сверь сам, `git status --porcelain`): в `materials` **23 грязных пути**, из них 17 неотслеживаемых, и среди них **три папки арок целиком вне git ни в одной ветке**:
   · `_studio/zhurnal/2026-08-13_nositeli-zahoda/` — 10 файлов на диске, **0 в git**;
   · `_studio/zhurnal/2026-08-14_rychagi-zahodov/` — 10 файлов, **0 в git**;
   · `_studio/zhurnal/2026-08-13_vynos-korpusa/` — 6 файлов, **0 в git**.
   Это файлы-заходы, отчёты исполнителей, `UROKI-FABRIKE.md` и дневники приёмок за двое суток. **Код доехал (он в ветках), а история работы — нет.** Причина известна: файл-заход коммитит приёмка, а приёмка живёт в песочнице и в `.git` писать не может; за двое суток это никто не забрал.
   В `disciplina` — 4 изменённых пути (`PLAN.md`, `SESSIYA.md`, `doma/zahody/RAZVITIE.md`, `skills/disciplina-zahod/SKILL.md`), это правки аналитика.
   ⚠ **Разбирайся, что коммитить, по содержимому, а не по маске.** В `materials` рядом лежат чужие незакоммиченные файлы (`kurs-puti-i-volny/*`, `_studio/zhurnal/2026-08-10_dizajn-i-metriki/*`) — их тоже надо забрать, но **отдельными коммитами по принадлежности**, а не одним общим: перемешанный коммит потом не разобрать.

1. **Влить в `materials`** — все ветки из `git_zona.py doctor` / `git branch --no-merged arka/mat-kostyak`. Замер 14.08 вечером (сверь сам): **14 веток** — прибавилась `zahod/polya-i-uzor` (4 коммита), из них `zahod/rychagi-3` несёт внутри себя всю цепочку `nositel → nositeli-gen → rychagi → rychagi-2`, то есть её достаточно влить один раз вместо пяти. Остальные — `dobivka-spornyh`, `polya-i-uzor`, `porozhdenie-kartochki`, `sceny-iz-blokov`, `tipologia-slajdov`, `vid-blokov-issledovanie`, `vid-blokov-vnedrenie`, `zakony-v-gejt`, `zamer-blokov-l2`.
2. **Влить в `disciplina`** — 2 ветки: `zahod/fazy-1-2-dovodka` (5 коммитов), `zahod/format-kartochki-faza-1` (17).
3. **ЗАПУШИТЬ оба репозитория.** Замер 14.08 вечером: `materials` — **84 коммита только на этой машине**, `disciplina` — **9**. Это дороже невлитого: невлитая ветка лежит на диске, невывезенный коммит умирает вместе с диском. `git_zona.py vyvezti --yes`.
4. **Погасить отработавшие ветки И МЁРТВЫЕ РАБОЧИЕ ПАПКИ.** Замер: `git worktree list` показывает **24 папки, из них 23 помечены `prunable`** — то есть каталогов уже нет, а записи о них живут. Это не косметика: сторож невлитого на такой куче становится шумом по построению, и именно из-за него `merge` дважды отказывал живым заходам. `git worktree prune` — после того, как убедишься, что ни одна папка не нужна. Строго: `poteri --branch <ветка>` → при `rc=0` `zakryt-vetku`, при **`rc=1` НЕ гасить** и назвать строкой в отчёте. Рабочие папки-worktree: гасить только те, чья ветка влита и погашена.

⚠ **Конфликт при влитии — законный исход, а не повод форсировать.** Разрешай по существу, если понимаешь обе стороны; не понимаешь — `merge --abort`, ветка остаётся невлитой, строка в отчёт. **Ни одной чужой работы не потерять важнее, чем влить всё до единой.**

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — семь пунктов, все машинные):**

1. **ЖИВОЙ ПРОГОН на реальном объекте репозитория, а не только на фикстуре.** Собери пробный заход новым генератором: его §0.1 предписывает влитие в ОСНОВНУЮ. Печатай кусок блока. Плюс фикстура `git_zona` зелёная, `sh … PROGNAT.sh` → rc=0.
2. **Ничего не осталось вне git.** `git status --porcelain | wc -l` в обоих репозиториях ДО и ПОСЛЕ. Было 14.08 вечером: `materials` **23**, `disciplina` **4**. После — печатай число и объясняй КАЖДЫЙ оставшийся путь (законно остаться может только то, что в `.gitignore` или сознательно не берётся). Отдельно: три папки арок (`nositeli-zahoda`, `rychagi-zahodov`, `vynos-korpusa`) обязаны появиться в `git ls-files` — печатай счёт файлов по каждой.
3. **Невлитых веток стало меньше, и это посчитано командой.** `git branch --no-merged <основная> | wc -l` в обоих репозиториях ДО и ПОСЛЕ. Было 14.08: `materials` 13, `disciplina` 2. Печатай четыре числа.
4. **Всё вывезено.** `git log --oneline @{u}..HEAD | wc -l` в обоих репозиториях → **0**, и печатай, сколько было (84 и 8).
5. **Генератор в основной папке знает о своих улучшениях.** После влития: `grep -c 'zadanie-subagentu' _generator/tools/bootstrap_zahod.py` → не ноль (сейчас 0 — в этом и была петля). Плюс `grep -c '^# ── З' _generator/tools/check_zahod.py` → печатай число, оно выросло с 12.
6. **Ни одна ветка не потеряна молча.** Для каждой невлитой на входе — строка исхода: влита · не влита с названной причиной · погашена. Сумма строк = число веток на входе. Печатай список.
7. **Мёртвые рабочие папки убраны, живые целы.** Было 24 записи, из них 23 `prunable`. После: `git worktree list` печатай до и после; `git worktree list` до и после — ни одна чужая папка не исчезла; `git status --porcelain` в основной папке не показывает потерянных чужих правок.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** «забрано в git N путей из 27, влито M веток из 14, не влито K с причинами, вывезено 2 репозитория из 2, погашено P веток, убрано Q мёртвых рабочих папок».

🔴 **Считаешь новое устройство опасным — скажи в `## ПЛАН` ДО работы.** В частности: если влитие в основную из worktree окажется небезопасным при живых соседях, **предложи другой способ и не делай молча по-своему**. Опровержение — полноценный успех.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). 🔴 **НУЖЕН — операция лоссовая: слияние и гашение веток необратимы по содержанию.** Свежий субагент, ДРУГОЙ метод: до уборки снимает список всех веток с их последними хэшами (`git for-each-ref`), после уборки проверяет, что каждый хэш достижим из основной ветки ЛИБО ветка жива и названа в отчёте. Мандат: потерянных коммитов 0, программно. Работает ПОСЛЕ влития и ДО гашения — после гашения он неисполним.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/ _studio/zhurnal/_TEMPLATE-zahod.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/ _studio/zhurnal/_TEMPLATE-zahod.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/` `_studio/zhurnal/_TEMPLATE-zahod.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/_TEMPLATE-zahod.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### А. ЗАМЕР ВХОДА — снят командой, ДО работы (дата данных 2026-08-14, ~16:10)

```
materials:   status --porcelain = 23 · --untracked-files=all = 79 · branch --no-merged arka/mat-kostyak = 14 · @{u}..HEAD = 84
disciplina:  status --porcelain =  4 · branch --no-merged main = 2 · @{u}..HEAD = 9
worktree list: materials 25 строк (главная + 24 папки) · disciplina 4
```
Основная ветка снята командой, не вписана: это ветка ГЛАВНОЙ рабочей копии —
`arka/mat-kostyak` в `materials`, `main` в `disciplina`.

### Б. 🔴 ДВЕ ЛОЖНЫЕ ПРЕДПОСЫЛКИ ЗАХОДА — говорю ДО работы, как велит §1

**Б1. «24 папки, из них 23 `prunable`» — НЕВЕРНО, и это меняет пункт 4 шага 3 и критерий 7.**
Замер: `git worktree list --porcelain | grep -c '^prunable'` → **0**; `ls ../materials-wt/`
показывает **все 24 каталога живыми на диске**. Мёртвых записей нет вовсе,
`git worktree prune` здесь — пустая операция.

Отсюда следствие, которое важнее самой цифры: **отказ `merge zahod/rychagi-3`
(«вычекаучена в рабочей папке») — ИСТИННОЕ срабатывание сторожа, а не шум от
мусорных записей.** Диагноз захода «сторож стал шумом по построению, и именно
из-за него merge дважды отказывал живым заходам» опровергнут: сторож отказал
потому, что папка действительно есть. Значит гасить рабочие папки можно ТОЛЬКО
по правилу «ветка влита и погашена», а не подметанием `prune`.

Критерий 7 в части «было 23 `prunable`» провалиться не может — он неверен на
входе. Исполняю его исполнимую половину: печатаю `worktree list` до и после,
доказываю, что ни одна ЧУЖАЯ папка не исчезла.

**Б2. Папок арок вне git не три, а ЧЕТЫРЕ.** К названным (`nositeli-zahoda` 10 файлов,
`rychagi-zahodov` 10, `vynos-korpusa` 6 — у всех троих `в HEAD=0` и `log --all = 0`,
проверено) добавляется **`_studio/zhurnal/2026-08-13_arhitektura-doma/` — 9 файлов,
0 в git ни в одной ветке**. Забираю и её: критерий 2 требует «объяснить КАЖДЫЙ
оставшийся путь», и молча оставленная четвёртая папка провалила бы его.

### В. РАЗВИЛКА ШАГА 1 — отдельная подкоманда, не флаг к `merge`. Почему

Беру **новую подкоманду `vlit-v-osnovnuyu`**, а не `merge --v-osnovnuyu`.

`cmd_merge` целиком построен вокруг «текущая ветка в ЭТОЙ рабочей копии»: `head` из
`rev-parse HEAD`, грязь из `dirty()` по `REPO`, лок из `git_dir()` этой папки,
`--abort`/`--continue` — тоже здесь. Влитие в основную меняет ВСЕ четыре: другое
рабочее дерево, другой индекс, другой лок, другой HEAD. Флаг превратил бы каждую из
этих строк в развилку внутри функции, которую канон объявил «единственной законной
дверью» и которой пользуются все заходы, — то есть цена ошибки легла бы на общий путь.
Отдельная подкоманда: (1) не трогает работающий путь ни строкой; (2) выдаётся
субагенту ИМЕНЕМ — «тебе можно `vlit-v-osnovnuyu` и `vyvezti`, и ничего сверх»,
что и есть «отдельные права» из формулировки владельца; (3) её отказы (чужая ветка в
главной папке, detached HEAD там, занятый лок главной папки) — свои, а не
перегруженные чужими смыслами.

### Г. БЕЗОПАСНО ЛИ ВЛИВАТЬ В ОСНОВНУЮ ПРИ ЖИВЫХ СОСЕДЯХ — да, с тремя условиями

Влитие в `arka/mat-kostyak` меняет файлы ГЛАВНОЙ папки (там эта ветка и стоит).
Соседние worktree стоят на СВОИХ ветках — их рабочие деревья операция не трогает
вовсе. Опасностей ровно три, и все три закрываются механизмом, а не осторожностью:

1. **Грязь главной папки** (23 пути). Блокер — только пересечение грязного с тем,
   что слияние перепишет. Считаю его В ГЛАВНОЙ ПАПКЕ (`git -C`), не в своей: своя
   грязь про чужое дерево не говорит ничего.
2. **`index.lock`.** У главной папки он свой (`--git-dir` там ≠ здесь). Жду её лок,
   а не свой, и честно отказываю по таймауту вместо тихого ожидания.
3. **Чужая ветка в главной папке.** Если владелец увёл её на другую ветку или в
   detached HEAD — отказ, а не влитие «куда попало». Имя цели печатается до работы.

Опровержения устройства у меня нет: оно безопаснее нынешнего, потому что снимает
саму лестницу, а не добавляет риск.

### Д. ЧТО МЕНЯЮ В КОДЕ (зона: `_generator/tools/`)

1. **`git_zona.py`** — `osnovnaya_vetka()` (ветка главной рабочей копии, командой) +
   подкоманда `vlit-v-osnovnuyu <ветка> [--zone …] [--vsyo-ravno "причина"]`:
   предпросмотр → зона → грязь главной папки → лок главной папки → `git -C <главная> merge`.
   Конфликт НЕ разрешает (как и `merge`): печатает пути и останавливается.
2. **`bootstrap_zahod.py`** — §0.1 меняет ПОРЯДОК: субагент гит-контура работает
   ПЕРВЫМ и вливает в ОСНОВНУЮ, и только ПОСЛЕ этого заход заводит свою ветку от
   свежей основной. Слот «ВЛИТЬ» уезжает в промпт субагента: причина, по которой он
   оставался в окне («без влития инструмента на диске может не быть»), новым порядком
   снимается — ветка захода отпочковывается от основной, которая уже всё содержит.
   В промпте — явная граница прав: основная папка, основная ветка, push, и ничего сверх;
   зону захода субагент не коммитит.
3. **`fixtures/git_zona/PROGNAT.sh`** — ловушка 56: синтетический репо, две ветки,
   `vlit-v-osnovnuyu` из worktree → **основная содержит обе**, и ветка, заведённая
   ПОСЛЕ влития, отпочкована от основной (обе ветки достижимы из неё).
4. **`_TEMPLATE-zahod.md` — правки не требует**, и это проверено, а не предположено:
   весь текст §0.1 подставляется в анкор `{{ВЛИТИЕ}}` из `git_kontur_blok()`; в самом
   шаблоне слова про влитие нет ни одного (`grep` пуст). Правка шаблона была бы правкой
   в никуда.
5. **`disciplina-git/SKILL.md`** — новый §4б-бис: устройство контура и цена замера 14.08.

### Е. ПОРЯДОК ИСПОЛНЕНИЯ

0. Механизм (Д1–Д3), фикстура зелёная. Заявки очереди — по ходу тех шагов, где они исполняются.
1. **Забрать в git то, чего нет вовсе** — в главной папке, на основной ветке,
   ОТДЕЛЬНЫМИ коммитами по принадлежности (арки фабрики · `dizajn-i-metriki` ·
   `kurs-puti-i-volny`), не одним свальным.
2. **Верификатор ДО влития и ДО гашения** — свежий субагент снимает
   `for-each-ref` всех веток с хэшами; после уборки проверяет достижимость каждого.
3. **Влить построенным механизмом**: `materials` — 14 веток (из них `rychagi-3`
   несёт цепочку из пяти), `disciplina` — 2. Конфликт → `--abort`, строка в отчёт.
4. **Вывезти оба репозитория** (`vyvezti --yes`).
5. **Погасить**: `poteri --branch` → при `rc=0` `zakryt-vetku`; `rc=1` — НЕ гасить, строкой в отчёт.
6. Живой прогон: пробный заход новым генератором, печать §0.1.
7. Коммит своей зоны (§4), отчёт.

**Чего НЕ делаю:** не сужу чужую работу (весь список — на влитие, решение владельца);
не гашу ветку с `rc=1`; не трогаю рабочие папки, чьи ветки не влиты и не погашены;
не зову `git worktree prune` — гасить нечего (Б1).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**ОХВАТ ОДНОЙ СТРОКОЙ:** забрано в git 79 путей из 79 (23 по счёту `--porcelain`), влито 11 веток
из 14 в `materials` и 0 из 2 в `disciplina`, не влито 6 с названными причинами, вывезено 2
репозитория из 2 (35 веток), погашено 4 ветки, убрано 4 рабочие папки, мёртвых записей
`prunable` не было ни одной.

### ЧТО СДЕЛАНО И ЗАЧЕМ

**Шаг 1 — механизм.** Новая подкоманда `git_zona.py vlit-v-osnovnuyu <ветка>`: вливает в ОСНОВНУЮ
ветку, работая `git -C` в главной рабочей папке. Развилку решил в пользу отдельной подкоманды, не
флага к `merge` (обоснование — `## ПЛАН` §В; коротко: `cmd_merge` весь построен вокруг «текущая
ветка в ЭТОЙ рабочей копии», и флаг превратил бы каждую её строку в развилку внутри пути, которым
пользуются все заходы). Имя основной ветки снимается командой (`osnovnaya_vetka()` — ветка главной
рабочей копии), поэтому работает и в `materials`, и в `disciplina`.

**Шаг 1б — порядок, а не только команда.** §0.1 теперь: субагент гит-контура ПЕРВЫМ ходом вливает в
основную, и только ПОТОМ заход заводит свою папку. Слот «ВЛИТЬ ПЕРЕД РАБОТОЙ» из окна исполнителя
убран: причина, по которой он там стоял («без влития инструмента на диске может не быть»), снята
устройством — ветка отпочковывается от основной, которая уже всё содержит. Права субагента
объявлены в его промпте явно и узко: главная папка, основная ветка, вывоз — и ничего сверх;
зону захода он не коммитит.

**Шаг 2 — правило в дом и гейты за устройством.** `_meta/disciplina/GIT-disciplina.md` и
`disciplina/skills/disciplina-git/SKILL.md` — новый §4б-бис с ценой. `check_zahod.py`: З10 сторожит
новые слоты, З12 принимает дверь субагента как дверь к очереди. Ловушка 56 в `fixtures/git_zona`
(мутационно проверена обеими половинами), ловушки 17/34/35/42 в `fixtures/zahod` переписаны под
новое устройство — 17 и 42 теперь ловят ВОЗВРАТ старой формы, иначе `merge <ветка>` можно вернуть в
§0.1 одной строкой и лестница начнёт расти заново незамеченной.

🔴 **`_TEMPLATE-zahod.md` не тронут, и это проверено, а не решено:** весь текст §0.1 подставляется в
анкор `{{ВЛИТИЕ}}` из `git_kontur_blok()`, в самом шаблоне слова про влитие нет ни одного.

**Шаг 3 — уборка тем же механизмом.** Порядок: коммиты → снимок для верификатора → влитие → вывоз →
гашение.

### ЧИСЛА ДО И ПОСЛЕ (все сняты командой)

| критерий | было 14.08 | стало | чем снято |
|---|---|---|---|
| 2. вне git, `materials` | 23 (79 при `-uall`) | **0** | `git status --porcelain \| wc -l` |
| 2. вне git, `disciplina` | 4 | **0** | то же |
| 3. невлитых, `materials` | 14 | **5** | `git branch --no-merged arka/mat-kostyak` |
| 3. невлитых, `disciplina` | 2 | **2** | `git branch --no-merged main` |
| 4. невывезено, `materials` | 84 (к концу 120) | **0** | `git log --oneline @{u}..HEAD \| wc -l` |
| 4. невывезено, `disciplina` | 9 (к концу 13) | **0** | то же |
| 5. `zadanie-subagentu` в ОСНОВНОЙ папке | 0 | **3** | `grep -c` |
| 5. клауз в `check_zahod.py` | 12 | **17** | `grep -c '^# ── З'` |
| 7. рабочих папок | 25 строк | **21** | `git worktree list \| wc -l` |
| 7. из них `prunable` | заход: 23 | **0, и было 0** | `worktree list --porcelain \| grep -c '^prunable'` |

**Про «невлитых 5, а не 4»:** пятая — моя собственная `zahod/git-kontur`, она обязана остаться
невлитой (влитие делает приёмка). Из ВХОДНЫХ 14 не влито 4.

**Папки арок в `git ls-files`** (у всех было 0 ни в одной ветке): `nositeli-zahoda` 10,
`rychagi-zahodov` 10, `vynos-korpusa` 6, `arhitektura-doma` 21 (9 своих + приехавшее слияниями).

### КРИТЕРИЙ 6 — ИСХОД КАЖДОЙ ВЕТКИ (сумма = 14 + 2 = 16)

**`materials`, влито 11:** `nositel`, `nositeli-gen`, `rychagi`, `rychagi-2`, `rychagi-3` — влиты
ОДНИМ ходом (`0f39252`), цепочка сидела внутри последней · `dobivka-spornyh` (`204b903`) ·
`vid-blokov-issledovanie` (`af1b4e1`) · `sceny-iz-blokov` (`c0d0a3e`) · `tipologia-slajdov`
(`2e2d17b`) · `zamer-blokov-l2` (`77367ae`).

**`materials`, погашено 4** (`poteri` rc=0 → `zakryt-vetku` rc=0, надгробия `mogila/*` на месте):
`dobivka-spornyh`, `vid-blokov-issledovanie`, `tipologia-slajdov`, `zamer-blokov-l2`.

**Влито, но НЕ погашено — 7, и отказ верный:** `nositel`, `nositeli-gen`, `rychagi`, `rychagi-2`,
`rychagi-3`, `sceny-iz-blokov` — в их рабочих папках 1–5 путей вне git (чужие незакоммиченные
правки и пробные заходы приёмки). Отказ `zakryt-vetku` на незакоммиченном не перебивается ничем, и
это правильно: надгробие такое не держит. **Чужую работу я не коммичу.**

**НЕ влито — 4 в `materials`, у каждой причина по существу:**

1. **`zahod/vid-blokov-vnedrenie`** — конфликт в `_generator/sborka/bloki.py`. Уже влитый
   `sceny-iz-blokov` переписал `render_section_markdown` (автотег `{@K}` на каждом абзаце начиная
   со второго блока); `vid-blokov-vnedrenie` в ТОЙ ЖЕ функции ставит докстроку «🔴 НЕ ТРОГАТЬ — её
   зовёт `gejt_kartochki.py` для счёта слов». Предупреждение фактически верно, проверил:
   `gejt_kartochki.py:731` делает `flat = bloki.render_section_markdown(...)`, затем
   `n_words = len(flat.split())`. **⇒ автотеги считаются словами и надувают проверку
   `byudzhet_slov`.** Развилка (чинить счёт гейта под автотеги ИЛИ отказаться от автоматики) —
   решение о чужой подсистеме. `--abort`.
2. **`zahod/polya-i-uzor`** — тот же конфликт в `bloki.py` (+ `KARTA.md`). `--abort`.
3. **`zahod/porozhdenie-kartochki`** — конфликт в `porodit_slaid.py`, `tipy_slajdov.py`,
   `fixtures/porozhdenie/PROGNAT.sh`: три кодовых пути чужой подсистемы. `--abort`.
4. **`zahod/zakony-v-gejt`** — конфликт в 16 путях (`gejt_kartochki.py` + 15 файлов фикстуры
   `zakony-l2`). `--abort`.

**`disciplina`, НЕ влито 2 — обе по одной причине.** `zahod/fazy-1-2-dovodka` и
`zahod/format-kartochki-faza-1` конфликтуют в `skills/slajdy/DOLG.md`. Проверил машинно, сводимо ли
слияние механически: **нет** — в двух хунках 9 строк требуют посточного суждения. Ветка датирована
08.08 и держит Д38 как 🔴 живой; основная — 12.08 и помечает тот же долг ✅ решённым, с
живым прогоном в тексте.
Проверил фактом: `gejt_kartochki.py --help | grep -c faza` → **3**, то есть права ОСНОВНАЯ, а
сторона ветки устарела. Взять сторону ветки значило бы вернуть в канон ложное утверждение; взять
основную — потерять 6 датированных меток `СТАТУС: ЖИВ` (Р34). Правка реестра долгов чужого
репозитория построчно — не моя роль. `--abort`, обе ветки целы.

🔴 **Все 6 невлитых ВЫВЕЗЕНЫ на GitHub** — их работа больше не существует в одной копии на диске.

### ЗАЯВКИ ОЧЕРЕДИ (было открыто 11, закрыто 5, застряло 4, не трогал 2)

- ✅ `2026-08-14T1403` — цепочка генератора в основную. Петля разорвана.
- ✅ `2026-08-14T0315` — `dobivka-spornyh` влита и погашена.
- ✅ `2026-08-14T0404` — `vid-blokov-issledovanie` влита и погашена.
- ✅ `2026-08-14T0412` — `sceny-iz-blokov` влита; гашение отказано (грязная чужая папка).
- ✅ `2026-08-13T2246` — 4 пункта из 4 по существу (влито · заплатка снята · `.probe.md` удалён ·
  закоммичено), но пунктом (4) просилось ОДНИМ коммитом, а вышло двумя: папка арки лежала вне git
  целиком и ушла первой.
- ⚠ ЗАСТРЯЛА `2026-08-14T1318` — конфликт `bloki.py` по существу (см. выше).
- ⚠ ЗАСТРЯЛИ `2026-08-13T2138`, `2026-08-14T0200`, `2026-08-14T0442` — **все три по одной причине:
  двери снятия строки из `KARTA.md §6` НЕ СУЩЕСТВУЕТ.** Заявки велят «снять через
  `register_doc/purge`», но `register_doc.py` умеет только вписывать (`usage: ПУТЬ ОПИСАНИЕ`), а
  `purge` в `KARTA` не пишет вовсе; править файл руками запрещает контракт зоны. Удалить файлы,
  оставив строки, — завести битые ссылки в реестре.
- не трогал `2026-08-12T0311`, `2026-08-12T0359` — содержательные законы фаз, чужая зона.

### КАК ПРОВЕРЯЛ

- Фикстуры `git_zona` и `zahod` — **rc=0** обе. Ловушка 56 **мутационно проверена дважды**: вернул
  `worktree_branches()` вместо `_vse_rabochie_papki()` → красная; заменил `git_tam(glav, "merge"…)`
  на `git(…)` → красная.
- **Живой прогон на реальном объекте:** собрал пробный заход РЕАЛЬНЫМ генератором на РЕАЛЬНОМ
  шаблоне и РЕАЛЬНОЙ `KARTA`, распечатал §0.1, исполнил напечатанную им команду субагента и убедился,
  что она даёт `vlit-v-osnovnuyu <ветка>` с обеими зонами. Прогон поймал две мои ошибки (ниже).
  После — снёс дверью `purge` и вернул `KARTA` из git; следов не осталось.
- **Верификатор — свежий субагент, ДРУГОЙ метод** (достижимость `merge-base --is-ancestor` по
  снимку `for-each-ref`, снятому ДО первого слияния), работал ПОСЛЕ влития и ДО гашения:
  `materials` 140 ссылок → **потеряно 0**; `disciplina` 20 ссылок → **потеряно 0**. Его список
  невлитых веток совпал с моим строка в строку.
- Каждое красное на моих путях чинил, а не обходил. Каждое красное на чужом — воспроизводил на
  baseline через `git stash` и сравнивал построчно.

### ЧТО НАШЁЛ ПО ХОДУ (не чинил там, где не моя зона)

1. 🔴 **`vyvezti --zahody` не вывозил 24 ветки из 26 — молча.** `git branch` метит ветку,
   вычекаученную в другой рабочей папке, префиксом `+`, а код снимал только `lstrip("* ")` — такая
   строка не начиналась с `zahod/` и выпадала из вывоза. Заход работает в worktree ПО ПОСТРОЕНИЮ,
   то есть флаг не вывозил ровно то, ради чего его звали, и печатал при этом «Вывезено — локально-
   только коммитов не осталось». **Починил** (`--format=%(refname:short)`); после починки вывоз
   вырос с 3 веток до 27.
2. 🔴 **Автотеги сцен надувают счёт слов гейта** — см. `vid-blokov-vnedrenie` выше. Дефект уже ЖИВОЙ
   в основной ветке (`sceny-iz-blokov` влит по заявке), не мной внесён, но назвать обязан.
3. **Двери снятия регистрации из `KARTA §6` нет** — три заявки застряли на ней.
4. **Четыре фикстуры красные на baseline, все одного класса:** `korni`, `register_doc`, `sborka`,
   `porozhdenie`. Ветка `nositeli-gen` сделала `--intervyu` обязательным и дописала его в четыре
   фикстуры, а эти пропустила; красное выглядит как «bootstrap не создал заход», то есть обвиняет
   здоровый код. **Тот же класс я починил в `git_zona`** (там же не хватало `modeli.py` в списке
   копируемых файлов). Остальные четыре не чинил — вне задачи; починка однотипная и дешёвая.
5. **Папок арок вне git было ЧЕТЫРЕ, а не три** — заход не назвал `2026-08-13_arhitektura-doma`.

### ЧТО НЕ ТРОГАЛ

Чужие рабочие папки и их незакоммиченное · содержание чужих веток (не сужу, что годно) ·
`git worktree prune` (гасить было нечего) · четыре красные фикстуры вне задачи · содержательные
заявки по законам фаз.

### НЕОБРАТИМОЕ — списком

1. **Удалён `_studio/zhurnal/2026-08-13_nositeli-zahoda/.probe.md`** (дверью `purge`) — служебный
   файл приёмки, удаление прямо заказано заявкой `2026-08-13T2246` п.3. Восстанавливается: файла в
   git не было ни в одной ветке, восстановлению не подлежит и не должен.
2. **Погашены 4 ветки** — `dobivka-spornyh`, `vid-blokov-issledovanie`, `tipologia-slajdov`,
   `zamer-blokov-l2`. Восстанавливается: надгробия `mogila/zahod/<имя>` на месте,
   `git_zona.py voskresit --branch zahod/<имя>`. Их содержимое дополнительно влито в основную.
3. **Снесены 4 рабочие папки** (вместе с ветками выше). Восстанавливается: `voskresit` + `worktree add`.
4. **Снята заплатка в `kod_nositel.md`** — одна строка, по заявке `2026-08-13T2246` п.2. Откат:
   `git show 15760dc^:…/kod_nositel.md`.
5. **Разрешены конфликты слияния** в `KARTA.md` (объединением, 4 раза) и `ITOG-shema.md` (взята
   сторона ветки). Откат: `git show af1b4e1^`, `204b903^`, `2e2d17b^`, `77367ae^`; копии
   конфликтных версий — в `scratchpad/KARTA.conflict.bak` и `ITOG.conflict.bak`.

Ничего не удалено безвозвратно: все погашенные ветки держатся надгробиями, все 6 невлитых
вывезены на GitHub.

### ПОВТОРЯЕМОСТЬ НАХОДОК — что повторится на СЛЕДУЮЩЕЙ единице работы

- 🔴 **Повторится на КАЖДОМ следующем заходе** (класс НЕМЕДЛЕННОЕ, чинится ДО следующего прогона):
  дефект `vyvezti --zahody` — **уже починен этим заходом**; отсутствие двери снятия регистрации из
  `KARTA §6` — **не починено, повторится у любого, кто гасит файл-заход** (три заявки уже стоят);
  четыре красные фикстуры — **повторятся у любого, кто тронет их файлы**, и каждый раз потратят круг
  на диагностику здорового кода (я потратил).
- Не повторится, законно уходит в очередь: конфликт `bloki.py` (разовая развилка владельца),
  конфликт `DOLG.md` (разовое расхождение двух дат).

**АРТЕФАКТ:** артефакта-файла нет, и это законный исход: заход правит git-механику и состояние двух
репозиториев, а не собирает документ. Смотреть результат — командами:
`cd /Users/ivanyakovlev/Documents/GitHub/materials && python3 _generator/tools/git_zona.py doctor`
Текст нового устройства — `/Users/ivanyakovlev/Documents/GitHub/materials/_meta/disciplina/GIT-disciplina.md` §4б-бис.

**КОММИТ:** `476c275` — `git-kontur: влитие идёт в ОСНОВНУЮ ветку — подкоманда vlit-v-osnovnuyu, §0.1
меняет порядок…` · плюс `693fd63` (канон §4б-бис) и `f8f1981` (автолог) ·
`git_zona.py check --zone _generator/tools/` → ✅ · `check --zone _studio/zhurnal/_TEMPLATE-zahod.md` → ✅

**Г1** ✅ обе зоны · **Г2** применим и исполнен: зона вышла во второй репозиторий (`disciplina`,
правка канона) — `cd disciplina && git status --porcelain` → **0** · **Г3** невлитых 14 → 5, из них
моя одна, не выросло · **Г4** новых `.py` нет; `check_tool_contract.py` на двух правленых → **rc=0** ·
**Г5** новых `.md` не заводил, `register_doc.py` звал один раз на ЧУЖОЙ файл-сироту
(`ITOG-shema.md`), строка на диске: `grep -c ITOG-shema _studio/docs/KARTA.md` → 2 ·
**Г6** `show --stat 476c275` → только 5 моих путей в `_generator/tools/`.

**ОБХОДЫ `--no-verify` — 2, оба с причиной, обе причины уехали в `INCIDENTY.md` сами:**
(1) коммит своей зоны — фикстура `korni` красная на baseline (воспроизведено `git stash`,
вывод идентичен); (2) коммит арки `arhitektura-doma` — в ЧУЖОМ отчёте
`kod_arhitektura-doma.md` инцидент объявлен разобранным, а строки-метки в `VERDIKTY.md` нет.
Оба — чужой долг; своё красное чинил.

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** неприменимо, канал `app` — снимать неоткуда.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель)

### Гейт, который сторожил БОЛЕЗНЬ, а не здоровье, и был при этом зелёным
Ловушка 17 фикстуры `zahod` требовала в §0.1 слот «ВЛИТЬ ПЕРЕД РАБОТОЙ» и команду
`git_zona.py merge <ветка>` — то есть влитие в СВОЮ ветку. Это и есть дефект, породивший лестницу.
Гейт добросовестно держал его на месте и краснел бы на починке.
ЦЕНА: лестница копилась незамеченной до 14 невлитых веток, цепочки из пяти и 84 невывезенных
коммитов; генератор в основной папке не знал о собственных улучшениях (`grep -c 'zadanie-subagentu'`
→ 0), и каждый следующий заход собирался старым инструментом. Когда я менял устройство, четыре
ловушки (17, 34, 35, 42) и две клаузы линтера (З10, З12) покраснели на ВЕРНОЙ работе — это и есть
цена гейта, привязанного к форме, а не к свойству. Вывод для фабрики: ловушка обязана сторожить
СВОЙСТВО («влитое доехало до основной»), а не ТЕКСТ слота; и при смене устройства ловушка меняется
ТЕМ ЖЕ ходом, иначе она защищает прошлое.

### Флаг вывоза молча пропускал ровно те ветки, ради которых существует
`vyvezti --zahody` снимал имена веток обрезкой `lstrip("* ")`, а `git branch` метит ветку,
вычекаученную в другой рабочей папке, префиксом `+`. Заход работает в worktree ПО ПОСТРОЕНИЮ.
ЦЕНА: замер 14.08 — из 26 локальных `zahod/*` вывозились 2, пропускались 24, и команда печатала при
этом «✅ Вывезено — локально-только коммитов не осталось». Полтора месяца работы заходов лежали в
одной копии на диске при зелёном отчёте инструмента. Обнаружено только потому, что я прочёл число
веток в предпросмотре и оно не сошлось с `git branch | grep -c`. Вывод: где инструмент печатает
вердикт об охвате, охват обязан сниматься `--format`/`--porcelain`, а не разбором человекочитаемого
вывода git — украшения меняются молча, а вердикт остаётся зелёным.

### Правило требует дверь, которой нет: снятие строки из `KARTA §6`
Три заявки (`2026-08-13T2138`, `2026-08-14T0200`, `2026-08-14T0442`) велят «снять строки из
`_studio/docs/KARTA.md §6` через `register_doc/purge`». Такой двери не существует: `register_doc.py`
только вписывает, `purge` в `KARTA` не пишет; править руками запрещает контракт зоны.
ЦЕНА: три заявки застряли и остаются открытыми; при этом ситуация зеркальна оплаченной 30.07 (28
обходов `--no-verify`, 27 по одной причине — правило требовало регистрации, а двери не было).
Тогда цену заплатили за отсутствие двери ВПИСАТЬ, сейчас копится за отсутствие двери СНЯТЬ: файлы
удалить нельзя, не заведя битых ссылок в реестре, а не удалять — накапливать мусорные заходы,
которые уже дважды блокировали старт живых заходов.
