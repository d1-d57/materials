# Канал исполнителя — vyvoz-i-ochered (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — восемь влитий подряд — операция лоссовая и необратимая, а решение по маршрутизации очереди (какое поле завести, что считать признаком тупика) на бумаге не расписано и рождается по ходу.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — восемь влитий подряд лоссовы, а решение по маршрутизации очереди рождается по ходу.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/vyvoz.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_vyvoz-i-ochered.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add vyvoz --branch zahod/vyvoz-i-ochered, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-14_rychagi-zahodov`
Область: «_studio/zhurnal/2026-08-14_rychagi-zahodov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/magical-elegant-clarke/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. `materials/_generator/tools/` — общий движок на два репозитория (`materials` и соседний `disciplina`); работа в нём идёт заходами, каждый в своей ветке и рабочей папке, а влитие в основную делает гит-контур первым ходом. Прошлый этап: заход `shablony-odin-dom` принят приёмкой 15.08 — `korni.шаблон()` теперь собирается от репозитория ИНСТРУМЕНТОВ, а не от `GIT_ZONA_REPO`. Но его ветка не влита, и таких невлитых восемь; параллельно очередь заявок гит-контура встала намертво (4 заявки, закрыто 0, старейшая висит с 12.08 с восемнадцатью одинаковыми отказами), а хвост Cowork в обоих репозиториях не забран в git. ЦЕЛЬ: вывезти всё накопленное и починить очередь так, чтобы тупик краснел, а не копился.
Приёмка — по ОТЧЁТУ, без построчной сверки. <Если стоп до цели: получишь X, но НЕ Y.>

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-15) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Владелец 15.08 дословно: влитие и любые git-операции идут ПУНКТОМ ЗАХОДА, а не строкой владельцу в чат. Владелец git руками не трогает принципиально — «ты не должен мне говорить, что я должен что-то вливать, ты должен написать туда»
2. Заявка, висящая с 12.08 и получившая 18 одинаковых отказов, признана владельцем НЕНОРМАЛЬНОЙ: «то, что что-то висит с 12 августа, это ненормально». Чинится маршрутизация, а не терпение
3. Содержание самих заявок 0311 и 0359 (новый закон фазы 2 в gejt_kartochki.py про все-нарративные слайды) в этом заходе НЕ реализуется — заход их переадресует и заводит сторож. Реализация — отдельный заход-разработчик по фазе 2
4. Заход З-0 принят приёмкой 15.08 (гейтов 11 из 14, три красных — дефекты приёмки и долг репозитория). Ветка zahod/shablony-odin-dom вливается здесь первым ходом: без неё bootstrap_zahod.py не может собрать ни одного захода для репозитория disciplina

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/vyvoz`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/vyvoz-i-ochered`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add vyvoz --branch zahod/vyvoz-i-ochered
  cd ../materials-wt/vyvoz
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_studio/zhurnal/_INFRA-git/zayavki/` `_generator/tools/git_zona.py` `_generator/tools/fixtures/git_zona/PROGNAT.sh` `_generator/tools/fixtures/register_doc/PROGNAT.sh` `_generator/tools/fixtures/korni/PROGNAT.sh` `_generator/tools/fixtures/zahod/PROGNAT.sh` `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dom-shablonov.md` `_studio/docs/KARTA.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/tools/_pp_tmp.py` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/.probe` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/_TEMPLATE-arka/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/README.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/POKRYTIE.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --vlit zahod/shablony-odin-dom --vlit zahod/arhitektura-doma --vlit zahod/dovodka-solvera --vlit zahod/kadr-uzor-i-vmeshchenie --vlit zahod/rychagi --vlit zahod/rychagi-2 --vlit zahod/rychagi-3 --vlit zahod/vid-blokov-vnedrenie --zone _studio/zhurnal/_INFRA-git/zayavki/ --zone _generator/tools/git_zona.py --zone _generator/tools/fixtures/git_zona/PROGNAT.sh --zone _generator/tools/fixtures/register_doc/PROGNAT.sh --zone _generator/tools/fixtures/korni/PROGNAT.sh --zone _generator/tools/fixtures/zahod/PROGNAT.sh --zone _studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dom-shablonov.md --zone _studio/docs/KARTA.md --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/tools/_pp_tmp.py --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/.probe --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/_TEMPLATE-arka/ --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/ --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/README.md --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/POKRYTIE.md --kommitit 'materials: весь хвост Cowork вне git — снять командой, не списком по памяти; на 15.08 это 13 путей, в том числе AUDIT-BAZA-2026-08-15.md, HANDOFF-2026-08-16-kachestvo.md, TZ-desyatyj-skill.md, kod_shablony-odin-dom.md, kod_dom-shablonov.md и UROKI-FABRIKE.md арки 2026-08-14' --kommitit 'materials: собственные правки зоны — git_zona.py, четыре PROGNAT.sh, zayavki/' --kommitit 'materials: _studio/docs/KARTA.md в ТОТ ЖЕ коммит, если звал register_doc.py' --kommitit 'disciplina ОТДЕЛЬНЫМ коммитом: новая арка _studio/zhurnal/2026-08-15_skill-kachestva/ (пять файлов) + README.md с их регистрацией + doma/meta/POKRYTIE.md §11 + удаления мусора' --zakryt 'после влития — погасить рабочие папки и ветки влитых заходов: подкомандами poteri и zakryt-vetku --vse-zelenye того же git_zona.py. Ветка с rc=1 НЕ удаляется никогда — вынести строкой в отчёт' --zakryt 'НЕПРИКОСНОВЕННО содержание _generator/sborka/gejt_kartochki.py и PRAVILA-FAZY-2.md: заявки 0311 и 0359 в этом заходе НЕ исполняются, только переадресуются'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

Что влить, названо аналитиком и уехало в задание субагента (8): `zahod/shablony-odin-dom`, `zahod/arhitektura-doma`, `zahod/dovodka-solvera`, `zahod/kadr-uzor-i-vmeshchenie`, `zahod/rychagi`, `zahod/rychagi-2`, `zahod/rychagi-3`, `zahod/vid-blokov-vnedrenie`. Влитие идёт в ОСНОВНУЮ ветку, не в твою.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/vyvoz-i-ochered`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_studio/zhurnal/_INFRA-git/zayavki/` `_generator/tools/git_zona.py` `_generator/tools/fixtures/git_zona/PROGNAT.sh` `_generator/tools/fixtures/register_doc/PROGNAT.sh` `_generator/tools/fixtures/korni/PROGNAT.sh` `_generator/tools/fixtures/zahod/PROGNAT.sh` `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dom-shablonov.md` `_studio/docs/KARTA.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/tools/_pp_tmp.py` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/.probe` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/_TEMPLATE-arka/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/README.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/POKRYTIE.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: все четыре файла `_studio/zhurnal/_INFRA-git/zayavki/*.md` целиком (в них история отказов — она и есть предмет) · `_generator/tools/git_zona.py`, подкоманды `zayavki`, `vlit-v-osnovnuyu`, `poteri`, `zakryt-vetku` · `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/POKRYTIE.md` §11 (разбор ситуации с очередью, с числами и с тем, чего не хватает) · `_generator/tools/fixtures/korni/PROGNAT.sh` строки 20–30 (шапка с числом ловушек) · `_generator/tools/fixtures/zahod/PROGNAT.sh` строки 468–480 (мёртвая половина условия). Больше ничего; проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
Пять работ. Первая делается субагентом гит-контура по §0.1 и в задачу здесь не дублируется — ниже только то, что делаешь ты сам.

**1. Очередь заявок — починить МАРШРУТИЗАЦИЮ, а не разобрать завал руками.** Прочитай все четыре заявки и §11 дома. Факт (🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `for f in _studio/zhurnal/_INFRA-git/zayavki/*.md; do echo -n "$f "; grep -c '^ЗАСТРЯЛА:' $f; done`): заявка `…T0311` несла 18 строк `ЗАСТРЯЛА:`, `…T0359` — 16, всего заявок было 4 и закрыто 0. Числа к твоему прогону могли вырасти — это не меняет сути. Все отказы верны по существу и одинаковы: заявка просит содержательную правку кода, а гит-контур вправе только вливать, вывозить и гасить ветки. Заявка возвращается в ту же очередь и достаётся следующему контуру. Три вещи, которых не хватает, названы в §11 — сделай их:
   - **поле рода работы** у заявки (гит-операция / содержательная правка), чтобы сортировать было чем ещё на входе;
   - **сторож на повтор**: три одинаковых отказа подряд обязаны КРАСНЕТЬ. Это признак не занятости исполнителя, а неверного адреса;
   - **второй дом**: заявке, которой нужен заход-разработчик, должно быть куда лечь, кроме очереди гит-контура.
   Развилки (имя поля, где живёт второй дом, чем именно краснеет сторож) — твои; назови их в `## ПЛАН` до работы. 🔴 Содержание заявок 0311 и 0359 НЕ реализуй: `gejt_kartochki.py` и `PRAVILA-FAZY-2.md` неприкосновенны. Их надо переадресовать, а не исполнить.

**2. Хвост Cowork — в git, в обоих репозиториях.** В `materials` вне git 13 путей (снято 15.08 — пересчитай командой, не по этому списку). В `disciplina` не забраны: новая арка `_studio/zhurnal/2026-08-15_skill-kachestva/` (пять файлов), их регистрация в `README.md §6` и запись `doma/meta/POKRYTIE.md §11`. 🔴 `README.md` едет в ТОТ ЖЕ коммит, что файлы арки: ворота 5 читают §6 с диска, а не из индекса.

**3. Мусор песочницы — удалить.** Cowork в примонтированной папке удалять не может (`Operation not permitted`, FUSE), поэтому копится: `/Users/ivanyakovlev/Documents/GitHub/disciplina/tools/_pp_tmp.py` (с 15.08) · `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/.probe` · `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dom-shablonov.md` (заход-брак, замазан надгробием — прочитай его, там названо, почему он брак) · `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/_TEMPLATE-arka/` — протухшая вторая копия шаблона; после влития `shablony-odin-dom` она мертва по построению, но пока лежит, следующий исполнитель может взять её.

**4. Четыре долга фикстур** (пункты очереди принятого захода `shablony-odin-dom`, дома названы там же): `fixtures/git_zona/PROGNAT.sh` и `fixtures/register_doc/PROGNAT.sh` красны на чистом `HEAD` — в списке `cp` нет `check_tool_contract.py`, правка однострочная и уже сделана в `fixtures/korni` · шапка `fixtures/korni/PROGNAT.sh` (строка 25) объявляет «ВОСЕМЬ ЛОВУШЕК», их 13 — число обязано считаться командой, а не стоять словом (`KONSTITUCIYA §10`) · `fixtures/zahod/PROGNAT.sh:474` копирует шаблон из несуществующего `_generator/_studio/…` и живёт только на откате `2>/dev/null ||` строкой ниже: мёртвую половину условия убрать либо объяснить комментарием, почему она нужна.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. **Влито.** `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep -c 'zahod/'` → `0`. Не ноль — каждая оставшаяся названа поимённо с выводом `python3 _generator/tools/git_zona.py poteri` рядом и с причиной, почему её вливать нельзя. Молчаливый остаток — провал.
2. 🔴 **Сторож очереди КРАСНЕЕТ на живом объекте, а не на выдуманном.** Прогнать его на `2026-08-12T0311` ДО переадресации — обязан дать красное с диагнозом; ПОСЛЕ — обязан замолчать. Приведи оба вывода и оба `rc`. Сторож, зелёный на заявке с восемнадцатью одинаковыми отказами, — гейт без гейта, и это провал критерия.
3. **Чисто в обоих репозиториях.** `python3 _generator/tools/git_zona.py check` в `materials` и то же в `disciplina` (через `GIT_ZONA_REPO`) → вне git ничего.
4. **Мусора нет.** По каждому из четырёх путей `test -e <путь>; echo $?` → `1`. Приведи все четыре строки.
5. **Фикстуры.** `bash _generator/tools/fixtures/git_zona/PROGNAT.sh; echo $?` → `0` и то же для `register_doc` (сейчас у обеих `1`). Шапка `fixtures/korni/PROGNAT.sh` называет число ловушек командой; прогони её и покажи, что число совпало с фактическим.
6. 🔴 **ЖИВОЙ ПРОГОН — доказательство, что влитие доехало до диска, а не только до истории.** Из ГЛАВНОЙ папки, после влития:
   ```
   cd /Users/ivanyakovlev/Documents/GitHub/materials && GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/disciplina \
     python3 -c "import sys;sys.path.insert(0,'_generator/tools');import korni;p=korni.шаблон('_TEMPLATE-zahod.md');print(p, p.is_file())"
   ```
   Обязано напечатать путь внутри `materials` и `True`. Путь внутри `disciplina` или `False` — влитие не доехало, критерий провален.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). **Здесь верификатор ОБЯЗАТЕЛЕН: заход лоссовый** — восемь влитий и пять удалений. Свежий субагент, ДРУГОЙ метод: сверить `python3 _generator/tools/git_zona.py poteri` ДО и ПОСЛЕ (список потерь обязан быть ПУСТ) и `git --no-optional-locks show --stat` по каждому влитию; отдельно грепнуть влитые файлы на маркеры конфликта (`<<<<<<<`, `>>>>>>>`) — влитие с неразрешённым конфликтом проходит МОЛЧА. Его ответ обязан кончаться строкой «выдано N позиций из M найденных».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/_INFRA-git/zayavki/ _generator/tools/git_zona.py _generator/tools/fixtures/git_zona/PROGNAT.sh _generator/tools/fixtures/register_doc/PROGNAT.sh _generator/tools/fixtures/korni/PROGNAT.sh _generator/tools/fixtures/zahod/PROGNAT.sh _studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dom-shablonov.md _studio/docs/KARTA.md /Users/ivanyakovlev/Documents/GitHub/disciplina/tools/_pp_tmp.py /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/.probe /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/_TEMPLATE-arka/ /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/ /Users/ivanyakovlev/Documents/GitHub/disciplina/README.md /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/POKRYTIE.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/_INFRA-git/zayavki/ _generator/tools/git_zona.py _generator/tools/fixtures/git_zona/PROGNAT.sh _generator/tools/fixtures/register_doc/PROGNAT.sh _generator/tools/fixtures/korni/PROGNAT.sh _generator/tools/fixtures/zahod/PROGNAT.sh _studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dom-shablonov.md _studio/docs/KARTA.md /Users/ivanyakovlev/Documents/GitHub/disciplina/tools/_pp_tmp.py /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/.probe /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/_TEMPLATE-arka/ /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/ /Users/ivanyakovlev/Documents/GitHub/disciplina/README.md /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/POKRYTIE.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_studio/zhurnal/_INFRA-git/zayavki/` `_generator/tools/git_zona.py` `_generator/tools/fixtures/git_zona/PROGNAT.sh` `_generator/tools/fixtures/register_doc/PROGNAT.sh` `_generator/tools/fixtures/korni/PROGNAT.sh` `_generator/tools/fixtures/zahod/PROGNAT.sh` `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dom-shablonov.md` `_studio/docs/KARTA.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/tools/_pp_tmp.py` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/.probe` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/_TEMPLATE-arka/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/README.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/POKRYTIE.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/_INFRA-git/zayavki/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/git_zona.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/git_zona/PROGNAT.sh` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/register_doc/PROGNAT.sh` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/korni/PROGNAT.sh` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/zahod/PROGNAT.sh` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dom-shablonov.md` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/docs/KARTA.md` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/tools/_pp_tmp.py` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/.probe` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/_TEMPLATE-arka/` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/README.md` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/POKRYTIE.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Снято ПЕРВЫМ ходом, до плана (числа мои, не из шапки):**
```
schet_nezakrytogo.py 2026-08-14_rychagi-zahodov → инцидентов 0 · долгов ЖИВ 39 · уроков без ВЕРДИКТ 0 · очередь «ДОСТАВЛЕНО: нет» 0
```
Расхождение с шапкой: пункт 2 в шапке «н/д (DOLG.md недоступен)», у меня **39** — репозиторий `disciplina` с моей стороны читается, из песочницы аналитика нет. Это не долг захода, а разная видимость каналов; в `## УРОКИ ФАБРИКЕ` вынесено.
```
ЗАСТРЯЛА по заявкам: …T0311 = 18 · …T0359 = 17 (в шапке 16 — выросла за ночь) · …T1544 = 2 · …T1920 = 1
невлитых веток zahod/ на входе: 8 (все восемь — ровно названные аналитиком)
```

### Р0. Оспаривание КРИТЕРИЯ ГОТОВНОСТИ — до работы, как велит §1
**Критерий 1 неисполним буквально и провалится на честной работе.** Он требует
`git branch --no-merged arka/mat-kostyak | grep -c 'zahod/'` → `0`. Но §4 обязывает
меня закоммитить свою зону в `zahod/vyvoz-i-ochered` последним ходом — и ровно этим
ходом моя собственная ветка становится невлитой. Счётчик даст `1` не потому, что
что-то не вывезено, а потому, что я исполнил §4. Ноль достижим только если НЕ
коммитить зону, то есть провалить §4, либо влить свою ветку самому, что прямо
запрещено контрактом зоны («ветку в конце НЕ вливай»).
**Поправка, по которой и буду отчитываться:** считаю невлитые `zahod/` **за вычетом
своей ветки** — `grep 'zahod/' | grep -vc 'vyvoz-i-ochered'` → обязан быть `0`, и
отдельной строкой печатаю полный список, чтобы вычет был виден, а не спрятан.

### Р1. Порядок §0.1 против стартовой строки — расхождение и что я делаю
§0.1 требует: сначала субагент вливает в основную, ПОТОМ я завожу рабочую папку —
чтобы моя ветка отпочковалась от свежей основной. Стартовая строка владельца велела
завести папку ПЕРВЫМ ходом; я исполнил её и получил ветку от основной **до** восьми
влитий. Следствие проверено, а не предположено: `fixtures/korni/PROGNAT.sh` у меня в
папке ещё БЕЗ `check_tool_contract.py` в списке `cp` — то есть я держу в руках
дореформенный инструмент, и правил бы его вслепую поверх чужой работы.
**Решение:** после доклада субагента подтягиваю свежую основную в свою ветку
(`merge arka/mat-kostyak` в свою, не наоборот) и только потом правлю код. Это не
влитие моей ветки в основную и контракт не нарушает.

### Р2. Развилки задачи 1 — очередь. Называю до работы, как требует §2
- **Поле рода работы — `РОД:`**, третьей строкой шапки заявки, рядом с `СРОЧНОСТЬ:`
  (тот же формат: `КЛЮЧ: latin-значение`, тот же парсер). Значения два:
  `git-operaciya` и `pravka-koda`. Ставится флагом `--rod` у `zayavka`; у старых
  заявок поля нет — читается как `?` и это НЕ ошибка (иначе четыре живые заявки
  стали бы битыми). `zayavki` печатает род в строке заявки.
- **Второй дом — `_studio/zhurnal/_INFRA-git/zayavki/na-zahod/`.** Причина выбора
  места: зона захода не выпускает меня за `zayavki/`, а рядом уже живёт `sdelano/`
  по тому же образцу; `_zayavki_otkrytyye()` глобит `*.md` без рекурсии — значит
  переадресованная заявка исчезает из очереди контура **по построению**, без
  отдельного фильтра. Дверь: новая подкоманда `zayavka-peredat <id> --pochemu "…"`.
  `zayavki` печатает их ОТДЕЛЬНЫМ блоком с охватом — уехавшее не должно пропасть
  из виду, оно должно перестать предлагаться не тому исполнителю.
- **Чем краснеет сторож.** Отдельная подкоманда `zayavka-storozh [<id>]`, `rc=1` —
  это то, что можно прогнать на живом объекте и показать оба кода возврата, как
  требует критерий 2. Порог: **≥3 `ЗАСТРЯЛА:` подряд без промежуточного результата**.
  Диагноз печатает сходство последних трёх (Жаккар по нормализованным словам, %) —
  «одинаковость» я показываю числом, а не утверждаю словом (`KONSTITUCIYA §10`).
  Молчит после переадресации: файла в очереди больше нет, а по id сторож находит
  его в `na-zahod/` и отвечает зелёным с указанием, куда тот лёг.
  🔴 Развилка внутри развилки: делать ли `rc=1` у самого `zayavki`. Сторож в
  отдельной команде звать может и не каждый; `zayavki` контур зовёт первым ходом
  всегда. Проверю грепом, не держат ли фикстуры `zayavki` за `rc=0`; не держат —
  краснеет и `zayavki`, и это сильный рычаг. Держат — оставляю печать в `zayavki`
  и `rc` только у сторожа, и называю это в отчёте.
- 🔴 Содержание 0311/0359 НЕ реализую: `gejt_kartochki.py`, `tipy_slajdov.py`,
  `PRAVILA-FAZY-2.md` не открываю вовсе. Обе уезжают в `na-zahod/`.

### Р3. Порядок ходов
1. Субагент гит-контура целиком по §0.1 — один, жду, шесть строк дословно в отчёт.
2. Подтягиваю свежую основную в свою ветку (Р1), сверяю, что фикстуры стали теми.
3. Задача 1 — очередь: `РОД:` + `na-zahod/` + сторож; прогон сторожа на 0311 ДО.
4. Переадресация 0311 и 0359; прогон сторожа ПОСЛЕ; оба `rc` в отчёт.
5. Задача 4 — четыре долга фикстур; `bash PROGNAT.sh; echo $?` по каждой.
6. Задача 3 — мусор песочницы, пять путей; `test -e` по каждому.
7. Задача 2 — хвост Cowork в обоих репозиториях (что не забрал контур).
8. Верификатор §3 — свежий субагент, ДРУГИМ методом, обязателен (заход лоссовый).
9. Коммит зоны двумя ходами по §4, гигиена Г1–Г6, отчёт.

**Предпосылки, которые могут оказаться ложными и которые я проверяю, а не считаю
данностью:** (а) что все восемь веток вливаются без конфликта — конфликт законный
исход, ветка останется невлитой со строкой причины; (б) что `_TEMPLATE-arka/` в
`disciplina` мертва после влития — проверю, что на неё никто не ссылается, прежде
чем удалять; (в) что «13 путей хвоста» — число из шапки, снимаю командой заново.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged <основная>     # невлитые
git --no-optional-locks status --porcelain | wc -l        # не закоммичено
git --no-optional-locks log --oneline @{u}.. | wc -l      # не вывезено
python3 _generator/tools/git_zona.py zayavki              # открытые заявки
```
<сюда — вывод, дословно>

**ЧТО СДЕЛАНО** *(с хэшами)*
<влито / закоммичено / вывезено / погашено / заявки закрыты — поимённо>

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `<да | нет>`
*(`нет` законно — но ТОЛЬКО со списком поимённо: что осталось и почему это непроходимо ТВОИМИ
правами (чужая живая рабочая папка, нужно решение владельца, конфликт, обеих сторон которого
не понимаешь). «Сложно» и «не моя тема» причинами не являются. `нет` без списка = красный.)*

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
