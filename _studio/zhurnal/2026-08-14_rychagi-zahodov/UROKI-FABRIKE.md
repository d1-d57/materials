# УРОКИ ФАБРИКЕ — арка «2026-08-14_rychagi-zahodov»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

### Сторож «работа доехала в git» зеленеет на зоне, которой не существует
ЦЕНА: замер приёмки 14.08 — `git_zona.py check --zone <несуществующая-папка>` даёт `✅` и rc=0; то же на зоне в чужом репозитории. Гейт 0 приёмки стоит первым и держит весь остальной ритуал, а отличить «чисто» от «такой зоны нет» он не умеет. В тот же день приёмка прогнала гейт 0 на двух зонах и получила ✅, из которых напечатана была одна.
ДОМ: disciplina/doma/priyomka/POKRYTIE.md
ВЕРДИКТ:

### Починка проверена тем же способом, каким ловился дефект, — прогоном на чужой машине, а не чтением кода
ЦЕНА (положительная, устройство): замер 14.08 — приёмка собрала пробный заход генератором ветки `kanon-put` из ПЕСОЧНИЦЫ (корень `/tmp/pesok-42/mnt/materials`, декларация указывает на путь владельца): вхождений пути песочницы 0, канонического пути 2. Две редакции той же правки до этого выглядели верными при чтении и падали на прогоне — вторую поймала ловушка 61, а не глаза.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Гейт 0 приёмки был декоративным на каждом заходе — и это нашлось только потому, что починили соседний гейт
ЦЕНА: находка `gejty-ne-tuda` 14.08. `priyomka.py` брал зону первым совпадением по файлу, а первым всегда шёл плейсхолдер шаблона `check --zone <зона>`; `git_zona.py check` при этом зеленел на несуществующей зоне. Два дефекта гасили друг друга: гейт печатал ✅ и означал «проверять было нечего». **Так приняты оба отчёта 14.08.** Второй виток той же ловушки поймал догфуд: первая редакция починки стала брать демонстрационную команду из текста задачи. Урок не про зону: **два дефекта, каждый из которых делает второй невидимым, не находятся проверкой — только починкой одного из них.**
ДОМ: disciplina/doma/priyomka/POKRYTIE.md
ВЕРДИКТ:

### Приёмка из песочницы не может проверить то, что живёт в рабочей папке захода
ЦЕНА: замер 14.08 — рабочие папки `materials-wt/*` в песочнице Cowork не резолвятся как git-репозитории: файл `.git` несёт абсолютный путь машины владельца (`gitdir: /Users/…`), и `rev-parse` падает. Из-за этого Г3 у приёмки красный там, где у исполнителя зелёный, а перегнать числовое утверждение отчёта нечем. Сегодня это стоило двух ложных расхождений с отчётом, оба сняты только после того, как приёмка поняла причину. Правильный ход — назвать охват честно («подтверждение только со слов отчёта»), а не записать исполнителю брак.
ДОМ: disciplina/doma/priyomka/POKRYTIE.md
ВЕРДИКТ:

### Клауза, поставленная по букве задания, молчала бы на своём эталонном дефекте
ЦЕНА: замер захода `chislo-i-kirillica` 15.08. Задание требовало красить «число без команды И без метки времени». Живой прогон показал: в `kod_gejty-ne-tuda.md` рядом с «невлитых веток 0» команда СТОЯЛА — соврало время, и клауза по букве промолчала бы ровно на том дефекте, ради которого заведена. Исполнитель поправил критерий в `## ПЛАН` ДО работы и обосновал замером на корпусе (292 файла: «команда ИЛИ метка» — 112 красных, «только метка» — 140). **Аналитик формулирует клаузу по симптому, который помнит, а не по тому, чем симптом отличается от здорового случая.**
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Замер сделан обёрткой, а не тем инструментом, который стоит у владельца
ЦЕНА: замер 15.08, поймано самим исполнителем сверкой с числами захода. В его сессии `grep` — функция-обёртка над `ugrep` (Unicode-aware), и она печатала верный ответ во всех локалях; на `/usr/bin/grep` владельца `LC_ALL=C` даёт три строки вместо одной. Приёмка со своей стороны замерила GNU grep песочницы и получила третий результат — падение «Invalid collation character». **Три среды, три разных ответа на один вопрос.** Стоило бы всего построения, если бы цифры не разошлись с заданием и никто не пересматривал.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Sonnet справился там, где механика расписана — замер, а не ощущение
ЦЕНА (положительная, устройство): заход `ohvat-verdikta` 15.08 — первый на Sonnet после трёх на Opus. Все шесть пунктов критерия закрыты, приёмка воспроизвела числа: 11 красных из 300 сошлись точно, три образца положительного контроля зелёные, оба бывших ложных стали зелёными. Больше того, по ходу калибровки Sonnet сам нашёл два ложных срабатывания («не проверяется» — отрицание, а не вердикт; «§1.4/1.6» читалось как охват «4/1») и сузил регексп. **Что за это заплачено на стороне аналитика:** файл назван один, функция-образец названа поимённо, место вставки указано, три файла положительного контроля выписаны, вторая половина семьи закрыта прямым запретом. Это дороже обычного захода в написании и дешевле в прогоне.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Новый гейт продублировал печать, которая уже была в том же файле
ЦЕНА: находка исполнителя 15.08, приёмкой подтверждена грепом: `priyomka.py` уже содержал `ohvat_polozhitelnogo()` и печать «Охват у вердикта (часть C3, печать)» — ровно ту же мысль, что новый Г11, но как печать для рук, а не гейт. Ни аналитик при написании захода, ни исполнитель до самого конца этого не заметили; сейчас в одном файле два механизма про одно. Цена пока мала (дубль печати), но растёт: разойдутся формулировки — читатель получит два разных ответа на один вопрос.
ДОМ: disciplina/doma/priyomka/POKRYTIE.md
ВЕРДИКТ:

### Субагент гит-контура доложил «влито», а ветка не влита
ЦЕНА: замер приёмки 15.08. Доклад контура 15.08 сообщал: «vynos-2/kanon-put/pravila-kadra/gejty-ne-tuda были влиты ДО меня — проверено фактом (0 невлитых)». Прямая проверка приёмки: `git merge-base --is-ancestor zahod/pravila-kadra arka/mat-kostyak` → НЕ влита, и она же стоит в `branch --no-merged`. Исполнитель заметил расхождение сам и вынес в вопросы — то есть поймал его не гейт, а человек. Ложное «влито» опаснее честного «не влил»: оно закрывает вопрос.
ДОМ: disciplina/doma/priyomka/POKRYTIE.md
ВЕРДИКТ:

### Гейт печатал номер строки, которого в файле нет — адрес считался по урезанному тексту
ЦЕНА: находка захода `granicy-poiska` 15.08. Первая редакция клаузы строила номер строки по тексту БЕЗ стартового блока, а печатала его как номер в исходном файле: на живом корпусе три «находки» указывали на строки, где грепа нет вовсе. Поймано калибровкой, а не чтением. Класс шире одной клаузы: **гейт, печатающий неверный адрес, тратит время читателя ровно как ложное срабатывание, но выглядит достовернее** — адрес есть, значит проверяли.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Заявки копятся, потому что гашение веток блокирует один и тот же файл вне git
ЦЕНА: замер контура 15.08 — заявок открыто 8, закрыто 0. Четыре ветки (`kanon-put`, `gejty-ne-tuda`, `chislo-i-kirillica`, `ohvat-verdikta`) влиты, но не погашены: `zakryt-vetku` отказывает у всех четырёх по ОДНОЙ причине — незакоммиченный `_studio/zhurnal/_INFRA-git/INCIDENTY.md` в их рабочих папках. Файл этот дописывает сам `git_zona.py` при влитии, то есть **инструмент своим автологом блокирует собственную уборку**. Один дефект держит восемь заявок.
ДОМ: disciplina/doma/priyomka/POKRYTIE.md
ВЕРДИКТ:

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

### Приёмка оставила пробные файлы в рабочей папке принимаемого захода — и заблокировала следующий
ЦЕНА: при приёмке `nositeli-gen` я собирал пробные заходы `probaA/B/C` прямо в его worktree, проверяя новый флаг `--intervyu`. Файлы остались незакоммиченными. Следующий заход `rychagi` первым же ходом попытался влить ту ветку, увидел в папке чужие правки со свежим временем, честно принял их за живую параллельную работу и **остановился целиком** — ни один из девяти рычагов не начат, ночной прогон простоял. Осторожность исполнителя была правильной: отличить мусор приёмки от живой работы он не мог по построению. Класс: **приёмка пишет в рабочую папку принимаемого захода, а следующий заход читает её как чужую живую работу.**
ДОМ: disciplina/doma/priyomka/POKRYTIE.md
ВЕРДИКТ:

### Калибровку гейта проверили на СТАРЫХ файлах, а красным он стал на НОВЫХ, которые сам же генератор и рождает
ЦЕНА: З6 калибровали 14.08 по 25 живым `kod_*.md` — вышло «1 срабатывание, ложных 0», приёмка это подтвердила и приняла. Но в том же заходе переписали блок §0.1: гит-контур получил вид `**1. ВЛИТЬ ПЕРЕД РАБОТОЙ — САМ, В ЭТОМ ОКНЕ.**` — жирный спан, начинающийся с номера пункта. З6 читает его как числовое утверждение без соседней команды. Итог: **каждый свежесобранный заход рождается КРАСНЫМ по З6** — проверено приёмкой на трёх пробных сборках подряд (`probaZ`, `probaY`, `probaX`, оба канала). Ровно тот исход, против которого калибровка и делалась: гейт, красный на здоровом, обходят `--no-verify` вместе со всей защитой. Промах приёмки назван честно: я проверил эталон из СТАРЫХ файлов и не собрал ни одного НОВОГО — а формат сменился в том же заходе.
ДОМ: disciplina/doma/zahody/POKRYTIE.md, disciplina/doma/priyomka/POKRYTIE.md
ВЕРДИКТ:

### Три чужие фикстуры красны не от нового флага, а от списка копирования, который отстал от импортов
ЦЕНА: `gigiena`, `korni`, `register_doc` красные и после починки `--intervyu da`. Приёмка проверила причину: их синтетический репозиторий собирается `cp` по куратируемому списку файлов, и `modeli.py` в нём нет ни у одной из трёх (`grep -c` = 0), а `bootstrap_zahod.py` его импортирует. Контрольный прогон на коде БЕЗ захода даёт то же красное — регрессии нет, класс унаследован и уже записан уроком 14.08 по фикстуре `git_zona`. Значит класс шире, чем думали: **не одна фикстура отстала, а четыре из восьми.**
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Калибровка заявлена чистой, но не воспроизводится: клауза ловит СЛОВО, а не роль
ЦЕНА: отчёт `rychagi-2` заявляет по клаузе З15 «прогнал по всему корпусу — 271 живой `kod_*.md`; срабатываний по здоровому корпусу 0, ложных 0». Приёмка перегнала на живом корпусе (289 файлов): **срабатываний 2, и оба ЛОЖНЫЕ.** `kod_arhitektura-doma.md` — слово «обязательные» в последнем пункте относится к секциям стандарта («печатает, у кого какие обязательные секции есть»); `kod_tipologia-dochistka.md` — к видам дефекта («недостающий обязательный · нарушенный порядок»). Ни в одном случае обязательным не назван сам пункт критерия. Класс промаха тот же, что уже ловили у первых редакций З8, З9 и у моего замера возражательных триггеров: **проверяется присутствие слова, а не его роль.** По критерию захода («ложных > 0 — рычаг не считается построенным») З15 требует сужения, а не отката.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Исполнитель отказался строить половину рычага — и это лучшее решение в отчёте
ЦЕНА: часть Б рычага 2 («охват X из Y в отрицательном вердикте», 7 записей) НЕ построена, и довод проверяемый: канонические формулировки самого правила в корпусе используют БУКВЕННЫЕ плейсхолдеры («X источников из Y намеченных»), поэтому гейт, требующий цифр рядом с фразой, красил бы сами примеры правила; а гейт «слово из слово» тонет в русском предлоге «из». Плюс 5 записей из 7 живут в `priyomka` — их механизм принял бы `priyomka.py`, а не `check_zahod.py`, то есть это другая работа, а не недоделанная эта. Отдельно отвергнута проба «маркер 🔴 на последнем пункте»: законен в 83 файлах из 271, узнано ДО попадания в `HARD_CHECKS`. **Ценность записи в том, что отказ обоснован замером, а не вкусом** — ровно то, ради чего в заходе стоит «опровержение — полноценный успех».
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Заход улучшает ГЕНЕРАТОР — и следующий заход рождается генератором ДО этих улучшений
ЦЕНА: замер 14.08. Механизм «гит-контур уходит в субагента» и фраза-исключение «git-контур — законное исключение из „ничего сверх задачи"» построены заходом `nositeli-gen` и живут в ветках (`grep -c 'zadanie-subagentu'` → 3 в `zahod/rychagi-2` и `zahod/rychagi-3`). В основной ветке `arka/mat-kostyak` их **0**: невлитых веток тринадцать, включая всю цепочку `nositel → nositeli-gen → rychagi → rychagi-2 → rychagi-3`. Аналитик собирал ВСЕ три последних захода генератором из ОСНОВНОЙ папки — то есть старым: `grep -c 'законное исключение'` → **0 во всех трёх**. Владелец увидел это первым: очередной заход спросил разрешения работать вне зоны — ровно то, что фраза-исключение и снимает. **Петля замкнута: улучшение генератора не действует, пока ветка не влита, а следующий заход собирается тем же неулучшенным генератором и повторяет исходную ошибку.** Класс шире отдельного механизма: он касается ЛЮБОЙ правки `bootstrap_zahod.py`.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Калибровка на выборке в 27 файлов и на корпусе в 290 — разные калибровки
ЦЕНА: клауза `З14` (Рычаг 1) калибровалась на 27 файлах и была заявлена как «ложных 0». На полном корпусе — **18 срабатываний из 293**. Приёмка посмотрела три подряд («Два признака из девяти…», «ЖИВ», «Спецификация лежит В ЭТОМ ФАЙЛЕ») — все выглядят настоящими фактами о мире без команды рядом, то есть клауза скорее работает, чем врёт; но **числа «ложных 0» на 27 файлах и на 290 — не одно и то же утверждение**, и второе никто не снимал. Исполнитель `rychagi-3` заметил разрыв сам, назвал его в `## ВОПРОСЫ` и честно сказал, что истинность не проверял — не его зона. Правило захода «калибровать на живом корпусе» с этого дня означает **весь корпус, число снимать `ls | wc -l`**, а не удобную выборку.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Сужение «с присутствия слова на роль» опровергло напрашивающееся правило замером
ЦЕНА: при сужении `З15` напрашивалось простое правило — «короткая форма (обязателен/обязательна) говорит о пункте, полная (обязательный) о рабочем объекте». Исполнитель проверил все 10 живых вхождений «обязательн*» в пунктах критерия по 290 файлам и **опроверг его двумя контрпримерами**: «ловушка… обязательна» и «следствие обязательно» — короткие формы при рабочем объекте, а обе истинные роли выражены полной формой. Итог: судится не форма слова, а **слово, к которому обязательность отнесена** (`пункт`, `условие`, `критерий`, `требование`). Ценность записи в том, что дешёвая гипотеза была проверена ДО постройки, а не после первого ложного красного.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Петля «улучшение генератора не доезжает до генератора» разорвана — и это проверяется одним грепом
ЦЕНА (обратная, положительная): до захода `git-kontur` в основной папке было 12 клауз линтера и **0** упоминаний `zadanie-subagentu` при 17 и 3 в ветках — то есть каждый следующий заход собирался инструментом ДО собственных улучшений и повторял закрытые ошибки. Три захода подряд вышли старыми, поймал владелец по симптому. После влития: в основной папке **17 клауз и 3 упоминания**, петли нет. Механизм починен по существу: подкоманда `vlit-v-osnovnuyu` вливает в основную ветку через `git -C` главной рабочей копии, слот «ВЛИТЬ ПЕРЕД РАБОТОЙ» из окна исполнителя убран — надобность отпала, ветка отпочковывается от основной, которая уже всё содержит.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Конфликт слияния — это не git-задача, а неразрешённая развилка в чужой подсистеме
ЦЕНА: из 14 веток 4 не влиты, и ни одна не по технической причине. `vid-blokov-vnedrenie` и `polya-i-uzor` конфликтуют в `bloki.py` содержательно: влитый `sceny-i z-blokov` ставит автотег `{@K}` на каждом абзаце, а невлитая ветка предупреждает «не трогать, эту функцию зовёт `gejt_kartochki.py` для счёта слов». Приёмка проверила предупреждение и подтвердила фактом: `gejt_kartochki.py:731-732` действительно делает `flat = bloki.render_section_markdown(...)` и `n_words = len(flat.split())` — **автотеги попадут в счёт и надуют `byudzhet_slov`**. То есть за конфликтом стоит живой дефект, а не расхождение текстов; слить его «выбрав сторону» значит либо вернуть дефект, либо молча отменить чужую работу. Исполнитель отступил `--abort` и назвал развилку — верно. Класс: **конфликт в чужой подсистеме разрешает тот, кто ей владеет, а не тот, кто вливает.**
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Порог гейта, поставленный по двум состояниям, — это не калибровка, и заход сказал это сам
ЦЕНА: порог «вне git > 15» у нового гейта Г10 выбран по выборке из ДВУХ состояний — здоровое (11 путей) и больное (23). Исполнитель назвал это в `## ВОПРОСЫ` сам, не дожидаясь приёмки: две точки не задают порога, между ними умещается любое число. Класс тот же, что оплачен на З6 и З14 («ложных 0 на 27 файлах» ≠ «на 290»), только теперь про пороги, а не про клаузы. Практически: порог живёт до первого честного замера на месяце работы, и это надо знать, а не считать его установленным.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Гейт накопления судит репозиторий, и потому обязан честно печатать, чего он НЕ видит
ЦЕНА (положительная, устройство): Г10 при прогоне из рабочей папки честно напечатал «невывезено НЕПРОВЕРЯЕМО — у репозитория нет ни одного удалённого» и строку «не видит: чужие репозитории вне зоны · ветки только на origin · репозитории без удалённого». Без этой строки зелёный Г10 в песочнице читался бы как «долга нет», хотя проверена была одна величина из трёх. Приёмка это подтвердила прогоном: два красных гейта (Г0, Г1) на том же прогоне оказались артефактами песочницы — хэш `be97b27` в основной папке существует, зона чиста (`rc=0`).
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

---

## Пункты очереди заходов, доставленные `dostavit_urok.py`

> Дословные находки из секций `## ВОПРОСЫ` файлов-заходов — голос исполнителя, который об это споткнулся, не редактора. Разбирает их человек: находка описывает, что мешало, но задачей сама не становится.
> Строка `ДОСТАВЛЕНО: <заход>#<N>` внизу каждой записи — та же метка, что стоит в источнике; по ней Г7 `priyomka.py` проверяет, что доставка не объявлена ложно.

#### gejty-ne-tuda#1

1. Стартовое сообщение и §0.1 предписывают разный первый ход; ветка захода отпочковалась от
   основной ДО влития и отстала на 7 коммитов, один из них правил файлы моей же зоны.
   ДОМ: _studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
ДОСТАВЛЕНО: gejty-ne-tuda#1 · 2026-08-14

#### gejty-ne-tuda#2

2. «Невлитых веток 0» вшито в §0.1 числом при сборке и к прогону устарело (их было 3, на них стояла
   блокирующая заявка) — вписать команду, а не число (`KONSTITUCIYA §10`).
   ДОМ: _studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
ДОСТАВЛЕНО: gejty-ne-tuda#2 · 2026-08-14

#### gejty-ne-tuda#3

3. Плейсхолдер `<зона>` из шаблона §4 делал Г0 приёмки декоративным на каждом заходе; в
   `priyomka.py` починено, но лечится и со стороны генератора — §4 может печатать конкретную зону
   захода вместо `<зона>`, тогда гейту не придётся её угадывать.
   ДОМ: _studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
ДОСТАВЛЕНО: gejty-ne-tuda#3 · 2026-08-14

#### ohvat-verdikta#1

1. Стартовое сообщение владельца (шаблон `bootstrap_zahod.py`) предписывает `worktree add` первым ходом, а тело §0.1 того же захода требует обратный порядок (сначала влитие субагентом в основную) — см. `## УРОКИ ФАБРИКЕ` выше, там же цена.
   ДОМ: `_studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md`
ДОСТАВЛЕНО: ohvat-verdikta#1 · 2026-08-15

#### granicy-poiska#1

1. **`git commit -- <путь>` игнорирует индекс и коммитит содержимое РАБОЧЕЙ КОПИИ
   напрямую** — не задокументированная в явном виде здесь ловушка git, а не находка
   про эту задачу. Цена: `disciplina/doma/zahody/POKRYTIE.md` нёс чужие
   незакоммиченные правки (аналитика) плюс мою одну строку; аккуратно застейдженный
   через `git hash-object` + `update-index --cacheinfo` blob (ровно моя строка) был
   ПРОИГНОРИРОВАН, потому что `commit` был вызван с явным pathspec — команда в этом
   режиме документированно берёт текущее содержимое перечисленных файлов, а не индекс,
   и в коммит уехали все чужие незакоммиченные строки тоже. Поймано ДО пуша, отменено
   `git reset --mixed HEAD~1` (рабочая копия не тронута), пересобрано правильно —
   застейджен тот же blob, `commit` вызван БЕЗ pathspec (взял ровно то, что в индексе).
   Урок: хирургический коммит одной строки в файле, где есть чужие незакоммиченные
   правки, обязан коммититься БЕЗ pathspec после точечного `update-index`, а не с ним —
   иначе «--» в `git_zona.py`-каноне (`GIT-disciplina.md §3`, «`--` в самом commit») не
   спасает от этого конкретного случая, потому что канон говорит про ЧУЖОЕ в ИНДЕКСЕ,
   а не про то, что `commit` с pathspec обходит индекс целиком для НАЗВАННЫХ путей.
   ДОМ: 2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
ДОСТАВЛЕНО: granicy-poiska#1 · 2026-08-15

### З6 `check_zahod.py` читает номер раздела «§0.1» как число 0 и краснеет на вставке САМОГО генератора
ЦЕНА: свежесобранный `kod_shablony-odin-dom.md` (15.08) даёт ровно одно красное, и оно ложное — З6 указывает на абзац «ПЕРВЫМ ходом — СУБАГЕНТ на доделку git-дисциплины (§0.1: …)», который вписывает `bootstrap_zahod.py`, а не аналитик. Дата в этом абзаце есть («замер 2026-08-15»), но она дальше окна ±300 символов. Значит КАЖДЫЙ заход, собранный после появления блока §0.1, рождается с красным З6, которое аналитик исправить не может, не правя чужую вставку. Это тот самый класс «гейт, который врёт»: красное, снять которое нечем, обучает не смотреть на гейт вовсе. Поймано при сборке З-0, сверено на трёх соседних заходах арки (`kod_rychagi-3` — 4 красных, `kod_ohvat-verdikta` — 1, `kod_kanon-put` — 2).
ВЕРДИКТ:

### `--zone` у `bootstrap_zahod.py` принимает прозу и молча собирает из неё мусорный контракт зоны
ЦЕНА: один потраченный вызов генератора и один НЕСТИРАЕМЫЙ файл в журнале (`kod_dom-shablonov.md`, замазан надгробием — Cowork удалять в примонтированной папке не может). Значение `--zone` разбивается по пробелам и каждое слово оборачивается в бэктики, поэтому фраза «В materials: …, ПЛЮС в репозитории …» превратилась в контракт зоны из бэктиченных обрывков, а вместе с ним сломалась и команда точки отката `git add <зона>`. Отказа нет ни на одном шаге: генератор доволен, `check_sborki` зелёный, дефект виден только глазами в готовом файле.
ВЕРДИКТ:

### Предупреждение «нужен был `--worktree`» печатается ПОСЛЕ того, как файл уже создан и перезаписи не подлежит
ЦЕНА: та же, что и выше, и в том же вызове — генератор сам сказал «Правильно: пересоздать с `--worktree`», но пересоздать нельзя: он идемпотентен и существующий файл не перезатирает, а Cowork не может его удалить. Совет, выданный после необратимого шага, исполнить нечем. Обстановка на гонку была вычислима ДО записи: 13 живых рабочих папок и основная папка на чужой ветке.
ВЕРДИКТ:

### Проверка «а можно ли сюда писать» через `touch` оставляет мусор, который потом нечем убрать
ЦЕНА: `disciplina/_studio/zhurnal/.probe` — файл, созданный одной командой ради ответа на вопрос и переживший сессию: `rm` и `os.remove` в примонтированной папке дают `Operation not permitted`. Второй такой же файл (`disciplina/tools/_pp_tmp.py`) висит с 15.08 по той же причине. Запись проверяется тем, что пишешь НУЖНЫЙ файл, а не пробный.
ВЕРДИКТ:

### Гейт Г12 приёмки требует секцию, которую генератор захода не создаёт, — и краснеет на 16 заходах из 16
ЦЕНА: `_TEMPLATE-zahod.md` строка 75 несёт `## ГИГИЕНА ВХОДА`, `priyomka.py` (Г12) её требует, а `bootstrap_zahod.py` о ней не знает вовсе — слова «ГИГИЕНА ВХОДА» в нём нет ни разу. Проверено по всем заходам арки: `grep -c 'ГИГИЕНА ВХОДА' kod_*.md` → 0 у всех шестнадцати. То есть гейт не «иногда краснеет» — он НИ РАЗУ не был зелёным ни на одном живом объекте, и отличить «заход плох» от «гейт требует несуществующего» приёмке нечем. Найдено на приёмке З-0 15.08.
ВЕРДИКТ:

### Г2 и Г3 приёмки судят ОДНУ строку `АРТЕФАКТ` по разным правилам, и кодовый заход не может пройти оба
ЦЕНА: Г2 принял строку «артефакта-сборки нет: заход кодовый, HTML/PDF/колода не собирались» как заполненную — и правильно, форма это разрешает. Следующий гейт Г3 взял ту же строку как ПУТЬ, сделал по ней stat и покраснел: «артефакт не найден на диске: /…/materials/артефакта-сборки нет: заход кодовый…». Любой заход, который ничего не собирает глазами, обязан покраснеть на Г3 по построению. Приёмка З-0 15.08: 2 красных гейта из 3 — дефекты приёмки, а не работы.
ВЕРДИКТ:

### Аналитик вписал в заход число, снятое СВОИМ грепом, а не тем, которым считает сам файл
ЦЕНА: заход З-0 утверждал «сейчас там 13 ловушек — пересчитай»; фактически было 12. Я снял число шаблоном `grep -c 'ЛОВУШКА\|^# [0-9]'`, файл считает себя по `^# ── ЛОВУШКА`. Разница в один — та самая, на которую критерий велел смотреть («число ловушек выросло ровно на новую»), то есть ложное число стояло ровно в месте проверки. Спасло только то, что исполнитель пересчитал и взял фактическое, как было велено, и сказал о расхождении вслух.
ВЕРДИКТ:

### Один и тот же счёт назван в заходе трижды и трижды по-разному
ЦЕНА: §0.1 захода З-0 нёс «невлитых `zahod/*`-веток было 0», слот `--zakryt` того же файла — «НЕПРИКОСНОВЕННЫ девять невлитых веток», факт на прогоне — 7. Два числа противоречили друг другу уже В МОМЕНТ СБОРКИ, до всякого устаревания, и исполнитель потратил первый ход на разрешение противоречия вместо работы. Корень: ноль напечатал генератор командой, девятку вписал я рукой из вчерашнего снимка хэндоффа. Найдено исполнителем, подтверждено приёмкой.
ВЕРДИКТ:
