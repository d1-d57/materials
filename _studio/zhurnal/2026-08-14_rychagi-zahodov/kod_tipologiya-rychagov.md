# Канал исполнителя — tipologiya-rychagov (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — работа целиком классификационная: типология рычагов не расписана нигде, её надо ВЫВЕСТИ из живых файлов, а не применить готовую.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — типологию рычагов надо вывести из живых файлов, готовой нет.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/tipologiya.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_tipologiya-rychagov.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add tipologiya --branch zahod/tipologiya-rychagov, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-14_rychagi-zahodov`
Область: «_studio/zhurnal/2026-08-14_rychagi-zahodov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/magical-elegant-clarke/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Скилл `disciplina-kachestvo` в репозитории `disciplina` — мерило качества остальных девяти скиллов фабрики; собран и принят 15.08, 14 счётных осей, движок `tools/ocenka_skilla.py`, фикстура на 14 ловушек. Прошлый этап: приёмка нашла, что ЗАКОН скилла («правило без рычага — надежда; либо рычаг, либо объявленная слепая зона, либо вычеркнуть; третьего состояния нет») его собственным движком не обеспечен: ось 3 краснеет только при «рычагов 0», и сам скилл несёт 19 правил без рычага и без объявления при 42 всего, оставаясь зелёным. У остальных так же: `disciplina-zahod` — 74, `slajdy` — 112, `reserch-cikl` — 61. ЦЕЛЬ: сделать закон исполнимым — не требуя скрипта под каждое правило.

🔴 **РАБОТА В ДРУГОМ РЕПОЗИТОРИИ, ЧЕМ ИНСТРУМЕНТЫ.** Файл-заход и `_generator/tools/` живут в `materials`, вся зона — в `/Users/ivanyakovlev/Documents/GitHub/disciplina`. Любая команда `git_zona.py` по `disciplina` зовётся с `GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/disciplina`; без переменной инструмент молча возьмёт `materials` и ответит про ЧУЖОЙ репозиторий. Память арки — `disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/`.
Приёмка — по ОТЧЁТУ, без построчной сверки. <Если стоп до цели: получишь X, но НЕ Y.>

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-15) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Владелец 15.08: расписка — это ТИП РЫЧАГА, а не третий носитель рядом с рычагом и слепой зоной. Моя формулировка про три носителя отвергнута владельцем прямо: «мне кажется, расписка — это просто ещё один тип рычага»
2. Расписка мерит НЕ ЗАБЫЛ, а не СДЕЛАЛ, и это пишется рядом с полем. Владелец: «мы предполагаем, что исполнитель или верификатор не будут нам врать — они просто забывают иногда исполнять эти правила»
3. Класс, который расписка закрывает законно и который рычагом не закрывается: ПРОПУСК ШАГА длинного алгоритма. Владелец: «когда запускаешь агента, он какой-то шаг просто пропускает, перепрыгивает почему-то и вообще не делает, особенно когда длинный диалог»
4. Цель — чтобы правил без носителя не оставалось вовсе, и чтобы по возможности носитель был СЧИТАЕМЫЙ, а не расписка. Слепая зона перестаёт быть равноправной опцией: она признание поражения и загоняется в угол
5. Порядок работ: сначала довести скилл контроля качества до идеала, и только потом смотреть на остальные девять. Владелец: «сначала надо этот условный наш скилл контроля довести до идеала, потом уже смотреть на остальные»
6. Рассадка полей-расписок по генераторам bootstrap_* в этот заход НЕ входит — это правка каждого будущего захода, отдельная работа. Фазы и переключение субагентов между ними — тоже отдельная тема, не смешивать
7. Ресёрч 15.08 проведён и лежит в арке (`RESERCH-rychagi-2026-08-15.md`): иерархия из пяти уровней силы, параметры чек-листа (5–9 пунктов, 60–90 секунд, pause point, READ-DO против DO-CONFIRM), измерения по агентам (при восьми одновременных условиях все восемь выполняются в 5.7 % случаев; проверка каждого шага ловит 0.89 сбоев против 0.41 у проверки итога)
8. Владелец 15.08: скилл обязан быть САМ образцом того, чего требует — «он должен быть сам идеальным примером того, что мы пытаемся добиться». Поэтому заход состоит из двух работ, и вторая проверяет первую
9. Владелец 15.08, два статуса и никакого третьего: «либо простроенные, либо считать, что это долги». Правило без носителя обязано быть записано в долг с адресом, молчание запрещено
10. Владелец 15.08: числовые ограничения (сколько правил на фазу и сколько пунктов на расписку) записываются как правила канона, а не как пожелание

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/tipologiya`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/tipologiya-rychagov`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add tipologiya --branch zahod/tipologiya-rychagov
  cd ../materials-wt/tipologiya
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/tools/ocenka_skilla.py` `/Users/ivanyakovlev/Documents/GitHub/disciplina/fixtures/kachestvo/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/kachestvo/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/ --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/tools/ocenka_skilla.py --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/fixtures/kachestvo/ --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/kachestvo/ --kommitit 'materials: ХВОСТ COWORK ЗАБРАТЬ ОБЯЗАТЕЛЬНО — на 16.08 вне git остались файлы-заходы kod_vyvoz-i-ochered.md и kod_tipologiya-rychagov.md, UROKI-FABRIKE.md арки 2026-08-14 и правки соседней арки 2026-08-10; снять командой, не по этому списку' --kommitit 'disciplina, одним коммитом: skills/disciplina-kachestvo/, tools/ocenka_skilla.py, fixtures/kachestvo/, doma/meta/kachestvo/' --zakryt 'после приёмки гасятся ветка zahod/tipologiya-rychagov и рабочая папка ../materials-wt/tipologiya. В disciplina ОТДЕЛЬНЫМ коммитом забрать хвост арки 2026-08-15_skill-kachestva: SESSIYA.md, PLAN.md, UROKI-FABRIKE.md, RESERCH-rychagi-2026-08-15.md, HANDOFF-2026-08-17.md и README.md с их регистрацией — README едет в ТОТ ЖЕ коммит, ворота 5 читают §6 с диска' --zakryt 'НЕПРИКОСНОВЕННЫ девять остальных скиллов и все генераторы materials: рассадка полей-расписок по bootstrap_* — СЛЕДУЮЩИЙ заход, не этот'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/tipologiya-rychagov`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/tools/ocenka_skilla.py` `/Users/ivanyakovlev/Documents/GitHub/disciplina/fixtures/kachestvo/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/kachestvo/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/SKILL.md` целиком (ЗАКОН и ось 3 — предмет правки) · `/Users/ivanyakovlev/Documents/GitHub/disciplina/tools/ocenka_skilla.py`, регулярки `RYCHAG` и `SLEPAYA` в начале файла · `/Users/ivanyakovlev/Documents/GitHub/disciplina/fixtures/kachestvo/PROGNAT.sh` · для сбора материала — все девять `SKILL.md` каталога скиллов репозитория `disciplina` (только чтение, править их запрещено) и `_studio/zhurnal/_TEMPLATE-zahod.md` вместе с `_generator/tools/bootstrap_zahod.py` (образцы уже работающих полей-расписок, читать, НЕ править). Больше ничего; проект не изучай.. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
**ДВЕ РАБОТЫ, и вторая проверяет первую.** Записать канон носителей — и тем же заходом сделать сам скилл его образцом. Скилл про качество, не выполняющий собственных требований, канон не доказывает, а опровергает.

---

### РАБОТА А — записать канон носителей

Вход — `disciplina/_studio/zhurnal/2026-08-15_skill-kachestva/RESERCH-rychagi-2026-08-15.md`. Он даёт рамку и числа; твоё дело — превратить это в правила, у каждого из которых есть чем покраснеть.

**А1. Иерархия силы, пять уровней.** Механизмы ранжируются по одному признаку: насколько защита зависит от того, что человек или агент вспомнит. Сверху вниз: устранить сам шаг · заменить безопасным · физически заблокировать продолжение · регламент и чек-лист · напоминание. 🔴 **На каждый уровень — НАШ живой пример с адресом `файл:строка`, либо явная строка «у нас на этом уровне пусто».** Молчание про пустой уровень и есть та халтура, которую скилл запрещает. Предварительный замер ресёрча: почти все восемь наших носителей — четвёртый уровень и обнаружение постфактум; единственный близкий к третьему — «единственная дверь» `git_zona.py`. Проверь это сам, не бери на веру.

**А2. Правило подъёма — главное правило канона.** У каждого правила назван его уровень. Стоит на четвёртом или пятом — рядом обязана быть строка «чем это поднять выше и что мешает», конкретно, а не «когда-нибудь». Без неё нижний уровень становится вечным.

**А3. Расписка — тип носителя с ПАРАМЕТРАМИ, а не просто поле.** Параметры взяты из традиции чек-листов и обязаны стоять числами: не более 5–9 пунктов на одну паузу · не дольше 60–90 секунд на прохождение · привязка к конкретной паузе процесса, а не «когда-нибудь» · назван режим (READ-DO — читаю шаг, делаю, отмечаю, для сложного и редкого; DO-CONFIRM — делаю по памяти, потом сверяюсь, для рутины).
🔴 **Условие законности расписки, без которого она не работает:** отказ происходит по ЗАБЫВАНИЮ, а не по мотиву, И заполняющий не совпадает с тем, кого правило ограничивает. Живая цена, оплаченная 15.08: флаг `--intervyu da` проставлен аналитиком четырежды за день, и ни один гейт не мог возразить — заполнял тот, кого правило ограничивает. Класс, который расписка закрывает законно, назван владельцем: **пропуск шага длинного алгоритма**; агент не спорит с правилом, он до него не доходит.

**А4. Бюджет одновременных условий.** Не более четырёх-пяти жёстко проверяемых условий на одну фазу. Обоснование числом: при восьми одновременных условиях модель выполняет одно в 41 % случаев, а все восемь сразу — в 5.7 %; условия, набегающие по ходу диалога, соблюдаются хуже поданных списком сразу. Правило обязано нести то, чем оно краснеет.

**А5. Два статуса и никакого третьего** (решение владельца, не развилка). Правило либо имеет носитель, либо ЗАПИСАНО В ДОЛГ по названному адресу. Третье состояние — «правило есть, носителя нет, и об этом молчат» — запрещено. Что оно у нас сейчас массовое, проверь сам: `cd /Users/ivanyakovlev/Documents/GitHub/disciplina && python3 tools/ocenka_skilla.py | grep 'без того и другого'`. Долги после починки считаются той же командой.

**А6. Порядок шагов — не проза.** Самый сильный из доступных нам механизмов, по ресёрчу: порядок, вынесенный из текста в код-оркестратор, единственный не зависит от согласия модели. Опиши это правилом и назови вслух, где у нас порядок до сих пор держится прозой.

**А7. Починить размазанное слово «верификатор».** По живым файлам это всегда свежий субагент с пустым контекстом, двух типов: ДО-верификатор судит состояние перед работой и идёт первым ходом (цена измерена: на разрезе дал 9 расхождений из 38 границ и изменил результат работы), ПОСЛЕ-верификатор судит результат. Но тем же словом названы скрипты — есть ветка `zahod/verifikatory-stilya`. Владелец сказал прямо, что термина не понимает. Дай одно определение и назови адреса, где слово занято вторым смыслом. 🔴 Чужие файлы НЕ правь — только назови.

**Габарит.** Если тело перестаёт помещаться, канон уезжает в `references/` одним уровнем, а в теле остаётся роутер. Это твоё же правило из `KAK-ZAVESTI-SKILL.md §3–§4` — примени его к себе, а не проси исключения.

---

### РАБОТА Б — сделать скилл образцом этого канона

Всё, что записано в работе А, применяется к самому `disciplina-kachestvo`. Сегодня он несёт **19 правил без носителя и без объявления при 42 всего** и при этом печатает «✅ красного нет» — снято его же движком 15.08, пересчитай.

- у каждого правила скилла помечен уровень;
- правил без носителя и без записи в долг — ноль; остальное уехало в долг по названному адресу;
- расписки внутри скилла уложены в габарит 5–9 и привязаны к паузам;
- ось 3 перестаёт быть индикатором и становится воротами.

🔴 **Если после ужесточения скилл краснеет на себе — чини СКИЛЛ, а не порог.** Порог, подогнанный под собственный результат, мерилом не является, и это ровно тот способ, которым гейты у нас уже дважды становились декоративными.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. **Уровень назван у каждого правила.** Движок печатает разбивку по пяти уровням; правил без уровня — `0`, число командой.
2. 🔴 **Правил без носителя И без записи в долг — `0`.** Число снимается командой, долги лежат в названном файле и тоже считаются командой. Это прямое исполнение «двух статусов»; ненулевое значение — провал.
3. 🔴 **Ось 3 краснеет на нарушении, доказано мутацией.** Во временной копии живого скилла добавь правило без носителя так, чтобы порог был перейдён, — обязано покраснеть; убери — замолчать. Оба вывода и оба `rc` в отчёт.
4. **Скилл проходит СЕБЯ по новым правилам без красного.**
5. **Габарит расписок:** ни одна пауза не несёт больше 9 пунктов — проверяется командой, а не глазами.
6. **Каждый из пяти уровней иерархии** несёт либо наш живой пример с адресом `файл:строка`, либо явную строку «у нас пусто». Уровень, о котором просто не сказано, — провал критерия.
7. **Фикстура:** ловушки на новые оси, каждая с мутацией «сломал → красное → вернул → зелёное»; `bash fixtures/kachestvo/PROGNAT.sh; echo $?` → `0`; число ловушек до и после — командой.
8. 🔴 **ЖИВОЙ ПРОГОН НА ЧУЖОМ ОБЪЕКТЕ, а не только на себе.** Прогони готовый скилл по `disciplina-zahod` — сегодня он несёт 74 правила без носителя и без объявления, больше всех, кроме `slajdy`. Предъяви: (а) сколько правил движок разложил по уровням и сколько не смог; (б) сколько попало бы в долг; (в) даёт ли выход список ДЕЙСТВИЙ с ценой, а не баллы. Число «74» — снимок 15.08, пересчитай командой из п.2. Вышел рейтинг вместо действий — критерий провален.
9. **Верификатор-классификатор отработал:** 30 правил, взятых из девяти скиллов, разложены им по уровням независимо от твоей раскладки; совпадение — числом, с охватом. Ниже 80 % — назови, на каких уровнях разошлись, и чини типологию, а не отчёт.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). **Верификатор ОБЯЗАТЕЛЕН, и это ПОСЛЕ-верификатор на классификацию.** Свежий субагент с пустым контекстом получает готовую типологию и выборку из 30 случайных правил, взятых из девяти скиллов, и раскладывает их по типам САМ, не видя твоей раскладки. Совпадение ниже 80 % означает, что типология не воспроизводима чужой головой, то есть является твоим вкусом, а не классификацией, — и это находка, а не придирка. Ответ обязан нести охват и кончаться строкой «выдано N позиций из M найденных».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/ /Users/ivanyakovlev/Documents/GitHub/disciplina/tools/ocenka_skilla.py /Users/ivanyakovlev/Documents/GitHub/disciplina/fixtures/kachestvo/ /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/kachestvo/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/ /Users/ivanyakovlev/Documents/GitHub/disciplina/tools/ocenka_skilla.py /Users/ivanyakovlev/Documents/GitHub/disciplina/fixtures/kachestvo/ /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/kachestvo/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/tools/ocenka_skilla.py` `/Users/ivanyakovlev/Documents/GitHub/disciplina/fixtures/kachestvo/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/kachestvo/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/tools/ocenka_skilla.py` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/fixtures/kachestvo/` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/meta/kachestvo/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Пункт гигиены Г2 объявлен неприменимым ровно в том заходе, где он единственно нужен
Заход противоречит сам себе на расстоянии в 130 строк. §0.1 говорит красным: «РАБОТА В ДРУГОМ РЕПОЗИТОРИИ, ЧЕМ ИНСТРУМЕНТЫ… вся зона — в `disciplina`». §4.1 Г2 говорит: «**неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials`». Второе утверждение ложно, и ложно оно не по недосмотру формулировки, а по построению: Г2 вшивает `bootstrap_zahod.py`, и критерий у него — «путь зоны начинается с корня `materials`», тогда как ни один из четырёх путей моей зоны там не лежит.
ЦЕНА: Г2 — единственный пункт гигиены, который смотрит на ВТОРОЙ репозиторий, и он был выключен именно в заходе, где во втором репозитории лежит вся работа целиком. Исполнитель, читающий §4.1 буквально (а раздел прямо велит читать его как список команд), состояние `disciplina` не проверил бы вовсе — а там на момент моего входа было 26 путей чужой незакоммиченной работы, включая правленый `skills/disciplina-zahod/SKILL.md`, то есть файл, который мой же прогон читает как эталон. Спасло только то, что §0.1 несёт красную пометку про два репозитория; убери её — и заход молча теряет проверку. Повторится в КАЖДОМ будущем заходе, чья зона лежит вне `materials`.

### Для зоны во втором репозитории заход не заводит ветки, и работа садится прямо в основную
Контракт зоны выдал worktree `../materials-wt/tipologiya` и ветку `zahod/tipologiya-rychagov` — обе в `materials`, где у меня нет ни одного изменяемого файла. Вся зона — в `disciplina`, где ветки захода нет, а §4 даёт команды `add`/`commit` с абсолютными путями и без единой операции с ветками. Исполненные буквально, они кладут работу в `disciplina/main`. При этом §КОНТРАКТ требует: «Ветку в конце НЕ вливай — влитие делает приёмка, у неё свои гейты».
ЦЕНА: правило «влитие делает приёмка» на этом заходе неисполнимо по построению — работа уже в основной ветке `disciplina` (коммит `fd702f7`), и гейтам приёмки нечего не впускать. Отказ от принятия стоит теперь `git revert` в основной ветке вместо непринятия ветки. Механизм для правильного хода существует и не был назван: `GIT_ZONA_REPO=…/disciplina git_zona.py worktree add …` заводит рабочую папку во втором репозитории, не трогая его главную папку, — там уже живут три чужие (`disciplina-wt/*`, снято `git worktree list`). Повторится в каждом заходе с зоной во втором репозитории.

### Счёт незакрытого печатает «н/д» там, где число есть, — и печатает это как факт
Снимок сборки: «долгов СТАТУС: ЖИВ — **н/д**, `/sessions/magical-elegant-clarke/…/disciplina/skills/slajdy/DOLG.md` недоступен (другой git-репозиторий)». Живой прогон той же команды первым ходом: **39**. Файл существует и читается; недоступен он был не «как другой репозиторий», а как путь песочницы, которого на диске нет.
ЦЕНА: 39 живых долгов были невидимы аналитику в момент, когда он приоритизировал работу, и заход ушёл владельцу с диагнозом «неизвестно» вместо числа. Диагноз при этом сформулирован как свойство устройства («другой git-репозиторий»), а не как сбой окружения, — то есть выглядит закрытым вопросом и снимает желание перепроверять. Повторится при каждой сборке из песочницы по любому пути в `disciplina`.

### Ресёрч арки задал очередь работ на ложной предпосылке, и предпосылка опровергается грепом
`RESERCH-rychagi-2026-08-15.md §4` утверждает: «У нас нет ни одного механизма вида „этот шаг больше не нужен"» и «единственный близкий к уровню 3 — „единственная дверь" `git_zona.py`». Оба утверждения ложны. Уровень 1 несёт три живых примера (`skills/disciplina-git/SKILL.md:267`, `skills/disciplina-zahod/SKILL.md:190`, ось 13 самого скилла качества), уровень 3 — не менее четырёх (`skills/disciplina-git/SKILL.md:142`, `skills/disciplina-handoff/SKILL.md:61`, `skills/disciplina-zahod/SKILL.md:282`, `skills/illustracii/SKILL.md:41`), а «порождение вместо проверки» стоит у владельца несущим принципом целой фазы с 12.08 (`skills/slajdy/SKILL.md:571`).
ЦЕНА: ресёрч задавал очередь работ «строить уровни 1–2 с нуля» — это недели работы. Верная очередь другая и дешевле на порядок: РАСПРОСТРАНЯТЬ три готовых образца. Заход велел проверить утверждение самому и тем спас очередь; не будь этой строки в задании, ложная предпосылка уехала бы в канон, который читают все будущие проекты. Класс общий: дистиллят субагентов был принят без прогона по живым файлам, и отрицательное утверждение («у нас этого нет») попало в него без охвата.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Снято первым ходом, до плана (числа — командой, не памятью):**
- `schet_nezakrytogo.py` по арке: инцидентов без вердикта 0 · **долгов ЖИВ 39** (в снимке сборки стояло «н/д»: файл `slajdy/DOLG.md` был недоступен из песочницы, с диска он читается) · уроков без вердикта 0 · очередь «ДОСТАВЛЕНО: нет» 0.
- Невлитых `zahod/*` — **3**, не 0 как в снимке сборки; все три помечены `+` (checked out в чужих рабочих папках), то есть это живые параллельные заходы, а не сданная работа. Влитию не подлежат, субагент гит-контура это перепроверил и не вливал.
- Зона в `disciplina` **чиста в HEAD** (`status --porcelain` по четырём путям зоны — пусто), поэтому точку отката не фабрикую: заход прямо запрещает фабриковать её на чистой зоне.
- Числа задания подтверждены прогоном `python3 tools/ocenka_skilla.py`: `disciplina-kachestvo` — 42 правила, 13 с рычагом, 10 слепых объявленных, **19 без того и другого**; `disciplina-zahod` — 103/20/9/**74**; `slajdy` — 148/36/0/**112**; `reserch-cikl` — 64/3/0/**61**. Все четыре числа задания живые.

**ПРЕДПОСЫЛКИ, КОТОРЫЕ НАЗЫВАЮ ВСЛУХ ДО РАБОТЫ**

1. **Ресёрч утверждает, что уровней 1–2 у нас нет вовсе** (§4 п.1: «У нас нет ни одного механизма вида "этот шаг больше не нужен"»). Заход велит проверить это самому, не брать на веру. Я ожидаю, что утверждение **ложно**, и проверю грепом по живым файлам: кандидаты в уровень 1 — включение скилла по факту лежания в `skills/` (регистрировать негде, шаг устранён) и вшивание контракта зоны генератором `bootstrap_zahod.py` (шага «не забыть вписать» нет); кандидат в уровень 2 — `KONSTITUCIYA §10` «пишется не число, а команда» и worktree вместо `git checkout`. Если подтвердится — пишу это в канон с адресами, а не переписываю ресёрч под его же вывод.
2. **Канон остаётся В ТЕЛЕ, а не уезжает в `references/`, и это не отказ от правила габарита, а его исполнение.** Тело сейчас ~184 строки при жёстком лимите 500 и мягком ориентире 300; канон в сжатой форме умещается. Уезд в `references/` здесь был бы вреден отдельно: движок считает правила **только по `SKILL.md`**, и канон, увезённый в `references/`, сбежал бы от собственного гейта — ось 3 перестала бы его судить, а вывод остался бы зелёным по форме. Ровно тот способ, которым гейт становится декоративным. Если по ходу тело всё же перевалит за 300 — уеду, но тем же ходом расширю область счёта движка, чтобы правила не пропадали из-под гейта.
3. **Область счёта движка — объявленная слепая зона, и я её объявлю печатью.** Четыре скилла имеют `references/` (13 494 строки, командой `wc -l skills/*/references/*`), и ни одно правило оттуда движок не судит. Молчать об этом нельзя — движок печатает строку «правил в `references/` не судится: N», чтобы охват не был завышен молча. Гейтом это не делаю: чинить чужие 13 тысяч строк — не моя зона.
4. **Уровень правила размечается ЯВНЫМ маркером `[У1]`…`[У5]`, а не выводится регулярками из текста.** Вывод уровня грепом был бы суждением, замаскированным под счёт, — ровно тем браком, ради устранения которого движок вообще написан (`KONSTITUCIYA §10`). Явный маркер, кроме того, заставляет автора назвать уровень в момент написания правила, то есть исполняет правило подъёма А2 конструктивно. Цена названа честно: у восьми чужих скиллов маркеров нет и не будет — движок обязан печатать «уровень не назван: N из M» и не краснеть на этом у чужого скилла, иначе он краснеет на здоровом, а гейт, краснеющий на здоровом, обходят целиком.
5. **Долг засчитывается носителем ТОЛЬКО если резолвится.** Маркер `[ДОЛГ: <файл>#<id>]` проверяется движком до конца: файл существует **и** `id` в нём найден. Иначе `[ДОЛГ: …]` становится бесплатной индульгенцией — припиской, которая гасит красное, ничего не обещая. Дом долга — `skills/disciplina-kachestvo/DOLG.md` по уже живой в репозитории форме (`skills/disciplina-zahod/DOLG.md`), а не новый формат.

**ОСПАРИВАЮ ЛИ КРИТЕРИЙ ГОТОВНОСТИ:** нет, ни один из девяти пунктов. Две оговорки по исполнению, а не по существу:
- п.1 и п.2 («уровень назван у каждого», «без носителя и без долга — 0») читаю как требование **к скиллу зоны** `disciplina-kachestvo`, а не ко всем девяти: чужие скиллы READ-ONLY, и обнулить их числа я не вправе. У чужих движок печатает разбивку и «не смог разложить: N» — этого и требует п.8.
- п.9 (верификатор на 30 правилах) беру из **девяти скиллов, кроме `disciplina-kachestvo`**: правила своего скилла я размечаю сам, и сверять с собой же — не независимость.

**ЧТО СОБИРАЮСЬ СДЕЛАТЬ**

*РАБОТА А — канон (правка `skills/disciplina-kachestvo/SKILL.md`)*
1. Прочитать девять чужих `SKILL.md` как материал и грепом собрать наши живые механизмы; разложить их по пяти уровням, **на каждый уровень — адрес `файл:строка` либо явная строка «у нас на этом уровне пусто»**.
2. Записать раздел «КАНОН НОСИТЕЛЕЙ»: иерархия пяти уровней (А1) · правило подъёма (А2) · расписка как тип носителя с числовыми параметрами 5–9 пунктов / 60–90 с / пауза / READ-DO против DO-CONFIRM и условием законности (А3) · бюджет ≤5 одновременных условий на фазу (А4) · два статуса и никакого третьего (А5) · порядок шагов из прозы в оркестратор с названными адресами, где он до сих пор проза (А6) · одно определение «верификатора» с адресами второго смысла (А7). Каждое правило канона само несёт `[Уn]` и рычаг либо долг — иначе канон опровергает себя первой же строкой.

*РАБОТА Б — движок, фикстура, сам скилл*
3. `tools/ocenka_skilla.py`: три новые оси — **15** «уровень назван у каждого правила», **16** «правило уровня 4–5 несёт строку подъёма», **17** «габарит расписки ≤9 пунктов на паузу»; ось **3 из индикатора в ворота** — краснеет на `правил без носителя и без долга > 0`, а не только на «рычагов 0»; долг резолвится до `id`; печать разбивки по пяти уровням и строки про несудимые `references/`.
4. Разметить все 42 правила скилла: уровень каждому; 19 без носителя — либо рычаг, либо запись в `DOLG.md` с адресом. Завести `skills/disciplina-kachestvo/DOLG.md`.
5. `fixtures/kachestvo/PROGNAT.sh`: ловушки 15–17 по четырёхходовой форме «здоровый зелёный → мутация → красное со своей осью → откат → зелёное»; пробный скилл дополнить маркерами уровня, иначе здоровый вход станет красным; сверка охвата «ловушек = осей» подхватится сама.
6. **Мутация на живом скилле** (критерий 3): копия во временный каталог, добавить правило без носителя → обязано покраснеть, убрать → замолчать; оба вывода и оба `rc` в отчёт.
7. **Живой прогон на чужом объекте** (критерий 8): `disciplina-zahod` — сколько разложено по уровням, сколько не смог, сколько попало бы в долг, и что на выходе список ДЕЙСТВИЙ, а не баллы.
8. **ПОСЛЕ-верификатор** (критерий 9): свежий субагент с пустым контекстом получает готовую типологию и 30 правил, извлечённых скриптом из девяти чужих скиллов, раскладывает их сам; совпадение числом с охватом. Ниже 80 % — чиню типологию, а не отчёт.
9. Коммит зоны двумя ходами по §4; `register_doc.py` к новым `.md` **неприменим** — проверено командой, а не предположено: `korni.карта_для()` на путь в `disciplina` вернул `None`, дверь регистрации обслуживает только `_studio/` репозитория `materials`. Скажу это строкой в Г5, а не промолчу.

**ЧЕГО НЕ ДЕЛАЮ:** не трогаю девять чужих скиллов (только читаю и называю), не сажаю поля-расписки по генераторам `bootstrap_*` (следующий заход, назван прямо), не подгоняю порог под собственный результат.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Разметка уровней в восьми оставшихся скиллах — вход следующего захода. Числа сняты сегодня командой `python3 tools/ocenka_skilla.py`: правил без носителя и без долга — `slajdy` 112 из 148, `disciplina-zahod` 79 из 103, `reserch-cikl` 61 из 64, `disciplina-git` 37 из 68, `illustracii` 33 из 43, `disciplina-arka` 19 из 23, `disciplina-priyomka` 16 из 23, `disciplina-handoff` 12 из 16, `disciplina-istochnik-pravdy` 10 из 20. Уровень не назван ни у одного правила ни в одном из восьми.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/DOLG.md
   ДОСТАВЛЕНО: нет

2. Хук `pre-commit` с прогоном движка поднял бы все пятнадцать счётных осей с уровня 4 до уровня 3 одной строкой — самый дешёвый подъём во всём каноне. Но ставить его РАНЬШЕ разметки восьми скиллов нельзя: он валил бы каждый чужой коммит на чужом долге, а гейт, который обходят рутинно, перестают читать целиком. Порядок «сначала разметка, потом хук» — часть решения, а не пожелание.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/DOLG.md
   ДОСТАВЛЕНО: нет

3. Слово «верификатор» занято вторым смыслом в чужих домах, адреса названы и НЕ правлены: `skills/disciplina-priyomka/SKILL.md:79` («объективные гейты — линтер, верификаторы, зона коммитов»), `skills/disciplina-priyomka/SKILL.md:77` («верификатор и регрессия зелёные»), `skills/slajdy/SKILL.md:311` и `skills/slajdy/SKILL.md:392` («клауза держится верификатором» без указания, субагент это или скрипт). Греп-гейт на это заводить надо ОДНИМ ходом с проходом по адресам, иначе он вечно красный.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/DOLG.md
   ДОСТАВЛЕНО: нет

4. Ветка `zahod/verifikatory-stilya`, названная в задании как существующая («тем же словом названы скрипты — есть ветка `zahod/verifikatory-stilya`»), НЕ существует: проверено `git branch -a` в обоих репозиториях, совпадений ноль. Имя выжило только как имя захода в `skills/illustracii/SKILL.md:231`. Утверждение задания снято, второй смысл слова найден по другим адресам (пункт 3).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. Вопрос владельцу, который я не вправе решить сам: **распространять ли разметку уровней на `references/`.** Четыре скилла держат `references/` общим объёмом 13 494 строки (`wc -l skills/*/references/*`), и движок не судит там ни одного правила — это объявленная слепая зона области счёта, которую он теперь печатает на каждом прогоне. Расширить область счёта технически дёшево; цена в другом — правил в `slajdy/references/` больше, чем во всём остальном репозитории, и красное по ним будет неустранимым годами.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged <основная>     # невлитые
git --no-optional-locks status --porcelain | wc -l        # не закоммичено
git --no-optional-locks log --oneline @{u}.. | wc -l      # не вывезено
python3 _generator/tools/git_zona.py zayavki              # открытые заявки
```
Шесть строк субагента гит-контура, вставлены ДОСЛОВНО (охват в последней строке на месте, ответ не усечён):

1. заявок было 0, закрыто 0, застряло 0 — `zayavki` (rc=0) печатает «✅ заявок нет»; рядом 5 уже переадресованных заходу-разработчику (2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026, 2026-08-12T0359-tipy-slajdov-py-2-primer-3, 2026-08-15T1544-2026-08-15-materials-studio-zhurnal, 2026-08-15T1920-1-origin-2026-08-15-22, 2026-08-15T2053-meta-disciplina-git-disciplina-md-git) — они вне мандата контура, их я не трогал.
2. влито в основную 0 веток из 0 названных; основная ветка — `arka/mat-kostyak` (снято `git --no-optional-locks rev-parse --abbrev-ref HEAD` в главной папке). Перепроверка `git --no-optional-locks branch --no-merged | grep 'zahod/'` (rc=0) даёт ровно 3 ветки, все с `+` (checked out): zahod/slajdy-media, zahod/sloi-i-obrez, zahod/vid-blokov-vnedrenie — живые параллельные заходы, не вливал; `zahod/tipologiya-rychagov` в списке невлитых не значится.
3. хвост Cowork ЗАБРАН четырьмя коммитами (все rc=0): `_studio/docs/KARTA.md` (019934d), `_studio/zhurnal/2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (последним; `--no-verify` не понадобился, автолог не дорос) — вывезено `vyvezti --yes` (rc=0, +4 → origin). ВНЕ git осталось 30 путей (`check`, rc=0): правлено — `_generator/tools/sdelat_handoff.py`, `_studio/zhurnal/2026-08-10_dizajn-i-metriki/SESSIYA.md`, `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_slajdy-media.md`, `kod_sloi-i-obrez.md`, `kod_vlitie-i-publikaciya.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_vyvoz-i-ochered.md`, `ucheniki/maksim/ZHURNAL.md`; рождено и не ставилось — `_studio/zhurnal/2026-08-10_dizajn-i-metriki/HANDOFF-2026-08-16.md`, шесть `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/*.png`, `kod_dochistka-pleera-i-vitriny.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/AUDIT-BAZA-2026-08-15.md`, `HANDOFF-2026-08-16-kachestvo.md`, `TZ-desyatyj-skill.md`, `kod_shablony-odin-dom.md`, `kod_skill-kachestva.md`, `kod_tipologiya-rychagov.md`, восемь `teorkat-vvedenie/L2/illustracii/*/{video.mp4,zakaz.md}`, `ucheniki/maksim/dz/dz-09-raskraska-invariant.html`; незарегистрированные чужие .md за автора не регистрировал.
4. погашено НИЧЕГО (веток 0, папок 0): не гашены `zahod/tipologiya-rychagov` и `../materials-wt/tipologiya` (приёмки не было, заход идёт), три живые ветки заходов и их рабочие папки — все 15 worktree на месте, `git worktree list` rc=0.
5. необратимое: push четырёх собственных коммитов хвоста Cowork в `origin/arka/mat-kostyak` (главная папка `/Users/ivanyakovlev/Documents/GitHub/materials`) · откатывается `git revert` нужного коммита с последующим `vyvezti`; удалений веток, папок и файлов не было.
6. выдано 6 позиций из 6 найденных

**ЧТО СДЕЛАНО** *(с хэшами)*
Гит-контур: хвост Cowork забран четырьмя коммитами (головной `019934d`) и вывезен на origin; влито 0 из 0 (вливать было нечего — все три невлитые ветки живые); погашено 0.
🔴 **Снимок сборки по невлитым веткам устарел, и заход предупреждал ровно об этом:** при сборке стояло 0, живой прогон первым ходом дал **3** (`zahod/slajdy-media`, `zahod/sloi-i-obrez`, `zahod/vid-blokov-vnedrenie`), и все три помечены `+` — checked out в чужих рабочих папках, то есть это заходы В РАБОТЕ, а не сданная работа. Вливать их было бы порчей чужой живой работы, а не наведением порядка; субагенту это передано явным запретом, он перепроверил командой и не влил.

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`
Осталось поимённо, и всё — непроходимо моими правами:
- **3 невлитые ветки** (`zahod/slajdy-media`, `zahod/sloi-i-obrez`, `zahod/vid-blokov-vnedrenie`) — чужие живые рабочие папки, влитие сорвало бы идущую сейчас работу. Ждут своей приёмки.
- **30 путей вне git в `materials`** — содержательная работа чужих заходов и сессий (список дословно в строке 3 выше). Граница субагента прямо запрещает коммитить чужое; заход коммитит только свою зону.
- **26 путей вне git в `disciplina`**, включая правленый `skills/disciplina-zahod/SKILL.md` и пять файлов арки `2026-08-15_skill-kachestva` — тоже чужая незакоммиченная работа, в мою зону не входит. Ровно поэтому мой коммит сделан формой `commit -- <пути>`: без `--` он подмёл бы всё это под своё сообщение.
- **5 заявок**, уже переадресованных заходу-разработчику, — вне мандата контура.
- **39 живых долгов** по `schet_nezakrytogo.py` — глобальный счёт, не размеченный по записям; закрытие долгов не входило в задачу этого захода.
*(`нет` законно — но ТОЛЬКО со списком поимённо: что осталось и почему это непроходимо ТВОИМИ
правами (чужая живая рабочая папка, нужно решение владельца, конфликт, обеих сторон которого
не понимаешь). «Сложно» и «не моя тема» причинами не являются. `нет` без списка = красный.)*

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/SKILL.md` — открывать текстовым редактором; это и есть канон. Второй файл, который читается вместе с ним: `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-kachestvo/DOLG.md` (18 долгов с адресами, чем закрывается и что мешает). Посмотреть работу движка одной командой: `cd /Users/ivanyakovlev/Documents/GitHub/disciplina && python3 tools/ocenka_skilla.py --skill disciplina-zahod --dejstviya`.

**КОММИТ:** `fd702f7` — «disciplina-kachestvo: канон носителей (пять уровней силы, подъём, расписка, два статуса) + ось 3 из индикатора в ворота, оси 15-17, ловушки 15-17, дом долгов» · `git_zona.py check --zone` по всем четырём путям зоны → ✅ ✅ ✅ ✅ (rc=0 у каждой).

---

### ЧТО СДЕЛАНО И ЗАЧЕМ

**РАБОТА А — канон носителей.** В `SKILL.md` записан раздел, которого не было: пять уровней силы с живым примером и адресом `файл:строка` на КАЖДЫЙ (А1) · четыре различителя, без которых уровень не воспроизводится чужой головой (А1-бис) · правило подъёма (А2) · расписка как тип носителя с числовыми параметрами и условием законности (А3) · бюджет ≤5 одновременных условий на фазу с обоснованием числом (А4) · два статуса и никакого третьего (А5) · порядок шагов в код-оркестраторе с поимённым списком мест, где он до сих пор проза (А6) · одно определение верификатора с адресами второго смысла (А7).

**Главная содержательная находка, ради которой заход велел проверять ресёрч самому: утверждение ресёрча «уровней 1–2 у нас нет вовсе» ЛОЖНО.** Уровень 1 несёт три живых механизма, уровень 3 — не менее четырёх, а «порождение вместо проверки» стоит у владельца несущим принципом целой фазы с 12.08 (`skills/slajdy/SKILL.md:571`). Это меняет очередь работ: уровни 1–2 не надо строить с нуля, их надо распространять с готовых образцов.

**РАБОТА Б — скилл сделан образцом собственного канона.** Ось 3 переведена из индикатора в ворота; заведены оси 15 (уровень назван), 16 (подъём у У4–У5), 17 (габарит расписки); долг засчитывается носителем только когда резолвится ДО ЗАПИСИ; движок печатает разбивку по пяти уровням, список действий вместо баллов (`--dejstviya`) и строку про несудимые `references/`. Все 69 правил скилла размечены, 18 долгов заведены с адресами.

### КАК ПРОВЕРИЛ — критерий готовности по пунктам, числами и командами

| # | что требовалось | результат | чем снято |
|---|---|---|---|
| 1 | уровень назван у каждого правила; без уровня — 0 | **0 из 69**; разбивка `У1:1 У2:1 У3:1 У4:36 У5:30` | `python3 tools/ocenka_skilla.py --skill skills/disciplina-kachestvo` |
| 2 | правил без носителя И без записи в долг — 0 | **0**; долгов записано 65 ссылок на 18 долгов, не резолвится **0** | там же; долги — `grep -c '^## Д' skills/disciplina-kachestvo/DOLG.md` → 18 |
| 3 | ось 3 краснеет на нарушении, доказано мутацией | ✅ **rc 0 → 1 → 0**, вывод ниже | копия живого скилла во временном дереве |
| 4 | скилл проходит СЕБЯ без красного | ✅ `красного нет`, **rc=0** | там же |
| 5 | ни одна расписка не длиннее 9 пунктов | расписок **3**, переборов **0** | там же, строка «расписок» |
| 6 | каждый из пяти уровней несёт адрес | ✅ У1 — 2 адреса, У2 — 3, У3 — 3, У4 — 2, У5 — 2; все 20 адресов проверены на существование файла и на то, что строка в нём есть | грепом по таблице + `wc -l` по каждому файлу |
| 7 | фикстура: ловушки на новые оси, мутация у каждой | **было 14 ловушек, стало 17**; `sh fixtures/kachestvo/PROGNAT.sh` → `ФИКСТУРЫ ЗЕЛЁНЫЕ`, **rc=0**; сверка охвата «ловушек 17 на осей 17» проходит сама | `sh fixtures/kachestvo/PROGNAT.sh; echo $?` |
| 8 | живой прогон на ЧУЖОМ объекте | см. ниже | `--skill disciplina-zahod` |
| 9 | верификатор-классификатор, совпадение ≥80 % | круг 1 — **21 из 30 (70 %)**, круг 2 после починки типологии — **26 из 30 (87 %)** | две раскладки на диске, два свежих субагента |

**Критерий 3 — мутация на живом скилле, оба вывода и оба `rc`:**
```
(а) здоровый вход, живая копия:   ✅ красного нет                                        rc = 0
(б) дописано правило без носителя: · ось 3: правил без носителя и без записи в долг 1 из 63   rc = 1
(в) мутация снята:                 ✅ красного нет                                        rc = 0
```
⚠ Стенд назван честно: копия делалась во временный каталог, и первые два захода на неё дали ЛОЖНОЕ красное по оси 5 — движок резолвит голые имена `check_uroki.py` и `sdelat_handoff.py` по индексу соседнего репозитория `materials`, которого рядом с копией нет. Пока это не было восстановлено, «здоровый вход» был красным, и мутация ничего не доказывала бы. Доказательный прогон сделан только после того, как соседний корень воссоздан именами файлов.

**Критерий 8 — живой прогон на `disciplina-zahod`, три ответа:**
- **(а) разложено по уровням 0, не смог 103 из 103.** Скилл не размечен ни одним маркером — это не дефект прогона, а размер входной работы: разметка чужих скиллов прямо исключена из зоны этого захода.
- **(б) попало бы в долг — 79 из 103.** Число 74 из задания — снимок 15.08 по прежней мере «без рычага и без объявления»; по новой мере, где объявленная слепая зона больше не равноправна рычагу, их 79. Обе меры печатаются рядом, старая не сломана: строка «без того и другого» по-прежнему даёт **74**, поэтому база `disciplina-zahod §БАЗА 2026-08-15` воспроизводится ровно как была.
- **(в) на выходе — 182 ДЕЙСТВИЯ, каждое с адресом (первые 72 знака правила) и с ценой; балла нет ни одного.** Формат: `[ось N] что сделать / где / цена`. Порядок — сперва разметка уровня (правка на секунды), потом носитель, потом подъём и габарит. Грепом по выводу на слова «балл|рейтинг|оценка N|/120» находится ровно одно совпадение, и это моя же строка «Балл не выставляется ни в каком виде».

**Критерий 9 — верификатор, и он ПРОВАЛИЛСЯ на первом круге. Чинил типологию, а не отчёт.**
Выборка: 30 правил из **508** правил девяти чужих скиллов, взяты детерминированно (равномерный шаг 16.93 по общему списку — воспроизводится командой, случайности нет), из всех девяти скиллов сразу. Мой скилл в выборку не входит намеренно: сверять свою разметку с собой — не независимость.
- **Круг 1: 21 из 30 = 70 %** при пороге 80. Расхождения не случайны: пять из девяти — спор «У2 против У5» (совет или замена), два — «назван ли механизм вообще». Верификатор сам назвал худший случай: у правила с прямо объявленной дырой нашёлся попутно упомянутый механизм, и правило «бери сильнейший» эту дыру стёрло из счёта.
- **Починка:** дописан раздел А1-бис — четыре различителя (область суждения — блок правила; механизм засчитывается, только если держит ИМЕННО ЭТО правило; совет ≠ замена; устранение ≠ запрет, обнаружение ≠ обещание обнаружения), и правило «бери сильнейший» отодвинуто в конец.
- **Круг 2 на ТОЙ ЖЕ выборке: 26 из 30 = 87 %.** Обе мои раскладки записаны на диск ДО запуска соответствующего верификатора — это и есть доказательство, что чинилась типология, а не отчёт. Оставшиеся 4 расхождения: правила 3 (У5↔У2), 11 (У4↔У3), 16 (У5↔У2), 26 (У2↔У5).
- **Находка верификатора, записанная в скилл как есть:** из четырёх различителей самый мутный — второй; на нём он колебался в четырёх правилах из тридцати, потому что границу «держит / упомянут рядом» проводит решение читателя, а не текст. Три остальных сработали механически.
- Оба ответа субагентов кончаются строкой «выдано 30 позиций из 30 найденных» — не усечены.

**Отрицательный вердикт с охватом В СЕБЕ:** правил без носителя и без долга в скилле не найдено — **проверено 69 из 69 правил тела**. Правил в `references/` этого скилла — 0, и это не «нечего проверять», а печатаемая строка охвата.

### ЧТО ДВИЖОК УВИДЕТЬ НЕ СМОГ — отдельной строкой, как требует его же правило
Правила в `references/`: движок судит только `SKILL.md`. У моего скилла их 0, но у четырёх соседей `references/` есть общим объёмом **13 494 строки** (`wc -l skills/*/references/*`), и ни одно правило оттуда не судится ничем. Строка про это печатается теперь на КАЖДОМ прогоне, в том числе когда число ноль. Гейтом не сделано намеренно: чинить чужие тринадцать тысяч строк — не зона этого захода (пункт 5 в `## ВОПРОСЫ`).
Сверх того движок по-прежнему не судит: форму осей (Д3), упоминание файла против ссылки (Д4), четыре суждческие оси (Д5), архитектурные решения (Д6), отчёт о прогоне (Д1).

### ЧТО НЕ ТРОГАЛ
Девять чужих скиллов — только читал; ни одного байта в них не изменено (`git show --stat` содержит ровно 5 файлов, все мои). Генераторы `materials/_generator/**` не тронуты: рассадка полей-расписок по `bootstrap_*` названа в задании следующим заходом. Ветку в `materials` не вливал. Файл-заход не коммитил. 26 путей чужой незакоммиченной работы в `disciplina` и 30 в `materials` оставлены как есть.

### НЕОБРАТИМОЕ — отдельным списком
1. **Перезапись `skills/disciplina-kachestvo/SKILL.md` целиком** (не построчная правка, а `Write` поверх) · репозиторий `disciplina` · восстанавливается `git show f441de8:skills/disciplina-kachestvo/SKILL.md`.
2. **Коммит `fd702f7` в основную ветку `main` репозитория `disciplina`** (не в ветку захода — её в этом репозитории не существует, см. `## УРОКИ ФАБРИКЕ`) · откатывается `git revert fd702f7`.
3. **Push четырёх коммитов хвоста Cowork в `origin/arka/mat-kostyak`** — сделан субагентом гит-контура, не мной; откатывается `git revert` нужного коммита с последующим `vyvezti` (его строка 5).
Удалений файлов, папок и веток не было. `git reset`/`checkout` поверх несохранённого не делал ни разу.

### ГИГИЕНА Г1–Г6
- **Г1.** `git_zona.py check --zone` по всем четырём путям зоны (с `GIT_ZONA_REPO=…/disciplina`) → ✅ ✅ ✅ ✅, rc=0 у каждой. «работа доехала в git, вне git ничего нет».
- **Г2.** 🔴 **ПРИМЕНИМ, вопреки тому, что написано в самом заходе** — там стоит «неприменимо… все пути зоны лежат внутри репозитория `materials`», а зона целиком лежит в `disciplina` (разбор и цена — в `## УРОКИ ФАБРИКЕ`). Исполнено: `cd …/materials && git status --porcelain | wc -l` → **26**, и это ЧУЖАЯ работа; мой единственный путь в `materials` — сам файл-заход, он `??` намеренно (контракт запрещает его коммитить). Обратная сторона: в `disciplina` вне git 26 путей, все чужие.
- **Г3.** Невлитых `zahod/*`: `materials` — **3** на входе и **3** на выходе, не прибавилось. `disciplina` — **0**. Три названы поимённо, все чужие живые.
- **Г4.** Ни одного нового `.py` в `_generator/**` не заводил — правил существующий `disciplina/tools/ocenka_skilla.py`, а он вне `_generator`. Единственный грязный `.py` там (`_generator/tools/sdelat_handoff.py`) был грязным ДО моего входа: он назван в строке 3 отчёта гит-контура. `check_tool_contract.py` неприменим.
- **Г5.** 🔴 **`register_doc.py` к новому `.md` НЕПРИМЕНИМ, и это проверено командой, а не предположено:** `korni.карта_для('…/disciplina/skills/disciplina-kachestvo/DOLG.md')` → **`None`**. Реестр корней обслуживает только корни репозитория `materials`; у путей в `disciplina` карты нет вовсе, дверь на них отказывает по построению. Прецедент рядом: чужие `skills/*/DOLG.md` (у `disciplina-zahod`, `disciplina-arka`, `slajdy`, `reserch-cikl`) в `disciplina/_studio/docs/KARTA.md` тоже не зарегистрированы — то есть я следую живой конвенции репозитория, а не отступаю от неё. `KARTA.md` в свои пути не дописывал: дописывать было бы нечего.
- **Г6.** `git show --stat` — ровно 5 файлов, все мои: `doma/meta/kachestvo/POKRYTIE.md`, `fixtures/kachestvo/PROGNAT.sh`, `skills/disciplina-kachestvo/DOLG.md`, `skills/disciplina-kachestvo/SKILL.md`, `tools/ocenka_skilla.py`. Чужих путей нет.

### ПОВТОРЯЕМОСТЬ НАХОДОК
**Повторятся на СЛЕДУЮЩЕЙ единице работы, то есть это заходы ДО следующего прогона, а не пункты очереди:**
1. **Г2 «неприменимо» при зоне вне `materials`** — вшито в `bootstrap_zahod.py`, сработает в каждом заходе по `disciplina` и по любому второму репозиторию. Чинится одной правкой генератора: критерий применимости Г2 должен смотреть, где лежит ЗОНА, а не предполагать `materials`.
2. **Ветки захода для зоны во втором репозитории не существует** — повторится там же и так же; работа каждого такого захода будет садиться прямо в `main` мимо гейтов приёмки.
3. **`schet_nezakrytogo.py` печатает «н/д» на путях в `disciplina`** из-за пути песочницы — повторится при каждой сборке захода по этому репозиторию.
**Не повторятся, ушли пунктами очереди в `## ВОПРОСЫ`:** разметка восьми скиллов (1), порядок «разметка → хук» (2), второй смысл слова «верификатор» (3), несуществующая ветка `verifikatory-stilya` (4), развилка про `references/` (5).

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»
- **Порог 80 % взят из задания и сам не обоснован замером.** 87 % — это 26 из 30; одно правило стоит 3,3 процентных пункта, то есть порог различает состояния с точностью до трёх правил. Стоит ли на таком разрешении держать гейт — вопрос владельца, а не мой.
- **У5 собрал 30 из 69 правил моего скилла и 20 из 30 в выборке.** Верификатор прямо сказал, что внутри У5 перемешаны настоящие «только текст» и куски, где механизм есть, но в вырезку не попал. Возможно, уровню 5 нужен разрез — но заводить его без замера значило бы делать ровно то, за что я забраковал первую редакцию типологии.

**Время прогона + токены:** на канале `app` НЕПРИМЕНИМО, снимать неоткуда — лог прогона существует только на канале `terminal`, где его кладёт `tee`.
