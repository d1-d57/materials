# Канал исполнителя — chislo-i-kirillica (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — обе правки требуют РАЗЛИЧЕНИЯ близких случаев (шелл против питона, снимок против факта), а не механики: сторож, красящий всё подряд, отключат в тот же день.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — сторож должен различать близкие случаи, а не красить всё подряд.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/chislo-i-kirillica.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_chislo-i-kirillica.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add chislo-i-kirillica --branch zahod/chislo-i-kirillica, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-14_rychagi-zahodov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Фабрика заходов: генератор собирает задание исполнителю, гейты судят вернувшийся отчёт. Прошлый этап: заход `gejty-ne-tuda` закрыл пять гейтов, судивших не то, что заявляли; остались 37 записей без рычага, разобранные аналитиком на шесть семей (`~/Documents/GitHub/disciplina/doma/zahody/RAZVITIE.md`). ЦЕЛЬ: две самые дешёвые семьи — А (число стареет между замером и применением) и Д (кириллический диапазон в regex).
Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели: получишь два сторожа и правдивые записи дома, но НЕ разбор остальных четырёх семей.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-14) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Семья A: генератор пишет КОМАНДУ вместо числа — снимок при сборке заменяется строкой «сними первым ходом», потому что чинится корень, а не симптом
2. Семья D: сторож кириллицы в regex ставится И в коде инструментов, И в грепах внутри файлов-заходов — оба места дали живые инциденты

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/chislo-i-kirillica`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/chislo-i-kirillica`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add chislo-i-kirillica --branch zahod/chislo-i-kirillica
  cd ../materials-wt/chislo-i-kirillica
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --vlit zahod/gejty-ne-tuda --zone _generator/tools/ --kommitit 'зону _generator/tools/ целиком, включая фикстуры' --zakryt 'свою ветку после влития — гасит приёмка, не заход'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

Что влить, названо аналитиком и уехало в задание субагента (1): `zahod/gejty-ne-tuda`. Влитие идёт в ОСНОВНУЮ ветку, не в твою.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/chislo-i-kirillica`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Две семьи инцидентов, обе разобраны аналитиком до тебя. Обе в твоей зоне. Порядок — А, потом Д; застрял на одной, переходи ко второй и опиши застрявшую строкой отчёта.

### СЕМЬЯ А. Генератор вшивает в заход ЧИСЛО, а не команду, которая его считает

**Точный адрес, найден и проверен чтением кода:** `_generator/tools/bootstrap_zahod.py`, функция `_sostoyanie_nevlityh()` (искать по имени, не по номеру строки). При `invlitye == []` она пишет в §0.1 файла-захода дословно: *«вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0»*.

**Цена, замерена на живом заходе 14.08.** Заход `gejty-ne-tuda` нёс эту строку, а к моменту прогона невлитых веток было ТРИ (`vynos-2`, `kanon-put`, `pravila-kadra`), и на них стояла блокирующая заявка. Файл противоречил сам себе: раздел «ЧТО ФИНАЛИЗИРОВАНО» требовал влить две ветки, §0.1 говорил «нечего». Субагент разрешил противоречие командой и влил все три — но его же задание велит «не судить, годна ли чужая ветка: список просужен аналитиком». Поверь он тексту — лестница невлитого выросла бы заново, молча.

**Что чинить.** Счётное утверждение о состоянии репозитория генератор больше не подаёт как ФАКТ. Форма: **команда, которую исполнитель гонит первым ходом**, плюс — если снимок полезен — он помечен временем и словом «снимок при сборке, проверь». Читатель должен видеть, что число могло устареть, БЕЗ чтения кода генератора.
⚠ **Не только эта функция.** Тем же снимком печатается блок «СЧЁТ НЕЗАКРЫТОГО» в шапке захода (`инцидентов без вердикта: 0` и три соседние строки) — пройди по генератору и найди ВСЕ места, где в файл уезжает посчитанное при сборке число. Сколько их — установи прогоном/грепом и назови числом в отчёте; я насчитал два, но своё число не проверял на полноту.
🔴 **Чего НЕ ломать:** четыре различимых исхода слота влития (ветки названы · невлитых нет · невлитые есть, но не названы · сторож не отработал) обязаны остаться различимыми — это объяснено в докстринге самой функции, прочти его прежде чем править. Схлопнешь их — «аналитик забыл назвать ветки» станет неотличимо от «вливать нечего».

**Клауза линтера входа.** `check_zahod.py` уже несёт З6 (число рядом с командой) — она судит СОСЕДСТВО команды, а не ВОЗРАСТ замера, поэтому состарившийся снимок её не будит (проверено прогоном заходом `rychagi-3`). Добавь то, чего не хватает: утверждение о состоянии репозитория, поданное числом без команды и без метки времени рядом, — красное. Порог возраста не выдумывай: метка времени либо есть, либо нет.

### СЕМЬЯ Д. Кириллический диапазон в regex — ложный результат или падение, смотря по locale

**Замер приёмки 14.08, воспроизводится в три строки:**
```
printf 'Привет\nмир\n' > /tmp/k.txt
LC_ALL=C      grep -cE '[А-Я]' /tmp/k.txt     # печатает 2 — обе строки, побайтовый матч, ЛОЖЬ
LC_ALL=C.UTF-8 grep -cE '[А-Я]' /tmp/k.txt    # grep: Invalid collation character — ПАДЕНИЕ
python3 -c "import re; print(re.findall(r'[А-Я][а-я]+','Привет Мир'))"   # ['Привет','Мир'] — ВЕРНО
```
То есть в шелле диапазон даёт либо ложь, либо падение, а в питоновском `re` работает корректно. **Сторож обязан различать эти два случая** — иначе он зашумит каждый питоновский regex и его отключат.

🔴 **Записи дома врали, и это часть задачи.** КРТ-033 и КРТ-РАЗН-21 числятся НАКРЫТЫМИ рычагом `check_tool_contract.py — переносимость, GNU-измы краснеют`. Приёмка прогнала этот гейт на файле с `re.findall(r'[А-Я][а-я]+')` и `grep -cE "[А-Я]-[а-я]"`: **про кириллицу он не сказал ни слова** — он ловит `sed -i`, `date -d`, `xargs`, то есть GNU-измы. Рычаг назван, а на дефекте молчит.

**Что чинить — два места, оба дали живые инциденты:**
1. **`check_tool_contract.py`** — кириллический диапазон в ШЕЛЛ-контексте краснеет: в `.sh`, в аргументах `subprocess`, в строках, уходящих в `grep`/`sed`/`awk`. Чистый питоновский `re` — не красить (проверено выше, он корректен); если решишь красить и его — обоснуй в отчёте прогоном, а не соображением.
2. **`check_zahod.py`** — тот же диапазон в греп-критерии ВНУТРИ файла-захода. Цена этого места оплачена: критерий, заданный кириллическим классом, даёт ложный ноль, а ложный ноль в критерии читается как «дефектов нет» и стоит целого прохода.

Обеим правкам — ловушка в своей фикстуре (`fixtures/tool_contract/`, `fixtures/zahod/`): дефект краснеет, чистый файл — нет.

### Порядок и стоп до цели
А → Д. **Ни одна правка не должна менять поведение заходов** — только правдивость гейтов и форму счётных утверждений. Обнаружишь по ходу третью семью — в отчёт, не трогай.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. **Живой прогон на реальном объекте:** собери пробный заход `bootstrap_zahod.py` в одноразовую арку — в готовом файле **ни одного счётного утверждения о состоянии репозитория без соседней команды или метки времени** (проверь грепом, число назови). Пробные файлы за собой убери.
2. Четыре исхода слота влития по-прежнему различимы: покажи прогоном минимум два разных (например «ветки названы» через `--vlit` и «невлитых нет»).
3. `check_zahod.py` на заходе с голым числом → **rc=1**; на нормальном (`kod_gejty-ne-tuda.md`, лежит в этой же арке) → красных по новой клаузе **0**.
4. `check_tool_contract.py` на дефектном файле с кириллическим диапазоном в шелл-контексте → **rc≠0 и в тексте названа кириллица**; на живых инструментах `_generator/tools/*.py` новых красных — **0** (назови, сколько файлов прогнал).
5. `check_zahod.py` на заходе с греп-критерием `[А-Я]` → **rc=1**.
6. Фикстуры: `sh _generator/tools/fixtures/tool_contract/PROGNAT.sh` и `sh _generator/tools/fixtures/zahod/PROGNAT.sh` → **rc=0**, число ловушек у обеих выросло (шапки его больше не хранят, прогон печатает сам).
7. Записи КРТ-033 и КРТ-РАЗН-21 в `~/Documents/GitHub/disciplina/doma/zahody/POKRYTIE.md §2` — рычаг переписан на построенный тобою носитель, а не оставлен натянутым. Это ДРУГОЙ репозиторий: коммить его отдельно и хэш назвать отдельно.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Не нужен — операция не лоссовая. Его роль играют пункты критерия: каждый прогоняется командой на живом файле.
⚠ **Одно исключение, где самопроверка врёт по построению:** пункт 4. «Гейт краснеет на моём дефекте» и «гейт ловит класс» — разные утверждения; ровно на этой разнице сегодня и попались две записи дома. Прогони новый сторож на ЧУЖОМ дефекте — например на строке из чужой фикстуры или из живого `.sh`, который ты не писал.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Стартовое сообщение владельцу и §0.1 предписывают РАЗНЫЙ первый ход, и оба сказаны словами «первым ходом»
ЦЕНА: два лишних хода на этом заходе. Стартовая строка велит `worktree add` ПЕРВЫМ ходом, §0.1 велит первым ходом субагента-контур и объясняет, почему папка обязана заводиться ПОСЛЕ влития (ветка отпочковывается от уже влитой основной). Исполнитель, читающий стартовое сообщение раньше файла — а он читает его раньше по построению, — заводит папку от УСТАРЕВШЕЙ основной. Лечение стоило снятия рабочей папки, повторного `worktree add` и влития основной в свою ветку с `--vsyo-ravno` (основная вычекаучена в главной папке, инструмент законно упирается). Оба текста печатает один генератор, `startovoe_telo()` и `git_kontur_blok()`, — расхождение чинится в нём, а не памятью исполнителя.

### Критерий готовности требовал молчания гейта на файле, который сам несёт этот дефект
ЦЕНА: исполненный буквально, пункт 3 дал бы сторожа, молчащего на своём классе, — то есть новую натянутую запись в `POKRYTIE.md` вместо рычага. Ровно тот дефект, который вторая половина этого же захода и чинит (КРТ-033: «рычаг назван, а на дефекте молчит»). Механизм общий, а не разовый: эталоном «здорового» назван ЖИВОЙ файл, собранный СТАРЫМ генератором, а правка меняет как раз то, что генератор пишет. Всякий раз, когда заход чинит генератор, «нормальный» файл прошлой сборки перестаёт быть эталоном — и его нельзя брать отрицательным контролем, не проверив, что дефекта в нём нет.

### `check_tool_contract.py --all` исполняет `--help` живых инструментов, и один из них ПИШЕТ в проверяемый репозиторий
ЦЕНА: после каждого прогона `--all` в зоне появляется `_generator/tools/fixtures/sborka/obratnyj-progon/koridor-proba/slajdy/proba/slaid.md` (его создаёт `koridor_validacia.py`, у которого `--help` не возвращается за 10 с — линтер сам это и печатает). На этом заходе файл дважды оказался в рабочем дереве и один раз доехал до ИНДЕКСА вместе с зоной: поймано `git status --porcelain` перед коммитом, иначе уехал бы в коммит как чужая работа. Урок 1.7 фикстуры `tool_contract` («гейт не смеет писать в проверяемый репозиторий») нарушает сам гейт — в режиме `--all`, где он единственный раз запускает процесс.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

> Прочитано ДО плана: только названные заходом файлы — `bootstrap_zahod.py`
> (`_sostoyanie_nevlityh`, `git_kontur_blok`, `subagent_zadanie`),
> `schet_nezakrytogo.py`, `check_zahod.py` (З6/З14 и списки клауз),
> `check_tool_contract.py` (каркас `CHECKS`), обе фикстуры-шапки,
> `POKRYTIE.md §2`. Проект не изучал.

### 0. Порядок §0.1 против стартового сообщения — расхождение, названное вслух
Стартовое сообщение владельца велит ПЕРВЫМ ходом `worktree add`; §0.1 захода
велит ПЕРВЫМ ходом субагента-контур и **только потом** рабочую папку, и объясняет
почему (ветка обязана отпочковаться от УЖЕ влитой основной). Я исполнил
стартовую строку буквально и папка завелась ДО влития. Лечение: после доклада
субагента снимаю папку `git_zona.py worktree drop` и завожу заново — на тот
момент в ней ни одной моей правки, потеря нулевая, а ветка получается
отпочкованной от свежей основной, как того требует §0.1. Урок фабрике об этом —
в своей секции.

### 1. Семья А — где именно врёт число
Мест, где посчитанное при сборке число уезжает в файл, я вижу **два**
(число проверю грепом по генератору и назову в отчёте — заход сам просит не
верить его «двум» на слово):
- `bootstrap_zahod.py::_sostoyanie_nevlityh()` — ветка `invlitye == []`:
  «вливать нечего, проверено командой … невлитых веток 0». Плюс ветка
  `invlitye != []` печатает `{n}` и поимённый список — это тот же снимок.
- `schet_nezakrytogo.otchet()` — четыре строки «СЧЁТ НЕЗАКРЫТОГО» в шапке.
  Инструмент зовут ДВОЕ: `bootstrap_zahod.py` (уезжает в файл — снимок) и
  `zakryt_sessiyu.py` (печать в консоль — там число свежее по построению).
  Поэтому метку времени добавляю ПАРАМЕТРОМ, а не безусловно: назвать «снимком»
  живую печать было бы такой же ложью, только в другую сторону.

**Форма правки (одна на оба места):** число остаётся, но рядом встают (а) дата
снимка и слово «снимок при сборке, проверь» и (б) КОМАНДА, которой читатель
пересчитывает его первым ходом. Четыре исхода слота влития остаются четырьмя —
трогаю только текст исхода «невлитых нет» и исхода «невлитые есть»; ветки
`vlit_vetki` и `invlitye_err` не касаюсь.

**Клауза линтера — З18** (З17 занята, проверено грепом `def check_z1[4-9]`).
Судит не соседство backtick (этого мало: в шапке рядом со «СЧЁТОМ» стоит абзац
КОНТЕКСТ с backtick-именами веток, и любой такой сторож на нём молча зазеленеет),
а: счётное утверждение о состоянии репозитория → в окне обязана быть либо
исполнимая команда (`python3 …`/`git …`/`.py`), либо метка времени. Порога
возраста не ввожу — метка либо есть, либо нет.

### 2. 🔴 Оспариваю КРИТЕРИЙ ГОТОВНОСТИ, пункт 3 — он невыполним по построению
«`check_zahod.py` на нормальном (`kod_gejty-ne-tuda.md`) → красных по новой
клаузе 0» требует молчания на файле, который САМ несёт ровно тот дефект, ради
которого клауза заводится: строки 32–38 этого файла — «инцидентов без вердикта
: 0», «уроков фабрике без ВЕРДИКТ : 0», «пунктов очереди … : 0» без команды и
без метки времени. Он собран СТАРЫМ генератором, до правки. Сторож, молчащий на
нём, молчит и на всём классе — то есть пункт 3 в нынешнем виде требует построить
сторожа, который не работает.

**Поправка, по которой и буду работать** (та же архитектура, что уже принята в
этом линтере для З10–З13 — «объявленное исключение + живой положительный
контроль»):
- отрицательный контроль — заход, собранный ИСПРАВЛЕННЫМ генератором (его и так
  строит пункт 1 критерия): красных по З18 — 0;
- `kod_gejty-ne-tuda.md` становится ПОЛОЖИТЕЛЬНЫМ контролем: красное на нём
  истинно, и это ловушка фикстуры, а не провал.
Проверено, что поправка ничего не ломает: два калибровочных живых захода ловушки
1 (`kod_tirazh-ocheredi.md`, `kod_ochered-domov.md`, 30.07) блока «СЧЁТ» не
несут вовсе — грепнул, вхождений 0, значит ловушка 1 остаётся зелёной без
вычетов. `pre-commit` `check_zahod.py` не зовёт (грепнул) — красное на старых
заходах ничьих коммитов не роняет.

### 3. Семья Д — что я померил сам, прежде чем строить
Замер захода воспроизвёл, и он воспроизводится НЕ дословно (`/usr/bin/grep`,
macOS, файл из четырёх строк — «Привет», «мир», «ясно», «zzz»):
- `LC_ALL=C` → матчит ТРИ строки вместо одной — ложь, как и сказано;
- `LC_ALL=C.UTF-8` и `ru_RU.UTF-8` → одна строка, ВЕРНО, падения
  «Invalid collation character» на этой машине нет (оно у GNU grep приёмки);
- python `re` → верно.
Вывод для сторожа тот же, что в задании, и опирается теперь на свой прогон:
результат шелл-диапазона зависит от locale, питоновский `re` — нет. Крашу
только шелл-контекст.

**Два места:**
1. `check_tool_contract.py` — новая проверка в `CHECKS`: кириллический диапазон
   в `.sh`-строках, в списках аргументов `subprocess`, в строках `shell=True` и
   в строках, уходящих в `grep`/`sed`/`awk`. Чистый питоновский `re` — не крашу.
2. `check_zahod.py` — новая клауза: тот же диапазон в греп-критерии внутри
   файла-захода (ложный ноль в критерии читается как «дефектов нет»).
Обеим — ловушка в своей фикстуре: дефект краснеет, чистый файл молчит. Плюс
прогон нового сторожа на ЧУЖОМ дефекте (§3 захода) — беру строку из фикстуры,
которую писал не я.

### 4. Порядок ходов
А (генератор → З18 → фикстура `zahod`) → Д (`check_tool_contract` → клауза
захода → обе фикстуры) → живой прогон всех семи пунктов критерия → `POKRYTIE.md`
(другой репозиторий, отдельный коммит) → коммит своей зоны.

⚠ В `POKRYTIE.md §2` тем же дефектом натянута ТРЕТЬЯ запись — **Р5-92** («число,
снятое при написании захода, стареет к моменту запуска», рычаг «нет: З6 судит
соседство команды, а не время замера»). Это ровно семья А, и после правки её
рычагом станет З18. Критерий называет только КРТ-033 и КРТ-РАЗН-21; Р5-92
допишу тем же ходом (тот же файл, та же секция, тот же коммит) и назову
отдельной строкой отчёта — оставить её лживой значило бы воспроизвести дефект,
который этот заход и лечит. Если это выход за задачу — снимается одной строкой.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. З18 в построенном виде краснеет на 140 живых заходах из 292 — это УНАСЛЕДОВАННОЕ (числа без даты, вписанные до 15.08), а не регресс. Гейт в `pre-commit` не стоит (проверено грепом), ничьих коммитов красное не роняет, но приёмка увидит его на каждом старом файле. Нужно решение: чистить задним числом, проставить датам «дата данных» пакетно или объявить старые заходы вне охвата.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. Мой собственный файл-заход краснеет по З19 (строка 90 — замер аналитика `grep -cE '[А-Я]' /tmp/k.txt` внутри ```-блока). Красное ИСТИННОЕ: строка исполнима, скопировавший её получит ту же ложь. Но это значит, что заход, чинящий класс, обязан показывать дефект в форме, которую сторож не примет за критерий — либо клауза должна уметь отличать ДЕМОНСТРАЦИЮ от ТРЕБОВАНИЯ, а признака для этого сегодня нет.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/zahody/RAZVITIE.md
   ДОСТАВЛЕНО: нет
3. КРТ-073 («значение записано в одной кодировке, контракт требует другую») осталась натянутой — она названа в §3 `POKRYTIE.md` третьей из трёх, я её не трогал: она не про кириллический диапазон и своего носителя по-прежнему не имеет.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/zahody/POKRYTIE.md
   ДОСТАВЛЕНО: нет
4. `schet_nezakrytogo.py` не различает «позвали неверно» (rc=2) и «чисто» (rc=0) — дефект живёт в HEAD, поймано хуком на моём коммите, чинить его я не стал: это меняло бы поведение инструмента за пределами задачи. Обход записан в `INCIDENTY.md`.
   ДОМ: /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

### ЧТО СДЕЛАНО И ЗАЧЕМ

**Семья А — генератор больше не подаёт счётное утверждение как факт.**
Мест, где посчитанное ПРИ СБОРКЕ число уезжает В ФАЙЛ, оказалось **три, в двух
функциях** (аналитик насчитал два — он считал функции, и это единственная
разница): `_sostoyanie_nevlityh()` в исходе «невлитых нет» и в исходе «невлитые
есть, но не названы», плюс блок «СЧЁТ НЕЗАКРЫТОГО» через
`schet_nezakrytogo.otchet()`. Найдено грепом `\{len\(|\{n\}|\.count\(` по
генератору — 11 кандидатов, из них 4 печатают в stdout/stderr, 5 считают якоря
шаблона, 2 уезжают в файл; третье место (голый ноль в тексте) греп не ловит, оно
найдено чтением функции. Четвёртый счётный текст — `({len(vlit_vetki)}):` в
исходе «ветки названы» — оставлен намеренно: это счёт СПИСКА, который стоит тут
же и проверяется глазами, а не замер диска.
Форма правки одна на все места: число осталось, но рядом встали **дата снимка** и
**команда**, которой читатель его пересчитывает (`snimok_metka()`,
`SNIMOK_FORMA` — один текст на все места, чтобы копии не разъехались). Метка в
`schet_nezakrytogo.otchet()` едет ПАРАМЕТРОМ, а не безусловно: тот же инструмент
печатает эти числа в консоль из `zakryt_sessiyu.py`, где они свежие по
построению, и назвать их «снимком при сборке» значило бы соврать в другую
сторону. Четыре исхода слота влития не тронуты — правился текст двух из них.

**Клауза З18 `check_zahod.py`** (З17 была занята). Судит не соседство команды
(это З6), а ВОЗРАСТ замера: счётное утверждение о состоянии репозитория обязано
нести в СВОЁМ АБЗАЦЕ метку времени.
🔴 **Одно сознательное отступление от буквы задания, названное прямо.** Заход
просил красить «число без команды И без метки времени». Живой прогон показал,
что такая клауза НЕ ЛОВИТ СВОЙ ЭТАЛОННЫЙ ДЕФЕКТ: в `kod_gejty-ne-tuda.md` рядом
с «невлитых веток 0» стояла `git branch --no-merged` — команда была, соврало
ВРЕМЯ. Клауза, молчащая на дефекте, ради которого заведена, — это новая
натянутая запись в доме, то есть ровно то, что чинит вторая половина захода.
Поэтому требуется именно МЕТКА ВРЕМЕНИ; соседство команды остаётся за З6. Замер
выбора на живом корпусе (292 файла `kod_*.md`): «команда ИЛИ метка» — 112
красных, «только метка» — 140. Оба калибровочных захода закона 2 молчат в обоих
режимах, свежесобранный исправленным генератором — тоже.
Окно — **абзац**, а не ±300 символов, и это несущая деталь: в
`kod_gejty-ne-tuda.md` сразу за блоком «СЧЁТ» идёт абзац КОНТЕКСТ со словами
«приёмка 14.08 приняла два захода», и при окне ±300 чужая дата из соседнего
абзаца засчиталась бы этим нулям меткой — клауза молчала бы на своём дефекте.
Калибровка сужения: первая редакция (существительное состояния + любая цифра в
±60) дала десятки ложных на живом корпусе («## 3. ЗАДАЧА — четыре выхода»,
«Субагенты ≤5», «стр. 9»). Итог сужения — две формы счётного утверждения,
отсечка по `§`/`v`/заглавной перед цифрой, отсечка по запятой и по цитате.
Разбор оставшихся 140: 92 — два места генератора (целевой дефект), 18 — живые
ручные замеры без даты (истинные), 3 — историческая проза, где число описывает
ПРОШЛОЕ событие («ЦЕНА: 2 моих коммита», «счётчик печатал 6 вместо 5»); лечится
той же датой, которую канон и так требует в строке ЦЕНА.

**Семья Д — сторож различает шелл и питон, и это проверено, а не заявлено.**
Замер захода воспроизведён и уточнён: на `/usr/bin/grep` владельца (macOS)
`LC_ALL=C` даёт **3 строки вместо одной** — ложь; `C.UTF-8` и `ru_RU.UTF-8` дают
верную одну, падения «Invalid collation character» на этой машине НЕТ (оно у GNU
grep приёмки); питоновский `re` верен везде. ⚠ Первый мой замер был НЕВЕРЕН и
это стоило бы всего построения: в этой сессии `grep` — функция-обёртка над
`ugrep` (Unicode-aware), и она печатала правильный ответ во всех локалях. Поймано
тем, что цифры не сошлись с заходом; все замеры пересняты через `/usr/bin/grep`.
1. `check_tool_contract.check_cyrillic_range` — кластер «переносимость» (имя
   кластера то же намеренно: `judge()` пускает на не-`.py` файлы ровно проверки
   с этим именем, иначе `.sh` остались бы вне охвата). Шелл-контекст назван
   поимённо: `.sh` целиком, `os.system`/`shell=True`, список аргументов с
   `grep`/`sed`/`awk`, строка, ПЕЧАТАЕМАЯ владельцу. Питоновский `re` не
   красится. Объявленная слепая зона: строка `.sh` со словом `python` не судится
   вовсе — там живёт здоровая форма `python3 -c`.
2. `check_zahod.py` З19 — тот же диапазон в греп-критерии ВНУТРИ файла-захода.
   Признак берётся ИМПОРТОМ из `check_tool_contract` (`KIR_RANGE_RE`,
   `SHELL_TOOL_RE`), а не пишется вторым регекспом: дефект один, мест два.

**Записи дома переписаны на построенные носители** — КРТ-033, КРТ-РАЗН-21 и
дополнительно **Р5-92** (семья А; критерий её не называл, но её рычаг стоял «нет»
и после этой работы стал бы лживым в другую сторону). Плюс поправлена запись §3
«Натянутое „накрыто“»: две записи из трёх сняты с натяжки, КРТ-073 названа
оставшейся.

### ДОКЛАД СУБАГЕНТА ГИТ-КОНТУРА (вставлен дословно, шесть строк)

1. **Заявок было 5, закрыто 0, застряло 3** — `2026-08-14T2048-arka-mat-kostyak-zahod-vynos-2` (влития и вывоз сделаны, но `zakryt-vetku zahod/kanon-put` отказал), `2026-08-14T2054-zahod-pravila-kadra-arka-mat-kostyak` (влитие есть, гашение отказано), `2026-08-14T2317-14-08-1-arka-mat-kostyak` (влитие и оба вывоза сделаны, гашение и коммит — не моя граница); ещё 2 заявки — `2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026` и `2026-08-12T0359-tipy-slajdov-py-2-primer-3` — не трогал: это содержательные правки `gejt_kartochki.py`/`tipy_slajdov.py`, командами `git_zona.py` не исполняются и гит-контуру запрещены.
2. **Влито в основную 4 ветки из 4 названных; основная ветка — `arka/mat-kostyak`** (имя снято командой, HEAD теперь `af21bbb`). `zahod/gejty-ne-tuda` влита мной сейчас (fast-forward, приехали ровно `ea6c08e` и `af21bbb`, пять путей, все в `_generator/tools/`, конфликтов нет); потребовался `--vsyo-ravno` — инструмент отказывался, т.к. ветка вычекаучена в живой рабочей папке `/Users/ivanyakovlev/Documents/GitHub/materials-wt/gejty-ne-tuda`, а причина перебоя записана в вывод: ветку назвал к влитию сам её заход, приёмка 14.08 приняла ровно эти два коммита. `zahod/vynos-2` (`388b8d4`), `zahod/kanon-put` (`3e87d55`), `zahod/pravila-kadra` (`eb32ff9`) были влиты до меня. Проверка фактом по основной: `git branch --no-merged arka/mat-kostyak | grep -c` = **0** по каждой из четырёх. Невлитых нет. Вывезено на origin: materials `arka/mat-kostyak` (+2) и disciplina `main` (+2: `60501f2`, `8edf412`) — прежний отказ по disciplina объяснён: его собственный `tools/git_zona.py` запрещает push при любых грязных отслеживаемых файлах, новый `git_zona.py` из materials этого запрета не несёт, грязные пути не тронуты.
3. **Что забрать в git** (`git_zona.py check`, 17 путей; сам не коммитил — это ход захода). Зона `_generator/tools/` целиком, включая фикстуры, УЖЕ в git — приехала слиянием, вне git по ней ничего не осталось. Рождено и никогда не ставилось (10): `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/README.md`, `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_pravila-kadra.md`, `_studio/zhurnal/2026-08-13_vynos-korpusa/kod_vynos-2.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_chislo-i-kirillica.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dolgi-nol.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dolgi-zero.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_gejty-ne-tuda.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_kanon-put.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_konflikty-2.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_konflikty.md`. Правлено и не закоммичено (7, включая правки аналитика): `_meta/disciplina/GIT-disciplina.md`, `_studio/docs/KARTA.md`, `_studio/zhurnal/2026-08-10_dizajn-i-metriki/SESSIYA.md`, `_studio/zhurnal/2026-08-13_vynos-korpusa/UROKI-FABRIKE.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, `teorkat-vvedenie/L2/dist/index.html`.
4. **Погашено: ничего мной; не погашено 3 ветки.** `zahod/kanon-put` — `poteri` rc=0, потерь нет, но `zakryt-vetku` отказал: в `/materials-wt/kanon-put` 1 путь вне git (`_studio/zhurnal/_INFRA-git/INCIDENTY.md`), никогда не коммиченный файл надгробие не держит и `--vsyo-ravno` этот отказ не перебивает. `zahod/pravila-kadra` — `poteri` rc=0, `zakryt-vetku` отказал: 13 путей вне git в `/materials-wt/pravila-kadra` (`teorkat-vvedenie/L2/dist/index.html` + 12 `teorkat-vvedenie/L2/slajdy/*/slaid.md`). `zahod/gejty-ne-tuda` — НЕ гашу намеренно: своя ветка живого захода, её гасит приёмка. `zahod/vynos-2` гасить нечего — надгробие `mogila/zahod/vynos-2` от 14.08 уже стоит. Чужие рабочие папки не трогал ни одной; 20 воркtree на месте.
5. **Необратимого не было.** Единственное изменение вне обратимого — два push (materials `arka/mat-kostyak`, disciplina `main`); откатывается на origin через `git revert`/принудительный откат владельцем, локальная история цела. Ни одна ветка не удалена, ни один файл не снесён, индекс не тронут; ход работы дописывался на диск в `/private/tmp/claude-501/-Users-ivanyakovlev/de4b1a89-4a1d-4c1b-9e51-3c1125ca6a89/scratchpad/git_kontur.log`.
6. выдано 32 позиций из 32 найденных

### КАК ПРОВЕРЕНО — семь пунктов критерия, каждый командой

1. **Живой прогон генератора** в одноразовую арку `_studio/zhurnal/2026-08-15_kriterij` (убрана): счётных утверждений о состоянии репозитория без метки времени в собранном файле — **0** (клауза З18 на нём же), меток «снимок при сборке» — **2**. Сборка rc=0.
2. **Четыре исхода различимы**, показаны два прогоном: «невлитых нет» → строка «вливать нечего, проверено…» (1 вхождение), «ветки названы» (`--vlit zahod/gejty-ne-tuda`) → «Что влить, названо аналитиком…» (1 вхождение). Два других исхода (`НЕ ЗАПОЛНЕНО` при невлитых без `--vlit`; сторож не отработал) в коде не тронуты, их сторожат ловушки 19 и 27 фикстуры.
3. **`check_zahod.py` на голом числе → rc=1**, красное именно З18 (1 вхождение) — `kod_gejty-ne-tuda.md`. На свежесобранном заходе красных по З18 — **0**. ⚠ Пункт исполнен В ПОПРАВЛЕННОМ ВИДЕ, поправка объявлена в `## ПЛАН` ДО работы: `kod_gejty-ne-tuda.md` не может быть отрицательным контролем, он сам несёт дефект (строки 32–38 — четыре голых нуля). Отрицательный контроль — файл, собранный ИСПРАВЛЕННЫМ генератором.
4. **`check_tool_contract.py` на дефектном файле → rc=1**, в тексте названа кириллица (1 вхождение «КИРИЛЛИЧЕСКИЙ ДИАПАЗОН»). На живых инструментах — **0 новых красных**; прогнано **41 файл `.py`** (`--all`, включая фикстурные) **и 31 `.sh`**-фикстура. Общее число находок линтера осталось 28 — те же, что были до правки; единственная находка на моих файлах (`schet_nezakrytogo.py`, «три исхода rc») проверена на версии ИЗ HEAD и существует там же.
5. **`check_zahod.py` на заходе с греп-критерием `[А-Я]` → rc=1**, красное З19 (1 вхождение) — на этом самом файле-заходе, строка 90. По живому корпусу З19 даёт **1 красное на 292 файла**, и оно ИСТИННОЕ: `kod_sborka-l2-deka.md` строка 203, `grep -c` с кириллическим классом в критерии.
6. **Обе фикстуры rc=0**, «ФИКСТУРЫ ЗЕЛЁНЫЕ». Число ловушек печатает прогон: `zahod` — **66** (было 61), `tool_contract` — **50** (было 46; счётчик там заведён этой правкой, шапка числа не хранит).
7. **`POKRYTIE.md §2`** — КРТ-033, КРТ-РАЗН-21 и Р5-92 переписаны на построенные носители с числами прогонов; §3 «Натянутое „накрыто“» помечена частично закрытой. Другой репозиторий, отдельный коммит, хэш ниже.

**Верификатор:** отдельный не нужен (операция не лоссовая), его роль сыграли
пункты критерия. Исключение §3 исполнено: новый сторож прогнан на ЧУЖОМ
дефекте — на строках замера ИЗ ЭТОГО ФАЙЛА-ЗАХОДА, написанных аналитиком, а не
мной (`printf … | LC_ALL=C grep -cE '[А-Я]'`): в `.sh` покраснели ровно две
шелл-строки (3 и 4), строка с `python3 -c` смолчала. То есть гейт ловит КЛАСС, а
не мой образец.

**Что НЕ трогал:** ничего вне `_generator/tools/`. `_studio/docs/KARTA.md`
дважды изменялся автоматически (генератор регистрирует пробные заходы) и оба
раза восстановлен побайтово из копии — `diff` чист. Пробные арки удалены. Четыре
исхода слота влития, поведение заходов, З1–З17 не менялись. КРТ-073 не трогал.

**Время прогона + токены:** НЕПРИМЕНИМО — канал `app`, следа `tee` не остаётся.

**ПОВТОРЯЕМОСТЬ находок.** Повторятся на СЛЕДУЮЩЕЙ единице работы (значит —
заход ДО следующего прогона, не пункт очереди):
— расхождение «стартовое сообщение против §0.1» повторится на КАЖДОМ следующем
  заходе: оба текста печатает один генератор, исполнитель читает стартовое
  раньше файла по построению;
— побочный файл от `check_tool_contract --all` появляется при каждом прогоне
  `--all`, то есть у каждого, кто калибрует любую проверку контракта.
Не повторятся (законно уходят в очередь): 140 унаследованных красных З18 на
старых заходах, КРТ-073, rc=2 у `schet_nezakrytogo.py`.

**НЕОБРАТИМОЕ** (список полный):
— снята и заведена заново рабочая папка `materials-wt/chislo-i-kirillica` · ветка
  `zahod/chislo-i-kirillica` при этом НЕ удалялась, своих коммитов не имела
  (`merge-base --is-ancestor` → rc=0) · восстанавливается тем же `worktree add`;
— основная ветка влита в мою с `--vsyo-ravno` (fast-forward до `af21bbb`) ·
  восстанавливается `git reset --hard <прежний HEAD>` — прежним был `eb32ff9`;
— удалены пробные арки `_studio/zhurnal/2026-08-15_proba-chislo` и
  `2026-08-15_kriterij` (4 файла, все порождены мной этой сессией) ·
  восстанавливаются повторной сборкой генератором, команды — в п.1–2 выше;
— `_studio/docs/KARTA.md` перезаписан копией (дважды) · копия лежит в
  `…/scratchpad/KARTA.md.bak` и `KARTA.bak2`, `diff` с рабочей — чист;
— побочный `koridor-proba/…/slaid.md` НЕ удалён, а перенесён в
  `…/scratchpad/pobochnoe/koridor-proba/` — оттуда и восстанавливается.

**ДОЛГ (обход хука, названо отдельной строкой по §4):** коммит зоны прошёл с
`--no-verify` и причиной «чужой долг: `check_tool_contract` на
`schet_nezakrytogo.py` даёт [три исхода rc] — у `main()` нет кода 2». Дефект
существует в HEAD (проверено прогоном линтера на версии из HEAD), моими правками
не внесён; починка меняла бы поведение инструмента за пределами задачи. Причина
уехала в `INCIDENTY.md` инструментом.

**АРТЕФАКТ:** артефакта-документа нет: заход правит инструменты, собранного
файла на выходе не бывает, а все пробные — убраны по требованию критерия.
Проверяемый выход открывается двумя командами (обе rc=0, «ФИКСТУРЫ ЗЕЛЁНЫЕ»):
`sh /Users/ivanyakovlev/Documents/GitHub/materials-wt/chislo-i-kirillica/_generator/tools/fixtures/zahod/PROGNAT.sh`
и `sh /Users/ivanyakovlev/Documents/GitHub/materials-wt/chislo-i-kirillica/_generator/tools/fixtures/tool_contract/PROGNAT.sh`.
**КОММИТ:** `e89380c` — «_generator/tools: счётное утверждение о репозитории едет
снимком (дата+команда), клауза З18; кириллический диапазон в шелл-контексте
краснеет — check_cyrillic_range и З19» · `git_zona.py check --zone
_generator/tools/` → ✅ · `show --stat` — ровно шесть моих путей, чужого нет.
**КОММИТ ВТОРОГО РЕПОЗИТОРИЯ** (`disciplina`, отдельный по требованию п.7):
`241b58f` — «zahody/POKRYTIE: КРТ-033, КРТ-РАЗН-21 и Р5-92 — рычаг переписан на
построенные носители (З18, З19, check_cyrillic_range)», 1 файл.
Ни одна ветка не влита и не вывезена мной — это ходы приёмки.
