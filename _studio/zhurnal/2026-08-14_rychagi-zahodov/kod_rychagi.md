# Канал исполнителя — rychagi (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — девять рычагов расписаны поимённо, с классами записей и образцом; суждение только в калибровке, и у неё есть замер и право остановиться.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — девять рычагов расписаны поимённо, с классами записей и образцом.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/rychagi.
Зона в ДВУХ репозиториях: код в materials, клетки таблиц домов в
/Users/ivanyakovlev/Documents/GitHub/disciplina — коммить их вместе, класс за классом.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_rychagi.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Работа длинная и резать её МОЖНО: один класс — один коммит, следующий не
начинается, пока предыдущий не зелёный. Дошёл до третьего рычага и кончилось
время — это нормальный исход, так и отчитайся.

Первым ходом: python3 _generator/tools/git_zona.py worktree add rychagi --branch zahod/rychagi,
затем чтение захода, затем секция ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-14_rychagi-zahodov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. «Дом инцидентов» — таблица в `disciplina/doma/<дом>/POKRYTIE.md §2`, где у каждой записи стоит РЫЧАГ: что именно краснеет, если этот класс ошибок повторится. Четыре вида рычага и правило их различения — `doma/README.md §3.3`. Записи с рычагом «нет: … не построен» и есть очередь работы: правило записано, а покраснеть нечему, значит оно держится на памяти и не исполняется (`KONSTITUCIYA §11`).

Прошлый этап: 13–14.08 собрана вся структура — дома `zahody` (81 запись) и `priyomka` (27), стандарт дома, генератор карты инструментов, дверь интервью, калибровка З6. **Структура готова, рычагов не хватает.** Замер аналитика 14.08 командой по обоим домам: **82 записи без машинного рычага, Σ кратности 104.**

ЦЕЛЬ: закрыть их машинными носителями. Классы уже сгруппированы (§2) — гипотеза владельца, подтверждённая замером: 82 записи лечатся девятью рычагами, а не восемьюдесятью двумя правками.

Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 **СТОП ДО ЦЕЛИ, громко: до конца дойти НЕ обязательно.** Один класс — один коммит; следующий не начинается, пока предыдущий не зелёный. Кончилось время на третьем рычаге — **это нормальный исход, а не провал**: три работающих рычага лучше девяти недоведённых.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. порядок — от самого крупного класса к мелким: крупный даёт образец, по которому остальные строятся быстрее (решение владельца 14.08)
2. калибровка обязательна у КАЖДОГО рычага; не сошлась — рычаг НЕ ставится, и это законный исход: «лучше пять честных рычагов, чем семь, из которых два обходят --no-verify»
3. 22 «прочих» тоже разбираются — владелец 14.08: «если готов их посмотреть и увидеть, что есть ещё три инструмента, которые всё закроют, будет здорово; часть окажется ерундой, случайно сюда попавшей»
4. один класс — один коммит; следующий не начинается, пока предыдущий не зелёный; кончилось время на третьем рычаге — нормальный исход, не провал
5. делать максимально дёшево и машинно: бесплатные проверки, прогоны, готовые инструменты — а не рассуждения

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/rychagi`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/rychagi`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add rychagi --branch zahod/rychagi
  cd ../materials-wt/rychagi
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/` `_studio/zhurnal/_TEMPLATE-zahod.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-zahod/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-priyomka/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.**

🔴 **ОЧЕРЕДЬ И ПОЧИНКУ ФИКСТУР ИСПОЛНЯЕТ СУБАГЕНТ, НЕ ТЫ — И ЭТО ПЕРВЫЙ ХОД.**

В очереди лежит заявка, **блокирующая твои коммиты**: обязательный флаг `--intervyu` (появился 14.08) уронил вызовы `bootstrap_zahod.py` в ЧЕТЫРЁХ фикстурах вне твоей рабочей зоны — `fixtures/dvizhok/` (и её `zvat.py`), `fixtures/korni/PROGNAT.sh`, `fixtures/gigiena/PROGNAT.sh`, `fixtures/register_doc/PROGNAT.sh`. Пока не починено, `pre-commit` краснеет на КАЖДОМ твоём коммите класса. Каждая чинится одной строкой: дописать `--intervyu da` в вызов генератора.

**Запусти ОДНОГО субагента, дай ему это задание целиком, дождись, вставь его отчёт пятью строками в `## ОТЧЁТ` — и всё. Сам в детали починки не вникай.** Причина ровно одна и она про твоё окно: содержание этой починки **никак не связано** с постройкой рычагов, а прочитанное остаётся в контексте до утра. Ночной прогон длинный, и место в окне — расходный материал (решение владельца 14.08).

Субагенту скажи явно: **он НЕ коммитит** (коммитит ведущий заход), пишет добычу на диск по ходу, а последней строкой даёт самоотчёт «починено N фикстур из 4». Нет строки — считай ответ неполным и переспроси.

⚠ **Проверить результат ты обязан САМ, одной командой, а не на слово субагента:** `sh _generator/tools/fixtures/gigiena/PROGNAT.sh` → rc=0. Не сошлось — не начинай рычаги, пока не сойдётся: иначе каждый следующий коммит потребует `--no-verify`, и красное перестанет что-либо значить.

**Остальная очередь — тем же субагентом, тем же ходом.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

```
python3 _generator/tools/git_zona.py merge zahod/nositeli-gen --zone _generator/tools/ --zone _studio/zhurnal/_TEMPLATE-zahod.md --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/ --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-zahod/ --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-priyomka/
```

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/nositeli-gen`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

Забирать чужого нечего. ⚠ Коммитов у тебя МНОГО — по одному на класс рычага, и в ДВУХ репозиториях: код в `materials`, клетки таблиц в `disciplina`. Клетку записи коммить ВМЕСТЕ с носителем, который в ней назван: порознь получится ложно-зелёная метрика при живом отсутствии рычага.

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

После влития `zahod/nositeli-gen` (слот 1) погаси её и `zahod/nositel`, которая приезжает внутри неё: `git_zona.py poteri --branch <ветка>` → при `rc=0` `zakryt-vetku`, при `rc=1` НЕ гасить и назвать строкой в отчёте. Чужие ветки и рабочие папки не трогай.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/rychagi`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/` `_studio/zhurnal/_TEMPLATE-zahod.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-zahod/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-priyomka/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО это:
  · `~/Documents/GitHub/disciplina/doma/zahody/POKRYTIE.md` и `~/Documents/GitHub/disciplina/doma/priyomka/POKRYTIE.md` — таблицы §2, твой рабочий список;
  · `~/Documents/GitHub/disciplina/doma/README.md` §3.3 — четыре вида рычага и как счётчик их узнаёт (форма клетки решает, засчитается ли твоя работа);
  · `_generator/tools/check_zahod.py` — **главный образец**: клауза З6 после калибровки 14.08 (пять классов ложного, найденных на живом корпусе) — по ней строятся почти все рычаги ниже;
  · `_generator/tools/check_sborki.py` — второй линтер, там С3 (пути с диском) и С5 (команды);
  · `_generator/tools/check_tool_contract.py` — генерик-тестер инструментов, он же готовый носитель для класса 7;
  · `_generator/tools/fixtures/zahod/PROGNAT.sh` — куда добавляются ловушки.
  Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Девять рычагов, по классам записей. **Порядок фиксирован: от крупного к мелкому** — крупный даёт образец, по которому остальные строятся быстрее.

🔴 **ОДИН РИТУАЛ НА КАЖДЫЙ РЫЧАГ, без исключений.** Пропуск любого шага делает рычаг хуже его отсутствия:

1. **Прочитай записи класса** в таблице дома — ими рычаг и оправдан.
2. **Построй проверку** — клаузой существующего линтера, если предмет его, иначе отдельным скриптом в `_generator/tools/`.
3. 🔴 **КАЛИБРОВКА на живом эталоне — ДО того, как объявишь рычаг готовым.** Прогони по 25 свежим `kod_*.md` журнала. **Ложных срабатываний должно быть 0.** Есть хоть одно — сужай, пока не ноль, или **не ставь рычаг вовсе**. *Замер, ради которого правило существует: З6 в лоб красила 20 живых заходов из 25, на 8 это была единственная претензия — гейт, красный на здоровом, обходят `--no-verify` вместе со всей защитой.*
4. **Две ловушки в фикстуру** — отрицательная (дефект краснеет поимённо) и положительная (здоровое молчит).
5. **Впиши рычаг в клетку каждой записи класса** — дословно, с именем носителя: `` `check_zahod.py` З14 ``. Счётчик читает ПОСЛЕДНЮЮ клетку строки (`doma/README.md §3.2`) и узнаёт машинный рычаг по имени файла или id гейта. Без этого работа не засчитается метрикой.
6. **Коммит класса.** Следующий класс — только после зелёного.

⚠ **Три записи из списка, возможно, УЖЕ закрыты вчерашними правками** (`З6` в жёстких, `З13`, `--intervyu`): `КРТ-019`, `Р5-114`, `КРТ-РАЗН-19`, `КРТ-РАЗН-30`, `КРТ-РАЗН-35`, `КРТ-РАЗН-71`, `Р5-112`. **Первым делом проверь их прогоном и, если закрыты, просто впиши носитель** — это самая дешёвая часть работы. Число проверенных назови в отчёте.

---

### Рычаг 1 — ФАКТ О МИРЕ НЕСЁТ КОМАНДУ (26 записей, самый крупный)

Записи: `Р5-31` `КРТ-026` `Р5-60` `КРТ-038a` `КРТ-102` `КРТ-РАЗН-3` `КРТ-РАЗН-16` `КРТ-РАЗН-25` `Р5-22` `Р5-68` `Р5-88` `Р5-115` `Р5-116` `КРТ-071` `КРТ-075` `Р5-23` `Р5-59` и соседние (полный список — грепом по клетке «сверка … не построена»).

**Суть класса.** Заход утверждает факты о мире: «файл лежит там-то», «в источнике этого нет», «инструмент доступен», «раздел на месте», «предпосылка жива». Ни одно такое утверждение сегодня не обязано нести рядом команду, которой оно проверено. **З6 уже решил ровно эту задачу для ЧИСЕЛ — обобщи её на нечисловые утверждения.**

Что ловить: утверждение о состоянии диска/среды в жирном спане или в §0, рядом с которым в окне нет ни команды в бэктиках, ни резолвимого пути. Что НЕ ловить (проверь на живом корпусе, список пополни сам): рассуждения о будущем («получишь X»), цитаты правил, отрицания-запреты, текст внутри ```-блоков.

⚠ **Частично закрыто:** `С3` `check_sborki.py` уже сверяет буквальные ПУТИ с диском. Новый рычаг — про утверждения, которые путём не выражены. Не дублируй С3: если предмет строки — путь, оставь его С3 и так и напиши.

### Рычаг 2 — ФОРМА КРИТЕРИЯ: порядок пунктов и охват внутри вердикта (10 записей)

Записи: `КРТ-041` `Р5-10` `Р5-67` и соседние («порядок пунктов критерия», «охват»).

Две проверки, обе дешёвые: **(а)** обязательный пункт критерия не стоит последним — недобор по построению, исполнитель до него не доходит; **(б)** отрицательный вердикт обязан нести охват В СЕБЕ («проверено X из Y»), а не в примечании. Плюс `Р5-67`: в заходе РОВНО ОДИН критерий готовности (`З9` уже считает строку «КРИТЕРИЙ ГОТОВНОСТИ» — проверь, не закрыто ли).

### Рычаг 3 — КОНТРАКТ ГЕЙТА: три исхода, самоцитирование, пустой вход (7 записей)

Записи: `Р5-3` `Р5-57` `Р5-27` `Р5-12` `КРТ-РАЗН-12` `КРТ-РАЗН-32` `КРТ-061`.

**Суть.** Гейты ломаются одинаково: код возврата не различает «чисто» / «нашёл дефект» / «позвали неверно»; проверка находит СОБСТВЕННЫЕ служебные маркеры в тексте, который о них рассказывает; честная приписка «этот файл я не читал» глушит гейт; пустой вход даёт зелёное.

🔴 **Носитель уже существует — `check_tool_contract.py`.** Не пиши новый: расширь его контракт этими четырьмя пунктами и прогони по всем `check_*.py`. Красное на чужих инструментах — **в отчёт списком, чинить их ты не обязан**.

### Рычаг 4 — ЧИСЛО И ЕГО СТАРЕНИЕ (10 записей, частично закрыт вчера)

Записи: `КРТ-019` `Р5-114` `КРТ-РАЗН-19` `КРТ-РАЗН-30` `КРТ-РАЗН-35` `КРТ-РАЗН-71` и соседние.

З6 стал жёстким 14.08 — **проверь прогоном, какие из этих записей он уже закрывает, и впиши носитель.** Остаток — про СТАРЕНИЕ числа между замером и применением (`Р5-114`) и про сходимость числа с собственной таблицей захода (`КРТ-РАЗН-35`). Второе дешевле: два числа в одном документе, которые обязаны совпасть.

### Рычаг 5 — КАЛИБРОВКА КАК ОБЯЗАННОСТЬ (6 записей)

Записи: `КРТ-РАЗН-24` `КРТ-РАЗН-29` `КРТ-РАЗН-76` `КРТ-РАЗН-81` и соседние.

**Суть.** Критерий предписывает форму вызова гейта, красную на здоровом; grep-паттерн ловит не то, что заявлено; верификатор даёт ложные срабатывания. Рычаг: заход, вводящий НОВУЮ проверку, обязан нести строку «прогнано на живом эталоне, ложных 0». Проверяй наличие этой строки, когда в заходе есть новый гейт. **Ты сам обязан исполнить это правило девять раз — заодно проверишь, исполнимо ли оно.**

### Рычаг 6 — ДО и ПОСЛЕ (4 записи)

Записи: `Р5-70` `Р5-94` `КРТ-127` и соседняя.

ДО-верификатор судит состояние, которое работа уничтожит, — после работы он неисполним, а в шаблоне стоит ПОСЛЕ. Критерий, посчитанный до работы, применяется к состоянию после. Рычаг: в шаблоне ДО-верификатор физически выше, и линтер краснеет, если заход называет ДО-верификатор ниже секции работы.

### Рычаг 7 — СУБАГЕНТ: усечение и прозрачность канала (2 записи)

Записи: `Р5-6` `КРТ-020a`.

Ответ субагента мог быть усечён, и это неотличимо от честного «мало нашлось». Правило есть: финальная строка «выдано N позиций из M найденных». Рычаг: генератор вписывает это требование в промпт субагента автоматически (`--zadanie-subagentu` уже печатает промпт — добавь туда), а приёмка краснеет на отчёте субагента без такой строки.

### Рычаг 8 — ФОРМАТ И МЕСТО ОБЪЯВЛЕНЫ (3 записи)

Записи: `КРТ-028a` `КРТ-РАЗН-1` `КРТ-068`.

Заход требует артефакт, но не говорит, где он ляжет и в каком формате. Рычаг: если заход обещает собранный артефакт, он обязан назвать путь ДО работы — как `АРТЕФАКТ:` в отчёте, только на входе.

### Рычаг 9 — ОДИНОЧКИ: разобрать и назвать (около 9 записей)

`Р5-65` `Р5-75` `Р5-87` `Р5-103` `Р5-107` `КРТ-073` `КРТ-РАЗН-14` `КРТ-048` `КРТ-039`.

🔴 **Здесь работа не «построить», а РАССУДИТЬ, и владелец разрешил обе стороны.** По каждой: либо она накрывается уже построенным рычагом (впиши носитель), либо это не инцидент фабрики и попала сюда случайно (напиши прямо), либо нужен свой носитель — тогда назови какой и **не строй его**, вынеси в очередь. `Р5-112` («аналитик берёт дефолт среды вместо того, чтобы спросить») скорее всего закрыт вчерашним `--intervyu` — проверь.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — семь пунктов, все машинные):**

0. **ЖИВОЙ ПРОГОН на реальных объектах репозитория, а не только на фикстурах** — это отдельный первый пункт, потому что фикстура сторожит поломку, а он сторожит излишнюю строгость, от которой линтер отключат целиком. После КАЖДОГО построенного рычага прогони оба линтера по 25 свежим `kod_*.md` журнала и по свежесобранному заходу; печатай сводку. **Красным светит только то, что и должно, — ни одного нового красного на здоровом файле.**
1. **Доля машинного покрытия ВЫРОСЛА, и это посчитано скриптом, а не заявлено.** Прогони классификатор рычагов по обоим домам ДО и ПОСЛЕ, печатай оба вывода. Было на 14.08: 82 записи без машинного рычага, Σ 104. **Стало — назови числом.** Рост 0 при непустой работе — провал.
2. **У КАЖДОГО построенного рычага калибровка чистая.** Для каждого печатай: «прогнано 25 живых `kod_*.md`, срабатываний N, из них ложных **0**». Ложных > 0 — рычаг не считается построенным, даже если код написан.
3. **Фикстуры зелёные целиком.** `sh _generator/tools/fixtures/zahod/PROGNAT.sh` → rc=0, и число ловушек выросло — печатай до и после командой `grep -c '^#  *[0-9]*\.'`.
4. **Ни один здоровый заход не сломался.** Свежесобранный генератором заход проходит `check_zahod.py` и `check_sborki.py` начисто. Собери пробный и покажи оба rc.
5. **Каждая закрытая запись несёт ИМЯ носителя в своей клетке.** Печатай счёт: «записей с машинным рычагом было X, стало Y», и список закрытых id.
6. **Разобраны все 22 «прочих»** — у каждой один из трёх исходов (накрыта рычагом · не инцидент фабрики · нужен свой носитель, назван и вынесен в очередь). Печатай три числа, сумма = 22.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «рычаги построены», а «построено N рычагов из 9, закрыто M записей из 82, калибровка чистая у K из N».

🔴 **Считаешь рычаг вредным или невозможным — скажи в `## ПЛАН` ДО работы и не строй.** Опровержение — полноценный успех. **Брак — только выдуманный вердикт:** рычаг, объявленный построенным без чистой калибровки, хуже отсутствующего, потому что его обойдут вместе со всеми остальными.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/ _studio/zhurnal/_TEMPLATE-zahod.md /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/ /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-zahod/ /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-priyomka/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/ _studio/zhurnal/_TEMPLATE-zahod.md /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/ /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-zahod/ /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-priyomka/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/` `_studio/zhurnal/_TEMPLATE-zahod.md` `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-zahod/` `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-priyomka/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/_TEMPLATE-zahod.md` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-zahod/` → ✅; `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-priyomka/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Порядок из §0.1: слот 0 (субагент чинит очередь фикстур + заявки) → слот 1
(влить `zahod/nositeli-gen`) → слот 2/3 (коммит/закрытие веток) → прочитать
названные якоря → строить рычаги 1→9 от крупного к мелкому, каждый со своим
коммитом. Критерий готовности и сама задача выглядят здравыми, поправок не
предлагаю.

Фактический ход остановился на слоте 1: см. `## ВОПРОСЫ` — это СТОП по
явному правилу захода («не сошлось хоть для одной — стоп»), не решение
пропустить шаг. Рычаги 1–9 не начаты.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Слияние `zahod/nositeli-gen` → `zahod/rychagi` (§0.1 слот 1, обязательный
   шаг ДО работы) отказало командой `git_zona.py merge`: ветка вычекаучена в
   параллельной рабочей папке `/Users/ivanyakovlev/Documents/GitHub/materials-wt/nositeli-gen`.
   Read-only проверка той папки (`git status --porcelain`, `git log -1`) нашла
   незакоммиченные правки (`_studio/docs/KARTA.md`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`)
   и новый нетрекнутый файл `_studio/zhurnal/2026-08-13_skill-mat-teksta/kod_probaC.md`;
   последний коммит там — сегодня, 2026-08-14 03:38. Похоже на живой параллельный
   заход, не на брошенную папку. Инструмент сам предложил два пути: спросить того,
   кто там работает, либо повторить с `--vsyo-ravno "<причина>"`. Спросить синхронно
   не могу, а форсировать чужую живую работу без подтверждения — риск, которого
   заход прямо просит избегать («не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`,
   дальше не иди»). Без этого слияния флаг `--intervyu` в `bootstrap_zahod.py`
   недоступен на этой ветке, поэтому уже сделанный фикс 4 фикстур (см. `## ОТЧЁТ`)
   не проходит собственную проверку (`gigiena/PROGNAT.sh` → rc=1), и ни один из
   девяти рычагов не начат.
   ДОМ: владелец
   ДОСТАВЛЕНО: rychagi#1 · 2026-08-14

   🟢 **ОТВЕТ АНАЛИТИКА (14.08, вписан в файл-заход; продолжай работу с этого места).**

   **Ты остановился правильно, и осторожность была уместна — но живой работы там НЕТ.**
   Незакоммиченные правки в `materials-wt/nositeli-gen` оставила ПРИЁМКА, а не параллельный
   заход. Это следы проверки твоего же предшественника `nositeli-gen`: я собирал в его
   рабочей папке пробные заходы `probaA`/`probaB`/`probaC`, проверяя, что `--intervyu`
   отказывает без флага, печатает шесть вопросов при `net` и вшивает строку при `da`.
   Отсюда и `kod_probaC.md` (14.08 03:58), и строка его регистрации в `KARTA.md`, и запись
   в `INCIDENTY.md`. Проверено мной перед этим ответом: `git worktree list` помечает эту
   папку `prunable`, содержательного коммита после `1dcc839` в ней нет.

   ⇒ **ФОРСИРУЙ СЛИЯНИЕ, причина — дословно такая:**
   ```
   python3 _generator/tools/git_zona.py merge --branch zahod/nositeli-gen \
       --vsyo-ravno "правки в materials-wt/nositeli-gen — следы приёмки 14.08 (пробные заходы probaA/B/C при проверке --intervyu), не живая работа; подтверждено аналитиком по git worktree list (prunable) и по времени файлов"
   ```

   ⚠ **Если `merge` упрётся во что-то ещё — не форсируй второй раз, возьми зону пофайлово:**
   `git_zona.py adopt --branch zahod/nositeli-gen --zone <путь-файла> --yes` по каждому из
   пяти файлов генератора. `adopt` не требует, чтобы ветка была свободна, — он копирует
   содержимое. Пофайлово, а не одной командой по всей зоне: широкая зона рядом с чужой
   незакоммиченной работой стирает её молча.

   **Проверка, что слияние доехало** (её делай сам, не на слово команды):
   `grep -c '^# ── З' _generator/tools/check_zahod.py` → **13** (было 12 до влития), и
   `python3 _generator/tools/bootstrap_zahod.py --help | grep -c intervyu` → не ноль.
   Только после этого прогоняй `sh _generator/tools/fixtures/gigiena/PROGNAT.sh` → rc=0 и
   начинай рычаг 1.

   **Пробные файлы `kod_probaA/B/C.md` и их строки в `KARTA.md` — мусор приёмки, не твой.**
   Убирать их ты НЕ должен: они вне твоей зоны. Если они попадутся под ноги (например,
   `check_kartoteka` покраснеет на них) — назови их строкой в `## ОТЧЁТ` и работай дальше.

   **Стоп-до-цели из задания остаётся в силе:** один класс — один коммит, до конца доходить
   не обязательно, три работающих рычага лучше девяти недоведённых.

2. При постройке Рычага 1 (17 записей `zahody`, класс «факт о мире без
   соседней команды») нашёл 5 текстово похожих записей в `priyomka §2`
   (`Р5-71`, `Р5-77`, `Р5-80`, `Р5-102`, `Р5-11` — все «нет: сверка X с Y
   не построена»), которые Рычаг 1 НЕ закрывает: их механизм — сверка
   ОТЧЁТА исполнителя с АРТЕФАКТОМ ПОСЛЕ работы (домен приёмки, требует
   живого сравнения двух значений, не текстового «есть ли backtick
   рядом»), а не текстовый линт заявок ДО работы, который умеет
   `check_zahod.py` З14. Кандидат на отдельный рычаг в `priyomka.py`
   (симметричный С6 `check_sborki.py`, но для пары отчёт↔артефакт), не
   строил его — вне сегодняшнего класса.
   ДОМ: `doma/priyomka/POKRYTIE.md §2`
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Охват (отрицательный вердикт, число в себе):** построен 1 рычаг из 9
(Рычаг 1), закрыто 17 записей из 82 этим рычагом + 4 записи быстрой
проверкой прогоном (итого 21 закрытая запись, все в доме `zahody`);
калибровка чистая у 1 из 1 построенного рычага. Дошёл до конца git-контура,
прочитал названные якоря, построил Рычаг 1 целиком по ритуалу (шесть шагов),
закоммитил в обоих репозиториях. Рычаги 2–9 НЕ начаты — законный стоп
после первого класса («три рычага лучше девяти недоведённых», задание §2).

**Часть А — git-контур (после ответа аналитика в `## ВОПРОСЫ`, пункт 1):**
1. Форсировал слияние ровно командой аналитика (с поправкой синтаксиса:
   `merge <ветка>` — позиционный аргумент, не `--branch`; `--vsyo-ravno`
   с той же причиной дословно) → **RC=0**, влито без конфликтов, коммит
   `1dcc839` виден в `git log --oneline`.
2. Проверил слияние ФАКТОМ, не словом инструмента: `grep -c '^# ── З'
   _generator/tools/check_zahod.py` → **13** (было 12); `bootstrap_zahod.py
   --help | grep -c intervyu` → **2** (не ноль). Оба сошлись.
3. `sh _generator/tools/fixtures/gigiena/PROGNAT.sh` → **rc=0** — фикстуры
   зелёные, блокировка снята.
4. §0.1 слот 3 «закрыть после»: `git_zona.py poteri --branch
   zahod/nositeli-gen` → rc=0 (потерь нет), но `zakryt-vetku --branch
   zahod/nositeli-gen` → **rc=1**: рабочая папка несёт 3 некоммиченных пути
   (мусор приёмки `probaC.md`/`KARTA.md`/`INCIDENTY.md`, тот же случай, что
   уже объяснил аналитик) — по правилу «rc=1 — НЕ гасить» ветку оставил.
   То же для вложенной `zahod/nositel`: `poteri` → rc=0, `zakryt-vetku` →
   **rc=1** (1 некоммиченный путь в её рабочей папке `materials-wt/nositel`)
   — тоже не погашена. Обе ветки называю здесь строкой, как предписано.
5. Закоммитил слот 2 «что забрать в git» — 4 файла фикстур
   (`--intervyu da`, дело субагента) в `materials` отдельным коммитом ДО
   начала Рычага 1 (см. `КОММИТ` ниже, коммит 1).

**Часть Б — быстрая проверка 7 записей «первым делом» (задание §2):**
Проверил все 7 прогоном. Закрыто **4 из 7**:
- `КРТ-019`, `КРТ-РАЗН-19`, `КРТ-РАЗН-30` — З6 действительно переведена в
  `HARD_CHECKS` 14.08 (`заход nositeli-gen`), а не лежит в `SOFT_CHECKS`,
  как утверждали старые клетки; проверено кодом:
  `check_z6_chislo in HARD_CHECKS` → `True`.
- `Р5-112` — `bootstrap_zahod.py --intervyu {da,net}` обязателен, дефолта
  нет; проверено `--help`: `ОБЯЗАТЕЛЕН, дефолта нет`.
Честно НЕ закрыты (описаны в задании как «остаток Рычага 4», не мой класс
сегодня): `Р5-114` (старение числа между замером и применением — hard-З6
проверяет только соседство команды, не актуальность числа во времени),
`КРТ-РАЗН-35` (сходимость числа с собственной таблицей захода — другой
механизм), `КРТ-РАЗН-71` (верность самого числа, не только наличие
backtick рядом — тот же класс ограничения).

**Часть В — прочитал названные якоря:** `doma/zahody/POKRYTIE.md` и
`doma/priyomka/POKRYTIE.md` §2, `doma/README.md §3.3`, `check_zahod.py`
(клауза З6 как образец), `check_sborki.py` (С3/С5/С6 как соседние
механизмы), `check_tool_contract.py`, `fixtures/zahod/PROGNAT.sh`. Проект
дальше не изучал.

**Часть Г — построил Рычаг 1 (26/17 записей по грепу, самый крупный класс),
шесть шагов ритуала:**
1. Прочитал 17 записей класса «факт о мире без соседней команды» в
   `zahody §2` — суть: заход утверждает состояние диска/среды
   («лежит», «доступен», «на месте», «жива», «существует», «отсутствует»,
   «поставлен», «выполнен») без backtick-подтверждения рядом.
   ⚠ Честная находка: заход называет класс «26 записей», но грепом по
   клетке «сверка … не построена» в `zahody` нашлось ровно **17**, не 26 —
   разница не моя выдумка, число не подгонял. Дополнительно нашёл 5 похожих
   по ТЕКСТУ записей в `priyomka` (`Р5-71`, `Р5-77`, `Р5-80`, `Р5-102`,
   `Р5-11`), но их МЕХАНИЗМ другой — сверка ОТЧЁТА исполнителя с
   АРТЕФАКТОМ после работы (домен приёмки, живая сверка), а не текстовый
   линт заявок ДО работы; НЕ закрывал их этим рычагом, чтобы не выдумывать
   покрытие — оставил как есть, кандидат для будущего прохода (см.
   `## ВОПРОСЫ`).
2. Построил проверку клаузой существующего линтера, как предписано —
   `check_zahod.py`, новая клауза **З14**, прямое обобщение уже описанного
   в докстринге механизма З6 (число без соседней команды) на нечисловые
   стативные предикаты. Код и подробное обоснование сужения — в самой
   клаузе (`_generator/tools/check_zahod.py`, секция «З14»).
3. 🔴 **Калибровка на живом эталоне ДО объявления готовым.** Прогнал ЧЕРЕЗ
   САМ ИНСТРУМЕНТ (`check_zahod.py`, не черновой скрипт) на 27 живых файлах:
   25 свежих `kod_*.md` (по дате арки, тот же метод, что и у исходной
   калибровки З6) + 2 канонических калибровочных
   (`kod_tirazh-ocheredi.md`, `kod_ochered-domov.md`). **Срабатываний З14:
   0 из 27, ложных 0.** Первая черновая версия регэкспа (открытые основы
   `существ\w*`/`жив\w*` без точных границ) на том же корпусе ловила
   ложное на «Существительное не выбрасывается» (подстрока «существ» —
   не форма глагола) — сужена до точных словоформ ДО того, как клауза
   попала в `HARD_CHECKS`, тем же приёмом, что калибровка З6.
4. Две ловушки в `fixtures/zahod/PROGNAT.sh` (45 — отрицательная, факт без
   команды красит `rc=1`; 46 — положительная, тот же факт с backtick-
   командой рядом молчит + проверка `check_z14_fakt_o_mire in HARD_CHECKS`).
   Число ловушек в теле фикстуры: было 44, стало **46** (`grep -c '^echo
   "── ловушка'`), фикстура целиком зелёная (`ФИКСТУРЫ ЗЕЛЁНЫЕ`, rc=0).
5. Вписал носитель `` `check_zahod.py` З14 `` дословно в клетку каждой
   из 17 записей `zahody §2` (батчем, точным regex-замещением по id,
   17 из 17 совпало с первого прохода).
6. Коммит класса — двумя ходами, код в `materials`, клетки в `disciplina`
   (см. `КОММИТ`). Следующий класс не начинал.

**Живой прогон после КАЖДОГО рычага (критерий §0):** прогнал ОБА линтера
на свежесобранном заходе (`bootstrap_zahod.py` с синтетической аркой) —
`check_zahod.py`: rc=1, но красное — З2/З10 (пустые незаполненные слоты
синтетического вызова, ожидаемо, не про З14); **З14 в выводе НЕТ**.
`check_sborki.py`: rc=1 на несвязанном С4 (я не подставил реальный
критерий-тест), тоже без упоминания моей клаузы. Пробный файл удалил
после проверки, в git не попал. ⚠ Самонаходка: сборка пробника
(`bootstrap_zahod.py`) успела дописать его регистрацию в `_studio/docs/
KARTA.md` ДО того, как я удалил сам файл — заметил в `git status`,
осиротевшую строку убрал вручную тем же ходом (это моя оплошность, не
чужая; `git diff` подтверждает — сейчас файл идентичен `HEAD`, в коммит
ничего не попало).

**Доля машинного покрытия — посчитана классификатором, не на глаз.**
Официальный `pokrytie_rychagami.py` для имён `zahody`/`priyomka` сейчас
сломан (`doma/README.md §3.5`: «нужен `classify_06tekst()` из ветки
`zahod/pereschet-i-schetchik`, пока не влита — падает трейсбеком»), поэтому
собрал классификатор ПО ПРАВИЛУ `doma/README.md §3.3` дословно (машинный —
backtick-код/гейт-id/команда без «не построен» рядом; процедурный —
«правило»/«рецепт» без имени файла; не накрывается — «не существует»;
не накрыто — «нет»/«ничем»/«—»/«не построен»/«не реализован»/«не заведён»)
и прогнал по `git show HEAD:doma/zahody/POKRYTIE.md` (ДО) и рабочему дереву
(ПОСЛЕ), а также по `priyomka/POKRYTIE.md` (не трогал, для контроля):
```
ДО (zahody+priyomka):    машинный 23 записи · не накрыто 72
ПОСЛЕ (zahody+priyomka): машинный 44 записи · не накрыто 51
рост машинного покрытия: +21 запись
```
⚠ Мой классификатор — не тот же код, что официальный (тот недоступен для
этих имён домов), поэтому абсолютные числа могут на 1–2 разойтись с ручной
классификацией прошлых заходов (проверено: «ДО» по моему счётчику даёт
15 машинных в `zahody`, а в тексте `§1.1` документа написано 17 — разница
в паре пограничных клеток вида «частично: …», где граница «машинный/не
накрыто» спорна). ОТНОСИТЕЛЬНЫЙ рост (+21) устойчив к этой погрешности —
он равен числу клеток, которые я лично поменял (17+4), проверено `git
diff --stat` (`21 insertions, 21 deletions` в `doma/zahody/POKRYTIE.md`).

**Что НЕ трогал:** всё вне контракт-зоны; в `disciplina` есть чужие
незакоммиченные правки (`_studio/zhurnal/2026-08-13_disciplina-zahodov/
PLAN.md`, `SESSIYA.md`, `doma/zahody/RAZVITIE.md`,
`skills/disciplina-zahod/SKILL.md`, метки времени 01:31–04:23, ДО начала
моей сессии) — не мои, не коммитил, не трогал вовсе (`git show --stat`
подтверждает: в обоих моих коммитах только свои пути).

**Результат верификатора:** неприменимо — операция не лоссовая (только
добавления в код/таблицу, ничего не двигал/не терял/не жал).

**Время прогона + токены:** неприменимо, канал `app`, снимать неоткуда.

**ПОВТОРЯЕМОСТЬ находок:**
- Расхождение «26 записей заявлено — 17 нашёл грепом» — заход, а не
  запись: если аналитик пишет число класса на глаз, а не командой, это
  KONSTITUCIYA §10 в чистом виде и повторится на каждом следующем рычаге,
  если не сверять грепом перед стройкой (я сверял).
- 5 записей `priyomka` с похожим текстом, но другим механизмом (сверка
  ОТЧЁТА с АРТЕФАКТОМ, не заявки с диском) — законно уходит пунктом
  очереди в `## ВОПРОСЫ`, не заход: это не ошибка, а честная граница
  предмета Рычага 1.

**Необратимое:** необратимого нет.

**АРТЕФАКТ:** артефакта нет — эта работа кодовая (линтер + фикстура +
клетки таблицы), не сборка документа/HTML/PDF.

**КОММИТ:**
1. `materials` (`zahod/rychagi`): `728eb6a` — «_generator/tools: Рычаг 1 —
   З14 «факт о мире» + быстрые закрытия по --intervyu/З6-hard» ·
   `git_zona.py check --zone _generator/tools/` → ✅ · `git show --stat`
   → только `check_zahod.py` и `fixtures/zahod/PROGNAT.sh`.
2. `disciplina` (`main`): `3e936e0` — «zahody: Рычаг 1 (check_zahod.py
   З14) закрывает 17 записей + 4 быстрых закрытия» · `git show --stat` →
   только `doma/zahody/POKRYTIE.md`.
Оба коммита прошли pre-commit хук штатно (фикстура `fixtures/zahod/
PROGNAT.sh` прогналась целиком внутри хука при коммите 1, все 46 ловушек
зелёные), `--no-verify` не понадобился ни разу за сессию.

**Открытое «возвращаться»:** Рычаги 2–9 не начаты (8 из 9); 5 похожих
записей `priyomka` (см. `ПОВТОРЯЕМОСТЬ`); `Р5-114`/`КРТ-РАЗН-35`/
`КРТ-РАЗН-71` остаются Рычагу 4; 22 «прочих» не разобраны; 2 невлитые
ветки-надгробья (`zahod/nositeli-gen`, `zahod/nositel`) блокированы чужим
некоммиченным мусором приёмки, не мной.
