# Канал исполнителя — shablony-odin-dom (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — правка трёхстрочная, но шов несущий: девять фикстур подменяют `GIT_ZONA_REPO`, и какие из них после правки начнут читать боевой шаблон вместо временного, заранее не расписать — это надо увидеть по ходу.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — правка трёхстрочная, но шов несущий: какие из девяти фикстур после правки поедут, заранее не расписать.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/shablony.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_shablony-odin-dom.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Подготовка, до всяких действий: python3 _generator/tools/git_zona.py worktree add shablony --branch zahod/shablony-odin-dom, затем чтение захода,
затем секция ## ПЛАН внутри него.

ПЕРВЫМ ходом — СУБАГЕНТ на доделку git-дисциплины (§0.1: очередь заявок, влития в основную, гашение отработавших папок). Дождись его отчёта и только ПОТОМ берись за задачу захода: это порядок, а не пожелание — сам ты эти пункты не исполняешь, и до отчёта субагента основная работа не начинается.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-14_rychagi-zahodov`
Область: «_studio/zhurnal/2026-08-14_rychagi-zahodov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/magical-elegant-clarke/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. `materials/_generator/tools/` — общий движок на два репозитория: сами инструменты лежат здесь, а работать умеют и по соседнему репозиторию `disciplina` через переменную `GIT_ZONA_REPO` (реестр корней — `korni.py`). Прошлый этап: расшивка вшитого литерала `_studio` доведена до реестра, и `--koren` у `bootstrap_arka.py` уже работает — но ШАБЛОНЫ остались привязаны к `GIT_ZONA_REPO`, а не к репозиторию инструментов. Следствие поймано живьём 15.08: у `disciplina` нет `_TEMPLATE-zahod.md` вовсе (`bootstrap_zahod.py` падает «шаблон не найден»), а копия `_TEMPLATE-arka` там протухла — несёт старый греп-гейт `^ЦЕНА: `, починенный в `materials` 06.08. ЦЕЛЬ: сделать репозиторий инструментов единственным домом шаблонов, и закрыть это ловушкой, которая краснеет на старом коде.
Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: получишь правку `korni.py` и её ловушку, но НЕ получишь прогона остальных восьми фикстур — тогда так и скажи, назвав, каких именно.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-15) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Шаблоны арки и захода живут В ОДНОМ доме — в репозитории ИНСТРУМЕНТОВ (materials), а не в том, на который указывает GIT_ZONA_REPO. Владелец 15.08 выбрал этот вариант из трёх предложенных
2. Повод: у репозитория disciplina нет _TEMPLATE-zahod.md вовсе, а копия _TEMPLATE-arka там ПРОТУХЛА — несёт старый греп-гейт '^ЦЕНА: ', который на арке 2026-07-25_lekcia-1 дал ложный зелёный на 13 уроках из 19 и был починен в materials 06.08
3. Эта правка БЛОКИРУЕТ сборку заходов З-Б и З-В арки disciplina/_studio/zhurnal/2026-08-15_skill-kachestva — они собираются в Cowork ПОСЛЕ приёмки, а не этим заходом
4. Удаление протухшей копии шаблона в disciplina и мусора (tools/_pp_tmp.py, _studio/zhurnal/.probe, kod_dom-shablonov.md) в ЭТОТ заход НЕ входит — уходит в заход З-А, который владелец отложил на конец

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/shablony`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/shablony-odin-dom`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add shablony --branch zahod/shablony-odin-dom
  cd ../materials-wt/shablony
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/korni.py` `_generator/tools/fixtures/korni/` `_studio/docs/KARTA.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПЕРВЫМ ходом — СУБАГЕНТ на доделку git-дисциплины (§0.1: очередь заявок, влития в основную, гашение отработавших папок). Дождись его отчёта и только ПОТОМ берись за задачу захода: это порядок, а не пожелание — сам ты эти пункты не исполняешь, и до отчёта субагента основная работа не начинается.** *Почему это стоит отдельной строкой, хотя ниже написано то же самое: оговорка-исключение уже стояла во всех трёх местах — и пять исполнителей подряд контур всё равно не исполнили, потому что нигде не было сказано ЧЬИМИ РУКАМИ и В КАКОЙ МОМЕНТ. Цена: четыре ветки, принятые приёмкой, не влиты ни одна (замер 2026-08-15).*

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone _generator/tools/korni.py --zone _generator/tools/fixtures/korni/ --zone _studio/docs/KARTA.md --kommitit '_generator/tools/korni.py — правка шва' --kommitit '_generator/tools/fixtures/korni/ — ловушка на новое поведение шаблона' --kommitit '_studio/docs/KARTA.md — в ТОТ ЖЕ коммит, если звал register_doc.py' --zakryt 'после приёмки гасятся ветка zahod/shablony-odin-dom и рабочая папка ../materials-wt/shablony' --zakryt 'НЕПРИКОСНОВЕННЫ девять невлитых веток materials (zahod/pravila-kadra, zahod/sluzhebnye-slajdy и семь прочих): ни одна не касается шва — проверено '"'"'git diff --name-only main..<ветка> -- _generator/tools/korni.py _generator/tools/bootstrap_arka.py _generator/tools/bootstrap_zahod.py _generator/tools/fixtures/korni'"'"' → 0 для всех девяти, 2026-08-15'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/shablony-odin-dom`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/korni.py` `_generator/tools/fixtures/korni/` `_studio/docs/KARTA.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `_generator/tools/korni.py` целиком (шов — функция `шаблон()` в конце файла и константа `REPO` в начале) · `_generator/tools/bootstrap_arka.py` строки 30–40 и 118–135 (точка вызова) · `_generator/tools/bootstrap_zahod.py` строки 70–80 и 1190–1200 (вторая точка вызова) · `_generator/tools/fixtures/korni/PROGNAT.sh` целиком. Больше ничего; проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
Сделать репозиторий ИНСТРУМЕНТОВ единственным домом шаблонов `_TEMPLATE-arka/` и `_TEMPLATE-zahod.md`.

1. **Развести два разных вопроса отдельными константами.** Сегодня `korni.шаблон()` собирает путь от `REPO`, а `REPO` переключается переменной `GIT_ZONA_REPO`. Но «в каком репозитории я работаю» и «где лежат шаблоны инструментов» — разные вопросы, и второй от `GIT_ZONA_REPO` не зависит. Завести отдельную константу «репозиторий инструментов» = `Path(__file__).resolve().parents[2]`, **не читающую окружение**, и собирать `шаблон()` от неё. 🔴 Рядом — комментарий, объясняющий, ПОЧЕМУ это другой вопрос, чем `REPO`: ровно тем же приёмом, каким в этом же файле отдельной строкой объяснён `KANON_FAJL`. Без объяснения следующий исполнитель схлопнет две константы обратно в одну, «убирая дублирование».
2. **Формула `parents[2]`, а не `.git`** — по той же причине, что уже записана у `REPO`: фикстуры копируют инструменты в `$T/_generator/tools/`, и `parents[2]` честно указывает на `$T` ещё до того, как там появится git.
3. **Найти фикстуры, которые правка сдвигает.** Шаблон трогают девять: `dvizhok`, `gigiena`, `git_zona`, `korni`, `nablyudenia`, `priyomka`, `register_doc`, `sessiya`, `zahod` (снято `grep -rl 'TEMPLATE\|шаблон' _generator/tools/fixtures/`, 2026-08-15 — пересчитай первым ходом). Опасен один класс: фикстура ставит `GIT_ZONA_REPO` во ВРЕМЕННОЕ дерево, а инструменты гоняет из БОЕВОГО `_generator/tools/` — такая после правки начнёт читать боевой шаблон вместо временного. Найти такие и назвать поимённо в отчёте; сломались — починить в пределах зоны, вышло за зону — в отчёт, не трогать.
4. **Завести ловушку в `_generator/tools/fixtures/korni/PROGNAT.sh`** (сейчас там 13 ловушек — пересчитай): при `GIT_ZONA_REPO`, указывающем на дерево БЕЗ `_studio/zhurnal/_TEMPLATE-*`, `korni.шаблон('_TEMPLATE-zahod.md')` обязан вернуть путь ВНУТРИ репозитория инструментов и этот файл обязан существовать.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. `bash _generator/tools/fixtures/korni/PROGNAT.sh; echo $?` → `0`, и число ловушек выросло ровно на новую (приведи оба числа, до и после).
2. 🔴 **Новая ловушка КРАСНЕЕТ на старом коде.** Временно откати правку `шаблон()`, прогони фикстуру, покажи `rc≠0` и текст падения, верни правку. Без этого пункта ловушка — гейт без гейта: зелёная потому, что ничего не проверяет.
3. **ЖИВОЙ ПРОГОН на реальном объекте** — не на фикстуре, а на настоящем соседнем репозитории, и без побочных эффектов:
   ```
   GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 -c "import sys; sys.path.insert(0,'_generator/tools'); import korni; \
     [print(n, korni.шаблон(n), korni.шаблон(n).exists()) for n in ('_TEMPLATE-arka','_TEMPLATE-zahod.md')]"
   ```
   Обе строки обязаны напечатать путь, лежащий внутри `materials`, и `True`. Путь внутри `disciplina` или `False` — критерий провален.
4. **Восемь остальных фикстур из п.3 задачи прогнаны**, у каждой назван `rc`. Вердикт несёт охват: «прогнано X из Y», а не «фикстуры зелёные».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). **Здесь верификатор НЕ нужен: операция не лоссовая** — ни один файл не двигается, не удаляется и не переименовывается, правка сводится к одной константе и одной функции. Роль верификатора берёт на себя пункт 2 критерия: ловушка обязана покраснеть на откаченной правке, и это проверка ДРУГИМ методом, а не самоподтверждение.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/korni.py _generator/tools/fixtures/korni/ _studio/docs/KARTA.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/korni.py _generator/tools/fixtures/korni/ _studio/docs/KARTA.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/korni.py` `_generator/tools/fixtures/korni/` `_studio/docs/KARTA.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/korni.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/korni/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/docs/KARTA.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### `bootstrap_zahod.py --zadanie-subagentu` печатает задание с НЕЗАПОЛНЕННОЙ дыркой в пункте 2
ЦЕНА: в пункте «ВЛИТЬ НАЗВАННЫЕ ВЕТКИ» стоял литерал `🔴 <НЕ ЗАПОЛНЕНО — чем заполнять: … флагом `--vlit <ветка>`. Вливать нечего — так и вписать явно>`, потому что команда сборки задания флага `--vlit` не несла. Инструмент напечатал ЗАДАНИЕ, а не диагноз: отдать его субагенту дословно (как велит §0.1 — «полный текст задания печатает команда») значило отправить контур с дыркой ровно в том пункте, ради которого он существует. Заполнял руками, сверяя с `--zakryt`. Субагент, получивший такое задание без правки, встаёт перед выбором «ничего не влить» или «выбрать самому» — а выбирать ему прямо запрещено тем же заданием («НЕ судить, годна ли чужая ветка»). Это тот же класс «два обязательных указания, невыполнимых разом», что уже стоил 27 обходов `--no-verify`. Чинится краснотой: команда, собирающая задание без `--vlit` и без явного «вливать нечего», обязана отказывать, а не печатать плейсхолдер.

### Одно и то же число невлитых веток названо в заходе ТРИЖДЫ и трижды по-разному
ЦЕНА: §0.1 говорит «вливать нечего … невлитых `zahod/*`-веток было 0»; `--zakryt` того же файла говорит «НЕПРИКОСНОВЕННЫ девять невлитых веток»; факт на прогоне — 7 (`git branch --no-merged | grep -c 'zahod/'`, снято мной и независимо субагентом, имена совпали). Заход честно предупреждает «число могло устареть» — но предупреждение не спасает от того, что два числа в ОДНОМ файле противоречат друг другу уже в момент сборки, до всякого устаревания. Исполнитель не может понять, какое из них — снимок, а какое — намерение, и первым ходом тратит проверку на разрешение противоречия. Чинится тем же приёмом, что уже записан в `KONSTITUCIYA §10`: в заход идёт КОМАНДА, а не число, — во всех трёх местах одна и та же.

### Список копирования инструментов в фикстурах — ручной, а их зависимости — нет
ЦЕНА: три фикстуры из девяти были КРАСНЫ на чистом `HEAD`, до единой моей правки: `korni` (rc=1), `git_zona` (rc=1), `register_doc` (rc=1), по 2–3 ❌ каждая. Корень один: `check_zahod.py` обзавёлся импортом `check_tool_contract` (строка 84), а списки `cp` в фикстурах о нём не знают — падает `ModuleNotFoundError`, через него `bootstrap_zahod.py`, через него всё, что его зовёт. Класс уже оплачен ДВАЖДЫ и оба раза записан комментарием в этих же файлах (`modeli.py` — «ЦЕНА 14.08: семь обходов `--no-verify` подряд», и тот же случай раньше в `fixtures/git_zona`). Третий повтор — не случайность, а свойство устройства: пока список ручной, каждый новый импорт любого из семи инструментов гасит фикстуры молча, и гасит их ИМЕННО на baseline, где красноту списывают на предшественника. Чинится не третьим комментарием, а тем, чтобы список копирования вычислялся из импортов (или чтобы фикстура падала с диагнозом «в списке нет модуля X», а не с трассировкой).
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Снимки первым ходом (числа захода устарели — пересчитано 2026-08-15):**
- `schet_nezakrytogo.py` по арке: инцидентов без вердикта 0 · долгов ЖИВ **39** (в заходе «н/д» — `DOLG.md` теперь читается) · уроков без ВЕРДИКТ 0 · очередь «ДОСТАВЛЕНО: нет» 0.
- `git branch --no-merged | grep -c 'zahod/'` → **7**, а не 0 (снимок захода) и не 9 (число в `--zakryt`). Ни одна не влита — все объявлены неприкосновенными; см. ОТЧЁТ, строка 2 субагента.
- Фикстур, трогающих шаблон: `grep -rl 'TEMPLATE\|шаблон' _generator/tools/fixtures/` → 10 попаданий, из них PROGNAT.sh — **9** (десятое — `sborka/zhivoj-teorkat/dist/index.html`, артефакт сборки, не фикстура). Список захода подтверждён поимённо.
- Ловушек в `fixtures/korni/PROGNAT.sh`: `grep -c '^# ── ЛОВУШКА'` → **12** (в заходе сказано 13 — пересчитано, число в задаче завышено на одну).

**Предпосылка проверена, и одна из них ложна.** Шапка фикстуры `korni/PROGNAT.sh` строка 25 объявляет «ВОСЕМЬ ЛОВУШЕК», а маркеров 12 — комментарий отстал от кода. Правки шапки НЕ делаю (это не моя задача), но число «до» беру командой, а не из текста.

**Шаги:**
1. `korni.py`: завести константу `ИНСТРУМЕНТЫ = Path(__file__).resolve().parents[2]`, **не читающую окружение**, рядом — комментарий в том же приёме, что у `KANON_FAJL`: почему это ДРУГОЙ вопрос, чем `REPO`. `шаблон()` собирать от неё. Формула `parents[2]`, не `.git` — по причине, уже записанной у `REPO` (фикстуры копируют инструменты в `$T/_generator/tools/` до `git init`).
2. Найти опасный класс: фикстура ставит `GIT_ZONA_REPO` во ВРЕМЕННОЕ дерево, а инструмент гоняет из БОЕВОГО `$TOOLS/` — такая после правки начнёт читать боевой шаблон. По разведке кандидаты: `zahod` (≈20 вызовов `GIT_ZONA_REPO="$Rn" python3 "$TOOLS/bootstrap_zahod.py"`), `priyomka` (4 вызова через `python3 -c`), `git_zona`. Проверять прогоном, а не чтением.
3. Ловушка в `fixtures/korni/PROGNAT.sh`: при `GIT_ZONA_REPO`, указывающем на дерево БЕЗ `_studio/zhurnal/_TEMPLATE-*`, `korni.шаблон('_TEMPLATE-zahod.md')` обязан вернуть путь ВНУТРИ репозитория инструментов и файл обязан существовать.
4. Критерий 2 (краснота на старом коде): временно вернуть `REPO` в `шаблон()`, прогнать, показать `rc≠0` и текст падения, вернуть правку.
5. Критерий 3: живой прогон на `disciplina`. 6. Критерий 4: прогнать все девять фикстур, назвать `rc` каждой.

**Критерий готовности не оспариваю** — он вычислим и может провалиться. Одна оговорка к нему: п.1 требует «число ловушек выросло ровно на новую», а в задаче названо 13 при фактических 12 — беру фактическое, привожу оба числа командой.

**Расхождение, замеченное на входе (не чиню, докладываю):** моя ветка отпочкована ДО того, как субагент забрал хвост Cowork (`8e44a53`), потому что стартовое сообщение велело завести рабочую папку ПЕРВЫМ ходом, а §0.1 — ПОСЛЕ влития. Расходится ровно один файл — `_studio/docs/KARTA.md`. `register_doc.py` мне не нужен (новых `.md` не завожу), поэтому `KARTA.md` не трогаю вовсе и конфликта при влитии не создаю.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Фикстуры `git_zona` и `register_doc` красны на чистом `HEAD` по той же причине, что была у `korni` (нет `check_tool_contract.py` в списке `cp`): `git_zona` — 3 ❌ («bootstrap_zahod.py не создал заход», «ЧАСТЬ D СЛОМАНА: урок 23 вернулся», «worktree не заведён»), `register_doc` — 3 ❌ («не создал заход» ×2, «ТИХАЯ ЗАГЛУШКА, вхождений пометки: 0»). Обе ВНЕ моей зоны — не трогал. Правка однострочная и та же, что я сделал у себя: дописать `"$TOOLS/check_tool_contract.py"` в список копирования.
   ДОМ: _generator/tools/fixtures/git_zona/PROGNAT.sh, _generator/tools/fixtures/register_doc/PROGNAT.sh
   ДОСТАВЛЕНО: нет
2. Шапка `fixtures/korni/PROGNAT.sh` (строка 25) объявляет «ВОСЕМЬ ЛОВУШЕК», а фактически их было 12 и стало 13. Комментарий отстал от кода на пять ловушек; число ловушек — ровно тот случай, что `KONSTITUCIYA §10` велит считать командой, а не вписывать. Правка шапки в задачу не входила — не трогал.
   ДОМ: _generator/tools/fixtures/korni/PROGNAT.sh
   ДОСТАВЛЕНО: нет
3. Очередь заявок субагента: 4 заявки, закрыто 0, застряло 4. Две из них (`2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026`, `2026-08-12T0359-tipy-slajdov-py-2-primer-3`) висят с 12.08 и получают ОДИН И ТОТ ЖЕ отказ от каждого контура — 18-й и 16-й раз подряд. Это поломка маршрутизации, а не судьба заявки: их дом — заход-разработчик, а не очередь гит-контура, которой запрещено править код.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
4. `fixtures/zahod/PROGNAT.sh` строка 474 копирует шаблон из `"$TOOLS/../_studio/zhurnal/…"`, то есть из `_generator/_studio/…`, которого не существует; работает только откат `2>/dev/null ||` на строке 475. Мёртвая половина условия, не мой класс дефекта и вне зоны — не трогал.
   ДОМ: _generator/tools/fixtures/zahod/PROGNAT.sh
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)
### ГИТ-КОНТУР — шесть строк субагента, вставлены ДОСЛОВНО

1. **заявок было 4, закрыто 0, застряло 4** — `2026-08-15T1920-1-origin-2026-08-15-22` (п.1 «вывезти на origin» ИСПОЛНЕН, п.2 «опубликовать L2» упёрся), `2026-08-15T1544-2026-08-15-materials-studio-zhurnal` (содержательная зона захода + чужой репозиторий `disciplina/`), `2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026` (правка кода; 18-й одинаковый отказ подряд), `2026-08-12T0359-tipy-slajdov-py-2-primer-3` (решение по типологии; 16-й одинаковый отказ). Две последние висят с 12.08 и получают один и тот же отказ от каждого контура — это поломка маршрутизации, а не судьба заявки: их дом — заход-разработчик по фазе 2, а не очередь гит-контура.
2. **влито в основную 0 веток из 0 названных.** Основная ветка — `arka/mat-kostyak`, имя снято `git_zona.py: osnovnaya_vetka()` (не вписано на глаз); она же вычекаучена в главной папке. `git --no-optional-locks branch --no-merged` даёт **7** невлитых, не 9: `zahod/arhitektura-doma`, `zahod/dovodka-solvera`, `zahod/kadr-uzor-i-vmeshchenie`, `zahod/rychagi`, `zahod/rychagi-2`, `zahod/rychagi-3`, `zahod/vid-blokov-vnedrenie`. Все объявлены неприкосновенными — ни одна не тронута. Сверх плана по заявке: `vyvezti --yes` вывез `arka/mat-kostyak` на origin, 23 коммита, локально-только не осталось ничего.
3. **хвост Cowork: забран коммитом `8e44a53`** — 6 путей: `_studio/docs/KARTA.md` + `_studio/zhurnal/2026-08-10_dizajn-i-metriki/VYGRUZKA-2026-08-15{,-2,-3,-4,-5}.md` (KARTA регистрировала ровно эти пять выгрузок — в тот же коммит). `INCIDENTY.md`/`VERDIKTY.md` на старте были чисты. Вне git осталось 12 путей (снято `check`): `_generator/tools/sdelat_handoff.py`, `_studio/zhurnal/2026-08-10_dizajn-i-metriki/{HANDOFF-2026-08-16.md, kod_kadr-uzor-i-vmeshchenie.md, SESSIYA.md, UROKI-FABRIKE.md}`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/{AUDIT-BAZA-2026-08-15.md, HANDOFF-2026-08-16-kachestvo.md, TZ-desyatyj-skill.md, kod_dom-shablonov.md, kod_shablony-odin-dom.md}`, `ucheniki/maksim/{ZHURNAL.md, dz/dz-09-raskraska-invariant.html}`. `UROKI-FABRIKE.md` отрос по ходу и НЕ забран намеренно: запись парная к незакоммиченной правке `sdelat_handoff.py` живой соседней сессии, а слияний, которые он мог бы заблокировать, в этом заходе нет. `korni.py` и `fixtures/korni/` в грязных не значатся вовсе — шов заход ещё не трогал.
4. **погашено: НИЧЕГО**, `poteri`/`zakryt-vetku` не звал ни разу, rc не снимал. НЕ погашены и живы: ветка `zahod/shablony-odin-dom` и папка `../materials-wt/shablony` (приёмки не было, заход идёт), а также все 7 невлитых веток и их рабочие папки — `worktree list` по-прежнему 14 строк, как до меня.
5. **необратимое:** (а) устаревший черновик `_studio/.commit-plan` от 2026-08-12 съеден штатным ходом `commit` · копия лежит в `/private/tmp/claude-501/-Users-ivanyakovlev/0dd7d821-b9bf-47f5-b5e4-de4435985785/scratchpad/commit-plan.BACKUP-2026-08-15`, восстанавливается копированием обратно; (б) push 23 коммитов `arka/mat-kostyak` на origin — по заявке, откатывается только force-push владельцем.

выдано 6 позиций из 6 найденных

### ЧТО СДЕЛАНО И ЗАЧЕМ

**1. Развёл два вопроса отдельными константами** (`_generator/tools/korni.py`). Заведена `ИНСТРУМЕНТЫ = Path(__file__).resolve().parents[2]`, окружение НЕ читающая, и `шаблон()` собирается от неё, а не от `REPO`. Рядом — комментарий тем же приёмом, что у `KANON_FAJL`: почему это другой вопрос («в каком дереве я работаю» ≠ «где лежат шаблоны инструмента»), с ценой 15.08 и с явным «схлопнешь обратно — вернёшь дефект». Формула `parents[2]`, а не `.git`, по причине, уже записанной у `REPO`.

**2. Ловушка 9 «дом шаблонов»** в `_generator/tools/fixtures/korni/PROGNAT.sh`: `GIT_ZONA_REPO` показывает на дерево, где журнал есть, а `_TEMPLATE-*` нет (ровно состояние `disciplina`); ответ обязан лежать внутри дерева ИНСТРУМЕНТОВ **и** файл обязан существовать — проверяются обе половины, потому что верный путь в дерево без файла первую половину проходит. Диагноз разделяет три исхода: уехал за `GIT_ZONA_REPO` · внутри инструментов, но файла нет · не там и не там.

**3. Наследованный долг, починенный В ПРЕДЕЛАХ ЗОНЫ (называю отдельно — я его не создавал).** На чистом `HEAD`, ДО единой моей правки, фикстура `korni` была КРАСНОЙ: `check_zahod.py` импортирует `check_tool_contract`, которого нет в списке `cp` фикстуры → `ModuleNotFoundError` → ловушки 5 и 6 давали ❌. Без этого критерий 1 («rc → 0») недостижим в принципе, чинить или не чинить — выбора нет. Дописана одна строка в список копирования плюс комментарий, что это тот же класс, что уже оплачен с `modeli.py`. Если приёмка считает это выходом за задачу — правка снимается отдельно, но тогда критерий 1 надо переписать.

### КАК ПРОВЕРИЛ — по критериям, каждый числом

**Критерий 1 — `bash _generator/tools/fixtures/korni/PROGNAT.sh; echo $?` → `0`.** Ловушек ДО: **12**, ПОСЛЕ: **13** (`grep -c '^# ── ЛОВУШКА'`, оба числа сняты командой, не памятью). Выросло ровно на новую. ⚠ В задаче названо «сейчас там 13 ловушек» — фактически было 12; взял фактическое, как и велено пересчитать.

**Критерий 2 — ловушка КРАСНЕЕТ на старом коде.** Правка `шаблон()` временно откачена якорной заменой (abort при ≠1 совпадении), прогон дал `rc=1` и текст падения:
```
❌ ДОМ ШАБЛОНОВ УЕХАЛ ЗА GIT_ZONA_REPO — _TEMPLATE-zahod.md взят из дерева без шаблонов: /var/…/tmp.eQG8fD2rD5/net-shablonov/_studio/zhurnal/_TEMPLATE-zahod.md
❌ ДОМ ШАБЛОНОВ УЕХАЛ ЗА GIT_ZONA_REPO — _TEMPLATE-arka взят из дерева без шаблонов: /var/…/tmp.eQG8fD2rD5/net-shablonov/_studio/zhurnal/_TEMPLATE-arka
ФИКСТУРЫ КРАСНЫЕ
```
Правка возвращена из бэкапа, `git diff --stat` сверен.

**Критерий 3 — ЖИВОЙ ПРОГОН на реальной `disciplina`.** ДО правки (baseline, снят до всякой работы):
```
_TEMPLATE-arka       …/disciplina/_studio/zhurnal/_TEMPLATE-arka        True   ← протухшая копия
_TEMPLATE-zahod.md   …/disciplina/_studio/zhurnal/_TEMPLATE-zahod.md    False  ← нет вовсе
```
ПОСЛЕ правки — обе строки `True` и обе из репозитория инструментов:
```
_TEMPLATE-arka       /Users/…/materials-wt/shablony/_studio/zhurnal/_TEMPLATE-arka       True
_TEMPLATE-zahod.md   /Users/…/materials-wt/shablony/_studio/zhurnal/_TEMPLATE-zahod.md   True
```
🔴 **Оговорка к букве критерия, а не к его сути:** критерий требует «путь внутри `materials`», а напечатан путь внутри `materials-wt/shablony` — потому что заход ОБЯЗАН работать в рабочей папке, и `parents[2]` честно даёт её. Это и есть верное поведение: дом шаблонов — тот checkout, из которого запущен инструмент. Запрещённые исходы («путь внутри `disciplina`» / `False`) не наступили ни разу; в главной папке та же формула даст `/Users/…/materials`. Шов напечатан явно: `REPO = …/disciplina`, `ИНСТРУМЕНТЫ = …/materials-wt/shablony`, «шаблон в дереве инструментов: True», «шаблон в дереве GIT_ZONA_REPO: False». Побочных эффектов в `disciplina` нет — её `status --porcelain` показывает только чужую грязь, существовавшую до прогона (`README.md`, `SKILL.md`, `.probe`, `_pp_tmp.py` — тот самый мусор, отложенный интервью в заход З-А).

**Критерий 4 — ОХВАТ: прогнано 9 фикстур из 9.** `rc` каждой, до и после:

| фикстура | rc ДО | rc ПОСЛЕ | что это значит |
|---|---|---|---|
| dvizhok | 0 | 0 | зелёная |
| gigiena | 0 | 0 | зелёная |
| git_zona | **1** | **1** | красна и ДО меня, наследованный долг, вне зоны |
| korni | **1** | **0** | была красна на baseline, долг починен в зоне |
| nablyudenia | 0 | 0 | зелёная |
| priyomka | 0 | 0 | зелёная |
| register_doc | **1** | **1** | красна и ДО меня, наследованный долг, вне зоны |
| sessiya | 0 | 0 | зелёная |
| zahod | 0 | 0 | зелёная, 71 ✅ |

У `git_zona` и `register_doc` список ❌ ПОСЛЕ построчно совпадает со списком ДО — краснота не моя и не выросла.

### ОПАСНЫЙ КЛАСС — НАЗВАН ПОИМЁННО, И НАЗВАН ПО ИЗМЕРЕНИЮ, А НЕ ПО ЧТЕНИЮ

Задача велела найти фикстуры, которые ставят `GIT_ZONA_REPO` во временное дерево, а инструменты гоняют из БОЕВОГО `_generator/tools/` — такие после правки начинают читать боевой шаблон. Греп по вызовам давал неверный ответ, поэтому мерил: временно инструментировал `шаблон()` логом `(REPO, ИНСТРУМЕНТЫ, возвращённый путь)`, прогнал все девять на НОВОМ коде и те же на СТАРОМ, сравнил по фактически возвращённому пути. Зонд снят, `korni.py` возвращён из бэкапа (`grep -c ZOND_LOG` → 0).

| фикстура | вызовов `шаблон()` | читают боевой шаблон СТАРО → НОВО | сдвинула ли правка |
|---|---|---|---|
| **zahod** | 27 | 3 → **27** | 🔴 ДА |
| **dvizhok** | 16 | 0 → **16** | 🔴 ДА |
| gigiena | 2 | 1 → 1 | нет |
| git_zona | 2 | 2 → 2 | нет |
| korni | 5 | 0 → 0 | нет |
| register_doc | 1 | 0 → 0 | нет |
| nablyudenia · priyomka · sessiya | 0 | 0 → 0 | шаблон не трогают вовсе |

**Сдвинуты две — `zahod` и `dvizhok`. Обе ВНЕ моей зоны, обе остались зелёными, и обе я проверил на «не онемел ли гейт молча», потому что `rc=0` этого сам по себе не доказывает:**

- **`zahod`** — сдвиг семантически ПУСТ. `sobrat_repo()` кладёт в `$R` побайтовую КОПИЮ боевого `_TEMPLATE-zahod.md` и нигде её не мутирует (`grep` по `_TEMPLATE-zahod` даёт две строки, обе — тот самый `cp`). Инструмент читает теперь оригинал вместо копии оригинала. 71 ✅, 0 ❌.
- **`dvizhok`** — сдвиг РЕАЛЕН, но инертен, и это надо было доказать: фикстура пишет в `$R3` заведомо ПОЛОМАННЫЙ шаблон (без якоря токенов) и требует, чтобы генератор его ОТКЛОНИЛ. Уедь чтение на боевой шаблон — якорь там есть, генератор принял бы, и ловушка позеленела бы ВПУСТУЮ: ровно тот «молчащий гейт», ради которого затеян реестр. Не произошло: обёртка `zvat.py` переопределяет `bz.TEMPLATE` на дерево `$R` уже после импорта, поэтому 16 сдвинутых вызовов — это импортное значение, которое немедленно выбрасывается. Доказательство не рассуждением, а выводом: после правки ловушка по-прежнему печатает `✅ шаблон без якоря токенов ОТКЛОНЁН` и `✅ при отказе по якорю файл НЕ создан`. Отклонить она могла только поломанный шаблон из `$R3`.

**Ничего не сломалось — проверено 9 из 9; сдвинуто 2 из 9, обе разобраны поимённо и обе остались рабочими.**

### ЧТО НЕ ТРОГАЛ
`bootstrap_arka.py`, `bootstrap_zahod.py` (обе точки вызова `шаблон()` — читал, не правил: правка сделана так, чтобы они не требовали изменений) · фикстуры `git_zona`, `register_doc`, `zahod`, `dvizhok`, `gigiena`, `nablyudenia`, `priyomka`, `sessiya` · `_studio/docs/KARTA.md` (новых `.md` не заводил, `register_doc.py` не звал — потому файл и не в коммите) · шапка `fixtures/korni/PROGNAT.sh` со списком «ВОСЕМЬ ЛОВУШЕК» (отстала от кода, но правка шапки в задачу не входит — в ВОПРОСЫ) · протухшая копия шаблона в `disciplina` и мусор там же (интервью, п.4: уходит в заход З-А).

### ВЕРИФИКАТОР
Заход объявил его неприменимым («операция не лоссовая»), и это подтвердилось: ни один файл не двигался, не удалялся, не переименовывался. Роль верификатора взял критерий 2 — ловушка обязана покраснеть на откаченной правке, и покраснела (`rc=1`, обе строки). Сверх того применён ВТОРОЙ независимый метод, которого заход не требовал: инструментальный замер разрешения `шаблон()` на старом и новом коде по всем девяти фикстурам — именно он поймал, что мой первый вариант замера сравнивал не ту колонку и потому давал ложное «правка не сдвинула никого».

### ГИГИЕНА Г1–Г6
- **Г1.** `git_zona.py check --zone` по всем трём путям зоны → ✅ ✅ ✅ («работа доехала в git, вне git ничего нет»).
- **Г2.** Неприменимо, и это проверено, а не предположено: все пути зоны лежат внутри `materials`; за его пределы зона не выходила. В `disciplina` только читал — её `status --porcelain` показывает исключительно чужую грязь, бывшую там до прогона.
- **Г3.** `git branch --no-merged arka/mat-kostyak | grep -c 'zahod/'`: на входе **7**, после коммита **8**. Выросло на одну — на мою собственную `zahod/shablony-odin-dom`, и это законно: до коммита она была пуста. Чужих не прибавилось.
- **Г4.** Неприменимо: ни одного нового `.py` не заведено (правка — в существующем `korni.py`).
- **Г5.** Неприменимо: ни одного нового `.md` не заведено, `register_doc.py` не звал.
- **Г6.** `git show --stat` — ровно два пути, оба мои: `_generator/tools/fixtures/korni/PROGNAT.sh` (+60/−1), `_generator/tools/korni.py` (+39/−2). Чужого нет.

### НЕОБРАТИМОЕ (мой заход)
**Необратимого нет.** Ни одного удаления, переименования, перемещения или `reset`/`checkout` поверх несохранённого. Два временных отката `шаблон()` (критерий 2 и замер на старом коде) сделаны якорной заменой с abort при ≠1 совпадении, оба возвращены копированием из бэкапа `korni.py.PRAVKA`, состояние сверено `git diff --stat` и `grep -c ZOND_LOG` → 0. Необратимое СУБАГЕНТА — отдельно, в строке 5 его отчёта выше (съеденный `.commit-plan` с копией в scratchpad и push 23 коммитов на origin).

### ПОВТОРЯЕМОСТЬ НАХОДОК
**Повторятся на СЛЕДУЮЩЕМ заходе (класс НЕМЕДЛЕННОЕ — чинить ДО следующего прогона, а не записью в очередь):**
- **Плейсхолдер `<НЕ ЗАПОЛНЕНО>` в задании субагенту** — повторится на КАЖДОМ заходе, который собирают без `--vlit`, то есть на каждом, где вливать нечего. Это самый частый случай.
- **Наследованный `ModuleNotFoundError` в фикстурах** — повторится на каждом заходе, чей критерий трогает `git_zona` или `register_doc` (у меня был `korni` — совпало). Пока список `cp` ручной, повторится и на любой следующей фикстуре при следующем новом импорте.
- **Противоречие «стартовое сообщение велит завести рабочую папку ПЕРВЫМ ходом» ↔ «§0.1 велит завести её ПОСЛЕ влития»** — повторится дословно на каждом заходе, потому что оба текста вшиты в сборку. У меня обошлось (ветка отпочковалась до `8e44a53`, разошёлся только `KARTA.md`, а он мне не понадобился); у захода, которому понадобится `register_doc.py`, это даст конфликт при влитии.

**Не повторится, законно уходит в очередь:** число ловушек 12 vs 13 в задаче и «ВОСЕМЬ» в шапке фикстуры (разовое расхождение конкретного файла) · мёртвая половина `cp` в `fixtures/zahod` строка 474.

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** на канале `app` НЕПРИМЕНИМО, снимать неоткуда.

**АРТЕФАКТ:** артефакта-сборки нет: заход кодовый, HTML/PDF/колода не собирались. Проверяемый результат — два файла и одна команда:
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/shablony/_generator/tools/korni.py` — редактор (константа `ИНСТРУМЕНТЫ`, функция `шаблон()`)
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/shablony/_generator/tools/fixtures/korni/PROGNAT.sh` — редактор (ловушка 9 «дом шаблонов»)
Открыть результат в действии: `bash _generator/tools/fixtures/korni/PROGNAT.sh` из корня рабочей папки → `ФИКСТУРЫ ЗЕЛЁНЫЕ`, `rc=0`.
**КОММИТ:** `2cec3a0` — `shablony: дом шаблонов — репозиторий инструментов, а не GIT_ZONA_REPO; ловушка на новое поведение` · `git_zona.py check --zone` по всем трём путям зоны → ✅ ✅ ✅. Хук `pre-commit` прошёл ЗЕЛЁНЫМ, `--no-verify` не звался ни разу.
