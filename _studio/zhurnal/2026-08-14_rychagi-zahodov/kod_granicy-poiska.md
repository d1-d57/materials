# Канал исполнителя — granicy-poiska (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — в файле уже лежит образец (З19, разбирает те же греп-критерии), показать пальцем есть на что. Правило подтверждено замером 15.08 на заходе `ohvat-verdikta`.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — образец в файле есть (клауза З19), изобретать нечего; встретишь развилку — остановись и спроси.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/granicy-poiska.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_granicy-poiska.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add granicy-poiska --branch zahod/granicy-poiska, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-14_rychagi-zahodov`
Область: «_studio/zhurnal/2026-08-14_rychagi-zahodov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Фабрика заходов: генератор собирает задание исполнителю, гейты судят вернувшийся отчёт. Прошлый этап: закрыты пять гейтов, судивших не то, что заявляли, и три семьи инцидентов — старение числа, кириллический диапазон, охват у вердикта; семья одиночек разобрана вердиктами без заходов. ЦЕЛЬ: механическая часть семьи C — клауза «греп-критерий без ограничивающего пути».
Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели: получишь З20 с калибровкой, но НЕ охват по смыслу (Р5-47, Р5-56) и не границу задания субагенту (Р5-18).

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-15) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Берём из семьи C только то, что расписывается механикой: клауза «греп-критерий без ограничивающего пути» (Р5-66) и устойчивость признака к форме (Р5-57). Р5-47, Р5-56, Р5-18 в заход НЕ входят
2. Модель Sonnet — второй замер того же правила: берём дешёвую модель, когда в репозитории есть образец, на который можно показать пальцем (здесь это З19)

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/granicy-poiska`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/granicy-poiska`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add granicy-poiska --branch zahod/granicy-poiska
  cd ../materials-wt/granicy-poiska
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --vlit zahod/ohvat-verdikta --zone _generator/tools/ --kommitit 'зону _generator/tools/ целиком, включая фикстуру zahod' --zakryt 'свою ветку после влития — гасит приёмка, не заход'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

Что влить, названо аналитиком и уехало в задание субагента (1): `zahod/ohvat-verdikta`. Влитие идёт в ОСНОВНУЮ ветку, не в твою.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/granicy-poiska`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Одна новая клауза в `_generator/tools/check_zahod.py` — **З20 «греп-критерий без ограничивающего пути»**. Механика расписана ниже; **упёрся в развилку, которой здесь нет, — не выбирай сам: пиши в `## ВОПРОСЫ` и останавливайся.** Вопрос стоит паузы, неверно выбранная развилка — целого прохода.

### Что ловим

**Р5-66: «греп-критерий задан без ограничивающего пути».** Критерий готовности в заходе часто формулируют так: «`grep -c "фраза"` даёт 0». Без пути греп идёт по всему дереву — и находит совпадения в чужих файлах, в старых заходах, в собственном тексте задания. Исполнитель получает число, которое не про его работу, и либо гоняется за фантомом, либо принимает ложный ноль за успех.

**Живой случай, свежий: 15.08.** Клауза З19 (кириллический диапазон) даёт красное на файле-заходе, который дефект **цитирует** — потому что признак не отличает исполняемый критерий от примера в тексте. Это соседний класс той же болезни: **признак смотрит на форму строки, а не на её роль.** Тебе он понадобится — см. ниже.

### Механика

**Файл ровно один: `_generator/tools/check_zahod.py`.**

1. **Образец для подражания — клауза З19** (ищи по строке `З19`). Она уже разбирает греп-критерии внутри файла-захода: у неё есть регексп признака, есть отсечки, есть докстринг со слепыми зонами. Твоя З20 живёт рядом и смотрит на те же строки — **переиспользуй её разбор, а не пиши второй**. Дефект один, места два — этот же приём применён 15.08 между `check_tool_contract` и `check_zahod` (признак берётся импортом).

2. **Что красить.** Строка критерия содержит `grep` (в любой форме: `grep -c`, `grep -rn`, `rg`), и в этой же команде нет ни пути, ни `--include`, ни ограничения каталогом. Красное — только в секции КРИТЕРИЯ и ЗАДАЧИ; в прозе, в цитатах и в блоках-примерах — нет.

3. **🔴 Отсечка, без которой клауза станет вредной.** Заход, который ЦИТИРУЕТ дефектный греп как пример (ровно как этот абзац), краснеть не должен. Признак роли, а не формы: команда внутри блока кода, следующая за словами «пример», «дефект», «было», «нельзя», — цитата. Не уверен, что отличишь, — **сделай отсечку заведомо широкой и назови это в докстринге слепой зоной**: пропустить дефект дешевле, чем красить верную работу. Гейт, красящий верное, обходят вместе со всей защитой.

4. **Ловушки в `_generator/tools/fixtures/zahod/PROGNAT.sh`:** греп без пути в критерии — красное; греп с путём — зелёное; тот же греп в цитате-примере — зелёное. Шапка числа ловушек не хранит, прогон печатает счёт сам.

5. **Записи дома** `~/Documents/GitHub/disciplina/doma/zahody/POKRYTIE.md §2`: у **Р5-66** рычаг стоит «нет» — перепиши на построенный носитель. **Р5-57** («автоматика ищет точную строку, живая формулировка иная по форме») — если твоя отсечка по роли её закрывает хотя бы частично, так и напиши, честно назвав, какая часть осталась; не закрывает — оставь «нет». **Это ДРУГОЙ репозиторий: отдельный коммит, хэш назови отдельно.**

### 🔴 КАЛИБРОВКА — половина работы

- прогони З20 по всему живому корпусу: `for f in _studio/zhurnal/*/kod_*.md; do python3 _generator/tools/check_zahod.py "$f"; done` — файлов около 300 (пересними число своей командой);
- назови **число красных**;
- **разбери поимённо пять**: истинные или ложные. Ложные → сужай признак и перепрогоняй;
- ⚠ **ложное красное здесь вероятнее, чем в прошлых клаузах**, потому что заходы полны команд в тексте задания. Если после двух сужений ложных всё ещё много — это сигнал, что признак выбран неверно: остановись и напиши в `## ВОПРОСЫ`, не подгоняй регексп бесконечно.

### 🔴 СТОП ДО ЦЕЛИ — чего в этом заходе НЕТ

Р5-47 («критерий охвата задан синтаксисом источника, теряет прозу»), Р5-56 («гейт слеп к неблочному содержимому») и Р5-18 («слишком узкая граница задания субагенту») — **не бери**. Первые две — про охват по смыслу, а не по форме; третья процедурная. Увидел, как их сделать — в `## ВОПРОСЫ`.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. **Живой прогон на реальном объекте:** З20 краснеет на заходе с грепом без пути в критерии (файл назови поимённо, найдя калибровкой) и молчит на заходе, где греп с путём.
2. **Цитата не краснеет:** на ЭТОМ файле-заходе (`kod_granicy-poiska.md`) — З20 **зелёная**, хотя в §2 выше дефектный греп процитирован дважды. Это отрицательный контроль, и он важнее положительного.
3. **Калибровка предъявлена числом:** прогон по всему корпусу, названо число красных, пять разобраны поимённо, ложных среди разобранных — **0**.
4. `sh _generator/tools/fixtures/zahod/PROGNAT.sh` → **rc=0**, ловушек больше, чем было (прогон печатает счёт сам).
5. `python3 _generator/tools/check_zahod.py <любой живой заход>` — без трейсбека.
6. Записи Р5-66 (и Р5-57, если закрылась) в доме `zahody` переписаны; коммит второго репозитория назван отдельным хэшем.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Не нужен — операция не лоссовая. Его роль играет калибровка §2.
⚠ **Где самопроверка врёт по построению:** пункт 2. Твой отрицательный контроль — этот самый файл, написанный аналитиком, а не тобой; на нём и проверяй. «Клауза краснеет на моём примере» и «клауза различает роль» — разные утверждения.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Развилка порядка, разрешена сам, не остановка.** Стартовое сообщение владельца говорит
«первым ходом — заведи рабочую папку», но §0.1 ГИТ-КОНТУР этого же файла явно требует
обратный порядок: сначала субагент вливает `zahod/ohvat-verdikta` в основную ветку, и
только потом заводится рабочая папка (иначе она отпочкуется от старой основной, и
влитый инструмент не окажется на диске — ровно тот сбой, что явно описан и оценён в
самом §0.1). Я уже завёл папку раньше времени по стартовой строке, но в ней не было ни
одного коммита — снял её (`worktree drop`), прогнал субагента гит-контура целиком,
затем завёл папку заново уже от свежей основной. Это не неразрешённая развилка: файл
сам объясняет корректный порядок и его цену, поэтому не стал звать `## ВОПРОСЫ`.

1. Прочитать `check_zahod.py` целиком вокруг клаузы З19 (образец) — понять её приём:
   граница «задание» через `ZADANIE_KONEC_RE`, построчный проход, докстринг со слепыми
   зонами, импорт признака вместо второго парсера.
2. Спроектировать З20: греп-токен (`grep`/`egrep`/`fgrep`/`rg`) внутри секции
   «## 2. ЗАДАЧА» (там же живёт КРИТЕРИЙ ГОТОВНОСТИ — отдельного `##`-заголовка у него
   нет) без пути/`--include`/каталога в той же команде — красное; отсечка по РОЛИ
   (цитата/пример), не по форме — заведомо широкая, слепые зоны названы в докстринге.
3. Живой прогон на трёх синтетических случаях (без пути / с путём / цитата) ДО
   калибровки по корпусу — ловить свои же баги на дешёвом входе, не гоняясь за
   корпусом вслепую.
4. Калибровка по всему корпусу `_studio/zhurnal/*/kod_*.md`; разобрать красные
   поимённо; сужать регексп только на подтверждённых ложных, не бесконечно (заход сам
   предупреждает: «после двух сужений ложных всё ещё много — остановись»).
5. Фикстура `PROGNAT.sh` — три новые ловушки (без пути / с путём / цитата), прогнать
   ВСЮ фикстуру целиком (не только новые ловушки — регрессия старых важнее).
6. Записи дома `disciplina/doma/zahody/POKRYTIE.md §2` — Р5-66 на построенный
   носитель; Р5-57 — оценить, закрывает ли отсечка по роли хоть частично.
7. Коммит зоны `_generator/tools/` в `materials`, отдельно — коммит `POKRYTIE.md` в
   `disciplina` (другой репозиторий, другой хэш).

**Критерий готовности кривым не считаю** — он явно допускает провал (п.1–2 живые, п.3
предъявляет число, п.4 — прогон фикстуры со счётом, п.5 — синтаксис, п.6 — два разных
хэша), возражений нет.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **`git commit -- <путь>` игнорирует индекс и коммитит содержимое РАБОЧЕЙ КОПИИ
   напрямую** — не задокументированная в явном виде здесь ловушка git, а не находка
   про эту задачу. Цена: `disciplina/doma/zahody/POKRYTIE.md` нёс чужие
   незакоммиченные правки (аналитика) плюс мою одну строку; аккуратно застейдженный
   через `git hash-object` + `update-index --cacheinfo` blob (ровно моя строка) был
   ПРОИГНОРИРОВАН, потому что `commit` был вызван с явным pathspec — команда в этом
   режиме документированно берёт текущее содержимое перечисленных файлов, а не индекс,
   и в коммит уехали все чужие незакоммиченные строки тоже. Поймано ДО пуша, отменено
   `git reset --mixed HEAD~1` (рабочая копия не тронута), пересобрано правильно —
   застейджен тот же blob, `commit` вызван БЕЗ pathspec (взял ровно то, что в индексе).
   Урок: хирургический коммит одной строки в файле, где есть чужие незакоммиченные
   правки, обязан коммититься БЕЗ pathspec после точечного `update-index`, а не с ним —
   иначе «--» в `git_zona.py`-каноне (`GIT-disciplina.md §3`, «`--` в самом commit») не
   спасает от этого конкретного случая, потому что канон говорит про ЧУЖОЕ в ИНДЕКСЕ,
   а не про то, что `commit` с pathspec обходит индекс целиком для НАЗВАННЫХ путей.
   ДОМ: 2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: granicy-poiska#1

## ОТЧЁТ — (заполняет исполнитель)

### §0.1 ГИТ-КОНТУР — шесть строк субагента ДОСЛОВНО
1. заявок было 8, закрыто 0, застряло 8: `2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026`, `2026-08-12T0359-tipy-slajdov-py-2-primer-3`, `2026-08-14T2048-arka-mat-kostyak-zahod-vynos-2`, `2026-08-14T2054-zahod-pravila-kadra-arka-mat-kostyak`, `2026-08-14T2317-14-08-1-arka-mat-kostyak`, `2026-08-15T0046-arka-mat-kostyak-zahod-chislo-i`, `2026-08-15T0121-zahod-pravila-kadra-2-arka-mat`, `2026-08-15T0136-zahod-ohvat-verdikta-f7c8cd7-arka-mat`
2. влито в основную (arka/mat-kostyak) 1 ветка из 1 названной: `zahod/ohvat-verdikta` (f7c8cd7, без конфликтов, `--vsyo-ravno` — рабочая папка без живого процесса). Проверено фактом: `git branch --no-merged arka/mat-kostyak | grep -c zahod/ohvat-verdikta` → 0. Попутно обнаружено и подтверждено фактом: `zahod/kanon-put`, `zahod/gejty-ne-tuda`, `zahod/chislo-i-kirillica` уже были влиты прошлым контуром (poteri rc=0). `zahod/pravila-kadra` (дочистка-2, 770ccf1/77fd09c/c93924f) НЕ влита — блок: 2 пути (KARTA.md, INCIDENTY.md) одновременно грязные в главной папке и участвующие в слиянии, `vlit-v-osnovnuyu` отказал сам, коммитить их не моё право — merge не начинался, `--abort` было нечего откатывать.
3. что забрать в git — `python3 _generator/tools/git_zona.py check` из главной папки: 21 путь (`_meta/disciplina/GIT-disciplina.md`, `_studio/docs/KARTA.md`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, `_studio/zhurnal/2026-08-10_dizajn-i-metriki/{SESSIYA.md,UROKI-FABRIKE.md,kod_rebra-blokov.md,kadry/README.md,kod_pravila-kadra.md}`, `_studio/zhurnal/2026-08-13_vynos-korpusa/{UROKI-FABRIKE.md,kod_vynos-2.md}`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/{UROKI-FABRIKE.md,kod_chislo-i-kirillica.md,kod_dolgi-nol.md,kod_dolgi-zero.md,kod_gejty-ne-tuda.md,kod_granicy-poiska.md,kod_kanon-put.md,kod_konflikty-2.md,kod_konflikty.md,kod_ohvat-verdikta.md}`, `teorkat-vvedenie/L2/dist/index.html`). В рабочей папке `pravila-kadra` отдельно 17 путей (`git_zona.py check` из неё) — правки `_generator/sborka/*.py` через `dist/index.html` и 15 `slaid.md`, содержательная работа по L2.
4. погашено: 0 веток и 0 папок. НЕ погашены (все rc=1/отказ): `zahod/kanon-put`, `zahod/gejty-ne-tuda`, `zahod/chislo-i-kirillica`, `zahod/ohvat-verdikta` — `zakryt-vetku` отказал у всех четырёх по одному и тому же незакоммиченному `INCIDENTY.md` в их рабочих папках (надгробие держит только коммиты, `--vsyo-ravno` это не перебивает); `zahod/pravila-kadra` — `poteri` дал rc=1 (не влита), не трогал.
5. необратимого не было — все отказы инструмент остановил ДО потери чего-либо; ничего не удалено, не форсировано мимо защит.
6. выдано 6 позиций из 6 найденных

### Что сделал и зачем
Одна новая клауза `check_z20_grep_bez_puti` в `_generator/tools/check_zahod.py`, рядом с
З19 (тот же приём разбора: граница «задание» через `ZADANIE_KONEC_RE`, построчный
проход). Ловит греп-критерий (`grep`/`egrep`/`fgrep`/`rg`) внутри секции «## 2. ЗАДАЧА»
(там же — КРИТЕРИЙ ГОТОВНОСТИ) без пути/`--include`/расширения/`<плейсхолдера>` в той же
команде (Р5-66) — без ограничения греп идёт по всему дереву репозитория, ложный ноль
читается как «дефектов нет». Отсечка по РОЛИ, не по форме, с двумя независимыми
сигналами (греп-токен внутри « », слово-маркер цитаты раньше по строке) плюс три
доп.признака, найденные калибровкой: пайп-греп (`cmd | grep` — читает stdin, не дерево),
голое упоминание токена без аргумента-паттерна («`grep` по репозиторию…» — описание
намерения, не команда) и составное слово («grep-паттерн» — не токен вовсе).

**Найден и починен побочный баг в процессе:** первая версия строила номер строки по
урезанному (`_bez_startovogo`) тексту, а сообщение печатало его как будто по исходному
файлу — на живом корпусе три «находки» указывали на строки без единого `grep`. Клауза
переведена в `CHECKS_NA_POLNOM_TEKSTE` (второй член множества после З13, по той же
причине разной: не секция нужна, а верный номер строки) — see докстринг клаузы.

### Как проверил
- Три живых ловушки на синтетике (без пути → красная, с путём → зелёная, цитата → зелёная)
  на каждом шаге сужения регекспа — не только в конце.
- Отрицательный контроль — ЭТОТ САМЫЙ файл-заход (`kod_granicy-poiska.md`), цитирующий
  дефектный греп дважды в §2 (строка «Что ловим» + строка «Что красить»): `python3
  _generator/tools/check_zahod.py kod_granicy-poiska.md` → `rc=0`, молчит.
- Калибровка по всему живому корпусу: `for f in _studio/zhurnal/*/kod_*.md; do python3
  _generator/tools/check_zahod.py "$f"; done` (число файлов — той же командой через
  `wc -l`) → **301 файл**, красных **10** (после трёх раундов сужения: было 38 → 27 → 22
  → 10). Разобрано поимённо больше пяти (фактически ~15+ по ходу сужения); финальные
  9 из 10 — истинные (греп без пути/с прозаическим «по X файлам» без литерального пути/с
  «…» вместо пути — все матчи формы «команда есть, ограничения нет», ровно то, что ловит
  Р5-66); 1 из 10 — известный ложный (`kod_doktrina-v-nastoyashchuyu-disciplinu.md:142`,
  путь на СЛЕДУЮЩЕЙ физической строке markdown-переноса) — назван слепой зоной в
  докстринге клаузы явно, не закрыт намеренно (риск склейки строк — замаскировать
  реальный дефект в СОСЕДНЕМ пункте нумерованного списка — хуже, чем этот один пропуск;
  «не подгоняй регексп бесконечно», заход сам).
- Живой прогон на реальном объекте (критерий п.1): `kod_vid-blokov-issledovanie.md:187`
  (`grep -c '^## '` → 6, без пути) — З20 краснеет поимённо со строкой; `kod_dajdzhesty-
  i-mit.md:189` (`grep -c '^## \`' VYCHITANO.md`, путь есть) — молчит.
- Ни единого трейсбека на всех 301 живых файлах корпуса (стандартный вывод пуст помимо
  ожидаемых сообщений клауз).
- `sh _generator/tools/fixtures/zahod/PROGNAT.sh` → `rc=0`, три новые ловушки (67–69:
  отрицательная/положительная/цитата), счёт ловушек вырос **66 → 69** (считает сама
  команда `grep -c '^echo "── ловушка'`, не вписан рукой); ВСЕ 69 ловушек зелёные —
  регрессии на старых нет.
- `python3 _generator/tools/check_zahod.py <любой живой заход>` — без трейсбека,
  проверено на всём корпусе (см. калибровку выше), не только на одном файле.

### Что НЕ трогал
Ничего вне `_generator/tools/` в `materials`. В `disciplina` — только клетку `Р5-66` в
`doma/zahody/POKRYTIE.md`; клетку `Р5-57` НЕ менял (см. решение ниже); остальные пять
файлов того репозитория, уже незакоммиченные ДО меня (правки аналитика — `SESSIYA.md`,
`INCIDENTY.md`, `priyomka/POKRYTIE.md`, `priyomka/RAZVITIE.md`, `zahody/RAZVITIE.md`),
и остальные строки самого `zahody/POKRYTIE.md` (Р5-61, КРТ-068, Р5-65, Р5-70,
КРТ-РАЗН-1/14/59/81 — были уже изменены в рабочей копии до того, как я открыл файл) —
не тронул ни байта; коммит `96d0a18` несёт ровно одну мою строку, проверено `git show
--stat` (1 файл, +1/-1) и вторым проходом после отмены первой, ошибочной попытки (см.
`## ВОПРОСЫ` п.1).

### Р5-57 — решение, не забыл
Мехника ЗАДАЧИ просила оценить, закрывает ли отсечка по роли Р5-57 («автоматика ищет
точную строку, живая формулировка иная по форме») хотя бы частично. Моя отсечка
использует НЕСКОЛЬКО поверхностных форм одной роли «цитата» (« »-обёртка, шесть
слов-маркеров, `⚠`) вместо одной точной фразы — но это устройство ОДНОЙ клаузы, не
переиспользуемый носитель для Р5-57 в целом (другие клаузы этого файла продолжают
искать точные фразы). Посчитал, что это НЕ засчитывается как закрытие даже частичное —
`POKRYTIE.md` строку Р5-57 не трогал, осталось «нет».

### Вопросы
См. `## ВОПРОСЫ` выше — один пункт очереди (находка про фабрику: `git commit --
<pathspec>` игнорирует индекс).

### Результат верификатора
Не заводил отдельно — §3 захода прямо говорит, что калибровка §2 играет его роль; она
выше, с числом и поимённым разбором.

### Время прогона + токены
НЕПРИМЕНИМО — канал `app`, снимать неоткуда (лог существует только на канале
`terminal`, куда его кладёт `tee`; сессия в приложении Claude Code такого следа не
оставляет). Не недосмотр, свойство канала.

### ПОВТОРЯЕМОСТЬ находок
Ни одна находка этой сессии не повторится на следующей единице работы буквально —
это не лекция/слайд/цикл повторяющихся правок, а разовая клауза линтера. Единственная
находка с шансом повториться — сама ловушка «`git commit -- <путь>` игнорирует
индекс» — но она про фабрику git-дисциплины, не про содержание задачи, и уже оформлена
пунктом очереди в `## ВОПРОСЫ`, а не заходом: следующая единица работы (следующий
заход) — не то же самое действие, что было здесь, критерий «немедленное» не подходит.

### Необратимое
Необратимого нет. Один локальный (не запушенный) коммит в `disciplina` был по ошибке
слишком широким (см. `## ВОПРОСЫ` п.1) — отменён `git reset --mixed HEAD~1` ДО пуша,
рабочая копия и чужие незакоммиченные правки не пострадали; воспроизводимо: старый
хэш `9572c13` (уже не существует в истории ветки, только в reflog).

**АРТЕФАКТ:** артефакта нет: задача кодовая (новая клауза линтера), не деки/документа —
собранного файла для владельца открывать нечего. Результат — сам изменённый
`_generator/tools/check_zahod.py` и лог прогона фикстуры/калибровки в этом отчёте.
**КОММИТ:** `3b0f7b4` — `check_zahod: З20 — греп-критерий без ограничивающего пути (Р5-66)` · `git_zona.py check --zone _generator/tools/` → ✅
Второй репозиторий (`disciplina`, отдельный хэш): `96d0a18` — `zahody/POKRYTIE: Р5-66 — рычаг переписан на построенный носитель (check_zahod.py З20)`.
