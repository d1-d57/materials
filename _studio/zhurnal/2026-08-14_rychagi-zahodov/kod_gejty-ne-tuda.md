# Канал исполнителя — gejty-ne-tuda (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — механика расписана, но каждая из пяти правок трогает гейт, которым судят чужую работу: цена ошибки здесь не «не сделал», а «сделал сторожа, который врёт зелёным».

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — правки трогают гейты, которыми судят чужую работу; сторож, который врёт зелёным, дороже несделанной правки.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/gejty-ne-tuda.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_gejty-ne-tuda.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add gejty-ne-tuda --branch zahod/gejty-ne-tuda, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-14_rychagi-zahodov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Фабрика заходов: генератор собирает задание исполнителю, гейты судят вернувшийся отчёт. Прошлый этап: приёмка 14.08 приняла два захода (`vynos-2`, `kanon-put`) и на обоих напоролась на один класс — гейты писались под однорепозиторный заход в главной папке, а заходы сегодня двухрепозиторные и worktree-шные. ЦЕЛЬ: пять правок, после которых гейты судят то, что заявляют.
Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели: получишь пять честных гейтов, но НЕ новые гейты и не изменение поведения самих заходов — правится только правдивость проверок.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-14) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Берём пять «гейтов не туда» — свежие инциденты приёмки 14.08, все в _generator/tools/, все проверяемы фикстурой
2. Влитие двух принятых веток (vynos-2, kanon-put) исполняет гит-контур этого захода по блокирующей заявке 2026-08-14T2048, отдельного захода на гигиену не делаем

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/gejty-ne-tuda`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/gejty-ne-tuda`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add gejty-ne-tuda --branch zahod/gejty-ne-tuda
  cd ../materials-wt/gejty-ne-tuda
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone _generator/tools/ --kommitit 'зону _generator/tools/ целиком, включая фикстуры' --zakryt 'свою ветку после влития — гасит приёмка, не заход'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/gejty-ne-tuda`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

**Контекст в одну фразу.** Приёмка 14.08 приняла два захода и на обоих напоролась на один класс дефекта: **гейты писались под однорепозиторный заход в главной папке, а заходы сегодня двухрепозиторные и всегда worktree-шные.** Пять правок ниже — один класс причины, а не пять разных багов. Все пять в твоей зоне.

Инциденты уже записаны в домах (читать НЕ обязательно, всё нужное продублировано здесь): `~/Documents/GitHub/disciplina/doma/priyomka/POKRYTIE.md §3` — первые три, `.../doma/zahody/POKRYTIE.md §3` — последние две.

### П1. `git_zona.py check --zone` зеленеет на зоне, которой нет — ЭТО ГЕЙТ 0 ПРИЁМКИ
Замер приёмки 14.08, воспроизводится одной командой из корня репозитория:
```
python3 _generator/tools/git_zona.py check --zone nesushchestvuyushchaya-papka-xyz ; echo "rc=$?"
python3 _generator/tools/git_zona.py check --zone ~/Documents/GitHub/disciplina ; echo "rc=$?"
```
Обе печатают `✅ … работа доехала в git, вне git ничего нет.` и `rc=0`. Значит **опечатка в имени зоны и зона в чужом репозитории неотличимы от чистой зоны**, а `check --zone` — гейт 0 всего ритуала приёмки: с него начинается приём КАЖДОГО отчёта.
**Чинить:** зона, которой нет на диске ИЛИ которая лежит вне корня этого репозитория, обязана давать `rc=1` и говорить, что именно не так («такой зоны здесь нет» / «зона в другом репозитории — проверь его отдельно»). Зелёный остаётся только у существующей зоны внутри репозитория.
⚠ Не сломай законный случай: зона может существовать и быть ПУСТОЙ (папка без изменений) — это по-прежнему зелёный.

### П2. `priyomka.py` Г1 ищет хэш только в `materials`
Замер 14.08 на отчёте `vynos-2`: Г1 напечатал «хэш 60501f2 не найден в репозитории», при том что коммит существует и виден одной командой в `disciplina` (`cd ~/Documents/GitHub/disciplina && git --no-optional-locks show --stat 60501f2`). Заход был двухрепозиторным ПО ЗАДАНИЮ, и его главный коммит лежал во втором репозитории.
**Чинить:** Г1 ищет хэш в текущем репозитории, а не нашёл — ищет в репозиториях, названных в контракте зоны или в строке `**КОММИТ:**` отчёта (там хэши даны с именем репозитория: `disciplina — 60501f2 · materials — 4102456`). Нашёл во втором — зелёный с указанием, ГДЕ нашёл. Нигде — красный.

### П3. `priyomka.py` Г3 берёт mtime вместо коммит-времени ветки захода
Замер 14.08 на отчёте `kanon-put` (находка исполнителя, приёмка подтвердила): артефакт закоммичен на ветку захода, а гейт гонится из главной папки, стоящей на другой ветке. Коммит-время оттуда недоступно, остаётся mtime — а по mtime код ВСЕГДА старше файла-захода, потому что отчёт дописывается последним. Красное будет на КАЖДОМ worktree-заходе, чей артефакт — код, то есть почти на каждом.
**Чинить:** брать коммит-время по ветке захода — `git log -1 --format=%ct <ветка> -- <путь>`; имя ветки есть в контракте зоны файла-захода. Ветку не нашёл или пути в ней нет — так и печатать («коммит-время недоступно, сверка по mtime»), а не красить молча.
⚠ **Печать Г10 разбирает имя ветки с хвостовой запятой** («Ветки `zahod/kanon-put,` нет среди 20 проверяемых») — почини заодно, это одна строка разбора и ровно тот же класс.

### П4. Две фикстуры красны в HEAD из-за неполного списка `cp`
`fixtures/korni/PROGNAT.sh` и `fixtures/register_doc/PROGNAT.sh` не копируют `modeli.py`, который `bootstrap_zahod.py` импортирует верхним уровнем → `ModuleNotFoundError` → «заход не заведён». **Красны они были ДО тебя** — это установлено заходом `kanon-put` базовым прогоном с откатом на HEAD, ПЕРЕОТКРЫВАТЬ НЕ НАДО. Чинится одной строкой в каждой.

### П5. Шапка `fixtures/zahod/PROGNAT.sh` хранит число ловушек текстом
Объявлено «ЛОВУШЕК ТРИДЦАТЬ ВОСЕМЬ», в теле — 61 (`grep -c '^echo "── ловушка' _generator/tools/fixtures/zahod/PROGNAT.sh`). Врало и до `kanon-put`; правка числа руками воспроизводит ровно тот класс, против которого стоит `KONSTITUCIYA §10`.
**Чинить: шапка число НЕ хранит** — печатается прогоном (счёт грепом по собственному телу). Если тем же приёмом лечатся шапки соседних фикстур — почини и их, они в зоне; если приём не переносится, скажи об этом строкой отчёта и не трогай.

### Порядок и стоп до цели
П1 → П2 → П3 → П4 → П5, по дешевизне проверки. **Застрял на любом — переходи к следующему, а застрявший опиши строкой отчёта: четыре из пяти лучше, чем ноль из пяти после суток в одном.** Ни одна из пяти правок не меняет ПОВЕДЕНИЯ заходов — только правдивость гейтов; менять что-то ещё в зоне не надо.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — пять пунктов, каждый называет то, что покраснеет:**
1. `check --zone nesushchestvuyushchaya-papka-xyz` → **rc=1**; `check --zone <существующая чистая зона>` → **rc=0** (обе команды прогнаны и коды приведены в отчёте).
2. Г1 на файле `_studio/zhurnal/2026-08-13_vynos-korpusa/kod_vynos-2.md` → **зелёный** (сегодня красный), и в тексте гейта названо, в каком репозитории найден хэш.
3. Г3 на файле `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_kanon-put.md` → **зелёный** (сегодня красный), причём зелёный получен из ГЛАВНОЙ папки, а не из твоей рабочей.
4. `sh _generator/tools/fixtures/korni/PROGNAT.sh` и `sh _generator/tools/fixtures/register_doc/PROGNAT.sh` → **rc=0** оба.
5. Число ловушек в шапке `fixtures/zahod/PROGNAT.sh` не хранится текстом: греп по шапке на число даёт 0 совпадений, а прогон печатает актуальное число.
6. **ЖИВОЙ ПРОГОН НА РЕАЛЬНОМ ОБЪЕКТЕ РЕПОЗИТОРИЯ, не на фикстуре:** `python3 _generator/tools/priyomka.py _studio/zhurnal/2026-08-13_vynos-korpusa/kod_vynos-2.md` и то же на `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_kanon-put.md` — оба прогоняются на живых файлах-заходах, что лежат в репозитории прямо сейчас, и число зелёных гейтов у каждого обязано ВЫРАСТИ против замера 14.08 (было 7 из 12 и 10 из 12). Зелёная фикстура этот пункт не заменяет.
Плюс общий гейт: **`sh` по ВСЕМ фикстурам `_generator/tools/fixtures/*/PROGNAT.sh` — назови rc каждой**; красная, которая была красной до тебя, — не твой брак, но должна быть названа поимённо.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Не нужен — операция не лоссовая, ничего не двигается и не удаляется. Его роль играют пункты критерия: каждый прогоняется командой на живом файле, а не глазами по коду.
⚠ **Одно исключение, где самопроверка врёт по построению:** П1. Проверять «зелёный остался зелёным» на СВОЕЙ же зоне мало — прогони `check --zone` минимум на четырёх случаях (существующая грязная · существующая чистая · несуществующая · в чужом репозитории) и приведи все четыре кода возврата.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Стартовое сообщение и §0.1 предписывают РАЗНЫЙ первый ход
ЦЕНА: стартовая строка велит `worktree add` первым ходом, §0.1 того же файла — сперва субагент
гит-контура и только ПОТОМ своя папка, «чтобы ветка отпочковалась от основной, которая уже всё
содержит». Исполнено по стартовой строке (она приходит в чат раньше, чем читается файл) — и ветка
отпочковалась от основной ДО влития. Контур влил три ветки, моя папка осталась на 7 коммитов позади,
причём один из них (`703e771`, `kanon-put`) правил ФАЙЛЫ МОЕЙ ЗОНЫ — те самые фикстуры `korni` и
`register_doc` из П4. Не заметь я этого, П4 и П5 делались бы на устаревшей копии, а приёмка получила
бы конфликт при влитии. Починка стоила отдельного хода `git_zona.py merge arka/mat-kostyak
--vsyo-ravno "<причина>"` (инструмент законно отказывался: основная вычекаучена в главной папке) и
записи в `INCIDENTY.md`. Обещание §0.1 «инструмент оказывается на диске сам, дотаскивать нечего»
сегодня не выполнялось.

### Число невлитых веток вшито в заход при сборке, а не снимается командой при прогоне
ЦЕНА: §0.1 утверждал «вливать нечего, проверено командой `git branch --no-merged` при сборке:
невлитых веток 0», а на прогоне их было ТРИ (`vynos-2`, `kanon-put`, `pravila-kadra`), и на них
стояла блокирующая заявка `2026-08-14T2048`. Заход при этом сам себе противоречил: раздел
«ЧТО ФИНАЛИЗИРОВАНО» требовал влить две ветки, §0.1 говорил «нечего». Субагент разрешил
противоречие командой и влил все три — но задание же велит ему «не судить, годна ли чужая ветка:
список влития уже просужен аналитиком». Поверь он тексту — три ветки остались бы невлитыми, то есть
воспроизвелась бы ровно та лестница, против которой §0.1 и написан. Это `KONSTITUCIYA §10` в чистом
виде: в заход вписано ЧИСЛО, а не команда, которая его считает.

### Плейсхолдер `<зона>` из шаблона §4 делал гейт 0 приёмки декоративным на КАЖДОМ заходе
ЦЕНА: `priyomka.py` берёт зону первым совпадением `check --zone <...>` по файлу, а первое совпадение
— всегда шаблонная строка §4 «`check --zone <зона>`», она стоит выше отчёта. Пока `git_zona.py check`
зеленел на несуществующей зоне (дефект П1), Г0 печатал «--zone <зона> → ✅» и означал «проверять было
нечего». Так приняты ОБА отчёта 14.08 — `vynos-2` и `kanon-put`: гейт, с которого начинается приём
каждого отчёта, у обоих был зелен ни на чём. Обнаружено только потому, что П1 сделал несуществующую
зону красной; правку пришлось делать той же рукой, иначе Г0 из всегда-зелёного стал бы
всегда-красным — тот же декоративный гейт с другим знаком.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Порядок — как в задании: П1 → П2 → П3 → П4 → П5.** Ниже — что именно правится, каким
приёмом и чем меряется. Разведка read-only уже сделана (чтение зоны в рабочей папке), мутаций
до этого плана не было.

**П1. `git_zona.py cmd_check` (≈стр. 1903) — зона обязана существовать и лежать в этом репозитории.**
Сегодня `cmd_check` зовёт `dirty(zone)`, а `dirty` фильтрует строки `git status --porcelain`
префиксом `in_zone(path, zone)`: путь, которого нет, просто ни с чем не совпадает → строк ноль →
«✅ работа доехала в git». Правка: перед `dirty()` резолвить зону (`~` разворачиваем; относительный
путь — от `REPO`, а не от `cwd`: зона по смыслу репозиторно-относительна) и давать `rc=1` с
названной причиной в двух случаях — нет на диске («такой зоны здесь нет») и вне корня репозитория
(«зона в другом репозитории — проверь его отдельно»). Существующая пустая зона остаётся зелёной:
пустоту решает `dirty()`, а не проверка существования. Абсолютный путь ВНУТРИ репозитория заодно
приводится к репозиторно-относительному, иначе `in_zone` не сматчит его никогда (тот же класс
ложно-зелёного).

**П2. `priyomka.py gate_g1` (≈стр. 149) — хэш ищется во всех репозиториях зоны.**
Кандидаты берём готовой функцией `repozitorii_zony()` (уже есть в файле, Г10 её зовёт): `REPO` плюс
корни абсолютных путей из строки `**ЗОНА (можно менять):**` контракта — второй репозиторий гейт не
угадывает, его называет сам контракт. Хэши берём из backticks строки `**КОММИТ:**` (в живых отчётах
они всегда там), с откатом на прежний «первый попавшийся» при их отсутствии. **Почему не «все
голые токены»:** `\b[0-9a-f]{6,40}\b` матчит и обычные слова («decade», «faded») — гейт, который
краснеет на прозе, обойдут вместе со всей защитой. Зелёный — когда каждый найден; в тексте
названо, ГДЕ найден каждый.

**П3. `priyomka.py gate_g3` (≈стр. 204) — два ремонта одного класса.**
(а) `otnositelno_repo()` умеет только `relative_to(REPO)`, поэтому артефакт в рабочей папке
(`materials-wt/<заход>/...` — а сегодня это КАЖДЫЙ worktree-заход) считается «вне репозитория»,
проваливается в ветку mtime и краснеет по построению. Чиню командой, а не догадкой: корень дерева
артефакта снимается `git rev-parse --show-toplevel`, принадлежность тому же репозиторию —
`--git-common-dir`; совпало → путь приводится к репозиторно-относительному.
(б) в оставшейся временнóй ветке, когда коммит-время по HEAD недоступно, беру его по ветке захода
(`git log -1 --format=%ct <ветка> -- <путь>`, ветка — из контракта); не нашлось ни ветки, ни пути —
печатаю это словами («коммит-время недоступно, сверка по mtime»), а не крашу молча.
(в) ⚠ из задания: `izvlech_vetku()` тащит хвостовую запятую из стартовой строки
(`--branch zahod/kanon-put, затем чтение`) → «Ветки `zahod/kanon-put,` нет среди 20 проверяемых».
Одна строка разбора: срезать хвостовую пунктуацию `,;:.` — в именах веток этого репозитория её нет.

**П4. Две фикстуры — предпосылка задания проверяется прогоном, а не глазами.**
🔴 **Оспариваю предпосылку, до работы, как велит §1.** В `fixtures/korni/PROGNAT.sh:68` и
`fixtures/register_doc/PROGNAT.sh:57` `modeli.py` **уже стоит в списке `cp`** — вместе с
комментарием, объясняющим ровно ту причину, что описана в П4. Похоже, правка приехала в основную
ветку раньше меня (моя ветка отпочкована от свежей основной — см. §0.1). Поэтому П4 исполняю как
ПРОВЕРКУ: гоняю обе фикстуры, называю `rc`. Красны — чиню; зелены — так и пишу, с выводом команды.
Задание говорит «переоткрывать не надо» про ЗАМЕР 14.08 — это не запрет убедиться, что правка
на месте сегодня.

**П5. Число ловушек в шапке — считается прогоном.**
В `fixtures/zahod/PROGNAT.sh` шапка объявляет «ЛОВУШЕК ТРИДЦАТЬ ВОСЕМЬ», а
`grep -c '^echo "── ловушка' …` в моей копии даёт 59 (в задании 61 — число разъезжается ровно
потому, что хранится текстом). Правка: из шапки число убирается, а прогон печатает его грепом по
СОБСТВЕННОМУ телу (`$0`) вместе с командой счёта. Приём переносится на соседей с тем же маркером —
`fixtures/priyomka/PROGNAT.sh` («ДВАДЦАТЬ ВОСЕМЬ», в теле 29) и `fixtures/sborka/PROGNAT.sh`
(«ДВАДЦАТЬ ЧЕТЫРЕ», в теле 44); чиню и их. `fixtures/provodka/PROGNAT.sh` («ЛОВУШЕК ВОСЕМЬ»)
маркера `── ловушка` не имеет вовсе (её блоки — `── П1:`/`── П2:`), приём туда не переносится —
не трогаю и говорю об этом строкой отчёта, как разрешает задание.

**Чем меряю (каждый пункт критерия — командой, из ГЛАВНОЙ папки там, где этого требует критерий):**
1. `check --zone` на ЧЕТЫРЁХ случаях (§3): существующая грязная · существующая чистая ·
   несуществующая · в чужом репозитории — привожу все четыре `rc`.
2. Базовый замер `priyomka.py` на обоих живых файлах-заходах СНИМАЮ ДО правок и повторяю ПОСЛЕ —
   иначе «выросло ли число зелёных» нечем доказать. Оба прогона — из главной папки.
3. Все фикстуры `fixtures/*/PROGNAT.sh` — `rc` каждой поимённо, красные до меня названы отдельно.

**Риск, который держу в голове на протяжении П1:** правка Г0 может перекрасить в красное живые
отчёты, чья строка `--zone` указывает на несуществующий или чужой путь. Это и есть цель правки,
но критерий 6 требует РОСТА числа зелёных — поэтому базовый замер снимается до правок, и если
Г0 где-то честно покраснеет, я назову это отдельной строкой, а не спрячу.

**Оговорка о порядке первого хода (не жалоба, а расхождение двух указаний):** стартовое сообщение
владельца велит `worktree add` ПЕРВЫМ ходом, §0.1 — сперва субагент гит-контура, и только потом
своя папка. Исполнено по стартовой строке (папка), контур запущен следом. Цена сегодня нулевая:
§0.1 сам сообщает «вливать нечего, невлитых веток 0», то есть отпочковываться было не от чего.
Подробнее — в `## УРОКИ ФАБРИКЕ`.

> 🔴 **ПОПРАВКА К ЭТОМУ АБЗАЦУ, ВПИСАНА ПОСЛЕ ПРОГОНА (план не переписываю — он документ того,
> что я знал в тот момент).** «Цена нулевая» оказалась НЕВЕРНОЙ: замер «невлитых 0» был снят при
> сборке захода и к прогону устарел — субагент насчитал командой ТРИ невлитых ветки и влил их. Моя
> папка к этому моменту уже существовала и отстала от основной на 7 коммитов, включая правки в моей
> же зоне (`703e771` kanon-put). Пришлось отдельным ходом влить основную в свою ветку. Подробности
> и цена — в `## УРОКИ ФАБРИКЕ`.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Стартовое сообщение и §0.1 предписывают разный первый ход; ветка захода отпочковалась от
   основной ДО влития и отстала на 7 коммитов, один из них правил файлы моей же зоны.
   ДОМ: _studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: gejty-ne-tuda#1
2. «Невлитых веток 0» вшито в §0.1 числом при сборке и к прогону устарело (их было 3, на них стояла
   блокирующая заявка) — вписать команду, а не число (`KONSTITUCIYA §10`).
   ДОМ: _studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: gejty-ne-tuda#2
3. Плейсхолдер `<зона>` из шаблона §4 делал Г0 приёмки декоративным на каждом заходе; в
   `priyomka.py` починено, но лечится и со стороны генератора — §4 может печатать конкретную зону
   захода вместо `<зона>`, тогда гейту не придётся её угадывать.
   ДОМ: _studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: gejty-ne-tuda#3
4. Три фикстуры красны в HEAD не по моей вине и не из моей задачи: `porozhdenie` (Т3 у
   `zamena-bazisa` — не хватает обязательного блока `dokazatelstvo`), `sborka`
   (`vektornye-prostranstva` — 14.98 против 17.02 строк, вне допуска 1.50), `sessiya`
   (падает на положительном контроле здорового входа). Все три проверены прогоном ДО моих правок
   инструментом из главной папки — красные до меня.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
5. Репозиторий `disciplina` не вывезен на origin (ahead 2: `60501f2`, `8edf412`): `vyvezti` отказал
   из-за 5 незакоммиченных отслеживаемых файлов, флага обхода у команды нет, а коммитить чужое
   гит-контур не вправе. Это единственный НЕОБРАТИМЫЙ вид долга (коммит живёт только на этом диске).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

### Что сделано и ЗАЧЕМ — пять пунктов задания, все пять закрыты

**П1. `git_zona.py check --zone` больше не зеленеет на зоне, которой нет.** Добавлена
`zona_sushchestvuet()` (вызывается первой в `cmd_check`): зона резолвится от корня репозитория,
и `rc=1` даётся в двух случаях — нет ни на диске, ни в git («такой зоны здесь нет») и лежит вне
корня («зона в другом репозитории — проверь его отдельно»). ЗАЧЕМ: `dirty()` фильтрует строки
`git status` префиксом зоны, и несовпадение печаталось как «вне git ничего нет» — гейт 0 приёмки был
зелен ни на чём. Законные случаи сохранены нарочно: существующая ПУСТАЯ зона зелена (пустоту судит
`dirty()`), а зона, которую заход СНОСИТ (на диске нет, но `git ls-files` о ней помнит), в отказ не
уходит — там есть настоящая работа.

**П2. Г1 ищет хэш в обоих репозиториях.** Кандидатов даёт `repozitorii_zony()` — та же функция и тот
же текст контракта, что у Г10, поэтому два гейта не разъедутся. Хэши берутся из backticks строки
`**КОММИТ:**`; голый поиск оставлен откатом. ЗАЧЕМ backticks: `\b[0-9a-f]{6,40}\b` матчит и обычные
слова из букв a-f («decade», «faded»), а гейт, краснеющий на прозе отчёта, обходят вместе со всей
защитой. В вердикте названо, ГДЕ найден каждый хэш.

**П3. Г3 перестал мерить mtime вместо коммит-времени.** Три ремонта одного класса:
(а) `otnositelno_repo()` научился рабочим папкам — корень дерева артефакта снимается
`git rev-parse --show-toplevel`, принадлежность тому же репозиторию — сверкой `--git-common-dir`
(командой, а не по имени папки `materials-wt/*`);
(б) в оставшейся временнóй ветке коммит-время берётся по ветке захода
(`git log -1 --format=%ct <ветка> -- <путь>`), а когда его нет — это ПЕЧАТАЕТСЯ словами
(«коммит-время недоступно … — сверка по mtime»), а не красится молча;
(в) ⚠ из задания: `izvlech_vetku()` срезает хвостовую пунктуацию — печать больше не говорит
«Ветки `zahod/kanon-put,` нет среди 20 проверяемых».

**П4. Две фикстуры — предпосылка задания к моменту прогона УЖЕ БЫЛА НЕВЕРНА, чинить нечего.**
`modeli.py` стоит в списках `cp` обеих фикстур вместе с комментарием, объясняющим ровно эту причину:
правка приехала в основную ветку раньше меня (частью влития `kanon-put`, где конфликт в этих двух
файлах разрешал гит-контур). Проверено не глазами, а прогоном: `korni` rc=0, `register_doc` rc=0.
Ни одной строки в них я не менял.

**П5. Шапки фикстур не хранят число ловушек.** В `zahod`, `priyomka`, `sborka` объявление заменено
на счёт грепом по собственному телу (`LOVUSHEK=$(grep -c '^echo "── ловушка' "$0" || true)`), число
печатается первой строкой прогона. `|| true` не украшение: при `set -e` греп с нулём совпадений
уронил бы прогон молча. **`provodka` не тронута — приём туда не переносится:** её шапка объявляет
«ЛОВУШЕК ВОСЕМЬ», но маркера `── ловушка` в теле нет вовсе (блоки называются `── П1:`/`── П2:`),
считать нечего. Это ровно тот случай, который задание разрешило описать строкой и не трогать.

**Сверх пяти пунктов — две правки, без которых П1 и П3 не доезжают** (обе того же класса «гейт
судит не то, что заявляет», обе названы здесь, чтобы приёмка не искала их в диффе):
1. `izvlech_zonu()` берёт зону по убыванию достоверности: из `## ОТЧЁТ` (там строка §4, заполненная
   исполнителем) → из КОНТРАКТА → и только потом из всего файла, пропуская плейсхолдер `<зона>`.
   Без этого П1 превратил бы Г0 из всегда-зелёного во всегда-красный на каждом заходе — см.
   `## УРОКИ ФАБРИКЕ`. **Порядок «сперва отчёт» поймал догфуд** (прогон приёмки по ЭТОМУ отчёту):
   первая редакция брала первое совпадение по файлу, и им оказалась ДЕМОНСТРАЦИОННАЯ команда из §2
   ЗАДАЧА — `check --zone nesushchestvuyushchaya-papka-xyz`, та самая, которой воспроизводится
   дефект П1. Гейт 0 проверял зону из примера. Пока `check` зеленел на несуществующем, это было
   невидимо; после П1 стало красным и потому нашлось.
2. `priyomka.py` резолвит репозиторий по МЕСТУ ВЫЗОВА (`git rev-parse --show-toplevel`), а не по
   `__file__` — тем же приёмом и по той же причине, что уже вылечено в `git_zona.py._resolve_repo`.
   `PRIYOMKA_REPO` остался приоритетным (дверь фикстур). Без этого приёмка, запущенная ИЗ ГЛАВНОЙ
   папки копией инструмента из рабочей, судила рабочую — то есть критерий 3 («зелёный получен из
   главной папки») нечем было бы исполнить честно.

### Как проверил — каждый пункт критерия командой, коды возврата первыми

**1. `check --zone`, четыре случая (§3 требует именно четыре, из корня репозитория):**
```
rc=1  _generator/tools/                       — существующая ГРЯЗНАЯ (замер до коммита)
rc=0  _studio/docs/                           — существующая ЧИСТАЯ
rc=1  nesushchestvuyushchaya-papka-xyz        — «такой зоны здесь нет»
rc=1  /Users/ivanyakovlev/Documents/GitHub/disciplina — «зона в другом репозитории»
rc=0  _generator/tools/                       — та же зона ПОСЛЕ коммита (зелёный вернулся)
```
Обе команды из формулировки задания дают `rc=1` и говорят, что именно не так; зелёный остался
только у существующей зоны внутри репозитория.

**2/3/6. Живой прогон приёмки на двух реальных файлах-заходах — ДО и ПОСЛЕ, оба из ГЛАВНОЙ папки:**

| файл | было (инструмент из главной папки, без правок) | стало |
|---|---|---|
| `kod_vynos-2.md` | зелёных **7 из 12**, `rc=1` | зелёных **8 из 12**, `rc=1` |
| `kod_kanon-put.md` | зелёных **9 из 12**, `rc=1` | зелёных **10 из 12**, `rc=1` |

Выросло у обоих. Базовый замер снят МОЕЙ рукой, тем же инструментом и в тот же час, — иначе «выросло
ли» нечем доказать. По `vynos-2` он сошёлся с замером задания (7 из 12) точно; по `kanon-put` дал 9
против названных заданием 10: за сутки покраснел Г7 (пункт очереди помечен доставленным, а метки по
адресу ДОМ нет) — это движение репозитория, а не моя правка, и рост считается от честного «сегодня».
- Г1 на `vynos-2` — **зелёный** (был красный), и назван репозиторий каждого хэша:
  `60501f2 — в disciplina`, `4102456 — в materials`, `5053753 — в materials`.
- Г3 на `kanon-put` — **зелёный** (был красный): «артефакт … внутри зоны и входит в коммит `703e771`
  из §4 КОММИТ [ветка: внутри зоны — сверка по коммиту, не по времени]». Прогон — из
  `/Users/ivanyakovlev/Documents/GitHub/materials`, не из рабочей папки.
- Печать жизни ветки на `kanon-put` была «`zahod/kanon-put,`: ❌ Ветки … нет среди 19 проверяемых»,
  стала «✅ `zahod/kanon-put` — метрика зелёная».
- Оба по-прежнему `rc=1` — у обоих остаются СВОИ красные (у `vynos-2` пустой АРТЕФАКТ и Г9,
  у `kanon-put` Г7; у обоих Г10 судит репозиторий). Это дефекты тех отчётов, а не моей правки.

**4. Две фикстуры П4:** `sh fixtures/korni/PROGNAT.sh` → `rc=0`, `sh fixtures/register_doc/PROGNAT.sh`
→ `rc=0`, обе печатают `ФИКСТУРЫ ЗЕЛЁНЫЕ`.

**5. Число ловушек в шапках:** греп на объявленное число даёт **0 совпадений** во всех трёх шапках
(`sed -n '1,60p' … | grep -cE 'ЛОВУШЕК +(…|[0-9]+)'` → 0, 0, 0), а прогон печатает актуальное:
`zahod` → 61, `priyomka` → 29, `sborka` → 44. Показательно, что живое число `zahod` за сегодня уже
уехало с 59 на 61 (пока я работал, влилась ветка с новыми ловушками) — при хранении текстом шапка
соврала бы в третий раз.

**Общий гейт — ВСЕ фикстуры, `rc` каждой (31 из 31 прогнана, 28 зелёных, 3 красных):**
```
dvizhok 0 · gigiena 0 · git_zona 0 · graf 0 · illyustracii 0 · incidenty 0 · kadry 0 · karty 0
korni 0 · lenta 0 · marker 0 · modeli 0 · nablyudenia 0 · nezakrytogo 0 · nomera 0
porozhdenie 1 · priyomka 0 · provodka 0 · register_doc 0 · reviziya_dolgov 0 · sborka 1
schet_blokov 0 · sessiya 1 · sostoyanie 0 · termin 0 · tool_contract 0 · urok 0 · uroki 0
verstki 0 · ves 0 · zahod 0
```
🔴 **Красные поимённо и с ответом «моё или не моё» — проверено прогоном, а не рассуждением:**
`porozhdenie`, `sborka`, `sessiya` прогнаны ИНСТРУМЕНТОМ ИЗ ГЛАВНОЙ ПАПКИ (без моих правок) и там
тоже `rc=1` — красны ДО меня, содержательные (нехватка блока `dokazatelstvo` у `zamena-bazisa`,
недобор строк у `vektornye-prostranstva`, падение на положительном контроле в `sessiya`).
**Одна фикстура была сломана МНОЮ и починена мною:** `priyomka` — она нарочно опиралась на
несуществующую зону (`_studio/zhurnal/_never-existing-zone-priyomka-fixture`), то есть на ровно ту
ложь, которую снимает П1. Развязка перенесена на ОДНОРАЗОВЫЙ репозиторий в `/tmp` (`PRIYOMKA_REPO`,
та же дверь, что `GIT_ZONA_REPO` у фикстур): зона там существует, чиста и закоммичена по построению,
и в фикстуру добавлена строка-основание, которая останавливает прогон, если это перестанет быть
правдой. Это строго крепче прежнего: раньше зелёный Г0 означал «проверять было нечего», теперь —
«проверено на чистой зоне». Ловушка 3 доучена требовать, чтобы красный Г1 называл, ГДЕ искал.

### Результат верификатора (§3)
Отдельный верификатор не заводился — операция не лоссовая, ничего не двигается и не удаляется, и
задание прямо отдаёт его роль пунктам критерия. Единственное объявленное исключение — П1, где
самопроверка врёт по построению: четыре случая `check --zone` прогнаны и все четыре кода возврата
приведены выше (плюс пятый, после коммита).

### Что НЕ трогал
Всё вне `_generator/tools/`. `provodka/PROGNAT.sh` — по названной причине. Фикстуры `korni` и
`register_doc` — там чинить нечего. Ветку не вливал (это ход приёмки). Три красные фикстуры,
не мои, не трогал: они вне задачи, и их починка — отдельный заход.
🔴 **Один путь ВНЕ зоны остался изменённым в рабочей папке и НЕ закоммичен мной:**
`_studio/zhurnal/_INFRA-git/INCIDENTY.md` — это автолог, который `git_zona.py` дописал сам, когда я
влил основную ветку с `--vsyo-ravno`. Он вне моей зоны, коммитить его я не вправе; решение — за
приёмкой.

### Гигиена §4.1 — выводы команд, а не пересказ
- **Г1. Зона доехала в git.** `git_zona.py check --zone _generator/tools/` → `rc=0`,
  «✅ зона `_generator/tools/`: работа доехала в git, вне git ничего нет».
- **Г2. Второй репозиторий.** Неприменимо: все пути зоны внутри `materials`. Стань применим —
  команда та же: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто.
- **Г3. Невлитых веток не прибавилось.** `git branch --no-merged arka/mat-kostyak` → **1**, и это
  моя собственная `zahod/gejty-ne-tuda`. На входе (после работы контура) было 0. Прирост законен:
  ветка живого захода — это и есть один идущий заход, гасит её приёмка.
- **Г4. Новый инструмент имеет точку вызова.** Ни одного НОВОГО `.py` не заведено:
  `git show --diff-filter=A --stat --format= HEAD` → пусто. Оба `.py` в коммите — правки
  существующих. `check_tool_contract.py` неприменим.
- **Г5. Новый `.md` зарегистрирован.** Неприменимо: ни одного нового `.md` не заводил,
  `register_doc.py` не звал, `KARTA.md` не трогал.
- **Г6. В коммите нет чужих путей.** Коммитов два. `ea6c08e` — ровно пять путей, все в
  `_generator/tools/`: `fixtures/priyomka/PROGNAT.sh`, `fixtures/sborka/PROGNAT.sh`,
  `fixtures/zahod/PROGNAT.sh`, `git_zona.py`, `priyomka.py`. `af21bbb` — один путь,
  `_generator/tools/priyomka.py`. Чужого нет ни в одном.

### Догфуд — приёмка по СОБСТВЕННОМУ отчёту (не пункт критерия, но сильнейшая из проверок)
`python3 priyomka.py …/kod_gejty-ne-tuda.md` → `rc=1`, **зелёных 11 из 12**. Единственный красный —
Г10 (накопление git-долга): он судит РЕПОЗИТОРИЙ, а не отчёт, и красен по состоянию дерева
(незакоммиченные файлы-заходы и `disciplina` без вывоза), а не по этой работе. Именно этот прогон
поймал дефект выбора зоны, описанный выше.

### Гит-контур §0.1 — шесть строк субагента, вставлены ДОСЛОВНО
1. **заявок было 4, закрыто 0, застряло 2** — `2026-08-14T2048` (влито обе ветки, vynos-2 погашена, materials вывезен; застряло: kanon-put не гасится, disciplina не вывозится) и `2026-08-14T2054` (ветка влита; застряло: не гасится). Заявки `2026-08-12T0311` (правило фазы 2 в `gejt_kartochki.py`) и `2026-08-12T0359` (`tipy_slajdov.py`, слоты Т2/Т3) — не гит-заявки, гит-контуром не исполняются, оставлены открытыми нетронутыми.
2. **влито в основную `arka/mat-kostyak` (имя снято `git_zona.py check`) 3 ветки из 3 невлитых**: `zahod/vynos-2` → merge `388b8d4` (без конфликтов), `zahod/kanon-put` → merge `3e87d55` (конфликт в `_generator/tools/fixtures/korni/PROGNAT.sh` и `_generator/tools/fixtures/register_doc/PROGNAT.sh` разрешён ОБЪЕДИНЕНИЕМ: копирование `modeli.py` из HEAD + декларация `KANON-KOREN` из ветки; обе фикстуры прогнаны — `ФИКСТУРЫ ЗЕЛЁНЫЕ`, rc=0), `zahod/pravila-kadra` → merge `eb32ff9` (без конфликтов). Невлитых не осталось: `git branch --no-merged` пуст. Замер задания «0 невлитых веток» был устаревшим, противоречие разрешено командой: заявка 2048 в очереди есть. Все три сливались с `--vsyo-ravno` (живая рабочая папка принятого захода) — причины записаны в INCIDENTY. Вывезено: `arka/mat-kostyak` → origin, 15 коммитов; `disciplina` НЕ вывезена (main ahead 2: `60501f2`, `8edf412`) — её `tools/git_zona.py vyvezti` отказал из-за 5 незакоммиченных отслеживаемых файлов, флага обхода у команды нет, коммитить чужое контуру нельзя.
3. **забрать в git** (`git_zona.py check`, 16 путей; зона `_generator/tools` чиста — «работа доехала в git»): `?? _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/README.md`, `?? _studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_pravila-kadra.md`, `?? _studio/zhurnal/2026-08-13_vynos-korpusa/kod_vynos-2.md`, `?? _studio/zhurnal/2026-08-14_rychagi-zahodov/kod_dolgi-nol.md`, `?? …/kod_dolgi-zero.md`, `?? …/kod_gejty-ne-tuda.md`, `?? …/kod_kanon-put.md`, `?? …/kod_konflikty-2.md`, `?? …/kod_konflikty.md`, `M _meta/disciplina/GIT-disciplina.md`, `M _studio/docs/KARTA.md`, `M _studio/zhurnal/2026-08-10_dizajn-i-metriki/SESSIYA.md`, `M _studio/zhurnal/2026-08-13_vynos-korpusa/UROKI-FABRIKE.md`, `M _studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md`, `M _studio/zhurnal/_INFRA-git/INCIDENTY.md`, `M teorkat-vvedenie/L2/dist/index.html`.
4. **погашено**: `zahod/vynos-2` (poteri rc=0, надгробие `mogila/zahod/vynos-2` → `50537537`, папка `materials-wt/vynos-2` снята). **НЕ погашено**: `zahod/kanon-put` (poteri rc=0, но `zakryt-vetku` отказал — в `materials-wt/kanon-put` 1 путь вне git: `?? _studio/zhurnal/_INFRA-git/INCIDENTY.md`; `--vsyo-ravno` грязную папку не перебивает) и `zahod/pravila-kadra` (poteri rc=0, отказ — `?? teorkat-vvedenie/L2/dist/index.html`). Папка и ветка `gejty-ne-tuda` не тронуты.
5. **необратимое**: снос рабочей папки `/Users/ivanyakovlev/Documents/GitHub/materials-wt/vynos-2` — папка была чистой, коммиты держит надгробие, восстанавливается `python3 _generator/tools/git_zona.py voskresit --branch zahod/vynos-2`. Больше ничего необратимого не делалось; правки конфликта — в merge-коммите `3e87d55`, уже на origin.

выдано 28 позиций из 28 найденных

### 🔴 НЕОБРАТИМОЕ — отдельным списком
**Моё, за эту сессию:**
1. Влитие основной ветки `arka/mat-kostyak` в мою `zahod/gejty-ne-tuda` (fast-forward, дерево было
   чисто, своих коммитов 0) · рабочая папка `materials-wt/gejty-ne-tuda` · откат:
   `git_zona.py` ветку не терял, прежняя точка — `b8f8655`; моя работа поверх — `ea6c08e`.
   Формально обратимо, но названо здесь, потому что делалось под `--vsyo-ravno`, то есть в обход
   отказа инструмента, и владелец этого хода не видел.
2. Запись в автолог `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (причина `--vsyo-ravno`) ·
   рабочая папка захода · не закоммичена, снимается `git checkout --` по этому пути.
**Необратимое гит-контура** — в его строке 5 выше (снос рабочей папки `materials-wt/vynos-2`,
восстанавливается `git_zona.py voskresit --branch zahod/vynos-2`). Больше необратимого нет:
ничего не удалялось, не переименовывалось и не перезаписывалось.

### ПОВТОРЯЕМОСТЬ находок — что придёт на СЛЕДУЮЩЕМ заходе, а что нет
🔴 **Повторятся (класс НЕМЕДЛЕННОЕ — заход ДО следующего прогона, а не пункт очереди):**
- расхождение «стартовое сообщение против §0.1» — печатается генератором в КАЖДЫЙ заход, значит
  следующий исполнитель отпочкует ветку так же рано, с той же ценой;
- «невлитых веток N» числом вместо команды — вшивается при каждой сборке и устаревает к прогону;
- плейсхолдер `<зона>` в §4 — стоит в каждом заходе; со стороны приёмки я его закрыл, со стороны
  генератора он остаётся (§4 может печатать конкретную зону — тогда угадывать не придётся).
**Не повторятся (законно уходят в очередь):** три красные фикстуры `porozhdenie`/`sborka`/`sessiya`
— это конкретные содержательные дефекты конкретных материалов, а не свойство следующей единицы
работы; невывезенная `disciplina` — состояние сегодняшнего дня.
**Пробным прогоном найдено и починено в тот же заход:** (а) сломанная моей же правкой фикстура
`priyomka`; (б) выбор зоны Г0 из демо-команды в тексте задачи — найден ДОГФУДОМ, прогоном правленой
приёмки по этому самому отчёту, и закрыт до сдачи (проба, после которой ничего не починили, —
потраченные токены). Догфуд стоит вписать в ритуал: он нашёл дефект, которого не увидели ни оба
живых прогона, ни 29 ловушек фикстуры — просто потому, что ни один заход, кроме этого, не цитирует
`check --zone` в тексте задачи.

### Открытое «возвращаться»
- П4 остался без единой правки — если приёмка считает, что ремонт всё же нужен, нужно назвать, что
  именно красное, потому что сегодня обе фикстуры зелёные.
- `provodka` живёт с объявленным числом ловушек в шапке до тех пор, пока её блоки не получат единый
  маркер; сделать это можно, но это правка её структуры, а не шапки, и в задание она не входила.
- Три красные фикстуры не разбирались по существу — только идентифицированы как «красные до меня».

**Время прогона + токены:** НЕПРИМЕНИМО — канал `app`, следа прогона не остаётся (лог существует
только на канале `terminal`, где его кладёт `tee`).

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/gejty-ne-tuda/_generator/tools/priyomka.py` — любой текстовый редактор
*(это продукт захода: правленый инструмент приёмки. Рядом, тем же коммитом, — четыре файла:
`_generator/tools/git_zona.py`, `fixtures/priyomka/PROGNAT.sh`, `fixtures/sborka/PROGNAT.sh`,
`fixtures/zahod/PROGNAT.sh`. Собранного HTML/PDF нет — заход правит гейты, а не материал.
⚠ Путь ведёт в рабочую папку захода; после её сноса приёмкой тот же файл живёт по
`_generator/tools/priyomka.py` в главной папке — но только ПОСЛЕ влития ветки.)*
**КОММИТ:** `af21bbb` — `_generator/tools: зона Г0 берётся из ОТЧЁТА, а не из демо-команды в тексте задачи (поймано догфудом приёмки по собственному отчёту)`; предыдущий, основной — `ea6c08e` — `_generator/tools: гейты судят то, что заявляют — зона check существует и своя, хэш ищется в обоих репозиториях, коммит-время по ветке захода, число ловушек считает прогон` · `git_zona.py check --zone _generator/tools/` → ✅ (`rc=0`)
