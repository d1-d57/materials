# Канал исполнителя — zayavki-podklyuchenie (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — <одна фраза почему (Sonnet — механика расписана; Opus — план рождается по ходу)>.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-11_ochered-zayavok» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/sweet-inspiring-mendel/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Cowork в песочнице в `.git` не пишет и потому раньше писал git-команду в чат владельцу —
тот оказывался транспортом, команда молча не срабатывала, следа не оставалось. Заход `zayavki-yadro`
(принят 11.08, 9 гейтов приёмки зелёные) сделал ЯДРО почтового ящика: `git_zona.py zayavka` кладёт
просьбу файлом, `zayavki` печатает, `zayavka-zakryt` закрывает. Устройство целиком —
`_studio/zhurnal/2026-08-11_ochered-zayavok/PROEKT.md`, правило — `_meta/disciplina/GIT-disciplina.md §4г`.

ЦЕЛЬ. Механизм есть, но его **никто не вычитывает**: очередь работает руками. Ты подключаешь её к
трём местам — генератор захода зовёт её первой строкой, гейт сборки краснеет на заходе без этой
строки, приёмка краснеет на заявке, пролежавшей больше суток. После тебя владельцу git-команд не
выдают вообще.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. Ядро (З1) принято 11.08: 9 гейтов priyomka.py зелёные, подкоманды zayavka/zayavki/zayavka-zakryt на ветке zahod/zayavki-yadro
2. Канон _meta/disciplina/GIT-disciplina.md §4г дописан аналитиком — красная ловушка 6 фикстуры этим закрывается
3. Вычитка очереди вшивается в генератор ПЕРВОЙ строкой §0.1, а не отдельным блоком: второе место, где про git можно забыть, — это та болезнь, которую блок лечит
4. Гейт приёмки Л1: заявка открыта дольше 24 ч → красный; постоянные исключения возрастом не судятся

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/zayavki-podklyuchenie`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/zayavki-podklyuchenie`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add zayavki-podklyuchenie --branch zahod/zayavki-podklyuchenie
  cd ../materials-wt/zayavki-podklyuchenie
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/bootstrap_zahod.py` `_generator/tools/check_zahod.py` `_generator/tools/priyomka.py` `_generator/tools/fixtures/zahod` `_generator/tools/fixtures/priyomka`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

🔴 **Генератор подставил сюда зону ПРАВКИ, а нужна зона СЛИЯНИЯ — это разные вещи, и команду он
собрал неисполнимую.** Ветка `zahod/zayavki-yadro` привозит ровно три пути, и ни один из них ты не
правишь: `merge` с зоной правки откажет («выходит за зону»). Правильная команда — ниже, зоны в ней
названы по тому, что ветка ТРОГАЕТ:

```
python3 _generator/tools/git_zona.py merge zahod/zayavki-yadro --zone _generator/tools/git_zona.py --zone _generator/tools/fixtures/git_zona --zone .gitignore
```

⚠ По этой же причине `check_sborki.py` даёт на этом файле **один ❌ С7** («ветка трогает 3 пути вне
зоны слияния»). Гейт прав по факту и не различает зону правки и зону слияния — объявленная слепая
зона, не твой дефект. Красное на нём чинить не надо.
⚠ Там же три ❌ С5 («подкоманды `zayavka`/`zayavki`/`zayavka-zakryt` у `git_zona.py` НЕТ»): гейт
смотрит `--help` ГЛАВНОЙ папки, а подкоманды лежат на ещё не влитой ветке `zahod/zayavki-yadro`.
**После твоего первого хода они исчезнут** — это бесплатная проверка, что слияние доехало:
`python3 _generator/tools/git_zona.py --help | grep zayav` → три строки.

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/zayavki-yadro`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- свою зону + правки аналитика вне git: _meta/disciplina/GIT-disciplina.md, _studio/zhurnal/2026-08-11_ochered-zayavok/, _studio/zhurnal/_INFRA-git/POSTOYANNYE.md, _studio/docs/KARTA.md — точный список снимать git_zona.py check

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- НИЧЕГО не гасить. Неприкосновенны materials-wt/tipologia-slajdov и materials-wt/zamer-blokov-l2 — там живые заходы

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/zayavki-podklyuchenie`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/bootstrap_zahod.py` `_generator/tools/check_zahod.py` `_generator/tools/priyomka.py` `_generator/tools/fixtures/zahod` `_generator/tools/fixtures/priyomka`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Три шага. Читай ТОЛЬКО: этот файл, `_studio/zhurnal/2026-08-11_ochered-zayavok/PROEKT.md` (устройство
механизма — §3 про двери, §4 про ловушки), `_meta/disciplina/GIT-disciplina.md §4г` (правило),
и три файла своей зоны. Проект не изучай.

### Ш1. `bootstrap_zahod.py` — вычитка очереди ПЕРВОЙ строкой §0.1

В блоке `git_kontur_blok()` (ищи по строке `### 0.1 🔴 ГИТ-КОНТУР`) — **перед слотом 1**, вшитой
строкой, а не слотом: аналитик её не заполняет и забыть не может.

```
**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.
```

🔴 **Почему в тот же блок, а не отдельным.** Второе место, где про git можно забыть, — ровно та
болезнь, которую §0.1 и лечит (комментарий об этом уже стоит в файле над `git_kontur_blok`).

### Ш2. `check_zahod.py` — гейт З12

По образцу З10 (он стережёт слоты §0.1): нет строки `**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.**` или нет в ней вызова
`git_zona.py zayavki` — красный «заход не вычитывает очередь заявок: просьбы Cowork до исполнителя
не доедут». Положительный контроль — на живом заходе, собранном твоим же `bootstrap_zahod.py`, а не
на синтетике (так сделано у З10, см. ловушку 24 фикстуры `zahod`).

### Ш3. `priyomka.py` — гейт Л1 на просроченной заявке

Новый гейт в список `gates.append(...)` рядом с Г8: **заявка открыта дольше 24 ч → красный**, с
именем и возрастом каждой. Возраст — по времени файла, а не по имени. Заявок нет или все моложе —
зелёный с охватом («проверено N заявок»). 🔴 **Постоянные исключения (`POSTOYANNYE.md`) возрастом НЕ
судятся вовсе** — иначе гейт красит каждую приёмку и его отключат первым же вечером.

Гейту нужен путь очереди — бери его у `git_zona` (там уже есть `zayavki_dir()`), второй реализации
не заводи: два разных ответа на «где очередь» разъедутся молча.

### Ш4. Ловушки — в обе фикстуры

`fixtures/zahod/PROGNAT.sh`: собранный заход несёт строку вычитки · З12 краснеет на заходе, из
которого строку убрали. `fixtures/priyomka/PROGNAT.sh`: заявка возрастом 30 ч → приёмка красная ·
заявка возрастом 1 ч → зелёная · пустая очередь → зелёная · постоянное исключение любого возраста
возрастом не судится. 🔴 Возраст в фикстуре подделывай **`python3 -c` через `os.utime`**, а не
`touch -d`: BSD-touch на macOS этого не понимает, и фикстура позеленеет у тебя, оставаясь красной у
владельца (`GIT-disciplina §0`, цена названа там же).

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. `bash _generator/tools/fixtures/zahod/PROGNAT.sh` и `bash _generator/tools/fixtures/priyomka/PROGNAT.sh` — обе печатают «ФИКСТУРЫ ЗЕЛЁНЫЕ», rc=0. Красное на старте — сними ДО работы и назови: чужое не чини.
2. Живой прогон, вывод дословно в `## ОТЧЁТ`: собрать пробный заход своим `bootstrap_zahod.py` во временной папке и показать, что строка вычитки в нём есть (`grep 'ВЫЧИТАТЬ ОЧЕРЕДЬ'` → 1).
3. `python3 _generator/tools/check_zahod.py` на этом же пробном заходе → З12 зелёный; на нём же с удалённой строкой → З12 красный. Оба вывода в отчёт.
4. `python3 _generator/tools/priyomka.py <любой принятый заход этой арки>` → новый гейт печатается и зелёный (очередь пуста).
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/bootstrap_zahod.py _generator/tools/check_zahod.py _generator/tools/priyomka.py _generator/tools/fixtures/zahod _generator/tools/fixtures/priyomka                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/bootstrap_zahod.py _generator/tools/check_zahod.py _generator/tools/priyomka.py _generator/tools/fixtures/zahod _generator/tools/fixtures/priyomka   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/bootstrap_zahod.py` `_generator/tools/check_zahod.py` `_generator/tools/priyomka.py` `_generator/tools/fixtures/zahod` `_generator/tools/fixtures/priyomka`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/bootstrap_zahod.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/check_zahod.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/priyomka.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/zahod` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/priyomka` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Три шага строго по заданию, в порядке §2:
1. `bootstrap_zahod.py`: вшить строку «**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.**» первым слотом функции `git_kontur_blok()` (перед слотом «1. ВЛИТЬ ПЕРЕД РАБОТОЙ») — константой `GIT_KONTUR_VYCHITKA_OCHEREDI`, текст взят дословно из задания.
2. `check_zahod.py`: новый гейт З12 по образцу З10/З11 (третье объявленное исключение закона калибровки) — ищет строку `**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.**` и вызов `git_zona.py zayavki` внутри неё, судит только текст ДО `## ПЛАН`. Положительный контроль — на живых `kod_tirazh-ocheredi.md`/`kod_ochered-domov.md` (они реально не несут строки, красное истинно).
3. `priyomka.py`: гейт Л1 рядом с Г8 — красный, если открытая заявка (не постоянное исключение) старше 24 ч; путь очереди берётся у `git_zona.zayavki_dir()`/`_zayavki_otkrytyye()`, второй реализации не завожу.
4. Ловушки в обе фикстуры (`fixtures/zahod/PROGNAT.sh`, `fixtures/priyomka/PROGNAT.sh`) — возраст заявки подделываю `os.utime`, не `touch -d` (BSD-touch на macOS не понимает `-d`, урок канона).

Критерий готовности — четыре пункта §2, включаю живой прогон на реальном пробном заходе.

## ВОПРОСЫ — (заполняет исполнитель)
Вопросов владельцу нет. Пунктов очереди нет.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Реализовал все три шага задания §2 механизма заявок Cowork → заход (`_studio/zhurnal/2026-08-11_ochered-zayavok/PROEKT.md`, `GIT-disciplina §4г`):

- **Ш1** (`bootstrap_zahod.py`): в `git_kontur_blok()` перед слотом 1 вшита (не заполняемая аналитиком) строка `**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.**` с командами `git_zona.py zayavki` / `zayavka-zakryt` — текст дословно из задания, оформлен константой `GIT_KONTUR_VYCHITKA_OCHEREDI`.
- **Ш2** (`check_zahod.py`): новый жёсткий гейт `check_z12_vychitka_ocheredi` (код «З12») в `HARD_CHECKS`, судит только текст ДО `## ПЛАН` (как З10/З11). Docstring файла обновлён — теперь три объявленных исключения закона калибровки (З10/З11/З12).
- **Ш3** (`priyomka.py`): новый гейт `gate_l1()`, подключён в список `gates` рядом с Г8 (после него, как и просило задание). Красный только для НЕпостоянных заявок старше 24 ч (`ЛIMIT_VOZRASTA_CH = 24`); постоянные исключения (`СРОЧНОСТЬ: postoyannaya`) возрастом не судятся вовсе. Путь очереди — импорт `git_zona`, вызов `git_zona._zayavki_otkrytyye()`, второй реализации нет.
- **Ш4**: в `fixtures/zahod/PROGNAT.sh` — ловушка 34 (синтетический здоровый заход без строки → З12 краснеет поимённо) и 35 (живой позитивный контроль: оба калибровочных захода без строки краснеют; свежесобранный `bootstrap_zahod.py`-скелет несёт строку и молчит). Синтетический эталон `sobrat_zdorovyj()` дополнен строкой вычитки — без неё сломались бы все ловушки, построенные на этом эталоне. В `fixtures/priyomka/PROGNAT.sh` — ловушки 21-24: возраст 30 ч → красный (поимённо), 1 ч → зелёный, пустая очередь → зелёный с охватом «проверено 0 заявок», постоянное исключение возрастом 999 ч → зелёный (не судится). Возраст подделан `os.utime`, не `touch -d`.

**Как проверил.**
1. `bash _generator/tools/fixtures/zahod/PROGNAT.sh` → «ФИКСТУРЫ ЗЕЛЁНЫЕ», rc=0 (35 ловушек, было 33).
2. `bash _generator/tools/fixtures/priyomka/PROGNAT.sh` → «ФИКСТУРЫ ЗЕЛЁНЫЕ», rc=0 (24 ловушки, было 20).
3. Живой прогон (критерий §2 п.2): собрал пробный заход `bootstrap_zahod.py` во временной папке (`_studio/zhurnal/proba probaotcheta --branch probaotcheta-branch --zone '_generator/tools/dummy-zone/fake.py' --kanal terminal --finalizirovano … --finalizirovano …`), rc=0. `grep -c 'ВЫЧИТАТЬ ОЧЕРЕДЬ' kod_probaotcheta.md` → `1`.
4. Живой прогон (критерий §2 п.3): `check_zahod.py` на этом же пробном заходе — З12 в выводе НЕТ (молчит; красны только не связанные с задачей З2/З10-слоты 2-3/З6-печать, ожидаемо для свежего скелета без критерия). После программного удаления строки «**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.**…» из того же файла — `check_zahod.py` печатает `❌ З12: нет строки «**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.**» в блоке §0.1 — заход не вычитывает очередь заявок…`, rc=1. Оба вывода воспроизводятся дословно командой выше (см. ПЛАН, шаги 1-2).
5. Живой прогон (критерий §2 п.4): `python3 _generator/tools/priyomka.py _studio/zhurnal/2026-08-11_ochered-zayavok/kod_zayavki-yadro.md` (принятый заход этой же арки) → `✅ Л1 заявка Cowork открыта дольше 24 ч: проверено 0 заявок, просроченных (>24 ч, кроме постоянных исключений) нет`, «Гейтов зелёных: 10 из 10».
6. Гит-контур §0.1 первым ходом: ветка `zahod/zayavki-yadro` влита командой `git_zona.py merge zahod/zayavki-yadro --zone _generator/tools/git_zona.py --zone _generator/tools/fixtures/git_zona --zone .gitignore` — коммит `915bb08`/`5e94a39` виден в `git log --oneline`, `git_zona.py --help | grep zayav` дал три строки (`zayavka`/`zayavki`/`zayavka-zakryt`) — сошлось.

**Заявки Cowork.** Очередь вычитана `git_zona.py zayavki` первым ходом (§0.1 слот 0 задания) — заявок 0, постоянных исключений 1 (не связано с этой зоной). За ход новых заявок не появилось (повторная проверка перед коммитом — та же пустая очередь). Заявок к исполнению не было — раздел неприменим, ни одна не исполнялась.

**Что НЕ трогал.** Проект вне зоны (`_generator/tools/{bootstrap_zahod.py, check_zahod.py, priyomka.py, fixtures/zahod, fixtures/priyomka}`) не изучал и не правил. Три застейдженных файла `_studio/zhurnal/_INFRA-git/zayavki/sdelano/*.md` (смок-тест механизма заявок, не моя работа) — не трогал, оставлены как есть, в мой коммит не попали (зона `_generator/tools` их не покрывает). Ветки `materials-wt/tipologia-slajdov` и `materials-wt/zamer-blokov-l2` — не тронуты, как и предписано.

**Верификатор.** Не заводил — операция не лоссовая (только добавление кода и тестов, без удаления/перемещения/потери), самопроверка критерия §2 (4 пункта, все зелёные) покрывает риск.

**Повторяемость находок.** Не было находок, требующих отдельного захода: обе фикстуры сразу дополнялись по месту находки в той же сессии, а не откладывались.

**Необратимое.** Необратимого нет: только новый код в git-отслеживаемых файлах своей зоны плюс новый коммит (обратим `git revert`). Временные папки `mktemp -d`, использованные для живых прогонов и фикстур, самоочистились (`trap … EXIT` в фикстурах, ручное `rm -rf` в моих командах).

**Время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога).

**АРТЕФАКТ:** артефакта нет: заход инфраструктурный (правка инструментов фабрики), собранного файла для владельца не производит — результат виден в самих инструментах и в зелёных фикстурах.
**КОММИТ:** `1dfde8d` — «zayavki-podklyuchenie: очередь заявок вшита в §0.1 слот 0 (bootstrap_zahod.py), гейты З12/Л1» · `git_zona.py check --zone _generator/tools` → ✅ (проверено по каждому из пяти путей зоны отдельно, см. Г1 ниже — все ✅).
*Коммит прошёл через `--no-verify` с причиной: хук `pre-commit` поднял фикстуру `_generator/tools/fixtures/git_zona/PROGNAT.sh`, и она красная на ДВУХ ловушках («ЧАСТЬ D СЛОМАНА: файла-захода в основной папке нет», «worktree не заведён»), НЕ связанных с этим заходом — проверено `git stash` + повторный прогон фикстуры: те же две ловушки красны и БЕЗ моих изменений. Обход законен по `GIT-disciplina §3`, причина ушла в `INCIDENTY.md` автоматически инструментом `git_zona.py commit`.*

**ГИГИЕНА (§4.1, вывод команд, не пересказ):**
- **Г1.** `check --zone _generator/tools/bootstrap_zahod.py` → ✅; `.../check_zahod.py` → ✅; `.../priyomka.py` → ✅; `.../fixtures/zahod` → ✅; `.../fixtures/priyomka` → ✅ (все пять команд прогнаны отдельно после коммита, вывод у каждой — «работа доехала в git, вне git ничего нет»).
- **Г2.** Неприменимо, проверено при сборке: все пять путей зоны лежат внутри `materials` (совпадает со списком зоны в контракте — второго репозитория не задействовано).
- **Г3.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` → `zahod/tipologia-slajdov`, `zahod/zamer-blokov-l2` (2 ветки — те же две неприкосновенные ветки соседних живых заходов, названные в §0.1 слоте 3 задания как «НЕ гасить»; список не вырос за счёт моей работы: обе ветки существовали на входе, я их не заводил и не трогал).
- **Г4.** Ни одного нового `.py` не завёл — все правки в существующих файлах зоны (`bootstrap_zahod.py`, `check_zahod.py`, `priyomka.py`) и существующих фикстурах (`.sh`). Неприменимо.
- **Г5.** Ни одного нового `.md` не завёл. Неприменимо.
- **Г6.** `git --no-optional-locks show --stat 1dfde8d` → ровно пять путей, все из зоны `_generator/tools/{bootstrap_zahod.py, check_zahod.py, fixtures/priyomka/PROGNAT.sh, fixtures/zahod/PROGNAT.sh, priyomka.py}` — чужих путей нет.
