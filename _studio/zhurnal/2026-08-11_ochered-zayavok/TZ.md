# ТЗ арки «2026-08-11_ochered-zayavok»

> Короткое ТЗ арки — форма из `../../docs/spravka/PROFILI-CIFERBLATY.md §TZ`. Заполняется на Ф0 (до go); фиксирует, ЧТО и ЗАЧЕМ, чтобы заход исполнителю опирался на него.

- **Цель** — снять с владельца роль транспорта git-команд. Cowork не может писать в `.git` и потому
  сегодня пишет команду в чат; она не выдаётся, не копируется, молча не срабатывает, и следа не
  остаётся нигде. Вместо чата — файл-заявка в репозитории, который следующий заход забирает первым ходом.
- **Аудитория / скоуп** — три писателя репозитория: Cowork (пишет заявки), Claude Code (исполняет),
  владелец (не делает ничего и не должен).
- **Deliverable** — работающий механизм: `git_zona.py zayavka|zayavki|zayavka-zakryt`, вычитка
  очереди первой строкой каждого захода, гейт приёмки на просроченной заявке, раздел в каноне.
- **Границы / anti-scope** — не автоматическая гигиена: заход НЕ «приводит репозиторий в чистое
  состояние» по своей инициативе, он исполняет заявки и ничего сверх. Не сведение двух разошедшихся
  экземпляров канона git. Не разбор 43 ловушек фикстуры сверх трёх красных.
- **Ворота go/no-go** — цена замерена 11.08: за один вечер три потери (невыданная команда запуска,
  непрошедший `merge`, чистка веток на два пункта из восьми). Класс невидим: в `INCIDENTY.md`
  246 строк, «песочница: запись в .git запрещена» — 3.
- **Открытые вопросы** — вынесены в `PLAN.md §Открытое`: два дома канона; две метрики «что не влито».

<!-- Курс-масштабная арка? добавь явно (PROFILI §TZ): профиль + циферблаты · формат (число лекций/дней) · throughline (одна ось) -->
