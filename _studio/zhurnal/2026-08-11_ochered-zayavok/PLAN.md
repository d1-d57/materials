# ПЛАН арки «2026-08-11_ochered-zayavok»

## ⭐ СЕЙЧАС — первый конкретный шаг следующего хода

Запустить заход **З1 `kod_zayavki-yadro.md`** (собран 11.08, гейты прогнаны, стартовая строка выдана
владельцу). Пока он не принят, З2 не собирать: он зовёт подкоманды, которых ещё нет.

## Шаги

- [x] Ф0: арка заведена, проект решения написан — `PROEKT.md`
- [x] `POSTOYANNYE.md` заведён аналитиком (дом постоянных исключений)
- [ ] **З1 — ядро.** `git_zona.py`: `zayavka` / `zayavki` / `zayavka-zakryt`, строка в `doctor`,
      отказ `merge` из песочницы, отказ `merge` на ветке с живой рабочей папкой, ловушки в фикстуру.
      Файл: `kod_zayavki-yadro.md`. Зона: `_generator/tools/git_zona.py`,
      `_generator/tools/fixtures/git_zona`, `.gitignore`.
- [ ] **Приёмка З1** — `priyomka.py`, гейт 0 первым ходом.
- [ ] **З2 — подключение.** `bootstrap_zahod.py` (§0.1 вычитывает очередь первой строкой),
      `check_zahod.py` (З12: нет строки вычитки → заход не собирается),
      `priyomka.py` (Л1: заявка открыта дольше 24 ч → приёмка красная). Собирать ПОСЛЕ приёмки З1.
- [ ] **К — канон.** Раздел про заявки в скилл `disciplina-git`: устройство, оговорка «исполнение
      заявки — единственное законное действие за пределами зоны», порядок при конфликте.
      Правит аналитик, не заход.

## Открытое, названное и НЕ чинимое этой аркой

- **Два дома канона git разъехались на 153 строки** — `materials/_meta/disciplina/GIT-disciplina.md`
  (452 строки) против скилла `disciplina/skills/disciplina-git/SKILL.md` (399, но новее).
  Разбор — `PROEKT.md §8`. Новое правило пишется в скилл; сведение двух экземпляров — отдельный ход.
- **Две метрики «что не влито» дают разные ответы.** `git branch --no-merged` говорит «невлитых 0»,
  `git_zona.py doctor` — «на двух ветках лежит текст, которого здесь нет». Обе честны по своей мерке
  (достижимость коммитов против содержимого файлов), но слот 1 §0.1 заходов заполняется ПЕРВОЙ.
  Замер 11.08. Не чиним здесь — названо, чтобы не удивляться.

<!-- при закрытии арки — баннер «✅ АРКА ЗАКРЫТА (<дата>)» сверху (ARKA §7 п.4 / §10 C) -->
