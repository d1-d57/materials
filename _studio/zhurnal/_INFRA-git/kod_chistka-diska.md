# Канал исполнителя — chistka-diska (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — что удалять, уже замерено и названо поимённо; работа механическая, а решения — в таблице ниже. Риск снимается одной проверкой перед каждым удалением, а не рассуждением.

КОНТЕКСТ. У владельца кончается место на диске, компьютер тормозит, распознавание речи стало работать хуже. Папка `materials/` весит **5,2 ГБ, из них реальной работы — 180 МБ** (это её трекаемое git содержимое). Остальное — четыре кучи файлов, лежащих на диске мимо git.

**ЦЕЛЬ: снять с папки около 3,5 ГБ, не тронув НИ ОДНОГО файла, который есть в git, и ни одной книги.**

Приёмка — по ОТЧЁТУ, без построчной сверки.

⚠ **СТОП ДО ЦЕЛИ.** Ты НЕ трогаешь `.git` (1,1 ГБ раздутой истории) — её чистка переписывает историю целиком и делается отдельно, с бэкапом, и только после того, как сойдутся все ветки. Ты НЕ удаляешь книги и статьи. Скажи об этом строкой в отчёте.

🔴 **ПРАВИЛО БЕЗОПАСНОСТИ — ДВЕ ПРОВЕРКИ, НЕ ОДНА.** Перед КАЖДЫМ удалением спрашивай отдельно про индекс и отдельно про историю — это разные вопросы с разными последствиями:
```
git --no-optional-locks ls-files <путь> | wc -l          # 1) в индексе сейчас
git --no-optional-locks log --all --oneline -- <путь> | wc -l   # 2) в истории на ЛЮБОЙ ветке
```

| индекс | история | что значит | что делать |
|---|---|---|---|
| **не 0** | — | файл трекается | **НЕ УДАЛЯТЬ.** Назвать в отчёте, идти дальше |
| 0 | **не 0** | удалён из индекса раньше, но лежит в истории | Удалять можно, **удаление ОБРАТИМО**: `git show <ветка>:<путь>`. В отчёте назвать команду восстановления |
| 0 | 0 | файла нет в git вообще | Удалять можно, **удаление ОКОНЧАТЕЛЬНО**. В отчёте — отдельной строкой в «НЕОБРАТИМОЕ», с указанием, откуда файл берётся заново |

*Почему это уточнено 01.08:* заход по консолидации веток показал, что **файлы, живущие только на старых ветках, есть — и все до одного имеют в истории коммит сознательного удаления** (библиотечные pdf и снапшоты по решению 21.07, `.DS_Store`, переезд ученических файлов). То есть «нет в индексе» ≠ «пропадёт навсегда», и путать эти два случая — значит либо бояться там, где не надо, либо не предупредить там, где надо.

**Замер по нашим трём кучам уже сделан, перепроверь:** модель Whisper — **0 и 0**, то есть удаление окончательное, восстановление только повторной загрузкой. Снапшоты каталана — **0 в индексе, 2 коммита в истории**, то есть обратимо.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/zhurnal/_INFRA-git _studio/zhurnal/_INFRA-git/kod_chistka-diska.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add _studio/zhurnal/_INFRA-git _studio/zhurnal/_INFRA-git/kod_chistka-diska.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Числа — замер аналитика 01.08, **перепроверь их сам первым ходом**.

⚠ **Что изменилось между написанием захода и его запуском** (заход по консолидации веток отработал и принят):
- **Все ветки сошлись:** невлитой работы не осталось ни на одной, `main` и `zahod/sayt-drakon` влиты, сайт «кривой дракона» приехал 48 файлами. Значит чистить теперь безопасно — под тобой ничего не двигается.
- **Папка выросла до 5,3 ГБ** (приехал сайт), а путей вне git осталось **2** вместо 21 — остаток закоммичен. Число «21» в старом тексте ниже читай как историю, а не как задание.
- **`.git` перестал быть неприкосновенным по причине «идёт работа»** — ветки сошлись. Но по существу запрет держится: переписывание истории делается отдельно и с бэкапом, не этим заходом.

### Что удаляем — три кучи, все проверены: в git ноль файлов

| куча | вес | путь | почему удаляем |
|---|---|---|---|
| **Модель распознавания речи** | **2,95 ГБ** | `teorkat-vvedenie/repeticia/models/ggml-large-v3.bin` | Модель Whisper, скачанная под разовую расшифровку репетиции. Лежит внутри папки лекции — там ей не место; качается обратно одной командой, когда снова понадобится. **Это больше, чем все остальные кучи вместе.** Владелец: *«мы её скачивали специально для какой-то работы, я предполагал, что она почистится»* |
| **Аудио репетиции** | **227 МБ**, 18 файлов | `teorkat-vvedenie/repeticia/.rabota/*.wav` | Исходные записи; расшифровки давно сделаны и лежат текстом рядом (`akt-*-syroj.md`, `akt-*-macwhisper-syroj.md`). Владелец: *«записи репетиции убираем»* |
| **Снапшоты каталана** | **341 МБ**, 24 архива | `catalan/snapshots/*.tar`, `*.tar.gz` | tar-архивы рабочего дерева. **Владелец решил про них ещё 21.07** — решение записано прямо в `.gitignore`: *«git САМ и есть механизм снимков: tar-архив дерева внутри git — снимок снимка»*. Из git их тогда убрали, а с диска не удаляли. Владелец 01.08: *«что такое снапшоты, я не очень понимаю… наверняка это стоит убрать»* — **объясни ему в отчёте одной фразой, что это было** |

**Ход по каждой куче:** проверка `git ls-files` → удаление → печать освобождённого объёма командой `du`.

### Что НЕ удаляем и что с этим делаем вместо

🔴 **Книги и статьи — 271 МБ, 92 PDF. Не удалять ни одной.** Владелец объяснил зачем они: *«я их скачиваю, потому что мы по ним делаем поиск, когда хотим узнать конкретную информацию, которую нельзя установить из интернета… иногда нам нужно из всех книжек взять упражнения»*. То есть ценность — в **локальном полнотекстовом поиске**, и удаление её убивает.

Где они лежат (замер): `teoriya-kategoriy/istochniki/pdf` 171 МБ · `nagliadnaya-geometriya/istochniki/polina` 61 МБ · `catalan/biblioteka/istochniki/{knigi,stati-obzory,konspekty}` 92 МБ · остальное по мелочи в `<проект>/istochniki/pdf`.

**Твоя задача по ним — не сжатие, а РЕЕСТР.** Схема уже существует и работает в одном проекте: `catalan/biblioteka/` — `istochniki/` + `VYCHITANO.md` + `istochniki-perechislitelnaya.md`. Распространи её: собери **один сводный реестр** `_studio/zhurnal/_INFRA-git/REESTR-istochnikov.md` — строка на файл: путь · размер · проект · есть ли отметка в `VYCHITANO`. Реестр в git идёт, PDF — нет.

⚠ **Сжимать PDF не пытайся.** Они уже сжаты; перегон через ghostscript даёт 10–20 % и портит формулы. Архивировать в zip тоже почти ничего не даёт по той же причине — и ломает поиск, ради которого файлы и держат.

### Что трогать ЗАПРЕЩЕНО

- **`.git` (1,1 ГБ).** Раздут историей снапшотов, которые трекались до 21.07. `.gitignore` про это честно предупреждает: *«вес .git от этого не упадёт: история уже написана. Вернуть 265 МБ может только git-filter-repo — отдельная операция с бэкапом, не походя»*. У владельца сейчас несколько веток в работе — переписывание истории под ними сломает всё. **Не трогать, назвать в отчёте как отдельную будущую задачу.**
- Любой файл, у которого `git ls-files` вернул не ноль.

### Механизм, чтобы это не наросло снова — главное, что останется после захода

Владелец сформулировал так: *«подумаем, как оптимизировать остальное, чтобы оно неконтролируемо не накапливалось»*. Разовая уборка без механизма — это уборка, которую придётся делать снова.

Заведи **чекер `_generator/tools/check_ves.py`** (stdlib, детерминизм, без сети):
- печатает вес папки, вес `.git` и **вес трекаемого дерева** тремя числами;
- называет всё, что тяжелее порога и **не в git**, — с путём и размером;
- **красный при появлении НОВОГО тяжёлого файла вне git**, зелёный на известных (список известных — рядом, файлом);
- честно печатает, чего он НЕ проверяет.

Порог подбери по факту: он обязан быть тихим на здоровой папке ПОСЛЕ уборки — прогони его на ней прежде, чем сдавать. Красный на здоровом дереве — ложный гейт, его обойдут, и защита пропадёт (`../../docs/kak-delat/RUKOVODSTVO-zahodami.md`).

### КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ)

```
# 1. три кучи ушли
ls teorkat-vvedenie/repeticia/models/ggml-large-v3.bin 2>&1        # обязано быть «нет такого файла»
find teorkat-vvedenie/repeticia/.rabota -name "*.wav" | wc -l      # было 18 → обязано стать 0
find catalan/snapshots -name "*.tar*" | wc -l                      # было 24 → обязано стать 0
# 2. книги на месте, все до одной
find . -name "*.pdf" -not -path "./.git/*" | wc -l                 # было 92 → обязано остаться 92
# 3. git не пострадал — главная проверка захода
git --no-optional-locks status --porcelain | grep "^ D" | wc -l    # обязано быть 0: НИ ОДНОГО удалённого трекаемого файла
python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/_INFRA-git
# 4. вес
du -sh . ; du -sh .git                                             # было 5,2 ГБ и 1,1 ГБ
# 5. чекер тих на убранной папке
python3 _generator/tools/check_ves.py ; echo rc=$?                 # обязано быть 0
# 6. реестр покрывает все PDF
grep -c "^|" _studio/zhurnal/_INFRA-git/REESTR-istochnikov.md      # обязано сойтись с числом из п.2
```

**Готово = все шесть сошлись, и папка весит меньше 1,8 ГБ.**

**ОХВАТ В САМОМ ВЕРДИКТЕ:** «удалено N файлов из M намеченных, проверено git-принадлежностью N из N», числа посчитаны командой.

## 3. ВЕРИФИКАТОР — обязателен, операция необратимая

Удаление вне git не отменяется ничем. Свежий субагент, **другой метод**, ПОСЛЕ уборки:

- по `git ls-files` строит список ВСЕХ трекаемых путей и проверяет, что каждый существует на диске — **пропавших должно быть 0**;
- независимо пересчитывает PDF и сверяет с реестром;
- проверяет, что `check_ves.py` действительно умеет краснеть: подкладывает временный тяжёлый файл вне git и смотрит, что гейт его называет, потом убирает.

**Опровержение — полноценный успех.** Брак — только выдуманный вердикт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/_INFRA-git _studio/zhurnal/_INFRA-git/kod_chistka-diska.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/_INFRA-git _studio/zhurnal/_INFRA-git/kod_chistka-diska.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### Ход 0 — перепроверка чисел (сделана ДО плана, как велено §2)

Ветка: `git branch --show-current` → `arka/mat-kostyak`, rc=0. ✅ Совпала, `checkout` не нужен.

| что | в заходе | факт | команда |
|---|---|---|---|
| папка | 5,3 ГБ | **5.2G** | `du -sh .` |
| `.git` | 1,1 ГБ | **1.0G** | `du -sh .git` |
| трекаемое дерево | 180 МБ | **226M** | `git ls-files -z \| xargs -0 du -ch \| tail -1` |
| модель Whisper | 2,95 ГБ | **2.9G** (3 095 033 483 B) ✅ | `ls -l …/ggml-large-v3.bin` |
| аудио репетиции | 18 файлов, 227 МБ | **17 файлов, 224M** | `find …/.rabota -name "*.wav" \| wc -l` |
| снапшоты каталана | 24 архива, 341 МБ | **19 архивов, папка 337M** | `find catalan/snapshots -name "*.tar*" \| wc -l` |
| **книги и статьи** | **92 PDF, 271 МБ** | **166 PDF, 378M** | `find . -name "*.pdf" -not -path "./.git/*" \| wc -l` |
| `teoriya-kategoriy/istochniki/pdf` | 171 МБ | 177M | `du -sh` |
| `nagliadnaya-geometriya/istochniki/polina` | 61 МБ | 61M ✅ | `du -sh` |
| `catalan/biblioteka/istochniki` | 92 МБ | 92M ✅ | `du -sh` |

**Три расхождения, которые меняют работу, а не только цифру в отчёте:**

🔴 **1. КРИТЕРИЙ ГОТОВНОСТИ п.2 СЛОМАН — оспариваю до работы (§1).** В заходе: «было 92 → обязано остаться 92». PDF на диске **166**. Прогони критерий как написано — он провалится на здоровом дереве, где не удалено ни одной книги. Замер «92» покрывал только три названные папки; за его границей 166−92 остались `informacia-i-kody` (11), `nagliadnaya-geometriya/istochniki/mccme` (9), `graph-course` (6), `krivaya-drakona` (6, приехал с сайтом), `fibonacci` (3) и мелочь по проектам. **Поправка:** инвариант — не «92», а «сколько было, столько и осталось»; число снимается командой до и после (`KONSTITUCIYA §10`). Фиксирую до-значение **166** и сверяю с ним.

🔴 **2. `catalan/biblioteka/VYCHITANO.md` НЕ СУЩЕСТВУЕТ** — заход называет его образцом работающей схемы. Фактически: `catalan/biblioteka/istochniki/VYCHITANO.md` (на уровень ниже), и таких файлов в репо **8** — по одному в `<проект>/istochniki/`. Реестр строю по живой схеме (8 файлов), а не по названной в заходе.

⚠ **3. Снапшотов 19, не 24**, и в папке лежит 20-й файл — `pre-svg-patch-…-191712.html.bak` (210 КБ), не архив. Задание называет `*.tar`, `*.tar.gz` — удаляю ровно их, `.html.bak` **оставляю** (минимум; критерий п.1 на `*.tar*` его не считает). Назову в отчёте.

⚠ **Найдено вне трёх куч, НЕ трогаю** (сохранять по умолчанию, §Минимум): `teorkat-vvedenie/repeticia/audio/*.m4a` — 60M, исходники тех же репетиций; `kurs leto 2026/snapshots/pre-0koncept-move_2026-07-02.zip` — 25M. Задание их не называет. Чекер их назовёт — в отчёт как следующий круг.

### Двойная проверка по каждой куче — сделана ДО удаления (🔴 правило)

| куча | индекс | история | вердикт |
|---|---|---|---|
| `…/models/ggml-large-v3.bin` | **0** | **0** | **ОКОНЧАТЕЛЬНО.** Совпало с заходом. Возврат — только повторная загрузка |
| `…/repeticia/.rabota` (wav) | **0** | **0** | **ОКОНЧАТЕЛЬНО.** Заход этой пары не называл (сказано лишь «в git ноль файлов») — история тоже ноль, значит это второй необратимый пункт, не один |
| `catalan/snapshots` | **0** | **2** | **ОБРАТИМО**, `git show`. Совпало с заходом |

Обоснование удаления wav проверено по живым файлам: расшифровки на месте — `akt-1..6-syroj.md` и `akt-1..6-macwhisper-syroj.md` в `teorkat-vvedenie/repeticia/`.

### Что делаю дальше

1. **Точка отката** — зона не чиста в HEAD (`?? kod_chistka-diska.md`), значит коммит нужен, не фабрикация.
2. **Реестр** `REESTR-istochnikov.md` — 166 строк, путь · размер · проект · отметка в `VYCHITANO`. Собираю ДО удалений (PDF не трогаю вовсе, порядок роли не играет, но так до-число зафиксировано). Команду сборки вписываю в шапку реестра, чтобы он пересобирался, а не переписывался руками.
3. **Удаление трёх куч** — по одной, `du` до и после каждой.
4. **`check_ves.py`** + список известных рядом. Порог подбираю ПОСЛЕ уборки, чтобы был тих на здоровой папке.
5. Критерий готовности целиком (с поправкой п.2), верификатор-субагент, отчёт, коммит.

⚠ **Коллизия зоны, называю до работы.** ЗОНА — `_studio/zhurnal/_INFRA-git`, всё вне неё READ-ONLY. Но §2 прямо велит завести `_generator/tools/check_ves.py`, и критерий готовности п.5 запускает его именно оттуда. Читаю это как задание, а не как «заодно»: создаю **новый** файл по названному пути, ничего существующего вне зоны не правлю, и коммичу его своими путями. В `УРОКИ ФАБРИКЕ` — строкой.

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
