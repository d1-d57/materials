# Канал исполнителя — tool-contract (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — задача проектная и с высоким риском навредить: форму проверок и калибровку выбираешь по ходу, цена ошибки — заблокировать здоровые коммиты всем троим писателям.

КОНТЕКСТ. Монорепо `materials/`. Git — ТОЛЬКО через `_generator/tools/git_zona.py` (канон `GIT-disciplina.md`); коммиты судит хук `.githooks/pre-commit` — три гейта: уроки, картотека `--staged`, фикстура git_zona ТОЛЬКО при правке `git_zona.py`. Три писателя: владелец (zsh/macOS), Cowork (песочница, в git не пишет), Claude Code (host — ты). **Расследование 23.07 (корень рекуррентности):** за четыре сессии ~15 поломок сводятся к ОДНОМУ классу, повторяющемуся в новых аренах, потому что лекарство — ПРОЗА (заметка/канон/урок), а проза не переносится на следующий инструмент. Живое доказательство: фикстура git_zona поднимается хуком только при правке `git_zona.py`, на правку `bootstrap_*` НЕ срабатывает — **у гейта триггер уже его охвата.** ЦЕЛЬ: заменить прозу МЕХАНИЗМОМ — контракт инструмента, который хук проверяет на КАЖДЫЙ staged-инструмент фабрики, так что нарушитель физически не доедет до репо.
Приёмка — по ОТЧЁТУ. 🔴 **СТОП ДО ЦЕЛИ и ГЛАВНЫЙ РИСК: ты усиливаешь систему, НЕ ломая её.** Гейт с ложными срабатываниями блокирует здоровые коммиты всем троим — это ровно «сейф-код стал опасностью» (поломки 1.1/2.5, арена E расследования). Поэтому железно: судить ТОЛЬКО staged-диф (не ретроспективно всё дерево); проверки статические/грепом/AST, БЕЗ side-effects (**НИКОГДА не исполнять боевой инструмент с мусорным входом**); три существующих гейта хука не трогать; `--no-verify` остаётся громким клапаном. Здоровый коммит, который новый гейт остановил, — провал захода. **Токены: работай эффективно** — читай только якоря, ОДИН верификатор (не рой), MVP без золочения; часов не жалко, токенов — да.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке репозитория — `git checkout arka/mat-kostyak` первым ходом. Ветку НЕ переключай дальше, в другие НЕ коммить.
- **ЗОНА (можно менять):** .githooks/pre-commit _generator/tools/check_tool_contract.py _generator/tools/fixtures _studio/docs/kak-delat/GIT-disciplina.md _studio/zhurnal/_INFRA-git. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout arka/mat-kostyak`.
- Точка отката: `git add .githooks/pre-commit _generator/tools/check_tool_contract.py _generator/tools/fixtures _studio/docs/kak-delat/GIT-disciplina.md _studio/zhurnal/_INFRA-git` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО** (экономь токены — больше НИЧЕГО не открывай):
  1. `.githooks/pre-commit` — три гейта, форма `--staged` и `if … || exit 1`, закон «молчит когда чисто» (Р31), `env -u` для среды хука.
  2. `_generator/tools/git_zona.py` — ЭТАЛОНЫ, которые контракт кодифицирует (детект песочницы/`refuse_write`, `--no-optional-locks`, как строки печатаются владельцу). НЕ переписывай их.
  3. `_generator/tools/fixtures/git_zona/PROGNAT.sh` — как ловушка гоняется на одноразовом репо и мутируется в ОБЕ стороны; твоя мета-фикстура по этому образцу.
  4. `_studio/docs/kak-delat/GIT-disciplina.md` §0 и §5 — куда встанет ОДНА ссылка на контракт.
  5. `_studio/zhurnal/_INFRA-git/{SOSTOYANIE,ZHURNAL}.md` — состояние и хроника, бегло.
  Список инструментов для калибровки — командой (`ls _generator/tools/*.py`); тела целиком не перечитывай, контракту хватает грепа.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА — контракт инструмента как МЕХАНИЗМ, не проза

Корень эпистемический: урок живёт текстом и не переносится на следующий инструмент. Лечение — машинная проверка, которую хук гонит на каждый staged-инструмент. **Держись MVP:** дешёвые статические проверки, покрывающие корпус; без золочения (это и токены, и риск ложных).

**Шаг 1 — `_generator/tools/check_tool_contract.py --staged`**, статический линтер staged `_generator/tools/*.py`. Проверки грепом/AST, БЕЗ запуска инструмента:
- **Переносимость (кластер B):** нет GNU-измов (`touch -d`,`date -d`,`sed -i`,`grep -P`,`stat -c`,`xargs -r`) в коде и в печатаемых строках; каждый вызов git несёт `--no-optional-locks`; нет печати владельцу сырого shell (`rm `,`&&`,`$(`,glob) — только `git_zona.py <cmd>`.
- **Запись отказывает в песочнице (E/B):** файл, импортирующий снос/запись (`shutil`,`os.remove/rmdir`,`unlink`, git-`commit`/`worktree add`) или трогающий `.git`, обязан ссылаться на детект песочницы/`refuse_write`. Проверка ПРИСУТСТВИЯ защиты, не её работы.
- **Нет тихого «не то» на входе (кластер A):** файл с `argv`/`argparse` обязан иметь спутник-фикстуру, доказывающую отказ на кривом входе, ИЛИ пометку `# TOOL-CONTRACT: no-input`. Линтер проверяет НАЛИЧИЕ фикстуры/пометки — мусорный вход сам НЕ подаёт.
- **Честный сигнал (A/E):** только если дёшево и без ложных (напр. греп `print("❌"` в ветке нормального исхода); сомневаешься — оставь на фикстуры конкретных инструментов, скажи в отчёт. Ложных ради полноты не гони.
🔴 Судит **staged-диф** (как `check_kartoteka --staged`): пре-существующее нарушение НЕ блокирует, пока файл не правят. Молчит, когда чисто. Rc=1 только на реальном нарушении — с человеческим сообщением и путём к правилу.

**Шаг 2 — хук.** В `.githooks/pre-commit`: если в staged есть `_generator/tools/*.py` — гнать `check_tool_contract.py --staged` (с `env -u …` как у фикстуры git_zona), и если среди staged есть инструмент со спутник-фикстурой — поднять её. Это ОБОБЩАЕТ узкий триггер (сейчас только `git_zona.py`) на все инструменты. Три существующих гейта — не трогать, порядок и форму `if … || exit 1` сохранить.

**Шаг 3 — мета-фикстура** `_generator/tools/fixtures/tool_contract/PROGNAT.sh` на одноразовом репо: на КАЖДУЮ проверку — временный инструмент-нарушитель → линтер КРАСНЕЕТ; здоровый временный → ЗЕЛЁНЫЙ (обе стороны). Боевой репо не трогать, окружение вычистить (`unset $(git rev-parse --local-env-vars)`).

**Шаг 4 — калибровка (гейт безопасности, не формальность):** прогнать `check_tool_contract.py` по ВСЕМ текущим `_generator/tools/*.py` — **все обязаны пройти, ноль ложных.** Проверка, краснящая на здоровом коде, → СМЯГЧИТЬ проверку (она кривая), а не чинить инструмент (вне зоны, риск). Настоящую латентную дыру, если нашёл, — строкой в ОТЧЁТ, здесь не чини.

**Шаг 5 — канон/журнал:** `GIT-disciplina.md` — ОДНА ссылка (§0 или §5): «новый/правленый инструмент фабрики → контракт; держит гейт, не память; обход `--no-verify` — громко». `SOSTOYANIE` §механизмы (+строка). `ZHURNAL` — запись про эпистемический корень и его закрытие механизмом, с ЦЕНОЙ (тот же класс возвращался каждой сессией новой ареной; последняя цена — bootstrap-мусор чуть не уехал в гейт-коммит).

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. **Ноль ложных на текущем дереве:** линтер по всем `_generator/tools/*.py` не покраснил ни одного здорового — проверено N из N (перечисли).
2. **Реальное нарушение краснеет:** мета-фикстура, каждая проверка мутационно ловит нарушителя И пропускает здорового (проверок K → 2K мутаций).
3. **Три старых гейта хука целы:** healthy-коммит проходит; посаженное нарушение каждого из трёх (урок без цены, .md без KARTA, регрессия git_zona) по-прежнему краснеет.
4. **Триггер = охват:** правка ЛЮБОГО `_generator/tools/*.py` (не только `git_zona.py`) поднимает контракт — показать на `bootstrap_*`.
5. **Тихо когда чисто, быстро:** коммит, не трогающий инструменты, гейт не будит; линтер < ~1–2 с.
6. **Клапан и канон:** `--no-verify` по-прежнему обходит (не молча); контракт описан в каноне, ловушка «канон отстал» зелёная.
**Отрицательный вердикт несёт ОХВАТ:** «проверено N из N», а не «вроде норм».

## 3. ВЕРИФИКАТОР — ОБЯЗАТЕЛЕН, но ОДИН и УЗКИЙ (экономим токены)
Ровно ОДИН свежий субагент, пустой контекст, свой метод — НЕ рой. Узкий мандат: (1) **ложные срабатывания** — прогнать линтер по текущим инструментам и по 2–3 намеренно-здоровым временным; ни один не должен покраснеть (это главный риск — гейт, бьющий по своим); (2) **обход** — построить нарушителя, который проверку пытается обмануть (GNU-изм в строке-переменной, git-вызов через обёртку); доложить, что просочилось; (3) **старый хук цел** — healthy-коммит проходит, три прежних гейта живы. Атакует, не правит. Ложно покраснело / тихо просочилось — регресс, списком в отчёт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- .githooks/pre-commit _generator/tools/check_tool_contract.py _generator/tools/fixtures _studio/docs/kak-delat/GIT-disciplina.md _studio/zhurnal/_INFRA-git                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- .githooks/pre-commit _generator/tools/check_tool_contract.py _generator/tools/fixtures _studio/docs/kak-delat/GIT-disciplina.md _studio/zhurnal/_INFRA-git   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Заход закоммичен не в ту ветку, и его собственный контракт зоны это не ловит
Контракт зоны предписывал `arka/mat-kostyak`, а файл уехал в `ac93536` на `zahod/dek-paskal-v2`. `bootstrap_zahod.py` вшивает ветку в текст захода, но ничто не проверяет, что заход ЛЕЖИТ на той ветке, которую сам называет. Проверка дешёвая и напрашивается ровно там, где уже стоят остальные: `.md` вида `zhurnal/**/kod_*.md` со строкой «ветка `X`» в контракте зоны — коммитить только на ветку `X` либо краснеть.
**ЦЕНА:** первый ход захода («прочитай файл») упёрся в `File does not exist`, и вместо работы ушёл целый заход разведки — `git log --all`, `ls-tree`, грепы по `_studio/` — чтобы установить, что предпосылка входа ложна, а не что я ищу не там. На чужой ветке осталась копия-двойник без ПЛАНА и ОТЧЁТА, которая теперь расходится с рабочей (см. `## ВОПРОСЫ §3`). Это тот же класс, что `ZHURNAL §5.1`: правило вшито ТЕКСТОМ в шаблон, и ничто не краснеет, когда текст расходится с делом.

### Заход требует «ноль красных на дереве», но проверка может найти НАСТОЯЩЕЕ нарушение вне зоны
Критерий готовности 1 («линтер не покраснил ни одного здорового — N из N») читается как «rc=0 на всём дереве», и при буквальном чтении толкает смягчить проверку до зелёного. Но на живом дереве нашлись две **настоящие** дыры (`check_uroki.py`, `git_zona.py`) — в файлах ВНЕ зоны, чинить которые заход прямо запрещает. Критерий не различает «покраснело ложно» и «покраснело верно, а починка вне зоны», хотя реакции противоположны: первое — чинить проверку, второе — чинить нельзя вообще. Формулировать надо про ЛОЖНЫЕ срабатывания и требовать раздельный счёт: сколько зелёных, сколько ложных, сколько настоящих-вне-зоны.
**ЦЕНА:** развилка съела отдельный ход рассуждения на середине захода, и цена ошибки была несимметрична — «привести к зелёному» означало бы выпустить гейт, слепой ровно к тем двум нарушениям, ради которых он писался. В отчёте пришлось отдельно доказывать, что rc=1 — это успех, а не провал критерия.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 0. Оспоренная предпосылка ВХОДА (до работы, как требует §1)
**Заход лежал не на своей ветке.** Контракт зоны предписывает `arka/mat-kostyak`; сам файл закоммичен в `ac93536` на ветке `zahod/dek-paskal-v2` и на `arka/mat-kostyak` отсутствует (`git ls-tree -r HEAD -- _studio/zhurnal/_INFRA-git/` его не содержит; `_studio/.commit-plan` строка 19 его при этом называет — то есть `plan` собирался, когда файл ещё лежал untracked, а уехал он в чужую ветку). Восстановил его в зону из `ac93536` (`git show … > …`) — путь входит в зону, содержимое не менял. Урок с ценой — в `## УРОКИ ФАБРИКЕ`.

Точку отката НЕ фабрикую: зона в HEAD чиста (в дереве грязны только `catalan/`, `teoriya-kategoriy/`, `lsh-2026-…` — всё вне зоны), откат берётся из git.

### 1. Оспоренный КРИТЕРИЙ ГОТОВНОСТИ (п. 1 против «нет тихого не-то на входе»)
Критерий 1 требует **ноль красных** по всем 10 текущим `_generator/tools/*.py`. Проверка «файл с `argv`/`argparse` обязан иметь спутник-фикстуру ИЛИ пометку `# TOOL-CONTRACT: no-input`» этому противоречит **на живом дереве**: фикстуры есть у двух корней (`fixtures/git_zona`, `fixtures/uroki`), а инструментов десять; пометку я проставить не могу — все `_generator/tools/*.py`, кроме создаваемого мной, **вне зоны**. Жёсткая форма этой проверки означала бы: любой правит `check_view.py` одной строкой — гейт требует от него фикстуру. Это ровно «здоровый коммит остановлен», то есть провал захода.

**Поправка (смягчение, как предписывает Шаг 4):** ввожу явный greppable baseline `_generator/tools/fixtures/tool_contract/BASELINE` — список инструментов, существовавших ДО контракта. Инструмент из baseline освобождён **только** от требования фикстуры; проверки переносимости / `--no-optional-locks` / shell-в-чат / sandbox-guard применяются к нему в полную силу. **Новый** инструмент (нет в baseline) обязан иметь фикстуру или пометку. Долг виден строкой в файле и тает по мере обрастания фикстурами.

### 2. Что строю (MVP, порядок ходов)
1. **`_generator/tools/check_tool_contract.py`** — статический линтер, БЕЗ запуска проверяемого инструмента. Режимы: `--staged` (судит staged `_generator/tools/*.py`, молчит когда чисто, rc=1 только на реальном нарушении), `<пути…>` (для фикстуры и калибровки), `--fixtures-for <пути…>` (печатает, какие фикстуры поднять — для хука). Проверки:
   - **B. Переносимость** — GNU-измы (`touch -d`, `date -d`, `sed -i`, `grep -P`, `stat -c`, `xargs -r`) в исполняемом коде и в печатаемых строках; комментарии пропускаются (иначе гейт краснеет на собственном списке — цена уже оплачена в ловушке 7).
   - **B. `--no-optional-locks`** — AST: список-аргумент `subprocess.*`, начинающийся с `"git"`, обязан нести флаг.
   - **§0 канона. Shell владельцу** — строковые литералы внутри `print(...)` не должны нести сырой shell.
   - **E/B. Sandbox-guard** — файл, трогающий `.git` или делающий пишущие git-операции, обязан ссылаться на `in_sandbox`/`refuse_write` (проверка ПРИСУТСТВИЯ защиты, не её работы).
   - **A. Вход** — `argv`/`argparse` ⇒ спутник-фикстура или пометка; baseline освобождает (см. §1).
   - **Честный сигнал** (греп `❌` в здоровой ветке) — **НЕ делаю**: дёшево и без ложных не выходит, а ложное красное дороже пропуска. Строкой в отчёт, как разрешает Шаг 1.
2. **«Триггер = охват» — механизмом, а не текстом.** Фикстура объявляет в шапке `# TOOL-CONTRACT-COVERS: <файлы>`; хук поднимает её, когда staged ЛЮБОЙ из покрытых. В `fixtures/git_zona/PROGNAT.sh` добавляю строку с `bootstrap_arka|zahod|lekcia.py` — это и закрывает зазор, названный в `ZHURNAL §4.4` и в контексте захода как живое доказательство.
3. **`.githooks/pre-commit`** — ЧЕТВЁРТЫЙ блок, три существующих не трогаю, порядок и форма `if … || exit 1` сохранены; `env -u` как у блока git_zona. Фикстуру `git_zona` мой блок не поднимает повторно, когда её уже поднял старый блок (двойной прогон — только потеря секунды).
4. **`_generator/tools/fixtures/tool_contract/PROGNAT.sh`** — мета-фикстура на одноразовом каталоге: на КАЖДУЮ проверку временный нарушитель → красное, временный здоровый → зелёное. Боевой репо не трогает, окружение чистит.
5. **Калибровка** — прогон по всем 10 живым инструментам, ноль ложных, охват «N из N» цифрой.
6. **Канон/журнал** — ОДНА строка в `GIT-disciplina §5`, строка в `SOSTOYANIE §механизмы`, запись в `ZHURNAL` с ЦЕНОЙ. Ловушка 6 («канон отстал») обязана остаться зелёной.
7. **Верификатор** — ОДИН свежий субагент, узкий мандат §3.
8. **Коммит зоны** — последним ходом, по форме §4.

### 3. Названные риски
- **Главный:** ложное красное на здоровом коде. Управляю калибровкой (шаг 5) и правом смягчать проверку, а не чинить инструмент вне зоны.
- **Второй:** мой новый `.md` в `_studio/` не зарегистрирован в `KARTA.md §6` (ворота 5 `check_kartoteka`), а `_studio/docs/KARTA.md` мне READ-ONLY. Готовая строка — первым пунктом `## ВОПРОСЫ`; если хук встанет на этом, обхожу с названной причиной по §4 и пишу долгом в отчёт.

## ВОПРОСЫ — (заполняет исполнитель)

### 1. 🔴 РЕГИСТРАЦИЯ НОВОГО `.md` — за аналитиком, строка готова
`_studio/docs/KARTA.md §6` мне READ-ONLY (контракт зоны сильнее требования корня). Строка ниже вставляется в перечень файлов `_INFRA-git` в §6 — сразу после `_INFRA-git/kod_bootstrap-guard.md (…)`, тем же форматом:

> ` · `_INFRA-git/kod_tool-contract.md` (**заход: правила инструмента фабрики держит ГЕЙТ, а не память исполнителя — `check_tool_contract.py --staged` на каждый правленый `_generator/tools/*.py`; фикстура объявляет охват строкой `TOOL-CONTRACT-COVERS`, чем закрыт зазор «триггер уже охвата»**)`

Пока строки нет, ворота 5 `check_kartoteka` краснеют на МОЁМ пути — это не чужой долг, обходить его нечем, поэтому смотри строку КОММИТ в отчёте.

### 2. Две настоящие дыры в живых инструментах — чинить не мне (вне зоны)
Найдены калибровкой, обе описаны в `SOSTOYANIE §хвост 00`, обе в одну строку. **До починки гейт краснеет на правке этих двух файлов** — красное настоящее, не ложное, но правящему это будет неожиданно, поэтому лучше закрыть заранее:
- `check_uroki.py:82` — вставить `"--no-optional-locks"` вторым элементом списка аргументов.
- `git_zona.py:338` — вместо цепочки `cd … && rm -f .git/*.lock && find …` печатать `python3 _generator/tools/git_zona.py clean`.

Чинить их будет тот, кому эти файлы в зоне; после починки красных на дереве не останется вовсе.

### 3. Заход лежал не на своей ветке — нужно решение, что делать с копией в `zahod/dek-paskal-v2`
Файл закоммичен в `ac93536` на ветке `zahod/dek-paskal-v2`, хотя его собственный контракт зоны предписывает `arka/mat-kostyak`. Я восстановил его в свою ветку и работал в ней. **Копия на `zahod/dek-paskal-v2` осталась и разойдётся с этой** (там нет ни ПЛАНА, ни ОТЧЁТА). Что с ней делать — ваше решение: чужая ветка мне READ-ONLY, и я её не трогал.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и ЗАЧЕМ
Проза заменена механизмом — пять файлов, ровно зона, ничего сверх.

1. **`_generator/tools/check_tool_contract.py`** — статический линтер. Пять проверок, каждая кодифицирует УЖЕ ОПЛАЧЕННЫЙ урок канона: переносимость (GNU-измы), `--no-optional-locks` на вызовах git, сырой shell владельцу, пишущая операция без `in_sandbox`/`refuse_write`, разбор входа без доказательства. Проверяемый инструмент НИКОГДА не исполняется — только текст, AST, греп.
2. **Охват объявляет фикстура** строкой `# TOOL-CONTRACT-COVERS:` в шапке; линтер и хук читают её. Это и есть лечение зазора «триггер уже охвата»: `fixtures/git_zona/PROGNAT.sh` теперь объявляет три `bootstrap_*`, которые сторожит ловушка 14, и правка любого из них поднимает фикстуру.
3. **`.githooks/pre-commit`** — четвёртый блок. Три существующих не тронуты, порядок и форма `if … || exit 1` сохранены.
4. **`fixtures/tool_contract/PROGNAT.sh`** — мета-фикстура, 33 утверждения на одноразовом каталоге.
5. **`BASELINE`** — послабления названы явным списком (5 инструментов), а не спрятаны в смягчённой проверке.
6. **Канон/журнал** — одна ссылка в `GIT-disciplina §5`, две строки в `SOSTOYANIE §механизмы` + хвост 00, запись `ZHURNAL §сессия 5`.

### Как проверил (код возврата читал первым)
- **Калибровка, 11 из 11:** 9 зелёных, 2 красных. 🔴 **Оба красных — НАСТОЯЩИЕ нарушения канона, не ложные** (см. ниже). Ложных срабатываний на здоровом коде — ноль; по дороге смягчены четыре кривые проверки, каждая ловилась калибровкой (докстринг с перечнем запрещённого; `"/.git/"` в списке ИСКЛЮЧЕНИЙ `check_kartoteka`; обратный апостроф markdown-цитирования — 60+ ложных; `.remove()` у обычного списка).
- **Мутационно, 5 из 5 проверок:** снял проверку → мета-фикстура краснеет. Файл восстановлен, хэш сверен.
- **Внутри ХУКА, на одноразовом репо** (как требует `GIT-disciplina §5` — «в тех условиях, где гейт сработает»): три старых гейта живы (урок без ЦЕНЫ · `.md` без `KARTA` · регрессия `git_zona` со снятым pathspec — все три по-прежнему краснеют); `--no-verify` провозит; коммит без инструментов гейт не будит; здоровая правка инструмента проходит; нарушитель останавливается с диагностичным сообщением; **правка `git_zona.py` и `check_uroki.py` с их пре-существующим долгом проходит**, а нарушение в добавленной строке краснеет. Линтер по всему корпусу — 0.27 с.
- Три фикстуры (`git_zona`, `uroki`, `tool_contract`) зелёные, включая ловушку 6 «канон не отстал» после правки канона.

### Верификатор — снял два дефекта, которые убивали затею молча
Один свежий субагент, пустой контекст, свой метод: 23 судимых файла, 9 нарушителей, 5 сценариев уровня хука, 4 варианта подделки охвата. **Нашёл два дефекта, невидимых ни на фикстурах, ни на калибровке, — оба починены и закрыты ловушками:**
- **линтер читал рабочую копию вместо staged-блоба** — застейджил нарушителя, поправил файл на диске, гейт зелёный, нарушитель уезжает в коммит;
- **судил файл целиком, а не добавленные строки** — из-за чего `git_zona.py` был бы незакоммитабелен без `--no-verify` с первого же дня.

Заодно по его находкам закрыты формы, которые он справедливо назвал не трюком, а обычным стилем: GNU-изм списком аргументов, `os.system("git commit …")`, `.py` в подпапках `tools/`, GNU-изм в `.sh`, невычищенные `GIT_ZONA_REPO`/`TOOL_CONTRACT_HOME` (обе молча выключали гейт целиком), объявление охвата подделкой строки в heredoc — и три ложных срабатывания. Мета-фикстура выросла с 21 утверждения до 33, все зелёные.
**Остались НЕ закрытыми (сознательно, MVP):** обёртка вокруг git, названная не `git` (узнаётся только это имя); подкоманда в переменной; GNU-изм через `.format()`/конкатенацию. Это уже обходы-трюки, а не стиль; закрывать их дороже, чем они стоят, и цена ошибки в другую сторону выше.

### Что НЕ трогал
Ничего вне зоны. Три существующих гейта хука, ядро `git_zona.py`, все прочие `_generator/tools/*.py`, `_studio/docs/KARTA.md`, ветка `zahod/dek-paskal-v2` с копией этого захода. Пока я работал, рядом прошёл чужой коммит `b93d112` — коммитил по явным путям, чужого не подобрал.

### 🔴 Найдены две НАСТОЯЩИЕ латентные дыры — не чинил, вне зоны
Обе в `SOSTOYANIE §хвост 00` и в `## ВОПРОСЫ §2`, обе в одну строку:
- `check_uroki.py:82` — git без `--no-optional-locks`, причём из `pre-commit`, то есть при каждом коммите всех троих писателей.
- `git_zona.py:338` — печать владельцу `cd … && rm -f .git/*.lock && find …`: цепочка с glob из таблицы цен `§0`, взамен которой и заводилась подкоманда `clean`.

Это лучший аргумент за механизм: правило про обе существовало ПРОЗОЙ в каноне, который читали и на который ссылались, — и обе дыры всё это время жили в репозитории.

### Открытое «возвращаться»
- **Боем не проверено:** настоящего чужого коммита через новый гейт ещё не было. Первая правка инструмента владельцем — и есть проверка; результат строкой в `ZHURNAL`.
- `BASELINE` тает по мере обрастания фикстурами — 5 инструментов ждут своих.

### Время и токены
Один заход, ~1 ч 20 мин прогона, ~230 тыс. токенов (из них ~117 тыс. — верификатор).

**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
