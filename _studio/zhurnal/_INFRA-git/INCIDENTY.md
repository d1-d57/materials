# INCIDENTY — сырые симптомы неуспешных коммитов (пишет сам git_zona.py)

> Входящая корзина, НЕ журнал уроков: при каждом неуспешном `commit`
> инструмент САМ дописывает сюда строку — в любом чате, без просьбы.
> Руками строки не заводятся. Разобранное переносится в `ZHURNAL.md`
> уроком с ЦЕНОЙ; здесь у строки статус меняется на `→ ZHURNAL`.
> Формат: `- дата время · ветка · симптом · → подсказка · статус`.

- 2026-08-11 15:55 · zahod/modeli-arn · коммит с --no-verify: чужой долг: fixtures/dvizhok/PROGNAT.sh (охват над bootstrap_zahod.py) не копирует check_marker.py в свой одноразовый тестовый репозиторий — гейт check_marker.py добавлен позже (заход zapret-rukopisnyh, a91a3b2) и стенд не обновлён; ловушка 5 фикстуры падает на 'No such file' независимо от моей правки · → назвать причину долгом в отчёте захода; своё красное — чинить · статус: открыт
