# VERDIKTY — вердикты по КЛАССАМ инцидентов коммита (не по строкам)

> Дом гейта `check_incidenty.py`. Одна строка = один класс, ключ — ДОСЛОВНЫЙ
> вывод функции `cls()` из `RAZBOR-povtorov.md §6`: разойдётся ключ — разойдётся
> и сверка. Формат жёсткий:
> ```
> - <ключ класса> · вердикт: <дефект → заход <имя-файла-захода> | шум: <почему это не дефект> | закрыт <ГГГГ-ММ-ДД>> · дата данных: <ГГГГ-ММ-ДД ЧЧ:ММ>
> ```
> `дата данных` — с точностью до минуты (заход `kod_pohorony-main.md` 2026-08-04):
> вердикт «шум» сверяется СТРОГО по этому штампу против строк класса, и внутри
> одного календарного дня без времени порядок «вердикт → строка» не восстановить.
> Старый формат (только дата, без времени) гейт по-прежнему читает — как полночь
> этого дня; строки написаны, только когда это не меняет смысл вердикта.
> `<не собран>` вместо имени файла — законная форма для заходов очереди
> `RAZBOR-povtorov.md §4`, которых ещё нет на диске: гейт на это не краснеет
> (случай 2 краснеет только на имени файла, которого нет РЕАЛЬНО).
> Заполнено заходом `kod_gejt-razbora.md` 2026-08-04 по таблице `RAZBOR-povtorov.md §2`.

- A регистрация .md vs зона · вердикт: закрыт 2026-07-30 · дата данных: 2026-08-04
- B окружение песочницы · вердикт: дефект → заход <не собран> · дата данных: 2026-08-04
- C прочий чужой долг · вердикт: шум: класс-корзина (разнородный долг под `--no-verify`, ключи не образуют одного дефекта) — последний разбор 2026-08-04; единственный найденный внутри НАСТОЯЩИЙ дефект (строка 2026-08-04 04:33, check_ves.py не спрашивал git про дубли блобов) закрыт заходом kod_korzina-i-dolgi.md, коммит fd41e84 · дата данных: 2026-08-04 14:00
- D merge отбит грязным деревом · вердикт: дефект → заход <не собран> · дата данных: 2026-08-04
- E конфликт путей · вердикт: шум: конфликты при merge — обычная жизнь ветвления, чинятся руками · дата данных: 2026-08-04 14:00
- F merge без диагноза · вердикт: дефект → заход <не собран> · дата данных: 2026-08-04
- G коммит не целиком · вердикт: дефект → заход <не собран> · дата данных: 2026-08-04
- H commit без плана · вердикт: дефект → заход <не собран> · дата данных: 2026-08-04
- I план битый · вердикт: дефект → заход <не собран> · дата данных: 2026-08-04
- K мусорные аргументы · вердикт: дефект → заход <не собран> · дата данных: 2026-08-04
- L песочница · вердикт: закрыт 2026-07-28 · дата данных: 2026-08-04
- план называет пути, которых нет на диске · вердикт: дефект → заход <не собран> · дата данных: 2026-08-04
- E конфликт путей · вердикт: не шум, механизм назван · причина: конфликты при слиянии двух отработавших веток в одну арку — предсказуемое следствие параллельных заходов в общей зоне _studio/docs/KARTA.md, где обе стороны ДОБАВЛЯЮТ разные регистрации; лечится не устранением конфликта, а разрешением через объединение обеих сторон с проверкой каждой записи грепом (сделано в консолидации 05.08, коммиты cae8238/27519e7) · дата данных: 2026-08-05 14:30

## Разбор при закрытии сессии 2026-08-07/08 (арка `2026-08-07_arhitektura-slajdov`)

🔴 **Попутная находка: вердикт от 2026-08-05 по классу E СЛОМАН и потому невидим для гейта.**
Он записан формой «не шум, механизм назван · причина: …», а парсер `check_incidenty.py:104-118`
знает ровно три формы — `дефект → заход <имя>`, `шум: <причина>`, `закрыт <дата>`. Всё прочее
разбирается в `None`, то есть «вердикта нет». Из-за этого гейт три дня продолжал предъявлять
старый вердикт «шум» от 2026-08-04, а содержательный разбор 05.08 лежал рядом и не считался.
Класс отказа знакомый: **запись сделана, но механизмом не прочитана.**

**E «конфликт путей» — не шум.** С 2026-08-04 девять новых строк, все одного вида: merge ветки
захода в `arka/mat-kostyak`, конфликт в 1–3 путях. Механизм назван ещё 05.08 и подтверждается
повторами: параллельные заходы ДОБАВЛЯЮТ разные регистрации в общий `_studio/docs/KARTA.md`,
и append-конфликт там структурен, а не случаен. Девять повторов за четыре дня — это не «обычная
жизнь ветвления», это цена, которую платит аналитик на каждом слиянии.

**C «прочий чужой долг» — не шум, и это не корзина.** Обе новые строки (2026-08-06 21:15 —
битая ссылка на несуществующую карточку `reserch-cikl`, оставленная прежним исполнителем;
2026-08-07 00:36 — фикстура `sessiya/PROGNAT.sh` падает на ПОЛОЖИТЕЛЬНОМ контроле не из-за патча)
несут один механизм: **чужой незакрытый долг краснеет на хуке у того, кто его не заводил, и
единственная доступная ему дверь — `--no-verify`.** Это ровно та причина, на которую в разборе
`kod_registracia-bez-obhoda.md` пришлось 27 обходов из 28.

- E конфликт путей · вердикт: дефект → заход <не собран> · дата данных: 2026-08-07 23:40
- C прочий чужой долг · вердикт: дефект → заход <не собран> · дата данных: 2026-08-07 23:40

## Разбор 2026-08-15 (закрытие сессии «рычаги заходов»)

**Занятое имя надгробия — не шум, но и не новый класс.** `zakryt-vetku` отказал по
`zahod/dovodka-fazy-intervyu`: тег `mogila/zahod/dovodka-fazy-intervyu` уже занят предыдущей,
незавершённой попыткой закрытия ТОЙ ЖЕ ветки. Механизм: надгробие ставится до полного успеха
операции, и повторный заход на ту же ветку упирается в собственный след. Цена пока одна ветка,
и она не гасится — работа при этом цела (ветка влита, содержимое в основной). Лечится либо
идемпотентным именем (суффикс попытки), либо проверкой «тег указывает на тот же коммит →
считать успехом», а не отказом. Заход не собран: за паузу ничего не портится, ветка просто
висит.

**Чужой лок дольше 90 с — шум песочницы, а не дефект.** Единственный повтор, старейшая строка
десятидневной давности; источник — параллельная работа Cowork и живого git на одной машине.
Ни одной потери за десять дней, лок снимается сам. Пересматривать при повторе с потерей.

- zakryt-vetku --vsyo-ravno: zahod/dovodka-fazy-intervyu — ветка — незавершённая предыдущая попытка ЭТОГО ЖЕ захода (форк до консолидации kartoteka-intervyu+razbros-po-fazam, охват 74 записи вместо текущих 145); заход прямо предписывает пересобрать ветку от свежего arka/mat-kostyak (0.3), где 04.5-intervyu/ уже консолидирован; работа не теряется — надгробие mogila/ восстановимо voskresit · вердикт: дефект → заход <не собран> · дата данных: 2026-08-15 04:18
- чужой лок держится дольше 90 с · вердикт: шум: параллельная работа песочницы и живого git, за 10 дней ни одной потери · дата данных: 2026-08-15
- push упал · вердикт: шум: сеть, а не дефект инструмента — два повтора за восемь дней (2026-08-08 17:38 и 2026-08-11 02:01, оба на `arka/mat-kostyak`), в обоих случаях инструмент сам напечатал верное лечение — «коммиты целы; повторить `commit --push`», и повтор срабатывал. Ни одной потери работы; вывоз 15.08 прошёл штатно (23 коммита на origin). Пересматривать, если появится случай, где коммиты НЕ целы, либо если повтор перестанет помогать · дата данных: 2026-08-16
