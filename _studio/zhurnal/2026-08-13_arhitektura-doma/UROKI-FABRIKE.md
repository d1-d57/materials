# УРОКИ ФАБРИКЕ — арка «2026-08-13_arhitektura-doma»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

### Заход с зоной в СОСЕДНЕМ репозитории оставляет свою побочную работу вне git — по построению
ЦЕНА: заход `arhitektura-doma` (зона — `disciplina`) породил в `materials` тринадцать файлов ресёрча, ~300 КБ, включая четыре отчёта субагентов и верификатор развода. Все они лежат в worktree `materials-wt/arhitektura-doma` **и не попали в git ни в одну ветку**: контракт зоны запрещал исполнителю писать и коммитить в `materials`, а работать без порождения файлов там он не мог — стартовая папка была именно там. Следствие поймано приёмкой: `STANDART-DOMA.md` ссылается на `materials/_studio/.../RESERCH-arhitektura.md`, а по этому адресу файла НЕТ — стандарт вышел с битой ссылкой на собственное обоснование. Исполнитель назвал дефект сам и сказал, что структурно не может его починить. Это дефект ЗАХОДА, не исполнителя: аналитик поставил его между двумя обязательными указаниями.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Ветка захода заводится в одном репозитории, а работа идёт в другом — и коммит уходит в main
ЦЕНА: тот же заход. Стартовая строка заводила worktree и ветку `zahod/arhitektura-doma` в `materials`, а зона лежала в `disciplina`, где ветки не заводилось вовсе. Исполнитель закоммитил зону прямо в `main` соседнего репозитория (`3c59baf`) — единственное, что ему оставалось. Приёмка искала работу в ветке и не нашла: `git log main..zahod/arhitektura-doma` пуст, потому что ветки нет. Гейт `priyomka.py` тоже промахнулся дважды — Г1 не нашёл хэш, Г3 не нашёл артефакт: оба смотрят в `materials`.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Карта инструментов не увидела клаузу, у которой в заголовке стоит пометка в скобках
ЦЕНА: `karta_instrumentov.py` напечатал «гейты входа: 11», живой `check_zahod.py` даёт 12 (`grep -c '^# ── З'`). Пропущена ровно З6: регексп ждёт двоеточие сразу после номера, а в файле стоит `# ── З6 (ПЕЧАТЬ, не гейт): …`. Пропала не случайная клауза, а именно та, что стоит ПЕРВЫМ пунктом очереди рычагов (перенос З6 в жёсткие, 12 Σ кратности) — карта не показывает ровно тот рычаг, который собирались чинить первым. Поймано приёмкой, потому что критерий захода велел сверить число генератора с живым файлом руками один раз.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Гипотеза «спорные — это записи про обе стороны разом» не подтвердилась ни разу
ЦЕНА: владелец предположил 14.08, что спорные лежат между заходом и приёмкой, и попросил класть такие в оба дома. Правило заведено (пятый законный исход, `STANDART-DOMA.md`) — и при разборе 74 записей ПО АДРЕСУ первоисточника подошло к **нулю** из них. Реальная причина спорности оказалась иной: 23 записи чужого дома `meta`, 5 иллюстраций и слайдов, 6 вообще не про инструмент (содержание курса), 14 неразличимы и после адреса. Цена не в потерянной работе, а в том, что решение принималось по догадке о содержании кучи, которую никто не открывал: правило завели верное, но задача, под которую его завели, была другой.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Приёмка не может прочитать отчёт заходом в СОСЕДНИЙ репозиторий — гейты смотрят не туда
ЦЕНА: `priyomka.py` дал Г1 «хэш не найден» и Г2/Г3 «артефакт отсутствует» на здоровом отчёте — оба гейта ищут в `materials`, а работа лежала в `disciplina` (коммит `30f22fe`). То же было днём раньше на заходе `arhitektura-doma`. Итог: на кросс-репозиторных заходах машинная приёмка даёт **два ложных красных из десяти гейтов**, и различить их от настоящих можно только руками. Второй раз подряд — значит класс, а не случай.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Bash держит cwd между вызовами, и git-команда уходит не в ту папку молча
ЦЕНА: исполнитель выполнил первый git-контур из корня `materials` вместо своего worktree — коммит ушёл на общую ветку `arka/mat-kostyak` и подмёл ~74 строки чужих незакоммиченных правок `KARTA.md`, потому что `git add` берёт ПОЛНОЕ содержимое файла, а не дифф. Поймал сам до конца хода и восстановил (cherry-pick, mixed-reset, точечная чистка) — ~20 минут и заметный кусок токенов. Приёмка проверила независимо: `git branch --contains` ошибочного коммита пуст, KARTA.md на 702 строки и 303 записи, обе стороны влития только добавляли — чужое цело. Класс назван исполнителем точно: инструкция не переспрашивает cwd перед git-командой.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Новый ОБЯЗАТЕЛЬНЫЙ флаг генератора ломает все чужие фикстуры, которые его зовут
ЦЕНА: `--intervyu` сделан обязательным по образцу `--kanal` — и мгновенно уронил четыре фикстуры ВНЕ зоны захода: `dvizhok` (была ЗЕЛЁНОЙ до правки, стала rc=2 — проверено приёмкой контрольным прогоном на коде без захода), `korni`, `gigiena`, `register_doc`. Все они зовут `bootstrap_zahod.py` и ни одна не передаёт нового флага (`grep -c '--intervyu'` = 0 у семи фикстур из восьми). Следствие: `pre-commit` красный у каждого, кто тронет генератор, пока не починено. Каждая чинится одной строкой, но контракт зоны запрещает исполнителю трогать их — он честно назвал это пунктом очереди №1 и не выдал своё красное за чужое. Класс: **обязательный параметр — это правка ВСЕХ точек вызова, а зона захода их не покрывает по построению.**
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:

### Вынос блока в субагента УДВОИЛ блок, пока задание печатали в сам заход
ЦЕНА: первая редакция шага «контур в субагента» вставила промпт субагента прямо в §0.1 — блок вырос с 28 строк до 53, то есть вынос сделал ровно обратное цели. Поймано замером самого исполнителя, а не глазом. Переделано: задание печатает команда `bootstrap_zahod.py --zadanie-subagentu`, инструкции живут в инструменте, в заходе остаётся вызов. Итог 28 → 18 строк; приёмка независимо намерила на своих файлах 45 → 20. Класс: **вынос считается по РАЗМЕРУ ОКНА, а не по факту «текст переехал в другую роль»** — переезд текста внутрь того же файла не является выносом.
ДОМ: disciplina/doma/zahody/POKRYTIE.md
ВЕРДИКТ:
