# Канал исполнителя — dobivka-spornyh (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — суждение по адресу расписано правилом, план по ходу не рождается.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — суждение по адресу расписано правилом, план по ходу не рождается.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/dobivka-spornyh.
Зона у тебя в ДВУХ репозиториях: disciplina (дома и стандарт) и папка арки в materials
(куда ты первым ходом забираешь в git 13 файлов ресёрча, висящих вне истории).

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-13_arhitektura-doma/kod_dobivka-spornyh.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Первым ходом: python3 _generator/tools/git_zona.py worktree add dobivka-spornyh --branch zahod/dobivka-spornyh,
затем чтение захода, затем секция ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-13_arhitektura-doma» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/festive-sweet-clarke/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. В репозитории `disciplina` у каждого скилла есть «дом инцидентов» — место, где копятся его проблемы и строятся рычаги против них. 14.08 заход `arhitektura-doma` развёл 156 записей под-дома «заходы и приёмка» на три списка: `zahody` 65 · `priyomka` 17 · **СПОРНЫЕ 74** (`doma/SPORNYE.md §Б`).

Прошлый этап: тот заход честно объявил границу своего метода — судил **по колонкам среза** («что за инцидент» + «рычаг»), а **адрес первоисточника не поднимал**. Отсюда и размер спорной кучи. Приёмка 14.08 разобрала эти 74 грубо и получила: ~39 «решать по адресу» · ~12 про `check_tool_contract.py` (общий тестер любого `check_*.py` фабрики) · ~11 похоже на ОБЕ стороны · ~10 чужой дом (иллюстрации/слайды) · 2 записи про содержание курса с Σ кратности 38. **Числа приблизительные, снятые грубой эвристикой по словам — сверь сам, не верь им.**

ЦЕЛЬ: разобрать 74 спорных ПО АДРЕСУ первоисточника и раздать по домам, включая новый законный исход «в оба дома копией». После этого очередь скилла станет настоящей, а не завышенной.

Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 **СТОП ДО ЦЕЛИ, громко: ты добиваешь СТРУКТУРУ и не строишь ни одного рычага.** Расстановка рычагов — следующая сессия, решение владельца.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. запись, относящаяся к обеим сторонам, идёт в ОБА дома копией — решение владельца 14.08: «внутри одного отчёта две проблемы, так можно и туда и туда, в такой ситуации мы к двум домам причисляем спокойно»
2. спорные судятся ПО АДРЕСУ первоисточника, а не по колонкам среза: прошлый заход честно объявил, что адрес не поднимал — отсюда 39 записей «решать по адресу»
3. заход добивает СТРУКТУРУ и по-прежнему НЕ строит рычаги: расстановка рычагов — следующая сессия (решение владельца)
4. канал app, Sonnet

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/dobivka-spornyh`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/dobivka-spornyh`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add dobivka-spornyh --branch zahod/dobivka-spornyh
  cd ../materials-wt/dobivka-spornyh
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `/Users/ivanyakovlev/Documents/GitHub/disciplina/` `_studio/zhurnal/2026-08-13_arhitektura-doma/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

🔴 **ЗАБРАТЬ В GIT ЧУЖУЮ РАБОТУ — ПЕРВЫМ ХОДОМ, ДО СВОЕЙ ЗАДАЧИ.** В рабочей папке `../materials-wt/arhitektura-doma/_studio/zhurnal/2026-08-13_arhitektura-doma/` лежат **13 файлов ресёрча предыдущего захода, ~300 КБ, не попавшие в git ни в одну ветку**: `RESERCH-1-disciplina.md` · `RESERCH-2-materials-studio.md` · `RESERCH-3-malye-proekty.md` · `RESERCH-4-materials-vne-studio.md` · `RESERCH-arhitektura.md` · `RAZVOD-156.md` · `VERIFIKATOR-razvod-156.md` · `srez-7.2-vhodnye-156.md` · `table-zahody-65.md` · `table-priyomka-17.md` · `table-spornye-74.md` · `joined-3-lists.json` и копию `_studio/docs/KARTA.md` с уже сделанной регистрацией. Смерть worktree = потеря всей ночной работы. Перенеси их в ОСНОВНУЮ папку арки и закоммить вместе с `_studio/docs/KARTA.md`. *Почему это досталось тебе: у прошлого захода зона была `disciplina`, писать и коммитить в `materials` контракт ему запрещал — он назвал это сам и не смог починить структурно.* После переноса ссылка `STANDART-DOMA.md` на `materials/_studio/zhurnal/2026-08-13_arhitektura-doma/RESERCH-arhitektura.md` станет живой; сейчас она битая — проверь `test -e` до и после.

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

Гасить нечего. Чужие ветки и рабочие папки не трогай. ⚠ Рабочую папку `materials-wt/arhitektura-doma` НЕ удаляй даже после переноса — решение о её судьбе принимает приёмка.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/dobivka-spornyh`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `/Users/ivanyakovlev/Documents/GitHub/disciplina/` `_studio/zhurnal/2026-08-13_arhitektura-doma/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО это:
  · `~/Documents/GitHub/disciplina/doma/SPORNYE.md` §Б — твои 74 записи, целиком, вместе с преамбулой (там названы классы, по которым они сюда попали);
  · `~/Documents/GitHub/disciplina/doma/README.md` §1а (таблица скилл→дом) и §3 (контракт формы: четыре вида рычага, и §3.1 — «судить по АДРЕСУ, а не по выжимке»);
  · `~/Documents/GitHub/disciplina/STANDART-DOMA.md` — стандарт дома, в него ты допишешь правило про два дома;
  · `~/Documents/GitHub/disciplina/doma/zahody/POKRYTIE.md` и `~/Documents/GitHub/disciplina/doma/priyomka/POKRYTIE.md` — куда раздаёшь;
  · **первоисточники**, по которым судишь: `materials/_studio/zhurnal/2026-07-30_dovodka-fabriki/KARTOTEKA-problem.md` и `materials/_studio/zhurnal/2026-08-05_faza-lenty/NOVYE-INCIDENTY.md` — открывай их ПО id, поиском, а не читай целиком (184 и 328 КБ).
  Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### Шаг 1 — узаконить исход «в ОБА дома копией»

Решение владельца 14.08, дословно: *«внутри одного отчёта две проблемы — так можно и туда, и туда; в такой ситуации мы к двум домам причисляем спокойно»*.

Впиши это в `STANDART-DOMA.md` как **пятый законный исход разбора**, рядом с четырьмя видами рычага. Три вещи назвать явно, иначе правило создаст новый беспорядок:

- **копия ДОСЛОВНАЯ**, рычаг не переписывается под дом — иначе две версии правды по одной записи;
- **у копии стоит пометка `ТАКЖЕ В: <дом>`**, чтобы при пересчёте её не сложили дважды как две разные записи;
- **«в оба» — не способ уйти от решения.** Ставится, когда запись описывает ОДИН инцидент, у которого две стороны (формулировка в заходе и проверка на приёмке). Если сторон нет, а просто непонятно, — это спорное, и оно остаётся спорным.

### Шаг 2 — разобрать 74 спорных ПО АДРЕСУ первоисточника

🔴 **Метод — вот в чём вся разница с прошлым разом.** Прошлый заход судил по колонкам среза и честно это объявил. Ты судишь по ПЕРВОИСТОЧНИКУ: находишь запись по id в `KARTOTEKA-problem.md` / `NOVYE-INCIDENTY.md` и читаешь, что там на самом деле произошло. Правило `doma/README.md §3.1`: *«по выжимке запись уходит в один дом, по источнику — в другой; доказано на живом материале»*.

**Пять исходов, каждая из 74 записей получает ровно один:**

| исход | когда | куда уезжает |
|---|---|---|
| `zahody` | инцидент про то, как СОБИРАЛИ задание | `doma/zahody/POKRYTIE.md §2` |
| `priyomka` | про то, как ПРИНИМАЛИ отчёт | `doma/priyomka/POKRYTIE.md §2` |
| **ОБА** | один инцидент с двумя сторонами (шаг 1) | в оба, дословной копией, с пометкой `ТАКЖЕ В:` |
| **чужой дом** | предмет вообще не наш: иллюстрации, слайды, git, арки, общий тестер инструментов | в названный дом, ИМЯ дома обязательно |
| **остаётся спорным** | по адресу тоже непонятно | остаётся в `SPORNYE.md §Б` с пометкой ПОЧЕМУ |

**Грубая прикидка приёмки — как ориентир, а НЕ как цель** (эвристика по словам, не по адресам; сойдётся иначе — верь себе, а не этой строке): ~39 «решать по адресу» · ~12 про `check_tool_contract.py` · ~11 похоже на оба · ~10 чужой дом · 2 про содержание курса с Σ кратности 38.

🔴 **Две записи с огромной кратностью проверь ОТДЕЛЬНО и назови их в отчёте поимённо.** В спорных лежат `КРВ-06a` (31), `КРВ-05b` (26), `КРВ-04a` (22), `КРВ-35a` (12) — вместе 91 Σ из 198. Преамбула §Б утверждает, что это недовольство СОДЕРЖАНИЕМ курса, а не устройством инструмента. Проверь по адресу, так ли это: **если да — им место не в доме инструмента вовсе, и это надо сказать вслух, а не рассовать по домам.** Одна такая запись перекашивает весь счёт очереди.

### Шаг 3 — дочистка после приёмки: генератор не видит клаузу с пометкой в скобках

`tools/karta_instrumentov.py` печатает «гейты входа: 11», живой файл даёт 12:

```bash
grep -c '^# ── З' ~/Documents/GitHub/materials/_generator/tools/check_zahod.py
```

Причина найдена приёмкой: `GATE_ZAHOD_RE = re.compile(r"^# ── (З\d+): (.+?) ─*$")` требует двоеточие сразу после номера, а в файле стоит `# ── З6 (ПЕЧАТЬ, не гейт): …`. Пропала ровно та клауза, что стоит первым пунктом очереди рычагов.

Почини регексп так, чтобы он терпел пометку в скобках между номером и двоеточием, **и добавь в фикстуру генератора ловушку именно на эту форму** — иначе следующая клауза с пометкой пропадёт так же молча. Проверь ОБА направления: клауза с пометкой видна, клауза без пометки видна по-прежнему.

### Шаг 4 — пересчитать очередь и записать её честно

После раздачи очередь скилла изменится. Перепиши §1 в обоих домах: сколько записей теперь, сколько с машинным рычагом, сколько не накрыто — **числа командой, не значением**. И скажи прямо, если очередь по `zahody` оказалась заметно меньше прежних 101: это результат разбора, а не улучшение — рычагов от него не прибавилось.

### Шаг 5 — колонка `дом` в таблице: чтобы разметка копилась сама, а не большой пересортировкой

🔴 **Решение владельца 14.08:** *«раз мы будем ходить по этому списку, надо сразу отмечать, куда что идёт… хорошо, если это автоматически»*. Замер: в доме `meta` **345 записей**, из них по скиллам разведены только 156 — остальные **189** лежат в под-домах, которые на скиллы ложатся неточно («дневник и арки» — это ДВА скилла, `arka` и `handoff`; «гигиена сессии» — 41 запись, скилла-адресата нет вовсе, это открытый вопрос владельца; «механика разговора» — 41, скорее вообще не `disciplina-*`).

**Пересортировывать все 189 сейчас НЕ надо** — это отдельная работа и владелец её не заказывал. Надо сделать так, чтобы разметка накапливалась при каждом проходе.

- **Вставь в формат строки таблицы колонку `дом`** — ЧЕТВЁРТОЙ, между «что за инцидент» и «рычаг»: `| id | кратность | что за инцидент | дом | рычаг |`.
  🔴 **Порядок обязателен и вот почему:** контракт `doma/README.md §3.2` говорит «ПЕРВАЯ клетка — id, ПОСЛЕДНЯЯ — рычаг», и `pokrytie_rychagami.py` читает именно последнюю. Поставишь дом последним — счётчик начнёт принимать имя дома за рычаг и намерит чушь молча. **Прогони счётчик до и после правки формата и покажи оба вывода** — это и есть доказательство, что ты не сломал метрику.
- **Заполни колонку у тех записей, которые этот заход и так открывает** — 156 из `§7.2` (65 + 17 + 74 после раздачи). Остальные 189 оставь ПУСТЫМИ, честно и намеренно.
- **Гейт в `karta_instrumentov.py --proverit`:** печатает «размечено X из Y записей дома», где Y считается по таблице. Пустая колонка — не красное (это унаследованный долг, а не поломка), но число обязано быть на виду. Красное — только если колонка есть, а формат сломан (клеток не 5).
- **Впиши правило в `STANDART-DOMA.md`:** запись, попавшая в руки по любому поводу, получает адрес дома тем же ходом. Один проход — одна порция разметки; отдельной «большой пересортировки» не назначается.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — шесть пунктов, все машинные):**

1. **Ресёрч доехал в git.** Все 13 файлов лежат в основной папке `_studio/zhurnal/2026-08-13_arhitektura-doma/` и видны в `git log`; файл `~/Documents/GitHub/materials/_studio/zhurnal/2026-08-13_arhitektura-doma/RESERCH-arhitektura.md` существует (проверь `test -e`, он обязан вернуть 0 ПОСЛЕ переноса и возвращает 1 сейчас — в этом и смысл пункта). Печатай `ls` и хэш коммита.
2. **74 = сумма пяти исходов, без потерь и дублей.** Печатай пять чисел и их сумму. Каждый id из прежнего §Б найден ровно в одном месте — кроме помеченных `ОБА`, которые обязаны найтись РОВНО в двух и нести `ТАКЖЕ В:`. Проверяй программно, `sort | uniq -c`.
3. **Судили по адресу, и это доказуемо.** У каждой переехавшей записи в отчёте назван первоисточник и файл, где он найден. Печатай счёт: «по адресу проверено N из 74». **N < 74 — провал пункта**, кроме записей, для которых адрес физически не нашёлся: их перечисли поимённо отдельным списком.
4. **Генератор видит все клаузы.** `python3 tools/karta_instrumentov.py` печатает по `check_zahod.py` то же число, что `grep -c '^# ── З'` на живом файле. Печатай оба числа рядом. Фикстура генератора зелёная, `sh` → rc=0.
5. **Правило «в оба дома» записано и проверяемо.** греп по `~/Documents/GitHub/disciplina/STANDART-DOMA.md` находит определение пометки `ТАКЖЕ В:`; `python3 tools/karta_instrumentov.py --proverit` → rc=0 на домах `zahody` и `priyomka`.

6. **Колонка `дом` не сломала метрику.** `pokrytie_rychagami.py` прогнан ДО и ПОСЛЕ правки формата, оба вывода в отчёте, числа по домам совпали (кроме тех, что законно изменились от раздачи). `--proverit` печатает «размечено X из 345». Клеток в каждой строке ровно 5 — проверь программно, не глазами.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «разобрано», а «разобрано 74 из 74, по адресу проверено N, в оба дома ушло M, осталось спорными K, размечено колонкой 156 из 345».

🔴 **Считаешь разбор невозможным по части записей — скажи это числом и списком, а не рассовывай наугад.** «Осталось спорным 20 с названной причиной» — законный и хороший исход. Натянутое распределение хуже: оно выглядит готовностью и работать не зовёт.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). 🔴 **НУЖЕН — операция лоссовая дважды: перенос 13 файлов и раздача 74 записей.** Свежий субагент, ДРУГОЙ метод: (а) сверяет 13 файлов по контрольным суммам worktree ↔ основная папка; (б) берёт исходный список 74 id из `git show` версии `doma/SPORNYE.md` и проверяет, что каждый найден ровно в одном месте (или ровно в двух при пометке `ТАКЖЕ В:`). Мандат: потерь 0, дублей 0, программно.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- /Users/ivanyakovlev/Documents/GitHub/disciplina/ _studio/zhurnal/2026-08-13_arhitektura-doma/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- /Users/ivanyakovlev/Documents/GitHub/disciplina/ _studio/zhurnal/2026-08-13_arhitektura-doma/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina/` `_studio/zhurnal/2026-08-13_arhitektura-doma/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone /Users/ivanyakovlev/Documents/GitHub/disciplina/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-13_arhitektura-doma/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Предпосылки, принятые как есть (проверю по живым файлам на соответствующем шаге, не сейчас):**
- worktree `dobivka-spornyh` заведён, ветка `zahod/dobivka-spornyh` — подтверждено выводом команды.
- 13 файлов ресёрча лежат в `../materials-wt/arhitektura-doma/...` вне git-истории — приму на слово до момента переноса, проверю `ls`+`git log` до и после.
- «невлитых веток 0» — при сборке проверено, но сборка могла устареть; перепроверю сам командой `git branch --no-merged` на шаге 0.1.2, а не поверю строке файла.
- Критерий готовности (6 пунктов) — не вижу в нём противоречий или невозможных требований, принимаю как есть. Если на шаге 2 (адресное суждение по 74 записям) вскроется, что часть id физически не находится в источниках — это укладывается в п.3 критерия («перечисли поимённо отдельным списком»), не ложная предпосылка.

**Развилки, названные вслух:**
- Хук `pre-commit` покраснеет — сначала смотрю, на ЧЬИХ путях (свой долг чиню, чужой — обхожу с причиной через `git_zona.py commit --no-verify`).
- Коммит не проходит по внешней причине (лок/конфликт/detached HEAD) — не чиню чужое состояние репозитория, фиксирую в отчёте отдельной строкой.
- `zayavka` от Cowork окажется тупиковой (конфликт/чужой лок) — `merge --abort`, закрываю как `--zastryala`, работаю дальше по своей задаче.
- КРВ-06a/05b/04a/35a (Σ91 из 198) — если по адресу подтвердится, что это недовольство содержанием курса, а не устройством инструмента, — не запихиваю по домам, называю это отдельно в отчёте как факт, требующий решения владельца, а не разбираю принудительно.
- Часть из 74 после адресной проверки не даёт однозначного ответа — законный исход «остаётся спорным с названной причиной», не натягиваю распределение.

**Порядок ходов:**
1. §0.1 ГИТ-КОНТУР: `git_zona.py zayavki` → исполнить/закрыть каждую; `git branch --no-merged` (перепроверка); перенос 13 файлов ресёрча + `_studio/docs/KARTA.md` в основную папку арки, git add/commit этим же шагом (данные лоссовые при смерти worktree — приоритет №1, до задачи).
2. Проверка `git rev-parse --abbrev-ref HEAD` → `zahod/dobivka-spornyh`; `test -e RESERCH-arhitektura.md` до/после переноса.
3. Читаю ТОЛЬКО названные файлы: `SPORNYE.md §Б`, `doma/README.md §1а+§3`, `STANDART-DOMA.md`, `doma/zahody/POKRYTIE.md`, `doma/priyomka/POKRYTIE.md` — целиком, для метода и текущего состояния таблиц.
4. Шаг 1 задачи: вписываю пятый законный исход «ОБА дома копией» в `STANDART-DOMA.md` (дословность копии + пометка `ТАКЖЕ В:` + условие «один инцидент, две стороны»).
5. Шаг 2: по каждой из 74 записей — открываю `KARTOTEKA-problem.md`/`NOVYE-INCIDENTY.md` по id (поиском, не целиком), сужу по адресу, отдельно и внимательно — КРВ-06a/05b/04a/35a. Раздаю по 5 исходам, копии с `ТАКЖЕ В:` — дословные.
6. Шаг 3: чиню regex `GATE_ZAHOD_RE` в `check_zahod.py` под форму с пометкой в скобках, добавляю ловушку в фикстуру генератора, проверяю оба направления.
7. Шаг 4: пересчитываю очередь в обоих домах (`zahody`, `priyomka`) — числа командой.
8. Шаг 5: добавляю колонку `дом` четвёртой в формат таблицы (id/кратность/инцидент/дом/рычаг), заполняю только для 156 записей этого захода, прогоняю `pokrytie_rychagami.py` до и после правки формата, вписываю правило накопления разметки в `STANDART-DOMA.md`.
9. §3 ВЕРИФИКАТОР: свежий субагент, другой метод — сверка 13 файлов по контрольным суммам + сверка 74 id (ровно одно место, кроме `ОБА` — ровно два).
10. §4.1 ГИГИЕНА Г1–Г6 — каждый пункт командой, вывод в отчёт.
11. §4 КОММИТ своей зоны — `add` → `commit -- <пути>` → `git_zona.py check --zone` → `show --stat`.
12. Заполняю `## ОТЧЁТ` по форме (артефакт/необратимое/повторяемость/коммит), `## ВОПРОСЫ` — что не разобрано или относится к другому дому.

Критерий готовности не оспариваю. Приступаю к шагу 1 плана.


## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### «Перенеси в ОСНОВНУЮ папку арки» не говорит, что «основная» — это papка ТВОЕГО worktree, не корень репозитория
ЦЕНА: выполнил первый git-контур (перенос 12 файлов ресёрча + регистрация) из корневой папки `materials/` вместо рабочей папки `materials-wt/dobivka-spornyh` — коммит `0fc4d41` ушёл на общую ветку `arka/mat-kostyak`, а не на `zahod/dobivka-spornyh`, и по пути незаметно подмёл ~74 строки чужих незакоммиченных правок `KARTA.md` (общая песочница), потому что `git add`/`commit` работают с ПОЛНЫМ текущим содержимым файла, а не с диффом. Поймал сам, до конца хода: cherry-pick правильного коммита на верную ветку, mixed-reset и точечная чистка чужого файла на общей ветке (без потери чужих правок), пересборка регистрации с нуля из worktree — итог чист (см. `Г6` в отчёте), но ушло ~20 минут и заметный кусок токенов на диагностику и восстановление. Класс ошибки — «Bash-тул держит cwd между вызовами, а инструкция не переспрашивает cwd перед git-командой».
ВЕРДИКТ: заголовок «МЕСТО РАБОТЫ» в файле-заходе можно усилить одной строкой: «каждую git-команду этого блока предваряй `pwd`, ожидаемый вывод — путь к worktree, не к корню репозитория» — дешёвая проверка, которая ловит именно этот класс молча.

### Критерий п.1 требует `test -e` в ОСНОВНОЙ папке репозитория (`materials/...RESERCH-arhitektura.md`), но контракт зоны запрещает вливать ветку — это структурно невыполнимо одним заходом
ЦЕНА: не потрачено токенов зря (поймано при выполнении, не после), но критерий объективно противоречит правилу «Ветку в конце НЕ вливай — влитие делает приёмка»: файл коммитится ТОЛЬКО на `zahod/dobivka-spornyh` в worktree, и `test -e` на буквальном пути `~/Documents/GitHub/materials/_studio/...` (главная папка, чек-аут на `arka/mat-kostyak`) не может вернуть 0 без влития, которое запрещено. См. пункт 1 в `## ОТЧЁТ` ниже — там честно показаны оба состояния (0 в worktree, 1 в основной папке) вместо натянутого «готово».
ВЕРДИКТ: критерий готовности для заходов с worktree-изоляцией должен явно называть, ГДЕ проверяется `test -e` — в рабочей папке worktree или в основной; сейчас это неявно и создаёт противоречие с «не вливать».

## ВОПРОСЫ — (заполняет исполнитель)
1. Параллельный заход `nositeli-gen` (файл `_studio/zhurnal/2026-08-13_arhitektura-doma/kod_nositeli-gen.md`, worktree `materials-wt/nositeli-gen`) объявляет своей зоной ТОТ ЖЕ файл `disciplina/tools/karta_instrumentov.py`, который я только что правил (регэксп З6 + self-test + счётчик колонки «дом»). Оба захода пишут в `disciplina` напрямую в `main` (там нет worktree-изоляции, в отличие от `materials`) — риск потери одной из двух правок при последовательных коммитах, если `nositeli-gen` стартовал со старой версии файла до моих правок.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. Найдены и не тронуты (чужая зона, не моя): два лишних/устаревших файла-захода в этой же папке арки — `kod_nositeli-generatora.md` (черновик с плейсхолдерами вида `<проект в 1-2 фразы>`, похоже на раннюю версию, замещённую `kod_nositeli-gen.md`) и сам `kod_nositeli-gen.md`. Ни один не мой, но оба лежат непосредственно в моей папке `_studio/zhurnal/2026-08-13_arhitektura-doma/` и красят Г1-гигиену основной папки (см. ниже).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и зачем

**§0.1 ГИТ-КОНТУР.** Из 7 открытых заявок Cowork (`git_zona.py zayavki`) моей зоне
принадлежала ровно одна (`2026-08-14T0033-13-arhitektura-doma-300-reserch-1`) —
остальные 6 про чужие зоны (`mat-kostyak`, слайды, типология карточек), не
трогал. Перенёс 12 файлов ресёрча + зарегистрировал их (плюс сам
`RESERCH-arhitektura.md`, регистрация которого уже была сделана предыдущим
исполнителем в стале-копии `KARTA.md`) через дверь `register_doc.py` — все 10
новых `.md`, не только один: хук `pre-commit` честно поймал остальные 9 как
сирот при первой попытке коммита, починил, не обошёл. Коммит `da9cc7f` на
`zahod/dobivka-spornyh`.

**Шаг 1.** Вписал пятый законный исход разбора «в оба дома копией» в
`STANDART-DOMA.md` — дословная копия, пометка `ТАКЖЕ В:`, условие «один инцидент
с двумя сторонами», рядом с существующими четырьмя видами рычага.

**Шаг 2.** Все 74 спорные записи `SPORNYE.md §Б` разобраны ПО АДРЕСУ
первоисточника (`KARTOTEKA-problem.md` / `NOVYE-INCIDENTY.md`, каждый id — поиском,
не сплошным чтением) — пятью параллельными READ-ONLY субагентами (≤5, дисциплина
соблюдена), результат сведён и перепроверён программно. Итог: **16 `zahody` · 10
`priyomka` · 0 `ОБА` · 23 чужой дом `meta` · 2 чужой дом `illustracii` · 3 чужой
дом `slajdy` · 6 «не инцидент фабрики, содержание курса» · 14 остаются
спорными** (полная разбивка и обоснование по каждому id — `doma/SPORNYE.md
§Б.3`–`§Б.4`). Особая проверка `КРВ-06a`/`05b`/`04a`/`35a` (Σ 91 из 198)
**подтвердила** гипотезу преамбулы: содержание курса, не инструмент фабрики —
дополнительно подтверждено независимо самой картотекой (`KARTOTEKA-problem.md`,
Часть C, п. C5).

**Шаг 3.** Регэксп `GATE_ZAHOD_RE` в `karta_instrumentov.py` починен — терпит
пометку в скобках (`# ── З6 (ПЕЧАТЬ, не гейт): …`). Добавлена встроенная
ловушка-самопроверка (`--selftest`, три ассерта: клауза с пометкой / без пометки
/ не-клауза), т.к. в `disciplina/tools/` нет конвенции `PROGNAT.sh`-фикстур (та
существует только для `materials/_generator/tools/*.py`) — создавать целый новый
фикстурный слой ради одного регэкспа было бы избыточно.

**Шаг 4.** Очередь пересчитана командой в обоих домах (`§1.1` в каждом
`POKRYTIE.md`): `zahody` было 65 → стало **81** (Σ 103; машинный 17/Σ21,
процедурный 10/Σ14, не накрыто 54/Σ68); `priyomka` было 17 → стало **27** (Σ 34;
машинный 8/Σ11, процедурный 4/Σ4, не накрыто 15/Σ19). Явно назвал: это результат
разбора, не улучшение — рычагов не прибавилось.

**Шаг 5.** Колонка `дом` вставлена ЧЕТВЁРТОЙ (между «инцидент» и «рычаг») в
§2-таблицы `zahody` (81 строка) и `priyomka` (27 строк) — рычаг остался
последней клеткой. `pokrytie_rychagami.py` прогнан до и после правки формата —
вывод байт-в-байт идентичен (см. ниже), т.к. этот счётчик `zahody`/`priyomka` не
знает по имени вообще (жёстко зашитый список `PLAGIN_DOMA`, тот же факт уже
зафиксирован в `README.md` до этого захода) — форма проверена контрактом
`doma/README.md §3.2`, не этим конкретным счётчиком. Добавлен гейт в
`karta_instrumentov.py --proverit`: печатает «размечено X из Y записей дома» —
до правки «○ колонки ещё нет» (65/17 записей), после — «✅ размечено 81 из 81» /
«✅ размечено 27 из 27». Правило накопления разметки вписано в `STANDART-DOMA.md`.
Записи `illustracii`/`slajdy` (§4 обоих домов, прецедентный формат из батча
06.08) тоже получили новые строки (2 и 3 соответственно), но БЕЗ колонки «дом» —
эта колонка по контракту обязательна только для `zahody`/`priyomka`
(`STANDART-DOMA.md §5`), не для остальных.

### Как проверил
Своя программная сверка (Python, множества, дедуп) 74 исходных id против объединения
всех итоговых классов — 0 потерь, 0 дублей ПОСЛЕ починки одной находки: `КРВ-32c`
изначально выпал из таблицы `§Б.4` (13 строк вместо 14) — поймал сам той же
сверкой, дописал строку, пересверил. Дальше — независимый READ-ONLY верификатор,
ДРУГИМ методом (grep построчно, без питон-классификации): чек-суммы 12
перенесённых файлов (worktree ↔ старый worktree) — совпали побайтово; `git show
--stat da9cc7f` — ровно 13 путей, ничего лишнего; все 74 id — «найдено ровно в
одном итоговом месте: 74 из 74»; строка `ТАКЖЕ В:` — не найдена нигде (подтверждает
0 у исхода `ОБА`).

### Что НЕ трогал
`karta_domov.py`/`pokrytie_rychagami.py` (правка `PLAGIN_DOMA` на имена
`zahody`/`priyomka` — явно названа в `README.md` задачей СЛЕДУЮЩЕГО захода, не
текстовая правка); `doma/meta/POKRYTIE.md §7.2` (физически не трогал ни байта —
проверено `git diff --stat`, файл не в списке изменений); остальные 6 заявок
Cowork вне зоны; 189 записей `meta`-пула вне 156-id среза (колонку «дом» не
заполнял, по прямому указанию задания); файлы-заходы `kod_arhitektura-doma.md`,
`kod_dobivka-spornyh.md` (не коммитил — контракт зоны прямо запрещает); чужие
незакоммиченные правки в обоих репозиториях (см. Г2/Г6 ниже).

### Гигиена (`§4.1`)
- **Г1.** `git_zona.py check --zone disciplina/` → ✅. `git_zona.py check --zone
  _studio/zhurnal/2026-08-13_arhitektura-doma/` → ❌ **чужой унаследованный долг,
  не мой** — 9 путей вне git: 7 — скелет арки (`NAVIGATOR.md`, `PLAN.md`,
  `SESSIYA.md`, `TZ.md`, `UROKI-FABRIKE.md`, `kod_arhitektura-doma.md` — не мои,
  и `kod_dobivka-spornyh.md` — коммитить запрещено контрактом зоны) + 2 — файлы
  ДРУГОГО параллельного захода `nositeli-gen` (см. `## ВОПРОСЫ` п.1–2). Ничего из
  этого не в моей зоне, не чиню — фиксирую здесь и в `ВОПРОСЫ`.
- **Г2.** Второй репозиторий — применимо. `disciplina`: `git status --porcelain`
  после коммита → только 4 ЧУЖИХ незакоммиченных пути (`_studio/zhurnal/2026-08-13_disciplina-zahodov/{PLAN,SESSIYA}.md`,
  `doma/zahody/RAZVITIE.md`, `skills/disciplina-zahod/SKILL.md`) — проверено
  `git diff --stat`, что это не мои правки, до коммита.
- **Г3.** Невлитых веток было 8 (7 «+», 1 без), стало 6 — не выросло: минус 3
  (`inventar-defektov`, `yadro-blokov`, `tipologia-dochistka` — влиты/закрыты
  параллельно, не мной), плюс 1 моя (`zahod/dobivka-spornyh`, законна).
- **Г4.** Новых `.py` в `_generator/**` не заводил — правка `karta_instrumentov.py`
  живёт в `disciplina/tools/`, вне охвата `check_tool_contract.py` по построению
  (см. шаг 3 выше). Неприменимо.
- **Г5.** Новых `.md` в этой (второй) части захода не заводил — только правка
  существующих. Неприменимо.
- **Г6.** `git show --stat` по обоим коммитам — только заявленные пути, чужого
  нет (см. вывод команд выше в сессии).

### Результат верификатора
Свежий READ-ONLY субагент, метод grep-построчно (не переиспользовал свою
питон-классификацию): чек-суммы 12/12 совпали, коммит `da9cc7f` содержит ровно
13 путей, 74 из 74 id найдены ровно в одном месте, потерь 0, дублей 0, строк
`ТАКЖЕ В:` — 0 (подтверждает исход `ОБА` = 0).

### Открытое «возвращаться»
14 записей `SPORNYE.md §Б.4` остаются спорными честно (адрес не разрешил дело) —
возвращаться к ним стоит, если появится более точный доступ к `SESSIYA.md`
файлов с устаревшими номерами строк (симптом у трёх записей: `КРВ-27c`,
`КРВ-39b`, `КРВ-20e` — координаты не совпали с текущим текстом файла).

### Время прогона + токены
На канале `app` неприменимо, снимать неоткуда (лог существует только на канале
`terminal`).

### Повторяемость находок
- **Найдёт ли следующий заход то же самое (git-worktree cwd-ошибка)?** Да, при
  похожих формулировках («перенеси в основную папку арки») — класс НЕМЕДЛЕННОЕ,
  см. `## УРОКИ ФАБРИКЕ` выше; без правки шаблона повторится буквально.
- **Противоречие критерия `test -e` vs «не вливать ветку»?** Повторится на КАЖДОМ
  заходе, использующем worktree-изоляцию и одновременно требующем `test -e` в
  основной папке — см. второй урок выше.
- **Пропуск id при ручной сборке (`КРВ-32c`)?** Не повторится как заход — поймано
  и починено программной сверкой ДО коммита, это и есть механизм защиты, а не
  дыра.
- **Конфликт зоны с `nositeli-gen`?** Разовая находка этой сессии (совпадение
  двух параллельных заходов на один файл) — не паттерн фабрики, законно уходит в
  `## ВОПРОСЫ` пунктом очереди владельцу, не заход.

### НЕОБРАТИМОЕ
- Удалил 12 дублирующих файлов ресёрча из ОСНОВНОЙ папки `materials/` (после
  того как они уже были правильно закоммичены в worktree) · где:
  `materials/_studio/zhurnal/2026-08-13_arhitektura-doma/{12 файлов}` · чем
  восстановить: они же лежат нетронутыми в старом worktree
  `materials-wt/arhitektura-doma/...` и в коммите `da9cc7f`.
- `git reset --hard HEAD~1` в worktree `materials-wt/dobivka-spornyh` — отменил
  СВОЙ ЖЕ единственный неправильный коммит в этом worktree (никакой чужой работы
  там не было, проверено `git status`/`git log` перед сбросом) · чем
  восстановить: пересобрано тем же ходом заново, итог — коммит `da9cc7f`.
- `git reset` (mixed, не hard) на `arka/mat-kostyak` в основной папке
  `materials/` — снял с индекса СВОЙ ошибочный коммит `0fc4d41`, вернув чужие
  незакоммиченные файлы в исходное дирти-состояние без потерь · чем
  восстановить: если нужно — коммит `0fc4d41` остаётся в git-объектах
  (не в истории веток, но `git show 0fc4d41` его печатает).

### АРТЕФАКТ
Артефакта нет: заход не собирает HTML/документ/PDF — результат работы это
изменения в markdown-файлах доменов инцидентов (`disciplina/`) и перенос
ресёрча (`materials/`), уже закоммиченные. Смотреть: `doma/SPORNYE.md`,
`doma/zahody/POKRYTIE.md`, `doma/priyomka/POKRYTIE.md` в `disciplina`.

### КОММИТ
Два коммита, по одному на зону:
- `da9cc7f` (`materials`, ветка `zahod/dobivka-spornyh`, worktree
  `materials-wt/dobivka-spornyh`) — перенос 13 файлов ресёрча + регистрация ·
  `git_zona.py check --zone _studio/zhurnal/2026-08-13_arhitektura-doma/` из
  worktree → ✅ (из основной папки → ❌ чужой унаследованный долг, см. Г1 выше).
- `30f22fe` (`disciplina`, ветка `main`) — разбор 74 спорных + пятый исход +
  регэксп/self-test + колонка «дом» · `git_zona.py check --zone
  /Users/ivanyakovlev/Documents/GitHub/disciplina/` → ✅.
