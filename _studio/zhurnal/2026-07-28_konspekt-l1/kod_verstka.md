# Канал исполнителя — verstka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — механика расписана, но решений «какая раскладка этому слайду» будет тридцать, и каждое надо принять по содержанию.

## 🔴 ТЫ ТРЕТИЙ В НОЧНОЙ ЦЕПОЧКЕ. ПРОЧТИ ЭТО ПЕРВЫМ

Ночью подряд, без человека между звеньями, идут три захода: **разрез конспекта на слайды** (`kod_razrez.md`) → **лента, тексты слайдов** (`kod_predverstka.md`) → **ты, вёрстка дека**. Сторож запустил тебя по факту коммита предыдущего. Владелец спит и увидит всё утром одним куском.

🔴 **У ТЕБЯ ЕСТЬ ГЛАЗА, И БЕЗ НИХ ЭТУ РАБОТУ НЕ СДЕЛАТЬ.** Ты работаешь на машине владельца, а не в песочнице: `_generator/render.py` снимает каждый слайд в PNG 1440×810 через Chrome, и **эти PNG ты можешь ОТКРЫТЬ И ПОСМОТРЕТЬ** обычным чтением файла. Вёрстка вслепую — брак; определить, где провести границу текстовой колонки и полосы полей, иначе нельзя. Петля обязательна и описана в шаге 6.

**Первым ходом проверь среду:**
```
python3 -c "import playwright, PIL; print('render OK')"
```
Ответило `render OK` — работаешь с петлёй. Упало — **скажи это первой строкой отчёта и работай вслепую по образцам `buffon/`**, но тогда честно назови результат черновым и непроверенным. Сам ничего не устанавливай.

🔴 **ЧЕСТНАЯ ГРАНИЦА РЕЗУЛЬТАТА — назови её в отчёте ровно так, не мягче.** Даже с петлёй ты доводишь дек до «**собран, линтер PASS, каждый слайд просмотрен и переполнений нет**». Ты **не решаешь**, хорошо ли он выглядит: последний слой гейта `G11` — вкус владельца (скучная раскладка, неудачный ритм, «не то настроение»), и он не автоматизируется. Отчёт кончается строкой о том, что прогнано и что осталось владельцу. Тихо пометить дек готовым — брак.

КОНТЕКСТ. Курс «Зачем нужны категории», лекция 1, ~2 часа, зал — сильные первокурсники и взрослые. Фаза I закончена: есть **лента** `teorkat-vvedenie/raskadrovka/teksty/*.md` — тексты в том виде, в котором они лягут на слайд, плюс иллюстрации инлайновыми `<svg>` внутри `<figure>`. Скелет Фазы II уже скопирован аналитиком в `teorkat-vvedenie/src/`, манифест `src/brief.md` заполнен — **кроме `slide_order`, его заполняешь ты**.

🔴 **ЦЕЛЬ: собрать дек — арки 7 (вёрстка), 8 (сцены), 10 (сборка).** Из ленты порождаются `content/<id>.md`, верстаются `slides/<id>.html` с пер-слайдовым гридом, иллюстрации переезжают в `illustrations/`, расставляются сцены, всё собирается `build_deck.py` в `dist/index.html`.

Приёмка — по ОТЧЁТУ. Остановишься до конца — доведи **до собранного дека меньшее число слайдов** и честно назови остальные неразвёрстанными. Полдека, которое открывается в браузере, полезнее целого, которое не собирается.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/src/ _studio/zhurnal/2026-07-28_konspekt-l1/kod_verstka.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/src/ _studio/zhurnal/2026-07-28_konspekt-l1/kod_verstka.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО:
  - **`_studio/konvejer/FORMAT-ISTOCHNIKA.md`** — устройство `src/`, схема `brief.md`, плейсхолдеры `shablon.html`, как из ленты порождается `content/`. Первый и главный якорь.
  - **`_studio/konvejer/07-verstka/DOK.md`** и **`07-verstka/PRIMERY.md`** — механика вёрстки, таблица архетипов раскладки, живые проценты по колонкам.
  - **`_studio/konvejer/08-sceny/DOK.md`** — синтаксис шорткатов сцен.
  - **`_studio/konvejer/10-sborka-qa/DOK.md`** — сборка и пять слоёв гейта.
  - **`_studio/konvejer/GEJTY.md`**, гейты **G6, G8, G9, G10, G11, G12** — то, по чему тебя примут.
  - **`_generator/skeleton/SPEC.md`** — контракт скелета, граница `base.css` против пер-слайдового грида.
  - **вся лента `teorkat-vvedenie/raskadrovka/teksty/*.md`** — твой единственный источник текста.
  - **`buffon/src/`** — эталон-образец: `shablon.html`, два-три `slides/*.html`, соответствующие `content/*.md`. **Копируй его паттерны, не изобретай свои.** Второй, попроще — `dandelin/src/`.
  - **`_studio/zhurnal/2026-07-28_konspekt-l1/kod_predverstka.md`, секция `## ОТЧЁТ`** — что предыдущий заход сделал и что оставил открытым.
  **Больше ничего.** Конспект `KONSPEKT-L1/`, картотеку, `SPEKA.md` не открывай: твой вход — лента, не конспект.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

Семь шагов. Шаги 1–3 механические и делаются точно, шаг 4 — единственный, где решения, шаги 5–7 — сцены, сборка, честный отчёт.

---

### ШАГ 1. ПОРОДИТЬ `content/<id>.md` ИЗ ЛЕНТЫ

Каждый `## `-раздел ленты становится одним слайдом. Заведи `id` по схеме `s01`…`sNN` в порядке ленты (нумерация сквозная через все пять блоков) и на каждый — файл `teorkat-vvedenie/src/content/<id>.md` с текстом раздела.

**Что при переносе снимается:**
- **`> поле:mn` целиком, включая пометку раскладки** — это режиссура для читателя ленты, на слайд не идёт. Раскладку прочитай и запомни, она нужна на шаге 4.
- **`<figure>`, `<figcaption>` и весь html** — гейт `G6` прямо требует, чтобы `content/<id>.md` не содержал html (`regex <[a-zA-Z]` не должен матчить). SVG уезжает в `illustrations/` на шаге 2, подпись — выбрасывается: **на слайде рисунок объясняет лектор**.
- Заголовок раздела — по правилам `FORMAT-ISTOCHNIKA.md`; сверься с тем, как это сделано в `buffon/src/content/`.

**Что остаётся дословно:** сам текст. **Ты его не переписываешь и не сокращаешь** — он уже написан под слайд и просужен по бюджету. Нашёл место, где текст явно не влезет, — это шаг 4, там и решается, и лечится раскладкой или `SPLIT`, а не правкой текста.

**Заполни `slide_order` в `src/brief.md`** — все `id` по порядку. Служебные слайды (обложка, визитка, финал) туда **не входят**: их строит генератор из полей манифеста, и рукописный `slides/sl-title.html` — это FAIL по `G12`.

---

### ШАГ 2. ИЛЛЮСТРАЦИИ ПЕРЕЕЗЖАЮТ В `illustrations/`

В ленте они лежат инлайновыми `<svg>` внутри `<figure>`. Каждую вынь в отдельный файл `teorkat-vvedenie/src/illustrations/<name>.svg`, имя — осмысленный слаг. В вёрстке она подключается пустым боксом `<div class="ill-box" data-ill="<name>"></div>`.

🔴 **Гейт `G10`:** каждая переменная `var(--x)` внутри SVG обязана быть определена в `tokens.css`; `id` внутри разных SVG не должны конфликтовать (при склейке в один документ они окажутся в общем пространстве имён — переименуй с префиксом слайда). Оба условия проверяются грепом, без браузера. Проверь и напечатай результат.

⚠ Иллюстраций может не хватить: лента рисовала только обязательный минимум. **Новых сложных не изобретай** — владелец запретил вкладываться в иллюстрации до того, как дек сойдётся. Слайд без картинки — законный слайд; посчитай такие и назови число в отчёте.

---

### ШАГ 3. СЛУЖЕБНЫЕ СЛАЙДЫ — НЕ ВЕРСТАТЬ

Обложка, визитка, финал порождаются генератором из полей `brief.md` (`title`, `cover_sub`, `cover_date`, `cover_place`, `vizitka`, `final_ill`). Поля уже заполнены аналитиком; `cover_place` намеренно пусто — зал неизвестен, и «нет поля — нет элемента». **Руками эти слайды не создавай**, это прямой FAIL по `G12`.

---

### ШАГ 4. ⭐ ВЁРСТКА — единственный шаг, где ты принимаешь решения

На каждый `id`: `slides/<id>.html` с гридом и слотом `{{MD:<id>}}`, плюс пер-слайдовый CSS `#<id> .grid{…}` **внутри `<style>` в `shablon.html`** (отдельного `grids.css` в каноне нет).

Образец, который надо копировать (`buffon/src/slides/sl-plan.html`):
```html
<section class="slide" id="sl-plan" data-scenes="2">
  <div class="grid">
    <div class="zone copy t-body">{{MD:sl-plan}}</div>
    <div class="rail">
      <div class="panel p1 ill-box" data-ill="rail-needle3"></div>
    </div>
  </div>
</section>
```
```css
#sl-plan .grid { position:absolute; inset:0; display:grid;
  grid-template-columns: 20px 1080px 23px 317px; grid-template-rows: 36px 1fr; }
#sl-plan .copy { grid-area: 2 / 2; }
#sl-plan .rail { grid-area: 1 / 4 / 3 / 5; background: var(--board); position: relative; }
```

**Раскладку каждому слайду лента уже назначила** — ты её исполняешь, а не выбираешь заново (`07-verstka/DOK.md`: «арка 7 реализует принятую раскладку»). Два архетипа: текстовая колонка ~75% ширины плюс вертикальная рейка иллюстраций ~22%; или мало текста и крупная горизонтальная иллюстрация снизу.

🔴 **Три жёстких правила, нарушение каждого — брак:**
1. **`--t-body` НИКОГДА не уменьшается** ниже 35px (в токенах 38px). **Не влезло — делишь слайд (`SPLIT`), а не ужимаешь шрифт.** Каждый `SPLIT` называешь в отчёте поимённо с причиной; `slide_order` при этом растёт.
2. **Разнообразие раскладок (Р24):** не гнать все тридцать слайдов одной схемой. Напечатай в отчёте распределение по архетипам — если один архетип покрывает больше двух третей дека, это сигнал, а не достижение.
3. **Опорные точки (Р23):** цветное слово-надзаголовок у левого поля — «Определение» зелёным, «Утверждение»/«Теорема» стальным, «Задача N» кирпичным; формулировка ниже на всю ширину, без точки в конце; **картинка после формулировки, не до**. Формулу не разрывать переносом строки.

---

### ШАГ 5. СЦЕНЫ

Шорткаты пишутся в `content/<id>.md`: `{@N}` / `{@N-M}` / `{@-M}` на абзаце, инлайн `{@N|текст}`, шторка `{fill@N|бланк|ответ}`, блюр `{blur@N|значение}`. `data-scene-from/until` генератор проставит сам; **`data-scenes="N"` на `<section>` пока приходится выписывать руками** — это известный долг генератора, и `G14` сверяет число с максимальным маркером в тексте. Выпиши и проверь.

**Правила «сколько сцен» нет** — это драматургия, а не норма. Где ставить: доказательства по шагам (лента писала их короткими фразами именно под это), ответы под шторкой, длинные списки, которые не надо вываливать разом. Владелец: *«вот формулировка, потом кликнул — первая часть, ещё кликнул — вторая»*.

🔴 **`G9`, дед-клики:** каждый клик обязан что-то менять. Полноценно это ловится только `--scene-diff` в браузере, которого у тебя нет, **но статическую половину прогони**: связка (то, что делает текст связным) не должна прятаться под `{blur@` — грепом проверяется.

---

### ШАГ 6. ⭐ СБОРКА И ЗРИТЕЛЬНАЯ ПЕТЛЯ — здесь делается качество

```
python3 _generator/build_deck.py teorkat-vvedenie/src --lint     # только линтер
python3 _generator/build_deck.py teorkat-vvedenie/src            # → src/dist/index.html
python3 _generator/render.py teorkat-vvedenie/src/dist/index.html _render   # → PNG всех слайдов
python3 _generator/audit.py teorkat-vvedenie/src/dist/index.html            # overflow, палитра, кегль
python3 _generator/audit.py teorkat-vvedenie/src/dist/index.html --scene-diff  # дед-клики
python3 _generator/tools/sostoyanie.py teorkat-vvedenie          # трекер гейтов, read-only
```
Код возврата — первым, до содержательного вывода.

🔴 **ПЕТЛЯ, и она главная в этом заходе.** Никакая статическая проверка не скажет, где провести границу текстовой колонки и полосы полей, — это видно только глазом. Поэтому:

1. Собери дек и сними PNG.
2. **ОТКРОЙ И ПОСМОТРИ каждый PNG.** Не выборочно — все. Смотришь на: текст, вылезший за зону или упёршийся в край · пустое поле, которое просится под иллюстрацию · картинку, раздавленную боксом или болтающуюся в нём · строку, разорванную неудачно · формулу, уехавшую на две строки · слайд, который просто выглядит пустым или, наоборот, забитым.
3. **Правь пер-слайдовый грид** — колонки, строки, отступы, `--t-body` этого слайда **вверх** (вниз ниже 35px нельзя) — и пересобирай.
4. Повторяй, пока слайд не встанет. **Это нормально — по три-четыре круга на слайд.** Времени на это заложено достаточно.

⚠ **Правь геометрию, а не текст.** Текст ленты просужен и переписыванию не подлежит. Не влезает после честной работы с гридом — это `SPLIT`, и он называется в отчёте.

**Что печатает `audit.py`** и что обязано быть нулём: overflow `.zone` · тени, скругления, градиенты · цвета вне палитры токенов · `.t-body` ниже 35px · `<figcaption>` в деке. `--scene-diff` ловит дед-клики: соседние сцены обязаны отличаться только **добавленными** пикселями.

⚠ Если `render OK` не прошло на первом ходу — пункты 2–4 невыполнимы. Тогда верстай по образцам `buffon/src/`, гони линтер, и **первой строкой отчёта скажи, что дек не просмотрен ни разу**.

---

### ШАГ 7. ОТЧЁТ ЧЕСТНЫМИ СЛОВАМИ

Кроме обычного — **обязательная последняя строка** про границу (см. шапку): что прогнано, что нет, и что владельцу нужно сделать локально. Плюс: сколько слайдов, сколько `SPLIT` и почему, распределение по архетипам раскладки, сколько слайдов без иллюстрации, сколько сцен всего.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — печатается, а не утверждается):**
1. `build_deck.py --lint` → RC=0, предупреждений 0.
2. `build_deck.py` → RC=0, файл `teorkat-vvedenie/src/dist/index.html` существует и непуст.
3. `slide_order` непуст и **точно** покрывает `content/` и `slides/` — без сирот и дублей (проверяет линтер).
4. **Ни одного html-тега в `content/*.md`** — печатается счёт совпадений `<[a-zA-Z]`, обязан быть 0 (`G6`).
5. Каждый `data-ill` имеет файл в `illustrations/`; каждая `var(--…)` внутри SVG определена в `tokens.css`; конфликтов `id` между SVG нет — все три печатаются числом (`G10`).
6. **Служебных слайдов, написанных руками, — 0** (`G12`): в `slides/` нет `sl-title`, `sl-vizitka`, `sl-final` и им подобных.
7. `--t-body` нигде не опущен ниже 35px — печатается минимум по деку (`G8`).
7а. 🔴 **`audit.py` → overflow `.zone` = 0**, цветов вне палитры 0, `<figcaption>` 0. И отдельно: **сколько слайдов ты просмотрел глазами из скольких** — «просмотрено 31 из 31». Число меньше полного — назови, какие не смотрел и почему.
7б. **`audit.py --scene-diff` → дед-кликов 0**, либо каждый назван.
8. `data-scenes` на каждом слайде совпадает с максимальным маркером сцены в его тексте (`G14`).
9. Лента `raskadrovka/` **не изменена ни на байт** — `git diff --stat` по ней пуст.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Операция **не лоссовая**: лента не трогается, `src/` пишется с нуля. Верификатор сохранности не нужен — критерий §2 п.9 закрывает это грепом.

**Но одна проверка нужна, и её нельзя делать самому.** Свежий субагент, которому НЕ показывают ленту: он открывает только `content/*.md` и `slides/*.html` и отвечает на два вопроса. (1) **Полнота переноса:** для каждого `## `-раздела ленты есть ровно один `content/<id>.md`, и текст в нём совпадает с разделом дословно, минус `> поле:` и html — расхождения выписать поимённо. (2) **Разнообразие и осмысленность раскладок:** пройти по гридам и назвать слайды, где раскладка противоречит содержанию — вертикальная рейка при горизонтальной по природе иллюстрации, крупная картинка при трёх абзацах текста, одинаковая схема пять слайдов подряд.

⚠ Дальше начинаются браузерные слои, и их у тебя нет. **Не имитируй их рассуждением** — просто назови непрогнанными.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/src/ _studio/zhurnal/2026-07-28_konspekt-l1/kod_verstka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/src/ _studio/zhurnal/2026-07-28_konspekt-l1/kod_verstka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Канон обещает порождение служебных слайдов, а живой `build_deck.py` его не реализует
`FORMAT-ISTOCHNIKA.md` («решено 28.07.2026»), `07-verstka/DOK.md §Три служебных слайда` и гейт `G12` описывают механику `{{SLIDES}}` + `{{SLUZHEBNYE_CSS}}` + `_generator/skeleton/sluzhebnye/` как действующую. `grep -rln 'SLUZHEBNYE|sluzhebnye|{{SLIDES}}' _generator/` даёт **единственное совпадение — в `DESIGN.md`**, то есть в документации. Папки `skeleton/sluzhebnye/` не существует. Замер внутри самого G12 («`{{SLIDES}}` — 1 после миграции, `paskal`») тоже не подтверждается: в `paskal/src/shablon.html` два совпадения `{{SLIDE`, и **оба в комментариях-подсказках скелета**, рабочего плейсхолдера нет.
**ЦЕНА:** заход отвёл этому целый шаг (ШАГ 3) с формулировкой «руками не создавай, это прямой FAIL по G12», и исполнитель, читающий канон буквально, получает дек **без обложки, визитки и финала** — и узнаёт об этом только собрав дек. У меня так и вышло: 55 слайдов собраны, а первый кадр лекции — «Тождества на доске». Гейт при этом зелёный (`0` рукописных служебных), то есть G12 подтверждает отсутствие нарушения там, где отсутствует сам механизм.

### `render.py` не умеет снять финальную сцену: `?scene=99` против классового каскада
`_generator/render.py:29` снимает кадр как `?only=N&scene=99` с комментарием «scene=99 → показать слайд в финальной сцене». Движок (`engine.js:54 applyScene`) раскрывает сцены **классом** `.scene-K`, а канон-каскад в `base.css` выписан до `.scene-5`. Класса `.scene-99` не существует ни в одном деке ⇒ на снятом PNG слайд со сценами показан в **ПЕРВОЙ** сцене, а не в финальной. Чинится в одну строку в любом из трёх мест: `render.py` (послать реальное `data-scenes`), `engine.js` (клампить `k` к `scenesOf`), база (добавить `.scene-99` в каскад).
**ЦЕНА:** зрительная петля — единственный слой гейта, который ловит содержательные дефекты (у меня их пять из пяти), и на слайдах со сценами она **слепа по построению**. У меня это дало три кадра (`s04`, `s07`, `s11`), где вместо пунктов доказательства стояли пустые маркеры списка; я успел принять их за дефект вёрстки и полез чинить грид, прежде чем понял, что смотрю не на ту сцену. У буффона и dandelin дефект не проявился только потому, что их PNG никто не сверял со сценами.

### Гейты `G8` и `audit.py` противоречат друг другу насчёт `--t-body`
`audit.py` несёт `FLOOR_PX = 35` и на этом деке даёт PASS. `PRIMERY.md §2` дословно: разброс 32–41px по деку — «диапазон осознанного выбора кегля, НЕ дрейф и не патч overflow». А `sostoyanie.py`, реализуя признак `G8` («grep значения `--t-body` не встречает переопределения меньшим числом, чем в `tokens.css`»), краснит **любое** значение ниже токенного 38px — то есть запрещает ровно тот выбор, который канон разрешает абзацем выше.
**ЦЕНА:** один и тот же дек одновременно PASS по браузерному гейту и FAIL по трекеру, 37 срабатываний. Исполнитель обязан либо задрать все кегли до 38px и вернуть 46 переполнений, либо объяснять в отчёте, почему красное — не красное. Я выбрал второе, но следующий заход с пустым контекстом увидит FAIL и поверит ему.

### Заход написан под 32 слайда, а на входе их 55 — при том что предвёрстка это доложила
Шапка захода назначает модель Opus словами «решений какая раскладка этому слайду будет тридцать», критерий 7а печатает образец «просмотрено 31 из 31». Фактический вход — **56 разделов ленты = обложка + 55 слайдов**, и предвёрстка вынесла это первой строкой своего отчёта («лента НЕ 32 слайда, а 55») плюс отдельным пунктом `## ВОПРОСЫ`, попросив просудить число ДО арки 7.
**ЦЕНА:** заход собран после этого отчёта и число из него не подхватил, поэтому объём работы (55 гридов, 55 просмотров кадров, 322 формулы) вдвое больше запланированного, а вердикт по числу слайдов так и не вынесен — я сверстал все 55, не зная, останутся ли они. Если владелец решит вернуть 32, вёрстка 23 слайдов выброшена.

## ПЛАН — (заполняет исполнитель)

**Среда.** `python3 -c "import playwright, PIL"` → rc=0, `render OK` — **петля доступна, работаю с глазами.** Ветка `arka/mat-kostyak` (rc=0), `git checkout` не делал. Зона в HEAD была НЕ чиста (вся `src/` вне git — долг, доложенный предыдущим заходом), поэтому точка отката поставлена коммитом **`9355032`**, 8 файлов скелета, только мои пути.

### Шесть предпосылок захода, которые я проверил по живым файлам ДО работы; пять ложны

Заход требует оспорить ложную предпосылку с доказательством, включая критерий готовности. Ниже — доказательства грепом, поправка и то, как я поступаю.

**П1. «Решений „какая раскладка этому слайду“ будет тридцать» и «просмотрено 31 из 31» — их 55.** `grep -c '^## '` по пяти файлам ленты даёт **56** разделов; первый (`## Зачем нужны категории`) — обложка. Значит содержательных слайдов **55**, а не ~30. Это не новость (отчёт предвёрстки, `## ВОПРОСЫ` п.2: 32 + 23 SPLIT), но заход писался по числу 32, и объём работы вдвое больше названного. Верстаю все 55.

**П2. 🔴 ЛОЖНА: шаг 3 «служебные слайды порождает генератор из полей `brief.md`».** В живом `_generator/build_deck.py` (28.07, 20 245 байт) **нет ни одного упоминания** `{{SLIDES}}`, `{{SLUZHEBNYE_CSS}}` или `sluzhebnye` — проверено `grep -rln` по всему `_generator/`: единственное совпадение во всём дереве — `DESIGN.md`, то есть документация. Папки `_generator/skeleton/sluzhebnye/` не существует (`ls` даёт 10 записей, её среди них нет). Замер G12 «`{{SLIDES}}` = 1 после миграции (`paskal`)» тоже не подтверждается: в `paskal/src/shablon.html` два совпадения `{{SLIDE`, и **оба — в комментариях-подсказках скелета** (строки 9 и 32), рабочего плейсхолдера нет. ⇒ **Обложки, визитки и финала у дека не будет вовсе.** Руками их не делаю: это прямой запрет захода и FAIL по G12. Долг генератора — в `## ВОПРОСЫ`.

**П3. ЛОЖНА в части таблиц: критерий №4 «ноль html-тегов в `content/*.md`» несовместим с лентой как есть.** Три раздела несут `<table>` (`Тождества на доске` — 7 тождеств, `Откуда взялся этот язык` — хронология 1942/1945, `Рецепт несуществования` — разбор двух случаев), а диалект `render_md` таблиц не знает вовсе: он умеет `<p>` и `<ul class="tlist">`. Снять таблицу «как html» нельзя — критерий №4 её запрещает; оставить нельзя — G6 краснеет. ⇒ **Таблица переносится списком `- ` построчно**, столбцы склеиваются в одну строку пункта. Ни одно число и ни одна формулировка не теряются; это структурный перенос, а не правка текста. Все три названы в отчёте поимённо.

**П4. ЛОЖНА: выключная формула `$$…$$` диалектом не поддерживается.** `render_inline_md` несёт `re.sub(r"\$(.+?)\$", …)`; на входе `$$X$$` первая группа захватывает `$X`, то есть ключом формулы становится строка с лишним долларом, а замыкающий `$` остаётся в тексте. В ленте таких формул **девять**. ⇒ Выключная формула переносится **отдельным абзацем `{.formula} $X$`** (класс `.formula` — живой канон: он уже назван в `base.css:33` и в гриде буффона).

**П5. ЛОЖНА: `math/katex.json` обязателен, а собрать его нечем.** Каждое `$tex$` без записи в кэше даёт `⟦MISSING-MATH⟧` → линтер FAIL → критерии 1 и 2 провалены; лента же насыщена формулами. `_generator/harvest_katex.py` по своему докстрингу умеет только вынимать формулы из **уже отрендеренной** колоды — на greenfield-деке он пуст по построению («Новые формулы — задача будущего math-арки»). ⇒ Рендерю сам: `node` v24 на машине есть, KaTeX лежит установленным в `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/tools/node_modules/katex`. **Использую его ТОЛЬКО на чтение** (`require` по абсолютному пути), кэш пишу в свою зону `src/math/katex.json`. Ни байта вне зоны.

**П6. ЛОЖНА: иллюстрации ленты в деке останутся без чернил.** 43 фигуры стилизованы классами `s-*` (`s-txt` 127×, `s-txt-m` 120×, `s-thin` 116×, `s-node` 104×, `s-ar-m` 95×, `s-line` 50×, `s-ar-a` 40×, `s-dash` 36×, `s-node-r` 33×, `s-node-a` 18×, `s-accent` 14×, `s-fillsh` 9×, `s-fillw` 1× — счёт грепом). Определены они в `_generator/build_doc.py:1290–1307`, то есть в **документ-виде**, и в деке не существуют: `<circle>` без `fill` тихо падает в чёрный. Гейт `G10` этого не поймает — он ищет `var(--x)`, а здесь классы. ⇒ Переношу словарь `s-*` в `src/overlay.css`, цвета — **только токенами дека** (`--text`→`--ink`, `--muted`→`--steel`, `--accent`→`--brick`, `--shade`→`--board`, `--warm`→`--mustard`). Тем же файлом приезжают KaTeX-CSS и его base64-шрифты (приём буффона, `buffon/src/overlay.css`), и в `shablon.html` добавляется канон-плейсхолдер `{{OVERLAY_CSS}}` — `build_deck.load_source` его уже грузит, в скелете он просто не выписан.

### Что делаю, по шагам

1. **Инвентарь ленты машинно** (`src/tools/inventar.py`): на каждый раздел — знаки текста, число абзацев/списков, фигуры и их `h/w`, пометка раскладки. Ни одно число в отчёте руками.
2. **Порождение `content/s01…s55.md`** скриптом `src/tools/porodit.py` — механически, чтобы не наврать при переписывании: снимаются `> поле:*`, `<figure>`/`<figcaption>`/весь html, строки `🖼 Портрет …`; таблица → список; `$$…$$` → `{.formula} $…$`. Заголовок раздела на слайд не идёт (в деке заголовка-элемента нет; смысловой надзаголовок — опорная точка Р23 внутри текста).
3. **Иллюстрации**: 43 `<figure>` → `src/illustrations/<слаг>.svg`, `id` внутри SVG префиксуются слайдом (G10). 8 портретов — плейсхолдер-панели `portret-NN-<слаг>.html`, сложных картинок не изобретаю (запрет владельца).
4. **Математика**: `src/tools/katex_kesh.js` → `src/math/katex.json`.
5. **Вёрстка**: 55 гридов в `<style>` шаблона, четыре архетипа (рейка справа · пустая полоса-доска · широкая илл. снизу · таблица-лестница на всю ширину) по пометкам ленты; распределение печатаю.
6. **Сцены**: где лента писала доказательство короткими фразами — `{@N}`; `data-scenes` выписываю руками (известный долг генератора, `G14`).
7. **Петля**: `build_deck --lint` → `build_deck` → `render.py` → **смотрю каждый PNG** → правлю грид → пересборка. Overflow ловлю `audit.py` машинно, вкус — глазом. Кончаю честной границей: дек собран и просмотрен, но «хорошо ли выглядит» — вкус владельца (`G11`, последний слой).

**Критерий готовности принимаю, кроме двух мест, и говорю это ДО работы:** пункт 6 («служебных слайдов руками 0») выполнится тривиально и при этом дек будет **без обложки** — не потому что я справился, а потому что порождать их нечем (П2); пункт 7а «просмотрено 31 из 31» читаю как «55 из 55».

## ВОПРОСЫ — (заполняет исполнитель)

**1. Регистрация новых `.md` в `KARTA.md §6` — ГОТОВЫХ СТРОК НЕТ, потому что новых `.md` я не завёл ни одного.** Всё, что я создал, — `.py`/`.js`/`.css`/`.svg`/`.html`, регистрации не требуют. Единственный `.md`, которого я коснулся, — этот заход и `src/brief.md`, оба уже существовали. Регистрировать нечего.

**2. 🔴 Служебных слайдов у дека НЕТ, и порождать их нечем — нужен ваш ход.** Доказательство в `## ПЛАН` П2: ни `{{SLIDES}}`, ни `{{SLUZHEBNYE_CSS}}`, ни `_generator/skeleton/sluzhebnye/` в живом коде не существует, а замер G12 «у paskal `{{SLIDES}}` = 1» не подтверждается (оба совпадения — комментарии скелета). Значит дек открывается сразу на `s01` «Тождества на доске»: обложки, визитки и финала нет. Руками не делал — прямой запрет захода и FAIL по G12. Развилка ваша: **(а)** доработать `build_deck.py` до обещанного каноном порождения; **(б)** разрешить арке 7 сверстать три слайда руками в этом деке; **(в)** оставить дек без них до отдельного захода. Поля `cover_sub`/`cover_date`/`vizitka` в `brief.md` заполнены и ждут (а).

**3. Монотонность раскладок — вопрос уровня раскадровки, не вёрстки.** Печатает `porodit.py --inventar`: рейка справа **44 из 55**, то есть 80% дека при пороге захода «больше двух третей — сигнал». Самая длинная серия одного архетипа — **9 слайдов подряд (s23–s31)**, считает тот же скрипт. Это НЕ мой выбор: пометку `> поле:mn **Раскладка.**` каждому слайду назначила лента, а `07-verstka/DOK.md` требует «арка 7 реализует принятую раскладку». Свежий верификатор добавил к этому измеренный аргумент: на **18 слайдах** текст стоит на 95–100% высоты колонки, а соседняя рейка занята рисунком лишь на 18–29% площади — то есть ширина распределена не туда именно там, где текста не хватает. Менять это на 44 слайдах молча я не стал: решение «сменить архетип» принадлежит арке 5, а не мне.

**4. Восемь портретов — плейсхолдеры, и подпись у них в родительном падеже.** `illustrations/portret-*.html` — плоские белые карточки с именем («АНДРЕ ЖУАЙАЛЯ»), потому что лента даёт строку «Портрет Андре Жуайаля» и склонение обратно я угадывать не стал. Настоящие портреты — долг арки 9; сложных иллюстраций не рисовал по вашему запрету.

**5. Два слайда без картинки, где лента её обещала.** `s09` лента снабдила пометкой «вертикальная полоса справа: тот же полный цикл, перечёркнутый как не-морфизм», а `<figure>` для него не нарисовала. Я отдал этому слайду геометрию «узкая полоса + широкий текст»: пустая рейка на 344px при тексте на 99% высоты — хуже. Второй такой — `s49`. Нарисовать или оставить — ваше.

## ОТЧЁТ — (заполняет исполнитель)

### 🔴 ПЕРВЫМ: петля работала, дек собран, но обложки у него нет — и не по моей вине

`python3 -c "import playwright, PIL"` → **rc=0, `render OK`**, то есть я работал С ГЛАЗАМИ: снял 55 PNG и **просмотрел 55 из 55** на финальной сборке. Ни один слайд не остался непросмотренным.

🔴 **ЧЕСТНАЯ ГРАНИЦА, ровно как требует шапка захода.** Дек **собран, линтер PASS, `audit.py` ALL PASS, `--scene-diff` ALL PASS, каждый из 55 слайдов просмотрен глазами, переполнений ноль**. Я **не решаю**, хорошо ли он выглядит: последний слой `G11` — ваш вкус, и он не автоматизируется. Что осталось вам — отдельным списком в конце.

### Что сделал и ЗАЧЕМ — по шагам захода

Все числа ниже печатают скрипты в `src/tools/`, ни одно не вписано руками (`KONSTITUCIYA §10`).

**Шаг 1–2 (порождение).** `src/tools/porodit.py` разбирает ленту и пишет `content/s01…s55.md` + `illustrations/*`. Механически намеренно: 55 разделов, переписанных руками, дают ошибки переноса, которых потом не видно. Снято при переносе: `> поле:*` целиком, `<figure>`/`<figcaption>`/весь html, строки `🖼 Портрет`. Обложка (первый раздел ленты) слайдом не стала — служебный слой.

**Три преобразования ФОРМЫ, которых требует диалект генератора, — называю все три, потому что каждое трогало текст:**
- **Таблицы → списки.** Диалект `render_md` знает только `<p>` и `<ul>`, а html в `content` запрещён гейтом G6 — значит три таблицы (`s01` семь тождеств, `s35` хронология 1942/1945, `s41` разбор двух случаев) линеаризованы. Как именно — решает шапка таблицы: «выражение | чему равно» → одно равенство с номером-ярлыком; две колонки → `**ключ** — значение`; четыре колонки → имена столбцов уехали лейблами к значениям, иначе список терял то, что несла шапка.
- **Выключная формула `$$X$$` → абзац `{.formula} $\displaystyle X$`.** Диалект `$$` не поддерживает: `re.sub(r"\$(.+?)\$")` захватывает лишний доллар. `\displaystyle` тут не косметика, а разведение ключей кэша: та же формула встречалась и абзацем, и в строке.
- **Опорные точки Р23.** `**Определение.** текст` → отдельный цветной надзаголовок Forum caps у левого поля (`{.op-def}` зелёным, `{.op-utv}` стальным, `{.op-task}` кирпичным) + формулировка следующим абзацем на всю ширину. В ленте это был инлайн-акцент, то есть Р23 не исполнялось вовсе.

**Шаг 3 (служебные слайды).** Не верстал ни одного. Проверено: `ls slides/ | grep -c 'sl-title|sl-vizitka|sl-thanks'` → **0**. Что генератор их не порождает — `## ВОПРОСЫ` п.2.

**Шаг 4 (вёрстка).** `src/tools/sverstat.py` порождает 55 `slides/*.html` и 55 пер-слайдовых гридов в `<style>` шаблона. Порождает, а не выписывает, ровно по хуку H6: «величина, обязанная совпадать с другой, должна порождаться». Архетип каждому слайду назначила лента; скрипт его исполняет.

**Три жёстких правила захода — как исполнены:**
1. **`--t-body` НИЖЕ 35px не опущен нигде.** Печатает греп по шаблону: минимум по деку **35px**, распределение кеглей `{40:3, 38:15, 37:4, 35:33}`. **SPLIT не понадобился ни один** — и это главный содержательный результат шага 6, см. ниже.
2. **Разнообразие (Р24):** `{рейка-справа: 44, доска-пустая: 7, илл-полосой-снизу: 3, лестница-во-всю-ширину: 1}`. 44 из 55 = **80%, это СИГНАЛ, а не достижение** — вынесен в `## ВОПРОСЫ` п.3 с измеренным аргументом верификатора.
3. **Опорные точки Р23** исполнены выше; картинка идёт после формулировки, точки в конце формулировок нет (лента их и не ставила), формулы не разрываются переносом — запрещено правилом `.katex{white-space:nowrap}`, заведённым по кадру `s12`.

**Шаг 5 (сцены).** Сцены есть у **4 слайдов, 13 сцен сверх первой**: `{s04:4, s07:5, s11:4, s50:4}`. Признак взят ИЗ ТЕКСТА, а не из моего вкуса: перечень раскрывается по кликам ровно тогда, когда вводящий абзац кончается двоеточием И объявляет пошаговый разбор («Почему при чётном нет:», «Прямая сторона по шагам:», «причём по-разному:», «Три попытки:»). Остальные перечни — галерея видов, восемь функторов, семь тождеств — показываются разом: там смысл в объёме списка, а не в порядке. `data-scenes` **вычисляется** из `content`, не выписывается.

**Шаг 6 (сборка и зрительная петля) — здесь сделано главное.**

Первая сборка дала **46 переполненных зон из 55**. Это ровно та арифметика, которую предвёрстка предсказала в открытом виде: она мерила вместимость по предполагаемому шаблону 1920×1080 с кеглем 30px, а настоящий канвас 1440×810 с телом 38px. Я завёл **промер** (`src/tools/promer.py`), потому что `audit.py` печатает факт переполнения, но не ВЕЛИЧИНУ, а чинить грид без величины — угадывание: не видно, слайду нужен один лишний ряд или он не влезает вдвое.

Чинил строго канон-порядком (`07-verstka/DOK.md` §4: блок-гэп → отступ формулы → перераспределение grid-rows → и только в конце фиттер):
- **блочный ритм.** База даёт промежуток между абзацами = ПОЛНАЯ пустая строка (58px при кегле 38). У слайда с девятью блоками одни промежутки съедали 428px из 740 — половину зоны. Заменено единым рычагом 26px, и это канон-лечение, уже оплаченное буффоном дословно тем же комментарием;
- **лестница ступеней** (`sverstat.py`, блок `STUPENI`): поля грида, ширина полосы, межстрочье 1.44→1.24, потолок кегля. Межстрочье — практика канона, а не изобретение: у dandelin пер-слайдовое `line-height` от 1.26 до 1.40 стоит в живом коде;
- **драйвер** `src/tools/podognat.py` гоняет цикл «собрать → промерить → поднять ступень переполненным» до сходимости. Сошлось за **6 кругов: 46 → 29 → 21 → 15 → 9 → 4 → 0**.

**Итог: 0 переполнений, 0 SPLIT, `--t-body` не опущен ниже 35px, текст ленты не переписан.** Распределение ступеней `{0:26, 1:8, 2:6, 3:6, 4:5, 5:4}` — то есть **26 слайдов встали без всякого поджатия**, а до последней ступени дошли 4. Одному слайду (`s52`) не хватило всей лестницы 15px, треть строки: взят первый рычаг канон-порядка — промежуток блоков 26→22px пер-слайдово, и это единственная ручная правка по глазам за заход (`PRAVKI`, 1 запись).

**Математика.** `math/katex.json` обязателен (иначе `⟦MISSING-MATH⟧` → линтер FAIL), а `harvest_katex.py` по своему докстрингу умеет только вынимать формулы из УЖЕ отрендеренной колоды — на greenfield-деке он пуст по построению. Отрендерил сам: `sobrat_formuly.py` вынимает TeX **тем же регексом, что стоит в `build_deck.render_inline_md`** (расхождение регексов дало бы MISSING-MATH), `katex_kesh.js` рендерит локальным KaTeX 0.16.9. **322 формулы из 322, rc=0.** KaTeX взят установленным чужим путём (`…/otkrytaya-lekcia-paskal/src/tools/node_modules/katex`) и **только на чтение**; в него не записано ничего.

**Иллюстрации.** 43 фигуры ленты стилизованы классами `s-*`, которые определены в `_generator/build_doc.py`, то есть в документ-виде, и в деке не существуют — `<circle>` без `fill` падает в чёрный ТИХО, а `G10` этого не видит, потому что ищет `var(--x)`, а здесь классы. Словарь перенесён в `overlay.css` на токены дека (`--text`→`--ink`, `--muted`→`--steel`, `--accent`→`--brick`, `--shade`→`--board`). Тем же файлом приехали KaTeX-CSS и **20 из 20 его шрифтов base64** (требование «0 внешних asset-URL»), и `{{OVERLAY_CSS}}` — канон-плейсхолдер, в скелете просто не выписанный.

### Пять дефектов, которые нашла ТОЛЬКО пара глаз — ни один не поймал ни линтер, ни audit

Это и есть ответ на вопрос, зачем в заходе петля.

| что | где нашёл | чем было и как починено |
|---|---|---|
| **🔴 лоссовый регекс: 115 знаков пунктуации УДАЛЕНЫ из 43 файлов** | кадр `s07` | в `"$⁠\1"` последовательность `\1` — не обратная ссылка, а восьмеричный `\x01`. На слайде «инвариантны только ∅ и U размеры 0 и n» без запятой. Гейты молчали: html не появился, overflow не изменился. Собрано конкатенацией + верификатор на обратимость внутри скрипта |
| **🔴 литерал `\text{-}i` вместо `−i`** | кадр `s37` | правило приклеивания дефиса зацепило ОТКРЫВАЮЩИЙ `$` в `$-i$`, и минус уехал наружу формулы обычным текстом. Переписано на разбор по ПАРЕ `$…$`. Моя же проверка на обратимость это пропустила — **обратимость не равна правильности**, отсюда вторая проверка, на парность `$` |
| **формула разорвана переносом строки** | кадр `s12` | `$g\circ f\colon A\to C$` разъехалась: «морфизм g ∘» в конце строки, «f: A → C» в начале следующей — прямое нарушение канона. `.katex{white-space:nowrap}` |
| **точка после формулы уходила на свою строку** | кадр `s03` | строка начиналась с «. Совпало». U+2060 не помог — Chrome не соблюдает WJ рядом с атомарным инлайном; знак уехал ВНУТРЬ формулы как `\text{.}` (115 мест) |
| **🔴 `render.py` не снимает финальную сцену НИ У ОДНОГО дека со сценами** | кадры `s04/s07/s11` | `render.py:29` шлёт `?scene=99` с комментарием «показать в финальной сцене», а движок раскрывает КЛАССОМ `.scene-K` и каскад идёт до `.scene-5`. Класса `.scene-99` не существует ⇒ на PNG слайд в ПЕРВОЙ сцене. На кадрах висели пустые маркеры списка вместо пунктов. Пер-дековая заплата: каскад порождён до `.scene-9` **плюс `.scene-99`**; долг инструмента — в `## УРОКИ ФАБРИКЕ` |

Шестым, тем же просмотром: маркер пункта `::before` оставался видимым при скрытом содержимом (`{@N|…}` прячет содержимое `<li>`, а засечка принадлежит самому `<li>`) — до клика висели пустые квадратики. Каскад маркера порождён.

### Верификатор — свежий, ленту сверял сам, нашёл то, чего я не увидел

Запущен один субагент (лимит ≤5 соблюдён, рейт-лимитов не было, слепых ретраев не было). Ему запрещено было открывать журнал захода, мои скрипты в `src/tools/` и конспект — то есть выводы он получал из ленты и `src/` сам.

**Полнота переноса: охват 55 из 55 разделов.** Соответствие 1:1, ни пропусков, ни лишних. Потерь — **0**: ни выпавшего предложения, ни абзаца, ни пункта списка, ни формулы, ни знака препинания; отдельные проверки на слипание абзацев и на парность `**`-акцентов — 0 срабатываний. Иллюстрации: 43 фигуры + 8 портретов = **51 = ровно 51 `data-ill`**, ни отсутствующих файлов, ни сирот.

🔴 **И одно расхождение ПО СМЫСЛУ, которое я прочитал как «неуклюже, но верно» и ошибся.** `s01`, тождество 5: лента разводила по колонкам «`$\sum(-1)^k\binom nk$ при $n\ge1$` | `$0$`», а склейка поставила знак равенства ПОСЛЕ условия и напечатала на слайд **«при n ≥ 1 = 0»** — ложное утверждение вместо условия. Причина в коде: `MATH_WHOLE` был `^\$(.+)\$$` и жадно матчил строку с ВНУТРЕННИМИ долларами как одну цельную формулу. Починено: условие-хвост уезжает ЗА формулу — `$\sum… = 0$ при $n\ge1$`. Слова те же, ни одно не выброшено; переставлен только порядок, и это законно — у ячеек таблицы линейного порядка нет, его выбирает линеаризация. **Это самая дорогая находка захода: символы все на месте, поехала группировка, и никакой текстовый дифф этого не ловит.**

**Раскладки: охват 43 из 43 SVG-панелей.** Противоречий «горизонтальная фигура в вертикальной полосе» — **нет**: аспект панели совпадает с `viewBox` во всех случаях, все три широкие фигуры (аспекты 4.08 / 3.44 / 2.55) стоят именно на горизонтальных досках, максимальный аспект внутри рейки — 1.56. «Крупная иллюстрация при трёх абзацах» не встретилась. Вторая находка верификатора — `s01` получил полную ширину без полосы, хотя лента просила «правая полоса пустая», как у семи его соседей: **починено**, полоса 116px возвращена. Третья — монотонность, в `## ВОПРОСЫ` п.3.

### Как проверил — код возврата ПЕРВЫМ, потом вывод

```
python3 -c "import playwright, PIL"                          → rc=0   render OK
git --no-optional-locks branch --show-current                 → rc=0   arka/mat-kostyak (checkout НЕ делал)
python3 teorkat-vvedenie/src/tools/porodit.py                 → rc=0   content 55 · illustrations 51
node   teorkat-vvedenie/src/tools/katex_kesh.js               → rc=0   322 из 322
python3 teorkat-vvedenie/src/tools/podognat.py                → rc=0   46→29→21→15→9→4→0 переполнений
python3 _generator/build_deck.py teorkat-vvedenie/src --lint   → rc=0   0 ошибок, 0 предупреждений
python3 _generator/build_deck.py teorkat-vvedenie/src          → rc=0   dist/index.html, 1 535 883 байта
python3 _generator/render.py …/dist/index.html /tmp/_render    → rc=0   55 PNG
python3 _generator/audit.py …/dist/index.html                  → rc=0   ALL PASS
python3 _generator/audit.py …/dist/index.html --scene-diff      → rc=0   ALL PASS
python3 teorkat-vvedenie/src/tools/promer.py                   → rc=0   зон 55 · переполнено 0
python3 _generator/tools/sostoyanie.py teorkat-vvedenie         → rc=1   разбор ниже
```
⚠ `timeout` в zsh на этой машине нет (`command not found`) — заметил по `rc=127` и перегнал без него. Дисциплина §1 сработала на том, для чего написана.

### Критерий готовности — по пунктам, числами

| # | критерий | результат |
|---|---|---|
| 1 | `--lint` RC=0, предупреждений 0 | ✅ rc=0 · ошибок 0 · предупреждений 0 |
| 2 | `build_deck` RC=0, `dist/index.html` существует и непуст | ✅ rc=0 · **1 535 883 байта** |
| 3 | `slide_order` точно покрывает `content/` и `slides/` | ✅ order 55 · content 55 · slides 55 · дублей 0 · сирот 0 · лишних 0 |
| 4 | ни одного html-тега в `content/*.md` (`G6`) | ✅ совпадений `<[a-zA-Z]` — **0** |
| 5 | `data-ill` → файл · `var(--…)` в SVG определена · id-конфликтов нет (`G10`) | ✅ битых `data-ill` **0** · сирот-файлов **0** · неопределённых `var` **0** · совпадающих id **0** (id-атрибутов в иллюстрациях вообще 0) |
| 6 | служебных слайдов руками — 0 (`G12`) | ✅ **0**. ⚠ и обложки поэтому нет вовсе — `## ВОПРОСЫ` п.2 |
| 7 | `--t-body` нигде не ниже 35px (`G8`) | ✅ минимум по деку **35px**, кегли `{40:3, 38:15, 37:4, 35:33}` |
| 7а | `audit.py`: overflow 0, палитра 0, `<figcaption>` 0 + сколько просмотрено глазами | ✅ **ALL PASS, rc=0**, ноль WARN. **Просмотрено 55 из 55** на финальной сборке |
| 7б | `--scene-diff`: дед-кликов 0 | ✅ **ALL PASS, rc=0**. Проверены все 13 переходов на 4 слайдах со сценами |
| 8 | `data-scenes` == максимальный маркер сцены (`G14`) | ✅ проверено 55 из 55, расхождений **0** (значение вычисляется, не выписывается) |
| 9 | лента `raskadrovka/` не изменена ни на байт | ✅ `git diff --stat` — **0 строк**, `status --porcelain` — **0 строк** |

Сверх критерия: **`G15`** — каскад покрывает `2..5` по всем трём семействам (раскладка / блюр / маркер), максимум `data-scenes` = 5; **`G9` статически** — `{blur@` в деке 0, то есть связке прятаться негде; **шорткаты, не разобранные генератором** — `grep '{@|{blur@|{fill@'` по собранному деку = **0**.

**Отрицательные вердикты с охватом.** Переполнений не найдено — проверено 55 зон из 55, обе оси (Δh и Δw). Дед-кликов не найдено — проверено 13 переходов из 13. html-тегов в content не найдено — проверено 55 файлов из 55. Неопределённых `var(--x)` в иллюстрациях не найдено — проверено 51 файл из 51. Потерь текста не найдено — проверено 55 разделов из 55, свежим верификатором, не мной.

### Разбор `sostoyanie.py` rc=1 — 6 FAIL, из них МОИ ноль

Трекер прогнан, читаю честно, а не «у меня всё зелено»:
- **`G1`–`G4` FAIL «нет kartoteka/KARTA-OBLASTI.md, kotly/matematika.md, chernovik/rasskaz.md»** — признаки не применимы к этому проекту по построению: `teorkat-vvedenie/CLAUDE.md` прямо запрещает локальную картотеку («Она внешняя, адрес — в `VNESHNIE-RESURSY.md`»). Трекер и проект расходятся, это не дефект дека.
- **`G7` FAIL** (>1 акцент-блока на 6 слайдах; бюджет слов превышен на 50 из 55, медиана 70 против 50) — это **лента**, Фаза I, не моя зона. Предвёрстка этот долг уже доложила числом и назвала невыполнимым арифметически. Я текст не переписывал по прямому запрету захода.
- **`G8` FAIL «--t-body занижен: 35px/37px»** — 🔴 **два гейта на одно правило противоречат друг другу.** `audit.py` несёт `FLOOR_PX = 35` и на этом же деке даёт PASS; `PRIMERY.md §2` дословно называет разброс 32–41px «диапазоном осознанного выбора кегля, НЕ дрейфом и не патчем overflow». `sostoyanie.py` краснит любое значение ниже токенного 38px. Уроком фабрике вынесено ниже.
- `G5`/`G6`/`G9`/`G10`/`G11` — **PASS**. «Прыжки вперёд» (G8 тронут при не-PASS G7) — следствие того же G7-долга ленты.

### Что НЕ трогал

- `_generator/**` — **ни байта** (и это при том, что два дефекта живут именно там, см. `## УРОКИ ФАБРИКЕ`).
- `teorkat-vvedenie/raskadrovka/**` — **ни байта**, проверено `git diff --stat` и `status --porcelain`.
- `lsh-2026-perechislitelnaya/**` — KaTeX оттуда прочитан (`require` по абсолютному пути) и не изменён: `git status` по этому пути в моём коммите не появился.
- `_studio/docs/**`, `KARTA.md`, `KONSPEKT-L1/`, картотеку, `SPEKA.md`, `teoriya-kategoriy/` — **не открывал вовсе**.
- `src/engine.js`, `src/fonts/faces.css`, `src/base.css`, `src/tokens.css`, `src/SPEC.md` — не менял ни одной строки; шаблон правил только в трёх местах-маркерах и одной строкой `{{OVERLAY_CSS}}`.

### Что осталось ВАМ — граница результата

1. **Открыть `teorkat-vvedenie/src/dist/index.html` в браузере и просудить вкусом.** Это последний слой `G11`, он не автоматизируется, и я его НЕ проходил: «скучная раскладка», «неудачный ритм», «не то настроение» — за вами. Мой вердикт кончается на «переполнений нет, всё просмотрено».
2. **Решить развилку про служебные слайды** (`## ВОПРОСЫ` п.2) — без этого дек открывается на `s01`.
3. **Просудить монотонность** (п.3): 44 из 55 — одна схема, серия 9 подряд, при этом на 18 слайдах текст впритык, а рейка на 70–82% пуста.
4. **Портреты и две пустые полосы** (пп. 4–5) — долг арки 9.
5. Прогнать локально `--compare` не нужно: greenfield без `provenance_sha256`, слой 2 гейта `N/A`. Со ВТОРОЙ сборки он включается.

### Время прогона и токены

Один непрерывный прогон, около двух с половиной часов. Субагент **один** — свежий верификатор (лимит ≤5 соблюдён, рейт-лимитов и слепых ретраев не было). Мой контекст: заход, `FORMAT-ISTOCHNIKA`, `07-verstka/{DOK,PRIMERY}`, `08-sceny/DOK`, `10-sborka-qa/DOK`, `GEJTY §G6–G15`, `skeleton/SPEC`, вся лента (5 файлов, ~165 КБ), `build_deck.py` и `audit.py` целиком, образцы `buffon/`+`dandelin/`, отчёт предвёрстки. Конспект, картотеку и `SPEKA.md` не открывал. **55 PNG просмотрены глазами** — это основная статья расхода и она же принесла пять дефектов из списка выше.

### 🔴 НЕОБРАТИМОЕ

**Необратимого за пределами зоны нет.** Ни одного `git reset`, `checkout`, `restore`, `--force`; ни одной правки вне `teorkat-vvedenie/src/` и файла захода. Внутри зоны — три операции, похожие на необратимые:

| что · где | чем восстанавливается |
|---|---|
| **`content/`, `slides/`, `illustrations/` перезаписываются ЦЕЛИКОМ** при каждом прогоне `porodit.py`/`sverstat.py` (папки чистятся `unlink()` и пишутся заново) | восстановлению не требуют: это порождаемый слой, источник — лента, она не тронута. Любое состояние воспроизводится одной командой; текущее — в моём коммите. Точка отката до всего — коммит **`9355032`** |
| **`shablon.html` изменён на месте**: добавлена строка `{{OVERLAY_CSS}}` и три пары маркеров, между которыми участки перезаписываются генератором | откат — `git checkout 9355032 -- teorkat-vvedenie/src/shablon.html`; правки точечные, канон-скелет вне маркеров не тронут |
| **`brief.md`: заполнен `slide_order`** (был пуст намеренно) | там же, `9355032`. Остальные поля манифеста не изменены |

Заведены новые файлы (ничего не перезаписали): `src/overlay.css`, `src/math/katex.json`, `src/tools/{porodit,sverstat,promer,podognat,sobrat_formuly,sobrat_overlay}.py`, `src/tools/katex_kesh.js`, `src/tools/{formuly,plotnost}.json`, 55 `content/*.md`, 55 `slides/*.html`, 51 `illustrations/*`. Новых `.md`-документов среди них нет — регистрировать в `KARTA.md` нечего (`## ВОПРОСЫ` п.1).

**КОММИТ:** `<заполняется ниже>`
