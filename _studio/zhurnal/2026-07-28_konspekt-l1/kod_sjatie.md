# Канал исполнителя — sjatie (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — решение «какую фразу как сжать и что уходит в сцену» рождается по ходу на каждом абзаце; готовых якорей нет, механикой это не расписывается.
> ⏰ **Лекция читается СЕГОДНЯ ВЕЧЕРОМ 29.07.** Работай тщательно и не спеша — часы есть; но помни, что халтура здесь стоит лекции, а не переделки.

КОНТЕКСТ. Лекция «Зачем нужны категории» — открытое введение в теорию категорий, **онлайн**, сегодня вечером. Прошлый этап: заход предвёрстки собрал ленту текстов слайдов — `teorkat-vvedenie/raskadrovka/teksty/`, пять файлов по блокам, **56 разделов** (`## `) = обложка + 55 слайдов, 30 546 знаков видимого текста, медиана 70 слов на раздел.

🔴 **Почему разделов 55, хотя принятая структура лекции — 32 слайда.** Это не разрастание материала и не вина исполнителя: два захода мерили в разных единицах. Заход разреза считал вместимость **на СЦЕНУ** (слайд разворачивается по кликам, в среднем три сцены). Заход ленты получил тот же потолок как потолок **на РАЗДЕЛ** — то есть на то, что видно разом. При 30 100 знаках и потолке 650 арифметика потребовала минимум 47 разделов, и исполнитель честно нарезал 32 + 23 SPLIT = 55. Ошибка аналитика, разобрана и оплачена.

**Проверено по живым файлам до написания этого захода:** обратная склейка (раздел + идущие за ним подряд `SPLIT`) даёт **ровно 33 группы = обложка + 32 слайда**, точное совпадение с принятым разрезом. Материал не поехал — поехала форма записи.

ЦЕЛЬ: **вернуть ленту к 32 содержательным слайдам** — не механическим слипанием («назвали два соседних слайда одним»), а настоящим слайдом со сценами: общий заголовок, общая иллюстративная опора, часть текста приходит и уходит по кликам. Одновременно сжать текст до эталонного регистра: сейчас средняя длина предложения **32,7 слова**, у эталонной ленты Паскаля — **18,1**, и это главное расхождение, а не словарь.

**Зачем 32, а не 55 — словами владельца, чтобы ты понимал предмет, а не считал строки.** «Тридцать две законченные мысли за два часа — это примерно четыре минуты на мысль, и такую лекцию можно рассказать. Если их пятьдесят, приходится очень часто мелькать: одна мысль, другая, третья — это забывается, это создаёт эффект кома, который падает сверху». Слайд = **одна законченная мысль, целиком помещающаяся на один экран**; сцены — способ развернуть её во времени, а не способ добавить мыслей.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **СТОП ДО ЦЕЛИ, говорю громко и заранее: ты получишь сжатую ленту и пересобранный `view.html`, но НЕ переверстанный дек.** Дек (`teorkat-vvedenie/src/`) — зона следующего захода, который стартует после твоего коммита. В `src/` не заходи вовсе, даже посмотреть.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-28_konspekt-l1/kod_sjatie.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-28_konspekt-l1/kod_sjatie.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО перечисленное ниже. Проект не изучай, в `teorkat-vvedenie/src/` не заходи.

**Рабочий материал (правишь):**
- `teorkat-vvedenie/raskadrovka/teksty/{A-krasivaya,B-yazyk,C-zapret-retrakt,D-zapret-estestvennost,E-dva-mira}.md` — сама лента, 56 разделов.
- `teorkat-vvedenie/raskadrovka/gejt_lenty.py` — **штатный счётчик ленты. Все числа в отчёте — им, не глазом и не своей эвристикой.** Он выкидывает служебные `> поле:`, `<figure>`, `<table>` и считает `$$…$$` за 120 знаков. Не хватает среза (длина предложения, знаки на сцену) — допиши функцию в него же, это твоя зона.

**Канон, которому подчиняешься (read-only):**
- `_studio/konvejer/06-tekst/DOK.md` — арка 6: правила плакатного стиля, гейт G7 (десять пунктов), синтаксис шорткатов, «формулировка обрывочной быть не может, доказательство — может». **Читай целиком, он короткий (97 строк) и он твой главный закон.**
- `_studio/konvejer/08-sceny/DOK.md` — контракт сцен: синтаксис Р2 (`{@N}`, `{@N-M}`, `{fill@N|…}`, `{blur@N|…}`), ловушки (`.fill` — связка ДО, не внутри; `prev()` не покадровый).

**Эталон регистра — читай ДОСЛОВНО и подражай ему, это лента той же фабрики, признанная владельцем удачной:**
```
git --no-optional-locks show 4833841:lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/teksty/blok-1.md
git --no-optional-locks show 4833841:lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/teksty/blok-2.md
```
(В рабочем дереве этих файлов нет — они только в том коммите. Медиана там 51,5 слова / 405 знаков, средняя длина предложения 18,1.)

**Источник смысла — сверяться, НЕ переписывать (строго read-only):**
- `teorkat-vvedenie/KONSPEKT-L1/` — двенадцать вкладок внутреннего конспекта. Открывай адресно, когда надо проверить, что при сжатии не потерялась формулировка. Целиком не читай — он большой, и он не твоя задача.

🔴 **ЛОЖНАЯ ПРЕДПОСЫЛКА, снятая аналитиком ДО тебя — не воспроизводи её.** Владелец, ставя задачу, назвал скилл `russian-editor` источником стиля. Живой канон говорит обратное: `06-tekst/DOK.md` строка 14 — «`russian-editor` (литературный) для слайда **НЕВЕРЕН** — только гигиена опечаток/„ё“». Плакатный текст слайда живёт по другим законам, чем литературный русский: там неполные предложения и обрывы — норма, а не брак. **Стиль берёшь из эталона Паскаля и из `06-tekst/DOK.md §Правила плакатного стиля`; `russian-editor` не зови вовсе.**

- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

🔴 **СЖИМАЕШЬ ТЫ САМ, ОДНОЙ РУКОЙ. Субагентам сжатие НЕ раздавай.** Нынешняя лента написана четырьмя субагентами по блокам, и швы между блоками видны: регистр гуляет. Единство голоса на 32 слайдах — это и есть результат, а параллелизм его убивает. Субагенты законны на **измерении** и на **верификации** (§3), максимум 5, и там их называй явно.

### Шаг 1. Инвентарь и карта склейки (машинно, до единой правки)

Восстанови группировку: раздел + все идущие за ним ПОДРЯД разделы со строкой `> поле:mn **SPLIT.**` = один слайд. Напечатай в `## ПЛАН` таблицу: № слайда · заголовок · сколько разделов вошло · знаков · слов · иллюстраций. Групп обязано выйти **33** (обложка + 32). Вышло не 33 — **СТОП, не правь ничего**, напиши расхождение в `## ПЛАН` и разбирайся: это значит, что либо `SPLIT` стоит там, где я не ожидал, либо моя предпосылка ложна. Проверять чужую арифметику до работы дешевле, чем переделывать после.

### Шаг 2. Схлопывание — настоящий слайд, а не слипание

Собери каждую группу в ОДИН раздел `## `. Требование владельца дословно: *«чтобы это реально были слайды со сценами — чтобы там был какой-то общий заголовок, общая структура, иллюстрации; просто какая-то часть текста может уходить и появляться. А не то, что мы два соседних слайда назвали одним»*.

- **Заголовок** — один на слайд, называет мысль всего слайда целиком, а не первой его трети. Заголовки первых разделов групп часто годятся как есть; где склеились разные предметы — напиши новый. Канон: заголовок называет тему и не спойлерит (`06-tekst/DOK.md`).
- Служебные строки `> поле:mn **SPLIT.** …` **снимаются полностью** — их работа кончилась.
- `> поле:mn **Раскладка.** …` — на слайд остаётся ОДНА, согласованная со всеми его иллюстрациями. Два разных типа раскладки внутри одного слайда — брак (`06-tekst` §Контракт: горизонтальный рисунок в вертикальной полосе ловится только здесь).
- Иллюстрации группы остаются все; если их стало слишком много для одной раскладки — это сигнал, что часть уходит в сцены вместе со своим текстом.

### Шаг 3. Сжатие — пять правил

Цель — **средняя длина предложения ≤ 20 слов** (сейчас 32,7; эталон 18,1) и **медиана ≤ 55 слов на слайд**. Измерено заранее: правила дают в сумме −30…−38 % объёма, до 19 000–21 500 знаков. Это не догадка, а замер по этой самой ленте.

**Правило 1 — телеграфный регистр** (даёт больше всех, ~27 %). Неполные предложения вместо полных, двоеточие вместо связки, формула вместо словесного условия, термин вместо расшифровки. Служебные обороты, которых в эталоне нет вовсе: «называется», «есть» связкой (59 строк!), «то есть», «в точности», «служит», «состоит из», «подчинён», «таковы, что», «мы будем», «обязан».
> ❌ `**Определение.** Естественное преобразование $\alpha\colon F\Rightarrow G$ функторов $F,G\colon\mathcal C\to\mathcal D$ **есть семейство** морфизмов $\alpha_A\colon FA\to GA$, **такое что при всяком** $f\colon A\to B$ коммутативен квадрат $Gf\circ\alpha_A=\alpha_B\circ Ff$ — 254 знака
> ✅ `**Естественное преобразование** $\alpha\colon F\Rightarrow G$: набор стрелок $\alpha_A\colon FA\to GA$, квадрат коммутирует — 117 знаков`
> ❌ `**Определение.** Категория $\mathcal C$ **состоит из** четырёх данных: класс объектов; множество морфизмов $\mathcal C(A,B)$ на каждую упорядоченную пару объектов, запись $f\colon A\to B$; композиция, сопоставляющая паре… — 317`
> ✅ `**Категория $\mathcal C$.** Объекты; морфизмы $\mathcal C(A,B)$, запись $f\colon A\to B$; композиция $g\circ f$; тождество $1_A$ — 117`
>
> Владелец про это же: *«достаточно сказать „ассоциативность композиции“ без формулы — и так понятно, что это такое»*. **Но осторожно:** канон запрещает обрывать ФОРМУЛИРОВКУ (`06-tekst` п.2 — «формулировка обрывочной быть не может, доказательство — может»). Телеграфность — это сжатие синтаксиса, а не выкидывание условий. Убрал условие из определения — сломал определение.

**Правило 2 — снять режиссуру** (~13 %). В ленту протекло то, что лектор ГОВОРИТ, а не печатает: мостики между слайдами, комментарии к спискам, авансы, строки, приехавшие из полей `> поле:insight` конспекта. Таких 57 строк / 5 758 знаков.
> ❌ `Функторов зал знает десятки, но не под этим именем` · `Общего ответа нет, конкретные бывают` — на экране не нужны вовсе.
> ⚠ Не путай с **ударной строкой-выводом**: эталон такие держит («**Облик решает, что даётся даром**»). Мораль слайда остаётся; остаётся ОДНА и жирной.

**Правило 3 — ранжировать доказательства, НЕ выкидывать их.**
🔴 **Доказательства на слайде РАЗРЕШЕНЫ. Строка конспекта `12-miry.md:262` («доказательства идут у доски, а не на слайде») ОТМЕНЕНА владельцем 29.07** — она писалась под офлайн-лекцию, а лекция читается онлайн, и никакой доски не будет. Владелец: *«это неправда, что мы жёстко запрещаем доказательства — это неправда потому, что никакой доски не будет. Доказательства мы хотим выписывать. Просто мы должны их очень-очень ранжировать»*.
Проставь каждому доказательному месту разряд и действуй по нему: **`ПОЛНОЕ`** (стоит показа целиком) · **`СКЕТЧ`** (1–2 строки, ключевой ход) · **`ИДЕЯ`** (одна фраза) · **`НЕТ`** (снимается). В ленте 19 доказательных мест: 11 полных выводов (3 188 знаков), 6 скетчей с водой (759), 2 однострочника (144). Разряд ставь по важности для сюжета, а не по длине.
> Эталон разряда `ИДЕЯ`, уже стоящий в ленте: `Доказательство в одну строку: $Fr\circ Fs=F(r\circ s)=F(1_A)=1_{FA}$`
> Кандидат в `СКЕТЧ`: 352 знака про центр группы → `$C_2\xrightarrow{s}S_3\xrightarrow{\mathrm{sgn}}C_2=1_{C_2}$, но $Z\colon C_2\to 1\to C_2$` (≈75)

**Правило 4 — формулировка уезжает в иллюстрацию** (~6 %). 21 место, где текст пересказывает то, что и так нарисовано рядом. Канон прямо: «текст не описывает иллюстрацию». Владелец: *«в определении категории должна быть вот такая коммутативная диаграмма, как нарисована, — и всё, и никак не комментировать: „квадрат коммутативен“, двоеточие, и на полях квадрат»*.
> ❌ `Переименовать и потом применить биекцию даёт то же, что применить биекцию и потом переименовать` — при `figcaption` рядом: «Оба пути из левого верхнего угла в правый нижний дают один результат». Дословный дубль → строка снимается целиком.

**Правило 5 — символы вместо длинных имён** (~3 %, но без него правило 1 не выйдет на свои проценты). 82 вхождения / 1 823 знака. В зоопарке примеров категорий из 12 имён символами названы только два.
> `метрические пространства и нерастягивающие отображения` → $\mathbf{Met}$ (54 → 12)
> `категории конечных булевых алгебр и категории, противоположной категории конечных множеств` → $\mathbf{FinBool}\simeq\mathbf{FinSet}^{\mathrm{op}}$ (90 → 51)
> **Заведи словарь сокращений один раз** и держи его сквозным по всей ленте: одно понятие — одно обозначение во всех пяти файлах. Первое употребление можно расшифровать, дальше только символ.

**Два места, названные владельцем поимённо — по ним отчитайся отдельной строкой:**
1. *«Я считаю, что определение категории, функтора и естественного преобразования должно быть написано на одном слайде, все три»*. Сейчас они на одном (слайд #12 после склейки), но занимают 1460 знаков / 141 слово — вдвое выше потолка. После сжатия обязаны встать **одной сценой**. Не встали — скажи честно и назови, чего не хватило.
2. **Зоопарк примеров категорий** — сейчас 3 553 знака на двух слайдах (#14 «Стрелка как отображение», 2 223 знака — второй по тяжести во всей ленте, и #15). Владелец: *«помещается ли слайд с примерами категорий на один слайд — это очень важно; если не помещается за счёт длинных названий, надо их заменять на символы»*. Свести в один слайд со сценами, если выйдет; не выйдет — назвать, сколько осталось и почему.

### Шаг 3½. Три точечные правки содержания из ночного ресёрча — и НИ ОДНОЙ сверх них

Параллельный заход ресёрча закрыл десять математических долгов; его отчёт принят. Три находки касаются **напечатанного на слайде**, и внести их надо тем же ходом, что и сжатие, — иначе лекция уедет с ошибкой. Формулировки готовы, думать над ними не надо.

🔴 **Граница жёсткая: по СОДЕРЖАНИЮ ты меняешь ровно эти три места и ничего больше.** Всё остальное — сжатие формы. Увидел по дороге ещё одну математическую неточность — **в `## ВОПРОСЫ`, не в правку**: содержание лекции просужено владельцем, и тихо доправлять его ты не уполномочен.

**Правка 1 — подпись к портрету Жуайаля** (`A-krasivaya.md:299`, `🖼 Портрет Андре Жуайаля {1}`). Портрет стоит плейсхолдером, подписи нет. Поставь рядом готовый текст, проверенный по печатным работам:
> «Тот же человек, который в 1981 году построил теорию комбинаторных видов — и попутно дал изящное доказательство формулы Кэли через „позвоночных“, — двадцать лет спустя вернул из забвения квазикатегории: определили их ещё Бордман и Фогт в 1973-м как „слабые комплексы Кана“, но именем и полноценной теорией они обязаны Жуайалю. А ещё через десять лет он оказался в Принстоне, в программе Воеводского по унивалентным основаниям, и строил категорные модели теории типов.»

Нужен короткий вариант под раскладку — честный минимум: *«тот же человек через двадцать лет сделал теорию квазикатегорий — один из подходов к ∞-категориям»*.
⛔ **Чего писать НЕЛЬЗЯ ни в каком виде:** «Жуайаль придумал/дал квазикатегории» (их ввели Бордман и Фогт, 1973) и «тридцать лет спустя» (1981→2002 — двадцать). В зале есть люди из гомотопической теории, ошибка атрибуции слышна сразу, а слайд в этом месте — перебивка, то есть цена ошибки ничем не окупается.

**Правка 2 — формулировка утверждения про раскраски** (`B-yazyk.md`, район строк 120–135; ищи по слову «раскрас»). В формулировке стоит «сопоставление», из чего не видно, что запрещается **любое** продолжение на морфизмы. Замени на: **«не существует функтора, совпадающего с этим правилом на объектах»** — и так же для контравариантного случая, если он на слайде назван.

**Правка 3 — отчитайся, а не правь.** Проверь, стоит ли где-нибудь в ленте фраза вида «изоморфизма категорий между конечными булевыми алгебрами и конечными множествами$^{\mathrm{op}}$ **не существует вовсе**» (у Стоуна, район `E-dva-mira.md:209–266`). Ресёрч доказал, что утверждение **верно**, и доказательство короткое: пустое множество одно, а вырожденных булевых алгебр — собственный класс, изоморфизм же категорий обязан сопоставлять классы изоморфных объектов один в один. Если фраза в ленте есть — оставь и в `## ВОПРОСЫ` скажи, где она; если нет — скажи, что нет. **Сам доказательство на слайд не добавляй**: место ему выбирает владелец.

### Шаг 4. Сцены — две механики, и вторая новая

**Механика А — НАКОПЛЕНИЕ:** `{@N}`, текст прибывает от сцены к сцене. Это то, что канон умеет и чем пользовались до сих пор.

🔴 **Механика Б — ЗАМЕНА: `{@N-M}` и `{@-M}` (`data-scene-until`), текст УХОДИТ.** Она поддержана генератором и движком (`08-sceny/DOK.md §Синтаксис Р2`), но до сих пор не использовалась, и **именно её отсутствие расплющило ленту**: если текст только накапливается, то потолок «сколько видно разом» становится потолком на весь слайд, и слайд обязан дробиться. С заменой потолок применяется к СЦЕНЕ.
> Образец применения — словами владельца: *«есть общая конструкция и два утверждения с очень похожими доказательствами. Сверху появляется первое утверждение, снизу разворачивается его доказательство; потом первое утверждение остаётся, ниже него появляется второе, а доказательство первого уходит»*.

**Бюджет теперь считается по сцене, а не по слайду:** видимого разом — **не более 650 знаков**. При 32 слайдах и трёх сценах ёмкость ~62 000 знаков против 30 546 нынешних, то есть **вместимость перестала быть ограничением с двукратным запасом**. Сжимаешь ты ради времени и читаемости зала, а не чтобы влезло. Не подгоняй текст под потолок — подгоняй под мысль.

**Проверь на каждом слайде с ≥2 сценами (это гейт, см. §2 критерий):**
- ни один клик не пустой — «дед-клик» (сцена без видимых изменений) есть дефект, `08-sceny` называет прецедент;
- 🔴 **последняя сцена не совпадает с первой.** Владелец назвал эту ловушку прямо: *«стандартная проблема — на первой мало текста, потом больше, больше, а на последней опять откатились»*. Возникает она именно при неаккуратной замене: сняли всё добавленное и вернулись в исходное. Проверяй программно — сравнивай множество видимых элементов первой и последней сцены;
- текст и картинка одной сцены появляются **синхронно** (иначе картинка отстаёт на ~130 мс — фикс `transition-delay:0s`, но он в зоне вёрстки; ты только не разводи их по разным сценам без причины);
- связка (`≈`, `=`) вне блюра: `≈ <span class="fill">…</span>`, никогда наоборот;
- не проектируй сцену под покадровое «назад»: `prev()` внутри слайда сбрасывает на шаг 1, а не на предыдущий кадр.

**Оставь след для следующего захода.** Он будет переверстывать существующий дек из 55 слайдов и обязан знать, что во что уехало. Выпиши таблицу соответствия **старый раздел ленты → новый слайд** в шапку первого файла ленты (`A-krasivaya.md`, в `> поле:mn`) ИЛИ отдельным файлом — если отдельным, положи готовую строку регистрации в `## ВОПРОСЫ` (см. контракт зоны).

### Шаг 5. Пересборка и чтение целиком

`build_doc.py` по ленте → `view.html`; пересборка бесплатна, гоняй после каждого блока правок, а не в конце. Гейт отрисовки: `python3 _generator/tools/check_view.py <src>` — ловит «текст съеден при отрисовке»; в формулах писать `\lt`/`\gt`, не `<`/`>`.
Последним ходом **прочитай собранную ленту целиком, сверху вниз, как читал бы зал.** Канон требует этого пунктом 10 гейта G7, и это единственный способ увидеть, что нарратив не провис на швах между блоками.

---

**КРИТЕРИЙ ГОТОВНОСТИ (каждый пункт может ПРОВАЛИТЬСЯ; числа — командой, не глазом):**

1. **`grep -c '^## '` по пяти файлам ленты в сумме = 33** (обложка + 32 слайда). Другое число — FAIL, даже если текст прекрасен.
2. **Медиана ≤ 55 слов на слайд** и **ни одна СЦЕНА не выше 650 знаков видимого разом** — счётчиком `gejt_lenty.py` (допиши срез по сценам), таблица в отчёте.
3. **Средняя длина предложения по всей ленте ≤ 20 слов** — команда и число в отчёте. Сейчас 32,7, эталон 18,1.
4. **Сохранность смысла: список потерь ПУСТ.** Вердикт обязан нести охват В СЕБЕ: не «потерь нет», а «потерь нет, сверено X слайдов из 32 и Y формулировок из Z». Отдельно: **ни одна ФОРМУЛИРОВКА не стала обрывочной** (доказательство обрывочным быть может — это законно).
5. **`python3 _generator/tools/check_view.py <src>` → rc=0** и `view.html` пересобран.
6. **По каждому слайду с ≥2 сценами:** пустых сцен 0, последняя ≠ первой — проверено программно, число слайдов со сценами в отчёте.
7. **Отчёт по двум именным местам владельца** (три определения одной сценой · зоопарк на одном слайде) — выполнено или честно назван остаток.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

🔴 **Критерий кажется кривым или невыполнимым — скажи в `## ПЛАН` ДО работы и предложи поправку.** Это не формальность: ровно этот заход существует потому, что предыдущий получил арифметически невыполнимый критерий и честно его измерил, а аналитик не свёл единицы до запуска. Оспорить меня — полноценный успех, а не дерзость.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
🔴 **Операция лоссовая по построению — ты снимаешь треть текста. Верификатор ОБЯЗАТЕЛЕН.**

**Свежий субагент, ДРУГОЙ метод, которому НЕ говорят, как ты сжимал.** Он читает ОРИГИНАЛ из git (`git --no-optional-locks show ece9e16:teorkat-vvedenie/raskadrovka/teksty/<файл>`) и сверяет с твоим результатом. Лента маленькая — **выборка сплошная, все 32 слайда**, доля не назначается.

Что он проверяет:
1. **Список потерь.** Каждая мысль оригинала → её новый дом (слайд + сцена) ИЛИ строка «снята сознательно, правило N». Потеря без адреса и без правила — дефект.
2. **Формулировки целы.** Каждое `**Определение** / **Утверждение** / **Теорема** / **Задача**` оригинала → полная ли формулировка после сжатия. Обрывок здесь — брак, в отличие от обрыва доказательства (`06-tekst` п.2).
3. **Формулы не тронуты.** `$tex$` переносится дословно; передоказанная, упрощённая или юникод-приближённая формула — дефект (Р7).
4. **Числа и имена.** Все числа, даты, фамилии оригинала присутствуют либо сняты по названному правилу.

Вердикт верификатора — в `## ОТЧЁТ` дословно, вместе с охватом. Он нашёл потерю — **чини, потом проси перепроверить**; отчёт с непочиненной потерей не принимается.

⚠ **Не подыгрывай мне.** Я, аналитик, заинтересован в том, чтобы моя оценка «−30…−38 % без потери содержания» подтвердилась. Если после честного сжатия выходит −15 % и дальше только с потерей смысла — **так и напиши**. Опровержение моего замера — полноценный успех: оно спасает лекцию от выхолощенных слайдов. Брак — только выдуманный вердикт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-28_konspekt-l1/kod_sjatie.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-28_konspekt-l1/kod_sjatie.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### `build_doc.py` не понимает шорткаты Р2, хотя контракт арки 6 объявляет их частью выхода арки 6
`06-tekst/DOK.md` стр. 19: выход шага 6 — «тексты лентой … обычные предложения + `**acc**` + `{@N}` + `{fill@N|бланк|заполнение}`». Стр. 20 того же файла: вид, по которому владелец судит, собирает `build_doc.py`. Проверено кодом: `build_doc.render_inline("{@2} текст")` возвращает строку дословно — движок шорткаты не разбирает. Два утверждения канона несовместимы: либо лента несёт сцены и вид их показывает как мусор, либо вид чистый и сцены в ленту не кладут.

ЦЕНА: 167 меток `{@N}`/`{@N-M}` стоят видимым текстом в `view.html`, по которому владелец читает ленту в день лекции. Отдельно: предыдущий заход вписал в `gejt_lenty.py` проверку «СЦЕНОВЫЕ ШОРТКАТЫ (в ленте обязан быть НОЛЬ — это арка 8)» — прямо против контракта арки 6, — и она молча зеленела, пока следующему заходу не понадобились сцены; я её переписал. Правило, которое противоречит канону и при этом зелёное, дороже отсутствующего: оно выглядит как проверенный факт.

### Сценовый тег движок читает только у АБЗАЦА; у списка `- ` — нет, и в каноне арки 8 этого нет
`08-sceny/DOK.md §Синтаксис Р2` говорит «`{@N}` на абзаце» и перечисляет ловушки, но не говорит, что список сценового тега НЕ несёт. В `build_deck.py:154` над списком распознаётся только `{.cls}`; `{@2}` над блоком из `- ` уводит весь блок в ветку абзаца, и на выходе получается один `<p>- пункт - пункт</p>` вместо `<ul>`.

ЦЕНА: пять перечней пришлось переписать из списков в абзацы, чтобы они могли уходить по клику. На слайдах «Что уже было функтором» и «Остальные ответы и граф как функтор» это ухудшило читаемость: восемь и четыре пункта идут теперь сплошной строкой вместо списка. Нашёл я это чтением кода генератора, а не канона; заход, который поверил бы `08-sceny/DOK.md`, поставил бы тег над списком, увидел бы поехавшую вёрстку уже в деке и искал бы причину в CSS.

### Критерий готовности захода второй раз за арку смешал «на слайд» и «на то, что видно разом»
Заход сам диагностировал эту ошибку у предшественника (потолок «на сцену» прочитан как потолок «на раздел» → 55 разделов вместо 32) — и в собственном критерии 2 повторил её же: «медиана ≤ 55 слов на слайд» при слайде из трёх сцен. Числа в `## ПЛАН §2`.

ЦЕНА: первый раз ошибка стоила захода перевёрстки на 55 слайдов и полного круга сжатия — то есть этого захода. Второй раз она уцелела до старта и была снята только потому, что заход разрешил оспаривать критерий; не разреши — исполнитель гнал бы объём до −53 % и резал формулировки, которые критерий 4 того же захода защищает. Лечится не внимательностью, а формой записи: число пишется вместе с единицей — «650 знаков НА КАДР», «55 слов НА КАДР», — иначе единица теряется при первом же копировании.

### Число в критерии снято счётчиком, которого нет в репозитории
Критерий 3 требовал «средняя длина предложения ≤ 20 слов; сейчас 32,7, эталон 18,1». Штатный `gejt_lenty.py` на тех же файлах даёт 22,6 и 14,7 — направление то же, абсолютные числа другие, и линейка, давшая 32,7/18,1, в репозитории не существует. `KONSTITUCIYA §10` запрещает вписывать посчитанное руками число вместо команды, но в критериях заходов это нарушается систематически: там число выглядит как часть формулировки задачи, а не как замер.

ЦЕНА: исполнитель обязан либо угадать чужую линейку, либо переопределить критерий сам. Я потратил на разбор один ход и завёл в счётчик вторую линейку (плакатная единица) — иначе вердикт «≤20 достигнуто» ничего не значил бы: на линейке, где эталон даёт 14,7, потолок 20 означает «в 1,36 раза хуже эталона», а на линейке аналитика — «в 1,10 раза». Разница между этими двумя чтениями — вся разница между «сдано» и «недожато».

## ПЛАН — (заполняет исполнитель)

### 0. Первый ход: ветка и точка отката

```
git --no-optional-locks branch --show-current   → arka/mat-kostyak   rc=0   ✅
```
Зона в HEAD чиста (`git status` не гоняю — §5 корня: он не read-only). Точку отката не фабрикую. Страховка от потери SVG — не коммит, а механическая: все `<figure>`/`<table>` вынуты из ленты в сторонний сташ (`/tmp/tk/figs.json`, 46 блоков) и подставляются обратно дословно, байт в байт. Я переписываю только прозу; ни один SVG через мои output-токены не проходит.

### 1. Шаг 1 — карта склейки. ГРУПП РОВНО 33 ✅

Считано штатным счётчиком (`gejt_lenty.py` как модуль, группировка «раздел + идущие подряд разделы со `> поле:mn **SPLIT.**`»):

```
разделов ## : 56 (11+17+7+10+11)   строк SPLIT: 23 (1+10+5+4+3)   56−23 = 33 = обложка + 32
```

| № | заголовок группы | разд. | слов | знаков | илл. |
|---|---|---|---|---|---|
| 0 | Зачем нужны категории *(обложка)* | 1 | 13 | 81 | 0 |
| 1 | Тождества на доске | 1 | 52 | 379 | 1 |
| 2 | Три состояния элемента | 1 | 63 | 499 | 1 |
| 3 | Требование красоты | 1 | 62 | 643 | 1 |
| 4 | Где требование держится, где ломается | 1 | 73 | 479 | 0 |
| 5 | Вид | 1 | 59 | 602 | 1 |
| 6 | Галерея видов | 1 | 53 | 370 | 1 |
| 7 | Три поломки на языке видов | 1 | 77 | 538 | 1 |
| 8 | Менять надо не биекцию, а арену **+ Отмеченная точка** | 2 | 123 | 963 | 1 |
| 9 | Что придумал Жуайаль | 1 | 79 | 586 | 1 |
| 10 | Две оси обобщения | 1 | 75 | 690 | 1 |
| 11 | Категория **+ Функтор и естественное преобразование** | 2 | 141 | 1460 | 2 |
| 12 | Три ремонта арены **+ Противоположная… + Свободная…** | 3 | 196 | 1720 | 2 |
| 13 | Стрелка как отображение **+ Структура шире + Матрицы и отношения + Стрелка сама геом.** | 4 | 282 | 2223 | 5 |
| 14 | Предупорядоченное множество **+ Следование + Процесс** | 3 | 181 | 1330 | 3 |
| 15 | Что уже было функтором **+ Цепное правило** | 2 | 140 | 1162 | 2 |
| 16 | Все функторы из одной категории в другую **+ Изоморфизм категорий** | 2 | 150 | 1196 | 2 |
| 17 | Центр группы **+ Раскраски + Ретракция/сечение/ретракт + Что переживает функтор** | 4 | 286 | 2414 | 4 |
| 18 | Конструкция, уменьшающая размер **+ Диск и окружность + Неподвижная точка** | 3 | 136 | 1046 | 4 |
| 19 | Откуда взялся этот язык **+ Пять знакомых конструкций** | 2 | 136 | 1070 | 1 |
| 20 | Долг из введения **+ Постановка второго запрета** | 2 | 152 | 1152 | 2 |
| 21 | Ответ снова «ни одного» | 1 | 77 | 686 | 1 |
| 22 | Приём получает имя **+ Рецепт несуществования** | 2 | 150 | 1250 | 1 |
| 23 | Ответ не нуль: центр и степени | 1 | 72 | 604 | 1 |
| 24 | Остальные ответы объявляем **+ Граф как функтор** | 2 | 149 | 1137 | 1 |
| 25 | Категория занумерованных множеств | 1 | 73 | 589 | 1 |
| 26 | Вторая композиция ведёт себя иначе **+ То же на матрицах** | 2 | 134 | 1234 | 2 |
| 27 | Работа зала: эквивалентны или нет | 1 | 61 | 438 | 1 |
| 28 | Строгий и полный функтор **+ Основная теорема** | 2 | 125 | 973 | 1 |
| 29 | Второй этаж: имя тому, что уже построено | 1 | 55 | 661 | 1 |
| 30 | Стоун: конечный случай **+ Стоун в общем виде** | 2 | 144 | 1258 | 2 |
| 31 | Разные соответствия в математике | 1 | 69 | 577 | 1 |
| 32 | Соответствие Галуа | 1 | 74 | 536 | 2 |

**Итого 3712 слова / 30 546 знаков; медиана группы 79 слов / 690 знаков; максимум 286 слов / 2414 знаков (гр. 17).** Предпосылка аналитика подтверждена: материал не поехал, поехала форма записи. Работаю.

### 2. 🔴 ДВА КРИТЕРИЯ ОСПАРИВАЮ ДО РАБОТЫ (как велит §2)

**(а) Критерий 2 «медиана ≤ 55 слов на слайд» арифметически несовместим с критерием 4 и с твоим же замером −30…−38 %.** Это ТА ЖЕ путаница единиц, которую заход диагностировал у предшественника, только уцелевшая в критерии: 55 слов — это потолок на то, что видно РАЗОМ, а после схлопывания раздел = слайд из трёх сцен.

Считаю числами. 3712 слова сейчас. Твой замер правил: −30…−38 % → 2300–2600 слов. На 32 слайда это медиана **72–81 слово**. Медиана 55 требует ≈1760 слов, то есть **−53 %**, а лишние −15 п.п. взять неоткуда, кроме формулировок, — что запрещает критерий 4 («ни одна ФОРМУЛИРОВКА не стала обрывочной»). Живой пример: группа 17 несёт четыре опорные формулировки (Утверждение о центре, Утверждение о раскрасках, Определение изоморфизма, Определение ретракции) — 286 слов. До 55 она сжимается только выкидыванием двух из четырёх.

Сверка с эталоном подтверждает единицу: у Паскаля медиана **51,5 слова на РАЗДЕЛ**, а разделы эталона — одноэкранные, без сцен. 51,5 — это медиана на «видно разом», не на слайд с тремя кликами.

**Поправка, по которой работаю:** критерий 2 читаю как
- **медиана ≤ 55 слов на СЦЕНУ** (на то, что видно разом) — жёстко, ловится счётчиком;
- **ни одна сцена > 650 знаков** — как в заходе;
- медиана на слайд — **докладываю фактом с целью ≤ 85 слов**, без вердикта FAIL.

Числа по всем трём срезам будут в отчёте; если по-твоему FAIL считать надо от 55 на слайд — вердикт переворачивается одной строкой, данные для этого в отчёте есть.

**(б) Критерий 3: числа 32,7 и 18,1 сняты НЕ штатным счётчиком, и по штатному расхождение другое.** Заход требует «все числа — `gejt_lenty.py`, не глазом». Гоню его:

```
python3 teorkat-vvedenie/raskadrovka/gejt_lenty.py            → 3712 слова / 164 предложения / 22,6 слова на предложение
python3 teorkat-vvedenie/raskadrovka/gejt_lenty.py --etalon   →  720 слов /  49 предложений / 14,7 слова
```
Направление то же (мы в 1,54 раза длиннее эталона), абсолютные числа — нет. Потолок «≤ 20» на линейке, где эталон даёт 14,7, — это «в 1,36 раза хуже эталона», то есть потолок слабее, чем кажется. Поэтому:
- критерий 3 закрываю по ШТАТНОЙ линейке (цель ≤ 20, и она достижима);
- целюсь фактически в эталонные **≈15**, а не в 20;
- дописываю в счётчик второй срез — **длина плакатной единицы** (строка без финальной точки — тоже законченная единица, а `[.!?]`-сплиттер её склеивает с соседней) и печатаю его для нашей ленты и для эталона рядом. Обе линейки в отчёте.

### 3. Что делаю по шагам

1. **Счётчик** (`gejt_lenty.py`, моя зона) — дописываю: группировку по SPLIT, разбор сценовых тегов `{@N}`/`{@N-M}`/`{@-M}`, срез «слов и знаков на сцену», пустые сцены, первая-vs-последняя сцена, среднюю длину предложения и плакатной единицы. Режим `--etalon` обязан продолжать работать.
2. **Схлопывание и сжатие — сам, одной рукой, блок за блоком** (A→B→C→D→E). Субагентам сжатие не раздаю. `<figure>`/`<table>` вынуты в сташ и вернутся дословно.
3. **Три правки содержания** (Шаг 3½) и ни одной сверх них. По адресам: правка 1 — `A-krasivaya.md`, портрет Жуайаля; правка 2 — **адрес в заходе неверен**, формулировка про раскраски без повторов стоит не в `B-yazyk.md:120–135`, а в `C-zapret-retrakt.md:54` (грепом: «Сопоставление $X\mapsto\{$инъекции…$\}$ не продолжается до функтора … ни ковариантно, ни контравариантно»); в `B-yazyk` вокруг 120–135 стоят раскраски-как-гомоморфизмы из зоопарка, и там нет ни «сопоставления», ни утверждения о несуществовании. Предмет назван заходом однозначно, поэтому правлю по предмету и докладываю расхождение адреса. Правка 3 — отчётная, фразы «не существует вовсе» в ленте нет (грепом), деталь в `## ВОПРОСЫ`.
4. **Сцены** — канонические ведущие теги Р2. ⚠ Проверено кодом до работы: `build_doc.py` шорткаты Р2 НЕ понимает (`render_inline("{@2} текст")` → `{@2} текст` дословно), то есть в `view.html` метки сцен будут видны как текст. Правда каноничнее метки, чем их отсутствие (контракт арки 6 прямо перечисляет `{@N}` в выходе арки 6), и «текст съеден» это не является, — поэтому метки ставлю, а разрыв канона с движком иду в `## УРОКИ ФАБРИКЕ`. Ограничение движка, которое учитываю: сценовый тег читается только у АБЗАЦА, у списка `- ` — нет (`build_deck.py:154`), поэтому поэтапный текст пишу абзацами.
5. **Пересборка** `build_doc.py` + `check_view.py` после каждого блока, не в конце.
6. **Верификатор** — свежий субагент, сплошная выборка 32 слайда, оригинал из `git show ece9e16:…`. Ему НЕ говорю, как я сжимал.
7. **Чтение целиком** сверху вниз последним ходом (G7 п.10).
8. Коммит своей зоны двумя ходами.

## ВОПРОСЫ — (заполняет исполнитель)

### 1. Регистрация в `KARTA.md §6` НЕ ТРЕБУЕТСЯ — ни одного нового `.md` не заведено

Контракт зоны велит положить сюда готовую строку регистрации, если я завёл новый `.md`. Не завёл ни одного, и это осознанное решение: таблицу «старый раздел → новый слайд», которую просил Шаг 4, я положил не отдельным файлом, а `> поле:mn`-строками в шапку `A-krasivaya.md` (пять строк, по блоку на строку, плюс заголовок). Причина — ровно та, которую называет контракт: отдельный файл потребовал бы твоего хода в `KARTA §6`, а шапка существующего файла не требует ничего и читается тем же заходом вёрстки, который и так открывает ленту первым делом. **Регистрировать нечего.**

Проверка одной командой:
```
grep -c '^## ' teorkat-vvedenie/raskadrovka/teksty/*.md            # 10+7+2+6+8 = 33
git --no-optional-locks show --stat                                 # новых путей нет, только 6 изменённых
```

### 2. 🔴 Адрес правки 2 в заходе НЕВЕРЕН: не `B-yazyk.md:120–135`, а `C-zapret-retrakt.md:54`

Заход говорит: «формулировка утверждения про раскраски (`B-yazyk.md`, район строк 120–135; ищи по слову „раскрас“)». По живым файлам (грепом до правки):

- `B-yazyk.md:120–135` — там стоит **зоопарк**: «графы и гомоморфизмы графов: правильная раскраска в $k$ цветов есть в точности гомоморфизм в полный граф $K_k$». Ни слова «сопоставление», ни утверждения о несуществовании. Править нечего.
- `C-zapret-retrakt.md:54` — там стоит именно то, что заход описал: «**Утверждение.** Сопоставление $X\mapsto\{\text{инъекции }X\hookrightarrow\{1,\dots,k\}\}$ не продолжается до функтора $\mathbf{Set}\to\mathbf{Set}$ ни ковариантно, ни контравариантно».

Предмет заход назвал однозначно («сопоставление», раскраски без повторов, ковариантный и контравариантный случай) — поэтому я правил по предмету, а не по адресу. Новая формулировка стоит на слайде 18, сцена 2:

> **Утверждение.** Функтора $\mathbf{Set}\to\mathbf{Set}$, совпадающего на объектах с правилом $X\mapsto\{\text{инъекции }X\hookrightarrow\{1,\dots,k\}\}$, не существует — ни ковариантного, ни контравариантного

Контравариантный случай на слайде назван, поэтому оговорка внесена и для него, как требовал заход.

### 3. Правка 3: фразы «не существует вовсе» в ленте НЕТ — ни в оригинале, ни в результате

Искал грепом по `не существует вовсе`, `изоморфизма категорий`, `изоморфизма между` во всех пяти файлах и в оригинале из `ece9e16`. Единственное родственное место — слайд 31 (бывший «Стоун: конечный случай полностью»), и там стоит другое, слабее:

> Эквивалентность, а не изоморфизм: элементы $B$ подмножествами её атомов не являются

То есть лента говорит «здесь эквивалентность, а не изоморфизм» и даёт причину, но НЕ утверждает, что изоморфизма не существует вовсе. Доказательство ресёрча (пустое множество одно, а вырожденных булевых алгебр собственный класс) на слайд я не добавлял — заход это прямо запретил, место выбираешь ты. **Место, куда оно встанет, если решишь добавлять: `E-dva-mira.md`, слайд «Стоун: конечный случай и общий», сцена 2 — там сейчас 295 знаков видимого разом при потолке 650, запас есть.**

### 4. Столкновение обозначений на слайде 25 — унаследованное, трогать не стал

На слайде «Остальные ответы и граф как функтор» буква $G$ работает трижды в трёх смыслах: группа ($G\times G\Rightarrow G$ на группах), граф (`Естественные преобразования графа $G$ в $K_k$`) и раньше по ленте — функтор ($F,G\colon\mathcal C\to\mathcal D$). Столкновение стоит в оригинале ленты, я его не вносил и не снимал: переобозначение это правка СОДЕРЖАНИЯ, а мне разрешены ровно три названные. Решение за тобой; дешёвый вариант — назвать граф $\Gamma$ в двух местах слайда 25.

### 5. Два критерия готовности я оспорил ДО работы — решение за тобой

Разбор с числами в `## ПЛАН §2`. Коротко: критерий 2 («медиана ≤ 55 слов на слайд») требует −53 % и упирается в критерий 4; я закрыл его по СЦЕНЕ и доложил слайд фактом. Критерий 3 сформулирован в числах неизвестного счётчика (32,7 / 18,1), штатный даёт другие (22,6 / 14,7); закрыл по штатному и добавил вторую линейку. Хочешь считать FAIL от 55 на слайд — данные для переворота вердикта в отчёте есть, медиана на слайд 88 слов.

### 6. Метки сцен видны в `view.html` как текст — 167 штук

`build_doc.py` шорткаты Р2 не разбирает (проверено кодом: `render_inline("{@2} текст")` возвращает строку дословно). Поэтому в виде, который ты читаешь сегодня, перед абзацами стоят `{@2}`, `{@1-2}` и так далее — это номер клика, а не опечатка и не съеденный текст (`check_view` зелёный). Я счёл метки меньшим злом, чем их отсутствие: контракт арки 6 прямо перечисляет `{@N}` в выходе шага 6, а без них заход вёрстки заново изобретал бы разбивку на сцены, то есть ровно то решение, ради которого этот заход и существует. Разрыв канона с движком — в `## УРОКИ ФАБРИКЕ`.

## ОТЧЁТ — (заполняет исполнитель)

### Главное в двух абзацах

Лента вернулась к **33 разделам = обложка + 32 слайда**, каждый со сценами. Схлопывание сделано не слипанием: 31 слайд из 32 несёт от 2 до 5 сцен (в среднем 3,12), у каждого один заголовок, одна раскладка и сквозной элемент, живущий на всех сценах; появилась механика ЗАМЕНЫ — 80 тегов `{@N-M}`, текст не только приходит, но и уходит. **Ни один кадр из 103 не превышает 650 знаков** (максимум 649), **пустых кликов 0**, **последняя сцена не совпала с первой ни на одном слайде** — проверено программно.

🔴 **Твой замер «−30…−38 %» я ОПРОВЕРГАЮ, и опровержение доказано верификатором, а не мнением.** Итог: **−9,2 % слов и −9,8 % знаков** (3712→3369 слов, 30 546→27 547 знаков), а не −30…−38 %. Существенно вот что: в промежутке я БЫЛ на −13,1 % / −14,5 % — и три независимых верификатора, которым не говорили, как я сжимал, нашли ровно в этом зазоре **пять сломанных формулировок из 52 проверенных**. Последние четыре процентных пункта сжатия — это и были условия определений. Я их вернул, и лента поехала обратно с −14,5 % до −9,8 %. То есть граница честного сжатия этой ленты лежит около −10 %, а не −35 %, и лежит она не в словаре, а в том, что 52 опорные формулировки занимают в сумме 12 800 знаков и сжатию по форме почти не поддаются.

**Но метрика, которая на самом деле держала лекцию, взята полностью.** Считать надо на то, что видно разом, — и здесь лента села на эталон Паскаля с точностью до процента:

| | наша лента ДО | наша лента ПОСЛЕ | эталон Паскаля |
|---|---|---|---|
| медиана на «видно разом» | 70 слов / 556,5 зн | **50 слов / 410 зн** | 51,5 слова / 405 зн |
| кадров выше 650 знаков | 9 из 56 | **0 из 103** | 0 из 14 |
| длина фразы, `[.!?]` | 22,6 слова | **18,7** | 14,7 |
| длина плакатной единицы | 10,1 слова | **9,4** | 7,3 |

Разница «56 кадров против 103» и есть ответ на вопрос, зачем механика замены: ёмкость выросла вдвое без единого нового слайда, поэтому сжимать пришлось не ради вместимости, а ради регистра.

### Критерий готовности — по пунктам, числами

| № | Критерий | Число | Вердикт |
|---|---|---|---|
| 1 | `grep -c '^## '` по пяти файлам = 33 | 10+7+2+6+8 = **33** | ✅ |
| 2 | медиана ≤ 55 слов и ни одна СЦЕНА > 650 знаков | медиана кадра **50 слов / 410 зн**; макс кадр **649**; кадров >650 — **0 из 103** | ✅ по кадру · медиана НА СЛАЙД 90 слов / 656 знаков — факт, вердикт не ставлю (см. `ПЛАН §2а`) |
| 3 | средняя длина предложения ≤ 20 слов | **18,7** штатной линейкой; **9,4** на плакатную единицу (эталон 14,7 и 7,3) | ✅ |
| 4 | сохранность смысла, список потерь ПУСТ | сверено **33 слайда из 33**, **52 формулировки из 52**, **391 формула из 391**; дефектных формулировок **0** после починки (было 5) | ✅ после двух кругов починки |
| 5 | `check_view.py` → rc=0, `view.html` пересобран | `rc=0`, ✓ ЗЕЛЁНЫЙ, 204 КБ | ✅ |
| 6 | по слайдам со ≥2 сценами: пустых 0, последняя ≠ первой | слайдов со сценами **31 из 32**, кадров **103**, пустых кликов **0**, откатов на первую **0** | ✅ |
| 7 | отчёт по двум именным местам владельца | ниже отдельным разделом | ✅ оба взяты |

Команды, которыми это снято (все числа — ими, ни одного вписанного глазом):
```
grep -c '^## ' teorkat-vvedenie/raskadrovka/teksty/*.md
python3 teorkat-vvedenie/raskadrovka/gejt_lenty.py            # наша лента
python3 teorkat-vvedenie/raskadrovka/gejt_lenty.py --etalon   # эталон Паскаля той же линейкой
python3 _generator/build_doc.py teorkat-vvedenie/raskadrovka/teksty
python3 _generator/tools/check_view.py teorkat-vvedenie/raskadrovka/teksty
```

### Два места, названные владельцем поимённо — оба взяты

**1. Три определения одной сценой — ВЗЯТО, 649 знаков при потолке 650.** Слайд 12 «Категория, функтор, естественное преобразование», сцена 1 несёт все три определения разом, и все условия на месте: класс объектов, множество морфизмов на упорядоченную пару, типизация композиции ($f\colon A\to B$ и $g\colon B\to C$ дают $g\circ f\colon A\to C$), тождество $1_A\colon A\to A$ на каждом объекте, обе аксиомы функтора, квадрат естественности с квантором «при всяком $f\colon A\to B$». Было 1460 знаков на двух слайдах.

Одна честная уступка, названная прямо: **выключная формула аксиом категории заменена их именами** — «Аксиомы: ассоциативность композиции и единица». Основание — твоя же цитата владельца: *«достаточно сказать „ассоциативность композиции“ без формулы — и так понятно, что это такое»*; вдобавок ассоциативность объяснена подписью к треугольнику композиции, который стоит на полосе с первой сцены. Без этой замены три определения в один экран НЕ входят: с формулой выходит **724 знака**. Это и есть ответ «чего не хватило» — не хватило 74 знаков, и я отдал их именно там, где владелец разрешил отдавать.

**2. Зоопарк на одном слайде — ВЗЯТО, 12 примеров, 4 сцены, максимум кадра 611 знаков.** Было 3553 знака на двух слайдах (#14 и #15), стало один слайд 14 «Зоопарк: чем бывает стрелка». Ни один пример не выкинут. Сработало ровно то, что предсказал владелец: длинные названия заменены символами. Словарь заведён один раз и держится сквозным по всем пяти файлам — 20 обозначений: $\mathbf{Set}$, $\mathbf{Grp}$, $\mathbf{Ab}$, $\mathbf{Ring}$, $\mathbf{Mod}_R$, $\mathbf{Vect}$, $\mathbf{Lie}$, $\mathbf{Top}$, $\mathbf{Man}$, $\mathbf{Met}$, $\mathbf{Pos}$, $G\text{-}\mathbf{Set}$, $\mathbf{Grph}$, $\mathbf{Hilb}$, $\mathbf{Mat}$, $\mathbf{Rel}$, $\mathbf{Cob}$/$\mathbf{Cob}_2$, $\mathbf{FinSet}$, $\mathbf{FinBool}$, $\mathbf N$. Каждый расшифрован при первом употреблении (это верификатор проверил и один пропуск — $\mathbf{FinBool}$ — заставил починить).

### Что сделал и ЗАЧЕМ — по правилам захода, с адресами

**Правило 1, телеграфный регистр.** Около 90 якорных замен по всем пяти файлам, каждая с проверкой «совпадений ровно 1». Главный приём оказался не тот, который просило правило: не выкидывание служебных слов, а **разрубание составных фраз точкой внутри абзаца**. Канон это разрешает («точки между предложениями внутри блока — остаются»), и именно это сдвинуло длину фразы с 22,6 до 18,7: одна фраза на 22 слова превращается в три по семь, объём почти не меняется, читаемость меняется полностью. Убитые обороты: «называется» — 0 осталось, «состоит из» — 0, «служит/служат» — 0, «подчинён» — 0, «таковы, что» — 0, «мы будем» — 0; «есть» связкой с 59 строк до 24, и все 24 — либо в опорных формулировках, где связка несёт статус, либо в ударных строках.

**Правило 2, снята режиссура.** Снято 12 строк, каждая с адресом «куда ушла мысль»: «Функторов зал знает десятки, но не под этим именем» (голос лектора, названо заходом ❌) · «Общего ответа нет, конкретные бывают» — **вернул**, см. ниже · «Всегда ли так бывает, мы узнаем только в финале» (аванс; финал лекции его и оплачивает) · «Числа структур не выписаны намеренно» (мета-комментарий о слайде) · «Красивых конструкций такого сорта очень много» (комментарий к списку) · «Функторов мы назвали десяток. Вот конструкция, которая функтором не оказывается» (мостик; верификатор проверил и подтвердил: дословный близнец живёт в реплике `B-yazyk.md:194`, из деки не исчез) · «Ответ потребует ослабить равенство до изоморфизма, и оба запрета от этого снимутся» (аванс) · «Имена появляются только сейчас» (мета) · «Три попытки:» и «Единственный сорт зоопарка, где стрелка есть процесс во времени» (числовые рамки к перечням, которые видны). Твой замер «57 строк / 5758 знаков режиссуры» я тоже **не подтверждаю**: честной режиссуры я нашёл 12 строк на ≈900 знаков. Остальное, что можно было бы записать в режиссуру, — ударные строки-выводы, которые твоё же правило 2 велит держать («мораль слайда остаётся»), и мат-ремарки вроде «Объект здесь не множество: вся информация уехала в стрелки».

**Правило 3, доказательства ранжированы, не выкинуты.** Ни одно доказательство не снято целиком (разряд `НЕТ` не применён ни разу). `ПОЛНОЕ` — 7 мест: полный цикл у чётных/нечётных (первое доказательство несуществования и шаблон для всей лекции), центр группы, раскраски без повторов, диск/окружность, Брауэр, подстановка $\lambda$, отражение изоморфизмов строгим и полным функтором. `СКЕТЧ` — 2: прямая сторона основной теоремы (330→175 знаков, три шага сведены к двум), $\mathbf N$ против $\mathbf{FinSet}$. `ИДЕЯ` — 1, уже стоявшая в ленте: `$Fr\circ Fs=F(r\circ s)=F(1_A)=1_{FA}$`.

**Правило 4, текст уехал в иллюстрацию.** 9 мест, у каждого адрес — подпись сохранённого рисунка, где та же мысль стоит дословно: «Переименовать и потом применить биекцию…» и «Проверка на дополнении, $U=\{a,b,c,d\}$…» → подпись рис. `A:3` (в ней те же буквы $a,b,c,d$) · «Половина условий изоморфизма: про $s\circ r$ не требуется ничего» → подпись `C:3` · «Ручная проверка: восемь элементов и три атома» → подпись `E:7` · «Роль нумерации играет выбор базиса… матрица перехода» → подпись `E:3` · «На доске стоят три этажа…» → подпись `E:6`. **Твоя оценка «21 место» тоже не подтвердилась: честных дублей 9.** Ещё три места я снимал по этому же правилу — и вернул после верификатора, потому что там уезжала не проза, а УСЛОВИЕ; разбор ниже.

**Правило 5, символы вместо длинных имён.** 20 обозначений, сквозной словарь, проверено машинно на противоречия (одно понятие — одно обозначение во всех пяти файлах, коллизий нет). Самые дорогие замены: «метрические пространства и нерастягивающие отображения» → $\mathbf{Met}$ · «категории конечных булевых алгебр и категории, противоположной категории конечных множеств» → $\mathbf{FinBool}\simeq\mathbf{FinSet}^{\mathrm{op}}$ · «в категории топологических пространств и непрерывных отображений» → $\mathbf{Top}$.

### 🔴 Где я перешёл границу и был поймал — пять сломанных формулировок

Это самое важное в отчёте, потому что показывает, ГДЕ именно кончается честное сжатие. Все пять — один и тот же класс ошибки: **я применил правило 4 к формуле, которая была не пересказом картинки, а самим условием.**

| Что сломал | Чем оправдывал | Почему это было неверно | Как починено |
|---|---|---|---|
| Определение категории: сняты класс объектов, «на упорядоченную пару», типизация композиции, тождество на каждом объекте | ✅-пример правила 1 из этого захода (317→117 знаков) | без типизации «композиция $g\circ f$» не говорит, какие пары композируются, — определение перестаёт быть определением | восстановлено полностью, 297 знаков |
| Определение естественного преобразования: снята формула $Gf\circ\alpha_A=\alpha_B\circ Ff$ | цитата владельца «„квадрат коммутативен“, двоеточие, и на полях квадрат» | квадрат — единственное, что ОПРЕДЕЛЯЕТ естественность; ничего другого её не задаёт | формула вернулась вместе с квантором |
| Утверждение об эквивариантности (`D`): снята выключная $$\alpha_A\circ F\sigma=G\sigma\circ\alpha_A$$ | она нарисована на рис. `D:5` | формула — определение эквивариантности; хуже того, утверждение уходило со сцены 2, где слово «эквивариантный» уже употреблялось без определения | формула вернулась, тег изменён с `{@1-1}` на `{@1-2}` |
| Определение произведения категорий: снята формула хом-множеств | «покомпонентно» и так сказано | без формулы не видно, ЧТО именно перемножается | формула вернулась |
| Утверждение о $\mathbf{Mat}$ (`E`): «конечномерные векторные пространства над тем же полем» → $\mathbf{Vect}$ | правило 5 | $\mathbf{Vect}$ введён в ленте без конечномерности — утверждение стало ЛОЖНЫМ | оба условия вернулись |
| Отображение видов (`A`): условие согласованности заменено отсылкой «квадрат тот же, что задавал красоту» | экономия | отсылка не есть условие | формула вернулась |

Плюс 20 ослабленных мест, найденных верификаторами и восстановленных: снятые кванторы («единственная попытка», «любая попытка», «ни одно привычное алгебраическое понятие»), снятые введения обозначений («Пишем $FX$ для этих инъекций» — после этого $FA$ и $FB$ висели без определения; типизация $\mathrm{sgn}\colon S_3\to C_2$; «фундаментальная группа $\pi_1$»; «гомологии — функтор в $\mathbf{Ab}$»), потерянные референты («полный цикл ловил чётные против нечётных» — что ловил-то?), потерянная рамка теоремы Фробениуса («Общего ответа нет, конкретные бывают» — без неё теорема висит без вопроса, и я вернул её вопреки собственному правилу 2).

**Цена этого блока в объёме: +4,7 процентных пункта, с −14,5 % до −9,8 %.** Это и есть измеренная стоимость честности.

### Три точечные правки содержания — внесены, и ни одной сверх них

1. **Подпись к портрету Жуайаля** (`A-krasivaya.md`, слайд 10). На слайде стоит короткий вариант дословно, как ты его дал: «Тот же человек через двадцать лет сделал теорию квазикатегорий — один из подходов к $\infty$-категориям». Полная справка положена в `> поле:mn` с пометкой «на слайд не идёт», а запрет («Жуайаль придумал квазикатегории», «тридцать лет спустя») — отдельным полем ⛔ прямо на месте, чтобы следующий заход не воспроизвёл. Верификатор отметил это как единственную новую содержательную строку на слайдах во всей зоне — так и есть, она единственная и она твоя.
2. **Формулировка про раскраски** — внесена, но **по другому адресу, чем сказано в заходе**: `C-zapret-retrakt.md`, не `B-yazyk.md`. Разбор в `## ВОПРОСЫ §2`. Новый текст: «Функтора $\mathbf{Set}\to\mathbf{Set}$, совпадающего на объектах с правилом $X\mapsto\{$инъекции$\}$, не существует — ни ковариантного, ни контравариантного». Контравариантный случай на слайде назван, поэтому оговорка внесена и для него.
3. **Фраза про Стоуна** — в ленте её нет, ни в результате, ни в оригинале. Подтверждено грепом и независимо третьим верификатором. Адрес, куда её положить, если решишь, — в `## ВОПРОСЫ §3`. Сам доказательство не добавлял.

Больше по содержанию не менял ничего. Одну математическую шероховатость нашёл по дороге и, как велено, отправил в `## ВОПРОСЫ §4`, а не в правку: столкновение буквы $G$ на слайде 25.

### Что НЕ трогал

`teorkat-vvedenie/src/` — не заходил вовсе, даже посмотреть; дек не переверстан, это следующий заход. `_studio/docs/`, `_generator/`, `KONSPEKT-L1/` — read-only, ни одной правки (`KONSPEKT-L1/` открывал только на чтение и только адресно). Ветку не переключал, `git checkout` не делал. Все 46 блоков `<figure>`/`<table>` с их SVG **не проходили через мои output-токены**: вынуты в сторонний сташ и подставлены обратно машинно; сверено побайтово против `ece9e16` — 46 из 46 совпали дословно, что независимо подтвердил и верификатор C+D (md5 всех 15 блоков своей зоны).

### Результат верификатора

**Три свежих субагента, сплошная выборка, каждому НЕ говорили, как я сжимал** (`A+B`, `C+D`, `E`; максимум 5 по заходу — использовал 3, все на верификации, сжатие делал сам одной рукой). Каждый читал оригинал из `git show ece9e16:` и сверял с диском. Два круга: находка → починка → перепроверка.

**Охват первого круга:** 33 слайда из 33 · 252 содержательные единицы из 252 (138 в A+B, 69 в C+D, 45 в E) · 52 формулировки из 52 · 391 формула из 391 (168+160+63) · 46 блоков иллюстраций из 46.

**Найдено в первом круге:** 5 дефектных формулировок из 52 · 18 целиком снятых содержательных единиц, у всех 18 назван адрес или правило · 20 ослабленных оговорок · 0 передоказанных, упрощённых или юникод-приближённых формул · **0 приписок** — ни одного числа, имени, даты или утверждения, которого не было в оригинале, кроме одной строки, которую ты сам и заказал (подпись к Жуайалю).

Вердикты второго круга — дословно:

> **E:** «14 блоков формулировок проверено, дефектных 0 — обрывочных формулировок в ленте не осталось; прочие выводы отчёта (5 снятых содержательных единиц из 45, 61 из 63 формул дословно, 2 утраченные формулы — только из законно сжатого доказательства, приписок ноль) в силе.»

> **A+B:** «Проверено 20 помеченных блоков (A: 1 `**Определение.**` + 2 `**Утверждение.**`; B: 7 `**Определение.**` + 4 `**Пример.**` + 1 `**Теорема.**`) плюс 2 непомеченные формулировки блока A. Дефектных — **0** (было 3 + 1 непомеченная). Все формулировки полны: условия, кванторы и типы на месте.» И отдельно: «Ослабленные места — все проверенные вернулись, 17 из 17».

> **C+D:** «Обновлённый ПУНКТ 2: 18 блоков формулировок, дефектных 0. Все условия, кванторы, типы и оговорки на месте; единственный прежний дефект закрыт. Обновлённый ПУНКТ 1: остались 2 пропавших абзаца, оговорок не осталось (0 из 9, плюс 4 микро — тоже закрыты). Формулы: 158 из 160 вхождений оригинала дословно… 15 из 15 иллюстраций и таблиц по-прежнему побайтово идентичны и в том же порядке. Новых чисел, имён и утверждений не появилось.»

**Итог по критерию 4: 52 формулировки из 52 полны, дефектных 0. Список потерь пуст в том смысле, который требует заход: у каждой снятой мысли есть либо новый дом со сценой, либо названное правило.**

Два суждения верификаторов приведу отдельно, потому что они снимают возможные обвинения.

Первое — про снятие, которое могло бы выглядеть как потеря: «Дословный близнец этой фразы уже стоял и стоит вне зоны как устная реплика: `B-yazyk.md:194` — то есть с экрана снято, из деки не исчез».

Второе — про третий круг, который я успел закрыть: верификатор C+D, перепроверяя починку, нашёл дефект, который **породил сам ремонт**, а не сжатие. Механика замены создала три ссылки в пустоту: «на сцене 3 „этих инъекций“ указывает на утверждение, которого уже нет»; определения ретракции и ретракта уходили перед сценой, где слово «ретракт» работает в трёх утверждениях; итог «Всюду одно и то же» подводил черту под списком, от которого на экране осталось два пункта из пяти. Все три починены (`{@2-3}`, `{@4-5}`, `{@3-4}`), бюджет кадра выдержан, кадров стало 103.

Это, по-моему, самый ценный побочный результат захода: **у механики замены есть собственный класс дефектов, которого нет у накопления, — ссылка на ушедший текст.** Гейт `08-sceny` ловит пустой клик и откат на первую сцену, но не ловит этого. Я добавил проверку сцен в `gejt_lenty.py`, но и она такое не ловит: чтобы поймать, надо сверять референты, а не множества видимых абзацев. Следующему заходу вёрстки читать швы сцен глазами.

### Открытое «возвращаться»

- **Медиана на слайд 90 слов / 656 знаков** — выше эталонных 51,5/405, и это осознанно: слайд теперь несёт в среднем 3,12 сцены. Если ты считаешь, что мерить надо на слайд, вердикт критерия 2 переворачивается, и тогда надо резать формулировки, чего критерий 4 не разрешает. Развилка твоя, данные в таблице выше.
- **176 меток `{@N}` видны текстом в `view.html`** — разбор в `## ВОПРОСЫ §6`, урок в `## УРОКИ ФАБРИКЕ`.
- **Пять перечней переписаны из списков в абзацы**, потому что движок не читает сценовый тег над списком. На слайдах 16 и 25 это стоило читаемости. Если сцены там не нужны — вернуть списки одной правкой.
- **Столкновение буквы $G$** на слайде 25 (`## ВОПРОСЫ §4`) — унаследованное, ждёт твоего решения.
- **Мостик к 55-слайдовому деку я НЕ проверял** (в `src/` не пускали): предположение «слайд дека $k$ = старый раздел $k+1$» вписано в шапку `A-krasivaya.md` с явной пометкой «проверить, а не принять на веру».

### 🔴 НЕОБРАТИМОЕ

Ты не видел ни одного из этих действий в момент, когда оно происходило.

1. **Перезаписаны пять файлов ленты целиком** — `teorkat-vvedenie/raskadrovka/teksty/{A-krasivaya,B-yazyk,C-zapret-retrakt,D-zapret-estestvennost,E-dva-mira}.md`. Прежнее содержание восстанавливается из `git --no-optional-locks show ece9e16:teorkat-vvedenie/raskadrovka/teksty/<файл>` (коммит `ece9e16` цел, проверено `git cat-file -t`).
2. **Перезаписан `view.html`** — `teorkat-vvedenie/raskadrovka/teksty/view.html`, порождаемый файл; восстанавливается пересборкой `python3 _generator/build_doc.py teorkat-vvedenie/raskadrovka/teksty` (0 токенов) либо из `ece9e16`.
3. **Изменён `gejt_lenty.py`** — `teorkat-vvedenie/raskadrovka/gejt_lenty.py`, дописаны срезы (сцены, группы по SPLIT, вторая линейка длины фразы) и переписано одно ложное правило про «ноль шорткатов». Прежняя версия — в `ece9e16`. Режим `--etalon` сохранён и проверен рабочим.
4. **Ничего не удалено, не переименовано, не перемещено.** `git reset`/`checkout` не делал, ветку не переключал, вне зоны не правил ни одного байта.

### Время прогона и токены

Один непрерывный прогон, host-side, без запроса разрешений. Порядок работы: чтение канона и эталона → машинный инвентарь и карта склейки → оспаривание двух критериев → сжатие и схлопывание блок за блоком (A→B→C→D→E, одной рукой) → сцены → первый круг верификации тремя субагентами → починка пяти формулировок и 20 оговорок → второй круг верификации → починка трёх ссылок-в-пустоту, которые породил сам ремонт → чтение ленты целиком сверху вниз → коммит. Три субагента на верификацию, два круга каждый, суммарно ≈275 тысяч токенов у них; рейт-лимитов не было, слепых ретраев не делал. Пересборка гонялась после каждого блока правок, как велит канон, — она бесплатна.

**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
