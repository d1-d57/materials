# Канал исполнителя — upakovka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — механики здесь нет: почти всё задание требует решения «как перепаковать», а не «где заменить строку».

🔴 **МЕТРИКА ЭТОГО ЗАХОДА — КАЧЕСТВО, НЕ СКОРОСТЬ И НЕ ЭКОНОМИЯ ТОКЕНОВ.** Владелец дословно: *«чтобы он не в смысле потратил миллион токенов, запустив сто субагентов, а наоборот — эффективно, спокойно, аккуратно, вдумчиво сделал бы эту работу на тысячу процентов, прыгнул выше головы»*. ⇒ **думай долго над каждым решением о перепаковке; субагентов заводи мало и по делу.** Часы работы — норма. Механический прогон по списку правок — брак: половина задания состоит в том, чтобы найти лучшее расположение, а не исполнить найденное.

КОНТЕКСТ. Курс «Зачем нужны категории», лекция 1 (~2 часа, обзорная, экспозиционная, зал — сильные первокурсники и взрослые с разным бэкграундом). Есть читаемый конспект `teorkat-vvedenie/KONSPEKT-L1/` — 12 вкладок, 137 унаследованных блоков + новые, собирается `build_doc.py`. Прошлый этап (`kod_perekomponovka.md`, коммит `7c363d0`, **принят**): расставлены типы слайдов и оценки в минутах, перевёрнуто введение, вычистки по разбору владельца. Итог того захода: **48 слайдов / 178 минут против рамки 120**.

🔴 **ЦЕЛЬ ЭТОГО ЗАХОДА — УПАКОВКА, А НЕ РЕЗКА.** Владелец в трёх местах разговора сказал одно и то же: *«мы вообще сейчас не стараемся что-то убирать… если что, спрячем в комментарий, потом достанем»*; *«не за счёт того, что мы удаляем информацию, а наоборот — за счёт того, что мы находим, как его расставить»*; и образ, который объясняет задачу лучше любого критерия: *«мы как бы шары в ящик кладём: если их правильно расположить, оно как мозаика друг в друга входит, лучше прилегает — объём не меняется, но больше помещается»*.
⇒ **Целевая цифра — около 40 содержательных слайдов вместо 48, и ни один факт при этом не удалён.** Достигается перегруппировкой, сценами внутри слайда и правильной единицей счёта, а не сокращением материала. Владелец: *«если получится сузить до 40 слайдов, я бы считал, что это хороший результат»*.

Приёмка — по ОТЧЁТУ, без построчной сверки. Если остановишься до цели: сделай шаги 1–3 полностью (они дают правильную систему счёта), остальное честно назови несделанным. Лучше три шага доведённых, чем семь начатых.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/KONSPEKT-L1/ _studio/zhurnal/2026-07-28_konspekt-l1/kod_upakovka.md _generator/build_doc.py. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/KONSPEKT-L1/ _studio/zhurnal/2026-07-28_konspekt-l1/kod_upakovka.md _generator/build_doc.py` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: **все 12 файлов `teorkat-vvedenie/KONSPEKT-L1/*.md`** (это предмет работы) · `_studio/zhurnal/2026-07-28_konspekt-l1/kod_perekomponovka.md` секции `## ОТЧЁТ` и `## ВОПРОСЫ` (что было сделано прошлым заходом и какие вопросы остались) · `_generator/build_doc.py` (только чтобы найти строку 228 для §2 шага 8). **Больше ничего.** Всё, что нужно знать о лекции, вписано в этот заход — он самоценен. `SPEKA.md`, картотеку, `INTERVYU-narrativ.md`, `TZ-perekomponovka.md` **не читай**: их содержание уже перенесено сюда в нужном объёме, а чтение съест контекст, который нужен на думание.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

Восемь шагов. Шаги 1–3 меняют систему счёта и обязаны быть сделаны первыми — без них остальные шаги нечем мерить. Шаги 4–6 — собственно упаковка, это главная работа и на неё уходит основное время. Шаги 7–8 — гигиена.

---

### ШАГ 1. 🔴 ЗАМЕНИТЬ ШКАЛУ МИНУТ НА ДВЕ ОСИ: ЖАНР И ЦЕНА

Прошлый заход пометил каждую секцию `· ТИП · N мин`, где минуты — редакторская оценка на глаз. **Владелец эту калибровку отверг, и его довод содержательный, а не вкусовой:**

> *«Калибровка по времени — супер неочевидная вещь. Условно говоря, сколько мне дашь времени, столько я и буду рассказывать: дашь слайд, скажешь на него три минуты — расскажу за три минуты; скажешь минута — расскажу за минуту; скажешь десять — расскажу за десять. Поэтому это условность. Нужно не по времени калибровать, а по ИДЕЙНОСТИ. Вот эта важная мысль — мы на ней долго останавливаемся. А вот это неважно: например, про якобиан мы говорим за секунду, это не то чтобы какая-то мысль, которую надо донести, это просто момент узнавания знакомой конструкции, микро-вау-эффект. По статусу нужно разделять это.»*

⇒ **Первичные данные — не минуты. Минуты становятся ВЫЧИСЛЯЕМОЙ величиной, а не вписанной.** Это же требует `KONSTITUCIYA §10` (число не пишут — пишут команду, которая его считает), и прошлый заход нарушил его наполовину: итог он считал, а слагаемые вписывал руками.

🔴 **И вот главное, из-за чего первая попытка шкалы провалилась при разборе с владельцем: осей ДВЕ, а метка была одна.** Жанр куска (что это математически) и цена (что мы с ним делаем в зале) — **независимы**, и склеивать их в одно слово нельзя. Проверочный случай, на котором это вскрылось: **Фробениус по жанру — теорема, а по цене — «называем и идём дальше»**; в одномерной шкале для такой клетки слова не было, и он ошибочно попал в «перебивку».

**ОСЬ 1 — ЖАНР:** `ОПР` (определение) · `УТВ` (утверждение, теорема) · `ПРИМЕР` · `КОНСТР` (конструкция: произведение категорий, противоположная, скелет) · `ИСТОРИЯ` (портрет, научпоп-сюжет).
Жанр **не участвует во времени**. Он нужен для баланса (три определения подряд — сигнал) и для матрицы примеров (шаг 5).

**ОСЬ 2 — ЦЕНА, три ступени и ноль. Только она даёт время:**

| цена | что происходит в зале | вклад |
|---|---|---|
| **ВЫВОДИМ** | зал работает: мысль строится при нём, есть доказательство или вопрос в зал | 4–5 мин |
| **ПОКАЗЫВАЕМ** | предъявляем готовое и объясняем, зал смотрит и понимает | 2 мин |
| **НАЗЫВАЕМ** | произносим и идём дальше; момент узнавания, не мысль | 0,3–0,5 мин |
| **НЕ НА СЛАЙДЕ** | на слайде не появляется вовсе, произносится устно, если спросят | 0 |

**Разметка на живых кусках — это калибровка, сверяйся с ней:**

| кусок | жанр | цена |
|---|---|---|
| якобиан: цепное правило функториально | ПРИМЕР | НАЗЫВАЕМ |
| Фробениус: классификация функторов | УТВ | НАЗЫВАЕМ *(формулируем, не доказываем)* |
| $V$ против $V^*$ — разбираем | УТВ | ВЫВОДИМ |
| определение комбинаторного вида | ОПР | ВЫВОДИМ *(владелец выводит его из требования красоты)* |
| определение категории | ОПР | ПОКАЗЫВАЕМ *(даётся готовым)* |
| категория кобордизмов — упоминание в списке | ПРИМЕР | НАЗЫВАЕМ |
| функтор из кобордизмов | ПРИМЕР | ВЫВОДИМ |
| критерий эквивалентности, прямая сторона | УТВ | ВЫВОДИМ |
| построение $G$ (обратная сторона) | УТВ | упражнение, см. ниже |
| Жуайаль, портрет | ИСТОРИЯ | ПОКАЗЫВАЕМ |
| аксиома выбора | УТВ | НЕ НА СЛАЙДЕ |

**Вопрос владельца, который эта сетка снимает:** *«как ты определение оценишь и сравнишь с какой-нибудь важной девятиминутной теоремой?»* — **жанры не сравниваются, сравниваются только цены.** Одно и то же определение стоит пять минут или две в зависимости от того, выводим мы его или даём готовым; это не свойство определения, а наше решение о подаче.

🔴 **И вторая поправка архитектуры, важнее первой: метки вешаются на БЛОК, а не на слайд.**
- **блок** — единица материала (определение, утверждение, пример). У него есть жанр и цена.
- **слайд** — группа блоков, отвечающих на ОДИН вопрос (шаг 2). Его цена **складывается** из блоков и потому вычисляется, а не назначается.
- **сцена** — способ развернуть слайд во времени (шаг 3). Единицей счёта не является.

Это структурно убивает абсурд прошлого захода: «список из одного пункта» становится невозможен, а слайд с пятнадцатью примерами на `НАЗЫВАЕМ` честно считается как ~5 минут, а не как катастрофа.

**Сходимость, которую стоит держать в голове как проверку:** владелец хочет смысловые куски по 3–5 минут и «чем меньше, тем лучше»; 40 слайдов × 3 мин = 120 минут. Все три числа, которые вечер считались по-разному, сходятся именно здесь.

🔴 **Два следствия, каждое обязательно:**

1. **СПИСОК как метка ИСЧЕЗАЕТ.** Список — форма подачи (несколько пунктов на слайде), а не свойство куска. Отсюда абсурд прошлого захода: во вкладке 4 стоят пять «СПИСКОВ» по одному пункту.
2. **УПРАЖНЕНИЕ как метка ИСЧЕЗАЕТ**, и вот точное определение владельца, по которому теперь размечается:
   > *«У нас не бывает упражнений. У нас бывают утверждения. С точки зрения математики упражнение — то же самое утверждение. С точки зрения лекции это просто то, что мы НЕ доказываем: доказываем — не упражнение, не доказываем — упражнение. Естественно, слайд не может быть посвящён тому, что мы не доказываем. Это должна быть приписка внизу. Это мало места, это строка. Можно даже без полной формулировки: „полная классификация — упражнение 5 в листочке“.»*
   ⇒ упражнение — **строка внизу слайда**, не секция и не слайд. Формулировка может быть неполной со ссылкой на листок.

**Слайд, все блоки которого имеют цену `НЕ НА СЛАЙДЕ`, слайдом не считается** — вердикт владельца: *«реплика не считается слайдом, очевидно, не считается»*. В прошлом счёте четыре таких попали в 48.

**Что сделать физически:**
- у каждого **блока** — метка `ЖАНР/ЦЕНА` (форму выбери сам: поле, суффикс метки блока — главное, чтобы разбиралась грепом и не ломала вид);
- у каждой **секции** (= слайда) хвост `· ТИП · N мин` из заголовка **убрать полностью**: и тип, и минуты больше не данные, они вычисляются снизу;
- команда (там же, где нынешняя «Команда 1» в служебной секции `12-miry.md`) переписывается и печатает: **число слайдов** (секции, где все блоки `НЕ НА СЛАЙДЕ`, не в счёт) · **разбивку блоков по жанрам и по ценам** · **вычисленное время лекции** и время по каждой вкладке.

---

### ШАГ 2. 🔴 ГЕЙТ СКЛЕЙКИ: ЕДИНИЦА ЗАДАЁТСЯ ВОПРОСОМ

Правило гранулярности, сформулированное владельцем (он поправил формулировку аналитика, и поправка существенная):

> *«Цепное правило — это функториальность. Это тоже не кусок, понимаешь? Важно, что там три подряд заголовка типа „вот это — функториальность“, и мне кажется, что это надо давать на одном слайде в одном блоке.»*

⇒ **Кусок задаётся ВОПРОСОМ, на который он отвечает, а не утверждением, которое в нём стоит.** Цепное правило, якобиан, $\pi_1$, гомологии — разные ответы на один вопрос «что уже было функтором» ⇒ **один слайд, список внутри**. Разные вопросы — разные слайды, даже если материал соседний.

**Что сделать:**
1. Над каждой секцией основного яруса выписать **вопрос**, на который она отвечает. Вопрос пишется полем `> вопрос: ...` первой строкой секции (используй уже существующий в конспекте механизм полей `> поле:`; если он для этого не годится — заведи ближайшую форму и назови её в отчёте).
2. **Написать команду-гейт**, которая печатает пары соседних секций с совпадающим (с точностью до формулировки — сверяй смысл сам, команда печатает пары для глаза) вопросом. **Такие пары — брак разбиения: склеиваются в одну секцию.**
3. Прогнать гейт и склеить всё, что он показал. Ничего при склейке не теряется: пункты становятся пунктами списка внутри слайда, раскрытия уходят в свёрнутые плашки по месту (механизм уже работает, 30 плашек).

⚠ **Обратная сторона правила, и она важнее прямой.** Владелец предупредил ровно про неё: **склейка не означает обеднение**. Наоборот — там, где вопрос один, список должен стать **богаче**, а не короче. См. шаг 4.

---

### ШАГ 3. СЦЕНЫ — ОДИН ИЗ МЕХАНИЗМОВ, НЕ ПАНАЦЕЯ

🔴 **Сначала — граница, поставленная владельцем, потому что без неё этот шаг вредит.**
> *«Надо понимать, что даже со сценами не так много может быть. Не может быть такого, что слайдов мало, а сцен очень много… Это не панацея, естественно.»*

⇒ **Настоящая задача не в сценах.** Она такая:
> *«Нам надо понять, как разбить это на разумного, примерно одинакового размера куски материала, которые логично выстраиваются в историю. И каждый из них — это законченная история. Это слайд.»*

⇒ **Критерий слайда: примерно одинаковый размер (3–5 минут) и законченность — слайд можно пересказать как отдельную маленькую историю.** Сцены — способ развернуть такой слайд во времени, чтобы не шокировать зал всем сразу; они не единица счёта и не оправдание тому, чтобы утрамбовать в слайд лишнее. Замер владельца для калибровки: во вчерашней лекции про треугольник Паскаля было ~15 слайдов и больше 30 сцен, и сцены там были простые — «три абзаца появляются по очереди».

Механизм придумал владелец, описывая вкладку 6:

> *«У тебя перед глазами должны быть примеры, и эти конструкции, и их аналогичность прямо должно быть видно. Если одна ретракция пропадает, на её месте появляется другая — прямо на том же самом месте. И то же самое происходит. Оно не должно быть разными слайдами, оно должно быть разными СЦЕНАМИ.»*

⇒ **Сцена — единица внутри слайда.** Один слайд, один визуальный якорь, на месте которого последовательно сменяется содержимое. Три такта, которые были тремя слайдами, становятся **одним слайдом с тремя сценами** — и при этом читаются лучше, потому что аналогия видна глазами, а не держится в памяти.

**Что сделать:** там, где подряд идут секции с одинаковой структурой и разным материалом, свести их в одну секцию с явной разметкой сцен (`**Сцена 1.** …` новой строкой внутри блока — этот приём уже работает в конспекте, `build_doc.py:199` даёт каждой такой строке свой абзац внутри рамки). В отчёте назвать поимённо каждое место, где сцены применены, и сколько слайдов это сняло.

**Кандидаты, названные владельцем прямо** (не единственные — ищи ещё):
- **вкладка 6, три такта** (центр · раскраски · имя) — один слайд, три сцены. Владелец: *«это не три слайда… мне не кажется, что это 14 минут, это короткая вещь, минут семь»*, и отдельно: *«третий шаг бесплатный»* — общий признак формулируется даром, когда две сцены уже отыграны;
- **вкладка 1**, разбор одной биекции и требование — проверить, не одна ли это сцена-пара.

---

### ШАГ 4. ТОЧЕЧНЫЕ ВЕРДИКТЫ ВЛАДЕЛЬЦА — исполнить дословно

Каждый пункт — прямое решение, обсуждению не подлежит; в скобках только то, что решаешь ты.

**4.1. 🔴 Вкладка 4 — это слайд(ы) про ПРИМЕРЫ КАТЕГОРИЙ, и примеров должно стать втрое больше.**
> *«Их точно не пять. Их должно быть скорее пятнадцать, если не больше. Это как раз задача — найти интересный пример категории. И это должен быть слоган: вся математика — категории, даже то, что вы знаете со школы, это тоже категории.»*
- **Донабрать до ~15 примеров.** Это не «добавь строк» — это содержательная работа, думай.
- **Гранулярность внутри списка задана владельцем:** *«векторные пространства, модули, ещё что-то алгебраическое — они через запятую проносятся, это один пункт. А топологические пространства, многообразия — это другой пункт, и над ним хочется остановиться»*. ⇒ однородное семейство = один пункт; смена жанра = новый пункт.
- **Группировка в слайды:** *«стрелка-отображение и стрелка-геометрический объект — это одна пара, которая занимает слайд. А стрелка-следование и стрелка-процесс — дискретные категории, не похожие на две предыдущие, больше из логики»*. ⇒ **один слайд или два, решаешь ты по наполнению; пять — точно нет.**
- 🔴 **«Стрелка — элемент моноида» — под нож (в плашку, не удалять).** Владелец: *«элемент моноида — это вообще не пример категории, это просто конструкция, что бывает категория из одного объекта… идея, что бывают скучные категории. Это меня точно вызывает вопрос»*. То же отношение к путям в графе: произнести где-то можно, но как пример категории «не прикольно».

**4.2. 🔴 Вкладка 6 — порядок тактов НЕ ТРОГАТЬ.**
Владелец обдумывал перестановку (начать с раскрасок, центр — в упражнение) и **снял вопрос с повестки**: *«давай мы не будем это сильно сейчас фиксировать, давай не будем пока переставлять порядок, соберём как получится. Это всё не самый ключевой вопрос»*.
⇒ **порядок центр → раскраски → имя остаётся как есть.** Сжимать сценами (шаг 3) — да; переставлять — нет. Своих доводов на эту тему в отчёт не пиши, вопрос закрыт владельцем.

**4.3. Вкладка 11, «Основная теорема» (9 мин, самое длинное доказательство).**
Разбор аналитика по тексту: утверждение 125 (строгий и полный отражает изоморфизмы) — три строки, минута. Теорема 126 прямая сторона (эквивалентность ⇒ строг, полон, существенно сюръективен) — короткая и красивая. **Длинная только обратная сторона — построение $G$**, и именно там живёт аксиома выбора.
⇒ **125 и прямая сторона 126 остаются УЗЛОМ и разбираются. Построение $G$ — формулируется, доказательство уходит в свёрнутую плашку по месту.** Не удалять.
**Аксиома выбора — позиция владельца дословно:** *«я прихожу и рассказываю доказательство, не ссылаясь на аксиому выбора. С высокой вероятностью большинство скажет: да, конечно, логично. Кто-нибудь образованный спросит: а ты здесь не пользовался аксиомой выбора? — Пользовался. Спасибо за вопрос, я его ждал»*. ⇒ **`УТВ / НЕ НА СЛАЙДЕ`, как и стояло.** И отдельно: *«можно вообще убрать про то, как устроены ретракты в множествах, из первой части — это микрозаготовка, вернёмся на второй лекции»*. ⇒ разбор ретрактов в множествах во вкладке 6 сжать до заготовки с пометкой «подробно — на Л2».

**4.4. 🔴 Тождества вкладки 1 — три правки.**
- **Паскаль ОСТАЁТСЯ на слайде.** Прошлый заход свёл его к устной фразе — вердикт отменён. Владелец: *«Паскаля я бы оставил на слайде и поговорил бы про это»*.
- ⭐ **И содержательная поправка владельца, которая меняет смысл пункта:** *«А почему непонятно, кстати? Ведь понятно, как доказывать. Просто нужен отмеченный элемент, и всё. Если у тебя есть отмеченный элемент, то ты можешь брать подмножества, не содержащие отмеченный элемент — вот такой функтор. Отмеченный элемент решает Паскаля»*. ⇒ **формулировка «правая часть вообще не конструкция от $U$» неверна и должна уйти.** Паскаль не «не формулируется» — он **требует отмеченной точки**, ровно как остальные починяемые случаи, и потому он **позитивный вход в отмеченную точку**, а не тупик. Перепиши пункт под это.
- ⭐ **$3^n$ переписать двойной суммой** — прямой заказ владельца: *«давай $3^n$ поставим справа, а слева сделаем двойную сумму: выбираем $k$-элементное подмножество, внутри берём $l$-элементное. Не писать двойной знак суммы — написать „сумма по $k$ и $l$“. Только границы везде указывать»*:
  $$\sum_{0\le l\le k\le n}\binom nk\binom kl=3^n$$
  Разбираемая биекция та же (тройка состояний элемента), но теперь левая часть выглядит страшно, а ответ удивляет. Владелец: *«люди удивятся, откуда $3^n$ в комбинаторике»*.
- **Тождество $\sum_k\binom nk\binom kl=\binom nl2^{n-l}$ можно вернуть** в список: *«у нас нет требования экономии тождеств на первом слайде, мы же не будем все биекции разбирать»*. И шире — во вкладке 2 можно сделать **список тождеств разной сложности, их можно выписать много**.

**4.5. Вкладка 10 — НЕ вырезать.** Прошлый заход предлагал снять её целиком; владелец: *«наверняка не нужно вырезать целую вкладку. Мы не должны сейчас этим заниматься, мы должны крутым образом запаковывать существующий материал»*. Сжимать сценами и склейкой — да, вырезать — нет.

**4.6. 🔴 ВЕРНУТЬ ТЕОРЕМУ СТОУНА — прямой заказ владельца.**
> *«Теорему Стоуна мы убрали, а её надо вернуть.»*

Сейчас во вкладке 12 стоит только **конечный случай** (теорема 132: конечные булевы алгебры ≃ конечным множествам наизнанку). Общая двойственность Стоуна — булевы алгебры против стоуновых пространств (вполне несвязных компактных хаусдорфовых) — из конспекта пропала.
⇒ **Вернуть формулировку общей теоремы** сразу за конечным случаем, жанр `УТВ`, цена `ПОКАЗЫВАЕМ` или `НАЗЫВАЕМ` (доказательства нет и не будет — конечный случай для того и разбирается полностью, чтобы общий можно было просто назвать).
🎯 **И она оплачивает нить:** топологические пространства заявляются в зоопарке категорий (вкладка 4) и до сих пор нигде не платили. Стоун — их оплата, и это ровно тот тип моста, который заказан шагом 6. Назови эту связь в тексте.
⚠ Если по тексту окажется, что «убрали» относилось к чему-то другому, — восстанови общую теорему всё равно (она нужна сама по себе) и поставь пункт в `## ВОПРОСЫ`.

**4.7. Кобордизмы — два разных куска, не один.** Владелец: *«просто сказать, что есть такая категория — это тридцать секунд. А описать функтор из неё — это глубокая работа, наверное три минуты, может и пять. Это отдельный слайд, и я считаю, что стоит в него вложиться, потому что это интересно»*. ⇒ упоминание категории — пункт в списке примеров, `ПРИМЕР / НАЗЫВАЕМ`; функтор из кобордизмов — **свой слайд**, `ПРИМЕР / ВЫВОДИМ`, и его надо усилить, а не сжать. Цепное правило, наоборот, — `ПРИМЕР / НАЗЫВАЕМ`, 30 секунд.

---

### ШАГ 5. МАТРИЦА ПРИМЕРОВ — посчитать программно, выводы дать владельцу

Правило отбора примеров, собранное из двух ходов разговора. Пример держится, если выполнено **хотя бы одно**:
- **(а) повторяемость** — появляется в ≥2 местах лекции; владелец: *«лучше не в двух, а в трёх, даже может в четырёх»*;
- **(б) единственность жанра** — единственный представитель своего сорта, и потому даёт разнообразие: *«есть пример про поверхности, кобордизмы — он единственный геометрический; можно добавить танглы или ribbon graphs. Какой-нибудь пример, где алгоритмы являются объектами и морфизмами, типа правил вывода — он единственный совершенно по-другому устроенный»*.

**Что сделать:** написать команду, которая по всем 12 вкладкам строит таблицу «пример × в каких вкладках встречается», и вручную проставить каждому **жанр** (алгебраический · геометрический · топологический · дискретно-логический · вычислительный · комбинаторный). Таблицу положить в служебную секцию конспекта, чтобы она пересчитывалась.
**Выводы — в отчёт, а не в правки:** (1) примеры с одним появлением и уже представленным жанром — список кандидатов на понижение до плашки, **решение владельца, сам не режь**; (2) примеры, у которых стоит найти второе появление — *«придумать, как те же самые поверхности где-то ещё упомянуть»*; (3) жанры, представленные слабо, — куда донабирать.
⚠ Владелец про множества и векторные пространства: *«максимально мы стараемся на них больше всего сыграть»* — их повторяемость не обсуждается, она цель.

---

### ШАГ 6. ДОЛГОВАЯ МЕТРИКА И ПОРЯДОК — считать, а не спорить

Лекция держится нитями: каждая **заявляется** в одном месте и **платит** в другом. Нить, которая нигде не платит, — украшение. Формализация: **долговой пролёт** = расстояние в слайдах между заявкой и оплатой. Длинные пролёты — это места, где слушатель забыл заявку раньше, чем получил оплату; они читаются как перегрузка, даже когда материала немного.

**Что сделать:**
1. Проставить в конспекте поля `> долг: <имя нити> — заявка` и `> долг: <имя нити> — оплата` там, где это видно по тексту. Нити, которые уже есть в конспекте по смыслу: *каноничность всегда относительно чего-то* · *симметрии целой категории* · *нужна правильная упаковка (другая категория)* · *где прячется выбор* · *живёт на биекциях, ломается на всех отображениях* · *приём раньше имени* · *изоморфизм категорий → эквивалентность*. Найди в тексте их места сам; чего в тексте нет — не выдумывай, назови в отчёте.
2. Команда печатает по каждой нити: заявка, оплата, **пролёт в слайдах**, и суммарный пролёт по лекции.
3. **Найти перестановку, уменьшающую суммарный пролёт**, при жёстких ограничениях (определение раньше использования; порядок вкладок 1→12 как рассказ не ломать без причины). Перебор по блокам — секунды, но **решение о перестановке принимай головой, а не оптимизатором**: метрика показывает подозрительные места, оценку делаешь ты.
4. В отчёт: текущий суммарный пролёт, предложенные перестановки с числом и с объяснением **что от этого станет лучше для слушателя**. Крупные перестановки (перенос между вкладками) — **предлагай, не делай**: это решение владельца.

---

### ШАГ 7. ВИЗУАЛЬНАЯ ПРОГРАММА

Прямой заказ владельца: *«должны быть эти визуальные образы, картинки, коммутативные диаграммы, ещё какие-то — побольше визуала должно быть. Он умеет это делать. Следующая версия должна уже походить на крутой рабочий конспект лекции»*.

1. 🔴 **Несущий визуальный образ всей лекции — РАСТУЩИЙ КВАДРАТ, утверждён владельцем.** Один и тот же коммутативный квадрат проходит лекцию насквозь с всё более богатыми обитателями: **вкладка 1** — переименование элементов (штрихи $a\to a'$) · **вкладка 6** — функториальность · **вкладки 9–10** — естественность · **вкладка 11** — квадрат, который коммутирует **с точностью до изоморфизма**. ⇒ рисуй их **в одной геометрии, одном кегле, одной композиции**, чтобы узнавание работало без слов. Момент во вкладке 11, где требование «коммутирует на носу» ослабевает, — визуально главный переход лекции, и он должен быть виден.
2. **Диаграммы и иллюстрации — на поля** (`figure.mn`). Прошлый заход выяснил рабочий критерий: на поле встаёт только фигура с h/w ≥ 0,6, старая геометрия `420×160` даёт 0,38 и потому выглядела мелкой с длинными стрелками. Держи новую геометрию (~250×200, 300×200) и явный кегль меток.
3. **Донарисовать там, где сейчас текст вместо картинки.** Минимум: определение вида (владелец: *«в определении вида не должно быть ничего, кроме картинки»*) · кобордизмы · цепное правило (сфера, касательное пространство) · сцены вкладки 6 на общем якоре.
4. **Портреты стоят плейсхолдерами `🖼`** — изображения ищет владелец, плейсхолдеры не трогай. Но правило размещения владелец назвал, и оно относится к тексту, а не к картинкам:
   > *«Не обязательно, чтобы все люди появлялись по нескольку раз за лекцию, но это прикольно. Портрет должен быть в ОДНОМ месте. А если есть ссылка на человека в разных местах — это здорово, это один из вариантов, как простроить мосты внутри лекции. И можно где-то на полях это указывать: это не нужно в основном тексте.»*
   ⇒ **портрет — один раз; упоминания имени — сколько угодно, и они идут КРОСС-ССЫЛКАМИ НА ПОЛЯ** (`> поле:mn`), вперёд и назад, не в основной текст. Это тот же механизм, что для примеров в шаге 5, и работает он на ту же цель — сшивание лекции.
   **Готовый случай, который надо провести:** Эйленберг появляется дважды — аксиомы гомологий в зоопарке функторов и естественные преобразования на стыке C｜D. Между ними две вкладки, и связь сейчас нигде не отмечена. Поставь кросс-ссылку на поле в обоих местах.

---

### ШАГ 8. ЗАКАЗ ДВИЖКУ — одна строка, разрешена владельцем

`_generator/build_doc.py` **входит в зону только ради этой правки**. Строка 228: `label = "%s %s%s." % (kw, num, name)` → форма **без `num`**, то есть номер блока уходит из **отображаемой** метки, оставаясь в источнике и в якоре. Диагноз прошлого захода принят владельцем: спотыкание вызывал не сам номер, а **два перемешанных ряда** в отображении (201, 202, 2, 203, 204, 6, 205).
🔴 **Больше в `_generator/` не трогать ничего.** Якорь на строке 230 берёт номер отдельно и не должен пострадать; автоссылки «по утверждению 207» на строке 287 — тоже. После правки пересобрать и прогнать оба гейта; сломалось что-то ещё — откати правку и напиши в отчёт.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — печатается командами, не утверждается словами):**
1. `build_doc.py` → RC=0 и `check_view.py` → RC=0.
2. Команда меток печатает: **блоков основного яруса без пары `ЖАНР/ЦЕНА` — 0** · **вхождений старых меток (`РАЗБОР`, `СПИСОК`, `ПЕРЕБИВКА`, `РЕПЛИКА`, `УПРАЖНЕНИЕ`, `· N мин` в заголовках) — 0**, шкала заменена целиком · **слайдов — ≤ 42**, цель 40 · вычисленное время лекции напечатано (не вписано).
3. Гейт склейки печатает: **пар соседних секций с одинаковым вопросом — 0**; **секций без поля `> вопрос:` — 0**.
4. Гейт охвата печатает: **потеряно из 137 унаследованных блоков — []**, дублей номеров — 0. Новые блоки названы поимённо в отчёте.
5. Примеров категорий во вкладке 4 — **не менее 15** (печатается счётом пунктов).
6. Матрица примеров и долговая таблица **существуют как команды в конспекте и печатаются** (RC=0), а не как вписанный руками текст.
7. В отчёте отдельным списком: сколько слайдов сняла каждая мера (склейка / сцены / реплики), **и ни одного удалённого факта** — всё «убранное со слайда» лежит в свёрнутых плашках по месту.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
🔴 **Нужен обязательно: заход лоссовый по построению** — склейки, сцены, перенос на поля и в плашки двигают почти весь текст.

Свежий субагент, ДРУГОЙ метод: сверка **абзацами и предложениями** через `git --no-optional-locks show HEAD:<файл>`, не по номерам блоков (гейт по номерам слеп к прозе, полям, флагам ⚑ и командам — прошлый заход это оплатил, потеря нашлась только у верификатора). Сверять отдельно: абзацы · поля `> поле:` · флаги `⚑` · пункты упражнений · bash-команды. Список потерь обязан быть ПУСТ, охват назван числом.

**И вторая задача верификатора, которой в прошлый раз не было:** проверить не сохранность, а **осмысленность разметки** — взять 15 случайных блоков и независимо, не глядя на проставленную пару `ЖАНР/ЦЕНА`, назначить свою; расхождения выписать поимённо. Цена — теперь главные данные конспекта, из неё считается всё время лекции, и если она расставлена формально, все выводы считаются по мусору.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/KONSPEKT-L1/ _studio/zhurnal/2026-07-28_konspekt-l1/kod_upakovka.md _generator/build_doc.py                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/KONSPEKT-L1/ _studio/zhurnal/2026-07-28_konspekt-l1/kod_upakovka.md _generator/build_doc.py   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
