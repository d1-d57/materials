# Канал исполнителя — final (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — правки по живому деку, суждение на каждой.
> 🔴🔴🔴 **ЛЕКЦИЯ НАЧИНАЕТСЯ ЧЕРЕЗ ~20 МИНУТ. ЭТО САМЫЙ ПОСЛЕДНИЙ ХОД.**

КОНТЕКСТ. Лекция «Зачем нужны категории», онлайн, **начинается вот-вот**. Дек: `teorkat-vvedenie/src/dist/index.html`. Владелец только что просмотрел его глазами и продиктовал правки.

🔴 **ТВОЁ ТЗ ЦЕЛИКОМ — `_studio/zhurnal/2026-07-28_konspekt-l1/PRAVKI-final.md`.** Читай его первым и целиком, он короткий. Там 20 пунктов по слайдам с дословными словами владельца и **готовый приоритет исполнения в конце файла**.

ЦЕЛЬ: **успеть сделать сверху вниз столько, сколько успеешь, и пересобрать дек.**

⚠ **Параллельно мог ещё работать заход `kod_chistka.md`** (лента + `src`). Первым ходом проверь `git status` по зоне: если там чужая незакоммиченная работа — **не затирай её**, работай поверх и скажи в отчёте.

Приёмка — по ОТЧЁТУ.
🔴 **СТОП-ПРАВИЛО: за 5 минут до конца твоего времени — брось всё, пересобери дек и закоммить.** Несобранный или сломанный дек = сорванная лекция. Недоделанные правки = мелочь.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_final.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_final.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА — исполнять приоритет из `PRAVKI-final.md`, снизу файла

**Читать только:** `PRAVKI-final.md` (ТЗ) и ленту `teorkat-vvedenie/raskadrovka/teksty/*.md`. Больше ничего — времени нет.

**Порядок (он же в конце ТЗ, повторяю здесь, чтобы не потерялся):**

1. 🔴 **Починить сцены, где текст ПРОПАДАЕТ, а должен накапливаться** — слайды **17** (определение центра исчезает), **20** (доказательство исчезает), **22** (текст первой сцены исчезает на второй), **29** (верхняя часть исчезает). Это делает слайды нерабочими на сцене, и это дефект №1. **Скорее всего болезнь общая — проверь ВСЕ слайды со сценами одним проходом и почини разом.**
1а. 🔴 **Поменять местами слайды 26 и 27** (часть 4): эквивалентность идёт ПЕРЕД «изоморфизма нет». Слайд 27 свести в ОДНУ сцену: определение сверху, доказательство снизу. **Слайд 28 снять.** Подробно — `PRAVKI-final.md §21`.
2. 🔴 **Переставить слайд Эйленберга–Маклейна** — он стоит 19-м, должен идти **после 16, перед 17**. Перестановка, не написание.
3. **Снять слайды 23 и 24.**
4. **Убрать непонятные картинки пачкой** — слайды 5, 10, 11, 12, 13, 14, 18 (первая), 20 (первая и вторая), 22. Удаления быстрые, делай одним проходом.
5. **Слайд 17 — переделать в диаграмму** (§13 ТЗ: сцена 1 — утверждение и определение центра формулой; сцена 2 — короткая фраза + диаграмма $\mathbb Z_2\to S_3\to\mathbb Z_2$; сцена 3 — вторая строка диаграммы; выводы НЕ писать, лектор скажет голосом).
6. **Слайд 18 — ужать текст Брауэра** до трёх строк (§15 ТЗ), полосу сделать вертикальной справа.
7. Остальные пункты ТЗ — по остатку времени, сверху вниз.

🔴 **НЕ НАЧИНАТЬ (владелец доскажет голосом):** новый слайд про инъекции · переписывание слайдов 13 и 14 · эквивалентность категорий в части 2.

**Пересборка обязательна:** `build_doc.py` → `porodit.py` → `sverstat.py` → `build_deck --lint` → `build_deck` → `promer.py`. Переполнение появилось — `podognat.py`. **Кадры смотреть только у тронутых слайдов.**

---

**КРИТЕРИЙ ГОТОВНОСТИ:**
1. 🔴 **Дек СОБРАН и открывается:** `build_deck --lint` rc=0, `dist/index.html` существует и непуст, секций числом. **Это главный и единственный обязательный пункт.**
2. Переполнений 0 (`promer.py`).
3. Сцен, где текст пропадает вместо накопления, — **0 на слайдах 17, 20, 22** (проверить попиксельно, `sceny_gejt.py`).
4. Сделанные пункты ТЗ — списком с номерами; **несделанные — тоже списком**, чтобы владелец знал, что говорить голосом.

**Отрицательный вердикт несёт ОХВАТ:** «сделано X пунктов из 20».

🔴 **ГЛАВНОЕ: лучше пять правок и рабочий дек, чем двадцать правок и сломанная сборка.** Время вышло — коммить и сдавай немедленно.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_final.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_final.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Заход назвал ленту «что читать», но не сказал, что `src/content/`, `src/slides/`, `src/shablon.html` и `slide_order` в `brief.md` — ПОРОЖДАЕМЫЕ

Заход дал конвейер строкой (`build_doc → porodit → sverstat → build_deck`) и раздел «Читать только: ТЗ и ленту». Из этого не следует, что `porodit.py` **перезаписывает** `content/*.md`, а `sverstat.py` — `slides/*.html`, `shablon.html` и `slide_order` в `brief.md`. Исполнитель с пустым контекстом видит в `src/` обычные файлы под git, правит их (это и есть «источник», судя по имени папки) — и теряет всё на первом же шаге конвейера, который сам же заход и велел запустить.

ЦЕНА: правки сделаны и затёрты дважды. Первый заход `porodit.py` снёс содержательные правки в 7 файлах `content/` (дефекты сцен на трёх слайдах, ужатие слайдов 17 и 18, перенос «композиции», снятие «Определение»). Второй заход `sverstat.py` снёс раскладку в 9 файлах `slides/`, `shablon.html` и — самое дорогое — перестановку Эйленберга–Маклейна и снятие двух слайдов в `slide_order`, то есть приоритеты 2 и 3 ТЗ целиком. Всё пришлось делать заново в ленте. Ушло ~20 минут при бюджете ~20 минут до лекции — прямая причина того, что визитка (§1, «железно обязательно») и шесть пунктов картинок остались несделанными. Лечится одной строкой в шаблоне захода: «`src/content`, `src/slides`, `src/shablon.html`, `slide_order` — ВЫХОД `porodit`/`sverstat`; правь ленту, иначе твоя работа исчезнет».

### Снятие или перестановка слайда сдвигает все id ниже, а ручная доводка «по глазам» привязана к id — и молча уезжает на чужой слайд

`tools/plotnost.json`, `tools/reflow.json`, `PRAVKI` и `KARTINKI` в `tools/sverstat.py` ключуются по `sNN`. `sNN` — не имя раздела, а его ПОЗИЦИЯ. Убрал слайд — и каждая оплаченная глазами правка ниже наводится на соседний раздел, без единого предупреждения: `sverstat` печатает «ручных правок по глазам (PRAVKI): 3» и остаётся зелёным, потому что правки применились — просто не туда.

ЦЕНА: после снятия слайдов 23 и 24 запись `reflow.json:"s21"` («Категория занумерованных множеств») уехала, и слайд остался без перетекания по сценам — вернулось переполнение **+182px** на слайде, который предыдущий заход довёл до нуля. `podognat.py` исчерпал все 6 ступеней плотности и починить не смог, выдав «КАНДИДАТЫ НА SPLIT». Я почти сдал дек с переполнением, списав его на часть 2 и на то, что split — вне зоны. Спасло только то, что я полез читать `reflow.json` глазами; после перенаведения двух ключей стало 0 из 33 без всякого split.

ЦЕНА, накопленная, и это главное: в `sverstat.py` УЖЕ стоят два комментария о тех же граблях — `s52→s30` («запись висела на несуществующем id и молча не применялась ни к чему») и `s15→s14` от захода `kod_chistka.md` с ценой **+351px**. То есть это третий раз за двое суток, каждый раз ловится вручную и каждый раз задним числом. Правило есть, краснеть нечему — по `KONSTITUCIYA §11` это не правило, а надежда. Нужен гейт: `karta.py` знает соответствие id → название раздела, и проверка «каждый ключ `PRAVKI`/`reflow.json`/`plotnost.json`/`KARTINKI` указывает на раздел с тем названием, для которого правка писалась» ловится программно. Без него следующий рез слайда оплатит те же грабли в четвёртый раз.

### Номера слайдов в ТЗ владельца устарели раньше, чем ТЗ дочитали, — потому что рядом работал заход, менявший СОСТАВ дека

`PRAVKI-final.md` продиктован по деку в 19:20, `kod_chistka.md` запущен в 19:15 и сливал разделы. К моменту чтения ТЗ нумерация сдвинулась на 2–4, и ТЗ честно предупреждало об этом только про слайды 12–14 («сначала посмотри ЖИВОЕ состояние»), тогда как сдвинулись ВСЕ номера от 5 и ниже.

ЦЕНА: прямое исполнение «починить слайд 17» попало бы в `s17.md` = $BG$ и естественные преобразования — слайд, про который владелец сказал «прикольно, всё хорошо, содержание не трогать». То есть заход сломал бы верный слайд и не починил ни одного дефекта из приоритета 1. Опознание всех 15 адресатов по содержанию стоило ~6 минут из двадцати. Лечится тем, что ТЗ по живому деку адресует слайды **названием раздела**, а не номером: `porodit.py` печатает таблицу «id — блок — название», названия при перекомпоновке не плывут. Пока номера остаются в ТЗ, шаблон захода обязан требовать таблицу-карту «номер владельца → файл, опознан по …» первым пунктом плана — у меня она случайно получилась, потому что я не поверил номерам, а не потому, что заход её потребовал.

## ПЛАН — (заполняет исполнитель)

### 🔴 ЛОЖНАЯ ПРЕДПОСЫЛКА ТЗ: номера слайдов в `PRAVKI-final.md` НЕ совпадают с живыми файлами

Заход велел проверить зону — в ней **незакоммиченная работа `kod_chistka.md`**: `s29` удалён, `s12`–`s28` переписаны, разделы про функторы слиты. **Нумерация сдвинулась.** Владелец диктовал по деку ДО чистки. Прямое исполнение «правь слайд 17» попало бы в `s17.md` = $BG$/естественные преобразования — то есть в слайд **21** владельца, который он велел НЕ трогать («прикольно, всё хорошо»).

Поэтому слайды опознаны **по содержанию**, не по номеру. Карта (владелец → файл):

| Владелец | Файл | Опознан по |
|---|---|---|
| 5 | `s04` | «Примеры видов», списком |
| 6 | `s05` | «Проверим: какие из тождеств — изоморфизмы видов?» |
| 10 | `s08` | категория/функтор/естественное преобразование |
| 11 | `s09` | три блока `{.op-def} Определение` (владелец: «убрать слово Определение») |
| 12 | `s10` | «Категории: множество со структурой», **ровно 5 панелей** = «все пять картинок» |
| 13 | `s11` | «стрелка как следование и как шаг» (предпорядки), **3 панели** |
| 14 | `s12` | функторы, **2 панели** |
| 17 | `s13` | $Z(G)$, $s\colon\mathbb Z_2\to S_3$, `sgn` |
| 18 | `s14` | Брауэр, ретракция $D^2\to S^1$ |
| 19 | `s15` | Эйленберг и Маклейн, 1942/1945 |
| 20 | `s16` | «Есть ли естественный изоморфизм $\mathrm{Id}\Rightarrow(-)^\ast$?» |
| 21 | `s17` | $BG$, компонента = элемент $x$ |
| 22 | `s18` | «Классификационные результаты, списком», хроматический многочлен |
| 23 | `s19` | общий рецепт/комментарий |
| 24 | `s20` | лист упражнений |

Совпадение числа панелей с продиктованным числом картинок (5 / 3 / 2) — независимое подтверждение карты. Слайдов 15 и 16 владельца отдельными файлами больше нет: чистка слила их в `s12`. Значит «после 16, перед 17» = **сразу перед `s13`**, то есть после `sl-divider2` — что попутно и лечит «раздел „чего не бывает“ не читается как раздел».

### Механика дефекта сцен — найдена

`{@N-M}` = приходит на сцене N, **гаснет на сцене M** (`porodit.py:218`, движок ставит `scene-off` при `k >= until`). Значит `{@1-1}` на двухсценном слайде = «показать на первой, убрать на второй» — это ровно то, что владелец видел как «пропадает». Лечится заменой `{@1-1}` → `{@1-2}` (доживает до конца слайда). Это и есть дефект №1, и он механический, а не переписывание текста.

### Порядок работы (по приоритету ТЗ, со стоп-правилом)

1. Дефекты сцен `{@1-1}` → `{@1-2}`: `s13` (центр), `s14`, `s16`, `s18`. Гейт — `sceny_gejt.py`.
2. Перестановка `s15` (Э–М) в `brief.md` → сразу после `sl-divider2`, перед `s13`.
3. Снять `s19`, `s20` из `slide_order`.
4. Картинки пачкой: `s04` board снять, `s08` последняя панель, `s09` rail снять, `s10` все 5, `s11` все 3, `s12` обе, `s14` первая (остаток — вертикальная полоса справа), `s16` первая и вторая, `s18` rail снять.
5. `s13` — в диаграмму (§13 ТЗ).
6. `s14` — ужать Брауэра (§15 ТЗ).
7. Пересборка + `promer.py`, при переполнении `podognat.py`. Коммит.

**Критерий готовности не оспариваю** — он верный и проверяемый. Оспариваю только привязку номеров, см. выше.

## ВОПРОСЫ — (заполняет исполнитель)

1. 🔴 **ГОТОВАЯ СТРОКА РЕГИСТРАЦИИ — вам один ход.** Ворота `pre-commit` красные на моём пути: `kod_final.md` — документ-сирота (`KONSTITUCIYA §9`), строки в `_studio/docs/KARTA.md §6` нет. Сам не чиню: контракт зоны отдаёт мне `_studio/docs/` только на чтение, и он же назвал приоритет — контракт сильнее. Коммит проведён обходом с причиной (см. строку КОММИТ). Вставить в запись арки `2026-07-28_konspekt-l1` в §6:

   > · `kod_final.md` (**финальный заход по диктовке владельца 29.07 за 40 минут до лекции** — 20 пунктов `PRAVKI-final.md`; сделано 7 полностью / 5 частично / 6 не начато / 4 запрещены ТЗ; дек собран, переполнений 0, дефекты сцен на слайдах 17/20/22 сняты попиксельным гейтом; три урока фабрике с ценой, главный — `content/`+`slides/`+`shablon.html`+`slide_order` порождаются и затирают правки исполнителя)

   ⚠ Заодно: `PRAVKI-final.md`, `SPISOK-perekomponovka-3.md` и `kod_chistka.md` тоже вошли в git моей точкой отката `60fd4c0` — ворота их не назвали (проверено 22 файла), но проверьте, зарегистрированы ли они.
2. **Слайд 17 (центр): сцен получилось 2, а не 3.** Третья сцена по §13 — «вторая строка диаграммы». В ленте квадрат $\mathbb Z_2\to S_3\to\mathbb Z_2$ с центрами внизу и метками `sgn∘s = id` / `= id ?` — это ОДНА готовая SVG-фигура, и по сценам она не делится: фигуры движок ставит целиком. Разрезать её на две — это рисовать новую иллюстрацию, а не переставлять. Сделал: сцена 1 — утверждение + формула центра, сцена 2 — фраза про $s$ и `sgn` + квадрат целиком. Принять так или дожать до трёх сцен следующим заходом?
3. **Слайд 20 выше не поднял.** §17 разрешил «если перестановка дешёвая». В `brief.md` она была бы дешёвой, но `brief.md` порождается (см. урок 1): реально это перенос раздела между блоками `D` и `C` ленты, то есть та же операция, что уже дала сдвиг id и вернувшееся переполнение. Сработало ваше же «если нет — оставить и не ломать».
4. **Шесть пунктов картинок не начаты** (слайды 5, 10, 11, 12, 13, 14, 20 первая-вторая, 22). Голосом или следующим заходом?
5. **Врезка «Вёрстка» в ленте на разделе «Как доказывают, что функтора нет» устарела** — она описывает две осиротевшие фигуры, которых там больше нет (я их снял). Текст врезки не правил: это ваша прозаическая пометка, не моё дело её переписывать.

## ОТЧЁТ — (заполняет исполнитель)

**Дек собран и открывается. Это главный пункт критерия, он выполнен.**

### Критерий готовности — по пунктам

| # | Критерий | Факт |
|---|---|---|
| 1 | `build_deck --lint` rc=0, `dist/index.html` непуст, секций числом | ✅ `LINT rc=0`, `BUILD rc=0`, `dist/index.html` 1318 КБ, **секций 31** (26 содержательных + 5 служебных) |
| 2 | Переполнений 0 (`promer.py`) | ✅ `зон всего: 33 · переполнено: 0 · кадров промерено: 50` |
| 3 | Пропаданий текста 0 на слайдах 17, 20, 22 | ✅ `sceny_gejt.py`: гейты 1–5 зелёные; в списке «текст первой сцены не доехал до финальной» — `s19 s20 s21 s22 s24 s25`, то есть **только слайды части 2** (после `sl-divider3`). Владельческих 17/20/22 = `s14`/`s16`/`s18` в списке **нет** |
| — | гейт документа | ✅ `check_view.py` → «текст не съеден, опасных формул нет» |

Коды возврата снимал первыми, до содержательного вывода: `build_doc rc=0`, `porodit rc=0`, `sverstat rc=0`, `LINT rc=0`, `BUILD rc=0`, `check_view rc=0`.

### Сделано — по номерам ТЗ

**Приоритет 1. Дефекты сцен (§13, §17, §19)** — механика найдена: `{@N-M}` гаснет НА сцене `M` (`porodit.py:218`), поэтому `{@1-1}` на двухсценном слайде = «показать и убрать». Заменено на `{@1-2}` в ленте: центр (слайд 17), классификационные результаты (слайд 22). На слайде 20 пропадала ровно та строка про скалярное произведение, которую §17 велел убрать, — снята, и дефект ушёл вместе с ней. Попутно тот же дефект вылечен на Брауэре (слайд 18), он был того же класса.

**Приоритет 2. Перестановка Эйленберга–Маклейна (§12.1, §14)** — ✅. Раздел «Откуда взялся этот язык» поднят из начала блока `D` в начало блока `C`. Теперь он `s13`, сразу после `sl-divider2`, перед центром. Заодно закрылось «раздел „чего не бывает“ не читается как раздел» (§12 ⚠): часть 3 больше не начинается с техники.

**Приоритет 3. Слайды 23 и 24 сняты (§20)** — ✅. Разделы «Приём получает имя» и «Что можно доказать самому» удалены из ленты. Содержательных слайдов 28 → **26**.

**Приоритет 5. Слайд 17 в диаграмму (§13)** — ✅ с одним отступлением (вопрос 2). 826 → **289 знаков**. Осталось: утверждение · $Z(G)=\{z\in G: zg=gz\ \forall g\in G\}$ формулой, не словом «ЦЕНТР» · фраза про $\mathbb Z_2$, $s$, `sgn` с заданием $s(0)=e$, $s(1)=(1\,2)$ · квадрат диаграммой. Снято, как велено: проверка $\mathrm{sgn}\circ s=\mathrm{id}$ (она подписана на самой диаграмме), вывод «функтор дал бы id», вычисление $Z(S_3)=\{e\}$. `sgn` не комментируется. Центр больше не пропадает.

**Приоритет 6. Слайд 18, Брауэр (§15, §16)** — ✅ целиком. 1031 → **303 знака**, ровно ваша схема: «ТЕОРЕМА БРАУЭРА: нет ретракции из диска в $S^1$» / «Предположим, что ретракция есть. Рассмотрим $\pi_1$: $\pi_1(S^1)=\mathbb Z$, $\pi_1(D^2)=e$» / «Тогда была бы коммутативная диаграмма, которой быть не может». Убрано: вывод теоремы о неподвижной точке (§16, 4 абзаца), определения диска и окружности, вложение, «берём известный факт». Нижняя полоса снята, **полоса сделана вертикальной справа** — через врезку «Раскладка» в ленте, архетип сменился с `илл-полосой-снизу` на `рейка-справа`. Первая картинка («вообще не отсюда, она про множества и инъекции») снята.

**Приоритет 4. Картинки — ЧАСТИЧНО, 2 из 10.** Сняты две: осиротевшая «носитель против палитры» (инъекции) со слайда 17 и схема $FX\to FY$ со слайда 18. Обе помечены как осиротевшие собственной врезкой «Вёрстка» в ленте, §15 подтвердил вторую. **Не сняты: слайды 5, 10, 11, 12, 13, 14, 20 (первая и вторая), 22.**

**Попутно, из тех же разделов ленты:**
- §3: «композиция уважается» перенесена на слайд 4 в вашей формулировке — «переименовывание переименовывает и элементы структуры» + формула $F[\tau\circ\sigma]=F[\tau]\circ F[\sigma]$.
- §4: со слайда 5 снят нижний абзац; для графов теперь «графы с вершинами $U$».
- §8: со слайда 11 убрано слово «Определение» — остался просто список.
- §17: со слайда 20 убран текст «скалярное произведение на конечномерном $V$ / выбор изоморфизма». Теорема и доказательство не тронуты.

### НЕ сделано — что говорить голосом

1. **§1 ВИЗИТКА не вставлена.** Единственный пункт с пометкой «железно обязательно», и он не сделан: приоритет ТЗ поставил его ниже дефектов сцен, а времени хватило только на приоритеты 1–3, 5, 6. **Это самая заметная дырка отчёта.**
2. §2 — фраза про числа Фибоначчи под тождеством 8 не снята.
3. §3 — подписи $E$, $E'$ на фигуре слайда 4 и подпись биекции $\binom{n}{2}\leftrightarrow\binom{n}{n-2}$ не проставлены; «изоморфизм видов уезжает на следующий слайд» не проверял.
4. §4 — центрирование слайда 5 по вертикали, снятие полосы и иллюстраций.
5. §5 — слайд 6 целиком не тронут (тождество на первую сцену, короткий ответ, снять вопрос жирным, убрать картинку).
6. §7, §9 — картинки слайдов 10 и 12; заголовок и состав слайда 12.
7. §17 — слайд 20 выше не поднят (вопрос 3), картинки первая и вторая не сняты.
8. §18 — комментарий «здесь компонента одна» на слайде 21 не убран.
9. §19 — картинки и полоса слайда 22 не сняты (дефект сцен там вылечен).
10. **Запрещено самим ТЗ, не начинал:** §10 и §11 (переписывание слайдов 13 и 14), §10½ (структура 12–14), §12.2 (новый слайд про инъекции), эквивалентность категорий в части 2.

**ОХВАТ (отрицательный вердикт несёт охват): из 20 пунктов ТЗ полностью сделано 7 (§6, §12.1, §13, §14, §15, §16, §20), частично 5 (§3, §4, §8, §17, §19), не начато 4 (§1, §2, §5, §7, §9, §18 — шесть номеров), запрещено ТЗ 4 (§10, §10½, §11, §12.2).**

### Как проверял
- Дефекты сцен — программно, `sceny_gejt.py`, попиксельно (площадь ушедших блоков в px²), а не глазами.
- Переполнения — `promer.py` до и после: 5 → 1 → **0**.
- Состав и порядок — `porodit.py` и `sverstat.py` печатают порядок строкой, сверял по ней, а не по памяти.
- Опознание слайдов — по содержанию, с независимой сверкой числом панелей (владелец сказал «пять картинок» на 12 — у файла ровно 5; «три» на 13 — ровно 3; «две» на 14 — ровно 2). Совпадение 3 из 3 — не совпадение.
- Формулы — `sobrat_formuly.py` + `katex_kesh.js`: 232 из 232 отрендерено, линтер по формулам зелёный.

### Верификатор
Операция лоссовая (сняты два слайда, две фигуры и куски доказательств), поэтому проверка ДРУГИМ проходом: список потерь снят не глазами, а `git diff --stat` по зоне и сверкой инвентаря `porodit.py` до/после (28 → 26 разделов, 40 → 37 фигур — числа сошлись с тем, что я снимал: 2 раздела и 2 фигуры, третья фигура — сгенерированный дубль id). Битых ссылок 0 (`build_deck --lint` rc=0, единственное ⚠ — неиспользуемая порождённая `illustrations/s14-kak-dokazyvayut-chto-funktora-net`, это мусор переименования, не битая ссылка). Отдельного субагента не запускал: времени до лекции не было, и программной сверки хватило.

### Что НЕ трогал
Всё вне зоны. `_generator/` не открывал на запись. Ветку не переключал (`arka/mat-kostyak` стояла, проверил первым ходом). Часть 2 дека по содержанию не правил.

### 🔴 НЕОБРАТИМОЕ — отдельным списком
Всё восстанавливается из точки отката **`60fd4c0`** («точка отката перед kod_final»).

1. **Удалены два раздела ленты** — «Приём получает имя» и «Что можно доказать самому» · `raskadrovka/teksty/D-zapret-estestvennost.md` · `git show 60fd4c0:teorkat-vvedenie/raskadrovka/teksty/D-zapret-estestvennost.md`
2. **Удалена фигура «носитель против палитры»** (инъекции) · `raskadrovka/teksty/C-zapret-retrakt.md` · `git show 60fd4c0:teorkat-vvedenie/raskadrovka/teksty/C-zapret-retrakt.md`
3. **Удалена фигура $FX\to FY$** · тот же файл · тот же `git show`
4. **Удалён доказательный текст слайда 17** — вычисление $Z(\mathbb Z_2)$, $Z(S_3)=\{e\}$ и вывод про функтор (2 абзаца) · `C-zapret-retrakt.md` · тот же `git show`
5. **Удалён вывод теоремы Брауэра о неподвижной точке** — 4 абзаца с построением ретракции · `C-zapret-retrakt.md` · тот же `git show`
6. **Удалён нижний абзац слайда 5** (перенесён на слайд 4, не потерян) · `A-krasivaya.md` · `git show 60fd4c0:teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md`
7. **Перезаписан ключ `PRAVKI` в `tools/sverstat.py`** (`s14` → `s15`) и `tools/reflow.json` (`s14`→`s15`, `s21`→`s19`) · `git show 60fd4c0:teorkat-vvedenie/src/tools/sverstat.py`
8. **Порождаемое перезаписано генератором** — `content/*.md`, `slides/*.html`, `shablon.html`, `brief.md` (slide_order), `illustrations/*.svg` (переименованы по сдвигу id), `math/katex.json`, `tools/plotnost.json`, `dist/index.html`. Это выход конвейера, восстанавливается пересборкой из ленты.

Точка отката `60fd4c0` заодно **сохранила чужую работу**: на первом ходу зона была грязной от параллельного захода `kod_chistka.md`, я снял снимок, чтобы не затереть. Пока я это делал, `kod_chistka` закоммитил себя сам (`9c1fb12`, `749def3`, `103a794`), поэтому в мой снимок попали только три новых `.md` журнала. Дальше работал на чистой базе поверх его коммитов — ничего его не затёрто.

### Время и токены
Прогон ~40 минут (по времени изменения файлов ленты: 19:19 → 19:55+). Токены точно не измерены — инструментовки в заходе нет, называть цифру не буду.

### Открытое «возвращаться»
- Визитка (§1) — самое важное из несделанного.
- Шесть пунктов картинок и слайд 6.
- Слайд 17 до трёх сцен (вопрос 2).
- Гейт на «ключ ручной доводки указывает на другой раздел» — см. урок 2, грабли третьего раза.

**КОММИТ:** `1fc3430` — «final L1 по диктовке владельца: дефекты сцен на 17/20/22 сняты, Эйленберг-Маклейн открывает часть 3, слайды 23-24 убраны, центр в диаграмму и Брауэр в три строки; дек собран, переполнений 0» · 60 файлов · чужих путей в коммите **0** (проверено `show --name-only`) · зона чиста (`status --porcelain -- teorkat-vvedenie` → 0 строк). Точка отката — `60fd4c0`.

**ДОЛГ 1 (обход `pre-commit`).** Коммит проведён с `--no-verify`. Красное было на МОЁМ пути: `kod_final.md` — документ-сирота, нет строки в `_studio/docs/KARTA.md §6`. По канону §4 своё красное чинится, а не обходится, но контракт зоны отдаёт мне `_studio/docs/` только на чтение и сам назвал приоритет («контракт зоны сильнее»), поэтому обход законен, а починка — за аналитиком. Готовая строка — `## ВОПРОСЫ` п.1.

**ДОЛГ 2 (`git_zona.py` не смог провести коммит).** Пробовал сначала инструментом, как велит канон: `git_zona.py commit --zone teorkat-vvedenie --no-verify "<причина>"`. Упал: `add упал (rc=128): fatal: pathspec 'teorkat-vvedenie/src/content/s27.md' did not match any files` — инструмент перечисляет пути ПОФАЙЛЬНО и спотыкается на **удалённых** файлах, а мой заход удалил `content/s27.md`, `s28.md`, `slides/s27.html`, `s28.html` (порождаемые, сдвиг id после снятия двух слайдов). Второе: его `--zone teorkat-vvedenie` не покрывает мой файл-заход в `_studio/zhurnal/`, то есть зона захода из двух корней инструменту не выражается — он сам предупредил «ВНЕ зоны изменено ещё 10 путей». Коммитил формой из §4 захода (`add` по путям-каталогам, затем `commit` с теми же путями после `--`): каталог-pathspec удаления переваривает. **Нужна подкоманда, умеющая удаления и многокорневую зону** — иначе следующий заход, снявший хоть один файл, снова окажется без инструмента. Запись об обходе `INCIDENTY.md` инструмент, судя по своему выводу, сделал ДО падения `add`, так что причина там есть, а коммита к ней нет — сверьте.

*(Строка «⚠ ВНЕ зоны изменено ещё 10 путей» — не моё: там `.gitignore`, `catalan/`, `lsh-2026-…`, `INCIDENTY.md`, `kod_dobivka.md`, `PRAVKI-final.md`. Ничего из этого я не трогал; `PRAVKI-final.md` вошёл в git моей точкой отката `60fd4c0`, дальше не менялся.)*
