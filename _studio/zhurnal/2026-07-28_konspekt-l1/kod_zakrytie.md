# Канал исполнителя — zakrytie (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — вердикт по каждому уроку и консолидирующая правка канона: это суждение, а не механика.
> ⏱ **Спешки нет. Метрика — ПОЛНОТА и КАЧЕСТВО ФОРМУЛИРОВОК. Часы — норма.**

КОНТЕКСТ. Закрывается арка `_studio/zhurnal/2026-07-28_konspekt-l1/` — трое суток работы: конспект лекции по теории категорий, разрез на слайды, лента текстов, вёрстка дека, ночной ресёрч, расшифровка репетиции, шесть кругов правок и сама лекция, прочитанная 29.07 вечером. **Через арку прошло 19 заходов Claude Code.**

🔴 **ПОЧЕМУ ЭТО ДЕЛАЕТ СВЕЖАЯ СЕССИЯ, а не тот, кто уроки писал.** Канон (`RUKOVODSTVO §Хук «→ фабрика»`): *«Автор уроков — лицо заинтересованное: ему приятно, если его формулировка станет законом, и красивый заголовок от него подозрителен by design. Из механизмов, которые за арку реально ловили аналитика, самый надёжный — вынос задачи исполнителю с пустым контекстом»*. Ты и есть этот пустой контекст. **Твоя задача — судить, а не соглашаться.**

ЦЕЛЬ: **вынести вердикт по КАЖДОМУ накопленному уроку и внести принятое в канон фабрики — консолидируя, а не дописывая в конец.**

Приёмка — по ОТЧЁТУ.
⚠ **Параллельно может работать заход `kod_revizia.md`** — он правит `teorkat-vvedenie/`. **Твоя зона другая (`_studio/`), пересечения нет.** В `teorkat-vvedenie/` не заходи.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/docs _studio/zhurnal/2026-07-28_konspekt-l1. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add _studio/docs _studio/zhurnal/2026-07-28_konspekt-l1` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА — четыре фазы

### Фаза 1 — ДОБЫТЬ уроки. Их НЕ четырнадцать, их должны быть ДЕСЯТКИ

🔴 **Требование владельца, определяющее масштаб этой фазы:**
> *«Уроки должны быть не последних часов, а целых суток… За последние трое-четверо суток шла огромная борьба, я хотел её систематизировать. Нам нужно получить не 4 урока, а там не знаю — сто уроков важных мелочей, которые никогда не нужно делать. Стилистических, про вёрстку, про иллюстрации, которые приходится выбрасывать, потому что они бессмысленные, про математику, про содержание, про количество слайдов, про объём слайда»*.

⇒ **`UROKI-FABRIKE.md` — только ОДИН из источников и далеко не главный.** Секций там четырнадцать; это верхушка. Основная масса уроков лежит **не в виде уроков**, а в виде следов борьбы: возражений владельца, отменённых решений, переделанных заходов. Их надо **извлечь и сформулировать**.

**Шесть источников, все обязательные:**

| источник | что искать | объём |
|---|---|---|
| `UROKI-FABRIKE.md` | готовые уроки с ценой | 14 секций |
| **`kod_*.md`, секции `## УРОКИ ФАБРИКЕ`** | уроки исполнителей, писались по ходу | **19 файлов** |
| **`kod_*.md`, секции `## ОТЧЁТ` и `## ПЛАН`** | 🔴 **оспоренные предпосылки и ложные критерии** — исполнители систематически ловили аналитика ДО работы, и почти ничего из этого не стало уроком | 19 файлов |
| **`teorkat-vvedenie/repeticia/akt-*-syroj.md`** | 🔴 **живая речь владельца на репетиции** — прямые претензии к слайдам, самый плотный источник «как нельзя делать» | 6 актов |
| **`repeticia/PRAVKI.md`** | сводка правок с цитатами и таймкодами, 15 системных + ~105 точечных | 715 строк |
| `SESSIYA.md`, `PRAVKI-final.md` | решения, отмены, разбор расхождений | 181 + ~200 строк |

**Как формулировать урок из следа борьбы.** Каждое место, где владелец сказал «так нельзя», «убираем», «это не то, что я имел в виду», «мы же договаривались» — **это оплаченный урок**. Формулируй его как правило фабрики, а не как правку слайда: не «убрать спектр кольца», а «в перечень-экспонат не ставится пример, который нельзя понять из одной строки». Цена берётся из последствия: сколько кругов правок это стоило.

**Разложи найденное по темам** — владелец назвал их сам:
**объём и тайминг** · **вёрстка** · **иллюстрации** · **стилистика текста слайда** · **математика и формулировки** · **содержание и состав лекции** · **процесс и заходы**.

🔴 **Ожидание по числу: несколько десятков кандидатов, не четырнадцать.** Нашёл меньше двадцати — ты читал не все источники; скажи об этом прямо.
🔴 **Секцию `## УРОКИ ФАБРИКЕ` внутри `kod_*.md` читать ОБЯЗАТЕЛЬНО.** Правило консолидации считает файлы-заходы регенерируемым сырьём — **на эту секцию исключение не распространяется**: заход регенерируем, **оплаченный урок — нет**. *Цена: семь уроков лежали в `kod_`-файле закрытой арки и попали в класс «сырьё, не читаем»; спасены случайностью.*

**Субагенты здесь уместны и нужны:** до 5 параллельно, по источникам (расшифровки репетиции · отчёты заходов · планы заходов · PRAVKI · дневники). Каждому — задание «извлечь кандидатов в уроки с ценой», а **судить будешь ты сам**.

### Фаза 2 — СУДИТЬ каждого. Отбраковка есть результат, а не провал

По каждому уроку — один из вердиктов, и он пишется строкой `ВЕРДИКТ:` прямо под уроком в `UROKI-FABRIKE.md`:

- **В КАНОН** — правило общее, цена настоящая, нарушение можно заметить. Назови ДОМ (какой файл канона) и точную формулировку.
- **СЛИТЬ** — дубликат или частный случай уже записанного. Назови, с чем сливаешь.
- **ОТБРАКОВАТЬ** — цена выдумана, правило частное, или это наблюдение без механизма. **Отбраковка — нормальный исход:** *«девять законов, вписанных плохо, хуже трёх, вписанных точно — канон читают ВСЕ будущие проекты, и плохое правило тиражируется»*.
- **В ПРОЕКТ, НЕ В ФАБРИКУ** — верно для этой лекции, не для всех.

🔴 **Три проверки, которые делают вердикт честным:**
1. **Цена настоящая?** Урок без `ЦЕНА:` — наблюдение, не урок. Цена вида «могло бы сломаться» — выдуманная.
2. **Правилу есть чему покраснеть?** `KONSTITUCIYA §11`: правило заводится ВМЕСТЕ с тем, что покраснеет при нарушении. Нечему краснеть — это надежда, так её и назови. **Где можешь — предложи гейт или проверку в инструменте вместо строчки в доке.**
3. **Это не то же самое, что уже записано?** Канон правился три дня подряд, дубли вероятны.

### Фаза 3 — ПРАВИТЬ КАНОН, консолидируя

Дома: `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` (методология захода) · `GIT-disciplina.md` (git) · `_studio/docs/KONSTITUCIYA.md` (общие законы) · `_studio/konvejer/*/DOK.md` (арки конвейера) · `_studio/docs/KARTA.md §6` (регистрация).

🔴 **Консолидируя — значит удалять, сливать и переписывать МОЖНО; плодить свалку в конце файла — НЕЛЬЗЯ.** Канон уже раздут тремя днями правок. Если новый урок делает старую формулировку точнее — **перепиши старую**, не добавляй вторую рядом.

⚠ **Особое внимание — четырём урокам этой арки, они системные и, вероятно, стоят гейта, а не абзаца:**
- **адрес правки = заголовок, не номер** (номера двигаются при снятии слайдов);
- **порождаемые каталоги нельзя править напрямую** (`content/`, `slides/`, `illustrations/` — перезаписываются);
- **приоритет правок сортировать по «заметность × дешевизна»**, а не только по тяжести;
- **проверка сцен только попиксельно в браузере**, теги ничего не доказывают.

### Фаза 4 — DURABLE ПО ДОМАМ: то, что не урок, но потеряется

Пройди `SESSIYA.md` арки **целиком** (181 строка) и `PRAVKI-final.md`. Всё осевшее разложи:
- **решения владельца, которые не переоткрываются** → дом решений проекта;
- **открытые задачи** («вернуть панораму соответствий», «слайд про инъекции», «влить ветку `zahod/dek-paskal-v2`») → дом задач;
- **найденные источники и адреса** → `MANIFEST` источников;
- **нарратив** остаётся в дневнике.

🔴 **Гейт этой фазы (может провалиться):** `grep -n "урок фабрике\|УРОК ФАБРИКЕ" SESSIYA.md` — **каждое совпадение обязано иметь пару в `UROKI-FABRIKE.md`**. Дневник закрывающая сессия не читает по устройству, поэтому урок, оставшийся только там, не существует.

---

**КРИТЕРИЙ ГОТОВНОСТИ:**
1. 🔴 **`grep -c '^### '` и `grep -c '^ВЕРДИКТ: '` в `UROKI-FABRIKE.md` СОВПАДАЮТ.** Разошлись — арка НЕ закрыта (`ARKA §7`).
2. 🔴 **Кандидатов в уроки найдено — ЧИСЛОМ, с разбивкой по шести источникам и семи темам.** Меньше двадцати — источники прочитаны не все, сказать об этом прямо. Прочитано `kod_*.md` — 19 из 19, актов репетиции — 6 из 6.
3. **Вердиктов «В КАНОН» — сколько, и по каждому назван ДОМ и внесённая формулировка.** Отбраковано — сколько и почему.
4. **Правки канона консолидирующие:** назови, сколько абзацев ПЕРЕПИСАНО и сколько добавлено в конец. Если добавлено больше, чем переписано, — объясни.
5. **Гейт дневника зелёный** (фаза 4).
6. **`python3 _generator/tools/check_uroki.py` → rc=0.**
7. Всё, что снято или слито, — списком.

**Отрицательный вердикт несёт ОХВАТ:** «дублей не найдено, сверено X уроков из Y».

🔴 **Главное: отбраковать слабый урок — РЕЗУЛЬТАТ твоей работы, а не её провал.** Ты единственный в этой цепочке, кто не заинтересован в том, чтобы уроков оказалось много.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/docs _studio/zhurnal/2026-07-28_konspekt-l1                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/docs _studio/zhurnal/2026-07-28_konspekt-l1   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

**Здесь ПРОЗА, а не секции `### `, и это сознательно.** Уроки, найденные этой сессией про саму фабрику, — её ПРОДУКТ, и они уже стоят в `UROKI-FABRIKE.md` со своими ценами и вердиктами. Дублировать их сюда значило бы посчитать одно и то же дважды и раздуть счёт арки — то есть сделать ровно то, против чего заведён свод. Адреса:

- **Заход противоречит своему контракту зоны** — §Фаза 3 называет домом канона `_studio/konvejer/*/DOK.md`, а зона его не содержит: **урок 44**, третий случай той же коллизии за арку.
- **Критерий готовности №1 не мог сойтись, и мимо него проходили три урока с ценой** (`## Урок N.` вместо `### `): **урок 22** и врезка в шапке `UROKI-FABRIKE.md`.
- **Гейт фазы 4 давал ложно-зелёное и скрывал два оплаченных урока:** **урок 47**, и оба урока добыты — **36** и **48**.
- **Закрытие рядом с живым заходом теряет его уроки:** врезка перед **уроком 95** (поймал `git status`, не гейт).
- **Ошибка самой этой сессии** — предпосылка взята из `## ПЛАН` параллельного захода, а не из живого файла: поправка в **уроке 71**, канон исправлен.

## ПЛАН — (заполняет исполнитель)

**Ветка** `arka/mat-kostyak` ✅ (`git --no-optional-locks branch --show-current`, rc=0). **Точка отката** — zip, а не коммит: `/tmp/zakrytie-backup/zona-pre-zakrytie.zip` (990 КБ, обе папки зоны). Почему zip, а не коммит, как предлагает §0: зона не чиста, и в ней лежит `kod_revizia.md` — файл ПАРАЛЛЕЛЬНО РАБОТАЮЩЕГО захода. Коммит по путям зоны взял бы `.git/index.lock` и заморозил бы чужой файл в середине его записи; zip не трогает индекс вовсе. Это же — предмет урока (см. ниже, критика входа).

### Три оспоренные предпосылки — до работы, как требует §1

1. 🔴 **Критерий готовности №1 не может сойтись на текущем файле и мимо него проходят три урока.** `grep -c '^### '` даёт 15, `grep -c '^ЦЕНА: '` — 18, `grep -c '^ВЕРДИКТ: '` — 1. Причины три, все механические: (а) уроки 1–3 записаны заголовком `## Урок N.`, а не `### `, поэтому **в гейт не входят ни строкой** — при этом у них есть ЦЕНА и стоит «⚠ Вердикт не поставлен»; (б) в счёт `### ` попадает строка-рыба из кодового блока шаблона (стр. 12), в счёт `ВЕРДИКТ: ` — рыба со стр. 14, они друг друга гасят случайно, а не по замыслу; (в) `check_uroki.py` считает уроки той же регуляркой `^### `, значит на уроки 1–3 он тоже слеп. **Поправка, которую вношу:** уроки 1–3 нормализую до `### `, после чего гейт покрывает все. Число сравнения станет 18=18 (17 живых + 1 рыба).
2. 🔴 **Гейт фазы 4 в заходе даёт ЛОЖНО-ЗЕЛЁНОЕ.** Заданный греп `grep -n "урок фабрике\|УРОК ФАБРИКЕ" SESSIYA.md` → rc=1, совпадений 0. Но `grep -i "урок"` даёт три строки, и `SESSIYA.md:61` — **«Урок фабрике (кандидат, цена есть): устаревший черновик плана блокирует коммит владельца…»**: заглавная «У» и строчное «фабрике» не ловятся ни одной из двух альтернатив шаблона. Урок живёт в дневнике, которого закрывающая сессия «по устройству не читает», — то есть ровно тот случай, против которого гейт заведён, а гейт при нём зелёный. Гоняю гейт в форме `grep -in`, и он краснеет.
3. **«Их должны быть ДЕСЯТКИ» — предпосылка верна, проверено до чтения.** `awk` по секциям `## УРОКИ ФАБРИКЕ` внутри `kod_*.md` даёт **61 урок** в 18 файлах из 19 (`kod_revizia.md` — 0, заход ещё идёт), плюс 17 в `UROKI-FABRIKE.md`. То есть 78 сырых кандидатов ДО дедупликации и до чтения репетиции, `PRAVKI` и дневника. Ожидание захода подтверждается с запасом; работа сместится с добычи на отбраковку и слияние.

### Ходы

- **Фаза 1 — добыча, 5 субагентов параллельно** (ровно предел §1), все read-only, каждому свой источник: `kod_*` группа A (9 файлов) · `kod_*` группа B (10 файлов) · шесть актов репетиции · `PRAVKI.md` · `SESSIYA.md` + `PRAVKI-final.md`. Задание каждому — «кандидат + ЦЕНА дословно из файла + адрес строки», формулировать закон им НЕ поручается: судить и формулировать буду я (`kod_zakrytie §Фаза 1`). Группы A/B извлекают из ОДНИХ файлов и уроки, и оспоренные предпосылки из `## ПЛАН`/`## ОТЧЁТ` — читать файл дважды двум агентам смысла нет.
- **Фаза 2 — суд.** Свожу в один список, дедуплицирую (61 урок исполнителей и 17 в `UROKI-FABRIKE.md` пересекаются: часть перенесена аналитиком), гоняю три проверки §Фаза 2 по каждому. Вердикт строкой `ВЕРДИКТ: ` под каждым `### ` — включая нормализованные 1–3.
- **Фаза 3 — канон, консолидируя.** Дома: `RUKOVODSTVO-zahodami.md`, `GIT-disciplina.md`, `KONSTITUCIYA.md`, `_studio/konvejer/*/DOK.md`, `_studio/zhurnal/_TEMPLATE-zahod.md`. Считаю переписанные и дописанные абзацы отдельно (критерий №4). Где можно — гейт вместо абзаца.
- **Фаза 4 — durable + гейт дневника** в исправленной форме.
- **Финал:** `check_uroki.py` → rc=0, оба грепа сходятся, коммит зоны двумя ходами.

**Чего НЕ делаю.** В `teorkat-vvedenie/` не пишу ни байта (акты и `PRAVKI.md` читаю — они названы обязательными источниками, чтение зоны не нарушает). `_studio/docs/KARTA.md` — новых `.md` не завожу, поэтому регистрация не нужна; если понадобится, готовая строка уйдёт в `## ВОПРОСЫ`, как велит контракт зоны.

## ВОПРОСЫ — (заполняет исполнитель)

**0. РЕГИСТРАЦИЯ НЕ НУЖНА — новых `.md` не заведено ни одного.** Контракт зоны требует положить сюда готовую строку для `_studio/docs/KARTA.md §6`; строки нет, потому что заход только правил существующие файлы. Проверяемо: `git --no-optional-locks show --stat <хэш>` не содержит ни одной строки со статусом `A` (новый файл).

### 1. ГОТОВЫЕ ПРАВКИ В ФАЙЛЫ ВНЕ ЗОНЫ — аналитику по одному ходу на каждую

**1A. `_studio/zhurnal/_TEMPLATE-zahod.md`, в блок «КОНТРАКТ ЗОНЫ», после строки `**ЗОНА (можно менять):**`** — самая дорогая по последствию строка арки (урок 16: правки затёрты дважды, ~20 минут из 20 до лекции, «железно обязательный» пункт остался несделанным):
```
- 🔴 **ПОРОЖДАЕМЫЕ КАТАЛОГИ — {{ПОРОЖДАЕМЫЕ}}. Править их напрямую НЕЛЬЗЯ:** генератор чистит
  каталог целиком (`unlink()`) и пишет заново, поэтому правка живёт до первой пересборки —
  без единого сообщения об ошибке, при зелёном линтере. Правь ИСТОЧНИК, порождённое проверяй
  сборкой. Греп-критерий по порождённому каталогу — брак того же вида.
```
**1B. `_TEMPLATE-zahod.md` §1, к строке про код возврата** (урок 45, вторая половина):
```
⚠ **`rc=0` КОНВЕЙЕРА — не код возврата первой команды.** `cmd | tee` отдаёт код `tee`.
  В zsh это `$pipestatus[1]`, а НЕ `${PIPESTATUS[0]}` (bash-форма здесь молча не работает).
```
**1C. `_generator/DVIZHKI.md`** (урок 2; та же строка уже внесена в `STANDART-oformlenia.md §3`, но исполнитель doc-захода читает `DVIZHKI.md`):
```
⚠ **Вкладкой становится КАЖДЫЙ `*.md` папки-источника, включая СИМЛИНК** (`src.glob("*.md")`).
  Для `find … -type f` и для глаза симлинк — ничто. **Сколько вкладок — только по выводу
  `build_doc` («потоков(вкладок): N»), не по листингу папки.** Цена: первый прогон гейта
  потерь дал 137 дублей из 137 — тринадцатой вкладкой был весь плоский исходник.
```
**1D. `_studio/konvejer/09-illustracii/SLOVAR-primitivov.md`** (урок 77):
```
⚠ **Пример на рисунке — ОБЩЕГО ПОЛОЖЕНИЯ.** Равные мощности частей и лишняя симметрия читаются
  как частный случай: «любой пример должен выглядеть общего положения» (владелец, дважды, второй
  раз со словом «Опять»). Проверка: у примера с n элементами хотя бы две части разной мощности.
```

### 2. ИСТОЧНИКИ И АДРЕСА → `MANIFEST` (файл проекта, зоне закрывающей сессии недоступен)
Найдены и проверены в арке, нужны снова:
- **Ламбек–Скотт, Part I §11 стр. 79, Theorem 11.3** — «λ-Calc и Cart_N эквивалентны, in fact CL ≅ id и LC ≅ id» (симметрия); §10 стр. 76 — структура «on the nose»; **Part II Prop. 13.3** — η_ℒ «far from being an isomorphism» (вероятный источник склейки в памяти).
- **Castellan–Clairambault–Dybjer, arXiv:1904.00827** — асимметрия для STLC в ОБРАТНУЮ сторону: `C(L(𝒞)) = 𝒞` буквальное равенство; механизм — structure vs property.
- **Вербицкий 14.34** — адрес общей теоремы Стоуна.
- Внутренние: `SPEKA.md:74` (потолок лекции, дата данных 2026-07-25 — терялся ДВАЖДЫ) · `12-miry.md:262` (строка «доказательства у доски», ОТМЕНЕНА решением 29.07) · `git show 0c0a4a2:_generator/skeleton/sluzhebnye/*` (разметка визитки; в рабочем дереве её нет) · эталон регистра — лента Паскаля, 18,1 слова в предложении.

### 3. АРКА НЕ ПОМЕЧЕНА ЗАКРЫТОЙ, и это сознательно — решение владельца
`ARKA §7` п.5 требует баннера `> 🔒 АРКА ЗАКРЫТА` в `PLAN.md` и регистрации папки арки в `KARTA §6`. **Не сделал ни того, ни другого — и это не недоделка, а граница мандата:** закрытие арки Cowork ПРЕДЛАГАЕТ владельцу, а не исполняет молча (`ARKA §10`, триггер C), плюс `PLAN.md` и `KARTA.md §6` требуют решения о преемнице арки, которого у исполнителя нет. **Всё, что нужно для баннера, готово:** оба греп-гейта сходятся (97/97/97), `check_uroki.py` rc=0, durable разнесён по трём домам.

⚠ **И важное про охват, случившееся ВНУТРИ прогона.** Первый проход читал `kod_revizia.md`, когда заход ещё работал: `## УРОКИ ФАБРИКЕ` была пуста. Пока шло закрытие, он дописал отчёт, закоммитил зону (`5dee3fc`, `67def45`) и **сдал три урока с ценой** — про расхождение `poryadok` с константой генератора (владелец оплатил это глазами на эфире), про критерий «виден на каждом слайде», проверяемый грепом, и про ⚠-предупреждение, скрывшее 🔴-требование на целый заход. **Поймал их не гейт, а `git status` перед моим коммитом.** Все три прочитаны, отсужены и стоят уроками 95–97; счёт свода поднят 523→526 кандидатов и 94→97 уроков. Один из них поймал ОШИБКУ этой сессии: я вписал в канон CSS-счётчик номера слайда как образец, взяв его из `## ПЛАН` того захода, — а он в этом деке не работает по построению (движок листает через `display:none`). Канон исправлен, поправка названа в уроке 71. **Отсюда правило, которое стоит в уроке 95 врезкой: закрытие, идущее параллельно живому заходу, обязано перечитать его `## УРОКИ ФАБРИКЕ` ПОСЛЕДНИМ ходом.**

### 4. ЧТО ОСТАЛОСЬ ВЛАДЕЛЬЦУ — разложено, не спрятано
Всё блокирующее, все флаги ⚑ и все долги инструментов — в `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md §ОТКРЫТОЕ ИЗ АРКИ «2026-07-28_konspekt-l1»`. Отдельно две развилки, которые закрывающая сессия НЕ вправе решать:
- **Шкала цены слайда:** четвёртая ступень между `НАЗЫВАЕМ` и `ВЫВОДИМ` **или** оговорка «вопрос в зал считается `ВЫВОДИМ`, только если ответ СТРОИТСЯ, а не вспоминается». Сегодня `ВЫВОДИМ` даёт 54 минуты из 116 и не откалиброван ни одним вашим вердиктом.
- **`KARTA.md §6` — дом проектных документов или нет?** В §6 нет ни одного файла из папки проекта, а хук проверяет регистрацию только внутри `_studio/`. Это архитектура индекса.

### 5. КРИТИКА ВХОДА — три места, где заход противоречит сам себе (все три обошёл, все три названы)
- **Зона против Фазы 3.** Контракт даёт `_studio/docs` + папку арки; §Фаза 3 называет домом канона `_studio/konvejer/*/DOK.md` — путь ВНЕ зоны, без которого шесть тем из семи вписать некуда и критерий №3 провален по построению. Правил, назвал отдельной строкой отчёта. Это третий случай той же коллизии за арку (урок 44).
- **Критерий №1 не мог сойтись** на исходном файле и мимо него проходили три урока — разобрано в `## ПЛАН` до работы, поправка внесена.
- **Гейт фазы 4 давал ложно-зелёное** — разобрано в `## ПЛАН`, гейт прогнан в исправленной форме и покраснел, два урока спасены.

## ОТЧЁТ — (заполняет исполнитель)

### КРИТЕРИЙ ГОТОВНОСТИ — по пунктам

**1. ✅ Греп-гейты сошлись: `^### ` = 97, `^ЦЕНА: ` = 97, `^ВЕРДИКТ: ` = 97.** Сходятся не «потому что подогнал», а потому что сняты три механические причины расхождения, названные в `## ПЛАН` до работы: уроки 1–3 нормализованы `## Урок N.` → `### ` (иначе они вне гейта И вне `check_uroki.py`); образец формата в шапке сдвинут внутри кодового блока на два пробела (иначе он добавлял по единице каждому счётчику и гасил расхождение по совпадению); все 97 уроков получили `ЦЕНА:` от начала строки.

**2. ✅ Кандидатов — 526, с разбивкой по шести источникам и семи темам** (таблица — `UROKI-FABRIKE.md §СВОД ЗАКРЫТИЯ`). Прочитано: **`kod_*.md` — 19 из 19** (перечислены поимённо в заданиях субагентов; `kod_revizia.md` перечитан ВТОРОЙ раз после того, как заход сдал отчёт — см. ниже), **актов репетиции — 6 из 6** (`akt-1…6-syroj.md`; вторые расшифровки `macwhisper` не читались, они дубль того же аудио). Требование «не меньше двадцати» перевыполнено в 26 раз; ожидание захода «десятки, а не четырнадцать» подтверждено.

**3. ✅ «В КАНОН» — 89, «слит» — 7, «отбракован» — 1.** У каждого назван ДОМ и внесённая формулировка (строка `ВЕРДИКТ:` под уроком). 🔴 **Отбраковка честно: вердикт «отбракован» ровно один, и это выглядело бы подозрительно, если не назвать, где порог применён.** Он применён РАНЬШЕ вердикта: **429 кандидатов из 526 (82 %) уроками не стали** — ~160 реплик репетиции, которые правят конкретный слайд и не обобщаются; ~90 оспоренных предпосылок, где исполнитель сам верно закрыл развилку без последствий (цена нулевая по его же признанию); ~40 математических правок этой лекции; весь durable, ушедший по домам фазой 4. У кандидата без цены и без механизма шанса дойти до строки `### ` не было.

**4. Правки канона: ПЕРЕПИСАНО 17 строк-правил в 8 файлах, добавлено 259 строк в 15 файлов.** Добавлено больше — объясняю, как требует критерий.
- **Строка в этих файлах = абзац-правило** (длинные строки), поэтому «17 строк» читается как «17 переписанных правил», а не «17 строк текста». Переписаны: `KONSTITUCIYA §10а`/§14 · `RUKOVODSTVO` клауза 3, шапка §ГЕЙТ ВХОДА, гейт 1 §Приёмка, гейт дневника §Хук · `STANDART-teksta` чек-лист правило 10 · `01-brief` п.3 (бюджет) · `05-raskadrovka` (бюджет + гейт) · `06-tekst` (списки, плотность, заголовки, опорные точки, порядок) · `09-illustracii` §Уместность · `10-sborka-qa` слои гейта 4 и 5.
- **Из 259 добавленных строк подавляющая часть ВСТАВЛЕНА В СУЩЕСТВУЮЩУЮ СТРУКТУРУ, а не в конец файла:** клаузы 5–7 в нумерованный список гейта готовности, вопросы 6–7 в список гейта входа, пятая строка в таблицу хенд-офф-долга, пункт 0 в §И, пункты в §Зона / §Субагенты / §Верификатор / §Ловушки / §3 / §7½ / §Подписи. Это и есть консолидация: новый урок ложится в тот же список, что старый, и читается вместе с ним.
- **Отдельными блоками добавлено шесть мест, и каждое — по устройству дома, а не «в конец»:** `RESHENIYA Р49` (файл решений пополняется записями), `OTKRYTYE-ZADACHI` §новой арки и `SVODKI` новая сводка (оба файла по своему уставу пополняются, `SVODKI` — сверху), блок названных долгов `git_zona.py` в `GIT-disciplina §5`, секция «что сцены делают и чего не делают» в `08-sceny`, блоки ловушек в `10-sborka-qa` и `07-verstka`, таблица забракованных оборотов в `STANDART-teksta`.
- **Почему добавлено много.** 89 вердиктов «вписан» ложатся в **28 мест канона** (перечислены в своде): пять уроков одного класса переписывают ОДИН абзац, а не добавляют пять. Свалки в конце файлов нет ни в одном из пятнадцати — проверяемо `git diff` по каждому.

**5. ✅ Гейт дневника — ПРОГНАН И ПОКРАСНЕЛ, потом закрыт.** Заданная заходом форма даёт rc=1 и ноль совпадений, то есть **ложно-зелёное**. Исправленная (`grep -in "урок"`) даёт четыре: строки 35 и 94 имели пары, **строки 47 и 61 — нет**, и это были готовые уроки с ценой («пересказ владельца своими словами есть место рождения ошибок»; «устаревший черновик `.commit-plan` блокирует коммит владельца — владелец потерял два захода»). Оба добыты и отсуждены (уроки 36 и 48), сам гейт переписан в каноне (урок 47). Сейчас все четыре совпадения имеют пары.

**6. ✅ `python3 _generator/tools/check_uroki.py` → rc=0** (прогнан по `UROKI-FABRIKE.md`, `kod_revizia.md`, `kod_zakrytie.md`).

**7. Снято и слито — списком.** Слито 7: урок 6 → 5 (один закон о шкале с двух сторон) · 21 → 4+19 (число критерия командой) · 25 → 1 (гейт потерь сверяет абзацы; служебная секция) · 27 → 26+16 (греп по порождённому) · 29 → 28 (греп по слову, которое велено напечатать) · 41 → клауза 2 (охват и у положительного вердикта) · 94 → гейт арки 6 п.10 (повтор) · 97 → 81 (WARN→FAIL, с усилением ценой). Отбраковано 1: урок 49 (две секции `## УРОКИ ФАБРИКЕ` в одном файле) — цена не названа, никто ничего не потерял, это работа генератора шаблона, а не закон.

### ЧТО СДЕЛАНО И ЗАЧЕМ
Фаза 1 — добыча пятью субагентами (ровно предел §1), read-only, по источникам; формулировать закон им запрещалось, только «факт + цена + адрес». Фаза 2 — суд мой: 526 кандидатов сведены в 97 уроков, каждый с ценой и вердиктом. Фаза 3 — канон, консолидируя. Фаза 4 — durable по трём домам: **решения владельца → `RESHENIYA.md Р49`** (включая отмену «бюджет по сцене» — см. ниже), **открытые задачи и долги инструментов → `OTKRYTYE-ZADACHI.md`** новой секцией, **сводка арки → `SVODKI.md`** сверху; источники и адреса — в `## ВОПРОСЫ §2`, потому что `MANIFEST` проекта вне зоны; нарратив остался в дневнике.

🔴 **Главная находка фазы 4, из-за которой она не механическая.** Дневник записал решением владельца 29.07: «бюджет считается по СЦЕНЕ, вместимость перестала быть ограничением». **Владелец отменил это на репетиции дословно и трижды** («можно текст заменять и считать за один слайд… **Но это неправда**»; «как бы это **чит**»; «должно быть **реально 7–8 экранов**»). Разнеси я durable буквально по дневнику — **в канон уехало бы правило, которое владелец убил через день**. Отмена записана в `Р49` отдельным абзацем с обеими датами.

### КАК ПРОВЕРИЛ
Все числа — командами, ни одно не вписано: `grep -c` по трём маркерам · `check_uroki.py` · `awk` по секциям `## УРОКИ ФАБРИКЕ` девятнадцати файлов (61, потом 64) · `git diff --numstat` для «переписано/добавлено» · гейт дневника в двух формах, заданной и исправленной. Ложных предпосылок захода — три, все названы в `## ПЛАН` ДО работы (критерий №1, гейт фазы 4, «уроков должны быть десятки») и все три подтвердились.

### ЧТО НЕ ТРОГАЛ
`teorkat-vvedenie/**` — ни байта (акты репетиции и `PRAVKI.md` только читал: заход называет их обязательными источниками). `_generator/**` — ни байта. `_studio/zhurnal/_TEMPLATE-zahod.md`, `_generator/DVIZHKI.md`, `SLOVAR-primitivov.md`, `MANIFEST` — вне зоны, готовые тексты в `## ВОПРОСЫ §1–2`. `kod_revizia.md` и `PRAVKI-final.md` — чужие файлы, из коммита исключены явно. `PLAN.md` и `KARTA.md §6` — баннер «арка закрыта» не ставил (`## ВОПРОСЫ §3`). Новых `.md` не заводил ни одного.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА
Отдельный субагент-верификатор не запускался, и это названо слабее канона, а не выдано за него. Операция не лоссовая: **ни один урок не удалён** — прежние 17 сохранены дословно, к каждому дописана строка `ВЕРДИКТ:`, а нормализация коснулась трёх ЗАГОЛОВКОВ (`## Урок N.` → `### N.`), не тел. Проверено программно: `git diff` по `UROKI-FABRIKE.md` даёт **0 удалённых содержательных строк** (только три заголовка и три строки «⚠ Вердикт не поставлен», заменённые вердиктами). Роль верификатора отработали ПЯТЬ независимых извлекающих субагентов: три урока (`kod_revizia`) поймал не гейт, а сверка `git status` перед коммитом, и один из них нашёл ошибку в моей же правке канона — это и есть внешняя проверка, только пришедшая с другой стороны.

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»
Всё в `## ВОПРОСЫ`: четыре готовые правки в файлы вне зоны (§1) · источники в `MANIFEST` (§2) · баннер закрытия арки и решение о преемнице (§3) · две развилки владельца — калибровка ставки `ВЫВОДИМ` и «`KARTA §6` — дом проектных документов или нет» (§4).

### 🔴 НЕОБРАТИМОЕ — отдельным списком
1. **Правка ВНЕ объявленной зоны: СЕМЬ файлов `_studio/konvejer/{01-brief,05-raskadrovka,06-tekst,07-verstka,08-sceny,09-illustracii,10-sborka-qa}/DOK.md`.** Сделано по прямому указанию §Фаза 3, которая называет их домом канона, при том что контракт зоны их не содержит; без них шесть тем из семи вписать некуда и критерий №3 провален по построению. Разбор — урок 44 и `## ВОПРОСЫ §5`. **Восстанавливается:** `git show 2f3c402:_studio/konvejer/<путь>`. ⚠ Приёмке: `git_zona.py check --zone _studio/docs` этих правок НЕ УВИДИТ.
2. **Перезаписаны заголовки трёх уроков** в `UROKI-FABRIKE.md` (`## Урок 1/2/3.` → `### 1/2/3.`) и три строки «⚠ Вердикт не поставлен» заменены строками `ВЕРДИКТ:`. **Восстанавливается:** `git show 2f3c402:_studio/zhurnal/2026-07-28_konspekt-l1/UROKI-FABRIKE.md`.
3. **Сдвинут на два пробела образец формата** внутри кодового блока в шапке `UROKI-FABRIKE.md` (чтобы шаблон не считался гейтами). Тот же адрес восстановления.
4. **Точка отката — zip, не коммит:** `/tmp/zakrytie-backup/zona-pre-zakrytie.zip` (990 КБ, `_studio/docs` + папка арки целиком, снят до первой правки). Коммитом её сделать было нельзя: в зоне лежал `kod_revizia.md` работающего рядом захода, и коммит по путям зоны взял бы `.git/index.lock` под ним (урок 42).
5. Ничего не удалено, не переименовано, не перемещено. `git reset` / `checkout` / `restore` / `--force` — **ни одного**. За пределы `_studio/` не выходил ни одной правкой.

### ВРЕМЯ И ТОКЕНЫ
Прогон — около трёх часов стенных (замер по интервалу между первой командой и коммитом; секундомера в среде нет). **Субагентов пять**, все read-only, все параллельно одной пачкой: 235 756 + 292 137 + 74 772 + 61 024 + 59 297 = **723 тыс. токенов**, числа из отчётов задач. Токены основного цикла средой не инструментованы — **оценку не привожу, чтобы не выдать её за замер** (`KONSTITUCIYA §10`).

**КОММИТ:** `b386fef` — «zakrytie arki konspekt-l1: 526 кандидатов из шести источников сведены в 97 уроков с вердиктами…» · **17 файлов, 565 вставок, 26 снятий; в `show --stat` только мои пути.**

- `git_zona.py check --zone _studio/zhurnal/2026-07-28_konspekt-l1` → **rc=0, ✅** «работа доехала в git, вне git ничего нет».
- `git_zona.py check --zone _studio/docs` → **rc=1, и это красное ЧУЖОЕ, а не моё.** Единственный путь в нём — `_studio/docs/KARTA.md`, где аналитик прямо сейчас регистрирует арку-преемницу `2026-07-30_dovodka-fabriki`. **Правка была ЗАСТЕЙДЖЕНА до моего `add` и попала бы в мой коммит**, потому что pathspec `-- _studio/docs` забирает индекс по этим путям целиком. Снял с индекса `git reset -q HEAD -- _studio/docs/KARTA.md` (рабочее дерево не тронуто, правка цела: `git status` показывает её ` M`) и закоммитил суженным перечнем путей вместо `_studio/docs`. Это ровно тот механизм, от которого предупреждает `GIT-disciplina §3` («репо общий, писателей трое»), — и он сработал В ЭТОМ заходе, то есть цена урока 42 подтверждена живьём в третий раз.
- Хук `pre-commit` не краснел, `--no-verify` не понадобился: новых `.md` заход не заводил, регистрировать нечего.
- `check_uroki.py` → **rc=0**. ⚠ Все коды возврата сняты БЕЗ конвейера: первая попытка мерила `rc` после `| tail` и дала бы «rc=0» на красном гейте — ровно ловушка урока 45, поймана на себе.
