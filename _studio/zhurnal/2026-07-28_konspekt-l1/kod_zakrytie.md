# Канал исполнителя — zakrytie (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — вердикт по каждому уроку и консолидирующая правка канона: это суждение, а не механика.
> ⏱ **Спешки нет. Метрика — ПОЛНОТА и КАЧЕСТВО ФОРМУЛИРОВОК. Часы — норма.**

КОНТЕКСТ. Закрывается арка `_studio/zhurnal/2026-07-28_konspekt-l1/` — трое суток работы: конспект лекции по теории категорий, разрез на слайды, лента текстов, вёрстка дека, ночной ресёрч, расшифровка репетиции, шесть кругов правок и сама лекция, прочитанная 29.07 вечером. **Через арку прошло 19 заходов Claude Code.**

🔴 **ПОЧЕМУ ЭТО ДЕЛАЕТ СВЕЖАЯ СЕССИЯ, а не тот, кто уроки писал.** Канон (`RUKOVODSTVO §Хук «→ фабрика»`): *«Автор уроков — лицо заинтересованное: ему приятно, если его формулировка станет законом, и красивый заголовок от него подозрителен by design. Из механизмов, которые за арку реально ловили аналитика, самый надёжный — вынос задачи исполнителю с пустым контекстом»*. Ты и есть этот пустой контекст. **Твоя задача — судить, а не соглашаться.**

ЦЕЛЬ: **вынести вердикт по КАЖДОМУ накопленному уроку и внести принятое в канон фабрики — консолидируя, а не дописывая в конец.**

Приёмка — по ОТЧЁТУ.
⚠ **Параллельно может работать заход `kod_revizia.md`** — он правит `teorkat-vvedenie/`. **Твоя зона другая (`_studio/`), пересечения нет.** В `teorkat-vvedenie/` не заходи.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/docs _studio/zhurnal/2026-07-28_konspekt-l1. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add _studio/docs _studio/zhurnal/2026-07-28_konspekt-l1` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА — четыре фазы

### Фаза 1 — ДОБЫТЬ уроки. Их НЕ четырнадцать, их должны быть ДЕСЯТКИ

🔴 **Требование владельца, определяющее масштаб этой фазы:**
> *«Уроки должны быть не последних часов, а целых суток… За последние трое-четверо суток шла огромная борьба, я хотел её систематизировать. Нам нужно получить не 4 урока, а там не знаю — сто уроков важных мелочей, которые никогда не нужно делать. Стилистических, про вёрстку, про иллюстрации, которые приходится выбрасывать, потому что они бессмысленные, про математику, про содержание, про количество слайдов, про объём слайда»*.

⇒ **`UROKI-FABRIKE.md` — только ОДИН из источников и далеко не главный.** Секций там четырнадцать; это верхушка. Основная масса уроков лежит **не в виде уроков**, а в виде следов борьбы: возражений владельца, отменённых решений, переделанных заходов. Их надо **извлечь и сформулировать**.

**Шесть источников, все обязательные:**

| источник | что искать | объём |
|---|---|---|
| `UROKI-FABRIKE.md` | готовые уроки с ценой | 14 секций |
| **`kod_*.md`, секции `## УРОКИ ФАБРИКЕ`** | уроки исполнителей, писались по ходу | **19 файлов** |
| **`kod_*.md`, секции `## ОТЧЁТ` и `## ПЛАН`** | 🔴 **оспоренные предпосылки и ложные критерии** — исполнители систематически ловили аналитика ДО работы, и почти ничего из этого не стало уроком | 19 файлов |
| **`teorkat-vvedenie/repeticia/akt-*-syroj.md`** | 🔴 **живая речь владельца на репетиции** — прямые претензии к слайдам, самый плотный источник «как нельзя делать» | 6 актов |
| **`repeticia/PRAVKI.md`** | сводка правок с цитатами и таймкодами, 15 системных + ~105 точечных | 715 строк |
| `SESSIYA.md`, `PRAVKI-final.md` | решения, отмены, разбор расхождений | 181 + ~200 строк |

**Как формулировать урок из следа борьбы.** Каждое место, где владелец сказал «так нельзя», «убираем», «это не то, что я имел в виду», «мы же договаривались» — **это оплаченный урок**. Формулируй его как правило фабрики, а не как правку слайда: не «убрать спектр кольца», а «в перечень-экспонат не ставится пример, который нельзя понять из одной строки». Цена берётся из последствия: сколько кругов правок это стоило.

**Разложи найденное по темам** — владелец назвал их сам:
**объём и тайминг** · **вёрстка** · **иллюстрации** · **стилистика текста слайда** · **математика и формулировки** · **содержание и состав лекции** · **процесс и заходы**.

🔴 **Ожидание по числу: несколько десятков кандидатов, не четырнадцать.** Нашёл меньше двадцати — ты читал не все источники; скажи об этом прямо.
🔴 **Секцию `## УРОКИ ФАБРИКЕ` внутри `kod_*.md` читать ОБЯЗАТЕЛЬНО.** Правило консолидации считает файлы-заходы регенерируемым сырьём — **на эту секцию исключение не распространяется**: заход регенерируем, **оплаченный урок — нет**. *Цена: семь уроков лежали в `kod_`-файле закрытой арки и попали в класс «сырьё, не читаем»; спасены случайностью.*

**Субагенты здесь уместны и нужны:** до 5 параллельно, по источникам (расшифровки репетиции · отчёты заходов · планы заходов · PRAVKI · дневники). Каждому — задание «извлечь кандидатов в уроки с ценой», а **судить будешь ты сам**.

### Фаза 2 — СУДИТЬ каждого. Отбраковка есть результат, а не провал

По каждому уроку — один из вердиктов, и он пишется строкой `ВЕРДИКТ:` прямо под уроком в `UROKI-FABRIKE.md`:

- **В КАНОН** — правило общее, цена настоящая, нарушение можно заметить. Назови ДОМ (какой файл канона) и точную формулировку.
- **СЛИТЬ** — дубликат или частный случай уже записанного. Назови, с чем сливаешь.
- **ОТБРАКОВАТЬ** — цена выдумана, правило частное, или это наблюдение без механизма. **Отбраковка — нормальный исход:** *«девять законов, вписанных плохо, хуже трёх, вписанных точно — канон читают ВСЕ будущие проекты, и плохое правило тиражируется»*.
- **В ПРОЕКТ, НЕ В ФАБРИКУ** — верно для этой лекции, не для всех.

🔴 **Три проверки, которые делают вердикт честным:**
1. **Цена настоящая?** Урок без `ЦЕНА:` — наблюдение, не урок. Цена вида «могло бы сломаться» — выдуманная.
2. **Правилу есть чему покраснеть?** `KONSTITUCIYA §11`: правило заводится ВМЕСТЕ с тем, что покраснеет при нарушении. Нечему краснеть — это надежда, так её и назови. **Где можешь — предложи гейт или проверку в инструменте вместо строчки в доке.**
3. **Это не то же самое, что уже записано?** Канон правился три дня подряд, дубли вероятны.

### Фаза 3 — ПРАВИТЬ КАНОН, консолидируя

Дома: `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` (методология захода) · `GIT-disciplina.md` (git) · `_studio/docs/KONSTITUCIYA.md` (общие законы) · `_studio/konvejer/*/DOK.md` (арки конвейера) · `_studio/docs/KARTA.md §6` (регистрация).

🔴 **Консолидируя — значит удалять, сливать и переписывать МОЖНО; плодить свалку в конце файла — НЕЛЬЗЯ.** Канон уже раздут тремя днями правок. Если новый урок делает старую формулировку точнее — **перепиши старую**, не добавляй вторую рядом.

⚠ **Особое внимание — четырём урокам этой арки, они системные и, вероятно, стоят гейта, а не абзаца:**
- **адрес правки = заголовок, не номер** (номера двигаются при снятии слайдов);
- **порождаемые каталоги нельзя править напрямую** (`content/`, `slides/`, `illustrations/` — перезаписываются);
- **приоритет правок сортировать по «заметность × дешевизна»**, а не только по тяжести;
- **проверка сцен только попиксельно в браузере**, теги ничего не доказывают.

### Фаза 4 — DURABLE ПО ДОМАМ: то, что не урок, но потеряется

Пройди `SESSIYA.md` арки **целиком** (181 строка) и `PRAVKI-final.md`. Всё осевшее разложи:
- **решения владельца, которые не переоткрываются** → дом решений проекта;
- **открытые задачи** («вернуть панораму соответствий», «слайд про инъекции», «влить ветку `zahod/dek-paskal-v2`») → дом задач;
- **найденные источники и адреса** → `MANIFEST` источников;
- **нарратив** остаётся в дневнике.

🔴 **Гейт этой фазы (может провалиться):** `grep -n "урок фабрике\|УРОК ФАБРИКЕ" SESSIYA.md` — **каждое совпадение обязано иметь пару в `UROKI-FABRIKE.md`**. Дневник закрывающая сессия не читает по устройству, поэтому урок, оставшийся только там, не существует.

---

**КРИТЕРИЙ ГОТОВНОСТИ:**
1. 🔴 **`grep -c '^### '` и `grep -c '^ВЕРДИКТ: '` в `UROKI-FABRIKE.md` СОВПАДАЮТ.** Разошлись — арка НЕ закрыта (`ARKA §7`).
2. 🔴 **Кандидатов в уроки найдено — ЧИСЛОМ, с разбивкой по шести источникам и семи темам.** Меньше двадцати — источники прочитаны не все, сказать об этом прямо. Прочитано `kod_*.md` — 19 из 19, актов репетиции — 6 из 6.
3. **Вердиктов «В КАНОН» — сколько, и по каждому назван ДОМ и внесённая формулировка.** Отбраковано — сколько и почему.
4. **Правки канона консолидирующие:** назови, сколько абзацев ПЕРЕПИСАНО и сколько добавлено в конец. Если добавлено больше, чем переписано, — объясни.
5. **Гейт дневника зелёный** (фаза 4).
6. **`python3 _generator/tools/check_uroki.py` → rc=0.**
7. Всё, что снято или слито, — списком.

**Отрицательный вердикт несёт ОХВАТ:** «дублей не найдено, сверено X уроков из Y».

🔴 **Главное: отбраковать слабый урок — РЕЗУЛЬТАТ твоей работы, а не её провал.** Ты единственный в этой цепочке, кто не заинтересован в том, чтобы уроков оказалось много.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/docs _studio/zhurnal/2026-07-28_konspekt-l1                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/docs _studio/zhurnal/2026-07-28_konspekt-l1   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка** `arka/mat-kostyak` ✅ (`git --no-optional-locks branch --show-current`, rc=0). **Точка отката** — zip, а не коммит: `/tmp/zakrytie-backup/zona-pre-zakrytie.zip` (990 КБ, обе папки зоны). Почему zip, а не коммит, как предлагает §0: зона не чиста, и в ней лежит `kod_revizia.md` — файл ПАРАЛЛЕЛЬНО РАБОТАЮЩЕГО захода. Коммит по путям зоны взял бы `.git/index.lock` и заморозил бы чужой файл в середине его записи; zip не трогает индекс вовсе. Это же — предмет урока (см. ниже, критика входа).

### Три оспоренные предпосылки — до работы, как требует §1

1. 🔴 **Критерий готовности №1 не может сойтись на текущем файле и мимо него проходят три урока.** `grep -c '^### '` даёт 15, `grep -c '^ЦЕНА: '` — 18, `grep -c '^ВЕРДИКТ: '` — 1. Причины три, все механические: (а) уроки 1–3 записаны заголовком `## Урок N.`, а не `### `, поэтому **в гейт не входят ни строкой** — при этом у них есть ЦЕНА и стоит «⚠ Вердикт не поставлен»; (б) в счёт `### ` попадает строка-рыба из кодового блока шаблона (стр. 12), в счёт `ВЕРДИКТ: ` — рыба со стр. 14, они друг друга гасят случайно, а не по замыслу; (в) `check_uroki.py` считает уроки той же регуляркой `^### `, значит на уроки 1–3 он тоже слеп. **Поправка, которую вношу:** уроки 1–3 нормализую до `### `, после чего гейт покрывает все. Число сравнения станет 18=18 (17 живых + 1 рыба).
2. 🔴 **Гейт фазы 4 в заходе даёт ЛОЖНО-ЗЕЛЁНОЕ.** Заданный греп `grep -n "урок фабрике\|УРОК ФАБРИКЕ" SESSIYA.md` → rc=1, совпадений 0. Но `grep -i "урок"` даёт три строки, и `SESSIYA.md:61` — **«Урок фабрике (кандидат, цена есть): устаревший черновик плана блокирует коммит владельца…»**: заглавная «У» и строчное «фабрике» не ловятся ни одной из двух альтернатив шаблона. Урок живёт в дневнике, которого закрывающая сессия «по устройству не читает», — то есть ровно тот случай, против которого гейт заведён, а гейт при нём зелёный. Гоняю гейт в форме `grep -in`, и он краснеет.
3. **«Их должны быть ДЕСЯТКИ» — предпосылка верна, проверено до чтения.** `awk` по секциям `## УРОКИ ФАБРИКЕ` внутри `kod_*.md` даёт **61 урок** в 18 файлах из 19 (`kod_revizia.md` — 0, заход ещё идёт), плюс 17 в `UROKI-FABRIKE.md`. То есть 78 сырых кандидатов ДО дедупликации и до чтения репетиции, `PRAVKI` и дневника. Ожидание захода подтверждается с запасом; работа сместится с добычи на отбраковку и слияние.

### Ходы

- **Фаза 1 — добыча, 5 субагентов параллельно** (ровно предел §1), все read-only, каждому свой источник: `kod_*` группа A (9 файлов) · `kod_*` группа B (10 файлов) · шесть актов репетиции · `PRAVKI.md` · `SESSIYA.md` + `PRAVKI-final.md`. Задание каждому — «кандидат + ЦЕНА дословно из файла + адрес строки», формулировать закон им НЕ поручается: судить и формулировать буду я (`kod_zakrytie §Фаза 1`). Группы A/B извлекают из ОДНИХ файлов и уроки, и оспоренные предпосылки из `## ПЛАН`/`## ОТЧЁТ` — читать файл дважды двум агентам смысла нет.
- **Фаза 2 — суд.** Свожу в один список, дедуплицирую (61 урок исполнителей и 17 в `UROKI-FABRIKE.md` пересекаются: часть перенесена аналитиком), гоняю три проверки §Фаза 2 по каждому. Вердикт строкой `ВЕРДИКТ: ` под каждым `### ` — включая нормализованные 1–3.
- **Фаза 3 — канон, консолидируя.** Дома: `RUKOVODSTVO-zahodami.md`, `GIT-disciplina.md`, `KONSTITUCIYA.md`, `_studio/konvejer/*/DOK.md`, `_studio/zhurnal/_TEMPLATE-zahod.md`. Считаю переписанные и дописанные абзацы отдельно (критерий №4). Где можно — гейт вместо абзаца.
- **Фаза 4 — durable + гейт дневника** в исправленной форме.
- **Финал:** `check_uroki.py` → rc=0, оба грепа сходятся, коммит зоны двумя ходами.

**Чего НЕ делаю.** В `teorkat-vvedenie/` не пишу ни байта (акты и `PRAVKI.md` читаю — они названы обязательными источниками, чтение зоны не нарушает). `_studio/docs/KARTA.md` — новых `.md` не завожу, поэтому регистрация не нужна; если понадобится, готовая строка уйдёт в `## ВОПРОСЫ`, как велит контракт зоны.

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
