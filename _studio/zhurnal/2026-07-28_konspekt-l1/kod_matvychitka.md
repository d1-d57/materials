# Канал исполнителя — matvychitka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — это математическая вычитка: надо проверять доказательства, а не применять правила.
> ⏰ **ЛЕКЦИЯ СЕГОДНЯ ВЕЧЕРОМ, осталось около трёх часов.** Работай быстро и параллельно (§2).

КОНТЕКСТ. Лекция «Зачем нужны категории», онлайн, сегодня вечером. Лента текстов слайдов — `teorkat-vvedenie/raskadrovka/teksty/*.md`, **30 разделов** после реза (заход `kod_rez.md`). Дек собран из неё.

🔴 **ПОВОД. Владелец нашёл на слайде рассуждение, которое не смог проверить глазами, и это его главный страх перед лекцией: «сейчас у нас на слайдах будет какая-то чушь».** Разбор конкретного места — в §3, там же образец правильной записи. Диагноз общий: **отображения не выписаны, и из текста невозможно проверить, куда что идёт.** Математика при этом оказалась верной — но лектор не смог в этом убедиться за десять секунд, а значит на сцене не сможет тем более.

ЦЕЛЬ: **пройти по всем утверждениям, которые реально напечатаны на слайдах, и довести формулировку и доказательство до состояния, проверяемого глазами.** Не переписать красивее — сделать **проверяемым**.

Приёмка — по ОТЧЁТУ.
⚠ **Ты работаешь ТОЛЬКО с лентой.** Дек пересобирается отдельно и потом; `teorkat-vvedenie/src/` не трогай вовсе.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `teorkat-vvedenie/DOKAZATELSTVA-L1/` (новая папка, заводишь сам) и `_studio/zhurnal/2026-07-28_konspekt-l1/kod_matvychitka.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно». ⚠ **Ленту `raskadrovka/` и дек `src/` НЕ ТРОГАЙ ВОВСЕ** — они правятся отдельно; ты только читаешь.
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-28_konspekt-l1/kod_matvychitka.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА — собрать ОДИН документ доказательств. Ничего не искать, всё уже есть

🔴 **ПРОДУКТ: `teorkat-vvedenie/DOKAZATELSTVA-L1/` — исходник `.md` + собранный `view.html` (`build_doc.py`).** Это шпаргалка лектора на сегодня: он ни разу не читал эти доказательства подряд и хочет за полчаса убедиться, что умеет их доказать.

**Новых источников НЕ ищи. Книг не читай, в веб не ходи.** Всё лежит в двух местах, и владелец считает, что этого достаточно:
- `teorkat-vvedenie/KONSPEKT-L1/` — вкладки 06–12 (разделы 3 и 4);
- `_studio/zhurnal/2026-07-28_konspekt-l1/RESERCH-dolgi.md` — ночной ресёрч, Часть I: десять долгов, где доказательства были неполными или неверными, там же исправленные.

**ОХВАТ — только разделы 3 и 4** (вкладки 06–12). Разделы 1 и 2 **не трогай**: владелец сказал прямо, что там либо всё известно, либо доказывать нечего.

**ОТБОР — только то, что реально ДОКАЗЫВАЕТСЯ на лекции.** Не всё, что названо утверждением. Не упражнения. Не то, что проговаривается голосом («функтор сохраняет изоморфизмы», «обратный морфизм единствен»). Ожидание владельца: **6–7 утверждений, максимум 10 на оба раздела.** Получилось сильно больше — ты набрал лишнего, режь.

### 🎯 ГЛАВНОЕ: документ строится ЛИНИЯМИ, а не списком

Владелец: *«есть длинная линия, где мы идём от истории про то, что инъекции не дают функтор, к теореме Брауэра, и там предполагается, что каждое следующее утверждение мотивирует предыдущее… а у нас это на данный момент не так»*.

⇒ Каждая линия — **связная последовательность от начала до конца**, где после каждого утверждения видно, **зачем оно и что мотивирует следующее**. Линий, насколько мы знаем, три; найдёшь четвёртую — добавь:

**Линия A. «Конструкция не функториальна» → теорема Брауэра.** Самая важная и самая непонятная сейчас.
**Линия B. Естественных преобразований нет / сколько их бывает** — до утверждения про автоморфизмы.
**Линия C. Стоун + основная теорема** об эквивалентности.

**Гипотеза о линии A — проверь её, а не принимай на веру.** Похоже, всю линию держит один приём: **ретракт переживает функтор.** Если $r\circ s=\mathrm{id}_A$, то $Fr\circ Fs=F(r\circ s)=F(\mathrm{id}_A)=\mathrm{id}_{FA}$, то есть $FA$ — ретракт $FB$. Отсюда два разных употребления, и в этом вся драматургия:
- **прямое:** нашли ретракт, у которого образ ретрактом не является ⇒ **функтора нет** (центр группы; правило $X\mapsto\mathrm{Inj}(X,[k])$);
- **обратное:** функтор ЕСТЬ (например $\pi_1$), а образ ретрактом не является ⇒ **ретракции нет** ⇒ Брауэр: $S^1$ не ретракт $D^2$, ибо $\pi_1(S^1)=\mathbb Z$ не ретракт $\pi_1(D^2)=0$.

Если это так — **скажи это прямо одной фразой в начале линии**, и тогда каждый шаг мотивирован. Если не так — покажи, чем линия держится на самом деле.

🔴 **Задача Вербицкого — отдельно, владелец её не понимает и готов выкинуть.** Дословно: *«я даже формулировки не понимаю: „не существует множество меньшего числа элементов, согласованное с отображением“ — что значит согласованное? Функтора не существует? В общем, непонятно, при чём здесь Брауэр. Может, если это не мотивирует Брауэра, вообще уберём»*. Ресёрч по ней отработал (`RESERCH-dolgi`, долг 1) и сказал: **ответ «да по инструменту, нет в редакции задачи 79»**, а настоящий близнец Брауэра — **утверждение 75**, не 79. ⇒ **Твой ход: либо переформулируй так, что связь с Брауэром видна с первого чтения, либо честно предложи снять и скажи, чем линия закрывается без неё.** Половинчатого ответа не давай.

### 🔴 ЗАКОН ЗАПИСИ — то, ради чего заход

> **Каждое отображение выписано явно: что куда переходит. Каждое равенство композиций проверено на элементе. Каждый нестандартный термин определён на месте. Читатель убеждается в верности ГЛАЗАМИ, ничего не достраивая в уме.**

Владелец: *«кратко, но содержательно все шаги должны быть написаны»* и *«краткость и чёткость — общий принцип для всего, что мы делаем сегодня»*. **Это не формализм ради формализма — наоборот, ради краткости: выписанная стрелка короче абзаца, который её описывает.**

**Живой образец — место, на котором владелец споткнулся** (центр группы). Математика ВЕРНА, поймана непрозрачность записи:

❌ **Было:** «возьмём вложение $s\colon C_2\to S_3$ подгруппы транспозиции и знак $\mathrm{sgn}\colon S_3\to C_2$; транспозиция нечётна, поэтому $\mathrm{sgn}\circ s=1_{C_2}$. Функтор сохранил бы эту пару, но $Z(C_2)=C_2$, $Z(S_3)=1$, а $C_2\to 1\to C_2$ тождественным не бывает»

✅ **Стало:**
> **Центр** $Z(G)=\{g\in G: gh=hg\ \forall h\in G\}$
> $\mathbb Z_2=\{0,1\}$, $s\colon\mathbb Z_2\to S_3$: $\;1\mapsto(12)$ · $\mathrm{sgn}\colon S_3\to\mathbb Z_2$: чётные $\mapsto 0$, нечётные $\mapsto 1$
> $\mathrm{sgn}\circ s\colon 1\mapsto(12)\mapsto 1$, значит $\mathrm{sgn}\circ s=\mathrm{id}$ — то есть $\mathbb Z_2$ ретракт $S_3$
> $Z(\mathbb Z_2)=\mathbb Z_2$ (абелева) · $Z(S_3)=\{e\}$ (у $(12)$ и $(123)$ общей коммутации нет)
> Функтор дал бы $\mathrm{id}=Z(\mathrm{sgn})\circ Z(s)\colon\mathbb Z_2\to\{e\}\to\mathbb Z_2$ — постоянное, не тождество ∎

**Что владелец не смог достроить:** направление вложения ($\mathbb Z_2$ **в** $S_3$) из фразы не читается; обозначение $C_2$ он прочёл как свободную группу; определения центра нет, и приходится вспоминать. **Все три лечатся выписыванием, а не добавлением слов.**

🔴 **ОБОЗНАЧЕНИЯ — решение владельца: писать $\mathbb Z_2$, а не $C_2$.** Приведи к этому весь документ и заведи единый словарь обозначений в шапке. Одно понятие — одно обозначение по всему тексту.

### Как работать — быстро

1. **Сам, ~10 мин:** пройди вкладки 06–12 и `RESERCH-dolgi` Часть I, выпиши **реестр**: что доказываем, в какую линию входит, где лежит доказательство (конспект / ресёрч / нет нигде). Напечатай реестр в `## ПЛАН` — владелец увидит его первым и сможет вмешаться.
2. **До 3 субагентов параллельно, ~20 мин:** по линии на субагента. Каждому — закон записи дословно, его утверждения, адреса источников. Собирают выкладки, приводят к закону записи, возвращают готовый кусок. Не коммитят.
3. **Сам, ~15 мин:** свести в один документ, проверить сквозную связность («что мотивирует что» — читается ли), единый словарь обозначений, собрать `build_doc.py`, прогнать `check_view.py`.

⚠ **Где источники расходятся — верь ресёрчу, он верифицирован четырьмя независимыми верификаторами и в двух местах развернул вердикты конспекта.** Расхождение назови отдельной строкой отчёта.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. **Документ существует, собран `build_doc.py`, `check_view.py` → rc=0.**
2. **Утверждений 6–10.** Больше — объяснено, почему без них нельзя.
3. **Линий 3 (или больше, если нашёл), у каждой видна связность:** после каждого утверждения написано, зачем оно и что мотивирует дальше. **Утверждений, висящих без мотивировки, — ноль.**
4. 🔴 **Отображений, упомянутых, но не выписанных явно, — НОЛЬ.** Охват в вердикте: «сверено X из X».
5. **«Композиция тождественна» без проверки на элементе — ноль случаев.**
6. **Нестандартные термины определены на месте** (центр, ретракт, естественное преобразование…): проверено, ненайденных определений 0.
7. **Обозначения единые**, $C_2$ в документе не встречается (греп).
8. **Вердикт по задаче Вербицкого дан однозначный** — переформулирована или снята, с объяснением.
9. **Найденные математические ошибки — списком**, что было и что стало.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «ошибок нет», а «ошибок нет, сверено X из Y».

🔴 **Страх владельца назван им дословно: «самое страшное — сейчас у нас на слайдах будет какая-то чушь». Утверждение, которое ты сам не смог проверить за десять секунд, в документ не идёт: либо доводи, либо выноси в отчёт как проблемное.**

### СПРАВОЧНО (не задача этого захода)

Пройди все пять файлов ленты и выпиши **список того, что реально напечатано на слайдах и разбирается с доказательством**. Владелец: *«мне нужен список утверждений, которые на слайдах будут и которые важны… примерно 5, суммарно 6, ну не больше 7 в разделах 3 и 4, а в разделах 1 и 2 по пальцам пересчитать»*.

⚠ **Не путай с полным списком конспекта.** В `KONSPEKT-L1/` их около шестидесяти, но большинство на слайд не идёт (например «функтор сохраняет изоморфизмы» или «обратный морфизм единствен» — это проговаривается голосом). **Считай по ЛЕНТЕ, а не по конспекту**, и включай только то, что имеет на слайде формулировку И разбирается.

Таблица в `## ПЛАН`: № · раздел ленты · формулировка одной строкой · есть ли доказательство на слайде · разряд (`ПОЛНОЕ`/`СКЕТЧ`/`ИДЕЯ`).
**Ожидание владельца — порядка 10–14 позиций на всю лекцию.** Вышло сильно больше — скажи и объясни, что ты посчитал лишнего.

### Фаза 2 (ДО ЧЕТЫРЁХ СУБАГЕНТОВ ПАРАЛЛЕЛЬНО, ~30 мин) — вычитка по файлам

Зоны не пересекаются: `A` · `B` · `C`+`D` · `E`. Каждому даёшь **дословно** закон записи (ниже) и его кусок реестра. Субагенты не коммитят.

### 🔴 ЗАКОН ЗАПИСИ — то, ради чего заход

> **Каждое отображение выписано явно: что куда переходит. Каждое равенство композиций проверено на элементе. Читатель обязан убедиться в верности ГЛАЗАМИ, не достраивая ничего в уме.**

Это **не длиннее**, это **точнее**. Требование владельца дословно: *«где ты пишешь какое-то отображение, оно должно быть прям чётко записано; где ты пишешь, что композиция тождественная, должно быть прописано это. Кратко, но содержательно все шаги должны быть написаны»*.

**Живой образец — место, на котором владелец споткнулся** (`C-zapret-retrakt`, центр группы). Математика ВЕРНА, поймана только непрозрачность записи:

❌ **Было:** «возьмём вложение $s\colon C_2\to S_3$ подгруппы транспозиции и знак $\mathrm{sgn}\colon S_3\to C_2$; транспозиция нечётна, поэтому $\mathrm{sgn}\circ s=1_{C_2}$. Функтор сохранил бы эту пару, но $Z(C_2)=C_2$, $Z(S_3)=1$, а $C_2\to 1\to C_2$ тождественным не бывает»

✅ **Стало:**
> $C_2=\{1,a\}$, $s\colon C_2\to S_3$: $\;a\mapsto(12)$ · $\mathrm{sgn}\colon S_3\to C_2$: чётные $\mapsto 1$, нечётные $\mapsto a$
> $\mathrm{sgn}\circ s\colon a\mapsto(12)\mapsto a$, значит $\mathrm{sgn}\circ s=\mathrm{id}_{C_2}$
> $Z(C_2)=C_2$ (абелева) · $Z(S_3)=\{e\}$ (у $(12)$ и $(123)$ нет общей коммутации)
> Функтор дал бы $\mathrm{id}_{C_2}=Z(\mathrm{sgn})\circ Z(s)\colon C_2\to\{e\}\to C_2$ — постоянное, не тождество

**Что владелец не смог достроить и почему:** направление вложения ($C_2$ **в** $S_3$, а не наоборот) из фразы не читается, а $C_2\cong S_2$ — он прочёл «центр это все $S_2$» и потерял нить. Плюс приплёл свободную группу, которой здесь нет вовсе. **Всё это лечится выписанными стрелками, а не добавлением слов.**

**По каждому утверждению реестра проверь пять вещей:**
1. **формулировка полна** — все условия на месте, кванторы не потеряны;
2. **все отображения выписаны** — задано, что куда идёт, на образующих или на элементах;
3. **композиции проверены** — «тождественна» подтверждено вычислением на элементе, а не словом;
4. **доказательство проверяется глазами** за десять секунд, без достройки;
5. 🔴 **математика ВЕРНА.** Нашёл настоящую ошибку — **чини и назови отдельной строкой**: это дороже любой стилистики.

**Границы:** формулы не «упрощай» · длину не экономь за счёт п.1–4 (лучше выкинуть утверждение целиком, чем оставить непроверяемым) · `<figure>`/SVG не трогай · других разделов не касайся · бюджет 650 знаков на раздел держи, но **точность важнее бюджета** — вылез, скажи, на сколько и где.

### Фаза 3 (сам, ~15 мин) — правки ресёрча и сборка

**Внеси из `_studio/zhurnal/2026-07-28_konspekt-l1/RESERCH-dolgi.md` то, что касается напечатанного** (читай адресно, целиком не надо — 105 КБ):
- **Часть I, долг 6:** «изоморфизма категорий между $\mathbf{FinBool}$ и $\mathbf{FinSet}^{\mathrm{op}}$ нет» — **утверждение ВЕРНО**, доказательство в две фразы (пустое множество одно, вырожденных булевых алгебр — класс). Если это на слайде есть — поставь доказательство;
- **Часть I, долг 2:** формулировка про раскраски — «не существует функтора, совпадающего с этим правилом на объектах» (не «сопоставление»);
- **Часть I, долг 4:** утверждение про $\pi_1$ без отмеченной точки — формулировка конспекта недоказуема как написана, в досье лежит доказуемая;
- **Часть III, п.1 (Ламбек):** если на слайде есть — только «эквивалентность категорий», **без асимметрии кругового хода** (опровергнута, Theorem 11.3 говорит обратное).

Потом: `gejt_lenty.py` → пересобрать `view.html` (`build_doc.py`) → `check_view.py` (rc=0) → коммит.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. **Реестр утверждений напечатан**, число названо. Порядка 10–14; сильно больше — объяснено.
2. 🔴 **Утверждений, где отображение упомянуто, но не выписано явно, — НОЛЬ.** Проверено по всему реестру, охват в вердикте: «сверено X из X».
3. **Утверждений, где сказано «композиция тождественна» без проверки на элементе, — ноль.**
4. **Найденные математические ошибки — списком**, каждая с тем, что было и что стало. Список пуст — так и напиши, но только после сплошной проверки.
5. **Правки ресёрча внесены**: применено X из 4, неприменённые — с причиной «на слайде этого нет».
6. `check_view.py` → rc=0, `view.html` пересобран.
7. Разделов в ленте — 30, **новых не заводить**; знаков сверх 650 — назвать числом, если появились.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «ошибок нет», а «ошибок нет, сверено X утверждений из Y».

🔴 **Главный страх владельца назван им дословно: «самое страшное — сейчас у нас на слайдах будет какая-то чушь». Твоя работа — не стиль, а его спокойствие. Утверждение, которое ты сам не смог проверить за десять секунд, оставлять на слайде нельзя: либо чини, либо снимай и пиши в отчёт.**

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-28_konspekt-l1/kod_matvychitka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-28_konspekt-l1/kod_matvychitka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Шаблонная команда коммита в §4 называет путь, который контракт зоны того же захода объявляет «НЕ ТРОГАЙ ВОВСЕ»
§4 даёт готовые строки с `-- teorkat-vvedenie/raskadrovka`, а КОНТРАКТ ЗОНЫ выше пишет: «⚠ Ленту `raskadrovka/` и дек `src/` НЕ ТРОГАЙ ВОВСЕ». Исполнитель обязан выбрать между двумя обязательными пунктами одного файла, и выбирает молча.
**ЦЕНА: у меня — ноль, противоречие поймано до исполнения. Но неверный выбор здесь стоит ровно того, от чего §4 и защищает: staged чужая зона, которую правят параллельно (заход сам пишет «дек пересобирается отдельно и потом»), то есть инцидент 21.07 — коммит на 89 файлов вместо трёх. Шаблон `bootstrap_zahod.py` должен подставлять в §4 ПУТИ ЗОНЫ из `--zone`, а не пример.**

### Заход содержит две разные задачи с двумя разными критериями готовности, и вторая помечена «не задача этого захода», но требует работы
§2 — собрать `DOKAZATELSTVA-L1/`. Дальше секция «СПРАВОЧНО (не задача этого захода)» несёт вторую полноценную задачу: «Фаза 2» на четырёх субагентах, «Фаза 3», ВТОРОЙ дословно продублированный ЗАКОН ЗАПИСИ, ВТОРОЙ КРИТЕРИЙ ГОТОВНОСТИ на 7 пунктов — и требование `gejt_lenty.py` → пересобрать `view.html` → коммит, то есть править ленту, что контракт зоны запрещает.
**ЦЕНА: часть прохода ушла на разбор, какой из двух критериев мой, при лекции через три часа. Хуже: два пункта второго критерия («правки ресёрча внесены: применено X из 4» и «разделов в ленте 30») исполнять было НАДО — владелец явно перечислил четыре правки ресёрча, — то есть заголовок «не задача этого захода» врёт, и отличить в нём живое от мёртвого может только тот, кто уже прочёл весь заход. Один заход = один критерий готовности; вторую задачу выносить в отдельный файл.**

### Гейт `check_view.py` краснеет на том, что `build_doc.py` прячет НАМЕРЕННО, и толкает к правке, противоположной цели документа
`build_doc.py` распознаёт `*Доказательство…*` как зачин свёрнутого ката (`<details class="d-proof">`, СВЁРНУТ ПО УМОЛЧАНИЮ) и CSS-ом скрывает сам зачин. `check_view.py` затем не находит хвост блока в тексте выхода и печатает «ХВОСТ БЛОКА НЕ ВИДЕН В HTML» — формулировку, которая означает «текст съеден при отрисовке», хотя текст спрятан своим же движком по решению владельца.
**ЦЕНА: один ложный красный гейт (5 срабатываний) и цикл диагностики с чтением обоих инструментов — около десяти минут при трёхчасовом сроке. Опаснее другое: «естественная» починка по тексту гейта — засунуть тело доказательства внутрь ката — увела бы ВСЕ доказательства шпаргалки лектора под клики, то есть гейт подталкивал к решению, прямо противоположному назначению документа (лектор читает выкладки подряд за полчаса). Гейт обязан либо знать про свой же кат и молчать, либо сообщать «блок ушёл под `<details>`, это ожидаемо?» вместо «текст съеден».**

### Охват «разделы 3 и 4» не сходится с нумерацией ленты, и по буквальному чтению третья линия невозможна
§2 пишет: «ОХВАТ — только разделы 3 и 4 (вкладки 06–12)». В ленте `poryadok: 3` — блок `C`, `4` — блок `D`, а вкладки 06–12 конспекта покрывают `C`, `D` **и** `E`. Линия C захода («Стоун + основная теорема») живёт целиком в блоке `E`.
**ЦЕНА: при буквальном чтении «разделы 3 и 4 = блоки C и D» одна из трёх обязательных линий не собирается вовсе, и критерий готовности п. 3 («линий 3») провалился бы по вине формулировки охвата. Развилку решил в пользу вкладок 06–12, потому что они названы номерами. Охват задавать ОДНОЙ системой нумерации — либо вкладки конспекта, либо блоки ленты, не смешивая.**
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка** `arka/mat-kostyak` ✅ (`git branch --show-current`, rc=0). Зоны `DOKAZATELSTVA-L1/` в HEAD нет вовсе — точку отката не фабрикую. Прочитано: лента `C`/`D`/`E`, `RESERCH-dolgi.md` Часть I (долги 1,2,3,4,6). Ленту и `src/` не трогаю.

### Реестр: что доказываем, куда входит, где лежит доказательство

Отбор строгий: в документ идёт только то, что **напечатано на слайде И разбирается**. Считано по ЛЕНТЕ (блоки `C`, `D`, `E` = разделы 3–4), не по конспекту.

| № | линия | утверждение | лента | доказательство лежит |
|---|---|---|---|---|
| A1 | A | **Инструмент.** $r\circ s=1_A\Rightarrow Fr\circ Fs=1_{FA}$: ретракт переживает функтор. Обратная форма — критерий «пары с тождественной композицией внизу нет ⇒ функтора нет» | C:30, C:95 | одна строка, вывожу сам |
| A2 | A | $G\mapsto Z(G)$ не продолжается до функтора $\mathbf{Grp}\to\mathbf{Grp}$ | C:18 | конспект вкл. 06 (сильная форма) |
| A3 | A | $S^1$ не ретракт $D^2$ в $\mathbf{Top}$ | C:97 | конспект вкл. 06 |
| A4 | A | **Брауэр.** Всякое непрерывное $f\colon D^2\to D^2$ имеет неподвижную точку | C:101 | конспект вкл. 06; луч выпишу формулой |
| B1 | B | **Инструмент.** На группоиде компонента $\alpha_A$ — в точности $\mathrm{Aut}(A)$-эквивариантный морфизм | D:96 | конспект вкл. 09–10 |
| B2 | B | Естественного изоморфизма $\mathrm{Id}\Rightarrow(-)^{\ast}$ на группоиде $\mathcal V$ нет (поле с $\lambda^2\ne1$); для $(-)^{\ast\ast}$ препятствия нет | D:82, D:84 | конспект вкл. 09 |
| B3 | B | Для $BG$: $\mathrm{Nat}(\mathrm{Id},\mathrm{Id})\leftrightarrow Z(G)$ | D:133 | конспект вкл. 10 |
| B4 | B | Для $U\colon\mathbf{Grp}\to\mathbf{Set}$: $\mathrm{Nat}(U,U)\leftrightarrow\mathbb Z$, это $x\mapsto x^n$ | D:137 | конспект вкл. 10 |
| B5 | B | $\mathrm{Nat}(G,K_k)$ — в точности правильные раскраски; их число $P(G,k)$ | D:151 | конспект вкл. 10 |
| C1 | C | $\mathbf N$ и $\mathbf{FinSet}$ не изоморфны (а эквивалентны) | E:22, E:53 | конспект вкл. 11–12 |
| C2 | C | Строгий и полный функтор отражает изоморфизмы | E:116 | на слайде 3 шага, вкл. 11 |
| C3 | C | Конечная булева алгебра $\cong\mathcal P(\mathrm{At}(B))$ | E:193 | конспект вкл. 12 |

**Число: 12.** Потолок владельца — 10. Считаю честно: **10 содержательных применений + 2 инструмента линий** (A1 и B1). Инструменты снять нельзя — они и есть то, чем линия держится, и оба напечатаны на слайде словом «Утверждение».

**Не идёт в документ (и почему):** основная теорема об эквивалентности (`E:122` — на лекции **не доказывается**, «построение $G$ требует аксиомы выбора»; даю двумя строками «если спросят») · теорема Стоуна общая, Галуа, неразрешимость 5-й степени, $\mathbf{Mat}\simeq\mathbf{Vect}^{\mathrm{fd}}$, изоморфность трёх определений топологии (`E`) — напечатаны, но не разбираются · «функтор сохраняет изоморфизмы», «обратный морфизм единствен» — голосом · утв. 92 (ровно $n!$ преобразований $L\Rightarrow S$, долг 3 ресёрча) — **на ленте этого нет** · упражнения (`D:40`, `E:195`).

### Гипотеза захода про линию A — ПРОВЕРЕНА, верна

Всю линию держит один приём, и у него ровно два употребления. Прямое: ретракт нашли, внизу пары нет ⇒ **функтора нет** ($Z$). Обратное: функтор есть ($\pi_1$), внизу пары нет ⇒ **ретракции нет** ⇒ Брауэр. Пишу это первой фразой линии.

### Задача Вербицкого — вердикт: СНЯТЬ, и он уже исполнен резом

Два независимых довода. (1) **По ленте её на слайдах больше нет:** `C-zapret-retrakt.md:16` прямо пишет, что «пример с инъекциями выкинут по бюджету», иллюстрация «носитель против палитры» осиротела. (2) **По математике она Брауэра не мотивирует:** ресёрч (долг 1) показал, что там из двух стрелок ретракта работает одна — механизм не «ретракт переживает функтор», а «$\varnothing$ строго начально: в пустое ведёт только пустое». Близнец Брауэра — утв. 75, а не 79, но и оно с ленты снято тем же резом. **Чем линия закрывается без неё:** прямое употребление несёт $Z(G)$ — там обе стрелки работают по-настоящему, и центр возвращается в линии B (B3), то есть пример ещё и окупается дважды.

### Ходы

1. Реестр (этот) — напечатан.
2. 3 субагента параллельно — **не пишут, а снимают**: каждому свои вкладки конспекта, вернуть доказательства дословно + назвать пробелы. Почему не по заходу (субагент пишет кусок): закон захода — «утверждение, которое ты сам не смог проверить за десять секунд, в документ не идёт»; текст, сочинённый субагентом, мне всё равно пришлось бы перепроверять целиком, а сверка чужой выкладки дороже собственной. Математику всех 12 утверждений я проверил сам до найма.
3. Пишу документ сам, единый словарь обозначений, $\mathbb Z_2$ вместо $C_2$.
4. `build_doc.py` → `check_view.py` (rc=0) → коммит зоны.
5. Свежие субагенты-верификаторы по линии: атаковать на «где читателю приходится достраивать» + «где математика врёт».

**Оспариваю критерий готовности, п. 2** («утверждений 6–10»): при строгом отборе по ленте выходит 12, и меньше без потери напечатанного не получается — см. счёт выше. Если владелец хочет ровно 10, первыми снимаются C3 (единственное доказательство, которое за десять секунд глазами не проверяется — оно на семь шагов) и B5.

## ВОПРОСЫ — (заполняет исполнитель)

### 1. РЕГИСТРАЦИЯ — готовая строка, аналитику остался один ход

Завёл 5 новых `.md` (одна папка-документ). `_studio/docs/` мне READ-ONLY, поэтому строка — здесь. Вставить в `_studio/docs/KARTA.md`, **строка 69**, в конец перечисления проекта `../../teorkat-vvedenie/` (после блока про `KONSPEKT-L1/`), тем же разделителем ` · `:

> **`DOKAZATELSTVA-L1/` — 5 вкладок doc-вида** (**шпаргалка лектора на разделы 3–4 Л1: 12 утверждений, реально напечатанных на слайдах И разбираемых, доведённых до проверяемости глазами — каждое отображение выписано, каждая композиция посчитана на элементе, каждый термин определён на месте. Собрана линиями, а не списком: A «конструкция не функториальна → Брауэр» (4) · B «естественных преобразований нет / сколько бывает» (5) · C «изоморфизм против эквивалентности → Стоун» (3); после каждого утверждения стоит строка «Зачем и что дальше». Пятая вкладка «Находки» — **три настоящие математические ошибки в источниках**, из них одна напечатана на слайде (`D:147` «$\mathrm{Id}\Rightarrow(-)\otimes(-)$: только нулевое» без оговорки о поле — над $\mathbb F_2$ ложно, контрпример приведён), и семь мест, где слайд заявляет шаг, но не проводит его. Вид `build_doc.py teorkat-vvedenie/DOKAZATELSTVA-L1`, `check_view.py` зелёный. Собрана заходом `../zhurnal/2026-07-28_konspekt-l1/kod_matvychitka.md`**)

### 2. Правки, которые лежат ВНЕ моей зоны — нужен твой ход

Лента и конспект мне read-only. Каждая правка — одна строка, все проверены мной вручную, разбор в вкладке «Находки».

| # | где | что | цена, если не править |
|---|---|---|---|
| 1 | 🔴 лента `D-zapret-estestvennost.md:147` | в списке классификационных результатов «$\mathrm{Id}\Rightarrow(-)\otimes(-)$ на $\mathcal V$: только нулевое» — **дописать оговорку о поле** (как у соседней теоремы в строке 84) либо один раз вслух зафиксировать $\mathbb R$ | **на слайде ложное утверждение.** Над $\mathbb F_2$ есть ненулевое преобразование; и рядом стоят две родственные фразы, у одной оговорка есть, у другой нет — за это зацепятся |
| 2 | лента `E-dva-mira.md:18` | «объекты $\mathbf N$ — натуральные числа» → «**включая нуль**» | без нуля функтор $X\mapsto\lvert X\rvert$ со слайда `E:53` не определён на $\varnothing$, а эквивалентность ложна |
| 3 | лента `E-dva-mira.md:53` | к «$F\circ G=\mathrm{Id}_{\mathbf N}$» — оговорка, что нумерация на $[n]$ выбрана тождественной | иначе буквальное равенство неверно, а весь контраст слайда именно в нём |
| 4 | лента `E-dva-mira.md:24` | доказательство «не изоморфны» не закрывается: добавить слово «**каждый** класс» или вести довод через обратный функтор | шаг «образ объекта $3$ — трёхэлементное множество» на слайде не сделан (вывести его можно — через терминальный объект) |
| 4а | лента `E-dva-mira.md:120` | «изоморфизмы отражают — **только** строгие и полные» → «достаточно строгости и полноты» | как утверждение ложно: забывающий $U\colon\mathbf{Grp}\to\mathbf{Set}$ не полон, а обратимость отражает |
| 4б | лента `D-zapret-estestvennost.md:84` | «над полем с элементом $\lambda^2\ne1$» → «с **обратимым** элементом $\lambda$» | условию удовлетворяет любое поле ($\lambda=0$ годится), то есть оно ничего не отсекает, а доказательство пользуется им как отсекающим |
| 5 | конспект `09-dva-zapreta.md:90` | «при $\lambda=2$» → «берём тот $\lambda$, который дан условием» | шаг незаконен (в характеристике $3$ у двойки $\lambda^2=1$). На слайд не попало |
| 6 | конспект `10-skolko-byvaet.md:114`, `:124` | утв. 99 — вписать условие на поле в формулировку И в вывод | источник ошибки № 1 |
| 7 | конспект `11-dve-kategorii.md:87` | «$\mathbf N$ есть скелет $\mathbf{FinSet}$» → «**изоморфна** скелету» | противоречит собственному определению скелета (полная подкатегория) в строке 85; на ленте есть реплика лектора об этом (`E:108`) |
| 8 | конспект `12-miry.md:48` | флаг «рассуждение утв. 116 сюда не переносится — классы бесконечны с обеих сторон» — **снять, он неверен** | ресёрч (долг 6) это уже развернул: пустое множество ровно одно, а вырожденных булевых алгебр класс. Флаг держит доказанное утверждение в статусе недоказанного |

### 3. Развилки, где решает владелец

1. **Утверждений 12, твой потолок 10.** Считаю честно: 10 применений + 2 инструмента линий. Резать до 10 — снимать C3 (конечная булева алгебра, семь шагов, единственное доказательство не на десять секунд) и B5 (раскраски). Моё мнение: не резать, обе напечатаны на слайде словом «Теорема», и без доказательства ты их не отобьёшь.
2. **Задача Вербицкого — я её снял** (вердикт однозначный, два довода, разбор в «Находках»). Если хочешь вернуть — скажи, но тогда нужен носитель обратно в ленту, а рез его уже выкинул.
3. **Правка ресёрча про Ламбека (Часть III, п.1) неприменима:** грепом по всем пяти файлам ленты имени Ламбека нет вовсе — на слайд он не идёт. Если он должен там быть, это отдельная задача.
4. **Долг 6 (изоморфизма $\mathbf{FinBool}\cong\mathbf{FinSet}^{\mathrm{op}}$ нет) на слайдах не напечатан** — поставил его двумя фразами справочно, в поле вкладки C, помечено «на слайде этого нет». Хочешь на слайд — скажи.
5. **Долг 4 ($\pi_1$ без отмеченной точки) на ленте отсутствует** как утверждение. Полезную половину внёс: почему в A3 выбор точки безвреден ($\pi_1(S^1)$ абелева). Само утверждение не нёс — его на слайде нет.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и ЗАЧЕМ

Собрал `teorkat-vvedenie/DOKAZATELSTVA-L1/` — 5 вкладок, `.md` + `view.html`. Зачем именно так: цель захода — не «переписать красивее», а сделать выкладки проверяемыми глазами, поэтому документ строится **линиями** с обязательной строкой «Зачем и что дальше» под каждым утверждением (по ней же считается охват — числа руками не вписаны, `КОНСТИТУЦИЯ §10`).

**12 утверждений** = 10 содержательных применений + 2 инструмента линий. Линия A — 4, B — 5, C — 3. Пятая вкладка «Находки» — то, чего произносить нельзя.

**Гипотеза захода про линию A проверена и верна:** всю линию держит одно равенство $Fr\circ Fs=\mathrm{id}_{FA}$, прочитанное в две стороны (прямое — функтора нет, обратное — ретракции нет). Написано первой фразой линии.

**Задача Вербицкого — СНЯТА**, вердикт однозначный, два независимых довода: (1) её носитель уже выкинут резом с ленты (`C-zapret-retrakt.md:16`), (2) Брауэра она не мотивирует — там из двух стрелок ретракта работает одна, механизм «в пустое ведёт только пустое», а у Брауэра нужны обе. Линия закрывается центром группы, который вдобавок окупается дважды: в линии B он возвращается как **ответ** (B3).

### Найденные математические ошибки — списком

**Настоящие ошибки (3):**

1. 🔴 **Напечатано на слайде и ложно.** Лента `D:147`: «$\mathrm{Id}\Rightarrow(-)\otimes(-)$ на $\mathcal V$: только нулевое» — без оговорки о поле. Над $\mathbb F_2$ ложно: у одномерного пространства базис канонический (ненулевой вектор один), $\Delta_V(v)=v\otimes v$ линейно (аддитивность держится на $e\otimes e+e\otimes e=0$ в характеристике 2) и естественно, между разными размерностями изоморфизмов нет. **Было:** без условия. **Стало:** нужно «есть $\lambda\notin\{0,1\}$», то есть $k\ne\mathbb F_2$. Источник — конспект утв. 99 (`10-skolko-byvaet.md:114`, `:124`).
2. Конспект `09-dva-zapreta.md:90`: «при $\lambda=2$ отсюда $\eta_V=0$» — незаконный шаг, у двойки $\lambda^2\ne1$ есть не в каждом поле (характеристика 3). **Было:** подстановка двойки. **Стало:** брать $\lambda$ из условия. На слайд не попало. Плюс второй дефект того же места: условие «есть $\lambda$ с $\lambda^2\ne1$» выполнено в **любом** поле ($\lambda=0$), нужно «обратимый $\lambda$» — это уже и на ленте, `D:84`.
3. Конспект `11-dve-kategorii.md:87`: «$\mathbf N$ есть скелет $\mathbf{FinSet}$» противоречит своему же определению скелета как полной подкатегории (`:85`). **Стало:** «изоморфна скелету»; причина — не «числа против множеств» (в ZFC число и есть множество), а несовпадение множеств морфизмов.

**Неполные формулировки на ленте (5):** нужен нуль в $\mathbf N$ (`E:18`) · оговорка про тождественную нумерацию, без которой «$F\circ G=\mathrm{Id}$» буквально неверно (`E:53`) · незакрывающийся довод в «не изоморфны» (`E:24`) · «отражают — **только** строгие и полные» ложно как утверждение (`E:120`) · условие о поле ничего не отсекает (`D:84`). Все восемь позиций с разбором — вкладка «Находки», сводка для правки — `## ВОПРОСЫ` §2.

**Ошибка в моей собственной находке — поймана верификатором и исправлена.** Я написал, что пропущенный на слайде шаг «образ объекта $3$ трёхэлементен» из определения изоморфизма категорий **не выводится**, потому что мощность носителя не категорное понятие. Это неверно: выводится через терминальный объект ($H\{\ast\}=1$, дальше $HX=\lvert\mathbf N(1,HX)\rvert=\lvert X\rvert$). Дефект слайда в том, что шаг не сделан, а не в том, что он невозможен. Вывод теперь стоит в документе и попутно даёт третье, самое короткое доказательство C1a.

### Как проверил

- `build_doc.py` → rc=0, `check_view.py` → **rc=0 зелёный** (прогонял после каждой правки). Первый прогон был красный, 5 срабатываний — разбор в уроках фабрике.
- Обозначения: `grep "C_2\|1_{"` по всем пяти файлам → **0 вхождений**, единый $\mathbb Z_2$ и $\mathrm{id}$; тривиальная группа сведена к одному обозначению $\{e\}$.
- Связность линий: `grep -c "Зачем и что дальше"` → 4 + 5 + 3 = **12**, то есть висящих без мотивировки утверждений ноль.
- **Верификаторы — свежие субагенты, другим проходом, по линии на каждого, задача «найти ошибку, а не одобрить».** Улов: A — 3 дефекта, B — 6, C и находки — 4. Все приняты и внесены, кроме одного **отклонённого**: верификатор объявил теорему B2 ложной над $\mathbb F_3$ и привёл «контрпример» — но его семейство нулевое в размерностях $\ne1$, то есть это естественное преобразование, а не изоморфизм, а теорема про изоморфизм. Пересчитал сам: над $\mathbb F_3$ в размерности 2 инвариантная форма вынуждена быть нулевой ($B(e_1,e_1)=0$ из трансвекции, затем $2B(e_1,e_2)=0$), изоморфизма нет, теорема верна. Пересчёт вписан в документ.
- Самые дорогие из принятых правок: **теорема B5 как записана была ложна** (нет требования конечности графа — биекция с раскрасками верна всегда, а равенство их числа $P(X,k)$ нет); **формулировка A3 была шире доказательства** (ретракт допускает любое сечение, разбирался только случай $s=i$ — переписано на произвольное $s$); в B4 **стороны квадрата естественности были подписаны наоборот** и противоречили моему же словарю.

### Что НЕ трогал

Ленту `raskadrovka/` и дек `src/` — не открывал на запись ни разу, только читал. Конспект `KONSPEKT-L1/` — только чтение. `_studio/docs/` — только чтение (строка регистрации отдана аналитику готовой, `## ВОПРОСЫ` §1). Разделы 1 и 2 (блоки `A`, `B` ленты) — вне охвата, не касался. `gejt_lenty.py` не гонял: он про ленту, а лента не моя зона.

### Правки ресёрча: применено 2 из 4, одна справочно, одна неприменима

| правка | статус |
|---|---|
| долг 2 (формулировка «не существует функтора, совпадающего с правилом на объектах») | **внесена** — носитель (инъекции) с ленты снят резом, но урок формулировки применён к выжившему утверждению A2 |
| долг 4 ($\pi_1$ без отмеченной точки) | **внесена половина** — самого утверждения на ленте нет; полезная часть (почему в A3 выбор точки безвреден) стоит полем при A3 |
| долг 6 (изоморфизма $\mathbf{FinBool}\cong\mathbf{FinSet}^{\mathrm{op}}$ нет) | **справочно, на слайде этого нет** — поставил двумя фразами полем при C3, с пометкой. Довод усилен против ресёрча: нужен шаг «в $\mathbf{FinBool}$ КАЖДЫЙ класс изоморфных — собственный класс», без него «класс против класса» противоречия не даёт |
| Часть III п. 1 (Ламбек) | **неприменима** — грепом по всем пяти файлам ленты имени Ламбека нет вовсе |

Расхождение источников, названное отдельной строкой (как требует §2): **конспект `12-miry.md:48` держит флаг «рассуждение утв. 116 сюда не переносится — классы бесконечны с обеих сторон», и он неверен.** Ресёрч (долг 6) это уже развернул; пустое множество ровно одно. Верю ресёрчу, флаг надо снять — правка 8 в `## ВОПРОСЫ`.

### Открытое — возвращаться

1. **Список классификационных результатов на слайде `D:147` проверен НЕ ЦЕЛИКОМ.** Один пункт из четырёх оказался ложным; три остальных я не проверял, и вердикт на них не распространяется. Список стоит перепроверить целиком — это ровно тот жанр, где ошибка прячется.
2. **12 утверждений против потолка 10.** Развилка владельца, разбор в `## ВОПРОСЫ` §3.1.
3. Восемь правок вне зоны (лента и конспект) — исполнить некому, кроме владельца или аналитика.

### Необратимое

**Необратимого нет.** Ни одного удаления, переименования, перемещения, `git reset`/`checkout`. Все пять `.md` — новые файлы в новой папке; `view.html` порождён генератором. Ни один файл вне зоны не изменён.

### Время и токены

Один проход, около 55 минут. Шесть субагентов: три на снятие доказательств из конспекта (read-only) и три верификатора; суммарно у них ~221 тыс. токенов. Рейт-лимитов не было. **Отступление от §2 п. 2:** субагенты писали не куски документа, а снимали источники и атаковали готовый текст — причина названа в `## ПЛАН` до найма (сверять чужую выкладку дороже, чем написать свою, а закон захода запрещает нести в документ то, что я сам не проверил). Оценка себя: ставка сыграла — верификаторы нашли 13 дефектов, включая ошибку в моей же находке, чего проверка собственного текста собственными глазами не дала бы.

**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
