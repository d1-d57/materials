# Канал исполнителя — dobivka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — правки по живому деку.
> 🔴🔴🔴 **ЛЕКЦИЯ ИДЁТ ПРЯМО СЕЙЧАС. Владелец подключит твою версию в перерыве — у тебя примерно 20–25 минут. Все задачи ниже простые: почти всё это УДАЛЕНИЯ.**

КОНТЕКСТ. Лекция «Зачем нужны категории» читается в эту минуту. Дек — `teorkat-vvedenie/src/dist/index.html`, 31 секция, собран, работает.

Предыдущий заход (`kod_final.md`) успел 7 пунктов ТЗ из 20 и **честно перечислил несделанное**. Ты доделываешь ровно этот остаток — ничего сверх.

🔴 **ТЗ — `_studio/zhurnal/2026-07-28_konspekt-l1/PRAVKI-final.md`.** Читай его, но **только те §, что названы ниже**; остальное уже сделано, не переделывай.

ЦЕЛЬ: **закрыть остаток и пересобрать дек.**

Приёмка — по ОТЧЁТУ.
🔴 **СТОП-ПРАВИЛО: за 5 минут до конца — брось всё, пересобери, закоммить.** Дек в этот момент открыт у владельца на экране: **сломанная сборка = сорванная лекция**.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА — восемь пунктов, делать СВЕРХУ ВНИЗ

**Читать только:** `PRAVKI-final.md` (названные §) и ленту. Больше ничего.

1. 🔴 **§2 ФИБОНАЧЧИ.** В `A-krasivaya.md` (и порождённом `src/content/s01.md`) есть строка `{@2} Тождество 8 — числа Фибоначчи: диагональ треугольника Паскаля…` — **удалить эту строку целиком.** Само тождество 8 в таблице **остаётся**. Владелец наткнулся на это первым делом.

2. 🔴 **§1 ВИЗИТКА.** Единственный пункт с пометкой «железно обязательно», и он до сих пор не сделан. Разметку взять готовой:
   - **из буффона:** `buffon/src/` — слайд визитки;
   - **или** `git show 0c0a4a2:_generator/skeleton/sluzhebnye/vizitka.html` (и `vizitka.md`, `style.css`) — ветка `zahod/dek-paskal-v2` не влита, потому файлов нет в дереве.
   Скопировать в `teorkat-vvedenie/src/`, поставить **вторым слайдом**. ⚠ Это осознанное отступление от `G12`, разрешённое владельцем — **назови строкой в отчёте**. `_generator/` не трогай.

3. **§5 СЛАЙД 6** (проверка тождеств) — не тронут вовсе. Первая сцена: **само тождество** вместо списка из трёх пунктов. Вторая: короткий ответ «2, 3, 8 — не вид» · «5 — обе стороны вида, изоморфизма нет». **Вопрос жирным внизу снять** (он должен звучать до проверки). **Картинку из полосы убрать.** Про отмеченный элемент и раскраски **не писать**.

4. **§4 СЛАЙД 5.** Убрать нижнюю часть (две иллюстрации и нижний абзац), убрать полосу, оставить только список примеров видов и **центрировать по вертикали**. Графам писать «**графы с вершинами $U$**».

5. **Картинки пачкой — одним проходом:** слайд **10** (нижняя справа) · **12** (все пять) · **20** (первая и вторая сверху, оставить только нижнюю по центру) · **22** (иллюстрация справа + полоса) · **30** и **32** (по одной картинке). Удаления, делай разом.

6. **§3 СЛАЙД 4.** Подписать на фигуре буквы $E$, $E'$; подпись под диаграммой справа — биекция между $\binom{n}{2}$ и $\binom{n}{n-2}$.

7. **§18 СЛАЙД 21.** Убрать комментарий «здесь компонента одна, это элемент $x$».

8. **§9 СЛАЙД 12.** Заголовок по смыслу «Категории: множества с алгебраической структурой»; $\mathbf{Mat}$ и соответствия — в конец списка; список горизонтальный; нижнюю фразу снять.

🔴 **НЕ НАЧИНАТЬ:** переписывание слайдов 13 и 14 · новый слайд про инъекции · эквивалентность категорий в части 2 · панорама соответствий. Владелец говорит это голосом.

**Пересборка обязательна:** `build_doc.py` → `porodit.py` → `sverstat.py` → `build_deck --lint` → `build_deck` → `promer.py`; переполнение — `podognat.py`.

---

**КРИТЕРИЙ ГОТОВНОСТИ:**
1. 🔴 **Дек собран и открывается**, `build_deck --lint` rc=0, секций числом. Единственный обязательный пункт.
2. **Греп `Фибоначчи` по `src/content/` → 0 вхождений** (тождество в таблице при этом на месте).
3. **Визитка в деке есть** — назвать её id и позицию.
4. Переполнений 0.
5. Сделанные пункты — списком с номерами; несделанные — тоже списком.

🔴 **Лучше три правки и рабочий дек, чем восемь и сломанная сборка.**

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка** `arka/mat-kostyak` ✅ (rc=0). **Зона чиста в HEAD** (единственное несохранённое — сам этот заход, untracked) ⇒ точку отката не фабрикую, откат = HEAD `4e069ea`.

**Разведка дала два факта, меняющих способ исполнения п.2 (визитка):**
1. `sverstat.py` **стирает `src/slides/*` целиком** (`for old in (SRC/"slides").glob("*"): old.unlink()`) и порождает поток слайдов **программно** — `poryadok()`, а не из `brief.slide_order`. Рукописный `slides/sl-vizitka.html`, скопированный из буффона, был бы снесён первой же пересборкой, и притом молча.
2. `porodit.py` так же стирает **`src/content/*.md` и `src/illustrations/*`**. Значит ни `content/sl-vizitka.md`, ни `illustrations/vizitka-{photo,qr}.html` копированием тоже не живут.
⇒ **Визитка вставляется в `sluzhebnye()` + `poryadok()` в `src/tools/sverstat.py`** (это моя зона), разметка и CSS — взятые из `buffon/src/slides/sl-vizitka.html` и `buffon/src/shablon.html:136-149`, текст и картинки — инлайном, без `{{MD:}}`/`{{ILL:}}`, потому что оба каталога затираются. Base64-картинки кладу в **новый каталог `src/sluzhebnye/`** (генераторы его не трогают), `sluzhebnye()` их читает.

**ОСПАРИВАЮ КРИТЕРИЙ №2.** «Греп `Фибоначчи` по `src/content/` → 0 вхождений» — выполним, но правит он не тот файл: `content/` **порождается** `porodit.py` из ленты и затирается при каждой сборке. Ноль в `content/` достигается правкой `raskadrovka/teksty/A-krasivaya.md`; правка самого `content/s01.md` дала бы ноль на грепе и откат при сборке. Делаю по ленте, критерий проверяю после `porodit.py`. Заход это, к слову, знает («и порождённом `src/content/s01.md`») — расходится с ним только критерий.

**ОСПАРИВАЮ НУМЕРАЦИЮ СЛАЙДОВ в пп. 3–8.** `PRAVKI-final.md` собран в 19:20 по тогдашнему деку, а предыдущий заход дек **уже переставил и укоротил** (снял 23–24, поднял Эйленберга–Маклейна). Номера владельца указывают на состояние до этих правок ⇒ адресовать правки по номеру = риск попасть в чужой слайд. **Ищу цели по СОДЕРЖАНИЮ** (греп по характерной фразе), номер использую только как подсказку. Где содержание не опознаётся однозначно — не угадываю, пункт остаётся несделанным и уходит в отчёт строкой.

**Порядок исполнения** (стоп-правило: за 5 мин до конца — пересборка и коммит любой ценой):
1. п.1 Фибоначчи — снять строку в ленте `A-krasivaya.md` (1 правка, критерий №2).
2. п.2 Визитка — `sverstat.py` (критерий №3, «железно обязательно»).
3. **Пересборка и коммит здесь же** — чтобы обязательные пункты были в безопасности до того, как я полезу в мелочь.
4. пп. 5, 7, 4, 8, 3, 6 — удаления и текстовые правки по ленте, сколько влезет; после каждого блока сборка.

**Верификатор (§3):** операция лоссовая (удаления картинок и текста) — нужен, но полный субагент-проход в 20 минут не окупается. Заменяю программной сверкой: `build_deck --lint` rc=0 + число секций + `promer.py` (переполнений 0) + греп-критерии. Это слабее канона; называю это в отчёте, а не выдаю за верификатор.

## ВОПРОСЫ — (заполняет исполнитель)

1. 🔴 **ГОТОВАЯ СТРОКА РЕГИСТРАЦИИ — вставить в `_studio/docs/KARTA.md §6`** (мне этот файл READ-ONLY по контракту зоны; хук на этом красный, обойден с причиной, см. ОТЧЁТ):
   ```
   | `zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md` | заход-добивка: остаток ТЗ `PRAVKI-final.md` по живому деку L1 | исполнитель | 29.07.2026 |
   ```
   (колонки подогнать под фактическую шапку §6 — я в файл не заходил и её формат не сверял.)
2. **Нумерация слайдов в `PRAVKI-final.md` больше не адресует дек.** Пункты 3, 4, 6, 8 захода указывают слайды по номерам состояния 19:20; предыдущий заход дек переставил. Я адресовал по содержанию, где оно опознаётся однозначно, и остановился, где нет (см. ОТЧЁТ, несделанное). **Нужно решение владельца: какие именно слайды он имел в виду** — быстрее назвать заголовком, чем номером.
3. **Долг `sverstat.py` унаследован и не мой:** `poryadok()` строит поток программно, а `brief.slide_order` при этом в `brief.md` живёт и выглядит источником правды. Поле `vizitka: da` стоит там с 28.07 и слайда не породило — именно поэтому визитки в деке не было (G12 обещает порождение, генератор его не делает). Чинить это в 20 минут по живому деку я не стал.

## ОТЧЁТ — (заполняет исполнитель)

### КРИТЕРИЙ ГОТОВНОСТИ — по пунктам, с командой на каждое число

| # | критерий | как проверено | итог |
|---|---|---|---|
| 1 | дек собран, `--lint` rc=0, секций числом | `python3 _generator/build_deck.py teorkat-vvedenie/src` → `✓ dist/index.html (1412 КБ)`; `grep -c 'class="slide"' src/dist/index.html` | **rc=0 · секций 32** (было 31, +визитка) ✅ |
| 2 | греп `Фибоначчи` по `src/content/` → 0, тождество в таблице на месте | `grep -rn Фибоначчи src/content/ \| wc -l` → `0` · `grep -c 'F_{n+2}' src/content/s01.md` → `1` | **0 · тождество 8 стоит** ✅ |
| 3 | визитка в деке, назвать id и позицию | `grep -n 'id="sl-vizitka"' src/dist/index.html` | **`sl-vizitka`, ВТОРОЙ слайд** (после `sl-title`, перед `s01`) ✅ |
| 4 | переполнений 0 | `python3 src/tools/promer.py` | **зон 35 · переполнено 0 · кадров 51** ✅ |

Гейт ленты тоже зелёный: `build_doc.py` rc=0, `check_view.py` → `✓ ЗЕЛЁНЫЙ: текст не съеден, опасных формул нет`.

### СДЕЛАНО — 4 пункта из 8

1. **п.1 §2 Фибоначчи.** Снята строка `{@2} Тождество 8 — числа Фибоначчи: диагональ треугольника Паскаля…` — в **ленте** `A-krasivaya.md`, не в `content/s01.md`. *Зачем так:* `content/` порождается и затирается `porodit.py` при каждой сборке, правка выхода откатилась бы молча. Тождество 8 в таблице ($F_{n+2}$) на месте.
2. **п.2 §1 Визитка — «железно обязательно», сделано.** `sl-vizitka`, второй слайд. Разметка, гриды и текст взяты из буффона (`buffon/src/slides/sl-vizitka.html` + `buffon/src/shablon.html:136–149`), картинки — оттуда же base64-файлами. ⚠ **Осознанное отступление от `G12`, разрешённое владельцем — называю строкой, как требует заход.**
   *Способ отличается от буквы задания, и вот почему.* Заход велел «скопировать в `teorkat-vvedenie/src/`». Так нельзя: `sverstat.py` **стирает `src/slides/*` целиком**, а `porodit.py` — **`src/content/*.md` и `src/illustrations/*` целиком**. Скопированный слайд, его `.md` и обе картинки были бы снесены первой же пересборкой — а пересборка в этом же заходе обязательна. То есть буквальное исполнение дало бы «визитка есть» на моём экране и «визитки нет» на экране владельца. Поэтому визитка вставлена в **порождающий слой**: `sluzhebnye()` в `src/tools/sverstat.py` (разметка + CSS инлайном, без `{{MD:}}`/`{{ILL:}}`) и `poryadok()` (`flow` начинается `["sl-title", "sl-vizitka"]`). Ассеты лежат в **новом каталоге `src/sluzhebnye/`** — единственном, который не затирает ни один генератор. `_generator/` не тронут.
   Два отличия от буффона, оба вынужденные: `font-family: 'Glacial Indifference'` снят (в этом деке шрифт не подключён, взят его собственный `--t-body`), и `.ill-box > img` получил явные размеры — в буффоне картинки шли через `{{ILL:}}` и ловились правилом `.ill-box > svg`, здесь это `<img>` инлайном.
3. **п.4 §4 Слайд 5** (`s04` «Галерея видов», опознан содержанием — «остаются только примеры видов»). Сняты **обе** иллюстрации (нижняя горизонтальная «пять конструкций в ряд» и диаграмма двух этажей в правой полосе), полоса убрана (`.rail{display:none}`), список **центрирован по вертикали** (`align-content:center`, сетка на всю ширину). «Графам писать графы с вершинами $U$» — **уже стояло так**, не менял. Правка вёрстки — через штатный механизм `PRAVKI` в `sverstat.py`, не правкой выхода.
4. **п.3 §5 Слайд 6** (`s05` «Три поломки на языке видов», опознан по строке «Проверим: какие из тождеств — изоморфизмы видов?»). Первая сцена: список из трёх пунктов заменён **самим тождеством** $\sum(-1)^k\binom nk=0$. Вторая сцена: короткий ответ «**1, 4, 6, 7** — да · **2, 3, 8** — не вид · **5** — обе стороны вида, изоморфизма нет». Про отмеченный элемент и раскраски не пишется. Вопрос жирным внизу («Тождество 4: какая здесь биекция?») снят. Картинка из полосы (полный цикл на четырёх точках) снята.
   ⚠ **Допущение, назову его прямо:** «вывести САМО ТОЖДЕСТВО» — единственное число, а какое именно, ТЗ не говорит. Взял **пятое**: весь остаток слайда (биекция для 5, утверждение про $E^{+}$/$E^{-}$, случай $n=2$, упражнение) — про него. Если владелец имел в виду другое, правка на одну строку в ленте.
5. **п.7 §18 Слайд 21.** Комментарий снят — он нашёлся не там, где ждал заход: `D-zapret-estestvennost.md`, раздел «Ответ не нуль: центр и степени», строка `{@1-2} Компонента одна, это элемент $x\in G$; квадрат естественности…`. Опознан по содержанию ($BG$, естественные преобразования, центр) — совпадает с §18. Комментарий про забывающий функтор оставлен: §18 разрешает.

*(пунктов в списке пять, потому что п.1 и п.2 — два разных задания; «4 из 8» считаю по нумерации задания: сделаны 1, 2, 3, 4, 7.)*

### НЕ СДЕЛАНО — 3 пункта, и почему именно они

- **п.5 Картинки пачкой** (слайды 10 · 12 · 20 · 22 · 30 · 32) — **не начато**. Причина не время, а адресация: **слайдов 30 и 32 в деке больше нет.** Контентных слайдов 26, то есть максимальный номер владельца — 27. Предыдущий заход снял 23 и 24 и переставил Эйленберга–Маклейна, а `PRAVKI-final.md` собран в 19:20 по деку до этих правок. Пункт требует «делай разом», а разом по номерам попадёт в чужие слайды. Слайды 10 и 12 сверх того лежат за первым разделителем, где нумерация владельца расходится на единицу в зависимости от того, считал он разделители или нет: до разделителя обе гипотезы дают одно и то же (проверено — «слайд 5» = `s04` при любой), после — разные слайды. Угадывать на живом деке за 20 минут я не стал.
- **п.6 §3 Слайд 4** (подписать $E$, $E'$ на фигуре; подпись под диаграммой справа — биекция между $\binom{n}{2}$ и $\binom{n}{n-2}$) — **не начато**. Цель по номеру — `s03` «Требование красоты», и его фигура — квадрат с $\{a,b\}\to\{a',b'\}$, а не то, что описывает §3: ни $E$/$E'$, ни $\binom{n}{2}$ там нет, «диаграммы справа» с биекцией биномов тоже. Либо §3 адресует другой слайд, либо требует нарисовать новое. Правка SVG по догадке в дек, открытый на экране, — не та цена.
- **п.8 §9 Слайд 12** (заголовок «Категории: множества с алгебраической структурой», $\mathbf{Mat}$ и соответствия в конец, список горизонтальный, нижнюю фразу снять) — **не начато**. Кандидат по содержанию найден (`B-yazyk.md:78`, зоопарк категорий с $\mathbf{Mat}$/$\mathbf{Rel}$), но пункт из четырёх правок, включая перекомпоновку списка в горизонтальный, — это вёрстка, а не удаление. Стоп-правило дороже.

Ни один из трёх не «забыт»: все три — про адресацию по номерам, которая перестала работать. См. `## ВОПРОСЫ` п.2.

**Запрет захода соблюдён:** слайды 13 и 14 не переписывались, слайд про инъекции не заводился, эквивалентность категорий в часть 2 не вносилась, панорама соответствий не восстанавливалась.

### НЕОБРАТИМОЕ — отдельным списком

Всё восстанавливается из git: точка отката — **`4e069ea`** (HEAD на входе; зона была чиста, поэтому откатную фабрикацию не делал).

| что | где | чем восстанавливается |
|---|---|---|
| удалена строка про числа Фибоначчи | `raskadrovka/teksty/A-krasivaya.md` | `git show 4e069ea:teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` |
| удалены 2 фигуры (`<figure>` «пять конструкций в ряд» и `<figure class="mn">` диаграмма двух этажей), 40 строк | там же, раздел «Галерея видов» | тот же `git show 4e069ea:…` |
| удалена фигура «полный цикл на четырёх точках», 20 строк | там же, раздел «Три поломки на языке видов» | тот же `git show 4e069ea:…` |
| удалены 3 строки-пункта списка вердиктов + строка `{@2} **Тождество 4: какая здесь биекция?**` | там же | тот же `git show 4e069ea:…` |
| удалена строка `{@1-2} Компонента одна, это элемент $x\in G$…` | `raskadrovka/teksty/D-zapret-estestvennost.md` | `git show 4e069ea:teorkat-vvedenie/raskadrovka/teksty/D-zapret-estestvennost.md` |
| перезаписана запись `PRAVKI["s04"]` (ключ `board: 116` снят) | `src/tools/sverstat.py` | `git show 4e069ea:teorkat-vvedenie/src/tools/sverstat.py`; прежний текст сохранён комментарием рядом |
| перезаписаны выходы: `src/content/*.md` (26), `src/illustrations/*` (37→34), `src/slides/*`, `src/shablon.html`, `src/math/katex.json`, `src/tools/formuly.json`, `src/dist/index.html`, `raskadrovka/teksty/view.html` | `teorkat-vvedenie/src`, `raskadrovka/teksty` | порождаются заново из ленты: `porodit.py` → `sverstat.py` → `sobrat_formuly.py` → `katex_kesh.js` → `build_deck.py` |

**За зону не выходил.** `_generator/`, `_studio/docs/`, `buffon/` — только на чтение. Три файла новые: `src/sluzhebnye/vizitka-photo.html`, `src/sluzhebnye/vizitka-qr.html` (копии из буффона), и сам этот заход.

### ВЕРИФИКАТОР

Операция лоссовая (удалено 5 иллюстраций и 5 строк текста), но полного субагента другим проходом за 20 минут по живому деку я не запускал — **и не выдаю замену за него**. Что вместо: `build_deck --lint` rc=0 (структурные проверки + связность `data-ill`), число секций 32, `promer.py` — переполнено 0 из 35 зон, `check_view.py` зелёный, оба греп-критерия. Это ловит «сломалось» и «потерялась ссылка»; чего НЕ ловит — «удалили не то, что просили». Единственное предупреждение линтера, `illustrations/s14-kak-dokazyvayut-chto-funktora-net не используется`, — **унаследованное, не моё**: оно стояло и до моих правок (в первой же сборке захода, до единого удаления картинок).

### ОТКРЫТОЕ — «возвращаться»

1. Три несделанных пункта (5, 6, 8) — после того как владелец назовёт слайды **заголовками**, а не номерами.
2. `s04`: `column-count:2` и `--blok:22px` оставлены, хотя причина для них (доска, отбиравшая 290px) отпала. Без доски список, вероятно, встанет в одну колонку — но промер без них не гонялся. Снять при следующей подгонке.
3. Долг `brief.md` ⟷ `sverstat.py`: `slide_order` и `vizitka: da` в `brief.md` не влияют ни на что, поток строит `poryadok()`. Это и есть причина, по которой визитки не было. См. `## ВОПРОСЫ` п.3.

### ДОЛГИ (обходы, названные строкой, как требует §4)

- **Обход `pre-commit` на моём же пути** — `_studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md:1` документ-сирота (`KONSTITUCIYA §9`, ворота 5). Не «чужой долг» в смысле §4, а **конфликт двух обязательных указаний**: ворота требуют строки в `_studio/docs/KARTA.md §6`, контракт зоны отдаёт регистрацию аналитику и делает `_studio/docs/` READ-ONLY. Приоритет назван самим заходом — контракт зоны сильнее. Причина уехала в `INCIDENTY.md`; готовая строка регистрации — `## ВОПРОСЫ` п.1. **Закрывает аналитик одним ходом.**
- **`git_zona.py` не умеет многокорневую зону** (`--zone` берёт один префикс) — долг, уже названный предыдущим заходом. Обошёл двумя коммитами: `--zone teorkat-vvedenie` и `--zone _studio/zhurnal/.../kod_dobivka.md`. Первый шире моей зоны как префикс, но фактически в `teorkat-vvedenie/` вне `raskadrovka` и `src` изменений нет — проверено `show --stat`.

### ПРОГОН

Время: ~35 минут (заход обещал 20–25; сверх бюджета ушло на разведку затирающих генераторов — без неё визитка не выжила бы). Токены: ~135k. Модель: opus, как велел заход.

**КОММИТ:** три коммита, все в `arka/mat-kostyak`:
- **`2c11864`** — визитка вторым слайдом через порождающий слой, строка Фибоначчи снята из ленты (пп. 1–2, сделан сразу, чтобы обязательное было в git до работы над мелочью)
- **`be6eb65`** — план, вопросы, отчёт захода (с обходом ворот, причина выше)
- **`66b0119`** — слайд 5 разгружен, слайд 6 по сценам, комментарий про компоненту снят (пп. 3, 4, 7)

`git_zona.py check --zone teorkat-vvedenie` → ✅ · `git_zona.py check --zone _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md` → ✅
⚠ **Не вывезено на origin:** `--push` не давал, 34 коммита живут только на этой машине (`git_zona.py` предупреждает об этом сам). Вывоз — ход владельца.

## УРОКИ ФАБРИКЕ

### Заход велел копировать файлы в каталоги, которые генераторы затирают целиком
`kod_dobivka.md §2 п.2`: «Скопировать в `teorkat-vvedenie/src/`, поставить вторым слайдом» — и тут же «Пересборка обязательна». Но `sverstat.py` начинает с `for old in (SRC/"slides").glob("*"): old.unlink()`, а `porodit.py` — так же с `content/` и `illustrations/`. Буквальное исполнение = визитка есть до пересборки и исчезает после, **без единого сообщения об ошибке**: линтер зелёный, дек собран, слайда нет.
**ЦЕНА:** поймано разведкой до правки, поэтому визитка стоит в деке — но цена уже оплачена дважды в этом же заходе, обеими сторонами. Владелец: `brief.md` несёт `vizitka: da` **с 28.07**, `G12` обещает породить служебный слайд из этого поля, генератор его не порождает — визитки не было сутки, и заход 29.07 открывался словами «единственный пункт с пометкой железно обязательно, и он до сих пор не сделан». Предыдущий заход `kod_final.md` не сделал его вовсе. Оба раза дефект выглядел как «не успели», а был «не туда положили». **Что менять:** заход, который велит положить файл в проект с генератором, обязан назвать, порождается ли этот каталог, — либо не называть каталог вообще, а называть задачу.

### Критерий готовности проверял порождённый выход, а не источник
Критерий №2: «Греп `Фибоначчи` по `src/content/` → 0 вхождений». `src/content/` **порождается** `porodit.py` и затирается каждой сборкой. Критерий выполним правкой выхода — и тогда он зелёный, а следующая сборка возвращает удалённое. Тело того же захода при этом знает правильный адрес («в `A-krasivaya.md` **и порождённом** `src/content/s01.md`) — то есть заход противоречит своему же критерию.
**ЦЕНА:** здесь ноль, критерий сошёлся и по источнику и по выходу, потому что я правил ленту. Но цена этого класса в проекте уже оплачена **трижды и вслух**: `PRAVKI["s30"]` и `PRAVKI["s17"]` в `sverstat.py` несут пометки «🔴 СНЯТО 29.07: запись висела на несуществующем id и молча не применялась», а `PRAVKI["s15"]` переключался с ключа на ключ **дважды за один день** — и комментарий там прямо пишет: «ручная правка, оплаченная глазами на старом слайде, была наведена на чужой — а гейт этого не видит: ключ существует, css применяется, дек зелёный». **Что менять:** критерий на порождаемый файл — брак того же вида. Грепать источник, а порождённое проверять сборкой.

### Номера слайдов в ТЗ мертвеют быстрее, чем ТЗ исполняется
`PRAVKI-final.md` адресует правки номерами (слайд 5, 10, 12, 20, 22, 30, 32). Собран в 19:20; заход `kod_final.md`, работавший в 19:15–20:00, снял слайды 23 и 24 и переставил Эйленберга–Маклейна. К моменту моего чтения **слайдов 30 и 32 не существует**, а после первого разделителя нумерация владельца расходится с id на единицу.
**ЦЕНА:** три пункта задания (5, 6, 8) из восьми не сделаны — не по времени, а потому что цель не адресуется. Это 37% захода. Плюс ровно та же болезнь оплачена трижды внутри `sverstat.py` (см. урок выше): «ВСЕ id после B съехали на −1», «ключ стал наводить правку на другой слайд». **Что менять:** правки владельца записывать **заголовком слайда** (он живёт) рядом с номером (он умирает). Два лишних слова при диктовке против трёх невыполнимых пунктов на приёмке.
