# Канал исполнителя — dobivka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — правки по живому деку.
> 🔴🔴🔴 **ЛЕКЦИЯ ИДЁТ ПРЯМО СЕЙЧАС. Владелец подключит твою версию в перерыве — у тебя примерно 20–25 минут. Все задачи ниже простые: почти всё это УДАЛЕНИЯ.**

КОНТЕКСТ. Лекция «Зачем нужны категории» читается в эту минуту. Дек — `teorkat-vvedenie/src/dist/index.html`, 31 секция, собран, работает.

Предыдущий заход (`kod_final.md`) успел 7 пунктов ТЗ из 20 и **честно перечислил несделанное**. Ты доделываешь ровно этот остаток — ничего сверх.

🔴 **ТЗ — `_studio/zhurnal/2026-07-28_konspekt-l1/PRAVKI-final.md`.** Читай его, но **только те §, что названы ниже**; остальное уже сделано, не переделывай.

ЦЕЛЬ: **закрыть остаток и пересобрать дек.**

Приёмка — по ОТЧЁТУ.
🔴 **СТОП-ПРАВИЛО: за 5 минут до конца — брось всё, пересобери, закоммить.** Дек в этот момент открыт у владельца на экране: **сломанная сборка = сорванная лекция**.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА — восемь пунктов, делать СВЕРХУ ВНИЗ

**Читать только:** `PRAVKI-final.md` (названные §) и ленту. Больше ничего.

1. 🔴 **§2 ФИБОНАЧЧИ.** В `A-krasivaya.md` (и порождённом `src/content/s01.md`) есть строка `{@2} Тождество 8 — числа Фибоначчи: диагональ треугольника Паскаля…` — **удалить эту строку целиком.** Само тождество 8 в таблице **остаётся**. Владелец наткнулся на это первым делом.

2. 🔴 **§1 ВИЗИТКА.** Единственный пункт с пометкой «железно обязательно», и он до сих пор не сделан. Разметку взять готовой:
   - **из буффона:** `buffon/src/` — слайд визитки;
   - **или** `git show 0c0a4a2:_generator/skeleton/sluzhebnye/vizitka.html` (и `vizitka.md`, `style.css`) — ветка `zahod/dek-paskal-v2` не влита, потому файлов нет в дереве.
   Скопировать в `teorkat-vvedenie/src/`, поставить **вторым слайдом**. ⚠ Это осознанное отступление от `G12`, разрешённое владельцем — **назови строкой в отчёте**. `_generator/` не трогай.

3. **§5 СЛАЙД 6** (проверка тождеств) — не тронут вовсе. Первая сцена: **само тождество** вместо списка из трёх пунктов. Вторая: короткий ответ «2, 3, 8 — не вид» · «5 — обе стороны вида, изоморфизма нет». **Вопрос жирным внизу снять** (он должен звучать до проверки). **Картинку из полосы убрать.** Про отмеченный элемент и раскраски **не писать**.

4. **§4 СЛАЙД 5.** Убрать нижнюю часть (две иллюстрации и нижний абзац), убрать полосу, оставить только список примеров видов и **центрировать по вертикали**. Графам писать «**графы с вершинами $U$**».

5. **Картинки пачкой — одним проходом:** слайд **10** (нижняя справа) · **12** (все пять) · **20** (первая и вторая сверху, оставить только нижнюю по центру) · **22** (иллюстрация справа + полоса) · **30** и **32** (по одной картинке). Удаления, делай разом.

6. **§3 СЛАЙД 4.** Подписать на фигуре буквы $E$, $E'$; подпись под диаграммой справа — биекция между $\binom{n}{2}$ и $\binom{n}{n-2}$.

7. **§18 СЛАЙД 21.** Убрать комментарий «здесь компонента одна, это элемент $x$».

8. **§9 СЛАЙД 12.** Заголовок по смыслу «Категории: множества с алгебраической структурой»; $\mathbf{Mat}$ и соответствия — в конец списка; список горизонтальный; нижнюю фразу снять.

🔴 **НЕ НАЧИНАТЬ:** переписывание слайдов 13 и 14 · новый слайд про инъекции · эквивалентность категорий в части 2 · панорама соответствий. Владелец говорит это голосом.

**Пересборка обязательна:** `build_doc.py` → `porodit.py` → `sverstat.py` → `build_deck --lint` → `build_deck` → `promer.py`; переполнение — `podognat.py`.

---

**КРИТЕРИЙ ГОТОВНОСТИ:**
1. 🔴 **Дек собран и открывается**, `build_deck --lint` rc=0, секций числом. Единственный обязательный пункт.
2. **Греп `Фибоначчи` по `src/content/` → 0 вхождений** (тождество в таблице при этом на месте).
3. **Визитка в деке есть** — назвать её id и позицию.
4. Переполнений 0.
5. Сделанные пункты — списком с номерами; несделанные — тоже списком.

🔴 **Лучше три правки и рабочий дек, чем восемь и сломанная сборка.**

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka teorkat-vvedenie/src _studio/zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка** `arka/mat-kostyak` ✅ (rc=0). **Зона чиста в HEAD** (единственное несохранённое — сам этот заход, untracked) ⇒ точку отката не фабрикую, откат = HEAD `4e069ea`.

**Разведка дала два факта, меняющих способ исполнения п.2 (визитка):**
1. `sverstat.py` **стирает `src/slides/*` целиком** (`for old in (SRC/"slides").glob("*"): old.unlink()`) и порождает поток слайдов **программно** — `poryadok()`, а не из `brief.slide_order`. Рукописный `slides/sl-vizitka.html`, скопированный из буффона, был бы снесён первой же пересборкой, и притом молча.
2. `porodit.py` так же стирает **`src/content/*.md` и `src/illustrations/*`**. Значит ни `content/sl-vizitka.md`, ни `illustrations/vizitka-{photo,qr}.html` копированием тоже не живут.
⇒ **Визитка вставляется в `sluzhebnye()` + `poryadok()` в `src/tools/sverstat.py`** (это моя зона), разметка и CSS — взятые из `buffon/src/slides/sl-vizitka.html` и `buffon/src/shablon.html:136-149`, текст и картинки — инлайном, без `{{MD:}}`/`{{ILL:}}`, потому что оба каталога затираются. Base64-картинки кладу в **новый каталог `src/sluzhebnye/`** (генераторы его не трогают), `sluzhebnye()` их читает.

**ОСПАРИВАЮ КРИТЕРИЙ №2.** «Греп `Фибоначчи` по `src/content/` → 0 вхождений» — выполним, но правит он не тот файл: `content/` **порождается** `porodit.py` из ленты и затирается при каждой сборке. Ноль в `content/` достигается правкой `raskadrovka/teksty/A-krasivaya.md`; правка самого `content/s01.md` дала бы ноль на грепе и откат при сборке. Делаю по ленте, критерий проверяю после `porodit.py`. Заход это, к слову, знает («и порождённом `src/content/s01.md`») — расходится с ним только критерий.

**ОСПАРИВАЮ НУМЕРАЦИЮ СЛАЙДОВ в пп. 3–8.** `PRAVKI-final.md` собран в 19:20 по тогдашнему деку, а предыдущий заход дек **уже переставил и укоротил** (снял 23–24, поднял Эйленберга–Маклейна). Номера владельца указывают на состояние до этих правок ⇒ адресовать правки по номеру = риск попасть в чужой слайд. **Ищу цели по СОДЕРЖАНИЮ** (греп по характерной фразе), номер использую только как подсказку. Где содержание не опознаётся однозначно — не угадываю, пункт остаётся несделанным и уходит в отчёт строкой.

**Порядок исполнения** (стоп-правило: за 5 мин до конца — пересборка и коммит любой ценой):
1. п.1 Фибоначчи — снять строку в ленте `A-krasivaya.md` (1 правка, критерий №2).
2. п.2 Визитка — `sverstat.py` (критерий №3, «железно обязательно»).
3. **Пересборка и коммит здесь же** — чтобы обязательные пункты были в безопасности до того, как я полезу в мелочь.
4. пп. 5, 7, 4, 8, 3, 6 — удаления и текстовые правки по ленте, сколько влезет; после каждого блока сборка.

**Верификатор (§3):** операция лоссовая (удаления картинок и текста) — нужен, но полный субагент-проход в 20 минут не окупается. Заменяю программной сверкой: `build_deck --lint` rc=0 + число секций + `promer.py` (переполнений 0) + греп-критерии. Это слабее канона; называю это в отчёте, а не выдаю за верификатор.

## ВОПРОСЫ — (заполняет исполнитель)

1. 🔴 **ГОТОВАЯ СТРОКА РЕГИСТРАЦИИ — вставить в `_studio/docs/KARTA.md §6`** (мне этот файл READ-ONLY по контракту зоны; хук на этом красный, обойден с причиной, см. ОТЧЁТ):
   ```
   | `zhurnal/2026-07-28_konspekt-l1/kod_dobivka.md` | заход-добивка: остаток ТЗ `PRAVKI-final.md` по живому деку L1 | исполнитель | 29.07.2026 |
   ```
   (колонки подогнать под фактическую шапку §6 — я в файл не заходил и её формат не сверял.)
2. **Нумерация слайдов в `PRAVKI-final.md` больше не адресует дек.** Пункты 3, 4, 6, 8 захода указывают слайды по номерам состояния 19:20; предыдущий заход дек переставил. Я адресовал по содержанию, где оно опознаётся однозначно, и остановился, где нет (см. ОТЧЁТ, несделанное). **Нужно решение владельца: какие именно слайды он имел в виду** — быстрее назвать заголовком, чем номером.
3. **Долг `sverstat.py` унаследован и не мой:** `poryadok()` строит поток программно, а `brief.slide_order` при этом в `brief.md` живёт и выглядит источником правды. Поле `vizitka: da` стоит там с 28.07 и слайда не породило — именно поэтому визитки в деке не было (G12 обещает порождение, генератор его не делает). Чинить это в 20 минут по живому деку я не стал.

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
