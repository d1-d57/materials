# Канал исполнителя — graf-zavisimostej (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — формат связей и форма отчёта заданы здесь до полей; пишется один
> самостоятельный скрипт на stdlib. Субагенты не нужны.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.**

КОНТЕКСТ. Фабрика научно-популярных лекций. Лекция живёт как папка на слайд: `slaid.md` с шапкой
(поля `vvodit`, `opiraetsya_na`, `zachem`, `minuty`) и телом из блоков вида
`### [<тип>] <мысль>`, тип — из закрытого списка семи: `narrativ · opredelenie · primer ·
utverzhdenie · dokazatelstvo · itog · uprazhnenie`.

Сегодня связность лекции проверяется постфактум и одной клаузой: «термин не употреблён до
определения». Она ловит одну ошибку из многих и **ничего не говорит, пока лекция ещё
планируется** — а именно тогда перестановка стоит дёшево.

ЦЕЛЬ: собрать из уже существующих полей **граф зависимостей лекции** и научиться его читать
программно, без единого токена LLM. Не для того, чтобы машина расставила слайды, — для того, чтобы
человек, придумывая порядок, видел, что от чего зависит и где лекция закольцована.

🔴 **Что ты НЕ делаешь — говорю громко и заранее.** Ты **не** упорядочиваешь лекцию и не
предлагаешь порядок. Нарративный порядок и порядок зависимостей — разные вещи, и решает первый
человек. Твой выход: **карта связей, список нарушений предъявленного порядка и найденные циклы.**
Инструмент, который переставляет слайды сам, будет отвергнут на приёмке, каким бы умным ни был.

Приёмка — по ОТЧЁТУ, без построчной сверки.
Стоп до цели: получишь скрипт, формат связей и прогон на живой лекции. НЕ получишь встраивание в
`SKILL.md` и в анкету `bootstrap_lekcii.py` — это делает отдельный заход, чтобы не столкнуться
зонами с идущим рядом `vhod-fazy-1`.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/graf-zavisimostej`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/graf-zavisimostej`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add graf-zavisimostej --branch zahod/graf-zavisimostej
  cd ../materials-wt/graf-zavisimostej
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/graf_zavisimostej.py` `_generator/tools/fixtures/graf/**`, и во ВТОРОМ репозитории — новый файл `~/Documents/GitHub/disciplina/skills/slajdy/references/graf-zavisimostej.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **`SKILL.md`, `references/kartochka-slajda.md`, `_generator/sborka/bootstrap_lekcii.py` — НЕ ТРОГАТЬ, даже одной строкой.** Рядом идёт заход `vhod-fazy-1`, у которого это зона; ваши правки столкнутся и одна пропадёт. Нужно вписать поле в формат карточки — **назови это строкой в `## ВОПРОСЫ`** пунктом очереди, не делай.
- 🔴 **ДВА РЕПОЗИТОРИЯ — ДВА ОТДЕЛЬНЫХ КОММИТА**, пути из разных репо никогда не собираются в одну командную строку. Второй — `GIT_ZONA_REPO=~/Documents/GitHub/disciplina`.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/graf-zavisimostej`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/graf_zavisimostej.py` `_generator/tools/fixtures/graf/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.** Спецификация — в §2.

| # | файл | зачем |
|---|---|---|
| Я1 | `_generator/sborka/formaty.py` · `_generator/sborka/bloki.py` | 🔴 READ-ONLY: чем УЖЕ парсится шапка и тело карточки. Свой парсер не писать — импортировать эти |
| Я2 | `~/Documents/GitHub/disciplina/skills/slajdy/references/kartochka-slajda.md` | READ-ONLY: какие поля есть сегодня и как они пишутся |
| Я3 | `teorkat-vvedenie/` — живая лекция владельца | 🔴 **READ-ONLY, ни одной правки.** На ней гоняешь скрипт |
| Я4 | `_generator/tools/check_termin.py` | существующая проверка «термин до определения» — из неё видно, как эта задача решалась раньше |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — три хода

### Г1. ФОРМАТ СВЯЗЕЙ — сначала он, потом код

Сегодня в шапке есть `vvodit` (что слайд вводит) и `opiraetsya_na` (на что опирается). Этого мало:
владелец назвал четыре вида связей, и три из них записать негде.

**Спецификация, которую надо описать в новом файле
`disciplina/skills/slajdy/references/graf-zavisimostej.md`:**

1. **термин → слайд-хозяин.** Есть сегодня (`vvodit`).
2. 🔴 **первое появление слова — ОТДЕЛЬНО от определения, и оно ЗАКОННО может быть раньше.**
   Требование владельца дословно, оно ломает наивную проверку: *«функтор был определён на первой
   лекции… это была специальная стратегия: сначала поговорить про функториальность как про
   свойство, а потом, когда привыкнем, произнести определение»*. Значит нужно поле вида
   `pervoe_upominanie: [...]`, и проверка обязана различать три состояния: слово впервые
   **упомянуто** · слово **определено** · слово **использовано рабочим образом**. Красным
   становится только третье до второго, и только если первое не заявлено намеренно.
3. **утверждение → утверждение** («доказательство этого пользуется тем»).
4. **пример / иллюстрация → определение или утверждение**, и **с направлением мотивации**:
   пример может идти ДО (мотивирует, готовит почву) или ПОСЛЕ (иллюстрирует готовое). Оба
   законны — но они разные, и порядок нарушается по-разному. Направление обязано быть записано,
   а не выведено из положения на ленте.

**Форма записи — на твоё решение, но с одним жёстким требованием: она читается тем же парсером,
что уже есть (Я1), и не заводит второго формата карточки.** Развилку назови в `## ПЛАН` до кода.

**КРИТЕРИЙ Г1:** файл написан; для каждого из четырёх видов связи назван **физический адрес
записи** (какое поле, в какой шапке, в каком синтаксисе) и пример строкой. Вид связи, для
которого адрес не назван, считается неописанным — это ровно тот дефект, который эта дисциплина
объявляет дефектом в своей преамбуле.

### Г2. СКРИПТ `_generator/tools/graf_zavisimostej.py`

Читает папку лекции, строит граф, печатает **три вещи и ничего больше**:

1. **резюме лекции** — сколько слайдов, терминов, утверждений, примеров; какие термины ни на что
   не опираются (входы), какие ни для чего не нужны (тупики — кандидаты на вырезание);
2. 🔴 **циклы.** Есть цикл — напечатать его **целиком, поимённо**: «A опирается на B, B на C, C на
   A». Цикл — это точное место, где лекция логически закольцована, и ради него всё затевалось;
3. **нарушения предъявленного порядка** — берётся `slide_order` из `brief.md` как ЗАДАННЫЙ
   человеком порядок, и печатается список рёбер, которые он нарушает, с формулировкой
   «на слайде X рабочим образом использован Y, а его дом — слайд Z, который ниже».

Требования к коду, они же — то, ради чего это вообще делается **без токенов**:
- **только stdlib**, никаких зависимостей, никаких сетевых вызовов, никакого LLM;
- **детерминизм**: два прогона на одном входе дают побайтово одинаковый вывод;
- **exit-код**: 0 — нарушений нет; 1 — есть. Иначе это не гейт, а распечатка;
- **не падать на неполной лекции**: половина полей пуста — это нормальное состояние фазы 1,
  и скрипт обязан печатать, что знает, а не трейсбек.

**КРИТЕРИЙ Г2 (может ПРОВАЛИТЬСЯ):**
1. `python3 _generator/tools/graf_zavisimostej.py teorkat-vvedenie` отрабатывает и печатает все
   три раздела. Команда, `rc` и вывод — в отчёт **целиком**, не пересказом;
2. фикстура в `_generator/tools/fixtures/graf/` с **заведомо закольцованной** лекцией: скрипт
   обязан найти цикл и вернуть `rc=1`. Не находит — заход не выполнен;
3. фикстура с законным «первое упоминание раньше определения»: скрипт **НЕ краснеет**. Краснеет —
   заход не выполнен, потому что он запретил владельцу его рабочий приём;
4. прогон дважды подряд даёт идентичный вывод (`diff` пуст) — показано командой.

### Г3. 🔴 ЧТО ГРАФ СКАЗАЛ ПРО ЖИВУЮ ЛЕКЦИЮ — это и есть ответ владельцу

Прогнать на `teorkat-vvedenie` и написать в отчёте **простыми словами, а не языком графов**:
нашлись ли циклы, сколько нарушений порядка, есть ли тупиковые термины, и **стоила ли эта штука
того**. Ноль находок на живой лекции — законный и полезный результат: значит проблема реже, чем
казалось. Написать это честно, а не подкручивать правила, пока что-нибудь не покраснеет.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «нарушений нет», а «нарушений нет, проверено
N слайдов и M связей».

## 3. ВЕРИФИКАТОР

Операция не лоссовая — ничего не двигается и не удаляется, пишется новый файл. Верификация
встроена в критерий: две фикстуры (одна обязана покраснеть, другая обязана НЕ покраснеть) плюс
детерминизм. Фикстура, которая только зеленеет, ничего не проверяет.

🔴 **`teorkat-vvedenie/` — живая лекция владельца, READ-ONLY.** Скрипт её только читает. Захотелось
дописать в её карточки недостающие поля, «чтобы прогон прошёл», — нельзя: для этого есть фикстуры.
Не хватает полей на живой лекции — это находка и строка отчёта, а не повод её править.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/graf_zavisimostej.py` `_generator/tools/fixtures/graf/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/graf_zavisimostej.py` `_generator/tools/fixtures/graf/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Прочитано:** Я1 (`formaty.py`, `bloki.py`), Я2 (`kartochka-slajda.md`), Я3-проверка (сама лекция, см. находку ниже), Я4 (`check_termin.py`).

### Предпосылка, которую надо проговорить ДО кода

Проверил по живым файлам: `teorkat-vvedenie/` **не содержит НИ ОДНОЙ карточки нового формата**.
Ни одного `slajdy/<imya>/slaid.md`, ни одного вхождения `vvodit:`/`opiraetsya_na:` во всём дереве
(`grep -rl` — пусто). Тело слайдов — старый плоский диалект `src/content/s01.md…s12.md` со сценами
`{@N}`, без врезок `**Определение N (…).**` (проверил и по формату check_termin — тоже пусто, один
ложный `**Примеры видов**` не совпадает с врезкой). `brief.md` есть, `slide_order` в нём заполнен, но
это карточка ЛЕКЦИИ старого диалекта (`id/title/canvas/provenance…`), не новых полей `uzhe_vvedeno_ranee`.
Карточка (Я2) спроектирована ДАТОЙ ДАННЫХ 2026-08-08 — то есть сегодня, и `teorkat-vvedenie` её
физически не могла подхватить.

Это **не ломает заход**: §2 сам называет ровно этот случай нормой фазы 1 («половина полей пуста —
скрипт обязан печатать, что знает, а не трейсбек») и Г3 сам предусматривает «ноль находок — законный
результат». Довожу до предела: у живой лекции пусто НЕ наполовину, а ЦЕЛИКОМ — скрипт напечатает
резюме с нулями по всем трём разделам. Это я подробно распишу как находку в ОТЧЁТЕ/УРОКАХ (не как
провал критерия — критерий Г2.1 требует только «отрабатывает и печатает три раздела», это выполнимо
и на пустом входе), но честности ради Г3 будет не «проблема реже, чем казалось», а «проверить пока
физически не на чем — лекция ещё не переехала на карточки».

### Развилка Г1 — формат связей (называю явно, как просит §2)

Читается **тем же мини-парсером шапки** (`formaty.parse_front_matter`, импорт read-only) — новых
полей ДОСТАТОЧНО завести в шапке `slaid.md`, второго диалекта не создаю.

1. **термин → слайд-хозяин** — существующее поле `vvodit: [термин, …]`. Без изменений.
2. **первое упоминание отдельно от определения** — РЕШЕНИЕ: не завожу отдельное поле
   `pervoe_upominanie`, а **переиспользую уже существующее** `bez_opredeleniya_namerenno` (Я1/Я2:
   `- {termin: X, poyasnyaetsya: …, formalno: <id-слайда-хозяина>}`). Оно уже дословно кодирует три
   состояния спецификации: слайд, на котором стоит эта запись, — «упомянуто/использовано намеренно
   раньше»; `formalno` — указатель на слайд, где «определено» (`vvodit`); а «использовано рабочим
   образом» — это отдельно стоящее `opiraetsya_na` на любом (том же или другом) слайде. Красным
   становится ровно: `opiraetsya_na` на термин X раньше слайда-хозяина X И ни на одном слайде
   не позже точки использования нет `bez_opredeleniya_namerenno` с `termin: X` и `formalno`,
   совпадающим с реальным слайдом-хозяином. Причина решения: одна запись уже покрывает функционал
   дословно (пример «функтор» из Я2 — ровно это), а параллельное поле создало бы два способа
   написать одно и то же — вслепую нарушило бы «не заводит второго формата». Раз в `## ВОПРОСЫ`
   поставлю пункт очереди для аналитика на подтверждение (это решение конкретной, а не тривиальной,
   развилки).
3. **утверждение → утверждение** — НОВОЕ поле `dokazatelstvo_opiraetsya_na: [<id-слайда>, …]` на
   слайде, чей блок `dokazatelstvo`/`utverzhdenie` пользуется другим утверждением. Гранулярность —
   слайд (утверждения не именованы отдельно от слайда-хозяина, как и термины адресуются по
   `vvedeno`/`formalno` через id слайда — тот же стиль). Пример:
   `dokazatelstvo_opiraetsya_na: [s05]`.
4. **пример → определение/утверждение, с направлением** — НОВОЕ поле `primer_dlya: [{tsel: <id>,
   napravlenie: do|posle}]` на слайде-примере. `tsel` — id слайда-хозяина того, что иллюстрируется;
   `napravlenie: do` — мотивирует (пример раньше), `posle` — иллюстрирует (пример позже). Записывается
   явно, не выводится из положения на ленте (дословное требование §2 п.4).

Всё это опишу в `disciplina/skills/slajdy/references/graf-zavisimostej.md` с физическим адресом и
примером на каждый вид — по критерию Г1.

### Развилка Г2 — как скрипт находит карточки лекции

Канонiчный путь по Я1: `<лекция>/slajdy/<imya>/slaid.md`. `brief.md` у `teorkat-vvedenie` лежит НЕ в
корне лекции, а в `src/brief.md` — значит ищу `brief.md` рекурсивно (`glob **/brief.md`, беру первый
по алфавиту/глубине, детерминированно сортирую) и `slaid.md` тоже рекурсивно
(`glob **/slajdy/*/slaid.md`), а не жёстко фиксирую путь — так скрипт переживёт обе раскладки
(канон Я1 и фактическую `teorkat-vvedenie/src/…`).

### Что НЕ регистрирую через `register_doc.py`

Новый файл в `disciplina/skills/slajdy/references/` — **вне `_studio/`** этого репозитория (другой
репозиторий вообще), а дверь регистрации (§KONTRAKT) сама заявляет «отказывает на пути вне
`_studio/`». Значит она про эту дверь не звонит — регистрации не будет, это не пропуск правила, а
неприменимость. Фикстуры (`fixtures/graf/**/*.md`) — тестовые данные инструмента, не документация
`_studio/docs/`, тоже не регистрирую.

### Порядок ходов
1. Написать спецификацию (Г1) → `disciplina/skills/slajdy/references/graf-zavisimostej.md`.
2. Написать `_generator/tools/graf_zavisimostej.py` (Г2): резюме / циклы (термин-граф +
   утверждение-граф, раздельно) / нарушения порядка (по `opiraetsya_na` vs `slide_order`,
   с учётом легализации через `bez_opredeleniya_namerenno`). stdlib, детерминизм, exit-код.
3. Две фикстуры в `_generator/tools/fixtures/graf/` — заведомый цикл (rc=1) и законное раннее
   упоминание (rc=0 на этом конкретном случае).
4. Прогон на `teorkat-vvedenie` (Г2.1) + прогон дважды подряд + `diff` (Г2.4).
5. Отчёт (Г3) — честно, включая находку о пустой лекции.
6. Коммит зоны в `materials` (граф + фикстуры) и ОТДЕЛЬНЫЙ коммит в `disciplina` (спека).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Развилка Г1 п.2 (вид связи «первое упоминание») решена переиспользованием существующего
   `bez_opredeleniya_namerenno` вместо нового поля `pervoe_upominanie`, которое буквально называл
   заход (§2). Причина в ПЛАНе выше. Решение принял сам (в моей зоне), но раз заход называл конкретное
   имя поля — стоит подтвердить, что переиспользование устраивает, а не молча разошлось с ожиданием.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. Новые поля видов 3–4 (`dokazatelstvo_opiraetsya_na`, `primer_dlya`) описаны ТОЛЬКО в новом
   `graf-zavisimostej.md` — канонический список полей карточки, `kartochka-slajda.md`, их не знает
   (эта задача его трогать не могла — контракт зоны, чужой заход `vhod-fazy-1` рядом). Когда до неё
   дойдёт очередь — стоит добавить туда же, чтобы не было двух источников правды про поля шапки.
   ДОМ: disciplina/skills/slajdy/references/kartochka-slajda.md
   ДОСТАВЛЕНО: нет

3. Урок фабрике (закономерность, не про эту сессию): заход назвал `teorkat-vvedenie` «живой лекцией»
   (Я3) для инструмента, читающего карточку `slaid.md` — а карточка спроектирована ДАТОЙ ДАННЫХ
   2026-08-08, тем же днём, и лекция физически не могла её подхватить (проверил grep'ом — ни одного
   `vvodit:`/`opiraetsya_na:`/`slajdy/` во всём дереве). Заход не пострадал только потому, что его
   же критерий заранее разрешал «половину полей пусто», а я довёл это до предела «пусто целиком» и
   не подкрутил правила, чтобы что-то покраснело. Общая закономерность для будущих заходов: назвал
   «живую лекцию» опорой для инструмента под НОВЫЙ формат — не значит, что лекция на этот формат
   уже мигрировала; стоит проверять это в заходе, а не оставлять исполнителю выяснять по факту.
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.**
1. Спецификация `disciplina/skills/slajdy/references/graf-zavisimostej.md` (Г1) — все четыре вида
   связи с физическим адресом поля и примером; виды 1–2 переиспользуют существующие поля
   (`vvodit`, `opiraetsya_na`, `bez_opredeleniya_namerenno`), виды 3–4 заводят два новых
   (`dokazatelstvo_opiraetsya_na`, `primer_dlya`) в том же диалекте шапки — второго формата не
   заведено, парсер тот же (`formaty.parse_front_matter`).
2. `_generator/tools/graf_zavisimostej.py` (Г2) — читает лекцию (рекурсивный поиск `brief.md` и
   `**/slajdy/*/slaid.md`, переживает и канонiчную раскладку, и фактическую `teorkat-vvedenie/src/…`),
   печатает резюме / циклы (термин-граф и утверждение-граф раздельно) / нарушения заявленного
   порядка. Только stdlib, импорт `formaty`/`bloki` — read-only, тем же путём, что уже использует
   `gejt_kartochki.py` (`sys.path.insert` на `_generator/sborka`).
3. Две фикстуры + `PROGNAT.sh` (Г2 крит. 2–3 и контракт инструмента, кластер «кривой вход»,
   `_generator/tools/fixtures/graf/`): `cikl` — три термина по кругу, `rannee-upominanie` — живой
   случай «функтор» из `kartochka-slajda.md`. `PROGNAT.sh` гоняет обе плюс четыре кривых/краевых
   входа (пустая лекция, несуществующая папка, файл вместо папки, битая карточка рядом со здоровой).

**Как проверил.**
- `python3 _generator/tools/graf_zavisimostej.py teorkat-vvedenie` → `rc=0`, печатает все три
  раздела (Г2 крит. 1), полный вывод:
  ```
  ── РЕЗЮМЕ ЛЕКЦИИ (teorkat-vvedenie) ──
  слайдов с карточкой нового формата: 0
  slide_order: 15 слайдов заявлено, но ни один id не совпал с найденными карточками (0)
  терминов (введено через vvodit): 0
    входы (ни на что не опираются): —
    тупики (ничего не опирается на них — кандидаты на вырезание): —
  связей «доказательство опирается на утверждение»: 0
  примеров с направлением мотивации: 0 (до цели: 0, после цели: 0)
  ⚠ карточек формата slaid.md не найдено — граф пуст не потому, что лекция чиста, а потому что нечего читать (лекция ещё не на новом формате карточки).

  ── ЦИКЛЫ ──
  ✓ циклов в терминах нет (проверено 0 терминов, 0 рёбер зависимости)
  ✓ циклов в утверждениях нет (проверено 0 связей «доказательство опирается»)

  ── НАРУШЕНИЯ ЗАЯВЛЕННОГО ПОРЯДКА ──
  ⚠ slide_order задан (15), но ни один id не совпал с найденными карточками (0) — сравнивать не с чем
  ```
- Фикстура `cikl`: `rc=1`, находит `✗ ЦИКЛ В ТЕРМИНАХ: A → B → C → A` (Г2 крит. 2 — обязана покраснеть, покраснела).
- Фикстура `rannee-upominanie`: `rc=0`, `✓ нарушений нет` (Г2 крит. 3 — обязана НЕ краснеть, не покраснела).
- Детерминизм (Г2 крит. 4): каждый из трёх входов (`teorkat-vvedenie`, `cikl`, `rannee-upominanie`)
  прогнан дважды подряд, `diff` пуст на всех трёх, коды возврата совпали (`0/0`, `1/1`, `0/0`).
- `bash _generator/tools/fixtures/graf/PROGNAT.sh` → зелёный, все 6 случаев (в т.ч. три кривых
  входа — несуществующая папка, файл вместо папки, битая карточка — без единого трейсбека).
- `--help` → `rc=0`, непустой вывод (проверил вручную, хоть хук это и не требует в `--staged`-режиме).

**Что НЕ трогал.** `SKILL.md`, `kartochka-slajda.md`, `bootstrap_lekcii.py` — не открывал на запись.
`teorkat-vvedenie/` — только читал (`glob`/`read_text` скриптом и мной вручную), ни одной правки.
Всё вне зоны (`_generator/tools/graf_zavisimostej.py`, `_generator/tools/fixtures/graf/**`,
`disciplina/skills/slajdy/references/graf-zavisimostej.md`) не касался.

**Результат верификатора (§3).** Обе фикстуры дали заявленный вердикт (цикл → красный, легальное
раннее упоминание → зелёный), плюс детерминизм на трёх входах и зелёный `PROGNAT.sh` с фикстурой
кривого входа — верификация встроенного в критерий типа выполнена полностью.

**Г3 — что граф сказал про живую лекцию, простыми словами.** Ничего — и не потому, что лекция
чиста, а потому что `teorkat-vvedenie` ещё не переехала на карточку `slaid.md`: карточка
спроектирована 2026-08-08 (см. ВОПРОС 3), лекция старше и целиком на плоском диалекте
(`content/s01.md…s12.md` со сценами `{@N}`, без единой `vvodit`/`opiraetsya_na`). Охват честно: из
15 заявленных в `slide_order` слайдов ни один не имеет карточки нового формата — проверено 0
терминов и 0 связей «опирается на», циклов и нарушений порядка не найдено на этом основании, а не
потому что их там нет. Стоила ли штука того — на этом входе ответить нельзя, инструмент себя
показал только на фикстурах; повторный прогон после переноса лекции на карточку (или на любой
другой лекции, которая уже на новом формате) даст содержательный ответ.

**Время прогона + токены** — снимает приёмка из лог-строки `result` (см. §5 захода).

**АРТЕФАКТ:** артефакта нет — заход про код и спецификацию, не про сборку HTML/PDF/документа.
Предъявляемые файлы — сами закоммиченные пути, см. КОММИТ ниже.

**НЕОБРАТИМОЕ:** необратимого нет — только новые файлы поверх зоны, ничего не удалено, не
перезаписано, не перемещено; `teorkat-vvedenie/` не тронута ни одной правкой.

**КОММИТ:**
- `materials`: `afe99fe` — «graf-zavisimostej: скрипт графа зависимостей лекции (резюме/циклы/нарушения порядка) + две фикстуры и PROGNAT.sh контракта» · `git_zona.py check --zone _generator/tools/graf_zavisimostej.py` → ✅ · `check --zone _generator/tools/fixtures/graf` → ✅ (ветка `zahod/graf-zavisimostej`, влитие — приёмка, не я).
- `disciplina` (`GIT_ZONA_REPO=~/Documents/GitHub/disciplina`): `5268593` — «slajdy: спецификация формата связей графа зависимостей лекции (заход graf-zavisimostej в materials/)» · `check --zone skills/slajdy/references/graf-zavisimostej.md` → ✅. ⚠ Коммит лёг на ветку `zahod/fazy-1-2-dovodka` — в этом репозитории заходу не выделялся отдельный worktree/ветка (контракт называл только `GIT_ZONA_REPO`-переключатель), а на месте оказалась чужая активная ветка соседнего захода `vhod-fazy-1`. Коммит узкий (ровно один новый файл в моей зоне, `check --zone` подтверждает — вне git ничего не осталось), но физически он лёг НЕ на отдельную ветку. Называю это отдельной строкой, не молчу.
