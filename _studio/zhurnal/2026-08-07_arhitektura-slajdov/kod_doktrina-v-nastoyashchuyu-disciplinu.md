# Канал исполнителя — doktrina-v-nastoyashchuyu-disciplinu (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — работа переносная и ровно по адресам, названным ниже. Изобретать нечего,
> и изобретать ЗАПРЕЩЕНО: доктрина уже написана и утверждена владельцем.

КОНТЕКСТ. Фабрика научно-популярных лекций. **Ты чинишь конкретную поломку, сделанную предыдущим
заходом по вине его контракта зоны.**

**Что произошло.** Заход `verstka-reshaetsya-v-faze-1` должен был записать доктрину вёрстки (правила
Д1–Д6, продиктованные владельцем) в дом дисциплины. В его контракте зона была названа как
`disciplina/skills/slajdy/**` — **без указания, что `disciplina` это ОТДЕЛЬНЫЙ РЕПОЗИТОРИЙ рядом с
`materials`, а не папка внутри него.** Исполнитель честно проверил предпосылку, увидел, что изнутри
`materials` такого пути нет, назвал предпосылку ложной — и завёл файлы с нуля **внутри `materials`**.

**Замер поломки:**
```
НАСТОЯЩИЕ (~/Documents/GitHub/disciplina):  SKILL.md 1473 строки · kartochka-slajda.md 371
ДУБЛЬ (в ветке zahod/verstka-v-faze-1):     SKILL.md  129 строк · kartochka-slajda.md  62

grep -c 'obosnovanie_verstki'      в настоящих → 0
grep -c 'zagolovok_snyat_namerenno' в настоящих → 0
grep -ci 'полоса без иллюстрац'     в настоящих → 0
```
🔴 **Доктрина легла в пустой дубль, а не в файл, который читают все фазы.** Гейт `gejt_verstki.py`
при этом написан правильно и краснеет верно — но правила, на которые он опирается, исполнитель
фазы 1–2 не прочитает: их нет там, где он смотрит.

✅ **Хорошая новость, она определяет объём работы:**
```
git ls-tree arka/mat-kostyak     -- disciplina/  → 0 файлов
git ls-tree zahod/verstka-v-faze-1 -- disciplina/ → 2 файла
```
**Дубль живёт ТОЛЬКО на невлитой ветке.** Ты не чинишь основное дерево — ты не даёшь влить сломанное.

ЦЕЛЬ: Д1–Д6 стоят в настоящей дисциплине, дубля не существует, `arka/mat-kostyak` по-прежнему без
папки `disciplina/`.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: ты НЕ переписываешь доктрину и НЕ улучшаешь её формулировки.** Она утверждена
владельцем дословно. Твоя работа — перенести на правильный адрес, ничего не потеряв.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/doktrina-v-disciplinu`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/doktrina-v-disciplinu`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add doktrina-v-disciplinu --branch zahod/doktrina-v-disciplinu
  cd ../materials-wt/doktrina-v-disciplinu
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- 🔴🔴 **ДВА РЕПОЗИТОРИЯ. ПУТАНИЦА МЕЖДУ НИМИ И ЕСТЬ ТА ПОЛОМКА, КОТОРУЮ ТЫ ЧИНИШЬ — не повтори её.**

| репо | АБСОЛЮТНЫЙ путь | что делаешь | ветка |
|---|---|---|---|
| **`disciplina`** — тут живёт настоящая дисциплина | `~/Documents/GitHub/disciplina` | 🔴 **ПИШЕШЬ:** `skills/slajdy/SKILL.md`, `skills/slajdy/references/kartochka-slajda.md` | 🔴 **вычекаученная, НЕ переключай** |
| **`materials`** — тут лежит дубль и весь остальной проект | `~/Documents/GitHub/materials` (worktree `../materials-wt/doktrina-v-disciplinu`) | 🔴 **УДАЛЯЕШЬ ровно два файла дубля** под `disciplina/` | `zahod/doktrina-v-disciplinu` |

- **ЗОНА (можно менять):** в репо `disciplina` — `skills/slajdy/SKILL.md` и `skills/slajdy/references/kartochka-slajda.md`; в репо `materials` — ТОЛЬКО `disciplina/skills/slajdy/SKILL.md` и `disciplina/skills/slajdy/references/kartochka-slajda.md` (удаление). Всё вне — **READ-ONLY**.
- 🔴 **Проверь, в каком ты репозитории, ПЕРЕД каждой правкой:** `git remote -v` или `basename $(git rev-parse --show-toplevel)`. Совпадение имён папок — ровно та ловушка, что сработала в прошлый раз.
- 🔴🔴 **ЧУЖИЕ ЗОНЫ — рядом идут два захода, не трогать ни байта:** `_generator/sborka/**`, `_generator/vendor/**` (`svedenie-i-smeta`) · `_studio/konvejer/**`, `_generator/tools/sostoyanie.py` (`karta-konvejera`). Также **не трогай `_generator/tools/gejt_verstki.py`** — он написан верно и остаётся как есть.
- 🔴 **`teorkat-vvedenie/**` — READ-ONLY ЖЁСТКО:** там прямо сейчас владелец правит живую лекцию в другом чате.
- ⚠ **Удаление — единственное необратимое действие захода, и оно разрешено ТОЛЬКО для двух названных файлов дубля.** Порядок жёсткий: сначала перенос и проверка, что ничего не потеряно, и только потом удаление. Не наоборот.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/doktrina-v-disciplinu`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `disciplina/skills/slajdy/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 **ДУБЛЬ, из которого переносишь** — в ветке: `git show zahod/verstka-v-faze-1:disciplina/skills/slajdy/SKILL.md` и `…:disciplina/skills/slajdy/references/kartochka-slajda.md` | источник переноса: Д1–Д6 и два новых поля |
| Я2 | 🔴 `_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-SBORKA-L2.md` | **первоисточник доктрины с цитатами владельца.** Расхождение с Я1 → прав Я2 |
| Я3 | `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` — **разделы ФАЗА 1 и ФАЗА 2**, не весь файл (1473 строки) | куда переносишь; как там оформлены решения фаз |
| Я4 | `~/Documents/GitHub/disciplina/skills/slajdy/references/kartochka-slajda.md` | куда переносишь поля карточки; **там уже есть образец** `bez_opredeleniya_namerenno` |
| Я5 | `_generator/tools/gejt_verstki.py` — **только чтение** | какие имена полей гейт уже проверяет: текст обязан совпасть с кодом |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — четыре шага, порядок жёсткий

### Ш1. ПЕРЕНЕСТИ Д1–Д6 В НАСТОЯЩИЙ `SKILL.md`

Из Я1 в Я3, в разделы **ФАЗА 1** и **ФАЗА 2** — по месту, а не блоком в конец файла: правило
должно лежать там, где его читают, принимая решение.

🔴 **Сверять с Я2, а не только с Я1.** Дубль писал исполнитель, у которого не было первоисточника
под рукой; **цитаты владельца — в Я2**, и при расхождении прав Я2. Формулировки владельца
переносить дословно: правило без «почему» переизобретается.

Что именно переносится:
- **Д1** — таблица выбора типа вёрстки от объёма текста и нужности иллюстрации. ⚠ Границы
  «много / среднее / мало» в строках **НЕ выдумывать** — их даст `smeta.py` соседнего захода;
  оставить пустое место с адресом, как и было;
- **Д2** — полоса без иллюстрации запрещена;
- **Д3** — иллюстрация обязана что-то иллюстрировать; вопрос «что именно она показывает» — явный
  пункт фазы, ответ предъявляется владельцу **в ленте**, не только в `zakaz.md`;
- **Д4** — обложку, визитку и финальный вставляет **генератор**, отключение явным флагом, у обложки
  отключения нет. 🔴 **Это СПЕКА ДЛЯ ГЕНЕРАТОРА, не требование к автору** — не преврати её обратно
  в «автор обязан завести обложку»;
- **Д5** — заголовок по умолчанию есть, снимается осознанно, нехватка места — законная причина;
- **Д6** — обоснование вёрстки обязательно для каждого слайда, с образцами жанра из Я2.

### Ш2. ДВА ПОЛЯ — В НАСТОЯЩИЙ `kartochka-slajda.md`

`obosnovanie_verstki` (Д6) и `zagolovok_snyat_namerenno` (Д5) — в Я4, **по образцу уже живущего там
`bez_opredeleniya_namerenno`**, не заводя новый стиль описания полей.

🔴 **Имена полей проверить по Я5, а не по памяти.** Гейт `gejt_verstki.py` уже краснеет на живой
лекции по этим именам; текст обязан совпасть с кодом до символа. Разошлись — **прав код**, и это в
отчёт отдельной строкой.

### Ш3. СНЕСТИ ДУБЛЬ

Только после Ш1–Ш2 и только два файла:
`disciplina/skills/slajdy/SKILL.md` и `disciplina/skills/slajdy/references/kartochka-slajda.md`
в репозитории `materials`. Папка `disciplina/` внутри `materials` обязана исчезнуть целиком.

### Ш4. ПРОВЕРИТЬ, ЧТО ЭТО ЕДИНСТВЕННЫЙ СЛУЧАЙ

Та же ошибка могла случиться раньше и тише. Прогнать по всем веткам:
```
for b in $(git branch --format='%(refname:short)'); do
  echo "$b: $(git ls-tree -r --name-only $b -- disciplina/ | wc -l)"
done
```
Нашлось ещё — **не чинить молча**: назвать в отчёте, чинить только если это тот же дубль тех же двух
файлов. Другое — пункт очереди.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — пять условий:**
1. В настоящем репозитории `disciplina`: `grep -c 'obosnovanie_verstki'` и
   `grep -c 'zagolovok_snyat_namerenno'` по `SKILL.md` + `kartochka-slajda.md` → **больше 0** (было 0
   у обоих). Д1…Д6 найдены поимённо, **шесть из шести**. Команды и вывод в отчёт.
2. `git ls-tree -r --name-only zahod/doktrina-v-disciplinu -- disciplina/ | wc -l` → **0**.
   `arka/mat-kostyak` — по-прежнему **0** (не сломай то, что цело).
3. 🔴 **Живой прогон на реальном объекте:** `python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2`
   (**только чтение**) отрабатывает как раньше — имена полей в тексте и в коде совпали. Вывод и rc
   в отчёт. ⚠ Лекция сейчас меняется владельцем в другом чате: **число слайдов и число замечаний
   могут отличаться от вчерашних — это НЕ регрессия.** Смотри на то, что гейт не упал и поля узнаёт.
4. Ш4 прогнан по всем веткам, результат числом.
5. Оба репозитория закоммичены, `git show --stat` в каждом — только свои пути, **два хэша в отчёте
   отдельными строками**.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «перенёс», а «перенесено Д N из 6, полей M из 2,
веток проверено K из L».

## 3. ВЕРИФИКАТОР

🔴 **Перенос с последующим удалением — операция лоссовая, и потеря здесь не кричит:** дубль
исчезнет, а недоехавшее правило заметят через неделю, когда очередная лекция снова придёт с полосой
без иллюстрации.

- 🔴 **Свежий субагент сверяет Я2 → настоящий `SKILL.md`** по одному правилу: Д1…Д6, у каждого —
  цитата владельца на месте или пересказ? **Пересказ считается потерей.** Охват числом, шесть из шести.
- 🔴 **Слепая проверка адреса — главный тест, ради него всё:** другой свежий субагент, видевший
  ТОЛЬКО репозиторий `disciplina`, отвечает на два вопроса: *«можно ли делать вертикальную полосу,
  если иллюстрации нет?»* и *«кто вставляет обложку и как отключить визитку?»*. Ответы обязаны быть
  верными. Вопросы, ответы и вердикт — в отчёт дословно. **Ответил мимо — правило не доехало,
  сколько бы букв ни перенеслось.**
- **Ничего не потеряно при удалении:** до `rm` — `git show` обоих файлов дубля в лог отчёта или во
  временную копию; после переноса — сверка, что каждый содержательный абзац дубля имеет адрес в
  настоящей дисциплине **или назван как сознательно не перенесённый**.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `disciplina/skills/slajdy/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `disciplina/skills/slajdy/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Первый ход выполнен:** worktree заведён (`git_zona.py worktree add doktrina-v-disciplinu --branch
zahod/doktrina-v-disciplinu`), `git rev-parse --abbrev-ref HEAD` → `zahod/doktrina-v-disciplinu` ✅,
зона `disciplina/` в HEAD пуста (нечего откатывать).

### 🔴 ДВЕ ЛОЖНЫЕ ПРЕДПОСЫЛКИ ЗАХОДА — с доказательством, до работы

**П1. Я2 (`POMARKI-SBORKA-L2.md`) не существует там, где заход велит его читать.**
Файл `_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-SBORKA-L2.md` отсутствует и в моей
рабочей папке, и на `arka/mat-kostyak` — он существует ТОЛЬКО как часть того же самого сломанного
коммита `77630e4` на ветке `zahod/verstka-v-faze-1` (той же ветке, что и Я1), рядом с дублем
`disciplina/`. Отдельно, в основной папке репозитория (не в моём worktree), лежит НЕЗАКоммиченный
файл `PRAVKI-SBORKI-L2.md` (326 строк, владелец правил живьём 2026-08-08/09) — более полный, но под
ДРУГИМ именем.
Сверил оба текста: цитаты владельца по Д1, Д2, Д5, Д6 (таблица объёма, «пустая правая полоса»,
«заголовок… соображений места», «должно быть обоснование») **дословно совпадают** в обоих файлах —
расхождения по содержанию НЕТ, только разное имя/объём документа.
**Решение:** читаю Я2 через `git show zahod/verstka-v-faze-1:_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-SBORKA-L2.md`
(та же ветка и тот же метод доступа, что заход уже предписал для Я1) — этот файл организован ровно
по Д1…Д6, как и просит заход. `PRAVKI-SBORKI-L2.md` использован ТОЛЬКО как сверка, содержание не
править ни там, ни там (оба вне зоны, только чтение).

**П2. Я5 (`gejt_verstki.py`) тоже не существует на моей ветке.**
`_generator/tools/gejt_verstki.py` отсутствует на `arka/mat-kostyak` и в моём worktree — он тоже
существует ТОЛЬКО на ветке `zahod/verstka-v-faze-1` (та же поломка: гейт написан правильно, но
никогда не вливался). Прочитал его через `git show` — имена полей подтверждены: `tip_verstki`,
`obosnovanie_verstki`, `zagolovok_na_ekrane`, `zagolovok_snyat_namerenno` (значение `da`),
`illustracii`. Совпадают с тем, что называет дубль (Я1) — расхождений нет.
**Проблема для критерия готовности №3** («живой прогон `gejt_verstki.py`») — команду в моём
worktree буквально запустить нельзя, файла нет. **Поправка:** извлеку скрипт через `git show` во
ВРЕМЕННЫЙ незакоммиченный файл по тому же пути в своём worktree, прогоню на
`teorkat-vvedenie/L2` для честного «живого прогона», вывод и `rc` — в отчёт, затем **удалю временный
файл** (не коммичу — `_generator/tools/gejt_verstki.py` чужая зона, «не трогай» = не оставлять
даже незакоммиченный след после проверки).

### Хорошая новость, меняющая объём работы

Ш3 («снести дубль») **уже выполнен без меня**: моя ветка ответвлена от `arka/mat-kostyak`, где
`disciplina/` и так пуста —
`git ls-tree -r --name-only HEAD -- disciplina/` → **0** (проверено). В `materials`-репозитории
для меня нет мутации вообще, только диагностика (Ш4) и коммит (если что-то менялось — не менялось).

### Куда именно ложится Д1–Д6 в Я3 — «по месту» с учётом РЕАЛЬНОЙ структуры

Я3 (`~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md`) — не двухфазная схема, как у дубля Я1, а
семь фаз. Прочитал Фаза 1 (168–449) и Фаза 2 (449–736) целиком (в границах Я3). Реальная структура
УЖЕ отчасти это решает — placement по существующим «РЕШЕНИЯ ФАЗЫ», не в конец файла:
- **Д3** (иллюстрация обязана иллюстрировать) → Фаза 1, §5.4 «Параметры слайдов» (родовые решения
  по иллюстрациям уже там) — addendum; + короткая перекрёстная строка в Фаза 2 п.4 (заказ
  иллюстраций, `zakaz.md`), где уже есть поле «что изображено».
- **Д1 + Д2 + Д6** → Фаза 2, РЕШЕНИЯ ФАЗЫ п.3 (это и есть решение `tip_verstki`/`liniya` — таблица
  объёма и запрет «полоса без иллюстрации» логически туда же, `obosnovanie_verstki` — туда же, как
  причина выбора).
- **Д5** → тоже Фаза 2 п.3, НЕ Фаза 1: реальный SKILL.md прямо говорит (строка 217) — «`zagolovok_na_ekrane`
  … решается не здесь [Фаза 1]», и Фаза 2 п.3 уже описывает снятие заглушки `zagolovok_na_ekrane`
  (строка 551–554). Дубль (Я1) положил Д5 в «ФАЗА 1» по своей же, более бедной, двухфазной схеме —
  это не расхождение содержания, а следствие того, что дубль не знал о реальной структуре; кладу по
  месту решения, а не по разметке дубля.
- **Д4** (спека для генератора) → рядом с закрытым списком `tip_verstki` в Фаза 2 п.3 (там уже
  перечислены `oblozhka`/`vizitka`/`finalnyj`/`razdelitel`) — отдельным блоком-цитатой, без правки
  соседних разделов (Фаза 4/6), которые уже фиксируют иной статус реализации («названо дырой» для
  НОВОГО генератора) — это вне моей зоны, я НЕ трогаю Фазу 4/6 и не разрешаю то противоречие
  (уйдёт пунктом очереди в `## ВОПРОСЫ`).
- Кладу **дословно**, не переформулирую; только адрес меняется.

### Kartochka-slajda.md (Я4)

Два новых поля — `obosnovanie_verstki` (§2, рядом с `tip_verstki`/`liniya`) и
`zagolovok_snyat_namerenno` (§2, рядом с `zagolovok_na_ekrane`) — по формату уже живущих там полей
(например, `bez_opredeleniya_namerenno`: явный «намеренно»-флаг тем же приёмом, не тем же
YAML-типом — дубль имел в виду приём, а не буквальную форму). Имена и семантика — по Я5 (код), не
по памяти.

### Порядок действий дальше
1. Внести Д1–Д6 в Я3 (по месту выше), два поля в Я4 — дословно из Я1, сверено с Я2.
2. Верификаторы §3 заходa: (а) свежий субагент сверяет Я2 → готовый SKILL.md, Д1…Д6, цитата или
   пересказ, охват 6 из 6; (б) свежий субагент, видевший ТОЛЬКО репо `disciplina`, отвечает на два
   вопроса заходa.
3. Живой прогон `gejt_verstki.py` (временная копия, см. П2) на `teorkat-vvedenie/L2`.
4. Ш4 — скан всех веток на `disciplina/`.
5. Коммит `disciplina` (два хода add+commit). Коммит `materials` — только если Ш4 или что-то ещё
   потребовало правки; иначе в отчёте прямо: «мутации в materials не было, коммитить нечего».

## ВОПРОСЫ — (заполняет исполнитель)

1. **Гейт `gejt_verstki.py` (+ его фикстуры + `PROGNAT.sh`) существует ТОЛЬКО на мёртвой ветке
   `zahod/verstka-v-faze-1` — он не мержился отдельно от сломанного дубля.** Доктрина Д1–Д6 теперь
   стоит в настоящей дисциплине (этот заход), но НИЧТО в `materials` её механически не проверяет:
   `arka/mat-kostyak` не содержит `_generator/tools/gejt_verstki.py` вовсе. Правило проверил живым
   прогоном временной копии (см. ОТЧЁТ, критерий №3) — код рабочий, просто не влит. Следующая
   лекция с «полосой без иллюстрации» пройдёт МОЛЧА, как и прошлая, пока скрипт не окажется на
   живой ветке.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

2. **Спека Д4 (генератор сам вставляет обложку/визитку/финал) в реальном `SKILL.md` соседствует с
   уже записанным разрывом:** Фаза 4 «ВЫХОД» (строки ~1013–1021 до правки) прямо говорит, что НОВЫЙ
   генератор (`deck.py`) ничего не порождает сам — старый механизм принадлежал `build_deck.py`, и
   «кто заводит служебные карточки в новом дереве — не решено ни одним документом». Я вставил Д4
   доктринально (как спеку, чего хочет владелец), но НЕ трогал Фазу 4/6 и не разрешал это
   противоречие — оно было там до меня и остаётся. Нужно решение владельца, реализует ли `deck.py`
   Д4 или спека ждёт отдельного захода.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. **Побочная находка верификатора:** в `SKILL.md` есть ещё два независимых упоминания `(Д4)` и
   `(Д5)` вне только что вставленных разделов (строки ~905, ~1455 после моей правки) — это другая,
   несвязанная нумерация долгов `DOLG.md`, случайно совпавшая маркировкой с доктриной владельца.
   К переносу отношения не имеет, но при будущем `grep '(Д[1-6])'` может запутать читающего.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Перенёс доктрину вёрстки Д1–Д6 (правила владельца по разбору собранной
Лекции 2) из невлитого дубля в НАСТОЯЩИЙ репозиторий `disciplina`: `SKILL.md` (Фаза 1 §5.4 — Д3;
Фаза 2, РЕШЕНИЯ ФАЗЫ п.3 — Д1, Д2, Д4, Д5, Д6, по месту принятия решения, а не блоком в конец) и два
новых поля в `kartochka-slajda.md` (`obosnovanie_verstki`, `zagolovok_snyat_namerenno`). Цель: чтобы
доктрину читал исполнитель фазы 1–2 по адресу, который он реально открывает, а не по адресу, куда её
случайно завёл сломанный контракт зоны прошлого захода. Ш3 («снести дубль») делать было не нужно:
моя ветка ответвлена от `arka/mat-kostyak`, где `disciplina/` и так пуста (см. ниже, критерий №2).

**Две ложные предпосылки захода — обнаружены и обойдены, см. `## ПЛАН` выше с доказательствами:**
- Я2 (`POMARKI-SBORKA-L2.md`) не существует ни в моей рабочей папке, ни на `arka/mat-kostyak` —
  прочитан через `git show zahod/verstka-v-faze-1:...` (та же ветка, что и Я1). Сверен построчно с
  параллельно существующим `PRAVKI-SBORKI-L2.md` (незакоммиченный файл в основной папке репозитория) —
  расхождений в цитатах владельца по Д1–Д6 нет.
- Я5 (`gejt_verstki.py`) тоже не существует на моей ветке — прочитан через `git show`, имена полей
  подтверждены. Для «живого прогона» (критерий №3) извлёк временную незакоммиченную копию по
  правильному пути в своём worktree, прогнал, удалил после проверки (см. НЕОБРАТИМОЕ ниже).

**Как проверил (критерий готовности, все пять пунктов):**
1. `grep -c 'obosnovanie_verstki'` и `grep -c 'zagolovok_snyat_namerenno'` по `SKILL.md` +
   `kartochka-slajda.md` в `disciplina` → 1+3 и 1+3 (было 0/0). Д1…Д6 найдены поимённо маркерами
   `(Д1)`…`(Д6)`, по одному разу каждый в правильных разделах — **6 из 6**.
2. `git ls-tree -r --name-only zahod/doktrina-v-disciplinu -- disciplina/ | wc -l` → **0**.
   `git ls-tree -r --name-only arka/mat-kostyak -- disciplina/ | wc -l` → **0** (не сломано).
3. Живой прогон: `python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2` (временная копия,
   см. выше) → `RC=1`, красный на `izomorfizm` (Д2) и на всех 15/15 без `obosnovanie_verstki` (Д6),
   предупреждение Д5 на 15/15 без `zagolovok_snyat_namerenno` — **ровно то же поведение, что описано
   в докстринге гейта и в коммите, создавшем его**: имена полей в тексте и в коде совпали. Красный —
   ожидаемый результат (обоснований в карточках объективно ещё нет, доктрина только что легла), не
   регрессия.
4. Ш4 прогнан по всем 29 веткам (`for b in $(git branch --format='%(refname:short)'); do git ls-tree
   -r --name-only $b -- disciplina/ | wc -l; done`) — единственный случай с ненулевым счётом:
   `zahod/verstka-v-faze-1` (2 файла, уже известный дубль). Больше нигде.
5. Коммит `disciplina` ниже. Коммита `materials` нет — мутации не было (Ш3 не потребовался), зона
   `disciplina/` в моём worktree чиста и без него.

**Результат верификатора (§3 захода, два свежих субагента):**
- Сверка Я2 → готовый `SKILL.md`: **6 из 6 пунктов перенесены с дословной цитатой, без потерь**
  (по каждому Д1…Д6 отдельно сверены и таблица, и цитаты, и техническая формулировка правила).
  Побочная находка — namespace-коллизия маркеров `(Д4)`/`(Д5)` с чужой нумерацией долгов, вынесена
  в `## ВОПРОСЫ`.
- Слепая проверка адреса (субагент видел ТОЛЬКО репозиторий `disciplina`): на оба контрольных
  вопроса («можно ли полоса без иллюстрации» / «кто вставляет обложку, как отключить визитку»)
  ответил верно с опорой на текст SKILL.md. Вердикт: **«ПРАВИЛО ДОЕХАЛО»**.

**Что НЕ трогал.** `_generator/sborka/**`, `_generator/vendor/**`, `_studio/konvejer/**`,
`_generator/tools/sostoyanie.py`, `teorkat-vvedenie/**` (кроме чтения `L2` для живого прогона гейта —
только чтение), Фазу 4/6 `SKILL.md` (там уже записан отдельный, не мой, разрыв про новый генератор —
см. `## ВОПРОСЫ` п.2), `PRAVKI-SBORKI-L2.md`/`POMARKI-SBORKA-L2.md` (только читал), формулировки
доктрины не переписывал и не улучшал — перенос дословный.

**Время прогона + токены** — на канале `app` неприменимо, снимать неоткуда.

**ПОВТОРЯЕМОСТЬ находок.** Ложная предпосылка про Я2/Я5 (файлы существуют только на мёртвой ветке) —
это НЕ повторится на следующей единице работы, она устранена этим же заходом там, где касалась моей
зоны, и относится к прошлому контракту зоны, а не к будущим лекциям — законно уходит в `## ВОПРОСЫ`.
🔴 **Но одна находка ПОВТОРИТСЯ, и это важнее самого переноса: `gejt_verstki.py` не влит НИКУДА,
кроме мёртвой ветки** — значит на СЛЕДУЮЩЕЙ лекции запрет «полоса без иллюстрации» и требование
`obosnovanie_verstki` будут нарушаться так же молча, как на Л2, потому что механического гейта на
живой ветке нет. Доктрина теперь в правильном ФАЙЛЕ, но без гейта это по-прежнему только текст,
который можно не прочитать. См. `## ВОПРОСЫ` п.1, класс «НЕМЕДЛЕННОЕ» — не пункт очереди по факту,
но чинить его не моя зона (`gejt_verstki.py` явно назван «не трогай» в контракте, а перенос его в
живую ветку — не про сегодняшний дубль, а про мёртвую отдельную ветку целиком).

**АРТЕФАКТ:** артефакта нет — задача текстовая (перенос доктрины в markdown-файлы дисциплины), не
сборка HTML/PDF/документа. Изменённые файлы:
`/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` и
`/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/references/kartochka-slajda.md` —
открывать текстовым редактором/`git show`.

**НЕОБРАТИМОЕ:** необратимого нет. Единственное действие за пределами штатного плана — временная
копия `_generator/tools/gejt_verstki.py` в моём worktree (извлечена `git show` для живого прогона
критерия №3, путь `_generator/tools/gejt_verstki.py` в `materials-wt/doktrina-v-disciplinu`) —
НЕЗАКОММИЧЕНА и удалена сразу после прогона; `git status` подтверждает зону чистой.

**КОММИТ:** `c344433587107cc49414efeb16a8850d7daa9df8` — «slajdy: доктрина вёрстки Д1-Д6 переносена
в настоящую дисциплину (была в невлитом дубле)» — репозиторий `~/Documents/GitHub/disciplina`,
ветка `zahod/format-kartochki-faza-1` (вычекаученная заходом, не переключал). `git_zona.py` для
этого репозитория неприменим — это отдельный самостоятельный репозиторий, не часть `materials`,
коммитил обычным `git --no-optional-locks add/commit` с явными путями (§4 захода, общее правило
двух ходов). Коммит `materials` — отсутствует по факту: мутации там не было (см. выше).
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
