# Канал исполнителя — gejty-vrut (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — пять дефектов названы вместе с командой, которая каждый воспроизводит;
> плана, рождающегося по ходу, нет. Субагенты не нужны.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.**

КОНТЕКСТ. Фабрика научно-популярных лекций. Дисциплина здесь держится не на памяти, а на гейтах:
десяток скриптов в `_generator/tools/` проверяют коммиты, заходы, документы, потери веток.

🔴 **Класс дефекта, ради которого написан заход: инструмент, которому верят, врёт.** Он не падает —
он **отвечает**, и ответ принимают за факт. За одни сутки этот класс проявился пять раз в нашем
собственном инструментарии. Ниже все пять, каждый с командой.

**ЦЕНА, общая и она больше суммы частей:** гейт, красный на верном, перестают читать. После двух
ложных красных подряд третье, настоящее, уже не увидят — а ради него всё и строилось. Это не
гипотеза: по автологу `_INFRA-git/INCIDENTY.md` **28 обходов `--no-verify` из 56 срывов коммита**,
27 из них по одной-единственной причине.

ЦЕЛЬ: чтобы каждый гейт краснел ровно на том, на чём должен, и был исполним в той среде, где его
зовут.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее:** ты чинишь **инструменты**, а не содержание. Не трогаешь
`_generator/sborka/**`, `_generator/skeleton/**`, `disciplina/skills/**` и `teorkat-vvedenie/**` —
там прямо сейчас работают три соседних захода и живое интервью с владельцем.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/gejty-vrut`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/gejty-vrut`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add gejty-vrut --branch zahod/gejty-vrut
  cd ../materials-wt/gejty-vrut
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять) — ПОИМЁННО, не по маске.** В `materials`: `_generator/tools/check_sborki.py` · `git_zona.py` · `priyomka.py` · `bootstrap_zahod.py` · `reviziya_dolgov.py` · и фикстуры `_generator/tools/fixtures/{git_zona,priyomka,reviziya_dolgov,zahod}/**`. Во ВТОРОМ репозитории: `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md` (**только строки `СТАТУС:`**). Всё вне — **READ-ONLY**.
- 🔴🔴 **`_generator/tools/**` ЦЕЛИКОМ — НЕ ТВОЯ ЗОНА, и это важно.** Рядом идут три захода, и их файлы лежат в той же папке: `gejt_illyustracij.py` и `fixtures/illyustracii/**` — заход `provod-illyustracij`; `fixtures/sborka/**` — `format-kartochki-faza-1`; `fixtures/verifikatory/**` — `verifikatory-novoe-derevo`. Увидел там чужую незакоммиченную работу — **не подметай её своим коммитом**, коммить только свои пути поимённо.
- 🔴 **Текст самих долгов в `DOLG.md` НЕ переписывать и НЕ удалять** — только строку `СТАТУС:`. «Записи дословные, голос источника; пересказ убивает цену».
- 🔴🔴 **В `disciplina` НЕ ЗАВОДИ ВЕТКУ И НЕ ДЕЛАЙ `checkout`.** Там одна рабочая папка на всех, и в ней прямо сейчас работают три соседних захода. Коммить на вычекаученную ветку, явными путями, и проверить `git show --stat`.
- 🔴 **`teorkat-vvedenie/**` — READ-ONLY жёстко:** живая лекция и идущее интервью с владельцем.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/gejty-vrut`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.** Спецификация — в §2.

| # | файл | зачем |
|---|---|---|
| Я1 | `_generator/tools/check_sborki.py`, проверка **С5** и проверка путей **С3** | предмет Г1 и Г2 |
| Я2 | `_generator/tools/git_zona.py`, функция резолва репозитория и `cmd_check` | предмет Г3 |
| Я3 | `_generator/tools/priyomka.py` + его фикстуры | предмет Г4 |
| Я4 | `_generator/tools/bootstrap_zahod.py`, ветка канала `app` и текст про «время и токены» | предмет Г5 |
| Я5 | `_generator/tools/reviziya_dolgov.py` | предмет Г6 |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — шесть дефектов, каждый с командой

🔴 **Правило для всех шести: сначала ВОСПРОИЗВЕДИ, потом чини.** Не совпало с описанием — скажи
это и не чини: **опровержение — полноценный успех захода**, оно спасает от правки того, что не
сломано. Аналитик мог ошибиться, он ошибался сегодня трижды.

### Г1. `check_sborki.py` С5 краснеет на ВЕРНОЙ форме `git_zona.py merge`

```
python3 _generator/tools/check_sborki.py _studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_format-kartochki-faza-1.md
→ ❌ С5: заход зовёт `git_zona.py merge` с позиционным аргументом, а живой CLI позиционных
   здесь не принимает
python3 _generator/tools/git_zona.py merge --help
→ usage: git_zona.py merge [-h] [--zone ZONE] [--abort] [--continue] [branch]
```
Позиционный `branch` **есть**, и именно этой формой успешно слили ветки заходы `vhod-fazy-1` и
`svedenie-vetok`. Проверка врёт.

### Г2. `check_sborki.py` С3 краснеет на пути рабочей папки, которой ещё нет

```
python3 _generator/tools/check_sborki.py _studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_skelet-sverka.md
→ ❌ С3: путь `../materials-wt/skelet-sverka` НЕ СУЩЕСТВУЕТ … а назван АДРЕСОМ работы
```
Этот заход был написан, прогнан и **принят без замечаний**. Папка worktree по построению
**создаётся исполнителем первым ходом** и снимается после приёмки — значит она НЕ МОЖЕТ
существовать в момент проверки. Сегодня С3 красен на каждом worktree-заходе и потому не значит
ничего.
⚠ **Не выключай С3 целиком** — он ловит настоящие битые пути. Нужна одна оговорка: путь вида
`../materials-wt/<имя>`, совпадающий с именем ветки захода, — не адрес на диске, а обещание.

### Г3. `git_zona.py check --zone` резолвит репозиторий по расположению СКРИПТА

Позванный абсолютным путём из основной папки, стоя в worktree, он проверяет **не тот
репозиторий** — молча. Найдено исполнителем `tihie-polomki`.
**Почему это тяжелее прочего:** `check --zone` — **первый гейт приёмки**. Инструмент, который
молча меряет не то, делает приёмку декоративной.
Сделать так, чтобы репозиторий определялся по **месту вызова** (`git rev-parse --show-toplevel`
от cwd), и чтобы он **печатал, какой репозиторий и какую ветку проверяет**. Печать обязательна:
молчаливая правильность неотличима от молчаливой ошибки.

### Г4. `priyomka.py` — две фикстуры красные на HEAD независимо ни от чего

«ПРИЁМКА МОЛЧИТ ПРО ВЕТКУ ЗАХОДА» и «ПРИЁМКА ЗОВЁТ ЗАКРЫТЬ НЕПЕРЕНЕСЁННОЕ» краснеют без чьих-либо
правок — проверено откатом на HEAD исполнителем `vhod-fazy-1`.
**ЦЕНА прямая:** из-за них общий `PROGNAT.sh` красный, и **каждый заход коммитит через
`--no-verify`**. По автологу это 27 обходов из 28 по одной причине.
⚠ **Разберись, кто виноват — код или фикстура.** Возможно, права фикстура и `priyomka.py`
действительно молчит там, где должна говорить; тогда чини код. Возможно, фикстура описывает
поведение, которое сознательно изменили; тогда чини фикстуру и скажи, почему. **Не глуши ни то ни
другое.**

### Г5. Гейт «время прогона + токены» неисполним на канале `app`

Формулировка написана под канал `terminal`, где `tee` кладёт `jsonl` на диск и приёмка снимает
число командой. На канале `app` лога нет — снимать неоткуда. Значит гейт, объявленный обязательным
в каждом заходе, **тихо не выполняется**, и это заметила только приёмка `graf-zavisimostej`.
Поправить `bootstrap_zahod.py`: на канале `app` требование либо заменяется на исполнимое, либо
**явно объявляется неприменимым** прямо в порождаемом заходе. Молчаливо оставить нельзя — это тот
же класс, что и всё остальное в этом заходе.

### Г6. 🔴 ПЕРЕСЧЁТ: сколько долгов закрыто заходами, но не отмечено

Владелец спросил прямо: *«сколько их сейчас, чтобы было понятно, куда мы идём и с какой
скоростью»*. Сегодня ответить нельзя: `reviziya_dolgov.py` даёт **38 живых**, но это число не
знает про заходы, принятые ПОСЛЕ ревизии.

Влей принятые ветки к себе и перепроверь **поимённо** долги, которые они закрывали:
```
git --no-optional-locks merge zahod/porcia-1 ; git --no-optional-locks merge zahod/gruppa-D
git --no-optional-locks merge zahod/tihie-polomki
```
| долг | что утверждает метка | чем проверить на сведённом дереве |
|---|---|---|
| Д17 | «список и сцены несовместимы» | `cd _generator && python3 -c "from build_deck import render_md; print('<ul' in render_md('{@3}\n- раз\n- два', {}, None))"` → True ⇒ метка устарела |
| Д18 | «три типа вёрстки отложены» | `python3 -c "import sys;sys.path.insert(0,'_generator/sborka');import tipy;print(tipy.ОТЛОЖЕННЫЕ)"` → осталось два ⇒ **частично**, метку НЕ снимать |
| Д27 · Д28 · Д52#2 | закрыты заходом `gruppa-D` | проверить по формулировке каждого |
| Д19 | «bootstrap должен парсить `INTERVYU.md`» | 🔴 **решение аналитика: УСТАРЕЛ.** Р-А отменил парсинг интервью — имена слайдов живут в `slide_order`. Д33 на том же факте уже закрыт |

**КРИТЕРИЙ Г6:** `reviziya_dolgov.py` прогнан после правок, **новое число живых названо**, и по
каждому изменённому долгу — команда и вывод. 🔴 Метку ставить **только по команде**: правило О23
(«заход не закрывает долг, пока чекер не покажет МЁРТВ») здесь применяется впервые.

---

⚠ **Порядок важности:** `Г3` ≫ `Г4` ≫ `Г6` ≫ `Г1` ≈ `Г2` ≫ `Г5`. Не успеваешь — оставь открытым и
опиши одной строкой; плохо сделанное хуже несделанного.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «починено», а «закрыто N из 6, опровергнуто K».

## 3. ВЕРИФИКАТОР

🔴 **Операция особо опасная: ты правишь сами гейты.** Ослабленный гейт выглядит как починенный —
он зеленеет. Поэтому единственный годный верификатор здесь — **пара фикстур на каждую правку**:
одна, которая обязана краснеть, и одна, которая обязана зеленеть.

- **На каждый из Г1–Г5** — фикстура, красная на непочиненном коде и зелёная после. Ловушка,
  которая только зеленеет, ничего не проверяет.
- **Отдельно и обязательно: гейт не должен перестать ловить настоящее.** Для Г1 и Г2 подсунь
  заведомо **битый** путь и заведомо **несуществующую** подкоманду — С3 и С5 обязаны покраснеть.
  Не покраснели — ты не починил гейт, а выключил его, и это хуже, чем было.
- **Регресс:** `bash _generator/tools/fixtures/git_zona/PROGNAT.sh` и все прочие `PROGNAT.sh` в
  `_generator/tools/fixtures/**` — прогони **до** первой правки и покажи вывод, иначе в конце не
  отличить свою поломку от чужой.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Порядок — как велено: Г3 ≫ Г4 ≫ Г6 ≫ Г1 ≈ Г2 ≫ Г5. По каждому — сначала воспроизвести
командой из §2, потом чинить (или объявить опровергнутым).

- **Г1 (check_sborki.py С5, merge позиционный).** УЖЕ воспроизвёл: команда из захода на
  живом `kod_format-kartochki-faza-1.md` даёт ГЕЙТ ЗЕЛЁНЫЙ — С5 молчит, ❌ про merge нет
  (`git log` файла — один коммит, форма вызова там всегда `--branch`, не позиционная;
  позиционной формы в истории файла не было). Отдельно прогнал изолированный кейс
  `git_zona.py merge zahod/porcia-1` (позиционно, как в §2) — тоже зелёный. Разбор
  `_usage_forma`/`_razobrat_vyzov` по usage `[branch]` даёт `pozic=1`, `dano_pozic=1` —
  ошибки нет. **Предпосылка Г1 не подтверждается: заявляю опровержение**, правку не
  делаю (незачем чинить то, что не сломано); опишу разбор в отчёте.
- **Г2 (check_sborki.py С3, путь worktree).** Воспроизвёл: `kod_skelet-sverka.md` даёт
  ❌ С3 на `../materials-wt/skelet-sverka` — путь стоит в КОНТРАКТЕ ЗОНЫ рядом с
  `worktree add skelet-sverka --branch zahod/skelet-sverka`, папки на диске ещё нет.
  Фикс — точечная оговорка в `check_s3_puti`: путь вида `../<имя-репо>-wt/<X>`
  (репо — не литерал «materials», а `glavnyj_repo().name`, по образцу уже живой логики
  С2/С3) не краснеет ❌, если в тексте захода есть `worktree add <X>` — это ровно то
  же самое имя, что использует `cmd_worktree` для `WT_HOME / args.name`. Остаётся ⚠,
  как у путей зоны. **Ловушка регресса:** битый путь `../materials-wt/net-takogo` без
  соответствующего `worktree add net-takogo` обязан остаться ❌.
- **Г3 (git_zona.py резолв репозитория).** Подтверждаю по коду ДО прогона: `REPO =
  korni.REPO`, а `korni.REPO` резолвится через `Path(__file__).resolve().parents[2]` —
  т.е. по расположению *файла инструмента*, не по `cwd`. Позванный абсолютным путём
  из основной папки, стоя в worktree, инструмент проверит основную папку. Фикс —
  ЛОКАЛЬНО в `git_zona.py` (не трогая `korni.py` — он вне зоны и общий для других
  инструментов): своя `_resolve_repo()`, приоритет `GIT_ZONA_REPO` (не ломать
  фикстуры) → `git rev-parse --show-toplevel` от `cwd` → откат на `korni.REPO`, если
  git не ответил. Плюс печать в `cmd_check`: какой репозиторий и какая ветка
  проверяются — гейт первого хода приёмки обязан объявлять, что именно он смотрит.
- **Г4 (priyomka.py, две красные фикстуры).** Читаю `priyomka.py` целиком (Я3) и его
  PROGNAT.sh, нахожу обе фикстуры по именам, разбираюсь код это или фикстура устарела,
  прогоняю на HEAD ДО правки — фиксирую вывод, чиню одну сторону, не глушу другую.
- **Г6 (пересчёт долгов).** Вливаю к себе `zahod/porcia-1`, `zahod/gruppa-D`,
  `zahod/tihie-polomki` (см. точку отката ниже — сначала зафиксирую состояние зоны).
  Прогоняю `reviziya_dolgov.py`, проверяю Д17/Д18/Д19/Д27/Д28/Д52#2 командами из
  таблицы §2, снимаю метки ТОЛЬКО там, где чекер сказал МЁРТВ (О23).
- **Г5 (bootstrap_zahod.py, гейт времени/токенов на канале app).** Читаю ветку канала
  `app` и текст про «время и токены», делаю недоисполнимое требование либо исполнимым,
  либо явно объявленным неприменимым в заходе — молчаливо не оставляю.

**Точка отката:** перед Г6-merge зафиксирую состояние своей зоны (`git add
`_generator/tools/**`` + commit, если грязно) — обещано §0.
**Ревизия по ходу:** если между шагами предпосылка провалится — остановлюсь и допишу
сюда одной строкой, не буду тащить готовое решение по инерции.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Починка Г3 (`git_zona.py` резолвит репо по `cwd`, не по `__file__`) меняет контракт для
   ЛЮБОГО стороннего кода, который зовёт `git_zona.py` subprocess'ом без явного `cwd=`
   или `GIT_ZONA_REPO=`: раньше такой вызов молча резолвился по расположению САМОГО
   `git_zona.py`, теперь — по каталогу вызывающего процесса. Нашёл и починил ОДИН такой
   случай в своей зоне (`bootstrap_zahod.py:198`, `ensure_worktree` — регрессия
   `fixtures/git_zona` ловушки 22 доказала это живым прогоном). Не проверял системно
   код ВНЕ своей зоны — есть шанс, что похожий паттерн живёт ещё где-то.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет
2. `check_sborki.py` — в моей зоне поимённо, но его единственный регрессионный набор
   (`fixtures/sborka/**`) — в зоне СОСЕДНЕГО захода (`format-kartochki-faza-1`). Правку
   Г2 (обещание worktree в С3) проверил ad hoc (`/tmp`) и убедился, что живой
   `fixtures/sborka/PROGNAT.sh` остаётся зелёным, но НЕ смог добавить туда постоянную
   пару ловушек (красная/зелёная) — коммитить чужую зону запрещено контрактом. У
   `check_sborki.py` нет своего дома фикстур вовсе.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Сделал + зачем — по дефектам:**

- **Г1 — ОПРОВЕРГНУТ.** Команда захода на живом `kod_format-kartochki-faza-1.md` даёт
  ГЕЙТ ЗЕЛЁНЫЙ: `python3 _generator/tools/check_sborki.py _studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_format-kartochki-faza-1.md`
  → `✅ ГЕЙТ СБОРКИ: красных нет`, С5 про merge молчит. `git log` файла — один коммит,
  живая форма вызова там всегда `--branch`, позиционной не было НИКОГДА. Отдельно
  прогнал изолированный кейс — ровно ту форму, что называет заход
  (`git_zona.py merge zahod/porcia-1`, позиционно) в синтетическом заходе: тоже
  зелёный, С5 молчит. Разбор `_usage_forma`/`_razobrat_vyzov` по `usage: … [branch]`
  даёт `pozic=1`, вызов даёт `dano_pozic=1` — сравнение `1 > 1` ложно, ошибки нет.
  Правку НЕ делал: опровержение полноценный успех, чинить нечего.
- **Г2 — ПОЧИНЕН.** `check_sborki.py`: С3 получила узкую оговорку —
  путь вида `../<имя-главного-репо>-wt/<X>` (репо — не литерал, `glavnyj_repo().name`,
  тем же приёмом, что у С2/С3) не краснеет ❌, если тем же текстом заход зовёт
  `worktree add <X>` — это ровно её же будущий адрес. Проверено на живом
  `kod_skelet-sverka.md`: `../materials-wt/skelet-sverka` было ❌, стало ⚠
  «обещание worktree». Регресс: синтетический битый путь `../materials-wt/net-takoj-papki`
  БЕЗ парного `worktree add` остаётся ❌ — С3 не выключена, ловит настоящее.
- **Г3 — ПОЧИНЕН.** `git_zona.py`: своя `_resolve_repo()` вместо `REPO = korni.REPO` —
  приоритет `GIT_ZONA_REPO` (не тронут, дверь фикстур), иначе
  `git rev-parse --show-toplevel` от `cwd`, иначе откат на `korni.REPO`. `cmd_check`
  печатает `🔎 Репозиторий: … · ветка: …` первой строкой. Живая демонстрация: старый
  (главный) `git_zona.py`, позванный АБСОЛЮТНЫМ путём из основной папки, стоя в этом
  worktree, молча проверял ОСНОВНУЮ (27 чужих untracked-путей); починенный видит
  worktree и печатает, что смотрит. Побочный эффект — регрессия в
  `bootstrap_zahod.py:ensure_worktree` (subprocess к `git_zona.py worktree add` без
  `cwd=`), найдена живым прогоном `fixtures/git_zona` ловушки 22 и починена там же
  (`cwd=REPO_ROOT`).
- **Г4 — ПОЧИНЕН.** Обе фикстуры красны были не из-за `priyomka.py`, а из-за СВОЕЙ
  ЖЕ фикстуры: `fixtures/git_zona/PROGNAT.sh` копирует `priyomka.py` в одноразовый
  репозиторий, но не копировала его модульные зависимости — `dostavit_urok.py`
  (Г8, `import dostavit_urok`) и, транзитивно, `check_uroki.py`
  (`dostavit_urok.py: import check_uroki`). Без них ЛЮБОЙ вызов приёмки в фикстуре
  падал `ModuleNotFoundError` ДО единой строки вывода — ловушки грепали вывод краша,
  а не вердикт приёмки. Добавил обе копии в `cp`-строку. Найдено прямым ручным
  повтором сценария фикстуры (не код `priyomka.py` — он не тронут).
- **Г5 — ПОЧИНЕН.** `bootstrap_zahod.py`: текст про «время+токены» стал условным по
  `a.kanal`. На `terminal` — прежняя формулировка (там реально есть `tee`-лог). На
  `app` — явное «НЕПРИМЕНИМО, снимать неоткуда» с объяснением почему (лога не
  существует на этом канале). Дефект подтверждён и на СВОЁМ заходе: `kod_gejty-vrut.md`
  несёт именно старую формулировку на канале `app`.
- **Г6 — СДЕЛАН.** Слил `zahod/porcia-1`, `zahod/gruppa-D`, `zahod/tihie-polomki` в
  свою ветку (без конфликтов). `reviziya_dolgov.py` ДО правок — 38 живых (совпало с
  заявленным). Проверил Д17/Д18/Д19/Д27/Д28/Д52#2 поимённо командами (см. ниже),
  ИЗМЕНИЛ статус там, где чекер это подтвердил, — только по О23:
  - **Д17 → МЁРТВ.** `python3 -c "from build_deck import render_md; print(render_md('{@3}\n- раз\n- два', {}, None))"`
    (из `_generator/`) → `<ul data-scene-from="3" class="tlist">…` — тег сцены СИДИТ
    на `<ul>`, список и сцена совместимы. Починка приехала слиянием `porcia-1`.
  - **Д19 → УСТАРЕЛ.** `grep -n "INTERVYU" _generator/sborka/bootstrap_lekcii.py` →
    «Вход — явный список ИМЁН слайдов через CLI, НЕ парсинг INTERVYU.md» — решение
    аналитика отменило парсинг интервью, тот же факт уже закрыл Д33 (сверил: Д33
    МЁРТВ с тем же обоснованием).
  - **Д18 — ЖИВ, без изменений.** `python3 -c "import sys;sys.path.insert(0,'_generator/sborka');import tipy;print(tipy.ОТЛОЖЕННЫЕ)"` → `('kartochka_centr', 'rejka_sajdbar')` — было три отложенных типа, стало два: частично, метку по инструкции НЕ снимал.
  - **Д27 — ЖИВ, без изменений.** `grep -n tlist _generator/skeleton/base.css` и
    `grep -n mezhstrochye _generator/sborka/tipy.py` — маркер списка и межстрочье НА
    МЕСТЕ (это правда закрыто); но `grep -rn nomer _generator/skeleton/*.css
    _generator/skeleton/*.js` → пусто — номера слайда всё ещё нет. Долг из 4 пунктов
    закрыт по 2 — О23 запрещает снимать метку при частичном закрытии.
  - **Д28 — ЖИВ, без изменений.** `grep -n kegl_dlya_znakov _generator/sborka/tipy.py _generator/sborka/slaid.py`
    → пусто (0 вызовов), сама функция подтверждает в своём докстринге
    `podgonka.py`: «НЕ вызывается нигде в пайплайне». `gruppa-D` эту функцию не
    трогала — заявление захода «закрыты gruppa-D» здесь не подтвердилось.
  - **Д52#2 (SVG-масштаб) — ЖИВ, без изменений.** `base.css`: `.panel > svg, .ill-box > svg { width:100%;height:100%;display:block }` — по-прежнему без `object-fit`/`preserveAspectRatio` (в отличие от `<img>`, у которого `object-fit:contain` уже есть). `audit.py` (строки 91-104) ловит SVG, вылезающий ЗА панель (overflow), а не SVG МЕЛЬЧЕ панели — ровно та дыра, что называл долг. `gruppa-D` трогала `base.css`/`engine.js`, но эту конкретную CSS-строку не меняла.
  - **Новое число:** `python3 _generator/tools/reviziya_dolgov.py` → `ЖИВ: 36` (было
    38), `МЁРТВ: 24` (было 23), `УСТАРЕЛ: 3` (было 2), сумма 68 сходится.
  - `DOLG.md` — правил ТОЛЬКО строки `СТАТУС:` у Д17 и Д19 (контракт: текст долга не
    трогать); закоммитил во ВТОРОМ репозитории (`disciplina`, на вычекаученной там
    ветке `zahod/format-kartochki-faza-1`, поимённым путём — ветку не заводил,
    checkout не делал).

**Как проверил (верификатор §3):**
- Регресс СНЯТ ДО первой правки: полный прогон `bash fixtures/git_zona/PROGNAT.sh` на
  чистом HEAD — 4 красных: 2 предсуществующих (Г4-фикстуры) + 2 моих новых ловушки
  42/43 (заведомо красные без починки, проверка на то, что ловушка вообще ловит).
  После всех правок — 0 красных, `ФИКСТУРЫ ЗЕЛЁНЫЕ`.
- На каждый Г2/Г3/Г5 — своя пара «красная до / зелёная после» (см. пункты выше и
  ловушки 42-43 в `fixtures/git_zona/PROGNAT.sh`, ловушка 24 в `fixtures/zahod/PROGNAT.sh`).
  Регрессия отдельно проверена: битый путь (Г2) и канал `terminal` (Г5) остаются
  красными/прежними — гейты не выключены, а сужены точно.
- Финальный сплошной прогон ВСЕХ смежных наборов после всех правок:
  `git_zona`, `priyomka`, `zahod`, `korni`, `register_doc`, `sborka`, `dvizhok`,
  `reviziya_dolgov` — все `ФИКСТУРЫ ЗЕЛЁНЫЕ` / `ВСЁ ЗЕЛЁНОЕ`, exit 0.
- pre-commit хук сам прогнал все связанные PROGNAT.sh на обоих коммитах — тот же
  результат живьём при коммите (не только у меня руками).

**Что НЕ трогал:** `_generator/sborka/**`, `_generator/skeleton/**` (кроме содержимого,
привезённого СЛИЯНИЕМ веток `gruppa-D`/`tihie-polomki` по прямому указанию §2 Г6 —
сам ни строки там не писал), `disciplina/skills/**` (кроме строк `СТАТУС:` в
`DOLG.md`, разрешено контрактом), `teorkat-vvedenie/**`, текст самих долгов в
`DOLG.md` (только их `СТАТУС:`-строки).

**Результат верификатора:** зелёный на всех прогонах выше, регресс-ловушки (битый
путь без парного `worktree add`, канал `terminal`, `GIT_ZONA_REPO`-приоритет)
подтверждают, что ни один гейт не выключен, а сужен/дополнен точно.

**Открытое «возвращаться»:** оба пункта в `## ВОПРОСЫ` выше (систематическая проверка
других вызовов `git_zona.py` без `cwd=`/`GIT_ZONA_REPO` вне моей зоны; отсутствие
дома фикстур у `check_sborki.py`).

**Время прогона + токены** — их снимает ПРИЁМКА из лога прогона (канал `app` в этой
сессии; см. правку Г5 — на этом канале требование неприменимо буквально, снимать
неоткуда).

**НЕОБРАТИМОЕ:**
- Три слияния (`zahod/porcia-1`, `zahod/gruppa-D`, `zahod/tihie-polomki`) в свою
  ветку `zahod/gejty-vrut` — по прямому указанию §2 Г6. Восстановимо: коммит ДО
  слияний — `fcc2f28`; ветка не запушена и не влита в `main`, решение о влитии — за
  приёмкой.
- Коммит `0e8b977` в РЕПОЗИТОРИИ `disciplina` (две строки `СТАТУС:` в `DOLG.md`, на
  чужой вычекаученной ветке `zahod/format-kartochki-faza-1`) — восстановимо
  `git revert 0e8b977` в том репозитории.
- Во время разведки Г1 (до всяких правок) случайно вызвал `git_zona.py merge --branch …`
  напрямую в этом worktree — инструмент не понял флаг и сам дописал строку в РЕАЛЬНЫЙ
  `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (вне моей зоны). Заметил и вернул файл
  `git checkout -- _studio/zhurnal/_INFRA-git/INCIDENTY.md` тем же ходом — на диске
  сейчас чисто, но называю отдельно, раз в момент это было необъявленное вторжение
  вне зоны.

**АРТЕФАКТ:** артефакта нет: заход правит инструменты (`git_zona.py`,
`check_sborki.py`, `bootstrap_zahod.py`, фикстуры) и статусы долгов в `DOLG.md`, а не
собирает HTML/PDF/картинку. Результат — в коммитах ниже и в прогонах фикстур выше
(воспроизводимо: `bash _generator/tools/fixtures/git_zona/PROGNAT.sh` и
`bash _generator/tools/fixtures/zahod/PROGNAT.sh` из корня `materials-wt/gejty-vrut`).

**КОММИТ:**
- `fcc2f28` — «gejty-vrut: Г2 (С3 обещание worktree) + Г3 (репо по cwd) + Г4
  (фикстуры priyomka без dostavit_urok/check_uroki)»
- слияния `28d3c5f` (porcia-1) → `27e4f28` (gruppa-D) → `8c24425` (tihie-polomki),
  по прямому указанию §2 Г6
- `8fc97e6` — «gejty-vrut: Г5 — гейт «время+токены» на канале app объявлен явно
  неприменимым, не молчаливо неисполним»
- `0e8b977` в репозитории `disciplina` — статусы Д17/Д19 в `DOLG.md`
- `python3 _generator/tools/git_zona.py check --zone _generator/tools` → ✅
  (`🔎 Репозиторий: …/materials-wt/gejty-vrut · ветка: zahod/gejty-vrut` ·
  «работа доехала в git, вне git ничего нет»)
