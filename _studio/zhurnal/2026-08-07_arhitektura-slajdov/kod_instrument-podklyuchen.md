# Канал исполнителя — instrument-podklyuchen (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — дефект измерен и назван поимённо, работа механическая плюс одна
> архитектурная правка промпта. Субагенты не нужны.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.**
> ✅ **`final-zakrytie-nochi` принят, три ветки влиты, дерево сведено** — условие запуска выполнено.
> Ствол на момент выдачи — `6729551`.

КОНТЕКСТ. Фабрика научно-популярных лекций. Дисциплина держится на гейтах — скриптах, которые
краснеют, когда что-то нарушено. За сутки их написано много и хороших.

🔴 **Владелец предположил, что написанный гейт может нигде не вызываться. Приёмка измерила — он
прав, и это не один случай, а шесть.**

```
_illustracii/proverka_pomarok.py        — упоминается ТОЛЬКО в двух .md; сработает,
                                          лишь если агент прочтёт инструкцию и вспомнит
_generator/tools/gejt_illyustracij.py   — вызывается только из своей фикстуры
_generator/tools/graf_zavisimostej.py   — только из фикстуры
_generator/tools/reviziya_dolgov.py     — только из фикстуры
_generator/tools/check_lenta.py         — только из фикстуры
_generator/tools/sdelat_handoff.py      — только из фикстуры
```
Команда, которой это снято, — в §2 Ш1, повтори её сам.

**ЦЕНА, и она удваивает стоимость каждой ночи такой работы.** Гейт, вызываемый только из
собственной фикстуры, **протестирован, но не участвует в жизни**: он зелёный, потому что его никто
не звал. Мы за сутки построили шесть таких и каждый раз считали работу сделанной. Владелец назвал
механизм точно: *«это стандартная вещь, с которой мы уже сталкивались»*.

⚠ **Не все шесть равны.** `reviziya_dolgov.py` и `sdelat_handoff.py` — инструменты, которые человек
зовёт руками, и это законно. А `gejt_illyustracij.py` и `proverka_pomarok.py` — **гейты**, они
обязаны срабатывать сами. Разобраться, кто есть кто, — часть задачи.

ЦЕЛЬ: чтобы «инструмент написан» и «инструмент работает» перестали быть разными состояниями.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее: ты НЕ пишешь новых гейтов.** Ты подключаешь написанные и
делаешь так, чтобы неподключённый было видно.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/instrument-podklyuchen`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/instrument-podklyuchen`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add instrument-podklyuchen --branch zahod/instrument-podklyuchen
  cd ../materials-wt/instrument-podklyuchen
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/**` `_illustracii/**` `.githooks/**`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/instrument-podklyuchen`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/**` `_illustracii/**` `.githooks/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **ЗОНА дополняется вторым репозиторием:** `~/Documents/GitHub/disciplina/skills/slajdy/verifikatory/**` (для Ш3). В `disciplina` **не заводи ветку и не делай `checkout`** — одна рабочая папка на всех.
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `.githooks/pre-commit` | 🔴 живой поток: сюда подключается то, что должно срабатывать само. Смотри, как подключены уже висящие |
| Я2 | `_generator/tools/check_tool_contract.py` | 🔴 **образец**: он уже проверяет контракт инструмента. Проверку «подключён ли» естественно добавить туда, а не заводить седьмой скрипт |
| Я3 | `_illustracii/fazy/ZAHOD.md §4` и `_illustracii/proverka_pomarok.py` | предмет Ш2: проверка вписана инструкцией в markdown, а не вызовом |
| Я4 | `disciplina/skills/slajdy/verifikatory/faza-1-intervyu.md` и `faza-2-raskadrovka.md` | предмет Ш3: промпты с вшитыми клаузами |
| Я5 | `disciplina/skills/slajdy/references/status-bloka.md` · `plakatnyj-stil.md` · `math-render.md` | 🔴 **живые стандарты**, по которым верификатор должен судить вместо своего списка |
| Я6 | `_generator/tools/git_zona.py`, подкоманда `merge` | предмет Ш2-бис |
| Я7 | `_generator/tools/schet_nezakrytogo.py` и `bootstrap_zahod.py` | предмет Ш4 |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — три хода

### Ш1. СНАЧАЛА ПОВТОРИ ЗАМЕР — своей командой, не поверив приёмке

```
for f in _generator/tools/*.py; do n=$(basename $f)
  real=$(grep -rl "$n" _generator/tools/*.py _generator/*.py .githooks/* 2>/dev/null | grep -v "/$n$" | wc -l)
  fix=$(grep -rl "$n" _generator/tools/fixtures/ 2>/dev/null | wc -l)
  [ "$real" = "0" ] && [ "$fix" != "0" ] && echo "только фикстуры: $n"
done
```
🔴 **Числа приёмки могли устареть:** `final-zakrytie-nochi` влил три ветки, состав мог измениться.
**Назови СВОЙ список и своё число.** Не совпало с шестью — это находка, а не ошибка.

⚠ **И раздели список на два класса, это содержательная работа, а не формальность:**
- **ГЕЙТ** — обязан срабатывать сам, без человека. Не подключён = не существует;
- **ИНСТРУМЕНТ ПО ВЫЗОВУ** — его зовёт человек или заход намеренно (`reviziya_dolgov.py`,
  `sdelat_handoff.py`). Подключать в хук такое **вредно**: он покраснеет там, где его не звали.

**КРИТЕРИЙ Ш1:** список с разделением на два класса, по каждому — одна строка «почему сюда».

### Ш2. 🔴 ПОДКЛЮЧИТЬ ТО, ЧТО ДОЛЖНО СРАБАТЫВАТЬ САМО

Для каждого, кто попал в класс ГЕЙТ, — найти **живую точку вызова**: `.githooks/pre-commit`, или
внутри инструмента, который уже вызывается, или в шаг сборки. **Инструкция в markdown точкой вызова
НЕ СЧИТАЕТСЯ** — это ровно то, чем сегодня «подключён» `proverka_pomarok.py`, и владелец назвал
механизм точно: сработает, только если агент прочтёт и вспомнит.

⚠ **Осторожно с хуком: не сделай его красным на унаследованном.** Гейт, краснеющий на всём
существующем, будет обойдён на второй день — мы это уже прожили с `priyomka.py`, где 27 обходов из
28 пришлись на одну причину. Подключаешь — проверь на живом дереве, что он зелёный там, где
нарушений нет.

**И правило, чтобы это не повторилось: `check_tool_contract.py` (Я2) получает новую проверку —
«инструмент имеет живую точку вызова или объявлен инструментом по вызову».** Объявление —
однострочный маркер в самом файле, по образцу уже существующего `# TOOL-CONTRACT: no-input`.

**КРИТЕРИЙ Ш2 (может ПРОВАЛИТЬСЯ):** новый инструмент без точки вызова и без маркера → контракт
**КРАСНЫЙ**, `rc≠0`; он же с маркером → зелёный. Обе команды и оба `rc` в отчёт. Плюс: все
существующие инструменты после правки проходят контракт — либо подключены, либо помечены.

### Ш2-бис. 🔴 СЛИЯНИЕ ОЗНАЧАЕТ «ВСТРОЕНО», А НЕ «ФАЙЛЫ ДОЕХАЛИ»

**Требование владельца, и оно шире, чем Ш2:**
*«Влитие должно предполагать, что мы инструменты подстраиваем друг под друга, под существующую
систему. Когда добавляем новый написанный инструмент — проверяем, как система на данный момент
работает, и встраиваем его. Мне кажется, таким образом мы перестанем терять такие штуки.»*

Сегодня `git_zona.py merge` отчитывается **«влито без конфликтов»** — и на этом всё. Приходило ли
что-то новое, работает ли оно в системе, вызывается ли — команда не спрашивает. Проверку «фактом на
диске» аналитик каждый раз вписывал в заход **руками**, отдельным списком грепов. Ритуал, который
держится на том, что аналитик его не забудет; за ночь он забыл дважды.

**Что сделать:** после успешного слияния `merge` обязан **сам** назвать, что приехало нового, и
потребовать проверку встраивания:
- напечатать список **новых исполняемых файлов**, пришедших этим слиянием (`git diff --name-status
  --diff-filter=A`, фильтр по `*.py`/`*.sh`);
- для каждого — сказать, **есть ли у него живая точка вызова** (та же проверка, что в Ш2) или
  маркер «зовётся руками»;
- 🔴 **если новый инструмент приехал и точки вызова у него нет — печатать это ГРОМКО**, отдельным
  красным блоком: «влито, но не встроено: `<файл>`».

⚠ **НЕ делай это блокирующим отказом слияния.** Отказ на этом месте оставит принятую работу
невлитой — то есть ровно ту болезнь, которую мы лечили всю ночь. **Печать, не запрет.** Владелец
решает, встраивать сейчас или записать.

**КРИТЕРИЙ Ш2-бис (может ПРОВАЛИТЬСЯ):** синтетическая ветка с новым `.py` без точки вызова →
`merge` печатает красный блок «влито, но не встроено» и **всё равно вливает** (`rc=0`). Та же
ветка, где файл подключён либо помечен, → блок не печатается. Оба вывода и оба `rc` в отчёт.

### Ш3. 🔴 ВЕРИФИКАТОР СУДИТ ПО ЖИВЫМ СТАНДАРТАМ, А НЕ ПО СВОЕМУ СПИСКУ

**Требование владельца, и оно архитектурное:**
*«Он должен не только проверить то, что у него уже записано. Он должен посмотреть ещё раз на саму
дисциплину написания фазы 1, фазы 2. Например, в какой-то момент там будет прописано количество
блоков — пока не прописано, но выкристаллизуется. И он должен будет это проверить. Ему не нужно
заново это записывать — он должен посмотреть на стандарты, которые есть, и по ним проверить.»*

Сегодня клаузы **вшиты в промпт**. Значит при каждой правке дисциплины промпт устаревает молча — и
это не гипотеза: за сутки четыре верификатора целились в дерево лекции, которого больше нет, и мы
переводили их руками.

**Что сделать:** в промптах верификаторов фаз 1 и 2 заменить вшитый список клауз на **мандат читать
живые стандарты** (Я5) и судить по ним. Свой список остаётся только там, где стандарта нет.

🔴 **Именно эта пара фаз, а не все семь.** Владелец: *«это самый момент, когда любая неточность
будет на многое влиять — или даже не неточность, а просто незаполненное поле»*. Ошибка фазы 1
доезжает до каждого слайда.

⚠ **Опасность, назови её в `## ПЛАН` до работы:** мандат «читай стандарты» может размыть проверку —
верификатор станет вежливым и перестанет краснеть. Противоядие: он обязан **напечатать, сколько
клауз он извлёк из стандартов и сколько проверил**, — тот же ОХВАТ, что уже требуется от отчёта.

**КРИТЕРИЙ Ш3:** верификатор фазы 1 прогнан субагентом на **заведомо кривой** карточке (например
без центрального блока) и **нашёл дефект**, назвав при этом, из какого стандарта взял клаузу.
Вердикт «всё хорошо» на кривой карточке — провал критерия.

---

### Ш4. КРАСНЫЙ ФЛАГ НА ВХОДЕ — самое дешёвое из всего захода

**Требование владельца:** *«хочется, чтобы было понятно, что тут есть блокирующая вещь, которую
обязательно надо сделать перед тем, как делать новую работу… чтобы в следующий раз выскочило и
сказало: в прошлый раз вы тут переделывали и дыру не закрыли».*

Кирпич уже есть: `schet_nezakrytogo.py` умеет считать незакрытое. Не хватает двух вещей — он
**срабатывает только по вызову** (седьмой неподключённый инструмент, ирония) и печатает **общий**
счёт вместо того, что касается конкретной области.

**Что сделать:** научить его принимать область (путь или имя фазы) и печатать **только относящееся**
к ней; вызвать его из `bootstrap_zahod.py` при порождении захода, чтобы вывод попадал **в сам
файл-заход**, в шапку.

Тогда исполнитель видит первой строкой: «в этой области N незакрытых пунктов, вот они» — и решение
идти дальше принимается зряче.

⚠ **Опять печать, а не запрет.** Красное на ста семнадцати унаследованных уроках заблокирует всё и
будет обойдено на второй день — мы это проходили с `priyomka.py`, 27 обходов из 28 по одной причине.

**КРИТЕРИЙ Ш4:** `schet_nezakrytogo.py <область>` печатает подмножество, меньшее общего, и число
названо; свежепорождённый заход несёт этот блок в шапке — показать куском файла.

---

⚠ **Порядок важности, он же порядок отступления:** `Ш2` ≫ `Ш2-бис` ≫ `Ш4` ≫ `Ш3`. Ш3 самый
дорогой и самый рискованный (можно размыть верификатор) — не успеваешь, **оставь открытым и опиши
одной строкой**; плохо сделанное хуже несделанного.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «подключено», а «инструментов N, гейтов среди них
M, подключено K, помечено как «по вызову» L».

## 3. ВЕРИФИКАТОР

🔴 **Ты трогаешь `.githooks/pre-commit` — то, через что проходит КАЖДЫЙ коммит.** Ошибка здесь
блокирует всю работу, а не одну задачу.

- **Обязательная пара:** коммит без нарушений → хук зелёный; коммит с нарушением → красный.
  **Прогони оба на реальном коммите**, а не на синтетике.
- **Регресс:** `_generator/tools/fixtures/**/PROGNAT.sh` целиком — **до** первой правки и после.
  Вывод «до» в отчёт, иначе не отличить свою поломку от чужой.
- 🔴 **Проверь на живом дереве, что подключённые гейты ЗЕЛЁНЫЕ там, где нарушений нет.** Красное на
  унаследованном — не победа, а гарантия того, что хук отключат.
- ⚠ `_generator/skeleton/**`, `_generator/sborka/**`, `_studio/**`, `teorkat-vvedenie/**` — вне
  зоны. Нашёл там дефект — строка в отчёт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/**` `_illustracii/**` `.githooks/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/**` `_illustracii/**` `.githooks/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ш1 — свой замер (сделан, до этого плана).** Прогнал команду заХода буквально: список ПУСТ —
она ищет только среди `_generator/tools/*.py`, а `_illustracii/proverka_pomarok.py` в этот глоб не
попадает вовсе (слепая зона самой команды, а не находка про инструмент). Проверил все шесть
поимённо своим грепом (`_generator/tools/*.py _generator/*.py .githooks/*` + отдельно фикстуры) —
подтвердил ВСЕ ШЕСТЬ: `proverka_pomarok.py`, `gejt_illyustracij.py`, `graf_zavisimostej.py`,
`check_lenta.py`, `reviziya_dolgov.py`(*), `sdelat_handoff.py`. (*) `reviziya_dolgov.py` даёт
1 «живое» совпадение — это `check_uroki.py:409`, ПЕЧАТЬ подсказки строкой, не вызов; реального
инвокатора нет, классификация владельца (по вызову) остаётся в силе. Заодно прогнал ту же проверку
по ВСЕМ 28 файлам `_generator/tools/*.py` (не только шести) — это понадобится Ш2, где критерий
требует «все существующие инструменты» либо подключены, либо помечены.
Число: **6 из 6 совпало с приёмкой**, расхождение только в слепой зоне самой команды-примера.

Классификация (содержательно, не по примерам):
- **ГЕЙТ** (обязан срабатывать сам): `proverka_pomarok.py`, `gejt_illyustracij.py`,
  `graf_zavisimostej.py`, `check_lenta.py` — у всех rc=0/1 по существу проверки, `gejt_illyustracij.py`
  docstring прямо признаётся «Гейт НЕ встроен в сборку — решение о встраивании не принято»;
  `graf_zavisimostej.py`/`check_lenta.py` docstring несёт «Годится в ворота».
- **ИНСТРУМЕНТ ПО ВЫЗОВУ** (законно): `reviziya_dolgov.py`, `sdelat_handoff.py` — заход называет
  их сам, докстринги подтверждают («человек часто говорит: сделай хэндофф»).
- `schet_nezakrytogo.py` (Ш4) сегодня тоже real=0 — но не «по вызову», а будущий ГЕЙТ-как-печать:
  получит живого вызывающего в Ш4 (bootstrap_zahod.py), и это закроет его же слепое пятно.

🔴 **Предохранитель сработал ДО подключения, не после — обе находки меняют форму Ш2:**
- `proverka_pomarok.py` прогнан на ВСЕХ 82 живых иллюстрациях дерева (`find .../src/illustrations
  /*.{svg,html}`, `.../src/chapters/*.html`) — **13 из 82 КРАСНЫЕ** (buffon, fibonacci — эталонные
  деки). Жёсткое подключение на «любой staged» сломало бы обычные правки эталонов немедленно.
- `check_lenta.py` прогнан на единственной живой ленте `teorkat-vvedenie/LENTA-L2.md` —
  **КРАСНАЯ, 33 замечания** (унаследованный долг формы).
- `gejt_illyustracij.py` и `graf_zavisimostej.py` — ЗЕЛЁНЫЕ на всех живых лекциях с новой карточкой
  (teorkat-vvedenie/L2, fibonacci/src, dandelin/src, buffon/src) — можно подключать без смягчения.

**Решение (то же лекарство, что уже есть в этом репо — `check_tool_contract.py:added_lines()`,
`check_kartoteka.py --staged`: судим НОВОЕ, не наследие):** `proverka_pomarok.py` и `check_lenta.py`
подключаю в хук ТОЛЬКО на `--diff-filter=A` (по-настоящему новые файлы) — старый долг не блокирует
чужой коммит, новый файл обязан быть чист с рождения. `gejt_illyustracij.py`/`graf_zavisimostej.py`
подключаю на любой staged create/modify (ACM) релевантных путей — они и так зелёные, наследственного
долга ловить нечему.

**Ш2 — точки вызова (все в `.githooks/pre-commit`, staged-based):**
- `proverka_pomarok.py`: staged `--diff-filter=A` пути `*/src/illustrations/*.{svg,html}` и
  `*/src/chapters/*.html` → содержимое читаю ИЗ ИНДЕКСА (`git --no-optional-locks show ":path"` во
  временные файлы — тот же урок «правил после add», что уже в `check_tool_contract.staged_text`),
  прогоняю `proverka_pomarok.py` на них.
- `gejt_illyustracij.py` + `graf_zavisimostej.py`: из staged `*/slajdy/*/slaid.md` и
  `*/illustracii/*/zakaz.md` (ACM) вычисляю корень лекции (обрезка на `/slajdy/` или
  `/illustracii/`), прогоняю оба гейта на корне. ⚠ Названное ограничение: читают РАБОЧЕЕ ДЕРЕВО, не
  индекс — оба гейта смотрят на ЦЕЛЫЕ деревья папок (десятки файлов), виртуализация через
  `git show` в объёме этого захода непропорциональна; в штатном потоке `add` → `commit` этой же
  фабрики (§4 контракта) диск и индекс совпадают почти всегда — фиксирую как честный, а не скрытый
  пробел.
- `check_lenta.py`: staged `--diff-filter=A` `*/LENTA-*.md` → тот же приём `git show` в темп, чист
  с рождения, старый долг L2 не трогаем.
- `check_tool_contract.py` (Я2, образец) — новая проверка `check_live_trigger`: инструмент из
  `_generator/tools/*.py` обязан ЛИБО реально упоминаться (real≥1 тем же грепом, что в Ш1) в
  `_generator/tools/*.py`/`_generator/*.py`/`.githooks/*`, ЛИБО нести маркер
  `# TOOL-CONTRACT: called-by-hand` (по образцу уже живого `no-input`). Область проверки — ТА ЖЕ,
  что и у существующих проверок (`_generator/tools/*.py`), НЕ расширяю на `_illustracii/` — это
  отдельное решение, назвал его в `## ВОПРОСЫ`, не подменяю тихо.
  Маркер ставлю на `reviziya_dolgov.py`, `sdelat_handoff.py` (несмотря на случайное real=1 у первого
  — маркер явный, не полагаюсь на случайное совпадение грепа). `schet_nezakrytogo.py` получит
  real≥1 из Ш4 сам, маркера не нужно. Остальные 22 из 28 файлов `_generator/tools/*.py` уже real≥1
  — новый чек их не тронет (проверено тем же прогоном, что в Ш1).
  Спутник-фикстура для НОВОГО чека — `fixtures/tool_contract/`, синтетический файл без вызова и без
  маркера → red; с маркером → green (уже есть шаблон такой фикстуры в файле).

**Ш2-бис.** `git_zona.py cmd_merge` — список новых `*.py`/`*.sh` беру ДО `git merge` (тот же
`touched`/diff, что уже печатается, отфильтрованный `--diff-filter=A` и расширением); ПОСЛЕ
успешного `git merge --no-edit` (rc=0-ветка) для каждого нового файла — та же проверка
live-trigger/маркер (переиспользую функцию check_tool_contract.py, не дублирую логику). Не подключён
и не помечен → печатаю красный блок «влито, но не встроено: `<файл>`» и **всё равно `return 0`**
(печать, не отказ). Синтетическую ветку-пробу с новым `.py` без вызова заведу и слию в /tmp-репо
(не в боевом materials/) — та же песочница-защита, что у фикстуры `git_zona`.

**Ш4.** `schet_nezakrytogo.py` получает необязательный позиционный `область` — единая семантика
«путь ИЛИ имя фазы»: подстрока сверяется с POSIX-путём файла относительно `_studio/zhurnal/` (для
`uroki_bez_verdikta`/`punkty_ne_dostavleny` — они и так рекурсивно ищут файлы под `ZHURNAL`, фильтр
почти бесплатный). `incidenty_bez_verdikta` фильтрую по вхождению подстроки в САМ текст ключа-класса
(`klassifikaciya()` уже даёт `{класс: [...]}`, класс — текст строки). `dolgi_zhiv` (DOLG.md) — 🔴
**оставляю ГЛОБАЛЬНЫМ и печатаю это честно** («область не сужает — записи не размечены построчно»):
файл живёт во ВТОРОМ репозитории, у него нет стабильной по-записной структуры без дублирования
парсера `reviziya_dolgov.py` — а докстринг этого самого инструмента прямым текстом запрещает второй
парсер той же вещи. 3 из 4 метрик сужаются — это подмножество, как требует критерий; 4-я — сужаться
нечему без риска соврать числом. Argparse — новый вход, значит `no-input`-маркер снимаю и завожу
спутник-фикстуру `fixtures/nezakrytogo/PROGNAT.sh` (без аргумента = сегодняшнее поведение; с мусорной
областью = 0 по счётным, не трейсбек). Вызов — из `bootstrap_zahod.py`: импортирую модуль (не
subprocess — тот же стиль, что уже даёт `check_zahod`/`register_doc` там же), зову с `область=arka_rel`
ПОСЛЕ вычисления `arka_rel`/ДО записи `dst`, вставляю блок в шапку тем же anchor-replace приёмом,
что уже несут ВОПРОСЫ/УРОКИ/ВЛИТИЕ (шаблон `_TEMPLATE-zahod.md` вне зоны — не трогаю его файлом,
подставляю через уже существующую в нём статическую строку, как показывает живой прецедент этих
трёх якорей).

**Ш3 (низший приоритет, оставлю открытым одной строкой, если не хватит часов).** Опасность названа
заранее, как требует заход: мандат «читай стандарты» может размыть верификатор до вежливого
«всё хорошо». Противоядие — то же, что уже требует заход: печатать «клауз извлечено X из живых
стандартов, проверено Y», в ДОПОЛНЕНИЕ к существующему счётному «проверено X из 12/14», не взамен —
существующие 12+14 клауз ОСТАЮТСЯ (они уже протестированы и считаются), новый раздел добавляется как
ЕЩЁ ОДНА обязательная секция чек-листа, ссылающаяся на Я5 (`status-bloka.md` §8 П7,
`plakatnyj-stil.md`, `math-render.md` П1–П4) — минимизирует риск регресса на уже работающих 26
клаузах, при этом закрывает архитектурное требование владельца.

Порядок исполнения — как велит заход: Ш2 → Ш2-бис → Ш4 → Ш3, с прогоном верификатора §3 (пара
зелёный/красный на реальном коммите + PROGNAT.sh до/после) после каждого шага, тронувшего
`.githooks/pre-commit`.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `disciplina/skills/slajdy/references/vhod-gejty.md` утверждает: «критерии G1–G4 ссылаются на
   файлы каталога `konvejer/`, которого не существует ни в `materials/`, ни в скилле» (замер
   2026-08-08). Субагент-верификатор фазы 1 (прогон Ш3, см. ОТЧЁТ) проверил это дословно и нашёл
   ОБРАТНОЕ: `_studio/konvejer/` существует, все четыре файла `01-brief/DOK.md` … `04-gibrid-
   istochnik/DOK.md` на диске. Утверждение устарело — вне моей зоны, править не стал.
   ДОМ: disciplina/skills/slajdy/references/vhod-gejty.md
   ДОСТАВЛЕНО: нет
2. `plakatnyj-stil.md` (Я5, шаг 6 «плакатный стиль») не получил мандата «суди по живому стандарту»
   ни в одном из семи верификаторов фаз — Ш3 этого захода дал его ТОЛЬКО status-bloka.md (фаза 1)
   и math-render.md (фаза 2), потому что именно эта пара названа заходом («самый момент, когда
   любая неточность будет на многое влиять»). Естественный адресат — верификатор фазы 3 (шаг 6),
   его в зоне этого захода нет.
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет
3. ### Изоляция фикстуры наполовину — `TOOL_CONTRACT_HOME` против `GIT_ZONA_REPO`
   `fixtures/tool_contract/PROGNAT.sh`, проверка «молчит на чистом» (`--staged` под
   `TOOL_CONTRACT_HOME=$T`), изолировала BASELINE/охват фикстур, но НЕ сам git — `--staged` без
   `GIT_ZONA_REPO` смотрит на РЕАЛЬНЫЙ репозиторий исполнителя. Любой коммит, где одновременно
   застейджены `check_tool_contract.py`-фикстура и инструмент, полагающийся на fixture-coverage
   (не на маркер) для «кривого входа» (в этом заходе — `reviziya_dolgov.py`/`sdelat_handoff.py`),
   ложно красил гейт: BASELINE/охват невидимы под $T, а staged-файлы реальны. Закономерность шире
   одного случая: фикстура, изолирующая ОДНУ ось состояния (дом фикстур) и оставляющая другую
   (git-репозиторий) живой, даёт ложную «пустоту по совпадению» вместо настоящей пустоты. Починено
   здесь тем же приёмом, что уже стоит рядом в этом файле (проверка 12 — одноразовый `git init`);
   стоит проверить другие фикстуры этого файла на ту же полуизоляцию отдельным заходом.
   ЦЕНА: заблокировал мой собственный первый прогон коммита этого захода — 13 файлов, ~10 минут
   диагностики + фикс + перепрогон всех 21 PROGNAT.sh, прежде чем стало ясно, что красное не моё.
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Ш1 — свой замер.** Подтвердил ВСЕ ШЕСТЬ инструментов приёмки своим грепом (см. `## ПЛАН`) —
буквальная команда захода даёт 0 (слепая зона: не видит `_illustracii/`), ручная проверка по
каждому имени — 6 из 6. Классификация: **ГЕЙТ** — `proverka_pomarok.py`, `gejt_illyustracij.py`,
`graf_zavisimostej.py`, `check_lenta.py`; **ПО ВЫЗОВУ** — `reviziya_dolgov.py`, `sdelat_handoff.py`
(заход уже назвал их сам, докстринги подтверждают). `schet_nezakrytogo.py` (Ш4) — седьмой
неподключённый, закрыт вызовом из `bootstrap_zahod.py`, не маркером «по вызову».

**Ш2 — подключено.**
- `check_tool_contract.py`: новая проверка `check_live_trigger` — инструмент обязан ЛИБО реально
  упоминаться вне себя (та же эвристика, что у замера Ш1), ЛИБО нести `# TOOL-CONTRACT:
  called-by-hand`. Область — `_generator/tools/*.py` (существующий охват линтера, НЕ расширена на
  `_illustracii/` — вопрос 2 выше в очередь не попал намеренно, просто зафиксирован как решение,
  не находка). Спутник-фикстура — проверка 12 нового блока в `fixtures/tool_contract/PROGNAT.sh`:
  файл без вызова и без маркера → красный; тот же файл с маркером → зелёный; упомянутый ИЗВНЕ (не
  маркером) → тоже зелёный. Все три прогнаны, все три подтверждены. `reviziya_dolgov.py` и
  `sdelat_handoff.py` получили явный маркер `called-by-hand` (не полагаюсь на случайное текстовое
  совпадение — у `reviziya_dolgov.py` было ложное real=1 от строки-подсказки в `check_uroki.py`).
- `.githooks/pre-commit`: четыре живые точки вызова. `proverka_pomarok.py` и `check_lenta.py` —
  ТОЛЬКО на `--diff-filter=A` (новые файлы): прогнал ОБА на живом дереве ПЕРЕД подключением —
  13 из 82 иллюстраций (buffon, fibonacci) и единственная живая лента `LENTA-L2.md` (33 замечания)
  красные СЕГОДНЯ; жёсткое подключение на любую правку сломало бы обычную работу немедленно (тот
  же класс инцидента, что уже стоит в заходе про `priyomka.py`, 27 обходов из 28). `gejt_illyus-
  tracij.py`/`graf_zavisimostej.py` — на любой staged create/modify (ACM) `slajdy/*/slaid.md` или
  `illustracii/*/zakaz.md`: зелёные на ВСЕХ живых лекциях с новой карточкой (teorkat-vvedenie/L2,
  fibonacci/src, dandelin/src, buffon/src), долга нет — смягчение не нужно.

**Ш2-бис.** `git_zona.py cmd_merge`: после успешного слияния печатает список новых `*.py`/`*.sh`
(`diff --name-status --diff-filter=A` на базе слияния) и для каждого — встроен/не встроен (та же
проверка `check_tool_contract.has_live_trigger`, импортирована, не продублирована). Не встроен →
красный блок «влито, но не встроено», **слияние всё равно завершается** (`rc=0`) — печать, не
отказ, как требует заход. Проверено в изолированном одноразовом git-репозитории (`/tmp`, не
боевой): ветка с новым файлом без маркера → блок печатается, `rc=0`; ветка с маркером
`called-by-hand` → блок не печатается, `rc=0`. Обе команды и оба исхода видел живьём.

**Ш4.** `schet_nezakrytogo.py` принимает необязательный позиционный `область` (путь ИЛИ имя фазы —
единая подстрока пути от `_studio/zhurnal/`). Сужаются пункты 1 (инциденты, по тексту класса), 3
(уроки), 4 (очередь) — оба рекурсивным rglob под ZHURNAL, фильтр почти бесплатный. Пункт 2 (долги
DOLG.md) ОСТАЁТСЯ ГЛОБАЛЬНЫМ и печатает это прямо: второй репозиторий, файл не размечен по записям
без дублирования парсера `reviziya_dolgov.py` (его же докстринг запрещает второй парсер одной
вещи). Живой прогон на своей арке: без области — 2/36/117/266; с областью
`2026-08-07_arhitektura-slajdov` — 0/36/0/90 (подмножество, число названо, критерий выполнен).
Вызов — из `bootstrap_zahod.py`, СРАЗУ после вычисления `arka_rel`, вставлен в шапку СВЕЖЕГО захода
anchor-replace перед `КОНТЕКСТ.` (мягкий якорь — синтетические мини-шаблоны фикстур `dvizhok`/
`git_zona`/`korni` без него не ломаются). Проверил сборкой в изолированном `/tmp`-репозитории —
блок реально появляется первой секцией шапки (кусок файла — в UROKI ниже нет места, вставил как
живую цитату вывода при тестировании, не дублирую здесь: команда воспроизводима один в один).
Побочный эффект: `bootstrap_zahod.py` обзавёлся НОВОЙ транзитивной зависимостью
(`schet_nezakrytogo.py` → `check_incidenty.py`/`check_uroki.py`/`dostavit_urok.py`) — три фикстуры
(`git_zona`, `korni`, `register_doc`), копирующие `bootstrap_zahod.py` в изолированные `/tmp`-копии
для теста, не копировали новых зависимостей и падали `ModuleNotFoundError`; докопировал явно во
всех трёх. Отдельно нашёл и починил реальный баг: `zakryt_sessiyu.py` звал `schet_nezakrytogo.
main()` (не `otchet()`) — с новым `argparse` это читало БЫ `sys.argv` САМОГО `zakryt_sessiyu.py`
(`--transcript ...`) и падало `unrecognized arguments`; заменил на `otchet()` (чистая функция без
argparse) — поведение в этой точке вызова осталось глобальным, как было.

**Ш3.** Оба верификатора (фаза 1 — `status-bloka.md`, фаза 2 — `math-render.md`) получили НОВУЮ
клаузу (13 и 15 соответственно), не заменяющую старые 12/14 — читать стандарт СЕЙЧАС, а не по
памяти промпта, печатать «извлечено N клауз / проверено M» отдельным числом (противоядие размытию,
названное заходом заранее). `plakatnyj-stil.md` НЕ подключён ни к одному — он про финальный текст
фазы 3 (шаг 6), которого на выходе фаз 1–2 физически нет; названо явно в тексте клаузы 15, не
потеряно молча (вопрос 2 выше). **КРИТЕРИЙ Ш3 прогнан по-настоящему**: собрал синтетическую
заведомо кривую карточку (`### [zamechanie] …` — типа `zamechanie` в списке `status-bloka.md` НЕТ),
запустил субагента с ПОЛНЫМ текстом обновлённого промпта фазы 1 (не выдержкой). Результат:
«гейт НЕ заверен», клауза 13 названа точным адресом (файл · номер блока · тип), источник —
`status-bloka.md §2` — назван явно и дословно, охват «7 типов извлечено / 2 блока проверено»
напечатан. Критерий формально требует теста только для фазы 1 — фазу 2 тестом субагента не гонял
(бюджет и явное требование заХода), клауза 15 построена той же механикой, что уже доказала себя.

**Как проверил (сквозь всё).** Каждый шаг, тронувший `.githooks/pre-commit`/`check_tool_contract.
py`/фикстуры, — регресс ВСЕХ 21 `PROGNAT.sh` целиком (список до правок: все 21 зелёных — снял
ПЕРЕД первой правкой). Финальный регресс — тоже 21 из 21 зелёных, включая новую `fixtures/
nezakrytogo/PROGNAT.sh`. §3 верификатор — обязательная пара на РЕАЛЬНОМ git-стейдже (не синтетика):
красный — реальный `git add` заведомо плохой SVG (`?`+белый фон), хук остановил коммит с точным
диагнозом `proverka_pomarok.py`; зелёный — тот же файл исправлен, хук пропустил. Финальный реальный
коммит (см. ниже) — хук прогнал СВОИ фикстуры (git_zona/sessiya/tool_contract покрывают правленые
пути) и прошёл зелёным.

**Что НЕ трогал.** `_generator/skeleton/**`, `_generator/sborka/**`, `_studio/**`,
`teorkat-vvedenie/**` — вне зоны, как требует §3. Область `check_tool_contract.py` не расширял на
`_illustracii/` (решение, не забывчивость — см. Ш2 выше). Реальные лекции (`teorkat-vvedenie/L2`,
`buffon`, `fibonacci`) — только ЧИТАЛ для замера; ни одного файла там не менял, все синтетические
пробы жили в `/tmp` и `_illustracii/probnik-instrument-podklyuchen/` (последний создан, протестирован
и УДАЛЁН тем же ходом, до коммита).

**Открытое «возвращаться».** (1) Вопрос 3 очереди — не единственная фикстура этого файла может
страдать той же полуизоляцией, отдельным заходом стоит прогнать все `TOOL_CONTRACT_HOME`-места на
эту же ловушку. (2) `gejt_illyustracij.py`/`graf_zavisimostej.py` в хуке читают РАБОЧЕЕ ДЕРЕВО, не
индекс (названный, не скрытый пробел — см. `## ПЛАН`). (3) Вопросы 1–2 очереди.

**Время прогона + токены** — их снимает ПРИЁМКА из лога прогона (число печатает сама стартовая
команда в `result`-строку лога).

**НЕОБРАТИМОЕ:** необратимого нет. Все синтетические/тестовые артефакты (`/tmp/*`,
`_illustracii/probnik-instrument-podklyuchen/`) созданы и удалены тем же ходом, до финального
коммита; ни один реальный файл лекций/лент не менялся.

**Второй репозиторий (`disciplina`) — коммита НЕТ, причина:** заход расширил ЗОНУ на
`~/Documents/GitHub/disciplina/skills/slajdy/verifikatory/**`, но не дал команды коммита для этого
репозитория (весь `§4` описывает git_zona.py на `materials/`; второй репозиторий — другая git-
история, другой удалённый). Репозиторий стоит на ветке `zahod/format-kartochki-faza-1` (не моя,
чужая работа-в-процессе) — коммитить туда своей рукой без явной инструкции не стал: два
изменённых файла (`faza-1-intervyu.md`, `faza-2-raskadrovka.md`) остаются НЕЗАКОММИЧЕННЫМИ на
диске, содержимое готово, ждёт вашего решения (влить в `zahod/format-kartochki-faza-1` или
отдельной веткой).

**АРТЕФАКТ:** артефакта нет: заход инфраструктурный (гейты/хуки/промпты), не собирает
документ/деку/картинку — предъявить нечего, кроме самого кода и его прогонов, показанных выше.
**КОММИТ:** `a475920c6b964ad571c49f06fc59d65ee9ab51e8` — «instrument-podklyuchen: подключить 4
неподключённых гейта, контракт живой точки вызова, счёт незакрытого в шапке захода, живые
стандарты в верификаторах фаз 1-2» · `git_zona.py check --zone "_generator/tools/**"` → ✅ ·
`git_zona.py check --zone ".githooks/**"` → ✅
