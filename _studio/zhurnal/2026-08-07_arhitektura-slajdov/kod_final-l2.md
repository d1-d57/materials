# Канал исполнителя — final-l2 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — слияние двух веток, доводка и решение по контрасту требуют суждения по ходу.
> 🔴 **ЭТО ПОСЛЕДНИЙ ЗАХОД ПО Л2.** После него дека идёт в эфир. Ничего нового не начинать.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-07_arhitektura-slajdov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/bold-brave-cray/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекция 2 по теории категорий, **эфир в среду**. Владелец: *«давай кратко доведём все
мелочи, добавим обложку, визитку и последнюю вкладку, и собирать финальную версию. Последний
финальный заход… и всё вливать и коммитить»*.

**Всё построено, осталось свести и добить.** Две ветки ждут влития — влить их первым ходом:
```
git branch --no-merged arka/mat-kostyak → zahod/tri-provodki · zahod/finalnaya-dovodka
```
- `tri-provodki` — солвер видит полосы (было 0 из 9, стало 9 из 9) · **генератор порождает обложку,
  визитку и финальный** · подбор перестал быть одноразовым;
- `finalnaya-dovodka` — правила стрелок в словаре с ловушкой, наконечники пересчитаны, правки по
  восьми слайдам.

🔴 **Служебные слайды НЕ заводить руками — их порождает `deck.py`,** это и есть работа `tri-provodki`.
Твоё дело — дать ему данные в `brief.md` и убедиться, что они появились.

ЦЕЛЬ: дека в **основной папке**, со служебными слайдами, мелочи закрыты, всё влито и закоммичено.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: сломалось при слиянии — скажи громко и сразу.** До эфира считанные дни, и «почти
собрал» здесь хуже, чем «встал вот на чём».

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/final-l2`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/final-l2`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add final-l2 --branch zahod/final-l2
  cd ../materials-wt/final-l2
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `teorkat-vvedenie/L2/**` `_illustracii/**` `_generator/skeleton/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `disciplina/skills/slajdy/**`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 ГИТ-КОНТУР

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Порядок именно такой — до всего остального.

```
python3 _generator/tools/git_zona.py merge zahod/tri-provodki --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _fond --zone obzory --zone ucheniki --zone _illustracii
python3 _generator/tools/git_zona.py merge zahod/finalnaya-dovodka --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _fond --zone obzory --zone ucheniki --zone _illustracii
```

🔴 **Зона слияния — НЕ рабочая зона захода, а ВСЁ, что трогают ветки.** Узкая зона → инструмент
откажется слить что-либо; на этом вставали четыре захода подряд. Отказал — **возьми строку зон из
его собственного отказа.**
⚠ **Порядок обязателен:** `tri-provodki` правит генератор, `finalnaya-dovodka` — лекцию и словарь.
⚠ Оба трогают `teorkat-vvedenie/L2/**`: конфликт вероятен. **Разрешать в пользу `finalnaya-dovodka`
по СОДЕРЖАНИЮ карточек** (там последние правки владельца) и **в пользу `tri-provodki` по коду**.
Каждое разрешение — командой-доказательством в отчёт, не «взял по смыслу».

**Проверка фактом по ОБЕИМ:**
```
git --no-optional-locks merge-base --is-ancestor zahod/tri-provodki HEAD ; echo $?
git --no-optional-locks merge-base --is-ancestor zahod/finalnaya-dovodka HEAD ; echo $?
git --no-optional-locks branch --no-merged arka/mat-kostyak
```
Ветки НЕ удаляй ни при каком исходе.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда.

🔴 **Забрать ВСЁ, что осталось незакоммиченным** — заход финальный, хвостов после него быть не должно. Своя зона целиком, включая пересобранную `teorkat-vvedenie/L2/dist/index.html`. 🔴 **И правку аналитика во ВТОРОМ репозитории:** `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` — раздел «Две части конвейера» с контрактом шва, написан Cowork и лежит незакоммиченным. **Два репозитория — два коммита, второй забывают.** ⚠ `_fond/**`, `obzory/**`, `ucheniki/**`, `_generator/build_listok.py` — чужое, не трогать.

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся и какие трогать НЕЛЬЗЯ.

🔴 **Гасить нечего: ветки `zahod/*` и рабочие папки не удаляются никогда — решение владельца.** Свою ветку не вливай, влитие за приёмкой. Параллельных заходов сейчас нет — поле чистое.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/final-l2`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `teorkat-vvedenie/L2/**` `_illustracii/**` `_generator/skeleton/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `disciplina/skills/slajdy/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_finalnaya-dovodka-l2.md`, секция `## ОТЧЁТ` | **девять находок вне задания** — из них ты делаешь Ш3; там же замеры, не переоткрывай |
| Я2 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_tri-provodki.md`, секция `## ОТЧЁТ` | какие поля `brief.md` читает генератор служебных слайдов (Ш2) |
| Я3 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/PRAVKI-SBORKI-L2.md`, **§2 С3** | что владелец хотел на обложке |
| Я4 | `teorkat-vvedenie/L2/brief.md` | вход Ш2 |
| Я5 | `_illustracii/DISCIPLINA.md`, разделы про палитру и `--steel` | предмет Ш4 (контраст) |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — пять шагов, все короткие

### Ш1. ВЛИТЬ ОБЕ ВЕТКИ (команды — §0.1) и убедиться фактом

### Ш2. СЛУЖЕБНЫЕ СЛАЙДЫ — дать данные, генератор сделает сам

Заполнить в `brief.md` (Я4) поля, которые читает `deck.py` (список — в Я2): заголовок обложки,
иллюстрация обложки, заголовок и текст визитки, заголовок финального.
🔴 **Папки слайдов НЕ заводить** — обложку, визитку и финальный порождает генератор.

**Что на обложке — решение владельца (Я3 §2 С3), дословно:** *«коммутативную диаграмму с первого
слайда можно поместить на обложку — написать „Функторы“ и нарисовать коммутативную диаграмму, это
будет красиво»*. Картинка `pustoj-kvadrat` снята со слайда 1 и лежит в `illustracii/_snyato/` —
**вернуть её оттуда на обложку, а не рисовать заново.**
⚠ Заголовок обложки ≠ внутренний `title`. Владелец назвал «Функторы»; если на обложке уместнее
полная формулировка — предложи, но по умолчанию бери его слова.
⚠ **Визитка уже заложена в генератор целиком — пропусков в ней нет.** Владелец: *«визитка загружена
в генератор, там нету пропусков, просто надо породить стандартным способом»*. ⇒ **Ничего для неё не
заполняй и не выдумывай — просто дай генератору её породить.**

### Ш3. МЕЛОЧИ — из девяти находок Я1 бери ТОЛЬКО дешёвые и бесспорные

🔴 **Владелец сказал «кратко доведём мелочи». Это не новый круг доводки.** Делай то, что чинится
без решения владельца, остальное — строкой в отчёт:

**Делать:**
- слайд 5: **остриё вектора утоплено в узел** (вершина под `circle r=4.6`) — развести;
- слайд 5: **пунктир-проекция не доходит до оси** 2 единицы — дотянуть;
- слайд 4: **метка `2v` вплотную к параллелограмму** — отодвинуть;
- слайды 7 и 9: **горизонтальные стрелки не отцентрованы** (зазор считался без 9 единиц
  наконечника) — пересчитать;
- слайд 9: **звёздочки набраны ASCII** `**` вместо юникодных надстрочников — привести к тому же
  виду, что на слайде 7.

**НЕ делать, только назвать:**
- слайд 9, `A` против `f` — **выбор владельца**, `A` стоит по его же указанию в карточке;
- слайд 5, правая половина картинки дублирует строку текста — **решение о том, что убрать, его**;
- слайд 18, три диаграммы в разных масштабах — правка не мелкая, отдельный вопрос.

### Ш4. КОНТРАСТ ПОДПИСЕЙ — если выходит дёшево

Замер `finalnaya-dovodka`: `.s-txt-m` на доске даёт **1,6 : 1** (`--steel: #8195ad` на
`--board: #a7c2cb`). Имя стрелки в коммутативном квадрате — это содержание, а сейчас оно самое
бледное на рисунке.
🔴 **И в словаре записана ложная предпосылка** (правка палитры 31.07): «`--steel` не трогаем, им
крашены приглушённые подписи, **а они стоят не на доске**». В деке они стоят именно на доске.
⇒ Подобрать значение, дающее **не меньше 4,5 : 1**, проверить, что не сломало другие носители, и
**поправить предпосылку в словаре с датой**. ⚠ Не выходит дёшево или ломает соседей — **не трогай**,
строкой в отчёт: до эфира важнее целая дека, чем идеальная.

### Ш5. СОБРАТЬ, ПОСМОТРЕТЬ, ЗАКОММИТИТЬ ВСЁ

Сборка **в основную папку** — `teorkat-vvedenie/L2/dist/index.html`, БЕЗ `--bez-podbora`.
Затем коммит всего незакоммиченного (см. слот «ЗАКОММИТИТЬ ПО ХОДУ»), **включая правку Cowork во
втором репозитории**.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `git branch --no-merged arka/mat-kostyak` → только твоя ветка; `merge-base --is-ancestor` по каждой из двух → rc=0.
2. 🔴 **Живой прогон на реальном объекте:** дека собрана БЕЗ `--bez-podbora`, rc=0;
   **в ней есть обложка, визитка и финальный** (`grep -c 'id="oblozhka"'` и т.д. → по 1); плюс
   `ls teorkat-vvedenie/L2/slajdy/` → папок служебных слайдов там нет, все три пришли из `deck.py`;
   `grep -c MISSING-MATH` → **0**; `gejt_vmeshcheniya.py` → **обрезанных 0**.
3. Гейты зелёные: `gejt_kartochki.py`, `gejt_verstki.py`, `gejt_illyustracij.py`,
   `proverka_pomarok.py`, `check_karty.py` — все rc в отчёт.
4. Ш3: **пять мелочей из пяти** сделаны, три названы как не-мелочи. Охват числом.
5. Ш4: либо контраст ≥ 4,5 : 1 и предпосылка в словаре поправлена, либо честное «не трогал, потому
   что…». **Молчания быть не должно.**
6. 🔴 **Хвостов не осталось:** `git status --porcelain` в ОБОИХ репозиториях → пусто (кроме чужого
   `_fond/`, `obzory/`, `ucheniki/`, `build_listok.py` — их перечислить отдельно).

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «доведено», а «мелочей N из 5, служебных слайдов
M из 3, гейтов зелёных K из 5».

## 3. ВЕРИФИКАТОР

🔴 **Это последняя проверка перед эфиром. Смотреть надо на то, что увидит зал.**

- 🔴 **Глазами смотреть ТОЛЬКО ТРИ НОВЫХ КАДРА** — обложку, визитку, финальный. Требование владельца
  дословно: *«смотреть 21 кадр глазами не нужно, максимум новые слайды — лучше, быстрее и дешевле
  с этим справится»*. **Восемнадцать содержательных слайдов проверены прошлым заходом покадрово и
  повторной проверки не требуют.**
  ⚠ Остальное меряется, а не смотрится: горизонтальный вылет и обрез — командой по всем слайдам
  (`gejt_vmeshcheniya.py` + замер ширин), наконечники — ловушкой `proverka_pomarok.py`. **Глаз нужен
  там, где нет замера, а не вместо него.**
- **Ничего не потеряно при слиянии:** `git show --stat` по merge-коммитам; правки
  `finalnaya-dovodka` по восьми слайдам на месте — проверить выборочно три из восьми командой.
- **Эталоны целы:** `vlozhenie-i-vozvrat` и `_snyato/retrakt-i-obraz` не изменены.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/L2/** _illustracii/** _generator/skeleton/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/** _studio/docs/KARTA.md disciplina/skills/slajdy/**                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/L2/** _illustracii/** _generator/skeleton/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/** _studio/docs/KARTA.md disciplina/skills/slajdy/**   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `teorkat-vvedenie/L2/**` `_illustracii/**` `_generator/skeleton/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `disciplina/skills/slajdy/**`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/L2/** _illustracii/** _generator/skeleton/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/** _studio/docs/KARTA.md disciplina/skills/slajdy/**` → ✅. Красное здесь — отчёт не принимается: приёмка гоняет этот же гейт первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Критерий 1 неисполним по построению: он требует ровно того, что контракт зоны запрещает
Клауза 1 велит довести `git branch --no-merged arka/mat-kostyak` до «только твоя ветка». Но
`tri-provodki` и `finalnaya-dovodka` сходят с этого списка не тогда, когда я влил их В СЕБЯ, а
тогда, когда МОЯ ветка влита в `arka/mat-kostyak`, — а это тот же заход прямо запрещает («Ветку в
конце НЕ вливай — влитие делает приёмка»). Проверяемо: после обоих успешных слияний список
показывает три ветки вместо одной.
ЦЕНА: развилка, у которой оба выхода плохие. Аккуратный исполнитель тратит ход на разбор и сдаёт
формально проваленный критерий при полностью выполненной работе; неаккуратный — вливает свою ветку
в `arka/mat-kostyak` из рабочей папки и подменяет файлы у восемнадцати живых worktree. Рабочая
формулировка: «`merge-base --is-ancestor <каждая> HEAD` → rc=0», она и была единственной, что
реально что-то проверяла.

### Гейт иллюстраций ослеп ровно в тот день, когда служебные слайды стали порождаемыми
`gejt_illyustracij.py` собирает список «на что ссылаются» только из карточек на диске
(`slajdy/*/slaid.md`). После `tri-provodki` обложка/визитка/финальный карточек на диске не имеют —
их иллюстрации названы в `brief.md` полями `*_illustracii`. Итог: картинка на обложке ВСЕГДА
краснеет как `Z3` «заказ-сирота», при полностью исправной деке.
ЦЕНА: на Л2 — один красный гейт из пяти на финальном заходе перед эфиром, разбирать который
пришлось вручную. Дальше повторится на каждой лекции, где на обложке есть картинка, — то есть по
замыслу владельца на каждой. Два слитых заходом куска фабрики не знали друг о друге, и не увидел
этого ни один из них: каждый по отдельности зелёный.

### Заход пересказал слова владельца там, где ниже сам их цитирует, — и пересказ разошёлся с цитатой
Ш2 абзацем задачи велит заполнить «заголовок и текст визитки», а ⚠ двумя строками ниже приводит
прямую речь владельца: «визитка загружена в генератор, там нету пропусков… ничего для неё не
заполняй и не выдумывай». Два указания взаимоисключающие, и оба обязательные.
ЦЕНА: исполнитель, читающий сверху вниз и остановившийся на первом абзаце, сочиняет ВЛАДЕЛЬЦУ
БИОГРАФИЮ и ставит её на слайд, который пойдёт в эфир. Класс шире одной визитки: когда заход
пересказывает своими словами то, что сам же приводит цитатой, пересказ и цитата расходятся, а
доверять надо цитате — но об этом в заходе нигде не сказано, и порядок чтения решает всё.

### Пункт гигиены, объявленный «неприменимым», отключает проверку ровно там, где заход её требует
Г2 контракта зоны написан как «**неприменимо, и это проверено при сборке**: все пути зоны лежат
внутри репозитория `materials`». В той же зоне стоит `disciplina/skills/slajdy/**`, а папки
`materials/disciplina` не существует вовсе (`ls -d disciplina` → No such file) — это второй
репозиторий, и §0.1 двумя экранами выше пишет капслоком «Два репозитория — два коммита, второй
забывают».
ЦЕНА: заход одновременно кричит «не забудь второй репозиторий» и печатает «второго репозитория
нет, проверять нечего». Строку «неприменимо» генерирует `bootstrap_zahod.py` по критерию С2, и
критерий не смотрит на зону — он смотрит на что-то другое. Пока это так, автоматическая часть
контракта опровергает ручную, а исполнитель выбирает между ними наугад.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Состояние на входе (снято командами, не по памяти):**
- рабочая папка `../materials-wt/final-l2`, ветка `zahod/final-l2`, дерево чистое; отпочкована от `5f92c98` (`arka/mat-kostyak`);
- `git branch --no-merged arka/mat-kostyak` → **2**: `zahod/tri-provodki` (`afffeaa`), `zahod/finalnaya-dovodka` (`303b53b`);
- в ОСНОВНОЙ папке незакоммичено, в моей зоне: `_studio/docs/KARTA.md` (M), `PRAVKI-SBORKI-L2.md` (M), `kod_finalnaya-dovodka-l2.md` (??), `kod_tri-provodki.md` (??). Вне зоны и не трогаю: `_fond/**`, `obzory/**`, `ucheniki/**`, `_generator/build_listok.py`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, артефакты прогонов `_generator/tools/fixtures/sborka/obratnyj-progon/*/dist|math/`;
- второй репозиторий `~/Documents/GitHub/disciplina`, ветка `zahod/format-kartochki-faza-1`: `skills/slajdy/SKILL.md` (M) — правка Cowork, забрать.

**Три оспоренные предпосылки — до работы, как велит §1:**

1. 🔴 **Ш2 сам себе противоречит.** Абзац задачи велит заполнить «заголовок и текст визитки», а ⚠ двумя строками ниже — прямая речь владельца: «визитка загружена в генератор, там нету пропусков… ничего для неё не заполняй». **Беру ⚠** (это слова владельца, а первый абзац — пересказ). Ш2 = три поля обложки/финального, визитка — ноль полей. ⚠ При этом Я2 (отчёт `tri-provodki`) называет пустую визитку **своим вопросом 3**: «порождение по умолчанию сработает у всех, а поля визитки не заполняет никто», и на пробе визитка вышла с заголовком «Про меня». То есть «пропусков нет» — предпосылка, которая может оказаться ложной. **Проверяю глазом на порождённом кадре** (визитка и так в тройке кадров, которые велено смотреть) и, если она пустая, говорю громко, а не молча заполняю за владельца.
2. ⚠ **Г2 контракта («неприменимо, все пути зоны внутри `materials`») ложен по своему же критерию:** в зоне стоит `disciplina/skills/slajdy/**`, а папки `materials/disciplina` не существует (`ls -d disciplina` → No such file) — это ВТОРОЙ репозиторий, и §0.1 сам велит там коммитить. Г2 применим; гоню его командой. Урок фабрике.
3. ⚠ **Критерий 6 «`git status --porcelain` в обоих репозиториях → пусто» неисполним для ОСНОВНОЙ папки `materials` по устройству worktree.** Правки аналитика лежат в рабочем дереве основной папки, я коммичу их на СВОЮ ветку из своей папки — до влития (а влитие за приёмкой) основная папка так и будет показывать их как `M`. Читаю критерий как «пусто в МОЕЙ рабочей папке + пусто в `disciplina`», а по основной папке отдельно перечисляю остаток и его причину. Поправка предложена, работу не блокирует.

**Шаги:**
- **Ш1.** Два слияния строго в порядке `tri-provodki` → `finalnaya-dovodka`, зоны — строкой из §0.1. Конфликт: код → `tri-provodki`, содержание карточек → `finalnaya-dovodka`, каждое разрешение командой-доказательством. Факт: `merge-base --is-ancestor` по обеим → rc=0.
- **Ш2.** По слитому коду `deck.py` сверяю ИМЕНА полей (Я2 называет девять; верю коду, не отчёту), заполняю в `brief.md`: `oblozhka_zagolovok: Функторы`, `oblozhka_illustracii: pustoj-kvadrat`, `finalnyj_zagolovok`. Картинку возвращаю из `illustracii/_snyato/pustoj-kvadrat`, не рисую. Папок служебных слайдов не завожу.
- **Ш3.** Пять мелочей из девяти находок Я1 (слайд 5 — остриё и пунктир; слайд 4 — метка `2v`; слайды 7 и 9 — центровка горизонтальных стрелок; слайд 9 — юникодные звёздочки). Три не-мелочи только называю.
- **Ш4.** Контраст `--steel` на `--board`: считаю нынешнее отношение, подбираю ≥ 4,5 : 1, проверяю остальные носители токена; ломает соседей или дорого — не трогаю и говорю почему. Предпосылку в `DISCIPLINA.md` правлю с датой в любом исходе (она ложна независимо от того, поменяю ли я цвет).
- **Ш5.** Сборка БЕЗ `--bez-podbora` и **БЕЗ `--zanovo`** (Я2, вопрос 1: `--zanovo` переподбирает 10 из 18 карточек и затирает ручные `liniya` владельца — на финальном заходе это порча, а не доводка) в основную папку зоны. Глазами — только три новых кадра. Гейты. Коммит в двух репозиториях.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. 🔴 **ВИЗИТКА ПУСТА, и это надо решить ДО среды.** Предпосылка «визитка загружена в генератор,
   там нету пропусков» ложна: в генератор загружена РАСКЛАДКА (доска слева, заголовок, слот фото,
   слот QR, колонка текста справа), а содержания нет нигде. Замер по собранной деке:
   `#vizitka .copy` → текст `""`, панелей с картинкой `0`, заголовок — умолчание кода «Про меня».
   Я ничего не заполнял намеренно: биография, фото и QR — ваши данные, выдумывать их нельзя.
   Два выхода, оба в одну строку `brief.md`: заполнить `vizitka_zagolovok` · `vizitka_tekst` ·
   `vizitka_illustracii: [фото, qr]` — или снять слайд флагом `bez_vizitki: da`.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. **Гейт `gejt_illyustracij.py` слеп к иллюстрациям служебных слайдов** — список «на что
   ссылаются» он собирает только из `slajdy/*/slaid.md`, а обложка/визитка/финальный карточек на
   диске не имеют. Лечится тремя строками: добавить в тот же список значения полей
   `oblozhka_illustracii` · `vizitka_illustracii` · `finalnyj_illustracii` из `brief.md`.
   Класс НЕМЕДЛЕННОЕ: повторится на каждой лекции с картинкой на обложке.
   ДОМ: _generator/tools/gejt_illyustracij.py
   ДОСТАВЛЕНО: нет
3. **Диаграмма на обложке мельче, чем просил владелец («это будет красиво»).** Причина названа
   замером, а не на глаз: зона `.art` обложки жёстко `height:250px` (минус padding 28×2 = 194),
   и квадратная диаграмма упирается в высоту — рисуется 225×193 при ширине зоны 1380.
   `tipy.oblozhka` вне зоны захода, не трогал. Решение о размере — ваше.
   ДОМ: _generator/sborka/tipy.py
   ДОСТАВЛЕНО: нет
4. **Контраст подписей вылечен в ОДНОМ носителе из двух.** Дек берёт CSS из
   `_generator/skeleton/` — там сделано (1,64 → 4,54 : 1). Doc-вид `teorkat-vvedenie` вяжет свои
   токены в `src/tokens.css` + `src/tools/sobrat_overlay.py`, это вне зоны, и там осталось 1,64 : 1.
   ДОМ: teorkat-vvedenie/src/tools/sobrat_overlay.py
   ДОСТАВЛЕНО: нет
5. **Мёртвый `font-size` в атрибуте `<text>` теперь лежит В ЗОНЕ, но я его НЕ чинил осознанно.**
   `_generator/skeleton/base.css` — моя зона, и правка «`.s-txt{font-size:inherit}`» и правда в
   одну строку. Но она разом ОЖИВИТ атрибуты 14–17 во ВСЕХ иллюстрациях ВСЕХ деков, то есть
   изменит каждую подпись на каждой картинке за день до эфира. Это не мелочь и не моё решение.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
6. **`namotka-petli/risunok.svg` несёт `width="200"`** — тот же класс, что я снял у
   `pustoj-kvadrat`: атрибут doc-вида на дек-иллюстрации (словарь, §Обязательное п.1). Не трогал:
   это верхняя картинка слайда 15, названная владельцем «отличной» и покадрово проверенная прошлым
   заходом; трогать её ради консистентности накануне эфира — плохой размен.
   ДОМ: teorkat-vvedenie/L2/illustracii/namotka-petli/risunok.svg
   ДОСТАВЛЕНО: нет
7. **Три не-мелочи из девяти находок Я1 — как и велено, только названы.** Слайд 9: `A` на
   диаграмме против `f` в тексте (`A` стоит по вашему указанию в карточке — выбор ваш). Слайд 5:
   правая половина иллюстрации дословно повторяет строку текста (что убрать — ваше). Слайд 18:
   три диаграммы в масштабах 158 / 561 / 336 px (правка не мелкая).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
8. **Заморозка легаси-типографики осталась нерешённой (вопрос 1 захода `tri-provodki`).** Я собрал
   БЕЗ `--zanovo` намеренно: `--zanovo` переподбирает 10 карточек из 18 и затирает ваши ручные
   `liniya` (`dvazhdy-dvojstvennoe` 70 → 79, `inyekcii` 58 → 82 и далее). На финальном заходе это
   порча, а не доводка. Но состояние наследуется: 15 карточек так и стоят «заморожены».
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/final-l2/teorkat-vvedenie/L2/dist/index.html` — открывать браузером (двойной клик), листается ← →, 21 слайд.
🔴 **Путь — в РАБОЧЕЙ папке захода, и это не описка.** Ветка `zahod/final-l2` не влита (влитие за приёмкой), поэтому в ОСНОВНОЙ папке `~/Documents/GitHub/materials/teorkat-vvedenie/L2/dist/index.html` до сих пор лежит СТАРАЯ дека без обложки. Откроете её — увидите работу до правок и решите, что ничего не сработало.

### ВЕРДИКТ С ОХВАТОМ ВНУТРИ

**Служебных слайдов 3 из 3 · мелочей 5 из 5 · не-мелочей названо 3 из 3 · гейтов зелёных 4 из 5 · веток влито 2 из 2 · репозиториев закоммичено 2 из 2.** Красный — пятый гейт, и красный он не по содержанию деки: разбор в клаузе 3 ниже.

### Ш1. ВЛИТИ ОБЕ ВЕТКИ — 2 из 2, конфликтов НОЛЬ

Порядок соблюдён: `tri-provodki` (5 путей, код генератора) → `finalnaya-dovodka` (16 путей, лекция и словарь). Заход предупреждал, что оба трогают `teorkat-vvedenie/L2/**` и конфликт вероятен, — **конфликта не было ни одного**, пересечения путей нет: первый правит только `_generator/**`, второй только `teorkat-vvedenie/L2/**` + `_illustracii/**`. Правило «код → tri-provodki, содержание → finalnaya-dovodka» поэтому не применялось ни разу; выдумывать разрешение мне было нечего.

```
git merge-base --is-ancestor zahod/tri-provodki      HEAD → rc=0
git merge-base --is-ancestor zahod/finalnaya-dovodka HEAD → rc=0
```
⚠ **Клауза 1 критерия при этом ПРОВАЛЕНА, и провалена она по построению, а не по работе.**
`git branch --no-merged arka/mat-kostyak` → три ветки (`final-l2`, `finalnaya-dovodka`, `tri-provodki`), а не одна. Обе слитые ветки сойдут с этого списка ровно тогда, когда МОЯ ветка будет влита в `arka/mat-kostyak`, — а это тот же контракт запрещает («Ветку в конце НЕ вливай»). Разбор — первым пунктом `## УРОКИ ФАБРИКЕ`. Ветки не удалял, как и велено.

### Ш2. СЛУЖЕБНЫЕ СЛАЙДЫ — 3 из 3, руками не заведено ни одной папки

Имена полей взял из ЖИВОГО кода (`deck.py:SLUZHEBNYE`), а не из отчёта Я2 — совпали. Вписано в `brief.md` три строки:
```
oblozhka_zagolovok: Функторы          ← ваши слова, дословно
oblozhka_illustracii: pustoj-kvadrat
finalnyj_zagolovok: Спасибо за внимание
```
Третья строка равна умолчанию кода и вписана нарочно: в `brief.md` её видно и можно поменять, в `tipy.finalnyj` — нет.

Картинка **возвращена из `illustracii/_snyato/`, а не нарисована заново** — git видит перемещение (`rename … (70%)`, содержание правил только я и только в одном месте, см. ниже).

Проверка фактом: `grep -c 'id="oblozhka"'` → 1, `id="vizitka"` → 1, `id="finalnyj"` → 1; папок служебных слайдов в `slajdy/` → **0**; слайдов в деке 21 (было 18).

🔴 **ВИЗИТКА ВЫШЛА ПУСТОЙ — говорю громко, как велит заход.** Ваша предпосылка «визитка загружена в генератор, там нету пропусков, просто надо породить стандартным способом» **проверена и оказалась ложной ровно наполовину**. Загружена РАСКЛАДКА: доска слева, заголовок, слот фото 251×260, слот QR 250×250, колонка текста справа. Содержания нет нигде — ни в генераторе, ни в лекции, ни в одной другой деке репозитория (единственная визитка на диске — фикстура `demo-karta/sl-vizitka` с заглушкой «Имя Фамилия / Строка биографии»). Замер по собранной деке, не на глаз: `#vizitka .copy` текст → `""`, панелей с картинкой → `0`, заголовок → умолчание кода «Про меня». На экране это доска с надписью «ПРО МЕНЯ» и два пустых поля.
**Ничего не выдумывал сознательно** — это ваши имя, биография, фото и QR. Что делать, двумя способами в одну строку — вопрос 1.

### Ш3. МЕЛОЧИ — 5 из 5, ни одной сверх списка

| # | что было названо | что сделано | проверка |
|---|---|---|---|
| 1 | слайд 5, остриё вектора утоплено в узел | вершина отведена от центра узла на 7 ед. при `r=4.6` — зазор 2,4; наконечник пересчитан по СК1 целиком (основание сдвинуто вместе с ним, линия укорочена до нового основания) | `proverka_pomarok.py` → rc=0 на всех 11 |
| 2 | слайд 5, пунктир-проекция не доходит до оси 2 ед. | `y2: 130 → 132`, ровно на ось | греп |
| 3 | слайд 4, метка `2v` вплотную к параллелограмму | `(170,176) → (172,198)` — метка ушла из полосы верхней сцены под свою же стрелку | греп |
| 4 | слайды 7 и 9, горизонтальные стрелки не отцентрованы | пересчитаны по условию симметрии `x1 + x2 = 241` (объекты в 46 и 204, наконечник +9): `zamena-bazisa` обе стрелки 26/19 → **26/26**, `kvadrat-eta` верх 20/13 → **20/20**, низ 32/23 → **32/32** | замер до и после командой |
| 5 | слайд 9, звёздочки ASCII вместо юникодных надстрочников | `V**`/`W**`/`A**` → `V∗∗`/`W∗∗`/`A∗∗` юникодным `∗` (U+2217) в поднятом `tspan` | замер `getBBox` в браузере: надстрочник занимает ВЕРХНЮЮ часть высоты буквы (`tspan` 188–199 против текста 188–206), то есть поднят, а не сидит на базовой линии |

⚠ **Замеры зазоров у меня разошлись с Я1.** Я1 назвал «35/20 и 25/13» — по живым файлам вышло 20/13 (`kvadrat-eta` верх, совпало), 32/23 (`kvadrat-eta` низ) и 26/19 (обе стрелки `zamena-bazisa`, которых Я1 не считал отдельно). Диагноз Я1 при этом подтверждён дословно: правый зазор ВЕЗДЕ меньше левого на 7–9 единиц, то есть ровно на наконечник.
⚠ **Кегль надстрочника считал от РЕАЛЬНОГО, а не от написанного в атрибуте.** `.s-txt{font-size:13px}` в `base.css` бьёт атрибут `font-size="17"` (ловушка, записанная Я1), поэтому надстрочник 8,5 при родителе 13 — а не 11 при 17, как вышло бы, поверь я атрибуту.

**Три не-мелочи названы и не тронуты:** слайд 9 `A` против `f`; слайд 5, дублирование картинкой строки текста; слайд 18, три масштаба. Все три — вопрос 7.

### Ш4. КОНТРАСТ — сделан, 1,64 → 4,54 : 1

Замер Я1 подтверждён точно: `--steel #8195ad` по `--board #a7c2cb` = **1,64 : 1**. Проверил и соседа: `--steel-d #566b86` = 2,92 : 1, тоже ниже порога.

**Что сделал:** завёл ТРЕТИЙ токен `--steel-t: #3f4e61` в `_generator/skeleton/tokens.css` и перекрасил им `.s-txt-m` в `_generator/skeleton/base.css`. Тон и насыщенность `--steel` сохранены (HLS `h=.591 s=.212`), погашена только светлота — до первого значения, берущего порог: **4,54 : 1** на доске, 6,73 на `--paper`, 8,49 на `--card`.

**Почему третий токен, а не правка двух прежних (это и есть «не сломать соседей»):** `--steel` носит ещё и буллиты списков `.bul`/`.lst` и `.blur-reveal`, а они стоят на `--paper` и в лечении не нуждаются; `--steel-d` носит служебную ЛИНИЮ и НАКОНЕЧНИК — темнить всю графику всех деков накануне эфира никто не заказывал. Тронут ровно один класс — тот, что назван дефектом.

**Ложная предпосылка в словаре поправлена с датой** (`_illustracii/DISCIPLINA.md`, новый раздел «ТРЕТЬЯ ПРАВКА ПАЛИТРЫ», дата данных 2026-08-09): опровергнутая цитата приведена дословно, рядом таблица замеров, названо, почему `.s-txt-m` стоит именно на доске, и честно записано, что правка доехала в один носитель из двух.

Проверка по СОБРАННОМУ файлу, а не по исходнику: `grep -o '--steel-t: #[0-9a-f]*'` → `--steel-t: #3f4e61`, `grep -c 'fill:var(--steel-t)'` → 1.

### Ш5. СБОРКА, ГЕЙТЫ, ГЛАЗА

**Сборка** — `deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html`, БЕЗ `--bez-podbora`, rc=0, 21 слайд, `MISSING-MATH` → **0**.
🔴 **И БЕЗ `--zanovo` — это моё решение, назову его явно.** Солвер отчитался «0 подобрано, 18 пропущено, 15 заморожены как легаси». Это вопрос 1 захода `tri-provodki`, и на финальном заходе он решается только так: `--zanovo` переподбирает 10 карточек из 18 и затирает ваши ручные `liniya` (`dvazhdy-dvojstvennoe` 70 → 79,17 — ту самую полосу, которую вы расширяли, чтобы квадрат не висел в пустоте; `inyekcii` 58 → 82; на трёх слайдах кегль 44 → 41). Накануне эфира это порча, а не доводка. Состояние наследуется — вопрос 8.

**Гейты — 4 из 5 зелёных, все rc:**
```
gejt_kartochki.py teorkat-vvedenie/L2   → rc=0   18 из 18 карточек, замечаний 0
gejt_verstki.py teorkat-vvedenie/L2     → rc=0   ✓ ЗЕЛЁНЫЙ
gejt_illyustracij.py teorkat-vvedenie/L2→ rc=1   ✗ КРАСНЫЙ, замечаний 1
proverka_pomarok.py <11 иллюстраций>    → rc=0   все 11 зелёные
check_karty.py                          → rc=0   52 из 52 строк, 52 из 52 инструментов
gejt_vmeshcheniya.py <18 скомпилированных> → rc=0  обрезанных 0 (15 с текстом влезли, 3 без текстовой зоны)
```
🔴 **Разбор единственного красного, потому что «красный гейт» и «сломанная дека» — разные вещи.**
Замечание одно: `Z3 заказ 'pustoj-kvadrat' — сирота, ни один слайд не ссылается`. Слайд и правда не ссылается: на обложку картинку зовёт `brief.md`, а карточки обложки на диске нет — её порождает `deck.py`. Гейт собирает список «на что ссылаются» ТОЛЬКО из `slajdy/*/slaid.md` и про поля `*_illustracii` не знает. Проверено до и после возврата картинки: до — «заказов в пуле 10, ✓ ЗЕЛЁНЫЙ», после — «заказов 11, ✗ КРАСНЫЙ», ссылок в обоих случаях 10.
**Не чинил: `_generator/tools/` вне зоны захода.** Лечение — три строки, вопрос 2. Выбор был между «дека без картинки на обложке, которую вы просили» и «красный гейт по причине, не касающейся содержания»; выбрал второе и называю громко.

**Глазами — ровно три новых кадра, как велено** (восемнадцать содержательных не переоткрывал):
- **Обложка** ✅ «ФУНКТОРЫ» крупно + коммутативный квадрат внизу по центру. ⚠ Диаграмма меньше, чем просили: зона `.art` жёстко `height:250px`, квадрат упирается в высоту и рисуется 225×193 при ширине зоны 1380. `tipy.oblozhka` вне зоны — вопрос 3.
- **Визитка** ❌ пуста, разбор выше.
- **Финальный** ✅ «СПАСИБО ЗА ВНИМАНИЕ» по центру доски, чисто.

**Остальное мерено, а не смотрено** (глаз — там, где нет замера): прогон по ВСЕМ 21 слайдам в браузере на переполнение зон по ширине И высоте → **вылетов 0**; наконечники — ловушкой `proverka_pomarok.py` → 11 из 11; вмещение — гейтом.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА

- **Ничего не потеряно при слиянии.** Оба слитых коммита в истории (`349f5a8`, `303b53b`); merge-коммит `7978390`. Правки `finalnaya-dovodka` выборочно, командой — **4 из 8** (просили 3): слайд 2 «топологические пространства» → 3 вхождения; слайд 3 «Канонические конструкции» → 4 вхождения; слайд 9 `liniya: 70` (было 54); слайд 4 `liniya: 74` (было 66).
- **Эталоны целы.** `git diff --stat 303b53b -- vlozhenie-i-vozvrat/ _snyato/retrakt-i-obraz/` → **пусто**.
- 🔴 **Собственная ошибка, пойманная и исправленная по ходу — называю, потому что молчание о ней было бы враньём.** Правку словаря я сперва применил в ОСНОВНОЙ папке вместо рабочей. Поймал грепом статуса, восстановил файл побайтово обратной правкой (не `git checkout` — он в основной папке запрещён) и проверил: `git status --porcelain -- _illustracii/` → пусто. Затем применил правку там, где положено.
- 🔴 **Вторая, пойманная тем же способом: рабочий каталог оболочки сместился в основную папку**, и проверка «дошёл ли контраст до деки» отработала по СТАРОЙ деке и СТАРОМУ `base.css` — дала ложный ноль. Поймал тем, что усомнился в собственном зелёном и повторил с `pwd`. Настоящий результат — выше, снят из рабочей папки. Урок для приёмки: `pwd` в первой строке проверочного блока стоит дешевле, чем разбор ложной тревоги.

### ЧТО НЕ ТРОГАЛ

`_fond/**`, `obzory/**`, `ucheniki/**`, `_generator/build_listok.py`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — чужое. `_generator/tools/**` и `_generator/sborka/**` — вне зоны (там лежат оба лечения из вопросов 2 и 3). `teorkat-vvedenie/src/**` — вне зоны (лечение из вопроса 4). Карточки слайдов — ни одной: солвер их не переписывал, сборка шла без `--zanovo`. `namotka-petli` — вопрос 6. Ветку не вливал, ветки не удалял, рабочие папки не гасил.

### НЕОБРАТИМОЕ

1. **Перемещение** `teorkat-vvedenie/L2/illustracii/_snyato/pustoj-kvadrat/` → `illustracii/pustoj-kvadrat/` (обе — `risunok.svg` и `zakaz.md`). Прямое задание Ш2. Восстанавливается из коммита `dca87a7` (git видит перемещение как rename, содержание `zakaz.md` не изменилось вовсе).
2. **Снят атрибут** `width="250"` у `pustoj-kvadrat/risunok.svg`, заменён на `preserveAspectRatio="xMidYMid meet"` — идиома дека по словарю (§Обязательное п.1), тогда как `width` есть идиома doc-вида; из одиннадцати иллюстраций Л2 такой атрибут несут только две. Визуально на кадре не изменилось ничего (размер задаёт панель, а не SVG) — проверено скриншотом до и после. Восстанавливается из `dca87a7`.
3. **Правка в основной папке и её откат** — описано выше в блоке верификатора; итог: файл побайтово равен HEAD.
Больше необратимого нет: ничего не удалял, не переименовывал, `reset`/`checkout` поверх несохранённого не делал.

### ПОВТОРЯЕМОСТЬ НАХОДОК

- 🔴 **ПОВТОРИТСЯ, класс НЕМЕДЛЕННОЕ: гейт иллюстраций краснеет на любой обложке с картинкой** (вопрос 2). Не «может повторится» — повторится по построению на каждой лекции, где обложка несёт иллюстрацию, то есть по вашему замыслу на каждой. Чинится тремя строками ДО следующего прогона.
- 🔴 **ПОВТОРИТСЯ, класс НЕМЕДЛЕННОЕ: пустая визитка** (вопрос 1). Порождение по умолчанию сработает у КАЖДОЙ будущей лекции, а поля визитки не заполняет никто и заполнить их может только владелец. Уже предсказано заходом `tri-provodki` (его вопрос 3) и за один заход не рассосалось.
- ⚠ **ПОВТОРИТСЯ: заморозка легаси-типографики** (вопрос 8). Наследуемое состояние, а не событие: любая лекция, собранная старым `deck.py` хоть раз, сегодня не переподбирается.
- ⚠ **ПОВТОРИТСЯ: контраст `.s-txt-m` в doc-виде** (вопрос 4) — на каждой картинке каждого документа, пока второй носитель не вылечен тем же способом.
- **Не повторится:** сами пять мелочей — они про конкретные четыре SVG Л2. Расхождение моих замеров зазора с Я1 — про то, как считали, а не про механизм.
- **Проба, после которой починили:** возврат картинки на обложку СРАЗУ показал слепоту гейта — то есть пробный прогон себя окупил, и находка чинится до следующего прогона, а не уходит в очередь.

**ВРЕМЯ ПРОГОНА И ТОКЕНЫ:** неприменимо на канале `app` — лог кладёт `tee` на канале `terminal`, сессия в приложении такого следа не оставляет.

### ГИГИЕНА (§4.1) — командами

- **Г1. Зона доехала.** Инструмент две зоны одной командой не берёт — гонял по одной, без глоба (форма с глобом даёт ложно-зелёный, урок захода `finalnaya-dovodka`): `--zone teorkat-vvedenie/L2` → ✅ · `--zone _illustracii` → ✅ · `--zone _generator/skeleton` → ✅ · `--zone _studio/zhurnal/2026-08-07_arhitektura-slajdov` → ✅ · `--zone _studio/docs` → ✅.
- **Г2. ПРИМЕНИМ, вопреки тексту раздела** (разбор — четвёртым уроком фабрике): зона содержит `disciplina/skills/slajdy/**`, а `materials/disciplina` не существует. `cd ~/Documents/GitHub/disciplina && git status --porcelain` → **пусто** после коммита.
- **Г3. Невлитых веток стало 3, было 2** — прибавилась ровно моя `zahod/final-l2`. Законно: ветка захода, влитие за приёмкой, гашение веток решением владельца запрещено.
- **Г4. Новых `.py` ноль** (`git show --stat HEAD | grep -c '^A.*\.py$'` → 0) — контракт инструмента неприменим.
- **Г5. Новых `.md` мною не заведено ни одного.** Два `.md`, добавленные коммитом, — это чужие файлы-заходы (`kod_finalnaya-dovodka-l2.md`, `kod_tri-provodki.md`), рождённые `bootstrap_zahod.py` уже зарегистрированными; проверка на диске: `grep -c` каждого в `_studio/docs/KARTA.md` → **1** и **1**. `register_doc.py` не звался за ненадобностью; `KARTA.md` в коммите — это правка АНАЛИТИКА, забранная мной, а не моя строка.
- **Г6. Чужих путей в коммите ноль** — `git show --stat` даёт 15 файлов, все в пяти путях зоны.

### КРИТЕРИЙ 6 — ХВОСТОВ НЕ ОСТАЛОСЬ, с одной названной оговоркой

- рабочая папка захода `materials-wt/final-l2`: `git status --porcelain` → **пусто**;
- второй репозиторий `disciplina`: `git status --porcelain` → **пусто**;
- ⚠ **ОСНОВНАЯ папка `materials` пустой быть не может, и это свойство worktree, а не хвост.** Правки аналитика лежат в её рабочем дереве, я закоммитил их на СВОЮ ветку из своей папки — пока приёмка не влила её в `arka/mat-kostyak`, основная папка так и будет показывать те же файлы как `M`. Остаток поимённо: `_studio/docs/KARTA.md`, `PRAVKI-SBORKI-L2.md`, `kod_finalnaya-dovodka-l2.md`, `kod_tri-provodki.md` — **всё это забрано коммитом `dca87a7` и погаснет само при влитии**; `kod_final-l2.md` — этот файл, его по контракту коммитит приёмка; `INCIDENTY.md` и `_generator/build_listok.py` — чужое, вне зоны; 27 записей `_generator/tools/fixtures/sborka/obratnyj-progon/*/dist|math/` — артефакты чужих прогонов, вне зоны. Плюс `_fond/` (12), `ucheniki/` (9), `obzory/` (2) — чужое, как и предупреждал заход.

**КОММИТ (materials):** `dca87a7` — «final-l2: обложка/визитка/финальный порождены генератором из brief.md (картинка pustoj-kvadrat возвращена из _snyato на обложку); пять мелочей по слайдам 4, 5, 7, 9; контраст подписей на доске 1,64 → 4,54 : 1 третьим токеном --steel-t, ложная предпосылка в словаре опровергнута замером; забраны правки аналитика (KARTA, PRAVKI-SBORKI-L2) и два незакоммиченных файла-захода» · 15 файлов, +1674 −24 · **хук `pre-commit` прошёл ЗЕЛЁНЫМ, обхода `--no-verify` не было** · `git_zona.py check` по всем пяти зонам → ✅ · `git show --stat` — только пути зоны.
**КОММИТ (disciplina, второй репозиторий — тот, который забывают):** `c0875d9` — «slajdy: раздел „Две части конвейера“ — контракт шва между частями и список нерешённого по нему (правка Cowork, забрана заходом final-l2)» · 1 файл, +91 · ветку `zahod/format-kartochki-faza-1` НЕ переключал: рядом может идти чужая работа · `git status --porcelain` → пусто.
