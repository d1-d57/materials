# Канал исполнителя — sborka-l2-fazy-4-7 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — механика расписана до команд, входные данные готовы, изобретать нечего.

КОНТЕКСТ. Фабрика научно-популярных лекций. Лекция 2 по теории категорий (`teorkat-vvedenie/L2`)
прошла фазы 1–3: интервью, раскадровку и **тексты слайдов**. Владелец текст перечитал глазами и
принял: *«она сделана, на мой взгляд, хорошо и всё готово»*. Иллюстрации нарисованы.
**Ты делаешь фазы 4–7: вёрстка → иллюстрации → сборка и QA → сцены.**

🔴 **ГЛАВНОЕ ТРЕБОВАНИЕ ВЛАДЕЛЬЦА — СОБРАТЬ С ПЕРВОГО РАЗА.** Дословно: *«ничего переделывать не
нужно, нужно сразу вставить правильный текст, собрать, и в идеале с первого раза чтобы всё
получилось»*. Причина не в перфекционизме: круг переделки стоит десятки тысяч токенов, и на прошлой
пробе одного слайда ушло 100–200 тысяч. **Поэтому все известные грабли перечислены ниже поимённо —
раздел «ЧТО СЛОМАЕТСЯ, ЕСЛИ НЕ ЗНАТЬ». Прочитай его ДО первой команды, а не когда покраснеет.**

🔴🔴 **ВТОРОЕ ТРЕБОВАНИЕ — ИНТЕРНЕТ МОЖЕТ ОТВАЛИТЬСЯ В ЛЮБОЙ МОМЕНТ.** У владельца плохая связь.
Заход поэтому разрезан на **ЭТАП A (целиком офлайн)** и **ЭТАП B (требует сети)**, и порядок
жёсткий: **сначала A целиком и коммит, потом B.** Если связь умрёт — на диске уже лежит собранная
дека, а не полуфабрикат. Подробности — §2.

**Состояние входа проверено командами ПЕРЕД сборкой захода, не предположено:**

| что | команда | результат |
|---|---|---|
| карточки | `python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2` | **ЗЕЛЁНЫЙ, 15 из 15, замечаний 0** |
| заказ иллюстраций | `python3 _generator/tools/gejt_illyustracij.py teorkat-vvedenie/L2` | **ЗЕЛЁНЫЙ**, 11 ссылок = 11 заказов, все с `risunok.svg` |
| порядок понятий | `python3 _generator/tools/graf_zavisimostej.py teorkat-vvedenie/L2` | **циклов нет, нарушений порядка нет** (15 слайдов, 12 связей) |
| 🔴 помарки картинок | `python3 _illustracii/proverka_pomarok.py teorkat-vvedenie/L2/illustracii/*/risunok.svg` | 🔴 **rc=1: три иллюстрации КРАСНЫЕ** |
| 🔴 сборка деки | `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o /tmp/x.html` | 🔴 **rc=1: playwright не установлен** |

ЦЕЛЬ: дека `teorkat-vvedenie/L2/dist/index.html`, собранная с подбором типографики, без красных
гейтов, с чистыми иллюстрациями.

Приёмка — по ОТЧЁТУ, без построчной сверки.
**Стоп до цели:** сеть не вернулась — ты сдаёшь деку ЭТАПА A (без подбора кегля) и честно пишешь,
что подбор не прогнан. Это законный исход. **Не законный исход — молча собрать без подбора и
назвать это готовым.**

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/sborka-l2`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/sborka-l2`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add sborka-l2 --branch zahod/sborka-l2
  cd ../materials-wt/sborka-l2
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `teorkat-vvedenie/L2/**` `_generator/skeleton/**`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- ⚠ **`teorkat-vvedenie/L2/**` обычно READ-ONLY для заходов — здесь запрет СНЯТ владельцем осознанно**, ты и есть тот, кто эту лекцию собирает. Но `teorkat-vvedenie/` ВЫШЕ уровня `L2/` (`LENTA-L2.md`, `RAZMETKA-L2.md`, `KONTRAKT-L2.md`, `src/`, `raskadrovka/`) — **читаешь, не пишешь.**
- 🔴🔴 **`_generator/sborka/**` — ЧУЖАЯ ЗОНА, рядом идёт заход `uroki-faz-1-2`, не трогать ни байта.** Ты этими скриптами ПОЛЬЗУЕШЬСЯ, но их не правишь. Нашёл в них дефект → в отчёт строкой. 🔴 Особый случай: `gejt_kartochki.py` там может измениться под тобой прямо во время работы (соседний заход ужесточает `--faza 1`). Покраснел гейт на карточке, которая была зелёной час назад, — **это не твоя поломка**: зафиксируй вывод, назови в отчёте и иди дальше, чинить его не твоё дело.
- ⚠ `_generator/skeleton/**` в зоне НЕ для творчества: правь там, только если сборка физически ломается, и каждой правке — строка в отчёте с причиной.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/sborka-l2`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `teorkat-vvedenie/L2/**` `_generator/skeleton/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `teorkat-vvedenie/L2/slajdy/*/slaid.md` — **все 15** | **источник истины.** Текст фазы 3 уже внутри, в секции «Текст слайда — сжато». Ты его НЕ переписываешь |
| Я2 | `teorkat-vvedenie/L2/brief.md` | `slide_order` — порядок деки |
| Я3 | 🔴 `_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-2026-08-09.md` **целиком** | 13 правил владельца с разбора первого собранного слайда. **П9–П13 — прямо про твою работу** |
| Я4 | `_illustracii/DISCIPLINA.md` | читать ПЕРЕД правкой любого SVG (правило 3 корневого `CLAUDE.md`) |
| Я5 | `teorkat-vvedenie/L2/illustracii/*/zakaz.md` — **только у трёх красных** | что рисунок обязан показывать |
| Я6 | `_generator/sborka/deck.py`, `slaid.py`, `vmeshchenie.py` — **докстринги и `--help`, не весь код** | как звать; читаешь, не правишь |

⚠ **`LENTA-L2/faza3.md` — НЕ якорь и НЕ источник.** Это витрина того же текста для чтения глазами.
Источник — карточки (Я1). Расхождение между ними → в отчёт, карточка права.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### 🔴 ЧТО СЛОМАЕТСЯ, ЕСЛИ НЕ ЗНАТЬ — читать ДО первой команды

Каждый пункт уже стоил круга переделки. Это не «советы», это список оплаченных ошибок.

**Г1. 🔴 Три иллюстрации из одиннадцати КРАСНЫЕ прямо сейчас.** Проверено:
```
python3 _illustracii/proverka_pomarok.py teorkat-vvedenie/L2/illustracii/*/risunok.svg   # rc=1
```
```
КРАСНЫЙ centr-i-ego-obraz : ✗ П9 «?» в тексте ('= id ?') · ✗ П10/П11 равенство композиции
                              подписью ('sgn ∘ s = id') — рисуется ДУГОЙ
КРАСНЫЙ disk-i-okruzhnost : ✗ П9 «?» в тексте ('= id ?')
КРАСНЫЙ retrakt-i-obraz   : ✗ П10/П11 равенство композиции подписью ('r ∘ s = 1')
```
Соберёшь деку не починив — соберёшь её с дефектами, которые владелец уже один раз называл.
**Чинишь ДО сборки.** Правила — Я3 §3, дисциплина рисования — Я4, смысл каждого рисунка — Я5.

**Г2. 🔴 `deck.py` НЕ СОБЕРЁТСЯ БЕЗ `playwright` — и это единственное место, где нужна сеть.**
Проверено, дословный отказ:
```
playwright не установлен — подбор типографики недоступен и деку БЕЗ подбора я не соберу молча:
либо `pip install playwright && playwright install chrome`, либо явный флаг --bez-podbora
```
Отказ правильный, не чини его. Флаг `--bez-podbora` существует — он и есть твой офлайн-путь.

**Г3. TeX в формулах: писать `\lt` и `\gt`, НИКОГДА голые `<` и `>`.** Голый `<` съедается при
отрисовке — текст исчезает молча, HTML при этом валиден. Ловит это `check_view.py` (Г4), но дешевле
не создавать.

**Г4. 🔴 Собрал документ — прогони гейт вида, это правило корневого `CLAUDE.md`:**
```
python3 _generator/tools/check_view.py <собранный html>
```
Он ловит ровно класс «текст есть в исходнике, но съеден при отрисовке». **Сборка без этого гейта
не считается сборкой.**

**Г5. Заглушки, которые надо снять руками — их две, обе найдены грепом:**
- `kommentarij_lektoru: заполнить` — **на всех 15 карточках** (`grep -c "kommentarij_lektoru: заполнить" teorkat-vvedenie/L2/slajdy/*/slaid.md`). Поле для устного комментария лектору. ⚠ **Сам его НЕ сочиняй** — это речь владельца, не твоя. Правильный ход: снять заглушку (пустое значение `""`), а факт назвать в отчёте одной строкой. Порождать заглушку и не иметь шага, который её снимает, — уже записанный урок фабрики.
- `teorkat-vvedenie/L2/slajdy/izomorfizm/slaid.md`, секция `## Правки`, строка `- заполнить` — мусор, убрать.

**Г6. Помарки владельца П12/П13 в движке УЖЕ ПОЧИНЕНЫ — не чини повторно.** Проверено:
`_generator/skeleton/base.css:83` → `.panel { background: none; }` (прозрачный фон),
`base.css:71` → `#lect-number` (номер слайда), `tokens.css:3` → `--accent: #785a18; --marker: #c0aa5a`.
**Твоя работа — УВИДЕТЬ их на собранной деке.** Не увидел — вот это и есть находка, в отчёт.

**Г7. Ни один текст слайда ты не переписываешь.** Фаза 3 принята владельцем. Кажется, что фраза
плохая, — **в отчёт строкой, а не правкой в карточку**. Это прямое требование: переделывать нечего.

---

### ЭТАП A — ЦЕЛИКОМ ОФЛАЙН. Сеть не нужна ни на одном шаге. Делать первым и до конца.

**A1.** Починить три красных SVG (Г1). Критерий — `proverka_pomarok.py` по всем одиннадцати даёт
**rc=0**, и в выводе одиннадцать «ЗЕЛЁНЫЙ».
**A2.** Снять две заглушки (Г5).
**A3.** Фаза 4 — собрать и посмотреть **по одному слайду**, начиная с самого плотного
(`inyekcii`, 8 блоков) и самого лёгкого (`napominanie`, 2 блока):
`python3 _generator/sborka/slaid.py teorkat-vvedenie/L2/slajdy/<имя> -o /tmp/<имя>.html`, затем Г4.
Плотный и лёгкий — две границы; если обе целы, середина почти наверняка цела.
**A4.** Собрать всю деку БЕЗ подбора:
`python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html --bez-podbora`,
затем Г4 по результату.
**A5.** 🔴 **КОММИТ ЭТАПА A — не откладывать до конца захода.** Это и есть страховка от обрыва:
после него на диске и в git лежит смотрибельная дека, даже если сеть больше не вернётся.

### ЭТАП B — ТРЕБУЕТ СЕТИ. Только после коммита A.

**B1.** `pip install playwright && playwright install chrome` — это и есть единственный сетевой шаг.
**Упало по сети — не геройствуй, не ищи обходов, не ставь ничего другого.** Иди в B-СТОП.
**B2.** Полная сборка с подбором (без `--bez-podbora`), затем Г4.
**B3.** Гейт вмещения — ⚠ он принимает **HTML отдельных слайдов, а не деку**
(`usage: gejt_vmeshcheniya.py html [html ...]`, «краснеет на обрезанном тексте»). Значит: собрать
все 15 через `slaid.py` в `/tmp`, затем `python3 _generator/sborka/gejt_vmeshcheniya.py /tmp/*.html`.
Охват назвать числом: «проверено N из 15».
**B4.** Фаза 7 (сцены) — только если A и B зелёные и время осталось.
**B5.** Коммит этапа B отдельно от A.

**B-СТОП (законный исход, не провал):** в отчёте строкой — «подбор типографики не прогнан: сеть.
Дека этапа A на месте, путь такой-то, коммит такой-то». 🔴 **Между A и B ничего не переделывай
заново** — B только доводит, A уже сдан.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — пять условий, каждое называет краснеющий артефакт:**
1. `python3 _illustracii/proverka_pomarok.py teorkat-vvedenie/L2/illustracii/*/risunok.svg` → **rc=0**, одиннадцать «ЗЕЛЁНЫЙ». Команда и rc в отчёт.
2. `grep -c "заполнить" teorkat-vvedenie/L2/slajdy/*/slaid.md` → суммарно **0**. Вывод в отчёт.
3. 🔴 **Живой прогон на реальном объекте репозитория:** дека `teorkat-vvedenie/L2/dist/index.html` существует, все **15** слайдов в ней, `python3 _generator/tools/check_view.py` по ней → **rc=0**.
4. Гейты входа остались зелёными ПОСЛЕ твоих правок, не только до: `gejt_kartochki.py`, `gejt_illyustracij.py`, `graf_zavisimostej.py` — три команды, три rc в отчёт.
5. `git_zona.py check --zone` ✅, `git show --stat` без чужих путей, оба коммита (A и B) названы хэшами.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «слайды собрались», а «проверено 15 из 15
слайдов, 11 из 11 иллюстраций».

## 3. ВЕРИФИКАТОР

🔴 **Ты правишь картинки — а «стало лучше» на глаз проверить нельзя, это и есть класс ошибок,
ради которого владелец записал 13 помарок.**

- 🔴 **Свежий субагент смотрит собранную деку ГЛАЗАМИ** (отрисовать и посмотреть — `visual-design-review`), со списком П9–П13 из Я3 на руках, и отвечает по каждому пункту отдельно: видит ли номер слайда, прозрачен ли фон иллюстраций, нет ли «?» и подписей поверх стрелок. **Охват назвать числом: «проверено N слайдов из 15».**
- **Три починенных SVG сверить с их `zakaz.md`** (Я5): убрал «?» — смысл рисунка обязан сохраниться. Убрать вопрос, потеряв мысль, — не починка, а порча; это отдельно проверяет тот же субагент.
- **Не полагайся на `proverka_pomarok.py` как на доказательство качества.** Он ловит `<text>` с «?» и текстовые равенства — то есть форму. «Композиция нарисована дугой» он подтвердить не может; это глазами.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ

🔴🔴 **У ТЕБЯ ДВА КОММИТА, И ПЕРВЫЙ — НЕ В КОНЦЕ.** Это не бюрократия, это защита от обрыва связи:
```
коммит A — после шага A5, ДО всякой сети.  Подставлять вместо <зона>:
              "teorkat-vvedenie/L2/**" "_generator/skeleton/**"
коммит B — после шага B5, если сеть дожила.
```
Сеть умерла между A и B — **у владельца всё равно есть собранная дека в git**. Вот ради чего.
Оба хэша — в отчёт, каждый своей строкой, с пометкой A/B.

---
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `teorkat-vvedenie/L2/**` `_generator/skeleton/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `teorkat-vvedenie/L2/**` `_generator/skeleton/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Оспоренная (и уже снятая) ложная предпосылка — до всякой правки контента.**

Worktree завёлся от коммита `6729551`, а фаза-3-текст уехал в основную ветку `arka/mat-kostyak`
одним коммитом позже (`5c35f4c "L2: фаза 3 — текст слайдов, 654 слова, медиана 41; правки
владельца"`) — сам коммит не потерялся, просто ветка захода отпочковалась чуть раньше. Живые
команды на старой точке давали ровно обратное входной таблице:
- `gejt_kartochki.py` → КРАСНЫЙ, 14 из 14, 83 замечания (вместо «ЗЕЛЁНЫЙ, 15 из 15, 0»)
- `graf_zavisimostej.py` → 14 слайдов, 0 связей (вместо «15 слайдов, 12 связей»)
- в 13 из 14 карточек секция «Текст слайда — сжато» несла только заголовки блоков без тел
- папки `izomorfizm` не существовало вовсе (слайд «Диск и окружность» держала старая папка
  `fundamentalnaya-gruppa`, `izomorfizm` — новый файл именно в коммите `5c35f4c`)

По указанию владельца (сообщение в чат) выполнено:
```
python3 _generator/tools/git_zona.py adopt --branch arka/mat-kostyak --zone teorkat-vvedenie/L2/ --yes
```
Взял ровно зону `teorkat-vvedenie/L2/**` с `arka/mat-kostyak` (16 файлов, включая новый
`izomorfizm/slaid.md`), в индекс. Первая попытка была `merge` (не `adopt`) — та же ветка вне зоны
расходится ещё и в `teorkat-vvedenie/LENTA-L2/{faza2,faza3,view.html}` (выше `L2/`, мне read-only) —
`merge` отказал целиком, что и правильно; `adopt` берёт только названную зону. Побочный эффект —
инструмент сам дописал строку в `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (лог отказанной попытки
merge); это вне моей зоны, не трогаю и не коммичу.

Живые гейты ПОСЛЕ adopt сошлись с исходной таблицей захода один в один:
`gejt_kartochki.py` → ЗЕЛЁНЫЙ 15/15, 0 замечаний · `graf_zavisimostej.py` → 15 слайдов, 12 связей,
циклов и нарушений порядка нет · ровно 14 карточек с `kommentarij_lektoru: заполнить` (не 15 — у
`centr-gruppy` уже снято) и ровно одна мусорная строка `- заполнить` в `izomorfizm/slaid.md` —
то есть Г5 описывал реальность верно, просто по коммиту, которого в моей ветке ещё не было.

**Дальше — по заходу без отклонений:** ЭТАП A (A1 три красных SVG → A2 две заглушки → A3 два
пробных слайда inyekcii/napominanie → A4 вся дека `--bez-podbora` → A5 коммит) — этот `adopt`
войдёт в тот же коммит A, он и есть законный источник контента зоны. Затем ЭТАП B при живой сети.
Отдельно проверю на Этапе B: `playwright` как python-пакет уже импортируется в этом окружении
(незаявленное расхождение с «playwright не установлен» из входной таблицы) — но это не значит, что
браузерный бинарник (`playwright install chrome`) уже стоит; гоню `--bez-podbora` A4 независимо от
этого статуса, B1 проверяю отдельно и по факту, не угадывая.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
