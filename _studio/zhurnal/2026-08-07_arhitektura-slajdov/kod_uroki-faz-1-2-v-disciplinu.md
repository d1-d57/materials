# Канал исполнителя — uroki-faz-1-2-v-disciplinu (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — весь материал уже собран и записан в якоре Я1, работа переводческая плюс
> три правки в коде. Субагенты не нужны.
> 🔴 **Спецификация захода — якорь Я1 целиком плюс раздел ЗАДАЧА ниже.**

КОНТЕКСТ. Фабрика научно-популярных лекций, семь фаз. Фазы 1 (интервью) и 2 (раскадровка) прошли
вживую на лекции 2 по теории категорий — и прошли дорого.

**Второй проход по этим фазам сделан МЕХАНИЧЕСКИ, по транскрипту и git-истории, а не по памяти.**
Все числа ниже — снятые командами, дом каждого назван в Я1 (`§6` — разбор транскрипта, `§7` — цена
переделок, `§8` — повторённые требования); проверить: `grep -n "819\|107 переписываний" <Я1>`.

- транскрипт **819 записей**, **29 реплик владельца на 95 487 знаков**;
- **107 переписываний по 15 карточкам**. Рекорд — `bazis-i-matricy`, **10 кругов**, и причина не в
  содержании: нигде не было записано, что порядок понятий это **решение фазы 1**, а не следствие
  порядка в ленте. `napominanie` — 9 кругов на два блока текста, и каждый круг рождал правило,
  которого до него не существовало;
- **тела блоков переписаны целиком трижды по всей лекции** — 25 блоков × 3, потому что формата
  артефакта фазы не существовало;
- три требования владелец повторил больше десяти раз каждое: **«этого не нужно на слайде» — 17 реплик
  из 29 · «не решай за меня, я не знаю» — 14 · «порядок неправильный» — 13.**

🔴 **Три верхние строки — не стилистика, а три отсутствующие спецификации:** что попадает на слайд ·
где граница решения исполнителя и решения владельца · как определяется порядок понятий.

**И главное, что нашлось (У12 в Я1): механизм против сущностной ошибки СУЩЕСТВОВАЛ и не был
включён.** На слайде стояло «изоморфны ⟺ размерности равны» — при том что размерность вводится двумя
слайдами позже. Поймал владелец глазами. А в карточке с самого начала есть поля `vvodit` и
`opiraetsya_na`, и гейт их **проверяет**. Они стояли пустыми у всех 15 карточек. Проверено пробой:
заполнил зависимости и вернул ошибку искусственно — **гейт краснеет поимённо**.

ЦЕЛЬ: чтобы всё это перестало держаться на внимании. Требование владельца дословно: *«внести прямо
сейчас, не откладывая»*.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее: ты НЕ трогаешь лекцию `teorkat-vvedenie/`** — там живая работа
владельца. Ты правишь формат, гейт и дисциплину.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/uroki-faz-1-2`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/uroki-faz-1-2`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add uroki-faz-1-2 --branch zahod/uroki-faz-1-2
  cd ../materials-wt/uroki-faz-1-2
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять).** В `materials`: `_generator/sborka/**`. Во ВТОРОМ репозитории: `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` и `~/Documents/GitHub/disciplina/skills/slajdy/references/**`. Всё вне — **READ-ONLY**.
- 🔴🔴 **ЧУЖИЕ ЗОНЫ — рядом идёт заход `instrument-podklyuchen`, не трогать ни байта:** `_generator/tools/**` · `_illustracii/**` · `.githooks/**` · `disciplina/skills/slajdy/verifikatory/**`. ⚠ **В Я1 есть дефект `zakryt_sessiyu.py --spisok`** (печатает пустоту с `rc=0`, хотя файл лежит по пути, который сам скрипт перечисляет) — это `_generator/tools/`, **чужая зона: строкой в отчёт, не правкой.**
- 🔴 **`teorkat-vvedenie/**` — READ-ONLY ЖЁСТКО:** там живая лекция и работа владельца.
- 🔴 **В `disciplina` НЕ ЗАВОДИ ВЕТКУ И НЕ ДЕЛАЙ `checkout`** — одна рабочая папка на всех, и рядом в ней работает соседний заход. Коммить на вычекаученную, явными путями, `git show --stat` проверить.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
🔴 **ВЛИВАТЬ НИЧЕГО НЕ НАДО — и это проверено, а не предположено.** Строку, которую сюда вписал
генератор («невлитых веток 0»), Cowork снял и заменил на живой замер
`git --no-optional-locks branch --no-merged arka/mat-kostyak`:

```
  zahod/instrument-podklyuchen            ← заход, который идёт ПРЯМО СЕЙЧАС
+ zahod/konsolidaciya-i-git-kontur        ← 4b669c0, трогает _generator/tools/** — ЧУЖАЯ зона
```

**Обе — чужие, обе не твои.** Не вливай, не переключайся, не «подтягивай к себе». Если тебе покажется,
что чего-то не хватает в `bootstrap_zahod.py` или `check_zahod.py` — это оно и есть, и оно придёт
позже чужими руками.

### 🔴 0-бис. СПАСТИ ЧУЖУЮ ГОТОВУЮ РАБОТУ В `disciplina` — ДО своей первой правки

В репозитории `~/Documents/GitHub/disciplina` (ветка `zahod/format-kartochki-faza-1`) **лежат
незакоммиченными два файла законченной работы предыдущего захода:**

```
 M skills/slajdy/verifikatory/faza-1-intervyu.md
 M skills/slajdy/verifikatory/faza-2-raskadrovka.md
```

Это клаузы 13 и 15 верификаторов фаз 1 и 2 («читать стандарт сейчас, а не по памяти промпта»).
Работа закончена и проверена, автор её не закоммитил: его заход не дал команды коммита для второго
репозитория. **Ты работаешь в этом же дереве** — твой первый же `checkout`, `stash` или неаккуратный
`commit` их сотрёт или подметёт в свой коммит.

**Первым ходом в `disciplina`, ДО любой своей правки, закоммить их ОТДЕЛЬНО от своей работы:**
```
cd ~/Documents/GitHub/disciplina
git --no-optional-locks add -- skills/slajdy/verifikatory/faza-1-intervyu.md skills/slajdy/verifikatory/faza-2-raskadrovka.md
git --no-optional-locks commit -m "verifikatory: клаузы 13/15 — живой стандарт вместо памяти промпта (работа захода instrument-podklyuchen)" -- skills/slajdy/verifikatory/faza-1-intervyu.md skills/slajdy/verifikatory/faza-2-raskadrovka.md
git --no-optional-locks show --stat        # обязаны быть ровно эти два файла
```
🔴 **Ветку НЕ переключай — коммить на ту, что вычекаучена.** Хэш этого коммита — **отдельной строкой
в отчёте**, не смешивая со своим. Дерево оказалось чистым (кто-то успел раньше) — так и напиши, это
законный исход.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/uroki-faz-1-2`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/sborka/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FAZ-1-2.md` **ЦЕЛИКОМ, 570 строк** | **спецификация захода.** §9 — готовые спецификации шести артефактов, У12 — механизм зависимостей, У13 — `itog` и эталонное распределение, У14 и У9 — правила регистра, §10 — пять проблем промптов с цитатами. **Формулировки владельца цитировать, не пересказывать** |
| Я2 | `_generator/sborka/gejt_kartochki.py` · `bootstrap_lekcii.py` · `formaty.py` | предмет правок в коде |
| Я3 | `disciplina/skills/slajdy/SKILL.md`, разделы **ФАЗА 1** и **ФАЗА 2** | дом процедуры и решений фаз |
| Я4 | `disciplina/skills/slajdy/references/kartochka-slajda.md` · `status-bloka.md` · `fazy-1-2-plan.md` | дома спецификаций формата и регистров |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — четыре хода по убыванию цены

### Ф1. 🔴 ВКЛЮЧИТЬ МЕХАНИЗМ, КОТОРЫЙ УЖЕ ЕСТЬ (У12) — самое дорогое, делай первым

Поля `vvodit` и `opiraetsya_na` в карточке существуют, гейт их **проверяет**, и они стояли пустыми
у всех пятнадцати карточек. Никто не требовал их заполнять: гейт на пустом списке молчит, процедура
фазы 1 их не называет, блок «ЧТО ДАЛЬШЕ С ЭТИМ ФАЙЛОМ» о них не говорит. **Зелёный от промаха мимо
входа.**

Цена уже оплачена: сущностная ошибка «понятие использовано раньше, чем введено» дошла до собранного
слайда и была поймана глазами владельца.

**Три правки, все три обязательны — по отдельности каждая не работает:**
1. **`gejt_kartochki.py --faza 1` требует непустых `vvodit`/`opiraetsya_na`** — как сегодня требует
   `tip_idei` и `centralnyj_blok`. ⚠ Слайд может законно ничего не вводить (`narrativ`,
   `divider`) — значит красное на **пустых обоих сразу** либо явный маркер «ничего не вводит и ни на
   что не опирается»; выбери и обоснуй в `## ПЛАН`.
2. **Процедура фазы 1 в `SKILL.md` называет их выходом фазы** — сегодня не называет.
3. **Блок «ЧТО ДАЛЬШЕ С ЭТИМ ФАЙЛОМ» в `bootstrap_lekcii.py` называет их** в строке ФАЗЫ 1.

**И два дефекта, найденных той же пробой — чинятся здесь же:**
- 🔴 **инлайн-форма `opiraetsya_na: [{termin: X, vvedeno: Y}]` роняет гейт трейсбеком**
  (`AttributeError: 'str' object has no attribute 'get'`). Рабочая форма — многострочная, но **обе
  выглядят одинаково законными и формат нигде не назван**. Чинить: внятное сообщение вместо
  трейсбека **и** форма записана в `kartochka-slajda.md`;
- `zagolovok_na_ekrane: заполнить` краснел на 14 карточках из 15 — заглушка, которую бутстрап
  порождает, а снять её никто не обязан. Назвать шаг, который её снимает, либо не порождать
  заглушку в этом поле.

**КРИТЕРИЙ Ф1 (может ПРОВАЛИТЬСЯ):** свежепорождённая лекция с пустыми `vvodit`/`opiraetsya_na` →
`--faza 1` **КРАСНЫЙ**; с заполненными → зелёный. Карточка с термином, который опирается на слайд,
где его нет, → красное **поимённо**, как в пробе Я1 У12. Инлайн-форма → **внятное сообщение, не
трейсбек**. Все четыре команды и коды возврата в отчёт.

### Ф2. 🔴 ШЕСТЬ СПЕЦИФИКАЦИЙ АРТЕФАКТОВ — в дисциплину (У8, §9 в Я1)

В `Я1 §9` они **уже написаны**, с четырьмя пунктами каждая: что внутри и чего быть не должно · кто
заполняет и кто читает · чем проверяется · **зачем он такой**. Артефакты: `brief.md` · `slaid.md` ·
тело блока · лента фазы 2 · `zakaz.md` · пара `INTERVYU.md`/`RAZMETKA-L2.md`.

**Перенести в дисциплину цитатами.** 🔴 **Пункт «зачем» не выбрасывать** — он и есть защита от
переизобретения. Образец, который стоил трёх переписываний: *«лента из блоков — это тест на полноту
разметки: если важная мысль не попала ни в один блок, её физически негде показать, значит либо план
неполон, либо мысль лишняя; и выясняется это до написания текстов, а не после сборки»*.

⚠ **Главная проблема промптов из §10 закрывается здесь же:** формат тела блока был назван
(`### [тип] мысль`), **а регистр и читатель — нет ни слова**. Это стоило трёх полных переписываний
всех 25 блоков. Регистр по типу блока — в `status-bloka.md`.

**КРИТЕРИЙ Ф2:** шесть спецификаций в домах, у каждой все четыре пункта, **«зачем» на месте**;
проверяется грепом по ключевой фразе каждой — команду и вывод в отчёт.

### Ф3. `itog` ВЫВЕДЕН + ЭТАЛОННОЕ РАСПРЕДЕЛЕНИЕ (У13)

**Решение владельца дословно:** *«эта идея с итогом очень странная. Я бы заменил это на нарратив.
Это просто классический блок с большим количеством подблоков, ничем не отличается по смыслу от
введения. Я бы везде прописал: типы блоков лишние, лучше сделать ещё один нарративный блок и
уменьшить непонятный разброс»*.
⇒ `itog` из употребления выведен, итоговый слайд пишется нарративом. Тип остаётся в закрытом списке
кода, но помечается неиспользуемым.

🔴 **И первая норма распределения, названная владельцем образцовой** — до сих пор её не
существовало вовсе: `narrativ` 11 · `utverzhdenie` 9 · `opredelenie` 7 · `primer` 6 ·
`dokazatelstvo` 4, **всего 37 на 15 слайдов, в среднем 2,5 блока на слайд**. Слова владельца:
*«конечное количество этих блоков и распределение по ним сейчас будет максимально правильным, и
соответствует тому, как я бы это видел и в других лекциях тоже»*.

Записать как **точку отсчёта, а не как жёсткий порог**: лекция, сильно уехавшая от этих пропорций,
— повод посмотреть, а не ошибка.

**КРИТЕРИЙ Ф3:** решение и распределение записаны в `status-bloka.md` цитатами; одна команда
печатает распределение типов блоков по лекции — прогнана на `teorkat-vvedenie/L2` (**только чтение**),
вывод в отчёт.

### Ф4. ПРАВИЛА РЕГИСТРА (У14 и У9)

Три правила регистра с акта 2 и восемь правил из разбора вкладки фазы 2 — в `status-bloka.md` и
`plakatnyj-stil.md`, цитатами.

---

⚠ **Порядок отступления:** `Ф1` ≫ `Ф2` ≫ `Ф3` ≫ `Ф4`. Не успеваешь — оставь открытым и опиши одной
строкой, что мешает. **Ф1 оставить открытым нельзя**: ради него заход и написан.

🔴 **Что НЕ делать: У11 (граница фаз 1 и 2 проведена не там) — гипотеза, ТРЕБУЮЩАЯ РЕШЕНИЯ
ВЛАДЕЛЬЦА.** Не реализуй её и не переставляй границу сам. Строкой в отчёт.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — четыре условия, каждое называет краснеющий артефакт:**
1. `python3 _generator/sborka/gejt_kartochki.py --faza 1 <карточка с пустыми vvodit/opiraetsya_na>` →
   **rc=1**; та же карточка с заполненными → **rc=0**; карточка с ненайденным термином → rc=1 и имя
   термина в тексте ошибки; карточка с инлайн-формой → rc=1 **без трейсбека**. Четыре команды и
   четыре rc в отчёте.
2. 🔴 **Живой прогон на реальном объекте репозитория, а не только на фикстурах:** `--faza 1` по всем
   пятнадцати карточкам живой лекции `teorkat-vvedenie/L2` (**только чтение**) — **до и после**
   правки, разница названа числом. Плюс регресс
   `bash _generator/tools/fixtures/sborka/PROGNAT.sh`, оба вывода в отчёте.
3. Шесть спецификаций §9 в домах, **у каждой пункт «зачем» на месте** — подтверждает свежий
   субагент §3, поимённо по шести.
4. Оба коммита есть, `git_zona.py check --zone` ✅ по обеим зонам, `git show --stat` без чужих путей.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «уроки внесены», а «правок в коде N из 3,
спецификаций M из 6, правил регистра K из 11».

## 3. ВЕРИФИКАТОР

🔴 **Ты меняешь гейт фазы 1 — то, через что пройдёт КАЖДАЯ будущая лекция.** Ужесточение выглядит
как улучшение ровно до момента, когда оно покраснеет на работающем.

- 🔴 **Проверь на ЖИВОЙ лекции, что новый гейт не покраснел ложно.** `teorkat-vvedenie/L2` —
  **только чтение**: прогони `--faza 1` до и после правки и **назови разницу числом**. Красное на
  корректных карточках — не победа, а гарантия, что гейт отключат.
- **Регресс:** `bash _generator/tools/fixtures/sborka/PROGNAT.sh` — **до** первой правки и после,
  оба вывода в отчёт.
- **Пара фикстур на Ф1**: одна с пустыми зависимостями (красная), одна с заполненными (зелёная).
  Плюс третья — с инлайн-формой: обязана дать **сообщение, а не трейсбек**.
- 🔴 **Свежий субагент сверяет §9 из Я1 с тем, что ты написал в домах**, по одному: «спецификация →
  дом → все четыре пункта на месте, включая „зачем"». Пропавший «зачем» считается потерей.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ

🔴🔴 **У ТЕБЯ ДВА РЕПОЗИТОРИЯ — ДВА КОММИТА, И ВТОРОЙ ЗАБЫВАЮТ.** Ниже подставляй:

| репо | путь | зона (`<зона>` ниже) | ветка |
|---|---|---|---|
| `materials` | `../materials-wt/uroki-faz-1-2` | `_generator/sborka` | `zahod/uroki-faz-1-2` (worktree) |
| `disciplina` | `~/Documents/GitHub/disciplina` | `skills/slajdy/SKILL.md skills/slajdy/references` | 🔴 **та, что вычекаучена — НЕ переключай** |

⚠ **Урок 18, три случая: правка в `disciplina`, не влитая тем же ходом, не видна из `materials`** — и
следующий заход честно делает её заново. Хэши **обоих** коммитов — в отчёт, каждый своей строкой.

Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/sborka/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/sborka/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**0-бис выполнен первым ходом** (до этой правки): коммит `719258f` в `disciplina` — ровно два файла
`verifikatory/faza-1-intervyu.md` + `faza-2-raskadrovka.md`, ветка `zahod/format-kartochki-faza-1` не
переключалась. `zakryt_sessiyu.py --spisok` не трогаю — уже починен соседним заходом, как сказано во
вставке.

**Порядок работы: Ф1 → Ф2 → Ф3 → Ф4**, как велит заход. Ф1 — код в `materials-wt/uroki-faz-1-2`
(зона `_generator/sborka`), Ф2–Ф4 — дисциплина в `disciplina` (зона `skills/slajdy/SKILL.md` +
`skills/slajdy/references/**`).

### Ф1 — развилка «выбери и обоснуй» (пункт 1 задания)

Требование: `--faza 1` краснеет на пустых `vvodit`/`opiraetsya_na`, но слайд `divider`/`cover`/
`vizitka`/`closing`/иногда `narrativ` законно ничего не вводит. Выбираю **не маркер, а условие
«красное только когда оба поля пусты ОДНОВременно, и не для служебных типов идеи»**:

- `tip_idei ∈ {divider, cover, vizitka, closing}` — проверка вообще не применяется: у этих типов по
  определению (таблица `TIPY_IDEI`, `fazy-1-2-plan.md §2`) нет содержания, нечего вводить и не на
  что опираться;
- иначе — красное, если `vvodit == []` **и** `opiraetsya_na == []` одновременно; заполнен хотя бы
  один — зелёное.

**Почему не отдельный маркер-заглушка** (второй вариант, предложенный заходом): маркер вида
`vvodit: [ничего]` добавляет новое зарезервированное значение в формат, которое придётся
документировать, парсить отдельно от обычных терминов и учить читать `check_composition`/сверку
задвоений (`SKILL.md`, гейт выхода фазы 2, строка про `grep ... vvodit ... uniq -d`) — то есть третий
формат сверх уже существующего `CENTRALNYJ_PERECHISLENIE`. Условие «оба пусты» переиспользует уже
принятые в коде понятия (пустой список, `TIPY_IDEI`) и ничего не добавляет в парсер `formaty.py`.
Цена решения: слайд `narrativ`, который сущностно ничего не вводит и ни на что не опирается (редкий
случай — большинство `narrativ` либо опираются на прежде введённое, либо готовят к следующему
термину), краснеет и требует явно вписать в любое из двух полей — это по духу совпадает с «фаза 1
обязана решить», а не с «фаза 1 обязана угадать формат маркера».

Критерий Ф1 из задания («свежепорождённая лекция с пустыми vvodit/opiraetsya_na → КРАСНЫЙ; с
заполненными → зелёный») проверяется этим условием буквально: у бутстрапа `tip_idei` = `заполнить`
(не в списке служебных), оба поля `[]` → красный.

### Ф1 — три правки кода (пункт 2)
1. `gejt_kartochki.py`: новая проверка `_check_vvodit_opiraetsya`, вызывается при `faza == 1`, до
   ветки K3 (слайд без текста — вызывается независимо от наличия тела, т.к. смотрит только шапку).
2. `bootstrap_lekcii.py`: `LIFECYCLE_TMPL`, строка ФАЗА 1 — дописать «заполнить vvodit/opiraetsya_na»
   с краткой причиной (У12); строка ФАЗА 2 — дописать `zagolovok_na_ekrane` в список полей, которые
   решает фаза 2 (снимает заглушку `заполнить`, которая красила 14/15 живых карточек, — сегодня её
   снятие нигде не названо).
3. `SKILL.md` фаза 1: решение 3 (разбиение на блоки) и/или процедура — назвать `vvodit`/
   `opiraetsya_na` выходом фазы 1 рядом с центральным блоком; `kartochka-slajda.md` таблица «какая
   фаза что решает» — добавить строку.

### Ф1 — два дефекта той же пробой
- Инлайн-форма `opiraetsya_na: [{termin: X, vvedeno: Y}]` парсится в список СТРОК (не словарей) —
  `INLINE_LIST_RE` режет по запятой наивно. Чиню в `gejt_kartochki.py`: guard `isinstance(item, dict)`
  перед `.get()`, внятное сообщение вместо `AttributeError`. Парсер `formaty.py` НЕ трогаю (не в
  списке трёх правок, не заявлено как «поддержать инлайн» — только «внятное сообщение», задача явно
  говорит, что рабочая форма многострочная) — минимизирую площадь правки.
- Форма фиксируется в `kartochka-slajda.md` (уже показана примером в §2, но не названа явно
  «единственно рабочей» — добавлю одну строку это называющую, рядом с примером).

### Ф2 (шесть спецификаций из Я1 §9) и Ф3 (itog + распределение) и Ф4 (правила регистра)
— переносятся цитатами в `disciplina/skills/slajdy/SKILL.md`/`references/*.md`, дома конкретных
правок определяю по месту существующей структуры файлов (уже прочитаны целиком: `SKILL.md` фазы 1–2,
`kartochka-slajda.md`, `status-bloka.md`, `fazy-1-2-plan.md`, `plakatnyj-stil.md`). Ф4 (У9+У14) кладу
в `status-bloka.md` (новые §9/§10, тот же дом, что уже несёт аналогичный разбор §8 из того же
источника POMARKI) и в `plakatnyj-stil.md` (регистровое подмножество — как новые пункты «Правила»,
без дублирования полных цитат — по одной строке-указателю на `status-bloka.md`).

Критерий готовности возможен к провалу — если Ф2–Ф4 не уложатся по времени, останавливаюсь на
границе фазы и называю в отчёте «N из 3 / M из 6 / K из 11», как требует раздел КРИТЕРИЙ ГОТОВНОСТИ.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
