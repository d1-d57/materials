# Канал исполнителя — vizitka-i-oblozhka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — всё найдено и предъявлено путями, работа проводочная: подключить готовое.
> 🔴 **ЭТО САМЫЙ ПОСЛЕДНИЙ ЗАХОД ПО Л2.** После него дека идёт в эфир. Ничего сверх списка.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-07_arhitektura-slajdov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/bold-brave-cray/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекция 2 по теории категорий, **эфир в среду**. Дека собрана: 21 слайд, всё зелёное,
осталось **две вещи**, обе — подключить готовое.

## 🔴 В1. ВИЗИТКА ПУСТА, ПОТОМУ ЧТО НОВЫЙ `deck.py` НЕ ЗНАЕТ ПРО СТАНДАРТНУЮ ПАПКУ

Прошлый заход отчитался: *«содержания нет нигде — ни в генераторе, ни в лекции»*, и назвал
предпосылку владельца ложной. **Он ошибся: искал в `tipy.py` и в карточках.** Аналитик нашёл
содержание за одну команду — оно лежит в генераторе с 30 июля:
```
_generator/skeleton/sluzhebnye/
  vizitka.md          587 б   ← текст: Матфак ВШЭ · 179 школа · @turings_crossword · @d1_d57
  vizitka-photo.html   96 КБ  ← фотография владельца
  vizitka-qr.html     2,1 КБ  ← QR
  vizitka.html · oblozhka.html · final.html · style.css
```
⇒ **Класс дефекта: механизм построен и не подключён** — старый движок эту папку брал, новый
`deck.py` порождает визитку из `tipy.vizitka`, где только раскладка.

🔴 **Требование владельца — раз и навсегда, не разово.** Дословно: *«визитка должна быть стандартным
файлом… просто положить туда буквально тот текст. Там не должно быть ничего заполняться. Это для
меня всегда мои слайды, и там всегда один и тот же текст. Когда его поменяю — я потом в генераторе
поменяю»*.
⇒ Содержание визитки **берётся из `_generator/skeleton/sluzhebnye/` всегда и без полей в `brief.md`.**
Автор лекции про визитку не думает вовсе.

## 🔴 В2. ЗАГОЛОВОК ОБЛОЖКИ — как в лекции 1

Владелец: *«посмотреть, как это было в первой лекции, повторить — что-то типа „Введение в теорию
категорий, лекция…“»*. Замер по живой Л1 (`teorkat-vvedenie/src/dist/index.html`, слайд `sl-title`):
```
Введение в теорию категорий
Лекция 1
29 июля 2026
```
Шаблон это умеет — `_generator/skeleton/sluzhebnye/oblozhka.html` несёт три слота:
`{TITLE}` · `?SUB{SUB}` · `?DATEPLACE{DATEPLACE}`. Сегодня заполняется только первый.

ЦЕЛЬ: визитка с настоящим содержанием, обложка с тремя строками, дека пересобрана, всё влито.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: не выходит подключить папку `sluzhebnye/` — скажи громко и сразу.** До эфира
считанные дни; дека без визитки хуже, чем дека с задержкой на один ход.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/vizitka-oblozhka`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/vizitka-i-oblozhka`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add vizitka-oblozhka --branch zahod/vizitka-i-oblozhka
  cd ../materials-wt/vizitka-oblozhka
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/sborka/**` `teorkat-vvedenie/L2/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Первым ходом, до всего остального.

```
python3 _generator/tools/git_zona.py merge zahod/final-l2 --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _illustracii --zone _fond --zone obzory --zone ucheniki
```

🔴 **Зона слияния — НЕ рабочая зона захода, а ВСЁ, что трогает ветка** (проверено при сборке:
32 пути вне рабочей зоны). Узкая зона → инструмент откажется слить что-либо. Отказал — **возьми
строку зон из его собственного отказа.**

Проверка фактом:
```
git --no-optional-locks merge-base --is-ancestor zahod/final-l2 HEAD ; echo $?
```
Ветки НЕ удаляй ни при каком исходе.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

🔴 **Забрать в git ВСЁ незакоммиченное своей зоны** — заход финальный. Включая пересобранную `teorkat-vvedenie/L2/dist/index.html` и файлы-заходы арки, оставленные аналитиком (`kod_final-l2.md` и прочие: список снимать `git_zona.py check`, не по перечню). ⚠ `_fond/**`, `obzory/**`, `ucheniki/**`, `_generator/build_listok.py`, `INCIDENTY.md` — чужое, не трогать."`. Забирать нечего — так и вписать явно>

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

🔴 **Гасить нечего: ветки `zahod/*` и рабочие папки не удаляются никогда — решение владельца.** Свою ветку не вливай, влитие за приёмкой. Параллельных заходов нет."`. Гасить нечего — так и вписать явно>

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/vizitka-i-oblozhka`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/sborka/**` `teorkat-vvedenie/L2/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 `_generator/skeleton/sluzhebnye/` — **все семь файлов** | источник содержания визитки, обложки и финального. **Читать, НЕ переписывать** |
| Я2 | `_generator/sborka/deck.py` — функция порождения служебных слайдов | предмет правки В1 |
| Я3 | `_generator/sborka/tipy.py` — `vizitka`, `oblozhka`, `finalnyj` | куда приходят данные |
| Я4 | `teorkat-vvedenie/L2/brief.md` | вход В2 |
| Я5 | `teorkat-vvedenie/src/dist/index.html`, слайд `sl-title` — **только чтение** | эталон обложки Л1 |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — три шага, все короткие

### Ш1. ВЛИТЬ `final-l2` (команды — §0.1)

### Ш2. 🔴 ВИЗИТКА БЕРЁТСЯ ИЗ `sluzhebnye/` — навсегда, без полей в лекции

`deck.py`, порождая `vizitka`, читает `_generator/skeleton/sluzhebnye/vizitka.md` (текст) и кладёт
`vizitka-photo.html` / `vizitka-qr.html` в слоты фото и QR, которые `tipy.vizitka` уже размечает
(251×260 и 250×250).
🔴 **Никаких новых полей в `brief.md` для визитки.** Владелец сказал прямо: заполняться там нечему.
⚠ **Тем же ходом проверь обложку и финальный** — `oblozhka.html` и `final.html` лежат в той же
папке; если и они игнорируются новым движком, это тот же дефект, почини заодно и скажи в отчёте.
⚠ **Файлы `sluzhebnye/` не переписывай** — они канон, ты их только читаешь.

### Ш3. ЗАГОЛОВОК ОБЛОЖКИ — три строки

В `brief.md` дать данные под три слота шаблона:
```
Введение в теорию категорий      → {TITLE}
Лекция 2                          → {SUB}
<дата эфира>                      → {DATEPLACE}
```
⚠ **Дату уточни у владельца в `## ВОПРОСЫ`, если её нет в `brief.md`** — не выдумывай.
Слово «Функторы» из нынешнего `oblozhka_zagolovok` уходит: владелец заменил его на строй Л1.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. 🔴 **Живой прогон на реальном объекте:** дека пересобрана БЕЗ `--bez-podbora`, rc=0, 21 слайд,
   `grep -c MISSING-MATH` → **0**.
2. 🔴 **Визитка НЕ пуста — проверять замером, а не глазами:** в собранной деке внутри `#vizitka`
   есть текст из `sluzhebnye/vizitka.md` (`grep -c 'turings_crossword'` → ≥1), фотография и QR
   (панелей с картинкой → 2, было 0). Команды и числа в отчёт.
3. Обложка несёт три строки: «Введение в теорию категорий», «Лекция 2», дату. Греп по деке.
4. Гейты: `gejt_kartochki.py`, `gejt_verstki.py`, `gejt_vmeshcheniya.py`, `proverka_pomarok.py` —
   rc в отчёт. ⚠ `gejt_illyustracij.py` даст **известный красный** («`pustoj-kvadrat` — сирота»):
   он собирает ссылки только из карточек и про поля `brief.md` не знает. **Это не регрессия**,
   лечение вне зоны — назвать и идти дальше.
5. Регресс: все `PROGNAT.sh` (число — командой), зелёных N из N.
6. 🔴 **Хвостов нет:** `git status --porcelain` в рабочей папке → пусто.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «визитка сделана», а «служебных слайдов с
содержанием N из 3, строк обложки M из 3».

## 3. ВЕРИФИКАТОР

🔴 **Прошлый исполнитель объявил предпосылку владельца ложной, не найдя файлов, которые лежали в
генераторе с 30 июля.** Урок: **«не нашёл» и «нет» — разные утверждения**, и второе требует
названного места поиска.

- 🔴 **Глазами — ТОЛЬКО визитка и обложка** (два кадра). Остальное мерено гейтами; владелец
  запретил переоткрывать восемнадцать содержательных слайдов.
  На визитке проверить: **текст, фотография и QR все три на месте и не наезжают друг на друга.**
- **Ничего не потеряно при слиянии:** `git show --stat` по merge-коммиту; правки `final-l2`
  (контраст `--steel-t`, пять мелочей) на месте — проверить две выборочно командой.
- **`sluzhebnye/` не изменены:** `git diff --stat -- _generator/skeleton/sluzhebnye/` → **пусто**.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/sborka/** teorkat-vvedenie/L2/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/**                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/sborka/** teorkat-vvedenie/L2/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/**   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/sborka/**` `teorkat-vvedenie/L2/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/sborka/** teorkat-vvedenie/L2/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/**` → ✅. Красное здесь — отчёт не принимается: приёмка гоняет этот же гейт первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Гит-контур сделан:** `final-l2` влит в рабочую ветку (rc=0, `merge-base --is-ancestor` подтвердил), 32 пути, без конфликтов. Ветка `zahod/vizitka-i-oblozhka` активна.

**Дата эфира (Ш3) уточнена у владельца:** «12 августа 2026» (в `brief.md`/файлах фазы даты не было — только «эфир в среду», выдумывать запрещено, спросил явно).

**Диагноз по Я1–Я3 (прочитаны все семь файлов `sluzhebnye/` + `deck.py` + `tipy.py` + `brief.md`):** предпосылка владельца подтверждена, прошлый исполнитель ошибся, не найдя файлы, которые лежат в генераторе с 30.07 (не найдено ≠ нет). Механизм НЕ переиспользован новым пайплайном:
- `tipy.oblozhka()` рисует HTML/CSS САМ, с нуля, только `{TITLE}` — слотов SUB/DATEPLACE НЕТ вовсе, хотя `sluzhebnye/oblozhka.html`+`style.css` их несут.
- `tipy.vizitka()` уже размечает верные позиции (251×260, 250×250 — совпадает с `style.css`), но ждёт фото/QR через `data-ill` (пул иллюстраций лекции) — а `sluzhebnye/vizitka-photo.html`/`vizitka-qr.html` это готовые `<img src="data:...">`, не элементы того пула. Текст визитки (`vizitka.md`) вообще не читается — `plan_sluzhebnyh` брал текст только из поля `brief.md:vizitka_tekst`, которого никогда не было.
- `finalnyj` — ПРОВЕРЕНО, дефекта нет: `finalnyj_zagolovok` уже в `brief.md`, `tipy.finalnyj()` его уже подставляет; иллюстрации у L2 для финала нет, ветка `thx-solo` уже работает через существующую логику `if ills else`. Чинить нечего.
- Старый движок брал этот канон через `build_deck.py:generate_service_slides/fill_template` (READ-ONLY, вне зоны) — не копирую его схему id (`sl-title`/`sl-vizitka`/`sl-thanks`) буквально, потому что заход прямо говорит: фото/QR ложатся в слоты, которые **`tipy.vizitka` уже размечает** — то есть CSS/раскладку нового пайплайна не меняем, только источник контента.

**Правки (только моя зона):**
1. `_generator/sborka/tipy.py`
   - `GLOBAL_CSS` — добавить правила `.bullets`/`.bullets li`/`.bullets li::before`/`.tg-link`, дословно с числами `sluzhebnye/style.css` (без `#sl-vizitka`-скоупа — в новом пайплайне класс всё равно встречается только на визитке). Без этого `render_body` роняет `vizitka.md` гейтом «класс не найден в словаре» (проверено: `python3 -c` с реальным текстом карточки — ошибка воспроизведена).
   - `vizitka()` — вместо `ills[0]/ills[1]` + `data-ill` принимает `p["photo_html"]`/`p["qr_html"]` (готовый HTML от deck.py) и вставляет как есть в `.p-photo`/`.p-qr`; добавить `img{width/height:100%;object-fit:cover}` в CSS функции (canon этого не пишет явно, потому что у старого движка сайзинг брал класс `ill-box`, которого в новом пайплайне на этих div нет).
   - `oblozhka()` — добавить необязательные `.sub`/`.sub2` (числа дословно из `style.css`: 32px/22px, margin-top 36/14, letter-spacing .04em, opacity .78) из `p["sub"]`/`p["dateplace"]`.
2. `_generator/sborka/deck.py`
   - `SLUZHEBNYE`: для `vizitka` поля заголовка/иллюстраций → `None` (больше не читаются).
   - `plan_sluzhebnyh()`: для `tip=="vizitka"` — не трогает `brief.md` вовсе, читает `sluzhebnye/vizitka.md` (текст-тело плана), `vizitka-photo.html`/`vizitka-qr.html` (в `params["photo_html"/"qr_html"]`) напрямую с диска (READ-ONLY чтение, не правка). Для `tip=="oblozhka"` — читает новые необязательные поля `brief.md:oblozhka_sub`/`oblozhka_dateplace` → `params["sub"/"dateplace"]`.
   - Рендер тела визитки идёт тем же `render_body(...)`, что и раньше (никакого обхода гейта) — сработает, потому что `.bullets`/`.tg-link` теперь в словаре.
3. `teorkat-vvedenie/L2/brief.md`
   - `oblozhka_zagolovok: Функторы` → `Введение в теорию категорий` (слово «Функторы» уходит только отсюда, `title:` — внутреннее рабочее имя, не трогаю).
   - Добавить `oblozhka_sub: Лекция 2`.
   - Добавить `oblozhka_dateplace: 12 августа 2026`.

**После правок:** пересборка `deck.py` БЕЗ `--bez-podbora` (rc=0, солвер отработает 18 карточек), затем критерий готовности п.1–6 замером, гейты п.4, регресс п.5, `git status --porcelain` пусто, коммит своей зоны.

**Спор с критерием готовности:** не нашёл — критерий соответствует диагнозу, идти по нему как есть.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. На этой машине нет `timeout`/`gtimeout` (macOS без coreutils) — прогон всех 26 `PROGNAT.sh` наивным foreground-циклом без ограничителя ушёл в 10-минутный таймаут инструмента вслепую (ни одной строки результата), реальный виновник — фикстура `sborka` (несколько полных сборок деки + плейбрайт внутри, минуты, не секунды). Почини фоновым процессом с ручным вотчдогом (`sleep N && kill`) — сработало, все 26 отработали за один прогон. Повторится на следующем заходе, который погонит `PROGNAT.sh` целиком тем же наивным способом.
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** В1: `deck.py`/`tipy.py` подключены к `_generator/skeleton/sluzhebnye/` — механизм, который старый движок брал, а новый порождал визитку из `tipy.vizitka` с пустой раскладкой (диагноз владельца подтверждён, прошлый исполнитель ошибся, не найдя файлы — искал не там). Визитка теперь читает текст ИЗ `sluzhebnye/vizitka.md`, фото/QR — ИЗ `vizitka-photo.html`/`vizitka-qr.html`, при КАЖДОЙ сборке, без единого поля в `brief.md` — ровно как просил владелец («это всегда мои слайды, менять буду в генераторе»). В2: обложка получила необязательные `.sub`/`.sub2` (числа CSS дословно из `sluzhebnye/style.css`), `brief.md` L2 несёт `oblozhka_zagolovok`/`oblozhka_sub`/`oblozhka_dateplace` — строй Л1. `finalnyj` проверен тем же ходом (заход требовал явно) — дефекта не нашёл, уже работал корректно от `finalnyj_zagolovok`, не трогал.

**Как проверил.**
- rc=0 сборки, 21 слайд, `grep -c MISSING-MATH` → 0.
- визитка: `grep -c turings_crossword` → 1; панелей с картинкой (`data:image` внутри секции `#vizitka`) → 2, было 0.
- обложка: все три строки («Введение в теорию категорий», «Лекция 2», «12 августа 2026») подтверждены разбором HTML-секции `#oblozhka`.
- гейты: `gejt_kartochki.py` → ЗЕЛЁНЫЙ rc=0; `gejt_verstki.py` → ЗЕЛЁНЫЙ rc=0; `gejt_vmeshcheniya.py teorkat-vvedenie/L2/dist/index.html` → «влезло», rc=0; `proverka_pomarok.py` по всем 11 SVG лекции → ЗЕЛЁНЫЙ rc=0 на каждом. `gejt_illyustracij.py` → КРАСНЫЙ rc=0(инструмент), 1 замечание: «заказ `pustoj-kvadrat` — сирота» — это ИЗВЕСТНЫЙ красный, названный самим заходом (гейт не знает про поля `brief.md`), не регрессия.
- регресс: `find _generator -iname PROGNAT.sh | wc -l` → 26, все 26 зелёные (числа и имена — в логе прогона, повторяемо командой).
- глазами (два кадра, как требовал заход): обложка и визитка открыты в браузере на реальном `dist/index.html` — три строки на обложке читаемы и не наезжают друг на друга; на визитке текст/фото/QR все на месте, ничего не перекрывается (скриншоты сняты в сессии).
- `git status --porcelain` в рабочей папке → пусто (после коммита).
- `git diff --stat -- _generator/skeleton/sluzhebnye/` → пусто (канон не тронут).
- слияние `final-l2` ничего не потеряло: `--steel-t` на месте (2 вхождения `tokens.css`, 1 в `base.css`), `pustoj-kvadrat` реально переехал из `_snyato/` (две выборочные проверки командой, как просил верификатор).

**Что НЕ трогал.** `sluzhebnye/*` (только читал), `finalnyj`-логику (проверил — не дефектна), всё вне зоны (`_generator/build_deck.py`, `_illustracii/`, гейты в `_generator/tools/` — только вызывал, не правил), ветки `zahod/*` (не удалял, свою не вливал).

**Вопросы.** Дата эфира для обложки уточнена явно у владельца в сессии (не было в `brief.md`) — «12 августа 2026», внесена. Один пункт очереди — урок фабрике про отсутствие `timeout` на машине — см. `## ВОПРОСЫ` выше.

**Результат верификатора:** не запускал отдельно — это заход-исполнитель, верификация № 3 (визитка/обложка глазами, слияние, `sluzhebnye/` не тронут) выполнена мной самим по пунктам его чек-листа и описана в «Как проверил» выше.

**Время прогона + токены:** неприменимо — канал `app`, снимать неоткуда (см. правило захода).

**ПОВТОРЯЕМОСТЬ находок:** ничего не повторится на следующей лекции по СОДЕРЖАНИЮ — дефект был структурный (механизм не подключён), правка в `deck.py`/`tipy.py` закрывает его РАЗ И НАВСЕГДА для всех будущих лекций (визитка больше никогда не спросит `brief.md`, обложка у любой лекции сможет взять `oblozhka_sub`/`oblozhka_dateplace`, если их впишут). Единственное повторяемое — процедурная находка про `PROGNAT.sh`/`timeout`, она в очереди отдельным пунктом.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/vizitka-oblozhka/teorkat-vvedenie/L2/dist/index.html` — открывать браузером (после приёмки и слияния — по тому же пути в `materials/teorkat-vvedenie/L2/dist/index.html`).

**НЕОБРАТИМОЕ:** необратимого нет. Все правки — модификации отслеживаемых файлов в git, откат — `git checkout`/`git revert` на коммит `bcde0d7`; ветки не удалялись, `sluzhebnye/` не переписывался.

**КОММИТ:** `bcde0d7b8a74e5eb56faf3e53bec4d8f42b03e3c` — «vizitka-i-oblozhka: визитка читает канон sluzhebnye/ вместо пустых полей brief.md, обложка несёт три строки Л1» · `git_zona.py check --zone "_generator/sborka/**"` → ✅ · `--zone "teorkat-vvedenie/L2/**"` → ✅ · `--zone "_studio/zhurnal/2026-08-07_arhitektura-slajdov/**"` → ✅ (три отдельных вызова — флаг `--zone` берёт одно значение, не список).
