# Канал исполнителя — vhod-fazy-1 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — все три работы расписаны здесь до команд и чисел; плана, рождающегося по
> ходу, нет. Субагенты не нужны.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.** За сутки трижды заход не увидел
> того, что аналитик написал, но не смог закоммитить: ты работаешь в своей копии дерева.

КОНТЕКСТ. Фабрика научно-популярных лекций. Скилл `slajdy` описывает семь фаз сборки лекции;
фаза 1 — интервью с владельцем, из которого рождается структура: части, слайды, блоки.

Прошлый этап: за сутки скилл переписан в семь фаз единой формы, принято двенадцать заходов,
появились источник истины (папка на слайд), компилятор слайда и солвер вмещения.

🔴 **Но фаза 1 не выполнима вообще, и рвётся на первом же исполняемом шаге.** Её процедура велит
породить структуру скриптом (шаг 3, «руками структуру не заводят»), а скрипту нужен список имён
слайдов, который является выходом подфазы 5.2 — идущей ПОЗЖЕ. Воспроизведено командой:

```
python3 _generator/sborka/bootstrap_lekcii.py /tmp/proba-razryv
→ error: the following arguments are required: --slajdy
```

ЦЕЛЬ: сделать вход фазы 1 исполнимым — и по дороге закрыть две вещи, без которых этому нельзя
верить: незакоммиченную работу аналитика и гейт потерь, который дал два противоположных вердикта
об одной ветке.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, говорю громко и заранее: ты НЕ собираешь лекцию.** Ты чинишь вход. Живой прогон
лекции насквозь — отдельный заход (З6), и он идёт после З2. Соблазн «раз уж починил — давай
попробую собрать» — это НЕ твоя задача, и результат такой пробы никто не сможет принять.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ — ДВА МЕСТА, и это единственное отступление от обычного; читай внимательно.**
  - **Э0 (коммит консолидации) делается в ОСНОВНОЙ папке `~/Documents/GitHub/materials`, НА ТЕКУЩЕЙ ветке `arka/mat-kostyak`, БЕЗ переключения ветки.** Иначе нельзя: файлы, которые надо закоммитить, физически лежат в рабочем дереве основной папки и в worktree их нет. `git commit` дерево не трогает — это безопасно; `git checkout` ЗАПРЕЩЁН.
  - **Э1–Э3 (весь код) — в отдельной рабочей папке.** 🔴 Заводишь её САМ, ПОСЛЕ Э0, из корня репозитория:
    ```
    python3 _generator/tools/git_zona.py worktree add vhod-fazy-1 --branch zahod/vhod-fazy-1
    cd ../materials-wt/vhod-fazy-1
    ```
    Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы (живых рабочих папок 5), переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка.
- **ЗОНА (можно менять):**
  - в основной папке, ТОЛЬКО ходом Э0 и только коммитом: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/_INFRA-git/VERDIKTY.md`, и во ВТОРОМ репозитории `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md`. Содержание этих файлов НЕ править — это чужой текст, ты его только сохраняешь;
  - в рабочей папке: `_generator/tools/git_zona.py` `_generator/tools/fixtures/git_zona/**` `_generator/sborka/bootstrap_lekcii.py`, и во ВТОРОМ репозитории `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` `~/Documents/GitHub/disciplina/skills/slajdy/references/vhod-gejty.md` `~/Documents/GitHub/disciplina/skills/slajdy/references/byudzhet-obyoma.md`.
  - Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ДВА РЕПОЗИТОРИЯ — ДВА ОТДЕЛЬНЫХ КОММИТА, и пути из разных репозиториев НИКОГДА не собираются в одну командную строку.** Второй репозиторий — `GIT_ZONA_REPO=~/Documents/GitHub/disciplina`. Это известные грабли: команда, склеившая пути обоих репо, уже ловилась верификатором.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- Проверь ветку: `git branch --show-current` — обязано быть `zahod/vhod-fazy-1`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: она же Э0 — консолидация коммитится ПЕРВЫМ ходом, и после неё в основной папке чисто. Кодовая зона (`_generator/**`) в HEAD чиста — фабриковать пустой коммит не надо.
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.** Спецификация — в §2 этого файла.

| # | файл | зачем |
|---|---|---|
| Я1 | `_generator/sborka/bootstrap_lekcii.py` (125 строк) | 🔴 предмет правки Э2 |
| Я2 | `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md`, **только раздел «ФАЗА 1»** (примерно строки 180–360: ВХОД · РЕШЕНИЯ ФАЗЫ · ПРОЦЕДУРА · подфазы 5.1–5.4 · гейт выхода) | что фаза обязана решить и куда это записывается |
| Я3 | `~/Documents/GitHub/disciplina/skills/slajdy/references/byudzhet-obyoma.md` | откуда взялась формула `минут × 110` и корпусная медиана 45 слов |
| Я4 | `~/Documents/GitHub/disciplina/skills/slajdy/verifikatory/faza-1-intervyu.md` | 🔴 **здесь уже описана структура `INTERVYU.md`** — разделы, которые верификатор грепает. НЕ изобретать формат заново |
| Я5 | `_generator/tools/git_zona.py`, функции `branch_loss`, `blobs_elsewhere`, `blobs_v_istorii`, `batch_oids`, `cmd_poteri` | предмет правки Э1 |

- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — пять ходов, строго по порядку

### Э−1. 🔴 ВЛИТЬ ПРИНЯТЫЙ СОЛВЕР — САМЫЙ ПЕРВЫЙ ХОД, ДО ВСЕГО ОСТАЛЬНОГО

Заход `solver-v3-dyhanie` **принят владельцем 2026-08-08**: отчёт полный, таблица по 16 слайдам на
месте, вердикт положительный. Но его работа **не влита** и из основной папки не видна. Владелец
сказал про это дословно: *«это четвёртый раз за арку, и он должен быть первым блоком следующего
захода, а не строкой в чат»*.

В основной папке `~/Documents/GitHub/materials`, на текущей ветке `arka/mat-kostyak`, **не
переключая ветку**:
```
python3 _generator/tools/git_zona.py merge zahod/solver-v3-dyhanie
echo "rc=$?"
```
**Проверка фактом на диске, а не словом инструмента** — все три обязаны сойтись:
```
ls _generator/tools/fixtures/sborka/obratnyj-progon/izmerit_dyhanie.py
grep -c blok_koef _generator/sborka/vmeshchenie.py     # > 0
python3 _generator/sborka/vmeshchenie.py --demo-zony    # kegl [35, 41.0]
```
⚠ **Ветку после слияния НЕ УДАЛЯТЬ.** Рядом идёт заход `dovodka-solvera`, который берёт из неё
коммит `d5c4e57`; и вообще ветки в этом репозитории не удаляет никто, кроме владельца.

**КРИТЕРИЙ Э−1:** три проверки выше сошлись, хэш merge-коммита назван в отчёте. Слияние дало
конфликт — **не разруливай наугад**: зафиксируй, напиши в `## ВОПРОСЫ` и остановись. Конфликт тут
структурный (класс E, известен), и цена неверного разрешения выше цены остановки.

---

### Э0. 🔴 КОММИТ КОНСОЛИДАЦИИ — СРАЗУ ПОСЛЕ СЛИЯНИЯ, ДО ВСЯКОЙ РАБОТЫ

Аналитик из Cowork написал за вечер 08.08 несколько файлов и **закоммитить их не может по
построению**: песочница в `.git` не пишет. Владелец git-команд принципиально не выполняет сам.
Пока эти файлы не в git, для любого захода в отдельной рабочей папке **их физически не
существует** — ровно на этом уже потерян один круг (урок 8 арки).

**Списки проверены командой 2026-08-08 в песочнице, а не переписаны из чужого документа.**
⚠ Список в `PEREDACHA-2026-08-08.md §0` **неполон и частично неверен** — он называет как
незакоммиченные файлы, которые давно в git, и пропускает два файла-захода. Правый список — этот.

**Репозиторий `materials` (ветка `arka/mat-kostyak`, основная папка) — новые файлы:**
```
_studio/zhurnal/2026-08-07_arhitektura-slajdov/PEREDACHA-2026-08-08.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/VYGRUZKA-2026-08-07.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/VYGRUZKA-2026-08-07-2.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_solver-v3-dyhanie.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_vhod-fazy-1.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_priyomka-solvera-v3.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_dovodka-solvera.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_graf-zavisimostej.md
```
⚠ `kod_priyomka-solvera-v3.md` ОТМЕНЁН (приёмка состоялась в чате, задача переехала в
`kod_dovodka-solvera.md`) — но коммитится всё равно: отменённый заход с объяснением причины
дешевле, чем файл-призрак, зарегистрированный в `KARTA.md` и отсутствующий на диске.
**Он же — уже отслеживаемые, но изменённые:**
```
_studio/docs/KARTA.md
_studio/zhurnal/_INFRA-git/VERDIKTY.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/NAVIGATOR.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/SESSIYA.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_obratnyj-progon.md
```
**Репозиторий `disciplina` — ОТДЕЛЬНАЯ команда, отдельный коммит:**
```
skills/slajdy/DOLG.md
```

🔴 **`kod_solver-v3-dyhanie.md` и `kod_obratnyj-progon.md` в списке ПЕРЕДАЧИ отсутствовали, а
терять их дороже всего:** в них лежат ПЛАН и ОТЧЁТ исполнителей — единственная запись о том, что
уже сделано и какими числами это подтверждено.

⚠ Выгрузок транскрипта две, содержимое одинаковое (`-2` — повторный прогон при проверке гейта).
Коммить **обе**: удалять чужой файл — необратимая операция, а сэкономит она 474 КБ текста, который
никто не читает. Хочешь снести — назови это отдельной строкой отчёта и спроси, не делай молча.

Коммит — **только явными путями**, никогда `-A`. Два репозитория — две команды, пути не склеивать.

**КРИТЕРИЙ Э0:** `git --no-optional-locks status --porcelain -- _studio/` в `materials` печатает
пусто по всем перечисленным путям, и `git status --porcelain` в `disciplina` — тоже. Оба хэша
названы в отчёте. Не прошло по внешней причине — законный отчёт отдельной строкой, но не молчание.

---

### Э1. 🔴 ГЕЙТ ПОТЕРЬ ДАЁТ ДВА ПРОТИВОПОЛОЖНЫХ ВЕРДИКТА ОБ ОДНОЙ ВЕТКЕ

**Наблюдение, снятое в одной сессии 2026-08-08 с интервалом в минуты, без единой записи в
репозиторий между прогонами.** Команда одна и та же:
`python3 _generator/tools/git_zona.py doctor`.

**Прогон 1 напечатал:**
```
✅ Потерь нет: удаление любой ветки не унесёт содержимого — проверено 4 из 4 …
✅ Безопасны (4 из 4):
   · содержимое ДОЕХАЛО, хотя история не влита: zahod/solver-v3-dyhanie
```
**Прогон 2 напечатал:**
```
🔴 ПРОПАДЁТ РАБОТА — на 1 ветк(е/ах) из 4 лежит текст, которого здесь нет:
   · zahod/solver-v3-dyhanie — пропадёт файлов: 20 (нет здесь вовсе: 20 …);
     есть ТОЛЬКО на этой машине
```
Правым является ВТОРОЙ: проверено независимо, другим методом —
`git diff --name-status arka/mat-kostyak zahod/solver-v3-dyhanie` даёт 20 путей,
`_generator/tools/fixtures/sborka/obratnyj-progon/izmerit_dyhanie.py` в основной папке
отсутствует, `_generator/sborka/vmeshchenie.py` и `korpus.py` различаются. Прямой вызов движка
`branch_loss('zahod/solver-v3-dyhanie')` из питона тоже возвращает `verdict: "loss"` с теми же
20 путями.

🔴 **То есть движок считает верно, а `doctor` однажды напечатал обратное.** Это гейт, который
сторожит ПОТЕРЮ РАБОТЫ, и его зелёное уже уехало фактом в `PEREDACHA-2026-08-08.md §0`
(«обе ветки солвера влиты и запушены») — то есть в документ, который следующая сессия читает
первым и целиком.

**Задача.** Найти, почему вердикт непостоянен, и сделать его детерминированным. Гипотеза
аналитика — **не указание, а место, откуда дешевле начать**, и её надо либо подтвердить, либо
опровергнуть фактом: сопоставление ответов `batch_oids` с запросами позиционное
(`out[i]` против `queries[i]`), а `git cat-file --batch-check` при неудачном запросе может не
дать строки — тогда все последующие сравнения съезжают на одну позицию и произвольные пути
объявляются «доехавшими». Подтверждается это не рассуждением, а прогоном: скорми `batch_oids`
список, где часть путей заведомо отсутствует, и сравни `len(out)` с `len(queries)`.

⚠ **Гипотеза может оказаться ложной — тогда так и скажи в `## ПЛАН` и ищи дальше.** Второй
кандидат, который надо проверить прежде, чем чинить: `doctor` в первом прогоне НЕ напечатал
строку «ветка вычекаучена в рабочей папке», а во втором напечатал — значит `worktree_branches()`
в первом прогоне вернул другое. Это может указывать на то, что разошлось не сравнение блобов, а
чтение состояния репозитория целиком.

**КРИТЕРИЙ Э1 (может ПРОВАЛИТЬСЯ):**
1. причина названа **фактом, а не гипотезой** — командой, которая её показывает, с кодом возврата;
2. в `_generator/tools/fixtures/git_zona/` заведена фикстура, которая **воспроизводит ложно-зелёное
   ДО правки** (то есть на непочиненном коде падает) и зеленеет после;
3. `python3 _generator/tools/git_zona.py doctor`, запущенный **десять раз подряд**, печатает про
   `zahod/solver-v3-dyhanie` один и тот же вердикт — и это `🔴 ПРОПАДЁТ РАБОТА`. Команда и её
   вывод — в отчёт;
4. 🔴 **ветку `zahod/solver-v3-dyhanie` НЕ УДАЛЯТЬ и НЕ ВЛИВАТЬ** ни при каком вердикте. На ней
   лежит незаконченный заход, её принимает отдельный заход `priyomka-solvera-v3`.

---

### Э2. 🔴 РАЗРЫВ ЦИКЛА ФАЗЫ 1

**Предпосылка, записанная в `DOLG.md` Д33 и в `PEREDACHA §2`, — «bootstrap должен порождать лекцию
ИЗ `INTERVYU.md`». Аналитик считает её ЛОЖНОЙ и просит тебя проверить это по живым файлам первым
делом, до всякой правки.** Основание:

- по `SKILL.md` подфаза 5.1 пишет в `INTERVYU.md` **части лекции и сквозную арку**, а имена
  слайдов рождаются позже, в 5.2, и их адрес — **`slide_order` в `brief.md`**, не `INTERVYU.md`;
- значит на шаге 3 процедуры (порождение структуры скриптом) `INTERVYU.md` ещё пуст ровно так же,
  как пуст список имён. Научив скрипт его парсить, цикл не разорвёшь — только перенесёшь;
- вдобавок Д33 утверждает, что «формат документа интервью нигде не специфицирован». Проверено
  грепом: разделы `## Части` и `## Предполагаемые иллюстрации` названы в `SKILL.md` как адреса
  выходов 5.1 и 5.4, и верификатор `verifikatory/faza-1-intervyu.md` их грепает. Формат
  специфицирован частично, и его надо **дописать, а не сочинить с нуля**.

**Форма починки, которую предлагает аналитик — два хода вместо одного.** Согласен — делай; не
согласен — скажи в `## ПЛАН` с доказательством ДО работы:

1. **ход «каркас»** (шаг 3 процедуры, до разговора): скрипт порождает `brief.md` с анкетой
   лекции и **пустым `slide_order`**, и `INTERVYU.md` со скелетом разделов. Папок слайдов ещё нет —
   их и не может быть, имена не названы;
2. **ход «досыпать»** (после подфазы 5.2, когда владелец назвал и принял список): скрипт читает
   `slide_order` из уже заполненного `brief.md` и порождает **недостающие** папки слайдов.
   Идемпотентность по слайдам в скрипте уже есть — существующий `slaid.md` не перезаписывается.

`--slajdy` при этом **не удалять**: он остаётся законным входом для фикстур и для случая, когда
список известен заранее. Убрать рабочий флаг ради красоты — не задача этого захода.

**КРИТЕРИЙ Э2 (может ПРОВАЛИТЬСЯ):**
1. `python3 _generator/sborka/bootstrap_lekcii.py /tmp/proba-karkas` **отрабатывает с rc=0 без
   единого аргумента сверх пути** — сегодня падает на `--slajdy`. Команда и rc — в отчёт;
2. после него на диске лежат `brief.md` с пустым `slide_order` и `INTERVYU.md` со скелетом
   разделов; папки `slajdy/` либо нет, либо она пуста;
3. дописываешь в `/tmp/proba-karkas/brief.md` три имени в `slide_order`, гоняешь второй ход —
   появляются ровно три папки со всеми полями и пометкой «заполнить». Гоняешь его ВТОРОЙ раз —
   ничего не меняется и rc=0 (идемпотентность показана прогоном, не утверждением);
4. `python3 _generator/sborka/gejt_kartochki.py` на порождённом каркасе краснеет **на пустых полях
   анкеты, а не трейсбеком**. Краснеет трейсбеком — это долг Д38, чинит его заход З2: **не чини
   здесь**, назови строкой отчёта;
5. раздел «ПРОЦЕДУРА» фазы 1 в `SKILL.md` описывает оба хода и называет, между какими подфазами
   стоит второй. Расхождение скилла с живым скриптом — это и есть тот дефект, который чиним.

---

### Э3. ПЕЙСИНГ — ОДНА ФОРМУЛА ВМЕСТО ТРЁХ ОТВЕТОВ

Сегодня из живых документов на вопрос «сколько слайдов на 20 минут» получается три разных ответа:
формула речи (`минут × 110` слов, при корпусной медиане ~45 слов на слайд), корпусная цель на
слайд, и живой замер темпа (6–7 минут на слайд против плановых 3,75). Числа расходятся в разы,
и цена этого записана: **половина живой лекции не прозвучала.**

**Задача — НЕ выбрать одну из трёх, а развести их по назначению и оставить ОДНУ величину, которой
меряют объём.** Аналитик утверждает: правая величина — **замеренный темп минут-на-слайд**, потому
что она единственная снята с реальности, а не выведена; формула речи остаётся проверкой «влезает
ли материал в минуты» и в поле `byudzhet.slov_vsego` не пишется никогда (там слова НА СЛАЙДАХ —
величина другой природы, в живом примере расходится в 19 раз).

**КРИТЕРИЙ Э3 (может ПРОВАЛИТЬСЯ):**
1. в `references/byudzhet-obyoma.md` названо, какая из трёх величин нормативна для оценки числа
   слайдов, и **что делают с двумя другими** — каждая либо получает своё назначение, либо
   объявляется отменённой. «Обе полезны» ответом не считается;
2. одна команда, запущенная на карточке лекции, печатает прикидку числа слайдов — и на одном
   входе даёт ОДНО число. Команда и её вывод на живом примере — в отчёт;
3. 🔴 **пересчёт «речь → слайд» либо задан числом, либо явно объявлен незаданным** с указанием,
   что его закрывает живой замер на З6. Тихо оставить его неопределённым нельзя: именно на этом
   месте прикидка одной природы затирает величину другой.

---

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено
X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят
одинаково.

## 3. ВЕРИФИКАТОР

Э0 — операция сохраняющая, но **её провал молчалив**: незакоммиченный файл выглядит ровно как
закоммиченный, пока кто-то не откроет отдельную рабочую папку. Поэтому гейт:
**после коммита перечитай список путей из Э0 командой** `git --no-optional-locks log -1 --stat` в
каждом из двух репозиториев и сверь построчно с двумя списками выше. Расхождение — в отчёт.

Э1 — верификатор встроен в критерий (десять прогонов подряд + фикстура, падающая до правки).

Э2 — операция мутирующая по поведению скрипта, которым будет порождаться КАЖДАЯ лекция. Проверка —
живой прогон на `/tmp/proba-karkas`, не мок. 🔴 **`--slajdy`-путь обязан продолжать работать**:
прогони старую форму вызова с тремя именами и покажи, что результат прежний.

🔴 **Ни одна живая лекция в `materials/*/src/` не трогается — они READ-ONLY.** Все пробы — в `/tmp`.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
🔴 **Коммитов у тебя ЧЕТЫРЕ, и они не смешиваются.** Э0 даёт два (консолидация: `materials` +
`disciplina`), Э1–Э3 — ещё два (код в рабочей папке `materials` + правки скилла в `disciplina`).
Пути разных репозиториев **никогда не стоят в одной командной строке**.

Кодовый коммит в рабочей папке (`materials`):
```
git --no-optional-locks add -- _generator/tools/git_zona.py _generator/tools/fixtures/git_zona _generator/sborka/bootstrap_lekcii.py
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/git_zona.py _generator/tools/fixtures/git_zona _generator/sborka/bootstrap_lekcii.py
python3 _generator/tools/git_zona.py check --zone _generator   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                            # обязаны быть ТОЛЬКО твои пути
```
Коммит скилла — то же самое с `GIT_ZONA_REPO=~/Documents/GitHub/disciplina` и своими путями,
ОТДЕЛЬНОЙ командой.
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
