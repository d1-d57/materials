# Канал исполнителя — verstka-reshaetsya-v-faze-1 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — правила продиктованы владельцем дословно и приведены ниже целиком;
> твоя работа — записать их в дом и построить гейт. Изобретать доктрину НЕ надо.

КОНТЕКСТ. Фабрика научно-популярных лекций. Лекция 2 собрана целиком и **разобрана владельцем по
слайдам**. Большинство претензий оказались не про вёрстку и не про текст, а про **решения, которые
никто не принимал**: какой тип вёрстки у слайда, нужна ли ему иллюстрация вообще, есть ли заголовок,
существует ли у лекции обложка.

**Три вещи проверены командами по живой лекции, не предположены:**
```
grep -h "^tip_verstki:" slajdy/*/slaid.md | sort | uniq -c
   9 polosa_vertikalnaya · 3 polosa_gorizontalnaya · 2 tolko_tekst · 1 polnyj_ekran
   → обложки, визитки и финального слайда НЕТ НИ ОДНОГО

grep -h "^zagolovok_na_ekrane:" slajdy/*/slaid.md | sort | uniq -c
   15 zagolovok_na_ekrane: ""        → поле есть, не использовано ни разу

izomorfizm: tip_verstki polosa_vertikalnaya + illustracii: []
   → полоса выделена, класть в неё нечего; на экране пустая колонка
```

⚠ **Генератор ни при чём, и это важно для твоего плана.** Типы `oblozhka`/`vizitka`/`finalnyj`/
`razdelitel` есть в `REESTR` (`_generator/sborka/tipy.py`), а `deck.py` даже сортирует обложку
первой и финальный последним. **Механизм построен и просто не вызван, потому что слайдов такого
типа не завели.** Ровно тот класс, который фабрика ловит всю неделю: не «нет инструмента», а «нет
решения и нет гейта, который его требует».

Плюс: **5 слайдов из 15 физически обрезаны** при том, что ни один не превысил бюджет слов. Владелец
разобрал каждый и в половине случаев причина одна — **тип вёрстки выбран не под объём текста**
(мало текста в вертикальной полосе, много текста в горизонтальной).

ЦЕЛЬ, словами владельца: *«сделать так, чтобы в дальнейшем мы на стадии интервью обязательно
обсуждали эти вещи»*. Вёрстка, заголовок, нужность иллюстрации и обложка становятся **решением
фазы 1 с гейтом**, а не тем, что всплывает на собранной деке.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: ты НЕ правишь лекцию L2** — ни карточки, ни тексты, ни иллюстрации. Её будет
переделывать владелец в чате интервью, твоими правилами. Ты строишь правила и гейт.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/verstka-v-faze-1`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/verstka-v-faze-1`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add verstka-v-faze-1 --branch zahod/verstka-v-faze-1
  cd ../materials-wt/verstka-v-faze-1
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `disciplina/skills/slajdy/**` `_generator/tools/gejt_verstki.py` `_generator/tools/fixtures/verstki/**`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/verstka-v-faze-1`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `disciplina/skills/slajdy/**` `_generator/tools/gejt_verstki.py` `_generator/tools/fixtures/verstki/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 **§2 ЭТОГО ФАЙЛА, раздел «ДОКТРИНА»** | правила владельца дословно — главная спецификация |
| Я2 | `disciplina/skills/slajdy/SKILL.md`, разделы **ФАЗА 1** и **ФАЗА 2** | дом решений фаз |
| Я3 | `disciplina/skills/slajdy/references/kartochka-slajda.md` | дом формата карточки: поля `tip_verstki`, `zagolovok_na_ekrane`, `illustracii` |
| Я4 | `_generator/sborka/tipy.py` — **только `REESTR` и сигнатуры типов**; `deck.py:166-171` | какие типы существуют и как сортируется обложка. **Читаешь, НЕ правишь — чужая зона** |
| Я5 | `_generator/tools/gejt_illyustracij.py` | **образец гейта того же класса** в той же папке — пиши свой по его образцу, не изобретай форму |
| Я6 | `teorkat-vvedenie/L2/slajdy/*/slaid.md` — **ТОЛЬКО ЧТЕНИЕ**, полигон | 15 живых карточек: на них гейт обязан покраснеть в известных местах |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

# 🔴 ДОКТРИНА — продиктована владельцем, переносить ЦИТАТАМИ, не пересказом

## Д1. ВЫБОР ВЁРСТКИ — от объёма текста и от того, нужна ли иллюстрация

Дословно: *«Если текста очень много, то вообще не нужно никаких полос. Если там обязательно нужна
картинка, вынести её можно на отдельный слайд. Если текста среднее — полоса у нас всегда
вертикальная, только если там есть иллюстрация. Если там иллюстрация не нужна, то вообще не нужно
ничего. Если текста совсем мало, то полоса горизонтальная — опять-таки, если есть картинка. Ну а
если текста мало и картинки нет, тогда, наверное, надо объединить несколько слайдов в один»*.

| объём текста | иллюстрация нужна | тип вёрстки |
|---|---|---|
| **много** | — | `tolko_tekst`. Картинка обязательна → она уезжает **на отдельный слайд** |
| **среднее** | да | `polosa_vertikalnaya` |
| **среднее** | нет | `tolko_tekst` — полосы нет вовсе |
| **мало** | да | `polosa_gorizontalnaya` |
| **мало** | нет | 🔴 **объединить с соседним слайдом** |

⚠ **Границы «много / среднее / мало» в строках — НЕ выдумывай.** Их даст `smeta.py` соседнего
захода `svedenie-i-smeta`; сейчас запиши правило качественно и **назови место, куда встанут числа**.
Вписать выдуманный порог хуже, чем оставить пустое место с адресом.

## Д2. 🔴 ПОЛОСА БЕЗ ИЛЛЮСТРАЦИИ — ЗАПРЕЩЕНА

Дословно: *«здесь пустая правая полоса — зачем? Тоже должно быть очевидное правило: если мы делаем
вертикальную полосу или горизонтальную, там должна быть иллюстрация. Если её нет — тогда не нужно,
тогда здесь нужно сделать другой тип вёрстки»*.
⇒ `tip_verstki` начинается на `polosa_` И `illustracii: []` → **КРАСНЫЙ**. Живой случай — `izomorfizm`.

## Д3. ИЛЛЮСТРАЦИЯ ОБЯЗАНА ЧТО-ТО ИЛЛЮСТРИРОВАТЬ

Владелец снял иллюстрации на нескольких слайдах со словами *«она совершенно не нужна и ничего не
иллюстрирует»*, *«картинка здесь какая-то бессмысленная, вообще непонятно, что имеется в виду»*.
Плюс важное признание: *«описание картинок я ни разу не читал подробно»* — то есть заказы
иллюстраций утверждались не глядя.
⇒ **Механическим гейтом не проверяется** — это вкус. Но обязано быть **явным пунктом фазы 1**:
у каждой иллюстрации спрашивается «что именно она показывает», и ответ предъявляется владельцу
**в ленте, а не только в `zakaz.md`**. Владелец: *«может быть, это должно было быть в ленте
как-то лучше обозначено»*.
⚠ **Первый ход при «не влезает» — не сжимать текст, а спросить, нужна ли тут картинка вообще.**
Лишняя иллюстрация это ещё и −50 % места под текст.

## Д4. 🔴 ОБЛОЖКА, ВИЗИТКА, ФИНАЛЬНЫЙ — ВСТАВЛЯЕТ ГЕНЕРАТОР, А НЕ АВТОР

🔴 **Владелец поправил формулировку отдельно, это не мелочь — это смена уровня, на котором живёт
решение.** Дословно: *«обложка обязательна так же, как и последний слайд, и визитка. Мы пытались
это встроить в генератор, чтобы автоматически, когда ты с помощью него собираешь презентацию, он бы
вставлял эти слайды. И только если ты где-то отключишь, например, визитку, то можно это убрать. Всё
остальное обязательно должно быть — и обложка, и первый слайд, и последний. Это просто обязательные
вещи. Это должно быть на уровне генератора»*.

⇒ **Правило, а не пожелание:**
- `deck.py` **вставляет обложку, визитку и финальный слайд САМ**, без участия автора. Отсутствие
  их в `slide_order` — норма, а не ошибка;
- **отключение — явный флаг в `brief.md`** (например `bez_vizitki: true`), и только он. Молчание
  означает «вставить»;
- 🔴 **у обложки отключения нет вовсе** — она безусловна.

⚠ **РЕАЛИЗАЦИЯ НЕ ТВОЯ, И ЭТО НАМЕРЕННО.** `_generator/sborka/**` — зона идущего рядом захода
`svedenie-i-smeta`, туда нельзя ни байта. **Твоя работа здесь — написать СПЕЦИФИКАЦИЮ** так, чтобы
следующий заход реализовал её без второго круга вопросов: какие поля, какое поведение по умолчанию,
какой флаг отключения, что делать, если автор всё-таки завёл такой слайд руками. Спека — в
`SKILL.md` и `kartochka-slajda.md`, а **сам пункт «реализовать в `deck.py`» отдельной строкой в
`## ВОПРОСЫ`** как пункт очереди с домом.

⚠ Идея владельца, записать как решение: *«коммутативную диаграмму с первого слайда можно поместить
на обложку — написать „функторы“ и нарисовать коммутативную диаграмму, это будет красиво. Но она не
нужна там, где много текста»*.

## Д5. ЗАГОЛОВОК СЛАЙДА — ПЕРЕКЛЮЧАЕМЫЙ, И ПЕРЕКЛЮЧАТЕЛЬ В ТОМ ЧИСЛЕ ПРО МЕСТО

Дословно: *«у них название есть, почему бы не ставить заголовок? Заголовок всегда полезен и что-то
позволяет сформулировать, о чём идёт речь»*. И отдельно: *«дополнительный элемент — что заголовки
можно включать или отключать. Мы можем придумывать их или не выставлять, в том числе исходя из
соображений места»*.
⇒ Заголовок — **сознательный переключатель, а не забытое поле**: включён по умолчанию, снимается
осознанно, и **нехватка места — законная причина снять**. Сегодня `zagolovok_na_ekrane: ""` на
15 из 15 — то есть переключателем никто ни разу не воспользовался, он просто не существовал в
разговоре.

## Д6. 🔴 ВЁРСТКА КАЖДОГО СЛАЙДА ОБСУЖДАЕТСЯ И ОБОСНОВЫВАЕТСЯ — НАРАВНЕ СО ВСЕМ ОСТАЛЬНЫМ

Дословно: *«должно быть очень жёстко понятно про каждый слайд — вёрстка, и она должна быть
обсуждена точно так же, как всё остальное. И должно быть обоснование»*. Его же примеры того, как
звучит обоснование — **перенести их как образцы, они задают жанр**:

> *«здесь вёрстка такая, потому что здесь мало текста и можно поставить большую красивую картинку —
> значит горизонтальная полоса. А вот здесь нам нужны две иллюстрации, но текста много: иллюстрации
> сделаем поменьше, вертикальная полоса. А здесь иллюстрации нет и текста много — сделаем текст во
> весь слайд. А вот здесь и текста мало, и иллюстрация мелкая — так может тогда объединим два
> слайда просто и всё? Или увеличим кегль»*.

⇒ **Обоснование вёрстки — обязательный выход фазы, наравне с `centralnyj_blok` и `zachem`.** Не
«тип проставлен», а «тип проставлен и сказано, почему именно он». 🔴 **Реши и обоснуй в `## ПЛАН`:
новое поле карточки или расширение существующего** — но решение назови, а не оставляй на потом.
⚠ Сегодня обоснования нет ни у одного из 15 слайдов, поэтому и вышло 9 вертикальных полос подряд
«по умолчанию» — включая слайды, где текста мало, а полоса пустует.

---

# ЗАДАЧА — четыре шага

**В1. Сохранить помарки владельца дословно.** Завести
`_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-SBORKA-L2.md` и перенести туда **раздел
ДОКТРИНА целиком, цитатами**, плюс пометку, что разбор по отдельным слайдам владелец повезёт в чат
интервью сам. 🔴 Зарегистрировать `register_doc.py` тем же ходом.
⚠ Это первый шаг не случайно: **пока правила живут только в файле-заходе, они умрут вместе с ним.**

**В2. Доктрина — в дисциплину.** Д1–Д6 в `SKILL.md` (решения фазы) и `kartochka-slajda.md`
(поля). Цитаты владельца сохранять; правило без цитаты теряет «почему» и переизобретается.
🔴 **Д4 переносится как СПЕКА ДЛЯ ГЕНЕРАТОРА** (поля, поведение по умолчанию, флаг отключения,
что делать при заведённом руками слайде), а не как требование к автору.

**В3. `_generator/tools/gejt_verstki.py`** — по образцу Я5, тот же класс и та же папка:
- Д2: `polosa_*` при пустых `illustracii` → красный, поимённо;
- Д6: тип вёрстки без обоснования → красный (поле выбираешь в `## ПЛАН`);
- Д5: пустой `zagolovok_na_ekrane` **без пометки «снят осознанно»** → предупреждение с числом
  «пусто на N из M»;
- Д1: **пока НЕ гейтится** (нет чисел) — но печатается справкой: тип вёрстки каждого слайда рядом
  с числом блоков, чтобы расхождение бросалось в глаза;
- 🔴 **Д4 гейтом НЕ проверять вовсе.** Раз обложку вставляет генератор, требовать её от автора —
  значит краснеть на здоровой лекции. Проверять там будет нечего: это станет свойством сборки.
🔴 **Живая точка вызова обязательна** — иначе это девятый написанный и не подключённый инструмент.
Маркер, который понимает `check_tool_contract.has_live_trigger`, плюс строка в блок «ЧТО ДАЛЬШЕ»
владелец-заход `svedenie-i-smeta` трогает `bootstrap_lekcii.py` — **туда не лезь, чужая зона**:
назови нужную строку в отчёте.

**В4. Фикстуры** `_generator/tools/fixtures/verstki/PROGNAT.sh` — по образцу соседних: полоса без
иллюстрации (красный) · полоса с иллюстрацией (зелёный) · лекция без обложки (красный) · лекция с
обложкой (зелёный).

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. 🔴 **Живой прогон на реальном объекте:** `python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2`
   (**только чтение**) → красный **ровно на известном**: `izomorfizm` (Д2) и 15 из 15 без
   обоснования вёрстки (Д6); предупреждение «заголовок пуст на 15 из 15». **Ни одного ложного
   красного.** Полный вывод и rc — в отчёт.
2. `bash _generator/tools/fixtures/verstki/PROGNAT.sh` → зелёный, четыре ловушки минимум.
3. `check_tool_contract.py` не считает `gejt_verstki.py` неподключённым. Команда и вывод в отчёт.
4. `POMARKI-SBORKA-L2.md` существует, зарегистрирован (`grep -c` по `KARTA.md` → 1), Д1–Д6 внутри
   цитатами. Д1–Д6 найдены грепом в `SKILL.md`/`kartochka-slajda.md` — поимённо, шесть из шести.
5. 🔴 **Спека Д4 проверяется чтением, а не гейтом:** свежий субагент, видевший ТОЛЬКО дисциплину,
   отвечает на вопрос *«кто и когда вставляет обложку, визитку и финальный слайд, и как отключить
   визитку?»* — обязан назвать генератор, флаг и безусловность обложки. Вопрос и ответ в отчёт.
6. Оба репозитория закоммичены, `git show --stat` без чужих путей.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «правила записаны», а «Д из 6 перенесено,
проверок в гейте N из 4, ловушек K».

## 3. ВЕРИФИКАТОР

🔴 **Главный риск — гейт, который краснеет на здоровом.** Мы это уже получили сегодня: ужесточение
`--faza 1` дало два ложных красных из трёх, и такой гейт отключают, а не чинят.

- 🔴 **Прогнать `gejt_verstki.py` на ДРУГИХ живых лекциях** (`buffon/src`, `fibonacci/src`,
  `teorkat-vvedenie/src` — **только чтение**). Красное там — назвать и **разобрать, законно ли оно**.
  Незаконное красное на чужой лекции = гейт не готов, чинить до сдачи.
- **Свежий субагент сверяет Д1–Д5** в дисциплине с §2 этого файла: цитата на месте или пересказана?
  Пересказ считается потерей — «почему» переизобретается вместе с правилом.
- **Ничего не потеряно:** `git show --stat` по обоим коммитам — только свои пути.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `disciplina/skills/slajdy/**` `_generator/tools/gejt_verstki.py` `_generator/tools/fixtures/verstki/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `disciplina/skills/slajdy/**` `_generator/tools/gejt_verstki.py` `_generator/tools/fixtures/verstki/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### Оспариваю две предпосылки — командами, ДО работы

**П1. Я2/Я3 не существуют нигде.** `disciplina/skills/slajdy/SKILL.md` и
`.../references/kartochka-slajda.md` отсутствуют не только в рабочей папке, но и во ВСЕЙ истории
git по всем веткам:
```
find . -iname "SKILL.md" -o -iname "*kartochka-slajda*"           → пусто
git log --all --diff-filter=A --name-only | grep "kartochka-slajda\|skills/slajdy"  → пусто
```
Ни одна ветка (включая уже влитый `zahod/kartochka-i-sborka`, b5f5527) их не создавала. При этом
`_generator/sborka/formaty.py` (докстринг) и `gejt_illyustracij.py` (Я5, строка 21) уже ССЫЛАЮТСЯ на
`kartochka-slajda.md` как на «канон» — обещание было дано раньше, чем выполнено. **Решение: не жду
второго круга — завожу оба файла с нуля, ровно в объёме, который называет заход** (SKILL.md: секции
ФАЗА 1/ФАЗА 2 со скелетом + «РЕШЕНИЯ ФАЗЫ» под Д1–Д6; kartochka-slajda.md: только три поля, названные
в Я3, — `tip_verstki`/`zagolovok_na_ekrane`/`illustracii` — плюс два новых поля из П2 ниже. Полную
спецификацию остальных 15 полей карточки (`matematika_iz`, `opiraetsya_na` и т.д.) НЕ пишу — это
не моя задача и не мой зона).

**П2. `check_tool_contract.has_live_trigger` (В3) в моей ветке не существует.** Грепом по всему
`_generator/`: ноль вхождений. Причина найдена: механизм живёт в `zahod/instrument-podklyuchen`
(коммит `a475920`, «контракт живой точки вызова»), а он НЕ влит в `origin/main` и НЕ является предком
моей ветки (`git merge-base --is-ancestor` → нет). Это соседний заход того же журнала
(`kod_instrument-podklyuchen.md`), ещё не сведённый. Но в моей ветке ЕСТЬ работающий механизм ТОГО ЖЕ
класса — маркер `# TOOL-CONTRACT-COVERS: <файл>.py` в шапке `PROGNAT.sh`, который читает
`fixture_coverage()` (`check_tool_contract.py:209-226`) и которым УЖЕ пользуется сам образец Я5
(`fixtures/illyustracii/PROGNAT.sh:2`). **Решение: подключаюсь этим маркером** — это и есть «живая
точка вызова» в терминах кода, который реально лежит в моей ветке; расхождение в имени
(`has_live_trigger` vs `TOOL-CONTRACT-COVERS`) называю в отчёте как открытый пункт для свода веток.

### П3. Новое поле карточки для Д6 (обоснование вёрстки)

Ввожу `obosnovanie_verstki` (строка, обязательна при ЛЮБОМ `tip_verstki` — Д6 дословно «про каждый
слайд», не только про полосы). Имя — в одном словаре с `tip_verstki`/`liniya`, находится тем же
грепом. Гейт: пусто/отсутствует → красный, поимённо.

### П4. Новый переключатель для Д5 (заголовок снят осознанно)

Ввожу `zagolovok_snyat_namerenno: da` — по образцу УЖЕ существующего в формате карточки поля
`bez_opredeleniya_namerenno` (тот же приём «намеренно» как явный флаг снятия дефолта, а не
случайная пустота). Гейт: `zagolovok_na_ekrane` пусто И флага нет → предупреждение (не красный),
со счётом «пусто на N из M».

### П5. Разночтение В3 vs В4 по Д4 — оставляю В3 в силе

В3 требует «Д4 гейтом НЕ проверять вовсе» (🔴, с обоснованием: обложку вставляет генератор, автору
её требовать — красный на здоровой лекции). В4 в списке ловушек называет «лекция без обложки
(красный) · лекция с обложкой (зелёный)» — это прямое противоречие. БеруB3 как решающий (он
подробнее, помечен 🔴, и совпадает с Д4 доктрины дословно), а требование «минимум 4 ловушки» В4
выполняю ловушками по Д2 и Д6 (по два зелёных/красных случая каждая = 4). Дополнительно завожу
5-ю: «лекция без обложки → ЗЕЛЁНЫЙ (Д4 сознательно не гейтится)» — она и удовлетворяет букву В4
(проверка присутствует), и служит регрессионной защитой РОВНО от того, что В4 просит по ошибке.

### П6. Гейт `gejt_verstki.py` — по образцу Я5, своя мини-разборка шапки (не импортирую formaty.py — чужая зона)
- красное (rc=1): Д2 (`tip_verstki` начинается на `polosa_` и `illustracii: []`); Д6 (`tip_verstki`
  есть, `obosnovanie_verstki` пусто/отсутствует)
- предупреждение (rc не меняет): Д5 (`zagolovok_na_ekrane` пусто без `zagolovok_snyat_namerenno`) —
  печатается счётом «пусто на N из M»
- справка (всегда, не гейтится): Д1 — тип вёрстки каждого слайда рядом с числом блоков раздела
  «Текст слайда — сжато» (`###`-заголовков) — то, что реально уходит на экран
- Д4: не проверяется вовсе (П5)
- несуществующая папка лекции → rc=2, как в Я5 (кривой вход ≠ «лекция без слайдов»)

### П7. Фикстуры — 5 ловушек (П5)
`_generator/tools/fixtures/verstki/PROGNAT.sh` с `# TOOL-CONTRACT-COVERS: gejt_verstki.py`:
1. полоса + `illustracii: []` → красный (Д2) 2. полоса + illustracii → зелёный (Д2)
3. `tip_verstki` без `obosnovanie_verstki` → красный (Д6) 4. с `obosnovanie_verstki` → зелёный (Д6)
5. лекция без `oblozhka` среди типов → зелёный (Д4 не гейтится, П5)

### Порядок действий
В1 (POMARKI + регистрация) → П3/П4 решения вписываю в SKILL.md/kartochka-slajda.md вместе с В2 →
В3 (gejt_verstki.py) → В4 (фикстуры) → живой прогон на `teorkat-vvedenie/L2` и трёх чужих лекциях →
верификация (свежий субагент по Д4 и по цитатам Д1–Д5) → коммит зоны → отчёт.


## ВОПРОСЫ — (заполняет исполнитель)

1. Реализовать Д4 в `deck.py`: вставка `oblozhka`/`vizitka`/`finalnyj` генератором, флаг
   `bez_vizitki: true` в `brief.md`, у обложки флага нет вовсе, авторский слайд того же типа не
   задваивается. Полная спека — `disciplina/skills/slajdy/SKILL.md`, раздел «СПЕКА ДЛЯ ГЕНЕРАТОРА».
   ДОМ: владелец (реализация — зона захода `svedenie-i-smeta`, не моя)
   ДОСТАВЛЕНО: нет

2. Живая точка вызова `gejt_verstki.py` в lifecycle-блоке карточки (`bootstrap_lekcii.py`, чужая
   зона) — нужная строка: к ФАЗЕ 2 добавить `&& python3 _generator/tools/gejt_verstki.py <лекция>`
   (после существующего `gejt_kartochki.py --faza 2 <лекция>`), так как Д2/Д6 решаются ровно там же,
   где `tip_verstki`.
   ДОМ: `_generator/tools/bootstrap_lekcii.py` (зона захода `svedenie-i-smeta`)
   ДОСТАВЛЕНО: нет

3. Границы «много / среднее / мало» (Д1) в словах текста — сейчас качественное правило и справка
   без чисел (`gejt_verstki.py` печатает тип + число блоков рядом, не гейтит). Числа даст
   `smeta.py`.
   ДОМ: владелец (заход `svedenie-i-smeta`)
   ДОСТАВЛЕНО: нет

4. Разночтение имени «живой точки вызова»: этот заход ссылался на
   `check_tool_contract.has_live_trigger` (В3), но в моей ветке такого имени нет нигде в
   `_generator/` — механизм с этим именем лежит на невлитом соседнем заходе
   `zahod/instrument-podklyuchen` (коммит `a475920`, «контракт живой точки вызова»), который не
   является предком моей ветки. Я подключился РАБОТАЮЩИМ в моей ветке механизмом того же класса —
   маркером `# TOOL-CONTRACT-COVERS: gejt_verstki.py` (`check_tool_contract.py:209-226`,
   уже используется соседним гейтом Я5). При сведении веток нужно свериться: не задумал ли
   `instrument-podklyuchen` что-то иное под именем `has_live_trigger`, что должно применяться и к
   `gejt_verstki.py`.
   ДОМ: владелец (свод веток `instrument-podklyuchen` × `verstka-v-faze-1`)
   ДОСТАВЛЕНО: нет

5. Урок фабрике — заход ссылался (Я2, Я3) на `disciplina/skills/slajdy/SKILL.md` и
   `kartochka-slajda.md` как на существующие «дома решений», хотя эти пути не существовали НИ В
   ОДНОЙ ветке репозитория (проверено `git log --all --diff-filter=A` по обоим путям — пусто).
   Подробности и цена — см. `## ПЛАН` этого файла, П1.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

6. Урок фабрике — внутри одного и того же захода В3 (🔴 «Д4 гейтом НЕ проверять вовсе») и В4
   («лекция без обложки (красный)» в списке ловушек) требовали противоположного поведения от
   одного и того же гейта. Подробности и разрешение — `## ПЛАН`, П5.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**ЧТО СДЕЛАНО И ЗАЧЕМ.** Заход строил не правку лекции, а гейт и дисциплину, которые заставляют
принимать решения о вёрстке в фазах 1–2, а не после сборки (цель владельца — доктрина обсуждается
на интервью, не всплывает на собранной деке).
- В1: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-SBORKA-L2.md` — доктрина Д1–Д6
  цитатами, зарегистрирован (`register_doc.py`, rc=0, строка в `KARTA.md:412`).
- В2: `disciplina/skills/slajdy/SKILL.md` и `.../references/kartochka-slajda.md` — **заведены с
  нуля**, а не дополнены (см. «ложная предпосылка» ниже). SKILL.md: ФАЗА 1 (Д3, Д5) и ФАЗА 2 (Д1,
  Д2, Д6 + формат `zakaz.md` п.4, на который уже ссылался живой `gejt_illyustracij.py`) и отдельная
  «СПЕКА ДЛЯ ГЕНЕРАТОРА» (Д4, только текст, без кода — реализация не моя). kartochka-slajda.md:
  три поля Я3 (`tip_verstki`/`zagolovok_na_ekrane`/`illustracii`) + два новых поля, решённых в
  `## ПЛАН` (П3, П4): `obosnovanie_verstki` (Д6) и `zagolovok_snyat_namerenno` (Д5, по образцу
  уже существующего `bez_opredeleniya_namerenno`).
- В3: `_generator/tools/gejt_verstki.py`, по образцу `gejt_illyustracij.py` — свой парсер шапки
  (не импортирует `formaty.py`, чужая зона). Красное: Д2, Д6. Предупреждение (не красное): Д5.
  Справка (не гейтится): Д1. Д4 не проверяется нигде в инструменте (см. «разночтение В3/В4» ниже).
- В4: `_generator/tools/fixtures/verstki/PROGNAT.sh`, маркер `# TOOL-CONTRACT-COVERS:
  gejt_verstki.py` (тот же механизм, что у Я5 — см. ниже про `has_live_trigger`), 5 живых ловушек +
  2 кривых входа.

**ЛОЖНАЯ ПРЕДПОСЫЛКА 1 (Я2/Я3).** `disciplina/skills/slajdy/SKILL.md` и `kartochka-slajda.md`,
названные заходом как существующие «дома решений», не существовали НИ В ОДНОЙ ветке репозитория
(`find`, `git log --all --diff-filter=A` по обоим путям — пусто; проверено и на уже влитом
`zahod/kartochka-i-sborka`). При этом `formaty.py` и `gejt_illyustracij.py` уже ссылались на
`kartochka-slajda.md` как на канон — обещание было дано раньше, чем выполнено. Разобрано в
`## ПЛАН`, П1, командами. Решение: завёл файлы с нуля, ровно в объёме, который называет заход —
не всю карточку целиком (15 полей карточки формально этим файлом не специфицированы, только три
из Я3 плюс два новых).

**ЛОЖНАЯ ПРЕДПОСЫЛКА 2 (`has_live_trigger`).** В3 требовал маркер, понятный
`check_tool_contract.has_live_trigger` — такого имени нет нигде в `_generator/` моей ветки (грепом).
Причина: механизм с этим именем лежит на невлитом соседнем `zahod/instrument-podklyuchen`, не
являющемся предком моей ветки. Подключился РАБОТАЮЩИМ механизмом того же класса —
`# TOOL-CONTRACT-COVERS`, уже применяемым Я5 (`fixtures/illyustracii/PROGNAT.sh:2`,
`check_tool_contract.py:209-226`). Названо пунктом очереди (`## ВОПРОСЫ` №4) для свода веток.

**ПРОТИВОРЕЧИЕ В3/В4.** В3 (🔴): «Д4 гейтом НЕ проверять вовсе». В4: ловушка «лекция без обложки
(красный)». Прямое противоречие внутри одного захода. Взял В3 решающим (подробнее, помечен 🔴,
дословно совпадает с самой доктриной Д4) — `## ПЛАН`, П5. Ловушка 5 фикстур это фиксирует
регрессионно: лекция без `oblozhka` → ЗЕЛЁНЫЙ, с явным комментарием почему.

**КАК ПРОВЕРИЛ.**
1. Живой прогон `python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2` → rc=1, красное
   РОВНО на `izomorfizm` (Д2, 1 замечание) и 15 из 15 без `obosnovanie_verstki` (Д6, 15 замечаний,
   16 итого) — точное совпадение с предсказанием критерия готовности. Предупреждение «Д5: … пусто
   на 15 из 15» напечатано. Ложных красных нет.
2. `bash _generator/tools/fixtures/verstki/PROGNAT.sh` → `ФИКСТУРЫ ЗЕЛЁНЫЕ`, rc=0, 7 проверок (5
   живых ловушек + 2 кривых входа).
3. `python3 _generator/tools/check_tool_contract.py _generator/tools/gejt_verstki.py` →
   `✅ gejt_verstki.py` / `✅ Контракт держится: проверено 1 из 1.`, rc=0 — не считает инструмент
   неподключённым.
4. Верификатор — прогон на ДРУГИХ живых лекциях (`buffon/src`, `fibonacci/src`,
   `teorkat-vvedenie/src`, только чтение): все три rc=0, но **вхолостую** — ни у одной нет
   `slajdy/*/slaid.md` (`find . -iname slajdy` по всему репозиторию, не считая фикстур и `L2`, дал
   ровно ОДИН результат: `teorkat-vvedenie/L2`). Зелёный там — не доказательство отсутствия ложных
   красных, а отсутствие материала для проверки; честно называю это, не выдаю за живую сверку.
   `teorkat-vvedenie/L2` — единственный реальный полигон, и на нём совпадение точное (п.1).
5. Свежий субагент (без контекста этой сессии), прочитавший ТОЛЬКО `SKILL.md`, ответил на «кто и
   когда вставляет обложку/визитку/финальный слайд и как отключить визитку» — верно назвал `deck.py`
   как исполнителя, `bez_vizitki: true` как флаг, безусловность обложки. Полный Q&A — в истории
   субагента, вердикт: спека самодостаточна.
6. Свежий субагент сверил цитаты Д1–Д5 в источнике (§2 этого файла) против трёх переносов
   (`SKILL.md`, `kartochka-slajda.md`, `POMARKI-SBORKA-L2.md`) — все пять на месте дословно, потерь
   не найдено (`kartochka-slajda.md` цитат не содержит по замыслу — формат полей, не решения; охват
   закрыт `SKILL.md` + `POMARKI`).
7. Регистрация: `grep -c "POMARKI-SBORKA-L2.md" _studio/docs/KARTA.md` → 1. Д1–Д6 поимённо (после
   правки — добавил явные метки `(ДN)`, изначально Д1 и Д3 не находились грепом отдельным токеном):
   `grep -oh "Д[1-6]" disciplina/skills/slajdy/SKILL.md .../kartochka-slajda.md | sort -u` → все
   шесть.

**ЧТО НЕ ТРОГАЛ.** `_generator/sborka/**` (`tipy.py`, `deck.py`, `formaty.py` — только читал, Д4 не
реализовывал), `bootstrap_lekcii.py` (не тронут — нужная строка названа в `## ВОПРОСЫ` №2, не
вписана), `_generator/tools/check_tool_contract.py` (не редактировал, работающий там маркер только
использовал), содержимое `teorkat-vvedenie/L2/slajdy/**` (только читал командами — саму лекцию не
правил, это стоп-условие захода), `smeta.py` не существует и не заводил (границы Д1 оставлены
пустым местом с адресом, как требовал заход).

**РЕЗУЛЬТАТ ВЕРИФИКАТОРА.** Красных на чужих живых лекциях не найдено (но проверка вхолостую, см.
п.4 выше — честно назвал). Цитаты Д1–Д5 дословны (п.6). `git show --stat` — только свои пути (ниже).

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** неприменимо — канал `app`, снимать неоткуда (лог существует только на
канале `terminal`, где его кладёт `tee`).

**ПОВТОРЯЕМОСТЬ НАХОДОК.** Обе ложные предпосылки (Я2/Я3, `has_live_trigger`) и противоречие В3/В4
— находки ПРО ЭТОТ ЗАХОД, не про лекцию/слайд: они уже разрешены моим же бутстрапом файлов и
явным решением в `## ПЛАН`, поэтому на следующей единице работы (следующая лекция, следующий заход)
в ЭТОЙ ЖЕ форме не повторятся — законно уходят в `## ВОПРОСЫ` пунктом очереди (сделано, №4–6), а не
требуют захода ДО следующего прогона. Единственное, что действительно повторится на КАЖДОЙ будущей
лекции, переведённой на формат `slajdy/slaid.md`, — это сам Д6: `gejt_verstki.py` будет красным,
пока `obosnovanie_verstki` не заполнено у каждого слайда. Это ЗАДУМАННОЕ поведение гейта (та самая
цель захода), не дефект, повторение здесь — часть работы, не находка.

**НЕОБРАТИМОЕ:** необратимого нет. Все действия — новые файлы (add, не перезапись) + одна
строка-регистрация в `_studio/docs/KARTA.md` (через `register_doc.py`) + один коммит (§4 захода
требует его явно). `git reset`/`checkout` не выполнялись, ничего чужого не тронуто.

**АРТЕФАКТ:** артефакта нет — заход про код и гейт (заведён инструмент, не собран документ/дека).
Ближайший эквивалент — живой вывод гейта, воспроизводится командой
`python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2` (только чтение, полный вывод — в
п.1 «КАК ПРОВЕРИЛ» выше).

**КОММИТ:** `77630e44a838ceede4293f3c74cd81caca29913d` —
«slajdy: гейт вёрстки (Д1-Д6) — вёрстка и обоснование решаются в фазе 1-2, не после сборки» ·
`git_zona.py check --zone disciplina/skills/slajdy` → ✅ ·
`git_zona.py check --zone _generator/tools/gejt_verstki.py` → ✅ ·
`git_zona.py check --zone _generator/tools/fixtures/verstki` → ✅ ·
`git_zona.py check --zone _studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-SBORKA-L2.md` → ✅ ·
`git show --stat` — 11 файлов, все внутри объявленной зоны + `_studio/docs/KARTA.md`
(регистрация, обязательна тем же ходом по контракту зоны). Ветку в `arka/mat-kostyak` НЕ вливал —
не моя часть приёмки.
