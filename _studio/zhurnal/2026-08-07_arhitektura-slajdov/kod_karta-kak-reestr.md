# Канал исполнителя — karta-kak-reestr (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — Ш1 и Ш4 механические, а Ш2 (превратить карту в реестр) и Ш3 (гейт
> согласованности) требуют решений по ходу: что считать рассогласованием, а что законной пустотой.

КОНТЕКСТ. Фабрика научно-популярных лекций. **Ты делаешь так, чтобы после закрытия сессии можно было
сказать про инструменты фабрики что-то проверяемое, а не вспоминать.**

Требование владельца дословно: *«у нас должна быть карта, в которой всё актуальное — какие
инструменты есть, какие работают, что они все подключены, что они соединены друг с другом. И это
должно быть прописано так, чтобы оно краснело, если вдруг что-то изменится и оно перестанет быть
согласовано»*. И отдельно: *«чем меньше документов, тем лучше»* — **новых файлов не заводить**,
реестр живёт внутри уже существующей карты.

**Почему текстовой карты не хватает — доказано на ней самой.** `KARTA-ZHIVOGO-KONVEJERA.md` написана
вчера, и в ней стояло «ГЕЙТ ВМЕЩЕНИЯ — не существует, долг», при том что `gejt_vmeshcheniya.py`
лежал рядом, написанный и работающий. **Карта прочитала документацию вместо диска** — ровно тот
грех, ради разоблачения которого писалась. Исправлено вчера руками; сегодня надо сделать так,
чтобы такое краснело само.

**Состояние инструментов — замер, а не оценка:**
```
python3 _generator/tools/check_tool_contract.py _generator/tools/*.py _generator/sborka/*.py
    инструментов        51
    нарушают контракт    3   — lenta.py · slaid.py · vmeshchenie.py
    класс нарушения: «кривой вход» (нет спутник-фикстуры), НЕ «не вызывается»
```
⚠ **Мёртвых инструментов у фабрики нет** — это проверено, и владелец спрашивал прямо. Из семи
недавно найденных «без вызова» **трое были ложными** (импортируются соседями, а охват проверки не
смотрел в подпапку). Удалять нечего.

🔴 **А вот описательный слой мёртв целиком, и это настоящая потеря:**
```
_studio/konvejer/ — 17 гейтов + 11 стадий, разметка в KARTA-ZHIVOGO-KONVEJERA.md:
    ПЕРЕЕХАЛ  33 · АРХИВ 5 · ЖИВ 1
```

ЦЕЛЬ: карта — **проверяемый реестр**, гейт краснеет на рассогласовании, мёртвый слой помечен архивом.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: гейт, который краснеет всегда или не краснеет никогда, — провал захода, а не
частичный успех.** Мы дважды получали механизм, который отключают: `--no-verify` (28 обходов из 56
срывов коммита) и ложные FAIL трекера.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/karta-reestr`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/karta-kak-reestr`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add karta-reestr --branch zahod/karta-kak-reestr
  cd ../materials-wt/karta-reestr
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/**` `_studio/konvejer/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ВЛИТИЕ — ДО ВСЕГО ОСТАЛЬНОГО

```
python3 _generator/tools/git_zona.py merge zahod/gigiena-i-svedenie --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _fond --zone obzory --zone ucheniki
```

🔴 **Зона слияния — НЕ рабочая зона захода, а ВСЁ, что трогает вливаемая ветка** (проверено при
сборке: 66 путей вне рабочей зоны). Узкая зона → инструмент откажется слить что-либо; на этом
вставали три захода подряд. Отказал — **возьми строку зон из его собственного отказа**.

⚠ **Известно заранее, не пугайся:** прошлый заход получил на этом же слиянии конфликт в двух путях
и `UnicodeDecodeError` в `merge --continue` (хук пишет в тот же пайп десятками процессов). Дефект
починен (`errors="replace"`), но если повторится — доводи `git --no-optional-locks commit --no-edit`
и **скажи об этом в отчёте**, а не молчи.

Проверка фактом:
```
git --no-optional-locks merge-base --is-ancestor zahod/gigiena-i-svedenie HEAD ; echo $?
```
Ветки НЕ удаляй ни при каком исходе.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/karta-kak-reestr`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/**` `_studio/konvejer/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 `_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` **целиком** | **предмет главной работы** — её ты превращаешь в реестр, а не пишешь заново |
| Я2 | `_generator/tools/check_tool_contract.py` | **уже умеет** судить «есть живая точка вызова» и «отвергает кривой вход». Гейт согласованности обязан ЗВАТЬ его, а не повторять |
| Я3 | `_generator/tools/check_sborki.py` — **как устроены С7 и С8** | образец гейта, который сверяет документ с диском; пиши по нему |
| Я4 | `_studio/konvejer/GEJTY.md` · `00-KONVEJER.md` | мёртвый описательный слой — предмет Ш4 |
| Я5 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_gigiena-i-svedenie.md`, секции `## ОТЧЁТ` и `## ВОПРОСЫ` | что уже сделано и три «немедленных» дефекта — **не переоткрывай** |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — четыре шага

### Ш1. ВЛИТЬ (команды — §0.1) и убедиться фактом

### Ш2. 🔴 КАРТА СТАНОВИТСЯ РЕЕСТРОМ — главная работа

В Я1 **добавить машиночитаемый реестр инструментов**. Новый файл НЕ заводить — владелец сказал
прямо: *«чем меньше документов, тем лучше»*.

**Строка реестра — на инструмент, колонки:**

| колонка | что в ней | откуда берётся |
|---|---|---|
| файл | путь от корня репо | диск |
| **фаза / роль** | какой фазе Ф1…Ф7 служит либо «инфраструктура» | решает человек |
| **кто зовёт** | хук · другой инструмент · шаг сборки · «по руке» | `check_tool_contract` |
| **чем проверен** | какая `PROGNAT.sh` его покрывает | маркер `TOOL-CONTRACT-COVERS` |
| статус | ЖИВ · АРХИВ | человек |

🔴 **Формат — строгий и разбираемый регуляркой.** Реестр, который нельзя прочитать программой, —
это снова текст, а текст протух уже дважды.
⚠ **Заполняй не по памяти и не по докстрингам, а прогонами.** Именно на чтении документации карта
и соврала про гейт вмещения.

### Ш3. 🔴 ГЕЙТ СОГЛАСОВАННОСТИ — то, ради чего заход

Новая проверка (`check_karty.py` либо проверка внутри `check_sborki.py` — **выбери и обоснуй в
`## ПЛАН`**). **Четыре инварианта, каждый краснеет отдельно и называет виновника поимённо:**

1. **каждый `.py` в `_generator/**` есть в реестре** — завёл инструмент, не вписал → красное;
2. **каждая строка реестра существует на диске** — удалил файл, забыл строку → красное;
3. **у каждого ЖИВОГО есть кто-зовёт** — 🔴 **не повторяй логику `check_tool_contract.py`, ЗОВИ его**
   (Я2). Третий источник правды о том же — ровно наша болезнь;
4. **каждый гейт, названный в описании фазы, существует и вызывается** — этого нет сегодня, и
   отсюда пришла ложь про гейт вмещения.

🔴 **Гейт обязан отличать рассогласование от законной пустоты.** У фазы может не быть верификатора —
это находка, а не ошибка; у инструмента может быть статус АРХИВ — это решение, а не дыра.
**Пустая клетка с пометкой «нет, и вот почему» — зелёная. Пустая молча — красная.**

⚠ **Фикстуры обязательны, минимум четыре — по одной на инвариант:** новый `.py` без строки реестра →
красное; строка реестра без файла → красное; ЖИВОЙ без кто-зовёт → красное; всё согласовано → зелёное.

### Ш4. АРХИВ СТАРОГО КОНВЕЙЕРА + ТРИ НЕМЕДЛЕННЫХ

**(а) Пометить `_studio/konvejer/` архивом явно** — `GEJTY.md`, `ALGORITM.md`, `KATALOG.md`,
`00-KONVEJER.md` и папки стадий. Шапкой в каждом файле: «АРХИВ, конвейер до 27.07.2026, живой —
Ф1…Ф7, карта такая-то».
🔴 **НИЧЕГО НЕ УДАЛЯТЬ.** История решений — единственное, чем доказывается «почему так»; стадия может
оказаться живой в другом проекте. Помечаем, не стираем.
⚠ **И гейт Ш3 обязан их не считать живыми** — иначе он покраснеет на 17 архивных гейтах и его отключат.

**(б) Три дефекта класса НЕМЕДЛЕННОЕ** — найдены прошлым заходом, повторятся на следующем же прогоне
(подробности — Я5, `## ВОПРОСЫ`):
- **бэктики в shell-блоке §4** шаблона захода — бьют каждого исполнителя на последнем ходе;
- 🔴 **фикстура пишет дату в отслеживаемый `_generator/tools/fixtures/uroki/zakrytie/DOLG.md`** —
  каждый регресс пачкает зону и роняет Г1 у того, кто регресс прогнал. **Гигиена начнёт краснеть на
  собственном инструментарии, и её объявят шумной в первый же вечер;**
- **хук принимает фикстуру со `slajdy/` за живую лекцию** — у прошлого захода это остановило слияние.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `git branch --no-merged arka/mat-kostyak` — `gigiena-i-svedenie` среди несведённых нет.
2. 🔴 **Живой прогон гейта на реальном объекте репозитория:** гейт согласованности на живом дереве →
   **зелёный**; и **четыре искусственных рассогласования** (по одному на инвариант) → **красное с
   именем виновника**. Все пять команд и rc в отчёт.
3. Реестр покрывает **все 51** инструмента (число — командой `ls _generator/tools/*.py _generator/sborka/*.py _generator/*.py | wc -l`), ни одного вне реестра.
4. `check_tool_contract.py` по всем → те же **3** нарушения, что были (`lenta.py`, `slaid.py`,
   `vmeshchenie.py`), **ни одним больше** — гейт согласованности не должен плодить новых.
5. Регресс: все `PROGNAT.sh` (число — командой), зелёных N из N. **Прогнать ДВАЖДЫ подряд** — второй
   прогон обязан оставить зону чистой (проверка дефекта Ш4б).
6. `_studio/konvejer/` помечен архивом; гейт на его файлах не краснеет.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «реестр собран», а «в реестре N из 51, инвариантов
работает M из 4, фикстур K».

## 3. ВЕРИФИКАТОР

🔴 **Самый вероятный провал — реестр, который через неделю снова врёт.** Он ровно для того и
строится, и если гейт его не сторожит по-настоящему, мы получим третью протухшую карту.

- 🔴 **Порви согласованность руками и проверь, что покраснело** — четыре пробы (пункт 2 критерия).
  Гейт, не проверенный на разрыве, не проверен вовсе.
- 🔴 **Свежий субагент, не видевший сессии, отвечает по одному РЕЕСТРУ:** *«какие инструменты
  обслуживают фазу 4 и чем они проверены?»*. Не смог ответить — реестр написан для нас, а не для
  читателя. Вопрос, ответ и вердикт в отчёт.
- **Сверить реестр с диском независимо:** другой субагент берёт **пять случайных строк** и проверяет
  своими командами, что «кто зовёт» и «чем проверен» — правда. Пять из скольких — числом.
- **Ничего не удалено:** `git show --stat` — в `_studio/konvejer/` только добавления шапок,
  удалённых строк 0.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/**` `_studio/konvejer/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/**` `_studio/konvejer/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Пункт 1 КРИТЕРИЯ ГОТОВНОСТИ требует состояния, которое контракт зоны исполнителю ЗАПРЕЩАЕТ создавать
`git branch --no-merged arka/mat-kostyak` перечисляет ветки, не влитые В АРКУ. Влить в арку может
только приёмка — контракт зоны того же захода говорит прямо: «Ветку в конце НЕ вливай». Значит
исполнитель обязан сдать критерий, зелёным которого он сделать не вправе: `gigiena-i-svedenie`
стоит в списке несведённых с аркой и после безупречного слияния. Правильный вопрос — «влито ли
в МОЮ ветку», и он проверяется `merge-base --is-ancestor` (та самая команда, что уже стоит в §0.1).
ЦЕНА: два захода подряд сдают этот пункт с оговоркой вместо вердикта — прошлый написал «✅ …→ 10
(9 чужих + моя)», то есть зелёным назван красный по букве. Гейт, который всегда сдают с
пояснением, перестают читать; а формулировка чинится одной командой.

### Свежий верификатор, сверявший реестр с диском, нашёл 4 неточности из 5 строк — и все одного класса
Колонка «кто зовёт» была заполнена тем же инструментом, который её потом и проверяет
(`check_tool_contract.has_live_trigger` — текстовая эвристика «упомянут ли basename»). В итоге в
реестр уехали как вызовы: докстринги (`priyomka.py` — «зовут четыре инструмента», на деле ни
одного), комментарии (`bloki.py` ← `bootstrap_lekcii.py`), печатаемые владельцу строки-советы
(`gejt_illyustracij.py` ← `sostoyanie.py`) — и наоборот, потерялись девять настоящих вызовов
`python3 -m dvizhki` в трёх проектах.
ЦЕНА: колонка переписана целиком по отдельному замеру (импорт · запуск процессом · `-m модуль` ·
строка хука), плюс потребовалась вторая независимая сверка. Общая цена — примерно треть захода.
Урок переносимый: **заполнять реестр тем же механизмом, которым его потом проверяешь, — значит
получить документ, согласованный сам с собой и расходящийся с диском.** Источник данных для
документа и источник данных для гейта обязаны быть разными.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Ш1 сделан до плана — он же §0.1 «ДО ВСЕГО ОСТАЛЬНОГО», и без него Я1 физически
нечитаем: `KARTA-ZHIVOGO-KONVEJERA.md` приезжает вливаемой веткой.
`git_zona.py merge …` (зоны строкой из задания) → **влито без конфликтов**, `d4bfeb6`;
`git merge-base --is-ancestor zahod/gigiena-i-svedenie HEAD` → **rc=0**.
Конфликтов и `UnicodeDecodeError`, которых велено не пугаться, не было.

### Развилка 1 — гейт отдельным файлом `check_karty.py`, НЕ внутри `check_sborki.py`

Выбираю новый файл, и это не «удобнее», а два проверяемых довода:
1. **Предмет другой.** `check_sborki.py` судит ФАЙЛ-ЗАХОД: его CLI принимает `kod_*.md`,
   `zadanie()` режет текст по `## ПЛАН`, весь аппарат — белый список команд, выполняемых
   из чужого текста. Мой гейт судит ДЕРЕВО против ОДНОГО документа; ни одна из этих
   машин ему не нужна, а его вход в CLI `check_sborki` не помещается.
2. 🔴 **Он бы никогда не сработал.** `check_sborki` зовётся только на свежесобранном
   заходе (его же объявленная слепая зона: «старые заходы не перелинтуются»). Реестр
   рассогласуется, когда правят инструмент, а не когда пишут заход. Гейт, повешенный
   на чужой триггер, — это ровно болезнь Я2 («у гейта триггер уже его собственного охвата»).

### Развилка 2 — точка вызова нового инструмента

Хук `.githooks/pre-commit` **вне зоны** (зона: `_generator/tools/**`, `_studio/konvejer/**`,
папка арки, `_studio/docs/KARTA.md`). Значит инвариант 3 на моём же инструменте закрываю
той формой, которую фабрика приняла 08.08 для `gejt_verstki.py`: маркер
`# TOOL-CONTRACT: called-by-hand` с НАЗВАННОЙ точкой + пункт очереди с готовой строкой
для хука. Строку в хук не пишу — это чужая зона, и «чинится в одну строку» её не открывает.

### Развилка 3 — формат реестра

Раздел `## 5` внутри Я1 (новых файлов не завожу — требование владельца). Таблица,
разбираемая построчным регекспом; пять колонок задания; статус `ЖИВ`/`АРХИВ`.
🔴 **Законная пустота отличается от дыры ФОРМОЙ:** клетка либо несёт содержание, либо
ровно `нет: <причина>`. Пустая или `—` — красное. Это и есть «пустая молча — красная».

### Развилка 4 — охват инварианта 1

`_generator/**/*.py` **минус `fixtures/**`** (там `.py` — нагрузка одной фикстуры, не
инструмент фабрики; ровно то же исключение уже сделано в `check_tool_contract.check_live_trigger`)
и минус `vendor/`. Числом: `find _generator -name '*.py' | wc -l` → 58, из них под реестром
`ls _generator/tools/*.py _generator/sborka/*.py _generator/*.py | wc -l` → **51**. Разницу
объявляю слепой зоной гейта печатью, а не умолчанием.

### 🔴 Оспариваю пункт 4 КРИТЕРИЯ ГОТОВНОСТИ (до работы, как велено)

«`check_tool_contract.py` **по всем** → те же **3** нарушения» — предпосылка верна не для
«всех». Замер шапки захода гоняет **43** файла (`tools/*.py sborka/*.py`) и даёт 3. Реестр
по пункту 3 обязан покрыть **51** — с `_generator/*.py`, которых контракт не гонял никогда:

```
python3 _generator/tools/check_tool_contract.py $(ls _generator/tools/*.py _generator/sborka/*.py)                    → 3
python3 _generator/tools/check_tool_contract.py $(ls _generator/tools/*.py _generator/sborka/*.py _generator/*.py)   → 9
```

Лишние 6 — предсуществующие, класс «кривой вход» + один «нет вызова»
(`audit.py`, `build_doc.py`, `build_listok.py` ×2, `dvizhki.py`, `harvest_katex.py`), все в
`_generator/**` вне моей зоны. Поправка к критерию: сдаю ОБА числа обеими командами, а
клаузу «ни одним больше» проверяю по обеим — она и есть содержательная часть пункта.

### 🔴 Ш4б — из трёх «немедленных» в моей зоне полторы, называю ДО работы

| дефект | дом | в зоне? | что делаю |
|---|---|---|---|
| фикстура пишет дату в отслеживаемый `fixtures/uroki/zakrytie/DOLG.md` | `_generator/tools/fixtures/uroki/PROGNAT.sh` | **да** | чиню (`Z=` уводится в одноразовый каталог) |
| бэктики в shell-блоке §4 | `_studio/zhurnal/_TEMPLATE-zahod.md` + `bootstrap_zahod.py` | **шаблон — НЕТ**, генератор — да | чиню в генераторе якорной заменой двух командных строк; чистая починка двумя плейсхолдерами остаётся долгом шаблона |
| хук принимает фикстуру со `slajdy/` за живую лекцию | `.githooks/pre-commit` | **нет** | НЕ трогаю, пункт очереди с готовой строкой |

### Шаги

1. **Ш2.** Собрать данные ПРОГОНАМИ (не докстрингами): «кто зовёт» — грепом по тому же
   охвату, что у `has_live_trigger`; «чем проверен» — `check_tool_contract.py --fixtures-for`.
   «фаза/роль» — решение человека, но сверенное с реальными точками вызова. Раздел `## 5` в Я1.
2. **Ш3.** `check_karty.py`, четыре инварианта, каждый краснеет отдельно и называет виновника.
   Инвариант 3 — ЗОВЁТ `check_tool_contract.py` (третьего источника правды не завожу).
   Инвариант 4 читает ТОЛЬКО таблицу §1 живой карты — архивных гейтов не касается вовсе,
   поэтому 17 архивных гейтов его не красят по построению, а не по списку исключений.
   Фикстура `fixtures/karty/PROGNAT.sh` — ≥4 ловушки, по одной на инвариант, плюс зелёная.
3. **Ш4а.** Шапка «АРХИВ» во ВСЕ `.md` папки `_studio/konvejer/` **кроме**
   `KARTA-ZHIVOGO-KONVEJERA.md` (она живая). Это шире четырёх названных файлов: `FORMAT-ISTOCHNIKA.md`
   и `ZHURNAL.md` — того же мёртвого слоя, и оставить их непомеченными значит оставить
   ровно ту двусмысленность, ради которой заход. Ничего не удаляю.
4. **Ш4б** — по таблице выше. 5. Регресс ×2, критерий, верификаторы, коммит.

Точку отката не фабрикую: `git status --porcelain` по зоне пуст, зона чиста в HEAD.

## ВОПРОСЫ — (заполняет исполнитель)

1. 🔴 **Хук не зовёт `check_karty.py` — строка готова, дом вне зоны.** Гейт согласованности живёт
   маркером `called-by-hand`; чтобы он краснел САМ, в `.githooks/pre-commit` нужен блок: если среди
   staged есть `_generator/**/*.py` либо `_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` — гнать
   `python3 _generator/tools/check_karty.py`. Без этой строки реестр сторожится только руками, а
   именно «написан и не подключён» — тот класс, ради которого заход и делался.
   ДОМ: `.githooks/pre-commit`
   ДОСТАВЛЕНО: нет

2. **Бэктики в строке «Точка отката» §0 шаблона — тот же дефект, что починен в §4.** Блок команд §4
   вылечен в генераторе (якорная замена), но `- Точка отката: \`git add {{ЗОНА}}\`` даёт вложенные
   бэктики и так же неисполним дословно. Чистая починка обоих мест — два плейсхолдера
   (`{{ЗОНА}}` для прозы, `{{ЗОНА_КОМАНДОЙ}}` для кода); шаблон вне зоны этого захода.
   ДОМ: `_studio/zhurnal/_TEMPLATE-zahod.md` (+ `_generator/tools/bootstrap_zahod.py`)
   ДОСТАВЛЕНО: нет

3. **Хук принимает фикстуру со `slajdy/` за живую лекцию** (унаследовано от `gigiena-i-svedenie`
   п.3, не переоткрываю — подтверждаю, что дом прежний и правка не сделана): `.githooks/**` вне моей
   зоны, не трогал. Корневая починка — одна строка `case "$LEC" in */fixtures/*) continue;; esac`.
   ДОМ: `.githooks/pre-commit`
   ДОСТАВЛЕНО: нет

4. 🔴 **Эвристика «живой точки вызова» слепа в ОБЕ стороны, и это измерено.** Ложный ЗЕЛЁНЫЙ:
   `priyomka.py`, `podgonka.py`, `korpus.py`, `harvest_katex.py` — имя названо только прозой соседа
   либо докстрингом самого гейта. Ложное КРАСНОЕ по смыслу: `dvizhki.py` — девять живых
   `python3 -m dvizhki` в `krivaya-drakona/{lekciya,razbor}/assemble.py`,
   `obzory/sluchajnye-bluzhdaniya-na-gruppah/zahod/render.py`, `teorkat-vvedenie/src/tools/podognat.py`,
   плюс консольная точка входа в `_generator/setup.cfg`. Лечение — считать вызовом ещё
   `import <модуль>` и `-m <модуль>`; сознательно НЕ делал: это меняет базовые числа контракта
   (3 и 9), по которым сдаётся критерий, и заслуживает своего замера.
   ДОМ: `_generator/tools/check_tool_contract.py`
   ДОСТАВЛЕНО: нет

5. **Докстринг `check_tool_contract.check_live_trigger` (строки 468–470) утверждает неправду.**
   Там сказано, что `podgonka` импортируется из `vmeshchenie` и `zamer_smety`; `grep -rnE '^\s*(import
   podgonka|from podgonka)'` по репозиторию → пусто. Поправка вписана в карту (§1а), но сам докстринг
   я не правил — он в чужой части файла и его чинить надо вместе с п.4.
   ДОМ: `_generator/tools/check_tool_contract.py`
   ДОСТАВЛЕНО: нет

6. **`_generator/sborka/podgonka.py` не покрыт НИЧЕМ и никем не вызывается.** Ни импорта, ни
   запуска, ни CLI (`__main__`/`argparse` нет — руками его не запустить), ни фикстуры, ни косвенного
   покрытия: ни одна `PROGNAT.sh` не исполняет ни строки. Его собственный докстринг говорит, что
   `kegl_dlya_znakov` «не вызывается нигде в пайплайне, доступна для будущего захода». Кандидат в
   статус АРХИВ, но это решение владельца, а не исполнителя — в реестре стоит `ЖИВ` с честно
   объявленной пустотой в обеих клетках.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

7. **Два статуса `АРХИВ` в реестре — мои решения, оспариваются одной строкой.**
   `_generator/tools/sostoyanie.py` (трекер по `GEJTY.md`, дерево `src/`+`slides/`) и
   `_generator/tools/bootstrap_lekcia.py` (стаб-дерево по `PAPKA-LEKCII.md`, заменён
   `_generator/sborka/bootstrap_lekcii.py`). Основание: предмет обоих помечен архивом Ш4а, на дереве
   `slajdy/` они не наводятся. Обе фикстуры при этом зелёные — инструменты целы, архивен предмет.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

8. **Восемь инструментов без фикстуры вовсе:** `check_kurs.py`, `check_view.py`,
   `sostoyanie_lekcii.py`, `audit.py`, `build_doc.py`, `build_listok.py`, `dvizhki.py`,
   `harvest_katex.py`. Пять из них дают КРАСНЫЙ контракта по «кривому входу» и живут вне охвата хука
   (он фильтрует только `^_generator/tools/.*\.py$`). В реестре каждая пустота объявлена с причиной.
   ДОМ: `_generator/**`
   ДОСТАВЛЕНО: нет

9. **`_generator/tools/fixtures/uroki/zakrytie/` осталась неиспользуемым остатком.** Фикстура больше
   туда не пишет (дефект Ш4б починен), но три отслеживаемых файла в папке остались. Не удалял:
   удаление истории фикстуры — не моё решение.
   ДОМ: `_generator/tools/fixtures/uroki/`
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/karta-reestr/_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` — открыть любым просмотрщиком markdown; смотреть новый **раздел «## 5. РЕЕСТР ИНСТРУМЕНТОВ»**. Файл лежит в РАБОЧЕЙ папке захода (ветка `zahod/karta-kak-reestr`); в основную папку он приедет, когда приёмка вольёт ветку. Проверить его состоятельность одной командой из рабочей папки: `python3 _generator/tools/check_karty.py`.
**КОММИТ:** `c14decb` — «karta-reestr: карта стала машиночитаемым реестром (52 из 52 инструментов), гейт согласованности check_karty.py с 4 инвариантами и 10 ловушками, старый конвейер помечен архивом (47 файлов, удалений 0), два немедленных дефекта в зоне починены» · `git_zona.py check --zone _generator/tools` → ✅ · `--zone _studio/konvejer` → ✅ · `git show --stat` → 52 файла, чужих путей нет.

---

### Вердикт с охватом в себе

**Реестр покрывает 52 из 52** инструментов (`ls _generator/tools/*.py _generator/sborka/*.py _generator/*.py | wc -l` → 52 после появления `check_karty.py`; строк реестра — 52 той же командой-грепом), вне реестра **0**. **Инвариантов работает 4 из 4**, каждый краснеет отдельно и называет виновника поимённо. **Ловушек фикстуры 10** (4 на инварианты + зелёная + 5 на форму и на законную пустоту). **Регресс 25 из 25 зелёных, дважды подряд, зона после второго прогона не изменилась.** **Архивом помечено 47 из 47** файлов старого конвейера (48-й — симлинк на уже помеченный), **удалённых строк 0**.

### Ш1. Слияние — гладко, в отличие от прошлого раза

`git_zona.py merge zahod/gigiena-i-svedenie` с зонами из задания → **влито без конфликтов**, `d4bfeb6`. Обещанных конфликтов в двух путях и `UnicodeDecodeError` в `merge --continue` **не было** — починка `errors="replace"` держится, обхода единственной двери не потребовалось. Проверка фактом: `git merge-base --is-ancestor zahod/gigiena-i-svedenie HEAD` → **rc=0**.

### Ш2. Реестр — и почему пришлось делать его дважды

Раздел `## 5` внутри живой карты; новых файлов не заведено (требование владельца). Пять колонок задания, строгий построчный формат, статус одним словом.

🔴 **Первая редакция колонки «кто зовёт» была заполнена `check_tool_contract` — тем самым механизмом, который потом её и проверяет. Независимый субагент, сверявший пять случайных строк своими командами, дал вердикт «правдивых 1 из 5».** Класс ошибки один: эвристика считает вызовом упоминание имени в докстринге, комментарии и даже в ПЕЧАТАЕМОЙ владельцу строке-совете. Примеры из его отчёта: у `priyomka.py` записаны четыре вызывающих инструмента — на деле ни одного, а связь с двумя из них ОБРАТНАЯ (`priyomka.py` сама их импортирует); `dvizhki.py` записан «по руке» при девяти живых `python3 -m dvizhki` в трёх проектах.

Колонка переписана целиком по отдельному замеру: настоящий вызов — это `import <модуль>`, запуск процессом, `-m <модуль>` или строка хука; комментарии и печатаемые советы не считаются. **Вторая независимая сверка, другой жребий, свежий агент: «правдивых 3 из 5», две строки частично.** Обе оставшиеся неточности тут же починены и перепроверены фактом:
- `git_zona.py` — убраны три записанных вызова, которых нет (`check_kartoteka.py` печатает команду советом; фикстуры `sborka` и `tool_contract` держат её строкой в шаблоне), добавлен пропущенный настоящий (`fixtures/dvizhok/PROGNAT.sh`);
- `podgonka.py` — «по руке» заменено на объявленную пустоту: у модуля нет ни `__main__`, ни `argparse`, запускать его нечем; заявленное «косвенное покрытие» оказалось нулевым — фикстура `sborka` о нём не знает и ни строки его не исполняет. Заодно проверено, что у `korpus.py` косвенное покрытие РЕАЛЬНОЕ: `fixtures/sborka` гоняет `gejt_vmeshcheniya.py`, а тот через `vmeshchenie.py` грузит `korpus` — замер показал модуль в `sys.modules`.

Попутно опровергнуто унаследованное утверждение §1а карты («`podgonka` импортируется из `vmeshchenie`») — поправка вписана в карту с командой замера, исходный текст не вычеркнут.

### Ш3. Гейт `check_karty.py` — четыре инварианта и одно решение, которое пришлось пересмотреть

**Развилка решена отдельным файлом, не внутри `check_sborki.py`** — обоснование в `## ПЛАН` до работы; решающий довод: `check_sborki` зовётся только на свежесобранном заходе (его собственная объявленная слепая зона), то есть реестр он не сторожил бы НИКОГДА.

Инвариант 3 не повторяет логику, а **зовёт** `check_tool_contract.py` одним прогоном на все живые пути.
Инвариант 4 читает ТОЛЬКО таблицу фаз §1 живой карты — архивных гейтов не касается вовсе, поэтому 17 архивных гейтов его не красят по построению, а не по списку исключений.

🔴 **Три дефекта поймано СВОИМИ прогонами, а не рассуждением:**
1. Гейт покраснел на собственной документации: пример формата строки внутри ```-блока §5.1 был принят за настоящую строку реестра. Чинил РАЗБОР, не текст.
2. **Инвариант 3 молча зеленел на любом дереве.** Сверка «кто без вызова» шла строками, а `check_tool_contract` печатает путь относительно своего корня — на macOS `/var` симлинк на `/private/var`, и во временном дереве фикстуры пути не совпадали, множество «без зовущего» выходило пустым. Поймала ловушка 3 фикстуры; сверка переведена на разрешённый путь.
3. **Правило «реестр отстал» пришлось снять с красного.** Изначально: реестр говорит «зовущего нет», а гейт его находит → красное. Замер показал обратное — реестр строже эвристики, и красное наказывало бы за правду. Теперь это печатаемое РАСХОЖДЕНИЕ, ловушка 7 сторожит именно печать.

### Ш4. Архив и два дефекта из трёх

**(а)** Шапка `🗄 АРХИВ` стоит во всех `.md` папки `_studio/konvejer/` **кроме живой карты** — 47 файлов, включая не названные в задании `FORMAT-ISTOCHNIKA.md` и `ZHURNAL.md` (тот же мёртвый слой; оставить их непомеченными значило оставить ровно ту двусмысленность, ради которой заход). Два эталона с YAML-шапкой получили её ПОСЛЕ front matter, чтобы не сломать разбор. **`git diff --numstat` по архивным файлам: добавлено 465, удалено 0.**

**(б)** Из трёх «немедленных» в зоне оказались полтора — названо в `## ПЛАН` ДО работы:
- ✅ **фикстура писала дату в отслеживаемый `DOLG.md`** — `Z` уведён в одноразовый каталог с `trap`. Дефект маскировался ровно один день: в дереве лежала дата последнего коммита фикстуры, и сегодня перезапись совпадала байт в байт — то есть на живом прогоне он бы НЕ проявился, и это сказано прямо, а не выдано за пойманное красное;
- ✅ **бэктики в блоке команд §4** — починены в генераторе якорной заменой двух командных строк (шаблон вне зоны). Проверено настоящим прогоном на клоне: в собранном заходе `add -- _generator/tools/** …` без бэктиков, а проза КОНТРАКТА бэктики сохранила (гейт З3 цел). 🔴 Первая версия правки **сломала генератор** на синтетическом шаблоне фикстуры `dvizhok` — семь ловушек покраснели разом; поймано первым же прогоном регресса, условие исправлено на «ноль совпадений законно, два и больше — нет»;
- ❌ **хук принимает фикстуру за живую лекцию** — `.githooks/**` вне зоны, НЕ трогал, пункт очереди 3.

### КРИТЕРИЙ ГОТОВНОСТИ — по пунктам, включая проваленный

1. 🔴 **ПО БУКВЕ НЕ ВЫПОЛНЕН, и выполнить его исполнителю нечем.** `git branch --no-merged arka/mat-kostyak` → **12 веток, `zahod/gigiena-i-svedenie` среди них ЕСТЬ**. Влить в арку может только приёмка — контракт того же захода запрещает: «Ветку в конце НЕ вливай». По существу же ветка влита: `git branch --no-merged HEAD | grep -c gigiena-i-svedenie` → **0**, `merge-base --is-ancestor` → **rc=0**. Пункт вынесен в `## УРОКИ ФАБРИКЕ` как дефект формулировки, а не подан зелёным с оговоркой.
2. ✅ **Живой прогон на реальном объекте — пять команд, все rc в отчёте.** Живое дерево → **rc=0**. Четыре искусственных разрыва прямо в рабочем дереве, каждый назвал виновника поимённо:
   - И1 (новый `.py` без строки) → **rc=1**, «инструмент `_generator/tools/proba_razryva.py` есть на диске, но строки в реестре §5 у него НЕТ»;
   - И2 (строка без файла) → **rc=1**, «строка реестра 239 называет `_generator/tools/prizrak_i2.py`, а файла на диске НЕТ»;
   - И3 (ЖИВОЙ без зовущего) → **rc=1**, «`_generator/tools/sirota_i3.py` числится ЖИВЫМ, а живой точки вызова у него нет»;
   - И4 (фаза называет несуществующий гейт) → **rc=1**, «фаза «4 · Вёрстка» называет гейтом `gejt_vmeshcheniya_novyj.py`, а такого инструмента НЕТ в реестре §5».
   Карта восстановлена из бэкапа **побайтово** (`shasum` совпал), пробные файлы удалены, живое дерево снова **rc=0**.
3. ✅ **Реестр покрывает все 52 из 52** (`ls … | wc -l` → 52; `grep -cE '^\| \`_generator/[^\`]+\` \|'` → 52). Вне реестра 0 — это и есть инвариант И1, он зелёный.
4. ⚠ **Выполнен, но предпосылка пункта была неверна — сказано в `## ПЛАН` ДО работы.** «По всем → те же 3» верно для команды из шапки захода (43 файла, `tools`+`sborka`): **rc=1, красных 3** — `lenta.py`, `slaid.py`, `vmeshchenie.py`, те же. На всех 52 (с `_generator/*.py`, которых контракт не гонял никогда) красных **9 в 8 файлах** — и это ровно то же число, что было до моей работы: `audit.py`, `build_doc.py`, `build_listok.py` (×2), `dvizhki.py`, `harvest_katex.py` плюс те же три. **Ни одним больше по ОБЕИМ командам.** Новый `check_karty.py` — зелёный (`rc=0`).
5. ✅ **Регресс: `find _generator -name PROGNAT.sh | wc -l` → 25, зелёных 25 из 25, ДВАЖДЫ подряд.** `git status --porcelain` по зоне до и после второго прогона — `diff` пуст: второй прогон зону не тронул (проверка дефекта Ш4б).
6. ✅ `_studio/konvejer/` помечен архивом; гейт на его файлах не краснеет — он их **не читает вовсе**, и это записано в его объявленных слепых зонах, а не оставлено на удачу.

### Верификатор

1. **Свежий читатель, получивший ТОЛЬКО раздел реестра** (файл-выдержка, репозиторий ему не показывали). Вопрос: «какие инструменты обслуживают фазу 4 и чем они проверены?» — **ответил, перечислив 9 строк с колонкой «чем проверен» дословно, и сказал, что отвечал не догадываясь.** Правильно понял, что значит клетка `нет: …` (законная пустота, зелёная — в отличие от прочерка). **Нашёл две настоящие дыры изложения:** внутри §5 не сказано, что делает каждая фаза (ссылка вела в §1, которого он не видел), и не объяснён разделитель `Ф4/Ф6`. Обе дописаны в §5.1. *Оговорка: этот прогон был на редакции ДО переписывания колонки «кто зовёт»; колонки «фаза / роль» и «чем проверен», по которым он отвечал, правка не касалась — кроме двух строк, где «чем проверен» стало строже.*
2. **Независимая сверка реестра с диском — ДВА прогона, разные жребии, разные агенты.** Первый: **1 правдивая из 5** → колонка переписана. Второй: **3 из 5, две частично** → обе починены, разбор выше. Оба брали строки жребием командой (`sort -R`), а не по моему выбору.
3. **Ничего не удалено:** `git diff --numstat` по `_studio/konvejer/` без живой карты — **добавлено 465, удалено 0, файлов 47**. Две удалённые строки во всём коммите — мои две правки клеток таблицы фаз §1 (Ф1 и Ф7), обе в живой карте.
4. **Коммит чист:** `git show --name-only` — только `_generator/tools/` и `_studio/konvejer/`, чужих путей 0.

### Что НЕ трогал

`.githooks/**` (вне зоны — включая обе просимые правки: точка вызова `check_karty.py` и фильтр фикстур); `_studio/zhurnal/_TEMPLATE-zahod.md` (вне зоны, ни байта); `_generator/sborka/**` и `_generator/*.py` (вне зоны — все шесть красных контракта там и остались); `_studio/docs/KARTA.md` (не менял: новых `.md` не заводил, регистрировать нечего); `teorkat-vvedenie/**`, `_fond/**`, `obzory/**`, `ucheniki/**`; ветки не удалял; свою ветку не вливал; файл-заход не коммитил.

**Замечено вне зоны, не трогал:** в ОСНОВНОЙ папке висит untracked `_generator/tools/fixtures/sborka/obratnyj-progon/teorkat-vvedenie-s10/math/katex.json` — артефакт чужого прогона фикстуры, из-за него `git_zona.py check --zone _generator/tools`, запущенный ИЗ ОСНОВНОЙ папки, красный. В моей рабочей папке обе зоны ✅.

### НЕОБРАТИМОЕ

1. **Четыре искусственных разрыва карты и создание двух пробных `.py` в рабочем дереве** (пункт 2 критерия) · восстановлено побайтово из бэкапа `scratchpad/KARTA.backup`, `shasum` сверен, пробные файлы удалены · дополнительно восстанавливается: `git show c14decb:_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md`.
2. **Перезапись строки `Z=` в `_generator/tools/fixtures/uroki/PROGNAT.sh`** (дефект Ш4б) · восстанавливается: `git show d4bfeb6:_generator/tools/fixtures/uroki/PROGNAT.sh`.
3. **Одноразовые клоны в `/tmp` (`probaZ`, `probaZ2`) созданы и перезаписаны** · содержимое — копия репозитория, восстановлению не подлежит и не нужно.

Больше необратимого нет: **ни одного файла не удалил, не переименовал и не переместил**; `git reset`/`checkout` поверх несохранённого не делал; ветки не трогал.

### ПОВТОРЯЕМОСТЬ находок

**Повторятся на следующем же заходе — класс НЕМЕДЛЕННОЕ, а не пункт очереди:**
- **Хук не зовёт `check_karty.py`** (`## ВОПРОСЫ` п.1) — реестр начнёт расходиться с диском на первом же заходе, который заведёт инструмент, и никто не узнает: гейт написан и не подключён — ровно тот класс, ради которого заход делался. Одна строка в чужой зоне.
- **Бэктики в «Точке отката» §0** (п.2) — бьёт каждого исполнителя, который решит сделать точку отката, то есть на каждом рискованном заходе.
- **Хук принимает фикстуру за лекцию** (п.3) — унаследованное, сработает на следующей же фикстуре со `slajdy/`.
- **Эвристика «живой точки вызова»** (п.4) — соврёт на следующем же инструменте, который зовут через `import` или `-m`; она уже соврала мне в обе стороны и стоила трети захода.

**Не повторятся, законно уходят в очередь:** докстринг `check_live_trigger` (п.5, разовая правка текста), `podgonka.py` без покрытия (п.6, решение владельца), два статуса АРХИВ (п.7, решение владельца), восемь инструментов без фикстур (п.8, отдельный заход), остаток `fixtures/uroki/zakrytie/` (п.9, разовая уборка).

**Время прогона + токены** — на канале `app` НЕПРИМЕНИМО, снимать неоткуда: лог прогона существует только на канале `terminal`, где его кладёт `tee`.
