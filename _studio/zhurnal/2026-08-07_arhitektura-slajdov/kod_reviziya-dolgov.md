# Канал исполнителя — reviziya-dolgov (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — работа проверочная: у каждого долга есть команда, которая скажет, жив он
> или мёртв. Субагенты: до 5 читателей, если решишь делить список — назови в `## ПЛАН` сколько и
> на что.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.**

КОНТЕКСТ. Фабрика научно-популярных лекций. Долги скилла `slajdy` живут в
`~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md` — их там 50+, накоплены за сутки очень
плотной работы, у каждого есть номер (Д1…Д54) и формулировка «что не так».

🔴 **Проблема, ради которой заход существует: часть этих долгов уже мертва, и никто не знает
какая.** За сутки принято двенадцать заходов, каждый чинил своё и по дороге закрывал чужое, не
отмечая. Аналитик проверил два долга командой прямо перед сборкой этого захода — оба оказались
неверны:

- **Д30** («метки `.formula`, `.spisok`, `.op-utv`, `.lst` не стилизованы в `base.css` вовсе») —
  `grep -c` в `_generator/skeleton/base.css` находит `.formula` 6 раз, `.spisok` 4, `.op-utv` 6,
  `.lst` 3. Долг мёртв;
- **Д31** («`tokens.css` не несёт `--hollow`/`--steel-d`») — оба присутствуют. Долг мёртв.

Два из двух проверенных наугад. Значит доля мёртвых в списке заметная, и **любой план, построенный
на этом списке, планирует работу, которой нет**.

ЦЕЛЬ: пройти список целиком и у каждого долга поставить статус, **подтверждённый командой**.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее: ты НИЧЕГО НЕ ЧИНИШЬ.** Заход **аналитический**: ты
измеряешь, а не правишь. Увидел долг, который «чинится в одну строку», — не чини, поставь статус
и напиши в отчёт. Починка идёт отдельными заходами, у них свои зоны, и рядом их работает пять.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/reviziya-dolgov`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/reviziya-dolgov`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add reviziya-dolgov --branch zahod/reviziya-dolgov
  cd ../materials-wt/reviziya-dolgov
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/reviziya_dolgov.py` (новый скрипт), и во ВТОРОМ репозитории — **только раздел статусов** в `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md`. Всё вне — **READ-ONLY**.
- 🔴 **Как править `DOLG.md`, чтобы не поссориться с чужой работой и не убить голос источника:** текст каждого долга **не переписывать и не удалять**. Ты только **дописываешь** к строке долга маркер статуса и команду-доказательство. Правило файла дословно: «Записи дословные, голос источника. Переписывать их под себя нельзя: пересказ убивает цену».
- 🔴 **`SKILL.md`, `references/**`, `_generator/skeleton/**`, `_generator/sborka/**` — НЕ ТРОГАТЬ.** Рядом идут пять заходов, и всё это чужие зоны.
- 🔴 **ДВА РЕПОЗИТОРИЯ — ДВА ОТДЕЛЬНЫХ КОММИТА**, пути из разных репо не собираются в одну командную строку. Второй — `GIT_ZONA_REPO=~/Documents/GitHub/disciplina`.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/reviziya-dolgov`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/reviziya_dolgov.py`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.** Спецификация — в §2.

| # | файл | зачем |
|---|---|---|
| Я1 | `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md` **целиком** | 🔴 предмет ревизии. Единственный файл, который читается целиком |
| Я2 | всё, что назовёт КОНКРЕТНЫЙ долг | по адресу из самого долга, точечно и только на чтение. Файл, не названный ни одним долгом, открывать не надо |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — статус каждому долгу, и статус доказан командой

### Р1. ПЯТЬ СТАТУСОВ, ЗАКРЫТЫЙ СПИСОК

У каждого долга ровно один:

| статус | когда ставится |
|---|---|
| **МЁРТВ** | проверено командой: описанного дефекта на диске больше нет. Как Д30 и Д31 |
| **ЖИВ** | проверено командой: дефект воспроизводится сегодня |
| **УСТАРЕЛ** | дефект, может, и есть, но **относится к коду или документу, которого больше нет** либо который решением 2026-08-08 переписывается начисто. Пример класса: долги про рукописную вёрстку после появления компилятора |
| **НЕПРОВЕРЯЕМ** | долг сформулирован так, что команды не придумать («улучшить качество», «посмотреть и вобрать полезное»). 🔴 **Это не поражение, это находка:** долг без проверяемой формулировки не закрывается никогда и висит вечно |
| **РЕШЕНИЕ ВЛАДЕЛЬЦА** | не дефект кода, а незакрытый выбор. Проверять нечего, ждёт человека |

🔴 **«МЁРТВ» и «ЖИВ» без команды в строке не принимаются.** Команда должна быть такой, чтобы её
можно было скопировать и повторить, и рядом — её вывод или код возврата. Прочитал файл глазами и
решил — это не проверка, это мнение; ровно так долги и врут.

### Р2. ПРОХОД ПО ВСЕМУ СПИСКУ, БЕЗ ПРОПУСКОВ

Пройти **все** записи `DOLG.md` — Д1…Д54 плюс «Долг 1–4» прошлой арки плюс всё, что найдётся
сверх нумерации. Ни одного пропуска: пропущенный долг неотличим от несуществующего.

Дописать к каждому строку **строго такой формы** (по ней потом соберётся гейт):
```
СТАТУС: <МЁРТВ|ЖИВ|УСТАРЕЛ|НЕПРОВЕРЯЕМ|РЕШЕНИЕ ВЛАДЕЛЬЦА> · 2026-08-08 · <команда> → <вывод/rc>
```
Форма жёсткая намеренно. *Цена, уже оплаченная в этой арке (урок 12): вердикт, написанный
свободной формой, три дня не существовал для гейта — парсер знает конечное число форм, всё прочее
читает как «вердикта нет». Запись была на месте, глазами читалась, гейт молчал.*

Текст самого долга **не трогать**: ни переписывать, ни сокращать, ни удалять даже мёртвый.
Мёртвый долг с датой смерти и командой — это знание; удалённый долг — это дыра, в которую через
неделю снова напишут то же самое.

### Р3. СКРИПТ `_generator/tools/reviziya_dolgov.py` — чтобы это не пришлось делать второй раз

Скрипт читает `DOLG.md` и печатает: сколько долгов всего, сколько в каждом статусе, и **список
тех, у кого статуса нет вовсе**. Только stdlib, детерминизм, exit 1 — если хоть один долг без
статуса.

Это и есть превращение разовой сверки в постоянный чекер: ручная ревизия ловит то, что уже
разъехалось, чекер не даёт разъехаться.

### Р4. 🔴 ЧТО ИЗ ЭТОГО СЛЕДУЕТ — три строки владельцу, простыми словами

В конце отчёта:
1. **Сколько долгов на самом деле живо** — число, а не ощущение;
2. **Какие из живых мешают собрать одну лекцию от интервью до готового дека** — поимённо. Это
   единственный вопрос, который владельца сейчас интересует;
3. **Сколько НЕПРОВЕРЯЕМЫХ** — и предложение по каждому: переформулировать так, чтобы появилась
   команда, либо закрыть как несостоявшийся.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `python3 _generator/tools/reviziya_dolgov.py` даёт **rc=0** — то есть долгов без статуса ноль.
   Это и есть проверяемое условие: пока хоть один пропущен, скрипт краснеет;
2. распределение по пяти статусам напечатано числами; сумма сходится с общим числом долгов;
3. 🔴 **у каждого МЁРТВ и ЖИВ в строке стоит команда.** Проверяется грепом: строк со статусом
   МЁРТВ/ЖИВ ровно столько же, сколько строк со стрелкой `→` внутри статуса;
4. три вывода Р4 написаны простыми словами, без номеров долгов в качестве объяснения.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «долги проверены», а «проверено N из N».

## 3. ВЕРИФИКАТОР

Операция **лоссовая по смыслу**: ты ставишь метки на чужой текст, и неверная метка «МЁРТВ»
хуже отсутствия метки — по ней долг перестанут проверять, а дефект останется.

Поэтому гейт: **свежий субагент берёт 8 долгов, помеченных МЁРТВ, случайно, и перепроверяет их
ДРУГИМ способом** — не повторяя твою команду, а воспроизводя сам дефект по формулировке долга.
Расхождение хоть на одном — назвать в отчёте и снять метку со всех долгов того же класса.

⚠ Если МЁРТВЫХ меньше восьми — берётся сколько есть, и охват называется числом.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/reviziya_dolgov.py`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/reviziya_dolgov.py`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Приёмка «заход ПРИНЯТ» не перепроверяет реальное закрытие названных долгов
ЦЕНА: заход `solver-v3-dyhanie` в таблице маршрутизации ПЛАН ДОБИВАНИЯ (верх DOLG.md) помечен
«✅ ПРИНЯТ 2026-08-08» и заявлен закрывающим Д28 · Д52-svg · Д53 (без оговорки «частично» — она
стоит только у Д54 в той же ячейке). Независимая проверка ЭТИМ ЖЕ ДНЁМ нашла все три живыми:
Д28 (`kegl_dlya_znakov` определена, но нигде не вызывается из компилятора), Д52-svg (структурная
дыра масштабирования SVG внутри панели подтверждена чтением кода — нет `preserveAspectRatio`, нет
проверки «SVG меньше панели»), Д53 (наложение стрелки на подпись на `s08` воспроизведено рендером
в браузере, ~6px). То есть отметка «ПРИНЯТ» разошлась с состоянием кода в день собственной
приёмки без какого-либо интервала на регресс — доверие шло к отчёту исполнителя, а не к
независимой перепроверке каждого заявленного пункта закрытия.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Охват.** Полным проходом `DOLG.md` (398 строк) насчитано 67 записей, требующих статуса:
Д1–Д57 (57, включая уже помеченные ✅ в тексте — им тоже нужна строка строгой формы, датированная
сегодня), Б1–Б5 (5, таблица «БЛОКЕРЫ ДО ПЕРВОЙ СБОРКИ»), «Долг 1–4» прошлой арки (4, у каждого уже
есть отдельный абзац с прозовым статусом от 2026-08-07 — тоже нужна строгая форма), и один дефект
без номера («термин „функтор" употреблён раньше определения» — раздел «Дефект конкретной лекции»,
это находка сверх нумерации, включаю её). 67 = 57+5+4+1.

Разделы «ПЛАН ДОБИВАНИЯ» (З1–З6) и «РЕШЕНИЯ ПРИЁМКИ» (Р-А…Р-З) в счёт не идут: это не долги, а
маршрутизация и уже принятые волевые решения — статусу «долга» они не подлежат по своей природе.

**Конвенция дописывания (решаю сама, в задаче не специфицировано дословно).** Долг живёт либо
строкой markdown-таблицы (Д/Б), либо абзацем под заголовком `## Долг N`. Чтобы не трогать
исходный текст и остаться на ОДНОЙ физической строке файла (это и парсит будущий гейт):
- **Табличная запись** — статус дописывается в конец ТОЙ ЖЕ ячейки/строки таблицы, отделён `<br>`.
- **Долг 1–4** и безномерной дефект лекции — статус дописывается НОВОЙ строкой сразу после
  существующего абзаца (перед `---`), не заменяя прежний текст.

**5 читателей (до 5, как разрешено).** Делю 67 записей на 5 тематических пачек, каждая — отдельный
read-only субагент с Bash: находит команду, гоняет её от корня `~/Documents/GitHub/materials` и
`~/Documents/GitHub/disciplina` (главные папки, не мой worktree — долг живой, если жив в СТВОЛЕ, а
не в чужой невлитой ветке), возвращает готовую строку статуса на каждый долг, ничего не пишет в
файлы:
- **Пачка 1** (18): Д1–Д9, Д11–Д14, Б1–Б5
- **Пачка 2** (14): Д15–Д28
- **Пачка 3** (15): Д29–Д43
- **Пачка 4** (14): Д44–Д57
- **Пачка 5** (6): Д10, Долг 1–4, дефект лекции без номера

Я сама дописываю все 67 строк в `DOLG.md` по возвращённым данным (единая точка записи — меньше
риска конфликта формата), затем пишу `_generator/tools/reviziya_dolgov.py`, гоняю его до rc=0,
затем — верификатор §3 (свежий 6-й субагент, ДРУГИМ способом перепроверяет 8 случайных МЁРТВ),
затем Р4 и коммиты по обоим репозиториям.

**Оспариваемых предпосылок и кривизны критерия готовности не вижу — приступаю без остановки.**

## ВОПРОСЫ — (заполняет исполнитель)

1. Приёмка «заход ПРИНЯТ» доверяет отчёту исполнителя вместо независимой перепроверки заявленных
   закрытий — на `solver-v3-dyhanie` это дало 3 из 3 заявленных-закрытыми долгов (Д28, Д52-svg,
   Д53) живыми в день собственной приёмки. Полный разбор — `## УРОКИ ФАБРИКЕ` выше.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + ЗАЧЕМ.** Прошёл весь список долгов скилла `slajdy` (`DOLG.md`, 398 строк на момент
начала) — 68 записей: Д1–Д57 (57 номеров, но Д52 задвоен как отдельная запись, признанная самим
файлом коллизией — итого 58 табличных строк с номерами Д), Б1–Б5, «Долг 1–4» прошлой арки, плюс
один дефект сверх нумерации (термин «функтор» употреблён раньше своего определения). Задача была
не чинить, а поставить статус, доказанный командой: двенадцать заходов приняты за сутки, каждый
чинил своё не отмечая, и доля мёртвых долгов уже подтвердилась заметной — 2 из 2, проверенных
аналитиком наугад перед сборкой захода, оказались мертвы.

**Как проверил.** Пять read-only субагентов параллельно, пачками по 6–19 долгов тематически
(по разделам DOLG.md), каждый гонял РЕАЛЬНЫЕ команды по главным папкам `~/Documents/GitHub/materials`
и `~/Documents/GitHub/disciplina` — не по worktree. Отдельно проверила и назвала предпосылку: у
обоих репозиториев `origin/main` специально устарел на сотни коммитов (пуш туда не делается
решением владельца — этот же факт уже стоит в самом DOLG.md, «Пуш в оба репозитория не делался»),
поэтому «стволом» для ревизии считала текущий чекаут главной папки; рабочее дерево для всех
проверенных путей (`_generator/sborka`, `skeleton`, `teorkat-vvedenie`, `skills/slajdy`) было
чистым — незакоммиченные правки лежали только в несвязанных зонах (`_fond/zadachi/bank/` и т.п.).
Второй экземпляр Д52 (про каталог `konvejer/`, номер задвоен и не попал ни в одну из пяти пачек)
проверила лично: каталог существует со всеми `DOK.md`, гейты названы G1–G4 как положено — мёртв.
Все 68 строк статуса дописаны в `DOLG.md` дополнением к тем же строкам/абзацам — исходный текст
долгов не тронут ни в одном месте (проверено диффом построчно). Написан
`_generator/tools/reviziya_dolgov.py` + фикстура контракта инструмента (`fixtures/reviziya_dolgov/PROGNAT.sh`,
обязательна была для коммита — pre-commit хук покраснел на моём же новом файле, почина, не обошла);
скрипт даёт rc=0 на боевом `DOLG.md` (68/68 со статусом) и rc=1 на копии с одним снятым статусом
(негативный тест, сама проверила перед коммитом).

**Верификатор §3.** Свежий 6-й субагент адверсариально перепроверил 8 СЛУЧАЙНЫХ из 20 МЁРТВ
(Д12, Д24, Долг3, Д52-konvejer, Д29, Долг4, Долг2, Д50) — ДРУГИМ методом, не повторяя мои команды:
собирал живые фикстуры (например реально вставил длинный текст в блок и прогнал гейт вмещения на
Д29 вместо повторения моего грепа), читал код целиком, а не по ключевому слову. Расхождений не
нашла ни одного — 8 из 8 подтверждены МЁРТВ. Охват — 8 из 20 (40%).

**Что НЕ трогал.** Ни один код-файл `skills/slajdy/` или `_generator/` не менялся, кроме нового
`reviziya_dolgov.py` и его фикстуры. Текст самих долгов в `DOLG.md` не переписывался, не сокращался,
не удалялся — только дописан статус. `SKILL.md`, `references/**`, `_generator/skeleton/**`,
`_generator/sborka/**` — только читала.

### Р4 — три строки владельцу
1. **Реально живых долгов: 41 из 68** (число со скрипта `reviziya_dolgov.py`, не ощущение). Ещё
   20 мертвы, 2 устарели (относятся к переписываемому решением 2026-08-08), 2 непроверяемы,
   3 — решения владельца, не код.
2. **Какие из живых мешают собрать одну лекцию от интервью до готового дека — поимённо (21):**
   - *Вход фазы 1 не запускается вовсе:* Б1, Д19, Д33 (`bootstrap_lekcii.py` требует список имён
     слайдов раньше, чем они появляются по скиллу, и не читает `INTERVYU.md`), Д42 (прикидка
     объёма путает минуты речи со словами на слайде — разница в 19 раз), Д51 (поле замера темпа
     никуда не пишется), Б5 (пейсинг для 20 минут даёт одновременно 3, 10 и 49 слайдов)
   - *Карточка слайда не проходит свой же гейт:* Б3, Б4 (канонический пример из документации
     красный на боевом гейте; строка приёмки роняет гейт трейсбеком), Д38 (гейт не умеет
     проверять карточку по фазам), Д39, Д49 (у «центрального блока» и «типа идеи слайда» нет
     места в формате карточки — решения фазы 1 негде записать)
   - *Скелет не отображает то, что порождает генератор:* Д44 (скрытие ответа до клика не
     работает — виден сразу), Д17 (список внутри блока со сценой схлопывается в абзац), Д27
     (номера слайда нет), Д47 (потолок «≤3 сцены» превышен на живой фикстуре)
   - *Два дерева лекции всё ещё живут одновременно:* Д40 (промпты фаз 4, 6, 7 держат старое
     дерево `src/slides`), Д41 (два похожих по имени bootstrap-скрипта, канон не назван)
   - *Регресс уже принятого захода `solver-v3-dyhanie`* (см. `## УРОКИ ФАБРИКЕ`): Д28, Д52-svg,
     Д53 заявлены закрытыми, но живы; Д54 содержательно непроверяем, пока Д56 (невалидный
     столбец «солвер» в `REZULTATY.md`) не почищен
3. **НЕПРОВЕРЯЕМЫХ — 2.** Д6 («вобрать полезное из `frontend-slides`») — переформулировать в
   список конкретных функций для переноса (тогда появится команда-грep), либо закрыть как
   несостоявшийся. Д48 («обратное направление верификации не покрыто») — методологическая заметка
   о слепой зоне верификатора, не долг конкретного файла; переформулировать в тест «N синтетических
   новых правил вне корпуса, проверить, ловит ли верификатор их появление», либо закрыть.

**Открытое «возвращаться».** Второй экземпляр Д52 не был назначен ни одной из пяти пачек на старте
(в ПЛАНе я насчитала 67, реальный итог — 68 — расхождение объясняю здесь, чтобы не потерялось: сама
DOLG.md признаёт коллизию номеров Д52–Д54, я её недоучла при первом подсчёте и поймала только по
возврату Пачки 4). Итог не пострадал — проверила лично и отдельно, но за подсчётом на будущее стоит
следить внимательнее при повторной ревизии.

**Время прогона + токены:** снимает приёмка из лога прогона (см. `result`-строку).

**НЕОБРАТИМОЕ:** необратимого нет.

**АРТЕФАКТ:** артефакта нет: заход аналитический (ревизия долгов), не собирает HTML/PDF/документ.
Результат — правки на диске: `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/DOLG.md`
(68 строк статуса дописаны) и `/Users/ivanyakovlev/Documents/GitHub/materials/_generator/tools/reviziya_dolgov.py`
+ `/Users/ivanyakovlev/Documents/GitHub/materials/_generator/tools/fixtures/reviziya_dolgov/PROGNAT.sh`
(новый чекер с фикстурой контракта).

**КОММИТ:**
- `materials` (worktree `zahod/reviziya-dolgov`): `80e2aa1` — «reviziya-dolgov: чекер статусов
  DOLG.md + фикстура контракта инструмента» · `git_zona.py check --zone _generator/tools/reviziya_dolgov.py` → ✅
- `disciplina`: `d99fdd5` — «slajdy: ревизия долгов — статус проверен командой у всех 68 записей
  DOLG.md (20 мёртв · 41 жив · 2 устарел · 2 непроверяем · 3 решение владельца)» ·
  `GIT_ZONA_REPO=~/Documents/GitHub/disciplina git_zona.py check --zone skills/slajdy/DOLG.md` → ✅
  (ветку не вливал — влитие делает приёмка)
