# Канал исполнителя — final-zakrytie-nochi (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — всё расписано до команд и хэшей, работа организационная. Субагенты не нужны.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.**

КОНТЕКСТ. Фабрика научно-популярных лекций. Ночная сессия закончена: **принято четырнадцать
заходов**, фаза 1 лекции 2 по теории категорий пройдена владельцем вживую, первый слайд собран
насквозь и разобран, тринадцать помарок владельца закрыты кодом и дисциплиной.

🔴 **Но состояние на диске разошлось с состоянием в git, и разошлось в трёх местах сразу:**
- **четыре ветки приняты и НЕ влиты** — `gejty-vrut`, `pomarki-slajd`, `zamykanie-reestrov` и
  старая `konsolidaciya-i-git-kontur`;
- **работа аналитика за сессию не закоммичена** — передача, помарки, уроки, восемь файлов-заходов
  с отчётами;
- **работа интервьюера в лекции L2 не закоммичена** — четыре карточки и `RAZMETKA-L2.md`.

Один раз за эту ночь слияние уже **сорвалось именно из-за этого**: `git_zona.py` отказал, потому что
четыре пути были одновременно грязными и участвовали в слиянии. Исполнитель поступил верно —
не стал ни коммитить чужое под своим сообщением, ни откатывать. **Порядок в §2 составлен так, чтобы
это не повторилось: сначала коммит, потом слияние.**

ЦЕЛЬ: свести ночь в одно состояние — чтобы завтра можно было начать с чистого дерева и верных чисел.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее: ты НИЧЕГО НЕ ЧИНИШЬ и не правишь содержание.** Заход
организационный: коммит, слияние, уборка, замер. Увидел дефект — строка в отчёт, не правка.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/_INFRA-git/INCIDENTY.md` `teorkat-vvedenie/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_final-zakrytie-nochi.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- 🔴 **ТОЧКА ОТКАТА — ПЕРВОЙ КОМАНДОЙ, до всего.** Запиши хэш ствола в `## ПЛАН`:
  `git --no-optional-locks rev-parse arka/mat-kostyak`. На момент сборки захода — `548c024`.
  Это единственное, чем откатывается всё сведение.
  🔴 **Ветки `zahod/*` НЕ УДАЛЯТЬ ни одну** — решение владельца. Убираются только рабочие ПАПКИ.
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.** Спецификация — в §2.

| # | файл | зачем |
|---|---|---|
| Я1 | `_generator/tools/git_zona.py` — подкоманды `merge`, `doctor`, `worktree`, `check`, `commit` | единственная дверь к git |
| Я2 | `_studio/docs/kak-delat/GIT-disciplina.md §3` | форма коммита и цена каждого хода |
| Я3 | вывод `git --no-optional-locks status --porcelain` | 🔴 **главный источник правды о том, что коммитить.** Списки в §2 — ориентир, живой `status` — закон |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — четыре хода, и ПОРЯДОК ВАЖНЕЕ СОДЕРЖАНИЯ

🔴 **Сначала коммит, потом слияние. Не наоборот.** Один раз за эту ночь слияние сорвалось ровно из-за
обратного порядка: `git_zona.py` отказал, потому что четыре пути были одновременно грязными и
участвовали в слиянии. Коммит снимает эту блокировку.

### З1. ТРИ КОММИТА В `materials` — раздельно, по авторам работы

**(а) Работа аналитика за сессию.** Ожидаемый состав — сверь с живым `status`, он закон:
```
_studio/docs/KARTA.md                                   (изменён)
_studio/zhurnal/_INFRA-git/INCIDENTY.md                 (изменён)
_studio/zhurnal/2026-08-07_arhitektura-slajdov/OCHERED.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/PEREDACHA-2026-08-09.md      (новый)
_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-2026-08-09.md        (новый)
_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FAZ-1-2.md             (новый)
_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_*.md                     (восемь новых + правленые)
```
🔴 **Содержание этих файлов НЕ править** — чужой текст, ты его только сохраняешь.

**(б) Работа интервьюера в лекции L2** — отдельным коммитом, потому что это другая работа и другой
автор:
```
teorkat-vvedenie/L2/slajdy/{bazis-i-matricy,fundamentalnaya-gruppa,napominanie,teorema-brauera}/slaid.md
teorkat-vvedenie/RAZMETKA-L2.md
```
⚠ Это пересборка блоков по помаркам владельца — **56 блоков сведены к 37**, работа живая и ценная.

**(в) 🔴 ЧУЖОЕ НЕ ПОДМЕТАТЬ.** В дереве лежат грязными `_fond/zadachi/bank/**`,
`_generator/build_listok.py`, `obzory/**`, `ucheniki/**` — это **другая работа владельца, не наша**.
Коммить **только явными путями**, никогда `-A`. После каждого коммита `git show --stat` — обязаны
быть только твои пути.

**КРИТЕРИЙ З1:** `git status --porcelain -- _studio/ teorkat-vvedenie/` пуст; `_fond/`, `obzory/`,
`ucheniki/`, `build_listok.py` **остались грязными** — их не тронули. Оба факта показать командой.

### З2. ВЛИТЬ ЧЕТЫРЕ ВЕТКИ — по одной, с кодом возврата после каждой

```
python3 _generator/tools/git_zona.py merge zahod/gejty-vrut          ; echo "rc=$?"
python3 _generator/tools/git_zona.py merge zahod/pomarki-slajd       ; echo "rc=$?"
python3 _generator/tools/git_zona.py merge zahod/zamykanie-reestrov  ; echo "rc=$?"
```
Все три приняты приёмкой. ⚠ **`zahod/konsolidaciya-i-git-kontur` НЕ трогай** — она старше этой арки
и разбирается отдельно; её история держится только `origin/*`.

✅ **Конфликтов быть не должно — проверено приёмкой перед выдачей, а не предположено.**
`merge-tree` прогнан и со стволом, и **попарно между всеми тремя ветками**: чисто во всех шести
сочетаниях. Хэши на момент выдачи: `gejty-vrut` `8fc97e6` · `pomarki-slajd` `22bd8eb` ·
`zamykanie-reestrov` `e9012e6`, ствол `548c024`.

🔴 **Если конфликт всё-таки будет — НЕ разрешай.** Останови, назови файл и строки в `## ВОПРОСЫ`:
обе стороны — принятая работа, склеить наугад значит тихо потерять половину чьего-то захода. И
скажи это прямо: значит дерево изменилось между выдачей захода и его запуском.

**КРИТЕРИЙ З2 — проверка фактом, а не словом инструмента.** После слияний обязаны сойтись все:
```
grep -c "background: none" _generator/skeleton/base.css                    # > 0  (pomarki-slajd)
grep -c "lect-number" _generator/skeleton/engine.js                        # > 0  (pomarki-slajd)
ls _illustracii/proverka_pomarok.py                                        # есть (pomarki-slajd)
ls _generator/tools/schet_nezakrytogo.py                                   # есть (zamykanie)
grep -c "ЗАКРЫТИЕ БЕЗ МЕТКИ" _generator/tools/check_uroki.py               # > 0  (zamykanie)
grep -c "nestabilno" _generator/tools/git_zona.py                          # > 0  (gejty-vrut)
```
Хоть одно не сошлось — слияние неполное, **СТОП**, в отчёт.

### З3. УБРАТЬ РАБОЧИЕ ПАПКИ

`python3 _generator/tools/git_zona.py worktree drop <имя>` по одной.
🔴 **`--force` НЕ применять ни к одной.** Отказ без `--force` означает незакоммиченное внутри — это
**находка**: назови папку и что в ней, папку оставь. За эту ночь так уже спасли строку автолога,
которая иначе исчезла бы.

**КРИТЕРИЙ З3:** `git worktree list` печатает основную папку и те, что честно отказались; каждая
оставшаяся названа в отчёте с причиной.

### З4. ПЕРЕМЕРИТЬ И ПРЕДЪЯВИТЬ ЧИСЛА

```
python3 _generator/tools/git_zona.py doctor
python3 _generator/tools/schet_nezakrytogo.py
python3 _generator/tools/reviziya_dolgov.py ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
```
Ожидание приёмки: `doctor` — потерь нет по всем веткам · долгов ЖИВ **36** · инцидентов без
вердикта **2** · уроков без вердикта **117** по девятнадцати аркам.

🔴 **`schet_nezakrytogo.py` печатает пункты очереди, и его число расходится с ручным счётом
приёмки** (247 у инструмента против 286 и 311 при разных способах счёта). **Не подгоняй.** Назови,
что печатает инструмент, и добавь одной строкой, **что именно он считает** — по какому шаблону и в
каких файлах. Пока способ счёта не назван, число остаётся оценкой; назвать его — и есть работа.

---

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «всё сведено», а «коммитов 3 из 3, влито N из 3,
папок снято M из K».

## 3. ВЕРИФИКАТОР

🔴 **Операция лоссовая:** коммит чужого текста и слияние могут унести работу молча.

- **После коммитов** перечитай состав каждого: `git log -1 --stat` — и сверь построчно со списками
  §2. Расхождение — в отчёт.
- **После слияний** — `doctor`: раздел потерь обязан показать безопасно по всем веткам.
- 🔴 **Гейт на чужое:** `git status --porcelain` после всей работы обязан **по-прежнему** показывать
  грязными `_fond/`, `obzory/`, `ucheniki/`, `_generator/build_listok.py`. Если они исчезли — ты
  подмёл чужую работу своим коммитом, и это надо назвать первой строкой отчёта.
- ⚠ Коммит **этого файла-захода** может потребовать `--no-verify`: новый гейт `check_uroki.py` ловит
  фразы о закрытии, а в §2 они встречаются как инструкция. Обход законный, **но с причиной**, и
  причину назови отдельной строкой отчёта.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/_INFRA-git/INCIDENTY.md` `teorkat-vvedenie/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_final-zakrytie-nochi.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/_INFRA-git/INCIDENTY.md` `teorkat-vvedenie/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_final-zakrytie-nochi.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

- Ветка: `arka/mat-kostyak` (проверено `git branch --show-current`, rc=0) — совпадает с контрактом.
- **Точка отката (первой командой):** `git --no-optional-locks rev-parse arka/mat-kostyak` →
  `548c024596e75c9f7418e813de24e2d22cce4055` (совпадает с заявленным в заходе `548c024`).
- Прочитаны только якоря Я1 (`git_zona.py`), Я2 (`GIT-disciplina.md §3`), Я3 (живой
  `git --no-optional-locks status --porcelain`, снят и сверен ниже).
- 🔴 **Ложная предпосылка найдена в §0:** строка «вливать нечего, проверено при сборке: невлитых
  веток 0» противоречит §2 З2 и живому репо. Проверено `merge-base --is-ancestor` для всех четырёх
  веток `zahod/*` из §2 — ни одна НЕ предок `arka/mat-kostyak` (все "NOT ancestor"), хэши голов
  совпадают с заявленными в заходе (`gejty-vrut` 8fc97e6, `pomarki-slajd` 22bd8eb,
  `zamykanie-reestrov` e9012e6, `konsolidaciya-i-git-kontur` не трогаем). Действую по детальной
  спецификации §2 (она внутренне согласована и проверяема фактами), фраза в §0 — вероятно, обрывок
  черновика; фиксирую находку в `## ВОПРОСЫ` как урок фабрике, содержание заходом не правлю.
- Живой `status --porcelain` сверен со списками §2:
  - (а) аналитик — состав из списка §2 подтверждён живым `status` + один файл сверх списка:
    `kod_instrument-podklyuchen.md` (untracked) — по счёту «восемь новых kod_*.md» это восьмой
    (`kod_final-zakrytie-nochi.md` в счёт не идёт — он уходит в §4, коммит зоны исполнителя).
  - (б) интервьюер — живой `status` шире явного списка §2 (десять `slaid.md` вместо четырёх,
    плюс `LENTA-L2/view.html` и untracked `LENTA-L2/faza2.md`). Разрешено КРИТЕРИЕМ З1: он требует
    `git status --porcelain -- teorkat-vvedenie/` пустым целиком — коммичу весь `teorkat-vvedenie/`
    живым списком, список в §2 — ориентир, не исчерпывающий состав.
  - (в) чужое — `_fond/zadachi/bank/**`, `_generator/build_listok.py`, `obzory/**`, `ucheniki/**` —
    не трогаю, остаются грязными.
- Порядок хода: З1 (три коммита: аналитик → интервьюер → сверка) → З2 (три слияния по одному,
  `konsolidaciya-i-git-kontur` не трогаю) → З3 (worktree drop) → З4 (замер) → §4 (коммит своей
  зоны, последний ход).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
