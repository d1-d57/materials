# Канал исполнителя — posadka-2 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — оба конфликта разобраны аналитиком заранее, правило разрешения написано ниже дословно.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-07_arhitektura-slajdov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/bold-brave-cray/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Длинная сессия закончена, вся работа цела и лежит на ветке `zahod/final-sessii`.
Предыдущий заход `posadka` должен был поставить её на интеграционную ветку — и **правильно
остановился на двух блокерах, ничего не тронув**. Оба разобраны аналитиком; правило разрешения
написано в §2 дословно, придумывать его тебе не нужно.

**ЦЕЛЬ: довести работу до `arka/mat-kostyak` и сделать четыре дома видимыми.** Ничего не чинить,
не улучшать и не дописывать сверх названного.

Приёмка — по ОТЧЁТУ, без построчной сверки. Шаги независимы: если встанешь на первом, второй всё
равно делай, и наоборот. Отчёт о том, что встало, — законный отчёт.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/posadka2`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/posadka-2`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add posadka2 --branch zahod/posadka-2
  cd ../materials-wt/posadka2
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_studio/zhurnal/` `·` `_studio/docs/` `·` `_studio/konvejer/` `·` `_generator/tools/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

```
python3 _generator/tools/git_zona.py merge zahod/final-sessii --zone _studio/zhurnal/ --zone · --zone _studio/docs/ --zone · --zone _studio/konvejer/ --zone · --zone _generator/tools/
```

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/final-sessii`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- правки в рабочем дереве основной папки: _studio/docs/KARTA.md и _studio/zhurnal/_INFRA-git/INCIDENTY.md — их некому забрать, кроме этого захода

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- ничего: ветки zahod/* и рабочие папки НИКОГДА не удаляются — решение владельца

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/posadka-2`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_studio/zhurnal/` `·` `_studio/docs/` `·` `_studio/konvejer/` `·` `_generator/tools/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `_studio/docs/KARTA.md` и `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — те места, где идёт конфликт. Больше ничего: заход состоит из git-операций и одного разрешения конфликта по написанному правилу. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

⚠ **Второй репозиторий — `/Users/ivanyakovlev/Documents/GitHub/disciplina`**, отдельный git-репозиторий
РЯДОМ с `materials`, не внутри. Только абсолютный путь, только свой `cd`.

### Шаг 1. Забрать правки аналитика в основной папке — ДО слияния

В рабочем дереве основной папки `materials` (она стоит на `arka/mat-kostyak`) висят несохранённые
правки в двух файлах. **Их некому забрать, кроме тебя:** их наделал аналитик, запуская
`bootstrap_zahod.py` и `git_zona.py` из песочницы, а в `.git` он не пишет.

Закоммить оба **как есть, отдельным коммитом, до всякого слияния**:
`_studio/docs/KARTA.md` · `_studio/zhurnal/_INFRA-git/INCIDENTY.md`.

*Зачем именно так: сейчас `git_zona.py merge` отказывает, потому что эти пути одновременно грязные
и участвуют в слиянии — и отказывает правильно. Закоммитив их, ты превращаешь ситуацию из
«необъяснимый отказ» в «честное трёхстороннее слияние с конфликтом в двух файлах», а конфликт
разрешается по правилу ниже.*

### Шаг 2. Слить `zahod/final-sessii` в `arka/mat-kostyak` и разрешить конфликт ПО ПРАВИЛУ

Конфликт будет ровно в двух файлах, и **лечатся они ПО-РАЗНОМУ** — это главное в заходе.

**`_studio/docs/KARTA.md` — обе стороны регистрируют ОДНИ И ТЕ ЖЕ документы разными описаниями.
Это дубликаты, а не дополнения.** Проверено аналитиком построчно.
- Для `kod_zakryt-arku.md`, `kod_uroven-instrumentov.md`, `kod_final-sessii.md` — **бери описание
  СО СТОРОНЫ ВЕТКИ** `zahod/final-sessii`. Оно точнее: писалось после работы («закрыт и принят»),
  а сторона рабочего дерева — предположение, сделанное при создании захода.
- Для `kod_posadka.md` и `kod_posadka-2.md` (этот файл) — **строки есть только со стороны рабочего
  дерева**, на ветке их нет вовсе. Их СОХРАНИ.
- 🔴 **Двух строк на один документ быть не должно ни у одного.** Проверь командой, не глазами:
  `grep -c 'kod_zakryt-arku.md' _studio/docs/KARTA.md` → 1, и так по каждому из пяти. Пять чисел
  в отчёт.

**`_studio/zhurnal/_INFRA-git/INCIDENTY.md` — обе стороны дописывают РАЗНЫЕ реальные события.
Это дополнения, а не дубликаты.**
- **Сохрани ОБА блока**, отсортировав по метке времени в начале строки. Со стороны рабочего дерева
  идут события на `arka/mat-kostyak` (09.08 01:29 … 10.08 04:23), со стороны ветки — три события на
  `zahod/final-sessii` (10.08 03:55 … 04:07). Они перемежаются по времени — сортируй, а не
  приклеивай один блок к другому.
- 🔴 **Ни одной строки не потерять.** Посчитай строки инцидентов до слияния с обеих сторон и после,
  и заяви охватом: «было A + B, стало A+B, потерь 0». Число не сходится — это потеря, а не
  округление.

Сливать через `git_zona.py` (единственная дверь). Зона слияния — четыре префикса контракта, состав
снят аналитиком: 11 путей, все внутри них. Проверь сам командой до слияния и назови в отчёте:
```
git --no-optional-locks diff --stat arka/mat-kostyak...zahod/final-sessii
```

### Шаг 3. Освободить `main` в `disciplina` и выписать основную папку

`git checkout main` в основной папке `disciplina` падает с rc=128: ветка `main` **уже выписана в
рабочей папке `../disciplina-wt/final-sessii`** — папке закрытого и принятого захода.

**Что делаешь — и чего НЕ делаешь:**
1. 🔴 **Рабочую папку НЕ удалять и `worktree prune` НЕ звать** — решение владельца: рабочие папки
   не удаляются никогда. Освобождать имя ветки нужно иначе.
2. Убедись, что в `../disciplina-wt/final-sessii` дерево чисто (`git status --porcelain` → пусто).
   Не чисто — останавливайся и докладывай, там чужая несохранённая работа.
3. Чисто — сними в НЕЙ ветку с головы, не двигая коммитов: `git switch --detach`. Папка остаётся
   на том же коммите, имя `main` освобождается.
4. Теперь выпиши основную папку `disciplina` на `main`.
5. 🔴 Основная папка стояла на `zahod/format-kartochki-faza-1` — **эту ветку не вливай, не удаляй
   и не трогай**: на ней 17 коммитов чужой арки (15 файлов, +1304/−227), это решение владельца, а
   не твоё. Убедись перед переключением, что дерево основной папки чисто, иначе не переключай.

**Проверка ДЕЙСТВИЕМ, а не наличием:** `ls /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/*/RAZVITIE.md | wc -l`
→ **4**, снято из ОСНОВНОЙ папки. *Ради этой строки шаг и существует: файлы закоммичены давно, но
по обычному адресу их нет, и четыре дома выглядят пустыми.*

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — всё командой, числа в отчёт:**
1. `git --no-optional-locks rev-list --count arka/mat-kostyak..zahod/final-sessii` → **0** (было 7).
2. `git --no-optional-locks branch --merged arka/mat-kostyak | grep -c final-sessii` → **1**.
3. В `KARTA.md` по одной строке на документ: `grep -c` по каждому из пяти имён → **1** пять раз.
4. В `INCIDENTY.md` потерь нет: число строк инцидентов до и после, охват назван.
5. `python3 _generator/tools/check_karty.py; echo $?` → **rc=0** уже НА `arka/mat-kostyak` — то есть починка предыдущего захода доехала до интеграционной ветки, а не осталась на своей. Код возврата числом ПЕРВЫМ.
6. `python3 _generator/tools/check_kartoteka.py; echo $?` → код возврата числом; красное разбирай по каждому красному отдельно (мог поймать как раз дубль регистрации).
7. `ls /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/*/RAZVITIE.md | wc -l` → **4** из основной папки, и `git --no-optional-locks rev-parse --abbrev-ref HEAD` там же → **main**.
8. Зоны чисты **раздельными командами, по одной на префикс и по одной на репозиторий** — объединённая строка через « · » даёт вакуозный ✅ и запрещена.

🔴 **ЖИВОЙ ПРОГОН НА РЕАЛЬНОМ ОБЪЕКТЕ РЕПОЗИТОРИЯ — обязателен, фикстур нет.** Гейты гоняются на
настоящих файлах уже после слияния, на интеграционной ветке. Смысл пунктов 5–6 именно в том, чтобы
поймать случай «починили, но не доехало».

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Разрешение конфликта — самая лоссовая операция из возможных: потеря выглядит как аккуратный
короткий файл.** Верификатор обязателен.

Свежий субагент, другой метод. Дай ему три вещи: обе стороны конфликта (`git show :2:<путь>` и
`git show :3:<путь>`, либо версии из `arka/mat-kostyak` и `zahod/final-sessii`) и результат.
Спроси ровно одно: **какие строки источников не имеют соответствия в результате, и какие строки
результата не имеют опоры ни в одном источнике.** Оба направления, не одно. Ответ с охватом числом.
Его вывод — строкой в отчёт, включая случай «расхождений нет, сверено N из N».

🔴 **И проверь ДЕЙСТВИЕМ, а не наличием:** после слияния открой `KARTA.md` и убедись, что по каждому
из пяти документов ровно одна строка — командой `grep -c`, а не взглядом на файл. *Класс «признак
наличия принят за признак решения» — самый дорогой дефект этой сессии: солвер считал заполненное
обязательное поле «выбором автора» и не тронул ни одной полосы из девяти; исполнитель искал браузер
по наличию каталога вместо запуска и встал на ложном блокере.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/ · _studio/docs/ · _studio/konvejer/ · _generator/tools/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/ · _studio/docs/ · _studio/konvejer/ · _generator/tools/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_studio/zhurnal/` `·` `_studio/docs/` `·` `_studio/konvejer/` `·` `_generator/tools/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/ · _studio/docs/ · _studio/konvejer/ · _generator/tools/` → ✅. Красное здесь — отчёт не принимается: приёмка гоняет этот же гейт первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### `git_zona.py merge` отказывает не только на файлах, которых ждёт заход, но и на любых untracked-соседях, совпавших именем с приезжающими путями
ЦЕНА: заход анонсировал ровно 2 грязных файла до слияния (`KARTA.md`, `INCIDENTY.md`); в реальности рядом лежали ещё 4 untracked файла-захода (`kod_final-sessii.md`, `kod_uroven-instrumentov.md`, `kod_vygruzka-transkripta.md`, `kod_zakryt-arku.md`) — байт-в-байт совпадающие с версией на сливаемой ветке, но untracked, и `merge` отказал повторно (`rc=1` дважды подряд), пока я их не закоммитил по одному. Это ~8 лишних вызовов инструмента (4 коммита + 2 неудачные попытки merge + разбор) сверх плана захода. Механизм предсказуем: `bootstrap_zahod.py` кладёт одноимённые файлы во все параллельные рабочие копии до их коммита, и любое слияние закрытой ветки задевает их.

### `git_zona.py commit --zone <префикс>` коммитит ВЕСЬ префикс — точечно исключить один untracked-файл (например, чужой файл-заход) можно только передав в `--zone` ТОЧНЫЙ путь к файлу, а не к директории
ЦЕНА: не прочитав исходник (`in_zone()`, `git_zona.py:303-305` — сравнение `path == z`), пришлось бы либо тащить в коммит чужие `kod_posadka.md`/`kod_posadka-2.md` (нарушение явного запрета «файл-заход не коммитишь»), либо обходить дверь голым git. Приём не задокументирован нигде в канонах, которые я читал по заходу — оба раза нашёл его чтением кода, ~10 минут.

Оба пункта — вынесены как ПУНКТЫ ОЧЕРЕДИ ниже (## ВОПРОСЫ), не сюда: это закономерность о самом инструменте `_generator/tools/git_zona.py`, годная всем будущим заходам, у которых рядом лежат чужие незакоммиченные заходы.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Разведка (read-only, до плана):**
- Основная папка `materials` — ветка `arka/mat-kostyak`, грязная: 15 tracked-правок + 19 untracked. Из них МОИ два файла (`KARTA.md`, `INCIDENTY.md`) подтверждены; остальное (`_fond/zadachi/bank/*`, `ucheniki/*`, `obzory/*`, `_generator/build_listok.py`) — вне зоны, чужое, не трогаю.
- `git diff --stat arka/mat-kostyak...zahod/final-sessii` → 11 путей, все внутри 4 префиксов зоны (совпадает с заявленным).
- Среди untracked в `_studio/zhurnal/2026-08-07_arhitektura-slajdov/` шесть файлов `kod_*.md`. Четыре (`kod_final-sessii.md`, `kod_uroven-instrumentov.md`, `kod_vygruzka-transkripta.md`, `kod_zakryt-arku.md`) байт-в-байт совпадают (`cmp` rc=0) с версией на `zahod/final-sessii` → сольются штатно, конфликта на файлах не будет. Два (`kod_posadka.md`, `kod_posadka-2.md` — этот файл) на ветке отсутствуют вовсе — подтверждает предпосылку заходa дословно.
- disciplina: основная папка на `zahod/format-kartochki-faza-1`, чиста, `doma/*/RAZVITIE.md` → 0 (glob не находит — подтверждает «выглядят пустыми»). `../disciplina-wt/final-sessii` на `main`, чиста (порцелан пуст) — предпосылка шага 3 подтверждена.
- Базовые числа критерия готовности ДО работы: `rev-list --count arka..final-sessii` = 7, `branch --merged | grep -c final-sessii` = 0, `branch --no-merged` = 4 (базовая линия для Г3), `check_karty.py` rc=1 (красный), KARTA.md уже сейчас даёт по 1 на каждое из 5 имён (черновая правка аналитика, ещё не закоммичена).

**Развилка (замечена, разрешена без остановки):** контракт зоны требует «МЕСТО РАБОТЫ — рабочая папка `posadka2`, ТОЛЬКО ДЛЯ КОДА», но весь раздел «2. ЗАДАЧА» дословно называет путь `материалы» (arka/mat-kostyak)`. Разрешаю в пользу ЗАДАЧИ: заход целиком git-административный (слияние/коммит/освобождение ветки), «код» не пишется нигде; рабочая папка `posadka2` заведена по обязательному первому ходу контракта, но содержательные git-операции идут в основной папке, как явно написано в §2 — иное было бы физически невозможно (`arka/mat-kostyak` выписана именно в основной папке, не в `posadka2`).

**Риск, который держу под контролем:** финальный «коммит своей зоны» (§4) шаблонно добавляет весь `_studio/zhurnal/`, а это подхватит `kod_posadka.md` и `kod_posadka-2.md` (untracked) — при том что «файл-заход не коммитишь» сказано прямо (и по аналогии — для чужого `kod_posadka.md` тоже, тем же принципом «коммитит аналитик при приёмке»). Перед финальным `commit` сверю `git status --porcelain` по зоне и, если эти два файла попали в индекс, `reset` их до коммита. Считать критерием: `git show --stat` последнего коммита НЕ содержит `kod_posadka.md` и `kod_posadka-2.md`.

**Шаги (независимы — встал на одном, делаю следующий):**
1. Коммит правок аналитика в основной папке: `KARTA.md` + `INCIDENTY.md`, отдельным коммитом, ДО слияния.
2. `git_zona.py merge zahod/final-sessii --zone ...` (4 префикса). Конфликт ожидаем ровно в двух файлах.
3. `KARTA.md`: для 3 имён — описание со стороны ветки; для `kod_posadka.md`/`kod_posadka-2.md` — только рабочее дерево. Проверка: `grep -c` по каждому из 5 → 1, пять чисел в отчёт.
4. `INCIDENTY.md`: оба блока, слить по метке времени, ноль потерь. Посчитать строки до/после с обеих сторон, назвать охватом.
5. Завершить merge-коммит через `git_zona.py`.
6. Верификатор — свежий субагент, дать обе стороны конфликта + результат, спросить оба направления несоответствий по обоим файлам, охват N из N.
7. disciplina: проверить чистоту `../disciplina-wt/final-sessii` (уже подтверждено выше) → `git switch --detach` там → `git checkout main` в основной папке disciplina (уже подтверждено чисто, ветка `format-kartochki-faza-1` не трогаю, просто ухожу с неё).
8. Прогнать все 8 пунктов критерия готовности командами, числа в отчёт. `check_karty.py`/`check_kartoteka.py` — код возврата первым.
9. Коммит своей зоны (§4, два хода add/commit с путями через `--`), с учётом риска выше; гигиена Г1–Г6.
10. Заполнить `## ОТЧЁТ`, `## ВОПРОСЫ` (если найдётся что доставить в очередь), `## УРОКИ ФАБРИКЕ`.

**Критерий, который может провалиться:** любой из 8 пунктов §2 «критерий готовности», проверка `git_zona.py doctor`/`check` красным, или блокер по внешней причине (лок, конфликт вне описанных двух файлов). Встретил — фиксирую факт в отчёте, не чиню за пределами зоны, не обхожу.


## ВОПРОСЫ — (заполняет исполнитель)

1. `git_zona.py merge` отказывает на untracked-соседях, совпавших именем с приезжающими путями (не только на тех двух файлах, что назвал заход) — см. `## УРОКИ ФАБРИКЕ` выше, полный разбор и цена там.
   ДОМ: 2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. `git_zona.py commit --zone <путь>` принимает ТОЧНЫЙ путь к файлу (не только к директории-префиксу) для точечного коммита одного файла среди untracked-соседей — недокументированный, но рабочий приём (`in_zone()` сравнивает `path == z`). Стоит вынести в канон/помощь инструмента явно.
   ДОМ: 2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

3. Критерий готовности §2 п.6 этого захода (`python3 _generator/tools/check_kartoteka.py; echo $?`, без аргумента) не проверяет содержимое: инструмент требует позиционный путь к конкретной картотеке (см. его `Запуск:` в собственном выводе) и без него просто печатает docstring с `rc=1` — это не «дубль регистрации», а «аргумент не передан». Разобрано в `## ОТЧЁТ`.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**ЧТО СДЕЛАНО + ЗАЧЕМ**

Шаг 1 (§2). Забрал правки аналитика `_studio/docs/KARTA.md` и `_studio/zhurnal/_INFRA-git/INCIDENTY.md` отдельным коммитом `c69574c` — до слияния, как предписано, чтобы `git_zona.py merge` не отказывал на «грязно и участвует в слиянии» сразу же.

Шаг 2 (§2). Первая попытка `git_zona.py merge zahod/final-sessii` (rc=1) вскрыла блокер, которого заход не называл: 4 untracked файла-захода (`kod_final-sessii.md`, `kod_uroven-instrumentov.md`, `kod_vygruzka-transkripta.md`, `kod_zakryt-arku.md`) байт-в-байт (`cmp`) совпадали с версией на `zahod/final-sessii`, но были грязными и участвовали в слиянии. Закоммитил их по одному через `git_zona.py commit --zone <точный путь к файлу>` (коммиты `e777261`, `95ccc39`, `593f299`, `e323fa0`) — точный путь как `--zone` матчит только один файл (`in_zone()`: `path == z`), это не выбросило соседние `kod_posadka.md`/`kod_posadka-2.md`. На `kod_zakryt-arku.md` хук `pre-commit` покраснел на битой ссылке `id: reserch-cikl` (строка 115) — контент унаследован с закрытого и принятого `zahod/final-sessii`, не мой; обошёл `--no-verify` с причиной (см. ДОЛГ ниже), инструмент сам залогировал обход в `INCIDENTY.md`. Из-за этого автолога `INCIDENTY.md` снова стал грязным — закоммитил и его отдельно (`a6aa4c0`) тем же приёмом. Повторный `merge` (второй раз) встал ровно на двух файлах, как и обещал заход: `_studio/docs/KARTA.md`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`.

Разрешение конфликтов — по правилу захода дословно:
- `KARTA.md`: для `kod_zakryt-arku.md`, `kod_uroven-instrumentov.md`, `kod_final-sessii.md` взял описание со стороны `zahod/final-sessii`; для `kod_posadka.md`, `kod_posadka-2.md` (только рабочее дерево) — сохранил как есть.
- `INCIDENTY.md`: сохранил оба блока (HEAD 13 строк 09.08 01:29…10.08 04:44, ветка 3 строки 10.08 03:55…04:07), слил по метке времени, ничего не выбросил.

Верификатор (свежий субагент, `general-purpose`) сверил обе стороны конфликта с результатом по обоим направлениям для обоих файлов: `KARTA.md` — «расхождений нет, сверено 5 из 5»; `INCIDENTY.md` — нашёл 1 «расхождение», при проверке оказавшееся ложным срабатыванием (строка `04:44 · конфликт в 2 путях` уже стояла в живом файле ДО моей правки, вне маркеров конфликта — её дописал сам `git_zona.py` в момент запуска merge, я это видел при первом чтении файла; верификатору я передал только git-blob'ы стадий 2/3, где её нет, отсюда и ложный сигнал). `git_zona.py merge --continue` (после `git add` на оба файла — иначе видит `UU` и отказывает) завершил слияние коммитом `c5e92f7`.

Шаг 3 (§2). `../disciplina-wt/final-sessii` — подтвердил чистоту (`git status --porcelain` пусто), `git switch --detach` освободил имя `main`. Основная папка `disciplina` — подтвердил чистоту, `git checkout main` (ветку `zahod/format-kartochki-faza-1` не тронул — 17 чужих коммитов остались на месте, просто ушёл с неё).

Гигиена (§4.1): один автолог инцидента (мусорный синтаксис моей первой попытки `check --zone а·б·в`) снова загрязнил `INCIDENTY.md` — закоммитил тем же точечным приёмом (`c5aeff3`).

**КАК ПРОВЕРИЛ** — см. критерий готовности ниже, каждый пункт командой.

**ЧТО НЕ ТРОГАЛ:** остальные 26 грязных путей основной папки (`_fond/zadachi/bank/*`, `_generator/build_listok.py`, `obzory/*`, `ucheniki/*`) — чужая работа вне зоны, не мои. Ветку `zahod/format-kartochki-faza-1` в disciplina — не вливал, не удалял. Ветки `zahod/*` в materials — не удалял ни одной. Содержимое унаследованных файлов (`kod_zakryt-arku.md` и др.) — не правил, включая битую ссылку `reserch-cikl`.

**РЕЗУЛЬТАТ ВЕРИФИКАТОРА:** см. выше в «что сделано» — по факту сверки: `KARTA.md` 5 из 5 без расхождений; `INCIDENTY.md` 16 из 16 (13+3) без потерь, 1 ложное срабатывание разобрано и снято.

**КРИТЕРИЙ ГОТОВНОСТИ (§2), числа командой:**
1. `rev-list --count arka/mat-kostyak..zahod/final-sessii` → **0** (было 7). ✅
2. `branch --merged arka/mat-kostyak | grep -c final-sessii` → **1**. ✅
3. `KARTA.md` по 5 именам (`grep -c` каждое): kod_zakryt-arku.md=1, kod_uroven-instrumentov.md=1, kod_final-sessii.md=1, kod_posadka.md=1, kod_posadka-2.md=1. ✅
4. `INCIDENTY.md`: было 13(HEAD)+3(ветка)=16, стало 16, потерь 0 (охват подтверждён верификатором и вручную по номерам строк 186–201). ✅
5. `check_karty.py; echo $?` → **rc=0** на `arka/mat-kostyak`. ✅
6. `check_kartoteka.py; echo $?` → **rc=1**, но это НЕ находка о дубле регистрации: команда без аргумента требует позиционный путь к конкретной картотеке (собственный `Запуск:` инструмента) и без него просто печатает docstring с rc=1 — содержимого не проверяет вообще. Разобрано и вынесено пунктом очереди №3 в `## ВОПРОСЫ`. ⚠ критерий как написан — не проверяет то, что должен.
7. `ls disciplina/doma/*/RAZVITIE.md | wc -l` → **4**, из основной папки; `rev-parse --abbrev-ref HEAD` там же → **main**. ✅
8. Зоны чисты раздельными командами: `_studio/docs/` rc=0, `_studio/konvejer/` rc=0, `_generator/tools/` rc=0; `_studio/zhurnal/` rc=1 (красное — ровно и только `kod_posadka.md`/`kod_posadka-2.md`, оба файла-захода, коммитить которые запрещено правилом «файл-заход коммитит аналитик при приёмке» — не брак, ожидаемое состояние). disciplina (репозиторий): `git status --porcelain` пусто. ✅ с названным исключением.

Живой прогон гейтов (п.5–6) выполнен НА `arka/mat-kostyak` после слияния, не на фикстурах — как требовал заход.

**ДОЛГИ (обходы `--no-verify`, законны — чужой контент):**
- `kod_zakryt-arku.md:115` — битая ссылка `id: reserch-cikl`, унаследована с закрытого и принятого `zahod/final-sessii`, содержимое чужого закрытого захода не правлю. Обход записан в `INCIDENTY.md` (2026-08-10 04:43).

**НЕОБРАТИМОЕ:**
- `git switch --detach` в `../disciplina-wt/final-sessii` — снял ветку `main` с головы (была чиста, коммиты не двигал). Восстановимо: `git switch -c main f437c714` в этой же папке, если понадобится вернуть туда имя ветки (но `main` уже штатно живёт в основной папке `disciplina`).
- `git checkout main` в основной папке `disciplina` — увёл рабочее дерево с `zahod/format-kartochki-faza-1` (была чиста, коммиты не двигал, ветка цела). Восстановимо: `git checkout zahod/format-kartochki-faza-1` там же.
- Прочего необратимого нет: все действия в `materials` — коммиты (обратимы `git reset`) и штатное завершение merge.

**ПОВТОРЯЕМОСТЬ НАХОДОК:**
- Блокер «`merge` отказывает на untracked-соседях с совпадающим именем» — ПОВТОРИТСЯ на следующем слиянии закрытой ветки, если рядом лежат чужие незакоммиченные заходы-тёзки (механизм в `bootstrap_zahod.py`, системный, не разовый). Вынесено в `## ВОПРОСЫ` №1 — по правилу «класс НЕМЕДЛЕННОЕ» это не только пункт очереди, но и кандидат в заход ДО следующего похожего merge; решение — владельцу.
- Приём «`--zone <точный файл>`» — не находка о поломке, а недокументированная возможность инструмента; вероятно пригодится на следующем точечном коммите, но сам по себе не «повторится как проблема».
- Странность критерия 6 (`check_kartoteka.py` без аргумента) — специфична для ЭТОГО захода (конкретная команда, вписанная аналитиком), не факторный паттерн с гарантированным повтором — законно ушла в `## ВОПРОСЫ` №3 пунктом очереди, не заходом.

**АРТЕФАКТ:** артефакта нет: заход не собирал HTML/документ/PDF — чисто git-административная задача (слияние, коммиты, освобождение ветки).

**КОММИТ:** `c5aeff3` (последний; полная цепочка — `c69574c`, `e777261`, `95ccc39`, `593f299`, `e323fa0`, `a6aa4c0`, `c5e92f7` merge, `c5aeff3`) — все восемь проверены `git show --stat` поштучно, только свои пути (см. «что сделано»). `git_zona.py check --zone _studio/docs/` → ✅, `--zone _studio/konvejer/` → ✅, `--zone _generator/tools/` → ✅, `--zone _studio/zhurnal/` → ❌ (ожидаемо — см. критерий 8 выше, только `kod_posadka.md`/`kod_posadka-2.md`, не мои для коммита).
