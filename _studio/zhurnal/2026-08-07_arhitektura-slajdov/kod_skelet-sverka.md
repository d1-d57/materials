# Канал исполнителя — skelet-sverka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — работа механическая и полностью описана: собрать два множества имён,
> вычесть, починить разницу. Субагенты не нужны.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.**

КОНТЕКСТ. Фабрика научно-популярных лекций. Слайд собирается так: компилятор
(`_generator/sborka/`) читает карточку `slaid.md` и **порождает HTML** с классами и CSS-токенами;
скелет (`_generator/skeleton/` — `base.css`, `tokens.css`, `engine.js`, `shablon.html`) эти имена
**потребляет**: раскрашивает, расставляет, показывает по сценам.

🔴 **Класс отказа, ради которого заход существует: «разметка порождается, но не работает».**
Компилятор напечатал имя, которого скелет не знает — и **ничего не ломается**. Абзац отрендерится
обычным, иллюстрация станет чёрной (неопределённая CSS-переменная = чёрная заливка), сцена не
переключится. **Все гейты при этом зелёные**, потому что каждый из них проверяет свою сторону
отдельно. Владелец видит это за час до лекции и говорит «всё некрасиво».

ЦЕЛЬ: узнать **число** — сколько имён порождается вхолостую, — и починить те, что действительно
сломаны.

⚠ **Список симптомов в `DOLG.md` (Д30, Д31, Д32, Д27, Д17, Д44, Д47) СТАРЫЙ и частично мёртвый.**
Аналитик проверил два из них командой прямо перед сборкой этого захода:
`grep -c` в `_generator/skeleton/base.css` находит `.formula` 6 раз, `.spisok` 4, `.op-utv` 6,
`.lst` 3, `.tlist` 7 — то есть Д30 («не стилизованы вовсе») **уже неверен**; `--hollow` и
`--steel-d` в `_generator/skeleton/tokens.css` **присутствуют** — Д31 тоже. Значит долги закрывались
по дороге и никто не отмечал. **Не чини по списку. Считай по файлам.**

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее: первый ход — СЧЁТ, а не починка.** Если счёт покажет, что
дыр мало или их нет вовсе, — это **успешный** заход и правильный результат, а не пустой. Худший
исход — начать чинить по устаревшему списку и «починить» то, что работает.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/skelet-sverka`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/skelet-sverka`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add skelet-sverka --branch zahod/skelet-sverka
  cd ../materials-wt/skelet-sverka
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/skeleton/**`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/skelet-sverka`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/skeleton/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.** Спецификация — в §2.

| # | файл | зачем |
|---|---|---|
| Я1 | `_generator/sborka/*.py` — **READ-ONLY, только читать** | 🔴 сторона «порождает»: отсюда собирается множество имён. Компилятор НЕ ПРАВИТЬ ни строкой, он вне зоны |
| Я2 | `_generator/skeleton/{base.css, tokens.css, engine.js, shablon.html}` | сторона «потребляет» — и твоя зона |
| Я3 | `teorkat-vvedenie/src/{tokens.css, overlay.css}` — READ-ONLY | живой дек: чем он отличается от общего скелета. Расхождение и есть источник «чёрных иллюстраций» |
| Я4 | `_generator/tools/fixtures/sborka/zhivoj-teorkat/` | на ней собираешь и смотришь глазами |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — сначала счёт, потом починка, потом глаз

### С1. 🔴 СЧЁТ — таблица «порождается · потребляется · вердикт»

Собрать **четыре множества имён** и вычесть попарно:

| сторона | что собрать | откуда |
|---|---|---|
| порождается | CSS-классы, которые компилятор печатает в HTML | литералы `class="…"` и подстановки в `_generator/sborka/*.py` |
| потребляется | CSS-классы, на которые есть правило | селекторы в `skeleton/base.css` (+ `katex.css`) |
| порождается | CSS-переменные `--имя`, на которые ссылается `var(--имя)` в порождаемом и в скелете | `sborka/*.py`, `skeleton/*.css` |
| потребляется | CSS-переменные, которые где-то **объявлены** | `skeleton/tokens.css`, `:root` и прочие блоки |

Плюс отдельно — **синтаксис сцен**: какие формы разметки сцен компилятор порождает
(`{blur@N}`, `{@N}`, `{@-N}` и что там ещё есть — собрать по коду, не по памяти) и какие из них
`skeleton/engine.js` действительно обрабатывает.

🔴 **Собирать программой, а не глазами.** Разовый скрипт, который печатает три таблицы —
он и есть доказательство охвата. Ручной обход тридцати файлов даст «вроде всё проверил».

**Вывести таблицу вида:**

| имя | порождается | потребляется | вердикт |
|---|---|---|---|
| `.formula` | да | да | ✅ работает |
| `--steel-d` | да | да в skeleton, **нет** в деке | ⚠ расхождение деков |
| `{@-4}` | да | **нет** | 🔴 порождается вхолостую |

**КРИТЕРИЙ С1 (может ПРОВАЛИТЬСЯ):** таблица в отчёте **целиком**, с числами в шапке:
«классов порождается N, из них не потребляется K; переменных M, не объявлено L; форм сцен P, не
обработано Q». 🔴 **Без этих шести чисел заход не принимается**, даже если что-то починено:
именно их никто никогда не считал, и они — главный выход захода.

⚠ **Разница между `skeleton/tokens.css` и `teorkat-vvedenie/src/tokens.css` — отдельная строка
таблицы, а не шум.** Общий и лекционный токен-файлы разъехались, и это отдельный класс: имя
объявлено в одном месте и не объявлено в другом, поэтому дек, собранный общим скелетом,
отрисуется иначе, чем живой.

### С2. ПОЧИНКА — только того, что счёт назвал сломанным

Правило простое: **чинится то, что в таблице получило 🔴, и ничего кроме.** Имя, которое
порождается и потребляется, не трогать, даже если код выглядит некрасиво.

🔴 **Расхождение деков (`--hollow`/`--steel-d` и подобные) чинится в СКЕЛЕТЕ, а не в деке.**
`teorkat-vvedenie/src/` — живая лекция владельца, READ-ONLY. Скелет обязан объявлять всё, чем
пользуется порождаемый HTML, — тогда дек не обязан ничего доопределять.

### С3. 🔴 ГЛАЗ — потому что счёт не ловит уродство

Собрать фикстуру `zhivoj-teorkat` (`s03`, `s04`, `s08`) и **посмотреть на результат**. Скриншот
или рендер — на твоё усмотрение, но посмотреть обязательно и написать словами, что видно.

Проверить поимённо, потому что это известные места:
- формулы отрисованы, а не `⟦MISSING-MATH⟧` (кэш KaTeX);
- иллюстрации в обводке, а не сплошной чёрной заливкой;
- маркеры списка есть;
- номер слайда на месте;
- на `s08` текст цел — там когда-то пропало четыре абзаца из семи.

**КРИТЕРИЙ С3:** по каждому из пяти пунктов — слово «есть» / «нет», а не «вроде нормально».
Артефактом в отчёте назвать **абсолютный путь к собранному HTML**, который владелец откроет.

---

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено
N классов, M переменных, P форм сцен».

## 3. ВЕРИФИКАТОР

Счёт (С1) сам себе верификатор: он программный и печатает охват.
Починка (С2) проверяется тем же счётом, прогнанным ПОВТОРНО после правок — 🔴-строк обязано стать
меньше, и ни одна ✅ не должна превратиться в 🔴.

🔴 **Компилятор (`_generator/sborka/**`) — READ-ONLY, ни одной строки.** Рядом идут заходы,
которые его правят. Нашёл, что виновата сторона порождения, а не скелет, — **это находка и строка
отчёта, а не повод залезть**. Такая находка ценнее починки: она значит, что чинить надо в другом
месте, и лучше узнать это сейчас.

🔴 **`teorkat-vvedenie/src/` — живая лекция владельца, READ-ONLY.**

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/skeleton/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/skeleton/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Отклонение от Я1, названо вслух до работы.** Я1 называет `_generator/sborka/*.py`
как единственный источник стороны «порождается». По факту `formaty.py:59`
(`from build_deck import render_md, render_inline_md`), `deck.py`/`slaid.py`/`lenta.py`
(`from build_deck import max_scenes, scene_cascade_css, parse_brief, read_text`)
импортируют оттуда РЕАЛЬНУЮ логику генерации: разбор markdown тела карточки
(`.acc`/`.tlist`/`.fill`/`.blur-reveal`), синтаксис сцен (`{@N}`, `{@N-M}`, `{@-M}`,
`{.cls}`, `{@N|…}`, `{fill@N|…}`, `{blur@N|…}`) и каскад сцен — вся эта логика
физически лежит в `_generator/build_deck.py`, НЕ в `sborka/`. Без чтения этого файла
задача С1 «какие формы сцен порождает компилятор» невыполнима корректно: список
форм пришлось бы взять по памяти, что запрещено явно. `build_deck.py` — READ-ONLY,
той же стороны «порождает», ни строки не менял; прочитан программой (grep) и
целиком (Read) как прямая зависимость Я1, не как посторонний код проекта.

**Методология C1 (потребляется), скорректирована по факту.** Табличная формулировка
задачи предполагает единственный потребитель — `skeleton/base.css(+katex.css)`.
Чтением `tipy.py` установлено: часть классов (`board`,`copy`,`grid`,`full`,`wrap`,
`head`,`art`,`thx-*`,`brd-title`,`p-photo`,`p-qr`,`zagolovok`) стилизуется САМИМ
компилятором — `GLOBAL_CSS`/CSS, которую каждый handler возвращает вторым
элементом кортежа, попадает в тот же `<style>`, что и `skeleton/base.css`
(`slaid.py:127-134`). Это не дыра, а архитектура: `shablon.html` прямо называет
пер-слайдовые гриды bespoke-зоной (`{{FONTS_CSS}}…` блок [1], комментарий
«пер-слайдовые гриды (bespoke — добавляет арка 7 «вёрстка»)»). Считать их 🔴
значило бы «чинить то, что работает» — прямой запрет §2. Поэтому «потребляется»
= skeleton CSS (`base.css`+`katex.css`+`tokens.css`+`sluzhebnye/style.css`)
∪ bespoke-CSS самого компилятора (тот же вывод, тот же `<style>`). Классы без
покрытия НИ ОДНИМ из двух — настоящая дыра. Разница отражена в отчёте отдельной
строкой для прозрачности (сколько именно bespoke, а не «скелетных»).

**План действий:**
1. Разовый скрипт (`sverka.py`, вне зоны, scratchpad) — механический черновой
   проход по трём таблицам; его сырой вывод шумит (регэксп цепляет докстринги/
   комментарии как `class=`/`var(--x)`) — далее каждый кандидат на 🔴 проверен
   ЧТЕНИЕМ (не угадан), шум отфильтрован поимённо с причиной в отчёте.
2. С1 — таблица и шесть чисел в `## ОТЧЁТ`.
3. С2 — почини только 🔴 (ожидание по счёту: один класс `.blur-reveal`/
   `[data-reveal]` — порождается `{blur@N|…}` через `build_deck.py:225-228`,
   не обработан ни `base.css`, ни `engine.js`; сверено с живым каноном
   `teorkat-vvedenie/src/overlay.css:187-243`, где ровно этот каскад уже есть).
4. Повторный прогон скрипта — 🔴-строк меньше, ни одна ✅ не должна стать 🔴.
5. С3 — собрать `zhivoj-teorkat` (slaid.py по s03/s04/s08), посмотреть, отчитаться
   по пяти пунктам.
6. Коммит зоны, отчёт.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

### С1. СЧЁТ — таблица «порождается · потребляется · вердикт»

Собрано разовым скриптом (`sverka.py`, вне зоны — scratchpad, не коммитился; логика
и все цифры ниже воспроизводимы им же) + КАЖДЫЙ кандидат на 🔴 из сырого вывода
скрипта провалидирован ЧТЕНИЕМ исходников (методология и причина отклонения от
буквального Я1 — в `## ПЛАН` выше). Сырой вывод скрипта шумноват (регэксп на
`class="..."`/`var(--x)` цепляет докстринги/комментарии как код) — это признано
явно, а не скрыто; таблица ниже — после ручной сверки, с пометкой, что было шумом.

| имя | порождается | потребляется | вердикт |
|---|---|---|---|
| `.slide`,`.zone`,`.t-body`,`.panel`,`.acc`,`.tlist`,`.fill`,`.formula`,`.op-def/-utv/-task`,`.lestnica`,`.spisok`,`.bul`,`.lst`,`.s-*`(16 шт) | да | да, в `base.css` | ✅ работает |
| `.board`,`.copy`,`.grid`,`.full`,`.wrap`,`.head`,`.art`,`.thx-wrap`,`.thx-head`,`.thx-art`,`.brd-title`,`.p-photo`,`.p-qr`,`.zagolovok` | да | да, но НЕ в skeleton CSS — стилизует сам компилятор (`GLOBAL_CSS`/per-handler `css` в `tipy.py`, тот же `<style>`, что видит браузер; `.wrap`/`.head`/`.art`/`.thx-*`/`.brd-title`/`.p-photo`/`.p-qr` дополнительно продублированы в `skeleton/sluzhebnye/style.css` для служебных слайдов build_deck.py-пайплайна) | ◻ работает, не дыра — bespoke-архитектура (см. `## ПЛАН`), НЕ чинил |
| `.ill-row` | да | НЕТ ни в skeleton, ни bespoke-CSS | ◻ не дыра: весь бокс-модель инлайновым `style=` на том же элементе (`tipy.py:_ill_zone`), класс — голый маркер без визуальной зависимости |
| `--paper…--t-small` (17 шт, включая `--hollow`/`--steel-d`) | да | да, `tokens.css` `:root` | ✅ работает |
| `--liniya` | да (`tipy.py`) | да, но локально (`style="--liniya:…%"` на том же элементе, что и `var(--liniya)`) | ◻ не токен-файла дело, self-contained |
| `--blush`,`--corange`,`--p4`,`--p5`,`--rule` | — | объявлены, не используются | ⚠ мёртвый вес токен-файла, не порождается — не дыра, не чинил (не входит в критерий: спрашивает про обратное направление) |
| `--blok`: `skeleton/tokens.css` (`:root`, 26px) vs `teorkat-vvedenie/src/tokens.css` | ОТСУТСТВУЕТ в teorkat `tokens.css` | но объявлен ЛОКАЛЬНО в `teorkat…/overlay.css:92` (`.t-body{--blok:26px}`), тот же кегль | ✅ проверено чтением: расхождение файлов есть, расхождения ПОВЕДЕНИЯ нет — деки идентичны по факту. Ожидание §2 захода (Д31-подобная дыра) НЕ подтвердилось |
| `{@N}`,`{@N-M}`,`{@-M}`,`{@N\|text}`,`{fill@N\|a\|b}` (5 форм) | да | да, `engine.js`/`base.css` (`[data-scene-from]`+сгенерированный каскад, `.scene-off`, `.fill`) | ✅ работает |
| `{.cls}` (класс абзаца/списка) | да, СВОБОДНАЯ форма — `_attrs_from_tag` (`build_deck.py`) принимает ЛЮБУЮ строку после `.`, компилятор её не валидирует | — | ⚠ структурная находка, не численная дыра: пространство имён неограничено статическим анализом; в реально существующем контенте (фикстура Я4) встречены `.formula`/`.op-utv`/`.spisok` — все три уже в `base.css` |
| `{blur@N\|text}` → `class="blur-reveal" data-reveal="N"` | да (`build_deck.py:225-228`, `render_inline_md`, вызывается ОБОИМИ пайплайнами: `formaty.py:59`→`slaid.py`/`deck.py` И напрямую `build_deck.py`) | НЕТ — ни `base.css`, ни `engine.js` не знали `.blur-reveal`/`[data-reveal]` | 🔴 БЫЛА ДЫРА → **починено** (см. С2) |

**ШЕСТЬ ЧИСЕЛ:** классов порождается **27** (пересчитано вручную из сырых 31 —
исключены 4 шумовых регэксп-совпадения на докстрингах: `note`,`pA`,`[^`,`…`,
подробности в самом `sverka.py`), из них не потреблялось **1** (`.blur-reveal`,
починено). Переменных **17** (пересчитано из сырых 18 — исключён `--x`, шум из
комментария `build_deck.py:464`), не объявлено **0**. Форм сцен **7**, не
обработано **1** (`{blur@N|…}`, починено).

**Отдельная строка про `.ill-row`/докстринг-шум** — сознательно не в счёте
🔴/K, потому что после чтения выяснено: это не дыры (см. таблицу).

### С2. ПОЧИНКА
Один класс, ровно тот, что дала таблица: `.blur-reveal`/`[data-reveal="N"]` —
каскад `.scene-N .blur-reveal[data-reveal="K"]{filter:none;opacity:1;color:var(--brick)}`
для K=1..N, N=1..10 (SCENE_CAP из `build_deck.py`, прочитан, не менялся) + базовое
правило `.blur-reveal{filter:blur(5.5px) saturate(.55);opacity:.9;color:var(--steel)}`.
Порт дословно из `teorkat-vvedenie/src/overlay.css:187-243` (продлён с их `.scene-9`
до `.scene-10` под документированный потолок). Ничего другого не трогал — счёт
больше 🔴 не дал.

Повторный прогон `sverka.py` после правки: 🔴-строк классов стало 6→было 7 в сыром
выводе (реально «дыр» — 0, все 6 остающихся — тот же докстринг-шум/`.ill-row`,
подтверждено построчно), НИ ОДНА ✅-строка не стала 🔴 — регресса нет.

### С3. ГЛАЗ
Собрано `slaid.py` по одному (s03/s04/s08 → rc=0 у всех трёх) и целиком через
`deck.py` (3 слайда, 0 в резерве, rc=0). Смотрел в Browser pane (скриншоты +
`getComputedStyle` там, где скриншот вызывал сомнение — см. ниже про s08).

- **формулы отрисованы, не `⟦MISSING-MATH⟧`** — ЕСТЬ. Проверено на s03 (три
  формулы с биномиальными коэффициентами) и s08 (сумма с `(-1)^k`) — везде
  честный KaTeX, маркера пропуска не встретил ни разу.
- **иллюстрации в обводке, не сплошной чёрной заливкой** — ЕСТЬ. s03: кружки
  белые/чёрные с чёткой чёрной обводкой (`.s-node`/`.s-node-r`), не заливка.
  s08: коммутативная диаграмма тонкими линиями на белой панели.
- **маркеры списка есть** — ЕСТЬ. s08, сцена 3: `.spisok .lst` даёт два пункта
  с квадратными маркерами (`::before`), проверено скриншотом.
- **номер слайда на месте** — ЕСТЬ, но с оговоркой: у пайплайна `sborka/`
  (`slaid.py`/`deck.py`) нет визуального счётчика/прогресс-бара (та фича —
  `#lect-progress`/`#lect-zone` — живёт только в `shablon.html`, у ДРУГОГО
  пайплайна build_deck.py/{{SLIDES}}, который `sborka/` сознательно не трогает,
  см. `deck.py:16` "build_deck.py этим НЕ трогается"). Интерпретировал пункт как
  «слайд корректно идентифицируется и адресуется» — проверил structurally
  (`id="s03"`/`id="s04"`/`id="s08"` + верные `data-scenes="4"/"1"/"3"` в собранной
  деке) и навигационно (`showSingle(2,1)` → на экране действительно `id=s08`).
  Если владелец имел в виду именно визуальный счётчик — это находка вне зоны
  (build_deck.py, READ-ONLY), не чинил, см. ВОПРОСЫ.
- **на s08 текст цел, все 7 абзацев** — ЕСТЬ. На сцене 3 (последней) в DOM
  все 7 блоков тела (`formula`, 3× обычный `<p>`, `op-utv`, `.spisok`), визуально
  без обрезки/наложения — скриншотом подтверждено, текст старого дефекта
  (пропадали 4 из 7) не воспроизвёлся.

Отдельно вручную (вне фикстуры, т.к. `{blur@N|…}` в ней не встречается) собрал
минимальный тест `blur-test.html` с реальными `tokens.css`+`base.css`: на
`.scene-2` спан с `data-reveal="1"` — чёткий, кирпичный; спан с `data-reveal="3"`
— размыт, приглушённо-стальной. Починка ведёт себя ровно так, как канон
teorkat-vvedenie.

*(Один скриншот в самом начале визуально выглядел серым/десатурированным —
перепроверено `getComputedStyle`: фон `.slide` был правильным `rgb(230,229,225)`
(`--paper`), без `filter`/`opacity`. Артефакт оказался цветовой схемой самого
инструмента скриншота (`resize_window` без явного `colorScheme:"light"`), не
багом деки — переснято с `colorScheme:"light"`, картинка нормальная. Называю
явно, чтобы не выглядело как замалчивание находки.)*

### Что НЕ трогал
`_generator/sborka/**`, `_generator/build_deck.py`, `teorkat-vvedenie/src/**` —
все READ-ONLY, ни строки. Свободную форму `{.cls}` (структурная находка выше)
не «чинил» — чинить в скелете нечего, это компиляторная развязка, вне зоны и вне
критерия (счёт просит числа по конкретным именам, не по неограниченному
пространству).

### Вопросы
1. У `sborka/`-пайплайна (`slaid.py`/`deck.py`) нет визуального номера/прогресс-бара
   слайда — эта фича есть только в параллельном пайплайне `build_deck.py`+
   `shablon.html`. Осознанное расхождение архитектуры или недоделка? Если нужно
   — это задача на `build_deck.py`, вне зоны этого захода.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. `{.cls}` в markdown-теле карточки (`build_deck.py:_attrs_from_tag`) принимает
   ЛЮБУЮ строку без валидации — компилятор не проверяет, что имя класса вообще
   существует в скелете. Опечатка в контенте (`{.tlsit}` вместо `{.tlist}`) даст
   молчаливый брак ровно по классу отказа, ради которого собран этот заход,
   и текущий счёт её не ловит (пространство неограничено статически). Стоит
   завести гейт (по образцу `lint()` в `build_deck.py` п.3 про `var(--x)` в
   иллюстрациях) — сверять КАЖДОЕ `{.имя}` из контента против известного словаря
   скелета на этапе сборки, а не постфактум глазами.
   ДОМ: `2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

## УРОКИ ФАБРИКЕ

### Анкор Я1 захода не покрыл реальную сторону «порождает»
Заход называет `_generator/sborka/*.py` единственным источником стороны
«порождается», но ключевая генерирующая логика (`render_md`/`render_inline_md`,
весь синтаксис сцен, `max_scenes`/`scene_cascade_css`) физически лежит в
`_generator/build_deck.py` — файле ВНЕ названного анкора, к которому `sborka/*.py`
обращается импортами (`formaty.py:59` и еще 4 места). Заход прямым текстом
предупреждает «Проект сверх них не изучай» — но задача С1 («какие формы сцен
порождает компилятор — собрать по коду, не по памяти») без чтения этого файла
физически невыполнима корректно.
ЦЕНА: без отклонения (см. `## ПЛАН`) числа С1 либо не совпали бы с реальностью
(отсутствие `.blur-reveal` не было бы найдено вовсе — оно не лежит в `sborka/*.py`
буквально, туда попадает только вызов чужой функции), либо потребовался бы
второй заход на то же самое. Названо здесь, а не додумано молча.

**АРТЕФАКТ:** `/tmp/skelet-sverka-c3/dek.html` — открыть в браузере (собранная фикстурная дека, 3 слайда s03/s04/s08 с уже применённой починкой). Рядом там же: `s03.html`/`s04.html`/`s08.html` (те же слайды по одному) и `blur-test.html` (изолированная проверка починки `.blur-reveal`). Путь вне git (не коммитил — вне зоны, `/tmp` эфемерен) — пересобрать в любой момент: `python3 _generator/sborka/deck.py _generator/tools/fixtures/sborka/zhivoj-teorkat -o /tmp/skelet-sverka-c3/dek.html` из корня `materials-wt/skelet-sverka`.
**КОММИТ:** `4ebf755` — `skeleton: порт каскада .blur-reveal/[data-reveal] из teorkat-vvedenie/src/overlay.css` · `git_zona.py check --zone skelet-sverka` → ✅
**НЕОБРАТИМОЕ:** необратимого нет.
