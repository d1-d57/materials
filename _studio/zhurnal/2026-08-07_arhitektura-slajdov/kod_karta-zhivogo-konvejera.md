# Канал исполнителя — karta-zhivogo-konvejera (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — работа инвентаризационная: сверить список с диском и честно разметить.
> Изобретать архитектуру НЕ надо и прямо запрещено (§2).

КОНТЕКСТ. Фабрика научно-популярных лекций. **У неё есть полное описание своего конвейера — и оно
описывает конвейер, которым больше не пользуются.**

`_studio/konvejer/` — одиннадцать стадий (`01-brief` … `10-sborka-qa`), `GEJTY.md` с гейтами,
`ALGORITM.md`, `KATALOG.md`, и к ним написан рабочий трекер `_generator/tools/sostoyanie.py`.
Живой же конвейер сегодня — **семь фаз Ф1…Ф7** (интервью · раскадровка · текст слайдов · вёрстка ·
иллюстрации · сборка и QA · сцены), дерево лекции — `<лекция>/{brief.md, slajdy/<имя>/slaid.md,
illustracii/<имя>/{zakaz.md, risunok.svg}}`, инструменты — `_generator/sborka/`.

**Прогон трекера на здоровой живой лекции — вот что он печатает сегодня:**
```
python3 _generator/tools/sostoyanie.py teorkat-vvedenie/L2
слайды: ?
G1  бриф закрыт       FAIL   нет полей №[1..9]
G2  ресёрч закрыт     FAIL   нет kartoteka/KARTA-OBLASTI.md
G3  математика верна  FAIL   нет kotly/matematika.md
G5  раскадровка       FAIL   нет src/brief.md
G8  вёрстка           FAIL   нет src/tokens.css
G10 иллюстрации       FAIL   нет src/
G11 сборка + QA       FAIL   нет src/
```
🔴 **У этой лекции 15 карточек, гейт карточек ЗЕЛЁНЫЙ 15 из 15, 11 иллюстраций с заказами и
написанный текст фазы 3.** Трекер даёт сплошной FAIL, потому что ищет файлы дерева, которого нет.
Он даже не может сосчитать слайды — печатает `слайды: ?`. Один из гейтов проговаривается сам:
*«шаг 6 шёл по старой схеме (конвейер до 27.07.2026)»*.

🔴 **ВОТ ПОЧЕМУ НИКТО НЕ ЗАМЕЧАЕТ, ЧТО КУСОК ФАБРИКИ ОТКЛЮЧЁН.** Механизм, обязанный это
показывать, построен и работает — но сверяет с территорией, которой нет. Вердикты бессмысленны,
поэтому их не читают, поэтому он и не ловит настоящие отключения.

ЦЕЛЬ — требование владельца дословно: *«хотя бы просто описать всю систему, как мы её видим, как мы
её строили, чтобы потом могли вернуться и заметить, что этот кусок вообще отключён — и для этого не
приходилось бы полностью с начала до конца всё исследовать»*. Плюс: это **точка входа в следующий
раз**, арку закрывают ею.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее: ты НЕ проектируешь новый конвейер и НЕ переставляешь фазы.**
Владелец отдельно сказал, что пересборка списка фаз — работа следующей недели и не твоя. Ты
описываешь то, что есть, и честно помечаешь то, чего нет.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/karta-konvejera`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/karta-konvejera`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add karta-konvejera --branch zahod/karta-konvejera
  cd ../materials-wt/karta-konvejera
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_studio/konvejer/**` `_generator/tools/sostoyanie.py`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴🔴 **ЧУЖАЯ ЗОНА — рядом идёт заход `sborka-l2`, собирает лекцию, не трогать ни байта:** `teorkat-vvedenie/**` · `_generator/sborka/**` · `_generator/skeleton/**` · `_generator/vendor/**`. Лекцию `teorkat-vvedenie/L2` ты **ЧИТАЕШЬ** (она твой полигон для прогонов трекера) и **не пишешь в неё ничего**, включая временные файлы.
- 🔴 `_studio/docs/**` — READ-ONLY. Там лежит `spravka/ARHITEKTURA-FABRIKI.md`, тоже описывающий конвейер: **прочитай, назови расхождения в отчёте, но НЕ правь.** Дверь туда одна и она не твоя.
- 🔴 **Ничего не УДАЛЯЙ.** Устаревшее помечается, а не стирается: стадия старого конвейера может оказаться живой в другом проекте, а история решений — единственное, чем доказывается «почему так». Удаление — необратимо и в этом заходе запрещено целиком.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/karta-konvejera`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_studio/konvejer/**` `_generator/tools/sostoyanie.py`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `_studio/konvejer/GEJTY.md` **целиком** | реестр гейтов старого конвейера — главный предмет сверки |
| Я2 | `_studio/konvejer/ALGORITM.md` · `KATALOG.md` · `00-KONVEJER.md` | как стадии должны были сцепляться |
| Я3 | `_generator/tools/sostoyanie.py` | трекер: по каким признакам-на-диске он судит |
| Я4 | `disciplina/skills/slajdy/SKILL.md`, разделы **ФАЗА 1 … ФАЗА 7** | 🔴 **описание ЖИВОГО конвейера** — вторая сторона сверки |
| Я5 | `_studio/docs/spravka/ARHITEKTURA-FABRIKI.md` | третье описание; **читать, не править** |
| Я6 | `teorkat-vvedenie/L2/` — **дерево, не содержимое**: `ls -R`, `brief.md`, одна карточка на выбор | как выглядит живая лекция на диске |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — три шага, и ни одного лишнего

🔴 **Владелец просил МИНИМАЛЬНУЮ работу, после которой арку можно закрыть честно.** Не «навести
порядок везде». Соблазн расширить будет на каждом шаге — не поддавайся, лишнее уводит в отчёт.

### К1. 🔴 СВЕРКА: каждый гейт и каждая стадия — жив / переехал / архив

Пройти **все** гейты `GEJTY.md` (Я1) и **все** стадии `_studio/konvejer/*/` и по каждому ответить
ровно одно из трёх, **с доказательством на диске, а не по памяти**:

| метка | что значит | чем доказывается |
|---|---|---|
| **ЖИВ** | делает то же и работает на живом дереве | команда, прогнанная на `teorkat-vvedenie/L2`, и её вывод |
| **ПЕРЕЕХАЛ** | то же по смыслу делает другой механизм в Ф1…Ф7 | имя нового механизма + путь к нему |
| **АРХИВ** | замысла больше нет в живом конвейере | одна фраза, чем заменён или почему отпал |

⚠ **Число гейтов не переписывай руками** (`KONSTITUCIYA §10`) — оно считается:
`grep -c '^### G' _studio/konvejer/GEJTY.md`. То же для числа стадий.
🔴 **Метка ставится по ПРИЗНАКУ-НА-ДИСКЕ, а не по названию.** «G10 иллюстрации» звучит живо, но
ищет `src/` — а живой механизм это `gejt_illyustracij.py` на `<лекция>/illustracii/`. Совпадение
имён — не совпадение сущностей; ровно на этом трекер и врёт.

### К2. 🔴 ТРЕКЕР ПЕРЕСТАЁТ ВРАТЬ — минимальной ценой

`sostoyanie.py` сегодня печатает семь FAIL по здоровой лекции. **Это хуже, чем если бы он молчал:**
ложный красный учит игнорировать красное, и мы уже платили — 28 обходов `--no-verify` из 56 срывов
коммита, 27 по одной причине.

**Выбери и обоснуй в `## ПЛАН` одно из двух — большего не делай:**
- **(а) дёшево и честно:** трекер распознаёт дерево лекции и, если оно нового формата, **явно
  отказывается**: «лекция формата Ф1–Ф7, гейты `GEJTY.md` относятся к конвейеру до 27.07.2026,
  живые гейты — вот эти команды: …». Ноль ложных FAIL, ноль вранья;
- **(б) дороже:** признаки-на-диске у гейтов, помеченных ПЕРЕЕХАЛ, переписаны на живое дерево.

⚠ **(б) бери, только если К1 показала, что переехавших мало и они однотипны.** Не уверен — бери
(а): молчать честнее, чем врать, и это можно доделать следующим заходом. **Смешивать нельзя:**
трекер, который половину проверяет верно, а половину врёт, — тот же ложный красный.

### К3. 🔴 ВЫХОДНОЙ ДОКУМЕНТ — `_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md`

Это и есть то, ради чего заход. Требование владельца дословно: *«чтобы потом могли вернуться и
заметить, что этот кусок вообще отключён, и для этого не приходилось бы полностью с начала до конца
всё исследовать»*.

**Ровно четыре раздела, больше не надо:**
1. **Живой конвейер Ф1…Ф7 — одной таблицей.** Строка на фазу, колонки: *решение фазы · артефакт ·
   дом на диске · гейт · верификатор · чем доказано, что гейт кто-то зовёт*. Пустая клетка — **не
   позор, а находка**: так и оставь пустой, это и есть «кусок отключён». 🔴 **Не выдумывай
   содержимое пустых клеток.**
2. **Таблица сверки К1**: старый гейт/стадия → метка → доказательство.
3. **Что заведомо НЕ покрыто ничем** — списком, честно.
4. 🔴 **«С ЧЕГО НАЧАТЬ В СЛЕДУЮЩИЙ РАЗ»** — 5–7 строк, по убыванию цены, каждая с командой, которую
   надо прогнать первой. Это **точка входа**, а не пересказ. Владелец закрывает арку этим разделом.

🔴 **Документ регистрируй тем же ходом:** `python3 _generator/tools/register_doc.py <путь> "<описание>"`,
и допиши `_studio/docs/KARTA.md` к путям коммита (см. КОНТРАКТ ЗОНЫ).

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — четыре условия:**
1. 🔴 **Живой прогон на реальном объекте:** `python3 _generator/tools/sostoyanie.py teorkat-vvedenie/L2`
   (лекция — **только чтение**) → **ложных FAIL ноль**. Вывод ДО и ПОСЛЕ в отчёт целиком.
   ⚠ Ноль FAIL — не самоцель: законный FAIL на реально незакрытой фазе остаётся. Провал — это FAIL
   на том, что на диске есть и работает.
2. Размечены **все** гейты и **все** стадии, ни одного без метки. Охват числом, и число — из
   команды: `grep -c '^### G' _studio/konvejer/GEJTY.md` против числа размеченных.
3. `KARTA-ZHIVOGO-KONVEJERA.md` существует, зарегистрирован (`grep -c` по `KARTA.md §6` → 1), все
   четыре раздела на месте, в разделе 4 — от пяти строк, **у каждой команда**.
4. 🔴 **Слепая проверка входа:** свежий субагент, не видевший сессии, получает ТОЛЬКО путь к новому
   документу и вопрос *«с чего начать работу над фабрикой и что сейчас сломано?»*. Ответ обязан
   назвать хотя бы два конкретных пункта с командами. Вопрос, ответ и вердикт — в отчёт дословно.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «сверил», а «размечено N гейтов из M, стадий K из L».

## 3. ВЕРИФИКАТОР

🔴 **Главный риск этого захода — не потеря файлов, а красивый документ, который снова врёт.**
Ты пишешь карту; карту проверяют не чтением, а тем, что по ней доходят.

- **Верификатор — пункт 4 критерия** (слепой субагент, только документ). Ответил мимо — правь и
  гоняй **другим** субагентом, не тем же.
- 🔴 **Вторая пара глаз на самое опасное:** свежий субагент берёт **три случайные метки ЖИВ** из
  таблицы К1 и проверяет их своей командой. Метка ЖИВ, которую нельзя воспроизвести, — то же
  враньё, только новое. Три из скольких — назвать числом.
- **Ничего не удалено:** `git show --stat` — только добавления и правки; строк удалено 0 в
  `GEJTY.md`, `ALGORITM.md`, `KATALOG.md`. Удаление в этом заходе запрещено (КОНТРАКТ ЗОНЫ).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/konvejer/**` `_generator/tools/sostoyanie.py`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/konvejer/**` `_generator/tools/sostoyanie.py`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Якоря прочитаны целиком** (Я1 `GEJTY.md` 282 стр. · Я2 `ALGORITM.md`+`KATALOG.md`+`00-KONVEJER.md` · Я3
`sostoyanie.py` 1148 стр. целиком · Я4 `SKILL.md` из `~/Documents/GitHub/disciplina/skills/slajdy/`
(1473 стр., все 7 фаз) · Я5 `ARHITEKTURA-FABRIKI.md` · Я6 `teorkat-vvedenie/L2` — `ls -R`, `brief.md`,
карточка `centr-gruppy`). Живой прогон трекера ДО правки снят: **9 жёстких FAIL** из 15 реализованных
гейтов (не 7, как в иллюстрации захода, — дерево лекции с 2026-08-07 подросло; тот же диагноз).

**Ключевая находка, снимающая главный риск К2.** `sostoyanie.py` читает СТРОГО старое дерево
(`kartoteka/`, `kotly/`, `chernovik/`, `src/{content,slides,illustrations,shablon.html}`) — проверено
чтением всех 15 `check_gN`/`check_hN` функций подряд, ни одна не обращается к `slajdy/`,
`illustracii/<имя>/zakaz.md`, `LENTA-<id>.md`, `INTERVYU.md`, `VYREZANO.md` или строкам
`ПРИНЯТО фаза-N:`. Живая лекция `teorkat-vvedenie/L2` — дерево Ф1–Ф7 (`slajdy/<имя>/slaid.md` ·
`illustracii/<имя>/{zakaz.md,risunok.svg}`), подтверждено `ls -R` и карточкой `centr-gruppy` (оба
раздела «Математика — развёрнуто»/«Текст слайда — сжато» заполнены, шапка несёт поля фазы 1–2). Ни
одной строки `ПРИНЯТО фаза-N:` в `brief.md` нет вовсе — три блокирующих хука (SKILL.md) сегодня не
закрыты формально, при том что содержание (судя по карточкам) фактически готово дальше третьей фазы.

**К2 — решение: (а), дёшево и честно.** Обоснование: у гейтов G1–G15 в SKILL.md есть «Сводная карта
гейтов» (строки 1404–1423), но для 7 из 12 отражённых там соответствий (`G6,G8,G10,G13,G15,H5,H6`)
сам SKILL.md пишет «сформулирован? нет» — формулировки признака-на-диске для нового дерева не
существует. Переписывать признаки G1–G15 на новое дерево значило бы либо изобретать 7 неоформленных
признаков самостоятельно (запрещено §2 захода — не проектирую конвейер), либо оставить их
недоделанными наполовину — а «трекер, который половину проверяет верно, половину врёт» прямо запрещён
клаузой (б). Каждый живой гейт вдобавок висит на РАЗНОМ инструменте (`gejt_kartochki.py`,
`gejt_illyustracij.py`, `audit.py`, грепы `ПРИНЯТО фаза-N:`) — не однотипная переделка. Беру (а):
трекер детектирует новое дерево по одному дешёвому и однозначному признаку — наличию
`<лекция>/slajdy/` (в старом дереве такой папки не бывает ни у одного дека, проверено: у
`teorkat-vvedenie/src` его нет) — и явно отказывается с текстом диагноза + списком живых команд
(`gejt_kartochki.py`, `gejt_illyustracij.py`, грепы `ПРИНЯТО фаза-N:`), вместо таблицы FAIL. Это
закрывает критерий готовности п.1 (ложных FAIL ноль) чисто: цикл гейтов вообще не печатается на новом
дереве, значит FAIL в нём взяться неоткуда.

**Порядок работы дальше:**
1. **К1 — сверка.** Пройти все 17 записей `### G` (`G1–G4,G16,G16.9,G5,G7,G6,G8–G15`) и 6 `### H`,
   плюс все 11 папок `_studio/konvejer/*/` (`01-brief … 10-sborka-qa`, `04.5-intervyu`) — для каждой
   пометка ЖИВ/ПЕРЕЕХАЛ/АРХИВ с командой-доказательством на `teorkat-vvedenie/L2`. Опора — «Сводная
   карта гейтов» SKILL.md как ЧЕРНОВИК соответствия (её ещё не проверял на диске), плюс собственная
   проверка H1–H6 и G16/G16.9 (в сводной карте не упомянуты вовсе) и всех 11 папок-стадий (карта
   гейтов не покрывает стадии, только гейты-границы).
2. **К2 — правка `sostoyanie.py`.** Один ранний выход в `run()`/`main()`: `<лекция>/slajdy` существует
   → печать диагноза (формат Ф1–Ф7, гейты `GEJTY.md` — конвейер до 27.07.2026, живые команды по факту
   К1) и `return`/`exit` без таблицы гейтов. Прогон ДО/ПОСЛЕ — в отчёт целиком.
3. **К3 — `KARTA-ZHIVOGO-KONVEJERA.md`.** Четыре раздела строго по заходу; пустые клетки — не
   заполняются выдумкой. Регистрация тем же ходом (`register_doc.py` + `docs/KARTA.md §6` в коммите).
4. **Верификация.** Слепой субагент на готовый документ (только путь, вопрос дословно из захода) +
   свежий субагент проверяет 3 случайные метки ЖИВ своей командой (число «3 из скольких» — из готовой
   таблицы К1).
5. **Коммит зоны** (`_studio/konvejer/**`, `_generator/tools/sostoyanie.py`) двумя ходами с путями,
   `git_zona.py check --zone`.

**Оспариваю не критерий, а один нюанс:** критерий п.1 требует «вывод ДО и ПОСЛЕ в отчёт целиком» —
ДО уже снят выше (9 FAIL), ПОСЛЕ будет снят после правки К2; расхождение с числом «7» на иллюстрации
захода — не ошибка чтения, а естественный дрейф живой лекции за день, укажу это в отчёте, не считаю
находкой для ВОПРОСОВ.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **Авто-подбор кегля (`_generator/sborka/deck.py` без `--bez-podbora`) падает на живой лекции
   `teorkat-vvedenie/L2` — `RuntimeError: зона .zone.t-body не найдена на слайде`.** Найдено пробным
   read-only прогоном (вывод в `/tmp`, лекция не тронута), воспроизведено дважды тем же способом.
   `_generator/sborka/**` — чужая зона этого захода (владеет `sborka-l2`); чинить не стал.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_solver-vmeshcheniya.md` (похоже на его
   предмет — «ГЕЙТ ВМЕЩЕНИЯ» — но я этот файл не читал, вне якорей моего захода; пусть аналитик сверит)
   ДОСТАВЛЕНО: нет
2. **`teorkat-vvedenie/KONTRAKT-L2.md` осиротел.** Файл старой модели G16 (`status: utverzhden`),
   новая Фаза 1 (SKILL.md) его не читает вовсе — гейт живёт в `ПРИНЯТО фаза-1: <дата>` в `brief.md`,
   которой там нет. Архивировать явно или удалить — решение не моё (вне зоны, `teorkat-vvedenie/**`).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
3. **Критерий готовности «три случайные метки ЖИВ» на этой сверке выродился в ноль меток ЖИВ**
   (34 из 34 записей — ПЕРЕЕХАЛ/АРХИВ, ни одной ЖИВ) — ожидаемо для полностью замещённого конвейера,
   но будущему заходу такого рода стоит знать это заранее и не удивляться. Заменил на «три случайные
   ПЕРЕЕХАЛ», объяснил в самом документе (§2, врезка перед таблицей). Не костит урок (цены не было —
   адаптировался без потерь), просто наблюдение для следующего похожего захода.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + зачем.** Прочитал целиком все шесть якорей (Я1–Я6) и SKILL.md живого конвейера Ф1–Ф7
(1473 строки, `~/Documents/GitHub/disciplina/skills/slajdy/`). Прошёл все 17 гейтов, 6 хуков и 11
папок-стадий старого реестра `GEJTY.md`/`00-KONVEJER.md` и разметил каждый ЖИВ/ПЕРЕЕХАЛ/АРХИВ с
командой-доказательством на живой лекции `teorkat-vvedenie/L2` (единственный живой экземпляр дерева
Ф1–Ф7 в репозитории) — зачем: это и есть требование владельца, «карта, чтобы не пришлось заново
исследовать, что отключено». Результат — 30 ПЕРЕЕХАЛ + 4 АРХИВ + **0 ЖИВ** (находка сама по себе: ни
один старый признак-на-диске не читает новое дерево, это и есть исходный диагноз захода, подтверждённый
исчерпывающе, не выборочно). Внёс минимальную правку в `_generator/tools/sostoyanie.py` — трекер
детектирует дерево Ф1–Ф7 по признаку `<лекция>/slajdy/` и явно отказывается с диагнозом и списком живых
команд, вместо семи-девяти ложных FAIL (вариант (а) из К2, обоснование — в `## ПЛАН`: 7 из 12
отражённых в SKILL.md соответствий сами помечены «сформулирован? нет» — переписывать признаки G1–G15
значило бы либо изобретать конвейер (запрещено), либо чинить наполовину (запрещено явно). Написал
`_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` (четыре раздела строго по заходу) и зарегистрировал его
`register_doc.py`.

**Как проверил.**
- Прогон трекера ДО правки на `teorkat-vvedenie/L2`: **9 жёстких FAIL** (`G1,G2,G3,G4,G5,G6,G8,G10,G11`)
  + два вакуумных PASS/N-A на пустоте (`H5`, `G13`/`G14` читают несуществующий `L2/src/brief.md` и
  вырождаются в пустые множества — то есть даже «зелёное» в старой таблице на этом дереве ничего не
  доказывает, не только «красное» врёт).
- Прогон трекера ПОСЛЕ правки: печатает диагноз и список живых команд, **таблицы гейтов нет вовсе** —
  ложных FAIL ноль, потому что цикл гейтов не запускается. Оба вывода — выше в этом файле (первая
  Bash-команда после плана) и в транскрипте сессии.
- Регрессия: `bash _generator/tools/fixtures/sostoyanie/PROGNAT.sh` — ФИКСТУРЫ ЗЕЛЁНЫЕ (все фикстуры
  без `slajdy/` идут старой веткой без изменений); `sostoyanie.py buffon` — тоже старая ветка, не задета.
- Каждая метка К1 подтверждена командой прямо в тексте `KARTA-ZHIVOGO-KONVEJERA.md` — не пересказом
  документации, а прогоном на диске (`gejt_kartochki.py` → ЗЕЛЁНЫЙ 15/15, `gejt_illyustracij.py` →
  ЗЕЛЁНЫЙ 11/11, `deck.py --bez-podbora` → 15 слайдов/0 в резерве, `lenta.py` → 15 разделов, находка
  44 `⟦MISSING-MATH⟧` в собранном деке, `slide_order`↔`slajdy/*` сверены питоном — 0 расхождений).
- **Верификатор п.4 критерия (слепой вход):** свежий субагент получил ТОЛЬКО путь к документу и вопрос
  дословно «с чего начать работу над фабрикой и что сейчас сломано?». Ответ назвал два (фактически все
  семь) конкретных пункта с командами, ничего не выдумав сверх текста документа. **Прошёл.**
- **Вторая пара глаз (риск ложного ПЕРЕЕХАЛ):** метка ЖИВ в таблице отсутствует (см. находку в
  ВОПРОСАХ), поэтому свежий субагент независимо проверил **3 случайные метки ПЕРЕЕХАЛ** (G10, G3, G5 —
  выбраны `random.sample` по номеру строки) своими командами, не читая саму карту. Все три
  подтвердились: резолв 11/11 иллюстраций в пул + отсутствие var(--x)-линтера на новом дереве (G10);
  0 файлов `kotly/matematika.md`, 15/15 карточек несут `## Математика — развёрнуто` (G3);
  `slide_order` и папки `slajdy/*` совпадают как множества, `diff` пуст (G5). **3 из 3 подтвердились.**

**Что НЕ трогал.** `teorkat-vvedenie/**` (только читал, ни одной записи — все сборки уводил в `/tmp`),
`_generator/sborka/**`, `_generator/skeleton/**`, `_generator/vendor/**` (чужая зона `sborka-l2`),
`_studio/docs/**` кроме одной строки `KARTA.md §6`, которую обязан дописать `register_doc.py` по
контракту зоны. Ничего не удалил нигде.

**Результат верификатора:** прошёл оба прогона (слепой вход п.4 критерия + сверка 3 случайных меток —
см. выше). `git show --stat` — только добавления/правки, GEJTY.md/ALGORITM.md/KATALOG.md не тронуты
вовсе (0 строк).

**Открытое «возвращаться»:** нет — все четыре условия критерия закрыты (см. ниже), заход закрывается
чисто. Незакрытые технические долги фабрики (не долги этого захода) — в разделе 4 самого документа и
в `## ВОПРОСЫ` выше.

**Время прогона + токены:** НЕПРИМЕНИМО — канал `app`, лог существует только на канале `terminal`.

**ПОВТОРЯЕМОСТЬ находок.** Из находок раздела 3 документа четыре систематические — повторятся на
КАЖДОЙ следующей лекции, собираемой этим же живым конвейером, пока не починены в `_generator/sborka/`
(не в моей зоне, потому не чиню, а называю громко здесь и в ВОПРОСАХ №1): (1) формулы `⟦MISSING-MATH⟧`
не рендерятся — это уже известный, поименованный долг Д27 самого SKILL.md, я лишь подтвердил его живым
числом (44) на L2, не открыл заново; (2) авто-подбор кегля падает `RuntimeError` — новая находка,
не была поименована нигде из прочитанного, см. ВОПРОСЫ №1; (3) `var(--x)`/русский текст в SVG не
проверяются ничем на дереве `illustracii/` — уже названная дыра SKILL.md (Фаза 5), подтверждена;
(4) служебные слайды (обложка/визитка/финал) не имеют механизма порождения в новом дереве — уже
названная дыра SKILL.md (Фаза 4 ВЫХОД), подтверждена. Три хука `ПРИНЯТО фаза-N:`/`VYREZANO.md` —
НЕ повторяющиеся в строгом смысле (это discipline-пробел конкретной сессии работы над L2, не баг
инструмента) — законно уходят пунктами в раздел 4 документа, не заходом.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/karta-konvejera/_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` — markdown, открывать текстовым редактором или `cat`.

**НЕОБРАТИМОЕ:** необратимого нет.

**КОММИТ:** `3b7b5e81b4a83811ffb92aad40525e9d1d9651b6` — «konvejer: карта живого конвейера Ф1-Ф7 против
GEJTY.md + честный отказ sostoyanie.py на новом дереве» · `git_zona.py check --zone karta-konvejera`
→ ✅ «работа доехала в git, вне git ничего нет».
