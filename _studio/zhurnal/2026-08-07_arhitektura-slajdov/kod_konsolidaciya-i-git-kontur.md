# Канал исполнителя — konsolidaciya-i-git-kontur (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — половина работы это суждение: где мы находимся и цел ли канон после суток
> переписывания. Такое не расписывается заранее. Субагенты: ≤2, только на Э5.
> 🔴 **Спецификация — в §2 этого файла целиком.** Ссылок наружу за ней нет: за сутки трижды
> повторилась одна ошибка — заход не увидел то, что аналитик написал, но не смог закоммитить.

КОНТЕКСТ. Фабрика научно-популярных лекций. За сутки скилл `slajdy` переписан целиком: семь фаз
единой формы, карточка слайда как источник истины, компилятор слайда из параметров, солвер
вмещения, пул иллюстраций, сборщики дека и ленты, протоколы, верификаторы. Шесть заходов приняты.

🔴 **Но контур не замкнут, и это накопилось.** Проверено 2026-08-08:
- в `materials` не влиты `zahod/solver-vmeshcheniya` и `zahod/solver-v2` — весь солвер недоступен
  из основной ветки;
- в `disciplina` работа лежит на ветке `zahod/fazy-1-2-dovodka`, в основную не сведена;
- правки аналитика вне git снова накопились (Cowork в песочнице в git не пишет);
- живы шесть веток заходов и несколько рабочих папок от закрытых.

🔴 **И главное — причина, а не следствие.** За сутки ТРИЖДЫ повторился один класс ошибки: заход
посылали пользоваться инструментом с невлитой ветки, и он терял ход целиком. Каждый раз это
чинили вручную, вписывая слияние в текст захода. **Владелец назвал верное лечение: место под
git-контур должно быть в самом генераторе заходов, а не в памяти аналитика.**

ЦЕЛЬ: замкнуть контур и **сделать так, чтобы он больше не размыкался** — вписать обязательный
git-блок в генератор и завести гейт, который краснеет на незаполненном.

Приёмка — по ОТЧЁТУ, без построчной сверки.
Если стоп до цели: получишь сведённый git и правку генератора, но НЕ актуальную карту готовности —
она последняя намеренно.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/tools/**` `_studio/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_konsolidaciya-i-git-kontur.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_generator/tools/**` `_studio/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_konsolidaciya-i-git-kontur.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `_generator/tools/bootstrap_zahod.py` | 🔴 предмет Э2: генератор заходов, сюда вписывается обязательный git-блок |
| Я2 | `_generator/tools/check_zahod.py` | 🔴 предмет Э3: гейт захода, сюда — проверка на заполненность блока |
| Я3 | `_studio/docs/kak-delat/GIT-disciplina.md` | канон git: слияние, надгробия, worktree. Читать ПЕРЕД Э1 |
| Я4 | `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md`, раздел «КАРТА ГОТОВНОСТИ СЕМИ ФАЗ» | предмет Э5: карта, снятая утром, устарела за день |
| Я5 | `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md` | 54 долга и четыре блока решений приёмок — **не переоткрывать**, только актуализировать статусы |
| Я6 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/SESSIYA.md` и `UROKI-FABRIKE.md` | дневник арки и десять уроков с ценой |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Пять шагов. Э2 и Э3 — то, ради чего заход существует: они лечат причину, а не следствие.

### Э1. Замкнуть контур — влить, закоммитить, запушить

**В `materials`:** влить `zahod/solver-v2` (он содержит `zahod/solver-vmeshcheniya` внутри себя —
проверить фактом `merge-base --is-ancestor`, а не предположить).
⚠ **`zahod/obratnyj-progon` НЕ ТРОГАТЬ** — по нему прямо сейчас идёт работа.

**В `disciplina`:** свести `zahod/fazy-1-2-dovodka` с основной веткой.

**Закоммитить правки аналитика в обоих репозиториях** — он их закоммитить не может. На 2026-08-08
это `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md` (долги Д52–Д54 и исправление коллизии
номеров) и файлы в `_studio/` репозитория `materials`. Снять точный список командой, не по этому
перечню: он мог устареть.

**`git push` в обоих** — последним ходом Э1. Не прошло — строкой в отчёт, работу не прерывать.

### Э2. 🔴 ГИТ-КОНТУР В ГЕНЕРАТОРЕ ЗАХОДОВ — главное в этом заходе

**Причина, а не следствие.** За сутки трижды повторилось одно: заход посылали пользоваться
инструментом с невлитой ветки, и он терял ход целиком. Каждый раз чинили вручную — вписывали
слияние в текст. Аналитик забывал, потому что **места под это в шаблоне не было**.

Требование владельца дословно: «надо это делать просто программно. В генераторе заходов должно
быть записано и закрытие веток, и коммиты, и вливание — вначале всегда. Там должно быть место, в
котором это указывается, а ты сам вписываешь туда конкретику. Но место должно быть в начале
каждого захода».

Вписать в `_generator/tools/bootstrap_zahod.py` **обязательный блок в шапке каждого захода**,
сразу после контракта зоны, с тремя слотами:

1. **ВЛИТЬ ПЕРЕД РАБОТОЙ** — какие ветки нужны, чтобы инструменты вообще существовали. Генератор
   умеет это узнать сам: `git branch --no-merged <текущая>` даёт кандидатов, и они печатаются
   подсказкой. Не заполнено — вписывается явное «вливать нечего, проверено командой: невлитых
   веток N».
2. **ЗАКОММИТИТЬ ПО ХОДУ** — что заход обязан забрать в git, включая правки аналитика, которые он
   сам закоммитить не может.
3. **ЗАКРЫТЬ ПОСЛЕ** — какие ветки и рабочие папки гасятся, и какие трогать нельзя (соседний
   заход).

Флаг `--vlit` у генератора уже есть — **не плодить второй механизм**: расширить существующий до
трёх слотов, а не завести рядом новый.

### Э3. Гейт на этот блок — иначе это не правило, а надежда

Вписать в `_generator/tools/check_zahod.py` проверку: блок git-контура присутствует и **все три
слота непусты** — либо конкретика, либо явное «нечего, проверено командой». Пустой слот — красный.

Это прямое требование конституции: правило заводится вместе с тем, что покраснеет при нарушении.
Без гейта мы через день снова будем чинить вручную.

⚠ Прогнать новый гейт **на уже существующих заходах арки** (их девять) — он обязан покраснеть на
тех, где блока нет. Не покраснел ни на одном — гейт не работает, так и написать.

### Э4. Гигиена — закрыть отработавшее

По канону Я3: `poteri --branch` по каждой ветке (`rc=1` — не удалять), затем `zakryt-vetku` для
тех, у кого терять нечего. Рабочие папки закрытых заходов удалить — владелец 2026-08-08: «временные
рабочие папки нужно все удалять, даже можно у меня не спрашивать».
⚠ Не трогать рабочую папку и ветку `obratnyj-progon` — там идёт работа.

**Зафиксировать имя ленты (Д50).** Решение владельца: **правое имя — то, которое уже существует**,
то есть живой файл, а не канонический `MATEMATIKA.md`, которого в репозитории ноль. Привести канон
к живому имени, плейсхолдер `<лента>` в сверке покрытия заменить на него.

### Э5. Где мы находимся — актуализировать карту и посмотреть свежими глазами

Карта готовности семи фаз (Я4) снята утром и устарела: за день переписаны фазы 1–7, написан
солвер, закрыты десятки долгов. Пересчитать по каждой фазе: дисциплина · инструменты · прогнано ли
на живом — и назвать, **что осталось до первой чистой сборки лекции**.

Плюс субагент со свежим контекстом: даётся `SKILL.md` и справочники, **не даётся** этот заход и
дневник. Вопрос один: **можно ли по этому скиллу вести лекцию, и где он порвётся первым?**

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. из основной ветки `materials` виден солвер: `ls` по папке sborka генератора печатает
   `vmeshchenie.py` и `korpus.py`; `git status --short` в обоих репозиториях не показывает
   незакоммиченного в зоне; `push` выполнен либо назван отказ;
2. 🔴 **генератор заходов порождает блок git-контура с тремя слотами** — показано выводом:
   собран пробный заход во временную папку, в нём блок присутствует;
3. 🔴 **ЖИВОЙ ПРОГОН на реальных объектах репозитория:** новый гейт прогнан по ВСЕМ файлам-заходам
   арки `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_*.md` — это девять живых заходов, не
   фикстуры. Названо, на скольких покраснел и почему. Плюс отдельно показано, что гейт краснеет на
   пробном заходе с намеренно пустым слотом;
4. `--vlit` не продублирован вторым механизмом — назван способ, которым расширен существующий;
5. `poteri` прогнан по каждой закрываемой ветке, вывод приложен; `obratnyj-progon` не тронут —
   показано, что ветка и папка на месте;
6. имя ленты приведено к живому: `grep -c "MATEMATIKA.md"` по скиллу печатает `0`;
7. карта готовности пересчитана; названо, **что осталось до первой чистой сборки**;
8. вердикт свежего взгляда приложен с охватом.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

## 3. ВЕРИФИКАТОР
🔴 **Операция лоссовая дважды:** ты сводишь и закрываешь ветки, и ты правишь генератор, которым
собираются ВСЕ будущие заходы. По веткам — `poteri` до закрытия, вывод в отчёт, ветка с `rc=1` не
закрывается никогда. По генератору — собрать пробный заход ДО и ПОСЛЕ правки и показать, что
старые секции не потерялись.

На Э5 — до 2 субагентов. *Оплачено 08.08: сплошная сверка нашла 4 молчаливые потери из 68 правил,
и одна из них оказалась живым дефектом, а не потерей текста.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/**` `_studio/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_konsolidaciya-i-git-kontur.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/**` `_studio/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_konsolidaciya-i-git-kontur.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### 12. Шапка захода описывала состояние git, которое его же §0 опровергал строкой ниже
ЦЕНА: заход трижды утверждал «в `materials` не влиты `zahod/solver-vmeshcheniya` и `zahod/solver-v2`
— весь солвер недоступен из основной ветки», и на этом стояла вся Э1. Проверка фактом
(`merge-base --is-ancestor` → rc=0 обеим, `branch --no-merged` пусто) показала, что обе влиты ещё
до сборки захода; собственный §0 файла, собранный генератором КОМАНДОЙ, говорил правду («невлитых
веток 0»). То есть в одном файле стояли два взаимоисключающих утверждения о git, и правым было
машинное. Стоило это одного круга проверки — но при обратном исходе (заход поверил бы шапке и
пошёл «вливать») он потратил бы ход на слияние уже влитого и мог сорвать чужой merge. **Класс:
человеческая часть захода про состояние git протухает за часы, машинная — нет; когда они
расходятся, старшинство должно быть у команды, и это старшинство нигде не записано.**

### 13. Субагент-верификатор объявил живой инструмент мёртвым, потому что брифинг назвал ему не тот репозиторий
ЦЕНА: субагент со свежим контекстом выдал главной находкой «`tools/schet_byudzheta.py` и
`schet_slov.py` МЁРТВЫ — файлов нет нигде, нет и в истории git; SKILL.md утверждает „проверено
прогоном“, а прогнать нечем». Проверка заняла одну команду: оба файла живы, лежат в
`disciplina/skills/slajdy/tools/`. Субагент искал добросовестно — он искал ровно там, куда его
послал мой брифинг («инструменты лежат в `materials/`»), а `tools/` в скилле относительны САМОМУ
СКИЛЛУ. Находка была бы принята как факт, если бы её не перепроверили: она подробна, названа
командой и выглядит убедительнее остальных семидесяти девяти. **Класс: в двухрепозиторной работе
брифинг субагента обязан называть ОБА корня и правило разрешения относительных путей — иначе
верификатор с честным охватом «80 из 80» уверенно врёт про один из них.** (Тот же класс, что урок
8 арки, но с другой стороны: там якорь не доехал до исполнителя, здесь — до субагента.)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 0. Что оказалось не так, как написано в заходе — ДО работы, четыре пункта

**П-1. Предпосылка Э1 про `materials` УСТАРЕЛА: вливать нечего, всё уже влито.**
Проверено фактом, не словом (из основной папки, `--no-optional-locks`):
```
git merge-base --is-ancestor zahod/solver-v2 arka/mat-kostyak          → rc=0
git merge-base --is-ancestor zahod/solver-vmeshcheniya arka/mat-kostyak → rc=0
git branch --no-merged arka/mat-kostyak                                 → пусто
```
HEAD `arka/mat-kostyak` = `d52e07c Merge branch 'zahod/solver-v2' into arka/mat-kostyak`;
`ls _generator/sborka/` печатает `vmeshchenie.py` и `korpus.py`. То есть шапка захода
(«весь солвер недоступен из основной ветки») описывает состояние ДО чьего-то слияния, а
собственная строка §0 того же файла («вливать нечего, проверено при сборке: невлитых веток 0»)
описывает состояние ПОСЛЕ. Правой считаю строку §0 — она снята командой при сборке, а не по
памяти. Э1 в `materials` сводится к коммиту правок аналитика и пушу.

**П-2. `disciplina`: сведение = fast-forward, обратного слияния не требуется.**
`git branch --no-merged` (относительно текущей `zahod/fazy-1-2-dovodka`) — пусто, то есть
`main`, `zahod/fazy-3-7`, `zahod/verifikatory-stilya`, `zahod/arhitektura-slajdov`,
`zahod/etap-20-i-perenos` все УЖЕ внутри `fazy-1-2-dovodka`. Значит `main` двигается вперёд
без merge-коммита. Делаю это БЕЗ `checkout` (`git fetch . HEAD:main` — обновляет ссылку, не
трогая рабочее дерево): рабочая папка `disciplina` стоит на `fazy-1-2-dovodka`, и checkout там
запрещён ровно по §4 GIT-disciplina. `git_zona.py` к `disciplina` неприменим — он вычисляет
`REPO` от своего `__file__` и всегда указывает в `materials` (`korni.py`); это названный предел
инструмента, не мой обход.

**П-3. Правки аналитика физически лежат в РАБОЧЕМ ДЕРЕВЕ основной папки, а я в worktree.**
Стартовая строка владельца завела мне worktree `konsolidaciya-i-git-kontur` на своей ветке,
хотя контракт зоны написан под checkout-режим (`arka/mat-kostyak` в основной папке). У worktree
свой индекс и своё дерево — незакоммиченного из основной папки в нём НЕТ ВОВСЕ. ⇒ коммитов
будет ДВА, и это не расширение зоны, а единственная возможная форма:
- **в worktree, на своей ветке** — код: `_generator/tools/**`;
- **в основной папке, на `arka/mat-kostyak`** — правки аналитика в `_studio/**` и этот файл-заход.
Ветку при этом НЕ переключаю нигде; оба коммита — строго по pathspec, `_fond/`, `ucheniki/`,
`obzory/`, `_generator/build_listok.py` (тоже грязные в основной папке) не трогаю — они вне зоны.

**П-4. 🔴 Критерий 3 ПРОТИВОРЕЧИТ закону калибровки `check_zahod.py` — и я выбираю критерий.**
Докстринг линтера (строки 14–22) объявляет законом: каждая проверка обязана МОЛЧА пропускать
два живых принятых захода (`kod_tirazh-ocheredi.md`, `kod_ochered-domov.md`), и это держится
ловушкой 1 фикстуры `fixtures/zahod/PROGNAT.sh` (`rc=0` И пустой вывод). Критерий 3 требует
обратного: гейт обязан покраснеть на заходах без блока — а блока нет ни у одного файла,
написанного до сегодня, включая оба калибровочных.
**Развилка не обходится**: любая проверка, говорящая хоть что-то про отсутствие блока (хоть
жёстко, хоть предупреждением), ломает ловушку 1, потому что та требует ПУСТОГО вывода.
**Решение:** З10 — жёсткая; ловушка 1 переписывается как «калибровочные файлы молчат по всем
проверкам, КРОМЕ З10», и те же два файла становятся для З10 **положительным контролем**
(живой, не синтетический, заход без блока обязан краснеть). Закон калибровки при этом не
отменяется, а уточняется: он запрещает ЛОЖНОЕ красное, а здесь красное ИСТИННОЕ — файл
действительно не несёт блока, ставшего обязательным. Практический шум нулевой: все будущие
заходы рождаются из генератора уже с блоком. Правку докстринга и ловушки называю в отчёте
отдельной строкой как ослабление чужого гейта.

### Шаги

1. **Э1.** Коммит правок аналитика: `disciplina` (`skills/*/DOLG.md`) и `materials`
   (`_studio/**`, без моего файла-захода — он уедет последним ходом вместе с отчётом).
   Затем ff `main` в `disciplina`. Затем `push` в обоих; не прошло — строкой в отчёт.
2. **Э2.** `bootstrap_zahod.py`: `vlitie_blok()` → `git_kontur_blok()` с тремя слотами.
   Расширяю СУЩЕСТВУЮЩИЙ механизм `--vlit` (та же функция, тот же анкор `{{ВЛИТИЕ}}` шаблона,
   то же место — начало `## 0. ПЕРВЫЙ ХОД`, сразу под контрактом зоны), добавляя к нему
   `--kommitit` и `--zakryt`; второго механизма и второго анкора не завожу, шаблон
   `_TEMPLATE-zahod.md` не трогаю ни байтом. Блок печатается ВСЕГДА: незаполненный слот
   получает явный маркер `<НЕ ЗАПОЛНЕНО …>` (сегодня в этом случае в файл не уходит ничего —
   ровно та тишина, из-за которой три захода потеряли ход).
3. **Э3.** `check_zahod.py`: З10 — блок есть и все три слота непусты. Плюс ловушки в фикстуре
   (блок есть · слот пуст · свежесобранный молчит · калибровочные как положительный контроль).
4. **Живой прогон** гейта по всем `kod_*.md` арки — их **11**, а не 9, как говорит критерий
   (`ls _studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_*.md | wc -l`); считаю по факту и
   называю расхождение. Плюс пробный заход в одноразовый репозиторий (`GIT_ZONA_REPO`) — до и
   после правки, чтобы показать, что старые секции не потерялись (§3 верификатора).
5. **Э4.** `poteri --branch` по каждой ветке → `zakryt-vetku` зелёным; рабочие папки закрытых
   снять. `obratnyj-progon` и своя ветка — не трогать. Имя ленты Д50 → живое.
6. **Э5.** Пересчёт карты семи фаз + два субагента свежего взгляда.
7. **Коммит** обеих зон, отчёт.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Ветку `zahod/dovodka-fazy-intervyu` закрыть НЕЛЬЗЯ: под тем же именем уже лежит надгробие
   `mogila/zahod/dovodka-fazy-intervyu` (8fc9951), и инструмент правильно отказывается его
   перезаписать. Хуже другого: **похороненное там НЕ влито никуда** — `merge-base --is-ancestor
   mogila/zahod/dovodka-fazy-intervyu arka/mat-kostyak` → rc=1, и то же rc=1 против живой ветки того
   же имени. В надгробии четыре коммита и 1037 добавленных строк, в том числе
   `_studio/konvejer/04.5-intervyu/{POKRYTIE,PRIMERY,ZAHOD}.md` и `kod_dovodka-fazy-intervyu.md`
   на 586 строк. Это ровно класс «работа существует в двух версиях и расходится молча»
   (`GIT-disciplina §4б`). Решать владельцу: воскресить и слить, либо признать устаревшим.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. Гейт вмещения `_generator/sborka/gejt_vmeshcheniya.py` печатает «заполнение», которое на всём,
   что влезло, равно ровно `100.0 %` (замер сырыми: слайд из одной фразы — `sh=302, ch=302`;
   боевой `s08` — `664, 664`). Метрика двоична, а целевая полоса солвера `FILL_LO, FILL_HI = 75, 90`
   (`vmeshchenie.py:33`) через этот путь измерения недостижима вовсе. Сам гейт при этом РАБОЧИЙ —
   краснеет на переполненном (`1064.9 %`, rc=1). Слепая зона объявлена: солвер меряет другой
   функцией (`vmeshchenie.py:207` против `:419`), её я не проверял.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
3. `SKILL.md:194` печатает первую исполняемую команду фазы 1 без обязательного флага: `python3
   _generator/sborka/bootstrap_lekcii.py <лекция>` → `error: the following arguments are required:
   --slajdy`, rc=2 (проверено мной лично, не со слов субагента). И это не опечатка, а цикл:
   `--slajdy` требует имена всех слайдов, а имена — выход подфазы 5.2, которая идёт ПОСЛЕ этого шага.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
4. Кэш формул `<лекция>/math/katex.json` не назван в `SKILL.md` НИ РАЗУ (`grep -c "math/" SKILL.md`
   → `0`), хотя без него дек собирается с `rc=0` и с видимыми `⟦MISSING-MATH⟧`, а гейт карточки
   при этом зелёный. Плюс `SKILL.md:1152–1157` утверждает, что «формулы на новом пути НЕ
   РЕНДЕРЯТСЯ и клауза «0 `⟦MISSING-MATH⟧`» физически не может быть зелёной» — это ложно в
   опасную сторону: читатель решит, что дек в принципе не сдаётся, и остановится зря.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
5. Канонический пример карточки в `references/kartochka-slajda.md §3` КРАСНЫЙ на боевом гейте:
   `bloki.py:124-125` сравнивает `(tip, mysl)` ДОСЛОВНО, а справочник говорит «совпадает по
   составу», «меняется форма, не состав» — из этой формулировки запрет менять формулировку мысли
   не следует. Кто скопирует пример из справочника, упрётся в `КРАСНЫЙ … состав блоков не совпадает`.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
6. Строка приёмки `ПРИНЯТО фаза-N: <дата>` (форма из Д43), вписанная в `brief.md`, роняет
   `gejt_kartochki.py` ТРЕЙСБЕКОМ: `formaty.py:66` — `KEY_RE = ^([A-Za-z_]+):`, кириллицу не берёт,
   а `brief.md` состоит целиком из фронт-маттера, и другого места в файле нет. Ни один справочник
   не говорит, что строка идёт после закрывающего `---`.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
7. Пейсинг фазы 1 несовместим сам с собой: для лекции 20 минут скилл одновременно даёт «3 слайда»
   (`vhod-gejty.md:19`, живой замер 6–7 мин/слайд), «10 слайдов» (`vhod-gejty.md:18`, ставка
   ≈2 мин) и «49 слайдов» (`byudzhet-obyoma.md:56`, цель 45 слов + формула `:137` `минут × 110`).
   Формула `×110` расходится с эталонной карточкой лекции (`kartochka-slajda.md:22-23`: 120 мин →
   700 слов) в ~19 раз. Ни одна величина не помечена как правая — это Д42, но масштаб больше:
   первое же решение фазы 1 принимается по числу, которого нет.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
8. `references/vhod-gejty.md:43-58` держит БЛОКИРУЮЩИЙ гейт G16 («пока красный, шаги 5–10 не
   стартуют») над файлами `KONTRAKT-*.md` и `ANKETA.md`, которых в дереве лекции нет и которые
   ничто не порождает: `grep -c "KONTRAKT\|ANKETA" SKILL.md` → `0`. Это продолжение Д52 (там был
   назван несуществующий каталог `konvejer/`), но с добавкой: гейт объявлен непропускающим и при
   этом физически незакрываем.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
9. `_generator/audit.py` не запускается на машине владельца: `BrowserType.launch: Executable
   doesn't exist … chromium_headless_shell`. Лекарство (`playwright install`) в скилле не названо.
   Мертвы: слой 4 фазы 6 и `--scene-diff` фазы 7. ⚠ Гейт вмещения при этом РАБОТАЕТ — он поднимает
   `channel="chrome"`, то есть системный Chrome, а не докачиваемый shell.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
10. Д31 в `DOLG.md` числится открытым («`skeleton/tokens.css` не несёт `--hollow`/`--steel-d` → в
    иллюстрациях сплошные чёрные заливки»), но переменные там УЖЕ есть: `grep -c -- "--hollow\|
    --steel-d" _generator/skeleton/tokens.css` → `4`, причём с комментарием-починкой в коде. Долг
    закрыт кодом и не закрыт в реестре.
    ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
    ДОСТАВЛЕНО: нет
11. Урок фабрике №12 (шапка захода против его же машинного §0) и №13 (брифинг субагента назвал не
    тот репозиторий) — оформлены в `## УРОКИ ФАБРИКЕ` выше, оба с ценой.
    ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
    ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)
### Что сделал и ЗАЧЕМ

**Э1. Контур замкнут — но не тем способом, каким предписывал заход.**
Предпосылка «в `materials` не влиты две ветки солвера» оказалась устаревшей: обе влиты до сборки
захода (`merge-base --is-ancestor` → rc=0 обеим; `branch --no-merged arka/mat-kostyak` пусто), и
`ls _generator/sborka/` печатает `korpus.py` и `vmeshchenie.py`. Вливать было нечего — сказал это
в `## ПЛАН` до работы, а не задним числом. Сделано остальное:
- **`disciplina`:** `main` сведён с `zahod/fazy-1-2-dovodka` — **без `checkout`**, через
  `git fetch . zahod/fazy-1-2-dovodka:main` (рабочая папка стоит на ветке захода, checkout там
  запрещён §4). Слияние оказалось fast-forward: все пять веток уже внутри.
- **Правки аналитика забраны в обоих репозиториях** (он в песочнице в `.git` не пишет):
  `disciplina` — Д52–Д54 и дома долга пяти скиллов (`1428a84`); `materials` — урок 11 арки,
  `KARTA.md`, `INCIDENTY.md`, заход `obratnyj-progon` и хэндофф (`64c4488`).
- **`push` прошёл в обоих**, все три ветки. Решение приёмки «54 коммита живут только на этой
  машине» этим закрыто.

**Э2. Место под git — в генераторе, а не в памяти аналитика.** В `bootstrap_zahod.py` блок §0.1
ГИТ-КОНТУР теперь вписывается в шапку КАЖДОГО захода, сразу под контрактом зоны, с тремя слотами:
влить ДО · закоммитить ПО ХОДУ · закрыть ПОСЛЕ. **Расширен существующий механизм, а не заведён
второй** (критерий 4): та же функция сборки (`git_kontur_blok`, единственная точка), тот же анкор
`{{ВЛИТИЕ}}`, то же место в файле; рядом с `--vlit` встали `--kommitit` и `--zakryt`. Шаблон
`_TEMPLATE-zahod.md` не тронут ни байтом (`git status` по нему пуст).
🔴 **Главное изменение поведения:** блок печатается ВСЕГДА. Раньше в одном из четырёх исходов
сторожа генератор не писал в файл НИЧЕГО, и это молчание было неотличимо от «вливать нечего» —
ровно та тишина, из-за которой три захода за сутки потеряли ход. Теперь пустой слот несёт явный
маркер и предупреждение в консоль.

**Э3. Гейт З10 — иначе это надежда, а не правило.** `check_zahod.py` краснеет, если блока нет или
если хоть один слот пуст/несёт маркер генератора. Судит только ЗАДАНИЕ (текст до `## ПЛАН`), как
З8, — чтобы заход, обсуждающий маркер в своём отчёте, не краснел от цитаты.

**Э4. Гигиена.** Веток было 8, стало 4. Закрыты `solver-vmeshcheniya`, `solver-v2`,
`kartochka-i-sborka`, `edinstvennost-sekcij` — каждая через `poteri` (rc=0, «потерь нет»), каждая
с надгробием. Снята пустая осиротевшая папка `materials-wt/linter-zahoda-wt`. Имя ленты приведено
к живому: Д50 закрыт.

**Э5.** Карта готовности семи фаз пересчитана по живым файлам; два субагента свежего взгляда.

### Как проверил — критерий по пунктам

| # | критерий | чем закрыт |
|---|---|---|
| 1 | солвер виден из основной ветки, зоны чисты, push | `ls _generator/sborka/` → `korpus.py`, `vmeshchenie.py`; `git_zona.py check --zone _generator/tools` → ✅; `disciplina`: `git status --short \| wc -l` → `0`; push прошёл в обоих (`64c4488`, `182e9cb`, `4b669c0`) |
| 2 | генератор порождает блок с тремя слотами | пробный заход собран в одноразовый репозиторий, блок в нём есть — см. АРТЕФАКТ |
| 3 | 🔴 ЖИВОЙ ПРОГОН по всем `kod_*.md` арки | **З10 покраснел на 11 из 11** — и файлов именно 11, а не 9, как говорит критерий (`ls … kod_*.md \| wc -l` → `11`; девятью они были до `obratnyj-progon` и до этого захода). Причина у всех одна: блока нет, они собраны до его появления. Отдельно: на пробном заходе с намеренно пустыми слотами 2 и 3 гейт называет **оба слота поимённо** |
| 4 | `--vlit` не продублирован | одна `git_kontur_blok()`, один анкор `{{ВЛИТИЕ}}` в шаблоне, шаблон не тронут; `--kommitit`/`--zakryt` — параметры той же функции |
| 5 | `poteri` по каждой закрываемой, `obratnyj-progon` цел | вывод `poteri` приложен ниже; ветка `zahod/obratnyj-progon` → `9c24b76` (двинулась сама — сосед работает), папка `materials-wt/obratnyj-progon` на месте |
| 6 | `grep -c "MATEMATIKA.md"` по скиллу → 0 | `grep -ro "MATEMATIKA.md" ~/Documents/GitHub/disciplina/skills/slajdy \| wc -l` → **0** |
| 7 | карта пересчитана, названо что осталось | `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md`, раздел «ЧТО ОСТАЛОСЬ ДО ПЕРВОЙ ЧИСТОЙ СБОРКИ» — семь позиций |
| 8 | вердикт свежего взгляда с охватом | ниже |

**Вывод `poteri` (по каждой закрываемой ветке, дословно один и тот же):** `✅ Потерь нет: удаление
любой ветки не унесёт содержимого — проверено 1 из 1 (кроме текущей arka/mat-kostyak)`, rc=0 —
для `dovodka-fazy-intervyu`, `edinstvennost-sekcij`, `kartochka-i-sborka`, `solver-v2`,
`solver-vmeshcheniya`.

### Верификатор (§3) — операция лоссовая дважды, проверено обеими сторонами

**По генератору — сборка ДО и ПОСЛЕ.** Пробный заход собран дважды с одинаковыми аргументами:
версией из `HEAD` и новой. `diff` показывает РОВНО одно изменение — появился блок §0.1; ни одна
старая секция не потеряна и не сдвинута.

**По гейту — мутация, а не «фикстура зелёная».** Фикстура `fixtures/zahod/PROGNAT.sh` доведена с
23 ловушек до 26 и промутирована в обе стороны:
- убрал З10 из `HARD_CHECKS` → `rc=1`, «ЛОВУШКА 24: З10 не покраснел на живом заходе без блока»;
- вернул генератору молчание на пустом слоте → `rc=1`, «ЛОВУШКА 26: генератор НЕ поставил маркер
  пустого слота — вернулось молчание»;
- обе правки откатил, контрольный прогон → `ФИКСТУРЫ ЗЕЛЁНЫЕ`.
Соседние фикстуры, объявившие охват над правленым: `dvizhok`, `korni`, `register_doc` — все `rc=0`.

**🔴 Пришлось ослабить чужой гейт, и это названо, а не спрятано.** Ловушка 1 фикстуры требовала от
двух калибровочных заходов ПУСТОГО вывода линтера. З10 на них краснеет — блок стал обязательным
позже, чем они написаны. Развилка не обходится: любая проверка, говорящая что-либо про отсутствие
блока, ломает ловушку 1. Выбрал критерий 3 захода против закона калибровки, потому что красное
здесь ИСТИННОЕ, а закон запрещает ЛОЖНОЕ. Ловушка 1 теперь требует молчания по всем проверкам
КРОМЕ З10, эти же два файла стали для З10 живым положительным контролем (ловушка 24), и
отступление вписано в докстринг `check_zahod.py` рядом с самим законом — чтобы следующий читатель
не принял его за поломку.

### Свежий взгляд — два субагента, вердикт с охватом

🔴 **Вердикт: вести лекцию по скиллу сегодня НЕЛЬЗЯ, и рвётся он на первом же исполняемом шаге.**

**Место первого разрыва — фаза 1, шаг 3 процедуры (`SKILL.md:194`/`:255`).** Скилл велит породить
анкеты скриптом и даёт команду `python3 _generator/sborka/bootstrap_lekcii.py <лекция>`. Она не
запускается: `error: the following arguments are required: --slajdy`, rc=2 (**перепроверено мной
лично**). Под этим не опечатка, а цикл: `--slajdy` требует имена всех слайдов, а имена — выход
подфазы 5.2, которая идёт ПОСЛЕ этого шага.

Остальное — в `## ВОПРОСЫ` пунктами 3–10, каждый с командой. Самое дорогое после первого разрыва:
кэш формул `math/katex.json` не назван в скилле ни разу при том, что без него дек собирается
зелёным с видимыми `⟦MISSING-MATH⟧`; канонический пример карточки красный на боевом гейте; строка
приёмки `ПРИНЯТО фаза-N:` роняет гейт трейсбеком; пейсинг фазы 1 даёт для 20 минут одновременно
3, 10 и 49 слайдов.

**ОХВАТ.** Прочитано 7 фаз из 7 (`SKILL.md`, 1364 строки); путей проверено командой 74 из 74 у
первого верификатора и 80 из 80 у второго; справочников прочитано целиком 8 из 36 (третьим
агентом), путей в них проверено `test -e` 38. **Не проверено и названо:** содержание остальных 28
справочников против кода; поведенческие утверждения `render_md`; числовые замеры по внешнему
корпусу деков.

**Отбраковал одну находку субагента как ложную.** Вердикт «`tools/schet_byudzheta.py` и
`schet_slov.py` мертвы — файлов нет нигде и не было в истории git» не подтвердился: оба живы, в
`disciplina/skills/slajdy/tools/`. Субагент искал ровно там, куда его послал мой брифинг, — это
моя ошибка, а не его; урок 13 фабрике об этом.

**⚠ Субагентов оказалось больше двух, и это выход за букву задания.** Заход разрешал ≤2 на Э5, я
запустил ровно 2 — но один из них сам делегировал работу ещё двум, о чём я узнал из его отчёта
(он честно написал, что их результата не дождался и за свой не выдаёт). Отчёт одного из внуков
пришёл после и дал 11 разрывов; четыре самых дорогих я перепроверил лично, остальные помечены как
непроверенные мной. Называю, потому что фактический расход субагентов вдвое выше объявленного.

### Что НЕ трогал

- `zahod/obratnyj-progon` — ни ветку, ни рабочую папку; он живой и двигался во время моей работы.
- `_studio/zhurnal/…/kod_obratnyj-progon.md` — файл модифицирован соседом прямо сейчас; я
  закоммитил его СНИМОК до начала своей работы и больше к нему не прикасался.
- Грязные пути вне зоны в основной папке: `_fond/zadachi/bank/**`, `ucheniki/**`, `obzory/**`,
  `_generator/build_listok.py` — 17 изменённых и 6 новых. Не мои, в отчёт, не тронуты.
- Ветка `zahod/dovodka-fazy-intervyu` — закрыть не дал инструмент, и правильно (`## ВОПРОСЫ` 1).
- `skills/slajdy` в `disciplina` — правил ТОЛЬКО по Д50 (имя ленты). Найденные разрывы 3–10 не
  чинил: они вне выданного задания, их дом — `DOLG.md`, решение аналитика.

### Открытое — «сюда возвращаться»

1. **Ветка `dovodka-fazy-intervyu` и её надгробие-тёзка** — единственное место, где сегодня лежит
   невлитая работа. `## ВОПРОСЫ` 1.
2. **Чужой долг, из-за которого я обошёл хук:** фикстура `git_zona` красная на двух ловушках
   `priyomka.py` («ПРИЁМКА МОЛЧИТ ПРО ВЕТКУ ЗАХОДА», «ПРИЁМКА ЗОВЁТ ЗАКРЫТЬ НЕПЕРЕНЕСЁННОЕ»).
   Проверено: она красная и на `HEAD` ДО моей правки, то есть долг не мой. Причина обхода уехала в
   `INCIDENTY.md` сама.
3. **З10 на девяти старых заходах арки останется красным навсегда** — они историчны и не
   пересобираются. Если приёмка гоняет линтер по корпусу, ей нужен фильтр по дате или отсечка;
   сегодня такого механизма нет.

**Время прогона + токены** — их снимает ПРИЁМКА из лога прогона.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Downloads/proba-git-kontur-2026-08-07/` — три файла, открывать
любым текстовым редактором: `1-vse-tri-slota-zapolneny.md` (заход, собранный новым генератором со
всеми тремя слотами — блок §0.1 виден сразу под контрактом зоны), `2-sloty-2-i-3-pustye-gejt-krasneet.md`
(тот же генератор без `--kommitit`/`--zakryt` — в слотах стоит маркер `НЕ ЗАПОЛНЕНО`),
`3-vyvod-gejta.txt` (вывод `check_zahod.py` на обоих: на первом З10 молчит, на втором краснеет
дважды и называет оба пустых слота поимённо).
Второй артефакт, который стоит открыть: `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` —
пересчитанная карта готовности семи фаз и семь позиций «что осталось до первой чистой сборки».

**КОММИТ:** пять, в двух репозиториях (зона захода разошлась по двум рабочим деревьям — почему,
объяснено в `## ПЛАН` П-3):
- `materials`, ветка `arka/mat-kostyak`: `64c4488` — правки аналитика в `_studio/**` ·
  `2c8ed23` — спасённые строки автолога · `45201c6` — пересчитанная карта готовности ·
  плюс коммит этого файла-захода последним ходом;
- `materials`, ветка `zahod/konsolidaciya-i-git-kontur` (worktree): `4b669c0` — весь код
  (`bootstrap_zahod.py`, `check_zahod.py`, фикстура). ⚠ Сделан через
  `git_zona.py commit --no-verify "<причина>"` — хук встал на ЧУЖОМ красном (см. «Открытое» 2);
- `disciplina`: `1428a84` — правки аналитика · `182e9cb` — имя ленты (Д50). `main` сведён и
  запушен.

`git_zona.py check --zone _generator/tools` → ✅ · `disciplina`: `git status --short` пуст.

🔴 **`git_zona.py check --zone _studio` КРАСНЫЙ, и это чужое — не чини, не читай как мой брак.**
Всё моё в этой зоне закоммичено (`64c4488`, `2c8ed23`, `45201c6`, `c7a562c`). Красное дают четыре
пути, появившиеся под рукой соседей ПОКА я работал:
`kod_obratnyj-progon.md` (живой заход пишет в него прямо сейчас) · `kod_solver-v3-dyhanie.md`
(новый заход, собран аналитиком после моего старта) · `KARTA.md` и `OTKRYTYE-ZADACHI.md`
(правлены аналитиком ПОСЛЕ моего коммита `45201c6` — мой текст в них цел, поверх лежит его).
Это ровно объявленная слепая зона гейта (`GIT-disciplina §4`: «гейт зоны судит каталог целиком и
краснеет на чужом при чистом своём»). Забирать чужое незакоммиченное под своё сообщение я не стал
— это тот самый инцидент 21.07, от которого защищает pathspec.

**🔴 НЕОБРАТИМОЕ — четыре действия, каждое с адресом восстановления:**
1. **Закрыты четыре ветки** (`zahod/solver-vmeshcheniya`, `zahod/solver-v2`,
   `zahod/kartochka-i-sborka`, `zahod/edinstvennost-sekcij`) вместе с их рабочими папками в
   `materials-wt/`. Восстанавливается по одной командой
   `python3 _generator/tools/git_zona.py voskresit --branch <ветка>` — надгробия
   `mogila/<имя>` поставлены, `poteri` перед каждым закрытием дал rc=0.
2. **Две ветки закрыты через `--vsyo-ravno`** (`kartochka-i-sborka`, `solver-v2`) — инструмент
   отказывался из-за игнорируемого мусора в папках. Что снесено: только `__pycache__/*.pyc`
   (шесть файлов, перечислены инструментом поимённо). Восстанавливается пересборкой; сами ветки —
   тем же `voskresit`.
3. **Откачен незакоммиченный файл** `_studio/zhurnal/_INFRA-git/INCIDENTY.md` в рабочей папке
   `materials-wt/wt-edinstvennost` (`git checkout --`), чтобы `zakryt-vetku` перестал отказывать.
   Его содержимое НЕ потеряно: две уникальные строки автолога перенесены в основной файл
   коммитом `2c8ed23`, плюс бэкап целого файла лежит в
   `<scratchpad>/BACKUP-INCIDENTY-wt-edinstvennost.md`.
4. **Удалена пустая папка** `materials-wt/linter-zahoda-wt` (`rmdir`; 0 байт, без `.git`, не
   значилась в `git worktree list`). Восстанавливать нечего — она была пуста.
*Всё остальное необратимого не содержит: правки файлов сделаны поверх git и откатываются коммитами
выше.*
