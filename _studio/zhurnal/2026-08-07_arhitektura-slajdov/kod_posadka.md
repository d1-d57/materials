# Канал исполнителя — posadka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — две git-операции и один отчёт; ничего не сочиняется.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-07_arhitektura-slajdov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/bold-brave-cray/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Длинная сессия закончена: арка `2026-08-07_arhitektura-slajdov` закрыта, планы владельца
разнесены по домам, инструменты размечены уровнями, два ложных зелёных починены. **Вся работа
физически цела, но стоит не там, где её будут искать.**

- `materials`: всё лежит на ветке `zahod/final-sessii`; интеграционная `arka/mat-kostyak` отстаёт
  на 7 коммитов. Влить её в себя предыдущий заход не мог: контракт запрещает исполнителю вливать
  свою рабочую ветку, это делает приёмка. Приёмка — аналитик, а он в песочнице в `.git` не пишет.
  **Поэтому это единственное, что осталось, и сделать это может только заход.**
- `disciplina`: `main` содержит все четыре `RAZVITIE.md`, но ОСНОВНАЯ рабочая папка выписана на
  чужую ветку `zahod/format-kartochki-faza-1` — поэтому по обычному пути файлов не видно, и дом
  выглядит пустым.

**ЦЕЛЬ: посадить сессию.** Ничего не чинить, не дописывать и не улучшать — только поставить работу
туда, где её найдут, и честно доложить о том, что трогать не стал.

Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: шаг 1 ценен сам по себе; шаг 2 —
отдельная операция во втором репозитории и без первого тоже осмыслен.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/posadka`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/posadka`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add posadka --branch zahod/posadka
  cd ../materials-wt/posadka
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_studio/zhurnal/` `·` `_studio/docs/` `·` `_studio/konvejer/` `·` `_generator/tools/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

```
python3 _generator/tools/git_zona.py merge zahod/final-sessii --zone _studio/zhurnal/ --zone · --zone _studio/docs/ --zone · --zone _studio/konvejer/ --zone · --zone _generator/tools/
```

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/final-sessii`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- ничего своего заход не пишет — только сливает; если после слияния что-то осталось грязным, назвать и забрать

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- ничего: ветки zahod/* и рабочие папки НИКОГДА не удаляются — решение владельца

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/posadka`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_studio/zhurnal/` `·` `_studio/docs/` `·` `_studio/konvejer/` `·` `_generator/tools/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: ничего сверх этого файла. Заход целиком состоит из git-операций и замеров; содержательных файлов читать не нужно. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

⚠ **Второй репозиторий — `/Users/ivanyakovlev/Documents/GitHub/disciplina`**, отдельный git-репозиторий
РЯДОМ с `materials`, не внутри. Только абсолютный путь, только свой `cd`. *Цена относительного пути
в этой же арке: исполнитель не нашёл репозиторий изнутри `materials` и завёл дубль скилла внутри
`materials` — 129 строк против настоящих 1473, потерян целый заход.*

### Шаг 1. Продвинуть `arka/mat-kostyak` до `zahod/final-sessii` — в `materials`

Ветка `zahod/final-sessii` содержит ВСЮ работу сессии: в неё уже влиты `zahod/zakryt-arku`,
`zahod/uroven-instrumentov` и `zahod/vygruzka-transkripta` (проверено `merge-base --is-ancestor`,
все три — предки). `arka/mat-kostyak` отстаёт ровно на 7 коммитов и **не имеет ничего своего
сверху** — то есть слияние должно быть перемоткой, без конфликтов.

🔴 **Проверь это утверждение САМ, до слияния, и не верь ему на слово:**
```
git --no-optional-locks rev-list --count arka/mat-kostyak..zahod/final-sessii   # ожидается 7
```
и **то же самое в обратную сторону**, поменяв две ветки местами в диапазоне — ожидается **0**:
столько своего есть на интеграционной ветке сверх рабочей.

Обратное число не ноль — **останавливайся и докладывай**, не сливай: значит на интеграционной ветке
появилась чужая работа, которой аналитик не видел, и решать это не твоё дело. *Ровно на такой
ситуации в этом репо `git status` взял `.git/index.lock` и сорвал три чужих коммита за сессию.*

Сливать через `git_zona.py` (единственная дверь к git). 🔴 **Зона слияния обязана накрывать ВСЁ,
чего касается вливаемая ветка, а не то, где ты работаешь** — сними состав командой и назови его в
отчёте до слияния:
```
git --no-optional-locks diff --stat arka/mat-kostyak...zahod/final-sessii
```
*Аналитик промахнулся этой зоной пять раз за сессию, каждый раз одинаково.*

### Шаг 2. Вернуть основную папку `disciplina` на `main` — и НЕ решать за владельца

Основная папка выписана на `zahod/format-kartochki-faza-1`. На этой ветке **17 коммитов, которых
нет в `main`** — это чужая незакрытая работа из другой арки.

**Что делаешь:**
1. Убедись, что рабочее дерево основной папки ЧИСТО: `cd /Users/ivanyakovlev/Documents/GitHub/disciplina && git --no-optional-locks status --porcelain` → пусто. 🔴 **Не пусто — НИЧЕГО не переключай и доложи**: переключение поверх несохранённого уничтожит чужую работу необратимо.
2. Чисто — выпиши папку на `main`.
3. Проверь ДЕЙСТВИЕМ, а не наличием: `ls /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/*/RAZVITIE.md | wc -l` → **4**, снято из ОСНОВНОЙ папки. *Именно ради этой строки шаг существует: файлы закоммичены давно, но по обычному адресу их нет, и четыре дома выглядят пустыми.*

🔴 **Ветку `zahod/format-kartochki-faza-1` НЕ вливать, НЕ удалять и НЕ трогать.** Вместо этого —
**доложи о ней в отчёте отдельным блоком**: сколько коммитов, какие файлы затронуты
(`git --no-optional-locks diff --stat main...zahod/format-kartochki-faza-1`), даты первого и
последнего. Это решение владельца, а не твоё: 17 коммитов чужой арки — не тот объём, который
вливают заодно. *Правило, оплаченное в этой сессии: «не нашёл» ≠ «нет», и «влилось бы чисто» ≠
«надо влить».*

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — всё командой, числа в отчёт:**
1. `git --no-optional-locks rev-list --count arka/mat-kostyak..zahod/final-sessii` → **0** (было 7).
2. `git --no-optional-locks branch --merged arka/mat-kostyak | grep -c final-sessii` → **1**.
3. `ls /Users/ivanyakovlev/Documents/GitHub/disciplina/doma/*/RAZVITIE.md | wc -l` → **4**, из основной папки.
4. `cd /Users/ivanyakovlev/Documents/GitHub/disciplina && git --no-optional-locks rev-parse --abbrev-ref HEAD` → **main**.
5. `python3 _generator/tools/check_karty.py; echo $?` → **rc=0** после слияния — то есть починка предыдущего захода доехала до интеграционной ветки, а не осталась на своей. Код возврата числом ПЕРВЫМ.
6. Блок про `zahod/format-kartochki-faza-1` в отчёте есть и несёт число коммитов и состав файлов.
7. Обе зоны чисты **раздельными командами, по одной на репозиторий** — объединённая строка через « · » даёт вакуозный ✅ и запрещена (это чинилось предыдущим заходом, не воспроизводи).

🔴 **ЖИВОЙ ПРОГОН НА РЕАЛЬНОМ ОБЪЕКТЕ РЕПОЗИТОРИЯ — обязателен, фикстур нет.** `check_karty.py`
гоняется на настоящем реестре уже НА `arka/mat-kostyak` после слияния, а не на своей ветке: смысл
пункта 5 именно в том, чтобы поймать случай «починили, но не доехало».

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Слияние и переключение ветки — обе операции лоссовые и теряют молча.** Верификатор нужен, но
короткий: сверка состава программно, без субагента.

До слияния сними `git --no-optional-locks diff --stat arka/mat-kostyak...zahod/final-sessii`
(список файлов и строк). После — убедись, что каждый файл из этого списка присутствует на
`arka/mat-kostyak` с тем же числом строк. **Вердикт заявляй охватом: «сверено N файлов из N».**
Без охвата вердикт не принимается — «проверено 2 из 7» и «7 из 7» выглядят одинаково.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/ · _studio/docs/ · _studio/konvejer/ · _generator/tools/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/ · _studio/docs/ · _studio/konvejer/ · _generator/tools/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_studio/zhurnal/` `·` `_studio/docs/` `·` `_studio/konvejer/` `·` `_generator/tools/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/ · _studio/docs/ · _studio/konvejer/ · _generator/tools/` → ✅. Красное здесь — отчёт не принимается: приёмка гоняет этот же гейт первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

1. Worktree `posadka` уже заведён (`python3 _generator/tools/git_zona.py worktree add posadka --branch zahod/posadka`) — идемпотентно, папка существует. `cd` в неё, проверить `git rev-parse --abbrev-ref HEAD` == `zahod/posadka`.
2. §0.1: слить `zahod/final-sessii` в свою зону (`_studio/zhurnal/` · `_studio/docs/` · `_studio/konvejer/` · `_generator/tools/`) через `git_zona.py merge`, проверить фактом (`git log --oneline` — виден коммит `zahod/final-sessii`). Не сошлось — СТОП в `## ВОПРОСЫ`.
3. Шаг 1 (materials): САМ проверить предпосылку "7 коммитов вперёд, 0 своих сверху" двумя `rev-list --count` в обе стороны — не верить числу из захода на слово. Снять состав `diff --stat arka/mat-kostyak...zahod/final-sessii` для зоны слияния и отчёта. Только если оба числа сошлись (7 и 0) — слить через `git_zona.py` (синтаксис для слияния «в чужую ветку arka/mat-kostyak» в файле не дан явно для этого направления — уточню через `--help` инструмента, это использование двери, не изучение проекта). Не сошлось — СТОП, доложить, не сливать.
4. Верификатор (без субагента): сверить список файлов/строк из `diff --stat` до слияния с тем, что реально лежит на `arka/mat-kostyak` после — вердикт с охватом «сверено N из N».
5. Шаг 2 (disciplina, второй репозиторий, абсолютный путь): проверить `git status --porcelain` пусто → если пусто, `checkout main`; если НЕ пусто — ничего не переключать, доложить. После — проверить действием (`ls doma/*/RAZVITIE.md | wc -l` == 4) и `rev-parse --abbrev-ref HEAD` == `main`. Ветку `zahod/format-kartochki-faza-1` не трогать, снять по ней `diff --stat main...` и даты для отдельного блока отчёта.
6. Прогнать критерий готовности (7 пунктов) командами, коды возврата первым.
7. Гигиена Г1–Г6 в зоне `_studio/zhurnal/` · `_studio/docs/` · `_studio/konvejer/` · `_generator/tools/`.
8. Коммит своей зоны (§4, два хода: `add`, затем `commit -- <пути>`), `git_zona.py check --zone`, `show --stat`.
9. Заполнить `## ОТЧЁТ` с АРТЕФАКТОМ (или «артефакта нет: git-заход, файлов не собирает») и КОММИТОМ.

Оспариваю ли критерий готовности: нет, выглядит корректным и совпадает с задачей. Открытая развилка — единственная: точный синтаксис `git_zona.py merge` для слияния в НЕ-текущую ветку (`arka/mat-kostyak`), проверю через `--help` до слияния, не угадывая.

## ВОПРОСЫ — (заполняет исполнитель)

1. **Шаг 1 (materials) — СТОП, не слито.** Премиса захода «`arka/mat-kostyak` не имеет ничего своего сверху → перемотка без конфликтов» верна для КОММИТОВ (проверено: `rev-list --count arka/mat-kostyak..zahod/final-sessii` = 7, обратное = 0), но НЕВЕРНА для рабочего дерева основной папки: там несохранённые правки аналитика в `_studio/docs/KARTA.md` (+8 строк, регистрация `kod_zakryt-arku.md`/`kod_uroven-instrumentov.md`/`kod_final-sessii.md`/`kod_posadka.md` СВОИМИ описаниями) и в `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (+9 строк, инциденты 09–10.08 на ветке `arka/mat-kostyak`), которые содержательно РАСХОДЯТСЯ с тем, что несёт то же слияние (те же файлы регистрирует по-своему на `zahod/final-sessii`, плюс 3 своих строки инцидентов). `git_zona.py merge` сам это увидел и отказал (rc=1, ничего не тронуто): «6 путей одновременно грязные и участвуют в слиянии — не сливаю». Из шести четыре (`kod_final-sessii.md`, `kod_uroven-instrumentov.md`, `kod_vygruzka-transkripta.md`, `kod_zakryt-arku.md`) — байт-в-байт дубликаты входящего содержимого, безвредны; два (`KARTA.md`, `INCIDENTY.md`) — реальное расхождение контента (обе стороны добавляют РАЗНЫЕ строки в одну точку вставки), это решение по содержанию документов, не git-операция, и не в зоне моих полномочий («заход… содержательных файлов не пишет»). Нужно решение владельца: либо владелец/аналитик сам коммитит свои правки в `KARTA.md`/`INCIDENTY.md` на `arka/mat-kostyak` (после чего слияние станет реальным трёхсторонним, вероятно с конфликтом ровно в этих двух файлах — обе стороны надо объединить, не выбирать), либо откатывает свои несохранённые правки и повторяет попытку. Состав слияния (для зоны, когда решение принято) — `_studio/zhurnal/` · `_studio/docs/` · `_studio/konvejer/` · `_generator/tools/`, снято командой `git --no-optional-locks diff --stat arka/mat-kostyak...zahod/final-sessii` (11 путей, все внутри этих четырёх префиксов).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. **Шаг 2 (disciplina) — СТОП, не переключено.** Рабочее дерево основной папки чисто (`git status --porcelain` → пусто), но `git checkout main` упал с rc=128: `'main' is already used by worktree at '.../disciplina-wt/final-sessii'` — ветка `main` уже выписана в ЧУЖОЙ параллельной рабочей папке. HEAD основной папки не сдвинулся (проверено повторным `rev-parse` — всё та же `zahod/format-kartochki-faza-1`), ничего не тронуто. Контракт запрещает трогать чужие рабочие папки соседних заходов — решаю не форсировать. Нужно решение владельца: либо снять `main` с `disciplina-wt/final-sessii` (если тот заход закрыт), либо посадка disciplina откладывается до его закрытия.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. **Блок про `zahod/format-kartochki-faza-1` (disciplina)** — не вливать, не удалять, не трогать (по контракту), для решения владельца: 17 коммитов вперёд `main`, диапазон 2026-08-08 01:40 → 2026-08-09 23:13, состав — `skills/slajdy/DOLG.md`, `skills/slajdy/SKILL.md` и 13 файлов в `skills/slajdy/references/` и `skills/slajdy/verifikatory/` (15 файлов, +1304/−227 строк). Снято `git --no-optional-locks diff --stat main...zahod/format-kartochki-faza-1`.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. **Урок фабрике** — критерий готовности захода на «перемотку без конфликтов» проверяется только по КОММИТАМ (`rev-list --count`), но не по РАБОЧЕМУ ДЕРЕВУ целевой ветки — а именно рабочее дерево держит несохранённые правки аналитика (сам заход это знает и называет как отдельную категорию находок в §0.1.2, но не связывает с проверкой премисы слияния в §2). На эту же основную папку может быть выписана чужая ветка другим параллельным заходом (как случилось с `main` в disciplina) — критерий готовности «просто checkout main» этого не предусматривает и не даёт альтернативы. Оба раза инструмент (`git_zona.py merge`, `git checkout`) сам безопасно отказал, потерь нет — но обе ступени задачи не выполнены.
   ДОМ: 2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал / зачем:**
- Создал воркчейв `posadka` (`git_zona.py worktree add`), проверил ветку — `zahod/posadka`.
- §0.1: слил `zahod/final-sessii` в зону воркчейва `posadka` (`git_zona.py merge`, rc=0, fast-forward до `be19bb0`). Проверено фактом: коммит виден в `git log --oneline`.
- Шаг 1 (materials): проверил премису двумя `rev-list --count` в обе стороны (7 и 0 — сошлось), снял `diff --stat` для зоны слияния (11 путей, все внутри моих 4 префиксов), затем попытался слить `zahod/final-sessii` → `arka/mat-kostyak` из основной папки (она и так стоит на этой ветке — чекаут не требовался). `git_zona.py` сам отказал: 6 путей зоны грязные и пересекаются со сливаемым. Разобрал, какие из них безвредные дубликаты и какие — реальное расхождение контента (детали — `## ВОПРОСЫ` #1). Не сливал. Ничего не тронуто (подтверждено повторной проверкой branch/MERGE_HEAD/числа грязных путей — 31 до и после).
- Шаг 2 (disciplina): проверил чистоту дерева основной папки (`status --porcelain` пусто), попытался `checkout main` — упал rc=128 (ветка занята другим воркчейвом). HEAD не сдвинулся. Не форсировал.
- Гигиена: Г1 — все 4 префикса зоны воркчейва `posadka` по отдельности `git_zona.py check --zone` → ✅ (после известного бага "литеральный префикс" чек нужен ПООДИНОЧНЕ, не одной строкой с `·`). Г2 — второй репозиторий (disciplina) проверен, чист. Г3 — `branch --no-merged arka/mat-kostyak` = 4, из них 1 — моя же новая `zahod/posadka` (легитимно, контракт). Г4 — новых `.py` не заводил, неприменимо. Г5 — новых `.md` не заводил (кроме самого файла-захода, который не регистрируется по правилу), неприменимо. Г6 — коммитов не было, неприменимо.

**Как проверил:** каждая команда — код возврата первым, критические факты (7/0, MERGE_HEAD, число грязных путей, содержимое диффов) сняты и перечитаны, а не приняты на слово из текста захода.

**Что НЕ трогал:** содержимое `KARTA.md`/`INCIDENTY.md` в основной папке `materials` (несохранённые правки аналитика — не моя зона решений); ветку и рабочую папку `zahod/format-kartochki-faza-1`; ветку `main` и рабочую папку `disciplina-wt/final-sessii`; всё вне зоны (`_fond/`, `ucheniki/`, `obzory/`, `_generator/build_listok.py` — увидены как грязные в основной папке `materials` мимоходом при `status --porcelain`, не мои, не трогал).

**Результат верификатора:** неприменимо — слияние Шага 1 не состоялось, сверять состав после слияния не с чем (0 из 0).

**Время прогона + токены:** неприменимо, канал `app`.

**ПОВТОРЯЕМОСТЬ находок:** повторится на следующей единице работы — да, класс НЕМЕДЛЕННОЕ: приёмка/следующий заход посадки столкнётся с той же парой блокеров, если владелец не примет решение по ВОПРОСАМ #1 и #2 (это не «может подождать», а условие, без которого сама задача захода не выполнима). Находка не про содержание слайдов/лекций, а про саму фабрику заходов — вынесена отдельно в `## ВОПРОСЫ` #4 (`ДОМ: UROKI-FABRIKE.md`), как и требует правило.

**НЕОБРАТИМОЕ:** необратимого нет — обе попытки (`git checkout`, `git_zona.py merge`) сами безопасно отказали до какого-либо изменения диска; проверено повторными `rev-parse`/`status`.

**АРТЕФАКТ:** артефакта нет: заход состоит из git-операций и замеров, файлов не собирает.

**КОММИТ:** нет хэша — в зоне (`_studio/zhurnal/` · `_studio/docs/` · `_studio/konvejer/` · `_generator/tools/`) воркчейва `posadka` не появилось ничего своего: единственное изменение — fast-forward слияние `zahod/final-sessii` → `zahod/posadka` (не создаёт нового коммита, только сдвигает указатель на уже существующий `be19bb0`). Зона чиста, `git_zona.py check --zone` → ✅ по всем четырём префиксам отдельно. Коммитить нечего — обе содержательные ступени задачи (Шаг 1, Шаг 2) остановлены до решения владельца, ничего своего заход не писал.
