# Канал исполнителя — svedenie-arki (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — слияние механическое, но проверка совместной работы требует решений по ходу:
> что считать регрессией, а что законным красным. Субагент — на сверку обещаний, см. §3.

КОНТЕКСТ. Фабрика научно-популярных лекций. **Ты закрываешь длинную арку: сводишь всё сделанное за
сессию в основное дерево и проверяешь, что оно работает ВМЕСТЕ.**

🔴 **До твоего слияния в живом дереве не работает НИЧЕГО из сделанного за день.** Проверено:
```
git branch --no-merged arka/mat-kostyak   → 6 веток
git ls-tree arka/mat-kostyak -- _generator/sborka/smeta.py        → пусто (сметы нет)
git ls-tree arka/mat-kostyak -- _generator/tools/gejt_verstki.py  → пусто (гейта вёрстки нет)
```
Смета вмещения, гейт вёрстки, карта живого конвейера, кэш формул KaTeX, ужесточение гейта фазы 1 —
всё это построено, проверено, закоммичено и **невидимо из основной папки**. Доктрина вёрстки уже
доехала в репозиторий `disciplina` отдельно (коммит `c344433`), но её механический гейт — нет.

**Что сводится (три ветки; остальные три уже влиты внутрь `svedenie-i-smeta`):**

| ветка | что несёт |
|---|---|
| `zahod/svedenie-i-smeta` | смета вмещения, гейт расхождения, подключённый гейт вмещения **+ уже влитые внутрь** `instrument-podklyuchen`, `uroki-faz-1-2`, `hvosty-adresa`, `sborka-l2`, `konsolidaciya-i-git-kontur` |
| `zahod/verstka-v-faze-1` | `gejt_verstki.py`, фикстуры, `POMARKI-SBORKA-L2.md`. ⚠ **И дубль `disciplina/` внутри `materials` — два файла, которые надо снять при слиянии, см. Ш2** |
| `zahod/karta-konvejera` | карта живого конвейера, честный отказ `sostoyanie.py` |

**Вторая половина работы — нумерация.** Верификатор прошлого захода нашёл коллизию, я подтвердил
командой: в `DOLG.md` **57 долгов Д1–Д57**, а доктрина вёрстки пришла с маркерами `(Д1)…(Д6)`.
В одном файле `SKILL.md` строка 608 — `(Д4)` про обложку, строка 905 — `(Д4)` про долг о весах слов.
**Одно обозначение, два смысла, соседние страницы.** Грепом одно ловится вместо другого — а грепом
в этой фабрике проверяется всё.

ЦЕЛЬ: `git branch --no-merged arka/mat-kostyak` пуст, всё построенное работает вместе на живой
лекции, маркеры разведены.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: слияние прошло, а совместная проверка красная — это ЗАКОННЫЙ исход.** Сдай
слияние с честным списком красного. **Не законный исход — «влил, гейты не гонял».**

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/svedenie-arki`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/svedenie-arki`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add svedenie-arki --branch zahod/svedenie-arki
  cd ../materials-wt/svedenie-arki
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- 🔴🔴 **ДВА РЕПОЗИТОРИЯ. Путаница между ними уже стоила одного целого захода — не повтори.**

| репо | АБСОЛЮТНЫЙ путь | что делаешь |
|---|---|---|
| **`materials`** | `~/Documents/GitHub/materials`, worktree `../materials-wt/svedenie-arki` | **сливаешь три ветки**; правишь `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` и `_studio/docs/KARTA.md` |
| **`disciplina`** | `~/Documents/GitHub/disciplina` | правишь `skills/slajdy/**` (только переименование маркеров, Ш3). 🔴 **Ветку НЕ переключай** |

- 🔴 **Перед каждой правкой проверь, где ты:** `basename $(git rev-parse --show-toplevel)`.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `disciplina/skills/slajdy/**` (последний путь — во ВТОРОМ репозитории, см. таблицу выше). Всё вне — **READ-ONLY**.
- ⚠ **Слияние — операция НАД ветками, а не правка зоны:** сливая, ты законно приносишь чужие файлы (`_generator/**`, `.githooks/**`, `teorkat-vvedenie/L2/**`). Это не нарушение зоны. Но **руками** вне зоны не правь ничего: сломалось после слияния → в отчёт, не чини.
- 🔴 **Заходов рядом сейчас НЕТ — поле чистое.** Но `teorkat-vvedenie/L2/**` владелец правит в другом чате прямо сейчас: **не пиши туда ни байта**, только читай. Изменения там могут появиться под тобой — это не конфликт слияния, это живая работа.
- 🔴 **В основной папке `materials` есть незакоммиченное, НЕ ТВОЁ:** `_fond/**`, `obzory/**`, `_generator/build_listok.py` — чужая незавершённая работа. **Не коммить и не трогай.** Твои из незакоммиченных — только файлы арки и `KARTA.md` (Ш4).
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ВЛИТИЕ — ДО ВСЕГО ОСТАЛЬНОГО

```
python3 _generator/tools/git_zona.py merge zahod/svedenie-i-smeta --zone _studio/zhurnal/2026-08-07_arhitektura-slajdov/** --zone _studio/docs/KARTA.md --zone disciplina/skills/slajdy/**
python3 _generator/tools/git_zona.py merge zahod/verstka-v-faze-1 --zone _studio/zhurnal/2026-08-07_arhitektura-slajdov/** --zone _studio/docs/KARTA.md --zone disciplina/skills/slajdy/**
python3 _generator/tools/git_zona.py merge zahod/karta-konvejera --zone _studio/zhurnal/2026-08-07_arhitektura-slajdov/** --zone _studio/docs/KARTA.md --zone disciplina/skills/slajdy/**
```

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/svedenie-i-smeta`
# обязан быть виден коммит ветки `zahod/verstka-v-faze-1`
# обязан быть виден коммит ветки `zahod/karta-konvejera`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/svedenie-arki`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `disciplina/skills/slajdy/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 **Секции `## ОТЧЁТ` трёх сливаемых заходов**: `kod_svedenie-i-smeta.md`, `kod_verstka-reshaetsya-v-faze-1.md`, `kod_karta-zhivogo-konvejera.md` | **список обещаний**, которые ты обязан проверить фактом после слияния (§3) |
| Я2 | `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` — **строки 600–620 и 900–910**, не весь файл | обе стороны коллизии маркеров, Ш3 |
| Я3 | `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md` — **только формат строк долгов** | как выглядит `Д<N>` долга; 57 занятых номеров |
| Я4 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/PRAVKI-SBORKI-L2.md` и `POMARKI-SBORKA-L2.md` | ещё два дома тех же маркеров — переименование обязано быть сквозным |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — четыре шага

### Ш1. СЛИТЬ ТРИ ВЕТКИ

Команды — в §0 выше. ⚠ **Урок прошлого захода: `--zone` в командах слияния должна покрывать ВСЁ,
что ветка трогает, а не только твою зону** — иначе инструмент откажется сливать и напечатает верную
строку зон; возьми её. Ветки НЕ удаляй ни при каком исходе — это решение владельца.

**Проверка фактом, а не словом инструмента:** по каждой из трёх — `git merge-base --is-ancestor`.
Плюс по пяти, влитым внутрь `svedenie-i-smeta`: они обязаны стать предками тоже.

### Ш2. 🔴 СНЯТЬ ДУБЛЬ, КОТОРЫЙ ПРИЕДЕТ СО СЛИЯНИЕМ

`zahod/verstka-v-faze-1` несёт **два файла-дубля внутри `materials`**:
```
disciplina/skills/slajdy/SKILL.md                       (129 строк — обрубок)
disciplina/skills/slajdy/references/kartochka-slajda.md  (62 строки — обрубок)
```
Настоящие живут в отдельном репозитории и там **1473 и 371 строка**. Содержание из обрубков уже
перенесено в настоящие (коммит `c344433`), проверено субагентом — шесть правил из шести.
⇒ **После слияния удалить оба и папку `disciplina/` внутри `materials` целиком.**
🔴 **Сначала убедись, что перенос на месте**, и только потом удаляй:
`grep -c 'obosnovanie_verstki' ~/Documents/GitHub/disciplina/skills/slajdy/references/kartochka-slajda.md` → **3**.
Не сошлось — НЕ удаляй, в отчёт.

### Ш3. РАЗВЕСТИ МАРКЕРЫ

`(Д1)…(Д6)` доктрины вёрстки сталкиваются с `Д1…Д57` долгов. **Выбери префикс сам и обоснуй в
`## ПЛАН`** — требования к нему три: не пересекается с существующими обозначениями фабрики ·
греппится однозначно · читается человеком. (Очевидный кандидат — `ДВ<N>`, «доктрина вёрстки», но
решаешь ты: проверь грепом, что выбранное не занято.)

🔴 **Переименование сквозное, все дома одним ходом** — их четыре:
`disciplina/skills/slajdy/SKILL.md` · `disciplina/skills/slajdy/references/kartochka-slajda.md` ·
`POMARKI-SBORKA-L2.md` · `PRAVKI-SBORKI-L2.md`.
⚠ **Не тронь `(Д4)` в `SKILL.md` строка ~905** — там это ссылка на долг Д4, а не на доктрину. Разбери
каждое вхождение по смыслу; слепой `sed` по всему файлу здесь запрещён.

### Ш4. ЗАКОММИТИТЬ ХВОСТЫ АРКИ

В основной папке незакоммичены отчёты заходов и `_studio/docs/KARTA.md`. Их коммитишь **явными
именами файлов**. 🔴 `_fond/**`, `obzory/**`, `_generator/build_listok.py` — **чужое, не трогать.**

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `git branch --no-merged arka/mat-kostyak` → **пусто**. Все восемь веток — предки, поимённо.
2. 🔴 **ЖИВОЙ ПРОГОН НА РЕАЛЬНОМ ОБЪЕКТЕ РЕПОЗИТОРИЯ — главный пункт, ради него заход.** После Ш1,
   по живой лекции `teorkat-vvedenie/L2` (**только чтение**), все команды с rc в отчёт:
   ```
   python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2
   python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2 --faza 1
   python3 _generator/tools/gejt_illyustracij.py teorkat-vvedenie/L2
   python3 _generator/tools/graf_zavisimostej.py teorkat-vvedenie/L2
   python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2
   python3 _generator/tools/sostoyanie.py teorkat-vvedenie/L2
   python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o /tmp/L2.html --bez-podbora
   grep -c MISSING-MATH /tmp/L2.html
   python3 _generator/sborka/smeta.py --sverit teorkat-vvedenie/L2
   ```
   ⚠ **Красное здесь не всегда регрессия.** `gejt_verstki.py` обязан краснеть — обоснований вёрстки
   в карточках ещё нет. `--faza 1` может краснеть на `centr-gruppy`. **По каждому красному скажи:
   законное или регрессия**, и это главное содержание отчёта.
   ⚠ Лекцию правит владелец в другом чате — числа могут отличаться от вчерашних. Не регрессия.
3. Регресс: все `PROGNAT.sh` (число — `find _generator -name PROGNAT.sh | wc -l`), зелёных N из N.
4. Дубля нет: `git ls-tree -r --name-only HEAD -- disciplina/ | wc -l` → **0**; перенос на месте (Ш2).
5. Маркеры разведены: старый префикс не встречается ни в одном из четырёх домов; новый встречается
   шесть раз в каждом, где должен. Команды и числа в отчёт. `Д4`-ссылка на долг цела.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «свёл», а «влито N из 3, обещаний проверено M из K,
красных гейтов L, из них регрессий R».

## 3. ВЕРИФИКАТОР

🔴 **Слияние — операция лоссовая, и потеря при ней не кричит:** конфликт разрешается выбором, а
выброшенное обнаруживается через неделю, когда следующий заход делает сделанное заново. Сегодня так
уже было трижды.

- 🔴 **Свежий субагент со списком обещаний из Я1** (три секции `## ОТЧЁТ`) проверяет **фактом на
  слитом дереве**, что каждое на месте: смета и её команды, гейт расхождения, подключённый гейт
  вмещения, гейт вёрстки и фикстуры, карта конвейера, отказ `sostoyanie.py`, кэш формул KaTeX,
  вендор `katex.min.js` под git, освобождение `narrative`, клаузы верификаторов, адрес рендерера.
  **Охват числом: проверено N обещаний из M.** Пропавшее — потеря, а не мелочь.
- **Ш3 проверяет ДРУГОЙ субагент**, по смыслу, а не грепом: каждое вхождение нового префикса
  действительно про доктрину вёрстки, каждое оставшееся `Д<N>` — действительно про долг.
- **Ничего лишнего не уехало:** `git show --stat` по своим коммитам; в них нет `_fond/`, `obzory/`,
  `build_listok.py`.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `disciplina/skills/slajdy/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `disciplina/skills/slajdy/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Обозначение заводят в документе, а инструмент, который его печатает, размечают тем же ходом — иначе разведение обозначений разводит только половину
Доктрина вёрстки пришла как `Д1…Д6` в трёх документах И в гейте `gejt_verstki.py`, который эти
маркеры печатает пользователю на экран. Заход на разведение перечислил только документы. В
результате правка, сделанная строго по заданию, оставляет фабрику в состоянии хуже исходного:
раньше документ и инструмент говорили одинаково неверно (`Д6` = и долг, и доктрина), теперь они
говорят РАЗНОЕ — файл `ДВ6`, экран `Д6`. Класс шире этого случая: обозначение живёт не там, где его
определили, а везде, где его печатают.
ЦЕНА: коллизия, ради устранения которой заход и запускался, в инструменте осталась жива —
16 вхождений в `gejt_verstki.py` плюс фикстуры; отдельный заход поверх только что закрытого.
Найдено не заданием, а прогоном самого гейта на живой лекции: он напечатал `[Д1] границы
«много/среднее/мало»` через минуту после того, как я переименовал `Д1` в документах.

### Зелёный контракт инструмента на своей ветке ничего не обещает: строгость проверки приезжает с чужой ветки, и «проверено 1 из 1» протухает в момент свода
`gejt_verstki.py` сдал `check_tool_contract.py` с rc=0 на ветке `verstka-v-faze-1` — честно: на той
ветке проверка состояла из одной половины (маркер охвата). Вторая половина, `has_live_trigger`,
жила на невлитой соседней ветке `instrument-podklyuchen` и требует ЖИВОЙ ТОЧКИ ВЫЗОВА. На слитом
дереве та же команда даёт rc=1. Ни один из двух исполнителей не ошибся и не соврал; сломалось
ровно то, что на ветках не проверяется в принципе — совместимость контрактов. Класс: гейт, который
сам приезжает ветками, не может служить критерием готовности ветки.
ЦЕНА: `pre-commit` зовёт `check_tool_contract.py --staged`, поэтому следующий исполнитель, тронувший
`_generator/tools/gejt_verstki.py`, встанет на красном, которого не создавал, — и, по автологу
`INCIDENTY.md`, пойдёт его обходить (28 обходов `--no-verify` из 56 срывов коммита). В этой арке
разъезд «инструмент влит раньше строгой проверки» случился трижды; сегодня он дал первую живую
поломку, и нашли её не девять команд критерия, а верификатор обещаний — то есть исполнитель,
отчитавшийся строго по критерию, сдал бы этот заход зелёным.

### Список домов в задании — это гипотеза аналитика, а не факт с диска, и проверять его надо ДО правки
Заход назвал четыре дома маркеров и потребовал сквозного переименования во всех четырёх. Проверка
командой: в одном из них (`PRAVKI-SBORKI-L2.md`, 425 строк) маркеров нет вовсе —
`grep -cE 'Д[0-9]' → 0`. Это разбор владельца по слайдам, то есть ИСТОЧНИК доктрины; пронумеровали
её позже и в других файлах. При этом пятый, настоящий дом (сам гейт) в списке отсутствовал. То есть
список был неверен с обеих сторон сразу — и по лишнему, и по недостающему.
ЦЕНА: исполнитель, доверившийся списку, либо впишет `(ДВ1)…(ДВ6)` в живой текст владельца, чтобы
«сквозное переименование» сошлось (порча источника), либо отчитается «4 из 4 домов» при трёх
реальных из пяти. Оба исхода тихие: критерий готовности покраснеть на них не может, потому что он
сформулирован через тот же самый список.

### Критерий готовности, сформулированный через ветку, которую исполнителю запрещено трогать, не может позеленеть ни при какой работе
Критерий 1 требует `git branch --no-merged arka/mat-kostyak` → пусто. Контракт зоны того же файла
запрещает вливать свою ветку: «влитие делает приёмка». Пока моя ветка не влита, три слитых в неё
ветки предками `arka/mat-kostyak` не становятся — критерий красный по построению, а не по работе.
ЦЕНА: второй заход подряд платит одним и тем же. Исполнитель `svedenie-i-smeta` разобрал ровно это
в своих `## ВОПРОСЫ` п.1 вчера; правка в генератор заходов не доехала, и блок пришёл ко мне
дословно тем же. Стоимость на заход невелика (разбор + два прогона вместо одного), но она
повторяется на КАЖДОМ заходе со сведением, а главное — приучает читать красный критерий как шум.

### Однокомандная тропа коммита не умеет сказать «эти пути, но не соседние» — и на живом репозитории это не редкость, а норма
`git_zona.py commit --zone X -m …` берёт ВСЁ грязное под зоной. В папке арки рядом с моими десятью
хвостами лежали два файла, которые мне коммитить запрещено: мой собственный файл-заход (по
контракту его коммитит приёмка) и `kod_granica-analitik-ispolnitel.md`, заведённый владельцем в
другом чате в ту же минуту. Общего префикса пути, который включает первые и исключает вторые, не
существует — они в одной папке.
ЦЕНА: одна командная тропа не работает вовсе; пришлось идти через `plan` с правкой файла плана
руками — ровно тот путь, который сам инструмент в комментарии к `commit_zone_oneshot` называет
источником пяти живых срывов 23.07. Это не «редкий случай»: в репозитории трое писателей и правило
«свой заход не коммитить», так что исключение из зоны — штатная ситуация, а не край.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Разведка сделана только чтением (никаких мутаций до этой строки). Ниже — что я нашёл ДО работы,
включая три места, где задание расходится с диском, и мои решения по ним.

### П1. Префикс маркеров доктрины: `ДВ<N>` («Доктрина Вёрстки»)

Три требования захода проверены командами, а не рассуждением:
- **не занят:** `grep -rnoE '\bДВ ?[0-9]+' --include='*.md' --include='*.py'` по ОБОИМ репозиториям
  (`disciplina` и `materials`) → пусто, 0 вхождений;
- **греппится однозначно в обе стороны:** `Д` в `ДВ1` не сопровождается цифрой, поэтому будущий
  `grep -oE 'Д[0-9]+'` (которым в фабрике считают долги) новый префикс НЕ подхватит, а `grep 'ДВ1'`
  не подхватит долг `Д1`. Разведение полное, а не косметическое;
- **читается человеком:** «ДВ4» разворачивается в «доктрина вёрстки, пункт 4».
Занятые однобуквенные обозначения в `skills/slajdy/*.md` (счёт командой): `Д` 170, `Ф` 95, `Р` 41,
`Б` 24, `З` 19, `Н` 18, `Ш` 16, `В` 8, `Я` 4, `К` 4, `Э` 3, `У` 3, `Г` 2, `С` 1 — двухбуквенный
префикс не сталкивается ни с одним из них.

### П2. Порядок ходов
1. **Ш1** — три `git_zona.py merge` в рабочей папке, зоны беру из отказа инструмента, если моя строка
   `--zone` не покрывает всё, что трогают ветки (урок прошлого захода, §2 Ш1). Проверка — не словом
   инструмента, а `git merge-base --is-ancestor` поимённо по всем восьми веткам.
2. **Ш2** — сперва доказать перенос (`grep -c 'obosnovanie_verstki' …/kartochka-slajda.md` → 3),
   только потом сносить `disciplina/` внутри `materials`.
3. **Критерий 2 — живой прогон** девяти команд на `teorkat-vvedenie/L2` с кодами возврата, и по
   каждому красному отдельный вердикт «законное / регрессия». Это главное содержание отчёта.
4. **Критерий 3** — регресс всех `PROGNAT.sh`, число — командой.
5. **Ш3** — переименование по смыслу, поштучно, каждое вхождение разобрано (список — П4).
6. **Верификаторы** — два свежих субагента (§3): обещания трёх отчётов и смысловая проверка Ш3.
7. **Ш4 + коммит** зоны.

### П3. 🔴 Три расхождения задания с диском — называю ДО работы

**(а) `PRAVKI-SBORKI-L2.md` НЕ является домом маркеров.** Заход (Ш3, Я4) называет четыре дома, в
каждом из которых переименование обязано быть сквозным. Факт с диска:
`grep -cE 'Д[0-9]' _studio/zhurnal/2026-08-07_arhitektura-slajdov/PRAVKI-SBORKI-L2.md` → **0**;
`grep -noE 'Д ?[0-9]+'` → пусто на всех 425 строках. Это разбор владельца по слайдам (§1 «ПРАВИЛА
ВЁРСТКИ — доктрина владельца»), то есть ИСТОЧНИК доктрины, но пронумерована она была уже позже, в
`POMARKI-SBORKA-L2.md` и `SKILL.md`. **Решение: домов маркеров три, а не четыре.** Маркеры в этот
файл не вписываю — дописывать `(ДВ1)…(ДВ6)` в живой текст владельца значит редактировать источник
под свою задачу. В отчёте это будет «0 из 0», а не спрятанный ноль.

**(б) «шесть раз в каждом доме» (критерий 5) не сходится с диском ни в одном из домов.** Разметка
доктрины несимметрична, и это её свойство, а не дефект:
| дом | вхождений доктрины | какие |
|---|---|---|
| `SKILL.md` | **7** | Д1, Д2, Д3 (дважды: §5.4 фазы 1 и «продолжение» в фазе 2), Д4, Д5, Д6 |
| `references/kartochka-slajda.md` | **6** | Д5 ×3, Д6 ×3 (только два поля этим файлом и заводятся) |
| `POMARKI-SBORKA-L2.md` | **6** | Д1…Д6 по одному |
Формулировку критерия читаю как «все шесть маркеров разведены в каждом доме, где они есть», а числом
в отчёт понесу фактические 7 / 6 / 6 — а не подгоню разметку под цифру задания.

**(в) 🔴 Критерий 1 недостижим по построению, и это уже второй заход подряд.** Заход требует
`git branch --no-merged arka/mat-kostyak` → пусто, и ОДНОВРЕМЕННО (контракт зоны) «ветку в конце НЕ
вливай — влитие делает приёмка». Я сливаю три ветки в `zahod/svedenie-arki`; предками
`arka/mat-kostyak` они от этого не становятся. Тот же разбор уже сдал исполнитель
`kod_svedenie-i-smeta.md` (`## ВОПРОСЫ` п.1) — правка в генератор заходов не доехала.
**Решение:** гоняю ОБА и сдаю оба числа: `--no-merged zahod/svedenie-arki` (исполнимый эквивалент,
обязан быть пуст — это мой критерий) и `--no-merged arka/mat-kostyak` (обязан печатать ровно мою
ветку и ничего больше — это доказательство, что приёмке остался один ход). Пункт очереди — ВОПРОСЫ.

### П4. Разбор каждого вхождения Д1–Д6 по смыслу (слепой `sed` запрещён)
Переименовываю **только** помеченные ⇒:
- `SKILL.md:321` `(Д3)` иллюстрация обязана иллюстрировать ⇒ **доктрина**
- `SKILL.md:565` `(Д1)` выбор `tip_verstki` от объёма текста ⇒ **доктрина**
- `SKILL.md:587` `(Д2)` полоса без иллюстрации запрещена ⇒ **доктрина**
- `SKILL.md:593` `(Д6)` обоснование вёрстки — выход фазы ⇒ **доктрина**
- `SKILL.md:608` `(Д4)` обложка/визитка/финал — спека генератора ⇒ **доктрина**
- `SKILL.md:629` `(Д5)` заголовок — сознательный переключатель ⇒ **доктрина**
- `SKILL.md:642` `(Д3, продолжение с фазы 1 §5.4)` ⇒ **доктрина**
- `kartochka-slajda.md:65, 115, 124` Д5 · `:79, 106, 113` Д6 ⇒ **доктрина** (оба поля заведены ею)
- `POMARKI-SBORKA-L2.md:38, 57, 64, 77, 97, 107` Д1…Д6 ⇒ **доктрина**

**НЕ трогаю** (это долги `DOLG.md`, соседние страницы того же файла):
- `SKILL.md:905` `(Д4)` веса слов = 1.0, уточнять на живом тексте — прямо назван заходом;
  сверено с `DOLG.md:36`: «*веса слов по типу блока* (Д4 — уточняется на живом тексте)». Совпадает.
- `SKILL.md:911, 912, 947` Д2 — долг про `schet_byudzheta.py`, в тексте прямо «`DOLG.md` Д2»
- `SKILL.md:1455` Д5 — долг про 39 ложных отказов инструмента
- `kartochka-slajda.md:100` Д51/Д38, `:361` Д38 — долги
- все прочие Д7…Д57 в обоих файлах

### П5. Куда ложится Ш4 (называю, потому что это единственное моё действие ВНЕ рабочей папки)
Хвосты арки физически лежат в рабочем дереве ОСНОВНОЙ папки (`arka/mat-kostyak`) и из worktree
недоступны — значит и коммит их возможен только там. Ш4 так и написан («в основной папке»).
`git checkout` там не делаю ни при каком исходе; только `add`/`commit` поимённо по своим файлам.
`_fond/**`, `obzory/**`, `_generator/build_listok.py`, `teorkat-vvedenie/L2/**`, `ucheniki/**` —
чужое, мимо. Свой файл-заход `kod_svedenie-arki.md` не коммичу (контракт зоны), хотя его строка
регистрации в `KARTA.md` уже стоит — назову это в отчёте, чтобы приёмка не приняла за потерю.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. 🔴 **Развести маркеры в САМОМ ИНСТРУМЕНТЕ — `_generator/tools/gejt_verstki.py` (16 вхождений) и
   его фикстурах** (`fixtures/verstki/PROGNAT.sh`, `fixtures/verstki/polosa-bez-illustracii/…/slaid.md`).
   Заход назвал четыре дома маркеров, но пятый — это гейт, который их ПЕЧАТАЕТ НА ЭКРАН: докстринг
   строки 18/20/22/25/29, код 144/146/148/150/152/161, справка 184/193/194/195. Сегодня документация
   говорит «ДВ2 → красный поимённо (`gejt_verstki.py`)», а гейт в терминале печатает `Д2` — то есть
   номер долга «останавливающий гейт объёма фазы 3». Коллизия, ради которой заход и делался, в
   инструменте ЖИВА. `_generator/**` — не моя зона, руками не тронул (контракт зоны, §КОНТРАКТ).
   Подтверждено независимо вторым верификатором Ш3. Правка механическая: `Д<N>` → `ДВ<N>` во всех
   вхождениях этих трёх файлов, все до одного про доктрину.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. **Карточка `POMARKI-SBORKA-L2.md` в `_studio/docs/KARTA.md:412` описывает файл старыми номерами**
   («Доктрина владельца по вёрстке слайдов Д1-Д6, цитатами»), а в самом файле теперь ДВ1–ДВ6.
   Починить нечем: `register_doc.py` — единственная дверь в `docs/`, но у неё нет режима
   «переописать уже зарегистрированный документ» (`register_doc.py --help`: два позиционных
   аргумента, ни одного флага), а руками `docs/` править запрещено контрактом зоны. Либо дверь
   получает такой режим, либо правило «руками нельзя» получает исключение — но не «и то, и то».
   ДОМ: _studio/docs/kak-delat/GIT-disciplina.md
   ДОСТАВЛЕНО: нет
3. **Фикстура `_generator/tools/fixtures/uroki/zakrytie/DOLG.md` пачкает дерево каждым прогоном
   регресса.** Она пишет в себя СЕГОДНЯШНЮЮ дату (`СТАТУС: МЁРТВ · 2026-08-08` → `· 2026-08-09`), то
   есть после любого прогона `PROGNAT.sh` в новый календарный день исполнитель получает грязный путь
   ВНЕ своей зоны, который ему же запрещено чистить. Повторится у каждого следующего исполнителя,
   гоняющего полный регресс. Ожидаемый результат фикстуры надо либо нормализовать по дате, либо
   писать во временную папку.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
4. 🔴 **Приёмке: слияние `zahod/svedenie-arki` → `arka/mat-kostyak` даст РОВНО ОДИН конфликт, и он
   безопасный.** Проверено пробным слиянием, ничего не меняющим:
   `git merge-tree --write-tree --name-only arka/mat-kostyak zahod/svedenie-arki` → rc=1,
   `CONFLICT (content): _studio/docs/KARTA.md`, других файлов в выводе нет. Причина названа:
   обе стороны ДОПИСЫВАЮТ строки регистрации в §6 (моя ветка — `POMARKI-SBORKA-L2.md` и
   `KARTA-ZHIVOGO-KONVEJERA.md`, ваша сторона — коммит `b6d8169` с восемью карточками хвостов).
   Разрешение — объединение, а не выбор стороны: потерять здесь можно только регистрацию, а
   незарегистрированный документ в этой фабрике не существует.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
5. **Отчёт захода `hvosty-adresa` заявляет закрытие долга Д27, а метку `СТАТУС:` в `DOLG.md` не
   переставил** — на этом краснеет `pre-commit` у КАЖДОГО, кто потом коммитит этот файл (у меня — на
   Ш4). Мне чинить нечем: `DOLG.md` живёт во втором репозитории, и контракт зоны разрешает мне там
   только переименование маркеров. Что могу дать ревизии — число и команду, а не вердикт: на слитом
   дереве `grep -c MISSING-MATH /tmp/L2.html` → **0** (в отчёте `karta-konvejera` на невлитом дереве
   было 44), то есть симптом на единственной живой лекции не воспроизводится. Хватит ли одной
   лекции, чтобы переставить метку, решает ревизия долгов, а не я.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
6. 🔴 **ГЛАВНАЯ НАХОДКА ЗАХОДА — `gejt_verstki.py` нарушает контракт инструмента, и видно это ТОЛЬКО
   на слитом дереве.** `python3 _generator/tools/check_tool_contract.py _generator/tools/gejt_verstki.py`
   → **rc=1**, «инструмент нигде не упоминается как вызываемый и не несёт маркера
   `# TOOL-CONTRACT: called-by-hand`». Отчёт захода `verstka-v-faze-1` сдавал по этой же команде
   rc=0 — и не врал: строгая проверка `has_live_trigger` жила на СОСЕДНЕЙ ветке
   `instrument-podklyuchen`, невлитой на тот момент, и на своей ветке гейт вёрстки проходил по
   слабой половине контракта (маркер охвата `# TOOL-CONTRACT-COVERS` — он на месте, строка 2
   фикстуры). Проверено моим грепом независимо: `grep -rn 'gejt_verstki' .githooks _generator
   _studio` вне самого инструмента и его фикстуры не даёт **ни одной** строки — живой точки вызова
   действительно нет. Цена немедленная: `pre-commit` зовёт `check_tool_contract.py --staged`, то
   есть СЛЕДУЮЩИЙ, кто тронет этот файл, встанет на чужом красном. Чинится одной строкой — либо
   подключить гейт вёрстки в `LIFECYCLE_TMPL`/`pre-commit` как фазу, либо объявить его вызываемым
   по руке. `_generator/**` не моя зона, руками не тронул.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
7. **Шапка `_generator/tools/fixtures/zahod/PROGNAT.sh` разошлась с собственным содержимым после
   слияния двух веток в неё.** Строка 9 объявляет «ЛОВУШЕК ДВАДЦАТЬ ШЕСТЬ», а заголовков ловушек в
   файле **27**; строка 3 объявляет охват «девять проверок (З1-З9)», хотя З10 живёт и покрыт
   ловушками 25–27. Фикстура при этом ЗЕЛЁНАЯ — то есть охват объявлен числом, которое ничему не
   обязано соответствовать, и разъезд молчит. Отчёт `svedenie-i-smeta` обещал здесь «29 ловушек и
   шапку, приведённую в соответствие»; из трёх чисел (26 в шапке, 27 в файле, 29 в отчёте) не
   сходится ни одна пара. Вне моей зоны.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
8. **Мой снос дубля (Ш2) оборвал относительный адрес в `_generator/tools/gejt_verstki.py:15`**:
   докстрока пишет «Доктрина — `disciplina/skills/slajdy/SKILL.md`», и внутри `materials` этот путь
   теперь не резолвится. Правильную форму того же адреса показывает соседний инструмент —
   `sostoyanie.py:1147` пишет `~/Documents/GitHub/disciplina/skills/slajdy/`. Правка в одну строку,
   но `_generator/**` не моя зона. Называю как ПОСЛЕДСТВИЕ СВОЕГО ДЕЙСТВИЯ, а не как чужую находку.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Downloads/L2-2026-08-09-svedenie-arki.html` — открыть браузером.
Дека Л2, собранная НА СЛИТОМ ДЕРЕВЕ: 15 слайдов, `grep -c MISSING-MATH` → **0**, rc=0. Собрана в
`~/Downloads`, а не в `teorkat-vvedenie/L2/dist/`, намеренно: лекция по контракту зоны READ-ONLY и
её правит владелец в другом чате прямо сейчас. Тот же файл лежит в `/tmp/L2.html` — это выход
команды из критерия 2 дословно.

**КОММИТЫ — три, в двух репозиториях** (везде `add` + `commit` с теми же путями после `--`,
`--no-optional-locks`; `git show --stat` по каждому — только мои пути, `_fond/`, `obzory/`,
`build_listok.py`, `teorkat-vvedenie/`, `ucheniki/` не попали ни в один, проверено грепом → 0):
| # | репо · ветка | хэш | что |
|---|---|---|---|
| 1 | `materials` · `arka/mat-kostyak` (основная папка) | `b6d8169` | Ш4: хвосты арки, 10 файлов, 3606 строк |
| 2 | `disciplina` · `zahod/format-kartochki-faza-1` | `0d4dfc4` | Ш3: 13 маркеров, 2 файла, 13+/13− |
| 3 | `materials` · `zahod/svedenie-arki` (рабочая папка) | `c73487a` | Ш1+Ш2+Ш3: слияние, снос дубля, POMARKI |

`python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-07_arhitektura-slajdov`
→ ✅ «работа доехала в git, вне git ничего нет».

🔴 **Коммит №1 сделан с `--no-verify` и НАЗВАННОЙ ПРИЧИНОЙ — называю её долгом отдельной строкой,
как требует §4.** `pre-commit` покраснел на ЧУЖОМ пути: отчёт захода `hvosty-adresa` заявляет
закрытие долга Д27, а метку `СТАТУС:` с сегодняшней датой в `DOLG.md` не переставил. Чинить нечем —
`DOLG.md` во втором репозитории, где мне контрактом зоны разрешено только переименование маркеров.
Причина уехала в `INCIDENTY.md` автологом (строка `2026-08-09 01:29`), пункт очереди — ВОПРОСЫ №5.
Своего красного не обходил: на моих путях хук молчал.

---

### ЧТО СДЕЛАНО И ЗАЧЕМ

**Ш1 — три ветки слиты, конфликтов ноль.** Команды §0.1 не работают дословно (тот же отказ, что
поймал прошлый заход): `--zone` в них перечисляет мою зону, а ветки трогают `.githooks/`,
`_generator/`, `teorkat-vvedenie/` — инструмент отказывается сливать НИЧЕГО и сам печатает верную
строку зон. Взял её. Слияния: `zahod/svedenie-i-smeta` → `3972592`, `zahod/verstka-v-faze-1` →
merge `6a6f381`, `zahod/karta-konvejera` → merge `ac81738`, все три rc=0, «влито без конфликтов».

**Проверка фактом, а не словом инструмента** — `git merge-base --is-ancestor <ветка> HEAD` поимённо
по всем ВОСЬМИ, каждая rc=0: `svedenie-i-smeta` `verstka-v-faze-1` `karta-konvejera`
`instrument-podklyuchen` `uroki-faz-1-2` `hvosty-adresa` `sborka-l2` `konsolidaciya-i-git-kontur`.
**Влито 3 из 3 названных; предками стали 8 из 8.**

**Ш2 — дубль снят, но СНАЧАЛА доказан перенос.** `grep -c 'obosnovanie_verstki'` в настоящей
`kartochka-slajda.md` → **3** (заход требовал ровно 3). Сверх этого проверил, что настоящие файлы
не обрубки: 1559 и 393 строки против 129 и 62 у дубля (заход называл 1473 и 371 — файлы с тех пор
выросли, не усохли), все шесть маркеров доктрины и все пять полей карточки на месте. Только после
этого `git rm -r disciplina/`.

**Ш3 — маркеры разведены, префикс `ДВ<N>`.** Обоснование выбора и разбор каждого вхождения по
смыслу — в `## ПЛАН` П1 и П4. Переименование сделано якорной заменой со сверкой «ровно одно
совпадение на якорь, иначе abort и ни один файл не тронут» — не `sed`.

**Ш4 — хвосты арки.** Десять файлов явными именами. Два соседних файла в той же папке в коммит НЕ
взял и называю это вслух: `kod_svedenie-arki.md` (мой файл-заход, его коммитит приёмка по контракту
зоны — но строка его регистрации в `KARTA.md` уже уехала коммитом `b6d8169`, это не потеря, а
рассинхрон на один ход) и `kod_granica-analitik-ispolnitel.md` (заведён владельцем в другом чате в
ту же минуту — чужое). Однокомандная тропа `commit --zone` их обоих подхватывала, поэтому шёл через
`plan` с правкой плана руками; разбор — `## УРОКИ ФАБРИКЕ`.

---

### 🔴 КРИТЕРИЙ 2 — ЖИВОЙ ПРОГОН НА `teorkat-vvedenie/L2` (только чтение)

Все девять команд, коды возврата ПЕРВЫМИ, вердикт по каждому красному:

| команда | rc | результат | вердикт |
|---|---|---|---|
| `sborka/gejt_kartochki.py L2` | **0** | ЗЕЛЁНЫЙ, проверено 15 из 15, замечаний 0 | — |
| `sborka/gejt_kartochki.py L2 --faza 1` | **1** | КРАСНЫЙ, 15 из 15, замечаний **1**: `centr-gruppy` | **ЗАКОННОЕ.** Заход назвал этот случай заранее; отчёт `svedenie-i-smeta` сдал ровно то же число и то же имя. Честный незакрытый выход фазы 1, не регрессия |
| `tools/gejt_illyustracij.py L2` | **0** | ЗЕЛЁНЫЙ, 15 слайдов · 11 ссылок · 11 заказов | — |
| `tools/graf_zavisimostej.py L2` | **0** | циклов нет (15 терминов, 16 рёбер), нарушений порядка нет (15 слайдов, 12 связей) | — |
| `tools/gejt_verstki.py L2` | **1** | КРАСНЫЙ, замечаний **16**: ДВ6 на 15 из 15 карточек + ДВ2 на `izomorfizm` | **ЗАКОННОЕ, и это ЦЕЛЬ гейта.** Обоснований вёрстки в карточках ещё нет вовсе. Число совпало с обещанием отчёта `verstka-v-faze-1` (15 + 1 = 16) и с именем слайда — знак, что при слиянии гейт не потерял ни строки |
| `tools/sostoyanie.py L2` | **2** | «ЭТОТ ТРЕКЕР НЕ ПРИМЕНИМ К ЭТОЙ ЛЕКЦИИ» + диагноз + список живых команд | **ЗАКОННОЕ, и это ЦЕЛЬ захода `karta-konvejera`.** До него здесь печаталось 9 ложных FAIL; ненулевой rc — форма честного отказа, а не поломка |
| `sborka/deck.py L2 -o /tmp/L2.html --bez-podbora` | **0** | собран дек: слайдов 15, в резерве 0 | — |
| `grep -c MISSING-MATH /tmp/L2.html` | 1 (это код `grep` «совпадений нет») | **0** | — |
| `sborka/smeta.py --sverit L2` | **0** | ЗЕЛЁНЫЙ: сошлась на всех **14 из 14** мереных карточках в допуске 1.50; 15-я (`itog`, `polnyj_ekran`) текстовой зоны не имеет и объявлена отдельно, а не спрятана в знаменателе | — |

**Красных три, и все три ЗАКОННЫЕ — регрессий среди них ноль.** Каждое предсказано отчётом своей
ветки заранее, совпали и числа, и имена слайдов.
⚠ **Но одна настоящая регрессия у захода ЕСТЬ, и девять команд критерия её не видят** — её нашёл
верификатор обещаний десятой командой (`check_tool_contract.py` на гейте вёрстки, ВОПРОСЫ №6). Не
прячу её в «красных законных»: критерий 2 зелёный по своему списку, а совместная работа — нет.

🟢 **Находка прогона, которой в задании нет: симптом, которым мерили долг Д27, на слитом дереве не
воспроизводится.** Отчёт `karta-konvejera` намерил на своём (невлитом) дереве **44**
`⟦MISSING-MATH⟧` на L2. На слитом дереве их **0**: кэш формул KaTeX приехал с `svedenie-i-smeta`, а
карта конвейера — с другой ветки, и поодиночке ни одна из них этого показать не могла. Это ровно
тот эффект, ради которого заход и делался: вместе работает не то же, что по отдельности.
⚠ **Вердикта по самому долгу я не выношу и метку в `DOLG.md` не ставлю** — реестр долгов во втором
репозитории, где контракт зоны разрешает мне только переименование маркеров, а «симптом не
воспроизводится на одной лекции» и «долг мёртв» — разные утверждения. Отдаю числом и командой,
решение — ревизии долгов (ВОПРОСЫ №5).

### КРИТЕРИЙ 3 — РЕГРЕСС
Число фикстур — командой: `find _generator -name PROGNAT.sh | wc -l` → **23** (было 22 у прошлого
захода, +1 принесла `fixtures/verstki/` с гейтом вёрстки). **Зелёных 23 из 23, красных 0.**
Прогнан ДВАЖДЫ: сразу после слияния и повторно после сноса дубля и переименования маркеров — оба
раза 23 из 23.

### КРИТЕРИЙ 4 — ДУБЛЯ НЕТ
`git ls-tree -r --name-only HEAD -- disciplina/ | wc -l` → **0**. Перенос на месте: `grep -c
'obosnovanie_verstki'` в настоящем файле → **3**.

### КРИТЕРИЙ 5 — МАРКЕРЫ РАЗВЕДЕНЫ
| дом | новых `ДВ<N>` | оставшихся `Д1`–`Д6` | что это оставшееся |
|---|---|---|---|
| `disciplina/skills/slajdy/SKILL.md` | **7** | 5 (строки 905, 911, 912, 947, 1455) | всё до одного — ссылки на ДОЛГИ `DOLG.md`, проверено верификатором по смыслу |
| `disciplina/…/references/kartochka-slajda.md` | **6** | **0** | — |
| `POMARKI-SBORKA-L2.md` | **6** | **0** | — |
| `PRAVKI-SBORKI-L2.md` | 0 | **0** | маркеров в файле нет и не было, см. `## ПЛАН` П3(а) |
**Итого переименовано 19 вхождений в трёх домах из трёх реальных** (заход называл четыре — четвёртый
маркеров не содержит; зато существует ПЯТЫЙ, вне моей зоны, ВОПРОСЫ №1). Числа 7/6/6 вместо
«шесть в каждом»: `Д3` доктрины стоит в `SKILL.md` дважды (правило и «продолжение с фазы 1 §5.4»),
а `kartochka-slajda.md` заводит только два поля из шести правил — разметка несимметрична по
построению, я её под цифру задания не подгонял.
🔴 **`Д4`-ссылка на долг цела:** `SKILL.md:905` («веса всех слов = 1.0, уточнять на живом тексте, не
на корпусе») не тронут; верификатор сверил его с `DOLG.md:67` — долг Д4 там именно про веса слов по
типу блока. Переименована ровно вторая, `SKILL.md:608` про обложку.

### КРИТЕРИЙ 1 — 🔴 НЕДОСТИЖИМ ДОСЛОВНО, СДАЮ ОБА ЧИСЛА
- **Исполнимый эквивалент — ✅:** `git branch --no-merged zahod/svedenie-arki` не печатает **ни
  одной** ветки заходов. Все восемь — предки, поимённо, каждая проверена `--is-ancestor` (список
  выше). Единственная строка в выводе — `arka/mat-kostyak`, и это не заход, а база: она ушла вперёд
  на один коммит `b6d8169`, который я же в неё и положил ходом Ш4.
- **Дословная формулировка — ❌ и не может быть иначе:** `git branch --no-merged arka/mat-kostyak`
  печатает восемь веток заходов плюс мою. Разбор — `## ПЛАН` П3(в): критерий требует влития, а
  контракт зоны его запрещает. **Приёмке остался ровно один ход** — влить `zahod/svedenie-arki`,
  и все девять строк исчезнут разом. Конфликт при этом будет ровно один и безопасный, я его
  проверил пробным слиянием: ВОПРОСЫ №4.

---

### РЕЗУЛЬТАТ ВЕРИФИКАТОРОВ (§3) — два свежих субагента, оба на слитом дереве

**(а) Обещания трёх отчётов, проверенные фактом.** Список строил сам из секций `## ОТЧЁТ`, каждое
обещание — своей командой с кодом возврата. **Охват: проверено 44 обещания из 44; НА МЕСТЕ 42,
ПОТЕРЯНО 2, НЕПРОВЕРЯЕМО 0.** Прицельно проверены три места, где ветки пересекались руками: обе
ловушки конфликтного `fixtures/zahod/PROGNAT.sh` сложены (24 = Г5, 25–27 = З10), 11 карточек L2
несут `kommentarij_lektoru: ""` без единого плейсхолдера «заполнить», `has_live_trigger` и
`# TOOL-CONTRACT-COVERS` сосуществуют. **Содержательных потерь при слиянии — ноль.**

🔴 **Оба «ПОТЕРЯНО» — не потери текста при слиянии, а обещания, сломавшиеся ОТ СОВМЕСТНОЙ РАБОТЫ.
Я перепроверил каждое своей командой, не поверив на слово:**
1. **`check_tool_contract.py _generator/tools/gejt_verstki.py` → rc=1**, тогда как отчёт
   `verstka-v-faze-1` сдавал rc=0 и «проверено 1 из 1». **Это НАСТОЯЩАЯ РЕГРЕССИЯ, и ровно та,
   ради которой заход существует:** оба отчёта были честны на своих ветках, несовместимость
   родилась в момент их встречи. Разбор и цена — ВОПРОСЫ №6.
2. **Шапка `fixtures/zahod/PROGNAT.sh` объявляет 26 ловушек при 27 в файле** (отчёт обещал 29 и
   «шапку в соответствии»). Косметика комментария — фикстура зелёная, обе спорные ловушки на месте,
   — но обещание не держится, а объявленный охват в этой фабрике имеет вес числа. ВОПРОСЫ №7.

Он же назвал два расхождения, которые я числю СВОИМИ последствиями, а не находками: разъезд
«документ `ДВ<N>` ↔ инструмент `Д<N>`» (ВОПРОСЫ №1) и оборванный адрес в докстроке
`gejt_verstki.py:15` после моего сноса дубля (ВОПРОСЫ №8).

**(б) Ш3 по смыслу, а не грепом.** Читал окружающий абзац у каждого вхождения, сверялся с `DOLG.md`.
**Охват: проверено 19 вхождений `ДВ<N>` и 5 оставшихся `Д1`–`Д6`; ошибок переименования 0.** Ни одно
`ДВ<N>` не оказалось замаскированной ссылкой на долг; все пять оставшихся `Д<N>` — настоящие долги
(Д4 = веса слов `DOLG.md:67`, Д2 = гейт объёма `DOLG.md:65`, Д5 = `--scene-diff`, `DOLG.md:81`).
Он же нашёл ДВА места вне трёх домов, где доктрина осталась под старыми номерами и теперь читается
как ссылка на чужой долг: `gejt_verstki.py` (ВОПРОСЫ №1) и карточка в `KARTA.md:412` (ВОПРОСЫ №2).
Оба вне моей зоны для правки — не тронул, назвал.

### ЧТО НЕ ТРОГАЛ
`teorkat-vvedenie/**` — только чтение, ни одного байта записи (её правит владелец в другом чате);
`_generator/**` — законно приехало слиянием, руками не правил ни строки, включая `gejt_verstki.py`,
где правка «в одну строку» напрашивалась (ВОПРОСЫ №1); `_fond/**`, `obzory/**`,
`_generator/build_listok.py`, `ucheniki/**` — чужое незакоммиченное, мимо; `DOLG.md` — только читал;
`_studio/docs/` руками не правил (в `KARTA.md` уехали только строки, написанные `register_doc.py`
чужими заходами); ветки НЕ удалял ни одну; свою ветку НЕ вливал; `git checkout` в основной папке не
делал; новых `.md` не заводил (регистрировать нечего).

### НЕОБРАТИМОЕ — один пункт
- **Удалена папка `disciplina/` внутри репозитория `materials`** (2 файла-обрубка: `skills/slajdy/SKILL.md`
  129 строк и `skills/slajdy/references/kartochka-slajda.md` 62 строки) · рабочая папка
  `../materials-wt/svedenie-arki`, ветка `zahod/svedenie-arki` · **восстанавливается** из истории:
  `git show 77630e4:disciplina/skills/slajdy/SKILL.md` (и тот же путь для второго файла), либо
  целиком `git checkout ac81738 -- disciplina/` — родитель моего коммита `c73487a`.
  Содержание из обоих обрубков живёт в настоящем репозитории `disciplina` (проверено до удаления,
  см. Ш2). Предписано заданием, Ш2.
  ⚠ **Побочное следствие этого удаления, называю сам:** относительный адрес
  `disciplina/skills/slajdy/SKILL.md` в докстроке `_generator/tools/gejt_verstki.py:15` внутри
  `materials` больше не резолвится. Правка в одну строку, но файл вне моей зоны — ВОПРОСЫ №8.
Больше необратимого нет: `git reset`/`checkout` поверх несохранённого не делал, ничего не
перезаписывал, ветки не удалял, артефакт положен новым именем с датой.

### ПОВТОРЯЕМОСТЬ НАХОДОК
🔴 **Повторится — значит заход ДО следующего прогона, а не пункт очереди:**
- **Расхождение «документ ДВ<N> ↔ гейт Д<N>»** ударит при ПЕРВОМ же прогоне `gejt_verstki.py` на
  следующей лекции: человек читает в спеке `ДВ2`, а в терминале видит `Д2` и идёт искать долг про
  гейт объёма. Ловится глазами каждый раз, чинится один раз (ВОПРОСЫ №1).
- **Фикстура `fixtures/uroki/zakrytie/DOLG.md` пачкает дерево у КАЖДОГО, кто гоняет полный регресс
  в новый календарный день** — путь вне зоны, чистить его исполнителю запрещено (ВОПРОСЫ №3).
- **Сломанные команды §0.1 генератора заходов** — шестой отказ подряд первым ходом, блок вшивается
  в каждый заход с `--vlit`. Прошлый заход назвал это тем же словом; правка не доехала.
- **Критерий через `--no-merged <чужая ветка>`** — второй заход подряд сдаёт красный критерий,
  который не мог позеленеть (`## УРОКИ ФАБРИКЕ`).
- **Нарушенный контракт `gejt_verstki.py`** (ВОПРОСЫ №6) — не «когда-нибудь», а у СЛЕДУЮЩЕГО, кто
  тронет этот файл: `pre-commit` зовёт `check_tool_contract.py --staged` первым ходом. Класс шире
  одного файла: любой инструмент, чья ветка ушла на приёмку раньше, чем ветка со строгой проверкой,
  сдаст зелёный контракт и покраснеет после свода. Заходов с таким разъездом в этой арке было три.
**Не повторится, законно уходит пунктом очереди:** карточка `KARTA.md:412` (ВОПРОСЫ №2) —
разовая, конкретного файла; метка долга Д27 (ВОПРОСЫ №5) — разовая, чужого захода; конфликт
`KARTA.md` при влитии (ВОПРОСЫ №4) — свойство этой конкретной пары коммитов; шапка фикстуры
(ВОПРОСЫ №7) и адрес в докстроке (ВОПРОСЫ №8) — разовые, по одной строке каждая.

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ» — два пункта
1. **ВОПРОСЫ №6** — контракт `gejt_verstki.py`. Пока он красный, следующий коммит в `_generator/tools/`
   встанет на чужом долге, а исполнитель пойдёт обходить хук — тот самый класс, из-за которого
   `--no-verify` дал 28 обходов из 56 срывов коммита.
2. **ВОПРОСЫ №1** — разведение маркеров сделано в документах, но не в инструменте, который их
   печатает. До этого момента задача «развести маркеры» закрыта наполовину.
Оба лежат в `_generator/**` — не моя зона, поэтому не сделаны, а названы. Остальные шесть пунктов
самостоятельны и меня не ждут.

### ВЕРДИКТ ЗАХОДА — с охватом ВНУТРИ, а не рядом
**Влито 3 ветки из 3 названных (предками стали 8 из 8). Обещаний проверено 44 из 44: на месте 42,
сломано 2. Красных гейтов на живой лекции 3, регрессий среди них 0. Регрессия совместной работы —
одна, найдена вне списка команд критерия (ВОПРОСЫ №6). Потерь текста при слиянии — ноль.
Маркеры разведены в 3 домах из 3 реальных; пятый дом, вне моей зоны, остался со старой нумерацией.**
Это исход «слияние прошло, совместная проверка нашла красное» — тот, который заход назвал законным.

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** на канале `app` НЕПРИМЕНИМО — снимать неоткуда.
