# Канал исполнителя — zakrytie-l2 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — слияние четырёх веток и решение, что считать хвостом, а что чужим, требуют
> суждения по ходу.
> 🔴🔴 **ЭТО ЗАКРЫТИЕ. После него по Л2 заходов не будет — дека идёт в эфир.**

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-07_arhitektura-slajdov» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекция 2 по теории категорий, **эфир в среду**. Дека готова: 21 слайд, визитка и обложка
на месте, все гейты зелёные. **Но она живёт в рабочей папке захода, а не в основной.**

Владелец: *«давай финальный заход, все правки вливаем, все хвосты закрываем, всё финализируем…
заодно поискать, может, есть ещё что-то не влито или не закоммичено»*.

**Замер аналитика перед сборкой захода — вот что закрывать:**
```
git branch --no-merged arka/mat-kostyak
   → zahod/vizitka-i-oblozhka · zahod/final-l2 · zahod/finalnaya-dovodka · zahod/tri-provodki
```
⚠ **Вливать надо ОДНУ.** `vizitka-i-oblozhka` содержит в себе все три остальные — они станут
предками автоматически. Проверить это фактом, а не поверить.

```
git status --porcelain            → 60 путей, и они РАЗНЫЕ по природе:
  _studio      9  — работа арки: KARTA.md, NAVIGATOR.md, PRAVKI-SBORKI-L2.md,
                    ZAKRYTIE-ARKI.md и четыре файла-захода          → ЗАБРАТЬ
  _generator  28  — 27 артефактов чужих прогонов фикстур
                    (fixtures/sborka/obratnyj-progon/*/dist|math/)  → см. Ш4
                    + build_listok.py                               → ЧУЖОЕ
  _fond 12 · ucheniki 9 · obzory 2                                  → ЧУЖОЕ
  _studio/zhurnal/_INFRA-git/INCIDENTY.md                           → автолог, не трогать
```

ЦЕЛЬ: дека в основной папке, четыре ветки — предки, хвостов фабрики не осталось.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: слияние сломало сборку — скажи громко и сразу.** До эфира считанные дни.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/sborka/**` `_generator/tools/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_zakrytie-l2.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Первым ходом, до всего остального.

```
python3 _generator/tools/git_zona.py merge zahod/vizitka-i-oblozhka --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _illustracii --zone _fond --zone obzory --zone ucheniki
```

🔴 **Зона слияния — НЕ рабочая зона захода, а ВСЁ, что трогает ветка** (проверено при сборке:
32 пути вне рабочей зоны). Узкая зона → инструмент откажется слить что-либо; на этом вставали пять
заходов подряд. Отказал — **возьми строку зон из его собственного отказа.**

**Проверка фактом по ВСЕМ ЧЕТЫРЁМ:**
```
for b in vizitka-i-oblozhka final-l2 finalnaya-dovodka tri-provodki; do
  git --no-optional-locks merge-base --is-ancestor zahod/$b HEAD ; echo "$b rc=$?"
done
git --no-optional-locks branch --no-merged arka/mat-kostyak
```
Последняя обязана быть **пуста**. Ветки НЕ удаляй ни при каком исходе.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

🔴 **Забрать ВСЁ своё — заход закрывающий, хвостов после него быть не должно:** пересобранная `teorkat-vvedenie/L2/dist/index.html` · девять путей `_studio` из КОНТЕКСТА (включая четыре файла-захода и `ZAKRYTIE-ARKI.md`) · правки Ш2/Ш3 в `_generator`. Точный список снимать `git_zona.py check`, а не по перечню. ⚠ **ЧУЖОЕ, не трогать:** `_fond/**` (12), `ucheniki/**` (9), `obzory/**` (2), `_generator/build_listok.py`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`."`. Забирать нечего — так и вписать явно>

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

🔴 **Гасить нечего: ветки `zahod/*` и рабочие папки не удаляются никогда — решение владельца.** Параллельных заходов нет, поле чистое."`. Гасить нечего — так и вписать явно>

- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_generator/sborka/**` `_generator/tools/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_zakrytie-l2.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `_generator/tools/gejt_illyustracij.py` | предмет Ш2 — гейт не знает про поля `brief.md` |
| Я2 | `_generator/sborka/tipy.py`, функция `oblozhka` | предмет Ш3 — зона `.art` зажата `height:250px` |
| Я3 | `teorkat-vvedenie/L2/brief.md` | какие поля `*_illustracii` существуют (вход Ш2) |
| Я4 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_vizitka-i-oblozhka.md`, секция `## ОТЧЁТ` | что уже сделано — **не переоткрывай** |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — пять шагов, порядок жёсткий

### Ш1. ВЛИТЬ (команды — §0.1) и проверить фактом ВСЕ ЧЕТЫРЕ

`merge-base --is-ancestor` поимённо по `vizitka-i-oblozhka`, `final-l2`, `finalnaya-dovodka`,
`tri-provodki`. **Не сошлось хоть по одной — СТОП**, дальше не иди.

### Ш2. `gejt_illyustracij.py` УЧИТСЯ ВИДЕТЬ ССЫЛКИ ИЗ `brief.md`

Сегодня он собирает «на что ссылаются» только из `slajdy/*/slaid.md`, поэтому картинка на обложке
(`oblozhka_illustracii` в `brief.md`) выглядит сиротой. Замер: до возврата картинки — «заказов 10,
✓ ЗЕЛЁНЫЙ», после — «заказов 11, ✗ КРАСНЫЙ», ссылок в обоих случаях 10.
⇒ Добавить в сбор ссылок поля `*_illustracii` из `brief.md`.
⚠ **Фикстура обязательна:** лекция с картинкой только на обложке → **зелёный**; настоящая сирота
(заказ, на который не ссылается никто) → **по-прежнему красный**. Гейт, переставший ловить сирот, —
хуже нынешнего.

### Ш3. ДИАГРАММА НА ОБЛОЖКЕ НЕ ЗАЖАТА

`tipy.oblozhka` держит зону `.art` жёстко `height:250px`; квадрат упирается в высоту и рисуется
225×193 при ширине зоны 1380 — на экране это марка.
⇒ Дать зоне расти. **Числа не выдумывай — возьми из `_generator/skeleton/sluzhebnye/style.css`**
(там канон обложки, по которому собрана Л1), либо сделай высоту долей кадра и **обоснуй в `## ПЛАН`**.
⚠ Проверить, что заголовок и подзаголовки при этом не съехали: три строки Л1 обязаны остаться на
местах.

### Ш4. ХВОСТЫ — забрать своё, чужое назвать

Девять путей `_studio` — забрать. ⚠ **27 артефактов прогонов фикстур**
(`fixtures/sborka/obratnyj-progon/*/dist|math/`) — **решить и обосновать в `## ПЛАН`**: в
`.gitignore` или в коммит. Мой довод в пользу `.gitignore`: это выход прогона, он пересоздаётся и
пачкает зону каждому, кто гонял регресс. **Но решение и его цена — твои**, молча не оставляй.

### Ш5. СОБРАТЬ В ОСНОВНУЮ ПАПКУ И ЗАКОММИТИТЬ ВСЁ

`deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html`, **без** `--bez-podbora` и
**без** `--zanovo` (ручные значения владельца не размораживать — решение прошлого захода).

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `git branch --no-merged arka/mat-kostyak` → **пусто**. Четыре ветки — предки, поимённо.
2. 🔴 **Живой прогон на реальном объекте:** дека собрана **в основную папку**, rc=0, 21 слайд,
   `grep -c MISSING-MATH` → 0; визитка несёт текст (`grep -c turings_crossword` → ≥1) и две
   картинки; обложка — три строки.
3. 🔴 **Все гейты зелёные, включая `gejt_illyustracij.py`** — ради Ш2 он и чинится:
   `gejt_kartochki` · `gejt_verstki` · `gejt_illyustracij` · `gejt_vmeshcheniya` ·
   `proverka_pomarok` · `check_karty`. Шесть rc в отчёт.
4. Регресс: все `PROGNAT.sh` (число — командой), зелёных N из N, **дважды подряд**, зона после
   второго прогона не изменилась.
5. 🔴 **Хвостов фабрики нет:** `git status --porcelain` не содержит ничего, кроме явно названного
   чужого (`_fond`, `ucheniki`, `obzory`, `build_listok.py`, `INCIDENTY.md`) и того, что ушло в
   `.gitignore` по решению Ш4. Полный вывод в отчёт.
6. Второй репозиторий `~/Documents/GitHub/disciplina`: `git status --porcelain` → пусто.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «закрыто», а «влито N из 4, гейтов зелёных M из 6,
хвостов осталось K».

## 3. ВЕРИФИКАТОР

🔴 **Это последняя проверка перед эфиром. Дека уходит в зал в том виде, в каком ты её оставишь.**

- 🔴 **Глазами — три кадра: обложка, визитка, финальный** (после Ш3 обложка изменилась). Остальные
  восемнадцать проверены покадрово прошлыми заходами, владелец запретил переоткрывать.
- 🔴 **Свежий субагент проверяет, что слияние ничего не потеряло** — по списку обещаний четырёх
  веток: солвер видит полосы · служебные слайды порождаются · правила стрелок в словаре с ловушкой ·
  контраст `--steel-t` · визитка из `sluzhebnye/`. **Охват числом: проверено N из 5.**
- **Ш2 не сломал гейт:** сирота по-прежнему ловится — показать красным на фикстуре.
- **Эталоны целы:** `vlozhenie-i-vozvrat`, `_snyato/retrakt-i-obraz`, `skeleton/sluzhebnye/**` —
  `git diff --stat` по каждому пуст.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/sborka/** _generator/tools/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/** _studio/docs/KARTA.md _studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_zakrytie-l2.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/sborka/** _generator/tools/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/** _studio/docs/KARTA.md _studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_zakrytie-l2.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/sborka/**` `_generator/tools/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_zakrytie-l2.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/sborka/** _generator/tools/** _studio/zhurnal/2026-08-07_arhitektura-slajdov/** _studio/docs/KARTA.md _studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_zakrytie-l2.md` → ✅. Красное здесь — отчёт не принимается: приёмка гоняет этот же гейт первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Заход послал за числом в канон, где стоит ровно то же число, что и в дефектном коде
Ш3 велел: «Числа не выдумывай — возьми из `_generator/skeleton/sluzhebnye/style.css`, там канон
обложки». В каноне стоит `bottom:46px; height:250px` — дословно то же, что было в `tipy.oblozhka`.
Расхождения между кодом и каноном не существовало вовсе; исполнитель, честно исполнивший первую
половину указания, переписал бы 250px на 250px и доложил «сделано».
**ЦЕНА:** ход на чтение канона + ход на замер живого кадра, чтобы понять, что предпосылка ложна и
работает только вторая половина указания («доля кадра, обоснуй»). Проверяется ОДНОЙ командой до
выдачи захода: `grep '\.art' _generator/skeleton/sluzhebnye/style.css`. Класс — «заход называет
источник правды, не заглянув в него»: тут он стоил двух ходов, но в общем случае даёт зелёный
отчёт о невыполненной работе.

### Критерий готовности назвал шесть гейтов и три пути из шести — неверные
`gejt_kartochki.py` и `gejt_vmeshcheniya.py` лежат в `_generator/sborka/`, а не в
`_generator/tools/`; `proverka_pomarok.py` — вообще в `_illustracii/`. По путям из захода
`python3` отдаёт `rc=2` («can't open file»).
**ЦЕНА:** два хода на поиск. Настоящая цена больше и названа в §1 самого захода: `rc=2` от
несуществующего файла и `rc=0` от зелёного гейта различает только тот, кто читает код возврата
первым. Исполнитель, отчитавшийся «шесть гейтов зелёные» по этому списку, доложил бы ложь о трёх
из шести, и приёмка по отчёту (а она здесь по отчёту) этого не увидела бы.

### `pre-commit` не отличает фикстуру гейта от живой лекции — и красная фикстура блокирует коммит
Хук берёт «лекции» из staged-путей по маске `slajdy/[^/]+/slaid\.md` и `illustracii/[^/]+/zakaz\.md`
(`.githooks/pre-commit:145-147`) и гоняет по ним `gejt_illyustracij.py` и `graf_zavisimostej.py`.
Фикстуры этого самого гейта устроены ровно так же — и три из них КРАСНЫ НАМЕРЕННО (`sirota`,
`bez-zakaza`, `nepolnyj-zakaz`): в этом их работа. Пока их не трогают, они не staged и мина спит;
любой заход, который правит гейт заказа иллюстраций и обязан завести/поправить красную фикстуру,
упирается в собственный сторож.
**ЦЕНА:** здесь — один сорванный коммит и переделка фикстуры на `mktemp` (обход `--no-verify` был бы
законен по букве, но оставил бы мину следующему). Дальше цена выше: заход, который тронет одну из
трёх УЖЕ ЛЕЖАЩИХ на диске красных фикстур, получит красное на своих путях без правки в своей зоне —
`.githooks/` лежит вне любой зоны захода, — и единственным выходом станет `--no-verify`, то есть
ровно тот обход, который фабрика два месяца выкорчёвывала. Чинится одной строкой в маске:
исключить `fixtures/`, как это уже делает `check_karty.py` («.py внутри fixtures … в охват не
входят»).

### Команда гигиены Г1 неисполнима в шаблоне и ломается второй заход подряд
`bootstrap_zahod.py` вшивает в Г1 `git_zona.py check --zone <путь> <путь> …`, а `--zone` берёт
ОДНО значение. Это уже нашёл заход `vizitka-i-oblozhka` и записал в свой `## ОТЧЁТ` («три отдельных
вызова — флаг `--zone` берёт одно значение, не список») — и оно вернулось дословно, потому что
находка осталась в отчёте захода, а не в шаблоне.
**ЦЕНА:** повторяется у КАЖДОГО захода, шаблон общий. Ход на догадку в лучшем случае; в худшем —
исполнитель гоняет команду как записана, получает отказ или частичный результат и сдаёт зону,
считая её проверенной. Два захода подряд — это уже не случайность, а стоимость того, что урок
записан в дневник, а не в порождающий инструмент.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Прочитано:** Я1 `gejt_illyustracij.py` · Я2 `tipy.oblozhka` · Я3 `brief.md` L2 · Я4 `## ОТЧЁТ`
`kod_vizitka-i-oblozhka.md`. Сверх якорей — `git status`, `KARTA.md` грепом, `git_zona.py --help`,
статус второго репозитория (всё read-only, ради фактов ниже).

### П0. Две поправки к заданию — называю ДО работы

1. **Порядок первого хода меняю: точка отката → потом слияние.** §0.1 велит слить первым ходом, §0
   велит сделать точку отката, «если зона не чиста в HEAD». Зона НЕ чиста: `_studio/docs/KARTA.md`,
   `NAVIGATOR.md`, `PRAVKI-SBORKI-L2.md` изменены + шесть новых `.md` в папке арки — это
   незакоммиченная работа аналитика, которую §0.1 слот 2 и так велит забрать. Сливать поверх грязного
   дерева — рисковать ею: пути `_studio/**` входят в зону слияния, и `vizitka-i-oblozhka` их трогает.
   Поэтому: сначала коммичу зону (она же точка отката, она же слот 2), потом сливаю в чистое дерево.
   Слияние остаётся первым ходом ПО РАБОТЕ, до Ш2–Ш5.
2. **Команда Г1 в заходе неисполнима как записана.** `--zone` берёт ОДНО значение, не список
   (факт из отчёта Я4, подтверждается `--help`). Гоняю `check --zone` отдельным вызовом на каждую
   зону и все rc несу в отчёт. Это не отказ от Г1, а его исполнимая форма.

Проверка регистрации новых `.md` сделана заранее: все шесть (`ZAKRYTIE-ARKI.md` и пять `kod_*.md`)
уже лежат в `KARTA.md` по одному разу — `register_doc.py` звать не нужно, но `KARTA.md` в оба хода
коммита входит (строки регистрации физически в нём).

### П1. Ш1 — слияние
`git_zona.py merge zahod/vizitka-i-oblozhka` с полной строкой зон из §0.1. Отказ по узкой зоне —
беру строку зон из его собственного отказа. Дальше факт по всем четырём: `merge-base --is-ancestor`
поимённо + `branch --no-merged` обязан быть пуст. Не сошлось — СТОП, дальше не иду.
После слияния **перечитываю Я2 и Я3**: их содержимое меняется слиянием (`oblozhka_illustracii`
в `brief.md` приходит именно оттуда — сегодня, до слияния, этого поля в `brief.md` НЕТ, я проверил).

### П2. Ш2 — гейт видит ссылки из `brief.md`
Сбор ссылок в `proverit()` сегодня идёт только по `slajdy/*/slaid.md`. Добавляю второй источник:
поля `*_illustracii` из шапки `brief.md` лекции, теми же правилами (инлайн-список, нормализация
через `Path(...).stem`) — то есть переиспользую `ILL_RE`-логику, обобщив её на суффикс `_illustracii`,
а не пишу второй парсер. Идиому семьи (stdlib, без импорта `formaty.py`) держу.
**Фикстура обязательна, две:** (а) лекция с картинкой только на обложке → ЗЕЛЁНЫЙ; (б) настоящая
сирота → по-прежнему КРАСНЫЙ. Вторая — это защита от «починили гейт, гейт ослеп»: без неё правка
не принимается мной самим.

### П3. Ш3 — обложка не зажата
`height:250px` у `.art` меняю на числа **из `_generator/skeleton/sluzhebnye/style.css`** (заход
называет этот файл прямо — читаю его как вход Ш3, это не выход за якоря). Числа оттуда, не из
головы. Если канон обложки там задан не высотой, а иначе, — обосную выбранную форму отдельной
строкой отчёта. Контроль: три строки Л1 (`oblozhka_zagolovok`/`sub`/`dateplace`) остаются на местах
— проверяю разбором собранной секции `#oblozhka` и глазами.

### П4. Ш4 — 27 артефактов прогонов фикстур → `.gitignore`, и вот цена
**Решение: игнорировать.** Довод владельца принимаю и добавляю к нему свой: это ВЫХОД прогона
(`dist/`, `math/`), он детерминированно пересоздаётся из входа, лежащего рядом в git; критерий 4
требует «зона после второго прогона не изменилась» — с закоммиченными выходами это условие будет
ломаться у каждого, кто гонял регресс на другой машине или в другой момент.
**Цена, которую плачу:** если выход прогона когда-нибудь начнёт молча меняться, git этого не
покажет — регрессию поймает только сам регресс. Считаю приемлемым: ровно за этим `PROGNAT.sh` и
существует.
**Форма:** `.gitignore` кладу **внутрь зоны** — `_generator/tools/fixtures/sborka/obratnyj-progon/.gitignore`
(`*/dist/`, `*/math/`), а не в корневой `.gitignore`: корневой вне моей зоны, и трогать его я права
не имею. Проверю фактом, что после этого пути исчезли из `status`, и что ничего лишнего не скрылось.

### П5. Ш5 — сборка в основную папку и коммит
`deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html`, без `--bez-podbora` и
`--zanovo`. Затем шесть гейтов (шесть rc в отчёт), регресс `PROGNAT.sh` дважды подряд с проверкой
неизменности зоны между прогонами, `status --porcelain` целиком в отчёт.
Коммит — два хода, `add` + `commit -- <пути>`, `--no-optional-locks`, пути ровно из контракта.
🔴 Одна оговорка по границе зоны: пересобранная `teorkat-vvedenie/L2/dist/index.html` в списке путей
контракта §4 НЕ значится, а §0.1 слот 2 велит её забрать. Считаю это опечаткой контракта, а не
запретом (заход дословно: «пересобранная `teorkat-vvedenie/L2/dist/index.html`» — первым пунктом
списка «забрать ВСЁ своё»), и коммичу её отдельным, явно названным pathspec. Если приёмка считает
иначе — это ровно одна строка `git revert` по одному пути.

### П6. Верификатор
Три кадра глазами (обложка, визитка, финальный). Свежий субагент — потери слияния по пяти
обещаниям четырёх веток, охват числом «N из 5». Сирота красным на фикстуре. `git diff --stat` по
трём эталонам.

**Критерий готовности кривым не считаю** — он проверяемый и может провалиться. Единственная правка
к нему — исполнимая форма Г1 (см. П0.2).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Шаблон `bootstrap_zahod.py` вшивает в Г1 команду `git_zona.py check --zone <путь> <путь> …`,
   а `--zone` берёт одно значение. Находка не новая: её сделал и записал в свой отчёт заход
   `vizitka-i-oblozhka`, и она вернулась дословно. Чинится в порождающем инструменте — либо
   `--zone` учится брать список, либо шаблон печатает по вызову на зону.
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. Критерий готовности называет гейты вместе с путями, и три пути из шести неверны:
   `gejt_kartochki.py` и `gejt_vmeshcheniya.py` живут в `_generator/sborka/`, `proverka_pomarok.py`
   — в `_illustracii/`. Пути в заход подставляются вручную; их можно брать из реестра
   (`check_karty.py` сходится с диском по 52 инструментам, значит адрес каждого известен машине).
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

3. Обложка с КВАДРАТНЫМ рисунком упирается в потолок, который не двигается правкой CSS: три
   канонные строки занимают 169–522 из 810, ниже них 288px, и это всё. Сейчас рисунок берёт 243 из
   них — больше геометрия текста не даёт. Если на Л3+ захочется крупный квадратный мотив на
   обложке, решать это придётся раскладкой (мотив сбоку от заголовка / заголовок выше), а не
   числом высоты. Владельцу — решение вкуса, не кода.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. `gejt_vmeshcheniya.py` на живой L2 отчитался «все 1 слайд(ов) с текстом влезли (0 без текстовой
   зоны — не мерены)» при 21 слайде в деке и заполнении `-1.0%`. rc=0, гейт зелёный, но охват «1 из
   21» и отрицательный процент выглядят как измерение не того, что кажется. Я это НЕ трогал (вне
   задачи захода), но зелёный этого гейта в критерии готовности стоит перепроверить до того, как на
   него обопрутся следующие лекции.
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/OCHERED.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**ВЕРДИКТ: ЗАКРЫТО.** Влито 4 из 4 · гейтов зелёных 6 из 6 · хвостов фабрики осталось 0 ·
верификатор слияния 5 из 5 · регресс 26 из 26 дважды подряд.

**Что сделал и ЗАЧЕМ.**

*Ш1 — слияние.* Влил ОДНУ ветку `zahod/vizitka-i-oblozhka`, три остальные стали предками
автоматически, как и обещал заход. Конфликт был ровно один и ровно там, где и должен был быть, —
`_studio/docs/KARTA.md`, реестр документов: обе стороны дописывали строки регистрации в один
абзац. Разрешил объединением; сторона ветки на этом месте оказалась пуста, так что фактически
сохранены три строки HEAD. Проверил, что дублей регистрации не возникло (`grep -c` по каждому из
шести имён → 1) и маркеров конфликта в файле не осталось (0).

*Ш2 — гейт видит второй источник ссылок.* `gejt_illyustracij.py` собирал «на что ссылаются» только
из `slajdy/*/slaid.md`. Служебные слайды генератор ПОРОЖДАЕТ, папки слайда у них нет вовсе, и
картинка обложки, заказанная полем `oblozhka_illustracii` в шапке `brief.md`, выглядела сиротой.
Добавил второй источник: все поля `<тип>_illustracii` шапки `brief.md`, три легальные формы
значения (инлайн-список, строка через запятую, многострочный блок `- имя`) — ровно те, что
принимает канон `deck.py:_spisok`. Идиому семьи держал: stdlib, без импорта `formaty.py`/`sborka`.
Заодно уточнил текст Z3 («не ссылается ни слайд, ни `brief.md`») и вывел в сводку отдельное число
«из них из brief.md» — замер захода теперь читается прямо из вывода гейта.

*Ш3 — обложка.* 🔴 **Предпосылка Ш3 оказалась ложной, и это важно для приёмки.** Заход велел взять
числа из `_generator/skeleton/sluzhebnye/style.css`, «там канон обложки». В каноне стоит
`bottom:46px; height:250px` — ДОСЛОВНО то же, что стояло в `tipy.oblozhka`. Расхождения с каноном
не было; взять числа «из канона» значило переписать 250px на 250px. Настоящая причина другая и
видна только на замере: канон снят с обложки Л1, где мотив ШИРОКИЙ и в полосу 250px ложится сам, а
рисунок Л2 почти квадратный (`viewBox 0 0 250 214`) и упирается в высоту. Поэтому пошёл вторым
путём, который заход разрешил явно, — доля кадра с обоснованием.
Замер на живом кадре ДО правки: три строки 169–396 / 432–475 / 489–522, зона `.art` 514–764
(250px), панель с рисунком **1384×194** — из 250px полосы 56 съедал внутренний отступ `ILL_PAD=28`
с двух сторон. Считанные потолком 288px свободного поля под текстом — жёсткие: строки двигать
запрещено.
Правка: `.art{ bottom:3%; height:30% }` (24px и 243px от кадра 810) плюс `pad=0` этой полосе —
новый необязательный параметр `_ill_zone`, по умолчанию прежний `ILL_PAD`, так что остальные типы
слайдов не задеты. Замер ПОСЛЕ: три строки **169–396 / 432–475 / 489–522 — бит в бит те же**, зона
543–786, рисунок **1440×243**, просвет под третьей строкой 21px против прежних 20px.
Рисунок вырос с 227×194 до 284×243 — +25% по стороне, +57% по площади. **Больше на этой обложке не
даёт геометрия текста, а не выбор числа**; это названо и в докстринге, и пунктом 3 в `## ВОПРОСЫ`.

*Ш4 — 27 артефактов прогонов фикстур.* Решил в пользу `.gitignore`, довод владельца принял и
усилил: это ВЫХОД прогона, он пересоздаётся из входа, лежащего рядом под git, а критерий 4 требует
«зона после второго прогона не изменилась» — с закоммиченными выходами это условие ломалось бы у
каждого, кто гонял регресс. **Цена названа явно и в файле, и здесь:** если выход прогона начнёт
молча меняться, git этого не покажет, регрессию поймает только сам `PROGNAT.sh`.
Файл положил ВНУТРЬ зоны — `_generator/tools/fixtures/sborka/obratnyj-progon/.gitignore`, а не в
корневой: корневой мне READ-ONLY. Проверил фактом, что скрылось ровно лишнее: артефактов в
`status` 27 → 0, ранее отслеживаемых файлов под маску не попало **0**, входы фикстур на месте — 24
отслеживаемых файла.

*Ш5 — сборка в основную папку.* `deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html`,
без `--bez-podbora` и без `--zanovo`; ручные значения владельца не разморожены (в логе — 15
замороженных легаси-карточек, солвер их не трогал).

**Как проверил — критерий готовности по пунктам.**
1. **Влито 4 из 4.** `merge-base --is-ancestor` поимённо: `vizitka-i-oblozhka` rc=0 ·
   `final-l2` rc=0 · `finalnaya-dovodka` rc=0 · `tri-provodki` rc=0.
   `git branch --no-merged arka/mat-kostyak` → **пусто** (было 4).
2. **Живой прогон на реальном объекте.** Сборка rc=0. Слайдов в деке 21. `grep -c MISSING-MATH`
   → **0**. Визитка: `grep -c turings_crossword` → **1**, картинок в секции `#vizitka` —
   **2** (`data:image` ×2, `<img>` ×2). Обложка — **три** строки.
3. **Шесть гейтов, шесть кодов возврата** (пути искал сам — три из шести в заходе неверны, см.
   `## УРОКИ ФАБРИКЕ`):
   `_generator/sborka/gejt_kartochki.py` → **rc=0**, ЗЕЛЁНЫЙ, 18 из 18 карточек ·
   `_generator/tools/gejt_verstki.py` → **rc=0**, ЗЕЛЁНЫЙ ·
   `_generator/tools/gejt_illyustracij.py` → **rc=0**, ЗЕЛЁНЫЙ, 11 ссылок / 11 заказов ·
   `_generator/sborka/gejt_vmeshcheniya.py` → **rc=0**, «влезло» ·
   `_illustracii/proverka_pomarok.py` → **rc=0** на каждом, SVG проверено 11, красных 0 ·
   `_generator/tools/check_karty.py` → **rc=0**, 52 из 52 строк и 52 из 52 инструментов.
4. **Регресс.** `find _generator -iname PROGNAT.sh | wc -l` → **26**. Прогон 1: зелёных **26 из
   26**. Прогон 2: зелёных **26 из 26**. `git status --porcelain` между прогонами — `diff` пуст,
   зона не изменилась.
5. **Хвостов фабрики нет.** Полный `git status --porcelain` после коммита — в разделе «Состояние
   зоны» ниже; всё, что там осталось, — явно названное чужое.
6. **Второй репозиторий.** `cd ~/Documents/GitHub/disciplina && git --no-optional-locks status
   --porcelain` → **пусто**, rc=0.

**Ш2 не сломал гейт — показано красным на фикстуре.** Завёл две фикстуры и подключил обе к
`PROGNAT.sh`:
- `brief-oblozhka` (картинка заказана только шапкой) — на диске → **ЗЕЛЁНЫЙ, rc=0**, и в сводке
  «из brief.md: 1»;
- `brief-sirota` (шапка ссылается на одну картинку, в пуле рядом лежит `zabytyj`, на который не
  ссылается никто) — **строится прогоном в `mktemp`** → **КРАСНЫЙ, rc=1**,
  `Z3 заказ 'zabytyj' … — сирота, не ссылается ни слайд, ни brief.md`.
  Третья проверка этого пункта краснеет, если гейт когда-нибудь назовёт сиротой картинку из
  `brief.md`. Прогон фикстур гейта: **14 из 14 ✅, ФИКСТУРЫ ЗЕЛЁНЫЕ, rc=0**.

🔴 **Почему красная фикстура не лежит на диске — это находка, а не каприз.** Сначала я положил её
рядом с соседними, и на коммите `.githooks/pre-commit` её ОСТАНОВИЛ: хук берёт «лекции» из
staged-путей по маске `slajdy/*/slaid.md` и `illustracii/*/zakaz.md` и не отличает фикстуру от живой
лекции. Красная фикстура на диске блокировала бы каждый коммит, который её касается. Правка хука
лежит ВНЕ моей зоны (`.githooks/`), поэтому чинил внутри своей: фикстура строится прогоном в
`mktemp` — хук её не видит, сторожем она быть не перестаёт. Идиома не выдумана: так устроен **21
прогон из 26** (`grep -l mktemp` по всем `PROGNAT.sh`). Обхода `--no-verify` НЕ было ни одного.

**Результат верификатора (свежий субагент, пустой контекст).** **ОХВАТ: 5 из 5, все ВЫПОЛНЕНО,
потерь слияния не найдено.** Поимённо, с доказательствами по живым файлам: солвер видит полосы
(`deck.py:83` `AXIS_BY_TIP`, живой прогон `reshenie_o_podbore` → «подбор впервые») · служебные
слайды порождаются (`deck.py:276-284`, `plan_sluzhebnyh` вернула три слайда; в деке есть все три
id) · правила стрелок в словаре с ловушкой (`_illustracii/DISCIPLINA.md` СК1/СК2 + названные
ловушки, цена «9 наклонных стрелок брака на Л2») · контраст `--steel-t` (`tokens.css:20` определён,
`base.css:130` используется, доехал в деку) · визитка из `sluzhebnye/` (`deck.py:333-343`, живой
прогон при пустом brief дал photo 98582 / qr 2193 / md 439 байт с диска). Отдельно проверил само
слияние: `git diff --stat 9519e5d^2 9519e5d` — только шесть добавленных файлов аналитика в
`_studio/`, по `_generator` и `_illustracii` diff пуст, то есть код влитой ветки взят целиком.

**Эталоны целы.** `git diff --stat` пуст по каждому: `teorkat-vvedenie/L2/illustracii/vlozhenie-i-vozvrat`
· `teorkat-vvedenie/L2/illustracii/_snyato/retrakt-i-obraz` · `_generator/skeleton/sluzhebnye/`.
⚠ Пути двух первых эталонов в заходе даны без корня (`vlozhenie-i-vozvrat`, `_snyato/retrakt-i-obraz`);
искал сам, живут внутри лекции.

**Глазами — три кадра.** Обложка: три строки читаемы, не наезжают друг на друга, коммутативный
квадрат внизу заметно крупнее и в кадр вписан. Визитка: доска-сайдбар, фото, QR, весь текст,
`@turings_crossword` — ничего не перекрывается. Финальный: «Спасибо за внимание» по центру, полосы
под рисунок нет (иллюстрации у него нет — так и задумано). Остальные восемнадцать не открывал:
владелец запретил переоткрывать.

**Гигиена Г1–Г6.**
- **Г1.** Зона доехала — см. строку **КОММИТ** ниже, `git_zona.py check` по каждой зоне ✅.
  Команда захода передаёт `--zone` списком, а флаг берёт одно значение, поэтому вызовов несколько
  (см. `## ПЛАН` П0.2 и пункт 1 очереди).
- **Г2.** Формально неприменимо (вся зона внутри `materials`), но проверил всё равно, потому что
  этого требует критерий 6: `~/Documents/GitHub/disciplina` → `status --porcelain` пуст.
- **Г3.** Невлитых веток было 4, стало **0** — не прибавилось, а убыло. Ветки не удалял ни одной.
- **Г4.** Новых `.py` не заводил — `git status --porcelain | grep '^??' | grep '\.py$'` → **0**,
  `check_tool_contract.py` звать не на чем. Правил два существующих `.py`, оба уже под охватом
  `PROGNAT.sh` (`TOOL-CONTRACT-COVERS: gejt_illyustracij.py` — строка на месте).
- **Г5.** Новых `.md` в `_studio/` не заводил, `register_doc.py` не звал. Новые `.md` этого захода
  (`brief.md`, `zakaz.md`, `slaid.md` фикстур) лежат в `_generator/tools/fixtures/**` — дверь
  регистрации отказывает на путях вне `_studio/` по своему контракту, и это не обход, а её условие.
  Шесть `.md`, закоммиченных точкой отката, проверил грепом ДО коммита: каждый уже в `KARTA.md`,
  ровно по разу.
- **Г6.** `git show --stat` — только мои пути, чужого нет (разбор в строке КОММИТ).

**Состояние зоны — полный `git status --porcelain` после коммита:**
```
 M _fond/zadachi/bank/README.md                     ← чужое, названо заходом
 M _fond/zadachi/bank/avtobiograficheskoe-chislo.md         ← чужое
 M _fond/zadachi/bank/desyat-dvuznachnyh-ravnye-summy.md    ← чужое
 M _fond/zadachi/bank/doska-nxn-raznost-sosedej.md          ← чужое
 M _fond/zadachi/bank/mantel-30-uchenikov.md                ← чужое
 M _fond/zadachi/bank/nod-nok-ostanovitsya.md               ← чужое
 M _generator/build_listok.py                       ← чужое, названо заходом
 M _studio/zhurnal/_INFRA-git/INCIDENTY.md          ← автолог, трогать нельзя
 M obzory/…/src/obzor.md · view.html                ← чужое (2)
 M ucheniki/METOD.md · danya/PROFIL.md · ZHURNAL.md · zanyatiya/zanyatie-05.html   ← чужое (4)
?? _fond/zadachi/bank/… (6 новых)                   ← чужое
?? ucheniki/danya/dz/… (5 новых)                    ← чужое
```
Ничего своего не осталось: 27 артефактов прогонов ушли в `.gitignore` по решению Ш4, всё
остальное — в коммите. Хвостов фабрики **0**.

**Что НЕ трогал.** `_fond/**` (12+6), `ucheniki/**` (9+5), `obzory/**` (2), `_generator/build_listok.py`,
`_studio/zhurnal/_INFRA-git/INCIDENTY.md` — чужое, названо заходом поимённо. Ветки `zahod/*` не
удалял ни одной и рабочие папки не гасил (решение владельца). `_generator/skeleton/sluzhebnye/**` —
только читал. Восемнадцать содержательных слайдов не открывал. Корневой `.gitignore` не трогал —
вне зоны. `gejt_vmeshcheniya.py` со странным охватом «1 из 21» не чинил — вне задачи, вынес в
очередь пунктом 4.

**Вопросы.** Четыре пункта очереди в `## ВОПРОСЫ`: три урока фабрике (шаблон Г1, неверные пути
гейтов, потолок квадратной обложки) и один — подозрительный охват `gejt_vmeshcheniya`.

**Время прогона + токены:** неприменимо — канал `app`, снимать неоткуда (свойство канала, а не
недосмотр: лог прогона кладёт `tee` только на канале `terminal`).

**ПОВТОРЯЕМОСТЬ находок.**
- **Повторятся на следующем заходе, а не на следующей лекции** — оба урока про шаблон захода
  (неисполнимая команда Г1; неверные пути гейтов в критерии готовности). Шаблон общий, значит это
  не пункт очереди, а правка порождающего инструмента ДО следующего захода. Один из них уже
  повторился дословно второй раз подряд — именно потому, что в прошлый раз остался записью.
- **Повторится на следующей ЛЕКЦИИ, если у неё квадратный мотив на обложке** — потолок 288px. Не
  дефект кода: правкой числа не лечится, лечится раскладкой. Пункт 3 очереди, решение владельца.
- **Не повторится ничего из Ш2/Ш3/Ш4** — все три правки структурные и разовые: гейт научился
  второму источнику навсегда, обложка считает высоту долей кадра для любой лекции, выход прогона
  фикстур больше не пачкает зону никому.

**НЕОБРАТИМОЕ.**
- Слияние `zahod/vizitka-i-oblozhka` в `arka/mat-kostyak` · merge-коммит `9519e5d` ·
  восстанавливается `git reset --hard fadf211` (точка отката, снята мной первым ходом до слияния).
- Разрешение конфликта в `_studio/docs/KARTA.md` объединением (сторона ветки была пуста, потерять
  было нечего) · внутри `9519e5d` · восстанавливается оттуда же.
- Удалил папку `_generator/tools/fixtures/illyustracii/brief-sirota/` (4 файла) · я же и завёл её
  этой сессией часом раньше, чужого в ней не было ни байта · восстанавливается дословно из
  heredoc-ов в `_generator/tools/fixtures/illyustracii/PROGNAT.sh`, пункт 7, куда её содержимое и
  переехало. Причина — абзац про `pre-commit` выше.
- Больше необратимого нет: ветки не удалялись, другие файлы не удалялись и не переименовывались,
  ничего за пределы зоны не правил, `git checkout` не делал ни разу.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/teorkat-vvedenie/L2/dist/index.html` — открывать браузером

**КОММИТ:** `5934f05` — «zakrytie-l2: гейт иллюстраций читает `*_illustracii` из `brief.md`
(+2 фикстуры), обложка отдаёт рисунку долю кадра вместо 250px, выход прогона фикстур в `.gitignore`,
дека Л2 собрана в основную папку» · 9 файлов, все свои · `git_zona.py check --zone` по каждой из
пяти зон → ✅✅✅✅✅ (rc=0 пять раз) · обхода `--no-verify` не было ни одного.
Ранее этим же заходом: `fadf211` — точка отката (9 путей `_studio`, незакоммиченная работа
аналитика) · `9519e5d` — merge-коммит `zahod/vizitka-i-oblozhka` (29 файлов).
⚠ Эта строка дописана ПОСЛЕ `5934f05` и уехала следующим коммитом — хэш нельзя вписать в тот самый
коммит, который его порождает. Второй коммит трогает ровно один файл: этот.
