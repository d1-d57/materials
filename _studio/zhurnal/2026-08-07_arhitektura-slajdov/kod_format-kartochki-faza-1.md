# Канал исполнителя — format-kartochki-faza-1 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — решение о том, ЧЕМ помечать центральный блок и как разрезать гейт по фазам, рождается по ходу: сойтись обязаны три места (спека, бутстрап, гейт), и носитель пометки заходом не назначен, а предложен.
> 🔴 **Спецификация — в §2 этого файла целиком.** Ссылок наружу за ней нет: якоря §0 читаются ради формулировок и кода, но задача сформулирована здесь.

КОНТЕКСТ. Фабрика научно-популярных лекций. Скилл `slajdy` — семь фаз единой формы; фазы 1–2
описаны заходом `fazy-1-2-dovodka` 2026-08-08, формат карточки — `references/kartochka-slajda.md`.

🔴 **Прямо сейчас идёт ПЕРВОЕ живое применение фазы 1** — интервью по лекции 2 курса «Введение в
теорию категорий» (13 слайдов, 96 минут, контракт утверждён 05.08). Интервью упёрлось в стену, и
разбор стены дал не список мелких недоделок, а **одно системное расхождение: спецификация фазы 1 и
код, который её якобы проверяет, говорят про выход фазы 1 ПРОТИВОПОЛОЖНОЕ.**

⚠ **Честная история этого захода, читай её как предупреждение о себе же.** Первая редакция несла
пять «дыр», найденных грепом по рабочему дереву основной папки. При сборке аналитик прогнал вопрос
входа «задача уже сделана?» — и **две из пяти оказались закрыты** на невлитой ветке `zahod/porcia-1`
(коммиты `9d00c27`, `9f21a84`). Заход переписан. Отсюда прямое указание тебе: **все числа и грепы
ниже сняты с ветки `zahod/porcia-1`, а не с `arka/mat-kostyak`** — вливай её первым ходом (§0) и
перепроверяй сам, прежде чем что-то чинить.

ЦЕЛЬ: свести спецификацию фазы 1 и её гейт в одно непротиворечивое утверждение и сделать так, чтобы
выход фазы 1 — слайды с размеченными блоками — физически ложился в карточку и проверялся командой,
которая МОЖЕТ покраснеть.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **Порядок важности, он же порядок отступления — прямое указание владельца, четыре утра.**
`Р` ≫ `Д-1` ≈ `Д-2` ≫ `Д-6` ≫ `Д-5`. Не успеваешь — **оставь открытым и опиши одной строкой, что
мешает**; это лучше, чем сделать наспех. Инвариант арки: **плохо сделанное хуже несделанного.**
Единственное, что нельзя оставить открытым, — `Р`: ради него заход и написан.

⚠ **Стоп до цели, громко:** ты чинишь ФОРМАТ и ГЕЙТ. Ты **не собираешь лекцию**, не пишешь тексты
слайдов и не заходишь в `teorkat-vvedenie/L2/` — там прямо сейчас идёт живое интервью с владельцем.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **ФАЙЛ ДЛЯ ОТЧЁТА — вот этот физический файл, он лежит в ОСНОВНОЙ папке репозитория, абсолютным путём:** `~/Documents/GitHub/materials/_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_format-kartochki-faza-1.md`. План, вопросы и отчёт пишутся В НЕГО, в секции внизу, и никуда больше. *Урок 46: три приёмки подряд читали пустой файл, потому что отчёт лежал в worktree.*
- **МЕСТО РАБОТЫ — ДВА РЕПОЗИТОРИЯ, и в каждом своё.**
  - `materials`: 🔴 **worktree, ветку в основной папке НЕ трогай.** Замер при сборке захода: основная папка стоит на `arka/mat-kostyak`, живых рабочих папок 10 — `git checkout` там МОЛЧА откатит дерево под всеми. Первым ходом заведи себе папку сам:
    `python3 _generator/tools/git_zona.py worktree add format-kartochki-faza-1 --branch zahod/format-kartochki-faza-1`, затем `cd` в неё. Команда идемпотентна: папка уже есть — просто перейди, это не ошибка и не повод останавливаться.
  - `disciplina`: свою ветку заводишь сам — `cd ~/Documents/GitHub/disciplina && git checkout -b zahod/format-kartochki-faza-1`.
- **ЗОНА (можно менять), три корня:**
  - `~/Documents/GitHub/disciplina/skills/slajdy/references/**` — спецификация формата;
  - `_generator/sborka/**` в `materials` — `bootstrap_lekcii.py`, `gejt_kartochki.py`, `formaty.py`, `bloki.py`;
  - `_generator/tools/fixtures/sborka/**` в `materials` — фикстуры под новый режим гейта;
  - файл-заход (только секции `## ПЛАН`, `## ВОПРОСЫ`, `## ОТЧЁТ`, `## УРОКИ ФАБРИКЕ`).
- 🔴 **`teorkat-vvedenie` — READ-ONLY ЖЁСТКО.** По этим файлам прямо сейчас идёт живое интервью с владельцем в другом окне. Дерево лекции L2 внутри этой папки **не заводить**: его заводит интервью, а не ты. Читать — можно и нужно, писать — нет.
- 🔴 **Второй репозиторий — `GIT_ZONA_REPO=~/Documents/GitHub/disciplina`. ДВА РЕПОЗИТОРИЯ — ДВА КОММИТА.**
- ⚠ Рядом могут идти другие заходы в `materials/_generator/**` — если увидишь чужую незакоммиченную работу в своей зоне, НЕ подметай её своим коммитом: коммить только пути, которые правил сам, поимённо.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД

🔴 **ВЛИТЬ ТРИ ПРИНЯТЫЕ ВЕТКИ ПЕРВЫМ ХОДОМ — не одну.** Все три приняты, ни одна не влита в
`arka/mat-kostyak` (проверено аналитиком перед выдачей захода). Без них §2 читается как ложный, а
две последние **правят те же файлы, что и ты** — начнёшь без них, получишь конфликт или затрёшь
чужую принятую работу.

```
python3 _generator/tools/git_zona.py merge zahod/porcia-1      ; echo "rc=$?"
python3 _generator/tools/git_zona.py merge zahod/gruppa-D      ; echo "rc=$?"
python3 _generator/tools/git_zona.py merge zahod/tihie-polomki ; echo "rc=$?"
```
| ветка | что несёт | почему тебе это нужно |
|---|---|---|
| `zahod/porcia-1` | `--faza`, `BODY_TMPL`, блок «что дальше», `status: rezerv` | предмет §2: без неё Д-3/Д-4 покажутся живыми, а они закрыты |
| `zahod/gruppa-D` | тип вёрстки `kompozit` в `tipy.py` | твоя же зона `_generator/sborka/**` |
| `zahod/tihie-polomki` | правки `build_deck.py` и `sborka/**` под тихие поломки | 🔴 **зона совпадает с твоей полностью** — этот заход шёл прямо перед тобой |

**Проверка фактом, все четыре обязаны сойтись:**
```
grep -n "FAZA_POLYA" _generator/sborka/gejt_kartochki.py                                  # непусто
grep -n "BODY_TMPL" _generator/sborka/bootstrap_lekcii.py                                 # непусто
python3 -c "import sys;sys.path.insert(0,'_generator/sborka');import tipy;print('kompozit' in tipy.REESTR)"   # True
bash _generator/tools/fixtures/sborka/PROGNAT.sh ; echo "rc=$?"                           # зелёный ДО твоих правок
```
🔴 **Последняя строка обязательна и она про честность:** прогони регресс ДО единой своей правки и
покажи вывод в отчёте. Иначе при красном в конце будет не отличить твою поломку от чужой.

⚠ **Про форму команды слияния — не поддавайся гейту.** `check_sborki.py` (проверка С5) краснеет на
`git_zona.py merge <ветка>` и утверждает, что позиционный аргумент не принимается. **Это ложное
срабатывание гейта:** `git_zona.py merge --help` печатает `merge [-h] [--zone ZONE] [--abort]
[--continue] [branch]` — позиционный `branch` есть, и именно этой формой успешно слили ветки заходы
`vhod-fazy-1` и `svedenie-vetok`. Проверено аналитиком перед выдачей. Форма выше верна; красное С5
на этом файле — известный дефект гейта, не твой.

Влитие не прошло (конфликт, чужой лок) — **СТОП, не чини чужое состояние**: напиши строкой в
`## ВОПРОСЫ` и остановись. Работать поверх невлитого нельзя: ты починишь то, что уже починено.

- **ТОЧКА ВХОДА — первая команда:** `git branch --show-current` в обоих репозиториях, `echo $?` после каждой. Дальше — блок «МЕСТО РАБОТЫ».
- Точка отката: зона не чиста в HEAD — коммит или zip перед работой (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО это, в таком порядке. Проект не изучай:**

| # | файл | зачем |
|---|---|---|
| Я1 | `~/Documents/GitHub/disciplina/skills/slajdy/references/kartochka-slajda.md` **целиком** | действующий контракт формата: §1 карточка лекции, §2 шапка слайда, §3 тело, §4 блоки и калибровка К1–К4, §4-бис резерв, §6 что проверяет скрипт |
| Я2 | `~/Documents/GitHub/disciplina/skills/slajdy/references/fazy-1-2-plan.md` §1–§4 | терминология, типология идеи слайда (201 слайд), **гейт выхода фазы 1 — восемь строк, ради которых заход существует**, и гейт выхода фазы 2 |
| Я3 | `_generator/sborka/bootstrap_lekcii.py` · `gejt_kartochki.py` · `formaty.py` · `bloki.py` | живой код: что реально порождается и что реально проверяется |
| Я4 | `_generator/tools/fixtures/sborka/PROGNAT.sh` шапка | как в этом репозитории устроены фикстуры и ловушки — форму переиспользуй, не изобретай |
| Я5 | `teorkat-vvedenie/KONTRAKT-L2.md` ЧАСТЬ 2 (**read-only**) | тринадцать живых карточек слайдов — материал, на котором проверяется, что формат вмещает настоящую лекцию |

- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **НЕ ПОДЫГРЫВАЙ.** Дыры Д-1…Д-5 нашёл аналитик, и ему приятно, если они подтвердятся. **Опровержение любой из них — полноценный успех захода**, оно спасает от правки того, что не сломано: скажи «дыры нет, вот команда, вот вывод». Брак — только выдуманный вердикт. Отдельно про Д-2: носитель пометки центрального блока — **предложение аналитика, а не решение**; свой вариант ты вправе предпочесть, но обязан назвать, чем он лучше по трём пунктам разом — грепается · не ломает `BLOCK_RE` · даёт записать исключение «слайд-перечисление».

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11.

## 2. ЗАДАЧА

### 🔴 Р. ГЛАВНОЕ: `--faza 1` в коде и «гейт выхода фазы 1» в спеке — одно имя, обратный смысл

**Что говорит спека** (`fazy-1-2-plan.md §3`, гейт выхода фазы 1): у каждого слайда назван тип идеи ·
у каждого размечены блоки с типом и мыслью · **ровно один блок помечен центральным** · сумма минут
сходится · число слайдов и иллюстраций в бюджете · у каждого термина есть слайд-хозяин. Дословно
владелец, там же: «результатом интервью является разбиение на слайды и у каждого слайда разбиение на
блоки. Сам факт, что слайдов 15, и что на седьмом слайде 4 блока, а третий из них — нарратив,
объясняющий такую-то идею, — это результат первой фазы».

**Что говорит код** (ветка `zahod/porcia-1`, прогони сам после влития):
```
grep -n "FAZA_POLYA\|_razdely_dlya_fazy" -A4 _generator/sborka/gejt_kartochki.py
grep -n "FAZY_OBYAZATELNY_V_BLOKE" _generator/sborka/gejt_kartochki.py
```
`FAZA_POLYA = {1: ()}` с комментарием «карточка слайда ЕЩЁ НЕ ТРОНУТА (все поля шапки решает Ф2) —
пустой набор требований» · `_razdely_dlya_fazy(1)` возвращает пустой кортеж, то есть **тело на фазе 1
не судится вовсе** · блок «ЧТО ДАЛЬШЕ С ЭТИМ ФАЙЛОМ» называет фазы 2–6 и **фазу 1 не называет**.

**Следствие, ради которого заход написан: `--faza 1` даёт ЗЕЛЁНЫЙ на свежепорождённой карточке, где
нет ни типа идеи, ни блоков, ни центрального блока.** Ни одна из восьми строк гейта выхода фазы 1 не
проверяется ни одной строкой кода. **Это опаснее, чем отсутствие флага:** флаг с правильным именем
существует, отвечает зелёным, и ему верят — ровно тот класс «зелёный чекер с необъявленной слепой
зоной», что стоит в `RUKOVODSTVO` клаузами 4 и 4½.

**РАЗВИЛКА — и она РЕШЕНА аналитиком, а не оставлена тебе. Делай (A).**

- **(A) ПРАВ СПЕКА — ВЫБРАНО.** Фаза 1 пишет в карточку слайда: `tip_idei`, блоки
  `### [тип] мысль`, пометка центрального. `--faza 1` проверяет это и **краснеет на
  незаполненном**. Блок «что дальше» начинает с Ф1.
- **(B) прав код** — отвергнуто.

**Три причины, чтобы ты понимал решение, а не исполнял приказ:**

1. 🔴 **Владелец сказал это дословно**, и фраза стоит в спеке цитатой: «результатом интервью
   является разбиение на слайды и у каждого слайда разбиение на блоки». Вариант (B) означает
   переписать спеку против прямой формулировки владельца — и сделать это в момент, когда интервью
   уже идёт по варианту (A).
2. **Разведённые по файлам, план и исполнение разъедутся молча.** `bloki.check_composition`
   сверяет два раздела ОДНОЙ карточки; межфайловое расхождение он не увидит вовсе. Пришлось бы
   писать второй парсер под второй формат — мы это уже проходили с двумя деревьями лекции, и это
   до сих пор открытый долг.
3. **`matematika_iz` (Д-5) по замыслу живёт в карточке**, то есть покрытие ленты всё равно
   считается по карточкам. Держать план фазы 1 в другом месте — значит завести две системы
   координат для одного объекта.

⚠ **Право возразить остаётся, но планка выше обычного:** опровергай **фактом из кода**, а не
соображением, и скажи это в `## ПЛАН` ДО работы. Нашёл, что (A) физически неисполнимо (например
парсер не переживает нового поля) — это находка и успех захода, не провал.

🔴 **Признание аналитика, чтобы ты не искал злого умысла в коде.** Дефект спроектировал он же:
в заходе `porcia-1` формулировка была «на выходе Ф1 требований к полям шапки нет, их решает Ф2»,
и при приёмке он своими руками прогнал `--faza 1` на пустой карточке, увидел зелёный и **назвал
это успехом критерия**. При этом в заходе на интервью, написанном в тот же вечер, у него же стоит
«выход фазы 1 — слайды с размеченными блоками». Два противоположных утверждения в двух своих
файлах. Ты чинишь не чужую небрежность, а именно это.

### Д-1. Нет поля `tip_idei` — типология не применяется ни к чему

**Требование.** Гейт выхода фазы 1 (`fazy-1-2-plan.md §3`) первой строкой: «у каждого слайда назван
тип идеи и идея одной фразой». Закрытый список типов — там же, §2, выведен замером 201 слайда:
`definition · proof_step · narrative · problem · divider · cover · vizitka · closing`.

**Доказательство дыры (прогони сам, число не переписывай из захода):**
```
grep -rn "tip_idei" ~/Documents/GitHub/disciplina/skills/slajdy/references/ _generator/sborka/ | wc -l
```
**Цена.** Типология измерена по шести декам и живой лекции — и не доезжает ни до одной карточки.
Из типа идеи выводится регистр текста на фазе 3 и предсказание раскладки по жанру («в деках-
доказательствах доминирует `proof_step`»); сегодня это знание существует только в документе.
На живой Л2 тринадцать слайдов, тип каждого назвать некуда.

**Что сделать.** Завести `tip_idei` в шапке карточки слайда: спека (Я1 §2) · шаблон
`SLIDE_CARD_TMPL` · `OBYAZATELNYE_POLYA` (в режиме фазы 1, см. Д-4). Значение вне закрытого списка —
красное, с печатью допустимых. ⚠ `fazy-1-2-plan.md §2` явно говорит, что список закрытый, но **не
окончательный**: слайд, не ложащийся ни в один тип, — повод обсудить новый тип, а не подобрать
ближайший. Значит сообщение об ошибке обязано это говорить, а не только ругаться.

### Д-2. Центральный блок пометить нечем

**Требование.** Тот же гейт: «ровно один блок помечен центральным (исключение — слайд-перечисление,
помечается явно)». Гипотеза подтверждена числом: **195 слайдов из 201 несут ровно один центральный
тезис, слайдов без центра — ноль, все 6 исключений — списки однородных пунктов** (`fazy-1-2-plan.md §2`).

**Доказательство дыры:**
```
grep -rn "centraln" ~/Documents/GitHub/disciplina/skills/slajdy/references/ _generator/sborka/ | wc -l
grep -n "BLOCK_RE" _generator/sborka/bloki.py     # в заголовке блока места под пометку нет
```
**Цена.** Это единственная строка гейта фазы 1, которой физически нечем покраснеть, — а по
`KONSTITUCIYA §11` правило, которому нечему покраснеть, не правило, а надежда. Правило при этом
несущее: вокруг центрального блока строится вся раскладка слайда.

**Предложение аналитика (оспариваемое).** Поле `centralnyj_blok: <мысль центрального блока>` в
шапке, спецзначение `perechislenie` для тех самых 6 из 201. Обоснование: грепается · не трогает
`BLOCK_RE` и потому не ломает разбор существующих тел · даёт записать исключение явно, как и
требует гейт. Альтернатива, которую аналитик рассматривал и отверг, — маркер внутри заголовка блока
(`### [utverzhdenie!] мысль`): ломает парсер и не даёт места исключению.

### ✅ Д-3 и Д-4 — ЗАКРЫТЫ, НЕ ДЕЛАТЬ. Оставлено, чтобы не переоткрыли

Первая редакция захода требовала «завести скелет тела» и «добавить флаг `--faza`». **Оба уже
сделаны** заходом `porcia-1` (коммиты `9d00c27`, `9f21a84`), просто не влиты в основную ветку:
`BODY_TMPL` порождает три канонических раздела с блоком-заглушкой, `--faza` есть в `argparse`,
`status: rezerv` понимает `deck.py`. Проверь сам после влития и не чини починенное:
```
grep -n "BODY_TMPL" -A12 _generator/sborka/bootstrap_lekcii.py
grep -n "add_argument" _generator/sborka/gejt_kartochki.py
grep -n "rezerv" _generator/sborka/deck.py
```
⚠ **Но одна половина Д-3 жива и относится к развилке Р.** Тело порождается заглушкой
`### [narrativ] заполнить` в ОБОИХ разделах — и `--faza 1` её не судит (`_razdely_dlya_fazy(1)`
пуст), тогда как полный гейт судит (дочистка `9f21a84`). Значит **главное действие фазы 1 —
заменить заглушку настоящими блоками — сегодня не проверяется ничем.** По варианту (A) это чинится
тем же режимом; по варианту (B) заглушка законна, и чинить нечего.

### Д-5. Два поля контракта не знает ни один скрипт + решение владельца по резерву

**Доказательство:**
```
grep -rn "matematika_iz\|zamer_tempa" _generator/sborka/ | wc -l
```
Оба поля описаны в `kartochka-slajda.md §2` и там же помечены долгами Д51/Д38 — **живым кодом
подтверждено, что они действительно не знаемы ни бутстрапом, ни гейтом.**

**Цена `matematika_iz` — несущая.** По нему считается сверка покрытия ленты на выходе фазы 2
(`fazy-1-2-plan.md §4`): каждый кусок ленты либо уехал в блок конкретного слайда, либо помечен «в
лекцию не входит» с причиной. Нет поля — не на чем строить гейт — материал теряется молча, а это
дословный страх владельца: «чтобы не было такого, что материал из ленты переедет на два соседних
слайда или наоборот пропадёт при переезде».
⚠ **Форма обязательна:** `matematika_iz` и `illustracii` — ИНЛАЙН-список в одну строку.
Многострочный YAML те команды читают как пустоту и дают **ложно-зелёный** на потерянном материале
(`kartochka-slajda.md §2`).

**🔴 Решение владельца 2026-08-08: `status: rezerv` ПОДТВЕРЖДЁН — правится только СПЕКА, код уже умеет.**
`grep -n "rezerv" _generator/sborka/deck.py` даёт живую поддержку (слайд не входит в дек и в счёт),
а `kartochka-slajda.md §4-бис` до сих пор стоит с пометкой «предложено аналитиком, владельцем ещё не
подтверждено». Пометку снять, дату решения проставить, смысл записать рядом — он не декоративный:
«выкинуть» перестаёт означать «потерять», и это снимает сопротивление на фазе, где режут объём.
⚠ Там же оставлена проверка на первый прогон: если резерв начнёт расти вместо того, чтобы
разгружать, — правило вредное; **строку не удаляй.**

### 🔴 Д-6. ЧТО ДЕЛАТЬ С ИНТЕРВЬЮ, КОТОРОЕ ИДЁТ ПРЯМО СЕЙЧАС — обязательный ход, не вежливость

Пока ты работаешь, владелец в другом окне проходит интервью по лекции 2 и **порождает карточки
слайдов**. Ты добавляешь в формат обязательные поля. Значит через час его карточки станут красными
на твоём же гейте — и это ровно то, чего он просил не допустить: *«цель — не переделывать слайды».*

**Два требования, оба обязательны:**

1. **Миграция должна существовать как КОМАНДА, а не как инструкция «допишите поле».**
   Скрипт (или флаг у `bootstrap_lekcii.py`) дописывает недостающие поля в **уже существующие**
   карточки со значением-заглушкой, не трогая заполненного. Идемпотентно: второй прогон ничего не
   меняет. 🔴 **Сам ты `teorkat-vvedenie/L2/` НЕ трогаешь** — папка read-only, её ведёт интервью;
   ты даёшь команду, прогонит её владелец.
2. **В отчёте — готовая записка для интервью**, три-четыре строки простым языком: какие поля
   появились, какие значения допустимы, какой командой мигрировать существующее дерево. Владелец
   скопирует её в то окно.

**КРИТЕРИЙ Д-6:** команда миграции прогнана на копии свежепорождённого дерева в `/tmp` — поля
появились, ранее заполненное не тронуто, повторный прогон даёт нулевой диф. Записка в отчёте есть.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ). Один, четыре прогона, все выводы — в отчёт дословно.**

Критерий записан под вариант (A). **Выбрал (B) — перепиши критерий в `## ПЛАН` под свой носитель ДО
работы**; неизменным остаётся только его существо: гейт выхода фазы 1 обязан краснеть на артефакте,
где решения фазы 1 нет, и зеленеть на том, где оно есть.

```
# 1. порождение: каркас → slide_order из 13 имён Л2 (Я5) → досыпка
python3 _generator/sborka/bootstrap_lekcii.py /tmp/proba-faza1
#    ... вписать 13 имён в slide_order ...
python3 _generator/sborka/bootstrap_lekcii.py /tmp/proba-faza1

# 2. 🔴 ГЛАВНЫЙ ПРОГОН. Гейт ОБЯЗАН ПОКРАСНЕТЬ на свежепорождённом — сегодня он ЗЕЛЁНЫЙ,
#    и это ровно тот дефект, ради которого написан заход
python3 _generator/sborka/gejt_kartochki.py --faza 1 /tmp/proba-faza1 ; echo $?
#    ожидание: exit 1 · красное поимённо по tip_idei и по центральному блоку ·
#    строка охвата «проверено 13 из 13» · строка «НЕ проверяю: <перечень>»

# 3. заполнить tip_idei, блоки и центральный на ВСЕХ слайдах → гейт ЗЕЛЁНЫЙ
python3 _generator/sborka/gejt_kartochki.py --faza 1 /tmp/proba-faza1 ; echo $?
#    ожидание: exit 0

# 4. РЕГРЕССИЯ: поведение БЕЗ флага и на других фазах не тронуто
sh _generator/tools/fixtures/sborka/PROGNAT.sh ; echo $?
```

**Заход НЕ сдан, если прогон 2 зелёный.** Гейт, который не может провалиться, — не гейт; красный на
свежепорождённой карточке и есть единственное доказательство, что расхождение Р закрыто.
🔴 **Прогон 2 сделай ДО единой строки правки и покажи его вывод тоже** — это фиксация исходного
состояния: сегодня он зелёный, и приёмка обязана увидеть оба вывода, «было зелено» и «стало красно».

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из
Y». То же про сами Д-1…Д-5: опроверг какую-то — назови команду и вывод, а не мнение.

## 3. ВЕРИФИКАТОР
Нужен: заход меняет формат, по которому уже порождены живые деревья лекций. Свежий субагент, другой
метод: взять карточку слайда из `_generator/tools/fixtures/sborka/zhivoj-teorkat` (или любую живую,
`ls` покажет), прогнать через изменённый гейт БЕЗ флага фазы и подтвердить, что вердикт совпадает с
вердиктом до правки (`git stash` / `git show HEAD:` — на выбор). Список расхождений обязан быть ПУСТ;
непустой — это регрессия, а не «улучшение».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая. **Два
репозитория — два коммита, оба хэша в отчёт.**
```
# в materials (своя worktree-папка)
git --no-optional-locks add -- _generator/sborka _generator/tools/fixtures/sborka
git --no-optional-locks commit -m "sborka: режим фазы 1 у гейта карточек, скелет тела, tip_idei" -- _generator/sborka _generator/tools/fixtures/sborka
python3 _generator/tools/git_zona.py check --zone _generator/sborka   # должен быть ✅
git --no-optional-locks show --stat                                    # ТОЛЬКО твои пути

# в disciplina
cd ~/Documents/GitHub/disciplina
git --no-optional-locks add -- skills/slajdy/references
git --no-optional-locks commit -m "slajdy/references: tip_idei, центральный блок, резерв подтверждён" -- skills/slajdy/references
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ здесь — не собранный HTML, а ТРИ ВЫВОДА ПРОГОНОВ критерия дословно**, плюс абсолютные пути изменённых файлов списком. Приёмка обязана увидеть красный прогон 2 и зелёный прогон 3 своими глазами.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Фикстура закрепила дефект как желаемое свойство — и защищала его от починки

Ловушки 29 и 32 в `PROGNAT.sh` были написаны предыдущим заходом так, что сторожили **само
расхождение**: «`--faza 1` зелёный на свежепорождённой карточке» и «`--faza 1` зелёный на теле-
заглушке». Пока они стояли, любая попытка привести гейт в соответствие со спекой валила регресс, и
исполнитель без этого захода в руках прочитал бы красное как СВОЮ поломку и откатился бы.

ЦЕНА: дефект прожил от `porcia-1` до этого захода с зелёным регрессом над ним; при приёмке
`porcia-1` зелёный `--faza 1` на пустой карточке был **назван успехом критерия** — фикстура
подтвердила ложное утверждение, вместо того чтобы его поймать. Стоило целого захода на разбор и
переписывания двух ловушек; сколько ещё стоило бы, если бы формат поехал в живую лекцию, не
измерено, потому что успели раньше.

### Критерий верификатора требовал невозможного — «список расхождений ПУСТ» при задаче менять формат

§3 захода велел прогнать живую карточку через изменённый гейт и подтвердить, что вердикт совпадает
с прежним, а список расхождений пуст. Заход при этом добавляет обязательные поля — то есть пустой
список расхождений означал бы, что работа не сделана. Критерий верификатора был написан по шаблону
«ничего не сломалось», не глядя на то, что именно меняется.

ЦЕНА: исполнитель, исполнивший бы его буквально, обязан был бы либо не добавлять поля, либо
подогнать вердикт — то есть выбирать между невыполнением задачи и подлогом. Я критерий переписал в
`## ПЛАН` ДО работы (сохранив существо: старые замечания на месте, новые — только объяснимые) и
получил осмысленный результат — 0 исчезнувших, 20 добавленных, все двадцати объяснены. Прямых
потерь нет; цена — риск подлога, заложенный в формулировку.

## ПЛАН — (заполняет исполнитель)

**Вход отработан, все замеры сняты САМИМ, до единой правки.**

Влиты три ветки, все три без конфликта: `porcia-1` → `bd2db0d`, `gruppa-D` → `74065c9`,
`tihie-polomki` → `ad7fd6e`. Четыре проверки факта сошлись: `FAZA_POLYA` есть (строка 46),
`BODY_TMPL` есть (строка 89), `'kompozit' in tipy.REESTR` → True, `PROGNAT.sh` → **ФИКСТУРЫ
ЗЕЛЁНЫЕ** ДО моих правок.

**Р воспроизведён своими руками** (вывод — в отчёте): свежепорождённое дерево из 13 карточек Л2,
`--faza 1` → `ЗЕЛЁНЫЙ: 13 карточек проверено, замечаний 0`, rc=0. Ни типа идеи, ни блоков, ни
центрального — гейт молчит. Дефект живой.

**Д-1, Д-2, Д-5 подтверждены грепом, все три дают 0** (не переписываю из захода, вывод в отчёте).
**Д-3/Д-4 действительно закрыты** — `BODY_TMPL`, `--faza`, `rezerv` в `deck.py` на месте, не трогаю.

Иду вариантом **(A)**, спор не открываю: физических препятствий не нашёл, парсер новое поле
переживает (шапка — flat key:value, `KEY_RE` берёт любой `[A-Za-z_]+`).

### Что делаю — семь ходов

1. **`tip_idei` (Д-1).** Закрытый список восемью значениями из `fazy-1-2-plan.md §2`, живёт в
   `formaty.py` рядом с `OBYAZATELNYE_POLYA` — один источник на бутстрап и гейт. Значение вне
   списка — красное, и сообщение говорит **«повод обсудить новый тип, а не подобрать ближайший»**
   (`§2` явно про незакрытость списка, ругани мало).
2. **`centralnyj_blok` (Д-2) — беру предложение аналитика, оно проходит все три пункта.**
   Проверил на живом коде, а не на соображении: `BLOCK_RE = ^###\s+\[([a-z_]+)\]\s+(.+?)\s*$` —
   маркер в заголовке пришлось бы прятать внутрь группы `мысль`, а её `check_composition` сверяет
   между разделами по паре `(tip, mysl)`, значит маркер надо дублировать в оба раздела, иначе
   состав разъедется. Поле в шапке этого не делает вовсе. Своего варианта не предлагаю.
   🔴 **Одно усиление, без него поле декоративно:** значение обязано **резолвиться ровно в один
   блок** раздела «Математика» по его мысли (или быть `perechislenie`). Иначе опечатка в копии
   мысли даёт зелёный, а «ровно один» не проверяется ничем — та же болезнь, что чиню.
3. **Идея одной фразой — это `zachem`, нового поля не завожу.** Гейт спеки требует «тип идеи И
   идея одной фразой»; `zachem` в `kartochka-slajda.md §2` дословно этим и является («зритель
   должен увидеть, что…»). Записываю это в спеку явно, чтобы следующий не гадал.
4. **`FAZA_POLYA[1]` = `imya · nazvanie · zachem · tip_idei · minuty`.** `minuty` здесь потому,
   что гейт Ф1 требует сходимости суммы минут — значит минуты решаются на Ф1, а не на Ф2 (и
   `KONTRAKT-L2` их назначил именно на интервью). `akcent`, `vazhnost`, `tip_verstki`, `liniya`,
   `byudzhet_slov` остаются за Ф2.
5. **Тело на Ф1 судится наполовину.** `_razdely_dlya_fazy(1)` сегодня пуст — развожу на два
   вопроса: **мысль блока** обязана быть настоящей уже на Ф1 (это и есть «размечены блоки»),
   **тело блока** — с Ф2. Так главное действие фазы 1 перестаёт быть непроверяемым.
6. **Блок «что дальше» начинает с Ф1** — `FAZY_OBYAZATELNY_V_BLOKE` → `(1,…,6)`, строка ФАЗА 1 в
   `LIFECYCLE_TMPL`.
7. **Д-6 — миграция флагом `--migraciya` у `bootstrap_lekcii.py`.** Дописывает недостающие поля
   заглушкой и перешивает машинный блок «что дальше», заполненного не трогает, второй прогон даёт
   нулевой диф. Текстовая вставка в шапку, не YAML-круг: парсер шапки лишён комментариев и порядка,
   круг через него потерял бы и то, и другое.

### Что НЕ делаю и объявляю слепой зоной вслух — три строки гейта Ф1 из шести

Гейт печатает их сам, строкой `НЕ проверяю:`; молча не останется ни одна.

- **сумма минут против `dlitelnost_minut`** — допуска владелец не называл, а точное равенство
  покраснеет на живой Л2 (сумма поштучных времён `KONTRAKT-L2 §ЧАСТЬ 2` меньше заявленных 120).
  Придумывать допуск за владельца — ровно то, за что этот заход и написан. → `## ВОПРОСЫ` №1.
- **число слайдов против `byudzhet.slajdov`** — поле порождается нулём (`_brief_text`, `n =
  len(slide_ids)`, а на ходе «каркас» список пуст) и ходом «досыпать» не обновляется. Гейт на нём
  краснел бы всегда и на всём. Дефект бутстрапа, чинить его — за границей задачи. → `## ВОПРОСЫ` №2.
- **слайд-хозяин каждого вводимого термина** — списка терминов лекции не существует ни в одном
  файле; сверять не с чем.

### 🔴 Спорю с критерием §3 ВЕРИФИКАТОРА — он не может быть выполнен как написан

«Вердикт на живой карточке БЕЗ флага совпадает с вердиктом до правки, список расхождений ПУСТ» —
это требование **несовместимо с самой задачей**: заход добавляет обязательные поля, значит полный
гейт на старой карточке обязан покраснеть по-новому. Пустой список расхождений тут означал бы, что
я ничего не сделал. Заменяю на проверяемое и более сильное, существо сохраняю:

- вердикт на живых фикстурах **до** правки уже КРАСНЫЙ (`zhivoj-teorkat` 3 из 3, `demo-karta`
  9 из 9 — на отсутствии блока «что дальше», унаследованный долг); после правки он обязан
  остаться красным, **старые замечания — все до одного на месте**, а новые — только те, что
  объясняются добавленными полями, поимённо;
- **после `--migraciya`** та же карточка обязана позеленеть ровно настолько, насколько была
  зелёной до захода. Это и есть настоящий смысл «не переделывать слайды».

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **Какой допуск у «сумма минут сходится с длительностью лекции»?** Точное равенство поставить
   нельзя. Замер (команда, а не глаз; из корня репозитория):
   ```
   awk 'NR>=345 && NR<=636' teorkat-vvedenie/KONTRAKT-L2.md | grep -o "ВРЕМЯ ~[0-9]* мин" \
     | grep -o "[0-9]*" | awk '{s+=$1} END {print "сумма минут:", s, "· слайдов с временем:", NR}'
   ```
   → **сумма минут: 91 · слайдов с временем: 12** при заявленных в том же файле ~120 минутах, и
   тринадцатый слайд («Итог») времени не несёт вовсе. То есть точное равенство покраснело бы на
   живой лекции сразу и дважды. Придумывать допуск за владельца я не стал — это ровно тот
   класс решения, из-за которого написан этот заход. Пока строка объявлена слепой зоной гейта.
   Назовёте допуск (±10 %? ±15 минут? «не больше длительности»?) — клауза закрывается двумя строками.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. **`byudzhet.slajdov` порождается нулём и никогда не обновляется.** `bootstrap_lekcii._brief_text`
   пишет `slajdov: len(slide_ids)`, а на ходе «каркас» список пуст — в `brief.md` уезжает `0`; ход
   «досыпать» это поле не трогает. Из-за этого клауза «число слайдов укладывается в бюджет» не
   заводится: гейт краснел бы всегда и на всём. Чинится либо пересчётом при досыпке, либо тем, что
   бюджет становится решением владельца и порождается пометкой «заполнить». Вне моей зоны, не трогал.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/`
   ДОСТАВЛЕНО: нет

3. **`PROGNAT.sh` врёт про собственный охват.** Шапка объявляет «ЛОВУШЕК ДВАДЦАТЬ ЧЕТЫРЕ» и
   описывает 1–24, тогда как ловушек в файле 35 (число снято `grep -c '── ловушка'`). Ловушки 25–34
   добавлялись заходами до меня и в шапку не попали; я добавил 35-ю и шапку тоже не переписывал —
   это чужой накопленный долг, а не мой. Отдельно тревожно, что этот файл — сам гейт охвата.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/`
   ДОСТАВЛЕНО: нет

4. **`KONTRAKT-L2.md` сам с собой не сходится в числе слайдов** (read-only, не трогал): строка 353
   говорит «12 слайдов, ~120 минут», а заголовков `**§` в ЧАСТЬ 2 — тринадцать. Заход тоже называет
   13, и греп подтверждает 13, так что я работал по 13. Но расхождение живое, и оно в файле, по
   которому прямо сейчас идёт интервью.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и зачем

**Р закрыт: `--faza 1` теперь краснеет ровно там, где решений фазы 1 нет.** Вариант (A), спор не
открывал. `FAZA_POLYA[1]` вместо пустого кортежа несёт `imya · nazvanie · zachem · tip_idei ·
minuty`; разметка блоков судится с Ф1, тело блока — с Ф2/Ф3; блок «что дальше» начинается с ФАЗЫ 1.

**Д-1: `tip_idei`** — закрытый список восемью значениями живёт в `formaty.TIPY_IDEI`, один источник
на бутстрап и гейт. Сообщение об ошибке печатает допустимые И говорит, что список закрыт, но не
окончателен: «повод обсудить НОВЫЙ тип, а не подобрать ближайший».

**Д-2: `centralnyj_blok`** — взял предложение аналитика, своего варианта не выдвигаю: проверил по
живому коду, что маркер в заголовке блока лёг бы внутрь группы «мысль» у `bloki.BLOCK_RE`, а её
`check_composition` сверяет между двумя разделами по паре (тип, мысль) — маркер пришлось бы
дублировать в оба. Одно усиление: значение обязано **резолвиться ровно в один** блок «Математики»
(или быть `perechislenie`), иначе поле декоративно — копия мысли с опечаткой давала бы зелёный.

**Д-5: `status: rezerv`** — пометка «владельцем не подтверждено» снята, дата решения 2026-08-08
проставлена, смысл записан рядом; строку-условие отмены («если резерв начнёт расти») не тронул.
`matematika_iz` и `zamer_tempa` теперь порождаются бутстрапом; гейта на них по-прежнему нет — это
объявлено вслух, а не умолчано.

**Д-6: миграция** — флаг `bootstrap_lekcii.py <лекция> --migraciya`. Дописывает недостающие поля
заглушкой по именованным якорям и перешивает машинный блок «что дальше»; заполненного не трогает,
тело карточки не трогает вовсе, второй прогон даёт нулевой диф.

**Д-3/Д-4 не трогал** — проверил после влития, действительно закрыты.

### 🔴 Дыра, которую создала моя же правка, и я её закрыл

До захода бутстрап всегда клал в тело слово «заполнить», поэтому **пустого** тела блока не бывало.
Перевод Ф1 на разметку делает «блок с мыслью и БЕЗ тела» законным промежуточным состоянием — и
полный гейт стал бы пропускать размеченный, но ненаписанный блок ЗЕЛЁНЫМ. Нашёл не глазами:
на этом провалилась фикстура 29, которую я же писал. Полный гейт теперь краснеет и на пустом теле.

### Как проверил

Четыре прогона критерия — выводы ниже дословно. Плюс отрицательный контроль (в критерии его нет,
но без него «краснеет» ничего не значит): испортил `tip_idei` на одном слайде и внёс опечатку в
`centralnyj_blok` на другом — гейт дал ровно два замечания, поимённо, с перечнем допустимых типов и
со списком реальных мыслей блоков.

### Что НЕ трогал

`teorkat-vvedenie/**` — только читал (`KONTRAKT-L2.md` ЧАСТЬ 2, 13 карточек). Дерево лекции L2 не
заводил. `_studio/docs/**` не правил, новых `.md` не заводил — `register_doc.py` звать не за чем.
Чужую работу в `_generator/**` не подметал: в зоне ровно четыре моих файла.

### Результат верификатора (свежий субагент, другой метод)

Реконструировал гейт ДО правки из коммита `ad7fd6e` в `/tmp` и сравнил поимённо на двух живых
деревьях (`zhivoj-teorkat`, `demo-karta`):
- **исчезло старых замечаний: 0** — все 12 «блок „что дальше“ отсутствует» на месте дословно;
- **добавилось 20**, все до одного из двух объяснимых классов: 12 × `tip_idei` не заполнено, 8 ×
  `centralnyj_blok` не заполнен. Ничего сверх;
- расхождение 12 против 8 объяснено фактом: 4 карточки `demo-karta` несут пустое тело, `check_slide`
  уходит в ранний `return` по K3 — это ровно та слепая зона, что объявлена гейтом;
- новое требование к телу блока на живых карточках сработало **0 раз** — ложных срабатываний нет;
- трейсбеков нет ни в одном из шести прогонов;
- миграция на `demo-karta`: сверено программно **155 пар «поле → значение»** — расхождений 0,
  потерянных полей 0; `difflib.ndiff` по шапкам даёт только `+`-строки трёх новых полей, ни одной
  `-`-строки; второй прогон — `diff -r` пуст;
- на мигрированном дереве все 9 замечаний «что дальше» исчезли — перешитый блок новому набору
  Ф1–Ф6 удовлетворяет, а не остаётся вечно красным.

**«РЕГРЕССИЙ НЕ НАЙДЕНО, проверено 12 из 12 карточек»** (число снято командой, подтверждено самим
гейтом: «проверено 3 из 3» + «проверено 9 из 9»).
Слепые зоны верификатор назвал сам: миграцию гонял только на `demo-karta`; режим `--faza 1` в
сравнение «до/после» не входил по построению (старый давал зелёный на всём, сравнивать нечего).

### 🔴 ЗАПИСКА ДЛЯ ОКНА, ГДЕ ИДЁТ ИНТЕРВЬЮ (Д-6, копировать целиком)

> Формат карточки слайда дополнен — три поля и одна команда.
>
> **Что появилось в шапке карточки слайда:**
> - `tip_idei` — тип идеи слайда. Допустимо ровно восемь значений: `definition`, `proof_step`,
>   `narrative`, `problem`, `divider`, `cover`, `vizitka`, `closing`. Слайд не ложится ни в один —
>   это повод обсудить новый тип, а не подобрать ближайший.
> - `centralnyj_blok` — мысль центрального блока, **дословно та же строка**, что стоит в заголовке
>   блока `### [тип] мысль` в разделе «Математика — развёрнуто». Слайд-перечисление (список
>   однородных пунктов) помечается значением `perechislenie`.
> - `matematika_iz` — куски ленты, инлайн-списком в одну строку. На фазе 1 не заполняется.
>
> **Что теперь требует гейт выхода фазы 1** (`python3 _generator/sborka/gejt_kartochki.py --faza 1
> <папка лекции>`): `nazvanie`, `tip_idei`, `zachem` (идея одной фразой), `minuty`,
> `centralnyj_blok` и размеченные блоки. ⚠ Заголовки блоков пишутся **в обоих разделах одинаково**
> («Математика — развёрнуто» и «Текст слайда — сжато») — состав блоков сверяется между ними; тела
> при этом на фазе 1 остаются пустыми, это законно.
>
> **Уже созданные карточки поднимаются одной командой, слайды переделывать не нужно:**
> ```
> python3 _generator/sborka/bootstrap_lekcii.py <папка лекции> --migraciya
> ```
> Дописывает недостающие поля пометкой «заполнить» и обновляет служебный блок «что дальше».
> Заполненного не трогает, тело карточки не трогает вовсе; прогнать повторно безопасно — второй раз
> ничего не меняет.

### 🔴 НЕОБРАТИМОЕ

Необратимого нет. Ни одного удаления, переименования и перемещения в репозиториях; `git reset` и
`git checkout` поверх несохранённого не делал. Три ветки влиты штатным `git_zona.py merge`, все три
без конфликта, каждая — отдельным коммитом слияния (хэши ниже), откат — обычный `git`. Всё
временное лежало в `/tmp` (`proba-faza1`, `proba-neg`, `proba-staroe*`, `verif_*`, `mig_*`) и
репозиториев не касалось. Временный файл `_generator/sborka/_staryj_bootstrap_VREMENNO.py` (копия
прежней версии из git, нужна была, чтобы породить дерево старого формата) удалён тем же ходом;
`git status` показывает ровно четыре изменённых файла, его среди них нет.

---

**АРТЕФАКТ:**

**Прогон 1 — порождение** (13 имён Л2 из `KONTRAKT-L2.md` ЧАСТЬ 2, число снято грепом, не с потолка):
```
каркас: /tmp/proba-faza1/brief.md (slide_order пуст, INTERVYU.md со скелетом разделов) — …
досыпано: 13 новых карточек слайдов из 13 в slide_order (новых слайдов 13/13)
rc=0
```

**Прогон 2 — ГЛАВНЫЙ. ДО единой строки правки (фиксация исходного состояния):**
```
=========== ПРОГОН 2 (ДО ПРАВОК) ===========
ЗЕЛЁНЫЙ: 13 карточек проверено, замечаний 0
rc=0
```
🔴 Вот он, дефект: ни типа идеи, ни блоков, ни центрального блока — гейт молчит и отвечает зелёным.

**Прогон 2 — ПОСЛЕ правок (тот же вход, то же дерево):**
```
КРАСНЫЙ (выход фазы 1): проверено 13 из 13 карточек, замечаний 91
  ✗ chto-delali-v-proshlyj-raz: обязательное поле 'nazvanie' не заполнено
  ✗ chto-delali-v-proshlyj-raz: обязательное поле 'zachem' не заполнено
  ✗ chto-delali-v-proshlyj-raz: обязательное поле 'tip_idei' не заполнено
  ✗ chto-delali-v-proshlyj-raz: обязательное поле 'minuty' не заполнено
  ✗ chto-delali-v-proshlyj-raz: раздел «Математика — развёрнуто» несёт блок с незаполненной мыслью
      («заполнить») — разметка блоков это выход ФАЗЫ 1, а не украшение шаблона
  ✗ chto-delali-v-proshlyj-raz: раздел «Текст слайда — сжато» несёт блок с незаполненной мыслью
      («заполнить») — разметка блоков это выход ФАЗЫ 1, а не украшение шаблона
  ✗ chto-delali-v-proshlyj-raz: centralnyj_blok не заполнен — гейт выхода Ф1 требует, чтобы ровно
      один блок был помечен центральным (или 'perechislenie' для слайда-перечисления)
  … те же семь замечаний на каждом из 13 слайдов, 91 = 13 × 7 …
НЕ проверяю: 7 пункт(ов) — ниже поимённо
  · сумма минут против dlitelnost_minut лекции (допуск владельцем не назван; точное равенство
    покраснело бы на живой лекции)
  · число слайдов и иллюстраций против byudzhet карточки лекции (byudzhet.slajdov порождается
    нулём и ходом «досыпать» не обновляется)
  · слайд-хозяин каждого вводимого термина (списка терминов лекции нет ни в одном файле)
  · центральный блок на слайде БЕЗ текста (K3 — законный случай, блоков нет вовсе)
  · регистр текста и соответствие мысли заявленной — это слой LLM-верификатора (Я1 §6)
  · matematika_iz и zamer_tempa — покрытие ленты и замер темпа (долги Д51/Д38, поля порождаются,
    но гейта на них ещё нет)
  · верен ли акцент и та ли это вообще лекция — только человек (Я1 §6)
rc=1
```

**Прогон 3 — план фазы 1 заполнен на ВСЕХ 13 слайдах (тип идеи, идея, минуты, блоки с мыслью,
центральный блок; на «Итоге» — `perechislenie`):**
```
ЗЕЛЁНЫЙ (выход фазы 1): проверено 13 из 13 карточек, замечаний 0
НЕ проверяю: 7 пункт(ов) — ниже поимённо
  · сумма минут против dlitelnost_minut лекции (допуск владельцем не назван; точное равенство
    покраснело бы на живой лекции)
  · число слайдов и иллюстраций против byudzhet карточки лекции (byudzhet.slajdov порождается
    нулём и ходом «досыпать» не обновляется)
  · слайд-хозяин каждого вводимого термина (списка терминов лекции нет ни в одном файле)
  · центральный блок на слайде БЕЗ текста (K3 — законный случай, блоков нет вовсе)
  · регистр текста и соответствие мысли заявленной — это слой LLM-верификатора (Я1 §6)
  · matematika_iz и zamer_tempa — покрытие ленты и замер темпа (долги Д51/Д38, поля порождаются,
    но гейта на них ещё нет)
  · верен ли акцент и та ли это вообще лекция — только человек (Я1 §6)
rc=0
```

**Отрицательный контроль (сверх критерия) — гейт умеет краснеть не только на пустом:**
```
КРАСНЫЙ (выход фазы 1): проверено 13 из 13 карточек, замечаний 2
  ✗ kategoriya: centralnyj_blok 'объект, морфизм, композиция и едница' не совпал ни с одной мыслью
      блока раздела «Математика — развёрнуто». Мысли блоков: «объект, морфизм, композиция и
      единица», «аксиомы категории», «функтор»
  ✗ retrakt: tip_idei 'teorema' не из закрытого списка. Допустимы: definition, proof_step,
      narrative, problem, divider, cover, vizitka, closing. ⚠ Список закрыт, но НЕ окончателен
      (fazy-1-2-plan.md §2): слайд, не ложащийся ни в один тип, — повод обсудить НОВЫЙ тип с
      владельцем, а не подобрать ближайший
rc=1
```

**Прогон 4 — РЕГРЕССИЯ, `sh _generator/tools/fixtures/sborka/PROGNAT.sh`:**
```
  ✅ ловушка 28: порождённая карточка (шапка и тело заполнены по-настоящему) — гейт зелёный
── ловушка 29: 🔴 Р (заход format-kartochki-faza-1) — --faza 1 КРАСНЕЕТ на свежепорождённой карточке
  ✅ ловушка 29: свежая карточка — --faza 1 красный поимённо (tip_idei, центральный блок, мысль
     блока), с охватом и слепыми зонами; принятый план фазы 1 — зелёный, полный гейт всё ещё красный
  ✅ ловушка 30: playwright в наличии — явный kegl_px пережил сборку (kegl_px: 77), дек собран
  ✅ ловушка 31: карточка без блока «что дальше» — гейт красный, поимённо
── ловушка 32: 🔴 МЫСЛЬ блока — выход Ф1, ТЕЛО блока — выход Ф2/Ф3, и оба умеют краснеть
  ✅ ловушка 32: заглушка — оба красные на МЫСЛИ; размечено без тел — faza1 зелёный, полный красный
     на ТЕЛЕ; написано — оба зелёные
  ✅ ловушка 33: {@3}+список → <ul> с data-scene-from="3" и двумя <li>
  ✅ ловушка 34а: {.tlsit} — красное поимённо, с номером строки, rc≠0
  ✅ ловушка 34б: {.tlist} — зелёное, rc=0
── ловушка 35: 🔴 Д-6 — миграция дописывает поля, не трогая заполненного, и идемпотентна
  ✅ ловушка 35: поля дописаны, блок «что дальше» перешит с ФАЗЫ 1, заполненное цело, второй
     прогон — нулевой диф
ФИКСТУРЫ ЗЕЛЁНЫЕ
rc=0
```
⚠ Тот же `PROGNAT.sh` прогнан **ДО единой своей правки** и был зелёным (вывод снят первым ходом) —
без этого при красном в конце было бы не отличить мою поломку от чужой.

**Изменённые файлы, абсолютными путями:**
- `/Users/ivanyakovlev/Documents/GitHub/materials-wt/format-kartochki-faza-1/_generator/sborka/gejt_kartochki.py`
- `/Users/ivanyakovlev/Documents/GitHub/materials-wt/format-kartochki-faza-1/_generator/sborka/formaty.py`
- `/Users/ivanyakovlev/Documents/GitHub/materials-wt/format-kartochki-faza-1/_generator/sborka/bootstrap_lekcii.py`
- `/Users/ivanyakovlev/Documents/GitHub/materials-wt/format-kartochki-faza-1/_generator/tools/fixtures/sborka/PROGNAT.sh`
- `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/references/kartochka-slajda.md`
- `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/references/fazy-1-2-plan.md`
- `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_format-kartochki-faza-1.md` (этот файл)

**Влитые ветки (§0):** `zahod/porcia-1` → `bd2db0d` · `zahod/gruppa-D` → `74065c9` ·
`zahod/tihie-polomki` → `ad7fd6e`. Все три без конфликта.

**КОММИТ:** `7abab36` (materials, ветка `zahod/format-kartochki-faza-1`, worktree
`materials-wt/format-kartochki-faza-1`) — «sborka: режим фазы 1 у гейта карточек — tip_idei,
центральный блок, разметка блоков, миграция живых деревьев», 4 файла · `673e361` (disciplina, ветка
`zahod/format-kartochki-faza-1`) — «slajdy/references: фаза 1 пишет в карточку — tip_idei,
центральный блок, резерв подтверждён владельцем», 2 файла.
`python3 _generator/tools/git_zona.py check --zone _generator/sborka` → **✅ работа доехала в git,
вне git ничего нет.** `git show --stat` в обоих репозиториях — только мои пути, чужого не подмёл.
⚠ Сам файл-заход (этот) лежит в ОСНОВНОЙ папке `materials` на ветке `arka/mat-kostyak` и в мою
worktree-ветку не входит — коммитит его приёмка вместе с вердиктом, как обычно.
