# Канал исполнителя — final-svedenie-i-proba (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — всё расписано до команд и хэшей; плана, рождающегося по ходу, нет.
> Субагенты не нужны.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.**

КОНТЕКСТ. Фабрика научно-популярных лекций. За ночь принято **одиннадцать** заходов: фаза 1 стала
исполнимой и проверяемой, конвейер замкнулся одной командой, солвер встроен в сборку, композит
собирается, гейты перестали врать, иллюстрации получили заказы и гейт.

**Фаза 1 лекции 2 по теории категорий пройдена владельцем вживую:** 14 слайдов · 56 блоков (3–5 на
слайд) · `tip_idei` и `centralnyj_blok` у всех 14 · 11 иллюстраций с заказами · гейт `--faza 1`
зелёный, 14 из 14. Материал готов к вёрстке.

ЦЕЛЬ: свести ночь в одно состояние и **собрать один слайд насквозь** — чтобы владелец увидел
результат до того, как запустится автономный прогон по всем четырнадцати.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее: ты собираешь ОДИН слайд, а не лекцию.** Остальные тринадцать
не трогаешь. Смысл пробы — увидеть, где рвётся, потратив один слайд вместо четырнадцати.

🔴 **И ты проходишь по нему фазы 2 и 3 ПО-НАСТОЯЩЕМУ — с раскладкой ленты по блокам и сжатием
текста.** Требование владельца дословно: *«ключевая вещь, которая требует искусственного
интеллекта, — именно сжатие объёма текста»*. Пустой слайд проверил бы механику и не проверил
единственное, чего скрипт сделать не может. Подробности — Ш4.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_generator/skeleton/tokens.css` `_generator/skeleton/base.css` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_final-svedenie-i-proba.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- 🔴 **ТОЧКА ОТКАТА — ПЕРВЫМ ДЕЛОМ.** Запиши хэш ствола в `## ПЛАН` до первого `merge`:
  `git --no-optional-locks rev-parse arka/mat-kostyak`. Это единственное, чем откатывается всё сведение.
  🔴 **Ветки `zahod/*` НЕ УДАЛЯТЬ ни одну** — решение владельца. Убираются только рабочие ПАПКИ.
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.** Спецификация — в §2.

| # | файл | зачем |
|---|---|---|
| Я1 | `_generator/tools/git_zona.py` подкоманды `merge`, `doctor`, `worktree`, `check` | единственная дверь к git |
| Я2 | `~/Documents/GitHub/disciplina/skills/slajdy/references/style-core.md`, раздел «Accent — one ochre» | 🔴 готовые значения для Ш3, копируются, не выдумываются |
| Я3 | `teorkat-vvedenie/L2/slajdy/<любой>/slaid.md` — **READ-ONLY** | сверху блок «ЧТО ДАЛЬШЕ С ЭТИМ ФАЙЛОМ» с командами всех фаз |
| Я4 | 🔴 `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md`, разделы **`## Фаза 2 · Раскадровка`** и **`## Фаза 3 · Текст слайдов`** ЦЕЛИКОМ | **главные якоря Ш4.** Как раскладывать ленту по блокам и как сжимать. Читать и применять, не изобретать |
| Я5 | `teorkat-vvedenie/LENTA-L2.md` — **READ-ONLY** | математика лекции: 13 разделов `## <название> · ~N мин`. Отсюда берётся содержание блоков |
| Я6 | `~/Documents/GitHub/disciplina/skills/slajdy/verifikatory/faza-3-tekst-slaidov.md` | промпт для Ш5 |
| Я7 | `_generator/sborka/deck.py --help` · `slaid.py --help` · `gejt_kartochki.py --help` | команды сборки и проверки |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — четыре хода, строго по порядку

### Ш1. ВЛИТЬ ТРИ ОСТАВШИЕСЯ ВЕТКИ

Пятнадцать из восемнадцати уже влиты (проверено приёмкой). Остались три:
```
python3 _generator/tools/git_zona.py merge zahod/dolgi-po-fazam              ; echo "rc=$?"
python3 _generator/tools/git_zona.py merge zahod/gejty-vrut                  ; echo "rc=$?"
python3 _generator/tools/git_zona.py merge zahod/gejt-illyustracij-lifecycle ; echo "rc=$?"
```
⚠ `zahod/konsolidaciya-i-git-kontur` тоже числится невлитой — **её НЕ трогай**, она старше этой
арки, разбирается отдельно.
Конфликт — **не разруливай**, останови и в `## ВОПРОСЫ`: обе стороны — принятая работа.

**КРИТЕРИЙ Ш1:** `python3 _generator/tools/git_zona.py doctor` → раздел потерь показывает
**безопасно по всем**; `python3 _generator/tools/gejt_illyustracij.py teorkat-vvedenie/L2` → зелёный
с числами `слайдов: 14 · ссылок: 11 · заказов: 11`.

### Ш2. 🔴 ПЕРЕМЕРИТЬ ДОЛГИ — ПО ФОРМУЛИРОВКЕ ДОЛГА, А НЕ ПО СТРОКЕ ИЗ МЕТКИ

Заход `gejty-vrut` объявил четыре долга группы D живыми. **Приёмка перепроверила и нашла у него
ошибку метода: он грепал не то.** Проверено командами на его же ветке:

| долг | как он проверял | что на самом деле |
|---|---|---|
| **Д52#2** | `grep preserveAspectRatio base.css` → 0 | `gruppa-D` положила его в **`engine.js`**: 3 вхождения, хук `[data-ill]`, строки 43/50/51. **Долг закрыт** |
| **Д27** (номер слайда) | `grep nomer *.css *.js` → пусто | оно называется **`lect-progress`**: `engine.js` 2 вхождения, `base.css` 1. **Прогресс-бар на месте** |
| **Д28** | искал ВЫЗОВ `kegl_dlya_znakov` | долг спрашивал «кто выигрывает при конфликте — не решено». Решено: `podgonka.py` — «ВЫИГРЫВАЕТ ЯВНОЕ (`kegl_px`)», и это исполняется кодом `tipy.py:61` (`kegl_px` читается первым, возврат до автоподбора). **Долг закрыт** |
| **Д18** | `tipy.ОТЛОЖЕННЫЕ` → 2 из 3 | 🟢 **тут он прав**: частично, метку не снимать |

🔴 **Правило, по которому переставляешь метку: читаешь ТЕКСТ ДОЛГА и проверяешь ТО, ЧТО В НЁМ
НАПИСАНО.** Команда из метки — подсказка прошлого измерения, а не определение долга. Она могла
устареть вместе с тем, где лежит починка.

**Что сделать:** перепроверить своими командами Д52#2, Д27, Д28 (Д18 не трогать) и переставить
метки там, где подтвердится. Затем прогнать `reviziya_dolgov.py` и назвать **новое число**.
Ожидание приёмки: было 36, станет **34** (Д52#2 и Д28 в МЁРТВ; Д27 остаётся частичным).
🔴 **Не подгоняй под это число.** Не сошлось — так и напиши, число приёмки тоже может быть неверным.

**КРИТЕРИЙ Ш2:** по каждому из трёх — команда и вывод; новое число названо; `rc=0` у чекера.

### Ш3. ВЕРНУТЬ ВТОРОЙ ЯРУС ВЫДЕЛЕНИЯ — три строки, значения даны каноном

`style-core.md`, раздел «Accent — one ochre», задаёт дословно:
```css
:root{ --accent:#785a18; --marker:#c0aa5a; }
.acc{color:var(--accent)}
```
Контраст 5.09:1 на бумаге `#e6e5e1`, AA-pass. Это **ярус 2 из трёх** в иерархии выделения:
вес → **цвет** → отдельная формула-результат.
Сегодня в `_generator/skeleton/tokens.css` **`--accent` и `--marker` не объявлены вовсе** (0
вхождений), а `base.css` несёт `.acc{font-weight:700;color:var(--ink)}` — только жир.

Объявить оба токена в `tokens.css`, дописать цвет в `.acc` **поверх** жирности (не вместо).
🔴 **Ничего не выдумывать:** значения скопировать из канона.

**КРИТЕРИЙ Ш3:** `grep -c -- "--accent:" _generator/skeleton/tokens.css` → 1; в собранном на Ш4
слайде элемент `.acc` имеет цвет `#785a18` — показать строкой из HTML/CSS, а не утверждением.

### Ш4. 🔴 ОДИН СЛАЙД НАСКВОЗЬ, ВКЛЮЧАЯ ФАЗЫ 2 И 3 — ради этого всё

🔴 **Требование владельца, оно и делает эту пробу осмысленной:** *«ключевая вещь, которая требует
искусственного интеллекта, — именно сжатие объёма текста»*. Собрать пустой слайд значит проверить
механику и **не проверить то единственное, что не может сделать скрипт**. Поэтому ты проходишь
фазы 2 и 3 по-настоящему, на одном слайде.

**Материал.** Слайд берёшь из `teorkat-vvedenie/L2` — тот, у кого 5 блоков и есть иллюстрация.
🔴 **Копию в `/tmp`, живую лекцию не трогай**: утром по ней продолжат.

**Инструкцию НЕ ИЗОБРЕТАЙ — прочти и примени.** Оба раздела `SKILL.md` целиком:
- **`## Фаза 2 · Раскадровка`** — какой кусок ленты ложится в какой блок. Адрес выхода: раздел
  `## Математика — развёрнуто` карточки. ⚠ Поле `matematika_iz` — **список заголовков кусков
  ленты в одну строку**, `matematika_iz: [<заголовок>, <заголовок>]`, а не вольная фраза: по нему
  считается покрытие ленты, и вольная фраза его обнуляет.
- **`## Фаза 3 · Текст слайдов`** — сжатие. Адрес выхода: раздел `## Текст слайда — сжато`, тот же
  состав блоков, что в «Математике». Сжимать **внутри блока**, в регистре своего типа: определение
  сжимается не так, как нарратив.

Источник математики — `teorkat-vvedenie/LENTA-L2.md`, раздел, соответствующий этому слайду.

**Бюджет.** `byudzhet_slov` в карточке не заполнен (это поле фазы 2). Возьми из `brief.md`:
`slov_vsego: 700` на 14 слайдов = **50 слов**, проставь его в карточку и скажи об этом. Гейт
считает слова раздела «Текст слайда — сжато» против этого числа, все слова с весом 1.0.

**Поля вёрстки** (`tip_verstki`, `liniya`) тоже фазы 2 — проставь минимально разумно и назови,
что проставил: владелец пересмотрит.

🔴 **Что ты НЕ решаешь:** состав и порядок блоков, их типы и мысли — **это выход фазы 1, принятый
владельцем**. Кажется, что блок лишний или мысль неточна — **строка в отчёт, не правка**.

### Ш5. ПРОГНАТЬ ВЕРИФИКАТОР ФАЗЫ 3 НА СВОЁМ ЖЕ ТЕКСТЕ

`disciplina/skills/slajdy/verifikatory/faza-3-tekst-slaidov.md` — промпт, только что переведённый
на новое дерево, но **на живом материале ни разу не прогнанный** (долг Д14: из пяти промптов
подтверждён один). Прогони его субагентом на своём сжатом тексте.

🔴 **Ты автор текста — значит судить его должен не ты.** Субагент свежий, промпт применяет как
написано. Вердикт «всё хорошо» на первом же собственном тексте — подозрителен: приведи его
замечания дословно, включая те, с которыми не согласен.

**КРИТЕРИЙ Ш4–Ш5 (может ПРОВАЛИТЬСЯ):**
1. HTML собран, `rc=0`, **абсолютный путь — в строку АРТЕФАКТ**;
2. в слайде есть **настоящий сжатый текст**, а не заголовки блоков. Число слов названо и
   сопоставлено с бюджетом 50;
3. гейт карточки на этом слайде зелёный **без флага фазы** (полная проверка);
4. солвер отработал — строка вывода; иллюстрация есть и не чёрная; `.acc` цветной (Ш3);
5. верификатор фазы 3 прогнан, замечания приведены дословно;
6. 🔴 **список того, что пришлось доделать руками**, чтобы слайд собрался. Пусто — лучший исход,
   так и напиши. Каждая доделка — **находка**, а не мелочь: завтра она умножится на тринадцать.

---

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «собралось», а «прошло N шагов из N, руками
доделано K вещей».

## 3. ВЕРИФИКАТОР

Ш1 — лоссовый: `doctor` после сведения обязан показать безопасно по всем веткам.
Ш2 — метка, поставленная неверно, хуже отсутствующей: **на каждый изменённый статус в отчёте
команда и её вывод**, иначе метка не принимается.
Ш3 — регресс: `bash _generator/tools/fixtures/sborka/PROGNAT.sh` зелёный.
Ш4 — сам себе верификатор: артефакт открывается глазами.

🔴 **`teorkat-vvedenie/L2/` — READ-ONLY.** Все пробы в `/tmp`. Утром по этой лекции продолжат.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_generator/skeleton/tokens.css` `_generator/skeleton/base.css` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_final-svedenie-i-proba.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_generator/skeleton/tokens.css` `_generator/skeleton/base.css` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_final-svedenie-i-proba.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Точка отката** (записана ПЕРВЫМ делом, до первого `merge`):
`git --no-optional-locks rev-parse arka/mat-kostyak` → `08fddc65b80b59fab241410ab05276816ef11aec`
(HEAD: `Merge branch 'zahod/provod-illyustracij' into arka/mat-kostyak`).

**Ветка проверена:** `git branch --show-current` → `arka/mat-kostyak` ✅, уже активна, `checkout` не делаю.

**Якоря Я1–Я7 прочитаны** (только они, проект сверх них не открывал):
Я1 `git_zona.py merge/doctor/worktree/check --help`, Я2 `style-core.md` §Accent (значения `--accent:#785a18`, `--marker:#c0aa5a`), Я3 блок «ЧТО ДАЛЬШЕ» карточки `kategoriya/slaid.md`, Я4 `SKILL.md` §Фаза 2 и §Фаза 3 целиком, Я5 `LENTA-L2.md` (первые ~570 строк — вернусь дочитать разделы под выбранный на Ш4 слайд адресно), Я6 промпт верификатора фазы 3, Я7 `--help` трёх команд сборки.

**Предпосылка §0 сверена перед планом, а не принята на слово:** шапка §0 несёт строку «вливать нечего, проверено при сборке: невлитых веток 0» — она противоречит Ш1 («остались три»). Прогнал `git_zona.py doctor`: реально невлитых **3** (`zahod/dolgi-po-fazam`, `zahod/gejty-vrut`, `zahod/gejt-illyustracij-lifecycle`), ровно как в Ш1; строка в §0 — стухший артефакт, не команда к действию. Дальше иду по Ш1, не по шапке §0.

**Проверка риска мержа (doctor предупредил — «спроси того, кто в ветке работает»):** все три ветки вычекаучены в `materials-wt/<имя>`; `git status --short` в каждой из трёх — пусто, живого недокоммиченного захода нет. Мержить безопасно.

**Дальше — четыре хода строго по §2, без импровизации:**
1. **Ш1.** Три `git_zona.py merge` (dolgi-po-fazam, gejty-vrut, gejt-illyustracij-lifecycle); `zahod/konsolidaciya-i-git-kontur` не трогаю. Конфликт → стоп, в `## ВОПРОСЫ`, не разруливаю. Критерий: `doctor` безопасно по всем + `gejt_illyustracij.py teorkat-vvedenie/L2` зелёный 14/11/11.
2. **Ш2.** Перепроверить Д52#2, Д27, Д28 своими командами (не по строке из метки), Д18 не трогать; `reviziya_dolgov.py` → новое число (ожидание 34, не подгоняю).
3. **Ш3.** `--accent`/`--marker` в `tokens.css` из Я2 дословно; `.acc` — цвет поверх жирности, не вместо.
4. **Ш4–Ш5.** Один слайд (5 блоков + иллюстрация) из `teorkat-vvedenie/L2` копирую в `/tmp`, живую лекцию не трогаю. Прохожу фазы 2 и 3 по SKILL.md (Я4) по-настоящему: раскладка ленты по блокам (`matematika_iz` — инлайн-список заголовков кусков) + сжатие текста (плакатный стиль, термин не раньше определения). Бюджет слов 50 (700/14 из `brief.md`) проставляю в карточку и называю это отдельно. `tip_verstki`/`liniya` — минимально разумно, назвать. Собрать `slaid.py`, гейт без флага фазы, солвер, иллюстрация не чёрная, `.acc` цветной. Верификатор фазы 3 (Я6) субагентом на своём тексте — замечания дословно. Список ручных доделок — отдельно.

Затем §4 (коммит своей зоны, `add`+`commit` с путями, `git_zona.py check --zone`) и `## ОТЧЁТ`.

## ВОПРОСЫ — (заполняет исполнитель)

1. Ш1, третий мерж (`zahod/gejty-vrut`) заблокирован самим `git_zona.py`: 4 пути одновременно
   грязные и участвуют в слиянии — `_studio/docs/KARTA.md`, `OCHERED.md`,
   `kod_format-kartochki-faza-1.md`, `kod_tihie-polomki.md` (734 строки суммарного диффа, не мои —
   существовали на диске ДО начала этого захода). Инструмент сам предложил «закоммить или откатить»
   — сделал ни то, ни другое: коммитить чужой непонятный дифф под своим сообщением и откатывать
   чужую незакоммиченную работу — обе операции запрещены дисциплиной без выяснения, чьё это. Мерж
   НЕ доведён; `doctor` показывает «безопасно 17 из 18» вместо ожидаемых 18 из 18.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. Ш2 предполагает, что метки долгов (Д52#2/Д27/Д28/Д18) переставляются мной — но живой источник
   `DOLG.md` лежит по пути `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md`, то есть в ДРУГОМ
   git-репозитории (`disciplina`, не `materials`), вне зоны и вне досягаемости `git_zona.py`. Тот
   репозиторий к тому же стоит на branch `zahod/format-kartochki-faza-1`, не на дефолтной ветке —
   ещё один повод не трогать его наугад. Я перепроверил Д52#2/Д27/Д28 своими командами (совпало с
   таблицей захода дословно), но физически не переставил метки: `reviziya_dolgov.py` печатает
   прежние 36 живых, не ожидаемые 34.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. 🔴 Урок фабрике: для «гринфилд»-лекции (ни разу не собиравшейся в дек) в репозитории НЕТ
   санкционированного пути получить `<лекция>/math/katex.json`. `_generator/harvest_katex.py` по
   собственному докстрингу работает только round-trip’ом с УЖЕ отрендеренной колоды («новые формулы
   — задача будущего „math-арки“»); единственный живой рендерер живого KaTeX
   (`teorkat-vvedenie/src/tools/katex_kesh.js`) читает npm-пакет `katex`, физически лежащий внутри
   `lsh-2026-perechislitelnaya/.../node_modules/katex` — совсем другого проекта, и написан под ОДНУ
   конкретную лекцию, не как переиспользуемый инструмент. Я обошёл это разовым скриптом в `/tmp` (не
   в зоне, не коммичу), но без общего инструмента цена повторится на всех тринадцати слайдах L2 и на
   любой другой новой лекции.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

4. 🔴 Урок фабрике: поле `zagolovok_na_ekrane`, оставленное со значением-заглушкой `заполнить`
   (как его кладёт `bootstrap_lekcii.py`), рендерится БУКВАЛЬНО на экран слайда («ЗАПОЛНИТЬ» крупным
   Forum-капсом) — а `gejt_kartochki.py` его не проверяет вовсе (в списке девяти обязательных полей
   этого поля нет). Карточка проходит гейт зелёной и собирается «успешно», выдавая на экран техническую
   заглушку. Нашёл только потому, что довёл слайд до реальной сборки картинкой, а не до зелёного гейта.
   ДОМ: <эта арка>/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Отрицательный вердикт с охватом: прошло 4 из 4 ходов, но Ш1 и Ш2 — не до конца; Ш3–Ш5 —
полностью.** Ниже по шагам.

**Ш1 (влить три ветки) — 2 из 3.** `zahod/dolgi-po-fazam` и `zahod/gejt-illyustracij-lifecycle`
влиты без конфликтов (`482adb2`, `2637467`). `zahod/gejty-vrut` — НЕ влит: `git_zona.py` сам
отказал, 4 пути одновременно грязные и участвуют в слиянии, не мои (см. ВОПРОС 1). Не разруливал,
не коммитил и не откатывал чужое. `zahod/konsolidaciya-i-git-kontur` не трогал, как велено.
Проверено: worktree всех трёх веток чистые (`git status --short` пусто) — активного захода в них
нет, риска затереть чужую сессию не было. Критерий: `doctor` → «безопасно 17 из 18» (не 18 из 18);
`gejt_illyustracij.py teorkat-vvedenie/L2` → `слайдов: 14 · ссылок: 11 · заказов: 11 · ЗЕЛЁНЫЙ`, эта
часть критерия выполнена.

**Ш2 (перемерить долги) — команды прогнаны, метки НЕ переставлены.**
- Д52#2: `grep -n data-ill engine.js` → хук на месте (`skeleton/engine.js:35`), рядом
  `svg.setAttribute('preserveAspectRatio', ...)` (`engine.js:47-51`) — долг закрыт, совпадает с
  заходом.
- Д27: `grep -n lect-progress skeleton/*.css skeleton/*.js` → `base.css:63`, `engine.js:18,9` —
  прогресс-бар на месте, совпадает.
- Д28: `grep -n kegl_dlya_znakov\|kegl_px sborka/tipy.py` → `kegl_px` читается первым и побеждает
  (`tipy.py:61-63`) — совпадает.
- Д18 не трогал.
Новое число НЕ поменялось: `reviziya_dolgov.py` печатает прежние **36 живых** (ожидание приёмки
было 34). Причина — не в проверке, а в том, что физически негде переставить метку: см. ВОПРОС 2
(`DOLG.md` в другом репозитории, вне зоны). Не подгонял число под ожидание.

**Ш3 (второй ярус выделения) — выполнено.** `_generator/skeleton/tokens.css`: добавлена строка
`--accent: #785a18; --marker: #c0aa5a;` (значения дословно из `style-core.md`, без выдумывания).
`_generator/skeleton/base.css`: `.acc{font-weight:700;color:var(--ink)}` →
`.acc{font-weight:700;color:var(--accent)}` — цвет поверх жирности, не вместо. Критерий:
`grep -c -- "--accent:" tokens.css` → `1`; регресс `PROGNAT.sh` → `ФИКСТУРЫ ЗЕЛЁНЫЕ, rc=0`; в
собранном на Ш4 слайде — строкой из HTML: `.acc{font-weight:700;color:var(--accent)}` и
`--accent: #785a18` (не «на глаз»).

**Ш4–Ш5 (один слайд насквозь, фазы 2+3 по-настоящему) — выполнено, критерий прошёл.**
Слайд `centr-gruppy` (5 блоков, иллюстрация `centr-i-ego-obraz`) скопирован в `/tmp/L2-proba`,
живая `teorkat-vvedenie/L2/` не тронута. Раздел «Математика — развёрнуто» заполнен полными фразами
по `LENTA-L2.md`, разделу «Центр группы · ~7 мин» (`matematika_iz: [Центр группы]`). Раздел «Текст
слайда — сжато» сжат по `plakatnyj-stil.md`/`status-bloka.md` (регистр по типу блока, доказательство
шагами, правило первого употребления). `byudzhet_slov: 50` (700/14 из `brief.md`, проставлено и
названо). `tip_verstki: polosa_vertikalnaya`, `liniya: 67` (иллюстрация «около трети ширины» из
`zakaz.md` → текстовая доля ~67%, проставлено и названо).
1. **АРТЕФАКТ:** `/tmp/centr-gruppy.html` — открывать браузером (кадр для просмотра —
   `/tmp/centr-gruppy.png`), `rc=0`.
2. Число слов сжатого текста — **50 из 50** (посчитано `bloki.render_section_markdown` + `.split()`,
   та же формула, что у гейта).
3. `gejt_kartochki.py /tmp/L2-proba` (без `--faza`, полная проверка) — на `centr-gruppy` **ни
   одного ✗** (остальные 13 слайдов лекции красные ожидаемо — не начаты, не моя зона по заданию).
4. Солвер: `math/katex.json` для этой лекции не существовал вовсе (см. УРОК 3) — собран разовым
   скриптом `/tmp/build_math_cache.js` тем же локальным npm-пакетом `katex`, что использует READ-ONLY
   `teorkat-vvedenie/src/tools/katex_kesh.js`; строка вывода: `katex 0.16.9, отрендерено: 14 из 14`.
   Иллюстрация — реальный SVG (`centr-i-ego-obraz`), не чёрная, видна на кадре. `.acc` — цвета
   `#785a18` (Ш3), подтверждено строкой из собранного HTML.
5. Верификатор фазы 3 (Я6) прогнан субагентом на моём тексте. Нашёл: 3 механических нарушения
   («без финальной точки» — 3 из 5 блоков заканчивались точкой), 1 содержательный дефект (шаг
   доказательства «тогда …» терял стартовую гипотезу «пусть функтор существует» при сжатии), 1
   пограничную находку (слово «центр» нигде не напечатано на экране, только символ `Z(G)`).
   Исходный вердикт был «гейт не закрыт». Все три класса замечаний признаю верными и поправил:
   убраны точки, доказательство получило явную посылку «пусть $Z$ функтор:», слово «центр» вписано
   в определение. После правки бюджет пересчитан заново (50 слов), гейт и сборка прогнаны повторно
   — чисто.
6. **Что пришлось доделать руками** (находки, не мелочи):
   - `zagolovok_na_ekrane: заполнить` рендерился на экран буквально как «ЗАПОЛНИТЬ» — гейтом не
     ловится (см. ВОПРОС 4); очищено до `""`.
   - `math/katex.json` отсутствовал целиком — собран вручную (см. ВОПРОС 3 и п.4 выше).
   - 3 правки по итогам верификатора фазы 3 (см. п.5).

**Верификатор (§3 захода):** Ш1 — лоссовый, `doctor` после сведения безопасен НЕ по всем (17/18,
см. ВОПРОС 1) — частичный провал признан, не скрыт. Ш2 — по каждому изменённому статусу команда и
вывод приведены выше; статус физически не переставлен (см. ВОПРОС 2), число не подогнано. Ш3 —
регресс `PROGNAT.sh` зелёный. Ш4 — артефакт открыт и просмотрен глазами (кадр приложен по тексту
выше).

**Что НЕ трогал:** остальные 13 слайдов `teorkat-vvedenie/L2` (READ-ONLY), `zahod/konsolidaciya-i-git-kontur`,
всё вне зоны, `_studio/docs/KARTA.md` (не звал `register_doc.py` — новых `.md` не заводил, а файл
и так дирти чужим — см. ВОПРОС 1, включать в свой коммит не стал).

**Необратимого нет.** Оба мержа — новые merge-коммиты поверх точки отката
`08fddc65b80b59fab241410ab05276816ef11aec`, откатываются `git reset --hard` на неё же (не выполнял,
называю для владельца). `--no-verify` не использовал. `push` не делал — коммиты только локальные
(`doctor` предупреждает: 6 коммитов впереди origin); решение о `push` оставляю владельцу.

**Время прогона + токены** — печатает стартовая команда в `result`-строку лога, здесь не привожу
(исполнителю счётчик недоступен).

**АРТЕФАКТ:** `/tmp/centr-gruppy.html` — открывать браузером; кадр для быстрого просмотра —
`/tmp/centr-gruppy.png`.
**КОММИТ:** сделан следующим ходом сразу после этой записи, тремя путями (см. ниже) —
`git_zona.py check --zone final-svedenie-i-proba` → должен быть ✅, хэш допишу после самого коммита.
