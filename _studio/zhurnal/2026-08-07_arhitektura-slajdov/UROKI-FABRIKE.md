# УРОКИ ФАБРИКЕ — арка «2026-08-07_arhitektura-slajdov»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---

# УРОКИ

### 1. Оркестратор делал работу сам вместо того, чтобы её раздавать
ЦЕНА: ночной заход `arhitektura-slajdov` — ≈0,7 млн токенов дорогой модели ушло в собственный
контекст Opus при ≈1,86 млн на всех десяти субагентах вместе. Владелец увидел расход через 20 минут
после старта и попросил экономить; в ответ исполнитель слил пять пар шагов и снял два шага целиком,
из них один — живой прогон, ради которого заход и заводился. То есть перерасход оркестратора
оплачен не токенами, а вырезанной проверкой. Поймал владелец, не гейт.
ВЕРДИКТ: <пусто до закрытия арки>

### 2. Решение владельца, не доехавшее в дневник тем же ходом, врёт исполнителю
ЦЕНА: владелец разрешил править генератор в ночном заходе, но запись осталась только в чате;
дневник продолжал утверждать «генератор — отдельный заход». Заход, которому предписано при
расхождении считать правым ДНЕВНИК, законно отменил шаг Г15 и генератор не тронул — потерян один
шаг из восьми и один день. Ошибка аналитика, не исполнителя.
ВЕРДИКТ: <пусто до закрытия арки>

### 3. Число, у которого нет команды, живёт как факт и расходится по документам
ЦЕНА: «~650 записей корпуса инцидентов» — фантом; реальная база 445 (`srez_po_faze.py`). Число
успело попасть в дневник интервью, в текст ночного захода и в его же контекст, прежде чем ночная
разведка пересчитала. Проценты 12/54/33 при этом оказались верны — то есть ложным был именно
знаменатель, и это самая незаметная форма вранья.
ВЕРДИКТ: <пусто до закрытия арки>

### 4. Верификатор фактологии заходов окупился трижды за сутки
ЦЕНА: на ночном заходе поймал несуществующий флаг `disable-model-invocation` у `slajdy` (критерий
приёмки был бы зелёным, не проверив ничего) и ложное «в disciplina писатель один». На двух дневных
— команду коммита, собиравшую пути из двух репозиториев в одну строку: она упала бы на обязательном
последнем ходу, и два имени типов слайда, которых нет в словаре. Стоимость верификатора — один
субагент на заход; стоимость несрабатывания — сорванный коммит и потерянный отчёт.
ВЕРДИКТ: <пусто до закрытия арки>

### 5. Промпт верификатора писался против формата входа, которого в живом материале не существует
ЦЕНА: `verifikatory/faza-3-tekst-slaidov.md` ожидал свёрстанный `slides/<id>.html` с `####`-разметкой
статуса блока; единственный живой черновик (`teorkat-vvedenie/src/content/*.md`) разметки не несёт
вовсе — и `status-bloka.md §0` это прямо предсказывал. Промпт физически не мог запуститься, пока
исполнитель не дописал ему раздел про неразмеченный вход. То есть болезнь «рычаг, который ни на чём
не краснел» воспроизвелась НА УРОВНЕ ФОРМАТА ВХОДА, раньше уровня критериев. Пять оставшихся
промптов (фазы 1, 2, 4, 6, 7) не прогнаны и, вероятно, несут тот же дефект.
ДОСТАВЛЕНО: verifikatory-stilya#1
ВЕРДИКТ: <пусто до закрытия арки>

### 6. Правило держалось на атрибуте, которого не было ни на одном живом файле
ЦЕНА: канон требовал `data-text-ok="diagram"` как единственную легальную дверь для букв в рисунке.
Приёмка 2026-08-08 прогнала `grep -c` по всем десяти иллюстрациям теории категорий — атрибут
отсутствует у ВСЕХ, включая четыре рисунка, которые по содержанию являются законными
коммутативными диаграммами. То есть правило год держалось на содержании и на добросовестности
читателя, а формальная метка не ставилась никогда и ничего не проверяла. Поймал не гейт сборки
(он судит только кириллицу, а буквы там латинские), а новый верификатор понятности.
ДОСТАВЛЕНО: verifikatory-stilya#2
ВЕРДИКТ: <пусто до закрытия арки>

### 7. Параллельные заходы безопасны только при физически непересекающихся зонах
ЦЕНА: два дневных захода пошли одновременно и не столкнулись — но проверено это было командой, а
не глазом, и в одном из них зона изначально включала путь, который писал и сосед. Прошлая арка при
четырёх одновременных заходах дала половину всех ошибок суток: заход на несуществующей ветке,
забытый worktree, зона без нужного файла.
ВЕРДИКТ: <пусто до закрытия арки>

### 8. Аналитик писал якорь в основную папку, исполнитель работал в worktree — файла он не увидел
ЦЕНА: заход `solver-vmeshcheniya` получил как «ГЛАВНЫЙ ВХОД» раздел «План фазы 4» в
`_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md`. Раздел был написан аналитиком, но **не закоммичен**
(Cowork не может писать в git из песочницы), а заход работал в отдельной рабочей папке — там
файл был версии из ветки, без правок. Исполнитель честно доложил «раздела нет физически, grep по
735 строкам пуст» и сделал работу по дублю из тела захода. Итог: из солвера выпало ограничение на
минимальный размер иллюстрации, и он ужал её до полоски 8 % высоты ради текста — формально прав,
по сути провал, круг переделки. **Класс:** якорь захода обязан быть в git, иначе для worktree его
не существует; аналитик, который не может коммитить, не может и назначать якорем свою свежую правку.
ВЕРДИКТ: <пусто до закрытия арки>

### 9. Разметка разбирается генератором, но ни CSS, ни движок её не обрабатывают — и все гейты зелёные
ЦЕНА: `{blur@N|…}` (скрыть ответ до клика) превращается генератором в `<span class="blur-reveal"
data-reveal="N">`, но в `_generator/skeleton/base.css` и `engine.js` этого класса и атрибута НЕТ
ВОВСЕ — проверено грепом при приёмке 08.08, ноль совпадений в обоих. Значит **ответ виден с самого
начала**, а HTML валиден и все пять слоёв QA зелёные. Нашёл не гейт, а верификатор потерь, и
только потому, что сверял правило с кодом, а не с текстом. **Класс:** «разметка порождается» и
«разметка работает» — разные утверждения, и второе не проверяет никто. Тот же класс отказа однажды
стоил 79 блоков из 83.
ВЕРДИКТ: <пусто до закрытия арки>

### 10. Отрицательный вердикт верификатора оказался единственным способом найти четыре молчаливые потери
ЦЕНА: при переписывании пяти фаз из 68 живых правил **4 потеряны молча** — не замечены ни автором,
ни гейтами, ни чтением. Нашлись только сплошной сверкой со старой версией (400 строк из 400).
Одна из четырёх оказалась не потерей правила, а живым дефектом (урок 9). **Класс:** на лоссовой
операции выборка «не менее трети» — это минимум для отчёта, но правду даёт только сплошная; и
вердикт «потерь нет» на переписывании канона следует считать подозрительным по умолчанию.
ВЕРДИКТ: <пусто до закрытия арки>

### 11. Заход посылали пользоваться инструментом с невлитой ветки — ТРИЖДЫ за сутки
ЦЕНА: `kartochka-i-sborka` не нашёл бы `_generator/sborka/` (поймано при сборке, вписано слияние
вручную) · `solver-v2` не нашёл бы `vmeshchenie.py` (то же) · `obratnyj-progon` **был запущен и
работал без солвера вообще** — поймано аналитиком уже после старта, владельцу пришлось слать
поправку в живую сессию. Каждый раз лечение было одно и то же: вписать слияние в текст захода
руками. Аналитик забывал не по невнимательности — **места под это в шаблоне захода не было**.
**Класс:** правило, которое приходится помнить на каждом применении, механизмом не является
(`KONSTITUCIYA §11`). Лечится не внимательностью, а слотом в генераторе плюс гейтом на пустоту —
заведено заходом `konsolidaciya-i-git-kontur`.
ВЕРДИКТ: <пусто до закрытия арки>

### 12. Вердикт был написан свободной формой — и три дня не существовал для гейта
ЦЕНА: разбор класса «E конфликт путей» от 2026-08-05 записан формой «не шум, механизм назван ·
причина: …», а парсер `check_incidenty.py:104-118` знает ровно три формы. Всё прочее разбирается
в `None` = «вердикта нет». Итог: гейт три дня предъявлял СТАРЫЙ вердикт «шум» от 04.08, а
содержательный разбор лежал строкой ниже и не считался; поймано только 07.08 при закрытии сессии,
когда пришлось лезть в код парсера, чтобы понять, почему класс «без вердикта» при видимом вердикте.
**Класс:** дом записи и читатель записи разошлись по формату, и это НЕ ловится ничем — запись на
месте, глазами читается, гейт молчит про причину. Тот же класс, что урок 9 («разметка порождается»
и «разметка работает» — разные утверждения).
ВЕРДИКТ: <пусто до закрытия арки>

### 13. Измеритель молча терял весь текст карточки — обратный прогон измерен на ПУСТОТЕ
ЦЕНА: `postroit_kartochku.py` строил временную карточку без обёртки `### [tip] мысль`; парсер
блоков не находил ни одного заголовка, весь текст уходил в `orphan`, а рендер собирает markdown
ТОЛЬКО из `blocks` — на выходе пустая зона. Без исключения, без единого красного гейта. Итог:
весь столбец «солвер» в `REZULTATY.md` обратного прогона (16 строк) измерен на пустом контенте —
у пустого блока `scrollHeight===clientHeight`, поэтому «влезал» ЛЮБОЙ кегль. Вывод «солвер
систематически завышает кегль», на котором аналитик принял заход `obratnyj-progon` и построил
заход `solver-v3`, оказался ЧАСТИЧНО артефактом измерения (при починке разница уменьшилась, но не
исчезла — реальный эффект тоже есть). Поймал следующий исполнитель, а не гейт: `bloki.py` сам
объявляет orphan «фактом, не ошибкой — гейт решает», но гейта на пустую карточку в конвейере нет.
**Класс:** приёмка проверяла ЧИСЛА отчёта и их воспроизводимость, но не то, что измеряли на
непустом входе; «инструмент отработал без ошибки» и «инструмент измерил то, что нужно» — разные
утверждения. Тот же класс, что уроки 9 и 12.
ВЕРДИКТ: <пусто до закрытия арки>

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

---

## Улов сессии 2026-08-08 (ночная, Cowork-аналитик). Уроки 14–19

> Собраны механически из секций `## УРОКИ ФАБРИКЕ` пятнадцати заходов этой сессии плюс из приёмок.
> **Вердикты пусты — их ставит отдельная сессия закрытия арки, по §7.3.**

### 14. 🔴 ОДИН КЛАСС, ЧЕТЫРЕ СЛУЧАЯ: заход противоречит сам себе внутри одного файла
Четыре независимых исполнителя нашли одно и то же, каждый в своём заходе:
шапка описывала состояние git, которое опровергал §0 строкой ниже (`konsolidaciya-i-git-kontur`) ·
секция «0. ПЕРВЫЙ ХОД» утверждала обратное заданию (`svedenie-i-gigiena`) ·
заход назвал адрес, который сам же опровергает (`arhitektura-slajdov`) ·
репро-команда печатала вывод, снятый с другого состояния файла (`porcia-1`).
ЦЕНА: у `porcia-1` — «≈15 минут на два прогона и чтение двух модулей, прежде чем нашёл настоящую
причину» (слова исполнителя); у остальных — по кругу остановки каждый. Ловили ИСПОЛНИТЕЛИ, ни один
не пойман аналитиком при письме. Общее у всех четырёх: **аналитик не прогонял команды, которые
вписывал в заход.**
ВЕРДИКТ: <пусто до закрытия арки>

### 15. 🔴 ИЗМЕРЕНИЕ НЕ ТОГО: пять раз за сессию число вписано без проверки, ЧТО считается
Аналитик: посчитал 112 блоков вместо 56 (грепал оба раздела карточки) · «ловушек станет 54» при
27→32 (шаблон цеплял лишние строки) · объявил «обе ветки солвера влиты» по выводу `doctor`, который
в тот прогон соврал · принял «закрыто 4 из 4» группы D по отчёту · назвал корпусные 6–7 мин/слайд,
когда для этой лекции был свой замер 10.
Исполнители: `gejty-vrut` грепал `preserveAspectRatio` в `base.css`, а починка лежала в
`engine.js`; искал `nomer`, когда оно называется `lect-progress`; искал ВЫЗОВ функции, когда долг
спрашивал про решение развилки.
ЦЕНА: трижды владелец возражал по существу и был прав; один раз это едва не сняло с очереди живой
дефект, один раз — едва не объявило невыполненной сделанную работу группы D.
**Механизм у всех один: команда написана, но не проверено, что она меряет предмет утверждения.**
ВЕРДИКТ: <пусто до закрытия арки>

### 16. Фикстура закрепила дефект как желаемое свойство — и защищала его от починки
`porcia-1` написал ловушку 28 на поведение «шапка заполнена, тело не тронуто → зелёный». Дочистка
приёмки сделала это поведение неверным — ловушка покраснела первой и была переписана.
ЦЕНА: ноль (поймано сразу), но механизм назван: ловушка, написанная под текущее поведение, а не
под требование, превращается в защиту дефекта. Второй экземпляр — `format-kartochki-faza-1`.
ВЕРДИКТ: <пусто до закрытия арки>

### 17. Критерий готовности требовал невозможного, и это выяснилось только у исполнителя
`format-kartochki-faza-1`: верификатор требовал «список расхождений ПУСТ» при задаче, которая по
замыслу МЕНЯЕТ вердикты. `vhod-fazy-1`: критерий требовал десять красных прогонов `doctor` про
ветку, которую тот же заход первым ходом ВЛИВАЛ. `fazy-3-7-novoe-derevo`: измерение поставлено
ПОСЛЕ починки того же объекта — результат замера стёрся бы.
ЦЕНА: каждый раз исполнитель придумывал замену сам и называл её вслух; ни один не подставил молча.
Но это работа, которую аналитик переложил, не заметив.
ВЕРДИКТ: <пусто до закрытия арки>

### 18. 🔴 ДВА ЗАХОДА, СДАННЫЕ В ОДИН ДЕНЬ, ДРУГ ДРУГА НЕ ВИДЯТ — три случая подряд
`porcia-1` × `gruppa-D` (старый `slaid.py` на ветке второго) · `format-kartochki` ×
`provod-illyustracij` (первый вписал lifecycle-блок ДО шапки, второй написал парсер, требующий
`^---`) · `verifikatory` × `provod` (обе взяли `verifikatory/**` в зону).
🔴 И решение «не импортировать чужой модуль, это чужая зона» было **правильным в момент принятия** —
оно стало неверным ровно тогда, когда соседняя ветка сдалась.
ЦЕНА: гейт иллюстраций падал на КАЖДОЙ карточке нового бутстрапа, то есть дефект попадал не в одну
лекцию, а во все будущие. Поймал владелец живой работой, не приёмка.
ВЕРДИКТ: <пусто до закрытия арки>

### 19. Утверждение о файле-заходе, снятое из git, расходится с диском по построению
`gejty-vrut` объявил дефект С5 опровергнутым: прогнал гейт на закоммиченной версии файла-захода.
Но файлы-заходы правит аналитик и НЕ коммитит — это делает приёмка. На диске дефект был.
ЦЕНА: опровержение записано в отчёт как факт; полшага до снятия живого дефекта с очереди. Поймано
приёмкой прогоном его же инструмента на дисковой версии.
ВЕРДИКТ: <пусто до закрытия арки>

### 20. 🔴 Первый же собранный слайд дал 13 правил, которых не было ни в одном документе
Владелец посмотрел на первый слайд, прошедший конвейер насквозь, и назвал тринадцать нарушений:
четыре по математическому языку (приложение читается как сказуемое к неверному подлежащему ·
двусторонняя стрелка без биекции · слово-пересказ рядом с формулой · форма объявления
гомоморфизмов), четыре по построению рассуждения («противоречие» не пункт плана · лишний аргумент ·
🔴 экономить надо ВНИЗУ, а подводку расширять · порядок «объяви, что доказываешь, потом собирай
материал»), четыре по иллюстрациям (вопросительные знаки ЗАПРЕЩЕНЫ · подпись поверх стрелки ·
композиция рисуется ДУГОЙ · белый фон убрать, освободившееся место отдать шрифту) и один по
слайду (нет номера).
ЦЕНА: **исполнитель потратил, по оценке владельца, 100–200 тыс. токенов** на цикл «собрал →
посмотрел → не то → пересобрал» — и всё равно получил тринадцать замечаний, потому что правил
не существовало нигде. Умножается на тринадцать оставшихся слайдов и на каждую будущую лекцию.
Дословное требование владельца: *«должны быть все уроки раз навсегда прописаны, чтобы ему не
приходилось это делать. Весь процесс чтобы получался с первого раза.»*
Все тринадцать записаны дословно в `POMARKI-2026-08-09.md` с адресами домов.
🔴 **Владелец потребовал НЕ откладывать этот урок до закрытия арки:** *«нельзя так делать, их надо
сразу чинить, иначе мы с ними никогда не справимся. В чём был смысл того прогона? Мы же хотели
сразу после него что-то починить.»* — и это само по себе правило: **урок, найденный живым прогоном,
чинится ДО следующего прогона, а не копится.**
ВЕРДИКТ: чинится немедленно заходом `kod_pomarki-v-disciplinu.md` (2026-08-09), не откладывается
до сессии закрытия арки — по прямому требованию владельца.
