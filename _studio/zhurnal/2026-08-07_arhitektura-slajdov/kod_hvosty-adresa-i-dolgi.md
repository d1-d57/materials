# Канал исполнителя — hvosty-adresa-i-dolgi (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — все находки уже сделаны и проверены командами, работа адресная: перенести,
> дописать, переразметить. Изобретать нечего.

КОНТЕКСТ. Фабрика научно-популярных лекций. Ты закрываешь **хвосты**: вещи, найденные и оплаченные
на прошлых заходах, но не доведённые — и потому подхваченные заново, каждая по нескольку раз.

🔴 **Требование владельца, задающее смысл всего захода:** *«все такие вещи нужно сразу решать —
раз и навсегда показать путь. Правильно прописать адрес»*. Ключевое слово — **адрес**. Почти каждый
хвост здесь это не «инструмента нет», а **«инструмент есть, но его негде найти»**.

**Свежий пример, ради которого заход и собран.** Сборка лекции 2 встала: 153 формулы из 168
показывались на слайдах видимым мусором `⟦MISSING-MATH:…⟧`, и исполнитель отчитался, что рендерера
новых формул в репозитории нет. **Рендерер есть.** Проверено моим прогоном:
```
node -v                                                                          → v22.22.3
lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/tools/node_modules/katex   → katex 0.16.9
teorkat-vvedenie/src/tools/katex_kesh.js                                          → 50 строк, рендерит
```
Отрендерил им ту самую формулу — вернулся нормальный `<span class="katex">`. **Сеть не нужна.**
На этом споткнулись подряд ДВА исполнителя, второй — потратив на диагностику полсессии. Первый
жаловался три-четыре раза; ответом было «задача будущей math-арки» в докстринге `harvest_katex.py`.

ЦЕЛЬ: чтобы третий не споткнулся. Адреса — в дисциплине, долги — с честными датами.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко: ты НЕ пишешь код.** Ни `_generator/**`, ни `_illustracii/**`. Заплатку
рендерера прямо сейчас делает ДРУГОЙ заход в соседней папке — ты пишешь **адрес и дисциплину**.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/hvosty-adresa`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/hvosty-adresa`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add hvosty-adresa --branch zahod/hvosty-adresa
  cd ../materials-wt/hvosty-adresa
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять).** Во ВТОРОМ репозитории `~/Documents/GitHub/disciplina`: `skills/slajdy/**`. В `materials`: `teorkat-vvedenie/*.md` — **только файлы верхнего уровня, глубже не спускаться.** Всё вне — **READ-ONLY**.
- 🔴🔴 **ЧУЖАЯ ЗОНА — рядом идёт заход `sborka-l2`, собирает лекцию, не трогать ни байта:** `teorkat-vvedenie/L2/**` · `_generator/sborka/**` · `_generator/skeleton/**` · `_generator/vendor/**`. ⚠ **Именно он делает заплатку рендерера формул** — ты про неё пишешь адрес, а её саму не пишешь. Полез в `_generator/**` — сломал ему работу под руками.
- 🔴 `_generator/tools/**` · `_illustracii/**` · `.githooks/**` — тоже НЕ твоё. Ты в этом заходе **не пишешь ни строки кода вообще**, только `.md`.
- 🔴 **В `disciplina` НЕ ЗАВОДИ ВЕТКУ И НЕ ДЕЛАЙ `checkout`** — одна рабочая папка на всех, там уже лежит чужая работа-в-процессе. Коммить на вычекаученную ветку, явными путями, `git show --stat` проверить.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/hvosty-adresa`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `disciplina/skills/slajdy/**` `teorkat-vvedenie/*.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `disciplina/skills/slajdy/references/math-render.md` | **дом всей математики в дисциплине** — сюда идёт адрес рендерера (Х1) |
| Я2 | `teorkat-vvedenie/src/tools/katex_kesh.js` — **50 строк, читать целиком** | сам рендерер: что делает, чем зовётся, какой формат на выходе |
| Я3 | 🔴 `_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-2026-08-09.md`, **§1–§2 (П1–П8)** | восемь правил математического языка — их предстоит вписать в верификатор фазы 3 (Х2) |
| Я4 | `disciplina/skills/slajdy/verifikatory/faza-3-tekst-slaidov.md` | предмет правки Х2 |
| Я5 | `disciplina/skills/slajdy/DOLG.md`, записи **Д27 · Д28 · Д52 (обе, их две)** | предмет ревизии Х4 |
| Я6 | `teorkat-vvedenie/NAVIGATION.md` · `RAZMETKA-L2.md` · `KONTRAKT-L2.md` · `LENTA-L2.md` — **только шапки и названные строки** | предмет правки Х3 |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — четыре хвоста, все независимы, порядок по убыванию цены

### Х1. 🔴 АДРЕС РЕНДЕРЕРА ФОРМУЛ — в дисциплину, навсегда

**Что известно, всё проверено командами, перепроверять не надо:**

| факт | проверено |
|---|---|
| `node` есть | `node -v` → `v22.22.3` |
| пакет `katex` 0.16.9 лежит в `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/tools/node_modules/katex` | `require()` отработал, формула отрендерилась |
| рендерер новых формул — `teorkat-vvedenie/src/tools/katex_kesh.js` | 50 строк, `output:'htmlAndMathml'` |
| контракт кэша — `<лекция>/math/katex.json`, словарь `{tex → готовый HTML}` | `slaid.py:43`, `formaty.py:239` читают именно его |
| шрифты уже вшиты | `_generator/skeleton/katex.css`: **20 `@font-face`, все `data:font/woff2;base64`** — докачивать нечего |
| 🔴 пакет `katex` **НЕ ОТСЛЕЖИВАЕТСЯ git** | `git ls-files <путь к node_modules/katex>` → **0 файлов** |

🔴 **Последняя строка — самое опасное во всей фабрике сегодня.** Рендеринг формул ВСЕХ лекций
держится на неотслеживаемой папке внутри посторонней лекции. Один `git clean` — и ни одна лекция
больше не соберётся, а выглядеть это будет как «формулы пропали», не как «katex исчез».

**Что делаешь ты (только текст, кода не пишешь):** в `math-render.md` (Я1) — раздел
**«Откуда берётся рендер формул»**, и в нём:
1. **контракт**: `<лекция>/math/katex.json`, `{tex → HTML}`, `output:'htmlAndMathml'` обязателен
   (иначе ломается round-trip `harvest_katex.py`) — с адресами строк-читателей из таблицы выше;
2. **где рендерер и чем зовётся** — сегодняшний путь `katex_kesh.js`;
3. 🔴 **ЦЕЛЕВОЙ адрес, куда он переезжает прямо сейчас:** `_generator/sborka/kesh_formul.*`, а сам
   пакет — `_generator/vendor/katex/katex.min.js` **под git**. Это делает соседний заход `sborka-l2`.
   ⚠ **Пиши целевой адрес как решение, а старый — как «до переезда».** На момент твоего коммита
   переезд может быть ещё не закончен: не проверяй существование `_generator/vendor/` и не
   подстраивайся под то, что видишь на диске;
4. 🔴 **отказ, а не тишина**: `⟦MISSING-MATH:…⟧` на слайде означает «кэш не собран», и это **не
   косметика, а несобранная лекция**. Записать явно — на этом встали ДВА исполнителя подряд;
5. **чего делать НЕ надо** — не искать CDN, не ставить пакеты, не звать playwright: рендер формул
   офлайновый и к playwright отношения не имеет (playwright нужен только замеру типографики).

⚠ **`harvest_katex.py` своим докстрингом («новые формулы — задача будущей math-арки») отправляет
читателя в несуществующее будущее.** Файл в чужой зоне — **строкой в отчёт, не правкой.**

### Х2. 🔴 О47 — ВЕРИФИКАТОР ФАЗЫ 3 НЕ ЗНАЕТ ПРАВИЛ, ПО КОТОРЫМ ЕГО СУДЯТ

Проверено грепом по всей дисциплине: правила П1–П8 знают `math-render.md`, `status-bloka.md`,
`plakatnyj-stil.md` и верификатор фазы **2**. **`verifikatory/faza-3-tekst-slaidov.md` (Я4) не знает
ни одного.** То есть фаза, которая пишет текст на слайд, проверяется тем, кто не слышал ни про
запрет двусторонней стрелки, ни про принцип П7.

Перенести П1–П8 из Я3 §1–§2 **цитатами владельца, не пересказом**, новой клаузой. Форма — как
клаузы 13/15 у фаз 1–2: **читать стандарт СЕЙЧАС из файла-дома, а не по памяти промпта**, и печатать
охват отдельным числом («извлечено N правил / проверено M блоков»).
🔴 **П7 — общий принцип, ради которого записаны П1–П6, и владелец правил его формулировку лично.**
Цитируй итоговую версию из Я3, не первую.

### Х3. УСТАРЕВШИЕ ЧИСЛА СЛАЙДОВ — четыре документа врут

Живая правда: **15**, и она сходится в четырёх местах (`ls -1 teorkat-vvedenie/L2/slajdy/ | wc -l`,
`slide_order` в `brief.md`, разделы `##` в `LENTA-L2/faza3.md`, `gejt_kartochki.py` → «15 из 15»).
Врут (Я6):

```
teorkat-vvedenie/NAVIGATION.md:56-57   «14 слайдов», «гейт зелёный, 14 из 14»
teorkat-vvedenie/RAZMETKA-L2.md:167    «§4. СТРУКТУРА — 14 слайдов, 37 блоков»
teorkat-vvedenie/KONTRAKT-L2.md:21,666 «13 слайдов»
teorkat-vvedenie/LENTA-L2.md:10        «Тринадцать слайдов»
```
Причина: 09.08 слайд «Изоморфизм» отделили от «Базиса». Исполнитель `sborka-l2` на этом уже
споткнулся и решил, что слайд потерян.

🔴 **Чинить не подстановкой «15», а по `KONSTITUCIYA §10`: вместо числа — команда, которая его
печатает.** Особенно `NAVIGATION.md:57`, где рукописное число стоит прямо в строке про гейт — это
и есть запрещённое «я посчитал и вписал». `KONTRAKT-L2.md` — исторический документ решения от
05.08: **не переписывай прошлое**, пометь как «число на дату, живая правда — команда такая-то».
⚠ **И ловушка, из-за которой кажется, что слайд пропал:** папка `fundamentalnaya-gruppa` несёт
`nazvanie: Диск и окружность`. Слайд переименовали, папку нет. Записать там, где смотрят.

### Х4. РЕВИЗИЯ ТРЁХ ДОЛГОВ — даты данных устарели

Правило Р34: **отметка без ДАТЫ ДАННЫХ доверия не заслуживает.** У всех трёх статус снят 2026-08-08,
и мир с тех пор поменялся. Перепроверь **живым грепом**, дату данных поставь сегодняшнюю.

- **Д27** — «нет кэша KaTeX; нет номера слайда». 🔴 Половина про номер слайда **устарела**: `grep -n "lect-number" _generator/skeleton/base.css` → строка 71, номер есть. Половина про кэш формул — **живая, и это ровно Х1**.
- **Д28** — «`kegl_dlya_znakov` не подключён». Статус снят чтением кода 08.08. Перепроверь тем же грепом и запиши, что увидел сегодня.
- **Д52 — их ДВЕ разных с одним номером** (в файле есть предупреждение о коллизии): одна про `konvejer/` (уже МЁРТВ), вторая про масштаб SVG в панели (ЖИВ). ⚠ **Разведи их номера или пометь однозначно** — «Д52 закрыт» уже один раз означало не то, что читали.

🔴 **Не закрывай долг, если не можешь показать команду, доказывающую смерть.** Не смог проверить —
пиши «проверить не удалось, потому что…», это законная запись. Выдуманное закрытие хуже открытого
долга: оно запрещает проверять.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — четыре условия, каждое называет краснеющий артефакт:**
1. 🔴 **Живой прогон на реальном объекте, а не рассуждение:** свежий субагент, не видевший этой
   сессии, получает вопрос *«лекция собирается, формулы показываются как `⟦MISSING-MATH⟧` — что
   делать?»* и **только дисциплину** (без этого захода и без чата). Ответ обязан назвать контракт
   `<лекция>/math/katex.json` и рендерер. Не назвал — Х1 провален, адрес не работает. Вопрос,
   ответ и вердикт — в отчёт дословно.
2. `grep -c "П7\|двусторонняя стрелка" disciplina/skills/slajdy/verifikatory/faza-3-tekst-slaidov.md`
   → **больше 0**; все восемь правил на месте, проверить поимённо П1…П8, охват числом.
3. Ни одного рукописного числа слайдов не осталось: `grep -n "13 слайд\|14 слайд\|Тринадцать слайд" teorkat-vvedenie/*.md`
   → пусто ЛИБО каждое совпадение помечено как историческое с датой. Вывод команды в отчёт.
4. Три долга несут дату данных **сегодняшнюю** и команду рядом с вердиктом. `git_zona.py check --zone` ✅ по обеим зонам.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «хвосты закрыты», а «закрыто X из 4, правил
перенесено N из 8, документов поправлено M из 4».

## 3. ВЕРИФИКАТОР

🔴 **Ты пишешь адреса — а адрес проверяется только тем, что по нему приходят.** «Я написал понятно»
не проверка: оба споткнувшихся исполнителя читали дисциплину и не нашли.

- **Главный верификатор — пункт 1 критерия** (свежий субагент, слепой к сессии, спрашивает как
  живой исполнитель). Ответил мимо — правь текст и **гоняй заново другим субагентом**, не тем же.
- **Х2 сверить по одному:** П1…П8 из Я3 → клауза в Я4, **цитатой, а не пересказом**. Пересказанное
  правило — потеря; за пересказ П7 уже платили кругом правок.
- **Ничего не пропало:** `git show --stat` по обоим коммитам — только твои пути; ни один файл в
  `disciplina` не переписан целиком (ты дописываешь, а не заменяешь).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ

🔴🔴 **ДВА РЕПОЗИТОРИЯ — ДВА КОММИТА, И ВТОРОЙ ЗАБЫВАЮТ.** Подставляй ниже:

| репо | путь | `<зона>` | ветка |
|---|---|---|---|
| `disciplina` | `~/Documents/GitHub/disciplina` | `skills/slajdy` | 🔴 **вычекаученная — НЕ переключай** |
| `materials` | `../materials-wt/hvosty-adresa` | `teorkat-vvedenie` | `zahod/hvosty-adresa` (worktree) |

⚠ **Урок 18, уже три случая: правка в `disciplina`, не закоммиченная тем же ходом, невидима из
`materials`** — и следующий заход честно делает её заново. На прошлой неделе так чуть не потерялась
готовая работа целого захода.
⚠ **В `materials` коммить `teorkat-vvedenie/*.md` явными именами файлов.** Голый glob в разных
оболочках раскрывается по-разному, а рядом лежит `L2/` — чужая зона, в которую нельзя ни байта.
Оба хэша — в отчёт, каждый своей строкой.

---
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `disciplina/skills/slajdy/**` `teorkat-vvedenie/*.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `disciplina/skills/slajdy/**` `teorkat-vvedenie/*.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Критерий готовности не оспариваю — все четыре условия проверяемы командой/живым прогоном. Якоря
Я1–Я6 прочитаны; факты Х1/Х4 перепроверены живым грепом ДО правок (не полагаюсь на таблицы захода
вслепую там, где сам могу проверить дёшево):
- `grep -n "lect-number" _generator/skeleton/base.css` → строка 71, номер слайда есть (Д27/половина).
- `grep -n "kegl_dlya_znakov" _generator/sborka/tipy.py _generator/sborka/slaid.py` → 0 (Д28 жив).
- `git ls-files lsh-2026-perechislitelnaya/.../node_modules/katex` → 0 файлов (Х1, опасность жива).
- `find . -iname konvejer` → `_studio/konvejer` существует; `grep G1\|G16 vhod-gejty.md` → гейты
  названы верно (Д52-konvejer остаётся МЁРТВ).
- `grep -n "svg{width:100%;height:100%}" _generator/skeleton/base.css` → строки 85,134 (номера
  сдвинулись с 62,111 захода — файл вырос, это не расхождение по сути), `preserveAspectRatio`
  нигде не задан; `audit.py` строки ~96-104 ловят ТОЛЬКО overflow (SVG больше панели), не недолив
  (SVG меньше панели) — Д52-svg остаётся ЖИВ.

Порядок — по убыванию цены, как в заходе (Х1→Х2→Х3→Х4), четыре хвоста независимы:

1. **Х1** — `disciplina/skills/slajdy/references/math-render.md`: новый раздел «Откуда берётся
   рендер формул» (контракт `katex.json` с адресами `slaid.py:43`/`formaty.py:239`, текущий адрес
   рендерера `katex_kesh.js`, ЦЕЛЕВОЙ адрес `_generator/sborka/kesh_formul.*`+`vendor/katex/` как
   решение, отказ `⟦MISSING-MATH⟧` = несобранная лекция, запрет CDN/playwright, факт «пакет не
   под git» как красная опасность). TOC файла — дополнить строкой. `harvest_katex.py`
   (чужая зона) — только строка в отчёт, не правка.
2. **Х2** — `disciplina/skills/slajdy/verifikatory/faza-3-tekst-slaidov.md`: новая клауза (форма
   клауз 13/15 фаз 1–2) — читает `math-render.md` (П1–П4) и `status-bloka.md`/`plakatnyj-stil.md`
   (П5–П8) ЖИВЬЁМ, цитаты владельца из Я3 дословно (не пересказ, особенно П7 — итоговая
   формулировка, не черновая), строка ОХВАТ в шаблон `## Отчёт`.
3. **Х3** — четыре файла `teorkat-vvedenie/*.md` (только верхний уровень):
   `NAVIGATION.md:56-57` и `RAZMETKA-L2.md:167` и `LENTA-L2.md:10` — хардкод числа слайдов
   заменить на команду (`KONSTITUCIYA §10`), не на «15». `KONTRAKT-L2.md:21,666` — НЕ переписывать
   (исторический документ решения 05.08): пометить рядом «число на дату, живая правда — команда».
   Ловушку `fundamentalnaya-gruppa` / `nazvanie: Диск и окружность` — записать в `NAVIGATION.md`
   рядом со строкой `L2/`, куда смотрят при навигации по слайду.
4. **Х4** — `disciplina/skills/slajdy/DOLG.md`, записи Д27/Д28/Д52(×2): дата данных → сегодня
   (2026-08-08, факты перепроверены живым грепом ВЫШЕ в этом плане, не полагаюсь на старую отметку
   той же даты — прогон свежий), Д27 разбит на «номер слайда — закрыт» / «кэш формул — жив, это
   Х1», Д52 разведены по номеру или помечены однозначно (`Д52-konvejer` / `Д52-svg`).
5. Верификация — по `## 3. ВЕРИФИКАТОР`: главный тест — свежий субагент без контекста сессии и без
   файла-захода получает только вопрос про `⟦MISSING-MATH⟧` и дисциплину; П1–П8 сверяю цитатой
   один-в-один; `git show --stat` на оба коммита.
6. Коммит по `## 4` — два репозитория, два хода `add`+`commit -- <пути>` каждый,
   `git_zona.py check --zone` на обеих зонах.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + зачем.** Четыре хвоста закрыты правкой (не кодом — только `.md`, как требовал заход).

- **Х1.** `disciplina/skills/slajdy/references/math-render.md` — новый раздел «Откуда берётся
  рендер формул (офлайн-кэш конвейера фабрики)»: контракт `<лекция>/math/katex.json`
  (`{tex→HTML}`, `output:'htmlAndMathml'`, читатели `slaid.py:43`/`formaty.py:239`), сегодняшний
  адрес рендерера `teorkat-vvedenie/src/tools/katex_kesh.js`, ЦЕЛЕВОЙ адрес
  `_generator/sborka/kesh_formul.*`+`vendor/katex/` как решение (не факт с диска — переезд делает
  сосед `sborka-l2`), явный отказ `⟦MISSING-MATH⟧` = несобранная лекция, запрет CDN/playwright,
  предупреждение про неотслеживаемый git-пакет. TOC файла дополнен строкой. `harvest_katex.py`
  (чужая зона) НЕ трогал — его вводящий в заблуждение докстринг только назван ниже, в «что НЕ
  трогал».
- **Х2.** `disciplina/skills/slajdy/verifikatory/faza-3-tekst-slaidov.md` — новая клауза «🔴 6.
  Клауза из ЖИВОГО стандарта — язык формул и регистр рассуждения (П1–П8)», форма клауз 13/15 фаз
  1–2: инструкция читать `math-render.md` (П1–П4) и `status-bloka.md`/`plakatnyj-stil.md` (П5–П8)
  живьём, все восемь правил процитированы дословно из `POMARKI-2026-08-09.md §1–§2` (П7 — итоговая
  формулировка владельца, не черновая «экономить внизу»), строка ОХВАТ добавлена в шаблон
  `## Отчёт` верификатора.
- **Х3.** `NAVIGATION.md:56-57`, `RAZMETKA-L2.md:167`, `LENTA-L2.md:10` — хардкод «13/14 слайдов»
  заменён на команду `ls -1 teorkat-vvedenie/L2/slajdy/ | wc -l` (`KONSTITUCIYA §10`), не на «15».
  `KONTRAKT-L2.md:21,668,671,673` — числа НЕ переписаны (исторический документ решения 05.08),
  рядом добавлены две пометки «число на дату, живая правда — команда». В `NAVIGATION.md` записана
  ловушка: папка `L2/slajdy/fundamentalnaya-gruppa/` несёт `nazvanie: Диск и окружность` — слайд
  переименовали, папку нет.
- **Х4.** `disciplina/skills/slajdy/DOLG.md`: Д27 разбит на «номер слайда — ЗАКРЫТ» (прежняя
  проверка искала `nomer`, а не `lect-number`, и промахнулась) и «кэш формул — ЖИВ, это Х1»; Д28
  перепроверен тем же грепом, статус не изменился; коллизия номеров Д52 разведена суффиксами
  `Д52-вх` (konvejer, МЁРТВ) / `Д52-svg` (панель, ЖИВ) — теми же, которыми их уже звали ссылки-
  потребители (`З1`, `З3`), просто сами строки таблицы этих суффиксов не несли. У всех — дата
  данных 2026-08-08, перепроверено этим заходом позже того же дня, живой командой рядом с
  вердиктом.

**Как проверил.**
- Живым грепом ДО правок (см. `## ПЛАН`): `lect-number:71` есть, `kegl_dlya_znakov` — 0 вызовов,
  `katex`-пакет — 0 файлов под git, `konvejer/` существует и гейты названы верно, SVG-паттерн без
  `preserveAspectRatio` жив (строки сдвинулись 62,111→85,134 — файл вырос, не расхождение по сути),
  `audit.py` действительно ловит только overflow, не недолив.
- **Главный верификатор (критерий 1 дословно).** Свежий субагент (`general-purpose`, без контекста
  этой сессии и без файла-захода) получил ТОЛЬКО вопрос и путь `disciplina/skills/slajdy/`:
  *Вопрос:* «Лекция собирается, формулы на слайдах показываются как ⟦MISSING-MATH:…⟧ вместо
  нормального рендера. Что делать? Откуда берётся рендер формул, куда смотреть, что запускать?»
  *Ответ (дословно):* «Нашёл прямой ответ — файл `references/math-render.md`, раздел „Откуда
  берётся рендер формул (офлайн-кэш конвейера фабрики)“ (строки 28–66)… **1. Контракт кэша.**
  `<лекция>/math/katex.json`… **2. Чем рендерится.** Сегодня — `teorkat-vvedenie/src/tools/
  katex_kesh.js`… ⚠ Целевой адрес переезда — `_generator/sborka/kesh_formul.*`… **3. Что означает
  плейсхолдер.** `⟦MISSING-MATH:…⟧` — это НЕ косметика: значит «кэш `katex.json` не собран»…»
  *Вердикт:* Х1 пройден — контракт и рендерер названы верно, отказ не спутан с косметикой. Бонус:
  субагент сам нашёл в `DOLG.md` дополнительное предупреждение про ложно-зелёный линтер сборки
  (`Б2`) и процитировал его — доказывает, что раздел интегрируется с остальной дисциплиной, а не
  висит изолированно.
- Критерий 2: `grep -c "П7\|двусторонняя стрелка" disciplina/skills/slajdy/verifikatory/faza-3-tekst-slaidov.md`
  → **2** (>0). П1…П8 сверены цитатой один-в-один с `POMARKI-2026-08-09.md` (grep подтвердил
  дословные фразы «я не понимаю», «так не обозначаются», «точно не нужен пункт противоречия»,
  «гораздо важнее описать понятие» — все на месте).
- Критерий 3: `grep -n "13 слайд\|14 слайд\|Тринадцать слайд" teorkat-vvedenie/*.md` → только
  `KONTRAKT-L2.md` (строки 21,26,671,673), каждое либо помечено историческим снимком с датой и
  командой, либо лежит внутри уже помеченного абзаца. `NAVIGATION.md`/`RAZMETKA-L2.md`/
  `LENTA-L2.md` — пусто.
- Критерий 4: Д27/Д28/Д52-вх/Д52-svg несут дату данных 2026-08-08 и команду рядом с вердиктом
  (см. «Как проверил» выше). `git_zona.py check --zone teorkat-vvedenie` → ✅ (см. «Коммит»). Для
  зоны `disciplina` инструмент неприменим — он читает состояние ТОЛЬКО репозитория `materials/`
  (`git_zona.py --help`: «Вся работа с git в `materials/` — через этот файл»); эквивалент для
  `disciplina` — `git status --porcelain` (пусто после коммита) и `git show --stat` (только мои
  три пути) — оба выполнены, см. «Коммит».
- `git --no-optional-locks show --stat -1` на обоих коммитах — только пути своей зоны, ничего
  чужого не подмешалось.

**Что НЕ трогал.**
- `_generator/**`, `_illustracii/**`, `.githooks/**`, `teorkat-vvedenie/L2/**` — не открывал на
  запись ни разу (только чтение для перепроверки фактов заранее объявленным грепом).
- `harvest_katex.py` — не правил (чужая зона). Его докстринг («новые формулы — задача будущей
  math-арки») по-прежнему отправляет читателя в несуществующее будущее — находка захода, не моя,
  просто подтверждаю, что не тронул файл и что проблема остаётся открытой для владельца файла.
- Новых `.md` не заводил — `register_doc.py` не требовался.
- `_generator/vendor/`, `_generator/sborka/kesh_formul.*` — не проверял на диске и не подстраивал
  под них текст (заход прямо запретил): целевой адрес в `math-render.md` написан как решение.

**Вопросы.** Нет — заход самодостаточен, ни один якорь не потребовал уточнения у владельца.

**Результат верификатора.** Все четыре условия критерия готовности выполнены (детали — «Как
проверил» выше): 1 — живой прогон прошёл, адрес найден и понят верно; 2 — П7/стрелка на месте
(2>0); 3 — рукописных чисел не осталось вне историч. пометок; 4 — даты данных сегодняшние с
командой, обе зоны чисты.

**Открытое «возвращаться».** Нет — все четыре хвоста закрыты в рамках зоны этого захода.

**Время прогона + токены:** неприменимо (канал `app`, снимать неоткуда — см. правило `## 5`).

**ПОВТОРЯЕМОСТЬ находок.** Ни одна из четырёх находок сама по себе не повторится на следующей
лекции/слайде/заходе: все адреса и клаузы теперь лежат в `skills/slajdy/`, которую читает КАЖДЫЙ
будущий заход и верификатор фазы 3, не только этот, и это подтверждено живым слепым прогоном
(критерий 1). Единственный остаточный риск повторения — НЕ находка этой сессии, а уже названный
факт вне моей зоны: пакет `katex` не под git до завершения переезда соседа `sborka-l2` (Х1,
раздел «целевой адрес»); если `git clean` случится раньше переезда — формулы сломаются на
СЛЕДУЮЩЕЙ лекции тоже, не только на текущей. Это не новый пункт очереди — работа над этим уже
идёт в соседнем заходе, я только фиксирую, что риск реальный и относится не только к сегодняшнему
дню.

**НЕОБРАТИМОЕ.** Необратимого нет: оба коммита — предписанная заходом (§4) сдача зоны, не
самовольное действие; ничего не удалено, не перезаписано, не переименовано и не сдвинуто за
пределы зоны.

**АРТЕФАКТ:** артефакта нет: заход — текстовый (только `.md`-документация и дисциплина), кода не
писал и ничего не собирал по требованию захода («ты НЕ пишешь код», `## КОНТРАКТ ЗОНЫ`).
**КОММИТ:**
- `disciplina` (зона `skills/slajdy`, ветка `zahod/format-kartochki-faza-1`, уже вычекаученная —
  не переключал): `d50beb5` — «skills/slajdy: адрес рендерера формул (Х1) + клауза П1-П8 в
  верификаторе фазы 3 (Х2) + ревизия Д27/Д28/Д52 (Х4)» · `git show --stat` → только
  `DOLG.md`/`math-render.md`/`faza-3-tekst-slaidov.md`.
- `materials` (зона `teorkat-vvedenie`, worktree `../materials-wt/hvosty-adresa`, ветка
  `zahod/hvosty-adresa`): `a82a5b8` — «teorkat-vvedenie: числа слайдов — команда вместо хардкода,
  ловушка fundamentalnaya-gruppa (Х3)» · `git_zona.py check --zone teorkat-vvedenie` → ✅ «работа
  доехала в git, вне git ничего нет».
