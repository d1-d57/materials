# Канал исполнителя — verifikatory-novoe-derevo (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — работа переводческая и измерительная: четыре однотипных файла переводятся на
> известное дерево, образец уже сделан для двух других. Субагенты: до 4 читателей по одному на
> файл, если решишь делить — назови в `## ПЛАН`.
> 🔴 **Спецификация — в §2 целиком, ссылок наружу за ней нет.**

КОНТЕКСТ. Фабрика научно-популярных лекций, семь фаз. У каждой фазы есть **верификатор** — промпт в
`disciplina/skills/slajdy/verifikatory/faza-N-*.md`, который читает артефакты фазы и выносит вердикт:
сошлось или нет. Это второй слой проверки, там, где скриптовый гейт ловит форму, а не смысл.

🔴 **Проблема, ради которой заход написан: четыре из семи верификаторов целятся в дерево лекции,
которого больше нет.** Решением Р-А 2026-08-08 правым объявлено новое дерево
`<лекция>/{brief.md · slajdy/<имя>/slaid.md · illustracii/<имя>/}`; старое
(`<лекция>/src/{content,slides}`) принадлежит генератору, который переписывается начисто.
Замер аналитика прямо перед сборкой захода:

| файл | вхождений старого дерева |
|---|---|
| `faza-1-intervyu.md` · `faza-2-raskadrovka.md` | **0** — уже переведены заходом `fazy-1-2-dovodka`, **образец для тебя** |
| `faza-3-tekst-slaidov.md` | **4** |
| `faza-4-verstka.md` | **3** |
| `faza-6-sborka-qa.md` | **2** |
| `faza-7-sceny.md` | **3** |

**ЦЕНА, и владелец сформулировал её сам:** верификатор, натравленный на несуществующие файлы,
не падает — он **выносит вердикт**. Вердикт по пустоте выглядит как вердикт по работе, и ему верят,
потому что ради этого его и заводили. Дословно владелец: *«мы же их выстраивали, они должны хорошие
вердикты выносить, в этом их соль»*. Плюс долг Д14: **пять промптов ни разу не прогонялись на живом
материале** — у фазы 3 при первом же прогоне выяснилось, что промпт ждал формат входа, которого в
реальности не существует, то есть он физически не мог запуститься.

ЦЕЛЬ: чтобы вердикт верификатора относился к тому, что реально лежит на диске.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее:** ты **не пишешь новых верификаторов** и не изобретаешь клауз.
Ты переводишь существующие на новое дерево и проверяешь, что они запускаются. Показалось, что
клауза бессмысленна, — **строка в отчёт, не удаление**: каждая из них оплачена инцидентом.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/verifikatory-novoe-derevo`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/verifikatory-novoe-derevo`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add verifikatory-novoe-derevo --branch zahod/verifikatory-novoe-derevo
  cd ../materials-wt/verifikatory-novoe-derevo
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/fixtures/verifikatory/**` в `materials`, и во ВТОРОМ репозитории — **РОВНО ЧЕТЫРЕ ФАЙЛА, поимённо:** `~/Documents/GitHub/disciplina/skills/slajdy/verifikatory/faza-3-tekst-slaidov.md` · `faza-4-verstka.md` · `faza-6-sborka-qa.md` · `faza-7-sceny.md`. Всё вне — **READ-ONLY**.
- 🔴🔴 **`verifikatory/faza-5-illyustracii.md` — НЕ ТВОЙ, не открывай на запись.** Рядом прямо сейчас идёт заход `provod-illyustracij`, у него в зоне стоит `verifikatory/**` целиком, и фаза 5 — его. У него 3 вхождения старого дерева, и они **останутся** — это не твоя недоработка, назови её строкой в отчёте. Увидел в `git status` незакоммиченные правки в `faza-5` — **не подметай их своим коммитом**.
- 🔴🔴 **В `disciplina` НЕ ЗАВОДИ ВЕТКУ И НЕ ДЕЛАЙ `checkout`.** Там **одна рабочая папка на всех**, и в ней прямо сейчас работают два соседних захода. `git checkout -b` переключит ветку у них под ногами, следа в git не останется. Коммить на ту ветку, что уже вычекаучена, **строго явными путями** (свои четыре файла), и проверить `git show --stat`: только они.
- 🔴 **`teorkat-vvedenie/**` — READ-ONLY жёстко:** там живая лекция и идущее интервью с владельцем.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/verifikatory-novoe-derevo`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/fixtures/verifikatory/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.** Спецификация — в §2.

| # | файл | зачем |
|---|---|---|
| Я1 | `disciplina/skills/slajdy/verifikatory/faza-1-intervyu.md` и `faza-2-raskadrovka.md` — **READ-ONLY** | 🔴 **ОБРАЗЕЦ. Их уже перевели.** Смотри, как там сделаны плейсхолдер `{{ЛЕКЦИЯ}}`, форма отчёта с ОХВАТОМ («проверено X клауз из N» + список непроверенного с причиной) — и повтори, не изобретай |
| Я2 | четыре твоих файла: `faza-3-tekst-slaidov.md` · `faza-4-verstka.md` · `faza-6-sborka-qa.md` · `faza-7-sceny.md` | предмет правки |
| Я3 | `disciplina/skills/slajdy/references/kartochka-slajda.md` — **READ-ONLY, чужая зона** | куда что реально кладётся в новом дереве |
| Я4 | `_generator/sborka/bootstrap_lekcii.py --help` и порождённая им карточка | 🔴 самый честный источник: **что физически существует на диске.** В карточке сверху блок «ЧТО ДАЛЬШЕ С ЭТИМ ФАЙЛОМ» — там команды всех фаз |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — три хода

### В1. ПЕРЕВЕСТИ ЧЕТЫРЕ ПРОМПТА НА НОВОЕ ДЕРЕВО

Каждое упоминание старого дерева заменить на адрес, который **реально существует**. Проверка —
греп, а не память:
```
grep -nE "src/slides|src/content|src/illustrations|KONTRAKT-|TZ-" \
  ~/Documents/GitHub/disciplina/skills/slajdy/verifikatory/faza-{3,4,6,7}-*.md
```
Соответствие берётся из Я3 и Я4, а не выдумывается. Плейсхолдер и форму отчёта — как в Я1.

🔴 **Клаузы сохраняются ПОШТУЧНО.** Перевод адреса — не повод сократить проверку. Клауза, которая
после перевода стала неисполнимой (артефакта нет ни в каком виде), **не удаляется**: помечается
явно «не проверяется, потому что …» и попадает в отчёт списком. *Так уже сделали фазы 1–2, там же
клаузы даже ПОПОЛНИЛИ теми, что скилл поручал верификатору, а промпт не проверял.*

**КРИТЕРИЙ В1:** греп выше даёт **0** по всем четырём файлам; число клауз до и после названо по
каждому файлу; список «стало неисполнимым» — с причиной на каждую.

### В2. 🔴 ХОТЯ БЫ ОДИН ПРОГНАТЬ НА ЖИВОМ МАТЕРИАЛЕ — это главное, а не перевод

Долг Д14 дословно: **пять промптов ни разу не прогонялись на живом материале.** У фазы 3 при первом
же прогоне выяснилось, что промпт ждал формат входа, которого не существует — то есть он физически
не мог запуститься. Перевести адреса и не проверить запуск значит повторить ровно это.

Собрать **лабораторную лекцию** (`bootstrap_lekcii.py` в `/tmp`, три слайда, заполнить руками) и
прогнать на ней верификатор **фазы 4** — он ближе всех к готовому артефакту. Прогон — субагентом с
промптом из файла, как он и задуман.

**КРИТЕРИЙ В2:** верификатор **запустился и выдал вердикт по существу** — то есть его замечания
относятся к тому, что реально лежит в лабораторной лекции. Вердикт «всё хорошо» на заведомо
кривой лекции — **провал критерия**, а не успех: подсунь ему лекцию с явным дефектом (например
слайд без центрального блока) и покажи, что он это назвал.
Не запустился — **это тоже результат и его надо предъявить**: значит долг Д14 подтверждён на
втором промпте из пяти, и это ценнее молчаливого «перевёл».

### В3. ЧТО ОСТАЛОСЬ НЕПРОВЕРЕННЫМ — назвать числом

Три из пяти промптов ты не прогоняешь (нет времени и нет материала). Напиши прямо: **прогнан 1 из
4, переведено 4 из 4**, и какие именно остались без живого прогона.

---

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «верификаторы починены», а «переведено 4 из 4,
прогнан 1 из 4, клауз сохранено N из N, неисполнимых K».

## 3. ВЕРИФИКАТОР

Операция лоссовая по смыслу: ты правишь текст, каждая строка которого оплачена инцидентом.
Потерянная клауза не заметна — верификатор просто перестанет её проверять и будет молчать.

Гейт: **свежий субагент берёт исходные версии четырёх файлов (`git show HEAD:<путь>`) и сверяет
список клауз до и после.** Список потерь обязан быть **ПУСТ**; клауза, помеченная «не
проверяется», потерей не считается, но обязана быть в отчёте поимённо. Непустой список — это
регрессия, а не «упрощение».

⚠ Живые лекции и `teorkat-vvedenie/**` — READ-ONLY. Лабораторная лекция В2 — только в `/tmp`.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/fixtures/verifikatory/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/fixtures/verifikatory/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Worktree заведён (`zahod/verifikatory-novoe-derevo`, HEAD подтверждён). Якоря Я1–Я4 прочитаны:
faza-1/faza-2 (образец), мои четыре файла, `kartochka-slajda.md`, `bootstrap_lekcii.py --help` +
живая лабораторная карточка в `/tmp/lab-lekcija` (3 слайда, пустая шапка + `INTERVYU.md`, без тел
карточек — тела пишутся руками по формату Я3, это и есть В2).

**Разведка вместо памяти:** прогнал ровно греп из §2 по всем четырём файлам ДО правки. Совпадения
есть только в трёх местах каждого файла — (а) таблица «было/читать как» в шапке (строки ~6–17,
она сама физически содержит старые адреса и потому не может остаться как есть), (б) строка
подстановки плейсхолдеров перед текстом субагента, (в) короткий кусок `## Что проверяешь` в теле.
KONTRAKT-/TZ- — 0 совпадений во всех четырёх (это, видимо, метка для fazy-1-2, не для этих файлов).
Дальше в теле (Мандат чтения, Слепая проверка, Чек-лист, Полномочия, Петля, Отчёт) старых адресов
нет вовсе — они ссылаются на файлы дисциплины (`references/...`), те не переехали.

**Решение по форме перевода** — как у faza-1/2, не таблицей: таблица «было — читать как» сама
состоит из строк со старыми путями и грепом не пройдёт никогда, будь она хоть трижды правильной.
Заменяю её на прозу без буквальных старых путей (историческая справка словами, как уже сделано в
Я1/Я2), плейсхолдеры и `## Что проверяешь` — на реальные новые адреса. Судящую часть (чек-листы,
слепые проверки, мандаты, петли, форму отчёта) не трогаю — только адрес.

**Клауза `{{ЭТАЛОН}}` фазы 6** (мандат п.3, пример — `dandelin/src`/`buffon/src`) — это **старое
дерево живым текстом**, эталон нужно взять из нового, а такого прошедшего QA деку я не знаю
(Я1–Я4 о нём не говорят, выдумывать нельзя) → помечаю как «не проверялось: эталона нового дерева
нет» вместо подстановки старого пути, это и есть тот случай из §92 захода («стала неисполнима —
не удаляется, помечается»).

**{{SHABLON}} фазы 4** (per-slide grid-CSS) — по таблице (строка 5) в новом дереве отдельного
per-лекционного шаблон-файла нет вовсе, общий канон переехал в `_generator/skeleton/`; grid-CSS
слайда теперь сидит прямо в выходе компилятора. Не завожу новый плейсхолдер под несуществующий
файл — читаю из того же `dist/index.html`, что и `{{SLIDES}}` (это выход компилятора и по старой
табличной строке 2 тоже).

**Ход:**
1. Перевожу все четыре файла сам (Edit), без субагентов-читателей — файлы у меня уже полностью
   прочитаны, а перевод точечный (три места на файл), делить дороже, чем сделать.
2. Греп из §2 по всем четырём → должен дать 0.
3. Заполняю лабораторную лекцию в `/tmp/lab-lekcija` руками: у двух слайдов — здоровое тело
   (шапка + `## Математика — развёрнуто` + `## Текст слайда — сжато`, блоки по Я3 §3), у третьего —
   явный дефект «нет центрального блока» (пустой `## Текст слайда — сжато` или блок без мысли),
   как требует В2.
4. Прогоняю верификатор фазы 4 (мой переведённый текст) СВЕЖИМ субагентом на этой лекции — задача
   субагента получает дословный текст промпта, {{ЛЕКЦИЯ}}=`/tmp/lab-lekcija`. Смотрю на вердикт:
   ловит ли дефект по существу.
5. Гейт §3: СВЕЖИЙ субагент без моего контекста берёт `git show HEAD:<путь>` для всех четырёх
   файлов + мои новые версии, сверяет список клауз до/после, ищет пропажи.
6. Коммичу зону (`_generator/tools/fixtures/verifikatory/**` — в ней сегодня, видимо, нечего
   класть: заход не просил заводить фикстуры, только лабораторную лекцию в `/tmp`; если так и
   останется — назову строкой в отчёте, а не завожу фикстуру из воздуха) + четыре файла disciplina
   явными путями.
7. Отчёт с охватом.

Не завожу новых `.md` вне `/tmp` — регистрировать нечего.


## ВОПРОСЫ — (заполняет исполнитель)

1. Живой прогон верификатора фазы 4 на лабораторной лекции нашёл настоящий баг общего канона (не
   моей зоны, не лабораторной лекции): `--accent`/`--marker` не объявлены в `:root` шаблона —
   `.acc{color:var(--ink)}` всюду падает на серый вместо охры, во всём деке нет ни одного цветного
   акцента. Это ровно регресс, задокументированный в `style-core.md` («deck built before it
   existed had every `var(--accent)` fall back to `#333`»), — похоже, воспроизведённый заново в
   `_generator/skeleton/`. Не чинил — вне зоны.
   ДОМ: `_generator/skeleton/` (владелец решает, где заводить)
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Перевёл четыре промпта-верификатора (`faza-3-tekst-slaidov.md` ·
`faza-4-verstka.md` · `faza-6-sborka-qa.md` · `faza-7-sceny.md`) на дерево `<лекция>/{brief.md ·
slajdy/<имя>/slaid.md · illustracii/<имя>/}`: в каждом файле правка касалась ровно трёх мест
(шапка-таблица «было/читать как» → проза без буквальных старых путей, строка подстановки
плейсхолдеров, кусок `## Что проверяешь`); судящую часть (чек-листы, мандаты чтения, слепые
проверки, словарь запретов, полномочия, петля, форма отчёта) не трогал. Затем прогнал переведённый
промпт фазы 4 на лабораторной лекции — это и есть В2, «главное, а не перевод».

**ОХВАТ:** переведено 4 из 4 · прогнан на живом материале 1 из 4 (фаза 4) · клауз сохранено 85 из
85 по всем четырём файлам (чек-лист 5+8+7+8=28, мандат чтения 5+5+3+4=17, словарь запретов
(только у фазы 3) 7, «три клаузы против SKILL.md» 4×4=16, таблица адресов 7×4=28 строк
соответствия — все проверены свежим субагентом-гейтом, потерь 0) · неисполнимых клауз явно
помечено 2: (а) фаза 3 — запасной путь «черновик без `####`-разметки», снят осознанно (в новом
дереве статус блока обязателен с рождения карточки, разметки без статуса не бывает); (б) фаза 6 —
клауза «прогнать по живому эталону `dandelin/src`/`buffon/src`» переведена в «не проверялось:
эталон отсутствует» (эталона нового дерева сегодня нет ни одного, выдумывать нельзя).

**Как проверил.**
1. Греп из §2 (`src/slides|src/content|src/illustrations|KONTRAKT-|TZ-`) по всем четырём файлам
   ПОСЛЕ правки → 0 совпадений (до правки — 5-6 совпадений на файл, все в шапке).
2. Свежий субагент без моего контекста сверил `git show HEAD:<путь>` (до) с диском (после) по всем
   четырём файлам, посчитал клаузы всех категорий (не только чек-лист) — «СПИСОК ПОТЕРЬ ПУСТ ПО
   ВСЕМ ЧЕТЫРЁМ».
3. В2: собрал лабораторную лекцию `/tmp/lab-lekcija` (`bootstrap_lekcii.py --slajdy
   vvedenie,opredelenie,itog`), заполнил три карточки руками по формату Я3 (§2-3) — два здоровых
   слайда, третий («itog») с намеренным дефектом: текст раздела «Текст слайда — сжато» в 6 раз
   выше `byudzhet_slov` (125 слов при бюджете 20), одним слитным абзацем. Собрал компилятором
   `_generator/sborka/deck.py` (**он уже существует и работает на новом дереве** — важная находка:
   старый `_generator/build_deck.py` действительно старый, ждёт `shablon.html`/`tokens.css`/`src/`,
   но `deck.py` — новый, ждёт ровно `brief.md`+`slajdy/`+`illustracii/`), отрендерил
   `_generator/render.py` → 3 PNG. Свежий субагент прогнал переведённый промпт фазы 4 на этом
   материале.

**Результат верификатора фазы 4 (В2 — критерий выполнен, не рубим печать «всё хорошо»):** нашёл
дефект по существу — overflow на слайде `itog`, ~230-290px / 4-5 строк не показаны, верно назвал
причину («не гэп/отступ/грид/фиттер — текст фазы 3 в 6 раз выше бюджета»), отличил «текст не влез»
от «вёрстка сломана» (правило из «Полномочий»: текст не переписывать). Вердикт: гейт выхода закрыт.
Заодно — не как ответ на подсунутый дефект, а самостоятельно — нашёл реальный баг общего канона
(`--accent`/`--marker` не объявлены, акцентный цвет нигде не проявляется); это вне моей зоны,
вынесено в `## ВОПРОСЫ`.

**Что НЕ трогал:** `faza-5-illyustracii.md` (чужая зона, заход `provod-illyustracij`) —
не открывал на запись; `teorkat-vvedenie/**` — не открывал вовсе; старый `_generator/build_deck.py`
— не редактировал, только прочитал `--help`, чтобы отличить старый генератор от нового `deck.py`;
`_generator/tools/fixtures/verifikatory/**` в `materials` — не заводил ничего: заход не требовал
фикстуры, лабораторная лекция сделана в `/tmp` по прямому указанию §3 захода («Лабораторная лекция
В2 — только в `/tmp`»), поэтому в зоне `materials` коммитить нечего (`git status --short` в
worktree пуст и до, и после).

**Вопросы:** один пункт очереди — баг `--accent`/`--marker` общего канона, см. `## ВОПРОСЫ`.

**Возвращаться:** три из пяти промптов ни разу не прогонялись на живом материале — переведены, но
не проверены запуском: фаза 3 (текст), фаза 6 (сборка-QA), фаза 7 (сцены); фаза 5 — не моя зона
вовсе (см. контракт). Долг Д14 закрыт частично: 1 промпт из 5 подтверждённо запускается и судит по
существу (фаза 4), 4 остаются неподтверждёнными живым прогоном.

**Необратимое:** необратимого нет.

**Время прогона + токены:** снимает приёмка из лога прогона (см. §5 захода).

**АРТЕФАКТ:** `/tmp/lab-lekcija/dist/index.html` — собранный лабораторный дек (3 слайда,
намеренно кривой) — открывать браузером; рендер-кадры рядом в `/tmp/lab-lekcija/_render/00.png ·
01.png · 02.png` (02.png — слайд `itog` с видимым overflow, тем самым дефектом, который поймал
верификатор). Это не проектный артефакт, а лабораторный материал В2 — по заходу ему и положено
жить только в `/tmp`.
**КОММИТ:** `7ce09b1` — «verifikatory: перевод фаз 3/4/6/7 на новое дерево лекции» (репозиторий
`disciplina`, ветка `zahod/format-kartochki-faza-1` — чужая, уже вычекаученная, коммитил явными
путями строго по своим четырём файлам, `git show --stat HEAD` подтверждает: только они) ·
`git_zona.py check --zone verifikatory-novoe-derevo` (репозиторий `materials`) → ✅ «работа доехала
в git, вне git ничего нет» — в зоне `materials` коммита нет, потому что нечего было коммитить
(см. «Что НЕ трогал» выше).
