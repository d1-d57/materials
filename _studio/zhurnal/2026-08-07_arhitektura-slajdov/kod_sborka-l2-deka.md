# Канал исполнителя — sborka-l2-deka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — главная задача не механическая: кэш формул для новой лекции никто никогда не собирал, и как его получить — решение по ходу. Плюс доводка вёрстки по промеру, а не по инструкции.

КОНТЕКСТ. Лекция 2 курса «Введение в теорию категорий», среда — эфир. Содержание, блоки и тексты
слайдов **приняты владельцем** после трёх кругов разбора: *«с текстом, с блоками, со стилистикой, с
порядком идей — почти всё идеально»*. Провалилась **только сборка**.

🔴 **Дека сейчас негодна, и причины установлены точно — не гадай, они ниже.** Аналитик собрал её из
песочницы Cowork, где нет браузера, и получил три дефекта разом. Твоя задача — собрать деку, которую
можно показать залу.

ЦЕЛЬ: `teorkat-vvedenie/L2/dist/index.html` открывается и читается: формулы отрисованы, кегли
подобраны, ни один слайд не обрезан.
Приёмка — по ОТЧЁТУ, без построчной сверки.
Если стоп до цели: **скажи это громко и сразу.** До эфира четверо суток, и «почти собрал» здесь
хуже, чем «не смог, вот на чём встал».

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/sborka-l2-deka`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/sborka-l2-deka`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add sborka-l2-deka --branch zahod/sborka-l2-deka
  cd ../materials-wt/sborka-l2-deka
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `teorkat-vvedenie/L2/**`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴🔴 **СТАРТОВАТЬ ТОЛЬКО ПОСЛЕ СВЕДЕНИЯ ВЕТОК. Проверь ПЕРВЫМ ХОДОМ, до всего:**
  ```
  git --no-optional-locks branch --no-merged arka/mat-kostyak     # обязано быть ПУСТО
  ls _generator/sborka/kesh_formul.js _generator/vendor/katex/katex.min.js
  ls _generator/sborka/smeta.py _generator/tools/gejt_verstki.py
  ```
  **Хоть чего-то нет — СТОП, не работай, скажи владельцу.** Вся вчерашняя работа (кэш формул, смета
  вмещения, гейт вёрстки) лежала по невлитым веткам; заход `svedenie-arki` их сводит. Стартуешь
  раньше — не увидишь ничего из этого и переизобретёшь заново. **Это уже случилось дважды за сутки.**
- 🔴 **`_generator/**` — READ-ONLY.** Ты этими инструментами ПОЛЬЗУЕШЬСЯ, но их не правишь. ⚠ **Известный дефект, о который ты споткнёшься:** `deck.py` **без** `--bez-podbora` падает `SystemExit`, если солвер не может уложить хотя бы один слайд, — теряя всю деку ради одного. Это не твоя поломка: зафиксируй, назови слайд, собери остальное и **скажи в отчёте**. Чинить генератор — не твоя зона.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/sborka-l2-deka`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `teorkat-vvedenie/L2`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Проверь среду ПЕРВЫМ делом — от этого зависит, выполним ли заход вообще:**
  ```
  python3 -c "import playwright; print('playwright есть')" ; echo $?
  python3 -c "from playwright.sync_api import sync_playwright
  with sync_playwright() as p: b=p.chromium.launch(); print('браузер запускается'); b.close()"
  ```
  Браузер не запускается — **СТОП, напиши в `## ВОПРОСЫ` и остановись.** Без него не работают ни
  подбор типографики, ни гейт вместимости, то есть две трети задачи. Ставится так:
  `pip install playwright && playwright install chromium`.

- **Прочитать ТОЛЬКО это, в таком порядке:**

| # | файл | зачем |
|---|---|---|
| Я1 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/PRAVKI-SBORKI-L2.md` **целиком** | 🔴 разбор деки владельцем: §1 — доктрина вёрстки, по которой судить; §3 — по слайдам. Цитаты владельца дословные, интерпретировать их заново не надо |
| Я2 | `teorkat-vvedenie/L2/brief.md` | карточка лекции: `slide_order` из 18 имён, бюджеты, что точно не показываем |
| Я3 | `teorkat-vvedenie/src/math/katex.json` | **кэш формул Л1, 65 штук** — образец формата и единственный живой пример того, что тебе надо породить для Л2 |
| Я4 | `_generator/harvest_katex.py` шапка | как кэш добывался раньше: из УЖЕ отрендеренной колоды. Для новых формул механизма нет — это и есть Э1 |
| Я5 | `_generator/sborka/deck.py`, `slaid.py`, `gejt_vmeshcheniya.py` | компилятор, подбор типографики и гейт вместимости |

- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **ГЛАВНОЕ ПРАВИЛО ЭТОГО ЗАХОДА: НИЧЕГО НЕ ДЕЛАЙ ЗАНОВО.** Лекция прошла фазы 1–3 и разобрана
владельцем по слайдам. Ты идёшь по фазам 4→6 **инкрементально**: берёшь существующий слайд и
применяешь к нему конкретные, уже написанные правки. Пересборка «с нуля» запрещена — она сотрёт три
круга согласований.

### 🔴 ГДЕ ЛЕЖИТ ТВОЁ ЗАДАНИЕ — в самих карточках

**В каждой из 18 карточек `teorkat-vvedenie/L2/slajdy/<имя>/slaid.md` есть раздел
`## Доводка вёрстки — задание фазам 4–6`.** Там написано поимённо, что поправить на этом слайде, с
дословными цитатами владельца. Это и есть твой список работ, слайд за слайдом.
```
grep -l 'Доводка вёрстки' teorkat-vvedenie/L2/slajdy/*/slaid.md | wc -l
```
Раздел заканчивается блоком «Сквозное для всех слайдов» — пять правил, которые владелец назвал на
двух-трёх слайдах, но которые действуют везде: наконечники на дугах · композиция рисуется дугой, а не
подписью · на диаграмме конкретные объекты слайда, а не общий вид · русских слов внутри графики нет ·
заголовок ставится там, где стало просторно.

### ТРИ УСТАНОВЛЕННЫХ ДЕФЕКТА СБОРКИ — не диагностируй заново, чини

**Э1. ВСЯ МАТЕМАТИКА ВЫШЛА КАК `⟦MISSING-MATH:…⟧`.**
🔴🔴 **ЛОЖНАЯ ПРЕДПОСЫЛКА ИСХОДНОГО ЗАДАНИЯ — ИСПРАВЛЕНО АНАЛИТИКОМ ФАБРИКИ. Ничего не изобретай.**

В исходной редакции здесь стояло «механизма для новых формул в фабрике нет, путь выбираешь сам».
**Это неверно.** Механизм построен вчера и проверен: 102 формулы из 102 отрендерены на этой самой
лекции. Он ждёт тебя в дереве:
```
_generator/sborka/kesh_formul.js      вход — КАРТОЧКИ, тем же регекспом, что рендерер
_generator/vendor/katex/katex.min.js  katex 0.16.9, ПОД GIT, сеть не нужна

node _generator/sborka/kesh_formul.js teorkat-vvedenie/L2      →  <лекция>/math/katex.json
```
⚠ Шрифты докачивать не надо: 20 `@font-face` уже вшиты base64 в `_generator/skeleton/katex.css`.
⚠ `harvest_katex.py` и `teorkat-vvedenie/src/tools/katex_kesh.js` — **старые двери, не ходи в них.**
⚠ `deck.py`/`slaid.py` теперь **сами краснеют** при `MISSING-MATH` и печатают команду выше — молча
негодную деку ты больше не соберёшь.

⇒ **Твой шаг Ш1 — одна команда, а не исследование.** Если она не сработала — это находка, в отчёт;
обходных путей не искать (см. §1, правило про двойной отказ).

**Э2. Типографика не подобрана** — дека собрана с `--bez-podbora`, потому что в песочнице Cowork нет
браузера. Собирать надо **без этого флага**.

**Э3. Гейт вместимости ни разу не гонялся** — по той же причине. До правок было обрезано 5 слайдов из
15 при нуле превышений бюджета слов. После правок вёрстки должно стать лучше, но это гипотеза.

### ЧЕГО НЕ ТРОГАТЬ

🔴 **Тексты слайдов и состав блоков.** Они прошли три круга разбора и приняты владельцем дословно:
*«с текстом, с блоками, со стилистикой, с порядком идей — почти всё идеально»*. Не влезает — чини
вёрсткой, полосой и кеглем. Не помогло — **приходи с вопросом, а не режь текст.**

🔴 **Решения вёрстки, уже внесённые в шапки.** Типы перераспределены по доктрине владельца
(`Я1 §1`), полос без иллюстрации не осталось, заголовки проставлены на восьми. Менять их можно, но
**только с обоснованием в отчёте** — за каждым стоит цитата.

### ПОРЯДОК РАБОТЫ — по фазам, инкрементально

**Ш1 (Э1).** Породить кэш формул. Без него дальше идти бессмысленно: формулы это ~90 % текста слайдов.

**Ш2 (фаза 4, послайдово).** По каждому из 18: прочитать раздел «Доводка вёрстки» в его карточке,
применить названное, собрать слайд **отдельно** и посмотреть:
`python3 _generator/sborka/slaid.py teorkat-vvedenie/L2/slajdy/<имя> -o /tmp/<имя>.html`
Смотреть глазами каждый. Не «собрал все 18 и глянул на дек» — послайдово, как и предписывает фаза 4.

**Ш3 (фаза 5, иллюстрации).** Четыре SVG нарисованы аналитиком **начерно** и требуют доводки:
`vektory-na-ploskosti`, `izomorfizmy-primery`, `kvadrat-zameny-bazisa`, `kvadrat-eta`. Плюс правки
существующих — наконечники на дугах и дуга композиции вместо подписи (образец продиктован владельцем
в карточке `inyekcii`). Снятые картинки лежат в `illustracii/_snyato/` — не удалены, при нужде
достать оттуда.
⚠ `illustracii/pustoj-kvadrat/` и `illustracii/kvadrat-estestvennosti/` остались **пустыми папками**:
файлы перенесены, а сами каталоги из песочницы удалить не удалось. Убери их.

**Ш4 (фаза 6).** Собрать деку целиком **с подбором типографики** и прогнать гейт вместимости.
Обрезанное чинить вёрсткой в порядке доктрины: сменить тип → сдвинуть полосу → снять иллюстрацию,
если она ничего не иллюстрирует.
⚠ Правило владельца, полезное здесь: **у горизонтальной полосы запас по высоте большой, её можно
двигать вверх-вниз; у вертикальной запас маленький.**

**Ш5.** Служебных слайдов в деке не видно — считает их `grep -c 'class="slide"' teorkat-vvedenie/L2/dist/index.html`, и он даёт ровно 18, то есть на служебные ничего не пришлось. По решению владельца их вставляет
`deck.py` сам. Проверь, вставляет ли, и если нет — скажи в отчёте: заводить их руками не надо.
На обложку просится `pustoj-kvadrat` из `_snyato/` плюс название «Функторы».

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** 🔴 **Все проверки — ЖИВЫМ ПРОГОНОМ на реальном объекте
репозитория, на живой лекции `teorkat-vvedenie/L2`, а не на фикстурах.** Выводы в отчёт дословно.

```
# 1. маркеров формул не осталось
grep -c 'MISSING-MATH' teorkat-vvedenie/L2/dist/index.html
#    ожидание: 0

# 2. дека собрана С подбором — команда без --bez-podbora
python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html ; echo $?

# 3. вместимость: обрезанных 0 из 18
#    ⚠ гейт принимает СКОМПИЛИРОВАННЫЕ HTML СЛАЙДОВ, а не папку лекции
#      (usage: gejt_vmeshcheniya.py html [html ...]) — собери 18 через slaid.py в /tmp
python3 _generator/sborka/gejt_vmeshcheniya.py /tmp/*.html ; echo $?

# 3-бис. 🔴 СМЕТА — предскажи ДО сборки, чем мерить постфактум. Она без браузера.
python3 _generator/sborka/smeta.py teorkat-vvedenie/L2 ; echo $?
#    вчера предсказала ровно 5 обрезанных из 14 мереных: 0 ложных, 0 пропусков
#    расходится с гейтом вместимости → это находка про смету, в отчёт обе цифры

# 3-тер. гейт вёрстки по доктрине владельца
python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2 ; echo $?
#    ⚠ красное на `obosnovanie_verstki` ЗАКОННО, если поле ещё не заполнено — не регрессия

# 4. карточки не сломаны правками
python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2 ; echo $?

# 5. русских слов внутри графики нет
grep -o '>[^<]*<' teorkat-vvedenie/L2/illustracii/*/risunok.svg | grep -c '[а-яА-Я]'
#    ожидание: 0
```

🔴 **Заход НЕ сдан, если прогон 1 даёт не ноль.**
🔴 **И не сдан, если раздел «Доводка вёрстки» отработан не на всех 18.** В отчёте — строка на слайд:
что было названо, что сделано, что осталось. **«Проверено 18 из 18», а не «всё поправил».**

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «обрезанных нет», а «обрезанных нет, проверено 18
из 18».

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Нужен, и он визуальный: **свежий субагент смотрит собранную деку глазами, не зная, что чинили.** Дай ему только файл `dist/index.html` и вопрос «на каком слайде текст обрезан, наезжает или висит в пустоте». Совпал с твоим списком — хорошо; нашёл лишнее — чини. *Причина: гейт вместимости ловит переполнение, но не ловит «пусто и некрасиво», а владелец забраковал деку именно глазами.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `teorkat-vvedenie/L2`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `teorkat-vvedenie/L2`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Заход отправлен в отдельную рабочую папку, а его задание лежало незакоммиченным в основной
Контракт зоны велит завести worktree первым ходом и работать только там. Задание захода — раздел
«Доводка вёрстки» в 18 карточках, четыре черновых SVG, три новых слайда — аналитик написал в
рабочем дереве ОСНОВНОЙ папки и не закоммитил. Worktree — отдельный checkout: чужого
незакоммиченного дерева он не видит и увидеть не может. Гейт входа §0 этого не ловит: он проверяет
сведённость веток и наличие инструментов, но не проверяет, доехало ли до git **само задание**.
ЦЕНА: заход остановлен на нулевом ходу, ни один из шагов Ш1–Ш5 не начат; при четырёх сутках до
эфира потеряна вся сессия, а разблокировка требует действий владельца, которых он не ждал. Тот же
контракт «работай в worktree» стоит во всех заходах фабрики — повторится на любом следующем, чьё
задание аналитик пишет руками в основной папке.

### Гейт входа §0 проверяет ветки, но не проверяет, что зона захода закоммичена
Проверка §0 — три команды: `branch --no-merged`, два `ls` на инструменты. Первая же покраснела (Б1),
и это спасло случайно: даже будь ветки сведены, Б3 остался бы невидимым до момента, когда
исполнитель откроет карточку и не найдёт в ней задания — то есть после нескольких ходов работы по
устаревшей версии слайдов.
ЦЕНА: в этот раз ноль сверх уже названной (Б1 сработал раньше), но механизм проверки дырявый:
достаточно одной команды `git status --porcelain -- <зона>` в гейте входа, чтобы блокер вида Б3
называл себя сам. Без неё цена следующего срабатывания — работа по молча устаревшему заданию,
которая выглядит выполненной.

### Две расходящиеся версии одной лекции L2 живут одновременно, и ни один гейт этого не видит
На ветке `svedenie-arki`: 15 карточек, кэш 102 формулы, дека собрана, `MISSING-MATH: 0`. В основной
папке незакоммичено: 18 карточек с правками владельца и разделом «Доводка вёрстки», пять
иллюстраций снято в `_snyato/`, четыре новых нарисовано, дека собрана без браузера и кэша —
`MISSING-MATH: 44`. Обе называются `teorkat-vvedenie/L2/dist/index.html`.
ЦЕНА: слияние этих двух состояний стало ручной работой с риском затереть либо три круга
согласований владельца, либо единственный рабочий кэш формул. Прямой причиной был запуск двух
заходов на одну зону `teorkat-vvedenie/L2/**` без объявления, кто из них владеет зоной.

## ПЛАН — (заполняет исполнитель)

🔴 **ПЛАНА РАБОТ НЕТ: заход остановлен на гейте входа §0, до первого содержательного хода.**
Три блокера, все ВНЕ моей зоны, все подтверждены живым прогоном. Подробности и рецепт — в `## ОТЧЁТ`.

Намеревался идти Ш1→Ш5 как написано. Дальше проверки среды не ушёл: §0 предписывает при отказе
СТОП, и отказали сразу два его пункта из двух, плюс нашёлся третий, заходом не предусмотренный.

**Замечания к КРИТЕРИЮ ГОТОВНОСТИ (§1 требует оспорить ДО работы — оспариваю):**
1. Прогон 3 — `gejt_vmeshcheniya.py /tmp/*.html`. Глоб заметает **любой** чужой html в `/tmp`,
   включая оставленный соседним заходом, и вердикт «обрезанных 0 из 18» станет вердиктом
   неизвестно о чём. Нужен свой префикс: собирать в `/tmp/l2-<имя>.html` и мерить `/tmp/l2-*.html`,
   тогда охват «18 из 18» проверяем счётом файлов. (В этой сессии канал даёт свою папку-черновик,
   `…/scratchpad`, — она изолирована и годится лучше `/tmp`.)
2. Прогон 3 не несёт охвата в себе: `gejt_vmeshcheniya.py` не знает, сколько слайдов ДОЛЖНО быть.
   Отдай ему 12 файлов вместо 18 — он скажет зелёное. «18 из 18» надо считать отдельной командой,
   иначе клауза «охват внутри вердикта» не исполняется.
3. Прогон 5 (`русских слов внутри графики нет`) не покрывает `illustracii/_snyato/**`: глоб
   `illustracii/*/risunok.svg` — один уровень, `_snyato/` лежит на два. Пока картинки в `_snyato/`
   не вернули — это законно, но при возврате любой из них (заход прямо предлагает вернуть
   `pustoj-kvadrat` на обложку) гейт её не увидит.

## ВОПРОСЫ — (заполняет исполнитель)

1. **Б1. `zahod/svedenie-arki` не влит в `arka/mat-kostyak` — четырёх инструментов захода нет.**
   Ветка на 18 коммитов впереди и на 0 позади (слияние — перемотка), в ней уже сведены все семь
   веток. Слить её — не моя зона (контракт: «влитие делает приёмка»).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. **Б2. Браузера playwright в системе нет вообще** — каталог `~/Library/Caches/ms-playwright/`
   отсутствует, сам пакет `playwright` при этом стоит. Без браузера не работают ни подбор
   типографики (Э2), ни гейт вместимости (Э3). Ставится одной командой, но это установка софта с
   сетевой докачкой — делаю не я.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. **Б3. Задание захода — 18 карточек с разделом «Доводка вёрстки» — не закоммичено ни в одну
   ветку.** Оно лежит незакоммиченным в рабочем дереве ОСНОВНОЙ папки (35 путей). Заход при этом
   предписывает работать в отдельной рабочей папке, а она чужого незакоммиченного дерева не видит
   физически. Пока это не закоммичено — заход не может быть выполнен ни в какой рабочей папке.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. **Б1 и Б3 сцеплены: слияние `svedenie-arki` в основной папке ПРЯМО СЕЙЧАС затрёт работу
   аналитика.** Ветка трогает те же пути: 15 тех же `slaid.md`, плюс приносит свои
   `L2/dist/index.html` и `L2/math/katex.json`, а в основной папке `dist/` — untracked. Git на
   таком слиянии либо откажет, либо перезапишет untracked. Порядок обязателен: **сперва
   закоммитить работу аналитика, потом сливать.**
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. **Находка, которая экономит работу: на ветке `svedenie-arki` уже лежит готовый кэш формул L2 —
   `teorkat-vvedenie/L2/math/katex.json`, 102 формулы, и собранная с ним дека с `MISSING-MATH: 0`.**
   То есть Ш1 (Э1) на 15 старых карточках уже отработан и его результат не потерян. После сведения
   кэш надо будет до-породить только на новые/изменённые карточки — прогоном той же команды
   `node _generator/sborka/kesh_formul.js teorkat-vvedenie/L2`, а не с нуля.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
