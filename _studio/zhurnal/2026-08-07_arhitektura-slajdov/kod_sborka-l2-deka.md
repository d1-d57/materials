# Канал исполнителя — sborka-l2-deka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — главная задача не механическая: кэш формул для новой лекции никто никогда не собирал, и как его получить — решение по ходу. Плюс доводка вёрстки по промеру, а не по инструкции.

КОНТЕКСТ. Лекция 2 курса «Введение в теорию категорий», среда — эфир. Содержание, блоки и тексты
слайдов **приняты владельцем** после трёх кругов разбора: *«с текстом, с блоками, со стилистикой, с
порядком идей — почти всё идеально»*. Провалилась **только сборка**.

🔴 **Дека сейчас негодна, и причины установлены точно — не гадай, они ниже.** Аналитик собрал её из
песочницы Cowork, где нет браузера, и получил три дефекта разом. Твоя задача — собрать деку, которую
можно показать залу.

ЦЕЛЬ: `teorkat-vvedenie/L2/dist/index.html` открывается и читается: формулы отрисованы, кегли
подобраны, ни один слайд не обрезан.
Приёмка — по ОТЧЁТУ, без построчной сверки.
Если стоп до цели: **скажи это громко и сразу.** До эфира четверо суток, и «почти собрал» здесь
хуже, чем «не смог, вот на чём встал».

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/sborka-l2-deka`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/sborka-l2-deka`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add sborka-l2-deka --branch zahod/sborka-l2-deka
  cd ../materials-wt/sborka-l2-deka
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `teorkat-vvedenie/L2/**`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴🔴 **ВЕТКА-ОСНОВА ИЗМЕНЕНА АНАЛИТИКОМ — читай внимательно, это не то, что было вчера.**
  Всё, что тебе нужно (кэш формул, смета, гейты, 18 карточек, `PRAVKI-SBORKI-L2.md`), лежит **не в
  `arka/mat-kostyak`**, а на ветке `zahod/gigiena-i-svedenie` — она сведена, проверена и принята,
  но ещё не влита приёмкой. Поэтому **свою рабочую папку заводишь ОТ НЕЁ:**
  ```
  python3 _generator/tools/git_zona.py worktree add sborka-l2-deka --branch zahod/sborka-l2-deka --ot zahod/gigiena-i-svedenie
  ```
  ⚠ Флага `--ot` может не быть — тогда заведи обычным ходом и **первым делом влей основу**:
  `python3 _generator/tools/git_zona.py merge zahod/gigiena-i-svedenie --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _fond --zone obzory --zone ucheniki`
- 🔴 **ПРОВЕРЬ ПЕРЕД РАБОТОЙ, что основа на месте — шесть путей, все обязаны существовать:**
  ```
  ls _generator/sborka/kesh_formul.js _generator/vendor/katex/katex.min.js \
     _generator/sborka/smeta.py _generator/sborka/gejt_vmeshcheniya.py \
     _generator/tools/gejt_verstki.py teorkat-vvedenie/L2/math/katex.json
  ls -1 teorkat-vvedenie/L2/slajdy/ | wc -l      # обязано быть 18
  ```
  **Чего-то нет — СТОП, скажи владельцу.** Вчера два исполнителя подряд переизобретали готовое
  именно потому, что стартовали с ветки, где его не было.
  ⚠ **`git branch --no-merged` НЕ проверяй** — он не пуст и не должен быть: рядом идут другие
  заходы. Признак готовности — шесть путей выше, а не пустой список веток.
- 🔴 **`_generator/**` — READ-ONLY.** Ты этими инструментами ПОЛЬЗУЕШЬСЯ, но их не правишь. ⚠ **Известный дефект, о который ты споткнёшься:** `deck.py` **без** `--bez-podbora` падает `SystemExit`, если солвер не может уложить хотя бы один слайд, — теряя всю деку ради одного. Это не твоя поломка: зафиксируй, назови слайд, собери остальное и **скажи в отчёте**. Чинить генератор — не твоя зона.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/sborka-l2-deka`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `teorkat-vvedenie/L2`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Проверь среду ПЕРВЫМ делом — от этого зависит, выполним ли заход вообще:**
  ```
  python3 -c "import playwright; print('playwright есть')" ; echo $?
  python3 -c "from playwright.sync_api import sync_playwright
  with sync_playwright() as p: b=p.chromium.launch(); print('браузер запускается'); b.close()"
  ```
  Браузер не запускается — **СТОП, напиши в `## ВОПРОСЫ` и остановись.** Без него не работают ни
  подбор типографики, ни гейт вместимости, то есть две трети задачи. Ставится так:
  `pip install playwright && playwright install chromium`.

- **Прочитать ТОЛЬКО это, в таком порядке:**

| # | файл | зачем |
|---|---|---|
| Я1 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/PRAVKI-SBORKI-L2.md` **целиком** | 🔴 разбор деки владельцем: §1 — доктрина вёрстки, по которой судить; §3 — по слайдам. Цитаты владельца дословные, интерпретировать их заново не надо |
| Я2 | `teorkat-vvedenie/L2/brief.md` | карточка лекции: `slide_order` из 18 имён, бюджеты, что точно не показываем |
| Я3 | `teorkat-vvedenie/src/math/katex.json` | **кэш формул Л1, 65 штук** — образец формата и единственный живой пример того, что тебе надо породить для Л2 |
| Я4 | `_generator/harvest_katex.py` шапка | как кэш добывался раньше: из УЖЕ отрендеренной колоды. Для новых формул механизма нет — это и есть Э1 |
| Я5 | `_generator/sborka/deck.py`, `slaid.py`, `gejt_vmeshcheniya.py` | компилятор, подбор типографики и гейт вместимости |

- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **ГЛАВНОЕ ПРАВИЛО ЭТОГО ЗАХОДА: НИЧЕГО НЕ ДЕЛАЙ ЗАНОВО.** Лекция прошла фазы 1–3 и разобрана
владельцем по слайдам. Ты идёшь по фазам 4→6 **инкрементально**: берёшь существующий слайд и
применяешь к нему конкретные, уже написанные правки. Пересборка «с нуля» запрещена — она сотрёт три
круга согласований.

### 🔴 ГДЕ ЛЕЖИТ ТВОЁ ЗАДАНИЕ — в самих карточках

**В каждой из 18 карточек `teorkat-vvedenie/L2/slajdy/<имя>/slaid.md` есть раздел
`## Доводка вёрстки — задание фазам 4–6`.** Там написано поимённо, что поправить на этом слайде, с
дословными цитатами владельца. Это и есть твой список работ, слайд за слайдом.
```
grep -l 'Доводка вёрстки' teorkat-vvedenie/L2/slajdy/*/slaid.md | wc -l
```
Раздел заканчивается блоком «Сквозное для всех слайдов» — пять правил, которые владелец назвал на
двух-трёх слайдах, но которые действуют везде: наконечники на дугах · композиция рисуется дугой, а не
подписью · на диаграмме конкретные объекты слайда, а не общий вид · русских слов внутри графики нет ·
заголовок ставится там, где стало просторно.

### ТРИ УСТАНОВЛЕННЫХ ДЕФЕКТА СБОРКИ — не диагностируй заново, чини

**Э1. ВСЯ МАТЕМАТИКА ВЫШЛА КАК `⟦MISSING-MATH:…⟧`.**
🔴🔴 **ЛОЖНАЯ ПРЕДПОСЫЛКА ИСХОДНОГО ЗАДАНИЯ — ИСПРАВЛЕНО АНАЛИТИКОМ ФАБРИКИ. Ничего не изобретай.**

В исходной редакции здесь стояло «механизма для новых формул в фабрике нет, путь выбираешь сам».
**Это неверно.** Механизм построен вчера и проверен: 102 формулы из 102 отрендерены на этой самой
лекции. Он ждёт тебя в дереве:
```
_generator/sborka/kesh_formul.js      вход — КАРТОЧКИ, тем же регекспом, что рендерер
_generator/vendor/katex/katex.min.js  katex 0.16.9, ПОД GIT, сеть не нужна

node _generator/sborka/kesh_formul.js teorkat-vvedenie/L2      →  <лекция>/math/katex.json
```
⚠ Шрифты докачивать не надо: 20 `@font-face` уже вшиты base64 в `_generator/skeleton/katex.css`.
⚠ `harvest_katex.py` и `teorkat-vvedenie/src/tools/katex_kesh.js` — **старые двери, не ходи в них.**
⚠ `deck.py`/`slaid.py` теперь **сами краснеют** при `MISSING-MATH` и печатают команду выше — молча
негодную деку ты больше не соберёшь.

⇒ **Твой шаг Ш1 — одна команда, а не исследование.** Если она не сработала — это находка, в отчёт;
обходных путей не искать (см. §1, правило про двойной отказ).

**Э2. Типографика не подобрана** — дека собрана с `--bez-podbora`, потому что в песочнице Cowork нет
браузера. Собирать надо **без этого флага**.

**Э3. Гейт вместимости ни разу не гонялся** — по той же причине. До правок было обрезано 5 слайдов из
15 при нуле превышений бюджета слов. После правок вёрстки должно стать лучше, но это гипотеза.

### ЧЕГО НЕ ТРОГАТЬ

🔴 **Тексты слайдов и состав блоков.** Они прошли три круга разбора и приняты владельцем дословно:
*«с текстом, с блоками, со стилистикой, с порядком идей — почти всё идеально»*. Не влезает — чини
вёрсткой, полосой и кеглем. Не помогло — **приходи с вопросом, а не режь текст.**

🔴 **Решения вёрстки, уже внесённые в шапки.** Типы перераспределены по доктрине владельца
(`Я1 §1`), полос без иллюстрации не осталось, заголовки проставлены на восьми. Менять их можно, но
**только с обоснованием в отчёте** — за каждым стоит цитата.

### ПОРЯДОК РАБОТЫ — по фазам, инкрементально

**Ш1 (Э1).** Породить кэш формул. Без него дальше идти бессмысленно: формулы это ~90 % текста слайдов.

**Ш2 (фаза 4, послайдово).** По каждому из 18: прочитать раздел «Доводка вёрстки» в его карточке,
применить названное, собрать слайд **отдельно** и посмотреть:
`python3 _generator/sborka/slaid.py teorkat-vvedenie/L2/slajdy/<имя> -o /tmp/<имя>.html`
Смотреть глазами каждый. Не «собрал все 18 и глянул на дек» — послайдово, как и предписывает фаза 4.

**Ш3 (фаза 5, иллюстрации).** Четыре SVG нарисованы аналитиком **начерно** и требуют доводки:
`vektory-na-ploskosti`, `izomorfizmy-primery`, `kvadrat-zameny-bazisa`, `kvadrat-eta`. Плюс правки
существующих — наконечники на дугах и дуга композиции вместо подписи (образец продиктован владельцем
в карточке `inyekcii`). Снятые картинки лежат в `illustracii/_snyato/` — не удалены, при нужде
достать оттуда.
⚠ `illustracii/pustoj-kvadrat/` и `illustracii/kvadrat-estestvennosti/` остались **пустыми папками**:
файлы перенесены, а сами каталоги из песочницы удалить не удалось. Убери их.

**Ш4 (фаза 6).** Собрать деку целиком **с подбором типографики** и прогнать гейт вместимости.
Обрезанное чинить вёрсткой в порядке доктрины: сменить тип → сдвинуть полосу → снять иллюстрацию,
если она ничего не иллюстрирует.
⚠ Правило владельца, полезное здесь: **у горизонтальной полосы запас по высоте большой, её можно
двигать вверх-вниз; у вертикальной запас маленький.**

**Ш5.** Служебных слайдов в деке не видно — считает их `grep -c 'class="slide"' teorkat-vvedenie/L2/dist/index.html`, и он даёт ровно 18, то есть на служебные ничего не пришлось. По решению владельца их вставляет
`deck.py` сам. Проверь, вставляет ли, и если нет — скажи в отчёте: заводить их руками не надо.
На обложку просится `pustoj-kvadrat` из `_snyato/` плюс название «Функторы».

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** 🔴 **Все проверки — ЖИВЫМ ПРОГОНОМ на реальном объекте
репозитория, на живой лекции `teorkat-vvedenie/L2`, а не на фикстурах.** Выводы в отчёт дословно.

```
# 1. маркеров формул не осталось
grep -c 'MISSING-MATH' teorkat-vvedenie/L2/dist/index.html
#    ожидание: 0

# 2. дека собрана С подбором — команда без --bez-podbora
python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html ; echo $?

# 3. вместимость: обрезанных 0 из 18
#    ⚠ гейт принимает СКОМПИЛИРОВАННЫЕ HTML СЛАЙДОВ, а не папку лекции
#      (usage: gejt_vmeshcheniya.py html [html ...]) — собери 18 через slaid.py в /tmp
python3 _generator/sborka/gejt_vmeshcheniya.py /tmp/*.html ; echo $?

# 3-бис. 🔴 СМЕТА — предскажи ДО сборки, чем мерить постфактум. Она без браузера.
python3 _generator/sborka/smeta.py teorkat-vvedenie/L2 ; echo $?
#    вчера предсказала ровно 5 обрезанных из 14 мереных: 0 ложных, 0 пропусков
#    расходится с гейтом вместимости → это находка про смету, в отчёт обе цифры

# 3-тер. гейт вёрстки по доктрине владельца
python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2 ; echo $?
#    ⚠ красное на `obosnovanie_verstki` ЗАКОННО, если поле ещё не заполнено — не регрессия

# 4. карточки не сломаны правками
python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2 ; echo $?

# 5. русских слов внутри графики нет
grep -o '>[^<]*<' teorkat-vvedenie/L2/illustracii/*/risunok.svg | grep -c '[а-яА-Я]'
#    ожидание: 0
```

🔴 **Заход НЕ сдан, если прогон 1 даёт не ноль.**
🔴 **И не сдан, если раздел «Доводка вёрстки» отработан не на всех 18.** В отчёте — строка на слайд:
что было названо, что сделано, что осталось. **«Проверено 18 из 18», а не «всё поправил».**

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «обрезанных нет», а «обрезанных нет, проверено 18
из 18».

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Нужен, и он визуальный: **свежий субагент смотрит собранную деку глазами, не зная, что чинили.** Дай ему только файл `dist/index.html` и вопрос «на каком слайде текст обрезан, наезжает или висит в пустоте». Совпал с твоим списком — хорошо; нашёл лишнее — чини. *Причина: гейт вместимости ловит переполнение, но не ловит «пусто и некрасиво», а владелец забраковал деку именно глазами.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `teorkat-vvedenie/L2`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `teorkat-vvedenie/L2`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Заход отправлен в отдельную рабочую папку, а его задание лежало незакоммиченным в основной
Контракт зоны велит завести worktree первым ходом и работать только там. Задание захода — раздел
«Доводка вёрстки» в 18 карточках, четыре черновых SVG, три новых слайда — аналитик написал в
рабочем дереве ОСНОВНОЙ папки и не закоммитил. Worktree — отдельный checkout: чужого
незакоммиченного дерева он не видит и увидеть не может. Гейт входа §0 этого не ловит: он проверяет
сведённость веток и наличие инструментов, но не проверяет, доехало ли до git **само задание**.
ЦЕНА: заход остановлен на нулевом ходу, ни один из шагов Ш1–Ш5 не начат; при четырёх сутках до
эфира потеряна вся сессия, а разблокировка требует действий владельца, которых он не ждал. Тот же
контракт «работай в worktree» стоит во всех заходах фабрики — повторится на любом следующем, чьё
задание аналитик пишет руками в основной папке.

### Гейт входа §0 проверяет ветки, но не проверяет, что зона захода закоммичена
Проверка §0 — три команды: `branch --no-merged`, два `ls` на инструменты. Первая же покраснела (Б1),
и это спасло случайно: даже будь ветки сведены, Б3 остался бы невидимым до момента, когда
исполнитель откроет карточку и не найдёт в ней задания — то есть после нескольких ходов работы по
устаревшей версии слайдов.
ЦЕНА: в этот раз ноль сверх уже названной (Б1 сработал раньше), но механизм проверки дырявый:
достаточно одной команды `git status --porcelain -- <зона>` в гейте входа, чтобы блокер вида Б3
называл себя сам. Без неё цена следующего срабатывания — работа по молча устаревшему заданию,
которая выглядит выполненной.

### Две расходящиеся версии одной лекции L2 живут одновременно, и ни один гейт этого не видит
На ветке `svedenie-arki`: 15 карточек, кэш 102 формулы, дека собрана, `MISSING-MATH: 0`. В основной
папке незакоммичено: 18 карточек с правками владельца и разделом «Доводка вёрстки», пять
иллюстраций снято в `_snyato/`, четыре новых нарисовано, дека собрана без браузера и кэша —
`MISSING-MATH: 44`. Обе называются `teorkat-vvedenie/L2/dist/index.html`.
ЦЕНА: слияние этих двух состояний стало ручной работой с риском затереть либо три круга
согласований владельца, либо единственный рабочий кэш формул. Прямой причиной был запуск двух
заходов на одну зону `teorkat-vvedenie/L2/**` без объявления, кто из них владеет зоной.

### Ось `liniya` солвера — мёртвый код: солвер не может тронуть НИ ОДНУ полосу
`tipy._require_liniya` делает `float(p["liniya"])` — без числа слайд типа `polosa_*` не компилируется
вовсе, и `gejt_kartochki.py` требует это поле заполненным. А `deck._podobrat_tipografiku` считает
явным ЛЮБОЕ число: `liniya_yavnaya = axis is not None and params.get("liniya") not in (None, "",
"заполнить")` — и слайд пропускает целиком, вместе с подбором кегля. Значит для любой карточки,
прошедшей гейт, ось `liniya` недостижима: «заполнить» роняет компиляцию, число выключает солвер.
ЦЕНА: **6 из 9 обрезанных слайдов этой лекции — полосы, и солвер не тронул ни одной.** Вместимость
на них добиралась вручную промером через браузер — примерно половина времени захода. Сам солвер,
главный инструмент арки `solver-vmeshcheniya`, отработал на 5 карточках из 18; на замере он при этом
решает полосы отлично (проверил прямым вызовом `podobrat_slide` — все шесть решились).

### `deck.py` замораживает типографику в карточке, и второй раз солвер туда не заходит
`vmeshchenie.apply_to_card` дописывает `kegl_px`/`mezhstrochye`/`otstup_bloka` в шапку. На следующей
сборке эти поля уже «явные» — слайд пропускается. То есть подбор одноразовый: любая правка текста
ПОСЛЕ первой сборки не переподбирается и молча едет на старом кегле.
ЦЕНА: поймало меня на `dvojstvennoe-prostranstvo` — я чинил там слипшиеся абзацы (стало на один блок
больше) уже после того, как солвер заморозил кегль 41; вместимость пришлось проверять отдельным
промером вручную. Ничего не покраснело бы: второй прогон `deck.py` печатает «пропущен, явные значения
в шапке» и выглядит нормальной работой.

### Тип `tolko_tekst` — «для многого текста» — даёт места МЕНЬШЕ всех типов
`tipy.tolko_tekst` ставит на `.grid` своё `padding:64px 96px`, а `GLOBAL_CSS` уже дал зоне
`.zone.copy{padding:46px 64px}`. Поля складываются. Промер: зона `tolko_tekst` — **1120×590**, зона
типа без поля grid — **1312×718**. Тип, который доктрина назначает случаю «текста много, иллюстрации
нет», отнимает 128px высоты — две с половиной строки.
ЦЕНА: `kategoriya` резалась на 167 %, и владелец увидел это глазами, но объяснил себе неверно
(«область сдвинута к центру… нет вертикальной полосы»). Ушёл круг разбора на слайд, у которого
дефект был в одной строке чужого типа. Повторится на КАЖДОЙ лекции: `tolko_tekst` — самый частый тип
(6 из 18 здесь).

### В словаре примитивов нет наконечника цвета `--ink` — а дуга композиции рисуется именно им
`.s-line` — ink 2px; наконечники в движке только два: `.s-ar-m` (сталь) и `.s-ar-a` (кирпич). Владелец
потребовал наконечники ровно на чёрных дугах композиции — значит нужен ink-наконечник, а его нет ни в
словаре `_illustracii/DISCIPLINA.md`, ни в `_generator/skeleton/base.css`.
ЦЕНА: поставил `style="fill:var(--ink)"` инлайном в 6 местах трёх рисунков. Работает и красится
токеном, но обходит правило словаря «цвет — только классом»; следующий, кто нарисует дугу композиции,
обойдёт его так же, и в деках заведётся два разных способа красить одну и ту же вещь. Чинится одной
строкой `.s-ar-l{fill:var(--ink)}` в `base.css` плюс строка в таблице словаря — но обе вне моей зоны.

## ПЛАН — (заполняет исполнитель)

🔴 **ПЛАНА РАБОТ НЕТ: заход остановлен на гейте входа §0, до первого содержательного хода.**
Три блокера, все ВНЕ моей зоны, все подтверждены живым прогоном. Подробности и рецепт — в `## ОТЧЁТ`.

Намеревался идти Ш1→Ш5 как написано. Дальше проверки среды не ушёл: §0 предписывает при отказе
СТОП, и отказали сразу два его пункта из двух, плюс нашёлся третий, заходом не предусмотренный.

**Замечания к КРИТЕРИЮ ГОТОВНОСТИ (§1 требует оспорить ДО работы — оспариваю):**
1. Прогон 3 — `gejt_vmeshcheniya.py /tmp/*.html`. Глоб заметает **любой** чужой html в `/tmp`,
   включая оставленный соседним заходом, и вердикт «обрезанных 0 из 18» станет вердиктом
   неизвестно о чём. Нужен свой префикс: собирать в `/tmp/l2-<имя>.html` и мерить `/tmp/l2-*.html`,
   тогда охват «18 из 18» проверяем счётом файлов. (В этой сессии канал даёт свою папку-черновик,
   `…/scratchpad`, — она изолирована и годится лучше `/tmp`.)
2. Прогон 3 не несёт охвата в себе: `gejt_vmeshcheniya.py` не знает, сколько слайдов ДОЛЖНО быть.
   Отдай ему 12 файлов вместо 18 — он скажет зелёное. «18 из 18» надо считать отдельной командой,
   иначе клауза «охват внутри вердикта» не исполняется.
3. Прогон 5 (`русских слов внутри графики нет`) не покрывает `illustracii/_snyato/**`: глоб
   `illustracii/*/risunok.svg` — один уровень, `_snyato/` лежит на два. Пока картинки в `_snyato/`
   не вернули — это законно, но при возврате любой из них (заход прямо предлагает вернуть
   `pustoj-kvadrat` на обложку) гейт её не увидит.

---

### ПЛАН — круг 2 (после снятия блокеров), выполнен

Основа влита из `zahod/gigiena-i-svedenie`, все шесть обязательных на месте, chrome запускается.
По Б2 признаю ошибку: я проверил НАЛИЧИЕ каталога браузеров вместо ЗАПУСКА, и канал `chrome`
берёт системный Chrome мимо этого каталога. Проверка была не та.

Порядок, которым шёл:
1. **Ш1** — кэш формул одной командой.
2. **Промер до правок**: собрал все 18 и снял браузером базовую линию, чтобы правки мерились, а не
   оценивались.
3. **Разбор находки владельца по `kategoriya`** — промером геометрии зоны, а не по инструкции.
4. **Ш2/фаза 4** — вёрстка послайдово: по каждому слайду читал «Доводку», решал `liniya`/кегль
   промером, писал `obosnovanie_verstki`.
5. **Ш3/фаза 5** — графика: сквозные правила владельца (наконечники, дуга композиции), четыре
   черновых SVG, пропорции под промеренные полосы.
6. **Ш4/фаза 6** — сборка с подбором, шесть гейтов.
7. **§3** — визуальный верификатор, два независимых круга; его находки чинил.

**Замечание к порядку захода, которое стоило времени:** Ш1 нельзя выполнять первым ходом «и забыть».
`deck.py` при сборке ПИШЕТ подобранные `kegl_px`/`mezhstrochye`/`otstup_bloka` обратно в карточки —
и после этого солвер на такой слайд больше не заходит НИКОГДА (поле стало «явным»). Значит любая
правка текста ПОСЛЕ первой сборки уже не переподбирается. Меня это поймало на
`dvojstvennoe-prostranstvo`: я чинил там слипшиеся абзацы уже после того, как солвер заморозил кегль.
Правильный порядок — сначала весь текст и вёрстка, сборка с подбором последней.

## ВОПРОСЫ — (заполняет исполнитель)

> 🟢 **Пункты 1–4 закрыты владельцем: основа влита, браузер есть, задание закоммичено.**
> Оставлены как след. Живые вопросы — с пункта 6.

1. **Б1. `zahod/svedenie-arki` не влит в `arka/mat-kostyak` — четырёх инструментов захода нет.**
   Ветка на 18 коммитов впереди и на 0 позади (слияние — перемотка), в ней уже сведены все семь
   веток. Слить её — не моя зона (контракт: «влитие делает приёмка»).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. **Б2. Браузера playwright в системе нет вообще** — каталог `~/Library/Caches/ms-playwright/`
   отсутствует, сам пакет `playwright` при этом стоит. Без браузера не работают ни подбор
   типографики (Э2), ни гейт вместимости (Э3). Ставится одной командой, но это установка софта с
   сетевой докачкой — делаю не я.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. **Б3. Задание захода — 18 карточек с разделом «Доводка вёрстки» — не закоммичено ни в одну
   ветку.** Оно лежит незакоммиченным в рабочем дереве ОСНОВНОЙ папки (35 путей). Заход при этом
   предписывает работать в отдельной рабочей папке, а она чужого незакоммиченного дерева не видит
   физически. Пока это не закоммичено — заход не может быть выполнен ни в какой рабочей папке.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. **Б1 и Б3 сцеплены: слияние `svedenie-arki` в основной папке ПРЯМО СЕЙЧАС затрёт работу
   аналитика.** Ветка трогает те же пути: 15 тех же `slaid.md`, плюс приносит свои
   `L2/dist/index.html` и `L2/math/katex.json`, а в основной папке `dist/` — untracked. Git на
   таком слиянии либо откажет, либо перезапишет untracked. Порядок обязателен: **сперва
   закоммитить работу аналитика, потом сливать.**
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. **Находка, которая экономит работу: на ветке `svedenie-arki` уже лежит готовый кэш формул L2 —
   `teorkat-vvedenie/L2/math/katex.json`, 102 формулы, и собранная с ним дека с `MISSING-MATH: 0`.**
   То есть Ш1 (Э1) на 15 старых карточках уже отработан и его результат не потерян. После сведения
   кэш надо будет до-породить только на новые/изменённые карточки — прогоном той же команды
   `node _generator/sborka/kesh_formul.js teorkat-vvedenie/L2`, а не с нуля.
   ДОМ: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет

---
### Живые вопросы круга 2 — решения, которые владелец может переиграть

6. 🔴 **`kategoriya`: заголовок «Категория и функтор» я СНЯЛ.** Слайду не хватает места ни при каком
   законном рычаге. Все варианты — числа с живого промера, 100,0 % = влезает ровно:

   | вариант | кегль | межстрочье | текст |
   |---|---|---|---|
   | как было (`tolko_tekst` + заголовок) | 35 — пол канона | 1,20 | **121,6 %** — режется |
   | `polnyj_ekran` + заголовок | 35 | 1,20 | 100,5 % — режется |
   | `polnyj_ekran` + заголовок | 36 | **1,18** | 100,0 % — влезает, межстрочье зажато |
   | **`polnyj_ekran`, заголовок снят** ← сделано | **36** | **1,25** | 100,0 % — влезает и дышит |

   Взял последний: владелец сам назвал заголовок переключателем «в том числе исходя из соображений
   места», а зажатое межстрочье на самом плотном слайде деки читается хуже отсутствующего заголовка.
   Вернуть заголовок — две строки в карточке (`zagolovok_na_ekrane` + `mezhstrochye: 1.18`), текст
   не трогается.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

7. **Ответ на вопрос владельца «почему в Л1 влезало, а тут нет» — найден, и он НЕ про вертикальную
   полосу.** Три причины, все промерены:
   (а) `tolko_tekst` кладёт своё поле `64/96px` ПОВЕРХ поля зоны `46/64px` — двойное поле. Зона выходит
   **1120×590** против **1312×718** у типов без него: тип «для многого текста» даёт места МЕНЬШЕ всех,
   это −128px высоты, две с половиной строки;
   (б) в Л1 этот слайд **тоже не влезал** — в `teorkat-vvedenie/src/tools/sverstat.py` на нём стоит
   ручная заплатка `"s11": {"css": [".copy{--blok:14px}"]}` с комментарием автора дословно:
   *«переполнение здесь ДОРЕЗНОЕ, оно жило и на старом деке»*, Δh +23px. «Влезало» = «влезало впритык
   после ручной правки», запаса не было;
   (в) в Л1 список функтора был компактной строкой `.spisok`, в Л2 стал буллетным `ul.tlist`, а у него
   `margin-top: var(--blok)` с обеих сторон.
   ⇒ Предпосылка «те же 89 слов помещались» верна по словам и неверна по форме и по запасу.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

8. **Заголовки: где ещё можно поставить и чего это стоит.** Владелец просил ставить заголовки, но на
   четырёх слайдах место уже занято. Промер — кегль сейчас → кегль с заголовком:
   `centr-gruppy` 40 → **36** · `vektornye-prostranstva` 40 → **36** (и межстрочье до 1,2) ·
   `zamena-bazisa` 38 → **36** · `inyekcii` — **не влезает даже на полу кегля**.
   Проставил `zagolovok_snyat_namerenno: da`. Вернуть — строка в карточке плюс названная цена.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

9. **Круглая или квадратная картинка в ГОРИЗОНТАЛЬНОЙ полосе всегда оставит пустые бока — это
   геометрия, а не брак сборки.** Полоса 1380×283 имеет пропорцию 4,9; диск `luch-brauera` — 1,03,
   квадрат `kvadrat-so-skalyarom` — 1,6. Картинка вписывается по ВЫСОТЕ и занимает 20–35 % ширины.
   Закрывается только более широкой композицией (на Брауэре — диск и рядом окружность-цель со стрелкой
   ретракции), а это содержание картинки, не вёрстка. Тип обоих слайдов владелец назвал сам, поэтому
   не трогал; пустоту со стороны текста добрал кеглем до канонного потолка 44.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

10. **`retrakt` и `razdelitel-retrakty` несут ОДИН заголовок «Ретракты»**, между ними два слайда.
    Оба стоят по прямой цитате владельца (разделитель — *«прям назвать это „Ретракты“»*, слайд —
    *«написать большими заголовок „Ретракты“»*), поэтому сам не менял.
    ДОМ: владелец
    ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** артефакта нет — заход остановлен гейтом входа §0 до первого содержательного хода;
деку не собирал, ни один шаг Ш1–Ш5 не выполнялся.
🔴 **Чтобы не открыли по ошибке как «новую»:** файл
`/Users/ivanyakovlev/Documents/GitHub/materials/teorkat-vvedenie/L2/dist/index.html`
(971 955 байт, 08.08 22:18) — это **старая негодная дека из песочницы Cowork**, та самая, с
`MISSING-MATH: 44`. Я её не трогал и не пересобирал.

**КОММИТ:** коммита нет, и это не сбой — **коммитить нечего**: зона `teorkat-vvedenie/L2/**` в моей
рабочей папке не изменена ни на байт, потому что работа не начиналась.
`python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/L2` → `rc=0`,
`✅ зона teorkat-vvedenie/L2: работа доехала в git, вне git ничего нет.`
Мой след этой сессии целиком — текст в ЭТОМ файле-заходе (ПЛАН/ВОПРОСЫ/УРОКИ/ОТЧЁТ), а его по
контракту коммитит приёмка, не я.

---

### 🔴 ГЛАВНОЕ: СТОП. Заход невыполним в текущем состоянии репозитория — три блокера

Все три ВНЕ моей зоны, все три требуют действия владельца, все подтверждены живым прогоном.
Говорю сразу и громко, как предписано: до эфира четверо суток, «почти собрал» я не предъявляю.

**Б1. `zahod/svedenie-arki` не влит — четырёх инструментов захода нет в дереве.**
Проверка §0 покраснела первой же командой:
```
ls: _generator/sborka/kesh_formul.js: No such file or directory
ls: _generator/sborka/smeta.py: No such file or directory
ls: _generator/tools/gejt_verstki.py: No such file or directory
ls: _generator/vendor/katex/katex.min.js: No such file or directory
rc=1
```
Все четыре лежат на `zahod/svedenie-arki`. Она на **18 коммитов впереди** `arka/mat-kostyak` и на
**0 позади** — слияние будет перемоткой. В ней уже сведены семь веток (`instrument-podklyuchen`,
`uroki-faz-1-2`, `hvosty-adresa`, `sborka-l2`, `konsolidaciya-i-git-kontur`, `verstka-v-faze-1`,
`karta-konvejera`). Сливать не стал: контракт зоны прямо говорит «ветку не вливай, влитие делает
приёмка», а `_generator/**` мне read-only.

**Б2. Браузера playwright в системе нет вообще.** Пакет стоит, браузер не скачан:
```
playwright есть                                    ← rc=0
BrowserType.launch: Executable doesn't exist at
  …/ms-playwright/chromium_headless_shell-1217/…/chrome-headless-shell
rc=1
ls ~/Library/Caches/ms-playwright/  →  каталога нет
```
Каталога нет целиком, то есть не «обновился и слетел», а не ставился ни разу. Без него мертвы
и Э2 (подбор типографики), и Э3 (гейт вместимости) — две трети задачи. §0 предписывает на этом
остановиться, что я и делаю.
*(`node v24.14.1` на месте — для `kesh_formul.js` среда готова.)*

**Б3. 🔴 Само задание захода не закоммичено ни в одну ветку — заходом не предусмотрено.**
Это не было в списке дефектов, я нашёл это проверкой предпосылки §2 («в каждой из 18 карточек есть
раздел `## Доводка вёрстки`»). Предпосылка ложна для git и истинна только для рабочего дерева
ОСНОВНОЙ папки:

| где | карточек `slaid.md` | из них с «Доводка вёрстки» | `dist/index.html` |
|---|---|---|---|
| ветка `arka/mat-kostyak` (база моей папки) | 15 | **0** | нет |
| ветка `zahod/svedenie-arki` | 15 | **0** | есть (0 MISSING-MATH) |
| ветка `zahod/sborka-l2` | 15 | **0** | нет |
| **основная папка, НА ДИСКЕ** | **18** | **18** | есть (44 MISSING-MATH) |

`git status --porcelain -- teorkat-vvedenie/L2` в основной папке даёт **35 незакоммиченных путей**:
16 изменённых карточек, 3 новых слайда (`itogi`, `razdelitel-estestvennost`, `razdelitel-retrakty`),
4 новых иллюстрации (`vektory-na-ploskosti`, `izomorfizmy-primery`, `kvadrat-zameny-bazisa`,
`kvadrat-eta`), 5 снятых в `_snyato/`, `dist/`, правленый `brief.md`.
**Рабочая папка захода — отдельный checkout: чужого незакоммиченного дерева она не видит и не может.**
А работать в основной папке контракт запрещает («рядом идут другие заходы»). Пока это не в git,
заход невыполним в любой рабочей папке, включая основную.

---

### 🔴 Б1 и Б3 СЦЕПЛЕНЫ — порядок разблокировки обязателен, иначе потеря работы

`svedenie-arki` трогает ту же зону: **24 файла в `teorkat-vvedenie/`**, из них 15 тех же `slaid.md`,
плюс приносит свои `L2/dist/index.html` (876 строк) и `L2/math/katex.json`.
**Если слить её в основную папку прямо сейчас** — git либо откажет на 15 изменённых карточках, либо
затрёт untracked `dist/`. Сперва коммит работы аналитика, потом слияние. Не наоборот.

Конфликты при слиянии предсказуемы поимённо, и все разрешаются дёшево:

| путь | природа | как решать |
|---|---|---|
| 15 × `slajdy/*/slaid.md` | обе стороны правили | взять версию **основной папки** (тексты приняты владельцем), затем повторить одну механическую правку ветки: `kommentarij_lektoru: заполнить` → `kommentarij_lektoru: ""` — это ВСЁ, что там меняла ветка |
| `L2/dist/index.html` | обе стороны добавили | взять любую и выбросить: обе устарели, деку всё равно пересобирать |
| `L2/math/katex.json` | только ветка | конфликта нет, приедет сам — **и это ценно, см. ниже** |
| `illustracii/retrakt-i-obraz/risunok.svg` | ветка правила / аналитик удалил (снял в `_snyato/`) | оставить удаление, но правку ветки не терять — см. ниже |

### 🟢 Хорошая новость: часть работы захода УЖЕ сделана на `svedenie-arki` и её нельзя потерять

1. **Кэш формул L2 существует.** `teorkat-vvedenie/L2/math/katex.json` — **102 формулы**, и собранная
   с ним дека даёт `MISSING-MATH: 0`. То есть Ш1 (Э1) на старых 15 карточках отработан. После
   сведения его надо будет только до-породить на новые и изменённые карточки — тем же прогоном
   `node _generator/sborka/kesh_formul.js teorkat-vvedenie/L2`, не с нуля.
2. **Сквозное правило «композиция рисуется дугой, а не подписью» уже применено на четырёх
   иллюстрациях** — ветка заменила текстовые подписи `sgn ∘ s = id`, `r ∘ s = id`, `= id ?` на
   настоящие дуги с расширением `viewBox`, на `centr-i-ego-obraz`, `disk-i-okruzhnost`,
   `vlozhenie-i-vozvrat`, `retrakt-i-obraz`. Это ровно то, что в Ш3 стоит как работа «сделать».
   ⚠ Из этих четырёх **`retrakt-i-obraz` аналитик снял в `_snyato/`** — то есть при простом
   разрешении «оставить удаление» готовая правка уйдёт вместе с файлом. Если картинку решат
   вернуть, брать её надо **с ветки, а не из `_snyato/`**.

---

### Что сделал и ЗАЧЕМ
Ровно одно: **прошёл гейт входа §0 и проверил предпосылки задания по живым файлам**, как велит §1
(«предпосылку проверяй по живым файлам, включая написанную владельцем»). Никакой правки в зоне не
делал — по §0 при красном гейте работать нельзя, а я получил красное дважды из двух.
Диагностику довёл до конца вместо того, чтобы остановиться на первом отказе: иначе владелец узнавал
бы про Б2 и Б3 по очереди, отдельными кругами, а круг здесь стоит суток.

### Как проверил
Всё — живым прогоном на реальном репозитории, `rc` читал первым. Выводы дословно в блоках выше.
Из критерия готовности прогнал те проверки, которые исполнимы без инструментов и браузера:
`MISSING-MATH` в текущей деке = **44** (не 0 — критерий 1 провален на входе);
`class="slide"` = **18** (служебные слайды `deck.py` действительно не добавляет — Ш5 подтверждён
наблюдением, деку заводить руками не надо).
Остальные прогоны (2, 3, 3-бис, 3-тер, 4, 5) невыполнимы: их инструменты отсутствуют (Б1) либо
требуют браузера (Б2).

### Что НЕ трогал
Ничего. Ни одного файла в `teorkat-vvedenie/L2/**` ни в рабочей, ни в основной папке. `_generator/**`
не правил. Ветки не сливал, `git checkout` в основной папке не делал. В основной папке — только
чтение; весь git шёл с `--no-optional-locks`, чтобы не отобрать `.git/index.lock` у соседних заходов
(их сейчас живо 13 рабочих папок).

### НЕОБРАТИМОЕ
**Необратимого нет.** Ни удалений, ни перезаписи, ни переименований, ни перемещений, ни `reset`/
`checkout`. Единственная запись на диск за сессию — текст в этом файле-заходе.

### Верификатор
Не запускал: он визуальный и смотрит собранную деку, а деки нет. Запускать его на старой негодной
колоде — значит получить список дефектов, которые заход и так перечисляет.

### ПОВТОРЯЕМОСТЬ находок
- **Б3 и Б2 — класс НЕМЕДЛЕННОЕ, повторятся на следующей же единице работы.** Б3 повторится на любом
  заходе, чьё задание аналитик пишет руками в основной папке, а исполнителя шлёт в рабочую, — то
  есть на всех заходах с worktree-контрактом. Б2 повторится на каждом заходе, которому нужен подбор
  типографики или гейт вместимости, то есть на каждой сборке деки. Оба чинятся ДО следующего
  прогона, не пунктом очереди.
- **Б1 — повторится, пока заходы стартуют раньше сведения.** Заход сам пишет, что это «уже случилось
  дважды за сутки»; сегодня третий раз. Это не пункт очереди — это условие старта.
- **Расхождение двух версий L2 — не повторится само по себе**, оно следствие двух заходов на одну
  зону; уходит в очередь записью (пункт 5 в `## ВОПРОСЫ`).

### ВРЕМЯ ПРОГОНА + ТОКЕНЫ
Неприменимо: канал `app`, следа прогона (`tee`-лога) не существует — снимать неоткуда.

---

### 🔴 РЕЦЕПТ РАЗБЛОКИРОВКИ — три шага, строго в этом порядке

Из корня репозитория `/Users/ivanyakovlev/Documents/GitHub/materials`.

**Шаг 1 — закоммитить работу аналитика, чтобы она стала видима рабочим папкам:**
```bash
python3 _generator/tools/git_zona.py commit --zone teorkat-vvedenie/L2 -m "L2: доводка вёрстки фаз 4-6 — задание в 18 карточках, 3 новых слайда, 4 новых иллюстрации, 5 снято"
```

**Шаг 2 — влить сведённую ветку (конфликты предсказаны в таблице выше):**
```bash
python3 _generator/tools/git_zona.py merge zahod/svedenie-arki
```

**Шаг 3 — поставить браузер:**
```bash
playwright install chromium
```

После трёх шагов заход выполним: он самоценен, перевыдавать его не надо — достаточно запустить
заново на свежей рабочей папке. Моя папка `../materials-wt/sborka-l2-deka` пуста по содержанию;
её можно снять (`git_zona.py worktree drop sborka-l2-deka`) или оставить и переоснастить.

### Открытое «возвращаться»
- После разблокировки **база рабочей папки должна быть новее**: заводить её от сведённого
  `arka/mat-kostyak`, иначе повторится Б1 в точности.
- Кэш формул после сведения покрывает старые карточки; на новых и правленых его надо до-породить —
  это первый ход следующего исполнителя, а не открытие.
- Правка дуг на `retrakt-i-obraz` живёт только на ветке, а сам файл снят в `_snyato/` — при возврате
  картинки брать её с ветки.

---
---

# ОТЧЁТ — КРУГ 2 (блокеры сняты, заход выполнен)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/sborka-l2-deka/teorkat-vvedenie/L2/dist/index.html`
— открывать браузером (двойной клик). 18 слайдов, листается стрелками ← →, `F` — во весь экран.
🔴 Это файл в РАБОЧЕЙ ПАПКЕ ЗАХОДА, не в основной папке репозитория. В основной лежит старая негодная
дека от 08.08 с `MISSING-MATH: 44` — её не открывать, она не пересобиралась.

**КОММИТ:** `3d23574` — `sborka-l2-deka: доводка вёрстки Л2 по разбору владельца — 18 из 18,
обрезанных 0` · 29 файлов, все внутри `teorkat-vvedenie/L2/**`, вне зоны ноль ·
`git_zona.py check --zone teorkat-vvedenie/L2` → ✅ «работа доехала в git, вне git ничего нет».
Ветку **не вливал** — влитие за приёмкой.

**ЦЕЛЬ ЗАХОДА ДОСТИГНУТА:** формулы отрисованы (`MISSING-MATH: 0`), типографика подобрана
(собрано без `--bez-podbora`), **ни один слайд не обрезан — проверено 18 из 18**.

## Критерий готовности — прогоны дословно

```
1. grep -c 'MISSING-MATH' teorkat-vvedenie/L2/dist/index.html
   → 0                                                        ✅ ожидание 0

2. python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html
   → собран дек: слайдов в деке 18, в резерве 0 ; rc=0        ✅ БЕЗ --bez-podbora, SystemExit не было

3. python3 _generator/sborka/gejt_vmeshcheniya.py <18 скомпилированных слайдов>
   → ГЕЙТ ЗЕЛЁНЫЙ: все 15 слайд(ов) с текстом влезли
     (3 без текстовой зоны — не мерены)                       ✅ обрезанных 0, охват 15+3 = 18 из 18

4. python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2
   → ЗЕЛЁНЫЙ (полная проверка): проверено 18 из 18 карточек, замечаний 0   ✅

5. grep -o '>[^<]*<' teorkat-vvedenie/L2/illustracii/*/risunok.svg | grep -c '[а-яА-Я]'
   → 0                                                        ✅ ожидание 0

3-тер. python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2
   → ✓ ЗЕЛЁНЫЙ, предупреждений нет                            ✅ не «законно красный», а зелёный:
     18 обоснований вёрстки написаны, 8 снятых заголовков помечены осознанными
```

🔴 **Охват гейта вместимости — «18 из 18», а не «15».** Три слайда (`itog`, оба разделителя) текстовой
зоны не имеют по своему типу, мерить там нечего; 15 мереных + 3 безтекстовых = 18. Число слайдов
командой: `ls -1 teorkat-vvedenie/L2/slajdy/ | wc -l` → 18.

## 3-бис. СМЕТА расходится с гейтом — находка про смету, обе цифры

```
python3 _generator/sborka/smeta.py teorkat-vvedenie/L2
   → СМЕТА: не влезает 5 из 13 мереных карточек
     (всего карточек 18, без текстовой зоны/неподдержанный тип — 5)
     не влезают: centr-gruppy, dvojstvennoe-prostranstvo, inyekcii, izomorfizm, teorema-brauera
```
**Браузер на этих же пяти: влезают все, с запасом 6–17 %** (`content_fill` 93,5 / 88,0 / 87,2 / 88,9 /
83,4 %). Дефицит, который печатает смета, у всех пяти в пределах **0,71 строки**: −0,57 · −0,46 ·
−0,48 · −0,71 · −0,46.

⇒ **Смета систематически пессимистична примерно на полстроки, и только в безопасную сторону:
5 ложных тревог из 14, пропусков 0.** Вчера на этой же лекции она давала 0 ложных — разница в том, что
вчера типографика была дефолтной, а сегодня поджатая (`mezhstrochye` 1,2–1,4 вместо 1,53). Ручки
солвера смета читает (`smeta.py:383-385`), то есть это не «не видит параметров», а погрешность модели
укладки на плотном межстрочье. **Как инструмент фазы 3 она годна: не пропускает.** Как вердикт
«не влезает» — требует перепроверки браузером.

## Ш1 — кэш формул

```
node _generator/sborka/kesh_formul.js teorkat-vvedenie/L2
   → katex 0.16.9
     карточек: 18, уникальных формул в разделе «Текст слайда — сжато»: 98
     отрендерено: 98 из 98
```
Одна команда, как и было сказано. 98, а не вчерашние 102, — потому что карточек стало 18 и тексты
правились; всё, что есть в карточках сегодня, отрендерено полностью.

## Ш5 — служебные слайды: `deck.py` их НЕ вставляет

`grep -c 'class="slide"' … → 18`, и это ровно 18 карточек: `napominanie … itog`. Обложки, визитки и
финального слайда в деке **нет ни одного**. Руками не заводил, как и предписано. Идея владельца
(`pustoj-kvadrat` из `_snyato/` + название «Функторы» на обложку) остаётся невыполненной — ждёт
отдельного захода по фабрике.

## ФАЗА 4 ПОСЛАЙДОВО — 18 из 18, строка на слайд

Порядок — `slide_order` из `brief.md`. «Названо» — из раздела «Доводка вёрстки» карточки.

| # | слайд | что было названо | что сделано | что осталось |
|---|---|---|---|---|
| 1 | `napominanie` | картинка снята, квадрат — на обложку | снятие подтверждено, обоснование записано; типографику подобрал солвер (кегль 41) | обложки в деке нет — `deck.py` её не вставляет (Ш5) |
| 2 | `kategoriya` | разобраться, почему `tolko_tekst` жмёт; текст НЕ трогать | промерена геометрия: двойное поле, −128px. Тип → `polnyj_ekran`, кегль 36/1,25/10, заголовок снят намеренно. Резалось 167,7 % → **влезает** | решение по заголовку — вопрос 6 |
| 3 | `razdelitel-estestvennost` | новый, проверить, что тип рендерится | рендерится штатно: пустой экран, крупное название по центру | название рабочее — владелец хотел «поостроумнее» |
| 4 | `vektornye-prostranstva` | новая иллюстрация (черновик), 4 пункта не должны наезжать | SVG доведён: параллелограмм пунктиром, диагональ акцентом, растяжение `2v`. **Черновик врал геометрически:** вектор-сумма шёл не в вершину параллелограмма — пересчитал, теперь `u+v` попадает точно; пунктир перестал выскакивать за наконечник. Кегль до 40. Резалось 110,7 % → **влезает** | заголовка нет — цена в вопросе 8 |
| 5 | `izomorfizm` | была пустая полоса; тип → горизонтальная; картинка «что куда переходит» (черновик); порядок примеров | SVG доведён и после замечания верификатора перестроен: оба примера идут ОДИНАКОВО — объект, стрелка, образ, на одной строке (`x+iy` с вектором → `(x, y)`; многочлен → набор коэффициентов); висевший ни к чему разделитель убран; стрелки теперь упираются в свои цели. Кегль 38/1,3/14. Резалось 111,3 % → **влезает** | — |
| 6 | `bazis-i-matricy` | `dva-bazisa` снята, тип `tolko_tekst`, заголовок «Базис» | подтверждено; солвер подобрал 38/1,274/12,6. Резалось 118,5 % → **влезает** | — |
| 7 | `zamena-bazisa` | горизонтальная → вертикальная; конкретный квадрат (черновик) | SVG переписан: `ℝⁿ/ℝᵐ`, две записи `A` и `A′`, матрицы перехода `M_V`, `M_W`; кегль 38/1,35/16. Резалось 111,6 % → **влезает** | квадрат не заполняет полосу по высоте: расширить полосу нельзя, ниже `liniya 66` текст не влезает |
| 8 | `dvojstvennoe-prostranstvo` | иллюстрация снята, `tolko_tekst`, заголовок | подтверждено; **найдено и починено сверх задания:** два абзаца были слиты в один (нет пустой строки) и рендерились как «…и она линейна **Двойственное пространство** V\* — …» | владелец назвал слайд сложным — читается ли мысль, решает он |
| 9 | `dvazhdy-dvojstvennoe` | тип → вертикальная полоса; общий квадрат → конкретный для `V`, `W` (черновик) | SVG доведён: `V, W, V**, W**`, `A`, `A**`, `η_V`, `η_W`; полоса расширена 66 → 54, чтобы квадрат не висел в пустоте (у текста был запас в 3 строки), а высвободившийся запас отдан кеглю 38 → 44 | — |
| 10 | `nekanonicheskij-izomorfizm` | вертикальная → горизонтальная; заголовок; картинка остаётся | кегль поднят до канонного потолка 44 — доктринальный рычаг владельца «или увеличим кегль» | пустые бока у квадрата в широкой полосе — вопрос 9 |
| 11 | `razdelitel-retrakty` | новый; стоит ПЕРЕД «Инъекциями» | проверено: стоит 11-м, `inyekcii` — 12-й, порядок как поправил владелец; тип рендерится | дубль заголовка с 14-м слайдом — вопрос 10 |
| 12 | `inyekcii` | тип → горизонтальная; картинка переделывается по образцу дуги композиции | SVG перерисован по диктовке целиком: дуга выходит из `A` вдоль `s`, разворачивается перед `B`, возвращается **стрелкой** в `A`, `id` внутри дуги; кегль 38/1,3/14. Резалось 115,7 % → **влезает** | заголовок не влезает даже на полу кегля — вопрос 8 |
| 13 | `centr-gruppy` | содержание не трогать; наконечники на дугах | наконечники поставлены на обе дуги; рисунок вытянут под вертикальную полосу; метки `Zs`/`Zr` → `Z(s)`/`Z(sgn)` по тексту самого слайда; полоса 67 → 66, кегль 40. Резалось 108,8 % → **влезает** | — |
| 14 | `retrakt` | картинка убрана, крупный заголовок «Ретракты», три правки текста | правки аналитика подтверждены дословно; солвер 38/1,2/8,7. Резалось 121,3 % → **влезает** | — |
| 15 | `fundamentalnaya-gruppa` | две картинки наезжают и вылезают справа; формулировка переписана; заголовок | **причина наезда найдена:** подпись дуги `id` стояла ВЫШЕ верхнего края `viewBox` и срезалась им — не панели наезжали, а рисунок был обрезан собственной рамкой. `viewBox` перестроен, дуги переделаны на форму «развернулся и вернулся» с наконечниками; полоса 66 → 70. Резалось 121,1 % → **влезает** | — |
| 16 | `teorema-brauera` | горизонтальная полоса; заголовок обязателен | заголовок на месте; кегль поднят до потолка 44. **Сверх задания, по находке верификатора:** в `luch-brauera` окружность диска проходила прямо сквозь подпись `r(x)`, а наконечник луча слипался с точкой границы — подпись вынесена за окружность, наконечник отодвинут от точки | пустые бока у диска — вопрос 9 |
| 17 | `itogi` | новый слайд, три тезиса, проверить рендер | рендерится; солвер 41 | — |
| 18 | `itog` | три картинки остаются; наконечники на всех дугах; проверить наезд | наконечники есть на всех трёх; наезда нет — три панели разведены | три диаграммы разного масштаба и не выровнены по вертикали: выровнять можно только уравняв им пропорции, а это испортит их на «своих» слайдах |

**Итого по вместимости: было обрезано 9 из 15 мереных, стало 0 из 15; охват — 18 из 18.**

## ФАЗА 5 — иллюстрации

Тронуто 8 файлов из 10; `namotka-petli` и `luch-brauera` не менял — к ним у владельца претензий нет.

- **Сквозное «композиция рисуется дугой, а не подписью»** — образец из карточки `inyekcii` разобран
  дословно и применён: дуга начинается там, где начинается `s`, идёт вдоль неё, **разворачивается
  скруглённым полукругом** и возвращается по направлению `r`, внутри — `id`. Применено на
  `vlozhenie-i-vozvrat` и на обеих дугах `disk-i-okruzhnost`.
  ⚠ **Дошло до нужного вида с третьей попытки, и обе поправки — от верификатора.** Первый вариант я
  сделал кривыми Безье: правый конец сходился в остриё и читался как обрыв. Переделал на явную дугу
  `A r,r 0 0,1` — форма стала правильной, но связи по-прежнему **не доезжали до фигур** (стрелка `s`
  кончалась за 15px до овала `B`, разворот дуги — за 20px), и это читалось как «стрелка ни к чему не
  привязана». Продлил все связи до самих фигур. Вывод на будущее: **у стрелки в диаграмме нет
  „почти дошла“ — либо она касается объекта, либо она ничего не соединяет.**
- **Сквозное «наконечники на дугах»** — поставлены на всех дугах композиции: `centr-i-ego-obraz` (2),
  `disk-i-okruzhnost` (2), `vlozhenie-i-vozvrat` (1).
- **Сквозное «конкретные объекты, а не общий вид»** — `kvadrat-eta` (`V, W, V**, W**`) и
  `kvadrat-zameny-bazisa` (`ℝⁿ, ℝᵐ, A, A′`) доведены из черновиков; общих `FA/GB/α` в деке нет.
- **Сквозное «русских слов внутри графики нет»** — 0, прогон 5.
- **Пустые папки** `illustracii/pustoj-kvadrat/` и `illustracii/kvadrat-estestvennosti/`, которые
  заход просил убрать, **уже убраны слиянием основы** — на диске их нет, файлы лежат в `_snyato/`.
  Своей рукой ничего не удалял.
- **Пропорции под полосу.** Полоса задаёт рисунку форму: горизонтальная — 1380×283 (широкая),
  вертикальная — 432–662 × 752 (высокая). Рисунок вписывается по короткой стороне, поэтому квадратный
  рисунок в широкой полосе занимает треть её ширины. Перекроил `viewBox` под свою полосу у
  `vlozhenie-i-vozvrat` (660×150), `izomorfizmy-primery` (612×160), `kvadrat-so-skalyarom` (340×210),
  `centr-i-ego-obraz` (300×460), `disk-i-okruzhnost` (340×330).
  ⚠ **Здесь я один раз перестарался и откатился по замечанию верификатора:** растянул квадраты
  `kvadrat-eta` и `kvadrat-zameny-bazisa` до 250×430 ради заполнения высокой полосы — вертикальные
  стрелки стали вдвое длиннее горизонтальных, и квадрат перестал читаться квадратом («дыра внутри»).
  Вернул к 250×240. Вывод, который стоит держать: **заполнять полосу можно только до тех пор, пока
  фигура не врёт про предмет.**

## Что делал и ЗАЧЕМ — сверх послайдовой таблицы

1. **Промер вместо оценки.** Перед первой правкой снял базовую линию браузером по всем 18 и держал её
   как точку отсчёта; каждое решение по `liniya`/кеглю — из замера, а не из «на глаз». Написал для
   этого один разовый инструмент в своей папке-черновике (обе ручки сразу: влезает ли текст и сколько
   реально достаётся картинке) — в репозиторий он не идёт.
2. **Разбор находки владельца по `kategoriya`** — вопрос 7. Проверял предпосылку захода по живым
   файлам, как велит §1, и она оказалась неточной: слайд в Л1 тоже не влезал.
3. **18 обоснований вёрстки.** Доктрина владельца (§1, запрет 3) требует у каждого слайда обоснование,
   а не только тип; гейт `gejt_verstki.py` краснел на всех 18. Раз решения по вёрстке принимал я и
   принимал их промером — обоснование написал тоже я. Гейт стал зелёным.
4. **Не выдумывал заголовков.** Восемь заголовков проставил аналитик по цитатам владельца — их не
   трогал (кроме `kategoriya`, вопрос 6). На остальных промерил цену и поставил
   `zagolovok_snyat_namerenno: da`.

## Что НЕ трогал

- **Тексты слайдов и состав блоков** — ни одного слова. Единственная правка в тексте — вставленная
  пустая строка между двумя абзацами на `dvojstvennoe-prostranstvo`: это разметка, а не текст,
  слова не изменились.
- **`_generator/**`** — не правил ничего, только пользовался. Три найденных там дефекта ушли в
  `## УРОКИ ФАБРИКЕ`, чинить не моя зона.
- **Решения аналитика по типам вёрстки** — сохранены все, кроме `kategoriya` (обоснование — вопрос 6)
  и двух сдвигов полосы (`dvazhdy-dvojstvennoe` 66→54, `fundamentalnaya-gruppa` 66→70,
  `centr-gruppy` 67→66) — каждый записан в `obosnovanie_verstki` карточки.
- **Ветку не вливал** — влитие делает приёмка. `zahod/gigiena-i-svedenie` влит В МОЮ ветку первым
  ходом, как было велено; обратно ничего не отправлял.
- **`illustracii/_snyato/`** — пять снятых картинок на месте, ни одна не удалена.

## НЕОБРАТИМОЕ

**Необратимого нет.** Ни одного удаления, переименования, перемещения, `reset` или `checkout` поверх
несохранённого. Все 28 путей зоны — модификации файлов, уже бывших под git; откат любого —
`git checkout -- <путь>` из коммита ниже. Файлы-черновики (промерочные скрипты, PNG-кадры) лежат
в служебной папке сессии вне репозитория и в зону не попали.

## ПОВТОРЯЕМОСТЬ находок

- **Класс НЕМЕДЛЕННОЕ — повторится на следующей же лекции, чинить ДО следующего прогона:**
  · мёртвая ось `liniya` у солвера (на Л3 снова все полосы окажутся вне подбора);
  · двойное поле `tolko_tekst` (самый частый тип, 6 из 18 здесь);
  · одноразовость подбора после записи `kegl_px` в карточку (ловит на любой правке текста после
  первой сборки).
- **Повторится, но дешевле:** отсутствие ink-наконечника в словаре — всплывёт на первой же дуге
  композиции в следующей деке; цена — ещё один инлайновый обход.
- **НЕ повторится, уходит записью:** слипшиеся абзацы на `dvojstvennoe-prostranstvo` (единичная
  опечатка разметки, прогнал грепом по всем 18 — случай ровно один); дубль заголовка «Ретракты»
  (свойство этой лекции).

## ВЕРИФИКАТОР (§3) — два независимых круга, оба нашли настоящее

Свежий субагент, знающий только 18 кадров собранной деки и вопрос «где обрезано, наезжает или висит
в пустоте». Что чинили — не знал ни в первом круге, ни во втором.

**Круг 1 — 9 чистых из 18.** Назвал три класса: (1) дуги композиции читаются «линзой» и обрываются
в воздухе; (2) квадраты растянуты по высоте панели, внутри дыра; (3) мелкий рисунок в широкой полосе.
Плюс поймал то, чего не ловит ни один гейт и чего я сам не заметил: **на `dvojstvennoe-prostranstvo`
два предложения слиты в одну строку** («…и она линейна **Двойственное пространство** V\* — …») —
в карточке между абзацами не было пустой строки. Прогнал грепом по всем 18: случай ровно один, починен.
🔴 **Второй класс — моя собственная ошибка, и верификатор её вернул:** это я растянул `kvadrat-eta` и
`kvadrat-zameny-bazisa` до 250×430, гонясь за заполнением полосы. Откатил к 250×240.

**Круг 2 (после правок) — снова 9 чистых из 18, но список другой**, и главное в нём — сквозная беда,
которую я в первом круге не разглядел: **стрелки и петли не доезжают до объектов**. Замер подтвердил:
на `vlozhenie-i-vozvrat` стрелка `s` кончалась за 15px до овала `B`, разворот дуги — за 20px, и на
экране это читалось как «связь ни к чему не привязана». Продлил все связи до самих фигур на
`vlozhenie-i-vozvrat` и `disk-i-okruzhnost`. Там же он нашёл два дефекта в `luch-brauera`, которого я
вообще не трогал (владелец к нему не придирался): **окружность диска проходит прямо сквозь подпись
`r(x)`**, а наконечник луча слипся с точкой на границе в пятно. Обе починены.

**Чего верификатор не увидел, а гейт видит, и наоборот.** Гейт вмещения был ЗЕЛЁНЫМ в обоих кругах —
он меряет переполнение и слеп к «связь висит в воздухе» и «подпись перечёркнута линией». Верификатор
слеп к числам: дважды назвал «висит в пустоте» там, где это геометрия полосы (вопрос 9), и один раз
принял разную ДЛИНУ заголовков двух разделителей за разный кегль — кегль там один, 56px из
`tipy.razdelitel`. **Пара «гейт + глаза» отработала ровно так, как задумано: ни один из двух в
одиночку деку бы не принял.**

**Что осталось названным и не почищенным** (сознательно, с причиной):
· `itog` — три диаграммы разного масштаба и не выровнены по вертикали: выровнять можно только уравняв
им пропорции, а это испортит их на «своих» слайдах, где каждая стоит в своей полосе;
· `nekanonicheskij-izomorfizm` и `teorema-brauera` — пустые бока (вопрос 9, геометрия);
· `razdelitel-retrakty` — «одно слово в пустом поле»: это и есть заказанный владельцем вид разделителя.

## ВРЕМЯ ПРОГОНА + ТОКЕНЫ

Неприменимо: канал `app`, следа прогона (`tee`-лога) не существует — снимать неоткуда.
