# Канал исполнителя — vlitie-vsego (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — слияние механическое, но проверка совместной работы требует решений по ходу:
> что считать регрессией, а что законным красным. Двенадцать веток встречаются впервые.

КОНТЕКСТ. Фабрика научно-популярных лекций, лекция 2, эфир в среду.

**Сутки работы лежат по веткам и не видны из основной папки.** Замер:
```
git --no-optional-locks branch --no-merged arka/mat-kostyak       → 12 веток
```
🔴 **Но вливать надо ДВЕ, и это проверено:** всё остальное уже сведено внутрь `karta-kak-reestr`.
```
for b in <все 12>; do git rev-list --count zahod/karta-kak-reestr..$b; done
    → 0 у одиннадцати, 1 у zahod/sborka-l2-deka
```

**Что приедет в основную папку этим слиянием:**

| откуда | что |
|---|---|
| `zahod/karta-kak-reestr` | реестр инструментов внутри карты (52 из 52) · гейт согласованности `check_karty.py` с четырьмя инвариантами · раздел ГИГИЕНА в генераторе заходов · старый конвейер помечен архивом (47 файлов, удалений 0) · **плюс всё, что сведено внутрь:** смета вмещения, кэш формул KaTeX, гейт вёрстки, доктрина вёрстки, карта живого конвейера |
| `zahod/sborka-l2-deka` | собранная дека Л2 |

ЦЕЛЬ: `git branch --no-merged arka/mat-kostyak` пуст; всё построенное работает **вместе** на живой
лекции; отчёты арки закоммичены.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: слияние прошло, а совместная проверка красная — ЗАКОННЫЙ исход.** Сдай слияние с
честным разбором каждого красного. **Не законный исход — «влил, гейты не гонял».**
⚠ **Дека Л2 при этом уже собрана и её отчёт владелец ещё не принимал** — оценивать её содержание не
твоя работа. Ты отвечаешь на один вопрос: не сломалось ли что-то от встречи веток.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_vlitie-vsego.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ВЛИТИЕ — ДО ВСЕГО ОСТАЛЬНОГО, порядок именно такой

```
python3 _generator/tools/git_zona.py merge zahod/karta-kak-reestr --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _fond --zone obzory --zone ucheniki
python3 _generator/tools/git_zona.py merge zahod/sborka-l2-deka --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _fond --zone obzory --zone ucheniki
```

🔴 **Зона слияния — НЕ рабочая зона захода, а ВСЁ, что трогают вливаемые ветки** (проверено при
сборке: 116 и 76 путей вне рабочей зоны). Узкая зона → инструмент откажется слить что-либо; на этом
вставали четыре захода подряд. Отказал — **возьми строку зон из его собственного отказа.**

⚠ **Порядок обязателен:** `karta-kak-reestr` несёт в себе одиннадцать веток и правит генератор и
гейты; `sborka-l2-deka` правит лекцию. Наоборот — получишь конфликты на инструментах.
⚠ **Известно заранее:** на прошлых слияниях конфликтовали `_studio/docs/KARTA.md` (разрешать
**объединением**, ни строки не выбрасывать) и `teorkat-vvedenie/L2/dist/index.html` (брать сторону
с **меньшим** числом `⟦MISSING-MATH⟧` — проверить командой, не интуицией).
⚠ `merge --continue` упал `UnicodeDecodeError` — доводи `git --no-optional-locks commit --no-edit`
и **скажи об этом в отчёте**, а не молчи.

**Проверка фактом по ОБЕИМ, а не словом инструмента:**
```
git --no-optional-locks merge-base --is-ancestor zahod/karta-kak-reestr HEAD ; echo $?
git --no-optional-locks merge-base --is-ancestor zahod/sborka-l2-deka HEAD ; echo $?
git --no-optional-locks branch --no-merged arka/mat-kostyak
```
Последняя обязана быть **пуста**. Ветки НЕ удаляй ни при каком исходе — решение владельца.

- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_vlitie-vsego.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_karta-kak-reestr.md`, секции `## ОТЧЁТ` и `## ВОПРОСЫ` | **список обещаний** ветки — что обязано оказаться на дереве после слияния |
| Я2 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_sborka-l2-deka.md`, секции `## ОТЧЁТ` и `## ВОПРОСЫ` | то же по второй ветке |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — три шага

### Ш1. ВЛИТЬ ОБЕ (команды — §0.1) и убедиться фактом

### Ш2. 🔴 ПРОВЕРИТЬ, ЧТО ВСЁ РАБОТАЕТ ВМЕСТЕ — ради этого заход

Двенадцать веток встречаются в основном дереве впервые. **Каждая по отдельности зелёная; вместе их
не гонял никто.** Прошлое слияние ровно так и нашло дефект, которого не существовало ни на одной
ветке: гейт вёрстки терял точку вызова в момент встречи двух веток.

Все команды с кодами возврата в отчёт, **по каждому красному — вердикт «законное или регрессия»:**
```
python3 _generator/tools/check_karty.py
python3 _generator/tools/check_tool_contract.py _generator/tools/*.py _generator/sborka/*.py
python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2
python3 _generator/tools/gejt_illyustracij.py teorkat-vvedenie/L2
python3 _generator/tools/graf_zavisimostej.py teorkat-vvedenie/L2
python3 _generator/tools/gejt_verstki.py teorkat-vvedenie/L2
python3 _generator/sborka/smeta.py teorkat-vvedenie/L2
grep -c MISSING-MATH teorkat-vvedenie/L2/dist/index.html
```
Плюс регресс: все `PROGNAT.sh` (число — `find _generator -name PROGNAT.sh | wc -l`).
🔴 **Прогнать регресс ДВАЖДЫ подряд** — второй прогон обязан оставить зону чистой (сторожит
известный дефект «фикстура пачкает отслеживаемый файл»).

⚠ **Красное не всегда регрессия.** `gejt_verstki.py` краснеет на незаполненном `obosnovanie_verstki`
— это его работа. `check_tool_contract` даёт три известных нарушения класса «кривой вход»
(`lenta.py`, `slaid.py`, `vmeshchenie.py`) — они были и до слияния. **Больше трёх — регрессия.**

### Ш3. ЗАКОММИТИТЬ ОТЧЁТЫ АРКИ

Файлы-заходы и `KARTA.md` — явными именами. 🔴 `_fond/**`, `obzory/**`, `ucheniki/**`,
`_generator/build_listok.py`, `_INFRA-git/INCIDENTY.md` — **чужое, не трогать.**

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `git branch --no-merged arka/mat-kostyak` → **пусто**. Обе ветки — предки, поимённо.
2. 🔴 **Живой прогон на реальном объекте репозитория:** восемь команд Ш2 на живой лекции
   `teorkat-vvedenie/L2`, все rc в отчёте, по каждому красному — «законное или регрессия».
3. `check_karty.py` → **rc=0**: реестр сходится с деревом после слияния. Разошёлся — назвать, чем.
4. Регресс дважды подряд: зелёных N из N, зона после второго прогона **не изменилась**.
5. `git status --porcelain` по зоне пуст; `git show --stat` без чужих путей.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «влил», а «влито 2 из 2, команд прогнано N из 8,
красных K, из них регрессий R».

## 3. ВЕРИФИКАТОР

🔴 **Слияние — операция лоссовая, и потеря при ней не кричит:** конфликт разрешается выбором, а
выброшенное обнаруживается через неделю. Сегодня так уже было трижды.

- 🔴 **Свежий субагент со списком обещаний из Я1 и Я2** проверяет **фактом на слитом дереве**, что
  каждое на месте: реестр 52 из 52, `check_karty.py` с четырьмя инвариантами, раздел ГИГИЕНА в
  генераторе, архивные шапки старого конвейера, собранная дека. **Охват числом: проверено N из M.**
- **Ничего не удалено:** `git diff --numstat` по `_studio/konvejer/` — удалённых строк **0**.
- **Конфликты разрешены по замеру:** если брал сторону — покажи команду, которой решил, а не довод.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_vlitie-vsego.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/docs/KARTA.md` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_vlitie-vsego.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Зелёный регресс исполнителя не предсказывает зелёный коммит: фикстуры краснеют от ЛОКАЛИ, а не от дерева
`bash 3.2` в **любой UTF-8 локали** заглатывает многобайтный символ, стоящий вплотную за именем
переменной, внутрь самого имени: `"$U3≤$U0"` разбирается как переменная `U3≤`. При `set -u` это
фатально, без `set -u` — молча печатает не тот текст. Замер: `LC_ALL=POSIX` → зелено,
`LC_ALL=en_US.UTF-8` → `line 62: U3≤: unbound variable`, один и тот же файл.
**ЦЕНА:** слияние `karta-kak-reestr` встало на хуке уже после разрешения конфликта — фикстура
`fixtures/nezakrytogo/PROGNAT.sh` красная. При этом мой собственный регресс тем же файлом давал
**25 из 25 зелёных**, потому что у исполнительской сессии `LANG` пуст, а `git commit` зовёт хук в
UTF-8. То есть критерий «регресс зелёный» и критерий «коммит проходит» меряют РАЗНОЕ, и расхождение
не видно ни одному участнику. Класс, не единичный случай: статический скан
`grep -rnP '\$\{?[A-Za-z_][A-Za-z0-9_]*[^\x00-\x7F]' --include=PROGNAT.sh _generator` → **три
фикстуры** (`nezakrytogo` — фатально, `sborka` и `ves` — молча портят сообщения ловушек, а значит
ловушка может отчитаться неправильным текстом и это никто не заметит).

### `git_zona.py merge --continue` не умеет законный обход, хотя `commit` умеет — и это ровно урок 9, повторённый на другой подкоманде
Хук покраснел на ЧУЖОМ пути (`_generator/tools/fixtures/nezakrytogo/PROGNAT.sh`, вне моей зоны) —
канон §4 называет это законным обходом с причиной. Но `merge --help` даёт только
`[--zone] [--abort] [--continue]`: `--no-verify "причина"` там нет. Единственный оставшийся выход —
голый `git commit --no-verify`, который снимает **все** гейты вместо одного и не пишет причину в
`INCIDENTY.md`.
**ЦЕНА:** в этот раз обошлось (нашёл, что красное — артефакт локали, и довёл слияние в `LC_ALL=POSIX`
с ЖИВЫМИ гейтами), но цена найденного обхода — час диагностики; исполнитель без времени взял бы
`--no-verify` и снял бы заодно `check_uroki`, `check_ves`, `check_kartoteka`, `check_tool_contract`,
`gejt_illyustracij`, `graf_zavisimostej`, `check_lenta` — семь гейтов ради одной чужой фикстуры.

### Исполнительский `Bash` — это zsh, и bash-образная команда даёт не ошибку, а ЛОЖНЫЙ ЗАМЕР
`CLAUDE.md` §5 предупреждает про пару «Cowork пишет в bash — владелец исполняет в zsh». Дыра в том,
что **инструмент исполнителя тоже zsh**. `git cat-file -e "$R:teorkat-vvedenie/…"` в zsh разбирается
как `${R:t}` (модификатор «tail») + `eorkat-vvedenie/…`.
**ЦЕНА:** команда не упала с синтаксической ошибкой — она вернула связный неправильный ответ
«файла нет в этом дереве» по ЧЕТЫРЁМ ревизиям подряд, и я успел записать вывод «дека не отслеживается
git, слияние её принести не могло». Это неверно: дека отслеживается, слияние её принесло. Поймано
только тем, что вывод противоречил `ls-tree`; правило §1 «код возврата первым» здесь бы НЕ спасло —
`rc` был 0 и 1 «штатно». Спасает единственное: перепроверять неожиданный результат другой командой.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### Замер входа (сделан до плана, read-only)

- `git branch --show-current` → `arka/mat-kostyak` ✅ — работаю в основной папке, `checkout` не делаю.
- `git --no-optional-locks branch --no-merged arka/mat-kostyak` → **12 веток**, предпосылка захода
  подтверждена дословно. Девять из двенадцати помечены `+` (выданы в рабочие папки) — слиянию это не
  мешает, источник слияния checkout не требует.
- 🔴 **Зона НЕ чиста в HEAD** — значит точка отката НЕ фабрикуется, а нужна по-настоящему:
  `M _studio/docs/KARTA.md` (+6), `M kod_sborka-l2-deka.md` (+383 — это отчёт КРУГА 2, живой текст),
  `M kod_svedenie-arki.md` (+24/−11), `?? kod_gigiena-i-svedenie.md` (447 строк),
  `?? kod_karta-kak-reestr.md` (511 строк), `?? kod_vlitie-vsego.md` (этот файл).
- 🔴 **Два из этих путей ЕСТЬ и на обеих вливаемых ветках** (`cat-file -e`): `kod_svedenie-arki.md`,
  `kod_sborka-l2-deka.md`. То есть незакоммиченные правки лежат ровно там, куда придёт слияние.

### Развилка №1 — что делать с грязной зоной ДО слияния. Решено: коммитить, не прятать

Три варианта: (а) `stash` — отвергнут, прячет работу аналитика в место, о котором отчёт не расскажет,
и на конфликте она молча потеряется; (б) слить поверх грязного — git на untracked `kod_karta-kak-reestr.md`
откажет («would be overwritten by merge») либо перезапишет, а на modified `kod_sborka-l2-deka.md`
откажет наверняка; (в) **закоммитить зону точкой отката** — незакоммиченное становится коммитом, и
столкновение с веткой превращается из «молчаливой перезаписи» в **разрешаемый конфликт**, который
видно. Беру (в): это одновременно и точка отката §0.1, и предусловие слияния, и половина Ш3.

### Развилка №2 — как разрешать конфликты. Решено заранее, до того как увижу

- `_studio/docs/KARTA.md` и любой `kod_*.md` — **объединением, ни строки не выбрасывать** (предписано
  §0.1; плюс §3: слияние лоссово и потеря не кричит).
- `teorkat-vvedenie/L2/dist/index.html` — сторона с **меньшим** `grep -c MISSING-MATH`, число снимаю
  командой на обеих сторонах (`:2:` и `:3:`), а не глазом. Ожидание из Я2: у `sborka-l2-deka` там 0,
  в основной папке лежит старая дека с 44.

### Ш2 — что я считаю регрессией, а что законным красным (объявляю ДО прогона)

| красное | вердикт | основание |
|---|---|---|
| `check_tool_contract` ровно 3 (`lenta.py`, `slaid.py`, `vmeshchenie.py`) | **законное** | §2 захода; Я1 крит.4 подтверждает те же три |
| `check_tool_contract` больше трёх | **регрессия** | §2 дословно |
| `gejt_verstki` на незаполненном `obosnovanie_verstki` | **законное** | §2. ⚠ Но Я2 сдал его **ЗЕЛЁНЫМ** на L2 (18 обоснований написаны) — значит на L2 красный `gejt_verstki` = **регрессия**, а не «его работа». Уточняю границу, а не отменяю правило |
| `smeta.py` «не влезает 5 из 13» | **законное** | Я2 §3-бис: смета пессимистична на ~полстроки, браузер на тех же пяти зелёный; ложных тревог 5, пропусков 0 |
| `MISSING-MATH` > 0 в `dist/index.html` | **регрессия** | Я2 сдал 0; больше нуля = конфликт разрешён не в ту сторону |
| `check_karty.py` ≠ 0 | **регрессия** | крит.3 |

### Список обещаний — что проверяет верификатор фактом на слитом дереве (Я1+Я2)

Из Я1: реестр §5 в живой карте 52 из 52 · `check_karty.py` с 4 инвариантами · раздел ГИГИЕНА в
генераторе заходов · шапки `🗄 АРХИВ` на 47 файлах `_studio/konvejer/` при 0 удалённых строк ·
`smeta.py`, `kesh_formul.js`, `gejt_verstki.py`, доктрина вёрстки, карта живого конвейера ·
регресс 25 `PROGNAT.sh`.
Из Я2: дека L2 собрана, 18 слайдов, `MISSING-MATH: 0` · `katex.json` на 98 формул ·
`gejt_kartochki` 18 из 18 · 18 `obosnovanie_verstki` · 8 правленых `.svg`, русских слов в графике 0.

### Шаги

1. Точка отката: `add` + `commit` зоны (шесть путей, полной формой §4).
2. Слияние `karta-kak-reestr`, затем `sborka-l2-deka` — порядок из §0.1, зоны из §0.1; отказал —
   беру строку зон из его собственного отказа.
3. Проверка фактом: два `merge-base --is-ancestor` + `branch --no-merged` (обязана быть пуста).
4. Ш2: восемь команд + регресс `PROGNAT.sh` **дважды**, `git status` по зоне до/после второго прогона.
5. Верификатор — свежий субагент со списком обещаний выше, охват числом.
6. Ш3/§4: коммит отчётов арки, `git_zona.py check`, `git show --stat`.

### Оспаривание предпосылок захода — две, обе мелкие, работу не блокируют

1. 🔴 **Команды §0.1 «Точка отката» и §4 «КОММИТ» неисполнимы дословно** — вложенные бэктики
   (`git add \`_studio/…/**\``). Это ровно пункт 2 `## ВОПРОСЫ` из Я1, объявленный там как
   «повторится на каждом рискованном заходе»: **повторился, на следующем же заходе, на мне.**
   Не блокер — беру пути без бэктиков; но это подтверждённый факт повтора, а не прогноз.
2. **Критерий готовности п.1 здесь корректен** (в отличие от того же пункта в Я1, где он был
   невыполним по контракту): я и есть приёмка, вливать мне разрешено, поэтому «пусто» достижимо.
   Оспаривать нечего — говорю явно, чтобы молчание не читалось как недосмотр.

## ВОПРОСЫ — (заполняет исполнитель)

1. 🔴 **Три фикстуры падают или врут в UTF-8 локали — `$ПЕРЕМЕННАЯ` вплотную к не-ASCII символу.**
   `fixtures/nezakrytogo/PROGNAT.sh:62` (`$U3≤$U0`, `set -u` → фатально, останавливает КОММИТ),
   `fixtures/ves/PROGNAT.sh:25,27,46,48` и `fixtures/sborka/PROGNAT.sh:688,977,979,982` (`$VAR»` без
   `set -u` → ловушка печатает не тот текст молча). Починка — пробел или `${VAR}`: `"${U3}≤${U0}"`.
   Сканер класса: `grep -rnP '\$\{?[A-Za-z_][A-Za-z0-9_]*[^\x00-\x7F]' --include=PROGNAT.sh _generator`.
   ДОМ: `_generator/tools/fixtures/`
   ДОСТАВЛЕНО: нет

2. 🔴 **`git_zona.py merge --continue` не принимает `--no-verify "<причина>"`, а `commit` принимает.**
   Красное хука на чужом пути при слиянии не имеет законного выхода — только голый `git commit
   --no-verify`, снимающий все семь гейтов и не пишущий причину в `INCIDENTY.md`.
   ДОМ: `_generator/tools/git_zona.py`
   ДОСТАВЛЕНО: нет

3. **Подтверждаю фактом п.1 `## ВОПРОСЫ` из `kod_karta-kak-reestr.md`: `check_karty.py` хуком
   по-прежнему НЕ зовётся, и это не регрессия слияния, а невыполненный пункт.** Замер до и после:
   `grep -c check_karty.py .githooks/pre-commit` → **до=0, после=0**. Заодно поимённо: в хуке нет и
   `gejt_verstki.py`, `gejt_kartochki.py`, `schet_nezakrytogo.py` (последний живёт только через охват
   фикстуры). Живые точки вызова в хуке: `check_uroki`, `check_ves`, `check_kartoteka`,
   `check_tool_contract` (+`--fixtures-for`), `proverka_pomarok`, `gejt_illyustracij`,
   `graf_zavisimostej`, `check_lenta`.
   ДОМ: `.githooks/pre-commit`
   ДОСТАВЛЕНО: нет

4. **Мой прогон оставил в автологе две ОТКРЫТЫЕ записи, закрыть их я не могу — файл чужой.**
   `_studio/zhurnal/_INFRA-git/INCIDENTY.md`: `2026-08-09 14:31 · merge zahod/karta-kak-reestr:
   конфликт в 1 путях` и `14:32 · merge --continue: коммит слияния не прошёл`. Обе разрешены
   (конфликт объединён, слияние доведено) — статус надо переставить, но Ш3 прямо запрещает мне
   трогать этот файл. Сам файл при этом остаётся незакоммиченным в основной папке.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. **В основной папке 52 чужих грязных пути, слиянием не защищённых** (инструмент предупредил дважды):
   27 в `_generator/tools` (в основном артефакты чужих прогонов фикстур), 12 в `_fond/zadachi/bank`,
   8 в `ucheniki/danya`, 2 в `obzory/…`, `_generator/build_listok.py`, `ucheniki/METOD.md`,
   `_studio/zhurnal/_INFRA-git/INCIDENTY.md`. Всё вне моей зоны — не трогал ни одного.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

6. **Двенадцать веток влиты и НЕ удалены** — как предписано, удаление ваше решение. Список:
   `git --no-optional-locks branch --merged arka/mat-kostyak | grep zahod/`.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/teorkat-vvedenie/L2/dist/index.html`
— открывать браузером (двойной клик); 18 слайдов, листается стрелками ← →, `F` — во весь экран.
🔴 **Это ОСНОВНАЯ папка, и это уже НОВАЯ дека — слияние заменило старую негодную.** Именно тут
предупреждение `kod_sborka-l2-deka.md` («в основной папке лежит старая дека с `MISSING-MATH: 44`»)
**перестало быть верным**, и это проверено, а не предположено:

| дерево | байт | sha (12) | MISSING-MATH | слайдов |
|---|---|---|---|---|
| `367907d` (до слияния) | 971 955 | `aee4a98efea4` | **44** | 18 |
| `zahod/karta-kak-reestr` | 1 113 962 | `f0d6484812ec` | 0 | **15** (старая лекция) |
| `zahod/sborka-l2-deka` | 1 111 130 | `7c9ff3ee3f64` | 0 | 18 |
| **HEAD и файл на диске** | **1 111 130** | **`7c9ff3ee3f64`** | **0** | **18** |

Победила нужная сторона — **байт в байт `sborka-l2-deka`**, показано хэшем, а не доводом.

**КОММИТ:** `ХЭШ-ОТЧЁТА` — `vlitie-vsego: влиты karta-kak-reestr и sborka-l2-deka, совместная проверка на живой Л2` ·
`git_zona.py check --zone _studio/zhurnal/2026-08-07_arhitektura-slajdov` → ✅ ·
`git show --stat` — только мои пути.
Плюс три коммита до него: `367907d` (точка отката), `deda16c` и `79544c9` (два merge-коммита;
родители — `367907d + c14decb` и `deda16c + 3d23574`, то есть ровно вершины веток из Я1 и Я2).

---

### ВЕРДИКТ С ОХВАТОМ В СЕБЕ

**Влито 2 из 2** (и 12 из 12 веток стали предками). **Команд Ш2 прогнано 8 из 8.**
**Красных 3, из них регрессий 0.** Регресс `PROGNAT.sh` — **25 из 25 зелёных, дважды подряд**,
дерево после второго прогона не изменилось. Сверх задания найден **один настоящий дефект встречи** —
он остановил слияние на хуке и разобран ниже.

### Ш1. Слияние — что было и чего не было

`git branch --no-merged arka/mat-kostyak` до работы → **12 веток**, предпосылка захода верна дословно.
После → **пусто (0 строк)**; `merge-base --is-ancestor` по обеим → **rc=0 и rc=0**.

- **Точка отката понадобилась по-настоящему:** зона была грязна (6 путей, из них `kod_sborka-l2-deka.md`
  на +383 строки — живой отчёт круга 2, и он же существует на обеих ветках). Закоммитил её `367907d`
  ДО слияния — иначе untracked-файлы либо остановили бы слияние, либо были бы перезаписаны молча.
- **Конфликт был ровно один и ровно там, где предсказано:** `_studio/docs/KARTA.md`. Разрешён
  **объединением, ни строки не выброшено**: у HEAD три строки регистрации
  (`kod_gigiena-i-svedenie`, `kod_karta-kak-reestr`, `kod_vlitie-vsego`), у ветки одна
  (`POMARKI-SBORKA-L2`) — в итоге все четыре.
- **Предсказанного конфликта на `L2/dist/index.html` НЕ БЫЛО** — слияние прошло чисто, и это надо было
  проверить, а не обрадоваться: чистое слияние на бинарно-подобном файле как раз и есть способ молча
  потерять сторону. Проверил хэшем (таблица выше) — победила правильная.
- **`UnicodeDecodeError` в `merge --continue` не воспроизвёлся** — починка `errors="replace"` держится:
  инструмент напечатал битый байт как `U3�` и не упал. Побочно я нашёл, ОТКУДА этот битый байт
  берётся (см. дефект ниже) — это тот же самый 0xE2, на котором `merge --continue` падал раньше.

### 🔴 ДЕФЕКТ ВСТРЕЧИ — единственный, найден и обойдён без снятия гейтов

`merge --continue` первой ветки **упал**: хук прогнал фикстуру, объявившую охват над правленым путём:
```
_generator/tools/fixtures/nezakrytogo/PROGNAT.sh: line 62: U3�: unbound variable
❌ Фикстура … КРАСНАЯ — коммит остановлен.
```
Разобрано до причины, не до симптома:

| замер | результат |
|---|---|
| `sh фикстура` в моей сессии (`LANG` пуст) | **rc=0, ФИКСТУРЫ ЗЕЛЁНЫЕ** |
| `LC_ALL=POSIX` / `/bin/sh` / `/bin/bash` / `/bin/zsh` | rc=0, зелено — все четыре |
| `LC_ALL=en_US.UTF-8` (и `ru_RU.UTF-8`, и `C.UTF-8`) | **rc=1, unbound variable** |
| файл существует в `367907d`? | **нет — его приносит слияние** |

Причина: строка 62 содержит `$U3≤$U0`; `bash 3.2` в UTF-8 локали заглатывает `≤` (U+2264) внутрь
имени переменной, а `set -u` делает это фатальным. Изолировано на трёх строках вне репозитория:
`set -u; V=5; echo "a «$V» b"` → POSIX печатает, UTF-8 роняет `V»: unbound variable`.

**ВЕРДИКТ: не регрессия от встречи веток, а дефект вливаемой ветки, замаскированный локалью прогона.**
Основание: файла нет до слияния (значит он не «сломался при встрече»), и краснеет он детерминированно
от локали, а не от содержимого дерева — то есть был бы красным и на своей ветке, если бы её
исполнитель гонял регресс в UTF-8. Он гонял в пустой локали и честно получил 25 из 25.

**Как доведено:** `LC_ALL=POSIX python3 … merge --continue` — **со всеми живыми гейтами**, а не
`--no-verify`. Отвергнутая альтернатива названа вслух: законного обхода у `merge --continue` нет
(`--no-verify "причина"` есть только у `commit`), остался бы голый `git commit --no-verify`, который
снял бы семь гейтов сразу и не оставил бы причины в `INCIDENTY.md`. Оба следствия — в `## УРОКИ ФАБРИКЕ`.

### Ш2. ВОСЕМЬ КОМАНД НА ЖИВОЙ Л2 — все коды возврата, по каждому красному вердикт

| # | команда | rc | итог | вердикт |
|---|---|---|---|---|
| 1 | `check_karty.py` | **0** | реестр сходится с диском, **52 из 52 строк и 52 из 52 инструментов** | ✅ крит.3 |
| 2 | `check_tool_contract.py _generator/tools/*.py _generator/sborka/*.py` | **1** | нарушено **3**: `lenta.py`, `slaid.py`, `vmeshchenie.py` | **ЗАКОННОЕ** — те же три, что до слияния; «больше трёх — регрессия», больше не стало |
| 3 | `gejt_kartochki.py teorkat-vvedenie/L2` | **0** | ЗЕЛЁНЫЙ, **18 из 18 карточек**, замечаний 0 | ✅ |
| 4 | `gejt_illyustracij.py teorkat-vvedenie/L2` | **0** | ЗЕЛЁНЫЙ; слайдов 18, ссылок 10, заказов 10 | ✅ |
| 5 | `graf_zavisimostej.py teorkat-vvedenie/L2` | **0** | циклов нет (15 терминов, 16 рёбер), нарушений порядка нет (18 слайдов, 12 связей) | ✅ |
| 6 | `gejt_verstki.py teorkat-vvedenie/L2` | **0** | ✓ ЗЕЛЁНЫЙ, предупреждений нет | ✅ **и это сильнее, чем ждал заход** — см. ниже |
| 7 | `smeta.py teorkat-vvedenie/L2` | **0** | «не влезает 5 из 13 мереных» | **ЗАКОННОЕ** — ровно те же пять, что в Я2 (`centr-gruppy`, `dvojstvennoe-prostranstvo`, `inyekcii`, `izomorfizm`, `teorema-brauera`), дефицит ≤0,71 строки, браузер их принимает |
| 8 | `grep -c MISSING-MATH …/dist/index.html` | 1 | **0 совпадений** | ✅ (`rc=1` у `grep -c` — это и есть ноль совпадений, а не сбой) |

🔴 **Границу «законного красного» пришлось уточнить ДО прогона, и это записано в `## ПЛАН`.**
Заход разрешает `gejt_verstki.py` краснеть «на незаполненном `obosnovanie_verstki` — это его работа».
Но Я2 сдал его **зелёным** на Л2 (18 обоснований написаны). Значит на этой лекции красный
`gejt_verstki` был бы **регрессией**, а не законным красным. Фактически — **rc=0**, вопрос снят.

**Регресс:** `find _generator -name PROGNAT.sh | wc -l` → **25**.
Прогон 1 → **25 из 25 зелёных**. Прогон 2 → **25 из 25**. `git status --porcelain` до и после
второго прогона — `diff` **пуст**: дефект «фикстура пачкает отслеживаемый файл» не воспроизвёлся.
⚠ Честно: этот регресс шёл в локали сессии (`LANG` пуст). В UTF-8 локали одна из 25 красная —
та самая `nezakrytogo`; полный прогон в UTF-8 не уложился в лимит команды, поэтому класс измерен
**статически** (грепом по всем 25 файлам) — так полнее, чем прогоном: найдено 3 файла, а не 1.

### Проверка «работает ВМЕСТЕ» сверх восьми команд

1. **Дека соответствует своим карточкам** — главный вопрос при встрече двух веток, обе из которых
   трогали Л2: `git diff --stat zahod/sborka-l2-deka HEAD -- teorkat-vvedenie/L2/` → **пусто**.
   То есть слитое дерево по всей Л2 байт в байт равно ветке, на которой дека собиралась.
   Без этого «дека 18 слайдов» ничего не значила бы: `karta-kak-reestr` несла деку на 15 слайдов.
2. **Точки вызова гейтов в хуке не потеряны при встрече** (класс дефекта, который прошлое слияние
   и нашло): замер `grep -c` по `.githooks/pre-commit` **до и после**. `check_tool_contract` 3→4,
   `gejt_illyustracij`, `graf_zavisimostej`, `check_lenta`, `check_uroki`, `check_ves`,
   `check_kartoteka`, `proverka_pomarok` на месте. Потерянных нет.
   ⚠ `check_karty.py` = **до 0, после 0** — не потеря слияния, а неподключённость, объявленная ещё
   в Я1 (`## ВОПРОСЫ` п.1). Подтверждаю как повтор, а не как новую находку — вопрос 3.
3. **Ничего не удалено:** `git diff --numstat 367907d HEAD -- _studio/konvejer/` → **48 файлов,
   добавлено 789, удалено 0.** По всему слиянию файла вида «много удалено, ноль добавлено» нет:
   топ-5 по удалённым — `dist/index.html` 191/155, `bootstrap_zahod.py` 341/39,
   `schet_nezakrytogo.py` 86/33, два `risunok.svg` 23/18 и 27/15 — везде добавлено больше удалённого.

### ВЕРИФИКАТОР (§3) — свежий субагент, список обещаний из Я1 и Я2, охват числом

Субагент получил **15 обещаний** двух веток и репозиторий; что я делал при слиянии — не знал.
Вердикт: **проверено 15 из 15 · ЕСТЬ 13 · ЧАСТИЧНО 2 · НЕТ 0.** Каждое — своей командой.

Доехало фактом: реестр **52 строки при 52 инструментах на диске** · `check_karty.py` с четырьмя
инвариантами (И1–И4 найдены построчно) и `rc=0` · шапки `🗄 АРХИВ` **47** · `smeta.py`,
`kesh_formul.js` (4604 байта), `gejt_verstki.py` (`rc=0`) · живая карта **не** помечена архивом ·
дека 18 слайдов и `MISSING-MATH` 0 · `katex.json` **98** формул · `obosnovanie_verstki` — **18 из 18**
карточек · русских слов внутри графики **0** · `_snyato/` — **5** картинок · `zagolovok_snyat_namerenno`
— **8 из 8**.

**Два ЧАСТИЧНО — оба оказались расхождением с моей формулировкой, а не потерей:**
- **ГИГИЕНА.** В `bootstrap_zahod.py` раздел есть (6 попаданий, `GIGIENA_ZAGOLOVOK`), и фикстура
  `fixtures/gigiena/PROGNAT.sh` прогоном подтверждает, что раздел рождается в СГЕНЕРИРОВАННОМ файле.
  В `_TEMPLATE-zahod.md` слова «ГИГИЕНА» нет ни одного — и это осознанно: комментарий генератора
  говорит, что шаблон чужой и не трогается ни байтом, вставка идёт anchor-replace'ом при генерации.
  Просить искать в обоих файлах было ошибкой моего задания верификатору.
- **Доктрина.** Шесть маркеров на месте, но зовутся `ДВ1…ДВ6`, а не `Д1…Д6`, как я написал.
  Обещание выполнено, кривая буква — у меня.

**Единственное число, разошедшееся с Я1, проверено отдельно и сошлось:** файлов `.md` в
`_studio/konvejer/` вне живой карты — **48**, шапку несут **47**. Непомеченный —
`06-tekst/ETALON-vid/ETALON.md`, и `ls -l` показывает `lrwxr-xr-x … -> ../ETALON.md`: это **симлинк**
на уже помеченный файл, ровно как оговаривал Я1 («48-й — симлинк»). Расхождения нет.

**Потери — три независимых замера, все нулевые:**
- `_studio/konvejer/`: **добавлено 789, удалено 0.**
- Признак потери «удалено >0 при добавлено =0» по всем **127** файлам слияния:
  `git diff --numstat 367907d..HEAD | awk '$2>0 && $1==0'` → **пусто**.
- 🔴 `KARTA.md`, единственный разрешённый мной конфликт — **пропавших строк 0, показано множествами,
  а не глазом:** строк регистрации у базы `367907d` — **263**, у ветки `c14decb` — **261**, в итоге —
  **264**, и `comm -23` в обе стороны пуст. 264 — это |base ∪ branch|, то есть итог **ровно
  объединение**, ни строки сверх и ни строки мимо.

**Слепые зоны, объявленные верификатором** (передаю как есть, не сглаживая): он сверял «база против
финала» и не разбирал одиннадцать промежуточных веток по отдельности; проверял наличие полей, а не
вменяемость их содержания (18 обоснований вёрстки не читал); из 127 файлов построчно разобрал только
топ-5 и `_studio/konvejer/` — частичная потеря СМЫСЛА внутри файла, где есть и добавления, и удаления,
его правилом не ловится; и он не аудировал, честно ли объявлены «дыры», которые сам `check_karty.py`
печатает как законную пустоту.

### Что делал и ЗАЧЕМ

1. **Точка отката до слияния** — потому что зона была грязна, а два грязных пути существуют и на
   ветках: без коммита это была бы молчаливая перезапись вместо видимого конфликта.
2. **Слияние в предписанном порядке** (инструменты → лекция), зонами из §0.1. Инструмент ни разу не
   отказал по узости зоны — строка зон из захода подошла как есть.
3. **Диагностика красного хука до причины.** Соблазн был `--no-verify`; вместо этого измерил, что
   краснеет от локали, и довёл слияние с живыми гейтами.
4. **Восемь команд + регресс дважды + три проверки «вместе»**, каждая — ради конкретного класса потери.

### Что НЕ трогал

`_fond/**`, `obzory/**`, `ucheniki/**`, `_generator/build_listok.py`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`
— чужое по Ш3, ни байта. `.githooks/**` — не правил (в том числе НЕ подключал `check_karty.py`, хотя
это напрашивалось: вне зоны). `_generator/**` — не правил ничего, в том числе **не чинил три
найденные фикстуры**, хотя починка однострочная (`${U3}` вместо `$U3`): вне зоны, ушло в вопрос 1.
`teorkat-vvedenie/**` — не правил и **деку не пересобирал**: она приехала слиянием готовой и
соответствует карточкам, а пересборка перед эфиром — риск без задания. Ветки не удалял.
52 чужих грязных пути в основной папке не трогал (вопрос 5).

### НЕОБРАТИМОЕ

1. **Два merge-коммита в `arka/mat-kostyak`** (`deda16c`, `79544c9`) — это и было заданием, но
   необратимость называю явно · откат: `git reset --hard 367907d` (точка отката до слияния).
2. **Разрешение конфликта в `_studio/docs/KARTA.md` объединением** — перезапись файла в дереве ·
   восстановление любой из сторон: `git show 367907d:_studio/docs/KARTA.md` либо
   `git show c14decb:_studio/docs/KARTA.md`.
3. **`teorkat-vvedenie/L2/dist/index.html` перезаписан слиянием** — старая дека
   (971 955 байт, `MISSING-MATH: 44`) больше не лежит на диске · восстановление:
   `git show 367907d:teorkat-vvedenie/L2/dist/index.html`. Это желаемый исход, но именно он —
   единственная перезапись готового артефакта за заход.
4. **Автолог `INCIDENTY.md` дописан инструментом на две строки** (не моей рукой; файл не коммитил).

Больше ничего: **ни одного файла не удалил, не переименовал и не переместил**; `git checkout` в
основной папке не делал; `git reset` не делал; ветку не переключал.

### ПОВТОРЯЕМОСТЬ находок

**Класс НЕМЕДЛЕННОЕ — повторится на следующей же единице работы, чинить ДО следующего прогона:**
- **Локаль-зависимые фикстуры (вопрос 1)** — повторится на КАЖДОМ коммите, который стажирует
  `schet_nezakrytogo.py` или саму фикстуру, то есть на следующем же заходе по инструментам. Хуже
  того, две другие фикстуры врут молча: ловушка `sborka` может напечатать неправильное сообщение и
  зачесться зелёной. Это не пункт очереди — это условие того, что регресс вообще что-то значит.
- **`merge --continue` без законного обхода (вопрос 2)** — повторится на следующем же слиянии, где
  хук покраснеет на чужом пути; сегодня это едва не стоило семи снятых гейтов.
- **`check_karty.py` не подключён (вопрос 3)** — повторится ровно как обещал Я1: на первом заходе,
  который заведёт инструмент. Заход-предшественник назвал это «повторится», и оно повторилось —
  но починки за сутки не случилось, потому что дом вне зоны у ОБОИХ исполнителей подряд.

**Не повторится, законно уходит в очередь записью:** две открытые строки автолога (вопрос 4,
разовая уборка), 52 чужих грязных пути (вопрос 5, состояние дня), неудалённые ветки (вопрос 6,
решение владельца).

### Открытое «возвращаться»

- **Дека к эфиру годна и лежит в основной папке** — по числам (18 слайдов, `MISSING-MATH` 0,
  четыре гейта зелёных). Её СОДЕРЖАНИЕ я не оценивал: отчёт `kod_sborka-l2-deka.md` владелец ещё не
  принимал, и это прямо не моя работа.
- **Пересборка деки на слитом дереве не проверялась.** Дека приехала готовой и соответствует
  карточкам; воспроизвести её `deck.py` на слитом дереве я не пробовал сознательно — это переписало
  бы артефакт за четверо суток до эфира ради проверки, которой заход не просил. Если владелец хочет
  гарантию воспроизводимости — это отдельный заход, и делать его надо НЕ поверх живого `dist/`.
- **Три фикстуры и `git_zona.py merge` ждут починки в чужой зоне** — вопросы 1 и 2.

**Время прогона + токены** — на канале `app` НЕПРИМЕНИМО, снимать неоткуда: лог прогона существует
только на канале `terminal`, где его кладёт `tee`.
