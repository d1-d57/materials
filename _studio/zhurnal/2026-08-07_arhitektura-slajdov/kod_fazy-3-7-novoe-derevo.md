# Канал исполнителя — fazy-3-7-novoe-derevo (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — это переписывание дисциплины по существу: решения, которые фаза принимает,
> и артефакты, которыми она обменивается, приходится выводить, а не переносить. Субагенты: ≤2,
> и только на Э5 (свежий взгляд).

КОНТЕКСТ. Фабрика научно-популярных лекций. Скилл `slajdy` описывает семь фаз единой формы.
Фазы 1 и 2 переписаны 2026-08-08 под новую архитектуру: **единица работы — слайд**, у каждого своя
папка с карточкой, которая накапливает поля по фазам; иллюстрации — отдельный пул; обе ленты —
представления, собираемые из карточек.

🔴 **Фазы 3–7 остались под СТАРОЕ дерево, и это блокер.** Вскрыто приёмкой 2026-08-08:
- фазы 1–2 и весь живой код знают `<лекция>/{brief.md, slajdy/<имя>/slaid.md, illustracii/<имя>/}`;
- фазы 3–7 знают `<лекция>/src/{content, slides, illustrations}`;
- следствия: у манифеста два адреса · у пула иллюстраций два адреса и два формата имени · `zakaz.md`,
  обязательный выход фазы 2, **не читает ни одна фаза** · вход фазы 6 не содержит карточки,
  объявленной источником истины.

**Решение принято аналитиком 2026-08-08: правое дерево — НОВОЕ.** Старое принадлежит генератору,
который решением владельца переписывается начисто, а старые деки не поддерживаются.

ЦЕЛЬ: переписать фазы 3–7 под новое дерево так, чтобы конвейер стыковался от интервью до сборки.

Приёмка — по ОТЧЁТУ, без построчной сверки.
Если стоп до цели: получишь переписанные фазы, но НЕ прогон верификаторов — он последний.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **ФАЙЛ ДЛЯ ОТЧЁТА — вот этот физический файл, абсолютным путём:**
  `~/Documents/GitHub/materials/_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_fazy-3-7-novoe-derevo.md`.
  План, вопросы и отчёт пишутся В НЕГО, в секции внизу, и никуда больше. Работаешь в основной папке,
  не в worktree. *Урок 46: три приёмки подряд читали пустой файл.*
- **МЕСТО РАБОТЫ:** 🔴 **в `materials` ветку НЕ трогай и НЕ создавай** — тебе там нужен ровно один файл, этот, с отчётом; работай на той ветке, что стоит. Вся содержательная работа идёт в `disciplina`, где заводишь свою ветку сам: `cd ~/Documents/GitHub/disciplina && git checkout -b zahod/fazy-3-7` (это разрешено: `checkout -b` заводит новую ветку от текущей и рабочее дерево не откатывает).
  ⚠ В `materials` `git checkout` ЗАПРЕЩЁН: в общей папке он МОЛЧА откатывает дерево (цена 27→28.07: файл откатился ночью, поймал владелец вручную; следа в git не остаётся). Там ты только дописываешь секции в свой файл-отчёт.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_fazy-3-7-novoe-derevo.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- **ТОЧКА ВХОДА — первая команда:** `git branch --show-current` в обоих репозиториях. В `materials` — что стоит, то и стоит. В `disciplina` — завести свою ветку по блоку «МЕСТО РАБОТЫ» выше, `echo $?` после.
- Точка отката: `git add `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_fazy-3-7-novoe-derevo.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` | 🔴 предмет работы. Фазы 1–2 уже переписаны — **они образец формы и источник новых адресов**; фазы 3–7 переписываешь ты |
| Я2 | `~/Documents/GitHub/disciplina/skills/slajdy/references/istochnik-istiny.md` | раскладка нового дерева, пул иллюстраций, где параллельность запрещена |
| Я3 | `~/Documents/GitHub/disciplina/skills/slajdy/references/kartochka-slajda.md` | формат карточки: поля, блоки, два вида редактуры |
| Я4 | `~/Documents/GitHub/disciplina/skills/slajdy/references/fazy-1-2-plan.md` | терминология и типология идеи слайда — то, на что опираются фазы 3–7 |
| Я5 | `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md`, раздел «РЕШЕНИЯ ПРИЁМКИ 2026-08-08» | 🔴 шесть решений, которые НЕ переоткрывать: правое дерево · markdown как источник · семь типов блока · и другие |
| Я6 | `_generator/sborka/` | живой код: он знает только новое дерево. **Дисциплина обязана совпасть с кодом, а не наоборот** |
| Я7 | `~/Documents/GitHub/disciplina/skills/slajdy/verifikatory/` | семь промптов; фазы 1 и 2 — предмет Э5 |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Пять шагов. Форма фазы — шесть блоков, как у фаз 1–2: ВХОД · РЕШЕНИЯ ФАЗЫ · ПРОЦЕДУРА · ВЫХОД ·
ГЕЙТ ВЫХОДА · МЕТРИКА. Ни одна клетка пустой не остаётся.

### Э1. Свести адреса — механическая часть, делается первой

Пройти фазы 3–7 и заменить старые адреса новыми:
- `<лекция>/src/content/<id>.md` → раздел карточки `slajdy/<имя>/slaid.md`;
- `<лекция>/src/slides/<id>.html` → выход компилятора `slajdy/<имя>/dist/`;
- `<лекция>/src/illustrations/*.svg` → пул `illustracii/<имя>/{zakaz.md, risunok.svg}`;
- манифест — один адрес, `<лекция>/brief.md`.

Каждая замена — якорной правкой с проверкой числа совпадений, не поиском по смыслу.
В отчёт: сколько адресов заменено, по каждой фазе.

### Э2. Переписать фазы 3–7 по существу

Адресов недостаточно: у фаз изменились **артефакты и решения**.

**Фаза 3 — текст слайдов.** Вход — карточки с разделом «Математика развёрнуто», размеченным
блоками. Работа идёт **по блокам**: у каждого типа свой регистр, алгоритм сжатия применяется
внутри блока, а не к слайду целиком. Выход — тот же файл, раздел «Текст слайда сжато», состав
блоков совпадает с первым разделом.
🔴 Решение Р-Б: **источник — markdown в карточке, HTML — выход компилятора.** Прежняя формулировка
«markdown на выходе запрещён категорически» переписывается как **«предъявлять владельцу только
собранным»**.
🔴 Решение Р-В: **закрытый список типов блока — СЕМЬ**, редакция кода (`bloki.py`):
`narrativ · opredelenie · primer · utverzhdenie · dokazatelstvo · itog · uprazhnenie`.
Существовало четыре редакции; привести к одной здесь И в `~/Documents/GitHub/disciplina/skills/slajdy/references/status-bloka.md`.
Следствие, которое это чинит: блок «задание залу» до сих пор не получал режима текста вовсе.

**Фаза 4 — вёрстка.** Больше не рукописный CSS. Вход — карточка со сжатым текстом; работа —
выбрать тип вёрстки и назвать параметры; выход — собранный слайд.
⚠ Параллельный заход `solver-vmeshcheniya` прямо сейчас делает автоподбор этих параметров. **Не
описывай механику подбора** — назови, что она есть и что гейт вмещения стоит на выходе фазы;
детали доберутся при сведении.

**Фаза 5 — иллюстрации.** Пул, а не папка слайда. `zakaz.md` — вход фазы: что показать, тип из
словаря скилла иллюстраций, чем заменяется словами. Слайд ссылается именем. **Первая генерация
всех иллюстраций лекции — одним агентом**, ради единого стиля; правки потом параллельно.
Граница со скиллом `illustracii`: здесь — нужна ли и влезает ли; там — как выглядит и читается ли.

**Фаза 6 — сборка и QA.** Вход — карточки и пул. Собирается дек из папок слайдов; `status: rezerv`
исключается; ленты собираются как представления. Владелец смотрит только собранное.

**Фаза 7 — сцены.** Сцена — свойство БЛОКА, не абзаца. Отдельного типа «вопрос залу» нет: пауза
для разговора — момент между раскрытием двух блоков. Отдельный механизм — **пропуск внутри блока**
(живой пример: правая часть формулы проступает на своей сцене).

### Э3. Свести список типов блока и снять остатки старых редакций

Пройти грепом по всему скиллу и справочникам: любое место, где список типов блока, адрес артефакта
или имя поля расходятся с кодом, — привести к коду. В отчёт: сколько расхождений найдено и где.

### Э4. Проверить, что конвейер стыкуется — сквозной проход по бумаге

Пройти семь фаз подряд и убедиться: **выход каждой фазы является входом следующей, дословно**.
Не «похож», а совпадает по имени артефакта и по адресу. Любой разрыв — находка.
Это дешёвая проверка, которая ловит ровно тот дефект, из-за которого заведён этот заход.

### Э5. 🔴 ПРОГНАТЬ ДВА НЕПРОГНАННЫХ ВЕРИФИКАТОРА — фаз 1 и 2

Пять промптов из семи никогда не запускались. При первом же запуске одного из них (фаза 3, 08.08)
выяснилось, что он **ждал разметку `####`, которой в живых файлах `materials/teorkat-vvedenie/src/content/` нет** —
проверить `grep -c '^####' materials/teorkat-vvedenie/src/content/s08.md`; промпт физически не мог
стартовать. Тот же дефект вероятен и здесь.

Прогнать `verifikatory/faza-1-intervyu.md` и `faza-2-raskadrovka.md` **на живом материале**:
`_generator/tools/fixtures/sborka/zhivoj-teorkat/` — три реальных слайда в новом формате.
Субагент с чистым контекстом, без этого захода.
В отчёт: запустился ли промпт вообще · что нашёл · **если не покраснел ни разу — это признак, что
критерии не предъявимы**, так и написать.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. в `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` **не осталось адресов старого дерева** —
   `grep -c "src/content\|src/slides\|src/illustrations"` печатает `0`;
2. у всех семи фаз шесть блоков формы — таблица 7×6 без пустых клеток, воспроизводится скриптом;
3. список типов блока встречается в скилле и справочниках **в одной редакции, из семи имён** —
   проверено грепом, названо сколько мест приведено;
4. сквозной проход Э4 выполнен: для каждой пары соседних фаз названо, каким артефактом они
   стыкуются; разрывов 0 либо разрывы названы поимённо;
5. 🔴 **ЖИВОЙ ПРОГОН на реальном объекте репозитория:** оба верификатора запущены на
   `_generator/tools/fixtures/sborka/zhivoj-teorkat/`, вердикты приложены с охватом. Фикстура-заглушка
   результатом не считается;
6. `~/Documents/GitHub/disciplina/skills/slajdy/references/status-bloka.md` приведён к семи типам.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

## 3. ВЕРИФИКАТОР
🔴 **Операция ЛОССОВАЯ** — ты переписываешь пять фаз действующего канона. Верификатор обязателен:
свежий субагент сверяет переписанные фазы с `git show HEAD:skills/slajdy/SKILL.md` и отвечает, не
исчезло ли молча живое правило. Исчезло намеренно — назови строкой.
**Задай ДОЛЮ ВЫБОРКИ** — сплошную не менее трети, и пусть назовёт, какую именно.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_fazy-3-7-novoe-derevo.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_fazy-3-7-novoe-derevo.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Критерий готовности, записанный одним грепом, проверял заметно меньше, чем задача рядом с ним
Э1 требовала заменить старые адреса в фазах 3–7; критерий №1 проверял это командой
`grep -c "src/content\|src/slides\|src/illustrations" SKILL.md` = 0. Команда считает СТРОКИ (не
вхождения) и видит только адреса с префиксом `src/` — а большинство старых адресов в фазах 3–7
стояло без него: `slides/<id>.html`, `content/<id>.md`, `illustrations/<name>.svg`.
ЦЕНА: критерий печатал бы `0` при живом дефекте. Конкретно: строка 98 Инварианта утверждала
«рабочий материал лекции (`<лекция>/src/`)» — то есть ровно то утверждение, которое заход послан
опровергнуть, — и критерий-как-записан её не видел. Поймал не критерий, а самодельный чекер,
которого заход не требовал. Правило шире случая: **критерий, записанный одной командой, надо
прочитать как команду** — что именно она считает и чего не видит, — а не как утверждение о мире.

### Заход поставил измерение ПОСЛЕ починки того же объекта, и это стёрло бы результат измерения
Э5 («прогнать два непрогнанных верификатора, выяснить, могут ли они вообще стартовать») стоит в
задании последним шагом, а Э3 («пройти грепом по всему скиллу, привести адреса к коду») — третьим.
Промпты верификаторов лежат внутри скилла и полны старых адресов, то есть Э3 — это починка ровно
того объекта, который Э5 обязан измерить.
ЦЕНА: пойди я по порядку задания, Э3 исправил бы адреса в промптах ДО прогона, Э5 замерил бы уже
починенные промпты и вернул бы «стартовали нормально» — единственное живое доказательство Д14
(«пять промптов ни разу не прогнаны, и один физически не мог стартовать») было бы уничтожено
работой самого захода, причём незаметно: отчёт выглядел бы лучше, а знание пропало бы. Я поменял
порядок и назвал это в `## ПЛАН` до работы. Правило: **в задании, где есть и шаг «замерь X», и шаг
«почини X», порядок между ними — не стилистика, и его надо задавать явно.**
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 0. Состояние на входе (проверено командой, не памятью)
- `materials`: ветка `arka/mat-kostyak` — что стояло, то и стоит, `checkout` не звался.
- `disciplina`: стояла `zahod/verifikatory-stilya` → заведена своя `zahod/fazy-3-7`
  (`git checkout -b`, rc=0, `git branch --show-current` подтверждает).
- Прочитаны все семь якорей целиком (Я1 — 934 строки, оба прохода; Я6 — `bloki.py`,
  `formaty.py`, `slaid.py`, `deck.py`, `gejt_kartochki.py`, `tipy.py`, `lenta.py`,
  `podgonka.py`, `bootstrap_lekcii.py`; Я7 — все семь промптов по списку, два прочитаны
  дословно). Проект сверх якорей не изучался.

### 1. Предпосылки и развилки — называю ДО работы, как требует §1

**Р1. Критерий готовности №1 в нынешней форме слабее задачи Э1 — предлагаю поправку.**
`grep -c "src/content\|src/slides\|src/illustrations" SKILL.md` печатает **3** и считает
СТРОКИ, а не вхождения; главное — он не видит старые адреса без префикса `src/`, а их в
фазах 3–7 большинство: `slides/<id>.html` (вход и выход фаз 3, 4, 5, 7), `content/<id>.md`
(выход фаз 4 и 7), `illustrations/<name>.svg`, `slide_order` как файловый манифест.
Заменив только три `src/…`-строки, я получу зелёный критерий при живом дефекте.
⇒ Делаю сильную версию и отчитываюсь по обеим: критерий №1 как записан (обязан дать `0`)
**плюс** `grep -nE '(^|[^-a-z/])(src/|content/|slides/|illustrations/)' SKILL.md`, и его
остаток объясняю поимённо (законный остаток — исторические пометки «здесь СТОЯЛО» и имена
файлов старого движка `build_deck.py`, который остаётся жить для старых деков).

**Р2. Бюджет субагентов: шапка и §3 противоречат друг другу.** Шапка — «≤2, и только на Э5»;
§3 — «верификатор ОБЯЗАТЕЛЕН, операция лоссовая». Разрешаю в пользу §3, потому что там
названа цена (переписываю пять фаз действующего канона): **три субагента всего** — два на Э5
(по промпту на фазу) и один верификатор §3 с долей выборки ≥⅓ сплошной. Больше не будет.

**Р3. Фаза 4 не имеет права переписывать `tip_verstki`/`liniya` — это поля фазы 2 (Н1).**
Старый текст фазы 4 писал рукописный per-slide CSS, и вопрос собственности не возникал;
в новом дереве возникает. Решаю по Н1 и по коду: у фазы 4 свои поля — `kegl` (единственный
параметр вёрстки, который `tipy.py:_kegl_style` читает из шапки помимо решений фазы 2) и
собранный `dist/`. Расхождение с реальным текстом фаза 4 не «правит молча», а возвращает
наверх — это же требует Н3.

**Р4. `data-scenes` больше не пишется руками — его ВЫЧИСЛЯЕТ компилятор** (`max_scenes`,
`slaid.py:88`, `deck.py:64`). Старая фаза 4 объявляла его своим слоем, старая фаза 7 —
«вручную сверен с числом сцен в тексте». Дисциплина обязана совпасть с кодом (Я6): клауза
переписывается на «сверить порождённое число с замыслом», а не «проставить».

**Р5. `build_deck.py --lint` на новое дерево не запускается** — он читает `<лекция>/src/`
(`shablon.html`, `content/`, `slides/`) и остаётся жить для СТАРЫХ деков. Значит скриптовый
слой гейтов фаз 3–7 в новом дереве другой: `gejt_kartochki.py` (форма карточки, состав
блоков, бюджет слов, резолв иллюстраций и терминов) · сама компиляция `slaid.py`/`deck.py`
(`FormatBlokov`/`FormatSlaida`/`TipVerstki` — отказ на неизвестном типе блока и на кривом
типе вёрстки) · `audit.py <dist>/index.html` и `render.py` (оба принимают ЛЮБОЙ собранный
дек, не привязаны к старому дереву — проверено чтением их входа). Изобретать несуществующий
линтер не буду; чего нет — назову дырой с именем.

**Р6. Что НЕ делаю.** Не описываю механику автоподбора вмещения (параллельный заход
`solver-vmeshcheniya`) — называю, что она есть и что гейт вмещения стоит на выходе фазы 4.
Не трогаю `_generator/**` (READ-ONLY: дисциплина совпадает с кодом, а не наоборот) и не
трогаю фазы 1–2 по существу — только там, где Э3 находит расхождение списка типов/адреса.
Решения Я5 (Р-А…Р-Е) не переоткрываю.

### 2. Порядок ходов
1. **Э5 первым, а не последним** — два субагента на промптах фаз 1 и 2 **в том виде, в каком
   они лежат в HEAD**, на живой фикстуре `zhivoj-teorkat`. Если сначала пройтись по ним Э3-грепом,
   я своими руками сотру ровно тот дефект, который Э5 обязан замерить. Пускаю в фоне.
2. **Э1+Э2 одним проходом по фазам 3–7** — адреса и существо переплетены: адрес меняется потому,
   что меняется артефакт. Каждая правка — якорной заменой с проверкой числа совпадений.
   Форма фазы — те же шесть блоков, ни одной пустой клетки.
3. **Э3** — греп по скиллу и справочникам на список типов блока, адреса артефактов и имена
   полей; приведение к коду. Сюда же критерий №6 — `references/status-bloka.md` на семь типов
   (сегодня в нём пять + нарратив, и `uprazhnenie` не имеет режима текста вовсе).
4. **Э4** — сквозной проход по шести стыкам (1→2 … 6→7): называю артефакт и его адрес с обеих
   сторон, дословно. Разрывы — поимённо.
5. **Верификатор §3** — свежий субагент, сверка с `git show HEAD:skills/slajdy/SKILL.md`,
   доля выборки ≥⅓ сплошная, вопрос «не исчезло ли молча живое правило».
6. **Коммиты в ОБА репозитория**: `disciplina` (ветка `zahod/fazy-3-7`, содержательная работа)
   и `materials` (только этот файл-отчёт, `--no-optional-locks`, `add` + `commit -- <пути>`).
   Новых `.md` не завожу — значит `register_doc.py` не нужен; если заведу, зову дверь тем же ходом.

## ВОПРОСЫ — (заполняет исполнитель)

1. **Календарь захода расходится с системной датой на сутки.** Системная дата прогона —
   2026-08-07; все решения, которые заход велит исполнять (Р-А…Р-Е, переписывание фаз 1–2,
   приёмка), датированы 2026-08-08. Я проставил в переписанных документах `дата данных: 2026-08-08`
   — по набору решений, который они исполняют, иначе правка встала бы ПЕРЕД решением, которое она
   исполняет. Если правильна системная дата, даты в `references/status-bloka.md` (шапка) и в
   пометках «редакция 2026-08-08» надо сдвинуть на сутки назад одной правкой.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. **Два выхода фаз не читает НИ ОДНА фаза — тот же класс дефекта, из-за которого заведён этот
   заход.** `zakaz.md` починен (теперь это вход фазы 5), но рядом стоят ещё два: `<лекция>/INTERVYU.md`
   (обязательный выход фазы 1) и `<лекция>/VYREZANO.md` (обязательный выход фазы 2, «файл обязателен,
   даже если вырезок ноль»). Проверено грепом по `SKILL.md`: оба встречаются только как ВЫХОД и в
   раскладке папок, ни в одном блоке ВХОД их нет. То есть дефект был не единичным, а классом, и
   двух его экземпляров приёмка не увидела. Чинится либо чтением (кто-то обязан их открывать), либо
   честной пометкой «архив замысла, не вход» — но не молчанием.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
3. **Стык 4→5 — не стык, а цикл, и это блокирует порядок фаз.** Слайд с непустым `illustracii`
   физически не компилируется, пока фаза 5 не положила `risunok.svg`: `slaid.py:load_illustrations`
   поднимает `FormatSlaida`, плейсхолдера (`<div class="ph">` старого дерева) в новом компиляторе
   нет. Значит «выход фазы 4 — собранный слайд» недостижим для всех иллюстрированных слайдов до
   фазы 5, а вход фазы 5 (`zakaz.md`) приходит не из фазы 4, а из фазы 2. Либо компилятору нужен
   плейсхолдер, либо фазы 4 и 5 надо менять местами, либо гейт фазы 4 делится надвое. Решать не
   волевым порядком: это перестановка фаз в каноне.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
4. **`schet_byudzheta.py` даёт ЛОЖНО-ЗЕЛЁНЫЙ на карточке нового дерева.** Он читает корпусный формат
   `### Слайд K — X слов` и `####`-статусы; на живой карточке печатает «слайдов проверено: 0,
   КРАСНЫХ: 0» с кодом возврата 0. Долг Д2 просит «встроить счётчик в фазу как непропускающий гейт»
   — встраивать в нынешнем виде нельзя, он молча пропустит всё. Следствие: клауза «блок вне `itog`
   — целиком список» (класс отказа `E`, Σ=79 записей, крупнейший в корпусе) сегодня не проверяется
   никакой командой. Формулировку Д2 надо переписать с «встроить» на «переписать под карточку».
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
5. **Формулы на новом пути не рендерятся вовсе.** `formaty.render_body` зовёт `render_md` с пустым
   кэшем, и каждая `$…$` уходит в дек маркером `⟦MISSING-MATH⟧`. Замер на живой фикстуре
   `zhivoj-teorkat`: три слайда, **8 маркеров** в собранном `index.html`. Это Д27, но там он записан
   как один пункт в списке «пробелы канон-скелета» — по факту это блокер сдачи любого
   математического дека, собранного новым путём.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
6. **Кто заводит служебные слайды в новом дереве — не решено ни одним документом.** В старом пути
   обложку/визитку/финал порождал `build_deck.py:generate_service_slides` из полей `brief.md` по
   шаблонам `_generator/skeleton/sluzhebnye/`, и фаза 4 прямо говорила: «фазе НЕ принадлежат».
   `deck.py` ничего не порождает — служебные слайды в новом дереве это обычные карточки с типами
   `oblozhka`/`vizitka`/`finalnyj`/`razdelitel`. Кто их заводит и из чего — дыра; я её назвал в
   фазе 4, но закрыть не мог: это решение, а не адрес.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md
   ДОСТАВЛЕНО: нет
7. **`DOLG.md` в `disciplina` был ГРЯЗНЫМ до меня, и я его не коммичу.** На ветке, с которой я
   ответвился (`zahod/verifikatory-stilya`), в рабочем дереве лежали незакоммиченные 41 строка —
   блок «🔴 РЕШЕНИЯ ПРИЁМКИ 2026-08-08» (Р-А…Р-Е), тот самый, который заход дал мне якорем Я5. В
   `HEAD` его нет. Я его не трогал и в свой коммит не включил (чужая работа в моём коммите — цена
   21.07). Следствие: мой текст ссылается на Р-А/Р-В, которых в git пока не существует. Нужно
   ваше действие — закоммитить этот блок отдельно.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и ЗАЧЕМ

**Э1 — адреса сведены.** Старое дерево `<лекция>/src/{content,slides,illustrations}` вычищено из
всех переписанных мест `SKILL.md`; вписано новое (`<лекция>/brief.md` · `slajdy/<имя>/slaid.md` ·
`slajdy/<имя>/dist/` · `illustracii/<имя>/{zakaz.md, risunok.svg}` · `<лекция>/dist/index.html`) —
53 строки несут новые адреса. По фазам: **фаза 3** — вход, выход и оба гейта (артефакт сменился с
`slides/<id>.html` на раздел карточки); **фаза 4** — вход, выход, процедура, гейт (исчез
`content/<id>.md` и `shablon.html`); **фаза 5** — вход, стык, выход, гейт (пул вместо
`src/illustrations/*.svg`, имя ПАПКИ вместо стема файла); **фаза 6** — вход и все пять слоёв
(`build_deck.py <src>` → `gejt_kartochki.py` + `deck.py` + `lenta.py`); **фаза 7** — вход, выход,
процедура (`content/<id>.md` → тело блока в карточке). Плюс Инвариант: раскладка источника
переписана целиком, и там же поймана строка, утверждавшая «рабочий материал лекции — `<лекция>/src/`».

**Э2 — фазы переписаны по существу, не только по адресам.** Что изменилось содержательно:
- **фаза 3**: работа идёт ПО БЛОКАМ; Р-Б («markdown — источник, HTML — выход компилятора»,
  «предъявлять только собранным») заменил прежнее «markdown на выходе запрещён категорически»;
  Р-В — семь типов вместо шести редакций, и `uprazhnenie` впервые получил режим текста; снято
  решение «фаза 3 назначает статус `####`-маркером» — тип размечает фаза 1 заголовком блока, и
  ровно это разбирает `bloki.py:BLOCK_RE`. Фаза 3 — единственная, которая не заводит ни одного
  поля шапки, и это названо явно;
- **фаза 4**: рукописного CSS больше нет — грид порождает `tipy.py`; собственный параметр фазы
  ровно один, `kegl` (пол 35px ⇒ `kegl ≥ 0.92`); `tip_verstki`/`liniya` остаются полями фазы 2, и
  фаза 4 их не переписывает (Н1), а возвращает слайд наверх ИМЕНЕМ; порядок починки переполнения
  переписан под реальные рычаги; гейт вмещения назван местом (механика — за параллельным заходом
  `solver-vmeshcheniya`, как велено);
- **фаза 5**: пул вместо папки слайда; `zakaz.md` объявлен ВХОДОМ фазы и его три поля выписаны —
  это и был главный блокер захода; первая генерация всех иллюстраций одним агентом; граница Н4;
- **фаза 6**: `rezerv` исключается безусловно как свойство слайда; `oblozhka`/`finalnyj` — по типу;
  ленты собираются `lenta.py` как представления; происхождение дека переопределено (новое дерево
  только greenfield ⇒ Р1 сверяет с предыдущим выходом, не с Canva-оригиналом);
- **фаза 7**: сцена — свойство БЛОКА; отдельного типа «вопрос залу» нет; пропуск внутри блока —
  второй механизм (`{@N| …}`, `{fill@N|…}`); `data-scenes` больше не проставляется руками.

**Э3 — расхождения сведены к коду.** Найдено и приведено: `references/status-bloka.md` (пять типов
+ «нарратив вне блоков» + собственный синтаксис `####` → семь типов, нарратив как полноправный
блок, синтаксис `### [<тип>] <мысль>`); `DYRY.md §1` (ложное доказательство закрытости — см. ниже);
Инвариант `SKILL.md` (врезка про «архетип»: переименование в фазах 4–7 выполнено, слово осталось
только там, где означает другой объект); пять промптов верификаторов фаз 3–7 (вставлена таблица
соответствия адресов старого дерева новым). Итого затронуто 9 файлов, +715/−308 строк.

**Э4 — сквозной проход, шесть стыков.** Ниже — артефакт с обеих сторон, дословно.

| стык | чем стыкуются | адрес | вердикт |
|---|---|---|---|
| 1→2 | карточки с шапкой фазы 1 и размеченными блоками без текста + порядок слайдов | `<лекция>/slajdy/<имя>/slaid.md`, `<лекция>/brief.md` | ✅ совпадает дословно |
| 2→3 | раздел `## Математика — развёрнуто` в тех же карточках + `byudzhet_slov`, жанр | тот же файл | ✅ |
| 3→4 | раздел `## Текст слайда — сжато` в тех же карточках | тот же файл | ✅ |
| 4→5 | — | — | ❌ **РАЗРЫВ, см. ниже** |
| 5→6 | рисунки пула + карточки | `<лекция>/illustracii/<имя>/risunok.svg` | ✅ |
| 6→7 | собранный дек + карточки со сжатым текстом | `<лекция>/dist/index.html` | ✅ |
| 7→6′ | пересобранный дек на повторный прогон слоёв QA | `<лекция>/dist/index.html` | ✅ названо в обеих фазах |

🔴 **Разрыв 4→5 — не «похоже», а цикл.** Вход фазы 5 (`zakaz.md`) приходит из фазы **2**, а не из
фазы 4; при этом выход фазы 4 («собранный слайд») для любого слайда с непустым `illustracii`
физически недостижим до фазы 5 — `slaid.py:load_illustrations` отказывает на ненайденном рисунке,
плейсхолдера в новом компиляторе нет. Подробно и с развилками — `## ВОПРОСЫ #3`.
🔴 **Плюс класс «выход без читателя», два экземпляра:** `INTERVYU.md` (выход фазы 1) и
`VYREZANO.md` (выход фазы 2, «файл обязателен, даже если вырезок ноль») не значатся во ВХОДЕ ни
одной фазы — проверено грепом. Это ровно тот дефект, из-за которого заведён заход (`zakaz.md`), —
значит он был не единичным случаем, а классом. `## ВОПРОСЫ #2`.

**Э5 — два непрогнанных верификатора прогнаны на живом материале** (`_generator/tools/fixtures/sborka/zhivoj-teorkat/`,
три реальных слайда в новом формате; фикстура-заглушка не использовалась). Прогон — субагентами с
чистым контекстом, без этого захода. **Оба промпта не смогли стартовать штатным путём.**

| | фаза 1 | фаза 2 |
|---|---|---|
| запустился ли | ❌ встал на подстановке плейсхолдеров | ❌ встал на первом шаге «Что проверяешь» |
| чего нет физически | `KONTRAKT-<артефакт>.md`, `TZ-<артефакт>.md` | `<лекция>/src/slides/*.html`, `<лекция>/src/brief.md` |
| клауз в чек-листе | 4 | 6 |
| проверено ШТАТНЫМ путём | **0 из 4** | **0 из 6** |
| проверено обходом (подставляя ближайшее поле нового формата) | 2 из 4 | 3 из 6 |
| из них красных | 2 из 2 | 1 (плюс 1 красная по нехватке артефакта) |

**Покраснело — но не благодаря промптам, а вопреки им.** Оба верификатора целятся в артефакты,
которых в новой архитектуре не существует: фаза 1 — в `KONTRAKT`/`ТЗ` (формат задокументирован в
`dok-04.5-intervyu.md` и `vhod-gejty.md`, но заменён «папкой на слайд»), фаза 2 — в `archetype`,
`accent_tag` и «таблицу раскадровки» (последней нет ни в одном живом `brief.md` — это же отмечено в
`INVENTAR-do-perepisyvaniya.md` R034). **Это признак, что критерии не предъявимы на новом входе, а
не что материал хорош** — так и пишу, как требует задание. Тот же дефект, что был найден у промпта
фазы 3 08.08, подтверждён ещё на двух из пяти; Д14 не гипотеза, а замер.
⚠ **Побочная находка прогона:** у слайда `s03` фикстуры `data-scenes="4"` при потолке «≤3 сцены»,
который заявляет промпт фазы 2. Найдено обходом, через собранный дек, а не штатным входом.

### Результат верификатора §3 — операция лоссовая, и она ДЕЙСТВИТЕЛЬНО потеряла

Свежий субагент сверил переписанные фазы с `git show HEAD:skills/slajdy/SKILL.md`.
**Доля выборки: 100 % — сплошной отрезок 464–863 старой версии, 400 из 400 строк, все пять фаз**
(задание требовало не менее трети). Дополнительно прогнан механический диф токенов, чтобы
построчное чтение не пропустило мелочь.

Счёт по 68 живым правилам выборки: **49 сохранено · 7 переехало · 7 снято намеренно с причиной в
самом тексте · 1 снято с причиной вне текста · 4 ПОТЕРЯНО МОЛЧА.**

🔴 **Вердикт был ОТРИЦАТЕЛЬНЫМ, и это правильный результат — четыре правила я потерял.** Все
четыре восстановлены, и каждое — не копипастом старой строки, а после проверки КОМАНДОЙ, живо ли
за ним что-то в коде. Что вышло:

| потерянное правило | что показала проверка кода | как восстановлено |
|---|---|---|
| роль `.fit` vs `--t-body`, «ровно один `.fit` на зону», клауза гейта «fit-группы консистентны» | `.fit` жив в `skeleton/base.css:17` и в `engine.js` (группы `dataset.fitGroup`), но `grep -c fit _generator/sborka/tipy.py` → **0**: компилятор не порождает ни одного, `data-max`/`data-min` в каноне не определены | решение 3 фазы 4 + клауза вернулась в гейт; записан статус «движок умеет, компилятор не порождает», чтобы правило не воскресло рукописным `.fit` в обход компилятора |
| шаг починки «блок-гэп между абзацами», цена 428px из 740 | `base.css:21` держит `.t-body p + p { margin-top: calc(1em * var(--lh)) }` — гэп переехал в канон и вычисляется от межстрочья | вписан в решение 4 как **намеренно отсутствующий шаг** с сохранённой ценой и запретом чинить `p+p` под один слайд |
| открытый спор «блюр и `transition-delay` — канон или per-deck», с парковкой в `## ВОПРОСЫ` | `grep -n transition-delay skeleton/base.css` → две строки: задержки **в общем каноне** | спор закрыт кодом, не мнением: per-deck CSS в новом дереве не существует как места, второй вариант перестал быть выразимым. Дата данных проставлена |
| `{blur@N|…}` — носитель `data-reveal`, а НЕ `data-scene-from` | 🔴 **хуже, чем потеря правила:** разметка разбирается (`build_deck.py:225` → `<span class="blur-reveal" data-reveal="N">`), но `grep -n "blur-reveal\|data-reveal"` по `skeleton/base.css` и `engine.js` → **пусто в обоих** | правило вернулось + вписан ЗАПРЕТ: спан не блюрится и не раскрывается, **ответ виден с самого начала при валидном HTML и зелёных гейтах** — тот же класс отказа, что стоил 79 блоков из 83 |

Пятое, пограничное: порог «иллюстрация ≥15% площади экрана» снят намеренно (метод забракован
владельцем, порог задаётся шириной полосы — Д11), но причина жила только в `DOLG.md`. Вписана в
фазу 5: читатель одного `SKILL.md` не должен принимать пропажу за потерю.

Верификатор дал и побочное, о чём я не спрашивал: три промпта верификаторов расходятся с
исправленным `SKILL.md` ровно по этим правилам — и это независимое подтверждение, что они выпали,
а не были сняты. В врезку всех пяти промптов дописан явный список из четырёх противоречий с
указанием «проверяй по `SKILL.md`, не подставляй».

**Что верификатор НЕ покрывает — называю его словами, а не своими:** проверка шла на присутствие
СТАРОГО правила в новом тексте; обратное направление — новые правила, введённые переписыванием и
ни на чём не основанные, — не верифицировано. Вне предмета также фазы 1–2, Инвариант, «Сводная
карта гейтов», «Якоря» и содержимое `references/`, `verifikatory/`, `DOLG.md`, `DYRY.md`.

### Как проверил — критерий по клаузам

Счётное воспроизводится одной командой (из корня `materials/`):
```bash
python3 _studio/zhurnal/2026-08-07_arhitektura-slajdov/proverka_formy.py
```
Печатает **«ИТОГ счётных клауз: зелёных 4 из 4»** и сам объявляет свои слепые зоны.

1. **Адресов старого дерева — 0.** `grep -c "src/content\|src/slides\|src/illustrations" SKILL.md`
   → `0`. ⚠ Критерий как записан слабее задачи (см. `## ПЛАН` Р1 и урок фабрике): он считает
   строки и не видит адреса без префикса `src/`. Сильная версия даёт остаток **4 строки**, и все
   четыре законны — каждая прямо говорит, что речь о СТАРОМ дереве старых деков либо о том, что
   старый справочник устарел (строки 112, 478, 844, 933). Пятая, найденная этим же чекером
   (Инвариант: «рабочий материал лекции (`<лекция>/src/`)»), была настоящим дефектом и починена.
2. **Таблица 7×6 — без пустых клеток**, воспроизводится тем же скриптом; самая тонкая клетка —
   ВЫХОД фазы 7 (4 строки), самая толстая — МЕТРИКА фазы 7 (75 строк, туда попадает хвост раздела).
3. **Список типов блока — одна редакция из семи имён.** Эталон берётся ИЗ КОДА
   (`bloki.py:TIPY_BLOKOV`), не из текста. Файлов, перечисляющих список, — 4, у всех 7/7:
   `SKILL.md`, `DOLG.md`, `references/kartochka-slajda.md`, `references/status-bloka.md`.
   Чужие редакции остались только как отрицание: `vopros_zalu` — 3 упоминания (все вида «в списке
   НЕТ, отменён владельцем»), `svyazka` — 2 (оба вида «существовало четыре редакции»).
   Приведено к коду в этом заходе: 2 места (`status-bloka.md` — §1, §2, §3, §6, §7; `DYRY.md` §1).
4. **Сквозной проход** — выполнен, таблица выше; разрывов 2, оба названы поимённо.
5. **Живой прогон** — выполнен на реальном объекте репозитория, вердикты с охватом выше.
6. **`status-bloka.md` приведён к семи типам** — проверено тем же скриптом (`К6 ✅ все семь на
   месте`), включая `uprazhnenie`, у которого режима текста не было вовсе.

Дополнительно проверено прогоном, а не чтением:
- фикстура собирается новым путём: `deck.py` → «слайдов в деке 3, в резерве 0», rc=0;
  `gejt_kartochki.py` → «ЗЕЛЁНЫЙ: 3 карточек проверено, замечаний 0», rc=0;
- **8 маркеров `⟦MISSING-MATH⟧`** в собранном деке из трёх слайдов — формулы новым путём не
  рендерятся (`## ВОПРОСЫ #5`);
- `schet_byudzheta.py` на живой карточке — «слайдов проверено: 0, КРАСНЫХ: 0», rc=0, то есть
  **ложно-зелёный** (`## ВОПРОСЫ #4`);
- ловушка разметки: `render_md("{@3}\n- раз\n- два")` → `<p data-scene-from="3">- раз - два</p>`,
  без тега тот же блок → `<ul class="tlist">`. Тег сцены молча схлопывает список в абзац; вписано
  в фазу 7 и в `status-bloka.md §3`.

### Что НЕ трогал
- `_generator/**` — READ-ONLY по контракту зоны. Дисциплина приведена к коду, ни одной строки кода
  не изменено. Всё, что названо долгом кода (Д2, Д17, Д27, Д28, Д29, Д30, Д31), названо, а не
  починено.
- Фазы 1 и 2 по существу — они образец формы, не предмет. Правки коснулись их ровно в двух местах,
  обе вынужденные: врезка про «архетип» в Инварианте (Э3) и раскладка источника в Инварианте (Э1).
- Промпты верификаторов фаз 1 и 2 — оставлены КАК ЕСТЬ намеренно: они предмет замера Э5, и правка
  уничтожила бы доказательство (см. урок фабрике). Промптам фаз 3–7 переписан только адресный слой,
  судящая часть — нет; это названо прямо внутри вставленной врезки.
- `DOLG.md` в `disciplina` — был грязным ДО меня (41 незакоммиченная строка, блок Я5). Не трогал и
  не коммичу: `## ВОПРОСЫ #7`.
- Ветку в `materials` не заводил и `git checkout` там не звал — работал на `arka/mat-kostyak`, как
  предписано.

### Открытое «возвращаться»
Семь пунктов в `## ВОПРОСЫ`; из них решения владельца/аналитика требуют #1 (календарь), #3
(перестановка фаз 4/5), #6 (кто заводит служебные слайды) и #7 (грязный `DOLG.md`). Остальные —
переформулировка долгов по факту замера.

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** снимает приёмка из лога прогона.

**НЕОБРАТИМОЕ:** необратимого нет. Ни одного файла не удалено, не перемещено и не переименовано;
`git reset`/`checkout` поверх несохранённого не звался ни разу; за пределы зоны правки не выходили.
Все изменения — правки текста в отслеживаемых git файлах, откатываются из git.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` — открывать
текстовым редактором, разделы «## Фаза 3 · Текст слайдов» … «## Фаза 7 · Сцены» (собранного HTML
этот заход не производит: предмет работы — сам канон, а не дек).
Проверяемый спутник: `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-07_arhitektura-slajdov/proverka_formy.py`
— запустить из корня `materials/`, печатает критерий числами.
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
