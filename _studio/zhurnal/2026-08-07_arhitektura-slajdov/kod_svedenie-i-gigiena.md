# Канал исполнителя — svedenie-i-gigiena (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — половина работы это СУЖДЕНИЕ: прочитать переписанный скилл свежими глазами и
> сказать, целен ли он. Такое не расписывается заранее. Вторая половина — хирургия над git, где
> цена ошибки высока. Субагенты: ≤2, и только на шаге Э4 (свежий взгляд).

КОНТЕКСТ. Фабрика научно-популярных лекций. За сутки скилл `slajdy` переписан из шести разнородных
шагов в семь фаз единой формы, написан компилятор слайда, введена карточка слайда как источник
истины, заведены протоколы и верификаторы. Четыре захода приняты.

🔴 **Но работа разбросана и в основную ветку не сведена.** Проверено 2026-08-08:
- в `materials` две ветки заходов не влиты — `zahod/kompilyator-slajda` и `zahod/kartochka-i-sborka`;
  весь новый путь сборки (`_generator/sborka/`) физически недоступен из основной папки;
- в `disciplina` работа лежит на ветке `zahod/verifikatory-stilya`; справочник про фазы 1–2
  (`skills/slajdy/references/fazy-1-2-plan.md`) **не в git вовсе**, правки в `skills/slajdy/DOLG.md`
  и `skills/illustracii/SKILL.md` не закоммичены;
- в `materials/.git` висит мёртвый `index.lock` от упавшего процесса (свыше часа), репозиторий
  не принимает запись ни от кого;
- девять рабочих папок worktree живы, часть из них — от закрытых заходов.

ЦЕЛЬ: **свести всё в одно место и убедиться, что получившийся скилл целен.** Не добавить новое, а
закрыть контур: влить, закоммитить, закрыть ветки, вписать фазы 1–2 в скилл как фазы, и прочитать
результат свежими глазами — не противоречит ли он сам себе.

Приёмка — по ОТЧЁТУ, без построчной сверки.
Если стоп до цели: получишь сведённый git и вписанные фазы, но НЕ вердикт о целостности — он
последний по порядку намеренно.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_svedenie-i-gigiena.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_svedenie-i-gigiena.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` | 🔴 предмет проверки: семь фаз единой формы + врезка «Четыре несущих решения» в инварианте |
| Я2 | `~/Documents/GitHub/disciplina/skills/slajdy/references/fazy-1-2-plan.md` | **не в git**: терминология, типология идеи слайда (8 типов по корпусу), процедура и гейты фаз 1 и 2. Предмет шага Э3 |
| Я3 | `~/Documents/GitHub/disciplina/skills/slajdy/references/kartochka-slajda.md` | формат карточки: поля, блоки, редактура, что проверяет скрипт |
| Я4 | `~/Documents/GitHub/disciplina/skills/slajdy/references/istochnik-istiny.md` | раскладка папок, пул иллюстраций, где параллельность запрещена |
| Я5 | `~/Documents/GitHub/disciplina/skills/slajdy/DOLG.md` | единый список открытого, 36 пунктов — сверить, не закрылось ли что-то по ходу |
| Я6 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/SESSIYA.md` | дневник арки: что было, что решено, две точки консолидации |
| Я7 | `_studio/docs/kak-delat/GIT-disciplina.md` | канон git: слияние, надгробия, worktree. **Читать ПЕРЕД шагом Э1** |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Пять шагов. Порядок обязателен: без Э1 остальное не на чем делать.

### Э1. Снять мёртвый лок и влить ветки заходов

В `materials/.git` висит `index.lock` от упавшего процесса (свыше часа на 2026-08-08) —
репозиторий не принимает запись. Снимается штатной дверью, руками файл не трогать:
`python3 _generator/tools/git_zona.py clean` (она сама щадит свежий лок живого git).

Дальше влить в текущую ветку, по одной, проверяя фактом:
- `zahod/kompilyator-slajda` — компилятор слайда;
- `zahod/kartochka-i-sborka` — карточка, гейт, bootstrap, пул иллюстраций, ленты, протоколы.
Слияние — `git_zona.py merge <ветка>`, оно останавливается на конфликте и показывает пути.
После обеих: `ls` по папке sborka генератора обязан показать файлы; не показал — СТОП, в отчёт.

⚠ Вторая ветка уже содержит первую (слияние сделано внутри захода) — возможно, хватит одной
команды. Проверь фактом, а не предположи.

### Э2. Закоммитить и свести `disciplina`

Там работа лежит на ветке `zahod/verifikatory-stilya`, и часть её вне git:
`skills/slajdy/references/fazy-1-2-plan.md` не закоммичен вовсе, правки в `skills/slajdy/DOLG.md`
и `skills/illustracii/SKILL.md` не закоммичены. Всё это — **работа аналитика, она верная и нужная.**

Закоммитить по путям своей зоны, затем свести ветку `zahod/verifikatory-stilya` с основной так,
как велит канон этого репозитория. ⚠ `git_zona.py` анкерится на `materials`; для `disciplina`
пользуйся формой с `GIT_ZONA_REPO=~/Documents/GitHub/disciplina` там, где она поддержана, а где
нет — обычным git с `--no-optional-locks` и явными путями.

### Э3. Вписать фазы 1 и 2 в скилл как ФАЗЫ, а не как справочник

Сегодня Я2 — отдельный документ, на который скилл не ссылается. Значит для исполнителя, читающего
`~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` подряд, фаз 1 и 2 по-прежнему нет.

Перенести в `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md`, в разделы «Фаза 1 · Интервью» и «Фаза 2 · Раскадровка», по единой форме
шести блоков (ВХОД · РЕШЕНИЯ ФАЗЫ · ПРОЦЕДУРА · ВЫХОД · ГЕЙТ ВЫХОДА · МЕТРИКА):
- **терминологию** (лекция → слайд → идея → блок → мысль → иллюстрация → сцена) — в инвариант,
  один раз, до всех фаз;
- **типологию идеи слайда** — восемь типов, выведенных из корпуса замером, с числами;
- 🔴 **выход фазы 1 — список слайдов С РАЗМЕЧЕННЫМИ БЛОКАМИ**, а не просто список слайдов. Это
  поправка владельца: «результатом интервью является разбиение на слайды и у каждого слайда
  разбиение на блоки»;
- **фаза 2 разносит математику по уже готовым блокам** и исполняется последовательно, без
  субагентов;
- гейты обеих фаз и независимый верификатор плана в конце фазы 1.

Справочник Я2 не удалять — он остаётся домом подробностей; в `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` идёт суть плюс ссылка.

### Э4. 🔴 СВЕЖИЙ ВЗГЛЯД НА СКИЛЛ ЦЕЛИКОМ — главный шаг

Субагент с пустым контекстом. Ему даётся переписанный `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` и три справочника (Я2, Я3, Я4).
**Не даётся** этот заход, дневник арки и история обсуждений. Он читает подряд, как исполнитель,
который видит скилл впервые, и отвечает:
- на каждой ли из семи фаз понятно, что подаётся на вход и что обязано получиться;
- **не противоречат ли документы друг другу** — три справочника писались в разное время суток и
  по ходу менялись решения (тип блока `vopros_zalu` отменён; пул блоков предложен и отклонён;
  метрика размера иллюстрации перезадана с площади на ширину полосы; иллюстрации переехали в
  отдельный пул). Каждое такое место, где старое решение осталось жить рядом с новым, — находка;
- есть ли решение, которое фаза принимает, но нигде не записывает;
- назван ли порог, который нельзя проверить командой;
- ссылается ли документ на файл, которого нет (проверять командой, не глазом).

Найденное — чинить, если это редакция; если это выбор — в `## ВОПРОСЫ` с рекомендацией.

### Э5. Гигиена: закрыть ветки и рабочие папки

По канону Я7: сперва `git_zona.py poteri --branch <ветка>` (что пропадёт, read-only; `rc=1` —
НЕ удалять), затем `git_zona.py zakryt-vetku` для тех, у кого терять нечего.
Живых рабочих папок девять — часть от давно закрытых заходов. Убрать те, что ничего не держат.
⚠ **Рабочую папку `kartochka-i-sborka` не трогать, пока владелец не откроет её артефакты** —
там лежат собранные деки и кадры. Спросить в отчёте, а не решать самому.

Сверить `DOLG.md` (Я5): не закрылось ли что-то по ходу этой работы. Закрылось — пометить ✅ с
датой данных, не удалять строку.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. из основной папки `materials` виден компилятор — команда `ls` по папке sborka генератора печатает девять питоновских модулей (формат, типы, компилятор слайда, сборщик дека, подгонка, блоки, bootstrap, гейт карточки, лента), а не «нет такого файла» (сейчас, ДО слияния, она честно даёт rc=2 — в этом и смысл критерия); лока в `.git`
   нет — `git_zona.py doctor` чист;
2. `git --no-optional-locks log --oneline -1 -- skills/slajdy/references/fazy-1-2-plan.md` в
   `disciplina` печатает коммит — файл больше не вне git;
3. `git status --short` в обоих репозиториях не показывает незакоммиченного в зоне;
4. в `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md` у фаз 1 и 2 присутствуют все шесть блоков единой формы — проверяется грепом,
   печатается таблица без пустых клеток;
5. 🔴 **вердикт свежего взгляда (Э4) приложен, с охватом**; каждое найденное противоречие названо
   поимённо и либо починено, либо вынесено в `## ВОПРОСЫ` с рекомендацией. «Противоречий нет» без
   охвата не принимается;
6. `git_zona.py poteri` прогнан по КАЖДОЙ закрываемой ветке, вывод приложен; ветки с `rc=1` не
   тронуты и названы в отчёте;
7. 🔴 **ЖИВОЙ ПРОГОН:** после слияния собрать демо-дек новым компилятором из основной папки
   (сборщиком дека из папки sborka генератора, по фикстуре `demo-karta`) — он обязан собраться там же, где
   собирался в рабочей папке захода. Не собрался — слияние неполно, это находка.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.**

## 3. ВЕРИФИКАТОР
🔴 **Операция ЛОССОВАЯ — ты сводишь ветки и закрываешь их.** Верификатор обязателен, и он не
формальность: `poteri` по каждой ветке ДО закрытия, вывод в отчёт. Ветка, у которой есть что
терять, не закрывается никогда — она уезжает строкой в отчёт.

Отдельно — Э4, свежий взгляд на скилл. Это ДРУГОЙ субагент и другая задача: он судит не потери,
а связность. Автор правок не может проверить свой текст на понятность — он достраивает
недостающее из головы. *Оплачено: самопроверка автора дала «неполных формулировок 0», свежий
читатель нашёл 26 мест.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_svedenie-i-gigiena.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_svedenie-i-gigiena.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### 🔴 Секция «0. ПЕРВЫЙ ХОД» захода утверждает обратное его же заданию

ЦЕНА: строка «вливать нечего, проверено при сборке: невлитых веток 0» стоит первой в файле, в
секции ПЕРВЫЙ ХОД, и написана голосом сборщика («проверено при сборке») — то есть выглядит
надёжнее прозы задания. Она ложна: `zahod/kartochka-i-sborka` несла 3 невлитых коммита и 48
файлов, которых в основной папке не было вовсе (`git_zona.py doctor`, до слияния). Ровно через
двадцать строк Э1 велит эти ветки влить. Исполнитель с пустым контекстом читает файл сверху вниз и
встречает опровержение раньше утверждения. **Реализованная цена здесь — ноль: я проверил фактом,
как велит §1 «предпосылку проверяй по живым файлам».** Но если бы поверил ПЕРВОМУ ХОДУ, не
выполнились бы Э1 (нечего вливать), Э5 (нечего закрывать) и критерии 1 и 7 (компилятора нет в
основной папке) — то есть весь заход, кроме Э2. Механизм дефекта не в человеке: автоподстановка
`bootstrap_zahod.py` считает невлитые ветки в момент СБОРКИ захода и печатает число как факт, а
между сборкой и запуском состояние репозитория меняется — здесь оно и изменилось (заход собран
на дату 2026-08-08, ветки влиты не были). Тот же класс, что «четыре числа критерия устарели за два
хода между написанием захода и его запуском» (`KONSTITUCIYA §10`), но опаснее: число стоит не в
критерии, а в ПЕРВОМ ХОДЕ и отменяет задание. Починка — либо не печатать снимок состояния в первом
ходе вовсе, либо печатать его КОМАНДОЙ, которую исполнитель обязан прогнать сам.

## ПЛАН — (заполняет исполнитель)

### Разведка ДО плана — что оказалось фактом, а что предпосылкой

Всё ниже снято read-only командами (`git_zona.py doctor`, `rev-list`, `merge-base`, `worktree list`),
до единого изменяющего хода.

| предпосылка захода | факт | вывод |
|---|---|---|
| §0 «вливать нечего, невлитых веток 0» | **ЛОЖНА.** `zahod/kartochka-i-sborka` — 3 коммита невлито, 48 файлов не существуют в основной папке | §0 противоречит Э1 в одном файле; работаю по Э1. Урок фабрике с ценой — ниже |
| Э1: влить ДВЕ ветки | `merge-base --is-ancestor zahod/kompilyator-slajda zahod/kartochka-i-sborka` → **rc=0** | вторая содержит первую (merge-коммит `3766282` внутри). Хватит ОДНОГО слияния — как и предупреждала ⚠ Э1 |
| мёртвый `index.lock` | есть, 97 мин, размер 0 | снимается `git_zona.py clean` |
| `_generator/sborka/` недоступна | `ls` → `No such file or directory`, **rc=1** (не 2: BSD-`ls` на macOS даёт 1; критерий п.1 называет 2 — косметика, не меняет смысла) | критерий сегодня честно красный |
| в `disciplina` три файла вне git | подтверждено: `?? references/fazy-1-2-plan.md`, `M DOLG.md`, `M illustracii/SKILL.md` | зона Э2 |
| `git_zona.py` не умеет `disciplina` | **ЛОЖНА.** `GIT_ZONA_REPO=…/disciplina … doctor` → rc=0, вывод корректный | дверь работает в обоих репозиториях, голый git — только там, где операции нет вовсе |
| «свести с основной» | `main` отстаёт от `zahod/verifikatory-stilya` на 8 коммитов, впереди на 0; `main == origin/main` | слияние — **fast-forward**, конфликтов быть не может |

Дополнительно, чего заход не называл: в `disciplina` вне git лежат ещё **пять** `DOLG.md` чужих
скиллов (`disciplina-arka`, `disciplina-git`, `disciplina-istochnik-pravdy`, `disciplina-zahody`,
`reserch-cikl`) — это работа параллельного захода в `disciplina-wt/etap-20-i-perenos`. **Не моя
зона, не коммичу, называю в отчёте.**

### Оспариваю ли критерий готовности

Критерий признаю рабочим; правлю в нём одно и предупреждаю об одном.
1. **п.1, `rc=2`** — на macOS `ls` на несуществующем каталоге даёт `rc=1`. Смысл критерия («сейчас
   честно красный») сохраняется, число в скобках неверно. Не блокирует.
2. **п.5 несёт риск ложно-зелёного:** «противоречий нет» без охвата не принимается — но и вердикт
   С охватом может быть зелёным ложно, если субагенту дать документы, которые я только что
   переписал, и не дать старую редакцию. Поэтому свежему взгляду даю ТОЛЬКО итоговые документы
   (как велит Э4) и отдельно сам предъявляю список мест, где старое решение жило рядом с новым ДО
   правки, — чтобы вердикт можно было сверить, а не принять на слово.

### Порядок работы

**Э0.** Точка отката по зоне в `materials`, если зона не чиста в HEAD (не фабриковать, если чиста).

**Э1.** `git_zona.py clean` → `git_zona.py merge zahod/kartochka-i-sborka` (одно слияние, не два —
по факту вложенности). Проверка фактом: `ls _generator/sborka/` обязан дать девять модулей.

**Э2.** `disciplina`: коммит трёх путей своей зоны через дверь с `GIT_ZONA_REPO`. Затем свести с
`main`. 🔴 **Отступление от буквы Э2, называю до работы:** слияние делаю **не `checkout main` +
`merge`**, а `git fetch . zahod/verifikatory-stilya:main` — это ровно тот же fast-forward, но он
**не трогает рабочее дерево вообще** и потому не может молча откатить файлы (цена 27→28.07,
контракт зоны). Не-fast-forward такая форма отвергает сама. `main` подтягиваю ДВАЖДЫ: сразу после
Э2 (чтобы работа доехала, даже если будет стоп) и ещё раз после Э4.

**Э3.** Вписать фазы 1 и 2 в `SKILL.md` как фазы по форме шести блоков. 🔴 Это не перенос, а
**разрешение конфликта**: сегодняшние фазы 1–2 в `SKILL.md` описывают СТАРУЮ архитектуру
(`KONTRAKT-…md` + `TZ-…md`, лента `slides/<id>.html`), а `fazy-1-2-plan.md` и `istochnik-istiny.md`
— новую (папка на слайд, карточка, блоки, пул иллюстраций). Оставить обе редакции рядом — ровно
тот дефект, который ищет Э4. Терминология уходит в инвариант один раз, типология идеи (8 типов,
201 слайд) — в фазу 1, выход фазы 1 = слайды **с размеченными блоками**, фаза 2 — разнесение
математики по готовым блокам, последовательно. Справочник не удаляю, ссылаюсь.

**Э4.** Свежий взгляд: субагент с пустым контекстом, ему — только `SKILL.md` + Я2/Я3/Я4, без
захода и дневника. Второй субагент (лимит ≤2) — на проверку ссылок командой, а не глазом.
Найденное: редакция — чиню, выбор — в `## ВОПРОСЫ` с рекомендацией.

**Э5.** `poteri` по КАЖДОЙ закрываемой ветке (вывод в отчёт), `zakryt-vetku` только там, где
`rc=0`. Рабочую папку `kartochka-i-sborka` **не трогаю** — там артефакты владельца, спрашиваю.
Сверяю `DOLG.md`: что закрылось по ходу — помечаю ✅ с датой данных, строку не удаляю.

**Живой прогон (критерий 7).** Демо-дек по фикстуре `demo-karta` собираю `_generator/sborka/deck.py`
из ОСНОВНОЙ папки, не из рабочей.

**Последний ход** — коммит своей зоны в `materials` по §4, двумя ходами, с `_studio/docs/KARTA.md`
в обоих, если звал `register_doc.py`.

## ВОПРОСЫ — (заполняет исполнитель)

Все — ВЫБОР, не редакция: чинить их своей волей значило бы решить за владельца. У каждого
рекомендация.

```
1. 🔴 ГЛАВНЫЙ. Два несовместимых дерева лекции живут рядом и не стыкуются.
   Фазы 1–2 и ВЕСЬ живой код sborka/ знают <лекция>/{brief.md, slajdy/<имя>/slaid.md,
   illustracii/<имя>/}. Фазы 4–7 знают <лекция>/src/{content,slides,illustrations}.
   Отсюда: у brief.md два адреса; у пула иллюстраций два адреса и два формата имени
   (кириллическая папка против английского .svg); zakaz.md — обязательный выход фазы 2 —
   не читает НИ ОДНА фаза; вход фазы 6 не содержит slaid.md, объявленный источником истины.
   РЕКОМЕНДАЦИЯ: правым объявить новое дерево — инструменты знают только его
   (gejt_kartochki.py:138, lenta.py:160 берут <лекция> с brief.md + slajdy/), а старое
   принадлежит генератору, который решением 9 переписывается начисто. Фазы 3–7 переписать
   отдельным заходом; это Д3, но масштаб больше, чем «согласовать фазу 4».
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. Выход фазы 3 задан двумя несовместимыми формами.
   SKILL.md фазы 3: «Выход — HTML. Markdown на выходе фазы запрещён категорически» (мотивировано
   словами владельца про то, что стиль в markdown не чувствуется). kartochka-slajda.md: сжатый
   текст живёт разделом `## Текст слайда — сжато` карточки, то есть markdown; так же устроен и
   живой гейт (gejt_kartochki.py:11 считает бюджет именно по этому разделу).
   РЕКОМЕНДАЦИЯ: источник — markdown в карточке, HTML — выход компилятора; тогда требование
   владельца «смотреть в HTML» выполняется сборкой одного слайда, а не форматом источника.
   Запрет «markdown запрещён» переформулировать как «предъявлять владельцу только собранным».
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. Закрытый список типов блока существует в четырёх редакциях.
   Семь (bloki.py:40 и kartochka-slajda §4) · шесть (SKILL.md фаза 3, решение 1: «закрытый список
   из шести статусов», выпало `uprazhnenie`) · пять+нарратив (references/status-bloka.md) ·
   плюс `svyazka` со статусом (был в примере istochnik-istiny — этот я снял).
   Следствие: блок «задание залу» не получает режима текста на фазе 3 вовсе.
   РЕКОМЕНДАЦИЯ: семь из bloki.py — единственная редакция, которую исполняет код; фазу 3 и
   status-bloka.md привести к ней.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md (фаза 3) + references/status-bloka.md
   ДОСТАВЛЕНО: нет

4. `status: v_deke | rezerv` порождается кодом, но владельцем не подтверждён.
   kartochka-slajda §4-бис честно пишет «предложено аналитиком, владельцем ещё не подтверждено», а
   bootstrap_lekcii.py:51 уже пишет `status: v_deke` в каждую анкету. При этом `status` не входит в
   девять обязательных полей — значит слайд-резерв пройдёт гейт как обычный и попадёт в бюджет,
   вопреки правилу «в бюджет не считается».
   РЕКОМЕНДАЦИЯ: подтвердить резерв (механизм снимает главную боль «выкинуть = потерять») и
   внести `status` в обязательные поля, либо убрать из анкеты. Третьего состояния быть не должно.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. Списки и сцены: один и тот же блокер объявлен закрытым и открытым.
   kartochka-slajda §К1: «механический блокер DYRY §12 исчезает по построению — тег висит на
   блоке». SKILL.md фаза 7 и DYRY §12: «сегодня несовместимы, законных варианта два».
   РЕКОМЕНДАЦИЯ: закрытие верно ТОЛЬКО если движок читает тег у блока; в живом коде этого пока
   нет. До правки движка считать блокер открытым, а формулировку в §К1 пометить как «станет
   верно после».
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

6. Рабочую папку `kartochka-i-sborka` удалять? В ней лежат собранные деки и кадры (Д36).
   Ветка влита, надгробие ей не нужно — терять нечего, кроме самих артефактов на диске.
   РЕКОМЕНДАЦИЯ: откройте артефакты, после чего одна команда:
   `python3 _generator/tools/git_zona.py zakryt-vetku --branch zahod/kartochka-i-sborka`
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

7. Ветка `zahod/dovodka-fazy-intervyu` не закрыта: надгробие с ТЕМ ЖЕ именем уже занято.
   `mogila/zahod/dovodka-fazy-intervyu` стоит на 8fc9951 (2026-08-05), а живая ветка на a0e3e18.
   Инструмент отказался перезаписывать — правильно: `-f` сделал бы работу похороненной ветки
   недостижимой. Своей волей не разбирал.
   РЕКОМЕНДАЦИЯ: `git_zona.py voskresit --branch zahod/dovodka-fazy-intervyu`, посмотреть, что
   там, и закрыть обе по очереди.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

8. Прикидка объёма и бюджет слов меряют разное, пересчёт не задан (Д42).
   `минут × 110` — темп РЕЧИ (120 мин → ≈13 200 слов); `byudzhet.slov_vsego` в живом примере 700 —
   это слова НА СЛАЙДАХ. Плюс третье число той же величины: корпусные 45/83 у schet_byudzheta.py
   против `byudzhet_slov: 55` у гейта карточки — два счётчика, две единицы (взвешенные слова
   против простых), кто прав при расхождении, не сказано.
   РЕКОМЕНДАЦИЯ: оставить формулу проверкой «влезает ли материал в минуты», а бюджет слайда
   набирать из корпусной медианы; один из двух счётчиков объявить главным.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

9. Три поля карточки не принадлежат ни одной фазе: `zagolovok_na_ekrane`, `kommentarij_lektoru`,
   `vvodit`. Анкета их порождает с пометкой «заполнить», гейт `vvodit` ПОТРЕБЛЯЕТ
   (gejt_kartochki.py:13), а кто и на какой фазе их заполняет — не сказано нигде.
   РЕКОМЕНДАЦИЯ: `vvodit` — фаза 1 (термины известны из плана), `zagolovok_na_ekrane` — фаза 3
   (это текст), `kommentarij_lektoru` — фаза 1.
   ДОМ: ~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md
   ДОСТАВЛЕНО: нет

10. Долг инструмента: `poteri --branch <ветка>` называет покрывающей ссылкой САМУ судимую ветку
    («история целиком есть в других ЛОКАЛЬНЫХ ссылках: zahod/X» при `--branch zahod/X`).
    Ложно-зелёного я не доказал — на двух живых ветках вердикт совпал с независимой сверкой
    (`rev-list --count HEAD..<ветка>` = 0). Но охват, названный самой судимой веткой, нечитаем, а
    канон прямо говорит, что верят именно зелёному вердикту.
    РЕКОМЕНДАЦИЯ: в одиночном режиме печатать ссылки БЕЗ судимой ветки; если список пуст —
    это и есть потеря.
    ДОМ: _generator/tools/git_zona.py + _meta/disciplina/GIT-disciplina.md §4
    ДОСТАВЛЕНО: нет

11. Ни один из двух репозиториев НЕ вывезен на origin — это моё сознательное воздержание, не забывчивость.
    `materials`: arka/mat-kostyak впереди origin на 52 коммита. `disciplina`: main впереди
    origin/main на 12 коммитов, и main там — витрина плагина-маркетплейса, которую другие машины
    тянут через `/plugin marketplace update`. Публиковать скилл, у которого семь открытых долгов и
    неразрешённый вопрос №1, — решение владельца, а не исполнителя.
    РЕКОМЕНДАЦИЯ, когда решите вывезти (по одной команде на репозиторий):
    `git -C ~/Documents/GitHub/materials push` и `git -C ~/Documents/GitHub/disciplina push origin main`
    ДОМ: владелец
    ДОСТАВЛЕНО: нет
```
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделано и зачем

**Э1 — контур замкнут.** Мёртвый `index.lock` (97 мин) снят `git_zona.py clean`. Влита ОДНА ветка
`zahod/kartochka-i-sborka`, а не две: `merge-base --is-ancestor zahod/kompilyator-slajda
zahod/kartochka-i-sborka` → `rc=0`, то есть первая физически внутри второй (merge-коммит `3766282`
на её истории). Слияние без конфликтов, merge-коммит `b5f5527`. Зачем именно так: два слияния на
вложенных ветках дали бы пустой второй merge и лишний шум в истории.

**Э2 — `disciplina` сведена.** Закоммичены три пути зоны, включая `references/fazy-1-2-plan.md`,
который не был в git ВОВСЕ. Ветка `zahod/verifikatory-stilya` сведена с `main` — **fast-forward**
(`main` отставала на 8 и была впереди на 0), четырьмя подтягиваниями по ходу работы, чтобы
результат доезжал в основную ветку, даже если бы случился стоп.
🔴 **Отступление от буквы Э2, объявлено в ПЛАНЕ до работы:** слияние сделано не `checkout main` +
`merge`, а `git fetch . zahod/verifikatory-stilya:main`. Это тот же fast-forward, но он **не
трогает рабочее дерево вообще** — а `checkout` в общей папке молча откатывает файлы (цена
27→28.07, названа в контракте зоны). Не-fast-forward такая форма отвергает сама.
`git_zona.py merge` здесь не подходил по направлению: он вливает в ТЕКУЩУЮ ветку, а нужна была
обратная.

**Э3 — фазы 1 и 2 вписаны как ФАЗЫ.** Это оказался не перенос, а разрешение конфликта: сегодняшние
фазы 1–2 описывали СТАРУЮ архитектуру (`KONTRAKT-<артефакт>.md` + `TZ-<артефакт>.md`, лента
`slides/<id>.html`), а справочники — новую (папка на слайд, карточка, блоки, отдельный пул
иллюстраций). Оставить обе редакции рядом значило бы своими руками завести тот дефект, который
ищет Э4. Сделано: терминология (лекция → слайд → идея → блок → мысль → иллюстрация → сцена) — в
инвариант один раз, до всех фаз; типология идеи слайда — восемь имён с числами замера (201 слайд);
🔴 выход фазы 1 = слайды **С РАЗМЕЧЕННЫМИ БЛОКАМИ** (поправка владельца), у каждого блока тип и
мысль, один центральный; фаза 2 — разнесение математики по ГОТОВЫМ блокам, последовательно, без
субагентов; гейты обеих фаз и независимый верификатор плана в конце фазы 1. Справочник Я2 не
удалён — он остался домом подробностей, из скилла на него три ссылки.
Хвосты, которые иначе повисли бы: адрес хука `ПРИНЯТО` переведён с несуществующего
`KONTRAKT-<артефакт>.md` на `<лекция>/brief.md` (7 мест), вход фазы 3 переписан с ленты
`slides/<id>.html` на карточки, сводный список семи фаз — с «markdown-ТЗ» на «план с блоками».

**Э4 — свежий взгляд, один субагент с пустым контекстом** (лимит ≤2 не выбран: второй планировался
на механическую проверку ссылок, но первый сделал её сам — 84 пути командой). Ему дан только
переписанный `SKILL.md` и три справочника; ни захода, ни дневника, ни истории он не видел.

**Э5 — гигиена.** `poteri` прогнан по КАЖДОЙ из 8 веток, вывод ниже. Закрыто 5 веток надгробием,
рабочих папок стало 4 из 9.

### Как проверял — каждый критерий командой

| # | критерий | проверка | итог |
|---|---|---|---|
| 1 | компилятор виден из основной папки | `ls _generator/sborka/` → `rc=0`, `ls _generator/sborka/*.py \| wc -l` → **9** (bloki · bootstrap_lekcii · deck · formaty · gejt_kartochki · lenta · podgonka · slaid · tipy); `git_zona.py doctor` → «Локи в .git: ✅ свободно» | ✅ |
| 2 | `fazy-1-2-plan.md` больше не вне git | `git log --oneline -1 -- skills/slajdy/references/fazy-1-2-plan.md` → `c6a646a` | ✅ |
| 3 | зона чиста в обоих репозиториях | `git_zona.py check --zone skills/slajdy` (через `GIT_ZONA_REPO`) → ✅; в `materials` — последним ходом, хэш ниже | ✅ |
| 4 | шесть блоков единой формы у фаз 1 и 2 | скрипт-таблица по всем фазам, **пустых клеток 0, охват 7 из 7 фаз / 42 блока** (таблица ниже) | ✅ |
| 5 | вердикт свежего взгляда с охватом | охват вердикта: 1412 строк четырёх файлов, 42 блока, 22 решения фаз, **84 пути проверено командой**, 41 находка; поимённо ниже | ✅ |
| 6 | `poteri` по каждой закрываемой ветке | 8 из 8, все `rc=0`; сверх того независимая перепроверка `rev-list --count HEAD..<ветка>` = 0 у всех восьми | ✅ |
| 7 | 🔴 живой прогон новым компилятором из ОСНОВНОЙ папки | `python3 _generator/sborka/deck.py _generator/tools/fixtures/sborka/demo-karta -o …` → `rc=0`, «слайдов в деке 8, в резерве 1» — ровно то же, что печаталось в рабочей папке захода (`SESSIYA.md §5-кватер`) | ✅ |

Таблица критерия 4 (воспроизводится скриптом, а не вписана руками):

| фаза | ВХОД | РЕШЕНИЯ ФАЗЫ | ПРОЦЕДУРА | ВЫХОД | ГЕЙТ ВЫХОДА | МЕТРИКА |
|---|---|---|---|---|---|---|
| Фаза 1 · Интервью | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Фаза 2 · Раскадровка | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Фазы 3–7 (контроль) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

### Результат верификатора — 41 находка поимённо, каждая с исходом

Починено на месте — 15 (редакции, каждая якорной заменой с abort при совпадениях ≠ 1):

| # | что нашёл | чем закрыто |
|---|---|---|
| 4 | гейт фазы 2 объявлен «применим ЦЕЛИКОМ», а `bloki.check_composition` даёт FAIL на корректной работе | текст гейта переписан честно + Д38 |
| 9 | `termin_bez_opredeleniya` против `bez_opredeleniya_namerenno` | выправлено по `bootstrap_lekcii.py` |
| 10 | `chto_rasskazyvaem` против `zachem` | то же |
| 11 | блоки: шапка против тела, фаза 1 против фазы 3 | пример шапки выправлен, `bloki:` из шапки снят |
| 12ч | тип `svyazka`, которого нет в закрытом списке | снят из примера (остаток — вопрос 3) |
| 13 | одна строка `ПРИНЯТО` на три разных хука ⇒ ни один гейт не проверяем | введена форма `ПРИНЯТО фаза-N:` + правило в инварианте, 7 адресов |
| 15 | отменённый верификатор «идея закончена» воскрешён клаузой «идея не распадается на две» | клауза снята, отмена названа явно |
| 16 | клауза «слайдов и иллюстраций в бюджете» пропала при переносе | возвращена верификатору плана |
| 18 | формула ×110 адресована в поле другой природы (13 200 против 700) | адрес и природа величин названы + Д42 |
| 19 | «архетип» и `tip_verstki` — один объект под двумя именами | врезка в терминологию |
| 26 | типология: 6 строк таблицы, 8 имён типов | закрытый список из восьми выписан явно |
| 27ч | «ровно один центральный блок» — решение без адреса записи | ⚠-врезка «синтаксиса нет» + Д39 |
| 29 | «доминирующий визуальный архетип» адресован в несуществующее поле | помечено, отнесено к Д39 |
| 34ч | `liniya` — ось не названа (ширина или высота?) | названа по коду: `rows` у горизонтальной, `columns` у вертикальной |
| 35/40 | `tools/schet_byudzheta.py` по правилам чтения путей указывает в пустоту | путь выправлен, исключение из инварианта названо |
| 36ч | `kartochka-slajda §6` обещает «проверяется скриптом» без команды | команда запуска вписана |
| 38 | ссылка на несуществующий `faza-5-illyustracii.md` | заменена на живой дом сита |

Не подтвердилось при перегонке — 1: **№39 «`KONSTITUCIYA §11` — раздела нет»**. Проверено:
нумерованные правила в файле есть (§10, §10а, §11а, §12, §15), и §11 цитируется внутри самого
файла. Ссылка живая, НЕ тронута. Записано и в `DOLG.md`, чтобы ложь не уехала в канон.

Вынесено в `## ВОПРОСЫ` с рекомендацией — 11 (выборы, решать владельцу): №1+2+5+6+7+23 (два дерева
лекции, пул иллюстраций, `zakaz.md` без потребителя) → вопрос 1 · №3 (HTML против markdown) →
вопрос 2 · №12 (четыре редакции списка типов блока) → вопрос 3 · №22 (`rezerv`) → вопрос 4 ·
№20 (списки и сцены) → вопрос 5 · №17+18 (два счётчика бюджета) → вопрос 8 · №28 (поля без
фазы-хозяина) → вопрос 9.

Заведено долгом в `disciplina/skills/slajdy/DOLG.md` — 7 новых (Д37–Д43), каждый с ценой и
командой воспроизведения: непроверяемые клаузы гейтов · `gejt_kartochki.py` не знает фаз ·
пометка центрального блока без синтаксиса · два дерева лекции · двойник `bootstrap_lekcia.py` ·
две природы бюджета · остаток по строке приёмки.

Оставлено фазам 3–7 (это Д3, вне Э3) — 8: №2, 5, 7, 8, 21, 24, 30, 31. Все они про артефакты и
решения фаз, которых заход не касался; каждое названо в `DOLG.md` как содержание Д40.

Верификатор честно объявил семь слепых зон, главная: **ни один гейт не прогонялся на живой лекции
нового формата** — вывод про гейт фазы 2 получен чтением кода, не прогоном. Я это подтвердил
независимо (`gejt_kartochki.py:82` → `bloki.check_composition`), но прогона на живой лекции в новом
формате действительно не было ни у кого: такой лекции пока не существует.

### `poteri` — вывод по КАЖДОЙ ветке (критерий 6)

Все восемь дали `rc=0` («потерь нет»). Так как формулировка `poteri --branch X` называет
покрывающей ссылкой саму `X` (см. вопрос 10), вердикт перепроверен независимым методом —
уникальных коммитов вне текущей ветки:

| ветка | `poteri` | `rev-list --count HEAD..<ветка>` | что сделано |
|---|---|---|---|
| `zahod/dver-urokov` | rc=0 | 0 | **закрыта**, надгробие `mogila/zahod/dver-urokov` → `9e57ffc` |
| `zahod/generator-po-imeni` | rc=0 | 0 | **закрыта**, надгробие → `abab709` |
| `zahod/kanal-nablyudenij` | rc=0 | 0 | **закрыта**, надгробие → `8243e48` |
| `zahod/gejt-sborki` | rc=0 | 0 | **закрыта**, надгробие → `202914a` |
| `zahod/kompilyator-slajda` | rc=0 | 0 | **закрыта**, надгробие → `4de0f88` |
| `zahod/kartochka-i-sborka` | rc=0 | 0 | **НЕ тронута** — по заданию: в рабочей папке артефакты владельца (вопрос 6) |
| `zahod/dovodka-fazy-intervyu` | rc=0 | 0 | **НЕ тронута** — надгробие-омоним уже занято другой веткой (вопрос 7) |
| `zahod/edinstvennost-sekcij` | rc=0 | 0 | **НЕ тронута** — в рабочей папке незакоммиченный автолог `_INFRA-git/INCIDENTY.md` |

Пять закрытий прошли с `--vsyo-ravno "мусор сборки: только __pycache__/egg-info…"`: голый вызов
отказывал из-за игнорируемых путей в рабочих папках. Перед тем как продавливать, проверил, что
там действительно ничего кроме кэша: `status --short --ignored`, отфильтровав
`__pycache__|.DS_Store|egg-info`, — **не-мусорных путей 0 во всех пяти**. Причина уехала в
`INCIDENTY.md` сама.

### Что НЕ трогал

- **`materials`: всё вне зоны** — 29 путей вне git в `_fond/`, `obzory/`, `ucheniki/`,
  `_generator/build_listok.py`, `_studio/docs/KARTA.md` и др. Чужая работа, в мои коммиты не
  попала (проверяется `show --stat` ниже).
- **`disciplina`: пять чужих `DOLG.md`** — `skills/{disciplina-arka,disciplina-git,
  disciplina-istochnik-pravdy,disciplina-zahody,reserch-cikl}/DOLG.md`, все untracked. Это работа
  параллельного захода в `disciplina-wt/etap-20-i-perenos`, заход их не называл — не коммитил и
  не смотрел.
- **Фазы 3–7 скилла** — правил только то, что иначе повисло бы ссылкой в никуда после переписывания
  фаз 1–2 (адрес хука `ПРИНЯТО`, вход фазы 3, путь счётчика бюджета). Переписывание самих фаз 3–7
  — Д3/Д40, отдельный заход.
- **Три рабочие папки и три ветки** — по причинам в таблице выше.
- **`push` в обоих репозиториях** — сознательно, вопрос 11.

### Открытое, к чему возвращаться

Вопрос 1 (два дерева лекции) — блокер: пока он не решён, фазы 1–2 и фазы 4–7 не стыкуются, и
Д38/Д39 чинить преждевременно. Следующий заход по логике — либо он, либо Д29 (гейт вмещения,
из-за которого «всё некрасиво за час до лекции»).

### 🔴 НЕОБРАТИМОЕ

1. **Закрыты 5 веток и снесены 5 рабочих папок** (`materials-wt/{wt-dver, wt-genname, wt-kanal,
   wt-sborki, kompilyator-slajda}`) · восстанавливается по одной командой
   `python3 _generator/tools/git_zona.py voskresit --branch <ветка>` — надгробия
   `mogila/zahod/{dver-urokov, generator-po-imeni, kanal-nablyudenij, gejt-sborki,
   kompilyator-slajda}` держат вершины `9e57ffc`, `abab709`, `8243e48`, `202914a`, `4de0f88`.
   ⚠ **Что надгробие НЕ держит:** игнорируемые пути снесённых папок (1+5+1+1+3 штук) — это
   `__pycache__` и `egg-info`, восстанавливаются пересборкой, проверено что иного там нет.
2. **`main` в `disciplina` передвинута** с `051d795` на `c24d621` (fast-forward, 12 коммитов) ·
   откат: `git -C ~/Documents/GitHub/disciplina branch -f main 051d795`. Рабочее дерево при этом
   не трогалось ни разу.
3. **Слияние ветки в `arka/mat-kostyak`** — merge-коммит `b5f5527` · откат:
   `git reset --hard aa003a1` (точка отката, сделана первым ходом до всех изменений).
4. **Перезаписаны разделы «Фаза 1» и «Фаза 2»** в `disciplina/skills/slajdy/SKILL.md` (старый текст
   удалён, не закомментирован) · восстанавливается `git show 60f5ebc^:skills/slajdy/SKILL.md`.
   Пример шапки в `references/istochnik-istiny.md §3` — `git show 35752db^:…`.

Больше необратимого не было: ни одного файла не удалено, ни одного пути не переименовано и не
перемещено, `git checkout` не делался ни разу ни в одном репозитории.

### Инцидент по ходу, называю сам

Один коммит в `disciplina` не прошёл: рабочий каталог `Bash` остался в `skills/slajdy` от
предыдущей команды, и `add` не нашёл путей. Хуже самого промаха то, что я прочитал `rc=0` — он
пришёл от `tail` в конвейере, а не от `git commit`. Это ровно класс, уже оплаченный и записанный
в `GIT-disciplina §5` («`rc=0` конвейера — НЕ код возврата первой команды»); нового урока не
завожу, потому что новой цены нет: поймано тем же ходом, коммит повторён из корня, `35752db`.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Downloads/demo-karta-svedenie-2026-08-07.html` — открыть
браузером (двойным щелчком). Это демо-дек, собранный **новым компилятором из ОСНОВНОЙ папки**
`materials` уже после слияния — доказательство критерия 7: путь сборки доехал и работает не только
в рабочей папке захода. 8 слайдов, один в резерве.
Второй предъявимый артефакт — сам скилл: `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md`,
семь фаз, 42 блока из 42.
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
