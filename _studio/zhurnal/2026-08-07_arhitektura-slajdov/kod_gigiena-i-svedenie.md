# Канал исполнителя — gigiena-i-svedenie (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — Ш1–Ш2 механические, а Ш3 (ревизия) и Ш5 (вшить гигиену в генератор) требуют
> решений по ходу: что считать потерянным, какие проверки обязательны каждому заходу.

КОНТЕКСТ. Фабрика научно-популярных лекций, лекция 2, эфир в среду.

**За сутки фабрика выросла сильно, но почти всё выросшее живёт по невлитым веткам.** Смета вмещения,
гейт вёрстки, кэш формул KaTeX, карта живого конвейера, доктрина вёрстки — построены, проверены,
закоммичены. Из основной папки видна **малая часть**.

**Замер на момент сборки захода:**
```
git --no-optional-locks branch --no-merged arka/mat-kostyak     → 9 веток
git --no-optional-locks rev-list --count arka/mat-kostyak..zahod/svedenie-arki  → 19
git --no-optional-locks rev-list --count zahod/svedenie-arki..arka/mat-kostyak  → 3
```
🔴 **`svedenie-arki` уже свела в себя все восемь веток и проверила их совместную работу** — но сама
не влита, и за это время основная папка ушла вперёд на 3 коммита. Слияние будет настоящим, не
перемоткой.

**И одна находка, которую нельзя было сделать до слияния** — её сделал `svedenie-arki`:
```
python3 _generator/tools/check_tool_contract.py _generator/tools/gejt_verstki.py   → ❌
grep -rn 'gejt_verstki' вне самого файла и его фикстуры                            → 0 строк
```
Заход `verstka-v-faze-1` сдавал по этой же команде `rc=0` и **не врал**: строгая проверка
`has_live_trigger` жила на соседней невлитой ветке. **Дефект родился в момент встречи двух веток.**
Цена немедленная: `pre-commit` зовёт `check_tool_contract.py --staged`, значит следующий, кто тронет
этот файл, встанет на чужом красном.

🔴 **ГЛАВНОЕ ТРЕБОВАНИЕ ВЛАДЕЛЬЦА — не разовая уборка, а механизм.** Дословно: *«проверить всё, что
могло быть не влито или не закоммичено, влить и закоммитить, проверить что всё согласовано,
обновить карту системы — и более того, прописать это как раздел ГИГИЕНА каждого захода, прописав в
генераторе автоматически»*.

ЦЕЛЬ: невлитого и потерянного нет; карта отражает слитое дерево; **каждый следующий заход рождается
с разделом «ГИГИЕНА» внутри, и аналитику остаётся только проставить конкретные зоны.**

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели: Ш1 (слияние) провалился — Ш5 (генератор) НЕ начинаешь.** Вшивать гигиену в
генератор поверх несведённого дерева значит вшить её дважды.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/gigiena`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/gigiena-i-svedenie`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add gigiena --branch zahod/gigiena-i-svedenie
  cd ../materials-wt/gigiena
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` `_studio/docs/KARTA.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ВЛИТИЕ — ДО ВСЕГО ОСТАЛЬНОГО

```
python3 _generator/tools/git_zona.py merge zahod/svedenie-arki --zone _generator --zone _studio --zone .githooks --zone teorkat-vvedenie --zone disciplina --zone _fond --zone obzory --zone ucheniki
```

🔴 **Зона слияния — НЕ рабочая зона захода, а ВСЁ, что трогает вливаемая ветка.** Проверено при
сборке: вливаемое трогает 63 пути вне рабочей зоны. Узкая зона → инструмент откажется слить
что-либо; на этом уже вставали два захода подряд.
⚠ **Отказал — возьми строку зон из его собственного отказа**, он печатает её верной. Не подгоняй
под свою зону: слияние это операция над ветками, а не правка зоны.

Проверка фактом на диске, а не словом инструмента:
```
git --no-optional-locks merge-base --is-ancestor zahod/svedenie-arki HEAD ; echo $?
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/gigiena-i-svedenie`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` `_studio/docs/KARTA.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 `_generator/tools/bootstrap_zahod.py` | **предмет главной работы Ш5** — сюда вшивается раздел ГИГИЕНА |
| Я2 | `_generator/tools/check_sborki.py` — **разделы про С1, С3, С7** | гейт, который уже умеет судить пути, ветки и зоны слияния; **гигиена обязана с ним сойтись, а не спорить** |
| Я3 | `_generator/tools/check_tool_contract.py` · `git_zona.py` — **докстринги и `--help`** | чем проверяется «инструмент подключён» и чем ходят в git |
| Я4 | `_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` **целиком** | предмет Ш4; уже существует, ты её обновляешь, а не пишешь заново |
| Я5 | `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_svedenie-arki.md`, **секции `## ОТЧЁТ` и `## ВОПРОСЫ`** | что уже сведено, что найдено и что осталось — не переоткрывай |
| Я6 | `_generator/sborka/gejt_vmeshcheniya.py` — **только докстринг** | образец инструмента, который был написан и годами не вызывался: ровно тот класс, который ловит гигиена |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — пять шагов, порядок жёсткий

### Ш1. ВЛИТЬ `svedenie-arki` (команды — §0.1)

Проверка фактом: `git merge-base --is-ancestor` по **всем девяти** веткам `zahod/*` поимённо.
Ветки НЕ удаляй ни при каком исходе — решение владельца.

### Ш2. 🔴 ПОДКЛЮЧИТЬ `_generator/tools/gejt_verstki.py` — иначе следующий встанет на чужом красном

Выбери и обоснуй в `## ПЛАН` одно из двух: живая точка вызова (строка в `LIFECYCLE_TMPL`
`bootstrap_lekcii.py` как шаг фазы, или в `.githooks/pre-commit`) **либо** явный маркер
`# TOOL-CONTRACT: called-by-hand`, если гейт по замыслу ручной.
⚠ **Не бери маркер по умолчанию как более дешёвый.** Гейт вёрстки проверяет доктрину владельца —
у неё есть фаза, на которой она решается. Маркер «по руке» означает «никто не обязан его звать».

### Ш3. 🔴 РЕВИЗИЯ: ЧТО ЕЩЁ МОГЛО ПОТЕРЯТЬСЯ — сплошная, не выборочная

Владелец просил проверить **всё**, что могло быть не влито или не закоммичено. Четыре класса, и по
каждому — команда, а не взгляд:

1. **Невлитые ветки** — `git branch --no-merged arka/mat-kostyak`. По каждой оставшейся: пуста
   (нечего вливать) или несёт работу? Число и вердикт по каждой.
2. **Незакоммиченное в основной папке** — `git status --porcelain`. Разделить на «работа фабрики» и
   «чужое/личное». 🔴 `_fond/**`, `obzory/**`, `_generator/build_listok.py`, `ucheniki/**` —
   **чужое, не трогать**; назвать числом и оставить.
3. **Второй репозиторий** — `~/Documents/GitHub/disciplina`: `git status --porcelain` и
   `git branch --no-merged`. Его забывали трижды за сутки.
4. 🔴 **Инструменты без живой точки вызова** — тот же класс, что Ш2, но сплошняком:
   `python3 _generator/tools/check_tool_contract.py $(ls _generator/tools/*.py _generator/sborka/*.py)`.
   Каждый красный — либо подключить, либо объявить ручным, либо **назвать в отчёте с причиной**,
   почему оставлен. **Молча оставленных быть не должно.**

⚠ **Ты НЕ чинишь всё найденное.** Чинишь Ш2 и то, что чинится строкой в твоей зоне. Остальное —
пунктами очереди с домом. Заход про ревизию, а не про героизм.

### Ш4. ОБНОВИТЬ КАРТУ ЖИВОГО КОНВЕЙЕРА (Я4)

Карта написана на несведённом дереве и в двух местах устарела — **обе правки известны, не ищи
заново:**
- 🔴 **Фаза 4, «ГЕЙТ ВМЕЩЕНИЯ (не существует — долг)» — НЕВЕРНО.** `gejt_vmeshcheniya.py`
  существует (Я6), написан давно, вчера подключён закрытием фазы 3. Карта в этом месте **прочитала
  документацию вместо диска** — ровно тот грех, ради разоблачения которого писалась;
- **44 `⟦MISSING-MATH⟧`** — число с невлитого дерева. На слитом их **0**.

🔴 **И добавь в карту колонку/пометку класса «инструмент есть, вызова нет»** — по итогам Ш3.4.
Это самый частый класс поломок фабрики, и карта обязана его показывать, иначе она показывает не то.

### Ш5. 🔴🔴 ГИГИЕНА — ВШИТЬ В ГЕНЕРАТОР ЗАХОДОВ. Главная работа захода.

Требование владельца: раздел **ГИГИЕНА** рождается в КАЖДОМ заходе автоматически, из
`bootstrap_zahod.py`, а аналитик проставляет в нём **только конкретные зоны**.

**Что в разделе — минимум; расширить можно, урезать нет. Каждый пункт — команда, не пожелание:**

| # | проверка | команда-образец |
|---|---|---|
| Г1 | зона доехала в git | `git_zona.py check --zone <ЗОНА>` |
| Г2 | во ВТОРОМ репозитории тоже (если зона туда заходит) | `cd <репо> && git status --porcelain` |
| Г3 | невлитых веток не прибавилось | `git branch --no-merged <база>` |
| Г4 | новый инструмент имеет живую точку вызова | `check_tool_contract.py <новые .py>` |
| Г5 | новый `.md` зарегистрирован | `grep -c <имя> _studio/docs/KARTA.md` |
| Г6 | в коммите нет чужих путей | `git show --stat` |

🔴 **Три свойства, без которых это не механизм, а надежда:**
1. **Слот зоны — пустой и обязательный.** Аналитик подставляет пути; **незаполненный слот обязан
   краснеть** в `check_sborki.py` (образец уже есть — гейт З10 на пустой слот, он приехал с ветки
   `zahod/konsolidaciya-i-git-kontur` (уже влита), посмотри как сделано).
2. **Раздел не удаляется исполнителем** — как `## КОНТРАКТ ЗОНЫ`.
3. **Заход, где зона не заходит во второй репозиторий, не должен нести пункт Г2 мёртвым грузом** —
   реши, как: генератор ставит его по факту `--zone`, или пункт несёт явное «неприменимо, потому что».

⚠ **Не дублируй то, что уже проверяет `check_sborki.py` (Я2).** С1, С3, С7 работают и ловят
существование ветки, путей и зоны слияния. Гигиена — про **состояние репозитория после работы**,
а не про правильность файла-захода. Пересечение назови явно и убери.

⚠ **Фикстуры обязательны:** заход, собранный с пустым слотом зоны → красный; с заполненным → зелёный.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `git branch --no-merged arka/mat-kostyak` — по каждой оставшейся ветке вердикт «пуста / несёт
   работу», числом. `svedenie-arki` среди оставшихся нет.
2. `check_tool_contract.py` по `_generator/tools/*.py _generator/sborka/*.py` → **rc=0**, либо
   каждый красный назван в отчёте с решением. `_generator/tools/gejt_verstki.py` зелёный.
3. 🔴 **Живой прогон на реальном объекте репозитория:** свежесобранный заход-проба
   (`bootstrap_zahod.py` в `/tmp`) **несёт раздел ГИГИЕНА**; с пустым слотом зоны
   `check_sborki.py` → **красный**, с заполненным → **зелёный**. Обе команды и оба rc в отчёт.
4. Регресс: все `PROGNAT.sh` (число — `find _generator -name PROGNAT.sh | wc -l`), зелёных N из N.
5. Карта: обе известные ошибки исправлены, класс «инструмент есть, вызова нет» в ней присутствует.
6. Оба репозитория чисты, `git show --stat` без чужих путей, хэши отдельными строками.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «всё сведено», а «влито N из M, инструментов без
вызова K, из них подключено L, оставлено с причиной P».

## 3. ВЕРИФИКАТОР

🔴 **Самый опасный исход этого захода — раздел ГИГИЕНА, который все начнут пропускать.** Мы уже
получили такое дважды: `--no-verify` (28 обходов из 56 срывов) и ложные FAIL трекера. Раздел,
который краснеет всегда или не краснеет никогда, будет вычеркнут из шаблона через неделю.

- 🔴 **Прогнать гигиену на ТРЁХ РЕАЛЬНЫХ заходах этой арки** — взять уже сданные `kod_svedenie-arki.md`,
  `kod_verstka-reshaetsya-v-faze-1.md`, `kod_sborka-l2-deka.md` и ответить: на каждом из них
  раздел дал бы **осмысленный** результат или шум? **Хотя бы один даёт шум — форма не готова.**
  Разбор по трём в отчёт.
- 🔴 **Свежий субагент, не видевший сессии, читает ТОЛЬКО свежесобранный заход** и отвечает: что
  он обязан сделать перед сдачей и как проверит. Не назвал зону и второй репозиторий — раздел
  написан для нас, а не для исполнителя.
- **Ш4 сверить фактом:** каждая исправленная строка карты подтверждена командой на слитом дереве,
  а не чтением документации. Это её же собственная болезнь.
- **Ничего не потеряно при слиянии:** `git show --stat`, регресс до и после.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` `_studio/docs/KARTA.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/**` `_studio/zhurnal/2026-08-07_arhitektura-slajdov/**` `_studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` `_studio/docs/KARTA.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Порядок шагов в задании был не последовательностью, а замком — и это видно только исполнителю
Ш1 (слияние) нельзя было завершить до Ш2 (подключить гейт): `pre-commit` не давал закоммитить merge, пока `gejt_verstki.py` без точки вызова. А Ш2 нельзя было начать до Ш1: файла на дереве не существовало. Задание же требовало «порядок жёсткий» и «Ш1 провалился — Ш5 не начинаешь». Общая форма: **когда заход вливает ветку, привозящую новый инструмент, гейты хука судят этот инструмент ПРЯМО В МОМЕНТ merge-коммита** — то есть шаг «влить» неотделим от шага «привести влитое в порядок», и разносить их по пунктам нельзя.
ЦЕНА: два падения `merge --continue` и полчаса на распутывание того, что выглядело как сбой инструмента. Плюс: правка `gejt_verstki.py` физически уехала в merge-коммит, а не в коммит зоны, — приёмка, читающая только последний коммит, её там не найдёт.

### Гейт на пустоту, написанный без прогона на пустоте, не краснеет никогда
Регулярка слота `\s*` вместо `[ \t]*`: `\s` включает `\n`, поэтому на ПУСТОМ слоте выражение перепрыгивало пустую строку и захватывало следующую строку файла как «содержимое». Гейт был зелёным во всех случаях, включая тот единственный, ради которого писался. Код при этом читается правильно, ревью его пропускает, и на заполненном слоте он работает верно.
ЦЕНА: поймано первым живым прогоном пустого случая — то есть ценой ноль. Но если бы критерий готовности требовал только «гейт написан и на здоровом заходе зелёный» (а так формулируют часто), дефект уехал бы в канон и обнаружился бы через неделю как «раздел ГИГИЕНА никого ни разу не остановил — выкидываем». **Правило: проверку на пустоту гоняют на пустоте, а не на заполненном образце.**

### Половина ложных красных превращает гейт в кандидата на обход — и это измеримо заранее
`check_tool_contract.py` давал 7 красных, из которых 3 были ложными: охват `has_live_trigger()` не смотрел в `_generator/sborka/`, где живёт боевой конвейер сборки. Инструменты, честно импортируемые соседями, числились «нигде не вызываемыми».
ЦЕНА: прямой цены в этом заходе нет — ложные красные никого не остановили, потому что их молча пролистывали. Это и есть цена: гейт, у которого 43 % срабатываний ложны, читают по диагонали, и настоящий красный (`gejt_verstki.py`) в этой куче неотличим от шума. Он и простоял незамеченным сутки, хотя `pre-commit` звал проверку на каждом коммите. **Доля ложных срабатываний — измеримая величина, и её надо мерить до того, как гейт объявят работающим.**

## ПЛАН — (заполняет исполнитель)

**Состояние на входе (снято командой, до действий):**
- рабочая папка `../materials-wt/gigiena` заведена, `git rev-parse --abbrev-ref HEAD` → `zahod/gigiena-i-svedenie`, `git status --porcelain` → пусто (точку отката фабриковать не надо: зона чиста в HEAD);
- `git branch --no-merged arka/mat-kostyak` → **9 веток**, `zahod/svedenie-arki` среди них;
- `_generator/tools/gejt_verstki.py` на этом дереве **НЕ СУЩЕСТВУЕТ** (`ls` → No such file). То есть Ш2 физически невозможен до Ш1 — порядок шагов в задании не формальность, а зависимость по файлу. Читать якоря Я2/Я3/Я4/Я5 имеет смысл только на СЛИТОМ дереве: З10, `gejt_verstki.py`, «44 MISSING-MATH» — всё это свойства слитого состояния. Поэтому Я1 (`bootstrap_zahod.py`, он есть и здесь) прочитан до плана, остальные якоря читаю сразу после Ш1.

**Предпосылки, которые проверю фактом, а не приму на веру:**
1. «`svedenie-arki` свела в себя все восемь веток» — проверю `merge-base --is-ancestor` по всем девяти `zahod/*` поимённо (Ш1), а не доверюсь числу из шапки захода.
2. «44 `⟦MISSING-MATH⟧` → на слитом 0» — пересчитаю командой на слитом дереве. Если не 0, в карту уйдёт посчитанное число, а не обещанное.
3. «Гейт З10 на пустой слот уже есть, приехал с `konsolidaciya-i-git-kontur`» — эта ветка сейчас числится невлитой в `arka/mat-kostyak`; значит «уже влита» относится к `svedenie-arki`, и З10 я увижу только после Ш1. Не увижу — скажу в ВОПРОСАХ и напишу гейт с нуля, не выдавая отсутствие образца за наличие.

**Развилка Ш2 — решаю по одному критерию, названному ДО чтения файла**, чтобы не подгонять критерий под то, что окажется проще: маркер `called-by-hand` законен ТОЛЬКО если у гейта нет фазы, на которой его обязаны звать. Есть фаза (доктрина вёрстки решается в фазе 1 — так называется соседний сданный заход `kod_verstka-reshaetsya-v-faze-1.md`) → живая точка вызова в `LIFECYCLE_TMPL` `bootstrap_lekcii.py`. Выбор и его обоснование допишу сюда же после чтения `gejt_verstki.py`, до правки.

**РЕШЕНИЕ Ш2 (дописано после чтения, до правки). Беру маркер — и НЕ потому, что он дешевле.** Оба варианта, названные в Ш2, отпали по проверяемым причинам:
1. 🔴 **Оба лежат ВНЕ моей зоны.** `bootstrap_lekcii.py` — это `_generator/sborka/**`, `.githooks/pre-commit` — это `.githooks/**`; зона захода — `_generator/tools/**`. Контракт зоны запрещает трогать их «даже если чинится в одну строку», и мой же `commit` с pathspec их бы не забрал — правка осталась бы висеть незакоммиченной и умерла бы при первом `checkout`.
2. 🔴 **Строка в `LIFECYCLE_TMPL` гейт бы и НЕ ПОЗЕЛЕНИЛА** — то есть вариант «живая точка вызова» в этой конкретной форме не работает вовсе, а не просто неудобен. `check_tool_contract.has_live_trigger()` ищет упоминание в `live_trigger_scope()` = `_generator/tools/*.py` + файлы верхнего уровня `_generator/` + `.githooks/*`. Папка `_generator/sborka/` в охват НЕ входит (`REPO/_generator` обходится `iterdir()` с `p.is_file()` — подпапка отсеивается). Доказательство на живом объекте, а не рассуждением: `_generator/sborka/gejt_vmeshcheniya.py` **имеет** строку в `LIFECYCLE_TMPL` («ФАЗА 3.9») и зелёный — но `grep 'gejt_vmeshcheniya' _generator/tools/*.py _generator/*.py .githooks/*` даёт **0 строк**; зелёный он ровно из-за своего маркера `called-by-hand`.
3. Фикстура `fixtures/verstki/PROGNAT.sh` с `TOOL-CONTRACT-COVERS: gejt_verstki.py` уже существует — но `fixtures/` тоже вне охвата `has_live_trigger`, поэтому охват тестами и живая точка вызова здесь разные вещи.
4. Подделывать вызов из файла в моей зоне (`bootstrap_lekcia.py`, `sostoyanie_lekcii.py`) отказываюсь: первый скаффолдит папку лекции по другому скелету, второй — VIEW над архивным курсом `teoriya-kategoriy`. Вызов оттуда был бы зелёным гейтом при мёртвом инструменте — ровно тот класс, который заход и ловит.

**Форма маркера — та же, что фабрика уже приняла 08.08 для `gejt_vmeshcheniya.py`: маркер + НАЗВАННАЯ фаза, на которой гейт обязаны звать.** Плюс пункт очереди с домом `_generator/sborka/bootstrap_lekcii.py` на саму строку фазы: без неё «по руке» действительно означает «никто не обязан», и я это не заминаю, а называю долгом с адресом. Заодно правлю докстринг `gejt_verstki.py`: он сейчас утверждает, что живая точка вызова — фикстура и строка lifecycle, а по факту первой нет в охвате, второй нет вовсе.

**🔴 Оспариваю пункт 3 КРИТЕРИЯ ГОТОВНОСТИ (формулировку, не суть).** «Свежесобранный заход-проба (`bootstrap_zahod.py` в `/tmp`)» **буквально неисполним**, и это видно по коду Я1: `bootstrap_zahod.py` отказывает на папке арки вне репозитория (`ОШИБКА: папка арки вне репозитория`), всегда пишет файл в `REPO_ROOT`, а следом зовёт `register_doc.py`, который допишет строку в живой `KARTA.md`. Проба «в /tmp» в лоб означала бы либо отказ инструмента, либо мусорный заход и мусорную строку индекса в боевом репозитории.
Исполню суть требования — живой прогон на реальном объекте, а не на моке — в форме, которую фабрика уже применяет: **одноразовая копия репозитория в `/tmp` (приём фикстур `_generator/tools/fixtures/`: инструменты кладутся в `$T/_generator/tools/`, `korni.REPO` = `parents[2]`, есть штатная дверь `GIT_ZONA_REPO`)**. Прогоняется НАСТОЯЩИЙ `bootstrap_zahod.py`, а не его имитация; обе команды и оба `rc` — в отчёт. Если владелец имел в виду именно боевой репозиторий — скажите, переделаю, но тогда за собой уберу пробный заход и строку индекса отдельным ходом.

**Порядок работы (жёсткий, со стоп-условиями):**
1. **Ш1** — `git_zona.py merge zahod/svedenie-arki` с зонами из §0.1; отказал по зонам — беру строку зон из его собственного отказа. Проверка: `merge-base --is-ancestor` по всем девяти веткам поимённо, `echo $?` первым. Не сошлось — СТОП в ВОПРОСЫ, Ш5 не начинаю (правило «стоп до цели»).
2. Чтение якорей Я2–Я6 на слитом дереве + перечитывание Я1 (мог измениться слиянием).
3. **Ш2** — по критерию выше; проверка `check_tool_contract.py _generator/tools/gejt_verstki.py` → rc=0.
4. **Ш3** — четыре класса сплошняком, каждый командой. Чиню только Ш2 и то, что чинится строкой в зоне; остальное — пунктами очереди с домом. Чужое (`_fond/**`, `obzory/**`, `_generator/build_listok.py`, `ucheniki/**`) называю числом и не трогаю.
5. **Ш4** — обе известные правки карты + колонка класса «инструмент есть, вызова нет»; каждая правка подтверждена командой на слитом дереве, а не чтением документации.
6. **Ш5** — раздел ГИГИЕНА в `bootstrap_zahod.py` (тем же приёмом anchor-replace, что уже применён к ВОПРОСЫ/УРОКИ/ВЛИТИЕ — свой приём не изобретаю), гейт на пустой слот в `check_sborki.py` по образцу З10, фикстура «пустой слот → красный / заполненный → зелёный». Пересечение с С1/С3/С7 назову явно и уберу.
7. Верификатор: прогон формы на трёх реальных заходах арки + свежий субагент на свежесобранном заходе (≤5 субагентов, рейт-лимит = отступить и доложить).
8. Регресс всех `PROGNAT.sh` (число — командой), коммит зоны двумя ходами с путями в обоих, `check --zone`, `show --stat`.

**Что НЕ делаю:** ветки не удаляю ни при каком исходе; свою ветку не вливаю; файл-заход не коммичу; ничего вне зоны не правлю, даже «в одну строку».

## ВОПРОСЫ — (заполняет исполнитель)

1. **Строка фазы для `gejt_verstki.py` в `bootstrap_lekcii.LIFECYCLE_TMPL` (ФАЗА 2) не поставлена.** Гейт подключён маркером `called-by-hand` с названной фазой; охват `has_live_trigger` этим заходом расширен на `_generator/sborka/*.py`, поэтому строка теперь СРАБОТАЕТ — и маркер можно будет снять. Не сделано, потому что файл в чужой зоне.
   ДОМ: `_generator/sborka/bootstrap_lekcii.py`
   ДОСТАВЛЕНО: нет

2. 🔴 **Бэктики внутри shell-блока §4 делают команду коммита неисполнимой дословно — во ВСЕХ заходах.** Генератор подставляет один и тот же `{{ЗОНА}}` и в строку КОНТРАКТА (там бэктики нужны — на них смотрит З3), и в блок кода §4, где `` `_generator/tools/**` `` — это подстановка команды, а не путь. Найдено свежим субагентом-верификатором, который прямо сказал: «выполнять дословно нельзя, бэктики надо снять». Чинится двумя плейсхолдерами (`{{ЗОНА}}` для прозы, `{{ЗОНА_КОМАНДОЙ}}` для кода), но шаблон вне зоны. **Повторяется на каждом заходе.**
   ДОМ: `_studio/zhurnal/_TEMPLATE-zahod.md` (+ `_generator/tools/bootstrap_zahod.py`)
   ДОСТАВЛЕНО: нет

3. **Хук `pre-commit` принимает фикстуру за живую лекцию.** `LEC_ROOTS` собирается из staged-путей вида `*/slajdy/*/slaid.md`, и `_generator/tools/fixtures/verstki/polosa-s-illustraciej` попала туда как лекция — `gejt_illyustracij.py` покраснел на тестовых данных и остановил слияние. Заткнул в своей зоне (положил фикстуре `zakaz.md`), но корневая починка — одна строка `case "$LEC" in */fixtures/*) continue;; esac`. **Повторится на следующей фикстуре со слайдами.**
   ДОМ: `.githooks/pre-commit`
   ДОСТАВЛЕНО: нет

4. **Три инструмента `_generator/sborka/` красны по классу «кривой вход»** — `lenta.py`, `slaid.py`, `vmeshchenie.py`: нет спутник-фикстуры, доказывающей, что кривой вход отвергается. Честно закрыть = написать три фикстуры кривого входа, это отдельный заход; объявить `no-input` было бы неправдой (все три разбирают вход).
   ДОМ: `_generator/sborka/**`
   ДОСТАВЛЕНО: нет

5. **Фикстура пишет в отслеживаемое дерево.** `fixtures/uroki/PROGNAT.sh` штампует сегодняшнюю дату в `fixtures/uroki/zakrytie/DOLG.md`, то есть каждый регресс делает зону грязной и роняет Г1 у того, кто регресс прогнал. Нарушает собственное правило семьи («гейт не смеет писать в проверяемый репозиторий», докстринг `check_tool_contract.py`, урок 1.7). Закоммитил изменённую дату, чтобы не оставлять зону грязной. **Повторяется на каждом прогоне регресса.**
   ДОМ: `_generator/tools/fixtures/uroki/PROGNAT.sh`
   ДОСТАВЛЕНО: нет

6. **Кэш формул отстал от карточки.** `math/katex.json` не содержит 3 формул слайда `centr-gruppy` — пересборка дека L2 сегодня встаёт с `rc=1`. Лечится `node _generator/sborka/kesh_formul.js teorkat-vvedenie/L2`.
   ДОМ: `teorkat-vvedenie/L2`
   ДОСТАВЛЕНО: нет

7. 🔴 **Приёмке — предупреждение о конфликте.** В ОСНОВНОЙ папке `_studio/docs/KARTA.md` изменён и НЕ закоммичен (+2 строки: регистрация этого захода), а моя ветка тот же файл меняла при разрешении конфликта слияния. Слияние моей ветки в `arka/mat-kostyak` с этой незакоммиченной правкой git отвергнет. Сначала закоммитить правку в основной папке, потом вливать.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Downloads/PROBA-zahod-s-razdelom-GIGIENA.md` — открыть любым просмотрщиком markdown или текстовым редактором. Это НАСТОЯЩИЙ заход, собранный настоящим `bootstrap_zahod.py` (не макет): смотреть раздел **«## 4.1 🔴 ГИГИЕНА»** — то, что теперь рождается в каждом заходе само. Пробное дерево, в котором он собран, снято после замера.

**КОММИТ:** `d4bfeb60cecf64b81c3c899edc83005cbadd8db8` — «gigiena: раздел ГИГИЕНА в генератор заходов (гейт С8), гейт вёрстки подключён, охват контракта инструментов расширен на sborka/, карта конвейера сведена со слитым деревом» · `git_zona.py check --zone _generator/tools` → ✅ · `--zone _studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md` → ✅ · `--zone _studio/docs/KARTA.md` → ✅ · `git show --stat` → 9 файлов, все свои.
**Коммит слияния:** `342babb` — `Merge branch 'zahod/svedenie-arki' into zahod/gigiena-i-svedenie`.

---

### Вердикт с охватом в себе

**Влито 31 из 31** ветки `zahod/*` (все — предки HEAD, `merge-base --is-ancestor` → `0` по каждой поимённо; `git branch --no-merged HEAD` → пусто). **Инструментов без вызова было 7, стало 3**, из них подключено 1 (`gejt_verstki.py`), 3 оказались ЛОЖНЫМИ красными и закрыты починкой самой проверки, 3 оставлены с причиной. **Регресс 24 из 24 зелёных.**

### Ш1. Слияние — сделано, но не так гладко, как в задании

`git_zona.py merge` дал **конфликт в 2 путях**, разрешил сам:
- `_studio/docs/KARTA.md` — объединением (индекс: обе стороны дописывали разные регистрации);
- `teorkat-vvedenie/L2/dist/index.html` — взял сторону `svedenie-arki` по замеру, а не по интуиции: наша сторона несла **44** `⟦MISSING-MATH⟧`, сторона ветки — **0**. Это же и подтвердило предпосылку Ш4 №2.

🔴 **Слияние дважды упало на дефекте, который сам же и привёз** — `pre-commit` не давал закоммитить merge, пока `gejt_verstki.py` без точки вызова. Порядок Ш1→Ш2 оказался не последовательностью, а замком: Ш2 нельзя сделать до слияния (файла нет на дереве) и слияние нельзя завершить до Ш2. Сделал Ш2 внутри разрешения конфликта — поэтому правка `gejt_verstki.py` уехала в merge-коммит.

🔴 **`git_zona.py merge --continue` падал `UnicodeDecodeError`** (байт `0xe2`, позиция 21674 в stderr, дважды подряд на одном смещении), при том что `git commit` отрабатывал. Причина: в stderr коммита пишет не только git — хук поднимает десятки python-процессов на одном пайпе, и параллельные записи длиннее `PIPE_BUF` режут многобайтовый символ. **Merge-коммит пришлось довести голым `git --no-optional-locks commit --no-edit` в обход единственной двери** — называю прямо, а не заминаю. Дефект починен в зоне: `errors="replace"` в `git_zona.git()`.

### Ш2. Гейт вёрстки — маркер, и вот почему это не «дешёвый выбор»

Критерий выбора назвал в ПЛАНЕ ДО чтения файла. **Оба варианта из задания отпали по проверяемым причинам:** оба лежат вне зоны (`_generator/sborka/**`, `.githooks/**`), а строка в `LIFECYCLE_TMPL` гейт бы **и не позеленила** — охват `has_live_trigger()` подпапку `_generator/sborka/` не смотрел вовсе. Доказательство живым объектом: у `gejt_vmeshcheniya.py` строка «ФАЗА 3.9» в LIFECYCLE есть, а `grep` по охвату даёт `0` — зелёный он тоже маркером. Взял форму, которую фабрика приняла 08.08: маркер + **названная фаза** (ФАЗА 2) + пункт очереди с адресом на саму строку. Заодно поправил докстринг, утверждавший, что живая точка вызова — фикстура (она вне охвата).
`check_tool_contract.py _generator/tools/gejt_verstki.py` → **rc=0**.

### Ш3. Ревизия — четыре класса, каждый командой

1. **Невлитые ветки.** Против `arka/mat-kostyak` — **10** (девять чужих + моя, ещё не влитая приёмкой). По каждой из девяти: `rev-list --count HEAD..<ветка>` → **0**, то есть **все девять пусты** — работы, которой нет в моей ветке, они не несут. `git_zona.py poteri` → «Потерь нет, проверено 31 из 31». Ветки не удалял.
2. **Незакоммиченное в основной папке — 55 путей**, разложены: **24 чужих** (`_fond/` 12, `ucheniki/` 9, `obzory/` 2, `_generator/build_listok.py` 1) — не трогал; **27 артефактов прогонов фикстур** (`fixtures/sborka/obratnyj-progon/*/dist/`, `*/math/`) — мусор, кандидат в `.gitignore`; **4 работы фабрики** — `KARTA.md` (+2), `kod_svedenie-arki.md` (отчёт прошлого захода), `INCIDENTY.md` (+1 автолог), и сам этот файл-заход (untracked — так и должно быть, его коммитит приёмка). Из worktree их закоммитить физически нельзя: другое рабочее дерево, другой индекс.
3. **Второй репозиторий `~/Documents/GitHub/disciplina`** — `git status --porcelain` → **пусто**, `git branch --no-merged` → **пусто**, стоит на `zahod/format-kartochki-faza-1`. Чист.
4. **Инструменты без живой точки вызова — было 7, стало 3.** 🔴 **Три из семи были ЛОЖНЫМИ**: `bloki.py`, `korpus.py`, `podgonka.py` импортируются соседями внутри `_generator/sborka/`, а охват проверки эту подпапку отсеивал (`iterdir()` + `p.is_file()`). Половина ложных срабатываний на сплошном прогоне — гейт с такой долей обходят, а не чинят по нему; канон здесь прямо велит чинить ПРОВЕРКУ. Охват расширен. Оставшиеся 3 — `lenta.py`, `slaid.py`, `vmeshchenie.py`, класс «кривой вход», названы в `## ВОПРОСЫ` п.4 с причиной. **Молча оставленных нет.**

Попутно, в своей зоне: хук принимал фикстуру `fixtures/verstki/polosa-s-illustraciej` за живую лекцию и краснел на ней `gejt_illyustracij.py` — положил фикстуре недостающий `zakaz.md` (корневая починка — в `## ВОПРОСЫ` п.3).

### Ш4. Карта — обе ошибки исправлены, каждая подтверждена командой

- **Фаза 4, «ГЕЙТ ВМЕЩЕНИЯ (не существует — долг)»** → неверно: `ls -la _generator/sborka/gejt_vmeshcheniya.py` → 5217 байт, `grep -c gejt_vmeshcheniya _generator/sborka/bootstrap_lekcii.py` → **1** (ФАЗА 3.9).
- **44 `⟦MISSING-MATH⟧`** → на слитом дереве **0** (`grep -c MISSING-MATH teorkat-vvedenie/L2/dist/index.html`). 🔴 **Изменилось не только число, а класс отказа:** та же команда, что дала 44, теперь даёт `rc=1` — сборка ОТКАЗЫВАЕТСЯ вместо тихой дыры (`ОШИБКА: слайд centr-gruppy: 3 формул(ы) нет в кэше`). Живой остаток записан в карту и в `## ВОПРОСЫ` п.6.
- **Добавлен раздел «§1а. Класс ИНСТРУМЕНТ ЕСТЬ, ВЫЗОВА НЕТ»** — с командой замера, таблицей 7→3, разбором ложных красных и объявленной слепой зоной эвристики.

### Ш5. ГИГИЕНА в генераторе — главная работа

**Раздел `## 4.1 ГИГИЕНА` рождается в каждом заходе** из `bootstrap_zahod.gigiena_blok()`, тем же приёмом anchor-replace, что ВОПРОСЫ/УРОКИ/ВЛИТИЕ (шаблон не тронут ни байтом — чужой файл). Шесть пунктов Г1–Г6, каждый командой.

**Пересечения названы и убраны — их оказалось два, и одно задание не предвидело:**
- 🔴 **`## 0.1 ГИТ-КОНТУР` уже существует** (приехал слиянием, гейт З10). Это НАМЕРЕНИЕ до работы: что влить, что коммитить, что закрыть. ГИГИЕНА — ФАКТ после работы: доехало ли. Слот 1 говорит «влей ветку X», Г3 спрашивает «не прибавилось ли невлитых, пока ты работал». Разные моменты, разные ответы — но если бы я этого не развёл, вышла бы третья копия одного места.
- **С1/С3/С7** судят правильность ФАЙЛА-захода (ветка существует, пути существуют, зона слияния не уже). Ни одна ничего не знает о репозитории ПОСЛЕ прогона. Ни один пункт Г1–Г6 их не повторяет.

**Три свойства из задания:**
1. **Слот зоны обязателен и краснеет пустым** — гейт **С8** в `check_sborki.py` (по образцу З10). Заполняет его генератор из `--zone`: требовать зону у аналитика второй раз значило бы завести второе место, где про то же можно забыть. С8 при этом не декоративен — краснеет на заходе, написанном или правленном руками, а руками их писали тринадцать раз подряд.
2. **Раздел не удаляется исполнителем** — С8 краснеет и на отсутствии раздела целиком, не только на пустом слоте.
3. **Г2 не висит мёртвым грузом** — генератор решает по факту `--zone`: зона внутри репозитория → явное «неприменимо, и это проверено при сборке» **с причиной и с командой на случай, когда станет применимо**; зона заходит в соседний репозиторий → живая команда с его именем.

🔴 **Первый живой прогон поймал, что гейт не краснеет НИКОГДА.** `\s*` в регулярке слота включает `\n`: на пустом слоте регексп перепрыгивал пустую строку и захватывал следующую («- **Г1…**») как содержимое. Починено на `[ \t]*`; ловушка 4 фикстуры теперь сторожит именно это — не «код написан», а «красное действительно наступает».

**Фикстура `_generator/tools/fixtures/gigiena/PROGNAT.sh` — 9 ловушек**, работает в одноразовом репозитории (генератор пишет на диск и зовёт `register_doc.py` — гонять его по боевому дереву значило бы плодить мусорные заходы при каждом прогоне хука).

### Критерий готовности — по пунктам

1. ✅ `git branch --no-merged arka/mat-kostyak` → 10 (9 чужих + моя); по каждой из девяти `rev-list --count HEAD..<b>` → **0**, вердикт «пуста». `svedenie-arki` среди несведённых нет — она предок HEAD.
2. ✅ `check_tool_contract.py` по 51 файлу → **rc=1, красных 3**, каждый назван с решением (`## ВОПРОСЫ` п.4). `gejt_verstki.py` — **зелёный** (rc=0).
3. ✅ **Живой прогон на реальном объекте.** Свежесобранный заход-проба несёт раздел (`grep -c '^## 4.1 🔴 ГИГИЕНА'` → **1**). `python3 _generator/tools/check_sborki.py <проба>` → **rc=0**; тот же файл с опустошённым слотом → **rc=1**, `❌ С8: слот «ЗОНА ГИГИЕНЫ» ПУСТ`. *Форма прогона — не буквальная: см. оговорку ниже.*
4. ✅ Регресс: `find _generator -name PROGNAT.sh | wc -l` → **24**, зелёных **24 из 24**.
5. ✅ Карта: обе ошибки исправлены командой, класс «инструмент есть, вызова нет» присутствует (§1а).
6. ✅ Оба репозитория: `disciplina` чист; зона в `materials` — ✅ по всем трём путям; `git show --stat` — только свои пути.

🔴 **Оговорка к пункту 3, названная в ПЛАНЕ до работы.** «`bootstrap_zahod.py` в `/tmp`» буквально неисполнимо: генератор отказывает на папке арки вне репозитория, всегда пишет в `REPO_ROOT` и зовёт `register_doc.py`, который допишет строку в живой `KARTA.md`. Исполнил суть — живой прогон настоящего инструмента, а не имитации — на **клоне реального дерева** (`git clone --local`), где ветка и `_INFRA-git/` настоящие. Именно поэтому пункт 3 даёт `rc=0`, а не «зелёный, если закрыть глаза на четыре чужих красных». Дерево снято после замера.

### Верификатор

**1. Форма на трёх реальных заходах арки — шума нет ни на одном:**

| заход | Г2 | Г1 на его зоне | вердикт |
|---|---|---|---|
| `kod_svedenie-arki` | **ЖИВОЙ**, `disciplina` | ✅ | сигнал: зона реально заходит во второй репозиторий, тот самый, что забывали трижды за сутки |
| `kod_verstka-reshaetsya-v-faze-1` | **ЖИВОЙ**, `disciplina` | **красный** на `gejt_verstki.py` | сигнал: ровно то состояние, ради которого Г1 и стоит перед сдачей |
| `kod_sborka-l2-deka` | неприменим, **с причиной** | ✅ | не шум: пункт объявлен неприменимым явно, а не молча выброшен |

**2. Свежий субагент, читавший ТОЛЬКО собранный заход** — зону назвал дословно, второй репозиторий назвал и сказал, что с ним делать. Требование задания выполнено. **И нашёл три настоящих дефекта, все починены:**
- 🔴 **«ЗОНА ГИГИЕНЫ» была УЖЕ «ЗОНЫ»** — раздел брал `a.zone` без дописанного генератором пути файла-захода, и «доехал ли файл-заход в git» не проверялось ничем. Две зоны в одном файле, одна тихо короче.
- **Г2 не называл команду в неприменимом случае** — «станет применимо» было нечем исполнить.
- **Столкновение нумерации:** `Г1–Г7` есть и у `priyomka.py`, в том же файле ниже. Развёл явной оговоркой в преамбуле.

*(Замечания субагента про пустой `## 2. ЗАДАЧА` и маркеры «НЕ ЗАПОЛНЕНО» в слотах 2–3 — свойство пробного захода, который аналитик не дозаполнял, а не дефект раздела.)*

**3. Ничего не потеряно при слиянии:** `git_zona.py poteri` → «Потерь нет, проверено 31 из 31». Регресс до захода и после — 24 из 24.

**4. Ш4 сверен фактом:** каждая исправленная строка карты подтверждена командой на слитом дереве (`ls`, `grep -c`, прогон `deck.py`), а не чтением документации — это её же собственная болезнь.

### Что НЕ трогал

`_fond/**`, `obzory/**`, `ucheniki/**`, `_generator/build_listok.py` (чужое, 24 пути); `_generator/sborka/**` и `.githooks/**` (вне зоны — включая обе точки вызова, названные в Ш2); `_studio/zhurnal/_TEMPLATE-zahod.md` (вне зоны, ни байта); `teorkat-vvedenie/**` кроме разрешения конфликта слияния; ветки не удалял; свою ветку не вливал; файл-заход не коммитил.

### НЕОБРАТИМОЕ

1. **Разрешение конфликта `teorkat-vvedenie/L2/dist/index.html`** — взята версия ветки `svedenie-arki`, версия `arka/mat-kostyak` (та, что с 44 `MISSING-MATH`) в рабочем дереве перезаписана · восстанавливается: `git show 8b48242:teorkat-vvedenie/L2/dist/index.html`.
2. **Разрешение конфликта `_studio/docs/KARTA.md`** — объединением, ни одна строка не выброшена · восстанавливается: `git show 8b48242:_studio/docs/KARTA.md` и `git show zahod/svedenie-arki:_studio/docs/KARTA.md`.
3. **Одноразовые деревья сняты** (`rm -rf` клона и пробного дерева в скретчпаде) · невосстановимо и не нужно: содержимое — копия репозитория плюс пробный заход, копия которого лежит в артефакте.

Больше необратимого нет: ни одного файла не удалил, не переименовал и не переместил; `git reset`/`checkout` поверх несохранённого не делал.

### ПОВТОРЯЕМОСТЬ находок

**Повторятся на следующем же заходе — это класс НЕМЕДЛЕННОЕ, не пункт очереди:**
- **Бэктики в shell-блоке §4** (`## ВОПРОСЫ` п.2) — бьёт КАЖДОГО исполнителя на последнем ходе, когда цена ошибки максимальна. Чинится в генераторе+шаблоне одним заходом.
- **Фикстура пишет дату в отслеживаемый `DOLG.md`** (п.5) — каждый регресс пачкает зону и роняет Г1 у того, кто регресс прогнал. Гигиена начнёт краснеть на собственном инструментарии, и первым же вечером её объявят шумной.
- **Хук принимает фикстуру за лекцию** (п.3) — сработает на следующей же фикстуре со `slajdy/`; у меня это остановило слияние.

**Не повторятся, законно уходят в очередь:** строка фазы в LIFECYCLE (п.1, разовый долг), три «кривых входа» в `sborka/` (п.4, отдельный заход), кэш формул L2 (п.6, разовое), конфликт `KARTA.md` при приёмке (п.7, разовое предупреждение).

**Время прогона + токены** — на канале `app` НЕПРИМЕНИМО, снимать неоткуда: лог прогона существует только на канале `terminal`, где его кладёт `tee`.
