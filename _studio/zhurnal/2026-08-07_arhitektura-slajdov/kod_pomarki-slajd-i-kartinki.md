# Канал исполнителя — pomarki-slajd-i-kartinki (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — правила записаны дословно в якоре Я1, дома названы, три правки в коде и
> одна в верификаторе. Субагенты не нужны.
> 🔴 **Спецификация — Я1 целиком плюс §2 этого файла.**

КОНТЕКСТ. Фабрика научно-популярных лекций. Ночью впервые собран слайд, прошедший конвейер
насквозь: интервью → раскладка ленты по блокам → сжатие (50 слов из 50) → вёрстка → сборка. Оба
гейта зелёные, верификатор фазы 3 прогнан.

**Владелец посмотрел на результат глазами и назвал тринадцать нарушений.** Ни одно не было записано
нигде — ни в скилле, ни в справочниках, ни в коде.

🔴 **ФИЛЬТР, по которому отобрана работа этого захода, и он же критерий владельца:**
*«зачем мы делали этот первый слайд? Чтобы сделать оставшиеся тринадцать».*
Значит чинится то, что **повторится на каждом из тринадцати оставшихся слайдов**. Белый фон
повторится тринадцать раз и каждый раз это переделка иллюстрации; заглушка на экране — тринадцать
раз; отсутствующий номер — тринадцать раз.

**ЦЕНА, названная владельцем:** на этот один слайд исполнитель потратил **100–200 тысяч токенов** в
цикле «собрал → посмотрел → не то → пересобрал» — и всё равно получил тринадцать замечаний, потому
что правил не существовало. Дословно: *«должны быть все уроки раз навсегда прописаны, чтобы ему не
приходилось это делать. Весь процесс чтобы получался с первого раза».*

ЦЕЛЬ: следующий слайд собирается правильно **с первого раза**, а нарушения ловит команда, а не глаз
владельца.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **Стоп до цели, громко и заранее: ты НЕ пересобираешь слайд и не трогаешь лекцию.** Композицию
слайда `centr-gruppy` прямо сейчас правит владелец в другом окне — это решение фазы 1, не твоё.
Ты пишешь правила и гейты, а проверяешь их на уже собранном артефакте (Я2).

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/pomarki-slajd`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/pomarki-slajd`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add pomarki-slajd --branch zahod/pomarki-slajd
  cd ../materials-wt/pomarki-slajd
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять).** В `materials`: `_generator/skeleton/**` · `_generator/sborka/gejt_kartochki.py` · `_illustracii/**`. Во ВТОРОМ репозитории: `~/Documents/GitHub/disciplina/skills/slajdy/references/{math-render.md, status-bloka.md, plakatnyj-stil.md}`. Всё вне — **READ-ONLY**.
- 🔴🔴 **`teorkat-vvedenie/**` — READ-ONLY ЖЁСТКО.** Там живая лекция L2 и **прямо сейчас в другом окне владелец правит композицию слайда `centr-gruppy`.** Ни байта. Артефакт Я2 только читаешь.
- 🔴 **`_generator/tools/**` — чужая зона:** параллельно идёт заход `zamykanie-reestrov`. Не трогать.
- 🔴 **В `disciplina` НЕ ЗАВОДИ ВЕТКУ И НЕ ДЕЛАЙ `checkout`** — там одна рабочая папка на всех. Коммить на вычекаученную, явными путями, проверить `git show --stat`.
- ⚠ `_illustracii/` лежит в `materials`, а не в `disciplina` — не перепутай репозиторий.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
вливать нечего, проверено при сборке: невлитых веток 0.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/pomarki-slajd`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/skeleton/**` `_generator/sborka/gejt_kartochki.py` `_illustracii/**`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря. Проект сверх них не изучай.**

| # | файл | зачем |
|---|---|---|
| Я1 | 🔴 `_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-2026-08-09.md` **ЦЕЛИКОМ** | **спецификация захода.** Тринадцать правил дословными словами владельца, дома в §6. Формулировки **цитировать, не пересказывать** |
| Я2 | `teorkat-vvedenie/L2/proba-centr-gruppy.html` — **READ-ONLY** | живой артефакт: на нём все нарушения и на нём проверяются гейты |
| Я3 | `_illustracii/DISCIPLINA.md` и `_illustracii/fazy/{DOK.md, ZAHOD.md}` | дом правил про картинки и **место, куда встраивается проверка** |
| Я4 | `teorkat-vvedenie/src/tokens.css` — **READ-ONLY** | 🔴 палитра лекции 1. Владелец: цвета «уже вроде придуманы… хорошо видны на глубоком». Берёшь оттуда, **не выдумываешь** |
| Я5 | `_generator/skeleton/base.css` строки 61–68 | `#lect-zone`/`#lect-progress` (П13) и `.panel{background:var(--card)}` (П12) |
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — четыре хода, порядок по убыванию повторяемости

### А1. 🔴 ТРИ ПРАВКИ В КОДЕ — каждая экономит тринадцать переделок

**(а) П12. Фон иллюстраций — ПРОЗРАЧНЫЙ, и это тянет за собой второе.**
`base.css:68` — `.panel { background: var(--card) }`. Владелец: *«мы убираем белый фон. Придумываем
сразу такие цвета — и они уже вроде придуманы для лекции один, — которые хорошо видны на глубоком.
При этом, как только ты уберёшь белый фон, ты заметишь, что больше пространства есть до границы, ты
сможешь увеличить шрифт.»*
🔴 **Решение аналитика, чтобы не колебаться: фон ПРОЗРАЧНЫЙ всегда.** Белая подложка в SVG не даёт
ничего, кроме привязки к одному цвету фона, и стоит переделки при каждой смене темы.
⚠ **Второе следствие важнее первого и его легко забыть:** освободившееся до границы место
отдаётся **шрифту иллюстрации**. Владелец: *«в целом я бы старался увеличивать шрифт»*.
Цвета — из Я4, не выдумывать.

**(б) П13. Номер слайда в правом нижнем углу.**
Сегодня в скелете есть `#lect-zone`/`#lect-progress` — это **полоса прогресса внизу, а не номер**.
Номера нет. Добавить.
🔴 Долг **Д27 в части номера был помечен закрытым ОШИБОЧНО**: приёмка приняла по имени
`lect-progress`, не проверив, что это другой механизм. Метку вернуть в ЖИВ — если `DOLG.md` вне
твоей зоны, строкой в отчёт.

**(в) Заглушка, доехавшая до зрителя.**
`zagolovok_na_ekrane: заполнить` **рендерится на экран буквально** — «ЗАПОЛНИТЬ» крупным капсом, —
а `gejt_kartochki.py` это поле не проверяет вовсе: его нет в списке обязательных. Карточка зелёная,
слайд «успешно» собран, на экране техническая заглушка. Найдено только потому, что довели до
картинки, а не до зелёного гейта.
Добавить поле в проверку заглушек — тем же механизмом, что уже ловит `заполнить` в других полях.

**КРИТЕРИЙ А1:** на **Я2** — уже собранном артефакте — после правок: фон панели не белый (строкой
из CSS), номер слайда присутствует (строкой из HTML). Карточка с `zagolovok_na_ekrane: заполнить`
даёт **КРАСНОЕ**, с настоящим заголовком — зелёное; обе команды и оба `rc` в отчёт.

### А2. 🔴 ПРОВЕРКИ ПРО КАРТИНКИ — В ВЕРИФИКАТОР ИЛЛЮСТРАЦИЙ, А НЕ В ГЕЙТ СЛАЙДА

**Решение владельца, и оно правильнее первоначального плана аналитика.** Аналитик собирался ставить
гейт на **собранный слайд**. Но белый фон, вопросительные знаки и мелкий шрифт — это дефекты
**создания картинки**; ловить их надо там, где картинка рождается. Иначе к моменту срабатывания у
нас уже одиннадцать нарисованных иллюстраций, которые надо переделывать.

Владелец: *«у нас есть верификатор, который включается, когда создаются иллюстрации, и проверяет их
на осмысленность. Может быть, его и нагрузить этой историей?»*

Нагрузить его тремя проверками из Я1:
- **П9. Вопросительные знаки на иллюстрации — ЗАПРЕЩЕНЫ.** Дословно владельца: *«это запрещено
  просто»*. Текстовые узлы внутри `<svg>`, содержащие `?`;
- **П12. Фон прозрачный**, не белый и не залитый;
- **П10/П11. Подпись поверх стрелки — не нужна; композиция рисуется ДУГОЙ**, а не подписью:
  сквозная стрелка сверху дугой, симметрично снизу дуга с `Z(id) = id`.

**КРИТЕРИЙ А2:** проверки живут в `_illustracii/`, вызываются при создании иллюстрации, и **есть
фикстура-нарушитель на каждую** — красная до правки. Проверка, которая только зеленеет, ничего не
проверяет.

### А3. ПРАВИЛА В ДИСЦИПЛИНУ — цитатами, не пересказом

П1–П8 из Я1 по домам, названным в её §6. **Формулировки владельца цитировать**: он говорил точно,
и точность здесь и есть содержание.

🔴 **Один пункт требует внимания, потому что владелец САМ поправил первую редакцию — П7.**
Правило НЕ «экономить внизу, расширять вверху», а **про природу блоков**: *«гораздо важнее описать
понятие, само утверждение, его смысл — и как устроен пример, что за отображение. Это важнее, чем
завершить доказательство, которое и так уже понятно по модулю примера.»*
⇒ `opredelenie`, `utverzhdenie` и устройство `primer`а **расширяются**; `dokazatelstvo` **сжимается**.
Дом — `status-bloka.md`, где живут регистры по типу блока. **Это меняет алгоритм сжатия фазы 3.**

⚠ **П8 (порядок: объяви, что доказываешь → потом собирай материал) и «определение с утверждением —
ОДИН блок» — решения ФАЗЫ 1**, не фазы 3. Класть их в фазу 3 значит ловить слишком поздно.

### А4. ЧТО НЕ ЗАКРЫТО КОМАНДОЙ — назвать числом

П1, П3, П5–П8 командой не ловятся: это суждения о языке и композиции, их слой — верификатор
фазы 3, не скрипт. **Напиши прямо: из тринадцати правил кодом закрыто N, проверкой картинок M,
текстом дисциплины K, не покрыто ничем L.** Заявить L = 0 нельзя — это будет неправдой.

---

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «правила перенесены», а «13 из 13 в домах, N под
проверкой, L держится только вниманием исполнителя».

## 3. ВЕРИФИКАТОР

🔴 **Ты меняешь правила, по которым будут писаться ВСЕ будущие слайды и рисоваться все картинки.**
Ослабленное правило выглядит как перенесённое.

- **Регресс:** `bash _generator/tools/fixtures/sborka/PROGNAT.sh` — зелёный до и после. Прогони
  **до** первой правки и покажи вывод, иначе не отличить свою поломку от чужой.
- **Фикстура-нарушитель на каждую проверку А2**, красная на непочиненном.
- 🔴 **Свежий субагент сверяет П1–П13 из `POMARKI` с тем, что ты написал**, по одному: список
  «правило → дом → цитата на месте» полон, потерь **0**. **Пересказ вместо цитаты считается
  потерей.**
- 🔴 **Живые деки и `teorkat-vvedenie/**` — READ-ONLY.** Правка фона затронет отрисовку всех
  существующих иллюстраций: **проверь на живом деке, что ничего не почернело и не пропало**, и
  скажи это числом. Почернело — откатывай и в отчёт: у нас это уже было (неопределённая переменная
  = чёрная заливка).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/skeleton/**` `_generator/sborka/gejt_kartochki.py` `_illustracii/**`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/skeleton/**` `_generator/sborka/gejt_kartochki.py` `_illustracii/**`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Разведка (сделана):** Я2 (`proba-centr-gruppy.html`) собран `_generator/sborka/slaid.py`, который читает
`base.css`/`engine.js`/`tokens.css` НАПРЯМУЮ из `_generator/skeleton/` (не из `shablon.html` — тот несёт
свой, параллельный, инлайн-шаблон `build_deck.py`-пайплайна). Значит правка `skeleton/base.css` и
`skeleton/engine.js` долетает до артефактов вида Я2 через `slaid.py`, а `shablon.html` трогать не нужно
(его `{{ENGINE_JS}}` подхватит ту же правку engine.js для своего пайплайна автоматически — один источник).

**Оспариваю деталь критерия А1, действую по исправленному прочтению (Карпатов, §1):** буквально
"на Я2 после правок: фон не белый / номер есть (строкой)" нельзя выполнить, не перезаписав Я2 —
а Я2 READ-ONLY и владелец правит его композицию в другом окне ПРЯМО СЕЙЧАС. Вместо перезаписи живого
файла — пересобираю `teorkat-vvedenie/L2/slajdy/centr-gruppy/slaid.md` тем же `slaid.py`, но с `-o` в
scratch (`/private/tmp/.../scratchpad/`), НЕ в `teorkat-vvedenie/`: источник только читаю (карточка и так
READ-ONLY по контракту), новый файл кладу вне репозитория. Это даёт настоящую (не имитированную)
проверку прогонки через реальный генератор, не касаясь живого артефакта и не рискуя гонкой с
владельцем. Обе строки (фон/номер) беру из СВЕЖЕСОБРАННОГО файла в scratch, а не из старого Я2.

**Порядок:**
1. `git status` зоны (точка отката), прогон `PROGNAT.sh` ДО правок — зелёный, вывод в отчёт.
2. Живой чек-бейзлайн: пересобрать `centr-gruppy` в scratch БЕЗ правок, сверить с живым Я2 (что
   `slaid.py` — действительно тот генератор, что дал Я2, а не смежный путь).
3. А1(а) — `skeleton/base.css`: `.panel{background:var(--card)}` → `background:none`.
4. А1(б) — `skeleton/engine.js`: новый `#lect-number` (создаётся рядом с `lect-zone`/`lect-progress`,
   обновляется в `updateProgress()`), CSS для него — в `skeleton/base.css` рядом с существующими
   правилами `#lect-zone`/`#lect-progress`.
5. А1(в) — `sborka/gejt_kartochki.py`: новая проверка «`zagolovok_na_ekrane` несёт ZAPOLNIT» (поле не
   в `OBYAZATELNYE_POLYA` из `formaty.py` — тот файл ВНЕ зоны, значит проверка добавляется отдельной
   функцией в самом гейте, без правки `formaty.py`). Пустая строка `""` — легальна (уже так у нескольких
   фикстур: слайд без заголовка на экране), красит только буквальное слово ZAPOLNIT.
6. Пересборка `centr-gruppy` в scratch С правками → грепаю обе строки критерия А1 оттуда.
7. А2 — новый скрипт `_illustracii/proverka_pomarok.py` (П9 вопросительные знаки · П12 фон-заливка ·
   П10/П11 равенство композиции текстом вместо дуги), + фикстуры-нарушители и чистая фикстура в
   `_illustracii/fixtures/`, + строка вызова в `_illustracii/fazy/ZAHOD.md §4`.
8. А3 — цитаты П1–П8 (дословно) в `disciplina/skills/slajdy/references/{math-render.md — П1-4,
   status-bloka.md — П7, plakatnyj-stil.md — П5,П6,П8}`. П8 — с явной пометкой «решение фазы 1» и
   вопросом в очередь (аналитик увидит несостыковку: единственные три файла зоны — все фаза-3, у П8
   естественного фаза-1 дома в зоне нет).
9. Свежий субагент сверяет П1–П13 → дом → цитата, потерь 0.
10. Живой деки: рендер сборки в браузере (Browser pane) до/после — скрин, глазами.
11. `PROGNAT.sh` ПОСЛЕ правок — зелёный, вывод в отчёт.
12. Коммит зоны в `materials` (§4) + отдельный коммит в `disciplina` (свои пути, без `git_zona.py`,
    он туда не смотрит).
13. ОТЧЁТ, включая А4 (счёт по 13 правилам), ВОПРОСЫ, УРОКИ.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Найден заход-двойник `_studio/zhurnal/2026-08-07_arhitektura-slajdov/kod_pomarki-v-disciplinu.md`
   в той же арке — покрывает практически те же 13 правил (свой А1/А2/А3, свой Я1 на тот же
   `POMARKI-2026-08-09.md`). Ветки `zahod/pomarki-v-disciplinu` нет, история `disciplina` его не
   касалась — не запускался, конфликта с этим заходом не случилось. Но если его выдадут другому
   исполнителю ПОСЛЕ этого захода — задвоение (правки в `math-render.md`/`status-bloka.md`/
   `plakatnyj-stil.md` уже сделаны здесь). Стоит архивировать один из двух дословно ДО следующей
   выдачи.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. Долг Д27 (часть «номер слайда») был закрыт ошибочно — принят по имени `lect-progress`, хотя это
   другой механизм (полоса прогресса, не номер). Механизм номера теперь есть (`#lect-number`,
   `_generator/skeleton/{base.css,engine.js}`), значит саму часть П13 долга можно закрыть по-новой,
   но пометку «жив» ставить и снимать может только тот, у кого `DOLG.md` в зоне — он вне зоны
   этого захода (не найден по названным Я1–Я5 адресам, не искал вслепую).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. **Урок фабрике: критерий захода буквально требовал перезаписи READ-ONLY артефакта, который
   владелец в это же время правит в другом окне.** А1 в §2 дословно просил проверить строки «после
   правок» **на Я2** — но Я2 нельзя пересобрать, не перезаписав его, а Я2 одновременно READ-ONLY
   и live (стоп-флаг тем же заходом, абзац 3). Разошлось замечено ДО правки (`## ПЛАН`), заменено
   пересборкой полной фикстуры `zhivoj-teorkat` + грепом текущего Я2 «до». Ничего не сломано, но
   критерий, требующий записи в файл, который тот же документ объявляет READ-ONLY, — противоречие
   в самом заходе, а не разночтение исполнителя; в текущем виде смысл различим только руками.
   ЦЕНА: без остановки на этом месте (Карпатов «оспорить ложную предпосылку») правка могла уйти в
   гонку записи с параллельным окном владельца поверх файла, который заход сам называет чужим.
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md

4. **Урок фабрике: непроверенный (untracked) артефакт не появляется в `git worktree add`.**
   `teorkat-vvedenie/L2/proba-centr-gruppy.html` (Я2) физически существовал только в основной папке
   репозитория — worktree заведён из индекса и untracked-файлы с собой не переносит. Относительный
   путь `teorkat-vvedenie/L2/proba-centr-gruppy.html`, взятый из рабочей папки worktree, тихо давал
   «No such file or directory», хотя якорь Я2 назван в заходе абсолютным путём и по нему файл
   читается штатно.
   ЦЕНА: одна потерянная команда на диагностику (не потеря данных — файл цел в основной папке);
   следующий исполнитель, менее насторожённый, рискует прочитать «файла нет» как «владелец его
   удалил» и запаниковать зря, вместо того чтобы вспомнить про untracked+worktree.
   ДОМ: _studio/zhurnal/2026-08-07_arhitektura-slajdov/UROKI-FABRIKE.md

## ОТЧЁТ — (заполняет исполнитель)

### Что сделано и зачем

**А1 — три правки в коде (`_generator/skeleton/`, `_generator/sborka/gejt_kartochki.py`):**
- (а) П12: `base.css` — `.panel{background:var(--card)}` → `background:none`, дословная цитата
  владельца в комментарии. Панель ложится прямо на `--board`, шрифт иллюстраций может расти —
  это следствие тем не менее НЕ код-изменение (нет глобального токена «кегль иллюстрации»), решение
  каждый раз принимает автор конкретной картинки; зафиксировано словом в комментарии, не гейтом.
- (б) П13: новый `#lect-number` (создаёт и обновляет `engine.js`, стиль — `base.css`, правый нижний
  угол); скрывается в `exportFrame()` вместе с `lect-zone`/`lect-progress` (тот же паттерн чистого
  кадра для PNG-экспорта). Долг Д27 в части номера — см. `## ВОПРОСЫ #2`.
- (в) заглушка `zagolovok_na_ekrane: заполнить`: новая проверка `_check_zagolovok_na_ekrane` в
  `gejt_kartochki.py` (поле не входит в `OBYAZATELNYE_POLYA` — тот список в `formaty.py`, вне зоны
  захода, поэтому проверка — отдельная функция, не правка списка). Не красит выход ФАЗЫ 1 (поле
  решается позже, как `tip_verstki`/`liniya`) — это НАЙДЕННЫЙ РЕГРЕСС первой версии правки, пойман
  прогоном `PROGNAT.sh` (ловушка 29), исправлен до сдачи.

**А2 — верификатор иллюстраций:** новый `_illustracii/proverka_pomarok.py` (П9 вопросительные знаки
· П12 заливка фона внутри SVG · П10/П11 равенство композиции текстом вместо дуги — эвристика: `<text>`
несёт одновременно `∘` и `=`). Вызов вписан в `_illustracii/fazy/ZAHOD.md §4` как шаг 2 (между
статическим lint'ом и headless-самопроверкой) — там, где начинается создание иллюстрации, а не в гейт
слайда (решение владельца, дословно в заходе). Фикстуры-нарушители + одна чистая контрольная — в
`_illustracii/fixtures/`. Правила П9–П11 дополнительно процитированы дословно в `_illustracii/
DISCIPLINA.md` (её же POMARKI §6 называет их домом).

**А3 — цитаты П1–П8 в `disciplina/skills/slajdy/references/`:** `math-render.md` (П1–П4, новый раздел
после `## Rules`) · `status-bloka.md §8` (П7, с явной связью с уже существующим §4 «доказательство
сворачивается») · `plakatnyj-stil.md` (П5, П6, П8; П8 — с явной пометкой «решение фазы 1», см. ниже).

**А4 — охват тринадцати правил (POMARKI §3 бьётся на 13 пронумерованных, П14 — отдельная тема
процесса, не в счёте):**

| категория | П-номера | сколько | как |
|---|---|---|---|
| кодом (гейт слайда + верификатор иллюстраций) | П9, П10, П11, П12, П13 | **5** | из них П9/П10/П11 — именно верификатором картинок (`proverka_pomarok.py`, фикстуры красные до правки); П12 — И код-фикс `base.css` (сама панель), И тот же верификатор (заливка внутри SVG); П13 — только код деки, верификатором иллюстраций не покрыт (это не иллюстрация) |
| текстом дисциплины (цитата дословная, БЕЗ счётчика) | П1, П2, П3, П4, П5, П6, П7, П8 | **8** | дом назван, цитата на месте (субагент сверил — 13 из 13 совпадений, 0 потерь, см. ниже); но ни одна не красит автоматически — заход сам называет их слой «верификатор фазы 3» (человек/LLM), который этим заходом не строился |
| не покрыто ничем | — | **0** | — |

**Оспариваю предпосылку §6 POMARKI про П2 и П4.** Текст захода (§4 А4) перечисляет «П1, П3, П5–П8» как
некомандные, тем самым НЕЯВНО причисляя П2 и П4 к командно-проверяемым — но я не построил для них
проверки: это не было явной задачей ни А1, ни А2. П2 (запрет `\leftrightarrow`) правда дёшево ловится
грепом по собранному источнику — отмечено в самой цитате `math-render.md` как готовый следующий шаг,
но не реализовано в этом заходе. Держу П2 и П4 в категории «текст дисциплины», не «код», пока проверка
не написана — иначе цифра 5/8/0 выше была бы неправдой.

**🔴 Честность цифры 8 (текст-дисциплина).** Дом есть, цитата дословная (сверено свежим субагентом),
но БЕЗ фазы-3 верификатора это не гейт, а справочник: нарушится — не покраснеет ничем, кроме второго
взгляда владельца. `KONSTITUCIYA §11` называет такое «надеждой, а не правилом» прямо. Заявляю честно,
не маскирую словом «покрыто».

### Как проверено
- `bash _generator/tools/fixtures/sborka/PROGNAT.sh` — **зелёный ДО** правок (rc=0, «ФИКСТУРЫ
  ЗЕЛЁНЫЕ», лог сохранён) и **зелёный ПОСЛЕ** (rc=0, тот же вывод; единственный diff — время одного
  замера 0.07→0.08с). Найденный по пути регресс (ловушка 29, `--faza 1` из-за новой проверки
  `zagolovok_na_ekrane`) исправлен ДО финального прогона, не после.
- А1(в): временная карточка в scratch (`bootstrap_lekcii.py`, не трогая `PROGNAT.sh`-фикстуры) —
  `gejt_kartochki.py` с `zagolovok_na_ekrane: заполнить` → `rc=1`, замечание поимённо; та же карточка
  с настоящим заголовком → `rc=0`; та же карточка с заглушкой, но `--faza 1` → `rc=0` (поле фазы 1 не
  касается). Все три команды прогнаны, оба направления показаны.
- А1(а)/(б) на **Я2**: буквально «на Я2 после правок» невыполнимо без перезаписи READ-ONLY-файла,
  который владелец правит в другом окне ПРЯМО СЕЙЧАС (карточка `centr-gruppy/slaid.md` стоит с
  `tip_verstki: заполнить` — пересборка физически падает, не только запрещена правилом). Оспорено в
  `## ПЛАН` до правки, см. `## ВОПРОСЫ #3`. Вместо этого: «до» — грепом текущего Я2 (`grep '.panel {'`
  → `background: var(--card)`; `grep -c lect-number` → `0`); «после» — реальной пересборкой ПОЛНОЙ,
  завершённой фикстуры `_generator/tools/fixtures/sborka/zhivoj-teorkat/slajdy/s03/slaid.md` тем же
  `slaid.py`, что и Я2, в scratch (не в `teorkat-vvedenie/`): `grep '.panel {'` → `background: none`,
  `grep lect-number` → строки создания элемента на месте. Плюс визуальный прогон через Browser pane
  (скриншот) — доска видна под иллюстрацией без белой панели, номер «1 / 1» в правом нижнем углу,
  консоль браузера чиста (`read_console_messages` без ошибок).
- А2: `python3 _illustracii/proverka_pomarok.py _illustracii/fixtures/*.svg` — три нарушителя красные
  поимённо (`П9`, `П12`, `П10/П11`), контрольная чистая фикстура зелёная; прогнано и по отдельности, и
  скопом.
- А3: свежий субагент (без контекста этой сессии) сверил П1–П13 → дом → цитата по первоисточнику
  `POMARKI-2026-08-09.md`. Итог: **13 из 13 дословных совпадений, 0 искажений, 0 не найдено**; две
  мелкие косметические правки (пропущенный маркер 🔴 в заголовках П9/П11 `DISCIPLINA.md`) внесены по
  результату сверки.

### Что НЕ трогал
`teorkat-vvedenie/**` — ни байта (READ-ONLY, композиция `centr-gruppy` правится владельцем live).
`_generator/tools/**` — не трогал (чужая зона, `zamykanie-reestrov`). `formaty.py` — не трогал
(`OBYAZATELNYE_POLYA` там, но файл вне зоны; заглушка `zagolovok_na_ekrane` поэтому реализована
отдельной функцией в `gejt_kartochki.py`, не правкой списка). `DOLG.md` — не нашёл в названных
якорях, не искал вслепую, вопрос в очередь. Слайд не пересобирал, лекцию не правил (стоп-флаг захода).

### Необратимое
Необратимого нет. Все правки — в зоне, под git, отменяются `git revert`. Временный `http.server`
(порт 8934, scratch-каталог) остановлен по завершении проверки.

### Результат верификатора
`PROGNAT.sh` зелёный до и после (см. выше) · фикстуры А2 красные/зелёные как задумано · субагент-сверка
П1–П13: 13/13, 0 потерь · живой рендер (Browser pane) на полной фикстуре — без почернений и пропаж,
консоль чиста. `render.py`/Playwright локально не проверялись (доступности не проверял, PROGNAT.sh сам
пропустил одну ловушку по той же причине «нет playwright» — заменил Browser pane, что дало то же
доказательство глазами).

### Открытое «возвращаться»
См. `## ВОПРОСЫ` — заход-двойник (#1), долг Д27 (#2), оба урока фабрике (#3, #4). Отдельно: 8 правил
из категории «текст дисциплины» не имеют счётчика (см. честность цифры выше) — следующий естественный
шаг фабрики: верификатор-LLM фазы 3, который заход сам называет («Я1 §6»), но не строит.

**АРТЕФАКТ:** артефакта нет: заход кодовый (три правки + верификатор + документация), собранного
HTML/PDF/картинки не производит. Проверочные пересборки — временные файлы в scratch
(`/private/tmp/claude-501/.../scratchpad/after-s03.html` и соседние), не для владельца, не сохраняю
как результат.
**КОММИТ:** `materials` — `22bd8ebf11d1f78598dbbabe81b23a5e58f3883f` — «pomarki-slajd: три правки кода
+ верификатор иллюстраций по помаркам владельца 2026-08-09» · `git_zona.py check --zone pomarki-slajd`
→ ✅. `disciplina` (свой репозиторий, без `git_zona.py` — он не смотрит за пределы `materials`,
инструкция захода явно велит коммитить туда напрямую) — `ef54d5420e349935f602f7ac2bfd28b50e136f86` —
«slajdy: помарки владельца П1-П8 дословно — язык формул, регистр блока, разбор centr-gruppy» ·
`git show --stat` подтверждён — только три названных файла.
