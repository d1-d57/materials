---
tab: Очередь — что доводим
status: ЖИВОЙ СПИСОК. Перезаписывается целиком, история не хранится
дата данных: 2026-08-08
---

# ОЧЕРЕДЬ — что мы хотим довести

> **Как этим пользоваться.** Каждый пункт несёт **фазу** (к какому шагу конвейера относится),
> **цену** (что сломается, если не сделать) и, где известно, **какие долги `DOLG.md` он закрывает**.
> Сделанное помечается ✅ и **остаётся на месте** — вычёркивание без следа не даёт увидеть, что
> работа шла.
>
> **Правила ведения — четыре, жёсткие:**
> 1. **Обновляется ТЕМ ЖЕ ХОДОМ, что и разговор.** Обсудили — записал. Пункт, названный в диалоге
>    и сюда не попавший, считается несуществующим.
> 2. 🔴 **Цена едет вместе с пунктом**, строкой прямо здесь, а не ссылкой на номер долга. Иначе при
>    выключении `DOLG.md` цена умрёт, а без цены пункт — «хорошо бы», и не исполняется никогда.
> 3. **У пункта есть фаза.** Не знаешь фазу — значит не понимаешь, что чинишь.
> 4. **Непроверяемый пункт помечается 🟡 и живёт отдельно.** Не запрещён — просто не встаёт в
>    очередь, пока не получит форму, которую можно проверить командой.
>
> **Семь фаз конвейера** (сверено с `SKILL.md`, не по памяти):
> **Ф1** интервью · **Ф2** раскадровка · **Ф3** текст слайдов · **Ф4** вёрстка ·
> **Ф5** иллюстрации · **Ф6** сборка и QA · **Ф7** сцены. Плюс **Ф0** — инфраструктура фабрики
> (git, гейты, документы), которая не фаза, но ломает всё остальное.

---

## 📊 СЧЁТ ДОЛГОВ — теперь точный. **Живых 38 из 68**

Заход `dolgi-po-fazam` закрыл вопрос: у всех 68 записей стоят статус и фаза, чекер даёт `rc=0`.

| статус | число |
|---|---|
| **ЖИВ** | **38** |
| МЁРТВ | 23 |
| УСТАРЕЛ · НЕПРОВЕРЯЕМ · РЕШЕНИЕ ВЛАДЕЛЬЦА | 2 · 2 · 3 |

**По фазам** (долг с двумя фазами считается в обеих): Ф0 — 9 живых · Ф1 — 13 · Ф2 — 10 ·
Ф3 — 8 · Ф4 — 4 · Ф5 — 1 · Ф6 — 6 · Ф7 — 6.

⚠ **О25 сработал, но покрытие оказалось 1 из 68 — и это надо сказать прямо.** Автопрогон команд
из меток технически работает (и поймал Д44 и Д33), но **67 меток не перепроверяемы автоматически**:
их команды либо не в белом списке, либо составные, либо текстовые. Механизм есть, применять его
почти не к чему.
🔴 **Вывод аналитика, чтобы не переоткрывать: доводить О25 до полного покрытия НЕ НАДО.** Это
означало бы переписать 67 команд ради того, чтобы число обновлялось само. Дешевле мерить его
руками на контрольных точках — и тратить силы на закрытие долгов, а не на их пересчёт. Идея была
моя, и измерение её не подтвердило.

---

## 🗺 ПЛАН ЗАКРЫТИЯ — 38 долгов это НЕ 38 заходов

Ревизия разложила живые долги по **12 группам**, где группа = общая зона файлов, то есть один
заход. Девять из двенадцати можно пускать параллельно.

🔴 **Девять долгов из 38 мы закрывать НЕ БУДЕМ до живого прогона** — они не мешают собрать лекцию,
и трогать их сейчас значит тратить вечер на то, что не приближает тест:
группа **L** «гигиена документации» (7 долгов: Д1, Д7, Д8, Д9, Д11, Д13, Д56) ·
**I** «дефект контента конкретной лекции» (1) · **J** «SVG на `s08`» (1, вообще вне скилла
`slajdy` — это домен `illustracii`).

**Остаётся 29 долгов в девяти группах, три волны:**

✅ **Группа D закрыта заходом `gruppa-D` (коммит `0d172c7`), проверено аналитиком своим прогоном:**
`kompozit` переехал из `ОТЛОЖЕННЫЕ` в `REESTR` и **собирается** (`rc=0`, `--liniya-ill:60%`, две
панели иллюстраций); Д28 закрыт **документацией без правки кода** — конфликта физически нет, явный
`kegl_px` и так выигрывает; Д27 — прогресс-бар в `engine.js`/`base.css`; Д52#2 —
`preserveAspectRatio` теперь **порождается**, а не только проверяется в `audit.py`.
⚠ **Д18 закрыт ЧАСТИЧНО: 1 отложенный тип из 3.** `rejka_sajdbar` и `kartochka_centr` остались —
исполнитель назвал это в теле отчёта прямо, но шапка отчёта говорит «4 из 4». Помечать Д18
закрытым нельзя.

**О35. Ф4 · Два отложенных типа вёрстки остались.** `rejka_sajdbar` (узкая боковая рейка с 1–3
иллюстрациями, 9 слайдов «Бюффона») и `kartochka_centr` (центрированная карточка на подложке,
`dandelin` s10/s11). Исполнитель оставил готовое описание геометрии обоих.
**ЦЕНА:** десять живых слайдов двух эталонных деков этими типами свёрстаны; если L2 попросит такой
— слайд не соберётся, ровно как было с композитом.

**О36. Ф0 · 🔴 Ни один прогон не проверял `porcia-1` и `gruppa-D` ВМЕСТЕ.**
Ветка `gruppa-D` отпочковалась до влития `porcia-1`, поэтому на ней ещё старый `slaid.py` — я это
поймал, когда мой же тест композита упал с `IsADirectoryError` на папке слайда. Хендлер `kompozit`
и новый `slaid.py`/`deck.py` друг друга не видели ни разу.
**ЦЕНА:** обе ветки приняты по отдельности, а собирать лекцию будут вместе. Первым ходом после
слияния — **прогнать композитный слайд через `deck.py`**, иначе первый же живой дек это и покажет.

**О37. Ф4 · Ф6 · `shablon.html` не участвует в пайплайне `sborka/` вообще.**
Найдено исполнителем: `deck.py`/`slaid.py` несут свой инлайн-шаблон и берут из скелета только
`base.css`, `engine.js`, `tokens.css`, шрифты и `katex.css`. Правка `shablon.html` была бы работой
в пустоту.
**ЦЕНА:** это второй экземпляр «двух деревьев» (О4), но уже в шаблонах, а не в путях. Пока не
решено, какой пайплайн канон, часть работы всегда будет уходить не туда.

| волна | заходы (параллельно) | долгов | что закрывает |
|---|---|---|---|
| **В1** | **A** карточка проходит свой гейт (Д34, Д35, Д38, Д43, Б2, Б3, Б4) · **B** bootstrap: недостающие поля анкеты (Д19, Д37, Д39, Д41, Д49, Д51) · **D** компилятор и канон-скелет (Д18, Д27, Д28, Д52#2) | **17** | Ф1–Ф4. Здесь же **композит** (Д18) — без него шесть слайдов «Бюффона» не соберутся, и **ручка человека над солвером** (Д28) |
| **В2** | **C** верификаторы фаз под новое дерево (Д14, Д40, Д46, Д47) · **E** список внутри блока не читает тег сцены (Д17) · **H** бюджет слов: гейт и веса (Д2, Д4) · **G** вкус солвера на живых слайдах (Д54) | **8** | Ф2, Ф6, Ф7. В **C** сидит **Д40** — тот самый провод `zakaz.md`, то есть О2 |
| **В3** | **K** пейсинг и оркестрация — текст `SKILL.md` (Д25, Д42, Б5), **строго после C** (общий файл) · **F** `--scene-diff`, 39 ложных отказов (Д5) — **первым ходом разведка**, путь инструмента в метке не назван | **4** | Ф1, Ф2, Ф7 |

**Пересечения зон названы:** C и K пишут в один `SKILL.md` → последовательно. Всё остальное
параллельно. A и G трогают разные файлы, несмотря на близость темы.

**Честная оценка:** девять заходов, три вечера при трёх параллельных. После В1 конвейер собирает
слайд любого типа; после В2 — с настоящими иллюстрациями и проверенным объёмом; В3 — тексты
дисциплины, её можно двигать.

---

## 🔀 ДВЕ РАЗВИЛКИ ВЛАДЕЛЬЦУ — обе от верификатора, обе меняют мои прошлые решения

**Р1. Д57 — моё решение «МЁРТВ» ОТМЕНЕНО верификатором, и правильно отменено.**
Я закрыл его, опираясь на фразу из отчёта `dovodka-solvera` «выбор не меняется даже при `w = 100`».
Независимый верификатор прогнал `progon_baza.py --dyhanie-w W` и нашёл: на сетке `0.2 … 1.6`
действительно стабильно, **но при `w = 100` выбор реально меняется на 4 слайдах из 16**
(`teorkat-vvedenie/s06`, `dandelin/s08`, `dandelin/s09p`, `buffon/sl-condition`). Значит «вес ни на
что не влияет» верно только в узком диапазоне.
**ЦЕНА:** я принял решение по непроверенной цитате из чужого отчёта — второй раз за сессию вписал
число, которого сам не измерял. Метка откачена в `РЕШЕНИЕ ВЛАДЕЛЬЦА`.
**Что решать:** калибровать в реалистичном диапазоне бессмысленно (доказано), значит вопрос не в
калибровке, а в том, нужен ли «дыхание» отдельным уровнем предпочтений вообще. Не блокирует ничего.

**Р2. Д19 — предлагаю УСТАРЕЛ, по той же причине, что Д33.**
Д19 просит «bootstrap, порождающий папки слайдов **по `INTERVYU.md`**». Ровно это решением Р-А
отменено: имена слайдов рождаются в подфазе 5.2 и живут в `slide_order` карточки лекции, а не
парсингом интервью. Д33 на том же факте уже закрыт. Исполнитель Д19 не тронул сам — метка
текстовая, чекер расхождения не видит, — и правильно сделал, что спросил.

---

---

## ⏳ ЗАКРЫТО ЗА СЕССИЮ 2026-08-08 — шесть заходов

| заход | фаза | итог |
|---|---|---|
| ✅ `solver-v3-dyhanie` | Ф4 | отступ стал долей кегля, размен исчез; решаемость 8 отказов из 16 → **1** |
| ✅ `vhod-fazy-1` | Ф1 · Ф0 | цикл фазы 1 разорван; `doctor` перестал врать (гонка TOCTOU); пейсинг — одна величина |
| ✅ `graf-zavisimostej` | Ф1 · Ф2 | граф зависимостей: циклы и нарушения порядка, четыре вида связи |
| ✅ `skelet-sverka` | Ф4 · Ф7 | счёт впервые: классов 27 / вхолостую 1 · переменных 17 / 0 · форм сцен 7 / 1. Дыра одна, починена |
| ✅ `dovodka-solvera` | Ф4 · Ф3 | Δотступа −3.73 → **−2.08**, на базе 40 слайдов лучше, чем на 16. Коридор — **провалился честно** |
| ✅ `reviziya-dolgov` | Ф0 | статус у всех 68 долгов, доказанный командой; постоянный чекер |
| ✅ `svedenie-vetok` | Ф0 | шесть веток влиты, `doctor` 9/9 безопасны, `_studio` чист |

---

## 🧪 ЖИВОЙ ПРОГОН КОНВЕЙЕРА 2026-08-08 — факт, а не список долгов

Аналитик прошёл конвейер на трёх выдуманных слайдах, от пустой папки до собранного дека.

**Работает насквозь, пять шагов подряд:** `bootstrap_lekcii.py <дир>` → каркас · заполнить
`slide_order`, тот же вызов → досыпал 3 папки · `gejt_kartochki.py` → красный на незаполненном,
**зелёный** на заполненном · `slaid.py …/slaid.md -o out.html` → собран · `deck.py <лекция> -o
dek.html` → «слайдов 3, в резерве 0».

⚠ **Чего прогон НЕ проверил:** солвер и гейт вмещения требуют `playwright`, которого в песочнице
нет — оба шага **не выполнялись**. Текст выдуманный, иллюстраций ноль, типов вёрстки три из восьми.

---

## 📦 ПОРЦИЯ 1 — ✅ ПРИНЯТА с одной точечной дочисткой

**Заход `porcia-1` закрыл О27, О28, О29, О30** — проверено аналитиком отдельным прогоном с ветки:
карточка порождается с блоком «что дальше» (машиночитаемый формат `ФАЗА N (имя): что :: команда`,
названы Ф2–Ф6, команды взяты из живого `--help`) · гейт **краснеет, если блок убрать** · `--faza 1`
зелёный там, где полный красный · `slaid.py` принимает и папку, и файл · `deck.py` зовёт солвер и
**отказывается собирать молча** без `playwright` · явный `kegl_px` солвер не перетирает.
Сквозной прогон: **6 шагов из 6**, ручных правок структуры ноль. Ловушек в `PROGNAT.sh` 40 → 53.

✅ **О33 ЗАКРЫТ дочисткой (коммит `9f21a84`).** Был: полный гейт давал ЗЕЛЁНЫЙ на карточке, тело
которой — слово «заполнить»; заглушка в шапке краснела, в теле была невидима. Стало, проверено
аналитиком своим прогоном: тело-заглушка → **КРАСНЫЙ, 2 замечания поимённо** («раздел „Математика —
развёрнуто“ несёт незаполненный блок-заглушку»), `--faza 1` на том же → **ЗЕЛЁНЫЙ**, настоящее тело
→ **оба зелёные**. Ловушек 27 → 32; ловушка 28 переписана, потому что проверяла ставшее неверным
поведение и покраснела первой.

**О34. Ф0 · Репро-команда в заходе была снята не с тех двух команд, что в ней написаны.**
Аналитик вставил в §2 сообщение гейта, которое видел ПОСЛЕ собственной ручной правки, а выдал за
вывод двух названных команд. Живьём они дают другое сообщение.
**ЦЕНА (слова исполнителя):** «≈15 минут на два прогона и чтение двух модулей, прежде чем нашёл
настоящую причину» — охота за фантомным сообщением. **Правило:** репро-снипет снимается живым
прогоном непосредственно перед вставкой в заход, а не по памяти сессии.
⚠ **Второй случай того же, в дочистке:** аналитик написал «ловушек станет 54», посчитав их
шаблоном, который цепляет лишние строки. Правильная команда — `grep -c '^echo "── ловушка'` —
даёт 27 → 32. Исполнитель назвал **то число, что дала команда**, а не то, которое «должно быть», и
явно сказал о расхождении. Это верное поведение; ошибка была на стороне аналитика.
**Правило то же и оно шире:** число в заход вписывается ТОЛЬКО вместе с командой, которая его
печатает (`KONSTITUCIYA §10`). Написал число без команды — написал догадку.

---

## 📦 ПОРЦИЯ 1 — состав (закрыто, оставлено для истории)

**Цель:** одна команда от папки лекции до готового дека с подобранной типографикой, без единого
ручного шага. Без неё «протестировать скилл» невозможно физически: сегодня тест — это пять команд
подряд и надежда не забыть шестую.

**О31. 🔴 ПРИНЦИП ПОРЦИИ, сформулирован владельцем 2026-08-08 — он важнее любого из пунктов ниже.**
*Дословно:* «дисциплина должна быть записана так, что её невозможно игнорировать физически».
Разворачивается в четыре требования, и они относятся ко ВСЕМ фазам, не только к Порции 1:
1. **У каждой фазы чёткий вход, чёткий выход и артефакт.**
2. **Артефакт выхода подключается ко входу следующего шага** — не «по смыслу», а поимённо.
3. **Артефакты порождаются генератором, а потом заполняются.** Генерируемой части — как можно
   больше; руками заводить структуру нельзя.
4. 🔴 **В сам порождаемый артефакт вписывается то, что нельзя забыть** — включая команды следующих
   шагов. Образец уже работает: `bootstrap_zahod.py` вписывает в каждый заход блок КОММИТ, а
   верификатор краснеет на незаполненном поле. Значит забыть коммит физически нельзя.
   **То же самое нужно `slaid.md`:** сверху порождённой карточки — план, что с этим файлом
   произойдёт дальше, на какой фазе и **какой командой**. Требование владельца дословно: чтобы
   файл, попавший к агенту без всякого контекста, сам объяснил, что с ним делать.
**ЦЕНА:** всё, что держится на внимании, забывается. Это единственная причина, по которой солвер
можно не позвать (О28), а карточку — не дозаполнить (О27).

**О27. Ф1 · 🔴 `bootstrap_lekcii.py` порождает карточку, которая не проходит свой же гейт.**
Скрипт создаёт только шапку; `gejt_kartochki.py` требует три раздела тела — «Математика —
развёрнуто», «Текст слайда — сжато», «Правки». Ни одного нет. Дописал руками — гейт позеленел.
**ЦЕНА:** генератор и гейт не согласованы, порождённая структура красна по построению, фаза 1
формально не закрывается никогда.

**О28. Ф4 · Ф6 · 🔴 Солвер НЕ встроен в сборку — дек собирается без подбора типографики.**
`deck.py` собрал дек и ни разу не позвал `vmeshchenie.py`.
**ЦЕНА:** «собрал дек» и «собрал дек, который влезает» — разные вещи, а команда одна и та же.
Забытый подбор обнаруживается только глазами на слайде, за час до лекции. Главный инструмент арки
к конвейеру не подключён.

**О29. Ф0 · Интерфейсы путей не согласованы.** `deck.py` берёт папку лекции, `slaid.py` — путь до
файла `slaid.md`; папка даёт `IsADirectoryError`.
**ЦЕНА:** круг каждому, кто пробует руками первый раз. Аналитику стоила одного.

**О30. Ф1 · Гейт карточки требует поля фазы 2 на выходе фазы 1** (закрывает **Д38**).
На пустых карточках краснеет на `tip_verstki` и `liniya`, которые решает фаза 2. Режима `--faza N`
нет.
**ЦЕНА:** гейт даёт FAIL на корректной работе — тот самый класс, из-за которого гейты отключают.

---

## 📦 ПОРЦИЯ 2 — «содержимое настоящее». Заходы не написаны

**О36. Ф1 · 🔴 `--faza 1` и «гейт выхода фазы 1» — одно имя, обратный смысл. Заход НАПИСАН:**
`kod_format-kartochki-faza-1.md` (собран 2026-08-08 из живого интервью по Л2 теоркат).
Спека (`fazy-1-2-plan.md §3`) требует восемь проверок и говорит, что выход фазы 1 — слайды с
размеченными блоками. Код (`FAZA_POLYA = {1: ()}`, `_razdely_dlya_fazy(1)` пуст, блок «что дальше»
называет Ф2–Ф6 и не называет Ф1) говорит обратное: «карточка ЕЩЁ НЕ ТРОНУТА». Сверх этого живы три
поля: `tip_idei` · пометка центрального блока · `matematika_iz`/`zamer_tempa` (Д51/Д38).
Плюс правка спеки: **`status: rezerv` подтверждён владельцем 2026-08-08** — пометку «не подтверждено»
в `kartochka-slajda.md §4-бис` снять; код резерв уже умеет (`deck.py`).
**ЦЕНА:** `--faza 1` сегодня даёт ЗЕЛЁНЫЙ на свежепорождённой карточке, где нет ни типа идеи, ни
блоков, ни центрального — то есть ни одна из восьми строк гейта фазы 1 не проверяется ничем, а флаг
с правильным именем отвечает «готово». Это дороже отсутствия флага: зелёному верят. Практическая
цена уже наступила — на этом встало живое интервью по Л2 08.08, тринадцать слайдов, записать
разметку блоков некуда.
⚠ **И отдельный факт для владельца: `zahod/porcia-1` помечена здесь ПРИНЯТОЙ, но в `arka/mat-kostyak`
не влита** (`git log --oneline arka/mat-kostyak..zahod/porcia-1` — непусто). Пока не влита, в основной
папке нет ни `--faza`, ни `BODY_TMPL`, ни блока «что дальше», ни поддержки резерва — то есть первая
редакция этого же захода насчитала две несуществующие дыры, пока предпосылку не проверили о ветку.

**О2. Ф5 · 🔴 Провод иллюстраций — соединить то, что уже написано** (закрывает **Д40** в части
`zakaz.md`, **Д14**, **Д21**, **Д24**).
Правило владельца («ни одна картинка не идёт в работу, пока не написано, что это, зачем и что
изображено; верификатор говорит нужна/не нужна; после рисования — понятно ли по ней, что
нарисовано») **уже записано в трёх местах**: `sito-illyustracij.md` (три ступени, пять родовых
вопросов на интервью), `ANKETA.md` G2 (что изображено · сколько объектов · что происходит ·
**чего рисовать НЕ надо** · размер), `zakaz.md` плюс верификатор-субагент. Сломана **проводка**:
`zakaz.md` — обязательный выход фазы 2, и **его не читает ни одна фаза**; верификаторы ни разу не
гонялись на живом материале.
**ЦЕНА:** класс «заказ и приёмка иллюстрации» — **112 записей на пяти фазах, самый тяжёлый
безрычаговый класс картотеки**: 16 «плохая или лишняя картинка», 13 «подписи и буквы на рисунке»,
12 «непонятно, что нарисовано». Оплачено: 34 фигуры → 11, девять слайдов из пятнадцати остались
без иллюстраций, включая все четыре заказанные.

**О3. Ф4 · 🔴 Ручка человека над солвером — без неё солвер необратим** (закрывает **Д28**).
Решение владельца: **в HTML руками не лезем принципиально.** Значит явный `kegl`/`liniya` в шапке
`slaid.md` должен **выигрывать** у автоподбора. Половина есть (`kegl`, `apply_to_card`); кто
побеждает при конфликте — не решено.
**ЦЕНА:** пока не решено, единственный способ поправить брак солвера — тот самый ручной HTML,
которого владелец не хочет и не умеет.

**О5. Ф4 · Композит и два других типа вёрстки** (закрывает **Д18**).
Компилятор покрывает восемь типов из одиннадцати; отложены `kartochka_centr`, `kompozit`,
`rejka_sajdbar` — проверено вызовом `tipy.REESTR`.
**ЦЕНА:** композит частый — у «Бюффона» шесть слайдов, у «Данделена» четыре. Слайд такого вида
новый путь **не соберёт вообще**.

**О6. Ф3 · Ф5 · Оркестрация по слайдам не прописана вовсе** (закрывает **Д25**, **Д26**).
Кто запускает агентов на слайды, как собирает обратно, где верификаторы. Первая генерация всех
иллюстраций — **одним** агентом ради единого стиля, правки потом параллельно.
**ЦЕНА:** это единственное место, где параллельность реально экономит. Параллелить питон
бессмысленно — замерено: 15 слайдов компилируются за 0,009 с.

**О4. Ф4 · Ф6 · Одно дерево, одно имя** (закрывает **Д40**, **Д41**, **Д3**, **Д22**).
Два несовместимых дерева лекции; `tools/bootstrap_lekcia.py` и `sborka/bootstrap_lekcii.py`
различаются одной буквой; фаза 4 описана под рукописную вёрстку, которой больше нет.
**ЦЕНА:** ловушка на автодополнении; у `brief.md` два адреса, у пула иллюстраций — два адреса и
два формата имени.

**О7. Ф1 · Сценарий интервью.**
Как вести разговор, несущий восемь разделов анкеты, пять родовых вопросов по картинкам и четыре
подфазы, — и не превращающийся в допрос. Не написан никем.
**ЦЕНА (слова владельца):** «мы до сих пор не очень понимаем, как именно вести интервью с учётом
огромного количества решений». Пока сценария нет, интервью идёт на импровизации — а это
единственный вход всего конвейера.

**О16. Ф3 · Ф4 · 🔴 Коридор — редизайн нормы. Первая версия измерена и не работает: 1 из 10.**
Причина содержательная: коридор считался при **одной глобальной норме кегля** (медиана 38 px), но
медиана доминирована слайдами без переопределения, а слайды с **тесной зоной** автор делал на
32–36 — человек намеренно ужимал кегль там, где зона короткая. Пол «кегль ≥ медианы» строже
настоящей типографики, и коридор систематически **недооценивает**.
**Что делать:** норма должна быть **условной** — кегль как функция тесноты зоны (регрессия по
`liniya`/геометрии), а не глобальная медиана. Данные есть: 40 слайдов с человеческими значениями.
⚠ Это тот же дефект, что чинили в солвере, этажом выше: там отступ был отдельной ручкой — связали
с кеглем; здесь кегль — глобальная константа, надо связать с зоной.
**ЦЕНА:** без этого фаза 3 не получит числа, и «сожми красиво» останется тем, чем было. Плюс
аналитик продвигал коридор как второй по величине выигрыш — измерение это не подтвердило.

**О17. Ф4 · Солвер отказывает на слайде ВНУТРИ основной массы длин.**
`teorkat-vvedenie/s09`, 853 знака при p95 = 930 — **не выброс**, в отличие от `s02` (954,
перцентиль 97.5). Причина не исследована.
**ЦЕНА:** пока не разобрано, «солвер отказывает только на экстремальных слайдах» — утверждение без
доказательства, а на нём стоит доверие ко всей типографике.

---

## 🔧 ФОНОМ — не мешает порциям, но копится

**О25. Ф0 · 🔴 Чекер долгов проверяет НАЛИЧИЕ метки, а не её ИСТИННОСТЬ — и это чинится почти
даром. Ближайший фоновый заход.**
Форма статуса — `СТАТУС: <вердикт> · <дата> · <команда> → <вывод/rc>`, то есть **у каждой метки уже
записана команда**. Значит чекер можно научить **прогонять их и сравнивать с записанным выводом**.
**ЦЕНА:** сегодня «≈38 живых» — оценка по четырём проверенным из сорока одного. Без автопрогона мы
после каждого сведения будем гадать заново, а метки будут стареть молча — ровно тот дефект,
который эта же ревизия и нашла. Это и есть ответ на вопрос владельца «как мы поймём, сколько
долгов актуально».

**О23. Ф0 · 🔴 Отметка «заход ПРИНЯТ» ставилась против таблицы обещаний, а не проверки долгов.**
`ПЛАН ДОБИВАНИЯ` назначил заходу `solver-v3-dyhanie` четыре долга, но в самом заходе про три из них
не было ни строки. Проверено: все три живы (два перепроверил аналитик лично).
**ЦЕНА:** «принято» разошлось с состоянием кода в день собственной приёмки. Рычаг есть: **заход не
закрывает долг, пока `reviziya_dolgov.py` не покажет по нему МЁРТВ.**

**О24. Ф0 · Д56 — `REZULTATY.md` несёт невалидный столбец «солвер».** Измерен на пустом контенте.
**ЦЕНА:** будет прочитан как факт — тот класс, что уже дал фантомный знаменатель в этой арке.
Нужна врезка: «столбец недействителен, дата данных, чем заменён». Работа аналитика.

**О19. Ф0 · `priyomka.py` — две фикстуры красные на HEAD, чужой долг, бьёт по всем.**
**ЦЕНА:** общий `PROGNAT.sh` красный, любой заход коммитит через `--no-verify`. Тот самый механизм,
на который в прошлом разборе пришлось 27 обходов из 28.

**О26. Ф0 · Пять рабочих папок на диске.** `dovodka-solvera` отказалась уезжать (88 нетслеженных
путей — регенерируемые артефакты сборки, ветка влита, терять нечего) плюс четыре старые.
**ЦЕНА:** `git worktree list` перестал быть читаемым, `doctor` считает их живыми соседями.

**О32. Ф0 · 🔴 `check_sborki.py` краснеет на КАЖДОМ заходе с отдельной рабочей папкой.**
Проверка С3 требует, чтобы путь `../materials-wt/<имя>` существовал на диске — а он по построению
**создаётся исполнителем первым ходом** и снимается после приёмки. Проверено: гейт красный и на
`kod_skelet-sverka.md`, который был написан, прогнан и **принят** без единого замечания.
**ЦЕНА:** гейт даёт FAIL на корректной работе — ровно тот класс, из-за которого гейты перестают
читать. Сегодня он красен на всех worktree-заходах и потому не значит ничего.

**О8. Ф0 · Гейт «время + токены» неисполним при канале `app`.** Формулировка написана под
`terminal`, где `tee` кладёт `jsonl` на диск.
**ЦЕНА:** гейт, объявленный обязательным в каждом заходе, тихо не выполняется.

**О9. Ф0 · Контракт захода не задаёт ветку во втором репозитории.**
**ЦЕНА:** коммит `graf-zavisimostej` лёг на чужую активную ветку. Обошлось, но обошлось.

**О20. Ф0 · Нет процедуры «написал не в ту папку — как откатиться безопасно».**
Контракт `git checkout` запрещает и альтернативы не даёт.
**ЦЕНА:** запрет без альтернативы толкает либо оставить нарушение, либо сделать запрещённое молча.

**О15. Ф6 · 🟡 Номер слайда есть только у одного из двух пайплайнов.** Счётчик и прогресс-бар живут
в `shablon.html` у `build_deck.py`; у `sborka/` их нет. Требует решения владельца: расхождение
осознанное или недоделка.

**О14. Ф3 · Ф4 · 🔴 Гейт на имена классов в контенте.** В теле карточки можно написать `{.имя}`, и
компилятор берёт **любую строку после точки, ничего не проверяя**. Опечатка `{.tlsit}` даст
молчаливый брак при зелёных гейтах.
**ЦЕНА:** это ровно тот класс отказа, ради которого делался `skelet-sverka`, и **счёт его не ловит
по построению** — пространство имён неограничено. Рычаг один: сверять каждое `{.имя}` против
словаря скелета на этапе сборки, по образцу линта на `var(--x)`.

---

## 🎯 ПОРЦИЯ 3 — живой прогон

**О12. Проба на трёх слайдах насквозь.** Микролекция строго по канону, от интервью до `dist/`.
Двадцать минут. Показывает, где рвётся, **не сжигая живой материал**.

**О13. Лекция 2 по теории категорий.** Готова лента на 13 разделов и ~96 минут — дорогой материал.
Прогонять его через непроверенный конвейер значит получить плохой дек и не понять, виноват
конвейер или лента. Инвариант арки: **плохо сделанное хуже несделанного.**

---

## 🟡 ИДЕИ БЕЗ ПРОВЕРЯЕМОЙ ФОРМЫ — не в очереди, пока не получат её

- **Граф зависимостей → в скилл ресёрча.** Идея владельца: аппарат полезен и при исследовании.
- **Разрез слайда на два по границе блоков.** Аппарат готов. 🔴 Условие владельца: **никогда
  молча** — только предъявлением. Пока не сформулировано, что и когда предъявляется, — не заход.

---

## ✅ РЕШЕНО — не переоткрывать

- **Д57 («вес дыхания не откалиброван») → МЁРТВ.** Формально метка верна, 0.6 захардкожен. Но на
  сетке `0.2 … 1.6` и при `w = 100` выбор солвера **побитово не меняется**. Калибровать нечего.
  Долг, который никогда не будет стоить починки, — не долг.
- **🔴 Поле `minuty` переезжает в подфазу 5.2.** Момент остановки 5.2 суммирует `minuty`, а по
  таблице решений оно заполняется в 5.4 — гейт складывает поле, которого нет. Двигать надо **поле,
  а не момент остановки**: остановка существует затем, чтобы резать ДО обрастания блоками.
- **Инкрементальная пересборка — СНЯТА.** 15 слайдов компилируются за 0,009 с. Нужны изолированность
  слайда (есть) и параллельность **агентов** (это О6), а не питона.
- **Остаточные 3,5 px превышения кегля не полируем** — область, где спорят глазами на живой лекции.
- **Граф не расставляет слайды** — показывает циклы и нарушения предъявленного человеком порядка.
- **Коридор объёма выдаётся ДО черновика.** Тип вёрстки решается на интервью и от объёма не зависит.
- **Первое появление слова законно раньше определения** — рабочий приём владельца, пишется
  существующим полем `bez_opredeleniya_namerenno`, отдельного не заводим.
- **Ветки не удаляем** — решение владельца, независимо от вердикта любого гейта.
- **Д31 («`tokens.css` разъехались») — МЁРТВ**, проверено дважды: расхождение файлов есть,
  расхождения поведения нет.
- **Часть классов стилизует сам компилятор** (`GLOBAL_CSS` в `tipy.py`) — архитектура, не дыра;
  четырнадцать классов, которые наивный счёт пометил бы красным, рабочие.
- **Вес «дыхания» ни на что не влияет** — измерено на всей сетке и при `w = 100`.
- **Готовый `fill` непригоден как мера «мельчит и пусто внизу»** — `.zone` растянута гридом.
  Введён `content_fill`; любая будущая мера пустоты должна брать его.
- **Перенос 41 живого долга сюда — заход с верификатором, а не ручная работа.** Цена живёт в
  дословной формулировке; пересказ её убивает. Один раз и целиком, не по кусочку.

---
*Ведёт аналитик. Пункт, обсуждённый в диалоге и сюда не попавший, считается несуществующим —
дописывать тем же ходом, а не потом.*
