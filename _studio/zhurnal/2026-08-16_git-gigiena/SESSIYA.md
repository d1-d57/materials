# СЕССИЯ — дневник арки «2026-08-16_git-gigiena»

> ДЕТАЛЬ + РЕШЕНИЯ + ИСТОРИЯ (ARKA §4). Дистиллят, не свалка; один док; у решения — «почему». С первого хода. Новая запись — снизу, с датой.

## 2026-08-16 — старт
Арка заведена скаффолдом `bootstrap_arka.py` из `_TEMPLATE-arka/`. Концепция и границы — в `NAVIGATOR.md`, ТЗ — в `TZ.md`. Дальше — по `PLAN.md` («⭐ СЕЙЧАС»).

## 2026-08-16 — арка заведена

Преемник закрытой арки `2026-08-10_dizajn-i-metriki`. Причина завести отдельную арку, а не пункт в старой: предмет другой — не дизайн слайдов, а контур доставки работы. Ворота пройдены: задача многосоставная (разбор + починка сторожа + новая фаза приёмки + счётчик долга), на несколько сессий, трогает структуру фабрики и второй репозиторий.

Вход арки: (1) отчёт ночного захода `kod_faza-priyomki.md`, запущенного 2026-08-16 и работающего на момент заведения; (2) 21 урок закрытой арки, у которых нет вердикта.

Замер долга на момент заведения, снят по каждому репозиторию рядом: `materials` — невлитых 3, вне git 8, не вывезено 0; `disciplina` — 0 / 11 / 3; `matema-fest` — 0 / 25 / 0; `matemdigest-map` — 0 / 66 / 17; `digest` — чисто. Порог владельца: пять невлитых веток и больше — красное.

## 2026-08-16 — принят отчёт ночного захода `kod_faza-priyomki.md`

**ПРИНЯТ с одной дочисткой.** Заход исследовательский, вернул три части: разбор контура, починку сторожа и новое место — фазу приёмки.

**Проверено приёмкой независимо, прогонами, а не по отчёту.**
- **Сторож прозрел, и починен правильно:** не `lstrip('*+ ')`, а `git branch --format=%(refname:short)` — убрана сама возможность ошибки, а не залатан симптом. Ловушка `fixtures/nevlitye/` прогнана: 4 из 4, включая отрицательный контроль («старая формула теряет worktree-ветку, новая — нет»).
- **Исполнитель нашёл то, чего не знал аналитик:** тот же дефект уже чинили 14.08 в `git_zona.py vyvezti --zahody` (там из 26 веток пропускалось 24) — «здесь он дожил до 16.08, потому что чинили место, а не класс».
- **Фаза приёмки с зубами:** гейты Г13/Г14/Г15 в `priyomka.py`. Г13 сверяет продублированные заявки С ОЧЕРЕДЬЮ — выдуманный id краснеет, а не проходит. Ловушки 5–12 прогнаны приёмкой: 8 из 8.
- **Стоп при отказе git-операции работает и знает границу:** конфликтное влитие → `rc≠0`, «СТОП», заявка-отказ в очереди; «нечего вливать» → `rc=0`, заявок не прибавилось.
- **Заявка на влитие без `--kak-vlit` отклоняется**, с инструкцией — проходит и текст доезжает в файл; заявка не про влитие проходит без инструкции (отрицательный контроль на месте).
- **Счётчик долга** на живом окружении: пять репозиториев, `rc=1`, два за порогом названы поимённо (`matema-fest` 25 путей вне git, `matemdigest-map` 67).

🔴 **ДОЧИСТКА, найденная приёмкой:** `dolg_repozitoriev.py` падает трейсбеком на одной недоступной папке рядом — воспроизведено, урок записан с ценой. Та же причина роняет ловушку 13 его собственной фикстуры.

**Пять находок исполнителя** в очереди, из них две адресованы владельцу: подключать ли гейт фазы приёмки к `.githooks/pre-commit` (сильнее, но лишний повод для `--no-verify`) и утвердить ли пороги «вне git ≥15 / не вывезено ≥10» — исполнитель честно назвал их СВОИМ предложением из замера, а не словом владельца. Отдельно ценно: `_studio/docs/kak-delat/GIT-disciplina.md` и `RUKOVODSTVO-zahodami.md` оказались РЕДИРЕКТАМИ по 33 и 77 строк, а настоящий канон (652 и 364) лежит в `_meta/disciplina/` — заходы третий раз подряд называют «каноном» не тот файл.

## 2026-08-16 — принят отчёт `kod_dobivka-dolgov.md`

**ПРИНЯТ ЧАСТИЧНО.** Ч2 и Ч3 закрыты полностью; Ч1 (долг) закрыт на треть, и все три остановки законны — исполнитель уперся в реальные препятствия и назвал их, а не обошёл.

**Сделано и проверено приёмкой независимо.**
- **Ч2:** счётчик пережил недоступного соседа — `try/except (PermissionError, OSError)`, пропуск печатается строкой. Ловушка 21 создаёт РЕАЛЬНУЮ папку с `chmod 000`, подтверждает падение голого `.exists()` и переживание инструментом, затем убирает за собой.
- **Ч3:** гейт фазы приёмки подключён в `.githooks/pre-commit` через новый `check_faza_priyomki.py`, который переиспользует Г13–Г15 из `priyomka.py`, а не дублирует их. Судит СТЕЙДЖЕННУЮ версию только `kod_*.md` этого коммита, на остальном молчит. Ловушки 22–24: пустая фаза → rc=1, заполненная → rc=0, коммит без захода → тишина.
- Фикстура целиком: **24/24 зелёных**.
- **`disciplina` закрыта полностью** — 0/0/0, подтверждено счётчиком и независимым верификатором.

🔴 **ЧТО ПРИЁМКА ВЫЯСНИЛА СВЕРХ ОТЧЁТА — и это снимает главный страх владельца.** Он опасался, что «мы будем считать, что всё влито, а что-то потеряно». Проверено фактом: **потерянной работы нет**.
- `matemdigest-map`, 23 «пути вне git» — это не работа, а **застейдженные УДАЛЕНИЯ мусора**: `.DS_Store` и `.fuse_hidden*`, все до одного уже в `.gitignore` (`git check-ignore -v` подтверждает построчно). Содержательного там ноль.
- `materials`, 6 путей — служебное и чужое: `sdelat_handoff.py`, сам файл-заход, журналы учеников `danya`/`maksim`.
- Единственная НАСТОЯЩАЯ невлитая работа — `zahod/vlitie-i-vitrina-kursa`: два коммита, и в них правка плеера по живой обратной связи владельца (ползунок прогресса, таймкод, кнопка крупнее) плюс пересобранные обе копии деки. `poteri` печатает 🔴: пропадёт 5 файлов, включая `_generator/skeleton/{base.css,engine.js}`. **Её вливать обязательно.**
- `zahod/vid-blokov-vnedrenie` — гасить без разбора, подтверждено вторично: merge в истории есть, `poteri` зелёный.

**Три законные остановки исполнителя, каждая — дефект контура, а не его работы:**
1. Влитие `vlitie-i-vitrina-kursa` упёрлось в зону слияния: ветка трогает `_generator/skeleton/`, зона захода их не покрывала. Расширять зону сам он не вправе — правильно остановился.
2. Гашение `vid-blokov-vnedrenie` упёрлось в чужой незакоммиченный файл в её рабочей папке.
3. Три пути в главной папке не закоммичены: у субагента гит-контура нет мандата коммитить содержательную работу, а у захода — прав писать в главную папку. **Структурный пробел: работу, лежащую в главной папке, коммитить некому.**

## 2026-08-16 — принят отчёт `kod_vlit-i-uspokoitsya.md`

**ПРИНЯТ.** Главное сделано: **обе ветки с настоящей работой влиты** (`79d1544`, `06bb657`), правка плеера доехала — приёмка проверила фактом, `grep -c 'video-ctl' _generator/skeleton/base.css` → 3. `materials` вывезен на origin, невывезенных 0. `disciplina` — 0/0/0. Погашено 6 тривиально слитых веток, журналы учеников и `sdelat_handoff.py` закоммичены.

Остаток на 2026-08-16, снят счётчиком:
- `materials` — невлитая 1 (`vid-blokov-vnedrenie`, законный стоп: в её рабочей папке чужой незакоммиченный файл), вне git 2 (оба — файлы-заходы, их коммитит приёмка).
- `matemdigest-map` — 23 пути, вынесены в `DOLG-ISKLYUCHENIA.md` с ТОЧНОЙ причиной вместо прежней неверной.
- `matema-fest` — не тронут, решение владельца.

🔴 **Приёмка проверила блокер `matemdigest-map` сама и уточнила диагноз.** Исполнитель назвал его «баг `git_zona.py` на `.gitignore`-путях» — это верно, но неполно. Точнее: 23 пути УЖЕ стоят в индексе как удаления (`git diff --cached --name-status` печатает `D` по каждому), то есть коммитятся обычным `git commit` без `add`. Спотыкается именно обязательная форма фабрики «`add` по путям, затем `commit -- <пути>`»: `git add` на игнорируемом пути отказывает. Урок записан с ценой и с готовой формулировкой починки: `git_zona.py commit` обязан различать «путь надо ввести в индекс» и «путь уже там».

**Системная находка исполнителя, стоящая отдельно:** стартовая команда велит завести worktree ПЕРВЫМ ходом, а §0.1 требует сначала влить и только потом отпочковываться. Он выполнил стартовую строку буквально, ветка отпочковалась от неслитой основной, и её пришлось пересоздавать. Это противоречие в самом генераторе, не разовая накладка.
