# Канал исполнителя — vlit-i-uspokoitsya (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — четыре git-команды с известным заранее результатом; думать здесь не над чем.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — четыре git-команды, всё расписано; см. шапку захода.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/vlit-i-uspokoitsya.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-16_git-gigiena/kod_vlit-i-uspokoitsya.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add vlit-i-uspokoitsya --branch zahod/vlit-i-uspokoitsya, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-16, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-16_git-gigiena`
Область: «_studio/zhurnal/2026-08-16_git-gigiena» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/beautiful-vigilant-euler/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Заход `kod_dobivka-dolgov.md` закрыл `disciplina` в ноль, починил счётчик долга и подключил гейт фазы приёмки к pre-commit, но упёрся в три препятствия и честно остановился. ЦЕЛЬ: снять остаток. Владелец: «давай всё вольём и на этом успокоимся».

🔴 **ЧТО ПРИЁМКА УЖЕ ВЫЯСНИЛА — не переоткрывай, это стоило времени.**
- **Настоящая невлитая работа ровно одна:** `zahod/vlitie-i-vitrina-kursa`, два коммита — правка плеера по живой обратной связи владельца (ползунок прогресса, таймкод, кнопка крупнее) и пересобранные обе копии деки. `poteri` по ней КРАСНЫЙ: пропадёт 5 файлов, включая `_generator/skeleton/{base.css,engine.js}`. **Прошлый заход не смог её влить только потому, что эти пути были вне его зоны — здесь зона специально широкая.**
- **`zahod/vid-blokov-vnedrenie` — гасить БЕЗ РАЗБОРА.** Проверено дважды: в истории основной стоит `71743c8 Merge branch 'zahod/vid-blokov-vnedrenie'`, `poteri` по ней зелёный («потерь нет, проверено 1 из 1»). Владелец: «просто закрой и выкини, не нужно ещё раз скрупулёзно разбираться».
- **В `matemdigest-map` нет потерь.** Те 23 «пути вне git» — застейдженные УДАЛЕНИЯ мусора: `.DS_Store` и `.fuse_hidden*`, все до одного уже в `.gitignore` (`git check-ignore -v` подтверждает построчно). Их надо просто зафиксировать коммитом.
- **`matema-fest` НЕ ТРОГАТЬ** — владелец отложил на завтра.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-16) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. вливаем всё и на этом успокаиваемся — решение владельца 2026-08-16
2. зона захода СПЕЦИАЛЬНО широкая: прошлый заход не смог влить ветку, потому что она трогала _generator/skeleton вне его зоны
3. vid-blokov-vnedrenie гасится без разбора: merge в истории есть, poteri зелёный — проверено дважды
4. в matemdigest-map лежат не потери, а застейдженные удаления мусора (.DS_Store, .fuse_hidden) — просто зафиксировать

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/vlit-i-uspokoitsya`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/vlit-i-uspokoitsya`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add vlit-i-uspokoitsya --branch zahod/vlit-i-uspokoitsya
  cd ../materials-wt/vlit-i-uspokoitsya
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/` `_studio/` `teorkat-vvedenie/` `sayt/` `ucheniki/` `.githooks/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --vlit zahod/vlitie-i-vitrina-kursa --vlit zahod/dobivka-dolgov --zone _generator/ --zone _studio/ --zone teorkat-vvedenie/ --zone sayt/ --zone ucheniki/ --zone .githooks/ --kommitit 'остатки вне git в materials и удаления мусора в matemdigest-map' --zakryt 'погасить vid-blokov-vnedrenie и все влитые ветки; matema-fest НЕ трогать'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

Что влить, названо аналитиком и уехало в задание субагента (2): `zahod/vlitie-i-vitrina-kursa`, `zahod/dobivka-dolgov`. Влитие идёт в ОСНОВНУЮ ветку, не в твою.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/vlit-i-uspokoitsya`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/` `_studio/` `teorkat-vvedenie/` `sayt/` `ucheniki/` `.githooks/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `_meta/disciplina/GIT-disciplina.md`, разделы `vlit-v-osnovnuyu`, `zakryt-vetku`, `vyvezti`. Больше ничего читать не нужно — вся фактура задачи уже в этом файле.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Четыре шага, все механические. Заход дешёвый по замыслу: не разбирайся, не чини по дороге, не переоткрывай решённое.

1. **Влить `zahod/vlitie-i-vitrina-kursa`** в основную. Зона широкая специально — `_generator/skeleton/` в неё входит. После влития проверь фактом, что правка плеера доехала: `grep -c 'video-ctl' _generator/skeleton/base.css` больше нуля.
2. **Погасить `zahod/vid-blokov-vnedrenie`** без разбора. Пересними `poteri` — зелёный, гаси. Упрёшься в чужой незакоммиченный файл в её рабочей папке (прошлый заход упёрся именно туда) — это законный стоп, опиши и иди дальше, ветку оставь.
3. **Закоммитить остатки:** в `materials` — то, что вне git (служебное и журналы учеников; сам файл-заход НЕ коммить, его коммитит приёмка); в `matemdigest-map` — застейдженные удаления мусора.
4. **Вывезти** оба репозитория на origin.

**Ограничитель:** содержание чужих файлов не правь. Видишь по дороге сломанное — строка в отчёт, дальше по списку.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).**

1. **Счётчиком, не на глаз:** `python3 _generator/tools/dolg_repozitoriev.py` печатает по `materials`, `disciplina`, `matemdigest-map` нули во всех трёх колонках. `matema-fest` остаётся с 25 путями — законно. Вывод целиком в отчёт.
2. **Правка плеера доехала:** `grep -c 'video-ctl' _generator/skeleton/base.css` > 0 и `git branch --no-merged` не печатает `vlitie-i-vitrina-kursa`.
3. **Маркеров конфликта ноль** — грепом по влитым путям, вывод приложить.
4. **Дека собирается:** `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o /tmp/L2-proverka.html` → rc=0, слайдов 30.

🔴 **ПРОВЕРЯЕТСЯ ЖИВЫМ ПРОГОНОМ НА РЕАЛЬНЫХ РЕПОЗИТОРИЯХ**, не на фикстуре.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «долгов нет», а «долгов нет, проверено 3 репозитория из 4, один вынесен по решению владельца».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Верификатор НЕ нужен** — влитие проверяется клаузами 2–4 фактом на диске, а не сверкой смыслов. Прошлый заход прогнал ПОСЛЕ-верификатора по тому же остатку, список потерь пуст.
🔴 Одно исключение: если при влитии придётся разрешать конфликт — останови и опиши. Разрешение конфликта по своему вкусу в этом заходе запрещено: цена ошибки — потерянная правка владельца.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/ _studio/ teorkat-vvedenie/ sayt/ ucheniki/ .githooks/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/ _studio/ teorkat-vvedenie/ sayt/ ucheniki/ .githooks/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/` `_studio/` `teorkat-vvedenie/` `sayt/` `ucheniki/` `.githooks/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/` → ✅; `python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/` → ✅; `python3 _generator/tools/git_zona.py check --zone sayt/` → ✅; `python3 _generator/tools/git_zona.py check --zone ucheniki/` → ✅; `python3 _generator/tools/git_zona.py check --zone .githooks/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Оговорка о порядке (до всяких действий).** Стартовое сообщение владельца велело первым ходом завести рабочую папку (`git_zona.py worktree add`) — я так и сделал, ДО чтения самого захода. Прочитав §0.1, вижу: это ровно тот порядок, который §0.1 называет ошибочным («заводя ветку ДО влития, отпочковываешь её от устаревшей основной — инструмент не дотаскивается сам»). Стартовое сообщение внутри файла само себя маркирует как «исполнителю здесь делать нечего, твоё задание ниже» — то есть не обязывающая инструкция, а контекст сборки. Действую по фактическому содержанию файла (§0.1), а не по преамбуле: расцениваю уже созданную папку как «уже есть — не пересоздавай» (файл прямо разрешает этот случай), и ПОСЛЕ влития субагентом подтяну свежую основную в свою ветку мержем — это git-контур, законное исключение из «ничего сверх задачи» (§0.1 сама так квалифицирует всю секцию).

**Расхождение в шаблоне, тоже до действий.** §0.1 п.1 говорит «вставь его шесть строк в `## ОТЧЁТ` дословно», а ниже в самом файле есть отдельная вшитая секция `## ГИГИЕНА ВХОДА` с явной пометкой «заполняет СУБАГЕНТ гит-контура, не исполнитель» и своей структурой (СНИМОК ВХОДА / ЧТО СДЕЛАНО / ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ). Печатный текст задания субагенту (`bootstrap_zahod.py --zadanie-subagentu`) не просит явно 4 диагностические команды СНИМКА ВХОДА — это read-only команды, нулевой риск. Решение: кладу шесть строк субагента в `## ГИГИЕНА ВХОДА` (это её специально вшитый дом, а не общий `## ОТЧЁТ`), и прошу субагента первым делом (до пункта 0 его задания) снять и напечатать эти 4 команды — это не расширение его прав, а то же самое чтение, что он и так делает через `doctor`/`zayavki`/`branch --no-merged`, просто до старта и полным списком. Свой собственный `## ОТЧЁТ` (исполнителя) пишу отдельно, ниже.

**Шаги:**
1. Запустить ОДНОГО субагента с полным текстом из `bootstrap_zahod.py --zadanie-subagentu ...` (напечатан выше), плюс просьба снять СНИМОК ВХОДА первым делом. Субагент работает в главной папке `materials`, не в моём worktree.
2. Дождаться его ответа, проверить хвостовую строку «выдано N позиций из M найденных» — без неё не вставлять, перезапросить.
3. Вставить снимок + шесть строк в `## ГИГИЕНА ВХОДА` дословно.
4. Подтянуть в `zahod/vlit-i-uspokoitsya` свежую основную (merge) — компенсация уже случившегося опережающего `worktree add`.
5. Проверить КРИТЕРИЙ ГОТОВНОСТИ фактом (4 пункта) — не исполняю сам влитие/гашение/коммит остатков, это уже сделал субагент.
6. Гигиена Г1–Г6 из §4.1.
7. Если в моей зоне (`_generator/`, `_studio/`, `teorkat-vvedenie/`, `sayt/`, `ucheniki/`, `.githooks/`) нет собственных изменений (я не редактирую контент, только читаю и подтягиваю main) — коммита своей зоны не будет; напишу это прямо в `## ОТЧЁТ`, а не сфабрикую пустой коммит.
8. Заполнить `## ОТЧЁТ`, необратимое, повторяемость.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Стартовое сообщение владельца («## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ», это стандартный блок bootstrap_zahod.py) велит `git_zona.py worktree add` буквально первым ходом, до чтения самого захода — а §0.1 этого же файла требует обратный порядок: сперва субагент вливает в основную, и только потом заводится рабочая папка, иначе она отпочковывается от устаревшей основной. Я выполнил стартовую команду буквально (до чтения §0.1) и получил ровно тот дефект, о котором §0.1 предупреждает текстом: пустая ветка `zahod/vlit-i-uspokoitsya` отпочковалась от main ДО влития. Обошлось без потерь только потому, что ветка была совсем пустая: гит-контур сам закрыл её как тривиально слитую (`zakryt-vetku --vse-zelenye`), и я пересоздал worktree вторым вызовом уже после влития. Если бы в момент опережающего `worktree add` в ветке уже была бы работа — так дёшево бы не отделались. ПОВТОРИТСЯ на каждом следующем заходе с этим же стартовым блоком и своим §0.1 — не пункт очереди, а повод поправить сам текст стартового сообщения (или §0.1) в `bootstrap_zahod.py`, чтобы порядок не противоречил себе.
   ДОМ: _studio/zhurnal/2026-08-16_git-gigiena/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. `_studio/zhurnal/2026-08-16_git-gigiena/kod_dobivka-dolgov.md` (файл-заход ЧУЖОГО захода, `dobivka-dolgov`) остался модифицирован, но некоммичен в `materials`: `git commit` на нём отказал хуком pre-commit — «ЗАКРЫТИЕ БЕЗ МЕТКИ»: коммит объявляет закрытие в его собственном `## ОТЧЁТ`, но `2026-08-16_git-gigiena/UROKI-FABRIKE.md` не получил непустой строки `ВЕРДИКТ:`. Не коммитил и не правил содержание: во-первых, это чужой файл-заход, содержание чужих файлов не трогаю по прямому ограничителю задачи; во-вторых, правило «файл-заход коммитит только приёмка» логично распространяется на любой файл-заход, не только на мой собственный. Не повторится на следующей единице работы — это разовая недоделка конкретно этого захода, законно уходит пунктом очереди.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снята субагентом гит-контура первым ходом, до всякой работы)*
```
$ git --no-optional-locks branch --no-merged arka/mat-kostyak
+ zahod/dobivka-dolgov
+ zahod/vid-blokov-vnedrenie
+ zahod/vlitie-i-vitrina-kursa

$ git --no-optional-locks status --porcelain | wc -l
       9

$ git --no-optional-locks log --oneline @{u}.. | wc -l
       0

$ python3 _generator/tools/git_zona.py zayavki
Открытых заявок: 2
   · 2026-08-16T0354-generator-tools-dolg-repozitoriev-py-5 (obychnaya, pravka-koda)
   · 2026-08-16T0515-kod-odin-repozitorij-users-ivanyakovlev-documents (blokiruet, git-operaciya)
+ 6 заявок в na-zahod/ (не мои, адресованы заходу-разработчику)
```

**ЧТО СДЕЛАНО** *(с хэшами, дословный отчёт субагента гит-контура)*
1. заявок было 2, закрыто 0, застряло 1 (2026-08-16T0515-kod-odin-repozitorij-users-ivanyakovlev-documents — П2 в matemdigest-map заблокирован известным багом `git_zona.py commit` на .gitignore-путях, вне мандата гит-контура); переадресована 1 (2026-08-16T0354-generator-tools-dolg-repozitoriev-py-5 → na-zahod/, содержательное авторство реестра, не git-операция)
2. влито в основную `arka/mat-kostyak` 2 ветки из 2 названных: `zahod/vlitie-i-vitrina-kursa` (79d1544, --vsyo-ravno: рабочая папка чиста и на tip) и `zahod/dobivka-dolgov` (06bb657, --vsyo-ravno: рабочая папка чиста и на tip); проверено фактом — `branch --no-merged arka/mat-kostyak` даёт 0 по обеим
3. хвост Cowork: забрано коммитами 6c1d3d0 (KARTA.md), 29dab42 (SESSIYA.md арки git-гигиена), e6fd580 (INCIDENTY.md, доросший на двух --vsyo-ravno влитиях); осталось вне git 7 путей — НЕ мои (зоны исполнителя): `_generator/tools/sdelat_handoff.py`, `_studio/zhurnal/2026-08-16_git-gigiena/kod_dobivka-dolgov.md`, `_studio/zhurnal/2026-08-16_git-gigiena/kod_vlit-i-uspokoitsya.md`, `ucheniki/danya/PROFIL.md`, `ucheniki/danya/ZHURNAL.md`, `ucheniki/maksim/ZHURNAL.md`, `ucheniki/maksim/dz/dz-09-raskraska-invariant.html`
4. погашено 6 веток (`zakryt-vetku --vse-zelenye`): `faza-priyomki`, `skill-kachestva`, `tipologiya-rychagov`, `vlit-i-uspokoitsya`, `vlitie-i-publikaciya`, `vyvoz-i-ochered`; НЕ погашено 12, все с rc=1 (не форсировано): `vid-blokov-vnedrenie` — 1 незакоммиченный `teorkat-vvedenie/L2/dist/index.html` в её рабочей папке (тот же упор, что у прошлого захода — законный стоп, ветка оставлена); `kadr-uzor-i-vmeshchenie`/`sluzhebnye-slajdy`/`polya-i-uzor` — тот же файл; `dovodka-solvera`/`obratnyj-progon`/`solver-v3-dyhanie` — 27–65 untracked путей; `modeli-arn` — 843 untracked; `dobivka-dolgov`/`dovodka-fazy-intervyu`/`uborka-pered-pauzoj`/`vlitie-i-vitrina-kursa` — надгробие под этим именем занято старой веткой (name collision)
5. необратимого не было — все отказы `zakryt-vetku` были именно защитой от необратимой потери (untracked-файлы вне надгробия), сама команда закрыла только зелёные ветки без риска
6. выдано 21 позиций из 21 найденных

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`
*Список поимённо (непроходимо правами субагента гит-контура — чужие незакоммиченные/untracked рабочие папки и известный баг инструмента, не «сложно»):*
- `vid-blokov-vnedrenie` + 3 родственные ветки — незакоммиченный чужой файл `teorkat-vvedenie/L2/dist/index.html` в рабочей папке; это ровно то препятствие, о котором предупреждал заход («упрёшься — законный стоп, опиши и иди дальше»)
- `dovodka-solvera`, `obratnyj-progon`, `solver-v3-dyhanie`, `modeli-arn` — untracked-файлы в чужих рабочих папках (27–843 путей), решение не в мандате гит-контура
- `dobivka-dolgov`, `dovodka-fazy-intervyu`, `uborka-pered-pauzoj`, `vlitie-i-vitrina-kursa` (как имена рабочих папок) — надгробие занято, коллизия имён
- `matemdigest-map` П2 (заявка 0515) — застейдженные удаления мусора заблокированы известным багом `git_zona.py commit` на .gitignore-путях; застряла, id указан
- `matema-fest` — по решению владельца НЕ трогалась (законное исключение, не долг)

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Задача была целиком git-гигиеническая (§0.1 — законное исключение из «ничего сверх задачи»), поэтому вся содержательная работа выполнена гит-контуром — одним субагентом (главная папка `materials`, права влития/гашения/вывоза) плюс мной как продолжением того же контура, когда субагент явно передал мне остаток: два коммита исполнителя `zahod/vlitie-i-vitrina-kursa` и `zahod/dobivka-dolgov` влиты в `arka/mat-kostyak` (79d1544, 06bb657); хвост Cowork (KARTA.md/SESSIYA.md/INCIDENTY.md) забран (6c1d3d0, 29dab42, e6fd580); 6 тривиально слитых веток погашены; `vid-blokov-vnedrenie` и ещё 11 веток оставлены — законный стоп по чужим незакоммиченным/untracked рабочим папкам или известному багу инструмента (полный список — `## ГИГИЕНА ВХОДА`). Дальше я сам (в главной папке, продолжая git-контур, а не в своём worktree — эти 7 путей физически лежали только в рабочем каталоге main) закоммитил 6 из 7 «остатков вне git» в `materials`: журналы учеников Дани и Максима одним коммитом (a1bade8), `sdelat_handoff.py` вторым коммитом с законным `--no-verify` на чужом долге контракта инструмента (8502b9f, причина записана в INCIDENTY.md), автолог этого обхода третьим коммитом (d01807d). 7-й путь — `kod_dobivka-dolgov.md` (чужой файл-заход) — оставлен некоммиченным, см. `## ВОПРОСЫ`#2. Материалы вывезены на origin (`vyvezti --yes`, 3 коммита). `matemdigest-map` — нечего вывозить (0 локальных коммитов), 23 пути вне git остаются заблокированы известным багом `git_zona.py` (уже заявка 0515, не мой мандат чинить).

Отдельно: я сам буквально выполнил стартовую команду «worktree add первым ходом» ДО чтения §0.1, из-за чего рабочая ветка `zahod/vlit-i-uspokoitsya` отпочковалась от main раньше влития — гит-контур сам закрыл её как тривиально слитую (она была пустой), я пересоздал worktree вторым вызовом уже после влития. Подробности и почему это системная, а не разовая накладка — `## ВОПРОСЫ`#1.

**Как проверил (КРИТЕРИЙ ГОТОВНОСТИ, все 4 пункта прогнаны живьём):**
1. `python3 _generator/tools/dolg_repozitoriev.py` — **ПРОВАЛ буквального «нули во всех трёх колонках»**, но с полным охватом и легитимной причиной. Дословный вывод (финальный прогон, после всех коммитов и вывоза):
```
═══ git-долг по репозиториям рядом с /Users/ivanyakovlev/Documents/GitHub ═══
Пороги: невлитых ≥5 · вне git ≥15 · не вывезено ≥10
репозиторий             невлитых   вне git   не вывезено
disciplina                     0         0             0
matema-fest                    0        25             0  ← вынесен
matemdigest-map                1        23             0  ← вынесен
materials                      1         2             0

── вынесено из счёта (старые долги чужих проектов) ──
   matema-fest: невлитых 0, вне git 25, не вывезено 0
      причина: завершённый сайт фестиваля, правки не ведутся; накопленное вне git — архивное, разбирается отдельным заходом · дата: 2026-08-16
   matemdigest-map: невлитых 1, вне git 23, не вывезено 0
      причина: владелец 2026-08-16 решил закрыть долг полностью (не «чужой проект» — прежняя причина устарела и снята); остаток НЕ закрывается по двум узким причинам — 23 пути вне git блокированы известным багом `git_zona.py` (`add` отказывает на путях под `.gitignore`, заявка `git-zona-py-commit-2026-08` переадресована заходу-разработчику), ветка `main` — несвязанная история старой витрины исходного `digest` до объединения репозиториев (`merge-base` не находит общего предка), сливать нечего · дата: 2026-08-16
   Список: /Users/ivanyakovlev/Documents/GitHub/materials/_studio/docs/kak-delat/DOLG-ISKLYUCHENIA.md

✅ Порогов никто не перешёл — проверено 2 репозиториев из 4 найденных (вынесено 2).
```
`materials` невлитых=1 — это `zahod/vid-blokov-vnedrenie`, законный стоп на чужом untracked-файле (см. `## ГИГИЕНА ВХОДА`); вне git=2 — мой собственный некоммиченный файл-заход `kod_vlit-i-uspokoitsya.md` (так и должно быть) плюс чужой `kod_dobivka-dolgov.md` (вопрос #2); не вывезено=0 (после `vyvezti`). `disciplina`=0/0/0. Проверено 2 репозитория из 4 найденных, 2 вынесены инструментом с датированной причиной.
2. `grep -c 'video-ctl' _generator/skeleton/base.css` → `3` (>0) ✅; `git branch --no-merged arka/mat-kostyak | grep -c vlitie-i-vitrina-kursa` → `0` ✅.
3. Маркеры конфликта (`<<<<<<<`/`=======`/`>>>>>>>`) по всем шести влитым путям (`_generator/ _studio/ teorkat-vvedenie/ sayt/ ucheniki/ .githooks/`) → `0` ✅.
4. `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o /tmp/L2-proverka.html` → `rc=0`, «собран дек: слайдов в деке 30, в резерве 0», «✅ гейт вмещения: видимого обреза нет, проверено 30 из 30 слайдов» ✅.

**Гигиена §4.1:** Г1 — `git_zona.py check --zone` по всем шести зонам → ✅ (прогнано из своего worktree). Г2 — неприменимо, вся зона внутри `materials`; `matemdigest-map` и `arka/mat-kostyak` смотрел только на чтение, не редактировал. Г3 — невлитых на входе было 3 (`dobivka-dolgov`, `vid-blokov-vnedrenie`, `vlitie-i-vitrina-kursa`), сейчас 1 (`vid-blokov-vnedrenie`) — не выросло. Г4 — ни одного нового `.py` не заводил (закоммитил уже существующий, изменённый не мной `sdelat_handoff.py`) — неприменимо. Г5 — ни одного нового `.md` не заводил — неприменимо. Г6 — все три моих коммита в главной папке проверены `show --stat`: только заявленные пути, ни одного чужого (см. вывод выше).

**Что НЕ трогал:** содержание `kod_dobivka-dolgov.md` и `sdelat_handoff.py` (чужие правки, только зафиксировал в git то, что уже было на диске); `matema-fest` (владелец отложил); 23 пути `matemdigest-map` (известный баг, вне мандата); 12 неслитых/незакрытых рабочих папок и их содержимое.

**Вопросы:** см. `## ВОПРОСЫ` (2 пункта, оба пунктом очереди).

**Результат верификатора:** не запускал — верификатор не нужен по §3, исключение (разрешение конфликта) не сработало: конфликтов при влитии не было (все влития субагента прошли `--vsyo-ravno` без конфликта).

**Время прогона + токены:** на канале `app` неприменимо, снимать неоткуда.

**ПОВТОРЯЕМОСТЬ находок:** находка #1 (порядок «worktree add до чтения §0.1») ПОВТОРИТСЯ на каждом следующем заходе с тем же стартовым блоком — не пункт очереди по существу, а повод для немедленной правки текста в `bootstrap_zahod.py` (см. `## ВОПРОСЫ`#1, оформлена пунктом очереди технически, но по критерию класса — НЕМЕДЛЕННОЕ). Находка #2 (`kod_dobivka-dolgov.md` без ВЕРДИКТ) НЕ повторится — разовая недоделка конкретно этого захода, законно ждёт в очереди.

**АРТЕФАКТ:** `/tmp/L2-proverka.html` — открывать браузером (собран пунктом 4 критерия готовности; постоянного артефакта эта задача не производит, это проверочная сборка, не результат работы).

**НЕОБРАТИМОЕ:**
- Закрыты (удалены и локально, и на origin) 6 веток: `zahod/faza-priyomki`, `zahod/skill-kachestva`, `zahod/tipologiya-rychagov`, `zahod/vlitie-i-publikaciya`, `zahod/vyvoz-i-ochered`, и первая (пустая) версия `zahod/vlit-i-uspokoitsya` — все проверены `poteri` зелёными ДО закрытия (гит-контуром), содержимое не потеряно: оно уже влито в историю `arka/mat-kostyak` через соответствующие merge-коммиты; восстановление — не веткой, а по хэшу коммита через `git log arka/mat-kostyak`, отдельных копий не требуется.
- Моя собственная рабочая ветка `zahod/vlit-i-uspokoitsya` пересоздана дважды (сначала до влития — пустая, закрыта гит-контуром; затем после влития — текущая). Потерь нет: первая версия не содержала ни одного коммита.
- Обход pre-commit (`--no-verify`) на `_generator/tools/sdelat_handoff.py` — не удаление, но отмечаю по духу правила «необратимое отдельным списком»: причина записана в `INCIDENTY.md` коммитом d01807d, восстановление не требуется (хук можно перезапустить в любой момент).

**КОММИТ:** в своей зоне (`zahod/vlit-i-uspokoitsya`, worktree) — коммита нет: рабочая папка чиста, я не редактировал в ней ни одного файла (задача не потребовала содержательной правки кода). `git_zona.py check --zone` → ✅ по всем шести зонам подтверждает, что зона синхронна с git и пуста. Содержательные git-операции этого захода — три коммита в главной папке на `arka/mat-kostyak`: `a1bade8` (журналы учеников), `8502b9f` (sdelat_handoff.py, чужой долг обойдён с причиной), `d01807d` (автолог обхода); плюс коммиты гит-контура `6c1d3d0`, `29dab42`, `e6fd580`, `79d1544`, `06bb657` — все вывезены на origin.

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 _generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `<принято | доработка | отклонено>` — `<почему именно так, одной фразой: что проверено и чем>`

**ВЕТКА РАБОТЫ:** `zahod/vlit-i-uspokoitsya`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 _generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 _generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 _generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- `<id заявки>` — `<род>` — `<суть одной строкой: влитие / коммит / вывоз / деплой / гашение>`

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
