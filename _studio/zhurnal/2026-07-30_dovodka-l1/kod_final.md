# Канал исполнителя — final (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — половина работы это дизайн и рисование: типографика, палитра доски, четыре перерисованные фигуры. План этой части рождается по ходу, глазами на рендере, и словами его не расписать.

КОНТЕКСТ. Курс «Введение в теорию категорий», лекция 1. Текст живёт «лентой» (`teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md`), из которой конвейер ПОРОЖДАЕТ колоду слайдов. Владелец прочитал колоду пять раз подряд и надиктовал правки — от запятой в формулировке до смены дизайн-системы. Всё сведено ПО СЛАЙДАМ в один файл; он и есть твоё ТЗ.

**ЦЕЛЬ: закрыть свод целиком и выдать лекцию — HTML и PDF, задеплоенные на GitHub Pages.**

Приёмка — по ОТЧЁТУ, без построчной сверки.

⚠ **СТОП ДО ЦЕЛИ, говорю заранее.** Ты дойдёшь до собранных HTML и PDF в папке `teorkat/`. Ты НЕ включаешь GitHub Pages и не проверяешь, что ссылка открылась наружу: настройки репозитория — не твоя зона. Скажешь об этом строкой в отчёте.

🔴 **ЧАСТЬ РАБОТЫ УЖЕ СДЕЛАНА — в своде она помечена ✅. Не делай её заново и не «улучшай».** Свод собран из пяти кругов, и владелец несколько раз отменял сам себя; отмены стоят рядом с претензиями. **Читай пункт целиком до конца, прежде чем исполнять его первую половину.**

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie _studio/zhurnal/2026-07-30_dovodka-l1 _studio/konvejer/09-illustracii _studio/zhurnal/2026-07-30_dovodka-l1/kod_final.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie _studio/zhurnal/2026-07-30_dovodka-l1 _studio/konvejer/09-illustracii _studio/zhurnal/2026-07-30_dovodka-l1/kod_final.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО это, в этом порядке. Проект не изучай, соседние арки не открывай.
  1. 🔴 `_studio/zhurnal/2026-07-30_dovodka-l1/SVOD-po-slajdam.md` — **твоё ТЗ целиком**. Девять сквозных законов и разбор каждого слайда, с отметками ✅ сделано / ⬜ нет.
  2. `_studio/konvejer/09-illustracii/SLOVAR-primitivov.md` — примитивы SVG и **раздел про фон рисунка** (записан 31.07, ключевой для блока 1).
  3. `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` — то, что правишь. `B-yazyk.md` пуст и уходит из конвейера (блок 1). `C-*`, `D-*`, `E-*` — READ-ONLY, это следующая лекция.
  4. `_generator/SLIDE-FORMAT.md` — формат текста слайда: сцены `{@2}`, инлайн `{@2| кусок}`, `{.formula}`.
  5. `teorkat-vvedenie/src/base.css`, `tokens.css` и docstring-шапки `src/tools/porodit.py`, `src/tools/sverstat.py` — здесь живут типографика, палитра и генерация раскладки.
  - Хронологический оригинал диктовки (`PRAVKI-2026-07-31-diktovka.md`) открывать **не нужно**: свод его заменяет. Загляни туда только если свод в чём-то противоречив, и скажи об этом в отчёте.
- ПЛАН — в `## ПЛАН` перед действиями.

### 🔴 КОНВЕЙЕР

```
raskadrovka/teksty/*.md → porodit.py → src/content/ + src/illustrations/
                        → sverstat.py → src/slides/ + src/shablon.html + slide_order в src/brief.md
                        → build_deck.py → src/dist/index.html
```

**ПОРОЖДАЕМЫЕ — правишь только через ленту, напрямую НИКОГДА:** `src/content/`, `src/slides/`, `src/illustrations/`, блок `slide_order:` в `src/brief.md`, пер-слайдовый грид в `src/shablon.html`. *Цена 29.07: правки, внесённые прямо в `content/`, снёс первый же прогон конвейера — 16 файлов, ~20 минут при бюджете 20 минут до лекции.*

**Править РАЗРЕШЕНО и НУЖНО:** `src/tools/porodit.py`, `src/tools/sverstat.py`, `src/base.css`, `src/tokens.css` — вся дизайн-система живёт там.

🔴 **`build_deck.py` — ОТДЕЛЬНОЙ командой, не через `&&`.** `sverstat.py` штатно возвращает ненулевой код (печатает расхождения с брифом как отчёт). *Цена 31.07: цепочка через `&&` оборвалась ровно перед сборкой, владелец открыл вчерашнюю колоду и решил, что правка не сработала — круг ушёл на поиск поломки, которой не было.*

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Пять блоков. **После каждого — коммит**, не в конце захода: 31.07 прогон умер на потере связи, и работа четырёх часов висела незакоммиченной. Порядок блоков — от системного к частному: сначала то, что меняет все слайды разом, иначе частные правки придётся переделывать поверх новой системы.

### БЛОК 1 — фон, палитра, полоса (генератор и словарь)

Закрывает законы **З3, З4** свода.

- **Белую панель под иллюстрацией убрать.** Физически: `src/base.css`, правило `.panel { background: var(--card) }`. Рисунок ложится прямо на доску. *«Раз и навсегда никаких белых прямоугольников не вставлять»*.
- **Две правки палитры** — без них на доске плохо, обе названы в `SLOVAR-primitivov.md §Фон рисунка`: `.s-node` уходит с чистого белого на светлый голубоватый; `.s-thin` и `.s-ar-m` темнеют относительно стали.
- **Голубая доска доходит до НИЗА слайда.** По ширине это починено в прошлом заходе, по низу — нет. *«Это просто должно быть везде прописано на уровне генератора»* — правится в `sverstat.py`, не по слайдам.
- **Иллюстрации центрируются и берут освободившуюся ширину** (З5).
- `B-yazyk.md` убрать из `BLOCKS` в `porodit.py` — файл пуст, разделов в нём не осталось. Сам файл НЕ удалять: в нём документировано, куда уехали разделы.

### БЛОК 2 — типографика и цвет

Закрывает законы **З1, З2**. Дизайн-система утверждена владельцем по показанным вариантам, вариант «Б».

- **Заголовок:** крупный Forum поверх бумаги, **золотой** (`--mustard`). Сейчас он 19px при теле 35–40px и приглушён прозрачностью — то есть вдвое мельче текста; это и есть дефект. Точный кегль подбираешь ты: он обязан читаться крупнее тела, но не съесть больше строки текста. Владелец рамку задал так: *«заголовок означает нечто важное, но не обязан занимать много места; может приглушаться, может выделяться нестандартно — но должен выделяться»*.
- **Место под заголовок резервируется на ВСЕХ слайдах**, включая те, где заголовка нет: *«где нет заголовка — на этом месте пустота»*. Слайды не должны прыгать. ⚠ Это **отменяет** прежний выключатель «Итог без заголовка» — теперь заголовок и там.
- **Термины уходят из золота в чёрный жирный.** Золото остаётся заголовку и словам-ярлыкам **«Утверждение»** и **«Упражнение»** (сейчас золотое только второе). Со слов «сопоставляет» и «красиво» золото снять.
- **Проверь бюджет:** заголовок отнимает высоту, на четырёх слайдах запаса нет. Чинится существующим механизмом плотности, новых не заводить.

### БЛОК 3 — тексты и сцены по своду

Все пункты ⬜ из §2–§13 свода. Формулировки владельца ставить **дословно там, где они процитированы**; своей прозы не сочинять (З8).

🔴 **Разделы «Почему это красиво», «Комбинаторные виды», «Естественный изоморфизм» текстом НЕ ТРОГАТЬ** — владелец назвал их хорошими, а по «Естественному изоморфизму» отдельно отменил и претензию, и приписанный ему дефект. Им достаются только сквозные правки блоков 1–2.

Самые тяжёлые два: **«Отмеченные виды»** (§10 свода — переписывается по смыслу, там настоящая математическая ошибка) и **«Категории»** (§12 — переупаковка определения).

### БЛОК 4 — иллюстрации

Законы **З5, З6, З7** плюс частные правки из свода.

- **Перерисовать:** «Примеры» — шесть биекций между трёхэлементными множествами тремя цветами, без букв (§7 свода).
- **Почистить по минимализму:** «Чётные и нечётные» — три тау, штрихованные линии, лишние нарезки · «Покрашенные виды» — вертикальные разделители в нижней части · «Почему это красиво» — пунктирный разделитель между этажами · «Соответствие задач» — два числа 105, двусторонняя стрелка, обводка одной точки **без подписи единицей**.
- **Сплошная проверка палочек** (З6): стрелка между элементами — с палочкой, между множествами — без. Проверить ВСЕ фигуры колоды и назвать в отчёте число проверенных.
- **Сцены по З7:** иллюстрация доказательства не стоит на той же сцене, что формулировка.

### БЛОК 5 — сборка, PDF, деплой

- Конвейер, `check_termin.py`, рендер, **посмотреть все PNG самому**.
- **PDF — постранично ПО СЦЕНАМ**, не по слайдам: *«PDF точно такой же должен быть»*. 🔴 **Число сцен снимай командой, ни в коем случае не из текста.** Живой счёт при сборке захода — **23** при 15 слайдах, и он ВЫРАСТЕТ от твоей же работы: блок 3 добавляет сцены минимум пяти слайдам. Команда:
  ```
  python3 -c "import re,glob;print(sum(int((re.search(r'data-scenes=\"(\d+)\"',open(f).read()) or [0,'1'])[1]) for f in glob.glob('teorkat-vvedenie/src/slides/*.html')))"
  ```
  *Почему красным: аналитик спросил владельца «PDF на 34 страницы?», владелец ответил «делаем 34» — а 34 было выдумано аналитиком, живых сцен было 23. Согласие владельца на выдуманное число не делает число верным.*
- **Папка `teorkat/` в корне репозитория**, внутри `lekciya-1.html` и `lekciya-1.pdf`. Репозиторий уже отдаёт корневую страницу — добавь строку-ссылку в корневой `index.html`.
- **Файл «что не поместилось на слайды»** — рядом с лекцией, markdown. Туда уходит снятое, но не выброшенное: фраза «биекция существует уже потому, что числа равны», счёт $n!$, тождество Фибоначчи (§14 свода).

### Ловушки этого проекта

1. 🔴 **CSS-класс бьёт presentation-атрибут.** `class="s-txt" font-size="17"` рисуется 13px. Другой кегль — только инлайновым `style`.
2. 🔴 **Класс, выдуманный на месте, не краснеет нигде.** `<line>` без своего `stroke` не рисуется ВООБЩЕ, и все гейты зелёные. Берёшь примитив — из словаря; не хватает — чини словарь.
3. **Адрес правки — ЗАГОЛОВОК раздела.** Номера слайдов не употребляй: этот заход меняет и состав, и сцены.
4. **Поля `> поле:mn 🎨` в ленте расходятся с нарисованными фигурами.** Прав рисунок и свод, не поле. Тронул фигуру — приведи поле к ней.

### КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ)

Числа — **команды**, а не значения; в скобках то, что они печатали при сборке захода.

```
python3 teorkat-vvedenie/src/tools/porodit.py  ; echo rc=$?
python3 teorkat-vvedenie/src/tools/sverstat.py ; echo rc=$?
python3 _generator/build_deck.py teorkat-vvedenie/src ; echo rc=$?
python3 _generator/tools/check_termin.py teorkat-vvedenie/src ; echo rc=$?

# 1. белой панели под иллюстрацией не осталось
grep -c "background:\s*var(--card)" teorkat-vvedenie/src/base.css        # сейчас 1 → обязано стать 0
# 2. заголовок крупнее тела
grep -oE "\.zagolovok\s*\{[^}]*font[^;]*" teorkat-vvedenie/src/base.css  # сейчас 19px при теле 35–40px
grep -oE "\-\-t-body:[0-9]+px" teorkat-vvedenie/src/shablon.html | sort -u
# 3. заголовок есть на ВСЕХ содержательных слайдах
grep -l "zagolovok" teorkat-vvedenie/src/slides/s*.html | wc -l          # сейчас 11 → обязано стать 12
# 4. «Утверждение» и «Упражнение» оформлены одинаково
grep -rn "Утверждение\|Упражнение" teorkat-vvedenie/src/content/
# 5. запрещённые слова
grep -rhoiE "носител|арен[аыуе]|голом множеств|починк|равномощн|школа требует" teorkat-vvedenie/src/content/   # ПУСТО
# 6. выдача на месте
ls -la teorkat/lekciya-1.html teorkat/lekciya-1.pdf
python3 -c "import sys;d=open('teorkat/lekciya-1.pdf','rb').read();print('страниц:',d.count(b'/Type /Page')-d.count(b'/Type /Pages'))"
grep -c "teorkat" index.html                                              # ссылка с корневой добавлена
```

**Готово = четыре rc осмыслены (`sverstat.py` штатно ненулевой), шесть проверок сошлись, PDF по числу страниц равен числу сцен, и ты посмотрел PNG всех слайдов глазами.**

**ОХВАТ В САМОМ ВЕРДИКТЕ.** В отчёте — таблица по **всем 12 содержательным слайдам**: «заголовок раздела → что сделано → что осознанно не делал». Вердикт вида «закрыто X пунктов ⬜ из Y», где Y ты посчитал по своду ДО работы. «Всё сделано» без числа не принимается.

## 3. ВЕРИФИКАТОР — обязателен

Заход лоссовый: снимается текст, снимаются детали с четырёх фигур, меняется палитра. Свежий субагент, **другой метод**:

- берёт оригинал из `git show HEAD:teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md`;
- каждую удалённую строку трассирует к пункту свода; не трассируется — потеря, красный вердикт;
- 🔴 **отдельно проверяет три раздела из списка «НЕ ТРОГАТЬ»** — «Почему это красиво», «Комбинаторные виды», «Естественный изоморфизм»: их текст не должен измениться ни на символ, кроме сквозных правок оформления;
- проверяет, что **отменённые владельцем правки НЕ внесены**: третья картинка в «Категориях», подпись единицей у кружка, диаграмма функтора в «Категориях», претензия про «красиво» в «Естественном изоморфизме»;
- считает палочки на стрелках во всех фигурах и печатает распределение, а не вердикт.

🔴 **Мандат чтения верификатора ШИРЕ твоего.** **Опровержение — полноценный успех.** Брак — только выдуманный вердикт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie _studio/zhurnal/2026-07-30_dovodka-l1 _studio/konvejer/09-illustracii _studio/zhurnal/2026-07-30_dovodka-l1/kod_final.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie _studio/zhurnal/2026-07-30_dovodka-l1 _studio/konvejer/09-illustracii _studio/zhurnal/2026-07-30_dovodka-l1/kod_final.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone teorkat-vvedenie \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка** `arka/mat-kostyak` ✅ (`git branch --show-current`, rc=0). Зона в HEAD НЕ чиста (6 путей) ⇒ точка отката коммитом первым ходом.

**Y (пунктов ⬜ в своде, посчитано ДО работы, командой):** `⬜ = 45`, `✅ = 39` по §1–§13. Разбивка: §2 — 9 · §3 — 5 · §4 — 4 · §5 — 2 · §6 — 2 · §7 — 2 · §9 — 6 · §10 — 6 · §11 — 3 · §12 — 4 · §13 — 2. Плюс девять сквозных законов §0 (без отметок) и §15 «Выдача».

**Базовые числа конвейера (сняты командами, до правок):** `porodit` rc=0 · `sverstat` rc=0 (сегодня НЕ ненулевой) · `build_deck` rc=0 · `check_termin` rc=0 · **сцен 23** при 15 секциях (12 содержательных + 3 служебных) · `grep -c background:var(--card) base.css` = 1 · `.zagolovok` = `700 19px/1.05 'Forum'` при теле 35/38/40px · слайдов с заголовком 11 · запрещённых слов 0 · `teorkat/` нет · ссылок «teorkat» в корневом `index.html` — 0.

### Оспоренные предпосылки захода (обе — до работы, обе с доказательством)

1. **«`sverstat.py` штатно возвращает ненулевой код»** — сегодня `rc=0` (снято командой). Правило «`build_deck` отдельной командой» это не отменяет, и я его держу; но критерий «четыре rc осмыслены» не должен ждать ненулевого именно от `sverstat`.
2. **Палитра `.s-node/.s-thin/.s-ar-m` живёт НЕ в названных заходом файлах.** Заход разрешает править `porodit.py`, `sverstat.py`, `base.css`, `tokens.css` — а классы `.s-*` дека определены в `src/tools/sobrat_overlay.py` (строки 175–188), откуда порождается `src/overlay.css` («ПОРОЖДЕНО … руками не править»). Правлю `sobrat_overlay.py` + `tokens.css` (токены) и перегоняю `overlay.css`; файл в зоне, но в заходе не назван.
3. **Критерий №3 («заголовок на ВСЕХ содержательных слайдах», 11 → 12) — верный**, проверил счётом: содержательных слайдов ровно 12, выключатель `> поле:zagolovok net` стоит ровно у «Итога».
4. **Предпосылка поля ленты у «Итога» ложна:** поле утверждает, что «строка-плейсхолдер `🖼 Портрет Андре Жуайаля {1}` осталась в теле раздела» — в теле её НЕТ (`grep 🖼` даёт одну строку, и это сама пометка). Поле приведу к файлу.
5. **`B-yazyk.md` уже не даёт ни одного раздела** (инвентарь: 13 разделов, все из `A-krasivaya.md`) — то есть вынос из `BLOCKS` чинит конвейер, а не колоду.

### Блок 1 — фон, палитра, полоса (законы З3, З4, З5)
- `base.css`: снять `background: var(--card)` у `.panel` (критерий №1 → 0).
- `tokens.css`: два НОВЫХ токена под правки палитры словаря — светлый голубоватый для полого узла и тёмная сталь для служебных линий. Литерального hex в фигурах не появится.
- `sobrat_overlay.py`: `.s-node` → новый токен узла; `.s-thin` и `.s-ar-m` → тёмная сталь. Перегнать `overlay.css`.
- `sverstat.py`, архетип «илл-полосой-снизу»: `.board{grid-area:4/1/5/4}` → `4/1/6/4` (доска забирает и нижнее поле, то есть доходит до низа слайда), вертикальное центрирование панелей считать по полной высоте доски.
- Тот же архетип + «рейка-справа»: паспарту больше нет ⇒ поле панели уменьшить и мерить ширину картинки по ВСЕЙ полосе (`W`), а не по текстовой колонке — это и есть З5 «берут освободившуюся ширину», центрирование сохраняется.
- `porodit.py`: `BLOCKS` без `B-yazyk.md`; сам файл не удалять.

### Блок 2 — типографика и цвет (З1, З2)
- `.zagolovok`: Forum, **золотой** `--mustard`, без глушения прозрачностью, кегль заметно крупнее тела (тело 35–40 ⇒ целюсь в 44px/1.0). `ZAG_H` поднять под новый кегль.
- Место под заголовок резервировать **безусловно** (`r_top += ZAG_H` без `if show_zag`) — «где нет заголовка, там пустота», слайды не прыгают.
- Лента: снять `> поле:zagolovok net` у «Итога» (З1 отменяет выключатель) ⇒ 12 слайдов с заголовком.
- `.acc` (`**термин**`) — из золота в **чёрный жирный**. Этим же ходом золото само уходит со «сопоставляет» и «красиво», и текст двух охраняемых разделов не трогается ни на символ.
- «Упражнение» в `OP_CLS` рядом с «Утверждением»; `.op-utv` → `--mustard`. Оба ярлыка становятся оформлены одинаково и золотом.
- Бюджет: после блока 3 прогнать `promer.py` и добирать существующей плотностью (`PRAVKI`/ступени), новых механизмов не заводить.

### Блок 3 — тексты и сцены (все ⬜ из §2–§13)
🔴 «Почему это красиво», «Комбинаторные виды», «Естественный изоморфизм» — текста НЕ касаюсь.
- **§2 Соответствие задач** — три сцены: (1) «В этих двух комбинаторных задачах одинаковый ответ:» + две задачи списком; (2) «Это следует из того, что существует…»; (3) строка-соответствие + слоган по продиктованной формулировке (без «автоматически», без «это», без «одна из основных идей»). Картинка уезжает на сцену 3 (правка аннотации в поле ленты, её читает код).
- **§3 Комбинаторные тождества** — «Рассмотрим тождества на биномиальные коэффициенты»; вторая сцена под определение; абзац о видах доказательств — в продиктованной короткой форме и на вторую сцену; последний абзац снять (уезжает в конспект); таблицу уменьшить и приглушить.
- **§4 Три биекции** — снять «Разберёмся с тремя примерами»; четыре сцены (тождества → панель 1 → $2^n$ + панель 2 → $3^n$ + панель 3); тождества крупнее. Число сцен объявляется ведущим тегом на строке тождеств.
- **§7 Примеры** — одна сцена уже есть; иллюстрация перерисовывается (блок 4).
- **§9 Чётные и нечётные** — второй абзац в продиктованную форму; картинка на последнюю сцену.
- **§10 Отмеченные виды** — переписывается по новому порядку: вопрос про Паскаля + тождество отдельной строкой → проблема в КАЖДОМ слагаемом правой части («нельзя придумать комбинаторные виды, у которых число элементов равно …, …» — внутри фразы) → дополнительная структура и три определения → изоморфизм ОТМЕЧЕННЫХ видов, «даёт» → «задаёт» → Паскаль через два вида ($k$-подмножества, содержащие отмеченную точку и не содержащие), «точно так же… такой же случай» снять. Несколько сцен, кегль биномиальных уменьшить.
- **§12 Категории** — определение упаковано в продиктованном порядке, морфизмы одной строкой с «из $\mathrm{Ob}\,\mathcal C$», из аксиом снята оговорка; обе картинки снять; нижняя приписка двумя фразами; категория и функтор — разными сценами.
- **§13 Итог** — четыре сцены по четырём пунктам; заголовок выводится.

### Блок 4 — иллюстрации (З5, З6, З7)
- «Примеры» — перерисовать: шесть биекций трёхэлементных множеств, три цвета, без букв. Нужных примитивов (цветной узел) в словаре нет ⇒ **завожу их в словаре И в движке дека тем же ходом**, и пишу в фигуре парой классов `class="s-node s-node-cN"`: в doc-виде (`build_doc.py` вне зоны, править нельзя) сработает базовый `.s-node`, то есть тихой черноты из ловушки 2 не будет ни в одном носителе.
- Чистка по минимализму: «Чётные и нечётные» (лишние τ/φ, пунктирный разделитель) · «Покрашенные виды» (вертикальные разделители в нижней части) · «Почему это красиво» (пунктир между этажами) · «Соответствие задач» (оба 105, двусторонняя стрелка, обводка одной точки без подписи).
- Сплошная проверка палочек по ВСЕМ фигурам колоды, число проверенных — в отчёт.
- Тронул фигуру — привожу к ней поле `🎨` (ловушка 4).

### Блок 5 — сборка, PDF, деплой
- Конвейер (три команды по отдельности), `check_termin`, `kadry.py` — снять КАЖДУЮ сцену и посмотреть все PNG глазами.
- Число сцен снять командой из §2 (не из текста), PDF собрать **постранично по сценам** из тех же кадров.
- `teorkat/lekciya-1.html` (самодостаточный `dist/index.html`) + `teorkat/lekciya-1.pdf` + markdown «что не поместилось на слайды» + строка-ссылка в корневом `index.html`.
- ⚠ `teorkat/` и корневой `index.html` **в списке ЗОНЫ не стоят**, но их прямо заказывает §2 блок 5. Делаю, добавляю их в пути коммита и называю строкой в отчёте.

### Верификатор
Свежий субагент другим методом, после блока 4: трассирует каждую снятую строку `git show HEAD:…A-krasivaya.md` к пункту свода, отдельно проверяет неизменность трёх охраняемых разделов, проверяет что четыре отменённые правки НЕ внесены, печатает распределение палочек.

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone teorkat-vvedenie` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
