# Канал исполнителя — final (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — половина работы это дизайн и рисование: типографика, палитра доски, четыре перерисованные фигуры. План этой части рождается по ходу, глазами на рендере, и словами его не расписать.

КОНТЕКСТ. Курс «Введение в теорию категорий», лекция 1. Текст живёт «лентой» (`teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md`), из которой конвейер ПОРОЖДАЕТ колоду слайдов. Владелец прочитал колоду пять раз подряд и надиктовал правки — от запятой в формулировке до смены дизайн-системы. Всё сведено ПО СЛАЙДАМ в один файл; он и есть твоё ТЗ.

**ЦЕЛЬ: закрыть свод целиком и выдать лекцию — HTML и PDF, задеплоенные на GitHub Pages.**

Приёмка — по ОТЧЁТУ, без построчной сверки.

⚠ **СТОП ДО ЦЕЛИ, говорю заранее.** Ты дойдёшь до собранных HTML и PDF в папке `teorkat/`. Ты НЕ включаешь GitHub Pages и не проверяешь, что ссылка открылась наружу: настройки репозитория — не твоя зона. Скажешь об этом строкой в отчёте.

🔴 **ЧАСТЬ РАБОТЫ УЖЕ СДЕЛАНА — в своде она помечена ✅. Не делай её заново и не «улучшай».** Свод собран из пяти кругов, и владелец несколько раз отменял сам себя; отмены стоят рядом с претензиями. **Читай пункт целиком до конца, прежде чем исполнять его первую половину.**

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie _studio/zhurnal/2026-07-30_dovodka-l1 _studio/konvejer/09-illustracii _studio/zhurnal/2026-07-30_dovodka-l1/kod_final.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie _studio/zhurnal/2026-07-30_dovodka-l1 _studio/konvejer/09-illustracii _studio/zhurnal/2026-07-30_dovodka-l1/kod_final.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО это, в этом порядке. Проект не изучай, соседние арки не открывай.
  1. 🔴 `_studio/zhurnal/2026-07-30_dovodka-l1/SVOD-po-slajdam.md` — **твоё ТЗ целиком**. Девять сквозных законов и разбор каждого слайда, с отметками ✅ сделано / ⬜ нет.
  2. `_studio/konvejer/09-illustracii/SLOVAR-primitivov.md` — примитивы SVG и **раздел про фон рисунка** (записан 31.07, ключевой для блока 1).
  3. `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` — то, что правишь. `B-yazyk.md` пуст и уходит из конвейера (блок 1). `C-*`, `D-*`, `E-*` — READ-ONLY, это следующая лекция.
  4. `_generator/SLIDE-FORMAT.md` — формат текста слайда: сцены `{@2}`, инлайн `{@2| кусок}`, `{.formula}`.
  5. `teorkat-vvedenie/src/base.css`, `tokens.css` и docstring-шапки `src/tools/porodit.py`, `src/tools/sverstat.py` — здесь живут типографика, палитра и генерация раскладки.
  - Хронологический оригинал диктовки (`PRAVKI-2026-07-31-diktovka.md`) открывать **не нужно**: свод его заменяет. Загляни туда только если свод в чём-то противоречив, и скажи об этом в отчёте.
- ПЛАН — в `## ПЛАН` перед действиями.

### 🔴 КОНВЕЙЕР

```
raskadrovka/teksty/*.md → porodit.py → src/content/ + src/illustrations/
                        → sverstat.py → src/slides/ + src/shablon.html + slide_order в src/brief.md
                        → build_deck.py → src/dist/index.html
```

**ПОРОЖДАЕМЫЕ — правишь только через ленту, напрямую НИКОГДА:** `src/content/`, `src/slides/`, `src/illustrations/`, блок `slide_order:` в `src/brief.md`, пер-слайдовый грид в `src/shablon.html`. *Цена 29.07: правки, внесённые прямо в `content/`, снёс первый же прогон конвейера — 16 файлов, ~20 минут при бюджете 20 минут до лекции.*

**Править РАЗРЕШЕНО и НУЖНО:** `src/tools/porodit.py`, `src/tools/sverstat.py`, `src/base.css`, `src/tokens.css` — вся дизайн-система живёт там.

🔴 **`build_deck.py` — ОТДЕЛЬНОЙ командой, не через `&&`.** `sverstat.py` штатно возвращает ненулевой код (печатает расхождения с брифом как отчёт). *Цена 31.07: цепочка через `&&` оборвалась ровно перед сборкой, владелец открыл вчерашнюю колоду и решил, что правка не сработала — круг ушёл на поиск поломки, которой не было.*

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Пять блоков. **После каждого — коммит**, не в конце захода: 31.07 прогон умер на потере связи, и работа четырёх часов висела незакоммиченной. Порядок блоков — от системного к частному: сначала то, что меняет все слайды разом, иначе частные правки придётся переделывать поверх новой системы.

### БЛОК 1 — фон, палитра, полоса (генератор и словарь)

Закрывает законы **З3, З4** свода.

- **Белую панель под иллюстрацией убрать.** Физически: `src/base.css`, правило `.panel { background: var(--card) }`. Рисунок ложится прямо на доску. *«Раз и навсегда никаких белых прямоугольников не вставлять»*.
- **Две правки палитры** — без них на доске плохо, обе названы в `SLOVAR-primitivov.md §Фон рисунка`: `.s-node` уходит с чистого белого на светлый голубоватый; `.s-thin` и `.s-ar-m` темнеют относительно стали.
- **Голубая доска доходит до НИЗА слайда.** По ширине это починено в прошлом заходе, по низу — нет. *«Это просто должно быть везде прописано на уровне генератора»* — правится в `sverstat.py`, не по слайдам.
- **Иллюстрации центрируются и берут освободившуюся ширину** (З5).
- `B-yazyk.md` убрать из `BLOCKS` в `porodit.py` — файл пуст, разделов в нём не осталось. Сам файл НЕ удалять: в нём документировано, куда уехали разделы.

### БЛОК 2 — типографика и цвет

Закрывает законы **З1, З2**. Дизайн-система утверждена владельцем по показанным вариантам, вариант «Б».

- **Заголовок:** крупный Forum поверх бумаги, **золотой** (`--mustard`). Сейчас он 19px при теле 35–40px и приглушён прозрачностью — то есть вдвое мельче текста; это и есть дефект. Точный кегль подбираешь ты: он обязан читаться крупнее тела, но не съесть больше строки текста. Владелец рамку задал так: *«заголовок означает нечто важное, но не обязан занимать много места; может приглушаться, может выделяться нестандартно — но должен выделяться»*.
- **Место под заголовок резервируется на ВСЕХ слайдах**, включая те, где заголовка нет: *«где нет заголовка — на этом месте пустота»*. Слайды не должны прыгать. ⚠ Это **отменяет** прежний выключатель «Итог без заголовка» — теперь заголовок и там.
- **Термины уходят из золота в чёрный жирный.** Золото остаётся заголовку и словам-ярлыкам **«Утверждение»** и **«Упражнение»** (сейчас золотое только второе). Со слов «сопоставляет» и «красиво» золото снять.
- **Проверь бюджет:** заголовок отнимает высоту, на четырёх слайдах запаса нет. Чинится существующим механизмом плотности, новых не заводить.

### БЛОК 3 — тексты и сцены по своду

Все пункты ⬜ из §2–§13 свода. Формулировки владельца ставить **дословно там, где они процитированы**; своей прозы не сочинять (З8).

🔴 **Разделы «Почему это красиво», «Комбинаторные виды», «Естественный изоморфизм» текстом НЕ ТРОГАТЬ** — владелец назвал их хорошими, а по «Естественному изоморфизму» отдельно отменил и претензию, и приписанный ему дефект. Им достаются только сквозные правки блоков 1–2.

Самые тяжёлые два: **«Отмеченные виды»** (§10 свода — переписывается по смыслу, там настоящая математическая ошибка) и **«Категории»** (§12 — переупаковка определения).

### БЛОК 4 — иллюстрации

Законы **З5, З6, З7** плюс частные правки из свода.

- **Перерисовать:** «Примеры» — шесть биекций между трёхэлементными множествами тремя цветами, без букв (§7 свода).
- **Почистить по минимализму:** «Чётные и нечётные» — три тау, штрихованные линии, лишние нарезки · «Покрашенные виды» — вертикальные разделители в нижней части · «Почему это красиво» — пунктирный разделитель между этажами · «Соответствие задач» — два числа 105, двусторонняя стрелка, обводка одной точки **без подписи единицей**.
- **Сплошная проверка палочек** (З6): стрелка между элементами — с палочкой, между множествами — без. Проверить ВСЕ фигуры колоды и назвать в отчёте число проверенных.
- **Сцены по З7:** иллюстрация доказательства не стоит на той же сцене, что формулировка.

### БЛОК 5 — сборка, PDF, деплой

- Конвейер, `check_termin.py`, рендер, **посмотреть все PNG самому**.
- **PDF — постранично ПО СЦЕНАМ**, не по слайдам: *«PDF точно такой же должен быть»*. 🔴 **Число сцен снимай командой, ни в коем случае не из текста.** Живой счёт при сборке захода — **23** при 15 слайдах, и он ВЫРАСТЕТ от твоей же работы: блок 3 добавляет сцены минимум пяти слайдам. Команда:
  ```
  python3 -c "import re,glob;print(sum(int((re.search(r'data-scenes=\"(\d+)\"',open(f).read()) or [0,'1'])[1]) for f in glob.glob('teorkat-vvedenie/src/slides/*.html')))"
  ```
  *Почему красным: аналитик спросил владельца «PDF на 34 страницы?», владелец ответил «делаем 34» — а 34 было выдумано аналитиком, живых сцен было 23. Согласие владельца на выдуманное число не делает число верным.*
- **Папка `teorkat/` в корне репозитория**, внутри `lekciya-1.html` и `lekciya-1.pdf`. Репозиторий уже отдаёт корневую страницу — добавь строку-ссылку в корневой `index.html`.
- **Файл «что не поместилось на слайды»** — рядом с лекцией, markdown. Туда уходит снятое, но не выброшенное: фраза «биекция существует уже потому, что числа равны», счёт $n!$, тождество Фибоначчи (§14 свода).

### Ловушки этого проекта

1. 🔴 **CSS-класс бьёт presentation-атрибут.** `class="s-txt" font-size="17"` рисуется 13px. Другой кегль — только инлайновым `style`.
2. 🔴 **Класс, выдуманный на месте, не краснеет нигде.** `<line>` без своего `stroke` не рисуется ВООБЩЕ, и все гейты зелёные. Берёшь примитив — из словаря; не хватает — чини словарь.
3. **Адрес правки — ЗАГОЛОВОК раздела.** Номера слайдов не употребляй: этот заход меняет и состав, и сцены.
4. **Поля `> поле:mn 🎨` в ленте расходятся с нарисованными фигурами.** Прав рисунок и свод, не поле. Тронул фигуру — приведи поле к ней.

### КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ)

Числа — **команды**, а не значения; в скобках то, что они печатали при сборке захода.

```
python3 teorkat-vvedenie/src/tools/porodit.py  ; echo rc=$?
python3 teorkat-vvedenie/src/tools/sverstat.py ; echo rc=$?
python3 _generator/build_deck.py teorkat-vvedenie/src ; echo rc=$?
python3 _generator/tools/check_termin.py teorkat-vvedenie/src ; echo rc=$?

# 1. белой панели под иллюстрацией не осталось
grep -c "background:\s*var(--card)" teorkat-vvedenie/src/base.css        # сейчас 1 → обязано стать 0
# 2. заголовок крупнее тела
grep -oE "\.zagolovok\s*\{[^}]*font[^;]*" teorkat-vvedenie/src/base.css  # сейчас 19px при теле 35–40px
grep -oE "\-\-t-body:[0-9]+px" teorkat-vvedenie/src/shablon.html | sort -u
# 3. заголовок есть на ВСЕХ содержательных слайдах
grep -l "zagolovok" teorkat-vvedenie/src/slides/s*.html | wc -l          # сейчас 11 → обязано стать 12
# 4. «Утверждение» и «Упражнение» оформлены одинаково
grep -rn "Утверждение\|Упражнение" teorkat-vvedenie/src/content/
# 5. запрещённые слова
grep -rhoiE "носител|арен[аыуе]|голом множеств|починк|равномощн|школа требует" teorkat-vvedenie/src/content/   # ПУСТО
# 6. выдача на месте
ls -la teorkat/lekciya-1.html teorkat/lekciya-1.pdf
python3 -c "import sys;d=open('teorkat/lekciya-1.pdf','rb').read();print('страниц:',d.count(b'/Type /Page')-d.count(b'/Type /Pages'))"
grep -c "teorkat" index.html                                              # ссылка с корневой добавлена
```

**Готово = четыре rc осмыслены (`sverstat.py` штатно ненулевой), шесть проверок сошлись, PDF по числу страниц равен числу сцен, и ты посмотрел PNG всех слайдов глазами.**

**ОХВАТ В САМОМ ВЕРДИКТЕ.** В отчёте — таблица по **всем 12 содержательным слайдам**: «заголовок раздела → что сделано → что осознанно не делал». Вердикт вида «закрыто X пунктов ⬜ из Y», где Y ты посчитал по своду ДО работы. «Всё сделано» без числа не принимается.

## 3. ВЕРИФИКАТОР — обязателен

Заход лоссовый: снимается текст, снимаются детали с четырёх фигур, меняется палитра. Свежий субагент, **другой метод**:

- берёт оригинал из `git show HEAD:teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md`;
- каждую удалённую строку трассирует к пункту свода; не трассируется — потеря, красный вердикт;
- 🔴 **отдельно проверяет три раздела из списка «НЕ ТРОГАТЬ»** — «Почему это красиво», «Комбинаторные виды», «Естественный изоморфизм»: их текст не должен измениться ни на символ, кроме сквозных правок оформления;
- проверяет, что **отменённые владельцем правки НЕ внесены**: третья картинка в «Категориях», подпись единицей у кружка, диаграмма функтора в «Категориях», претензия про «красиво» в «Естественном изоморфизме»;
- считает палочки на стрелках во всех фигурах и печатает распределение, а не вердикт.

🔴 **Мандат чтения верификатора ШИРЕ твоего.** **Опровержение — полноценный успех.** Брак — только выдуманный вердикт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie _studio/zhurnal/2026-07-30_dovodka-l1 _studio/konvejer/09-illustracii _studio/zhurnal/2026-07-30_dovodka-l1/kod_final.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie _studio/zhurnal/2026-07-30_dovodka-l1 _studio/konvejer/09-illustracii _studio/zhurnal/2026-07-30_dovodka-l1/kod_final.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone teorkat-vvedenie \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка** `arka/mat-kostyak` ✅ (`git branch --show-current`, rc=0). Зона в HEAD НЕ чиста (6 путей) ⇒ точка отката коммитом первым ходом.

**Y (пунктов ⬜ в своде, посчитано ДО работы, командой):** `⬜ = 45`, `✅ = 39` по §1–§13. Разбивка: §2 — 9 · §3 — 5 · §4 — 4 · §5 — 2 · §6 — 2 · §7 — 2 · §9 — 6 · §10 — 6 · §11 — 3 · §12 — 4 · §13 — 2. Плюс девять сквозных законов §0 (без отметок) и §15 «Выдача».

**Базовые числа конвейера (сняты командами, до правок):** `porodit` rc=0 · `sverstat` rc=0 (сегодня НЕ ненулевой) · `build_deck` rc=0 · `check_termin` rc=0 · **сцен 23** при 15 секциях (12 содержательных + 3 служебных) · `grep -c background:var(--card) base.css` = 1 · `.zagolovok` = `700 19px/1.05 'Forum'` при теле 35/38/40px · слайдов с заголовком 11 · запрещённых слов 0 · `teorkat/` нет · ссылок «teorkat» в корневом `index.html` — 0.

### Оспоренные предпосылки захода (обе — до работы, обе с доказательством)

1. **«`sverstat.py` штатно возвращает ненулевой код»** — сегодня `rc=0` (снято командой). Правило «`build_deck` отдельной командой» это не отменяет, и я его держу; но критерий «четыре rc осмыслены» не должен ждать ненулевого именно от `sverstat`.
2. **Палитра `.s-node/.s-thin/.s-ar-m` живёт НЕ в названных заходом файлах.** Заход разрешает править `porodit.py`, `sverstat.py`, `base.css`, `tokens.css` — а классы `.s-*` дека определены в `src/tools/sobrat_overlay.py` (строки 175–188), откуда порождается `src/overlay.css` («ПОРОЖДЕНО … руками не править»). Правлю `sobrat_overlay.py` + `tokens.css` (токены) и перегоняю `overlay.css`; файл в зоне, но в заходе не назван.
3. **Критерий №3 («заголовок на ВСЕХ содержательных слайдах», 11 → 12) — верный**, проверил счётом: содержательных слайдов ровно 12, выключатель `> поле:zagolovok net` стоит ровно у «Итога».
4. **Предпосылка поля ленты у «Итога» ложна:** поле утверждает, что «строка-плейсхолдер `🖼 Портрет Андре Жуайаля {1}` осталась в теле раздела» — в теле её НЕТ (`grep 🖼` даёт одну строку, и это сама пометка). Поле приведу к файлу.
5. **`B-yazyk.md` уже не даёт ни одного раздела** (инвентарь: 13 разделов, все из `A-krasivaya.md`) — то есть вынос из `BLOCKS` чинит конвейер, а не колоду.

### Блок 1 — фон, палитра, полоса (законы З3, З4, З5)
- `base.css`: снять `background: var(--card)` у `.panel` (критерий №1 → 0).
- `tokens.css`: два НОВЫХ токена под правки палитры словаря — светлый голубоватый для полого узла и тёмная сталь для служебных линий. Литерального hex в фигурах не появится.
- `sobrat_overlay.py`: `.s-node` → новый токен узла; `.s-thin` и `.s-ar-m` → тёмная сталь. Перегнать `overlay.css`.
- `sverstat.py`, архетип «илл-полосой-снизу»: `.board{grid-area:4/1/5/4}` → `4/1/6/4` (доска забирает и нижнее поле, то есть доходит до низа слайда), вертикальное центрирование панелей считать по полной высоте доски.
- Тот же архетип + «рейка-справа»: паспарту больше нет ⇒ поле панели уменьшить и мерить ширину картинки по ВСЕЙ полосе (`W`), а не по текстовой колонке — это и есть З5 «берут освободившуюся ширину», центрирование сохраняется.
- `porodit.py`: `BLOCKS` без `B-yazyk.md`; сам файл не удалять.

### Блок 2 — типографика и цвет (З1, З2)
- `.zagolovok`: Forum, **золотой** `--mustard`, без глушения прозрачностью, кегль заметно крупнее тела (тело 35–40 ⇒ целюсь в 44px/1.0). `ZAG_H` поднять под новый кегль.
- Место под заголовок резервировать **безусловно** (`r_top += ZAG_H` без `if show_zag`) — «где нет заголовка, там пустота», слайды не прыгают.
- Лента: снять `> поле:zagolovok net` у «Итога» (З1 отменяет выключатель) ⇒ 12 слайдов с заголовком.
- `.acc` (`**термин**`) — из золота в **чёрный жирный**. Этим же ходом золото само уходит со «сопоставляет» и «красиво», и текст двух охраняемых разделов не трогается ни на символ.
- «Упражнение» в `OP_CLS` рядом с «Утверждением»; `.op-utv` → `--mustard`. Оба ярлыка становятся оформлены одинаково и золотом.
- Бюджет: после блока 3 прогнать `promer.py` и добирать существующей плотностью (`PRAVKI`/ступени), новых механизмов не заводить.

### Блок 3 — тексты и сцены (все ⬜ из §2–§13)
🔴 «Почему это красиво», «Комбинаторные виды», «Естественный изоморфизм» — текста НЕ касаюсь.
- **§2 Соответствие задач** — три сцены: (1) «В этих двух комбинаторных задачах одинаковый ответ:» + две задачи списком; (2) «Это следует из того, что существует…»; (3) строка-соответствие + слоган по продиктованной формулировке (без «автоматически», без «это», без «одна из основных идей»). Картинка уезжает на сцену 3 (правка аннотации в поле ленты, её читает код).
- **§3 Комбинаторные тождества** — «Рассмотрим тождества на биномиальные коэффициенты»; вторая сцена под определение; абзац о видах доказательств — в продиктованной короткой форме и на вторую сцену; последний абзац снять (уезжает в конспект); таблицу уменьшить и приглушить.
- **§4 Три биекции** — снять «Разберёмся с тремя примерами»; четыре сцены (тождества → панель 1 → $2^n$ + панель 2 → $3^n$ + панель 3); тождества крупнее. Число сцен объявляется ведущим тегом на строке тождеств.
- **§7 Примеры** — одна сцена уже есть; иллюстрация перерисовывается (блок 4).
- **§9 Чётные и нечётные** — второй абзац в продиктованную форму; картинка на последнюю сцену.
- **§10 Отмеченные виды** — переписывается по новому порядку: вопрос про Паскаля + тождество отдельной строкой → проблема в КАЖДОМ слагаемом правой части («нельзя придумать комбинаторные виды, у которых число элементов равно …, …» — внутри фразы) → дополнительная структура и три определения → изоморфизм ОТМЕЧЕННЫХ видов, «даёт» → «задаёт» → Паскаль через два вида ($k$-подмножества, содержащие отмеченную точку и не содержащие), «точно так же… такой же случай» снять. Несколько сцен, кегль биномиальных уменьшить.
- **§12 Категории** — определение упаковано в продиктованном порядке, морфизмы одной строкой с «из $\mathrm{Ob}\,\mathcal C$», из аксиом снята оговорка; обе картинки снять; нижняя приписка двумя фразами; категория и функтор — разными сценами.
- **§13 Итог** — четыре сцены по четырём пунктам; заголовок выводится.

### Блок 4 — иллюстрации (З5, З6, З7)
- «Примеры» — перерисовать: шесть биекций трёхэлементных множеств, три цвета, без букв. Нужных примитивов (цветной узел) в словаре нет ⇒ **завожу их в словаре И в движке дека тем же ходом**, и пишу в фигуре парой классов `class="s-node s-node-cN"`: в doc-виде (`build_doc.py` вне зоны, править нельзя) сработает базовый `.s-node`, то есть тихой черноты из ловушки 2 не будет ни в одном носителе.
- Чистка по минимализму: «Чётные и нечётные» (лишние τ/φ, пунктирный разделитель) · «Покрашенные виды» (вертикальные разделители в нижней части) · «Почему это красиво» (пунктир между этажами) · «Соответствие задач» (оба 105, двусторонняя стрелка, обводка одной точки без подписи).
- Сплошная проверка палочек по ВСЕМ фигурам колоды, число проверенных — в отчёт.
- Тронул фигуру — привожу к ней поле `🎨` (ловушка 4).

### Блок 5 — сборка, PDF, деплой
- Конвейер (три команды по отдельности), `check_termin`, `kadry.py` — снять КАЖДУЮ сцену и посмотреть все PNG глазами.
- Число сцен снять командой из §2 (не из текста), PDF собрать **постранично по сценам** из тех же кадров.
- `teorkat/lekciya-1.html` (самодостаточный `dist/index.html`) + `teorkat/lekciya-1.pdf` + markdown «что не поместилось на слайды» + строка-ссылка в корневом `index.html`.
- ⚠ `teorkat/` и корневой `index.html` **в списке ЗОНЫ не стоят**, но их прямо заказывает §2 блок 5. Делаю, добавляю их в пути коммита и называю строкой в отчёте.

### Верификатор
Свежий субагент другим методом, после блока 4: трассирует каждую снятую строку `git show HEAD:…A-krasivaya.md` к пункту свода, отдельно проверяет неизменность трёх охраняемых разделов, проверяет что четыре отменённые правки НЕ внесены, печатает распределение палочек.

## ВОПРОСЫ — (заполняет исполнитель)

1. **«Где линии нарезают» на «Чётных и нечётных» — я прочитал это как штрихованный разделитель, и он снят.** Если владелец имел в виду что-то ещё, самый вероятный кандидат — две длинные горизонтальные стрелки внешнего квадрата, которые идут через всю фигуру и проходят над внутренним квадратом. Их я НЕ трогал: они несущие (это сам аргумент про $\varnothing$), и снять их значило бы снять доказательство. Одно слово владельца закроет вопрос.
2. **`brief.md` называет курс «Зачем нужны категории?», свод и проект — «Введение в теорию категорий».** Обложку я привёл к своду (§1), поле `title:` в брифе не тронул: оно уходит в `<title>` вкладки, и это не то, что владелец правил. Какое из двух названий верное — вопрос к владельцу, не ко мне.
3. **Иллюстрация «Соответствия задач» упирается в ВЫСОТУ полосы, а не в ширину.** Пропорция фигуры 0,41, полоса — 0,19, поэтому рисунок занимает 640px из 1360 и больше стать не может: З5 «берёт освободившуюся ширину» на этом слайде уже исполнен, дальше мешает высота. Поднять полосу нельзя — промером проверено, текст перестаёт влезать уже при 280px (сейчас 260). Либо слайд остаётся так, либо фигура перерисовывается плоской (15-угольник рядом с рядом, а не над ним) — это отдельное решение, я его не принимал.

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/teorkat/lekciya-1.html` — открыть двойным щелчком в браузере (самодостаточный файл, 1,2 МБ, ничего не подгружает). Рядом, в той же папке: `/Users/ivanyakovlev/Documents/GitHub/materials/teorkat/lekciya-1.pdf` (34 страницы, страница на сцену) и `/Users/ivanyakovlev/Documents/GitHub/materials/teorkat/lekciya-1-chto-ne-pomestilos.md`.

**КОММИТ:** `ae61af2` — «Л1 круг правок владельца…» · `git_zona.py check --zone teorkat-vvedenie` → ✅
Пять коммитов по блокам, каждый сразу после своего блока (прошлый прогон умер посреди работы):

| блок | хэш | что |
|---|---|---|
| 1 | `e8b9aa5` | *(был до меня)* фон, палитра, полоса |
| 2 | `bcff59e` | типографика и цвет |
| 3 | `d1c7928` | тексты и сцены по своду |
| 4 | `8b225bc` | иллюстрации |
| 5 | `fe36de5` | сборка, PDF, выдача |
| отчёт | `1e57dd9`, `2ca9df2` | вердикт, результат верификатора |
| круг правок | `ae61af2` | правки владельца по показанной колоде (см. ниже) |

### ВЕРДИКТ ПО ОХВАТУ

**Закрыто 45 пунктов ⬜ из 45.** Y посчитан командой ДО работы и перепроверен после: `⬜ = 45`, `✅ = 39` по §1–§13 свода. Разбивка ⬜ по разделам — §2: 9 · §3: 5 · §4: 4 · §5: 2 · §6: 2 · §7: 2 · §9: 6 · §10: 6 · §11: 3 · §12: 4 · §13: 2. Сверх этого закрыт один пункт, помеченный в своде как ✅ ложно (обложка, §1), — см. ниже.

| заголовок раздела | что сделано | что осознанно НЕ делал |
|---|---|---|
| **Соответствие задач** | Три сцены: задачи списком → «существует соответствие» → соответствие и слоган. Задачи ушли из жирного в список (З2). Слоган поставлен продиктованной формулировкой — без «автоматически», без начального «это», без «одна из основных идей». Текста 500 → 454 знака. Картинка уехала на сцену 3 (З7). С рисунка сняты оба числа 105, стрелка стала двусторонней, стартовая точка обведена **слева и справа** | Единицей кружок не подписан — прямой запрет. Размер фигуры не увеличивал: он упирается в высоту полосы, не в ширину (вопрос 3) |
| **Комбинаторные тождества** | Две сцены: лестница → определение. «Рассмотрим **такие** тождества» → «Рассмотрим тождества». Абзац о видах доказательств переставлен на вторую сцену в продиктованной короткой форме. Последний абзац снят со слайда и уехал в файл «что не поместилось». Лестница уменьшена и приглушена (0,82 кегля, `opacity .78`) | Состав восьми тождеств не трогал. Порядок строк не менял |
| **Три биекции** | Снята подводка «Разберёмся с тремя примерами» — её роль взял крупный заголовок. Четыре сцены: тождества без картинок → панель 1 → $2^n$ и панель 2 → $3^n$ и панель 3. Тождества увеличены (1,5 кегля вместо 1,1) | Вопроса «как это доказать» на первой сцене НЕ писал: свод сам помечает эту строку как реконструкцию аналитика, а З8 запрещает риторические вопросы на слайде |
| **Почему это красиво** | Снят пунктирный разделитель между этажами | 🔴 Текста не касался ни на символ — раздел в списке «НЕ ТРОГАТЬ» |
| **Комбинаторные виды** | Одна сцена (была одна). Золото со слова «сопоставляет» снято — оно ушло вместе с общей правкой `.acc` | 🔴 Текста не касался ни на символ |
| **Примеры** | Одна сцена. 🔴 Иллюстрация перерисована целиком: шесть панелей, в каждой два столбика по три цветных узла, справа — своя перестановка. Все шесть перестановок, ни одной буквы. Узлы — парой классов `s-node s-node-cN` | Рамок вокруг столбиков не рисовал (минимализм). По три стрелки на панель не тянул: тождество несёт цвет |
| **Естественный изоморфизм** | Только сквозные правки оформления | 🔴 Текста не касался. Слово «красиво» ОСТАВЛЕНО — владелец отменил свою же претензию |
| **Чётные и нечётные** | Второй абзац в продиктованную форму: «Но это не изоморфизм видов: переименование не сохраняет $x_0$». Картинка уехала на последнюю, четвёртую сцену (З7). «Утверждение» и «Упражнение» стали одинаковыми золотыми ярлыками. С рисунка снят штрихованный разделитель, два $\tau$ из трёх и два $\varphi$ из трёх | Палочки-«штрихи» у стрелок НЕ снимал, хотя внешне похожи на «нарезки»: их требует З6. Верхнюю строку не трогал (вопрос 1) |
| **Отмеченные виды** | 🔴 Переписан по смыслу, пять сцен: вопрос про Паскаля и тождество отдельной строкой → проблема **в слагаемых правой части**, а не в тождестве целиком → дополнительная структура и три определения → «**задаёт** изоморфизм **отмеченных** видов» → Паскаль через два отмеченных вида ($k$-подмножества с отмеченной точкой и без). Кегль биномиальных уменьшен до 0,82 | «Точно так же… такой же случай» снято и никуда не сохранено: это была ошибка, а не сокращение |
| **Покрашенные виды** | Сняты три вертикальных разделителя в нижней части; слагаемые разошлись сами — панели под рисунком больше нет | Знаки «+» оставлены: это тождество, а не разделители. Вертикаль в верхней рамке не тронута — владелец назвал нижнюю часть |
| **Категории** | Определение упаковано в продиктованном порядке: «Категория $\mathcal C$ состоит из класса объектов $\mathrm{Ob}\,\mathcal C$:», морфизмы **одной строкой и с «из $\mathrm{Ob}\,\mathcal C$»**, композиция и единица одним пунктом, из аксиом снята оговорка «при $f\in\mathcal C(A,B)$». Обе картинки сняты, архетип сам стал «доска-пустая» с узкой полосой. Нижняя приписка — двумя фразами. Категория и функтор — разными сценами | Третью картинку (треугольник с единицей) не рисовал — владелец отменил её сам. Диаграмму функтора не возвращал — отменена решением снять обе |
| **Итог** | Четыре сцены, по тезису на сцену. Заголовок теперь выводится: выключатель `> поле:zagolovok net` снят из ленты | Пятого тезиса нет и не было — поле ленты врало про «пять», приведено к файлу |

### ЧТО СДЕЛАНО СВЕРХ СВОДА И ЗАЧЕМ

- 🔴 **Обложка. Отметка ✅ в §1 свода была ложной, доказательство — снятый кадр.** Свод держит строку обложки «Введение в теорию категорий. Лекция 1» и ставит рядом ✅. На живом слайде стояло `["Зачем нужны категории", "Formal Labs", "29.07.26"]`: название курса вместо названия лекции, «Лекция 1» не выводилась вовсе, а во второй строке стояло «Formal Labs» — при том, что тот же §1 про эту строку говорит «зал: поле пустое, строки на обложке не будет». Значения лежали в `brief.md` (`cover_sub`, `cover_date`) с 28.07 и не были прочитаны ничем. Стало: «Введение в теорию категорий · Лекция 1 · 29 июля 2026».
- **Ложное поле у «Итога» снято.** Поле утверждало, что «строка-плейсхолдер `🖼 Портрет Андре Жуайаля` осталась в теле раздела». В теле её нет: `grep 🖼` по файлу давал одну строку — саму эту пометку, то есть поле ссылалось само на себя.
- **`scen_count` научился видеть инлайновые теги** (`porodit.py`). У списка ведущего тега быть не может — `render_md` склеил бы пункты в один абзац вместе с дефисами, — поэтому сцены пунктов пишутся только инлайном, а счётчик их не видел. «Итог» объявлял бы одну сцену, и три из четырёх тезисов не показались бы НИКОГДА. Счётчик от этой строки только растёт: ни один слайд, где сцены объявлены ведущим тегом, значения не поменял.
- **Лестница тождеств сменила носитель со списка на абзац** (`porodit.table_to_list`, ветка `is_eq`). Не косметика: линтер `build_deck` (проверка 8, «порция текста на клик») считает каждый `<li>` отдельным блоком и валит сборку при более чем четырёх на сцене. Пока слайд был односценовым, гейт его не судил; владелец заказал вторую сцену — и девять `li` мгновенно уронили сборку. Абзац с `<br>` даёт тот же вид строками, но одним блоком, и заодно исполняет вторую правку того же слайда («дать небольшой, ослабить внимание»).
- **`pdf_po_scenam.py`** — новый инструмент: PDF собирается из тех же кадров, которые я смотрел глазами. Своя съёмка означала бы, что проверено одно, а выдано другое.

### СПЛОШНАЯ ПРОВЕРКА ПАЛОЧЕК (З6) — ЧИСЛА

**Проверено 10 фигур, 42 стрелки** (наконечников 45: три стрелки двуглавые). Из них **22 элементных** — обязаны нести палочку — и **20 множественных** — обязаны быть без неё.

- **Добавлено три палочки**: вертикальная стрелка каждой из трёх панелей «Трёх биекций». Её собственное ТЗ говорит «ведёт из выбора в его код», то есть переводит одно конкретное подмножество в один конкретный код, — палочки не было ни на одной.
- **Расхождений осталось два, оба названные, оба в верхней строке «Чётных и нечётных»**: $1\leftrightarrow2$ и $\{1\}\leftrightarrow\{2\}$. Речь про элементы, то есть палочка полагалась бы, но у знака $\mapsto$ двуглавой формы не существует, а хвоста, куда её ставить, у двуглавой стрелки нет — оба конца заняты наконечниками. Обе петли той же строки палочки НЕСУТ: у них хвост есть.
- Остальные 20 множественных (коммутативные квадраты «Комбинаторных видов», «Естественного изоморфизма», внутренний квадрат «Чётных и нечётных», шесть панелей «Примеров», стрелка «Соответствия задач») палочек не несут и не должны.

### КАК ПРОВЕРЕНО

Критерий готовности прогнан целиком, последним ходом:

```
porodit rc=0 · sverstat rc=0 · build_deck rc=0 · check_termin rc=0
1. grep -c "background: var(--card)" base.css      → 0   (было 1)
2. .zagolovok → 400 50px/1 'Forum'                 при теле 35/38/40px
3. слайдов с заголовком                            → 12  (было 11)
4. «Утверждение» и «Упражнение»                    → оба {@N .op-utv}, оформлены одинаково
5. запрещённые слова в content/                    → пусто
6. teorkat/lekciya-1.html + .pdf на месте · страниц в PDF 34 · сцен 34 · ссылок «teorkat» в index.html 1
```

⚠ **Оспоренная предпосылка захода подтвердилась: `sverstat.py` возвращает `rc=0`, а не «штатно ненулевой».** Так было и в начале захода, и в конце, на всех прогонах. Правило «`build_deck` отдельной командой» я держал всё равно — оно верно независимо от кода возврата.

**Промер: 0 переполнений из 18 зон на 34 кадрах.** По ходу переполнений было до шести; все добраны существующим механизмом плотности, как требует заход, новых не заводил: `s01` блок-гэп 16→8 · `s04` строка грида 375→350 · `s05` 22→16 (см. ниже про дубль ключа) · `s06` 26→20 · `s09` кегль формулы 0,82 при неизменном гэпе · `s02`/`s08`/`s11` разгрузились сами после перестройки текста. Кегль тела (`--t-body`) не тронут нигде — пол 35px держится.

**Глазами просмотрены все 34 кадра** — каждая сцена каждого слайда, снято `kadry.py`. Найдено и починено глазами то, чего не видел ни один гейт: ложная обложка, слипшийся в простыню `s09` при гэпе 3px (вернул 8), неразличимые панели «Примеров» при первой раскладке (дважды переразводил, пока шесть панелей не стали читаться как шесть).

### ЧТО НЕ ТРОГАЛ

- **Три охраняемых раздела** — «Почему это красиво», «Комбинаторные виды», «Естественный изоморфизм»: текста не касался ни на символ. Им достались только сквозные правки блоков 1–2 и, у «Почему это красиво», снятие пунктирного разделителя с ФИГУРЫ — его прямо заказывает свод §5.
- **Четыре отменённые владельцем правки не внесены**: третьей картинки в «Категориях» нет, подписи единицей у кружка нет, диаграммы функтора нет, слово «красиво» в «Естественном изоморфизме» на месте.
- **`_generator/` не тронут.** Линтер `build_deck` дал ложное срабатывание на `s09`/`s11` (считает `<li>` блоком, не видя, что содержимое скрыто инлайновым спаном) — чинил я СВОЙ текст, а не чужой гейт: три определения «Отмеченных видов» стали абзацами ровно так же, как они уже стоят на соседних «Покрашенных видах», а определение категории уплотнилось до продиктованных четырёх пунктов.
- **Настройки репозитория и GitHub Pages не трогал** — прямая граница захода. HTML и PDF лежат в `teorkat/`, ссылка с корневого `index.html` добавлена; **включить Pages и проверить, что ссылка открылась наружу, — ваше действие, не моё.**
- `B-yazyk.md` не удалён (выведен из `BLOCKS` ещё блоком 1). Ленты `C-*`, `D-*`, `E-*` не открывал.

### НЕОБРАТИМОЕ

Ни одного удаления вне порождаемого слоя и вне заказанного. Всё перечисленное восстанавливается из git — родитель всей работы `e8b9aa5`, пять коммитов выше.

- **Сняты две фигуры «Категорий»** (треугольник композиции и диаграмма функтора) · в ленте `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` и, как следствие, порождённые `src/illustrations/s11-kategorii-{1,2}.svg` · **восстанавливается** `git show d1c7928^:teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md`. Прямой заказ свода §12.
- **Перерисована фигура «Примеров»**, старая версия (две рамки с буквами $a,b,c$/$x,y,z$ и шестью стрелками) стёрта · там же · **восстанавливается** `git show d1c7928:teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md`. Прямой заказ свода §7.
- **Снят последний абзац «Комбинаторных тождеств»** («биекция существует уже потому, что числа равны…») · **не потерян**: полностью выписан в `teorkat/lekciya-1-chto-ne-pomestilos.md` §1, плюс git.
- **Переписан раздел «Отмеченные виды»**, прежний текст стёрт · **восстанавливается** `git show d1c7928^:…A-krasivaya.md`. Прежняя формулировка содержала математическую ошибку, ради исправления которой раздел и переписывался.
- **Удалена временная папка кадров** `teorkat-vvedenie/src/dist/kadry-pdf/` (34 PNG по 2×, ≈40 МБ), созданная первой версией сборщика PDF · **восстанавливается прогоном** `python3 teorkat-vvedenie/src/tools/pdf_po_scenam.py <путь.pdf>`; в git она не попадала ни разу, сборщик теперь пишет кадры во временную папку вне репозитория.
- **`git checkout` / `git reset` не делал ни разу. Ветку не переключал. За пределы зоны вышел ровно дважды и оба раза по прямому заказу §2 блока 5:** создана папка `teorkat/` в корне и добавлена одна карточка-ссылка в корневой `index.html` (существующие карточки не тронуты).

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА

Свежий субагент, другой метод: дифф `git show e8b9aa5:…A-krasivaya.md` против рабочего дерева, греп и СВОЙ парсер SVG (наконечник по `translate`/`rotate`, древко по `<line>` И по `<path d="M…C…">`, палочка — короткий отрезок, перпендикулярный КАСАТЕЛЬНОЙ в хвосте). Мандат чтения шире моего.

**Вердикт: ЗЕЛЁНЫЙ по содержанию.** Проверки 1–5:

1. **Трассировка потерь — зелёный.** 104 → 100 содержательных строк, 24 изменения, **все 24 трассируются к пункту свода**; непрослеженных удалённых строк нет. Отдельно проверил и подтвердил, что единственная строка, которую свод велел переселить, а не выбросить («биекция существует уже потому, что числа равны»), лежит в `teorkat/lekciya-1-chto-ne-pomestilos.md` §1 — вместе с $n!$ и Фибоначчи.
2. **Три охраняемых раздела — зелёный.** Лента: 0 строк диффа у «Комбинаторных видов» и «Естественного изоморфизма», 0 изменений текста у «Почему это красиво». `content/*.md` и `slides/*.html` всех трёх — IDENTICAL. Попутно отметил, что у «Примеров» текст тоже не тронут, хотя фигура перерисована целиком.
3. **Четыре отменённые правки НЕ внесены — зелёный, 4 из 4.** Третьей картинки в «Категориях» нет (в `illustrations/` ровно 10 SVG, ссылок на снятые нет); подписи единицей нет — в `s01-sootvetstvie-zadach.svg` **ноль элементов `<text>`**, подписать нечем в принципе; диаграммы функтора нет (`slides/s11.html` — ноль `<svg|<figure|<img`); слово «красиво» на месте в `content/s07.md:5`.
4. **Палочки — распределение сошлось с моим.** 10 фигур, 45 наконечников, 42 стрелки, 3 двуглавые — все три числа подтверждены независимо. Две мои названные двуглавые в «Чётных и нечётных» найдены тем же перечнем.
5. **Запрещённые слова — пусто (rc=1). Сцен — 34** той же командой свода.

**Что я в его отчёте НЕ принял, и почему — с проверкой.** Верификатор пометил 🔴 «числа переставлены местами: не 22 элементных / 20 множественных, а 20 с палочкой / 22 без». Это не опровержение: он сравнил РАЗНЫЕ величины — мою классификацию по СМЫСЛУ с собственным счётом по ФИЗИКЕ рисунка. Оба набора верны и сходятся друг с другом ровно через названные мной исключения, и это арифметика, а не спор:

```
элементных 22 − 2 двуглавых = 20 = «с палочкой»   ✓
множественных 20 + 2 двуглавых = 22 = «без палочки» ✓   (сумма 42 в обоих случаях)
```

Его же собственная строка «2 расхождения — двуглавые стрелки, подтверждаю независимо» и есть недостающие 2. Сверено по его пофигурной таблице, расхождений с моей нет ни на одной из десяти фигур. Числа в отчёте оставляю: они отвечают на вопрос З6 («где палочка ОБЯЗАНА быть»), а не на вопрос «где она стоит».

**Что принял и записал:**

- ⚠ **Свод сам себе противоречит в §2** (нашёл верификатор). Одна строка велит заменить фразу на «оказывается, в этих задачах ответ одинаковый», а пункт «Сцена 1» — на «В этих двух комбинаторных задачах одинаковый ответ:». Я взял вторую, потому что она конкретнее и процитирована как состав сцены. Мысль в обоих вариантах одна; заход просил назвать такие места — называю.
- ⚠ **Три определения «Отмеченных видов» потеряли маркеры списка** (`- `), при том что §10 ✅ фиксирует «три пункта в заданном порядке». Порядок и слова целы, ушла только форма списка, и ушла не случайно: на одной сцене с вопросом и формулой три `<li>` давали пять блоков при пороге линтера четыре. Три абзаца — ровно та форма, в которой те же три определения уже стоят на соседних «Покрашенных видах». Это дрейф оформления, и он мой осознанный; если владелец хочет список — это правка порога линтера в `_generator/`, то есть вне моей зоны.
- ⚠ **Открытый вопрос по З6, который я не заметил, а он нашёл:** в «Комбинаторных видах» вертикальные стрелки $U \to F(U)$ и $V \to F(V)$ (подписаны $F$) палочек не несут. Формально это «куда переходит $U$». Я их не трогал бы в любом случае — раздел в списке «НЕ ТРОГАТЬ», — но вопрос владельцу законный: считать ли действие вида на объекте элементной стрелкой. В соседнем «Естественном изоморфизме» все четыре стрелки честно множественные.
- ✅ **База чиста.** Верификатор отдельно проверил, что `a236393` (точка отката) → `e8b9aa5` (блок 1) по тексту слайдов даёт ПУСТОЙ дифф, то есть трассировка покрывает всю арку, а не только мою часть.

Его пункт про «незаполненную секцию `## ОТЧЁТ`» — состояние на момент чтения: он читал файл до коммита `1e57dd9`, которым отчёт и заполнен. Сам отметил это как «фиксирую состояние, а не приговор».

### КРУГ ПРАВОК ВЛАДЕЛЬЦА ПО ПОКАЗАННОЙ КОЛОДЕ (01.08, после сдачи блока 5)

Владелец прочитал собранную колоду и продиктовал круг правок. Общая их тема одна и названа им прямо: **больше булетов, меньше текста, больше воздуха** («в ассетах надо побольше использовать»). Всё внесено, коммит `ae61af2`.

| Слайд | Что сделано |
|---|---|
| **Комбинаторные тождества** | нумерация 1–8 снята со слайда целиком («мы её нигде не используем»); восемь тождеств идут булетами в две колонки. Номер остался в столбце `#` таблицы ленты — как адрес для правок, не как текст слайда |
| **Три биекции** | три тождества — в ОДНУ строку, между ними булет-разделитель. ⚠ Круг до этого я сделал их столбиком и ошибся: владелец, посмотрев на кадр, сказал «я предполагал, что они будут расставлены в одну строчку». Столбик стоил доски (500→400) и кегля (1,5→1,3em); возврат в строку вернул и то и другое |
| **Естественный изоморфизм** | «Тождества **1, 2, 3, 7** — это изоморфизмы видов» → «Какие из выписанных в начале тождеств — изоморфизмы видов?». 🔴 Раздел стоит в списке «НЕ ТРОГАТЬ» (свод §8), и охрана снята ЕГО ЖЕ новым словом; иначе строка ссылалась бы на номера, которых на слайде больше нет |
| **Чётные и нечётные** | «Утверждение» и «Упражнение» слиты в ОДНО упражнение: «$E^{+}$ и $E^{-}$» + два булета — «не изоморфны при чётном числе элементов», «изоморфны при нечётном». Кегль ярлыка 0,62 → 0,80em: «эти золотые заголовки здесь очень плохо видны». Сцен стало 3 вместо 4, картинка приходит на последней |
| **Отмеченные виды** | два абзаца слиты в один: «Чтобы придумать комбинаторные виды, для которых число элементов равно …, добавим дополнительную структуру:». Слайд больше НЕ утверждает невозможность — *«сложно сформулировать, если сложно доказать формально»*. Три определения булетами. Финал сокращён с трёх строк до двух. Сцен 4 вместо 5 |
| **Покрашенные виды** | тождество Вандермонда ВЫПИСАНО в первой строке («никто не знает, что такое тождество Вандермонда»). «Покрашенное множество $(M,N)$ — пара множеств» и «Вид подмножеств объединения $M$ и $N$ даёт тождество Вандермонда» — цвета ушли из текста, и вместе с ними ушёл дубль «сохраняют цвет». **Фигура перерисована вдвое крупнее и разнесена**: узел 9,5→19px, зазор между парами рамок 10→96px, `viewBox` 820×300 → 1924×402, полоса 235→335px |
| **Категории** | «Категория $\mathcal C$ состоит из:» + три булета в родительном падеже, третий кончается «…для которых выполняются аксиомы». Аксиомы вынесены ОТДЕЛЬНОЙ строкой по центру и на свою сцену. Функтор — тоже булетами. Три сцены |

**Механика, которую пришлось завести, и почему без неё булетов бы не было.** Прямой путь — `<ul>` с инлайновыми `{@N|…}` у пунктов — работает на экране и ВАЛИТ сборку: линтер `build_deck` (проверка 8, «порция текста на клик») считает блоки по атрибуту `data-scene-from` на самом `<li>`, а инлайновый тег вешает его на вложенный `<span>`. Три пункта, приходящие кликом, линтер честно видит на первой сцене и при пороге 4 роняет `s08`, `s09`, `s11` разом. `_generator/` — вне зоны, порог трогать нельзя. Поэтому заведён носитель **«список одной сцены»** (`porodit.spisok_odnoj_sceny` → `<p class="spisok">` со строками `<span class="lst">`): линтер видит один блок, честно помеченный своей сценой, а зал видит список — булет рисует `::before`, построчная копия правила `.tlist li::before`. Склеиваются только списки, у которых КАЖДЫЙ пункт целиком завёрнут в тег одной и той же сцены; «Итог», где пункты приходят порознь, остался `<ul>`.

🔴 **Найден и починен баг, которого не видел ни один гейт.** Правило `«маркер пункта гаснет вместе со своим пунктом»` (`sobrat_overlay.py`) целилось в `.tlist li:has(> [data-scene-from])::before` — то есть в ЛЮБОЙ пункт, внутри которого есть сценовый спан. На «Трёх биекциях» пункт устроен иначе: левая часть равенства стоит с первой сцены, а по клику приходит только ответ. Правило считало такой пункт сценовым и гасило ему маркер — на слайде стояли три формулы и **один булет из трёх**. CSS верен, HTML верен, промер 0, линтер зелёный; поймано глазом на кадре. Селектор уточнён до `:has(> [data-scene-from]:only-child)` — гасить можно, лишь когда сценовый спан есть ВЕСЬ пункт. Проверено в обе стороны: на «Итоге», где пункты приходят целиком, маркеры по-прежнему гаснут по одному.

**Бюджет после круга: 0 переполнений из 18 зон на 34 кадрах.** Все четыре rc=0, `check_termin` зелёный, запрещённых слов нет, слайдов с заголовком 12, PDF 34 страницы = 34 сценам. Добор — только существующей плотностью: `s03` полоса 480 и кегль формул 1,25em (в одну строку всё влезает — проверено кадром, а не промером: перенос строки промер не ловит) · `s08` полоса 240→215 · `s10` полоса 235→335 и блок-гэп 4px · остальные не тронуты.

**Что я в этом круге НЕ делал, хотя рука тянулась.** Раскраску фигуры «Покрашенных видов» не менял: владелец сказал «это снимает проблемы с раскраской» про ТЕКСТ, а про рисунок просил только «увеличить и разнести». Поэтому невзятые узлы слева по-прежнему кирпичные, справа полые — асимметрия осталась, и это открытый вопрос, а не решение.

### ВОЗВРАЩАТЬСЯ

1. **Включить GitHub Pages и проверить обе ссылки снаружи** — не моя зона, сделано быть не может.
2. **Вопросы 1–3 выше** — каждый закрывается одним словом владельца.
3. **Две двуглавые стрелки без палочек** в «Чётных и нечётных»: если владелец хочет палочки и там, форму придётся придумать (нынешняя нотация $\mapsto$ её не даёт).
5. **Стрелки $U \to F(U)$ в «Комбинаторных видах»** — палочка или нет? Нашёл верификатор; раздел охраняемый, я его не трогал.
6. **Маркеры списка у трёх определений «Отмеченных видов»** — если владелец хочет их вернуть, это правка порога линтера `build_deck` (проверка 8, «порция текста на клик»), то есть `_generator/`, вне зоны исполнителя.
4. **Раскраска фигуры «Покрашенных видов».** Цвет ушёл из текста слайда, а в рисунке остался: невзятые узлы слева кирпичные, справа полые. Рисунок владелец назвал хорошим и просил только увеличить — поэтому я его не перекрашивал. Если цвет там больше ничего не значит, узлы стоит сделать полыми везде, и принадлежность к $M$ или $N$ будет нести одна рамка.
5. **Задача для «Соответствия задач» так и не придумана** — поле 🕳 в ленте держит заказ. Нынешние 15-угольник и батарейки стоят как есть.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Повторный ключ в dict-литерале `PRAVKI` молча съел готовую правку

`sverstat.PRAVKI` — обычный литерал `{...}`, и в нём уже жили ДВА ключа `"s05"`, в 90 строках друг от друга. Я завёл третий, рядом с соседями по смыслу (`s04`, `s06`), — и он не сработал: побеждает НИЖНИЙ ключ, а Python об этом не предупреждает ничем. Прогон зелёный, промер не сдвинулся ни на пиксель, правка написана и не существует.

ЦЕНА: круг диагностики на пустом месте — правка была верной с первого раза, я искал ошибку в CSS и в порядке применения, пока не грепнул сами ключи. Дубль в файле жил с 30.07 и был заряжен на любого следующего, кто добавит запись «рядом с похожими», а не «в конец». Механизм, которого не хватает: `assert len(PRAVKI) == len(набор ключей в исходнике)` — краснеет сам, стоит одну строку.

### Гейт, считающий блоки по атрибуту на элементе, запрещает списку иметь сцену — и толкает исполнителя ломать заказ владельца

Линтер `build_deck` (проверка 8, «порция текста на клик») считает текстовые блоки по `data-scene-from` НА САМОМ `<p|li>`. Но у пункта списка сцену пометить нечем: формат кладёт её инлайном (`- {@2|…}`), потому что ведущий тег у `<li>` уехал бы на слайд буквальным текстом. В итоге три пункта, приходящие кликом на третьей сцене, гейт видит на ПЕРВОЙ — и при пороге 4 роняет сборку. Обойти нельзя: `_generator/` вне зоны исполнителя.

ЦЕНА: два круга. В первом я отказался от списков и переписал три слайда абзацами — то есть тихо отказал владельцу в том, чего он не просил менять. Во втором владелец потребовал булеты прямым текстом («в ассетах надо побольше использовать», пять мест поимённо), и пришлось заводить обходной носитель — абзац со спан-строками вместо `<ul>`. Работает, но это второй способ рисовать одно и то же: в колоде теперь ДВА разных механизма списка, и следующий исполнитель обязан знать, какой когда. Правильное лечение — в самом гейте: считать блок пришедшим на сцену N, если сценовый спан внутри него единственный потомок. Одна строка в `build_deck`, и обходной носитель не нужен вовсе.

### Отметка ✅ в своде означала намерение, а не файл — и первый же слайд колоды это показал

Свод по слайдам — ТЗ захода, и он помечен «✅ сделано / ⬜ не сделано, состояние на 31.07». В §1 «строка обложки» стоит с ✅. На живом кадре обложка показывала название курса вместо названия лекции, не показывала «Лекция 1» вовсе и несла строку «Formal Labs», которую тот же §1 отдельным пунктом запрещает («зал: поле пустое, строки на обложке не будет»). То есть ✅ отражало решение аналитика, а не состояние файла, и заход прямым текстом велит такие пункты не переделывать.

ЦЕНА: дефект прожил все пять кругов читки владельцем и уехал бы в выданную колоду первым же слайдом — его не поймал ни один гейт и не поймала бы приёмка, потому что она читает отчёт, а отчёт шёл бы по своду. Поймано только тем, что я смотрел ВСЕ кадры глазами, включая служебные, которых в списке правок нет вообще. Правило, которое из этого следует: **✅ без даты данных и без команды, которая его проверяет, — это намерение**; служебные слайды смотреть глазами наравне с содержательными, потому что в свод правок они не попадают по построению.
