# Канал исполнителя — geometria (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — выбор геометрии под содержание слайда и написание ТЗ иллюстрации есть суждение; расписать его заранее нельзя.
> ⏱ **Спешки нет. Метрика — ОДНОЗНАЧНОСТЬ ТЗ и ЧЕСТНОСТЬ БЮДЖЕТА. Часы — норма.**

КОНТЕКСТ. Проект `teorkat-vvedenie/` — **записки Л1**: слайд-конспект прочитанной половины лекции, который выкладывают в интернет постфактум, чтобы люди прочитали и разобрались. Лектора нет, бюджета времени нет.

Лента уже переложена в **15 разделов** по утверждённому `SPISOK-SLAJDOV.md` (заход `kod_sborka.md`, принят 30.07): текст перенесён дословно, пустые слайды заведены с заказом. **Записок при этом ещё нет**, и главный дефицит — не текст, а ВИЗУАЛ: `<figure>` в ленте **6 штук на 15 разделов**, и **девять разделов не имеют ни одной**, включая все четыре, где владелец просил рисовать.

🔴 **Контекст, который меняет критерий для картинок целиком: их будет ПЕРЕРИСОВЫВАТЬ ХУДОЖНИК.** Владелец: *«меня сейчас просят выдать слайды, чтобы художник мог перерисовать картинки более качественно и профессионально. Наша задача — сделать такие слайды, чтобы было понятно, что происходит»*. Значит иллюстрация здесь — **не финальный артефакт, а ТЕХЗАДАНИЕ**, и мерится не красотой, а однозначностью: другой человек по описанию нарисует то же самое.

ЦЕЛЬ: **для каждого из 15 слайдов определить ГЕОМЕТРИЮ, посчитать БЮДЖЕТ текста и визуала и выдать ТЗ ИЛЛЮСТРАЦИЙ.** Порядок именно такой и он продиктован владельцем: *«определить для каждого слайда геометрию, определить, какой там должен быть объём текста и какой объём визуала, и в зависимости от этого понять, сколько текста дописывать»*.

🔴 **СТОП ДО ЦЕЛИ, простыми словами: ты НЕ ПИШЕШЬ НИ ОДНОГО ЗНАКА ТЕКСТА СЛАЙДА.** Ни строки в блоках, ни формулировки, ни связки. Ты правишь ТОЛЬКО служебные поля `> поле:mn` (раскладка и ТЗ картинки) и заводишь отдельный документ с бюджетом. Текст пишется СЛЕДУЮЩИМ заходом, по твоим числам, и владелец увидит его порциями.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_geometria.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_geometria.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- ПЛАН — в `## ПЛАН` перед действиями.

**Прочитать ТОЛЬКО это.** Адреса прогнаны через `test -f` при сборке захода 30.07.

| # | файл | зачем | режим |
|---|---|---|---|
| 1 | `_studio/zhurnal/2026-07-30_dovodka-l1/SPISOK-SLAJDOV.md` | 🔴 **читается ПЕРВЫМ.** Что на каждом слайде: мысль, что на экране, откуда текст. Плюс «Философия записок» — она объясняет, что несут картинки | чтение |
| 2 | `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` | лента, слайды 1–12 | **правишь ТОЛЬКО поля `> поле:mn`** |
| 3 | `teorkat-vvedenie/raskadrovka/teksty/B-yazyk.md` | лента, слайды 13–15 | то же |
| 4 | `_studio/konvejer/07-verstka/DOK.md` | канон вёрстки: архетипы, доли ширины, правило «доля одного архетипа краснеет выше двух третей» | чтение |
| 5 | `_studio/konvejer/09-illustracii/SLOVAR-primitivov.md` | 🔴 **словарь примитивов — визуальный язык ЗАДАН, изобретать его нельзя.** Плюс правило поля: `высота/ширина ≥ 0,6 → на поле, иначе во всю строку` | чтение |
| 6 | `_studio/konvejer/06-tekst/DOK.md` | потолки текста: 650 знаков и 55 слов **на КАДР**, медиана корпуса ~45 слов | чтение |
| 7 | `teorkat-vvedenie/src/tools/porodit.py`, строки 436–470 | 🔴 **как архетип выбирается ФАКТИЧЕСКИ** — см. ловушку 1 | чтение |

Не открывать: `KONSPEKT-L1/` · `C-*.md`, `D-*.md`, `E-*.md` · `src/` кроме названных строк `porodit.py`.

🔴 **АРХЕТИП ВЫБИРАЕТСЯ ПОДСТРОКОЙ В ПОЛЕ «Раскладка», а не твоим намерением.** `porodit.py:438`:
```
"на всю ширину"                          → лестница-во-всю-ширину  (панелей НЕТ вовсе)
"крупная горизонтальная иллюстрация снизу" → илл-полосой-снизу
"полоса пустая" / "полоса справа пустая"   → доска-пустая  (узкое поле, текст на всю ширину)
иначе                                     → рейка-справа  (вертикальная полоса 344px)
```
Плюс поправка `archetype()`: «рейка-справа» без фигур и портретов молча становится «доска-пустая».
**Это значит: ты не «выбираешь вёрстку», ты ПИШЕШЬ ФОРМУЛИРОВКУ, которая матчится.** Прогони `archetype_lenty` на своей формулировке и напечатай результат — иначе получишь не тот архетип и не заметишь.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Два выхода: **правленые поля `> поле:mn` в ленте** и **один новый файл** `_studio/zhurnal/2026-07-30_dovodka-l1/GEOMETRIYA-I-BYUDZHET.md`.

### Шаг 1. Геометрия — по слайду
Для каждого из 15 разделов выбрать архетип **под содержание, а не под привычку**, и записать формулировкой, которая матчится (ловушка 1). Ориентиры владельца:
- *«бывают вёрстки, когда у нас вертикальная полоса, бывает — когда горизонтальная. Здесь у нас появится какое-то количество таких горизонтальных слайдов, на которых текста меньше, картинок больше»*;
- *«когда мало текста — хорошо помещать вниз картинку»*;
- *«если это вертикальная полоса, там не так много поместится, зато может поместиться НЕСКОЛЬКО диаграмм, и на них должно быть не очень много деталей. Снизу может поместиться очень большая картинка, но для этого нужно оставить на текст меньше половины»*.
⚠ **Доля одного архетипа краснеет выше двух третей** (`07-verstka/DOK.md`). Напечатай распределение числом.

### Шаг 2. Бюджет — числами, по слайду
Таблица: **знаков текста сейчас · сколько влезает при выбранном архетипе · дефицит или профицит**. Потолок — 650 знаков и 55 слов **на КАДР** (не на слайд: слайд со сценами вмещает больше). Для архетипов с картинкой доля текста меньше: у «илл-полосой-снизу» — **меньше половины площади**, это прямое слово владельца.
🔴 **Правило владельца про недобор, вписать в таблицу отдельной колонкой «цель»:** *«меньше нормы лучше, чем больше нормы, хотя в идеале подобраться к норме. Норма определена стилистикой окружающей, помноженной на объём места, которое есть на слайде»*. Значит целевое число — не потолок, а норма минус запас; называй его явно.

### Шаг 3. ТЗ иллюстраций — главный выход захода
Для каждого слайда, которому нужна картинка, завести в ленте поле:
```
> поле:mn 🎨 **ЧТО НА КАРТИНКЕ.** ИЗОБРАЖЕНО: <объекты и их число> · ПОДПИСИ: <что подписано буквами> · ДЕЙСТВИЕ: <что происходит, какая стрелка куда> · НЕ РИСОВАТЬ: <чего быть не должно> · РАЗМЕР: <на поле / во всю строку, и почему>
```
Все пять полей обязательны. **Критерий: другой человек по описанию нарисует то же самое.** Картинки будут перерисованы художником — ты пишешь ТЗ, а не эскиз.

🔴 **ДВА ТЗ ВЛАДЕЛЕЦ ПРОДИКТОВАЛ САМ. Взять дословно, не пересказывать.**

**Переименование (слайд «Переименование ничего не ломает»):** *«сверху показывается, что происходит, когда ты переименовываешь $a,b,c$ на $a',b',c'$. А снизу показывается, как это переименование влияет на двухэлементные подмножества семиэлементного множества, и на пятиэлементные, и как переход к дополнению это сохраняет. То есть коммутативная диаграмма — на примере конкретного двухэлементного, семиэлементного»*.

**Изоморфизма нет (слайд «изоморфизма нет» — по владельцу самое сложное место записок):** *«предположим, что был бы такой изоморфизм. Посмотрим, как он действует на подмножества множества из двух элементов. Слева — перестановка, которая элементы меняет местами; справа — оставляет на месте. И получается коммутативная диаграмма, и там просто непонятно, куда переводить конкретный элемент: если он переходит куда-то, то с другой стороны он должен переходить в другое значение. Финальная существенная часть — коммутативная диаграмма с ОТДЕЛЬНЫМ описанием того, как действует перестановка»*.

**Три места, где владелец сказал, что содержание — ИМЕННО КАРТИНКИ, а не текст:**
- *«разбирать, почему это вид, у пяти видов скорее всего не будем — это именно картинки. Мы строим коммутативные диаграммы, где слева-справа что-то действует»*;
- *«общий аргумент про чётные и нечётные — это чисто картинки; можно тоже в нескольких фразах буквально сказать опорные идеи, но в основном это визуал»*;
- *«диагноз про Вандермонда и Паскаля — очень коротко, там можно что-то сказать»*.
⇒ У этих слайдов бюджет текста **малый по замыслу**, и это не дефицит, а решение. Отметь.

### Шаг 4. Общий принцип, который держит весь заход
Владелец: *«все математические вещи должны быть объяснены с помощью формул плюс картинок, то есть без слов, в основном — но при этом полностью объяснены, чтобы можно было понять, о чём речь: какие отображения, как они строятся»*. **Формула и картинка несут объяснение; текст только называет, что происходит.**

### 🔴 КРИТЕРИЙ ГОТОВНОСТИ — шесть гейтов, все могут ПРОВАЛИТЬСЯ
1. **АРХЕТИП ФАКТИЧЕСКИЙ.** Прогнать `archetype_lenty` (импортом из `porodit.py`) на каждом из 15 полей и напечатать таблицу «слайд → что я хотел → что вернула функция». **Расхождений 0**, иначе гейт красный.
2. **РАСПРЕДЕЛЕНИЕ.** Напечатать долю каждого архетипа. **Доля одного выше 2/3 — гейт красный** (правило канона).
3. **ТЗ ПОЛНЫ.** У каждого слайда с картинкой поле `🎨 ЧТО НА КАРТИНКЕ` содержит все пять подполей. Напечатать «слайдов с картинкой N · полных ТЗ N · неполных 0».
4. **БЮДЖЕТ ПОСЧИТАН.** Таблица на 15 строк, ни одной пустой клетки; напечатать суммарный дефицит знаков.
5. 🔴 **НИ ОДНОГО ЗНАКА ТЕКСТА СЛАЙДА НЕ ТРОНУТО.** Множество блоков (определение: непустая строка, не заголовок, не фронтматтер, не `> поле:mn`, не содержимое `<figure>`, не `---`) до и после **совпадает побайтово**. Напечатать «блоков 93 · изменённых 0». **Любое изменение — гейт красный, отчёт не принимается.**
6. **СБОРКА.** `build_doc.py` rc=0, `check_view.py` rc=0 — код возврата печатать первым.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «расхождений нет», а «нет, проверено X из 15».

### 🔴 ЧЕТЫРЕ ЛОВУШКИ
1. **АРХЕТИП — ЭТО ПОДСТРОКА, А НЕ НАМЕРЕНИЕ.** *Цена, уже оплаченная: слайд «Как доказывают, что функтора нет» получил формулировку «текстовая область на всю ширину», подстрока `на всю ширину` дала архетип `лестница-во-всю-ширину`, у которого панелей НЕТ ВОВСЕ, — и коммутативная диаграмма не попала на слайд вообще, при том что заход отчитался пунктом ✅. Ловилось только мягким ⚠ линтера.*
2. **НЕ ПИШИ ТЕКСТ.** Ты будешь видеть слайды, где текста явно мало, и рука потянется дописать. **Не тянись** — гейт 5 это ловит, и он завернёт отчёт целиком. Твоё дело — сказать ЧИСЛОМ, сколько дописать, а пишет следующий заход.
3. **НЕ ИЗОБРЕТАЙ ВИЗУАЛЬНЫЙ ЯЗЫК.** Он задан: `SLOVAR-primitivov.md` плюс шесть живых `<figure>` в ленте. Владелец: *«картинки бессмысленно задавать, они уже заданы в нормальном визуале — если там будет так, как там, то будет хорошо»*. Твоё дело — описать СОДЕРЖАНИЕ картинки, а не стиль. Примитива не хватило — **скажи в отчёт**, словарь не правь.
4. **МЕЛКАЯ КАРТИНКА — ДЕФЕКТ, А НЕ КОМПРОМИСС.** *«она должна быть не мелкая, на неё должно быть хорошо видно»*. Правило поля из словаря: `высота/ширина ≥ 0,6 → на поле, иначе во всю строку`; широкая полоса в жёлобе сжимается до нечитаемости. Не влезает — меняй архетип, а не размер.

## 3. ВЕРИФИКАТОР — свежий субагент, ДРУГОЙ метод

Заход не двигает текст, но правит служебные поля, от которых зависит геометрия всего дека, — и промах здесь виден только на собранном слайде. Поэтому верификатор с двумя задачами:

1. **Сохранность текста, программно:** берёт ленту из `git show HEAD:`, режет своим парсером и сверяет множество блоков с рабочим деревом **побайтово**. Печатает «блоков до N · после M · изменённых K». **K обязан быть 0.**
2. **Однозначность ТЗ — читательская проверка, и это главное.** Он **не видел** ни ленты целиком, ни списка слайдов: получает ТОЛЬКО тексты полей `🎨 ЧТО НА КАРТИНКЕ` и по каждому отвечает на один вопрос — **«могу ли я нарисовать эту картинку, не задавая вопросов? если нет, какого именно факта не хватает»**. Это ровно позиция художника, которому дек отдадут. Его список «не хватает» идёт в отчёт ЦЕЛИКОМ, включая пункты, с которыми ты не согласен.

⚠ **Критерий «описание однозначно» не проверяется автором описания ни в каком виде** — автор достраивает недостающее из головы, потому что помнит, что имел в виду. *Цена на этой арке: самопроверка автора ленты по критерию полноты дала «неполных формулировок 0»; свежий читатель, не видевший конспекта, нашёл 26 мест.*

Субагентов ≤5, рейт-лимит = отступить и доложить. Время ожидания мерь `date`, а не суммой `sleep`.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_geometria.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_geometria.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/raskadrovka   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone teorkat-vvedenie/raskadrovka \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Дефолт `archetype_lenty` — «рейка-справа» на ОТСУТСТВУЮЩЕЙ пометке, и покраснеть тут нечему
`porodit.py:448` возвращает рейку последней ветвью `return`, а `parse_section` (строки 282–285) ставит `layout = None` через `if m:` без `else` и без диагностики. Значит «раскладку забыли» и «раскладка = рейка» неотличимы по построению — молча, зелёно, в обе стороны. Это не про порядок подстрок (о нём заход предупреждает ловушкой 1), а про то, что дефолт вообще существует и никем не заявлен.
**ЦЕНА:** на живой ленте записок Л1 из 15 разделов пометки не было у **6** (слайды 1, 2, 6, 8, 11, 14 — замерено на базе захода `64352e5`), и все шесть молча получили рейку. Итоговая доля одного архетипа — **13 из 15 = 87 %** при канонном пороге 67 %: то есть гейт «доля одного архетипа краснеет выше двух третей» (`07-verstka/DOK.md`) был КРАСНЫМ и оставался красным незамеченным, потому что считать его никто не считал, а лента про свои дыры не сообщала. Плюс из тех же пяти дефолтных рейка стояла на слайде 12 под портрет, которого нет в проекте с 29.07 (`porodit.py:314`, портреты сняты решением владельца), — 344px, 24 % площади слайда, зарезервированные молча ни под что. Нашлось только тем, что заход был обязан напечатать распределение ЧИСЛОМ.

### Скрипт-гейт нельзя строить на коде возврата `git diff`: пустой вывод неотличим от «чисто»
`git diff` отдаёт **rc=0** и когда расхождений нет, и когда pathspec не совпал ни с чем. Второе случается само: в zsh неквотированное `$F` по словам НЕ разбивается, и `git diff HEAD -- $F` уходит одним склеенным путём.
**ЦЕНА:** верификатор сохранности текста получил на этом шаге **ложное «расхождений нет»** — то есть гейт, ради которого он и был запущен, подтвердил бы неверное утверждение. Поймал он сам, перепроверив явными путями (расхождение появилось: `43+/9−`, `19+/5−`), и это единственная причина, по которой ложный вердикт не уехал в отчёт. Это второй случай той же самой болезни в одном заходе — первый описан в `CLAUDE.md §5` как «Cowork пишет в bash, владелец исполняет в zsh», и там цена была измерена на командах в чат; здесь она повторилась ВНУТРИ скрипта верификатора. ⇒ Гейт на diff обязан сверять ЧИСЛА (сколько строк изменено, сколько путей совпало), а не читать `rc`; `git diff --numstat` с непустым выводом — проверяемый факт, `rc=0` — нет.

### Литерал HTML-тега в служебной пометке ленты съедает текст РАЗДЕЛА, и `check_view.py` этого не видит
`FIGURE_RE` в `porodit.py` ищет открывающий тег фигуры по всему разделу и тянет `.*?` до первого закрывающего. Тег, набранный внутри `> поле:mn` как пример или ссылка, становится настоящим открывающим: всё между ним и реальной фигурой исчезает из текста слайда.
**ЦЕНА:** оплачено в этом заходе. Одна пометка со ссылкой на тег фигуры обнулила раздел «Переименование ничего не ломает»: **57 знаков → 0, две сцены → одна**. Поймал гейт архетипов, потому что он печатает знаки и сцены по каждому слайду; `check_view.py` дал **rc=0 ЗЕЛЁНЫЙ**, и это не его вина — он смотрит собранный вид, а блок исчезал раньше, на разборе ленты. То есть штатный гейт «текст съеден при отрисовке» этот класс не ловит по построению, и единственная защита сегодня — печатать объём по слайдам и смотреть на нули. ⇒ Дешёвый гейт: `> поле:` не должна содержать `<figure`/`</figure`/`<table`/`</table`; грепом, одной строкой.

### Слепая проверка ТЗ иллюстрации находит НЕ отдельные дыры, а классы фактов, которых автор не задаёт вовсе
Самопроверка автора дала «одиннадцать полных ТЗ, неполных 0». Свежий читатель, видевший ТОЛЬКО тексты ТЗ, дал «могу нарисовать без вопросов 4 из 11».
**ЦЕНА:** **64 дыры** в одиннадцати ТЗ, и они не рассыпаны — три класса покрывают большинство: состояние узла (залит или белый), сторона и место подписи относительно объекта, горизонтальная привязка узкой фигуры в широкой полосе. Автор их не видит по механизму: он помнит картинку и достраивает молча. Дороже всего стоили две конкретные дыры — на слайде рекуррентного соотношения не было названо $k$ (два случая нечем заполнить, картинка не рисуется в принципе), и на главной картинке записок не было решено, один ряд рамок или два и чем изображён прослеженный элемент. ⇒ Практический вывод для арки 9: **три класса фактов вынести в ОДНО поле соглашений на блок**, а не повторять в каждом ТЗ, — повторяя, где-то забудут наверняка; и ставить слепого читателя ОБЯЗАТЕЛЬНЫМ гейтом ТЗ, а не опцией, потому что автор здесь неработоспособен по построению.

### Поле соглашений, написанное как лекарство от дыр в ТЗ, само рождает дыры — и одного прогона проверки мало
Три системных класса дыр я вынес в общее поле «СОГЛАШЕНИЯ» — и это сработало (с 4/11 до 9/11), но САМО поле оказалось внутренне противоречивым, и увидел это только следующий свежий читатель, не автор.
**ЦЕНА:** одно правило («метку стрелки ставить с внешней стороны, у горизонтальной — сверху») противоречило себе на НИЖНЕЙ стороне квадрата, где «внешняя сторона» и «сверху» — два разных места; таких стрелок в одиннадцати ТЗ оказалось **три**. Оно же не покрывало ОДИНОЧНУЮ вертикальную стрелку — она ни левая, ни правая, стороны у неё нет, правило к ней просто неприменимо, а таких стрелок в одном ТЗ оказалось **три**. Оно же ставило метку узла ПОД узлом там, где узлы стоят столбиком, то есть подпись садилась на следующий узел (**два** ТЗ). А другое правило было написано про «панели», тогда как ТЗ пользуются словами «этаж» и «ярус», — распространяется ли оно на них, из текста не следовало (**четыре** ТЗ). Итого **12 адресов, внесённых лекарством от 64 старых дыр**. Третий круг нашёл в переписанных соглашениях **ещё пять дефектов того же рода**: правило места метки не покрывало НАКЛОННЫЕ стрелки вовсе (а это все стороны треугольников — три ТЗ), не определяло область действия «под рамкой» (внутри своей части или через всю фигуру — три ТЗ), не работало на ВЛОЖЕННЫХ рамках, не говорило, что «часть фигуры» — это НЕ строка и НЕ треугольник (четыре ТЗ), и нигде не было сказано, **с какой стороны считаются позиции в ряду** — при том что после второго круга весь слайд рекуррентного соотношения держался на словах «позиция 1». ⇒ Слепой гейт ТЗ **обязан гоняться до сходимости, свежим читателем каждый раз**, а не однократно: правило общего вида проверяется только применением к каждой фигуре, которая ему подчинена, и делать это должен тот, кто фигуру не помнит. Три круга на одиннадцати ТЗ — это ещё не сходимость.

### Число слепого гейта НЕСРАВНИМО между прогонами, если менялся промпт, — и по нему легко отчитаться ложным прогрессом
Я гонял слепого читателя трижды и получил «4 из 11 · 64 дыры» → «9 из 11 · 28 дыр» → «2 из 11 · 41 дыра». Третье число хуже второго не потому, что артефакт испортился: между вторым и третьим прогоном я ДОБАВИЛ в задание требования искать противоречия самих соглашений и проверять сходимость чисел. Планка поднялась, и тот же текст получил другой вердикт.
**ЦЕНА:** здесь цена — не поломка, а близко пройденная возможность соврать в отчёте, и соврать дважды в обе стороны. Отчитайся я после второго прогона — вышло бы «однозначность ТЗ 9 из 11, гейт почти зелёный», и пять дефектов в общих правилах уехали бы художнику. Отчитайся я после третьего, сравнив с первым, — вышло бы «с 4 до 2, стало хуже, работа ухудшила артефакт», и это тоже неправда. Ни один из двух рассказов не выявляется числом: гейт печатает «N из 11» одинаково убедительно при любом промпте. ⇒ У слепого гейта ТЗ **промпт есть часть измерительного прибора и обязан фиксироваться вместе с числом**; сравнивать разрешено только прогоны с ОДИНАКОВЫМ заданием, а при смене задания счёт начинается заново и это говорится вслух. Иначе метрика «однозначность» управляется формулировкой проверки, а не качеством ТЗ.

### Параллельный заход подмёл мой файл в свой коммит — уже после того, как я его тронул
`register_doc.py` вписал строку регистрации в `_studio/docs/KARTA.md` и оставил её незастейдженной, как и положено. К моменту моего коммита файл был **чист**, а строка — уже **в HEAD**: её унёс коммит другого захода (`77790cd`, «разметка корпуса исполнителей по девяти осям»), которому `KARTA.md` не принадлежал.
**ЦЕНА:** здесь обошлось без потери — строка в HEAD, документ не сирота, риск «умрёт при первом `checkout`» снят. Но снят он случайно, чужой рукой: **документ и его строка регистрации уехали в РАЗНЫЕ коммиты**, то есть требование заходов «`KARTA.md` обязан уехать в ТОТ ЖЕ коммит» оказалось неисполнимым не по вине исполнителя — файл забрали до него. И тот же коммит унёс `UROKI-FABRIKE.md` этой арки с чужими девятью строками. Второй наблюдённый эффект того же корня: HEAD сдвинулся под работающим верификатором посреди проверки (`64352e5` → `f11bdf0`), и он уцелел только потому, что сам сверил blob-хэши базы. ⇒ `register_doc.py` мог бы **сам стейджить свою строку** (`git add -- _studio/docs/KARTA.md`): тогда чужому pathspec-коммиту её не подобрать, а инструкция «допиши KARTA в оба хода» станет исполнимой в общем репо, а не только в пустом.

## ПЛАН — (заполняет исполнитель)

**Ветка** `arka/mat-kostyak` ✅ (`git branch --show-current`, rc=0). **Зона `teorkat-vvedenie/raskadrovka` чиста в HEAD** — точку откката не фабрикую. В зоне лежит ЧУЖОЕ изменение: `_studio/zhurnal/2026-07-30_dovodka-l1/UROKI-FABRIKE.md` (+9 строк, не мои) — в свой коммит НЕ беру, см. ОТЧЁТ.

**База замерена до правок** (`/tmp/geom9/geom_bloki.py`): **блоков 93 · изменённых 0 · sha256 множества `52763f09a6847587`** — число из гейта 5 совпало с заходом.

### Три поправки к критерию готовности — говорю ДО работы, как велит §1

1. 🔴 **Гейт 1 требует геометрию на 15 полях, а канон даёт 14.** `07-verstka/DOK.md §13`: обложка — служебный слой, её «не проектируют, не верстают и не правят», генератор строит её из полей `brief.md`. Это подтверждено кодом: `porodit.py:481` — «обложка = первый раздел ленты; в slide_order не входит», и `gejt_lenty.py:123` требует пометку раскладки «кроме обложки». То есть архетип обложки НИКЕМ не читается. **Поправка: геометрию определяю для 14 содержательных слайдов, обложке поля не завожу, и в таблице гейта 1 она стоит строкой «служебный, вне арки 7».** Отрицательный вердикт несёт охват: расхождений 0, проверено 14 из 15, пятнадцатый исключён по канону с доказательством.

2. 🔴 **Измеритель объёма из `porodit.py` на формульных слайдах врёт втрое, и по нему бюджет считать нельзя.** `visible_chars` сам предупреждает: «формула считается своей длиной в TeX (иного объективного измерителя нет)». Живой замер: слайд 15 — 2460 знаков TeX, слайд 13 — 466, слайд 3 — 783; а глифов, которые видит читатель, — 1683, 226 и 490. Разница на слайде 13 — 2,1 раза, и бюджет по знакам TeX завернул бы слайд, у которого на экране полупусто. **Поправка: считаю ДВА числа рядом** — знаки ленты (измеритель `porodit.py`, для трассируемости) и **глифы** (`/tmp/geom9/glify.py`: команды TeX свёрнуты — `\mathbf{Set}`→`Set`, `\binom nk`→`nk`, прочая `\word`→один глиф). **Дефицит считается по глифам.** Эвристика свёртки названа в файле целиком и проверяема.

3. **Вместимость архетипа вывожу из геометрии, а не из вкуса** (потолок 650 знаков на КАДР, `06-tekst/DOK.md`): лестница 1,00 → 650 · доска-пустая 0,92 → 598 · рейка-справа 0,76 → 494 (рейка 344px из 1440, `07-verstka/DOK.md`) · илл-полосой-снизу 0,45 → 293 (владелец: «нужно оставить на текст меньше половины»). **Цель = 0,82 × вместимость**, и 0,82 — тоже не вкус: медиана корпуса 45 слов при потолке 55 слов на кадр (`06-tekst/DOK.md`) и есть 0,82 потолка.

### Что делаю
1. **Геометрия по 14 слайдам** — формулировкой, которая матчится подстрокой (`porodit.py:438`), с прогоном `archetype_lenty` на каждой. Распределение печатаю числом.
2. **ТЗ иллюстраций** — поле `> поле:mn 🎨 **ЧТО НА КАРТИНКЕ.**` с пятью обязательными подполями. Два ТЗ (слайды 5 и 9) — дословной диктовкой владельца.
3. **`GEOMETRIYA-I-BYUDZHET.md`** — таблица на 15 строк, регистрирую через `register_doc.py` тем же ходом.
4. **Гейты 1–6**, свежий верификатор двумя задачами (сохранность текста программно + читательская проверка ТЗ вслепую), коммит своей зоны.

**Ни одного знака текста слайда не пишу.** Правлю только поля `> поле:mn`.

## ВОПРОСЫ — (заполняет исполнитель)

Шесть, по убыванию цены. Первые три упираются в решение владельца, и обойти их геометрией нельзя.

### 1. 🔴 Слайд 15 (справочник) — 1683 глифа при вместимости 494, превышение в 3,4 раза
Слайд склеен из четырёх прежних, и потолок кадра он нарушает не текстом, а самим фактом склейки. Развилка, и выбрать за владельца нельзя:
- **либо справочник — явное исключение из потолка.** Основание есть в самом списке: он «не произносится вслух, лежит для тех, кому интересно», то есть читается глазами постфактум, как таблица, а не как кадр. Тогда потолок к нему просто не применяется, и это надо записать, а не подразумевать;
- **либо слайд делится** — и это сигнал наверх в раскадровку (`06-tekst §Гейт, пункт 8`), потому что делить слайды моему заходу запрещено.

### 2. 🔴 Слайд 7 несёт три визуальных сюжета, а в экран при читаемом размере входят два
(а) шесть биекций между двумя множествами — ТЗ выдано · (б) действие вида вдоль биекции — заказ владельца, «надо придумать, как эта идея реализуется; но это пока нам не так важно» · (в) **пять коммутативных диаграмм «почему это вид»**, про которые владелец сказал прямо, что это *«именно картинки»*, а не текст.
Третий сюжет в полосу не входит: пять диаграмм в 344px — это по 60px на диаграмму, то есть ловушка мелкой картинки в чистом виде. Либо (в) получает свой слайд, либо снимается. **Своей волей не снял и место под него не занял.**

### 3. Слайд 5 — `SPISOK-SLAJDOV.md` и прямая диктовка владельца описывают этажи в ОБРАТНОМ порядке
Список: *«Картинка: двухэтажная — внизу переименование, наверху что стало с конструкцией»*. Диктовка владельца в заходе: *«сверху показывается, что происходит, когда ты переименовываешь… А снизу показывается, как это переименование влияет на двухэлементные подмножества»*. **Взял порядок диктовки** (сверху переименование), потому что заход велит брать её дословно и не пересказывать. Если верен список — переворачивается один абзац ТЗ, но сказать об этом надо сейчас, а не после того, как художник нарисует.

### 4. Слайд 6 получил картинку моим решением, а не решением владельца
`SPISOK-SLAJDOV.md` слайду 6 картинку не заказывал: там только «Определение: целиком. Сцен: 1». Я её завёл, и основание одно — принцип захода шага 4: *«все математические вещи должны быть объяснены с помощью формул плюс картинок, то есть без слов»*. Определение вида ЕСТЬ уважение композиции, и словами оно не показывается. **Помечено в самом ТЗ как решение исполнителя; приёмка вправе снять одной строкой.**

### 5. Строка-плейсхолдер портрета осталась в теле слайда 12
`🖼 Портрет Андре Жуайаля {1}` стоит в разделе, а портреты сняты решением владельца 29.07 и `porodit.py:314` их не порождает. Полосу под портрет я снял (это раскладка, моя зона), а **строку не тронул: её снятие есть правка текста слайда**, что этому заходу запрещено гейтом 5. Снимать её — следующему заходу.

### 6. Слайд 14: геометрия назначена текстовой, потому что содержания нет
Владелец: *«после того как понятие категория названа, нужно показать какие-то свойства»* — какие, не сказал. Пока не сказал, неизвестен и род картинки. Поставил `доска-пустая` (текстовый слайд), а не «рейка под неизвестную картинку»: резервировать 24 % площади неизвестно под что дороже, чем переназначить архетип потом. **Число дефицита +490 в таблице помечено как недействующее** — это не заказ на 490 знаков, это отсутствие решения.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и ЗАЧЕМ

**Два выхода, как заказано.**

1. **Геометрия и ТЗ — в самой ленте, полями `> поле:mn`.** 14 содержательным слайдам проставлена раскладка формулировкой, которая матчится подстрокой; 11 слайдам с картинкой выданы ТЗ полем `🎨 ЧТО НА КАРТИНКЕ` с пятью подполями. Диффа по тексту нет ни на байт: `A-krasivaya.md` 45+/9−, `B-yazyk.md` 21+/5−, и все 76 изменённых строк — либо `> поле:`, либо пустые (проверено верификатором независимо).
2. **`GEOMETRIYA-I-BYUDZHET.md`** — таблица на 15 строк, зарегистрирован через `register_doc.py` (rc=0).
3. **`byudzhet_l1.py`** рядом с документом — не таблица руками, а команда, которая её печатает (`KONSTITUCIYA §10`). Один прогон даёт все пять гейтов и весь бюджет: `python3 _studio/zhurnal/2026-07-30_dovodka-l1/byudzhet_l1.py`.

**Главное решение по геометрии, и зачем оно.** Лента давала `рейка-справа` **13 слайдам из 15 = 87 %**, то есть гейт канона «доля одного архетипа краснеет выше двух третей» был красным, и красным молча — потому что `archetype_lenty` возвращает рейку ДЕФОЛТОМ, когда поля раскладки нет вовсе (`porodit.py:448`, ветки `else` там нет). Стало: рейка 50 % · илл-полосой-снизу 29 % · доска-пустая 14 % · лестница 7 %. Четыре слайда переведены в нижнюю полосу не для разнообразия, а по прямому правилу владельца «когда мало текста — хорошо помещать вниз картинку» и по правилу поля из словаря: у слайдов 5 и 9 картинки широкие, и в жёлобе 344px они нечитаемы.

**Главное решение по бюджету, и зачем оно.** Штатный измеритель `porodit.visible_chars` считает формулу длиной её TeX-разметки и предупреждает об этом сам. На слайде 13 это даёт 466 «знаков» при 226 глифах на экране — двукратное вранье, и бюджет по такому числу завернул бы слайд с полупустым экраном. Поэтому считаю ДВА числа рядом и дефицит вывожу по глифам; свёртка TeX названа целиком в `byudzhet_l1.py` и оспорима построчно.

### Как проверил

| гейт | результат |
|---|---|
| 1 · архетип фактический (`archetype_lenty` на каждом поле) | **расхождений 0, проверено 14 из 15**; слайд 1 исключён по канону с доказательством из кода |
| 2 · распределение | ✅ **максимум 50 % при пороге 67 %** (было 87 % — красный) |
| 3 · полнота ТЗ (структурная) | **слайдов с картинкой 11 · полных ТЗ 10 · ЗАКАЗ 1 · неполных 0.** ⚠ Это гейт на ПЯТЬ ПОДПОЛЕЙ, а не на однозначность: гейт однозначности — слепой читатель, и он **НЕ СОШЁЛСЯ**, последнее измеренное 5 из 11 при 43 дырах |
| 4 · бюджет | таблица на 15 строк, пустых клеток 0. **Дописать +1767 глифов, из них действующих +1038** · **срезать −1402, из них 1278 — один слайд 15** |
| 5 · текст слайда не тронут | **блоков 93 · изменённых 0**, sha256 множества `52763f09a6847587` до и после |
| 6 · сборка | `build_doc.py` **rc=0** · `check_view.py` **rc=0** · `check_termin.py` **rc=0** (коды печатались первыми) |

Сверх гейтов проверил математику всех числовых ТЗ своими руками: $\binom72=\binom75=21$ · $\binom42=\binom31+\binom32=6$ · $\binom52=1+6+3=10$ · и квадрат слайда 9 при $n=2$ прогнан перебором: на $E^{+}$ действие $\tau$ тождественно, на $E^{-}$ — перестановка, оба пути дают $\varphi_U(\{1\})=\varphi_U(\{2\})$, то есть противоречие ровно там, где ТЗ ставит знак $?$.

🔴 **Гейт поймал МОЮ ошибку, и она того класса, который канон уже знает.** В поле слайда 5 я написал литерал открывающего тега фигуры. `FIGURE_RE` в `porodit.py` ищет его по всему разделу и тянет `.*?` до первого закрывающего — и съел текстовый блок раздела ВМЕСТЕ с настоящей фигурой: раздел показал 0 знаков вместо 57 и 1 сцену вместо 2. Поймано прогоном гейта 1, а не глазами; `check_view.py` этого не видел (он смотрит собранный вид, а блок исчезал ещё на разборе ленты). Починено, и запрет вписан в само поле, чтобы не повторили. Проверено грепом: тегов в полях теперь 0.

### Результат верификаторов — два, разными методами

**Верификатор 1 — сохранность текста, программно (свой парсер, `git show HEAD:`).** Вердикт: **ПОДТВЕРЖДАЮ.** Блоков до 93 · после 93 · изменённых 0, сверено и как мультимножество, и как последовательность. Вторым независимым способом (`git diff -U0`): 76 изменённых строк, из них `> поле:` — 52, пустых — 24, **являющихся блоком — 0**. Оба способа сошлись. Охват назван: A — 251→285 строк, блоками признано 66 и 66; B — 92→106, блоками 27 и 27; файлы выросли с 26 КБ до 58 КБ и с 16 КБ до 24 КБ, и текстовая часть при этом побайтово та же.

Он же нашёл сверх задания три вещи, все стоят внимания:
- **`porodit.py:282-285` молчит на отсутствующей раскладке** — `if m:` без `else`. Для обложки это, возможно, задумано, но молчит одинаково и для обложки, и для забытого раздела: покраснеть тут нечему. Это ровно тот механизм, из-за которого лента годами держала 87 % рейки.
- **HEAD сдвинулся у него под руками посреди проверки** (`64352e5` → `f11bdf0`): параллельный агент коммитил во время верификации. Он проверил blob-хэши обоих файлов в HEAD, они не изменились, база сравнения та же — но факт назвал. Репо сейчас движется под работающими заходами.
- 🔴 **Он сам получил на один шаг ложное «расхождений нет» из-за zsh.** Собрал пути в переменную и написал `git diff HEAD -- $F`; в zsh неквотированное расширение параметра по словам НЕ разбивается, git получил один склеенный pathspec, не совпал ни с чем и вернул **пустой diff при rc=0**. И отдельно: **пустой `git diff` неотличим от «чисто» по коду возврата** — гейт, построенный на `rc` от `git diff`, будет врать молча. Поймал сам, перепроверил явными путями.

**Верификатор 2 — однозначность ТЗ, слепая читательская проверка** (видел ТОЛЬКО тексты полей `🎨`, без ленты, без списка слайдов, без номеров слайдов). Первый прогон: **«могу без вопросов 4 из 11, дыр 64»** — то есть по главной метрике захода гейт был КРАСНЫМ, и красным у автора, который считал ТЗ полными. Ровно предсказанная §3 цена самопроверки.

Его системный диагноз, и он оказался точнее моей построчной правки: ТЗ не задавали не «то и это», а **три класса фактов сразу** — состояние узла (залит/белый), сторону и место подписи относительно объекта, и горизонтальную привязку узкой фигуры в широкой полосе. Дороже всего стоили две дыры: на слайде 10 не было названо $k$ (два случая рекуррентности нечем заполнить), на слайде 9 не было решено, один ряд рамок или два и чем изображён прослеженный элемент со знаком `?`.

**Что я с этим сделал.** Три системных класса вынес в отдельное поле `📐 СОГЛАШЕНИЯ` — десять пунктов, по одной копии в начале каждого блока ленты (блок обязан быть самодостаточен: рисующий открывает свой слайд, а не всю ленту). Остальное починил построчно: зафиксировал $n=4$, $k=2$ на слайде 10 и развёл два независимых ремонта пунктирной линией и разной мощностью рамок (пятёрка вместо четвёрки — иначе «пример на 4 узлах» читается как противоречие с «выброшены носители мощности 4», и верификатор это поймал); на слайде 9 расписал оба яруса панели А, переупорядочил четыре рамки так, чтобы прямые стрелки стояли рядом и скрещенные рядом, назвал все четыре угла квадрата и приписки у каждого, и поставил `?` в конкретный угол; на слайде 5 назвал сокращение в диктовке владельца сокращением ($a,b,c$ против семи узлов) и залил узлы $a,b$ в обеих рамках, чтобы этажи были связаны; на слайде 7 заменил шесть дуг шестью прямыми стрелками на шести высотах; на слайде 11 снял цвет из графики вовсе — «два цвета» несут подпись и положение.

**Второй прогон, свежий слепой читатель, исправленные ТЗ плюс поле соглашений: «могу без вопросов 9 из 11, дыр 28».** С 4/11 и 64 дыр. Оба оставшихся «не могу» он назвал точно, и один из них — мой:
- **ТЗ слайда 10 не рисовалось вовсе:** в четырёх местах сказано «залит один», «залиты два другие» — и НИ РАЗУ не названа позиция. А от позиции $\ast$ зависит весь разбор случаев, то есть картинка не собирается ни в каком варианте. Мою правку первого круга («зафиксировал $n=4$, $k=2$») он принял, а вот что числа без позиций бесполезны — увидел только он;
- **ТЗ слайда 2** — «не могу» по решению владельца: задачи нет, объектов нет. Законно.

И главное, чего я не ожидал: **он нашёл, что поле соглашений противоречит САМО СЕБЕ.** Пункт 2 требовал ставить метку стрелки «с внешней стороны», а в скобках уточнял «у горизонтальной — сверху»: для НИЖНЕЙ стороны квадрата это два разных места, и таких стрелок в ТЗ три ($E_5[\sigma]$, $G[\sigma]$, нижняя $\varphi_U$). Плюс пункт 2 не покрывал одиночную вертикальную стрелку (ни левая, ни правая — стороны нет) и ставил метку узла под узлом там, где узлы стоят СТОЛБИКОМ, то есть подпись садилась на следующий узел. Плюс пункт 8 говорил «панели», а ТЗ пользуются словами «этаж» и «ярус», и распространяется ли на них правило разделителя — было неизвестно. То есть лекарство от трёх системных дыр само породило четыре новых, и поймал их не автор.

**Что сделал по второму кругу.** Позиции заливки на слайде 10 названы все четыре (ярус А1 — позиция 1; панель Б — верхняя рамка позиция 1, левый случай позиции 1 и 2, правый случай позиции 2 и 3 при обведённой пунктиром позиции 1; арифметика $\binom42=\binom31+\binom32$ сходится). Соглашения переписаны с 10 пунктов на 13: место метки расписано по каждому типу стрелки отдельно (верхняя горизонтальная сверху, НИЖНЯЯ горизонтальная СНИЗУ, одиночная вертикальная справа), узлы столбиком подписываются СЛЕВА, «панель/этаж/ярус» объявлены одним и тем же, добавлены «поле и полоса снизу — разные места, слова не смешивать», «части рядом выравниваются по верху», «цифра под своим узлом», «у петли наконечник по часовой», и вершина диаграммы объявлена выписанным именем для ЛЮБОЙ фигуры, а не только для коммутативного квадрата. Ссылка на соглашения дописана во все 11 ТЗ (в трёх её не было).

**Третий прогон, снова свежий читатель: «могу без вопросов 2 из 11, дыр 41».** Число ХУЖЕ второго, и я обязан сказать, почему, а не выдать это за регресс артефакта: **инструмент был другой.** Третьему читателю я добавил в задание два требования, которых у второго не было — искать противоречия САМИХ СОГЛАШЕНИЙ и проверять сходимость чисел в ТЗ. Планка поднялась, и он завернул ТЗ там, где второй считал факт покрытым. То есть сравнивать 9/11 и 2/11 нельзя; сравним будет только следующий прогон тем же промптом.

Что он нашёл по существу, и это стоило того:
- **Пять дефектов в самих соглашениях, каждый бьёт по нескольким ТЗ.** Правило места метки не покрывало НАКЛОННЫЕ стрелки вовсе (стороны треугольников в двух ТЗ, диагональные стрелки в третьем) — «снаружи» задаёт полуплоскость, а не сторону. Правило не определяло ОБЛАСТЬ ДЕЙСТВИЯ «под рамкой»: внутри своей части фигуры или через всю фигуру — от прочтения менялось место метки в трёх ТЗ. Правило не работало на ВЛОЖЕННЫХ рамках (под каждой стоит следующая, значит все три метки уходят влево, а уровни не заданы). Правило «часть фигуры» перечисляло панель/этаж/ярус, но не говорило, что строкой, рамкой и треугольником оно НЕ распространяется — и было неизвестно, ставить ли обязательный пунктирный разделитель между строками. И нигде не было сказано, **с какой стороны считаются позиции в ряду** — при том что после второго круга весь слайд 10 держится на словах «позиция 1».
- **Одно место, где стрелка обязана пробить границу рамки:** «от нижнего края верхней рамки к ЦЕНТРУ нижнего ряда» — центр лежит внутри рамки.
- **Самая дорогая его находка на слайде 10 у меня уже была исправлена**, и это независимое подтверждение, а не догон: метка $\ast$ была привязана к «залитому узлу» во всех трёх рамках, а в правом случае носитель $\ast$ специально не залит. Я нашёл это сам, перечитав ТЗ, — но выгрузка для третьего прогона была сделана ДО правки, и он нашёл то же самое своим путём.
- **Вся остальная математика сошлась** у него независимо: $7=2+5$ · заливки 2,3 → цифры 0,1,1,0 · вложенность 1–2/1–4/1–6 → 2,2,1,1,0,0 · оба пути квадрата слайда 5 дают одно множество · $3!=6$ · чётные/нечётные и склейка $\varphi_U(\{1\})=\varphi_U(\{2\})$ в знак $?$ · $6=3+3$ · $10=1+6+3$.

**Что сделал по третьему кругу.** Соглашения переписаны с 13 пунктов на 14 и по-другому устроены: место метки расписано по ТИПУ объекта с наклонной стрелкой отдельно, область действия правила ограничена «внутри своей части фигуры», вложенные рамки получили своё правило, добавлены нумерация позиций (в ряду слева направо, в столбике сверху вниз), классы линий выписаны прямо в поле (чтобы рисующему не приходилось открывать словарь), пропорция определена как высота/ширина, «часть фигуры» ограничена ровно панелью/этажом/ярусом, и добавлено правило «стрелка цепляется к КРАЮ объекта и границу рамки не пробивает». Все отступления в ТЗ теперь помечены словами «названное исключение» — их семь, каждое с причиной. Плюс восемнадцать точечных правок в ТЗ семи слайдов.

**Четвёртый прогон, ТЕМ ЖЕ промптом, что третий: «могу без вопросов 5 из 11, дыр 43».** Это единственная сравнимая пара чисел за весь заход: **2 из 11 → 5 из 11 при одинаковом инструменте.** Дыр стало на две больше (41 → 43), и причина названа честно: соглашения выросли с 13 пунктов до 14 и дали больше поверхности для придирки — каждое новое общее правило само становится предметом проверки.

Что он нашёл, и что я успел починить после него:
- 🔴 **Четыре ссылки на номера соглашений в ТЗ были НЕВЕРНЫ** — объективно моя ошибка, и ровно того класса, который заход запрещает: ТЗ ссылалось на «соглашение 7» вместо 12 (углы квадрата) в двух местах, на «соглашение 6» вместо 11 (вершина есть имя) в двух, на «соглашение 11» вместо 6 (поле 344px), и на «соглашение 5» там, где снимаются 7 и 8. Исправлены все шесть адресов.
- 🔴 **Самая дорогая находка, и она содержательная: на слайде 10 заливка означала ДВА РАЗНЫХ вещи, и из-за этого разбор случаев не сходился с $\binom42=3+3$.** В верхней рамке панели Б был залит один узел при метке $\binom42$ — то есть картинка «показывала» выбор двух элементов одним залитым узлом, и заранее решала тот самый признак, по которому идёт разбор. Починено сменой языка фигуры: **залит = ВЫБРАННЫЙ элемент, обведён пунктирным кружком = отмеченная точка $\ast$**, состояния независимы и могут стоять на одном узле вместе. Теперь верхняя рамка не выбирает ничего (носитель с $\ast$), левый случай — $\ast$ обведён И залит, правый — обведён и не залит. Смысл заливки стал одним на всю фигуру, и арифметика с ним сходится.
- **Конец стрелки в панели 1 слайда 4 был неопределим:** «к верхнему краю нижнего РЯДА», а ряд лежит внутри рамки, то есть стрелка обязана пробить границу, что запрещает соглашение 10. Переписано на «к верхнему краю нижней РАМКИ».
- **Три отдельные фигуры слайда 4 несли одну пропорцию** при правиле «пропорция считается на фигуру». Разрешено явно: у всех трёх один и тот же размер `viewBox`, отсюда общая пропорция, а ШАГ ряда подбирается под число узлов — прежняя формулировка «одинаковый шаг» была невыполнима, потому что ряды из 7, 4 и 6 узлов при равном шаге дают разную ширину.
- **Контур «штанов» не восстанавливался из слова «штаны»** — расписан по линиям: четыре образующие, где именно стоит развилка, куда упираются крайние линии, чем вытянуты овалы.
- **Ещё три типа объектов, которых соглашения не знали:** метка ГРУППЫ узлов, петля (направление обхода и куда наконечник), цифра под РАМКОЙ. Все три закрыты названными исключениями в самих ТЗ; исключений теперь семь, каждое с причиной.
- **Вся математика сошлась у него независимо в семи ТЗ из семи, где есть числа,** кроме того одного места на слайде 10, которое я и починил.

🔴 **ГЕЙТ ОДНОЗНАЧНОСТИ ТЗ НЕ СОШЁЛСЯ, и я говорю это числом, а не словом «почти».** Последнее ИЗМЕРЕННОЕ значение — **5 из 11 при 43 дырах**, и оно снято ДО последнего круга правок. Правки после него (шесть неверных ссылок, смысл заливки на слайде 10, конец стрелки, масштаб трёх фигур, контур «штанов», три названных исключения) НЕ ПРОВЕРЕНЫ слепым читателем: **бюджет субагентов захода — ≤5, и он исчерпан ровно на пяти** (четыре слепых читателя плюс один программный верификатор). Пятого слепого прогона я не запускал, а не «он показал улучшение».

Что это значит для приёмки практически: ТЗ сегодня заметно однозначнее, чем были (по сравнимому инструменту 2→5 из 11, и все шесть найденных содержательных ошибок закрыты), но **отдавать их художнику как готовые нельзя** — нужен ещё один слепой прогон тем же промптом, и он покажет остаток. Список всех 43 дыр четвёртого прогона и всех 41 третьего у меня есть целиком; в отчёт я вынес те, по которым действовал, а не сокращал.

### Что НЕ трогал

- **Ни одного знака текста слайда** — гейт 5 это доказывает побайтово, дважды и двумя парсерами.
- **Ни одной `<figure>`** — 6 фигур в HEAD, 6 в дереве, ни одного изменённого байта внутри. Замену `class="mn"` на слайде 5 (архетип сменился, картинка уходит в нижнюю полосу) я НЕ сделал: это правка внутри фигуры, её делает тот заход, который рисует. Названо прямо в поле слайда.
- **Строку `🖼 Портрет Андре Жуайаля {1}`** в слайде 12 — портреты сняты владельцем 29.07 и `porodit.py:314` их не порождает, но снятие строки есть правка текста. Полосу под портрет снял (это раскладка), строку оставил.
- **`SLOVAR-primitivov.md`** — примитивов хватило на все одиннадцать ТЗ, ни одного нового не понадобилось (узел, рамка-клетка, стрелка, дуга, пунктир, метка). Словарь не правил.
- Ничего вне зоны. `_studio/docs/KARTA.md` — только через `register_doc.py`, руками не касался.

### Вопросы

Шесть, в секции `## ВОПРОСЫ`. Первые три блокируют следующий заход: объём справочника (слайд 15, превышение ×3,4), третий визуальный сюжет слайда 7 (в экран не входит), и обратный порядок этажей слайда 5 между списком и диктовкой владельца.

### Открытое «возвращаться»

1. **`porodit.archetype()` не имеет симметричной поправки для «илл-полосой-снизу» без фигур.** У слайдов 2 и 9 фигур пока нет, и до появления картинки нижняя полоса отрисуется пустой. Для обоих это скорее правильно (видимый заказ), но механизм назван, чтобы его не приняли за дефект вёрстки.
2. **Четыре слайда (6, 7, 8, 11) до появления картинок отрисуются как `доска-пустая`, а не `рейка-справа`** — поправка `archetype()` строки 460. Это не расхождение, а честное состояние: геометрия рейки включается вместе с картинкой. Напечатано отдельным блоком в гейте 2.
3. **Слайд 13 несёт в тексте определение функтора, которое ред. 7 списка перенесла на слайд 11.** После переноса дефицит слайда 13 вырастет с +179 до ≈+280. Текст не мой — число назвал.
4. **Слайд 15 несёт вторую фигуру (квадрат естественности)** при блоках, которые список из финала снял. ТЗ на неё не выдал: перерисовывать имеет смысл то, что остаётся.
5. 🔴 **ГЛАВНОЕ ОТКРЫТОЕ: слепой гейт однозначности ТЗ не сошёлся, нужен пятый прогон.** Последнее измеренное — 5 из 11 при 43 дырах, снято ДО последнего круга правок; бюджет субагентов (≤5) исчерпан. Промпт для прогона зафиксирован и обязан быть ТЕМ ЖЕ, иначе число несравнимо (см. `## УРОКИ ФАБРИКЕ`). Выгрузка ТЗ для слепого читателя делается одной командой: полем `🎨` из ленты плюс поле `📐` — и без заголовков и номеров слайдов, иначе читатель перестаёт быть слепым.
6. **Соглашения выросли до 14 пунктов и сами стали предметом проверки.** Каждое новое общее правило добавляет поверхность для придирки: дыр 41 → 43 при том, что «могу» вырос с 2 до 5. Устойчивый выход, вероятно, не в наращивании правил, а в переносе их в `SLOVAR-primitivov.md` как канона рисунка, — но словарь этому заходу READ-ONLY, и правила пока живут полем в ленте.

### Необратимое — отдельным списком

**Необратимого нет.** Ни одного удаления, перезаписи, переименования, перемещения; ни `reset`, ни `checkout`, ни одной правки за пределами зоны. Все правки в ленте — вставки и замены внутри строк `> поле:mn`, откатываются из HEAD одной командой. Три поля слайда 15 переименованы из `**Раскладка.**` в `**Раскладка склеенного слайда (история 29.07).**` — текст полей сохранён дословно, ничего не удалено. `teksty/view.html` — порождаемый файл, пересобирается `build_doc.py` из ленты.

**Время прогона + токены** — снимает приёмка из `result`-строки лога прогона.

**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone teorkat-vvedenie/raskadrovka` → ✅
