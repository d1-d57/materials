# Канал исполнителя — geometria (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — выбор геометрии под содержание слайда и написание ТЗ иллюстрации есть суждение; расписать его заранее нельзя.
> ⏱ **Спешки нет. Метрика — ОДНОЗНАЧНОСТЬ ТЗ и ЧЕСТНОСТЬ БЮДЖЕТА. Часы — норма.**

КОНТЕКСТ. Проект `teorkat-vvedenie/` — **записки Л1**: слайд-конспект прочитанной половины лекции, который выкладывают в интернет постфактум, чтобы люди прочитали и разобрались. Лектора нет, бюджета времени нет.

Лента уже переложена в **15 разделов** по утверждённому `SPISOK-SLAJDOV.md` (заход `kod_sborka.md`, принят 30.07): текст перенесён дословно, пустые слайды заведены с заказом. **Записок при этом ещё нет**, и главный дефицит — не текст, а ВИЗУАЛ: `<figure>` в ленте **6 штук на 15 разделов**, и **девять разделов не имеют ни одной**, включая все четыре, где владелец просил рисовать.

🔴 **Контекст, который меняет критерий для картинок целиком: их будет ПЕРЕРИСОВЫВАТЬ ХУДОЖНИК.** Владелец: *«меня сейчас просят выдать слайды, чтобы художник мог перерисовать картинки более качественно и профессионально. Наша задача — сделать такие слайды, чтобы было понятно, что происходит»*. Значит иллюстрация здесь — **не финальный артефакт, а ТЕХЗАДАНИЕ**, и мерится не красотой, а однозначностью: другой человек по описанию нарисует то же самое.

ЦЕЛЬ: **для каждого из 15 слайдов определить ГЕОМЕТРИЮ, посчитать БЮДЖЕТ текста и визуала и выдать ТЗ ИЛЛЮСТРАЦИЙ.** Порядок именно такой и он продиктован владельцем: *«определить для каждого слайда геометрию, определить, какой там должен быть объём текста и какой объём визуала, и в зависимости от этого понять, сколько текста дописывать»*.

🔴 **СТОП ДО ЦЕЛИ, простыми словами: ты НЕ ПИШЕШЬ НИ ОДНОГО ЗНАКА ТЕКСТА СЛАЙДА.** Ни строки в блоках, ни формулировки, ни связки. Ты правишь ТОЛЬКО служебные поля `> поле:mn` (раскладка и ТЗ картинки) и заводишь отдельный документ с бюджетом. Текст пишется СЛЕДУЮЩИМ заходом, по твоим числам, и владелец увидит его порциями.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_geometria.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_geometria.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- ПЛАН — в `## ПЛАН` перед действиями.

**Прочитать ТОЛЬКО это.** Адреса прогнаны через `test -f` при сборке захода 30.07.

| # | файл | зачем | режим |
|---|---|---|---|
| 1 | `_studio/zhurnal/2026-07-30_dovodka-l1/SPISOK-SLAJDOV.md` | 🔴 **читается ПЕРВЫМ.** Что на каждом слайде: мысль, что на экране, откуда текст. Плюс «Философия записок» — она объясняет, что несут картинки | чтение |
| 2 | `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` | лента, слайды 1–12 | **правишь ТОЛЬКО поля `> поле:mn`** |
| 3 | `teorkat-vvedenie/raskadrovka/teksty/B-yazyk.md` | лента, слайды 13–15 | то же |
| 4 | `_studio/konvejer/07-verstka/DOK.md` | канон вёрстки: архетипы, доли ширины, правило «доля одного архетипа краснеет выше двух третей» | чтение |
| 5 | `_studio/konvejer/09-illustracii/SLOVAR-primitivov.md` | 🔴 **словарь примитивов — визуальный язык ЗАДАН, изобретать его нельзя.** Плюс правило поля: `высота/ширина ≥ 0,6 → на поле, иначе во всю строку` | чтение |
| 6 | `_studio/konvejer/06-tekst/DOK.md` | потолки текста: 650 знаков и 55 слов **на КАДР**, медиана корпуса ~45 слов | чтение |
| 7 | `teorkat-vvedenie/src/tools/porodit.py`, строки 436–470 | 🔴 **как архетип выбирается ФАКТИЧЕСКИ** — см. ловушку 1 | чтение |

Не открывать: `KONSPEKT-L1/` · `C-*.md`, `D-*.md`, `E-*.md` · `src/` кроме названных строк `porodit.py`.

🔴 **АРХЕТИП ВЫБИРАЕТСЯ ПОДСТРОКОЙ В ПОЛЕ «Раскладка», а не твоим намерением.** `porodit.py:438`:
```
"на всю ширину"                          → лестница-во-всю-ширину  (панелей НЕТ вовсе)
"крупная горизонтальная иллюстрация снизу" → илл-полосой-снизу
"полоса пустая" / "полоса справа пустая"   → доска-пустая  (узкое поле, текст на всю ширину)
иначе                                     → рейка-справа  (вертикальная полоса 344px)
```
Плюс поправка `archetype()`: «рейка-справа» без фигур и портретов молча становится «доска-пустая».
**Это значит: ты не «выбираешь вёрстку», ты ПИШЕШЬ ФОРМУЛИРОВКУ, которая матчится.** Прогони `archetype_lenty` на своей формулировке и напечатай результат — иначе получишь не тот архетип и не заметишь.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Два выхода: **правленые поля `> поле:mn` в ленте** и **один новый файл** `_studio/zhurnal/2026-07-30_dovodka-l1/GEOMETRIYA-I-BYUDZHET.md`.

### Шаг 1. Геометрия — по слайду
Для каждого из 15 разделов выбрать архетип **под содержание, а не под привычку**, и записать формулировкой, которая матчится (ловушка 1). Ориентиры владельца:
- *«бывают вёрстки, когда у нас вертикальная полоса, бывает — когда горизонтальная. Здесь у нас появится какое-то количество таких горизонтальных слайдов, на которых текста меньше, картинок больше»*;
- *«когда мало текста — хорошо помещать вниз картинку»*;
- *«если это вертикальная полоса, там не так много поместится, зато может поместиться НЕСКОЛЬКО диаграмм, и на них должно быть не очень много деталей. Снизу может поместиться очень большая картинка, но для этого нужно оставить на текст меньше половины»*.
⚠ **Доля одного архетипа краснеет выше двух третей** (`07-verstka/DOK.md`). Напечатай распределение числом.

### Шаг 2. Бюджет — числами, по слайду
Таблица: **знаков текста сейчас · сколько влезает при выбранном архетипе · дефицит или профицит**. Потолок — 650 знаков и 55 слов **на КАДР** (не на слайд: слайд со сценами вмещает больше). Для архетипов с картинкой доля текста меньше: у «илл-полосой-снизу» — **меньше половины площади**, это прямое слово владельца.
🔴 **Правило владельца про недобор, вписать в таблицу отдельной колонкой «цель»:** *«меньше нормы лучше, чем больше нормы, хотя в идеале подобраться к норме. Норма определена стилистикой окружающей, помноженной на объём места, которое есть на слайде»*. Значит целевое число — не потолок, а норма минус запас; называй его явно.

### Шаг 3. ТЗ иллюстраций — главный выход захода
Для каждого слайда, которому нужна картинка, завести в ленте поле:
```
> поле:mn 🎨 **ЧТО НА КАРТИНКЕ.** ИЗОБРАЖЕНО: <объекты и их число> · ПОДПИСИ: <что подписано буквами> · ДЕЙСТВИЕ: <что происходит, какая стрелка куда> · НЕ РИСОВАТЬ: <чего быть не должно> · РАЗМЕР: <на поле / во всю строку, и почему>
```
Все пять полей обязательны. **Критерий: другой человек по описанию нарисует то же самое.** Картинки будут перерисованы художником — ты пишешь ТЗ, а не эскиз.

🔴 **ДВА ТЗ ВЛАДЕЛЕЦ ПРОДИКТОВАЛ САМ. Взять дословно, не пересказывать.**

**Переименование (слайд «Переименование ничего не ломает»):** *«сверху показывается, что происходит, когда ты переименовываешь $a,b,c$ на $a',b',c'$. А снизу показывается, как это переименование влияет на двухэлементные подмножества семиэлементного множества, и на пятиэлементные, и как переход к дополнению это сохраняет. То есть коммутативная диаграмма — на примере конкретного двухэлементного, семиэлементного»*.

**Изоморфизма нет (слайд «изоморфизма нет» — по владельцу самое сложное место записок):** *«предположим, что был бы такой изоморфизм. Посмотрим, как он действует на подмножества множества из двух элементов. Слева — перестановка, которая элементы меняет местами; справа — оставляет на месте. И получается коммутативная диаграмма, и там просто непонятно, куда переводить конкретный элемент: если он переходит куда-то, то с другой стороны он должен переходить в другое значение. Финальная существенная часть — коммутативная диаграмма с ОТДЕЛЬНЫМ описанием того, как действует перестановка»*.

**Три места, где владелец сказал, что содержание — ИМЕННО КАРТИНКИ, а не текст:**
- *«разбирать, почему это вид, у пяти видов скорее всего не будем — это именно картинки. Мы строим коммутативные диаграммы, где слева-справа что-то действует»*;
- *«общий аргумент про чётные и нечётные — это чисто картинки; можно тоже в нескольких фразах буквально сказать опорные идеи, но в основном это визуал»*;
- *«диагноз про Вандермонда и Паскаля — очень коротко, там можно что-то сказать»*.
⇒ У этих слайдов бюджет текста **малый по замыслу**, и это не дефицит, а решение. Отметь.

### Шаг 4. Общий принцип, который держит весь заход
Владелец: *«все математические вещи должны быть объяснены с помощью формул плюс картинок, то есть без слов, в основном — но при этом полностью объяснены, чтобы можно было понять, о чём речь: какие отображения, как они строятся»*. **Формула и картинка несут объяснение; текст только называет, что происходит.**

### 🔴 КРИТЕРИЙ ГОТОВНОСТИ — шесть гейтов, все могут ПРОВАЛИТЬСЯ
1. **АРХЕТИП ФАКТИЧЕСКИЙ.** Прогнать `archetype_lenty` (импортом из `porodit.py`) на каждом из 15 полей и напечатать таблицу «слайд → что я хотел → что вернула функция». **Расхождений 0**, иначе гейт красный.
2. **РАСПРЕДЕЛЕНИЕ.** Напечатать долю каждого архетипа. **Доля одного выше 2/3 — гейт красный** (правило канона).
3. **ТЗ ПОЛНЫ.** У каждого слайда с картинкой поле `🎨 ЧТО НА КАРТИНКЕ` содержит все пять подполей. Напечатать «слайдов с картинкой N · полных ТЗ N · неполных 0».
4. **БЮДЖЕТ ПОСЧИТАН.** Таблица на 15 строк, ни одной пустой клетки; напечатать суммарный дефицит знаков.
5. 🔴 **НИ ОДНОГО ЗНАКА ТЕКСТА СЛАЙДА НЕ ТРОНУТО.** Множество блоков (определение: непустая строка, не заголовок, не фронтматтер, не `> поле:mn`, не содержимое `<figure>`, не `---`) до и после **совпадает побайтово**. Напечатать «блоков 93 · изменённых 0». **Любое изменение — гейт красный, отчёт не принимается.**
6. **СБОРКА.** `build_doc.py` rc=0, `check_view.py` rc=0 — код возврата печатать первым.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «расхождений нет», а «нет, проверено X из 15».

### 🔴 ЧЕТЫРЕ ЛОВУШКИ
1. **АРХЕТИП — ЭТО ПОДСТРОКА, А НЕ НАМЕРЕНИЕ.** *Цена, уже оплаченная: слайд «Как доказывают, что функтора нет» получил формулировку «текстовая область на всю ширину», подстрока `на всю ширину` дала архетип `лестница-во-всю-ширину`, у которого панелей НЕТ ВОВСЕ, — и коммутативная диаграмма не попала на слайд вообще, при том что заход отчитался пунктом ✅. Ловилось только мягким ⚠ линтера.*
2. **НЕ ПИШИ ТЕКСТ.** Ты будешь видеть слайды, где текста явно мало, и рука потянется дописать. **Не тянись** — гейт 5 это ловит, и он завернёт отчёт целиком. Твоё дело — сказать ЧИСЛОМ, сколько дописать, а пишет следующий заход.
3. **НЕ ИЗОБРЕТАЙ ВИЗУАЛЬНЫЙ ЯЗЫК.** Он задан: `SLOVAR-primitivov.md` плюс шесть живых `<figure>` в ленте. Владелец: *«картинки бессмысленно задавать, они уже заданы в нормальном визуале — если там будет так, как там, то будет хорошо»*. Твоё дело — описать СОДЕРЖАНИЕ картинки, а не стиль. Примитива не хватило — **скажи в отчёт**, словарь не правь.
4. **МЕЛКАЯ КАРТИНКА — ДЕФЕКТ, А НЕ КОМПРОМИСС.** *«она должна быть не мелкая, на неё должно быть хорошо видно»*. Правило поля из словаря: `высота/ширина ≥ 0,6 → на поле, иначе во всю строку`; широкая полоса в жёлобе сжимается до нечитаемости. Не влезает — меняй архетип, а не размер.

## 3. ВЕРИФИКАТОР — свежий субагент, ДРУГОЙ метод

Заход не двигает текст, но правит служебные поля, от которых зависит геометрия всего дека, — и промах здесь виден только на собранном слайде. Поэтому верификатор с двумя задачами:

1. **Сохранность текста, программно:** берёт ленту из `git show HEAD:`, режет своим парсером и сверяет множество блоков с рабочим деревом **побайтово**. Печатает «блоков до N · после M · изменённых K». **K обязан быть 0.**
2. **Однозначность ТЗ — читательская проверка, и это главное.** Он **не видел** ни ленты целиком, ни списка слайдов: получает ТОЛЬКО тексты полей `🎨 ЧТО НА КАРТИНКЕ` и по каждому отвечает на один вопрос — **«могу ли я нарисовать эту картинку, не задавая вопросов? если нет, какого именно факта не хватает»**. Это ровно позиция художника, которому дек отдадут. Его список «не хватает» идёт в отчёт ЦЕЛИКОМ, включая пункты, с которыми ты не согласен.

⚠ **Критерий «описание однозначно» не проверяется автором описания ни в каком виде** — автор достраивает недостающее из головы, потому что помнит, что имел в виду. *Цена на этой арке: самопроверка автора ленты по критерию полноты дала «неполных формулировок 0»; свежий читатель, не видевший конспекта, нашёл 26 мест.*

Субагентов ≤5, рейт-лимит = отступить и доложить. Время ожидания мерь `date`, а не суммой `sleep`.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_geometria.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_geometria.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/raskadrovka   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone teorkat-vvedenie/raskadrovka \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone teorkat-vvedenie/raskadrovka` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
