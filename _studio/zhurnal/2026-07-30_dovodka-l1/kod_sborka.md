# Канал исполнителя — sborka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — раскладка задана поимённо, но решение «какой абзац в какой раздел» есть суждение о математическом тексте и рождается по ходу чтения.
> ⏱ **Спешки нет. Метрика — НИ ОДИН БЛОК НЕ ПОТЕРЯН и НИ ОДНОГО НОВОГО ПРЕДЛОЖЕНИЯ. Часы — норма.**

КОНТЕКСТ. Проект `teorkat-vvedenie/` — лекция «Зачем нужны категории», прочитана 29.07, успела ровно половину. 🔴 **Жанр сменился: из прочитанной половины делаются ЗАПИСКИ — слайд-конспект, который выкладывают в интернет постфактум, чтобы люди прочитали и разобрались.** Лектора нет, бюджета времени нет.

Владелец за семь кругов правок собрал **список из 15 слайдов** — `SPISOK-SLAJDOV.md`. Это утверждённая раскладка, и она есть ВХОД этого захода, а не предмет обсуждения.

🔴 **Почему это важно назвать.** Предыдущий заход (`kod_perekomponovka.md`) велел «разложить материал по хребту», **не дав списка экранов**, — исполнитель вынужден был изобрести структуру сам, единственной измеримой опорой были знаки, и он разложил 7416 знаков на 38 экранов по 195 знаков. Владелец завернул это первой фразой. Урок записан (`UROKI-FABRIKE §7`). **Здесь список есть, он поимённый, и отступать от него нельзя.**

ЦЕЛЬ: **переложить существующий текст ленты в 15 разделов по списку и завести пустые разделы под ненаписанное — не дописав ни одного предложения.**

🔴 **СТОП ДО ЦЕЛИ, простыми словами.** Ты получишь **переложенную ленту и собранный документ-вид**, но **НЕ получишь готовых записок**: на трёх слайдах содержание владельцем ещё не решено (слайд 2 — сама задача не придумана · слайд 9 — картинку надо изобрести · слайд 14 — какие свойства, не названо). Эти места остаются ПУСТЫМИ с заказом. Дописывание — отдельный заход после решений владельца.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_sborka.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_sborka.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- ПЛАН — в `## ПЛАН` перед действиями.

**Прочитать ТОЛЬКО это.** Адреса прогнаны через `test -f` при сборке захода 30.07.

| # | файл | зачем | режим |
|---|---|---|---|
| 1 | `_studio/zhurnal/2026-07-30_dovodka-l1/SPISOK-SLAJDOV.md` | 🔴 **ГЛАВНЫЙ ВХОД, читается ПЕРВЫМ.** Утверждённая раскладка: 15 слайдов, у каждого — мысль, откуда берётся текст, что на экране. Плюс «Философия записок» и «Лестница имён» — они объясняют, ПОЧЕМУ порядок такой, и нужны при спорных сопоставлениях | чтение |
| 2 | `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` | лента части 1. Живой замер 30.07: разделов **8**, блоков **63**, знаков **4507**, `<figure>` **3** | **ПРАВИШЬ** |
| 3 | `teorkat-vvedenie/raskadrovka/teksty/B-yazyk.md` | лента части 2. Разделов **5**, блоков **27**, знаков **2909**, `<figure>` **3** | **ПРАВИШЬ** |
| 4 | `_studio/zhurnal/2026-07-30_dovodka-l1/PLAN-perekomponovki.md` | **ключ адресов**: все 90 блоков с адресами `A:117`/`B:44`, знаками и первыми словами. Забирай готовым, заново не считай | чтение |
| 5 | `_studio/konvejer/06-tekst/DOK.md` | канон текста слайда: формат ленты, служебные поля `> поле:mn`, сцены, потолки | чтение |

Всё остальное не открывать. `C-*.md`, `D-*.md`, `E-*.md` — вторая лекция. `KONSPEKT-L1/` не открывать: он нужен для ДОПИСЫВАНИЯ, а ты не дописываешь. `src/` не открывать и не править — **порождаемый слой**.

**Определение БЛОКА (то же, что в прошлом заходе, числа сошлись):** непустая строка ленты, которая не является заголовком (`#`, `##`), фронтматтером, служебным полем (`> поле:mn …`), содержимым `<figure>…</figure>` или `---`. Воспроизведи счёт первым ходом: 63 и 27.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Переложить ленту `A-krasivaya.md` + `B-yazyk.md` из нынешних **13 разделов** в **15 разделов** по `SPISOK-SLAJDOV.md`, перенеся текст ДОСЛОВНО и не написав ни одного нового предложения.

### Шаг 1. Куда что кладём — сначала таблица, потом правка
Первым делом выпиши в `## ПЛАН` сопоставление: **для каждого из 90 блоков — в какой из 15 новых разделов он едет**. Адреса — `A:117`/`B:44`, берутся готовыми из `PLAN-perekomponovki.md §Ключ адресов`. Только после этого трогай файлы. *Почему: правка без выписанной таблицы не проверяема ни тобой, ни приёмкой, а восстановить намерение по дифу нельзя.*

### Шаг 2. Как физически устроен результат
**Файлов остаётся два** — `A-krasivaya.md` и `B-yazyk.md`; новые файлы не заводить. Границу между ними проведи там, где её проводит список: часть 1 — слайды 1–12, финал и справочник — 13–15. Если по смыслу граница уехала, скажи в отчёте, файлы не переименовывай.

**Заголовок раздела = рабочее имя слайда из списка, ДОСЛОВНО.** Это адрес, по которому владелец будет диктовать правки, и он должен совпадать со списком буква в букву.

**Пустой раздел** (слайды 2, 9-картинка, 14) заводится так:
```
## <рабочее имя из списка>

> поле:mn 🕳 **ПУСТО — ЗАКАЗ.** <что здесь должно быть, дословно из списка>. Решение владельца: <что именно не решено>.
```
🔴 **Заказ живёт ТОЛЬКО в служебном поле `> поле:mn`, не в тексте раздела.** Служебные поля на слайд не печатаются; текст печатается. Пустой раздел обязан быть физически пустым по блокам — иначе заказ уедет на слайд и станет содержанием.

### Шаг 3. Что переносится дословно, а что можно
**Дословно и только дословно:** все 90 блоков. Разрешено ровно три операции:
1. **ПЕРЕНОС** блока в другой раздел — основная работа;
2. **ПОРЯДОК** блоков внутри раздела — можно менять, если этого требует список;
3. 🔴 **РАЗРЕЗ блока на два — РАЗРЕШЁН владельцем, но только там, где блок несёт две разные мысли**, и таких мест известно **два**: `A:124` объявляет «вид» и «изоморфизм видов» · `A:159` несёт вердикты трёх разных слайдов. Владелец: *«если в одном блоке объявляется несколько понятий, то мы их, конечно, разносим»*. **Разрез — это деление существующей строки на две существующие; ни одного нового слова при этом не появляется.** Нашёл третье такое место — назови в отчёте, но НЕ режь.

**Запрещено:** переписывать формулировки · «улучшать» стиль · дописывать связки между разделами · удалять блоки · трогать `<figure>` и служебные поля, кроме тех, что сам заводишь под заказ.

### Шаг 4. Собрать документ-вид и проверить
```
python3 _generator/build_doc.py <путь к ленте>      # rc=0
python3 _generator/tools/check_view.py <путь>       # rc=0 — ловит «текст съеден при отрисовке»
python3 teorkat-vvedenie/raskadrovka/gejt_lenty.py  # если запускается — прогнать, число в отчёт
```
⚠ **В формулах писать `\lt` / `\gt`, не `<` / `>`** — иначе `check_view` покраснеет.
⚠ **Дек НЕ собирать** (`porodit.py`, `sverstat.py`, `podognat.py`, `build_deck.py` — ни один). В нём будут пустые слайды, он ничего не покажет, а порождающий слой затрёт работу. Дек — отдельный ход после дописывания.

### 🔴 КРИТЕРИЙ ГОТОВНОСТИ — шесть гейтов, все числовые, все могут ПРОВАЛИТЬСЯ
Прогони САМ, командами, напечатай ЧИСЛА:

1. **РАЗДЕЛЫ.** `grep -c '^## ' A-krasivaya.md` + то же по `B-yazyk.md` = **15**. Их заголовки совпадают с рабочими именами из списка **дословно** (сверить построчно, напечатать «совпало X из 15»). **X ≠ 15 — гейт красный.**
2. **ПОЛНОТА.** Каждый из 90 блоков присутствует в новой ленте. Проверка: множество блоков ДО (`git show HEAD:<файл>`) против множества блоков ПОСЛЕ, сравнение по ТЕКСТУ строки, не по адресу (адреса сдвинулись). Напечатай «было 90 · стало N · пропало — список или ПУСТО · появилось — список или ПУСТО». **Непустой список пропавших — гейт красный.**
3. **НИ ОДНОГО НОВОГО ПРЕДЛОЖЕНИЯ.** `git --no-optional-locks diff -- <ленты> | grep '^+'` — каждая добавленная строка обязана быть либо заголовком `## `, либо служебным полем `> поле:mn`, либо строкой, дословно совпадающей с одной из снятых (перенос), либо половиной разрезанного блока. Напечатай «добавленных строк N · из них заголовков A · служебных B · переносов C · половин разреза D · **необъяснённых E**». **E > 0 — гейт красный, и каждая необъяснённая печатается целиком.**
4. **ЗНАКИ.** Сумма знаков блоков после = **7416** (разрез сумму не меняет; отклонение допустимо только на длину разделителя и должно быть названо). Напечатай оба числа и разницу.
5. **ЗАКАЗ НЕ УТЁК В ТЕКСТ.** В каждом пустом разделе блоков **0**; строка заказа лежит в `> поле:mn`. Напечатай «пустых разделов N, блоков в них 0».
6. **СБОРКА.** `build_doc.py` rc=0 и `check_view.py` rc=0. Печатать код возврата ПЕРВЫМ, до содержательного вывода.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «потерь нет», а «потерь нет, проверено X из Y».

### 🔴 ЧЕТЫРЕ ЛОВУШКИ — по каждой строка в отчёте
1. **НЕ УЛУЧШАЙ ПО ДОРОГЕ.** Ты будешь читать текст, который местами плох, и рука потянется. **Не тянись.** Список известных дефектов, которые ТРОГАТЬ НЕЛЬЗЯ: обложка врёт про жанр · заголовок «Три поломки» обещает три, разобрана одна · утверждение общее, а доказательство частное · `A:143` печатает `E[U]` против запрета владельца · врезка `A:153` обсуждает несуществующую картинку · `B:89` опирается на неопределённые обозначения. Всё это чинится ПОТОМ и отдельно. Нашёл новое — в отчёт, не чини.
2. **НЕ ОТСТУПАЙ ОТ СПИСКА.** Список утверждён владельцем за семь кругов. Кажется, что слайд лучше слить или разделить, — **скажи в `## ПЛАН` ДО работы и жди**, не делай. *Цена отступления: предыдущий заход отступил не по злому умыслу, а потому что списка не было вовсе, и потерял прогон целиком.*
3. **ПУСТОЕ МЕСТО — ЭТО РЕЗУЛЬТАТ, А НЕ НЕУДАЧА.** Соблазн «дописать хоть что-нибудь, чтобы раздел не выглядел голым» — главный риск этого захода. Владелец сказал прямо: *«если пока не придумано, можно оставить пустое место; текст пока не пишем»*. Голый раздел с честным заказом лучше раздела, заполненного твоей прозой.
4. **АДРЕС — ЗАГОЛОВОК, номер слайда НЕ УПОТРЕБЛЯТЬ.** В отчёте и в ленте адресуйся заголовками. Номера сдвинутся при первой же правке.

## 3. ВЕРИФИКАТОР — ОБЯЗАТЕЛЕН: заход ДВИГАЕТ текст, а движение теряет молча

Свежий субагент, **другим методом**: он берёт исходную ленту из `git show HEAD:<файл>` (не из твоего диффа), режет на блоки СВОИМ парсером и сверяет множества **по тексту строк**, а не по адресам. Его вердикт — числами:

- «блоков до N · после M · пропало — список · появилось — список» (оба обязаны быть пусты, кроме объяснённых разрезов);
- «добавленных строк, не объяснённых ни заголовком, ни служебным полем, ни переносом — K» (обязан быть **0**);
- «заголовков разделов 15, совпало со списком 15».

⚠ **Мандат верификатора ШИРЕ твоего в одном месте:** ему разрешено открыть `SPISOK-SLAJDOV.md` целиком и проверить то, чего ты проверить не можешь, — **не отступил ли ты от списка молча**: совпадает ли порядок разделов и их состав с утверждённым. Расхождение он печатает построчно.

Субагентов ≤5, рейт-лимит = отступить и доложить. Время ожидания мерь `date`, а не суммой `sleep`.

**Точка отката обязательна:** зона до работы чиста в HEAD — значит откат полный через `git`, zip не нужен. Проверь `git_zona.py check --zone teorkat-vvedenie/raskadrovka` ПЕРЕД первой правкой и напечатай результат.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_sborka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_sborka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/raskadrovka   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone teorkat-vvedenie/raskadrovka \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **КОММИТ (строка обязательна, см. §4)**.

**Плюс шесть блоков, специфичных для этого захода:**
1. **Шесть чисел критерия §2**, каждое с командой.
2. **Таблица «15 разделов → сколько блоков в каждом»**, плюс отдельной строкой пустые.
3. **Каждый разрез блока — отдельной строкой:** какой блок, на какие два, в какие разделы уехали половины, и почему это две разные мысли.
4. **Что я нашёл, но НЕ ТРОНУЛ** — список дефектов без починки (ловушка 1).
5. **Отступления от списка** — если их нет, написать «отступлений нет»; молчание читается как пустота.
6. 🔴 **Как выглядит пустой раздел** — процитировать один целиком, чтобы владелец увидел форму заказа и поправил её одним словом ДО того, как она размножится.


🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Команда гейта сборки в заходе названа неверно: `build_doc.py` берёт ПАПКУ, а заход даёт ей ФАЙЛ
Шаг 4 захода печатает `python3 _generator/build_doc.py <путь к ленте>`, а движок отвечает `✗ не папка: …A-krasivaya.md` и выходит с **rc=2**. Правильный вызов — `build_doc.py teorkat-vvedenie/raskadrovka/teksty` (папка целиком, вид собирается на все вкладки). Строка про «`<путь к ленте>`» выглядит проверенной, потому что рядом честно стоит `# rc=0`, — но проверялся, видимо, только `check_view.py`, который папку и файл принимает одинаково.
ЦЕНА: два прогона впустую с rc=2 и один лишний ход на чтение `--help` и исходника движка. Дёшево ровно потому, что заход требует печатать код возврата ПЕРВЫМ (§1): без этого правила «✗ не папка» уехало бы в лог как успех, и гейт 6 был бы отмечен зелёным при несобранном виде. Это второй известный случай, когда правило «код возврата первым» окупилось в том же заходе, где его прочитали.

### Побочный выход `build_doc.py` шире зоны исполнителя, и заход этого не оговаривает
Заход даёт зону `teorkat-vvedenie/raskadrovka` и требует собрать документ-вид. Но `view.html` — **один файл на папку**, и собирается он по всем `*.md` со `status:`-шапкой, включая `C-*.md`, `D-*.md`, `E-*.md`, которые тот же заход велит НЕ ОТКРЫВАТЬ (это вторая лекция). То есть исполнитель обязан породить артефакт, содержащий текст, читать который ему запрещено. Обойти нельзя: у движка нет ключа «только эти вкладки», а не собирать — значит провалить гейт 6.
ЦЕНА: пока — только оговорка в отчёте (сами файлы C/D/E не тронуты, в дифе три пути). Но цена уже реализовалась бы, будь C/D/E чужой зоной в параллельном заходе: `view.html` стал бы полем конфликта двух исполнителей, а гейт зоны этого не увидит — он смотрит пути, а `view.html` формально в моей зоне и лежит. Лечится либо ключом отбора вкладок у движка, либо строкой в контракте зоны: «`view.html` папки — общий, при параллельных заходах его не пересобирать».

## ПЛАН — (заполняет исполнитель)

**Первый ход, проверено:** ветка `arka/mat-kostyak` (`git branch --show-current`, rc=0) · зона чиста в HEAD (`git_zona.py check --zone teorkat-vvedenie/raskadrovka` → ✅ «работа доехала в git, вне git ничего нет», rc=0) ⇒ точка отката = HEAD, zip не нужен · счёт блоков своим парсером воспроизведён: A **63** блока / **4507** знаков, B **27** / **2909** ⇒ 90 / 7416. Сходится с заходом до знака.

### Границы файлов и раздел = слайд
`A-krasivaya.md` = слайды **1–12**, `B-yazyk.md` = слайды **13–15** — как велит список. **Ни один блок не переезжает между файлами** (проверено: слайды 1–12 наполняются только блоками A, слайды 13–15 — только блоками B), так что диф каждого файла читается отдельно.

### Раскладка 90 блоков по 15 разделам (адреса из `PLAN-perekomponovki.md §Ключ адресов`, сверены своим парсером — совпали все 90)

| слайд (заголовок = рабочее имя) | блоки | сколько | знаков |
|---|---|---|---|
| 1 Обложка | `A:34` | 1 | 81 |
| 2 Две задачи — разные, а ответ один | — | **0** 🕳 | 0 |
| 3 Комбинаторное тождество и как его доказывают | `A:40` · `A:42`–`A:54` (таблица, 13 блоков) · `A:56` · `A:58` · `A:60` | 17 | 1176 |
| 4 Три тождества и три доказательства | `A:66` · `A:68` · `A:70` · `A:72` · `A:73` · `A:75` · `A:77` | 7 | 339 |
| 5 Переименование ничего не ломает | `A:79` | 1 | 66 |
| 6 Определение вида, и это свойство называется функториальностью | `A:118` · `A:126` · `A:128` · `A:124`**a** | 4 | ≈351 |
| 7 Как устроены биекции, квадрат функториальности, галерея видов | `A:136` · `A:138`–`A:147` | 11 | 398 |
| 8 Естественное преобразование видов — и что такое изоморфизм видов | `A:120` · `A:122` · `A:124`**b** · `A:155` · `A:159`**a** | 5 | ≈379 |
| 9 САМОЕ СЛОЖНОЕ МЕСТО ЗАПИСОК: изоморфизма нет | `A:157` · `A:159`**c** · `A:161` · `A:163` · `A:165` · `A:167` | 6 | ≈507 |
| 10 Чинится сменой арены — ровно то, что провалилось на слайде 9 | `A:159`**b** · `A:173` · `A:175` · `A:177` · `A:179` | 5 | ≈398 |
| 11 Раскрашенные множества — и оказывается, что это всё функторы | `A:176` · `A:181` · `A:183` | 3 | 273 |
| 12 Итог части | `A:221`–`A:225` · `A:227` | 6 | 532 |
| 13 Три арены — что у них общего? | `B:16` · `B:18`–`B:21` · `B:23` | 6 | 444 |
| 14 Что теперь стало видно | — | **0** 🕳 | 0 |
| 15 Справочная таблица: категории и функторы | `B:49` · `B:51` · `B:53` · `B:55` · `B:57` · `B:63` · `B:65` · `B:67` · `B:69` · `B:71` · `B:79` · `B:81` · `B:83` · `B:85` · `B:87` · `B:89` **+ 5 бездомных:** `B:37` · `B:39` · `B:41` · `B:25` · `B:27` | 21 | 2465 |

Сумма: 61 целый блок A + 2 разреза (A:124, A:159) + 27 блоков B = **90 из 90**, сирот 0, дублей 0.

### Два разреза — те самые, что разрешил владелец, и ничего сверх
1. `A:124` → **a** «{@2} Такая конструкция $F$ — **вид**» (слайд 6, там падает имя «вид») · **b** «такая $\varphi$ — **изоморфизм видов**» (слайд 8, там падает имя «изоморфизм»). Режу по существующей запятой.
2. `A:159` → **a** «{@2} **1, 4, 6, 7** — да» (слайд 8: ответ на вопрос «какие тождества — изоморфизмы») · **b** «**2, 3, 8** — не вид» (слайд 10: это и есть диагноз, ради которого меняют арену; `A:179` на том же слайде его и подхватывает — «Паскаль и Вандермонд тоже становятся изоморфизмами») · **c** «**5** — обе стороны вида, изоморфизма нет» (слайд 9). Режу по существующим ` · `.
Ведущий тег `{@2}` остаётся ТОЛЬКО на первом куске каждого разреза (он там и стоял); остальные куски — дословные подстроки. Поэтому сумма знаков падает ровно на длину снятых разделителей: −2 (`, `) и −6 (два ` · `) = **−8**, и это единственное отклонение гейта 4. Дублировать тег значило бы добавить 15 знаков, которых в оригинале нет.

### Порядок внутри разделов
Оригинальный, кроме двух мест, где его требует список:
- **слайд 6:** `A:118` → `A:126` → `A:128` → `A:124a`. Имя «вид» уходит в конец: «Приём: имя падает последним» (`SPISOK §Философия`).
- **слайд 8:** `A:120` → `A:122` → `A:124b` → `A:155` → `A:159a`. Определение и имя ДО вопроса «какие из тождеств — изоморфизмы видов?» — иначе `A:155` употребляет термин раньше определения (правило 11 §1). В старой ленте он его и употреблял.

### Пустые разделы и заказы
Физически пустых по блокам — **два**: слайды 2 и 14. Заказ в каждом — только в `> поле:mn`, в тексте ноль блоков. Слайду 9 блоки достаются, но картинки у него нет — ему тоже вешаю `> поле:mn 🕳 ПУСТО — ЗАКАЗ (картинка)`, не трогая блоки.

### Служебные поля и `<figure>` — не правлю, но обязан куда-то положить
Их не редактирую; каждое едет с тем разделом, чей текст оно описывает: `A:38`→3 · `A:64`→4 · `A:116`(«квадрат на правой полосе»)→5, вместе с самим квадратом `A:130` · `A:134`→7 · `A:151`+`A:153`→9 · `A:171`→10, с фигурой `A:185` · `A:219`+`A:229`+`A:231`→12 · фигура `A:81`→4 · `B:14`+фигура `B:29`→13 · `B:45`,`B:47`,`B:61`,`B:77`,`B:91`,`B:35`+фигуры `B:73`,`B:31`→15. Шапочные поля A (строки 10–30) остаются шапкой файла.

### 🔴 Четыре расхождения списка с живой лентой — называю ДО работы, не молча
1. **Слайд 3, «три строки из таблицы `A:40`–`A:51`» — предпосылка ложна.** Три тождества слайда 4 — это $\binom nk=\binom n{n-k}$, $\sum\binom nk=2^n$ и $\sum\binom nk\binom kl=3^n$. В таблице восьми есть только третье (`A:51`). Доказательство: в таблице стоят $k\binom nk$, Паскаль, Вандермонд, $\sum k\binom nk$, знакопеременная, $\binom nl2^{n-l}$, $3^n$, Фибоначчи — ни $\binom n{n-k}$, ни $2^n$ там нет (то же зафиксировал `PLAN-perekomponovki` дырой №8 и `OPROSNIK` В-11). Выбрать «три строки» нельзя, не дописав двух новых, а гейт 2 запрещает терять остальные 11 блоков таблицы. **Поэтому таблица едет на слайд 3 ЦЕЛИКОМ**, все 8 строк, и владелец решает, какие вычеркнуть и какие дописать. Знаков на слайде 3 из-за этого 1176, из них 756 — теги разметки.
2. **`A:176` список отдаёт ДВУМ слайдам сразу** — «Откуда» слайда 10 и «Откуда» слайда 11 оба называют `A:176`. Блок один: «- два цвета: объект $(M,N)$, морфизмы сохраняют цвет». Кладу на **слайд 11**, потому что это дословно его содержание («Объект $(M,N)$, морфизмы сохраняют цвет — декартово произведение»), а слайд 10 держит два других ремонта — `A:175` (отмеченная точка) и `A:177` (носитель по чётности).
3. **Пять блоков B список СНЯЛ, а удалять нельзя.** «Снято из финала» — отдельный слайд конструкций над категориями (`B:37`, `B:39`, `B:41`) и **общее** определение естественного преобразования (`B:25`, `B:27`). Общее определение функтора `B:23` при этом дом имеет — слайд 13 («здесь он получает своё общее место»). Для пяти снятых дома в списке нет ни одного, а гейт 2 требует их сохранить. Кладу их **в хвост слайда 15** (единственный раздел, чья объявленная функция — «не произносится вслух, лежит для тех, кому интересно») и ставлю над ними отдельное `> поле:mn 🕳 БЕЗ ДОМА — ЗАКАЗ`, чтобы владелец снял их одним движением, если хотел именно удалить. На слайде 11 их положить нельзя: `B:23`/`B:25` определяют функтор и ЕП через категорию, а категория определяется только на слайде 13 — это прямое нарушение правила 11.
4. **`A:159` несёт вердикты ТРЁХ слайдов, а разрез в задании описан «на два».** Режу на три куска (два разреза), потому что сам же заход называет причину: «`A:159` несёт вердикты трёх разных слайдов». Если владелец имел в виду буквально два куска — скажите, соберу заново одной командой.

### Порядок ходов
1. ПЛАН (этот текст) — записан до правок. ✅
2. Сборщик на Python: тянет блоки, поля и `<figure>` из оригинала **по номерам строк**, не перепечатывая ни знака руками; куски разрезов получает `split()`-ом по разделителю. Ручного набора текста ленты — ноль, только 15 заголовков и 4 заказных поля.
3. Шесть гейтов §2 сам, числами; заголовки сверяю программно с `SPISOK-SLAJDOV.md`, а не глазами.
4. `build_doc.py` + `check_view.py` + `gejt_lenty.py` (+ `check_termin.py`, раз §1 его называет), код возврата первым.
5. Свежий верификатор — субагент, другим парсером, из `git show HEAD:`, с открытым `SPISOK-SLAJDOV.md`.
6. Коммит зоны двумя ходами, `--no-optional-locks`, пути после `--`.
7. Дек НЕ собираю: ни `porodit.py`, ни `sverstat.py`, ни `podognat.py`, ни `build_deck.py`.

## ВОПРОСЫ — (заполняет исполнитель)

Ни один не блокирует приёмку: лента собрана и гейты зелёные. Все пять — решения владельца о СОДЕРЖАНИИ, каждое чинится одной командой сборщика.

1. 🔴 **Слайд 3 несёт всю таблицу восьми тождеств, а список просил три строки.** Три тождества слайда 4 — это $\binom nk=\binom n{n-k}$, $\sum_k\binom nk=2^n$ и $\sum\binom nk\binom kl=3^n$; в таблице есть **только третье**. Проверено дважды, мной и верификатором: `\binom n{n-k}` → **0** вхождений во всей ленте A, `2^{\,n}` как правая часть → **0**, `3^{\,n}` → строка 7 таблицы. Выбрать «три строки» нельзя, не дописав двух новых тождеств (это запрещено) и не выбросив 11 блоков таблицы (это тоже). **Какие строки таблицы оставить на слайде 3 и какие два тождества дописать — ваше решение.**
2. **Слайд 5 остался почти пустым — 1 блок, 57 знаков видимого текста.** Список отдал ему только картинку («квадрат есть в `Требовании красоты`») и сказал, что разбор на нескольких примерах пишется. Я добавил туда `A:79` («Какие свойства этой биекции хорошие? Почему она красивая?») как вопрос, на который слайд отвечает. **Если `A:79` должен остаться финалом слайда 4 — скажите, верну одной командой.**
3. **Пять блоков B лежат без дома в хвосте справочника** (`B:37`, `B:39`, `B:41` — конструкции над категориями; `B:25`, `B:27` — общее определение ЕП и пример с матрицей перехода). Список объявил их снятыми, но удалять блоки заходу запрещено. Помечены полем `🕳 БЕЗ ДОМА — ЗАКАЗ`. **Снять совсем или оставить в справочнике?**
4. **`A:159` разрезан на три куска, а задание говорило «на два».** Разрезал на три, потому что задание же и называет причину: «несёт вердикты трёх разных слайдов»; и ожидание гейта 4 (−8 знаков) сходится только при трёх кусках. **Если имелись в виду буквально два — скажите, какие.**
5. **`A:222` (тезис естественности) оставлен в «Итоге части».** Список пишет, что вы хотите поднять его во вступление, «тогда здесь четыре» тезиса — но это желание, не решение, и `PLAN-perekomponovki` оставляет его вам. **Поднимать?**

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `969d29a` — «лента записок Л1 переложена в 15 разделов по утверждённому СПИСКУ-СЛАЙДОВ…» · `git_zona.py check --zone teorkat-vvedenie/raskadrovka` → ✅ («работа доехала в git, вне git ничего нет», rc=0) · `git show --stat` → ровно 4 пути, все мои: `kod_sborka.md`, `A-krasivaya.md`, `B-yazyk.md`, `view.html`. Хэш дописан вторым коммитом: строка не может знать хэша коммита, в который сама входит.

### Что сделал и ЗАЧЕМ
Переложил ленту `A-krasivaya.md` + `B-yazyk.md` из 13 разделов в **15** по `SPISOK-SLAJDOV.md`, ничего не дописав. Заголовок каждого раздела — рабочее имя слайда дословно, чтобы вы диктовали правки адресом, а не номером. Файлов осталось два, граница там, где её проводит список: **A = слайды 1–12, B = 13–15**; ни один блок не переехал между файлами, поэтому диф каждого читается отдельно.

**Зачем именно так:** предыдущий заход завернули за то, что он изобрёл структуру сам. Здесь структура — вход, а не предмет, поэтому вся работа ушла в одно решение на блок: «в какой из 15 разделов он едет». Чтобы это решение было проверяемым, я выписал раскладку всех 90 блоков в `## ПЛАН` **до** первой правки.

**Как физически сделано:** сборщиком на Python, который тянет блоки, служебные поля и `<figure>` из оригинала **по номерам строк**, а куски разрезов получает `split()`-ом по существующему разделителю. Руками набраны только 15 заголовков и 4 заказных поля — ни одного знака существующего текста я не перепечатывал. Поэтому «дословно» здесь не обещание, а свойство способа сборки.

### 🔴 Шесть чисел критерия — каждое с командой

| # | гейт | команда | число | вердикт |
|---|---|---|---|---|
| 1 | РАЗДЕЛЫ | `grep -c '^## ' A-krasivaya.md` + то же по B | A=**12** · B=**3** · сумма **15** | ✅ |
| 1б | заголовки дословны | питон: имена из `SPISOK-SLAJDOV.md` регэкспом `^\*\*(\d+) · (.+?)\*\*` против `^## ` ленты | **совпало 15 из 15**, и в том же порядке | ✅ |
| 2 | ПОЛНОТА | множества блоков `git show HEAD:<файл>` против рабочего дерева, сравнение по ТЕКСТУ строки | было **90** · стало **93** · пропало — **2** (`A:124`, `A:159`, оба разрезаны) · появилось — **5** (их куски) · **несклеиваемых потерь 0 из 90** · дублей **0** | ✅ |
| 3 | НИ ОДНОГО НОВОГО ПРЕДЛОЖЕНИЯ | `git --no-optional-locks diff -- <ленты>`, классификация каждой `+`-строки | добавленных **40** · заголовков **15** · служебных **5** · переносов **15** · половин разреза **5** · **необъяснённых 0** | ✅ |
| 4 | ЗНАКИ | сумма `len()` блоков до и после | **7416 → 7408 · разница −8** | ✅ с названным отклонением |
| 5 | ЗАКАЗ НЕ УТЁК В ТЕКСТ | питон: блоки и заказные поля по разделам | **пустых разделов 2, блоков в них 0**; заказ в обоих — в `> поле:mn` | ✅ |
| 6 | СБОРКА | `build_doc.py teksty` → `check_view.py teksty` | **rc=0** и **rc=0** (код возврата снят первым, до вывода) | ✅ |

**Отклонение гейта 4 названо:** −8 знаков — это ровно снятые разделители разрезов, `, ` (2) и два ` · ` (6). Ведущий тег `{@2}` я оставил только на первом куске каждого разреза, где он и стоял; дублировать его значило бы добавить 15 знаков, которых в оригинале нет. Куски 2 и 3 поэтому приходят на сцене 1 своих слайдов — сцены на них всё равно решаются при доводке.

**Сверх шести — прогнал ещё три:** `gejt_lenty.py` → **rc=0**, 29 разделов (мои 15 + 14 второй лекции), медиана 44 слова / 396 знаков · `check_termin.py teksty` → **rc=0**, «ни один термин не работает раньше своего определения» · целостность: служебных полей **29 → 33** (утрачено **0**, новых **4** — все мои заказы), блоков `<figure>` **6 → 6**, **ни одного изменённого байта внутри**, фронтматтер оба цел.

### Таблица «15 разделов → сколько блоков»

| # | раздел | блоков | знаков блоков | видимых знаков (`gejt_lenty`) |
|---|---|---|---|---|
| 1 | Обложка | 1 | 81 | 81 |
| 2 | Две задачи — разные, а ответ один | **0** 🕳 | 0 | 0 |
| 3 | Комбинаторное тождество и как его доказывают | 17 | 1176 | 391 |
| 4 | Три тождества и три доказательства | 7 | 339 | 375 |
| 5 | Переименование ничего не ломает | 1 | 66 | 57 |
| 6 | Определение вида, и это свойство называется функториальностью | 4 | 351 | 396 |
| 7 | Как устроены биекции, квадрат функториальности, галерея видов | 11 | 398 | 364 |
| 8 | Естественное преобразование видов — и что такое изоморфизм видов | 5 | 379 | 413 |
| 9 | САМОЕ СЛОЖНОЕ МЕСТО ЗАПИСОК: изоморфизма нет | 6 | 507 | 533 |
| 10 | Чинится сменой арены — ровно то, что провалилось на слайде 9 | 5 | 398 | 380 |
| 11 | Раскрашенные множества — и оказывается, что это всё функторы | 3 | 273 | 256 |
| 12 | Итог части | 6 | 532 | 506 |
| 13 | Три арены — что у них общего? | 6 | 444 | 409 |
| 14 | Что теперь стало видно | **0** 🕳 | 0 | 0 |
| 15 | Справочная таблица: категории и функторы | 21 | 2465 | 2303 |

**Пустые отдельной строкой: слайды 2 и 14 — блоков 0 в каждом.** Слайду 9 блоки достались, пуста только полоса иллюстрации, и на неё повешен отдельный заказ.

🔴 **Инвариант плотности (~570 знаков/слайд) сломан в четырёх местах, и я это не чинил, а называю:** слайд 15 — **2303** видимых знака (справочник, «не произносится вслух»; список сам положил туда ~1700, я добавил 5 бездомных блоков) · слайд 3 — 1176 знаков блоков, но **391 видимых**: остальные 785 — теги `<table>`, на слайд они не печатаются · слайд 5 — **57** · слайд 1 — 81. Слайды 3 и 15 — прямое следствие вопросов 1 и 3; решаются вашим вердиктом, не моей правкой.

### Каждый разрез — отдельной строкой

1. **`A:124`** «{@2} Такая конструкция $F$ — **вид**, такая $\varphi$ — **изоморфизм видов**» → режу по существующей запятой на **«{@2} Такая конструкция $F$ — вид»** (уехало на **слайд 6**) и **«такая $\varphi$ — изоморфизм видов»** (уехало на **слайд 8**). **Почему две разные мысли:** это два разных объекта и два разных имени — вид есть конструкция, изоморфизм есть отображение между конструкциями. По лестнице имён списка они и падают на разных слайдах: «вид» на 6-м, «изоморфизм видов» на 8-м. В старой ленте оба имени падали одной строкой, то есть «изоморфизм» приходил за два шага до своего вопроса.
2. **`A:159`** «{@2} **1, 4, 6, 7** — да · **2, 3, 8** — не вид · **5** — обе стороны вида, изоморфизма нет» → режу по существующим ` · ` на **три**: «1, 4, 6, 7 — да» (**слайд 8**, это ответ на вопрос `A:155` «какие из тождеств — изоморфизмы видов?») · «2, 3, 8 — не вид» (**слайд 10**: это и есть диагноз, ради которого меняют арену, и `A:179` на том же слайде его подхватывает — «Паскаль и Вандермонд тоже становятся изоморфизмами видов») · «5 — обе стороны вида, изоморфизма нет» (**слайд 9**, весь слайд про этот случай). **Почему три разные мысли:** три вердикта о трёх разных судьбах тождества — да, вообще не вид, вид без изоморфизма — и каждая разбирается на своём слайде.

**Третье место, которое просит разреза, — назвал и НЕ резал:** `A:179` («Над отмеченными $E^{+}\simeq E^{-}$… Паскаль и Вандермонд тоже становятся изоморфизмами видов») несёт Паскаля, чей выигрыш список отдал слайду 10, и Вандермонда, которого список со слайда 10 **снял** и отдал слайду 11. Одна строка на два слайда. Разрешение — за вами.

### Что я нашёл, но НЕ ТРОНУЛ

Из списка известных (ловушка 1) — все на месте, не чинил ни одного: обложка `A:34` врёт про жанр и обещает «два запрета», уехавшие во вторую лекцию · заголовок «Три поломки» больше не существует, но его врезка `A:153` цела · `A:143` печатает `E[U]` против запрета · врезка `A:153` обсуждает картинку с полным циклом на четырёх точках, которой в ленте **нет ни одной** (`<figure>` у слайда 9 отсутствует) · `B:89` опирается на `Mat`, `FinBool`, `FinSet`, из которых определено **ноль** (врезка `B:47` сама это признаёт).

**Новое, чего в списке ловушек не было:**

1. 🔴 **Рабочее имя слайда 9 — «САМОЕ СЛОЖНОЕ МЕСТО ЗАПИСОК: изоморфизма нет» — это редакторская помета, а не заголовок слайда.** Оно нарушает сразу два правила канона текста: капс запрещён (`06-tekst/DOK.md`: «Caps — это CSS-трансформ сборки, НЕ авторская печать») и заголовок не должен спойлерить вывод. Я поставил его дословно, потому что заход требует совпадения буква в букву, а заголовок — ваш адрес для правок. **Переименуйте — я перенесу.**
2. **Рабочее имя слайда 10 содержит номер слайда** — «ровно то, что провалилось на слайде 9». Ловушка 4 этого же захода запрещает номера как адреса: при первой перестановке номер соврёт.
3. **Заголовки 6 и 7 употребляют «функториальность», которой в блоках ленты 0 вхождений** — определение ещё не написано (дыра `PLAN-perekomponovki` №16). `check_termin.py` зелёный: он смотрит блоки, а слово живёт в заголовке.
4. **У `A:175` теперь висит точка с запятой:** список из трёх ремонтов потерял средний пункт (`A:176` уехал на слайд 11), и «- отмеченная точка: …$\ast$;» стоит перед последним пунктом. Чинится одним знаком, но это правка текста — не моя.
5. **Диапазон в самом списке неверен** (нашёл верификатор): список цитирует таблицу как `A:40`–`A:51`, а строки данных лежат в `A:45`–`A:52` — восьмая строка в диапазон не попала.
6. **Реплика `B:91` потеряла смысл:** «мостик в следующую вкладку: мы назвали десяток функторов, а вот эта конструкция функтором не оказывается» — теперь стоит в конце справочника, который «не произносится вслух».
7. **Шапочные поля A (строки 10–30) описывают ленту на 56 разделов** и мэппинг «Блок A: 1→1 · 2→2 …». Поле само уже признавало «разделов в файле теперь 8»; теперь их 12 — карта устарела дважды. Служебные поля править запрещено, поэтому оставил.
8. **`A:181`/`A:183` («Итог двойной», «Ответ зависит не от тождества, а от арены») стоят на слайде 11**, а не в «Итоге части»: список заканчивает аренную линию там, и `A:183` — прямая подводка к «это всё функторы». Если хотите их в слайд 12 — перенос двух блоков.

### Отступления от списка — есть, четыре, и вот они поимённо
Молчания здесь быть не должно, поэтому перечисляю всё, включая то, что верификатор поймал как незадекларированное.

1. **Слайд 3 получил всю таблицу (17 блоков), а не «три строки из таблицы».** Вынужденно: см. вопрос 1, предпосылка списка ложна и проверена командой.
2. **`A:176` отдан слайду 11, хотя список назвал его в «Откуда» и слайда 10, и слайда 11.** Выбрал 11, потому что это дословно его содержание; слайд 10 держит два других ремонта.
3. **Пять блоков B лежат в хвосте слайда 15, которого им список не давал.** Вынужденно: дома нет ни одного, а удалять запрещено.
4. **Три размещения сделаны моим суждением там, где графа «Откуда» молчит, — и в ПЛАНЕ я их не назвал; называю здесь, это правильная претензия верификатора:**
   - **`A:79`** («Какие свойства этой биекции хорошие? Почему она красивая?») уехал из «Трёх состояний элемента» на **слайд 5** как вопрос, на который тот слайд отвечает. Список отдавал слайду 5 только картинку. → вопрос 2.
   - **`A:120`, `A:122`** («красивое, если для каждого $U$ дана биекция $\varphi_U$…» + квадрат) уехали на **слайд 8**, а не на 5: это дословно определение ЕП на видах, которое список у слайда 8 и заказывал («семейство биекций $F[U]\to G[U]$, согласованное с переименованиями»). На слайде 5 они бы употребили $F[\sigma]$ раньше, чем `A:118` его вводит на слайде 6.
   - **`A:157`, `A:167`** (формула тождества 5 и «Упражнение. При нечётном $n$…») уехали на **слайд 9** — весь слайд про тождество 5; в «Откуда» слайда 9 названы только `A:161`, `A:163`–`A:165`.
   - Порядок внутри слайдов 6 и 8 изменён — заявлено в ПЛАНЕ: имя «вид» уходит в конец слайда 6 («имя падает последним»), а определение и имя на слайде 8 встают ДО вопроса `A:155`, иначе термин работает раньше определения.

**Отступлений, кроме этих четырёх, нет.** Проверено программно: имена и позиции разделов совпали 15 из 15, файловая граница ровно 1–12 / 13–15.

### Как выглядит пустой раздел — цитирую целиком, чтобы вы поправили форму одним словом ДО того, как она размножится

```
## Две задачи — разные, а ответ один

> поле:mn 🕳 **ПУСТО — ЗАКАЗ.** Две конкретные комбинаторные задачи с явными числами; обе про число элементов множества; обе можно посчитать руками, но считать не нужно: решаем одну, вторая решается сама; переход к общей технике. Картинка: разбиение на пары. Сцен: 1–2. Требование: нетривиальная, для взрослых, с явными числами, не про виды. **Решение владельца:** сама задача не придумана и придумывать её сейчас он отложил — «надо придумать задачу; если пока не придумано, можно оставить пустое место — текст пока не пишем»
```

Всё содержание заказа лежит в служебном поле, блоков в разделе ноль — значит на слайд заказ не уедет. Таких полей четыре: слайды 2 и 14 (пустые), слайд 9 (только картинка) и хвост слайда 15 (бездомные блоки).

### Как проверил
Каждое число выше — команда, а не мой счёт. Счёт блоков воспроизведён первым ходом до правок (63 и 27 — сошлось с заходом до знака). Полнота проверяется **множествами по тексту строк**, не по адресам: адреса сдвинулись, а текст нет. Гейт 3 классифицирует каждую добавленную строку и печатает необъяснённые целиком — их ноль. Разрезы проверяются на **склейку байт-в-байт**: куски обязаны собираться обратно в исходную строку разделителем.

### Что НЕ трогал
`src/` (порождаемый слой) · `C-*.md`, `D-*.md`, `E-*.md` (вторая лекция) · `KONSPEKT-L1/` (не открывал: он для дописывания, а я не дописываю) · `_studio/docs/` · дек: **ни `porodit.py`, ни `sverstat.py`, ни `podognat.py`, ни `build_deck.py`** — ни один не запускался. Ни одного `<figure>`, ни одного старого служебного поля, ни одного слова существующего текста.

`view.html` пересобран — он в зоне и есть требуемый выход. Оговорка: `build_doc.py` собирает вид **папки**, поэтому в `view.html` попали и вкладки второй лекции (5 вкладок). Сами файлы C/D/E не изменены — проверено `git status`, в дифе только три пути.

### Результат верификатора (свежий субагент, другой парсер, из `git show HEAD:`, с открытым списком)
- Откалибровал парсер на контракте самостоятельно: **90 блоков, 7416 знаков** — «совпало байт-в-байт» (альтернативную трактовку определения блока, дающую 50/27, отверг сам).
- «блоков до 90 → после 93 · пропало 2 · появилось 5 · **обе пропажи склеиваются из появившихся байт-в-байт** · **несклеиваемых потерь 0 из 90 проверенных**».
- «добавлено 40 непустых строк = 15 заголовков + 5 полей + 15 переносов + 5 кусков резов · **E = 0** · ни одной содержательной строки не выброшено».
- «**имя И позиция совпали 15 из 15**», «полей 29 → 33, **утрачено 0**», «`<figure>` и html-блоки — **байты идентичны, ни одного изменённого**».
- Подтвердил все **четыре** мои претензии к списку, каждую с доказательством из живых файлов, и добавил пятую (неверный диапазон `A:40`–`A:51`).
- Его вердикт: «как переноска текста работа безупречна — 0 потерь, 0 необъяснённых строк, 0 тронутых байт в html»; дыры — «только в дисциплине следования списку: 6 размещений сделаны вне заказа, и три из них исполнитель нигде не задекларировал». **Три незадекларированных принял и внёс в отступление 4 выше.** Остальные три (`A:159`, `A:124`, `A:176`) были заявлены в ПЛАНЕ до работы; его замечание, что выбор не помечен в самой ленте, отклоняю: пометка в ленте — это новый текст, а его запрещает ловушка 1.

**Цена верификации честно:** два первых верификатора погибли на обрыве соединения (не рейт-лимит и не мой код), потеряв результат. Слепого ретрая не делал — на третьем ходе сменил метод: верификатор пишет вердикт в файл, а мне отдаёт короткую сводку. От первого прогона уцелел восстановленный лог, и он независимо подтвердил базу 90/7416 и разбивку 13 разделов до знака.

### Открытое — «возвращаться»
- **Пять вопросов выше** — все содержательные, каждый чинится одной командой сборщика.
- **Дописывание — отдельный заход после ваших решений.** Стоп до цели сработал как объявлено: лента переложена, но записок ещё нет.
- **Дек не собран сознательно** — в нём были бы пустые слайды, а порождающий слой затёр бы работу.
- **Слайд 9 без картинки**, и это самое сложное место записок по вашему же диагнозу.

### 🔴 НЕОБРАТИМОЕ
**Необратимого нет.** Ни удалений, ни переименований, ни перемещений файлов, ни `git reset`/`checkout`. Изменены перезаписью три файла, все в зоне, все были чисты в HEAD до работы — восстанавливаются полностью из HEAD-коммита, предшествующего `de00263`: `A-krasivaya.md`, `B-yazyk.md`, `view.html`. За зону не вышел ни одной правкой: `git show --stat` показывает ровно 4 пути, и все они мои.

**Время прогона и токены** — снимает приёмка из `result`-строки лога прогона; исполнителю счётчик недоступен.
