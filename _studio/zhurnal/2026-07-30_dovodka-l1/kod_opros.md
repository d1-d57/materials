# Канал исполнителя — opros (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — заход СУДИТ математический текст на понятность и логическую связность; список вопросов рождается только из чтения, расписать его заранее нельзя.
> ⏱ **Спешки нет. Метрика — ПОЛНОТА ОХВАТА и ТОЧНОСТЬ АДРЕСОВ. Часы — норма.**

КОНТЕКСТ. Проект `teorkat-vvedenie/` — научпоп-лекция «Зачем нужны категории» (курс теории категорий, два лектора). **Лекция прочитана 29.07 вечером и провалилась по темпу: из 32 слайдов дека успели ровно 15 — части 1 и 2 целиком.** Владелец: *«я открыл и увидел ПЕРВЫЕ 15 СЛАЙДОВ, КОТОРЫЕ БЫЛИ СДЕЛАНЫ ПЛОХО. Если бы они были сделаны хорошо, у меня была бы другая лекция»*. Материал разделён на две лекции: прочитанное (части 1–2) доводится до идеала и читается снова, непрочитанное (части 3–4) собирается во вторую лекцию. **Этот заход — только про прочитанную половину.**

Владелец назвал, что именно развалилось (дословно, 30.07): *«в конце оказалось, что люди не все понимают, что такое биекция — не было примеров»* · *«очень резкий был переход, прыжок по сложности к категориям без примеров»* · *«потом были эти примеры, которые были непонятны — они таким скопом не должны быть»* · *«в каком-то слайде, где нужно было показывать инъекции и то, что там что-то не переставляется, не было диаграммы; диаграмма должна была быть очень конкретно выписана»* · *«то, что не существует изоморфизма между чётными и нечётными подмножествами — плохо выписано»* · *«идея естественного преобразования должна быть гораздо понятнее, чётче сформулирована и проиллюстрирована»*.

ЦЕЛЬ: **собрать ОПРОСНИК — артефакт, по которому владелец за один сеанс проговорит все решения по прочитанной половине.** Не план правок, не переписанные слайды: список вопросов с вариантами ответов и предложениями, адресованных заголовками разделов.

🔴 **СТОП ДО ЦЕЛИ, простыми словами: ты выдаёшь ОПРОСНИК, но НЕ правишь ни один слайд и ни одну строку лекции.** Дек, лента, конспект, упражнения — только на чтение. Правки пойдут отдельным заходом ПОСЛЕ того, как владелец ответит.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/zhurnal/2026-07-30_dovodka-l1. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add _studio/zhurnal/2026-07-30_dovodka-l1` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- ПЛАН — в `## ПЛАН` перед действиями.

**Прочитать ТОЛЬКО это. Проект не изучай, по сторонам не смотри.** Пути от корня репо; все девять адресов прогнаны через `test -f` при сборке захода 30.07 — существуют.

| # | файл | зачем именно он |
|---|---|---|
| 1 | `teorkat-vvedenie/SPEKA.md` | **читается ПЕРВЫМ** — охват, формат, регистр, роутер проекта: истина о том, что этой лекции можно, а чего нельзя |
| 2 | `teorkat-vvedenie/DUH.md` | эстетика и голос: чем «понятнее» отличается от «проще» в этом проекте |
| 3 | `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` | **лента части 1 ЦЕЛИКОМ** — источник текста слайдов; в полях `> поле:mn` лежат раскладки, реплики «не слайд» и следы прошлых правок |
| 4 | `teorkat-vvedenie/raskadrovka/teksty/B-yazyk.md` | **лента части 2 ЦЕЛИКОМ** — то же |
| 5 | `teorkat-vvedenie/src/content/s01.md` … `s12.md` (12 файлов) | что фактически напечатано на слайде. 🔴 **Это ПОРОЖДАЕМЫЙ слой** (`porodit.py` из ленты) — читать можно, править НЕЛЬЗЯ даже в мыслях: первая же сборка сотрёт |
| 6 | `teorkat-vvedenie/KONSPEKT-L1/01-krasivaya-biekciya.md`, `02-aksiomatizaciya.md`, `03-tri-opredeleniya.md`, `04-strelka.md`, `05-funktor.md` | исходный конспект, где мысль развёрнута полно (~185 КБ на пять файлов). **Главный источник ответа на вопрос «что потерялось при сжатии в слайд»** — примеры, мотивировки, переходы |
| 7 | `_studio/zhurnal/2026-07-28_konspekt-l1/PRAVKI-final.md` | диктовка владельца 29.07 (за 40 минут до лекции). 🔴 **Читается, чтобы НЕ ПЕРЕСПРАШИВАТЬ уже решённое** — см. ловушку 2 |
| 8 | `teorkat-vvedenie/repeticia/PRAVKI.md` | живая речь владельца на репетиции с претензиями, 150 КБ. Сплошное чтение НЕ требуется — грепай по заголовкам разделов и по ключевым словам («биекц», «непонятн», «пример», «диаграмм»); охват назови числом |
| 9 | `_studio/zhurnal/_SHTAB-teorkat-vvedenie/MARSHRUT.md` | штаб. Нужен ровно для ловушки 1: там записан ПОРЯДОК подачи от 23.07, и он противоречит живому деку |

Всё остальное — не открывать. `teorkat-vvedenie/raskadrovka/teksty/C-*.md`, `D-*.md`, `E-*.md` и `src/content/s13.md`…`s26.md` — это ВТОРАЯ лекция, она вне этого захода.

### Двенадцать разделов прочитанной половины — список авансом
Снят грепом 30.07 при сборке захода. **Список дан, чтобы ты не изобретал имена и не путал адреса; но истина — живой файл.** Расходится с живым — скажи в `## ПЛАН` и считай по живому, список не подгоняй.

Из `A-krasivaya.md` (часть 1, семь содержательных + титульный):
`Зачем нужны категории` ← **это ТИТУЛЬНЫЙ слайд, в двенадцать не входит** · `Тождества на доске` · `Три состояния элемента` · `Требование красоты` · `Галерея видов` · `Три поломки на языке видов` · `Менять надо не биекцию, а арену` · `Резюме`

Из `B-yazyk.md` (часть 2, пять):
`Категория, функтор, естественное преобразование` · `Конструкции` · `Категории: множество со структурой` · `Стрелка как следование и как шаг` · `Список функторов`

⚠ **Заголовок раздела ленты и текст на слайде расходятся, и это НОРМА, а не дефект:** дек заголовков разделов не показывает НИ У ОДНОГО слайда (проверено на предыдущей арке: `<h1>/<h2>` в `dist/index.html` — 0 штук). Например раздел `Конструкции` печатает на слайде «Противоположная $\mathcal C^{\mathrm{op}}$», а `Категории: множество со структурой` — «Стрелка — отображение, сохраняющее структуру». **Адресуйся заголовком РАЗДЕЛА** (владелец диктует по нему), но в поле `сейчас` называй то, что реально видит зал.

### ПРЕДПОСЫЛКИ АНАЛИТИКА — четыре штуки, каждую ПРОВЕРЬ и опровергни, если ложна
🔴 Это мои наблюдения, не факты, и мне приятно, если они подтвердятся. **Опровержение любой — полноценный результат, пиши его прямо.** Проверяется каждая одной командой или одним чтением:

1. **Все три центральных определения стоят ОДНИМ разделом**, и естественное преобразование получает в нём одну строку формулы плюс один пример (матрица перехода между базисами). Проверка: раздел `Категория, функтор, естественное преобразование` в `B-yazyk.md`, он же `src/content/s08.md`. **Если это так — при живом темпе 6 мин/слайд на центральное понятие лекции приходится меньше минуты, и это первый кандидат в корень провала.** Замечание владельца *«идея естественного преобразования должна быть гораздо понятнее, чётче сформулирована»* адресуется, вероятно, сюда.
2. **Квадрат естественности введён ДВАЖДЫ и не развёрнут ни разу:** первый раз в разделе `Требование красоты` (с буквами $E$, $E'$, $\sigma$, $\varphi$ и подписанной биекцией), второй — в разделе трёх определений, где его же `figcaption` говорит «этот квадрат уже стоял в требовании красоты». Проверка: `grep -c 'Квадрат естественности\|квадрат' raskadrovka/teksty/{A-krasivaya,B-yazyk}.md` и чтение обоих `<figure>`.
3. **Место для разворота лежит в двух шагах:** два раздела части 2 — `Конструкции` и `Категории: множество со структурой` — пусты примерно на 45 % и 50 % соответственно (замер предыдущей арки, 782px зоны, три абзаца текста). Проверка: длина `src/content/s09.md` и `s10.md` в знаках против остальных. **Если предпосылка верна, «дать ЕП больше места» не требует отнимать время у чего-то важного — только у зоопарка.**
4. **Иллюстраций в прочитанной половине осталось шесть** (`grep -c data-ill src/slides/s0[1-9].html src/slides/s1[0-2].html` — живое значение 30.07: `s02`=1, `s03`=1, `s06`=1, `s08`=2, `s11`=1, остальные 0). Ревизия в ночь на 30.07 сняла картинки массово (по ленте 34 → 11), поэтому **спрашивать «снять ли картинку», которой уже нет, — воровать время владельца** (ловушка 2).

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

Создать ОДИН файл: **`_studio/zhurnal/2026-07-30_dovodka-l1/OPROSNIK-L1.md`**. Больше файлов не заводи.

### Тип захода — АНАЛИТИЧЕСКИЙ. Что это значит буквально
Ты **измеряешь и спрашиваешь**, а не правишь. Лекцию не переписываешь, слайды не сочиняешь, математику не изобретаешь. Единственный источник содержания лекции — владелец; твоя работа — поставить ему вопросы так, чтобы он ответил один раз и по существу. Предложение своего варианта разрешено и приветствуется — но только в поле `предложение`, помеченным как предложение, никогда как факт.

### Адрес — ЗАГОЛОВОК РАЗДЕЛА, номер слайда НЕ УПОТРЕБЛЯТЬ ВООБЩЕ
🔴 Каждый вопрос адресуется дословным заголовком раздела ленты (`## <заголовок>` в `A-krasivaya.md` / `B-yazyk.md`). *Цена 29.07: из 20 пунктов диктовки, адресованных номерами, один заход сделал 7, следующий из 8 — четыре, и причину назвал сам: «цель не адресуется» — между диктовкой и исполнением слайды снимали и переставляли, всё после точки вставки сдвинулось. Восстановить нумерацию постфактум НЕ УДАЛОСЬ. Заход, перешедший на заголовки, сошёлся 20 из 20.* Номер слайда в опроснике запрещён даже справочно: он снова сдвинется, потому что заход-правка будет слайды добавлять.

### Прочитанная половина — двенадцать содержательных разделов
Считается командой (`KONSTITUCIYA §10` — в критерии пишем команду, не значение):
```
grep -c '^## ' teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md   # живое значение 30.07: 8
grep -c '^## ' teorkat-vvedenie/raskadrovka/teksty/B-yazyk.md       # живое значение 30.07: 5
```
8 + 5 = 13 разделов, минус **«Зачем нужны категории»** — он уходит на титульный слайд, а не в содержание. Итого **12 содержательных разделов**, ровно они и есть прочитанное. Прогони обе команды первым ходом: расхождение с 8/5 значит, что ленту тронули после сборки захода, — тогда скажи это в `## ПЛАН` и считай по живому.

Плюс три служебные позиции, которые зал тоже видел: титул «Зачем нужны категории» · визитка «Про меня» · разделитель «Зоопарк». Им — одна общая короткая секция, формат вопросов на них не распространяется.

### Структура `OPROSNIK-L1.md` — жёсткая, по ней идёт гейт

**Уровни заголовков в опроснике фиксированы, иначе гейт 1 считает не то:** `## Часть N. …` — части · `### <дословный заголовок раздела ленты>` — только разделы Части I, и больше НИГДЕ `###` не употребляется · `#### В-NN · …` — вопросы. То есть `grep '^### '` по опроснику обязан дать ровно двенадцать строк, и это и есть проверка охвата.

**Часть 0. АРХИТЕКТУРНЫЕ РАЗВИЛКИ** (уровень всей лекции, не отдельного слайда). Каждая — тем же форматом вопроса, что ниже. Три обязательные, больше — если найдёшь:

- **Р-1. Порядок подачи.** Гипотеза владельца 30.07 дословно: *«я бы основную часть с категориями чуть ужал, и там бы вводил понятие функтора, естественного преобразования подробно, раньше, больше коммутативных диаграмм давал, смотрел бы на разные примеры, как можно думать про это как про функтор, и потом постепенно наращивал информацию, пока не подвёлся бы к идее категории. Идею категории давал бы в самом конце, наверное»*. Проверь по материалу и назови цену обоих порядков. **См. ловушку 1 — подыгрывать этой гипотезе запрещено.**
- **Р-2. Сколько места вводному примеру.** Владелец: *«я бы сделал пример в конце мало»* и *«был большой заход красивый про комбинаторику, потом всё развалилось»*. Часть 1 — семь разделов из двенадцати, то есть больше половины прочитанного, и это комбинаторный вход. Назови, что именно режется и что при этом теряется.
- **Р-3. Бюджет слайдов.** 🔴 Ограничение «не увеличивая» СНЯТО владельцем 30.07: *«ничего страшного, если стоит больше слайдов на тот же самый материал, просто дадим больше места важным примерам, если оно станет понятнее»*. Живой замер темпа: **15 слайдов за 90 минут = 6 минут на слайд**. Посчитай и напечатай, сколько слайдов вмещает двухчасовая встреча при этом темпе, и на сколько позиций реально можно вырасти. Это число — рамка для всех остальных ответов, поэтому развилка стоит в Части 0, а не в конце.

**Часть I. ПО РАЗДЕЛАМ — двенадцать секций, по одной на раздел, в порядке ленты.** Заголовок секции — `### <дословный заголовок раздела>`. Внутри: короткая строка «что стоит сейчас» (факт из живого файла, не пересказ) и дальше вопросы.

**Часть II. ЧТО СКАЗАЛ ВЛАДЕЛЕЦ 30.07 — шесть названных дефектов, каждый с адресом.** Взять шесть цитат из блока КОНТЕКСТ выше и для КАЖДОЙ найти по ленте и конспекту, **где именно** она попадает, и поставить вопрос «чем лечим». Дефект, для которого адрес не нашёлся, — назови честно строкой «адрес не найден, проверено по таким-то файлам»: это законный результат, а молчание — нет.

**Часть III. СВЕЖИЙ ЗАЛ** — находки субагента-читателя (см. §3). Каждая находка — вопросом того же формата.

**Часть IV. УПРАЖНЕНИЯ.** В `UPRAZHNENIYA-L1.md` восемь упражнений (`grep -c '^## ' teorkat-vvedenie/UPRAZHNENIYA-L1.md` — живое значение 30.07: **8**), и они почти все про части 3–4. Владелец 30.07: *«в первых двух разделах собрать большое количество классных маленьких упражнений»*. Разметь имеющиеся восемь по частям (таблицей, с распределением — `RUKOVODSTVO §Заход` клауза 7: ноль в клетке разбирается) и **предложи кандидатов на маленькие упражнения по частям 1–2, вытащенные ИЗ КОНСПЕКТА, а не сочинённые.** Каждый кандидат — с адресом в конспекте. Число кандидатов не задано намеренно (см. ниже).

**Часть V. ЧЕГО ОПРОСНИК НЕ ПОКРЫЛ.** Честная слепая зона: что ты не смог оценить и почему. *Зелёный чекер с необъявленной слепой зоной опаснее красного — ему верят.* В частности: **дек ты глазами не видел** (браузера нет), значит всё про ВИД слайда — вне твоего мандата, и это надо сказать прямо.

### Формат ОДНОГО вопроса — шесть полей, все обязательны
По этому формату гоняется гейт, поэтому он буквальный:
```
#### В-<NN> · <вопрос одной строкой, с вопросительным знаком>
- **где:** <дословный заголовок раздела ленты | «вся лекция» для Части 0>
- **сейчас:** <что стоит в живом файле — факт, а не оценка>
- **почему спрашиваю:** <какая несостыковка / что теряется / чего зал не соберёт>
- **варианты:** (а) … · (б) … [· (в) …]     ← ЛИБО единственное слово ОТКРЫТЫЙ
- **предложение:** <один вариант по имени + одна фраза почему. Либо «нет предложения» — это законно>
- **цена:** <дёшево / средне / дорого> — <в слайдах и минутах, числом>
```
- **`варианты`** — либо не меньше двух, либо ровно слово `ОТКРЫТЫЙ` (когда варианты сочинять нельзя: нужно содержание, которого нет ни в одном файле). Один вариант — это не вопрос, а замаскированное решение за владельца.
- **`цена`** — потому что список правок сортируется по «заметность × дешевизна», а не по тяжести дефекта. *Цена 29.07: приоритет выстроили по важности, снятие одной фразы стоило десять секунд и стояло в хвосте — владелец наткнулся на неё первым взглядом и решил, что «ничего не внеслось», хотя было закрыто семь пунктов из двадцати, включая тяжёлые. Понадобился ещё один заход ради двух правок на минуту работы.*

### Числа вопросов НЕТ намеренно — вместо планки охват
Планку не ставлю ни снизу, ни сверху: снизу она подталкивает натянуть список до числа, сверху даёт право остановиться, когда число набрано, а сколько тут настоящих вопросов — заранее не знает никто. **Взамен обязателен ОХВАТ ТАБЛИЦЕЙ:** двенадцать строк «раздел → сколько вопросов». Раздел с нулём — законен, если рядом стоит причина; раздел с нулём и без причины — брак.

### 🔴 КРИТЕРИЙ ГОТОВНОСТИ — пять гейтов, каждый может ПРОВАЛИТЬСЯ
Прогони их САМ, командами, и напечатай **числа**, а не слова:

1. **ОХВАТ РАЗДЕЛОВ.** Все 12 заголовков содержательных разделов встречаются в `OPROSNIK-L1.md` дословно как `### `-секции, и других `### ` в файле нет. Проверка: `grep -c '^### ' OPROSNIK-L1.md` → обязано быть **12**; плюс выдерни заголовки из ленты и сверь построчно с секциями опросника. Напечатай «`### `-секций X, покрыто из 12 — Y, непокрытых: <список или ПУСТО>». **X ≠ 12 или Y ≠ 12 без названной причины — гейт красный.**
2. **ФОРМАТ ВОПРОСОВ.** `grep -c '^#### В-' OPROSNIK-L1.md` = N. У каждого из N вопросов присутствуют все шесть полей (`где` · `сейчас` · `почему спрашиваю` · `варианты` · `предложение` · `цена`). Напечатай «вопросов N, с полным набором полей N, неполных: <число>». **Неполных > 0 — гейт красный.**
3. **ВАРИАНТЫ.** У каждого вопроса либо ≥2 варианта, либо метка `ОТКРЫТЫЙ`. Напечатай «с вариантами A · ОТКРЫТЫХ B · A+B = N». **A+B ≠ N — гейт красный.**
4. **НЕ ПЕРЕСПРАШИВАЕМ РЕШЁННОЕ.** Каждый вопрос сверен с `PRAVKI-final.md` (число пунктов: `grep -c '^## ' _studio/zhurnal/2026-07-28_konspekt-l1/PRAVKI-final.md`, живое значение 30.07: **24** — 22 нумерованных пункта плюс `10½` и служебная секция адресации). Напечатай «сверено с N пунктами диктовки 29.07; отброшено как уже решённое — M вопросов, список с адресами». **M без списка — гейт красный.** Отбросить нечего — напиши «отброшено 0», молчание читается как пустота.
5. **АДРЕСА ЖИВЫЕ.** Ни один `где` не ссылается на заголовок, которого нет в ленте, и ни один вопрос не называет номер слайда. Проверка: `grep -o 'слайд [0-9]\+\|слайде [0-9]\+' OPROSNIK-L1.md | wc -l` → обязан быть **0**. Напечатай это число.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «несостыковок не найдено», а «не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 12» и «проверено 12 из 12» выглядят одинаково.

### 🔴 ЧЕТЫРЕ ЛОВУШКИ — назови в отчёте по каждой, поддался или нет

1. **НЕ ПОДЫГРЫВАЙ ГИПОТЕЗЕ Р-1 (порядок «функтор и ЕП раньше, категория в конце»).** Гипотеза владельца и аналитика; нам обоим ПРИЯТНО, если она подтвердится, и услужливый исполнитель её подтвердит. **Опровержение — полноценный успех: оно спасает от постройки на песке.** В репозитории лежат ДВА взаимно противоречащих следа, оба записаны: `MARSHRUT.md` фиксирует порядок владельца от 23.07 — «функтор (сначала Set) → что НЕ функтор ($Z(G)$) → категория внутри Set → ест. преобразование», то есть функтор ДО категории; а живой дек даёт все три определения ОДНИМ разделом «Категория, функтор, естественное преобразование». Найди, что говорит сам МАТЕРИАЛ (конспект `03-tri-opredeleniya.md` — уже своим названием другое решение), и скажи прямо, если переворот ломает что-то, чего сейчас не видно. **Брак — только выдуманный вердикт.**
2. **ЗАДАЧА УЖЕ СДЕЛАНА? — грепом, ДО того как писать вопрос.** Самый дешёвый вопрос входа и самый частый: *на этой арке он сработал бы ПЯТЬ раз за трое суток* — заход трижды ставил задачи, уже выполненные названной им же базой. Часть претензий владельца закрыта ревизией в ночь на 30.07 (иллюстраций в ленте стало 34 → 11, часть 4 без картинок, сцены переведены на накопление). Спрашивать про снятое — воровать время.
3. **`grep = 0` НЕ ДОКАЗЫВАЕТ ОТСУТСТВИЯ.** Вывод «этого в конспекте нет» законен только после чтения оглавления и раздела ГЛАЗАМИ. Греп — разведка «где смотреть», не доказательство. *Цена: «материал в `01-liniya-A.md`» при нуле вхождений искомого слова; и обратное — дайджест записал «у Ловера этого нет» по нулевому грепу, а предмет был, в самом дешёвом месте книги.*
4. **СОДЕРЖАНИЕ ЛЕКЦИИ НЕ СОЧИНЯЕТСЯ.** Формулировка, определение, пример, математическое утверждение — берутся из конспекта или ленты ДОСЛОВНО, с адресом. Если для вопроса нужно содержание, которого нет ни в одном прочитанном файле, — вопрос помечается `ОТКРЫТЫЙ`, и это правильный исход. *Цена: сводка правок, собранная пересказом, сняла бы с картинки цифры, которые владелец велел оставить, и удалила бы два примера, которых он не отменял.*

## 3. СУБАГЕНТЫ — шесть, роли назначены. Один из них ОБЯЗАТЕЛЕН как гейт

Fan-out ≤5 параллельно, вложенности не надо. Рейт-лимит = отступить и доложить, НЕ слепой ретрай. **Время ожидания мерь `date`, а не суммой своих `sleep`:** номинальная сумма набегает вдвое быстрее стенных часов, и отступать от живого субагента можно только по ВНЕШНЕМУ признаку — ошибка, отказ, рейт-лимит, — не по ощущению «долго». *Цена: исполнитель насчитал по `sleep` 43 минуты при 24 реальных, счёл поисковый субагент зависшим и отступил ЗА 7 МИНУТ ДО ФИНАЛА; запасная версия дала ОШИБОЧНЫЙ вердикт.*

**A. Пять читателей конспекта (параллельно, дешёвая модель годится).** По одному на файл: `01-krasivaya-biekciya.md` · `02-aksiomatizaciya.md` · `03-tri-opredeleniya.md` · `04-strelka.md` · `05-funktor.md`. Задание каждому одинаковое и узкое: **«выпиши, что в этом разделе конспекта развёрнуто и НЕ доехало до ленты — примеры, мотивировки, переходы, диаграммы; каждый пункт с цитатой-якорем и адресом»**. Им дать на сверку соответствующие разделы ленты. Это и есть основной источник Части I: вопрос «чего не хватает» получается вычитанием слайда из конспекта.

**B. 🔴 «СВЕЖИЙ ЗАЛ» — ОДИН субагент, и он ГЕЙТ, а не украшение.** Мандат жёсткий и обратный обычному: **он читает ТОЛЬКО `A-krasivaya.md`, `B-yazyk.md` и `src/content/s01.md`…`s12.md`, а конспект открывать ему ЗАПРЕЩЕНО.** Задание: *«ты слушатель с математическим бэкграундом, но категорий не знаешь. Читай подряд и отмечай КАЖДОЕ место, где потерялся: непонятно, что происходит · переход ничем не мотивирован · формулировка оборвана · термин употреблён раньше, чем определён · пример не объясняет то, ради чего приведён. Каждое место — с цитатой»*.

**Почему это гейт, а не бонус.** 🔴 **Критерий «текст понятен» не проверяется автором текста ни в каком виде: проверяющий обязан НЕ ВИДЕТЬ источника** — автор достраивает недостающее из головы, потому что помнит конспект. *Цена на этой же арке: самопроверка автора ленты по критерию полноты дала «неполных формулировок 0»; свежий читатель, не видевший конспекта, нашёл **26 мест** — 10 «непонятно, что происходит», 5 немотивированных переходов, 11 оборванных формулировок, — и автор честно переписал свой вердикт с 0 на 5. Самая дорогая находка: строка ссылалась на факт, которого на слайдах не было нигде.* Владелец говорит, что зал не понял, что такое биекция; этот субагент — единственный инструмент, который такое видит ДО зала. **Его находки идут в Часть III ЦЕЛИКОМ, включая те, с которыми ты не согласен: несогласие пиши рядом, но находку не выбрасывай** — судить будет владелец.

**Гейт по B:** в отчёте — число «свежий зал нашёл K мест, в Часть III внесено K». **Внесено < K — гейт красный.**

⚠ **Мандат чтения субагента B шире запрета только в одну сторону:** ему МЕНЬШЕ разрешено, чем тебе, и это по замыслу. Не «помоги ему» подсказкой из конспекта — подсказка убивает весь смысл замера.

**Лоссового верификатора не нужно:** заход ничего не двигает, не удаляет и не сжимает — файлы лекции он вообще не трогает.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/2026-07-30_dovodka-l1                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/2026-07-30_dovodka-l1   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-07-30_dovodka-l1   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone _studio/zhurnal/2026-07-30_dovodka-l1 \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

**Плюс шесть блоков, специфичных для этого захода — без них отчёт не принимается:**
1. **Пять чисел критерия §2**, каждое с командой, которой получено: покрыто X из 12 · вопросов N, неполных — · с вариантами A + ОТКРЫТЫХ B = N · сверено с диктовкой, отброшено M (список) · номеров слайдов 0.
2. **Таблица охвата:** двенадцать строк «раздел → сколько вопросов», у нулей — причина.
3. **Свежий зал:** нашёл K мест, внесено в Часть III K. Если расходится — почему, поимённо.
4. **Четыре ловушки §2:** по каждой одна строка «поддался / не поддался», и **по ловушке 1 — прямой вердикт по гипотезе Р-1**: подтверждается материалом, опровергается, или материал молчит. Опровержение — успех; вежливое «скорее да» без доказательства — брак.
5. **Что в конспекте оказалось богаче слайда** — три-пять самых дорогих находок субагентов A, с адресами. Это то, что владелец увидит первым.
6. **Ложные предпосылки самого захода.** Нашёл в этом файле неверный адрес, устаревшее число, требование, противоречащее живому файлу, — напиши. *На этой арке заход спасал сам исполнитель пять раз, и каждый раз в `## ПЛАН` ДО работы.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Служебное поле ленты (`поле:mn Раскладка`, строка `🖼`) никем не сверяется с порождённым слайдом, и устаревшее поле СКРЫВАЕТ невыполненный заказ владельца

Лента — источник, слайд — порождаемое. Но служебные поля ленты описывают то, чего в порождённом слайде может уже не быть, и ни один гейт этого не ловит: `check_view.py` смотрит отрисовку, `gejt_lenty.py` — структуру ленты, а соответствие «обещание раскладки ↔ наличие `<figure>` в слайде» не проверяет никто. Три живых случая в одной прочитанной половине, все найдены только потому, что заход считал фигуры руками:
- `Три поломки на языке видов`: раскладка обещает «вертикальная полоса справа: полный цикл на четырёх точках» — в `src/slides/s05.html` фигур 0.
- `Резюме`: лента несёт `🖼 Портрет Андре Жуайаля {1}` — в `src/slides/s07.html` фигур 0.
- `Список функторов`: портрет Фробениуса заказан в конспекте прямо (`TZ §5` п.6) — фигур 0.

**ЦЕНА:** заплачено трижды, и по-разному. **(1)** Поверх устаревшего поля в `Три поломки` аналитик написал врезку «⚠ Картинка и доказательство разошлись — решение за владельцем», предлагающую владельцу выбор между двумя вариантами картинки, **которой не существует** — то есть в ленте лежит готовый вопрос владельцу впустую, и он дожил до этого захода. **(2)** Субагент «свежий зал», работавший по ленте, принял обещание за факт и выдал ложную находку: «полминуты искал в тексте четвёртую точку и решил, что чего-то не понял в доказательстве» — то есть устаревшее поле произвело брак в замере, ради которого субагента и звали. **(3)** Заказ владельца с репетиции (8.3, дословно *«иллюстрацией здесь должна быть вот эта картинка, которую я тебе произнёс, про один-два, про множество из двух элементов»*) остался невыполненным **незамеченным**, потому что раскладка утверждала, что картинка есть; и ровно на это место владелец пожаловался 30.07 второй раз — «не было диаграммы». Один заказ, два дня, две жалобы, и всё это время ленту читали как доказательство, что картинка стоит.

**Чему покраснеть (иначе это не правило, а надежда, `KONSTITUCIYA §11`):** гейт на пару «лента → порождённый слайд», сверяющий обещания служебных полей с фактом: раздел, чья раскладка содержит «полоса… : <что-то>» или строку `🖼`, обязан дать `<figure>`/`<svg>` в своём `src/slides/sNN.html`, иначе `error` с адресом раздела. Проверяется теми же двумя грепами, которыми это нашёл заход: `grep -c '<figure' src/slides/sNN.html` против наличия обещания в разделе ленты. Гейт дешёвый, а ловит класс, который сейчас виден только при ручном пересчёте фигур — и который уже дал владельцу повторную жалобу на то же место.

## ПЛАН — (заполняет исполнитель)

### 0. Первый ход — сделано до плана (read-only разведка)
- `git branch --show-current` → `arka/mat-kostyak` ✅ (rc=0). Ветка та, `--worktree` не нужен.
- `git_zona.py check --zone _studio/zhurnal/2026-07-30_dovodka-l1` → ✅ rc=0, «работа доехала в git, вне git ничего нет». **Зона чиста в HEAD ⇒ точку отката НЕ фабрикую** (§0 прямо запрещает фабриковать при чистой зоне).

### 1. Четыре предпосылки аналитика — ПРОВЕРЕНЫ ВСЕ ЧЕТЫРЕ, все подтвердились
| # | предпосылка | вердикт | чем проверено |
|---|---|---|---|
| 1 | три определения одним разделом, ЕП = одна строка + один пример | ✅ **подтверждена** | `B-yazyk.md` раздел `Категория, функтор, естественное преобразование` = `s08.md`: категория 4 пункта, функтор 1 строка, ЕП 1 строка + «Пример: матрица перехода между двумя базисами». Мэппинг `s08` проверен по содержимому, не по номеру |
| 2 | квадрат естественности введён дважды, не развёрнут ни разу | ✅ **подтверждена** | `grep -c 'квадрат'` → A=4, B=2. В A: `Требование красоты` (буквы $E,E',\sigma,\varphi$, figcaption про $\binom n2\leftrightarrow\binom n{n-2}$) + `Три поломки` («квадрат не коммутирует»). В B: `<figure>` с $FA,FB,GA,GB$, figcaption дословно «Этот квадрат уже стоял в требовании красоты» |
| 3 | `Конструкции` и `Категории: множество со структурой` — самые пустые | ✅ **подтверждена по существу** | `wc -c`: s09=692, s10=**554** (минимум из 12). Оба несут в раскладке «полоса справа пустая — иллюстраций у слайда нет». ⚠ Уточнение: s02=635 и s04=681 короче s09 по знакам, но у них полоса занята иллюстрацией, поэтому «пусто на экране» верно именно для s09/s10 |
| 4 | иллюстраций в прочитанной половине шесть | ✅ **подтверждена точно** | `grep -c data-ill` → s02=1, s03=1, s06=1, s08=**2**, s11=1, остальные 0. Сумма 6, совпало пословно с числами захода |

### 2. Что я нашёл СВЕРХ предпосылок — три факта, меняющие адресацию
1. 🔴 **Мэппинг «раздел → файл» сдвинут на единицу относительно интуиции, и заход тут ПРАВ.** Титульный раздел `Зачем нужны категории` в `src/content/` файла НЕ имеет (он в `slides/sl-title.html`), поэтому `s01.md`=`Тождества на доске`, …, `s07.md`=`Резюме`, `s08.md`=`Категория, функтор, ЕП`, …, `s12.md`=`Список функторов`. Установлено чтением содержимого всех двенадцати файлов, а не счётом. Адрес `s08` в предпосылке 1 и `s09/s10` в предпосылке 3 — верные.
2. 🔴 **Арифметика дека сошлась и подтвердила «прочитано ровно 15»:** `ls src/slides/` → `s01…s26` + шесть служебных (`sl-title`, `sl-vizitka`, `sl-divider1..3`, `sl-thanks`) = **32** ✅. Первые 15 = титул + визитка + `s01…s12` + `sl-divider1` = 1+1+12+1. То есть «12 содержательных + 3 служебные» — не оценка захода, а факт дека.
3. 🔴 **НАХОДКА, которой в заходе нет: два слайда прочитанной половины потеряли иллюстрацию, а раскладка в ленте всё ещё её описывает.** `s05.html` (`Три поломки на языке видов`) и `s07.html` (`Резюме`): `figure=0 svg=0 data-ill=0`. При этом лента `Три поломки` в поле `> поле:mn Раскладка` обещает «вертикальная полоса справа: полный цикл на четырёх точках», и там же стоит целая врезка «⚠ Картинка и доказательство разошлись — решение за владельцем», обсуждающая, менять ли картинку на две точки. **Картинки нет вообще — врезка обсуждает несуществующий объект.** У `Резюме` лента несёт строку `🖼 Портрет Андре Жуайаля {1}`, в слайде портрета нет. Это ровно адрес двух претензий владельца 30.07 (диаграмма к инъекциям/непереставляемости и «чётные/нечётные плохо выписано») — веду их в Часть II.

### 3. Ложных предпосылок захода не найдено — но найдены две границы, которые назову слепой зоной
- **Конспект — 12 файлов, а не 5.** Заход называет `01`…`05` (~185 КБ — цифра точна: 36921+53642+28239+26810+39910=185522). Разведочный греп по всем двенадцати показывает, что материал прочитанной половины действительно сидит в `01`–`03` (`изоморфизм видов`: 02→5, 03→1, 12→1, остальные 0; `чётн`: 02→12, 01→3; `Жуайал`: 02→12, 03→2, 05→1), а `06`–`12` — вторая лекция. **Пятифайловая рамка обоснована, предпосылку не опровергаю**; но 7 файлов (~245 КБ) вне мандата — в Часть V.
- **Дек глазами не вижу** (браузера нет) ⇒ всё про ВИД — вне мандата, в Часть V прямой строкой.

### 4. Ловушка 2 отработана ДО написания вопросов — сверка с диктовкой 29.07
`grep -c '^## ' PRAVKI-final.md` → **24** (совпало с заходом). Из 24 позиций прочитанной половины касаются §1–§11 и §10½; §12–§22 — вторая лекция. Прогнал каждую против живой ленты; **закрыто ревизией 29–30.07 и потому спрашивать НЕЛЬЗЯ:** §1 визитка (`sl-vizitka.html` есть) · §2 фраза-комментарий про Фибоначчи (в ленте нет, само тождество 8 на месте) · §3 буквы $E/E'$ + подпись + переезд «композиция уважается» (всё в `Требовании красоты`) · §4 центрирование + снятые иллюстрации + «графы с вершинами $U$» · §5 тождество на сцене 1 + короткий ответ + снятая картинка · §7 третья иллюстрация «два базиса» · §8 обе картинки + «Определение» · §9 пять картинок + группоид + нижняя фраза · §10 переписан списком + «штаны» переехали · §11 заголовок «Список функторов» + картинки.
**Осталось ОТКРЫТЫМ и потому законно спрашивать:** §8 хвост («останется пустое место — добавить общие понятия, в т.ч. диаграмму» — не сделано, s09 пуст) · §9 частично ($\mathbf{Mat}$ по врезке ленты УТРАЧЕН, а не переехал) · §10½ (эквивалентности категорий в части 2 нет; приоритет 29.07 прямо писал «НЕ УСПЕВАЕМ: эквивалентность категорий в части 2»).

### 5. Дальше — порядок ходов
1. **Пять субагентов A параллельно** (конспект `01`…`05`, каждому свой файл + соответствующие разделы ленты): «что развёрнуто в конспекте и НЕ доехало до ленты». Источник Части I.
2. **Субагент B «свежий зал»** — мандат УЖЕ, чем мой: только `A-krasivaya.md`, `B-yazyk.md`, `s01…s12.md`; конспект запрещён. Гейт. Пускаю в той же волне, но БЕЗ подсказок из конспекта.
3. Мой собственный ход по **ловушке 1**: читаю `03-tri-opredeleniya.md` глазами (заход прямо велит) + `MARSHRUT.md` уже прочитан — вердикт по Р-1 выношу сам, не субагентом, чтобы не размазать ответственность.
4. Греп `repeticia/PRAVKI.md` по заголовкам и словам, охват назову числом.
5. `UPRAZHNENIYA-L1.md` — разметка восьми упражнений по частям + кандидаты ИЗ конспекта с адресами.
6. Сборка `OPROSNIK-L1.md`, пять гейтов командами, коммит двумя ходами.

**Критерий готовности кривым не считаю** — гейты 1–5 исполнимы и могут провалиться по-настоящему; замечаний к нему нет. Единственное, что уточняю: гейт 1 требует «`### ` ровно 12», поэтому Части 0, II, III, IV, V внутри себя `###` не употребляют — вопросы там сидят прямо на `####`.

## ВОПРОСЫ — (заполняет исполнитель)

### 1. РЕГИСТРАЦИЯ в `KARTA.md §6` — нового документа НЕ ЗАВОДИЛ, нужна ЗАМЕНА одной скобки
🔴 Проверил перед тем, как писать готовую строку: **`OPROSNIK-L1.md` в `§6` УЖЕ зарегистрирован** аналитиком заранее, строка 83, в конце записи арки — `· **`OPROSNIK-L1.md`** (продукт захода №1; на момент регистрации не собран).` Поэтому новой строки не нужно, нужен один ход: заменить скобку «на момент регистрации не собран» на факт. Готовый текст замены (аналитику — вставить вместо старой скобки, `_studio/docs/` мне READ-ONLY):

> · **`OPROSNIK-L1.md`** (**продукт захода №1, ✅ собран 30.07. 91 вопрос в шести частях, охват 12 из 12 разделов прочитанной половины, все пять гейтов зелёные. Часть 0 — четыре архитектурные развилки, и главная из них числовая: живой темп 6 мин/позиция против рамки владельца 3,75 (2 ч ÷ 32) даёт при двухчасовой встрече бюджет 18 позиций, то есть рост от нынешних 15 всего **+3** — отсюда вывод, что «дадим больше слайдов» задачу не решает и место под разворот ЕП берётся только из реза части 1. Вердикт по гипотезе переворота (ловушка 1): материал поддерживает ЗАМЫСЕЛ (конспект `03` сам пишет «мотивационный вес несут функтор и ЕП, а категория подаётся служебно»; примеры 26–28 употребляют слово «функтор» до определения 30; утв. 44 — вид уже функтор), но БУКВАЛЬНУЮ форму «категория в самом конце» ОПРОВЕРГАЕТ: определение 38 определяет функтор через «категорию» дважды, его аксиомы названы «про сохранение структуры категории», определение 40 опирается на 38 и 32 — переворот требует переписывания определений, а не перестановки, и отменяет прежний вердикт владельца «бессмысленно шестой раздел делать до определения категории». Часть III несёт 37 находок субагента «свежий зал» (конспект ему был запрещён) — 13 «непонятно, что происходит», 10 «термин раньше определения», 7 оборванных формулировок, 4 немотивированных перехода, 3 неработающих примера; хуже всего он потерялся на трёх последних экранах части 1. Часть IV: восемь имеющихся упражнений размечены — части 1–2 получают **0**, и предложено 19 кандидатов, все вытащены из конспекта с адресами, ни один не сочинён. Три вопроса помечены `ОТКРЫТЫЙ` — там, где нужного содержания нет ни в одном файле: источник вердикта «тождество 8 — не вид», и чем иллюстрировать ЕП (пример «матрица перехода» источника в конспекте НЕ имеет вообще)**).

### 2. Два расхождения ленты и слайда — решать владельцу, я не правил (READ-ONLY вне зоны)
Нашёл при замере, в задании их нет; оба сидят в `teorkat-vvedenie/`, то есть вне моей зоны, поэтому только докладываю.
- **`Три поломки на языке видов`:** раскладка ленты обещает «вертикальная полоса справа: полный цикл на четырёх точках», а в `src/slides/s05.html` фигур **ноль**. Поверх этого в ленте стоит врезка «⚠ Картинка и доказательство разошлись — решение за владельцем», предлагающая выбор между двумя вариантами картинки, **которой не существует**. Заказ владельца с репетиции (8.3, «картинка про множество из двух элементов») не исполнен.
- **`Резюме`:** лента несёт строку `🖼 Портрет Андре Жуайаля {1}`, в `src/slides/s07.html` фигур **ноль**. Тот же класс — и тот же класс в `Списке функторов`, где портрет Фробениуса заказан в конспекте прямо (`TZ §5` п.6) и тоже отсутствует.

### 3. Одно место, где я мог склеить две претензии в одну — проверьте меня
Претензия «нужно было показывать инъекции и то, что там что-то не переставляется, не было диаграммы» разложена мной на ДВА адреса (В-44): «не переставляется» → `Три поломки` (часть 1, моя зона), «инъекции» → пропавший слайд про инъекции из вашей диктовки §12.2 (часть 3, вторая лекция). Если вы имели в виду один адрес — скажите какой, вывод В-44 тогда меняется.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и ЗАЧЕМ.** Собрал `OPROSNIK-L1.md` — 91 вопрос в шести частях по прочитанной половине Л1. Зачем именно так: заход требовал не план правок, а артефакт для одного сеанса владельца, поэтому каждый вопрос несёт шесть полей, включая `цена` (список сортируется по «заметность × дешевизна», а не по тяжести дефекта) и `варианты` (≥2 либо `ОТКРЫТЫЙ`, чтобы вопрос не был замаскированным решением за владельца). Ни один файл лекции не тронут: дек, лента, конспект и упражнения читались только на чтение.

**Как проверил.** Пять гейтов критерия §2, прогнаны командами, числа ниже. Плюс девять цитат-якорей субагентов сверил грепом по живым файлам — все девять нашлись (`Полный цикл работает при всех чётных`, `транспозиция $(1\,2)$ обнаруживает препятствие только при $n=2$`, `1,2,4,4,8,8`, `морфизмы $n\to m$ — матрицы размера $m\times n$`, `нерастягивающ`×2, `R=\{(1,2)\}`, `S=\{f\ge1\}`, `инволюц`=0). Конспект `03-tri-opredeleniya.md` прочитал ЦЕЛИКОМ сам, не полагаясь на субагента, потому что от него зависит вердикт ловушки 1 и размазывать такую ответственность нельзя.

**Что НЕ трогал.** Всё вне `_studio/zhurnal/2026-07-30_dovodka-l1`: ленты, `src/content/*`, `src/slides/*`, конспект, `UPRAZHNENIYA-L1.md`, `PRAVKI-final.md`, `repeticia/PRAVKI.md`, `MARSHRUT.md`, `SPEKA`, `DUH`, `KARTA.md`. Порождающий слой `src/content/` не правил даже мысленно. Ветку не переключал.

**Время прогона.** ~50 минут стенных часов (замер по `date`: старт 03:39:53, гейты 04:2x). **Токены:** субагенты — 43,5к + 47,1к + 30,4к + 28,8к + 36,7к + 32,5к ≈ **219к**; основной контекст — порядка 190к. Итого ≈ 410к.

### 1. Пять чисел критерия §2, каждое с командой

| гейт | команда | результат |
|---|---|---|
| 1 · охват | `grep -c '^### ' OPROSNIK-L1.md` + построчный `comm` с заголовками ленты | `### `-секций **12**, покрыто из 12 — **12**, непокрытых: **ПУСТО**, лишних: **ПУСТО** ✅ |
| 2 · формат | `grep -c '^#### В-'` + пер-блочная проверка шести полей (python) | вопросов **91**, с полным набором полей **91**, неполных **0** ✅ |
| 3 · варианты | подсчёт метоk `(а)…(д)` либо слова `ОТКРЫТЫЙ` в каждом блоке | с вариантами **88** · ОТКРЫТЫХ **3** · A+B = **91** = N ✅ |
| 4 · не переспрашиваем решённое | `grep -c '^## ' PRAVKI-final.md` = **24**; каждый пункт прогнан против живой ленты | сверено с **24** пунктами, отброшено как уже решённое — **11**, список ниже ✅ |
| 5 · адреса живые | `grep -o 'слайд [0-9]\+\|слайде [0-9]\+' OPROSNIK-L1.md \| wc -l` | **0** ✅ (расширенная проверка на `слайда/слайдов/слайды` + число — тоже 0) |

**Отброшено как уже решённое — 11 пунктов диктовки 29.07, с адресами и способом проверки:**
1. **§1 визитка** → `ls src/slides/` даёт `sl-vizitka.html`. 2. **§2 фраза-комментарий про Фибоначчи** → в ленте единственное вхождение «фибоначч» сидит в служебном `поле:mn` про порядок строк, а не в тексте; в `s01.md` только само тождество с $F_{n+2}$. 3. **§3 буквы $E$/$E'$ + подпись $\binom n2\leftrightarrow\binom n{n-2}$ + переезд «композиция уважается»** → всё три в `Требовании красоты`. 4. **§4 центрирование + снятые иллюстрации + «графы с вершинами $U$»** → `Галерея видов`, все три греп-положительны. 5. **§5 тождество на первой сцене + короткий ответ + снятая картинка** → `Три поломки`; жирный вопрос перенесён раньше, он теперь в `Три состояния элемента`. 6. **§6 «слайды 3, 7, 8 нормально, не трогать»** → спрашивать нечего по определению. 7. **§7 третья иллюстрация «два базиса»** → в ленте упомянута только как СНЯТАЯ. 8. **§8 обе картинки + текст на всю ширину + слово «Определение»** → `Конструкции`, `grep -c '\*\*Определение'` = 0. 9. **§9 пять картинок + группоид + нижняя фраза + $\mathbf{Rel}$ в конце** → `Категории: множество со структурой`, «группоид» в ленте B = 0. 10. **§10 переписан списком + картинки убраны + «штаны» перенесены** → `Стрелка как следование и как шаг`, кобордизм на месте. 11. **§11 заголовок «Список функторов» + картинки убраны** → заголовок ровно такой.
⚠ **НЕ отброшено, потому что открыто:** хвост §8 (пустое место → добавить общие понятия, в т.ч. диаграмму) → В-31 · §9 в части $\mathbf{Mat}$ (по врезке ленты УТРАЧЕН, а не переехал) → В-32 · §10½ (эквивалентности категорий в части 2 нет; приоритет того вечера прямо писал «НЕ УСПЕВАЕМ») → В-39.

### 2. Таблица охвата — 12 строк, считано командой по полю `где`
Считаю по адресу вопроса, а не по месту в файле: находки Части III адресованы в те же разделы, и охват без них соврал бы вниз.

| раздел | вопросов (всего, по полю `где`) | из них в Части I |
|---|---|---|
| Тождества на доске | 6 | 3 |
| Три состояния элемента | 4 | 2 |
| Требование красоты | 10 | 3 |
| Галерея видов | 6 | 3 |
| Три поломки на языке видов | 11 | 4 |
| Менять надо не биекцию, а арену | 8 | 3 |
| Резюме | 4 | 2 |
| Категория, функтор, естественное преобразование | 11 | 4 |
| Конструкции | 4 | 3 |
| Категории: множество со структурой | 6 | 3 |
| Стрелка как следование и как шаг | 6 | 3 |
| Список функторов | 15 | 9 |

**Нулей нет ни в одной строке**, поэтому объяснять нули не нужно. Вне двенадцати разделов адресованы 6 вопросов: 5 — «вся лекция» (Часть 0 и В-84), 1 — вне прочитанной половины (В-85, врезка упражнений, ваш незакрытый вопрос с 29.07). Сумма адресаций 97 при 91 вопросе — шесть вопросов адресованы в несколько разделов сразу (В-43 в три, В-83 в три, В-41 в два).

### 3. Свежий зал
**Нашёл K = 37 мест, в Часть III внесено 37.** Расхождения нет. Разбивка его собственная: непонятно, что происходит — 13 · термин раньше определения — 10 · формулировка оборвана — 7 · переход не мотивирован — 4 · пример не объясняет того, ради чего приведён — 3. Внесены все, включая шесть, где я с ним не согласен или где находка дублирует мой вопрос: несогласие и дубль помечены в поле `предложение` (В-52, В-55, В-59, В-68, В-75, В-80, В-82), находка не выброшена — судить владельцу. Три места, где он потерялся хуже всего: `Требование красоты`, `Три поломки на языке видов`, `Менять надо не биекцию, а арену` — то есть три ПОСЛЕДНИХ содержательных экрана части 1; после третьего он ушёл, не понимая, какой вывод должен унести, и потому мотивировка всей части 2 до него не дошла. Это независимо подтверждает диагноз аналитика: плотность части 1 за пределом усвоения именно к её концу.
Мандат соблюдён: конспект субагенту был запрещён, подсказок из конспекта я ему не давал.

### 4. Четыре ловушки §2

**Ловушка 1 — не подыгрывать гипотезе Р-1: НЕ ПОДДАЛСЯ.** Прямой вердикт: **гипотеза подтверждается по замыслу и опровергается в буквальной форме.** Что поддерживает: конспект `03` сам пишет во врезке — *«мотивационный вес несут функтор и естественное преобразование, а категория подаётся служебно — как арена, на которой они живут»*; примеры 26–28 употребляют слово «функтор» физически раньше определения 30; утверждение 44 говорит, что вид — уже функтор, а изоморфизм видов — уже естественный изоморфизм, то есть часть 1 фактически учит обоим понятиям до всякой категории. Что опровергает: определение 38 определяет функтор через «объекту $A$ **категории** $\mathcal C$… объект $FA$ **категории** $\mathcal D$», его аксиомы названы «про сохранение **структуры категории**», а определение 40 опирается на 38 и на определение 32 — **переворот требует переписать определения, а не переставить блоки**, и такого текста нет ни в одном файле. Плюс три препятствия, которые я обязан назвать, потому что они не видны: конспект прямо аргументирует против дробления тройки (*«у промежуточного стыка своего содержания нет»*); существует **прежний вердикт владельца** *«бессмысленно шестой раздел делать до определения категории»*, который переворот отменяет и который надо отменить вслух; обходные формулировки живут на ОДНОЙ арене и ломаются, когда нужна вторая (замечание 29). И отдельно: **два следа владельца расходятся между собой** — `MARSHRUT.md` от 23.07 ставит категорию ТРЕТЬЕЙ из шести, слова 30.07 — последней. Поэтому в В-01 предложен вариант (б), мягкий, а не (а): он получает желаемое без нового математического текста.

**Ловушка 2 — задача уже сделана? НЕ ПОДДАЛСЯ.** Прогнал все 24 пункта диктовки против живой ленты ДО написания вопросов; 11 отбросил (список в блоке 1). Три пункта, которые выглядели закрытыми, но при проверке оказались открытыми, оставил вопросами и назвал причину (В-31, В-32, В-39).

**Ловушка 3 — `grep = 0` не доказывает отсутствия: НЕ ПОДДАЛСЯ.** Везде, где утверждаю отсутствие, есть чтение глазами: `03-tri-opredeleniya.md` прочитан целиком мной; «инволюции нет в конспекте `02`» — греп 0 ПЛЮС просмотр раздела `## Галерея видов` субагентом; «примера с матрицей перехода в конспекте нет» — субагент отчитался, что просматривал файл целиком, «не только грепом», и я проверил это независимо своим чтением того же файла. Обратный случай тоже отработал: греп по всем двенадцати файлам конспекта показал, что пятифайловая рамка захода обоснована, — но в отчёт это идёт как разведка, а не доказательство (Часть V, п. 3).

**Ловушка 4 — содержание лекции не сочиняется: НЕ ПОДДАЛСЯ.** Ни одного своего математического примера, определения или утверждения в опроснике нет. Три вопроса помечены `ОТКРЫТЫЙ` именно там, где нужного содержания нет ни в одном прочитанном файле: **В-07** (откуда вердикт «тождество 8 — не вид»; в конспекте `01` тождество 8 не разобрано вообще, а на репетиции владелец сказал про него две взаимно противоположные вещи), **В-25** и **В-72** (чем иллюстрировать ЕП; примера «матрица перехода» в конспекте `03` нет вообще, а картинку под него владелец снял как непонятную — значит рисовать заново, а не восстанавливать). Где предлагал направление, помечал предложением, а не фактом.

### 5. Что в конспекте оказалось богаче слайда — шесть самых дорогих находок
1. 🔴 **В напечатанном тексте всей ленты блока A единственная полная формула биекции — та, которая НЕ работает** ($S\mapsto S\triangle\{x_0\}$, приведена как требующая выбора). Работающая биекция лекции (три состояния) отдана рисунку и подписи; её обратное правило ($S=\{f\ge1\}$, $T=\{f=2\}$) — единственное место во всём конспекте, где показано, ПОЧЕМУ отображение биекция, — не доехало. Из семи конструкций биекций конспекта доехали три, ни одна полностью. Это прямой адрес претензии «люди не понимают, что такое биекция».
2. 🔴 **Доказательство неизоморфности $E^+$/$E^-$ доказывает не то, что заявлено.** На слайде утверждение «при чётном $n$», а инструмент — транспозиция при $n=2$, про которую конспект прямо предупреждает: *«уже при $n=4$ она даёт по 4 неподвижные точки с обеих сторон и ничего не доказывает. Полный цикл работает при всех чётных $n$ сразу»*. Общий аргумент готов и не доехал; критерий «изоморфизм сохраняет число неподвижных точек» тоже срезан, поэтому не видно, что запрет универсален по $\varphi$.
3. 🔴 **Раздел конспекта `## Подводка к общим определениям` не доехал НИ СТРОКОЙ** — три конкретных функтора до определения функтора (образ, прообраз с переворотом стрелки, $X\mapsto X^X$ где не удаётся) плюс единственная фраза корпуса, объявляющая тройку определений ответом на два запроса. Это и есть названный владельцем «прыжок по сложности без примеров», и без примера 28 аксиомы функтора выглядят пустыми.
4. 🔴 **Классификация ТКТП потеряна** — теорема 256 (*«функторы… находятся во взаимно однозначном соответствии с коммутативными алгебрами Фробениуса»*), которую конспект помечает как *«лучший ответ лекции на вопрос "а сколько бывает функторов"»*. На слайде остался ярлык «называется топологической теорией поля». Вместе с ней со слайда пропало слово «Фробениус» и заказанный владельцем портрет.
5. **$\mathbf{Rel}$ делает заголовок своего экрана ложным.** Экран заявляет «Стрелка — отображение, сохраняющее структуру», а $\mathbf{Rel}$ это опровергает; конспект называет подвох главным на экране и держит готовый контрпример ($X=\{1,2\}$, $R=\{(1,2)\}$), который не доехал. Свежий читатель нашёл ровно то же независимо.
6. **$\mathbf{Mat}$ восстанавливается из конспекта ПОЛНЕЕ, чем стоял на ленте** — с направлением стрелки (морфизмы $n\to m$ суть матрицы $m\times n$; врезка ленты направления не даёт) и с подвохом «объект здесь не множество», который на ленте не звучит нигде, хотя заголовок экрана утверждает обратное. Обращаться к `git show`, как советует врезка, не нужно.

### 6. Ложные предпосылки самого захода — НЕ НАЙДЕНО НИ ОДНОЙ, и это честный результат
Все четыре предпосылки аналитика подтвердились, две — пословно (числа иллюстраций `s02`=1, `s03`=1, `s06`=1, `s08`=2, `s11`=1; счёт разделов 8+5). Адреса: все девять путей существуют, `s08`/`s09`/`s10` в предпосылках 1 и 3 указаны верно (проверил не по номеру, а по содержимому всех двенадцати файлов). Числа: «~185 КБ на пять файлов» — точно (185522). Список двенадцати заголовков — совпал дословно, `comm` дал ноль расхождений.
**Три уточнения, не опровержения:** (1) предпосылка 3 говорит «пусты на 45 % и 50 %» — по знакам `s02`=635 и `s04`=681 короче, чем `s09`=692, но у них полоса занята иллюстрацией, поэтому «пусто на экране» верно именно для `s09`/`s10`, и `s10`=554 — минимум из двенадцати; (2) конспект — 12 файлов, а не 5, и рамку из пяти я подтвердил разведкой, но не чтением (Часть V, п. 3); (3) гипотеза захода о том, какие разделы ленты питает конспект `01`, оказалась неполной — читатель нашёл четыре раздела вместо двух, и поправил меня сам, как и просилось.
**Две находки СВЕРХ задания**, которых в заходе нет: расхождение раскладки ленты со слайдом в `Три поломки` (врезка обсуждает несуществующую картинку) и в `Резюме` (портрет заказан, фигуры ноль) — вынесены в `## ВОПРОСЫ` п. 2 и в урок фабрике.

### 🔴 НЕОБРАТИМОЕ
**Необратимого нет.** Ни одного удаления, перезаписи, переименования, перемещения, `git reset`/`checkout`. Заход только читал файлы лекции и создал один новый файл в своей зоне. Единственные записи на диск: `OPROSNIK-L1.md` (новый) и секции `## ПЛАН` / `## ВОПРОСЫ` / `## ОТЧЁТ` в этом файле. Точку отката не фабриковал — зона была чиста в HEAD, `git_zona.py check` подтвердил до начала работы.

**КОММИТ:** `38b7e3e` — «kod_opros: OPROSNIK-L1.md собран — 91 вопрос, охват 12 из 12…» · `git_zona.py check --zone _studio/zhurnal/2026-07-30_dovodka-l1` → **✅** «работа доехала в git, вне git ничего нет», rc=0 · `git show --stat` → ровно два пути, оба мои (`OPROSNIK-L1.md` +890, `kod_opros.md` +132), чужого не подмёл. Плюс второй коммит с этой финальной строкой отчёта.

**Хук `pre-commit` покраснел на МОЁМ пути — починил, не обошёл** (§4). Первая попытка коммита упала `rc=1` с одним нарушением в `OPROSNIK-L1.md:389`: ссылка на секцию «Ступени» в `SPEKA.md`, которой там нет. Проверил по живому файлу: заголовков `### ` в `teorkat-vvedenie/SPEKA.md` одиннадцать, а «Ступени Л1 / Л2» — не заголовок, а **выделенный блок внутри** `### Охват — ЧТО входит` (строка 99 при границах секции 80–115). Ссылку исправил на `SPEKA §Охват` с указанием блока и даты решения владельца. `--no-verify` не применял: обходить было нечего — красное стояло на моём тексте и было настоящей ошибкой, я сослался на секцию, которой нет. Остальные мои ссылки (`DUH §2а`, `§3`, `§7 Т7`, `§8`, `SPEKA §Формат единицы`) ворота прошли.

⚠ **И тот же гейт покраснел второй раз — уже на ОПИСАНИИ починки.** Когда я вписал в эту строку отчёта неисправную ссылку дословно, как цитату ошибки, ворота прочли цитату как живую ссылку и снова дали `rc=1` на `kod_opros.md`. Гейт тут не неправ: отличить «сослался неверно» от «привёл неверную ссылку в кавычках» он не может. Обошёл содержательно — переписал фразу так, чтобы неисправная форма не встречалась буквально. **Полезно знать всем будущим заходам: отчёт о починке битой ссылки нельзя писать, воспроизводя битую ссылку — ворота завалят коммит на самом отчёте.**

**Долг, не мой и не чинил:** инструмент сообщает «3 коммита есть только на этой машине». Из них мой — один; остальные чужие, рядом в дереве лежат 12 изменённых путей вне моей зоны (в т.ч. `_studio/docs/`, `catalan/`, `lsh-2026-…`, арка `dovodka-fabriki`). **`--push` не делал намеренно:** заход в §4 push не требует, а вывозить вместе со своим два чужих незакоммиченных-невывезенных состояния — не моё решение. Нужен ваш ход, если вывезти надо.
