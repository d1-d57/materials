# Канал исполнителя — perekomponovka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — решение «какой абзац едет на какой экран» есть суждение о математическом тексте, оно рождается по ходу чтения и заранее не расписывается.
> ⏱ **Спешки нет. Метрика — ПОЛНОТА (ни один блок не потерян) и ЧЕСТНОСТЬ ДЫР. Часы — норма.**

КОНТЕКСТ. Проект `teorkat-vvedenie/` — лекция «Зачем нужны категории», прочитана 29.07 и успела ровно половину: 15 слайдов из 32, то есть части 1 и 2 целиком. 🔴 **Жанр сменился: из прочитанной половины делается не новая лекция, а ЗАПИСКИ — слайд-конспект, который выкладывают в интернет постфактум, чтобы люди прочитали и разобрались.** Лектора нет, бюджета времени нет. Владелец: *«мы не делаем новую версию лекции — мы делаем слайд-конспекты к старой лекции, чтобы люди их прочитали»*.

Владелец продиктовал новый ХРЕБЕТ (в §2) и назвал сквозную мысль записок: *«по сути, она про то, как идеи комбинаторных видов подводят к идее категорий»*. Порядок частей 1–2 при этом почти не меняется — он **раздвигается**: *«порядок примерно правильный, просто нужно чуть-чуть это раскрыть и сделать эту часть больше слайдов, больше понятного объяснения»*.

ЦЕЛЬ: **составить ПЛАН ПЕРЕКОМПОНОВКИ — таблицу «новый экран → какие блоки существующего текста на него едут», по адресам.** Существующий текст раскладывается по большему числу экранов так, что **ни на одном экране не становится больше текста, чем было**, и **ни один блок не теряется**. Места, где по хребту нужен текст, которого нет, остаются ПУСТЫМИ и объявляются дырами с размером.

🔴 **СТОП ДО ЦЕЛИ, простыми словами, и это главное в заходе.** Ты выдаёшь **ТОЛЬКО ПЛАН-ТАБЛИЦУ**. Ты **НЕ правишь ленту**, **НЕ пишешь ни одного нового предложения**, **НЕ удаляешь ни одного существующего**, **НЕ собираешь HTML и не запускаешь ни одного скрипта конвейера** (`porodit.py`, `sverstat.py`, `build_deck.py`, `podognat.py` — ни один). Вся папка `teorkat-vvedenie/` тебе **READ-ONLY целиком**. Перекомпоновку по утверждённому плану сделает СЛЕДУЮЩИЙ заход.

**Почему так, дословно от владельца:** *«проблема в том, что я оценить результат могу, только когда я уже в конце смотрю и вижу, что что-то не так… когда открываешь слайд, то уже видно, что плохо»*. Твоя таблица — первый в этом проекте артефакт, который владелец читает ДО сборки, а не после. Поэтому она обязана быть читаемой глазами за две минуты, а не полной.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_perekomponovka.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_perekomponovka.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- ПЛАН — в `## ПЛАН` перед действиями.

**Прочитать ТОЛЬКО это, всё — на ЧТЕНИЕ.** Адреса прогнаны через `test -f` при сборке захода 30.07.

| # | файл | зачем |
|---|---|---|
| 1 | `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` | **лента части 1 — главный вход.** Живой замер 30.07: разделов **8**, содержательных блоков **63**, знаков **4507**, `<figure>` **3** |
| 2 | `teorkat-vvedenie/raskadrovka/teksty/B-yazyk.md` | **лента части 2.** Разделов **5**, блоков **27**, знаков **2909**, `<figure>` **3** |
| 3 | `_studio/konvejer/06-tekst/DOK.md` | **канон текста слайда — прочитать ДО работы.** Оттуда берётся единица вместимости и потолки; см. §2 «Гейты» |
| 4 | `_studio/zhurnal/2026-07-30_dovodka-l1/OPROSNIK-L1.md` | 91 вопрос по прочитанной половине. **Читать выборочно, грепом по разделу** — там в поле `сейчас` лежат готовые замеры каждого экрана, не пересчитывай их сам |
| 5 | `_studio/zhurnal/2026-07-30_dovodka-l1/SESSIYA.md` | записи от 30.07: жанр, хребет, решения владельца дословно. **Хребет продублирован в §2 этого файла — при расхождении верь §2** |

Всё остальное не открывать. `C-*.md`, `D-*.md`, `E-*.md` — вторая лекция, вне захода. `KONSPEKT-L1/` не открывать: он нужен для ДОПИСЫВАНИЯ, а ты не дописываешь.

**Определение БЛОКА — от него зависят все гейты.** Блок = одна непустая строка ленты, которая **не** является: заголовком (`#`, `##`), фронтматтером, служебным полем (`> поле:mn …`), содержимым `<figure>…</figure>`, разделителем `---`. Именно так посчитаны 63 и 27 выше — воспроизведи счёт первым ходом и сверь; расходится — скажи в `## ПЛАН` и считай по живому.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

Создать ОДИН файл: **`_studio/zhurnal/2026-07-30_dovodka-l1/PLAN-perekomponovki.md`**. Больше файлов не заводи.

### 🔴 ХРЕБЕТ — продиктован владельцем, это ВХОДНЫЕ ДАННЫЕ, а не твоё решение

Девять шагов первой части плюс финальный блок. Формулировки владельца:

1. **Мотив:** нас интересует простой способ доказать, что число элементов равно.
2. **Как доказать? Построить биекцию.** Бытовой разбор: как сравнить количество стульев и количество школьников — рассадить, разбить на пары. 🔴 **Здесь же — сколько всего биекций и что их можно КОМПОНОВАТЬ.** Владелец: *«люди не понимали, задавали вопрос, как их компонировать, что их можно компонировать. Это тоже очень важно. По сути, мы обсуждаем категорную структуру категории множеств с биекциями. На втором слайде это важно»*. Слово «категория» при этом НЕ произносится.
3. **Как доказать этим тождество.** Самый простой вход выбран владельцем: $\sum_k\binom nk=2^n$ — с одной стороны функции $U\to\{0,1\}$, с другой подмножества всех размеров. Затем содержательнее: $\binom nk=\binom n{n-k}$.
4. **Биекция нужна не любая, а КРАСИВАЯ.** На том же примере: переименовали — что происходит. *«Когда мы меняем Васю, Петю, Дашу на трёх других людей с такими же первыми буквами, картинка меняется, а всё остальное происходит то же самое»*.
5. **«А что для этого надо?» → ФУНКТОРИАЛЬНОСТЬ. Определяем ВИД, и тут же произносим слово.** *«Когда определяешь вид, надо сказать: это свойство называется функториальностью. Функториальность в этом месте произносится»*.
6. **Очень много примеров видов — НЕ СПИСКОМ.** *«Лучше не перечислять огромным списком, а про каждый как-то, чтобы можно было понять, о чём идёт речь и почему это, собственно, вид»*. Плюс сюжет **«не все конструкции — виды»** (контрпример владельца: подмножества, не содержащие данный элемент, — не функториально).
7. **«А что значит красивая?» → ИЗОМОРФИЗМ ВИДОВ.** Вот изоморфизм, вот нет — **хотя всё функториально**. Здесь произносится ЕСТЕСТВЕННОСТЬ.
8. **Когда изоморфизма не существует.**
9. **Когда это вообще не вид → обобщить арену** (пунктированные множества). *«Это надо прямо произнести, показать, что означает»*. Владелец: этот шаг **подводит к идее категорий**, то есть служит мостом.
10. **ФИНАЛЬНЫЙ БЛОК — объекты.** Функтор, естественное преобразование, категория определяются здесь и только здесь.

🔴 **НЕСУЩИЙ ПРИНЦИП, который надо удержать при раскладке.** В записках читатель всё время ПОЛЬЗУЕТСЯ тем, что назовут только в финале, и это сделано трижды: арена работает со второго экрана (композиция биекций) — имя «категория» в финале · функториальность работает с пятого — имя «функтор» в финале · естественность с седьмого — имя «естественное преобразование» в финале. **Рано вводятся только СВОЙСТВА, поздно — ОБЪЕКТЫ.** Раскладка, которая тащит объект вперёд, ломает замысел; заметил такое — скажи в отчёте.

### Что именно делать

**Шаг 1. Разобрать ленту на блоки** по определению из §0 и присвоить каждому устойчивый адрес `A:117` / `B:44` (файл : номер строки). Это единственный вид адреса, которым ты пользуешься. **Дословных вставок текста в план НЕ делай** — только адреса и первые 5–7 слов блока для узнаваемости. *Почему: план с вставленным текстом невозможно отличить от переписывания, и приёмка не сможет доказать, что ничего не изменилось.*

**Шаг 2. Разложить блоки по НОВЫМ экранам** согласно хребту. Один блок — ровно один новый экран; дублировать блок в два экрана запрещено (иначе гейт полноты станет ложно-зелёным).

**Шаг 3. Назвать ДЫРЫ.** Место хребта, которое существующими блоками не наполняется, — отдельная строка со статусом `ПУСТО` и оценкой, сколько экранов туда нужно. **Дыры — самый ценный выход захода**, потому что они и есть заказ на следующий шаг. Известные заранее (проверь и уточни, но не считай список полным): вход про биекцию — слова «биекция» в корпусе нет вовсе · разбор «почему это, собственно, вид» у примеров галереи · контрпример «не функториально» · композиция биекций и их счёт · $2^n$ как младший брат уже стоящего $3^n$.

**Шаг 4. Разметить СЛОЙ у каждого экрана — `элементарный` или `основной`.** Владелец решил, что записки обслуживают ОБЕ аудитории (сильные матшкольники и программисты), и это дёшево ровно при условии, что элементарное отделяемо: читатель, который знает, листает мимо. Экран смешанного слоя — дефект раскладки, назови его.

**Шаг 5. Отдельной строкой — судьба ЗООПАРКА.** Два экрана примеров категорий (`Категории: множество со структурой`, `Стрелка как следование и как шаг`) владелец обесценил (*«галерея примеров — это вообще последние десять минут, это вообще не важно»*) и рассматривает вынос: *«может, примеры категорий вообще куда-нибудь уберём, потому что они какие-то странные… можно как приложение в конце»*. **Вердикта нет.** Разложи их как ПРИЛОЖЕНИЕ в конце, пометив `КАНДИДАТ В ПРИЛОЖЕНИЕ — вердикта владельца нет`, и в отчёте скажи, что ломается, если их убрать совсем.

### Форма таблицы — она и есть продукт

Владелец читает её глазами за две минуты. Одна строка = один новый экран, по порядку:

```
| № | шаг | слой | рабочее имя экрана | блоки (адреса) | знаков было→стало | статус |
```
- **шаг** — номер из хребта (1–10) или `ПРИЛ`.
- **слой** — `элем` / `осн`.
- **рабочее имя** — из существующего заголовка раздела, либо описание дыры.
- **блоки** — адреса через `·`, не текст.
- **знаков было→стало**: «было» = знаков в том исходном разделе, откуда блоки взяты (максимум, если из нескольких); «стало» = сумма знаков блоков этого экрана.
- **статус** — `ПЕРЕЛОЖЕНО` / `ПУСТО` / `КАНДИДАТ В ПРИЛОЖЕНИЕ`.

Под таблицей — **карта обратного хода**: для каждого исходного раздела (13 штук) строка «раздел → на какие новые экраны разъехались его блоки». Она нужна приёмке, чтобы проверить полноту с другой стороны.

### 🔴 КРИТЕРИЙ ГОТОВНОСТИ — пять гейтов, все числовые, все могут ПРОВАЛИТЬСЯ

Прогони САМ, командами, напечатай ЧИСЛА:

1. **ПОЛНОТА — главный гейт.** Каждый блок ленты имеет **ровно один** дом. Напечатай: «блоков в ленте A+B — N (команда), в плане размещено — M, сирот (нигде) — X, дублей (в двух экранах) — Y». **X > 0 или Y > 0 — гейт красный.** Живое значение 30.07: 63 + 27 = **90**.
2. **СОХРАННОСТЬ ЗНАКОВ.** Сумма «стало» по всем экранам = сумма знаков всех блоков ленты. Напечатай оба числа и разницу. **Разница ≠ 0 — гейт красный.** Живое значение: 4507 + 2909 = **7416**.
3. **МОНОТОННОСТЬ — требование владельца, оно СТРОЖЕ канонного потолка.** Ни на одном новом экране «стало» не больше, чем «было». Напечатай: «экранов N, нарушений монотонности — K, список». **K > 0 — гейт красный.** *Почему это сильнее канона: `06-tekst/DOK.md` даёт потолок 650 знаков и 55 слов НА КАДР; монотонность запрещает даже разрешённое каноном утолщение. Владелец: «ни на одном слайде не станет больше текста, чем было — поскольку мы увеличиваем количество слайдов, это точно правильная работа».*
4. **НИ ОДНОГО НОВОГО ПРЕДЛОЖЕНИЯ.** План состоит из адресов; проверка — в плане нет ни одной строки исходного текста длиннее семи слов. Напечатай: «строк-цитат длиннее 7 слов — 0». **Больше нуля — гейт красный.**
5. **ЛЕНТА НЕ ТРОНУТА.** `git --no-optional-locks status --porcelain -- teorkat-vvedenie` → **пусто**. Напечатай вывод команды. **Непусто — гейт красный, отчёт не принимается.**

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «не найдено, проверено X из Y». Без охвата вердикт не принимается.

### 🔴 ЧЕТЫРЕ ЛОВУШКИ — по каждой строка в отчёте, поддался или нет

1. **НЕ ПОДЫГРЫВАЙ ХРЕБТУ.** Хребет — свежая диктовка владельца, ему приятно, если он сойдётся, и услужливый исполнитель его натянет. **Шаг, который существующим текстом не наполняется, — это НАХОДКА, а не неудача.** Скажи прямо: «шаг 6 наполняется на треть, вот чем, остальное дыра такого размера». Натянутая раскладка хуже честной дыры: дыру дописывают, а натяжку обнаруживают на слайдах.
2. **АДРЕС — СТРОКА ЛЕНТЫ, а не номер слайда и не заголовок на экране.** Заголовки разделов ленты и текст на слайде РАСХОДЯТСЯ, и это норма: дек заголовков разделов не показывает ни у одного слайда. Например раздел `Конструкции` печатает на слайде «Противоположная $\mathcal C^{\mathrm{op}}$». Номера слайдов не употребляй вовсе — они сдвинутся.
3. **ЗАМЕРЫ НЕ ПЕРЕСЧИТЫВАЙ, ЕСЛИ ОНИ УЖЕ ЕСТЬ.** В `OPROSNIK-L1.md` поле `сейчас` у каждого из 91 вопроса несёт факт из живого файла. Пересчитывать их заново — трата прогона. *На этой арке вопрос «задача уже сделана?» сработал бы пять раз за трое суток.*
4. **НЕ ЧИНИ ПО ДОРОГЕ.** Найдёшь в ленте настоящую ошибку — математическую, логическую, оборванную формулировку — **не правь и не перекладывай, чтобы «стало лучше»**: в отчёт строкой. Твоя работа — раскладка, а не редактура. *Цена попутных улучшений: они пачкают диф и тихо переписывают проект под владельцем.*

## 3. ВЕРИФИКАТОР — ОБЯЗАТЕЛЕН, потому что операция ЛОССОВАЯ ПО ВИДУ

Ничего не удаляется физически, но раскладка **теряет молча**: блок, не попавший ни в один экран, исчезает из будущей ленты, и увидит это только тот, кто читал оригинал. Поэтому свежий субагент, **другой метод**: он берёт ленту сам, режет на блоки СВОИМ скриптом (не твоим), и сверяет множество адресов из плана с множеством адресов ленты — программно, `set difference` в обе стороны.

**Его вердикт печатается числами:** «в ленте блоков N · в плане адресов M · в ленте, но не в плане — список · в плане, но не в ленте — список». **Оба списка обязаны быть ПУСТЫ.** Второй список ловит выдуманный адрес — самую опасную ошибку плана, потому что она выглядит как работа.

⚠ **Мандат верификатора ШИРЕ твоего:** ему разрешено открыть `KONSPEKT-L1/`, чтобы проверить одну вещь, которую ты проверить не можешь, — **не назвал ли ты дырой то, что в корпусе есть.** Ложная дыра стоит целого лишнего захода на дописывание того, что уже написано.

Субагентов ≤5 параллельно, рейт-лимит = отступить и доложить. Время ожидания мерь `date`, а не суммой `sleep`.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_perekomponovka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_perekomponovka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-07-30_dovodka-l1   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone _studio/zhurnal/2026-07-30_dovodka-l1 \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

**Плюс шесть блоков, специфичных для этого захода:**
1. **Пять чисел критерия §2**, каждое с командой: блоков N / размещено M / сирот X / дублей Y · сумма знаков было и стало, разница · нарушений монотонности K · цитат длиннее 7 слов · вывод `git status -- teorkat-vvedenie`.
2. **ДЫРЫ — отдельным списком, по убыванию размера.** Каждая: где в хребте · чего не хватает · сколько экранов оценочно · есть ли материал в корпусе (это проверяет верификатор, не ты). **Это главный выход захода для владельца, поставь его первым.**
3. **Числа раскладки:** экранов было 13 → стало N · из них элементарных / основных · самый толстый экран (знаков) · самый тонкий.
4. **Шаги хребта, которые НЕ наполнились** — с честной долей («шаг 6 наполнен на треть»). Ловушка 1.
5. **Что я нашёл, но НЕ тронул** — ошибки ленты, расхождения, странности. Строкой каждое, без починки.
6. **Судьба зоопарка:** как разложил, что ломается, если убрать совсем.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Заход дал исполнителю проверять отсутствие по ЛЕНТЕ, а формулировать вывод — про КОРПУС; и сам же закрыл ему корпус
Заход запретил открывать `KONSPEKT-L1/` (§«Всё остальное не открывать»), назвал пять дыр «известными заранее» и потребовал главным выходом список дыр. Проверить их исполнитель мог только по двум лентам — а «дыра» читается как «этого нет нигде», потому что дыра есть заказ на дописывание. Мандат на корпус отдан верификатору, то есть проверка приходит ПОСЛЕ того, как артефакт написан.
**ЦЕНА:** из 12 дыр, объявленных исполнителем и уехавших в артефакт под шапкой «всё, что здесь, надо писать с нуля», верификатор опроверг **4 полностью и 6 частично** — подтвердились **2**. Заказ на дописывание был завышен с «4 экрана с нуля» до «20 с нуля», то есть **впятеро**, и это не описка исполнителя, а прямое следствие развода мандатов: исполнитель написал ровно то, что мог проверить, и назвал это тем, чего проверить не мог. Плюс верификатор нашёл в корпусе ДВА факта, которые снимают работу целиком (конспект прямо объясняет, почему тождества $2^n$ нет; контрпример владельца в конспекте назван функтором) — оба уехали в `## ВОПРОСЫ` только потому, что мандат верификатора был шире; при более узком верификаторе следующий заход написал бы 20 экранов, из которых 7 уже написаны, а 2 запрещены прежним решением владельца.

### Определение блока не исключает HTML-разметку, и метрика знаков на таблицах перестаёт мерить текст
§0 определяет блок как непустую строку, не являющуюся заголовком, фронтматтером, полем `> поле:mn`, содержимым `<figure>` или `---`. Строки `<table>/<thead>/<tr>/<td>` в исключения не попали — и стали блоками. Именно так и получается заявленное заходом 63, то есть определение самосогласовано; но производная метрика «знаков» на таком экране мерит теги.
**ЦЕНА:** на экране №2 из 851 знака **756 — разметка** восьми тождеств; видимого текста втрое меньше. Гейт монотонности на этом экране сравнивает разметку с разметкой и не проверяет ничего, а число 851 при чтении канона (`06-tekst/DOK.md`: потолок 650 знаков на кадр) читается как «экран переполнен на 30 %», хотя он недогружен. По всей ленте это **13 блоков из 90 и 10,2 % суммы гейта 2** — то есть десятая часть главного числа сохранности не является текстом.

### Правило «строка регистрации обязана уехать в ТОТ ЖЕ коммит» не выдерживает параллельного писателя, и нарушает его не исполнитель
Контракт зоны требует дописывать `_studio/docs/KARTA.md` к своим путям в обоих ходах, потому что ворота 5 читают §6 с диска и документ иначе уедет сиротой. Механика верна, но предполагает, что коммит исполнителя — первый, кто заберёт `KARTA.md`.
**ЦЕНА:** в этом прогоне дверь `register_doc.py` вписала строку в 16:1x, а в **16:17:10 чужой коммит `e4decd4` (Claude Cowork) подмёл `KARTA.md` целиком** — вместе с моей строкой, которая в его сообщение не входит и в нём не описана. К моменту моего коммита файл чист, нести нечего: мой коммит `KARTA.md` не содержит, и приёмка, гоняющая гейт «регистрация в том же коммите», получит красное на честной работе, а сама строка окажется описана коммитом про другую арку. Ущерба нет ровно потому, что я это заметил и проверил `git show HEAD:… | grep -c` → 1; при молчании отчёта расследование пошло бы искать пропавшую регистрацию.

## ПЛАН — (заполняет исполнитель)

**Ветка проверена первым ходом:** `git branch --show-current` → `arka/mat-kostyak`, rc=0. Точку отката не фабрикую: зона в HEAD чиста, кроме моего файла-захода и `PLAN.md`/`SESSIYA.md` (чужая правка Cowork, не трогаю).

**Счёт блоков воспроизведён — сошёлся точно, считаю по своему скрипту:** A — 63 блока / 4507 знаков, B — 27 / 2909. Определение §0 применил дословно; строки `<table>…</table>` в исключения §0 не входят, поэтому 13 строк таблицы тождеств — это 13 блоков (именно так и получается заявленное 63). Значит метрика «знаков» на экране с таблицей завышена разметкой — скажу это числом в отчёте, но метрику не меняю: она задана заходом.

**Порядок работы.**
1. Прочитать ровно пять названных файлов (сделано). Ленты — целиком; `OPROSNIK-L1.md` — грепом по разделам, замеры из поля `сейчас` не пересчитывать.
2. Выгрузить все 90 блоков с адресами и знаками, сгруппировав по 13 исходным разделам (сделано одним скриптом).
3. Разложить блоки по новым экранам по хребту §2. Правило разрешения спора: блок едет туда, где он ПЕРВЫЙ раз нужен читателю; блок, несущий два имени сразу, едет по первому из них и помечается как требующий разреза (разрез — работа следующего захода, я блоки не делю).
4. Гранулярность: целюсь в 100–400 знаков на экран. Экран тоньше 100 знаков оставляю только там, где он несёт ровно один шаг хребта и рядом стоит дыра — тогда тонкость и есть сигнал.
5. Дыры — отдельными строками таблицы, с оценкой в экранах.
6. Прогнать пять гейтов §2 одним скриптом, напечатать числа.
7. Свежий верификатор другим методом (§3) + мандат на `KONSPEKT-L1/` (мне туда нельзя).
8. Завести `PLAN-perekomponovki.md`, тем же ходом позвать `register_doc.py`, коммитить с `KARTA.md` в обоих ходах.

**Предпосылки, которые проверил ДО работы (две поправки к заходу).**
- 🔴 **«Слова „биекция“ в корпусе нет вовсе» (§2, шаг 3 списка дыр) — для ЛЕНТЫ неверно.** `grep -c 'биекц'` → A = 11 вхождений, из них в блоках 7 (`A:34 · A:58 · A:60 · A:79 · A:173 · A:181 · A:221`); в B — 0. Верно другое, и оно и есть дыра: **нет определения** — `grep -c 'взаимно однозначн'` → 0 в обоих файлах, `grep -c 'стул'` → 0. Утверждение из `SESSIYA.md:55` касается `KONSPEKT-L1/`, а не ленты, и в заходе потеряло адресата. Дыру формулирую точно: не «слова нет», а «слово работает с первого экрана, а что оно значит — не сказано нигде».
- ✅ **Проверил остальные четыре заявленные дыры — все настоящие.** `функториальн` → **0** вхождений в A и B (значит шаг 5 не может произнести своё слово ничем существующим); `пунктирован` → **0** (шаг 9); `∑C(n,k)=2^n` в ленте нет — два вхождения `2^` это `$n\,2^{\,n-1}$` и `$\binom nl\,2^{\,n-l}$` внутри таблицы тождеств; разбора «почему это вид» у галереи нет (подтверждено и `OPROSNIK` В-13: разборов доехало ноль).

**Критерий готовности принимаю, с одной оговоркой по форме таблицы.** Колонка `шаг` допускает только `1–10` и `ПРИЛ`, а обложка (`A:34`) не является ни шагом хребта, ни приложением. Ввожу для неё значение `0` — иначе блок пришлось бы затолкать в шаг 1 и соврать о раскладке. Гейты от этого не меняются.

## ВОПРОСЫ — (заполняет исполнитель)

Восемь развилок, которые я разрешать не имел права. **Блокируют следующий заход четыре: №1, №2, №5 и №6** — без них перенос либо не разведёт шаги, либо перенесёт устаревшее, либо напишет то, что владелец сознательно выбросил. Развилки №5–№7 вскрыл верификатор своим расширенным мандатом на конспект, у меня их не было бы вовсе.

1. 🔴 **Разрешено ли СЛЕДУЮЩЕМУ заходу резать блоки?** Мне делить блок было запрещено, и из-за этого два места остаются грязными: `A:159` несёт вердикты шагов 7, 8 и 9 в одной строке, `A:124` объявляет «вид» (шаг 5) и «изоморфизм видов» (шаг 7). **Без разреза шаги 7/8/9 и 5/7 чисто не разводятся ни при какой раскладке** — это свойство текста, а не плана. Если резать нельзя, надо сознательно принять, что два имени падают раньше своих вопросов.
2. 🔴 **`A:222` (тезис естественности) — перенести на экран №4 или писать вступление с нуля?** Владелец требует тезис «в самом начале, неформально», а единственное вхождение слова «естественность» в ленте A — этот блок, и он в резюме. Перенос закрывает дыру №4 существующим текстом, но резюме теряет один из пяти тезисов. Я оставил в резюме и объявил дыру — то есть выбрал более дорогой вариант, потому что он не портит финал.
3. **Зоопарк: убрать, оставить приложением или вернуть в поток?** Вердикта нет, разложил приложением. Если убирать — нужен перенос `B:67`, `B:69`, `B:51`, иначе три строки шага 10 остаются без определений (подробно в отчёте, §7).
4. **Сцены — развилка из `SESSIYA` п.7 не решена, а раскладка от неё зависит.** В ленте A+B шорткатов `{@N}` двадцать четыре (`grep -c '{@' ` по двум файлам). Если записки остаются кликабельным HTML, сцены живут и часть моих раздвижек лишняя (например №10 и №11 — постановка и разбор — сегодня две сцены ОДНОГО слайда). Если записки плоские, сцены сливаются, и раздвижка обязательна. **Я раздвигал так, будто сцен нет** — это самое консервативное чтение, но чтение, и его надо подтвердить.
5. 🔴 **Два решения владельца про вход через сумму биномов = $2^n$ противоположны, и оба записаны.** `SESSIYA` порция 5 (30.07): *«действительно хорошая идея… это очень простая вещь»* — тождество выбрано самым простым входом. А конспект `KONSPEKT-L1/01-krasivaya-biekciya.md:22` (нашёл верификатор, мне файл был закрыт) прямо объясняет, почему его НЕТ: *«Отбор: не самые очевидные»*. Это не забывчивость исполнителей, а смена решения — но не отмеченная как смена. **Дыру №8 (2 экрана) писать нельзя, пока не сказано, какое из двух решений живое.**
6. 🔴 **У корпуса два дома, и они разошлись** (находка верификатора). Есть `teorkat-vvedenie/KONSPEKT-L1/` (12 файлов) и одиночный `teorkat-vvedenie/KONSPEKT-L1.md` (519 строк, `status: skelet`) с той же нумерацией утверждений, но другим содержанием: Утверждение 6 названо иначе, Пример 10 держит четыре вида вместо пяти. **Заходу на дописывание надо сказать, какой из двух канон** — иначе он перенесёт устаревшую версию, и это не поймает ни один гейт.
7. **Контрпример «подмножества, не содержащие данный элемент» — вид или не вид?** Владелец назвал его контрпримером («не функториально», шаг 6), а конспект `01-krasivaya-biekciya.md:166` называет ту же конструкцию функтором **над отмеченными**. Моё чтение: это одна конструкция на двух аренах, и она ровно та дуга, которую хребет ведёт от шага 6 к шагу 9 — тогда это подарок, а не противоречие. Но конспект я читать не имел права, поэтому вердикт не мой.
8. **Обложка `A:34` называет артефакт «Лекция 1» и перечисляет содержание всей лекции, включая непрочитанную половину.** Для записок, которые выкладывают в интернет, это первое, что прочитает человек. Правка вне моего мандата (я блоки не переписываю), но развилка есть и она однострочная.

## ОТЧЁТ — (заполняет исполнитель)

**ЧТО СДЕЛАЛ + ЗАЧЕМ.** Завёл ОДИН файл — `PLAN-perekomponovki.md`: таблицу из 50 строк-экранов, где все 90 блоков ленты A+B разложены по новым экранам согласно хребту §2, плюс 12 дыр, карту обратного хода по 13 исходным разделам и разбор судьбы зоопарка. Ленту не тронул ни одной правкой, ни один скрипт конвейера не запускал. **Зачем именно так:** таблица — первый артефакт проекта, который владелец читает ДО сборки, поэтому она стоит на адресах строк, а не на тексте: приёмка может доказать грепом, что ни одно предложение не переписано.

Файл собран не руками, а **скриптом-генератором из закодированной раскладки** (`/tmp/genplan.py`, вне репо) — иначе 90 адресов и 76 чисел «было→стало» переписывались бы вручную и разъехались бы с лентой на первой же опечатке. Тот же скрипт печатает гейты.

---

### 1. 🕳 ДЫРЫ — главный выход захода, по убыванию размера

**12 дыр, ≈20 экранов. Но с нуля из них пишутся 4, а 7 просто переносятся из конспекта** — эту вторую половину вердикта дал верификатор, которому был открыт `KONSPEKT-L1/` (мне заход его закрыл, §2 «не открывать»). **Первая версия секции говорила «всё, что здесь, надо писать с нуля» — это было утверждение о КОРПУСЕ при проверках только по ЛЕНТЕ, я его снял как неверное.** Полные формулировки, адреса материала и доказательство отсутствия — в таблице «ДЫРЫ» самого плана; здесь порядок, суть и вердикт по корпусу:

| экр. | шаг | дыра | материал в корпусе |
|---|---|---|---|
| **3** | 6 | у каждой группы примеров — «почему это, собственно, вид»: структура и её транспорт | 🟡 есть у 5 видов из 10 |
| **2** | 2 | что такое биекция (стулья и школьники, разбиение на пары) | ✅ нет нигде — **писать с нуля** |
| **2** | 2 | сколько всего биекций и что их можно компоновать | 🟡 счёт готов ($n!$), композиции нет |
| **2** | 3 | вход через сумму биномов = два в степени n | ✅ нет — но 🔴 конспект прямо пишет, что его нет НАРОЧНО |
| **2** | 4 | переименование как источник «красоты» | 🔴 **ложная дыра** — готово прозой |
| **2** | 6 | контрпример «не все конструкции — виды» | 🔴 **ложная дыра**, и материал сложнее заказа |
| **2** | 9 | диагноз, ПОЧЕМУ Вандермонд и Паскаль вообще не виды | 🔴 **ложная дыра** — диагноз обоих готов |
| **1** | 1 | неформальный тезис естественности во вступлении | 🟡 тезис есть, слова нет |
| **1** | 3 | биекция-дополнение между двумя биномиальными коэффициентами | 🔴 **ложная дыра** — с доказательством |
| **1** | 5 | само слово ФУНКТОРИАЛЬНОСТЬ | 🟡 слово есть, к видам не приклеено |
| **1** | 7 | само слово ЕСТЕСТВЕННОСТЬ в момент работы изоморфизма видов | 🟡 к видам применено, имени нет |
| **1** | 9 | «пунктированные множества» названы словом и показано, что это значит | 🟡 содержание есть, имя другое |

**Итог по стоимости: писать с нуля — 2 дыры / 4 экрана · переносить — 4 дыры / 7 экранов · переносить и дописывать — 6 дыр / 9 экранов.** То есть заказ на новый текст вдвое меньше, чем читается из одного размера.

🔴 **И главное, что даёт эта колонка, — смена ДИАГНОЗА.** Четыре ложные дыры лежат кучей и все четыре про одно: как устроена красивая биекция и почему плохая плохая (№9, №13, №21, №29). Текст на это написан и лежит в конспекте, а на ленту не доехал. Значит дефект части 1 — **не «не написано», а «написано и потеряно при переносе на слайды»**. Это другая болезнь с другой ценой: лечится переносом, а не сочинением, и предупреждает, что следующий перенос потеряет столько же, если не проверять его тем же гейтом полноты, что и эту раскладку.

**Три вещи, которые видны только из этого списка.**
1. 🔴 **Оба слова-имени, которые хребет требует произнести рано, в корпусе отсутствуют физически.** `grep -c 'функториальн'` → **0** в обоих файлах ленты, `grep -c 'пунктирован'` → **0**, а «естественность» встречается в A ровно один раз — в `A:222`, то есть в резюме, на три шага позже своего места. Симметрия «свойства рано, объекты в финале», принятая владельцем, сегодня не обеспечена ни одним словом текста.
2. 🔴 **Элементарный слой не существует, и это единственная область настоящего «с нуля».** Наполненных экранов слоя `элем` в плане **ноль**; все 6 его экранов — дыры (№6, №7, №8), и по конспекту определения биекции нет и там. Решение владельца «на записках делаем и то, и то» сегодня обслужено нулём экранов.
3. **Три из 12 дыр живут внутри `<svg>`/`<figcaption>`** — то есть материал нарисован, но не напечатан (№9, №13, частично №4). По определению блока §0 содержимое `<figure>` блоком не является, поэтому в записках без лектора этого текста читателю не достаётся.

**Уточнение к списку заранее известных дыр из §2 — одна из пяти сформулирована неверно.** «Слова „биекция“ в корпусе нет вовсе» — для ленты ложно: `grep -c 'биекц'` → **11** вхождений в A (в блоках 7: `A:34 · A:58 · A:60 · A:79 · A:173 · A:181 · A:221`), в B — 0. Отсутствует **определение**: `grep -c 'взаимно однозначн'` → 0, `grep -c 'стул'` → 0. Разница не косметическая: заказ следующему заходу «ввести слово» и «объяснить слово, которое уже работает с обложки» — это разные тексты. Остальные четыре дыры из §2 подтвердились.

---

### 2. Пять чисел критерия §2 — каждое с командой

Все пять получены одним скриптом по определению блока из §0; скрипт режет ленту сам, ничего не берёт из плана как факт.

| гейт | числа | команда | вердикт |
|---|---|---|---|
| **1 ПОЛНОТА** | блоков в ленте **90** (A 63 + B 27) · размещено **90** · сирот **0** · дублей **0** · выдуманных адресов **0** | парсер §0 + `set`-разность в обе стороны между множеством адресов таблицы плана и множеством блоков ленты | ✅ |
| **2 СОХРАННОСТЬ ЗНАКОВ** | сумма «стало» **7416** · сумма знаков блоков ленты **7416** · разница **0** | тот же скрипт; знаки = `len(line.strip())` | ✅ |
| **3 МОНОТОННОСТЬ** | экранов-строк **50** (наполненных 38, ПУСТО 12) · нарушений **0** · список пуст | для каждого экрана сравнение «стало» с «было» = знаки исходного раздела (максимум, если блоки из нескольких) | ✅ |
| **4 НИ ОДНОГО НОВОГО ПРЕДЛОЖЕНИЯ** | цитат из ленты длиной ≥8 слов — **0** | по каждому из 90 блоков построены все окна из 8 подряд идущих слов, каждое искалось подстрокой в тексте плана | ✅ |
| **5 ЛЕНТА НЕ ТРОНУТА** | `git --no-optional-locks status --porcelain -- teorkat-vvedenie` → **пустой вывод**, rc=0 | она же | ✅ |

🔴 **Гейт 5 прогнан ДВАЖДЫ, и второй раз был не формальностью.** В середине прогона в ветку приехал чужой merge (`7a131ec`, `zahod/dek-paskal-v2`), а в истории ленты числится коммит `05c42a1`, который правил `A-krasivaya.md` и `B-yazyk.md`. Если бы он приехал с этим merge, **все 90 адресов моего плана стали бы недействительны молча** — номера строк сдвинулись бы, а таблица выглядела бы рабочей. Проверил двумя командами: `git diff --quiet 724a31e HEAD -- <лента>` → обе ленты **идентичны** состоянию на старте захода, и `git merge-base --is-ancestor 05c42a1 724a31e` → **предок**, то есть merge ленту не двигал. Пересчёт блоков после merge дал те же 63/4507 и 27/2909.

**Отдельно — два дефекта, которые нашёл мой собственный самопроверочный скрипт, а не гейты захода.** Оба я починил в генераторе и пересобрал файл: (а) в примечании к экрану №44 я упомянул адрес `` `B:51` `` как ссылку, и регексп по таблице стал видеть **91 адрес вместо 90 с дублем `B:51`** — то есть моё же пояснение сделало бы гейт полноты ложно-красным у любого, кто парсит таблицу; (б) в «Ключе адресов» и в карте обратного хода адреса разделов стояли в тех же обратных кавычках, что и адреса блоков, и давали **103 адреса вместо 90**. Сейчас: таблица 90 уникальных, ключ 90 уникальных, множества совпадают.

---

### 3. Числа раскладки

- **Экранов было 13 → стало 58** (38 наполненных существующим текстом + ≈20 из дыр). Наполненных ровно в 2,9 раза больше, чем исходных разделов, **без единого нового предложения**.
- **Слои: `элем` 3 строки (все ПУСТО) · `осн` 47.** Смешанного слоя нет ни у одного экрана.
- **Самый толстый экран — №2, 851 знак.** ⚠ Число завышено метрикой: 756 из 851 — это теги `<table>/<tr>/<td>` восьми тождеств, видимого текста там втрое меньше. Второй по толщине — №39 (361).
- **Самый тонкий наполненный — №28, 71 знак** («Упражнение: нечётное n»); следующие — №3 (84) и №26 (105).
- **Знаков по шагам** (посчитано скриптом из самой таблицы плана): шаг 10 — 2403 · ПРИЛ — 1038 · шаг 1 — 935 · шаг 9 — 651 · шаг 8 — 466 · шаг 7 — 406 · шаг 6 — 398 · шаг 5 — 392 · шаг 3 — 339 · шаг 4 — 178 · шаг 2 — 129 · обложка — 81. Сумма — 7416, то есть гейт 2 сходится и по этой разбивке тоже.

---

### 4. Шаги хребта, которые НЕ наполнились — честная доля (ловушка 1)

Доля = наполненных экранов к общему числу экранов шага (наполненные + оценка дыр). Считается скриптом по таблице плана, не на глаз.

| шаг | доля | что именно есть и чего нет |
|---|---|---|
| **2** | **1/5 = 20 %** | 🔴 самый пустой шаг. Есть ОДИН блок (`A:58`, «школа требует комбинаторного доказательства»). Нет ни определения биекции, ни счёта, ни композиции — а именно композицию владелец назвал важнейшим («люди задавали вопрос, как их компонировать») |
| **4** | **1/3 = 33 %** | есть ВОПРОС («почему она красивая?», `A:79`) и есть довод, что существование биекции автоматично (`A:60`). Ответа — разбора переименования — нет: он нарисован в `<svg>` и в текст не выходит |
| **6** | **3/8 = 38 %** | перечень десяти видов есть целиком, разбора «почему это вид» нет ни у одного, кроме `A:140` (перестановки). Контрпримера «не функториально» нет. То есть шаг наполнен своей ЛЕГКОЙ половиной — списком, — и пуст в той, из-за которой владелец его переписывает |
| **3** | **2/5 = 40 %** | наполнен ТРЕТЬИМ, самым сложным примером ($3^n$, `Три состояния элемента`). Оба входа, выбранные владельцем как простые, отсутствуют |
| **9** | **4/7 = 57 %** | ремонт арены выписан весь; нет диагноза «почему не виды» и нет имени «пунктированные» |
| **1 · 5 · 7** | **2/3 = 67 %** | у каждого не хватает ровно одного экрана, и во всех трёх случаях это ЗАЯВКА или ИМЯ, а не математика |
| **8 · 10 · ПРИЛ** | **100 %** | наполнены целиком, дыр ноль. Шаг 8 — единственный содержательный шаг части 1 без дыр |

**Вывод, который я обязан сказать против интереса захода: хребет наполняется существующим текстом на 66 % по экранам.** Натягивать я не стал: шаги 2, 3, 4, 6 могли бы выглядеть наполненными, если растащить по ним `Требование красоты` и `Галерею видов`, но это соврало бы о готовности именно там, где владелец собирается писать.

🔴 **А вот вторую половину этого вывода я сначала написал неверно, и её поймал верификатор — исправляю здесь, потому что она меняет смысл.** Я написал «дефицит распределён крайне неровно: задняя половина готова, а вход стоит на пяти блоках». Оба числа не воспроизводятся:
- **дефицит поделён РОВНО ПОПОЛАМ.** Шаги 1–4 → 6 дыр на 10 экранов; шаги 5–10 + приложение → тоже 6 дыр на 10 экранов (посчитано по таблице дыр самого плана). Полностью готовы только шаги **8, 10 и приложение**, а самая большая одиночная дыра сидит не во входе, а в **шаге 6 — 5 экранов**;
- **«пять блоков» — неверное число.** Шаги 1–4 несут **25 блоков**, без 13 блоков-тегов таблицы тождеств — **12**. Верно то, что я имел в виду, но не написал: шаги 2–4 наполняются **10 блоками**, 7 из которых — один раздел с примером про три состояния, а собственно про биекцию и про «красивую» работают **три блока**: `A:58`, `A:60`, `A:79`. Шаг 2 — один блок.

Числа исправлены и в шапке плана. Урок за свой счёт: оба неверных числа я вписал глазами, не командой, — ровно то, что `КОНСТИТУЦИЯ §10` запрещает, и ровно там, где число попадает в первый абзац, который владелец прочитает.

---

### 5. Четыре ловушки §2 — поддался или нет

1. **НЕ ПОДЫГРЫВАЙ ХРЕБТУ — не поддался.** Вывод против интереса захода дан числом (66 %, шаг 2 — 20 %) и объявлен главным. Отдельно назвал, что заранее известная дыра из §2 сформулирована неверно, и не стал переписывать её «удобно».
2. **АДРЕС — СТРОКА ЛЕНТЫ — не поддался.** Все адреса вида `A:117`/`B:44`; номеров слайдов в плане нет ни одного (`grep -cE 'слайд[ае]* [0-9]'` по плану → 0). Рабочие имена экранов брал из заголовков разделов ленты, а не из текста на слайде.
3. **ЗАМЕРЫ НЕ ПЕРЕСЧИТЫВАЙ — соблюдено с одним оправданным исключением.** Из `OPROSNIK-L1.md` брал готовые факты поля `сейчас` (В-11, В-13, В-16, В-19) и на них ссылаюсь. Пересчитал только то, что §0 прямо велел воспроизвести первым ходом (63/4507 и 27/2909) — и оба сошлись точно.
4. **НЕ ЧИНИ ПО ДОРОГЕ — не поддался.** Ни одной правки вне зоны; найденное — списком ниже, без починки. Обнаружив, что тождество №5 из таблицы (`A:49`) печатается второй раз как выносная формула (`A:157`), раскладку под это не подгонял.

---

### 6. Что я НАШЁЛ, но НЕ ТРОНУЛ

- 🔴 **`B:89` опирается на четыре обозначения, из которых в ленте определено одно.** `Mat`, `FinBool`, `FinSet` не определены нигде; врезка `B:47` сама признаёт, что определение `Mat` утрачено правкой 29.07 и «не переехало». То есть экран №44 сегодня сломан независимо от любого решения про зоопарк.
- **Обложка `A:34` врёт о жанре и об охвате.** Подзаголовок «Лекция 1. Красивая биекция, язык для конструкций, два запрета и как они снимаются» описывает целую лекцию, а прочитана половина (части 1–2 из пяти блоков), и артефакт теперь не лекция, а записки. Первое, что прочитает человек в интернете, — слово «Лекция».
- **Заголовок раздела `Три поломки на языке видов` обещает три, разобрана одна.** Совпадает с `OPROSNIK` В-19; после раздвижения это станет виднее, потому что вердикт (`A:159`) и разбор (`A:163`–`A:165`) уедут на разные экраны.
- **`A:159` — один блок с вердиктами трёх разных шагов хребта** (изоморфизм / не вид / вид без изоморфизма). Пока он цел, шаги 7, 8 и 9 нельзя развести чисто. Разрез — работа следующего захода.
- **`A:124` объявляет два имени в одной строке** — «вид» (шаг 5) и «изоморфизм видов» (шаг 7). Положил на шаг 5; следствие — второе имя падает на два шага раньше своего вопроса.
- **Утверждение `A:163` общее («при чётном n»), а доказательство `A:165` частное (n = 2).** Не мой find — `OPROSNIK` В-16 знает и держит общий аргумент в конспекте; отмечаю потому, что раскладка ставит их на соседние экраны №26 и №27 и разрыв станет заметнее.
- **Врезка `A:153` обсуждает картинку, которой не существует** (полный цикл на четырёх точках): по `OPROSNIK` В-17 иллюстраций на этом слайде ноль. Врезка — не блок, в раскладку не входит, но следующий заход о неё споткнётся.
- **`A:143` печатает `E[U]` против прямого запрета владельца** (`OPROSNIK` В-15). Блок разложил как есть, обозначение не менял.

---

### 7. Судьба зоопарка

**Как разложил.** Оба обесцененных экрана — в самый конец, статусом `КАНДИДАТ В ПРИЛОЖЕНИЕ · вердикта владельца нет`, и раздвинул с двух экранов на пять (№46–50, 1038 знаков). Порядок внутри приложения сохранён: сначала «множество со структурой», потом «стрелка как следование и как шаг».

**Что ломается, если убрать совсем — три конкретные строки шага 10, а не «станет беднее».** Экран №43 определяет топологическую теорию поля через категорию кобордизмов и представление колчана через свободную категорию графа; оба объекта введены ТОЛЬКО в приложении (`B:69`, `B:67`). Экран №44 несёт эквивалентность с категорией конечномерных векторных пространств, а само обозначение вводится в `B:51`. То есть удаление приложения оставляет «Список функторов» с функторами между категориями, которых нигде нет.

**Дешёвый ремонт, если владелец решит убрать:** перенести `B:67` и `B:69` на №43, а `B:51` на №41. Тогда снимаются три экрана из пяти, а не пять, и ни одна строка шага 10 не остаётся без определения. Этот вариант в плане описан, но НЕ применён — вердикта нет.

---

### 8. Результат верификатора

Один свежий субагент, **другим методом**: свой парсер ленты (не мой), `set`-разность в обе стороны, своя арифметика по каждому экрану, плюс расширенный мандат на `KONSPEKT-L1/`. Контроль парсера он взял точно: A = 63, B = 27, 7416 знаков.

| задача | его вердикт | его числа |
|---|---|---|
| 1. Сверка множеств адресов | ✅ | в ленте 90 · в таблице 90 (уникальных 90, дублей 0) · «в ленте, не в плане» — **пусто** · «в плане, не в ленте» — **пусто**. Ключ адресов: 90 записей, 0 дублей, расхождений в знаках 0 |
| 2. Арифметика «было→стало» | ✅ | сумма «стало» 7416 = 7416, разница **0** · расхождений «стало» **0 из 50** · расхождений «было» **0 из 38** · нарушений монотонности **0** |
| 3. Ложные дыры | ❌ **ЗАВАЛИЛ** | из 12 дыр подтверждены **2**, ложных **4**, частичных **6**. Охват: 13 из 13 файлов конспекта прогреплены по 14 ключам, 4 прочитаны фрагментами |
| 4. Ни одного нового предложения | ✅ | цитат из ленты ≥8 слов — **0** |
| сверх задания: карта обратного хода | ✅ | блоки, знаки и списки экранов сошлись во всех 13 строках |

**Гейт 3 он завалил обоснованно, и артефакт из-за этого стал лучше:** заказ на дописывание сжался с «20 экранов с нуля» до «4 с нуля, 7 переносом, 9 переносом с дописыванием», а диагноз части 1 сменился с «не написано» на «написано и потеряно при переносе на слайды». Секцию дыр я переписал целиком: добавил шестую колонку с его адресами и снял формулировку «писать с нуля».

**Что из его находок я принял и починил** (каждую проверил своим счётом, не на слово): «пять блоков» → 25/12/10/3 · «задняя половина готова» → дефицит 50/50 · доля разметки в гейте 2 = 756 из 7416 знаков, 10,2 %, 13 блоков из 90 (он написал 14 — мелкая его ошибка, у меня 13, считается тривиально) · слабость правила «было = максимум» на экране №12 — проверил: при строгом правиле (минимум, 405) экран всё равно зелёный, 178 ≤ 405, то есть слабость реальная, но здесь безвредная.

**Что я НЕ принял, и почему.**
- **«Стр. 5 противоречит стр. 111: экран №15 вводит „изоморфизм видов“, который и есть естественный изоморфизм функторов».** Противоречия нет: несущий принцип хребта перечисляет ТРИ объекта (функтор, естественное преобразование, категория), моё утверждение именно о них и проверено командой — `grep` по блокам экранов №1–34 на `категор|функтор|преобразован` даёт **0**. Что «изоморфизм видов» падает на №15 раньше своего вопроса — я сам объявил отдельным пунктом «что этот план не решает». Формулировку в шапке всё же уточнять не стал: она уже несёт свою проверяющую команду.
- **«Автор не оспорил предпосылку про слово „биекция“, хотя заход требует».** Оспорил — в `## ПЛАН` первым ходом и в отчёте §1, с числами (11 вхождений в A, 7 из них в блоках). Он смотрел файл плана, а не мою секцию `ПЛАН`. Его собственный замер (41 вхождение в `01-krasivaya-biekciya.md`) мою поправку только усиливает.

---

### 9. Время прогона + токены

- **Прогон:** заход закоммичен `724a31e` в 16:02:33, первая моя правка файла-захода — 16:09:33 (mtime), коммит зоны — 16:31:1x по `date`. **Итого ≈29 минут**, из них 9 минут (543 878 мс по счётчику субагента) — работа верификатора, шедшая параллельно с моим письмом отчёта. Начало первым ходом по `date` не зафиксировал — мой промах; оценка снизу взята по mtime и по времени коммита захода.
- **Токены:** верификатор — **121 458** (его счётчик, 30 вызовов инструментов). Основной контекст — оценочно ≈170 тыс.: пять названных файлов целиком, выгрузка всех 90 блоков и две пересборки артефакта. Точного счётчика у меня нет, число помечено как оценка.
- **Субагентов:** 1 (лимит §1 — ≤5). Рейт-лимитов не было.

---

### 10. 🔴 НЕОБРАТИМОЕ

**Необратимого нет.** Ни одного удаления, переименования, перемещения, перезаписи существующего файла; `git reset`/`checkout`/`merge` не запускал ни разу. Заведён один новый файл, дважды перезаписан моим же генератором (перезапись собственного, только что созданного файла — не потеря: содержимое воспроизводится запуском `/tmp/genplan.py`). Правки — только в двух файлах моей зоны. `teorkat-vvedenie/` не тронут: гейт 5 пуст, прогнан дважды.

**Одно событие, которое я НЕ совершал, но обязан назвать, потому что оно необратимо изменило состояние:** строка регистрации моего документа в `_studio/docs/KARTA.md`, вписанная дверью `register_doc.py` в 16:1x, уехала в **ЧУЖОЙ коммит `e4decd4` (Claude Cowork, 16:17:10)**, а не в мой — он подмёл `KARTA.md` целиком за секунды до моего замера. Потери нет: строка есть и на диске, и в `HEAD` (`git show HEAD:_studio/docs/KARTA.md | grep -c 'PLAN-perekomponovki'` → 1), документ не сирота. Но мой коммит `KARTA.md` не несёт, потому что нести нечего — файл к моменту коммита чист. Это расхождение с контрактом зоны, и оно не моя правка, а гонка с параллельным писателем.

**КОММИТ:** `5689af5` — «план перекомпоновки записок Л1: 90 блоков ленты по 38 новым экранам + 12 дыр; лента не тронута» · `git_zona.py check --zone _studio/zhurnal/2026-07-30_dovodka-l1` → ✅ «работа доехала в git, вне git ничего нет», rc=0 · `git show --stat` → ровно два пути, оба мои (`PLAN-perekomponovki.md` +279, `kod_perekomponovka.md` +205/−2). Хук `pre-commit` не краснел, `--no-verify` не применялся ни раз. `KARTA.md` в коммит вошёл пустым изменением — причина в §10 отчёта.
*(эта строка допишется вторым коммитом: хэш первого коммита в него самого не влезает)*
