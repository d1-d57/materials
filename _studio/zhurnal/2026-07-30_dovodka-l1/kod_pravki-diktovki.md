# Канал исполнителя — pravki-diktovki (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — снятия и замены расписаны дословно, но три иллюстрации рисуются с нуля по устному ТЗ и доводятся по PNG глазами: план этой части рождается по ходу, и ошибка стоит полного круга переделки перед отправкой.

КОНТЕКСТ. Курс «Введение в теорию категорий», лекция 1; текст лекции живёт «лентой» — файлами `raskadrovka/teksty/*.md`, из которых конвейер ПОРОЖДАЕТ колоду слайдов. Прошлый этап: лента доведена до первого показа, 14 разделов, 12 иллюстраций, все гейты зелёные. 31.07 владелец впервые прочитал её вслух и надиктовал сплошные правки — от снятия целого раздела до перерисовки картинок; заодно выяснилось, что отдавать он собирается СЛАЙДЫ, а не записки, и колода из нынешней ленты никогда не собиралась.

**ЦЕЛЬ: внести все правки диктовки в ленту и собрать из неё колоду слайдов, готовую к отправке.**

Приёмка — по ОТЧЁТУ, без построчной сверки.

⚠ **СТОП ДО ЦЕЛИ, говорю заранее и простыми словами.** Ты получишь собранную колоду со всеми внесёнными правками. Ты НЕ получишь гарантии, что три новые иллюстрации понравятся владельцу с первого раза: про картинки словами договориться почти невозможно, и второй круг по ним — нормальный исход, а не провал захода. Твоя задача по ним — довести до состояния «я сам посмотрел на PNG и готов это показать», и честно написать в отчёте, чем ты недоволен.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/raskadrovka/teksty teorkat-vvedenie/src _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_pravki-diktovki.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add teorkat-vvedenie/raskadrovka/teksty teorkat-vvedenie/src _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_pravki-diktovki.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, в этом порядке. Проект не изучай, соседние арки не открывай.
  1. 🔴 `_studio/zhurnal/2026-07-30_dovodka-l1/PRAVKI-2026-07-31-diktovka.md` — **главный вход захода**. Все правки владельца, разобранные по адресам, его дословными формулировками. Это ТЗ; всё, чего в нём нет, ты не делаешь.
  2. `teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md` и `B-yazyk.md` — то, что правишь. Больше в этой папке ничего не трогай (`C-*`, `D-*`, `E-*` — READ-ONLY, в колоду не идут).
  3. `_generator/SLIDE-FORMAT.md` — формат текста слайда (сцены `{@2}`, `{.formula}`, акценты).
  4. `_studio/konvejer/09-illustracii/SLOVAR-primitivov.md` — примитивы SVG. Рисуешь ТОЛЬКО ими.
  5. `_studio/docs/kak-delat/STANDART-oformlenia.md` — канон коммутативных диаграмм (стрелка множеств против стрелки элемента с палочкой и т. д.). **Канон в силе, владелец его не отменял** — см. §2 ловушку 6.
  6. Docstring-шапки `teorkat-vvedenie/src/tools/porodit.py` и `sverstat.py` — только шапки, чтобы понять конвейер; целиком читать не надо.
- ПЛАН — в `## ПЛАН` перед действиями.

### 🔴 КОНВЕЙЕР — прочти прежде, чем тронешь хоть один файл

Колода **порождается из ленты**, а не пишется руками:

```
raskadrovka/teksty/*.md  →  porodit.py   →  src/content/*.md  +  src/illustrations/*.svg
                         →  sverstat.py  →  src/slides/*.html +  src/shablon.html + slide_order в src/brief.md
                         →  build_deck.py → src/dist/index.html
```

**ПОРОЖДАЕМЫЕ КАТАЛОГИ — правишь их только через ленту, напрямую НИКОГДА:** `src/content/`, `src/slides/`, `src/illustrations/`, блок `slide_order:` внутри `src/brief.md`, пер-слайдовый грид внутри `src/shablon.html`. Они лежат под git и выглядят как обычные исходники — это ловушка. *Цена 29.07 в этом же проекте: исполнитель правил `content/` руками, затем запустил конвейер, который сам же заход велел запустить, — `porodit.py` снёс содержательные правки в 7 файлах, `sverstat.py` снёс раскладку в 9 файлах и перестановку слайдов целиком; всё делалось заново, ~20 минут при бюджете 20 минут до лекции, и это прямая причина того, что шесть пунктов правок остались несделанными.*

Единственное исключение — **`src/tools/porodit.py`, строка `BLOCKS`**: сейчас там пять блоков, из-за чего в колоду уезжают 27 слайдов вместо 12. Её править НУЖНО (шаг 2 задачи). Это код, а не порождённое.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Внести в ленту (`A-krasivaya.md`, `B-yazyk.md`) **все** правки из `PRAVKI-2026-07-31-diktovka.md` и собрать колоду. Порядок блоков ниже — не по тяжести дефекта, а по «заметность × дешевизна»: правка на десять секунд идёт первой всегда. *Почему так: 29.07 приоритет выстроили по важности, снятие одной фразы стоило десять секунд и стояло в хвосте — владелец наткнулся на неё первым взглядом и решил, что не внеслось ничего, хотя закрыто было семь пунктов из двадцати.*

**БЛОК 1 — снятия (каждое: найти, удалить, всё).** Из §3 документа правок: `Fib_1=Fib_2=1` в таблице · «$f(x)$ **определяется** одним признаком» · «Носитель $U$, $\lvert U\rvert=n$» · «Оба ответа — 105, считать не нужно» · «Школа требует» · «препятствие — на чётных носителях» · «Ответ зависит не от тождества, а от того, какую арену…» · пункт «то, что делали всегда, стало формальным» в Итоге · «сужение: только нечётные мощности» · «начало не выделено» · «сопряжение $\sigma\pi\sigma^{-1}$» · «Читать сторону тождества как конструкцию $F$ удаётся не всегда» · объяснение «слева $k$-подмножества, справа выборы $r$ зелёных» · пункт $F[\mathrm{id}_U]=\mathrm{id}_{F(U)}$ · абзац «Виды $F$ и $G$ изоморфны, если…».

**БЛОК 2 — структура.** Снять раздел «Примеры категорий и функторов» целиком (вместе с двумя его фигурами) · переставить «Итог» и «Категории», чтобы «Итог» стал последним · переименовать «Естественное преобразование» → «Естественный изоморфизм» · в `src/tools/porodit.py` сузить `BLOCKS` до `A-krasivaya.md` и `B-yazyk.md`.

**БЛОК 3 — переписывание текстов** по §3 документа, раздел за разделом. Формулировки владельца ставить **дословно там, где они процитированы**; своей прозы не сочинять. Слова из таблицы §1 документа не употреблять нигде, включая `aria-label` и `figcaption`.

**БЛОК 4 — иллюстрации.** Дорисовать: вторую панель «Трёх биекций» (подмножество → последовательность нулей и единиц) · диаграмму функтора в «Категориях» · шесть биекций между двумя трёхэлементными множествами в «Примерах». Поправить: снять все буквы и цифры с трёх иллюстраций «Трёх биекций» и подписи под ними · в «Почему это красиво» вынести $\sigma$ левее, поставить палочки на стрелках элементов, $U$ и $V$ большими буквами левее со стрелкой без палочки · в «Чётных и нечётных» отдать диаграмме основное место, комментарий сжать в строку сверху.

**БЛОК 5 — конвейер и глаза.** `porodit.py` → `sverstat.py` → `build_deck.py` → `_generator/render.py` → **посмотреть все PNG самому**, до показа владельцу.

### Шесть ловушек этого проекта — все оплачены, все укусят снова

1. 🔴 **CSS-класс бьёт presentation-атрибут.** `class="s-txt" font-size="17"` рисуется 13px. Нужен другой кегль — только инлайновый `style`.
2. 🔴 **Класс, выдуманный на месте, не краснеет нигде.** `<line>` без своего `stroke` не рисуется ВООБЩЕ, даже чернотой, и все гейты остаются зелёными. Берёшь примитив — бери из словаря; не хватает примитива — чини словарь, а не свой рисунок.
3. **`byudzhet_l1.py` возвращает rc=1 при корректной работе текстового захода** — его гейт проверяет «текст не тронут», то есть ровно то, что тебе поручено сделать. Читай таблицу, не код возврата.
4. 🔴 **Поля `> поле:mn 🎨 ЧТО НА КАРТИНКЕ` в ленте расходятся со ВСЕМИ нарисованными фигурами** — все пять из пяти. Они остались от прежнего захода. **Рисовать по ним нельзя: уничтожишь принятую работу.** Прав рисунок и документ правок; поля при расхождении приводи в соответствие с рисунком, а не наоборот.
5. **Адрес правки — ЗАГОЛОВОК раздела. Номера не употребляй вовсе** — ни в работе, ни в отчёте. Номер умирает при первом снятии или перестановке, а этот заход делает и то, и другое.
6. **Запрет символов на иллюстрациях — ТОЧЕЧНЫЙ, не общий.** Владелец поправил это отдельно: символы снимаются с трёх иллюстраций «Трёх биекций», где они лишние. Канон коммутативных диаграмм остаётся в силе, и в «Почему это красиво» подписи, наоборот, ТРЕБУЮТСЯ. Снесёшь их там — сломаешь то, что владелец просил починить.

### КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ)

Всё гоняется из корня репо одним блоком; **числа ниже — команды, а не значения** (в скобках — что печатала команда в момент сборки захода, 31.07; расхождение на входе означает, что кто-то уже поработал, — скажи об этом в `## ПЛАН` и не гоняйся за старым числом):

```
python3 teorkat-vvedenie/src/tools/porodit.py   && echo rc=$?
python3 teorkat-vvedenie/src/tools/sverstat.py  && echo rc=$?
python3 _generator/build_deck.py teorkat-vvedenie/src && echo rc=$?
python3 _generator/tools/check_termin.py teorkat-vvedenie/src && echo rc=$?

# 1. слайдов в колоде — обложка в slide_order не входит
grep -c "^  - " teorkat-vvedenie/src/brief.md                       # сейчас 27 → обязано стать 12
# 2. запрещённые слова в ПОРОЖДЁННОМ тексте слайдов
grep -rhoiE "носител|арен[аыуе]|голом множеств|починк|равномощн|школа требует" teorkat-vvedenie/src/content/ | sort | uniq -c   # обязано быть ПУСТО
# 3. то же в ИСТОЧНИКЕ, мимо режиссёрских полей — два числа обязаны сойтись
grep -vh "^> поле:" teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md teorkat-vvedenie/raskadrovka/teksty/B-yazyk.md \
  | grep -ioE "носител|арен[аыуе]|голом множеств|починк|равномощн|школа требует" | sort | uniq -c
#    сейчас: носител 16 · арен 6 · голом множеств 1 · починк 1 · равномощн 1 · школа требует 1 → обязано стать ПУСТО
# 4. снятый раздел не уехал в колоду
grep -ril "Примеры категорий" teorkat-vvedenie/src/content/ | wc -l  # обязано быть 0
# 5. последний слайд колоды — «Итог»
tail -1 teorkat-vvedenie/src/brief.md                                 # (проверь, что это id раздела «Итог»)
# 6. иллюстраций — ДВА числа, источник и выход, обязаны сойтись
grep -ch "<svg" teorkat-vvedenie/raskadrovka/teksty/A-krasivaya.md teorkat-vvedenie/raskadrovka/teksty/B-yazyk.md   # сейчас 9 и 3 = 12
ls teorkat-vvedenie/src/illustrations/*.svg | wc -l                                                                 # сейчас 11
#    после работы оба обязаны дать 13: 12 нынешних − 2 снятых со справочником + 3 новых.
#    Расходятся — значит фигура потерялась при порождении; это дефект, а не мелочь.
```

⚠ **«препятствие» в общий греп НЕ ставится.** Слово снимается ровно в одном месте — в «Чётных и нечётных подмножествах»; общий греп по нему в этом проекте уже однажды едва не снёс ядро раздела за час до лекции.

**Готово = все четыре rc нулевые И все шесть чисел сошлись И ты посмотрел PNG всех слайдов глазами.**

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ.** В отчёте — таблица «заголовок раздела → правки внесены / не внесены / почему», строка на каждый из **12 разделов**, и вердикт вида «внесено 12 из 12», а не «всё сделано». Раздел, где ты правку не внёс, — законный исход, если названа причина; молчаливый пропуск — брак.

## 3. ВЕРИФИКАТОР — обязателен, заход лоссовый

Снимается целый раздел, снимаются пятнадцать фраз, переставляются разделы — потерять можно тихо. Свежий субагент, **другой метод**:

- читает `PRAVKI-2026-07-31-diktovka.md` и **оригинал ленты из `git show HEAD:<путь>`** (не твою рабочую копию);
- по каждой правке §3 документа отвечает: адрес найден? внесено? — таблицей;
- отдельно ищет **снятое сверх заказа**: `git diff` по ленте, каждая удалённая строка трассируется к пункту документа. Не трассируется — потеря, и это красный вердикт;
- проверяет, что канон коммутативных диаграмм НЕ снесён в «Почему это красиво» (ловушка 6).

🔴 **Мандат чтения у верификатора ШИРЕ твоего**: он ищет то, чего ты знать не мог, и вправе открыть любой файл. **Опровержение — полноценный успех.** Брак — только выдуманный вердикт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/raskadrovka/teksty teorkat-vvedenie/src _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_pravki-diktovki.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/raskadrovka/teksty teorkat-vvedenie/src _studio/zhurnal/2026-07-30_dovodka-l1 _studio/zhurnal/2026-07-30_dovodka-l1/kod_pravki-diktovki.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie                          # из корня репо; должен быть ✅
python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-07-30_dovodka-l1     # вторая половина зоны — тоже ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Ветка `arka/mat-kostyak` ✅ (`git branch --show-current`, rc=0). Зона в HEAD НЕ чиста (`teksty/view.html` изменён + два новых `.md`) → первым ходом точка отката коммитом. Оба новых `.md` уже стоят в `KARTA.md §6` — `register_doc.py` не нужен.

### Расхождения на входе — говорю до работы, старые числа не гоняю

1. 🔴 **КРИТЕРИЙ 1 КРИВОЙ, и он провалится не по моей вине.** `grep -c "^  - " brief.md` считает ВЕСЬ `slide_order`, а `sverstat.poryadok()` кладёт туда и служебные слайды: `sl-title`, `sl-vizitka`, `sl-divider1..3`, `sl-thanks`. Сейчас команда печатает **32**, а не 27 (26 содержательных + 6 служебных) — то есть и заявленное входное число уже не сходится. Содержательных слайдов обязано стать 12, и это число печатает сам конвейер: `sverstat.py` строкой «содержательных слайдов: N» и `porodit.py` строкой «содержательных слайдов: N». Поправка к критерию: **гейт 1 читаю по этой строке**, а `grep`ом проверяю производную величину `12 + служебные`.
2. **`ls illustrations/*.svg` = 11 при 12 `<svg>` в ленте УЖЕ на входе.** Это не потеря при порождении, а несвежий выход: каталог порождаемый и с последней правки ленты не пересобирался. Первый же `porodit.py` обязан дать 12; если не даст — тогда это дефект, и я его назову.
3. 🔴 **В критерии нет шага, без которого колода собирается с ⟦MISSING-MATH⟧.** Формулы дека берутся из кэша `src/math/katex.json` (242 записи), а кэш собирается двумя командами: `sobrat_formuly.py` → `katex_kesh.js`. Я переписываю тексты и завожу новые формулы ⇒ **между `sverstat.py` и `build_deck.py` обязаны стоять эти две команды**, иначе новые формулы уедут на слайд литералом. Добавляю их в прогон и в отчёт.
4. **Два класса словаря в деке не существуют.** `.s-thin-a` (9 линий) и `.s-txt-a` (1 метка) стоят в фигуре «Почему это красиво»; в `overlay.css` дека их нет, как нет и в `build_doc.py` (это ровно ловушка 2 захода, цена записана в `SLOVAR-primitivov.md` 31.07). Рисую только 14 классами, которые в `overlay.css` реально определены; заодно чинится и та фигура, поскольку её всё равно перерисовывать. Словарь и `_generator/` — вне зоны, в отчёт.
5. **Пустые акты после сужения `BLOCKS`.** `AKT_BLOKI = [A, B, C+D, E]`; блоки C, D, E из колоды уходят ⇒ акты 3 и 4 пусты, а `poryadok()` всё равно выкладывает `sl-divider2` и `sl-divider3` — в колоде появятся два слайда-заголовка, за которыми нет ни одного слайда. Это прямое следствие ТЗ, поэтому чиню одним условием: разделитель ставится только перед НЕПУСТЫМ актом.
6. **«Покрашенные виды»: архетип расходится с фигурой и со словом владельца.** Пометка обещает «полосу справа» (344px), а фигура раздела — 820×300 (пропорция 0,37), то есть по правилу словаря она «во всю строку»; владелец говорит про неё «картинка **внизу** — хорошая». Ставлю архетип «илл-полосой-снизу»: иначе картинка, которую велено не трогать, поедет в жёлоб вчетверо уже своей ширины.

### Как исполняется перестановка «Итог» ↔ «Категории»

Порядок колоды рождается порядком файлов в `BLOCKS` и порядком `## `-разделов внутри файла. «Итог» — последний раздел `A`, «Категории» — первый раздел `B`, поэтому натуральный порядок даёт «…Итог, Категории», а нужен обратный. Переставляю **переносом раздела «Категории» из `B-yazyk.md` в `A-krasivaya.md`** — на место перед «Итогом». Почему так, а не переносом «Итога» в `B`: во втором случае акт 2 остаётся непустым и перед «Категориями» встаёт разделитель **«Зоопарк»** — заголовок ровно того содержания, которое этим же ТЗ снимается («Примеры категорий и функторов»). После переноса непустым остаётся один акт, разделителей нет вовсе, и колода ложится дословно в порядок владельца из §0 документа правок. `B-yazyk.md` остаётся в `BLOCKS` (как требует ТЗ), но разделов в нём не остаётся — говорю это прямо.

### Порядок работы

1. Точка отката коммитом. Прогон `porodit.py --inventar` — снять живой инвентарь до правок.
2. **БЛОК 1 + 2 + 3 одним проходом по ленте, раздел за разделом** (снятия дешевле всего, но резать текст в двух проходах — значит дважды читать один и тот же абзац): по каждому разделу сразу и снятия, и переписывание по §3 документа. Формулировки владельца — дословно там, где процитированы. Слова из §1 — вон отовсюду, включая `aria-label` и `figcaption`.
3. **БЛОК 2 структурно:** снять «Примеры категорий и функторов» с двумя её фигурами · перенести «Категории» · переименовать «Естественное преобразование» → «Естественный изоморфизм» · `BLOCKS` → `[A, B]` · условие на разделитель пустого акта.
4. **БЛОК 4 иллюстрации.** Три с нуля (панель 2 «Трёх биекций» · диаграмма функтора в «Категориях» · шесть биекций в «Примерах») и три правки (снять буквы и цифры с трёх панелей «Трёх биекций» + подписи под ними · перерисовать «Почему это красиво» по буквальным правкам владельца, с палочками у стрелок элементов · «Чётные и нечётные»: комментарий строкой сверху, диаграмма снизу и крупно). Канон коммутативных диаграмм в силе; в «Почему это красиво» подписи ТРЕБУЮТСЯ (ловушка 6).
5. Сцены «Трёх биекций»: три панели в ОДНОЙ полосе, по одной на сцену — это не текст, а пометка ленты: `kartinki_po_scenam` читает аннотации `(1)`, `(2)`, `(3)` из поля «Раскладка», а число сцен слайда считается из тегов текста ⇒ слайду нужен ведущий тег, объявляющий три сцены. Без аннотаций три панели встанут РЯДОМ по 440px — ровно то, что поле запрещает.
6. **БЛОК 5:** `porodit.py` → `sverstat.py` → `sobrat_formuly.py` → `katex_kesh.js` → `build_deck.py` → `check_termin.py` → `render.py` → посмотреть все PNG глазами.
7. Верификатор свежим субагентом (§3), другим методом, по `git show HEAD:` — включая поиск снятого сверх заказа.
8. Коммит зоны двумя ходами (§4), отчёт с таблицей по 12 разделам.

**Что НЕ делаю:** диктовка не закончена («продолжение следует») — того, чего в `PRAVKI-2026-07-31-diktovka.md` нет, я не додумываю. Раздел «Соответствие задач» несёт незакрытый заказ владельца (задача не придумана) — картинку под него не рисую, поле 🕳 не трогаю. Блоки `C-*`, `D-*`, `E-*` — read-only, в колоду не идут, из ленты НЕ удаляю.

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
