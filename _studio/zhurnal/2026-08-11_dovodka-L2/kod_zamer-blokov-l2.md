# Канал исполнителя — zamer-blokov-l2 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — счётная работа с расписанной механикой; решений заход не принимает, он готовит материал для интервью.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-11_dovodka-L2» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/blissful-elegant-mccarthy/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Готовится лекция 2 курса «Введение в теорию категорий»: 18 карточек слайдов в
`teorkat-vvedenie/L2/slajdy/*/slaid.md`. Текст каждого слайда разбит на **блоки** — типизированные
куски (`### [тип] мысль`). Параллельно перерабатывается генератор: структура блоков должна стать
ВИДНА на собранном слайде, чтобы зал читал, где начинается и кончается мысль. Чтобы спроектировать
это визуальное разделение, нужна статистика: сколько блоков какого типа лежит на слайде — и сейчас,
и в целевой версии. Приём, рассчитанный на один блок, разваливается на четырёх.

ЦЕЛЬ — ДВА файла:

**А. `ZAMER-BLOKOV-L2.md`** — шесть частей: **факт** (замер по 18 живым карточкам) · **черновик**
(состав блоков 20 целевых слайдов из прозы `PLAN-PRAVOK-L2 §1`) · **дельта и красные флаги** ·
**иерархия блоков** (главное–вспомогательное) · **аксиомы разметки** (А1–А11 чужого файла плюс
Г-1…Г-16, проверенные на ЦЕЛЕВОЙ версии) · **две схемы слайда** — построение и опровержение.

**Б. `KANON-FAZY-2-draft.md`** — технический документ ДЛЯ ИСПОЛНИТЕЛЯ-CLAUDE: как устроен слайд из
блоков и что проверяется, прежде чем идти дальше. Скелет и границы — §2.7. 🔴 Он **собирается из
названных источников, а не сочиняется**; чего в источниках нет — помечается маркером и оставляется
владельцу.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **Заход диагностический: он НИЧЕГО не чинит и НИЧЕГО не решает.** Он готовит материал, по
которому аналитик проведёт интервью с владельцем. Порядок ценности при нехватке времени:
**часть 1 → часть 4 → часть 6 → часть 2 → часть 5 → часть 3 → файл Б.**
🔴 Часть 6 (две схемы) поднята высоко нарочно: она то, ради чего перестраивается генератор. Не успел — прямо назови, что не сделано; файл Б
без частей 1 и 4 бессмыслен и в одиночку не сдаётся.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. Доказательство — не самостоятельный несущий блок, а дополнение к утверждению: считается отдельно и на слайде будет вспомогательным, мельче (владелец, 11.08). Подтверждено файлами: ни одно доказательство Л2 не стоит без утверждения на том же слайде
1-бис. 🔴 **Иерархия блоков на слайде фиксируется в параметрах карточки** (владелец, 11.08): центральный блок · вспомогательные несущие · доказательства · нарративные. **У слайда обязана быть одна центральная мысль, и она обязана быть выражена центральным блоком — это будущий гейт.** Именно эту иерархию генератор будет выделять визуально
2. Потолок несущих блоков на слайде — 2; три и больше это red flag на разнесение, но решается на интервью, а не механически (владелец, 11.08)
3. Ровность по времени — критерий целевой версии: длинный слайд длинен потому, что его надо делить, и после деления картина обязана стать ровнее (владелец, 11.08)
4. Время приписано БЛОКУ, а не слайду; деление слайда бесплатно (Ф2-0). Потолок 25 слайдов и 120 минут. Карточки не тронуты ни одной правкой

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/zamer-blokov-l2`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/zamer-blokov-l2`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add zamer-blokov-l2 --branch zahod/zamer-blokov-l2
  cd ../materials-wt/zamer-blokov-l2
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_studio/zhurnal/2026-08-11_dovodka-L2/ZAMER-BLOKOV-L2.md` `_studio/zhurnal/2026-08-11_dovodka-L2/KANON-FAZY-2-draft.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

🔴 **ПЕРВЫМ ходом, ДО работы — забери в git правки аналитика.** Он работает в песочнице, в `.git`
не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). На 11.08 вне git шесть путей в
папке этой арки, включая хэндофф в следующую сессию.

```
python3 _generator/tools/git_zona.py commit --zone _studio/zhurnal/2026-08-11_dovodka-L2 \
    -m "dovodka-L2: хэндофф, выгрузка сессии, сводка открытого, уроки 3 и 4" --push
```
Точный список снимай КОМАНДОЙ, а не по перечню отсюда — он мог устареть:
`python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-11_dovodka-L2`.
⚠ Пусто — значит их уже забрал параллельный заход `tipologia-slajdov`. Это нормально, иди дальше.
⚠ Команда не прошла по внешней причине (чужой лок, конфликт) — **чужое состояние репозитория не
чини**: строка в отчёт, работай дальше.

**ПОСЛЕДНИМ ходом — своя зона:**
- `ZAMER-BLOKOV-L2.md` и `KANON-FAZY-2-draft.md` — два файла, которые ты создаёшь;
- `_studio/docs/KARTA.md` — после `register_doc.py`, тем же коммитом;
- `kod_zamer-blokov-l2.md` — 🔴 **коммить его САМ вместе с отчётом.** Шаблон говорит «это делает
  аналитик при приёмке» — здесь неверно: аналитик в песочнице и в `.git` не пишет, иначе твои
  `## ПЛАН` и `## ОТЧЁТ` останутся вне git.

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- рабочая папка zamer-blokov-l2 и ветка zahod/zamer-blokov-l2 гасятся после приёмки; ветки арки и main не трогать

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/zamer-blokov-l2`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката не нужна: оба файла зоны заход СОЗДАЁТ, ничего не перезаписывая. Проверить одной командой: `ls _studio/zhurnal/2026-08-11_dovodka-L2/` — ни `ZAMER-BLOKOV-L2.md`, ни `KANON-FAZY-2-draft.md` там сейчас быть не должно. Какой-то уже есть — СТОП, спроси в `## ВОПРОСЫ`, не затирай.
- **Прочитать ТОЛЬКО эти файлы-якоря, проект не изучай:**
  1. все 18 файлов `teorkat-vvedenie/L2/slajdy/*/slaid.md` — **READ-ONLY, не править ни одной буквы**;
  2. `teorkat-vvedenie/L2/brief.md` — `slide_order`, порядок слайдов;
  3. `_studio/zhurnal/2026-08-11_dovodka-L2/PLAN-PRAVOK-L2.md` — **только §1 и §3**. §1 — таблица
     двадцати целевых слайдов с минутами и прозой правок; это единственный источник для части 2.
  4. `_studio/zhurnal/2026-08-11_dovodka-L2/SPEC-BLOKA.md` — одна страница, спецификация формата
     блока. **Источник нормативной части файла Б** (§2.7). Её §7 «развилки» — это ОТКРЫТЫЕ
     вопросы, ответов на них нет: в файл Б они переносятся маркером, не решением.
  5. `_studio/zhurnal/2026-08-10_dizajn-i-metriki/AKSIOMY-RAZMETKI.md` — **одиннадцать аксиом
     разметки А1–А11 со счётом по каждой и с готовыми командами в §8.** Написан другим аналитиком,
     тем, кто перестраивает генератор; его §10 прямо называет ЭТОТ заход своим продолжением.
     🔴 Читай его целиком и не переоткрывай то, что там уже посчитано (§2.5, ход 1).
  🔴 Больше НИЧЕГО не открывай: ни `KONTRAKT-L2.md`, ни `RASSHIFROVKA-*`, ни `PRAVILA-FAZY-2.md`,
  ни конспект Л1. Они длинные, и заход в них утонет. Не хватило чего-то — это строка в
  `## ВОПРОСЫ`, а не повод расширять чтение.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Собрать **`_studio/zhurnal/2026-08-11_dovodka-L2/ZAMER-BLOKOV-L2.md`** — три части, строго по порядку.

### 2.0 Словарь — без него части 2 и 3 будут посчитаны неверно

**Блок** — строка `### [тип] мысль` и текст под ней до следующей `###`.
Типы, живущие в Л2: `narrativ` · `opredelenie` · `utverzhdenie` · `primer` · `dokazatelstvo`.

🔴 **Считай блоки ТОЛЬКО в разделе `## Математика — развёрнуто`.** В разделе `## Текст слайда —
сжато` те же заголовки повторяются один в один — двойной счёт удвоит все числа. Проверка на себя:
всего блоков в «Математике» по всем 18 карточкам обязано выйти **39**. Вышло 78 — ты считаешь оба
раздела.

**Три счётные категории — это главное различение захода:**

| категория | что входит | почему отдельно |
|---|---|---|
| **несущий блок** | `opredelenie` · `utverzhdenie` · `primer` | это мысли, ради которых слайд существует. Именно их считает потолок «максимум 2» |
| **вспомогательный** | `dokazatelstvo` | 🔴 **не самостоятельная единица, а дополнение к утверждению.** Владелец: слайд, где формулировка и доказательство рядом, автоматически выглядит перегруженным — и это нормально, делить его бессмысленно. На слайде доказательство пойдёт мельче, вспомогательным. **Из счёта несущих вычитается** |
| **связка** | `narrativ` | переход, анонс, итог. В счёт несущих не входит |

⚠ **Замер, сделанный аналитиком при сборке этого захода (проверь и подтверди или опровергни):**
39 блоков = 13 `narrativ` + 26 остальных; среди 26 — `utverzhdenie` 9, `opredelenie` 7, `primer` 6,
`dokazatelstvo` 4. Несущих на слайде: у 6 слайдов 0, у 3 — один, у 8 — два, у одного (`retrakt`) — три.
Все 4 доказательства стоят при ровно одном утверждении того же слайда.
🔴 **Это предпосылка, а не факт для тебя.** Пересчитай своими командами. Разошлось — назови
расхождение поимённо и считай правым СВОЙ счёт, а не этот абзац; расхождение уходит в `## ВОПРОСЫ`.

### 2.1 ЧАСТЬ 1 — ФАКТ: замер по 18 живым карточкам

Таблица, одна строка на слайд, порядок — как в `slide_order` из `brief.md`:

`имя` · `minuty` · всего блоков · `narrativ` · `dokazatelstvo` · **несущих** · перечень типов
несущих · `centralnyj_blok` (тип и мысль) · `tip_verstki` · сколько иллюстраций.

Плюс четыре сводки под таблицей, каждая — вывод команды:

1. **Распределение несущих:** сколько слайдов с 0, 1, 2, 3, 4+ несущими блоками.
2. **Распределение типов:** сколько всего блоков каждого из пяти типов.
3. **Распределение минут** по содержательным слайдам (разделители с `minuty: 1` — отдельно):
   отсортированный список, минимум, максимум, медиана, среднее.
4. **Какие типы соседствуют на одном слайде** — перечень встречающихся сочетаний с частотой
   (например «`opredelenie` + `utverzhdenie`» — сколько раз). Это прямой вход в проектирование
   визуального приёма: сочетание, встречающееся один раз, приёма не заслуживает.

### 2.2 ЧАСТЬ 2 — ЧЕРНОВИК: состав блоков двадцати целевых слайдов

🔴 **Это самая ценная часть захода и единственная, где ты что-то СОЧИНЯЕШЬ.**

`PLAN-PRAVOK-L2 §1` даёт двадцать целевых слайдов с минутами и прозой правок. Списка блоков там
нет — но проза часто перечисляет их почти прямо. Примеры того, что в ней лежит:

- `dvojstvennoe-prostranstvo`: *«разложить на блоки по одному: определение $V^*$ · $V^*$ —
  векторное пространство, как складываем и умножаем · координаты как элементы $V^*$ · разложение
  функционала по базису · двойственность — функтор»* — это пять блоков, названных прямым списком;
- `kategoriya`: *«определение категории + развёрнутый пример $\mathbf{FinSet}^{\mathrm{bij}}$»* — два;
- `funktor` ★: *«определение функтора + пример из Л1 + анонс»* — три;
- `nekanonicheskij-izomorfizm`: *«категория символом → два функтора → канонического изоморфизма
  нет → на объектах есть, на функторах нет»* — цепочка блоков.

**Что делать:** по каждому из двадцати целевых слайдов назвать список блоков `[тип] мысль`.
У каждого блока — **пометка происхождения, обязательна:**

| пометка | значение |
|---|---|
| `есть` | блок живёт в сегодняшней карточке, переезжает как есть. Назови, из какой карточки |
| `делится` | сегодняшний блок распадается на несколько. Назови исходный |
| `новый` | блок прямо назван прозой плана и в карточках его нет |
| `выведен` | 🔴 **блока в плане нет, ты его домыслил по смыслу.** Это законно и нужно, но помечать ОБЯЗАТЕЛЬНО: аналитик подтверждает такие блоки у владельца поимённо |

**Два новых слайда плана — `anons-dvojstvennogo` и `kategoriya-vect-iso` — карточек не имеют вовсе.**
Их состав целиком `новый` / `выведен`. Так и пометь, не пытайся найти несуществующий файл.

⚠ Минуты целевых слайдов бери из плана как есть, **своих не назначай.** План — источник правды по
времени; твоё дело показать, что из этих минут следует.

### 2.3 ЧАСТЬ 3 — ДЕЛЬТА И КРАСНЫЕ ФЛАГИ

Три проверки черновика против критериев, названных владельцем 11.08:

**Ф-1. Потолок несущих блоков — 2.** Перечисли целевые слайды, у которых по черновику три и больше
несущих блоков. По каждому: сколько минут, какие блоки, и **где напрашивается разрез** — по какой
границе слайд делится на два так, чтобы у каждой половины остался свой центральный блок.
⚠ Это red flag, а не приговор: решает интервью. Твоё дело — предъявить список и предложить разрез.

**Ф-2. Ровность по времени.** Владелец: длинный слайд длинен потому, что его надо делить, и после
деления картина обязана стать **ровнее**. Посчитай разброс и стандартное отклонение минут в трёх
состояниях и положи рядом:
  · сегодняшние 18 карточек;
  · целевые 20 слайдов, как записаны в плане;
  · целевые 20 после исполнения делений, которые план **сам предписывает** (`vektornye-prostranstva`
    10 мин и `dvojstvennoe-prostranstvo` 12 мин помечены «делится надвое»).
Стало ровнее или нет — скажи прямо числом, не оценкой. Не стало — это находка, а не ошибка счёта.

**Ф-3. Один центральный блок, без конкуренции.** У каждого целевого слайда назови кандидата в
`centralnyj_blok`. Где кандидатов два и они конкурируют — отдельный список: это второй, независимый
от Ф-1, признак того, что слайд надо делить.
📎 Опора: сегодня `centralnyj_blok` заполнен у всех 18 и ни разу не является доказательством или
примером — только `utverzhdenie`, `opredelenie` либо (на чисто связочных слайдах) `narrativ`.

### 2.4 ЧАСТЬ 4 — ИЕРАРХИЯ БЛОКОВ: главное против вспомогательного

🔴 **Ради этой части затевается визуальная переработка генератора.** Владелец: *«у слайда должна
быть центральная мысль, и эта мысль должна быть выражена центральным блоком. Это должно быть одним
из гейтов. Мы её будем выделять даже на уровне визуала»*.

Разложи блоки каждого слайда — и сегодняшнего, и целевого — на **четыре яруса**:

| ярус | что | как определяется |
|---|---|---|
| **1. центральный** | ровно один | поле `centralnyj_blok:` в шапке карточки |
| **2. вспомогательный несущий** | 0, 1 или больше | несущий блок (`opredelenie`/`utverzhdenie`/`primer`), не являющийся центральным |
| **3. доказательство** | 0 или больше | см. 🔴 ниже — считать НЕ только размеченные |
| **4. нарративный** | 0 или больше | блоки `[narrativ]` |

🔴 **ДОКАЗАТЕЛЬСТВО БЫВАЕТ НЕРАЗМЕЧЕННЫМ, И ЭТО ГЛАВНАЯ ЛОВУШКА ЭТОЙ ЧАСТИ.**
Проверено аналитиком при сборке захода: **блоков `### [dokazatelstvo]` в Л2 четыре, а доказательств
десять.** Шесть живут прозой ВНУТРИ блока `[utverzhdenie]`, курсивной врезкой вида
`*Доказательство — три строки.*` — на слайдах `bazis-i-matricy`, `dvazhdy-dvojstvennoe`,
`dvojstvennoe-prostranstvo`, `fundamentalnaya-gruppa`, `retrakt`, `zamena-bazisa`.
Для генератора такое доказательство **невидимо**: он режет текст по `### [тип]`, а внутрь тела не
смотрит — значит ни отделить его, ни дать ему другой кегль не сможет. Убедись сам одной командой:
`grep -c '^### \[dokazatelstvo\]' teorkat-vvedenie/L2/slajdy/*/slaid.md` — она найдёт только
размеченные, и её вывод сравни со своим счётом курсивных врезок. Дай **отдельную таблицу спрятанных
доказательств**: слайд · внутри какого блока · первые слова врезки.
⚠ Число «десять» — предпосылка аналитика, а не твой факт. Разошлось — прав твой счёт, расхождение в
`## ВОПРОСЫ`.

**Что выдать по части 4:**

1. **Таблица ярусов** по 18 сегодняшним слайдам: имя · центральный блок (тип и мысль) · сколько
   вспомогательных несущих · сколько доказательств (размеченных + спрятанных) · сколько нарративных.
2. **Та же таблица по 20 целевым слайдам** из части 2 — с кандидатом в центральный блок.
3. **Профили ярусов и их частота.** Сведи каждый слайд к строке вида `Ц1-В1-Д1-Н0` (один
   центральный, один вспомогательный, одно доказательство, ноль нарративных) и посчитай, сколько
   слайдов на каждый профиль — сегодня и в целевой версии. **Это главное число захода:** приём,
   рассчитанный на один профиль, разваливается на редком; профиль, встречающийся один раз, приёма
   не заслуживает.
4. **Слайды, где центральный блок под вопросом:** нет вовсе · это `[narrativ]` при наличии несущих
   блоков · два кандидата конкурируют.
   📎 Известный случай, проверь и опиши: `teorema-brauera` — два нарративных блока, ноль несущих,
   `centralnyj_blok` указывает на нарративный. Слайд с теоремой в названии не несёт утверждения.

### 2.5 ЧАСТЬ 5 — АКСИОМЫ РАЗМЕТКИ: проверка на ЦЕЛЕВОЙ версии

🔴 **Не начинай с нуля. Аксиомы уже сформулированы и уже посчитаны — другим аналитиком, на этих же
18 карточках.** Файл `_studio/zhurnal/2026-08-10_dizajn-i-metriki/AKSIOMY-RAZMETKI.md` (якорь 5)
несёт одиннадцать аксиом А1–А11 со счётом по каждой и с готовыми командами в §8. Его §10 прямо
называет этот заход своим продолжением.

**Твоя работа с ними — три хода, ни одного лишнего:**

**Ход 1. Перепроверить А1–А11 на 18 живых карточках** его же командами (§8 того файла).
Совпало — так и напиши одной строкой на аксиому. **Разошлось — это находка**, назови обе цифры и
не сглаживай: файл собран 11.08, карточки с тех пор не менялись, расхождение означало бы ошибку
счёта у одного из нас.

**Ход 2. 🔴 ГЛАВНОЕ: прогнать те же одиннадцать аксиом по ЦЕЛЕВОЙ версии** — по двадцати слайдам
черновика из части 2. Аксиома, держащаяся на обеих выборках, — правило **метода**; держащаяся
только на текущей — особенность L2, и в канон она не идёт. Выдай таблицу:
`аксиома · держится сейчас (X/18) · держится в целевой (Y/20) · вердикт`.

⚠ **Расширять выборку на другие лекции НЕ надо, и вот почему** — предпосылка §10 того файла ложна,
проверено командой при сборке захода:
`find . -path '*/slajdy/*/slaid.md' | sed 's|/slajdy/.*||' | sort -u` даёт только
`teorkat-vvedenie/L2` и фикстуры `_generator/tools/fixtures/**`. В `kurs leto 2026` и
`informacia-i-kody` карточек **ноль**. **L2 — единственная дека в карточном формате во всём
репозитории.** Целевая версия — единственная доступная сегодня вторая выборка. Скажи это в отчёте
отдельной строкой: она отменяет задание §10 того файла.

**Ход 3. Добить своими гипотезами** — ниже. Они НЕ пересекаются с А1–А11; пересекавшиеся я убрал.

**Форма ответа по каждой гипотезе — жёсткая, четыре поля:**

```
Г-N. <формулировка>
     СЧЁТ: сейчас X из 18 · в целевой Y из 20   ·   КОМАНДА: <чем посчитано>
     ИСКЛЮЧЕНИЯ: <слайд/блок поимённо, или «нет»>
     ВЕРДИКТ: закон · закон с исключением · не закон · не проверяемо механически
```

⚠ **Вердикт «не закон» — полноценный результат, не неудача.** Гипотеза, рассыпавшаяся на числах,
экономит правило, которое иначе завели бы на глаз. Не подгоняй.
⚠ **Гипотеза с ОДНИМ исключением — самая интересная.** Исключение это либо дефект слайда, либо
законный подвид; различить может только владелец — такие уходят в файл Б маркером
`⟦РЕШАЕТ ВЛАДЕЛЕЦ⟧`.

📎 **Замеры аналитика при сборке захода (предпосылки, не факты — пересчитай):** жанровый ярлык у
несущих блоков «Математики» 26/26, у нарративных 0/13 · врезка `> поле:mn` 39/39 · медианное сжатие
«Математика → Текст слайда»: `opredelenie` 2,1× · `narrativ` 2,4× · `dokazatelstvo` 3,7× ·
`primer` 3,9× · `utverzhdenie` 4,1×.

#### Уровень БЛОКА

- **Г-1.** Каждый блок несущего типа **в разделе «Математика»** открывается жанровым ярлыком
  (`**Определение (…).**`, `**Утверждение (…).**`, `**Пример (…).**`, `*Доказательство …*`),
  а `[narrativ]` — никогда. ⇒ ярлык есть машинный различитель «мысль или связка».
  ⚠ Проверь отдельно, держится ли то же в разделе «Текст слайда» — там ярлыков может не быть вовсе.
- **Г-2.** У каждого блока **ровно одна** врезка `> поле:mn` — не ноль и не две.
- **Г-3.** Блок `[opredelenie]` на экране маркированного списка почти никогда не содержит.
  🔴 Проверь это вместе с находкой А11 («пример всегда список, но список не всегда пример»):
  `KONTRAKT-L2 §1.5` требует обратного — «определение подаётся списком по пунктам, не абзацем».
  Числа против канона — назови прямо. (Сам `KONTRAKT` не открывай, требование процитировано целиком.)
- **Г-4.** Есть потолок длины блока на экране: ни один блок «Текста слайда» не длиннее N слов.
  Найди фактический максимум и назови блок-рекордсмен.
- **Г-5.** Коэффициент сжатия «конспект → экран» устойчив внутри типа. Дай **не только медиану, но и
  разброс** (мин–макс по типу). Разброс шире, чем вдвое, — медиана врёт, и Г-5 не закон.
  Это даёт норматив фазе 3: сколько слов конспекта во сколько слов экрана превращается.
- **Г-6.** В блоке `[primer]` всегда есть хотя бы одна формула `$…$`. То же отдельно для
  `[utverzhdenie]` и `[opredelenie]`.
- **Г-7.** Внутри одного блока никогда не стоит двух жанровых ярлыков (признак смешанного блока, Ф2-8).

#### Уровень СЛАЙДА

- **Г-8.** На слайде не бывает двух блоков одного типа. ⇒ прямой тест Ф2-8 «одна мысль — один блок».
- **Г-9.** Минут на слайде ÷ число блоков (несущие + доказательства) — величина, слабо меняющаяся от
  слайда к слайду. Дай список значений, мин, макс, медиану — и то же по целевой версии.
- **Г-10.** Фактическое число слов раздела «Текст слайда» ÷ `minuty` — величина, слабо меняющаяся.
  ⚠ Считай по ФАКТУ, а не по `byudzhet_slov`: поле может расходиться с текстом, и тогда интереснее факт.
- **Г-11.** Фактический `tip_verstki` соответствует правилу выбора вёрстки: **много текста →
  `tolko_tekst`** (картинка, если нужна, уезжает на отдельный слайд) · **средне + иллюстрация →
  `polosa_vertikalnaya`** · **средне без иллюстрации → `tolko_tekst`** · **мало + иллюстрация →
  `polosa_gorizontalnaya`** · **мало и без иллюстрации → слайд объединяется с соседним**.
  Границы «много/средне/мало» в словах не заданы — выведи их из данных сам, скажи, при каких
  границах правило выполняется на максимальном числе слайдов, и назови слайды, которых не спасает
  ни одна граница.
- **Г-12.** Полосы без иллюстрации не бывает: `tip_verstki` на `polosa_` ⇒ `illustracii` непуст.

#### Уровень ЛЕКЦИИ

- **Г-13.** Доля блоков `[narrativ]` близка к трети от всех блоков.
- **Г-14.** Второй прогон повторяющегося узора **короче первого** — по числу блоков и по минутам.
  Границы актов бери по слайдам-разделителям (`razdelitel-*`).
- **Г-15.** Слайды с нулём несущих блоков стоят только на границах — вход, разделители, итог.
  📎 Смежно с А6 («одинокий блок — нарративный», 5/5). Известный кандидат в исключения:
  `teorema-brauera` — два нарративных блока, ноль несущих, и это слайд с теоремой в названии.
- **Г-16.** Каждое утверждение лекции либо доказано (блок `[dokazatelstvo]` либо доказательство
  внутри того же блока), либо явно объявлено недоказанным. Найди утверждения без того и без другого.
  ⚠ Отдельным числом: **сколько доказательств живёт только в «Математике» и на экран не выходит.**
  Это законно — конспект лектора, — но число показывает, сколько лекции идёт голосом.

🔴 **Новых гипотез сверх А1–А11 и Г-1…Г-16 не выдумывай.** Заметил закономерность вне списка —
строка в `## ВОПРОСЫ`, а не семнадцатая проверка.

### 2.6 ЧАСТЬ 6 — ДВЕ СХЕМЫ СЛАЙДА: самое ценное, что нашлось

🔴 **Это главная гипотеза захода, и она уже подтверждена на текущей деке. Твоя работа — проверить
её на ЦЕЛЕВОЙ версии, где она может и рассыпаться.**

Замер аналитика при сборке (пересчитай, это предпосылка). Обозначения: О `opredelenie` ·
П `primer` · У `utverzhdenie` · Д `dokazatelstvo` · Н `narrativ`.

**С-1. Нарратив не заходит внутрь ядра.** Схема `Н* · <ядро без Н> · Н*` держится **17 из 18**;
единственный нарушитель — `inyekcii` (`УНПД`). Это тот же нарушитель, что у А2 и А10 — три аксиомы
чинятся одной перестановкой.

**С-2. Внутри ядра порядок НЕ свободен — он расщепляется на две схемы, и расщепление содержательное:**

| схема | последовательность статусных | держится | что общего у слайдов |
|---|---|---|---|
| **ПОСТРОЕНИЕ** | `О → П → У → Д?` | **7 из 7** | центральное утверждение положительное |
| **ОПРОВЕРЖЕНИЕ** | `У → П? → Д` | **3 из 3** | доказывается **несуществование** конструкции |

Опровержения: `centr-gruppy` («функтора центра не существует»), `inyekcii` («инъекции не
продолжаются до функтора»), `nekanonicheskij-izomorfizm` («канонического изоморфизма нет»).
**Ни одно построение не начинается с утверждения; ни одно опровержение не начинается с определения.**
Разделение чистое, исключений нет.

Это ровно порядок П8, продиктованный владельцем («сначала объявляем, что доказываем, потом собираем
материал»), — но он оказался не пожеланием к одному слайду, а вторым жанром.

**Что выдать по части 6:**

1. **Пересчёт С-1 и С-2** на 18 живых карточках — своими командами, со своими числами.
2. 🔴 **Классификация всех 20 целевых слайдов** по схеме: `ПОСТРОЕНИЕ` · `ОПРОВЕРЖЕНИЕ` ·
   `служебный` (ноль несущих) · `НЕ КЛАССИФИЦИРУЕТСЯ`. Последняя категория — самая важная: это
   кандидаты на пересборку, и назвать их надо поимённо с причиной.
3. **Проверка обратной импликации.** Схема ОПРОВЕРЖЕНИЯ предсказывает: если центральное утверждение
   формулирует несуществование, слайд обязан начинаться с `[utverzhdenie]` и нести `[dokazatelstvo]`.
   Найди в целевой версии утверждения о несуществовании и проверь, выполняется ли это. Не
   выполняется — это дефект целевой версии, а не гипотезы.
4. **Достаточно ли двух схем.** Прямой вопрос генератору: покрывают ли две схемы все содержательные
   слайды целевой версии, или нужна третья. Нужна — опиши её так же: последовательность и признак,
   по которому она узнаётся.
   ⚠ Третью схему **не изобретай ради красоты**: она вводится, только если есть минимум два слайда,
   которые ни в одну из двух не ложатся, и у них есть общий содержательный признак.

### 2.7 ФАЙЛ Б — `KANON-FAZY-2-draft.md`, технический документ для исполнителя-Claude

**Кто его читает.** Будущий исполнитель Claude с пустым контекстом, которому дали собрать или
проверить слайд. Он не знает проекта. Документ обязан быть самодостаточным и **операциональным**:
каждое утверждение либо говорит, что делать, либо называет команду, которая краснеет.

🔴 **ГЛАВНОЕ ОГРАНИЧЕНИЕ — НИЧЕГО НЕ СОЧИНЯТЬ.** Источников ровно три: `SPEC-BLOKA.md`,
секция «ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ» этого захода, и твои собственные замеры из частей 1–4.
Всё, чего в них нет, пишется маркером и НЕ додумывается:

```
⟦РЕШАЕТ ВЛАДЕЛЕЦ: <вопрос одной строкой>⟧
```

Документ с двадцатью такими маркерами — **хороший** результат. Документ, где маркеров нет, потому
что ты ответил за владельца, — брак, и он дороже пустоты: его прочитают как согласованный.

**Скелет — шесть разделов, ровно в этом порядке:**

1. **Что такое блок** — тип, мысль, тело; закрытый словарь типов; где блоки живут в карточке.
   Источник: `SPEC-BLOKA §0` и §1.
2. **Четыре яруса** — центральный / вспомогательный несущий / доказательство / нарративный.
   Источник: §2.4 выше.
3. **Что обязано быть внутри блока каждого типа** — слоты. Источник: `SPEC-BLOKA §2`.
4. **Что проверяется у слайда, прежде чем идти дальше** — список проверок. Каждая строка:
   *что проверяем · чем ловится сегодня · чем должно ловиться*. 🔴 Проверка, для которой нет
   ловушки, помечается `ЛОВУШКИ НЕТ` — не выбрасывается и не выдумывается.
   Источники: `SPEC-BLOKA §6` · критерии из «ЧТО ФИНАЛИЗИРОВАНО» · **все гипотезы части 5 с вердиктом «закон» и «закон с исключением»** — с числом и командой.
5. **Числа-ориентиры** — из твоих частей 1–5: типичное число блоков на слайде, типичные профили
   ярусов, разброс минут. 🔴 Каждое число с командой, которая его печатает (`KONSTITUCIYA §10`).
6. **Открытое** — все `⟦РЕШАЕТ ВЛАДЕЛЕЦ⟧` одним списком, чтобы владелец прошёл их подряд.

⚠ **ЧЕГО В ФАЙЛЕ Б БЫТЬ НЕ ДОЛЖНО** — это границы, и они названы владельцем, не тобой:
- **деления фаз конвейера** (что уходит в фазу 1, что в фазу 2). Владелец обдумывает это отдельно,
  решение за ним и за аналитиком;
- **критериев «хороший слайд / плохой слайд», не выведенных из трёх источников.** Соблазн написать
  общие слова про ясность и лаконичность — самая дорогая ошибка этого захода;
- **правки существующего канона** `_studio/konvejer/` — он вне зоны и вне чтения.

### 2.8 Чего НЕ делать

- **Не править карточки** и `brief.md`. Ни одной буквы. Зона — два новых файла, оба в папке арки.
- **Не пересчитывать минуты** и не переназначать их. План — источник правды по времени.
- **Не судить содержание математики.** «Пример слабый», «утверждение лишнее» — не твой суд.
- **Не проектировать визуальный приём.** Твоё дело — статистика, по которой его спроектируют.
- **Не расширять чтение** за три якоря §0.

### Готовность

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — каждый пункт печатает команда, не память:**

1. `ls _studio/zhurnal/2026-08-11_dovodka-L2/` → в списке появились ОБА: `ZAMER-BLOKOV-L2.md` и
   `KANON-FAZY-2-draft.md`.
2. В части 1 ровно **18** строк-слайдов; сумма блоков по колонке «всего» = **39**.
   Не 39 — почти наверняка посчитаны оба раздела карточки (см. §2.0), пересчитай.
3. В части 2 ровно **20** целевых слайдов, и **у каждого блока стоит пометка происхождения** из
   четырёх допустимых. Блок без пометки — критерий провален.
4. В части 3 присутствуют все три проверки Ф-1, Ф-2, Ф-3, и у Ф-2 напечатаны **три** набора чисел.
4-бис. В части 4 есть таблица спрятанных доказательств и таблица частоты профилей ярусов
   (`Ц…-В…-Д…-Н…`) — обе, и по сегодняшним 18, и по целевым 20.
4-квт. В части 5 разобраны **все 11** аксиом А1–А11 (таблица «сейчас X/18 · в целевой Y/20») и
   **все 16** гипотез Г-1…Г-16 с четырьмя полями каждая. Проверить:
   `grep -c '^     ВЕРДИКТ:' <файл А>` → 16.
4-пят. В части 6 классифицированы **все 20** целевых слайдов по схеме (построение · опровержение ·
   служебный · НЕ КЛАССИФИЦИРУЕТСЯ) и дан прямой ответ, хватает ли двух схем.
4-тер. Файл Б содержит все **шесть** разделов скелета §2.5 и хотя бы один маркер
   `⟦РЕШАЕТ ВЛАДЕЛЕЦ:` — ноль маркеров означает, что ты ответил за владельца, и это провал критерия,
   а не успех. Проверить командой: `grep -c '⟦РЕШАЕТ ВЛАДЕЛЕЦ' <файл Б>`.
5. 🔴 **ЖИВОЙ ПРОГОН на реальном объекте:** `python3 _generator/sborka/gejt_kartochki.py
   teorkat-vvedenie/L2` — вывод и `echo $?` дословно в файл.
   ⚠ При сборке захода он был **зелёный** («проверено 18 из 18, замечаний 0»), и это ожидаемо: ни
   одна величина этого замера гейтом не проверяется. Зелёный гейт замер не отменяет. Покраснел —
   значит кто-то тронул карточки между сборкой захода и прогоном: назови поимённо и **остановись**.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «слайдов с тремя несущими нет», а «нет, проверено
20 из 20». Без охвата вердикт не принимается — «проверено 2 из 20» и «20 из 20» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Верификатор потерь не нужен: операция не лоссовая** — заход только читает и создаёт один новый
файл, ничего не двигает и не перезаписывает.

⚠ Но **самопроверка счёта обязательна и делается ВТОРОЙ командой, не памятью.** Числа этого захода —
его единственный продукт; посчитанное один раз не проверено. Как минимум:
`ls teorkat-vvedenie/L2/slajdy/*/slaid.md | wc -l` → 18, и столько же строк в части 1;
сумма колонки «всего блоков» → 39;
`grep -c` по каждому типу отдельно → сумма пяти чисел совпадает с 39.
Разошлось — назови, что именно, и не сглаживай.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/2026-08-11_dovodka-L2/ZAMER-BLOKOV-L2.md _studio/zhurnal/2026-08-11_dovodka-L2/KANON-FAZY-2-draft.md _studio/docs/KARTA.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/2026-08-11_dovodka-L2/ZAMER-BLOKOV-L2.md _studio/zhurnal/2026-08-11_dovodka-L2/KANON-FAZY-2-draft.md _studio/docs/KARTA.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_studio/zhurnal/2026-08-11_dovodka-L2/ZAMER-BLOKOV-L2.md` `_studio/zhurnal/2026-08-11_dovodka-L2/KANON-FAZY-2-draft.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-11_dovodka-L2/ZAMER-BLOKOV-L2.md` → ✅; то же для `_studio/zhurnal/2026-08-11_dovodka-L2/KANON-FAZY-2-draft.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
