# Канал исполнителя — dovodka-pered-lekciej (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — правки визуальные, судятся глазами, и при жёстком дедлайне нужен тот, кто сам решит, где остановиться.
>
> 🔴 **ТЫ УЖЕ ЗАПУЩЕН В ТЕРМИНАЛЕ ВЛАДЕЛЬЦА, В ОСНОВНОЙ ПАПКЕ РЕПОЗИТОРИЯ, НА ЕГО РАБОЧЕЙ ВЕТКЕ.** Никаких `worktree`, никаких `checkout`, ветку не переключать: собранный дек должен лежать ровно там, откуда владелец его откроет через два часа. Разрешения — режим обхода (bypass): ничего не переспрашивай, работай.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-11_dovodka-L2» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/trusting-confident-hawking/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Собранный дек лекции `teorkat-vvedenie/L2` (26 слайдов, HTML 1440×810). Владелец
посмотрел его глазами и наговорил правки. **Лекция, на которой он этот дек показывает,
начинается менее чем через 2,5 часа от момента твоего старта.**

ЦЕЛЬ: внести правки по фазам Ф1–Ф5 (§2), **коммитя после каждой фазы**, чтобы в любой момент
на диске лежала показываемая версия.

🔴 **ГЛАВНОЕ ОГРАНИЧЕНИЕ — ВРЕМЯ, И ОНО ВАЖНЕЕ ПОЛНОТЫ.** Владелец дословно: «если он будет
больше работать, это никуда не получится». Прошлый заход шёл 2,5 часа и сжёг 400 тысяч токенов
на небольшие изменения, и владелец не понимает, почему. **Работай минимально тяжёлым способом:**
не изучай проект, не читай ничего сверх названного в §0, не рефактори, не пиши инструментов, не
объясняй в чат то, что и так делаешь. Одна правка слайда — **не более 15 минут**. Уперся дольше
— бросай эту правку, пиши в отчёт и иди дальше.

🔴 **СТОП ДО ЦЕЛИ.** Успеть все пять фаз не обязательно и не ожидается. Обязательно другое:
**каждая законченная фаза закоммичена**. Незакоммиченная работа при таком дедлайне равна
отсутствующей. Дошёл до границы времени посреди фазы — откати незаконченное или закоммить
отдельно с честной пометкой, но НЕ оставляй дек в несобираемом состоянии.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. ДЕДЛАЙН ЖЁСТКИЙ: лекция начинается менее чем через 2,5 часа от старта; успеть важнее, чем сделать всё
2. порядок фаз строгий, каждая заканчивается КОММИТОМ: Ф1 полосы и воздух -> Ф2 правки слайдов -> Ф3 обложка -> Ф4 сцены -> Ф5 блоки
3. Ф5 (блоки) делать ТОЛЬКО если осталось время и всё предыдущее закоммичено; на диске должны остаться ДВЕ версии — без блоков и с блоками
4. прошлый заход шёл 2,5 часа и 400 тысяч токенов на небольшие правки — так нельзя; работать минимально тяжёлым способом
5. правки отдельных слайдов раздать субагентам параллельно; на один слайд не более 15 минут
6. непонятно, откуда берётся сцена — лучше её не ставить

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `teorkat-vvedenie/L2/` `_generator/skeleton/` `_generator/sborka/` `_studio/zhurnal/2026-08-11_dovodka-L2/kod_dovodka-pered-lekciej.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- после КАЖДОЙ фазы отдельным коммитом — владелец должен иметь на диске показываемую версию в любой момент

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- ветку не гасить — работа идёт на рабочей ветке владельца

- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `teorkat-vvedenie/L2/` `_generator/skeleton/` `_generator/sborka/` `_studio/zhurnal/2026-08-11_dovodka-L2/kod_dovodka-pered-lekciej.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО это, поимённо. Проект не изучай, по сторонам не смотри:**
  - `teorkat-vvedenie/L2/brief.md` — `slide_order` и поля обложки.
  - Карточки ровно тех слайдов, которые правишь (§2 называет их по имени папки) — не все 23.
  - `_generator/skeleton/base.css`, `_generator/skeleton/uzor.css`, `_generator/sborka/tipy.py` — для Ф1.
  - `_studio/zhurnal/2026-08-10_dizajn-i-metriki/SPEC-blochnaya-model.md` — **только для Ф5**, не раньше.
  ⚠ Прошлый заход сгорел на широком чтении. Файл, не названный здесь, тебе не нужен.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА — пять фаз строго по порядку, коммит после каждой

### 🔴 КАРТА НОМЕРОВ — владелец говорит номерами, генератор знает имена

Владелец называет слайды НОМЕРАМИ в собранном деке. Соответствие выведено при сборке захода из
`brief.md → slide_order` со сдвигом **+2** (обложка и визитка стоят перед первой карточкой; в
собранном деке 26 слайдов = 23 карточки + обложка + визитка + финальный). **Сдвиг подтверждён
тремя независимыми совпадениями с речью владельца:** «седьмой слайд, векторное пространство» →
`vektornye-prostranstva`; «базис, 11 слайд» → `bazis-i-matricy`; «категория, в которой мы
работаем, десятый» → `kategoriya-vect-iso`.

| № владельца | карточка | тип вёрстки | иллюстрации |
|---|---|---|---|
| 4 | `kategoriya` | `polosa_gorizontalnaya` | `kompoziciya-perestanovok` |
| 5 | `funktor` | `polosa_gorizontalnaya` | `funktor-na-podmnozhestvah` |
| 7 | `vektornye-prostranstva` | `polosa_vertikalnaya` | `vektory-na-ploskosti` |
| 9 | `izomorfizm` | `polosa_gorizontalnaya` | `izomorfizmy-primery` |
| 10 | `kategoriya-vect-iso` | `polosa_gorizontalnaya` | `funktor-funkcii-na-mnozhestve` |
| 11 | `bazis-i-matricy` | `tolko_tekst` | — |
| 12 | `zamena-bazisa` | `polosa_vertikalnaya` | `kvadrat-zameny-bazisa` |

**Проверь карту первым делом, одной командой** — открой собранный дек и убедись, что 4-й слайд
это «Категория», а 10-й «Категория, в которой мы работаем». Не сошлось — пересчитай сдвиг и
скажи в `## ПЛАН`; дальше работай по СВОЕЙ карте, а не по моей.

---

### Ф1. ПОЛОСЫ ДО КРАЯ ЭКРАНА И ВОЗДУХ — общая правка генератора, не по слайдам

Это единственная фаза, которую нельзя раздать по слайдам: она общая. И она же самая ценная —
после неё половина претензий к отдельным слайдам закрывается сама.

**Ф1а. Раскраска продолжается до края экрана.** Слайд 16:9 стоит в окне, сверху и снизу
остаются поля, и сейчас они покрашены однотонно. Владелец: «есть области сверху и снизу,
которые покрашены не в тот цвет, что остальной слайд — туда надо продолжить раскраску с
основного слайда».

Практически: каждая точка поля берёт цвет той точки слайда, что стоит над/под ней по вертикали.
- зелёная панель иллюстрации доходит до самого низа/верха экрана;
- **вертикальный** разделитель (`polosa_vertikalnaya`) продолжается вверх и вниз до края — тогда
  верхнее поле само становится двухцветным: слева бежевое, справа зелёное, между ними
  разделитель (владелец назвал именно этот случай про слайд 12);
- **паттерн-узор** в правом верхнем и левом нижнем углу дорисовывается до края, а не
  обрывается на рамке.

Способ (гипотеза аналитика, опровергни, если неверна): это не покраска полей, а **вынос фоновых
слоёв за рамку 16:9** — контент остаётся внутри 1440×810, фоновые слои растягиваются на всё
окно, сохраняя горизонтальные границы. Тогда проекция получается сама, без чтения пикселей.

⚠ **Ловушка, записанная в самом `base.css`:** в комментарии CSS нельзя писать звёздочку со
слешем — блок уже один раз закрылся на «звёздочка-слеш» внутри пути-глоба, правило `html,body`
съелось парсером, и летербокс стал БЕЛЫМ.

**Диагностика края экрана** (подсказка владельца): внизу экрана стоит номер слайда. Если он
лежит на бежевом, а полоса над ним зелёная — значит раскраска не дотянулась.

**Ф1б. Воздух — не в полях, а между блоками.** Владелец: «сверху и снизу расстояние примерно
шириной в три строки; если уменьшить до полутора строк, будет ещё три лишних строки. Я бы это
везде раздвигал по умолчанию — огромное лишнее место сверху и снизу никому не нужно.»

- Раздвинуть текстовую область (и область рисунков в горизонтальной полосе — её низ опустить
  ниже) за счёт верхнего и нижнего поля.
- 🔴 **Освободившееся место отдать промежуткам МЕЖДУ БЛОКАМИ, а не обратно в поля.** Дословно:
  «дыхание должно быть не за счёт полей сверху и снизу, а дыхание между блоками. Это сущностная
  вещь». Образец эффекта — слайд 7 (`vektornye-prostranstva`): там не хватает перехода между
  определением и примерами, и он должен появиться сам.

⚠ **Что сломается, если не учесть.** В генератор встроена **центровка картинки по её полосе** и,
возможно, подбор её размера. Полоса стала больше — центровка и размер обязаны это увидеть.
Владелец назвал это прямо. Не учтёшь — картинки поедут на всех слайдах разом.

**Гейт Ф1:** дек собирается, `check_view.py` не хуже, чем до; посмотреть глазами слайды 4, 7,
10, 12 — на каждом раскраска доходит до края. **КОММИТ.**

---

### Ф2. ПРАВКИ КОНКРЕТНЫХ СЛАЙДОВ — раздать субагентам параллельно

🔴 **Это фаза для субагентов: каждый слайд правится независимо.** Не более 4 параллельно.
Каждому субагенту: своя карточка, задача одной фразой, цикл «поправил → пересобрал слайд →
посмотрел → доложил». Коммитишь ты, субагенты не коммитят. Один слайд — до 15 минут.

- **Слайд 4 (`kategoriya`).** Линии, отсекающие картинку, сделать **вертикальными вместо
  горизонтальных** — то есть сменить тип вёрстки на `polosa_vertikalnaya`. Там две картинки
  рядом; при вертикальной раскладке они встанут одна под другой и поместятся.
- **Слайд 5 (`funktor`).** Сейчас плохо. После Ф1 нижнюю границу области иллюстрации сместить
  ниже — там большой зазор — и **увеличить саму иллюстрацию: минимум на 40%, реально можно на
  60% и почти вдвое**. При той же раскладке можно увеличить и шрифт.
- **Слайд 9 (`izomorfizm`).** Иллюстрация старая и неуместная. **Убрать иллюстрацию совсем и
  убрать полосу** — слайд становится чисто текстовым (`tolko_tekst`). Не заменять, не чинить,
  просто убрать.
- **Слайд 10 (`kategoriya-vect-iso`).** Горизонтальную полосу продлить вниз и **увеличить
  изображения, НЕ увеличивая шрифт**. Отдельно: заголовок огромный, над ним много пустого —
  поднять его примерно на две строки; «функтор F» оставить где стоит, формулу `FA = R^A` можно
  чуть опустить, снизу есть запас.

**Гейт Ф2:** каждый правленый слайд посмотрен глазами; дек собирается. **КОММИТ.**

---

### Ф3. ОБЛОЖКА И ФИНАЛЬНЫЙ СЛАЙД — мелочь, можно параллельно с Ф2

В Загрузках владельца лежит `Теоркат_Шаблон.pdf` — слайды первой лекции. Взять оттуда **обложку
(первый слайд) и последний слайд**, поставить сюда.

- На обложке поменять только две вещи: «Лекция 1» → **«Лекция 2»**, «29 июля» → **«12 августа»**.
  (В `brief.md` уже стоит `oblozhka_sub: Лекция 2` и `oblozhka_dateplace: 12 августа 2026` —
  сверься, возможно менять нечего.)
- Последний слайд не менять вовсе.

Владелец: «вообще это не очень важная вещь». Не идёт за 15 минут — бросай и иди дальше.

**Гейт Ф3:** обложка и финальный собираются. **КОММИТ.**

---

### Ф4. СЦЕНЫ — постепенное появление текста

Сцены не расставлены вовсе. Нужно расставить так, чтобы при заходе на слайд текст появлялся
частями.

- **Ориентир — около двух сцен на слайд**, в среднем 2,4–2,5, **не три**. Владелец подчёркивает:
  «не больше».
- Типовая логика: сначала центральный блок, потом остальное. Или: сначала утверждение, потом
  доказательство — **эти двое одновременно появляться не должны никогда**.
- Слайды, где ничего не происходит (просто даётся определение), — **одна сцена**, дробить не надо.
- Образец, откуда можно взять меру: слайды лекции 1.

🔴 **Правило воздержания, названное владельцем дословно: «непонятно, откуда это берётся, — лучше
и не надо».** Слайд, на котором логика разбиения не очевидна, оставь одной сценой. Выдуманное
разбиение хуже отсутствующего.

**Гейт Ф4:** назвать **среднее число сцен на слайд** — посчитать командой, не на глаз. Вышло
больше 3 — ты перестарался, срезай. **КОММИТ.**

---

### Ф5. БЛОКИ — ТОЛЬКО ЕСЛИ ОСТАЛОСЬ ВРЕМЯ

🔴 **Условие входа в фазу, невыполнение = не начинать:** Ф1–Ф4 закоммичены, и на диске лежит
версия, которую владелец может открыть и показать на лекции.

Микроинструменты структуры блоков по `SPEC-blochnaya-model.md` (читать только сейчас):
выделение центрального блока тонкой вертикальной линейкой и отступом; доказательство — кеглем
на шаг вниз; нарратив и вспомогательный блок не выделяются ничем. Всё — **микро**, почти
незаметно.

🔴 **ДВЕ ВЕРСИИ НА ДИСКЕ — требование владельца, не пожелание.** Дословно: «я хочу на выходе две
версии: без блоков и блоки, потому что если она мне очень не понравится, я её откачу». Значит:
версию без блоков сохранить отдельным файлом ДО начала Ф5 и назвать её путь в отчёте. Оба пути
— в `## ОТЧЁТ`, отдельными строками.

---

## ЧЕГО НЕ ДЕЛАТЬ

- Не трогать содержание лекции: текст, формулировки, математику.
- Не переписывать солвер `vmeshchenie.py` и не перекалибровывать целевую функцию.
- Не заводить новых инструментов, не рефакторить, не «чинить заодно».
- Не читать файлы сверх названных в §0.
- Не делать Ф5 раньше Ф1–Ф4, даже если кажется, что так быстрее.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Каждая клауза — команда или адрес, не самооценка.

1. **ЖИВОЙ ПРОГОН на реальном объекте репозитория, не на фикстуре:** `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html; echo $?` → `0`. Код возврата печатать ПЕРВЫМ. Зелёные фикстуры этой клаузы не закрывают.
2. **Текст не съеден отрисовкой:** `python3 _generator/tools/check_view.py teorkat-vvedenie/L2` — число нарушений не больше, чем было ДО работы (снять ДО первой правки и выписать в `## ПЛАН`).
3. **Коммит после КАЖДОЙ законченной фазы**, хэш каждого — отдельной строкой отчёта. Фаза без коммита считается несделанной, как бы хорошо она ни выглядела.
4. **Охват внутри вердикта:** отчёт называет «сделано фаз X из 5» и по каждой правке слайда — сделана или брошена и почему. Не «всё поправил», а «слайдов правлено X из 4 запланированных».
5. **Смотрел глазами.** По каждой правке — строка «что изменилось» после того, как ты открыл результат, а не после того, как применил патч. Машинные гейты вид не проверяют; это объявленная слепая зона.
6. **Ф5 отдельно:** если делал — два пути в отчёте (версия без блоков и версия с блоками). Если не делал — строка «Ф5 не начата: не осталось времени».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР — при этом дедлайне ОТДЕЛЬНОГО не заводить

Операция не лоссовая, время дороже. Проверка — твоими же глазами по клаузе 5 критерия: открыл
результат, посмотрел, назвал. Единственное исключение: перед Ф5 сохрани версию без блоков — она
и есть точка отката, и она заменяет верификатора.

## 3-бис. СТАРЫЙ РАЗДЕЛ-ЗАГЛУШКА (оставлен генератором, не применяется)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/L2/ _generator/skeleton/ _generator/sborka/ _studio/zhurnal/2026-08-11_dovodka-L2/kod_dovodka-pered-lekciej.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/L2/ _generator/skeleton/ _generator/sborka/ _studio/zhurnal/2026-08-11_dovodka-L2/kod_dovodka-pered-lekciej.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `teorkat-vvedenie/L2/` `_generator/skeleton/` `_generator/sborka/` `_studio/zhurnal/2026-08-11_dovodka-L2/kod_dovodka-pered-lekciej.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/L2/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/skeleton/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-11_dovodka-L2/kod_dovodka-pered-lekciej.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**ПОПРАВКА ВЛАДЕЛЬЦА К ЗАДАНИЮ (устная, 18:16, отменяет «2,5 часа от старта» из §КОНТЕКСТ).**
Лекция в 19:55, но всё должно лежать закоммиченным «в пределах часа-полутора», потому что владелец
лёг спать и вопросов в чат никто не увидит. И главное — **критерий один: его глаза.** Дословно:
не гоняться за жёсткими автоматическими гейтами, не трогать солвер `vmeshchenie.py` и смету;
«если правка выглядит хорошо глазами, этого достаточно, дальше не дотягивай до зелёного гейта»
(прошлый заход сгорел 3,5 часа именно на последних 5 % через калибровку гейта). Порядок бюджета:
Ф1 первой (общая и самая ценная) → Ф2 субагентами параллельно → коммит → Ф4 → Ф5 только с запасом.

**БАЗОВЫЕ ЗАМЕРЫ, снятые ДО первой правки (клауза 2 критерия).**
- `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html` → **rc=0**, «слайдов в деке 26, в резерве 0».
- `python3 _generator/tools/check_view.py teorkat-vvedenie/L2` → **rc=1, нарушений 1**: «НЕТ ВЫХОДА: сначала собери: python3 _generator/build_doc.py …». Гейт ищет сборку **документа**, которой у дека не существует вовсе. База = 1; клауза 2 требует «не больше, чем было», и она держится. **Гейт этой правкой не чинится намеренно** — это ровно тот «жёсткий автоматический гейт», за которым владелец запретил гоняться.
- Карта номеров ПРОВЕРЕНА на собранном деке командой (номер печатает сам движок): 4 = `kategoriya`, 7 = `vektornye-prostranstva`, 10 = `kategoriya-vect-iso`, 11 = `bazis-i-matricy`, 12 = `zamena-bazisa`. Сдвиг +2 верен, работаю по карте захода.

**ГИПОТЕЗА АНАЛИТИКА ПО Ф1а — ЧАСТИЧНО ОПРОВЕРГНУТА (замером, до кода).** «Вынести фоновые слои за
рамку 16:9» внутри слайда невозможно: у `.slide` стоит `overflow:hidden`, он режет любой вылет, а
снять его нельзя — в поля полезет переполнение контента, которое этот же `overflow` сегодня прячет.
Поэтому слой вылета живёт **снаружи** слайда (`#bleed` на `body`), а раскраску полей снимает с
ЖИВОГО DOM: элемент с непрозрачным фоном, дотянувшийся до кромки кадра, продолжает свой цвет
наружу по этой кромке. Так проекция получается сама, без чтения пикселей и без второго списка
типов вёрстки, который отстал бы от `tipy.py` на следующем новом типе.

**ЧТО ДЕЛАЮ (порядок = порядок фаз, коммит после каждой).**
1. Ф1а — `#bleed` в `base.css` + маляр в `engine.js` (полосы и линейка-`::before` наружу), узор
   растягивается на окно (`uzor.css` через переменные). Глаза: слайды 4, 7, 10, 12 в окне 1440×900.
2. Ф1б — воздух в промежутки между блоками: `.zone.copy` → `flex column; space-between` (запас
   солвера расходится по стыкам сам, без числа), у `tolko_tekst` вертикальный отступ 64 → 24,
   у горизонтальной полосы отступ картинки 28 → 14 («низ опустить ниже»).
3. Ф2 — четыре субагента параллельно (слайды 4, 5, 9, 10), коммит мой.
4. Ф3 — сверка обложки с `brief.md` (по замеру уже «Лекция 2 / 12 августа 2026» — вероятно, менять нечего).
5. Ф4 — сцены, среднее считаю командой.
6. Ф5 — только если Ф1–Ф4 закоммичены и остаётся запас; версия без блоков сохраняется ДО начала.

**ЧЕМ СМОТРЮ ГЛАЗАМИ.** Скриншоты Chrome через playwright, скрипт одноразовый в `/tmp/shot.py` —
нового инструмента в репозитории не появляется (иначе Г4 требует точку вызова, а её нет).
`ms-playwright` пуст, поэтому запуск через системный Chrome (`channel="chrome"`).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
