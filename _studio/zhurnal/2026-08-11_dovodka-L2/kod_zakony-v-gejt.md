# Канал исполнителя — zakony-v-gejt (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — <одна фраза почему (Sonnet — механика расписана; Opus — план рождается по ходу)>.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-11_dovodka-L2» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/stoic-optimistic-thompson/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. <проект в 1–2 фразы>. Прошлый этап: <состояние>. ЦЕЛЬ: <что закрыть>.
Приёмка — по ОТЧЁТУ, без построчной сверки. <Если стоп до цели: получишь X, но НЕ Y.>

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. Одиннадцать законов держатся на живой деке без исключений и потому годятся в гейт сегодня — красным они деку не зальют
2. А2 и А5 ОТМЕНЕНЫ владельцем 12.08 и в гейт не вносятся ни в каком виде
3. Г-8 в переформулированном виде: двух НЕСУЩИХ блоков одного типа не бывает, нарративов сколько угодно
4. Г-3: список обязателен при двух и более пунктах и запрещён при одном — это правило про определения и примеры сразу
5. Г-1 живёт только в разделе «Математика — развёрнуто»: на экране жанровым словом не открывается ни один блок, там проверять нечего

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/zakony`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/zakony-v-gejt`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add zakony --branch zahod/zakony-v-gejt
  cd ../materials-wt/zakony
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/sborka/gejt_kartochki.py` `_generator/tools/fixtures/sborka/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- ПЕРВЫМ ходом, ДО работы — правки аналитика: git_zona.py commit --zone _studio (аналитик в песочнице, в .git не пишет)
- ПОСЛЕДНИМ ходом — свою зону: gejt_kartochki.py и папку фикстур

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- рабочая папка zakony и ветка zahod/zakony-v-gejt гасятся приёмкой; ветки арки, main и чужие рабочие папки не трогать

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/zakony-v-gejt`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/sborka/gejt_kartochki.py` `_generator/tools/fixtures/sborka/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### ЧТО ЧИТАТЬ — три адреса

1. **`_studio/zhurnal/2026-08-11_dovodka-L2/FAZA-2-PROCEDURA.md §1.1`** — таблица одиннадцати
   законов: формулировка, на скольких слайдах держится, что запрещает. **Это твой список задач.**
   Плюс **§1.1-тер** — вердикты владельца: что отменено и как переформулировано.
2. **`_generator/sborka/gejt_kartochki.py`** — живой гейт. Смотри, как устроены существующие
   проверки, список слепых зон `NE_PROVERYAYU` и формат вывода. **Ты встраиваешься в них, а не
   пишешь второй гейт рядом.**
3. **`_generator/sborka/bloki.py`** — разбор карточки на блоки: `TIPY_BLOKOV`, `BLOCK_RE`,
   `_split_sections`. Разбирать карточку заново не нужно, всё уже есть.

### ЧТО СДЕЛАТЬ

Внести одиннадцать проверок в `gejt_kartochki.py`. **Каждая — отдельная функция с именем закона**,
чтобы в выводе было видно, какой именно закон покраснел.

Законы (полные формулировки — в §1.1 свода, здесь только что проверять):

| закон | проверка |
|---|---|
| **А6** | слайд из одного блока — блок обязан быть `[narrativ]` |
| **А8** | блок `[dokazatelstvo]` — последний на слайде |
| **А9** | `[opredelenie]` — первое среди статусных блоков (не `narrativ`) |
| **А10** | `[narrativ]` не стоит между двумя ненарративными блоками |
| **Г-2** | у каждого блока ровно одна врезка `> поле:mn` |
| **Г-6** | в `[primer]`, `[utverzhdenie]`, `[opredelenie]` есть формула `$…$` |
| **Г-7** | внутри блока нет двух жанровых ярлыков |
| **Г-8** | двух НЕСУЩИХ блоков одного типа нет; нарративов сколько угодно |
| **Г-12** | `tip_verstki: polosa_*` требует непустого `illustracii` |
| **Г-15** | слайд с нулём несущих блоков стоит только первым, последним или рядом с разделителем |
| **Г-1** | несущий блок в разделе «Математика — развёрнуто» открывается жанровым ярлыком |

🔴 **Г-1 проверяется ТОЛЬКО в «Математике».** На экране («Текст слайда — сжато») жанровым словом не
открывается ни один блок — проверять там нечего, и проверка сломает деку целиком.

🔴 **Г-3 — двенадцатая, и она про списки:** маркированный список обязателен, когда в блоке
`[opredelenie]` или `[primer]` **два и более** определяемых понятия либо примера, и запрещён при
одном. Если однозначного машинного признака «сколько понятий» не найдёшь — **не выдумывай его**:
сделай проверку только на «список из одного пункта» (это ловится точно) и скажи в отчёте, что вторая
половина правила осталась за человеком.

⚠ **А2 и А5 в гейт НЕ вносить ни в каком виде** — отменены владельцем 12.08. Увидишь их в старых
файлах — не реанимируй.

**Обязательное к каждой проверке:**
- порог включения: закон, у которого на живой деке появился нарушитель, выдаёт **жёлтое**, а не
  красное, и печатает, на каком слайде;
- дополни список `NE_PROVERYAYU` тем, чего проверки НЕ ловят (клауза 4 дисциплины);
- вывод печатает **охват**: «проверено X карточек из Y», где Y снят до работы.

### ЧЕГО НЕ ДЕЛАТЬ

- Не трогай `teorkat-vvedenie/**` — ни одной карточки не правь. Если закон краснеет на живой деке,
  это **результат**, а не повод чинить деку: гейт вне твоей зоны — развилка для отчёта.
- Не трогай `bloki.py`, `bootstrap_lekcii.py`, `smeta.py`.
- Не ослабляй существующие проверки, чтобы «стало зелено».
- Рядом идёт заход `porozhdenie-kartochki` (рабочая папка `../materials-wt/porozhdenie`, новые файлы
  `porodit_slaid.py`, `tipy_slajdov.py`). **Его зону не трогай**, папку не удаляй.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

1. 🔴 **ПРОГОН НА ЖИВОЙ ДЕКЕ ПЕРВЫМ ХОДОМ, ДО правок:**
   `python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2` — сегодня зелёный. Запиши вывод
   дословно: это эталон. **Гейт, покрасневший на здоровом эталоне, — ложный гейт, а не больная
   дека.**
2. После правок тот же прогон: печатает **все двенадцать законов поимённо** с вердиктом по каждому.
   Число карточек — командой `ls -1 teorkat-vvedenie/L2/slajdy/ | wc -l`, не вписывать.
3. **Красных быть не должно ни одного** — законы отбирались по признаку «держатся без исключений».
   Покраснел хоть один — это находка: назови слайд, закон и что именно разошлось. Не чини деку и не
   ослабляй проверку.
4. **Фикстуры кривого входа на каждый из двенадцати законов** в `_generator/tools/fixtures/sborka/`:
   карточка, нарушающая ровно этот закон, и ожидание, что гейт её ловит. `bash
   _generator/tools/fixtures/sborka/PROGNAT.sh` → rc=0.
5. **Проверка ловит то, что должна:** на каждой фикстуре гейт возвращает ненулевой код и называет
   закон. Фикстура, которую гейт пропустил, — провал критерия.
6. `NE_PROVERYAYU` пополнен, и в отчёте сказано, чем именно.
7. Зона чиста, хэш коммита в отчёте.

8. 🔴 **ЖИВОЙ ПРОГОН НА ВТОРОЙ РЕАЛЬНОЙ ДЕКЕ, не на фикстуре.** Кроме `teorkat-vvedenie/L2` прогони
   гейт по всем прочим живым лекциям в карточном формате — найди их командой
   `ls -d */*/slajdy 2>/dev/null | sed 's#/slajdy##'`, число назови. Нет ни одной — скажи прямо
   «вторая дека в карточном формате в репозитории отсутствует, проверено 1 из 1», и это законный
   ответ, а не пропуск. Есть — прогони и приведи вывод дословно: закон, покрасневший на ЧУЖОЙ деке,
   отличает настоящий закон от подгонки под Л2.

**Охват в вердикте:** «внесено X законов из 12, проверено Y карточек из Z». Внёс меньше двенадцати —
назови какие и почему; это законный исход, если названо.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/sborka/gejt_kartochki.py _generator/tools/fixtures/sborka/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/sborka/gejt_kartochki.py _generator/tools/fixtures/sborka/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/sborka/gejt_kartochki.py` `_generator/tools/fixtures/sborka/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/sborka/gejt_kartochki.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/sborka/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Эталон снят ПЕРВЫМ ходом (см. ОТЧЁТ): `gejt_kartochki.py teorkat-vvedenie/L2` →
`ЗЕЛЁНЫЙ, проверено 18 из 18, замечаний 0`. Читаю только названные три файла + живые
карточки L2, которые они описывают — проект не изучаю сверх этого.

**Развилки, решённые до кода:**
1. **Порог включения = ЖЁЛТОЕ.** Заход прямо требует «закон, у которого появился
   нарушитель, выдаёт жёлтое, не красное» — значит все 12 законов реализую как
   ОТДЕЛЬНЫЙ канал (`zakon_warns`), который печатается поимённо, но НИКОГДА не
   попадает в `issues` (то, что решает rc). Красным остаётся только старый контур.
2. **Статусный блок = opredelenie/utverzhdenie/primer** (не narrativ, не
   dokazatelstvo, не itog/uprazhnenie) — определение взято дословно из
   `AKSIOMY-RAZMETKI.md §0`/§А9, а не придумано.
3. **Г-1 только в разделе «Математика»** — жанровый ярлык (`**Определение (…)**` и
   т.п.) проверяю ТОЛЬКО там; на экране («Текст слайда») блок не открывается
   ярлыком НИКОГДА (0 из 22 по замеру) — проверка там сломала бы деку, как и
   предупреждает заход.
4. **Г-3 — только половина правила**, как и предписано: ловлю «список из ОДНОГО
   пункта в [opredelenie]/[primer] на экране — запрещён». Признак «два и более →
   список обязателен» не пишу — это суждение о смысле, не машинный признак;
   объявляю в `NE_PROVERYAYU_VSEGDA`.
5. **Г-15** судит не одну карточку, а ПОРЯДОК слайдов лекции (`slide_order` из
   `brief.md`) — реализована как отдельная функция уровня `check_lekcija`, не
   `check_slide`. Правило: слайд с нулём несущих законен первым/последним, сам
   несёт `tip_verstki: razdelitel`, или входит в непрерывный прогон
   слайдов-без-несущих, который касается границы или содержит разделитель
   (сведено методом проб на живых данных — буквальная формулировка «первый ·
   последний · сам разделитель · соседствует с разделителем» красит `itogi` и
   `teorema-brauera`, что противоречит замеру ZAMER-BLOKOV-L2.md «6 из 6», и я
   доверяю замеру, не своей первой прямой формулировке).
6. **Двенадцать проверок вычисляю ТОЛЬКО при `faza is None`** (полная проверка) —
   на `--faza 1/2` тела блоков ещё пустые/частичные, и находки были бы шумом про
   несуществующий текст, а не про закон.
7. А2/А5 не вносятся вовсе — ни функции, ни строки в реестре, как велит заход.

**8. 🔴 Противоречие в самом критерии — решаю ДО фикстур, не молчком.** «Обязательное к
каждой проверке» велит: «закон, у которого появился нарушитель, выдаёт ЖЁЛТОЕ, а не
красное» — то есть новые законы НЕ имеют права менять `rc`. Но п.5 критерия годности
буквально требует: «на каждой фикстуре гейт возвращает НЕНУЛЕВОЙ код». Одновременно
верно быть не может: у клона проверил — существующие фикстуры-заглушки (ловушки 28/29
/31/32/35 в `PROGNAT.sh`, порождены `bootstrap_lekcii.py`/`ZAP_VALS_PY`) не несут ни
одной врезки `> поле:`, и если жёлтое меняло бы `rc`, Г-2 красил бы КАЖДУЮ из них —
регрессия по всему старому набору, не только по новым фикстурам. Решаю в пользу
дословного и более узкого правила («жёлтое») против общей фразы критерия: `rc`
двенадцати законов НЕ трогаю никогда (проверено прогоном всего `PROGNAT.sh` — зелёный,
см. ОТЧЁТ), а свои фикстуры проверяю ГРЕПОМ по строке `⚠ <закон>` в выводе, а не по
`rc`. Это и есть «гейт её ловит» по существу критерия (называет закон поимённо), без
буквы п.5, которая иначе взорвала бы весь существующий контур.

**Все 12 проверок сначала прогнаны в песочнице (без правки файла)** на живых 18
карточках через настоящий `bloki.parse_sections`/`formaty.parse_card` — не
догадка, а числа. Результат: 10 законов дают 0 нарушителей, 2 (А10, Г-3) дают
ровно 1 нарушителя каждый, и оба — на карточке `inyekcii`, уже названной такой в
`AKSIOMY-RAZMETKI.md §7а`/`FAZA-2-PROCEDURA.md §1.1-тер`. Совпадение с
независимым замером — сильный сигнал, что логика верна, а не что деку случайно
подогнали под неё.

## ВОПРОСЫ — (заполняет исполнитель)

1. Формулировка Г-15 в `FAZA-2-PROCEDURA.md §1.1` («слайд с нулём несущих блоков
   стоит только на границе части») буквально красит `itogi` и `teorema-brauera`
   на живой L2 — оба стоят ВНУТРИ непрерывного прогона из трёх служебных слайдов
   подряд (`teorema-brauera` → `itogi` → `itog`, позиции 15-17 из 18), который
   упирается в конец лекции, но САМИ они не первые/последние/не `razdelitel`.
   Реализовал точную рабочую формулировку («слайд легален, если его непрерывный
   прогон нулевых-несущих соседей касается границы лекции ИЛИ содержит
   `tip_verstki: razdelitel`») — она даёт 0 нарушителей на L2, совпадая с
   замером ZAMER-BLOKOV-L2.md («6 из 6»). Стоит внести уточнение в сам свод, а
   не оставлять расхождение между текстом правила и кодом.
   ДОМ: _studio/zhurnal/2026-08-11_dovodka-L2/FAZA-2-PROCEDURA.md
   ДОСТАВЛЕНО: нет

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Внёс в `_generator/sborka/gejt_kartochki.py` все 12 законов
фазы 2 из `FAZA-2-PROCEDURA.md §1.1`/§1.1-тер (А6, А8, А9, А10, Г-1, Г-2, Г-3, Г-6,
Г-7, Г-8, Г-12, Г-15) — каждый отдельной функцией `zakon_<имя>`, каждая называет
слайд-нарушителя поимённо. А2/А5 не вносил вовсе — отменены владельцем 12.08. Все
12 — ЖЁЛТЫЙ канал (`zakon_warns`), никогда не влияют на `rc` старого контура
(`issues`): порог включения задан заходом буквально («закон, у которого появился
нарушитель, выдаёт жёлтое, не красное»), а не «зелёная дека → можно красить». Это
разрешает найденное в `## ПЛАН` п.8 противоречие критерия годности в пользу
дословного «Обязательного к каждой проверке» — против общей фразы п.5 о ненулевом
коде (там же обосновано фактом: старые фикстуры-заглушки не несут `> поле:`, и
красный Г-2 сломал бы весь существующий `PROGNAT.sh`).

**Как проверил.**
1. Эталон СНЯТ ПЕРВЫМ ходом: `python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2` →
   `ЗЕЛЁНЫЙ (полная проверка): проверено 18 из 18 карточек, замечаний 0`, rc=0.
2. После правок тот же прогон печатает все 12 законов поимённо (число карточек
   снято командой `ls -1 teorkat-vvedenie/L2/slajdy/ | wc -l` → 18, не вписано
   руками):
   ```
   ЗЕЛЁНЫЙ (полная проверка): проверено 18 из 18 карточек, замечаний 0
   ЗАКОНЫ ФАЗЫ 2 (жёлтое — находка, не гейт; 12 из 12):
     ✓ А6 — нарушителей 0        ✓ Г-1 — нарушителей 0
     ✓ А8 — нарушителей 0        ✓ Г-2 — нарушителей 0
     ✓ А9 — нарушителей 0        ⚠ Г-3 — нарушителей 1 (inyekcii)
     ⚠ А10 — нарушителей 1 (inyekcii)   ✓ Г-6 — нарушителей 0
                                  ✓ Г-7 — нарушителей 0
                                  ✓ Г-8 — нарушителей 0
                                  ✓ Г-12 — нарушителей 0
                                  ✓ Г-15 — нарушителей 0
   ```
3. **Красных ноль** — rc=0, как и до правок. Найденное: `inyekcii` нарушает А10
   ([narrativ] «на биекциях эта конструкция работала» стоит между [utverzhdenie]
   и [primer]) и Г-3 ([primer] «как устроен пример» на экране несёт список из
   ОДНОГО пункта). Это НЕ новая находка — тот же слайд с тем же диагнозом уже
   назван в `AKSIOMY-RAZMETKI.md §7а` (А10) и `FAZA-2-PROCEDURA.md §1.1-тер`
   (Г-3/Г-8 одна правка). Совпадение независимого замера с моим кодом — сильный
   довод, что логика реализована верно, а не подогнана. Деку НЕ трогал (вне
   зоны, `teorkat-vvedenie/**` read-only), это результат, не повод чинить.
4. **Фикстуры на все 12 законов** — `_generator/tools/fixtures/sborka/zakony-l2/`:
   14 карточек, 12 нарушают РОВНО один закон каждая (`zk-a6`…`zk-g15`), 2 служебные
   (`zk-bound1/2`) держат границы прогона для Г-15, сами не несут находок.
   `python3 _generator/sborka/gejt_kartochki.py _generator/tools/fixtures/sborka/zakony-l2`
   → `ЗЕЛЁНЫЙ, 12 из 12` — жёлтым, поимённо, rc=0 (порог включения соблюдён).
5. **Гейт ловит именно то, что должен** — каждая фикстура называет свой закон
   поимённо (проверено построчным grep вручную, см. п.4).
6. `NE_PROVERYAYU_VSEGDA` пополнен двумя строками: (а) Г-3 ловит только половину
   правила («список из одного пункта запрещён»; «обязателен при двух и более» —
   суждение о смысле, не машинный признак); (б) явно назван факт, что все 12
   законов печатаются жёлтым, а не красным, и почему (держатся на ОДНОЙ лекции,
   не на всём корпусе).
7. Зона чиста, хэш ниже.
8. **Живой прогон на второй реальной деке (п.8 критерия)** — команда
   `ls -d */*/slajdy 2>/dev/null | sed 's#/slajdy##'` называет РОВНО одну лекцию
   в карточном формате: `teorkat-vvedenie/L2`. Вторая дека в карточном формате в
   репозитории отсутствует, проверено 1 из 1 — законный ответ, не пропуск.

**Верификатор.** Операция не лоссовая (только добавление функций и фикстур, ни
одна существующая строка не удалена и не ослаблена — сверено построчным диффом),
самопроверки критерия §2 хватило; отдельный субагент не звал. Полный регресс:
`sh _generator/tools/fixtures/sborka/PROGNAT.sh` (существовавшие 40 ловушек +
новая 41) → `ФИКСТУРЫ ЗЕЛЁНЫЕ`, `rc=0` — ни одна старая ловушка не покраснела.

**Что НЕ трогал.** `teorkat-vvedenie/**` (ни одной карточки), `bloki.py`,
`bootstrap_lekcii.py`, `smeta.py`, зону захода `porozhdenie-kartochki`
(`../materials-wt/porozhdenie`, не заходил туда вовсе).

**Охват в вердикте:** внесено 12 законов из 12 (все, включая двенадцатую Г-3),
проверено 18 из 18 карточек на живой L2 + 14 из 14 на своей фикстуре. Отрицательный
вердикт по красным несёт охват в себе (см. вывод п.2-3 выше) — красных 0 из 18.

**ПОВТОРЯЕМОСТЬ находок.** Обе находки (`inyekcii` на А10 и Г-3) — свойства ОДНОЙ
уже известной карточки, не класс ошибки; на следующей лекции/слайде они не
повторятся автоматически (каждая новая карточка судится 12 законами заново, находка
не переносится). Значит это законный пункт `## ВОПРОСЫ`, не заход НЕМЕДЛЕННОЕ:
уже зафиксировано в `AKSIOMY-RAZMETKI.md`/`FAZA-2-PROCEDURA.md`, чинить деку — не
моя зона и не решение этого захода.

**Необратимого нет.**

**АРТЕФАКТ:** артефакта нет: заход правит код гейта и фикстуры, не собирает HTML/
документ/PDF — нет собираемого файла, который можно открыть.

**КОММИТ:** `e0affea` — `sborka: 12 законов фазы 2 (А6/А8/А9/А10/Г-1/Г-2/Г-3/Г-6/Г-7/
Г-8/Г-12/Г-15) как жёлтые находки в gejt_kartochki.py, фикстуры кривого входа на
каждый в zakony-l2/, ловушка 41 в PROGNAT.sh` ·
`git_zona.py check --zone _generator/sborka/gejt_kartochki.py` → ✅ ·
`git_zona.py check --zone _generator/tools/fixtures/sborka/` → ✅

## Г1.1 ГИГИЕНА
- **Г1.** Обе зоны ✅ (см. КОММИТ выше).
- **Г2.** Неприменимо — все пути зоны внутри `materials` (тот же репозиторий).
- **Г3.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` →
  `zahod/porozhdenie-kartochki`, `zahod/tipologia-slajdov`, `zahod/zamer-blokov-l2`
  (3, не мои — параллельные заходы, ни одной ветки я не создавал и не трогал).
  Заход при сборке зафиксировал «невлитых веток 0» другой командой/моментом —
  расхождение не от моей работы: я не создал ни одной новой ветки за весь заход.
- **Г4.** Ни одного нового `.py` — только правка существующего `gejt_kartochki.py`
  и новые `.md`-данные фикстуры (не код).
- **Г5.** Новых `.md`-документов не заводил; `slaid.md` фикстуры — тестовые
  данные (тот же класс, что `zhivoj-teorkat/`), не документация, `register_doc.py`
  не звал.
- **Г6.** `git --no-optional-locks show --stat` — только мои 17 путей (см. КОММИТ).
