# Канал исполнителя — sluzhebnye-slajdy (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — правишь генератор, через который собираются ВСЕ деки: сломаешь — сломаются четыре разом, и render-identity это покажет только если ты её прогонишь.

КОНТЕКСТ. Монорепо `materials/`, фабрика `_studio` печёт научпоп-презентации; `_generator/build_deck.py` собирает `<лекция>/src/` в один самодостаточный `dist/index.html`.

**ПРОБЛЕМА, ради которой заход.** Три служебных слайда — **обложка**, **визитка «Про меня»** и **финал «Спасибо за внимание»** — верстаются руками в каждом деке. Владелец правит их, следующая большая правка возвращает мусор обратно, и так по кругу. Живой замер: у Паскаля обложка несёт лишний подзаголовок; визитки нет ВООБЩЕ, хотя она обязана быть в каждой деке; финал переписан сборщиком «от себя». Визитка существует ровно в одном деке (`buffon/src/slides/sl-vizitka.html` + `buffon/src/content/sl-vizitka.md`) и больше нигде.

**ЦЕЛЬ: перевести все три слайда на ПОРОЖДЕНИЕ генератором.** Не «написать правило и следить», а сделать так, чтобы отсебятину физически негде было написать: руками этих файлов не существует, генератор строит их из объявленных полей `brief.md` и канонных заготовок в `_generator/skeleton/`. Это прямое применение хука **H6** реестра гейтов («величина, обязанная совпадать с другой, порождается, а не выписывается руками») к слою вёрстки.

**Решения владельца — приняты, НЕ переоткрывать:**
1. **Обложка** несёт название и, если объявлена, одну иллюстрацию. Дата, место, подзаголовок — **только объявленным полем `brief.md`**. Нет поля — нет элемента на слайде. Придумать «а тут хорошо бы подпись» негде.
2. **Визитка** — **всегда, вторым слайдом**, во всех деках. Отключается только явным `vizitka: net` в `brief.md`. Текст, фото и QR — **один канон в `_generator/skeleton/`**, не копия в деке: правка биографии должна быть в одном месте.
3. **Финал** — «Спасибо за внимание» и, если объявлена, одна иллюстрация. Тела текста нет.

Приёмка — по ОТЧЁТУ, без построчной сверки. **Стоп до цели:** если после (1)+(2) станет ясно, что миграция живого дека Паскаля тянет за собой переверстку соседних слайдов — остановись, сдай генератор с фикстурами и напиши это отдельной строкой. Полугенератор с недоехавшим деком лучше, чем сломанный дек.

**Времени не жалко, токенов — да.** Работай хоть всю ночь, но читай только якоря §0, документы целиком не перечитывай, проверки — прогоном и грепом, правки точечные, один верификатор.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `zahod/dek-paskal-v2` в основной папке репозитория — `git checkout zahod/dek-paskal-v2` первым ходом. Ветку НЕ переключай дальше, в другие НЕ коммить.
- **ЗОНА (можно менять):** _generator/build_deck.py _generator/skeleton _generator/tools/sostoyanie.py _generator/tools/fixtures/sostoyanie _studio/konvejer/GEJTY.md _studio/konvejer/FORMAT-ISTOCHNIKA.md _studio/konvejer/07-verstka lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout zahod/dek-paskal-v2`.
- Точка отката: `git add _generator/build_deck.py _generator/skeleton _generator/tools/sostoyanie.py _generator/tools/fixtures/sostoyanie _studio/konvejer/GEJTY.md _studio/konvejer/FORMAT-ISTOCHNIKA.md _studio/konvejer/07-verstka lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти якоря, ничего сверх. Проект не изучай.
  1. `_generator/build_deck.py` — предмет правки; читай целиком.
  2. `_generator/DESIGN.md` и `_generator/SLIDE-FORMAT.md` — контракт сборки и формат слайда.
  3. `_studio/konvejer/FORMAT-ISTOCHNIKA.md` — схема `brief.md`, куда добавляются новые поля.
  4. **Живой образец визитки:** `buffon/src/slides/sl-vizitka.html` + `buffon/src/content/sl-vizitka.md` + её активы в `buffon/src/illustrations/`. Это единственная существующая визитка — канон снимается ДОСЛОВНО с неё, ничего не переписывая «поприятнее».
  5. **Живые обложки и финалы четырёх деков** — `buffon`, `dandelin`, `fibonacci`, `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal`: первый и последний файл в `src/slides/` каждого. Нужны, чтобы понять разброс форм ДО того, как выберешь одну.
  6. `_studio/konvejer/GEJTY.md` — записи **G12** (обложка/финал) и **H6**; их ты уточняешь под новую механику.
  7. `_studio/docs/kak-delat/GIT-disciplina.md` §3 — только если коммит пойдёт не так.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

### 2.1. Канон трёх служебных слайдов в `_generator/skeleton/`
Завести заготовки, из которых генератор строит обложку, визитку и финал. Визитку снять с `buffon` **дословно** — текст (матфак ВШЭ · 179 школа · преподаю · канал «Кроссворд Тьюринга» · личный телеграм), раскладку `board` + фото + QR. Активы (фото, QR) положить в канон и вшивать оттуда: копии в деках не заводить.
⚠ **Формы разъезжаются.** У `buffon` визитка — `zone board` + `zone copy`, у Паскаля обложка — `div.wrap/.head/.art`, у остальных иначе. Выбери ОДНУ форму каждого слайда и **назови в `## ПЛАН`, чью именно взял и что при этом теряется** — молчаливый выбор здесь даст «канон», в который никто не поверит.

### 2.2. Порождение в `build_deck.py`
Три слайда собираются генератором из полей `brief.md`; рукописных `slides/<id>.html` для них не существует.
- **Обложка:** `title` — обязательно. `cover_ill` — опционально, одна иллюстрация. `cover_sub`, `cover_date`, `cover_place` — опционально, каждое только если объявлено. **Нет поля — нет элемента**, никаких значений по умолчанию и никакого «выведем из `id`».
- **Визитка:** порождается ВСЕГДА вторым слайдом. `vizitka: net` в `brief.md` — единственный способ убрать. Забыть невозможно.
- **Финал:** «Спасибо за внимание» + опционально `final_ill`. Тела текста нет.
- `slide_order` эти три id получает **от генератора**, а не из руки. Имена id — твой выбор, назови в `## ПЛАН`.

🔴 **ГЛАВНЫЙ РИСК — сломать четыре живых дека.** Правка генератора гейтится нормализованной render-identity сверкой (`_generator/render.py --compare`, Р1, канон `DESIGN.md`). Старые деки (`buffon`, `dandelin`, `fibonacci`) **в этом заходе не мигрируются** и обязаны собираться байт-в-байт как прежде: рукописный служебный слайд, если он есть в `slides/`, имеет приоритет над порождением. Мигрируется ТОЛЬКО Паскаль (§2.3).

### 2.3. Мигрировать живой дек Паскаля
Снести рукописные обложку и финал, добавить недостающую визитку, перевести на порождение. Ожидаемый результат по замеру: обложка теряет подзаголовок «Открытая лекция курса…» (вернётся, только если владелец объявит `cover_sub`), появляется второй слайд-визитка, финал становится каноническим. `slide_order` и `data-scenes` не править руками — они порождаются.

### 2.4. Уточнить гейты и документацию
- **G12** в `GEJTY.md` переписать со счёта слов на структуру: обложка/визитка/финал **порождены, а не написаны руками**; наличие рукописного `slides/<служебный id>.html` в новом деке = FAIL. Счёт слов остаётся как проверка для старых, немигрированных деков — назови это в записи явно.
- `sostoyanie.py` и фикстуры `_generator/tools/fixtures/sostoyanie/` — привести к новому G12; **у каждой новой ветки проверки обязана быть фикстура, на которой она ПАДАЕТ**, и хотя бы одна здоровая, на которой она молчит.
- `FORMAT-ISTOCHNIKA.md` — новые поля `brief.md` в схему.
- `07-verstka/DOK.md` — записать, что три служебных слайда **вне зоны вёрстки**: их не проектируют, не верстают и не правят, ими управляют поля `brief.md`.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `python3 _generator/build_deck.py <дек>/src` → exit 0 на **всех четырёх** деках.
2. **Render-identity зелёная на трёх немигрированных** (`buffon`, `dandelin`, `fibonacci`): `render.py --compare` даёт `cmp` exit 0. Красный тут = генератор сломал чужие деки, и это провал захода независимо от остального. *(Нет браузера — скажи это прямо в отчёте отдельной строкой и назови, чем заменил; молчаливый пропуск браузерного слоя — брак.)*
3. В `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/slides/` **нет файлов** обложки, визитки и финала, а в `dist/index.html` все три **есть**, и визитка стоит второй.
4. `grep -c 'Кроссворд Тьюринга' lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/` → **0** (текст визитки в деке не лежит), при этом он же есть в собранном `dist/index.html`.
5. Убери из `brief.md` Паскаля поле `cover_ill` → пересобери → на обложке нет иллюстрации и сборка зелёная. Верни. **Это и есть проверка «нет поля — нет элемента»**, и она должна быть в отчёте прогоном, а не утверждением.
6. `sh _generator/tools/fixtures/sostoyanie/PROGNAT.sh` → зелёный, и фикстуры проверены **мутацией**: сломай проверку G12 → фикстуры краснеют → верни.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Операция лоссовая: ты сносишь рукописные слайды живого дека.** Один свежий субагент, другой метод: сверить `dist/index.html` Паскаля ДО и ПОСЛЕ по `git show` — список потерь должен содержать **ровно** подзаголовок обложки и старый финал, и ничего сверх. Плюс его же задача — найти **здоровый вход, на котором новый G12 покраснеет зря** (это самое дорогое, что он может дать). Вердикт — в отчёт с охватом.

⚠ **Предупреждение из прошлого захода этой же арки** (`kod_gejty-shaga-6`, урок 15): признаки гейтов, записанные «по памяти об инциденте», при буквальной реализации давали десятки ложных ❌ на здоровых деках. Пиши признак нового G12 **грепом по всем четырём декам тем же ходом** и вписывай в запись число совпадений.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/build_deck.py _generator/skeleton _generator/tools/sostoyanie.py _generator/tools/fixtures/sostoyanie _studio/konvejer/GEJTY.md _studio/konvejer/FORMAT-ISTOCHNIKA.md _studio/konvejer/07-verstka lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/build_deck.py _generator/skeleton _generator/tools/sostoyanie.py _generator/tools/fixtures/sostoyanie _studio/konvejer/GEJTY.md _studio/konvejer/FORMAT-ISTOCHNIKA.md _studio/konvejer/07-verstka lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 0. Что уже замерено ДО правок (это факты прогона, не намерения)
- Ветка `zahod/dek-paskal-v2` уже была активна; **зона чиста в HEAD** → точку отката не фабрикую (грязное в дереве — `_studio/docs/KARTA.md`, `INVENTAR-L1.md`, два `KARTA-OBLASTI.md` и два неотслеженных файла — всё ВНЕ моей зоны, не трогаю).
- **База render-identity снята сильнее, чем требует критерий.** Все три немигрируемых дека уже сегодня пересобираются **байт-в-байт** со своим закоммиченным `dist/index.html`:
  `build_deck.py <дек>/src -o <scratch>` → `cmp` exit 0 для `buffon`, `dandelin`, `fibonacci` (сборка в scratch, не в их `dist/` — их `dist/` вне зоны). Это и будет мой главный гейт после правки: байт-равенство сильнее пиксельного, браузер ему не нужен.
- `sostoyanie.py` по Паскалю: **G12 сейчас PASS** — то есть действующая проверка счётом слов ту самую перегруженную обложку, ради которой заход, НЕ ловит (подзаголовок короткий). Фикстуры `PROGNAT.sh` — зелёные.
- Греп признака нового G12 по всем четырём декам, **тем же ходом** (§3):
  `grep -l '{{SLIDES}}' */src/shablon.html */*/src/shablon.html _generator/skeleton/shablon.html` → **0 совпадений сегодня**;
  рукописные `slides/{sl-title,sl-vizitka,sl-thanks}.html`: buffon **3**, dandelin **0**, fibonacci **0**, paskal **1**.
  ⇒ Признак, привязанный к `{{SLIDES}}`, даёт **0 ложных ❌** на всех четырёх; три буффоновских рукописных слайда остаются законными, потому что buffon не мигрирован.

### 1. Форма каждого слайда — чью взял и что теряется
Формы действительно разъезжаются; выбираю по одной и называю цену.
- **Обложка — форма Паскаля/fibonacci** (`<section class="slide" data-kind="cover">` → `.wrap > .head > .t-display` (+`.sub`) + `.art > .ill-box`).
  *Почему:* она новее, ею уже написаны два дека из четырёх, и канон-`base.css` скелета несёт под неё анимацию именно с этими именами (`#sl-title .head`, `#sl-title .art`).
  *Теряется:* форма buffon (`.grid > .zone.head + .zone.art`, `#sl-title .grid{grid-template-rows:22px 64px 26px 667px 1fr}`) и её кегль 54px — канон встаёт на паскалевские 96px. Практической потери сейчас нет: buffon не мигрируется, у него рукописный слайд и приоритет за ним.
- **Визитка — форма buffon дословно** (`.grid > .zone.board(.brd-title + .panel.p-photo + .panel.p-qr) + .zone.copy.t-body`), текст — `content/sl-vizitka.md` buffon **дословно**, включая `<b>`, `<br>` и `.tg-link`.
  *Теряется:* форма dandelin (`s11b`) — тот же скелет, но текст рукописным `<ul class="bullets">` в слайде и инлайн-стилями на `<a>`; она хуже, потому что текст биографии не отделён от вёрстки.
- **Финал — форма buffon** (`.thx-wrap > .thx-head + .thx-art`), CSS дословно из `buffon/src/overlay.css`.
  *Теряется:* форма dandelin (`.zone.card > .fit.cer`) — она автофитит заголовок, то есть кегль финала зависит от длины строки; канонный финал строку не меняет, фитить нечего.
  *Одно расхождение с §2.2 называю явно:* buffon пишет «Спасибо за внимание**!**», заход дважды пишет без «!». Беру **формулировку захода** (текст решает владелец, вёрстку — эталон): `Спасибо за внимание`.

### 2. Имена id порождённых слайдов
`sl-title` (обложка) · `sl-vizitka` (визитка) · `sl-thanks` (финал) — как у buffon. Плюс: `base.css` скелета уже адресует `#sl-title .head/.art`, а канон-CSS визитки и финала снимается с buffon селекторами `#sl-vizitka …`/`#sl-thanks …` — при других именах пришлось бы переписывать снятый дословно CSS, то есть ровно то, чего заход велит не делать.

### 3. Механизм порождения — и почему он НЕ может сломать три чужих дека
Порождение включается **наличием плейсхолдера `{{SLIDES}}` в `shablon.html`** и больше ничем.
- `{{SLIDES}}` → генератор собирает ВЕСЬ поток слайдов сам: `[обложка] + [визитка] + slide_order + [финал]`, join через `\n`. Порядок берётся из `slide_order`, а не из руки — это заодно закрывает дублирование порядка (сегодня он выписан ДВАЖДЫ: в `brief.slide_order` и списком `{{SLIDE:id}}` в шаблоне — ровно случай H6).
- `{{SLUZHEBNYE_CSS}}` в `<style>` → канон-CSS трёх слайдов из `_generator/skeleton/sluzhebnye/style.css`.
- **Приоритет рукописного:** если `slides/<id>.html` физически есть — берётся он, порождение молчит.
- У `buffon`/`dandelin`/`fibonacci` в шаблонах **0 вхождений** обоих плейсхолдеров (замерено, §0) → ни одна новая ветка кода для них не исполняется; их выход обязан остаться байт-в-байт, и это гейт п.2 критерия.
- Линтер: `slide_order` сверяется с **эффективным** множеством (порождённые id считаются присутствующими) — иначе п.5 линтера объявил бы их сиротами.
- Порождённые слайды **не несут `data-scenes`** (движок читает `dataset.scenes || '1'`) — это целевое состояние H6, и фикстура `zdorovyj-neobychnyj` уже подтверждает, что трекер такой слайд принимает.

### 4. Поля `brief.md`
`cover_ill` · `cover_sub` · `cover_date` · `cover_place` · `final_ill` · `vizitka: net`.
Нет поля — нет элемента: значений по умолчанию не будет ни одного, `id`/`title` в подпись не подставляются. `title` — единственное обязательное (он уже обязателен). Дата и место, если объявлены, идут одной строкой `.sub.sub2` через ` · ` (класс уже есть в паскалевском cover-CSS, новых сущностей не завожу).

### 5. Канон в `_generator/skeleton/sluzhebnye/`
`style.css` (CSS трёх слайдов) · `oblozhka.html` · `vizitka.html` · `vizitka.md` (текст дословно с buffon) · `final.html` · `vizitka-photo.html` + `vizitka-qr.html` (активы дословно с buffon).
Активы **вшиваются прямо в порождённый слайд**, без `data-ill`/`<template>`: реестр ассетов в `shablon.html` — рукописный слой арки 9, и требовать от каждого дека две строки регистрации ради канонной визитки значило бы вернуть ручную работу с другой стороны. DOM после подстановки тот же (`.panel > img`), что у buffon после гидрации шаблона.
**Три защитных правила канон-CSS, которых нет у buffon** — называю, потому что это единственные строки, написанные не «дословно»: `#sl-vizitka .copy b{color:var(--ink)}`, `#sl-vizitka .board .panel{background:var(--card)}` и `#sl-vizitka .grid`-раскладка перебивают дековые `.slide .t-body b{color:var(--brick)}` / `.slide .board .panel{background:transparent}` Паскаля. Без них канонная визитка в чужом деке краснеет буквами и теряет белую подложку — то есть «один канон» перестаёт быть одним.

### 6. Миграция Паскаля — и что она СНОСИТ
Удаляю `slides/sl-title.html`, `slides/sl-course.html`, пустые `content/sl-title.md`, `content/sl-course.md`; в `shablon.html` 22 строки `{{SLIDE:…}}` → `{{SLIDES}}`, в `<style>` добавляю `{{SLUZHEBNYE_CSS}}` и убираю уехавший в канон блок `.slide[data-kind="cover"]`; из `slide_order` убираю `sl-title` и `sl-course`; в `brief.md` — `cover_ill: title-art`, `final_ill: sierpinski`.
🔴 **Цена, которую называю до работы:** финалом Паскаля сегодня служит не «спасибо», а содержательный слайд `sl-course` «Что осталось» — Серпинский + анонс листка и курса ЛШП. Канонный финал тела текста не несёт, значит **этот текст пропадает целиком**, а не только подзаголовок обложки. Заход это санкционирует дважды (§2.3 «снести рукописные обложку и финал», §3 «список потерь — ровно подзаголовок обложки и старый финал»), поэтому делаю, но выношу отдельным пунктом в `## ВОПРОСЫ`: анонс курса — вещь, которую владелец может захотеть вернуть отдельным слайдом ПЕРЕД финалом. `final_ill: sierpinski` беру, чтобы рисунок не остался мёртвым в реестре.

### 7. Гейты, фикстуры, доки
- **G12** переписывается на развилку: дек с `{{SLIDES}}` судится структурно (рукописный `slides/<служебный id>.html` = FAIL; счёт слов и `data-ill` к порождённым не применяется — они порождены и перегрузить их негде), дек без `{{SLIDES}}` судится прежним счётом слов. Признак пишу грепом (§0), число совпадений вписываю в запись.
- **H6** — снимаю названный в GEJTY.md предел «хук не отличает порождённое от рукописного» в той части, которая теперь закрыта: у порождённых слайдов `data-scenes` не выписывается вовсе.
- **Фикстуры:** две новые папки — `g12-porozhdenie` (дек с `{{SLIDES}}` и рукописной обложкой → **FAIL**) и `zdorovyj-porozhdenie` (дек с `{{SLIDES}}` без рукописных служебных → **PASS**), обе в `PROGNAT.sh`. Мутационная проверка: ломаю ветку G12 → обе краснеют → возвращаю.
- `FORMAT-ISTOCHNIKA.md` — шесть новых полей в схему `brief.md` + абзац про порождаемые слайды. `07-verstka/DOK.md` — три служебных слайда вне зоны вёрстки. `skeleton/SPEC.md` — состав `sluzhebnye/`.

### 8. Спор с КРИТЕРИЕМ ГОТОВНОСТИ (до работы, как велит §1)
Два пункта провалятся не по существу, а по букве — правлю форму, существо усиливаю:
1. **п.2** `render.py --compare` требует Chrome+playwright. `playwright` в системе есть, канал `chrome` — проверю; но **основным доказательством делаю байт-в-байт `cmp` пересборки с закоммиченным `dist/index.html`** всех трёх деков (базовая линия уже снята, §0). Байт-равенство влечёт пиксельное тождественно; пиксельная сверка — более слабая проверка, и подменять ею более сильную было бы регрессом. Если Chrome не поднимется — скажу отдельной строкой отчёта, как требует критерий.
2. **п.4** `grep -c 'Кроссворд Тьюринга' <…>/src/` на каталоге без `-r` вернёт не 0, а ошибку `Is a directory` — то есть «зелёный» тут неотличим от сломанной команды. Гоняю `grep -rl` / `grep -r … | wc -l` и печатаю обе стороны: 0 в `src/`, ≥1 в `dist/index.html`.
Остальные четыре пункта беру как есть.

### 9. Порядок ходов
(1) канон в `skeleton/sluzhebnye/` → (2) `build_deck.py` → (3) три дека байт-в-байт (гейт; красное = откат) → (4) миграция Паскаля → (5) прогон «нет поля — нет элемента» → (6) G12 + фикстуры + мутация → (7) доки → (8) верификатор → (9) коммит.


## ВОПРОСЫ — (заполняет исполнитель)

### 1. Регистрация в `KARTA.md §6` — строки ГОТОВЫ, вписывает аналитик
**Новых `.md` в `_studio/` я не завёл ни одного**, поэтому ворота 5 `check_kartoteka.py` (они судят только `_studio/**/*.md`) молчат по построению. Новые `.md` лежат в `_generator/` (`skeleton/sluzhebnye/vizitka.md` и по одному `brief.md` в двух новых фикстурах) — регистрации не требуют. Но **два существующих описания в §6 устарели** после этого захода; готовые замены (вставить внутрь абзаца «Инфра фабрики»):

- к пункту `skeleton/`: `skeleton/` (пустой greenfield-`src/` = «шаг 0»; несёт `base.css` канон-base **и `sluzhebnye/` — канон трёх служебных слайдов: обложка, визитка «Про меня», финал «Спасибо за внимание» порождаются генератором из полей `brief.md`, а не верстаются в каждом деке; заведено 28.07, заход `../zhurnal/2026-07-27_paskal-lekcia-sborka/kod_sluzhebnye-slajdy.md`, гейт — G12**)
- к пункту `tools/fixtures/`: добавить в перечисление папок `sostoyanie/` — `…десять папок → двенадцать: 28.07 добавлены **`g12-porozhdenie`** (дек на порождении с рукописным `sl-title` — G12 обязан покраснеть) и **`zdorovyj-porozhdenie`** (дек на порождении без рукописных служебных — G12 обязан молчать)`

### 2. 🔴 Финал Паскаля унёс с собой содержательный слайд — вернуть отдельным?
Финалом дека служил не «спасибо», а `sl-course` «Что осталось»: Серпинский + строка «в листок: сколько ждать двух орлов подряд · общее тождество Вандермонда · курс „Перечислительная комбинаторика“, ЛШП NlogN, август». Канонный финал тела текста не несёт — **этот текст из дека пропал целиком**. Заход санкционирует потерю дважды (§2.3, §3), поэтому я её сделал, но анонс курса и домашний листок — вещь, которую обычно не выбрасывают. Если нужно вернуть — это отдельный содержательный слайд ПЕРЕД финалом (`slide_order`, обычная арка 7), а не текст на финале: на финале его теперь физически негде написать, и это by design. Иллюстрацию я сохранил объявлением `final_ill: sierpinski` — она стоит на канонном финале.

### 3. Найдена невыполненная поблажка G7 — не чинил, гейт чужого захода
`GEJTY.md` §G7 утверждает, что раскладка не требуется от первого раздела-обложки («освобождено 28.07.2026 по вопросу исполнителя: иначе вечное ложное ❌ на каждой будущей ленте»). **В коде поблажки нет:** `check_g7` в `sostoyanie.py` перебирает ВСЕ разделы (`no_layout = [h for h, b in sections if not RASKLADKA_RE.search(b)]`), и на здоровой ленте Паскаля G7 печатает `без «поле:mn **Раскладка.**»: ['Внутри треугольника Паскаля']` — это ровно раздел-обложка. Ложное ❌, ровно того класса, о котором предупреждает §3 моего захода. Не чиню: G7 — предмет захода `kod_gejty-shaga-6`, а моя задача про G12; правка в одну строку (`sections[1:]` в этой сборке списка), но вердикт «какой раздел считать обложкой, когда лента из двух файлов» стоит принять владельцу/аналитику явно.

### 4. Когда мигрировать `buffon`, `dandelin`, `fibonacci`?
Сейчас они на рукописных служебных слайдах и потому судятся старым счётом слов. Механика их ждёт: достаточно заменить в их `shablon.html` список `{{SLIDE:…}}` на `{{SLIDES}}`, добавить `{{SLUZHEBNYE_CSS}}` и снести три файла. Я этого не делал — заход прямо запрещает (§2.2, главный риск). Заметьте цену ожидания: пока они не мигрированы, правка биографии в визитке — это три правки в трёх деках плюс одна в каноне.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделано и ЗАЧЕМ

**1. Канон `_generator/skeleton/sluzhebnye/` — семь файлов.** `oblozhka.html`, `vizitka.html`, `final.html` (заготовки), `vizitka.md` (текст визитки — дословная копия `buffon/src/content/sl-vizitka.md`), `style.css` (CSS всех трёх слайдов), `vizitka-photo.html` + `vizitka-qr.html` (активы — дословные копии из `buffon/src/illustrations/`). *Зачем именно так:* активы лежат в каноне и вшиваются оттуда, копий в деках нет — правка биографии, фото или QR теперь одна на все деки, а не по одной в каждом.

**2. `build_deck.py` — порождение (+95 строк, три точки врезки).** `fill_template()` — диалект заготовки из двух форм (`?ПОЛЕ`/`!ПОЛЕ` — условная строка, `{ПОЛЕ}` — подстановка). `generate_service_slides()` — три слайда из полей `brief.md`. В `load_source()` — включение по плейсхолдеру, в `lint()` — эффективный порядок. *Зачем диалект, а не f-string:* заготовка должна читаться как обычный HTML, иначе править её будут в питоне, а не в HTML, и канон снова расползётся.

**3. Механизм включения — плейсхолдер `{{SLIDES}}` в `shablon.html`, и это главное решение захода.** Порождение не «умное»: у дека либо есть этот плейсхолдер, либо нет. Есть — генератор строит весь поток сам (обложка + визитка + `slide_order` + финал) и приносит канон-CSS через `{{SLUZHEBNYE_CSS}}`. Нет — не исполняется ни одна новая ветка. *Зачем:* это и есть доказательство безопасности для трёх чужих деков — не «я аккуратно не сломал», а «код для них не выполняется». Побочно закрылось дублирование ПОРЯДКА слайдов: у дека на порождении он живёт только в `brief.slide_order`, а не второй раз списком `{{SLIDE:id}}` в шаблоне (H6).

**4. Приоритет рукописного.** Есть `slides/<служебный id>.html` — берётся он, порождение молчит. *Зачем:* дек не должен ломаться от того, что кто-то положил файл; ломаться должен ГЕЙТ, а не сборка.

**5. Мигрирован дек Паскаля.** Снесены `slides/sl-title.html`, `slides/sl-course.html` и пустые `content/sl-title.md`, `content/sl-course.md`; в `brief.md` добавлены `cover_ill: title-art` и `final_ill: sierpinski`, из `slide_order` убраны два id; в `shablon.html` 22 строки `{{SLIDE:…}}` → `{{SLIDES}}`, добавлен `{{SLUZHEBNYE_CSS}}`, удалён уехавший в канон блок `.slide[data-kind="cover"]`.

**6. Скелет greenfield переведён на порождение** (`skeleton/shablon.html` + `brief.md` + `SPEC.md`). *Зачем:* иначе каждый следующий дек стартовал бы с рукописных служебных слайдов, и структурная ветка G12 никогда бы не сработала. Проверено: `build_deck.py _generator/skeleton` даёт деку из ровно трёх слайдов — `sl-title`, `sl-vizitka`, `sl-thanks`.

**7. Гейты и доки.** `G12` переписан на развилку по грепу (структура для дека на порождении, прежний счёт слов для немигрированных); `H6` получил отметку о первом живом применении к слою вёрстки; `FORMAT-ISTOCHNIKA.md` — шесть полей в схему `brief.md` + раздел про порождение + предупреждение про плейсхолдер в комментарии; `07-verstka/DOK.md` — три слайда изъяты из зоны вёрстки; `skeleton/SPEC.md` — состав и правила канона.

### Как проверено (прогонами; охват назван)

| # критерия | Что | Результат |
|---|---|---|
| 1 | `build_deck.py <дек>/src` на **4 из 4** деков | exit **0** везде |
| 2 | render-identity трёх немигрированных | **байт-в-байт**, `cmp` exit 0 на 3 из 3 (сборка в scratch, чужой `dist/` не тронут). `render.py --compare` тоже прогнан — см. ниже, он оказался негодным гейтом |
| 3 | рукописных служебных в `paskal/src/slides/` | **0**; в `dist/index.html` все три есть, визитка **вторая** (`sl-title`, `sl-vizitka`, … , `sl-thanks`) |
| 4 | текст визитки в источнике / в выходе | `grep -rl 'Кроссворд Тьюринга' src --exclude-dir=dist` → **0 файлов**; `grep -c` по `dist/index.html` → **1** |
| 5 | «нет поля — нет элемента» | убрал `cover_ill` → сборка **exit 0**, на обложке `<div class="art">` физически нет (один мягкий warn «title-art не используется» — верное поведение), вернул |
| 6 | `PROGNAT.sh` | **ФИКСТУРЫ ЗЕЛЁНЫЕ**, exit 0; мутация в ОБЕ стороны: `hand = []` → краснеет `g12-porozhdenie`; `hand = все` → краснеет `zdorovyj-porozhdenie`; возврат → зелёные |

Сверх критерия прогнано ещё пять веток (охват **11 из 11** задуманных проверок):
- `vizitka: net` → дек из 22 секций, `sl-vizitka` отсутствует, текста визитки в выходе **0**;
- финал БЕЗ `final_ill` → заголовок в центре холста (`.thx-solo`), не в верхней ленте;
- `slide_order` со ссылкой на несуществующий слайд → падает ГРОМКО: два внятных сообщения линтера, exit 1;
- удалил `sluzhebnye/` из копии дека → сборка всё равно прошла: канон читается из генератора, а не из дека (то есть «один канон» — не декларация);
- геометрия обложки ДО/ПОСЛЕ в браузере: бокс `.art` совпал точно (`0,514,1440,250`), `.head` изменился ровно на высоту снятого подзаголовка (306→227px, блок пере-центрировался 187→216).
- G12 по четырём живым декам после правки: `buffon` PASS, `dandelin` PASS, `fibonacci` FAIL (**известный долг «финал-задача», был красным и до меня**), `paskal` PASS структурной веткой. Ни одного НОВОГО ❌.
- Полный трекер по Паскалю: жёсткие FAIL до захода — `G1, G2, G5, G7`; после — те же `G1, G2, G5, G7`. Новых не появилось.

**Признак нового G12 написан грепом по всем четырём декам тем же ходом** (как требует §3): `{{SLIDES}}` — 0 совпадений до миграции, 1 после (`paskal`; плюс скелет, он не дек); рукописные `slides/{sl-title,sl-vizitka,sl-thanks}.html` — buffon **3**, dandelin **0**, fibonacci **0**, paskal **1**→**0**. Структурная ветка не судит немигрированные деки вовсе ⇒ **0 ложных ❌** по построению, и три буффоновских слайда остаются законными.

### 🔴 Браузерный слой: прогнан, и он оказался НЕГОДНЫМ гейтом
Chrome + playwright на машине есть (`chrome 150.0.7871.186`), `render.py --compare` прогнан на всех трёх деках — **но пункт 2 критерия в его буквальной форме недостижим, и не из-за моей правки**:
1. `render.py` возвращает **exit 0 всегда** (`main()` → `return 0` независимо от результата сверки). «`cmp` exit 0» как гейт не может провалиться.
2. Сверка **недетерминирована на здоровых входах**. Контрольный опыт: файл сверяли **сам с собой** (`sha256` совпадает, `cmp` exit 0) — два прогона подряд дали `слайд 00: 20 пикселей` / `слайд 13: 278663` и `слайд 00: 44` / `слайд 13: 102300` / `слайд 14: 189408`. Расходятся канвас-симуляции, 3D-главы и вступительная анимация `deck-in`.

Поэтому доказательством пункта 2 я сделал **байт-равенство** пересборки с закоммиченным `dist/index.html` (объявил это в `## ПЛАН` до работы): оно строго сильнее пиксельного и не требует браузера. Все три дека — `cmp` exit 0.

### Найденный и починенный по ходу дефект вёрстки (замером, не на глаз)
Канонный финал с ВЕРТИКАЛЬНОЙ иллюстрацией уезжал за нижний край холста: у `buffon` финальный рисунок широкий (viewBox 1180×520) и вписывался сам, а Серпинский (322×286) брал высоту из своего viewBox — авто-строка внутренней сетки росла под содержимое, замер дал бокс **1048px в полосе 572px**. Починено определённой строкой + растяжкой (`grid-template-rows: minmax(0,1fr)` + `align-self: stretch`), после починки замер — **572 в 572**. Это единственное место, где канон-CSS отступает от эталона по геометрии; отступление и его цена записаны прямо в `style.css` у правила.

### Что НЕ трогал
- **`buffon`, `dandelin`, `fibonacci` — ни байта**, ни в источнике, ни в `dist/`. Их сборки для сверки клались в scratch через `-o`, чтобы не переписать чужой `dist/`.
- `engine.js`, `tokens.css`, `base.css`, `fonts/` — не тронуты нигде.
- `G7` в `sostoyanie.py` — найден невыполненный экземпшн (`## ВОПРОСЫ` п.3), НЕ чинил: гейт принадлежит другому заходу арки.
- Всё вне зоны. Грязное в дереве (`_studio/docs/KARTA.md`, `INVENTAR-L1.md`, два `KARTA-OBLASTI.md`, два неотслеженных файла) — чужое, не тронул и не коммичу.
- Сам файл захода `kod_sluzhebnye-slajdy.md` **не входит в мои пути коммита** (§4 их перечисляет, журнала среди них нет) — остаётся неотслеженным, его коммитит аналитик вместе с приёмкой.

### Время и токены
Заход занял около часа настенного времени в один проход; из них ~10 минут — браузерные прогоны `render.py --compare` (три дека послайдно, по два кадра на слайд) и контрольный опыт с двумя повторами. Точного счёта токенов у меня нет — инструмента, который бы его вернул, в зоне нет, а выписывать число «на глаз» запрещено (`KONSTITUCIYA §10`); по расходу это один длинный заход без параллельных субагентов, кроме одного верификатора.

**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
