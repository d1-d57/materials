# Канал исполнителя — dek-paskal (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — раскадровка расписана, но посадка текста на канвас и доводка рисунков рождаются по ходу, глазами.

КОНТЕКСТ. Открытая онлайн-лекция «Внутри треугольника Паскаля» для 9–10 класса, ~90 минут,
реклама курса перечислительной комбинаторики ЛШП NlogN.

Прошлый этап (Cowork, 27.07): концепт утверждён, **тексты слайдов написаны и приняты владельцем**,
**21 иллюстрация нарисована** — всё лежит в журнальном doc-виде `raskadrovka/teksty/`
(две вкладки, `build_doc.py`, оба гейта зелёные). Объём текста уже подогнан под слайд:
потолок сверен с эталоном `buffon/` и со скриншотом живого слайда, ни один слайд не превысил.

ЦЕЛЬ: собрать из этого **дек** — самодостаточный `dist/index.html` канона 1440×810 со сценами.
Тексты переносятся почти как есть; твоя работа — раскадровка по сценам, посадка на канвас,
доводка семи названных рисунков и сборка.

Приёмка — по ОТЧЁТУ, без построчной сверки.
Если стоп до цели: получишь собранный дек с посаженными текстами, но НЕ доведённые рисунки —
в таком порядке и работай (§2 шаг 5 идёт последним осознанно).

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `zahod/dek-paskal` в основной папке репозитория — `git checkout zahod/dek-paskal` первым ходом. Ветку НЕ переключай дальше, в другие НЕ коммить.
- **ЗОНА (можно менять):** lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/ lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/teksty/ _studio/zhurnal/2026-07-27_paskal-lekcia-sborka/kod_dek-paskal.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout zahod/dek-paskal`.
- Точка отката: `git add lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/ lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/teksty/ _studio/zhurnal/2026-07-27_paskal-lekcia-sborka/kod_dek-paskal.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти якоря, в этом порядке. Проект не изучай, ничего сверх списка не открывай:
  1. **`_studio/konvejer/FORMAT-ISTOCHNIKA.md`** — контракт источника дека: что лежит в `src/`,
     разметка сцен `{@N}`, механика `fill` / `blur-reveal`, как устроен слайд.
  2. **скилл `html-slides-studio`** (`SKILL.md` + `references/images.md`) — канон дека: 4-блочный
     контракт файла, фиксированные токены тела, навигация кликером, порядок гейтов, PNG-экспорт.
  3. **`buffon/src/`** — ЖИВОЙ ЭТАЛОН, на нём и сверяйся. `content/*.md` — объём и разметка
     текстов, `slides/*.html` — раскладки, `tokens.css` / `shablon.html` / `engine.js` — движок.
     **Это тот же курс и тот же автор: не изобретай вид, копируй устройство.**
  4. **`lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/teksty/blok-1.md`
     и `blok-2.md`** — ТЕКСТЫ И РИСУНКИ, твой вход. 14 разделов `##` = 14 слайдов подряд
     (7 в первом файле, 7 во втором). Внутри — готовые `<figure>` с инлайновым SVG.
  5. **`lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/SPEKA-risunkov.md`** —
     палитра классов `.s-*`, сетка, эталонный SVG, процедура «отрендерил → посмотрел глазами».
  6. **`_studio/konvejer/09-illustracii/SLOVAR-primitivov.md`** — словарь примитивов и ловушки.
  7. `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/KONCEPT.md` — **только §4
     (линия разрезов) и §6 (правила подачи)**, если понадобится сверить замысел. Остальное там
     устарело относительно текстов: слайдов теперь 14, телескоп и ответ слиты.
- ПЛАН — в `## ПЛАН` перед действиями.

🔴 **Работай с браузером в петле, а не вслепую.** У тебя, в отличие от Cowork, есть возможность
открыть собранный HTML и посмотреть. Заведи её ПЕРВЫМ делом и правь в цикле
«правка → пересборка (секунды, ноль токенов) → взгляд». Всё, что описано ниже как «некрасиво»,
«не влезло», «стрелки кривые», ловится только глазами; Cowork ровно на этом и споткнулся.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

### Шаг 1. Дешёвый макет — ДО всякой красоты

Не начинай с финального дека. Сначала посади все 14 слайдов в **макет**: тексты и рисунки
на канвасе 1440×810 без доводки, единственный вопрос — **что влезло, а что нет**.
Посмотри глазами все 14. Только после этого §Шаг 3.

Это порядок фабрики, а не перестраховка: посадка на фиксированный канвас — единственное,
чего нельзя проверить в journal-виде, и единственное, из-за чего придётся резать текст.

**Разрешение на правку текста.** Не влезает — можно сокращать и переформулировать, но:
экономь на служебных словах, не на математике; выкинутое утверждение или формула — это
изменение содержания, оно идёт в `## ВОПРОСЫ`, а не молча в дек.

### Шаг 2. Раскладки — из готовых пометок

У каждого слайда в исходнике стоит заметка на поле `> поле:mn **Раскладка.** …` — это решение
владельца о типе слайда, принятое вместе с текстом. Соблюдай. Два рабочих типа:

- **текст слева + вертикальная полоса иллюстраций справа** — рисунки стоят СТОПКОЙ сверху вниз,
  не сменяют друг друга;
- **текст сверху + крупная горизонтальная иллюстрация снизу** (или на всю ширину).

Заметки `> поле:mn` в дек НЕ переносятся — это служебное, для владельца.
Метки разрезов `✂ **разрез №N**` переносятся: это сквозной визуальный код лекции,
им место сбоку/на поле слайда мелким значком, НЕ в потоке текста.

### Шаг 3. РАСКАДРОВКА — точная, по ней и собирай

14 слайдов, 37 сцен. Ниже — что появляется в каждой сцене. Абзацы текста уже стоят в исходнике
в этом порядке, тебе нужно проставить `{@N}`.

**Блок I (`blok-1.md`)**

| слайд | сцен | что в сцене |
|---|---|---|
| **S1 · Титул** | 1 | имя лекции и строка про курс |
| **S2 · Пьяница у обрыва** | 2 | `@1` постановка + вопрос жирным · `@2` обещания (Вандермонд, Фибоначчи, Серпинский) + запуск канваса |
| **S3 · Столбцы** | 3 | `@1` вопрос «где окажется» · `@2` строка «записываем столбцами» + таблица, **заполняющаяся по столбцу**, + чётность · `@3` правило соседей + рисунок двух приходов |
| **S4 · Треугольник Паскаля** | 2 | `@1` «сколько траекторий» + оба треугольника · `@2` разрез №1 (метка) |
| **S5 · Один объект, много обликов** | 4 | `@1` три облика · `@2` три способа считать · `@3` симметрия и сумма строки · `@4` команда и капитан + выкладка |
| **S6 · Разрез по центру** | 3 | `@1` вопрос «а если резать по центру» + разрез · `@2` переворот второй половины + метка разреза №2 · `@3` обе стороны, $\binom{2n}n$ и $\sum\binom nk^2$ |
| **S7 · Сумма квадратов** | 3 | `@1` строка, квадраты, сумма 70 · `@2` два игрока по 10 монет · `@3` выкладка с $(1+x)^{2n}$ и хвост про Вандермонда |

**Блок II (`blok-2.md`)**

| слайд | сцен | что в сцене |
|---|---|---|
| **S8 · Фибоначчи** | 3 | `@1` вопрос про двух орлов подряд · `@2` выписанные слова 2, 3, 5 (дерево) · `@3` разрез №3 и рекуррента |
| **S9 · Домики** | 3 | `@1` домики на ломаной · `@2` «уберём спуск после подъёма» + сжатие · `@3` сумма и восходящие диагонали |
| **S10 · Пьяница возвращается** | 3 | `@1` вопрос + шесть траекторий · `@2` рекуррента есть, формулы не видно · `@3` **2, 6, 20, 70** узнаются, гипотеза $\binom{2n}n$, перечёркнутая метка разреза |
| **S11 · Считаем сорвавшихся** | 2 | `@1` «зайдём с изнанки» + разрез №4 · `@2` вопрос «что с левым куском можно сделать» |
| **S12 · Отражение** | 3 | `@1` отражаем · `@2` склеиваем, обратимость · `@3` формула $A_h$ и сходимость с перебором |
| **S13 · Телескоп и ответ** | 3 | `@1` сумма разностей · `@2` всё сокращается, остаётся один член · `@3` $\mathbb P=\binom{2n}n/4^n$, вопрос закрыт |
| **S14 · Что осталось** | 2 | `@1` картинка Серпинского · `@2` анонс курса |

🔴 **Сцена — новый кусок содержания, который в предыдущий не влезает.** Не дроби сцену ради
эффекта появления. Правило владельца из `KONCEPT §6`.

**Выделения.** Жирным — то, что уже выделено в исходнике (это решения владельца, не трогай).
Акцентным цветом — ответы и итоговые формулы. Блюров и спойлеров в этой версии **нет**:
вопросы в зал собираются отдельной работой и приедут потом. Если увидишь очевидное место
под блюр — не ставь, назови его в `## ВОПРОСЫ`.

**Сквозная нить, которую нельзя потерять.** Числа **2, 6, 20, 70** появляются трижды: S7
(сумма квадратов), S4 (центральный столбец) и S10 (перебор). На S10 зал должен их УЗНАТЬ —
подай их так, чтобы узнавание сработало визуально.

### Шаг 4. Сборка и гейты

Собери генератором (`build_deck.py`), прогони штатные гейты канона: var-lint, `svgOverflow`,
`svgProse`, переполнение зон, render-identity. Все зелёные — обязательное условие.

### Шаг 5. Доводка рисунков — СЕМЬ конкретных правок владельца

Рисунки уже нарисованы и в целом приняты («все иллюстрации хорошие»). Правь **только это**,
инициативы не проявляй:

1. **Стрелки не нарисованы** на рисунке «в клетку приходят из двух соседних» (S3). Наконечник
   задаётся ЗАЛИВКОЙ (`fill`), а не обводкой — в исходнике он собран как `<polyline>`, и в деке
   выйдет полым или невидимым. Почини здесь и проверь ВСЕ остальные стрелки в обоих файлах:
   владелец отдельно сказал, что стрелки местами некрасивые.
2. **Подписи `<figcaption>` в дек не переносятся вообще.** На слайде их нет: рисунок объясняет
   лектор голосом. Выкинуть все.
3. **Таблица на S3 не должна быть статичной картинкой.** Она заполняется по столбцу вместе
   с текстом — это сцена `@2`. Сейчас в исходнике два отдельных рисунка (таблица и таблица
   с траекторией); в деке это одна таблица, которая растёт, а траектория ложится поверх.
4. **«Четыре облика» (S5) переделать.** Владелец: путь нужен ДЛИННЕЕ — на четырёх шагах
   соответствие не читается. И обликов должно быть **три, а не четыре**: «выбор $k$ мест» —
   это то же самое, что слово из О и Р, четвёртая панель лишняя. Оставить: ломаная · слово ·
   маршрут в решётке. Текст слайда уже назван тремя обликами, сверься с ним.
5. **Симметрия (S5).** Если на слайде рядом уже стоит треугольник Паскаля — отмечать симметрию
   прямо на нём, отдельного рисунка не нужно. Если треугольника рядом нет — просто строка
   с осью. Стрелки в нынешнем варианте кривые, переделать.
6. **Сумма квадратов (S7).** Должно быть просто: конкретная строка треугольника, под ней
   квадраты её чисел, под ними сумма 70. Сейчас «идея правильная, но получается не очень
   красиво» — пересобери, глядя на результат.
7. **Телескоп (S13) — рисунок непонятный, в нынешнем виде НЕ нужен.** Два выхода, выбирай сам
   и назови выбор в отчёте: либо выкинуть совсем (сцена `@2` обойдётся текстом и формулой),
   либо сделать **маленький интерактив**: члены суммы появляются по одному, видно, как
   очередной гасит предыдущий, остаётся один. Второе лучше, если укладывается дёшево.

**Анимация — по необходимости, не для красоты.** Естественные кандидаты: канвас пьяницы на S2
(точка ходит, часть прогонов срывается), заполнение таблицы на S3, телескоп на S13. Больше
нигде не нужно.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
- собран `dist/index.html`, в нём **ровно 14 слайдов и 37 сцен** (проверяется скриптом-счётом,
  а не глазом);
- **все штатные гейты канона зелёные**: var-lint, `svgOverflow`, `svgProse`, переполнение зон,
  render-identity;
- **`svgProse` = 0** — прозы внутри `<svg>` нет нигде (главное правило `SLOVAR-primitivov.md`);
- **все 14 слайдов просмотрены глазами** в браузере и это названо в отчёте числом
  («просмотрено 14 из 14»), а не словами «выглядит хорошо»;
- **семь правок §Шаг 5 закрыты поимённо** — по каждой строка в отчёте, что сделано;
  правка 7 — с названным выбором.

Отрицательный вердикт несёт ОХВАТ: не «переполнений нет», а «переполнений нет, проверено
14 слайдов из 14».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР
Операция ЛОССОВАЯ: тексты и рисунки переносятся из journal-вида в дек, и потеря абзаца или
целого `<figure>` при переносе не видна ни одним гейтом — дек соберётся зелёным и без них.

Свежий субагент, другой метод: сверить программно `raskadrovka/teksty/blok-*.md` с собранным
`dist/index.html` — **список потерянных абзацев ПУСТ, список потерянных рисунков ПУСТ**.
Осознанно выкинутое (`figcaption`, заметки `> поле:mn`, четвёртая панель обликов, телескоп —
если выбрал выкинуть) в список потерь не идёт, но перечисляется отдельной строкой отчёта.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/ lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/teksty/ _studio/zhurnal/2026-07-27_paskal-lekcia-sborka/kod_dek-paskal.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/ lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/teksty/ _studio/zhurnal/2026-07-27_paskal-lekcia-sborka/kod_dek-paskal.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
