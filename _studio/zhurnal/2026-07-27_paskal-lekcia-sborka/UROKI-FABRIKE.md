# УРОКИ ФАБРИКЕ — арка «2026-07-27_paskal-lekcia-sborka»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c '^### '      UROKI-FABRIKE.md   # уроков
grep -c '^ЦЕНА: '    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -c '^ВЕРДИКТ: ' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---

### 1. Обсуждали как нерешённые две задачи, которые уже стояли решёнными в собственном тексте проекта
Владелец вошёл в сессию с двумя «сущностно нерешаемыми» проблемами — вероятностный вход в Серпинского и мотивировка финала. Обе были помечены открытыми в `istochniki/pascal-lecture-concept.md` (24.07). Но `kotly/matematika.md` (тоже 24.07, но позже) обе закрывал: там стоит игра про монеты и отражение в линии рекордов. Концепт и текст разошлись, а роутера между ними не было.
ЦЕНА: два полных круга диалога — сначала я подробно доказывал владельцу то, что уже лежало в его файле; поймал Cowork только когда полез читать файлы вместо того, чтобы отвечать по постановке. Обратная сторона той же цены: в проекте нет отметки «этот документ устарел, смотри такой-то», хотя `HANDOFF.md` там есть.
ВЕРДИКТ:

### 2. Вписал в собственный документ число, которое не сходится с его же таблицей
В `KONCEPT.md` стояло «60 сцен» при том, что сумма по блокам в той же таблице давала 72. Число было посчитано в уме и вписано руками — прямое нарушение `KONSTITUCIYA §10` («пишется не число, а команда, которая его считает»), совершённое в документе, который сам же и содержал слагаемые.
ЦЕНА: владелец принимал решение о резке по числу, заниженному на 17%, и один круг обсуждения объёма прошёл на ложных данных. Поймал я сам на следующем круге, когда пересчитывал для сверки с эталоном. При этом рядом, в `buffon/`, объём считался скриптом — то есть правильный способ был в руках и не был применён к своему файлу.
ВЕРДИКТ:

### 3. Проектировал габарит нового артефакта на глаз, имея живой эталон в том же репозитории
Три круга подряд предлагал 32, потом 27, потом 21 слайд «по ощущению», не сверяясь ни с чем. Эталон — `buffon/src/content/`, 17 слайдов и 46 сцен — назвал владелец, а не я, хотя папка лежит в корне `materials/` и в `KARTA §6` не упомянута только потому, что это проект, а не фабрика.
ЦЕНА: три круга проектирования вхолостую и одно решение (снять Серпинского), принятое под давлением объёма, которое можно было принять на два круга раньше и спокойнее. Пять минут скрипта по разметке `{@N}` дали габарит точнее, чем три круга интуиции.
ВЕРДИКТ:

### 4. Считал снятие блюра отдельной сценой
Размечал `{blur@N}` как смену сцены, из-за чего раскадровка была раздута примерно на десяток «сцен», не несущих нового содержания. Владелец: «снять блюр — это не новая сцена, это значит мы задаём вопрос и люди отвечают; новая сцена — когда ты выдаёшь новый кусок содержания, который в предыдущий уже не влезает».
ЦЕНА: завышенная оценка объёма поверх уже завышенного числа из урока 2, то есть ошибки сложились. Разметка при этом копировалась из Бюфона, где `blur-reveal` и `data-scene-from` — разные атрибуты; различие было в исходнике перед глазами и не было прочитано.
ВЕРДИКТ:

### 5. Три конкурирующие мотивации к одному факту выдавались за богатство выбора
Вандермонд входил то через сумму квадратов, то через задачу о двух игроках, то через «а давайте резать по центру». Я предлагал их как варианты. Владелец: «у нас слишком много разных мотиваций из разных мест… это очень некрасиво звучит» — и сформулировал правило: мотивация всегда одна, найти правильный разрез; всё остальное идёт ПОСЛЕ разреза, а не до.
ЦЕНА: два круга, в которых блок не принимался, и владелец всерьёз рассматривал снятие Вандермонда целиком — то есть чуть не потеряли сюжет, который в итоге оказался несущим (через него пришла последовательность 2, 6, 20, 70, работающая на финал). Признак был виден сразу: когда к факту предлагается три входа, это значит, что входа нет ни одного.
ВЕРДИКТ:

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

### 6. Габарит артефакта сверили с эталоном по ЧИСЛУ сцен, а не по плотности содержания
Объём будущей лекции считали скриптом от живого эталона `buffon/` — 15 слайдов, 42 сцены против 17 и 46. По этой мерке сошлось. Но сцена у нас оказалась «одно утверждение», а у эталона — разобранный сюжет: формулы были названы, но не прожиты на числах.
ЦЕНА: лекция, спроектированная на 90 минут, прочитана вживую за 50. Переделывается весь дек — с 15 слайдов до 20–22, плюс два круга голосового разбора владельца и полная перерисовка половины иллюстраций. Это ПОВТОР урока 3 в новой одежде: там габарит проектировали на глаз при живом эталоне, здесь сверились с эталоном, но не по той величине.
ВЕРДИКТ:

### 7. `data-scenes` слайда прописан руками и разошёлся с числом сцен в его `content`
Число сцен живёт в двух местах: маркеры `{@N}` / `blur@N` в markdown и атрибут `data-scenes` в `slides/*.html`. Второе проставляется вручную и после правки текста не обновляется.
ЦЕНА: кликер доходил до объявленного числа сцен и уходил на следующий слайд, а блюр не раскрывался никогда — то есть вопрос в зал переставал работать. Шесть слайдов из пятнадцати. Поймал владелец на живой лекции, вслепую, по симптому «нажимаю, а оно перескакивает». Ловится десятью строками Python, которых не было.
ВЕРДИКТ:

### 8. Каскады сцен в CSS выписаны руками и обрывались на `.scene-5`
И правила видимости `[data-scene-from]` в `base.css`, и правила раскрытия `.blur-reveal` в `shablon.html` перечислены вручную, парами «сцена × номер», до пятой сцены включительно.
ЦЕНА: на шестой сцене не срабатывало ни одно правило — всё появившееся пряталось обратно, и оставалась только первая сцена. Владелец описал это как «последняя сцена повторяет первую, лишняя сцена». Поймано на живой лекции; страдали все слайды, доросшие до шести сцен. Оба каскада обязаны генерироваться, а не выписываться.
ВЕРДИКТ:

### 9. Карта разворачивания классов в рецепте визуального гейта была неполной
`STANDART-oformlenia.md §6` даёт рецепт «разверни классы `.s-*` в атрибуты и посмотри PNG», но сама карта в нём стояла огрызком с многоточием — без `.s-ar-m` / `.s-ar-a`.
ЦЕНА: два субагента независимо дописали недостающие классы каждый в свою копию скрипта. То есть починка произошла дважды и не доехала ни до кого: общий рецепт остался сломанным для следующего. Плюс стрелка задаётся заливкой, а не обводкой — развёрнутая неверно, она рисуется полой.
ВЕРДИКТ:

### 10. `cairosvg` масштабирует по атрибуту `width` и игнорирует пропорции `viewBox`
Гейт «посмотри PNG глазами» выдавал растянутую картинку: рисунок 372×122 с `width="640"` вытягивался по горизонтали в 1,7 раза.
ЦЕНА: исполнитель смотрит на искажённый вид, чинит несуществующий перекос и ломает верную композицию. Поймано на четырёх параллельных исполнителях сразу — каждый оценивал свои рисунки по вранью.
ВЕРДИКТ:

### 11. Маркер сцены `{@N}` не работает внутри пункта списка
Блочная форма `{@N}` распознаётся только в начале абзаца; внутри `- ` нужна инлайновая `{@N|текст}`.
ЦЕНА: на живом слайде вместо пунктов напечатались буквальные `{@3}`, `{@4}`, `{@5}`. Поймал владелец глазами на собранном деке. В линтере этого случая нет, хотя ловится одним грепом по `dist`.
ВЕРДИКТ:

### 12. Жёсткая спека + эталонный SVG + обязательный «посмотри PNG» позволили распараллелить рисование на четырёх агентов
Написан один документ со списком разрешённых классов, сеткой, готовым образцом и процедурой проверки глазами. Четыре субагента нарисовали 19 иллюстраций примерно за 25 минут, стиль не разъехался, каждый рисунок кто-то реально видел и переделывал.
ЦЕНА: за что куплено — первая попытка рисовать без спеки дала таблицу в рамках, похожую на набор карточек, и ломаную, наезжающую на цифры, — два рисунка переделаны вручную. Цена самой спеки — один прогон на эталонном рисунке до раздачи задач.
ВЕРДИКТ:

### 13. У дек-движка кэш KaTeX статический: новая формула валит сборку линтером
Формулы в деке берутся из `math/katex.json`, который собирается отдельным `tools/build_math.py`. Правка формулы в тексте без прогона этого скрипта останавливает сборку.
ЦЕНА: один круг диагностики посреди правок перед лекцией — сообщение линтера про «не в katex.json» читалось как поломка источника, а не как отсутствующий шаг.
ВЕРДИКТ:

### 14. У Cowork нет браузера, и визуальная доводка ушла кругами через владельца
Всё, что видно только глазами — не нарисованные стрелки, съехавшая композиция, вылезший за строку текст, спойлерящая картинка, — аналитик проверить не может; проверял владелец по скриншотам.
ЦЕНА: пять кругов правок в день лекции, каждый — сообщение владельца со скриншотом и описанием симптома. Часть из них (два бага каскадов) дожила до эфира. Вывод, сформулированный владельцем прямо: такие задачи надо сразу отдавать Claude Code, у которого браузер есть.
ВЕРДИКТ:
