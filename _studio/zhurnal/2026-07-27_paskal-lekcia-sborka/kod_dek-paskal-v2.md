# Канал исполнителя — dek-paskal-v2 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — переделка содержательная: слайды разбиваются, рисунки перерисовываются,
> появляются интерактивные канвасы. Механику можно расписать, а решения по композиции — нет.

## 🕐 СПЕШКИ НЕТ. Это главное отличие от прошлого захода

Прошлый заход шёл в цейтноте перед живой лекцией, и это вышло боком: рисунки делались наспех,
часть пришлось переделывать вручную по три круга, а два бага в CSS дожили до эфира.

**Сейчас дедлайна нет. Работай 2–3 часа, спокойно и с первого раза.**
Метрика — не скорость и не экономия ходов, а то, что **переделывать не придётся**.

**Из этого следует способ работы, и он обязателен (подробно — §1а).**
Токены тратятся на *решения*, а не на *исполнение*. Всё, что можно поручить скрипту, —
поручай скрипту и гоняй его хоть тысячу раз: пересборка, рендер, скриншот, проверка,
подсчёт, генерация SVG по формуле. Это и есть экономия: один умный скрипт дешевле, чем
двадцать ручных правок координат.

---

КОНТЕКСТ. Открытая онлайн-лекция «Внутри треугольника Паскаля», 9–10 класс, ~90 минут,
реклама курса перечислительной комбинаторики ЛШП NlogN.

**Лекция уже прочитана вживую 27.07 и разобрана владельцем построчно.** Дек собран
(15 слайдов, `src/dist/index.html`), но по итогам прогона переделывается существенно:
лекция уложилась в 50 минут вместо 90, потому что вышла тезисной — формулы названы,
но не прожиты на числах.

ЦЕЛЬ: переделать дек по разбору. Слайдов станет ~20–22, часть рисунков перерисовывается,
появляются интерактивные канвасы, вся вторая половина переходит на другой язык изложения.

Приёмка — по ОТЧЁТУ, без построчной сверки.
Если стоп до цели: получишь переделанные тексты и раскадровку, но НЕ канвасы —
в таком порядке и работай (канвасы идут последним этапом осознанно).

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `zahod/dek-paskal-v2` в основной папке репозитория — `git checkout zahod/dek-paskal-v2` первым ходом. Ветку НЕ переключай дальше, в другие НЕ коммить.
- **ЗОНА (можно менять):** lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/ _studio/zhurnal/2026-07-27_paskal-lekcia-sborka/kod_dek-paskal-v2.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout zahod/dek-paskal-v2`.
- Точка отката: `git add lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/ _studio/zhurnal/2026-07-27_paskal-lekcia-sborka/kod_dek-paskal-v2.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти якоря, в этом порядке. Проект не изучай, сверх списка не открывай:
  1. 🔴 **`lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/RAZBOR-posle-lekcii-2026-07-27.md`**
     — **ГЛАВНЫЙ ВХОД, источник правды по переделке.** Разбор владельца после живого прогона:
     §0 почему лекция вдвое короче плана · §1 сквозные правки · §2–14 по слайдам ·
     §15 канвасы · §16–17 решения владельца по спорным местам · §18 остаток.
     **Всё, что там написано, — обязательно к исполнению.** Ниже в §2 я даю только
     план работ и то, чего в разборе нет.
  2. **`raskadrovka/VOPROSY-bot.md`** — 12 вопросов, на которые зал отвечает в бот
     во время лекции. **Сквозное требование: каждый должен быть отражён на слайдах.**
  3. **`raskadrovka/PRAVKI-sceny-i-animacii.md`** — карта «вопрос → слайд → что под блюром».
  4. **`_studio/konvejer/FORMAT-ISTOCHNIKA.md`** — контракт источника дека: `{@N}`,
     `{blur@N|…}`, `{fill@N|…|…}`, устройство `src/`.
  5. **скилл `html-slides-studio`**, и не только `SKILL.md`: тебе понадобятся
     **`references/simulations.md`** и **`references/animation-patterns.md`** (канвасы и анимация),
     `references/images.md` (канон рисунка), `references/iteration-protocol.md` (петля правок),
     `references/per-slide-algorithm.md`.
  6. **`raskadrovka/SPEKA-risunkov.md`** — палитра классов `.s-*`, сетка, эталон, гейт-рендер.
  7. **`_studio/konvejer/09-illustracii/SLOVAR-primitivov.md`** — словарь примитивов и ловушки.
  8. **`buffon/src/`** — живой эталон устройства дека (не изобретай вид, копируй устройство).
  9. **`fibonacci/src/content/`** — оттуда берём подачу биекции для сюжета Фибоначчи
     (см. `RAZBOR §16.5`). Сделано не идеально, но инструменты для рекурсии по словам из О и Р есть.
  10. Текущее состояние: `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/`
      (`content/*.md`, `slides/*.html`, `illustrations/*.svg`, `shablon.html`, `base.css`).
- ПЛАН — в `## ПЛАН` перед действиями.

## 1а. 🔴 СПОСОБ РАБОТЫ: скрипты вместо ручного труда

Это не совет, а требование заказчика, и оно определяет, как ты тратишь бюджет.

**Принцип.** Токен уходит на решение («какой должна быть картинка»), а не на исполнение
(«впечатать сорок пар координат»). Всё исполнение — скриптом, и скрипт можно гонять
бесконечно: он бесплатный.

**Заведи себе инструменты ПЕРВЫМ делом, до содержательной работы:**

1. **Генератор рисунков.** Не пиши SVG руками по точке. Напиши `tools/gen_ill.py`, который
   строит нужные фигуры из параметров: ломаная по списку шагов ±1, треугольник Паскаля на
   $N$ строк, ряд узлов, сетка-решётка, стрелка. Тогда «сделать треугольник до 10 строки
   вместо 7» — это правка одного числа, а не перерисовка. *Прошлый заход рисовал руками,
   и каждая правка владельца стоила полной перерисовки файла.*
2. **Петля «пересобрал → посмотрел».** `build_deck.py` работает секунды и не стоит ничего.
   Гоняй после каждой правки. У тебя, в отличие от аналитика, есть браузер — **пользуйся им
   постоянно**, а не в конце. Всё, что владелец назвал «некрасиво», «не влезло», «поехало»,
   ловится только глазами.
3. **Скриншоты пачкой.** `render.py` из канона дека снимает все слайды разом. Снимай все
   ~22 слайда одним прогоном и смотри — это дешевле, чем открывать по одному.
4. **Программные гейты — пиши свои, они окупаются мгновенно.** Обязательный минимум:
   - `data-scenes` на слайде **≥** максимума из `{@N}` и `blur@N` в его `content`;
   - каскады в `base.css` и `shablon.html` покрывают **все** сцены, какие есть в деке;
   - в собранном `dist` нет буквальных `{@N}` и `{blur@…}` (значит, разметка не распозналась);
   - каждый из 12 вопросов бота имеет слайд и сцену.
   *Цена, за которую это куплено: два бага дожили до живой лекции — `data-scenes` был меньше
   реального числа сцен (кликер уходил на следующий слайд вместо раскрытия блюра),
   а каскады CSS обрывались на `.scene-5` (на шестой сцене всё скрывалось, оставалась первая).
   Оба ловятся десятью строками Python и не ловятся глазами.*
5. **Проверяй математику перебором, а не рассуждением.** В лекции есть числа, которые обязаны
   сойтись: 42+90+75+35+9+1 = 252 = $\binom{10}5$; хороших слов длины 10 ровно 144; путей
   длины $L$, не уходящих ниже нуля, — 1, 1, 2, 3, 6, 10, 20, 35, 70, 126, 252.
   Написал число на слайд — проверь его `itertools`, это три строки.
   *В разборе уже поймана одна неверная формула, произнесённая владельцем вслух.*

**KaTeX.** В деке кэш формул статический. Новая формула ⇒ сначала
`python3 tools/build_math.py`, потом `build_deck.py`, иначе линтер валит сборку.
Правило: правил формулы — прогнал `build_math.py`.

**Субагенты.** Рисунки хорошо параллелятся: дай каждому спеку (`SPEKA-risunkov.md`) и
обязательный гейт «отрендерил → посмотрел глазами → переделал». Но **генератор пиши один
и общий** — иначе стили разъедутся, как в прошлый раз. ≤5 субагентов.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

**Содержание переделки целиком лежит в `RAZBOR-posle-lekcii-2026-07-27.md`. Не пересказываю
его здесь — читай оттуда и исполняй.** Ниже только порядок работ и то, чего в разборе нет.

### Этап 0. Инструменты (§1а)

Генератор рисунков, петля пересборки, пачечный рендер, четыре программных гейта.
До содержательной работы. Это окупится на первом же круге правок.

### Этап 1. Тексты и раскадровка

Переписать `content/*.md` по разбору. Что меняется структурно:

| было | станет | где в разборе |
|---|---|---|
| S5 «Облики» | **три слайда**: облики с канвасом · симметрия и сумма на большом треугольнике · соседи через задачу о дежурных | §5 |
| S8 «Фибоначчи» | **два**: выписываем слова с деревом · разрез с проверкой на конкретном числе | §8 |
| S9 «Домики» | **два**: конструкция на конкретном слове · применение и диагонали | §9 |
| S7 «Сумма квадратов» | **канвас без текста** | §7, §16.6 |
| S13–S14 | переписаны на язык числа орлов, выкладка по шагам | §12–14, §16.8, §17.3 |

Плюс сквозные правки §1 (числа вместо букв, О и Р на рёбрах, никакого $\sum$, метки без
слова «разрез», дроби TeX-ом) и точечные правки §2–14 по каждому слайду.

**Разрешение на правку текста есть**, но с границей: экономь на служебных словах, не на
математике. Выкинул утверждение или пример — это изменение содержания, оно идёт
в `## ВОПРОСЫ`, а не молча в дек.

### Этап 2. Рисунки

**Три рисунка содержат прямые ошибки, их перерисовать в первую очередь:**

1. **Домики (§9.0):** убран подъём вместо спуска — сжатая ломаная всегда идёт вниз,
   а должна идти вверх-вниз.
2. **Плохой путь в отражении (§11.4):** нарисован с одним касанием $-1$, тогда как путь
   может касаться несколько раз и уходить ниже. Пример должен быть общим.
3. **Правило соседей на S3 (§3):** две белых точки со стрелками в пустоте — не годится,
   правило показывается на живой заполненной таблице.

Остальные — по §2–14. Рисунки, которые владелец не назвал, **не трогай**: «все иллюстрации
хорошие» — его слова.

### Этап 3. Канвасы и анимации (§15)

Порядок по приоритету, он в разборе проставлен. **Обязательные два:**

- **S2 — лестница с ходящей точкой и растущий из неё график** (§17.1, §17.7). Один рисунок
  в две фазы, а не два рядом. Без него первый слайд не объясняет, зачем вообще ломаные.
- **S3 — таблица заполняется по столбцу**, оба яруса, синхронно с блюрами вопросов 1–3.

Дальше по силам: канвас суммы квадратов (§16.6), монетка с тремя обликами (§5A),
канвас биекции Фибоначчи (§16.5), переворот половины, сжатие домиков, отражение, телескоп.

### Этап 4. Сборка и гейты

`build_math.py` → `build_deck.py` → штатные гейты канона (var-lint, `svgOverflow`, `svgProse`,
переполнение зон, render-identity) → свои гейты из §1а.4 → **посмотреть все слайды глазами**.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**

- собран `dist/index.html`; число слайдов в диапазоне **20–22**;
- **все штатные гейты канона зелёные**, `svgProse` = 0;
- **четыре собственных гейта из §1а.4 зелёные**, и они существуют как файлы — не «я проверил»,
  а скрипт, который можно запустить повторно;
- **все 12 вопросов `VOPROSY-bot.md` отражены**: для каждого назван слайд и сцена, где звучит
  вопрос, и сцена, где появляется ответ. Таблицей в отчёте, 12 строк;
- **три ошибочных рисунка (§Этап 2) перерисованы** — по каждому строка в отчёте;
- **обязательные канвасы S2 и S3 работают**;
- **все слайды просмотрены глазами в браузере**, и это названо числом («просмотрено 21 из 21»);
- **числа на слайдах проверены перебором** — перечислить, какие именно проверялись.

Отрицательный вердикт несёт ОХВАТ: не «переполнений нет», а «переполнений нет,
проверен 21 слайд из 21».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР
Операция ЛОССОВАЯ: слайды разбиваются, тексты переписываются, рисунки заменяются — при такой
переработке легко потерять кусок содержания, и ни один гейт этого не покажет: дек соберётся
зелёным и без него.

Свежий субагент, другой метод, **программно**:
1. пройти `RAZBOR-posle-lekcii-2026-07-27.md` по пунктам §1–§14 и для каждого сказать,
   где в новом деке он исполнен. **Список неисполненных ПУСТ** — или объяснён в отчёте;
2. сверить старый `content/` с новым: **список пропавших утверждений и формул ПУСТ**
   (осознанно выкинутое перечисляется отдельно, оно в список потерь не идёт);
3. пересчитать перебором все числа, попавшие на слайды.

*Приём куплен ценой: в прошлом заходе исполнитель молча выкинул выкладку-раскрутку формулы,
владелец обнаружил пропажу только на живой лекции.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/ _studio/zhurnal/2026-07-27_paskal-lekcia-sborka/kod_dek-paskal-v2.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/src/ _studio/zhurnal/2026-07-27_paskal-lekcia-sborka/kod_dek-paskal-v2.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 0. Что уже сделано до плана (только чтение + точка отката)

- Ветка `zahod/dek-paskal-v2` **существовала** и держалась чистым worktree
  `materials-wt/dek-paskal-v2` — `git checkout` из основной папки был невозможен.
  Worktree снят (`git_zona.py worktree drop dek-paskal-v2`, он был чист, 0 изменений),
  ветка взята в основной папке, как требует контракт зоны. Подробно — в `## УРОКИ ФАБРИКЕ`.
- Зона в HEAD была грязна (3 правленых + 20 неотслеженных путей: весь `content/`,
  `slides/`, `illustrations/`, `dist/`, `math/`, `tools/`). Точка отката снята:
  коммит **`b99bfef`** «dek-paskal-v2: точка отката — дек v1 как он был на живой лекции 27.07».
- Прочитано ровно 10 якорей §0 и ничего сверх них.

### 1. Проверенные предпосылки (по живым файлам, до работы)

| предпосылка | статус |
|---|---|
| `build_deck.py` собирает дек за секунды | ✅ 15 слайдов, 885 КБ, линтер зелёный (2 мягких warn: `pascal-both`, `title-art` не используются) |
| `audit.py` — штатный гейт, гоняется здесь | ⚠ падал: `p.chromium.launch()` без браузера. Поставил `playwright install chromium` (окружение, не репозиторий). Теперь идёт |
| базовый прогон `audit.py` зелёный | ❌ **3 FAIL: `sl-columns`, `sl-squares`, `sl-reflect` — `overflow zone copy`**. То есть переполнения текста есть уже сейчас, и это часть моей работы, а не регресс |
| `render.py` снимает слайды пачкой | ⚠ **снимает НЕ то**: рецепт `?only=N&scene=99` кладёт слайду класс `.scene-99`, которого нет ни в каскаде `base.css` (до `.scene-9`), ни в блюр-каскаде `shablon.html`. Итог — весь материал под `data-scene-from` невидим: 10 слайдов из 15 снимаются с ПУСТОЙ доской. Проверено попарно (`?only=2&scene=99` против `?only=2`). Урок с ценой — в `## УРОКИ ФАБРИКЕ` |
| движок держит любое число сцен | ❌ **потолок 9**: `applyScene` снимает классы циклом `for (i=1; i<=9; i++)`, каскады в `base.css`/`shablon.html` кончаются на `.scene-9`. Держусь ≤9 сцен на слайд; `engine.js` — канон, не правлю |
| `data-scenes` сейчас меньше числа сцен (баг эфира) | ✅ **уже не так**: пересчитал все 15 слайдов, `data-scenes` ≥ max({@N}, blur@N) везде. Значит правка внесена после эфира и жива в правленых `base.css`/`shablon.html`. Гейт всё равно пишу — он должен краснеть, а не «я проверил» |
| «исходная версия, отправленная в Claude Code» (§5C) существует | ✅ `raskadrovka/teksty/blok-1.md` и `blok-2.md`. Раскрутка формулы через дежурных/капитана и цепочка `1 → 5 → 10 → 10 → 5` с множителями там ЕСТЬ (blok-1 §«Один объект, много обликов»). Возвращаю оттуда |

### 2. Спор с формулировками (оспариваю ДО работы, §1 захода)

1. **`render-identity` из Этапа 4 неприменим и должен быть снят.** Гейт сверяет
   пиксели новой сборки со старой; операция по определению лоссовая — слайды
   разбиваются, тексты переписываются, рисунки заменяются. Зелёный `render-identity`
   здесь означал бы, что переделка НЕ сделана. Замена — ровно та, что канон уже назвал
   для greenfield (`src/SPEC.md` §Гейт): **линтер + `audit.py` + глаз по всем слайдам**.
   Плюс мои четыре гейта. Считаю это уточнением критерия, а не его ослаблением.
2. **`audit.py --scene-diff` в части «adds pixels only» неприменим к части слайдов.**
   Инвариант «соседние сцены отличаются только ДОБАВЛЕННЫМИ пикселями» ломается всюду,
   где сцена ЗАМЕНЯЕТ рисунок (`data-scene-until` + `data-scene-from` на одной панели —
   так сделаны S6, S9, S10 уже сейчас) и всюду, где на доске живой канвас. Гоняю
   `--scene-diff` и разбираю его вывод, но проваленным считаю только вторую его половину —
   **«сцена не пуста»** (пустой клик = брак, это и есть баг эфира с кликером).
3. Критерий в остальном принимаю как есть, включая 20–22 слайда и 12 вопросов таблицей.

### 3. Структура нового дека — 21 слайд

Слева старый id, справа новый; `NEW` — слайд, которого не было.

| # | id | что несёт | откуда в разборе |
|---|---|---|---|
| 1 | `sl-title` | обложка | — |
| 2 | `sl-cliff` | пьяница на пожарной лестнице, **100 шагов**; соглашение орёл=вверх; **канвас в две фазы** | §2, §16.1, §17.1, §17.3, §17.7 |
| 3 | `sl-columns` | где может оказаться за 5 и за 10 шагов; верхний ярус точками, **канвас заполнения** | §3, вопросы 1–2 |
| 4 | `sl-cells` **NEW** | нижний ярус с вероятностями + правило соседей **на живой таблице** | §3 (разрез S3 по границе `@4`, разрешён `PRAVKI`), вопрос 3 |
| 5 | `sl-pascal` | ×2^шагов, почему это Паскаль, разрез по последнему шагу с показом разреза | §4 |
| 6 | `sl-views` | три облика + **канвас монетки** | §5A |
| 7 | `sl-tri` **NEW** | большой треугольник: симметрия на строке, суммы 1,2,4,8,16 | §5B, вопрос 4 |
| 8 | `sl-team` **NEW** | 25 школьников, три способа счёта → раскрутка формулы по диагонали и по строке | §5C, §16.4, §17.6, вопрос 5 |
| 9 | `sl-cut-middle` | «сколькими способами за 100 шагов попасть в ноль?» → разрез по центру, пара путей | §6 |
| 10 | `sl-pairs` **NEW** | два игрока по 10 монет (вопрос 7) + утверждение про пару траекторий | §6 |
| 11 | `sl-squares` | **канвас без текста**: щелчок по строке → сумма квадратов → середина строки 2n | §7, §16.6, вопрос 6 |
| 12 | `sl-ban` | 10 бросков, выписываем слова 2/3/5 списком + дерево | §8A, §17.4, вопрос 8 |
| 13 | `sl-fib` **NEW** | $F_{n+1}=F_n+F_{n-1}$, биекция на $F_4$: 3+5=8, ряд до 144 | §8B, §16.5, §17.4 |
| 14 | `sl-domiki` | конструкция на конкретном слове; **исправленный рисунок** | §9.0, §9A, вопрос 9 |
| 15 | `sl-diagonals` | применение, восходящие диагонали, сумма явно | §9B |
| 16 | `sl-cliff-return` | перебор с $2n=0$; 1, 2, 6, 20, 70; узнавание | §10, вопрос 10 |
| 17 | `sl-cut4` | язык числа орлов: $k$ от 5 до 10; считаем плохие; **общий плохой путь** | §11.1–11.4, §16.7 |
| 18 | `sl-reflect` | отражение, **число орлов меняется на единицу** | §11.5–11.7, §16.9, вопрос 11 |
| 19 | `sl-formula` **NEW** | от конкретного: хороших длины 10 с 5 орлами $=\binom{10}5-\binom{10}6$ | §12 |
| 20 | `sl-telescope` | выкладка по шагам: шесть первых слагаемых, две последние скобки, 252 | §13, §16.8, §17.3, вопрос 12 |
| 21 | `sl-answer` | ответ от 10 к 100; нечётная длина — одной строкой | §14, §16.2, §17.2 |
| — | `sl-course` | Серпинский + курс (остаётся) | — |

Итого 22 секции = 21 содержательный + обложка. В коридор 20–22 попадает.

### 4. Порядок работ (этапы захода; при стопе режется с хвоста)

**Э0. Инструменты — ДО содержательной работы (§1а).** Всё в `src/tools/`:
- `gen_ill.py` — параметрический генератор SVG: ломаная по списку шагов ±1 (с буквами О/Р
  снизу), треугольник Паскаля на N строк (числа/дроби/подсветка клетки/диагонали),
  ряд узлов, решётка-маршрут, стрелка, дерево слов. Один общий, как требует §1а.
- `render_all.py` — пачечный рендер всех слайдов В ФИНАЛЬНОЙ сцене (без `scene=99`)
  + пер-сценные кадры + контактный лист. Это замена сломанному рецепту `render.py`.
- `gates.py` — четыре гейта §1а.4 одним файлом, exit 1 при красном:
  (1) `data-scenes` ≥ max({@N}, blur@N, data-scene-from/until в `slides/`);
  (2) каскады `base.css` и `shablon.html` покрывают все сцены дека;
  (3) в `dist/index.html` нет буквальных `{@`, `{blur@`, `{fill@`;
  (4) каждый из 12 вопросов бота имеет слайд и сцену (карта вопрос→слайд→сцена — данными
  в `src/VOPROSY-karta.md`, гейт сверяет её с живыми `content/*.md`).
- `check_numbers.py` — перебор всех чисел, попадающих на слайды.

**Э1. Тексты и раскадровка.** `content/*.md` + `slides/*.html` + `brief.md`.
Сквозные §1 (числа вместо букв, О/Р на рёбрах, ноль $\sum$, ножницы без «разрез №N»,
дроби TeX-ом) и точечные §2–14. Блюры по карте вопросов.

**Э2. Рисунки.** Сначала три ошибочных (§Этап 2 захода: домики, плохой путь, соседи),
потом остальные по §2–14. Названные владельцем «хорошие» — не трогаю.

**Э3. Канвасы.** `sims/lab.core.js` (свой тонкий core по устройству buffon: реестр,
активация при показе слайда, `onScene`, `settle`, кнопки-глифы) + `sims/ext/*.js`.
Порядок: **S2 (обязателен) → S3 (обязателен)** → сумма квадратов → монетка →
биекция Фибоначчи → остальное по силам.

**Э4. Сборка и гейты.** `build_math.py` → `build_deck.py` → линтер → `audit.py`
(+`--scene-diff`) → мои 4 гейта → `check_numbers.py` → **глаз по всем слайдам**
двумя видами (масштабированный и экспортный кадр).

**Э5. Верификатор** — свежий субагент, программно, по §3 захода.

**Э6. Коммит зоны** (§4), отчёт, уроки.

### 5. Решения, которые принимаю сам (не блокирую работу)

- **Лестница вертикальная** (§17.1 уже решил; §18.1 — остаток). Фаза 1 — вертикальная
  лестница с ходящей точкой; фаза 2 — из неё вырастает ось времени вправо и график.
- **Порядок в S5C:** сначала заполнение **по диагонали** (проще), потом **по строке** —
  и уже на строчном заполнении довожу до общей формулы. Основание: §17.6 прямо говорит
  «на горизонтальном заполнении доводим до общей формулы» ⇒ строка идёт последней.
- **Нечётный случай** (§18.2) — одной строкой-замечанием на финальном слайде, без вывода,
  как и предложено в §18. Формула $\binom{2n+1}{n}$, проверена перебором.
- Рабочее число второй половины — 10 (§17.5); 100 живёт в постановке и в финале.

### 6. Субагенты

≤5 и только на рисунках Э2 (спека одна, генератор один общий) и на верификаторе Э5.
Тексты, канвасы и гейты — сам: там решения, а не исполнение.

## ВОПРОСЫ — (заполняет исполнитель)

**1. Готовая строка регистрации в `_studio/docs/KARTA.md §6` — за аналитиком (контракт
зоны запрещает мне править `_studio/docs/`).** Вставить в перечень документов арки
`2026-07-27_paskal-lekcia-sborka/`, рядом с `kod_dek-paskal.md`:

> `kod_dek-paskal-v2.md` (**переделка дека по разбору живой лекции 27.07: 15 слайдов → 22,
> вся вторая половина переведена на язык числа орлов, шесть новых слайдов, шесть канвасов
> вместо статики. Модель Opus, дедлайна нет — метрика качество. Ветка `zahod/dek-paskal-v2`**)

**2. 🔴 В РАЗБОРЕ §4 ВОПРОС, НЕВОЗМОЖНЫЙ ПО ЧЁТНОСТИ — я его поправил, нужно ваше «да».**
§4 предлагает спросить: «Сколькими способами можно попасть в точку 3 за 8 шагов?» Перебор
даёт **ноль**: за восемь шагов достижимы только чётные точки — ровно тот факт, который
лекция доказывает слайдом раньше (вопрос 1 бота). Поставил **«в точку 2 за 8 шагов»**,
ответ 56; число шагов ваше, поменялась только точка. Гейт `check_numbers.py` держит оба
утверждения, чтобы это не вернулось.

**3. Что я ВЫКИНУЛ из содержания — это изменение содержания, поэтому сюда, а не молча.**
Всё — из-за объёма слайда, не по решению разбора:
- **тождество Вандермонда** («а если резать не пополам — в листок», `blok-1`, S7);
- **вывод суммы квадратов через многочлен** $(1+x)^{2n}=[(1+x)^n]^2$ (`blok-1`, S7) —
  на слайде остался только комбинаторный вывод через пару путей;
- **третий способ счёта дежурных** ($25\cdot\binom{24}3$, §16.4 п. 2) — оставил два из трёх;
  на треугольнике показаны обе связи (по строке и по диагонали), как требует §17.6;
- **четвёртый облик** («выбор $k$ мест из $n$») перестал быть пунктом списка и стал
  отдельной фразой про $\binom nk$ на том же слайде.
Скажите, что из этого вернуть, — заведу под это отдельный слайд, место есть.

**4. §18.2 — нечётный случай.** Сделал, как вы предложили: одной строкой-замечанием на
финальном слайде, без вывода. Формула $\binom{2n+1}n$, проверена перебором до $L=12$.

**5. §18.3 — порядок в S5C.** Сначала заполнение по диагонали, потом по строке, и уже
на строчном доводим до формулы. Основание — §17.6 («на горизонтальном заполнении доводим
до общей формулы»), то есть строка обязана идти последней.

**6. Хронометраж я НЕ измерял и не берусь.** Слайдов 22 и на каждом теперь разобранный
пример, а не тезис, — но во сколько это выльется, показывает только живой прогон.

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
