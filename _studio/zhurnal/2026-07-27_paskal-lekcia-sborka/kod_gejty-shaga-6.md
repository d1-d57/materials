# Канал исполнителя — gejty-shaga-6 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — трогаешь трекер, который судит коммиты троих писателей: калибровку проверок выбираешь по ходу, а ложное срабатывание блокирует здоровые коммиты всем.

КОНТЕКСТ. Монорепо `materials/`, фабрика `_studio` печёт научпоп-презентации 10-шаговым конвейером. **27.07 конвейер перестроен** (Cowork, коммит уже в ветке): шаг 6 «Плакатный текст» переехал из Фазы II в Фазу I и стал **предвёрсточным документом** — тексты всех слайдов лентой в `<лекция>/raskadrovka/teksty/*.md` + собранный `view.html`, из них порождаются `src/content/<id>.md`. Гейт G7 переписан под это; заведены новые G12–G15 и хук H6.

**ЦЕЛЬ: закрыть три долга, которые эта перестройка оставила ЯВНО.** Все три — «правило записано, а покраснеть нечему» (`KONSTITUCIYA §11`), и до их закрытия новые гейты — не гейты, а надежды:
1. **G7 врёт.** Его признак-на-диске переписан на ленту (`raskadrovka/teksty/`), а трекер `sostoyanie.py` по-прежнему ищет `src/content/*.md`.
2. **G12–G15 и H6 не существуют в трекере** — только текстом в `GEJTY.md`.
3. **`FORMAT-ISTOCHNIKA.md` не знает про ленту** — там `src/content/*.md` описан как место авторской правки, хотя теперь это порождаемый слой.

Приёмка — по ОТЧЁТУ, без построчной сверки. **Стоп до цели:** сделай сначала (1) и (3) — они дешёвые и разблокируют трекер; (2) можно сдать частично, но тогда назови в отчёте, какие из G12–G15/H6 реализованы, а какие нет, поимённо. Молчаливый недобор — брак.

**Времени не жалко, токенов — да.** Работай хоть всю ночь, но: читай только якоря §0, не перечитывай документы целиком, проверки делай грепом и прогоном скрипта, правки — точечные. Один верификатор, не рой субагентов.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `zahod/dek-paskal-v2` в основной папке репозитория — `git checkout zahod/dek-paskal-v2` первым ходом. Ветку НЕ переключай дальше, в другие НЕ коммить.
- **ЗОНА (можно менять):** _generator/tools/sostoyanie.py _generator/tools/fixtures/sostoyanie _studio/konvejer/FORMAT-ISTOCHNIKA.md _studio/konvejer/GEJTY.md _studio/konvejer/KATALOG.md _studio/konvejer/06-tekst. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВОДИШЬ НОВЫЙ `.md` — РЕГИСТРИРУЕТ АНАЛИТИК, НЕ ТЫ.** Корень требует строку в `_studio/docs/KARTA.md §6` тем же ходом, а `_studio/docs/` тебе READ-ONLY — два обязательных указания противоречат друг другу, и приоритет теперь назван: **контракт зоны сильнее**. Твоя обязанность — положить ГОТОВУЮ строку регистрации первым пунктом `## ВОПРОСЫ`, чтобы аналитику остался один ход. *Почему в шаблоне, а не в каноне: этот урок записан 26.07 с ценой в пять документов-сирот и через два дня повторился дословно — правило жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout zahod/dek-paskal-v2` — **это, скорее всего, уже текущая ветка**; если да, checkout ничего не делает и это норма. Ветку не создавай и не переключай дальше.
- Точка отката: `git add _generator/tools/sostoyanie.py _generator/tools/fixtures/sostoyanie _studio/konvejer/FORMAT-ISTOCHNIKA.md _studio/konvejer/GEJTY.md _studio/konvejer/KATALOG.md _studio/konvejer/06-tekst` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти якоря, ничего сверх. Проект не изучай.
  1. `_studio/konvejer/GEJTY.md` — реестр гейтов. **Твой главный вход:** там дословно записаны G7 (переписанный), G12, G13, G14, G15, H6 с полем «признак-на-диске» — именно его ты реализуешь в коде. Не изобретай проверки заново, бери формулировки оттуда.
  2. `_generator/tools/sostoyanie.py` — трекер, который ты правишь (694 строки; читай целиком, это твой предмет).
  3. `_studio/konvejer/FORMAT-ISTOCHNIKA.md` — формат источника, третий долг.
  4. `_studio/konvejer/06-tekst/DOK.md` — контракт шага 6 (откуда берутся места файлов и что такое лента).
  5. `_studio/konvejer/06-tekst/ZAHOD.md` — помечен ⛔ УСТАРЕЛ, ты его переписываешь (задача 4).
  6. **Живой образец ленты** — `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/teksty/` (`blok-1.md`, `blok-2.md`, `view.html`). Это единственный существующий предвёрсточный документ; на нём G7 обязан быть ЗЕЛЁНЫМ. Читать `blok-1.md` (26 КБ) достаточно, второй — по нужде.
  7. `_studio/docs/kak-delat/GIT-disciplina.md` §3 — только если коммит пойдёт не так.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

### 2.1. Починить G7 в трекере (первым — он сейчас врёт)
`sostoyanie.py` проверяет G7 по `src/content/<id>.md`. Переписать под новый признак из `GEJTY.md`: лента `<лекция>/raskadrovka/teksty/*.md` (≥1 файл с фронтматтером `tab:`) · число разделов `^## ` по всем файлам ленты == длине `slide_order` · `view.html` существует и НЕ старше самого свежего `*.md` рядом · в каждом разделе есть `поле:mn **Раскладка.**` · ≤1 акцент-блок `**…**` на раздел · слова раздела в `word_budget_per_slide`±допуск.
⚠ **Ленты может не быть вовсе** (старые деки: `fibonacci`, `buffon`, `dandelin`). Это не FAIL — это «шаг 6 шёл по старой схеме». Реши, как отличать, и **назови решение в `## ПЛАН` до кода**: молчаливый выбор здесь — ровно тот класс ошибки, что даёт сотни ложных ❌ и обесценивает красный цвет.

### 2.2. Реализовать G12–G15 и H6
Признаки-на-диске выписаны в `GEJTY.md` дословно. Все статические, без браузера. Коротко:
- **G12** — обложка и финал не перегружены (≤25 слов, ≤1 `data-ill`, нет сцен).
- **G13** — иллюстрация-ответ не раньше своей сцены (`data-scene="N"` бьётся с `{@N}`/`{blur@N`; есть `{blur@`, а у `data-ill` нет `data-scene` → FAIL).
- **G14** — `data-scenes="N"` == max по маркерам сцен в тексте.
- **G15** — каскад `.scene-1 … .scene-N` покрывает всё без дыр.
- **H6** (WARN, не блокирует) — величина, обязанная совпадать с другой, выписана руками: `data-scenes="\d+"` и рукописные `.scene-\d+` в CSS.

🔴 **Главный риск — ложные срабатывания.** Трекер судит коммиты троих писателей; гейт, который краснеет на здоровом деке, будет обойден `--no-verify`, и тогда пропадёт ВСЯ защита, а не одна проверка. Поэтому: прогони новые проверки на **всех живых деках репозитория** и в отчёте назови по каждому, сколько ❌ и являются ли они настоящими. Настоящий долг — оставляй красным и пиши в отчёт; ложное срабатывание — чини калибровкой.

### 2.3. Привести `FORMAT-ISTOCHNIKA.md` к ленте
Описать `raskadrovka/teksty/<блок>.md` + `view.html` как место шага 6, а `src/content/<id>.md` — как **порождаемый** слой Фазы II, не место авторской правки. Дописать в схему `brief.md` поля `register:` и `word_budget_per_slide:` — `GEJTY.md` их требует с G5, а в схеме их нет (известный долг, зафиксирован в `zhurnal/2026-07-10_orkestracia/kod_treker.md` п.7). Правка точечная, файл не переписывать.

### 2.4. Переписать `06-tekst/ZAHOD.md`
Сейчас он помечен ⛔ УСТАРЕЛ: запускал шаг 6 как автономный прогон Claude Code со старым гейтом. Шаг 6 теперь интерактивный, в Cowork с владельцем — автономного захода на него быть не должно. Оставь заход **только на механическую часть**: породить `src/content/<id>.md` из уже принятой владельцем ленты (разделы `^## ` → файлы по `slide_order`, снять `<figure>`/`<figcaption>` и `поле:mn`, сохранить шорткаты `{@N}`/`{fill@}`/`{blur@}`/`**acc**`/`$tex$`). Всё про вкус, голос и приёмку — вырезать: это не его работа.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `python3 _generator/tools/sostoyanie.py <лекция>` печатает G7, G12, G13, G14, G15, H6 — все шесть, поимённо, ни одного «нет такой проверки».
2. На **лекции Паскаля** (`lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal`) **G7 зелёный** — там лежит эталонная лента. Красный G7 на ней = проверка написана неверно.
3. У каждой новой проверки есть **фикстура в `_generator/tools/fixtures/sostoyanie/`, на которой она ПАДАЕТ**, и `PROGNAT.sh`, который это показывает. Закон фабрики: у гейта, который может пройти, обязана быть фикстура, на которой он падает — иначе зелёный ничего не доказывает.
4. Прогон по всем живым декам репозитория сделан, и в отчёте по каждому названо число ❌ с разбором «настоящее / ложное».
5. `grep -n 'src/content' _studio/konvejer/FORMAT-ISTOCHNIKA.md` — каждое оставшееся вхождение стоит в контексте «порождаемый слой», не «место правки».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Операция не лоссовая — файлы ты не двигаешь и не жмёшь. Но **один свежий субагент нужен, и на другое:** дать ему `GEJTY.md` (только записи G7, G12–G15, H6) и твой готовый код, и попросить найти, **на каком здоровом входе новая проверка покраснеет зря**. Это ровно тот отказ, который дороже всего стоит. Его вердикт — в отчёт с охватом («проверено N проверок из 6»).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/sostoyanie.py _generator/tools/fixtures/sostoyanie _studio/konvejer/FORMAT-ISTOCHNIKA.md _studio/konvejer/GEJTY.md _studio/konvejer/KATALOG.md _studio/konvejer/06-tekst                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/sostoyanie.py _generator/tools/fixtures/sostoyanie _studio/konvejer/FORMAT-ISTOCHNIKA.md _studio/konvejer/GEJTY.md _studio/konvejer/KATALOG.md _studio/konvejer/06-tekst   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
