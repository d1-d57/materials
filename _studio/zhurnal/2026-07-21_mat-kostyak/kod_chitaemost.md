# Канал исполнителя — chitaemost (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — работа А требует решать по месту, что читателю мешает, а что нет; расписать это заранее нельзя, можно только назвать критерий.

КОНТЕКСТ. Короткий курс «Введение в теорию категорий» (три лекции, шесть часов, потолок — декартово замкнутые категории, финал — Карри–Ховард–Ламбек). Документ `teorkat-vvedenie/MAT-KOSTYAK.md` — **внутренняя карта курса**: 34 понятия, 51 утверждение. Он собран, замкнут по восьми учебникам, решения владельца внесены, состав больше не меняется.

**Осталась одна вещь, и она — цель всей арки: владелец должен сесть, прочитать документ подряд и понять, что он рассказывает в курсе.** Сейчас не может. Прошлый заход добавил связки между группами — это помогло, но диагноз был неполон: **читателя выбрасывает раньше, на служебной прозе документа.** Это не догадка, а результат независимого верификатора, которому дали только собранный вид и спросили, где текст перестаёт быть понятным.

ЦЕЛЬ: убрать то, обо что читатель спотыкается, **не трогая состав и математическое содержание.** Плюс шесть точечных правок, которые владелец утвердил.

Приёмка — по ОТЧЁТУ, без построчной сверки. **Стоп-правило:** работа А целиком важнее работ Б и В. Сделал А и половину Б — заяви громко и остановись; недоделанная А обесценивает весь заход, потому что ради неё он и запускается.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **ВЕТКА:** работай ТОЛЬКО на `arka/mat-kostyak` — `git checkout arka/mat-kostyak` первым ходом. Ветку НЕ переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/view.html. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am` и никогда `commit` без путей: репо `materials/` общий, писателей трое, и `commit` без путей забирает чужое из индекса. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца (цена: три сорванных коммита за сессию 21.07).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout arka/mat-kostyak`. Точка отката: `git add teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/view.html` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО:
  1. `teorkat-vvedenie/MAT-KOSTYAK.md` — **твоя зона, читать целиком.** Особенно `## Как считать этот документ` и `## Соглашения документа`: они и есть главный предмет работы А.
  2. `_studio/zhurnal/2026-07-21_mat-kostyak/kod_resheniya.md` — READ-ONLY, **только секцию `### Результат верификатора §3(б)`**. Там дословный отчёт независимого читателя: перечень мест, где текст ломается, с адресами. **Это твоё техзадание на работу А — не пересказ, а список.** Читать целиком его не надо, остальные секции тебе не нужны.
  3. Больше ничего. PDF не открывай, источники не перепроверяй, другие заходы не читай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата печатай ПЕРВЫМ, до содержательного вывода.** «Команда отработала» и «команда упала, а я читаю прошлое состояние» выглядят одинаково. *Цена 21.07: аналитик четырежды прочитал `rc=128` (сбой прав окружения) как содержательный результат и едва не откатил по нему верное правило.* Гейты — так же: сначала код возврата, потом выводы.

Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

🔴 **Сквозной запрет, сильнее всех предписаний ниже: состав и математика НЕ МЕНЯЮТСЯ.** Ни одного утверждения не добавить и не удалить, ни одного номера не сдвинуть, ни одной формулировки утверждения не переписать сверх названного в работе Б. Ты правишь **обрамление**, а не содержание. Гейты состава в §2.5 это ловят.

### 2.1 Работа А — убрать то, обо что спотыкается читатель (ГЛАВНАЯ)

**Твоё техзадание — не мой пересказ, а список из `kod_resheniya.md`, секция `### Результат верификатора §3(б)`.** Там независимый читатель прошёл документ и назвал места с адресами. Работай по нему; ниже — как именно, и где границы.

**А1. Шапка «Как считать этот документ» отпугивает на первом экране.** Первое, что видит читатель после обещания «здесь словарь и утверждения», — три экрана `awk`/`grep`, отладка сломанной команды и разбор ловушек подстроки. Сигнала «это служебное, математика ниже» нет.
⚠ **Команды удалять НЕЛЬЗЯ:** `KONSTITUCIYA §10` требует держать в документе не число, а команду, которая его печатает, и это правило написано кровью — в этом репозитории вписанное руками число врало пять раз. **Прочитай §10 сам и назови в `## ПЛАН`, что именно она требует.**
**Развилка, выбор твой, назови вслух с обоснованием:** (а) оставить блок на месте, но предварить короткой навигационной врезкой «служебное, можно пропустить, математика с раздела A» и сжать разборы ловушек; (б) увести блок в конец документа отдельным разделом, оставив в шапке две строки и ссылку. Оба варианта законны, если команды остаются в документе и остаются рабочими.

**А2. Служебные слова, которые нигде не введены:** `зона`, `приз`, `рамка`, `сюжетная арка`, `цепочка финал ⇒ CCC ⇒ минимум`. Читатель встречает их как известные. Два законных хода, можно смешивать: объяснить одной строкой в «Соглашениях» — или заменить на человеческие слова там, где термин не несёт нагрузки.
🔴 **Хуже всего формула «финал ⇒ CCC ⇒ минимум»: она повторена ДОСЛОВНО ВОСЕМЬ раз.** Считай сам, не верь числу на слово:
```
grep -c 'финал ⇒ CCC ⇒ минимум' teorkat-vvedenie/MAT-KOSTYAK.md
```
Живые адреса на 21.07: преамбула `A6` · блоки `П29`–`П34` (шесть) · один раз в `У28`. **Сказать один раз в преамбуле A6, в блоках оставить короткую отсылку.** Самая дешёвая и самая заметная правка захода.
*(В первой редакции этого захода стояло «пять раз, П29–П33» — число взято из отчёта прошлого исполнителя, а не из файла, и оказалось неверным и по количеству, и по составу блоков. Проверяй адреса грепом: в этой арке два предписания, написанных по отчёту, ушли в пустоту.)*

**А3. Редакционная археология внутри математического текста.** «Прежняя редакция писала…», «первая редакция закрывала флагом — это было ложью», «поймано верификатором захода `kod_zamykanie`», «условие владельца изменилось». Читатель первый раз открыл документ, а ему рассказывают, чем он отличается от версии, которой тот не видел.
⚠ **УДАЛЯТЬ НЕЛЬЗЯ, ТОЛЬКО ПЕРЕСЕЛЯТЬ.** Эти следы несут «почему так решено» и **даты данных** — по правилу репозитория (Р34) отметка без даты данных доверия не заслуживает, и терять их запрещено. Ход: перенести в раздел E (или в новый раздел «История решений» в конце, если E распухнет), в теле блока оставив, где нужно, одну короткую фразу без имён заходов и без спора с невидимым оппонентом. **Ни одна дата данных не должна пропасть — это проверяемо грепом, вставь счёт до/после в отчёт.**

**А4. Шкалы не названы.** Поле `уровень` печатает «школьно» / «требует-приручения» / «вне-школы», а что это за лестница и сколько на ней ступеней — не сказано (единственное объяснение «по лестнице формата матбазы» ничего не объясняет). Опиши шкалу одним абзацем в «Соглашениях».
*Отдельно, НЕ трогать:* три поля печатаются в вёрстке голыми словами без имён («мотивируемое / тривиально / безусловно») — это движок, он вне твоей зоны. В отчёт строкой, не чинить.

**Мерка для всей работы А, по ней же себя и проверяй:** после правки человек, который знает математику и не знает теории категорий, должен читать подряд и **не встречать слов, за которыми надо идти в другой документ**. Всё, что объясняет документ сам себе или спорит с прошлыми своими редакциями, — не для него.

### 2.2 Работа Б — шесть точечных правок, все утверждены владельцем

1. **У25 (Карри–Ховард–Ламбек) — вписать след решения.** Владелец решил оставить его в мотивируемом, но в документе этого решения нет нигде, и сильный довод против («формулировка называет $\lambda$-исчисление и логику, а это внешний материал») через месяц вернётся. **Прецедент бери в документе:** у У22 стоит абзац «Почему в мотивации, хотя утверждение важное» — ровно затем, чтобы решение не пересматривали дважды. Сделай такой же по форме, с датой данных `2026-07-21`.
2. **Связка B0 — слить с существовавшим вводным абзацем группы.** Они частично про одно (моя связка: «этим держится язык остального документа»; существующий: «этим молча пользуется весь дальнейший список»). ⚠ В слитом абзаце **сохрани зачин `**Зачем …**`** — по нему считает гейт §2.5.
3. **Пункт E11 — ничего не делать.** Владелец подтвердил: пометка «числа исторические» верна, переписывать чужую отчётную запись не надо. Пункт в `## ПЛАН`: подтвердить, что не тронул.
4. **У10 — починить порядковые числительные в доказательстве.** В формулировке законы идут (1) $(A^B)^C$, (2) $A^1$, (3) $1^A$, (4) $(A\times B)^C$, (5) $A^{B+C}$, (6) $A^0$; в доказательстве «четвёртый» — на самом деле шестой, «пятый» — первый, «шестой» — пятый. Приведи в соответствие. **Математику не трогай, только нумерацию ссылок.**
5. **У51 — доказательство не закрывается.** «Два разбиения сравниваются одним применением ассоциативности» — для двух разных точек разреза $i<j$ одного применения мало, нужна ещё индукция. Это **первое** доказательство раздела B, стоящее там как гарант законности всей дальнейшей записи. Допиши недостающий шаг, коротко и в стиле соседних выкладок.
6. **У24 — расцепить «строг и полон».** Сейчас склеены одной скобкой как общее свойство («биективен на каждом множестве морфизмов»), а в У37 разъезжаются и работают порознь («по полноте… и по строгости…»). Читатель по склеенной скобке шаг У37 воспроизвести не может. Дай двум словам раздельные пояснения.

**Плюс мелочь оттуда же:** знак `!` (единственный морфизм в терминальный объект) стоит **в формулировке У17**, а введён мимоходом в скобке внутри доказательства У7; в словаре его нет. Введи его явно — там, где вводится терминальный объект.

### 2.3 Работа В — терминология (только если А и Б закрыты)

Открытый спор документа (раздел E): **«предупорядоченное множество» против «частично упорядоченного»**. Решение владельца, данные на 21.07: *«по умолчанию ч.у.м., смотреть как принято в источниках»*. Аргумент, найденный Ф3, уже лежит в E4 (Лейнстер, Упр. 6.3.23(a), печ. с.167: всякое предупорядоченное множество эквивалентно как категория упорядоченному).

Сверь употребление по документу и приведи к одному термину — тому, который принят в русской математической традиции. **Если не успеваешь — не начинай**: полработы здесь хуже, чем ничего, потому что документ с двумя половинами в разных терминах хуже документа в одном последовательном.

### 2.4 Чего НЕ делать

- Не менять состав: ни одного утверждения/определения добавить, удалить, переномеровать.
- Не переписывать формулировки утверждений сверх шести правок работы Б.
- Не трогать движок и вёрстку (`_generator/` вне зоны) — дефекты вёрстки в отчёт строкой.
- Не заводить глоссарий курса — это отдельное решение владельца.
- Не трогать `SPEKA.md`, картотеку, `VYCHITANO.md`, другие заходы — всё вне зоны.
- Не терять ни одной **даты данных** и ни одного решения владельца при переселении по А3.

### 2.5 КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — прогнать буквально, выводы в отчёт дословно

```
# 1. СОСТАВ НЕ ТРОНУТ — четыре числа, все обязаны совпасть с текущими
grep -cE '^### У[0-9]+\.' teorkat-vvedenie/MAT-KOSTYAK.md          # 51
grep -cE '^> поле:следует-из ' teorkat-vvedenie/MAT-KOSTYAK.md     # 51
grep -cE '^> поле:тип мотивируемое$' teorkat-vvedenie/MAT-KOSTYAK.md  # 43
grep -cE '^> поле:тип мотивация$' teorkat-vvedenie/MAT-KOSTYAK.md     # 8

# 2. ГЛАВНЫЙ ИТОГ — обязан остаться 5 (команда из шапки документа, взять оттуда дословно)

# 3. СВЯЗКИ на месте: 14 маркеров (после слияния B0 их по-прежнему 14, а не 13)
grep -c '^\*\*Зачем ' teorkat-vvedenie/MAT-KOSTYAK.md

# 4. ДАТЫ ДАННЫХ не потеряны при переселении — счёт ДО и ПОСЛЕ, оба в отчёт
grep -c 'данные на 2026-07-21\|данных 2026-07-21' teorkat-vvedenie/MAT-KOSTYAK.md

# 5. движок (путь БЕЗ .md — движок берёт папку; форма с .md печатает ложный красный)
python3 _generator/build_doc.py teorkat-vvedenie/MAT-KOSTYAK
python3 _generator/tools/check_view.py teorkat-vvedenie/MAT-KOSTYAK
```

**🔴 6. Главный гейт захода — читательский, и он МОЖЕТ провалиться.** См. §3(б): свежий субагент проходит документ заново тем же вопросом. **Пункты прошлого списка разломов не должны воспроизвестись.** Воспроизвёлся хоть один из названных в А1–А4 — работа А не сделана, как бы хорошо ни выглядели правки. Новые пункты, которых в прошлом списке не было, — нормально и ожидаемо: это следующая итерация, а не провал.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «служебных слов не осталось», а «не осталось, проверено грепом, было X вхождений, стало Y». Счёт ДО работы сними ПЕРВЫМ ходом и вставь в `## ПЛАН` — после правок исходное число уже не восстановить:
```
for w in 'финал ⇒ CCC ⇒ минимум' 'Прежняя редакция' 'Первая редакция' 'kod_zamykanie'; do
  printf '%s: ' "$w"; grep -c "$w" teorkat-vvedenie/MAT-KOSTYAK.md; done
```

### 2.6 🔴 Боковое оглавление собранного вида — ПРОВЕРИТЬ ГЛАЗАМИ, гейты этого не ловят

Заголовок без примыкающей пустой строки перестаёт быть заголовком и молча уезжает в предыдущий абзац; `build_doc` и `check_view` при этом **зелёные**. В этой арке так уже теряли два раздела из семи. Ты двигаешь целые куски текста между разделами (А1, А3) — то есть работаешь ровно там, где эта ловушка живёт. Извлеки оглавление из собранного `view.html`, прочитай глазами и **вставь список в отчёт**: восемь заголовков `##` и все тринадцать групп на месте.

## 3. ВЕРИФИКАТОР — оба обязательны, операция сильно лоссовая

Ты переселяешь куски текста между разделами. Это класс, где потери не видны: текст «где-то есть», пока не спросишь конкретно.

**(а) Программно, против `git show HEAD`.** Разбери оба файла на блоки и сверь. Обязано выполниться: ни одно утверждение и определение не исчезло, номера не сменились, состав совпал. Отдельно — **список потерянных дат данных и решений владельца обязан быть ПУСТ**: собери из HEAD все строки с «данные на» / «решение владельца» и проверь, что каждая присутствует в новой версии (пусть и в другом разделе). Вывод скрипта — в отчёт дословно, с охватом («сверено N блоков из N»).

**(б) 🔴 Читательский — главный гейт захода.** Свежий субагент, ему дать **только собранный `view.html`** — без этого захода, без списка твоих правок, без прошлого отчёта. Вопрос тот же, что в прошлый раз: *«прочти подряд разделы A и B и скажи: в каком месте текст перестаёт быть понятным человеку, который знает математику, но теорию категорий не знает? назови конкретные места, не общее впечатление»*.

**Потом — и только потом — сам сравни его ответ с прошлым списком** (`kod_resheniya.md`, секция `### Результат верификатора §3(б)`) и в отчёте дай таблицу: пункт прошлого списка → воспроизвёлся / не воспроизвёлся. **Воспроизведённый пункт из А1–А4 — это провал работы А, и назвать его обязан ты, а не приёмка.** Ответ субагента вставь целиком, даже если он ругает результат: в прошлый раз именно его нелицеприятный ответ оказался самым ценным в заходе.

⚠ **Субагенту нельзя показывать прошлый список до того, как он даст свой** — иначе он будет искать названное, а не читать.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/view.html
git --no-optional-locks commit -m "маткостяк: читаемость — служебный язык убран, шесть правок владельца" \
    -- teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/view.html
python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, и пропуск любого уже стоил дорого 21.07** (полное «почему» — `RUKOVODSTVO §Коммит зоны`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути. Заведёшь новый файл и коммитнёшь без `add` — получишь `error: pathspec … did not match any file(s) known to git`. *Цена: аналитик убрал `add`, сочтя pathspec самодостаточным, и завалил все десять коммитов планового прогона.*
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое — владелец, Cowork, Claude Code). *Цена: коммит на **89 файлов вместо трёх** — уехали `SPEKA.md`, `KONSTITUCIYA.md`, незавершённая работа четырёх других арок.*
⚠ **Хук `pre-commit` («УРОК БЕЗ ЦЕНЫ») судит staged-пути.** При pathspec-коммите там только твоё, и `--no-verify` тебе не нужен. Хук покраснел — значит в коммит уехало чужое: **не обходи, пересобери коммит.**
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Заход вписал производный факт руками — в том самом абзаце, где предупреждает, что так делать нельзя

§2.1 A2 даёт «живые адреса на 21.07: преамбула `A6` · блоки `П29`–`П34` (шесть) · один раз в `У28`». Число 8 верно, **восьмой адрес — нет**: в `У28` формулы нет ни разу (`grep -c` по блоку → 0), восьмое вхождение живёт в разделе D, «Отвергнуто 5». Абзацем ниже заход сам объясняет, что в его первой редакции стояло «пять раз, П29–П33», что число было взято из отчёта прошлого исполнителя и оказалось неверным «и по количеству, и по составу блоков», и просит проверять грепом.

То есть аналитик знал класс ошибки, назвал его, написал предупреждение — и **в той же строке повторил её**, только тише: счёт починил, состав нет. Предупреждение «проверяй грепом» — это §11 в чистом виде, правило без механизма: нарушить его можно молча. Механизм здесь уже есть в каноне и не применён — `KONSTITUCIYA §10`: список адресов **вычисляется из живого файла**, значит в заходе должна стоять команда, а не выписанный руками перечень. Заход требует §10 от документа и не применяет его к себе.

**ЦЕНА:** три редакции одной строки, две из них с неверным составом адресов; по признанию самого захода, «в этой арке два предписания, написанных по отчёту, ушли в пустоту». В этом заходе я потерял на проверку один греп и абзац в `## ПЛАН` — дёшево только потому, что заход велел не верить себе. Исполнителю, который поверит списку, это стоит неверного охвата в отчёте: он отчитается «8 → 1», правя место, где править нечего, и не тронув то, где вхождение есть.

### Критерий готовности привязан к МЕСТУ, которое задание сам же разрешает изменить

§2.5 п.2: «ГЛАВНЫЙ ИТОГ — обязан остаться 5 (команда **из шапки документа**, взять оттуда дословно)». §2.6: «восемь заголовков `##`». Обе строки описывают расположение и число — а §2.1 A1 предлагает развилку, ветка (б) которой **уводит блок команд из шапки в конец** и (через A3) заводит новый раздел. Выбираешь ветку, разрешённую заданием, — и два пункта критерия перестают описывать здоровый документ: команды не в шапке, заголовков девять.

Критерий от этого не краснеет ложно (я прогнал его буквально, все числа сошлись), но перестаёт быть машинным: чтобы его прочесть, надо знать, что сдвиг санкционирован. Гейт, который надо интерпретировать, — уже не гейт.

**ЦЕНА:** решение, которое должен был принять автор захода, пришлось принимать мне — в `## ПЛАН` пришлось отдельно объявлять поправку к критерию и обосновывать, почему «восемь» читается как «все восемь прежних на месте», а не как «ровно восемь». Приёмка гоняет критерий первым ходом и без этого абзаца получила бы расхождение на здоровом документе. Починка на будущее — та же, что в уроке выше: пункт критерия должен звать содержимое **командой или якорем** (`grep -c '^## '`, «команда главного итога, где бы она ни лежала»), а не адресом «в шапке».

## ПЛАН — (заполняет исполнитель)

### 0. Счёт ДО — снят первым ходом, rc=0 по всем командам

```
финал ⇒ CCC ⇒ минимум: 8      Прежняя редакция: 2
Первая редакция: 2            kod_zamykanie: 3
```
Состав на входе (совпадает с ожидаемым в §2.5): У-блоков 51 · следует-из 51 · мотивируемое 43 · мотивация 8 · маркеров `**Зачем ` 14 · дат данных 12 · главный итог **5** · заголовков `##` восемь. Файл 1774 строки.

### 🔴 Ложная предпосылка захода — говорю ДО работы (§2.1 A2)

Заход называет живые адреса формулы: «преамбула `A6` · блоки `П29`–`П34` (шесть) · **один раз в `У28`**». Число 8 верное, **адрес восьмого вхождения — нет.** Греп:

```
488 (преамбула A6) · 498 П29 · 512 П30 · 528 П31 · 542 П32 · 556 П33 · 570 П34 · 1746 раздел D, «Отвергнуто 5»
sed -n '1614,1633p' … | grep -c 'финал ⇒ CCC ⇒ минимум'   →  0
```
В `У28` (теорема Кэли) формулы нет ни разу. Это **третья подряд** ошибка адреса в этой строке захода — заход сам предупреждает, что в первой редакции стояло «пять раз, П29–П33», и просит проверять грепом; проверил, промах остался, только переехал. В `## УРОКИ ФАБРИКЕ` вынесу с ценой.

Следствие для работы: правлю преамбулу A6 + шесть блоков П29–П34. **Вхождение в разделе D не трогаю** — это не часть повтора, а единственное употребление в служебном разделе, где оно самодостаточно («цепочкой … не требуется, а всё, что не требуется, обязано быть названным исключением»). Итог по охвату будет 8 → 2, а не 8 → 1.

### 1. КОНСТИТУЦИЯ §10 — что она требует (прочитал сам, §2.1 A1)

**«Производный факт — либо команда, либо его нет.»** Число, статус или охват, которые *можно вычислить из живых файлов*, руками в документ не пишутся — пишется команда, которая их считает. «Я посчитал и вписал» объявлен браком по определению, **даже когда цифра сегодня верна**. Цена в §10 названа: пять расхождений за трое суток (`MANIFEST` «15 pdf» при 18; `VYCHITANO` «ПУСТОЙ» при 16, следом «19» при 16 — и это была починка предыдущей строки; статус лекций вёлся руками в двух местах → Л4 помечена закрытой при одном «да» из трёх; скопированное представление устарело за час).

⚠ Что §10 **не** требует: чтобы команда стояла в шапке. Она требует, чтобы команда была **в документе** и работала. Обе ветки развилки A1 это условие держат — значит развилка решается читаемостью, а не §10.

### 2. Развилка A1 — выбираю (б), увести блок в конец. Обоснование

Беру **(б)**: `## Как считать этот документ` уезжает целиком и дословно в конец документа, после раздела E; в шапке остаются две строки и ссылка.

- Цель арки — чтобы владелец **сел и прочитал документ подряд**. Вариант (а) вешает табличку «можно пропустить», но три экрана `awk` остаются лежать между обещанием «здесь словарь и утверждения» и первым определением: читающий подряд всё равно идёт через них. (б) убирает их с дороги совсем — это единственная разница, и она ровно про заявленную цель.
- Блок — того же рода, что разделы C, D, E: не математика, а аппарат документа («почему так решено», «что открыто», «как это пересчитать»). Весь аппарат уже живёт в конце; шапка — единственное место, где он стоял в начале. (б) делает структуру честной, а не только терпимой.
- §10 удовлетворён буквально: команды остаются в документе и остаются рабочими (проверю прогоном главного итога после переноса).
- Риск (б) — ровно ловушка §2.6 (заголовок без пустой строки уезжает в абзац). Я и так двигаю куски по A3, то есть этот риск в заходе уже оплачен; проверю оглавление глазами один раз на всё.

Число заголовков `##` от переноса не меняется: было восемь, останется восемь + один новый (см. п. 4).

### 3. Что делаю по работе А

**A2 — служебные слова.** Формула «финал ⇒ CCC ⇒ минимум» объясняется **один раз** в преамбуле A6 (что за финал, что за минимум, почему это цепочка); в шести блоках П29–П34 остаётся короткая отсылка вместо дословного повтора. Слова `зона`, `приз`, `рамка`, `сюжетная арка` получают по строке в «Соглашениях документа» — там же оговариваю, что у слова «зона» в документе два употребления (поле блока и «в зоне курса»), иначе объяснение само станет ловушкой.

**A3 — археология.** Ход по заходу: **не удалять, переселять.** Граница, по которой решаю: археология внутри математического текста (разделы A, B, B′) переезжает; археология внутри разделов D и E **остаётся на месте** — эти разделы по своему заголовку и есть журнал решений, там она дома, и заголовок A3 в заходе говорит именно «внутри математического текста». Поэтому счёт «Первая редакция» уйдёт 2 → 1, а не 2 → 0, и это не потеря.

**A4 — шкала `уровень`.** Абзац в «Соглашениях»: три ступени, `школьно` (32 блока) / `требует-приручения` (17) / `вне-школы` (2), и главное — что шкала отвечает на другой вопрос, чем `вердикт`: вердикт про длину доказательства, уровень про то, что нужно знать читателю. Верификатор споткнулся ровно на паре «У42: тривиально + требует-приручения» — её и разбираю как пример, чтобы противоречие исчезло.
*Не чиню:* три поля печатаются в вёрстке голыми словами без имён — это движок, вне зоны, строкой в отчёт.

### 4. Куда переселяется археология

Завожу **новый раздел `## История решений`** в конце, перед перенесённым «Как считать». Не в раздел E, потому что E — «Открытые вопросы», а переселяемое — **закрытые** решения с датами; сложить закрытое в раздел открытого значит соврать заголовком обоих. Заход эту ветку разрешает прямо («или в новый раздел «История решений» в конце, если E распухнет»); E держит 11 пунктов и уже плотный.

⚠ Поэтому заголовков `##` станет **девять, а не восемь**: восемь прежних все на месте (проверю поимённо в §2.6), девятый — новый и назван здесь. Это добавление, а не потеря; молча подгонять счёт под «восемь» не буду.

**Ни одна дата данных не пропадает** — их 12, каждая переезжает вместе со своим решением. Счёт до/после и пофразную сверку даёт верификатор §3(а).

### 5. Работа Б — шесть правок, все утверждены

1. **У25** — абзац следа решения по форме У22 («Почему в мотивации, хотя утверждение важное»), с датой данных `2026-07-21`. Довод против назову честно, ответ — по границе из «Соглашений»: раздел определяется тем, **о чём** утверждение (о произвольной CCC), а не тем, какие слова в нём звучат.
2. **B0** — слить связку с вводным абзацем группы, зачин `**Зачем ` сохранить (гейт §2.5 п.3 считает по нему; маркеров останется 14).
3. **E11** — не трогаю. Подтверждаю здесь заранее.
4. **У10** — порядковые: «четвёртый» → шестой, «пятый» → первый, «шестой» → пятый; плюс назову явно, какие три закона идут первым абзацем. Математику не трогаю.
5. **У51** — дописать недостающий шаг: после разреза в точке $i$ нужна вторая индукция «$C_i=C_{i+1}$», а одно применение ассоциативности закрывает ровно один такой переход. Коротко, в стиле соседних выкладок.
6. **У24** — расцепить: `строг` = инъективен на множествах морфизмов, `полон` = сюръективен; вместе это прежняя скобка «биективен», но У37 теперь воспроизводится.
7. **Мелочь:** знак `!` вводится явно при терминальном объекте (определение 6), где ему и место.

### 6. КРИТЕРИЙ ГОТОВНОСТИ — принимаю, с одной поправкой

Гейты §2.5 принимаю целиком и прогоню буквально. Поправка одна, названа выше: **пункт §2.6 «восемь заголовков `##`» станет девятью** — из-за нового раздела «История решений», который сам заход и разрешает. Читать его буду как «все восемь прежних на месте», а не как «ровно восемь»; отклонение объявляю здесь, до работы, и перечислю заголовки поимённо.

Пункт 6 (читательский гейт) принимаю как есть, включая то, что он может провалиться: субагенту дам только `view.html` и тот же вопрос, прошлый список покажу себе только после его ответа.

### 7. Работа В — решаю после Б, по стоп-правилу

Не начинаю, пока А и Б не закрыты. Отдельно: у В есть содержательный риск, которого заход не называет, — сплошная замена «предупорядоченное» → «частично упорядоченное» **меняет математику** (У14 формулирует критерий замкнутости для предпорядка; антисимметрия там нигде не работает, а в критерии «тогда и только тогда» сужение посылки — не терминологическая правка). Если возьмусь — только там, где термин не несёт математической нагрузки; иначе пункт уходит в `## ВОПРОСЫ` целиком.

## ВОПРОСЫ — (заполняет исполнитель)

### В1. 🔴 Работа В не сделана намеренно: решение владельца и математика документа расходятся

Проверил предпосылку по живому файлу. **Разнобоя, который надо «свести к одному термину», в документе почти нет** — он уже последователен: носитель везде «предупорядоченное множество» / «предпорядок» (определение 14, определение 17, утверждение 14, раздел D). Единственное чужеродное употребление стояло в «Соглашениях» — «ч.у. множество — категория» в списке наблюдений, ссылающемся на определение 14, где стоит предупорядоченное. Это была не терминологическая развилка, а неверная перекрёстная ссылка, и я её починил: теперь «предпорядок — категория», как в параллельном месте раздела D.

**Сплошную замену на «ч.у.м.» не делал, и вот почему.** Решение владельца — «по умолчанию ч.у.м., смотреть как принято в источниках». Но здесь термин несёт математику, а не стиль:

- **Определение 14** строит категорию из **рефлексивного транзитивного** отношения. Антисимметрия не нужна и нигде не работает — это сказано в поле `не-говорим` того же блока.
- **Утверждение 14** — критерий «тогда и только тогда»: предпорядок декартово замкнут ⟺ есть наибольший элемент, инфимумы и $\Rightarrow$. Заменить посылку на ч.у.м. значит **сузить утверждение**, то есть изменить математику, что запрещено сквозным запретом §2.

То есть выполнить решение владельца буквально нельзя, не нарушив §2.4. Вторая половина его же формулировки — «смотреть как принято в источниках» — работает против первой: источник, уже лежащий в E4 (Лейнстер, Упр. 6.3.23(a), печ. с.167), говорит именно про предупорядоченные множества. **Нужно ваше решение**, и у него три варианта: (1) оставить как есть — документ последователен на «предпорядке», пункт E4 остаётся открытым; (2) перейти на ч.у.м. в языке **курса**, оставив «предпорядок» там, где он несёт математику, — тогда термина станет два, и это надо назвать явно, а не получить случайно; (3) перейти целиком, приняв сужение утверждения 14 как сознательную плату. По стоп-правилу захода («полработы здесь хуже, чем ничего») я не начинал.

### В2. E11 не тронут, но его указатель стал длиннее на один шаг

Заход велит E11 не трогать — не тронул. Побочный эффект переноса по A1: E11 говорит «действующие числа берутся командами **из шапки**», а команды теперь в приложении. Указатель не сломан — шапка прямо отсылает в приложение, — но ведёт в два шага вместо одного. Чинится двумя словами; **не чинил, потому что запрет прямой**. Скажите, править ли.

### В3. Дефект вёрстки, вне зоны, не чинил

Движок не пропускает формулы через заголовки и метки блоков: `<h3>П25. Просто типизированное $\lambda$-исчисление</h3>` и `<span class="lbl">Определение 25 (просто типизированное $\lambda$-исчисление)</span>` печатают `$\lambda$` сырым — и в тексте, и в боковом оглавлении. Это `_generator/`, вне зоны. Дефект не мой и был до захода.

Там же второе, названное самим заходом: три поля печатаются голыми словами без имён («мотивируемое / тривиально / безусловно»). Я закрыл это со стороны источника — «Соглашения» теперь объясняют все три шкалы, а преамбула раздела B перечисляет поля по порядку, — но имён в вёрстке по-прежнему нет.

## ОТЧЁТ — (заполняет исполнитель)

Работы **А и Б закрыты целиком**, работа **В не делалась** — сознательно, с обоснованием в `## ВОПРОСЫ`, В1. Стоп-правило соблюдено: А сделана раньше и полнее всего.

### Что сделал и ЗАЧЕМ

**A1 — служебный блок ушёл из-под первого экрана (развилка (б)).** Раздел `Как считать этот документ` (34 строки: `awk`, `grep`, разбор ловушек подстроки) перенесён **дословно и целиком** в конец документа как `## Приложение. Как считать этот документ`. В начале осталось два абзаца: что идёт дальше и что чисел, вписанных руками, в документе нет — команды в приложении, для математики читать их не нужно. Заголовка у этих абзацев нет намеренно: сперва я поставил `## Как читать этот документ`, увидел, что он отличается от `## Как считать` **одной буквой**, и убрал — два таких заголовка в оглавлении рядом хуже, чем ни одного. `KONSTITUCIYA §10` соблюдена буквально: она требует, чтобы число считалось командой, а не чтобы команда лежала в шапке; команды в документе и рабочие — прогнал их после переноса.

**A2 — служебные слова.** Формула `финал ⇒ CCC ⇒ минимум` объяснена **один раз** в преамбуле A6: что за финал (утверждение 25), что за CCC (определение 12), что за минимум (наименьший набор понятий, из которого CCC собирается) и почему это цепочка. В шести блоках `П29`–`П34` дословный повтор заменён отсылкой в одну строку. Слова `зона`, `рамка`, `приз`, `сюжетная арка` получили по абзацу в «Соглашениях» — первым, до того как ими начинают пользоваться; у слова «зона» отдельно оговорены **два** его употребления (пометка блока и «в зоне курса»), иначе объяснение само стало бы ловушкой. Убрана и опора на непрочитанное в объяснении пометки `условно` («её ввело решение по сопряжениям и Йонеде» — читатель не знает ни того, ни другого). Перечислены шесть обязательных полей, о которых документ говорил «сверх шести обязательных», нигде их не называя.

**A3 — археология переселена, не удалена.** Заведён новый раздел `## История решений` (13 пунктов) в конце, перед приложением. Почему не в раздел E: E называется «Открытые вопросы», а переселяемое — **закрытые** решения с датами; сложить одно в другое значит соврать заголовком обоих. Граница, по которой решал: археология внутри математического текста (разделы A, B, B′) переезжает, археология внутри разделов D и E остаётся — эти разделы по своему заголовку и есть журнал решений, там она дома. Из тела блоков ушли: сравнения с прежними редакциями, имена заходов, «условие владельца изменилось: было … стало …», спор с невидимым оппонентом в ⚠-абзаце A6. Осталось то, что читателю нужно: почему так решено — без имён заходов и без прошлых версий.

**A4 — шкала `уровень`.** Абзац в «Соглашениях»: три ступени, что каждая значит, и главное — что шкала спрашивает **не о том**, о чём `вердикт` (уровень про читателя, вердикт про доказательство). Разобрана ровно та пара, на которой споткнулся прошлый верификатор: `тривиально` + `требует-приручения` у утверждения 42. Преамбула раздела B больше не отсылает «по лестнице формата матбазы», а называет значения и указывает на «Соглашения».

**Б — шесть правок владельца плюс мелочь.**
1. **У25** — добавлен абзац «Почему в мотивируемом, хотя формулировка называет внешний материал», по форме прецедента У22, с датой данных `2026-07-21`. Довод против назван честно; ответ — по границе из «Соглашений»: раздел определяется тем, **о чём** утверждение (о произвольной CCC), а не тем, какие слова в нём звучат.
2. **B0** — связка слита с вводным абзацем группы в один; зачин `**Зачем ` сохранён, маркеров по-прежнему 14.
3. **E11 — не тронут.** Подтверждаю. Побочный эффект переноса описан в `## ВОПРОСЫ`, В2.
4. **У10** — порядковые приведены к формулировке: «четвёртый» → шестой, «пятый» → первый, «шестой» → пятый; три прямо выводимых закона названы номерами (второй, третий, четвёртый), и добавлена оговорка, что разбираются они по способу вывода, а не по порядку формулировки. Математика не тронута.
5. **У51** — доказательство закрыто. Разрез в точке $i$ даёт $C_i$; дописан недостающий шаг «$C_i=C_{i+1}$ при $1\le i\le n-2$», в котором одно применение ассоциативности как раз и работает — ровно то, чего не хватало.
6. **У24** — «строг и полон» расцеплены: строг = инъективен на множествах морфизмов, полон = сюръективен; сказано, что вместе это прежняя скобка «биективен», но в работе они расходятся (утверждение 37) — потому и названы порознь.
7. **Мелочь:** знак `!` введён явно там, где вводится терминальный объект (определение 6): `$!_X$`, а когда объект виден из записи — просто `$!$`.

**Две правки внесены уже ПОСЛЕ читательского верификатора — по его находкам, обе чинят мой же брак:** несведённое поле `следует-из` у У10 (правка Б4 была сделана наполовину) и фактически неверная иллюстрация к шкале `уровень` в «Соглашениях». Разбор — в результате верификатора §3(б).

**Попутно, потому что сломал сам:** перенос по A1 обессмыслил три ссылки на «шапку» (раздел D, вводная приложения, подвал) — починил. И примечание приложения про третью ловушку подстроки ссылалось на фразу внутри утверждения 10, которую я же и переселил, — переписал на то, что верно сейчас. Плюс одна неверная перекрёстная ссылка в «Соглашениях»: «ч.у. множество — категория» при определении 14 «предупорядоченное множество как категория» (подробно — В1).

### Как проверил

**§2.5, прогнано буквально, вывод дословно:**

```
# 1. СОСТАВ НЕ ТРОНУТ                    ДО → ПОСЛЕ
У-блоков           51 → 51  ✓        следует-из     51 → 51  ✓
мотивируемое       43 → 43  ✓        мотивация       8 →  8  ✓
# 2. ГЛАВНЫЙ ИТОГ (команда из приложения, дословно)     5 → 5  ✓
# 3. СВЯЗКИ (маркер '^\*\*Зачем ')                    14 → 14  ✓
# 4. ДАТЫ ДАННЫХ ('данные на 2026-07-21')             12 → 17
# 5. движок
python3 _generator/build_doc.py teorkat-vvedenie/MAT-KOSTYAK      rc=0
   ✓ структура и мат-долги в норме (0 мягк. предупрежд.) · открытых ⚑ всего: 0
python3 _generator/tools/check_view.py teorkat-vvedenie/MAT-KOSTYAK  rc=0
   ✓ ЗЕЛЁНЫЙ: текст не съеден, опасных формул нет
```

Дат стало **больше**, а не меньше (12 → 17): «История решений» несёт свои даты данных. Ни одна прежняя не потеряна — это отдельно проверено верификатором §3(а) пофразно, не по счёту.

**§2.5 п.6, ОХВАТ — счёт снят ДО работы первым ходом:**

| фраза | ДО | ПОСЛЕ | где осталось |
|---|---|---|---|
| `финал ⇒ CCC ⇒ минимум` | 8 | **2** | преамбула A6 (там она и объясняется) · раздел D, «Отвергнуто 5» |
| `Прежняя редакция` | 2 | **0** | — |
| `Первая редакция` | 2 | **1** | раздел D, «Отвергнуто 4» — служебный раздел, там она дома |
| `kod_zamykanie` | 3 | **1** | пункт E11, который заход велел не трогать |

**§2.6, оглавление собранного вида — прочитано глазами, ничего не съедено.** Заголовков `##` девять: `Соглашения документа` · `Раздел A` · `Раздел B` · `Раздел B′` · `Раздел C` · `Раздел D` · `Раздел E` · **`История решений`** (новый, объявлен в `## ПЛАН` до работы) · **`Приложение. Как считать этот документ`** (тот самый прежний первый раздел, переехавший). Все восемь прежних на месте. В дереве стоят все 34 блока `П`, все 51 блок `У` и все 16 групп — `A1`–`A6`, `B0`–`B6`, `B′1`–`B′3`; те тринадцать из них, что несут маркер `**Зачем `, на месте все.

### Результат верификатора §3(а) — вывод скрипта дословно

Скрипт разбирает обе версии на блоки и сверяет против `git show HEAD`. Прогонялся трижды по ходу работы; **первый прогон дал `rc=1`** — «археология не доехала: У12». Проверил грепом: фрагмент доехал, у меня в пробнике был захардкожен регистр («Первая» против живого «первая»). Сломан был пробник, не документ; сделал сверку регистронезависимой и перегнал. Пишу об этом, потому что «гейт покраснел, я посмотрел и решил, что он неправ» — самое опасное место отчёта, и оно должно быть видно.

```
── 1. СОСТАВ БЛОКОВ ──
  HEAD: 85 блоков · новая: 85 блоков
  ✓ те же 85 блоков, В ТОМ ЖЕ ПОРЯДКЕ, номера не сменились
  ✓ названия блоков: изменено 0 из 85
── 2. ФОРМУЛИРОВКИ ──
  ✓ ни одна не исчезла, номера совпали (85 из 85)
  переписано формулировок: 2 (по заданию разрешены только У24 и опр. 6)
      Определение 6 (терминальный объект)
      Утверждение 24 (критерий эквивалентности)
── 3. ПОЛЯ ──
  ✓ тип 51→51 · вердикт 51→51 · статус 51→51 · уровень 51→51 · следует-из 51→51
  ✓ зона 45→45 · зависимости 34→34 · оправдание 34→34 · не-говорим 34→34
  ✓ узел 96→96 · причина 11→11
  ✓ значения тип/вердикт/статус/уровень: сверено 204 пар, расхождений 0
── 4. ДАТЫ ДАННЫХ И РЕШЕНИЯ ВЛАДЕЛЬЦА ──
  строк-носителей: HEAD 14 → новая 19
  ✓ уцелели дословно: 9 из 14
  ⚠ строка изменена (проверяю пофразно): 5   — все пять ✓
  ИТОГ: цитат владельца в HEAD 21, потеряно 0
── 5. ПЕРЕСЕЛЕНИЕ АРХЕОЛОГИИ (A3) ──
  ✓ все 12 пробников нашлись в «Истории решений»
==============================================================
✓ ВЕРИФИКАТОР §3(а): ЧИСТО
  сверено 85 блоков из 85 · 85 формулировок из 85 · 204 значений полей ·
  14 строк с датами/решениями владельца
  список потерянных дат данных и решений владельца: ПУСТ
```

Переписаны ровно две формулировки, обе заказаны заданием (У24 — правка Б6, определение 6 — знак `!`). Больше ни одна формулировка утверждения или определения не тронута.

### Результат верификатора §3(б) — главный гейт захода

Свежему субагенту дан **только** `view.html`, тот же вопрос дословно, без захода, без списка правок, без прошлого отчёта. Прошлый список я ему не показывал и сам сверялся с ним только после его ответа.

⚠ **Оговорка о честности по времени.** Я пересобирал `view.html` один раз **во время** его работы (чинил ссылки на «шапку» и перекрёстную ссылку про ч.у.м.), так что он читал версию где-то между. После его ответа в источник внесены ещё пять правок: перечень шести обязательных полей, объяснение пометки `условно` без опоры на непрочитанное, расшифровка префиксов `П`/`У` — и **две починки по его же находкам** (ниже). То есть по трём пунктам его чтение строже, чем текущий файл, и ни по одному — мягче.

#### 🔴 Он поймал два дефекта, которые внёс Я. Оба чинил, оба перепроверил

1. **Правка Б4 была сделана наполовину.** Я привёл порядковые в теле доказательства У10 («четвёртый» → шестой, «пятый» → первый, «шестой» → пятый), но не тронул поле `следует-из` того же блока, где осталось «Внутри: **четвёртый** закон $A^0\cong 1$». Блок стал противоречить сам себе на расстоянии двадцати строк — то есть я заменил один разлом другим. Починено: «шестой закон $A^0\cong 1$».
2. **Иллюстрация к работе A4 была фактически неверна.** Я написал в «Соглашениях», что пара «тривиально + требует-приручения» законна «например, утверждение 42: чтобы эти три строки прочесть, надо быть привычным к языку Hom-множеств». Проверил по его наводке: `grep -c 'mathcal{C}('` по блоку У42 → **0**, Hom-множеств там нет вовсе, доказательство идёт чистой погоней по стрелкам. Флагманский пример новой шкалы описывал не то, что в нём написано. Переписан на то, что там есть на самом деле: опознать начальность $X\times 0$ и вытащить $\pi_1=i\circ\pi_2$.

Оба — мои, оба внесены этим заходом, ни один не поймали ни гейты состава, ни верификатор §3(а). Это цена работы «по смыслу»: программная сверка ловит потери, а не свежепосаженную неправду.

#### Таблица сверки с прошлым списком (`kod_resheniya.md`, §3(б))

**Ни один пункт, названный в А1–А4, не воспроизвёлся.** Единственное «да» в зоне работы А — пункт, который заход прямо велел не чинить.

| # | пункт прошлого списка | заказан? | воспроизвёлся? |
|---|---|---|---|
| 1 | «Как считать» на первом экране: три экрана `awk`, нет сигнала «пропусти» | **A1** | **нет** — блока на пути больше нет; читатель дошёл до него, только потому что читал целиком |
| 2 | «Соглашения»: `зона` как известное; опора на «решение по сопряжениям и Йонеде» | **A2** | **нет** — не упомянуто |
| 3 | «шесть обязательных полей» нигде не перечислены | — | **нет** |
| 4 | A6: приз/рамка/сюжетная арка/задание §2.3; формула повторена 5× дословно | **A2** | **нет** — A6 не назван ни разу |
| 5 | ⚠-абзац A6: спор с невидимым оппонентом | **A3** | **нет** |
| 6 | Редакционная археология в математическом тексте | **A3** | **нет** — ни «прежней редакции», ни имён заходов |
| 7 | Поле «Уровень»: шкала не названа, У42 читается противоречием | **A4** | **нет** — шкалу хвалит: «идея сильная и правильная»; претензия сместилась на **неровность применения** (У2, У32) и на мой неверный пример |
| 8 | Три голых слова без имён полей | **A4, «не чинить»** | **да** — как и предписано; это вёрстка, вне зоны |
| 9 | **У51 — доказательство не закрывается** | **Б5** | **нет** — прямо в разделе «читается гладко»: «У51 — аккуратная индукция по расстановкам скобок, **без пропусков**» |
| 10 | **У17 — знак `!` не введён** | **Б, мелочь** | **нет** — У17 разбирает подробно, про `!` не говорит ни слова |
| 11 | **У10 — порядковые не совпадают с формулировкой** | **Б4** | **нет в теле**; но нашёл несведённое поле `следует-из` — мой недоделанный Б4, см. выше |
| 12 | **У24 — «строг и полон» склеены скобкой** | **Б6** | **нет** — не упомянуто |
| 13 | П31: первый абзац не про сопряжённые функторы | не заказан | **да** — не трогал, объявлено в «Что НЕ трогал» |
| 14 | Определение 5 — не определение | не заказан | **да**, острее прежнего |
| 15 | Определение 7 «формально» без формального языка | не заказан | **да**, теперь через метку `школьно` у У2 |
| 16 | Определение 17 — переход «замкнутость → $\top,\wedge,\Rightarrow$» не показан | не заказан | **да** |
| 17 | У12 — опознание композиции объявлено, не выписано | не заказан | **да** |
| 18 | У49 — «слово в слово» проверить нечем | не заказан | **да**, точнее: правило $\eta$ во всём документе не написано нигде |
| 19 | У25 — конструкция на языке, который отказались вводить | не заказан | **да** |
| 20 | Порядок номеров, ссылки вперёд | не заказан | **да** (нет указателя по номерам; две ссылки вперёд в У10) |
| 21 | У32 «то есть запрета 2»; У4 поле спорит само с собой; преамбула B4 | не заказан | **нет** — не назван ни один |

Вердикт по §2.5 п.6: **работа А прошла.** Все четыре заказанных математических пункта (Б4, Б5, Б6, `!`) закрыты и в новом чтении отсутствуют.

#### Что он нашёл нового — это следующая итерация, а не брак этой

Самое ценное — **он назвал точку обрыва, которой в прошлом списке не было**: «раздел B ломается не местами, а в одной опознаваемой точке — на переходе к языку Hom-множеств (утверждения 8 и 9)… до У8 читатель проверяет глазом, после У8 — верит на слово». И два содержательных попадания, которых не видел никто: (1) **«Оговорка о порядке» после У8 называет не тот функтор** — в У8/У9 работает $\mathcal{C}(-,P)$, контравариантный, а оговорка и пункт E9 говорят про $\mathcal{C}(X,-)$; (2) **в доказательстве У17 дважды используется безымянный закон** $\langle u,v\rangle\circ a=\langle u\circ a,\,v\circ a\rangle$ — в документе, который сам объявил принципом «ход, которым пользуются дважды, обязан стоять под номером». Оба — про состав и математику, то есть под сквозным запретом §2; не трогал, вынес в «Открытое».

#### Ответ субагента — ЦЕЛИКОМ

> ## Короткий вывод
>
> Раздел A читается почти весь — до тех пор, пока он остаётся списком определений с примерами внутри. Раздел B ломается не «местами», а в одной опознаваемой точке: **на переходе к языку Hom-множеств (утверждения 8 и 9)**. С этого места и до конца B каждое доказательство — цепочка биекций Hom-множеств, и во всём разделе B нет ни одного места, где такая цепочка была бы посчитана в конкретной категории. Читатель, знающий математику, но не категории, до У8 проверяет глазом, после У8 — верит на слово. Это следствие жанра (документ сам запретил себе примеры), но адрес у обрыва точный, и его стоит знать.
>
> ## 1. Раздел A: четыре места, где не сказано нужного
>
> **Определение 5 (универсальное свойство) — первый настоящий обрыв, и самый дорогой.** Формулировка: «Объект $U$, снабжённый морфизмами-структурой, задан универсальным свойством, если для любого объекта $X$ с такой же структурой существует ровно один морфизм, связывающий $X$ с $U$, при котором соответствующая диаграмма коммутативна». Здесь **каждое несущее слово — незаполненный слот**: что такое «морфизмами-структурой», что такое «такой же структурой», в какую сторону идёт «морфизм, связывающий $X$ с $U$» и какая диаграмма «соответствующая». Направление принципиально: для произведения это $X\to P$, для копроизведения $S\to X$, для начального объекта $0\to X$ — то есть под «связывающий» спрятаны две разные конструкции. Читатель встречает это определение **до единственного примера** (терминальный объект идёт следующей группой). Формально это не схема, а мнемоника для того, кто примеры уже знает. Симптоматично, что позже документ сам это чинит — утверждение 32 даёт настоящую формулировку (терминальность во вспомогательной категории) — и тем задним числом показывает, что определение 5 было жестом.
>
> **Определение 17 (алгебра Гейтинга) — определение, в которое зашита теорема.** «Предупорядоченное множество, декартово замкнутое как категория и вдобавок имеющее начальный объект и копроизведения: наибольший элемент ⊤, наименьший ⊥, конечные инфимумы ∧, супремумы ∨ и операцию ⇒…». Двоеточие здесь работает как «то есть» — то есть **утверждается перевод** «терминальный = наибольший, произведение = инфимум, экспонента = импликация». Этот перевод доказывается утверждением 14, которое стоит примерно на шестьсот строк ниже, в разделе B′, куда читатель раздела A ещё не дошёл. Хуже: У14 прямо оговаривает, что «алгеброй Гейтинга такой предпорядок становится не сразу», — то есть уточняет то, что определение 17 подало как данность. Проверить корректность определения 17 на месте нельзя.
>
> **Категория $\mathbf{Set}$ нигде не заведена.** Она впервые появляется как структурный ингредиент в определении 22 ($\mathcal{C}(A,-)\colon\mathcal{C}\to\mathbf{Set}$), потом в определении 29, потом во всём B6, — и ни одной карточки П в словаре у неё нет. Для математика смысл очевиден, и как математическая проблема это ничто; но документ объявил словарь раздела A своим критерием комплектности («шесть полей», грепы в приложении), и главный носитель всей линии функторов из этого критерия выпал. Это не придирка к стилю: читатель, который пытается понять, что именно курс обязуется произнести, здесь получает неверную картину.
>
> **Врезка про действие экспоненты на морфизмах спрятана под чужой заголовок.** $g^A:=\lambda(g\circ\mathrm{ev})$ живёт в П31 «Сопряжённые функторы». Тот, кто ищет «как экспонента действует на стрелках», под заголовком про сопряжения искать не будет — а определение 10 без этого неполно. Документ объясняет причину (не предоплачивать технику условного блока), причина внятная, но эффект на читателя ровно такой.
>
> Отдельно: **«не говорим» в определении 18 ссылается на несуществующий объект** — «о полных, строгих и существенно сюръективных функторах вне **условного блока про эквивалентность**». Никакого условного блока про эквивалентность в документе нет: определение 23 в зоне `определяем`, утверждение 24 — `безусловно`, и именно там эти три слова и определяются. Это устаревшая ссылка, пережившая перекладку.
>
> Что при этом **не** дефект: определение 1 пользуется словом «класс» и тут же запрещает спрашивать, что это. Это честно помечено, и определение 30 позже открыто говорит, что понятие категории функторов «нельзя честно сформулировать, не переоткрыв закрытый вопрос». Так и надо; претензий нет.
>
> ## 2. Раздел B: где текст перестаёт читаться
>
> **(а) «Естественно по $X$» — под одним словом две разные вариантности, и оговорка называет не тот функтор.**
> В У8 семейство — это $X\mapsto\mathcal{C}(X,P)$, то есть $\mathcal{C}(-,P)$, **контравариантно**; в У9 то же самое; в У47 — $X\mapsto\mathcal{C}(A,X)$, то есть **ковариантно**. Определение 20 задаёт естественность только для ковариантных функторов. Что «естественно по $X$» в контравариантном случае означает другой квадрат (предкомпозиция вместо посткомпозиции), не сказано ни разу; догадаться можно только по направлению стрелки в проверках («для $h\colon X'\to X$» в У8, «для $u\colon X'\to X$» в У9 — против «применённая к $u\colon A\to X$» в У47). Это классическая яма для новичка, и документ в неё не ставит даже флажка.
>
> Хуже, что в этом самом месте документ **промахивается по существу**. «Оговорка о порядке» после У8: «слово „естественно по $X$“ опирается только на действие $\mathcal{C}(X,-)$ на морфизмах». Но в У8 и У9 работает не $\mathcal{C}(X,-)$, а $\mathcal{C}(-,P)$ и $\mathcal{C}(-,E)$ — другой слот, другая вариантность. Та же ошибка дословно повторена в разделе E, пункт 9: «проверка того, что $\mathcal{C}(X,-)$ действительно функтор, стоит ниже». То есть единственное место, где документ педантично разбирается со своим порядком, разбирается не с тем функтором. Для читателя, который как раз в этот момент пытается понять, что означает слово «естественно», это дезориентирует сильнее, чем молчание.
>
> **(б) Утверждение 17 (теорема о неподвижной точке) — самая тяжёлая формула документа плюс неназванная лемма.**
> Гипотеза записана как $\mathrm{ev}\circ\langle\varphi\circ a\circ !,\,1_A\rangle=f$. Это корректно и типизируется, но чтобы понять, что тут сказано («$\varphi(a)$ — это та самая функция $f$»), нужно самому раскодировать три вложенные конструкции. Примера нет и по уставу документа быть не может. Это трудность жанра, её можно принять.
>
> Но доказательство делает ход, которого в документе нет: «Подставим в это равенство саму точку $a$» — то есть пользуется законом $\langle u,v\rangle\circ a=\langle u\circ a,\;v\circ a\rangle$, дважды. Этот закон нигде не сформулирован: определение 8 даёт $f\times g$ и его функториальность, но не поведение скобки $\langle-,-\rangle$ при предкомпозиции; ближайшее, откуда он следует, — уравнения утверждения 49, стоящего много ниже и написанного совсем для другого. Документ, который объявляет принципом «ход, которым пользуются дважды, обязан стоять в списке под своим номером» (обоснование У47), в единственном по-настоящему содержательном месте блока B2 пользуется безымянным ходом дважды в одной строке.
>
> Тот же класс: в У41 молча используется «изоморфизм переносится через произведение» ($1\cong 0\Rightarrow A\times1\cong A\times0$). Выводится из врезки к определению 8, но не назван — при том что вокруг названы вещи куда мельче.
>
> **(в) Утверждение 10 (законы степеней) — сломанная внутренняя ссылка и две ссылки вперёд.**
> В формулировке шесть законов; доказательство разбирает их по номерам: «Второй, третий и четвёртый», затем «**Шестой** — $A^0\cong 1$», затем «Первый», затем «пятый». А в поле «Следует из» того же блока написано: «Внутри: **четвёртый** закон $A^0\cong 1$ получается приёмом утверждения 11». Четвёртый закон — это $(A\times B)^C\cong A^C\times B^C$. Блок противоречит сам себе на расстоянии двадцати строк.
>
> И там же — две ссылки вперёд, в документе, который обещал во вступлении: «Утверждение стоит после всего, от чего зависит». (1) «Шестой — тем же чтением, что в утверждении **11**» — а У11 стоит ниже и в своём поле заявляет, что приём «вводится **здесь**». (2) Закрытие флага про пятый закон опирается на утверждение **47**, стоящее в B4, то есть ещё ниже. Второй случай документ признаёт (преамбула B4: «по существу платит долги раздела B2»), первый — нет, и раздел E его тоже не ловит: пункт 9 говорит совсем про другое.
>
> **(г) Утверждение 49: правило $\eta$ существует только в одной фразе.**
> «Два последних уравнения и есть правила $\beta$ и $\eta$ типизированного $\lambda$-исчисления, слово в слово». Определение 25 описывает исчисление так: «вычисление сводит $(\lambda x.t)(u)$ к подстановке» — это $\beta$, и только $\beta$. Слово $\eta$ во всём документе встречается один раз — вот здесь, — и обозначает правило, которое нигде не написано. При этом фраза несёт вес: она — обоснование того, зачем утверждение 49 вообще стоит отдельной строкой, и мост к финалу. Проверить её читатель не может ни в каком смысле.
>
> **(д) Утверждение 2 (принцип двойственности) с меткой «школьно».**
> Это метатеорема: «Всякое утверждение, выведенное только из аксиом категории…». Чтобы её высказать, нужен формальный язык, в котором живут «утверждения о категориях», и операция $P\mapsto P^{\mathrm{op}}$ на нём. Ни того ни другого документ не вводит — определение 7 обещает «Формально:» и даёт вместо этого перифраз. Собственная шкала документа определяет `школьно` как «всё нужное уже введено»; здесь нужное не введено намеренно (определение 1: о размерах и классах не говорим). Метка не просто мягкая — она **сообщает читателю неправду о том, какого рода это утверждение**: единственное во всём разделе, чей предмет — не объект, а доказательство.
>
> **(е) Утверждение 32 с меткой «школьно / тривиально».**
> Блок строит две вспомогательные категории (тройки $(X,f,g)$; пары $(X,f\colon X\times A\to B)$), проверяет их аксиомы, отдельно оговаривает, что для второй нужна функториальность $(-)\times 1_A$, и отдельно переводит изоморфизмы обратно в $\mathcal{C}$. По собственной шкале документа (`требует-приручения` — «пользуется приёмом, который срабатывает не там, куда просится») это ровно `требует-приручения`: сдвиг с объекта на категорию объектов — прототип такого приёма. Читатель, доверившийся метке, решит, что не справляется со «школьным».
>
> **(ж) Утверждение 42 — и опрокинутая иллюстрация в «Соглашениях».**
> «Соглашения документа» приводят У42 как **образцовый** пример законной пары «тривиально + требует-приручения», объясняя: «чтобы эти три строки прочесть, надо уже быть привычным к языку Hom-множеств». В доказательстве У42 нет ни одного Hom-множества: это чистая погоня по стрелкам через $\pi_1=i\circ\pi_2$. Флагманский пример собственной шкалы описан неверно.
>
> Заодно там же вердикт спорит сам с собой: «Соглашения» говорят «ход, который надо изобрести… — `нетривиально`, даже если он в одну строку», а поле «Следует из» у У42 признаёт: «своего в выводе только ход „два морфизма из начального объекта равны, значит $\pi_1=i\circ\pi_2$“». Изобретённый ход есть, назван, — вердикт всё равно `тривиально`.
>
> **(з) Утверждение 7: «канонические, то есть перестановочные с проекциями» не типизируется для ассоциативности.**
> Для коммутативности и единицы фраза осмысленна. Для $(A\times B)\times C\cong A\times(B\times C)$ общих проекций у двух сторон нет вовсе: слева проекции в $A\times B$ и $C$, справа в $A$ и $B\times C$. Слово «канонический» определено здесь условием, которое в одном из трёх случаев, к которым оно применено, не имеет смысла. Внимательный читатель на этом остановится — и остановится справедливо.
>
> **(и) Утверждение 26 (лемма Йонеды), одна фраза скетча.**
> «…в $\mathcal{C}(A,A)$ больше ничего нет, с чем $\eta$ могла бы что-то сделать». Буквально это ложно: $\mathcal{C}(A,A)$ может быть каким угодно. Имелось в виду «всякий $u\in\mathcal{C}(A,X)$ есть образ $1_A$», и это в предыдущем предложении уже сказано формулой; приписка не поясняет, а сбивает.
>
> ## 3. Навигация и разметка — отдельный слой непонятности, не математический
>
> - **Префиксы «П» и «У» нигде не расшифрованы.** Документ специально заводит раздел «Соглашения», обещая назвать «четыре служебных слова, которыми документ говорит о себе», — и не называет два обозначения, которые стоят в каждом заголовке. Догадаться можно; но именно этот документ обещал не заставлять догадываться.
> - **Две параллельные нумерации с пересекающимся диапазоном.** Есть «определение 9 (копроизведение)» и «утверждение 9 (каррирование)»; «определение 12 (CCC)» и «утверждение 12 (дистрибутивность)»; «определение 8 (произведение)» и «утверждение 8»; «определение 25 ($\lambda$-исчисление)» и «утверждение 25 (Карри–Ховард–Ламбек)». В доказательстве У12 обе встречаются в одном предложении: «второй и предпоследний — утверждение 9, средний — универсальное свойство копроизведения (определение 9)». Различие держится на одном слове, которое глаз при быстром чтении не ловит.
> - **Индекса по номерам нет.** Оглавление даёт только заголовки разделов. При этом порядок утверждений — 51, 33, 34, 1, 2, 3, 45, 46, 32, 4, 5, 6, 7, 38, 8, 39, 9, 40, 17, 10, 11, … Каждое доказательство — цепочка номерных ссылок, и найти адресата можно только прокруткой. Сортировка по зависимостям — законное решение, отсутствие указателя при ней — нет.
> - **Три главных поля утверждения не подписаны.** Под каждым блоком стоят три голых слова: «мотивируемое», «тривиально», «безусловно» — без имени поля, тогда как «Уровень:», «Узел картотеки:», «Следует из:» подписаны, и все поля определений тоже. Два из трёх различаются только приставкой («тривиально / нетривиально», «безусловно / условно»). Про эту же ловушку подстрок документ предупреждает **сам** — в приложении, для грепов. Для человеческого глаза она ровно та же, и здесь не снята.
> - **Слово «колонка» означает три разные вещи.** «Третья колонка словаря финала» (определение 26) — это язык, одна из трёх. «Колонка „терм“» (У40) — это клетка внутри колонки типов. «Колонка „или“» (определение 9, У25) — это строка, которой нет. «Через экспоненту — две колонки словаря финала: тип пары и тип функции» (преамбула A2) — это две клетки, и вдобавок «тип пары» приходит от произведения, а не от экспоненты. Читатель, который честно пытается удержать в голове таблицу Карри–Ховарда–Ламбека, эту таблицу от такого описания не соберёт.
>
> ## 4. Где читается гладко — и это тоже результат
>
> - **A1, определения 1–4.** Учебниковая чистота, ничего лишнего, поля «не говорим» реально помогают: читатель видит границу и не ищет за ней.
> - **A2 как лестница: определение 10 → 11 → 12.** Экспонента → каррирование → CCC — лучшее место документа. Определение 10 даже страхует читателя явной сноской «Здесь $\lambda f\times 1_A$ — произведение морфизмов из определения 8». Так и надо было везде.
> - **A3 целиком (определения 14–16).** Единственное место, где примеры уцелели внутри словаря, — и единственное, где не знающий категорий читатель получает опору. Предпорядок, моноид, пути в графе объяснены так, что их можно проверить пальцем. Не случайно именно на них потом всё и держится.
> - **A5.** Читается легко ровно потому, что честно объявляет себя: «определение приведено для автора костяка… и не является планом произнести его». Когда документ говорит, чего он не делает, он мгновенно становится понятным.
> - **B0 (У51, У33, У34) и большая часть B1 (У1, У3, У45, У46).** Короткие, полные, проверяемые доказательства; У51 — аккуратная индукция по расстановкам скобок, без пропусков.
> - **B3 целиком (У18, У35, У19, У20).** Самый ровный блок раздела B. Метки «школьно» здесь честные, доказательства действительно проверяются глазом, и У35 («сохраняется всё, что выражается коммутативной диаграммой») — единственное место, где документ формулирует общее рабочее правило вместо очередной частности.
> - **Утверждение 47.** Образец того, как надо: «…то есть **вся биекция $\Phi$ есть предкомпозиция с одним морфизмом $g$**, и это единственный ход доказательства; дальше только подстановки». Читателю прямо сказано, где здесь мысль, а где бухгалтерия. Плюс отдельно и внятно разграничено с леммой Йонеды.
> - **Утверждение 25 (изложение Карри–Ховарда–Ламбека).** Три абзаца — «туда», «обратно», «в каком смысле взаимно обратны» — понятны без всякой подготовки, и признание «в курсе это финал-панорама, а не доказываемая теорема» снимает у читателя ложное чувство, что он что-то упустил.
> - **Различение «вердикт» / «уровень» в «Соглашениях».** Идея сильная и правильная: один вопрос про доказательство, другой про читателя. Проблема не в шкале, а в том, что применяется она неровно (см. У2, У32, У42) и проиллюстрирована неверным примером.
>
> ## 5. Что просто трудно — и претензий не заслуживает
>
> Чтобы не смешивать: **не** дефекты документа, а нормальная тяжесть материала — построение $\mathrm{ev}''=\mathrm{ev}\circ(\mathrm{ev}\times 1_B)\circ a$ в первом законе степеней (честно помечено «скетч» и «нетривиально»); пятизвенная цепочка биекций в У12; лемма Йонеды; теорема Лоувера о неподвижной точке как таковая. Здесь читатель обязан работать сам, и документ его об этом предупреждает правильными метками.
>
> Граница между «трудно» и «не сказали» проходит так: если недостающее — это идея, которую читатель должен изобрести, документ прав, что её не даёт. Если недостающее — это **факт, который документ обязан был назвать, чтобы фразу вообще можно было прочесть** (направление стрелки в определении 5; перевод «терминальный = наибольший» в определении 17; закон $\langle u,v\rangle\circ a$ в У17; вариантность в «естественно по $X$»; правило $\eta$ в У49), — документ не прав, и никакой тяжестью материала это не объясняется.
>
> ## 6. Что выглядит откровенно сломанным
>
> Вне A и B, но раз читал целиком — скажу прямо: **приложение «Как считать этот документ» развалилось при сборке.** Внутри код-спанов вместо звёздочек стоят литеральные теги `&lt;em&gt;`/`&lt;/em&gt;`, маркированный список схлопнулся в два гигантских абзаца с разделителями « - » внутри, а один код-спан проглотил прозу. Вдобавок два таких абзаца носят зелёный бейдж «✔ закрыт», хотя никакими флагами не являются — это просто пункты списка команд. То есть команды, которые документ объявляет источником всех своих чисел, в собранном виде **не копипастятся**: их надо реконструировать. Гейт `check_view.py`, судя по подписи внизу, при этом пройден — значит ловит он не то.

#### Про пункт 6 его ответа — проверил отдельно, это НЕ мой перенос

Обвинение серьёзное, и первое, что я сделал, — проверил, не сломал ли блок я, когда его двигал. **Не я.** Дефекты присутствуют в `HEAD` дословно, до захода:

```
git --no-optional-locks show HEAD:teorkat-vvedenie/MAT-KOSTYAK/view.html > /tmp/head_view.html   rc=0
'\<em>\</em>Следует из' внутри код-спана в HEAD:   3
'<p class="flag closed">' с командой awk внутри:  8
```

Причина видна из источника: движок (1) применяет разметку курсива **внутри** код-спанов, поэтому `\*\*` в грепе печатается как `\<em>\</em>`, и (2) видит литеральную подстроку `Флаг закрыт` **внутри самой команды** `awk '… /^Флаг закрыт: краткого вывода нет/ …'` и красит абзац зелёным бейджем «✔ закрыт». Оба — свойства текста, а не его положения в файле: блок переехал вместе со своей поломкой, ничего не приобретя и не потеряв.

Чинится это в `_generator/`, что **вне зоны** — по §2.4 дефекты вёрстки идут в отчёт строкой. Но находка сильная, и я её не смягчаю: `check_view.py` зелёный на документе, чьи команды в собранном виде не копипастятся, — то есть гейт, поставленный охранять §10, охраняет не то. Это кандидат в отдельный заход по движку.

### Что НЕ трогал

- **Состав и математику.** Ни одного утверждения не добавлено и не удалено, ни один номер не сдвинут. Единственная правка математического содержания — дописанный шаг в доказательстве У51, заказанный правкой Б5.
- **Пункт E11** — по прямому запрету, включая устаревший от моего переноса указатель «из шапки» (В2).
- **`_generator/`, вёрстку, `SPEKA.md`, картотеку, `VYCHITANO.md`, другие заходы** — ничего вне двух путей зоны.
- **Термин «предупорядоченное»** — работа В не начиналась (В1).
- **Врезку в начале П31** (прошлый верификатор называл её: под заголовком «Сопряжённые функторы» стоит абзац про действие экспоненты). Заход её не заказывал, а её место — сознательное решение документа. В отчёт, не чинил.

### Открытое — куда возвращаться

1. **Работа В** — три варианта на выбор владельца, В1. Пока документ последователен на «предпорядке».
2. **E11** — двухсловная починка указателя, В2.
3. **Дефект движка**: формулы не рендерятся в заголовках `h3` и в метках блоков — `П25. Просто типизированное $\lambda$-исчисление` печатается сырым и в тексте, и в боковом оглавлении. Вне зоны, В3.
4. **Пункты прошлого списка верификатора, которых заход не заказывал** и которые я не трогал: определение 5 («универсальное свойство» — не определение), определение 7 («формально» обещает точность, которой нет), определение 17 (переход «замкнутость → $\top,\wedge,\Rightarrow$» не показан, а его утверждение 14 стоит ниже), утверждение 12 (опознание композиции пяти биекций объявлено, не выписано), утверждение 49 («слово в слово» проверить нечем), утверждение 25 (конструкция на языке, который отказались вводить), порядок номеров в разделе B. Это следующая итерация, а не брак этой.

5. 🔴 **Новое от свежего верификатора — под сквозным запретом §2, поэтому не тронуто, но дорогое.** Два попадания по существу, которых не видел никто:
   - **«Оговорка о порядке» после У8 называет не тот функтор.** В У8 и У9 «естественно по $X$» относится к $\mathcal{C}(-,P)$ и $\mathcal{C}(-,E)$ — **контравариантным**; оговорка же (и пункт E9, дословно) говорит про $\mathcal{C}(X,-)$. То есть единственное место, где документ педантично разбирает свой порядок, разбирает не тот объект. Плюс общая яма: определение 20 задаёт естественность только для ковариантного случая, а слово «естественно по $X$» работает в обоих.
   - **В доказательстве У17 дважды используется безымянный закон** $\langle u,v\rangle\circ a=\langle u\circ a,\,v\circ a\rangle$ (за фразой «подставим саму точку $a$»). Определение 8 даёт $f\times g$ и его функториальность, но не поведение скобки при предкомпозиции. Документ, который сам объявил принципом «ход, которым пользуются дважды, обязан стоять под номером» (У47), нарушает его в самом содержательном месте B2. Тот же класс — молчаливый перенос изоморфизма через произведение в У41.

   Оба меняют состав или математику, значит решение владельца. Мелочью того же рода: `$\mathbf{Set}$ в словаре нет ни одной карточкой, хотя вся линия функторов на ней стоит; поле «не говорим» определения 18 ссылается на «условный блок про эквивалентность», которого в документе нет.

6. 🔴 **Движок ломает приложение, а гейт этого не видит** — подробно в результате верификатора §3(б). В собранном виде команды, которые документ объявляет источником всех своих чисел, **не копипастятся**: `\*\*` внутри код-спана печатается как `\<em>\</em>`, а абзацы с `awk` получают бейдж «✔ закрыт», потому что движок видит подстроку `Флаг закрыт` внутри самой команды. Проверено: то же самое в `HEAD`, дефект не мой и не от переноса. Чинить — в `_generator/`, вне зоны. Это прямой удар по `KONSTITUCIYA §10`: правило «пишем не число, а команду» держится на том, что команду можно взять и запустить.

### Время и токены

Один проход без перезапусков. Субагент один (читательский верификатор §3(б)); рейт-лимитов не было. Точных счётчиков сессии у меня нет — по объёму работы: чтение документа целиком (1774 строки), ~30 хирургических правок, три прогона верификатора, две пересборки.

**КОММИТ:** `e3aded1` — «маткостяк: читаемость — служебный язык убран, шесть правок владельца» · `git_zona.py check --zone teorkat-vvedenie` → ✅ «зона teorkat-vvedenie: работа доехала в git, вне git ничего нет»

```
git --no-optional-locks add -- teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/view.html   rc=0
git --no-optional-locks commit -m "…" -- <те же два пути>                                              rc=0
python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie                                     rc=0  ✅
git --no-optional-locks show --stat                                                                    rc=0
    teorkat-vvedenie/MAT-KOSTYAK.md        | 195 ++++++-------
    teorkat-vvedenie/MAT-KOSTYAK/view.html | 127 ++++++-----
    2 files changed, 201 insertions(+), 121 deletions(-)
```

В коммите **только два пути зоны**. Файл-заход (`kod_chitaemost.md`) намеренно НЕ коммичен: он вне зоны по контракту, а §4 перечисляет ровно два пути. Хук `pre-commit` не краснел — при pathspec-коммите в staged только моё.
