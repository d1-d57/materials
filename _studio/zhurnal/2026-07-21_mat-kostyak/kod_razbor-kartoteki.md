# Канал исполнителя — razbor-kartoteki (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — работа целиком в суждении: по каждому блоку решить, полагается ли ему карточка по стандарту. Механики тут почти нет, и ошибка в сторону «не полагается» тихая.

КОНТЕКСТ. `teorkat-vvedenie/MAT-KOSTYAK.md` — внутренняя карта курса «Введение в теорию категорий»: 34 понятия и 51 утверждение, состав заморожен и в этом заходе НЕ трогается. У каждого блока есть поле `узел` — указатель в картотеку курса. Прошлый этап: приёмка Ф5 закрыта 22.07; при ней обнаружилось, что **у 41 блока из 85 поле говорит «нет в картотеке»**, а в плане арки этот долг был записан руками как «20 новых утверждений» — вдвое меньше живого.

ЦЕЛЬ: **разложить эти блоки на три списка и назвать размер настоящего долга числом.** Ничего не писать ни в картотеку, ни в маткостяк — владелец сперва смотрит списки. Заход производит ОДИН файл-разбор.

Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели: разбирай **сначала утверждения `У`, потом понятия `П`** — у утверждений находки содержательнее. Получишь неполный охват, но он обязан быть НАЗВАН («разобрано X из Y»), а внутри каждой категории — полным по разобранному.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке репозитория — `git checkout arka/mat-kostyak` первым ходом. Ветку НЕ переключай дальше, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/zhurnal/2026-07-21_mat-kostyak/RAZBOR-kartoteka.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout arka/mat-kostyak`.
- Точка отката не нужна: зона — **один новый файл**, в HEAD его нет, терять нечего. Не фабрикуй.
- ⚠ **Если `git` жалуется на `.git/index.lock`** — это чужой лок от прерванной команды, а не твоя поломка. НЕ удаляй его сам: скажи в отчёте строкой и работай дальше, коммит оставь на владельца (§4, «внешняя причина»). *Цена 22.07: таймаут песочницы оборвал коммит и оставил лок, заблокировав запись в репо для всех троих писателей.*
- Прочитать ТОЛЬКО эти три файла, в этом порядке. Проект не изучай, других файлов не открывай:
  1. **`teoriya-kategoriy/kartoteka/STANDART-uzla.md`** (~90 строк) — правило, ЧТО кладётся в картотеку, а что запрещено. Это твой единственный судья; читаешь его ПЕРВЫМ, до маткостяка, чтобы не подгонять правило под материал.
  2. **`teorkat-vvedenie/MAT-KOSTYAK.md`** (~1830 строк) — источник. Читаешь блоки, попавшие в разбор, целиком: формулировку, доказательство и все поля.
  3. **`teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md`** (~4060 строк, 229 узлов) — живая картотека. Нужна для категории C (см. §2): узел мог появиться после того, как пометка была проставлена. Читай грепом по `^id: `, целиком не глотай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

### 2.0. Список блоков берётся КОМАНДОЙ, а не отсюда

🔴 **Здесь намеренно НЕ выписан перечень блоков.** В этой арке аналитик уже трижды подряд вписал адреса руками и трижды промахнулся по составу, последний раз — в абзаце, где сам предупреждает так не делать (`UROKI-FABRIKE.md`). Поэтому состав ты добываешь сам, из корня репо:

```
awk '/^### [ПУ][0-9]+\./{if(b&&c>0&&c==n)print id; b=1;c=0;n=0;id=$0} /^> поле:узел/{c++; if($0 ~ /Узел картотеки:\*\* нет/)n++} /^## Раздел C\./{if(b&&c>0&&c==n)print id; b=0} END{if(b&&c>0&&c==n)print id}' teorkat-vvedenie/MAT-KOSTYAK.md
```

Печатает заголовки блоков, у которых **все** строки поля `узел` говорят «нет в картотеке». На 22.07 таких **41** (12 `П` + 29 `У`) — это ожидание, а не факт: живое число печатает команда. **Разошлось с 41 — не подгоняй, скажи в `## ПЛАН`.**

⚠ Считаем по БЛОКАМ, не по строкам: у `У28` две строки поля `узел`, одна из них с настоящим id. Поэтому блок с хотя бы одним живым узлом в разбор не попадает.

### 2.1. По каждому блоку — ровно один вердикт из трёх

Судья — **только** `STANDART-uzla.md`, не твоё чувство важности. Он говорит прямо: писать — «находку, структурные связи между узлами, оценку берёт ли зал»; **НЕ писать** — «определения, полные конструкции, разборы примеров, методику». И там же: «находка без карточки = потеряна».

- **A — карточка не полагается.** В блоке нет ничего сверх определения/рутинного вывода: всё восстановимо по маткостяку, класть в картотеку нечего. Пометка «нет в картотеке» верна и долгом НЕ является. Обоснование — одна фраза, ссылающаяся на строку стандарта.
- **B — находка есть, карточка не заведена. ЭТО И ЕСТЬ ДОЛГ.** В блоке есть структурная связь, мотивация или несущий момент, который по стандарту обязан жить в картотеке. Пишешь **одной фразой, ЧТО именно за находка** — не «важное утверждение», а сама связь. Плюс предлагаемый `id` (slug по форме стандарта) и `род`. **Саму карточку НЕ пишешь и в `KARTA-OBLASTI.md` НЕ лезешь** — файл вне зоны.
- **C — пометка протухла.** Узел на эту тему в картотеке **уже есть**, просто блок на него не сослался: картотека выросла до 229 узлов, часть пометок старше. Называешь найденный `id` дословно. Правку в маткостяк не вносишь — он вне зоны, идёт в отчёт строкой.

**Категория C — не формальность.** Проверь её честно грепом по `KARTA-OBLASTI.md`: по теме блока, а не только по угаданному slug. Ложное «нет узла» стоит дороже ложного «есть»: по нему заведут дубль под другим именем, и картотека разъедется.

🔴 **Опасность этого захода — занижение.** Объявить блок категорией A дёшево, тихо и почти всегда правдоподобно; никакой гейт этого не поймает. Поэтому: **сомневаешься между A и B — ставь B** и говоришь в строке, что сомневаешься. Список B смотрит владелец, лишний пункт там стоит ему одной строки чтения; пропущенный не стоит ничего сегодня и теряет находку навсегда.

### 2.2. Выход — один файл `RAZBOR-kartoteka.md`

Самоценный: владелец читает ТОЛЬКО его, ни маткостяк, ни этот заход открывать не должен.

- Шапка: дата данных, команда состава и её живой вывод, охват («разобрано X из Y»), **три числа — |A|, |B|, |C|** и строка «настоящий долг картотеки = |B| + |C|».
- Три раздела, в каждом таблица: `блок` · `о чём он одной строкой` · `обоснование вердикта` (для B — что за находка + предлагаемый id и род; для C — найденный id).
- Внизу — «Что я НЕ смог решить»: блоки, где вердикт не встал, с вопросом владельцу. Пустой раздел законен.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. Файл `RAZBOR-kartoteka.md` существует и закоммичен, хэш назван.
2. **|A| + |B| + |C| = числу блоков, которое напечатала команда §2.0.** Не сошлось — критерий красный, блок потерян или посчитан дважды.
3. Ни один блок не стоит в двух разделах: `grep -o '^| У\?П\?[0-9]*' | sort | uniq -d` пуст.
4. **Все id, названные в разделе C, существуют в живой картотеке:** каждый находится грепом `^id: <тот-id>$` по `KARTA-OBLASTI.md`. Битых — **0**.
5. Ни один файл вне зоны не изменён: `git --no-optional-locks show --stat` печатает ровно один путь.
6. У каждой строки раздела B заполнено «что за находка» непустой фразой по существу — «важное место» и «несущий момент» без содержания не считаются.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «долга нет», а «в категорию B попало N, разобрано X из Y». Без охвата вердикт не принимается — «проверено 2 из 41» и «41 из 41» выглядят одинаково.

## 3. ВЕРИФИКАТОР — обязателен, и он атакует список A

Операция не лоссовая (ничего не двигается), поэтому программная сверка тут не нужна. Нужна другая: **проверка на занижение.** Твой список A — то место, где ошибка невидима.

Свежему субагенту с пустым контекстом дай **только** `STANDART-uzla.md` и **только** список A: заголовки блоков и твой текст блоков из маткостяка. **Ни своих обоснований, ни вердиктов, ни этого захода** — иначе он будет соглашаться с тобой, а не проверять. Вопрос ему: «по этому стандарту — есть ли в каком-нибудь из этих блоков находка, структурная связь или мотивация, которую стандарт велит положить в картотеку? Назови блок и что именно.»

Каждый блок, который он вытащил, ты **переносишь в B** или письменно объясняешь в отчёте, почему остаётся в A — с цитатой из стандарта, а не «мне кажется, это рутина». Результат — таблица «его находки → перенёс / оставил, почему» — в отчёт целиком, включая те, где ты с ним не согласился.

Субагентов ≤5. Список A большой — режь на части, но каждую часть отдавай **свежему** агенту.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/2026-07-21_mat-kostyak/RAZBOR-kartoteka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/2026-07-21_mat-kostyak/RAZBOR-kartoteka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` («УРОК БЕЗ ЦЕНЫ») судит staged-пути.** Коммитишь свою зону — там только твоё, `--no-verify` не нужен. Хук покраснел — значит в коммит уехало чужое: **не обходи, пересобери коммит.**
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
