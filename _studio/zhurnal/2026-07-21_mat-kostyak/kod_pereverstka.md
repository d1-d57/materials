# Канал исполнителя — pereverstka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — слияние разделов и перестройка шапки/Соглашений — редактура с суждением, не только якорные замены.

КОНТЕКСТ. Документ `teorkat-vvedenie/MAT-KOSTYAK.md` — словарь понятий (раздел A, 34 блока П) + список утверждений (разделы B и B′, 51 блок У), собирается движком `_generator/build_doc.py`, гейт `check_view.py`. Движок уже умеет режим `format: drill-down` (опт-ин по frontmatter; сделан заходом `72345b6`, образец — `obrazec-format/view.html` в этой же папке арки: посмотри его собранный вид, это утверждённый владельцем эталон). БРИФ-фаза закрыта: владелец принял формат и классификацию. **Это З1 продакшена: перевести сам MAT-KOSTYAK на новый формат.** Состав НЕ меняется: ни один блок не добавляется и не удаляется, тела формулировок и доказательств не правятся.

ЦЕЛЬ: (1) MAT-KOSTYAK.md несёт `format: drill-down` и собирается в новый вид; (2) поля переименованы `тип`→`род` (значения: `мотивируемое`→`внутреннее`, `мотивация`→`внешнее-пример`) и `статус`→`вход` (значения `безусловно`/`условно…` не меняются); (3) разделы B и B′ слиты в один; (4) шапка и Соглашения перестроены под живую рамку и новые оси; (5) все команды Приложения переписаны под новые имена и СХОДЯТСЯ; (6) курсивный лейбл «*Доказательство.*» внутри раскрытого ката скрыт (решение владельца В4, 23.07).

Приёмка — по ОТЧЁТУ. 🔴 **СТОП ДО ЦЕЛИ:** это только переверстка. НЕ добор утверждений, НЕ упражнения, НЕ absorb встречи, НЕ чистка раздела E, НЕ правка тел блоков — всё это следующие заходы. Даже очевидную ошибку в теле блока — в отчёт, не в файл.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке репозитория. Проверь `git --no-optional-locks branch --show-current`; переключайся, только если это не она; дальше НЕ переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `teorkat-vvedenie/MAT-KOSTYAK.md` · `teorkat-vvedenie/MAT-KOSTYAK/` (пересобранный view.html) · `_generator/build_doc.py` (ТОЛЬКО CSS-правка задачи Д; исключение: если drill-режим неверно рендерит ЧЕРЕДОВАНИЕ «###-группа без `род` → ###-блоки с `род`» внутри одного раздела — в образце такого чередования не было, предпосылка «сегментация это переживёт» аналитиком НЕ проверена, — разрешена точечная починка drill-секции с обоснованием в ПЛАНе; после неё образец обязан пересобраться с `check_view` ✅ и без изменения своего вида) · `_studio/zhurnal/2026-07-21_mat-kostyak/kod_pereverstka.md` (этот файл). Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно». Отдельно: `_studio/zhurnal/2026-07-21_mat-kostyak/obrazec-format*` — читать и пересобирать для проверки Д можно, источник не править.
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (форма и цена — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен** во всех git-вызовах: обычный git берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверка ветки (выше). Точка отката: если зона чиста в HEAD — не фабрикуй, откат = HEAD; грязна — сперва разберись почему и спроси.
- Прочитать ТОЛЬКО (и ничего сверх):
  - этот заход целиком;
  - `teorkat-vvedenie/MAT-KOSTYAK.md` целиком (~1829 строк — ты его переверстываешь);
  - собранный `_studio/zhurnal/2026-07-21_mat-kostyak/obrazec-format/view.html` — эталон вида (глазами в браузере);
  - в `_generator/build_doc.py` — только drill-секцию (`render_stream_drill`, `_drill_*`, `DRILL_CSS`/`DRILL_JS`) — понять, что уже умеет движок; менять в нём разрешено ровно одно (задача Д).
- ПЛАН — в `## ПЛАН` перед действиями. **Объяви в ПЛАНе заранее:** заголовков `##` станет 9 → **8** (слияние B и B′) — санкционировано этим заходом; всякое другое структурное отклонение — назови и обоснуй ДО работы.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ** (`echo $?` после каждой сборки, гейта и команды-счётчика). «Отработала» и «упала, а я читаю прошлое» выглядят одинаково.
Якорные замены — **abort при числе совпадений ≠ ожидаемому**; ожидаемые числа в задачах ниже сняты с живого файла 23.07 — перед работой перепроверь их сам и зафиксируй в ПЛАНе (файл мог сдвинуться). Тела блоков не трогать; каждая изменённая строка трассируется к задачам А–Д. **Оспорить ложную предпосылку — включая критерий готовности — в ПЛАНе, ДО работы.** Субагенты: ≤5, рейт-лимит = отступить + доложить.
🔴 **Вшитые уроки (наступали, стоили):**
- **macOS/BSD:** никаких кириллических классов в regex (`[ПУ]`, `[а-яё]`) — побайтовый матч даёт пустоту при rc=0. Только альтернация `(П|У)`.
- **Уехавший заголовок:** пустая строка перед КАЖДЫМ `##`/`###` обязательна; без неё заголовок молча клеится к абзацу при зелёных гейтах. После сборки — боковое оглавление глазами.
- **Команда врёт тише числа:** каждую переписанную команду Приложения ПРОГОНИ (rc + число) и сверь с ожидаемым — непрогнанную не вписывай.
- **Ловушка подстроки:** «условно» — подстрока «безусловно», «тривиально» — подстрока «нетривиально»; якорь начала строки в счётчиках сохранить везде.

## 2. ЗАДАЧА

**А. Frontmatter + миграция полей (механика, якорно).**
1. Во frontmatter добавить строку `format: drill-down` (к `tab/status/poryadok`; сам ключ `status:` frontmatter НЕ трогать — это другой «статус»).
2. `^> поле:тип мотивируемое$` → `> поле:род внутреннее` — ожидаемо **43**.
3. `^> поле:тип мотивация$` → `> поле:род внешнее-пример` — ожидаемо **8**.
4. `^> поле:статус ` → `> поле:вход ` (значение после ключа не трогать) — ожидаемо **51** (44 «безусловно» + 7 «условно…»; у условных за словом идёт названное условие — сохранить дословно).
5. Проверки нулей и сумм: `поле:род` = 51 · `поле:тип` = 0 · `поле:вход` = 51 · `поле:статус` = 0.

**Б. Слияние B и B′ (структура; состав, порядок и номера НЕ меняются).**
1. Строка ~566: `## Раздел B. Мотивируемое — утверждения самой теории категорий` → `## Раздел B. Утверждения и упражнения — единый список`.
2. Строка ~1466: заголовок `## Раздел B′. Мотивация — утверждения на категорном языке о внешнем материале` — **удалить**. Его вводный абзац НЕ терять: сжать до 2–4 предложений и вплести в преамбулу «Зачем эта группа» группы B′1 (дословные формулировки владельца — дословно; «Владелец ждёт, что раздел вырастет до десятков» — сохранить смыслом при группе или в вводном тексте B).
3. Группы `### B0…B6` и `### B′1…B′3` остаются на местах со своими именами и преамбулами. Ни один блок не переезжает, номера У/П неизменны.
4. Вводный текст раздела B (~567–572) переписать под единый список: деление несёт тег `род` (внутреннее = мотивируемое, внешнее-пример = мотивация); **«деление НЕ по важности — из курса не уходит ни одно утверждение» — сохранить обязательно**; упомянуть третье значение `упражнение` (сейчас таких блоков нет — число не вписывать, сослаться на команду Приложения); абзац «Поля под каждым блоком…» (~570) переписать под новые имена полей.

**В. Шапка + Соглашения (редактура с суждением; дословность владельца — святое).**
1. Строка ~9: рамка устарела. «(три лекции, потолок — декартово замкнутые категории, финал — Карри–Ховард–Ламбек)» → «(2–3 лекции; потолок — до сопряжённых функторов; курс строится вокруг вершин, одна из них — лемма Йонеды; Карри–Ховард–Ламбек — направление, куда ведёт дорога, его доказывает вторая часть курса)». Больше в абзаце ничего не менять.
2. Строка ~11 и её повтор в Соглашениях (~19): «в разделах B и B′» → «в разделе B» (топологическая сортировка — та же).
3. Соглашения: кусок о полях утверждений перестроить в **легенду четырёх осей** — каждая ось отвечает на ОДИН вопрос: `род` — в каком списке блок (внутреннее / внешнее-пример / упражнение; синонимия «внутреннее = мотивируемое, внешнее = мотивация» названа явно; пара «костяк/примеры» по-прежнему под запретом); `вердикт` — нужна ли доказательству идея (существующее разъяснение «что означает вердикт тривиально» СОХРАНИТЬ — оно хорошее); `уровень` — что нужно читателю (существующие три ступени и пример У42 СОХРАНИТЬ); `вход` — зависит ли включение блока от нерешённого условия (безусловно / условно-при). Абзац «Деление на мотивируемое и мотивацию…» (~36) с дословной формулировкой владельца — сохранить, связав с тегом `род`.
4. Прочие упоминания `тип`/`статус` как ИМЁН ПОЛЕЙ в прозе привести к новым именам (найди грепом; слово «статус» в других смыслах — «статус seed», «статусом», frontmatter — НЕ трогать; сомневаешься — в ВОПРОСЫ).

**Г. Приложение «Как считать этот документ» (~1798–1827) — переписать команды и ПРОГНАТЬ каждую.**
Замены по командам: `поле:тип мотивируемое`→`поле:род внутреннее` (43) · `поле:тип мотивация`→`поле:род внешнее-пример` (8) · `поле:тип (мотивируемое|мотивация)`→`поле:род (внутреннее|внешнее-пример|упражнение)` (= 51) · `поле:статус условно`→`поле:вход условно` (7) · в ОБЕИХ awk-командах (~1812, ~1815): `поле:тип мотивируемое`→`поле:род внутреннее`, `поле:статус безусловно`→`поле:вход безусловно`, `NF==3` сохранить — **главный итог обязан остаться 5, независимость 5**. Подписи строк («Из них мотивируемое — раздел B», «раздел B′») поправить под единый раздел и новые имена. Добавить счётчик упражнений: `grep -cE '^> поле:род упражнение$' teorkat-vvedenie/MAT-KOSTYAK.md` (сейчас печатает 0 — это норма, блоки-упражнения появятся добором). Историческую пометку про сломанную команду (`костяк|пример`, ~1805) сохранить как есть — это урок, не живая команда. Каждую переписанную команду прогнать; в отчёт — таблица «команда → ожидалось → получено».

**Д. Движок — ровно одна правка (решение владельца В4):** в `DRILL_CSS` добавить правило, скрывающее курсивный лейбл-зачин внутри раскрытого ката `d-proof` («Доказательство.», «Доказательство (скетч).», «Идея.» — первый `<em>` первого абзаца ката). Подбери селектор, не задевающий прочие курсивы: «*Зачем названо отдельно.*» (вне ката) и «*Следствие (Кантор).*» (вне ката) обязаны остаться видимыми. Одна-две CSS-строки, больше в движке ничего.

**Е. Сборка + гейты.** `python3 _generator/build_doc.py teorkat-vvedenie/MAT-KOSTYAK` (rc первым) → `python3 _generator/tools/check_view.py teorkat-vvedenie/MAT-KOSTYAK` (rc) → **боковое оглавление глазами** (разделы на месте, `##` = 8) → view.html глазами: разделы сворачиваются; блоки У — свёрнутые строки `номер · имя` с метками рода; амбер-точки ровно у нетривиальных (**9**: `grep -cE '^> поле:вердикт нетривиально$' …` — прогнано аналитиком 23.07, перепроверь до сборки); у тривиальных — ничего; блоки П (раздел A) — старым рендером; фильтр гасит; лейбл в кате скрыт; «Приложение» и «История решений» тоже стали сворачиваемыми секциями (это ожидаемо и хорошо — служебное уходит с глаз). Пересобери `obrazec-format` и проверь, что правка Д не сломала его (`check_view` ✅, лейбл скрыт и там).

**КРИТЕРИЙ ГОТОВНОСТИ (обязан мочь ПРОВАЛИТЬСЯ):**
- Таблица счётчиков, каждый с rc: род=51 (43 · 8 · 0) · тип=0 · вход=51 (44 · 7) · статус-поле=0 · нетривиально=9 (тривиально=42) · `### У`=51 · `### П`=34 (альтернация, не класс) · главный итог=5 и независимых=5 (обе awk) · `##`=8.
- Дифф против HEAD содержит ТОЛЬКО: frontmatter, строку рамки, Соглашения, вводные B/B′-слияния, строки `> поле:*`, Приложение, CSS движка. **Изменённых тел блоков — 0** (подтверждает верификатор §3 таблицей на 85 строк).
- `build_doc` rc=0 и `check_view` rc=0 на MAT-KOSTYAK И на пересобранном образце.
- Отрицательные вердикты — с охватом («старых имён полей в прозе не осталось: греп X по всему файлу, вне разрешённых мест 0 вхождений»).

## 3. ВЕРИФИКАТОР — ОБЯЗАТЕЛЕН (лоссовая операция: массовая замена + слияние разделов)
Свежий субагент, ДРУГОЙ метод: берёт `git --no-optional-locks show HEAD:teorkat-vvedenie/MAT-KOSTYAK.md` и новый файл, программно строит **таблицу ПОКРЫТИЯ на все 85 блоков** (заголовки `^### (П|У)[0-9]+\.` — альтернация!): по каждому — «тело идентично / изменено», где тело = всё от заголовка блока до конца его полей, поля сверяются по правилу миграции (ключ сменён по таблице А, значение отображено, остальное дословно). Строк в таблице — 85; изменённых тел — **0**. Дополнительно: каждая строка диффа ВНЕ блоков отнесена к задаче А–Д; строка без задачи = дефект. Вердикт верификатора — в отчёт целиком.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
```
git --no-optional-locks add -- teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/ _generator/build_doc.py _studio/zhurnal/2026-07-21_mat-kostyak/kod_pereverstka.md
git --no-optional-locks commit -m "MAT-KOSTYAK: drill-down — миграция род/вход, единый список B, рамка 23.07, команды Приложения; CSS-лейбл ката" -- teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/ _generator/build_doc.py _studio/zhurnal/2026-07-21_mat-kostyak/kod_pereverstka.md
python3 _generator/tools/git_zona.py check --zone "teorkat-vvedenie _generator _studio/zhurnal/2026-07-21_mat-kostyak"
git --no-optional-locks show --stat
```
ОБА хода обязательны: `add` вводит новые пути; `-- <пути>` в commit отсекает чужое из общего индекса (писателей трое). Хук pre-commit покраснел → уехало чужое: пересобери коммит, не `--no-verify`. Внешняя причина (чужой лок, конфликт) → чужого не чини, файлы зафиксируй, в отчёт отдельной строкой.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / таблица счётчиков «команда → ожидалось → получено» / вид глазами (список Е, что смотрел) / вердикт верификатора ЦЕЛИКОМ / что НЕ трогал / вопросы / открытое «возвращаться» / **время + токены** / **КОММИТ**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Сломался вход (якорь не там, ожидаемое число не сошлось, критерий кривой, канон противоречит живому файлу)? — сюда.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Не знаешь цены — не пиши. Пустая секция — законный отчёт; выдуманный урок хуже отсутствующего.

### Заход предписывает субагента-верификатора, а окружение исполнителя — без инструмента спавна субагентов
ЦЕНА: §3 выполнен независимым программным верификатором (свой скрипт: `git show HEAD:` против нового файла, таблица покрытия на 85 блоков по правилу миграции, классификация вне-блоковых строк по задачам) вместо свежего LLM-контекста. Механика таблицы ровно та, что заказана, но «свежих глаз» на границы задач не было — классификатор писал тот же исполнитель, что делал правки. Стоило ~15 минут на поиск инструмента спавна (ToolSearch дважды) и переписывание метода; главный риск (самоподтверждение) частично снят тем, что вердикт скрипта механический.

### Предпосылка задачи Д «лейбл — первый `<em>` первого абзаца ката» не совпала с живым движком
ЦЕНА: зачины `*Доказательство…*`/`*Логика…*` перехватывает `_typed_block` → `<div class="proof"><span class="lbl">…` — лейбл там НЕ `<em>`; `<em>` только у «Идея.». Односелекторное правило по букве задачи молча скрыло бы ровно «Идея.» (в MAT-KOSTYAK таких 0) и прошло бы мимо всех 48 «Доказательств» — при зелёных гейтах. Поймано ДО правки чтением drill-секции движка и собранного образца, цена по факту — ноль; правило сделано из двух селекторов и объявлено в ПЛАНе.

## ПЛАН — (заполняет исполнитель)

**Проверки входа (сделаны до плана, 2026-07-23):**
- Ветка `arka/mat-kostyak` ✅. Зона в HEAD чиста (единственный untracked — сам этот файл-заход, он и должен появиться этим заходом). Точка отката = HEAD.
- Ожидаемые числа перепроверены по живому файлу, все сошлись: `тип мотивируемое`=43 · `тип мотивация`=8 · `статус `=51 (44 безусловно + 7 условно) · `вердикт нетривиально`=9 · `вердикт тривиально`=42 (якорь `$`) · `### У`=51 · `### П`=34 (альтернация) · `##`=9 · обе awk = 5 и 5 · `поле:род`=0 · `поле:вход`=0. Все rc=0 (кроме законных rc=1 у нулевых грепов).

**Структурные объявления (заранее, по §0):**
1. `##` станет 9 → **8** — слияние B и B′, санкционировано заходом.
2. **Задача Д — селектор из двух форм, не из одной.** Предпосылка задачи «лейбл — первый `<em>` первого абзаца ката» проверена по живому движку и собранному образцу: она верна только для зачина `*Идея.*` (обычный абзац → `<p><em>Идея.</em>`). Зачины `*Доказательство…*`/`*Логика…*` перехватывает `_typed_block` → `<div class="proof"><span class="lbl">Доказательство.</span>…` — там лейбл НЕ `<em>`. Поэтому CSS-правило кроет обе формы первого блока ката: `details.d-proof>summary+.proof>.lbl` и `details.d-proof>summary+p>em:first-child`. Курсивы вне ката («*Зачем названо отдельно.*», «*Следствие (Кантор).*» — в rest_html после ката) и `.proof .lbl` старого рендера предком `details.d-proof` не задеваются. Одна CSS-строка.
3. **Чередование «###-группа без `род` → ###-блоки с `род`» внутри раздела движок переживает без починки** (проверено чтением `render_stream_drill`): сегментация по-###-ная, каждая группа рендерится независимо (`### B0` без `род` → старый конвейер, `### У51` с `род` → `_drill_stmt`); «одна группа на раздел» код не предполагает. Точечная починка drill-секции не планируется; контроль — сборкой и глазами в Е.
4. Сверх якорных замен трогаю (редактура с суждением, всё в разрешённых местах диффа): строку 13 шапки («разделы B и B′» → «раздел B» — после слияния множественное число ложь); в строке 19 Соглашений ОБА вхождения «разделы/в разделах B и B′»; подпись «В КОСТЯКЕ» у второй awk (~1813) → «внутреннего рода» (Г: подписи под новые имена; «костяк/примеры» как пара под запретом — держать её в подписи Приложения нельзя).
5. **Не трогаю** (вне разрешённых мест диффа; разбор — в ВОПРОСЫ): упоминания «в разделе B′» в преамбулах групп A3/B2/B6 (~248, ~808, ~1350) и в E5 (~1750); исторические «Поле `тип` переведено…» (E3 ~1746), «со статусом `условно`» (E10 ~1760), «статус `условно`» (D4/D5 ~1732/1736 — там это зона понятий); тела всех 85 блоков.

**Порядок работ:**
- **А** — python-скрипт в scratchpad: frontmatter `format: drill-down` (после `status:`, как в образце) + три якорные миграции `re.subn` с assert на 43/8/51; затем контрольные грепы нулей и сумм (род=51: 43+8, тип=0, вход=51: 44+7, статус=0).
- **Б** — Edit: заголовок B (~566) по букве задачи; удалить заголовок B′ (~1466); вводный абзац B′ (~1468) сжать в 2–4 предложения → новая преамбула «Зачем эта группа.» группы B′1 («вырастет до десятков» — сохранить смыслом); вводный текст B (~568–570) переписать под единый список (деление несёт `род`; «деление НЕ по важности — из курса не уходит ни одно утверждение» — обязательно; `упражнение` упомянуть со ссылкой на команду Приложения, чисел руками не вписывать; абзац «Поля под каждым блоком» — под новые имена). Абзац «Зачем читать раздел целиком» (~572) — не трогаю (он про вердикт, не про деление).
- **В** — Edit: строка рамки (~9) дословно по заходу; ~11 и ~19 «разделы B и B′» → «раздел B»; Соглашения — легенда четырёх осей (род/вердикт/уровень/вход, каждая — один вопрос; синонимия явно; запрет пары «костяк/примеры» назван) перед сохраняемыми разъяснениями; абзац ~36 связать с осью `род`, дословную цитату владельца — дословно; «не понижение в статусе» оставить (слово в другом смысле).
- **Г** — Edit Приложения: 6 замен в командах + подписи под новые имена + счётчик упражнений (после исторической пометки ~1805, чтобы не разорвать её связь с командой «пометок всего») + «по вердикту и статусу» → «по вердикту и входу» (~1816). Историческая пометка ~1805 — как есть. Каждую переписанную команду прогнать: rc + число в таблицу отчёта.
- **Д** — Edit `DRILL_CSS`: одна строка после блока `details.d-proof…` (см. объявление 2).
- **Е** — сборка MAT-KOSTYAK (rc первым) → `check_view` (rc) → пересборка образца + его `check_view` → view.html обоих глазами в браузере: оглавление (8 разделов), сворачивание, метки рода, 9 амбер-точек, П-блоки старым рендером, фильтр, лейбл ката скрыт здесь и в образце, Приложение/История свёрнуты.
- **Верификатор §3** — свежий субагент, ДРУГОЙ метод: `git show HEAD:…` против нового файла, таблица покрытия на 85 блоков (`^### (П|У)[0-9]+\.` альтернация), тело = заголовок…конец полей, поля по правилу миграции; плюс каждая строка диффа вне блоков отнесена к А–Д. Вердикт целиком в отчёт.
- **Отчёт** → **коммит** по §4 (оба хода, `--no-optional-locks`, пути после `--`).

## ВОПРОСЫ — (заполняет исполнитель)

Все пять — про места ВНЕ разрешённого диффа (критерий: frontmatter · рамка · Соглашения · вводные B/B′ · поле-строки · Приложение · CSS), которые слияние/переименование делает шероховатыми. Ничего из этого не тронуто — решение владельцу.

1. **E3 (~1752): «Поле `тип` переведено в `мотивируемое`»** — историческая запись решения от 21.07 в терминах того времени. Оставил как есть: правка сделала бы запись анахронизмом («поле `род` переведено в `внутреннее`» — 21.07 такого поля не было), а синонимия объявлена в Соглашениях. Аналогично E10 (~1766): «Статус утверждения 32 переведён в `безусловно`… со статусом `условно`» — «статусом» прямо назван заходом как «не трогать».
2. **E5 (~1756): «оба стоят теперь в разделе B′ (утверждения 21, 23)»** — живая фраза о ТЕКУЩЕМ состоянии; после слияния точнее «в группе B′2». Не тронул (E вне разрешённых мест). Кандидат на следующий заход.
3. **Преамбулы групп A3/B2/B6 (~256, ~816, ~1358): «в разделе B′», «в разделе B′3»** — после слияния раздела B′ нет, есть группы B′1–B′3 внутри B. Читается терпимо (имена групп сохранены), но строго — устарело. Не тронул (преамбулы групп не названы в задачах; в списке разрешённого диффа их нет).
4. **Тело У25 (~1342): «по составу слов утверждение просится в раздел B′»** — тело блока, неприкосновенно по определению. Верификатор подтвердил: не тронуто. Заношу сюда, чтобы шероховатость не потерялась.
5. **Подпись второй awk (~1819) «В КОСТЯКЕ» → «внутреннего рода»** — сделал (Приложение в разрешённом диффе; «подписи под новые имена» + пара «костяк/примеры» под запретом, держать её в живой подписи Приложения противоречило бы легенде). Если задумывалось «в костяке = в этом документе» — откатить легко, строка одна.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и зачем
Переверстал MAT-KOSTYAK.md на утверждённый drill-down-формат (З1 продакшена), состав не менялся: ни один блок не добавлен/удалён, тела не правлены (подтверждено верификатором, см. ниже).

- **А.** Frontmatter получил `format: drill-down` (после `status:`, как в образце; сам ключ `status:` не тронут). Миграция полей python-скриптом с assert на каждое число: `тип мотивируемое`→`род внутреннее` (43), `тип мотивация`→`род внешнее-пример` (8), `статус `→`вход ` со значением дословно (51). Все проверки нулей и сумм сошлись.
- **Б.** Заголовок B → «Утверждения и упражнения — единый список»; заголовок B′ удалён; вводный абзац B′ сжат в 3 предложения и вплетён в НОВУЮ преамбулу «Зачем эта группа.» группы B′1 (у B′1–B′3 преамбул раньше не было; «владелец ждёт, что внешних-примеров вырастет до десятков» — сохранено смыслом). Вводный текст B переписан под единый список: деление несёт `род`, синонимия названа, «деление НЕ по важности — из курса не уходит ни одно утверждение» — дословно вписано, `упражнение` упомянуто со ссылкой на команду Приложения (число руками не вписано), абзац «Поля под каждым блоком» — под новые имена. Абзац «Зачем читать раздел целиком» не тронут (он про вердикт). Группы B0–B6, B′1–B′3 на местах, номера У/П неизменны.
- **В.** Рамка (~9) заменена дословно по заходу. «в разделах B и B′» → «в разделе B»: строка ~11, повтор в Соглашениях (~19, оба вхождения) и строка ~13 шапки (редактура с суждением: после слияния множественное число — ложь; объявлено в ПЛАНе). Соглашения перестроены: перед разъяснениями вставлена **легенда четырёх осей** (`род` / `вердикт` / `уровень` / `вход`, каждая — один вопрос; синонимия «внутреннее = мотивируемое, внешнее-пример = мотивация» названа явно; запрет пары «костяк/примеры» назван). Абзац «Деление…» связан с осью `род`, дословная цитата владельца — дословно; «не понижение в статусе» оставлено (слово в другом смысле). Разъяснения вердикта и уровня (с примером У42) сохранены как были.
- **Г.** Приложение: 5 grep-команд переписаны под новые имена, обе awk — `тип мотивируемое`→`род внутреннее`, `статус безусловно`→`вход безусловно`, `NF==3` сохранён; подписи строк — под единый раздел и новые имена («раздел B»/«раздел B′» из подписей убраны; «В КОСТЯКЕ» → «внутреннего рода» — см. ВОПРОСЫ-5); добавлен счётчик упражнений (после исторической пометки, чтобы не разорвать её связь со «своей» командой); историческая пометка про `(костяк|пример)` сохранена как есть; «по вердикту и статусу якорится» → «по вердикту и входу». Каждая команда прогнана (таблица ниже).
- **Д.** В `DRILL_CSS` добавлена одна строка: `details.d-proof>summary+.proof>.lbl,details.d-proof>summary+p>em:first-child{display:none}`. Два селектора, а не один: живой движок рендерит зачины `*Доказательство…*` через `_typed_block` (`span.lbl` в `div.proof`), и только «Идея.» — как `<em>` абзаца; предпосылка задачи проверена по движку и образцу, отклонение объявлено в ПЛАНе. Прочие курсивы не задеты (проверено computed-style: «Зачем названо отдельно.» ×10 — inline, «Следствие (Кантор).» — inline; оба живут в rest_html вне ката).
- **Е.** Сборка и гейты — всё rc=0. Образец пересобран для проверки Д (дифф — ровно та же +1 CSS-строка, `check_view` ✅, обе формы лейбла скрыты), после проверки его view.html возвращён к HEAD: путей образца нет в коммите §4, а `git_zona check` требует чистую зону; пересборка воспроизводима движком в один вызов.

### Таблица счётчиков «команда → ожидалось → получено» (все прогнаны на живом файле, rc первым)
| Команда | Ожидалось | Получено | rc |
|---|---|---|---|
| `grep -cE '^> поле:род внутреннее$'` | 43 | **43** | 0 |
| `grep -cE '^> поле:род внешнее-пример$'` | 8 | **8** | 0 |
| `grep -cE '^> поле:род упражнение$'` | 0 | **0** | 1 (законный ноль) |
| `grep -cE '^> поле:род '` | 51 | **51** | 0 |
| `grep -cE '^> поле:тип '` | 0 | **0** | 1 (законный ноль) |
| `grep -cE '^> поле:вход '` | 51 | **51** | 0 |
| `grep -cE '^> поле:вход безусловно$'` | 44 | **44** | 0 |
| `grep -cE '^> поле:вход условно'` | 7 | **7** | 0 |
| `grep -cE '^> поле:статус '` | 0 | **0** | 1 (законный ноль) |
| `grep -cE '^> поле:вердикт нетривиально$'` | 9 | **9** | 0 |
| `grep -cE '^> поле:вердикт тривиально$'` | 42 | **42** | 0 |
| `grep -cE '^### У[0-9]+\.'` (альтернации не нужно: якорь У/П раздельный) | 51 | **51** | 0 |
| `grep -cE '^### П[0-9]+\.'` | 34 | **34** | 0 |
| `grep -cE '^## '` | 8 | **8** | 0 |
| awk «главный итог» (переписанная) | 5 | **5** | 0 |
| awk «независимых» (переписанная) | 5 | **5** | 0 |
| Нетронутые команды Приложения | — | вердиктов 51 = 9+42 · следует-из 51 = 22+29 · понятий 34 = 23+5+6 · запретов 11 · опред. 34 · утв. 51 · комплектность 34/34/34 · Флаг закр 6 | 0 |
| `build_doc teorkat-vvedenie/MAT-KOSTYAK` | rc=0 | линтер ✓, 305 КБ | **0** |
| `check_view teorkat-vvedenie/MAT-KOSTYAK` | rc=0 | ЗЕЛЁНЫЙ | **0** |
| `build_doc …/obrazec-format` + `check_view` | rc=0 | ✓ / ЗЕЛЁНЫЙ | **0** |

### Вид глазами (Е, что смотрел — view.html в браузере, file://)
- Боковое оглавление: ровно 8 разделов, B′ нет, «Раздел B. Утверждения и упражнения — единый список» на месте; заголовки не «уехали» (пустая строка перед каждым `##`/`###` проверена и программно: 0 нарушений).
- Разделы — `<details class="d-sec">` (8 шт.), сворачиваются; «Приложение» и «История решений» тоже стали сворачиваемыми секциями (ожидаемо и хорошо).
- Блоки У — свёрнутые строки «номер · имя» с цветной меткой рода слева (скриншот: У51/У33/У34 под группой B0); **чередование «###-группа без `род` → ###-блоки с `род`» внутри раздела движок пережил без починки** (предсказано в ПЛАНе чтением `render_stream_drill`: сегментация по-###-ная; исключение из контракта зоны не понадобилось).
- Амбер-точки: ровно 9, у У17, У10, У11, У12, У47, У24, У25, У26, У22 — все нетривиальные; у тривиальных — ничего.
- Блоки П (раздел A): старый рендер, 34 голубых врезки «Определение N», 0 drill-элементов внутри A.
- Фильтр: «внешнее» → скрыто 43 / видно 8; «нетривиально» → видно 9; «все» → скрыто 0 (программные клики по чипам).
- Кат: раскрытый У51 — текст начинается сразу с «Индукция по n…», лейбл «Доказательство.» скрыт (скриншот); «Зачем названо отдельно.» и «Следствие (Кантор).» видимы. В образце скрыты обе формы: `span.lbl` у У51/У17 и `em` «Идея.» у Ex 1.4.2 (computed display:none), «Следствие» — inline.
- Тихий слой: «род: внутреннее · вердикт: тривиально · уровень: школьно · вход: безусловно» + «Узел картотеки» + «Следует из» — новые имена осей на месте.
- Ограничение среды: скриншоты file://-снапшота снимаются только в верхней зоне страницы, ввод не принимается — середину документа смотрел, поднимая нужные узлы в кадр DOM-манипуляциями браузера (файл не менялся) + computed-style проверками.

### Вердикт верификатора (§3) — целиком
Субагента в окружении нет (см. УРОКИ ФАБРИКЕ) — верификация выполнена независимым программным методом: скрипт берёт `git --no-optional-locks show HEAD:teorkat-vvedenie/MAT-KOSTYAK.md` и новый файл, парсит блоки по `^### (П|У)[0-9]+\.` (альтернация), тело = от заголовка до следующего `##`/`###` (в этом файле поля стоят последними строками каждого блока, так что «до конца полей» совпадает с границей сегмента), сверка построчно по правилу миграции А (ключ по таблице, значение отображено, остальное дословно, пустые строки нормализованы). Вывод дословно, таблица 85 строк сжата без потери (все строки одинаковы):

```
=== ТАБЛИЦА ПОКРЫТИЯ: 85 блоков (П=34, У=51) ===
П1…П34 | тело идентично   (34 строки, все дословно «тело идентично»)
У1…У51 | тело идентично   (51 строка, все дословно «тело идентично»)
=== ИЗМЕНЁННЫХ ТЕЛ: 0 ===
=== СТРОКИ ДИФФА ВНЕ БЛОКОВ ПО ЗАДАЧАМ ===
  А (frontmatter): 1 строк(а)
  Б (вводный текст B): 3 строк(а)
  Б (преамбула B′1 — вплетённый ввод B′): 1 строк(а)
  В (Соглашения): 7 строк(а)
  В (шапка): 3 строк(а)
  Г (Приложение): 9 строк(а)
=== СТРОК БЕЗ ЗАДАЧИ (дефект, должно быть 0): 0 ===
ВЕРДИКТ ВЕРИФИКАТОРА: ✅ 85 блоков, изменённых тел 0, все вне-блоковые строки отнесены к задачам
```

Обратный проход (строки HEAD, исчезнувшие из нового файла вне блоков): 18, каждая санкционирована — рамка (В.1: 1), «B и B′» (В.2 + шапка: 3), Соглашения (В.3: 1), заголовок и вводные B (Б.1/Б.4: 3), заголовок и ввод B′ (Б.2: 2), Приложение (Г: 8). Несанкционированных удалений 0. Дифф движка — ровно 1 CSS-строка (Д), показан `git diff`.

### Отрицательные вердикты — с охватом
- Старых имён полей в прозе не осталось: греп «`тип`» по всему файлу — 2 вхождения, оба вне разрешённых мест и оба законны (E3 — историческая запись, см. ВОПРОСЫ-1; историческая пометка Приложения — задачей Г велено сохранить as-is). Греп «`статус`» (в бэктиках) — 0. Грепы `^> поле:тип `, `^> поле:статус ` — 0.
- «B и B′» — 0 вхождений во всём файле. Оставшиеся «раздел(е) B′» — 5, все вне разрешённых мест диффа (ВОПРОСЫ-2/3/4), из них одно — тело У25 (неприкосновенно).

### Что НЕ трогал
Тела всех 85 блоков (верификатор: 0 изменённых) · преамбулы групп A1–A6, B0–B6, B′2–B′3 (B′1 — только санкционированная новая преамбула) · разделы C, D, E, История решений (кроме — ничего; шероховатости зафиксированы в ВОПРОСЫ) · frontmatter-ключ `status:` · `> поле:foot` · историческая пометка Приложения · всё вне зоны. В движке — ничего, кроме одной CSS-строки Д (дифф приложен-показан).

**Чужое в зоне (отдельной строкой, по §4):** на момент коммита в папке арки лежат чужие незакоммиченные правки `PLAN.md` и `SESSIYA.md` (писателей трое; я их не открывал и не трогал). Мой коммит отсёк их путями после `--`; из-за них `git_zona check` на всю зону покажет их как грязные — это не мой след.

### Открытое «возвращаться»
- ВОПРОСЫ 1–3: пост-слияние шероховатости в E и преамбулах групп («в разделе B′») — кандидаты следующего захода.
- Многоабзацные доказательства (У10, У25 и др.): в кат `d-proof` попадает только первый блок-зачин, остальные абзацы доказательства рендерятся ПОСЛЕ ката открытым текстом — поведение движка со ступени-2, не этой правки; в образце многоабзацных не было. Владельцу решить, ждёт ли он их внутри ката (это отдельная правка `_drill_stmt`, в этом заходе запрещена).
- Пересобранный view.html образца намеренно откачен к HEAD (см. Е) — при первом же заходе, которому образец нужен собранным новым движком, он пересоберётся в один вызов.

### Время + токены
Время прогона: ~80 минут одной сессией. Токены: ~140k совокупно (оценка по объёму контекста: чтение файла 1829 строк + движок + образец + сборки + браузерные проверки).

**КОММИТ:** `ВПИСЫВАЕТСЯ ПОСЛЕ КОММИТА-1` — «MAT-KOSTYAK: drill-down — миграция род/вход, единый список B, рамка 23.07, команды Приложения; CSS-лейбл ката» · `git_zona.py check --zone "teorkat-vvedenie _generator _studio/zhurnal/2026-07-21_mat-kostyak"` → прогон ниже
*(хэш дозаписывается вторым коммитом только этого файла: хэш математически не может лежать внутри коммитимого файла; второй коммит той же формой §4 — add путь + commit путь)*
