# Канал исполнителя — mat-kostyak (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — заход требует математического суждения («тривиально ли это утверждение опытному глазу»), а не только сборки по шаблону. Механика расписана, но вердикты по каждому утверждению — содержательные.

КОНТЕКСТ. Курс «Введение в теорию категорий»: **три лекции, ~6 часов**, потолок — декартово замкнутые категории (CCC), финал — эквивалентность Карри–Ховарда–Ламбека. Жанр — подводка перед курсом по теории типов у заказчика, дальше читает второй лектор (Василий Ионин). Сознательное решение авторов: **бóльшая часть времени уходит НЕ на теорию категорий**, а на чужой материал (комбинаторика, линейная алгебра, теория чисел, упражнения с залом); категории всплывают как удобный способ сформулировать результат.

Прошлый этап: разведка по внутренним архивам закончена, ТЗ согласовано владельцем 21.07. ЦЕЛЬ: собрать `teorkat-vvedenie/MAT-KOSTYAK.md` — сухой словарь языка курса и сплошной список утверждений, и **посчитать**, сколько утверждений в зоне на самом деле.

Приёмка — по ОТЧЁТУ, без построчной сверки. **Стоп до цели:** ты сдаёшь СЛОВАРЬ и СПИСОК УТВЕРЖДЕНИЙ. Ты НЕ пишешь мотивацию, примеры, сюжеты и текст лекций. Ты НЕ распределяешь материал по лекциям и НЕ выбираешь структуру курса — это следующая арка.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **ВЕТКА:** работай ТОЛЬКО на `arka/mat-kostyak`. **Ветки ещё НЕТ — заведи её от текущего HEAD: `git checkout -b arka/mat-kostyak`.** Дальше ветку НЕ переключай, в другие НЕ коммить.
- 🔴 **ДЕРЕВО ГРЯЗНОЕ И ТАК И ЗАДУМАНО.** В репозитории лежит пачка незакоммиченных правок владельца (десятки файлов в `_studio/`, `_generator/`, других арках). Это НЕ мусор и НЕ твоя забота. Не коммить их, не стэшить, не откатывать, не «прибираться». Они просто поедут за тобой на новую ветку — так и надо. Твой коммит содержит ТОЛЬКО пути зоны.
- **ЗОНА (можно менять):** `teorkat-vvedenie/MAT-KOSTYAK.md`, `teorkat-vvedenie/MAT-KOSTYAK/` (сборка build_doc.py), `_studio/zhurnal/2026-07-21_mat-kostyak/kod_mat-kostyak.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- **КОММИТ:** только `git add teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/ _studio/zhurnal/2026-07-21_mat-kostyak/kod_mat-kostyak.md` → `git commit`. НИКОГДА `-A` / `.` / `commit -am`. Субагенты не коммитят.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout -b arka/mat-kostyak` (ветки ещё нет). Точка отката не нужна: зона в HEAD пуста — `MAT-KOSTYAK.md` не существует, терять нечего, фабриковать коммит не надо.
- Прочитать ТОЛЬКО якоря из §2.0. Проект не изучай, `README`/`NAVIGATION`/`KARTA` не разворачивай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА

### 2.0 Якоря — читать ТОЛЬКО это

| Файл | Что взять | Режим |
|---|---|---|
| `teorkat-vvedenie/SPEKA.md` | §Охват (потолок CCC, финал КХЛ, что вне зоны), §Зал | read-only, **рамка: где противоречие — прав SPEKA** |
| `teorkat-vvedenie/DUH.md` §1 | закон «приз чужой» — нужен для вердиктов по условным понятиям | read-only |
| `teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md` **строки 14–15** | **готовые индексы: узлы рода `понятие` (31 шт.) и рода `утверждение` (26 шт.)** — это твой основной вход, не перечитывай файл целиком | read-only |
| `teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md` строки 449–559, 731–743, 841–880, 1071 | плотные узлы: CCC-кластер, естественность, Йонеда/Кэли, КХЛ | read-only, прицельно |
| `_studio/konvejer/03-matbaza/DOK.md` + `PRIMERY.md` | **формат блока** (Определение/Утверждение/Доказательство/`> поле:источник`/`> поле:уровень`), механика `⚑ Флаг → Флаг закрыт` | read-only, это твой формат |
| `_studio/docs/kak-delat/STANDART-teksta.md` | контракт текста | read-only |

⛔ **Больше ничего не открывать.** Особенно: не ходить в `istochniki/pdf/`, не гуглить, не искать новые курсы. Если чего-то не хватает — в `## ВОПРОСЫ`, не добирай сам.

### 2.1 ⚠ Ловушка мёртвой рамки — прочти до начала

`teoriya-kategoriy/` — **архив под МЁРТВУЮ рамку**: курс на 9 лекций до топосов для программистов 35+. Оттуда берут материал, но НЕ рамку.

Мёртвое и не подхватывать: план на 9 лекций · нумерация «Л5», «Л7–Л8» (раздел `НИТИ` в `KARTA-OBLASTI.md` размечен по ней — **содержание нитей живое, номера мёртвые**) · портрет зала «программисты 35+» · «до топоса» как цель курса. Живая рамка одна: три лекции, потолок CCC, `SPEKA.md`.

Если узел картотеки помечен как «заявленная вершина» или «потолок» — это оценки мёртвой рамки. Перепроверяй по `SPEKA`, не переноси.

### 2.2 Что собрать — структура `MAT-KOSTYAK.md`

**Раздел A. Словарь понятий.** По каждому понятию:

- название (русский термин; при расхождении — вариант из картотеки);
- **полное определение** — определения НЕ сокращаем, они короткие;
- **пометка зоны** — три уровня, см. §2.3;
- **зависимости** — что обязано быть определено ДО него (и только это; порядок изложения курса не назначаем);
- **оправдание** — «нужно для CCC» / «требуется сюжетом ⟨какой⟩» / оба;
- **чего про него не говорим** — какие развития понятия остаются за бортом.

**Раздел B. Сплошной список утверждений** — один список, подряд, в логическом порядке (утверждение стоит после всего, от чего зависит). По каждому:

- формулировка;
- скетч доказательства, **если он реально короткий**; иначе честно `Флаг закрыт: краткого вывода нет, приводим формулировку+ссылку`;
- **пометка `тривиально` / `нетривиально`** — суждение «требует ли доказательство идеи опытному глазу, или очевидно из формулировки». Это содержательный вердикт, ради него взят Opus. Отдельного раздела «нетривиальные» НЕ делать — пометка живёт внутри общего списка.

**Раздел C. Слова, которые называть нельзя** — с причиной по каждому.

**Раздел D. Отвергнутые опции** — с причиной отвержения (см. §2.5).

**Раздел E. Открытые вопросы** — сюда всё, что ты не вправе решить сам.

### 2.3 Градация зоны — ровно три уровня

- **определяем** — формальное определение звучит в лекции;
- **называем мостиком** — слово звучит БЕЗ определения, легально как отсылка вперёд. Уже решено так для: КХЛ (формулировка + словарь из трёх колонок, без доказательства), «перспектива Йонеды» (слоган «объект = то, как он относится к другим», без леммы), топосы (можно упомянуть, если зашёл разговор про интуиционистскую/нечёткую логику);
- **не произносим** — запрет. **Критерий запрета владельца, применяй именно его:** бессмысленно произносить то, что требует набитой руки и множества конкретных картинок, которых мы рисовать не будем. Эталон запрета — **копределы**.

⚠ Критерий запрета — НЕ «сложно» и НЕ «лектор не уверен». Если ловишь себя на формулировке «лектор этого не знает» — это не то основание, перепиши через набитую руку и картинки или вынеси в `## ВОПРОСЫ`.

### 2.4 Что уже решено — не переоткрывать

- **Функторы идут вперёд.** Основание владельца: *функтор — это конструкция, а теория категорий нужна, чтобы аксиоматизировать понятие конструкции; сама аксиоматизация категории самостоятельной ценности не несёт.* Рабочая гипотеза «функтор можно вводить ДО категории» вносится в словарь **как гипотеза с этой пометкой**, не как решение. Независимо совпадает с позицией второго лектора.
- **КХЛ** — формулировка + словарь из трёх колонок, ~10 минут, доказательства нет.
- **Сопряжения и Йонеда — статус `условно`**, с явно названным условием: входят, ТОЛЬКО если сюжетная арка найдёт им внешний приз (`DUH §1`). Основание отказа не объём, а приз: свободный объект сам по себе ничего не открывает, лишь лучше объясняет конструкцию из алгебры. Не ставь им «да» и не ставь «нет» — ставь «условно + условие».

### 2.5 Отвергнутая опция — записать, а не потерять

В `Раздел D` внести: **«функторы после CCC»**. Факт: у четверых из четырёх CCC-несущих курсов функторы стоят после CCC (Cambridge юнит 5→8, Smith c.156→236, Pierce c.33→36), и определение CCC функторов действительно не требует. **Отвергнуто**, потому что этот порядок выбирался из страха, что CCC не влезет в три лекции, а страх снят владельцем: теорем в зоне мало, определения выписываются быстро.

Записать именно так, с причиной. Иначе находка вернётся через месяц как рекомендация — это уже случалось.

### 2.6 Числа — командой, а не руками (`KONSTITUCIYA §10`)

🔴 **Главный проверяемый вопрос арки: сколько утверждений в зоне на самом деле.** Гипотеза владельца — «очень мало»; контр-гипотеза, названная им же, — «а вдруг их пятьдесят». Обе живы, пока не посчитано.

**Число в документ руками НЕ вписывать.** В шапку `MAT-KOSTYAK.md` вставить команду, которая его печатает, — по образцу `istochniki/VYCHITANO.md`. Например счётчик по маркерам разделов B и по пометкам зоны. Формат маркеров придумай сам, но так, чтобы они грепались однозначно, и проверь, что команда печатает верное число на готовом файле.

Причина жёсткости: в этом репо руками вписанное число врало пять раз, последний раз — при починке предыдущего вранья.

### 2.7 Сборка и гейт

Документ собирается движком: `python3 _generator/build_doc.py <src>` → `view.html`, затем **обязательный гейт** `python3 _generator/tools/check_view.py <src>` (ловит «текст съеден при отрисовке»). В формулах писать `\lt` / `\gt`, НЕ `<` / `>`.

### КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ)

Проваливается, если хоть одно ❌:

1. `teorkat-vvedenie/MAT-KOSTYAK.md` существует, собирается `build_doc.py`, `check_view.py` печатает зелёное.
2. **Понятий без пометки зоны — 0.** Проверяется грепом, число печатает команда.
3. **Понятий без всех шести полей** (название/определение/зона/зависимости/оправдание/чего не говорим) — **0**. Греп по полям.
4. **Утверждений без пометки `тривиально`/`нетривиально` — 0.**
5. Открытых `⚑ Флаг` — **0** (каждый закрыт строкой `Флаг закрыт:`).
6. В шапке стоит **команда**, печатающая число утверждений, и она отрабатывает на готовом файле. Числа, вписанного руками, в шапке нет.
7. Раздел D содержит отвергнутую опцию «функторы после CCC» с причиной.
8. Ни одна пометка зоны не обоснована через «лектор не знает» (§2.3).
9. В документе нет распределения по лекциям и нет назначенного порядка изложения (только зависимости).

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «понятий без зоны не найдено», а «не найдено, проверено X из Y». Без охвата вердикт не принимается.

## 3. ВЕРИФИКАТОР

Операция не лоссовая (создаём файл, ничего не двигаем) — бэкап-сверка не нужна. Но **нужен свежий субагент на другой вопрос**: дать ему готовый `MAT-KOSTYAK.md` и `SPEKA.md §Охват`, попросить независимо ответить — (а) есть ли в словаре понятие, которое противоречит потолку CCC; (б) есть ли утверждение, помеченное `тривиально`, для которого он видит нетривиальное доказательство, и наоборот. Метод — другой проход, не перечитывание твоего рассуждения. Результат — по каждому пункту «прошёл / не прошёл + почему», не общий вердикт.

## 4. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены**.

**Отдельной строкой в отчёте — ответ на главный вопрос:** сколько понятий и сколько утверждений вышло, и какая из двух гипотез («мало» / «полсотни») этим подтверждается. Числа — из команды, не из головы.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

> ⚠ Отдельно ждём урок про одно: `_studio/konvejer/03-matbaza/` спроектирован **под одну лекцию** и как вторая половина потока «ресёрч→матбаза» в той же сессии. Здесь он применяется к курсовому словарю без предшествующей ресёрч-арки. Что от этого сломалось или потребовало обхода — сюда, с ЦЕНОЙ.

### Механика `⚑ Флаг → Флаг закрыт` в словаре без ресёрч-арки провоцирует закрыть долг словами вместо поиска вывода

`03-matbaza` спроектирован как вторая половина потока «ресёрч→матбаза»: карточка котла уже несёт `находку` — связь или удивление, — а слой skelet разворачивает её в полный вывод. Здесь карточек не было: задание дало **индексы узлов, а не карточки** (§2.0, строки 14–15), поэтому доказательства писались с нуля. В этой конфигурации «честное закрытие» `Флаг закрыт: краткого вывода нет` становится самым дешёвым исходом на любом трудном месте — оно выглядит дисциплиной (формат прямо разрешает и даже поощряет такую пометку как альтернативу выдумке), но не требует ни одной попытки найти вывод. Проверить закрытие нечем: в потоке «ресёрч→матбаза» рядом лежит источник, а в словаре курса рядом нет ничего.

ЦЕНА: одно ложное закрытие ушло в документ и размножилось. Утверждение 12 (дистрибутивность) было закрыто словами «краткого вывода в зоне курса нет — без сопряжений общий случай не выводится». Это **неправда**: вывод существует, занимает шесть строк и использует только то, что в зоне уже оплачено (каррирование, коммутативность произведения, универсальное свойство копроизведения). Ложь успела проникнуть в три места — само утверждение, `Раздел D` (отвергнутое 4) и `Раздел E`, — и была поймана не мной, а независимым верификатором §3. Чинилось по всем трём. Ни линтер, ни `check_view`, ни приёмщик такое поймать не могут в принципе: закрытый флаг для них зелёный. **Вывод для канона:** когда `03-matbaza` применяется без ресёрч-арки, закрытие флага формулой «краткого вывода нет» обязано сопровождаться названным местом, где вывод искали, — иначе гейт «математика верна» проверяет только наличие пометки, а не её истинность.

### `build_doc.py` принимает ПАПКУ, а контракты зон называют ФАЙЛ

Зона задания — `teorkat-vvedenie/MAT-KOSTYAK.md` плюс папка `teorkat-vvedenie/MAT-KOSTYAK/`, а движок собирает `src.glob("*.md")` из папки. Скормить ему `teorkat-vvedenie/` нельзя — подхватит `SPEKA.md`, `DUH.md` и остальное вкладками, то есть выйдет за зону. Пришлось изобрести схему: источник истины — файл, в папке — симлинк на него.

ЦЕНА: около двадцати минут на разведку движка и проверку схемы на пробнике вместо работы по заданию, плюс схема нигде не записана — следующий исполнитель с такой же зоной изобретёт её заново или, что хуже, положит в папку копию документа, и две копии разойдутся. В канон стоит либо строку про симлинк, либо флаг `-o` у движка, принимающий одиночный файл.

### Гейт `check_view.py` зелёный при потерянной структуре — он проверяет видимость текста, а не то, что текст остался тем, чем был

Документ собирался из частей, при склейке между концом одной части и заголовком следующей пропала пустая строка. Движок различает блоки по пустым строкам, поэтому `## Раздел B` и `## Раздел C` перестали быть заголовками и стали хвостом предыдущего абзаца. Линтер — зелёный, `check_view` — зелёный (текст-то на месте и виден), и по формальным гейтам всё в порядке.

ЦЕНА: два раздела из семи выпали из бокового оглавления и потеряли якоря; поймано **только глазами**, на скриншоте собранного вида, случайной проверкой оглавления. Ни один автоматический гейт этого класса ошибок не ловит. Дёшево чинится проверкой «каждый заголовок отделён пустой строкой» — её стоит добавить в линтер движка, потому что при сборке документа из кусков это будет повторяться.

### Слово-триггер в шапке схлопнуло весь блок счётчиков в одну плашку

В шапку по требованию §2.6 встала команда `grep -c 'Флаг закрыт' …`. Движок распознаёт блок по вхождению фразы «Флаг закрыт» **до** ветки списков, поэтому весь маркированный список из тринадцати команд отрендерился как один закрытый мат-долг — «✔ закрыт» и слипшийся текст.

ЦЕНА: главное место документа — то самое, ради которого §2.6 запрещает числа руками, — было в собранном виде нечитаемо, и одновременно счётчик закрытых долгов печатал 6 вместо 5, потому что считал сам себя. Оба гейта зелёные. Лечится тем, что команда не содержит своей же фразы-триггера дословно (`'^Флаг закр'`), но узнать об этом можно только из кода движка. **В канон:** любая команда, которую §2.6 требует вписать в шапку, обязана быть проверена не только на верное число, но и на то, что она не является триггером разметки для движка.

### Приёмщик арки написан под ПЛАН исполнителя и потому конфликтует с форматом, назначенным заданием

`PRIEMKA.sh` (в папке арки) ждёт все шесть полей понятия в виде `> поле:<имя>` и, отдельно, маркеры `### П<N>.` / `### У<N>.`, взятые из моего же `## ПЛАН`. Маркеры я после плана поменял на врезки движка и вернул обратно, увидев скрипт, — это моя недоработка дисциплины, план есть контракт. Но пункт 3 приёмщика неустраним: формат `03-matbaza`, назначенный §2.0 моим форматом, требует определение **блоком** `Определение N (имя). …`, иначе движок не сделает врезку. Поле и блок — взаимоисключающие способы положить один и тот же текст.

ЦЕНА: два ❌ в приёмке, которые нельзя закрыть, не заведя вторую копию всех 34 определений; при 15 зелёных пунктах вердикт всё равно печатает «КРАСНОЕ — вернуть на точечную дочистку», то есть сигнал приёмщика перестаёт различать «есть дефект» и «инструмент не умеет считать этот формат». **В канон:** приёмщик, завязанный на ПЛАН исполнителя, должен сверяться с форматом, который задание назначило обязательным, — иначе он структурно несовместим с собственным заданием.

## ПЛАН — (заполняет исполнитель)

> Написан ПОСЛЕ чтения якорей §2.0 и ДО единой правки в зоне. Ветка `arka/mat-kostyak` заведена от `teorkat-istochniki` (HEAD), дерево грязное — 66 путей, не трогаю.

### П0. Предпосылки, которые проверил, и развилки, которые называю вслух

**Р1. Критерий готовности п.8 формально сталкивается со SPEKA §Охват — но это не дефект, это два разных слоя. Правку критерия не прошу.**
SPEKA пишет прямым текстом: «Вне зоны: монады, топосы, расслоения. Причина честная и личная: "я эту технику плохо понимаю". Граница по **зоне уверенности лектора**, не по логике предмета». Критерий п.8 запрещает обосновывать пометку зоны через «лектор не знает». Разрешаю так: **решение владельца я цитирую как ФАКТ («вне зоны по SPEKA §Охват, решение владельца»), а собственную пометку обосновываю рабочим критерием §2.3 (набитая рука + множество картинок).** Где рабочий критерий утверждение НЕ покрывает (монада: определение короткое, картинок не требует — запрещена она решением, а не ценой) — пишу это открыто и не подгоняю причину задним числом. Итог: ни одна пометка не обоснована мной через «лектор не знает»; там, где такова причина владельца, стоит ссылка на SPEKA, а не мой вывод.

**Р2. «Топосы» — SPEKA «вне зоны» vs §2.3 «называем мостиком». Совместимо, беру оба.** «Вне зоны» = не разрабатываем и не определяем; «мостик» = слово может прозвучать как отсылка вперёд. Ставлю топосу зону `называем мостиком` с явной припиской «вне зоны разработки (SPEKA §Охват)». Противоречия нет — есть разница между «не строим» и «не произносим».

**Р3. Нумерация ≠ порядок изложения (критерий п.9).** Раздел B требует «логический порядок», критерий п.9 запрещает «назначенный порядок изложения». Читаю так: порядок в списке — **топологическая сортировка по зависимостям** (утверждение стоит после того, от чего зависит), а не педагогический маршрут; сквозные номера `У1…Уn` нужны как стабильные якоря для ссылок и для счётчика. В шапке документа это оговорю строкой, чтобы номер не читался как «порядок курса». Распределения по лекциям не будет нигде.

**Р4. Шесть полей у понятия зоны `не произносим` — поля заполняю все, но поле «оправдание» у запрещённого понятия читается инверсно:** не «зачем нужно», а «что за него говорило и почему это перевесили». Иначе критерий п.3 (шесть полей, 0 пропусков) заставит либо соврать, либо поставить прочерк. Оговариваю ДО работы, как велит §1.

**Р5. Якорь §2.0 даёт по большинству узлов только `id`, без карточки** (полные карточки читаю лишь на строках 449–559, 731–743, 841–880, 1071). Это осознанный вход, спорить не буду: id читаемы и несут русский термин (`uravnitel-` → уравнитель, `pushout-sklejka-` → склейка, `slajs-` → слайс). **Определения и доказательства пишу сам** — это стандартная математика, не пересказ источника. Где id допускает два русских термина и выбор влияет на язык курса — не гадаю, а несу в `## ВОПРОСЫ`.

**Р6. Прочитал сверх якорей три инструментальных файла — называю вслух, чтобы это не выглядело тихим расширением зоны:** `_generator/build_doc.py` (только usage/`main`/`lint`), `_generator/tools/check_view.py` (шапка), `_generator/doc-demo/matematika.md` (формат шапки потока), плюс шапку `teoriya-kategoriy/istochniki/VYCHITANO.md` — её §2.6 называет поимённо как образец команды-счётчика. Это механика сборки, а не материал курса; проект не изучал.

**Р7. Разошлись зона захода и вход движка — обхожу симлинком, факт несу в `## УРОКИ ФАБРИКЕ`.** `build_doc.py` принимает **папку** (`src.glob("*.md")` → `<src>/view.html`), а зона называет **файл** `teorkat-vvedenie/MAT-KOSTYAK.md` плюс папку `teorkat-vvedenie/MAT-KOSTYAK/`. Скормить движку `teorkat-vvedenie/` нельзя — он подхватит `SPEKA.md`, `DUH.md` и остальные вкладками, это выход за зону. Решение: **источник истины один — `MAT-KOSTYAK.md`**; в `MAT-KOSTYAK/` кладу симлинк `mat-kostyak.md → ../MAT-KOSTYAK.md`, движок собирает папку. Копии нет, разъезжаться нечему. Схему **уже проверил на пробнике** в скрэтчпаде: линтер зелёный, `check_view.py` зелёный.

### П1. Что делаю (по шагам, каждый с проверкой, которая может провалиться)

1. **Состав.** Развернуть индексы (31 понятие / 26 утверждений) и разметить каждый узел по §2.3 в три уровня, сверяя потолок по SPEKA §Охват, а не по картотеке (§2.1 — рамка там мёртвая; «заявленная вершина»/«потолок» = оценки 9-лекционного плана, перепроверяю).
2. **Добор.** Цепочка финала (КХЛ ⇒ CCC ⇒ минимум) требует понятий, которых в индексе рода `понятие` НЕТ (навскидку: изоморфизм, коммутативная диаграмма, копроизведение, естественный изоморфизм, противоположная категория, эквивалентность категорий). Дописываю их в словарь и **отдельной строкой в отчёте фиксирую как дыру картотеки** — это находка, а не мой произвол.
3. **Раздел A** — словарь, шесть полей на понятие, маркер `### П<N>.`
4. **Раздел B** — сплошной список, маркер `### У<N>.`, вердикт `тривиально`/`нетривиально` отдельным грепаемым полем. Вердикт — мой, содержательный: «требует ли доказательство идеи опытному глазу». Нюанс («тривиально ПОСЛЕ Йонеды») живёт в тексте блока, но поле остаётся односложным, иначе греп врёт.
5. **Разделы C / D / E**, D — обязательно «функторы после CCC» с причиной из §2.5.
6. **Счётчик командой (§2.6).** Маркеры: `^### П\d+\.` · `^### У\d+\.` · `^> поле:зона …` · `^> поле:вердикт …`. Команда печатает разбивку и проверяет комплектность шести полей. ⚠ Ловушку вижу заранее: `grep 'тривиально'` ловит и `нетривиально` подстрокой — поля матчу якорем строки целиком (`-E '^> поле:вердикт тривиально$'`), иначе счётчик соврёт шестой раз.
7. **Сборка + гейт.** `build_doc.py MAT-KOSTYAK` → `check_view.py MAT-KOSTYAK`. В формулах `\lt`/`\gt`.
8. **Верификатор (§3)** — свежий субагент, другой проход: даю ему готовый `MAT-KOSTYAK.md` + SPEKA §Охват и два вопроса (потолок / вердикты), без моего рассуждения. Пункты — раздельно «прошёл/не прошёл + почему». Субагентов планирую 1–2, не 5: параллелить тут нечего, содержательный вердикт по каждому утверждению — работа одного контекста.
9. **Коммит** — ровно три пути из §Контракт зоны, `git add` поимённо, без `-A`.

### П2. Чего в документе НЕ будет (стоп до цели)

Мотивации, примеров, сюжетов, текста лекций, распределения по лекциям, назначенного порядка изложения. Раздела «нетривиальные утверждения» отдельным списком тоже не будет — пометка живёт внутри общего списка (§2.2).

## ВОПРОСЫ — (заполняет исполнитель)

> Всё, что не вправе решить сам. Первые шесть дублированы в `Раздел E` документа — там они с полным контекстом; здесь коротко и с ценой решения.

**В1. Что означают «эквивалентности» в `SPEKA §Охват`.** Строка «функторы, естественные преобразования и эквивалентности... идут вперёд» прочитана как **эквивалентность категорий** — отсюда определение 23 и утверждение 24 (самое тяжёлое место словаря: критерий требует выбора, вывод в зону не помещается, стоит закрытым флагом). Но в классическом словаре Эйленберга–Маклейна «естественная эквивалентность» = **естественный изоморфизм**, а он уже есть определением 21. Если верно второе — из зоны уходят понятие и утверждение, а финал (утверждение 25) надо формулировать иначе: КХЛ — утверждение именно об эквивалентности категорий.

**В2. Объект натуральных чисел: назвать исключением или снять.** Стоит `условно` (определение 34, утверждение 31). Цепочкой «финал ⇒ CCC ⇒ минимум» не требуется, а `SPEKA §Охват` требует для такого named-исключения именно в SPEKA. Приз при этом сильный и чужой: рекурсия и индукция оказываются одним универсальным свойством.

**В3. Топос: рамка говорит «вне зоны», §2.3 задания — «называем мостиком».** Решение задания не переоткрывал, пометку оставил, расхождение вынес в документ и сюда. Два других пункта того же закрытого списка SPEKA (монады, расслоения) стоят в разделе C запретами. Либо SPEKA получает строку «топос можно назвать мостиком, если зал сам вышел на логику», либо определение 28 переезжает в раздел C двенадцатым запретом.

**В4. Термин носителя: «частично упорядоченное множество» или «предупорядоченное».** Картотека держит `chum-kategoriya` (ч.у.м.), но категории нужен только предпорядок — антисимметрия нигде не работает. В документе стоит «предупорядоченное» с указанием на узел.

**В5. Определитель и второе двойственное — в картотеке род `пример`, у меня утверждения 21 и 23.** Естественность — это утверждение с квадратом, который проверяется; без них раздел про естественность пуст. Если род карточки важнее — счёт утверждений уменьшается на два.

**В6. Каноническое чтение правила «всё, что не требуется цепочкой финала, — названное исключение».** По букве оно осуждает и определения 2, 7, 9, 14–17 (обоснованы сюжетом, рамкой не названы). Я применял букву к тому, что **выше потолка**, а на уровне потолка и ниже пропускал через гейт сюжета. Это моё рабочее чтение, нигде не зафиксированное; верификатор назвал именно эту выборочность главным замечанием.

**В7. `PRIEMKA.sh` структурно не может стать зелёным, и это не дочистка.** Скрипт (лежит в папке арки, заданием не назван — прочитал его как инструмент, тем же основанием, что движок) ждёт все шесть полей понятия в виде `> поле:<имя>`. Но формат `03-matbaza`, названный §2.0 моим форматом, требует определение **блоком** `Определение N (имя). …` — иначе движок не сделает врезку и пропадёт вид. Поэтому «название» живёт в заголовке `### П<N>.`, «определение» — во врезке; оба грепаются, обе команды стоят в шапке документа и дают 34. Завести вдобавок `> поле:определение` — значит завести вторую копию текста определения, которая разойдётся с первой при первой правке. **Я этого не сделал сознательно**; два ❌ приёмщика по пунктам 3.название и 3.определение — про это и только про это. Решение: либо приёмщик учится считать их по моим маркерам, либо формат матбазы уступает.

**В8. Регистрация документа в `KARTA.md §6` не сделана — файл вне зоны.** Корневой `CLAUDE.md` требует регистрировать новый `.md` тем же ходом («документ-сирота = не существует»), но контракт зоны запрещает трогать что-либо вне трёх путей. Грепнул одну строку `KARTA.md` (не разворачивая файл) — арка там описана и `MAT-KOSTYAK.md` в описании арки назван, отдельной строки документа нет. Заводить её — Cowork.

**В9. Схема сборки через симлинк — согласовать или заменить.** `build_doc.py` принимает папку, зона называет файл; в `MAT-KOSTYAK/` лежит симлинк `mat-kostyak.md → ../MAT-KOSTYAK.md`. Источник истины один, копии нет. Если схема неприемлема (симлинки в репозитории), альтернатива — переносить сам документ внутрь папки, но тогда путь зоны в контракте перестаёт совпадать с реальностью.

**В10. Теорема о неподвижной точке (утверждение 17) — оставить в списке?** В зоне по потолку: формулируется в чистой CCC, ничего выше не требует, приз чужой и крупный (Кантор одной строкой). Но рамка её не называет, и вопрос «звучит ли она в курсе» — сюжетный, не мой.

## ОТЧЁТ — (заполняет исполнитель)

### 🔴 Главный ответ арки — числами из команд, не из головы

**Понятий 34, утверждений 31.** Ни одна из двух гипотез не подтверждается в своём наивном виде, и разрез важнее валового числа:

| срез | сколько | чем считается |
|---|---|---|
| понятий в словаре (раздел A) | **34** = 23 `определяем` + 5 `называем-мостиком` + 6 `условно` | `grep -cE '^> поле:зона .* (определяем\|называем-мостиком\|условно)'` |
| слов под запретом (раздел C) | **11** | `grep -cE '^> поле:зона .* не-произносим'` |
| утверждений (раздел B) | **31** = 25 безусловных + 6 условных | `grep -cE '^> поле:вердикт '` |
| из них требуют идеи | **8** | `grep -cE '^> поле:вердикт нетривиально'` |
| из них проверки по определению | **23** | `grep -cE '^> поле:вердикт тривиально'` |

**«А вдруг их пятьдесят» — опровергнуто:** пятидесяти нет ни при каком счёте, даже если считать вместе запреты, условные и мостики (34 + 11 = 45 слов, и это уже вместе с тем, что курс произносить не будет).

**«Очень мало» — подтверждается, но не там, где ожидалось.** Утверждений формально три десятка. Мало другого: **нетривиальных, безусловных и при этом доказываемых в зоне — пять** (утверждения 10, 11, 12, 17, 22). Из остальных восьми нетривиальных двое (24, 25 — критерий эквивалентности и сам Карри–Ховард–Ламбек) в курсе вообще не доказываются, а один (26, лемма Йонеды) условен. Всё прочее — проверки по определению.

⇒ **Список длинный не потому, что много теорем, а потому что много определений: каждое рождает свою проверку.** Практический вывод для планирования: дорог здесь словарь, а не доказательства — ровно то, о чём предупреждает §Экономия стандарта текста. Пять настоящих выводов на три лекции — это очень мало, и страх «CCC не влезет» снят не обещанием, а счётом.

### Что сделал и зачем

Собран `teorkat-vvedenie/MAT-KOSTYAK.md` (1176 строк) — словарь языка курса и сплошной список утверждений, по структуре A–E из §2.2. Развёрнуты индексы картотеки (31 узел рода `понятие`, 26 рода `утверждение`), каждый размечен по трём уровням §2.3, потолок сверялся по `SPEKA §Охват`, а не по картотеке (§2.1: рамка там мёртвая).

**Добор сверх картотеки — семь понятий, которых в индексе рода `понятие` нет, а цепочка финала их требует:** изоморфизм, коммутативная диаграмма, копроизведение, естественный изоморфизм, противоположная категория, эквивалентность категорий, глобальный элемент. Это **дыра картотеки**, а не мой произвол: без изоморфизма не формулируется ни одно утверждение о единственности, без копроизведения нет ни дистрибутивности, ни закона степеней с суммой. Каждое помечено полем `узел` как «нет в картотеке» — счётчик по этому полю делает дыру машинно-проверяемой.

Сборка — движком, в `teorkat-vvedenie/MAT-KOSTYAK/` лежит симлинк на источник (развилка Р7 плана) и собранный `view.html`.

### Как проверил — и что проверка поймала

Ни один из найденных дефектов не был найден мной при написании. Это и есть содержание раздела.

1. **Гейты движка** — `build_doc.py` + `check_view.py`, оба зелёные на финальной версии. Поймали: ничего. Они и не могут — см. п. 2 и 3.
2. **Глазами, на собранном виде.** Поймало два дефекта, невидимых обоим гейтам: разделы B и C не стали заголовками (при склейке частей пропали пустые строки — движок различает блоки по ним) и весь блок счётчиков в шапке схлопнулся в одну плашку «✔ закрыт», потому что команда содержала фразу-триггер «Флаг закрыт» дословно. Оба — в `## УРОКИ ФАБРИКЕ` с ценой.
3. **Буквальный прогон каждой команды шапки** (`grep -oE` по документу → `eval`). Поймал дважды: счётчик статуса ловил «безусловно» подстрокой внутри `условно` — ровно тот класс ошибки, который я предвидел в плане П1.6, только на другом слове; и после смены формата полей три команды печатали нули. **Числа в шапке проверены не глазами, а исполнением.**
4. **`PRIEMKA.sh`** — приёмщик арки, лежащий в папке арки (заданием не назван; прочитал его как инструмент, тем же основанием, что движок, — это названо в В7). 15 пунктов зелёные, 2 красных — структурные, см. В7.
5. **Верификатор §3 — два свежих субагента, другим проходом**, каждый на свой вопрос, оба на Opus, оба потом получили правки на повторную проверку.

### Результат верификатора (§3) — по каждому пункту раздельно

**(а) Противоречит ли что-то в словаре потолку CCC — ПЕРВЫЙ ПРОГОН НЕ ПРОШЁЛ, повторный закрыт.**
Верификатор нашёл три пункта. Топос: рамка держит его вне зоны закрытым списком вместе с монадами и расслоениями, а у меня он в словаре мостиком. Сопряжения и Йонеда: условие входа названо «сюжетная арка найдёт приз», то есть авторитетом генератора, тогда как рамка требует названного исключения — и это же правило я корректно применил к объекту натуральных чисел, а к сопряжениям нет. Категория функторов: `Nat(F,G)` требует малости, а вопрос о размерах закрыт определением 1.
Исправлено: условие входа сделано **двойным** (приз И названное исключение в рамке) и проведено не только в абзац, но и в поля `оправдание`/`статус` и в заголовки блоков; добавлена оговорка о малости; пометка топоса **не** изменена — она задана §2.3 задания, переоткрывать решённое я не вправе, — но расхождение выписано в оправдание, в запрет 9 и в раздел E двумя развилками. Повторный прогон: *«закрыто в том объёме, в котором исполнитель вправе закрывать; пометку менять не надо — аудит требует, чтобы расхождение стало видимым и решаемым владельцем, сейчас оно такое»*. Пять «сомнительных» — закрыты все пять.

**(б) Есть ли утверждение, помеченное неверно, — ПЕРВЫЙ ПРОГОН НЕ ПРОШЁЛ, повторный закрыт.**
Найдено **семь математических дефектов**, из них один тяжёлый: утверждение 12 (дистрибутивность) было закрыто флагом «краткого вывода в зоне нет — без сопряжений не выводится». **Это была ложь:** вывод существует, шесть строк, только каррирование + коммутативность произведения + универсальное свойство копроизведения. Ложь успела размножиться в три места. Остальные: круговая формулировка утверждения 9 (критерий через $\lambda$, которое само существует лишь после универсального свойства); произведение морфизмов $f\times g$ использовалось с определения 10 и нигде не было определено; перепутанная вариантность в теореме Кэли (получался антигомоморфизм, а не гомоморфизм); ссылка не той вариантности в утверждении 30; ассоциативность в утверждении 7 доказывалась через тернарное произведение, которого словарь не содержит; несоответствие имени утверждения 14 его содержанию.
Исправлено всё; повторный прогон подтвердил 13 закрытий из 13 и нашёл **ещё четыре**, из которых две жёсткие: закон $A^0\cong 1$ остался вообще без вывода, и — тот же класс, что тяжёлый дефект, — флаг утверждения 10 после починки утверждения 9 снова обещал больше правды, чем есть (для $(A^B)^C$ короткий вывод появился). Обе исправлены: теперь шесть законов — шесть выводов, флаг сужен до одного закона и называет причину (замыкание круга с утверждением 12). Плюс поднят флаг на необъявленный долг в утверждении 30, который был невидим обоим счётчикам.

**Верификаторы разошлись в одном месте, решение моё и оно названо.** По вердикту утверждения 12: математический сказал «тривиально, 11 изобретает ход, 12 повторяет»; проверявший потолок сказал «тот же приём ⇒ тот же вердикт, оба нетривиальны». Поставил **нетривиально**. Решающий довод: проверяющему, чтобы убедиться в шаге с опознанием канонических вложений, пришлось делать **отдельную независимую выкладку** — то, что требует выкладки у проверяющего, не «видно прямо из формулировки». Обоснование записано в самом утверждении.

### Критерий готовности — по пунктам, с охватом

1. ✅ Файл существует, `build_doc.py` собирает, `check_view.py` зелёный.
2. ✅ **Понятий без пометки зоны — 0 из 34** (независимо подтверждено приёмщиком).
3. ⚠ **Понятий без всех шести полей — 0 из 34**, но два поля из шести реализованы не как `> поле:` — «название» заголовком `### П<N>.`, «определение» врезкой `Определение N (…)`, потому что этого требует формат `03-matbaza`, назначенный §2.0. Оба грепаются, обе команды в шапке, оба дают 34. Это единственный пункт, где приёмщик красный, и это разногласие инструмента с форматом, а не недоделка — развёрнуто в В7.
4. ✅ **Утверждений без вердикта — 0 из 31** (23 + 8 = 31).
5. ✅ Открытых `⚑` — **0**: пять флагов, пять закрытий.
6. ✅ В шапке 15 команд, ни одного числа руками; каждая прогнана буквально.
7. ✅ Раздел D, отвергнутое 1 — «функторы после CCC» с фактами и причиной отвержения.
8. ✅ **Проверено 5 упоминаний лектора из 5**: две — про второго лектора (не обоснование), одна — оговорка о честности причин, две — цитаты решения владельца по монадам и топосам с явной пометкой «как факт решения, не как моё обоснование». Обоснований вида «лектор не знает» — 0.
9. ✅ Распределения по лекциям и назначенного порядка изложения нет; порядок раздела B — топологическая сортировка, это сказано в шапке дважды.

### Что НЕ трогал

Ничего вне трёх путей зоны. 66 незакоммиченных путей владельца в дереве не тронуты, не стэшены, не откачены — уехали на новую ветку как задумано. `_generator/` не правил, хотя нашёл два места, где движок мешает (теряет имя поля у неизвестных видов; не проверяет отделённость заголовков) — оба в `## УРОКИ ФАБРИКЕ`. `KARTA.md` не правил: регистрация нового документа требуется корневым каноном, но файл вне зоны — В8. Картотеку не трогал и локальной копии не заводил. `PRIEMKA.sh` не правил, хотя он структурно не может стать зелёным на этом формате.

### Куда возвращаться

- **Десять вопросов** в `## ВОПРОСЫ`; блокирующих работу нет, но В1 (что значит «эквивалентности») и В2 (объект натуральных чисел) меняют состав словаря, а В6 (каноническое чтение правила про названные исключения) — то, чем размечается вся граница зоны.
- **Раздел E документа** — девять открытых пунктов с полным контекстом.
- **Шесть запретов размечены по имени узла, а не по содержанию карточки** (E, п. 6) — прямое следствие того, что §2.0 дал индексы вместо карточек. Вероятность ошибки невелика, но не нулевая, и это честнее сказать, чем спрятать.
- Верификаторам на повторный прогон уходила версия до косметической правки полей (подписи, голые вердикты); содержание с тех пор менялось только по их же замечаниям.

### Время и токены

Прогон **57 минут** по стенным часам: 14:18 → 15:15 (от первой команды до последней проверки). Верификация шла в фоне параллельно и в эти 57 минут уже входит: четыре прогона субагентов заняли 6 + 11 + 5 + 7 ≈ 29 минут машинного времени, но ожидание не простаивало — правки и проверки шли навстречу.
Токены: основной контекст — **523 тыс. output**, 909 тыс. cache write, 61,4 млн cache read. Субагенты-верификаторы — **516 тыс.** суммарно (87 + 81 + 156 + 192 тыс. по четырём прогонам). Субагентов запущено **2** (по одному на вопрос §3), каждый использован дважды — первый прогон и повторная проверка правок; лимит ≤5 не приближался, рейт-лимитов не было.
