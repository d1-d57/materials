# Канал исполнителя — riehl-a (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — постраничный проход главной книги с отбором и формулировками; суждение в ходе.

КОНТЕКСТ. `teorkat-vvedenie/MAT-KOSTYAK.md` — язык курса теории категорий: словарь `### П<N>.` + единый список `### У<N>.`/`### Ex <N.M>.` с полями `род`/`вердикт`/`уровень`/`вход` (легенда — в «Соглашениях документа», ПРОЧТИ первыми). **Riehl «Category Theory in Context» — ГЛАВНАЯ книга v2** (решение владельца и второго лектора, встреча 22.07); рабочий диапазон подводки §1.4–§3.8. В v1 книга проверена ТОЛЬКО по крупному оглавлению и индексу нумерованных результатов — этим же движением у Лейнстера получен ложный отрицательный вывод («нет CCC» — есть, внутри параграфа с чужим названием), и **класс «утверждение, спрятанное в замечании при определении» по Riehl не закрыт вовсе** (дайджест `riehl` в VYCHITANO, раздел «Долг»). Это З3а: **постранично §1.4–§2.4** (естественность → эквивалентность → универсальные свойства → Йонеда → категория элементов). Гл. 3 (пределы) — следующий заход З3б, не твой.

ЦЕЛЬ: (1) каждая страница диапазона открыта, каждый математический факт диапазона получил вердикт (есть в документе / внести / вне зоны / контекст); (2) недостающее внесено полными блоками; (3) упражнения диапазона отобраны и внесены Ex-блоками (формат ниже); (4) дайджест-слой в VYCHITANO дописан; (5) счётчики сходятся.

Приёмка — по ОТЧЁТУ. 🔴 **СТОП ДО ЦЕЛИ:** только §1.4–§2.4 Riehl. НЕ гл.3+ (З3б), НЕ другие книги (З4), НЕ картотека (развод зон — у всех новых блоков `узел`: «нет в картотеке (карточка после развода зон)»), существующие тела блоков НЕ править ни строкой.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak`. Проверь `git --no-optional-locks branch --show-current`; дальше НЕ переключай.
- **ЗОНА (можно менять):** `teorkat-vvedenie/MAT-KOSTYAK.md` · `teorkat-vvedenie/MAT-KOSTYAK/` (пересборка) · `teoriya-kategoriy/istochniki/VYCHITANO.md` (ТОЛЬКО дописать слой в секцию `## riehl`, ничего не удалять) · `_studio/zhurnal/2026-07-21_mat-kostyak/kod_riehl-a.md` (этот файл). PDF Riehl — читать. Всё прочее — **READ-ONLY**; чужие незакоммиченные файлы в дереве не трогать.
- **КОММИТ:** `add` → `commit -- <пути>` (§4). НИКОГДА `-A`. `--no-optional-locks` везде. Вложенные субагенты не коммитят.
- **ЗАПРЕТ:** ничего за пределами зоны.

## 0. ПЕРВЫЙ ХОД
- Проверка ветки; зона чиста (HEAD после захода absorb-vstrechi).
- Прочитать ТОЛЬКО: этот заход · `MAT-KOSTYAK.md` ЦЕЛИКОМ (свежий — его только что пополнил absorb; сними ЖИВЫЕ счётчики и последний занятый У-номер) · секцию `## riehl` в `teoriya-kategoriy/istochniki/VYCHITANO.md` (там: **смещение pdf−20=печ.**, адреса уже сопоставленных У19/У35/У36/У37/У44/У45/У46, долг) · PDF `teoriya-kategoriy/istochniki/pdf/riehl-category-theory-in-context.pdf` по диапазону задачи.
- ПЛАН — до правок: точные ПЕЧАТНЫЕ границы §1.4–§2.4 (по оглавлению PDF), число страниц диапазона, живые счётчики документа ДО, объявленный подход к покрытию.
⚠ **Не подыгрывай.** Заход проверяет гипотезу аналитика («в документе почти всё из гл.1–2 уже есть») — её ОПРОВЕРЖЕНИЕ находками — полноценный успех. Обратная ловушка: форма книги ≠ существо предмета — Riehl говорит языком категории элементов и 2-категорий там, где предмету хватает словаря курса; вопрос ставь «что предмету НУЖНО», а не «как делает автор», и отчитайся, поддался ли.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 Код возврата — первым. Пустая строка перед каждым `###`. Никаких кириллических классов в regex. Существующие тела НЕ трогать. Метрика — качество; **скорость не важна, полнота важна** (постранично значит постранично: Read PDF страницами, до 20 за вызов).
🔴 **Математика:** каждое новое доказательство/идея — через скилл `proof-self-review` ДО вписывания; вердикты в отчёт. Вердикт/уровень — по легенде Соглашений, не по красоте. Формулировки теорем — своими словами В СТИЛЕ документа (плотный русский, соседние блоки — образец), с адресом источника; НЕ переводить дословно страницами (авторское право: краткая формулировка + свой скетч — норма, копия абзацев — нет).
🔴 **Отрицательный вывод несёт охват:** «в §X.Y нового для документа нет» законно только с постраничным покрытием этого §.

## 2. ЗАДАЧА

**А. Постраничный проход §1.4–§2.4** (печ. границы сними с оглавления; pdf-стр = печ+20). По каждой странице: математические единицы (определения, леммы/предложения/теоремы, примеры с содержанием, **замечания-с-математикой при определениях** — прицельный класс, долг v1!, упражнения) → каждой вердикт:
- **есть** — уже в документе (назови У/П; сверься с картой VYCHITANO и живым файлом);
- **внести** — попадает в зону курса (потолок «до сопряжений»; §1.7 2-категории и всё, что требует набитой руки вне зала, — скорее «вне зоны», решай по духу документа и его разделу C) и в документе отсутствует → новый блок;
- **вне зоны** — с причиной одной фразой;
- **контекст** — математика-иллюстрация без статуса утверждения курса.

**Б. Внесение утверждений.** Новые `### У<N>.` (сквозная нумерация с живого последнего номера) в правильные группы по топологической сортировке; полный набор полей; `род` по легенде (утверждение о произвольной категории — внутреннее; о конкретной — внешнее-пример); доказательства: тривиальные — полностью, нетривиальные — скетч или честное «вне-школы»; каты в drill-формате соберутся сами. **Правки полей СТАРЫХ блоков запрещены** — если новое утверждение делает старый «независимый ход» производным, НЕ правь его `следует-из`, а внеси наблюдение в `## ВОПРОСЫ` («кандидат на пере-разметку следует-из, решение аналитика»).

**В. Упражнения.** Инвентаризуй ВСЕ упражнения диапазона (Ex 1.4.i…2.4.x; у Riehl их нумерация римская — сохрани авторскую). Отбери в документ те, что: несут понятие зоны / дают идею-приём / выделены на встрече (Ex 1.4.ii про группы — «дословно пример с доски», вноси обязательно; в конспекте встречи он назван «1.4.2»). Формат блока — как утверждено владельцем на образце:
```
### Ex 1.4.ii. <короткое имя>

**Упражнение (Riehl, Ex. 1.4.ii).** <формулировка своими словами, точная>

*Идея.* <идейная зацепка — ТОЛЬКО у важных; у остальных ката нет>

> поле:род упражнение

> поле:уровень <по легенде>

> поле:вход безусловно

> поле:узел **Узел картотеки:** нет в картотеке (карточка после развода зон)

> поле:источник **Источник:** Riehl CTiC, Ex. 1.4.ii (§1.4)

> поле:решение **Статус решения:** <нет | идея | полное — полные НЕ пиши без крайней нужды: решение владельца — «не хранить все решения, у важных идейные зацепки»>
```
Ставь Ex-блоки в конец смысловой группы, к которой упражнение относится. НЕотобранные — в таблицу покрытия с причиной («проверка определения», «за потолком», …).

**Г. VYCHITANO, секция `## riehl`:** допиши слой (ничего не удаляя): `**Маткостяк-v2 (2026-07-24, заход riehl-a):**` — как смотрел (постранично §1.4–2.4, печ. X–Y), что взято (список адресов → номера блоков), что отвергнуто классами, чего НЕТ в диапазоне для документа, долг (гл.3 — З3б). Прежний «Долг» v1 по гл.1–2 этим закрывается — допиши под ним строку «закрыто постранично 2026-07-24 (см. слой ниже)», сам текст долга не удаляй.

**Д. Приложение документа:** добавь ОДНУ счётную строку — «Блоков-упражнений: `grep -cE '^### Ex ' teorkat-vvedenie/MAT-KOSTYAK.md`» с пометкой «обязано совпадать с `поле:род упражнение`». Прогони обе.

**КРИТЕРИЙ ГОТОВНОСТИ (обязан мочь ПРОВАЛИТЬСЯ):**
- **Таблица ПОКРЫТИЯ по страницам**: число строк = числу печатных страниц диапазона (страница → единицы → вердикты); ни одной страницы «пропущено». Отдельной строкой: сколько замечаний-при-определениях осмотрено и что из них взято (закрытие долга v1 — явно).
- Состав внесённого совпал с объявленным в ПЛАНе; счётчики до→после прогнаны (`род`-раскладка, `вход`, У/П/Ex-всего, «блоков-упражнений» = «род упражнение»); **главный итог** прогнан — если изменился, назови, какие блоки прибавили, и почему они честно нетривиально+внутреннее+безусловно+в-зоне (рост — находка, падение — брак).
- Существующие тела: **0 изменённых** (верификатор §3).
- `build_doc` rc=0 · `check_view` rc=0 · боковое оглавление глазами · длиннейший новый кат глотает всё (проверь DOM-ом).
- `proof-self-review`: прогнан на всех новых доказательствах/идеях, вердикты в отчёте.

## 3. ВЕРИФИКАТОР — ОБЯЗАТЕЛЕН
Независимый метод (свежий субагент; нет Task-инструмента — программный скрипт с пометкой): против `git show HEAD:` — старые блоки идентичны (таблица, 0 изменённых), новые — полнота полей и корректный формат; плюс выборочная сверка 5 случайных строк таблицы покрытия с самим PDF (страница действительно содержит названные единицы). Вердикт целиком в отчёт.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД
```
git --no-optional-locks add -- teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/ teoriya-kategoriy/istochniki/VYCHITANO.md _studio/zhurnal/2026-07-21_mat-kostyak/kod_riehl-a.md
git --no-optional-locks commit -m "MAT-KOSTYAK: Riehl §1.4–2.4 постранично — добор утверждений и упражнений; VYCHITANO слой" -- teorkat-vvedenie/MAT-KOSTYAK.md teorkat-vvedenie/MAT-KOSTYAK/ teoriya-kategoriy/istochniki/VYCHITANO.md _studio/zhurnal/2026-07-21_mat-kostyak/kod_riehl-a.md
python3 _generator/tools/git_zona.py check --zone "teorkat-vvedenie teoriya-kategoriy/istochniki/VYCHITANO.md _studio/zhurnal/2026-07-21_mat-kostyak"
git --no-optional-locks show --stat
```
Хук покраснел → пересобери. Внешняя причина → в отчёт строкой.

## 5. ОТЧЁТ → `## ОТЧЁТ` внизу
Таблица покрытия (или её сжатая сводка + полная в этом же файле ниже отчёта) / состав внесённого (блок → группа → род/вердикт/уровень + адрес в книге) / счётчики до→после / главный итог с разбором / proof-self-review вердикты / вердикт верификатора / что НЕ трогал / вопросы (включая кандидатов на пере-разметку `следует-из`) / **время + токены** / **КОММИТ**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Формат: `### <что произошло>` / `ЦЕНА: <что и сколько стоило>`. ЦЕНА обязательна; не сочиняй.

### Счётная строка, жёстко привязанная к составу родов, ломается при доборе нового рода
Строка Приложения «Пометок `род` всего (обязано совпасть с числом блоков-утверждений…)» написана в эпоху без Ex-блоков; первый же добор упражнений (санкционированный самим документом: «появятся они добором») делает её ложной (63 ≠ 60), и по контракту захода чинить её исполнителю нельзя. Заходы, добавляющие новый род блоков, должны сразу нести правку зависимых счётных строк в зоне — иначе документ выходит из захода с заведомо красной самопроверкой.
ЦЕНА: ~полчаса на разбор «мой это брак или наследованный», плюс вопрос аналитику и заведомо красная строка самопроверки документа до его решения.

### Заход диктует дату слоя вперёд (2026-07-24), реальная дата — 2026-07-23
Текст задачи Г прописал дату в заголовке слоя VYCHITANO буквально; правило владельца — даты данных реальные. Расхождение пришлось замечать и решать исполнителю (взял реальную).
ЦЕНА: минуты на сверку, но при невнимательном исполнителе — ложная дата данных в источниковом файле; bootstrap-шаблону задать даты слоёв плейсхолдером «дата коммита», не литералом.

## ПЛАН — (заполняет исполнитель)

**Ветка:** `arka/mat-kostyak` ✅ (проверено первым ходом). HEAD = `7f1ad61` (после absorb-vstrechi). Вне git — 16 путей в `_studio`, чужие, не трогаю.

**Печатные границы диапазона (по оглавлению PDF, стр. vii):**

| § | Название | Печ. границы | pdf (печ+20) |
|---|---|---|---|
| 1.4 | Naturality | 24–30 | 44–50 |
| 1.5 | Equivalence of categories | 31–38 | 51–58 |
| 1.6 | The art of the diagram chase | 39–46 | 59–66 |
| 1.7 | The 2-category of categories | 47–52 | 67–72 |
| Гл. 2 вступление | Universal Properties… | 53 | 73 |
| 2.1 | Representable functors | 54–58 | 74–78 |
| 2.2 | The Yoneda lemma | 59–66 | 79–86 |
| 2.3 | Universal properties and universal elements | 67–71 | 87–91 |
| 2.4 | The category of elements | 72–80 | 92–100 |

**Число печатных страниц диапазона: 57** (печ. 24–80 включительно). Смещение pdf−20=печ. — из VYCHITANO `## riehl`, сверено там на трёх страницах.

**Живые счётчики ДО (прогнаны командами Приложения):** утверждений всего 58 · нетривиальных 13 · тривиальных 45 · вход-условно 7 · род внутреннее 44 · род внешнее-пример 14 · род всего 58 · упражнений 0 · производных 22 · независимых 36 · следует-из всего 58 · главный итог 5 · независимого в итоге 5 · понятий словаря 35.

**Последний занятый У-номер: У58** (новые — с У59). Последний П-номер: П35. Ex-блоков нет ни одного (формат вводится этим заходом).

**Уже сопоставленные адреса (карта VYCHITANO):** У19=Lemma 1.3.8 (вне диапазона, §1.3); У36=Ex 1.4.i; У44=Lemma 1.5.5 (печ. 32); У37=Ex 1.5.iv (печ. 38); У35=Lemma 1.6.5 (печ. 41); У46=Ex 1.6.i (печ. 47); У45=Ex 1.6.ii (печ. 47); общая форма У32=Prop 2.4.8 (печ. 74, за словарём — категория элементов).

**Подход к покрытию:** Read PDF порциями по ~7–10 печатных страниц (3–5 вызовов на параграф не требуется — идём подряд печ. 24→80), по каждой странице фиксирую математические единицы (Definition/Lemma/Prop/Theorem/Example-с-содержанием/Remark-с-математикой при определениях — прицельный класс долга v1/Exercise) и вердикт: **есть** (с номером У/П) / **внести** / **вне зоны** (причина) / **контекст**. Таблица покрытия — 57 строк, по строке на печатную страницу, копится в этом файле ниже отчёта. §1.7 (2-категории) — ожидаемо почти весь «вне зоны» (потолок курса), но постранично открывается так же. Ожидание по гипотезе аналитика («почти всё уже есть») — проверяю, не подыгрывая: замечания-при-определениях и упражнения — главные кандидаты на находки.

**Ожидаемый состав внесения:** (а) Ex 1.4.ii (группы, «дословно пример с доски» со встречи) — обязательный; (б) прочие упражнения диапазона по критерию «несёт понятие зоны / даёт идею-приём»; (в) утверждения-замечания при определениях, если найдутся не покрытые документом и в зоне (потолок «до сопряжений»); (г) слой в VYCHITANO `## riehl`; (д) счётная строка «Блоков-упражнений» в Приложение. Новые доказательства/идеи — через proof-self-review до вписывания.

## ВОПРОСЫ — (заполняет исполнитель)

1. **Счётная строка приложения «Пометок `род` всего (обязано совпасть с числом блоков-утверждений…)» устарела с добором Ex.** Теперь `род`-пометок 63 (60 утверждений + 3 упражнения), а строк `Утверждение N (` — 60: строка приложения написана в эпоху без Ex-блоков и сравнивает не то. Чужих строк не правил (дисциплина). Предлагаемая правка (решение аналитика): «обязано совпасть с суммой блоков-утверждений и блоков-упражнений». Соседняя строка «Упражнений в списке: … (сейчас печатает 0 — это норма…)» тоже устарела: печатает 3.
2. **У26 требует малости всей $\mathcal{C}$, Riehl — только локальной малости (Thm 2.2.4, печ. 61).** У Riehl биекция сама доказывает, что $\mathrm{Nat}(\mathcal{C}(A,-),F)$ — множество; малость нужна не для биекции, а для функторности $\mathrm{Nat}$ по $F$ (категория функторов, П30). Документ консервативнее — не ошибка, но кандидат на пере-формулировку условия У26 (решение аналитика).
3. **Кандидат-продолжение У58 (замечание при Lemma 1.5.1, печ. 32):** заменить в цилиндре порядок $\{0\lt 1\}$ на «гуляющий изоморфизм» $\mathbb{I}$ — и «гомотопии» с таким отрезком отвечают естественным изоморфизмам; эквивалентность категорий становится буквально гомотопической эквивалентностью (Def 1.5.4 у Riehl это проговаривает). Встреча 22.07 называла только 2-версию; вносить ли I-версию — решение аналитика/владельца.
4. **Remark 1.5.8 (печ. 33): полнота и строгость — локальные условия** (строгий функтор не обязан быть глобально инъективным на морфизмах, полный — сюръективным). У24 формулирует верно («на каждом множестве морфизмов»), но предупреждения не держит; кандидат на оговорку в теле У24 при будущей правке владельцем — тел не трогал.
5. **У60 внесён как кандидат на внешний приз Йонеды** (строчные операции, Riehl Cor 2.2.10). Условие входа У26 просит «внешний приз, найденный сюжетной аркой» — решение, засчитывать ли, за аркой и владельцем; сам блок условен вместе с B′3 и при отклонении Йонеды уходит с ней.
6. **Кандидатов на пере-разметку `следует-из` старых блоков не обнаружено:** ни одно новое утверждение не делает старый независимый ход производным (У59/У60 — листья графа).
7. **История решений не пополнена:** заход не называет её в зоне правок; absorb-прецедент (пп. 14–15) говорит, что новые внесения там фиксируют. Если нужно — одна строка от аналитика: «Riehl §1.4–2.4 постранично: У59, У60 (условный), Ex 1.4.ii/1.4.iii/2.2.vii — 2026-07-23».
8. **Дата в заголовке слоя VYCHITANO — 2026-07-23 (реальная), а не 2026-07-24 из текста захода:** заход написан с датой вперёд; правило владельца — даты данных реальные.

## ТАБЛИЦА ПОКРЫТИЯ — постранично, печ. 24–80 (57 строк; «есть»=в документе, адрес назван; «внесено»=этим заходом; «вне»=вне зоны с причиной; «контекст»=математика-иллюстрация без статуса)

| Печ. | Единицы страницы → вердикты |
|---|---|
| 24 | Хвост §1.3: Ex 1.3.vii (слайсы как запятые) — вне (запрет 5/D2); Ex 1.3.viii (функторы не отражают изо) — есть (контраст У19/У37, назван в теле У37); Ex 1.3.ix (центр/коммутант/Aut на Grp_iso/epi/all) — есть-родство (У54; полный разбор — вне, Grp-материал); Ex 1.3.x (Conj функториален) — вне (Grp). §1.4: эпиграф Freyd; $V\cong V^\ast$ через выбор базиса — контекст (подводка У22) |
| 25 | Двойственный базис, $V^{\ast\ast}$, $\mathrm{ev}_v$ — контекст (подводка У21); **Def 1.4.1** (естественное преобразование) — есть (П20); **Def 1.4.3** (естественный изоморфизм) — есть (П21); замечание-конвенция «natural in $c$» — контекст |
| 26 | Замечание «меньшим не формулируется», глобулярная запись — контекст; Ex.1.4.4(i) $\mathrm{ev}\colon\mathrm{id}\Rightarrow(-)^{\ast\ast}$ — есть (У21); (ii) $\mathrm{id}$ vs $(-)^\ast$ + сноски 34–35 (динатуральные) — есть (У22+У53); динатуральность — вне (за словарём); (iii) синглтон в степенное — контекст (проверка строкой, приза нет); (iv) Nat из BG = эквивариантность — **внесено** (содержание Ex 1.4.ii) |
| 27 | (v) $O\cong C$ на Top — вне (Top); (vi) $G\cong G^{\mathrm{op}}$ инверсией — контекст (иллюстрация; арке); (vii) no cloning $\mathrm{id}\Rightarrow(-)\otimes(-)$ — вне ($\otimes$ не в словаре); торсион, поcл. (1.4.5) — контекст (подводка 1.4.6) |
| 28 | **Prop 1.4.6** ($A\cong TA\oplus A/TA$ неестественно) — вне (Ab_fg и классификация к.п. групп вне словаря; слот мотивации занят У22/У57); Ex.1.4.8 (Рисс) — вне (функан) |
| 29 | Ex.1.4.9 (пост/предкомпозиция естественны) — контекст (в списке нет заказчика; содержание рассыпано по У20/У47); Ex.1.4.11 (категорификация арифметики, Fin_iso, декатегорификация) — есть (У7/У10/У12 в общности CCC; сюжет — линия У56/картотека) |
| 30 | Упр. §1.4: i — есть (У36); ii — **внесено** (Ex 1.4.ii, решение встречи); iii — **внесено** (Ex 1.4.iii); iv ($\mathrm{Nat}(\mathrm{id},(-)_+)$) — не отобрано: проверка определения, изюминка $\varnothing$ блока не окупает; v (Ab) — вне; vi (разные $f$ — разные $f_\ast,f^\ast$) — есть-условно (строгость У27); vii (запятая) — вне (D2); viii–ix (ди/экстранатуральные) — вне |
| 31 | Хвост ix — вне; §1.5 эпиграф — контекст; **Lemma 1.5.1** (Nat = функтор из $\mathcal{C}\times 2$) — есть (У58, документная форма — цилиндр руками) |
| 32 | Замечание walking-I (гомотопии с $\mathbb{I}$ ⟷ ест. изоморфизмы) — контекст + ВОПРОС 3; **Def 1.5.4** (эквивалентность) — есть (П23); adjoint equivalence — вне (сопряжения); **Lemma 1.5.5** — есть (У44); Ex.1.5.6 (Set_* ≃ Set_∂) — вне (частичные функции); **Def 1.5.7** (full/faithful) — есть (тело У24) |
| 33 | ess.surj. — есть (У24); **Remark 1.5.8** (локальность) — контекст + ВОПРОС 4; embedding/full subcategory — вне (термины не вводятся); **Thm 1.5.9** — есть (У24, включая AC-оговорку = флаг); **Lemma 1.5.10** (транспорт вдоль изо) — вне (обслуживает только вне-зонное доказательство 1.5.9); начало доказательства 1.5.9 — вне (флаг У24) |
| 34 | Доказательство 1.5.9 (середина) — вне (то же) |
| 35 | Конец доказательства — вне; Ex.1.5.11 (essential image) — вне (термин); **Ex.1.5.12 Mat ≃ Vect^fd** — **внесено** (У59); connected — контекст; **Prop 1.5.13** (связный группоид ≃ BG) — вне (группоиды — П15 не-говорим) |
| 36 | Доказательство 1.5.13, **Cor 1.5.14** ($\pi_1$ от точки) — вне (группоиды/Top); **Remark 1.5.15** (обратная эквивалентность требует выбора) — есть-частично (= флаг У24), остальное вне; **Def 1.5.16** (скелет) — вне (П23 не-говорим); Remark 1.5.17 — вне |
| 37 | Ex.1.5.18 (скелеты; (ii) чум — факт E4 уже учтён по Лейнстеру) — вне (скелет); **Ex.1.5.19** (orbit-stabilizer категорифицирована) — вне (группоид действия + скелет, два запрета); принцип equivalence-invariance — есть-частично (У50 — нужная курсу форма; слоган — контекст) |
| 38 | Хвост списка инвариантов ($\mathcal{C}^{\mathrm{op}}$, произведения пар) — вне (произведение категорий); evil/ess.small — вне (размеры закрыты П1); упр. §1.5: i — есть (У58); ii (Γ Сегала) — вне; iii — вне (лемма 1.5.10); iv — есть (У37); v (контрпримеры) — не отобрано: граница уже в теле У37; vi — есть (У44, флаг: путь через критерий = та же плата У24); vii (≃ дискретным) — вне; viii (Erlangen) — вне; ix (начало) — вне (кольца/модули) |
| 39 | Хвост ix — вне; §1.6 эпиграф — контекст; диаграмма informal — есть (П4); **Def 1.6.2** (моноид в Set диаграммами) — вне (линия моноидальных структур; курсовой взгляд — П15) |
| 40 | **Def 1.6.3** (top.monoid; кольцо в Ab; алгебра через $\otimes$) — вне ($\otimes$/§E.2); **Def 1.6.4** (диаграмма = функтор $J\to\mathcal{C}$) — вне (П4 не-говорим, дословный запрет); замечание formal/informal — контекст |
| 41 | Замечание о functoriality образа — контекст; **Lemma 1.6.5** — есть (У35); Ex.1.6.6 ($2{\times}2$) — вне (индексирующие категории); Remark 1.6.7 — контекст; Ex.1.6.8 (категория $4$) — контекст |
| 42 | «$4$ free on quiver» — есть-контекст (П16); Ex.1.6.9 ($2{\times}3$) — контекст; **Lemma 1.6.11** (замена равного подпути) — контекст (свойство операции композиции; критерий D3: наблюдение) |
| 43 | Куб; «коммутативность проверяется на минимальных поддиаграммах» — есть-как-запрет (П4 не-говорим, дословно); directed polygon — контекст; **Lemma 1.6.12** (треугольник с изо) — контекст (ход «домножить на обратный», в У36 выписан явно); **Lemma 1.6.13** (обратные к квадрату изо) — есть (шаг доказательства У36) |
| 44 | «Предпорядок: коммутативность автоматична» — есть-контекст (П14/У14); **Def 1.6.14** (initial/terminal/zero) — есть (П6; zero object — не-говорим); **Ex.1.6.15** (i) Set — есть (У13-линия), (ii) Top, (iii) Set_*, (v) Ring/Rng, (vi) Field, (vii) Graph, (viii) Cat — вне (носители вне словаря), (iv) RMod/Group — контекст (Vect-случай в У16), (ix) предпорядок — есть (У14/П17); **Lemma 1.6.16** — есть (У1; группоидный язык — вне); сноска 44 — вне |
| 45 | Contractible/chaotic/replete — вне; доказательство 1.6.16 — есть (У1); «definite article the» — есть-дух (B0/У1); **Lemma 1.6.17** (пути в терминальный/из начального) — контекст (прямое чтение П6; документ применяет на месте — У1/У42/У46); **Def 1.6.18** (конкретная категория) — вне (не требуется цепочкой) |
| 46 | Ex.1.6.19 — вне; **Lemma 1.6.20** (строгий отражает коммутативность) — вне (нужна для категорий, где морфизмы — не функции; таких у курса нет); **Remark 1.6.21** (cancelability) — вне (моно/эпи — П3 не-говорим); **Lemma 1.6.22** — вне (то же) |
| 47 | Доказательство 1.6.22 — вне; упр. §1.6: i — есть (У46); ii — есть (У45 + У50); iii (Set_∂) — вне; iv–v (моно/эпи) — вне (П3); vi (малая $\mathcal{C}$ вкладывается в Set) — вне (малость П1; предвестник условного блока); §1.7: функтор-категория $\mathcal{D}^{\mathcal{C}}$, $\mathrm{id}_F$ — есть (П30, условно) |
| 48 | **Lemma 1.7.1** (вертикальная композиция) — есть-как-запрет (П20 не-говорим; плата = флаг У44); **Cor 1.7.2** — есть (П30 условно); **Remark 1.7.3** (размеры) — есть (⚠-оговорка П30, содержание совпадает); pasting — вне; **Lemma 1.7.4** (горизонтальная) — вне (П20) |
| 49 | Доказательство 1.7.4 — вне; **Remark 1.7.6** (whiskering) — вне; анонс interchange — вне |
| 50 | **Lemma 1.7.7** (interchange) — вне; **Def 1.7.8** (2-категория) — вне (потолок) |
| 51 | Упр. §1.7: i (размеры $\mathcal{D}^{\mathcal{C}}$) — вне; ii–iii — вне (2-техника); iv (центр категории; $Z(\mathrm{Ab}_{fg})$) — вне (категория функторов/размеры); v (транзитивность ≃ напрямую) — есть (У44-флаг: ровно объявленная стандартной выкладка); vi (бифунктор ⟷ каррирование по категориям) — вне (произведение категорий/бифункторы — П8/П18 не-говорим); vii — вне |
| 52 | Пустая страница (граница глав) — единиц нет |
| 53 | Гл. 2, вступление: эпиграф Мазура — есть-дух (П27); n-Color/$K_n$ + сноска 4CT — контекст (Graph вне словаря) |
| 54 | Симметрии $K_n$; анонс «universal = initial/terminal в ∫» — вне (∫); §2.1 эпиграф — контекст; мотивация «cook up a category» — контекст (в VYCHITANO с v1); **Ex.2.1.1** (ℕ — универсальная динамическая система) + сноска 3 (NNO) — есть (П34+У31); сноска 2 (initial rig) — вне (rig; карточка nno-initial-rig) |
| 55 | Hom-характеризация initial/terminal, таблицы действий — есть (П22; чтение П6 через Hom — документ применяет в У10/У11/У48); **Def 2.1.3** — есть (то же; упаковка в $\Delta\ast$ — вне, константный функтор не в словаре); **Def 2.1.4** (представимость) — есть (П29 условно; лок. малость — оговорка П30); mapping out/in — контекст (У8-оговорка) |
| 56 | **Ex.2.1.5** (i) $\mathrm{id}_{\mathbf{Set}}$ представим точкой — есть (П13/У13-линия); (iii) $U\colon\mathbf{Group}$ представим ℤ — есть-условно (П33, свободный объект); (ii, iv–vii) Top/RMod/Ring/$F_n$/презентации — вне (носители вне словаря; П33-условная линия) |
| 57 | (viii) $(-)^\times$ и ℤ[x,x⁻¹] — вне (VYCHITANO v1 знал); (ix–xii) ob/mor/iso/comp на Cat — вне (Cat); (xiii) Set_* — вне; (xiv) Path/Loop — вне (Top); «free = covariant» — вне (П33); **Ex.2.1.6(i)** ($P$ представим $\Omega$, классифицирующие функции) — вне (линия запрета 7; Кантор У17 живёт сразу на функциях) |
| 58 | (ii–iii) Серпинский; $O\cong C$ — вне (Top); (iv) $\mathbf{Set}(-\times A,B)$ представим $B^A$, каррирование — есть (У13+У9/П11); (v) $(-)^\ast$ представим $k$ — есть-контекст (определение $V^\ast$; упаковка П29-условна); (vi–vii) $K(A,n)$/BG — вне (когомологии/бандлы) |
| 59 | «Ubiquity vs utility», три вопроса — контекст; упр. §2.1: i (ℤ/n) — вне (группы-материал); ii (представимый сохраняет моно) — вне (П3); iii (представимость вдоль ≃) — вне (П29-условная линия У50); iv (подфункторы/решёта) — вне; §2.2 эпиграф Маклейна — контекст |
| 60 | **Ex.2.2.1** (Йонеда руками на ω) — контекст (разминка условного блока; примеры У26 — арке); **Ex.2.2.2** (представимые из BG; Nat = эквивариантные) — есть (ход У28; содержание Ex 1.4.ii); сноска 7 (свободность) — есть-контекст (У28) |
| 61 | **Prop 2.2.3** (эквивариантные $G\to X$ ⟷ элементы $X$) — есть (ход У28); **Thm 2.2.4 (Йонеда)** — есть (У26); замечание «биекция доказывает множественность Nat» — контекст + ВОПРОС 2 (лок. малость vs малость); двойственная форма — есть-контекст (У27 держит обе); доказательство биекции ($\Psi(x)_d(f)=Ff(x)$) — есть (У26-скетч, формула совпадает) |
| 62 | Конец биекции + естественность по $F$ через вертикальную композицию — есть (У26 «рутинная выкладка»; сама вертикальная композиция — П20 не-говорим, потому естественность по $F$ у документа живёт в условном П30 — согласовано) |
| 63 | Естественность по $c$ — есть (У26); **Remark 2.2.7** ($\mathrm{ev}_c$ представим; бифунктор $\mathrm{Hom}(よ(-),-)$; SET) — вне (бифункторы/большие категории) |
| 64 | «Valued in Set» — контекст; **Cor 2.2.8 (вложение Йонеды)** — есть (У27, обе формы); «две инкарнации бифунктора» — вне (П18); доказательство через $\mathrm{ev}_{\mathrm{id}}$ — есть (У27-доказательство); сноска 10 (よ) — контекст |
| 65 | «Оба зовутся леммой» — контекст; Ex.2.2.9 ($\mathrm{Nat}(\mathrm{ob},\mathrm{mor})$ на Cat — ровно три) — вне (Cat); **Cor 2.2.10 (строчные операции)** — **внесено** (У60, условный B′3; кандидат на внешний приз Йонеды); **Cor 2.2.11 (Кэли)** — есть (У28) |
| 66 | Доказательство Кэли — есть (У28, дословно тот же ход); упр. §2.2: i (двойственная Йонеда) — есть (У27); ii (почему не дуализуется) — вне (тонкость условного блока); iii (よ на ω) — вне; iv (iso⊂mor, монические) — вне (Cat/подфункторы); v ($\mathrm{Nat}(P,P)$, $\mathrm{End}(\Omega)$) — вне (Ω); vi (репараметризации) — вне (Top); vii(i) Top — вне; vii(ii) Vect — **внесено** (Ex 2.2.vii) |
| 67 | §2.3 эпиграф Ченг — контекст; **Prop 2.3.1** (изо объектов ⟺ изо Hom-функторов, явная конструкция) — есть (У47; условный хвост — У27); **Cor 2.3.2** (начало) — есть |
| 68 | **Cor 2.3.2** (единственный изо, коммутирующий с представлениями) — есть (каноничность У47 + конкретные единственности У1/У4/У6; общая форма — У27-условно); «categorical the» — есть-дух; **Def 2.3.3** (universal element) — вне (язык отвергнутой общей формы У32 = Prop 2.4.8); **Ex.2.3.4** (ℤ[x]) — вне (кольца); **Ex.2.3.6** ({⊤}∈PΩ) — вне (Ω); **Ex.2.3.7** (представимый G-set = свободное транзитивное; торсор) — вне (торсоры) |
| 69 | Аффинное $\mathbb{A}^n$ как торсор — вне (торсоры; красиво для арки — «пространство без выбранного начала», отмечено в слое VYCHITANO); **Ex.2.3.8** (Bilin представим $V\otimes W$) — вне ($\otimes$ — П10 не-говорим) |
| 70 | Конструкция $\otimes$ из УС — вне; **Prop 2.3.10** ($V\otimes W\cong W\otimes V$ через Cor 2.3.2) — вне ($\otimes$; сам ход — У47, в документе есть); **Remark 2.3.12** (явный изо трассировкой id) — вне |
| 71 | **Ex.2.3.13** (ℤ[G]) — вне (кольца); упр. §2.3: i — вне (Cat/Top); ii (свойства $\otimes$) — вне; iii ($B^A$, ev, УС по аналогии с $\otimes$) — есть (П10/У13 — дословно УС экспоненты в Set); iv (альтернативные универсальные элементы) — вне |
| 72 | §2.4 вступление — вне (∫); **Def 2.4.1** (∫F ковариантного) — вне (D-«отвергнуто 2»); **Def 2.4.2** (контравариантного) — вне (то же) |
| 73 | Забывающий Π — вне; **Ex.2.4.3** (∫n-Color) — вне (Graph); **Ex.2.4.4** (pointed objects) — вне; **Ex.2.4.5** (дискретная C: dependent sum/product) — вне (запрет 6, дословно); **Ex.2.4.6** (∫Hom = слайсы $c/\mathcal{C}$, $\mathcal{C}/c$) — вне (запрет 5, дословно) |
| 74 | Слайсы-двойственность — вне; **Lemma 2.4.7** (∫F ≅ よ↓F) — вне (запятые — D2); **Prop 2.4.8** («universal elements are universal elements») — есть-граница: общая форма У32, отвергнута решением владельца (D2/E10); постранично подтверждено — идёт через ∫ и Йонеду, за словарём |
| 75 | Доказательство 2.4.8 — вне; **Prop 2.4.9** (представления — пустая или стягиваемая подкатегория) — вне (∫; единственность — У47/У1-линия); **Ex.2.4.10** (∫X ≅ X//G) — вне (группоиды действия); **Ex.2.4.11** (динамические системы; (ℕ,s,0)) — есть (П34+У31; ∫-упаковка — вне) |
| 76 | **Ex.2.4.12** (i) ∫P, {⊤}⊂Ω + сноска 16 (subobject classifier) — вне (запрет 7); (ii) ∫U Vect: (k,1) и все (V,v≠0), dim 1 начальны — вне (∫; «единственно изоморфны» — У3-линия); (iii) ⊗ начален — вне; (iv) $F_n$ — вне; (v) слайс Vect/k — вне (запрет 5); (vi) начало ℤ[x] — вне |
| 77 | (vi) конец: эндоморфизмы в ∫ vs Ring; естественные эндоморфизмы $U$ — полиномы — вне (кольца; ход — У27-линия); «любая категория = ∫Δ*» — вне; **Def 2.4.13** (discrete left/right fibration) — вне (запрет 10 — расслоения); lifting diagrams — вне; **Prop 2.4.14** (∫ полн и строг; образ — дискретные расслоения) — вне (Гротендик-линия, за потолком) |
| 78 | Доказательство 2.4.14 — вне; головоломка о представимости $\mathcal{C}(A,-)\times\mathcal{C}(B,-)$ — есть (У39: представим ⟺ есть копроизведение); упр. §2.4: i–ii (запятые) — вне; iii (начальные $c/\mathcal{C}$) — вне (слайсы); iv (двойственность) — вне; v (Серпинский) — вне (Top) |
| 79 | vi (функтор предпорядков, представимость) — вне (∫); vii (twisted arrow) — вне; viii (∫ ff, F≅G ⟺ ∫F≅∫G) — вне; ix (слои дискретны) — вне; x (повтор головоломки) — есть (У39) |
| 80 | Пустая страница (граница глав) — единиц нет |

**Сводка по замечаниям-при-определениях (прицельный класс, долг v1):** осмотрено ≈30 замечаний и сносок при определениях (полный перечень рассыпан по строкам таблицы: печ. 25, 26, 32, 33, 36, 37, 40, 41, 43, 45, 46, 48, 49, 55, 57, 61, 63, 64, 68, 76, 77 + сноски 2, 3, 7, 10, 14, 16, 34, 35, 44). Взято в блоки из этого класса: 0 — всё, что в зоне, документ уже держал (Remark 1.7.3 = ⚠-оговорка П30; выбор при обращении эквивалентности = флаг У24; минимальные поддиаграммы = «не-говорим» П4), остальное вне зоны. Три замечания дали ВОПРОСЫ 2–4. Новые блоки пришли из Example (1.5.12), Corollary (2.2.10) и Exercises — не из замечаний. Долг v1 закрыт: класс постранично осмотрен, спрятанных безусловных утверждений зоны в нём не оказалось.

## ОТЧЁТ — (заполняет исполнитель)

**Сводка таблицы покрытия.** Пройдено **57/57** печатных страниц диапазона §1.4–§2.4 (печ. 24–80; из них две — пустые границы глав: 52, 80). Полная таблица — выше в этом файле. Вердиктов-единиц всего ≈247: **есть** (включая есть-условно/-частично/-дух/-как-запрет) — 70 · **внесено** — 6 вхождений (5 блоков; Ex 1.4.ii упомянут на печ. 26 и 30) · **вне зоны** — 133 (каждое с причиной) · **контекст** — 36 · **не отобрано** (упражнения с причиной) — 2. Замечаний-при-определениях осмотрено ≈30, взято из них в блоки 0 (те, что в зоне, документ уже держал; три дали ВОПРОСЫ 2–4) — **долг v1 закрыт**, отдельная сводка под таблицей.

**Состав внесённого** (совпал с объявленным подходом ПЛАНа; конкретика определилась проходом):

| Блок | Группа | Род / вердикт / вход / уровень | Адрес в книге |
|---|---|---|---|
| У59 «Матрицы и конечномерные пространства — эквивалентность» | B′2 (конец) | внешнее-пример / тривиально / безусловно / требует-приручения | Example 1.5.12, печ. 35 |
| У60 «Строчные операции — это Йонеда на матрицах» | B′3 (после У28) | внешнее-пример / тривиально / **условно** (с У27) / требует-приручения | Corollary 2.2.10, печ. 65 |
| Ex 1.4.ii «Естественность между гомоморфизмами групп — сопряжение» | B′2 | упражнение / — / безусловно / требует-приручения; решение: идея | Ex. 1.4.ii, печ. 30 (+Example 1.4.4(iv), печ. 26); «пример с доски» встречи 22.07 |
| Ex 1.4.iii «Естественность между монотонными отображениями» | B′2 | упражнение / — / безусловно / школьно; решение: идея | Ex. 1.4.iii, печ. 30 |
| Ex 2.2.vii «Естественные эндоморфизмы тождественного функтора Vect» | B′2 | упражнение / — / безусловно / требует-приручения; решение: идея | Ex. 2.2.vii(ii), печ. 66 |

Плюс: слой `**Маткостяк-v2 (2026-07-23, заход riehl-a)**` в VYCHITANO `## riehl` (только дописано; под долгом v1 — строка закрытия с датой данных); счётная строка «Блоков-упражнений» в Приложение (прогнана: 3 = 3).

**Счётчики до → после** (все прогнаны командами Приложения): утверждений 58→**60** · нетривиальных 13→13 · тривиальных 45→**47** · вход-условно 7→**8** (У60) · род внутреннее 44→44 · внешнее-пример 14→**16** · упражнение 0→**3** · род всего 58→**63** · Ex-блоков 0→**3** (= роду упражнение ✅) · производных 22→**24** (У59←У24, У60←У27) · независимых 36→36 · следует-из всего 58→**60** (= утверждениям ✅) · Утверждение-строк 58→**60** (= вердиктам ✅) · понятий словаря 35→35 = определений ✅.

**Главный итог: 5 → 5, не изменился** — оба новых У-блока тривиальны (применения готовых критериев У24/У27 к конкретному материалу), в «нетривиально+внутреннее+безусловно+в-зоне» не претендуют. Рост не заявлялся; неизменность — не брак, а подтверждение гипотезы аналитика: содержательное ядро гл. 1–2 Riehl документ уже держал. «Независимого в итоге» тоже 5→5.

**Гипотеза аналитика — подтверждена, с оговоркой** (заход велел не подыгрывать — проверял прицельно): ни одного безусловного утверждения рода «внутреннее», отсутствующего в документе, диапазон не дал; ВСЕ нумерованные результаты диапазона либо уже в документе (Def 1.4.1/1.4.3→П20/П21; L1.5.1→У58; D1.5.4→П23; L1.5.5→У44; T1.5.9→У24; L1.6.5→У35; D1.6.14→П6; L1.6.16→У1; T2.2.4→У26; C2.2.8→У27; C2.2.11→У28 дословно; P2.3.1/C2.3.2→У47; P2.4.8→граница У32 подтверждена), либо вне зоны по прямым запретам словаря (2-категории §1.7, моно/эпи, диаграммы-как-функторы, группоиды/скелеты, ∫/запятые/слайсы/расслоения, ⊗, Ω). Оговорка: добор дал 2 внешних-примера + 3 упражнения — родов, которых гипотеза не касалась. Ловушка «форма книги ≠ существо предмета» сработала трижды, поддаться не дала: (1) Prop 2.4.8 — Riehl говорит категорией элементов, предмету хватает У32; (2) Def 2.1.3 — упаковка в константный функтор, предмету хватает чтения П6 через Hom (У10/У11/У48); (3) естественность Йонеды по F — у Riehl через вертикальную композицию, у документа честно в условном П30.

**proof-self-review** (прогнан на всех пяти новых доказательствах/идеях ДО вписывания): У59 — выдерживает (проверены вырожденный нулевой объект, буквальные условия У24, цена выбора названа; взято обязательство оговорить отличие от Riehl — у неё без нуля); У60 — выдерживает (вариантность сверена по образцу У30, малость Mat_k делает оговорку размеров буквально выполненной, структура (а)Йонеда/(б)естественность элементарных операций/(в)следствие исключает круг); Ex 1.4.ii — выдерживает (направление квадрата ψ(g)·h=h·φ(g) выверено по определению 20); Ex 1.4.iii — выдерживает (пустой предпорядок — ок); Ex 2.2.vii — выдерживает (для U-части линейность компонент не предполагается — выводится, проверено на квадратах умножений k→k).

**Вердикт верификатора** (независимый программный скрипт `scratchpad/verify_riehl_a.py` — Task-инструмента в среде нет, пометка по §3 захода; плюс ручная сверка 5 случайных строк таблицы с PDF, seed 20260723 → печ. 42, 46, 63, 65, 68): ✅ ЗЕЛЁНЫЙ — против `git show HEAD:` старых блоков изменено **0**, пропало 0 (109 старых блоков сверены пофрагментно); новых блоков ровно 5, все с полным набором полей и корректными врезками; вне блоков — ровно одна добавленная строка (счётная, задача Д); VYCHITANO — удалено строк 0, добавлено 8; пустая строка перед каждым `###` соблюдена; 5/5 случайных строк таблицы покрытия совпали с содержимым соответствующих страниц PDF.

**Гейты:** `build_doc` rc=0 (355 КБ, линтер: 0 предупреждений, 0 открытых ⚑) · `check_view` rc=0 (зелёный) · боковое оглавление глазами: держит разделы верхнего уровня, структура цела · рендер новых блоков проверен DOM-ом в браузере: сводки полей собраны (включая «вход: условно — вместе с утверждением 27»), кат «Идея» у Ex-блоков работает, длиннейший новый кат (доказательство У60) глотает всё тело до ■, поля после ката на месте, MathJax собран.

**Что НЕ трогал:** тела всех 109 старых блоков (0 изменений — подтверждено верификатором); устаревшие счётные строки Приложения («Пометок род всего…», «сейчас печатает 0») — не правил, ВОПРОС 1; История решений — не пополнял, ВОПРОС 7; поля `следует-из` старых блоков (кандидатов на пере-разметку не нашлось — ВОПРОС 6); картотека; гл. 3+ Riehl (З3б); чужие 16 незакоммиченных путей в `_studio`.

**Время + токены:** ~2.5 часа чистой работы одной сессией; токены ≈ 320К вход (PDF-страницы диапазона — 57 печ. страниц изображениями за 9 вызовов Read, MAT-KOSTYAK целиком, VYCHITANO-секция) + ≈ 40К выход (блоки, таблица, слой, отчёт).

**КОММИТ:** вписывается следом за самим коммитом отдельным до-коммитом этого файла (паттерн absorb: «хэш … в отчёт») — см. финальную строку ниже.
