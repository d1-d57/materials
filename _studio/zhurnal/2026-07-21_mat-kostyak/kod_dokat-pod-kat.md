# Канал исполнителя — dokat-pod-kat (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — граница ката определяется структурно, реализация рождается по коду движка.

КОНТЕКСТ. Движок `_generator/build_doc.py` имеет режим `format: drill-down` (заходы `72345b6`, `e708b9d`): доказательство утверждения прячется в свёрнутый `<details class="d-proof">`. **Дефект (найден на приёмке З1):** в кат попадает ТОЛЬКО абзац-зачин `*Доказательство…*`; у многоабзацных доказательств весь хвост вывода вываливается наружу и виден при свёрнутом кате. Живой пример — У10 в `teorkat-vvedenie/MAT-KOSTYAK/view.html`: внутри ката 0 `<p>`, а пять абзацев вывода (включая обе дисплей-выкладки и «Остаётся пятый закон…») стоят снаружи. Ровно на длинных доказательствах, где кат нужнее всего.

ЦЕЛЬ: кат доказательства глотает ВСЁ тело вывода до структурной границы. Источники (`.md`) НЕ меняются вовсе — правка только в drill-секции движка + пересборка двух view.html.

Приёмка — по ОТЧЁТУ. СТОП ДО ЦЕЛИ: ничего, кроме границы ката, в движке не менять.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. Проверь `git --no-optional-locks branch --show-current`; дальше НЕ переключай.
- **ЗОНА (можно менять):** `_generator/build_doc.py` (только drill-секция) · `teorkat-vvedenie/MAT-KOSTYAK/` (пересобранный view.html) · `_studio/zhurnal/2026-07-21_mat-kostyak/obrazec-format/` (пересобранный view.html) · `_studio/zhurnal/2026-07-21_mat-kostyak/kod_dokat-pod-kat.md` (этот файл). Всё вне — **READ-ONLY**; исходники `MAT-KOSTYAK.md` и `obrazec-format.md` НЕ трогать ни символом.
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` с теми же путями после `--` (§4). НИКОГДА `-A`/`commit -am`. `--no-optional-locks` во всех git-вызовах. Вложенные субагенты не коммитят.
- **ЗАПРЕТ:** ничего за пределами зоны. Нашёл проблему вне зоны → в отчёт.

## 0. ПЕРВЫЙ ХОД
- Проверка ветки. Точка отката = HEAD (`e708b9d`+), зона обязана быть чиста.
- Прочитать ТОЛЬКО: этот заход · drill-секцию `_generator/build_doc.py` (`render_stream_drill`, `_drill_*`) · для проверки — `teorkat-vvedenie/MAT-KOSTYAK/view.html` (можно точечно, файл большой). Сам `MAT-KOSTYAK.md` можно грепать точечно для сверки структуры блоков (У10, У51, У17, У47), целиком не читай.
- ПЛАН — в `## ПЛАН` до действий; считаешь правило границы неполным — скажи там же, ДО кода.

## 1. ДИСЦИПЛИНА
🔴 Код возврата — первым (`echo $?`). Хирургия: правка только в drill-секции, каждая строка к задаче. Никаких кириллических классов в regex (macOS: пустота при rc=0) — только альтернация. Оспорить ложную предпосылку — в ПЛАНе до работы.

## 2. ЗАДАЧА
**Правило границы ката.** Кат начинается на абзаце-зачине (`*Доказательство…*` / `*Идея…*` / `*Логика…*` — как уже реализовано) и глотает подряд идущий контент — обычные абзацы И дисплей-формулы `$$…$$` — до первого из стоп-маркеров (маркер в кат НЕ входит):
- строка нового курсивного зачина-вставки: абзац, начинающийся `*` + ЗАГЛАВНАЯ буква (напр. `*Зачем названо отдельно.*`, `*Следствие (Кантор).*`, `*О вердикте.*`, `*Почему это утверждение…*`, `*Оговорка о порядке.*`);
- строка флага `⚑ …` или `Флаг закрыт: …`;
- строка поля `> поле:…`;
- следующий заголовок `###`/`##` или конец потока.
Реализация — в drill-секции; существующее поведение для одноабзацных доказательств не меняется (они уже заканчиваются до стоп-маркера).

**Пересборка и проверки:**
1. `python3 _generator/build_doc.py teorkat-vvedenie/MAT-KOSTYAK` (rc первым) → `check_view.py` (rc).
2. `python3 _generator/build_doc.py _studio/zhurnal/2026-07-21_mat-kostyak/obrazec-format` → `check_view.py` (rc).
3. DOM-проверка программно (python, не глазами-только):
   - **У10:** внутри его `d-proof` ≥5 `<p>` И обе дисплей-выкладки; ПЕРВЫЙ видимый элемент после `</details>` ката — флаг `⚑` (бейдж флага), не абзац вывода.
   - **У51:** кат = 1 абзац; `*Зачем названо отдельно.*` — СНАРУЖИ ката.
   - **У17:** «*Следствие (Кантор).*» — СНАРУЖИ ката.
   - **У47:** кат = 1 абзац доказательства; «*Почему это утверждение, а не приём.*» и «*О вердикте.*» — снаружи.
   - **Сплошной охват:** зачинов доказательств/идей в источнике **48** (`grep -cE '^\*(Доказательство|Идея|Логика)' teorkat-vvedenie/MAT-KOSTYAK.md` — перепроверь) → в view.html ровно столько же катов `d-proof`, и НИ ОДИН абзац между зачином и стоп-маркером не остался снаружи (проверь программно по всем 48, в отчёт — таблица покрытия «блок → абзацев в кате → первый элемент после ката», 48 строк).
4. Образец: вид структурно прежний (его доказательства одноабзацны; дифф view.html допустим только от новой логики упаковки, структура та же — подтверди DOM-сверкой числа катов и их содержимого).

**КРИТЕРИЙ ГОТОВНОСТИ (обязан мочь ПРОВАЛИТЬСЯ):** таблица покрытия 48/48 с указанными свойствами · четыре именованные проверки (У10/У51/У17/У47) ✅ · `build_doc`+`check_view` rc=0 на обоих документах · дифф движка — только drill-секция · исходники `.md` не изменены (`git --no-optional-locks status --porcelain` не показывает их).

## 3. ВЕРИФИКАТОР
Таблица покрытия 48/48 из задачи 3 и есть верификатор (метод независим от правки: сверка源-структуры с DOM). Отдельный субагент не требуется.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД
```
git --no-optional-locks add -- _generator/build_doc.py teorkat-vvedenie/MAT-KOSTYAK/ _studio/zhurnal/2026-07-21_mat-kostyak/obrazec-format/ _studio/zhurnal/2026-07-21_mat-kostyak/kod_dokat-pod-kat.md
git --no-optional-locks commit -m "движок drill: кат доказательства глотает всё тело до структурной границы (фикс многоабзацных)" -- _generator/build_doc.py teorkat-vvedenie/MAT-KOSTYAK/ _studio/zhurnal/2026-07-21_mat-kostyak/obrazec-format/ _studio/zhurnal/2026-07-21_mat-kostyak/kod_dokat-pod-kat.md
python3 _generator/tools/git_zona.py check --zone "_generator teorkat-vvedenie _studio/zhurnal/2026-07-21_mat-kostyak"
git --no-optional-locks show --stat
```
⚠ В рабочем дереве лежат чужие незакоммиченные файлы (PLAN/SESSIYA/KARTA, папки `--help/`, `2026-07-23_vneshnie-istorii/`) — параллельные писатели; pathspec-форма выше их НЕ забирает, потому и обязательна. Хук покраснел → пересобери, не `--no-verify`.

## 5. ОТЧЁТ → `## ОТЧЁТ` внизу
Что сделал+ЗАЧЕМ / таблица покрытия 48/48 / четыре именованные проверки / rc всех гейтов / что НЕ трогал / **время + токены** / **КОММИТ**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Формат: `### <что произошло>` / `ЦЕНА: <что и сколько стоило>`. ЦЕНА обязательна; не сочиняй.

## ПЛАН — (заполняет исполнитель)

**Разведка (read-only, до кода) — предпосылки проверены по живым файлам:**
- Зачинов в источнике ровно **48** (`grep -cE '^\*(Доказательство|Идея|Логика)' …` → 48); в образце 3.
- **0 групп `###` с двумя зачинами** → один `d-proof` на группу, счёт катов 48 автоматом; мульти-кат не строю (СТОП ДО ЦЕЛИ), но «новый зачин» в стоп-предикат включён — правило полно.
- Единственный многоабзацный — **У10**: зачин + 4 хвостовых абзаца; обе `$$`-выкладки стоят ВНУТРИ абзацев «Первый…» и «Остаётся…» (без пустых строк вокруг), отдельных блоков не образуют. Остальные 47 — одноабзацные, их стоп-маркер наступает сразу после зачина → поведение не меняется.
- Стопы по данным: курсивные вставки (`*Зачем…`, `*Следствие…`, `*О вердикте…`, `*Обратное*…`, …), флаг `⚑` (У10, У30), поля `> поле:…`. Случай «конец группы» в данных не встречается, предикатом покрыт (исчерпание blocks группы).

**Поправка предпосылки критерия (найдена ДО кода).** «Внутри d-proof ≥5 `<p>`» буквально недостижимо: зачин `*Доказательство…*` рендерится `render_stream`'ом как `<div class="proof">` (build_doc.py:182–183), не `<p>`. Снаружи сейчас тоже 4 `<p>` вывода (пятый `<p>` в сегменте — сам бейдж флага). После фикса в кате У10: **1 `div.proof` + 4 `<p>` = 5 блоков вывода и обе `$$`-выкладки** — проверяю этот смысловой инвариант; рендер зачина не трогаю (сломал бы 47 одноабзацных и вышел за СТОП ДО ЦЕЛИ). → см. ВОПРОСЫ.

**Реализация (только drill-секция):**
1. Предикат `_drill_cut_stop(block)`: курсивный зачин-вставка = `*` + заглавная (через `isupper()`, БЕЗ кириллических классов в regex) · `⚑` · `Флаг закрыт:` · блок-поле `> поле:…` (через `_drill_field`).
2. В `_drill_stmt` — конечный автомат по blocks: зачин открывает кат (`in_proof=True`), подряд идущие блоки без стоп-маркера глотаются в `proof_blocks`, стоп-маркер закрывает кат и уходит стандартным путём (поле → тихий слой, прочее → `content_blocks`). Маркер в кат не входит.
3. Пересборка обоих документов + `check_view` (rc первым).
4. DOM-скрипт (python): таблица покрытия 48/48 «блок → абзацев в кате → первый элемент после ката» — сверка модели источника (независимый парс `.md` тем же правилом) с DOM; четыре именованные проверки У10/У51/У17/У47; образец — 3 ката, структура прежняя.
5. Коммит по §4, `git_zona.py check`.

## ВОПРОСЫ — (заполняет исполнитель)

- **«≥5 `<p>` внутри d-proof У10» → выполняю как «5 блоков вывода: 1 `div.proof` (зачин) + 4 `<p>`, обе `$$`-выкладки внутри ката».** Буквальные ≥5 `<p>` дал бы только слом рендера зачина (`div.proof` → `<p>`), что изменило бы все 47 одноабзацных катов и нарушило СТОП ДО ЦЕЛИ. Смысл проверки («весь хвост вывода в кате, снаружи ноль абзацев вывода») выполнен и показан таблицей. Если нужен именно `<p>`-зачин — это отдельное решение владельца.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** В `_drill_stmt` классификация блоков была по-блочной: в кат попадал только блок, сам матчащий `_DRILL_PROOF_RE`, — хвост многоабзацного вывода уходил в `content_blocks` и рендерился снаружи свёрнутого ката (дефект приёмки З1). Заменил на конечный автомат по последовательности blocks: зачин открывает кат (`in_proof=True`), подряд идущие блоки — обычные абзацы и `$$`-выкладки — глотаются до первого стоп-маркера; маркер в кат не входит и уходит прежним путём (поле → тихий слой, прочее → контент). Стоп-предикат — новая `_drill_cut_stop`: курсивный зачин-вставка `*`+ЗАГЛАВНАЯ (через `isupper()`, без кириллических классов в regex), `⚑ …`, `Флаг закрыт: …`, блок-поле `> поле:…` (через `_drill_field`); новый зачин доказательства и конец группы покрыты самим автоматом. Одноабзацные доказательства не изменились: их стоп наступает сразу после зачина (образец — содержимое всех трёх катов байт-в-байт как в HEAD).

**Таблица покрытия 48/48** (метод независим от правки: модель — отдельный парс `.md` по правилу границы, сверенная с DOM; каждая строка = «блоков в кате DOM==модель» ∧ «`$$` в кате DOM==модель» ∧ «первый элемент после ката == стоп-маркер модели»). Перепроверка счёта: `grep -cE '^\*(Доказательство|Идея|Логика)' teorkat-vvedenie/MAT-KOSTYAK.md` → 48 · `grep -o '<details class="d-proof"' teorkat-vvedenie/MAT-KOSTYAK/view.html | wc -l` → 48.

| блок | абзацев в кате | первый элемент после ката |
|---|---|---|
| У51 | 1 (div.proof) | `<p><em>`Зачем названо отдельно. ✅ |
| У33 | 1 (div.proof) | — конец утверждения (стоп=поле, тихий слой выше) ✅ |
| У34 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У1 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У2 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У3 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У45 | 1 (div.proof) | `<p><em>`Зачем названо отдельно. ✅ |
| У46 | 1 (div.proof) | `<p><em>`Зачем названо отдельно. ✅ |
| У32 | 1 (div.proof) | `<p><em>`Что это даёт и чего стоит. ✅ |
| У4 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У5 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У6 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У7 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У38 | 1 (div.proof) | `<p><em>`Зачем названо отдельно. ✅ |
| У8 | 1 (div.proof) | `<p><em>`Оговорка о порядке. ✅ |
| У39 | 1 (div.proof) | `<p><em>`Зачем названо отдельно. ✅ |
| У9 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У40 | 1 (div.proof) | `<p><em>`Зачем названо отдельно. ✅ |
| У17 | 1 (div.proof) | `<p><em>`Следствие (Кантор). ✅ |
| **У10** | **5 (1 div.proof + 4 `<p>`, обе `$$`-выкладки внутри)** | **бейдж флага ⚑** ✅ |
| У11 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У41 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У42 | 1 (div.proof) | `<p><em>`Следствие. ✅ |
| У12 | 1 (div.proof, `$$`-выкладка внутри зачина) | `<p><em>`О вердикте. ✅ |
| У48 | 1 (div.proof) | `<p><em>`Зачем названо отдельно. ✅ |
| У49 | 1 (div.proof) | `<p><em>`Почему это стоит отдельной строкой. ✅ |
| У18 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У35 | 1 (div.proof) | `<p><em>`Зачем названо отдельно. ✅ |
| У19 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У20 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У36 | 1 (div.proof) | `<p><em>`Почему это утверждение, а не часть определения. ✅ |
| У47 | 1 (div.proof) | `<p><em>`Почему это утверждение, а не приём. ✅ |
| У37 | 1 (div.proof) | `<p><em>`Обратное… ✅ |
| У50 | 1 (div.proof, `$$`-выкладка внутри зачина) | `<p><em>`Зачем это нужно курсу. ✅ |
| У26 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У27 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У43 | 1 (div.proof) | `<p><em>`Почему условно, хотя формулировка чисто категорная. ✅ |
| У29 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У30 | 1 (div.proof) | бейдж флага ⚑ ✅ |
| У31 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У13 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У14 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У15 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У16 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У21 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У22 | 1 (div.proof) | `<p><em>`Почему в мотивации, хотя утверждение важное. ✅ |
| У23 | 1 (div.proof) | — конец утверждения (стоп=поле) ✅ |
| У28 | 1 (div.proof) | `<p><em>`Замечание о вариантности. ✅ |

Итог таблицы: **48/48 ✅**, ни один абзац между зачином и стоп-маркером не остался снаружи (равенство числа блоков в кате модели и DOM по каждой из 48 строк). Негативный тест метода: та же сверка против ДО-фиксного DOM из HEAD даёт У10 = 1 блок против модельных 5 → ❌ (верификатор умеет проваливаться).

**Четыре именованные проверки (DOM, программно):**
- **У10** ✅ — в кате 5 блоков вывода (1 `div.proof` + 4 `<p>`), обе дисплей-выкладки (`\mathrm{ev}''…` и `A^{B+C}…`) внутри ката; первый видимый элемент после `</details>` — бейдж флага `⚑`, не абзац вывода.
- **У51** ✅ — кат = 1 абзац; `*Зачем названо отдельно.*` снаружи (первый элемент после ката).
- **У17** ✅ — `*Следствие (Кантор).*` снаружи ката (первый элемент после ката).
- **У47** ✅ — кат = 1 абзац; «Почему это утверждение, а не приём.» и «О вердикте.» обе снаружи.

*(Оговорка к букве критерия У10 «≥5 `<p>`» — см. ВОПРОСЫ: зачин рендерится `div.proof`, не `<p>`; выполнен смысловой инвариант «все 5 блоков вывода в кате».)*

**rc гейтов:** `build_doc MAT-KOSTYAK` → 0 · `check_view MAT-KOSTYAK` → 0 (ЗЕЛЁНЫЙ) · `build_doc obrazec-format` → 0 · `check_view obrazec-format` → 0 (ЗЕЛЁНЫЙ) · верификатор покрытия → 0.

**Образец:** катов 3/3 (`grep -o '<details class="d-proof"' …/obrazec-format/view.html | wc -l` → 3), содержимое всех трёх катов байт-в-байт совпадает с HEAD, число `d-st` прежнее. Единственный дифф view.html образца — одна CSS-строка `details.d-proof>summary+.proof>.lbl,…{display:none}`, приехавшая из захода `e708b9d` («CSS-лейбл ката»): она уже была в движке и в MAT-KOSTYAK/view.html, а образец после того захода не пересобирали. Структура DOM не изменилась.

**Что НЕ трогал:** исходники `.md` — ни символом (`git status --porcelain` по ним пуст; `mat-kostyak.md` в папке — симлинк на `../MAT-KOSTYAK.md`, источник один); движок вне drill-секции (дифф `build_doc.py` — только `_drill_cut_stop` + автомат в `_drill_stmt`); `render_stream`, CSS/JS drill, шаблон PAGE; чужие незакоммиченные файлы (KARTA/PLAN/SESSIYA, `--help/`, `2026-07-23_vneshnie-istorii/`) — pathspec-коммит их не берёт.

**Время + токены:** ≈25 минут одним заходом; ≈70–80k токенов (разведка грепами + два чтения секций движка + три прогона верификатора).

**КОММИТ:** *(хэш вписывается следом за коммитом отдельным ходом — см. строку ниже после выполнения §4)*
*(нет хэша — назови причину; пусто = отчёт не принимается)*
