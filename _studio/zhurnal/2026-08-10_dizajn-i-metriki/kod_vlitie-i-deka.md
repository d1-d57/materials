# Канал исполнителя — vlitie-i-deka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — слияния с конфликтами требуют суждения по ходу, а цена ошибки — потерянная работа четырёх принятых заходов.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — <одна фраза почему; см. шапку захода>.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-deka.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: git branch --show-current, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-10_dizajn-i-metriki`
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/nifty-keen-mccarthy/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Арка «дизайн и метрики» довела генератор слайдов до рабочего состояния: типология,
сцены из блоков, вид блоков, связи-рёбра, поля кадра, служебные слайды по шаблону дизайнеров.
**Четыре захода приняты приёмкой — и ни один не влит в основную ветку.**

🔴 **Почему они не влиты — причина системная, и её надо знать, чтобы не повторить.** Слот §0.1
любого захода велит исполнить очередь заявок, а стартовое сообщение велит «ничего сверх задачи
не трогай». Пять исполнителей подряд выбрали второе и написали об этом прямо: *«неотменяемые
операции с большим радиусом поражения, не имеющие отношения к теме захода; исполнение без
подтверждения владельца было бы расширением зоны»*. Они правы. Плюс техническая причина:
`vlit-v-osnovnuyu` отказывает — `_studio/docs/KARTA.md` одновременно грязный и участвует
в слиянии, живые рабочие папки, часть файлов требует слияния глазами.

⇒ **В этом заходе влитие — САМА ЗАДАЧА, а не housekeeping.** Противоречия больше нет.

ЦЕЛЬ: основная ветка содержит всю принятую работу, дека собрана и её можно смотреть глазами.

⚠ **Владелец ждёт деку, чтобы посмотреть.** Пока она не собрана из ВЛИТОГО состояния,
заход не сделан, чем бы ни закончились слияния.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-15) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. заявки на влитие не исполнялись пять заходов подряд, потому что противоречили указанию не трогать ничего сверх задачи — теперь влитие САМО является задачей
2. владелец хочет увидеть деку целиком и глазами до того, как сессия закроется
3. фотография на визитке обрезается пополам — зелёную зону надо расширить, а не кадрировать фото
4. четыре ветки приняты приёмкой поимённо: polya-i-uzor, pravila-kadra, rebra-blokov, sluzhebnye-slajdy

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `весь` `репозиторий` `—` `заход` `про` `слияние,` `зона` `не` `сужается` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-deka.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone 'весь' --zone 'репозиторий' --zone '—' --zone 'заход' --zone 'про' --zone 'слияние,' --zone 'зона' --zone 'не' --zone 'сужается' --kommitit 'слияния, гашение веток и собранная дека — хэши всех слияний в отчёт' --zakryt 'рабочие папки влитых веток гасятся после успешного слияния; чужие ветки (arhitektura-doma, dovodka-solvera, rychagi) НЕ трогать'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `весь` `репозиторий` `—` `заход` `про` `слияние,` `зона` `не` `сужается` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-deka.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО это:
  1. `_studio/docs/kak-delat/GIT-disciplina.md` — **главный вход**: канон слияния, гашения
     веток, `--vsyo-ravno`, законный `--no-verify` с причиной.
  2. Вывод `python3 _generator/tools/git_zona.py doctor` и `... zayavki` — живое состояние.
  3. Отчёты четырёх заходов в этой арке — **только секции `## ОТЧЁТ`**, и только если
     конфликт слияния непонятен: там написано, что именно правил каждый.
- 🔴 **Больше ничего не читай.** Заход про слияние, а не про содержание веток.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
### Э0. 🔴 ПОЧЕМУ КОНТУР НЕ СРАБОТАЛ — причина найдена, чинить генератор НЕ надо

⚠ **Первая редакция этого пункта велела править `bootstrap_zahod.py`. Она снята: механизм
исправен, ошибка была у аналитика.** Разбор оставлен здесь, чтобы ты не повторил вывод.

Механизм субагента гит-контура вшит в каждый заход и работает: *«ВЕСЬ КОНТУР — В СУБАГЕНТА,
ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ… сам эти пункты не исполняй»*, плюс отдельный режим
`--zadanie-subagentu`, печатающий готовый промпт. Отчёт прошлого захода подтверждает, что
субагент **запускался**: «гит-контур исполнен субагентом, шесть строк вставлены дословно».

**И он честно ничего не влил, потому что в его задании стояло «вливать нечего».**
А стоит там то, что передал аналитик флагом `--vlit`. Аналитик его не передавал **ни разу**
за пять заходов — генератор в этом случае вписывает либо «вливать нечего», либо маркер пустоты.

⇒ **Тебе из этого следует ровно одно: ветки для влития названы в Э1 ЯВНО и это ЗАДАЧА захода,
а не пункт контура.** Не полагайся на строку «вливать нечего», если она встретится в §0.1
этого файла — она собрана генератором из пустого `--vlit` и говорит о намерении аналитика,
а не о состоянии репозитория. Состояние снимай командой.

### Э0½. 🔴 ЗАПИСАТЬ ПРАВИЛОМ: ХВОСТ АНАЛИТИКА ЗАБИРАЕТСЯ ПЕРВЫМ, ДО ВЛИТИЯ

Решение владельца 2026-08-15: **это правило, а не разовая мера.**

**Что за стена.** `_studio/docs/KARTA.md` в главной папке грязен постоянно. Правит его Cowork —
каждый раз, когда регистрирует документ или собирает заход, — и **закоммитить из песочницы
не может по устройству**. `vlit-v-osnovnuyu` на этом отказывает: файл одновременно грязный
и участвует в слиянии. Каждый субагент гит-контура упирался сюда и честно писал «застряло»;
проверено по отчётам пяти заходов подряд. Три последних коммита в `KARTA.md` — это заходы,
которые вручную «забирали в git правки аналитика», то есть хвост подбирали стихийно и молча.

⇒ **Правило, которое надо вписать в код и в канон:**
> **Субагент гит-контура ПЕРВЫМ пунктом забирает в git то, что Cowork оставил грязным
> в главной папке (`KARTA.md`, `INCIDENTY.md`, автологи), и только ПОТОМ вливает ветки.**
> Это не выход за его права, а условие их применимости: пока хвост не забран, влитие
> отказывает технически.

**Куда вписать — оба дома, а не один:**
- `_generator/tools/bootstrap_zahod.py`, промпт субагента (район строки 840, «Твоя задача —
  четыре пункта») — новый пункт ПЕРЕД вычиткой очереди, с объяснением, почему он первый;
- `_studio/docs/kak-delat/GIT-disciplina.md` — единственный дом правил git; там же назвать цену.

🔴 **Твоя задача шире, чем вписать текст: РАЗБЕРИСЬ, ПОЧЕМУ НЕ РАБОТАЕТ, И ПОЧИНИ ПОЛНОСТЬЮ.**
Хвост аналитика — причина, которую нашла приёмка, но она могла быть не единственной. Отчёты
называли ещё две: живая рабочая папка у сливаемой ветки и «двадцать файлов требуют слияния
глазами». Проверь каждую фактом и почини так, чтобы **следующий заход не упёрся ни в одну**.
Наработки, которые ты уже сделал в этом прогоне, не выбрасывай — учти их вместе с этим.

⚠ **Аналитик в этой теме ошибся трижды подряд** (решил, что механизма субагента нет; решил,
что список веток передаёт аналитик флагом `--vlit`; не заметил, что стена техническая).
Все три раза — вывод по грепу без чтения найденного. **Не наследуй его выводы, проверяй сам.**

### Э1. 🔴 ВЛИТЬ ЧЕТЫРЕ ВЕТКИ В ЭТОМ ПОРЯДКЕ

Порядок хронологический и он не произволен — поздние правки опираются на ранние:

1. `zahod/polya-i-uzor` — поля кадра, узор, прозрачный фон
2. `zahod/pravila-kadra` — правила кадра, скриншотер, **починка сцен и инвариант «сцена = блок»**
3. `zahod/rebra-blokov` — один граф, рёбра блоков
4. `zahod/sluzhebnye-slajdy` — обложка, финальный, разделители, визитка без QR

🔴 **ЧУЖИЕ ВЕТКИ НЕ ТРОГАТЬ:** `arhitektura-doma`, `dovodka-solvera`, `rychagi` и любые другие
не из этого списка — они принадлежат другим аркам и не приняты этой приёмкой.

**Конфликты разрешать по существу, а не «взять свою сторону».** Известные точки:
`_studio/docs/KARTA.md` (регистрация документов — там обе стороны правы, объединяй строки),
`_generator/sborka/tipy.py`, `base.css`, `smeta.py` — их правили несколько заходов подряд.
⚠ **Слияние с неразрешёнными маркерами проходит молча** — после каждого слияния грепни
`<<<<<<<` по влитым файлам, а не полагайся на отсутствие ошибки.

### Э2. Проверить фактом, что влилось именно то

После всех слияний, командами и числами:
- **сумма сцен в собранной деке ≥ 55** — это то, что чинил заход 2, и то, что дважды терялось;
- **рёбра на месте:** `grep -l "^dokazatelstvo_opiraetsya_na:" teorkat-vvedenie/L2/slajdy/*/slaid.md | wc -l` → 23;
- **типов блоков пять**, `NE_KLASSIFICIROVAN` ровно 1;
- **QR не выводится:** `grep -rn "p-qr\|qr_html" _generator/sborka/tipy.py` → пусто;
- гейт карточек, гейт инварианта сцен, фикстуры `sborka/` и `graf/` — зелёные.

🔴 **Любое из этих чисел не сошлось — СТОП, разбирайся.** Молча принять «почти влилось» нельзя:
именно так дважды терялись сцены.

### Э3. Фото на визитке — расширить зелёную зону

Владелец, посмотрев собранный дек: *«кажется, на визитке обрезана моя фотография пополам,
это не очень хорошо. Но там вполне можно расширить зелёную зону, чтобы она влезала,
всё поместится»*.
⇒ Правится **геометрией зоны, а не кадрированием фото**. Место есть: QR убран заходом 4
и освободил квадрат. Проверь кадром, что фотография видна целиком.

### Э4. Погасить отработавшие рабочие папки

После успешного слияния — `git_zona.py poteri --branch <ветка>` и, если `rc=0`,
`zakryt-vetku`. **`rc=1` — не гасить**, назвать в отчёте.
Рабочих папок с меткой `prunable` сейчас 12; подмети те, чьи ветки влиты, чужие не трогай.

### Э5. Собрать деку — ради этого всё и делается

`teorkat-vvedenie/L2/dist/index.html`, **путь от корня репозитория**, С подбором солвера
(не `--bez-podbora`). Дека собирается из ВЛИТОГО состояния основной ветки, а не из чьей-то ветки.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — пять клауз)**

1. **Все четыре ветки влиты:** `git --no-optional-locks branch --no-merged arka/mat-kostyak`
   не содержит ни одной из четырёх. Какая-то не влилась — назови её и причину поимённо.
2. **Маркеров конфликта нет:** `grep -rn "^<<<<<<<" _generator/ _studio/ teorkat-vvedenie/` → пусто.
3. **Числа Э2 сошлись все до одного**, каждое напечатано в отчёте своей командой.
4. **Дека лежит в `teorkat-vvedenie/L2/dist/index.html`**, собрана С подбором, время файла
   свежее источников. Путь назван от корня репозитория.
5. **Фотография на визитке видна целиком** — кадр в отчёте.

🔴 **Код возврата смотри ПЕРВЫМ** (`echo $?`). Слияние, прошедшее с конфликтом, и слияние,
прошедшее чисто, снаружи выглядят одинаково.

6. **Правило записано в ОБА дома** — промпт субагента в генераторе и `GIT-disciplina.md`;
   покажи обе вставки. И **причина отказа устранена, а не обойдена**: следующий заход,
   собранный после твоего, не должен упираться в грязный `KARTA.md`.

**Охват — внутри вердикта:** не «всё влито», а «влито 4 из 4».

---

### Э6. 🔴 ПОСЛЕДНИМ ПУНКТОМ — СДЕЛАТЬ ГИГИЕНУ НЕОБХОДИМЫМ УСЛОВИЕМ ОТЧЁТА

Решение владельца 2026-08-15, дословная рамка: *«каждый заход — это две независимых работы.
Первая — навести полную гигиену со всем, что есть на данный момент. Вторая — собственно заход.
Они независимы: первая делается субагентом, вторая после, и от первой вообще не зависит.
Но первую сделать НЕОБХОДИМО. Обязательно. И это должно быть прописано как обязательная вещь,
без которой отчёт не принимается автоматически — он будет висеть красным, пока не будет
галочки, что все долги закрыты»*.

**Что завести:**

1. **Секцию в файле-заходе**, вшитую генератором, — рядом с `## ОТЧЁТ` или в его конце.
   Заполняет её СУБАГЕНТ гит-контура, не исполнитель. В ней три части:
   - **СНИМОК ВХОДА** — что было не влито, не закоммичено, не вывезено, какие заявки открыты
     НА МОМЕНТ НАЧАЛА захода. Снимается КОМАНДАМИ и записывается с их выводом.
     🔴 Без снимка входа фраза «все долги закрыты» непроверяема: неизвестно, какие были.
   - **ЧТО СДЕЛАНО** — что влито, закоммичено, вывезено, какие ветки погашены, какие заявки
     закрыты, с хэшами.
   - **ГАЛОЧКА:** `ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ: да` — либо `нет` **со списком поимённо**,
     что осталось и почему это невозможно ЕГО правами.

2. **Гейт в `priyomka.py`**: секции нет, снимок входа пуст или галочка не заполнена — **красный**,
   отчёт не принимается.

🔴 **ГЛАВНОЕ ОБ УСТРОЙСТВЕ, и оно важнее самой галочки: ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.**
Исполнители уже ставили такие отметки добросовестно и ошибочно — дословно из отчёта:
*«вливать не моя задача, это работа субагента, уже выполнена»*, при восьми невлитых ветках.
⇒ Приёмка обязана **прогнать те же команды заново** и сравнить с заявленным. Расходится —
красный, **независимо от галочки**. Это наш общий канон: число пишется командой, а не значением;
галочка декларирует намерение, истину устанавливает прогон.

⚠ **Законное «нет» оставить возможным.** Бывают стены, непроходимые его правами: чужая живая
рабочая папка, требуется решение владельца, конфликт, обеих сторон которого он не понимает.
Запретить `нет` совсем — значит заставить ставить `да` неправдиво. Поэтому `нет` допустимо
**только** со списком поимённо, и приёмка судит по списку, а не по факту отметки.

⚠ **Проверь гейт на поломке, а не только на здоровом:** подсунь отчёт без секции и отчёт
с галочкой `да` при реально невлитой ветке — оба обязаны краснеть. Гейт, который не поймал бы
ситуацию этой недели, не считается сделанным.

**Где вписывать:** секция — `bootstrap_zahod.py` (шаблон заходного файла, рядом с `## ОТЧЁТ`);
гейт — `_generator/tools/priyomka.py`; правило — `GIT-disciplina.md` вместе с правилом Э0½.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Нужен — слияние есть самая лоссовая операция из возможных: потерянное при конфликте
не отличается от того, чего никогда не было.**

**ПОСЛЕ-верификатор, свежий субагент, ДРУГОЙ метод:** для каждой из четырёх веток берёт
её вершину через `git show <ветка>:<файл>` и сверяет с влитым результатом — **не потерялась ли
правка, которую эта ветка вносила**. Особое внимание файлам, которые правили несколько веток
подряд: `tipy.py`, `base.css`, `smeta.py`, `KARTA.md`.
Охват: 4 ветки из 4, число внутри вердикта.

🔴 **Опровержение — полноценный успех.** «Правка ветки N потерялась при слиянии» — ровно та
находка, ради которой он зовётся, и стоит она дороже всего остального в этом заходе.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- весь репозиторий — заход про слияние, зона не сужается _studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-deka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- весь репозиторий — заход про слияние, зона не сужается _studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-deka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `весь` `репозиторий` `—` `заход` `про` `слияние,` `зона` `не` `сужается` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-deka.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone весь` → ✅; `python3 _generator/tools/git_zona.py check --zone репозиторий` → ✅; `python3 _generator/tools/git_zona.py check --zone —` → ✅; `python3 _generator/tools/git_zona.py check --zone заход` → ✅; `python3 _generator/tools/git_zona.py check --zone про` → ✅; `python3 _generator/tools/git_zona.py check --zone слияние,` → ✅; `python3 _generator/tools/git_zona.py check --zone зона` → ✅; `python3 _generator/tools/git_zona.py check --zone не` → ✅; `python3 _generator/tools/git_zona.py check --zone сужается` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-deka.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## 🔴 ДОЧИСТКА — приёмка 2026-08-15. Визитка: новая вёрстка не применяется к L2

Владелец открыл собранную деку и увидел **старую визитку с QR**, хотя заход её переделал
и QR убрал. Приёмка разобралась: **ничего не потеряно и вливать нечего** — причина в другом.

**Что происходит.** У лекции L2 в `teorkat-vvedenie/L2/brief.md` стоит override:
```
vizitka_illustracii: vizitka-celikom
```
Он говорит генератору: визитку НЕ рисуй, покажи вместо неё вот эту иллюстрацию
(`deck.py`, район строк 451–455; `tipy.py` — «канон `sluzhebnye/` не рисуется»).
А сама иллюстрация — **растр старой визитки**. Из её же заказа
(`teorkat-vvedenie/L2/illustracii/vizitka-celikom/zakaz.md`), дословно:

> *«слайд-визитка автора целиком (фото, QR, био, декоративные завитки) — перенесён растром 1:1
> со страницы 2 шаблона первой лекции, без изменений… по требованию владельца „перенести 1 в 1"»*
> источник: растеризация `Теоркат_Шаблон.pdf`, страница 2

⇒ Новая вёрстка визитки, которую ты сделал, к L2 не применяется вовсе, и QR виден потому,
что он нарисован внутри картинки.

### Д1. Снять override и проверить, что новая визитка встала

Убрать `vizitka_illustracii: vizitka-celikom` из `teorkat-vvedenie/L2/brief.md` — тогда визитка
рисуется генератором по вёрстке, которую ты сделал.
⚠ **Саму иллюстрацию и её заказ НЕ удалять** — она сделана по прямому требованию владельца
и может понадобиться; достаточно, чтобы её перестал звать `brief.md`.

**Проверить кадром, а не структурой** (пустой контейнер структурно валиден и гейтом не ловится):
фотография видна целиком и не обрезана пополам, `qr` на кадре нет, текст читается, слайд не пуст.
Кадр — в `kadry/`, имя назвать в отчёте.

### Д2. Проверить, нет ли того же у обложки и финального

Тот же механизм override мог стоять и на них. Грепни `brief.md` на поля вида `*_illustracii`
и назови в отчёте, что нашёл: если обложка тоже рисуется растром, твоя новая геометрия
не применяется и к ней — а мы этого не заметили, потому что смотрели на картинку.

### Д3. Пересобрать деку и назвать путь

`teorkat-vvedenie/L2/dist/index.html`, с подбором. Владелец смотрит её глазами.

**КРИТЕРИЙ ДОЧИСТКИ:**
1. `grep -n "vizitka_illustracii" teorkat-vvedenie/L2/brief.md` → пусто.
2. Кадр визитки в `kadry/`: фото целиком, QR нет, текст на месте.
3. Названо, есть ли такой же override у обложки и финального слайда.
4. Дека пересобрана, сумма сцен не упала (эталон 55), путь от корня репозитория.

---

## ПЛАН — (заполняет исполнитель)

### П0. Что снято командами ПЕРВЫМ ходом (не предположено)

| Проверка | Команда | Результат |
|---|---|---|
| ветка | `git branch --show-current` | `arka/mat-kostyak` ✅ (rc=0) |
| счёт незакрытого | `schet_nezakrytogo.py …` | инцидентов 0, уроков 0, очередь 0; **долгов ЖИВ 39** (в снимке сборки стояло «н/д») |
| невлитых `zahod/*` | `git branch --no-merged \| grep -c 'zahod/'` | **9** (в снимке сборки стоял 0 — снимок устарел, как заход и предупреждал) |
| грязь дерева | `git status --porcelain \| wc -l` | 29 путей |

### П1. 🔴 ТРИ ОСПОРЕННЫЕ ПРЕДПОСЫЛКИ — до работы, как велит §1

**П1.1. Ветки `zahod/rebra-blokov` НЕ СУЩЕСТВУЕТ — она уже влита, и это не потеря.**
`git branch -a --list '*rebra*'` → пусто и локально, и на `origin`. Причина найдена в логе:
`2027bdc Merge branch 'zahod/rebra-blokov' into arka/mat-kostyak` — ветка влита раньше и погашена.
Проверка по содержанию, а не по имени: рёбра на месте прямо сейчас в `arka/mat-kostyak` —
`grep -l "^dokazatelstvo_opiraetsya_na:" teorkat-vvedenie/L2/slajdy/*/slaid.md | wc -l` → **23**,
то есть число Э2 уже сходится до всяких слияний.
⇒ **Влить надо ТРИ ветки из четырёх названных, а не четыре.** Клауза 1 критерия от этого не
страдает: `branch --no-merged` не может содержать ветку, которой нет. Охват в вердикте — «3 из 3
существующих при 4 названных», а не «4 из 4».

**П1.2. `vlit-v-osnovnuyu` откажет на всех трёх, и это НЕ грязь `KARTA.md` — это живые рабочие папки.**
Причина в КОНТЕКСТЕ захода названа наполовину. Читаю код: `git_zona.py:3703` — `vlit-v-osnovnuyu`
отказывает, если ветка вычекаучена в рабочей папке, **включая свою собственную**. Все три ветки
стоят в `materials-wt/*` (`git worktree list`), значит отказ гарантирован для каждой.
⇒ **Вливаю `merge`, стоя на `arka/mat-kostyak` в главной папке.** Это то же самое влитие в основную
(`merge` вливает в ТЕКУЩУЮ ветку, а текущая и есть основная — `GIT-disciplina §4б-бис`), и лестница
от этого не растёт: я не в ветке захода, а в основной. Живая рабочая папка снимается
`--vsyo-ravno "<причина>"` — причина законная и называется поимённо: заходы приняты приёмкой
2026-08-15, их сессии окончены. **Перед каждым `--vsyo-ravno` проверяю рабочую папку на
незакоммиченное** (`git -C <папка> status --porcelain`): грязь там означает, что merge её не
заберёт, а Э4 потом сотрёт папку — ровно та потеря, ради которой зовётся верификатор §3.

**П1.3. §0.1 требует отдать слияния субагенту — а Э1 отдаёт их мне. Разрешаю в пользу Э1, вот почему.**
Три довода, каждый проверяемый:
1. **Собственное обоснование §0.1 здесь ложно.** Оно дословно: очередь, влития и гашение
   «на содержание твоей задачи не влияют». В ЭТОМ заходе они и есть задача — так сказано
   в КОНТЕКСТЕ («⇒ В этом заходе влитие — САМА ЗАДАЧА, а не housekeeping») и в Э0
   («у него git и есть задача»).
2. **Задание субагенту вышло с дырой на своём центральном пункте.** Я напечатал его командой из §0.1;
   пункт 1 буквально содержит `🔴 <НЕ ЗАПОЛНЕНО — чем заполнять: ветки … флагом --vlit>`:
   аналитик не передал `--vlit`. Субагент не узнал бы, ЧТО вливать.
3. **Предписанная ему команда — `vlit-v-osnovnuyu`** — откажет на всех трёх ветках (П1.2),
   а права «ничего сверх» не дают ему сменить инструмент.
Плюс шапка захода: модель выбрана потому, что «слияния с конфликтами требуют суждения по ходу» —
отдать это суждение субагенту с пустым контекстом арки значит выбросить причину выбора модели.
⇒ **Слияния делаю сам.** Шесть строк контура даю в `## ОТЧЁТ` в предписанной форме, но честно
подписанные как мои, а не как ответ субагента. **Субагент §3 (ПОСЛЕ-верификатор) запускается
обязательно** — он ловит именно потерю при слиянии и с Э1 не пересекается.

### П2. Порядок работы

0. **Точка отката** — зона грязна в HEAD, фабриковать не надо: тег/коммит текущего состояния
   + запись хэша `HEAD` (`bde341d`) в отчёт как адрес возврата.
1. **Э0** — правка `bootstrap_zahod.py` (стартовое сообщение обоих каналов + §0.1 + сужение
   лазейки «упёрся — работай дальше»), пробная сборка во временную папку, вывод обеих секций
   в отчёт. Пробный файл не коммитить.
2. **Расчистить грязь, которая пересекается со сливаемым** — иначе `merge` откажет законно.
   Пересечение снято командой, не угадано: `INCIDENTY.md` (с `polya-i-uzor` и `pravila-kadra`),
   `KARTA.md` + два `.png` кадра (с `pravila-kadra`). Коммичу их явными путями отдельным ходом.
   `ucheniki/maksim/*` со слияниями НЕ пересекается и мне не принадлежит — не трогаю, называю в отчёте.
3. **Э1 — три слияния в хронологическом порядке:** `polya-i-uzor` → `pravila-kadra` → `sluzhebnye-slajdy`.
   После каждого: `echo $?` первым, затем `grep -rn '^<<<<<<<'` по влитым путям (маркеры проходят молча).
   Конфликты — по существу; `KARTA.md` объединять строками, не выбирать сторону.
4. **Э2 — пять чисел, каждое своей командой** (сцены ≥55, рёбра 23, типов 5 + `NE_KLASSIFICIROVAN` 1,
   QR пусто, гейты зелёные). Не сошлось — СТОП и разбор, а не «почти влилось».
5. **Э3 — зелёная зона визитки шире, фото не кадрировать.** Проверка кадром.
6. **Э5 — сборка деки** `teorkat-vvedenie/L2/dist/index.html` С подбором солвера, из влитого состояния.
   (Э5 идёт до Э4: гасить папки, пока дека не собрана и не проверена, значит убирать страховку.)
7. **Э4 — гашение** `poteri --branch` → `zakryt-vetku`, `rc=1` не гасить. Чужие
   (`arhitektura-doma`, `dovodka-solvera`, `rychagi*`, `vid-blokov-vnedrenie`, `modeli-arn`,
   `obratnyj-progon`, `solver-v3-dyhanie`) не трогать.
8. **Верификатор §3** — свежий субагент, `git show <ветка>:<файл>` против влитого, охват 3 из 3.
9. **§4 — коммит своей зоны САМ**, два хода с явными путями, `--no-optional-locks`.

### П3. Что я НЕ делаю и почему (названо до работы, а не после)

- **`git checkout` — ни одного**, ни при каких обстоятельствах (контракт зоны, цена 27→28.07).
- **Вывоза на `origin` не делаю**: `push` в критерий готовности не входит, а это внешнее
  необратимое действие. Называю строкой в отчёте как открытое.
- **Заявки 1 и 2 очереди** (`gejt_kartochki.py` narrativ, `tipy_slajdov.py` Т2/Т3) — не git и
  `git_zona.py`-командами неисполнимы вовсе; чужая тема, в отчёт, не трогаю.
- **Второй репозиторий** (`disciplina`) — вне зоны и, по счёту незакрытого, физически недоступен.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `zahod/vid-blokov-vnedrenie` НЕ влита, и это решение, а не недосмотр. Она невлита, но в четвёрку
   приёмки не входит и в списке «чужих» тоже не названа. Единственный её невлитый коммит —
   `add14e2 «сохранено перед паузой, работа не проверялась»`: он тащит в КОРЕНЬ репозитория
   четыре черновых файла (`podbor-margin.md`, `podbor_margin.py`, `progon.log`, `progon.py`)
   и правит 14 слайдов Л2 непроверенной работой. Влить её значило бы внести в основную ветку
   непросуженное. Нужно ваше решение: принимать эту ветку отдельно или гасить.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. У визитки Л2 QR живёт ВТОРОЙ раз — впечатан в растр `teorkat-vvedenie/L2/illustracii/vizitka-celikom/`,
   и именно этой картинкой визитка Л2 рисуется (`vizitka_illustracii: vizitka-celikom` в `brief.md`).
   Решение «QR убирается совсем» дошло только до канона; в деке, который вы откроете, QR на месте,
   и правка Э3 (зелёная зона + фото целиком) там тоже не видна — канон-ветка в дек Л2 не попадает.
   Находка не моя: её назвал заход `sluzhebnye-slajdy` (его `## ОТЧЁТ`, п.1), и она пережила приёмку.
   Развилка ваша: либо перерисовать растр без QR, либо снять `vizitka_illustracii` и отдать визитку канону.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. Клауза Э2 «`grep -rn "p-qr\|qr_html" _generator/sborka/tipy.py` → пусто» невыполнима буквально
   и не станет выполнимой: ветка `sluzhebnye-slajdy` документирует снятие слота в докстринге
   (`tipy.py:471`, «слот `.p-qr`/`qr_html` не читается»). Гейт судит текст, а не смысл, поэтому
   критерий краснеет на честной документации. Критерий надо сузить до места вывода
   (например `grep -n '\.p-qr{' _generator/sborka/tipy.py` → пусто), иначе следующий заход либо
   сотрёт объяснение, либо припишет «сошлось» неправде.
   ДОМ: _studio/zhurnal/2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

4. Э0½ велит вписать правило в `_studio/docs/kak-delat/GIT-disciplina.md` как «единственный дом
   правил git». По этому адресу с 06.08 лежит УКАЗАТЕЛЬ: сам дом переехал в
   `_meta/disciplina/GIT-disciplina.md`, и файл-указатель это прямо говорит. Я вписал правило
   в настоящий дом (`_meta/...`), а не в указатель. Адрес в шаблоне заходов стоит поправить.
   ДОМ: _meta/disciplina/GIT-disciplina.md
   ДОСТАВЛЕНО: нет

5. Долг калибровки: ловушка 38 фикстуры `sborka` красна (смета против браузера, 4 карточки).
   Она была красной до этого захода — но моё утверждение в причине обхода («`smeta.py` не менялся»)
   оказалось ЛОЖНЫМ: слияние `pravila-kadra` принесло Д5, переключивший смету с литералов на импорт
   геометрии (27 вставок). Не установлено, сдвинул ли Д5 калибровку дополнительно; набор карточек
   сменился с 4 на 5 и обратно на 4. Поправка вписана в `INCIDENTY.md` той же датой.
   ДОМ: _studio/zhurnal/_INFRA-git/DOLG.md
   ДОСТАВЛЕНО: нет

6. Долг содержания: гейт карточек красный — `vektornye-prostranstva: бюджет слов превышен: 79 > 78`.
   Не моё и не слияния: текст слайда вырос с 2295 до 2303 слов в самой ветке `polya-i-uzor`
   (сверено по трём ревизиям), бюджет 78 не менялся ни в одной. То есть принятая приёмкой ветка
   принесла с собой красный гейт. Чинится словом автора, не слиянием.
   ДОМ: _studio/zhurnal/2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (обычно заполняет СУБАГЕНТ гит-контура)
> ⚠ **Здесь её заполнил исполнитель, и это законное исключение, названное вслух.** Этот заход
> собран СТАРЫМ шаблоном — секции в нём не было, она заведена ЭТИМ же заходом (Э6). Гит-контур
> тоже вёл я сам: в этом заходе он и есть задача (Э1), а не housekeeping. Со следующего
> собранного захода секция приезжает из `_TEMPLATE-zahod.md` и заполняет её субагент.

**СНИМОК ВХОДА** *(снят первым ходом, до всякой работы)*
```
$ git --no-optional-locks branch --no-merged | grep -c 'zahod/'
9                       # в §0.1 захода стояло 0 — снимок сборки соврал
$ git --no-optional-locks status --porcelain | wc -l
29                      # хвост Cowork: KARTA.md, INCIDENTY.md, VERDIKTY.md, УРОКИ,
                        # 7 выгрузок сессии, кадры арки, 5 незарегистрированных чужих .md
$ git --no-optional-locks log --oneline @{u}.. | wc -l
0                       # doctor: «впереди на 0, позади на 0»
$ python3 _generator/tools/git_zona.py zayavki
Открытых заявок: 4      # 2 содержательные (не git, командами git_zona неисполнимы),
                        # 1 blokiruet «влить zahod/pravila-kadra», 1 «закоммитить работу сессии»
$ python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-10_dizajn-i-metriki
долгов СТАТУС: ЖИВ — 39 # в заходе стояло «н/д»
```

**ЧТО СДЕЛАНО**
- **хвост Cowork забран** — `2366ee5` (24 файла: индекс, автологи, выгрузки, кадры);
  автолог добирался между слияниями — `7e2c780`, `37db001`;
- **влито в основную 3 ветки** — `a7ebca6` (`polya-i-uzor`), `07e1b9d` (`pravila-kadra`),
  `6d99278` (`sluzhebnye-slajdy`); четвёртая названная, `rebra-blokov`, влита раньше — `2027bdc`;
- **работа захода закоммичена** — `dcd9017` (21 файл), Э6 — коммитом ниже;
- **погашена 1 ветка** — `zahod/pravila-kadra`, надгробие `mogila/zahod/pravila-kadra`;
- **заявок закрыто 0.** Не закрывал намеренно: закрытие заявки — ход субагента гит-контура,
  которого в этом заходе не было, а приписывать себе чужой ход в очереди хуже, чем оставить её
  открытой. Заявка `2026-08-15T0121` («влить `pravila-kadra`») по существу исполнена — `07e1b9d`;
- **не вывезено** — `push` не делал: в критерий не входит, а это внешнее необратимое действие.

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`
`zahod/polya-i-uzor` — рабочая папка не гасится: `zakryt-vetku` rc=1, в ней несобранный `?? dist/index.html`; инструмент прямо говорит, что `--vsyo-ravno` грязную папку не перебивает
`zahod/sluzhebnye-slajdy` — то же, rc=1, та же причина
`zahod/vid-blokov-vnedrenie` — не влита: не в четвёрке приёмки, единственный коммит «работа не проверялась» тащит черновики в корень репозитория; нужно решение владельца (ВОПРОСЫ п.1)
`zahod/arhitektura-doma`, `zahod/dovodka-solvera`, `zahod/rychagi`, `zahod/rychagi-2`, `zahod/rychagi-3` — чужие арки, трогать запрещено контрактом захода
заявки 1 и 2 очереди — не git и командами `git_zona.py` неисполнимы вовсе; чужая тема
15 коммитов не вывезены на `origin` — нужно ваше решение, `push` вне критерия

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials/teorkat-vvedenie/L2/dist/index.html` — открывать браузером (двойным щелчком; файл самодостаточный, 3.7 МБ, шрифты и картинки внутри)
Второй артефакт, кадр для Э3: `/private/tmp/claude-501/-Users-ivanyakovlev/560e4900-4f46-4d8a-beb5-9c54ce511d8f/scratchpad/kadry/vizitka-KANON-POSLE.png` — визитка канона после правки.

### Что сделал и ЗАЧЕМ

**Стена снята первой, влитие — вторым.** Правки Cowork (`KARTA.md`, `INCIDENTY.md`, `VERDIKTY.md`,
уроки, семь выгрузок сессии, кадры арки) забраны в git коммитом `2366ee5` ДО первого слияния.
Проверено фактом, а не поверено: до коммита `merge zahod/polya-i-uzor` отказывал словами
«путей ОДНОВРЕМЕННО грязные и участвуют в слиянии», после — прошёл.

**Найдена ВТОРАЯ половина той же стены, которой в заходе не было.** Законный `--no-verify` пишет
строку в `INCIDENTY.md`, а он участвует в следующем слиянии — то есть каждое слияние с обходом
блокирует следующее. Замер: `merge zahod/pravila-kadra` отказал на `INCIDENTY.md`, ставшем грязным
секундой раньше из-за обхода при слиянии `polya-i-uzor`. Лечится коммитом автолога МЕЖДУ слияниями
(`7e2c780`, `37db001`); правило вписано в оба дома.

**Три названные причины отказа — проверены каждая, фактом:**

| причина из отчётов | вердикт | чем проверено |
|---|---|---|
| грязный `KARTA.md` (хвост Cowork) | **ИСТИННА** | отказ до `2366ee5`, проход после |
| живая рабочая папка сливаемой ветки | **ИСТИННА** | `git_zona.py:2863` и `:3708` отказывают; снимается `--vsyo-ravno` с причиной. Перед каждым снятием проверял папку на незакоммиченное — всюду лежал только собираемый `dist/index.html` |
| «20 файлов требуют слияния глазами» | **ИСТИННА, не фантом** | `git diff --diff-filter=U` при слиянии `pravila-kadra` → **19 файлов** |

### Влитие — 4 из 4 названных, но ветки было ТРИ

`zahod/rebra-blokov` **не существует** ни локально, ни на `origin`: она влита раньше коммитом
`2027bdc` и погашена. Проверил по содержанию, а не по имени — рёбра были на месте до всяких
слияний (23). Клауза 1 от этого не страдает: `branch --no-merged` не может содержать ветки, которой нет.

| ветка | хэш слияния | конфликтов | как разрешены |
|---|---|---|---|
| `zahod/polya-i-uzor` | `a7ebca6` | 1 | `INCIDENTY.md` — объединение записей по времени, 116 + 2 |
| `zahod/pravila-kadra` | `07e1b9d` | 19 | ниже поимённо |
| `zahod/rebra-blokov` | `2027bdc` (ранее) | — | влита до этого захода |
| `zahod/sluzhebnye-slajdy` | `6d99278` | 1 | `tipy.py` — ниже |

**Три конфликта разрешены объединением, а не выбором стороны** — в каждом столкнулись две живые
принятые правки, и взятие любой стороны целиком снесло бы чужую:
- `_generator/tools/fixtures/sborka/PROGNAT.sh` — **две РАЗНЫЕ ловушки получили один номер 44**
  в параллельных ветках (адресат рёбер от `rebra-blokov` и инвариант «сцена = блок» от `pravila-kadra`).
  Общего у них только номер. Обе сохранены, второй дан номер 44-бис и свои имена переменных.
- `_generator/sborka/porodit_slaid.py` — в одной строке встретились `zametki` (Д8 `pravila-kadra`)
  и `adresa` (рёбра `rebra-blokov`). Оба сохранены; проверено, что вызываемые функции принимают оба.
  Справка приведена к рантайму («первый СВОБОДНЫЙ слот»), рантайм прочитан, а не предположен.
- `_generator/sborka/tipy.py`, визитка — `sluzhebnye-slajdy` убирала код-квадрат, `pravila-kadra`
  тем же куском вводила override `padding-left:18px`. Квадрат убран, override сохранён, число
  подстановок пересчитано с восьми на шесть.

### 🔴 СЛИЯНИЕ ПОТЕРЯЛО РЁБРА, И Я ЭТО ПОЙМАЛ ЧИСЛОМ Э2 — САМОЕ ВАЖНОЕ В ЗАХОДЕ

13 файлов `slaid.md` конфликтовали в слиянии `pravila-kadra`. Я посмотрел ОДИН из них (`inyekcii`),
увидел там только кэш солвера (`liniya`, `podbor_avto`) и **обобщил вывод на все тринадцать**,
взяв сторону ветки целиком. Ветка отпочковалась ДО влития `rebra-blokov`, поэтому рёбер в её
слайдах нет: `dokazatelstvo_opiraetsya_na` упало **с 23 до 10**, `primer_dlya` — тоже с 23 до 10.

Поймано клаузой Э2, ровно как она и задумана. Разбор: `git diff <merge-base> zahod/pravila-kadra`
по этим файлам показал, что ветка меняла в них ТОЛЬКО поля солвера (`podbor_avto`, `kegl_px`,
`liniya`, `mezhstrochye`, `otstup_bloka`) — то есть корректное разрешение это версия `a7ebca6`
плюс пересчёт подбора. Восстановил 13 файлов из `a7ebca6`, пересобрал деку с `--zanovo`:
рёбра 23 и 23, ничего из работы `pravila-kadra` не потеряно.

**Урок себе и следующему: вид конфликта, снятый с одного файла, на остальные не переносится.**

### Числа Э2 — каждое своей командой

| что | команда | ответ | сошлось |
|---|---|---|---|
| сумма сцен ≥ 55 | `kadry.py teorkat-vvedenie/L2 --dist … --scen-ne-menshe 55` | `СЦЕНЫ ПО ДЕКЕ: сумма 55, слайдов 26`, rc=0 | ✅ |
| рёбра = 23 | `grep -l "^dokazatelstvo_opiraetsya_na:" teorkat-vvedenie/L2/slajdy/*/slaid.md \| wc -l` | `23` | ✅ |
| (то же для примеров) | `grep -l "^primer_dlya:" …` | `23` | ✅ |
| типов блоков пять | `python3 -c "import bloki; print(len(bloki.TIPY_BLOKOV))"` | `5` — `dokazatelstvo, narrativ, opredelenie, primer, utverzhdenie` | ✅ |
| `NE_KLASSIFICIROVAN` ровно 1 | `grep -l "^tip_slaida: *NE_KLASSIFICIROVAN" teorkat-vvedenie/L2/slajdy/*/slaid.md \| wc -l` | `1` (`kategoriya-vect-iso`) | ✅ |
| QR не выводится | `grep -rn "p-qr\|qr_html" _generator/sborka/tipy.py` | **1 строка** — докстринг `tipy.py:471`, объясняющий снятие слота; кода вывода нет | ⚠ см. ВОПРОСЫ п.3 |
| фикстура `graf/` | `bash _generator/tools/fixtures/graf/PROGNAT.sh` | rc=0, `ВСЁ ЗЕЛЁНОЕ` | ✅ |
| фикстура `zahod/` | `bash _generator/tools/fixtures/zahod/PROGNAT.sh` | rc=0, `ФИКСТУРЫ ЗЕЛЁНЫЕ` | ✅ |
| гейт карточек | `python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2` | rc=1, `проверено 23 из 23, замечаний 1` | ❌ долг, ВОПРОСЫ п.6 |
| фикстура `sborka/` | `bash _generator/tools/fixtures/sborka/PROGNAT.sh` | rc=1, ловушка 38 (смета) | ❌ долг, ВОПРОСЫ п.5 |

**Два красных — оба чужие и оба доказаны как таковые, а не объявлены.** Гейт карточек красен на
`79 > 78` слов в `vektornye-prostranstva`: текст вырос в самой ветке `polya-i-uzor` (2295→2303 слова
по трём ревизиям), бюджет 78 не менялся нигде. Ловушка 38 была красной до захода (две независимые
записи в `INCIDENTY.md` от 15:20 и 15:39 сегодня, одна с замеренным baseline).

### Э3 — фото на визитке

**Причина найдена, а не угадана.** Картинка канона — JPEG **448×448**, отношение сторон 1.000
(снято разбором заголовка SOF, не на глаз). Рамка была **251×555**, отношение 0.452. При
`object-fit:cover` квадрат растягивается по высоте и режется по ширине 555→251: видно 45 % кадра.
Породил это заход 4 — он отдал фото освободившееся от QR место, растянув рамку 260→555 по высоте
и не тронув ширину.

**Правил геометрией зоны, фото не кадрировал**, как и просили: рамка стала квадратной по родной
стороне картинки (448, без апскейла), доска расширилась с 410 до **607 px** ровно на столько,
чтобы поля слева и справа остались прежними (81 и 78); по вертикали фото по центру (181/181).
Числа не литералы — выведены в коде из стороны картинки. Солвер сам переподобрал текст под новую
колонку (`kegl=41.0`). **Кадр в артефактах выше: фотография видна целиком.**
⚠ В деке Л2 эта правка НЕ видна — визитка Л2 рисуется растром-override с QR (ВОПРОСЫ п.2).

### Э0½ — правило записано в оба дома

1. **`_generator/tools/bootstrap_zahod.py`**, промпт субагента: новый **пункт 0
   «ЗАБРАТЬ В GIT ХВОСТ COWORK — ПЕРВЫМ, ДО ОЧЕРЕДИ И ДО ВЛИТИЯ»** с ценой и с командами;
   снято противоречие, из-за которого правило и не работало — прежнее «Сам НЕ коммить»
   запрещало субагенту ровно то, без чего влитие невозможно. Запрет сужен до содержательной
   работы. Пункты перенумерованы (0–4), доклад остался шестистрочным.
2. **`_meta/disciplina/GIT-disciplina.md`**, §4б-бис — раздел «ХВОСТ COWORK» с ценой, со второй
   половиной болезни (автолог отрастает между слияниями) и с границей «что в хвост не входит».
   Про адрес из задания — ВОПРОСЫ п.4.
3. **Чем держится, а не только записано:** ловушка 42 фикстуры `zahod` доработана — она проверяет
   не наличие текста, а **порядок**: позиция пункта «хвост Cowork» обязана быть меньше позиции
   пункта влития. Пункт, стоящий после влития, не лечит ничего. Фикстура зелёная, rc=0.

### Что НЕ трогал

- **`ucheniki/maksim/`** (`ZHURNAL.md`, `dz-09-raskraska-invariant.html`) — ваша преподавательская
  работа, со слияниями не пересекается, к арке отношения не имеет.
- **Пять незарегистрированных чужих `.md`** аналитика (`AUDIT-BAZA-2026-08-15.md`,
  `HANDOFF-2026-08-16-kachestvo.md`, `TZ-desyatyj-skill.md`, `kod_dom-shablonov.md`,
  `kod_shablony-odin-dom.md`) — регистрировать за автора значило бы выдумать описание.
  Часть из них появилась ПО ХОДУ моего прогона: аналитик пишет прямо сейчас.
- **Чужие ветки** — `arhitektura-doma`, `dovodka-solvera`, `rychagi`, `rychagi-2`, `rychagi-3`.
- **Заявки 1 и 2 очереди** (`gejt_kartochki.py` narrativ, `tipy_slajdov.py` Т2/Т3) — это не git
  и командами `git_zona.py` неисполнимо вовсе; чужая тема.
- **Вывоза на `origin` не делал** — в критерий не входит, а это внешнее действие. Открытое.

### Э4 — гашение: 1 из 3, и это честный отказ

| ветка | `poteri` | `zakryt-vetku` | итог |
|---|---|---|---|
| `zahod/pravila-kadra` | rc=0, безопасна 1 из 1 | rc=0 | **погашена**, надгробие `mogila/zahod/pravila-kadra` |
| `zahod/polya-i-uzor` | rc=0 | **rc=1** | НЕ гашу: в её рабочей папке лежит несобранный `?? teorkat-vvedenie/L2/dist/index.html`; инструмент прямо говорит, что `--vsyo-ravno` грязную папку не перебивает |
| `zahod/sluzhebnye-slajdy` | rc=0 | **rc=1** | то же |

### Первый ход, снятый командами (снимки сборки устарели, как заход и предупреждал)

- невлитых `zahod/*`-веток было **9**, а не 0, как стояло в §0.1;
- долгов СТАТУС: ЖИВ — **39**, а не «н/д».

### НЕОБРАТИМОЕ — отдельным списком

1. **13 файлов `teorkat-vvedenie/L2/slajdy/*/slaid.md` перезаписаны** версией из `a7ebca6`
   (восстановление рёбер) · восстанавливается: `git show 07e1b9d:<путь>` вернёт до-восстановительное
   состояние, `git show a7ebca6:<путь>` — то, что положено сейчас.
2. **Три ветки влиты в `arka/mat-kostyak`** · откат: `git reset --hard 2366ee5` вернёт состояние
   до первого слияния; точка отката коммитом, а не на словах.
3. **Ветка `zahod/pravila-kadra` погашена** · воскрешается одной командой:
   `python3 _generator/tools/git_zona.py voskresit --branch zahod/pravila-kadra` (надгробие
   `mogila/zahod/pravila-kadra` на этом диске).
4. **Три `--no-verify`** при слияниях (чужой долг, причины в `INCIDENTY.md`) · необратимого следа
   не оставляют, но названы, потому что обход — решение, а не техника.
5. **Пробная сборка захода** во временную папку `_studio/zhurnal/_proba-tmp/` создана и удалена
   тем же ходом; `KARTA.md` восстановлен побайтно (md5 до `1afc117fd5acc879570016fcf6cafc30`,
   после — он же). Следа в репозитории нет.
6. **Правки `bootstrap_zahod.py` первой редакции Э0 откачены** по вашему указанию из HEAD;
   сохранены отдельно в
   `/private/tmp/claude-501/-Users-ivanyakovlev/560e4900-4f46-4d8a-beb5-9c54ce511d8f/scratchpad/otkloneno-E0/`.

### 🔴 ПОПРАВКА К СОБСТВЕННОМУ УТВЕРЖДЕНИЮ

В причине обхода при слиянии `pravila-kadra` я написал, что `smeta.py` «в индексе не менялся».
**Это ложь**, и обнаружила её моя же проверка в той же команде: `git diff --stat a7ebca6 HEAD --
_generator/sborka/smeta.py` даёт 27 вставок и 6 удалений — слияние принесло Д5, переключивший
смету на импорт геометрии. Верно осталось: конфликта в `smeta.py` не было и ловушка 38 краснела
до захода. Поправка вписана в `INCIDENTY.md` отдельной строкой, не затёрта.

### ПОВТОРЯЕМОСТЬ находок

- **ПОВТОРИТСЯ на следующем же заходе, класс НЕМЕДЛЕННОЕ** — хвост Cowork и его отрастание между
  слияниями. Поэтому и чинилось механизмом (пункт 0 промпта + канон + ловушка 42 на ПОРЯДОК),
  а не записью в очередь: следующий заход упрётся в ту же стену через час.
- **ПОВТОРИТСЯ** — потеря рёбер при слиянии ветки, отпочкованной до влития `rebra-blokov`.
  Живых таких веток осталось шесть. Дешёвая страховка: гонять число рёбер сразу после слияния.
- **НЕ повторится, законно уходит в очередь** — QR в растре визитки, бюджет слов
  `vektornye-prostranstva`, адрес `GIT-disciplina.md` в шаблоне, буква клаузы Э2 про `p-qr`.

### Время и токены

**НЕПРИМЕНИМО на канале `app`** — лог прогона кладёт `tee` только на канале `terminal`,
сессия в приложении такого следа не оставляет. Это свойство канала, а не недосмотр.

### Э6 — гигиена как необходимое условие отчёта

**Три места, как и велено, плюс четвёртое — то, чем это держится.**

1. **Секция** `## ГИГИЕНА ВХОДА` вшита в `_studio/zhurnal/_TEMPLATE-zahod.md` (шаблон, который
   кладёт `bootstrap_zahod.py` в каждый заход), перед `## ОТЧЁТ`: снимок входа · что сделано ·
   галочка. Проверено сборкой пробного захода — секция в свежем файле есть; проба удалена,
   `KARTA.md` восстановлена.
2. **Гейт `Г12`** в `_generator/tools/priyomka.py`, подключён в общий прогон (гейтов стало 14).
3. **Правило** — `_meta/disciplina/GIT-disciplina.md`, §4б-бис, рядом с правилом Э0½.
4. **Ловушки 33–36б** фикстуры `priyomka` — иначе новый гейт ничем не сторожится.

**🔴 ГАЛОЧКА НЕ ИСТОЧНИК ИСТИНЫ — и это проверено на поломке, а не объявлено.** Семь случаев:

| случай | вердикт | почему так и надо |
|---|---|---|
| секции нет вовсе | ❌ красный | гигиена — обязательная работа, не опция |
| снимок входа пуст (один шаблон) | ❌ красный | без снимка «долги закрыты» непроверяемо |
| галочка не заполнена (плейсхолдер) | ❌ красный | |
| галочка `нет` БЕЗ списка | ❌ красный | |
| галочка `нет` СО списком поимённо | ✅ зелёный | иначе заставим ставить `да` неправдиво |
| **галочка `да` при РЕАЛЬНО невлитой ветке** | **❌ красный** | **ситуация этой недели; гейт называет ветки поимённо** |
| галочка `да`, а команда не отработала | ✅ зелёный | гейт **воздерживается**, а не подтверждает |

Главная ловушка (36) сначала уходила в `SKIP`, когда в репозитории прогона невлитых не было, —
то есть не сторожила ровно в здоровом состоянии. Переписана: строит СВОЙ одноразовый репозиторий
с заведомо невлитой веткой, гоняется всегда. Рядом положительная пара 36б — иначе ловушка была бы
зелёной и у гейта, который краснеет на всём.

Фикстуры после Э6: `priyomka` rc=0, `zahod` rc=0.

### ВЕРИФИКАТОР (§3) — охват 4 из 4, две находки, обе разобраны

Свежий субагент, метод другой (сверка вершин через `git show`, не чтение diff слияния).
Методическая поправка от него: предписанная формула `merge-base arka/mat-kostyak <ветка>` даёт
пустой список — вершины уже предки арки; он считал от точки ветвления `merge-base <M^1> <M^2>`.
Обязательные факты а)–д) подтвердил командами: рёбра 23 и 23, обе ловушки 44 и 44-бис живы,
в `porodit` оба (`zametki` и `adresa`), QR у визитки нет и override Д5 на месте, `smeta.py`
импортирует геометрию из `tipy`. Моё утверждение про a7ebca6 проверил независимо и **подтвердил**:
`diff fff0d70 6862461` по слайдам — 102 изменённых строки, солверных 102, прочих **0**.

**Находка 1 — «солверная правка `anons-retraktov` откачена». ОПРОВЕРГНУТА прогоном, не доводом.**
Проверил не рассуждением: запустил `deck.py --zanovo` повторно. Солвер переподобрал слайд заново
и выдал те же `kegl=41 / 1.57 / 30.3` — файл байт-в-байт тот же (md5 `82caf0bb…` до и после).
Значит это ответ ТЕКУЩЕГО слитого генератора (`--zanovo` по контракту игнорирует и метку
`podbor_avto`, и легаси-значения), а не протечка устаревшей стороны. Верификатор не мог этого
знать — семантика `--zanovo` ему не была названа; сигнал был законный.

**Находка 2 — «рёбра отсутствуют в закоммиченном HEAD, восстановление только в рабочем дереве».
УСТАРЕЛА на один коммит, и это моя недоработка задания.** Верификатор стартовал, когда HEAD был
`6d99278`; коммит `dcd9017` с восстановлением лёг позже. Проверено:

| ревизия | `dokazatelstvo_opiraetsya_na` | `primer_dlya` |
|---|---|---|
| `6d99278` (что он читал) | 10 | 10 |
| `dcd9017` (мой коммит) | **23** | **23** |
| `HEAD` сейчас | **23** | **23** |

⚠ **Но находка по существу верна и стоит того, чтобы остаться в отчёте:** между слиянием и моим
коммитом состояние репозитория действительно было таким, что `git checkout` по этим путям вернул
бы потерю. Верификатор нашёл настоящее окно уязвимости — просто оно уже было закрыто.

**Вердикт: потерь при слиянии не найдено, проверено 4 из 4 веток** (`polya-i-uzor`,
`mogila/zahod/pravila-kadra`, `rebra-blokov` — по содержанию, `sluzhebnye-slajdy`).

**КОММИТ:** `dcd9017` — влитие трёх веток, восстановление рёбер, визитка, правило «хвост Cowork» ·
`04e976d` — Э6: секция гигиены, гейт Г12, ловушки 33–36б, канон · плюс контурные `2366ee5`,
`7e2c780`, `37db001` и три слияния `a7ebca6`, `07e1b9d`, `6d99278`.

⚠ **Оба моих коммита прошли с `--no-verify`, причина одна и она ЧУЖАЯ:** фикстура
`_generator/tools/fixtures/git_zona/PROGNAT.sh` красная — её собственный вызов
`bootstrap_zahod.py` не передаёт ставшие обязательными `--kanal`/`--intervyu`. Доказано, а не
объявлено: та же фикстура красна (rc=1, те же три ❌) при прогоне против **нетронутой** версии
`bootstrap_zahod.py` из `HEAD`. Долг чужой, назван здесь и в сообщениях коммитов.

⚠ **Свой промах в технике обхода:** первый раз я обошёл хук голым `git commit --no-verify`,
а не `git_zona.py commit --no-verify "<причина>"`. Причина при этом уехала в сообщение коммита,
но НЕ в автолог `INCIDENTY.md` — а именно автолог читает следующий. Дописал строкой вручную.

**Клаузы критерия — все пять плюс шестая:**

| клауза | команда | ответ |
|---|---|---|
| 1. четыре ветки влиты | `git branch --no-merged arka/mat-kostyak \| grep -cE '<четвёрка>'` | `0` — влито 4 из 4 (три слиянием, `rebra-blokov` раньше) |
| 2. маркеров конфликта нет | `grep -rn "^<<<<<<<" _generator/ _studio/ teorkat-vvedenie/` | пусто, rc=1 |
| 3. числа Э2 | таблица выше | 8 из 10 сошлись; 2 красных — чужие долги, оба доказаны |
| 4. дека собрана с подбором | `ls -l …/dist/index.html` | 3 832 965 Б, 16:32, свежее всех источников (`find -newer` → пусто) |
| 5. фото на визитке целиком | кадр `vizitka-KANON-POSLE.png` | видно целиком |
| 6. правило в оба дома + причина устранена | вставки показаны выше | генератор + канон + ловушка на ПОРЯДОК |

**Гигиена §4.1:** Г3 — невлитых было 9, стало **6** (не прибавилось, убыло на три влитые);
Г6 — `git show --stat` по обоим коммитам содержит только мои пути, чужого нет; Г4 — новых `.py`
не заводил; Г5 — новых `.md` не заводил (мой файл-заход рождён зарегистрированным);
Г2 — неприменимо, все пути зоны внутри `materials`.

---

## ОТЧЁТ О ДОЧИСТКЕ — (приёмка 2026-08-15)

**Диагноз приёмки подтверждён полностью: ничего не потеряно, вливать было нечего.** Причина
ровно та, что названа — override в `brief.md` подменял визитку растром старого слайда, и моя
вёрстка к L2 не применялась вовсе.

### Д1. Override снят, визитка встала

Из `teorkat-vvedenie/L2/brief.md` убрана одна строка — `vizitka_illustracii: vizitka-celikom`.
**Иллюстрация и её заказ НЕ тронуты**, как и велено: `teorkat-vvedenie/L2/illustracii/vizitka-celikom/`
содержит `risunok.svg` и `zakaz.md` — просто их больше никто не зовёт.

**Проверено КАДРОМ из собранной деки, а не структурой:**
`_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/L2_sluzhebnyj_vizitka-POSLE-snyatiya-override.png`
— фотография видна целиком (не обрезана), QR нет, био читается, слайд не пуст.

Вторая проверка, фактом по ВЫХОДУ, а не по исходнику:
`grep -c 'vizitka-celikom' teorkat-vvedenie/L2/dist/index.html` → **0**. Растр старой визитки
в деку не попал.

### Д2. У обложки и финального override ЕСТЬ — но механизм другой, и геометрия к ним применяется

Греп нашёл **три** поля, а не одно:

| строка `brief.md` | поле | что делает |
|---|---|---|
| 32 | `oblozhka_illustracii: oblozhka-diagramma` | иллюстрация **в слот** штатной раскладки |
| 34 | `finalnyj_illustracii: oblozhka-diagramma` | то же |
| 35 | `vizitka_illustracii: vizitka-celikom` | **подменял СЛАЙД ЦЕЛИКОМ** — снят |

**Разница не косметическая, и она проверена по коду и по заказам, а не предположена.**
`deck.py:376-382`, кортеж `SLUZHEBNYE`: у `oblozhka` и `finalnyj` поле иллюстраций — штатный
слот раскладки, у `vizitka` в этом кортеже стоит `None, None`, а её override обрабатывается
отдельно (`deck.py:451-455` → `tipy.vizitka`: *«вся визитка — одна иллюстрация во весь слайд…
канон `sluzhebnye/` не рисуется»*). Заказы говорят то же: `oblozhka-diagramma` — *«размер:
правая панель обложки»*, `vizitka-celikom` — *«весь слайд, full-bleed, 1440×810»*.

⇒ **Ответ на вопрос Д2: нет, к обложке и финальному новая геометрия применяется.**
Подтверждено кадрами, а не рассуждением:
`kadry/L2_sluzhebnyj_oblozhka-POSLE.png` и `kadry/L2_sluzhebnyj_finalnyj-POSLE.png` — оба
нарисованы генератором (текстовая зона слева, зелёная панель справа), диаграмма лежит внутри
слота. Растром подменена была только визитка. Трогать эти два поля не стал: они работают
как задумано.

### Д3. Дека пересобрана, сумма сцен не упала

`python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html --zanovo`
→ `собран дек: слайдов в деке 26`; визитка прошла через солвер (`kegl=41.0`).

| проверка | ответ |
|---|---|
| `kadry.py … --scen-ne-menshe 55` | `СЦЕНЫ ПО ДЕКЕ: сумма 55, слайдов 26`, rc=0 |
| рёбра не пострадали | `23` |
| растр старой визитки в деке | `0` |

**АРТЕФАКТ (дочистка):** `/Users/ivanyakovlev/Documents/GitHub/materials/teorkat-vvedenie/L2/dist/index.html`

**НЕОБРАТИМОЕ дочистки:** удалена одна строка `vizitka_illustracii: vizitka-celikom` из
`teorkat-vvedenie/L2/brief.md` · возвращается дописыванием той же строки; сама иллюстрация и
заказ на месте, ничего не удалялось.
