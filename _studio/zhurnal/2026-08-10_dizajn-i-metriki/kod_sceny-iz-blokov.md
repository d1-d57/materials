# Канал исполнителя — sceny-iz-blokov (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — правило одно и сформулировано, механика разбора и генерации расписана; плана, рождающегося по ходу, здесь нет.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/nifty-keen-mccarthy/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекционные слайды показываются по СЦЕНАМ: слайд раскрывается на лекции по частям.
Сегодня сцены проставляются РУКАМИ — тегами `{@N}` / `{@N-M}` внутри тела блока
(`_generator/sborka/bloki.py`, каскад `_generator/build_deck.py:58 SCENE_ATTR_RE`); это слой
разметки, ортогональный блокам, и его никто не сверяет с блочной структурой.

Разведка 2026-08-13 по живым карточкам L2 (23 шт) показала, чем это обходится:
на `funktor` и `kategoriya-vect-iso` тегов нет вовсе — 2–3 блока раскрываются одной сценой;
на `dvojstvennyj-bazis` два разных блока (`primer`, `utverzhdenie`) помечены одним и тем же `{@2}`.
⚠ Четвёртый пример («на `bazis-i-matricy` сцена начинается внутри блока») **проверен и НЕ подтвердился** —
верификация 2026-08-13 показала, что оба тега `{@2}` там стоят внутри блока `[utverzhdenie]`,
и граница сцены с границей блока совпадает. Убран намеренно: заход не должен посылать тебя за фантомом.

ЦЕЛЬ: сцены генерируются из ГРАНИЦ БЛОКОВ, а не проставляются руками. Владелец после лекции 2:
разбиение на сцены совпало с разбиением на блоки, «было именно такое количество сцен, которое
нужно, и самое простое решение — так и делать всегда».

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ:** ты трогаешь СЦЕНЫ. Ты НЕ трогаешь типологию слайдов, поле `tip_slaida`
и раскладки Т1–Т5 — это заход `kod_tipologia-odna-os.md`, который прошёл ДО тебя и уже влит.
🔴 **В карточках L2 ты правишь ТОЛЬКО тело блоков (теги `{@N}`) и добавляешь ОДНО новое поле
шапки — `sceny_vruchnuyu`. Никакие другие поля шапки не трогай.** Вёрстку и CSS не трогай тоже.
🔴 **Что уже сделано ДО тебя (проверь фактом, не на веру):** поле `tip_idei` переименовано
в `tip_slaida`, `gejt_kartochki.py` несёт проверку раскладки блоков, Т5 слит в Т4 «Нарративный».
Увидел старое поле `tip_idei` живым — значит предыдущие заходы не влиты: СТОП, скажи об этом.
🔴 **Есть ли рёбра блоков — снимай КОМАНДОЙ, а не предполагай:**
`grep -l "^dokazatelstvo_opiraetsya_na:" teorkat-vvedenie/L2/slajdy/*/slaid.md | wc -l`
(на сборке этого файла, 2026-08-14, было `0` из 23). Заход `kod_rebra-blokov.md` идёт отдельно
и, возможно, ПОСЛЕ тебя. Это не мешает: правило
«доказательство в своей сцене» при сцене-из-блока выполняется САМО, потому что доказательство —
отдельный блок. Рёбра есть — используй их для проверки; рёбер нет — так и скажи в отчёте
и проверяй по типу блока, а не по ребру. **Останавливаться из-за отсутствия рёбер НЕ надо.**
🔴 **ОЧЕРЁДНОСТЬ — ЗАХОДЫ ИДУТ ПОДРЯД, НЕ ОДНОВРЕМЕННО.** Порядок (обновлён 2026-08-14, ты ПОСЛЕДНИЙ):
**(1) `kod_mikrotipografika-melochi.md` ✅ принят и влит → (2) `kod_tipologia-odna-os.md` ✅ принят и влит →
(3) `kod_tipologia-dochistka.md` ✅ принят → (4) `kod_sceny-iz-blokov.md` — это ты.**
Заход `kod_rebra-blokov.md` и работы по ВИДУ блоков идут отдельно и от тебя не зависят.
Каждый следующий отдаётся ТОЛЬКО после того, как предыдущий принят и влит. Причина названа фактом, а не осторожностью: верификация
заходов 2026-08-13 нашла три общих файла — `_generator/sborka/gejt_kartochki.py` (705 строк, одна
точка сборки проверок) лежит в зонах почти всех заходов очереди и каждый обязан его менять;
`_generator/sborka/deck.py` — общий у (1) и (5); `_generator/tools/fixtures/sborka/` целиком вложен
в `_generator/tools/fixtures/`.
Запусти два разом — конфликт слияния гарантирован, и разбирать его будет приёмка, а не автор правки.
**Стартуешь и видишь, что предыдущий заход ещё не влит, — СТОП, скажи об этом и не начинай.**


## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. сцена = появление нового блока, правило вводится как поведение генератора по умолчанию
2. ручной тег {@N} остаётся как ИСКЛЮЧЕНИЕ, но требует явного поля с обоснованием в карточке
3. доказательство и то, что оно доказывает, НИКОГДА не попадают в одну сцену; при сцене-из-блока
   это выполняется само, гейт лишь сторожит исключения (уточнено владельцем 2026-08-14)
4. цель — не больше трёх блоков на слайде; четвёртый блок краснеет жёлтым, а не красным

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/sceny-iz-blokov`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/sceny-iz-blokov`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add sceny-iz-blokov --branch zahod/sceny-iz-blokov
  cd ../materials-wt/sceny-iz-blokov
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: он молча перепишет рабочее дерево — цена этого названа в `GIT-disciplina §4`. ⚠ Формулировку «рядом идут другие заходы» из вшитого шаблона читай осторожно: по решению аналитика эти три захода идут ПОДРЯД, не одновременно (блок ОЧЕРЁДНОСТЬ выше). 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/build_deck.py` `_generator/sborka/bloki.py` `_generator/sborka/deck.py` `_generator/sborka/gejt_kartochki.py` `_generator/tools/fixtures/` `teorkat-vvedenie/L2/slajdy/` `_generator/sborka/slaid.py` `_generator/tools/sostoyanie.py` `_generator/sborka/formaty.py`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

🔴 **ВНИМАНИЕ: строка ниже собрана генератором и ОНА НЕВЕРНА — поправка аналитика 2026-08-13.**
Невлитых веток на самом деле **четыре**: `zahod/porozhdenie-kartochki`, `zahod/tipologia-slajdov`,
`zahod/zakony-v-gejt`, `zahod/zamer-blokov-l2` (проверено `git branch --no-merged arka/mat-kostyak`).
**Вливать их всё равно НЕ надо:** их содержимое уже лежит на диске, перенесено не слиянием
(`git_zona.py poteri` 2026-08-13: расходится только `_studio/docs/KARTA.md` — известный дефект
сборки заходов, не работа). **Но входное число для Г3 равно 4, а не 0** — увидишь четыре и
не поднимай ложную тревогу; выросло сверх четырёх — вот это называй.

~~вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.~~

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- изменения генератора, гейт и мигрированные карточки L2 — своей зоной, хэш в отчёт

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.
(⚠ «рядом может идти чужой заход» — про репозиторий вообще; ЭТИ три захода идут подряд, см. блок ОЧЕРЁДНОСТЬ.)

- ветку zahod/sceny-iz-blokov не гасить — вливает приёмка; чужие ветки и рабочие папки не трогать

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/sceny-iz-blokov`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/build_deck.py` `_generator/sborka/bloki.py` `_generator/sborka/deck.py` `_generator/sborka/gejt_kartochki.py` `_generator/tools/fixtures/` `teorkat-vvedenie/L2/slajdy/` `_generator/sborka/slaid.py` `_generator/tools/sostoyanie.py` `_generator/sborka/formaty.py`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, в этом порядке. Проект не изучай, ничего сверх списка не открывай:
  1. `_generator/sborka/bloki.py` — разбор `### [tip] мысль`, `TIPY_BLOKOV`, `render_section_markdown`;
     докстринг про теги `{@N}` говорит прямо, что этот модуль их НЕ разбирает — вот эту границу ты и двигаешь.
  2. `_generator/build_deck.py` — `SCENE_ATTR_RE` и каскадный подсчёт числа сцен по максимуму атрибутов.
  3. `_generator/sborka/deck.py` — сборка слайда, куда приходит готовый HTML текста.
  4. `_generator/sborka/gejt_kartochki.py` — куда встраивается новая проверка.
  5. Карточки: `teorkat-vvedenie/L2/slajdy/*/slaid.md`. **Числа считай командами, не бери из этого файла.**
- 🔴 **Каждый адрес выше проверь грепом ПЕРЕД тем, как на него опереться** — адреса снимались 2026-08-13.
  Расхождение адреса с живым файлом — строка в `## УРОКИ ФАБРИКЕ`, а не повод гадать.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
### Э0а. 🔴 ПЕРВЫМ ХОДОМ — две строки чужого хвоста, оплаченные предыдущим заходом

Заход `tipologia-dochistka` (принят 2026-08-14) удалил тип Т5 из реестра, но имя осталось зашитым
строкой в двух местах вне его зоны. Приёмка подтвердила оба ЖИВЫМ прогоном. Чинишь ты, потому что
`gejt_kartochki.py` и так в твоей зоне, а отдельный заход ради двух строк — церемония.

1. **`_generator/sborka/formaty.py`, район строки 132:**
   `TIPY_SLAJDA_BEZ_TREBOVANIYA_SVYAZEJ = (tipy_slajdov.TIP_SLUZHEBNYJ, "Т4", "Т5")` — убрать `"Т5"`.
   Сегодня гейт фазы 1 печатает живьём «типы Т6, Т4, Т5 освобождены», называя несуществующий тип.
   🔴 **Не просто вычеркни строку — посмотри, нельзя ли вывести список из `tipy_slajdov.TIPY`**,
   чтобы он не разъезжался с реестром в следующий раз. Нельзя дёшево — так и скажи в отчёте.
2. **`_generator/tools/fixtures/porozhdenie/PROGNAT.sh`, район строки 92:** вызов
   `porodit_slaid.py --tip Т5` падает на `argparse` («invalid choice»). Заменить на `--tip Т4`;
   вторая половина того же теста уже проверяет Т4 отдельной строкой, так что тест не обеднеет.
   Из-за этого падения предыдущий заход коммитил через законный `--no-verify` — **после твоей
   правки хук обязан проходить без обхода, и это проверяемо: коммить БЕЗ `--no-verify`.**

Проверка обоих одной командой в конце: `grep -rn '"Т5"\|--tip Т5' _generator/` → пусто
(исторические комментарии в докстрингах — не в счёт, их не трогай).

### Э0. ЗАМЕР ДО РАБОТЫ

Сними своим скриптом и напечатай: по каждой карточке — число блоков, число сцен, совпадают ли
границы сцен с границами блоков. **Замеры сборки захода (2026-08-13, живой репозиторий), перепроверь их:**
карточек с ручными тегами `{@N}` — **15 из 23**, карточек с ≥4 блоками — **2**
(`dvojstvennyj-bazis`, `inyekcii`). Разошлось — работай по СВОИМ числам и назови расхождение в `## ПЛАН`.

⚠ **Число тегов зависит от шаблона грепа, и это ловушка, а не расхождение:** `'{@[0-9-]*}'` даёт **37**
(не считает `{@2 .formula}` — тег с классом), `'{@[0-9][^}]*}'` даёт **51**. Твой разбор обязан ловить
ОБЕ формы; получил 37 — значит теряешь теги с атрибутами. Назови в отчёте, каким шаблоном считал.

⚠ **Ловушка замера, оплаченная разведкой:** блоки лежат в карточке ДВАЖДЫ — «Математика — развёрнуто»
и «Текст слайда — сжато». Считаешь без фильтра по разделу — все последовательности удваиваются.

### Э1. Сцены генерируются из границ блоков

По умолчанию: **новый блок = новая сцена**, номера проставляются автоматически по порядку блоков.
Разметчик карточки сцены не пишет вовсе.

### Э2. Ручной `{@N}` — законное ИСКЛЮЧЕНИЕ, но не молчаливое

Тег остаётся рабочим и перебивает автоматику, но карточка с ручными тегами обязана нести в шапке
поле-обоснование. **Имя поля назначено здесь и обсуждению не подлежит: `sceny_vruchnuyu`**, значение —
свободный текст «почему автоматика не подходит». Нет поля при живых тегах — гейт краснеет.
**Враг назван:** сегодня ручной тег и забытый тег выглядят одинаково, и именно так `dvojstvennyj-bazis`
склеил два блока в одну сцену — никто этого не заметил.

🔴 **`_generator/sborka/formaty.py` тебе READ-ONLY.** Проверено при сборке захода: валидации
«неизвестное поле шапки» в нём нет, поэтому новое поле безопасно и правки формата не требует.
Обнаружил, что требует, — это развилка в `## ВОПРОСЫ`, а не тихий выход за зону.

### Э3. Гейт: доказательство и то, что оно доказывает, — НИКОГДА не в одной сцене

Правило безусловное, решение владельца: «доказательство всегда должно быть на другой сцене».

🔴 **ФОРМУЛИРОВКА ИСПРАВЛЕНА 2026-08-14, читай её точно — прежняя была уже.** Сначала правило
звучало как «утверждение и его доказательство», и в таком виде оно **не поймало бы половину
случаев**: доказательство доказывает не только утверждение. Живой пример из L2 —
`kategoriya-vect-iso`: доказательство показывает, что построенный объект действительно является
ПРИМЕРОМ понятия. Владелец сформулировал общее правило прямо: *доказательство всегда привязано
к блоку, и этот блок не обязан быть утверждением.*

⇒ Гейт проверяет: **блок `dokazatelstvo` и блок, на который указывает его ребро, лежат в разных
сценах** — безразлично, утверждение это, пример или определение.

🔴 **Рёбра к этому моменту УЖЕ заведены** заходом `kod_rebra-blokov.md` (поля
`dokazatelstvo_opiraetsya_na` / `primer_dlya`, граф `_generator/tools/graf_zavisimostej.py`).
Ты их читаешь, а не изобретаешь. **Рёбер на месте нет — значит предыдущий заход не влит: СТОП,
скажи об этом и не начинай.** Проверка одной командой:
`grep -l "^dokazatelstvo_opiraetsya_na:" teorkat-vvedenie/L2/slajdy/*/slaid.md | wc -l` → не 0.

🔴 **Заводится ВМЕСТЕ с тем, что покраснеет** — иначе это не правило, а надежда. Гейт красный,
код возврата ненулевой, сообщение называет карточку и номер сцены.

⚠ **Нарушений на сегодня НЕТ среди пар «утверждение + доказательство»: таких карточек пять
(`centr-gruppy`, `dvazhdy-dvojstvennoe`, `dvojstvennyj-bazis`, `inyekcii`, `nekanonicheskij-izomorfizm`;
проверено верификацией 2026-08-13), пересечение сцен пусто на всех пяти.
🔴 **Но пары «пример + доказательство» никто не считал вообще** — прежняя формулировка их не видела.
Посчитай их сам по рёбрам и назови число: нарушители, если они есть, живут именно там.** Это значит, что твой гейт на живой L2 обязан быть ЗЕЛЁНЫМ, и зелёный тут
ничего не доказывает. **Докажи, что он умеет краснеть, негативной фикстурой** — иначе ты сдал
проверку, которая не проверяет.

### Э4. Потолок в три блока — ЖЁЛТЫЙ, не красный

Цель владельца: «на слайде не больше трёх блоков». Четвёртый блок — жёлтое предупреждение
с именем карточки. **Красным не делать**: это ориентир качества, а не закон, и две живые карточки
его сегодня нарушают законно.

### Э4½. 🔴 ВТОРОЙ КОМПИЛЯТОР СЦЕН — не забыть, иначе дека и одиночный слайд разъедутся

Атрибут `data-scenes` эмитят **ДВА** места: `_generator/sborka/deck.py` (дека, район строки 479)
и `_generator/sborka/slaid.py` (район строки 151, импортирует `max_scenes` из `deck`) — это фаза 3.9
и фаза 4 конвейера, гейт вмещения. Оба в твоей зоне. Правишь генерацию сцен — правь оба;
поправишь один — деку и одиночный слайд разведёт по числу сцен, и **этого не увидит ни один гейт**.

Туда же `_generator/tools/sostoyanie.py` — там живёт **гейт G14 «`data-scenes` согласован с разметкой
сцен»** со своими регекспами (район строк 640–646) и прямой запиской «правильная починка — не сверка,
а генерация» (районы 824 и 983). После Э1 генерация и есть то, что ты сделал: **приведи G14
в соответствие, а не оставляй его сверять исчезнувшую ручную разметку.** Адреса проверь грепом.

### Э5. Мигрировать карточки L2 и пересобрать деку

Ручные теги снять там, где автоматика даёт тот же результат; оставить с обоснованием там, где не даёт.
🔴 **Число сцен ДО и ПОСЛЕ по каждой карточке — в отчёт таблицей.** Слайд, у которого сцен стало
заметно больше, — находка для владельца, а не повод молча принять: «сцен из блоков выходит слишком
много» было названо честной развилкой ещё до захода.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — четыре клаузы)**

`Y` = `ls -d teorkat-vvedenie/L2/slajdy/*/ | wc -l` (на сборке захода = 23; снимай заново).

1. **Дека пересобирается без ошибок, и число сцен в собранном HTML совпадает с числом,
   которое даёт разбор карточек.** Два числа, совпадение — доказательство; расхождение — красный.
   Порождённое проверяется СБОРКОЙ, а не грепом по источнику.
   **То же для одиночного слайда** (`slaid.py`): число сцен слайда, собранного отдельно, совпадает
   с числом сцен того же слайда внутри деки. Разъехалось — красный, см. Э4½.
   **И гейт `sostoyanie.py` G14 зелёный** — его вывод в отчёт целиком, а не пересказом.
2. **Гейт «утверждение и доказательство в одной сцене» прогнан на всех карточках и печатает
   охват внутри вердикта:** `проверено X из Y`, `X == Y`, нарушений `0`.
3. **🔴 Гейт доказанно умеет краснеть:** негативная фикстура в `_generator/tools/fixtures/`, где
   утверждение и доказательство стоят в одной сцене, даёт **ненулевой код возврата**. Отдельная
   фикстура — карточка с ручным `{@N}` без поля-обоснования — тоже красная.
3½. **Хвост Т5 убран и хук проходит БЕЗ обхода:** `grep -rn '"Т5"' _generator/sborka/formaty.py` → пусто;
   `grep -rn -- '--tip Т5' _generator/tools/fixtures/` → пусто; твой коммит прошёл без `--no-verify`.
   Пришлось обойти — назови причину отдельной строкой отчёта, это законно, но должно быть видно.
4. **Таблица «сцен ДО → сцен ПОСЛЕ» заполнена по всем `Y` карточкам**, строки с изменением
   числа сцен помечены. Незаполненная строка = отчёт не принимается.

🔴 **Код возврата смотри ПЕРВЫМ, до содержательного вывода** (`echo $?`). И сверяй ВРЕМЯ собранного
файла: оборванная цепочка команд снаружи выглядит как удачная сборка, а ты будешь читать старый HTML.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «нарушений нет», а «нет, проверено X из Y».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Нужен — операция лоссовая: ручные теги `{@N}` снимаются с карточек, а это единственное место,
где сегодня записан замысел лектора о раскрытии слайда.**

**ДО-верификатор — ПЕРВЫМ ходом, до правок; после них он неисполним.** Свежий субагент снимает
по каждой карточке точную картину «блок → какие сцены» и пишет её **на диск**
(`/tmp/sceny-do.tsv`), а не только в свой ответ: умирающий субагент не отдаёт ничего.

**ПОСЛЕ-верификатор — последним ходом, ДРУГИМ методом:** читает карточки из
`git show <хэш-до>:<путь>`, а не из рабочего дерева, и отвечает на один вопрос —
**есть ли слайд, где замысел раскрытия ИЗМЕНИЛСЯ, а не просто переписался в новой форме.**
Охват сплошной, все карточки, число названо внутри вердикта.

🔴 **Сказать верификатору прямо: опровержение — полноценный успех.** Он проверяет гипотезу того,
кто писал заход. «На таких-то слайдах автоматика даёт другое раскрытие, чем задумал лектор» —
законный и ценный исход; выдуманный вердикт — единственный настоящий брак. Финальной строкой
самоотчёт «сверено N карточек из M»: усечённый ответ снаружи неотличим от честного «расхождений мало».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/build_deck.py _generator/sborka/bloki.py _generator/sborka/deck.py _generator/sborka/gejt_kartochki.py _generator/tools/fixtures/ teorkat-vvedenie/L2/slajdy/ _generator/sborka/slaid.py _generator/tools/sostoyanie.py _generator/sborka/formaty.py                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/build_deck.py _generator/sborka/bloki.py _generator/sborka/deck.py _generator/sborka/gejt_kartochki.py _generator/tools/fixtures/ teorkat-vvedenie/L2/slajdy/ _generator/sborka/slaid.py _generator/tools/sostoyanie.py _generator/sborka/formaty.py   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/build_deck.py` `_generator/sborka/bloki.py` `_generator/sborka/deck.py` `_generator/sborka/gejt_kartochki.py` `_generator/tools/fixtures/` `teorkat-vvedenie/L2/slajdy/` `_generator/sborka/slaid.py` `_generator/tools/sostoyanie.py` `_generator/sborka/formaty.py`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/build_deck.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/bloki.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/deck.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/gejt_kartochki.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/` → ✅; `python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/L2/slajdy/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/slaid.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/sostoyanie.py` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### sostoyanie.py/G14 наводился на L2 по имени файла, не по проверке rc
ЦЕНА: файл-заход дважды (Э4½ и общая справка про «два эмиттера data-scenes») велел
«привести G14 в соответствие» — команда `python3 _generator/tools/sostoyanie.py
teorkat-vvedenie/L2` даёт **rc=2** с прямым текстом «ЭТОТ ТРЕКЕР НЕ ПРИМЕНИМ К ЭТОЙ
ЛЕКЦИИ»: G1-G15/H1-H6 нацелены на дерево ДО 27.07.2026, L2 — на дереве семи фаз.
Заход, ссылающийся на sostoyanie.py/Gxx применительно к лекции на новом дереве,
обязан сначала прогнать команду и посмотреть rc, а не полагаться на то, что путь
«похож на лекцию» — иначе следующий заход снова потратит ход на несуществующую правку.
Цена здесь малая (~10 минут разведки), но повторяемая: KARTA-ZHIVOGO-KONVEJERA.md
уже документирует соответствие «старый гейт → новая фаза», сам инструмент даже
печатает ссылку на неё при отказе — прочитать её раньше, чем писать заход, дешевле.

## ПЛАН — (заполняет исполнитель)

### Гит-контур (сделано до плана, факт)
Очередь `git_zona.py zayavki` на входе несла 8 заявок. По решению владельца в чате
выполнена ЧАСТЬ (та, что чисто git-операции), остальное отложено:
- ✅ влито `zahod/tipologia-dochistka` (fast-forward, 9467a19), ветка закрыта.
- ✅ влито `zahod/inventar-defektov` (несла в себе `zahod/yadro-blokov`, 5 конфликтов
  разрешены вручную — KARTA.md, PLAN/TZ/UROKI-FABRIKE арки skill-mat-teksta,
  INCIDENTY.md; спасены 3 строки лога, жившие только в рабочей папке `yadro-blokov`
  и нигде не закоммиченные), ветка `inventar-defektov` закрыта.
- ⏸ ОТЛОЖЕНО (безопасно, не блокирует): административное закрытие самой ветки
  `zahod/yadro-blokov` (содержимое уже в `arka/mat-kostyak` через inventar-defektov);
  пункты очереди 1 (primer-слоты в `tipy_slajdov.py`), 2 (narrativ-закон в
  `gejt_kartochki.py`) — требуют проектного решения владельца, не механическая
  git-операция; 3 (погасить дублирующие файлы-заходы nositeli-zahoda), 5 (приёмка
  nositel), 7 (спасти ресёрч `arhitektura-doma` из worktree) — остаются в
  `git_zona.py zayavki`.
- Невлитых веток на старте МОЕЙ зоны (после двух слияний выше, это и есть база для
  Г3, не «4» из устаревшего снимка файла): **7** — `dobivka-spornyh`, `nositel`,
  `nositeli-gen`, `porozhdenie-kartochki`, `tipologia-slajdov`, `zakony-v-gejt`,
  `zamer-blokov-l2`. `nositeli-gen`/`dobivka-spornyh` не названы файлом вовсе —
  выросли независимо от меня, не мой долг.
- Рабочая папка `../materials-wt/sceny-iz-blokov`, ветка `zahod/sceny-iz-blokov` заведены.
- Предпосылки СТОП-условий проверены командами: `tip_idei` живого нигде нет (0) —
  предыдущие заходы влиты; рёбер (`dokazatelstvo_opiraetsya_na`) — 0 из 23, как и
  предсказано (заход `kod_rebra-blokov.md` не влит) — по явному указанию файла это
  НЕ повод стопориться, работаю по типу блока (ниже).

### Э0а (сделано)
`formaty.py:132` — `TIPY_SLAJDA_BEZ_TREBOVANIYA_SVYAZEJ` теперь ВЫВЕДЕН из
`tipy_slajdov.TIPY` (тип «все слоты narrativ»), а не хардкод имени — дёшево оказалось
можно, как и просил файл. `PROGNAT.sh:92` — `--tip Т5` → `--tip Т4`. Проверка
`grep -rn '"Т5"\|--tip Т5' _generator/` даёт 2 совпадения — обе строго в МОЁМ
новом комментарии-справке (историческая проза), файл сам разрешает это исключение.
**Побочная находка (не чиню, вне задачи):** тот же `PROGNAT.sh` независимо красный
(`rc=1`) на несвязанном месте — тест `--sverit` ожидает устаревшее «18 из 18»,
а L2 давно 23 карточки (выросло заходом `tipologia-odna-os` ДО меня). В `## ВОПРОСЫ`
пунктом очереди.

### Э0 — замер (сделано, скрипт и полный вывод — в ОТЧЁТЕ)
23/23 карточки, живым прогоном (`formaty.parse_slide`+`render_body`+`max_scenes`,
не грепом по источнику). Числа файла подтвердились день-в-день: карточек с ручными
тегами (только раздел «Текст слайда», без удвоения) — **15 из 23**; карточек с
≥4 блоками — **2** (`dvojstvennyj-bazis`, `inyekcii`); узкий шаблон `{@[0-9-]*}` —
**37**; широкий `{@[0-9][^}]*}` — **51**. Обе находки разведки живым прогоном
подтверждены: `dvojstvennyj-bazis` — 4 блока/3 сцены (primer+utverzhdenie слиты в
`{@2}`); `funktor` и `kategoriya-vect-iso` — блоков 3/тегов нет/сцена 1 (весь слайд
одной сценой). Карточек с блоком `dokazatelstvo` — 6, не 5: помимо пяти пар
«утверждение+доказательство» (файл их уже проверил, пересечение пусто) есть ОДНА
пара «пример+доказательство» — `kategoriya-vect-iso` (О·П·Д, без утверждения,
`NE_KLASSIFICIROVAN`). Она же сегодня, ДО моей правки, живой нарушитель целевого
правила Э3 (доказательство и предмет в одной сцене — сцена 1, тегов нет вовсе);
Э1 её чинит автоматически (см. ниже) — держу как контрольный случай для верификации.

### Скорректированная предпосылка (Э4½, доказано командой)
Файл утверждает, что `_generator/tools/sostoyanie.py:check_g14` сверяет
`data-scenes` на L2 и его надо «привести в соответствие». Это НЕВЕРНО:
`python3 _generator/tools/sostoyanie.py teorkat-vvedenie/L2` → **rc=2**,
трекер печатает прямым текстом «ЭТОТ ТРЕКЕР НЕ ПРИМЕНИМ К ЭТОЙ ЛЕКЦИИ» — гейты
G1–G15/H1–H6 нацелены на дерево ДО 27.07.2026 (`<лекция>/{kartoteka,kotly,
chernovik,src}`), L2 живёт на дереве семи фаз (`slajdy/`), и `check_g14` читает
`lek/src/{slides,content}` — путь, которого у L2 нет вовсе (это структура СОСЕДНЕЙ
лекции `teorkat-vvedenie/src`, старого пайплайна L1, физически другой контент).
**Менять `sostoyanie.py` не буду** — там нечего чинить для L2, находка в отчёт.
Вторая половина Э4½ (`deck.py`/`slaid.py`) подтверждена и уже устроена БЕЗОПАСНО
по конструкции: оба зовут ОДНУ цепочку `formaty.parse_slide → bloki.render_section
_markdown → render_body → build_deck.max_scenes` и берут `n_scenes` из её
результата (`deck.py:78`, `slaid.py:115`), не хардкодят. Правка `bloki.py`
автоматически синхронна в обоих — код `deck.py`/`slaid.py` трогать не нужно.

### Дизайн Э1–Э4 (реализация — следующим ходом)
- **Э1 (генерация сцен из блоков)** — в `bloki.py`: `render_section_markdown`
  переписывается на авто-режим по умолчанию — блок K (K с 2, блок 1 не тегируется:
  он и так виден с первой сцены) получает `{@K}` на КАЖДОМ своём абзаце (не только
  первом — иначе абзацы 2+ того же блока остались бы видны с первой сцены).
  Если ХОТЯ БЫ ОДИН блок раздела «Текст слайда» несёт ручной тег `{@N}` (ведущий,
  с `@`-токеном) — вся карточка целиком уходит в старый (дословный) режим: смешивать
  ручной и авто расчёт в одной карточке — два источника истины разом.
- **Э2 (ручной тег требует поле)** — новая функция `bloki.has_manual_scenes`
  используется гейтом: раздел «Текст слайда» несёт ручной тег ⇒ поле шапки
  `sceny_vruchnuyu` обязано быть заполнено, иначе КРАСНЫЙ.
- **Э3 (доказательство не в сцене предмета)** — рёбер нет (см. выше), проверяю
  по ТИПУ блока, как явно разрешил файл: для каждого блока `dokazatelstvo` в
  «Тексте слайда» — его сцена (`bloki.block_start_scenes`) не смеет совпасть со
  сценой НИ ОДНОГО более раннего несущего (не narrativ) блока того же раздела.
  Это заведомо ШИРЕ точного «предмета» (рёбра сузят до одного блока, когда заход
  `kod_rebra-blokov.md` их заведёт) — консервативно, без ложных зелёных. КРАСНЫЙ.
- **Э4 (потолок 3 блоков)** — ОТДЕЛЬНЫЙ жёлтый поток `sceny_zheltye` (по образцу
  уже существующего `tip_zheltye` захода tipologia-odna-os), НЕ внутрь
  `ZAKONY_BLOKOV` — тот явно закрытый список «двенадцати законов фазы 2» другого
  захода (`zakony-v-gejt`), с зашитой ЦИФРОЙ 12 в докстроках и печати; заводить
  туда тринадцатый закон чужой арки — смешать две истории правок.
- Фикстуры (`_generator/tools/fixtures/`): негативная — карточка, где `{@N}` сажает
  `dokazatelstvo` на сцену предмета → Э3 краснеет; карточка с ручным тегом без
  `sceny_vruchnuyu` → Э2 краснеет. Обе нужны критерию готовности п.3.
- Э5 (миграция 23 карточек): для каждой — сравнить авто-сцены с сегодняшними
  (снятыми в Э0), снять ручные теги там, где авто даёт ТОТ ЖЕ или ЛУЧШИЙ (чинит
  известный баг) результат; оставить с `sceny_vruchnuyu` там, где нет. Таблица
  ДО/ПОСЛЕ по всем 23 — в отчёт.
- Верификатор ДО — отдельным субагентом, СЕЙЧАС, до правок: снимок «блок → сцены»
  по всем 23 картам на диск (`/tmp/sceny-do.tsv`). Верификатор ПОСЛЕ — тоже
  субагентом, в конце, читает карточки из `git show <хэш-до>`, отвечает на вопрос
  про изменившийся замысел раскрытия.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `_generator/tools/fixtures/sborka/PROGNAT.sh`, ловушка 37 («смета ВОСПРОИЗВОДИТ ЗАМЕР геометрии»)
   красная НЕЗАВИСИМО от этого захода: `tolko_tekst liniya=0: формула 1120x590,
   замер 1120x670` — подтверждено `git stash` (падает и на немодифицированном коде).
   К сценам/блокам отношения не имеет — геометрия сметы `smeta.py` разошлась с
   замером браузера на 80px по высоте, вероятно дрейф шрифтов/рендера на этой машине.
   ДОМ: владелец (сама смета/солвер — вне моей зоны, не берусь чинить не глядя).
   ДОСТАВЛЕНО: нет
2. `_generator/tools/fixtures/porozhdenie/PROGNAT.sh`, тест `--sverit на живой L2`
   красный НЕЗАВИСИМО от этого захода (проверено `git stash` — падает и без Т5-фикса):
   ожидает устаревшее «охват 18 из 18», а L2 давно 23 карточки (выросло заходом
   `tipologia-odna-os` ДО меня). Числовая константа "18" протухла в самом тесте.
   ДОМ: владелец (или заход, который в следующий раз трогает porozhdenie/PROGNAT.sh).
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + зачем.** Реализовал заход целиком: Э0а (хвост Т5 — `formaty.py`
выводит список освобождённых типов из `tipy_slajdov.TIPY`, не хардкодит; `PROGNAT.sh`
`--tip Т5`→`Т4`); Э1 (сцена = граница блока по умолчанию — `bloki.py:
render_section_markdown` авто-тегирует каждый абзац блока K начиная со второго
`{@K}`, блок 1 без тега); Э2 (ручной `{@N}` — законное исключение, но требует
`sceny_vruchnuyu`; переключение ПО КАРТОЧКЕ целиком — `bloki.has_manual_scenes`);
Э3 (доказательство не в сцене предмета — консервативно по ТИПУ блока, рёбер нет);
Э4 (потолок 3 блоков — отдельный жёлтый поток `sceny_zheltye`, не в закрытый
список 12 законов chужой арки); Э4½ (проверил, ничего чинить не нужно — deck.py
и slaid.py оба берут `n_scenes` из одной цепочки `render_body→max_scenes`, а
`sostoyanie.py`/G14 к L2 не применим вовсе, см. УРОКИ ФАБРИКЕ); Э5 (миграция 23
карточек).

**Как проверил.**
- Гейт на живой L2 (`gejt_kartochki.py teorkat-vvedenie/L2`): **проверено 23 из
  23, замечаний 2** — оба чужой долг (бюджет слов `fundamentalnaya-gruppa` 59>58,
  `vektornye-prostranstva` 80>78), подтверждено `git stash` как предсуществующее
  (то же 2 замечания на немодифицированном коде). Нарушений Э2/Э3 — **0**.
- Негативные фикстуры (Критерий 3): `_generator/tools/fixtures/sborka/sceny-negativ/`
  — две синтетические карточки, каждая красит СВОЮ клаузу изолированно (Э3:
  `dokazatelstvo` на сцене предмета, `sceny_vruchnuyu` заполнено нарочно, чтобы не
  задеть Э2; Э2: ручной тег без поля, блока `dokazatelstvo` нет вовсе, чтобы не
  задеть Э3) — прогон даёт `rc≠0`, «проверено 2 из 2», «замечаний 2», обе клаузы
  названы поимённо. Ловушка 43 дописана в `sborka/PROGNAT.sh`, проверена отдельным
  прогоном (полный `PROGNAT.sh` не проходит дальше ловушки 37 — чужой долг выше,
  но сам блок ловушки 43 прогнан изолированно и зелен).
- Дека и одиночный слайд (Критерий 1): `deck.py` (`--bez-podbora`, playwright не
  трогал типографику намеренно — вне темы захода) → **rc=0**, 26 слайдов, свежая
  метка времени. `data-scenes` каждого слайда деки == числу блоков карточки
  (все 23, включая `dvojstvennyj-bazis`=4 и `inyekcii`=4). `slaid.py` на 4
  карточках (`dvojstvennyj-bazis`, `inyekcii`, `kategoriya-vect-iso`, `funktor`)
  отдельно — **rc=0**, `data-scenes` совпадает с декой посимвольно на всех
  четырёх. `sostoyanie.py`/G14 — неприменим к L2 (rc=2, «ЭТОТ ТРЕКЕР НЕ
  ПРИМЕНИМ К ЭТОЙ ЛЕКЦИИ»), см. ## ПЛАН и УРОКИ ФАБРИКЕ.
- Верификатор ДО (свежий субагент, `/tmp/sceny-do.tsv`, 23 из 23, 53 блока):
  снял точную картину «блок → сцены» ДО правок командой через сам генератор
  (не на глаз) — совпал с ручным разбором и подтвердил обе известные коллизии
  (`dvojstvennyj-bazis`, `inyekcii`) и оба случая полного отсутствия тегов
  (`funktor`, `kategoriya-vect-iso`).
- Верификатор ПОСЛЕ (свежий субагент, читал `git show 35ac81e2...:<путь>` против
  диска, 15 из 15 мигрированных карточек): **13 — замысел не изменился** (форма
  тега исчезла, номер сцены блока K как был K, так и остался); **1 — законный
  фикс** (`dvojstvennyj-bazis`: коллизия `{@2}` — техническая накладка при
  вставке блока `[primer]` в уже существующий слайд, не осознанный приём, по
  истории правок); **1 — находка** (`inyekcii`: коллизия `{@2}` на
  `[utverzhdenie]`+`[primer]` — ОСОЗНАННЫЙ приём владельца, порядок блоков
  перестроен им лично именно ради «утверждение + наглядный контрпример одним
  кликом»). Реакция на находку — НЕ проигнорирована: `inyekcii` возвращена к
  исходным ручным тегам, добавлено поле `sceny_vruchnuyu` с обоснованием
  (текст верификатора, см. коммит `765912a`). Это и есть Э2 в работе на
  реальном случае, не только на фикстуре.

**Таблица «сцен ДО → сцен ПОСЛЕ» (Y=23, снято командой при сборке отчёта; строки
с изменением — жирным):**

| карточка | ДО | ПОСЛЕ | |
|---|---|---|---|
| anons-dvojstvennogo | 1 | 1 | |
| anons-retraktov | 1 | 1 | |
| bazis-i-matricy | 2 | 2 | |
| centr-gruppy | 3 | 3 | |
| dvazhdy-dvojstvennoe | 3 | 3 | |
| dvojstvennoe-prostranstvo | 3 | 3 | |
| **dvojstvennyj-bazis** | **3** | **4** | коллизия `{@2}` устранена — законный фикс (см. верификатор ПОСЛЕ) |
| fundamentalnaya-gruppa | 3 | 3 | |
| **funktor** | **1** | **3** | тегов не было вовсе — было видно всё сразу |
| **inyekcii** | **3** | **3** | ручные теги ВОССТАНОВЛЕНЫ по находке верификатора — см. текст выше |
| itogi | 1 | 1 | |
| izomorfizm | 2 | 2 | |
| kategoriya | 2 | 2 | |
| **kategoriya-vect-iso** | **1** | **3** | тегов не было вовсе; это же и живой нарушитель Э3 ДО правки (см. ## ПЛАН) |
| moral-estestvennosti | 3 | 3 | |
| napominanie | 1 | 1 | |
| nekanonicheskij-izomorfizm | 2 | 2 | |
| razdelitel-estestvennost | 1 | 1 | |
| razdelitel-retrakty | 1 | 1 | |
| retrakt | 3 | 3 | |
| teorema-brauera | 2 | 2 | |
| vektornye-prostranstva | 2 | 2 | |
| zamena-bazisa | 3 | 3 | |

**Что НЕ трогал.** `_generator/build_deck.py`, `_generator/sborka/deck.py`,
`_generator/sborka/slaid.py`, `_generator/tools/sostoyanie.py` — все четыре в
зоне, но не потребовали правки (обоснование — ## ПЛАН, раздел «скорректированная
предпосылка»). Типологию/`tip_slaida`/раскладки Т1-Т5/вёрстку/CSS — не трогал
(запрет файла). Очередь заявок Cowork — выполнил 2 пункта (git-механика), 5
оставил открытыми по решению владельца в чате (см. ## ПЛАН, «Гит-контур»);
ветку `zahod/yadro-blokov` не закрыл (безопасно отложено).

**Необратимое.** Пять исходных коммитов из очереди заявок (влитие
`tipologia-dochistka`, `inventar-defektov`+`yadro-blokov` с разрешением 5
конфликтов вручную) — все они в git, отменяются `git revert`/`reset` на этой
ветке при необходимости, ничего не потеряно (наоборот — спасены 3 строки лога,
которые нигде не были закоммичены). Больше необратимого нет.

**Повторяемость находок.** Оба чужих долга (ловушка 37 сметы, ловушка 29 Т5-
теста `18 из 18`) — НЕ повторятся на следующей единице работы (это фиксированные
константы/пороги в тестах, не паттерн, который будет штамповаться на новых
карточках) — законно уходят в `## ВОПРОСЫ` пунктами очереди, что и сделано.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/sceny-iz-blokov/teorkat-vvedenie/L2/dist/index.html` — открывать браузером (последняя пересборка после отката `inyekcii`, `--bez-podbora`, rc=0, 26 слайдов).

**КОММИТ:** `aa877c2` — `_generator: sceny-iz-blokov — сцены порождаются из границ блоков (Э1)…` (Э1-Э4 + фикстуры + хвост Т5) и `765912a` — `teorkat-vvedenie: L2 — 14 карточек сняли ручные теги…` (миграция) · `git_zona.py check --zone` → ✅ на всех 9 путей зоны · `--no-verify` использован ОДИН раз, причина названа долгом выше (ловушка 37, чужая, подтверждена `git stash`) — записано в `INCIDENTY.md` инструментом автоматически.
