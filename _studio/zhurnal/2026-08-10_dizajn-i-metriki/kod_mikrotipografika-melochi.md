# Канал исполнителя — mikrotipografika-melochi (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — список правок закрыт и расписан построчно, каждая обратима; рассуждения по ходу не требуется.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекционные слайды 1440×810 собираются в самодостаточный HTML; вёрстка на CSS в
`_generator/skeleton/` (`tokens.css` — единственный дом цвета и шрифта, `base.css` — роли текста,
`uzor.css` — узор в углах). Эталоном раньше был TeX, и часть его бесплатных удобств у нас просто
выключена. ЦЕЛЬ: закрыть список дешёвых мелочей, каждая из которых улучшает кадр и ничего не ломает.

🔴 **РАМКА ВЛАДЕЛЬЦА, действует на каждый пункт:** «если есть дешёвый инструмент, который легко
внедрить, и он не будет ничего портить, будет только улучшать — конечно, надо внедрять».
Обратное тоже верно: правка, которая двигает хоть одно число солвера или требует перекалибровки, —
**НЕ в этот заход**, а строкой в `## ВОПРОСЫ`.

🔴 **СТОП ДО ЦЕЛИ:** ты трогаешь ВИД. Ты НЕ трогаешь ни типологию слайдов, ни сцены, ни карточки
лекции — это соседние заходы `kod_tipologia-odna-os.md` и `kod_sceny-iz-blokov.md`, идущие ПОСЛЕ
тебя. Карточки `teorkat-vvedenie/**` тебе READ-ONLY целиком.
🔴 **`_generator/sborka/deck.py` формально в твоей зоне, но правки НЕ требует** — он там ради
`lang="ru"`, который уже стоит (`deck.py:500`). Тронул его — назови в отчёте отдельной строкой почему:
следующий за тобой заход про сцены правит этот же файл.
🔴 **ОЧЕРЁДНОСТЬ — ЗАХОДЫ ИДУТ ПОДРЯД, НЕ ОДНОВРЕМЕННО.** Порядок: **(1) `kod_mikrotipografika-melochi.md`
→ (2) `kod_tipologia-odna-os.md` → (3) `kod_sceny-iz-blokov.md`.** Каждый следующий отдаётся ТОЛЬКО
после того, как предыдущий принят и влит. Причина названа фактом, а не осторожностью: верификация
заходов 2026-08-13 нашла три общих файла — `_generator/sborka/gejt_kartochki.py` (705 строк, одна
точка сборки проверок) лежит в зонах (2) и (3) и оба обязаны его менять; `_generator/sborka/deck.py`
— в зонах (1) и (3); `_generator/tools/fixtures/sborka/` целиком вложен в `_generator/tools/fixtures/`.
Запусти два разом — конфликт слияния гарантирован, и разбирать его будет приёмка, а не автор правки.
**Стартуешь и видишь, что предыдущий заход ещё не влит, — СТОП, скажи об этом и не начинай.**


Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. заход дешёвый и косметический: каждая правка обязана быть обратимой и не двигать ни одного числа солвера
2. правки сортируются по заметность x дешевизна, самая дешёвая идёт первой
3. обрезанный узор в углу при горизонтальной вёрстке — регрессия правки 2026-08-11 про фоновые слои до края экрана
4. адреса путь:строка в каталоге инструментов устарели на 20-35 строк и подлежат пересчёту

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/mikrotipografika-melochi`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/mikrotipografika-melochi`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add mikrotipografika-melochi --branch zahod/mikrotipografika-melochi
  cd ../materials-wt/mikrotipografika-melochi
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: он молча перепишет рабочее дерево — цена этого названа в `GIT-disciplina §4`. ⚠ Формулировку «рядом идут другие заходы» из вшитого шаблона читай осторожно: по решению аналитика эти три захода идут ПОДРЯД, не одновременно (блок ОЧЕРЁДНОСТЬ выше). 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/skeleton/` `_generator/sborka/tipy.py` `_generator/sborka/deck.py` `_studio/konvejer/KATALOG-INSTRUMENTOV-VERSTKI.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

🔴 **ВНИМАНИЕ: строка ниже собрана генератором и ОНА НЕВЕРНА — поправка аналитика 2026-08-13.**
Невлитых веток на самом деле **четыре**: `zahod/porozhdenie-kartochki`, `zahod/tipologia-slajdov`,
`zahod/zakony-v-gejt`, `zahod/zamer-blokov-l2` (проверено `git branch --no-merged arka/mat-kostyak`).
**Вливать их всё равно НЕ надо:** их содержимое уже лежит на диске, перенесено не слиянием
(`git_zona.py poteri` 2026-08-13: расходится только `_studio/docs/KARTA.md` — известный дефект
сборки заходов, не работа). **Но входное число для Г3 равно 4, а не 0** — увидишь четыре и
не поднимай ложную тревогу; выросло сверх четырёх — вот это называй.

~~вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.~~

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- правки скелета, узора и каталога — своей зоной, хэш в отчёт

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.
(⚠ «рядом может идти чужой заход» — про репозиторий вообще; ЭТИ три захода идут подряд, см. блок ОЧЕРЁДНОСТЬ.)

- ветку zahod/mikrotipografika-melochi не гасить — вливает приёмка; чужие ветки и рабочие папки не трогать

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/mikrotipografika-melochi`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/skeleton/` `_generator/sborka/tipy.py` `_generator/sborka/deck.py` `_studio/konvejer/KATALOG-INSTRUMENTOV-VERSTKI.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы. Проект не изучай, ничего сверх списка не открывай:
  1. `_generator/skeleton/tokens.css` — цвет, шрифт, шкала кегля; единственный дом токенов.
  2. `_generator/skeleton/base.css` — роли текста (`.t-body`, `.t-small`, `.t-display`, `.t-math`), списки, классы `op-*`.
  3. `_generator/skeleton/uzor.css` — узор в углах.
     ⚠ **Он только ОБЪЯВЛЯЕТ переменные `--uzor-x/--uzor-y/--uzor-sz` с запасными значениями.**
     Считает их `_generator/skeleton/engine.js` (район строк 160–237, `bleedUzor.style.display`),
     а сам слой `#bleed` объявлен в `base.css` (район строки 30), НЕ в `uzor.css`.
  3½. `_generator/skeleton/engine.js` — **читать обязательно для Э6**: дефект узла углов почти
     наверняка живёт здесь, а не в CSS. Файл в зоне; запрет «ничего сверх списка» его не касается —
     он в списке.
  4. `_generator/sborka/tipy.py` — геометрия типов вёрстки, зоны слайда.
  5. `_studio/konvejer/KATALOG-INSTRUMENTOV-VERSTKI.md` — опись 38 элементов;
     ⚠ **дата данных 2026-08-11, адреса `путь:строка` в нём сместились на 20–35 строк** после правок
     08-12 (вставка слоя `#bleed`). Верить его номерам строк нельзя — только грепу.
- 🔴 **Каждый адрес проверь грепом ПЕРЕД тем, как на него опереться.** Расхождение — строка в `## УРОКИ ФАБРИКЕ`.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
🔴 **ПОРЯДОК ПУНКТОВ — «заметность × дешевизна», и он не случаен.** Правка на десять секунд идёт
первой ВСЕГДА: она не конкурирует за время, зато конкурирует за доверие. Иди сверху вниз.
Каждый пункт делается и проверяется ОТДЕЛЬНО, своим коммитом мысли: пункт, который что-то ломает,
должен быть виден поимённо, а не растворяться в общей правке.

**Все состояния ниже сняты грепом 2026-08-13 на живом репозитории** — перепроверь их первым ходом
своими командами; расхождение = предпосылка захода ложна, скажи об этом в `## ПЛАН` до работы.

### Э1. Переносы слов — `hyphens: auto` (замер: `grep -rn hyphens _generator/skeleton/` → **0**)

То, чем славится TeX, у нас выключено целиком. `lang="ru"` **уже стоит** на `<html>`
(`sborka/deck.py:500`, `sborka/slaid.py:136`, `skeleton/shablon.html:2`) — то есть цена вопроса
одна строка, а не аудит разметки. Ставить на текстовые роли `base.css`, не на всё подряд.

🔴 **Единственная правка списка, которая МОЖЕТ сдвинуть вмещение**, — переносы меняют высоту абзаца.
Поэтому она первая по заметности, и поэтому её эффект **измеряется, а не предполагается**:
сравни высоты текстовых зон до и после (см. критерий). Стало хуже хоть на одном слайде — откатывай
и пиши в отчёт, а не подкручивай числа солвера: солвер вне твоей зоны.

### Э2. `text-wrap: pretty` на остальные текстовые роли (замер: **2** вхождения)

Сегодня стоит только на `.t-body` (`base.css:49`) и `.t-display` (`base.css:64`, там `balance`).
Не покрыты подписи и прочие роли. Правило: `balance` — заголовкам и подписям (короткие, до ~6 строк),
`pretty` — всему остальному. Деградация чистая: браузер без поддержки просто игнорирует.

### Э3. `font-variant-numeric: tabular-nums` (замер: **0** вхождений)

Ровные колонки чисел, нулевая цена. Ставить туда, где числа стоят столбиком, а не глобально:
в сплошном тексте старостильные и табличные цифры выглядят по-разному, и глобальная установка —
это уже дизайнерское решение, а не мелочь.

### Э4. Класс акцента `.alert` (замер: `grep -c '\.alert' base.css` → **0**)

Каталог называет его отсутствующим, токен `--brick` при этом уже есть — вешать не на что.
Одна строка CSS, новый неиспользуемый класс: регрессии нет по построению.

### Э5. Токен трекинга и начертание подписи (замер: `letter-spacing` в `tokens.css` → **0**)

`--ls-caption` со **значением по умолчанию 0** — тогда поведение не меняется ни на одном слайде,
а ручка появляется. Каталог: «токенов трекинга нет вовсе», «начертание роли подпись не токенизировано».

### Э6. 🐞 Узор в углу обрезается при горизонтальной вёрстке

Регрессия правки 2026-08-11 «фоновые слои до края экрана» (слой `#bleed`, `uzor.css`).
Владелец глазами на собранном деке: когда вёрстка горизонтальная и иллюстрация снизу, узор
«странно обрезается — реально на него не налезает, но кусок обрезает».
🔴 **Владелец обещал прислать скриншот. Его нет в заходе — значит воспроизводи дефект сам сборкой,
а не догадывайся.** Не воспроизвёлся — это законный отчёт: «не воспроизвёл, собрал такие-то слайды,
нужен скриншот», и НЕ повод править `uzor.css` наугад.

### Э7. Обновить адреса в каталоге инструментов

🔴 **СКВОЗНОЙ НУМЕРАЦИИ 1–38 В КАТАЛОГЕ НЕТ — не ищи «пункт 28», не найдёшь.** Проверено при сборке:
в §3.1–§3.7 таблицы без колонки номера, «38» стоит только итогом. Ищи строки по НАЗВАНИЮ элемента
(первая ячейка строки) внутри своего §-раздела. Четыре адреса, которые надо пересчитать
(нумерация ниже — ревизии 2026-08-13, она внешняя по отношению к файлу):

- **маркер списка 1 уровня** — было `base.css:171-186`, живое ≈ `base.css:206-216`;
- **`itemsep` как токен** — было `base.css:182-183`, живое ≈ `base.css:208`;
- **колонтитул / номер слайда** — было `base.css:88`, живое ≈ `base.css:109-118`;
- **разделительная линия** — было `tipy.py:169-172`, живое ≈ функция `_linija_ill` `tipy.py:178-204`.

Плюс два статуса, которые каталог называет неверно:
- **визитка / QR** — числится долгом «без QR», а QR реально выведен (`sborka/deck.py:347-348`)
  с 2026-08-10, то есть ЕЩЁ ДО написания каталога: он описал устаревший долг в момент рождения;
- **летербокс** — сменил механизм с плоской заливки на активный слой `#bleed`.

🔴 **Все «живое ≈» выше — ориентиры, снятые 2026-08-13. Каждый проверь грепом и впиши СВОЁ число.**
Впишешь моё, не проверив, — воспроизведёшь ровно тот дефект, который чинишь (строка про визитку и QR: статус «долг» при живом коде).
🔴 **Правишь третью колонку — ставь рядом ДАТУ ДАННЫХ и команду, которой статус проверен.**
Отметка «внедрено» без даты данных доверия не заслуживает — этот каталог ровно так и разъехался.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — четыре клаузы)**

1. **Дека собирается, и высоты текстовых зон измерены ДО и ПОСЛЕ по ВСЕМ слайдам** —
   существующим инструментом, не самодельным: в репозитории есть `_generator/sborka/vmeshchenie.py`
   и гейт вмещения (`_generator/sborka/gejt_vmeshcheniya.py`); оба READ-ONLY, зови их, не переписывай.
   Не подошли — скажи чем и предложи замер в `## ПЛАН` ДО работы, а не изобретай молча.
   Таблица в отчёте; слайдов, где текст стал переполнять зону, — `0`. Есть такие — назови поимённо
   и откати вызвавшую правку: **чинить их подкруткой солвера запрещено, солвер вне зоны.**
2. **Каждый пункт Э1–Э7 имеет в отчёте строку `сделано` / `не сделано, потому что …`.**
   Пункт без строки = отчёт не принимается. «Не сделано» с причиной — законный исход, особенно Э6.
3. **Ни одно число солвера не изменилось:** `git diff --stat` не содержит `vmeshchenie.py`, `smeta.py`,
   `korpus.py` и вообще ничего вне объявленной зоны.
4. **Обратимость каждой правки названа:** для Э1–Э5 в отчёте — что именно откатить одной строкой,
   если владельцу не понравится вид. Правка без пути отката в косметическом заходе не принимается.

🔴 **Вид проверяется ГЛАЗАМИ, а не кодом, и это объявленная слепая зона гейтов.** Машинные проверки
ловят структуру и не ловят «стало некрасиво». Собери деку и **назови абсолютный путь к HTML в отчёте** —
смотреть будет владелец на своей машине.

🔴 **Код возврата смотри ПЕРВЫМ** (`echo $?`), и сверяй ВРЕМЯ собранного HTML: оборванная сборка
снаружи выглядит как удачная, и ты будешь измерять старый файл.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Не нужен: операция не лоссовая — правки аддитивные и обратимые, ничего не удаляется и не переносится.**
Хватает самопроверки критерия §2 (измерение высот до/после и `git diff --stat`).

**Одно исключение — Э7:** правка каталога переписывает утверждения о состоянии кода. Прежде чем
поменять статус любого пункта, **проверь его грепом по живому файлу и вставь команду рядом со статусом**.
Статус, переписанный по этому заходу без своей команды, — ровно тот дефект, который каталог уже дал
однажды (пункт 28: «долг» при живом коде).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/skeleton/ _generator/sborka/tipy.py _generator/sborka/deck.py _studio/konvejer/KATALOG-INSTRUMENTOV-VERSTKI.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/skeleton/ _generator/sborka/tipy.py _generator/sborka/deck.py _studio/konvejer/KATALOG-INSTRUMENTOV-VERSTKI.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/skeleton/` `_generator/sborka/tipy.py` `_generator/sborka/deck.py` `_studio/konvejer/KATALOG-INSTRUMENTOV-VERSTKI.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/skeleton/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/tipy.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/deck.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/konvejer/KATALOG-INSTRUMENTOV-VERSTKI.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Гит-контур пройден.** Заявок в очереди 2 (`gejt-kartochki-py-narrativ-2026`, `tipy-slajdov-py-2-primer-3`) — обе про файлы вне моей зоны (`gejt_kartochki.py`, `tipy_slajdov.py` — это НЕ `sborka/tipy.py` моей зоны) и про содержательные архитектурные решения (новый закон фазы 2, новый тип вёрстки), а не про git-операцию слияния/закрытия ветки. Это не тот класс заявок, для которого «единственное законное действие за пределами зоны» — git-команда; execute их значило бы решать чужую задачу вне рамки косметического захода. Оставляю ОБЕ открытыми, не исполняю и не закрываю — они принадлежат заходам 2/3 очереди (типология, законы карточек). Невлитых веток — 4, подтверждено `git branch --no-merged arka/mat-kostyak`, совпадает с поправкой аналитика.

**Все замеры из тела захода перепроверены грепом на живом дереве (2026-08-13) — совпали все:**
- Э1 `hyphens` в `_generator/skeleton/` → 0 вхождений.
- Э2 `text-wrap` в `base.css` → 2 (`.t-body: pretty` строка 49, `.t-display: balance` строка 64).
- Э3 `tabular-nums` → 0.
- Э4 `.alert` в `base.css` → 0.
- Э5 `letter-spacing` в `tokens.css` → 0.
- Э7 адреса: `.tlist` (маркер) `base.css:206-216` ✓, `itemsep`→margin-top `base.css:208` ✓, `#lect-number` `base.css:109-118` ✓, `_linija_ill` `tipy.py:178-204` ✓, QR `deck.py:347-348` ✓ (не долг — реализован).

**Порядок исполнения — заметность×дешевизна, как велит §2: Э4 → Э5 → Э3 → Э2 → Э1 → Э7 → Э6.**
(Э4/Э5/Э3 — по одной строке, нулевой риск; Э2 — расширение существующего правила на 1 роль; Э1 — единственный пункт, способный сдвинуть вмещение, поэтому идёт последним перед замером; Э7 — правка документа; Э6 — диагностика, воспроизведена ДО правок кода, см. ниже, чтобы не путать её эффект с Э1–Э5 в замере высот.)

**Э6 воспроизведён ДО остальных правок**, собственной сборкой `_generator/sborka/deck.py teorkat-vvedenie/L2` (READ-ONLY карточки, вывод — в scratchpad, не в `teorkat-vvedenie/dist`) + браузер с нестандартным окном 1200×900 (даёт вертикальный леттербокс, воспроизводящий бленд). Диагноз точный, не догадка:
- слайд `napominanie` (`tolko_tekst`, без иллюстрации) — узор в углу НЕПРЕРЫВЕН через границу кадра (контроль, шва нет).
- слайд `inyekcii` (`polosa_gorizontalnaya`, иллюстрация снизу) — в том же углу ЧЁТКИЙ ШОВ: узор виден снаружи кадра (рисует `#bleed-uzor`), но обрывается ровно на границе кадра и НЕ виден внутри.
- Причина в коде: `_generator/sborka/tipy.py` `GLOBAL_CSS` (`.zone.board{background:var(--board)}`) кладёт НЕПРОЗРАЧНЫЙ сплошной фон на дочернюю зону иллюстрации, которая у `polosa_gorizontalnaya` занимает нижнюю полосу и достаёт до обоих нижних углов кадра — этот фон рисуется ПОВЕРХ фона `.slide` (там, где по факту и лежит узор) и полностью его закрывает изнутри кадра.
- `engine.js:237-238` (`bleedUzor.style.display = getComputedStyle(slide).backgroundImage === 'none' ? 'none' : ''`) решает «рисовать ли узор снаружи» ТОЛЬКО по фону ВСЕГО слайда целиком (кейс служебных слайдов со сплошным `#id{background:...}`) — он не знает о ЧАСТИЧНОМ перекрытии одним дочерним элементом одного из двух углов. Для `polosa_gorizontalnaya`/`polosa_vertikalnaya`/`kompozit` (везде есть `.zone.board`, достающая до кромки) узор снаружи рисуется, а внутри — нет, и это ровно то, что глазами описал владелец: «реально на него [узор] не налезает [иллюстрация узор не перекрывает — конфликта содержания нет], но кусок обрезает [то есть тот же мотив виден и абрупто обрывается на границе]».
- Проверил математикой (`getBoundingClientRect`, `getComputedStyle`, инлайновые `--uzor-x/y/sz`) — размер и позиция узора на `.slide` и на `#bleed-uzor` СОВПАДАЮТ пиксель-в-пиксель, левый-нижний угол картинки у обоих ложится в одну и ту же точку окна. То есть блендовый слой (регрессия 08-11) устроен ВЕРНО для случая «нет перекрывающей зоны»; баг — в том, что вслепую включённое условие не учитывает частичное перекрытие одной зоной одного угла.
- **Чинить НЕ буду.** Честная правка требует научить `engine.js` определять перекрытие ПО УГЛУ (например, `document.elementFromPoint` в каждом из двух углов кадра, а не единственную проверку фона всего `.slide`) — это не одна строка и не заведомо безопасное расширение: трогает общий для ВСЕХ типов вёрстки блендовый механизм, есть риск непредвиденно погасить узор там, где он сегодня работает верно. Рамка владельца требует «дёшево и без риска»; это не тот случай. Дальше — в `## ВОПРОСЫ` пунктом очереди с полным диагнозом, чтобы решение принималось зряче, не на глаз.

**Критерий готовности п.1** — меряю existing-инструментом `_generator/sborka/vmeshchenie.py`/`gejt_vmeshcheniya.py` по всем 23 карточкам `teorkat-vvedenie/L2` (READ-ONLY контент, сборка в scratchpad) ДО и ПОСЛЕ Э1–Э5. Baseline снят: 21 из 23 слайдов с текстом — 100.0% заполнения (уже находятся на потолке солвера), 2 без текстовой зоны. Именно поэтому Э1 (единственный пункт, способный сдвинуть высоту) идёт последним и с особым вниманием — на потолке 100% любой рост абзаца даст переполнение мгновенно, это и есть критерий провала клаузы 1.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Узор в углу обрезается на любом слайде с иллюстрацией у кромки (`polosa_gorizontalnaya`/`polosa_vertikalnaya`/`kompozit` — везде есть `.zone.board`, достающая до угла), как только окно не 16:9 ровно (обычный случай реального экрана). Диагноз точный (см. `## ПЛАН`): `engine.js:237-238` проверяет фон ВСЕГО `.slide`, а не фон КОНКРЕТНОГО угла — там, где непрозрачная дочерняя зона перекрывает угол изнутри кадра, `#bleed-uzor` всё равно рисует узор снаружи, и на границе виден чёткий шов. Честный фикс — научить блендовый механизм проверять угол (`document.elementFromPoint` в двух точках кадра вместо одной проверки фона `.slide`), это расширение общего для всех типов вёрстки кода, не одна дешёвая строка — вне рамки этого захода. Найдётся на КАЖДОМ будущем слайде этого типа — не пункт очереди «когда-нибудь», а кандидат в заход СЕЙЧАС, до следующей сборки с иллюстрацией у кромки (правило «класс НЕМЕДЛЕННОЕ»).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Сделано (Э1–Э7):**
- Э1 hyphens:auto — **сделано.** `.t-body`/`.t-small` (`base.css:49-56,66-72`). Единственный пункт, способный сдвинуть вмещение — проверено гейтом (см. ниже), 0 расхождений. Откат: удалить три `hyphens`-свойства в обоих правилах.
- Э2 text-wrap на подписи — **сделано, `balance`, не `pretty`.** Тело пункта в заходе само уточняет правило: «balance — заголовкам и подписям (короткие, до ~6 строк), pretty — всему остальному» — заголовок пункта («pretty на остальные роли») разошёлся с этим уточнением, я следовал уточнению как более позднему и точному. `.t-small{text-wrap:balance}` (`base.css:70-72`). Откат: удалить свойство.
- Э3 tabular-nums — **не сделано.** Единственный числовой элемент в зоне — `#lect-number` («N / M», меняется при навигации, `engine.js:28`) — уже `font: ui-monospace, monospace` (`base.css:110-111`): цифры и так одинаковой ширины, `tabular-nums` там не даст никакого визуального эффекта. Других мест, где числа стоят столбиком, в грамматике нет (КАТАЛОГ §3.3/§3.5 подтверждает: ни нумерованного списка, ни таблицы на слайде не существует). Ставить его «просто чтобы был» — не решает задачу и не проходит рамку владельца («будет только улучшать»): здесь улучшать нечего.
- Э4 `.alert` — **сделано.** `base.css:189-193` (после `.acc`), `font-weight:700;color:var(--brick)`. Откат: удалить блок.
- Э5 `--ls-caption` — **сделано.** Токен `tokens.css:73-76` (дефолт `0`), применён `.t-small{letter-spacing:var(--ls-caption,0)}` (`base.css:70-72`). Поведение не изменилось ни на одном слайде (дефолт 0), ручка появилась. Откат: удалить токен и свойство.
- Э6 узор — **воспроизведён и диагностирован, не исправлен.** Полный диагноз и обоснование отказа от фикса — `## ПЛАН`; пункт очереди — `## ВОПРОСЫ` №1.
- Э7 адреса каталога — **сделано.** 4 адреса пересчитаны (маркер списка `base.css:171-186→206-216`, itemsep `base.css:183→208`, номер слайда `base.css:88→109-118`, разделительная линия `tipy.py:169-172→178-204`), везде проставлена дата данных 2026-08-13 и команда проверки. 2 статуса поправлены: визитка/QR (QR реально выведен `deck.py:347-348` с 08-10, «долг без QR» неактуален; долг «без пустоты снизу» не проверял — вне зоны, не переписываю то, чего не мерил) и летербокс (механизм сменился с плоской заливки на активный слой `#bleed`, доводка Л2 Ф1а).

**Заявки очереди (`git_zona.py zayavki`) — НЕ исполнены, обе вне зоны.** `2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026` (закон фазы 2 в `gejt_kartochki.py`) и `2026-08-12T0359-tipy-slajdov-py-2-primer-3` (правка `tipy_slajdov.py`, НЕ путать с моим `sborka/tipy.py`) — обе про содержательные архитектурные решения других файлов (типология, законы карточек), не про git-операцию слияния/закрытия ветки, которая единственно разрешена вне зоны. Оставлены открытыми для заходов 2/3 очереди.

**Как проверил:** грепом — все замеры §2 (Э1-Э5, четыре адреса Э7) переснял и они совпали с заявленными в теле захода; сборкой — собрал весь `teorkat-vvedenie/L2` (23 карточки, READ-ONLY контент, вывод только в scratchpad) до и после правок через `_generator/sborka/slaid.py` + `_generator/sborka/gejt_vmeshcheniya.py`, логи идентичны (`diff` → 0 расхождений, 21/21 «влезло» 100.0%, 2 «без текста» в обоих прогонах); глазами — открыл собранный дек в браузере на трёх типах слайдов (текстовый, обложка, горизонтальный с иллюстрацией), перенос слов и подпись выглядят штатно, регрессий не увидел; Э6 — воспроизвёл сборкой + геометрией DOM (`getBoundingClientRect`/`getComputedStyle`), не догадкой.

**Верификатор:** не нужен по §3 захода (операция не лоссовая, аддитивные обратимые правки). Самопроверка критерия §2 — таблица заполнения гейта вмещения ДО/ПОСЛЕ (см. `## ПЛАН` и логи выше), 0 расхождений; `git diff --stat` после коммита содержит только `base.css`, `tokens.css`, `KATALOG-INSTRUMENTOV-VERSTKI.md` — ни `vmeshchenie.py`, ни `smeta.py`, ни `korpus.py` не задеты.

**Что НЕ трогал:** `tipy.py` и `deck.py` (в зоне формально, правка не требовалась — не открывал их для правки, только читал по списку); `teorkat-vvedenie/**` (READ-ONLY, только собирал существующим генератором, вывод не внутри репозитория); `uzor.css`/`engine.js` — не правил, хотя они и в зоне (Э6 диагностирован, не исправлен, см. выше); типологию слайдов, сцены, карточки лекции — не трогал вовсе.

**Гигиена Г1-Г6:**
- Г1 `git_zona.py check --zone` по всем 4 путям зоны → ✅ везде.
- Г2 неприменимо — все пути внутри `materials/`.
- Г3 невлитых веток `git branch --no-merged arka/mat-kostyak` → 4 (`porozhdenie-kartochki`, `tipologia-slajdov`, `zakony-v-gejt`, `zamer-blokov-l2`) — не выросло сверх входных четырёх; моя `zahod/mikrotipografika-melochi` тоже в списке (закономерно, вливает приёмка).
- Г4 неприменимо — новых `.py` не заведено.
- Г5 неприменимо — новых `.md` не заведено.
- Г6 `git show --stat` → только мои 3 пути.

**ПОВТОРЯЕМОСТЬ находок:** Э6 (шов узора) повторится на КАЖДОМ будущем слайде типов `polosa_gorizontalnaya`/`polosa_vertikalnaya`/`kompozit` при нестандартном окне — это не пункт очереди «когда-нибудь», а кандидат в отдельный заход ДО следующей сборки с иллюстрацией у кромки кадра (класс НЕМЕДЛЕННОЕ, `## ВОПРОСЫ` №1). Остальные находки (Э3 «негде», Э7 устаревшие адреса) разовые, не повторяются на следующей карточке — законно уходят как есть, без пункта очереди (Э3 не про находку, а про честное «не сделано»; Э7 — сама правка).

**Необратимое:** необратимого нет. Все правки — аддитивные строки CSS/токенов и правка таблицы markdown; откат каждой назван построчно выше.

**Время прогона + токены:** на канале `app` неприменимо, снимать неоткуда (лог существует только на канале `terminal`).

**АРТЕФАКТ:** `/private/tmp/claude-501/-Users-ivanyakovlev/203f2db0-cf4c-4349-9306-68d795685c0d/scratchpad/mikrotipografika-priyomka/deck-POSLE.html` — открыть в браузере (пересобран при приёмке 2026-08-13 из коммита `7677e35` командой `_generator/sborka/deck.py teorkat-vvedenie/L2`, 26 слайдов, идентичен исходной сборке — код с 21:18 не менялся; прежний путь `.../4f250fb1-.../mikrotipografika/deck-POSLE.html` был старше файла-захода по mtime, гейт Г3 приёмки его не принял; лежит в scratchpad, не в репозитории — `teorkat-vvedenie/**` READ-ONLY).
**КОММИТ:** `7677e35` — «skeleton/каталог: микротипографика — переносы, text-wrap, alert, трекинг подписи» · `git_zona.py check --zone` → ✅ по всем 4 путям зоны
