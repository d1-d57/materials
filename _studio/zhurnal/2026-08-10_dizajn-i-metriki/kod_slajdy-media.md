# Канал исполнителя — slajdy-media (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — механика расписана: файлы разложены аналитиком, места вставки названы поимённо таблицей, порядок берётся из `slide_order`. Свободных решений два (устройство типа слайда и органы управления видео), оба названы развилками явно — этого Sonnet-у достаточно, а нерасписанного планирования здесь нет.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — механика расписана, развилки названы явно; см. шапку захода.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/slajdy-media.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_slajdy-media.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add slajdy-media --branch zahod/slajdy-media, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-10_dizajn-i-metriki`
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/beautiful-vigilant-euler/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекция `teorkat-vvedenie/L2` — HTML-дек 1440×810, собирается `_generator/sborka/deck.py`. Устройство источников уже такое, какого хочет владелец, и это ПРОВЕРЕНО по живым файлам 2026-08-16: по файлу на слайд (`teorkat-vvedenie/L2/slajdy/<имя>/slaid.md`), по папке на иллюстрацию (`teorkat-vvedenie/L2/illustracii/<имя>/risunok.svg` + `zakaz.md`), а карточка слайда ссылается на иллюстрацию строкой `illustracii: [имя]`, и генератор подтягивает её при сборке. ЦЕЛЬ: тем же способом научить дек носить МЕДИА — видео, канвас, 3D-сцену.

🔴 **СТОП ДО ЦЕЛИ, СКАЗАНО ЗАРАНЕЕ.** Ты делаешь ФОРМАТ и доказываешь его ОДНИМ пробным слайдом. Ты НЕ рисуешь manim-анимации лекции — это отдельная работа, и она начнётся после того, как владелец посмотрит пробный слайд глазами.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-15) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. формат общий для видео, канваса и 3D: узкая полоса фона снаружи, медиа в центре — они по устройству похожи и разными типами быть не должны
2. медиа лежит отдельным файлом, как иллюстрация: своя папка в illustracii/, генератор подтягивает по ссылке из карточки слайда — архитектура уже такая, новую не изобретать
3. видео встраивается файлом рядом, не base64 внутрь деки
4. заход НЕ рисует сами анимации — он делает формат и доказывает его одним пробным слайдом

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/slajdy-media`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/slajdy-media`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add slajdy-media --branch zahod/slajdy-media
  cd ../materials-wt/slajdy-media
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/sborka/,` `_generator/skeleton/,` `teorkat-vvedenie/L2/illustracii/,` `teorkat-vvedenie/L2/slajdy/,` `teorkat-vvedenie/L2/dist/,` `sayt/,` `_studio/docs/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone _generator/sborka/, --zone _generator/skeleton/, --zone teorkat-vvedenie/L2/illustracii/, --zone _studio/docs/ --kommitit 'формат слайда-контейнера, поддержка медиа-иллюстрации в сборке, пробный слайд с медиа' --zakryt 'свою ветку не гасить — гасит приёмка; чужие ветки и worktree не трогать'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/slajdy-media`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/sborka/,` `_generator/skeleton/,` `teorkat-vvedenie/L2/illustracii/,` `teorkat-vvedenie/L2/slajdy/,` `teorkat-vvedenie/L2/dist/,` `sayt/,` `_studio/docs/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, поимённо:
  - `teorkat-vvedenie/L2/slajdy/retrakt/slaid.md` — образец карточки: шапка, поле `illustracii:`, вшитый комментарий про фазы
  - `teorkat-vvedenie/L2/illustracii/luch-brauera/` — образец папки иллюстрации (`risunok.svg` + `zakaz.md`)
  - `_generator/sborka/tipy.py` — раскладки и `_ill_zone`: как иллюстрация встаёт в кадр (ищи по имени функции, не по номеру строки)
  - `_generator/sborka/deck.py` и `_generator/sborka/slaid.py` — как иллюстрация подтягивается при сборке
  - `_generator/skeleton/base.css` — правила `.ill-box`, `.panel`, `.zone`
- Проект сверх этого не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### Требование владельца — дословно, 2026-08-16
«Нам надо придумать общий такой формат, как бы должен был выглядеть слайд, на котором ничего не происходит кроме канваса или анимации. Мне кажется, они по сути очень похожим образом должны выглядеть: есть какая-то полоска фона снаружи, небольшая, а в центре встроенное какое-то стороннее… там сторонний кусок кода, который либо выносится отдельным файлом, как в случае с видео или сложной какой-то 3D-анимацией, либо может встраиваться внутри, но где-то лежать в отдельном месте… И иллюстрации я бы хранил в отдельном месте, и тексты в отдельном месте, и анимации тем более — в отдельных файлах, из которых генератор собирает либо один файл, либо несколько файлов для деплоя».
И про сам вид: «это, скорее всего, суперэлементарная вещь, то есть это будет просто картинка на зелёном фоне, и узкие поля — всё кроме полей занимает вот это видео или канвас. То есть там делать нечего, элементарно».

### М1. Тип слайда-контейнера под медиа
**Что сделать:** одна раскладка на все три случая (видео, канвас, 3D) — узкая полоса фона по краям, медиа занимает всё остальное. Разными типами они быть не должны: владелец прямо сказал, что устроены они одинаково.
**Ограничитель:** это САМАЯ ПРОСТАЯ вещь в заходе, и она такой и должна остаться. Ни подписей, ни рамок, ни заголовка поверх, ни хитрой сетки — фон, поля, медиа. Разрастётся сверх этого — ты решаешь не ту задачу.

### М2. Медиа как иллюстрация: своя папка, ссылка из карточки
**Что сделать:** медиа живёт в `teorkat-vvedenie/L2/illustracii/<имя>/` рядом с `zakaz.md` — ровно там же, где `risunok.svg` у обычной иллюстрации, и подтягивается той же строкой `illustracii: [имя]` из карточки слайда.
🔴 **АРХИТЕКТУРУ НЕ ИЗОБРЕТАТЬ — ОНА УЖЕ ЕСТЬ.** Владелец 16.08 спросил, была ли идея «отдельный файл на слайд, отдельный файл на иллюстрацию» реализована. Проверено по живым файлам: реализована. Твоя работа — РАСШИРИТЬ существующий механизм на медиа, а не построить рядом второй. Второй механизм рядом с первым — это дефект, даже если он лучше.
**Ограничитель:** имена и конвенции бери из живых файлов, не придумывай своих.

### М3. Видео — файлом рядом, не байтами внутрь
**Решение владельца, финализировано:** видео встраивается файлом рядом с декой, НЕ base64 внутрь.
🔴 **Это ломает нынешнее свойство дека, и слом надо назвать вслух, а не протащить молча.** Сегодня дек самодостаточен: один HTML без единой внешней ссылки — шрифты, картинки и узор лежат в нём base64 (шапка `uzor.css` объясняет, чем за это платят). Видео так носить нельзя, оно тяжёлое. Значит дек с медиа перестаёт быть одним файлом и становится файлом + папкой рядом.
**Что сделать:** описать в отчёте, ЧТО теперь нужно копировать вместе с деком, чтобы она работала у слушателя, и как это отражается на публикации. Развилку «как деплоить дек с медиа» НЕ решай сам — вынеси в `## ВОПРОСЫ`.

### М4. Встроить ЧЕТЫРЕ ГОТОВЫХ ВИДЕО в названные места
🔴 **Видео УЖЕ СДЕЛАНЫ владельцем и уже лежат в проекте.** Рисовать, генерировать, пересжимать или «улучшать» их НЕЛЬЗЯ — это авторский материал, сделанный вне фабрики. `manim` в проекте не установлен, и он тебе не нужен.

Аналитик разложил их 2026-08-16 по схеме обычной иллюстрации — папка на медиа, внутри `video.mp4` и `zakaz.md`:

| папка в `teorkat-vvedenie/L2/illustracii/` | что показывает | ставится ПОСЛЕ слайда |
|---|---|---|
| `kompoziciya-strelok/` | композиция `g∘f` и ассоциативность | `kategoriya` |
| `funktor-sohranyaet-kompoziciyu/` | функтор `F: C→D` переносит объекты, стрелки, композицию | `funktor` |
| `zamena-bazisa-estestvennost/` | `A' = M_W A M_V⁻¹` — два выбора координат и переход между ними | `zamena-bazisa` |
| `inyekciya-i-sechenie/` | `s: A→B`, `r: B→A`, образ `s(A)` внутри `B` | `inyekcii` |

**Порядок слайдов деки берётся из `teorkat-vvedenie/L2/brief.md`, ключ `slide_order`** — правишь именно его, вставляя четыре новых слайда каждый сразу за названным. Числа не переписывай из захода: `slide_order` читается с диска.

**Свойства файлов, снятые командой (`ffprobe`, 2026-08-16):** все четыре — MP4/H.264, 1920×1080, то есть ровно 16:9, в точности пропорции кадра 1440×810. Без звука. Длительности 47, 60, 77, 59 с; суммарный вес 7.9 МБ.
🔴 **Из длительности следует то, что меняет вёрстку: это НЕ короткие петли-украшения, а полноценные объяснения на минуту.** Слайд с таким видео лектор показывает и комментирует. Значит нужны органы управления (play/pause, перемотка) — иначе на лекции его нечем остановить. Автовоспроизведение и зацикливание тут скорее вредны; решаешь ты, но решение назови в отчёте вслух.
**Фон видео — тот же зелёный, что и `--board` темы**, а объекты внутри бежевые. Проверь совпадение оттенка фактом (пипеткой по кадру против значения токена) и, если сходится, ставь видео на зелёную подложку — шва не будет вовсе. Не сходится — скажи числом, насколько, и не подгоняй молча.

### М4½. Тип слайда — то, ради чего заход и затевался
**Слова владельца, 2026-08-16:** «придумать, как новый тип слайда... это вот именно слайд, который полностью без текста посвящён некоторой сторонней какой-то иллюстрации интересной. Да, может быть и картинка на самом деле статичная иногда».
**Что сделать:** тип слайда БЕЗ ЗАГОЛОВКА и без текстовых блоков — узкая полоса фона по краям, всё остальное занимает медиа. Один тип на все случаи: видео, канвас, 3D, статичная картинка. Это ровно то, что владелец описал 16.08 раньше: «полоска фона снаружи, небольшая, а в центре встроенное».
**Ограничитель:** тип простой, и таким должен остаться — фон, поля, медиа. Подписи, рамки, заголовки, номера — ничего этого здесь нет. Разрастается — ты решаешь не ту задачу.
⚠ Это НЕ разделитель: у разделителя есть текст и номер раздела, он служебный. Здесь слайд содержательный, просто без слов.

### М5. Выложить на витрину — тем же ходом
🔴 **Цель владельца дословно: «я хочу, чтобы они тоже были в версии на сайте».** Заход не закончен, пока видео не играет на витрине.
**Что сделать:** пересобрать деку, положить её в папку `sayt/static/teorkat/` под именем lekciya-2.html (файл там уже есть — заменяешь), медиа положить туда же рядом, вывезти `python3 _generator/tools/git_zona.py opublikovat --yes`.
🔴 **ГЛАВНЫЙ РИСК ЗАХОДА, и он проверяется отдельной клаузой.** Дека с видео перестаёт быть одним самодостаточным файлом: 7.9 МБ видео внутрь base64 не кладутся. Значит рядом с `lekciya-2.html` едет папка с медиа, и ссылки внутри деки обязаны быть ОТНОСИТЕЛЬНЫМИ. Абсолютный путь твоей рабочей папки протухает ровно в момент публикации, и слушатель увидит пустой прямоугольник вместо анимации.
**Проверка, которая ловит именно это:** открой опубликованную деку так, как её откроет слушатель — из папки `sayt/`, а не из своей рабочей, — и убедись, что видео играет. Кадр в отчёт.


---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).**

1. **М2 доказан фактом, а не описанием:** медиа-папка лежит в `illustracii/`, карточка ссылается на неё той же строкой `illustracii:`, что и на обычный рисунок. Грепом показать обе строки — из карточки и из кода, который их читает.
2. **М4:** все ЧЕТЫРЕ видео стоят каждое за своим слайдом — по `slide_order` из `brief.md` (вывод в отчёт) и по кадрам: четыре `.png` в `kadry/`, по одному на новый слайд. Кадром же закрывается и М4½ (геометрия: узкая полоса фона, медиа в центре, заголовка нет). Путь от корня репозитория, **кадры приложены** — `.png` в `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/`. Требование про ВИД закрывается кадром: решение владельца 2026-08-16, счёт в DOM и греп доказательством не считаются.
3. **Регрессия:** дек L2 собирается как раньше и не сломан — `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o /tmp/L2-proverka.html`, rc=0, число слайдов совпадает с тем, что было ДО твоей работы (сними его ПЕРВЫМ ходом и напечатай оба числа).
4. **М3+М5:** дека выложена на витрину, и в отчёте назван поимённо КАЖДЫЙ уехавший туда файл, включая медиа. Отдельной строкой — вывод проверки «открыл деку из папки `sayt/`, медиа играет», с кадром.
5. **Видео не тронуты:** `ffprobe` по каждому из четырёх после работы даёт те же кодек, разрешение и длительность, что до неё (оба вывода в отчёт). Пересжатое, обрезанное или перекодированное видео — брак, даже если «стало легче».

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «регрессии нет», а «регрессии нет, слайдов было N, стало N».

🔴 **ВСЁ ВЫШЕ ПРОВЕРЯЕТСЯ ЖИВЫМ ПРОГОНОМ НА РЕАЛЬНОМ ОБЪЕКТЕ РЕПОЗИТОРИЯ — на лекции `teorkat-vvedenie/L2`, а не на фикстуре.** Зелёная фикстура — необходимое условие и недостаточное: она проверяет, что код не падает, а не что владелец увидит починку. Прогнал только фикстуру — критерий НЕ закрыт.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Отдельный субагент-верификатор НЕ нужен** — заход не двигает и не жмёт существующее, он добавляет новое; сохранность старого закрывает клауза 3 критерия §2 (регрессия числом слайдов).
🔴 **Но одна ловушка объявляется прямо:** ты проектируешь формат по описанию владельца, а не по образцу — значит велик соблазн ПОДЫГРАТЬ собственному замыслу и объявить удачным то, что просто собралось. Кадр из клаузы 2 существует ровно против этого. **Считаешь, что формат по описанию владельца выходит хуже, чем мог бы, — скажи это в `## ПЛАН` ДО работы и предложи поправку; не переделывай молча.**

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/sborka/ _generator/skeleton/ teorkat-vvedenie/L2/illustracii/ teorkat-vvedenie/L2/slajdy/ teorkat-vvedenie/L2/dist/ sayt/ _studio/docs/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/sborka/ _generator/skeleton/ teorkat-vvedenie/L2/illustracii/ teorkat-vvedenie/L2/slajdy/ teorkat-vvedenie/L2/dist/ sayt/ _studio/docs/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/sborka/,` `_generator/skeleton/,` `teorkat-vvedenie/L2/illustracii/,` `teorkat-vvedenie/L2/slajdy/,` `teorkat-vvedenie/L2/dist/,` `sayt/,` `_studio/docs/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/sborka/,` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/skeleton/,` → ✅; `python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/L2/illustracii/,` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/docs/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Оспариваю неявную предпосылку М1/М4½.** Задача просит «придумать» новый тип слайда-контейнера
под медиа. По живым файлам (`_generator/sborka/tipy.py`, обработчик `polnyj_ekran`) он УЖЕ
существует: при непустом `illustracii` этот тип рендерит ТОЛЬКО `_ill_zone(..., cls="board full")`
без вызова `_text_zone` — то есть без заголовка, без текстовых блоков, с полем `ILL_PAD=28px`
(≈2 % ширины кадра) по всем сторонам и медиа на весь остаток. Это дословно то, что просит М4½
(«полоска фона снаружи, небольшая, в центре — медиа», «без заголовка»). CSS уже несёт частичную
поддержку разных видов медиа: `.panel > img`, `.panel > svg`, `.panel > canvas` — три правила под
три из четырёх заявленных видов; видео не хватает ровно одного правила. ⇒ М1 и геометрическая
часть М4½ закрываются ПЕРЕИСПОЛЬЗОВАНИЕМ `polnyj_ekran`, а не новым обработчиком — расширять
`tipy.py` не буду, это и есть требуемое «архитектуру не изобретать» из М2, применённое к вёрстке.
Если владелец считал, что нужен физически новый тип, — это и есть предлагаемая поправка, скажите,
если нет.

**Находка по git-контуру (см. §0.1 схема).** Заход предполагает, что четыре папки видео уже лежат
в рабочей папке захода — они лежали ТОЛЬКО в главной папке репозитория как untracked-файлы;
`git worktree` не переносит untracked-содержимое рабочего дерева. Скопировал руками (`cp -p`) в
свою зону, проверил md5 — идентичны. Это находка про фабрику, не про задачу; вынесена в `## ВОПРОСЫ`
пунктом очереди (не в `## УРОКИ ФАБРИКЕ` — по правилу шапки секции).

**М3/М5 — какой ход для медиа выбираю.** `slaid.py:load_illustrations` сегодня ВСЕГДА инлайнит
содержимое `risunok.svg`/`risunok.html` текстом в `<template>`. Для видео это невозможно (M3: не
base64). План: третья ветка в `load_illustrations` — при наличии `video.mp4` в папке иллюстрации
кладёт `<template id="ill-<stem>"><video src="<REL>" controls preload="metadata"></video></template>`,
где `<REL>` — путь ОТНОСИТЕЛЬНО папки итогового HTML (`os.path.relpath`), вычисленный из реального
расположения `illustracii/<stem>/video.mp4`. Для сборки в `L2/dist/index.html` это даст
`../illustracii/<stem>/video.mp4` — файл рядом, ничего копировать внутри репозитория не нужно
(`dist/` и `illustracii/` — соседние папки одной лекции). Для публикации на `sayt/` относительный
путь того же вида потребует физической копии `illustracii/<stem>/video.mp4` рядом с
`lekciya-2.html` — это и есть слом самодостаточности, названный в М3 вслух. `out_dir` придётся
прокинуть параметром через `compile_slide_html`/`build()` (сегодня оба не знают, куда пишут, в
момент загрузки иллюстраций).
**Органы управления (развилка владельца, М4):** нативные `<video controls>`, БЕЗ `autoplay`, БЕЗ
`loop`, `preload="metadata"` (не тянуть все 7.9 МБ заранее). Основание: видео длинные (47–77 с),
это не декоративные петли, а объяснения, которые лектор комментирует вручную — автовоспроизведение
и зацикливание владелец сам назвал скорее вредными.
**Зелёный фон (М4):** снял кадр `ffmpeg -ss 2` с каждого из 4 видео, пипеткой (PIL) сверил фон
против токена `--board:#C7D2C1`. Результат: видео-фон RGB(198,210,192), токен RGB(199,210,193) —
расхождение 1/255 по двум каналам, в пределах шума кодека. Ставлю видео на стандартную `.zone.board`
без отдельной подложки — шва не будет.

**Шаги:**
1. `_generator/skeleton/base.css`: добавить `.panel > video{width:100%;height:100%;object-fit:contain;display:block}`
   рядом с существующими правилами `.panel > img/svg/canvas`.
2. `_generator/sborka/slaid.py`: `load_illustrations` — ветка `video.mp4`; `compile_slide_html` и
   `main()` — прокинуть `out_dir=Path(args.out).parent`.
3. `_generator/sborka/deck.py`: `build()` — прокинуть `out_dir=Path(out).parent` в `load_ill`
   (переиспользуемый `load_illustrations`).
4. Четыре карточки `teorkat-vvedenie/L2/slajdy/<имя>/slaid.md`, `tip_verstki: polnyj_ekran`,
   `illustracii: [<стем>]`, без текстовых блоков, `tip_slaida: Т6` (служебный по составу — геометрию
   задаёт медиа, не блоки; тот же смысл, что у обложки/визитки/финала — вопрос классификации, если
   спорный, ухожу в `## ВОПРОСЫ`).
5. `teorkat-vvedenie/L2/brief.md` — вписать 4 новых имени в `slide_order`, каждое сразу за
   `kategoriya`/`funktor`/`zamena-bazisa`/`inyekcii` по таблице М4.
6. Собрать дек (снят баланс ДО: `deck.py teorkat-vvedenie/L2 -o /tmp/L2-proverka-DO.html` → rc=0,
   слайдов 26), сравнить со «стало». Снять кадры `kadry/*.png` по одному на новый слайд.
7. М5: пересобрать в `teorkat-vvedenie/L2/dist/`, скопировать в `sayt/static/teorkat/lekciya-2.html`
   + медиа рядом, `git_zona.py opublikovat --yes`, открыть опубликованную деку из `sayt/`, кадр.
8. `ffprobe` по каждому видео ПОСЛЕ работы — сверить с уже снятым «до».
9. Коммит зоны (§4).

**Критерий, который может провалиться:** относительный путь к видео при публикации на `sayt/` —
если структура `sayt/static/teorkat/` не сохраняет ту же вложенность `illustracii/<stem>/…`, видео
не найдётся у слушателя. Проверяю фактом (открыть деку из `sayt/`), не предположением.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `git worktree` не наследует untracked-содержимое рабочего дерева главной папки. Аналитик разложил
   4 папки медиа (видео + `zakaz.md`) в главном репозитории 2026-08-16, зная, что заход работает в
   отдельном worktree — но worktree делит только объекты `.git` и ветки, не untracked-файлы рабочего
   дерева. Без ручной проверки (`ls` главной папки против рабочей) исполнитель начал бы правку с
   пустым пулом иллюстраций и упёрся бы в `FormatSlaida` на первой же сборке. Починил сам: скопировал
   4 папки (`cp -p`) из главной в рабочую, сверил md5 (все 4 совпали). Повторится на КАЖДОМ заходе,
   где аналитик готовит untracked-файлы заранее для worktree-исполнителя — не разовая, а системная
   находка.
   ДОМ: _studio/zhurnal/2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. `_generator/tools/git_zona.py opublikovat` разворачивает `sayt/` на витрине с ФЛАТТЕНОМ (без
   префикса `sayt/`) и БЕЗ остального репозитория — витрина `main` не содержит `teorkat-vvedenie/`.
   Заход это как раз и предупреждал явно (§ГЛАВНЫЙ РИСК ЗАХОДА, М5) — но первая публикация
   (коммит `793e992`) всё равно ушла с видео, ссылающимся на `../../../teorkat-vvedenie/L2/
   illustracii/…` (путь, корректный только внутри полного checkout репозитория). Поймал сам,
   проверкой `git ls-tree origin/main` — «teorkat-vvedenie» не нашлось вовсе. Починил вторым
   коммитом (`fe1ae5a`): 4 видео физически скопированы в `sayt/static/teorkat/illustracii/`, пути в
   `lekciya-2.html` переписаны на локальные (`illustracii/<имя>/video.mp4`); проверено ИЗОЛИРОВАННОЙ
   копией без `teorkat-vvedenie/` рядом (все 4 видео грузятся, `readyState=4`, без ошибок) и
   переопубликовано (`f66ab48`). Живая витрина сейчас корректна. Урок фабрике: `deck.py`/`slaid.py`
   вычисляют относительный путь к видео от РЕАЛЬНОГО расположения файла в лекции — это верно для
   `dist/` (сосед `illustracii/` внутри той же лекции), но ЛОЖНО для `sayt/`, у которой другая
   топология. Механизм неявно предполагает «путь публикации совпадает с топологией лекции», и это
   предположение не названо нигде в коде. Повторится на КАЖДОЙ следующей лекции с медиа-слайдами,
   которую понадобится опубликовать на витрину.
   ДОМ: _studio/zhurnal/2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

3. Развилка М3 «как деплоить дек с медиа» — не решаю сам по прямому указанию захода, вопрос
   владельцу. Сейчас у механизма ДВЕ разные схемы копирования, обе рабочие, но неунифицированные:
   (а) для `dist/` (обычная пересборка) — НИЧЕГО не копируется, ссылка идёт на реальный файл в
   `illustracii/` лекции, дек перестаёт быть «одним файлом» и требует taскать папку `illustracii/`
   рядом с `dist/index.html`; (б) для `sayt/` (публикация) — видео физически ДУБЛИРУЕТСЯ в
   `sayt/static/teorkat/illustracii/` (я сделал это руками для М5, разово). Вопрос: нужен ли этот
   дубль СИСТЕМНО (встроить копирование в сам `opublikovat`/`deck.py`, чтобы будущие заходы не
   наступали на ту же находку №2), или дубль медиа в `sayt/` — сознательно временное решение до
   другой схемы деплоя (CDN, Git LFS, внешний хостинг видео)? Вес одной лекции с 4 видео — 7.9 МБ;
   при росте числа видео-лекций `sayt/` (и вся история git) начнёт расти пропорционально.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. `teorkat-vvedenie/L2/brief.md` — файл вне списка «ЗОНА (можно менять)» в контракте этого захода,
   но М4 прямым текстом требует «Порядок слайдов деки берётся из `teorkat-vvedenie/L2/brief.md`,
   ключ `slide_order` — правишь именно его». Без правки этого файла М4 физически невыполним (новые
   слайды не попадут в дек). Считаю это дефектом контракта зоны, не своей ошибкой: список ЗОНА не
   успел за конкретным заданием того же документа. Отредактировал `brief.md` (только `slide_order`,
   4 строки), закоммитил вместе с `teorkat-vvedenie/L2/illustracii/`+`slajdy/` группой — назвал здесь
   прямо, не протащил молча.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. Классификация `tip_slaida: Т6` для чисто медийных слайдов (М4) — расширение смысла Т6 сверх
   документированного в `formaty.py` («служебный: обложка/визитка/финал/разделитель — состав задаёт
   геометрия, не блоки») на пятый случай — медиа-слайд без текста. Компиляцию (`deck.py`/`slaid.py`)
   это не блокирует (поле не проверяется движком, только гейтом `gejt_kartochki.py`, который я не
   запускал — вне критерия готовности этого захода). Если аналитик сочтёт, что нужен отдельный тип
   (не Т6) — переклассифицировать четыре карточки дёшево, я решение не форсирую.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## УРОКИ ФАБРИКЕ — заполнено

Пусто — находка про фабрику (worktree не наследует untracked-подготовку) оформлена пунктом очереди
в `## ВОПРОСЫ` с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, как требует шапка этого раздела.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged <основная>     # невлитые
git --no-optional-locks status --porcelain | wc -l        # не закоммичено
git --no-optional-locks log --oneline @{u}.. | wc -l      # не вывезено
python3 _generator/tools/git_zona.py zayavki              # открытые заявки
```
branch --no-merged arka/mat-kostyak → zahod/sloi-i-obrez, zahod/vid-blokov-vnedrenie (2; на снимке
сборки 2026-08-15 было 0 — число устарело между сборкой и прогоном, заход это предупреждал заранее)
status --porcelain | wc -l → 18
log --oneline @{u}.. | wc -l → 0
zayavki → открытых: 1, переадресовано: 4, постоянных исключений: 1, сторож краснеет на 1

**ЧТО СДЕЛАНО** *(субагентом гит-контура, главная папка — 6 строк его отчёта дословно)*
1. заявок было 1, закрыто 0, застряло 0 — заявка `2026-08-15T1920-1-origin-2026-08-15-22` переадресована (`zayavka-peredat`) заходу-разработчику: П1 (`vyvezti --yes`) уже исполнен, П2 (публикация деки курса в чужой репозиторий) структурно вне мандата и возможностей `git_zona.py opublikovat`
2. влито в основную 0 веток из 0 названных; вливать нечего — аналитик не назвал ни одной ветки для влития; сверх снимка сборки на проверке найдены 2 невлитые zahod-ветки (`zahod/sloi-i-obrez`, `zahod/vid-blokov-vnedrenie`), не поименованы для влития — не трогал, доложил находкой
3. хвост Cowork: забирать пунктом 0 было нечего — `KARTA.md`, `INCIDENTY.md`, `VERDIKTY.md`, `UROKI-FABRIKE.md` чисты; вне git остались 22 пути содержательной работы (инструменты и документы чужих арок, 4 папки медиа в `teorkat-vvedenie/L2/illustracii/` — их забрал этот заход, `ucheniki/maksim/...`) — каждому заходу коммитить свою часть самому
4. погашено 0 веток/папок; ничего не гасил — гасить сверх пункта 0/очереди не поручалось, чужие ветки и worktree не трогал
5. необратимого не было
6. выдано 6 позиций из 6 найденных

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** нет
*(со списком: (а) 2 невлитые zahod-ветки — не мои права судить о влитии в основную, доложены
пунктом 2 выше; (б) заявка `...origin-2026-08-15-22`/П2 — публикация деки курса в ЧУЖОЙ репозиторий,
вне мандата гит-контура и вне зоны этого захода тоже, переадресована заходу-разработчику.)*

## ОТЧЁТ — (заполняет исполнитель)

**Что сделано и зачем.** М1/М4½ закрыты БЕЗ нового кода вёрстки: обработчик `polnyj_ekran`
(`_generator/sborka/tipy.py`) уже даёт узкую полосу фона по краям и медиа во весь остаток без
заголовка и текста — ровно формат, описанный владельцем 16.08. Расширил только загрузчик
иллюстраций (`slaid.py:load_illustrations`) третьей веткой для `video.mp4` (М2 — тот же механизм
папка+`zakaz.md`+ссылка `illustracii:`, что и у svg, ничего не изобретал), добавил CSS-правило
`.panel > video` (`base.css`) и seek-на-кадр в `engine.js` (иначе панель до нажатия play — чёрный
прямоугольник и на кадрах, и у слушателя). Видео встраивается ОТНОСИТЕЛЬНЫМ путём к файлу рядом
(М3), органы управления — нативные `controls`, без autoplay/loop (видео длинные, 47–77 с, лектор их
комментирует — решение владельца назвать вслух выполнено). Все 4 готовых видео (М4) поставлены
карточками `polnyj_ekran` сразу за названными слайдами `slide_order`. Дека выложена на витрину (М5);
по дороге поймал и починил реальный слом self-containment на живой публикации (см. `## ВОПРОСЫ` №2).

**Как проверил.**
- М2 фактом: карточка `teorkat-vvedenie/L2/slajdy/kompoziciya-strelok/slaid.md:14` несёт
  `illustracii: [kompoziciya-strelok]`, та же строка читается `_generator/sborka/slaid.py:125`
  (`params["illustracii"]`) — тем же кодом, что у обычных svg-иллюстраций.
- Регрессия: `deck.py teorkat-vvedenie/L2 -o /tmp/L2-proverka-DO.html` ДО работы → rc=0, слайдов 26.
  После всех правок (тем же `-o teorkat-vvedenie/L2/dist/index.html`) → rc=0, слайдов **30** (26+4).
- М4/М4½ кадром: 4 `.png`, путь от корня — `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/
  {kompoziciya-strelok,funktor-sohranyaet-kompoziciyu,zamena-bazisa-estestvennost,inyekciya-i-sechenie}.png`.
  Геометрия на кадрах: узкая зелёная полоса по всем четырём краям, видео в центре, заголовка и
  текста нет. Зелёный фон видео (пипетка по кадру, PIL) — RGB(198,210,192) против токена `--board`
  RGB(199,210,193): расхождение 1/255 по двум каналам — швов на кадрах не видно, подложка не
  понадобилась.
- М3+М5: `git_zona.py opublikovat --yes` дважды (первый раз — с дефектом, второй — с починкой,
  см. `## ВОПРОСЫ` №2); финальная проверка — прямой `git ls-tree`/`git show` по `origin/main` (не по
  своей рабочей копии): `static/teorkat/lekciya-2.html` и 4 `static/teorkat/illustracii/*/video.mp4`
  на месте, `<video src="illustracii/…">` — относительный, локальный. Живьём проверено ИЗОЛИРОВАННОЙ
  копией `sayt/static/teorkat/` (скопирована в `/tmp`, БЕЗ `teorkat-vvedenie/` рядом — точная модель
  топологии витрины): все 4 видео дают `readyState=4, error=null` при открытии через `file://`.
  Кадр открытия из `sayt/` (первый прогон, ДО находки дефекта) и кадр изолированной проверки (ПОСЛЕ
  починки) — оба в `kadры/` (`sayt-kompoziciya-strelok.png`,
  `sayt-izolirovanno-kompoziciya-strelok.png`).
- Видео не тронуты: `ffprobe` по каждому из 4 — кодек h264, 1920×1080, длительности 60.186003 /
  47.538021 / 76.605013 / 59.344010 с ДО и ПОСЛЕ работы совпадают побитово (md5 всех копий —
  рабочей и в `sayt/` — идентичны оригиналам в главной папке репозитория).
- `git_zona.py check --zone` по всем 4 зонам контракта (`_generator/sborka/`, `_generator/skeleton/`,
  `teorkat-vvedenie/L2/illustracii/`, `_studio/docs/`) → ✅ у каждой; `git show --stat` по всем 5
  коммитам захода — только мои пути (Г6).

**Что НЕ трогал.** Ничего сверх зоны и задачи; три заказа-сироты в пуле иллюстраций
(`izomorfizmy-primery`, `pustoj-kvadrat`, `vizitka-celikom`) и калибровку `smeta.py` на трёх чужих
карточках (`centr-gruppy`, `dvojstvennoe-prostranstvo`, `itogi`) — не мои, назвал `--no-verify`
причиной, не правил. Манim-анимаций лекции не рисовал (прямой запрет захода).

**Развилки, решённые мной (названы в задании как свободные):**
1. Устройство типа слайда — переиспользован `polnyj_ekran`, новый тип НЕ заведён (см. `## ПЛАН`).
2. Органы управления видео — нативные `controls`, без autoplay/loop, `preload="metadata"`.

**Необратимое:** необратимого нет. (Был момент риска — `rm -rf` по пути, оказавшемуся
ОТСЛЕЖИВАЕМЫМ у git внутри рабочей папки, — исправлено тем же ходом через `git checkout --`,
данные не потеряны; упомянуто для полноты, не как случившийся урон.)

**Повторяемость находок:** обе находки в `## ВОПРОСЫ` №1 и №2 — класс НЕМЕДЛЕННОЕ, обе уже
починены в рамках этого захода (worktree донабрал файлы руками; витрина перепубликована с
исправленными путями). На следующей лекции с медиа-слайдами повторится №2 (топология `sayt/` ≠
топология `dist/`), если предложенный в вопросе №3 механизм не будет встроен системно.

**Результат верификатора:** отдельный верификатор не запускался — заход прямо разрешил это (§3,
заход не двигает и не жмёт существующее). Ловушка, которую §3 называет прямо (подыграть
собственному замыслу), проверена кадром: геометрия и цвет видны на диске, не на моих словах.

**Время прогона + токены:** канал `app` — неприменимо, снимать неоткуда (см. форму отчёта §5).

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/slajdy-media/teorkat-vvedenie/L2/dist/index.html` — открывать браузером (Chrome/Safari), листать стрелками. Живая витрина: `https://d1-d57.github.io/materials/teorkat/lekciya-2.html` — ПРОВЕРЕНО прямым HTTP-запросом (`curl`) после деплоя: страница 200, все 4 `<video src="illustracii/…">` на месте, `content-length` видео `kompoziciya-strelok` = 1425090 байт (совпадает байт-в-байт с оригиналом). Путь без `/static/` — Hugo копирует `static/` в корень публикации (`baseURL` из `hugo.yaml`), не сохраняя сам сегмент `static/`; моя первая догадка об адресе (со `/static/`) была неверна, поймано и переправлено этим же ходом.

**КОММИТ:** 5 коммитов на ветке `zahod/slajdy-media`, все проверены `git_zona.py check --zone` → ✅ и `git show --stat` (только свои пути):
- `fb1ad31` — teorkat-vvedenie/L2: 4 медиа-слайда вставлены в slide_order, дека пересобрана (зона `teorkat-vvedenie`, `--no-verify`: чужие сироты пула Z3)
- `ce65f15` — _generator: поддержка видео в панели иллюстрации (зона `_generator`, `--no-verify`: чужая калибровка smeta.py, ловушка 38)
- `793e992` — sayt: lekciya-2.html выложена на витрину (зона `sayt`, чисто)
- `fe1ae5a` — sayt: медиа скопировано рядом с lekciya-2.html — починка найденного дефекта публикации (зона `sayt`, чисто)
- витрина `main` вывезена дважды: `bbaab3dc9682` (дефектная), `f66ab48fa3ae` (финальная, зелёный прогон)
