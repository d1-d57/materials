# Канал исполнителя — vlitie-i-vitrina-kursa (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — влитие, сборка и публикация расписаны по шагам; единственное содержательное место — формулировка строки на чужой странице курса, и она дана дословно.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — влитие и публикация по шагам; см. шапку захода.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/vlitie-i-vitrina-kursa.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-vitrina-kursa.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add vlitie-i-vitrina-kursa --branch zahod/vlitie-i-vitrina-kursa, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-16, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-10_dizajn-i-metriki`
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/beautiful-vigilant-euler/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекция `teorkat-vvedenie/L2` («Функторы») — HTML-дек, вторая лекция онлайн-курса теории категорий. За три захода в неё встроены четыре видео, починены дефекты вида и правится плеер. ЦЕЛЬ: свести всё это в основную ветку, выложить и поставить ссылку там, где курс смотрят студенты.

🔴 **ПОЧЕМУ ЭТОТ ЗАХОД СУЩЕСТВУЕТ.** Работа трёх заходов принята, но пока лежит по своим веткам и потому не видна нигде: влитие разрешено и выполняется ЗДЕСЬ, пунктом П1 (см. §0.1 и §2 — это и есть задача захода, а не побочное действие), живая титульная витрины до сих пор без лекции 2 (проверено фетчем страницы 2026-08-16, не предположено). Владелец сказал дословно: «я нигде это не увидел, хотя мы давали вроде задачу задеплоить». Второй раз подряд сделанная работа до него не доехала — поэтому здесь весь смысл в ДОСТАВКЕ, а не в новом коде.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-16) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. доступ к чужому репозиторию курса formal-labs у исполнителя ЕСТЬ — владелец подтвердил 2026-08-16, прежний отказ контура неверен
2. на странице онлайн-курса теории категорий ставится ссылка на слайды лекции 2 под лекцией 1
3. артефакт предъявляется ссылками, которые владелец открывает в браузере, а не путями в рабочей папке
4. ветки slajdy-media и dochistka-pleera-i-vitriny параллельны, вливать обе: в первой дека с видео, во второй правка плеера

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/vlitie-i-vitrina-kursa`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/vlitie-i-vitrina-kursa`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add vlitie-i-vitrina-kursa --branch zahod/vlitie-i-vitrina-kursa
  cd ../materials-wt/vlitie-i-vitrina-kursa
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `teorkat-vvedenie/L2/dist/` `sayt/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --vlit zahod/slajdy-media --vlit zahod/dochistka-pleera-i-vitriny --zone teorkat-vvedenie/L2/dist/ --zone sayt/ --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/ --kommitit 'пересобранная дека и опубликованная витрина' --zakryt 'влитые ветки погасить после проверки; третью ветку из --no-merged не трогать — чужая'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

Что влить, названо аналитиком и уехало в задание субагента (2): `zahod/slajdy-media`, `zahod/dochistka-pleera-i-vitriny`. Влитие идёт в ОСНОВНУЮ ветку, не в твою.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/vlitie-i-vitrina-kursa`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `teorkat-vvedenie/L2/dist/` `sayt/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, поимённо:
  - `sayt/data/spisok.yaml` — целиком, включая шапку: это единственный файл, правкой которого материал появляется на титульной
  - `_studio/docs/kak-delat/GIT-disciplina.md` — разделы `vlit-v-osnovnuyu`, `opublikovat`
- Проект сверх этого не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### П1. Влить ДВЕ ветки — они параллельны, не цепочка
`zahod/slajdy-media` (дека с четырьмя видео, `slide_order` 27 записей) и `zahod/dochistka-pleera-i-vitriny` (кнопка play вместо чёрной панели, лекция 2 в `spisok.yaml`, семь кадров). Проверено при сборке: вторая НЕ содержит первую, `merge-base --is-ancestor` даёт ложь. Числа пересними сам — `git branch --no-merged arka/mat-kostyak`.
🔴 **Конфликт ожидается в двух местах, и он не признак поломки:** обе ветки трогали `teorkat-vvedenie/L2/dist/index.html` (собранный файл) и обе правили `_generator/skeleton/`. Собранную деку разрешай ЛЮБОЙ стороной и пересобирай заново (П2) — её содержимое всё равно порождается. Правки скелета разрешай ОБЕ: они про разное (слой узора и органы управления видео). Маркеры конфликта после влития ищи грепом по влитым файлам, а не предполагай, что их нет.
**Ограничитель:** третья ветка, которую покажет `--no-merged`, к твоей задаче отношения не имеет — она чужая; её имя есть в §0.1, вливать и гасить её ЗАПРЕЩЕНО.

### П2. Пересобрать деку и убедиться, что видео и кнопка сошлись
После влития собери: `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html`.
**Проверь фактом, что обе правки живы одновременно** — это и есть смысл влития: в деке 30 слайдов, на медиа-слайде играет видео И стоит кнопка play из темы, а не чёрная панель браузера. **Кадр обязателен**, в `kadry/`.

### П3. Опубликовать витрину
`python3 _generator/tools/git_zona.py opublikovat --yes`. Уехать должны и дека, и папка с медиа рядом.
**Проверяется НА ЖИВОЙ СТРАНИЦЕ, а не в файле:** открой `https://d1-d57.github.io/materials/` и убедись, что в списке есть «Введение в теорию категорий. Лекция 2 / Функторы», а у лекции 1 описание «Комбинаторные виды». Затем открой саму деку по ссылке из списка и проверь, что видео играет. Публикация Pages идёт с задержкой — если страница ещё старая, подожди и проверь ещё раз, назвав в отчёте, сколько ждал.

### П4. Поставить ссылку на странице онлайн-курса
🔴 **Доступ у тебя ЕСТЬ — поправка владельца, сказанная при приёмке прошлого захода, дословно: «исполнителю доступен formal-labs из моего гитхаба, они дали доступ, разрешили это делать... там нет проблемы с доступом».** Прежний отказ гит-контура «репозиторий недоступен» признан неверным. Упрёшься в реальный отказ прав — назови команду и текст ошибки, а не общее «нет доступа».
**Что сделать:** на странице онлайн-курса теории категорий, где сейчас стоит лекция 1, добавить под ней лекцию 2 «Функторы» со ссылкой на слайды — `https://d1-d57.github.io/materials/teorkat/lekciya-2.html`. Формат записи возьми у соседней лекции 1 и повтори его, ничего не изобретая.
**Ограничитель:** это ЧУЖОЙ репозиторий. Меняешь ровно одну вещь — добавляешь строку про лекцию 2. Ничего больше там не трогай, даже если видишь, что поправил бы.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).**

1. **П1:** `git branch --no-merged arka/mat-kostyak` не содержит ни `slajdy-media`, ни `dochistka-pleera-i-vitriny` (вывод в отчёт). Маркеров конфликта во влитых файлах ноль — грепом, вывод приложить.
2. **П2:** дека собрана, слайдов 30, и **кадр медиа-слайда показывает видео вместе с кнопкой play в палитре темы**. Чёрной панели браузера на кадре нет.
3. **П3 — на живой странице:** титульная `https://d1-d57.github.io/materials/` содержит лекцию 2 и описание «Комбинаторные виды» у лекции 1. Дека по ссылке открывается, видео играет. Оба факта — с адресами в отчёте.
4. **П4:** ссылка на слайды лекции 2 стоит на странице курса; в отчёте — АДРЕС этой страницы, чтобы владелец открыл и посмотрел. Не вышло — назови команду и точный текст ошибки.

🔴 **ВСЁ ВЫШЕ ПРОВЕРЯЕТСЯ ЖИВЫМ ПРОГОНОМ НА РЕАЛЬНОМ ОБЪЕКТЕ** — на живых ветках, живой деке и живых страницах в интернете, а не на фикстуре и не по содержимому файлов.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «всё влито», а «невлитых 0 из N проверенных».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Отдельный субагент-верификатор НЕ нужен, но слияние — ЛОССОВАЯ операция, и одна проверка обязательна.** После влития убедись фактом, что НИ ОДНА из двух работ не потерялась: в деке 30 слайдов (пришло из `slajdy-media`) И кнопка play вместо чёрной панели (пришло из `dochistka`). Обе разом — на одном кадре. Пропала любая — влитие сделано не той стороной, и это надо чинить, а не описывать.
*В этой арке эталон дважды снимали с ветки, не являющейся предком, и получали мнимую регрессию — не повтори.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teorkat-vvedenie/L2/dist/ sayt/ _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teorkat-vvedenie/L2/dist/ sayt/ _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `teorkat-vvedenie/L2/dist/` `sayt/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/L2/dist/` → ✅; `python3 _generator/tools/git_zona.py check --zone sayt/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Субагент гит-контура вышел за права: сам пересобрал деку и опубликовал витрину
ЦЕНА: печатное задание `bootstrap_zahod.py --zadanie-subagentu` в пункте 3 несёт строку «- пересобранная дека и опубликованная витрина» как элемент списка «что ещё забрать в git по ходу … НЕ коммить, доложи списком заходу». Формулировка двусмысленна: первый прогон субагента (дословно то же задание) корректно НЕ тронул П2/П3 и доложил находку списком; второй прогон (то же задание, отличие только в расширенном `--zone`) исполнил П2 (пересборка `teorkat-vvedenie/L2/dist/index.html`, коммит) и П3 (`opublikovat --yes`) сам — это была работа заходa, не его. Разное поведение двух прогонов на идентичном тексте подтверждает двусмысленность формулировки, а не сбой одного из прогонов. Не будь независимой проверки на живой странице (правило §3 «проверяется на живой странице, а не в файле»), опубликованная витрина унесла бы РЕГРЕССИЮ: вторая копия дека `sayt/static/teorkat/lekciya-2.html` осталась НЕ пересобранной этим прогоном, и видео на живой странице играло бы с чёрной панелью браузера вместо тематизированной кнопки — ровно тот дефект, ради починки которого существует весь заход.

### `sayt/static/teorkat/lekciya-2.html` — вторая копия дека без своей команды пересборки
ЦЕНА: `teorkat-vvedenie/L2/dist/index.html` и `sayt/static/teorkat/lekciya-2.html` — два раздельных сборочных артефакта одной лекции с РАЗНЫМИ относительными путями к видео (`../illustracii/...` у первого; локальная копия `illustracii/...` внутри `sayt/static/teorkat/illustracii/` у второго — файлы туда клала ветка `zahod/slajdy-media` вручную). Наивная пересборка `deck.py teorkat-vvedenie/L2 -o sayt/static/teorkat/lekciya-2.html` вычисляет путь к видео НАРУЖУ из `sayt/` (`../../../teorkat-vvedenie/L2/illustracii/...`) — такая ссылка не публикуется (`opublikovat` архивирует только `sayt/`) и была бы битой на витрине. Пришлось точечно чинить 4 строки после каждой пересборки. В `_generator/` нет команды/флага, которая корректно (пере)собирает именно сайтовую копию с локальными путями — при следующей правке лекции 2 (или любой другой лекции с видео и сайтовой копией) тот же цикл «пересобрать → сломать пути → чинить руками» повторится.

### `--zone` в печатном задании `bootstrap_zahod.py --zadanie-subagentu` не сверяется с реальным дифом ветки
ЦЕНА: список `--zone` команды §0.1 (`dist/`+`sayt/`+`kadry/`) оказался у́же полного следа `zahod/slajdy-media` — диф ветки 22 пути, 15 вне зоны (`_generator/sborka/deck.py`, `slaid.py`, `brief.md`, `illustracii/*`, `slajdy/*.md` — то есть само содержание ветки, не постороннее). Первый прогон субагента отказал в обоих влитиях; второй, скорректированный вручную исполнителем (сверка `git diff --name-only <основная>...<ветка>` + побайтовая проверка теневых файлов через `git hash-object`), прошёл. `bootstrap_zahod.py` печатает `--zone` тем текстом, который вписал аналитик, не сверяя его с реальным дифом ветки на этапе сборки захода — простая проверка (`git diff --name-only`) поймала бы несовпадение до отправки субагенту и избавила бы от лишнего прогона.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Порядок §0.1 соблюдён с поправкой.** Стартовое сообщение велело завести рабочую папку первым ходом; сам заход (§0.1) требует обратного — сперва субагент вливает в основную, потом заводится рабочая папка. Успел только `worktree add` без единого коммита внутри — закрыл его чисто через `zakryt-vetku` (потерь 0, надгробие `mogila/zahod/vlitie-i-vitrina-kursa`) и пошёл по порядку из самого захода.

**Первый прогон субагента гит-контура (задание — дословно из `bootstrap_zahod.py`) обе ветки НЕ влил.** Не обычный конфликт слияния — структурные блокеры `vlit-v-osnovnuyu --zone`:
- `zahod/dochistka-pleera-i-vitriny`: зона (dist/+sayt/+kadry/+skeleton) подошла, но в главной папке 6 untracked PNG в `kadry/` пересекались со сливаемыми путями («грязь»). Проверил фактом: `git hash-object <файл>` и `git rev-parse zahod/dochistka-pleera-i-vitriny:<путь>` дали одинаковые хэши по всем шести — это осиротевшая тень, побайтово совпадающая с тем, что уже закоммичено на самой ветке, не чья-то непринятая работа.
- `zahod/slajdy-media`: полный диф веткиː 22 пути; 15 вне зоны — `_generator/sborka/deck.py`, `_generator/sborka/slaid.py` (код поддержки видео в генераторе), `teorkat-vvedenie/L2/brief.md`, четыре `illustracii/*/video.mp4`+`zakaz.md`, четыре `slajdy/*/slaid.md`. Это не постороннее и не «заодно» — это и есть содержание ветки (видео на слайдах), просто зона в команде §0.1 оказалась у́же реального следа ветки. Криво не сама задача, а список `--zone`, с которым эта задача была передана субагенту.

**Оспариваю предпосылку:** список `--zone` в команде §0.1 занижен относительно реального дифа `zahod/slajdy-media`. Правка: зону для повторного влития этой ветки нужно расширить путями её собственного следа (`_generator/sborka/`, `_generator/skeleton/`, `teorkat-vvedenie/L2/` целиком — источник слайдов и иллюстраций лежит там же, где `dist/`), а не только моей зоной исполнителя (dist/+sayt/+kadry/). Это не расширение МОЕЙ зоны редактирования — я по-прежнему не трогаю эти файлы руками; расширяется зона ДОПУСТИМОГО ДИФА для уже просуженной аналитиком ветки, которую вливает субагент.

**Что делаю дальше:**
1. Удаляю 6 файлов-теней в главной папке (`kadry/`) — обоснование выше, потеря нулевая (идентичны закоммиченному на ветке). Строка пойдёт в НЕОБРАТИМОЕ отчёта.
2. Ровно ОДИН повторный корректирующий прогон того же субагента гит-контура (не параллельный/слепой ретрай, а починка найденного узкого места) с расширенным `--zone` для `zahod/slajdy-media`. `zahod/dochistka-pleera-i-vitriny` после удаления теней должна пройти уже с исходной зоной.
3. Дальше — по заходу: создаю рабочую папку (уже от обновлённой основной), читаю два названных файла, П1–П4.

## ВОПРОСЫ — (заполняет исполнитель)

Вопросов владельцу нет — все находки по ходу захода решены в рамках прав исполнителя (см. `## ПЛАН`, `## ОТЧЁТ`); закономерности фабрики — в `## УРОКИ ФАБРИКЕ` напрямую, форматом гейта.

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД; печатное задание `bootstrap_zahod.py --zadanie-subagentu` не запросило этот снимок явно — субагент 1-го прогона его не снимал. Реконструирован исполнителем по факту: точка входа зафиксирована как коммит `5f5c767` ДО запуска субагента (см. `## ПЛАН`), это и есть база для «невлитых на входе».)*
```
$ git --no-optional-locks branch --no-merged 5f5c767
* arka/mat-kostyak
+ zahod/vid-blokov-vnedrenie
+ zahod/vlitie-i-vitrina-kursa
```
Показывает только СУЩЕСТВУЮЩИЕ СЕЙЧАС ветки — `zahod/slajdy-media` и `zahod/dochistka-pleera-i-vitriny` в списке нет, потому что обе уже влиты и погашены (см. «ЧТО СДЕЛАНО»); на входе они были невлитыми — это и есть весь смысл захода, зафиксировано текстом самого захода (§0.1/П1) и первым прогоном субагента.
```
$ python3 _generator/tools/git_zona.py zayavki   (снято 1-м прогоном субагента)
заявок открыто 0, переадресовано 6, постоянных исключений 1, сторож краснеет на 0
```
На входе была 1 актуальная заявка не своей темы (`2026-08-16T0141-generator-tools-bootstrap-zahod-py-nevlityje`, род `pravka-koda`) — переадресована 1-м прогоном в `na-zahod/`, не входит в мандат гит-контура.
`status --porcelain | wc -l` ДО работы отдельно не снят (та же причина — не было в печатном задании); косвенно: 1-й прогон субагента нашёл в главной папке 6 untracked PNG-теней в `kadry/`, пересекавшихся со сливаемым, — это и была видимая грязь на входе, снята исполнителем (см. `## ОТЧЁТ`, необратимое).

**ЧТО СДЕЛАНО** *(с хэшами, два прогона субагента гит-контура)*
- хвост Cowork забран шестью коммитами по ходу: `bc2f256`, `4ae4ff9`, `7155a22`, `d1fab21`, `2c42871` (`_studio/docs/KARTA.md`, `INCIDENTY.md`, `UROKI-FABRIKE.md`, выгрузки арок)
- влито в `arka/mat-kostyak`: `zahod/slajdy-media` → merge `088fd70`; `zahod/dochistka-pleera-i-vitriny` → merge `419220e`
- дека пересобрана и закоммичена: `445406a` (30 слайдов, гейт вмещения зелёный) — это пересборка П2, субагент сделал её сам во 2-м прогоне (см. `## УРОКИ ФАБРИКЕ` — не входило в его мандат, найдено и зафиксировано)
- витрина опубликована: `opublikovat --yes` → `main d7fc3fccd7e0`, Actions зелёный (проверено исполнителем независимо, см. `## ОТЧЁТ` П3)
- погашено: `zahod/slajdy-media` (`mogila/zahod/slajdy-media` → `fe1ae5a2`), `zahod/dochistka-pleera-i-vitriny` (`mogila/zahod/dochistka-pleera-i-vitriny` → `5e9fd961`)
- НЕ тронуто: `zahod/vid-blokov-vnedrenie` (третья, чужая, из `--no-merged`)
- заявка `2026-08-16T0141-generator-tools-bootstrap-zahod-py-nevlityje` переадресована, не закрыта (не тема гит-контура)

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `да`
*(обе названные ветки влиты и погашены, третья по имени и по праву не трогается; единственная заявка на входе — переадресована по своей теме, не является долгом гит-контура. Остаток вне git — 4 пути, названы субагентом строкой 3 второго прогона: `_generator/tools/sdelat_handoff.py`, `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_faza-priyomki.md`, `ucheniki/maksim/ZHURNAL.md`, `ucheniki/maksim/dz/dz-09-raskraska-invariant.html` — чужая содержательная работа, не мой мандат, не долг гит-контура.)*

## ОТЧЁТ — (заполняет исполнитель)

**Что сделано + зачем.** П1: две названные ветки влиты в основную (`arka/mat-kostyak`) — это и было целью захода («работа принята, но лежит по своим веткам и потому не видна нигде»). П2: дека пересобрана, кадр видео+кнопки снят и подтверждён живым прогоном. П3: витрина опубликована и перепроверена на реальном URL. П4: ссылка на лекцию 2 стоит на живой странице курса formal-labs. По ходу нашёл и исправил три легитимных блокера (узкая зона слияния, тень-дубликаты, несинхронная вторая копия дека) и одно превышение прав субагентом — все зафиксированы фактом, не пересказом, в `## ПЛАН` и `## УРОКИ ФАБРИКЕ`.

**Как проверил.** Каждый пункт — живым прогоном на реальном объекте, не по содержимому файла: `git branch --no-merged` фактом; сборка деки локально в headless-браузере (Playwright) с реальным кликом по кнопке и проверкой `video.paused===false`; тот же приём — на живой `https://d1-d57.github.io/materials/teorkat/lekciya-2.html` (уже после публикации, с принудительной перезагрузкой без кеша); текст живой титульной страницы `https://d1-d57.github.io/materials/`; текст живой страницы `https://formal-labs.github.io/categorytheory/` и `href` ссылки «Слайды» у лекции 2.

**Что НЕ трогал.** Всё вне зоны (`teorkat-vvedenie/L2/slajdy/`, `illustracii/` с исходниками, `_generator/sborka/`, `_generator/skeleton/` — их правили влитые ветки, не я). В `formal-labs/formal-labs.github.io` — ровно одна вставка (5 строк, показано diff'ом в `## ПЛАН`/ниже), ничего в остальном файле и репозитории.

**Результат верификатора (§3, лоссовая операция).** На ОДНОМ кадре одновременно: 30 слайдов (пришло из `slajdy-media`) И тематизированная кнопка play/pause вместо чёрной панели (пришло из `dochistka-pleera-i-vitriny`) — `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/2026-08-16_vlitie-i-vitrina-kursa_video-i-knopka-play.png`. Ни одна из двух работ не потеряна.

**Время прогона + токены:** на канале `app` неприменимо, снимать неоткуда (см. шапку раздела `## ОТЧЁТ` в каноне).

**ПОВТОРЯЕМОСТЬ находок.** Повторится на следующей единице работы (правка/пересборка любой лекции с видео, есть сайтовая копия) — заход ДО следующего прогона, не пункт очереди: несинхронная вторая копия дека `sayt/static/<...>.html` без своей команды пересборки (см. `## УРОКИ ФАБРИКЕ`, второй пункт) — это дом-класс «фабрика», не «эта лекция». Не повторится (одноразовые находки этой конкретной сессии): узкая `--zone` именно для `zahod/slajdy-media`, тени именно в `kadry/` — обе ветки уже влиты и погашены, тем же путём заходить больше некому.

---

### П1
```
$ git --no-optional-locks branch --no-merged arka/mat-kostyak
+ zahod/vid-blokov-vnedrenie
+ zahod/vlitie-i-vitrina-kursa
```
Ни `zahod/slajdy-media`, ни `zahod/dochistka-pleera-i-vitriny` в списке нет — обе влиты, погашены (`mogila/zahod/slajdy-media`→`fe1ae5a2`, `mogila/zahod/dochistka-pleera-i-vitriny`→`5e9fd961`). `zahod/vid-blokov-vnedrenie` — чужая, не тронута. `zahod/vlitie-i-vitrina-kursa` — своя рабочая, невлита по контракту (вливает приёмка). Маркеры конфликта: `grep -rl '^<<<<<<<\|^=======$\|^>>>>>>>' teorkat-vvedenie/L2/ sayt/` → пусто. **Охват: проверено 2 из 2 названных, невлитых 0 из 2.**

### П2
Дека собрана: `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html` → 30 слайдов, гейт вмещения зелёный (сделано субагентом во 2-м прогоне, коммит `445406a`; см. `## УРОКИ ФАБРИКЕ` — вышло за его права, но результат проверен и годен). Кадр — см. «Результат верификатора» выше. **Охват: проверен 1 из 1 медиа-слайд (`kompoziciya-strelok`), на нём — все 4 внедрённых видео используют тот же `.video-ctl` (проверено грепом шаблонов, не открывал остальные три поштучно).**

### П3
Титульная `https://d1-d57.github.io/materials/` (снято `get_page_text`, живой прогон): содержит «Введение в теорию категорий. Лекция 2 / Функторы»; у лекции 1 — «Комбинаторные виды». Дека по ссылке `https://d1-d57.github.io/materials/teorkat/lekciya-2.html` открывается, видео играет (`paused:false, readyState:4`, `src` — реальный URL живого CDN, клик по `.video-ctl` — реальный, не эмулированный «play()»). Публикация Pages ждал один цикл: первая публикация (сделанная субагентом-нарушителем) была РЕГРЕССИЕЙ — `sayt/static/teorkat/lekciya-2.html` содержал СТАРЫЙ движок (0 вхождений `video-ctl`, нативный `controls`); нашёл проверкой живой страницы, пересобрал сайтовую копию, поправил пути (см. `## ПЛАН`/`## УРОКИ ФАБРИКЕ`), закоммитил (`f448c67`), опубликовал повторно (`opublikovat --yes` → `main 0105e10d72b0`, Actions зелёный: https://github.com/d1-d57/materials/actions/runs/31914751988), перепроверил живую страницу с принудительной перезагрузкой — зелёно. **Охват: проверено 2 из 2 живых факта (титульная + дека), оба подтверждены после исправления.**

### П4
Доступ был — без отказов прав, конфликт не возникал. Правка внесена PR'ом (не голым push — «чужой репозиторий», конвенция дома `материалы лекций — блоком через PR»), см. diff в коммите `1b358f2` (5 строк), слита `gh pr merge` (по явному разрешению владельца, процитированному в захода) — https://github.com/formal-labs/formal-labs.github.io/pull/2 (`MERGED`). Живая страница: **https://formal-labs.github.io/categorytheory/** — «Лекция 2 / Функторы / Слайды», `href` ссылки → `https://d1-d57.github.io/materials/teorkat/lekciya-2.html` (проверено `.href`, не текстом). Формат повторяет лекцию 1 (`## Лекция N`, жирная строка темы, список ссылок) без pdf/видео — их для лекции 2 не существует, не изобретал. **Охват: проверено 1 из 1 (единственная требуемая строка), больше в файле ничего не менялось (diff --stat = 1 файл, 5 insertions).**

---

**НЕОБРАТИМОЕ:**
1. Удалены 6 файлов-теней в `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` (главная папка, ДО создания рабочей папки) — восстанавливаются побайтово идентичной версией на (уже влитой) `zahod/dochistka-pleera-i-vitriny`, проверено `git hash-object` до удаления.
2. Закрыта своя же преждевременная ветка `zahod/vlitie-i-vitrina-kursa` (первое создание, до порядка §0.1) — потерь 0 (ни одного коммита сверх точки ветвления), восстанавливается `git_zona.py voskresit --branch zahod/vlitie-i-vitrina-kursa` → `mogila/zahod/vlitie-i-vitrina-kursa`→`5f5c7670`.
3. В чужом репозитории `formal-labs/formal-labs.github.io` смёржен PR #2 в `main` (см. П4) — восстанавливается `git revert` коммита слияния владельцами репозитория при необходимости; изменение обратимо штатным способом, не форс-пушем.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/vlitie-i-vitrina-kursa/teorkat-vvedenie/L2/dist/index.html` — открывать в браузере (локально; живая копия — https://d1-d57.github.io/materials/teorkat/lekciya-2.html). Кадр-доказательство: `/Users/ivanyakovlev/Documents/GitHub/materials-wt/vlitie-i-vitrina-kursa/_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/2026-08-16_vlitie-i-vitrina-kursa_video-i-knopka-play.png` — открывать любым просмотрщиком изображений.

**КОММИТ:** `f448c673ee8d89a872bd9f0d9005b4ca07eae7a4` — «sayt+kadry: пересобрана sayt/static/teorkat/lekciya-2.html (была стара — без кнопки play, с чёрной панелью controls), путь к видео поправлен на локальную копию в sayt/; кадр видео+кнопки после влития» · `git_zona.py check --zone teorkat-vvedenie/L2/dist/` → ✅ · `git_zona.py check --zone sayt/` → ✅ · `git_zona.py check --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` → ✅ · `git show --stat HEAD` — только 2 своих пути (`sayt/static/teorkat/lekciya-2.html`, кадр в `kadry/`; `dist/index.html` не менялся этим коммитом — совпадал с коммитом субагента `445406a`, отдельно проверено `git status --porcelain` = пусто).

---

### ДОБАВКА (по живой обратной связи владельца после сдачи отчёта, тот же заход)

Владелец открыл живую деку и указал три конкретных дефекта плеера видео: (1) нет полоски прогресса — непонятно, где находишься, нельзя отмотать; (2) кнопка play/pause маленькая, в углу, «вылезает»; (3) непоследовательная чёрная рамка вокруг видео-панели (нативный browser focus-outline на `<video>`/кнопке — то появляется, то нет).

🔴 **Зона расширена за пределы контракта захода** — правка живёт в `_generator/skeleton/engine.js` и `base.css` (общий движок всех дек, не только этой лекции), а не в `dist/`/`sayt/`/`kadry/`. Решение принято по прямому указанию владельца в этом же диалоге, не самовольно; зона исходного контракта («ЗОНА (можно менять): dist/, sayt/, kadry/») этим ОДНИМ коммитом расширена — называю явно, а не молчу, как требует «ЗАПРЕТ» раздела контракта.

**Сделано:** заменена одна маленькая кнопка `.video-ctl` (34px, левый нижний угол) на бар во всю ширину панели (`.video-ctl-bar`): кнопка play/pause (30px, в баре) + ползунок прогресса (`<input type=range>`, живой seek через `timeupdate`/`input`) + таймкод `м:сс / м:сс`. Добавлен явный `outline: none` на `.panel > video` и элементы бара — снимает нативную рамку фокуса браузера, которая и была источником непоследовательной подсветки (проверено: `getComputedStyle(video).outlineStyle` до фикса не проверялся отдельно, но CSS-источника рамки в кодовой базе не было вовсе — только браузерный дефолт на фокусируемом `<video>`).

**Проверено живым прогоном** (headless Playwright + Browser pane, дважды — локально и на живой странице после публикации): бар виден, play/pause переключает текст кнопки, `pointerdown`→`input`→`pointerup` на ползунке двигает `video.currentTime` (проверено: значение 500/1000 дало `currentTime=30.09` при `duration=60.19` — ровно середина), таймкод обновляется, `outline: none` подтверждён вычисленным стилем на сфокусированном `<video>`.

**Коммит:** `e21a4bae3f900640726b9c8e5fd2e795e4604cf0` — «плеер видео: ползунок прогресса + таймкод, кнопка крупнее и в общем баре во всю ширину, снят непоследовательный focus-outline на панели» — пути: `_generator/skeleton/base.css`, `_generator/skeleton/engine.js`, `teorkat-vvedenie/L2/dist/index.html` (пересобран), `sayt/static/teorkat/lekciya-2.html` (пересобран, путь к видео поправлен точечно, как раньше), кадр в `kadry/` (перезаписан новым видом бара). Пре-коммит хук прогнал `PROGNAT.sh` (путь входит в её охват, движок общий) — зелёный на ловушках 3–5, ловушки 1–2 пропущены хуком (нет `playwright` в ЕГО окружении — не путать с тем, что я использовал `playwright` отдельно, вне хука, для верификации).

**Публикация:** `opublikovat --yes` → `main 97d8c2f1b489`, Actions зелёный: https://github.com/d1-d57/materials/actions/runs/31915771899. Живая страница перепроверена с принудительной перезагрузкой: бар/ползунок/таймкод на месте, `outline: none` подтверждён вычисленным стилем.

**Необратимое:** нет (правка коммитится обычным путём, откатывается `git revert e21a4ba`).

**Открыто «возвращаться».** Три момента НЕ проверены (честно, не молчанием): (а) как бар выглядит на реальном docking/проекторе — проверен только в headless-браузере 1280×810; (б) поведение `<input type=range>` на тач-устройствах (планшет лектора) — не тестировал, нативный range обычно работает, но не подтверждено фактом; (в) остальные 3 из 4 видео на деке используют тот же код по построению (общий цикл гидратации), но кадр снят только с одного (`kompoziciya-strelok`) — не открывал остальные три поштучно после этой правки.

---

**Гигиена (Г1–Г6):**
- Г1: `check --zone` по всем трём зонам → ✅ (см. КОММИТ выше).
- Г2: применимо (зона расширилась на `formal-labs/formal-labs.github.io` по ходу П4) — `git status --porcelain` в клоне → пусто, всё закоммичено и слито.
- Г3: невлитых веток не прибавилось (было 2 «чужих для входа» — `vid-blokov-vnedrenie`, своя `vlitie-i-vitrina-kursa` — сейчас те же 2, слитые `slajdy-media`/`dochistka` из списка ушли).
- Г4: ни одного нового `.py`/`.sh` в `_generator/**` — так и пишу (диф ветки по этим путям пуст).
- Г5: ни одного нового `.md` — так и пишу (правка в `formal-labs` — foreign repo, вне системы регистрации этого дома).
- Г6: `git show --stat HEAD` — только мои пути (см. КОММИТ выше).
