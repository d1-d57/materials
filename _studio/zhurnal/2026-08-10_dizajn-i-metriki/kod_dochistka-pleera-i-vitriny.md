# Канал исполнителя — dochistka-pleera-i-vitriny (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — три правки, каждая с названным адресом и проверяемым результатом; свободного планирования нет.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — три расписанные правки; см. шапку захода.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/dochistka-pleera-i-vitriny.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_dochistka-pleera-i-vitriny.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add dochistka-pleera-i-vitriny --branch zahod/dochistka-pleera-i-vitriny, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-16, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-10_dizajn-i-metriki`
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/beautiful-vigilant-euler/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекция `teorkat-vvedenie/L2` — HTML-дек 1440×810, опубликован на витрину курса. Прошлый заход (`kod_slajdy-media.md`) встроил четыре готовых видео владельца и выложил деку; приёмка приняла работу и нашла два хвоста, оба дешёвые. ЦЕЛЬ: закрыть их и довести витрину до состояния, в котором владелец находит лекцию сам.

🔴 **ГЛАВНОЕ ТРЕБОВАНИЕ ВЛАДЕЛЬЦА К ЭТОМУ ЗАХОДУ — про ОТЧЁТ, а не про код.** Дословно 2026-08-16: «у меня ещё случается, что он не показал мне артефакт. Артефакт можешь попросить, чтобы он, когда закончил заход, показал эти артефакты — я его сам посмотрел». И про деплой: «я нигде это не увидел, хотя мы давали вроде задачу задеплоить». Оба раза работа была СДЕЛАНА — и оба раза владелец её не нашёл. Поэтому §5 этого захода требует не путь, а АДРЕС, который открывается в браузере одним кликом.

Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-16) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. нативный чёрный плеер браузера на медиа-слайде — брак вида, его надо привести к теме деки
2. кадры обязаны доехать в git: правило владельца «требование про вид закрывается кадром на влитой ветке»
3. лекция 2 не появилась в списке витрины — владелец её не нашёл, хотя файл опубликован
4. описание лекции 1 меняется: «Комбинаторные виды» вместо «Что такое комбинаторные виды»
5. исполнитель обязан предъявить артефакт ссылкой, которую владелец откроет в браузере, а не путём в рабочей папке

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/dochistka-pleera-i-vitriny`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/dochistka-pleera-i-vitriny`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add dochistka-pleera-i-vitriny --branch zahod/dochistka-pleera-i-vitriny
  cd ../materials-wt/dochistka-pleera-i-vitriny
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/skeleton/` `_generator/sborka/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` `teorkat-vvedenie/L2/dist/` `sayt/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone _generator/skeleton/ --zone _generator/sborka/ --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/ --zone teorkat-vvedenie/L2/dist/ --zone sayt/ --kommitit 'правки скелета и сборки, кадры, sayt/data/spisok.yaml, пересобранная дека' --zakryt 'свою ветку не гасить — гасит приёмка; чужие ветки и worktree не трогать'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-16, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/dochistka-pleera-i-vitriny`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/skeleton/` `_generator/sborka/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` `teorkat-vvedenie/L2/dist/` `sayt/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, поимённо:
  - `_generator/skeleton/base.css` — правило `.panel > video` (его добавил прошлый заход)
  - `_generator/skeleton/engine.js` — seek-на-кадр для видео (тоже прошлый заход)
  - `sayt/data/spisok.yaml` — ЦЕЛИКОМ, включая комментарий в шапке: он объясняет, что это единственный файл, который правят для появления материала на витрине
  - `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/kompoziciya-strelok.png` — посмотри глазами, это дефект, который чинишь в Э1
- Проект сверх этого не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Три правки. Порядок — по «заметность × дешевизна».

### Э1. Лекция 2 появляется в списке витрины
**Претензия владельца, дословно:** «я нигде это не увидел, хотя мы давали вроде задачу задеплоить... тоже странно».
**Диагноз, снятый приёмкой фактом:** дека опубликована и живёт по адресу `https://d1-d57.github.io/materials/teorkat/lekciya-2.html` — открывается, видео играют. Но список на титульной собирается НЕ из файлов, а из `sayt/data/spisok.yaml` (это написано в шапке самого файла), и записи про лекцию 2 там нет. Файл лежит, ссылки на него нет — найти лекцию можно только зная точный адрес.
**Что сделать:**
1. Добавить запись про лекцию 2 по образцу соседних трёх строк: название в ряду с лекцией 1, описание — `Функторы`, ссылка — `teorkat/lekciya-2.html`.
2. **Поправить описание лекции 1:** сейчас `Что такое комбинаторные виды`, владелец просит `Комбинаторные виды` — «я бы убрал слово „что такое“».
**Ограничитель:** порядок записей = порядок на странице, лекция 2 идёт ПОСЛЕ лекции 1. Остальные материалы не трогай.

### Э2. Кадры доезжают в git
**Диагноз:** шесть кадров прошлого захода лежат в `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` как `??` — вне git. Их убьёт `git clean`. Причина — дефект контракта прошлого захода: `kadry/` не входила в его зону. В ТВОЮ зону она входит.
**Что сделать:** забрать все шесть в git своим коммитом.
**Ограничитель:** это чужие файлы, ты их не переснимаешь и не переименовываешь — только коммитишь как есть.

### Э3. Плеер приводится к теме деки
**Диагноз, найденный приёмкой на кадре** (посмотри `kadry/kompoziciya-strelok.png` глазами ПЕРЕД работой): нативная панель `controls` рисуется чёрным прямоугольником с градиентом на всю ширину кадра — на бежево-зелёной теме это чужеродный элемент. На панели есть иконка ЗВУКА, хотя звука в роликах нет вовсе, кнопка полноэкранного режима и меню «три точки». Слайд по умолчанию стоит на паузе, поэтому панель видна ВСЕГДА, а не только при наведении.
**Что сделать:** привести органы управления к теме. Способ выбираешь ты — стилизовать нативные `::-webkit-media-controls`, либо показывать панель только при наведении, либо свести к одной кнопке play/pause в палитре темы.
🔴 **Чего делать НЕЛЬЗЯ, и это решение владельца, а не вкус:** просто убрать `controls`. Ролики длиной 47–77 секунд, лектор их комментирует — без органов управления видео нечем остановить на лекции. Убрать кнопки = сломать сценарий показа.
**Ограничитель:** `autoplay` и `loop` не вводить — они мешают лектору говорить. Само видео не трогать: файлы авторские, побайтно неприкосновенны (проверяется клаузой 4).

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).**

1. **Э1 проверяется НА ЖИВОЙ ВИТРИНЕ, а не в файле:** после публикации титульная страница `https://d1-d57.github.io/materials/` содержит строку про лекцию 2 со ссылкой, которая открывается. Описание лекции 1 — `Комбинаторные виды`. Оба факта — в отчёт вместе с адресом.
2. **Э2:** `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` → ✅, кадров вне git ноль.
3. **Э3 закрывается КАДРОМ, и никак иначе:** новый `.png` того же слайда `kompoziciya-strelok`, снятый ПОСЛЕ правки, положенный в `kadry/` и закоммиченный. На кадре видно, что панель управления не выбивается из темы. Счёт в DOM, греп по CSS и рассуждение о стилях доказательством НЕ считаются — правило владельца от 2026-08-16.
4. **Видео не тронуты:** `md5sum` каждого из четырёх `teorkat-vvedenie/L2/illustracii/*/video.mp4` совпадает с тем, что в `HEAD` до твоей работы (оба списка в отчёт).
5. **Регрессия:** дека собирается, слайдов 30 (число сними командой ДО работы и повтори ПОСЛЕ — печатает сама сборка).

🔴 **ВСЁ ВЫШЕ ПРОВЕРЯЕТСЯ ЖИВЫМ ПРОГОНОМ НА РЕАЛЬНОМ ОБЪЕКТЕ РЕПОЗИТОРИЯ** — на лекции `teorkat-vvedenie/L2` и на живой витрине, а не на фикстуре.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «кадры закоммичены», а «закоммичено X из Y».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Отдельный субагент-верификатор НЕ нужен:** операция не лоссовая, а вид закрывается кадром (клауза 3) — его судит владелец, и это надёжнее любого верификатора.
🔴 **Но одну ловушку назови себе вслух:** ты сам будешь решать, «выбивается панель из темы или нет», и ты же её и стилизуешь. Автор правки — заинтересованное лицо ровно там, где уверен. Поэтому кадр снимается и кладётся в отчёт ДАЖЕ ЕСЛИ ты уверен, что получилось хорошо.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/skeleton/ _generator/sborka/ _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/ teorkat-vvedenie/L2/dist/ sayt/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/skeleton/ _generator/sborka/ _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/ teorkat-vvedenie/L2/dist/ sayt/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/skeleton/` `_generator/sborka/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` `teorkat-vvedenie/L2/dist/` `sayt/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/skeleton/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` → ✅; `python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/L2/dist/` → ✅; `python3 _generator/tools/git_zona.py check --zone sayt/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу

🔴 **ТРЕБОВАНИЕ ВЛАДЕЛЬЦА К ЭТОМУ ЗАХОДУ ОТДЕЛЬНОЙ СТРОКОЙ: АРТЕФАКТ ПРЕДЪЯВЛЯЕТСЯ ССЫЛКОЙ, КОТОРУЮ ОН ОТКРОЕТ.**
Дословно, 2026-08-16: «у меня ещё случается, что он не показал мне артефакт. Артефакт можешь попросить, чтобы он, когда закончил заход, показал эти артефакты — я его сам посмотрел».
Значит строка `**АРТЕФАКТ:**` этого захода несёт **три адреса, каждый отдельной строкой**:
1. **URL живой витрины** — `https://d1-d57.github.io/materials/` (титульная, где владелец обязан УВИДЕТЬ строку про лекцию 2) и `https://d1-d57.github.io/materials/teorkat/lekciya-2.html` (сама дека).
2. **Путь к кадру ПОСЛЕ правки плеера** — от корня репозитория, не из рабочей папки.
3. **Путь к пересобранной деке** — от корня репозитория.

⚠ **Путь внутри `materials-wt/<твоя папка>` артефактом НЕ СЧИТАЕТСЯ.** Владелец туда не ходит: дважды за эту арку работа была сделана, а он её не нашёл — оба раза из-за адреса. До чего читатель не дотягивается, то для него и не сделано.

Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Критерии готовности захода опирались на контент неслитой ветки, недостижимый моими правами в момент прогона
ЦЕНА: заход собран 2026-08-16 с ожиданием базовой сборки в 30 слайдов и «видео играют» на живой
витрине (криterии 1 и 5, диагноз Э3). Фактически в `arka/mat-kostyak` на момент прогона было 26
слайдов, ни одного `<video>` — весь видео-функционал (4 слайда с видео, CSS/JS для controls,
публикация media-файлов, коммит `fe1ae5a`) существовал ТОЛЬКО в неслитой `zahod/slajdy-media`,
заблокированной конфликтом на `teorkat-vvedenie/L2/illustracii/*` (untracked, вне git-контура и вне
зоны этого захода). Живая витрина при этом опубликована из СОСТОЯНИЯ `slajdy-media` — то есть
git-история `arka/mat-kostyak` и живой URL разошлись, и это не видно, если не сверять по факту.
Ушло заметное время (несколько раундов `git show`/`diff` по трём веткам, живые проверки браузером
на `d1-d57.github.io`) на то, чтобы отличить «диагноз захода неверен» от «диагноз верен, база
устарела». Без этой проверки был риск ровно того сценария, который и есть ГЛАВНОЕ ТРЕБОВАНИЕ
этого захода: сдать отчёт про то, чего владелец не увидит (кадр несуществующего слайда, критерий
«30 слайдов» — не совпадёт при живой проверке).

### `arka/mat-kostyak` теперь несёт регресс живой витрины лекции 2 (не моя правка, но лежит в моей зоне)
ЦЕНА: слияние `zahod/sloi-i-obrez` (сделано git-контуром этого захода, легитимно) принесло в
`arka/mat-kostyak` файл `sayt/static/teorkat/lekciya-2.html` — 26 слайдов, без видео. Живая
витрина сейчас — 30 слайдов, 4 работающих видео (опубликована из `zahod/slajdy-media`, не из
`arka/mat-kostyak`). Если этот файл когда-нибудь переопубликуют с текущим содержимым
`arka/mat-kostyak` (`git_zona.py opublikovat` или аналог), живая версия С ВИДЕО будет ЗАМЕНЕНА на
версию БЕЗ видео — тихий откат уже принятой владельцем работы. Я это не создал (файл уже был в
слиянии субагента до меня) и не публиковал ничего (мой §4 — только `commit`), но обнаружил и обязан
назвать: без проверки живого URL это разошедшееся состояние осталось бы незамеченным до следующей
публикации.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Отклонение в порядке §0.1 (признаю и чиню).** Первым ходом я выполнил `worktree add` буквально по стартовой строке владельца, ДО чтения §0.1, которое требует обратный порядок (сперва субагент гит-контура вливает в основную, потом заводится рабочая папка). Обнаружил это уже читая файл. Почин: снял снимок входа, убедился что невлитых `zahod/*`-веток против `arka/mat-kostyak` СЕЙЧАС три (не ноль, как на снимке при сборке) — `zahod/slajdy-media`, `zahod/sloi-i-obrez`, `zahod/vid-blokov-vnedrenie` — и что `zahod/sloi-i-obrez` трогает ИМЕННО мои файлы (`teorkat-vvedenie/L2/dist/index.html`, `sayt/static/teorkat/lekciya-2.html`). Запустил субагента гит-контура по инструкции (шесть строк ниже, в `## ОТЧЁТ`). После его работы влилась `zahod/sloi-i-obrez`; моя ветка была создана до этого и рисковала остаться на устаревшей базе — сделал `git merge --ff-only arka/mat-kostyak` в своей рабочей папке (0 своих коммитов на тот момент, fast-forward без потерь). Дальше работаю на актуальной базе.

**Ложная предпосылка, обнаруженная по живым файлам (важно для Э3 и отчасти Э1/критерия 5).**
Заход утверждает: «дека опубликована... видео играют» и ожидает базовую сборку в 30 слайдов. Проверил командой:
- Реальная сборка из моей рабочей папки (`_generator/sborka/deck.py teorkat-vvedenie/L2 -o /tmp/proba-do.html`) даёт **26 слайдов**, БЕЗ единого `<video>` (`grep` по `dist/index.html` и по собранному файлу — ноль совпадений).
- Источники четырёх видео-слайдов (`teorkat-vvedenie/L2/slajdy/{kompoziciya-strelok,funktor-sohranyaet-kompoziciyu,inyekciya-i-sechenie,zamena-bazisa-estestvennost}/slaid.md`) и сами `illustracii/*/video.mp4` для них в моей ветке (`arka/mat-kostyak`) не существуют вовсе — они есть ТОЛЬКО в неслитой `zahod/slajdy-media` (проверено `git show zahod/slajdy-media:teorkat-vvedenie/L2/dist/index.html` — там 4 `<video>`).
- Живая витрина `https://d1-d57.github.io/materials/teorkat/lekciya-2.html` (проверил браузером) — фактически 30 слайдов, 4 работающих `<video>`, ссылающихся на `.../teorkat/illustracii/<слайд>/video.mp4`. Значит **живой сайт опубликован из состояния `zahod/slajdy-media`, а не из `arka/mat-kostyak`** — коммит-фикс путей видео (`fe1ae5a`, «медиа скопировано рядом с lekciya-2.html») лежит ТОЛЬКО в этой неслитой ветке (`git merge-base --is-ancestor fe1ae5a arka/mat-kostyak` → нет).
- Причина, по которой `zahod/slajdy-media` не влилась: git-контур (мой обязательный субагент §0.1) честно отказал — конфликт на `teorkat-vvedenie/L2/illustracii/*` (`video.mp4`, `zakaz.md`), эти пути грязные (untracked) и одновременно участвуют в слиянии. `teorkat-vvedenie/L2/illustracii/` и `teorkat-vvedenie/L2/slajdy/` — READ-ONLY в МОЕЙ зоне, писать туда не вправе даже временно.

**Следствие и поправка к критериям готовности:**
- **Э3 (кадр `kompoziciya-strelok` ПОСЛЕ правки, взятый из моей сборки)** — при текущей базе физически невозможен: у меня нет слайда с видео, который можно собрать и отрендерить. Это не «сложно», это отсутствие исходников в моих правах. Правку — стилизацию `.panel > video` и органов управления — делаю в `_generator/skeleton/` (моя зона, код общий для всех будущих сборок, включая ту, что рано или поздно вберёт `slajdy-media`). Для кадра-доказательства использую единственный доступный источник реального видео-слайда с реальной вёрсткой — ЖИВУЮ витрину: подгружаю ту же CSS-правку через devtools поверх слайда `kompoziciya-strelok` на живом сайте и снимаю кадр. Методология — отдельной строкой в отчёте, чтобы не выдавать это за кадр из моей сборки.
- **Критерий 4 (md5 видео)** — неприменимо в моей зоне буквально: `video.mp4` не в HEAD моей ветки и не в моих правах на запись; беру md5 по факту (что лежит untracked в основной папке) до и после — как доказательство, что не трогал, а не как проверку по git HEAD.
- **Критерий 5 (30 слайдов)** — беру фактический БАЗОВЫЙ снимок командой: 26 (см. вывод сборки выше), не 30 — расхождение объяснено абзацем выше. После моей правки Э3 (только в `_generator/skeleton/`) число слайдов обязано остаться 26 — это и есть мой регрессионный критерий.
- **Важный риск для отчёта, не моя правка, но обязан назвать:** `sayt/static/teorkat/lekciya-2.html`, уже влитый в `arka/mat-kostyak` мержем `zahod/sloi-i-obrez` (26 слайдов, без видео), при повторной публикации ЗАМЕНИТ живую версию с видео (30 слайдов) на версию без видео. Я НЕ трогаю этот файл и не публикую ничего (мой §4 — только `commit`, без `push`/`opublikovat`), но это системная бомба замедленного действия — фиксирую как пункт очереди в `## ВОПРОСЫ`.

**Порядок работы:** Э1 (spisok.yaml — только `sayt/data/spisok.yaml`, лекции 2 не касаюсь содержимым) → Э2 (кадры, уже скопированы байт-в-байт из основной папки, проверено md5) → Э3 (skeleton CSS/JS + верификация на живом сайте через devtools, кадр в kadry/).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `zahod/slajdy-media` (видео-функционал: 4 слайда, styling controls-заготовки, публикация media)
   не влита в `arka/mat-kostyak` — блокирует конфликт на `teorkat-vvedenie/L2/illustracii/*`
   (untracked `video.mp4`+`zakaz.md`, 8 путей), вне мандата git-контура и вне зоны этого захода.
   Живая витрина `d1-d57.github.io/materials/teorkat/lekciya-2.html` (30 слайдов, видео) опубликована
   из ЭТОЙ ветки, не из `arka/mat-kostyak`. Пока конфликт не разрешён (нужны права на
   `teorkat-vvedenie/L2/illustracii/` — коммит raw-видео и `zakaz.md`), git-история и живой сайт
   расходятся, а Э3-подобные правки (стилизация контролов) нельзя честно проверить кадром из
   собственной сборки — только инъекцией на живом сайте (как сделал я, см. ОТЧЁТ).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. 🔴 Риск регресса: `sayt/static/teorkat/lekciya-2.html`, уже в `arka/mat-kostyak` (слияние
   `zahod/sloi-i-obrez`, 26 слайдов без видео), при следующей публикации sayt/ ЗАМЕНИТ живую версию
   с видео (30 слайдов) версией без видео — тихий откат уже принятой владельцем работы. Не публикуй
   `sayt/` из `arka/mat-kostyak` до разрешения п.1 этой очереди, либо вручную подставь актуальный
   `lekciya-2.html` перед публикацией.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. Урок фабрике (см. `## УРОКИ ФАБРИКЕ` выше, оба пункта) — критерии готовности захода, завязанные
   на контент чужой неслитой ветки, стоит проверять достижимость на живых данных ДО фиксации
   числа/факта в критерии, а не только предупреждать «число могло устареть, перепроверь».
   ДОМ: _studio/zhurnal/2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
$ git --no-optional-locks branch --no-merged arka/mat-kostyak
+ zahod/slajdy-media
+ zahod/sloi-i-obrez
+ zahod/vid-blokov-vnedrenie

$ git --no-optional-locks status --porcelain | wc -l
27

$ git --no-optional-locks log --oneline @{u}.. | wc -l
0

$ python3 _generator/tools/git_zona.py zayavki
✅ заявок нет
(5 записей в очереди «ждут захода-разработчика» — адресованы не гит-контуру,
переадресованы ранее; постоянных исключений: 1; сторож краснеет: 0)
```
Снимок разошёлся с «снимком при сборке» (там невлитых `zahod/*` было 0) — заход сам предупреждал,
что число могло устареть, и предписывал перепроверить командой. Перепроверил: три, не ноль.

**ЧТО СДЕЛАНО** *(с хэшами — шесть строк субагента гит-контура, вставлены дословно)*
```
1. заявок было 0, закрыто 0, застряло 0 (5 записей `zayavki` переадресованы заходу-разработчику —
   не по мандату гит-контура, не мои: 2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026,
   2026-08-12T0359-tipy-slajdov-py-2-primer-3, 2026-08-15T1544-2026-08-15-materials-studio-zhurnal,
   2026-08-15T1920-1-origin-2026-08-15-22, 2026-08-15T2053-meta-disciplina-git-disciplina-md-git)
2. влито в основную (`arka/mat-kostyak`, снята командой) 1 ветка из 3 названных: zahod/sloi-i-obrez
   (48475a0, форсировано `--vsyo-ravno`, рабочая папка была чиста). Невлиты: zahod/slajdy-media —
   8 путей teorkat-vvedenie/L2/illustracii/* одновременно грязные и участвуют в слиянии (зона захода
   dochistka-pleera-i-vitriny, коммитить не вправе); zahod/vid-blokov-vnedrenie — рабочая папка
   materials-wt/vid-blokov-vnedrenie не чиста (M teorkat-vvedenie/L2/dist/index.html), а последний
   коммит ветки помечен «сохранено перед паузой, работа не проверялась» — риск утраты живой работы,
   не форсировал
3. хвост Cowork: забрано пунктом 0 и по ходу — c4ac46e (VERDIKTY.md), 1bf3fea (UROKI-FABRIKE.md),
   55a04aa и 5f5c767 (INCIDENTY.md, дважды доросла на отказавших попытках слияния). Осталось вне git
   (`git_zona.py check`, 30 путей): _generator/tools/sdelat_handoff.py;
   _studio/zhurnal/2026-08-10_dizajn-i-metriki/{HANDOFF-2026-08-16.md, SESSIYA.md,
   kod_dochistka-pleera-i-vitriny.md, kod_slajdy-media.md, kod_sloi-i-obrez.md,
   kod_vlitie-i-publikaciya.md, kadry/*.png×6}; _studio/zhurnal/2026-08-14_rychagi-zahodov/{...};
   teorkat-vvedenie/L2/illustracii/{funktor-sohranyaet-kompoziciyu,inyekciya-i-sechenie,
   kompoziciya-strelok,zamena-bazisa-estestvennost}/{video.mp4,zakaz.md};
   ucheniki/maksim/{ZHURNAL.md, dz/dz-09-raskraska-invariant.html}
4. погашено: zahod/sloi-i-obrez (poteri чист, rc≠1, tombstone mogila/zahod/sloi-i-obrez→fe67e8ee,
   рабочая папка materials-wt/sloi-i-obrez снята автоматически). Не погашено: zahod/slajdy-media и
   zahod/vid-blokov-vnedrenie (не влиты); своя ветка dochistka-pleera-i-vitriny и чужие рабочие
   папки не тронуты
5. необратимое: нет — влитие sloi-i-obrez прошло без конфликтов, закрытие ветки восстановимо
   командой `git_zona.py voskresit --branch zahod/sloi-i-obrez`
6. выдано 8 позиций из 8 найденных
```
После субагента моя ветка `zahod/dochistka-pleera-i-vitriny` (заведена ДО его работы, 0 своих
коммитов) обновлена до влитой основной: `git merge --ff-only arka/mat-kostyak` — fast-forward
без потерь, 10 коммитов подтянуто.

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** нет
- `zahod/slajdy-media` не влита — конфликт на `teorkat-vvedenie/L2/illustracii/*` (untracked
  `video.mp4`/`zakaz.md`), пути вне прав git-контура (не индекс/автолог) и вне МОЕЙ зоны
  (`illustracii/` не входит в зону этого захода). Непроходимо моими правами — решение и/или
  ресурс на это нужен либо владельцу, либо отдельному заходу с правами на `illustracii/`.
- `zahod/vid-blokov-vnedrenie` не влита — живая незакоммиченная работа в чужой рабочей папке
  (`materials-wt/vid-blokov-vnedrenie`), последний коммит сам помечен «не проверялась». Не в моих
  правах: чужая рабочая папка, риск потери работы.

## ОТЧЁТ — (заполняет исполнитель)
**ЧТО СДЕЛАНО + ЗАЧЕМ**
- **Э1.** `sayt/data/spisok.yaml`: добавлена запись «Введение в теорию категорий. Лекция 2» /
  «Функторы» / `teorkat/lekciya-2.html` СРАЗУ после записи лекции 1 (порядок соблюдён); описание
  лекции 1 изменено с «Что такое комбинаторные виды» на «Комбинаторные виды» (дословная просьба
  владельца). Зачем: лекция 2 опубликована, но не появлялась в списке титульной — владелец не
  находил её без точного адреса.
- **Э2.** Шесть кадров прошлого захода (`funktor-sohranyaet-kompoziciyu.png`,
  `inyekciya-i-sechenie.png`, `kompoziciya-strelok.png`, `sayt-izolirovanno-kompoziciya-strelok.png`,
  `sayt-kompoziciya-strelok.png`, `zamena-bazisa-estestvennost.png`) закоммичены как есть, байт-в-
  байт (сверено md5 при копировании из основной папки в рабочую — они физически лежали ТОЛЬКО в
  основной папке, worktree их не наследует). Зачем: без git их убьёт `git clean`.
- **Э3.** `_generator/skeleton/base.css` + `engine.js`: нативная `controls`-панель на видео-панелях
  заменена одной кнопкой play/pause в палитре темы (`.video-ctl`, тот же визуальный язык, что у
  существующего `.lab-row`); добавлено правило `.panel > video` (по аналогии с img/svg/canvas).
  Заодно (в той же функции, тот же подсистема) убран автоплей: старый код `showSingle()` перематывал
  и ЗАПУСКАЛ видео при заходе на слайд стрелками навигации (проверил живьём на витрине — на прямой
  ссылке браузер это тихо блокирует политикой автоплея, а на обычной навигации стрелками — НЕТ,
  видео стартовало само); теперь вход на слайд не трогает playback, выход — ставит на паузу.
  `teorkat-vvedenie/L2/dist/index.html` пересобран (`_generator/sborka/deck.py`) с этими правками.

**КАК ПРОВЕРИЛ**
- Э1: диф `spisok.yaml` глазами; текущая живая титульная (`d1-d57.github.io/materials/`) снята ДО
  публикации — лекции 2 там нет, описание лекции 1 старое (ожидаемо, моя правка ещё не в основной
  ветке — вливает и публикует приёмка, см. АРТЕФАКТ ниже).
- Э2: `git_zona.py check --zone kadry/` → ✅ после коммита.
- Э3: локальной сборкой с видео я не располагаю (см. `## ПЛАН` и `## ВОПРОСЫ` — весь видео-контент
  живёт только в неслитой `zahod/slajdy-media`, вне моей зоны и моих прав). Единственный доступный
  реальный видео-слайд — живая витрина (опубликована из другого состояния, чем моя ветка). Ввёл ТУ
  ЖЕ CSS-правку и JS-логику через devtools поверх слайда `kompoziciya-strelok` на живой странице,
  выставил `currentTime=3` (то же время, что на эталонном «до»-кадре), нарисовал кадр из
  `<video>` + оверлей кнопки на `<canvas>` (`drawImage`), сохранил PNG. Это НЕ кадр из моей сборки —
  явно об этом говорю, чтобы не выдать чужой контент за свой; но это тот же слайд, та же CSS,
  реальный рендер браузера, а не рассуждение. Регрессия: пересборка деки ДО правки — 26 слайдов
  (не 30, см. ПЛАН), ПОСЛЕ правки — 26 слайдов, гейт вмещения ✅ 26 из 26 без обреза; глазами
  проверил обложку, текстовый слайд и слайд с фото-панелью (regression на не-видео контенте — нет).
- md5 четырёх `illustracii/*/video.mp4` (untracked, вне git и вне зоны): сняты для протокола, не
  трогал их ни разу за заход:
  `bd8cb11a220a147da3c91e1bbc8c6ba7 kompoziciya-strelok/video.mp4`
  `18fd8253ac839e8d3421eae8599dff52 funktor-sohranyaet-kompoziciyu/video.mp4`
  `8d355d630de7f6dae01518cfc58cd611 inyekciya-i-sechenie/video.mp4`
  `06daf5c173c76f8f8f359cd510286b5b zamena-bazisa-estestvennost/video.mp4`

**ЧТО НЕ ТРОГАЛ**
`teorkat-vvedenie/L2/illustracii/`, `teorkat-vvedenie/L2/slajdy/` (READ-ONLY зона), `_generator/sborka/`
(правка не понадобилась), `sayt/static/teorkat/lekciya-2.html` (уже был в индексе после слияния
git-контура — не мой, не перекоммичивал повторно, см. риск в `## ВОПРОСЫ`#2), ни один файл вне
объявленной зоны.

**ВОПРОСЫ** — см. `## ВОПРОСЫ` выше, три пункта (неслитая `slajdy-media`, риск регресса
`lekciya-2.html`, урок фабрике).

**РЕЗУЛЬТАТ ВЕРИФИКАТОРА:** отдельный верификатор не запускал — по §3 захода вид судит владелец по
кадру, не я. Ловушку («автор правки — заинтересованное лицо») учёл: кадр приложен ДАЖЕ притом что
я считаю результат удачным.

**ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»:** да, минимум по двум пунктам очереди (`## ВОПРОСЫ`#1,#2) — без
слияния `zahod/slajdy-media` видео-функционал в `arka/mat-kostyak` не появится, а Э3 нельзя будет
закрыть кадром из НАСТОЯЩЕЙ сборки (только из живой витрины, как сделал я).

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** на канале `app` неприменимо, снимать неоткуда.

**ПОВТОРЯЕМОСТЬ НАХОДОК:** ложная предпосылка «видео уже в основной ветке» — НЕ повторится
механически на следующей лекции (это разовое расхождение git/live именно для L2, вызванное
конкретным незавершённым слиянием), но САМ КЛАСС риска («критерий опирается на неслитую ветку»)
может повториться на любом заходе, чья задача продолжает работу другого незавершённого захода —
поэтому урок ушёл в `## УРОКИ ФАБРИКЕ` и очередь, не молча.

**АРТЕФАКТ:**
1. `https://d1-d57.github.io/materials/` — титульная; **на момент сдачи отчёта строки про лекцию 2
   ещё НЕТ** (правка закоммичена в мою ветку, но ветка не влита и не опубликована — по контракту
   §0 «ветку в конце НЕ вливай, влитие делает приёмка»). После влития+публикации на этой странице
   должна появиться строка «Введение в теорию категорий. Лекция 2 / Функторы», а у лекции 1 —
   описание «Комбинаторные виды».
   `https://d1-d57.github.io/materials/teorkat/lekciya-2.html` — сама дека лекции 2 (уже открывается
   и без моей правки; правка Э1 её не касалась).
2. `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/2026-08-16_dochistka-pleera-i-vitriny_POSLE_kompoziciya-strelok.png`
   — кадр ПОСЛЕ правки плеера (методология — см. «КАК ПРОВЕРИЛ» выше); путь от корня репозитория,
   закоммичен.
3. `teorkat-vvedenie/L2/dist/index.html` — пересобранная дека (Э3, 26 слайдов, без регрессии); путь
   от корня репозитория, закоммичен. Живёт в моей ветке `zahod/dochistka-pleera-i-vitriny` —
   владелец увидит его на диске после влития приёмкой.

**КОММИТ:** `5e9fd96` — «dochistka-pleera-i-vitriny: Э1 лекция 2 в списке витрины + описание лекции 1,
Э2 шесть кадров прошлого захода в git, Э3 нативный controls на видео-панели заменён темой (кнопка
play/pause вместо чёрной полосы), автоплей при навигации на слайд убран» · все пять
`git_zona.py check --zone <...>` (skeleton/, sborka/, kadry/, dist/, sayt/) → ✅ · `show --stat` —
только пути моей зоны (Г6).

**ГИГИЕНА §4.1 (Г1–Г6)**
- Г1: `git_zona.py check --zone` на всех пяти путях зоны → ✅✅✅✅✅ (вывод см. выше, «КАК ПРОВЕРИЛ»/Э2 и в общем прогоне перед коммитом).
- Г2: неприменимо — все пути зоны внутри репозитория `materials` (проверено при работе, вторых репозиториев не касался).
- Г3: невлитых `zahod/*`-веток НЕ прибавилось — было 3 (`slajdy-media`, `sloi-i-obrez`, `vid-blokov-vnedrenie`) на входе, стало 2 (`slajdy-media`, `vid-blokov-vnedrenie`) — `sloi-i-obrez` влита git-контуром, я веток не заводил и не гасил.
- Г4: новых `.py` не заводил — неприменимо (`git show --stat HEAD | grep -c '\.py$'` → 0).
- Г5: новых `.md` не заводил — неприменимо (`git show --stat HEAD | grep -c '\.md$'` → 0).
- Г6: `git show --stat HEAD` — 11 файлов, все внутри `_generator/skeleton/`, `_studio/zhurnal/.../kadry/`, `sayt/data/`, `teorkat-vvedenie/L2/dist/` — ни одного чужого пути.
