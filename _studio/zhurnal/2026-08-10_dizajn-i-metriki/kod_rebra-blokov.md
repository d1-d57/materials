# Канал исполнителя — rebra-blokov (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — механика графа, полей и гейта расписана поимённо с адресами; заполнение 23 карточек — работа на внимательность, не на изобретение.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Конвейер производства лекционных слайдов. Карточка слайда состоит из БЛОКОВ
(`### [tip] мысль`, типы: `narrativ`, `opredelenie`, `primer`, `utverzhdenie`, `dokazatelstvo`).
Заходы `tipologia-odna-os` и `tipologia-dochistka` навели порядок в СОСТАВЕ блоков: у каждого слайда
есть тип, тип задаёт допустимую раскладку, гейт фазы 2 её проверяет.

**Но состав — только половина правды. Вторая половина — СВЯЗИ между блоками, и её сегодня нет.**

Что уже есть (проверено разведкой 2026-08-14 по живым файлам): инструмент
`_generator/tools/graf_zavisimostej.py` — ЖИВОЙ, висит на `.githooks/pre-commit` (район строк 157–169)
и останавливает коммит лекции. Он строит граф терминов из `vvodit`/`opiraetsya_na`, ищет циклы
обходом в глубину и сверяет порядок понятий с `slide_order`. Работает.

Чего нет:

| ребро | что значит | заполнено в L2 |
|---|---|---|
| `vvodit` / `opiraetsya_na` | какие термины слайд вводит и на какие опирается | **23 из 23** |
| `dokazatelstvo_opiraetsya_na` | что доказывает это доказательство | **0 из 23** |
| `primer_dlya` | чему этот пример пример | **0 из 23** |

Два последних поля инструмент УЖЕ разбирает (`load_cards`, район строк 99–104), но:
их нет в шаблоне карточки (`bootstrap_lekcii.py`, `SLIDE_CARD_TMPL`), их не требует ни один гейт,
`porodit_slaid.py` их не порождает — то есть **написать их негде и никто не просит**.
Хуже: при пустых полях граф печатает «✓ циклов в утверждениях нет (проверено 0 связей)» —
**зелёным**. Пустота неотличима от проверенной чистоты.

ЦЕЛЬ (рамка владельца дословно): *«у блоков кроме собственных типов есть ещё мощная система
взаимосвязей, и она должна учитываться в типологии и быть дополнительной основой для анализа
и классификации»*. Связи — **вторая опора классификации**, а не вспомогательные данные.

🔴 **Один факт, ради которого заход и написан.** Карточка `kategoriya-vect-iso` (состав
определение · пример · доказательство) сегодня несёт метку `tip_slaida: NE_KLASSIFICIROVAN` —
«доказательство без утверждения». Владелец разобрал её и показал, что это не аномалия:
доказательство там доказывает, что построенный объект **действительно является примером** понятия.
То есть **доказательство всегда привязано к блоку, и этот блок не обязан быть утверждением.**
Записать это было нечем — ровно потому, что ребра нет.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ — читать внимательно, здесь два соблазна.**
(а) Ты НЕ переводишь двенадцать законов фазы 2 (А6/А8/А9/А10/Г-1/Г-2/Г-3/Г-6/Г-7/Г-8/Г-12/Г-15)
из жёлтого в красный. Они жёлтые ОСОЗНАННО: держатся на выборке из одной лекции, и код говорит это
прямо (`gejt_kartochki.py`, район строк 361–367). Покраснение требует замера по другим декам —
это отдельная работа, названная в `AKSIOMY-RAZMETKI.md` (район строк 425–430).
(б) Ты НЕ трогаешь аксиомы **А2 и А5** ни в каком виде — они отменены владельцем 12.08, и запрет
записан в самом коде (`gejt_kartochki.py`, район строки 349). Подробное их описание в
`AKSIOMY-RAZMETKI.md` — история, а не задание.
Сцены (`{@N}`), CSS и вёрстку тоже не трогай.

🔴 **ОЧЕРЁДНОСТЬ.** Ты идёшь ПОСЛЕ влития `zahod/tipologia-dochistka`. Проверка одной командой:
`grep -c "Нарративный" _generator/sborka/tipy_slajdov.py` → не 0. Ноль — предыдущий заход не влит,
**СТОП, скажи об этом и не начинай.**

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. доказательство ВСЕГДА привязано к блоку, и этот блок не обязан быть утверждением — доказывать можно и пример
2. связи блоков — вторая опора классификации наравне с составом блоков, а не вспомогательные данные
3. пустые поля связей сегодня печатаются ЗЕЛЁНЫМ — это молчаливая ложь, пустота обязана быть видимой
4. kategoriya-vect-iso снимает метку NE_KLASSIFICIROVAN, как только её доказательство получит ребро к примеру
5. перевод двенадцати законов фазы 2 из жёлтого в красный в этот заход НЕ входит — он требует замера по другим декам

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/rebra-blokov`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/rebra-blokov`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add rebra-blokov --branch zahod/rebra-blokov
  cd ../materials-wt/rebra-blokov
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/graf_zavisimostej.py` `_generator/sborka/gejt_kartochki.py` `_generator/sborka/bootstrap_lekcii.py` `_generator/sborka/formaty.py` `_generator/sborka/porodit_slaid.py` `_generator/tools/fixtures/graf/` `_generator/tools/fixtures/sborka/` `teorkat-vvedenie/L2/slajdy/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- правки графа, гейта, шаблона карточки и заполненные рёбрами карточки L2 — своей зоной, хэш в отчёт

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- ветку zahod/rebra-blokov не гасить — вливает приёмка; чужие ветки и рабочие папки не трогать

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/rebra-blokov`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/graf_zavisimostej.py` `_generator/sborka/gejt_kartochki.py` `_generator/sborka/bootstrap_lekcii.py` `_generator/sborka/formaty.py` `_generator/sborka/porodit_slaid.py` `_generator/tools/fixtures/graf/` `_generator/tools/fixtures/sborka/` `teorkat-vvedenie/L2/slajdy/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, в этом порядке. Проект не изучай, ничего сверх списка не открывай:
  1. `_generator/tools/graf_zavisimostej.py` — целиком, это ядро задачи. Особенно `load_cards`
     (разбор полей), `build_term_graph`, `build_statement_graph`, `find_cycle`, `render`.
  2. `_generator/sborka/gejt_kartochki.py` — куда встраивается требование связей; там же законы фазы 2
     (их НЕ трогать, см. СТОП) и проверка раскладки блоков.
  3. `_generator/sborka/bootstrap_lekcii.py` — `SLIDE_CARD_TMPL`, шаблон шапки карточки: сюда добавляются поля.
  4. `_generator/sborka/formaty.py` — `OBYAZATELNYE_POLYA`, `FAZA_POLYA`: что требуется и на какой фазе.
  5. `_generator/sborka/porodit_slaid.py` — порождение карточки, чтобы новые поля рождались вместе с ней.
  6. `.githooks/pre-commit` — живая точка вызова графа (район строк 157–169); **READ-ONLY, вне зоны**.
  7. Карточки `teorkat-vvedenie/L2/slajdy/*/slaid.md` — материал для Э3. Числа считай командами.
  8. `_studio/zhurnal/2026-08-10_dizajn-i-metriki/AKSIOMY-RAZMETKI.md` — **справочно**, обязательных
     действий не несёт; помни про отменённые А2/А5.
- 🔴 **Каждый адрес выше — «район строк», снятый 2026-08-14 с непринятых веток. Проверяй грепом,
  а не номером.** Расхождение — строка в `## УРОКИ ФАБРИКЕ`, а не повод гадать.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
### Э0. ЗАМЕР ДО РАБОТЫ

Сними и напечатай: сколько карточек несут каждое из четырёх полей связей; сколько блоков
`dokazatelstvo` и `primer` есть в L2 всего (это верхняя граница числа рёбер, которые предстоит
завести). Замер сборки захода: `vvodit` 23/23 · `opiraetsya_na` 23/23 ·
`dokazatelstvo_opiraetsya_na` **0/23** · `primer_dlya` **0/23**. Разошлось — работай по своим числам.

⚠ Ловушка замера: блоки лежат в карточке ДВАЖДЫ («Математика — развёрнуто» и «Текст слайда — сжато»).
Считаешь без фильтра по разделу — удвоишь всё.

### Э1. Ребро «доказательство доказывает X» — довести до графа

Сегодня `dokazatelstvo_opiraetsya_na` строит рёбра в `build_statement_graph`, а `primer_dlya`
**не строит рёбер вовсе** — разведка проверила: поле участвует только в счётчике «примеров
с направлением мотивации» (район строк 237–240), в граф не превращается.

Задача: **оба поля дают рёбра графа.** Адресатом доказательства может быть блок ЛЮБОГО типа —
утверждение, пример, определение. Это прямое решение владельца, а не расширение на всякий случай:
`kategoriya-vect-iso` доказывает пример.

🔴 **Ребро адресуется БЛОКУ, а не только слайду.** Сегодня оба поля указывают на слайд целиком
(id карточки). Внутри слайда бывает несколько блоков, и «доказательство доказывает вон тот блок
на этом же слайде» записать нечем. Реши это и назови решение в `## ПЛАН` ДО работы: либо адрес
вида `<слайд>#<тип-блока>`, либо явное «внутрислайдовые рёбра не вводим, потому что…».
**Молча оставить только слайдовую адресацию нельзя** — на этом стоит правило сцен следующего захода.

### Э2. Пустота перестаёт быть зелёной

Сегодня при нулевых связях граф печатает «✓ циклов в утверждениях нет (проверено 0 связей)» —
зелёным. Это ровно тот дефект, который канон называет худшим: **зелёный чекер с необъявленной
слепой зоной опаснее красного, ему верят.**

Заводится две вещи:
- **граф печатает ОХВАТ вместе с вердиктом:** «проверено N связей из M возможных», где M — число
  блоков `dokazatelstvo` и `primer` в лекции, посчитанное ДО проверки. `N = 0` при `M > 0` —
  **не зелёный**;
- **гейт фазы 2 требует адресата:** блок `dokazatelstvo` без записанного ребра — 🔴 **КРАСНЫЙ**
  (доказательство, которое неизвестно что доказывает, — это дефект разметки, и владелец назвал
  жёсткость здесь обязательной). Блок `primer` без `primer_dlya` — 🟡 **жёлтый**: пример иногда
  самостоятелен, и красить его по всей фабрике не на чем — выборка одна лекция.

### Э3. Заполнить рёбра во всех карточках L2

По каждому блоку `dokazatelstvo` и `primer` в 23 карточках. Заполняется **по смыслу карточки**,
а не механически: что именно доказывает это доказательство, чему пример этот пример.
🔴 **Тексты блоков и математику НЕ переписывать** — заход добавляет поля шапки, а не правит содержание.
Не смог определить адресата — это находка: назови карточку в `## ВОПРОСЫ`, не выдумывай ребро.
**Выдуманное ребро хуже отсутствующего:** оно попадёт в граф и будет молча участвовать в проверке циклов.

### Э4. Снять `NE_KLASSIFICIROVAN` с `kategoriya-vect-iso`

После Э3 её доказательство получает ребро к примеру. Проверь, ложится ли карточка в тип штатно,
и если да — назначь тип и сними метку. **Не ложится — оставь метку и скажи почему**, это законный
исход и развилка владельцу, а не провал.
Замер: карточек с меткой сегодня ровно **1 из 23**; после захода должно стать **0**, и если стало
больше — типология поехала, это красный сигнал, а не мелочь.

### Э5. Поля должны рождаться вместе с карточкой

`bootstrap_lekcii.SLIDE_CARD_TMPL` и `porodit_slaid.py` — новые поля попадают в шапку новой карточки,
`formaty.py` знает о них. Иначе через месяц поля снова окажутся пустыми во всех новых лекциях,
и это будет ровно тот же дефект, который ты сейчас чинишь.

### Э6. Спецификация связей — вытащить из невлитой ветки

Формальное описание видов связей лежит **в другом репозитории**: `disciplina`,
`skills/slajdy/references/graf-zavisimostej.md`, и висит на невлитой ветке `zahod/fazy-1-2-dovodka`
(коммит `5268593`). То есть код влит, а правила связей существуют только в непринятой ветке.
**Твоя работа — не влить её** (чужой репозиторий, вне зоны), а **прочитать и сверить**: совпадает
ли то, что ты делаешь, с тем, что там описано, и назвать расхождения в отчёте. Не достал —
законный исход, так и напиши.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — пять клауз)**

`Y` = `ls -d teorkat-vvedenie/L2/slajdy/*/ | wc -l` (на сборке 23; снимай заново).

1. **Рёбра заполнены и охват назван:** каждый блок `dokazatelstvo` в L2 несёт адресата;
   в отчёте — «связей заведено N из M», где M посчитано командой ДО работы. Блок без адресата —
   либо ребро, либо строка в `## ВОПРОСЫ` с причиной; молча пропущенных нет.
2. **🔴 Граф доказанно умеет краснеть на новых рёбрах:** заведи в `_generator/tools/fixtures/graf/`
   негативную фикстуру, где доказательства ссылаются друг на друга по кругу, и покажи **ненулевой
   код возврата**. Отдельно — фикстура «доказательство без адресата» краснит гейт фазы 2.
   Без этого Э1–Э2 недоказуемы: сегодня граф на пустоте зелёный, и зелёный после твоей работы
   сам по себе не значит ничего.
3. **Пустота больше не выглядит проверкой:** прогони граф на лекции, где связей нет
   (годится фикстура), и покажи, что вывод НЕ зелёный и печатает «0 из M».
4. **`NE_KLASSIFICIROVAN` в L2: было 1, стало 0** — либо стало 0, либо в отчёте названа причина,
   почему карточка осталась неклассифицированной. Стало больше одной — красный.
5. **Ничего не сломано у соседей:** гейт фазы 2 зелёный по типологии на всех Y карточках с охватом
   внутри вердикта; негативная фикстура `tipologia-e3-negativ` по-прежнему даёт ненулевой код;
   хук `pre-commit` проходит на твоём же коммите (он зовёт граф — это и есть боевая проверка).
   ⚠ Чужой долг, не чинить и не пугаться: два замечания по бюджету слов
   (`fundamentalnaya-gruppa`, `vektornye-prostranstva`) и красная ловушка 37 `PROGNAT.sh` на
   геометрии `tolko_tekst` — красные с 2026-08-12 по `_INFRA-git/INCIDENTY.md`, старше этих заходов.

🔴 **Код возврата смотри ПЕРВЫМ** (`echo $?`). Граф зелёный и граф не запустившийся снаружи одинаковы.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «висячих доказательств нет», а «нет, проверено N из M».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Нужен — но не на потери, а на СМЫСЛ рёбер.** Операция аддитивная (ничего не удаляется), зато
Э3 создаёт 20–40 утверждений о математике, каждое из которых легко выдумать правдоподобно.

**ПОСЛЕ-верификатор, свежий субагент, ДРУГОЙ метод:** он получает карточки и заведённые рёбра
и отвечает на один вопрос — **правда ли это доказательство доказывает именно тот блок, на который
указывает ребро.** Судит по тексту блоков, а НЕ по полю, которое проверяет. Охват сплошной,
число названо внутри вердикта.

🔴 **Скажи ему прямо: опровержение — полноценный успех.** Он проверяет работу того, кто
заинтересован в её правильности; услужливое «все рёбра верны» — брак. «Вот эти три ребра не
подтверждаются текстом» — ценный и законный исход. Финальной строкой самоотчёт «сверено N рёбер из M»:
усечённый ответ снаружи неотличим от честного «расхождений мало».
🔴 **Верификатор НЕ должен видеть твоих обоснований из отчёта** — только карточки и рёбра.
Автор достраивает недостающее из головы, и проверяющий, читавший его рассуждения, делает то же самое.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/graf_zavisimostej.py _generator/sborka/gejt_kartochki.py _generator/sborka/bootstrap_lekcii.py _generator/sborka/formaty.py _generator/sborka/porodit_slaid.py _generator/tools/fixtures/graf/ _generator/tools/fixtures/sborka/ teorkat-vvedenie/L2/slajdy/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/graf_zavisimostej.py _generator/sborka/gejt_kartochki.py _generator/sborka/bootstrap_lekcii.py _generator/sborka/formaty.py _generator/sborka/porodit_slaid.py _generator/tools/fixtures/graf/ _generator/tools/fixtures/sborka/ teorkat-vvedenie/L2/slajdy/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/graf_zavisimostej.py` `_generator/sborka/gejt_kartochki.py` `_generator/sborka/bootstrap_lekcii.py` `_generator/sborka/formaty.py` `_generator/sborka/porodit_slaid.py` `_generator/tools/fixtures/graf/` `_generator/tools/fixtures/sborka/` `teorkat-vvedenie/L2/slajdy/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/graf_zavisimostej.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/gejt_kartochki.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/bootstrap_lekcii.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/formaty.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/porodit_slaid.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/graf/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/sborka/` → ✅; `python3 _generator/tools/git_zona.py check --zone teorkat-vvedenie/L2/slajdy/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
