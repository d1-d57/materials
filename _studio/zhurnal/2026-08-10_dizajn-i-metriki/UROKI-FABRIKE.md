# УРОКИ ФАБРИКЕ — арка «2026-08-10_dizajn-i-metriki»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

### Артефакт в scratchpad исполнителя приёмке недоступен — Г3 краснеет законно, а починить его нечем
Заход `mikrotipografika-melochi` собрал деку в `/private/tmp/claude-501/…/scratchpad/` и назвал этот
путь строкой `АРТЕФАКТ:`. Путь абсолютный, живой на машине исполнителя и **не существующий ни для
приёмки, ни для владельца**. Заход сам запрещал писать в `teorkat-vvedenie/**` (READ-ONLY) — то есть
исполнитель поступил дисциплинированно, а положить артефакт было некуда: разрешённого места сборки
заход не назвал вовсе.
ЦЕНА: гейт Г3 `priyomka.py` красный при полностью корректной работе; приёмка пересобирала деку сама
(≈4 минуты) только чтобы получить файл, который можно показать владельцу. Класс повторяющийся:
любой заход с READ-ONLY контентом и собираемым артефактом даст то же самое.
ДОМ: disciplina/doma/meta

### В песочнице Cowork нет браузера, и заходы, чей критерий меряется браузером, приёмка проверить не может
Критерий захода требовал измерить высоты текстовых зон ДО/ПОСЛЕ через `gejt_vmeshcheniya.py`.
Инструмент работает только с playwright + chromium; `pip install playwright` проходит, а
`playwright install chromium` падает на системных зависимостях. Значит числовое утверждение отчёта
«0 расхождений, 21/21 влезло» приёмка воспроизвести НЕ МОЖЕТ — вопреки правилу «всякое числовое
утверждение об артефакте приёмка перегоняет сама».
ЦЕНА: главная клауза критерия принята на слово. Обошлось (правки аддитивные, `git diff --stat`
подтвердил, что числа солвера не тронуты), но приёмка была слепа ровно там, где заход считал себя
самым рискованным.
ДОМ: disciplina/doma/meta

### Гипотеза исполнителя о причине чужого красного гейта опровергается автологом за одну команду
Заход `tipologia-odna-os` предположил, что расхождение геометрии в ловушке 37 `PROGNAT.sh` вызвано
CSS-правками принятого накануне захода 1 (`hyphens`/`text-wrap` сдвинули метрику текста). Приёмка
грепнула `_INFRA-git/INCIDENTY.md` и нашла ту же ловушку красной **2026-08-12 19:53** — за сутки до
захода 1, и там же рядом третий случай того же класса (`centr-gruppy`, 2026-08-12). Гипотеза ложна,
причина — хрупкость калибровки сметы, долг открыт с 08-12.
ЦЕНА: сама по себе — ноль, исполнитель честно назвал это предположением, а не фактом. Но если бы
приёмка гипотезу приняла, следующим ходом откатывали бы принятый и уже влитый заход 1 по ложному
следу. Автолог инцидентов — готовый ответ на вопрос «моя ли это регрессия», и заход о нём не знал:
в шаблоне захода `INCIDENTY.md` не назван нигде.
ДОМ: disciplina/doma/meta

### Артефакт снова собран в scratchpad — тот же дефект, что у захода 1, в том же виде
`АРТЕФАКТ:` указывает на `/private/tmp/claude-501/…/scratchpad/tipologia-artefakt/…`, Г3 красный.
Урок про это уже записан после захода 1 (выше), и заход 2 повторил его дословно, потому что правка
в фабрику ещё не внесена.
ЦЕНА: второй красный Г3 подряд и вторая пересборка силами приёмки. Класс подтверждён повторением:
это не случайность одного исполнителя, а отсутствующее правило «куда класть собираемый артефакт,
когда контент READ-ONLY».
ДОМ: disciplina/doma/meta

### Грепом по названию алгоритма инструмент не находится — он назван по предметной области
Владелец спросил про «питоновский инструмент топологической сортировки блоков». Cowork грепнул
`topolog|toposort|tsort` по `_generator/`, получил ноль и сказал владельцу, что инструмента нет.
Инструмент есть: `_generator/tools/graf_zavisimostej.py`, влит, висит на `pre-commit` и
останавливает коммит лекции. Не нашёлся по двум причинам сразу: назван по предмету («граф
зависимостей»), а не по алгоритму, и топологической сортировки в нём нет намеренно — он ищет
ЦИКЛЫ обходом в глубину, докстринг прямо говорит «не переставляет слайды и не предлагает порядок».
ЦЕНА: аналитик выдал владельцу отрицательное утверждение о состоянии репозитория, опираясь на один
греп по имени алгоритма. Владелец не согласился по памяти и оказался прав — иначе следующим ходом
писали бы заход на инструмент, который живёт и работает. Отрицательный вывод «в репозитории этого
нет» требует поиска по ПРЕДМЕТУ и по структурным признакам (поля данных, точки вызова в хуках),
а не по имени алгоритма.
ДОМ: disciplina/doma/meta

### Гейт Г3 приёмки разбирает честное «артефакта нет» как путь и краснеет на нём
Заход `tipologia-dochistka` собираемого артефакта не производит — он правит реестр типов и CLI
порождения карточек. Исполнитель написал в строке `АРТЕФАКТ:` ровно то, что разрешает шаблон отчёта
(«артефакта нет: <почему>»), и получил красный Г3: `priyomka.py` склеил текст с корнем репозитория
и сообщил «артефакт не найден на диске: …/артефакта нет в смысле документа/деки».
ЦЕНА: третий красный Г3 подряд за три приёмки, и на этот раз ЛОЖНЫЙ — работа безупречна, отчёт
корректен, красное неистинно. Два предыдущих Г3 были истинными (артефакт в недоступном scratchpad),
и на фоне этой серии ложное красное перестаёт различаться: гейт, который краснеет и когда прав,
и когда не прав, перестают читать.
ДОМ: disciplina/doma/meta

### Заход исчезает из реестра, а строки с его именем остаются жить в чужих файлах
Э1 удалил тип Т5 из реестра типов. Живых карточек с Т5 не было ни одной, миграция данных не
потребовалась — и заход выглядел закрытым. Но имя «Т5» осталось зашитым строкой в двух чужих домах:
`formaty.py:132` (`TIPY_SLAJDA_BEZ_TREBOVANIYA_SVYAZEJ = (..., "Т4", "Т5")`) — и гейт фазы 1 ЖИВЬЁМ
печатает «типы Т6, Т4, Т5 освобождены», называя несуществующий тип; `fixtures/porozhdenie/PROGNAT.sh:92`
зовёт `porodit_slaid.py --tip Т5` и падает на `argparse`.
ЦЕНА: коммит захода потребовал законного обхода `--no-verify`, а гейт фазы 1 будет врать про Т5
на каждой будущей карточке. Обе правки — по одной строке, обе вне зоны исполнителя, и найти их
можно было ДО работы одним грепом по имени удаляемой сущности за пределами зоны.
Отсюда правило для сборки захода: **удаляешь имя из реестра — грепни его по ВСЕМУ репозиторию
и внеси найденные дома в зону, иначе заход закрывается наполовину.**
ДОМ: disciplina/doma/meta

### Дефект, который виден только на ЖИВОМ артефакте, а не на кадрах и не в числах
Исследование вида блоков сняло 57 статичных скриншотов и построило девятипризнаковую шкалу —
и не увидело двух вещей. Первую нашёл верификатор, ОТКРЫВ дек и пролистав клавишами: 7 слайдов
из 21 теряют текст под нижней кромкой. Вторую — там же: строка-заголовок и линейка блока
показываются сразу, а содержимое блока приходит со сценой позже, и зритель секунду смотрит
на пустую метку. Владелец, пролистав тот же дек, назвал ровно те же два дефекта и те же слайды.
ЦЕНА: без этого шага заход сдал бы схему, которая режет текст на трети лекции, и обнаружилось бы
это уже после внедрения. Оба дефекта невидимы и для численной шкалы, и для статичных кадров:
первый прячется под кромкой, второй существует только во времени.
⇒ Заход, чей продукт смотрят глазами, обязан нести отдельным шагом «ОТКРОЙ артефакт и пролистай
его как зритель» — не «сделай скриншоты», а именно живой прогон.
ДОМ: disciplina/doma/slajdy, disciplina/doma/meta

### Заход, меняющий вложенность DOM, ломает измерители, которые лежат ВНЕ его зоны
Внедрение вида блоков обернуло каждый блок в `<div class="blk">`. Два инструмента измерения —
`vmeshchenie.py` и `zamer_smety.py` — искали абзацы селектором `:scope > p, :scope > ul.tlist`,
то есть прямыми детьми `.t-body`, и после обёртки стали находить НОЛЬ абзацев на каждом слайде.
Браузер сообщал отрицательную высоту текста на всех 21 карточках, `zamer_smety.py --konstanty`
давал `n=0` вместо `n=33`. Оба файла лежали ВНЕ зоны захода; исполнитель починил их как явное
исключение и назвал это открыто, вторую половину нашёл верификатор.
ЦЕНА: не почини он их — измерение солвера и калибровка констант ломались бы МОЛЧА, и все числа
критерия готовности оказались бы вымышленными при зелёном виде. Аналитик, писавший зону, не увидел
связи «меняю разметку → ломаю тех, кто её меряет».
⇒ Правило для сборки захода: **меняешь структуру DOM — грепни, кто её РАЗБИРАЕТ селекторами
(`:scope >`, `querySelector`, регекспы по тегам), и внеси найденное в зону.** Тот же класс, что
и «удаляешь имя из реестра — грепни его по всему репозиторию», только про разметку.
ДОМ: disciplina/doma/meta, disciplina/doma/slajdy

### Клауза критерия требует артефакт, для которого зона не даёт места
Заход обязывал вести лог прогонщика на диске («лога нет — отчёт не принимается»), но зона
состояла из файлов генератора и не включала ни одной папки, куда этот лог можно положить под git.
Исполнитель сделал ровно то, что оставалось: положил лог в рабочую папку, которая приёмке
недоступна. Тот же дефект, что и с артефактом в scratchpad, но на этот раз его создала клауза,
написанная аналитиком.
ЦЕНА: главная клауза захода (ноль переполнений) проверена приёмкой только по отчёту — числа
принимать пришлось на слово. Проверка, ради которой лог и заводился, не состоялась.
ДОМ: disciplina/doma/meta

### Регрессию дал не заход, а ПЕРЕСЕЧЕНИЕ двух корректных веток — и эталон «до» был снят не с того предка
Сцены пропали на 14 слайдах из 26. Каждый заход по отдельности был прав: `sceny-iz-blokov`
снял ручные теги `{@N}` с 14 карточек, положившись на автогенерацию; `vid-blokov-vnedrenie`
перевёл рендер на новую функцию `blocks_for_render`, а авто-тегирование осталось в старой
`render_section_markdown`, которую новый путь не зовёт вовсе. Вместе — минус 22 сцены.
Хуже: приёмка сняла эталон «до поломки = 50 сцен» с ветки `polya-i-uzor`, которая отпочкована
ПОСЛЕ одного из этих заходов и ДО другого — то есть сравнивала состояния, между которыми
нет отношения «до/после». Те 50 сцен были ручными тегами, а не автогенерацией.
ЦЕНА: регрессия прошла приёмку и верификатора и всплыла у владельца; на её разбор ушёл
отдельный проход с пересборками. Ни один машинный гейт её увидеть не мог: сцены живут
в атрибутах, а проверялись текст и кадры.
⇒ Два правила. **Замер «до» берётся с ОБЩЕГО ПРЕДКА веток, а не с соседней ветки** —
иначе сравниваются несравнимые состояния. И **у правила вида «X всегда равно Y» обязан быть
гейт, краснеющий на поломке**, иначе пересечение веток снимет его молча.
ДОМ: disciplina/doma/meta, disciplina/doma/slajdy

### Ветки не вливались пять заходов подряд, потому что аналитик ни разу не передал `--vlit`
Механизм в порядке и работает: каждый заход велит отдать весь гит-контур СУБАГЕНТУ первым ходом,
до заведения своей рабочей папки («сам эти пункты не исполняй»), есть отдельный режим
`--zadanie-subagentu`, печатающий ему промпт. Отчёты подтверждают, что субагент запускался.
Он ничего не вливал, потому что в его задании стояло «вливать нечего» — а это то, что вписывает
генератор, когда аналитик не передал `--vlit <ветка>` при сборке. Аналитик не передавал его
НИ РАЗУ за пять заходов.
ЦЕНА: четыре принятых захода остались невлитыми; владелец смотрел деку без сцен и служебных
слайдов; приёмка ДВАЖДЫ намерила «регрессию», которой не было, потому что мерила ветку, куда
починка не доехала; на разбор ушло два прохода приёмки и один ложный вывод.
⚠ **Хуже всего был сам ложный вывод.** Аналитик грепнул слово «субагент», увидел 16 вхождений
в генераторе, НЕ ОТКРЫЛ НИ ОДНОГО и заключил «нигде не сказано субагентом» — после чего вписал
в заход починку того, что не сломано. Поймал владелец: «странно, что не нашёл, мы это точно
прописывали». Это ровно тот класс ошибки, который уже записан уроком выше про переносы:
отрицательный вывод по грепу без чтения найденного.
⇒ **Два следствия. (1) Аналитик, собирающий заход, обязан передать `--vlit` для каждой ветки,
ждущей влития, — или явно назвать, что вливать нечего, проверив командой. (2) Генератор
предупреждает в консоль, когда невлитые ветки есть, а `--vlit` не назван, — это предупреждение
надо читать, а не пролистывать.**
ДОМ: disciplina/doma/meta

### `sdelat_handoff.py` брал ПЕРВУЮ отсечку дневника вместо последней
Гейт «дневник разнесён» искал `<!-- РАЗНЕСЕНО ДО СЮДА: дата -->` через `re.search`, то есть брал
первое совпадение. Отсечки накапливаются по одной за сессию, свежая всегда последняя — и хэндофф
отказывался собираться при ПОЛНОСТЬЮ разнесённом дневнике, сообщая «найдено: 2026-08-13»,
когда рядом стояла отсечка сегодняшнего дня.
ЦЕНА: закрытие сессии заблокировано на исправной работе; чинится одной строкой
(`findall` + `max`), но обнаруживается только в момент, когда сессию уже пора закрывать —
то есть в худший из возможных моментов, на исходе контекста.
⇒ Гейт, судящий «последнее событие в накопительном файле», обязан брать максимум, а не первое
совпадение. Тот же класс — любые проверки по журналам, инцидентам, автологам.
ДОМ: disciplina/doma/meta

### Зона, переданная в `bootstrap_zahod.py` строкой с запятыми, роняет гейт Г0 приёмки ЛОЖНЫМ красным
Аналитик собрал заход командой `--zone "_studio/zhurnal/_INFRA-git/, _studio/docs/KARTA.md, sayt/"`.
Запятые уехали внутрь путей: в контракт зоны встало `_generator/skeleton/,` с запятой на конце,
а `priyomka.py` подставил всю строку целиком в кавычках — `check --zone "_studio/zhurnal/_INFRA-git/"`
→ «такой зоны здесь нет ни на диске, ни в git».
ЦЕНА: Г0 — самый первый гейт приёмки, тот, после которого отчёт «не принимается, что бы в нём ни
было написано». Он покраснел на исправной работе: те же три зоны, прогнанные руками по одной,
дали ✅ ✅ ✅. Приёмка сошлась 11 гейтов из 14 вместо 13 из 14, и оба лишних красных — дефекты
захода, а не исполнителя. Ровно тем и опасно: гейт, краснеющий на здоровом, учат игнорировать.
Тот же дефект сидит в трёх заходах этой сессии — собраны одной рукой в один час.
ВЕРДИКТ:
ДОМ: disciplina/doma/meta

### Артефакт в git оказался СТАРШЕ своих источников — и это не поймал ни один гейт захода
После влития `kadr-uzor-i-vmeshchenie` правки движка (`ZONA_PAD_BOTTOM=4`, `isIllustration`) легли
на диск, но `teorkat-vvedenie/L2/dist/index.html` в репозитории остался от 17:42, то есть собран
ДО них; источники — 20:36. Владелец, открывший деку из репозитория, увидит состояние до починки.
Свежая пересборка существует, но лежит НЕЗАКОММИЧЕННОЙ в чужой рабочей папке
`materials-wt/kadr-uzor-i-vmeshchenie` — и она же блокирует гашение ветки.
ЦЕНА: этот класс уже оплачен 31.07 («открыл СТАРУЮ колоду, потому что сборка молча не запустилась,
и решил, что правка не сработала — ушёл целый круг на диагностику того, чего не было»). Гейт Г3
`priyomka.py` умеет сравнивать даты артефакта и источника, но проверяет ТОЛЬКО путь из строки
`**АРТЕФАКТ:**` отчёта. Заход, который ничего не собирает и честно пишет «артефакта нет»,
проходит Г3 мимо — а устаревшую деку после влития оставляет именно он.
⇒ Влитие правок движка и пересборка того, что этими правками собирается, — один ход, не два.
ВЕРДИКТ:
ДОМ: disciplina/doma/meta

### Дека, собранная в песочнице Cowork с `--bez-podbora`, предъявлена владельцу как дека — и он судил по ней
Браузера в песочнице нет (`playwright` не поднимается: нет root, `libxdamage1`), поэтому
аналитик собрал L2 флагом `--bez-podbora` и отдал владельцу файлом. Владелец посмотрел
глазами и вернул четыре дефекта. Три оказались настоящими. Четвёртый — «формула
`ℤ → {e} → ℤ` обрезана снизу» — на нормальной сборке НЕ ВОСПРОИЗВОДИТСЯ: заход промерил
все 22 текстовые зоны и получил `scrollHeight − clientHeight = 0.0` везде, и на деке ДО
правки тоже; независимый верификатор, не читавший ни кода, ни плана, пришёл к тому же.
Проверено приёмкой на двух файлах: в сборке аналитика `overflow` у `.zone.copy` клипающий,
в сборке исполнителя — `visible`; кадры ДО/ПОСЛЕ показывают формулу целой в обоих.
ЦЕНА: владелец разбирал как «дефект, который чинили дважды и не починили» то, что было
свойством СПОСОБА СБОРКИ, а не деки; заход отработал по Э2 полностью и узнал это только
в конце. Структурно правка не лишняя (клип снят по существу), но адресована она была
призраку. Хуже цены — подорванное доверие: тот же разговор шёл о том, что приёмка
принимает невыполненное, и в него добавился дефект, которого в продукте не было.
⇒ Артефакт, собранный без браузера, владельцу как продукт НЕ отдаётся: он неотличим от
настоящего на вид и отличается по вёрстке. Либо сборка идёт host-side, либо файл
предъявляется с явной пометкой «собрано без подбора типографики, вёрстка непредставительна».
ВЕРДИКТ:
ДОМ: disciplina/doma/meta

### Зона захода не включала `kadry/`, хотя критерий требовал класть кадры именно туда
Аналитик собрал заход `slajdy-media` с зоной из семи путей и по ходу дважды её расширял
(`slajdy/`, `dist/`, `sayt/`), но `kadry/` не добавил ни разу — при том что клауза 2
критерия требует «четыре `.png` в `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/`».
Исполнитель кадры снял и положил в главную папку — единственное доступное место, — но
закоммитить их не мог: путь вне его зоны. Шесть кадров остались `??` вне git.
ЦЕНА: правило владельца от 2026-08-16 звучит «требование про ВИД закрывается только кадром
на ВЛИТОЙ ветке» — и первый же заход после его введения оставил кадры вне git, где их
убьёт `git clean`. Ровно тот отказ, против которого правило заводилось, воспроизведён
контрактом зоны. Это второй за сессию промах по третьему вопросу гейта входа («ЗОНА
содержит файл, в котором лежит ответ?») — первый был с зоной через запятые.
⇒ Критерий, называющий путь для артефакта, и `--zone` проверяются друг против друга
АРИФМЕТИЧЕСКИ при сборке захода: каждый путь из критерия обязан лежать внутри зоны.
Проверяется одной командой и ловится до выдачи.
ВЕРДИКТ:
ДОМ: disciplina/doma/meta

### Нативные `controls` браузера на медиа-слайде ломают тему деки чёрной плашкой
Видео встроено с `controls` — решение верное по существу (ролики длиной 47–77 с, лектор их
комментирует, останавливать нечем), но нативная панель Chrome рисуется чёрным прямоугольником
с градиентом на всю ширину кадра, с иконкой ЗВУКА (звука в роликах нет), кнопкой
полноэкранного режима и меню «три точки». Тема деки бежево-зелёная; на паузе — а слайд по
умолчанию именно на паузе — панель видна всегда.
ЦЕНА: поймано приёмкой на кадрах, до владельца не дошло. Но заход при этом честно выполнил
критерий: клауза про кадры требовала «геометрия: узкая полоса фона, медиа в центре,
заголовка нет» — и геометрия на кадрах верна. Про ВИД ОРГАНОВ УПРАВЛЕНИЯ критерий не
спрашивал, и гейт промолчал бы. Пятый случай за арку, когда критерий проверяет не тот
признак, каким владелец судит.
⇒ Вводя в кадр НОВЫЙ элемент интерфейса (плеер, кнопки, скроллбар), критерий обязан
спрашивать про его вид отдельно — «геометрия верна» и «выглядит как наше» разные вопросы.
ВЕРДИКТ:
ДОМ: disciplina/doma/meta

### Заход-дочистку выдали, не влив ветку, которую он дочищает — и он не смог проверить собственную работу
`dochistka-pleera-i-vitriny` чинит плеер у видео-слайдов, а сами видео-слайды живут в
ветке `slajdy-media`, которую приёмка приняла, но не влила (из песочницы Cowork влитие
невозможно). Ветка дочистки отпочковалась от основной и получила деку БЕЗ видео: 23 слайда
против 27. Исполнитель остался без единого реального объекта для проверки своей правки и
не мог опубликовать результат — публикация из его ветки СНЕСЛА БЫ с витрины уже стоящие там
видео. Он вышел из положения честно: воспроизвёл правку через devtools поверх живой витрины,
снял кадр и прямо написал, что это не кадр из его сборки.
ЦЕНА: клауза 1 критерия («проверяется НА ЖИВОЙ ВИТРИНЕ после публикации») оказалась
невыполнимой по построению — заход не имеет права публиковать, влитие есть ход приёмки.
Критерий требовал того, что заходу запрещено. Три ветки стоят невлитыми, и ни одна правка
трёх заходов подряд не видна ни на витрине, ни в главной папке — владелец второй раз подряд
не находит сделанную работу.
⇒ Заход, чей предмет лежит в невлитой ветке, НЕ ВЫДАЁТСЯ, пока та не влита. Проверяется
одной командой при сборке: `git branch --no-merged <база>` — если предмет захода там,
сначала влитие, потом заход.
ВЕРДИКТ:
ДОМ: disciplina/doma/meta

### Сторож невлитых веток слеп по построению: `lstrip('* ')` не снимает `+`, которым git метит worktree
`nevlityje_vetki()` в `bootstrap_zahod.py` снимает префикс строки git как `lstrip('* ')` —
звёздочка и пробел. Но `git branch --no-merged` помечает ЗНАКОМ ПЛЮС каждую ветку,
вычекаученную в worktree, а все заходы фабрики работают именно в worktree. Строка остаётся
`+ zahod/slajdy-media`, фильтр `startswith('zahod/')` её отвергает, список пуст — и в каждый
заход печатается «вливать нечего, невлитых zahod/*-веток было 0».
ЦЕНА: три захода подряд получили «вливать нечего» при двух-трёх реально невлитых ветках.
`dochistka-pleera-i-vitriny` чинил плеер видео-слайдов, не имея ни одного видео-слайда (дека
23 против 27), и не мог опубликовать работу. Владелец второй раз подряд не нашёл сделанное
на витрине и спросил прямо: «почему он первым делом не влил то, что должен был влить?».
Механизм заведён 2026-08-08 по его требованию «в генераторе должно быть записано вливание —
вначале всегда» и с перехода на worktree не сработал НИ РАЗУ.
🔴 Отдельно дорого то, что диагноз уже был под рукой и его прочли неверно: сам заход печатает
строку «14.08 заход нёс ровно этот ноль, а к прогону невлитых было три». Это списали на
устаревание снимка — то есть на СВОЙСТВО, объявленное честно, — вместо того чтобы проверить
фильтр. Ложное объяснение, лежащее рядом с настоящим, дороже отсутствия объяснения.
⇒ Проверка, читающая ВЫВОД git построчно, тестируется на выводе С ПРЕФИКСАМИ — `*` (текущая),
`+` (worktree), голое имя. Пара к любой такой правке — ловушка с этим выводом в фикстурах,
иначе починка регрессирует молча.
ВЕРДИКТ:
ДОМ: disciplina/doma/meta
