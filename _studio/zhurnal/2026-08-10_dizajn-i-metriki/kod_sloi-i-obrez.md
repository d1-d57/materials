# Канал исполнителя — sloi-i-obrez (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — правка Э2 (слой узора) требует решения по архитектуре слоёв, которое рождается по ходу: два прошлых захода чинили её механизмом, решавшим не ту задачу.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — решение по архитектуре слоёв рождается по ходу; см. шапку захода.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/sloi-i-obrez.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_sloi-i-obrez.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add sloi-i-obrez --branch zahod/sloi-i-obrez, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-10_dizajn-i-metriki`
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/beautiful-vigilant-euler/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Лекция `teorkat-vvedenie/L2` — HTML-дек 1440×810, собирается `_generator/sborka/deck.py` из карточек `teorkat-vvedenie/L2/slajdy/<имя>/slaid.md`. Прошлый этап: 2026-08-16 владелец посмотрел собранную деку глазами и вернул ЧЕТЫРЕ дефекта вида, два из которых чинились уже дважды и оба раза были приняты невыполненными. ЦЕЛЬ: закрыть все четыре так, чтобы каждый был доказан КАДРОМ, а не рассуждением о коде.

🔴 **ПОЧЕМУ ЭТОТ ЗАХОД СУЩЕСТВУЕТ — прочти прежде, чем чинить.** Дефекты 1 и 2 уже проходили через два захода (`polya-i-uzor`, `kadr-uzor-i-vmeshchenie`) и были закрыты отчётами «сделано». Разбор 16.08 показал, ЧЕМ они закрывались: счётом углов в DOM («7/7 открыто»), грепом на отсутствие правила и рассуждением о порядке слоёв CSS. Ни одного кадра, который владелец мог бы открыть, предъявлено не было — два снятых кадра лежали на невлитой ветке. **Поэтому здесь критерий готовности требует кадров, и отчёт без них не принимается, каким бы убедительным ни было объяснение в коде.**

Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-15) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. узор рисуется ОТДЕЛЬНЫМ слоем поверх зелёной подложки и под текстом; механизм-гаситель cornerBlocked сносится — он решал не ту задачу
2. текст НЕ обрезается: нижняя линия есть часть фона, текст идёт поверх неё
3. deck.py обязан звать гейт вмещения сам, и гейт мерит ВИДИМЫЙ обрез, а не запас бокса
4. на слайдах-разделителях появляется номер раздела
5. требование про ВИД закрывается только кадром, снятым на влитой ветке — счёт в DOM и греп доказательством не считаются

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/sloi-i-obrez`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/sloi-i-obrez`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add sloi-i-obrez --branch zahod/sloi-i-obrez
  cd ../materials-wt/sloi-i-obrez
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/skeleton/,` `_generator/sborka/,` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/,` `teorkat-vvedenie/L2/dist/,` `sayt/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone _generator/skeleton/, --zone _generator/sborka/, --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/ --kommitit 'правки движка и скелета своей зоны + кадры до/после в kadry/' --zakryt 'свою ветку не гасить — гасит приёмка; чужие ветки и worktree не трогать'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/sloi-i-obrez`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/skeleton/,` `_generator/sborka/,` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/,` `teorkat-vvedenie/L2/dist/,` `sayt/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, поимённо, и ничего сверх:
  - `_generator/skeleton/base.css` — раздел с `.slide` и `.zone` (там `overflow:hidden`, дефект 2)
  - `_generator/skeleton/uzor.css` — целиком КРОМЕ двух длинных `--uzor-tr`/`--uzor-bl` (это base64-растр на ~92 КБ, читать его незачем; шапка файла объясняет устройство)
  - `_generator/skeleton/engine.js` — функция `paintBleed` и всё вокруг `cornerBlocked` (дефект 1)
  - `_generator/sborka/tipy.py` — `.zone.copy`, `_ill_zone`, порождение служебных слайдов (дефекты 2 и 4)
  - `_generator/sborka/deck.py` — точка, где собирается дек (дефект 3: сюда встраивается вызов гейта)
  - `_generator/sborka/gejt_vmeshcheniya.py` и `_generator/sborka/vmeshchenie.py` — что и как меряется сейчас (дефект 3)
  - `teorkat-vvedenie/L2/slajdy/fundamentalnaya-gruppa/slaid.md` — только шапку, это слайд-пострадавший дефекта 2
- 🔴 **ПРОВЕРЬ ПРЕДПОСЫЛКУ ЗАХОДА ПЕРВЫМ ХОДОМ.** Блок §0.1 выше напечатал «вливать нечего» — это снимок сборки, и он МОГ УСТАРЕТЬ в обе стороны. На 2026-08-15 невлитой была ветка `zahod/kadr-uzor-i-vmeshchenie` (её вливает соседний заход `kod_vlitie-i-publikaciya.md`, идущий ПЕРЕД тобой). Её правки — `engine.js`, `tipy.py`, `smeta.py` — лежат ровно в твоей зоне. Команда: `git --no-optional-locks branch --no-merged arka/mat-kostyak | grep zahod/`. **Ветка ещё не влита — остановись и напиши в `## ВОПРОСЫ`, не чини поверх неё:** иначе ты будешь чинить состояние, которого после влития не будет, и правки схлопнутся в конфликт.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Четыре правки. Порядок — по «заметность × дешевизна», а не по тяжести: Э1 стоит десять минут и виден сразу, Э3 — самый тяжёлый. **Делай в этом порядке и коммить по ходу.**

### Э1. Номер раздела на слайдах-разделителях
**Претензия владельца (2026-08-16, дословно):** «на разделителях между разделами я просил еще добавить новый… раздела».
**Адрес:** порождение служебных слайдов в `_generator/sborka/tipy.py` — раздел про разделитель (ищи по подстроке `razdelitel`, не по номеру строки).
**Что сделать:** на слайде-разделителе появляется номер раздела. Живые разделители лекции: `razdelitel-estestvennost`, `razdelitel-retrakty` (проверь командой `ls teorkat-vvedenie/L2/slajdy | grep razdelitel`, число не переписывай из захода).
**Ограничитель:** нумерация — сквозная по разделам лекции в порядке их следования в деке; изобретать оглавление, менять состав разделов или трогать обложку/финальный слайд НЕ надо.

### Э2. Текст не обрезается — нижняя линия есть ФОН
**Претензия владельца (2026-08-16, дословно):** «было четкое указание, чтобы нижняя линия… она часть фона, а текст поверх фона. Поэтому Z не должно обрезаться».
Раньше, 2026-08-15, он же про этот слайд: «она обрезается не потому, что она сама формула большая, потому что просто обрезан кусок снизу, поля какие-то снизу введены».
**Адрес:** `_generator/skeleton/base.css`, правило `.zone` — там `overflow: hidden`. Пострадавший слайд — `fundamentalnaya-gruppa` («Диск и окружность»), у него срезана последняя строка с формулой `ℤ → {e} → ℤ`.
**Что сделать:** текстовая зона перестаёт физически резать содержимое. Линия, отделяющая зону от нижнего поля, остаётся на месте как фон — текст идёт ПОВЕРХ неё.
🔴 **Чем это НЕ чинится, и это проверено счётом 16.08:** подгонкой геометрии. Предыдущий заход ужал нижнее поле 12px → 4px; смета после этого даёт по слайду запас **+0.08 строки** вместо −0.10 — то есть формально «влезает», а владелец на кадре из ЭТОЙ сборки видит обрез. Запас в сотую строки не является починкой. Чини клип.
**Ограничитель:** снимая `overflow`, не дай тексту убежать за кадр — `.slide` свой `overflow:hidden` сохраняет. Кегли в карточках НЕ трогай: подбор типографики — чужая территория (солвер), и владелец отложил его намеренно.

### Э3. Узор — отдельный слой ПОВЕРХ зелёной подложки и ПОД текстом
**Претензия владельца (2026-08-16, дословно):** «Видишь, как паттерн устроен на этом слайде. Нижняя часть есть, верхняя обрезана… вдоль нижней линии он нарисован, а дальше где начинается зеленая большая область иллюстраций — обрезается. Но мы давным-давно это запретили».
**Первоисточник требования (2026-08-15):** «сначала рисовался шаблон — условно бежевый верх, зеленый низ, потом прозрачный прямоугольник с непрозрачной иллюстрацией поверх, потом паттерн в углу». И там же: «если оставлять, то он должен полностью рисоваться».
**Адрес:** `_generator/skeleton/uzor.css` (узор объявлен как `background-image` на `.slide` и на `#bleed-uzor`) + `_generator/skeleton/engine.js`, функция `paintBleed` и предикат `cornerBlocked`.
**Диагноз, с которого начинать (проверь его сам, но не переоткрывай):** узор лежит фоном САМОГО слайда, а `.zone.board` — непрозрачная зелёная подложка-потомок. По устройству CSS фон потомка кладётся поверх фона предка всегда. Значит поверх зелёного узор при нынешней архитектуре нарисоваться НЕ МОЖЕТ, сколько ни правь `cornerBlocked`.
🔴 **Почему предыдущие два захода не помогли — назван корень, не повторяй его.** Слова владельца «он должен присутствовать, и ЕСЛИ И пропадать, то аккуратно, в районе пересечения с линиями» (условное разрешение) превратились в заголовке захода в «рвётся ТОЛЬКО о линии слайда», а в комментарии `uzor.css` — в «узор ОБЯЗАН рваться на пересечении с линиями слайда». Механизм `cornerBlocked` построен на этом искажении: он отвечает на вопрос «закрыт ли угол», тогда как требование — «рисуй поверх». Приёмка сама это записала 15.08 и механизм всё равно оставила.
**Что сделать:** узор переезжает в СОБСТВЕННЫЙ слой, который лежит выше фоновых зон и ниже текста и иллюстраций. Механизм-гаситель `cornerBlocked` сносится: он решал не ту задачу. Узор по-прежнему не рисуется поверх самой картинки (`.panel`) и поверх букв.
**Ограничитель:** base64-байты узора НЕ дублировать — они объявлены один раз через custom property, и повторное объявление стоит ~90 КБ на дек; шапка `uzor.css` это объясняет. Служебные слайды со сплошным фоном (обложка, финальный) перекрывают узор своим фоном намеренно — это поведение сохраняется.

### Э4. Гейт вмещения зовётся сборкой и мерит ВИДИМЫЙ обрез
**Решение владельца (2026-08-16):** «гейт в сборку».
**Адрес:** `_generator/sborka/deck.py` (точка вызова) и `_generator/sborka/gejt_vmeshcheniya.py` (что меряется).
**Диагноз:** гейт объявлен `called-by-hand`, `deck.py` его не зовёт — пересборка деки проходит, не проверяя ничего. Мерит он `fill = 100*scrollHeight/clientHeight` по зоне `.zone.t-body`, то есть переполнение бокса. После Э2 клипа больше нет, и эта мера станет молчать ровно там, где раньше хоть что-то значила.
**Что сделать:** (а) `deck.py` зовёт проверку сам при сборке; (б) мера — видимый обрез/вылет содержимого относительно кадра, а не запас бокса.
**Ограничитель:** гейт краснеет — сборка обязана СКАЗАТЬ и назвать слайды поимённо. Решать за владельца, ронять ли сборку в ошибку, ты не должен: если считаешь, что ронять надо, вынеси это в `## ВОПРОСЫ` развилкой, а по умолчанию печатай громко и продолжай. 🔴 **Прогони новый гейт по ЖИВОМУ ЭТАЛОНУ прежде, чем по своей работе.** Красное на здоровом эталоне — ложный гейт, а не больной эталон; такой гейт обходят и защита пропадает целиком.

### Э5. Пересобрать деку и выложить её на витрину — ПОСЛЕДНИМ ходом, после Э1–Э4
🔴 **Это тот же ход, что и починка, а не отдельная работа — и причина названа уроком, оплаченным вчера.** Правки движка влиты 15.08, а `teorkat-vvedenie/L2/dist/index.html` в git остался собранным ДО них (артефакт 17:42, источники 20:36). Владелец, открывший деку из репозитория, увидел бы состояние до починок и решил, что они не сработали — этот круг диагностики «того, чего не было» фабрика уже проходила 31.07.

**Решение владельца 2026-08-16, развилка закрыта, повторно не спрашивать** (полный текст — в заявке `_studio/zhurnal/_INFRA-git/zayavki/2026-08-15T1920-1-origin-2026-08-15-22.md`, строка `РЕШЕНИЕ ВЛАДЕЛЬЦА`):
- **Витрина двухступенчатая.** Дека кладётся СЮДА, в `sayt/static/teorkat/` этого репозитория. У товарищей (GitHub курса `formal-labs`) ставится только ССЫЛКА, и ставит её сам владелец — **в чужой репозиторий не лезь, доступа не проси, это не твоя работа**.
- **Имя файла — `lekciya-2.html`.** Ряд продолжается; вопрос «а если третьей лекции не будет» владелец закрыл словами «не будет — вообще не важно».
- **`lekciya-1.pdf` (1.0 МБ → 7.0 МБ) публикуется заодно, и разбираться в росте НЕ НАДО.** Владелец объяснил: дизайнеры прислали новую версию, он её обновил. Прошлый контур держал публикацию из-за этого файла — **блокер снят явным разрешением, держать больше нечего.**

**Что сделать:** пересобрать деку (`deck.py`), положить её в папку `sayt/static/teorkat/` под именем lekciya-2.html, вывезти `python3 _generator/tools/git_zona.py opublikovat --yes`.
**Ограничитель:** ссылку в индексе курса ставит владелец. Индекс `formal-labs` не трогай.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Все пять клауз обязательны.

1. **Дек собран, путь назван абсолютным** — `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html`, rc=0.
2. **Э1:** на КАЖДОМ слайде-разделителе виден номер раздела. Число разделителей снять командой `ls teorkat-vvedenie/L2/slajdy | grep -c razdelitel` и напечатать в отчёте вместе с вердиктом; вердикт несёт охват в себе — «номер есть на X из Y».
3. **Э2:** на слайде `fundamentalnaya-gruppa` формула `ℤ → {e} → ℤ` видна ЦЕЛИКОМ, ни один пиксель последней строки не срезан. **Кадр обязателен.**
4. **Э3:** на всех слайдах с зелёной подложкой узор в углу не разрезан границей зелёной зоны. Число таких слайдов снять командой (не переписывать из захода), вердикт — «узор целый на X из Y». **Кадры обязательны: минимум `retrakt` и `fundamentalnaya-gruppa` — те два, что владелец прислал 16.08.**
6. **Э5:** в папке `sayt/static/teorkat/` появился файл с именем lekciya-2.html (создаёшь его ты — сейчас его там нет), его размер и время совпадают со свежесобранной декой (`ls -la` по обоим путям — в отчёт), и он НЕ старше своих источников. Вывод `opublikovat` называет каждый уехавший файл поимённо.
5. **Э4:** `deck.py` зовёт гейт сам — доказательство: вывод сборки содержит строку гейта. Гейт прогнан по эталону ДО работы и не покраснел на здоровом; оба вывода — в отчёт.

🔴 **ЧЕМ ЭТОТ КРИТЕРИЙ ЗАКРЫВАЕТСЯ И ЧЕМ НЕ ЗАКРЫВАЕТСЯ — решение владельца 2026-08-16, а не пожелание.** Клаузы 3 и 4 закрываются ТОЛЬКО кадром: `.png`, снятый с СОБРАННОГО дека, положенный в `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` и названный в отчёте путём от корня. **Счёт элементов в DOM, `elementFromPoint`, греп по CSS и рассуждение о порядке слоёв доказательством НЕ считаются** — именно ими закрывались оба прошлых захода, и оба дефекта дожили до сегодня. Кадры кладутся в зону этого захода и коммитятся вместе с кодом: кадр на невлитой ветке владельцу недоступен — ровно это и случилось 15.08, он написал дословно: «артефакт посмотреть не могу, кажется не на влитой ветке».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «узор целый», а «узор целый, проверено X из Y».

🔴 **ВСЁ ВЫШЕ ПРОВЕРЯЕТСЯ ЖИВЫМ ПРОГОНОМ НА РЕАЛЬНОМ ОБЪЕКТЕ РЕПОЗИТОРИЯ — на лекции `teorkat-vvedenie/L2`, а не на фикстуре.** Зелёная фикстура — необходимое условие и недостаточное: она проверяет, что код не падает, а не что владелец увидит починку. Прогнал только фикстуру — критерий НЕ закрыт.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
🔴 **ВЕРИФИКАТОР ЗДЕСЬ ОБЯЗАТЕЛЕН, и он ПОСЛЕ-типа — свежий субагент, ДРУГОЙ метод.** Операция формально не лоссовая, но проверять надо не сохранность файлов, а ВИД, и автор правки — заинтересованное лицо ровно в том месте, где он уверен.

**Задание верификатору (сформулируй так, дословно по смыслу):** «Тебе даны кадры, снятые с собранного дека, и претензии владельца. Кода, которым это чинилось, ты НЕ читаешь и объяснений автора не получаешь. Ответь по каждой претензии: устранено или нет, и покажи на кадре, где смотришь. Претензии: (1) узор в углу разрезан границей зелёной зоны иллюстрации; (2) последняя строка формулы срезана нижней линией зоны; (3) на разделителе нет номера раздела».

🔴 **Верификатор смотрит КАДРЫ, а не код — это и есть весь смысл.** Оба прошлых захода имели верификаторов, и оба верификатора судили по DOM: «bl-угол ложно заблокирован 7/7, стал открыт 7/7». Вердикт был честным и дефект пережил его — потому что вопрос «открыт ли угол в DOM» и вопрос «видит ли владелец разрезанный узор» оказались разными вопросами.
**Финальной строкой верификатор обязан вернуть:** «выдано N позиций из M найденных».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/skeleton/ _generator/sborka/ _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/ teorkat-vvedenie/L2/dist/ sayt/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/skeleton/ _generator/sborka/ _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/ teorkat-vvedenie/L2/dist/ sayt/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/skeleton/,` `_generator/sborka/,` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/,` `teorkat-vvedenie/L2/dist/,` `sayt/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/skeleton/,` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/,` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### `bootstrap_zahod.py` выдаёт зону из НЕСКОЛЬКИХ префиксов, а `git_zona.py commit --zone` берёт ОДИН
Заход собран с зоной из пяти путей (`_generator/skeleton/`, `_generator/sborka/`,
`…/kadry/`, `teorkat-vvedenie/L2/dist/`, `sayt/`), и §4 велит закоммитить её ОДНИМ
коммитом. Но законный обход красного хука прописан только через дверь инструмента —
`git_zona.py commit --zone <зона> --no-verify "<причина>"`, — а `--zone` принимает
РОВНО ОДИН префикс (`in_zone(path, zone)` сравнивает с одной строкой). Голый
`git commit --no-verify` не годится: причина тогда не уедет в `INCIDENTY.md`, ради
чего дверь и заведена. Развести нечем: либо один коммит без записи причины, либо
запись причины и коммит, распавшийся на части.
ЦЕНА: в этом прогоне зона уехала ПЯТЬЮ коммитами вместо одного (`3f39a19`, `89ab4f6`,
`8a60f56`, `ff97d1b`, `fe67e8e`) — приёмке, читающей «коммит захода», придётся
собирать его из пяти. Повезло, что хук покраснел на одном префиксе из пяти: краснел
бы на всех, в `INCIDENTY.md` уехало бы пять одинаковых записей об одной причине, и
автолог, по которому фабрика считает боль с коммитами, соврал бы впятеро.

### Относительный путь в рабочей папке захода молча меряет ЧУЖОЙ файл — одноимённый, но старый
Заход живёт в `materials-wt/<зона>`, а рядом стоит главная папка, где по ТОМУ ЖЕ
относительному пути лежит ДРУГОЕ содержимое — состояние до захода. Одна команда у меня
унаследовала рабочую папку главного репозитория (её сменил предыдущий `cd`), и проверка
целостности узора, запущенная как `… teorkat-vvedenie/L2/dist/index.html`, измерила
СТАРУЮ деку. Ни ошибки, ни предупреждения: путь существует, файл читается, проверка
отрабатывает штатно и печатает аккуратное число.
ЦЕНА: в отчёт едва не уехало «узор цел на 9 из 22» про деку, где он цел на 22 из 22 —
то есть заход отчитался бы ПРОВАЛОМ собственной, работающей починки. Поймано только
потому, что число совпало с замером ДО подозрительно точно; никакой гейт этого не ловит.
Вывод для канона: в рабочей папке захода пути к артефактам — абсолютные, а не
относительные, и `pwd` печатается рядом с замером, иначе замер непроверяем.

### Новый гейт «по виду» обязан исключать поддеревья, спрятанные для скринридеров
Гейт Э4 мерит видимый обрез — прямоугольник содержимого против клип-предков. Первый
же прогон по ЗДОРОВОМУ эталону дал красное на 20 слайдах из 26 «обрезом справа на
300–780px». Резала не вёрстка: KaTeX кладёт рядом с каждой видимой формулой её
MathML-дубль для ассистивных технологий и прячет его коробкой 1×1px с
`overflow:hidden`. Внутри — полноразмерное дерево `mi/mo/mn`, формально обрезанное на
сотни пикселей, фактически невидимое никому. Признак «клипер меньше 3px по любой
стороне ⇒ спрятано, а не обрезано» лечит это структурно; называть `.katex-mathml`
по имени нельзя — следующая библиотека с тем же приёмом покрасит гейт снова.
ЦЕНА: без правила гейт краснел бы ВСЕГДА и на всём; такой гейт обходят целиком, и
защита пропадает не в одном месте, а вся. Поймано ровно тем требованием захода
«прогони по живому эталону ДО своей работы» — оно окупилось в этом же прогоне.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Предпосылка захода проверена и ВЕРНА:** `zahod/kadr-uzor-i-vmeshchenie` уже влита
(`git branch --no-merged arka/mat-kostyak | grep zahod/` → только `vid-blokov-vnedrenie`
и `vyvoz-i-ochered`, обе заняты живыми соседними заходами). Чинить поверх неё можно.

**Диагноз Э3 проверен КАДРОМ, а не рассуждением** (кадры «ДО» сняты с собранной деки):
· `fundamentalnaya-gruppa` — правый верхний угол сплошь зелёный, узора нет вовсе;
  bl-угол на беже нарисован. · `retrakt` — зеркально: tr-угол на беже есть, bl-угол
  съеден зелёной полосой. Ровно то, что описал владелец. Диагноз захода («фон потомка
  всегда поверх фона предка») подтверждён.

**🔴 ОСПАРИВАНИЕ КЛАУЗЫ 3 КРИТЕРИЯ — до работы, как велит §1.** Клауза требует, чтобы
формула `ℤ → {e} → ℤ` была видна целиком. На СВЕЖЕЙ сборке нынешних источников она уже
видна целиком: промер всех 22 текстовых слайдов даёт `scrollHeight-clientHeight = 0.0`
везде, а на `fundamentalnaya-gruppa` последний блок стоит на 14.7px ВЫШЕ кромки зоны.
То есть клаузу закрывает не моя правка, а геометрия предыдущего захода — владелец же
видел обрез потому, что в git лежит дека, собранная ДО той правки (Э5, артефакт 17:42
при источниках 20:36). **Клип я всё равно снимаю**: заход прав, что запас в сотую строки
починкой не является, и Э2 сформулирован структурно («зона перестаёт резать»), а не как
«сделай, чтобы влезло». Кадр по клаузе 3 предъявлю, но вердикт назову честно: обрез снят
структурно, а не был снят мной впервые.

**Э1.** Порядок слайдов в деке известен ДО компиляции (`slide_order` + `tip_by_sid`), а
`_order` порядок «середины» не меняет — значит номер раздела считается там же и уезжает
в `_compile_one` рядом с путём, а `razdelitel()` берёт его параметром. Номера нет
(одиночный рендер `slaid.py`) — разделитель рисуется как раньше.

**Э2.** `overflow` снимается у `.zone.copy` в `GLOBAL_CSS` (`tipy.py`), а НЕ у общего
`.zone` в `base.css`, хотя заход называет адресом второе. Причина названа в самом
`base.css:24`: тот файл инлайнит `build_deck.py:295` в деки buffon/dandelin/fibonacci —
снятие клипа у общего `.zone` поменяло бы вид трёх чужих деков, лежащих вне зоны захода.
`.zone.board` клип сохраняет (картинки), `.slide` сохраняет (ограничитель Э2).

**Э3.** Узор переезжает в собственный слой `.slide::after` (`z-index:1`), содержимое зон
поднимается на `z-index:2` — текст и `.panel` остаются поверх узора. base64 не дублируется:
слой читает те же `var(--uzor-tr/--uzor-bl)`, объявленные один раз. `cornerBlocked` со
всей парой `--uzor-*-corner` сносится. Взамен — ОДИН пер-слайдовый признак «у слайда узора
нет вовсе» (служебные со сплошным фоном), иначе снаружи кадра остался бы тот самый
полурисуемый узор. Гашение у служебных называется теперь ЯВНО, функцией `_sploshnoj_fon()`
рядом с самим фоном: раньше оно было побочным действием шортката `background:`, а слой
таким шорткатом не гасится.

**Э4.** В `gejt_vmeshcheniya.py` — второй режим `--dek <index.html>`: мера не «запас бокса»,
а ВИДИМЫЙ обрез — прямоугольник каждого текстового листа и каждой `.panel` против
ПЕРЕСЕЧЕНИЯ клип-прямоугольников всех предков, включая `.slide`. `deck.py` зовёт его сам
после записи файла; печатает громко и слайды поимённо, сборку не роняет (развилка — в
`## ВОПРОСЫ`). Прогон по живому эталону — по `do.html`, деке ДО правок.

**Э5.** Пересборка → `sayt/static/teorkat/lekciya-2.html` → `opublikovat --yes`.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

**РАЗВИЛКА, которую заход велел вынести сюда, а не решать за владельца (Э4, ограничитель).**
Гейт вмещения теперь зовётся сборкой. Сейчас он **печатает громко и называет слайды
поимённо, но сборку НЕ роняет** (`deck.py` возвращает rc=0 даже на красном гейте) —
это умолчание, названное в ограничителе. Ронять ли сборку в ошибку, решать вам.
· За «ронять»: красный гейт при rc=0 CI не остановит, и артефакт с обрезанным текстом
  спокойно уедет на витрину — ровно тот класс, из-за которого этот заход и существует.
· Против: сборка нужна и в разгар работы над карточками, когда обрез — промежуточное
  состояние; падающая сборка заставит носить флаг-глушилку, а глушилку однажды забудут
  снять. Плюс гейт требует Chrome, и на машине без него сборка стала бы падать вовсе
  (сейчас такой случай печатается предупреждением и не мешает).
Скажете «ронять» — правка на одну строку в `deck.py:main()`.

1. Смета Л2 не откалибрована: оценщик расходится с браузером на ТРЁХ карточках из 21
   (`centr-gruppy` −3.16, `dvojstvennoe-prostranstvo` −1.79, `itogi` −2.51 строки при
   допуске 1.50), из-за чего ловушка 38 фикстуры `sborka` красная и БЛОКИРУЕТ коммит
   в `_generator/sborka/` любому следующему заходу. Долг чужой и доказан: та же тройка
   с теми же числами воспроизводится на нетронутом HEAD (сверка прогнана копией
   `_generator` из `git show HEAD`, таблица совпала с моей побайтно). Сам гейт говорит
   «калибруй смету, слайды тут ни при чём». Калибровка сметы вне зоны этого захода.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет
2. Стартовое сообщение владельцу и §0.1 захода противоречат друг другу по ПОРЯДКУ
   первого хода. Стартовое: «Первым ходом: `git_zona.py worktree add …`, затем чтение
   захода». §0.1: «🔴 ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА… Сначала субагент вливает В
   ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку», и там же объяснено,
   зачем: ветка обязана отпочковаться от основной, которая уже всё содержит. Я исполнил
   стартовое сообщение (оно пришло раньше и звучит категорично) и завёл папку ДО
   субагента. В этот раз не стоило ничего — вливать было нечего, субагент влил 0 веток
   из 0. Но если бы вливать было что, я работал бы на ветке от устаревшей основной,
   то есть ровно в том дефекте, который §0.1 и заводился чинить. Цены нет, поэтому это
   не урок, а пункт очереди: `bootstrap_zahod.py` печатает оба текста и должен печатать
   их согласованно.
   ДОМ: _studio/zhurnal/2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет
3. Публикация увезла на витрину, помимо моего `lekciya-2.html` и разрешённого заходом
   `lekciya-1.pdf` (1.0→7.0 МБ), ещё и три строки в `lekciya-1.html`, заходом не
   названные. Проверено перед публикацией: правка чисто комментарная — в HTML-комментарии
   обновлён путь к канону (`_studio/konvejer/09-illustracii/SLOVAR-primitivov.md` →
   `_illustracii/DISCIPLINA.md`) после переименования дока. На вид не влияет, моей
   работой не является, приехала с историей ветки. Называю, потому что предпросмотр
   `opublikovat` показал её, а заход — нет.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged <основная>     # невлитые
git --no-optional-locks status --porcelain | wc -l        # не закоммичено
git --no-optional-locks log --oneline @{u}.. | wc -l      # не вывезено
python3 _generator/tools/git_zona.py zayavki              # открытые заявки
```
```
$ git --no-optional-locks branch --no-merged arka/mat-kostyak
+ zahod/vid-blokov-vnedrenie
+ zahod/vyvoz-i-ochered

$ git --no-optional-locks status --porcelain | wc -l
      15

$ git --no-optional-locks log --oneline @{u}.. | wc -l
       1
   (9a87062 vlitie-i-publikaciya: снесён дубль kod_vlitie-i-deploj.md (П3)…; upstream = origin/arka/mat-kostyak)

$ python3 _generator/tools/git_zona.py zayavki
Открытых заявок: 3
   · 2026-08-15T1544-2026-08-15-materials-studio-zhurnal  (0 ч, blokiruet)
   · 2026-08-15T1920-1-origin-2026-08-15-22  (0 ч, blokiruet)
   · 2026-08-15T2053-meta-disciplina-git-disciplina-md-git  (0 ч, blokiruet)
Охват: заявок открыто 3, постоянных исключений 1
```

**ЧТО СДЕЛАНО** *(с хэшами)*
· **Влито:** 0 веток из 0 названных. Основная ветка, снятая командой — `arka/mat-kostyak`.
  Невлитыми остались две, и обе законно: `zahod/vid-blokov-vnedrenie` (занята живой
  рабочей папкой `materials-wt/vid-blokov-vnedrenie`) и `zahod/vyvoz-i-ochered` (занята
  живой `materials-wt/vyvoz`) — рядом идут соседние заходы, трогать их ветки нельзя.
  Критичная для этого захода `zahod/kadr-uzor-i-vmeshchenie` уже влита — подтверждено
  её отсутствием в `--no-merged`.
· **Закоммичено (хвост Cowork):** `8519459` `_INFRA-git/INCIDENTY.md`; `fde9b9a`
  `2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md`; `2a518ef`
  `2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md`. `KARTA.md` и `VERDIKTY.md` на входе
  были чисты.
· **Вывезено:** заявка `2026-08-15T1920-1-origin-2026-08-15-22`, пункт П1 — 4 коммита
  уехали на `origin/arka/mat-kostyak`.
· **Погашено:** ничего; к гашению не было названо ни ветки, ни папки.
· **Заявки закрыты:** 0 из 3 (разбор каждой — строка 1 гит-контура в `## ОТЧЁТ`).

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`
Поимённо, что осталось и почему непроходимо правами контура:
1. `2026-08-15T1920-1-origin-2026-08-15-22` — пункт П2 «opublikovat деку L2» сама заявка
   велит исполнять ТОЛЬКО после этого захода. На момент контура заход ещё шёл. **Теперь
   исполнен мной** (Э5, прогон Actions зелёный) — заявку можно закрывать, но закрывает её
   контур/приёмка: мандата на очередь у исполнителя нет.
2. `2026-08-15T1544-2026-08-15-materials-studio-zhurnal` — вне мандата контура: просит
   коммитить СОДЕРЖАТЕЛЬНУЮ работу захода и второй репозиторий `disciplina/`. Нужна
   переадресация в `zayavki/na-zahod/`, но подкоманды `zayavka-peredat` в `git_zona.py`
   главной папки НЕТ — она существует только на невлитой `zahod/vyvoz-i-ochered`, то
   есть инструмента для законного действия физически нет на диске. Пятая пометка подряд.
3. `2026-08-15T2053-meta-disciplina-git-disciplina-md-git` — род `pravka-koda`, правка
   канона, не операция контура. Сверх того неисполнима как написана: просит вписать в
   канон `--help` двух подкоманд, которых в главной папке не существует.
Ни одна из трёх не «сложна» и не «не моя тема» — у каждой названа механическая причина.
*(`нет` законно — но ТОЛЬКО со списком поимённо: что осталось и почему это непроходимо ТВОИМИ
правами (чужая живая рабочая папка, нужно решение владельца, конфликт, обеих сторон которого
не понимаешь). «Сложно» и «не моя тема» причинами не являются. `нет` без списка = красный.)*

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/sloi-i-obrez/teorkat-vvedenie/L2/dist/index.html` — открывать браузером (двойной клик)
Он же опубликован на витрине: `/Users/ivanyakovlev/Documents/GitHub/materials-wt/sloi-i-obrez/sayt/static/teorkat/lekciya-2.html` (побайтно тот же файл, `cmp` rc=0).
Кадры-доказательства, восемь штук парами ДО/ПОСЛЕ, открывать просмотрщиком:
`/Users/ivanyakovlev/Documents/GitHub/materials-wt/sloi-i-obrez/_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/2026-08-15_sloi-i-obrez_{DO,POSLE}_{fundamentalnaya-gruppa,retrakt,razdelitel-estestvennost,razdelitel-retrakty}.png`

**КОММИТ:** зона уехала ПЯТЬЮ коммитами, а не одним — почему, см. `## УРОКИ ФАБРИКЕ` (первый урок):
`3f39a19` кадры · `89ab4f6` дека · `8a60f56` витрина · `ff97d1b` скелет · `fe67e8e` сборка.
`git_zona.py check --zone` по всем пяти путям зоны → ✅ ✅ ✅ ✅ ✅ (вывод ниже, в Г1).

---

### ЧТО СДЕЛАНО И ЗАЧЕМ

**Э1 — номер раздела.** `deck.py` считает сквозной номер по разделителям в порядке
дека и передаёт его в карточку (`_compile_one` берёт теперь пару «путь + номер»),
`tipy.razdelitel` рисует строку «Раздел N» над заголовком. Считает именно сборка, а не
карточка: «какой это по счёту раздел» есть свойство МЕСТА слайда в деке, и вписанный в
карточку номер разъехался бы при первой перестановке разделов.

**Э2 — текстовая зона перестала резать.** `.zone.copy{overflow:visible}` в `GLOBAL_CSS`.
🔴 Сделано НЕ по адресу, названному заходом (`base.css`, правило `.zone`), и это
осознанное отступление: `base.css` инлайнит `build_deck.py` в деки buffon/dandelin/
fibonacci (причина записана в самом `base.css:24`), и снятие клипа у общего `.zone`
поменяло бы вид трёх чужих деков вне моей зоны. `.zone.board` клип сохранила (картинке
за свою полосу незачем), `.slide{overflow:hidden}` цел — ограничитель Э2 соблюдён.

**Э3 — узор отдельным слоем.** Узор переехал из фона `.slide` в собственный слой
`.slide::after` (`z-index:1`), содержимое зон поднято на `z-index:2` ОДНИМ правилом по
структуре (`.slide .zone > *`), а не списком типов вёрстки — список пришлось бы
дополнять на каждый новый тип, и забытая строка означала бы узор поверх букв. base64 не
продублирован: слой читает те же `var(--uzor-tr/--uzor-bl)`, объявленные один раз
(вес дека 3.47 МБ, +90 КБ не появилось). `cornerBlocked` снесён вместе с парой
`--uzor-*-corner`; вместо него один пер-слайдовый признак «узора нет вовсе» — иначе
снаружи кадра остался бы полурисуемый узор, худший из трёх исходов по слову владельца.
Гашение у служебных названо ЯВНО (`_sploshnoj_fon` / `_bez_uzora`), потому что раньше
оно было побочным действием шортката `background:`, а слой таким шорткатом не гасится.

**Э4 — гейт в сборке.** Второй режим `gejt_vmeshcheniya.py --dek <index.html>`: мера —
видимый обрез, прямоугольник каждого содержательного элемента против ПЕРЕСЕЧЕНИЯ
клип-прямоугольников всех предков, включая `.slide`. `deck.py:main()` зовёт его сам.
Сборку не роняет (печатает громко, слайды поимённо) — развилка вынесена в `## ВОПРОСЫ`,
как велел ограничитель. Цена вызова: сборка 7.7 с → 10.3 с.

**Э5 — витрина.** Дека пересобрана, положена как `sayt/static/teorkat/lekciya-2.html`,
вывезена `git_zona.py opublikovat --yes`. Прогон Actions зелёный:
https://github.com/d1-d57/materials/actions/runs/31902712470
Уехало поимённо (вывод `opublikovat`): `static/teorkat/lekciya-2.html` (новый, 1666
строк), `static/teorkat/lekciya-1.pdf` (1057166 → 6999601 байт — тот самый файл, который
заход разрешил явно), `static/teorkat/lekciya-1.html` (3 строки — заходом не названы,
разобраны в `## ВОПРОСЫ` п.3: правка чисто комментарная, не моя).

### КАК ПРОВЕРЕНО

**Клауза 1 — дек собран.** `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html`
→ «собран дек: слайдов в деке 26, в резерве 0», следом строка гейта. rc=0.

**Клауза 2 — Э1, охват.** `ls teorkat-vvedenie/L2/slajdy | grep -c razdelitel` → **2**.
Номер раздела есть на **2 из 2**: «Раздел 1» на `razdelitel-estestvennost`, «Раздел 2»
на `razdelitel-retrakty` — видно на кадрах ПОСЛЕ, подтверждено верификатором отдельно.

**Клауза 3 — Э2, кадр.** `2026-08-15_sloi-i-obrez_POSLE_fundamentalnaya-gruppa.png`:
формула `ℤ → {e} → ℤ` видна целиком, нижний край чернил y=790 при кромке кадра y=810.
🔴 **Вердикт называю честно, и он не тот, которого ждал критерий.** Обрез на свежей
сборке НЕ ВОСПРОИЗВОДИТСЯ — ни до моей правки, ни после: промер всех 22 текстовых зон
даёт `scrollHeight − clientHeight = 0.0` везде, а на этом слайде последний блок стоял на
14.7px ВЫШЕ кромки зоны ещё на деке ДО. Владелец видел обрез потому, что открывал деку
из git, собранную ДО правки полей предыдущего захода (ровно дефект, который чинит Э5).
Клип я всё равно снял структурно, как велел заход: запас в сотую строки починкой не
является, и `overflow` у `.zone.copy` теперь `visible` (проверено computed-стилем).
Это оспаривание было записано в `## ПЛАН` ДО работы; верификатор, не читавший ни кода,
ни плана, пришёл к тому же выводу самостоятельно.

**Клауза 4 — Э3, охват.** Проверка прямая и воспроизводимая: для каждого слайда снимаются
четыре рендера, и сравниваются две маски чернил узора — «как есть» и «с прозрачной
подложкой». Если узор рисуется ПОВЕРХ подложки, множества совпадают; если подложка его
режет — вторая маска больше на срезанный кусок.
· дека ДО: **узор цел на 9 из 22** слайдов, где слой узора есть (разрезан на 13);
· дека ПОСЛЕ (отгруженная `dist/index.html`): **узор цел на 22 из 22**.
Слой погашен на 4 слайдах из 26 — `oblozhka`, `finalnyj`, оба разделителя: сплошной фон
и полнокадровая картинка, поведение сохранено намеренно.
Калибровка меры названа, чтобы вердикт можно было перепроверить: на дефектном слайде
срезано 55–60 % чернил, и при снижении порога различия остаток РАСТЁТ (1449 → 1598 px,
по 24–39 px в строке — сплошная полоса вдоль границы); на здоровом остаток <1 % и при
снижении порога ПАДАЕТ (79 → 23 px, максимум 4 px в строке — рассеянные точки). Это
разные явления: второе — контраст одних и тех же чернил на зелени против бежа, не срез.
Кадры обязательные предъявлены оба: `retrakt` и `fundamentalnaya-gruppa`, ДО и ПОСЛЕ.

**Клауза 5 — Э4.** `deck.py` зовёт гейт сам, доказательство — строка в выводе сборки:
`✅ гейт вмещения: видимого обреза нет, проверено 26 из 26 слайдов`.
Прогон по живому эталону ДО работы: **первый прогон покраснел на 20 слайдах из 26** —
и это был ложный гейт, а не больной эталон (KaTeX прячет MathML-дубль формулы в коробке
1×1px, внутри неё полноразмерное дерево формально «обрезано» на 300–780px). Мера
исправлена структурно, после чего эталон зелёный:
`✅ гейт вмещения: видимого обреза нет, проверено 26 из 26 слайдов`, rc=0.
Второе ложное срабатывание на эталоне — подпись `X` в SVG, торчавшая на 5.6px пустого
лидинга; допуск привязан к интерлиньяжу, а не к магическому числу.
**Гейт УМЕЕТ провалиться** (иначе он украшение): на деке с намеренно раздутым кеглём
одного слайда — `❌ ГЕЙТ ВМЕЩЕНИЯ КРАСНЫЙ… fundamentalnaya-gruppa обрезано снизу на
1992.0px — «span.mord.mathbb ⟨Z⟩»`, rc=1. Он назвал и слайд, и ту самую формулу.

**Клауза 6 — Э5, витрина.**
```
-rw-r--r--  3470392  Aug 15 21:50  teorkat-vvedenie/L2/dist/index.html
-rw-r--r--  3470392  Aug 15 21:51  sayt/static/teorkat/lekciya-2.html
```
`cmp` двух файлов → rc=0, побайтно одно и то же. Витрина 21:51:09, самый свежий источник
(карточки + скелет + сборка) 21:49:20 — артефакт новее источников, не старше.

**Сверх критерия — регрессии.** Попиксельно сличены ВСЕ 26 слайдов ДО и ПОСЛЕ. Изменения
только там, где чинилось: углы узора и две строки «Раздел N». Ноль изменённых пикселей
на 8 слайдах (это чисто текстовые — у них узор и так был цел). Консоль отгруженной деки
на всех 26 слайдах: **0 ошибок и предупреждений**.
🔴 Одну регрессию я внёс сам и поймал этой же сверкой: подняв полнокадровый фон обложки
над слоем узора, я накрыл им заголовки — 28400 изменённых пикселей в текстовой зоне
обложки и 25005 на финальном. Исправлено (обложке и финальному узор гасится, а не
поднимается картинка), после правки обе разницы — 0 пикселей. Причина и замер записаны
в код (`tipy._bez_uzora`, `uzor.css`), чтобы правку не «починили» обратно.

### ЧТО НЕ ТРОГАЛ
Кегли и типографику карточек (солвер — чужая территория, владелец отложил намеренно).
`base.css` — правило `.zone` осталось как было, см. Э2 выше. Индекс курса `formal-labs`
и вообще чужой репозиторий. Смету и её калибровку (см. `## ВОПРОСЫ` п.1). Резервные
слайды. Чужие ветки и рабочие папки.
**Найдено вне зоны, не тронуто:** `_generator/tools/fixtures/sborka/zhivoj-teorkat/dist/index.html`
— замороженный снимок деки, внутри которого лежит СТАРЫЙ `engine.js` с `cornerBlocked` и
парой `--uzor-*-corner`. Фикстура на то и заморожена, но теперь она содержит механизм,
которого в движке больше нет; если её кто-то читает как образец — прочтёт неправду.

### ПОВТОРЯЕМОСТЬ НАХОДОК
· **Повторится на следующей лекции — значит заход, а не запись:** ничего. Все четыре
  дефекта чинились в ДВИЖКЕ (`skeleton/`, `sborka/`), а не в карточках Л2, поэтому Л3
  получит починку даром. Проверяемо: ни одна правка не упоминает `L2`.
· **Повторится на следующем ЗАХОДЕ:** долг сметы (`## ВОПРОСЫ` п.1) — он блокирует
  коммит в `_generator/sborka/` каждому следующему исполнителю и каждого заставит
  обходить хук. Это не пункт очереди, а работа ДО следующего прогона по этому пути.
· **Не повторится:** три строки в `lekciya-1.html` (разовый хвост переименования дока).
· Находок пробным прогоном, не починенных тут же, нет.

### НЕОБРАТИМОЕ
1. **Публикация витрины** · ветка `main` репозитория, коммит `964b4aae` · восстанавливается
   `git revert 964b4aae` на витрине с повторной публикацией. Сайт сейчас показывает
   `lekciya-2.html`, обновлённый `lekciya-1.pdf` и трёхстрочную правку комментария в
   `lekciya-1.html`. Действие названо заходом (Э5) и решением владельца 2026-08-16;
   предпросмотр без `--yes` я прогнал и сверил ДО публикации.
2. **Перезапись `teorkat-vvedenie/L2/dist/index.html`** · прежнее содержимое — в git,
   восстанавливается `git show 89ab4f6~1:teorkat-vvedenie/L2/dist/index.html`.
3. **Обход `pre-commit` на одном коммите** (`fe67e8e`) · причина записана в
   `INCIDENTY.md` инструментом, названа долгом в `## ВОПРОСЫ` п.1.
Больше необратимого нет: ни один файл не удалён, не переименован и не перемещён,
`git reset`/`checkout` поверх несохранённого не делался, за зону правки не выходили.

### ВЕРИФИКАТОР (§3) — свежий субагент, метод ДРУГОЙ: только кадры, кода не видел
Задание было дословно по смыслу из §3; кадры .png, запрет открывать `.css/.js/.py/.html`.
Вернул:
· **(1) узор, разрезанный границей зелёной зоны — устранено.** «`POSLE_fundamentalnaya`,
  правый верхний угол (≈x 1350–1440, y 0–130): узор нарисован ПОВЕРХ зелёной зоны и виден
  целиком; на `DO` в этом же прямоугольнике узора нет вообще». То же зеркально для
  `retrakt`, левый нижний угол. Сверх задания сверил геометрию: вырез угла на зелени и на
  беже «совпадает линия в линию».
· **(2) срезанная строка формулы — «дефекта на кадре нет, но и правки нет».** При
  увеличении 8× формула цела, а кадр ДО в этом месте «пиксель в пиксель тот же». Вывод
  его же: «либо претензия относилась к другому состоянию, либо по ней ничего не делали».
  🔴 Это независимое подтверждение оспаривания, записанного мной в `## ПЛАН` ДО работы:
  клауза 3 закрывается кадром, но обрез снят не мной и не сегодня.
· **(3) номер раздела — устранено**, «Раздел 1» и «Раздел 2», с координатами.
· **Регрессий не нашёл**, разница ДО/ПОСЛЕ локализована ровно в отремонтированных местах;
  «узор ни на одном кадре не лёг на буквы или на картинку».
Финальная строка охвата на месте: «выдано 4 позиций из 4 найденных».

### ГИГИЕНА Г1–Г6
**Г1.** `git_zona.py check --zone` по каждому пути зоны:
```
_generator/skeleton/                                ✅ работа доехала в git, вне git ничего нет
_generator/sborka/                                  ✅ работа доехала в git, вне git ничего нет
_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/  ✅ работа доехала в git, вне git ничего нет
teorkat-vvedenie/L2/dist/                           ✅ работа доехала в git, вне git ничего нет
sayt/                                               ✅ работа доехала в git, вне git ничего нет
```
**Г2. Неприменимо, потому что** все пять путей зоны лежат внутри репозитория `materials`;
за его пределы зона по ходу не расширялась (в чужой репозиторий `formal-labs` заход
запретил лезть, и я не лез).
**Г3.** `git branch --no-merged arka/mat-kostyak` → 3 против 2 на входе. Прибавилась ровно
одна и ровно моя — `zahod/sloi-i-obrez`; вливать её мне запрещено, вливает приёмка. Чужие
две (`vid-blokov-vnedrenie`, `vyvoz-i-ochered`) как были заняты живыми соседними заходами,
так и остались нетронуты.
**Г4. Неприменимо:** ни одного нового `.py` в `_generator/**` не заведено —
`git diff --diff-filter=A --name-only arka/mat-kostyak..HEAD -- '_generator/*.py'` → 0.
Оснастка прогона (съёмка именованных кадров, проверка целостности узора) осталась в
scratchpad и в репозиторий не клалась намеренно: это одноразовые пробы, а не инструменты
фабрики, и точки вызова у них нет.
**Г5. Неприменимо:** новых `.md` не заведено —
`git diff --diff-filter=A --name-only arka/mat-kostyak..HEAD -- '*.md'` → 0. Значит и
`register_doc.py` звать не пришлось, и `KARTA.md` в путях коммита не нужен.
**Г6.** `git log arka/mat-kostyak..HEAD --name-only` даёт 15 путей, и все пятнадцать —
мои: 5 файлов кода, 8 кадров, дека, витрина. Чужого нет ни одного.

### ВРЕМЯ ПРОГОНА И ТОКЕНЫ
Неприменимо: канал `app`. Лог прогона существует только на канале `terminal`, где его
кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет — снимать неоткуда.

### ГИТ-КОНТУР (вставлено ДОСЛОВНО из ответа субагента §0.1, шесть строк)
1. Заявок было 3, закрыто 0, застряло 3: `2026-08-15T1920-1-origin-2026-08-15-22` (П1
   «vyvezti на origin» ИСПОЛНЕН — 4 коммита вывезены; П2 «opublikovat деку L2» не
   исполнен, потому что сама заявка строкой «ОСТАЛОСЬ ДО ИСПОЛНЕНИЯ» велит публиковать
   только ПОСЛЕ захода `kod_sloi-i-obrez.md`, а он идёт прямо сейчас в
   `materials-wt/sloi-i-obrez`); `2026-08-15T1544-2026-08-15-materials-studio-zhurnal`
   (вне мандата, ПЯТАЯ пометка подряд — просит коммитить содержательную работу захода и
   второй репозиторий `disciplina/`; по правилу сторожа ей нужна переадресация в
   `zayavki/na-zahod/`, но подкоманды `zayavka-peredat` в `git_zona.py` главной папки НЕТ
   — она живёт только на невлитой `zahod/vyvoz-i-ochered`);
   `2026-08-15T2053-meta-disciplina-git-disciplina-md-git` (РОД `pravka-koda`, правка
   канона — не операция контура; сверх того неисполнима: просит вписать в канон `--help`
   двух подкоманд, которых в главной папке не существует, ловушка 6 покраснела бы наоборот).
2. Влито в основную 0 веток из 0 названных. Основная ветка, снятая командой:
   **`arka/mat-kostyak`**. Невлитыми числятся ровно две, обе подтверждены командой и обе
   НЕ тронуты: `zahod/vid-blokov-vnedrenie` — занята живой рабочей папкой
   `materials-wt/vid-blokov-vnedrenie`; `zahod/vyvoz-i-ochered` — занята живой
   `materials-wt/vyvoz`. `zahod/kadr-uzor-i-vmeshchenie` в `--no-merged` отсутствует, то
   есть уже влита — подтверждено.
3. Хвост Cowork ЗАБРАН тремя коммитами: `8519459` — `_studio/zhurnal/_INFRA-git/INCIDENTY.md`;
   `fde9b9a` — `_studio/zhurnal/2026-08-10_dizajn-i-metriki/UROKI-FABRIKE.md`; `2a518ef` —
   `_studio/zhurnal/2026-08-14_rychagi-zahodov/UROKI-FABRIKE.md` (`KARTA.md` и `VERDIKTY.md`
   были чисты на входе). Осталось вне git 12 путей (`git_zona.py check`, коммитит сам
   заход): `_generator/tools/sdelat_handoff.py`,
   `_studio/zhurnal/2026-08-10_dizajn-i-metriki/SESSIYA.md`,
   `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_sloi-i-obrez.md`,
   `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_vlitie-i-publikaciya.md`,
   `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_vyvoz-i-ochered.md`,
   `ucheniki/maksim/ZHURNAL.md`,
   `_studio/zhurnal/2026-08-10_dizajn-i-metriki/HANDOFF-2026-08-16.md`,
   `_studio/zhurnal/2026-08-14_rychagi-zahodov/AUDIT-BAZA-2026-08-15.md`,
   `_studio/zhurnal/2026-08-14_rychagi-zahodov/HANDOFF-2026-08-16-kachestvo.md`,
   `_studio/zhurnal/2026-08-14_rychagi-zahodov/TZ-desyatyj-skill.md`,
   `_studio/zhurnal/2026-08-14_rychagi-zahodov/kod_shablony-odin-dom.md`,
   `ucheniki/maksim/dz/dz-09-raskraska-invariant.html` — из них пять новых `.md` не
   зарегистрированы, за автора не регистрирую. После моей работы хвост не отрос: автологи
   и индекс чисты.
4. Погашено: ничего — к гашению не названо ни одной ветки и ни одной папки; свою
   `arka/mat-kostyak` гасит приёмка. Read-only замер на всякий:
   `poteri --branch zahod/dovodka-fazy-intervyu` rc=0, `zahod/uborka-pered-pauzoj` rc=0,
   `zahod/kadr-uzor-i-vmeshchenie` rc=0 — безопасны, но не названы, потому не тронуты.
   Все 10 рабочих папок в `materials-wt/` целы, включая `materials-wt/sloi-i-obrez` и
   `materials-wt/vyvoz`.
5. Необратимого нет. Три коммита (`8519459`, `fde9b9a`, `2a518ef`) и один `push` в
   `origin/arka/mat-kostyak` — коммиты откатываются `git revert`, вывоз на origin —
   обратной публикацией того же revert. Ни один файл не удалён и не перезаписан.
6. выдано 5 позиций из 5 найденных

*(🔴 Одно расхождение с пунктом 1 контура, называю сам: заявка
`2026-08-15T1920-1-origin-2026-08-15-22` осталась открытой на пункте «opublikovat деку
L2», потому что ждала ЭТОГО захода. Заход прошёл, публикация сделана — заявку теперь
можно закрывать, но закрывает её контур/приёмка, не я: у меня нет мандата на очередь.)*
