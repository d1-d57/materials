# Канал исполнителя — sluzhebnye-slajdy (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — геометрия взята из готового шаблона числами, решения владельца названы; изобретать дизайн не требуется.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Sonnet 5 — <одна фраза почему; см. шапку захода>.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/sluzhebnye-slajdy.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_sluzhebnye-slajdy.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: python3 _generator/tools/git_zona.py worktree add sluzhebnye-slajdy --branch zahod/sluzhebnye-slajdy, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py _studio/zhurnal/2026-08-10_dizajn-i-metriki`
Область: «_studio/zhurnal/2026-08-10_dizajn-i-metriki» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/nifty-keen-mccarthy/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Генератор собирает лекции-презентации 1440×810. Содержательные слайды за последние
заходы приведены в порядок: типология, сцены из блоков, вид блоков, связи, поля кадра.
**Служебные слайды остались как были** — обложка, визитка и финальный сделаны нашей собственной
вёрсткой, разделители разделов — третьей.

Владелец получил от дизайнеров beamer-тему своего курса и посмотрел её:
**обложка и финальный слайд у них свёрстаны иначе, и эта вёрстка ему нравится больше нашей.**
Тема целиком лежит в арке: `_studio/zhurnal/2026-08-10_dizajn-i-metriki/shablon-dizajnerov/`
(файл темы `beamerthemeCategoryTheoryFixed.sty`, ассеты в `assets/`, пример
`category-theory-lecture-1-fixed.tex`).

Их устройство обложки — **две зоны**: слева бежевое поле, где стоит текст; справа зелёная
панель примерно в треть кадра с фирменной диаграммой (`assets/cover.png`, 1672×941 — фон
на весь кадр, панель нарисована прямо в нём). Финальный слайд — тот же фон, слева по центру
вертикали «СПАСИБО ЗА ВНИМАНИЕ». Геометрия в `.sty` задана числами
(`\LectureCoverFrame`, `\LectureClosingFrame`, район строк 490–535): смещения от угла
в сантиметрах, кегли 31/16/10.5 pt, цвета `CTPlum`/`CTCharcoal`/`CTSageDark`.

ЦЕЛЬ: служебные слайды говорят одним языком — языком дизайнеров.

Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **СТОП ДО ЦЕЛИ:** ты НЕ трогаешь содержательные слайды, типологию, сцены, связи и солвер
как устройство. Общий источник констант (палитра, шкала кегля) — **отдельная будущая арка**,
её здесь НЕ начинать: цвета берёшь из существующих токенов, новых домов не заводишь.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-15) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. владелец посмотрел шаблон дизайнеров и решил: их вёрстка обложки и финального слайда берётся как есть
2. разделители разделов переделываются под ТОТ ЖЕ формат, что обложка — это прямое решение владельца
3. QR с визитки убирается совсем
4. визитка перестаёт быть жёсткой вёрсткой и доводится солвером, как обычный слайд

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/sluzhebnye-slajdy`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/sluzhebnye-slajdy`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add sluzhebnye-slajdy --branch zahod/sluzhebnye-slajdy
  cd ../materials-wt/sluzhebnye-slajdy
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/sborka/tipy.py` `_generator/sborka/deck.py` `_generator/skeleton/sluzhebnye/` `_generator/skeleton/illustrations/` `_generator/skeleton/base.css` `_generator/sborka/smeta.py` `_generator/tools/fixtures/sborka/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone _generator/sborka/tipy.py --zone _generator/sborka/deck.py --zone _generator/skeleton/sluzhebnye/ --zone _generator/skeleton/illustrations/ --zone _generator/skeleton/base.css --zone _generator/sborka/smeta.py --zone _generator/tools/fixtures/sborka/ --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/ --kommitit 'правки служебных типов, ассеты и кадры — своей зоной, хэш в отчёт' --zakryt 'ветку zahod/sluzhebnye-slajdy не гасить — вливает приёмка'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-15, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/sluzhebnye-slajdy`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/sborka/tipy.py` `_generator/sborka/deck.py` `_generator/skeleton/sluzhebnye/` `_generator/skeleton/illustrations/` `_generator/skeleton/base.css` `_generator/sborka/smeta.py` `_generator/tools/fixtures/sborka/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО это. Проект не изучай, ничего сверх списка не открывай:
  1. `_studio/zhurnal/2026-08-10_dizajn-i-metriki/shablon-dizajnerov/beamerthemeCategoryTheoryFixed.sty`
     — **главный вход**: геометрия обложки и финального слайда числами.
  2. `_studio/zhurnal/2026-08-10_dizajn-i-metriki/shablon-dizajnerov/assets/` — картинки
     (`cover.png` — фон обложки; `green-bg.png`, `tr-bg.png`, `bl-bg.png` — фоны панелей).
  3. `_generator/sborka/tipy.py` — наши нынешние `oblozhka`, `vizitka`, `finalnyj`, `razdelitel`.
  4. `_generator/skeleton/sluzhebnye/` — канон содержания служебных слайдов.
  5. `_generator/sborka/deck.py` — как служебные слайды порождаются и что из них идёт в подбор.
  6. `_generator/skeleton/tokens.css` — **READ-ONLY для новых цветов**: палитра уже несёт
     `CTIvory`/`CTCharcoal`/`CTPlum` под именами `--paper`/`--ink`/`--accent`, сверься с ней,
     а не заводи числа заново.
- 🔴 Адреса сняты 2026-08-15, проверяй грепом.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
## 🌙 РЕЖИМ — НОЧНОЙ И ДЕШЁВЫЙ

Времени сколько нужно, машина в распоряжении, токенов мало. Петля «правка → пересборка →
кадр → лог» после каждого шага; кадры складывай в `kadry/` штатным `_generator/tools/kadry.py`;
лог на диск. **Не изобретай там, где названо числом:** геометрия дизайнеров переносится,
а не переосмысляется.

### Э1. Обложка по геометрии дизайнеров

Перенести устройство: фон на весь кадр (`cover.png`), текст в левой зоне.
Числа `.sty` — в сантиметрах и пунктах; переведи в пиксели нашего кадра 1440×810 и **назови
коэффициент пересчёта в отчёте**, чтобы следующий заход не считал заново.
Слоты те же, что сегодня: название лекции, подзаголовок, дата.

⚠ `cover.png` 1672×941 — не наше соотношение. Реши, как сажать (растянуть, обрезать, вписать),
и назови решение; проверь на кадре, что диаграмма справа не искажена.

### Э2. Финальный слайд

Тот же фон, «СПАСИБО ЗА ВНИМАНИЕ» слева, по центру вертикали (`anchor=west` в `.sty`).

### Э3. 🔴 РАЗДЕЛИТЕЛИ — ПОД ТОТ ЖЕ ФОРМАТ. Это главное решение захода

Владелец: *«я бы и разделители разделов переделал под тот же формат, что обложка тут,
чтобы было поинтереснее»*.
⇒ Разделитель получает устройство обложки: заголовок раздела в левой зоне, панель справа.
Сегодня он свёрстан иначе (`tipy.razdelitel`: заголовок 56px сверху или по центру + иллюстрация).

**Что решить и назвать:** одна ли картинка на все разделители или своя на каждый. Дизайнеры дали
несколько фонов (`green-bg`, `tr-bg`, `bl-bg`) — **посмотри их и предложи**, но по умолчанию
бери одну: единообразие важнее разнообразия, а разнообразие добавляется дешевле, чем убирается.

### Э4. Визитка: QR убрать, довести солвером

Два решения владельца:
1. **QR с визитки убирается совсем.** Сегодня он выводится (`tipy.py`, район строки 404,
   `.p-qr` 250×250) — снять и слот, и вёрстку под него, освободившееся место отдать содержанию.
2. **Визитка перестаёт быть жёсткой вёрсткой и доводится солвером**, как обычный слайд:
   *«я бы её довёл до идеала с помощью солвера»*. Сегодня её раскладка задана числами
   (251×260, 250×250 и т.п.) и подбору не подлежит.
   ⚠ Проверь, что именно мешает: если визитка исключена из подбора списком в `deck.py` —
   это одна строка; если её вёрстка не переживает смену кегля — это работа, и тогда назови
   объём честно, а не сделай наполовину.

### Э5. Кадры и сборка

Пересними кадры служебных слайдов штатным скриншотером и положи в `kadry/`.
Собери деку в `teorkat-vvedenie/L2/dist/index.html` — **путь от корня репозитория**.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — шесть клауз)**

1. **Четыре типа служебных слайдов говорят одним языком:** обложка, финальный и разделители
   несут одно устройство (левая зона текста + панель справа). Кадры каждого — в `kadry/`,
   в отчёте названы файлы.
2. **Коэффициент пересчёта из `.sty` в пиксели назван числом** и применён единообразно;
   на кадре обложки диаграмма справа не искажена.
3. **QR не выводится нигде:** `grep -rn "qr" _generator/sborka/tipy.py _generator/skeleton/sluzhebnye/`
   — ни одной живой строки (исторические комментарии не в счёт, назови их числом).
4. **Визитка проходит через подбор** — либо показано, что солвер её теперь считает
   (вывод команды), либо честно названо, почему это работа большего объёма, с оценкой.
5. **Ничего не сломано:** переполнений не больше прежнего (замер до работы — 0 из 21),
   сумма сцен не упала (замер до работы — 55), `smeta.py --sverit` не хуже.
   🔴 **Сумму сцен проверь обязательно:** служебные слайды порождаются тем же кодом,
   и прошлый заход уже ронял сцены незаметно.
6. **Гейт инварианта «сцена = блок» зелёный**, фикстуры `sborka/` и `graf/` зелёные.

🔴 **Код возврата смотри ПЕРВЫМ** (`echo $?`), время собранной деки сверяй.

**Охват — внутри вердикта:** не «служебные слайды в порядке», а «проверено 4 типа из 4».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
**Нужен: старая вёрстка обложки, визитки и разделителя заменяется целиком — это лоссовая операция.**

**ПОСЛЕ-верификатор, свежий субагент, ДРУГОЙ метод:** собирает «до» из `git show <хэш-до>:`
и отвечает: **(а)** не потерялось ли содержание служебных слайдов (имя, роль, контакты,
названия разделов — посимвольно, а не на глаз); **(б)** совпадает ли новая геометрия
с `.sty` дизайнеров — сверить самостоятельно по числам, а не по описанию автора;
**(в)** не уехали ли содержательные слайды и сцены.

🔴 **Опровержение — полноценный успех.** ⚠ **Дек ПРОЛИСТАТЬ**, а не только померить:
дефекты вида в этой арке трижды находились только так.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/sborka/tipy.py _generator/sborka/deck.py _generator/skeleton/sluzhebnye/ _generator/skeleton/illustrations/ _generator/skeleton/base.css _generator/sborka/smeta.py _generator/tools/fixtures/sborka/ _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/sborka/tipy.py _generator/sborka/deck.py _generator/skeleton/sluzhebnye/ _generator/skeleton/illustrations/ _generator/skeleton/base.css _generator/sborka/smeta.py _generator/tools/fixtures/sborka/ _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/sborka/tipy.py` `_generator/sborka/deck.py` `_generator/skeleton/sluzhebnye/` `_generator/skeleton/illustrations/` `_generator/skeleton/base.css` `_generator/sborka/smeta.py` `_generator/tools/fixtures/sborka/` `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/sborka/tipy.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/deck.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/skeleton/sluzhebnye/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/skeleton/illustrations/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/skeleton/base.css` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/smeta.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/sborka/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**0. Гит-контур** — исполнен субагентом, шесть строк вставлены в `## ОТЧЁТ` дословно. Рабочая папка `materials-wt/sluzhebnye-slajdy`, ветка `zahod/sluzhebnye-slajdy`, зона чиста в HEAD (точка отката не нужна). Пересчёт `git --no-optional-locks branch --no-merged | grep -c 'zahod/'` дал 8 (не 0, снимок устарел) — печать, гейт не блокирует, вливать не моя задача (§0.1 п.2 — это работа субагента, уже выполнена).

**1. 🔴 НАХОДКА БЕЗОПАСНОСТИ, меняющая порядок работы:** обычный `deck.py <L2> -o …` (без `--bez-podbora`) молча переписывает `teorkat-vvedenie/L2/slajdy/*/slaid.md` — солвер видит расхождение отпечатка текста и живой правкой пишет новые kegl_px/liniya в 12 карточек, ЭТО ВНЕ МОЕЙ ЗОНЫ и не относится к задаче. Поймал на первом же пробном прогоне, откатил (`git checkout --` по этим 12 путям, `git status --porcelain` пуст, чужого следа не осталось). **Все прогоны deck.py в этом заходе — только с `--bez-podbora`**, кроме отдельного вызова солвера визитки (см. п.5в) — тот трогает ТОЛЬКО `sluzhebnye/vizitka.md`, файл в зоне.

**2. Коэффициент пересчёта — 90 px/см = 3,175 px/pt РОВНО** (90·2,54/72 = 3,175 точно). Источник: `aspectratio=169` беспримесного beamer даёт paperwidth=16см/paperheight=9см — 1440/16=90, 810/9=90, совпадает. Число НЕ изобретено — то же самое (3,175 px/pt) уже названо и используется в `_generator/skeleton/tokens.css` (READ-ONLY, вне зоны — не трогаю, только сверяюсь). Цвета — из СУЩЕСТВУЮЩИХ токенов: CTPlum=`var(--accent)`, CTCharcoal=`var(--ink)`, CTSageDark=`var(--steel)`, CTIvory≈`var(--paper)`, CTSage=`var(--board)` — новых не завожу.

**3. Живой конвейер для L2 — `_generator/sborka/deck.py` + `tipy.py`**, проверено грепом по `dist/index.html`: `id="oblozhka"`, `id="vizitka"`, `id="finalnyj"` (без префикса `sl-`). Геометрия Э1–Э3 идёт ТУДА.
   Находка вбок: `teorkat-vvedenie/src` (Л1, та же живая лекция курса) собирается ДРУГИМ, старым конвейером — `_generator/build_deck.py` (вне зоны, НЕ трогаю) + этот же `_generator/skeleton/sluzhebnye/` канон (`data-kind="cover"`, `#sl-vizitka`, `#sl-thanks` в `style.css`/`oblozhka.html`/`vizitka.html`/`final.html`). Я НЕ переношу геометрию дизайнеров туда (артефакт захода — только Л2, критерий не называет Л1, `build_deck.py` не в зоне на запись, риск живой регрессии Л1 без возможности пересобрать/сверить её здесь) — только убираю QR из этих файлов (см. п.5), т.к. решение владельца безусловное и грep-гейт §крит.3 бьёт по всей `sluzhebnye/`. Пункт про Л1 — в `## ВОПРОСЫ`.

**4. Обложка/финал — фон `cover.png` НАСТЕЖЬ (глобальный дефолт `tipy.oblozhka`/`tipy.finalnyj`, без параметризации через `brief.md`: он вне зоны, и сегодня этот конвейер держит только Л1/Л2 одного курса — блast-радиус посчитан, назван в `## ВОПРОСЫ` на будущее).**
   - `cover.png` 1672×941 vs холст 1440×810: 1672/941=1,77682, 16/9=1,77778 — расхождение 0,05%, растяжение `width:100%;height:100%` (то же, что `.sty`: `width=\paperwidth,height=\paperheight`) незаметно на глаз, проверю на кадре.
   - Слоты — ТОЛЬКО текст (заголовок/подзаголовок/дата), без иллюстрации: `.sty` не несёт слота под картинку у `LectureCoverFrame`/`LectureClosingFrame`, диаграмма впечатана в сам `cover.png`. `oblozhka_illustracii`/`finalnyj_illustracii` в живом `brief.md` Л2 останутся висеть неиспользуемыми — это поле чужой зоны (`brief.md`), не трогаю, называю в `## ВОПРОСЫ`.
   - Числа (90px/см, 3,175px/pt): title left 94.5px top 109.8px width 806.4px 98.4/120.6px (31/38pt) `var(--accent)`; subtitle left 97.2px top 468px 50.8/60.3px (16/19pt) `var(--ink)`; dateplace left 97.2px bottom 65.7px 33.3/41.3px (10.5/13pt) `var(--steel)`; финал — тот же фон, текст «СПАСИБО / ЗА ВНИМАНИЕ» left 97.2px, по центру вертикали, width 806.4px, 98.4/120.6px, `var(--accent)`, зазор строк 18px (.2cm).

**5. Разделитель — устройство обложки, но БЕЗ картинки-диаграммы.**
   - Проверил все три доп-фона: `green-bg.png` — плоский прямоугольник, замер пикселя (199,210,193)=`#C7D2C1` — **ровно** `var(--board)` (CTSage), уже наш токен; `tr-bg.png`/`bl-bg.png` — прозрачные УГЛОВЫЕ виньетки (тот же мотив, что уже несёт `CTDecoratedBackground`/паттерн в углу наших кадров), НЕ панели — не подходят под «панель справа». Решение: панель — просто `var(--board)`, БЕЗ встраивания картинки; левая зона — `var(--paper)` (совпадает с бежевым `cover.png`). Единообразно на все разделители (владелец: единообразие важнее разнообразия).

**6. Визитка (Э4).**
   - QR: убираю `.p-qr`/`qr_html`/`qr_raw` из `tipy.py`; из `skeleton/sluzhebnye/vizitka.html` — `<div class="panel p-qr">{QR}</div>`; из `style.css` — правило `.p-qr`. `vizitka-qr.html` **ОПУСТОШАЮ, не удаляю** — файл читает `build_deck.py:419-428` (вне зоны) БЕЗУСЛОВНО через `read_text()`; удаление уронило бы Л1 (`FileNotFoundError`), опустошение убирает QR по факту и держит контракт пути. Пустой файл не матчит `grep "qr"` — гейт §крит.3 чист.
   - Солвер: сегодня служебные слайды порождаются `deck.py:build()` ПОСЛЕ шага `_podobrat_tipografiku` и вообще не читаются с диска как карточка (`plan_sluzhebnyh` строит `params` в питоне) — это и есть причина исключения, не «одна строка списка». Чиню: завожу `sluzhebnye/vizitka.md` YAML-шапкой (`tip_verstki: vizitka`), делаю её настоящей карточкой для `formaty.parse_card`/`vmeshchenie.apply_to_card` (обе — чужие READ-ONLY функции, зову их как есть, не меняю). В `deck.py` — новая функция `_podobrat_vizitku()`: свой playwright-проход, свой HTML-обёртка (те же ассеты, что `slaid.compile_slide_html`, но через `render_body`, не `render_slide_blocks` — тело визитки НЕ несёт разметку блоков `### [tip]`, и совать его в парсер блоков нельзя), `vmeshchenie.podobrat_slide(page, html, axis=None)` + `apply_to_card`. Вызывается БЕЗУСЛОВНО (не за флагом `--bez-podbora`) — трогает только файл зоны, не карточки лекции; `reshenie_o_podbore` сама держит повтор дешёвым. `tipy.vizitka()` — копирую `_kegl_style(p)` в зону `.copy`, как у остальных типов.
   - ⚠ Находка: `teorkat-vvedenie/L2/brief.md` (вне зоны) несёт `vizitka_illustracii: vizitka-celikom` — визитка Л2 СЕГОДНЯ рисуется веткой-переопределением `tipy.vizitka()` (готовая иллюстрация во весь слайд), канон-ветка (фото+био, теперь без QR, теперь под солвером) в дек Л2 не попадает вовсе. Кадр канон-ветки сниму ОТДЕЛЬНО (не из `dist/index.html`) через `slaid.py`, назову в отчёте прямо, чтобы не выдать чужой кадр за критерий 1. Флип `brief.md`-поля — решение владельца о СОДЕРЖАНИИ Л2, не моё и не в зоне; в `## ВОПРОСЫ`.

**7. Базовые числа «не сломано» (замерены СЕЙЧАС, до правок, командами):**
   - Переполнения: `slaid.py` по всем 23 карточкам → `gejt_vmeshcheniya.py *.html` → `ГЕЙТ ЗЕЛЁНЫЙ: 21 слайд(ов) с текстом влезли (2 без текста)` — **0 из 21**, совпадает с числом файла-захода.
   - «Сумма сцен»: инструмент, печатавший эту строку в прошлом заходе (`_check_scena_ravna_bloku`/«СУММА СЦЕН ПО ЛЕКЦИИ») в живом `gejt_kartochki.py` и `kadry.py` БОЛЬШЕ НЕ СУЩЕСТВУЕТ (проверено грепом — ни одного совпадения). «55» не воспроизвёл. Считаю СВОИМ воспроизводимым способом: `deck.py L2 -o … --bez-podbora`, затем `grep -o 'data-scenes="[0-9]*"' <dist> | grep -o '[0-9]*' | awk '{s+=$1}'` → **28** (26 слайдов). Это и есть baseline «до»; после правок — та же команда. Расхождение с «55» — в `## ВОПРОСЫ`/уроки фабрике, не подгоняю.
   - `_generator/tools/fixtures/sborka/PROGNAT.sh`: КРАСНЫЙ на ловушке 38 ДО меня — расхождение сметы с браузером на 4 карточках (`anons-retraktov`, `centr-gruppy`, `itogi`, `vektornye-prostranstva`), сам гейт говорит «калибруй смету, слайды тут ни при чём» — чужой долг калибровки формулы, не служебные слайды, НЕ чиню (вне цели захода). После правок сверю: не появилось ли на ЭТОЙ ловушке или дальше НОВЫХ имён.
   - `_generator/tools/fixtures/graf/PROGNAT.sh`: ЗЕЛЁНЫЙ (rc=0) — контроль после правок тем же прогоном.

**8. Порядок исполнения:** Э1+Э2 вместе (общий фон/шрифты в tipy.py) → Э3 (razdelitel) → Э4 (QR+солвер, самое рискованное — отдельно, с проверкой на своей карточке) → Э5 (кадры + сборка `--bez-podbora`) → критерий готовности 6 клауз → верификатор (свежий субагент) → гигиена Г1-Г6 → коммит.

Критерий готовности не оспариваю — все шесть клауз вычислимы. Уточняю клаузу 2 («коэффициент назван») — он уже назван в §2 этого плана, число не изобретаю заново при исполнении.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. QR-код у визитки убран из канона (`_generator/skeleton/sluzhebnye/`, моя зона), но **живёт ВТОРОЙ раз** — впечатан в растровый файл кастомной иллюстрации `teorkat-vvedenie/L2/illustracii/vizitka-celikom/` (сама визитка Л2 сегодня рисуется ЭТИМ переопределением через `vizitka_illustracii: vizitka-celikom` в `brief.md`, канон-ветка в дек Л2 не попадает — см. кадр `L2_sluzhebnyj_vizitka-L2-override.png` против `L2_sluzhebnyj_vizitka-KANON-bez-QR-solver.png`). Решение владельца «QR убирается совсем» на эту картинку не распространилось — она вне моей зоны (`illustracii/` лекции, не `skeleton/illustrations/`).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. `teorkat-vvedenie/src` (Л1, та же лекция курса) собирается СТАРЫМ конвейером `_generator/build_deck.py` + тот же канон `_generator/skeleton/sluzhebnye/` (`data-kind="cover"`/`#sl-vizitka`/`#sl-thanks`, а не `tipy.py`). Геометрия дизайнеров в этом заходе перенесена ТОЛЬКО в `tipy.py` (конвейер Л2) — `build_deck.py` вне зоны на запись, а критерий/артефакт захода называют только Л2. Обложка и финал Л1 продолжают говорить СТАРЫМ языком (что и было, регрессии нет, но и переноса нет).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

3. Фон `cover.png` — глобальный дефолт `tipy.oblozhka`/`tipy.finalnyj` БЕЗ параметризации через `brief.md` (тот вне зоны). Сегодня этот конвейер держит только курс теоркат (Л1 старым путём, Л2 новым) — блast-радиус посчитан и безопасен. Если фабрика заведёт НОВЫЙ курс через тот же `_generator/sborka/deck.py`, он унаследует диаграмму теоркат по умолчанию, пока кто-то не заведёт параметризацию (поле в `brief.md` + чтение в `plan_sluzhebnyh`) — отдельный заход.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. Инструмент, печатавший «СУММА СЦЕН ПО ЛЕКЦИИ»/держащий инвариант «сцена=блок» с именем `_check_scena_ravna_bloku` (найден в прошлом заходе `kod_pravila-kadra.md`, там же родилось число «55» из шапки этого захода), в живых `gejt_kartochki.py`/`kadry.py` СЕГОДНЯ не существует — грепом ни одного совпадения. Число «55» не воспроизвелось; заменил своей воспроизводимой командой (`grep -o 'data-scenes="[0-9]*"' <dist> | ...` → 28 и ДО, и ПОСЛЕ правок, регрессии нет). Похоже на строку, осиротевшую при рефакторинге гейта, — не нашёл, ЧЕМ она заменена, если заменена вообще.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**ГИТ-КОНТУР §0.1 (субагент, дословно):**
1. заявок было 5, закрыто 2 (2026-08-15T0411-1-git-infra-git-kod-uborka; 2026-08-15T0422-15-08-git-handoff-2026-08), застряло 3 (2026-08-15T0121-zahod-pravila-kadra-2-arka-mat — грязный путь + живая рабочая папка + содержательный конфликт; 2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026 — не git-действие, правка gejt_kartochki.py; 2026-08-12T0359-tipy-slajdov-py-2-primer-3 — не git-действие, правка tipy_slajdov.py)
2. влито в основную 1 из 2 названных заявками веток (zahod/dokommit-i-hvosty — уже была полным предком HEAD, сливать было нечего, закрыта); zahod/pravila-kadra НЕ влита: `vlit-v-osnovnuyu` отказал (rc=1) — `_studio/docs/KARTA.md` в главной папке одновременно грязный и участвует в слиянии, плюс живая рабочая папка `materials-wt/pravila-kadra`, плюс 20 файлов требуют слияния «глазами» (совпадает с решением владельца из заявки T0411). Основная ветка, снятая командой: `arka/mat-kostyak`
3. забрать в git (`git_zona.py check`, 15 путей): новые — `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kod_sluzhebnye-slajdy.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/HANDOFF-2026-08-16.md`, `VYGRUZKA-2026-08-15-2.md`, `VYGRUZKA-2026-08-15-3.md`, `VYGRUZKA-2026-08-15-4.md`, `VYGRUZKA-2026-08-15-5.md`, `VYGRUZKA-2026-08-15.md`, `_studio/zhurnal/_INFRA-git/UROKI-FABRIKE.md`, `ucheniki/maksim/dz/dz-09-raskraska-invariant.html`; правленые — `_studio/docs/KARTA.md`, `_studio/zhurnal/2026-08-14_rychagi-zahodov/SESSIYA.md`, `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, `_studio/zhurnal/_INFRA-git/VERDIKTY.md`, `_studio/zhurnal/_INFRA-git/kod_dokommit-i-hvosty.md`, `ucheniki/maksim/ZHURNAL.md`
4. погашено: `zahod/dokommit-i-hvosty` (poteri rc=0 — потерь нет; zakryt-vetku rc=0 → `mogila/zahod/dokommit-i-hvosty`@423438d8). НЕ погашено: `zahod/pravila-kadra` — по прямому указанию захода («не гасить — вливает приёмка») и потому что не влита. `vyvezti` (предпросмотр) — вывозить было нечего, `arka/mat-kostyak` уже совпадает с `origin/arka/mat-kostyak`; второй репозиторий из «оба репозитория» (заявка T0422) вне зоны этого git-контура — не тронут
5. необратимого не было
6. выдано 20 позиций из 20 найденных

**ЧТО СДЕЛАНО + ЗАЧЕМ.**
- **Э1 (обложка) + Э2 (финал), `tipy.py: oblozhka()/finalnyj()`.** Фон `cover.png` во весь кадр (новый ассет `skeleton/sluzhebnye/cover-bg.html`, `<img>` с base64, читает `deck.py:plan_sluzhebnyh` один раз на оба типа). Текст — числа `.sty:\LectureCoverFrame`/`\LectureClosingFrame`, переведены в px коэффициентом 90 px/см = 3,175 px/pt (см. `## ПЛАН` п.2; тот же коэффициент уже несёт `tokens.css`, не изобретён). Слота под иллюстрацию больше нет — диаграмма впечатана в фон. Финал разбивает `zagolovok_na_ekrane` по первому пробелу + `text-transform:uppercase`, воспроизводя устройство `.sty` («СПАСИБО»/«ЗА ВНИМАНИЕ» двумя строками с зазором `.2cm`=18px), а не хардкодит чужой текст.
- **Э3 (разделители), `tipy.razdelitel()`.** То же устройство — левая зона `var(--paper)` + панель справа `var(--board)`. Ширина панели (465,9px = 32,36% кадра) снята по фактической границе беж/зелень в `cover.png` (PIL-скан пикселя, переход на x=1131 из 1672), не на глаз. Картинка НЕ встраивается: замер пикселя `green-bg.png` (199,210,193) дал **точное** совпадение с `var(--board)` (#C7D2C1); `tr-bg.png`/`bl-bg.png` оказались прозрачными угловыми виньетками, не панелями — не подошли под «панель справа», решение зафиксировано в коде.
- **Э4 (визитка), `tipy.vizitka()` + `deck.py`.** QR снят: `.p-qr`/`qr_html`/`qr_raw` — из `tipy.py`; `<div class="panel p-qr">{QR}</div>` — из `skeleton/sluzhebnye/vizitka.html`; правило `.p-qr` — из `style.css`; `vizitka-qr.html` **опустошён, не удалён** (файл безусловно читает `build_deck.py` вне зоны — удаление уронило бы сборку Л1). Освободившееся место отдано фото (260px→555px, до прежнего нижнего края QR-панели — не придуманное число). Визитка впервые проходит через СОЛВЕР типографики: `sluzhebnye/vizitka.md` получила YAML-шапку (`tip_verstki: vizitka`) и стала настоящей карточкой; новая `deck.py:_podobrat_vizitku()` гоняет её через `vmeshchenie.podobrat_slide`/`apply_to_card` ОТДЕЛЬНЫМ проходом (безусловно, не за `--bez-podbora` — трогает только файл зоны). Живой прогон: `подбор визитки: подбор впервые` → `kegl=41.0`, записано в шапку карточки, повторный прогон — `пропущен, вход не менялся` (идемпотентно).
- **Э5.** Дека собрана `_generator/sborka/deck.py teorkat-vvedenie/L2 -o teorkat-vvedenie/L2/dist/index.html --bez-podbora`. Кадры всех четырёх типов — в `kadry/` (пути ниже).

**КАК ПРОВЕРИЛ.**
- Кадры каждого из четырёх типов (АРТЕФАКТ ниже — сам дек; кадры — рядом, в этой же арке):
  `kadry/L2_sluzhebnyj_oblozhka.png`, `kadry/L2_sluzhebnyj_finalnyj.png`, `kadry/L2_sluzhebnyj_razdelitel-estestvennost.png` — все три из живого `dist/index.html` Л2;
  `kadry/L2_sluzhebnyj_vizitka-L2-override.png` — то, что СЕГОДНЯ реально в деке Л2 (переопределение иллюстрацией, канон не рисуется, см. `## ВОПРОСЫ` №1);
  `kadry/L2_sluzhebnyj_vizitka-KANON-bez-QR-solver.png` — ОТДЕЛЬНАЯ демонстрация канон-ветки визитки (не из `dist/index.html` Л2 — своя сборка через `slaid`-приёмы), фото+био без QR, кегль от солвера.
- **Крит.1 (один язык, кадры названы)** — четыре кадра выше, обложка/финал/разделитель — из ОДНОГО и того же `dist/index.html`, одинаковое устройство подтверждено глазами (левая зона + зелёная панель) и числами (см. крит.2).
- **Крит.2 (коэффициент назван, диаграмма не искажена)** — 90 px/см = 3,175 px/pt РОВНО (`## ПЛАН` п.2). cover.png 1672×941 (1,77682) против холста 16:9 (1,77778) — расхождение 0,05%, растяжение `width/height:100%` подтверждено на кадре обложки/финала: диаграмма не искажена на глаз.
- **Крит.3 (QR не выводится)** — `grep -rn "qr" _generator/sborka/tipy.py _generator/skeleton/sluzhebnye/` даёт РОВНО 3 срабатывания: 1 — исторический комментарий в `tipy.py` (объясняет, что убрано), 2 — случайные подстроки внутри BASE64 картинок (`cover-bg.html`, `vizitka-photo.html`; base64-алфавит регулярно содержит "qr" как шум, к QR-коду отношения не имеет). Живых строк про QR — 0.
- **Крит.4 (визитка через подбор)** — вывод команды выше (`подбор визитки: подбор впервые` → `kegl=41.0`); повторный прогон подтверждает идемпотентность.
- **Крит.5 (ничего не сломано)** — переполнения: `slaid.py` по всем 23 карточкам → `gejt_vmeshcheniya.py` → **0 из 21** ДО и ПОСЛЕ правок (числа идентичны, 2 без текстовой зоны — `razdelitel-*`). Сумма сцен (своя воспроизводимая команда, см. `## ПЛАН` п.7 — штатный инструмент с этим именем в живом коде не нашёлся): **28 ДО, 28 ПОСЛЕ** — не упала. `smeta.py --sverit teorkat-vvedenie/L2 --dopusk 999` — числа по всем 21 карточке побайтово совпали до и после (та же таблица расхождений, тот же гейт). `git diff HEAD -- teorkat-vvedenie/L2/slajdy/` — пуст и до коммита, и после.
- **Крит.6 (гейт сцена=блок, фикстуры sborka/graf)** — `_generator/tools/fixtures/graf/PROGNAT.sh` → ЗЕЛЁНЫЙ (rc=0) и до, и после. `_generator/tools/fixtures/sborka/PROGNAT.sh` → КРАСНЫЙ на ловушке 38 И ДО, И ПОСЛЕ, логи БАЙТ-В-БАЙТ идентичны (`diff` пуст) — чужой долг калибровки сметы (см. НЕОБРАТИМОЕ/КОММИТ ниже), новых регрессий нет. Инструмент с именем «инвариант сцена=блок»/`_check_scena_ravna_bloku` в живом `gejt_kartochki.py` не нашёлся (см. `## ВОПРОСЫ` №4) — заменил `graf`+`sborka` PROGNAT как ближайший вычислимый эквивалент, названный самим критерием.
- **ПОСЛЕ-верификатор** (свежий субагент, независимый прогон): (а) содержание — **9 из 9** текстовых элементов (5 буллетов визитки + 4 строки обложки/финала) совпали дословно; (б) геометрия — **18 из 18** числовых параметров `.sty` сошлись с кодом до пикселя (0 расхождений), плюс цвета CTPlum/CTCharcoal/CTSageDark сверены по hex; (в) содержание/сцены — `git diff` по зоне лекции пуст, 5 карточек сверены вручную, сумма сцен 28/28. Дек пролистан глазами (обложка/буллет-слайд/формулы/иллюстрация/многосценный слайд) — дефектов не найдено. Один пред-существующий (не мой) дефект НАЗВАН: мёртвые правила `#sl-vizitka`/`#sl-thanks`/`[data-kind=cover]` в `style.css` не матчат текущие id Л2 — это СТАРЫЙ факт (был так и в HEAD), не регрессия этого захода; я его не трогал (см. `## ПЛАН` п.3, конвейер Л1).

**ЧТО НЕ ТРОГАЛ.** Содержательные слайды/типологию/сцены/связи/солвер как устройство (не трогал файлы `slajdy/`, `bloki.py`, `vmeshchenie.py`, `formaty.py` — только ЗВАЛ их чужие функции). `_generator/build_deck.py` и конвейер Л1 (`teorkat-vvedenie/src`) — геометрия туда не перенесена, вне артефакта/критерия этого захода (см. `## ВОПРОСЫ` №2). `brief.md` любых лекций. Калибровку `smeta.py` (сама формула сметы, не число сравнения) — красная ловушка 38 чужая и не моя.

**ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ».** Все шесть клауз критерия закрыты; открытых пунктов, требующих обязательного возврата, нет. Четыре пункта в `## ВОПРОСЫ` — решения владельца/будущие заходы, не блокеры.

**ПОВТОРЯЕМОСТЬ находок.** QR в `vizitka-celikom.png` (## ВОПРОСЫ №1) — повторится РОВНО там же при следующей правке этой картинки (одна иллюстрация, не растущий список), это запись, а не заход. Отсутствие Л1-геометрии (№2) и непараметризованный `cover.png` (№3) — свойства АРХИТЕКТУРЫ, не повторяющаяся находка на «следующей единице работы» (следующий слайд/лекция их не зацепит, пока кто-то не соберёт новый курс тем же конвейером) — законно уходят в очередь. Осиротевший инструмент «сумма сцен» (№4) — тоже запись: следующий заход этой же ловушки не встретит, если сам не станет искать число «55».

🔴 **НЕОБРАТИМОЕ.**
1. `_generator/skeleton/sluzhebnye/vizitka-qr.html` — содержимое (base64 QR-картинки) стёрто, файл оставлен пустым (не удалён — контракт пути для `build_deck.py`). Восстановление: `git show 3e611063a7ad92a789099a5210fba3e28545051^:_generator/skeleton/sluzhebnye/vizitka-qr.html` (родитель коммита `3e61106`).
2. Предотвращённое, не случившееся: первый пробный прогон `deck.py teorkat-vvedenie/L2 -o ...` БЕЗ `--bez-podbora` переписал 12 файлов `teorkat-vvedenie/L2/slajdy/*/slaid.md` (солвер увидел расхождение отпечатка и молча подобрал новый kegl/liniya). Поймано ДО коммита, `git checkout --` по этим 12 путям вернул HEAD, `git status --porcelain` пуст — следа в истории нет вовсе. Называю строкой, т.к. правило требует называть даже предотвращённое действие, которое МОГЛО стать необратимым.
Больше необратимого не было.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/sluzhebnye-slajdy/teorkat-vvedenie/L2/dist/index.html` — открыть в браузере (файл собран `--bez-podbora`, живёт в рабочей папке захода, НЕ закоммичен — `dist/` вне зоны, см. `## ПЛАН` п.3; приёмка/владелец пересоберут той же командой при желании закоммиченной копии). Кадры четырёх типов служебных слайдов — рядом, в `_studio/zhurnal/2026-08-10_dizajn-i-metriki/kadry/L2_sluzhebnyj_*.png` (пять файлов, пути в разделе «КАК ПРОВЕРИЛ» выше).
**КОММИТ:** `3e611063a7ad92a789099a5210fba3e28545051` — «служебные слайды: геометрия дизайнеров курса (обложка/финал/разделители), QR убран из визитки, визитка доведена солвером» · `git_zona.py check --zone <каждый путь зоны>` → ✅ (все семь, см. «КАК ПРОВЕРИЛ»/Гигиена) · коммит сделан с `--no-verify` — ловушка 38 фикстуры `sborka/PROGNAT.sh` (чужой долг калибровки сметы, см. крит.6), причина записана автоматически в `INCIDENTY.md` инструментом `git_zona.py commit --no-verify`.

**ГИГИЕНА (Г1-Г6).**
- Г1 — ✅ все семь путей зоны (`git_zona.py check --zone <путь>` по каждому).
- Г2 — неприменимо, все пути внутри `materials` (тот же критерий, что С2).
- Г3 — `git branch --no-merged arka/mat-kostyak` → 9 (было 8 на снимке §0.1). +1 — моя же ветка `zahod/sluzhebnye-slajdy` с новым коммитом, законно не влита (контракт зоны: «вливает приёмка»).
- Г4 — неприменимо, новых `.py` не заводил (только правки существующих `tipy.py`/`deck.py`).
- Г5 — неприменимо, новых `.md` не заводил (`vizitka.md` существовал, это правка, не рождение файла).
- Г6 — ✅ `git show --stat 3e61106` несёт только семь путей зоны (см. «Э1…Э5» выше), посторонних нет.
