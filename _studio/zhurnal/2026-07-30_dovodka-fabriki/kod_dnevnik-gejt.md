# Канал исполнителя — dnevnik-gejt (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — механика расписана до уровня сигнатур команд и списка ловушек; плана, который рождался бы по ходу, здесь нет.

КОНТЕКСТ. Репозиторий `materials/` — фабрика, собирающая лекционные курсы. Над всеми её механизмами есть слой «сама сессия»: диалог владельца с аналитиком, из которого рождаются задания. Этот слой теряет знание. Независимая вычитка транскрипта 03.08 нашла **семь потерь за одну сессию** при том, что дневник вёлся по ходу и считался полным. Прошлый этап: заведены два инструмента — `zakryt_sessiyu.py` (выгружает реплики владельца из транскрипта) и `sdelat_handoff.py` (собирает хэндофф из параметров). **ЦЕЛЬ: закрыть последнее звено — сделать так, чтобы непопадание реплики владельца в дневник КРАСНЕЛО КОМАНДОЙ, а не зависело от внимательности аналитика.**
Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: получишь `dnevnik.py` с рабочей сверкой, но НЕ получишь регрессию на ловушки — тогда так и напиши, инструмент без фикстуры в коммит не пойдёт.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** отдельная рабочая папка (worktree), а НЕ основная. Заводится одной командой из корня репо: `python3 _generator/tools/git_zona.py worktree add dnevnik-gejt` — она создаст папку и поставит в ней ветку `zahod/dnevnik-gejt`. Дальше `cd` в неё и работай там. 🔴 **`git checkout` в основной папке НЕ делай:** она стоит на другой ветке, и checkout МОЛЧА откатит дерево под всеми, кто там работает (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/tools/dnevnik.py` `_generator/tools/zakryt_sessiyu.py` `_generator/tools/fixtures/sessiya/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dnevnik-gejt.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно». В частности `SESSIYA.md` арки ты **читаешь, но не пишешь**: дневник ведёт аналитик, ты строишь инструмент.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Заведи worktree командой из контракта выше и перейди в него. Проверь: `git branch --show-current` — обязано быть `zahod/dnevnik-gejt`.
- Точка отката: зона содержит **уже изменённый** `zakryt_sessiyu.py` (правки аналитика 03.08, см. §2 «что уже сделано»). Если он не чист в HEAD — сначала зафиксируй его отдельным коммитом, чтобы твои правки от чужих отличались.
- Прочитать ТОЛЬКО: `_generator/tools/zakryt_sessiyu.py` (222 строки, целиком — это твой главный якорь: формат транскрипта, разбор, эвристики), `_generator/tools/fixtures/sessiya/PROGNAT.sh` (образец фикстуры, которую расширяешь), `_generator/tools/check_tool_contract.py` — **только докстринг, первые 30 строк** (правила, которым обязан удовлетворять новый инструмент). Проект сверх этого не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Только stdlib.** Оба существующих инструмента дисциплины сессии на голом Python без зависимостей, и это условие их запуска на машине владельца. Новый — так же.

## 2. ЗАДАЧА

### Что уже сделано аналитиком 03.08 (не переделывать, но знать)

В `zakryt_sessiyu.py` закрыты две дыры, обе найдены на живом транскрипте:

1. **Поиск транскрипта не видел песочницу Cowork.** Искал по `~/.claude/projects/*/*.jsonl`, а в песочнице `~` — это `/sessions/<имя>/`, и папка смонтирована уровнем ниже, в `<mnt>/.claude/projects/`. Добавлены два шаблона. *Цена: аналитик объявил владельцу, что инструмент в Cowork не работает в принципе, и начал строить обходной путь. Владелец не поверил и оказался прав.*
2. 🔴 **Выгрузка отбрасывала целый класс реплик владельца.** Опознание стояло грепом `"tool_result" not in line[:400]`. Но ответы владельца на интервью через виджет приходят в транскрипт **именно как `tool_result`** с текстом `The user answered: …`. То есть терялись ровно те реплики, которые породила новая дисциплина. Переписано на структурное правило: `content` строкой — владелец набрал сам; `content` списком — результаты инструментов, и владелец там только если это ответ виджета (константа `OTVET_VIDZHETA`).

Прогон после правок на живом файле: **114 записей · 7 реплик владельца · 5 вопросов**, все три ответа виджета на месте.

### Что строишь ты

**A. `_generator/tools/dnevnik.py` — гейт полноты дневника.** Две подкоманды.

```
python3 _generator/tools/dnevnik.py proverit <папка-арки> [--transcript <путь.jsonl>]
python3 _generator/tools/dnevnik.py zapis    <папка-арки> --tekst "<текст>" [--repliki S1a2b-В03,S1a2b-В04]
```

- `proverit` — берёт транскрипт (функции `najti_transcript` и `razobrat` **импортируй из `zakryt_sessiyu.py`, не копируй**: параметр живёт ровно в одном месте, две копии разбора разъедутся на первом же изменении формата), нумерует реплики владельца, читает `SESSIYA.md` арки и печатает, каких реплик в дневнике нет.
- **Номер реплики обязан нести идентификатор сессии:** `S<первые 4 символа имени файла транскрипта>-В<NN>`. Без него нумерация двух сессий в одном дневнике сталкивается, и вторая сессия «покрывает» реплики первой своими номерами. Это тихая поломка ровно того вида, против которого инструмент и строится.
- **Покрытие определяется наличием номера в тексте `SESSIYA.md`** — где угодно, хоть в HTML-комментарии. Никакого «умного» сопоставления по смыслу: инструмент не решает, что важно (это главное проектное решение `zakryt_sessiyu.py`, читай его докстринг).
- **Вывод обязан нести ОХВАТ:** `покрыто N из M`. «Непокрытых нет» без числа — не вердикт: «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.
- **Непокрытые печатаются с номером и первыми ~300 символами** — чтобы аналитик разносил, не открывая транскрипт.
- **Exit 1 при непокрытых, exit 0 и МОЛЧАНИЕ при полном покрытии** (гейт, который шумит зря, отключат — `KONSTITUCIYA` Р31).
- `zapis` — дописывает запись в конец `SESSIYA.md` с датой и временем и со строкой-маркером покрытых реплик. Существующий текст дневника не переписывает и не переформатирует.

**B. Расширить фикстуру `_generator/tools/fixtures/sessiya/PROGNAT.sh`.** Охват в шапке (`TOOL-CONTRACT-COVERS`) дополнить `dnevnik.py`. Новые ловушки — **шесть**, каждая с комментарием «что ломается и чего это стоило»:

1. 🔴 **Реплика владельца, пришедшая как `tool_result` с `The user answered`, обязана попасть в выгрузку.** Прямая регрессия на дыру, найденную 03.08. Синтетический транскрипт из трёх строк: обычная реплика владельца, ответ виджета, чужой `tool_result` (результат `Read`). Ожидание: реплик владельца ровно **2**, чужой результат НЕ считается репликой.
2. **Транскрипт лежит по пути песочницы.** Временное дерево вида `<tmp>/mnt/.claude/projects/X/Y.jsonl`, `HOME=<tmp>` — `najti_transcript` обязан его найти. Регрессия на ложную тревогу «инструмент в Cowork не работает».
3. **Непокрытая реплика валит `proverit`:** дневник без номера реплики → exit 1, и в выводе стоит её номер.
4. **Полное покрытие:** тот же дневник с дописанным номером → exit 0 и пустой вывод.
5. **Столкновение сессий:** две реплики из РАЗНЫХ транскриптов не должны покрывать друг друга (проверяет, что идентификатор сессии в номере работает).
6. **Положительный контроль:** на здоровом входе `zapis` создаёт запись, и `proverit` после неё зеленеет. Без этого «краснеет всегда» неотличимо от «краснеет верно».

🔴 **Каждую ловушку обязан проверить мутацией:** временно откати ту строку инструмента, которую ловушка сторожит, и покажи в отчёте, что фикстура на этом краснеет. Ловушка, которая не краснела ни разу, ничего не сторожит — это надежда, а не гейт.

**C. Нашёл по дороге, чинить если дёшево** (не критерий, но назови в отчёте решение): `zakryt_sessiyu.py` пишет `VYGRUZKA-<дата>.md` и при втором закрытии в тот же день **молча перезатирает первое**. Длинные дни с двумя сессиями у этого проекта норма.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `sh _generator/tools/fixtures/sessiya/PROGNAT.sh` → `echo $?` даёт **0**, и в шапке `TOOL-CONTRACT-COVERS` стоит `dnevnik.py`.
2. Каждая из шести ловушек §B показана краснеющей под мутацией — **шесть строк в отчёте**, по одной на ловушку, с названием откаченной строки.
3. `python3 _generator/tools/check_tool_contract.py _generator/tools/dnevnik.py _generator/tools/zakryt_sessiyu.py` → зелёный.
4. `python3 _generator/tools/dnevnik.py proverit _studio/zhurnal/2026-07-30_dovodka-fabriki` отработал на ЖИВОМ транскрипте и напечатал строку `покрыто N из M` — числа в отчёт.
5. Зона закоммичена, `git_zona.py check --zone` → ✅.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y».

## 3. ВЕРИФИКАТОР
Нужен: инструмент строится против ТИХОЙ потери, а тихую потерю автор не видит по определению. Свежий субагент, другой метод: дай ему только живой транскрипт и `SESSIYA.md`, попроси **своим способом** назвать реплики владельца и сверь его список с тем, что печатает `dnevnik.py proverit`. Расхождение в любую сторону — в отчёт с разбором, чья сторона права.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/dnevnik.py _generator/tools/zakryt_sessiyu.py _generator/tools/fixtures/sessiya/ _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dnevnik-gejt.md
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/dnevnik.py _generator/tools/zakryt_sessiyu.py _generator/tools/fixtures/sessiya/ _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dnevnik-gejt.md
python3 _generator/tools/git_zona.py check --zone _generator/tools    # из корня репо; должен быть ✅
git --no-optional-locks show --stat                                   # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone _generator/tools \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Здесь артефакт — сам `dnevnik.py` плюс вывод прогона фикстуры. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы».*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Worktree заводится от ЗАКОММИЧЕННОГО состояния, а вход захода лежал незакоммиченным в основной папке
Контракт зоны требует worktree и запрещает `checkout` в основной папке. Но `git_zona.py worktree add` ставит ветку от HEAD, а обе вещи, на которые заход опирается, были не в HEAD, а в рабочем дереве ветки `arka/mat-kostyak`: правки `zakryt_sessiyu.py` от 03.08 и сам файл-заход. Исполнитель с пустым контекстом попадает в папку, где §0 говорит «зона содержит уже изменённый файл», а файла нет — и заметить это можно только сверив содержимое с основной папкой, чего заход не предписывает.
ЦЕНА: worktree-версия `zakryt_sessiyu.py` не содержала `OTVET_VIDZHETA` вовсе. Ловушка 1 (регрессия на дыру 03.08) строилась бы против дыры, которой в этом дереве нет, и была бы зелёной с первого дня, ничего не сторожа. Поймано сверкой `md5` первым ходом; стоило одного лишнего коммита-точки-отката и ~10 минут. Лекарство дешёвое: `bootstrap_zahod.py` / `git_zona.py worktree add` обязаны краснеть, если файл-заход или объявленные пути зоны отсутствуют в новой ветке.

### «Не переделывать сделанное аналитиком» и «критерий = зелёная фикстура» противоречили друг другу
§2 просит принять правки 03.08 как данность, критерий 1 требует зелёной `PROGNAT.sh`. Существующий положительный контроль фикстуры на этих правках был КРАСНЫМ — то есть выполнить оба указания было нельзя, и заход об этом не знал.
ЦЕНА: правило опознания реплики владельца пришлось переписать ТРИЖДЫ (str/list → типы блоков → origin + форма). Без фикстуры и верификатора гейт уехал бы в репо, объявляя 22 реплики владельца там, где их 18: пять ложных и одна потерянная. Ложные реплики дороже, чем кажется, — шумящий гейт отключают (Р31), и вместе с шумом уезжает вся польза.

### Синтетика фикстуры, не сверенная с живыми данными, задаёт ЛОЖНОЕ правило — и выглядит при этом как доказательство
Положительный контроль `norma.jsonl` подавал реплику владельца в форме «`content` списком без `origin`». Такой формы у владельца не бывает: замер по всем транскриптам обеих сред дал 18 822 записи роли `user` этой формы, и все до единой — результаты инструментов и служебный текст хоста. Фикстура при этом краснела «по делу», и первая моя починка подгоняла КОД под несуществующую форму.
ЦЕНА: та самая вторая редакция правила и пять ложных реплик на живом транскрипте. Общий вид урока: фикстура — это тоже утверждение о мире, и оно проверяется живыми данными ровно так же, как утверждение владельца (правило 4 корневого `CLAUDE.md` про предпосылки). Синтетический вход, не сверенный с транскриптом, доказывает только сам себя.

### Гейт построен, но его никто не зовёт
`dnevnik.py proverit` не подключён ни к хуку, ни к процедуре закрытия сессии — он запускается только руками. По `KONSTITUCIYA §11` это ещё не правило, а надежда.
ЦЕНА: измерима на предшественнике. `zakryt_sessiyu.py` заведён 03.08 и уже к вечеру того же дня существовал в двух несогласованных копиях (закоммиченная и рабочая), потому что его запуск тоже держался на памяти аналитика. Дневник арки на сегодня содержит НОЛЬ номеров реплик из 18 — гейт краснеет на первой же живой сверке, и это не поломка, а точная мера того, сколько разноса держалось на внимательности.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

### 0. Что уже выяснено первым ходом — два расхождения с входными данными

**Р1. Worktree пуст относительно зоны.** `git_zona.py worktree add dnevnik-gejt` завёл ветку от закоммиченного состояния, а обе вещи, на которые заход опирается, лежат **незакоммиченными в основной папке** на ветке `arka/mat-kostyak`:
- `zakryt_sessiyu.py` с правками аналитика 03.08 (`OTVET_VIDZHETA` — в worktree-версии его нет вовсе, `grep -c` даёт 0 против 2);
- **сам файл-заход** `kod_dnevnik-gejt.md` — в worktree его нет как файла.

Значит §0 «зона содержит уже изменённый `zakryt_sessiyu.py`» неверно для worktree. Действую по второй половине §0: забираю копией из основной папки и фиксирую **отдельным коммитом**, чтобы мои правки отличались от правок аналитика. Основную папку при этом не трогаю (только чтение).

**Р2. 🔴 Структурное правило аналитика ложно, и существующая фикстура это уже показывает.** Первый же коммит базовой версии остановлен хуком: он поднял `fixtures/sessiya/PROGNAT.sh` (правленый путь входит в её охват), и **положительный контроль покраснел**:
```
❌ в транскрипте norma.jsonl не найдено ни одной реплики владельца (2 записей, нераспознано 0)
```
Причина: правило «`content` строкой — владелец; `content` списком — результаты инструментов» отбрасывает роль `user` с `content` = списком **`text`-блоков**. Это не результат инструмента, это реплика владельца. Замер по живым транскриптам (40 файлов, `~/.claude/projects/*/*.jsonl`): у роли `user` встречаются три формы — `list:tool_result` 4300, `str` 252, **`list:text` 2**. Форма редкая, но существует, а сюда же попадает всё, что владелец отправляет с вложением (текст + картинка = список блоков).

⇒ Правка 03.08 закрыла одну дыру и открыла соседнюю **того же класса**: молчаливая потеря реплики владельца. Заход велит «не переделывать» сделанное аналитиком, но КРИТЕРИЙ 1 требует зелёной `PROGNAT.sh`, а зелёной она может быть только при исправном опознании. **Оспариваю предпосылку §2 «что уже сделано»: обе правки надо знать, но вторая не готова.** Чиню минимально — классификация по ТИПАМ блоков вместо `str`/`list`:
- `content` строкой → владелец;
- `content` списком, где есть блок не-`tool_result` (`text`, `image`) → владелец набрал сам;
- `content` списком из одних `tool_result` → владелец только при `OTVET_VIDZHETA`.
Замысел аналитика сохраняется целиком (ответы виджета внутри, чужие результаты снаружи), меняется одна ветка условия. Фикстуру под код НЕ подгоняю: её положительный контроль прав.

### 1. Порядок работы
1. Отдельный коммит базовой версии `zakryt_sessiyu.py` + файла-захода (точка отката). Требует правки из п.Р2, иначе хук не пустит — правку кладу тем же ходом и называю в отчёте.
2. `dnevnik.py`: `proverit` / `zapis`. `najti_transcript` и `razobrat` — **импортом** из `zakryt_sessiyu.py` (`importlib`, файл рядом), не копией. Номер реплики `S<4 символа имени транскрипта>-В<NN>`. Покрытие = подстрока номера в тексте `SESSIYA.md`. Вывод `покрыто N из M`; непокрытые — номер + ~300 символов; exit 1 при непокрытых, exit 0 и молчание при полном.
3. `PROGNAT.sh`: в `TOOL-CONTRACT-COVERS` добавить `dnevnik.py`, дописать шесть ловушек §B. Существующие семь не трогать.
4. **Мутационная проверка каждой из шести** — по одной откаченной строке на ловушку, шесть строк в отчёт.
5. `check_tool_contract.py` на оба инструмента (сейчас на существующих зелёный — калибровка снята).
6. Пункт C (перезапись `VYGRUZKA-<дата>.md` при втором закрытии в тот же день) — чинить, если укладывается в несколько строк; решение назвать в отчёте.
7. Верификатор §3 — свежий субагент, другой метод, сверка списка реплик.
8. Коммит зоны двумя ходами по §4 + `check --zone`.

### 2. Развилка, которую называю вслух до работы
Файл-заход физически существует в ДВУХ местах: основная папка (его открывает владелец, mtime 16:04, туда же пишет аналитик) и worktree (туда идёт коммит). **ПЛАН/ВОПРОСЫ/ОТЧЁТ пишу в основную папку** — иначе владелец их не увидит по тому пути, который сам назвал в стартовой строке; правки только дописывающие, в свои секции внизу. Перед коммитом копирую файл в worktree. Риск — аналитик правит файл параллельно; поэтому копирую один раз, последним ходом, и сверяю mtime.

### 3. Критерий готовности — принимаю, с одной поправкой
Пункты 1–5 оставляю как есть. Поправка к п.1: зелёная `PROGNAT.sh` **недостижима без правки `zakryt_sessiyu.py`**, которую §2 просит не делать. Считаю приоритет за критерием (он может провалиться, и он проверяем), правку делаю минимальной и выношу отдельной строкой отчёта.

## ВОПРОСЫ — (заполняет исполнитель)

1. **Гейт никем не вызывается.** `dnevnik.py proverit` запускается только руками. Куда его подключить — в `pre-commit` (краснеть на коммите в папку арки), в `zakryt_sessiyu.py` последним ходом, или он остаётся ручным инструментом закрытия сессии? Хук и процедура закрытия — вне моей зоны, поэтому не трогал.
2. **Дневник арки содержит НОЛЬ номеров реплик** — гейт на живом транскрипте даёт «покрыто 0 из 18». Проставлять ли номера задним числом за прошлые сессии (это работа аналитика, дневник мне read-only), или гейт считается включённым «с этой сессии вперёд»?
3. **Правило опознания реплики владельца переписано трижды** (см. ОТЧЁТ). Третья редакция опирается на поле `origin` транскрипта. Поле недокументированное и хостовое — если оно исчезнет, список форм придётся пересматривать снова. Принимаете такую зависимость или нужен более консервативный вариант (считать владельцем ВСЁ спорное и мириться с шумом)?
4. **Пороговое число в номере.** `S<4 символа>-В<NN>` при >99 репликах даёт `В100`; подстрочное совпадение я закрыл границей справа, но формат `:02d` на трёхзначных номерах перестаёт быть фиксированной ширины. Сессии на сотню реплик уже близко (18 за полсессии) — оставляем или переходим на `В<NNN>`?

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и ЗАЧЕМ

**1. `_generator/tools/dnevnik.py` — новый гейт, две подкоманды.**
`proverit` берёт транскрипт, нумерует реплики владельца, читает `SESSIYA.md` арки и печатает непокрытые. `zapis` дописывает запись в конец дневника с датой, временем и строкой-маркером разнесённых реплик. Реализовано ровно по §A: `najti_transcript` и `razobrat` **импортируются** из `zakryt_sessiyu.py` (не копия — иначе два разбора разъедутся молча); номер несёт идентификатор сессии `S<4 символа имени транскрипта>-В<NN>`; покрытие = наличие номера в тексте дневника, без «умного» сопоставления; вывод несёт охват `покрыто N из M`; непокрытые печатаются с номером и первыми 300 символами; exit 1 при непокрытых, exit 0 и **молчание** при полном покрытии.
Одно решение сверх задания, назову явно: проверка номера идёт регулярным выражением с границей справа, а не подстрокой. Без неё `S1a2b-В10` считался бы покрытым строкой `S1a2b-В100` — ложно-зелёный гейт на сессии в сотню реплик.

**2. 🔴 `zakryt_sessiyu.py` — опознание реплики владельца переписано. Это выход за «не переделывать», и вот почему он был неизбежен.**
Правки аналитика 03.08 роняли существующую фикстуру: её положительный контроль краснел, а критерий 1 требует зелёной фикстуры. Дальше каждая следующая редакция ловилась не рассуждением, а прогоном:
- *редакция 1 (аналитик):* «`content` строкой — владелец, списком — инструменты». Отбрасывала список текстовых блоков. Поймано **положительным контролем фикстуры**.
- *редакция 2 (моя первая):* «список с любым не-`tool_result` блоком — владелец». Дала на живом транскрипте **пять ложных реплик** (загрузка скилла, `[Request interrupted by user]`, `Continue from where you left off`, две подписи `[Image: …]`). Поймано **верификатором**.
- *редакция 3 (принятая):* два признака. `origin.kind == "human"` → владелец, чем бы ни был `content`; `origin` задан, но другой → служебная запись хоста, НЕ владелец; `origin` отсутствует (старый формат) → строка = владелец, список = только ответ виджета.
Одного признака не хватает ни в какую сторону, и это ЗАМЕРЕНО, а не предположено: `content` строкой есть у **203** записей `origin=task-notification` (не владелец), а поля `origin` нет вовсе у **733** настоящих реплик владельца в старых локальных транскриптах.

**3. 🔴 Два дефекта того же класса, найденные живым прогоном, а не разбором.**
- **Маркер виджета искался в любом месте текста.** Любой `Read` файла, который строку `The user answered` лишь *цитирует* (сам файл-заход, сам `zakryt_sessiyu.py`), засчитывался репликой владельца. Первый живой прогон `dnevnik.py` объявил 4 реплики вместо 1. Починено: маркер обязан стоять **в начале блока**; проверено на 5 живых ответах виджета в 4 транскриптах — позиция 0 всегда.
- **Порог `len(t) < 25` съедал короткие реплики владельца.** На живом транскрипте потерялась реплика «Опять упал, вот» (15 символов, со скриншотом падения коммита). Нашёл верификатор, не я. Порог — это ровно «скрипт решает, что важно», запрещённое докстрингом файла; для владельца снят, для аналитика оставлен.

**4. Фикстура `PROGNAT.sh`:** охват в шапке дополнен `dnevnik.py`; ловушка 0 (синтаксис 3.10) теперь гоняется и по `dnevnik.py` — охват её объявлял, а проверка мимо; добавлены шесть новых ловушек §B, каждая с комментарием «что ломается и чего стоило».

**5. Пункт C сделан** (дёшево — 5 строк): `VYGRUZKA-<дата>.md` при повторном закрытии в тот же день больше не перезатирается, второй файл получает суффикс `-2`. Под гейтом: проверяется довеском внутри ловушки 1.

### Как проверил

| Критерий | Команда | Результат |
|---|---|---|
| 1 | `sh _generator/tools/fixtures/sessiya/PROGNAT.sh` | **rc=0**, `ФИКСТУРЫ ЗЕЛЁНЫЕ`; в шапке `TOOL-CONTRACT-COVERS: zakryt_sessiyu.py sdelat_handoff.py dnevnik.py` |
| 2 | мутационный прогон | шесть строк ниже |
| 3 | `python3 _generator/tools/check_tool_contract.py _generator/tools/dnevnik.py _generator/tools/zakryt_sessiyu.py` | **rc=0**, «Контракт держится: проверено 2 из 2» |
| 4 | `python3 _generator/tools/dnevnik.py proverit _studio/zhurnal/2026-07-30_dovodka-fabriki` | **`покрыто 0 из 1`** на автоподобранном транскрипте (свежайший — транскрипт этого самого захода, в нём одна реплика владельца: стартовая строка). На транскрипте сессии аналитика 03.08 (`2e8f9b0f…jsonl`, 807 строк) против живого `SESSIYA.md`: **`покрыто 0 из 18`** |
| 5 | ниже, строка КОММИТ | ✅ |

Питон машины: **3.9.6** — прогоны выше сделаны на нём, не на новее.

**Мутационная проверка — шесть строк, по одной на ловушку.** Каждый раз откатывалась ровно одна сторожимая строка, гонялась фикстура, файл восстанавливался; базовый прогон до и после — зелёный.

1. **ловушка 1** — откачена константа `OTVET_VIDZHETA = "The user answered"` → `❌ ЛОВУШКА 1: реплик владельца не 3`
2. **ловушка 2** — откачен шаблон поиска `"~/mnt/.claude/projects/*/*.jsonl"` → `❌ ЛОВУШКА 2: транскрипт в песочнице НЕ найден` (и показательно: `najti_transcript` вернул вместо него ЧУЖОЙ настоящий транскрипт машины — ровно та молчаливая подмена источника, против которой ловушка и стоит)
3. **ловушка 3** — откачен `return 1` в конце `cmd_proverit` → `❌ ЛОВУШКА 3: дневник без номеров реплик дал exit 0`
4. **ловушка 4** — откачено молчание при полном покрытии (вставлен `print`) → `❌ ЛОВУШКА 4: при полном покрытии гейт шумит`
5. **ловушка 5** — откачен `id_sessii: return tr.stem[:4]` → `"aaaa"` → `❌ ЛОВУШКА 5: реплики ЧУЖОЙ сессии засчитаны покрытыми`
6. **ловушка 6** — откачен маркер разнесённых реплик в `zapis` → `❌ ЛОВУШКА 6: после zapis гейт всё равно красный`

Сверх шести — тем же способом доказаны три сторожа, добавленные в ловушку 1 по итогам верификатора: отсев служебного `origin`, снятый порог длины для владельца, маркер виджета в начале блока. Все три краснеют.

### Результат верификатора (§3)

Свежий субагент, другим методом (свой парсер, инструменты не открывал), на транскрипте сессии аналитика 03.08 и живом `SESSIYA.md`.

**Расхождение было и оно было существенным: верификатор 18, инструмент 22.** Разобрано по сырью — **прав верификатор по всем пяти позициям**:
- пять ложных реплик у инструмента: строки 83 (загрузка скилла), 530 (`[Request interrupted by user]`), 533 (`Continue from where you left off`), 710 и 772 (подписи `[Image: original …]`) — всё текстовые блоки роли `user`, порождённые хостом;
- одна потерянная реплика у ОБОИХ (у верификатора она нашлась, у инструмента нет): «Опять упал, вот», 15 символов — срезал порог длины.

Обе стороны расхождения починены, **после починки инструмент даёт ровно 18** — сошлось с верификатором точно. Верификатор дополнительно отметил 4 записи вида `Your questions have been answered: …(рекомендую)` — это автоподстановка рекомендованного варианта, а не текст владельца; согласен, репликами не считаю, но это пограничный случай, который стоит знать: решение там как бы принято, а слов владельца нет.

Верификатор подтвердил и пункт E: пометок вида `S<4 символа>-В<NN>` в `SESSIYA.md` **нет ни одной** (проверял и кириллическую `В`, и латинскую `B`) — то есть «0 из 18» это честный ноль, а не промах формата.

### Что НЕ трогал
- `SESSIYA.md` арки — только чтение, ни одной записи не сделал (дневник ведёт аналитик). `zapis` гонялся исключительно во временных папках фикстуры.
- Основную папку репозитория и её ветку `arka/mat-kostyak` — не переключал, не коммитил, файлы в ней не менял; читал `zakryt_sessiyu.py` и `SESSIYA.md`, писал только в свой файл-заход (см. НЕОБРАТИМОЕ).
- `sdelat_handoff.py`, `check_tool_contract.py`, хуки, `_studio/docs/` — вне зоны. Новых `.md` не заводил, поэтому `register_doc.py` и `KARTA.md` не требовались.
- Проект сверх названных заходом якорей не изучал.

### Открытое «возвращаться»
- Гейт ничем не вызывается автоматически (ВОПРОС 1) — до подключения это инструмент, а не гейт.
- Форма `origin` — хостовая и недокументированная (ВОПРОС 3). Если она изменится, разбор снова начнёт считать не то, и заметит это опять не автор, а верификатор.
- `zakryt_sessiyu.py` в основной папке остался в СТАРОЙ редакции (правки 03.08 без трёх починок) — он там незакоммичен, и я его не трогал. Пока ветки не сольют, у аналитика под рукой версия, которая на живом транскрипте даёт 22 вместо 18.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/dnevnik-gejt/_generator/tools/dnevnik.py` — открывать текстовым редактором; прогон, который его подтверждает: `sh /Users/ivanyakovlev/Documents/GitHub/materials-wt/dnevnik-gejt/_generator/tools/fixtures/sessiya/PROGNAT.sh` (ждать `ФИКСТУРЫ ЗЕЛЁНЫЕ`, `echo $?` → 0). Живая проверка: `python3 /Users/ivanyakovlev/Documents/GitHub/materials-wt/dnevnik-gejt/_generator/tools/dnevnik.py proverit /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-30_dovodka-fabriki`

### 🔴 НЕОБРАТИМОЕ — отдельным списком
1. **Перезапись** · `materials-wt/dnevnik-gejt/_generator/tools/zakryt_sessiyu.py` (worktree) — поверх версии из HEAD скопирована рабочая версия из основной папки · восстанавливается: `git show 1c5cf7f~1:_generator/tools/zakryt_sessiyu.py`
2. **Дописывание в файл вне worktree** · `materials/_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dnevnik-gejt.md` (основная папка, ветка `arka/mat-kostyak`) — дописаны секции ПЛАН / ВОПРОСЫ / ОТЧЁТ / УРОКИ ФАБРИКЕ, существующий текст не изменён · восстанавливается: `git show HEAD:_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dnevnik-gejt.md` — **но осторожно: файл там незакоммичен, в HEAD его нет вовсе**, так что откат = удаление моих секций вручную. Причина, по которой писал туда: владелец открывает файл по пути, названному в стартовой строке, а это основная папка.
3. **Правка существующего положительного контроля фикстуры** · `fixtures/sessiya/PROGNAT.sh`, синтетика `norma.jsonl` — реплике владельца добавлено `origin.kind = human` · восстанавливается: `git show 1c5cf7f:_generator/tools/fixtures/sessiya/PROGNAT.sh`. Это не косметика: старая форма увела разбор в ложное правило, обоснование замером — в комментарии над самой строкой.
4. Файлы `*.py.bak` мутационного прогона создавались и удалялись в том же ходе; после последнего прогона в зоне их нет.

**Времени и токенов не привожу — их снимает приёмка из `result`-строки лога прогона.**

**КОММИТ:** `002dbf8` — «dnevnik-gejt: гейт полноты дневника (proverit/zapis), шесть ловушек в фикстуре, починка опознания реплики владельца по origin + форме content» · 5 файлов, 673 вставки · ветка `zahod/dnevnik-gejt` в `/Users/ivanyakovlev/Documents/GitHub/materials-wt/dnevnik-gejt` · `git_zona.py check --zone _generator/tools` **из worktree** → ✅ «работа доехала в git, вне git ничего нет».
Перед ним — `1c5cf7f`, точка отката: правки аналитика 03.08, перенесённые в ветку копией из основной папки.
⚠ **Ту же проверку из ОСНОВНОЙ папки не читать как свою:** там она краснеет на `M _generator/tools/zakryt_sessiyu.py` — это незакоммиченный файл аналитика на ветке `arka/mat-kostyak`, я его не трогал.
🔴 **Хук останавливал коммит один раз** — ворота 5, `kod_dnevnik-gejt.md` не зарегистрирован в `KARTA.md §6` (регистрация аналитика тоже осталась незакоммиченной в основной папке, а ворота читают файл с диска ветки). Красное было на МОЁМ пути, поэтому не обход, а дверь: `register_doc.py`, затем `KARTA.md` дописан к путям в обоих ходах. `--no-verify` не применялся ни разу, чужого долга не встретил.

---

## ПРИЁМКА — (заполняет аналитик, 2026-08-03)

**Заход ПРИНЯТ.** Коммит `002dbf8`, точка отката `1c5cf7f`, `check --zone` из worktree ✅.

**Охват приёмки объявляю честно: 1 из 5.** Прогнан только гейт 0 (чистота зоны). Критерии 1–4 приняты **по отчёту**: worktree из песочницы аналитика не виден, прогнать фикстуру и мутации невозможно до слияния ветки. После слияния прогоняются повторно — это долг приёмки, а не формальность.

**Оспаривание предпосылки §2 признаю правильным.** Правка аналитика 03.08 была не «сделана», а «сделана наполовину и роняла существующую фикстуру». Исполнитель судил по критерию, а не по букве задания, и назвал развилку до работы — это ровно требуемое поведение.

### Ответы на ВОПРОСЫ

1. **Куда подключать гейт — в `zakryt_sessiyu.py` последним ходом, НЕ в `pre-commit`.** Коммит и сессия не совпадают: посреди сессии дневник законно неполон, и хук краснел бы на каждом коммите. Гейт, который шумит зря, отключат (Р31), и тогда мы получим худший исход — механизм, который считается работающим и не работает. Закрытие сессии — единственный момент, когда «полно» имеет смысл.
2. **Назад не размечать. Гейт включается с этой сессии вперёд**, граница — последняя отсечка `<!-- РАЗНЕСЕНО ДО СЮДА -->`. Прошлые сессии уже разнесены и проверены другим способом; разметка задним числом — работа без покупателя.
3. **Зависимость от `origin` принимаю, но с недостающим сторожем.** Сейчас исчезновение поля деградирует разбор молча — это тот же класс, что чинили весь заход. Нужна печать доли записей без `origin` в свежем транскрипте: ноль ожидается, ненулевое — сигнал, что формат изменился. Уходит следующим заходом.
4. **Переходим на `В<NNN>` сейчас.** Три знака стоят одной строки сегодня и переразметки всего дневника потом.

### Что уходит следующим заходом (из ответов выше и из отчёта)

- вызов `proverit` последним ходом `zakryt_sessiyu.py` — до этого построенное остаётся инструментом, а не гейтом;
- сторож формата `origin`;
- `В<NNN>`;
- повторный прогон критериев 1–4 после слияния.
