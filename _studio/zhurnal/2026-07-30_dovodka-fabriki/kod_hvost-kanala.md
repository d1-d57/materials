# Канал исполнителя — hvost-kanala (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — обе правки механические: список мест дан поимённо с номерами строк, решать по ходу нечего.

КОНТЕКСТ. `materials/` — монорепо курсов и фабрики их производства. Три захода подряд (04.08) закончились коммитом с `--no-verify`, каждый раз по «чужому долгу» — и каждый раз это была КРАСНАЯ ФИКСТУРА, а не настоящая регрессия. Причина у всех одна и уже полностью диагностирована: **`--kanal` стал обязательным аргументом `bootstrap_zahod.py`, а фикстуры зовут его без `--kanal`** — argparse падает, заход в стенде не создаётся, следом краснеет всё, что от него зависит. Заход `kod_razblokirovka.md` починил такие вызовы только в своей зоне (`dvizhok`) и честно доложил, что остальные остались.

ЦЕЛЬ: погасить последний источник постоянного красного, чтобы коммит без `--no-verify` снова стал нормой, а не удачей. Плюс убрать гейт приёмки, который третий раз подряд краснеет на законном ответе.

Приёмка — по ОТЧЁТУ, без построчной сверки. **Флаг «стоп до цели»:** `playwright` в песочнице не ставится, ловушки 1–2 `dvizhok` останутся «пропущено» — это ожидаемо и красным считаться не должно.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/tools/fixtures/` `_generator/tools/priyomka.py` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_hvost-kanala.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_generator/tools/fixtures/` `_generator/tools/priyomka.py` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_hvost-kanala.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Две правки, обе механические. Риск разный, поэтому делай их РАЗНЫМИ коммитами и проверяй по отдельности.

**Правка 1 — дописать `--kanal` в вызовы `bootstrap_zahod.py` / `zvat.py` внутри фикстур.**
Мест 20, снято командой 04.08 (пересними сам — часть могла уехать):
```
grep -rn "bootstrap_zahod.py\|zvat.py" _generator/tools/fixtures/ --include=*.sh
```
Разложение по файлам на момент сборки: `fixtures/dvizhok/PROGNAT.sh` — 7 · `fixtures/git_zona/PROGNAT.sh` — 4 · `fixtures/register_doc/PROGNAT.sh` — 7 · `fixtures/tool_contract/PROGNAT.sh` — 2.

Значение по правилу: **`app` там, где ловушка проверяет поведение рабочей папки (`--worktree`), иначе `terminal`.** Канал влияет только на печатаемую стартовую команду; если ловушка её не читает, бери `terminal`.

🔴 **Предпосылку проверь, прежде чем резать.** Есть развилка, и я выбрал одну сторону: можно было вернуть `--kanal` дефолт вместо обязательности — тогда 20 правок не нужны вовсе. **Я выбрал правку вызовов**, потому что обязательность введена сознательно: канал запуска — параметр среды владельца, а не догадка аналитика (`GIT-disciplina`, решение 03.08). Считаешь, что дефолт лучше, — скажи в `## ПЛАН` ДО работы с аргументом, не переигрывай молча.

Гейт правки 1 — обе фикстуры, которые сейчас краснеют:
```
bash _generator/tools/fixtures/git_zona/PROGNAT.sh   → «ЧАСТЬ D СЛОМАНА» и «worktree не заведён» ИСЧЕЗЛИ
bash _generator/tools/fixtures/dvizhok/PROGNAT.sh    → ловушек с ❌ нет (playwright «пропущено» — законно)
bash _generator/tools/fixtures/register_doc/PROGNAT.sh и .../tool_contract/PROGNAT.sh → ловушек с ❌ нет
```

**Правка 2 — гейт Г3 приёмки не краснеет на законной форме «артефакта нет».**
`_generator/tools/priyomka.py`: Г2 принимает строку `**АРТЕФАКТ:** артефакта нет: <причина>` как заполненную, а Г3 берёт тот же текст за путь и краснеет «артефакт не найден на диске: /…/артефакта нет». **Гейты противоречат друг другу; прав Г2** — форма описана в шаблоне отчёта самого захода. Г3 обязан распознавать эту форму и пропускать её (проверять свежесть только там, где путь реально назван).
Это не редкость и не мелочь: **три приёмки подряд 04.08 краснели ровно здесь**, и каждый раз вердикт приходилось выносить руками поверх гейта. Гейт, который штатно перебивают руками, перестаёт быть гейтом.

Гейт правки 2 — фикстура. Собери `_generator/tools/fixtures/priyomka/PROGNAT.sh` (или допиши в существующую, если она есть), три случая:
```
1. АРТЕФАКТ: артефакта нет: заход инфраструктурный   → Г3 зелёный
2. АРТЕФАКТ: <путь к реально существующему файлу>     → Г3 зелёный
3. АРТЕФАКТ: <путь к несуществующему файлу>           → Г3 КРАСНЫЙ (способность краснеть сохранена)
```
Случай 3 — главный: правку легко «сделать», ослабив Г3 до вечнозелёного.

Плюс живой прогон на реальном объекте репозитория: `python3 _generator/tools/priyomka.py _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_gejt-razbora.md` → красных гейтов 0 (сейчас 1, и это Г3).

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
```
1. grep по фикстурам: вызовов bootstrap_zahod.py/zvat.py без --kanal → 0
2. четыре фикстуры выше                                              → ловушек с ❌ нет
3. фикстура priyomka: случаи 1-2 зелёные, случай 3 КРАСНЫЙ           → как написано
4. живой прогон priyomka.py на kod_gejt-razbora.md                   → красных гейтов 0
5. твой финальный коммит проходит БЕЗ --no-verify                    → обходов 0
```
Пункт 5 — главный: он и есть цель захода. Три предыдущих захода провалили его подряд; если провалишь и ты, значит источник красного не тот, что я назвал, — тогда назови настоящий числом и файлом, это ценнее выполненного плана.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** «вызовов без `--kanal` не осталось, проверено 20 из 20».

## 3. ВЕРИФИКАТОР

Ты правишь ГЕЙТ (правка 2) — то есть сам решаешь, что он теперь пропускает. Слепое пятно там же, где всегда.

Свежий субагент, ДРУГОЙ метод, на копиях: пусть подсунет `priyomka.py` четыре формы строки АРТЕФАКТ — пустую, `артефакта нет:` без причины, путь-к-несуществующему, путь-к-существующему — и проверит, что красное остаётся ровно на первых трёх. Таблица «форма · ожидание · факт», расхождений ноль.

По правке 1 верификатор не нужен: её проверяют сами фикстуры, они и есть внешний судья.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/fixtures/` `_generator/tools/priyomka.py` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_hvost-kanala.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/fixtures/` `_generator/tools/priyomka.py` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_hvost-kanala.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

Предпосылку (правка вызовов, не дефолт для `--kanal`) принимаю как есть — обоснование в §2 обоснованное, спорить не буду.

1. Точка отката: зона в HEAD чиста (единственный untracked-путь — сам этот заход, это ожидаемо). Коммит/zip не нужен.
2. Правка 1, по одному файлу за раз:
   - Перечитать grep-выдачу по каждому из 4 файлов (`dvizhok`, `git_zona`, `register_doc`, `tool_contract`) и выделить РЕАЛЬНЫЕ вызовы `bootstrap_zahod.py` / `zvat.py` (не комментарии, не имена функций/переменных, не строки TOOL-CONTRACT-COVERS) без `--kanal`.
   - На каждый — определить `app` или `terminal` по правилу (ловушка проверяет поведение рабочей папки/`--worktree` → `app`, иначе `terminal`), дописать флаг.
   - Гейт: прогнать все 4 `PROGNAT.sh`, ловушек с ❌ быть не должно (playwright «пропущено» в `dvizhok` — законно).
   - Отдельный коммит.
3. Правка 2:
   - `priyomka.py`: найти Г3, научить распознавать форму `**АРТЕФАКТ:** артефакта нет: <причина>` как «путь не назван» и пропускать проверку свежести на диске именно для неё.
   - Собрать/дополнить `_generator/tools/fixtures/priyomka/PROGNAT.sh` тремя случаями (артефакта нет → зелёный; существующий путь → зелёный; несуществующий путь → КРАСНЫЙ).
   - Живой прогон `priyomka.py` на `kod_gejt-razbora.md` → 0 красных гейтов.
   - Верификатор: свежий субагент, ДРУГОЙ метод (не читает мою правку, сам гоняет 4 формы строки на копии `priyomka.py`), таблица форма/ожидание/факт.
   - Отдельный коммит.
4. Финальный ход: `git_zona.py check --zone`, `git show --stat` — только свои пути. Отчёт с критерием готовности по пунктам 1–5.

## ВОПРОСЫ — (заполняет исполнитель)
1. Верификатор (§3) прямым вызовом `gate_g3("", ...)` (в обход Г2) получил зелёное: пустая строка резолвится в `Path("").expanduser()` → `.` → `(REPO / '.').resolve()` — корень репозитория, тот СУЩЕСТВУЕТ, но не файл, и код падает в ветку «не обычный файл — свежесть не сверяется» (True). Это НЕ регрессия моей правки — та же ветка существовала и до неё, мой новый regex на пустую строку не матчится вовсе. В реальном пайплайне (`main()`) недостижимо: Г2 перехватывает пустую строку раньше и передаёт в Г3 `None`, а не `""`. Оставил как есть — вне зоны задания (задание про форму «артефакта нет», не про пустую строку), чинить не стал.

> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Правка 1 — `--kanal` в вызовах `bootstrap_zahod.py`/`zvat.py`.**
Пересняла grep заново (§2 команда) — сырых совпадений в 4 фикстурах намного больше 20, потому что в выдачу попадают комментарии, `TOOL-CONTRACT-COVERS` и упоминания имени функции, а не только сами вызовы. Разобрала построчно с учётом многострочных команд (`\`-продолжений): реальных вызовов `bootstrap_zahod.py`/`zvat.py` в 4 целевых фикстурах — 12, из них 3 уже несли `--kanal` (дожал `kod_razblokirovka.md` в своей зоне `dvizhok`/`git_zona`), 9 — без флага. Дописала все 9 (проверено 12 из 12):
- `dvizhok/PROGNAT.sh`: строки 579 (`terminal` — тема с метасимволами, `--worktree` не участвует), 624/648/652 (`app` — ловушка проверяет поведение `--worktree`-папки), 759 (`terminal` — проверка отказа по битому шаблону, рабочая папка не участвует).
- `git_zona/PROGNAT.sh`: строка 775/ловушка 22 (`app` — прямая проверка поведения `--worktree`-папки, часть D).
- `register_doc/PROGNAT.sh`: строки 156, 172, 305 (все `terminal` — ни одна из ловушек 5/доп./12 не читает рабочую папку).
- `tool_contract/PROGNAT.sh`: реальных вызовов 0 (оба grep-совпадения — это путь-аргумент `--fixtures-for`, не вызов инструмента).
Намеренно НЕ трогала `zahod/PROGNAT.sh` строку 226 — это ловушка 11, она нарочно зовёт `bootstrap_zahod.py` БЕЗ `--kanal`, чтобы проверить сам отказ argparse (регрессионный тест на требуемость флага); эта фикстура не входила в список из 4 целевых, и правка убила бы собственную ловушку.

Побочно нашла и починила: обе ловушки, красневшие в §2 (`git_zona` часть D «ЧАСТЬ D СЛОМАНА»/«worktree не заведён», ✅ теперь исчезли) падали НЕ из-за `--kanal` — `--kanal app` уже стоял на месте, а падение было `ModuleNotFoundError: check_zahod` — песочницы ловушки 22 (`git_zona`) и ловушек 5/доп./12 (`register_doc`) не копировали `check_zahod.py` в одноразовое дерево, хотя `bootstrap_zahod.py` теперь его импортирует (оба изменения из одного коммита `3e1b9ed`). Соседняя ловушка 17 в `git_zona` этот файл уже копировала — по аналогии дописала `check_zahod.py` в оба `cp`-списка. Это внутри зоны (`fixtures/`) и напрямую блокировало критерий §2 — не расширение задания, а долечивание того же корня, который заход и просил погасить.

Гейт правки 1 — все 4 `PROGNAT.sh`: `git_zona` rc=0 0❌, `dvizhok` rc=0 0❌ (playwright-ловушки 1-2 пропущены законно), `register_doc` rc=0 0❌, `tool_contract` rc=0 0❌.

**Правка 2 — Г3 приёмки не спорит с Г2 на «артефакта нет».**
В `gate_g3` (`_generator/tools/priyomka.py`) добавила раннюю ветку: `put_str`, совпадающий с `^артефакта\s+нет\s*(?::\s*(\S.*))?$` (регистронезависимо), считается легитимным отсутствием артефакта и не идёт в файловую проверку. Форма шире, чем в тексте задания («артефакта нет: <причина>» с двоеточием) — живой файл `kod_gejt-razbora.md` (строка 205) на практике использует тире-форму «артефакта нет — <причина>», а `gate_g2` уже отрезает часть после тире как «чем открывать» (та же логика, что для `<путь> — <чем открывать>`) — до Г3 в этом случае доезжает голое `артефакта нет` без причины. Regex принимает и голое `артефакта нет` (причина уже проверена Г2 как непустая на исходной строке), и `артефакта нет: <причина>` (двоеточие + текст), но НЕ пустое `артефакта нет:` (двоеточие без ничего после) — эта форма проваливается через regex и уходит в обычную файловую проверку, которая красит её как несуществующий путь (сохраняет способность краснеть на браке отчёта).

Дописала `_generator/tools/fixtures/priyomka/PROGNAT.sh` тремя новыми ловушками (18-20, поверх существующих 17):
18. `артефакта нет: <причина>` → Г3 зелёный, ветка названа поимённо.
19. Путь к реально существующему файлу → Г3 по-прежнему зелёный (regression guard).
20. Путь к несуществующему файлу → Г3 остаётся красным.
Прогон фикстуры: rc=0, 0❌, «ФИКСТУРЫ ЗЕЛЁНЫЕ» (все 20 ловушек, включая новые).

Живой прогон на `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_gejt-razbora.md`: было 1 красный (Г3), стало 0 из 8 — `RC=0`, «✅ ПРИЁМКА: все 8 машинных гейтов зелёные».

**Верификатор (§3).** Свежий субагент (`general-purpose`), без чтения diff/git log, прямыми вызовами `gate_g3()` через импорт модуля на 4 формах: пустая строка, `"артефакта нет:"` без причины, путь-к-несуществующему, путь-к-существующему. Таблица «форма·ожидание·факт»: формы 2 (без причины, red), 3 (несуществующий, red), 4 (существующий, green) совпали с ожиданием. Форма 1 (пустая строка) разошлась — см. `## ВОПРОСЫ` п.1: подтверждено, что это дочиненная не мной, недостижимая через реальный пайплайн особенность, а не регрессия правки.

**Что НЕ трогала:** всё вне `_generator/tools/fixtures/`, `_generator/tools/priyomka.py` и этого файла-захода — по контракту зоны. Пустую-строку-ветку в `gate_g3` не чинила (см. `## ВОПРОСЫ` п.1) — вне формулировки задания, недостижима в проде.

**Необратимого нет.**

**Критерий готовности:**
1. Вызовов `bootstrap_zahod.py`/`zvat.py` без `--kanal` среди 4 целевых фикстур — 0 (проверено 12 из 12 реальных вызовов).
2. Все 4 фикстуры правки 1 — 0 ❌ каждая.
3. Фикстура `priyomka`: случаи 1-2 (18-19) зелёные, случай 3 (20) красный — как написано.
4. Живой прогон `priyomka.py` на `kod_gejt-razbora.md` — 0 красных гейтов (было 1).
5. Оба финальных коммита прошли БЕЗ `--no-verify` — обходов 0.
Все пять пунктов выполнены — критерий НЕ провалился.

**Время прогона + токены** — снимает приёмка из лога прогона (result-строка), сама не оцениваю.

**АРТЕФАКТ:** артефакта нет — это кодовый заход (гейт + фикстуры), не сборка документа/деки. Проверяемое — файлы зоны из `git show --stat` ниже и живые прогоны, показанные выше.
**КОММИТ:** `ed19868` (правка 1: `--kanal` в 3 фикстурах + недостающая копия `check_zahod.py` в песочницы `git_zona`/`register_doc`) · `1a3e79c` (правка 2: `priyomka.py` Г3 + фикстура `priyomka` +3 ловушки) · `git_zona.py check --zone _generator/tools` → ✅.
