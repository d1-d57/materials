# Канал исполнителя — storozh-vetok (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу, В ЭТОМ ЖЕ ФАЙЛЕ. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — у захода есть фаза исследования (Ф0), где решение рождается по ходу, а не расписано заранее.

КОНТЕКСТ. Репозиторий `materials/` — монорепо: десятки проектов в одной папке, владелец работает над двумя-тремя одновременно (содержание одной лекции · визуал другой · обзор третьего). Отсюда параллельные заходы и отдельные рабочие папки `git worktree`. Механизм заводился спонтанно, в разговоре, вместе с большой переделкой дисциплины коммитов — и **системного проектирования у него не было ни разу.**

🔴 **ЦЕЛЬ: сделать невозможной тихую потерю работы в невлитой ветке.** Владелец: *«несколько раз уже было, когда уезжал в скрытую, невлитую ветку кусок работы… потом всё возвращалось, всё находилось, но случайно. Это потенциальная бомба замедленного действия»*.

**Замер на живом репозитории 03.08** (снят командами, не по памяти): веток **16**, из них **7 впереди текущей**; среди них `main` (23 файла) и `arka/vneshnie-istorii` — **чужой проект, 4 файла расходятся с нашей версией прямо сейчас**. Рабочих папок worktree — **5**, все помечены `prunable`.

🔴 **И главное, что этот замер показал: наивная метрика «влита / не влита» ВРЁТ В ОБЕ СТОРОНЫ.** `zahod/tirazh-ocheredi` формально не влита, но расхождений по содержанию **ноль** — работа доехала через `git_zona.py adopt`, который копирует содержимое, не сливая историю. А соседняя ветка при том же способе переноса даёт 5 расхождений. Сторож, считающий невлитые ветки, будет кричать волком на безопасных и молчать про опасные — и его отключат, как отключают всё шумное (`KONSTITUCIYA` Р31).

Приёмка — по ОТЧЁТУ. Если стоп до цели: Ф0 и часть A важнее всего; сделай их первыми и отчитайся честно.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **ПЛАН, ВОПРОСЫ, ОТЧЁТ и УРОКИ ФАБРИКЕ пиши В ЭТОТ ФАЙЛ — тот, по абсолютному пути, что назван в стартовой строке, в ОСНОВНОЙ папке репозитория.** Он существует в одном экземпляре. **Его самого НЕ коммить** — это делает аналитик при приёмке. *Цена обратного правила 03.08: приёмка трижды подряд открыла файл в основной папке, увидела пустые секции и объявила, что отчёта нет.*
- **МЕСТО РАБОТЫ ДЛЯ КОДА:** отдельная рабочая папка: `python3 _generator/tools/git_zona.py worktree add storozh-vetok --branch zahod/storozh-vetok`, затем `cd` в неё. 🔴 **`git checkout` в основной папке НЕ делай** — она стоит на другой ветке, и checkout МОЛЧА откатит дерево под всеми (цена 27→28.07: файл откатился ночью, следа в git НЕ остаётся).
- **ЗОНА КОДА (можно менять):** `_generator/tools/git_zona.py` `_generator/tools/fixtures/git_zona/`. Всё вне — **READ-ONLY**.
  🔴 **`git_zona.py` — центральный инструмент репозитория и ЕДИНСТВЕННАЯ дверь в git для всех.** Сломаешь его — встанет работа владельца и всех параллельных заходов. Правки только добавляющие: **новую подкоманду и новые проверки внутри `doctor`; поведение существующих подкоманд не менять**. Любая правка существующего пути исполнения — в `## ВОПРОСЫ`, не молча.
  🔴 **`register_doc.py` и `fixtures/register_doc/` не трогать даже на строку** — над ними идёт незакоммиченная работа владельца из другого чата. 🔴 **`GIT-disciplina.md` и прочий канон вне зоны** — канон правит аналитик по твоему отчёту.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ САМ, ТЕМ ЖЕ ХОДОМ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). Дверь ровно одна, и это она. **Красный хук на ТВОЁМ новом `.md` — повод позвать дверь, а не `--no-verify`.**
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`**. НИКОГДА `-A` / `.` / `commit -am`. Субагенты не коммитят. **`--no-optional-locks` обязателен.**
- **ЗАПРЕТ:** ничего за пределами зоны. Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Заведи worktree для кода и перейди в него. `git branch --show-current` обязано дать `zahod/storozh-vetok`. **Отчёт пиши в файл основной папки** — путь в стартовой строке.
- Прочитать ТОЛЬКО: `_generator/tools/git_zona.py` (целиком — главный якорь; там уже есть подкоманды `doctor`, `check`, `adopt`, `merge`, `worktree` и какая-то форма сторожа веток), `_generator/tools/fixtures/git_zona/PROGNAT.sh` (целиком), `_studio/docs/kak-delat/GIT-disciplina.md` (целиком — единственный дом канона git), и в `_studio/zhurnal/2026-07-30_dovodka-fabriki/UROKI-FABRIKE.md` — **уроки 23, 24, 25 поимённо** (три разные поломки этого же механизма, каждая с ценой). Проект сверх этого не изучай.
- Ф0 (ниже) — ДО всякого кода.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** *Цена 21.07: `rc=128` четырежды прочитан как результат.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия; критерий, который может провалиться. **Оспорить ложную предпосылку, включая КРИТЕРИЙ ГОТОВНОСТИ, — в `## ПЛАН`, ДО работы.** Субагенты: ≤5, рейт-лимит = отступить + доложить.

🔴 **Только stdlib.** Python владельца — **3.9.6**; аннотации вида `str | None` там падают.

🔴 **Ловушка обязана судить РЕЗУЛЬТАТ, а не присутствие подстроки в выводе** — это правило оплачено ДВАЖДЫ на этом же сторóже (урок 25): грубый греп имени ветки был зелёным на сломанном механизме, потому что имя встречается и в строке диагностики.

🔴 **Мера истины — прогон на живом репозитории.** За 03.08 живой прогон нашёл то, чего не нашла ни одна фикстура, четыре раза подряд.

## 2. ЗАДАЧА

### 🔴 Ф0 — ФАЗА ИССЛЕДОВАНИЯ. Решение принимается после неё, а не до

Аналитик **намеренно не задаёт конструкцию**. Механизм worktree родился спонтанно, чинился трижды точечно и ни разу не проектировался; принимать его форму по памяти аналитика — повторить ту же ошибку в четвёртый раз. Твоя работа — исследовать и предложить, а не исполнить.

**Ф0.1 — собственная история.** Прочитай уроки 23, 24, 25 и найди в них **общий корень**, а не три отдельные поломки. Подсказка, которую надо проверить, а не принять: во всех трёх случаях сломался не git, а **представление о том, где физически лежит работа** — файл захода, код гейтов, имя ветки. Подтверди или опровергни по тексту уроков.

**Ф0.2 — внешние источники.** Поищи, как задачу решают вообще: дисциплина `git worktree` в монорепозиториях, обнаружение отставших и заброшенных веток (stale branch detection), `git worktree prune` и что означает статус `prunable`, чем `merge-base` отличается от наивного сравнения веток, есть ли готовые практики «что потеряется, если удалить ветку». **Назови источники адресами** и отдельно скажи, что из найденного к нам НЕ применимо и почему — монорепо на одного человека с автономными агентами это не типичная командная разработка.

**Ф0.3 — предложение.** Сформулируй конструкцию сторожа и обоснуй её замерами по живому репозиторию. Обязательно ответь на четыре вопроса:

1. **Что считать «работа не влита»?** Наивное «ветка впереди» врёт (доказано выше). Кандидат аналитика — сравнение по содержанию через `merge-base`: файлы, тронутые веткой, у которых версия ветки расходится с нашей. Но и он не различает «у них новее» и «у нас новее». **Нужна метрика, отвечающая на вопрос владельца буквально: что именно пропадёт, если эту ветку удалить.** Предложи и обоснуй.
2. **Когда сторож срабатывает?** Владелец: *«если мне будет приходить в любой момент такое уведомление, я буду ждать, пока всё закончится в других местах, останавливаться, не запускать новое»*. Где точка — старт захода, `doctor`, коммит, приёмка? У каждой точки своя цена ложного срабатывания.
3. **Красное или печать?** Красное на коммите остановит работу владельца — а часть расхождений законна (ветка ждёт своей очереди). Предложи градацию и порог.
4. **Что делать с пятью `prunable` worktree?** Разобраться, что этот статус означает и **правда ли они prunable на машине владельца** — замер выше снят из песочницы аналитика, где путей `/Users/…` не существует вовсе, так что статус может быть артефактом среды. **Это предпосылка, которую надо проверить, а не унаследовать.**

**Гейт фазы Ф0:** в `## ПЛАН` есть раздел «Ф0» с ответами на все четыре вопроса, **не менее двух внешних источников с адресами**, и **не менее двух замеров по живому репозиторию с числами**. «Согласен с аналитиком» без замеров фазу не закрывает.

### ЧАСТЬ A — реализация сторожа

Реализуй то, что вышло из Ф0. Форма — **новая подкоманда `git_zona.py`** и/или расширение `doctor`; существующее поведение не менять.

Минимум, который должен получиться при любом исходе Ф0:
- команда отвечает на вопрос **«что пропадёт, если удалить ветку X»** — списком файлов, а не числом коммитов;
- вывод несёт **охват**: сколько веток проверено из скольких существующих;
- **ветка, чьё содержимое уже доехало** (как `zahod/tirazh-ocheredi` сегодня), обязана называться безопасной явно — иначе список опасных утонет в шуме;
- команда **read-only**: ничего не сливает, не удаляет и не переключает. Лечение печатается строкой, которую владелец копирует сам. *Основание — `GIT-disciplina §0`: владельцу в чат идут только команды `git_zona.py`, и он исполняет их в своём терминале.*

### ЧАСТЬ B — три старые поломки: проверить, живы ли

Каждую **проверить прогоном**, а не чтением, и починить только если жива и если это в зоне:

1. **Урок 23:** `bootstrap_zahod.py --worktree` пишет файл захода в основное дерево, а исполнителя отправляет в другое. *Файл вне твоей зоны* — только проверь и доложи, чинить не надо. (Аналитик отмечает: сегодня это уже не вредит, потому что отчёт по новому канону и так живёт в основной папке. Но канон и код могли разойтись — это и надо установить.)
2. **Урок 24:** `adopt` по путям привозит документацию без кода — 500 строк реализации месяц числились существующими. **Это твоя зона.** Можно ли заставить `adopt` печатать, какие файлы ветки он НЕ забрал? Если да — сделай печатью.
3. **Урок 25:** сторож веток через `%(refname:short)` при теге-омониме давал зелёное на живой невлитой работе. Проверь, жива ли починка, и что ловушка судит результат, а не подстроку.

### ЧАСТЬ C — фикстура

Расширить `fixtures/git_zona/PROGNAT.sh`. Обязательно среди ловушек:
- **ветка с уникальным содержимым** → названа опасной, файлы перечислены;
- **ветка, чьё содержимое доехало** (сымитируй `adopt`: скопируй содержимое в текущую, историю не сливая) → названа безопасной, **в списке опасных её нет**. Это главная ловушка: она сторожит не поломку, а шум, от которого сторожа отключат;
- **тег-омоним** → регрессия на урок 25;
- **охват печатается** и равен числу существующих веток.

🔴 **Каждую ловушку проверь мутацией и покажи, что краснеет ПОИМЕНОВАННАЯ проверка.**

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `## ПЛАН` содержит Ф0 с четырьмя ответами, ≥2 внешними источниками с адресами и ≥2 замерами по живому репозиторию.
2. `sh _generator/tools/fixtures/git_zona/PROGNAT.sh` → `echo $?` = **0**.
3. Каждая новая ловушка показана краснеющей под мутацией — по строке в отчёте.
4. `python3 _generator/tools/check_tool_contract.py _generator/tools/git_zona.py` → зелёный.
5. 🔴 **ЖИВОЙ ПРОГОН на настоящем репозитории:** сторож прогнан по всем 16 веткам, вывод целиком в отчёт. **`zahod/tirazh-ocheredi` обязана быть названа безопасной**, `arka/vneshnie-istorii` — разобрана поимённо: что в ней есть и пропадёт ли это.
6. Существующие подкоманды `git_zona.py` работают как раньше — показать прогоном `doctor` и `check --zone` до и после.
7. Зона кода закоммичена, `git_zona.py check --zone _generator/tools` → ✅.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «потерь не найдено», а «потерь не найдено, проверено X веток из Y».

## 3. ВЕРИФИКАТОР
Свежий субагент, **атака на метрику**: дай ему только описание твоей метрики (без кода) и попроси построить случай, когда она **назовёт безопасной ветку, где есть уникальная работа**. Это худший исход механизма — молчание при живой потере, ровно то, чем болел прошлый сторож (урок 25). Второй вопрос ему же: случай, когда метрика поднимет тревогу на пустом месте. Результат и твоя правка — в отчёт.

## 4. 🔴 КОММИТ ЗОНЫ КОДА — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
```
git --no-optional-locks add -- _generator/tools/git_zona.py _generator/tools/fixtures/git_zona/
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/git_zona.py _generator/tools/fixtures/git_zona/
python3 _generator/tools/git_zona.py check --zone _generator/tools    # из корня репо; должен быть ✅
git --no-optional-locks show --stat                                   # обязаны быть ТОЛЬКО твои пути
```
🔴 **Файл-заход в этот коммит НЕ входит** — он живёт в основной папке и коммитится аналитиком при приёмке.
🔴 **ОБА хода обязательны:** `add` вводит новые пути в индекс; `-- <пути>` в самом `commit` отсекает чужое, застейдженное рядом. *Обе половины оплачены 21.07: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх.*
⚠ **Известный чужой долг окружения:** `fixtures/dvizhok/PROGNAT.sh` краснеет на отсутствующем `chrome-headless-shell` (playwright не установлен). Не твоя регрессия — подтверди сравнением с baseline и назови причиной обхода, если придётся обходить.
**Коммит не прошёл по ВНЕШНЕЙ причине** — чужое состояние репозитория НЕ чини: напиши отдельной строкой «коммита нет, причина, нужно ваше действие».

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу ЭТОГО ФАЙЛА
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна)**. Время и токены снимает приёмка из лога — не оценивай.

🔴 **АРТЕФАКТ — ПУТЬ ОТ КОРНЯ РЕПОЗИТОРИЯ.** *Цена 03.08: путь внутрь `materials-wt/…` протух при первом же переносе зоны.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ.** Особенно здесь: ты работаешь с единственной дверью в git. Любое слияние, удаление ветки, `prune`, `reset` — каждое ОДНОЙ строкой: **что · где · чем восстанавливается**. **Необратимого не было — напиши «необратимого нет».**

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и видишь то, чего не видит аналитик. Сломался ВХОД? Критерий кривой? Канон противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<путь от корня репозитория>` — `<чем открывать>`
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone _generator/tools` → ✅
