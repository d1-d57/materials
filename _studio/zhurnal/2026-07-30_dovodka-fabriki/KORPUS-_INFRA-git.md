# KORPUS — арка `_studio/zhurnal/_INFRA-git`

Сырые кандидаты в уроки фабрике. Без закона, без вердикта, без морали — факт, адрес, цена (если названа в источнике). Дубли не склеены намеренно (кратность — работа `KRATNOST.md`).

## _studio/zhurnal/_INFRA-git/kod_bootstrap-guard.md

### `bootstrap_arka.py` принял флаг `--help` за имя арки и сфабриковал папку-сироту, которая ушла в git-план
АДРЕС: _studio/zhurnal/_INFRA-git/kod_bootstrap-guard.md#ЗАДАНИЕ, строки 6, 42
ЦЕНА: 23.07 папка-сирота `--help` чуть не уехала в гейт-коммит `_studio`, поймал только ручной просмотр; каждую коммит-сессию кто-то вручную выцепляет мусор из плана

### Пометка «удалить» в HANDOFF не удалила дубль `ZAPROS-fable-2-vneshnie.md` — заметка не механизм
АДРЕС: _studio/zhurnal/_INFRA-git/kod_bootstrap-guard.md#ЗАДАНИЕ, строки 6, 42
ЦЕНА: НЕ НАЗВАНА

### Задание содержало ложную цену: утверждало, что дубль «пролежал неделю» после пометки к удалению — проверка по mtime и HANDOFF показала тот же день
АДРЕС: _studio/zhurnal/_INFRA-git/kod_bootstrap-guard.md#ЗАДАНИЕ, строка 42; #ОТЧЁТ, строка 125
ЦЕНА: НЕ НАЗВАНА

### Код возврата `rc=128` (сбой прав окружения) четырежды прочитан как результат работы
АДРЕС: _studio/zhurnal/_INFRA-git/kod_bootstrap-guard.md#ЗАДАНИЕ, строка 27
ЦЕНА: 21.07, едва не откатили верное правило по ложным данным

### Голый `commit` без pathspec подмёл чужой индекс, «починка» без `add` завалила коммиты повторно
АДРЕС: _studio/zhurnal/_INFRA-git/kod_bootstrap-guard.md#ЗАДАНИЕ, строка 66
ЦЕНА: 21.07 в один вечер — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно

### Триггер фикстуры (поднимается хуком) уже, чем защита, которую она проверяет
АДРЕС: _studio/zhurnal/_INFRA-git/kod_bootstrap-guard.md#ОТЧЁТ, строки 122–123
ЦЕНА: НЕ НАЗВАНА

## _studio/zhurnal/_INFRA-git/kod_commit-ux.md

### Запуск `git_zona.py` не из корня репозитория падал без диагностики
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строка 37
ЦЕНА: дважды подряд; инструмент даже не загрузился, подсказать не мог

### Инлайн `#`-комментарий в команде ломался в zsh владельца, уходя в argv
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строка 38
ЦЕНА: `unrecognized arguments: # предпросмотр` — обе `adopt`-команды упали так

### `commit` без предварительного `plan` не был очевиден как двухшаговый процесс
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строка 39
ЦЕНА: НЕ НАЗВАНА (владелец получал «Плана нет»)

### Заглушка `== <переписать>` в `.commit-plan` требовала ручной правки, о которой владелец не знал, а вписывать её должен был другой писатель
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строка 40
ЦЕНА: НЕ НАЗВАНА

### worktree/adopt для параллельных заходов оставался многоходовым даже после частичной починки
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строка 41
ЦЕНА: НЕ НАЗВАНА

### Урок о поломке коммитов выжил только потому, что был вшит в конкретный файл-заход
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строки 57–59
ЦЕНА: НЕ НАЗВАНА («в чужом чате он бы исчез»)

### Погоня за 100% безотказностью породила отдельную арену «гейт врёт»: фикстура записала коммит в боевой репо, GNU-изм остановил здоровый коммит, гейт показал ❌ на успешном исходе
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строки 71–73
ЦЕНА: НЕ НАЗВАНА (упомянуты номера инцидентов 1.7, 2.5, 2.7 без цены в этом файле)

### Канон отстал от инструмента: подкоманда `adopt` существовала в `git_zona.py`, но не была описана в каноне, и висела незакоммиченной
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строка 80
ЦЕНА: НЕ НАЗВАНА

### Код возврата `rc=128` четырежды прочитан как результат
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строка 28
ЦЕНА: 21.07, едва не откатили верное правило по ложным данным

### Голый `commit` подмёл чужой индекс, «починка» без `add` завалила коммиты повторно
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ЗАДАНИЕ, строка 98
ЦЕНА: коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно

### Новый файл `INCIDENTY.md` требует регистрации в KARTA §6, но регистрация вне зоны исполнителя — оставлена как открытый вопрос
АДРЕС: _studio/zhurnal/_INFRA-git/kod_commit-ux.md#ВОПРОСЫ, строка 130
ЦЕНА: НЕ НАЗВАНА

## _studio/zhurnal/_INFRA-git/kod_terminal-kanal.md

### Предпосылку про `_generator/` писал аналитик, у которого в песочнице примонтирован только `_studio/` — предпосылка была непроверена по живому файлу
АДРЕС: _studio/zhurnal/_INFRA-git/kod_terminal-kanal.md#ЗАДАНИЕ, строки 32–33
ЦЕНА: НЕ НАЗВАНА (в этом случае предпосылка позже подтвердилась)

### Новый файл-заход вызвал красный гейт «документ-сирота» (нет строки в KARTA §6), коммит потребовал обхода `--no-verify`
АДРЕС: _studio/zhurnal/_INFRA-git/kod_terminal-kanal.md#ВОПРОСЫ, строки 92–94; #ОТЧЁТ, строки 117–121
ЦЕНА: НЕ НАЗВАНА (обход применён ко всем трём коммитам захода по одной и той же причине)

### Рабочая ветка `arka/mat-kostyak` несла несвязанные незакоммиченные правки нескольких проектов одновременно
АДРЕС: _studio/zhurnal/_INFRA-git/kod_terminal-kanal.md#ВОПРОСЫ, строка 96
ЦЕНА: НЕ НАЗВАНА

## _studio/zhurnal/_INFRA-git/kod_tool-contract.md

### За четыре сессии ~15 поломок сведены к одному повторяющемуся классу: лекарством была проза (заметка/канон/урок), которая не переносится на следующий инструмент
АДРЕС: _studio/zhurnal/_INFRA-git/kod_tool-contract.md#ЗАДАНИЕ, строка 6
ЦЕНА: за четыре сессии ~15 поломок

### Фикстура `git_zona` поднимается хуком только при правке `git_zona.py`, на правку `bootstrap_*` не срабатывает — триггер уже охвата защиты
АДРЕС: _studio/zhurnal/_INFRA-git/kod_tool-contract.md#ЗАДАНИЕ, строка 6
ЦЕНА: НЕ НАЗВАНА

### Гейт с ложными срабатываниями блокирует здоровые коммиты всем троим писателям — паттерн «сейф-код стал опасностью»
АДРЕС: _studio/zhurnal/_INFRA-git/kod_tool-contract.md#ЗАДАНИЕ, строка 7
ЦЕНА: НЕ НАЗВАНА (упомянуты номера инцидентов 1.1/2.5, «арена E расследования», без цифры в этом файле)

### Правило о регистрации новых `.md` в KARTA §6 записано 26.07 с ценой пять документов-сирот и через два дня повторилось дословно, потому что жило в `UROKI-FABRIKE`, куда исполнитель с пустым контекстом не заглядывает
АДРЕС: _studio/zhurnal/_INFRA-git/kod_tool-contract.md#ЗАДАНИЕ, строка 12
ЦЕНА: цена в пять документов-сирот (26.07); повтор нарушения дословно через два дня

### Обход `--no-verify` с причиной был законен по канону, но не существовал как механизм в инструменте — первый же коммит владельца встал на чужом долге
АДРЕС: _studio/zhurnal/_INFRA-git/kod_tool-contract.md#ЗАДАНИЕ, строка 81
ЦЕНА: первый же коммит владельца встал на чужом долге

### Код возврата `rc=128` четырежды прочитан как результат
АДРЕС: _studio/zhurnal/_INFRA-git/kod_tool-contract.md#ЗАДАНИЕ, строка 29
ЦЕНА: 21.07, едва не откатили верное правило по ложным данным

### Голый `commit` подмёл чужой индекс, «починка» без `add` завалила коммиты повторно
АДРЕС: _studio/zhurnal/_INFRA-git/kod_tool-contract.md#ЗАДАНИЕ, строка 73
ЦЕНА: коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно
