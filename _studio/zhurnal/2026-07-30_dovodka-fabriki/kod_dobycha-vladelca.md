# Канал исполнителя — dobycha-vladelca (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — граница «претензия к фабрике» ↔ «правка конкретной лекции» проходит по смыслу, а не по форме, и ошибка здесь отравляет канон, который читают все будущие проекты.

КОНТЕКСТ. Репозиторий `materials/` — фабрика курсов: собирает лекции-презентации и учебные тексты. Владелец — математик, он же лектор и заказчик; фабрика существует, чтобы выдавать результат, который ЕМУ нравится.

**Что уже сделано и почему этого мало.** В ночь на 31.07 прошла полная вычитка всех 118 файлов-заходов репозитория: собран `KORPUS-*.md` — **461 наблюдение** с адресами, и `KRATNOST.md` — 152 группы по механизму поломки. Материал хороший, но у него один источник: **отчёты ИСПОЛНИТЕЛЕЙ.** То есть агентов, которые описывали, что сломалось у них в процессе.

🔴 **Претензий самого владельца там почти нет.** А они существуют, их много, и лежат они в других файлах — в разборах после живых лекций, в правках после репетиций, в файлах замечаний, в дневниках арок. Владелец сказал прямо: *«есть большое количество моих собственных соображений, что мне не нравится, начиная от визуала и кончая контентом; это должно быть прописано, должны быть очень чёткие везде процедуры»*.

**ЦЕЛЬ: собрать корпус ЗАМЕЧАНИЙ ВЛАДЕЛЬЦА — тем же методом, что сработал ночью, но по другому источнику.**

Ты **добытчик, а не судья.** Законов не формулируешь, вердиктов не ставишь, канон не правишь. Твоя работа — чтобы ни одно замечание владельца не осталось только в файле, где его никто не найдёт.

**Приёмка — по ОТЧЁТУ.** Если стоп до цели: реестр и корпус по прочитанному — законный результат, работа возобновляема (см. §2 свойство №1). **НЕ сдавай корпус без адресов** — без них судья не вернётся к источнику, и всё повторит судьбу тех 429 кандидатов, что потерялись при прошлой склейке.

🔴 **ЗАХОД READ-ONLY, и это несущее свойство.** Ты не правишь НИ ОДНОГО файла вне своей зоны — параллельно в этом же дереве может идти другой заход, правящий `_generator/` и `.githooks/`. Твоя зона — только четыре новых файла в папке арки. `git checkout`, `merge`, `reset` — запрещены.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/REESTR-vladelca.md _studio/zhurnal/2026-07-30_dovodka-fabriki/KRATNOST-vladelca.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dobycha-vladelca.md _studio/docs/KARTA.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add _studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/REESTR-vladelca.md _studio/zhurnal/2026-07-30_dovodka-fabriki/KRATNOST-vladelca.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dobycha-vladelca.md _studio/docs/KARTA.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### 🔴 Свойство №1, важнее остальных: РАБОТА ВОЗОБНОВЛЯЕМА

Прогон длинный и может оборваться. **Реестр и корпус пишутся на диск ПОСЛЕ КАЖДОГО ФАЙЛА, а не в конце.** Оборвало на десятом источнике из пятнадцати — значит сделано десять, следующий прогон продолжит с одиннадцатого. **Прерви себя сам** после первых трёх источников, перечитай реестр с диска, убедись, что по нему можно продолжить, и напиши в отчёте, что проверил. Начинать пришлось бы заново — устройство неверное, переделай ДО того, как прочитаешь половину.

### Что читать — источники, перечисленные поимённо

Аналитик снял этот список командой; **сверь его сам** (`wc -l`) и назови расхождения. Плотные файлы замечаний:

| файл | строк |
|---|---|
| `teorkat-vvedenie/repeticia/PRAVKI.md` | 735 |
| `lsh-2026-perechislitelnaya/otkrytaya-lekcia-paskal/raskadrovka/RAZBOR-posle-lekcii-2026-07-27.md` | 586 |
| `teorkat-vvedenie/repeticia/akt-1-razbor.md` | 301 |
| `_studio/zhurnal/2026-07-28_konspekt-l1/PRAVKI-final.md` | 283 |
| `lsh-.../raskadrovka/PRAVKI-sceny-i-animacii.md` | 109 |
| `catalan/zhurnal/2026-07-03_programma-kursa/zamechaniya-statya-mir2.md` | 82 |
| `catalan/zhurnal/2026-07-05_mir1/zamechaniya-{L1-process-first,mosty-kartoteka,issledovanie-idei,disciplina-kartochek}.md` | 27+25+15+7 |

Плюс **помарки владельца в дневниках арок**: 15 файлов `_studio/zhurnal/*/SESSIYA.md` содержат прямую речь владельца (список получишь командой `grep -rl 'владелец' --include='SESSIYA.md' _studio/zhurnal/`). Читай в них **только куски с его речью и реакцией на артефакт**, а не дневник целиком.

**Что НЕ читать:** сырые расшифровки `teorkat-vvedenie/repeticia/akt-*-syroj.md` и `akt-*-macwhisper-syroj.md` (~7000 строк). Они уже дистиллированы аналитиком в `PRAVKI.md` и `akt-1-razbor.md`; читать их заново — недельная работа с низкой отдачей. **Если по ходу увидишь, что дистилляция что-то потеряла, — скажи в отчёте, не читай всё.**

### 🔴 Что считается замечанием, а что нет — граница проходит здесь

**ЗАМЕЧАНИЕ (идёт в корпус):** высказывание владельца о том, каким должен быть РЕЗУЛЬТАТ или ПРОЦЕДУРА, применимое к следующей лекции. «Текста на слайде слишком много» · «подпись не живёт на рисунке» · «в лекции должно быть меньше пятидесяти слайдов» · «эти вопросы уже про концепцию, а мы договорились её пока не трогать» · «мне не нравится, что картинки узкой полосой».

**НЕ замечание (в корпус НЕ идёт):** правка содержания конкретной лекции. «Переставить нумерацию тождеств» · «добавить элемент $E$ на рисунок» · «векторные пространства после групп» · исправление математической ошибки. *Именно эти классы составили большую часть тех 429 кандидатов, что были отброшены при прошлой склейке, — и отброшены правильно.*

**Сомневаешься — бери в корпус с пометкой `КЛАСС: спорное`.** Судья разберётся; молча выбрасывать нельзя.

🔴 **Отдельно ценно и почти всегда пропускается: замечания, где названо ЧИСЛО или ПОРОГ.** «Меньше пятидесяти слайдов», «полтора часа вместо двух», «750 знаков в текстовой колонке», «три с половиной минуты на позицию». Владелец прямо сказал, что хочет процедур с точными деталями — «какого размера должен быть объём текста на слайде, сколько текста в одной части лекции». **Каждое такое число помечай `ЧИСЛО: <какое, чего касается>`** — из них вырастут гейты.

### Артефакт 1 — `REESTR-vladelca.md`: след чтения, который нельзя подделать

Таблица, строка = **один источник** (для дневников — одна секция дневника): `путь` · `секция` · `строк` · `хэш блоба` (`git hash-object`) · `замечаний найдено` · `дата`.
Хэш обязателен: он делает пометку САМОИНВАЛИДИРУЮЩЕЙСЯ — файл дописали, пометка «прочитано» автоматически недействительна. `замечаний найдено = 0` — законный результат.

### Артефакт 2 — `KORPUS-VLADELCA.md`: сами замечания

Формат жёсткий, по нему пойдёт греп:
```
### <что владелец хочет или чем недоволен — его мыслью, не твоей>
АДРЕС: <путь>#<секция>, строки N–M
ЦИТАТА: <его слова, дословно; если пересказ — пиши ПЕРЕСКАЗ и не выдавай за цитату>
КАСАЕТСЯ: визуал | текст | структура лекции | процедура | математика | иное
ЧИСЛО: <какое и чего> | НЕТ
КЛАСС: замечание | спорное
```
- 🔴 **Закон НЕ формулировать.** Не «нужно всегда делать X», а «владелец сказал Y про Z».
- 🔴 **Цитату не улучшать.** Его формулировка ценнее твоей: она несёт то, что ему важно, включая интонацию недовольства.
- **Дубли не склеивать.** Одно и то же, сказанное трижды в трёх местах, — три записи. **Кратность и есть главное, что нужно судье:** сказанное трижды и сказанное однажды — разные вещи.

### Артефакт 3 — `KRATNOST-vladelca.md`

Группы близких замечаний с числом вхождений и полным списком адресов. **Группируй по ПРЕДМЕТУ недовольства** (плотность текста · подписи на рисунках · объём лекции · порядок подачи), а не по лекции, где сказано. Отдельным разделом — **все найденные `ЧИСЛО:`** одним списком: это заготовка будущих гейтов.

### Субагенты — по бюджету

Пачками по 1–3 источника (файлы крупные), **не больше 4 одновременно**. Субагенту запрещено то же, что тебе: формулировать законы, ставить вердикты, улучшать цитаты, писать в чужие файлы. Он отдаёт записи в формате артефакта 2, ты сам пишешь их на диск и сам ведёшь реестр. Рейт-лимит — отступить, подождать, доложить; слепой ретрай запрещён.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — шесть условий, каждое проверяется командой:**

1. **Все источники из таблицы выше — в реестре**, либо расхождение объяснено файлом за файлом (не итогом). Плюс просмотрены все 15 дневников из греп-списка.
2. **У каждой строки реестра непустой хэш**, совпадающий с живым `git hash-object` на момент сдачи. Проверь скриптом или циклом и приведи вывод.
3. **У каждой записи корпуса есть `АДРЕС:` и `КАСАЕТСЯ:`** — число `### ` равно числу `АДРЕС: ` и числу `КАСАЕТСЯ: `, проверяется грепом.
4. **`KRATNOST-vladelca.md` покрывает корпус:** сумма вхождений по группам = число записей корпуса. Расходится — назови, сколько не попало.
5. **Раздел чисел непуст** и каждое число несёт адрес. Ноль найденных чисел — законный результат, но тогда так и напиши: «чисел не найдено, просмотрено N источников из N».
6. **Возобновляемость проверена самопрерыванием** после трёх источников, результат описан.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «замечаний не найдено», а «не найдено, прочитано 6 секций из 6». Без охвата вердикт не принимается.

**Границы.** Ни одного файла вне зоны не менять — только читать. Не писать уроков, не ставить вердиктов, не править канон и стандарты. **Не трогать `_generator/`, `.githooks/` и арку `2026-07-30_dovodka-l1` вообще** — там параллельно работают. Найденное вне зоны (битая ссылка, противоречие канона) — строкой в отчёт, не в правку.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР — обязателен, и проверяет ОДНО: не соврал ли реестр

Операция не лоссовая — ты ничего не двигаешь. Но у неё свой класс дефектов, и он тут главный: **реестр может утверждать, что источник прочитан, а прочитан он невнимательно.** Строку в таблицу написать дешевле, чем прочитать 735 строк живой речи, и модель под конец длинного прогона склоняется к первому.

Свежий субагент **в самом конце**, метод другой (реестра он не писал):

1. Выбирает **сам, не по твоей подсказке, три источника** — обязательно включая один из двух самых крупных (`PRAVKI.md` 735 строк, `RAZBOR-posle-lekcii` 586).
2. Читает их **целиком** и независимо выписывает замечания владельца по той же границе «претензия ↔ правка лекции».
3. Сверяет с корпусом: есть ли у него замечание, которого там нет.
4. **Отдельно проверяет цитаты:** взять пять записей корпуса и убедиться, что `ЦИТАТА:` действительно есть в источнике по указанному адресу дословно. Улучшенная цитата — брак того же класса, что выдуманная.
5. Пересчитывает `git hash-object` по трём файлам и сверяет с реестром.
6. Вердикт с охватом: «пропусков не найдено, проверено 3 источника из N, 5 цитат из 5 дословны» или список с адресами.

Нашёл пропуск — **не переписывай корпус молча**: допиши и назови в отчёте, что пропустил и почему. Считаешь вердикт неверным — опровергай с доказательством, это законно и на этой фабрике уже пять раз окупилось.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/REESTR-vladelca.md _studio/zhurnal/2026-07-30_dovodka-fabriki/KRATNOST-vladelca.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dobycha-vladelca.md _studio/docs/KARTA.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/REESTR-vladelca.md _studio/zhurnal/2026-07-30_dovodka-fabriki/KRATNOST-vladelca.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dobycha-vladelca.md _studio/docs/KARTA.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Реестр защищает от ДОПИСЫВАНИЯ источника и не защищает от НЕДОЧИТЫВАНИЯ — а обрыв между источниками делает корпус старше реестра, и следующий прогон верит реестру
Хэш блоба самоинвалидирует пометку, если источник изменился. Но обрыв случается не между
источниками, а внутри работы, и тогда бывает ровно наоборот: **записи в корпусе есть, строки
реестра нет.** Реестр в этом состоянии не «краснеет» — он молча занижает сделанное, и вход
следующего прогона строится по нему.
ЦЕНА: три прогона подряд, каждый раз одна и та же поломка. Прогон 2 нашёл её на `RAZBOR`
(56 записей в корпусе, строки реестра нет, файл пройден на 243 строки из 586). Прогон 3 нашёл
её же на шести дневниках: вход утверждал «506 записей, осталось 23 дневника», по диску —
613 и 17. Шесть дневников (445 строк) были бы прочитаны заново, и это не главная потеря:
**107 записей легли бы в корпус вторым экземпляром.** Дубли в корпусе не склеиваются
намеренно — кратность и есть то, что нужно судье, — поэтому 107 ложных кратностей ушли бы
судье как «владелец сказал это дважды». Устройство, которое это закрывает, стоит одну строку:
писать строку реестра ТЕМ ЖЕ ХОДОМ, что вливание блока в корпус, а не «после каждого
источника». Прогон 3 так и сделал — реестр и корпус не расходились ни на одну пачку.

### Гейт «цитата дословна» без ярусов даёт 30 % ложных срабатываний: легальные приёмы цитирования выглядят как выдумка
Заход объявляет улучшенную цитату браком того же класса, что выдуманная, и это правильно.
Но машинная проверка «цитата — подстрока источника» ловит не только брак. Она краснеет на
трёх законных приёмах: элизия, помеченная многоточием (`…`); обрамляющие кавычки-ёлочки,
которых в источнике на этих позициях нет; склейка двух соседних реплик через выброшенный
служебный таймкод `**[01:19]**` расшифровки.
ЦЕНА: первый прогон гейта объявил недословными **173 цитаты из 565**. Поверить ему значило
переклеймить 173 живые дословные цитаты в `ПЕРЕСКАЗ` и обесценить их для судьи — а именно
дословность несёт интонацию недовольства, ради которой корпус и собирается. Ушло четыре
итерации гейта, чтобы отделить брак от приёма; после четвёртой не найденных **0 из 565**.
Вывод для фабрики: гейт на дословность обязан возвращать ЯРУС, а не «да/нет», и печатать,
каким ярусом запись прошла. Булев гейт здесь хуже отсутствующего — он гарантированно
потребует испортить хорошие данные.

### У формата `АДРЕС:` нет машинного контракта, и субагенты разошлись в нём тремя способами — а по нему считается колонка «замечаний найдено»
Заход задаёт формат записи как жёсткий («по нему пойдёт греп») и приводит образец. Образца
оказалось недостаточно: из 12 субагентских блоков **17 источников** написаны по образцу,
**3** — с абсолютными путями `/Users/ivanyakovlev/.../SESSIYA.md`, **3** — в виде
`SESSIYA.md # \`## заголовок\`` с пробелами вокруг `#` и секцией в бэктиках.
ЦЕНА: колонка «замечаний найдено» реестра снимается командой
`grep -c '^АДРЕС: <путь>'` — и на этих шести источниках она давала **0**. Ноль в этой колонке
объявлен заходом законным результатом, то есть шесть строк реестра сказали бы «источник
прочитан, замечаний у владельца нет» — ровно тот ложный ноль, против которого реестр и
заведён. Поймано только потому, что я считал «сколько адресов НЕ начинается ожидаемым путём»
отдельной командой на каждом блоке до вливания. Это не бдительность, а везение: такой
проверки заход не требует. Формат, по которому считают числа, должен иметь чекер, а не
образец в тексте.

### Заход запретил трогать арку `dovodka-l1` «вообще» — и его же греп-список требует прочитать её дневник
§«Границы» велит не трогать арку `2026-07-30_dovodka-l1` ни в чём, а список источников
задан командой `grep -rl 'владелец' --include='SESSIYA.md' _studio/zhurnal/`, которая этот
дневник возвращает. Два указания захода противоречат друг другу, и решение остаётся
исполнителю без критерия.
ЦЕНА: **35 записей, 11 из них с названным числом**, то есть ~8 % всей добычи по дневникам,
стояли перед молчаливым выбросом. Прогоны 1 и 3 решили одинаково — читать read-only и
пометить в реестре, что источник живой; но это решение принято дважды, каждый раз с нуля, и
любой третий исполнитель мог решить наоборот и не сказать. Запрет на зону должен различать
«не писать» и «не читать»: они разные, и путать их дорого.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Первый ход сделан, предпосылки сверены (дата данных 2026-07-30):**
- `git branch --show-current` → `arka/mat-kostyak`, rc=0. Ветка та, `checkout` не нужен.
- Зона чиста в HEAD: `git diff --name-only HEAD -- <5 путей зоны>` → пусто, rc=0. Точку отката НЕ фабрикую (по §0).
- `wc -l` по таблице источников: **все семь строк сошлись дословно** — 735 / 586 / 301 / 283 / 109 / 82 и мелочь catalan 27+25+15+7. Расхождений нет.
- 🔴 **Расхождение №1: дневников не 15, а 23.** `grep -rl 'владелец' --include='SESSIYA.md' _studio/zhurnal/ | wc -l` → **23**, rc=0. Список из 23 путей взят в работу целиком; в реестре будет 23 строки, а не 15.
- Из этих 23 один — `2026-07-30_dovodka-l1/SESSIYA.md`, а §«Границы» велит арку `dovodka-l1` «не трогать вообще». Читаю его **только на чтение** (мутации в него не делаю — это и есть смысл запрета), но в реестре помечаю, что файл прямо сейчас пишется параллельным заходом и хэш блоба может устареть между моей строкой и проверкой верификатора. Молча выбрасывать источник с речью владельца хуже.

**Устройство прогона (свойство №1 — возобновляемость):**
1. Реестр и корпус пишутся **после каждой пачки источников**, дописыванием в конец файла. Реестр — первым, корпус — сразу за ним; ни одна строка реестра не появляется раньше, чем на диск легли её записи.
2. **Самопрерывание после первых трёх источников** (три самых крупных): перечитываю реестр с диска, проверяю, что по нему можно продолжить (виден список сделанного и хэши), пишу результат в отчёт. Только потом иду дальше.
3. Пачки субагентов по 1–3 источника, **не больше 4 одновременно**. Субагент отдаёт записи в формате артефакта 2 и НЕ пишет в файлы — на диск пишу я.
4. **Свой гейт на цитаты (сверх задания, дёшево):** каждая `ЦИТАТА:` требуется субагенту как дословный фрагмент ОДНОЙ строки источника; после каждой пачки прогоняю по корпусу проверку `grep -F` фрагмента в файле-источнике. Не нашлось — либо правлю на дословное сам по источнику, либо переклеймляю в `ПЕРЕСКАЗ`. Причина: §3 объявляет «улучшенную цитату» браком, а машинно это ловится за секунды.
5. `KRATNOST-vladelca.md` — в конце, по готовому корпусу; группы по ПРЕДМЕТУ недовольства + отдельный раздел всех `ЧИСЛО:`.
6. Верификатор — свежий субагент в самом конце, три источника выбирает сам, включая один из двух крупных.
7. Регистрация трёх новых `.md` через `register_doc.py` (единственная дверь), затем коммит двумя ходами с `KARTA.md` в обоих.

**Порядок источников** (крупное вперёд — если оборвёт, ценное уже добыто):
пачка 1 = `PRAVKI.md`(735) · `RAZBOR-posle-lekcii`(586) · `akt-1-razbor.md`(301) → **самопрерывание**;
пачка 2 = `PRAVKI-final.md`(283) · `PRAVKI-sceny-i-animacii.md`(109) · `zamechaniya-statya-mir2.md`(82) · четыре `zamechaniya-*` mir1(74);
пачки 3–5 = 23 дневника `SESSIYA.md` по 3 на субагента, по 4 субагента за раз.

**Критерий готовности не оспариваю** — шесть условий проверяемы командами, каждая у меня есть. Единственная поправка по факту: в условии 1 «все 15 дневников» читаю как «все дневники из греп-списка», а их 23.

---

### ВОЗОБНОВЛЕНИЕ — прогон 2 (2026-07-30), после обрыва сети

Прогон 1 оборвался. Взято с диска, заново не собиралось: `REESTR-vladelca.md` (1 строка) и
`KORPUS-VLADELCA.md` (94 записи). Пересчитано мной, а не принято на слово:
`grep -c '^### '` → **94**, и ровно столько же `АДРЕС:`/`ЦИТАТА:`/`КАСАЕТСЯ:`/`КЛАСС:`;
по источникам — **38** akt-1-razbor + **56** RAZBOR-posle-lekcii.

🔴 **Расхождение №2 — предпосылка возобновления ЛОЖНА в одной половине.** Мне сказано, что
оба источника пройдены и перечитывать их не нужно. Для `akt-1-razbor.md` это верно.
Для `RAZBOR-posle-lekcii-2026-07-27.md` — **нет: файл пройден чуть меньше чем наполовину.**
Доказательство, три независимых улики:
- В источнике 19 разделов `## ` (`grep -n '^## '`), последний начинается на строке 575.
  В корпусе адресуются только разделы **§0 … §8B** — дальше `## 9. S9 · Домики` (строка 244)
  и всё до §18 включительно не адресовано ни одной записью. Непрочитано **строк 244–586 из 586**.
- Шапка источника в самом корпусе (строки 322–325) ссылается на записи из **§13 (строки 323–325)**
  и **§16.10 (строка 472)** как на «оставленные обе» — этих записей в корпусе НЕТ. Шапка писалась
  под полный проход, обрыв случился до него.
- Там же: «отброшено 6 кандидатов из 117», то есть ожидалось ≈111 записей, лежит 56.
**Вывод:** прогон 1 оборвался ВНУТРИ второго источника, а не между источниками. Дочитываю
§9–§18 и дописываю; §0–8B не перечитываю — они на диске и адресованы.

**Почему это не мелочь, а дефект устройства** — в `## УРОКИ ФАБРИКЕ`: реестр пишется «после
каждого источника», и обрыв внутри источника оставляет корпус с половиной файла, а реестр —
вообще без строки. Единственное, что спасло, — что строку не успели написать: напиши прогон 1
`RAZBOR | весь файл | 586 | <хэш> | 56` тем же ходом, что и записи, — и хэш совпал бы, пометка
«прочитано» была бы зелёной, а половина источника молча пропала бы навсегда. Реестр защищает
от дописывания файла, но не от недочитывания файла.

**Порядок прогона 2:**
1. Строка реестра по RAZBOR — **первым ходом, но по факту**: секция `§0–8B (частично)`, не «весь
   файл». Писать «весь файл» = ровно та ложь, против которой реестр и заведён.
2. Пачка A, 4 субагента: добор RAZBOR §9–18 · `PRAVKI.md`(735) · `PRAVKI-final.md`(283)+
   `PRAVKI-sceny-i-animacii.md`(109) · пять `zamechaniya-*` catalan (82+27+25+15+7).
   После неё — запись на диск и **самопрерывание** с перечиткой реестра.
3. Пачки B–C: 23 дневника `SESSIYA.md` по 3 на субагента, ≤4 субагента за раз.
4. Гейт дословности цитат `grep -F` по каждой пачке · `KRATNOST-vladelca.md` · верификатор ·
   `register_doc.py` × 3 · коммит двумя ходами с `KARTA.md`.

**Предпосылки прогона 2, сверенные по диску (дата данных 2026-07-30):**
- `git branch --show-current` → `arka/mat-kostyak`, rc=0.
- `wc -l` по всем семи строкам таблицы источников — сошлись дословно (735/283/109/82/27/25/15/7
  и 586/301 у пройденных). Все восемь непройденных файлов существуют.
- `git hash-object teorkat-vvedenie/repeticia/akt-1-razbor.md` → `7f9bdf0b…`, **совпал** с
  единственной строкой реестра: за время обрыва источник не менялся.
- Дневников по-прежнему 23, а не 15 (расхождение №1 прогона 1 подтверждено).

---

### ВОЗОБНОВЛЕНИЕ — прогон 3 (2026-07-30), после второго обрыва (сбой авторизации)

**Что взято с диска, заново не собиралось** (пересчитано мной командой, а не принято на слово):
- `KORPUS-VLADELCA.md` — **613 записей** (`grep -c '^### '` → 613), и ровно столько же
  `АДРЕС:` / `КАСАЕТСЯ:` / `ЧИСЛО:` / `КЛАСС:` (613/613/613/613). Хвост файла целый: последняя
  запись завершена всеми пятью полями, обрыв не разрезал запись.
- `REESTR-vladelca.md` — 12 строк-источников (7 файлов таблицы захода, `PRAVKI.md` и `RAZBOR`
  по двум строкам каждый).
- `KRATNOST-vladelca.md` — **не существует** (`ls`), артефакт 3 не начат.

🔴 **Расхождение №3 — вход прогона 3 занижает сделанное, и в обе стороны.** Мне сказано «там
уже 506 записей» и «осталось ровно два куска». По диску:
- записей не 506, а **613**: прогон 2 успел пройти **6 дневников из 23** и записать их в корпус
  (36+28+18+11+8+6 = 107). Разбивка снята командой
  `grep -h '^АДРЕС: ' KORPUS-VLADELCA.md | sed 's/#.*//' | sort | uniq -c`.
- 🔴 **но реестр об этих 6 дневниках не знает ни строкой.** 506 — это ровно итоговая цифра
  реестра, и вход прогона 3 повторил её потому, что верил реестру. Реестр отстал от корпуса
  на 107 записей.
- Значит осталось не 23 дневника, а **17**, и дополнительный первый долг — **дописать реестр
  по 6 уже пройденным**, иначе следующий прогон перечитает их заново.

**Это второй раз, когда реестр соврал в одну и ту же сторону** (прогон 2 нашёл то же на
`RAZBOR`: корпус есть, строки реестра нет). Механизм один, урок — в `## УРОКИ ФАБРИКЕ`.

**Десять плотных источников подтверждены пройденными и НЕ перечитываются** — не по словам
входа, а по хэшам: `git hash-object` по всем десяти совпал с колонкой реестра (вывод в отчёте),
то есть за время двух обрывов ни один источник не менялся, и пометка «прочитано» жива.

**Порядок прогона 3:**
1. **Долг реестра:** 6 строк по пройденным дневникам, числа сняты с корпуса `grep -c`, хэши —
   живым `git hash-object`, секции — перечислением фактически заадресованных секций.
2. **17 оставшихся дневников**, три пачки по ≤4 субагента, крупное вперёд:
   пачка B = `teorkat-programma-dizajn`(1645) · `teorkat-motivacia`(1293) · `lekcia-1`(1100) ·
   `krivaya-drakona`(725); пачка C = `mat-kostyak`(579) · `dovodka-fabriki`(481) ·
   `dovodka-l1`(274)+`paskal-lekcia-sborka`(206) ·
   `teorkat-landshaft`(192)+`konspekt-l1`(181)+`teorkat-l1`(118); пачка D =
   `vvedenie-sborka`(116)+`vneshnie-istorii`(99)+`osnovanie-vvedenie`(37) ·
   `teksty-l1`(80)+`teorkat-programma`(76)+`l3-kodirovanie`(62). Всего 7264 строки.
   В дневниках читаются **только куски с прямой речью владельца и его реакцией на артефакт**.
3. **Устройство против обрыва внутри источника** (дефект, найденный прогоном 2): субагент
   складывает свой блок в `/tmp/vlad/<имя>.md`, я вливаю его в корпус и **тем же ходом** пишу
   строку реестра. Реестр не отстаёт от корпуса ни на одну пачку — ровно то, чего не сделали
   прогоны 1 и 2.
4. Гейт дословности цитат `grep -F` по каждой пачке · `KRATNOST-vladelca.md` (сводка по предмету
   + раздел ВСЕХ `ЧИСЛО:`) · верификатор · `register_doc.py` × 2 (корпус и реестр уже могли быть
   не зарегистрированы — проверю) · коммит двумя ходами с `KARTA.md`.

**Предпосылки прогона 3, сверенные по диску (дата данных 2026-07-30):**
- `git branch --show-current` → `arka/mat-kostyak`, rc=0. `checkout` не делаю.
- Дневников по-прежнему **23** (`grep -rl` → 23), расхождение №1 подтверждено третий раз.
- Все 17 оставшихся дневников существуют, суммарно 7264 строки (`wc -l`).

## ВОПРОСЫ — (заполняет исполнитель)

1. **270 чисел смешивают три разные вещи, и различить их может только читающий адрес.**
   Часть — требования владельца («меньше пятидесяти слайдов»), часть — его ОТКАЗЫ от чужого
   числа («полтора часа — это много», «десять примеров — перебор», «числовые планки снял обе»),
   часть — замер, названный вслух («кончилась за 50 минут вместо 90»). Заход велел помечать
   `ЧИСЛО:` и не велел их классифицировать, я и не классифицировал — оговорка стоит в шапке
   §2 `KRATNOST-vladelca.md`. **Вопрос: классификация каждого числа на требование / отказ /
   замер — работа судьи или отдельного захода добычи?** Гейт нельзя написать ни с одной из
   трёх строк, не открыв источник, а это 270 открываний.
2. **Группировка кратности машинная, и остаток 137 записей (10 %) лежит в группах
   `<ось> — прочее, правилом не поймано»`.** Я сознательно не доводил правила до нулевого
   остатка: подгонка регексов под остаток даёт красивое число и группы, которых нет в природе.
   **Вопрос: остаток разбирает судья глазами, или нужен второй заход, доводящий словарь?**
3. **Инструменты прогона живут вне репозитория и умрут вместе с сессией.**
   `gate_citat.py` (четырёхъярусный гейт дословности) и `kratnost.py` (сборка кратности по
   таблице правил) лежат в `/tmp/vlad/`, потому что зона захода — четыре `.md` и `KARTA.md`,
   а класть `.py` в чужую зону я не стал. Таблица правил напечатана внутрь
   `KRATNOST-vladelca.md §3`, так что группировка воспроизводима руками, но не одной командой.
   **Вопрос: переносить ли оба скрипта в `_generator/tools/` отдельным заходом?** Гейт
   дословности выглядит нужным всем будущим корпусам, а не только этому.

## ОТЧЁТ — (заполняет исполнитель)

### Возобновление: прогон оборван ДВАЖДЫ, оба раза по сбою авторизации

Это **третий** прогон захода. Прогон 1 оборвался внутри второго источника, прогон 2 — на
дневниках. Работа с диска уцелела оба раза и заново не собиралась.

**Взято с диска, пересчитано командой, а не принято со слов входа:**
- `KORPUS-VLADELCA.md` — **613 записей** (`grep -c '^### '`), и столько же `АДРЕС:` / `ЦИТАТА:`
  / `КАСАЕТСЯ:` / `ЧИСЛО:` / `КЛАСС:`. Хвост файла целый: обрыв не разрезал запись.
- `REESTR-vladelca.md` — 12 строк-источников.
- `KRATNOST-vladelca.md` — не существовал, артефакт 3 не был начат.

🔴 **Вход прогона 3 занижал сделанное, и это оказалось не мелочью.** Мне сказано «там уже 506
записей» и «осталось ровно два куска: дневники и кратность». По диску записей **613**: прогон 2
успел пройти **6 дневников из 23** (36+28+18+11+8+6 = 107 записей) — **но строк реестра по ним
не написал.** 506 — это ровно итоговая цифра реестра, и вход повторил её потому, что верил
реестру. Следствия: осталось не 23 дневника, а 17; и первым долгом надо было дописать реестр
по шести пройденным, иначе следующий прогон перечитал бы их заново и положил бы 107 записей
вторым экземпляром — а дубли в этом корпусе намеренно не склеиваются, то есть ушли бы судье
как ложная кратность. Механизм разобран в `## УРОКИ ФАБРИКЕ`, урок первый.

**Десять плотных источников подтверждены пройденными не по словам входа, а по хэшам:** все
десять `git --no-optional-locks hash-object` совпали с колонкой реестра (вывод — 10 строк `OK`).
За время двух обрывов ни один источник не менялся, пометка «прочитано» жива, перечитывание
законно не нужно.

### Что сделано и зачем

**Прочитано источников: 33 из 33.** 10 плотных файлов замечаний (все 7 строк таблицы захода)
+ **все 23 дневника** `SESSIYA.md`. Прогон 3 добавил 17 дневников (7264 строки) и закрыл долг
реестра по шести.

**Корпус: 613 → 1379 записей.** Прирост 766 за этот прогон. Разбивка по источникам — в реестре
строка за строкой; счётчики сняты с корпуса командой `grep -c '^АДРЕС: <путь>'`, а не со слов
субагентов (прогон 2 показал, что они систематически занижают свой же счёт).

**Собран артефакт 3 `KRATNOST-vladelca.md`:** 56 групп по ПРЕДМЕТУ недовольства + раздел всех
270 `ЧИСЛО:` с адресами + таблица правил группировки. Группировка сделана машинно по
упорядоченной таблице регексов, а не «на глаз», по двум причинам: 1379 записей руками не
разложить без ошибок, и — важнее — ручная раскладка невоспроизводима, судья не может её
оспорить. Здесь он может пересобрать группировку, поменяв одну строку таблицы; таблица
напечатана в самом артефакте (§3). Сумма вхождений равна числу записей **по построению**, а не
по подсчёту.

**Устройство прогона против обрыва внутри работы** (дефект, найденный прогоном 2): субагент
складывал блок в `/tmp/vlad/<имя>.md`, я вливал его в корпус и **тем же ходом** писал строку
реестра. За весь прогон реестр не отставал от корпуса ни на одну пачку.

**Субагентов 12** (10 добывающих + 2 в последней пачке), не более 4 одновременно, слот
освобождался — запускался следующий. Ни один не писал в репозиторий; на диск в зону писал я.
Рейт-лимитов не было.

### Как проверил — шесть условий критерия готовности, каждое командой

| условие | команда | результат |
|---|---|---|
| 1. Все источники в реестре | `grep -h '^АДРЕС: ' $K \| sed 's/^АДРЕС: //;s/#.*//' \| sort -u \| wc -l` · `grep -rl 'владелец' --include='SESSIYA.md' _studio/zhurnal/ \| wc -l` | **33 источника · 23 дневника, все 23 заадресованы.** Расхождение с заходом: дневников 23, а не 15 — подтверждено трижды, тремя прогонами |
| 2. У каждой строки реестра живой хэш | цикл `git --no-optional-locks hash-object` по всем строкам таблиц | **строк 35 (33 уникальных пути) · совпало 35 · разошлось 0** |
| 3. У каждой записи `АДРЕС:` и `КАСАЕТСЯ:` | `grep -c` по пяти полям | **`###`=1379, `АДРЕС:`=1379, `ЦИТАТА:`=1379, `КАСАЕТСЯ:`=1379, `ЧИСЛО:`=1379, `КЛАСС:`=1379** |
| 4. Кратность покрывает корпус | сумма `**N**` сводной таблицы и сумма по заголовкам групп | **56 групп, сумма 1379 = 1379 записей**, обе суммы независимо |
| 5. Раздел чисел непуст, у каждого числа адрес | подсчёт строк таблицы §2 и строк без адреса | **270 строк, без адреса 0** |
| 6. Возобновляемость проверена | см. ниже | **проверена дважды, и один раз не мной** |

**Про условие 6 — честно.** Заход требует самопрерывания после трёх источников. Этот прогон
проверил возобновляемость сильнее и не по своей воле: **он сам является проверкой** — стартовал
с диска после двух обрывов и продолжил работу, а не начал заново. Дополнительно: устройство
«строка реестра тем же ходом» проверялось после каждой из пяти пачек перечитыванием реестра с
диска (по нему виден список сделанного и хэши), и в конце — полной сверкой 35 хэшей. Отдельного
искусственного самопрерывания я не делал: два настоящих обрыва — сильнее любого имитированного,
и второй из них поймал реальный дефект устройства (реестр молча отстаёт от корпуса).

**Сверх задания — гейт дословности цитат.** `§3` объявляет улучшенную цитату браком того же
класса, что выдуманная, а машинно это ловится за секунды. Собран `/tmp/vlad/gate_citat.py`,
прогнан по каждому блоку до вливания и по всему корпусу после. Итог по всем 1379 записям:
**не найдено дословно — 0.** Разбивка: 1149 дословны в одной строке источника · 19 через
перенос строк источника · 33 после снятия служебного таймкода расшифровки · 83 по фрагментам с
честно помеченной элизией `…` · 95 честных `ПЕРЕСКАЗ` (дословности не обязаны).
**Выдуманных цитат в корпусе нет — включая 613 записей, собранных прошлыми прогонами.**
Почему гейт четырёхъярусный, а не булев, и что это стоило — урок второй в `## УРОКИ ФАБРИКЕ`.

**Пойманное в чужой работе до вливания:** шесть источников из 23 пришли с `АДРЕС:` не по
образцу (3 с абсолютными путями, 3 с `# \`## секция\``). Приведены к конвенции корпуса
`sed`-ом до вливания. Незамеченным это дало бы шести строкам реестра «замечаний найдено = 0» —
ложный ноль. Урок третий.

### Содержательное, что стоит знать судье (не вердикты — наблюдения с адресом)

- **Про визуал владелец молчит там, где нет отрисованного артефакта.** В семи дневниках
  (`teorkat-l1`, `teorkat-landshaft`, `teorkat-programma`, `teksty-l1`, `l3-kodirovanie`,
  `teorkat-motivacia`, `lekcia-1`) замечаний про иллюстрации, вёрстку и компоновку **нет вовсе
  или почти нет**, и причина одна: слайдов на тот момент не существовало. Весь визуал живёт в
  `PRAVKI.md`, `RAZBOR-posle-lekcii`, `PRAVKI-final.md` и `PRAVKI-sceny-i-animacii.md` — то есть
  ТОЛЬКО там, где он смотрел готовый дек. Для фабрики это значит: без раннего живого артефакта
  замечаний по визуалу не будет — не потому, что всё хорошо.
- **Часть порогов в корпусе — не требования, а отказы.** В дневнике `dovodka-l1` владелец
  бракует числа (50 слайдов, полтора-два часа, десять примеров), но своего «должно быть N» не
  называет ни разу — все положительные потолки там аналитика или канона. См. `## ВОПРОСЫ` №1.
- **Законный ноль по числам:** `vneshnie-istorii/SESSIYA.md` — 27 замечаний и **ни одного
  числа**; все числа того дневника (129, 242, 265, 14) принадлежат аналитику и приёмке.
- **Кратность видна и она говорящая.** Порог «полтора часа» звучит в трёх видах в двух
  дневниках; «30–45 слайдов» назван дважды в одном; «не хватает анимации» — трижды в одном
  ходе; «обязательно визуализация» — дважды дословно. Дубли не склеены нарочно.
- **Один источник почти целиком пересказной:** `l3-kodirovanie/SESSIYA.md` — 23 записи из 26
  помечены `ПЕРЕСКАЗ`, потому что речь владельца дана там пересказом аналитика. Дословных
  цитат в этом дневнике почти нет физически, и это свойство источника, а не добычи.
- **Дистилляция сырых расшифровок, читать которые заход запретил, потерь не показала**
  в одном проверяемом месте: `akt-1-razbor.md` сам сознаётся, что четыре служебные реплики
  обрезаны многоточием (строки 27, 32, 35, 38) и полный текст есть только в сырой расшифровке.
  Две записи корпуса поэтому дословны, но неполны. Больше следов потерь не встретилось.

### Что НЕ трогал

Ничего вне зоны. Все 33 источника — только на чтение. `_generator/`, `.githooks/` и арку
`2026-07-30_dovodka-l1` не менял; дневник последней прочитан один раз read-only и помечен в
реестре как чужой и живой. Законов не формулировал, вердиктов не ставил, канон и стандарты не
правил. `_studio/docs/KARTA.md` изменён только через `register_doc.py` — руками не трогал.

### 🔴 НЕОБРАТИМОЕ

**Необратимого нет.** Ни удалений, ни перезаписей, ни переименований, ни перемещений, ни
`git reset`/`checkout`. Все правки — дописывание в конец трёх файлов моей зоны и один вызов
`register_doc.py`. Единственный созданный с нуля файл — `KRATNOST-vladelca.md`, он новый и
ничего не перезаписывал. Файлы вне зоны не менялись ни одним ходом.

### Верификатор

*(результат вписан ниже отдельным ходом, после его возврата)*

### Открытое «возвращаться»

- Три вопроса выше — решение владельца или судьи, не моё.
- Остаток 137 записей в группах «прочее» кратности: правила словаря можно доводить, но подгонка
  под остаток даёт группы, которых нет в природе. Оставлено честным остатком.
- `gate_citat.py` и `kratnost.py` живут в `/tmp/vlad/` и не переживут сессию.

**Время прогона + токены** — снимает приёмка из `result`-строки лога прогона (исполнителю
счётчик недоступен).

**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
