# Канал исполнителя — melkie-dolgi (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: sonnet** — пять независимых мелких правок, каждая расписана до строки, план по ходу рождаться не должен.

⚠ **Одна правка контракта вручную, называю честно:** аналитик дописал в `ЗОНУ` пути `.githooks/pre-commit` и `check_tool_contract.py` уже после генерации файла — генератор их не знал. Пересобрать заход из песочницы нельзя (`rm: Operation not permitted`), поэтому правка сделана Edit-ом. Контракт от этого не слабее, но знай: зона в этом файле собрана не одним ходом генератора.

КОНТЕКСТ. Репозиторий `materials/` — фабрика курсов: собирает лекции-презентации из markdown в самодостаточный HTML-дек. За сутки фабрику сильно чинили; ты закрываешь **пять мелких долгов**, которые предыдущие заходы нашли, но не могли тронуть — каждый лежал вне их зоны. Все пять проверены на живых файлах, ни один не выдуман.

**Долг 1 — 🔴 самый важный: фильтр путей в хуке уже объявленного охвата, и ядро едет без гейта.**
У фабрики есть контракт инструмента: у правленого инструмента обязана быть фикстура, которая его проверяет, и фикстура объявляет охват строкой `# TOOL-CONTRACT-COVERS:`. Механизм работает — но `.githooks/pre-commit:62-63` отбирает staged-пути грепом `^_generator/tools/.*\.py$` · `^_generator/tools/fixtures/.*\.sh$` · `^\.githooks/`, и только отобранное уходит в `check_tool_contract.py --fixtures-for`.
Проверено предъявлением: `--fixtures-for` честно находит фикстуру `dvizhok` для всех пяти объявленных имён, а фильтр хука пропускает лишь `bootstrap_zahod.py`. **`skeleton/engine.js`, `skeleton/base.css`, `build_deck.py`, `render.py` не подходят ни под один шаблон и до проверки не доходят.** Вчера правка движка и каскада — код, через который проходит каждый слайд каждого дека, — уехала в коммит при зелёном хуке и верно объявленном охвате.

**Долг 2 — `check_uroki.py` судит пунктуацию, а не наличие цены.** Гейт требует строку ровно `ЦЕНА: `. Урок с развёрнуто названной ценой, начинавшийся словами `ЦЕНА обратная, и она названа конкретно:`, объявлен уроком без цены и **остановил коммит владельца 31.07**. Первый случай того же класса — 21.07: жирная `**ЦЕНА:**` объявила «без цены» семь полноценных уроков закрытой арки и месяцами блокировала коммиты ЧУЖИХ заходов; тогда починили точечно, разрешив жирную, а класс не закрыли.

**Долг 3 — `audit.py --scene-diff` считает не те слайды.** Он берёт `.slide`, а движок — `.slide:not([data-skip])`. У `dandelin` это 19 против 18: каждый индекс после пропущенного слайда указывает на СОСЕДА, то есть аудит судит не тот слайд, что называет. Ровно этот дефект вчера нашли и починили в `render.py`; в `audit.py` он остался, потому что тот файл был вне зоны.

**Долг 4 — порождённый каскад сцен квадратичен, потолка нет.** Вчера каскад `.scene-N` перевели с рукописного списка на порождение по фактическому максимуму `data-scenes`. Размер растёт как квадрат: n=12 → 2,9 КБ, n=100 → 171 КБ, n=400 → 2,8 МБ CSS внутри самодостаточного монолита. Опечатка `data-scenes="44"` вместо `4` раздувает выход молча. Таких литералов в репозитории сейчас нет (проверено грепом) — это защита на будущее, а не починка сегодняшней поломки.

**Долг 5 — три хвостовые ветки шумят у сторожа.** Вчера в `git_zona.py doctor` поставлен сторож соседних веток. Он честно называет три ветки с невлитыми коммитами: `arka/vneshnie-istorii` (4), `arka/poisk-listochki` (2), `main` (1). **Содержимое всех трёх уже на рабочей ветке надмножеством** — сверено аналитиком пофайлово 31.07: `POISK-*` побайтово идентичны, `KARTOTEKA-SYUZHETOV` 557 строк против 546, `buffon/WORKLIST` 239 против 199 и ноль строк ветки отсутствует. То есть находки безобидны, но сторож будет предъявлять их при каждом `doctor`, и его перестанут читать — ровно тот механизм, которым `RESHENIYA Р31` объясняет смерть гейтов.

**ЦЕЛЬ: закрыть все пять. Долг 1 — первым, он блокирующий.**

Приёмка — по ОТЧЁТУ. **Если стоп до цели: сдай долг 1 с ловушкой — остальные четыре независимы и переносятся.**

🔴 **`--no-verify` запрещён без исключений.** Обходов в автологе на старте — **28**, приёмка сверит.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** .githooks/pre-commit _generator/tools/check_tool_contract.py _generator/tools/check_uroki.py _generator/tools/fixtures/uroki/ _generator/audit.py _generator/build_deck.py _generator/tools/fixtures/dvizhok/ _studio/docs/KARTA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_melkie-dolgi.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add .githooks/pre-commit _generator/tools/check_tool_contract.py _generator/tools/check_uroki.py _generator/tools/fixtures/uroki/ _generator/audit.py _generator/build_deck.py _generator/tools/fixtures/dvizhok/ _studio/docs/KARTA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_melkie-dolgi.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Снять ЧИСЛА ДО работы командами.** Мои — ориентир 31.07; разошлось, объясни и работай от своей базы:
```
python3 _generator/build_deck.py buffon/src --lint ; echo rc=$?
python3 _generator/build_deck.py dandelin/src --lint ; echo rc=$?
grep -cE '^- 2026.*--no-verify' _studio/zhurnal/_INFRA-git/INCIDENTY.md
python3 _generator/tools/check_kartoteka.py teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md --gates 2>&1 | grep 'Не зарегистрированы'
python3 _generator/tools/git_zona.py doctor 2>&1 | grep -A3 'СОСЕДНЕЙ ВЕТКИ'
```
База: `buffon` rc=0, `dandelin` **rc=1** (так и должно быть), обходов **28**, сирот **9**, сторож называет **три** ветки.
- **Прочитать ТОЛЬКО эти якоря** (проект не изучай):
  - `.githooks/pre-commit` — весь, правишь его; долг 1;
  - `_generator/tools/check_tool_contract.py` — как объявляется и разрешается охват (`--fixtures-for`, `TOOL-CONTRACT-COVERS`);
  - `_generator/tools/fixtures/dvizhok/PROGNAT.sh` — сюда добавляешь ловушку долга 1; заодно образец жанра;
  - `_generator/tools/check_uroki.py` + `_generator/tools/fixtures/uroki/PROGNAT.sh` — долг 2;
  - `_generator/audit.py` — **строки 55–70 и 176–215**; и `_generator/render.py` строки ~40–60, где вчера починили ровно этот счёт слайдов;
  - `_generator/build_deck.py` — функции `max_scenes()` и `scene_cascade_css()`, долг 4;
  - `_studio/docs/kak-delat/GIT-disciplina.md` §4б — подкоманда `merge`, долг 5.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

🔴 **Сквозное правило, купленное трижды за сутки: ЛОВУШКА СУДИТ РЕЗУЛЬТАТ ОПЕРАЦИИ, А НЕ ТЕКСТ ВЫВОДА.** «В выводе есть подстрока X» — не проверка: у вывода почти всегда есть легальный путь содержать X при сломанном механизме. Три оплаченных случая: `grep -c` на индексе-абзаце остался зелёным при снятой идемпотентности · грубый грep имени ветки прошёл на сломанном стороже, потому что имя встречалось в строке диагностики · проверка «в выводе есть число» прошла, потому что число напечатал сырой откат фильтра. **Каждую свою ловушку проверь мутацией: сломал → покраснела → вернул.** Не покраснела — ловушки нет, есть её видимость.

### Долг 1 — хук отбирает пути уже, чем объявлен охват

`.githooks/pre-commit:62-63`. Задача: под контракт инструмента обязано попадать всё, над чем какая-нибудь фикстура объявила `TOOL-CONTRACT-COVERS`, а не заранее выбранный список каталогов.

- **Форму решай сам и объясни в `## ПЛАН`.** Прямолинейное расширение шаблонов (дописать `_generator/skeleton/`, `build_deck.py`, `render.py`) закроет сегодняшний случай и оставит болезнь: следующий охват над новым путём снова не доедет. Ход, снимающий класс, — спрашивать у `check_tool_contract.py`, покрыт ли путь, вместо того чтобы гадать по шаблону. Взвесь стоимость: хук гоняется на КАЖДОМ коммите троих писателей, и лишний запуск python на каждый файл может стоить заметных секунд. Выбери и обоснуй.
- ⚠ **Не сделай хук шумным.** Он молчит, когда чисто, и это условие его выживания (`Р31`). Расширение фильтра не должно поднимать фикстуры на путях, где охвата нет.
- **Ловушка** (в `fixtures/dvizhok/PROGNAT.sh` — она и пострадала): сломать файл, покрытый охватом, но не подходящий под старые шаблоны (`skeleton/engine.js`), застейджить в одноразовом репо и убедиться, что коммит **остановлен**. Вторая половина обязательна: файл БЕЗ охвата коммитится молча.

### Долг 2 — гейт уроков судит наличие цены, а не пунктуацию

`check_uroki.py`. Форма проверки: строка начинается с токена `ЦЕНА`, дальше любой разделитель, дальше **непустой** текст. Пустую цену по-прежнему валить — это исходное требование гейта и его смысл.

Фикстура `fixtures/uroki/` уже есть, в ней четыре случая. Добавь обе исторические формы, на которых гейт ошибался: `ЦЕНА обратная, и она названа конкретно: …` (проходит) и `**ЦЕНА: …**` жирная (проходит, уже поддержана — закрепи, чтобы не потерять при правке регекспа). Плюс граничное: `ЦЕНАЛОГ: 5` — **не** цена, слово другое; гейт обязан краснеть.

### Долг 3 — `audit.py` считает те же слайды, что движок

Заменить счёт `.slide` на `.slide:not([data-skip])` — ровно как вчера в `render.py`, посмотри там на готовое решение. **Проверь на `dandelin`** (у него 19 против 18) и назови в отчёте, изменился ли вердикт аудита на нём.

### Долг 4 — потолок на `data-scenes`

`build_deck.py`. Порог назначь **по фактическому максимуму живых деков плюс запас**, а не из головы: максимумы сейчас `buffon` 5, `fibonacci` 4, `dandelin` 4, `teorkat` 2 (сверь сам). Превышение — внятная ошибка сборки с указанием слайда и числа, а не молчаливые мегабайты CSS. Ловушка: дек с `data-scenes` выше порога **не собирается**, дек на пороге — собирается.

### Долг 5 — влить три хвостовые ветки

`git_zona.py merge <ветка>` по одной. Ветки **не удалять** — это решение владельца.

- **Сначала убедись, что вливаешь безобидное.** Для каждой: взять строки, которые ветка добавляет, и проверить, что все они уже есть на рабочей. Разошлось хоть в одной — **стоп, в отчёт, не вливай**: значит замер аналитика устарел или был неверен, и это важнее, чем закрытая задача.
- **После вливания сторож обязан замолчать про эти три** и остаться зелёным с охватом «проверено N из N». Предъяви вывод `doctor` до и после.
- ⚠ Конфликт при вливании — **стоп и в отчёт**. Ожидается, что его не будет; если есть — предпосылка «содержимое уже на рабочей» неверна, и разбираться должен аналитик.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — шесть условий. Числа снимай СВОИ, мои — ориентир 31.07:**

1. **Долг 1 работает вживую:** в одноразовом репо сломанный `skeleton/engine.js` **останавливает** коммит, здоровый — не мешает. Обе половины проверены мутацией.
2. **Долг 2:** `check_uroki.py` пропускает обе исторические формы и краснеет на пустой цене и на `ЦЕНАЛОГ`. Фикстура `fixtures/uroki/PROGNAT.sh` → `rc=0` и содержит новые случаи.
3. **Долг 3:** `audit.py` на `dandelin` считает 18, а не 19; вердикт назван до и после.
4. **Долг 4:** дек выше порога не собирается с внятным сообщением; порог обоснован числом, а не назначен.
5. **Долг 5:** `doctor` после вливания молчит про три ветки, охват печатается; ни один merge не дал конфликта (иначе — стоп и отчёт).
6. **Все шесть фикстур зелёные** (`sostoyanie`, `git_zona`, `uroki`, `register_doc`, `tool_contract`, `dvizhok`), **три дека дают тот же результат, что до тебя** (`buffon` rc=0, `fibonacci` rc=0, `dandelin` rc=1 с шестью структурными — сверяй ИЗМЕНЕНИЕ, а не «собралось ли»), коммит без `--no-verify`, `KARTA.md` в том же коммите, обходов **28**, сирот **9**.

**Границы.** Не трогай `engine.js`, `base.css`, `render.py`, `git_zona.py`, `register_doc.py`, `check_kartoteka.py`, `bootstrap_*`, папки деков и `teorkat-vvedenie/`. Не чини `dandelin` (его шесть структурных — отдельный долг). Не снимай метку `otkat-pered-vlivaniem`. Ветки не удаляй.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР — обязателен, две линзы

Долг 5 лоссовый (merge), долг 1 меняет гейт, через который идёт каждый коммит троих писателей. Плюс у свежих защит на этой фабрике устойчивый дефект: **выглядит поставленной и не срабатывает** — вчера пять ловушек из свежих оказались декоративными, и вскрыл это только мутационный прогон.

Свежий субагент в конце, твоего контекста нет, боевой репозиторий читает, песочница в `/tmp`:

1. **Атака на долг 1: как закоммитить сломанный инструмент так, чтобы хук промолчал?** Путь под охватом, но с необычным именем · файл, добавленный переименованием · охват, объявленный в фикстуре, которую сама не покрывает · `git add` сломанного и восстановление на диске до `commit` (эта дыра известна и НЕ твоя задача — но проверь, не расширил ли ты её). Вердикт с охватом: «промолчать не удалось, проверено N способов из выписанных M».
2. **Потери при вливании трёх веток:** взять строки, которые каждая ветка добавляла, и предъявить числом — «потеряно 0 строк из N» либо поимённый список. Метка отката и версии веток живы, сверять есть с чем.
3. **Не стал ли хук шумным:** сделать три-четыре обычных коммита (правка `.md`, правка дека, правка инструмента без охвата) и убедиться, что хук молчит и не тормозит. Замерь время `pre-commit` до и после — если выросло заметно, это цена, которую надо назвать.

Нашёл — **не чини молча**: назови и почини, назвав, что было дырой. Считаешь вердикт неверным — опровергай с доказательством; на этой фабрике исполнитель уже пять раз был прав против аналитика.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- .githooks/pre-commit _generator/tools/check_tool_contract.py _generator/tools/check_uroki.py _generator/tools/fixtures/uroki/ _generator/audit.py _generator/build_deck.py _generator/tools/fixtures/dvizhok/ _studio/docs/KARTA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_melkie-dolgi.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- .githooks/pre-commit _generator/tools/check_tool_contract.py _generator/tools/check_uroki.py _generator/tools/fixtures/uroki/ _generator/audit.py _generator/build_deck.py _generator/tools/fixtures/dvizhok/ _studio/docs/KARTA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_melkie-dolgi.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Обнаружено ДО плана (важно для приёмки):** зона НЕ была чиста в HEAD — `.githooks/pre-commit`, `check_uroki.py`, `fixtures/uroki/PROGNAT.sh`, `audit.py`, `build_deck.py`, `KARTA.md` уже несли незакоммиченные правки, а `git log` показывает `2f1e396 Merge branch 'arka/poisk-listochki'` как текущий HEAD. Значит долги 1–4 частично закодированы, а долг 5 частично исполнен (одна из трёх веток уже влита) — предыдущим проходом этой же сессии до сжатия контекста. Беру это как базу, не переделываю с нуля; проверяю и достраиваю до критерия готовности.

Проверка по каждому долгу:
1. **Долг 1.** Хук (`pre-commit:70-99`) переписан: AST-гейт (`TOOLS_STAGED`) сузился до питон-инструментов, а охват фикстур (`ALL_STAGED` → `--fixtures-for`) теперь идёт по ВСЕМ staged-путям — ровно тот ход, что заход требовал («спрашивать check_tool_contract.py, а не гадать по шаблону»). `fixtures/dvizhok/PROGNAT.sh` шапка объявляет охват на пяти именах и в комментарии (строка 13) заявляет «ловушка 5 ниже держит именно этот маршрут» — **но самой ловушки 5 в файле нет** (проверено `grep`). Это ровно дефект, о котором предупреждает §2 захода: «выглядит поставленной и не срабатывает». Дописываю ловушку 5: сломать `skeleton/engine.js` (путь под охватом, но вне старых regex-шаблонов), застейджить в одноразовом репо, убедиться, что коммит остановлен; и вторую половину — путь БЕЗ охвата коммитится молча.
2. **Долг 2.** `check_uroki.py` regex и фикстура (`obratnaya`, `cenalog`) уже на месте — прогоняю `fixtures/uroki/PROGNAT.sh`, ожидаю `rc=0` со всеми 6 случаями.
3. **Долг 3.** `audit.py` уже переключён на `.slide:not([data-skip])` — проверяю на живом `dandelin/src/dist/index.html` браузером: старый селектор даёт 19, новый 18, называю оба числа как «до/после».
4. **Долг 4.** `SCENE_CAP=10` и проверка №9 в `lint()` уже на месте — тестирую на СКРЭТЧ-копии `buffon/src` (не трогаю сам `buffon/`): слайд с `data-scenes=11` не собирается с внятной ошибкой, слайд `=10` (на пороге) собирается.
5. **Долг 5.** `doctor` сейчас называет 2 ветки (`arka/vneshnie-istorii` 4 коммита, `main` 1 коммит) — `arka/poisk-listochki` уже влита. Для каждой из двух: сверяю добавляемые строки против рабочей (уже на рабочей надмножеством, по утверждению аналитика) — если разошлось, стоп и в отчёт; иначе `git_zona.py merge <ветка>` по одной, без удаления. После — `doctor` обязан замолчать про все три (включая уже влитую) с «проверено N из N».

Дальше: калибровка `check_tool_contract.py --all` (не сломал ли контракт при правке хука/фикстуры), верификатор (две линзы §3), коммит зоны (§4).

Модель — sonnet, задача мелкая по каждому долгу, план не разрастаю сверх сказанного.

## ВОПРОСЫ

Нет открытых вопросов, требующих решения владельца ДО приёмки — развилки (форма долга 1, потолок долга 4, стоп на конфликте долга 5) разобраны и обоснованы в ОТЧЁТЕ.

## ОТЧЁТ

**Долг 1 — `.githooks/pre-commit`.** Диагноз подтверждён по `fixtures/dvizhok/PROGNAT.sh` (комментарий-зазор вчерашнего захода). Правка: узкий каталоговый фильтр `TOOLS_STAGED` остался ТОЛЬКО триггером AST-гейта над python-инструментами (`check_tool_contract.py --staged`); вопрос «какую фикстуру поднять» полностью передан `check_tool_contract.py --fixtures-for $ALL_STAGED`, где `ALL_STAGED` — ВЕСЬ staged-список (`git diff --cached --name-only --diff-filter=ACMR`) без фильтра по каталогу. Проверено предъявлением: `--fixtures-for _generator/skeleton/engine.js _generator/build_deck.py _generator/render.py` теперь честно печатает `fixtures/dvizhok/PROGNAT.sh`. Комментарий-зазор в `dvizhok/PROGNAT.sh` заменён на «зазор закрыт», дописана **постоянная ловушка 5** (одноразовый repo в `/tmp`, копия РЕАЛЬНОГО `.githooks/pre-commit` + `check_tool_contract.py`, стейджится только тестовый модуль — не `-A`, чтобы сам скаффолдинг не попал под гейт): путь под охватом вне старых шаблонов — красная фикстура останавливает коммит, зелёная не мешает; путь без охвата коммитится молча. **Мутацией проверено лично**: вернул старый (снятый из HEAD) хук на файл — ловушка 5 покраснела (`ФИКСТУРЫ КРАСНЫЕ`), вернул починенный — снова зелёная.

Верификатор (свежий субагент, боевой репозиторий на чтение, песочница `/tmp`, без моего контекста) атаковал 4 способа обхода: (а) необычный каталог у покрытого пути, (б) `git mv`/rename, (в) фикстура-пустышка, которая просто заявляет охват, (г) `git add` сломанного + восстановление на диске до `commit`. Промолчать не удалось на (а)-(в). (г) — реальная, но НЕ моя дыра механизма (фикстура читает диск, не индекс): для файлов внутри `_generator/tools/` поведение старого и нового хука идентично (было/осталось), а для файлов вне неё (`engine.js` и три соседа) защита **впервые появилась** — раньше 0 защиты, теперь есть защита с тем же старым ограничением; это не регрессия, а расширенный периметр уже известного и не входящего в зону долга изъяна.

**Цена, которую называю прямо**: `check_tool_contract.py --fixtures-for` теперь запускается на КАЖДЫЙ коммит (раньше — только когда правили `_generator/tools/*.py`/`.githooks/`). Верификатор измерил ~60-70мс добавки (сопоставимо с уже существующим безусловным `check_uroki.py`) — заметно, но не критично; это цена закрытия долга (охват не срабатывал НИКОГДА на 4 из 5 объявленных имён), а не побочный эффект по недосмотру.

**Долг 2 — `check_uroki.py`.** `PRICE = re.compile(r"^\*{0,2}ЦЕНА(?!\w)[^\w]*(.*?)\*{0,2}$")` — токен «ЦЕНА» целым словом (граница `(?!\w)` отсекает «ЦЕНАЛОГ»), разделитель после токена — любые не-словесные символы, дальше текст. Добавлены `kod_obratnaya.md` («ЦЕНА обратная, и она названа конкретно: …» → проходит) и `kod_cenalog.md` («ЦЕНАЛОГ: 5» → падает) в `fixtures/uroki/zhurnal/test-arka/`, вписаны в `PROGNAT.sh`. Все 6 случаев зелёные: `bez-ceny 1, zhirnaya 0, golaya 0, pustaya 1, obratnaya 0, cenalog 1`.

**Долг 3 — `audit.py`.** Строка со счётом слайдов в `--scene-diff`: `.slide` → `.slide:not([data-skip])` (тот же счёт, что у `render.py:36`). Проверено на `dandelin` (сравнил ДО/ПОСЛЕ на одном и том же `dist/index.html`, версия «до» взята из `git show HEAD:_generator/audit.py`): вердикт ДО — `11 FAILURES`, ПОСЛЕ — `10 FAILURES`; индексы слайдов после пропущенного слайда сдвинулись («slide 10»/«slide 15» → «slide 9»/«slide 14»), подтверждая, что раньше индекс указывал на соседа.

**Долг 4 — `build_deck.py`.** Сверено грепом по живым `src/`: `buffon` максимум 5, `fibonacci` 4, `dandelin` 4, `teorkat-vvedenie` 2. `SCENE_CAP = 10` (вдвое больше фактического максимума живых деков). Проверка — новый пункт 9 линтера (`lint()`), использует уже существующий `_slide_sections()` + `max_scenes()` на каждый слайд отдельно (не встроена в сами `max_scenes()`/`scene_cascade_css()` — их с n=9 сознательно использует фикстура `dvizhok` для проверки не-хардкода, и потолок внутри них сломал бы чужую, не мою, проверку). Ошибка называет id слайда и число. **Мутацией на КОПИИ `teorkat-vvedenie/src` в `/tmp`** (боевой файл не трогался, только читал read-only): `data-scenes="10"` (порог) — новой ошибки нет, диф со списком ошибок базовой копии пуст; `data-scenes="11"` (выше порога) — новая ошибка `slides/s01.html: data-scenes/data-scene-from/-until даёт 11 — выше потолка 10 (build_deck.py:SCENE_CAP)`. `buffon`/`fibonacci`/`dandelin` — без изменений (то же rc, то же число ошибок, что до правки).

**Долг 5 — вливание веток.** Предпосылку проверил сам (не только по замеру аналитика): `arka/poisk-listochki` — 2 файла, 577 строк, байт-в-байт идентичны рабочей ветке → **влито без конфликтов**, коммит `2f1e396`. `arka/vneshnie-istorii` — при ручной проверке 2 из 8 файлов (`KARTOTEKA-SYUZHETOV.md`, `PLAN.md`) дали расхождение по строкам (при близком чтении — переработка текста, не потеря сути, но это не то же самое, что «байт-в-байт»); реальный `git_zona.py merge` дал **конфликт в 4 путях** (те два плюс `SESSIYA.md`, `UROKI-FABRIKE.md`) → **`merge --abort`, НЕ влито**, по правилу задачи «конфликт — стоп, разбирается аналитик». `main` — `.gitignore` (один из путей, которые тащит ветка) сейчас ОДНОВРЕМЕННО грязный в рабочем дереве (параллельный писатель правит его прямо сейчас) → `git_zona.py merge` сам отказал («грязные пути пересекаются со сливаемыми»), **НЕ влито**, ничего не тронуто. `doctor` до: 3 ветки; после: 2 ветки (`arka/vneshnie-istorii`, `main`), охват «проверено 8 из 8». Верификатор подтвердил независимо: 0 из 577 строк потеряно в смерженной ветке, `.git/MERGE_HEAD` не существует, обе отменённые попытки не оставили следов (`merge-base --is-ancestor` = false для обеих).

**Верификатор — сводно.** Реальных дыр не нашёл ни по одному из трёх заданных направлений (атака на хук / потери при вливании / шум-скорость хука); единственная находка (долг 1, способ г) — уже известное, не входящее в зону ограничение механизма, не расширенное там, где оно уже было.

**Что НЕ трогал:** всё вне зоны (`engine.js`, `base.css`, `render.py`, `git_zona.py`, `register_doc.py`, `check_kartoteka.py`, `bootstrap_*`, папки деков, `teorkat-vvedenie/` — только читал read-only для копии в `/tmp`). `dandelin` не чинил (его 6 структурных — отдельный долг, вердикт на нём изменился только по счёту слайдов, не по существу). Ветки не удалял.

**Числа (свои, сняты только что):** buffon rc=0, fibonacci rc=0, dandelin rc=1 (6 структурных — то же число, что до правки), обходов `--no-verify` 28, сирот 9, `doctor` — 2 ветки вместо 3. Все шесть фикстур (`sostoyanie`, `git_zona`, `uroki`, `register_doc`, `tool_contract`, `dvizhok`) — зелёные.

**Необратимое:** необратимого нет. Точка отката — zip (`/tmp/melkie-dolgi-backup/zona-otkat-*.zip`, не коммит: зона была нечиста в HEAD чужой незакоммиченной правкой `KARTA.md`, коммитить её под своим сообщением было бы неверно). `merge --abort` дважды — оба безопасно откатили git-состояние, ничего не потеряно (подтверждено верификатором). Слияние `arka/poisk-listochki` — коммит `2f1e396`, влитая ветка НЕ удалена (решение владельца).

**Время прогона + токены** — снимает приёмка из лога прогона (счётчик исполнителю недоступен).

**КОММИТ:** делается следующим ходом.
