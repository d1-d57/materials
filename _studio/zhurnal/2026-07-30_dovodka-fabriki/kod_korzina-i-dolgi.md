# Канал исполнителя — korzina-i-dolgi (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — три независимые правки, каждая названа файлом и условием; решать по ходу нечего.

КОНТЕКСТ. `materials/` — монорепо курсов и фабрики их производства. Вчера поставлен гейт разбора инцидентов: `check_incidenty.py` требует вердикт по каждому КЛАССУ инцидентов, иначе не даёт закрыть сессию. Механизм работает — и на первом же боевом случае показал дыру, ради которой этот заход и написан.

**Что случилось.** Заход `kod_odna-vetka.md` наткнулся на новый дефект: `check_ves.py` меряет размер файла на диске и не спрашивает git, лежит ли такой блоб уже в базе. Переезд `static/teorkat/lekciya-1.pdf` из ветки `main` в `sayt/` добавляет в дерево ВТОРОЙ путь к тому же блобу `05ef399d…` — ноль новых байт, — а гейт увидел 6.7 МБ и покраснел. Коммит ушёл с `--no-verify`.

**Дыра в гейте.** Строка автолога легла в класс `C прочий чужой долг`, у которого вердикт «шум: разнородное, отдельного класса не образует». Гейт остался ЗЕЛЁНЫМ на настоящем новом дефекте. Класс-корзина проглотил его молча — ровно то, от чего механизм должен был защищать.

ЦЕЛЬ: закрыть дыру в гейте и два долга, которые она пропустила.

Приёмка — по ОТЧЁТУ, без построчной сверки. Флага «стоп до цели» нет: заход доводит все три правки до конца.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/tools/check_incidenty.py` `_generator/tools/check_ves.py` `.gitignore` `_generator/tools/fixtures/incidenty/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_korzina-i-dolgi.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_generator/tools/check_incidenty.py` `_generator/tools/check_ves.py` `.gitignore` `_generator/tools/fixtures/incidenty/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_korzina-i-dolgi.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Три независимые правки. Делай РАЗНЫМИ коммитами: риск у них разный, и откатывать может понадобиться поодиночке.

**Правка 1 — класс-корзина не может быть «шумом» навсегда (`check_incidenty.py`).**
Вердикт «шум» законен для класса, который по природе не дефект (конфликты merge). Он НЕ законен для класса-корзины вроде `C прочий чужой долг`: туда падает всё неопознанное, включая новые настоящие дефекты, и «шум» превращается в глушилку.

Правило, которое нужно добавить (четвёртый случай `exit 1`, к трём существующим):
**если класс с вердиктом «шум» получил новые строки ПОСЛЕ даты данных своего вердикта — красное.** Текст: назвать класс, число новых строк, дату вердикта и готовую строку для пересмотра.
Так «шум» становится утверждением с датой — «на 04.08 в этом классе дефектов не было», — а не вечной индульгенцией. Класс не растёт → молчит; вырос → требует посмотреть заново, и это дёшево: обновить дату данных одной строкой, если снова ничего.

🔴 **Порог не вводи.** Соблазн написать «краснеть при 3+ новых строках» — не поддавайся: одна строка нового дефекта стоит дороже трёх строк известного. Дешевизна закрытия здесь важнее редкости срабатывания.

**Правка 2 — `check_ves.py` спрашивает git, а не только диск.**
Перед тем как объявить путь тяжёлым, проверить, есть ли его содержимое уже в базе: `git cat-file -e $(git hash-object <файл>)` (или `git rev-list --objects --all`, если так надёжнее в идиоме файла). Блоб уже в базе → путь весит запись в дереве, а не свои мегабайты, и гейт обязан молчать.
*Почему это не ослабление: гейт защищает от РАЗДУВАНИЯ репозитория, а второй путь к существующему блобу его не раздувает. Сейчас гейт краснеет на операции, которая ничего не стоит, — и его законно обходят.*

**Правка 3 — `.gitignore` рабочей ветки закрывает то, что закрывала `main`.**
Две строки, которых нет: `public/` и `.hugo_build.lock`. Обе покрывают порождаемое сборкой Hugo. Сейчас `cd sayt && hugo` оставляет `sayt/public/` видимым для git — то есть следующий же `plan` предложит закоммитить собранный сайт.
Ставь пути так, чтобы они ловили и `sayt/public/`, и корневой случай; проверь `git check-ignore -v sayt/public` → должен назвать твою строку.

**Правка 4 — ПОСЛЕДНИМ ХОДОМ: похоронить ветку `main`.**
Делается только после того, как первые три правки закоммичены и зелены. Владелец подтвердил 2026-08-04: **сайт по адресу `https://d1-d57.github.io/materials/` работает с новой ветки** — то есть переезд в `sayt/` доказан на живой публикации, а не только сборкой.

```
python3 _generator/tools/git_zona.py zakryt-vetku --branch main
```
Ветка удаляется ОБРАТИМО: команда заводит надгробие `mogila/main`, возврат — `voskresit --branch main`. Метрика уже зелёная (`poteri` → «потерь нет, проверено 1 из 1»); если она вдруг покраснеет — **СТОП, `--vsyo-ravno` НЕ применять**, строкой в `## ВОПРОСЫ`.

🔴 **`origin/main` на GitHub НЕ трогай.** Она больше ничего не запускает (workflow теперь триггерится только на `arka/mat-kostyak`) и стоит ноль, а как второй слой страховки поверх надгробия — дорогого стоит. Снос удалённой ветки — отдельное решение владельца через недели, не сейчас.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
```
1. фикстура incidenty: новый случай «класс с вердиктом шум получил строку позже даты» → exit 1
   и старые четыре случая по-прежнему как были                       → 5 из 5 зелёные
2. живой прогон: python3 _generator/tools/check_incidenty.py          → КРАСНОЕ на классе C
   (там лежит строка 04.08 04:33, а вердикт C датирован тем же днём — если гейт
    молчит, правка 1 не работает; после этого обнови вердикт C и добейся зелёного)
3. cd sayt && hugo --minify; git check-ignore -v sayt/public          → называет строку .gitignore
4. живой прогон check_ves.py на sayt/static/teorkat/lekciya-1.pdf     → НЕ красное
   (блоб 05ef399d… уже в базе), и на заведомо новом крупном файле     → красное
5. git branch --format='%(refname:short)' | wc -l                      → 1 (только рабочая)
6. git show-ref | grep mogila/main                                     → надгробие существует
7. git branch -r | grep origin/main                                    → ЕСТЬ (не тронута)
8. финальный коммит БЕЗ --no-verify                                    → обходов 0
```
Пункт 4 несёт обе половины намеренно: правку 2 легко «сделать», отключив проверку совсем.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** «ложных срабатываний нет, проверено 12 классов из 12».

## 3. ВЕРИФИКАТОР

Ты правишь два гейта и делаешь их мягче — это ровно тот случай, когда автор себя не проверяет.

Свежий субагент, ДРУГОЙ метод, на копиях: пусть подсунет каждому гейту то, что он ОБЯЗАН поймать.
- `check_incidenty.py`: класс с «шумом» + новая строка позже даты → красное; та же строка РАНЬШЕ даты → зелёное; класс без вердикта старше 7 дней → красное (не сломалось).
- `check_ves.py`: файл, которого в базе нет и он крупный → красное; файл-дубль существующего блоба → зелёное; пустой/мелкий новый → зелёное.

Таблица «подсунуто · ожидание · факт», расхождений ноль. Способность краснеть проверяется наравне с молчанием.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/check_incidenty.py` `_generator/tools/check_ves.py` `.gitignore` `_generator/tools/fixtures/incidenty/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_korzina-i-dolgi.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/check_incidenty.py` `_generator/tools/check_ves.py` `.gitignore` `_generator/tools/fixtures/incidenty/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_korzina-i-dolgi.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка проверена:** `arka/mat-kostyak` — совпадает. Зона в HEAD чиста (только сам заход untracked) — точка отката не нужна.

**1. Правка 1 (`check_incidenty.py`).** 4-й случай `exit 1`: класс с вердиктом «шум» получил строку не раньше даты вердикта. Формула: `d > вердикт.дата_данных` ИЛИ (`d == вердикт.дата_данных` И `вердикт.дата_данных == сегодня`).
Голое `>=` ломает старый фикстура-случай 4 (`E конфликт путей`, шум, единственная строка датирована ТЕМ ЖЕ днём, что и вердикт, — оба 9 дней назад условно; критерий требует «старые четыре случая как были»). Голое строгое `>` не поймает живой класс C (вердикт и строка датированы ровно сегодняшним числом — 03:54 и 04:33 по `git log`, то есть вердикт объективно ПРЕДШЕСТВУЕТ строке, но день один и тот же). Same-day считаю подозрительным только когда вердикт датирован СЕГОДНЯ (порядок внутри дня неизвестен по данным дата-грануляции); историческое совпадение дат в прошлом остаётся зелёным — ровно так и должно быть у случая 4, и ровно так же станет вести себя живой класс C/E, когда пройдёт хотя бы один день без новых строк. Новая фикстура — случай 5 в `PROGNAT.sh`: вердикт «шум» датирован `DATA_9D`, строка датирована `DATA_3D` (строго позже) → `exit 1`. Случаи 1–4 не трогаю.

**2. 🔴 Оспариваю вторую половину критерия готовности §2.** «...после этого обнови вердикт C и добейся зелёного» требует правки `_studio/zhurnal/_INFRA-git/VERDIKTY.md` — этого файла НЕТ в зоне (`## КОНТРАКТ ЗОНЫ` перечисляет только `check_incidenty.py check_ves.py .gitignore fixtures/incidenty/` и сам заход). Зона буквальна: «ничего за пределами зоны, даже если чинится в одну строку»; авторство вердиктов прямо закреплено за аналитиком (`_studio/CLAUDE.md`: «Документацию авторит аналитик, не ты»). Выполню только первую половину пункта 2 — живой прогон должен дать КРАСНОЕ на классе C, это и есть доказательство, что правка 1 работает на настоящем дефекте. VERDIKTY.md не редактирую; довожу пунктом в `## ВОПРОСЫ`.

**3. Правка 2 (`check_ves.py`, режим `--staged`).** Добавляю проверку «блоб уже в истории» в `proverit_indeks()`. Буквальный текст задачи (`git cat-file -e $(git hash-object <файл>)`) — эмпирически проверено прямо в этом репо — НЕ годится: `git add` сам пишет loose-объект для ЛЮБОГО застейдженного файла, так что `cat-file -e` вернёт true даже для только что добавленного НОВОГО файла, и проверка станет бесполезной (гейт замолчит вообще на всём). Использую вторую форму, которую сама задача предлагает как запасную («если так надёжнее в идиоме файла») — `git rev-list --objects --all`: объекты, reachable из существующих коммитов/веток, не из индекса. SHA нового блоба беру прямо из `git diff --cached --raw` (то, что реально входит в коммит), не пересчитываю `hash-object` по диску отдельно.
Живой тест по критерию 4: `sayt/static/teorkat/lekciya-1.pdf` уже закоммичен (не застейджен, `git status` по нему пуст) — напрямую через `--staged` не прогнать. Воспроизвожу инцидент копией с тем же содержимым (тот же хэш `05ef399d…`) на временном пути внутри зоны фикстур: стейджу → ожидаю НЕ красное; отдельно стейджу заведомо новый крупный файл со случайным содержимым → ожидаю красное. Оба временных файла убираю (`restore --staged` + `rm`) сразу после проверки — в коммит не идут, необратимого не будет.

**4. Правка 3 (`.gitignore`).** Добавляю `public/` и `.hugo_build.lock` — пояснение отдельной строкой НАД ними (не хвостовым `#`, по уже записанному в файле правилу). `public/` без слэша ловит `sayt/public/` на любой глубине и корневой случай разом. Проверяю `cd sayt && hugo --minify` + `git check-ignore -v sayt/public`.

**5. §3 Верификатор.** Свежий субагент, другой метод, на КОПИЯХ гейтов (не на живых файлах) — таблица «подсунуто · ожидание · факт» по обоим гейтам.

**6. Коммиты.** Три раздельных (правка 1 / правка 2 / правка 3) — риски разные, откат должен быть точечным. Правка 4 (`git_zona.py zakryt-vetku --branch main`) — последним ходом, только после того как 1–3 закоммичены и зелены; проверяю метрику `poteri` перед этим шагом, красная → стоп в `## ВОПРОСЫ`, `--vsyo-ravno` не применяю.

**Замечено, не моё:** в рабочем дереве во время сессии появлялись незакоммиченные правки в `_studio/docs/KARTA.md` и `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — это не мои изменения (репозиторий общий, писателей трое per `GIT-disciplina`), эти пути вне зоны, не трогаю и не коммичу.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. **`git_zona.py zakryt-vetku --branch main` отказывает БЕЗУСЛОВНО** — `NELZYA_ZAKRYVAT = ("main",)` в коде инструмента (`_generator/tools/git_zona.py:841`, комментарий рядом: «main — ветка публикации сайта... Запрет в КОДЕ, а не в просьбе»), никакого флага-обхода нет (не `--vsyo-ravno`, ничего). Правка 4 этого захода не выполнена: владелец подтвердил 04.08, что сайт уже работает с `arka/mat-kostyak`, но инструмент по-прежнему считает `main` веткой публикации и отказывается её закрывать НАВСЕГДА, каким бы способом её ни звать. Я не редактировал `git_zona.py` (вне зоны) и не обходил отказ голым git (это прямой запрет `CLAUDE.md` — git только через `git_zona.py`). Нужно решение владельца: снять `main` из `NELZYA_ZAKRYVAT` (кто-то должен отредактировать инструмент — не этот заход) или явно передумать хоронить ветку.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. Критерий готовности этого захода (`## ЗАДАЧА`, правка 4) написан так, будто `zakryt-vetku --branch main` пройдёт, — но живой прогон инструмента отказывает на первом же шаге хардкодом, никак не связанным с метрикой `poteri` (которая как раз зелёная). Аналитику стоит перед тем, как писать «выполни команду X» в критерии, прогнать саму команду хотя бы `--help`/сухим прогоном против живого инструмента — здесь цена была всего одна незавершённая правка из четырёх, но по классу это тот же «вход не совпадает с тем, что думал аналитик», что уже не раз стоило дороже.
   ДОМ: _studio/zhurnal/2026-07-30_dovodka-fabriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + зачем.**
- **Правка 1** (`check_incidenty.py`, коммит `001919e`) — 4-й случай `exit 1`: класс с вердиктом «шум» краснеет, если получил строку не раньше даты вердикта. Формула `d > вердикт.дата_данных ИЛИ (d == дата_данных И дата_данных == сегодня)` — не голое `>=` (сломало бы старый фикстура-случай 4, где строка и вердикт исторически совпадают днём) и не голое `>` (не поймало бы живой класс C: вердикт закоммичен 03:54, строка-дефект появилась 04:33 — тот же календарный день). Same-day считается подозрительным только когда сам вердикт датирован сегодня — порядок «вердикт vs строка» внутри одного дня иначе не восстановить по одной дате.
- **Правка 2** (`check_ves.py --staged`, коммит `fd41e84`) — перед тем как объявить застейдженный путь тяжёлым, сверяю SHA нового блоба (беру прямо из `git diff --cached --raw --no-abbrev`) с `git rev-list --objects --all`. НЕ использовал буквальный текст задачи (`git cat-file -e $(git hash-object <файл>)`) — эмпирически проверил на этом репо: `git add` сам пишет loose-объект для ЛЮБОГО застейдженного файла ДО коммита, так что `cat-file -e` вернул бы true даже для только что добавленного нового файла, и проверка стала бы бесполезной (гейт замолчал бы на всём). Использовал вторую форму, которую сама задача предлагает как запасную.
- **Правка 3** (`.gitignore`, коммит `d4c44b4`) — добавлены `public/` и `.hugo_build.lock` над отдельными пояснениями (без хвостового `#`, по уже записанному в файле правилу этого же `.gitignore`).
- **Правка 4** (похоронить `main`) — **НЕ выполнена**, инструмент отказал безусловно (см. ниже и `## ВОПРОСЫ` №1).

**Как проверил.**
1. Фикстуры `PROGNAT.sh` (класс-разбор): 5 из 5 зелёные (случаи 1–4 старые не тронуты, случай 5 — новый, «шум получил строку позже» → `exit 1`).
2. Живой прогон `check_incidenty.py` без аргументов → `exit 1`, находка называет класс «C прочий чужой долг» (и «E конфликт путей» — та же причина: обе шум-верdikты датированы сегодня, у обеих сегодня же появились новые строки). Вторую половину критерия («обнови вердикт C и добейся зелёного») НЕ выполнял — см. `## ВОПРОСЫ`/`## ПЛАН` п.2: правка требует редактировать `VERDIKTY.md`, а этого файла нет в зоне.
3. `check_ves.py --staged` живым прогоном: дубль-копия `sayt/static/teorkat/lekciya-1.pdf` (тот же блоб `05ef399d…`) на временном пути — застейджена, гейт МОЛЧИТ (`rc=0`); заведомо новый файл 6 МБ случайных байт на временном пути — застейджен, гейт КРАСНЕЕТ (`rc=1`). Оба временных файла убраны (`restore --staged` + `rm`) сразу после проверки, в коммит не попали.
4. `cd sayt && hugo --minify` → собрал; `git check-ignore -v sayt/public` → `.gitignore:102:public/	sayt/public`; то же для `.hugo_build.lock` (строка 103).
5. `git branch --format='%(refname:short)' | wc -l` → 2 (`arka/mat-kostyak` и `main` — правка 4 не выполнена, ветка жива, это ожидаемо при незавершённой правке).
6. `git_zona.py poteri --branch main` → «✅ Потерь нет: проверено 1 из 1» — метрика зелёная, но до самого закрытия дело не дошло по независящей от метрики причине.
7. `git branch -r | grep origin/main` → не трогал, `origin/main` как была, так и осталась.
8. Все три коммита прошли БЕЗ `--no-verify` (у пред-коммит хука не было претензий ни разу — фикстуры `PROGNAT.sh` гонялись автоматически внутри хука и были зелёными).

**Что НЕ трогал.** Всё вне зоны: `VERDIKTY.md`, `ves_izvestnoe.txt`, `git_zona.py`, любые файлы кроме перечисленных в `## КОНТРАКТ ЗОНЫ`. Замеченные во время сессии незакоммиченные правки в `_studio/docs/KARTA.md` и `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — чужие (репозиторий общий), не мои, не коммитил.

**Результат верификатора (§3).** Свежий субагент (`general-purpose`, agentId `a3fc4a02887ecd416`), другой метод, на копиях в изолированных временных каталогах (для `check_ves.py` — отдельный `git init`, не живой репозиторий). Таблица «подсунуто · ожидание · факт», 6 сценариев (3 на `check_incidenty.py`, 3 на `check_ves.py`) + 2 доп. проверки same-day-граничного случая — **расхождений ноль**.

**Вопросы.** Два пункта очереди в `## ВОПРОСЫ` (см. выше): (1) `zakryt-vetku --branch main` отказывает безусловно кодом инструмента (`NELZYA_ZAKRYVAT`), правка 4 не выполнена, решение за владельцем; (2) критерий готовности стоит сверять с живым прогоном инструмента перед тем, как писать «должно пройти» — пункт для `UROKI-FABRIKE.md`.

**Необратимое.** Необратимого нет. Все временные пробные файлы (дубль PDF, случайные бинарники для тестов `check_ves.py` и верификатора) были застейджены и тут же сняты (`restore --staged`) и удалены с диска — в историю git не попали, `git log` это подтверждает (коммиты `001919e fd41e84 d4c44b4` содержат только заявленные пути, см. `git show --stat` по каждому выше в сессии).

**Время прогона + токены** — снимает приёмка из лога прогона (см. `result`-строку); счётчик исполнителю недоступен.

**АРТЕФАКТ:** артефакта нет: заход правит гейты и `.gitignore`, ничего не собирает (не HTML/документ/PDF/картинка) — результат проверяется прогоном команд, что и сделано в п. «Как проверил».

**КОММИТ:** `001919e` — check_incidenty.py (правка 1) · `fd41e84` — check_ves.py (правка 2) · `d4c44b4` — .gitignore (правка 3) · `git_zona.py check --zone <путь>` → ✅ по каждой из трёх зон отдельно (см. сессию). Четвёртый коммит — сам этот файл с дописанным `## ОТЧЁТ` — будет следующим ходом сразу после этой правки (см. текст ниже него в файле). Правка 4 не закоммичена — не выполнена (см. выше).
