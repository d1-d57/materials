# РЕЕСТР ЧТЕНИЯ — добыча замечаний владельца

> След чтения, который нельзя подделать. Строка = один источник (для дневников — одна секция дневника).
> Заход: `kod_dobycha-vladelca.md`. Корпус: `KORPUS-VLADELCA.md`. Группировка: `KRATNOST-vladelca.md`.
>
> 🔴 **Хэш блоба делает пометку САМОИНВАЛИДИРУЮЩЕЙСЯ.** Файл дописали → хэш разошёлся → пометка
> «прочитано» автоматически недействительна, и это видно машиной, а не на веру:
> ```
> git --no-optional-locks hash-object <путь>   # обязан совпасть с колонкой «хэш блоба»
> ```
> `замечаний найдено = 0` — законный результат, а не признак недоработки.
>
> **Дата данных всех строк: 2026-07-30** (колонка «дата» — дата чтения источника).

## Пачка 1 — три самых крупных источника

| путь | секция | строк | хэш блоба | замечаний найдено | дата |
|---|---|---|---|---|---|
