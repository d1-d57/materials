# ПОКРЫТИЕ КАРКАСА — арифметика, посчитанная скриптом

> Числа считает `sobrat_razmetku.py`, не модель. Разбор и выводы заход дописывает НИЖЕ этой таблицы, в разделе «## РАЗБОР

🔴 **Разбор, написанный пачками H2a–H2d, УТЕРЯН 01.08 — и утерял его сборщик, а не заход.**

`sobrat_razmetku.py` перезаписывал этот файл целиком на каждом прогоне. Пачка 1 предсказала это в плане и спасала свой разбор через `/tmp`; пачка 2 назвала себя «первым потерпевшим» и спасала уже два чужих раздела (31 КБ разбора и 9 КБ конфликтующих чисел). Финальный шлюз в `noch-2.sh` прогнал сборку после всех пачек — и снёс всё молча.

**Скрипт починен** (`sohranit_hvost`): раздел `## РАЗБОР` теперь переносится в новую сборку, проверено маркером. Ловушка закрыта механизмом, а не инструкцией.

**Содержание не потеряно:** все выводы продублированы в отчётах четырёх пачек — `kod_razmetka-vladelca.md`, раздел `## ОТЧЁТ` (732 строки, по пачке на подраздел) и `## ВОПРОСЫ` (девять развилок, из них пять адресованы владельцу). Версия этого файла с разбором лежит в коммитах пачек: каждая коммитила свою зону.

**Пересобрать разбор из отчётов — отдельная задача, дешёвая и делается без модели-исполнителя.**
