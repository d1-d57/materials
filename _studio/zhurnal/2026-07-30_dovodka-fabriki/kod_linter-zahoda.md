# Канал исполнителя — linter-zahoda (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу, В ЭТОМ ЖЕ ФАЙЛЕ. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — механика расписана до уровня проверок и ловушек.

КОНТЕКСТ. Репозиторий `materials/` — фабрика, собирающая лекционные курсы. Работа идёт файлами-заходами: аналитик пишет задание в `kod_<тема>.md`, автономный исполнитель его выполняет и дописывает отчёт туда же, аналитик принимает командой `priyomka.py`.

🔴 **ЦЕЛЬ: ВХОД захода не судит никто, и это крупнейшая известная дыра фабрики.** Замер по корпусу из 461 наблюдения исполнителей: **ось А «вход захода дефектен» — 74 инцидента при НУЛЕ механизмов**, единственная строка очереди работ с бесконечным отношением «инцидентов на механизм». Проверено командой: ни один из 21 гейта `sostoyanie.py`, ни одни из 5 ворот `check_kartoteka`, ни один из 4 блоков `pre-commit` не читает файл-заход как ЗАДАНИЕ — адреса, числа, критерий готовности, зону. Отбирает `kod_*.md` ровно одна строка исполняемого кода во всей фабрике (`check_uroki.py:104`), и та ищет внутри уроки без цены.

**Второй повод, живой и свежий.** 03.08 сессия, которая строила дисциплину сессий, за тот же день завела **два правила-пожелания** — «канал запуска есть параметр среды владельца» и «файл-заход живёт в одном экземпляре» — обе прозой, ни одна ничем не краснеет. Нашлось это не механизмом, а разовым аудитом по устной просьбе владельца, и он же сформулировал лечение: *«это должен быть не урок, а гейт некоторый, что всегда, когда мы делаем работу, мы должны гонять её на конкретной практике»*. Уроки 47–48.

Приёмка — по ОТЧЁТУ. Если стоп до цели: часть A важнее части B; сделай её первой и отчитайся честно.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **ПЛАН, ВОПРОСЫ, ОТЧЁТ и УРОКИ ФАБРИКЕ пиши В ЭТОТ ФАЙЛ — тот, по абсолютному пути, что назван в стартовой строке, в ОСНОВНОЙ папке репозитория.** Он существует в одном экземпляре. **Его самого НЕ коммить** — это делает аналитик при приёмке. *Цена обратного правила, действовавшего полсуток 03.08: заходам было велено писать отчёт в свою рабочую папку, а она недоступна ни владельцу в чате, ни аналитику в песочнице; приёмка трижды подряд открыла файл в основной папке, увидела пустые секции и объявила, что отчёта нет. Владелец потребовал прекратить перекладывать файлы руками.*
- **МЕСТО РАБОТЫ ДЛЯ КОДА:** отдельная рабочая папка: `python3 _generator/tools/git_zona.py worktree add linter-zahoda --branch zahod/linter-zahoda`, затем `cd` в неё. 🔴 **`git checkout` в основной папке НЕ делай** — она стоит на другой ветке, и checkout МОЛЧА откатит дерево под всеми, кто там работает (цена 27→28.07: файл откатился ночью, поймал владелец вручную; следа в git НЕ остаётся).
- **ЗОНА КОДА (можно менять):** `_generator/tools/check_zahod.py` `_generator/tools/bootstrap_zahod.py` `_generator/tools/fixtures/zahod/` `_generator/tools/fixtures/git_zona/`. Всё вне — **READ-ONLY**.
  🔴 **`register_doc.py` и `fixtures/register_doc/` не трогать даже на строку** — над ними идёт незакоммиченная работа владельца из другого чата. 🔴 **`_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` вне зоны** — канон правит аналитик. 🔴 **Чужие файлы-заходы (`kod_*.md`, кроме своего) не трогать:** они приняты и закоммичены, ретроспективная правка их — не твоя работа.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ САМ, ТЕМ ЖЕ ХОДОМ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` READ-ONLY для правки руками — дверь ровно одна, и это она. **Красный хук на ТВОЁМ новом `.md` — повод позвать дверь, а не `--no-verify`.** *Цена: 27 обходов из 28 срывов коммита по одной этой причине, тринадцать исполнителей подряд.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (§4). НИКОГДА `-A` / `.` / `commit -am`. Субагенты не коммитят. **`--no-optional-locks` обязателен.**
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Заведи worktree для кода и перейди в него. `git branch --show-current` обязано дать `zahod/linter-zahoda`. **Отчёт при этом пиши в файл основной папки** — путь назван в стартовой строке.
- **Проверь свежесть базы:** `grep -c 'ДОСТАВЛЕНО' _generator/tools/bootstrap_zahod.py` должно быть больше нуля (очередь домов уже тиражирована в генератор). Ноль — СТОП и в `## ВОПРОСЫ`.
- Прочитать ТОЛЬКО: `_generator/tools/bootstrap_zahod.py` (целиком), `_generator/tools/check_tool_contract.py` — **докстринг и список проверок** (образец линтера с двумя законами: судить staged-диф и НИКОГДА не исполнять проверяемое), `_generator/tools/fixtures/priyomka/PROGNAT.sh` (образец фикстуры), и **два живых файла-захода как материал калибровки**: `kod_tirazh-ocheredi.md` и `kod_ochered-domov.md` в той же папке. Проект сверх этого не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково. *Цена 21.07: `rc=128` четырежды прочитан как результат.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия; критерий, который может провалиться. **Оспорить ложную предпосылку, включая КРИТЕРИЙ ГОТОВНОСТИ, — в `## ПЛАН`, ДО работы.** Субагенты: ≤5, рейт-лимит = отступить + доложить.

🔴 **Только stdlib.** Python владельца — **3.9.6**; аннотации вида `str | None` там падают.

🔴 **Мера истины — прогон на живом файле, а не убедительность правила** (`UROKI-FABRIKE.md` №41–42). За четыре захода подряд восемь «умных» правил оказались неверны, и **ни одно не нашёл автор** — находили положительный контроль фикстуры, верификатор и самоприменение инструмента к собственному артефакту. Последнее самое продуктивное: **прогони `check_zahod.py` на СЕБЕ самом** (этот файл — тоже заход) перед коммитом.

🔴 **Ловушка проверяет красноту ПОИМЕНОВАННОЙ проверки, а не слепой код возврата.** У прошлых заходов три ловушки были ложно-зелёными: падали по соседней причине.

## 2. ЗАДАЧА

### ЧАСТЬ A — `_generator/tools/check_zahod.py`: линтер входа

```
python3 _generator/tools/check_zahod.py <путь к kod_*.md> [...]
```

Судит файл-заход **как задание**, до того как его запустили. `exit 1` на любом красном, **молчание при зелёном** (`KONSTITUCIYA` Р31: гейт, который шумит зря, отключат).

🔴 **Два закона, без которых линтер опаснее его отсутствия** — оба взяты у `check_tool_contract.py`, читай его докстринг:
1. **Судится только текст файла.** Никаких запусков, никаких сетевых обращений, никакого исполнения того, что заход описывает.
2. **Ложное красное дороже пропуска здесь НЕ действует наоборот:** линтер будет стоять на пути каждого захода, поэтому каждая проверка обязана быть откалибрована на двух живых файлах-заходах (`kod_tirazh-ocheredi.md`, `kod_ochered-domov.md`) — они приняты и признаны хорошими, значит **линтер обязан пропускать их молча**. Проверка, краснеющая на них, неверна по построению.

Проверки — минимум эти семь, каждая с сообщением, называющим, что именно чинить:

| # | что | почему это, а не что-то ещё |
|---|---|---|
| З1 | **Критерий готовности существует и может ПРОВАЛИТЬСЯ** — называет команду, файл или число, а не «я закончу» / «проверь внимательно» | ось А, класс «негодный критерий»: **64 инцидента** по программному отчёту |
| З2 | 🔴 **В критерии есть пункт с ЖИВЫМ ПРОГОНОМ** — на настоящем объекте репозитория, а не только «фикстура зелёная» | требование владельца дословно: «всегда, когда мы делаем работу, мы должны гонять её на конкретной практике». За 03.08 живой прогон нашёл то, чего не нашла ни одна фикстура — **четыре раза подряд** |
| З3 | **Зона задана явным списком путей**, а не папкой-обобщением и не словами | ось З, 53 инцидента; «зона = папка» названа в корпусе поимённо |
| З4 | **Назван физический файл для отчёта** и он же — тот, что в стартовой строке | урок 46: три приёмки подряд читали пустой файл |
| З5 | **Назван канал запуска** (терминал или приложение) | урок 45: дефолт молча выбрал за владельца, две нерабочие инструкции подряд |
| З6 | **Каждое числовое утверждение задания имеет рядом команду или адрес**, откуда оно взято | `KONSTITUCIYA §10`; ось В, 50 инцидентов «число вписано руками и устарело» |
| З7 | **Секции-заготовки на месте** (`## ПЛАН`, `## ВОПРОСЫ`, `## ОТЧЁТ`, `## УРОКИ ФАБРИКЕ`) и очередь `ДОМ/ДОСТАВЛЕНО` в шапке `## ВОПРОСЫ` | иначе исполнителю физически некуда писать, а очередь домов вымоется первой же правкой генератора |

**З6 — самая рискованная по ложным срабатываниям.** Начни с печати-предупреждения, а не с красного, и **откалибруй на двух живых заходах**: если она краснеет на них, порог неверен. Реши сам и объясни решение в отчёте.

### ЧАСТЬ B — линтер встаёт на пути, а не лежит рядом

1. **`bootstrap_zahod.py` прогоняет `check_zahod.py` по только что собранному файлу** и **отказывается отдавать заход**, если линтер красный. Печатает, что чинить. *Иначе линтер разделит судьбу правил-пожеланий, ради которых он и пишется.*
2. **Убрать дефолт `--kanal`.** Сейчас дефолт `terminal`, и он молча выбирает за владельца. Флаг становится обязательным — забыть нечего (тот же приём «убрано место для ошибки», что с `--branch` и `--zone`).
3. **Шаблон получает строку про дом отчёта** — ту, что стоит в контракте зоны этого захода: писать в файл основной папки, сам файл не коммитить. Механизм тиражирования уже есть — инъекция по якорю, применённая предыдущим заходом для очереди домов; используй её же.

### ЧАСТЬ C — фикстура `_generator/tools/fixtures/zahod/PROGNAT.sh`

Новая, с объявленным `TOOL-CONTRACT-COVERS: check_zahod.py`. Ловушек — **не менее девяти**: по одной на каждую проверку З1–З7 плюс два контроля:

- 🔴 **ПОЛОЖИТЕЛЬНЫЙ КОНТРОЛЬ НА ЖИВЫХ ФАЙЛАХ:** `check_zahod.py` на `kod_tirazh-ocheredi.md` и `kod_ochered-domov.md` → **rc=0 и пустой вывод**. Это главная ловушка фикстуры: она сторожит не поломку, а излишнюю строгость, от которой линтер отключат.
- **`bootstrap_zahod.py` с заведомо дефектным заданием** (например, без `--kanal`) → заход не собирается.

🔴 **Каждую ловушку проверь мутацией и покажи, что краснеет ПОИМЕНОВАННАЯ проверка.**

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `sh _generator/tools/fixtures/zahod/PROGNAT.sh` → `echo $?` = **0**; `sh _generator/tools/fixtures/git_zona/PROGNAT.sh` → **0**.
2. Каждая ловушка показана краснеющей под мутацией — по строке в отчёте.
3. `python3 _generator/tools/check_tool_contract.py _generator/tools/check_zahod.py _generator/tools/bootstrap_zahod.py` → зелёный.
4. 🔴 **ЖИВОЙ ПРОГОН, и он же демонстрация З2:** `python3 _generator/tools/check_zahod.py` на **всех** файлах-заходах арки `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_*.md` — вывод целиком в отчёт, с числом «краснеет N из M». **Красное на старых заходах — результат, а не сбой:** они писались до линтера. Но на двух названных выше линтер обязан молчать.
5. 🔴 **Самоприменение:** `check_zahod.py` на ЭТОМ файле-заходе → зелёный. Красный здесь означает, что задание не удовлетворяет правилам, которые само вводит.
6. `bootstrap_zahod.py` без `--kanal` отказывается собирать — показать прогоном.
7. Зона кода закоммичена, `git_zona.py check --zone _generator/tools` → ✅.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y».

## 3. ВЕРИФИКАТОР
Свежий субагент, **атака на калибровку**: дай ему `check_zahod.py` и два живых захода, признанных хорошими, и попроси найти **заход, который линтер пропустит, хотя он дефектен** (ложно-зелёное), и **место, где линтер покраснеет на здоровом задании** (ложно-красное). Второе важнее: линтер стоит на пути каждой работы, и от шумного избавятся первым делом. Результат и твоя правка по нему — в отчёт.

## 4. 🔴 КОММИТ ЗОНЫ КОДА — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
```
git --no-optional-locks add -- _generator/tools/check_zahod.py _generator/tools/bootstrap_zahod.py _generator/tools/fixtures/zahod/ _generator/tools/fixtures/git_zona/
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/check_zahod.py _generator/tools/bootstrap_zahod.py _generator/tools/fixtures/zahod/ _generator/tools/fixtures/git_zona/
python3 _generator/tools/git_zona.py check --zone _generator/tools    # из корня репо; должен быть ✅
git --no-optional-locks show --stat                                   # обязаны быть ТОЛЬКО твои пути
```
🔴 **Файл-заход в этот коммит НЕ входит** — он живёт в основной папке и коммитится аналитиком при приёмке.
🔴 **ОБА хода обязательны:** `add` вводит новые пути в индекс; `-- <пути>` в самом `commit` отсекает чужое, застейдженное рядом. *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх.*
⚠ **Хук покраснел — смотри, на ЧЬИХ путях.** На твоих — чини. На чужом унаследованном долге — законный обход, но только с причиной:
```
python3 _generator/tools/git_zona.py commit --zone _generator/tools \
    --no-verify "чужой долг: <что покраснело>" -m "<что и зачем>" --push
```
⚠ **Известный чужой долг окружения:** `fixtures/dvizhok/PROGNAT.sh` краснеет на отсутствующем `chrome-headless-shell` (playwright не установлен). Это не твоя регрессия — подтверди сравнением с baseline и назови причиной обхода.
**Коммит не прошёл по ВНЕШНЕЙ причине** — чужое состояние репозитория НЕ чини: напиши отдельной строкой «коммита нет, причина, нужно ваше действие». Молчаливое «сделано» при незакоммиченной зоне — брак.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу ЭТОГО ФАЙЛА
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна)**. Время и токены снимает приёмка из лога — не оценивай.

🔴 **АРТЕФАКТ — ПУТЬ ОТ КОРНЯ РЕПОЗИТОРИЯ, не абсолютный путь рабочей папки.** *Цена 03.08: путь внутрь `materials-wt/…` протух при первом же переносе зоны, и гейт покраснел на исправном файле.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ.** Удаление, перезапись, переименование, любая правка вне зоны — каждое ОДНОЙ строкой: **что · где · чем восстанавливается**. Ты запущен без запроса разрешений: владелец НЕ видел ни одного из этих действий. **Необратимого не было — напиши «необратимого нет».**

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и видишь то, чего не видит аналитик. Сломался ВХОД? Критерий кривой? Канон противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Снятие дефолта у общего CLI-флага (`--kanal`) ломает ЧУЖИЕ фикстуры, вызывающие тот же инструмент без флага, и это не видно до живого прогона
`_generator/tools/fixtures/dvizhok/PROGNAT.sh` (вне зоны этого захода) внутри себя вызывает `bootstrap_zahod.py` несколько раз через свой `zvat.py`, ни разу не передавая `--kanal` — это было безопасно, пока флаг имел дефолт. Задание этого захода прямо требовало убрать дефолт (урок 45 — «дефолт молча выбрал за владельца»), но у `bootstrap_zahod.py` уже есть ДРУГИЕ покрывающие фикстуры-потребители, объявленные через `TOOL-CONTRACT-COVERS: ... bootstrap_zahod.py` (сам `dvizhok`, `git_zona`) — ни чтение кода `bootstrap_zahod.py`, ни его докстринг не показывают, кто и как его вызывает. Грепнуть `TOOL-CONTRACT-COVERS.*bootstrap_zahod` по всем фикстурам ПЕРЕД тем, как снимать дефолт с флага, — дешёвый и очевидный шаг, который был бы сделан раньше, если бы правило «снимаешь дефолт с общего инструмента — найди всех вызывающих» жило в каноне явно, а не выяснилось прогоном коммит-хука.
ЦЕНА: находка не через код-ревью, а через провал `check_tool_contract.py`/pre-commit хука на живом коммите; фикстуру `git_zona/PROGNAT.sh` (в зоне) я поправил тем же ходом, но `fixtures/dvizhok/PROGNAT.sh` (вне зоны) осталась красной для ВСЕХ будущих заходов, чей охват её триггерит, пока отдельный заход не добавит `--kanal` в её `zvat.py` — цена уже переносится на следующего исполнителя, тронувшего любой из пяти покрытых этой фикстурой файлов.

## ПЛАН — (заполняет исполнитель)

**Оспоренная предпосылка — З5 «канал запуска (терминал или приложение)».**
Проверил по живым файлам буквально: `grep -niE "канал|терминал|Claude Code в|десктоп|приложени" kod_tirazh-ocheredi.md kod_ochered-domov.md` — НИ ОДИН из двух калибровочных файлов не называет канал запуска словами «терминал»/«приложение» вообще (только сам этот заход — kod_linter-zahoda.md — упоминает «канал» в тексте задачи, не как заполненное поле). Буквальная реализация З5 («в тексте есть слово терминал/приложение») красила бы ОБА калибровочных файла — а закон калибровки безусловен: «проверка, краснеющая на них, неверна по построению». Решение: переопределяю З5 как «названа ОДНОЗНАЧНАЯ точка входа исполнителя» — присутствие `worktree add` или `git branch --show-current` в тексте. Это калибруется корректно (оба слова/паттерна есть во всех трёх живых файлах — сам этот заход, tirazh-ocheredi, ochered-domov) и ловит ровно тот же класс дефекта, что и урок 45 («дефолт молча выбрал за владельца»): заход, который вообще не называет, откуда исполнителю начинать (ни worktree, ни прямую ветку), — опасен тем же способом. Буквальный смысл «терминал vs приложение» (RUKOVODSTVO §Два канала запуска) в живых файлах пока не документируется как поле — это открытый разрыв, отмечаю в `## ВОПРОСЫ`.

**Общая конструкция check_zahod.py.**
Судит ТОЛЬКО текст (regex по markdown-структуре), не исполняет ничего — по закону check_tool_contract.py. Семь проверок; пять жёсткие (exit 1 при срабатывании): З1 (критерий существует и не пуст, содержит хотя бы одну команду/файл(backtick) или число), З2 (в критерии есть фраза «жив* прогон»), З3 (строка `**ЗОНА...(можно менять)**` содержит хотя бы один backtick-путь с «/»), З4 (секция `## КОНТРАКТ ЗОНЫ` есть и в ней встречаются слово «файл» и одно из «worktree/основн* папк/абсолютн* пут/рабоч* папк»), З5 (см. выше, переопределена), З7 (все четыре секции-заготовки `## ПЛАН`/`## ВОПРОСЫ`/`## ОТЧЁТ`/`## УРОКИ ФАБРИКЕ` есть, и в шапке `## ВОПРОСЫ` есть `ДОМ:` и `ДОСТАВЛЕНО:`). Две проверки печатаются ПРЕДУПРЕЖДЕНИЕМ, не гейтом (не влияют на exit-код) — по прямому указанию задания для З6: числовое утверждение в **жирном** виде с цифрой, чей абзац НЕ содержит НИ ОДНОГО backtick — печатается примером, не роняет прогон. Каждая проверка эмпирически прогнана на `kod_tirazh-ocheredi.md` и `kod_ochered-domov.md` (см. `## ОТЧЁТ`) и на них МОЛЧИТ (rc=0, пустой вывод) — иначе проверка переписывается.

**Часть B.** `bootstrap_zahod.py`: (1) после записи `dst` — вызов `check_zahod.py` на нём, отказ `return 1` при коде ≠0 (только чтение вывода, не исполнение задания); (2) `--kanal` без `default=` и без `choices` по умолчанию — делаю `required=True`; (3) в `mesto`-текст worktree-ветки добавляю ОДНУ фразу про дом отчёта (anchor-инъекция тем же приёмом, что уже используется для токенов/очереди в этом же файле) — текст беру дословно из контракта этого захода: «отчёт пишется в файл ОСНОВНОЙ папки репозитория, он существует в одном экземпляре, сам файл НЕ коммитится — это делает аналитик при приёмке». Это ТОЛЬКО текст-предупреждение в шаблон; физический путь записи `dst` (сейчас `wt_path/...` в режиме `--worktree`) НЕ меняю — это отдельный структурный разъезд между кодом и новым правилом-пожеланием, крупнее темы этого захода; выношу вопросом в очередь.

**Часть C.** `fixtures/zahod/PROGNAT.sh`: ≥9 ловушек (по одной на З1–З7 + 2 контроля: положительный на двух живых заходах rc=0/пустой вывод; `bootstrap_zahod.py` без `--kanal` не собирает заход) — каждая мутацией, с грепом по имени проверки в выводе.

**Проверка на себе.** Прогон `check_zahod.py` на ЭТОМ файле (после заполнения ПЛАН/ВОПРОСЫ/ОТЧЁТ) — обязан быть зелёным (критерий готовности п.5).

**🔴 Оспоренная предпосылка — Часть B п.1, живым прогоном.** Собрал пробный заход (`bootstrap_zahod.py _studio/zhurnal/_proba-linter-zahoda proba --branch proba-branch --zone "proba-zona/" --kanal terminal`, потом убран `rm -rf` + `git checkout -- _studio/docs/KARTA.md`, хэш `KARTA.md` совпал побайтово до/после) и прогнал на нём `check_zahod.py` — результат: **З2 красный уже на свежесобранном скелете** («в критерии нет пункта с ЖИВЫМ ПРОГОНОМ»). Причина: `## 2. ЗАДАЧА` в `_TEMPLATE-zahod.md` содержит буквальный плейсхолдер `<условие, которое НАЗЫВАЕТ АРТЕФАКТ...>` — реальный критерий (со словом «живой прогон») дозаполняет Cowork ОТДЕЛЬНЫМ ходом ПОСЛЕ `bootstrap_zahod.py` (докстринг инструмента: «Cowork дозаполняет `<...>`: контекст · цель · … · критерий»); `bootstrap_zahod.py` контента критерия вообще не касается. Значит буквальный гейт «прогнать `judge()` целиком и отказать на любом ❌» отказывал бы АБСОЛЮТНО КАЖДОМУ свежесобранному заходу, ещё до того как Cowork успел его дописать, — гейт был бы не «сторожем», а постоянно взведённой миной. Решение: bootstrap проверяет ТОЛЬКО те три проверки, за которые сам физически отвечает в момент генерации — З3 (ЗОНА, которую он сам подставляет из `--zone`), З5 (точка входа, которую он сам пишет в `{{КОНТРАКТ_МЕСТО}}`/`{{ПЕРВЫЙ_ХОД}}`) и З7 (секции+формат очереди, которые сам инъецирует якорями). За это ручаться уместно СРАЗУ; за критерий (З1/З2) и физический файл отчёта (З4, зависит от того, как Cowork опишет контракт зоны в ручной правке) — нет, это работа Cowork следующим ходом. `check_zahod.py` получает для этого маленький публичный список `STRUCTURAL_CHECKS`, `bootstrap_zahod.py` импортирует его вместо полного `judge()`.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе. Для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`.

1. **`bootstrap_zahod.py --worktree` физически кладёт сам файл-заход (`.md`) ВНУТРЬ рабочей папки (`wt_path/...`), а не в основную папку репозитория — это противоречит новому правилу-пожеланию «файл-заход живёт в одном экземпляре, в основной папке» (то самое, ради которого пишется этот заход, см. КОНТЕКСТ строка 10).** Этот заход (по своей зоне) добавил в шаблон worktree-режима ТОЛЬКО текстовое предупреждение об этом правиле (anchor-инъекция в `mesto`, часть B п.3) — физический путь записи `dst` НЕ менял: перенос самой точки записи `.md` в `--worktree`-режиме — операция крупнее темы линтера (меняет, где Cowork ищет свежесобранные заходы, и требует, видимо, копирования .md туда-сюда между worktree и основной папкой при коммите кода) и рискует сломать уже работающий тираж очереди домов (`kod_tirazh-ocheredi.md`). Нужен отдельный заход.
   ДОМ: `_studio/zhurnal/2026-07-30_dovodka-fabriki/UROKI-FABRIKE.md`
   ДОСТАВЛЕНО: нет
2. **З5 «канал запуска» переопределён этим заходом** (см. `## ПЛАН`) с буквального «слово терминал/приложение» на «названа однозначная точка входа» — потому что буквальная формулировка красила бы ОБА калибровочных файла (проверено грепом, см. план). Открытый вопрос аналитику: стоит ли ВООБЩЕ ЗАВОДИТЬ отдельное поле «канал: terminal/app» в теле будущих `kod_*.md` (RUKOVODSTVO §Два канала запуска сейчас живёт только в `bootstrap_zahod.py --kanal`, не в тексте самого захода) — или текущее переопределение (точка входа) и есть достаточная замена по существу.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Сделано + ЗАЧЕМ.**
- **Часть A.** `_generator/tools/check_zahod.py` — линтер входа, судит текст `kod_*.md` (7 проверок З1-З7, минимум по заданию). Пять жёстких (exit 1): З1 критерий существует/не пуст/содержит backtick-команду или число; З2 в критерии есть словоформа «жив*прогон»/«реальн*файл/объект» (живой прогон, не только фикстура); З3 ЗОНА — явный список backtick-путей с «/», исключая имя корня репозитория целиком; З4 «## КОНТРАКТ ЗОНЫ» называет физический файл отчёта (слово «файл» + worktree/основная папка/абсолютный путь); З5 названа однозначная точка входа (`worktree add` / `git branch --show-current` / `git rev-parse --abbrev-ref HEAD`); З7 все 4 секции-заготовки + формат очереди ДОМ:/ДОСТАВЛЕНО: в шапке ВОПРОСЫ. Две печати-предупреждения (не гейт, не роняют exit-код): З6 числовое утверждение без соседней команды/адреса в окне ±300 символов.
- **Часть B.** `bootstrap_zahod.py`: (1) после сборки текста (ДО записи на диск) вызывает `check_zahod.judge(text, checks=STRUCTURAL_CHECKS)` — подмножество из трёх проверок (З3/З5/З7), за которые генератор отвечает САМ; при красном — заход НЕ создаётся, печатается что чинить; (2) `--kanal` стал `required=True` без дефолта; (3) в worktree-режиме `mesto` получил фразу про дом отчёта (тем же приёмом anchor-текста, что уже применялся для токенов/очереди) — дословно контракт этого захода: файл живёт в основной папке в одном экземпляре, сам не коммитится.
- **Часть C.** `fixtures/zahod/PROGNAT.sh` — новая, `TOOL-CONTRACT-COVERS: check_zahod.py`, 13 ловушек (задание требовало ≥9): положительный контроль на двух живых заходах (rc=0 и ПУСТОЙ вывод — главная ловушка), синтетический положительный контроль, по одной ловушке на З1/З2/З3/З4/З5/З7×2 (секции+очередь), печать З6, отказ `bootstrap_zahod.py` без `--kanal`, и ДВЕ регресс-ловушки на находки верификатора (см. ниже).

**Оспоренные предпосылки (см. `## ПЛАН`, детали и живые замеры там):**
1. З5 «канал запуска (терминал/приложение)» переопределён на «однозначная точка входа» — буквальная формулировка красила бы ОБА калибровочных файла (проверено грепом — ни один не называет канал этими словами).
2. Часть B п.1: гейтить в `bootstrap_zahod.py` ПОЛНЫЙ `judge()` нельзя — живой прогон пробного захода показал, что свежесобранный скелет ВСЕГДА красный по З2 (критерий ещё не заполнен Cowork'ом, это отдельный ход). Решение — `STRUCTURAL_CHECKS`, подмножество из того, за что генератор отвечает сам.

**Как проверил (живые прогоны, охват в каждом):**
- `sh _generator/tools/fixtures/zahod/PROGNAT.sh` → **13 из 13** ловушек зелёные, exit 0.
- `sh _generator/tools/fixtures/git_zona/PROGNAT.sh` → зелёные, exit 0 (пришлось поправить встроенную ловушку 17 — она копирует РЕАЛЬНЫЙ `bootstrap_zahod.py`+шаблон в изолированный `$T17` и раньше не передавала `--kanal`/backtick-зону; без `check_zahod.py` в копируемом наборе файлов и без этих флагов моя же правка ломала эту чужую ловушку — почищено тем же ходом, т.к. `fixtures/git_zona/` в моей зоне).
- Каждая ловушка фикстуры `zahod` проверена мутацией и показывает краснеющий/появляющийся ПОИМЕНОВАННЫЙ код (текст мутаций — в самой фикстуре, каждая с `echo "── ловушка N"`).
- `python3 _generator/tools/check_tool_contract.py _generator/tools/check_zahod.py _generator/tools/bootstrap_zahod.py` → «проверено **2 из 2**».
- 🔴 **Живой прогон на всех файлах-заходах арки** (демонстрация З2/критерия 4): `for f in kod_*.md; do check_zahod.py "$f"; done` → **краснеет 14 из 17**. Зелёные (пустой вывод, кроме предупреждения З6 на себе): `kod_linter-zahoda.md` (этот файл), `kod_tirazh-ocheredi.md`, `kod_ochered-domov.md` — три файла, писавшихся с полным контрактом зоны/критерием/точкой входа. Красные 14 — заходы старее дисциплины (`ochered-domov`/`tirazh-ocheredi`), красное на них — результат, а не сбой линтера (писались до него); полный вывод по каждому — в транскрипте сессии, коды: чаще всего З2 (нет явного «живого прогона» в критерии) и З7 (нет формата очереди ДОМ/ДОСТАВЛЕНО — заходы старше этого механизма) и З3 (зона словами, не списком путей).
- 🔴 **Самоприменение**: `check_zahod.py` на ЭТОМ файле → **rc=0** (одно предупреждение З6, не гейт).
- `bootstrap_zahod.py` без `--kanal` → argparse отказывает («the following arguments are required: --kanal»), заход не создаётся — показано и живым вызовом, и ловушкой 11.
- Зона закоммичена: `git_zona.py check --zone _generator/tools` → ✅, `git show --stat` — только 4 пути моей зоны.

**Результат верификатора (свежий субагент, атака на калибровку — дай `check_zahod.py` + два живых захода, найди ложно-зелёное и ложно-красное).** Нашёл ДВА реальных пробела, оба сразу исправлены и подтверждены прогоном + добавлены регресс-ловушками 12/13 в фикстуру:
1. **Ложно-зелёное (З3):** `**ЗОНА:** \`materials/\`` (имя корня репозитория целиком, не сужает ничего) проходило проверку — regex требовал только «/» в токене. Починка: явное исключение точного совпадения с именем корня (`REPO_ROOT_NAME = "materials"`, зафиксировано строкой, а не выведено из `Path(__file__)` — первая попытка вывести динамически провалилась на живом прогоне из worktree, где `parents[2].name` — имя временной рабочей папки, а не «materials»).
2. **Ложно-красное (З2):** критерий, перефразирующий живой прогон без буквальной словоформы («по-настоящему исполненная команда на реальном объекте, не пересказ»), красился. Починка: `LIVE_RUN_RE` расширен синонимами («реальн\*файл/объект», «не только фикстура») — с честной оговоркой в коде, что чисто лексическая проверка не бывает полной (та же критика, что фабрика уже применяет к себе в `kod_ochered-domov.md`).
Обе починки подтверждены прогоном на исходных bait-файлах верификатора (`/tmp/kod_dyra-zona-vsyo.md`, `/tmp/kod_zdorovyj-zahod.md`) и на калибровочных файлах (по-прежнему rc=0, пустой вывод).

**Что НЕ трогал:** `_TEMPLATE-zahod.md` (внешний файл шаблона, вне зоны — anchor-инъекция вместо правки), `register_doc.py` и `fixtures/register_doc/` (незакоммиченная работа владельца), `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md`, чужие файлы-заходы (`kod_*.md` кроме своего — только читал), `fixtures/dvizhok/` (вне зоны, READ-ONLY — см. чужой долг ниже), физический путь записи `dst` в `--worktree`-режиме (см. `## ВОПРОСЫ` №1 — правка крупнее темы этого захода).

**Чужой долг (обход хука, §4):** коммит `3e1b9ed` прошёл с `--no-verify`, ДВЕ причины, обе подтверждены сравнением с baseline (`git stash`):
1. **Известный, предуказанный заходом:** `fixtures/dvizhok/PROGNAT.sh` краснеет на отсутствующем `chrome-headless-shell` (playwright не установлен) — идентично на baseline (тот же traceback без моих правок).
2. 🔴 **Новый, но не моя зона:** `fixtures/dvizhok/PROGNAT.sh` внутри себя (`zvat.py`) вызывает `bootstrap_zahod.py` БЕЗ `--kanal` в нескольких местах — на baseline (`git stash`) слово «kanal» вообще не встречается в логе фикстуры (0 вхождений), то есть это НЕ пред-существующий долг, а прямое следствие требования ЭТОЙ части задания («--kanal обязателен»). `fixtures/dvizhok/` вне моей зоны (READ-ONLY) — почему не чинил: правка чужой фикстуры запрещена контрактом дословно («Всё вне — READ-ONLY: не править... даже если чинится в одну строку»). Оформлено вопросом владельцу ниже.

**Вопросы:** два пункта очереди в `## ВОПРОСЫ` (место записи dst в --worktree режиме противоречит новому канону «один экземпляр»; нужен ли отдельный текстовый маркер канала terminal/app в теле файла-захода) + информация о чужом долге dvizhok выше.

**Открытое «возвращаться»:** да — минимум два хода: (1) `bootstrap_zahod.py --worktree` физически кладёт `.md` не в основную папку (см. ВОПРОС №1); (2) `fixtures/dvizhok/PROGNAT.sh`/`zvat.py` нужно обновить, чтобы передавать `--kanal` в свои вызовы `bootstrap_zahod.py` — иначе эта фикстура будет краснеть при КАЖДОМ будущем правленом пути её охвата, пока кто-то не тронет её саму.

**НЕОБРАТИМОЕ:**
- Пробы части-плана (`bootstrap_zahod.py --worktree`/без `--worktree`) создавались и убирались `rm -rf` + `git checkout -- _studio/docs/KARTA.md` (хэш `KARTA.md` сверен побайтово до/после — совпал, `b0c2c767a9488825d4a5a4c65ba0ad0bb21eb957`) + `git_zona.py worktree drop --force` на подтверждённо-мусорной пробной worktree-папке (единственный untracked путь там — пробный `kod_proba.md`, не тронутый мной содержательно) + `git branch -d` на пробной ветке.
- Остальное — необратимого нет.

**АРТЕФАКТ:** `_generator/tools/check_zahod.py` — `python3 _generator/tools/check_zahod.py <путь к kod_*.md>`
**КОММИТ:** `3e1b9ed9d6a1c0e9795ecc9a78e56b798303404a` — «_generator/tools: линтер входа check_zahod.py (7 проверок), гейт в bootstrap_zahod.py, --kanal обязателен, фикстуры» · 4 файла · `git_zona.py check --zone _generator/tools` → ✅ (не запушено — ветка не отслеживает origin, решение оставлено аналитику/владельцу)
