# Канал исполнителя — priyomka-mehanizm (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — механика расписана до уровня подкоманд, проверок и списка ловушек.

КОНТЕКСТ. Репозиторий `materials/` — фабрика, собирающая лекционные курсы. Работа идёт так: аналитик пишет файл-заход, автономный исполнитель его выполняет и дописывает отчёт, аналитик отчёт принимает. **Процедура приёмки расписана в каноне на шесть шагов, и носитель есть ровно у одного из них** — шага 0 (`git_zona.py check --zone`). Остальные пять — проза. Следствие измерено самим каноном: из 8 вопросов исполнителей до владельца дошло **0**, из 17 уроков до канона дошёл **1**. Владелец проговаривает процедуру голосом на каждом отчёте — дословно 03.08: *«обязательно всё консолидировать, обязательно всё разнести по домам, обязательно сразу после приёмки отчёта»*. **Правило, которое приходится произносить вслух на каждом применении, механизмом не является.**

**ЦЕЛЬ: дать приёмке носитель — одну команду, которая прогоняет всё машинно проверяемое и печатает аналитику ровно то, что он обязан разнести руками.** Плюс добить предыдущий заход: построенный им гейт полноты дневника пока ничем не вызывается.

Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: часть A (добивка) важнее части B — сделай её первой и, если встанешь, отчитайся честно.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** отдельная рабочая папка (worktree), а НЕ основная. Заводится одной командой из корня репо: `python3 _generator/tools/git_zona.py worktree add priyomka-mehanizm --branch zahod/priyomka-mehanizm`. Дальше `cd` в неё и работай там. 🔴 **`git checkout` в основной папке НЕ делай:** она стоит на другой ветке, и checkout МОЛЧА откатит дерево под всеми, кто там работает (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/tools/priyomka.py` `_generator/tools/dnevnik.py` `_generator/tools/zakryt_sessiyu.py` `_generator/tools/fixtures/priyomka/` `_generator/tools/fixtures/sessiya/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_priyomka-mehanizm.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
  🔴 **`_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` в зону НЕ входит намеренно.** Канон правит аналитик, и это не формальность: сам канон объясняет, почему консолидирующая правка посреди работы не делается никогда (§Хук «→ фабрика»). Твоё дело — механизм; текст канона приведёт в соответствие аналитик по твоему отчёту.
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным. **Красный хук на ТВОЁМ новом `.md` — не повод для `--no-verify`, а повод позвать дверь.** *Цена правила: 27 обходов `--no-verify` из 28 срывов коммита по одной этой причине, тринадцать исполнителей подряд.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (§4). НИКОГДА `-A` / `.` / `commit -am`. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Заведи worktree командой из контракта и перейди в него. `git branch --show-current` обязано дать `zahod/priyomka-mehanizm`.
- **Проверь, что база свежая:** `grep -c 'origin' _generator/tools/zakryt_sessiyu.py` должно быть больше нуля, и файл `_generator/tools/dnevnik.py` должен существовать. Если нет — ветка отпочковалась до того, как владелец влил предыдущий заход (`git_zona.py adopt --branch zahod/dnevnik-gejt --zone _generator/tools`). **Тогда СТОП и в `## ВОПРОСЫ`:** работать поверх старой базы нельзя, ты переоткроешь уже починенное.
- Прочитать ТОЛЬКО: `_generator/tools/dnevnik.py` (целиком — главный якорь части A), `_generator/tools/zakryt_sessiyu.py` (целиком), `_generator/tools/fixtures/sessiya/PROGNAT.sh` (образец фикстуры), `_generator/tools/check_tool_contract.py` — **только докстринг, первые 30 строк**, `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` — **только раздел `## Приёмка отчёта` и следующий за ним `## Хук «→ фабрика»`** (строки ~145–215; это спецификация части B, читай её как ТЗ). Проект сверх этого не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. *Цена 21.07: `rc=128` четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия; критерий, который может провалиться. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы.** Субагенты: ≤5, рейт-лимит = отступить + доложить.

🔴 **Только stdlib.** Условие запуска на машине владельца (Python 3.9.6 — аннотации PEP 604 вида `str | None` НЕ применять, они там падают).

🔴 **Правило, оплаченное предыдущим заходом дважды за день (`UROKI-FABRIKE.md` №41–42): грубая эвристика в инструменте против тихой потери — не экономия, а та же поломка.** Три редакции правила опознания подряд оказались неверны, и ни одну не нашёл автор — находили положительный контроль фикстуры и верификатор. **Мера истины здесь — прогон на живом файле, а не убедительность правила.** Каждое своё правило проверяй прогоном, а не рассуждением.

## 2. ЗАДАЧА

### ЧАСТЬ A — добивка: инструмент становится гейтом (делать первой)

Предыдущий заход построил `dnevnik.py` с подкомандами `proverit` и `zapis`. Четыре решения аналитика по его вопросам:

1. **Вызвать `proverit` последним ходом `zakryt_sessiyu.py`**, а НЕ вешать в `pre-commit`. Причина: коммит и сессия не совпадают — посреди сессии дневник законно неполон, хук краснел бы на каждом коммите, и его отключили бы. Гейт, который шумит зря, хуже отсутствующего. Закрытие сессии — единственный момент, когда «полно» имеет смысл. Форма: после записи выгрузки `zakryt_sessiyu.py` печатает результат сверки и **несёт её код возврата наружу** (выгрузка удалась, но дневник неполон → ненулевой выход и внятная строка, что делать).
2. **Сторож формата `origin`.** Сейчас исчезновение или переименование хостового поля деградирует разбор МОЛЧА — тот самый класс, против которого весь заход. Нужна печать доли записей роли `user` без поля `origin` **в свежем транскрипте** (свежесть определяй по mtime файла, порог задай сам и объясни). Ожидание — ноль; ненулевое печатается как предупреждение с числом. Порог, при котором это становится ошибкой, а не предупреждением, предложи в отчёте, не выдумывай молча.
3. **Номер реплики — `В<NNN>`, три знака.** Сейчас `В<NN>` перестаёт быть фиксированной ширины на сотне реплик, а сессии такого размера уже рядом. Старых номеров в дневниках нет (проверено, их там ноль), так что миграции не требуется.
4. **Прогнать критерии 1–4 предыдущего захода заново** — их приняли по отчёту, а не прогоном: worktree аналитику из песочницы не виден, охват приёмки был объявлен «1 из 5». Прогони фикстуру `sessiya`, `check_tool_contract` и `dnevnik.py proverit` на живом транскрипте, числа — в отчёт. **Расхождение с прошлым отчётом — находка, а не помеха: назови её прямо.**

5. 🔴 **`zakryt_sessiyu.py` рождает документ-сироту и роняет коммит владельца.** Он пишет `VYGRUZKA-<дата>.md` и не зовёт дверь регистрации — ворота 5 краснеют, и падает НЕ выгрузка, а следующий коммит, вместе со всей чужой зоной в нём. Живая цена 03.08: коммит дневника арки упал с `rc=1` на строке `VYGRUZKA-2026-08-03.md:1 — документ-сирота (KONSTITUCIYA §9)`, и вместе с ним не доехали пять путей — дневник, уроки, оба файла-захода. Лечение известно и уже применялось к `bootstrap_arka.py` (заход `kod_dobivka-registracii.md`): **инструмент, создающий `.md`, зовёт `register_doc.py` тем же ходом.** Сделать то же здесь. **И проверить остальные инструменты своей зоны на тот же дефект** — `dnevnik.py zapis` пишет в существующий файл и двери не требует, но убедись прогоном, а не рассуждением.

### ЧАСТЬ B — приёмка отчёта как механизм

Построить `_generator/tools/priyomka.py`. **Он не заменяет аналитика — он отбирает у него возможность забыть.**

```
python3 _generator/tools/priyomka.py <путь к kod_*.md> [--zone <префикс>]
```

Инструмент делает два разных дела, и в выводе они должны быть разделены.

**(1) МАШИННЫЕ ГЕЙТЫ — проверяет сам, exit 1 при любом красном:**

| # | что | как |
|---|---|---|
| Г0 | зона доехала в git | вызов `git_zona.py check --zone`; зона берётся из `--zone` или из контракта зоны в самом файле-заходе |
| Г1 | хэш коммита из отчёта существует | вытащить из строки `**КОММИТ:**`, проверить `git show --stat` |
| Г2 | строка `**АРТЕФАКТ:**` заполнена | не пустая и не плейсхолдер в угловых скобках |
| Г3 | артефакт существует и НЕ старше источника | если путь указывает на файл — сверить время; старше — красное («сборка молча не запускалась, владелец откроет прошлое состояние») |
| Г4 | секции отчёта заполнены | `## ОТЧЁТ` непустая, `## ПЛАН` непустая |
| Г5 | уроки исполнителя имеют `ЦЕНА:` | секция `## УРОКИ ФАБРИКЕ` файла-захода; пустая секция — законный исход, урок без цены — нет |
| Г6 | 🔴 в дневнике арки нет урока без пары в доме уроков | `grep -in "урок"` по `SESSIYA.md` — **регистронезависимо и по одному слову**; каждое совпадение печатается аналитику для сверки. *Этот гейт описан в каноне и не существует в коде; предыдущая его форма `grep -n "урок фабрике\|УРОК ФАБРИКЕ"` давала ЗЕЛЁНОЕ при двух незаписанных уроках, потому что в дневнике стояло «Урок фабрике» с заглавной. Терпимая форма ловит лишнее — это правильная сторона ошибки: ложное срабатывание стоит одного взгляда, пропуск стоит урока.* |

**(2) ПЕЧАТЬ ДЛЯ РУК — то, что машина проверить не может, но обязана положить аналитику на стол.** Не гейт, а список к разнесению. Печатать ВСЕГДА, даже когда всё зелёное:

- **`## ВОПРОСЫ` исполнителя** — списком, целиком. *Цена: 8 накопилось в файлах-заходах, 0 дошло до владельца, 6 протухло. Владелец файлы-заходы не читает ПО УСТРОЙСТВУ.*
- **`## УРОКИ ФАБРИКЕ` исполнителя** — списком, целиком, с пометкой, что переносить их не надо (исполнитель пишет сам), но прочитать обязан.
- **Строки отчёта с числами об артефакте** — их приёмка обязана воспроизвести командой на живом файле. Печатать найденные числовые утверждения. *Цена 29.07: отчёт заявил «106 блоков с адресами», греп по артефакту дал ПУСТО; приёмка приняла на слово, долг всплыл при закрытии арки.*
- **Чек-лист оставшихся шагов канона** одной короткой врезкой: разнести по домам · пересказать владельцу человеческим языком · короткая рефлексия · строка в `UROKI-FABRIKE.md`.

🔴 **Инструмент НЕ решает, что важно, и ничего не резюмирует.** То же проектное решение, что у `zakryt_sessiyu.py` (прочитай его докстринг): «умный фильтр» потеряет ровно то, что теряет человек. Печатай полностью.

### ЧАСТЬ C — фикстура `_generator/tools/fixtures/priyomka/PROGNAT.sh`

Новая фикстура с объявленным `TOOL-CONTRACT-COVERS: priyomka.py`. Ловушек — **не менее семи**, каждая с комментарием «что ломается и чего это стоило». Обязательно среди них:

1. **Отчёт с плейсхолдером в `АРТЕФАКТ`** (угловые скобки как есть) — обязан краснеть, а не считать строку заполненной.
2. **Артефакт старше источника** — красное.
3. **Хэш коммита, которого нет в репозитории** — красное.
4. **Урок без `ЦЕНА:`** — красное.
5. 🔴 **Урок в дневнике, написанный как «Урок фабрике» с ЗАГЛАВНОЙ буквы** — обязан быть найден. Прямая регрессия на форму грепа, которая давала ложное зелёное.
6. **Вопросы исполнителя ПЕЧАТАЮТСЯ даже при полностью зелёном прогоне** — иначе инструмент молча съест то, ради чего он в основном и нужен.
7. **Положительный контроль:** здоровый отчёт → exit 0, и в выводе есть блок печати для рук. Без него «краснеет всегда» неотличимо от «краснеет верно».

🔴 **Каждую ловушку проверь мутацией:** откати ровно ту строку инструмента, которую ловушка сторожит, покажи, что фикстура краснеет, восстанови. В отчёт — по строке на ловушку.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. `sh _generator/tools/fixtures/priyomka/PROGNAT.sh` → `echo $?` = **0**; `sh _generator/tools/fixtures/sessiya/PROGNAT.sh` → **0**.
2. Каждая ловушка части C показана краснеющей под мутацией — по строке в отчёте.
3. `python3 _generator/tools/check_tool_contract.py _generator/tools/priyomka.py _generator/tools/dnevnik.py _generator/tools/zakryt_sessiyu.py` → зелёный.
4. 🔴 **Живой прогон на настоящем отчёте:** `python3 _generator/tools/priyomka.py _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_dnevnik-gejt.md` — это принятый отчёт предыдущего захода. Вывод целиком в отчёт. **Если инструмент найдёт в нём красное — это результат, а не сбой:** аналитик принял тот отчёт по отчёту, охват приёмки был объявлен «1 из 5».
5. `zakryt_sessiyu.py` несёт наружу код возврата сверки дневника (часть A.1) — показать прогоном на живом транскрипте.
6. Зона закоммичена, `git_zona.py check --zone _generator/tools` → ✅.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y».

## 3. ВЕРИФИКАТОР
Нужен, и метод задан: свежий субагент получает **только** раздел `## Приёмка отчёта` канона и построенный `priyomka.py` и отвечает на один вопрос — **какие требования канона инструмент НЕ проверяет и не печатает**. Это не поиск багов, а замер охвата: у части B охват и есть главный риск, и назвать его обязан не автор. Ответ — в отчёт списком, вместе с твоим решением по каждому пункту (закрыл / оставил сознательно / долг).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
```
git --no-optional-locks add -- _generator/tools/priyomka.py _generator/tools/dnevnik.py _generator/tools/zakryt_sessiyu.py _generator/tools/fixtures/priyomka/ _generator/tools/fixtures/sessiya/ _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_priyomka-mehanizm.md
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/priyomka.py _generator/tools/dnevnik.py _generator/tools/zakryt_sessiyu.py _generator/tools/fixtures/priyomka/ _generator/tools/fixtures/sessiya/ _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_priyomka-mehanizm.md
python3 _generator/tools/git_zona.py check --zone _generator/tools    # из корня репо; должен быть ✅
git --no-optional-locks show --stat                                   # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны** (полное «почему» — `../docs/kak-delat/GIT-disciplina.md §3`): `add` вводит новые пути в индекс (без него pathspec-коммит их не знает); `-- <пути>` в самом `commit` отсекает чужое, застейдженное рядом. *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук покраснел — смотри, на ЧЬИХ путях.** Красное на твоих — чини, не обходи. Красное на чужом унаследованном долге — законный обход, но только с причиной:
```
python3 _generator/tools/git_zona.py commit --zone _generator/tools \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови строкой отчёта. **Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна)**. Время и токены снимает приёмка из лога прогона — не оценивай и не извиняйся за отсутствие счётчика.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ, отдельной строкой.** Здесь — `priyomka.py` плюс вывод живого прогона по критерию 4. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван, и владелец сказал прямо: «я всё время не могу найти твои новые файлы».*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если стояло в задании.** Удаление, перезапись, переименование, любая правка вне зоны — каждое ОДНОЙ строкой: **что · где · чем восстанавливается**. Ты запущен без запроса разрешений: владелец НЕ видел ни одного из этих действий в момент, когда оно происходило. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик. Сломался ВХОД? Критерий готовности кривой? Канон противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу>` — `<чем открывать>`
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone _generator/tools` → ✅
