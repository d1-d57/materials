# Канал исполнителя — razmetka-vladelca (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: opus** — задача целиком про суждение: отнести чужое замечание к ярусу, не подгоняя. Механика в одну строку, вся трудность в границах и в честности «не ложится».

КОНТЕКСТ. Репозиторий `materials/` — фабрика курсов: она собирает лекции по математике, и владелец три недели правил её результат вслух. 31.07 эти правки добыты из 35 источников (разборы после живых лекций, правки после репетиций, дневники арок) в **корпус из 1379 замечаний** — `KORPUS-VLADELCA.md`, жёсткий формат записи: `### ` заголовок · `АДРЕС:` · `ЦИТАТА:` · `КАСАЕТСЯ:` · `ЧИСЛО:` · `КЛАСС:`.

01.08 на интервью с владельцем собран **каркас аксиоматики** — `AKSIOMATIKA-karkas.md`. Он утверждает, что все замечания про продукт ложатся на четыре яруса, и что несущая аксиома одна — **порционность**. **Это гипотеза аналитика, проверенная выборкой из семи групп по пять записей, и не более того.** Ты запущен, чтобы проверить её на всех 1379 записях — и опровергнуть, если она не держится.

ЦЕЛЬ: **каждая из 1379 записей получает ЯРУС (с сохранением адреса); все названные числа собраны с контекстом применимости; покрытие каркаса измерено и напечатано.**

Ты **разметчик, а не законодатель.** Законов не формулируешь, правил не пишешь, вердиктов не ставишь, гейтов не заводишь — это работа судьи, и она отдельная.

Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: размеченная часть с адресами плюс замер покрытия по ней — законный результат, работа возобновляема. **НЕ сдавай разметку без третьего артефакта:** таблица «запись → ярус» без ответа на вопрос «сколько не легло и почему» не отвечает на вопрос, ради которого заход существует.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/zhurnal/2026-07-30_dovodka-fabriki/verdikt-vladelca.tsv _studio/zhurnal/2026-07-30_dovodka-fabriki/verdikt-chisla.tsv _studio/zhurnal/2026-07-30_dovodka-fabriki/RAZMETKA-VLADELCA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/CHISLA-s-kontekstom.md _studio/zhurnal/2026-07-30_dovodka-fabriki/POKRYTIE-karkasa.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_razmetka-vladelca.md _studio/docs/KARTA.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add _studio/zhurnal/2026-07-30_dovodka-fabriki/verdikt-vladelca.tsv _studio/zhurnal/2026-07-30_dovodka-fabriki/verdikt-chisla.tsv _studio/zhurnal/2026-07-30_dovodka-fabriki/RAZMETKA-VLADELCA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/CHISLA-s-kontekstom.md _studio/zhurnal/2026-07-30_dovodka-fabriki/POKRYTIE-karkasa.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_razmetka-vladelca.md _studio/docs/KARTA.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Снять ЧИСЛА ДО работы командой** (мои — ориентир, снятый 01.08 в 00:0x; разошлось — объясни расхождение и работай от СВОЕГО):
```
cd /Users/ivanyakovlev/Documents/GitHub/materials
grep -c '^### ' _studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md
grep '^ЧИСЛО: ' _studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md | grep -vc 'ЧИСЛО: НЕТ'
grep -c '^## Источник: ' _studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md
```
База, снятая мной 01.08 в 00:1x: **1379** записей · **269** записей с названным числом · **34** источника. *Осторожно с полем `ЧИСЛО:` — оно стоит у ВСЕХ 1379 записей, у большинства со значением `НЕТ`. Команда `grep -c '^ЧИСЛО: '` вернёт 1379 и соврёт; считать надо непустые, как выше.*
- **Прочитать ТОЛЬКО эти якоря:**
  - `_studio/zhurnal/2026-07-30_dovodka-fabriki/AKSIOMATIKA-karkas.md` — рамка, по которой размечаешь. Читать целиком, это твоя единственная спецификация ярусов;
  - `_studio/zhurnal/2026-07-30_dovodka-fabriki/skelet-vladelca.tsv` — **твой основной материал**: 1379 строк `id · источник · заголовок · АДРЕС · КАСАЕТСЯ · КЛАСС · ЦИТАТА (200 знаков)`. Цитата в скелете есть именно затем, чтобы судить, не открывая корпус;
  - `_studio/zhurnal/2026-07-30_dovodka-fabriki/skelet-chisla.tsv` — 269 строк с числами;
  - `_studio/zhurnal/2026-07-30_dovodka-fabriki/KORPUS-VLADELCA.md` — **открывай выборочно, по конкретной записи**, когда обрезанной цитаты не хватило для суждения. Читать его подряд не надо: всё, что нужно для 9 записей из 10, уже в скелете;
  - `_studio/zhurnal/2026-07-30_dovodka-fabriki/KRATNOST-vladelca.md` — **только как справка при сомнении, и с презумпцией недоверия** (почему — §2, «главная ловушка»);
  - **Проект дальше этого не изучай.** `KORPUS-*.md` без суффикса `VLADELCA` — чужой материал (корпус исполнителей), его размечает параллельный заход `kod_razmetka-osej.md` по другой рамке. Не открывай и не размечай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

### 🔴 ГЛАВНАЯ ЛОВУШКА ЭТОГО ЗАХОДА — читай до всего остального

Рядом лежит `KRATNOST-vladelca.md`: те же 1379 записей, уже разложенные по 56 группам с заголовками вроде «цвет и палитра» или «зал: кто слушатели». **Соблазн разметить 56 групп вместо 1379 записей огромен — он экономит девяносто процентов работы. Так делать нельзя, и вот замер, а не мнение.**

Аналитик взял из этого файла 7 групп по 5 случайных записей. В группе «цвет и палитра» **три записи из пяти оказались не про цвет**: «копии источников — под курс, общий слой — карта для поиска», «черновик отбракован: перекос в дриллы», «текст сократился в очень странном формате». Группировка делалась по ключевым словам и шумит.

⇒ **Разметив группу целиком, ты присвоишь ярус десяткам записей, которые в неё попали случайно, — и шум группировки станет фактом аксиоматики.** Открывай `KRATNOST-vladelca.md` только при сомнении по конкретной записи, и всегда проверяй её текст глазами.

*То же в обратную сторону: если ты видишь, что запись явно не про то, чем названа её группа, — это находка, и её надо назвать в отчёте числом («записей, чья группа не соответствует содержанию: N»). Судья это ждёт.*

### 🔴 ДИАПАЗОН — ты, скорее всего, размечаешь НЕ ВЕСЬ корпус

**Если в промпте запуска назван диапазон id (например «размечай V0346–V0690») — размечаешь ровно его и останавливаешься.** Соседние пачки делают другие сессии; их вердикты уже могут лежать в файле, и трогать их нельзя. Пиши **только дописыванием в конец** `verdikt-vladelca.tsv`, ничего в нём не переписывай и не сортируй.

Проверка своей пачки — по её же границам: `awk -F'\t' '$1>="V0346" && $1<="V0690"' verdikt-vladelca.tsv | wc -l` должно дать число записей диапазона.

**Сборщик `sobrat_razmetku.py vladelca` до последней пачки будет краснеть** на «без вердикта осталось N записей» — это НОРМА, а не твой брак. Смотри в его выводе только на претензии, относящиеся к ТВОИМ id (неизвестный ярус, ярус 1 без материи, «не ложится» без причины, дубль). Диапазон назван — критерий готовности читается по нему, а не по 1379.

**Почему дробим.** Стоимость хода растёт с длиной сессии: контекст перечитывается целиком на каждом ходу. Замер на заходе H1 01.08: 461 запись, 126 ходов, **33 млн токенов кэш-чтения** — это и была основная статья расхода, а не выход. Четыре сессии с чистым контекстом дешевле одной длинной при том же качестве суждения.

### 🔴 ФОРМА ВЫХОДА — ты пишешь ТОЛЬКО вердикты, таблицу собирает скрипт

**Не переписывай корпус.** Скелет уже нарезан скриптом и лежит готовым:

```
_studio/zhurnal/2026-07-30_dovodka-fabriki/skelet-vladelca.tsv   # 1379 строк: id · источник · заголовок · АДРЕС · КАСАЕТСЯ · КЛАСС
_studio/zhurnal/2026-07-30_dovodka-fabriki/skelet-chisla.tsv     #  269 строк: id · ЧИСЛО · АДРЕС · ЦИТАТА
```

Твой выход — **два файла вердиктов, по одной короткой строке на запись**:

```
verdikt-vladelca.tsv   id <TAB> ярус <TAB> материя <TAB> причина (только для «не ложится»)
verdikt-chisla.tsv     id <TAB> ПРИ КАКОМ МАСШТАБЕ <TAB> ФОРМА
```

Финальные `RAZMETKA-VLADELCA.md`, `POKRYTIE-karkasa.md` и `CHISLA-s-kontekstom.md` собирает
```
python3 _studio/zhurnal/2026-07-30_dovodka-fabriki/sobrat_razmetku.py vladelca
python3 _studio/zhurnal/2026-07-30_dovodka-fabriki/sobrat_razmetku.py chisla
```
**Гони эти команды сам после каждой сотни вердиктов** — они краснеют (exit 1) на пропущенном id, неизвестном ярусе, ярусе 1 без материи, «не ложится» без причины, пустом масштабе у числа. Это твоя проверка, а не приёмка судьи.

**Почему так, а не таблицей.** Переписывание 1379 строк — это ~55 000 токенов выхода на копирование того, что уже на диске, и главное — **адрес, прошедший через модель, может быть искажён**. Здесь адреса не проходят через тебя вообще: скрипт берёт их из скелета по id. Соответственно из критерия ушёл пункт «проверь 10 адресов грепом» — проверять нечего.

**Арифметику покрытия тоже считает скрипт** (таблица ярусов, доли, сумма, сверка с гипотезой 1067/151/161). Ты пишешь РАЗБОР — то, чего скрипт не умеет: почему так вышло, где каркас трещит, что делать судье.

*Формат строки вердикта, дословно:*

**Словарь `ярус`** — берётся из `AKSIOMATIKA-karkas.md §3`, ничего своего не выдумывай, кроме двух служебных значений:

| значение | что это | признак в записи |
|---|---|---|
| `0` | **замысел** — на кого лекция, зачем, что должно остаться, единая нить курса | говорит про зал, адресата, дух, смысл целого, что из курса убрать |
| `1` | **порционность** — материал разложен на порции, порция несёт одну мысль, порции идут по одной | говорит про объём поданного за раз, про порядок подачи, про «рано показали», про «одна мысль на слайд» |
| `2` | **читаемость** — показанное не считывается или сделано плохо | «непонятно, что нарисовано» · «так по-русски не говорят» · «пустая полоса — брак» · про цвет, подписи, единство стиля |
| `3` | **ориентир** — названо число или объём | сколько минут, задач, слайдов, лекций, примеров |
| `процесс` | замечание про ФАБРИКУ, а не про лекцию | где вести записи · приёмка и гейты · роль владельца · инструмент и архитектура |
| `не ложится` | ни одно из перечисленного, честно | **обязателен текст причины в той же строке** |

**Словарь `материя`** — заполняется **только для яруса 1**, иначе `—`: `время` · `кадр` · `рисунок` · `доказательство`.

🔴 **Ярус ставится по МЕХАНИЗМУ жалобы, а не по теме.** Замечание про рисунок может быть ярусом 1 («рисунок появился целиком, а надо было раскрывать»), ярусом 2 («непонятно, что нарисовано») или ярусом 3 («иллюстраций на слайде не больше двух»). Тема одна, ярусы разные. Это главная работа захода, и в ней вся его ценность.

**Одна запись — один ярус.** Тянет на два — ставь тот, который сильнее, и **помечай сам ярус вопросительным знаком**: `1?`. Число таких строк печатает скрипт, разбор пишешь ты.

**Работа возобновляема и обязана быть такой:** дописывай `verdikt-vladelca.tsv` **после каждого источника** (их 34), а не в конце — файл только пополняется, ничего не переписывается. После третьего источника прервись сам, прогони `sobrat_razmetku.py vladelca`, посмотри, что он говорит, и скажи в отчёте, что проверил.

### Артефакт 2 — числа: `verdikt-chisla.tsv`

269 строк скелета `skelet-chisla.tsv` уже несут `id · ЧИСЛО · АДРЕС · ЦИТАТА`. От тебя — **два поля на строку**, и первое из них важнее самого числа:

```
id <TAB> ПРИ КАКОМ МАСШТАБЕ <TAB> ФОРМА
```

- **ПРИ КАКОМ МАСШТАБЕ** — жанр артефакта, длительность, формат файла, тип лекции: всё, что видно из записи и её источника. Не выдумывай: чего в источнике нет, того не пиши. Скрипт краснеет на пустом и куцем масштабе — он и есть смысл файла.
- **ФОРМА** — одно из `ориентир` · `WARN` · `гейт`.

🔴 **`ориентир` — значение по умолчанию, и в этом заходе оно остаётся почти у всех.** Перевод числа в гейт — решение судьи с названной ценой, не твоё. Ставь `WARN` или `гейт` только там, где владелец сам сказал, что это жёсткая граница, и приведи его слова в поле масштаба.

**Почему контекст обязателен — цена названа владельцем 31.07 дословно:** *«сказать, что не может быть больше 50 слайдов, хотя в теории в одном html может быть содержание нескольких лекций»*. Число, вынутое из контекста и вбитое в гейт, начинает краснеть на здоровом; гейт, краснеющий на здоровом, отключают, и тогда пропадает вся защита, а не одна проверка (`RESHENIYA Р31`, оплачено 21.07).

**Отдельный раздел файла — `## КОНФЛИКТУЮЩИЕ`:** одна и та же величина, названная разными числами в разных источниках (например, длительность лекции). Не примиряй их и не выбирай правильное — **выпиши обе с датами**. Это развилки для владельца, и они ценнее согласованного свода.

### Артефакт 3 — раздел `## РАЗБОР` в `POKRYTIE-karkasa.md`, и он важнее первых двух

Сам файл печатает скрипт: таблицу ярусов с долями, сумму, список «не ложится», сверку с гипотезой 1067/151/161, предупреждение про частоту. **Складывать за ним не надо.** Внизу он оставляет пустой раздел `## РАЗБОР` — вот его пишешь ты, и он отвечает на один вопрос: **держится ли каркас на живом материале.**

Четыре обязательных пункта разбора:

1. **Что показала разница между гипотезой по группам и фактом по записям.** Аналитик получил продукт 1067 · процесс 151 · не легло 161, раскладывая 56 групп целиком. Твой счёт по записям почти наверняка иной. **Объясни разницу**: где группа оказалась смесью, где аналитик ошибся классом.
2. **Разбор «не ложится».** Если их больше сотни — **каркас неполон, так и напиши заголовком**. Это законный результат захода, а не его провал. Сгруппируй непоместившееся: что это за материал и какого яруса ему не хватило.
3. **Проверка несущей аксиомы.** Каркас утверждает, что порционность — несущая, а математика внутри неё материей `доказательство`, а не отдельным ярусом. Посмотри на свои числа: сколько записей яруса 1 пришлось на `доказательство` и не выглядят ли они чужеродно рядом с остальными тремя материями. **Считаешь, что доказательство — отдельный ярус, — скажи прямо, с числом и тремя примерами.** Владелец решил иначе, но решал он на четырёх лекциях, где содержание было готовым; твои данные шире, и возразить ему тут — работа, а не дерзость.
4. **Ярус 3 против ярусов 1–2.** Число названо почти всегда «внутри» другой жалобы («слишком много текста на слайде — не больше пяти строк»). Скажи, сколько раз тебе пришлось выбирать между `3` и другим ярусом, и по какому правилу ты выбирал. Если правило получилось шатким — это дефект каркаса, и судья обязан о нём узнать.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — пять условий, четыре из них проверяет скрипт.**

1. **`python3 .../sobrat_razmetku.py vladelca` завершается с кодом 0.** Это сразу: все 1379 id имеют вердикт · нет неизвестных ярусов · нет яруса 1 без материи · нет «не ложится» без причины · нет дублей id. Красный код — критерий провален, и чинить надо вердикты, а не скрипт.
2. **`python3 .../sobrat_razmetku.py chisla` завершается с кодом 0** — все 269 чисел имеют непустой масштаб и форму из словаря.
3. **Раздел `## РАЗБОР` заполнен по всем четырём пунктам выше.** Пустой разбор при зелёных скриптах = заход отработал механически и не ответил на свой вопрос.
4. **Спорные названы числом и разобраны.** Скрипт печатает, сколько строк помечено `?`; ты объясняешь, что это за случаи. *Ноль спорных на 1379 записей — подозрительный результат: либо идеальные границы, либо усталость разметчика.*
5. **Если ты не дошёл до конца — охват назван в отчёте числом**: «размечено 900 из 1379, оборвался на источнике N». Скрипт это подтвердит сам, врать бессмысленно.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «каркас держится», а «на ярусы легло 1290 из 1379, не легло 89, вот их разбор».

**Границы.** Правил не формулируй, вердиктов (`→ гейт` / `→ канон` / `слит` / `отброшен`) не ставь — это следующий заход. Гейтов не заводи. `KORPUS-VLADELCA.md`, `KRATNOST-vladelca.md`, `REESTR-vladelca.md` не правь: они приняты. `AKSIOMATIKA-karkas.md` не правь — возражения пиши в свой третий артефакт и в `## ВОПРОСЫ`. Корпус исполнителей (`KORPUS-*.md` без `VLADELCA`) и файлы `RAZMETKA-OSEJ.md` / `DYRY-po-osyam.md` — зона параллельного захода, не трогай.

## 3. ВЕРИФИКАТОР — обязателен, и он проверяет СУЖДЕНИЕ, а не арифметику

Свежий субагент с пустым контекстом, ОДИН, после того как оба скрипта зелёные. **Арифметику ему не поручай** — её уже посчитал скрипт, и субагент на ней только сожжёт токены. Он делает три вещи, каждую с числом:

1. **Берёт 15 строк вердикта случайно** (не первые 15 — начало вычитано внимательнее всего), открывает исходную запись в `KORPUS-VLADELCA.md` по id через скелет, читает `ЦИТАТА:` целиком и говорит по каждой: ярус верен · ярус спорен · ярус неверен. **Три и более «неверен» из 15 — разметка не принимается**, и это надо назвать в отчёте, а не замолчать.
2. **Берёт 5 строк из `verdikt-chisla.tsv`** и проверяет, что масштаб **следует из источника, а не додуман разметчиком**. Додуманный контекст опаснее пустого: пустой виден скрипту, а выдуманный пройдёт зелёным и станет основанием гейта.
3. **Ищет след группового мышления:** берёт две группы из `KRATNOST-vladelca.md` и смотрит, не получили ли все их записи один ярус механически. Единый ярус на всю группу возможен и законен — но он обязан подтверждаться текстом каждой записи, а не заголовком группы.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/2026-07-30_dovodka-fabriki/verdikt-vladelca.tsv _studio/zhurnal/2026-07-30_dovodka-fabriki/verdikt-chisla.tsv _studio/zhurnal/2026-07-30_dovodka-fabriki/RAZMETKA-VLADELCA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/CHISLA-s-kontekstom.md _studio/zhurnal/2026-07-30_dovodka-fabriki/POKRYTIE-karkasa.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_razmetka-vladelca.md _studio/docs/KARTA.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/2026-07-30_dovodka-fabriki/verdikt-vladelca.tsv _studio/zhurnal/2026-07-30_dovodka-fabriki/verdikt-chisla.tsv _studio/zhurnal/2026-07-30_dovodka-fabriki/RAZMETKA-VLADELCA.md _studio/zhurnal/2026-07-30_dovodka-fabriki/CHISLA-s-kontekstom.md _studio/zhurnal/2026-07-30_dovodka-fabriki/POKRYTIE-karkasa.md _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_razmetka-vladelca.md _studio/docs/KARTA.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **КОММИТ (строка обязательна, см. §4)**.

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Пачка: V0001–V0345, ровно 345 записей** (сверено `awk`: 345 строк скелета в диапазоне, из них 80 с числом).

Числа ДО работы (сняты своей командой, 31.07): **1379** записей · **269** с числом · **34** источника — совпало с базой аналитика от 01.08, расхождений нет. Зона чиста в HEAD (`git status --porcelain` по путям зоны — пусто), точку отката не фабрикую.

Источники в моём диапазоне — 5 из 34: `akt-1-razbor.md` (38, V0001–V0038) · `paskal/RAZBOR-` (109) · `konspekt-l1/PRAVKI-final.md` (62) · `paskal/PRAVKI-` (14) · `teorkat-vvedenie/repeticia/PRAVKI.md` (122). Дописываю вердикты в конец `verdikt-vladelca.tsv` **после каждого источника**; после третьего — прогон `sobrat_razmetku.py vladelca` с чтением его претензий.

Порядок:
1. Прочитать скелет своего диапазона блоками по источнику (цитата в скелете — основной материал; `KORPUS-VLADELCA.md` открывать только точечно по id, когда обрезанной цитаты не хватило).
2. По каждой записи — ярус по МЕХАНИЗМУ жалобы (не по теме), материя только для яруса 1.
3. 80 записей с числом — второй файл: масштаб строго из источника, форма `ориентир` по умолчанию.
4. Прогоны обоих скриптов до зелёного (в пределах претензий к моим id), затем верификатор (1 субагент, 3 задачи), затем `## РАЗБОР`, отчёт, коммит зоны.

**Оспариваю две предпосылки захода — до работы, как требует §1.**

**(а) Критерий готовности №1 и №4 в текущем инструменте несовместимы — конфликт в скрипте, не в вердиктах.** Заход требует помечать спорный вердикт вопросительным знаком **в самом ярусе** (`1?`) и отдельным пунктом критерия требует, чтобы спорных было не ноль. Но `sobrat_razmetku.py` валидирует ярус по словарю `YARUSY = {"0","1","2","3","процесс","не ложится"}` — значение `1?` в словарь не входит и даёт `неизвестный ярус` → exit 1 (строки 68–69). То же с материей: `MATERII` не содержит `время?` (строки 30, 70–71). При этом счётчик спорных в том же скрипте ищет ровно `y.endswith("?") or "?" in m` (строка 87) — то есть автор скрипта пометку задумал, а валидатор её убивает. Итог: пометить спорные по инструкции = провалить критерий 1; не пометить = провалить критерий 4.
**Поправка, которую применяю** (скрипт мне READ-ONLY, а заход прямо велит «чинить надо вердикты, а не скрипт»): спорность пишу **в четвёртое поле** строки вердикта префиксом `СПОРНО: <между какими ярусами и почему>`. Четвёртое поле скрипт читает, но проверяет только у «не ложится» (строки 67, 74–75) — значит остаётся зелёным. Число спорных считаю сам грепом и печатаю в `## РАЗБОР` вместе с разбором случаев; в выводе скрипта строка «Помечено спорными: 0» будет **ложной**, и это сказано здесь и в отчёте, чтобы судья не прочитал ноль как факт. Проверю оба варианта эмпирически перед выбором, а не по чтению кода.

**(б) `POKRYTIE-karkasa.md` и `CHISLA-s-kontekstom.md` скрипт перезаписывает ЦЕЛИКОМ на каждом прогоне** (строки 92, 118, 150) — вместе с разделами `## РАЗБОР` и `## КОНФЛИКТУЮЩИЕ`, которые в них дописывает заход. Пачка нарезана на четыре сессии ⇒ мой разбор умрёт при первом прогоне скрипта следующей пачкой, молча и без следа в выводе. Поэтому пишу разбор **в два места**: в `## РАЗБОР` файла покрытия (как велит заход) и полностью — в `## ОТЧЁТ` этого файла-захода, который никто не перезаписывает. Дубль сознательный, причина — выше.

### Пачка V0346–V0690 (вторая сессия, 31.07)

**Пачка: V0346–V0690, ровно 345 записей** (сверено `awk` по скелету: 345 строк в диапазоне, из них **68** с числом — не 80, как у пачки 1; числа распределены по корпусу неравномерно).

Числа ДО работы (сняты своей командой, 31.07): **1379** записей · **269** с числом · **34** источника — совпало и с базой аналитика, и с замером пачки 1. Расхождений нет. Зона чиста в HEAD; изменён только `_studio/docs/KARTA.md`, и правка **не моя** (то же состояние, что описала пачка 1).

**Источников в моём диапазоне 14 из 34, и они другого рода, чем у пачки 1.** У пачки 1 это были разборы живых лекций и репетиций; у меня **184 записи из 345 (53 %) — дневники сессий фабрики** `_studio/zhurnal/*/SESSIYA.md`:

| источник | записей |
|---|---|
| `teorkat-vvedenie/repeticia/PRAVKI.md` | 95 |
| `_studio/.../2026-07-16_teorkat-programma-dizajn/SESSIYA.md` | 60 |
| `_studio/.../2026-07-12_l2-skolko-informacii/SESSIYA.md` | 36 |
| `_studio/.../2026-07-11_fibonacci-kurs/SESSIYA.md` | 28 |
| `catalan/.../zamechaniya-statya-mir2.md` | 24 |
| `_studio/.../2026-07-10_sborka-konvejera/SESSIYA.md` | 18 |
| `_studio/.../2026-07-16_krivaya-drakona/SESSIYA.md` | 17 |
| `catalan/.../zamechaniya-mosty-kartoteka.md` | 16 |
| `catalan/.../zamechaniya-L1-process-first.md` | 15 |
| `_studio/.../2026-07-11_informacia-i-kody/SESSIYA.md` | 11 |
| `_studio/.../2026-07-11_geometria-6-nagliadnaya/SESSIYA.md` | 8 |
| `catalan/.../zamechaniya-issledovanie-idei.md` | 7 |
| `_studio/.../2026-07-11_reserch-zadach/SESSIYA.md` | 6 |
| `catalan/.../zamechaniya-disciplina-kartochek.md` | 4 |

⇒ **Предсказание до работы, чтобы его можно было проверить по факту:** доля `процесс` у меня выйдет заметно выше 7,8 % пачки 1, потому что половина моего материала — дневники фабрики, а не замечания к деку. Если это подтвердится, вывод не «каркас другой», а «доля яруса — функция источника», что усиливает `§1` каркаса.

**Калибровка по пачке 1 — сознательно беру её правила границ, а не выдумываю свои.** Разбор пачки 1 (спасён из `POKRYTIE-karkasa.md` до прогона) называет два правила, которых в каркасе нет, и на них держится большинство её вердиктов. Применяю те же, иначе агрегат из четырёх пачек будет несопоставим:
- **`1/кадр` против `2`:** правка убирает / разбивает / переставляет содержание → ярус 1; правка переписывает / перерисовывает / переоформляет тот же объём → ярус 2. Посторонний артефакт производства (служебная пометка, пустая полоса) убирается, но идёт в 2 — убираемое не является порцией.
- **`3` против всего остального:** ярус 3 — только когда без числа от замечания не остаётся ничего. Замечание выживает без числа → ярус по механизму, а число всё равно уходит в `verdikt-chisla.tsv`.
- Спорность — в четвёртое поле префиксом `СПОРНО:`, наблюдения — `ЗАМЕТКА:` (причина — ниже, пункт «а»).
- Материя ставится по механизму, а не по слову: «картинка» в цитате ≠ материя `рисунок`.

Где разойдусь с пачкой 1 — скажу в разборе поимённо, а не молча.

Порядок:
1. Читать скелет своего диапазона блоками по источнику; `KORPUS-VLADELCA.md` открывать точечно по id, когда обрезанной цитаты не хватило.
2. Ярус — по МЕХАНИЗМУ жалобы; материя только для яруса 1.
3. 68 записей с числом — второй файл: масштаб строго из источника, форма `ориентир` по умолчанию.
4. Дописывать вердикты в конец после каждого источника; после третьего — прогон сборщика с чтением претензий.
5. Оба скрипта до зелёного по моим id → верификатор (1 субагент, 3 задачи) → `## РАЗБОР` → отчёт → коммит зоны.

**Оспариваю предпосылки — до работы, как требует §1. Две подтверждаю за пачкой 1, третью добавляю своей.**

**(а) Конфликт критериев №1 и №4 подтверждаю по коду, независимо.** `sobrat_razmetku.py` валидирует ярус словарём `YARUSY` (строки 28, 68–69) и материю `MATERII` (строки 30, 70–71), поэтому предписанный заходом `1?` даёт `неизвестный ярус` → exit 1; а счётчик спорных в том же скрипте ищет ровно `y.endswith("?") or "?" in m` (строка 87). Пометить по инструкции = провалить критерий 1. Беру канал пачки 1 (`СПОРНО:` в четвёртом поле): не потому, что он лучше, а потому, что **два разных канала пометки в одном файле хуже одного неидеального** — судья не сможет посчитать спорных одной командой.

**(б) Сборщик снесёт разбор пачки 1 — и это уже не предсказание, а моя обязанность.** `POKRYTIE-karkasa.md` и `CHISLA-s-kontekstom.md` пишутся через `write_text` целиком (строки 92, 118, 150). В `POKRYTIE-karkasa.md` сейчас лежит разбор пачки 1 (132 строки, 31 КБ), в `CHISLA-s-kontekstom.md` — её `## КОНФЛИКТУЮЩИЕ` (69 строк, 9 КБ). **Мой первый же прогон уничтожил бы оба.** Поэтому ПЕРВЫМ ходом, до всякого прогона, оба раздела сохранены в `/tmp/h2b/` вместе с полными копиями четырёх файлов зоны; после прогонов вклеиваю разбор пачки 1 обратно и дописываю свой ПОД ним. Пачка 1 назвала эту ловушку в уроках фабрике — я её первый потенциальный потерпевший, и предупреждение сработало.

**(в) Своё, чего пачка 1 не называла: критерий готовности №1 требует «все 1379 id имеют вердикт», и для пачки из 345 он невыполним в принципе.** Заход это признаёт («сборщик до последней пачки будет краснеть — это НОРМА»), но формулировка критерия осталась от неразрезанного захода. Читаю критерий по своему диапазону, как велит §2, и проверяю машинно: грепаю вывод сборщика по маске своих id и предъявляю, что претензий к V0346–V0690 нет. **Красный `rc=1` у сборщика в моём отчёте — ожидаемый, а не скрытый брак.**

## ВОПРОСЫ — (заполняет исполнитель)

Работал ночью без человека. Четыре развилки решил сам, каждую с объяснением; три вопроса оставляю владельцу и судье.

**Решённые развилки (что выбрал и почему):**

1. **Пометка спорных.** Инструкция велит `1?`, скрипт такой ярус отвергает (см. `## ПЛАН`, пункт «а», проверено прогоном на копии скрипта). Выбрал: спорность в четвёртое поле префиксом `СПОРНО:`. Критерий 1 (зелёный скрипт) при этом выполняется, критерий 4 (спорные названы числом и разобраны) — тоже, но число считается грепом, а не скриптом, и в собранном `POKRYTIE-karkasa.md` про ложность строки «Помечено спорными: 0» сказано прямо.
2. **Разбор в двух местах.** Скрипт перезаписывает `POKRYTIE-karkasa.md` целиком на каждом прогоне и снёс мой `## РАЗБОР` ровно так, как я и предсказал в плане. Выбрал: разбор живёт в `POKRYTIE-karkasa.md` (как велит заход), а перед каждым прогоном сохраняется в `/tmp` и вклеивается обратно. **Следующей пачке этого хватит не будет:** её прогон снесёт мой разбор молча. Способ вклейки описан в отчёте ниже, чтобы его повторили.
3. **`_studio/docs/KARTA.md` в коммит НЕ включил.** §4 захода велит коммитить его вместе с зоной, но это условно: правило 🔴 говорит «звал `register_doc.py` — допиши KARTA.md к своим путям». Я `register_doc.py` не звал: ни одного нового `.md` не заводил, все пять файлов зоны уже существовали и зарегистрированы. При этом `KARTA.md` в рабочем дереве **изменён чужой правкой** (`git diff --stat` → 1 строка, дифф 90 КБ, я его не делал). Слепое исполнение §4 подмело бы чужое изменение в мой коммит — то самое, от чего §4 и защищает. Выбрал: коммитить ровно шесть своих путей.
4. **Правил свои уже дописанные строки вердикта.** Заход говорит «ничего в нём не переписывай». По находкам верификатора я всё же изменил три строки — `V0288`, `V0060`, `V0052`, все внутри моего диапазона. Обоснование: запрет стоит в разделе про диапазон, и причина названа там же — «соседние пачки делают другие сессии, их вердикты трогать нельзя». Чужих строк я не касался; замены сделаны якорно, с abort при числе совпадений ≠ 1 (в логе видно «три якорные замены применены, каждая по одному совпадению»). Альтернатива — дописать исправленный дубль в конец — дала бы дубль id, на котором скрипт краснеет.

**Вопросы, которые решать не мне:**

5. **Ярус 3 — оставлять ли ярусом?** Из 80 записей с названным числом ярус 3 получили 12; у 68 число названо внутри жалобы другого механизма. Из 43 спорных 8 — это ровно граница «3 против другого яруса». Предложение и его цена — `POKRYTIE-karkasa.md § РАЗБОР, п. 4`. Решение за судьёй: ярусы 0–2 названы по механизму, ярус 3 — по форме записи, и как значения одного поля они несовместимы.
6. **Куда девать одобрения владельца?** Три записи не легли только потому, что все ярусы §3 определены через механизм ЖАЛОБЫ, а «отличная вещь» жалобы не несёт (`п. 2` разбора). Каркасу, похоже, не хватает не яруса, а поля «знак: жалоба или образец». Сам каркас я не правил — границы захода.
7. **Столкновение внутри одного яруса каркас не разрешает.** Сцены: «накопление, ничего не пропадает» (`V0174`, `V0190`, `V0198`) против «пусть моргнут и пропадут» (`V0289`). Ярус у всех четырёх один — `1/время`, а §4 задаёт ранг только МЕЖДУ ярусами. Не примирял: это решение владельца, и это готовая первая строка журнала развилок.

### Пачка V0346–V0690 (вторая сессия, 31.07)

Работал ночью без человека. Шесть развилок решил сам, каждую с объяснением; шесть вопросов оставляю владельцу и судье.

**Решённые развилки (что выбрал и почему):**

1. **Пометка спорных — взял канал пачки 1, а не свой.** Конфликт критериев №1 и №4 подтвердил по коду независимо (см. `## ПЛАН`, пункт «а»). Мог бы придумать другую пометку, но выбрал ровно `СПОРНО:` в четвёртом поле — **не потому, что он лучший, а потому, что два разных канала пометки в одном файле хуже одного неидеального:** судья не сможет посчитать спорных по всему корпусу одной командой. Своих спорных 59, и в собранном `POKRYTIE-karkasa.md` про ложность строки «Помечено спорными: 0» сказано в шапке моего разбора.

2. **Спас чужой разбор до первого прогона — я оказался первым потерпевшим от ловушки, которую назвала пачка 1.** Её урок фабрике («сборщик молча съедает разделы») предупредил ровно про мой случай: в `POKRYTIE-karkasa.md` лежал её разбор на 31 КБ, в `CHISLA-s-kontekstom.md` — её `## КОНФЛИКТУЮЩИЕ` на 9 КБ, и мой первый же прогон уничтожил бы оба. **Сделал первым ходом, до любого прогона:** оба раздела плюс полные копии четырёх файлов зоны — в `/tmp/h2b/`. После сборки вклеил обратно и **проверил побайтово** (`diff` против бэкапа → совпадает). Предупреждение пачки 1 сработало ровно так, как задумано, — и это единственная причина, по которой её разбор ещё существует.

3. **Гейты гонял на КОПИИ скрипта** в `/tmp/gate/` с симлинками на настоящие `skelet-*` и `verdikt-*`, а настоящий прогон сделал ОДИН раз, в конце, перед вклейкой. Причина та же: каждый настоящий прогон стирает оба разбора. Проверка при этом честная — скрипт тот же и читает те же файлы вердиктов.

4. **`_studio/docs/KARTA.md` в коммит НЕ включил** — то же решение, что у пачки 1, и с более твёрдым доказательством. `register_doc.py` я не звал: нового `.md` не заводил, все шесть файлов зоны уже существовали. При этом `KARTA.md` в дереве изменён, и правка **не моя**: последний коммит по нему — `77790cd` «разметка корпуса исполнителей по девяти осям», то есть **параллельный заход `kod_razmetka-osej.md`**. Слепое исполнение §4 подмело бы в мой коммит работу соседней сессии.

5. **Правил пять своих уже дописанных строк вердикта** (`V0387`, `V0405`, `V0483`, `V0497`, `V0642`) по находкам верификатора. Все пять — внутри моего диапазона, чужих строк не касался. Замены якорные, с abort при числе совпадений ≠ 1 (в логе: «5 якорные замены применены, каждая по одному совпадению»). Альтернатива — дописать исправленный дубль в конец — дала бы дубль id, на котором скрипт краснеет.

6. **Границу «ярус 0 против яруса 1» провёл сам, потому что каркас её не проводит.** Правило: *решается, ЧТО входит в курс или лекцию → ярус 0; решается, В КАКОМ ПОРЯДКЕ подавать уже взятое → ярус 1.* На нём держатся все 74 записи яруса 0. Выписал его в разбор (`п. 3г`) с ценой — 16 спорных из 59 — именно чтобы судья мог его отменить. **Признаю дефект дисциплины, найденный верификатором:** в двух строках (`V0387`, `V0405`) правило сработало молча, без пометки; обе теперь помечены.

**Вопросы, которые решать не мне:**

7. **Материи `§3` описывают только ДЕК. Расширять или сузить аксиому?** 44 записи моего диапазона (13 %) — про **листок задач** и **занятие целиком**, и девяти из них, получившим ярус 1, пришлось выдать материю из словаря дека: листок не `кадр` (он не показывается за раз), занятие не `время` в смысле «порция на клик». Развилка честная: либо материи расширяются до сред «дек · листок · занятие», либо ярус 1 объявляется аксиомой ДЕКА, и листку нужен свой ярус. **Считаю первое верным** — механизм в листке буквально тот же («задач столько, чтобы порешали», «не вываливать всё сразу»), и это ровно та логика, которой владелец завёл материю `доказательство`. Но решает он. Разбор — `POKRYTIE-karkasa.md § РАЗБОР, п. 5`.

8. **Каркасу не хватает яруса «математическая состоятельность» — и это предсказал сам каркас.** Четыре записи не легли потому, что владелец судил, ВЕРНА и ЗАВЕРШЕНА ли математика («теорема доказана для второй конструкции, первая прикручена без связи»), а все ярусы `§3` описывают ПОДАЧУ. `§1` каркаса это предсказал дословно и назвал цену: «первый же курс, где содержание надо строить с нуля, вскрыл бы это на живой лекции». **Мой диапазон и содержит этот курс** (`catalan`, статья «Мир 2»), и вскрылось 4 записи из 24 одного источника — 17 %. Предсказание подтвердилось на данных. Разбор — `п. 2, класс А`.

9. **Ярус 0 знает ЗАЛ, но не знает ЛЕКТОРА.** `V0654`: «Дело не в том, что зал знает профессионально. Дело в том, что **я ничего перечисленного не знаю**». `§3` перечисляет «кто в зале и на кого рассчитывать» — про того, кто на сцене, нет ничего, а ограничение жёстче зального: зал можно доучить слайдом, лектора нельзя. Нужно ли ярусу 0 второе лицо — решение владельца. Разбор — `п. 8б`.

10. **Самая резкая развилка корпуса: «слайд = ОДИН экран» против «три сцены на одном слайде законны», и обе стороны почти стали гейтом.** `V0427`/`V0429` (слайд = экран, и нарушение считается в минутах) против `V0394`/`V0395`/`V0400` (три сцены/экрана на слайде — правильно), **всё внутри одного источника**. Не примирял. Если завести «слайд = экран» гейтом, он покраснеет на трёх слайдах, которые владелец назвал правильными, — это буквально сценарий `Р31`. Выписано в `CHISLA-s-kontekstom.md § КОНФЛИКТУЮЩИЕ, п. 1`.

11. **Уточнение к предложению пачки 1: каркасу нужно поле не «знак», а «признак назван / не назван».** Пачка 1 нашла «одобрения без признака» и предложила поле «знак: жалоба или образец». Я нашёл зеркало — **порицания без признака** (`V0366` «нужно кардинальное изменение», `V0381` «опять трэш»), а порицание это жалоба, и жалобы у пачки 1 ложились всегда. Значит дело не в знаке, а в том, назван ли признак: одобрение С признаком ярус получает нормально (`V0370`, `V0412`, `V0606`, `V0479`). Разбор — `п. 2, класс В`.

12. **Форма записи ориентира не вмещает УСЛОВИЕ, а без условия число врёт.** `§3` задаёт ориентиру четыре поля, и «если руками» из `V0677` («75 минут разбора держатся, ЕСЛИ руками» — против `V0607` «больше получаса не держат») приходится прятать прозой в масштаб. Две развилки из шести у меня именно такие, и поля для условия нет ни у одной. Плюс словарь `FORMY` не различает замеры, которые ориентирами продукта не являются вовсе (`V0428`, `V0577`, `V0608`, `V0655`, `V0667`) — своего значения не выдумывал, заход это запрещает. Разбор — `п. 8а` и `п. 9`.

## ОТЧЁТ — (заполняет исполнитель)

**Пачка сделана целиком: 345 из 345 записей диапазона V0001–V0345 (25 % корпуса).** Проверяется командой:
`awk -F'\t' '$1>="V0001" && $1<="V0345"' verdikt-vladelca.tsv | wc -l` → 345 · `... verdikt-chisla.tsv | wc -l` → 80.

### Что сделал и зачем

1. **`verdikt-vladelca.tsv` +345 строк** — ярус каждой записи по МЕХАНИЗМУ жалобы, материя для яруса 1. Дописывал пачками по источнику (их у меня 5 из 34), после третьего прервался и прогнал `sobrat_razmetku.py vladelca`, как велит заход, — единственная его претензия была «без вердикта осталось 1156», то есть чужие пачки. Зачем именно так: адрес и заголовок через модель не проходят вообще, скрипт берёт их из скелета по id.
2. **`verdikt-chisla.tsv` +80 строк** — масштаб применимости и форма. Масштаб писал из источника (жанр артефакта, номер слайда, длительность лекции, тип аудитории там, где источник её называет); чего в источнике нет, не писал. Формы: `ориентир` 75, `WARN` 5, `гейт` 0. `WARN` — только там, где жёсткую границу назвал сам владелец, и его слова стоят в поле масштаба (`V0116`, `V0227`, `V0230`, `V0238`, `V0264`).
3. **`## РАЗБОР` в `POKRYTIE-karkasa.md`** — все четыре обязательных пункта плюс три раздела, которых заход не требовал (столкновение сцен, кратность режет материю, находки верификатора). Это главный артефакт: он отвечает на вопрос, держится ли каркас.
4. **`## КОНФЛИКТУЮЩИЕ` в `CHISLA-s-kontekstom.md`** — шесть развилок, обе стороны выписаны, ни одна не примирена.

### Итог по ярусам (моя пачка, 345 записей)

| ярус | записей | доля пачки |
|---|---|---|
| 1 · порционность | 188 | 54,5 % |
| 2 · читаемость | 104 | 30,1 % |
| процесс | 27 | 7,8 % |
| 3 · ориентир | 13 | 3,8 % |
| 0 · замысел | 8 | 2,3 % |
| **не ложится** | **5** | **1,4 %** |

Материи яруса 1: кадр 75 · доказательство 53 · время 36 · рисунок 24. Спорных 43 (12,5 %), заметок 6.

**Отрицательный вердикт с охватом в себе, как требует заход:** *на ярусы легло 340 записей из 345, не легло 5, и ни одна из пяти не не легла по содержательной причине — это дефекты корпуса (служебный разделитель, обрезанная в источнике реплика) и одобрения без названного признака.* **Каркас на этой четверти корпуса держится.** Его слабое место не в покрытии, а в двух границах, которых он не проводит: `1/кадр` против `2` и `3` против всего остального — обе разобраны числами в `## РАЗБОР`.

**Гипотеза аналитика по группам разошлась с фактом по записям в трёх местах,** и все три — не про каркас, а про отнесение групп к этажам: хвост «161 не легло» ложится на ярусы на 87 % (гр. 8 «стилистика» — дословный ярус 2 из §3); из 22 записей «процессных» групп 18 оказались продуктом (гр. 13 «где вести записи» — все 7 её записей моего диапазона про картинки и кадры); и наоборот, 21 запись из продуктовых групп по механизму про фабрику, образуя класс «задуманное не доехало до дека», которого в каркасе нет.

**Замер главной ловушки захода.** Из 345 записей 106 (31 %) получили ярус, отличный от главного яруса своей группы; из них 32 (9 % пачки) содержательно к теме своей группы не относятся — почти все по совпадению слова («все 12 **вопросов** проверить перед сборкой» в группе «сначала вопрос залу»; «**задача** — сделать 32 слайда» в группе «сколько **задач** в листке»). Все 32 названы поимённо в разборе. **Разметка 56 групп целиком дала бы неверный ярус минимум каждой девятой записи.**

### Как проверил

- **Оба гейта зелены по моим id.** `sobrat_razmetku.py vladelca` и `chisla` дают `rc=1`, но единственная претензия у каждого — «без вердикта осталось N записей» про пачки V0346+, что заход прямо называет нормой. Проверил это машинно, а не на глаз: грепнул вывод обоих скриптов по маске моих id — **ни одной претензии к V0001–V0345** (нет неизвестных ярусов, нет яруса 1 без материи, нет «не ложится» без причины, нет дублей id, нет куцего масштаба).
- **Гейты гонял на КОПИИ скрипта** в `/tmp/gate/` со симлинками на настоящие `skelet-*`/`verdikt-*`. Причина: настоящий прогон перезаписывает `POKRYTIE-karkasa.md` и `CHISLA-s-kontekstom.md` целиком и снёс бы разбор. Проверка при этом честная — скрипт тот же и читает те же файлы вердиктов.
- **Числа корпуса снял сам до работы:** 1379 записей · 269 с числом · 34 источника — совпало с базой аналитика, расхождений нет.
- **Утверждение «скрипт отвергает `1?`» проверено прогоном, а не чтением кода:** оба варианта пометки (`1?` в ярусе и `кадр?` в материи) дали `❌ БРАК`; вариант с `СПОРНО:` в четвёртом поле прошёл чисто.
- **Верификатор** — один свежий субагент с пустым контекстом, три задачи: **12 верных · 2 спорных · 1 неверный из 15** при пороге непринятия 3. **Разметка принята.** Неверность одна и на уровне материи при верном ярусе (`V0288`: `рисунок` → `время`). Масштабы чисел: 5 из 5 следуют из источника, додуманных 0; он независимо пересчитал, что в `VOPROSY-bot.md` действительно 12 вопросов. Следа группового мышления как режима не нашёл. Две его находки я починил, третью пометил спорной, одну претензию не принял с обоснованием — всё в `## РАЗБОР, п. 7`.

### Чего НЕ трогал

- `KORPUS-VLADELCA.md`, `KRATNOST-vladelca.md`, `REESTR-vladelca.md`, `AKSIOMATIKA-karkas.md`, `skelet-*.tsv`, `sobrat_razmetku.py` — только на чтение. Возражения каркасу написаны в свой третий артефакт и в `## ВОПРОСЫ`, сам каркас не правлен.
- **Корпус исполнителей** (`KORPUS-*.md` без `VLADELCA`), `RAZMETKA-OSEJ.md`, `DYRY-po-osyam.md` — зона параллельного захода, не открывал и не размечал.
- **Строки вердиктов вне моего диапазона** — в файле их нет вообще (пачки V0346+ ещё не сделаны); я писал только дописыванием в конец, кроме трёх якорных правок своих же строк (см. `## ВОПРОСЫ`, п. 4).
- **`_studio/docs/KARTA.md`** — не правил и в коммит не включил, хотя §4 его перечисляет. Причина в `## ВОПРОСЫ`, п. 3: нового `.md` я не заводил, а файл изменён чужой правкой.
- Правил не формулировал, вердиктов `→ гейт` / `→ канон` не ставил, гейтов не заводил — это следующий заход.

### Найдено вне зоны (не трогал, отдаю в отчёт)

1. **`sobrat_razmetku.py` (не моя зона) отвергает пометку спорности, которую сам же считает.** Строка «Помечено спорными: N» будет печатать 0 у всех четырёх пачек, если исполнители пишут по инструкции.
2. **`sobrat_razmetku.py` перезаписывает `POKRYTIE-karkasa.md` и `CHISLA-s-kontekstom.md` целиком** — вместе с разделами `## РАЗБОР` и `## КОНФЛИКТУЮЩИЕ`, которые эти же файлы обязаны нести от заходов. Пачка нарезана на четыре сессии ⇒ разбор пачки 1 умрёт при первом прогоне пачки 2, и в выводе скрипта об этом не будет ни слова. **Рецепт для следующей пачки:** перед прогоном вырезать свой раздел от маркера `## РАЗБОР — заполняет заход` в `/tmp`, прогнать скрипт, вклеить обратно — я так и делал.
3. **`KORPUS-VLADELCA.md` содержит одну запись, которая не является замечанием** — служебный разделитель добычи `V0095`. Во всём корпусе он один (`grep -c '^### — — —'` → 1), но id получил наравне с замечаниями.

### Необратимое

- **Изменил три своих строки в `verdikt-vladelca.tsv`** (`V0288`, `V0060`, `V0052`) вместо дописывания — правка якорная, каждая с проверкой «ровно одно совпадение». **Восстанавливается:** предыдущее состояние файла — в коммите-предке этого коммита; исходный вид строк был `V0288<TAB>1<TAB>рисунок`, `V0060<TAB>1<TAB>рисунок`, `V0052<TAB>1<TAB>доказательство`.
- **Перезаписал `RAZMETKA-VLADELCA.md`, `POKRYTIE-karkasa.md`, `CHISLA-s-kontekstom.md`** — это делает `sobrat_razmetku.py` по построению, файлы для этого и заведены (в HEAD они были заглушками «Сейчас пуст — ночной заход ещё не отработал»). **Восстанавливается:** пересборкой из вердиктов той же командой; заглушки — из коммита-предка.
- Больше необратимого нет: файлов не удалял, не переименовывал, не перемещал, `git reset`/`checkout` не делал, за пределы зоны не выходил.

### Время и токены

Снимает приёмка из `result`-строки лога прогона — исполнителю счётчик недоступен.

**КОММИТ:** `7519478` — `razmetka-vladelca: пачка V0001–V0345 размечена по ярусам (345/345), 80 чисел с масштабом, разбор каркаса` · 6 files changed, 2429 insertions(+), 9 deletions(-) · `git show --stat` → в коммите ровно мои шесть путей, чужого нет · `git status --porcelain` по моим шести путям → **пусто, зона чиста**.

⚠ **`git_zona.py check --zone _studio/zhurnal/2026-07-30_dovodka-fabriki` краснеет, и это НЕ мой брак — гейт проверяет всю папку арки, а не мой контракт зоны.** Красное на трёх путях, ни один из которых в моём списке зоны не значится:
- `noch-logi-2/H2a.err` и `noch-logi-2/H2a.jsonl` — untracked логи **моего же прогона**, которые пишет запускающий скрипт `noch-2.sh`; `H2a.jsonl` был 2,1 МБ и продолжал расти в момент проверки. Коммитить их я не могу (не моя зона и файл пишется на ходу) — это решение владельца или орхестратора: либо в `.gitignore`, либо коммит логов отдельным ходом после ночи;
- `ITOG-nochi-2.md` — правлен не мной (последний коммит по нему — `7812aaf`, орхестратор ночи).

Ничего из этих трёх я не трогал и в коммит не включал.

### Пачка V0346–V0690 (вторая сессия, 31.07)

**Пачка сделана целиком: 345 из 345 записей диапазона V0346–V0690 (25 % корпуса), 68 из 68 чисел.** Проверяется командой:
`awk -F'\t' '$1>="V0346" && $1<="V0690"' verdikt-vladelca.tsv | wc -l` → 345 · `... verdikt-chisla.tsv | wc -l` → 68.

#### Что сделал и зачем

1. **`verdikt-vladelca.tsv` +345 строк** — ярус каждой записи по МЕХАНИЗМУ жалобы, материя для яруса 1. Дописывал пачками по источнику (их у меня 14 из 34), после шестого прервался и прогнал сборщик, как велит заход. Зачем именно так: адрес и заголовок через модель не проходят вообще, скрипт берёт их из скелета по id.
2. **`verdikt-chisla.tsv` +68 строк** — масштаб применимости и форма. Масштаб писал только из источника; формы: `ориентир` 66, `WARN` 2, `гейт` 0. `WARN` — только там, где жёсткую границу назвал сам владелец ДОСЛОВНО (`V0352` «Это жёсткое условие», `V0426` «у нас довольно жёсткий тайминг»).
3. **`## РАЗБОР` в `POKRYTIE-karkasa.md`** — четыре обязательных пункта плюс шесть, которых заход не требовал (материи листка, дубли добытчика, пересказ против цитаты, два новых столкновения, находки верификатора). Это главный артефакт.
4. **`## КОНФЛИКТУЮЩИЕ` в `CHISLA-s-kontekstom.md`** — шесть развилок с датами, ни одна не примирена; две из шести снимаются условием, которое назвал сам владелец, и это сказано отдельно.
5. **Разбор и развилки пачки 1 сохранены и вклеены обратно** — иначе сборщик снёс бы их молча (см. `## ВОПРОСЫ`, п. 2).

#### Итог по ярусам (моя пачка, 345 записей)

| ярус | записей | доля пачки | у пачки 1 |
|---|---|---|---|
| **процесс** | **112** | **32,5 %** | 7,8 % |
| 1 · порционность | 92 | 26,7 % | 54,5 % |
| **0 · замысел** | **74** | **21,4 %** | 2,3 % |
| 2 · читаемость | 47 | 13,6 % | 30,1 % |
| 3 · ориентир | 11 | 3,2 % | 3,8 % |
| **не ложится** | **9** | **2,6 %** | 1,4 % |

Материи яруса 1: кадр 39 · доказательство 33 · время 17 · рисунок 3. Спорных 59 (17,1 %), заметок 27.

**Отрицательный вердикт с охватом в себе, как требует заход:** *на ярусы легло 336 записей из 345, не легло 9 — четыре по мат. состоятельности, две как мат. рефлексия без предмета правки, три как порицание без названного признака.* **Каркас на этой четверти держится**, но его дыра НЕ та, что у пачки 1: у неё не легли одобрения, у меня — суждения о самой математике, и второе предсказано `§1` каркаса вместе с ценой.

**Главное, что даёт эта пачка сверх своей четверти — сопоставление с пачкой 1 на одной рамке.** Четыре замера воспроизвелись почти точно (число внутри чужой жалобы: 84 % против 85 %; ярус 3 почти тождествен полю `ЧИСЛО:`; хвост «161 не легло» ложится; доля спорных на границе яруса 3: 22 % против 19 %) — это уже свойства корпуса, а не мнение разметчика. А раскладка ярусов разошлась в 9 раз по ярусу 0 и в 4 по процессу, и причина названа числом: **53 % моих записей — дневники сессий фабрики, а не разборы дека.** ⇒ Доля яруса — функция источника; ожидание `§5.1` каркаса («ярусы 1 и 2 возьмут большую часть, ярус 0 — десятки») на моей четверти опровергнуто, и `§5.1` сам называет это законным результатом.

**Замер главной ловушки: 91 запись из 345 (26 %)** содержательно не относится к теме своей группы — против 32 (9 %) у пачки 1. Все 91 названы поимённо в разборе. Механизм — совпадение подстроки: «сейчас/потом/часть» тянет в группу «длительность и тайминг», «центр» — в «компоновку кадра», «список» — в «маркированный список». Верификатор считал две группы независимо и получил тот же порядок, добавив мне два залёта, которых я не увидел. **Разметка 56 групп целиком дала бы неверный ярус минимум каждой четвёртой записи моей четверти.** Оба примера шума из `§5.2` каркаса (`V0572`, `V0588`) лежат в моём диапазоне и оба подтверждены.

#### Как проверил

- **Оба гейта зелены по моим id.** `sobrat_razmetku.py vladelca` и `chisla` дают `rc=1`, но единственная претензия у каждого — «без вердикта осталось N записей» про пачки V0691+, что заход прямо называет нормой. Проверил машинно, а не на глаз: грепнул вывод обоих скриптов по маске своих id — **ни одной претензии к V0346–V0690** (нет неизвестных ярусов, нет яруса 1 без материи, нет «не ложится» без причины, нет дублей id, нет куцего масштаба).
- **Код возврата читал первым**, до содержательного вывода, на каждом прогоне.
- **Числа корпуса снял сам до работы:** 1379 записей · 269 с числом · 34 источника — совпало и с базой аналитика, и с замером пачки 1.
- **Конфликт критериев №1/№4 проверил по коду независимо** от пачки 1 (строки 28–30, 68–71, 87), а не принял на слово.
- **Целость чужого разбора проверил `diff` против бэкапа, снятого ДО первого прогона** — совпадение побайтовое, для обоих файлов.
- **Дубли внутри источника проверил полем `АДРЕС`**, а не на глаз: девять пар `V0441`–`V0452` против `V0455`–`V0464`, у вторых адрес «Приложение — сырой текст замечаний владельца». Всем девяти дал одинаковый ярус — девять пар, девять совпадений, бесплатная проверка самосогласованности.
- **Верификатор** — один свежий субагент с пустым контекстом, три задачи: **9 верных · 6 спорных · 0 НЕВЕРНЫХ из 15** при пороге непринятия 3. **Разметка принята.** Ошибок материи ноль. Масштабы чисел: 5 из 5 следуют из источника, додуманных 0 (проверял по самим файлам-источникам). Следа группового мышления не нашёл ни в одной из двух групп, и обосновал сильнее пересчёта: 24 записи группы «тайминг» получили 6 разных значений ярус·материя, 21 запись «компоновки кадра» — 8 разных, максимальная доля одного значения 19 %. **Пять его находок починил, две передал судье как дефекты формы, не свой брак** — всё в `## РАЗБОР, п. 9`.

#### Чего НЕ трогал

- `KORPUS-VLADELCA.md`, `KRATNOST-vladelca.md`, `REESTR-vladelca.md`, `AKSIOMATIKA-karkas.md`, `skelet-*.tsv`, `sobrat_razmetku.py` — только на чтение. Возражения каркасу написаны в третий артефакт и в `## ВОПРОСЫ`, сам каркас не правлен.
- **Корпус исполнителей** (`KORPUS-*.md` без `VLADELCA`), `RAZMETKA-OSEJ.md`, `DYRY-po-osyam.md`, `verdikt-osej.tsv` — зона параллельного захода, не открывал и не размечал.
- **Строки вердиктов пачки 1** (`V0001`–`V0345`) — не касался: писал дописыванием в конец, а пять якорных правок сделаны только по своим id. Проверяется тем, что `diff` разбора пачки 1 против бэкапа чист.
- **`_studio/docs/KARTA.md`** — не правил и в коммит не включил, хотя §4 его перечисляет. Причина в `## ВОПРОСЫ`, п. 4: нового `.md` не заводил, а файл изменён параллельным заходом.
- Правил не формулировал, вердиктов `→ гейт` / `→ канон` не ставил, гейтов не заводил — это следующий заход.

#### Найдено вне зоны (не трогал, отдаю в отчёт)

1. **`sobrat_razmetku.py` отвергает пометку спорности, которую сам же считает** — подтверждаю находку пачки 1 независимой проверкой по коду. Строка «Помечено спорными: 0» ложна у обеих пачек: фактически 43 + 59 = 102 спорных на 690 размеченных записей.
2. **`sobrat_razmetku.py` перезаписывает `POKRYTIE-karkasa.md` и `CHISLA-s-kontekstom.md` целиком.** Пачка 1 предсказала, что её разбор умрёт при первом прогоне пачки 2 — **предсказание верное, я это наблюдал.** Рецепт, который сработал и которым обязана воспользоваться пачка 3: сохранить в `/tmp` ВСЁ от маркера `## РАЗБОР — заполняет заход` (и `## КОНФЛИКТУЮЩИЕ — заполняет заход`) ДО первого прогона, после сборки вклеить обратно и сверить `diff`. К пачке 3 разделов будет уже два, к пачке 4 — три; **риск растёт с каждой пачкой, а предупреждения в выводе скрипта по-прежнему нет.**
3. **Корпус содержит девять пар механических дублей в одном источнике** (`zamechaniya-statya-mir2.md` внесён дважды — из разделов «Замечание N» и из своего же приложения «сырой текст»). Кратность этих девяти замечаний завышена ровно вдвое, и это 38 % объёма источника.
4. **48 записей моего диапазона (14 %) несут `ПЕРЕСКАЗ` аналитика вместо цитаты владельца** — против 4 у пачки 1. Для 14 записей с числом из 68 (21 %) это делает правило захода «`WARN` только если владелец сам назвал границу жёсткой» **неприменимым в принципе**: его слов в записи нет.
5. **9 записей несут цитату, обрезанную в самом корпусе** (`V0376` `V0377` `V0410` `V0417` `V0426` `V0428` `V0429` `V0430` `V0431` — открывающая кавычка без закрывающей); по всему корпусу таких 31 из 1379. Судил их по заголовку записи.
6. **Словарь `FORMY` (`ориентир` · `WARN` · `гейт`) не различает замеры, которые ориентирами продукта не являются вовсе** — `V0428`, `V0577`, `V0608`, `V0655`, `V0667` лежат в реестре чисел как ориентиры, хотя измеряют канал интервью, объём скачанного и известность подборки. Своего значения не выдумывал: заход это запрещает.

#### Необратимое

- **Изменил пять своих строк в `verdikt-vladelca.tsv`** (`V0387`, `V0405`, `V0483`, `V0497`, `V0642`) вместо дописывания — правки якорные, каждая с abort при числе совпадений ≠ 1. **Восстанавливается:** предыдущее состояние файла — в коммите-предке этого коммита; плюс копия файла до всех моих правок лежит в `/tmp/h2b/verdikt-vladelca.tsv`. Исходный вид: `V0387→1/доказательство` и `V0405→1/кадр` и `V0483→процесс/—` были без четвёртого поля, `V0497` несла `ЗАМЕТКА:`, `V0642` несла второго кандидата «1/кадр» вместо `1/время`.
- **Изменил одну свою строку в `verdikt-chisla.tsv`** (`V0539`: «того же курса» → «курса „Числа Фибоначчи“») — якорно, одно совпадение. **Восстанавливается** оттуда же.
- **Перезаписал `RAZMETKA-VLADELCA.md`, `POKRYTIE-karkasa.md`, `CHISLA-s-kontekstom.md`** — это делает `sobrat_razmetku.py` по построению. **Разбор и развилки пачки 1 при этом НЕ потеряны:** сняты в `/tmp/h2b/` до первого прогона, вклеены обратно, совпадение с бэкапом проверено `diff` побайтово. **Восстанавливается:** пересборкой из вердиктов плюс вклейкой из `/tmp/h2b/RAZBOR-pachka1.md` и `/tmp/h2b/KONFLIKT-pachka1.md`; они же есть в коммите `7519478`.
- Больше необратимого нет: файлов не удалял, не переименовывал, не перемещал, `git reset` / `checkout` не делал, за пределы зоны не выходил, ветку не переключал.

#### Время и токены

Снимает приёмка из `result`-строки лога прогона — исполнителю счётчик недоступен.

**КОММИТ:** см. строку ниже, дописана после коммита.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель)

### Гейт захода отвергает пометку, которую сам же считает: критерии готовности №1 и №4 несовместимы
ЦЕНА: `sobrat_razmetku.py` валидирует ярус по словарю `YARUSY` (строки 28, 68–69) и материю по `MATERII` (строки 30, 70–71), поэтому предписанный заходом `1?` даёт `неизвестный ярус` и exit 1 — а счётчик спорных в том же скрипте ищет ровно `y.endswith("?") or "?" in m` (строка 87). Исполнить инструкцию = провалить критерий 1; не исполнить = провалить критерий 4. Проверено прогоном, а не чтением. Стоило одного захода вхолостую на пробу и обходного канала (спорность в четвёртое поле); главная цена не у меня, а дальше: **строка «Помечено спорными: 0» в собранном `POKRYTIE-karkasa.md` ложна и останется ложной у всех четырёх пачек** — судья, читающий её как факт, получит ноль вместо 43 в моей пачке. Правило фабрики, которое здесь нарушено: гейт обязан краснеть на браке, а не на предписанной разметке.

### Сборщик молча съедает разделы, которые заход обязан дописать в собираемый им файл
ЦЕНА: `sobrat_razmetku.py` пишет `POKRYTIE-karkasa.md` и `CHISLA-s-kontekstom.md` через `write_text` целиком (строки 92, 118, 150), а `## РАЗБОР` и `## КОНФЛИКТУЮЩИЕ` заход дописывает в те же файлы. Мой разбор (16 КБ, главный артефакт захода по прямому тексту задания — «он важнее первых двух») был снесён первым же повторным прогоном; восстановил из копии в `/tmp`, потерь нет. Но корпус нарезан на четыре пачки, и **разбор пачки 1 умрёт при первом прогоне пачки 2 — молча, без строки в выводе скрипта и без следа в git, если исполнитель пачки 2 не знает про эту ловушку.** Общее правило: инструмент, который перезаписывает файл целиком, не может быть единственным писателем файла, у которого есть второй писатель; либо раздел живёт в отдельном файле, либо сборщик его сохраняет.

### Заход велит коммитить `_studio/docs/KARTA.md` безусловно, а условие спрятано в другом пункте
ЦЕНА: §4 перечисляет `KARTA.md` в обеих командах коммита без оговорок, и только правило 🔴 выше говорит, что дописывать его надо, **если звал `register_doc.py`**. Я не звал — нового `.md` не заводил. При этом `KARTA.md` в дереве изменён ЧУЖОЙ правкой (`git diff --stat` → 1 строка, дифф 90 КБ). Слепое исполнение §4 подмело бы чужое изменение в мой коммит — ровно тот класс поломки, от которого §4 защищает своим «`-- <пути>` в самом `commit`», и оплачен он уже был 21.07 (коммит на 89 файлов вместо трёх). Здесь не сработало, потому что я проверил `git status` по путям зоны до коммита; но заход, читаемый буквально и быстро, ведёт в эту ошибку, а не мимо неё.


### Ловушка сборщика не починена, и её цена растёт с каждой пачкой — я был первым, кого спасло только чужое предупреждение
ЦЕНА: пачка 1 заплатила снесённым разбором (16 КБ, главный артефакт захода по прямому тексту задания) и восстановила его из `/tmp`. Я не заплатил ничего — но исключительно потому, что прочитал её урок и снял оба раздела в `/tmp/h2b/` ПЕРВЫМ ходом, до любого прогона. Проверил потом `diff`: сборщик действительно стёр 31 КБ разбора пачки 1 и 9 КБ её развилок, ровно как она предсказала. **Инструмент за сутки не изменился, предупреждения в выводе по-прежнему нет, а ставка выросла:** пачка 3 рискует уже двумя разделами (~57 КБ собранного разбора), пачка 4 — тремя. Механизм риска — асимметрия знания: чтобы не потерять чужую работу, исполнитель обязан ЗАРАНЕЕ знать, что сборщик пишет `write_text` целиком; в заходе этого нет, это есть только в уроке предыдущей пачки, а уроки читают не все. Правило фабрики, которое здесь нарушено: инструмент, перезаписывающий файл целиком, не может быть единственным писателем файла, у которого есть второй писатель — и пока это не починено, ловушка обязана орать в выводе, а не жить в чужом отчёте.

### Сборщик не умеет проверять ДИАПАЗОН, хотя корпус для него нарезан на диапазоны
ЦЕНА: критерий готовности №1 («все 1379 id имеют вердикт, иначе критерий провален») для пачки из 345 записей невыполним по построению — сборщик обязан краснеть до последней пачки, и заход это признаёт словами «это НОРМА, а не твой брак». Но инструмент отличить норму от брака не умеет, поэтому **каждая пачка изобретает собственную машинную проверку**: я грепал вывод обоих скриптов по маске своих id и предъявлял пустой результат; пачка 1 делала то же самое своими словами. Это два раза потраченная работа на доказательство ОТСУТСТВИЯ дефекта, и будет четыре. Хуже цена риска: приёмка, читающая `rc=1` буквально, завернёт зелёную работу, а исполнитель, прочитавший «это норма» буквально, может вообще не проверить свои id и сдать красное как нормальное — заход прямо велит «смотри только на претензии к ТВОИМ id», то есть требует ровно того, чего инструмент не делает. Правится одним аргументом `--diapazon V0346-V0690`, после которого код возврата становится честным.

### Правило «WARN только со слов владельца» неприменимо к 14 % корпуса, и в собранном реестре этого не видно
ЦЕНА: заход требует ставить `WARN`/`гейт` только там, где владелец сам назвал границу жёсткой, «и приведи его слова в поле масштаба». В моём диапазоне **48 записей из 345 (14 %) несут в поле `ЦИТАТА:` не реплику владельца, а `ПЕРЕСКАЗ` аналитика** — среди записей с числом это 14 из 68 (21 %). Для них вопрос «назвал ли он границу жёсткой» неразрешим: его слов в записи нет. Итог измерим: мой `WARN` — 2 из 68, и это **не осторожность разметчика, а предел материала**, но в собранном `CHISLA-s-kontekstom.md` колонки «цитата или пересказ» нет, поэтому судья прочитает 2 как суждение. У пачки 1 пересказов было 4 из 345, и она в это не упёрлась — то есть **дефект проявляется только на определённом типе источника и потому не был виден, когда правило писалось.** Дешёвая правка: сборщик знает поле `ЦИТАТА:` и может печатать признак «пересказ» в таблице чисел сам.

### Оценка шума группировки, снятая на одном типе источника, вдвое занижена для другого
ЦЕНА: и заход, и `§5.2` каркаса оценили шум `KRATNOST-vladelca.md` по выборке 7 групп × 5 записей («три из пяти в группе про цвет — не про цвет») и на этом построили запрет размечать группы целиком. Запрет верный, но его СИЛА измерена не там: пачка 1 получила 9 % залётов, я — **26 %**, втрое больше, и не потому, что размечал строже. Причина структурная: группы нарезаны по словарю ДЕКА («кадр», «слайд», «заголовок», «цвет»), а 53 % моего материала — дневники сессий фабрики, где те же слова («сейчас», «часть», «центр», «список», «явно») попадают в цитату по совершенно другому поводу. Одна подстрока «центр» втянула в группу «компоновка кадра» четыре записи, из которых ни одна не про кадр. **При 26 % залётов совет «открывай KRATNOST при сомнении как справку» вреден чаще, чем полезен** — справка, врущая в четверти случаев, хуже отсутствия справки, потому что она уверенно подсказывает неверный ярус там, где разметчик как раз сомневается. Правило фабрики: замер шума инструмента обязан нести рядом тип источника, на котором он снят, иначе следующий заход получит заниженное предупреждение — что и произошло со мной.
