# Канал исполнителя — razblokirovka (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — три точечные правки в известных местах, каждая названа файлом и строкой; решать по ходу нечего.

🔴 **ЭТОТ ЗАХОД ПЕРВЫЙ В ОЧЕРЕДИ И БЛОКИРУЕТ ОСТАЛЬНЫЕ.** Пока он не принят, `bootstrap_zahod.py` не может создать НИ ОДНОГО захода, а каждый коммит идёт с отключённым хуком. Не начинай с чего-то другого.

КОНТЕКСТ. `materials/` — монорепо курсов и фабрики их производства. Заход `kod_svedenie-vetok.md` слил три ветки в одну — и **два инструмента, приехавшие из РАЗНЫХ веток, столкнулись друг с другом**. По отдельности каждый прав; вместе они образуют замок, который не открывается изнутри. Плюс к ним — фикстура, которая краснеет всегда и потому держит хук выключенным на каждом коммите.

Три поломки, все проверены живым прогоном 2026-08-04, ни одна не гипотеза:

1. **Замок: `bootstrap_zahod.py` не может создать заход.** Он подставляет зону в шаблон БЕЗ backticks (`_studio/zhurnal/_TEMPLATE-zahod.md:11`, `{{ЗОНА}}`), а сразу после сборки сам зовёт `check_zahod.py`, чей гейт З3 требует пути **в backticks** — и отказывается создавать файл. Живой прогон: три попытки подряд, `❌ З3`, файл не создан. Инструменты приехали из веток `linter-zahoda` и `zhizn-vetki` и до слияния не встречались.
2. **`fixtures/dvizhok/PROGNAT.sh` зовёт `bootstrap_zahod.py` без `--kanal`**, а он стал обязательным (`bootstrap_zahod.py:237`, `required=True`). Вызов падает в argparse ⇒ подряд краснеют три ловушки. Красное чужое и системное: встаёт перед каждым, кто тронет `engine.js`, `base.css`, `build_deck.py`, `render.py` или `bootstrap_zahod.py`.
3. **Ловушка 3 той же фикстуры проверяет ОТМЕНЁННОЕ правило.** Строка «✅ С `--worktree` заход лёг В РАБОЧУЮ ПАПКУ (дефект закрыт)» требует поведения, отменённого `GIT-disciplina §4` 03.08: файл-заход живёт в ОДНОМ экземпляре в основной папке. Инструмент реализует действующее правило, фикстура требует отменённого — гейт краснеет на ПРАВИЛЬНОМ поведении и будет краснеть всегда.

ЦЕНА бездействия названа числами: **6 обходов `pre-commit` за один заход 04.08** — то есть защита снята не с одной ловушки, а со всего коммита, шесть раз подряд.

ЦЕЛЬ: открыть замок и погасить постоянное красное, чтобы следующие заходы шли с ВКЛЮЧЁННЫМ хуком.

Приёмка — по ОТЧЁТУ, без построчной сверки. **Флаг «стоп до цели»:** `playwright` в песочнице ты не поставишь — это действие владельца. Твоя часть — сделать так, чтобы его отсутствие давало «пропущено», а не красное.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/tools/bootstrap_zahod.py` `_studio/zhurnal/_TEMPLATE-zahod.md` `_generator/tools/fixtures/dvizhok/` `_generator/tools/fixtures/zahod/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_razblokirovka.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- **Точка отката:** `git rev-parse HEAD` записать первым действием и вынести в отчёт. Зона должна быть чиста (заход `kod_svedenie-vetok.md` её вычистил) — не чиста, значит тот заход не принят, СТОП и строка в `## ВОПРОСЫ`.
- Прочитать ТОЛЬКО эти четыре: `_studio/zhurnal/2026-07-30_dovodka-fabriki/RAZBOR-povtorov.md` (таблица §2 — исходные вердикты, скрипт классификации §6 — бери его ДОСЛОВНО, не переизобретай), `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (шапка + формат строки), `_generator/tools/zakryt_sessiyu.py` (куда вставлять гейт — рядом с `pechat_uborki_vetok`), `_generator/tools/git_zona.py` (функция печати `doctor`). Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Три правки, каждая в названном файле. Ничего сверх них — соблазн «заодно причесать фикстуру» здесь особенно велик и особенно вреден.

**Правка 1 — открыть замок (делать ПЕРВОЙ, остальные без неё не проверить).**
Чинить надо ту сторону, которая неправа. Неправ **генератор**, а не линтер: требование «зона — явный список путей в backticks» осмысленно и ловит реальную дыру (`check_zahod.py`, комментарий про `materials/`). Значит `bootstrap_zahod.py` обязан оборачивать каждый путь зоны в backticks при подстановке `{{ЗОНА}}`. Шаблон не трогай, если правка помещается в генератор.
Живой прогон-доказательство: собрать пробный заход в `/tmp`-арке и убедиться, что он создаётся и `check_zahod.py` на нём даёт `rc=0`. **Уже созданные заходы не переписывать** — `kod_razblokirovka.md` починен вручную и проходит линтер.

**Правка 2 — `fixtures/dvizhok/PROGNAT.sh`: оба вызова `zvat.py` получают `--kanal`.** Значение — `terminal` для обычного режима и `app` там, где проверяется рабочая папка; если разницы для ловушки нет, ставь `terminal` в оба и скажи об этом в отчёте.

**Правка 3 — ловушка 3 приводится к действующему правилу.** Ожидание переворачивается: с `--worktree` файл-заход обязан лечь **в основную папку**, а в рабочей папке его быть НЕ должно; стартовая команда при этом ведёт `cd` в рабочую папку (там код, не заход). Соседнюю проверку «нет тени» не удаляй — она становится главной. В комментарии над ловушкой назови дату и источник правила (`GIT-disciplina §4`, 2026-08-03), чтобы следующий не переоткрыл.

**Правка 4 — отсутствие `playwright` даёт «пропущено», а не красное.** Ловушки 1–2 проверяют, есть ли `chrome-headless-shell`; нет — печатать `⚠ пропущено: нет playwright (поставить: playwright install)` и НЕ поднимать `FAIL`. *Почему это не «ослабление гейта»: гейт, который краснеет от отсутствия инструмента, неотличим от гейта, поймавшего регрессию, — и его начинают обходить целиком. Обошли уже 4 раза (класс B автолога). Пропуск виден и честен, красное от нечего-проверить — ложь.*

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — живой прогон, не только фикстура:**
```
1. python3 _generator/tools/bootstrap_zahod.py <арка-в-/tmp> proba --branch X \
      --zone "_generator/tools/ _studio/" --kanal app        → файл СОЗДАН
2. python3 _generator/tools/check_zahod.py <тот файл>          → rc=0
3. bash _generator/tools/fixtures/dvizhok/PROGNAT.sh           → rc=0, в выводе
      строка «пропущено: нет playwright», ловушек с ❌ нет
4. твой финальный коммит зоны проходит БЕЗ --no-verify         → обходов 0
```
Пункты 1–2 — живой прогон на реальном объекте репозитория (настоящий `bootstrap_zahod.py`, не копия), пункты 3–4 — не только фикстура, а собственный коммит захода.

Пункт 4 — главный и он же самый честный: если хук снова потребует обхода, значит замок не открыт, как бы хорошо ни выглядели первые три. Обошёл — заход не готов, так и пиши.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** «ловушек с ❌ нет, проверено N из N».

## 3. ВЕРИФИКАТОР

Операция не лоссовая, но **ты правишь ФИКСТУРУ — то есть сам решаешь, что она теперь считает нормой.** Слепое пятно ровно там же, где в заходе про гейт.

Свежий субагент, ДРУГОЙ метод: пусть на КОПИИ проверит, что починенная фикстура всё ещё ловит настоящие поломки. Три подсовывания в копию `bootstrap_zahod.py`: (а) убрать backticks обратно — ловушка обязана покраснеть; (б) сломать регистрацию в `KARTA.md` — обязана покраснеть; (в) вернуть запись захода в рабочую папку при `--worktree` — обязана покраснеть. Таблица «подсунутая поломка · поймано / НЕ поймано», строк «НЕ поймано» — ноль.

*Почему именно так: фикстуру легче всего «починить», ослабив до зелёного на всём. Проверяется не зелёный прогон, а сохранённая способность краснеть.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/ _studio/zhurnal/_INFRA-git/ _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_razblokirovka.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/ _studio/zhurnal/_INFRA-git/ _studio/zhurnal/2026-07-30_dovodka-fabriki/kod_razblokirovka.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone _generator/tools/ _studio/zhurnal/_INFRA-git/   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone _generator/tools/ _studio/zhurnal/_INFRA-git/ \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Точка отката:** `229a2c4fa31d7900e8ee61940dc43fbc01b52a87` (HEAD на момент начала). Зона чиста (единственный `??` — сам этот файл-заход, ещё не закоммиченный, что ожидаемо).

**Оспариваю предпосылку Правки 2.** Заход утверждает «оба вызова `zvat.py`» в `PROGNAT.sh`. Живая проверка (`grep -c 'zvat.py" "\$GEN"'`) даёт **7** вызовов, не 2: строки 381 (R1, ловушка 3), 396 (R2, ловушка 3), 545 (R4, ловушка 4б, в цикле по метасимволам), 590 (R5, ловушка 4в, в цикле по именам рабочих папок), 614 и 618 (R6, ловушка 4г, два вызова), 724 (R3, ловушка про якорь). Ни один из 7 сегодня не несёт `--kanal`.
Разбор всех семи показал: только R1/R2 (ловушка 3) дают явный `❌` при отсутствии `--kanal`, потому что там утверждение о существовании файла безусловное. Остальные пять деградируют МОЛЧА в ложно-зелёный/пропущенный исход (argparse падает до записи файла → `S` пусто → «✅ генератор отказал» ловушки 4б; `G=otkaz` → «✅ отвергнуто» ловушки 4в; папка не заводится → «⚠ ловушка 4г пропущена» ×2) — то есть они не тестируют то, что заявляют, но и не красят критерий готовности (нет `❌`, только маскировка). Правка 2 по букве («оба вызова») закрывает ровно то, что red today — R1 и R2. Пять остальных — реальный, но НЕ ЗАЯВЛЕННЫЙ в этом заходе дефект тестовой полноты; чиню только названные два (правка зоны — фикстуру трогать можно, но задача просит точечно, не «заодно причесать»), остальные — пунктом очереди в `## ВОПРОСЫ`.

**Порядок работ:**
1. Правка 1 (`bootstrap_zahod.py`): в месте вычисления `zone` (после строк 359–362, до `.replace("{{ЗОНА}}", zone)`) обернуть каждый путь зоны в backticks — `" ".join(f"`{p}`" for p in zone.split())`. Живой прогон-доказательство в `/tmp`-арке: заход создаётся, `check_zahod.py` → `rc=0`.
2. Правка 2 (`fixtures/dvizhok/PROGNAT.sh`): добавить `--kanal terminal` вызову R1 (обычный режим, строка ~381) и `--kanal app` вызову R2 (проверяет рабочую папку, строка ~396).
3. Правка 3 (та же фикстура, секция ЛОВУШКА 3): перевернуть ожидание — с `--worktree` файл обязан лежать в ОСНОВНОЙ папке (R2), а НЕ в рабочей (WT); проверка «нет тени» становится главной и проверяет ОТСУТСТВИЕ копии в WT. Комментарий над ловушкой получает дату/источник (`GIT-disciplina §4`, 2026-08-03). Соседние проверки (зона репо-относительна, регистрация в KARTA.md), которые сегодня читают файл из WT, физически обязаны переехать на чтение из R2 — иначе они красным остановят критерий готовности сразу после инверсии локации (не самостоятельная правка, а прямое следствие правки 3: без переноса эти строки будут читать несуществующий в WT файл). Формат ЗОНА в проверке обновляется под backticks из правки 1.
4. Правка 4 (та же фикстура, блок перед ловушками 1-2): заменить проверку «пакет playwright импортируется» на попытку РЕАЛЬНОГО запуска браузера (`sync_playwright().chromium.launch()`) — именно это ловит отсутствие `chrome-headless-shell` при установленном pip-пакете. При неудаче печатать `⚠ пропущено: нет playwright (поставить: playwright install)`, `FAIL` не поднимать (уже так — сохраняю).
5. Верификатор — свежий субагент на КОПИИ `bootstrap_zahod.py`, 3 подсовывания (§3 захода).
6. Живой прогон критерия готовности п.1–3, затем коммит зоны (двумя ходами, с путями), затем `git_zona.py check --zone`.

**Ещё две ложные предпосылки, найдены при живом прогоне (не гипотеза — `bash PROGNAT.sh` реально падал):**
- Синтетический мини-шаблон в `podgotovit()` (используется ловушками 3/4/4б/4в/4г) НЕ содержал секций `## ПЛАН`/`## ВОПРОСЫ`/`## ОТЧЁТ`/`## УРОКИ ФАБРИКЕ` (гейт `check_zahod.py` З7, часть `STRUCTURAL_CHECKS`, которой сам `bootstrap_zahod.py` гейтит ДО записи файла) и строку зоны писал как `ЗОНА: {{ЗОНА}}` вместо обязательного формата `**ЗОНА (можно менять):** {{ЗОНА}}` (гейт З3). Оба — счита́ю прямым следствием правок 1/3 (без них ловушка 3 в принципе не может дойти до своих же проверок, которые я обязан продемонстрировать зелёными по критерию готовности п.3) — исправил в `podgotovit()`.
- **Вне этого следствия:** ловушка 5 (охват хука) падает по совсем другой, никак не связанной с правками 1–4 причине — `.githooks/pre-commit`, скопированный в одноразовый стенд, зовёт `check_ves.py`, а стенд копирует туда только `check_tool_contract.py`/`check_uroki.py` (строки ~676-677 `PROGNAT.sh`). НЕ чиню — вне зоны четырёх правок этого захода, пункт в `## ВОПРОСЫ`.
- Пересчитал вызовы `zvat.py` в `PROGNAT.sh`: их **7**, не 2 («оба вызова» из текста правки 2 — неточная предпосылка). Правлю только 2 названных (R1/R2, ловушка 3) — они и были единственными, кто без `--kanal` давал явный `❌`; остальные 5 без `--kanal` деградируют молча в ложно-зелёный/пропущенный исход (не красят критерий, но и не тестируют то, что заявляют) — тоже в `## ВОПРОСЫ`.


## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Ловушка 5 фикстуры `fixtures/dvizhok/PROGNAT.sh` (охват хука по имени файла, не по каталоговому шаблону) падает по причине, никак не связанной с этим заходом: одноразовый стенд (строки ~676-677) копирует туда только `check_tool_contract.py` и `check_uroki.py`, а реальный `.githooks/pre-commit` за это время научился звать ещё и `check_ves.py` — стенд его не копирует, и КАЖДЫЙ коммит в стенде падает с `can't open file check_ves.py`. Класс тот же, что уже был закрыт долгом 1 в шапке этого файла («хук гонит фикстуру по охвату, а не по шаблону пути») — фикстуры-стенды тихо расходятся с тем, что реально копирует хук, и это узнаётся только живым прогоном, не чтением кода. Живой прогон 2026-08-04: `bash _generator/tools/fixtures/dvizhok/PROGNAT.sh` → три ❌ в ловушке 5 подряд, `python3: can't open file '.../check_ves.py'`.
   ДОМ: _studio/zhurnal/2026-07-30_dovodka-fabriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. В `fixtures/dvizhok/PROGNAT.sh` семь вызовов `zvat.py` (не два, как в тексте правки 2), и без `--kanal` шесть из семи (не только два) технически ломаются на `argparse` — но пять из шести деградируют МОЛЧА в ложно-зелёный/ложно-пропущенный исход (ловушки 4б/4в/4г читают отсутствие вывода/файла как «кривой вход отбит» вместо того, чтобы отличить это от реального отказа генератора), а не в явный ❌. Значит эти пять ловушек сегодня НЕ проверяют то, что заявляют в комментариях — идентично ситуации, вскрытой этим же заходом на других двух. Починил только названные правкой 2 два вызова (R1/R2, ловушка 3) — они единственные давали явный ❌ и единственные названы заходом; остальные пять вне зоны этих четырёх правок.
   ДОМ: _studio/zhurnal/2026-07-30_dovodka-fabriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone _generator/tools/ _studio/zhurnal/_INFRA-git/` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
