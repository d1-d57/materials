# УРОКИ ФАБРИКЕ — арка «2026-07-30_dovodka-fabriki»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c '^### '      UROKI-FABRIKE.md   # уроков
grep -c '^ЦЕНА: '    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -c '^ВЕРДИКТ: ' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---

### 1. Аналитик снял числа с файла, который в этот момент писался чужим заходом, и построил на них вывод для владельца
ЦЕНА: три неверных числа в чате и в `TZ.md §3` (уроков 209 вместо 232, вердиктов 136 вместо 159, в арке конспекта 75 вместо 97) плюс ложная тревога «429 кандидатов потеряны», на разбор которой ушёл целый обмен. Поймал ВЛАДЕЛЕЦ, сверив с тем, что видел у себя в чате, — ни один гейт этого не ловит и не мог: греп-гейты считают строки в моменте. Заход `kod_zakrytie.md` шёл 02:29→02:34, замер снят внутри этого окна.
ВЕРДИКТ:

### 2. Аналитик объявил владельцу потерю, не прочитав `## ОТЧЁТ` захода, где потеря разобрана классами
ЦЕНА: один полный обмен на опровержение собственной тревоги; владелец потратил силы на вопрос «есть потеря или нет», хотя ответ лежал в файле, который аналитик считал прочитанным. Читался СВОД в `UROKI-FABRIKE.md`, а разбор отбраковки по классам (~160 реплик репетиции → `PRAVKI-final.md`, ~90 предпосылок с нулевой ценой, ~40 матправок, durable → по домам) стоял в `## ОТЧЁТ` того же захода. Правило «предпосылку проверяй по живым файлам» аналитик применил к предпосылкам владельца и не применил к своей.
ВЕРДИКТ:

### 3. Порог гейта унаследовал ОБОСНОВАНИЕ, которое не воспроизводится из файла, — и обоснование уехало в текст сообщения для всех будущих авторов
ЦЕНА: `ETALON-DIF.md §5` объявил «порог 4 — худший слайд эталона `buffon/sl-polygons`». Следующий заход попытался воспроизвести и не смог: у `sl-polygons` максимум 2 блока на сцену, **ни один слайд buffon при буквальной группировке не даёт 4**, а буквальная реализация вообще краснеет на самом эталоне (`sl-vizitka`=5, `sl-reading`=6). Исполнитель обошёл честно (исключил однасценовые) и назвал расхождение вслух — но **ложное обоснование осталось дословно в тексте `✗`-сообщения**, которое теперь печатается при каждой сборке: «порог 4 — худший слайд эталона buffon/sl-polygons». Плюс два независимых замера одной метрики **расходятся по составу нарушителей**: `promer_dekov.py` назвал 8 слайдов teorkat (s21, s05, s23, s03, s20, s12, s13, s19), `lint()` даёт 7 других (s01=9 и s02=5 в первом списке отсутствуют вовсе). Поймала приёмка, прогнав `--lint` и сверив со списком предыдущей волны. Ущерб пока нулевой — гейт ловит настоящие дефекты, — но число в сообщении носит происхождение, которого у него нет.
ВЕРДИКТ:

### 4. Два захода подряд не назвали wall-clock прогона, хотя гейт стоит в шаблоне
ЦЕНА: `kod_etalon-dif` дал время верификатора (447 с) и не дал своё; `kod_gejty-lint` написал «один присест» и «токены не считал». Оба честно отказались выдумывать токены (счётчик недоступен, `КОНСТИТУЦИЯ §10`) — это правильно, — но **время по часам машины снять можно, и `kod_revizia` его снимал** («первый артефакт 00:45:42, последний съём 01:37:55»). Приёмка выводила время из mtime файлов дважды. Гейт «время прогона + токены» в шаблоне есть и молча не исполняется: цена — приёмка не может сказать, дорожает ли цикл, а вся арка про скорость цикла.
ВЕРДИКТ:

### 5. Заход собран генератором, но его нельзя пересобрать: песочница не удаляет файлы в смонтированной папке
ЦЕНА: `bootstrap_zahod.py` предупредил, что заход checkout-типа при живой чужой работе и нужен `--worktree`; пересоздать с флагом не удалось (`rm: Operation not permitted`), контракт зоны правился Edit-ом руками — ровно то, чего канон требует избегать. Обошлось (ветку не переключали, зона — новая папка), но приём остаётся: сгенерированный заход в этой обстановке необратим, и флаги надо выбирать с первого раза.
ВЕРДИКТ:

### 6. Сводное число в отчёте разошлось с собственным артефактом — и стояло в готовой строке для КАРТЫ, то есть в одном ходе от канона
ЦЕНА: отчёт ночного захода и его готовая строка для `KARTA §6` объявляли «`KRATNOST.md` — 135 групп»; живой файл даёт **152** нумерованные группы (+131 одиночное). Готовая строка задумана так, чтобы аналитику осталось одно копирование, — значит ложное число было в одном ходе от индекса, из которого его будут брать ВСЕ будущие сессии. Поймала приёмка, пересчитав грепом. Отдельно: в этом же файле группа **№135** называется «число в заголовке не совпадает ни с фактическим счётом, ни со сводным числом в другом документе» — механизм задокументирован автором и тут же им применён к себе. ⇒ Готовая строка для КАРТЫ обязана проходить те же ворота «число печатается командой» (`KONSTITUCIYA §10`), что и сам документ; сейчас она их не проходит, потому что попадает в канон копированием, минуя гейт.
ВЕРДИКТ:

### 7. Инвентарь «143 файла», на котором построен критерий готовности, не воспроизводится напечатанной командой
ЦЕНА: заход печатает `find . -name 'kod_*.md' … | wc -l` → 143, но многоточие скрывает фильтр: живой инвентарь даёт **147**, разница — 4 тестовые фикстуры `_generator/tools/fixtures/uroki/zhurnal/test-arka/kod_*.md`, лежащие в репо с 21.07. Критерий готовности п.1 («143 − 23 = 120 строк-файлов») стоял на числе, которое проверить было нечем. Ущерб нулевой — реестр как раз даёт 120, и арифметика сходится поимённо (120 + 18 `konspekt-l1` + 3 своих + 1 `dovodka-l1` + 1 `teorkat-vvedenie` + 4 фикстуры = 147), — но приёмка потратила отдельный ход на реконструкцию того, что должно было печататься. ⇒ Команда в заходе пишется целиком, без `…`: заход её не выполняет, он её ЦИТИРУЕТ, и цитата с многоточием непроверяема.
ВЕРДИКТ:

### 8. Секция помечена «кандидатов 0», а в ней лежит наблюдение с явно названной ценой — потому что оно пересказ чужого инцидента, а не своя находка
ЦЕНА: независимая проба приёмки (свежий субагент, 3 файла из 3 разных арок, 15 секций, 38 кандидатов с названной ценой) дала **37 из 38** — один пропуск: `2026-07-28_teksty-l1/kod_skelet-konspekt-l1.md#ОТЧЁТ`, строка 124, где `STANDART §7½` цитируется вместе с ценой «на этом однажды уехали девять блоков `VIDY-teoriya` при двух зелёных гейтах». В реестре эта секция стоит с «кандидатов найдено = 0», в корпусе из неё нет ни одной записи, грепа по `VIDY-teoriya` в 28 файлах корпуса — ноль. Механизм пропуска называется точно: наблюдение лежало внутри блока «КАК ПРОВЕРЕНО» как ПЕРЕСКАЗ уже известного инцидента, а сборщик искал СОБСТВЕННЫЕ находки захода. Класс не единичный: ровно так же пересказаны `rc=128`, «коммит на 89 файлов», «пять сирот» — эти в корпус попали, потому что стоят в шаблоне ЗАДАНИЯ, где их ждали. Побочно: восемь адресов в `KORPUS-2026-07-27_paskal-lekcia-sborka.md` смещены на 2 строки (225–232 против живых 227–234) — на суть не влияет, при точечной сверке по строке уводит. ⇒ Оценка полноты корпуса: ~97 % по независимой пробе, и недобор смещён в один опознанный класс — цитаты чужих инцидентов внутри секций «как проверено».
ВЕРДИКТ:

### 9. Ритуал велит дописывать дневник СНИЗУ, а самой нижней записью лежала САМАЯ СТАРАЯ
ЦЕНА: `NAVIGATOR` предписывает входящей сессии «хвост `SESSIYA.md`, последние 2–3 записи». Утром 31.07 хвост давал запись **03:05** («заведён дневник»), тогда как последние по времени — 04:10 и 04:30, физически лежащие выше. Вход прочитал самую старую запись как самую свежую и не заметил бы этого, если бы отдельно не читался `HANDOFF`. Порядок разъехался за одну ночь на трёх записях из тринадцати — то есть ритуал «снизу» держится памятью пишущего и ничем больше. ⇒ У ритуала дневника нет носителя: гейта, который сравнил бы порядок дат в заголовках `## `, не существует. Правило без того, что покраснеет, — надежда (`KONSTITUCIYA §11`); это кандидат в первую же партию волны D.
ВЕРДИКТ:

### 10. Двадцать семь обходов `--no-verify` из двадцати восьми — ОДНА пара взаимоисключающих правил, и её образец уже год лежит в плане незакрытым
ЦЕНА: хук `pre-commit` требует, чтобы новый `.md` был зарегистрирован в `KARTA §6`; контракт зоны отдаёт исполнителю `_studio/docs/` ТОЛЬКО НА ЧТЕНИЕ. Выполнить оба нельзя — исполнитель обязан обойти хук, чтобы сдать работу. По автологу `_INFRA-git/INCIDENTY.md`: 27 обходов из 28, то есть **48 % всех 56 зафиксированных срывов коммита** — этот один конфликт. Инцидент 29.07 23:30 называет его дословно: «конфликт двух обязательных указаний». Ночной заход вычитки — тринадцатый исполнитель на тех же граблях. Механизм срабатывает БЕЗУПРЕЧНО (причина потребована, записана, дедуплицирована) — брак не в инструменте, а в том, что правило неисполнимо by design. ⇒ Адрес починки — волна D, а не `_INFRA-git`: в инфра-арке чинить нечем, там git исправен. Варианты, между которыми выбирать: хук не судит регистрацию у путей вне `docs/` · исполнителю открывается ровно строка `KARTA §6` · регистрация становится вычислимой (генерится из дерева, а не пишется прозой).
ВЕРДИКТ:

### 11. Файл «читать ПЕРВЫМ» инфра-арки неделю описывает состояние, которого нет
ЦЕНА: `_INFRA-git/SOSTOYANIE.md` несёт «Дата данных: 2026-07-23» и в §Хвосты п.0 утверждает: «автолог `INCIDENTY` проверен только мутацией, настоящего неуспешного коммита в бою ещё не было». Живой `INCIDENTY.md` на ту же минуту — **56 автозаписей** из реального боя, начиная с того же 23.07. Хвост закрылся сам в первые часы и неделю числится открытым; вход в арку читает его первым и получает ложную картину приоритетов. Это ровно группа №1 `KRATNOST.md` («канон описывает состояние, которое разошлось с живыми файлами», 22 независимых вхождения) — на файле, который сам заведён, чтобы такое ловить. ⇒ У «даты данных» нет носителя: ничто не краснеет, когда файл со статусом старше N дней объявляет открытым то, что закрыто. Р34 требует даты, но не требует её СВЕРКИ.
ВЕРДИКТ:

### 12. Счётчик по СТРОКАМ структурно слеп на индексе, где целый раздел — одна строка
*Источник: заход `kod_registracia-bez-obhoda.md`, отчёт 31.07.* Ловушка «повторный вызов не плодит дубль» была написана как `grep -c 'ИМЯ' KARTA.md` и осталась **зелёной при снятой идемпотентности**: обе записи легли в ОДНУ строку раздела `§6`, а `grep -c` считает строки, не вхождения.
ЦЕНА: один полный цикл мутационной проверки впустую — и, что дороже, **готовая мёртвая ловушка, которая уехала бы в репозиторий с видом рабочей**. Поймано только потому, что исполнитель мутировал каждую ловушку поимённо, а не прогнал фикстуру целиком. ⇒ Правило шире случая: в `KARTA.md` и любом индексе-абзаце считать только `grep -o … | wc -l`. И шире: **зелёная ловушка ничего не значит, пока не показано, что она краснеет на своей поломке.**
ВЕРДИКТ:

### 13. Верификатор, сформулированный как АТАКА, нашёл 9 дыр — пять в коде, написанном час назад
*Источник: тот же заход.* Формулировка «найди способ обойти» вместо «проверь, что работает» дала за один прогон 27 выписанных способов, 27 проверенных, 9 дыр, из них 5 — в свежем коде.
ЦЕНА: обратная — измерено то, что НЕ произошло. Без атаки в канон уехали бы (Д1) дверь, молча плодящая сирот на пяти файлах КАЖДОЙ новой арки, и (Д5) состояние «дверь ✅ / хук ❌ / легального хода нет» — то есть заход, закрывающий 27 обходов `--no-verify`, **породил бы двадцать восьмой**. Заплачено 157 888 токенов и 19 минут. Отдельно ценно, что верификатор перечислил 8 непроверенных пунктов: это и есть охват, без которого «дыр не найдено» ничего не значит. ⇒ Кандидат в канон захода: у операций, где заход САМ пишет защиту, верификатор формулируется как атака по умолчанию, а не «если двигаем/теряем/жмём».
ВЕРДИКТ:

### 14. Зона захода не содержит самого файла-захода — и мой критерий готовности был построен на том, что содержит
*Ошибка аналитика, поймал исполнитель до работы.* Я написал критерием 4: «твоя зона содержит новый `.md` — сам этот заход, — так что условие непустое». Оба утверждения ложны: `{{ЗОНА}}` перечислена поимённо девятью путями, файла-захода среди них нет, и заход не новый (я зарегистрировал его сам).
ЦЕНА: (1) зонный коммит `d2f6fbf` не содержит ни одного нового `.md`, то есть главный гейт захода — ворота 5 — он не проверяет вовсе, и живое доказательство пришлось строить отдельным коммитом `6eaa205`; (2) **отчёт, который читает приёмка, исполнитель закоммитить не может** — файл вне его зоны, и на конец захода он висит вне git; приёмка видит грязь первым же ходом гейта 0. Это повторяется у КАЖДОГО захода, просто раньше тонуло в других обходах. ⇒ Лечится одной строкой в `bootstrap_zahod.py`: путь создаваемого захода дописывать в `{{ЗОНА}}` автоматически, а не надеяться, что аналитик вспомнит.
ВЕРДИКТ:

### 15. `zsh` не расщепляет `$VAR` по пробелам — и это бьёт по ИСПОЛНИТЕЛЮ, а канон объясняет это как проблему владельца
*Источник: тот же заход.* `GIT-disciplina §0` объясняет расхождение bash/zsh формулой «Cowork пишет — владелец исполняет». Но исполнитель тоже работает в zsh: сложил девять путей зоны в переменную, подставил `$ZONA` в `git add` — zsh отдал их git одной строкой, `rc=128`, «did not match any files».
ЦЕНА: один ход впустую. Мелко, но систематично: соблазн собрать длинный список путей в переменную возникает у каждого захода с многопутевой зоной, а таких теперь большинство. ⇒ Запрет на переменную-со-списком в каноне относится и к исполнителю, а не только к строке для владельца.
ВЕРДИКТ:

### 16. Фикстуры судят диск, а линтер контракта судит индекс — и через эту щель мутация уезжает в HEAD
*Общефабричная пробоина, найдена верификатором захода; касается ВСЕХ фикстур, включая `git_zona`.* `check_tool_contract.py` читает **индекс** (`staged_text`, это задокументировано), а фикстуры копируют инструменты **с диска**. Значит рутинная последовательность «`git add` сломанного инструмента → поправил файл на диске → `commit`» даёт `rc=0` и «ФИКСТУРЫ ЗЕЛЁНЫЕ», а в HEAD уезжает сломанная версия. Это не трюк, а обычный сценарий «поправил после add».
ЦЕНА: пока не материализовалась — но защита, объявленная над всеми инструментами фабрики, имеет обход, который срабатывает случайно, без злого умысла. Вторая половина: фикстуру **не покрывает никто** (`--fixtures-for fixtures/register_doc/PROGNAT.sh` → пусто, проверено приёмкой) — выпотрошенная фикстура `exit 0` коммитится зелёной. Та же дыра у `git_zona`. ⇒ Отдельный заход, не правка одной фикстуры.
ВЕРДИКТ:

### 17. Гейт уроков ВТОРОЙ раз завалил коммит на ФОРМЕ слова «ЦЕНА», при полностью названной цене
`check_uroki.py` требует строку, начинающуюся ровно с `ЦЕНА: `. Урок 13 нёс цену развёрнуто и конкретно — «157 888 токенов, 19 минут, без атаки заход породил бы двадцать восьмой обход» — но начинался словами `ЦЕНА обратная, и она названа конкретно:`. Гейт увидел отсутствие цены и остановил коммит.
ЦЕНА: владелец вставил выданную мной команду и получил красное — один круг потерян, работа осталась вне git ещё на ход. И это **второй случай того же класса у того же гейта**: 21.07 строгая форма объявила «уроками без цены» семь полноценных уроков закрытой арки, где цена была выделена жирным, **месяцами блокировала коммиты ЧУЖИХ заходов**, из-за неё один заход ушёл через `--no-verify`, а в канон уехал ложный урок «гейт штрафует за чужой долг» (`KARTA §6`, описание `check_uroki.py`). Тогда починили точечно — разрешили `**ЦЕНА: …**`. Класс не закрыли. ⇒ Правильная форма проверки: строка начинается с токена `ЦЕНА`, дальше любой разделитель, дальше непустой текст. Сейчас гейт судит пунктуацию, а не наличие цены, и каждый новый способ написать то же самое — это новая блокировка чужой работы. `check_uroki.py` вне зоны текущей добивки ⇒ отдельной строкой в долги.
ВЕРДИКТ:

### 18. Регистрация ШАБЛОНОМ выглядит выполненной и не считается: 28 документов остались сиротами
Регистрируя артефакты ночной вычитки в `KARTA §6`, я вписал их одной формулой — `KORPUS-<арка>.md — 28 файлов` — вместо 28 имён. Ворота 5 ищут ИМЯ файла подстрокой; шаблона они не видят.
ЦЕНА: 28 документов числились зарегистрированными и были сиротами; обнаружено только когда добивочный заход снял число сирот командой и оно не сошлось с ожидаемым. Это тот же класс, что дыра Д1 у двери («зарегистрирован по мнению человека» ≠ «по мнению ворот»), но совершил его аналитик, уже прочитавший про Д1 накануне. Снято прогоном двери по каждому файлу: **37 → 9**. ⇒ Обобщение: любая запись в индекс, сделанная человеком прозой, обязана быть проверена ТЕМ ЖЕ предикатом, которым судит гейт. Дверь это делает сама; рука — нет.
ВЕРДИКТ:

### 19. Число, снятое аналитиком при написании захода, стареет до его запуска — и исполнитель обязан пересчитать
Я дал в критерий базу «ворота 5 → 38». К моменту запуска команда давала 37: мой же коммит прошёл между замером и написанием. Исполнитель не подогнал результат и не промолчал — прогнал `git log` по `KARTA.md`, объяснил разницу одним конкретным документом и взял свою живую базу.
ЦЕНА: на этот раз нулевая, потому что исполнитель проверил. Но это **третий подряд заход, который ловит устаревшую предпосылку аналитика** (первый — «209 уроков» с файла, который писался в тот момент; второй — «зона содержит новый `.md`»), и однажды никто не проверит. ⇒ Форма критерия меняется: не «база N», а «сними базу командой и сверь с моей; разошлось — объясни расхождение и работай от своей». Число в заходе — ориентир и предмет проверки, а не факт.
ВЕРДИКТ:

### 20. 🔴 Реестр гейтов конвейера объявляет 15 гейтов и 6 хуков; трекер исполняет 11 и 5
`_studio/konvejer/GEJTY.md` описывает `G12–G15` и `H6` подробно, с ценой и признаком-на-диске. `_generator/tools/sostoyanie.py` их не знает: `GATE_ORDER` кончается на `G11`, `HOOK_ORDER` на `H5` (проверено приёмкой командой, не по отчёту). Реализация — около 500 строк — существует, но лежит в невлитой ветке `zahod/dek-paskal-v2`. Причина точная: коммит `1038d24 adopt` перенёс с ветки все 37 файлов `_studio/` (канон и журнал) и **ни одного файла `_generator/`**.
ЦЕНА: четыре гейта и хук, заведённые ПОСЛЕ живой лекции 27.07 именно затем, чтобы ловить дефекты слоя сцен и служебных слайдов, месяц числятся существующими и не работают. Прямое следствие видно в арке Л1: те же дефекты снова дожили до живого прогона. Плюс каждый, кто открывал `GEJTY.md`, получал ложную картину охвата — в том числе я, когда планировал волну E «дописать гейты», часть которых уже написана. ⇒ Класс шире случая: **перенос работы с ветки на ветку через `adopt` по путям умеет привезти документацию без кода**, и ничто не краснеет. Кандидат в гейт: реестр гейтов и `GATE_ORDER` трекера обязаны сверяться машиной — расхождение имён между ними красное.
ВЕРДИКТ:

### 21. 🔴 Снимок слайда для визуальной проверки делался с НЕРАСКРЫТЫМ содержимым — и об этом уже был написан комментарий в чужом инструменте
`_generator/render.py:29` открывает слайд как `?only=N&scene=99`. Дальше `engine.js:149` берёт число 99 буквально (`Q.get('scene') !== null ? +Q.get('scene') : …` — без ограничения сверху), а `applyScene` вешает на слайд класс `scene-99`. В `base.css` раскрытие описано селекторами `.scene-2 … .scene-5`; **правила для `.scene-99` нет ни в одном деке**, а базовое состояние `[data-scene-from]` — `opacity: 0; visibility: hidden`. ⇒ на снятом PNG всё пошаговое содержимое НЕВИДИМО.
ЦЕНА: проверено грепом — `.scene-99` есть только в `teorkat-vvedenie` (6 файлов), у `buffon`, `fibonacci`, `dandelin` — **ноль**. То есть каждый снимок многосценового слайда этих трёх деков показывал слайд с пустой доской, и вся визуальная приёмка через `render.py` смотрела не на то. Хуже: в `teorkat-vvedenie/src/tools/sobrat_overlay.py:67` **стоит комментарий, называющий этот дефект дословно** («`.scene-99` не существует ни в одном деке ⇒ на снятом PNG слайд со сценами…») и строка 318 печатает «`.scene-99` (заплата render.py)» — то есть один проект диагностировал и залатал у себя, а фабрика не всосала ни диагноз, ни заплату. Третий случай того же структурного класса: `buffon` так же оставил у себя харнесс `shot.py`. ⇒ Чинить не заплатой, а в движке: `applyScene` обязан ограничивать `k` числом сцен слайда (одна строка, лечит всех потребителей разом). Отдельно: цикл снятия классов в `engine.js:55` идёт `i <= 9` — классы `scene-10+` не снимаются вообще.
ВЕРДИКТ:

### 22. Каскад раскрытия сцен выписан в `base.css` РУКАМИ до `.scene-5`, а генерация каскада уже написана — в проекте, не в фабрике
`base.css` перечисляет пары «сцена × шаг» вручную: `.scene-2 [data-scene-from="2"]`, `.scene-3 …` и так до пятой. Слайд с шестью сценами физически не сможет раскрыть шаги с шестого — не потому что запрещено, а потому что строчку не дописали.
ЦЕНА: уже оплачена один раз в арке Паскаля («каскады сцен в CSS выписаны руками до `.scene-5`, на шестой оставалась первая сцена» — урок с ценой в `2026-07-27_paskal-lekcia-sborka/UROKI-FABRIKE.md`). Сейчас ни один живой дек не превышает пяти сцен (`buffon` 5, `fibonacci` 4, `dandelin` 4, `teorkat` 2), поэтому дефект спит — и проснётся на первом же плотном слайде. Генератор каскада существует и работает: `teorkat-vvedenie/src/tools/sobrat_overlay.py` порождает `.scene-1..N` по двум семействам селекторов. ⇒ Тот же вывод, что у урока 21: решение написано в проекте и не переехало в фабрику. Кандидат в `build_deck.py`: каскад порождается по фактическому максимуму `data-scenes` дека, руками в `base.css` не пишется вовсе.
ВЕРДИКТ:

### 23. `bootstrap_zahod.py --worktree` пишет файл захода в ОСНОВНОЕ дерево, а исполнителя отправляет в другое
Флаг `--worktree` заводит заходу отдельную рабочую папку (`ensure_worktree`, строка 87: `REPO_ROOT.parent / f"{REPO_ROOT.name}-wt"`) и вписывает в заход `cd` туда вместо `git checkout`. Но сам файл пишется по `dst = arka / f"kod_{тема}.md"`, где `arka = REPO_ROOT / arka` (строки 136 и 141) — **всегда основной репозиторий, независимо от флага**. Исполнитель приходит в свою папку, а захода там нет.
ЦЕНА: любой параллельный заход через `--worktree` сейчас сломан на старте — а именно worktree канон предписывает как единственный законный способ работать двум заходам сразу (`GIT-disciplina §4`), и владелец говорит, что параллельные заходы у него постоянны. Найдено верификатором добивки 31.07, воспроизведено кодом; `GIT-disciplina.md:226` при этом утверждает обратный порядок операций («Worktree заводится ПОСЛЕ того, как заход дописан»), то есть канон и код расходятся ещё и здесь. Пока дефект жив, «параллельные заходы через worktree» — не механизм, а надежда, и все параллельные заходы фактически идут в одну папку, деля рабочее дерево.
ВЕРДИКТ:

### 24. 🔴 Критерий, посчитанный на состоянии ДО работы, применяется к состоянию ПОСЛЕ — два раза в одном заходе
*Источник: `kod_vlivanie-i-storozh.md`, оба урока исполнителя; он сам свёл их в один класс и назвал «второй случай в одном заходе, значит не случайность».*
1. Я велел дописать `_generator/skeleton/SPEC.md`, назвав его «единственным документом, который merge не поправит сам», на основании «ноль упоминаний `sluzhebnye`». Верно ДО вливания — но `SPEC.md` **приезжает из ветки** вместе с механизмом и уже несёт нужный раздел, включая ровно то предупреждение, которое я велел дописать. Плюс файла не было в объявленной зоне, а §2 отдельно запрещает править что-либо вне неё.
2. Критерий 4 требовал «сторож называет три ветки и молчит про четыре». Веток восемь; после вливания молчать он обязан про ПЯТЬ — четыре изначально нулевые плюс сама влитая. Я это же и написал абзацем выше («после ЦЕЛИ-1 обязано стать 0») и не пересчитал критерий.
ЦЕНА: оба раза нулевая, потому что исполнитель проверил предпосылки и вынес возражения в `## ПЛАН` до работы. Но послушный исполнитель в первом случае обязан был либо нарушить границу зоны, либо дописать дубль в привезённый файл (обе ветки запрещены тем же заданием), а во втором — заставить сторожа шуметь на только что влитой ветке, то есть реализовать ровно тот шум на здоровом, который `Р31` запрещает и который соседним пунктом того же критерия объявлен провалом. **Противоречие разрешалось не механизмом, а догадкой исполнителя.** ⇒ Правило письма захода: критерий обязан быть сформулирован от состояния ПОСЛЕ операции. Где число снято ДО — писать не число, а команду и ожидаемое ИЗМЕНЕНИЕ («было X, обязано стать Y, потому что…»). Это тот же корень, что `KONSTITUCIYA §10`, только мимо правила проходит не цифра, а вывод из цифры.
ВЕРДИКТ:

### 25. Сторож веток чуть не родился с той самой болезнью, которую лечит: зелёный вердикт при живой невлитой работе
Первая версия сторожа брала имена веток через `%(refname:short)`. Git укорачивает имя лишь до однозначности: при **теге-омониме** он отдаёт `heads/vetka-a`, из чего собирается несуществующий `refs/heads/heads/vetka-a` — ветка уходит в «не прочитано», и сторож печатает **зелёное «невлитого нет» при живой невлитой работе на ней** (воспроизведено исполнителем).
ЦЕНА: нулевая — поймано мутацией до сдачи. Но поймано **со второго захода**: первая ловушка дефект ПРОПУСТИЛА, потому что грубо грепала имя ветки, а имя встречается и в строке «охват НЕПОЛОН». То есть ловушка была зелёной на сломанном сторóже. Переписана: судит саму находку, отсутствие строки «НЕПОЛОН» и правильное имя в команде лечения. ⇒ Дважды подтверждённое правило: **ловушка обязана судить РЕЗУЛЬТАТ, а не присутствие подстроки в выводе.** Первый случай — 31.07, `grep -c` на индексе-абзаце (урок 12); здесь — `grep` имени, встречающегося в диагностике. Оба раза защита выглядела поставленной и не срабатывала. И оба раза спасла только мутация, а не прогон фикстуры целиком.
ВЕРДИКТ:

### 26. 🔴🔴 «Единственное место» фабрики не доходит до уже собранных деков — правка скелета не меняет в них НИ БАЙТА
*Источник: `kod_dvizhok-i-harness.md`; проверено приёмкой независимо.* `build_deck.py:336` читает `engine.js` из папки ДЕКА (`src / "engine.js"`), `bootstrap_lekcia.py:274` копирует скелет в дек ОДИН раз (`shutil.copytree`), обратного распространения нет ни одного. Замер хэшей: `buffon/src/engine.js` уже разошёлся со скелетом (`7ba1bd7c` против `a756437e`), `fibonacci` и `dandelin` держат старую копию (`f38fca28`), а `base.css` у `buffon` и `dandelin` **вообще нет** — каскад лежит у них в `shablon.html`.
ЦЕНА: заход был построен на предпосылке «чиню в единственном месте — лечатся все», и она верна **только для будущих деков**. Исполнитель обнаружил это замером диффов и переставил починку в `render.py`; без замера он сдал бы зелёный отчёт с четырьмя закрытыми задачами и **неработающей зрительной петлёй на всех трёх живых деках**. Ровно так дефект `scene=99` и прожил месяц: заплата в `teorkat-vvedenie` снимала боль в одном деке, а «фабрика починена» читалось как «починены все». ⇒ Это объяснение целого класса жалоб «мы чиним фабрику, а результат не меняется»: у фабрики нет механизма распространения канона в уже заведённые деки. Пять живых деков несут свои копии движка и каскада. Кандидат в отдельную волну, а не в заход.
ВЕРДИКТ:

### 27. Фильтр путей хука УЖЕ объявленного охвата — ядро правки уехало в коммит без живого гейта
`.githooks/pre-commit:62-63` отбирает staged грепом `^_generator/tools/.*\.py$` · `^_generator/tools/fixtures/.*\.sh$` · `^\.githooks/`. Фикстура захода честно объявила охват над `engine.js`, `base.css`, `build_deck.py`, `render.py`, и `check_tool_contract.py --fixtures-for` её для всех пяти имён находит — но до `--fixtures-for` доходит только `bootstrap_zahod.py`, остальные четыре пути под шаблоны не подходят.
ЦЕНА: два ядровых механизма захода (движок и каскад — через них проходит каждый слайд каждого дека) закоммичены **без живого гейта**, при зелёной фикстуре и верно объявленном охвате; проверка верификатора «сломай `engine.js` → хук остановит» невыполнима по устройству. Это тот же дефект «триггер уже охвата», ради которого `TOOL-CONTRACT-COVERS` и заводился: лечение доехало до объявления охвата и не доехало до отбора путей. Чинится одной строкой в хуке, который заходу был запрещён поимённо.
ВЕРДИКТ:

### 28. `--verbose` в `-p`-режиме не показывает ход работы — флаг стоял ради различения живого и зависшего прогона и не различал ничего
Замерено живым прогоном: `claude -p --verbose "…"` печатает РОВНО финальный ответ — ни событий, ни имён инструментов, ни стоимости. Комментарий генератора при этом гласил: «`--verbose` — виден ход работы (иначе живой, зависший и оборванный прогоны выглядят одинаково)».
ЦЕНА: все три и выглядели одинаково — владелец смотрел в молчащий терминал и не мог отличить работу от зависания **за всё время существования канала `terminal`**. Вторая половина той же цены — 17 независимых записей «токены даны оценкой» в корпусе: число лежало в одной строке потока, которую формат вывода просто не просили напечатать. ⇒ Комментарий к флагу — тоже утверждение о живом поведении, и он ни разу не проверялся. Оба лечатся `--output-format stream-json`, что заход и сделал.
ВЕРДИКТ:

### 29. Канон описывал механизм, которого в git не существует, — и потому подтверждал сломанный код как верный
`GIT-disciplina.md:226`: «Worktree заводится ПОСЛЕ того, как заход дописан; **папка копирует заход в момент создания**». `git worktree add` выкладывает содержимое ВЕТКИ; untracked-файл не попадает в новую папку никогда, ни при каком порядке.
ЦЕНА: это единственное место канона, куда пойдёт проверяющий поведение `--worktree`, и оно объявляет наблюдаемый симптом («захода в рабочей папке нет») ожидаемым. Поэтому дефект `bootstrap_zahod.py` выглядел корректным ровно столько, сколько существовал, — при том что ломал единственный законный способ вести два захода одновременно, а у владельца параллельные заходы постоянны. ⇒ Отличается от группы №1 корпуса («канон отстал от кода»): здесь канон описывал механизм, которого не было НИКОГДА, и служил алиби.
ВЕРДИКТ:

### 30. Пять ловушек из свежих оказались ДЕКОРАТИВНЫМИ — вскрыл только обязательный мутационный прогон
Из 22 мутаций захода пять показали, что ловушка зелёная на своей поломке: проверка «в выводе есть число» проходила, потому что число печатал сырой откат фильтра · мутация целилась в боевой шаблон, которого фикстура не использует · слайд с одинарными кавычками не определял максимум, и схлопывание маскировал сосед · и ещё две.
ЦЕНА: без правила «каждую ловушку проверить мутацией» **три из четырёх заявленных ловушек уехали бы в коммит нерабочими**, а отчёт называл бы их защитой. Это **третий случай одного класса за сутки** — после `grep -c` на индексе-абзаце (урок 12) и грубого грепа имени ветки (урок 25). Общий признак всех трёх: проверка мерила ПРИСУТСТВИЕ подстроки в выводе, а у вывода был легальный путь содержать её при сломанном механизме. ⇒ Правило созрело до канона: **ловушка судит результат операции, а не текст вывода; «в выводе есть X» — не проверка.** Трижды оплачено, носитель — обязательный мутационный прогон, который уже стоит в шаблоне захода.
ВЕРДИКТ:

### 31. 🔴 Пять команд коммита подряд не выполнились: я дал относительный путь, канон требует абсолютный
Все команды `git_zona.py` я выдавал владельцу в форме `python3 _generator/tools/git_zona.py commit …`. Он запускал их из домашнего каталога — `can't open file '/Users/ivanyakovlev/_generator/tools/git_zona.py'`. Пять раз подряд, с 12:37 до 19:41.
ЦЕНА: **365 строк работы аналитика семь часов лежали вне git** — дневник (182), уроки 12–30 (67), план (116), плюс три файла заходов в состоянии «рождено и никогда не ставилось», то есть смертных от `git clean`. Обнаружено случайно: владелец прислал скриншот терминала по другому поводу. 🔴 **Правило существовало и было записано**: `_INFRA-git/SOSTOYANIE.md` прямо требует «Cowork выдаёт строку с АБСОЛЮТНЫМ путём (работает из любого каталога)». Я его не читал перед выдачей команды ни разу — и это тот же класс, что урок 20: канон существует, исполнитель его не открывает, ничто не краснеет. ⇒ Носителя нет: ни один гейт не проверяет форму команды, которую Cowork печатает владельцу в чат. Кандидат: линтер выдаваемых строк, либо `git_zona.py` печатает свою же команду абсолютным путём готовой к копированию.
ВЕРДИКТ:

### 32. Один и тот же заход запущен дважды в двух окнах — и узнать об этом было неоткуда
Владелец запустил `kod_melkie-dolgi.md` в 18:39 и повторно в 18:47 в другом окне, полагая, что второе окно — это другой заход. Две копии одного агента 45 минут правили `.githooks/pre-commit`, `check_uroki.py`, `audit.py` в одном рабочем дереве и писали в один лог.
ЦЕНА: на этот раз обошлось — следов перезаписи в файле захода нет (одна секция `ПЛАН`, одна `ОТЧЁТ`, дублей заголовков ноль), и второй заход (`dobycha-vladelca`) при этом **не был запущен вообще**, из-за чего 40 минут числился «работающим, но не пишущим на диск». Но защиты не было никакой: ни заход, ни инструмент, ни аналитик не могут ответить на вопрос «что сейчас запущено». Я отвечал на него по mtime файлов и угадал не всё. ⇒ Кандидат: заход при старте пишет метку «занят» в свой файл (pid, время, окно) и отказывается стартовать, если метка свежая; либо `doctor` показывает активные прогоны. Сейчас единственный способ узнать — посмотреть в окна терминала глазами.
ВЕРДИКТ:

### 33. Я объявил «сверено пофайлово», проверив три файла из восьми — исполнитель проверил и остановился
В задании на вливание трёх веток я написал: «содержимое всех трёх уже на рабочей ветке надмножеством — сверено аналитиком пофайлово 31.07». Фактически я сверил **три файла из восьми** у `arka/vneshnie-istorii` и распространил вывод на всю ветку. Исполнитель проверил сам, нашёл расхождение в двух файлах из проверенных им, получил реальный конфликт `merge` в четырёх путях и **остановился по правилу «конфликт — стоп, разбирается аналитик»**.
ЦЕНА: один пункт из пяти в заходе не закрыт, и доделывать проверку пришлось мне после отчёта. По существу я оказался прав — полная сверка всех восьми файлов дала **две уникальные строки на ветке**, обе устаревшие промежуточные формулировки, — но правым я оказался случайно, а не по методу. 🔴 **Это третий раз за сутки, когда моя предпосылка сделана на выборке и подана как полная** (первый — «209 уроков» с файла, который писался; второй — «зона содержит новый `.md`»). И это ровно то правило, которое я сам вписываю в каждый заход: «отрицательный вердикт несёт ОХВАТ В СЕБЕ». ⇒ К себе оно применяется хуже, чем к исполнителям, потому что у исполнителя есть гейт приёмки, а у моих утверждений в заходе — нет ни одного.
ВЕРДИКТ:

### 34. 🔴🔴 ДЕВЯТАЯ ОСЬ, которой не было в моей восьмёрке: вход захода, написанный аналитиком, не судит НИКТО
Две арки за одни сутки независимо пришли к одному. В этой — уроки 31–33 (относительный путь в команде, заход запущен дважды, предпосылка на выборке подана как полная). В параллельной `2026-07-30_dovodka-l1` — **семь уроков из тринадцати** про аналитика: жанр артефакта не назван до захода (обесценена целая часть опросника, 50 мин и 410 тыс. токенов) · заход велел «разложить», не дав перечня единиц выхода (два артефакта подряд приняты по гейтам и негодны) · рабочая записка отдана как спецификация и исполнена буквально вместе с редакторскими пометами · диагноз по своему артефакту вместо живых файлов, дважды · совет по числу, прочитанному до коммита владельца · шапка правленого документа врёт телу · вывод об отсутствии сформулирован шире открытого исполнителю охвата (заказ завышен впятеро).
ЦЕНА: сложением по обеим аркам — не меньше **четырёх машинных прогонов, обесцененных полностью или наполовину** (`kod_opros` частью, `kod_perekomponovka` целиком, два моих круга по коммитам и вливанию), плюс 365 строк семь часов вне git, плюс семь редакций списка слайдов за вечер. 🔴 **И ни один провал не поймал гейт — все поймал владелец или добросовестный исполнитель.** Симметрия объясняет почему: у исполнителя есть приёмка, у документа — ворота, у инструмента — фикстура, у коммита — хук; у текста, который аналитик пишет в заход и выдаёт в чат, **ноль проверок**. ⇒ Ось крупнее многих из восьми и должна войти в разметку девятой. Спецификация линтера `check_zahod.py` собрана в дневнике 31.07 ~21:20 — десять проверок, каждая оплачена конкретным прогоном.
ВЕРДИКТ:

### 35. Два чата коммитят `--zone _studio/` и подметают работу друг друга — в обе стороны, и это ловится только честностью отчётов
Параллельная арка записала уроком, что коммит `e4decd4` «подмёл `KARTA.md` целиком вместе с чужой строкой регистрации». Проверил: это коммит МОЕГО захода (`vlivanie-i-storozh`, пред-merge фиксация 16:17:10), и он честно назвал чужую строку в сообщении. Обратное тоже верно: мой коммит `0549918` забрал `_studio/docs/kak-delat/STANDART-teksta.md` — правку канона параллельной арки (правило 11 про термины) — под моим сообщением про регистрацию заходов.
ЦЕНА: пока нулевая, оба раза чужое доехало и было названо. Но механизма нет: `--zone _studio/` — законная и рекомендованная форма, а `_studio/` содержит рабочие папки обеих арок и общий канон. Обнаружено только потому, что владелец дал прочитать чужой файл уроков; ни один гейт этого не видит, а «коммит забрал чужое под своим сообщением» — ровно тот класс, который 21.07 стоил коммита на 89 файлов вместо трёх. ⇒ Кандидат: `commit --zone` предупреждает, когда в зоне есть правленые пути, не тронутые этой сессией; либо чаты договариваются о непересекающихся префиксах (`zhurnal/<своя арка>/` + отдельный ход для `docs/`).
ВЕРДИКТ:

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

### 36. Новый инструмент в `_generator/tools/` не проходит коммит без спутник-фикстуры — и это правильно
ЦЕНА: коммит упал (rc=1) на `zakryt_sessiyu.py` и `sdelat_handoff.py`: «инструмент разбирает вход, но ничто не доказывает, что кривой вход он отвергает». Я завёл два инструмента дисциплины сессии и не завёл им фикстуру — то есть построил механизм против потери знания и не доказал, что он сам не теряет молча. Гейт `check_tool_contract.py` поймал это до того, как инструменты уехали в работу. Написана `fixtures/sessiya/PROGNAT.sh`: семь ловушек (нет папки арки · нет транскрипта · **транскрипт без реплик владельца** · хэндофф без обязательного поля · хэндофф поверх существующего · `--zamenit` поверх рукописного файла) плюс положительный контроль. Третья ловушка — главная: пустая выгрузка выглядит ровно как «в сессии не было ничего важного».
ВЕРДИКТ: → гейт (существовал и сработал; урок в том, что фикстуру надо заводить ТЕМ ЖЕ ходом, что инструмент)

### 37. Хэндофф не называл стартовую строку — новый чат начинал с угадывания
ЦЕНА: владелец: *«каждый раз надо выписывать очень чётко короткий вводный текст про то, где мы сейчас находимся и что мы делаем… сказать чату, где он должен поискать информацию»*. До этого вход в новую сессию собирался устно и по-разному; хэндофф 31.07 не назвал даже, где запускать работу. Теперь `sdelat_handoff.py` печатает готовую строку в терминал и вставляет её первым разделом файла; фикстура краснеет, если строки нет или она не называет файл входа.
ВЕРДИКТ: → шаблон (генератор хэндоффа) + гейт (фикстура `sessiya`)

### 38. Генератор не защищал рукописный файл от собственной пересборки
ЦЕНА: предотвращена. `sdelat_handoff.py` отказывается писать поверх существующего файла — но при доработке параметров пересобирать свой же файл нужно, и флаг `--zamenit` напрашивался. Без различения «мой файл» и «рукописный» этот флаг однажды затёр бы хэндофф, написанный руками, вместе с не перенесённым знанием. Решение: маркер `<!-- собран sdelat_handoff.py -->` в шапке; `--zamenit` берёт только файлы с маркером, рукописные не трогает ни при каких флагах.
ВЕРДИКТ: → гейт (ловушка 7 в `fixtures/sessiya/PROGNAT.sh`)

### 39. 🔴 СРЕДА АНАЛИТИКА НОВЕЕ СРЕДЫ ВЛАДЕЛЬЦА — код, работающий у меня, падает у него
ЦЕНА: коммит упал во ВТОРОЙ раз подряд, теперь с `TypeError: unsupported operand type(s) for |: 'type' and 'NoneType'`. В `zakryt_sessiyu.py` стояла аннотация `str | None` — синтаксис PEP 604, появившийся в Python 3.10. В песочнице аналитика питон 3.10, у владельца старше, поэтому проверка прошла у одного и упала у другого, уронив вместе с собой фикстуру. **Это третий случай того же класса за трое суток:** скиллы Cowork недоступны в Claude Code (заход задавал невыполнимый гейт) · многострочная команда рвётся при вставке в чат владельца · теперь версия питона. Общее правило: **всё, что аналитик проверил у себя, проверено только у себя.** Лечится не внимательностью, а ловушкой: `fixtures/sessiya/PROGNAT.sh` теперь грепает `|` в сигнатурах и аннотациях.
ВЕРДИКТ: → гейт (ловушка 0 в `fixtures/sessiya/PROGNAT.sh`)

### 40. Ловушка, написанная против дефекта, не краснела на этом дефекте
ЦЕНА: предотвращена отрицательным контролем. Первая редакция ловушки 0 использовала класс символов `[A-Za-z.\[\]]` — в POSIX ERE обратный слэш внутри класса трактуется буквально и ломает его, поэтому греп не находил `str | None` даже в заведомо дефектном файле. Обнаружено ровно потому, что после написания ловушки я подсунул ей дефект и посмотрел, покраснеет ли. **Ловушка, которая не краснеет, хуже отсутствующей: она создаёт уверенность.** В фикстуру добавлен самоконтроль — она проверяет собственную ловушку на дефектном образце при каждом прогоне.
ВЕРДИКТ: → гейт (самоконтроль ловушки внутри `fixtures/sessiya/PROGNAT.sh`)

### 41. Инструмент против потери знания сам терял знание — одним грепом
ЦЕНА: обнаружено на живом файле в день сборки, до боевого применения. `zakryt_sessiyu.py` опознавал реплику владельца грепом `"tool_result" not in line[:400]` — и отбрасывал ЦЕЛЫЙ КЛАСС его реплик: ответы на интервью через виджет приходят в транскрипт именно как `tool_result` с текстом `The user answered`. То есть терялись ровно те реплики, которые породила новая дисциплина «заход начинается с интервью». В сессии 03.08 таких ответов было три, включая всю идею предохранителя и базы инцидентов; выгрузка потеряла бы их МОЛЧА, и результат читался бы как «владелец ничего не говорил». **Грубая эвристика в инструменте против тихой потери — не экономия, а тот же класс поломки, против которого он построен.**
ВЕРДИКТ:

### 42. Аналитик чинил греп грепом: три редакции правила подряд неверны, ни одну не нашёл автор
ЦЕНА: два круга работы захода плюс полная переделка правила. Опознание реплики владельца переписывалось трижды. Редакция аналитика («content строкой — владелец, списком — инструменты») отбрасывала владельца с вложением — поймал ПОЛОЖИТЕЛЬНЫЙ КОНТРОЛЬ существующей фикстуры, то есть заход не смог закоммитить базовую версию. Первая редакция исполнителя дала пять ложных реплик на живом транскрипте — поймал ВЕРИФИКАТОР. Принятая редакция опирается на два признака и подтверждена замером: `content` строкой есть у 203 записей `origin=task-notification`, а поля `origin` нет вовсе у 733 настоящих реплик владельца в старых транскриптах. **Разница между аналитиком и заходом была не в аккуратности: у захода были фикстура с положительным контролем и верификатор, у аналитика — убедительное рассуждение. На этом слое мера истины — прогон на живом файле.**
ВЕРДИКТ:

### 43. Приёмка отчёта расписана на шесть шагов, а носитель есть у одного
ЦЕНА: владелец проговаривает процедуру ГОЛОСОМ каждый раз, когда приходит отчёт, — дословно 03.08: «обязательно всё консолидировать, обязательно всё разнести по домам, обязательно сразу после приёмки отчёта». Правило, которое приходится проговаривать вслух на каждом применении, механизмом не является. Проверено по `RUKOVODSTVO-zahodami.md §Приёмка`: из шести шагов командой держится ОДИН — шаг 0 (`git_zona.py check --zone`). Шаг 2 «запись и закрытие хэндофф-долга», самый дорогой по собственной таблице канона (0 из 8 вопросов дошло, 1 из 17 уроков), не краснеет ничем. Живое подтверждение в тот же день: приёмка захода `dnevnik-gejt` записала два урока с ценой в `SESSIYA.md` вместо `UROKI-FABRIKE.md` — прямое нарушение правила «в дневнике только ссылка», и поймал его не гейт, а ручной прогон канонного грепа по подозрению.
ВЕРДИКТ:

### 44. `zakryt_sessiyu.py` рождает документ-сироту и роняет чужой коммит
ЦЕНА: коммит дневника арки упал с `rc=1`, и вместе с ним не доехали ПЯТЬ путей — `SESSIYA.md`, `UROKI-FABRIKE.md`, оба файла-захода и сама выгрузка. Инструмент пишет `VYGRUZKA-<дата>.md` и не зовёт `register_doc.py`, поэтому ворота 5 краснеют на документе-сироте (`KONSTITUCIYA §9`). Падает при этом не тот, кто нарушил, а следующий коммит целиком. Класс уже известен и уже лечился на `bootstrap_arka.py` (заход `kod_dobivka-registracii.md`, пять сирот на каждую новую арку) — **лекарство не было обобщено на остальные инструменты, создающие `.md`, и вернулось через три дня на новом инструменте.** Это ось Ф: решение не вернулось в фабрику.
ВЕРДИКТ:

### 45. Аналитик печатал стартовую строку не того канала запуска — дважды подряд
ЦЕНА: владелец получил нерабочую инструкцию на оба захода сессии и сказал прямо: «мы сейчас Claude Code не через терминал используем, а через встроенную систему». У `bootstrap_zahod.py` есть флаг `--kanal terminal|app` ровно для этого, канон описывает оба канала (`RUKOVODSTVO §Два канала запуска`), а дефолт — `terminal`. Аналитик взял дефолт и не спросил, чем владелец пользуется. **Канал запуска — параметр среды владельца, а не предпочтение аналитика; дефолт молча выбирает за него.** Отдельный дефект формы B: модель в ней передаётся словами («Модель: Sonnet»), а не флагом — канон сам называет это местом для ошибки, которое уже стреляло.
ВЕРДИКТ:

### 46. Отчёт велено писать туда, куда у читателя нет доступа
ЦЕНА: три приёмки подряд открыли файл-заход в основной папке, увидели пустые секции и объявили владельцу, что отчёта нет. Первый раз — круг с переспросом исполнителя («может, Соннет что-то напутал?»), дальше владелец каждый раз перекладывал файлы руками и потребовал прекратить. Причина не в исполнителе: аналитик сам вписал в контракт зоны строку «ОТЧЁТ пиши в свою копию файла-захода — ту, что в worktree», и исполнители делали ровно написанное. Правило выглядело логичным (у отчёта один дом, он едет вместе с кодом) и было негодным по одной причине: **worktree недоступен ни владельцу в чате, ни аналитику в песочнице**. Обобщение шире отчётов: **канал доставки выбирается по тому, у кого есть ДОСТУП, а не по тому, где логичнее лежать.** Артефакт в месте, куда читатель не дотягивается, не существует — тот же класс, что документ-сирота.
ВЕРДИКТ:
