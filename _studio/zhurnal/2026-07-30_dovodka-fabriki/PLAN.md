# ПЛАН арки «2026-07-30_dovodka-fabriki»

> **Вход в следующую сессию — `HANDOFF-2026-07-31.md`, он читается ПЕРВЫМ.** Контракт арки — `TZ.md`. Здесь только волны, порядок и состояние.

## ⭐ СЕЙЧАС

**✅ Волна A ПРИНЯТА** 30.07 ~03:20, коммит `bb1daf6`. Артефакт `ETALON-DIF.md`, инструмент `promer_dekov.py`.
**✅ Первые четыре гейта ПРИНЯТЫ** 30.07 ~04:10, коммит `6d4b604`. Гейт деков умеет краснеть: эталон `buffon/src --lint` → `rc=0`, `teorkat` → 9 ✗ `rc=1`, `dandelin` → 6 ✗ `rc=1`, `fibonacci` → 4 ⚠ `rc=0`. Проверено приёмкой независимо.

**✅ Ночной заход `kod_vychitka.md` ПРИНЯТ** 31.07 ~06:30, коммиты `aa5b00e` + `f38da5a`, 33 файла. Все шесть гейтов §2 прогнаны приёмкой независимо на живых файлах: 132 хэша пересчитаны сама (расхождений 0) · `check_vychitka.py` → 529 из 529, `rc=0` · вердикты 1/4/1/11/1/1 не тронуты · 461 = 461 = 461 по адресам · 330 + 131 = 461 по кратности. Собственный верификатор приёмки: 37 из 38 кандидатов уже в корпусе. Три дефекта названы уроками 6–8, работа ими не отменяется. **Волна C получила базу: 461 кандидат из 28 арок, 152 группы по механизму поломки.**

🔴 **ПЕРВЫМ ХОДОМ — хвост git владельца.** Пять путей вне git в зоне арки, включая `HANDOFF-2026-07-31.md`, который никогда не ставился. Песочница коммитить не умеет — команды владельцу выданы в чат 31.07.

🔴 **ДАЛЕЕ ТОЧЕЧНАЯ ДОЧИСТКА, до новых заходов — сверить порог проверки «порция текста на клик».** Текст `✗`-сообщения печатает «порог 4 — худший слайд эталона `buffon/sl-polygons`», а это обоснование **не воспроизводится**: у `sl-polygons` максимум 2 блока на сцену, ни один слайд эталона не даёт 4. Плюс два замера расходятся по составу нарушителей teorkat: `promer_dekov.py` назвал 8 слайдов, `lint()` даёт 7 других (s01=9 и s02=5 в первом списке отсутствуют). Задача: сверить два замера на одном деке, назначить порог по фактическому максимуму эталона среди многосценовых слайдов, переписать сообщение так, чтобы число ссылалось на воспроизводимую величину. Урок 3 в `UROKI-FABRIKE.md`.

**Дальше:**
1. ✅ ~~**Реестр вычитки + `check_vychitka.py`**~~ — сделано ночным заходом, принято 31.07. Единица учёта СЕКЦИЯ файла (`RESHENIYA Р50`), 529 секций, хэш блоба у каждой.
1б. **СУД над корпусом — следующий крупный кусок волны C.** 461 сырой кандидат ждёт судьи: склейка дублей, вердикты, отбор в канон. Порядок задан кратностью, не хронологией: группы 1–5 `KRATNOST.md` (22+17+15+14+12 = 80 инцидентов) идут первыми. Прицельный добор — один опознанный класс недобора (пересказ чужого инцидента внутри «КАК ПРОВЕРЕНО», урок 8), перечитывать всё не нужно.
2. **Три обхода гейтов, найденные верификатором** (`kod_gejty-lint.md §ВОПРОСЫ` п.3): дубль `id` под другим именем файла невидим (`chapter_base_name` ловит по имени, не по факту) · сирота прячется в HTML-комментарии · кириллица уходит в `<title>`/`aria-label`. Каждый — рецептом, ни один не закрыт.
3. **Гейты №5–9 из `ETALON-DIF.md`**: стена текста · слайд без вёрстки (WARN) · `accent_tag` · `grids.css` как слой (`RESHENIYA Р54`) · прокси G3.

⚠ **Столкновение с аркой `2026-07-30_dovodka-l1`, держать в голове.** Гейт теперь валит сборку `teorkat-vvedenie` (9 ✗). Пока та арка собирает опросник — конфликта нет. Её заход-правка после диктовки в гейт упрётся, и это придётся решать сознательно: 2 узла кириллицы в рисунках и 7 слайдов с порцией текста — ровно та работа, которую она и планирует, но гейт блокирует сборку до её завершения.

**✅ Точечная проба склейки 526 → 97 — сделана, `PROBA-526-97.md`.** 2 источника из 6, 11 кандидатов с названной ценой: 8 дошли до одного из 97 уроков, **2 не дошли**, 1 пограничный. ⇒ По правилу «нашлось — гоним по остальным четырём» проба РАЗВОРАЧИВАЕТСЯ в полный проход по оставшимся 4 источникам `konspekt-l1`. Два найденных потерянных: теорема 113 / определение 112 стояли `ПОКАЗЫВАЕМ` вместо `НАЗЫВАЕМ` (цена — вкладка 10 переоценена на 3,2 минуты) · порог велено калибровать по гистограмме при непрерывном распределении (цена — полчаса замера и весь шаг 3 по ложной линейке).

## Шесть волн и порядок

Порядок выбран аналитиком по просьбе владельца («может быть надо решать по очереди, ты сам скажи»). Замер времени сознательно опущен с первого места: он мерит не то, что владельца не устраивает (`TZ §1`).

| # | волна | тип | зависит от | почему здесь |
|---|---|---|---|---|
| **A** | **Эталонный диф** — чем `buffon` отличается от деков, которые не нравятся | аналитический, read-only | — | единственный ПОЛОЖИТЕЛЬНЫЙ критерий: все 209 уроков описывают, что сломалось, ни один — что вышло хорошо |
| **B** | **Невлитая ветка `zahod/dek-paskal-v2`** — 20 коммитов, 222 файла, 14 094 строки | аналитический → мутирующий | — | половина волны E **уже написана**: генератор служебных слайдов из `brief.md`, `gen_ill.py` (745 строк), `gates.py`, аксиоматизация шага 6. Лекцию Л1 собирали фабрикой, у которой этого не было |
| **C** | **Полная добыча уроков назад** + реестр вычитки + чекер | аналитический, волнами | A (даёт критерий приоритета) | 169 kod-файлов не читаны, 73 урока без вердикта, 13 арок без файла уроков. Три сущности не смешивать — `TZ §4` |
| **D** | **Архитектура домов** — одно правило, один дом, один носитель | организационный | C | против шизофрении. Метрика — доля правил с носителем, не число правил (`TZ §5`) |
| **E** | **Дизайн-система и запреты в КОД генератора** | мутирующий | A, B | канон, живущий прозой, при сборке не читается ⇒ переносим туда, где падает |
| **F** | **Замер и удары** — что сдвинулось | аналитический | A–E | 92 отчёта со временем, 113 с токенами, git log как хронология. Каждый удар — с числом, названным заранее |

**A и B независимы по зонам** и могли бы идти параллельно; идут последовательно, потому что приёмка двух отчётов одновременно грузит владельца сильнее, чем экономит часы. Если A уйдёт надолго — B запускать вторым worktree (`git_zona.py worktree`), не checkout'ом.

## Шаги

### Волна A
- [ ] `kod_etalon-dif.md` — написан 30.07, ждёт запуска. Выход — `ETALON-DIF.md`: пер-слайдовый замер четырёх деков + таблица различий с классом КОД / ПРАВИЛО ТЕКСТА / РЕШЕНИЕ АВТОРА.
- [ ] Приёмка по гейтам. Главный: **≥3 различия класса КОД с адресом в `_generator/`**. Их ноль ⇒ заход выдал эссе про дизайн, вернуть на дочистку.

### Волна B
- [ ] Заход-инвентарь ветки (read-only, `git show`, без переключения ветки): что применимо к дековому конвейеру вообще, а не только к Паскалю; что ломается при вливании; чего в ветке нет из ожидаемого.
- [ ] Решение владельца: вливать целиком, вливать по кускам или переносить отдельные инструменты. **Вливание требует руки владельца** — песочница не коммитит.

### Волна C
- [ ] Заход на **реестр вычитки** + `check_vychitka.py`: таблица «путь · хэш блоба · кем прочитан · когда», чекер печатает «прочитано N из 189» и краснеет на файле, чей хэш разошёлся с пометкой.
- [ ] Заход на расширение `check_uroki.py`: сейчас смотрит один файл; нужен проход по всем аркам, чтобы 13 арок без файла уроков и 73 сироты были видны воротам, а не мне грепом.
- [ ] Добыча волнами по домам: `paskal` (17 без вердикта, второй случай той же болезни) → `fibonacci` → `mat-kostyak` (29) → `teorkat-l1` (14) → `catalan` + `kurs leto 2026` → остальное.
- [ ] Просудить 73 существующих урока без вердикта — вместе с добычей их арки, одним заходом на арку.

### Волна D
- [ ] Аудит: пары правил, которые нельзя выполнить одновременно. Образец с ценой — «регистрируй новый `.md`» × «`docs/` только на чтение»: три обхода `pre-commit` тремя заходами подряд.
- [ ] Аудит: фазы 4–5–6–7 и предвёрсточный документ — контракт входа/выхода. *NB: аксиоматизация шага 6 уже сделана в ветке волны B — проверить, не закрыт ли пункт до начала работы (`README §4`: предпосылку сверять по живым файлам).*
- [ ] Развести стадии ресёрча (задачи · литература · научпоп) — владелец: «падает как снежный ком».
- [ ] Таблица «правило → один дом → носитель». Правила без носителя переименовать в надежды честно.

### Волна E
- [ ] Дизайн-система: шрифт и цвет на каждый вид элемента, точка правки одна. Референс — TeX-код владельца (`TZ §7`, не пришёл).
- [ ] Запреты в код: кириллица внутри рисунка (кроме коммутативных диаграмм — им отдельный шрифт) · минимальная площадь иллюстрации · число иллюстраций на слайд · стрелки.
- [ ] Генератор служебных слайдов — обложка, финал, разделители «просто слово написать». *Проверить, не решено ли в ветке B до того, как писать.*

### Волна F
- [ ] Замер: куда ушёл wall-clock по 92 отчётам + git log.
- [ ] По каждому внедрённому удару — число до и после. Не сдвинулось ⇒ удар не сработал, и это видно.

<!-- при закрытии арки — баннер «✅ АРКА ЗАКРЫТА (<дата>)» сверху (ARKA §7 п.4 / §10 C) -->
