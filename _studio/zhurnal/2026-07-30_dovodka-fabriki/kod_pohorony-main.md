# Канал исполнителя — pohorony-main (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — две точечные правки в названных функциях и одна команда; решать по ходу нечего.

КОНТЕКСТ. `materials/` — монорепо курсов и фабрики их производства. Сайт переехал в `sayt/` рабочей ветки, workflow собирает оттуда, **владелец подтвердил живую публикацию** по адресу `https://d1-d57.github.io/materials/`. Ветка `main` больше ничего не публикует и подлежит захоронению — но предыдущий заход не смог её закрыть, и по хорошей причине.

**Две поломки, обе — устаревшие факты, вшитые в код.**

1. **Сторож отказывает по факту, которого больше нет.** `git_zona.py:841`: `NELZYA_ZAKRYVAT = ("main",)`, отказ печатает «`main` не закрывается никогда — это ветка публикации сайта». Утверждение было верным до сегодня и стало ложным: публикуется `arka/mat-kostyak`, что видно в `.github/workflows/hugo.yml` (`branches: ["arka/mat-kostyak"]`). Правило вшило ИМЯ вместо СВОЙСТВА — и молча пережило факт.
2. **Вердикт «шум» нельзя закрыть в день его вынесения.** Вчерашняя правка дала `check_incidenty.py` четвёртый случай `exit 1`: класс с вердиктом «шум», получивший новые строки, краснеет. Логика верная, реализация упирается в разрешение времени: у вердикта только ДАТА, у строк инцидентов — дата И ВРЕМЯ. Внутри одного дня порядок «вердикт → строка» не восстановить, и предыдущий исполнитель честно это назвал, добавив костыль «same-day считается подозрительным, если вердикт датирован сегодня». Следствие: **гейт сейчас красный и закрыть его сегодня нечем** — обновление даты даёт ту же дату.

ЦЕЛЬ: починить обе, похоронить `main`.

Приёмка — по ОТЧЁТУ, без построчной сверки. Флага «стоп до цели» нет.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/tools/git_zona.py` `_generator/tools/check_incidenty.py` `_studio/zhurnal/_INFRA-git/VERDIKTY.md` `_generator/tools/fixtures/incidenty/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_pohorony-main.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- Проверь ветку: `git branch --show-current` — обязано быть `arka/mat-kostyak`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `_generator/tools/git_zona.py` `_generator/tools/check_incidenty.py` `_studio/zhurnal/_INFRA-git/VERDIKTY.md` `_generator/tools/fixtures/incidenty/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_pohorony-main.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: <названные файлы-якоря>. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Три шага строго по порядку: захоронение возможно только после правки 1, а зелёный гейт — только после правки 2.

**Правка 1 — сторож проверяет СВОЙСТВО, а не имя.**
`NELZYA_ZAKRYVAT = ("main",)` заменить на вычисление: **не закрывается та ветка, с которой публикуется сайт**, а это читается из `.github/workflows/hugo.yml`, поле `on.push.branches`. Файла нет или поле не читается → веди себя как раньше, консервативно (не закрывать ничего, о чём не смог убедиться), и скажи об этом в выводе.
Текст отказа тоже перепиши: он должен называть, ОТКУДА взято знание («ветка публикации по `.github/workflows/hugo.yml`»), а не утверждать факт голословно.

*Почему именно так, а не «убрать main из списка»: список имён устареет ровно так же, как устарел сейчас. Правило должно смотреть на живой файл. Это тот же класс дефекта, что чинили сегодня трижды — вшитый факт, переживший свою истинность.*

**Правка 2 — у вердикта появляется ВРЕМЯ.**
В `VERDIKTY.md` поле `дата данных:` получает формат `ГГГГ-ММ-ДД ЧЧ:ММ` (строки инцидентов уже такие). Сравнение в `check_incidenty.py` становится строгим `>` по полному штампу, а костыль «same-day, если вердикт датирован сегодня» **убирается** — он больше не нужен и вредит: делает гейт незакрываемым в день вынесения.
Совместимость: вердикт со старым форматом (только дата) читать как `ГГГГ-ММ-ДД 00:00` — тогда все сегодняшние строки считаются новыми, что верно и консервативно.
Шапку `VERDIKTY.md` с описанием формата обнови тем же ходом.

**Шаг 3 — закрыть красное и похоронить `main`.**
Сначала пересмотри два класса и впиши вердикты со временем — это содержательная работа, не формальность:
- **`E конфликт путей`** — по-прежнему шум: конфликты при merge это обычная жизнь ветвления.
- **`C прочий чужой долг`** — «шум» больше не годится. Внутри лежит строка 04.08 04:33 про `check_ves.py`, и это был НАСТОЯЩИЙ дефект, **уже починенный** заходом `kod_korzina-i-dolgi.md` (коммит `fd41e84`). Вердикт должен это отражать: класс-корзина, последний разбор такой-то, найденный дефект закрыт.

Затем:
```
python3 _generator/tools/git_zona.py zakryt-vetku --branch main
```
Обратимо: заводит надгробие `mogila/main`, возврат — `voskresit --branch main`. Метрика уже зелёная (`poteri` → «потерь нет, проверено 1 из 1»); покраснеет — **СТОП, `--vsyo-ravno` НЕ применять**, строкой в `## ВОПРОСЫ`.
🔴 **`origin/main` на GitHub НЕ трогай** — она ничего не запускает и служит вторым слоем страховки поверх надгробия.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
```
1. фикстура incidenty: старые случаи + новый «вердикт со временем, строка позже» → все зелёные
2. живой прогон check_incidenty.py                        → rc=0 (сейчас 1)
3. живой прогон: zakryt-vetku --branch arka/mat-kostyak   → ОТКАЗ со ссылкой на workflow
   (способность сторожа краснеть сохранена — проверять на ТЕКУЩЕЙ ветке безопасно)
4. git branch | wc -l                                     → 1
5. git show-ref | grep mogila/main                        → надгробие есть
6. git branch -r | grep origin/main                       → есть, не тронута
7. финальный коммит БЕЗ --no-verify                       → обходов 0
```
Пункт 3 — главный по риску: правку 1 легче всего «сделать», разрешив закрывать вообще всё.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** «ложных срабатываний нет, проверено 12 классов из 12».

## 3. ВЕРИФИКАТОР

Ты снимаешь предохранитель с удаления веток — это ровно тот случай, когда автор себя не проверяет.

Свежий субагент, ДРУГОЙ метод, на копии репозитория (`git clone` в `/tmp`, живой не трогать): пусть подсунет четыре ситуации и проверит, что сторож ведёт себя правильно в каждой.
- workflow называет `arka/mat-kostyak` → закрытие `main` РАЗРЕШЕНО, закрытие `arka/mat-kostyak` ЗАПРЕЩЕНО;
- workflow переписан обратно на `main` → закрытие `main` ЗАПРЕЩЕНО;
- файла workflow нет вовсе → закрытие ЗАПРЕЩЕНО (консервативный исход);
- поле `branches` пустое или битое → закрытие ЗАПРЕЩЕНО.

Таблица «ситуация · ожидание · факт», расхождений ноль. Отдельно: убедиться, что надгробие после закрытия действительно позволяет `voskresit` вернуть ветку с тем же хэшем.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- `_generator/tools/git_zona.py` `_generator/tools/check_incidenty.py` `_studio/zhurnal/_INFRA-git/VERDIKTY.md` `_generator/tools/fixtures/incidenty/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_pohorony-main.md`                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- `_generator/tools/git_zona.py` `_generator/tools/check_incidenty.py` `_studio/zhurnal/_INFRA-git/VERDIKTY.md` `_generator/tools/fixtures/incidenty/` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_pohorony-main.md`   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — их снимает ПРИЁМКА из лога прогона** (исполнителю счётчик недоступен: он снаружи его сессии, и `/cost` не существует — команда называется `/usage`. Извиняться за это не нужно и оценку писать не нужно: число печатает сама стартовая команда в `result`-строку лога) / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка проверена:** `arka/mat-kostyak` — совпадает. Зона в HEAD не была чиста (сам файл-заход untracked) → точка отката сделана: коммит `fe6ca82`.

**Прочитано:** `git_zona.py` (окрестности `NELZYA_ZAKRYVAT`, `zakryt_odnu`, `cmd_zakryt_vetku`), `check_incidenty.py` целиком, `VERDIKTY.md`, `.github/workflows/hugo.yml`, `fixtures/incidenty/PROGNAT.sh`, живые строки `INCIDENTY.md` классов E/C, история коммитов `001919e`/`fd41e84`/`kod_korzina-i-dolgi.md` (откуда взялся костыль и чем закрыт настоящий дефект C).

**Правка 1 (`git_zona.py`).** `NELZYA_ZAKRYVAT` статичным кортежем — на функцию `vetka_publikacii()`: читает `.github/workflows/hugo.yml` (`on.push.branches`; ловушка PyYAML — bare `on:` парсится как булев `True`, проверено эмпирически на живом файле — обе формы ключа проверяю). Файла нет / YAML не разбирается / поле пустое или не список строк → `(None, причина)`. Новая `zapret_zakrytiya(name)` вызывается в ОБОИХ местах, где раньше стоял `name in/not in NELZYA_ZAKRYVAT` (строка 956 — одиночная проверка, строка 1190 — фильтр `--vse-zelenye`): при известной ветке публикации сообщение цитирует источник (`.github/workflows/hugo.yml`, `on.push.branches`); при неизвестности — отказывает закрывать ЛЮБУЮ ветку и объясняет почему (буквально «не закрывать ничего, о чём не смог убедиться» — гарантии нет ни на одну, значит запрет на все, а не угаданный один).

**Правка 2 (`check_incidenty.py`).** `VERDIKT_RE` — группа даты принимает необязательное ` ЧЧ:ММ`. `klassifikaciya()` берёт из строки инцидента `r[2:18]` (полный штамп `ГГГГ-ММ-ДД ЧЧ:ММ`, он уже есть в каждой строке) вместо `r[2:12]` (только дата). Новая `data_polnaya()` — старый формат вердикта (10 символов) читает как `+ " 00:00"`. `dnej_nazad()` режет на первые 10 символов перед `strptime`, чтобы полный штамп не ломал возрастные вычисления (свежесть без вердикта, молчание вердикта «дефект»). В `ocenit()` ветка `шум`: костыль `d == v.data_dannyh and v.data_dannyh == segodnya_str` убран, сравнение — строгое `>` по `data_polnaya(v.data_dannyh)` против полного штампа строки. Предложенная строка вердикта в подсказках гейта получает реальное время (`datetime.now()`), раз формат теперь его требует.

**Правка `VERDIKTY.md`.** Шапка: описание формата `дата данных:` → `ГГГГ-ММ-ДД ЧЧ:ММ`, строкой ниже — примечание о совместимости (старый формат = полночь). Класс **E** — вердикт «шум» остаётся содержательно тем же (конфликты merge — обычная жизнь), получает время СЕЙЧАС (позже всех сегодняшних строк класса, последняя — 02:17). Класс **C** — вердикт переписан по существу: класс-корзина (разнородный `--no-verify`-долг), последний разбор сегодня, единственный найденный настоящий дефект (`check_ves.py`, строка 04.08 04:33) закрыт заходом `kod_korzina-i-dolgi.md` (коммит `fd41e84`) — «шум» как диагноз «здесь никогда ничего не бывает» был неточен, теперь текст называет находку прямо. Обе даты данных — позже последней сегодняшней строки своего класса (иначе сам же гейт их и покрасит).

**Правка `fixtures/incidenty/PROGNAT.sh`.** Случай 4 (историческое совпадение дня, вердикт «шум» 9 дней назад) — старый формат без времени под НОВОЙ логикой (compat = полночь) красил бы его: любая небудничная строка того же дня технически «позже полуночи». Добавляю вердикту явное время, позже времени строки — сохраняю исходный смысл теста (совпадение дня не ломает «шум»), но через новый механизм, а не через угадывание формата. Случаи 1–3, 5 не трогаю — не зависят от времени. Добавляю случаи 6 (вердикт со временем, строка ТОГО ЖЕ дня РАНЬШЕ по часам → зелёный — ровно тот сценарий, который чинит правка 2 и который живой класс C проходил красным) и 7 (та же пара, строка ПОЗЖЕ вердикта → по-прежнему красный, чтобы «шум» не стал вечным).

**Порядок исполнения:** правка 1 → живой прогон п.3 критерия (отказ на `arka/mat-kostyak`) → правка 2 + фикстуры → живой прогон `check_incidenty.py` (rc=0) → VERDIKTY.md → фикстура зелёная целиком → верификатор (§3, отдельный субагент на клоне в `/tmp`) → `zakryt-vetku --branch main` → проверка п.4–6 критерия → коммит (§4).

**Не оспариваю критерий готовности** — пункты проверяемы буквально, план их покрывает по порядку.

## ВОПРОСЫ — (заполняет исполнитель)

1. Формулировка правки 1 «файла нет или поле не читается → веди себя как раньше, консервативно (не закрывать ничего, о чём не смог убедиться)» прочиталась мной СНАЧАЛА буквально широко — «не закрывать вообще ни одну ветку», и первая версия правки так и делала. Это была бы РЕГРЕССИЯ: в любом репозитории без `.github/workflows/hugo.yml` (например, синтетический временный репозиторий, который `git init` создаёт фикстура `fixtures/git_zona/PROGNAT.sh` — она вне моей зоны) закрытие ЛЮБОЙ ветки стало бы невозможным. Поймано только потому, что `fixtures/git_zona/PROGNAT.sh` (чужая зона, не моя) гоняется тем же pre-commit хуком, что и моя правка, и покраснела на первом коммите — 20+ находок разом. Если бы этот хук не связывал чужую фикстуру с моими путями (или фикстуры не было вовсе), первая версия ушла бы в коммит незамеченной: моя собственная зона (`fixtures/incidenty/`) эту дыру устроена проверить не могла — она про другой гейт. Исправлено: «как раньше» — это буквально «под защитой по умолчанию `main`, ровно то имя, что было вшито», не «заблокировать всё»; перепроверено верификатором на клоне ДО итогового коммита.
   ДОМ: _studio/zhurnal/2026-07-30_dovodka-fabriki/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + зачем.**
- **Правка 1** (`git_zona.py`): `NELZYA_ZAKRYVAT = ("main",)` заменён на `vetka_publikacii()` + `zapret_zakrytiya()` — ветка публикации читается из `.github/workflows/hugo.yml`, поле `on.push.branches` (сейчас там `arka/mat-kostyak`), а не вшита именем. Обе точки вызова (`zakryt_odnu`, фильтр `--vse-zelenye`) переведены на новую функцию. Ловушка PyYAML — голое `on:` парсится как булев `True`, не строка `"on"` — обработана явно (проверено эмпирически на живом файле). Файл не читается/поле пустое или битое → «веди себя как раньше»: под защитой по умолчанию ровно `main` (то самое вшитое имя), не любая ветка — см. `## ВОПРОСЫ` п.1 про то, как я сперва сделал шире и поймал регрессию до коммита.
- **Правка 2** (`check_incidenty.py`): `VERDIKT_RE` принимает `дата данных: ГГГГ-ММ-ДД[ ЧЧ:ММ]`; `klassifikaciya()` берёт из строки инцидента полный штамп (`r[2:18]`) вместо одной даты; новая `data_polnaya()` — старый формат вердикта читается как полночь (обратная совместимость); `dnej_nazad()` режет на первые 10 символов перед `strptime`. Сравнение «шум» — строгое `>` по полному штампу; костыль `d == v.data_dannyh and v.data_dannyh == segodnya_str` убран. Подсказки гейта («строка для VERDIKTY.md») теперь печатают реальное время.
- **`VERDIKTY.md`**: шапка описывает новый формат даты и правило совместимости. Класс **E** (конфликт путей) — содержание не меняю (по-прежнему шум: конфликты merge — обычная жизнь), только время. Класс **C** (прочий чужой долг) — вердикт переписан по существу: класс-корзина, последний разбор 2026-08-04, единственный найденный настоящий дефект (check_ves.py, строка 04.08 04:33) закрыт заходом `kod_korzina-i-dolgi.md` (коммит `fd41e84`).
- **`fixtures/incidenty/PROGNAT.sh`**: случай 4 получил явное время у вердикта (иначе под новой логикой историческое совпадение дня стало бы красным — старый формат=полночь красит ЛЮБУЮ строку того же дня, а не только сегодняшние). Добавлены случаи 6 (вердикт со временем, строка того же дня раньше → зелёный) и 7 (та же пара, строка позже → красный) — это ровно живой сценарий класса C, который чинит правка 2.
- **Похороны**: `zakryt-vetku --branch main` — надгробие `mogila/main` на `86037c14`, `origin/main` не тронута.

**Как проверил.**
- Фикстура `fixtures/incidenty/PROGNAT.sh` — 7/7 зелёных (случаи 1-5 старые + 6-7 новые).
- Живой `check_incidenty.py` — было rc=1 (классы C и E без времени), стало rc=0 после правки VERDIKTY.md.
- Живой `zakryt-vetku --branch arka/mat-kostyak` на текущей ветке → отказ со ссылкой на `.github/workflows/hugo.yml` (критерий п.3).
- `git branch | wc -l` → 1; `git show-ref | grep mogila/main` → надгробие есть; `git branch -r | grep origin/main` → есть, не тронута (критерий п.4-6).
- Фикстура `fixtures/git_zona/PROGNAT.sh` (чужая зона, не редактировал) — прогнал ДО и ПОСЛЕ правки 1: до — зелёная (0 находок на HEAD), после первой версии правки — 20+ находок (регрессия, см. `## ВОПРОСЫ` п.1), после исправления — снова зелёная. Гоняется автоматически pre-commit хуком инструмента при правке `git_zona.py`, не мной вручную сверх этого.
- **Верификатор (§3)**: два прогона свежим субагентом на `git clone` в `/tmp` (второй — после того, как поймал и исправил регрессию из п.1; первый прогон тестировал уже неактуальную семантику и не в счёт). Финальный: 4 ситуации × 2-3 имени ветки = 8 проверок, расхождений 0. Подтверждено: `arka/mat-kostyak` в workflow → `main` закрывается, `arka/mat-kostyak` — нет; workflow переписан на `main` → `main` не закрывается; файла нет / поле пустое / поле битое (`null`, ключ отсутствует) → блокируется ИМЕННО `main`, любое другое имя — свободно. Отдельно проверено `voskresit`: тестовая ветка закрыта и поднята обратно на тот же хэш побайтово.

**Что НЕ трогал.** Всё вне зоны. В частности: `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — во время сессии стал грязным (2 новые строки, обе датированы 04:33, что НЕ совпадает с реальным временем моих действий ~13:47-14:10) — это не моя правка и не мой автолог; файл не в зоне, не коммитил, не трогал. `origin/main` на GitHub — не тронута, как предписано. `.github/workflows/hugo.yml` — только читал.

**Необратимое.** `git branch -D main` внутри `zakryt-vetku` — обратимо: надгробие `mogila/main → 86037c14` стоит, `voskresit --branch main` вернёт ветку на тот же хэш (механизм проверен верификатором на другой ветке). Больше необратимого не было.

**АРТЕФАКТ:** артефакта нет — это правка инструментов и данных, не сборка (нет HTML/PDF/документа для просмотра).

**КОММИТ:** `c819c08` — «pohorony-main: сторож смотрит на workflow, а не на имя `main`; «шум» сравнивается по времени» · `git_zona.py check --zone _generator` → ✅ · `check --zone _studio/zhurnal/_INFRA-git` → ❌, но исключительно на чужом `INCIDENTY.md` (см. «Что НЕ трогал») — мой путь в этой зоне, `VERDIKTY.md`, доехал. Второй коммит — сам этот файл с дописанным `## ОТЧЁТ` — следующим ходом сразу после этой строки.
