# Канал исполнителя — uborka-vetok (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу, В ЭТОМ ЖЕ ФАЙЛЕ. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — заход УДАЛЯЕТ и СЛИВАЕТ; цена ошибки высокая, решения принимаются по замерам, а не по образцу.

КОНТЕКСТ. Репозиторий `materials/` — монорепо на одного владельца. За 03.08 построен жизненный цикл ветки: метрика «что пропадёт при удалении», надгробия `mogila/<имя>`, команды `poteri` · `zakryt-vetku` · `voskresit` · `mogily`. Веток было 17, стало 7.

🔴 **ЦЕЛЬ: довести до минимума и убрать то, что мешает механизму работать.** Владелец: *«лучше, чтобы она была одна… просто всё закоммитить, слить, актуализировать и закрыть»*. Прямо сейчас `zakryt-vetku --vse-zelenye` закрывает **ноль веток из семи** — и отказывается по правильной причине: в трёх рабочих папках лежат незакоммиченные файлы, а это единственный необратимый канал (надгробие держит коммиты, файл вне git не держит ничто).

**Механизм работает верно. Мешает мусор, и разобрать его — ручная работа, которую нельзя делать вслепую.**

Приёмка — по ОТЧЁТУ. Если стоп до цели: часть A важнее всего; сделай её первой.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **ПЛАН, ВОПРОСЫ, ОТЧЁТ и УРОКИ ФАБРИКЕ пиши В ЭТОТ ФАЙЛ — тот, по абсолютному пути, что назван в стартовой строке, в ОСНОВНОЙ папке репозитория.**
- 🔴 **ЗАХОД РАБОТАЕТ В ОСНОВНОЙ ПАПКЕ, worktree НЕ заводит** — он как раз рабочие папки и разбирает. Ветку `arka/mat-kostyak` **не переключай**.
- **ЗОНА (можно менять):** `_studio/zhurnal/_INFRA-git/INCIDENTY.md` `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` `_studio/zhurnal/2026-07-30_dovodka-fabriki/kod_uborka-vetok.md`, плюс **разбор незакоммиченных файлов в рабочих папках `materials-wt/*`** (часть A) и **операции с ветками через `git_zona.py`** (часть B).
- 🔴 **ВСЕ операции с ветками — ТОЛЬКО через `git_zona.py`.** Голый `git branch -d/-D`, `git merge`, `git push`, `git reset` **запрещены**: дверь ставит надгробие, сверяет вершину перед удалением и печатает команду воскрешения, а голый git ничего этого не делает. Нужной операции нет в двери — **в `## ВОПРОСЫ`, не изобретай обход**.
- 🔴 **`main` НЕ ТРОГАТЬ ВООБЩЕ** — ни слить, ни удалить, ни переключить. Это ветка публикации сайта; часть C только ИССЛЕДУЕТ её роль и докладывает.
- **ЗАПРЕТ:** ничего за пределами зоны. `_generator/tools/` не править — инструменты закончены, здесь только их применение.

## 0. ПЕРВЫЙ ХОД
- Не заводи worktree. `git branch --show-current` — запиши, что показало, и **не меняй**.
- **Проверь свежесть базы:** `python3 _generator/tools/git_zona.py mogily` должен отработать. Ошибка — СТОП и в `## ВОПРОСЫ`.
- Прочитать ТОЛЬКО: `_studio/docs/kak-delat/GIT-disciplina.md` (целиком — там жизненный цикл ветки и таблица команд), вывод `python3 _generator/tools/git_zona.py doctor` и `poteri`. Проект сверх этого не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** *Цена 21.07: `rc=128` четырежды прочитан как результат.*
🔴 **Только stdlib** (если пишешь вспомогательное). Python владельца — **3.9.6**.
🔴 **Ты удаляешь. Каждое необратимое действие называется в отчёте ОДНОЙ строкой: что · надгробие · команда воскрешения.**
🔴 **Мера истины — прогон, а не рассуждение.** За 03.08 живой прогон нашёл то, чего не нашла ни одна фикстура, шесть раз подряд.

## 2. ЗАДАЧА

### ЧАСТЬ A — разобрать мусор в рабочих папках (делать первой)

`zakryt-vetku --vse-zelenye` отказал на двух ветках из-за незакоммиченных файлов. Известные:
- `materials-wt/sayt` → `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, `.claude/hook-test.log`
- `materials-wt/tirazh-ocheredi` → `_studio/zhurnal/_INFRA-git/INCIDENTY.md`

**Пройди ВСЕ рабочие папки** (`git_zona.py doctor` или `git worktree list`), в каждой посмотри, что вне git, и **по каждому файлу прими решение с обоснованием**:

| что это | что делать |
|---|---|
| `INCIDENTY.md` — автолог обходов хука | содержательные строки перенести в основную папку одним коммитом; дубли не плодить — **сверь, не записана ли строка уже** |
| `.claude/hook-test.log`, `*.bak`, следы прогонов | мусор — удалить, назвав в необратимом |
| что-то ещё | **не угадывай: опиши в `## ВОПРОСЫ` и оставь на месте** |

🔴 **Ни один файл не выбрасывается молча.** Каждый — строкой в отчёте: путь · что это · решение · обоснование. *Это единственный необратимый канал во всей конструкции: надгробие держит коммиты, а файл вне git не держит ничто.*

### ЧАСТЬ B — свести ветки к минимуму

После части A прогони `python3 _generator/tools/git_zona.py zakryt-vetku --vse-zelenye`.

Ветки, оставшиеся незакрытыми, **разбери поимённо**: что в них уникального, почему метрика краснеет, можно ли содержимое перенести `adopt`-ом и закрыть после этого. **Перенос — законный ход; удаление вопреки метрике — нет.** Если ветка несёт живую работу — так и напиши, оставь и назови дату последнего коммита.

**Цель — две ветки: рабочая и `main`.** Достигнута она или нет — назови числом «было 7, стало N» и объясни каждую оставшуюся.

### ЧАСТЬ C — 🔴 ИССЛЕДОВАНИЕ, НЕ ДЕЙСТВИЕ: чем `main` отличается от рабочей ветки

Владелец спрашивает прямо: *«Я не понимаю, в чём отличается main и arka/mat-kostyak. Может быть, это всё-таки должна быть одна ветка?»*

Установи **прогоном по живым файлам**, а не рассуждением:
1. Что физически лежит в `main`, чего нет в рабочей ветке, и наоборот. Утренний замер аналитика: у `main` свои `hugo.yaml`, `layouts/`, `static/`, а шесть файлов `buffon/` есть в рабочей и отсутствуют в `main`.
2. **Публикуется ли сайт с `main`** — есть ли GitHub Actions или Pages, привязанные к ней (`.github/workflows/`). Если да, слияние веток означает изменение того, что публикуется.
3. Расходятся ли они по замыслу (ветка-публикация против ветки-разработки) или это исторический хвост.

**Вывод — рекомендация с ценой, а НЕ действие.** Три варианта минимум: оставить как есть · слить в одну · переименовать рабочую в `main` и перевесить публикацию. У каждого назови, что сломается и как откатить. **Решение принимает владелец.**

### ЧАСТЬ D — актуализировать долги

По итогам: в `_studio/docs/sostoyanie/OTKRYTYE-ZADACHI.md` — строка про то, что осталось по веткам. В `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — перенесённые строки автолога, если они были.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. Все рабочие папки прошли разбор: по каждому файлу вне git — строка с решением и обоснованием.
2. `git_zona.py doctor` из ОСНОВНОЙ папки — чисто по рабочим папкам; вывод в отчёт.
3. **«было 7, стало N»** с объяснением каждой оставшейся ветки.
4. Часть C: рекомендация из трёх вариантов, каждый с ценой и откатом. `main` не тронут — доказать выводом `git log -1 main` до и после (хэш совпадает).
5. Ни одной операции голым git — доказать: все действия в отчёте названы командами `git_zona.py`.
6. Зона закоммичена, `git_zona.py check --zone _studio` → ✅.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «мусора нет», а «мусора нет, проверено X папок из Y».

## 3. ВЕРИФИКАТОР
Свежий субагент, **проверка обратимости после уборки**: дай ему список того, что ты удалил, и попроси **восстановить каждое** по напечатанным командам, а затем сверить хэш вершины с тем, что был до удаления. Расхождение или невозможность восстановить — красное, и важнее любой чистоты. Результат в отчёт.

## 4. 🔴 КОММИТ
```
python3 _generator/tools/git_zona.py commit --zone _studio/zhurnal/_INFRA-git -m "<что>"
python3 _generator/tools/git_zona.py commit --zone _studio/docs/sostoyanie -m "<что>"
python3 _generator/tools/git_zona.py check --zone _studio    # должен быть ✅
```
🔴 **Файл-заход в коммит НЕ входит** — его коммитит аналитик при приёмке.
⚠ **Известный чужой долг:** `_studio/outputs/gipoteza-yakobiana-dajdzhest.md` — незарегистрированный документ владельца, роняет широкие коммиты по `_studio`. **Не регистрируй его сам** (описание знает только владелец) — коммить узкими зонами и назови это в отчёте.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу ЭТОГО ФАЙЛА
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / **АРТЕФАКТ** / **КОММИТ**.

🔴 **АРТЕФАКТ — ОДИН ПУТЬ ОТ КОРНЯ РЕПОЗИТОРИЯ, без прозы.** Здесь артефакт — вывод `doctor` и `poteri` после уборки; путь к логу или к правленому файлу долгов.

🔴 **НЕОБРАТИМОЕ — ГЛАВНАЯ СЕКЦИЯ ЭТОГО ОТЧЁТА.** Каждое удаление файла и ветки ОДНОЙ строкой: **что · где · чем восстанавливается**. Файлы вне git восстановлению не подлежат — для них строка обязана нести обоснование, почему выброс безопасен.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`. **ЦЕНА обязательна. Не сочиняй.**

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому — оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<один путь от корня репозитория>` — `<чем открывать>`
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone _studio` → ✅
