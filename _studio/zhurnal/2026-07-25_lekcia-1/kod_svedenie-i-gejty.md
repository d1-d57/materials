# Канал исполнителя — svedenie-i-gejty (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — суждение о смысловых дублях и о том, что именно должен ловить гейт, рождается по ходу; механикой это не расписывается.

**КОНТЕКСТ.** Проект `materials/` — монорепо; внутри готовится курс «Введение в теорию категорий». Находки живут карточками в `teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md`; рама курса — в `teorkat-vvedenie/`; работа ведётся арками в `_studio/zhurnal/`.

**Прошлый этап.** Заход `konsolidacia-l1` вычитал 132 файла / 26 717 строк десяти теоркат-арок и собрал источник правды. Он же вскрыл, что **механизм потери находок работал прямо сейчас**: 26 карточек существовали, но не стояли в индексе по роду; карточки `retrakt-priyom` не было вовсе, хотя другая карточка на этот id уже ссылалась; семь секций `SPEKA`, на которые ссылаются карточки, не существуют.

**И ветки разошлись — по вине аналитика.** Команды коммита называли зону арки, а карточки писались в `teoriya-kategoriy/kartoteka/`. Итог на входе этого захода:

| Где | Карточек |
|---|---:|
| ветка `arka/mat-kostyak` (основная папка) | ~310 |
| ветка `arka/konsolidacia-l1` | 316 |
| после сведения, до схлопывания дублей | ~326 |

**ЦЕЛЬ — три работы в жёстком порядке:** ① свести ветки и схлопнуть смысловые дубли до одного источника правды; ② написать гейт, который **не даст этому разъехаться снова**, и вшить его в хук; ③ записать дисциплину документом, на который гейт ссылается.

**Приёмка — по ОТЧЁТУ, без построчной сверки.**

🔴 **ФЛАГ «СТОП ДО ЦЕЛИ», громко и заранее.** Ты приводишь проект в консистентное состояние и ставишь защиту. Ты **НЕ** разбираешь внешние сюжеты под лекцию 1 и **НЕ** пишешь документ лекции — это следующие заходы. Не начинай их, даже если останется время.

---

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)

- **МЕСТО РАБОТЫ: ОСНОВНАЯ папка репозитория, ветка `arka/mat-kostyak` — она уже стоит.** `git checkout` **НЕ делай** и ветку не переключай. Worktree здесь не годится: слияние идёт в ветку основной папки, а два worktree на одну ветку git не даёт.
  ⚠ Владелец перед запуском дропает отработавшие рабочие папки. Увидишь чужие незакоммиченные изменения в дереве — **стоп и в отчёт**, не коммить их и не чини.
- **ЗОНА (можно менять):** `teoriya-kategoriy/kartoteka/` · `_studio/zhurnal/2026-07-25_lekcia-1/` · `_generator/tools/` · `_studio/docs/` · `.githooks/`. Всё вне — **READ-ONLY**.
- 🔴 **Отдельно вне зоны, хотя рядом:** `teorkat-vvedenie/` — рама курса. Её правит только владелец утверждёнными формулировками. Читать можно, писать нельзя, **даже если гейт покраснеет именно на ней** — тогда это долг в отчёт, а не правка.
- **КОММИТ:** `add` по своим путям, затем `commit` **с теми же путями после `--`**. НИКОГДА `-A` / `.` / `commit -am`. Субагенты не коммитят. **`--no-optional-locks` обязателен.**
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «чинится в одну строку».

## 0. ПЕРВЫЙ ХОД

- Убедиться, что ты в основной папке и на `arka/mat-kostyak`; дерево вне зоны чисто.
- **Точка отката обязательна и до всего:** зона в HEAD чиста → сделай `zip`-бэкап `teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md` в `/tmp` перед первой правкой. Слияние — операция, где откат нужен реально.
- Прочитать ТОЛЬКО названное в §3. Проект не изучай.
- **ПЛАН — в `## ПЛАН`, до всяких действий.**

## 1. ДИСЦИПЛИНА (Карпатов)

🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково. *Цена 21.07: `rc=128` четырежды прочитан как результат.*

Предпосылки и развилки — вслух; минимум без спекуляций; хирургия. **Оспорить ложную предпосылку, включая КРИТЕРИЙ ГОТОВНОСТИ** — в `## ПЛАН`, до работы.

🔴 **Числа не назначать, а считать** (`KONSTITUCIYA §10`): всякое число в выходах — результат команды, команда рядом.

**СУБАГЕНТЫ:** до 5 параллельно; рейт-лимит = отступить и доложить. Здесь они почти не нужны — работа последовательная и требует одной головы; используй разве что на верификацию (§4).

🔴 **ТРИ ФАЗЫ КОММИТЯТСЯ ОТДЕЛЬНО, И ЭТО НЕ ОПЦИЯ.** Фаза A (слияние данных) и фаза B (код гейта) — разные риски. Коммит между ними даёт точку отката: если гейт окажется неверным, откатывается он один, а сведённая картотека остаётся. Порядок нарушать нельзя ещё и потому, что **гейтом фазы B ты проверяешь результат фазы A** — он для того и пишется.

## 2. ГРАНИЦЫ

- **Не трогать раму** `teorkat-vvedenie/` — даже под давлением красного гейта (см. контракт зоны).
- **Не трогать** зону арки `2026-07-23_vneshnie-istorii/`.
- **Не разбирать** внешние сюжеты и **не писать** документ лекции — следующие заходы.
- **Не переписывать** рабочий код инструментов ради красоты: `check_kartoteka.py`, `check_uroki.py`, `git_zona.py` дописываются, а не переделываются.
- **Ничего не удалять** из картотеки: схлопывание дублей — это слияние содержания в одну карточку и **редирект-строка** на месте второй, а не удаление (§3.A3).

## 3. ЗАДАЧА

### ФАЗА A. Свести ветки и схлопнуть дубли — один источник правды

**A1. Закрыть долг инструмента: у `git_zona.py` нет команды слияния.**
Он назван прямо в корневом `CLAUDE.md` («ветка не влита; у `git_zona.py` нет команды слияния — долг инструмента»), и обходить его голым `git merge` в чат владельцу канон запрещает (`GIT-disciplina §0`: владельцу — только `git_zona.py <команда>`, никогда shell).
**Добавь подкоманду** `git_zona.py merge <ветка> [--zone …]` в идиоме файла: печатает, что вливается, требует чистого дерева, при конфликте **останавливается и печатает список конфликтных путей** — сама не разрешает. Стиль, вывод и обработку ошибок бери с соседних подкоманд, своего не изобретай.

**A2. Влить `arka/konsolidacia-l1` в `arka/mat-kostyak`** своей же новой командой.
Конфликт в `KARTA-OBLASTI.md` ожидается и предсказуем: обе стороны **дописывали в конец файла** и обе правили индекс по роду. **Разрешение — взять обе половины**, ничего не выбрасывая. Индекс пересобрать так, чтобы он покрывал объединённое множество.

**A3. Схлопнуть смысловые дубли.** Пять пар, возникших оттого, что две стороны независимо описали одно и то же:

| карточка аналитика (ветка `mat-kostyak`) | карточка исполнителя (ветка `konsolidacia-l1`) |
|---|---|
| `retrakt-parnyj-vopros-k-kategorii` | `retrakt-priyom` |
| `tri-klassifikacionnye-zadachi-karkas` | `klassifikaciya-est-forma-otveta` |
| `primery-ne-blok-a-rassypany` | `primery-vsegda-vneshnee` |
| `zakaz-metateorema-dlya-estestvennosti` | `ep-etazh-bez-instrumenta` |
| `pervaya-chetvert-nachinaetsya-s-biekcij` | `vhod-l1-podveshen` |

**Правило схлопывания — содержание НЕ теряется:**
1. Прочитать обе, выбрать `id`-победитель. Критерий: на который **уже ссылаются** другие карточки (проверить грепом), при равенстве — более точное имя.
2. Всё, чего нет у победителя, **дописать в него** — особенно дословные цитаты владельца с датами: они невосстановимы.
3. На месте проигравшего оставить **редирект-строку** формата, принятого в картотеке (если такого формата нет — завести минимальный: `id` + `род: редирект` + `суть: слито в <id-победитель> <дата>`), чтобы старые ссылки не побились.
4. Все ссылки на проигравший id в картотеке и в файлах арки **переадресовать**.
⚠ **Последняя пара — не дубль, а пересечение:** `pervaya-chetvert-…` про порядок подачи, `vhod-l1-podveshen` про статус решения. Проверь по содержанию; не дубль — не схлопывай, а свяжи полем `связи` и скажи об этом в отчёте.

**A4. Пересчитать и сомкнуть.** После слияния: карточек — командой; индекс по роду покрывает все; висячих рёбер `связи` — 0 или названы поимённо с причиной.

🔴 **КОММИТ ФАЗЫ A** — отдельным коммитом, до фазы B.

### ФАЗА B. Гейт, который не даст разъехаться снова

**Требование владельца дословно:** *«давай выстроим дисциплину и построим программные ограничения, чтобы у нас был ровно один источник истины, чтобы мы записывали карточки ровно туда, куда нужно, и ничего не разъезжалось… поставить так, чтобы никогда в будущем это не разъезжалось»*.

**B1. Дописать чекер.** Дом — `_generator/tools/check_kartoteka.py` (он уже проверяет целостность графа и покрытие лекций родами). Новый файл заводи, **только если** дописать нельзя, и объясни почему. Пять проверок, каждая — по реальной поломке, уже случившейся:

| # | Что ловит | Откуда взялась |
|---|---|---|
| 1 | **карточка (`^id:`) в любом `.md` вне `KARTA-OBLASTI.md`** | один источник истины: карточки заводятся только в картотеке |
| 2 | **карточка не в индексе по роду** = сирота | 26 таких найдено `konsolidacia-l1`, среди них потерянный второй этаж |
| 3 | **ссылка на несуществующий `id`** — в поле `связи` и в файлах арок | `raskraski-inekcii-ne-funktor` ссылалась на `retrakt-priyom`, которого не было |
| 4 | **ссылка на несуществующую секцию рамы** (`SPEKA §…`, `DUH §…`) | семь мёртвых секций `SPEKA`, включая `§Калибровка` — «фильтр всех карточек» |
| 5 | **`.md` в `_studio/` не зарегистрирован** в `_studio/docs/KARTA.md §6` | `KONSTITUCIYA §9`: документ-сирота = не существует |

**Требования к чекеру:** stdlib, детерминизм, кода проекта не исполняет; `--gates` печатает ❌ по каждому нарушению с путём и строкой; код возврата ≠ 0 при нарушениях. Ложные срабатывания дороже пропусков: проверка 3 **не должна** тянуть любой обратно-кавыченный токен — это уже отравило прошлый гейт (урок `konsolidacia-l1` №3, ЦЕНА: две правки текста ради молчания гейта). Тяни id из **именованных полей и колонок**, а не из всего текста.

⚠ **Прогони чекер на текущем состоянии и покажи вывод.** Красное на раме `teorkat-vvedenie/` — **в отчёт долгом, не чинить** (§2).

**B2. Вшить в `.githooks/pre-commit`** рядом с `check_uroki.py`, тем же способом, не ломая существующее. Хук судит staged-пути.

**B3. Предупреждение в `git_zona.py commit --zone`** — ровно то, что стоило этому проекту разъехавшихся веток: если **вне** названной зоны остались изменённые пути, команда печатает их списком и предупреждает, что они НЕ уедут в коммит. Не запрещает — предупреждает: узкий коммит бывает законным.

🔴 **КОММИТ ФАЗЫ B** — отдельным коммитом.

### ФАЗА C. Записать дисциплину документом

**C1. Собрать `_studio/docs/kak-delat/ISTOCHNIK-PRAVDY.md`** — короткий (страница-полторы) документ, отвечающий на четыре вопроса:
- **где что живёт:** находка → карточка в `KARTA-OBLASTI.md`; язык и утверждения курса → `MAT-KOSTYAK.md`; рамка → `SPEKA`/`DUH`/`KOTYOL`; нарратив сессии → дневник арки (и **умирает вместе с ней**);
- **правило одного дома:** содержание живёт в одном месте, все прочие документы **ссылаются id-шниками, а не пересказывают**;
- **что краснеет** — пять проверок B1 таблицей «нарушение → какая проверка ловит»;
- **чек-лист перед коммитом:** все ли каталоги записи названы в `--zone`.
Заводи только если такого дома нет; есть похожий — **дописывай в него** и скажи в отчёте, куда.

**C2. Зарегистрировать** новые и незарегистрированные файлы в `_studio/docs/KARTA.md §6` — это **в зоне**, делаешь сам. В том числе `ISTOCHNIK-PRAVDY-teorkat.md` и `RAZMETKA-vneshnego-l1.md`: строки регистрации уже написаны предыдущим заходом и лежат в его отчёте (`kod_konsolidacia-l1.md`, раздел «Строки регистрации»).

🔴 **КОММИТ ФАЗЫ C.**

### КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ)

1. **`git_zona.py merge` существует и отработала**; ветка `arka/konsolidacia-l1` влита, `git log --oneline -1` показывает результат.
2. **Карточек — одно число, посчитанное командой**, и оно равно объединению минус схлопнутые; расхождение с ожидаемым объяснено строкой.
3. **Индекс сомкнут:** карточек в файле = карточек в индексе, `comm` в обе стороны пуст.
4. **Дубли схлопнуты:** для каждой из пяти пар — либо один `id` + редирект, либо объяснение «не дубль». Ссылок на проигравшие id **вне редирект-строк — 0**.
5. **Чекер существует, `--gates` отрабатывает, код возврата ≠ 0 при нарушениях**; вывод на текущем состоянии приложен. Проверка 1 на живом дереве даёт **0 карточек вне картотеки** (или список — тогда это находка в отчёт).
6. **Хук вызывает чекер:** проверено пробным коммитом (можно `--dry-run`-эквивалентом), результат в отчёте.
7. **Три отдельных коммита** — A, B, C; `git_zona.py check --zone` зелёный.
8. **Рама не тронута:** `git show --stat` по всем трём коммитам не содержит ни одного пути `teorkat-vvedenie/`.

🔴 **Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «нарушений не найдено», а «не найдено, проверено X файлов / Y карточек».

## 4. ВЕРИФИКАТОР — обязателен, слияние лоссово

Слияние с ручным разрешением конфликта теряет молча: выпавшая карточка не отличается от невведённой.

**Свежий субагент, ДРУГОЙ метод:**
- берёт **обе стороны из git** (`git show arka/konsolidacia-l1:…` и `git show <коммит-до-слияния>:…`), извлекает множества `id` **программно**;
- сверяет с результатом: **каждый id обеих сторон обязан присутствовать** в сведённой картотеке — либо своей карточкой, либо редирект-строкой;
- отдельно проверяет, что **у схлопнутых пар не потеряно содержание**: дословные цитаты владельца с датами из проигравшей карточки присутствуют у победителя;
- 🔴 **охват называет числом и громко**; выборка без объявления = провал гейта.

Список потерь обязан быть **ПУСТ**. Не пуст — чинишь и пишешь, что починил.

## 5. 🔴 КОММИТЫ — три, по фазам

```
git --no-optional-locks add -- <пути фазы>
git --no-optional-locks commit -m "svedenie-i-gejty: <фаза и что сделано>" -- <те же пути>
python3 _generator/tools/git_zona.py check --zone <зона>
git --no-optional-locks show --stat
```
🔴 **Оба хода обязательны** (`../docs/kak-delat/GIT-disciplina.md §3`): без `add` новый файл не войдёт; без `-- <пути>` в самом `commit` уедет чужой индекс. *Оплачено 21.07: голый `commit` подмёл 89 файлов вместо трёх.*
Хук покраснел — **не обходи, пересобери коммит**. Не прошло по внешней причине — чужое не чини, напиши строкой в отчёт.

## 6. ОТЧЁТ → секция `## ОТЧЁТ` внизу

Что сделал + **зачем** / числа с командами / как проверил / что НЕ трогал / **вывод чекера на текущем состоянии** / **долги рамы, на которых чекер покраснел** / вопросы / **результат верификатора с долей покрытия** / **время прогона + токены** / **ТРИ строки КОММИТОВ**.

**Отдельным разделом — «что теперь физически невозможно»:** по каждой из пяти проверок одной строкой, какую конкретную прошлую поломку она бы поймала. Это ответ на требование владельца, и он должен читаться без кода.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Число, посчитанное командой, всё равно врёт, если команд две и они не согласованы
Отчёт `konsolidacia-l1` заявил «индекс картотеки сомкнут **316/316**» — число получено `grep -c '^id: '`, то есть честно командой, по `KONSTITUCIYA §10`. Регексп чекера, который режет по ```-оградам, видел на той же ветке **315**: последняя карточка `menyat-korpus-a-ne-vyborku` не была закрыта оградой. Правило «числа считать, а не назначать» соблюдено — и не спасло, потому что не сказано, ЧЬЯ команда считает. Считать обязан ТОТ ЖЕ инструмент, который потом судит.
**ЦЕНА:** карточка, добытая сплошной вычиткой 132 файлов, для гейта не существовала — попала бы в следующую «потерю находок», и её завели бы заново. Плюс верификатор этого захода нашёл ещё **8 карточек**, которые терял наивный парсер из-за третьей ограды подряд, — итого 9 невидимых записей при зелёном отчёте. Час работы верификатора ушёл на то, чтобы это доказать.

### Гейт формы не отличает ССЫЛКУ от ЦИТИРОВАНИЯ мёртвой ссылки — и первым останавливает отчёт о долге
Хук завалил мой собственный коммит фазы C: в отчёте перечислены мёртвые секции рамы (это и был заказанный «долг рамы в отчёт»), а ворота 4 прочли перечисление как три новые ссылки. Гейт сработал ПРАВИЛЬНО по форме и НЕВЕРНО по сути. Обходить `--no-verify` не стал (§5), переписал перечисление так, чтобы имя файла не стояло вплотную к `§имени`.
**ЦЕНА:** один заваленный коммит и две правки собственного текста ради молчания гейта — ровно тот класс расплаты, за который прошлый заход уже платил (урок `konsolidacia-l1` №3). Вывод для фабрики: **любой документ, который ПЕРЕЧИСЛЯЕТ долги рамы, обязан называть их без сцепки `ФАЙЛ §Имя`** — иначе он сам себя не даст закоммитить. Это надо писать в шаблон отчёта, а не открывать заново каждым заходом.

### Гейт, написанный «по описанию поломки», ловит чужой проект
Первый прогон новых ворот дал **254** мёртвых ссылки на секции рамы — из них ~250 ложные: в репозитории ДВА курса, у каждого свой `SPEKA.md`, и ссылка `../SPEKA.md §Калибровка` из архива теорката резолвится в его собственную рамку, а не в живую. Заход называл проверку одной строкой («ссылка на несуществующую секцию рамы»), и по этой строке гейт пишется неверно.
**ЦЕНА:** две трети времени фазы B ушло не на проверки, а на отсев ложных срабатываний (34 источника из `MANIFEST.md`, 18 позиций `A1`/`G7`, колонка «дом» в `KARTA.md`, шаблон в `STANDART-uzla.md`). Не отсей — и гейт с 250 ложными ❌ отключили бы первым же коммитом, вместе с настоящими 47.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и видишь то, чего не видит аналитик. Сломался ВХОД? Критерий кривой? Канон противоречит живому файлу? — сюда, строкой.
> Формат жёсткий: `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Не знаешь цены — не пиши. Пустая секция — законный отчёт.

## ПЛАН — (заполняет исполнитель)

**Первый ход отработан до плана (§0):** ветка `arka/mat-kostyak` ✅ · зоны `teoriya-kategoriy/kartoteka` · `_generator/tools` · `.githooks` · `_studio/zhurnal/2026-07-25_lekcia-1` — все четыре чисты (`git_zona.py check --zone …`, rc=0) · zip-бэкап картотеки лежит в `/tmp/svedenie-backup/kartoteka-pre-merge.zip`.

### 🔴 Оспариваю две предпосылки захода — обе по живым файлам

**① «Карточек 316 на ветке `konsolidacia-l1`» — число ложное, и ложное ИМЕННО ДЛЯ ГЕЙТА.**
`grep -c '^id: '` даёт 316, а регексп чекера (`карточки()` в `check_kartoteka.py`, режет строго по ```-границам) — 315. Расхождение не округление: последняя карточка ветки `menyat-korpus-a-ne-vyborku` (строка 5595) **не закрыта ``` — файл кончается на `статус: seed`**. Для чекера её не существует; отчёт `konsolidacia-l1` «индекс сомкнут 316/316» опирался на греп, а гейт видит 315. Чинится в фазе A первым делом (закрыть ограду), в отчёт идёт уроком фабрике с ценой.

**② «Слияние требует чистого дерева» — в этом дереве недостижимо.**
`_studio/docs/KARTA.md` **правлен и не закоммичен ЧУЖОЙ рукой** (диффом: врезка «Выход Ф1 (27.07)» про `PUL-vnutrennij-l1.md` — работа предыдущей сессии, не моя). По контракту зоны я его не коммичу и не чиню. Следствия:
- команда `merge` пишется с точным условием вместо грубого: **отказ, если грязные пути ПЕРЕСЕКАЮТСЯ с путями, которые слияние трогает** (ровно то, от чего защищает «чистое дерево»), и громкое предупреждение списком про остальную грязь. Обоснование — в комментарии подкоманды;
- фаза C упирается в тот же файл: C2 велит дописать строки регистрации именно в `KARTA.md`. Пишу свои строки, а в коммит беру **только их** (`git add` файла + обратный `git apply --cached` чужого ханка), чужой ханк остаётся в рабочем дереве незакоммиченным. Не выйдет хирургически — регистрация уходит долгом в отчёт, файл не коммичу вовсе.

### Что делаю — по фазам

**ФАЗА A (один коммит — слиянием).**
A1. `git_zona.py merge <ветка> [--zone …] [--abort]` в идиоме файла: `refuse_write` из песочницы · печатает базу слияния, коммиты и файлы, которые вливаются · условие чистоты выше · `--zone` повторяемый, отказ, если слияние тащит пути вне названных зон (тот же механизм, что у `commit --zone`) · при конфликте **останавливается, печатает конфликтные пути и НЕ разрешает сама** · `log_incident` на отказ и на конфликт · коды: 0 / 1 отказ-конфликт / 2 лок / 3 песочница.
A2. Влить `arka/konsolidacia-l1` своей же командой. Конфликт ожидаю в `KARTA-OBLASTI.md` (обе стороны дописывали в конец и правили индекс) и в `kod_konsolidacia-l1.md` (обе стороны его правили). Разрешение — **обе половины**, ничего не выбрасывая; индекс пересобираю под объединённое множество.
A3. Четыре пары схлопываю, пятую — нет (по содержанию она пересечение, а не дубль; см. отчёт). Победитель — по грепу входящих ссылок, при равенстве — точность имени. Проигравший остаётся **редирект-строкой** (формата в картотеке нет — завожу минимальный: `id` + `род: редирект` + `суть: слито в <id> <дата>`); все входящие ссылки переадресую.
A4. Пересчёт командой; индекс сомкнут; висячие рёбра — 0 или поимённо.
🔴 Коммит A = **сам merge-коммит**. В него же входит `git_zona.py` с новой подкомандой: частичный коммит во время слияния git не даёт (`cannot do a partial commit during a merge`), поэтому форма `commit -- <пути>` из §5 здесь физически неприменима — беру `git commit` без pathspec, индекс при этом = результат слияния + то, что я добавил явно; чужой ханк `KARTA.md` в индекс не ставлю и он не уедет.

**ФАЗА B (один коммит).**
B1. Дописываю `check_kartoteka.py` (не новый файл): флаг `--gates`, пять проверок, каждая печатает ❌ с `путь:строка`, rc≠0. Проверка 3 тянет id **только из именованных полей и колонок** (`связи:`, колонка `карточка`/`узел`), не из любого обратного апострофа — это оплаченный урок. Режим `--staged` для хука: файловые проверки судят staged-пути, граф картотеки — только если сама картотека в коммите (идиома `check_uroki.py`: чужой долг чужие коммиты не валит).
B2. Вшиваю в `.githooks/pre-commit` рядом с `check_uroki.py`, тем же способом.
B3. `commit --zone`: изменённые пути ВНЕ зоны печатаются списком с предупреждением «в коммит НЕ уедут». Предупреждение, не запрет.
Прогон на текущем состоянии — в отчёт целиком; красное на раме `teorkat-vvedenie/` — долгом, не правкой.

**ФАЗА C (один коммит).** `_studio/docs/kak-delat/ISTOCHNIK-PRAVDY.md` (такого дома нет — проверено `ls kak-delat/`; `ISTOCHNIK-PRAVDY-teorkat.md` — не он, это выход арки про теоркат-корпус, лежит в папке арки) + регистрация в `KARTA.md §6`, включая две готовые строки из отчёта `konsolidacia-l1`.

**ВЕРИФИКАТОР (§4)** — свежий субагент после фазы A: множества id обеих сторон берёт из `git show` программно, сверяет с результатом, охват называет числом. Список потерь обязан быть пуст.

**Чего НЕ делаю:** рама `teorkat-vvedenie/` · арка `2026-07-23_vneshnie-istorii/` · внешние сюжеты и текст лекции · переписывание рабочего кода инструментов.

## ВОПРОСЫ — (заполняет исполнитель)

1. **Чужая незакоммиченная врезка в `_studio/docs/KARTA.md`** (выход Ф1 про `PUL-vnutrennij-l1.md`) лежала в дереве на входе. По контракту зоны я её не коммитил и не чинил, но C2 велит писать строки регистрации в тот же файл. Сделал хирургию: свои три врезки поставил в индекс, чужой ханк из индекса снял обратно (`git apply --cached -R`), в рабочем дереве он цел и по-прежнему не в git. **Её надо закоммитить владельцу** — она вне git и умрёт от любого `git clean`.
2. **Две карточки, на которые ссылаются, но которых нет** — `kombinatornye-vidy` и `u24-dokazyvaem-vybor-oplachen`. Заводить их = писать содержание за владельца, поэтому не завёл. Пока они висят, ворота краснеют на КАЖДОМ коммите, который трогает картотеку. Решение владельца: завести две карточки (содержание есть в теле ссылающихся) или снять ссылки.
3. **Имя карточки-победителя пары №2 отстаёт от формулировки владельца.** Победил `tri-klassifikacionnye-zadachi-karkas` (на него 3 входящие ссылки против 0), а поправка владельца 27.07 гласит «нужно не называть задачи, а называть утверждения». Переименование рвёт рёбра (`STANDART-uzla`), поэтому оставил id и вписал поправку в тело. Хочет владелец другое имя — это отдельный ход с переадресацией.
4. **`primery-teksta/` (8 файлов) зарегистрирована в `KARTA.md §6` ПАПКОЙ, а не построчно** — ворота 5 считают их сиротами. Считать ли регистрацию папкой достаточной — правило владельца, я его не менял.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и зачем — по фазам

**ФАЗА A. Ветки сведены, дубли схлопнуты, источник правды один.**
① Закрыт долг инструмента: подкоманда `git_zona.py merge <ветка> [--zone …] [--continue|--abort]` в идиоме файла (печатает базу, коммиты и пути ДО слияния; `--zone` повторяемый и отказывает на путях вне зон; при конфликте останавливается и печатает конфликтные пути, сама не разрешает; `log_incident` на отказ и конфликт; коды 0/1/2/3). Зачем именно так: голый `git merge` в чат владельцу запрещён `GIT-disciplina §0`, а команды слияния не существовало — из-за этого ветка и не вливалась месяц. Канон догнан тем же ходом: `GIT-disciplina §4б` (фикстура репо это ТРЕБУЕТ — она покраснела на «подкоманда есть, в каноне нет»).
② `arka/konsolidacia-l1` влита своей же командой. Конфликт был ровно один — `KARTA-OBLASTI.md`, обе стороны дописывали в конец. Разрешение — **обе половины целиком**, ничего не выброшено; индекс по роду пересобран под объединённое множество, заведены две недостающие строки рода (`приём`, `редирект`).
③ Схлопнуты **4 пары из 5**; пятая — не дубль (разбор ниже). Победитель выбран грепом входящих ссылок, проигравший остаётся карточкой `род: редирект` со ссылкой на победителя — ничего не удалено.
④ Починено то, чего заход не заказывал, но без чего «один источник правды» — слово: последняя карточка ветки `konsolidacia-l1` не была закрыта оградой (чекер её не видел), и в файле стояли **три ограды подряд** — на них наивный парсер терял ещё 8 карточек.

**ФАЗА B. Гейт, который не даст разъехаться снова.** Дописан `check_kartoteka.py` (новый файл не заводил): флаг `--gates`, пять ворот, ❌ с `путь:строка`, rc≠0. Вшит в `.githooks/pre-commit` рядом с `check_uroki.py`, режим `--staged`. В `git_zona.py commit --zone` добавлено предупреждение о путях вне зоны.

**ФАЗА C.** Заведён `_studio/docs/kak-delat/ISTOCHNIK-PRAVDY.md` (такого дома не было — проверено `ls kak-delat/`; `ISTOCHNIK-PRAVDY-teorkat.md` не он: это выход арки про теоркат-корпус, лежит в папке арки). Зарегистрированы в `KARTA.md §6`: новый док, обе строки из отчёта `konsolidacia-l1`, а сверх них ещё 5 файлов арки, которых там не было; обновлены записи `git_zona.py` (+`merge`), `check_kartoteka.py` (+ворота), `pre-commit` (+что гоняет).

### Числа — все командой, команда рядом

| Что | Число | Команда |
|---|---:|---|
| карточек до слияния, ветка `mat-kostyak` | **310** | `git show e48f7ea:…/KARTA-OBLASTI.md \| grep -c '^id: '` |
| карточек до слияния, ветка `konsolidacia-l1` | **316** / для чекера **315** | то же по `64fbb0d` · регексп `карточки()` видел 315 — ограда не закрыта |
| объединение множеств id | **325** | `sort -u` двух списков + `comm` |
| карточек после слияния | **326** | `check_kartoteka.py …` → «Карточек: 326» |
| сходимость | 325 объединение **+1** восстановленная (`menyat-korpus-a-ne-vyborku`) = 326 | расхождение объяснено строкой |
| индекс сомкнут | **326 / 326**, `comm` в обе стороны пуст | `comm -23` → 0, `comm -13` → 0 |
| висячие рёбра | **2**, названы поимённо | `check_kartoteka.py` → A |
| схлопнуто пар | **4** (+1 признана не дублем) | ниже |
| ворота на текущем состоянии | **241 ❌**, файлов **428**, карточек **326** | `check_kartoteka.py … --gates` |
| потерь при слиянии по верификатору | **0** из **326** проверенных id | ниже |

### Схлопывание — пять пар, четыре дубля

| проигравший | победитель | почему он | что дописано |
|---|---|---|---|
| `retrakt-parnyj-vopros-k-kategorii` | **`retrakt-priyom`** | 6 входящих ссылок против 3; и род `инструмент/донор` законен по `STANDART`, а `инструмент` — нет | словарь вопросов к незнакомой категории, три хода списком, вторая потеря консолидации (ретракт в `MAT-KOSTYAK` один раз и как топология), отклонённый расщепляющийся идемпотент |
| `klassifikaciya-est-forma-otveta` | **`tri-klassifikacionnye-zadachi-karkas`** | 3 входящих против 0 (см. ВОПРОС 3 про имя) | поправка владельца «называть утверждения, а не задачи», рабочее определение `DUH §3`, носители по трём этажам |
| `primery-ne-blok-a-rassypany` | **`primery-vsegda-vneshnee`** | 0 против 0 → более точное имя; и на него ссылаются файлы арки (`RAZMETKA`, реестр) | следствие для архитектуры: «галерея вообще растащится: частично в A, частично в C, частично в D» |
| `zakaz-metateorema-dlya-estestvennosti` | **`ep-etazh-bez-instrumenta`** | 5 входящих против 0 | полная формулировка заказа про метатеорему, перенос У47 на классификацию объектов |
| `pervaya-chetvert-nachinaetsya-s-biekcij` | — **НЕ ДУБЛЬ** | — | **пересечение, а не дубль**: одна про ПОРЯДОК первой четверти (биекции → все отображения → обращённая стрелка), вторая (`vhod-l1-podveshen`) про СТАТУС решения о входе. Порядок годен и после того, как вход выберут; выбор входа порядком не задаётся. Связаны полем `связи` в обе стороны, различение вписано в тела обеих |

**Ссылок на проигравшие id вне редирект-строк — 0 в графе:** ни одно поле `связи` на них не смотрит (2 переадресованы в картотеке, 6 строк реестра — в `ISTOCHNIK-PRAVDY-teorkat.md`). Остались 3 упоминания в ПОВЕСТВОВАНИИ: `SESSIYA.md:733` (дневниковая строка, которая сама предсказывает схлопывание этих пяти пар) и `kod_konsolidacia-l1.md:300` (сданный отчёт, перечисляющий заведённые карточки). Переписывать сданный отчёт и дневник задним числом не стал: это фальсификация записи, а редирект уже держит ссылки живыми. Плюс строка индекса `- **редирект:**` — она и есть редирект-строка.

### Вывод чекера на текущем состоянии

```
=== Гейт картотеки === Карточек: 326 · уникальных id: 326 · в индексе: 326
A. Висячие рёбра:  ['kombinatornye-vidy', 'u24-dokazyvaem-vybor-oplachen']
B. Сироты: НЕТ ✅   C. Дубликаты id: НЕТ ✅
По роду: пример 99 · находка 87 · утверждение 42 · понятие 31 · мотивация 24 · сцена 22 ·
         мостик 6 · затравка 5 · редирект 4 · инструмент/донор 3 · приём 3

═══ ВОРОТА (--gates) ═══   Судим файлов: 428 · карточек: 326
1. Карточки вне картотеки:                   47 ❌
2. Карточки не в индексе по роду:            НЕТ ✅
3. Ссылки на несуществующий id:               2 ❌
4. Ссылки на несуществующие секции рамы:     51 ❌
5. Не зарегистрированы в KARTA.md §6:       141 ❌
ИТОГ: ❌ нарушений 241 · проверено файлов 428, карточек 326
```
Полный вывод с адресами — `/tmp/svedenie-backup/gates-5.log` (регенерируется командой из таблицы).

**Разбор красного — это НАХОДКИ, а не поломка гейта:**
- **47 карточек вне картотеки:** 35 в четырёх `kartoteka/VERDIKT-*.md` (вердикты по Л4/Л5 архивного курса) и 12 в `_studio/zhurnal/2026-07-25_lekcia-1/KARTOCHKI-novye.md` — файл-накопитель карточек-заявок. Это ровно механизм потери: карточка, лежащая «пока не в картотеке», не находится ничем.
- **2 битые ссылки** — см. ВОПРОС 2.
- **51 мёртвая секция рамы:** самые частые в `SPEKA` — `§Визуал` (11), `§Поправки`, `§Ограничения`, `§Граница прибора`, `§Л1`. **7 из них — внутри самой картотеки**, и это независимое подтверждение находки `konsolidacia-l1` про «семь мёртвых секций SPEKA»: тот же результат другим методом.
- **141 незарегистрированный `.md` в `_studio/`** — самые толстые узлы: `2026-07-16_teorkat-programma-dizajn` (25), `2026-07-20_vvedenie-sborka` (24), `2026-07-10_sborka-konvejera` (20). Свои 7 я зарегистрировал, чужое — долг.

### 🔴 Долги рамы, на которых гейт покраснел (НЕ чинил — вне зоны, §2)

| Где | Что | Почему не тронул |
|---|---|---|
| `teorkat-vvedenie/DUH.md` | нет секций, на которые ссылаются: `§Т6` (из `PLAN.md` и `ЗАХОД-lekcia-1.md`), `§1–2`/`§1–3` как диапазон, `§2.12` (из `KOTYOL.md`) | рама, правит только владелец утверждёнными формулировками |
| `teorkat-vvedenie/KOTYOL.md` | нет `§Веришь/тянет`, `§Источники`, `§Лестница естественности` — на них ссылаются `SESSIYA.md`, `RULI-v-ramu.md`, `NAVIGATOR` арки маткостяка | то же |
| `teorkat-vvedenie/SPEKA.md` | нет `§18` (из `RUL.md` штаба и `ЗАХОД-vneshnie-istorii.md`) | то же |
| `teoriya-kategoriy/SPEKA.md` (архивная рама) | нет `§Визуал`, `§Ограничения`, `§Поправки`, `§Л1`, `§Ловушка №3`, `§ЭКСКУРС`-производных — на них смотрят карточки живой картотеки | архив вне зоны; но **ссылки идут из живого файла**, и это настоящий долг: либо секции вернуть, либо ссылки перецелить |

### Как проверил

- **Слияние:** своя же команда напечатала базу, 2 коммита и 4 пути ДО начала; конфликт назван (1 путь) и разрешён руками, взяты обе половины.
- **Граф:** `check_kartoteka.py` — 326/326, `comm` в обе стороны пуст.
- **Ворота:** полный прогон (выше) + проверка на ложные срабатывания: первый прогон дал 254 мёртвых секции и 68 битых id, разбор показал, что ~250 и 66 из них ЛОЖНЫЕ (два курса — две рамы; колонка `id` в `MANIFEST.md` — источники, а не карточки; `A1`/`G7` — не id; `STANDART-uzla.md` — дом формата). После сужения осталось 51 и 2, и они настоящие.
- **Хук — пробным коммитом на ОДНОРАЗОВОМ индексе** (`GIT_INDEX_FILE` в `/tmp`, боевой индекс не тронут): временный файл с карточкой мимо картотеки → `ХУК rc=1`, ворота 1 и 5 напечатали адреса; файл с чистым содержимым → `rc=0` и **пустой вывод** (гейт молчит, пока чисто — иначе его отключат).
- **Фикстура `git_zona`** после обеих правок инструмента: `rc=0`, 32 ✅, «ФИКСТУРЫ ЗЕЛЁНЫЕ». Она же поймала мой недосмотр — новая подкоманда не была вписана в канон.

### 🔴 Результат верификатора — с долей покрытия

Свежий субагент, ДРУГОЙ метод (свой парсер, обе стороны из `git show`, сверка программно):

- **id проверено 326 (100 % объединения A ∪ B)** → найдено своей карточкой **322**, редиректом **4**, **потеряно 0**. Список потерь ПУСТ.
- **Тел сверено 322**; короче исходного — 2 случая, оба ровно на −19 символов от переадресации ссылки, текст не тронут.
- **Рёбер `связи` проверено 2297**, потерянных при схлопывании — 0.
- **Цитат владельца проверено 2216** (17 в схлопнутых парах + сплошной проход по всем карточкам). **Найдено 2211, НЕ найдено 5** — и это была настоящая потеря: мой пересказ съел дословную речь владельца. Самая дорогая — «Честно говоря, не могу представить себе такого» (оценка владельца о примерах к У47; строго сильнее уцелевшего «если не получится — это вообще не нужно»).
- **Починил все 5** дословно, проверено грепом (`я понял эту симметрию`, `что здесь изоморфизм`, `в одном месте галереи`, `а потом говорим, что есть понятие эквивалентности`, `не могу представить себе такого` — по 1 вхождению каждая). Правка вошла в коммит фазы A (`--amend`, ветка локальная, не выгружена).
- **Побочная находка верификатора, дороже задания:** наивный парсер по парам оград терял **8 карточек** из-за трёх оград подряд, причём счётчик оград сходился (632, чётное) и выглядел здоровым. Ограда снята, теперь 652 = 326 пар ровно.

### Что НЕ трогал

Раму `teorkat-vvedenie/` (0 путей во всех трёх коммитах — `git show --stat`), зону арки `2026-07-23_vneshnie-istorii/`, внешние сюжеты и текст лекции, `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (его дописал сам инструмент при конфликте слияния — файл вне моей зоны, остался вне git), чужую врезку в `KARTA.md` (ВОПРОС 1). Рабочий код инструментов не переписывал — дописывал: `git_zona.py` +1 подкоманда и +1 предупреждение, `check_kartoteka.py` — вторая половина файла, первая не тронута.

### 🔴 Что теперь физически невозможно — по одной строке на ворота

| # | Ворота | Какую прошлую поломку поймали бы |
|---|---|---|
| 1 | карточка вне картотеки | **`KARTOCHKI-novye.md`** — 12 карточек-заявок, лежавших «пока» вне картотеки; и 35 карточек в `VERDIKT-*.md`. Ровно так находка перестаёт находиться: она есть, но не там, где её ищут |
| 2 | карточка не в индексе по роду | **26 сирот, найденных `konsolidacia-l1`**, среди них потерянный второй этаж — карточки существовали и не находились ни грепом по индексу, ни глазами |
| 3 | ссылка на несуществующий id | **`raskraski-inekcii-ne-funktor` → `retrakt-priyom`**: ссылка на карточку, которой не завели. Главный инструмент этажа объектов жил только в дневнике |
| 4 | ссылка на несуществующую секцию рамы | **семь мёртвых `SPEKA §…`**, включая `§Калибровка` — «фильтр всех карточек», на который смотрит шапка картотеки. Гейт воспроизвёл эти 7 независимо |
| 5 | `.md` без строки в `KARTA.md §6` | **`ISTOCHNIK-PRAVDY-teorkat.md` и `RAZMETKA-vneshnego-l1.md`**: строки регистрации написали ещё в прошлом заходе, но вставить было некому — файлы прожили сутки документами-сиротами |

**Плюс два механизма вне ворот:** `commit --zone` теперь печатает изменённые пути ВНЕ зоны — это ровно та слепота, которой разъехались ветки (команда называла зону арки, работа шла ещё и в картотеку); `merge` при конфликте останавливается и не разрешает сама — авто-разрешение здесь означает потерю, которую никто не заметит.

### Время прогона и токены

Прогон **35 минут** (19:41 → 20:16 по метке zip-бэкапа и `date`). Субагент-верификатор: **103 172 токена**, 20 вызовов инструментов, 7 минут. Токены основного контекста изнутри сессии не измеряются — не называю, чтобы не назначить число вместо счёта.

### Состояние зон на выходе

```
teoriya-kategoriy/kartoteka          ✅ rc=0
_generator/tools                     ✅ rc=0
.githooks                            ✅ rc=0
_studio/zhurnal/2026-07-25_lekcia-1  ✅ rc=0
_studio/docs                         ❌ rc=1 — ровно 1 путь: ЧУЖАЯ врезка в KARTA.md
```
Последняя строка красная НАМЕРЕННО и не моей работой: это незакоммиченная врезка предыдущей сессии (ВОПРОС 1). Мои три врезки в тот же файл в коммите C, чужая — нет.

**КОММИТЫ:** A `a4e7e1b` · B `e3d91bd` · C `9b65077` · `git_zona.py check --zone` → ✅ по всем четырём своим зонам; по `_studio/docs` ❌ из-за чужого пути, разобрано строкой выше.
*(четвёртый коммит — только эта строка с хэшами и два урока фабрике: хэш C в отчёт до коммита C не вписывается, идиома взята у захода `konsolidacia-l1`)*
