# Канал исполнителя — ТРЕКЕР (sostoyanie.py + журнал) · один заход до конца

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.

**Модель: `Sonnet`** — реализуешь заданный реестр гейтов механикой; abort + критерий ловят сбой. Если сверка журнал↔диск потребует дизайна по ходу — переключись на `opusplan` и скажи в `## ВОПРОСЫ`.

**Текст запуска (владелец → Claude Code):**
```
Модель: Sonnet — реализация по готовому реестру гейтов; механика.
Claude Code, твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-10_orkestracia/kod_treker.md. Прочитай ТОЛЬКО его и то, что он называет; остальной проект не изучай. Точка отката: git-коммит сразу. План/вопросы/отчёт пиши в этот же файл внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ). Ничего сверх задачи не трогай.
```

КОНТЕКСТ. Проект — фабрика `_studio/`. Арка «оркестрация фабрики», **ВТОРАЯ волна**. Строишь трекер поверх реестра гейтов (заход ①, `konvejer/GEJTY.md`) и layout папки лекции (заход ②, `docs/spravka/PAPKA-LEKCII.md` + `../_generator/tools/bootstrap_lekcia.py`). Оба ДОЛЖНЫ быть готовы. Ценность трекера (боль владельца): «где-то недоработали, а дальше пошли» — ловим молчаливый недобор. Приёмка — по ОТЧЁТУ.

⚠ **Сверка перекрывающей зоны:** `GEJTY.md` и `PAPKA-LEKCII.md` написаны заходами ①/②. ПЕРЕД кодом прочитай их на живых файлах; гейт неоднозначен или «признак-на-диске» не задан → в `## ВОПРОСЫ`, НЕ выдумывай свой критерий.

**ОБНОВЛЕНИЕ (Cowork, после Волны 1 — контракт УЖЕ сведён, шва нет):**
- Размещение Фазы I РЕШЕНО: канон — `docs/spravka/PAPKA-LEKCII.md` (`brief.md` в корне · `kartoteka/` · `istochniki/` · `kotly/` · `chernovik/` · опц. `src/`). Диск-признаки `GEJTY.md` G1–G4/H1–H4 УЖЕ приведены к нему (старого `faza1/` больше нет — если встретишь, это баг, флаг в ВОПРОСЫ).
- Слой `skelet` физически = **`kotly/matematika.md`** (G3/H4 грепают его на `⚑ Флаг`/уровень-тег; G4 — цитаты `chernovik/rasskaz.md` резолвятся в его якоря).
- Точные ключи полей `brief.md` (для G1) — **источник истины = стаб, который генерирует `_generator/tools/bootstrap_lekcia.py`**. Прочитай сгенерированный `brief.md`, возьми ключи оттуда, не выдумывай.
- Фикстуры строй через `bootstrap_lekcia.py <путь> --slides no|yes` (готов, прогнан) — не лепи руками с нуля.

## 0. ПЕРВЫЙ ХОД
Рабочая папка — корень `_studio/`; `../` = `materials/`. git-коммит сразу. Прочитать ТОЛЬКО:
- `konvejer/GEJTY.md` — РЕЕСТР гейтов (источник истины, ЧТО проверять; заход ①)
- `docs/spravka/PAPKA-LEKCII.md` — layout папки лекции (ГДЕ на диске читать; заход ②)
- `../_generator/audit.py` — ПАТТЕРН (PASS/FAIL/WARN построчно, exit-код; мы делаем ФАЙЛОВЫЙ аналог, не браузерный)
- `../_generator/build_deck.py` — существующие файловые проверки (lint: покрытие `slide_order`, резолв `data-ill`, определён `var`) — ПЕРЕИСПОЛЬЗОВАТЬ, не дублировать
- `konvejer/FORMAT-ISTOCHNIKA.md` — контракт `src/` (для гейтов Фазы II)
ПЛАН — в `## ПЛАН`.

## 1. ДИСЦИПЛИНА (Карпатов)
Предпосылки назвать; stdlib-only; abort при неоднозначности гейта (в ВОПРОСЫ, не самодеятельность); критерий может провалиться. Без роя субагентов.

## 2. ГРАНИЦЫ
- **Read-only инструмент:** `sostoyanie.py` НЕ мутирует файлы лекции.
- Гейты НЕ переопределять — источник истины `GEJTY.md`. Дыра/расхождение → ВОПРОС.
- Браузерные гейты (Фаза II аудит, render-identity) здесь НЕ реализовывать — пометить «браузерный, локально у владельца» и сослаться на существующий `../_generator/audit.py`.
- stdlib только (Р15 ядро). Ничего внешнего.
- `KARTA.md §6` НЕ править (заход ④ идёт параллельно). Новые файлы — в ОТЧЁТ.

## 3. ЗАДАЧА — два выхода

**(1) `../_generator/tools/sostoyanie.py`** (stdlib): `python3 sostoyanie.py <папка-лекции>` —
- обходит папку лекции, по КАЖДОМУ гейту из `GEJTY.md` считает PASS/FAIL/WARN по его «признаку-на-диске»;
- печатает таблицу: `гейт · статус · деталь`;
- вычисляет **`⏺ ТЫ ЗДЕСЬ`** = первый ЖЁСТКИЙ гейт не-PASS;
- exit 0, если нет жёстких FAIL (WARN не валит), 1 — если есть;
- **гейты-не-рельсы:** артефакт стадии N+1 при не-PASS гейта N → WARN «прыжок вперёд», НЕ FAIL;
- **дисциплина-хуки** (карточка без источника, источник без дайджеста, висячее ребро в `KARTA-OBLASTI`, открытый `⚑ Флаг`, `slide_order`-сироты) — WARN/FAIL по типу из реестра;
- гейты `src/` — переиспользовать функции `build_deck.py` (импортом/вызовом), не переписывать логику;
- «слайды: нет» в `brief.md` → гейты Фазы II и `src/` помечать «N/A (доска)», не FAIL.

**(2) Формат журнала** — новый файл `konvejer/ZHURNAL.md`: описывает append-only журнал вызовов, строка на сессию/волну:
```
дата · функция · волна# · гейт · результат(PASS|недобор) · время · токены
```
(его дописывает драйвер/аналитик после сессии; физически лежит в `<лекция>/dnevnik/zhurnal.md` или в `SESSIYA.md` — реши и зафиксируй, отрази форму в `ZHURNAL.md`). `sostoyanie.py` при наличии журнала: (а) печатает последовательность вызовов; (б) **сверяет журнал↔диск** — строка говорит PASS, а аудит гейта FAIL → флаг `⚠ молчаливый недобор`. Это ядро ценности.

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- `python3 sostoyanie.py <папка от bootstrap_lekcia.py --slides no>` печатает гейты Фазы I как pending/FAIL, `ТЫ ЗДЕСЬ = бриф`, exit 1.
- на РУЧНОЙ мини-фикстуре (заполни `brief.md` 8 полей + 1 карточку с `источник`) `ТЫ ЗДЕСЬ` сдвигается вперёд — доказать сдвиг двумя прогонами.
- продемонстрировать «молчаливый недобор»: строка журнала PASS + гейт FAIL → флаг сработал (на фикстуре).
- `py_compile` проходит; только stdlib; read-only (файлы лекции не изменены — сверить `git status`/mtime).
- КАЖДЫЙ гейт из `GEJTY.md` имеет обработчик ИЛИ явную пометку «браузерный/локально» — ни один молча не пропущен.
- фикстуры — в `/tmp/`, убрать за собой.

## 4. ВЕРИФИКАТОР
Инструмент read-only — потерь нет. Само-проверка: прогнать на (а) пустой bootstrap-папке, (б) заполненной фикстуре, (в) фикстуре с «молчаливым недобором» — приложить все выводы в ОТЧЁТ.

## 5. ОТЧЁТ → `## ОТЧЁТ`
что сделал + ЗАЧЕМ / как проверил (3 прогона) / что НЕ трогал / **новые файлы для `KARTA §6`** / решение по месту журнала / вопросы (нереализованные гейты — списком) / **время + токены**.

## ПЛАН — (заполняет исполнитель)

Прочитано (только названное): `GEJTY.md`, `PAPKA-LEKCII.md`, `../_generator/audit.py`, `../_generator/build_deck.py`, `FORMAT-ISTOCHNIKA.md`; + сгенерированный `<лекция>/brief.md` живьём через `bootstrap_lekcia.py` (по прямому указанию п.19 ОБНОВЛЕНИЯ).

**Находка про ключи `brief.md` (Фаза I):** реальный стаб — НЕ `ключ:` в начале строки, а нумерованный список `1. тема: <...>` … `9. слайды: да|нет` (9 полей, не 8 — 9-е добавлено заходом②). Строю G1 под РЕАЛЬНЫЙ формат, не под догадку `GEJTY.md`.

**Архитектура `sostoyanie.py`:** реестр из 16 записей (G1–G11, H1–H5) как Python-структуры (сам `GEJTY.md` — проза, не машиночитаем) с функцией-чекером на каждую; печать таблицы `гейт · статус · деталь`; `⏺ ТЫ ЗДЕСЬ` = первый G-гейт со статусом FAIL по порядку (WARN/N-A не блокируют); exit 1 при наличии хотя бы одного жёсткого FAIL. Гейты `src/`-лога (G6,G10,G11 слой1) — вызовом функций `build_deck.py` (импорт модуля), не копипаста. Браузерные части (G8/G9 audit-слой, G11 слои 2–4) — статус `[браузерный]`, не блокируют exit, печатается «прогнать локально: audit.py …».

«Гейты не рельсы»: если у гейта N+1 есть тронутые артефакты, а гейт N ещё не PASS/N-A → отдельная строка WARN «прыжок вперёд», статус самого N+1 считается по своим критериям (не форсится).

«слайды: нет» → G5–G11 и H5 помечаются `N/A (доска)` (все их признаки лежат в `src/`).

**Известные зоны неоднозначности** (иду по признаку буквально из `GEJTY.md`, где неоднозначно — WARN/advisory + вопрос владельцу, не гадаю):
- G1 «бюджет_слайдов кратен делению длительности на 2» — точной формулы нет; проверяю «целое число» жёстко, кратность — WARN-эвристика.
- G2 «есть но (разворот)» — вкус, грепаю только присутствие `статус: на-сверку` как advisory-сигнал, не как истину.
- G4 «🖼 ≥ число тактов нити» — «список тактов» физически не найден среди названных файлов → WARN «не проверяемо без списка тактов».
- G4 «цитата резолвится в skelet-якорь» — синтаксис якоря не задан явно → эвристика по slug заголовков, помечаю неточной.
- G5 признак трогает `src/brief.md`/`src/slides/*` — при этом PAPKA-LEKCII.md относит raskadrovka к Фазе I папке `raskadrovka/`, отдельной от `src/`. Похоже на нестыковку заходов ①/② — реализую ДОСЛОВНО по `GEJTY.md` (src/), несостыковку — в ВОПРОСЫ.
- H2 «дайджест VYCHITANO под источник» — сверяю по вхождению строки источника как заголовка `### <источник>`; точный формат заголовка VYCHITANO не проверен по живому файлу (не читал `catalan/spravochnik/*`).
- Формат карточки `kartoteka/KARTA-OBLASTI.md` (G2/H1/H3) — беру ровно поля, названные в `GEJTY.md` (`id:`,`род:`,`источник:`,`связи:[...]`,`статус:`), `STANDART-uzla.md` не читал (вне списка).

Формат журнала — новый `konvejer/ZHURNAL.md`; решение по физическому месту журнала — `<лекция>/dnevnik/zhurnal.md` (внутри лекции, рядом с рабочим дневником `SESSIYA.md`), т.к. журнал — история конкретной лекции, не общестудийная; `sostoyanie.py` при наличии печатает последовательность + сверяет журнал↔диск (⚠ молчаливый недобор).

Фикстуры — в `/tmp/`, удаляю после прогонов.

## ВОПРОСЫ — (заполняет исполнитель)

Гейтов без обработчика — **0** (все 16 записей G1–G11/H1–H5 имеют check-функцию; чисто браузерные части явно помечены `[браузерный]` и не блокируют exit). Ниже — места, где признак-на-диске в `GEJTY.md` неоднозначен или сведения нет среди прочитанных файлов; я не выдумывал критерий, а либо ослаблял до WARN/advisory, либо реализовывал дословно с пометкой риска:

1. **Поле 9 `brief.md`: `yes/no` vs `да/нет`.** `bootstrap_lekcia.py` пишет `слайды: {args.slides}` буквально англ. `yes`/`no`, а контракт (`PAPKA-LEKCII.md`, `GEJTY.md`) и сам текст поля документируют `да|нет`. Похоже на баг заход② `bootstrap_lekcia.py` (не переводит флаг). Трекер принимает `yes/no` как синонимы `да/нет` (WARN-пометка), но стоит поправить сам bootstrap, иначе расхождение тиражируется на каждую новую лекцию.
2. **G1 «бюджет_слайдов кратен делению длительности на 2».** Точной формулы (округление? допуск?) в `GEJTY.md` нет. Реализовал жёстко только «целое число»; кратность длительности/2 — WARN-эвристика, не блокирует. Нужно либо зафиксировать формулу в `GEJTY.md`, либо явно объявить гейт автор-чекпоинтом.
3. **Формат карточки `kartoteka/KARTA-OBLASTI.md`** (`id:`/`род:`/`источник:`/`связи:[...]`/`статус:`) взят буквально из полей, которые называет `GEJTY.md` в признаках G2/H1–H3. Реальный канон формата — `kartoteka/STANDART-uzla.md` — этим заходом НЕ читан (вне списка `## 0`). Сверить парсер с живым `STANDART-uzla.md` отдельным заходом, прежде чем доверять G2/H1/H3 на настоящих данных.
4. **G4 «🖼 ≥ число тактов нити».** «Список тактов» физически не найден ни в одном из прочитанных файлов (возможно, живёт в `kartoteka/KARTA-OBLASTI.md` §«Нити», не проверено). Сейчас — не проверяется, только печатается счётчик 🖼 с пометкой WARN.
5. **G4 синтаксис skelet-якоря** (`[skelet#<якорь>]`) не задан явно ни в `GEJTY.md`, ни в `PAPKA-LEKCII.md` (там прямо написано «не проверено по `04-gibrid-istochnik/DOK.md`»). Использована эвристика — slug заголовков `kotly/matematika.md`; при реальном формате якоря (например, явная метка `{#id}` рядом с Утверждением) сверка будет ложно находить/не находить висячие ссылки.
6. **G5 признак-на-диске указывает на `src/`** (`src/brief.md`, `src/slides/*`), а `PAPKA-LEKCII.md` относит раскадровку к отдельной Фазы-I папке `raskadrovka/`, ДО входа в `src/` (G6). Это выглядит нестыковкой заходов ①/②: раскадровка логически должна проверяться до появления `src/`. Реализовал G5 дословно по `GEJTY.md` (т.е. фактически он дублирует часть G6) — решить, что имелось в виду, отдельно.
7. **G5/G7 поля `register:`/`word_budget_per_slide:` в `src/brief.md`** не описаны в схеме `FORMAT-ISTOCHNIKA.md` (там `id/title/canvas/provenance/provenance_sha256/milestone/accent_tag/slide_order`) и не генерируются `_generator/skeleton/brief.md` (проверил живьём). `GEJTY.md` ожидает их — видимо, их должна дописывать арка 5/6 вручную поверх скелета. Реализовал как обычную проверку присутствия ключа — работает, но стоит зафиксировать в `FORMAT-ISTOCHNIKA.md`, иначе выглядит как гейт на несуществующее поле.
8. **G7 допуск бюджета слов «±допуск»** — число не задано; взял +20% эвристически, не как жёсткий порог (только WARN-деталь).
9. **H2 формат заголовка дайджеста** в `istochniki/VYCHITANO.md` (`### <файл или ссылка>`) взят по шаблону, который пишет `bootstrap_lekcia.py`, не по живому файлу `catalan/biblioteka/istochniki/` (вне списка чтения).
10. **Место журнала** — решил сам: `<лекция>/dnevnik/zhurnal.md` (не `SESSIYA.md`, который остаётся прозой), обоснование — в `ZHURNAL.md`. Если владелец хочет иное место — поправить один путь в `sostoyanie.py` (`check_journal`) и в `ZHURNAL.md`.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Два выхода по ТЗ:
1. [`_generator/tools/sostoyanie.py`](../../../../_generator/tools/sostoyanie.py) (stdlib, read-only) — реестр всех 16 гейтов/хуков `GEJTY.md` (G1–G11, H1–H5) как Python-структуры с check-функцией на каждый; печатает таблицу `гейт · статус · деталь`, блок «прыжки вперёд» (артефакт N+1 тронут, гейт N не PASS/N-A → WARN, не FAIL), `⏺ ТЫ ЗДЕСЬ` (первый G-гейт со статусом FAIL по порядку G1→G11), exit 1 при ≥1 жёстком FAIL. Гейты `src/`-слоя (G6 иллюстрации/каркас, G10 var(--x), G11 слой-1) переиспользуют `build_deck.py` импортом модуля (`parse_brief`, `load_source`, `lint`) — не копируют логику. Браузерные части (G8/G9 audit-слой, G11 слои 2–4) помечены `[браузерный]` с готовой командой `audit.py …` для владельца, не блокируют exit. «слайды: нет» → G5–G11 и H5 → `N/A (доска)`.
2. [`konvejer/ZHURNAL.md`](../../konvejer/ZHURNAL.md) — контракт формата строки журнала (`дата · функция · волна# · гейт · результат(PASS|недобор) · время · токены`), место — `<лекция>/dnevnik/zhurnal.md` (решение и обоснование внутри файла). `sostoyanie.py` при наличии журнала печатает разобранные строки и сверяет журнал↔диск — расхождение (журнал PASS, диск FAIL) печатает `⚠ МОЛЧАЛИВЫЙ НЕДОБОР` — это ядро ценности трекера.

**Как проверил (3+1 прогона, все выводы см. выше по ходу сессии, здесь — сводка):**
- **(а) пустая bootstrap-папка** (`bootstrap_lekcia.py fixt --slides no`): гейты Фазы I печатаются FAIL (кроме G3/G4, которые честно PASS на пустом файле — 0 флагов формально корректно), `⏺ ТЫ ЗДЕСЬ: G1`, `exit 1`. Совпадает с критерием готовности п.1. По пути поймал и исправил баг парсера поля 9 (`"слайды: no"` парсилось целиком, а не значение после `:` → `slides_flag` не выставлялся, Фаза-II гейты не уходили в N/A).
- **(б) ручная мини-фикстура** — заполнил `brief.md` (все 8+1 поля) и добавил 1 карточку с `источник:` в `KARTA-OBLASTI.md`. Прогон 1 (до правок): `ТЫ ЗДЕСЬ: G1`. Прогон 2 (после): `ТЫ ЗДЕСЬ: G2` (G1 стал PASS, G2 остался FAIL — 1 источник < 5, как и должно). Сдвиг доказан двумя прогонами.
- **(в) «молчаливый недобор»** — на той же фикстуре завёл `dnevnik/zhurnal.md` со строкой `G2 · PASS`, при этом сам гейт G2 на диске FAIL (1 источник < 5) → трекер напечатал `⚠ МОЛЧАЛИВЫЙ НЕДОБОР: G2 (журнал=PASS, диск=FAIL) — уникальных источников 1 < 5`. Флаг сработал.
- **read-only:** снял mtime всех файлов фикстуры до/после прогона (`stat -f %m`) — diff пуст, ни один файл лекции не тронут.
- **py_compile:** `python3 -m py_compile sostoyanie.py` — чисто; импорты только stdlib (`re, subprocess, sys, unicodedata, argparse, pathlib`) + локальный `build_deck` (переиспользование, не внешняя зависимость).
- **смоук-тест G5–G11:** прогнал на `--slides yes` скелет-фикстуре (пустой `slide_order`) — все обработчики (включая импорт `build_deck.load_source`/`lint` и subprocess `build_deck.py --lint`) отработали без исключений; заодно живьём сработал блок «прыжки вперёд» (`G6 тронут, а G5 ещё не PASS`) на скелете с непустым `brief.md`, но пустым `slide_order`.
- Все фикстуры создавались и удалялись из `/tmp/` (`fixt_a`, `fixt_b`, `fixt_empty`, `fixt_slides` — убраны по завершении).

**Что НЕ трогал.** `GEJTY.md`, `PAPKA-LEKCII.md`, `FORMAT-ISTOCHNIKA.md`, `build_deck.py`, `audit.py`, `bootstrap_lekcia.py`, `KARTA.md §6` (заход④ идёт параллельно) — не редактировал, только читал/импортировал. Браузерные гейты (Playwright-часть `audit.py`) не реализовывал — по границе захода, только сослался.

**Новые файлы для `KARTA §6`:**
- `_generator/tools/sostoyanie.py` — трекер состояния лекции (read-only, stdlib).
- `_studio/konvejer/ZHURNAL.md` — контракт формата журнала конвейера.

**Решение по месту журнала:** `<лекция>/dnevnik/zhurnal.md` (новая подпапка внутри папки лекции, отдельно от `SESSIYA.md`) — см. `ZHURNAL.md` §«Место на диске».

**Вопросы (нереализованные гейты):** списком — **нет** (0 из 16 без обработчика). Открытые зоны неоднозначности — см. `## ВОПРОСЫ` выше (10 пунктов, ни один не блокирует работу инструмента, все реализованы best-effort с explicit WARN-пометкой риска).

**Время + токены.** Точный метринг в этой среде мне недоступен (нет счётчика токенов/секундомера в рантайме инструмента) — сессия была одним цельным заходом: чтение 5 названных файлов + генерация 2 живых bootstrap-фикстур для сверки ключей → написание `sostoyanie.py` (~500 строк) и `ZHURNAL.md` → 1 баг найден и исправлен на первом же прогоне → 4 прогона (а/б×2/в) + read-only-сверка + смоук-тест G5–G11 → отчёт. Точные цифры владелец может снять по логам своей оболочки, если это принципиально для журнала конвейера.
