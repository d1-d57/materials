# ПЛАН арки «оркестрация фабрики»

## ⭐ СЕЙЧАС
Запустить **Волну 1** в Claude Code ПАРАЛЛЕЛЬНО (независимы, разные файлы): **① `kod_spina`** + **② `kod_bootstrap`**. После приёмки обоих по гейту — Cowork регистрирует новые файлы в `KARTA.md §6` (заходы её сами НЕ правят — параллель-безопасность) → **Волна 2** ПАРАЛЛЕЛЬНО: **③ `kod_treker`** + **④ `kod_katalog`**.

## Заходы (порядок + параллель + зависимости)
**Волна 1 (∥):**
- [ ] **① `kod_spina`** — единый `ALGORITM` обе фазы + реестр гейтов `konvejer/GEJTY.md` + фикс таблицы Арх.2. · `opusplan` · фундамент (③/④ на нём стоят)
- [ ] **② `kod_bootstrap`** — layout `docs/spravka/PAPKA-LEKCII.md` + `_generator/tools/bootstrap_lekcia.py`. · `opusplan`

**Волна 2 (∥, после приёмки В1):**
- [ ] **③ `kod_treker`** — `_generator/tools/sostoyanie.py` (по `GEJTY.md`) + формат журнала `konvejer/ZHURNAL.md`. · `Sonnet` (opusplan, если сверка журнал↔диск потребует дизайна) · читает выходы ①+②
- [ ] **④ `kod_katalog`** — `konvejer/KATALOG.md` (функции «инструмент-под-задачу»). · `Sonnet` · читает `GEJTY.md` (①) + DOK + вложенный дистиллят Каталана

## Cowork-консолидация между волнами
- После В1: зарегистрировать в `KARTA.md §6` — `GEJTY.md`, `PAPKA-LEKCII.md`, `bootstrap_lekcia.py`; свести отчёты; ответить на `## ВОПРОСЫ`.
- После В2: зарегистрировать `sostoyanie.py`, `ZHURNAL.md`, `KATALOG.md`.

## Приёмка каждого захода (RUKOVODSTVO §Приёмка)
критерий выполнен · числа сходятся · границы соблюдены · вопросы разобраны · **время+токены в отчёте** · нет тихого недобора. Вердикт: принять ИЛИ вернуть на точечную дочистку.

## Закрытие арки (ARKA §7)
durable по домам: решения → `RESHENIYA` (кандидаты: трекер-модель, каталог-вместо-скиллов, layout папки лекции); статус → `OTKRYTYE-ZADACHI`; архитектура → `ARHITEKTURA-FABRIKI`. Строка в `SVODKI`. Пометить закрытой.

## Шаги
- [x] Вход по ритуалу, изучены spine + пересобранная Фаза I + Фаза II + оба прогона
- [x] Архитектура согласована с владельцем (сессия=вызов; трекер=журнал+аудит; функции=процедуры не skill; папка=одна, слайды опц.)
- [x] Заведена арка, написаны 4 захода
- [ ] Волна 1 (① ∥ ②) прогнана и принята
- [ ] Волна 2 (③ ∥ ④) прогнана и принята
- [ ] Арка закрыта, durable по домам
