# ДНЕВНИК арки «оркестрация фабрики»

> Дистиллят хода + решения (у каждого «почему»), не свалка. Durable уходит по домам на закрытии.

## 2026-07-10 · старт (Cowork)

**Вход по ритуалу.** Изучены: spine (COWORK→KARTA→борд, Р1–Р20); пересобранная Фаза I (DOK `01–05` + `FAZA-1-REDESIGN.md`); Фаза II (`ALGORITM` + DOK `06–10` + `FORMAT-ISTOCHNIKA`); арк-механика (`ARKA`, `RUKOVODSTVO-zahodami`); оба реальных прогона — многогранники (`process-report.md`) и обе арки Каталана (дистиллят снят read-only субагентом).

**Выводы грунтовки.**
- **Многогранники:** Часть 3 отчёта уже нарисовала «линейный алгоритм с гейтами». Боли A/B/C/F (нет единого алгоритма фазы, нарратив срезает математику, метания MD/HTML, стиль не вовремя) **закрыты Р19/Р20** (карточный граф, `rasskaz‖skelet`, скиллы внутрь, doc-движок). Не закрыто ровно = 4 пункта этой арки.
- **Каталан:** дисциплина держалась НА ЧЕЛОВЕКЕ — карточки/дайджесты руками и забывали; «находка без карточки=потеряна», «источник без дайджеста=не читан» — увещевания, принудитель `check_docs.py` остался «в очереди»; `VYCHITANO` завели задним числом → PDF перечитывали по кругу; состояние жило в рукописных `zapusk-*` handoff; токены не трекались. Волны нелинейны (ресёрч/ось/статья ×3 переигрывались фидбеком) → **гейты-не-рельсы подтверждены практикой.**

**Согласовано с владельцем (архитектура).**
- **сессия = вызов чётко прописанной функции** (его формулировка). Две гранулярности: станции (10 пакетов `DOK/ZAHOD/PRIMERY`) + микрофункции (каталог + скрипты).
- **функции = внутренние процедуры, НЕ skill-файлы** (мой вердикт, принят): паттерн `DOK/ZAHOD/PRIMERY` уже есть; skill-файлы дают авто-триггер по фразе, что вредит внутри управляемого конвейера (Р17, провал «платоновых тел»); драйвер маршрутизирует явно. Скилл на всю фабрику — возможен потом одним скиллом (беклог PLAYBOOK §7).
- **трекер = журнал вызовов + аудит диска.** Журнал (драйвер дописывает: функция·волна·результат·время·токены) ‖ `sostoyanie.py` (независимо сверяет гейты по диску). Расхождение журнал↔диск = **пойман молчаливый недобор**. «Где мы» ПЕРЕсчитывается аудитом, не держится руками (не рассинхронится).
- **папка лекции = одна, всё по ролям, слайды опциональны** (поле «слайды:да/нет» в брифе; лекция «на доске» типа орнаментов не заводит `src/`). Модель — трёхдомная Каталана под лекцию.

**Решение по разбивке.** 4 захода, 2 волны по 2 параллельно (①∥② → ③∥④). Файлы заходов не пересекаются; `KARTA §6`-регистрацию делает Cowork между волнами (заходы её не правят — иначе параллельный конфликт). Тяжёлая сборка — Claude Code (свежий контекст); Cowork держит стратегию, роя субагентов не разводит.

**Открыто (на закрытие/по факту прогонов).** Точные имена (`GEJTY.md`, `KATALOG.md`, `ZHURNAL.md`, `_generator/tools/`) — предложены в заходах, финал по факту. Браузерные гейты (render-identity, `audit.py`) гоняются на машине владельца (Playwright). Формат журнала (md-строки vs jsonl) — решит заход ③, отразить в дневнике.

## 2026-07-10 · Волна 1 принята + шов сведён (Cowork)

**Приёмка ①②.** Оба зелёные по гейтам RUKOVODSTVO: `ALGORITM.md` переписан на обе фазы (волновой драйвер Фазы I + сохранён детерминированный 6→10; преамбул «диск-состояние / сессия=вызов / гейты-не-рельсы»); `GEJTY.md` — 16 записей (G1–G11 + H1–H5), у каждой диск-признак; таблица Арх.2 приведена к Р19; `bootstrap_lekcia.py` прогнан (no/yes/идемпотентность/py_compile — зелёно); `PAPKA-LEKCII.md` — канон папки лекции. Токены харнесс не отдаёт — заявлено громко (не тихий недобор).

**Шов Волны 1 (параллель → рассинхрон путей).** ① придумал размещение Фазы I `<лекция>/faza1/{…}` (в проекте нигде не зафиксировано — реальная дыра: `FORMAT-ISTOCHNIKA` знал только `src/`); ② независимо построил реальный layout `kartoteka/ istochniki/ kotly/ chernovik/`. Свёл (Cowork): **канон = `PAPKA-LEKCII.md`** (②); диск-признаки `GEJTY.md` G1–G4/H1–H4 приведены к нему; `ALGORITM.md` (инлайн + Открытые вопросы) обновлён. Решение: **слой `skelet` = `kotly/matematika.md`** (математика-котёл→строгий слой, по Р19/DOK03-04); `chernovik/skelet.md` — опциональный снимок 4b, по умолчанию цитируется `kotly/matematika.md`. Провизорно; подтвердит первый реальный прогон #14.

**Ответы на ВОПРОСЫ исполнителей.** ①-Q1/②-Q2 (размещение) — решено выше. ①-Q2 (ключи полей брифа) — источник истины = сгенерированный `brief.md`-стаб из `bootstrap_lekcia.py`; заход ③ читает его, не выдумывает. ②-Q1 (`kotly/matematika.md` vs `chernovik/skelet.md`) — решено (skelet=kotly/matematika.md). ①-Q3 (устаревшая фраза в `ARHITEKTURA §Форматы и нюансы` вне таблицы) — мелкий беклог Фазы II, не блок.

**Осталось:** зарегистрировать в `KARTA §6` — `GEJTY.md`, `PAPKA-LEKCII.md`, `tools/bootstrap_lekcia.py`; обновить заходы ③④ под сведённый контракт → выдать Волну 2. Коммит новых файлов (② оставил незакоммиченным) — на владельце/при закрытии.
