# Канал исполнителя — БУТСТРАП (папка лекции + scaffold) · один заход до конца

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.

**Модель: `opusplan`** — layout папки имеет вкус (Opus), scaffold-скрипт — механика (Sonnet).

**Текст запуска (владелец → Claude Code):**
```
Модель: opusplan — layout проектируется по ходу, скрипт — Sonnet.
Claude Code, твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-10_orkestracia/kod_bootstrap.md. Прочитай ТОЛЬКО его и то, что он называет; остальной проект не изучай. Точка отката: git-коммит сразу. План/вопросы/отчёт пиши в этот же файл внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ). Ничего сверх задачи не трогай.
```

КОНТЕКСТ. Проект — фабрика научпоп-презентаций `_studio/`. Идёт арка «оркестрация фабрики». Твой заход определяет **ОДНУ папку лекции**: всё в одном месте, разложено по ролям, слайды опциональны. Модель — трёхдомная система проекта «Каталан», ужатая под лекцию. **Параллелен заходу ①** (разные файлы — не столкнётесь). Приёмка — по ОТЧЁТУ.

⚠ **Стоп до цели:** ты создаёшь СКЕЛЕТ папки (стаб-файлы) и scaffold-скрипт, НЕ прогоняешь конвейер и НЕ строишь трекер (заход ③).

## 0. ПЕРВЫЙ ХОД
Рабочая папка — корень `_studio/` (`/Users/ivanyakovlev/Documents/GitHub/materials/_studio`). Пути ниже — от неё; `../` = `materials/`. Точка отката: git-коммит сразу. Прочитать ТОЛЬКО:
- этот заход (канон layout — ниже, §3)
- `docs/kak-delat/ARKA.md` §9 — шаблоны `NAVIGATOR/PLAN/SESSIYA` (дневник)
- `konvejer/01-brief/DOK.md` — 8 полей брифа (добавляем 9-е поле «слайды»)
- `konvejer/02-reserch/DOK.md`, `konvejer/03-matbaza/DOK.md` — формат картотеки (карточка STANDART-uzla, KARTA-OBLASTI, котлы, TERMINY)
- `../catalan/spravochnik/STANDART-uzla.md`, `../catalan/spravochnik/KARTA-OBLASTI.md` — образец формы карточки/карты (копируешь ФОРМУ, не содержание)
- `../catalan/biblioteka/README.md` + `ls ../catalan/biblioteka/` — форма библиотеки (MANIFEST/VYCHITANO/SPISOK — если нет отдельными файлами, воссоздай по DOK 02)
- `konvejer/FORMAT-ISTOCHNIKA.md` — формат `src/`
- `ls ../_generator/skeleton/` + `../_generator/skeleton/SPEC.md` (если есть) — готовый скелет дека (копируешь для `src/`, НЕ переизобретаешь; Р16 buffon-base)
- `ls ../_generator/doc-demo/` — фронтматтер вкладок doc-движка (для `kotly/`)
ПЛАН — в `## ПЛАН`.

## 1. ДИСЦИПЛИНА (Карпатов)
Предпосылки/развилки назвать; минимум без спекуляций; идемпотентный скрипт; критерий, который может провалиться. Оспорить ложную предпосылку. Без роя субагентов.

## 2. ГРАНИЦЫ
- `../catalan/` и `../_generator/skeleton/` — READ-ONLY образцы (копируешь форму, оригинал не правишь).
- Стабы создают СТРУКТУРУ, не содержание лекции.
- НЕ строить трекер/каталог.
- `src/` — не переизобретать: копия `../_generator/skeleton/`.
- **`KARTA.md §6` НЕ править** (параллельно идёт заход ①). Новые файлы — в ОТЧЁТ, Cowork зарегистрирует.

## 3. ЗАДАЧА — два выхода

**(1) Спека layout** — файл `docs/spravka/PAPKA-LEKCII.md`: архитектура папки лекции, что где живёт, дисциплина каждой подпапки. Канон:
```
<лекция>/
  NAVIGATOR.md PLAN.md SESSIYA.md   дневник (шаблоны ARKA §9)
  brief.md                          бриф 8 полей + «слайды: да|нет»
  kartoteka/                        карточки STANDART-uzla + KARTA-OBLASTI + нити + каталог объектов
  istochniki/                       MANIFEST.md · VYCHITANO.md · SPISOK-skachat.md · TERMINY.md · pdf/
  kotly/                            matematika.md ‖ naupop.md (doc-движок, две вкладки)
  chernovik/                        rasskaz.md ‖ skelet.md — гибрид-источник (черновик рассказа)
  raskadrovka/                      демо слайдов
  src/  dist/                       ОПЦ — только «слайды: да» (копия _generator/skeleton/)
```
Для каждой подпапки пропиши: назначение, ключевые стаб-файлы, какой гейт/хук её касается (сослаться на `konvejer/GEJTY.md` — его пишет заход ①; если ещё не готов на момент чтения, оставь ссылку-пометку `→ GEJTY.md#<гейт>`). Поле «слайды: да|нет» управляет: заводить ли `src/` и применять ли гейты Фазы II (лекция «на доске», напр. орнаменты, → «нет»).

**(2) Scaffold-скрипт** `../_generator/tools/bootstrap_lekcia.py` (stdlib, без зависимостей): `python3 bootstrap_lekcia.py <путь/имя-лекции> [--slides yes|no]` создаёт дерево выше со стаб-файлами:
- **дневник** по шаблонам `ARKA §9`, заполнены именем лекции;
- **brief.md** — 8 полей-заглушек + `слайды: <из флага>`;
- **kartoteka/** — пустой `KARTA-OBLASTI.md` (заголовок + пустой индекс) + `STANDART-uzla.md` (копия формы Каталана как памятка) + стаб каталога объектов;
- **istochniki/** — `MANIFEST.md`/`VYCHITANO.md`/`SPISOK-skachat.md`/`TERMINY.md` стабы с заголовками + `pdf/.gitkeep`;
- **kotly/** — `matematika.md`, `naupop.md` с фронтматтером doc-движка (форма из `../_generator/doc-demo/`);
- **chernovik/** — `rasskaz.md`, `skelet.md` стабы;
- **raskadrovka/** — `.gitkeep`;
- если `--slides yes`: скопировать содержимое `../_generator/skeleton/` в `src/`.
Идемпотентность: существующую папку НЕ перезатирать — предупредить и выйти с ненулевым кодом.

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- `python3 ../_generator/tools/bootstrap_lekcia.py /tmp/testlek --slides no` создаёт ПОЛНОЕ дерево; все стаб-файлы на месте; `tree /tmp/testlek` совпадает со спекой; 0 висячих ссылок в стабах.
- с `--slides yes` дополнительно появляется `src/` = содержимое `../_generator/skeleton/` (сверить `ls`).
- повторный запуск на существующей папке НЕ затирает (предупреждение, exit≠0).
- `py_compile` проходит; только stdlib.
- прогон в `/tmp/`, НЕ внутри проекта; тестовые папки удалить после.

## 4. ВЕРИФИКАТОР
Скрипт создаёт (не теряет) — тяжёлый верификатор не нужен; достаточно критерия. Прогоны — в `/tmp/`, убрать за собой.

## 5. ОТЧЁТ → `## ОТЧЁТ`
что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / **новые файлы для `KARTA §6`** / вопросы владельцу / **время + токены**.

## ПЛАН — (заполняет исполнитель)

Прочитаны: этот заход · `ARKA.md` §9 · `01-brief/DOK.md` · `02-reserch/DOK.md` · `03-matbaza/DOK.md` · `catalan/spravochnik/{STANDART-uzla,KARTA-OBLASTI}.md` · `catalan/biblioteka/README.md` + `ls istochniki/` (там уже живут `MANIFEST.md`/`VYCHITANO.md`/`SPISOK-skachat.md` — беру их шапки как форму) · `FORMAT-ISTOCHNIKA.md` · `ls _generator/skeleton/` + `SPEC.md` · `ls _generator/doc-demo/` + фронтматтер `matematika.md`/`naupop.md`.

Точка отката: параллельный заход① в момент старта сам сделал `git add -A && git commit` (чекпоинт `ed3c807` — держал `.git/index.lock`, я дождался релиза, не трогая лок). Это и есть моя точка отката; отдельный коммит поверх не нужен.

**Предпосылки / развилки, которые называю явно:**
- `GEJTY.md` (конвейер) ещё не существует (заход① пишет его параллельно) → в спеке ссылки на гейты — плейсхолдеры `→ GEJTY.md#<гейт>`, без выдумывания точных имён якорей, которых не видел.
- Не читал `04-gibrid-istochnik/DOK.md` и `05-raskadrovka/DOK.md` (не названы заходом) → назначение `chernovik/` и `raskadrovka/` беру ровно из канон-таблицы §3 этого захода + смежных прочитанных DOK (02/03), не додумывая детали гейтов этих стадий.
- Отношение `kotly/matematika.md` (вкладка doc-движка, растёт в арках 2–3, несёт слой skelet) к `chernovik/skelet.md` (упомянут в каноне отдельно) — трактую как: `kotly/` = живой рабочий вид графа во время ресёрча/строгости, `chernovik/skelet.md` = замороженная выжимка строгого слоя для сшивки арки 4b с `rasskaz.md` (цитата-сноска, `03-matbaza/DOK.md` «Стык с аркой 4»). Это моя интерпретация — помечаю в ВОПРОСАХ, не блокирует скелет (оба файла — пустые стабы).
- `slides: да|нет` управляет ТОЛЬКО `src/`/`dist/` (копия `_generator/skeleton/`) — подтверждено текстом захода.

**Шаги:**
1. Написать `docs/spravka/PAPKA-LEKCII.md` — архитектура папки лекции (канон-дерево + назначение/стаб-файлы/гейт-заглушка на подпапку).
2. Написать `../_generator/tools/bootstrap_lekcia.py` (stdlib): создаёт дерево, идемпотентен (существующая папка → предупреждение + exit≠0), `--slides yes|no`.
3. Прогнать в `/tmp/`: без слайдов, со слайдами, повторный запуск (не затирает), `py_compile`. Убрать тестовые папки.
4. Отчёт: что сделал, как проверил, что не трогал, новые файлы для `KARTA §6`, вопросы, время+токены.

## ВОПРОСЫ — (заполняет исполнитель)

1. **`kotly/matematika.md` vs `chernovik/skelet.md`** — оба, по прочитанным DOK, несут «строгий слой skelet», но живут в разных папках канона (§3 этого захода). Я развёл их как «живой рабочий вид (kotly, арки 2–3)» vs «замороженная выжимка для цитаты в rasskaz.md (арка 4b)» — это МОЯ трактовка, не проверенная по `04-gibrid-istochnik/DOK.md` (не читал, не назван заходом). Если задумано иначе (например, `chernovik/skelet.md` = симлинк/копия-синхронизация с `kotly/matematika.md`, либо один из двух файлов лишний) — поправьте, оба сейчас независимые пустые стабы, переделка дешёвая.
2. **Куда физически кладётся `<лекция>/`?** Ни этот заход, ни прочитанные DOK не называют родительскую папку (`_studio/lekcii/<имя>/`? корень репо? рядом с `dandelin/`/`buffon/`?). Скрипт этого не предполагает — принимает произвольный путь; но `PAPKA-LEKCII.md` и стабы намеренно НЕ содержат путей-ссылок вглубь `_studio/` (кроме её собственной шапки), чтобы не зависеть от глубины. Когда решится — можно добавить в `PAPKA-LEKCII.md` строку «где живёт».
3. Имена гейтов в `PAPKA-LEKCII.md` (`GEJTY.md#brief`, `#fаktura-razvorot`, `#matematika-verna`, `#gibrid-istochnik`, `#raskadrovka`, `#format-istochnika`, `#sborka-qa`) — заглушки моего сочинения (кроме первых трёх, где текст гейта явно есть в прочитанных DOK и имя правдоподобно). Заход①, который пишет `GEJTY.md`, может назвать якоря иначе — тогда это находится и правится одним greп'ом по `PAPKA-LEKCII.md`.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал / зачем.**
1. [`docs/spravka/PAPKA-LEKCII.md`](docs/spravka/PAPKA-LEKCII.md) — спека layout папки лекции: канон-дерево (§3 этого захода) + на каждую подпапку назначение, стаб-состав, гейт-заглушка (`→ GEJTY.md#<гейт>`, т.к. `GEJTY.md` ещё не существует — пишет параллельный заход①). Источники формы — прочитанные `01-brief/03-matbaza/02-reserch DOK.md`, `STANDART-uzla.md`/`KARTA-OBLASTI.md`, `FORMAT-ISTOCHNIKA.md`, `_generator/skeleton/SPEC.md`, фронтматтер `_generator/doc-demo/*.md`.
2. [`../_generator/tools/bootstrap_lekcia.py`](../_generator/tools/bootstrap_lekcia.py) — идемпотентный stdlib-скрипт: `python3 bootstrap_lekcia.py <путь> [--slides yes|no]` создаёт полное дерево по спеке из (1); `kartoteka/STANDART-uzla.md` — байт-в-байт копия `catalan/spravochnik/STANDART-uzla.md` (памятка формы, не переизобретение); `--slides yes` копирует `_generator/skeleton/` в `src/` (`shutil.copytree`, без изменений).

**Как проверил (в `/tmp/`, не в проекте).**
- `py_compile ../_generator/tools/bootstrap_lekcia.py` — прошёл.
- `bootstrap_lekcia.py /tmp/testlek --slides no` — exit 0; `find` дерева совпал 1:1 с деревом из `PAPKA-LEKCII.md` (17 файлов/папок, сверено построчно); греп `\]\(...\)` по всем стабам — 0 markdown-ссылок (условие «0 висячих ссылок» тривиально выполнено, стабы намеренно без путей-ссылок вглубь проекта, см. ВОПРОС 2).
- Повторный запуск `bootstrap_lekcia.py /tmp/testlek --slides no` — `ОШИБКА: папка уже существует ... не затираю`, exit 1; содержимое папки не тронуто.
- `bootstrap_lekcia.py /tmp/testlek2 --slides yes` — exit 0; `diff` списков файлов `testlek2/src/` и `_generator/skeleton/` — идентичны (та же копия — не переизобретение).
- `diff kartoteka/STANDART-uzla.md` (сгенерированный) vs `catalan/spravochnik/STANDART-uzla.md` — `IDENTICAL`.
- Тестовые папки `/tmp/testlek`, `/tmp/testlek2` удалены (`rm -rf`) после прогона.

**Что НЕ трогал.**
- `../catalan/` и `../_generator/skeleton/` — только читал/копировал, не редактировал (read-only образцы).
- `KARTA.md §6` — не правил (параллельно заход①).
- Не строил трекер/каталог, не прогонял конвейер, не читал ничего кроме названных заходом файлов + `catalan/biblioteka/istochniki/` листинга (для формы MANIFEST/VYCHITANO/SPISOK — они там УЖЕ существуют как отдельные файлы, доп. воссоздавать «по DOK 02» с нуля не понадобилось, только скопировал заголовочную форму).
- Git: отдельный чекпоинт-коммит не делал — параллельный заход① сам закоммитил снимок (`ed3c807`) в момент моего старта (я дождался снятия `.git/index.lock`, не трогая его руками); новые файлы этого захода (`PAPKA-LEKCII.md`, `bootstrap_lekcia.py`) остались НЕзакоммиченными — коммит не входил в задачу этого захода явно как отдельное финальное действие, оставляю владельцу/Cowork решить момент коммита.

**Новые файлы для `KARTA §6`.**
- `_studio/docs/spravka/PAPKA-LEKCII.md`
- `_generator/tools/bootstrap_lekcia.py`

**Время + токены.** Ход занял мало часов реального времени (~10 минут ожидания git-лока заходом①, остальное — быстро); токены не инструментированы этим каналом — оценка не даю, если нужно точное число — снять с харнесса.
