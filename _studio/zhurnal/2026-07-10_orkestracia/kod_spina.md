# Канал исполнителя — СПИНА (единый ALGORITM обе фазы + реестр гейтов) · один заход до конца

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО, не скорость. Часы — норма.

**Модель: `opusplan`** — спину и модель гейтов проектируешь по ходу (Opus), правку таблицы/линковку — механикой (Sonnet).

**Текст запуска (владелец → Claude Code):**
```
Модель: opusplan — дизайн спины рождается по ходу, механика правок — Sonnet.
Claude Code, твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-10_orkestracia/kod_spina.md. Прочитай ТОЛЬКО его и то, что он называет; остальной проект не изучай. Точка отката: git-коммит сразу. План/вопросы/отчёт пиши в этот же файл внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ). Ничего сверх задачи не трогай.
```

КОНТЕКСТ. Проект — фабрика научпоп-презентаций `_studio/` (конвейер идея→дек, 2 фазы, 10 шагов-пакетов). Идёт арка «оркестрация фабрики»: строим обёртку вокруг готового конвейера. Твой заход — **ПЕРВЫЙ, фундамент**: трекер (заход ③) и каталог (заход ④) опираются на реестр гейтов, который создашь ТЫ. Приёмка — по ОТЧЁТУ, без построчной сверки.

⚠ **Стоп до цели:** ты НЕ строишь трекер-скрипт (это заход ③). Ты только описываешь алгоритм и определяешь реестр гейтов КАК ДАННЫЕ, которые скрипт потом реализует.

## 0. ПЕРВЫЙ ХОД
Рабочая папка — корень `_studio/` (полный путь `/Users/ivanyakovlev/Documents/GitHub/materials/_studio`). Все пути ниже — от неё. Точка отката: git-коммит сразу. Прочитать ТОЛЬКО:
- `konvejer/ALGORITM.md` — текущий драйвер Фазы II (эталон формы; расширяешь на обе фазы)
- `konvejer/00-KONVEJER.md` — карта 10 шагов
- `konvejer/01-brief/DOK.md`, `konvejer/02-reserch/DOK.md`, `konvejer/03-matbaza/DOK.md`, `konvejer/04-gibrid-istochnik/DOK.md`, `konvejer/05-raskadrovka/DOK.md` — гейты Фазы I (строка «Гейт», контракт вход→выход, «next», развилки владельцу)
- `konvejer/06-tekst/DOK.md`, `konvejer/07-verstka/DOK.md`, `konvejer/08-sceny/DOK.md`, `konvejer/09-illustracii/DOK.md`, `konvejer/10-sborka-qa/DOK.md` — гейты Фазы II
- `docs/spravka/FAZA-1-REDESIGN.md` — пересобранная Фаза I (модель Р19)
- `docs/spravka/ARHITEKTURA-FABRIKI.md` — файл под правку (таблица Арх.2 Фазы I устарела)
- `docs/pochemu-i-videnie/RESHENIYA.md` — Р19, Р6, Р9 (модель фаз, паттерн арк-файла)
- `konvejer/FORMAT-ISTOCHNIKA.md` — где живёт проверяемое на диске состояние (слои `src/`, `brief.md`; картотека — из DOK 02/03)
ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (правишь `ALGORITM.md` и таблицу Арх.2 в `ARHITEKTURA-FABRIKI.md` точечно, не переписывая валидное); критерий, который может провалиться (§3). Оспорить ложную предпосылку. Сохранять по умолчанию. Без роя субагентов.

## 2. ГРАНИЦЫ
- НЕ переоткрывать развилки редизайна Фазы I (формат интервью A1, `proof-self-review` внутрь/внешне, упаковка нарратива 4a, форма записи слайда) — алгоритм/гейты описываешь ПОВЕРХ уже принятой модели (Р19).
- НЕ трогать содержимое DOK-пакетов — только читать и ссылаться.
- НЕ строить трекер-скрипт (заход ③). Определяешь реестр гейтов как данные для него.
- В `ARHITEKTURA-FABRIKI.md` правишь ТОЛЬКО таблицу Арх.2 Фазы I (устарела до Р19); Фазу II и остальной файл не трогать (валидны).
- Фаза II драйвер в `ALGORITM.md` уже хорош — расширяешь ВОКРУГ него, не ломая деталей (развилки, «что не продолбать»).
- **`KARTA.md §6` НЕ править** (параллельно идёт заход ②, тоже тронул бы её — конфликт). Новые файлы перечисли в ОТЧЁТЕ — Cowork зарегистрирует.

## 3. ЗАДАЧА — три выхода

**(1) Единый спинной алгоритм** — переписать `konvejer/ALGORITM.md` из «только Фаза II» в «обе фазы»:
- **общий преамбул:** модель состояния — гейты как чекпоинты НА ДИСКЕ (не в памяти сессии); «сессия = вызов функции»; **гейты-НЕ-рельсы** (возвраты-волны разрешены — математика/ресёрч идут волнами, grounded в Каталане; молчаливый прыжок вперёд через незакрытый гейт — дефект).
- **Фаза I (шаги 1–5): ВОЛНОВОЙ драйвер** — интерактив, гейты автора, возвраты разрешены. Состояние = карточный граф + котлы (doc-движок) + слой `skelet` + гибрид-источник. На каждый шаг: вход · действие (→DOK) · выход · гейт · развилки/чекпоинты владельцу. Явно отрази нелинейность (находка волны ретроактивно меняет карту).
- **стык Фаза I→II** — precondition (уже описан в текущем `ALGORITM`; сохранить/уточнить).
- **Фаза II (шаги 6–10):** существующий детерминированный драйвер (сохранить по сути; не потерять развилки/«что не продолбать»).
- одна терминология, один driver-раздел, одна сводка «что не продолбать» на обе фазы.

**(2) Реестр гейтов** — новый файл `konvejer/GEJTY.md`. КАЖДЫЙ гейт — структурная запись, которую скрипт-трекер (③) проверит по диску:
```
id · стадия/граница · что проверяет (человекочитаемо) · признак-на-диске (грепаемое/файл-условие) · тип (жёсткий-стоп-вперёд | автор-чекпоинт | дисциплина-хук=WARN) · возврат-разрешён (да/нет)
```
Покрыть:
- **5 гейтов-границ Фазы I:** бриф закрыт (8 полей + throughline-кандидат + бюджет слайдов посчитан); ресёрч (≥5 источников + «ни одной находки без карточки» + есть «но»); математика верна (0 открытых `⚑ Флаг` + уровень-тег на каждом мат-блоке); источник закрыт (0 флагов + 🖼 на такт + цитаты-сноски резолвятся в skelet-якоря); раскадровка (`slide_order` покрывает `slides/` + регистр+бюджет заданы в `brief.md`).
- **вход Фазы II** (precondition из `ALGORITM`).
- **5 гейтов Фазы II** (из DOK 06–10: плакатный баланс; 0 overflow + `--t-body` не уменьшен; сцены без дед-кликов; `var(--x)` определён; 5-слойный гейт сборки).
- **дисциплина-хуки:** карточка без `источник`; источник в `MANIFEST` без дайджеста `VYCHITANO`; висячее ребро в `KARTA-OBLASTI`; открытый `⚑ Флаг` без парного `Флаг закрыт`; `slide_order`-сироты/дубли.
Признак-на-диске — КОНКРЕТНО (напр.: «grep `⚑ Флаг` по `chernovik/`,`kotly/` → каждое совпадение имеет парный `Флаг закрыт`»). Гейты, требующие браузера (Фаза II аудит/render-identity), пометь «браузерный — локально у владельца» (трекер их вынесет отдельно).

**(3) Фикс Арх.2** — в `ARHITEKTURA-FABRIKI.md` привести таблицу Фазы I к Р19: карточный граф (котлы математика‖научпоп), слой `skelet`, `rasskaz`, doc-движок; бриф — 8 полей (не 5); убрать «один гибрид-HTML» и «HTML+MathJax с шага 3» там, где заменено редизайном. Одной строкой пометить источник модели (`FAZA-1-REDESIGN.md` + Р19).

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- `ALGORITM.md` содержит явные разделы для шагов 1–5 И 6–10, у каждого вход/выход/гейт/развилка; преамбул про диск-состояние и гейты-не-рельсы присутствует; Фаза II не потеряла деталей (сверить: прежние развилки и «что не продолбать» на месте).
- `GEJTY.md` существует; ≥ 11 гейтов + 5 хуков; у КАЖДОГО заполнены «признак-на-диске» (грепаемо/файл-условием) и «тип»; ни одной абстрактной записи («проверить качество» — недопустимо).
- `ARHITEKTURA-FABRIKI.md` Арх.2 Фаза I содержит «карточный граф»/«skelet»/«rasskaz» и НЕ содержит «один гибрид-HTML» в строке шага 4.
- 0 битых внутренних ссылок в изменённых/созданных файлах.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
что сделал + ЗАЧЕМ / как проверил критерий / что НЕ трогал / **список новых файлов для регистрации в `KARTA §6`** / вопросы владельцу (гейт неоднозначен → сюда, не выдумывать) / **время прогона + токены**.

## ПЛАН — (заполняет исполнитель)

Прочитано: `ALGORITM.md`, `00-KONVEJER.md`, `01-brief` … `10-sborka-qa` (все `DOK.md`), `FAZA-1-REDESIGN.md`, `ARHITEKTURA-FABRIKI.md`, `RESHENIYA.md` (Р1–Р20), `FORMAT-ISTOCHNIKA.md`. Остальной проект не открывал.

**Ключевая находка до кода:** физическое место хранения Фазы I на диске (карточный граф/skelet/rasskaz per-лекция) НИГДЕ явно не зафиксировано — `FORMAT-ISTOCHNIKA.md` описывает только `<лекция>/src/` (Фаза II). Это не одна из 4 запертых развилок редизайна (формат интервью A1 / proof-self-review / упаковка 4a / форма записи слайда) — отдельное, никем не решённое измерение. Раз ALGORITM.md обязан описать модель состояния Фазы I «на диске», предлагаю рабочую конвенцию `<лекция>/faza1/{brief.md, karta-oblasti.md, terminy.md, skelet.md, rasskaz.md}` (аналог `<лекция>/src/` для Фазы II) — помечаю явно как ПРЕДПОЛОЖЕНИЕ, не решение, выношу в `## ВОПРОСЫ`.

1. **`ALGORITM.md` — переписать целиком в «обе фазы»:**
   - общий преамбул (диск-состояние, «сессия=вызов функции», гейты-не-рельсы/волны, grounded в Каталане);
   - Фаза I (1–5): волновой псевдокод-драйвер с возвратами + по каждому шагу вход/действие/выход/гейт/развилки (беру из DOK 01–05, ничего не выдумываю сверх прочитанного);
   - стык Фаза I→II — сохранить существующий блок «Вход Фазы II» почти дословно (задача просит уточнить, не переписывать);
   - Фаза II (6–10) — существующий детерминированный драйвер и все секции («Стыки с архитектурой», «Гейт приёмки», развилки) сохранить по сути, не терять деталей;
   - объединить «Развилки/что не продолбать» в ОДНУ сводку на обе фазы;
   - обновить «Якоря» — добавить пакеты 01–05.
2. **`konvejer/GEJTY.md` — новый файл.** 11 гейтов (5 границ Фазы I + вход Фазы II + 5 гейтов Фазы II) + 5 дисциплина-хуков (WARN). У каждого: id·стадия·что проверяет·признак-на-диске (конкретный grep/файл-условие)·тип·возврат-разрешён. Браузерные гейты (сцены/аудит/scene-diff/рендер) помечаю «браузерный — локально у владельца».
3. **`ARHITEKTURA-FABRIKI.md` — точечная правка ТОЛЬКО таблицы Арх.2 Фазы I** (строки её 5 шагов): карточный граф/skelet/rasskaz/doc-движок, бриф 8 полей, убрать «один гибрид-HTML»/«HTML+MathJax с шага 3» из этой таблицы. Остальной файл (Фаза II, «Форматы и нюансы» — там тоже есть похожая устаревшая фраза, но она вне названной таблицы) НЕ трогаю — оставлю пометку в ОТЧЁТЕ как находку вне мандата.
4. Проверить критерий готовности (§3 захода) построчно, сверить внутренние ссылки на битость.
5. Заполнить `## ВОПРОСЫ` (включая находку выше) и `## ОТЧЁТ`.

Хирургия: правлю `ALGORITM.md` целиком (задача явно просит переписать), но не трогаю пакеты `01-05/06-10 DOK.md` (только читаю/ссылаюсь).

## ВОПРОСЫ — (заполняет исполнитель)

1. **Физическое расположение Фазы I на диске — не зафиксировано нигде в прочитанном.** `FORMAT-ISTOCHNIKA.md` описывает только `<лекция>/src/` (Фаза II). Для карточного графа/`skelet`/`rasskaz` per-лекция я предложил рабочую конвенцию `<лекция>/faza1/{brief.md, karta-oblasti.md, terminy.md, skelet.md, rasskaz.md}` (по аналогии с `src/`) и на неё опираются признаки-на-диске гейтов G1–G4 в `GEJTY.md`. Это НЕ решение владельца — только предположение, чтобы гейты не остались абстрактными («ни одной абстрактной записи» — прямое требование критерия). Нужно подтвердить или заменить: другое имя папки, другая разбивка файлов (напр. один файл на оба котла vs раздельные), или решение отложить до трекера (заход ③).
2. **Точные ключи полей брифа** (`тема:`, `аудитория:` и т.д. в `brief.md`) я подставил по смыслу 8 полей из `01-brief/DOK.md`, но нигде не встретил, что `01-brief/ZAHOD.md` их так называет буквально (ZAHOD-файлы пакетов не входили в заход — не читал). Признак-на-диске G1 в `GEJTY.md` явно помечен «сверить при первом деке» — трекер (заход ③) должен свериться с реальным `ZAHOD.md` перед реализацией.
3. Не мой вопрос, но нашёл по пути: `ARHITEKTURA-FABRIKI.md §Форматы и нюансы` (строка про «источник — HTML(+MathJax рантайм...) с шага 3») содержит ту же устаревшую формулировку, что и таблица Арх.2, но лежит ВНЕ названной таблицы → границей захода запрещено трогать. Оставляю как есть, флагом для отдельного захода.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + зачем:**
1. **`konvejer/ALGORITM.md` переписан целиком** — из «только Фаза II» в «обе фазы». Добавлен общий преамбул (диск-состояние, «сессия=вызов функции», «гейты-не-рельсы» с явным разрешением возвратов-волн, grounded в Каталане), волновой псевдокод-драйвер Фазы I с покроковым вход/действие/выход/гейт/развилки для шагов 1–5 (взято дословно из `DOK.md` 01–05, ничего не выдумано сверх прочитанного), контракт возврата throughline (B14) вынесен отдельным блоком. Существующий детерминированный драйвер Фазы II (6–10), «Стыки с архитектурой», оба «Гейта приёмки» и точный список развилок/«что не продолбать» — сохранены по сути; развилки объединены в ОДНУ сводку на обе фазы (как требовал §3). Якоря дополнены пакетами 01–05 и ссылкой на `GEJTY.md`.
2. **`konvejer/GEJTY.md` создан** — реестр из 16 записей: G1–G5 (границы Фазы I) · G6 (вход Фазы II) · G7–G11 (гейты Фазы II) · H1–H5 (дисциплина-хуки WARN). У каждой — id·стадия·что проверяет·признак-на-диске (конкретный grep/файл-условие, не абстракция)·тип·возврат-разрешён. Браузерные проверки помечены `[браузерный]`/«локально у владельца» (аудит/render-identity/scene-diff/svgOverflow), как просил заход. Формулировки гейтов взяты дословно из соответствующих `DOK.md`, не переизобретены.
3. **`docs/spravka/ARHITEKTURA-FABRIKI.md`** — точечно поправлена ТОЛЬКО таблица Арх.2 Фазы I (5 строк): «карточный граф»/`skelet`/`rasskaz`/doc-движок вместо «один гибрид-HTML»; бриф 8 полей вместо 5; «HTML+MathJax» убрано из строки шага 3. Добавлена одна строка-пометка источника модели (`FAZA-1-REDESIGN.md` + Р19). Остальной файл (Фаза II, «Форматы и нюансы», всё прочее) не тронут.

**Как проверил критерий:**
- `ALGORITM.md`: разделы для 1–5 и 6–10 присутствуют, у каждого шага явные вход/выход/гейт/развилка; преамбул на месте; Фаза II сверена построчно с исходным файлом (прочитан целиком до правки) — псевдокод, все 5 шагов, «переносим дословно», 4 пункта «Стыков», оба гейта-приёмки сохранены без потери деталей.
- `GEJTY.md`: посчитал записи — G1–G11 (11) + H1–H5 (5) = 16, ≥11+5 выполнено; у каждой записи заполнены «признак-на-диске» и «тип» (грепом проверил себя — ни одной строки вида «проверить качество»).
- `ARHITEKTURA-FABRIKI.md`: построчно сверил новую таблицу — есть «карточный граф», «skelet», «rasskaz»; строки «один гибрид-HTML» и «HTML+MathJax» в таблице отсутствуют (grep по файлу после правки).
- Битые ссылки: `git status`+`ls -e` по всем файлам, названным в изменённых/созданных документах (`00-KONVEJER.md`, `FORMAT-ISTOCHNIKA.md`, `FAZA-1-REDESIGN.md`, `RESHENIYA.md`, `verifikacia-paketov.md`, все `NN-*/DOK.md`, `_generator/{build_deck.py,SLIDE-FORMAT.md,DESIGN.md}`, `dandelin`, `buffon`) — все существуют, 0 битых.

**Что НЕ трогал:** сам конвейер (`01-05/06-10 DOK.md`, `ZAHOD.md`, `PRIMERY.md`) — только читал; `KARTA.md §6` — не открывал (граница захода, конфликт с заходом ②); `ARHITEKTURA-FABRIKI.md` Фазу II и «Форматы и нюансы»; трекер-скрипт не строил (это заход ③) — гейты оставлены как данные.

**Новые файлы для регистрации в `KARTA §6`:**
- `_studio/konvejer/GEJTY.md`

**Изменённые файлы (не новые, но затронутые):**
- `_studio/konvejer/ALGORITM.md`
- `_studio/docs/spravka/ARHITEKTURA-FABRIKI.md`

**Вопросы владельцу:** см. `## ВОПРОСЫ` выше (главный — расположение Фазы I на диске, влияет на весь `GEJTY.md`).

**Время прогона + токены:** заход прошёл одним непрерывным проходом (чтение 16 файлов параллельно → план → 3 записи/правки → верификация). Точных цифр токенов из сессии не вижу (харнесс не отдаёт счётчик агенту) — по объёму прочитанного+написанного оцениваю как заход среднего-крупного размера (много контекста на входе, три содержательных артефакта на выходе). Владелец может свериться по логам Cowork/биллингу точнее, чем я изнутри сессии.
