# Канал исполнителя — КАТАЛОГ функций «инструмент-под-задачу» · один заход до конца

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.

**Модель: `Sonnet`** — harvest из готовых DOK + вложенного дистиллята; план прямой.

**Текст запуска (владелец → Claude Code):**
```
Модель: Sonnet — harvest из готовых DOK, план прямой.
Claude Code, твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-10_orkestracia/kod_katalog.md. Прочитай ТОЛЬКО его и то, что он называет; остальной проект не изучай. Точка отката: git-коммит сразу. План/вопросы/отчёт пиши в этот же файл внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ). Ничего сверх задачи не трогай.
```

КОНТЕКСТ. Проект — фабрика `_studio/`. Арка «оркестрация фабрики», **ВТОРАЯ волна** (параллелен заходу ③, разные файлы). Собираешь КАТАЛОГ функций фабрики: что можно «вызвать» и каким инструментом. Владелец: «сессия = вызов чётко прописанной функции; хочу, чтобы функции были прописаны и вызывались». Две гранулярности: **станции** (10 шагов) + **микрофункции** (мелкие, часто повторяемые). **Skill-файлы НЕ заводим** (Р17 — скиллы внутрь; внешние допущены только код + 3D `threejs`). Приёмка — по ОТЧЁТУ.

## 0. ПЕРВЫЙ ХОД
Рабочая папка — корень `_studio/`. git-коммит сразу. Прочитать ТОЛЬКО:
- `konvejer/00-KONVEJER.md` + все `konvejer/NN/DOK.md` (`01`–`10`) — станции: назначение, вход→выход, гейт, ловушки, ВНЕШНИЕ вызовы, микрооперации внутри
- `konvejer/GEJTY.md` (готов, В1) — гейты станций = G1–G11, авто-хуки = H1–H5; привязывай микрофункции-хуки (карточка/дайджест/индекс/`⚑ Флаг`) к конкретным H1–H5, станции — к G-id
- `docs/spravka/PAPKA-LEKCII.md` (готов, В1) — layout папки лекции; учесть `bootstrap_lekcia.py` (готов) и `sostoyanie.py` (строит заход ③) как функции
- `docs/pochemu-i-videnie/RESHENIYA.md` Р17, Р20 — какие внешние допущены (только код + 3D)
- **вложенный дистиллят §Каталан ниже** — живой список микрофункций из прогонов; в `../catalan/` НЕ ходи (дорого), бери отсюда
ПЛАН — в `## ПЛАН`.

## §Каталан — микрофункции из живых прогонов (готовое сырьё, не переоткрывать)
- **скачать источник:** curl PDF → имя `Автор — Название (id/год).pdf` (авторы со страницы arXiv abstract, не выдумывать) → подпапка → строка в `MANIFEST`+`SPISOK`
- **извлечь текст из PDF:** `pdftotext`/`pdfplumber`, НЕ листать картинками — предусловие любого чтения
- **дайджест `VYCHITANO`:** «что ЕСТЬ / чего НЕТ / стр. / уровень»; правило «источник без дайджеста = не читан»
- **карточка-узел** (STANDART-uzla): находка + связи + указатель источника; правило «находка без карточки = потеряна» — **авто-хук**
- **индекс `KARTA-OBLASTI`:** резолвит каждый id; висячее ребро = дефект — **авто-хук**
- **`verify_*.py`:** численно сверить тождество на малых n (напр. рекуррента, вставка листа)
- **tar-снапшот** перед мутацией — **авто-хук** (перед правкой)
- **extract конструкции** (read-only): вынуть биекцию/пример + вердикт о «школьности» (отрицательный результат валиден)
- **бэкфилл дайджестов:** механически из старых отчётов, ноль выдумки
- **переупаковать проработанное** → скелет по актам
- **расширить HTML:** текст ×3–4, типографика, боковое оглавление, плейсхолдеры под картинки (SVG рисует аналитик, не Code)
- **сборка/нарезка карты** детерминированным скриптом (общий HTML → N лекций)
- **сверка `ls`↔`MANIFEST`** (0 сирот)

## 1. ДИСЦИПЛИНА (Карпатов)
Предпосылки назвать; каталог — индекс, не сочинение; критерий может провалиться. Без роя субагентов.

## 2. ГРАНИЦЫ
- Каталог — ИНДЕКС/harvest, НЕ новые процедуры: указывай на существующие `DOK`/`PRIMERY`/скрипты, не переписывай их.
- Ни одна функция не должна указывать на внешний скилл, кроме код/3D (Р17). Нашёл такую в DOK (напр. `math-russian-terminology` в `06`/`09`) → пометь «УСТАРЕЛО → внутрь / per-project `TERMINY`» (беклог Фазы II), НЕ узаконивай.
- Ничего исполняемого не строить.
- `KARTA.md §6` НЕ править (заход ③ параллельно). Новый файл — в ОТЧЁТ.

## 3. ЗАДАЧА — один выход
`konvejer/KATALOG.md` — каталог «инструмент-под-задачу». Вверху короткая преамбула: как пользоваться (драйвер `ALGORITM.md` направляет в станцию → станция при нужде дёргает микрофункцию → авто-хуки обязательны, их же проверяет `sostoyanie.py`). Две секции:

**A. Станции (крупные функции, «сессия ≈ вызов»).** Строка на каждый из 10 шагов + под-функции Фазы I, которые владелец назвал явно (интервью, матресёрч, научпопресёрч, нарратив, раскадровка — указать, где они внутри шагов 1–5 / котлов 2–3). Поля: `функция · когда вызывается · вход→выход · пакет NN/{DOK,ZAHOD,PRIMERY} · гейт (id из GEJTY)`.

**B. Микрофункции (мелкие, часто повторяемые).** Строка на каждую (из DOK 01–10 + §Каталан). Поля: `функция · когда/триггер · авто-хук? (да/нет) · вход→выход · инструмент (внутренний шаг-метод в DOK X | внешний: код | внешний: threejs | скрипт: путь) · где пример/процедура (указатель)`. Авто-хуки (карточка, дайджест, снапшот, индекс) — перекрёстно сослать на соответствующий гейт-хук в `GEJTY.md`.

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- секция A покрывает ВСЕ 10 станций + 5 названных под-функций Фазы I; у каждой пакет + гейт-id.
- секция B содержит КАЖДУЮ микрофункцию из §Каталан + все, что грепаются в DOK 01–10 (скачать/дайджест/карточка/`verify`/снапшот/`svgo`/`build_deck`/`render`/`audit`/`bootstrap`/`sostoyanie`…); у каждой заполнен «инструмент» + указатель.
- 0 ссылок на внешний скилл вне {код, `threejs`}; устаревшие ссылки помечены «УСТАРЕЛО».
- авто-хуки перекрёстно связаны с `GEJTY.md`.

## 5. ОТЧЁТ → `## ОТЧЁТ`
что сделал + ЗАЧЕМ / как проверил покрытие / что НЕ трогал / **новый файл для `KARTA §6`** / найденные устаревшие внешние вызовы (списком, в беклог Фазы II) / **время + токены**.

## ПЛАН — (заполняет исполнитель)
Точка отката: `339fc7a` (чекпоинт параллельного захода `kod_treker`, рабочее дерево `_studio` было чистым до правки — свежих правок ДО меня не было, коммитить нечего было).

Прочитано (ровно по списку §0): `00-KONVEJER.md`, `GEJTY.md`, `PAPKA-LEKCII.md`, `RESHENIYA.md` (целиком, Р17/Р20 — в контексте всех решений), все 10 `konvejer/NN/DOK.md`, §Каталан внутри этого файла. `../catalan/` не открывал.

Строю `konvejer/KATALOG.md` одним файлом:
- Преамбула: как пользоваться (`ALGORITM.md` → станция → микрофункция → авто-хуки, их проверяет `sostoyanie.py`).
- **Секция A (станции):** 10 строк станций 1–10 + 5 строк названных под-функций Фазы I (интервью=ст.1 целиком; матресёрч=котёл математика ст.2→дораст ст.3; научпопресёрч=котёл научпоп ст.2; нарратив=под-стадия 4a внутри ст.4; раскадровка=ст.5 целиком) — у каждой пакет+гейт-id.
- **Секция B (микрофункции):** все 13 пунктов §Каталан + микрофункции, грепнутые в DOK 01–10 по списку критерия (скачать/дайджест/карточка/verify/снапшот/svgo/build_deck/render/audit/bootstrap/sostoyanie + ⚑Флаг/🖼/TERMINY/slide_order-SPLIT/build_doc/proof-self-review/3D-harness/растр-пайплайн) — инструмент+указатель на каждую, авто-хуки перекрёстно на H1–H5.
- Устаревшую ссылку нашёл живьём ТОЛЬКО в `06-tekst/{DOK,PRIMERY,ZAHOD}.md` (грепом): `02-reserch`/`03-matbaza` её лишь УПОМИНАЮТ как «не звать» (уже исправлено, не стейл). `00-KONVEJER.md` утверждает, что стейл-ссылка есть ЕЩЁ и в арке 09 — грепом это НЕ подтвердилось (0 совпадений в `09-illustracii/*`). Расхождение фиксирую в ОТЧЁТЕ, файл `00-KONVEJER.md` не правлю (вне задачи).

Ничего в `KARTA.md §6` не трогаю (параллельный заход ③). Ничего исполняемого не строю — только индекс-таблицы с указателями.

## ВОПРОСЫ — (заполняет исполнитель)
Нет блокирующих. Одно расхождение зафиксировано ниже в ОТЧЁТе (не блокирует приёмку, беклог).

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал.** Собрал `konvejer/KATALOG.md` — индекс «инструмент под задачу», две секции:
- **A (станции):** 10 строк (станции 1–10, поля функция·когда·вход→выход·пакет·гейт) + 5 строк названных владельцем под-функций Фазы I (интервью=ст.1 целиком; матресёрч=котёл математика ст.2→дораст ст.3; научпопресёрч=котёл научпоп ст.2; нарратив=под-стадия 4a внутри ст.4; раскадровка=ст.5 целиком, дубль строки — владелец назвал её явно).
- **B (микрофункции):** 27 строк — все 13 пунктов §Каталан дословно + микрофункции, найденные грепом по DOK 01–10 через ключевые слова критерия (скачать/дайджест/карточка/verify/снапшот/svgo/build_deck/render/audit/bootstrap/sostoyanie) плюс смежные, явно названные в DOK (⚑Флаг/Флаг закрыт, 🖼-описание, sлайд_order-SPLIT, сцены-шорткаты→data-scene-from, proof-self-review, сверка TERMINY.md, build_doc.py, 3D standalone-harness, растр→base64). У каждой заполнены инструмент (внутренний шаг-метод в DOK X | внешний: код | внешний: threejs | скрипт: путь) и указатель.

**ЗАЧЕМ.** Владелец хочет вызывать функции фабрики явно («сессия = вызов чётко прописанной функции»); каталог даёт единую точку входа «чем это вызвать», не дублируя сами процедуры (они остаются в DOK/скриптах).

**Как проверил покрытие (критерий готовности).**
- Секция A: построчно сверил с таблицей `00-KONVEJER.md` (10/10 станций) + списком названных под-функций из задания (5/5: интервью, матресёрч, научпопресёрч, нарратив, раскадровка) — у каждой строки есть пакет и id гейта.
- Секция B: сверил построчно с §Каталан (13/13 пунктов) + прогнал `grep -rln` по ключевым словам критерия (скачать/дайджест/карточка/verify/снапшот/svgo/build_deck/render/audit/bootstrap/sostoyanie) по всем 10 `DOK.md` — каждое совпадение либо уже строка каталога, либо добавил новую.
- Внешние ссылки: `grep -n "внешний:"` по готовому файлу — 7 совпадений, все `код` или `threejs` (0 иных). `grep -rln "math-russian-terminology" konvejer/` — живой вызов только в `06-tekst/{DOK,PRIMERY,ZAHOD}.md`; помечен `УСТАРЕЛО` в каталоге и в беклоге файла.
- Авто-хуки: 5 строк секции B несут `да → Hx` (H1 карточка, H2 дайджест, H3 индекс, H4 флаг, H5 slide_order) — все 5 хуков `GEJTY.md` перекрёстно сосланы. Одна строка («tar-снапшот») — это авто-хук по факту (§Каталан так и называет), но собственного id в `H1–H5` не имеет — пометил как разрыв, не выдумал несуществующий id.

**Что НЕ трогал.** Сам проект вне названного списка (`../catalan/` не открывал); `ALGORITM.md`/`FORMAT-ISTOCHNIKA.md` не читал, только сослался по имени (как уже ссылались прочитанные файлы); `KARTA.md §6` не правил (параллельный заход ③, файл — ниже, для его регистрации); `00-KONVEJER.md` не правил, хотя нашёл в нём неточность.

**Новый файл для `KARTA §6`:** `_studio/konvejer/KATALOG.md`.

**Найденные устаревшие внешние вызовы (беклог Фазы II):**
- `06-tekst/{DOK,PRIMERY,ZAHOD}.md` — живой вызов `math-russian-terminology` (DOK.md строки 5, 44, 57) → внести внутрь / per-project `TERMINY.md` тем же методом, что уже применён в ст.2/3 (Р17).
- Побочная находка: `00-KONVEJER.md` (строка 43) утверждает, что устаревшая ссылка есть «в арках 06/09» — грепом по `09-illustracii/{DOK,PRIMERY,ZAHOD}.md` совпадений НЕ найдено (0). Похоже, `00-KONVEJER.md` сам устарел на этот счёт (арка 09 уже чиста) — не правил, файл вне границ этого захода; для сведения владельцу/следующему заходу по 00-KONVEJER.

**Время + токены.** Один проход, ~25 мин, ~95k токенов (оценка по объёму прочитанного+написанного).
