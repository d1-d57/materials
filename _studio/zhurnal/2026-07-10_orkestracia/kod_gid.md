# Канал исполнителя — ГИД + ЖИЗНЕННЫЙ ЦИКЛ АРКИ (заход ⑤, финальный) · один заход до конца

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.

**Модель: `opusplan`** — правила жизненного цикла проектируешь по ходу (Opus), шаблон/скрипт/коммит — механика (Sonnet).

**Текст запуска (владелец → Claude Code):**
```
Модель: opusplan — правила цикла рождаются по ходу, механика — Sonnet.
Claude Code, твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-10_orkestracia/kod_gid.md. Прочитай ТОЛЬКО его и то, что он называет; остальной проект не изучай. План/вопросы/отчёт пиши в этот же файл внизу (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ). Ничего сверх задачи не трогай.
```

КОНТЕКСТ. Проект — фабрика научпоп-презентаций `_studio/`. Арка «оркестрация фабрики» собрала обёртку (bootstrap папки лекции → трекер `sostoyanie.py` → единый `ALGORITM`+`GEJTY` → `KATALOG`). Ты — **ФИНАЛЬНЫЙ, 5-й заход**: достроить **гид + автоматический жизненный цикл арки**, чтобы владелец работал «на бегу» (только отвечал на вопросы и комментировал), а Cowork САМ вёл по стадиям, предлагал закрыть/открыть арку и делал хэндофф. Плюс — закоммитить всё (из песочницы Cowork коммит не прошёл; у тебя на машине пройдёт) и закрыть арку. Приёмка — по ОТЧЁТУ.

**Цель владельца (по смыслу, дословно важно):** «я не держу план в голове; Cowork, который читает доки, чётко понимает — какая следующая стадия и когда пора закрывать эту и переходить на следующую. Когда переполняется контекст — сам предлагает хэндофф и новый чат. Когда подходит конец арки (он изначально прописан) — сам предлагает закрыть и ТУТ ЖЕ открывает новую: создаёт все файлы, пишет хэндофф уже в новую арку. Идея: на входе создаём стандартную первую арку (как пример и первое место, куда садимся); при закрытии — перекопировать то же начальное состояние с новыми задачами/концепцией. Очень чёткое правило — тогда мы ничего не теряем, и это эффект памяти проекта, который удешевляет и улучшает работу всех сессий».

## 0. ПЕРВЫЙ ХОД — КОММИТ (точка отката + спасение консолидации Cowork)
Рабочая папка — корень репо `/Users/ivanyakovlev/Documents/GitHub/materials`. СРАЗУ:
```
rm -f .git/index.lock 2>/dev/null
git add _studio _generator/tools
git commit -m "Оркестрация: консолидация Cowork + tools (точка отката перед kod_gid)"
```
⚠ Это закоммитит незакоммиченную работу Cowork (сведение шва faza1↔layout, durable по домам Р21, `tools/{bootstrap_lekcia,sostoyanie}.py`, `konvejer/ZHURNAL.md`, дневник арки) — так и надо, это твоя точка отката. Проверь `git status` чистым по `_studio`/`_generator/tools` после. Затем прочитать ТОЛЬКО:
- `docs/kak-delat/ARKA.md` — канон работы в арке (расширяешь разделом автоцикла) + §9 шаблоны дневника
- `COWORK.md` — вход аналитика (роутер §4, дисциплина §5-6 — дополняешь указателем на автоцикл)
- `docs/pochemu-i-videnie/RESHENIYA.md` Р9, Р11, Р21 (паттерн арк-файла, ARC-дисциплина, обёртка-оркестрация)
- `konvejer/ALGORITM.md` §Модель исполнения + `konvejer/GEJTY.md` шапка (как трекер даёт «⏺ ты здесь» — гид на это опирается)
- `docs/KARTA.md` §6 (регистрация новых файлов — правишь САМ: заход один, не параллельный)
- прайор-арт: `ls ../catalan/zhurnal/` + `../catalan/ARKA.md` (живая арк-дисциплина; в удалённом `kurs-fabrika` был `shablon/zhurnal/_TEMPLATE-arka/` — образец-шаблон, его больше нет, воссоздаёшь идею)
ПЛАН — в `## ПЛАН`.

## 1. ДИСЦИПЛИНА (Карпатов)
Предпосылки/развилки назвать вслух; правила пиши КОНКРЕТНО (триггер → что именно Cowork делает), не общими словами; критерий-который-может-провалиться; идемпотентный скрипт; сохранять по умолчанию. Без роя субагентов.

## 2. ГРАНИЦЫ
- НЕ трогать готовое (только читать/ссылаться): конвейер `01–10`, `GEJTY`, `PAPKA-LEKCII`, `ALGORITM`, `sostoyanie.py`, `bootstrap_lekcia.py`.
- Не переоткрывать суть обёртки (Р21) — ты ДОСТРАИВАЕШЬ гид/цикл ПОВЕРХ.
- Пишешь в домах процесс-памяти (`ARKA.md`, `COWORK.md`) + новый шаблон-арка + новый скрипт + закрытие арки (durable ⑤). `bootstrap_arka` — НЕ дублирует `bootstrap_lekcia` (тот — папка ЛЕКЦИИ; этот — папка АРКИ / процесс-память).

## 3. ЗАДАЧА — четыре выхода + закрытие + коммит

**(1) Раздел «Автоматический жизненный цикл + гид» в `docs/kak-delat/ARKA.md`.** Конкретные правила, которые Cowork ЧИТАЕТ на входе и ЭНФОРСИТ — чтобы владелец не держал план в голове:
- **ГИД по стадиям.** На входе в арку и после каждого хода Cowork сверяется с `PLAN` («⭐ СЕЙЧАС»); при работе над лекцией — с прогоном `../../_generator/tools/sostoyanie.py` («⏺ ты здесь»). Всегда НАЗЫВАЕТ владельцу: текущая стадия · закрыта ли (гейт) · СЛЕДУЮЩИЙ конкретный шаг + чем (какой заход/вопрос/сессия). Владелец не ищет «что дальше».
- **Триггер «конец сессии» (контекст переполняется).** Cowork САМ, не дожидаясь молчаливого авто-компакта, предлагает: зафиксировать `PLAN`/`SESSIYA` → выдать хэндофф (ARKA §6) → новый чат. (Усиливает `COWORK.md §5` «окно конечно и схлопывается молча».)
- **Триггер «конец арки» (цель `PLAN` достигнута / гейты арки закрыты).** Cowork САМ предлагает: (а) закрыть арку — durable по домам (ARKA §7); (б) ТЕМ ЖЕ ходом открыть следующую — `bootstrap_arka.py` (выход 3) копирует шаблон, Cowork вписывает концепцию/задачи новой арки и хэндофф УЖЕ В НОВУЮ арку; (в) выдаёт владельцу строку запуска новой сессии. Владелец только подтверждает и переходит.
- **Инвариант:** пока работа продолжается — арка НИКОГДА не остаётся без преемника (закрыть текущую и открыть следующую — ОДИН ход). «Что осталось только в голове/чате — потеряно» → эффект памяти проекта.
- Развилки (порог «контекст переполняется», как понять «конец арки» точно) — назови в `## ВОПРОСЫ`, дай рабочий дефолт, не блокируй.

**(2) Шаблон-арка `_studio/zhurnal/_TEMPLATE-arka/`.** Стандартный скелет, копируемый на КАЖДУЮ новую арку (и «первая арка-пример», куда садится новая сессия): `NAVIGATOR.md` · `PLAN.md` · `SESSIYA.md` по шаблонам `ARKA §9` с плейсхолдерами (`<имя>`/`<дата>`/`<концепция>`/`<границы>`) + короткий `TZ.md` (цель · аудитория/скоуп · deliverable · границы/anti-scope · ворота go/no-go · открытые вопросы — форма из `../docs/spravka/PROFILI-CIFERBLATY.md §TZ`).

**(3) Скрипт `_generator/tools/bootstrap_arka.py`** (stdlib): `python3 bootstrap_arka.py <дата_тема> ["<концепция>"]` → создаёт `_studio/zhurnal/<дата_тема>/` копией `_TEMPLATE-arka/` с подставленными именем/датой/концепцией. Идемпотентен (папка существует → предупредить, exit≠0). Операционализация правила «перекопировать начальное состояние».

**(4) Роутер в `COWORK.md`.** В §4 (роутер) + §5-6 — короткие строки-указатели на новый раздел `ARKA §автоцикл` (триггеры конец-сессии / конец-арки → что делать), чтобы будущая Cowork-сессия видела это на входе.

**Затем — ЗАКРЫТЬ арку оркестрации** (⑤ — финал): `RESHENIYA` Р22 (автоцикл арки + шаблон + `bootstrap_arka`); `../sostoyanie/OTKRYTYE-ZADACHI.md` (арка ЗАКРЫТА ПОЛНОСТЬЮ; ⭐ следующее — реальный прогон #14); строку в `../sostoyanie/SVODKI.md`; зарегистрировать новые файлы в `KARTA §6`.

**Финальный ход — КОММИТ ⑤:**
```
git add _studio _generator/tools
git commit -m "Оркестрация ⑤: гид + автоцикл арки (шаблон + bootstrap_arka) + закрытие (Р22)"
```

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- `ARKA.md` несёт раздел автоцикла с 3 ЯВНЫМИ триггерами (гид-по-стадиям / конец-сессии / конец-арки), у каждого — что Cowork ДЕЛАЕТ (конкретика, не общие слова); инвариант «арка без преемника не остаётся» назван.
- `_studio/zhurnal/_TEMPLATE-arka/` существует: `NAVIGATOR.md`+`PLAN.md`+`SESSIYA.md`+`TZ.md`, все с плейсхолдерами; 0 битых ссылок.
- `python3 _generator/tools/bootstrap_arka.py 2099-01-01_proba "тест"` создаёт арку из шаблона с подставленными полями; повтор → предупреждение+exit≠0; `py_compile` чисто; только stdlib. Тестовую арку удалить после.
- `COWORK.md` роутер ссылается на `ARKA §автоцикл`.
- `RESHENIYA` Р22 + `OTKRYTYE-ZADACHI` (арка закрыта) + `SVODKI` + `KARTA §6` обновлены; 0 битых внутренних ссылок.
- **`git log --oneline -3` показывает 2 новых коммита** (точка отката + ⑤); `git status` чист по `_studio`/`_generator/tools`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
что сделал+зачем / как проверил (вкл. прогон `bootstrap_arka` + `git log -3` + `git status`) / что НЕ трогал / вопросы владельцу (развилки цикла — сюда) / **время прогона + токены**.

## ПЛАН — (заполняет исполнитель)
## ВОПРОСЫ — (заполняет исполнитель)
## ОТЧЁТ — (заполняет исполнитель)
