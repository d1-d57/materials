# УРОКИ ФАБРИКЕ — арка «2026-08-13_skill-mat-teksta»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---

### 1. Стартовое сообщение владельцу напечатано в stdout один раз и потеряно вместе с обрезанным выводом — заход сдан без него
ЦЕНА: собран заход `kod_yadro-blokov.md`, и владелец не получил блока запуска вовсе — поймал сам, репликой «ты регулярно это забываешь». Механизм потери назван точно и он не про внимательность: аналитик гонит `bootstrap_zahod.py` из песочницы и обрезает вывод (`| tail -14`), потому что скрипт печатает много; блок стоит ВЫШЕ хвоста и исчезает. Перечитать его неоткуда — **в самом файле-заходе его нет**, на диске он не остаётся нигде. Дальше файл правился руками ещё девять ходов, и ни один из них не мог напомнить о пропаже. Правило при этом в каноне есть и написано жёстко (`disciplina-zahod §Два канала запуска`: «Заход считается сданным владельцу только вместе с этим блоком») — то есть это не дыра канона, а **правило без носителя**: нарушение не краснеет ничем, ровно случай `KONSTITUCIYA §11`. Отдельная стоимость: восстановить блок по памяти запрещено тем же правилом, поэтому после пропажи законного выхода нет вовсе — только перечитывать исходник генератора.
ВЕРДИКТ: вписан → doma/zahody/POKRYTIE.md — закон: правило фабрики без НОСИТЕЛЯ и без машины, которая его краснеет, не существует, как бы жёстко оно ни написано; «нарушение ничем не краснеет» значит «правила нет». Консолидация частично уже случилась до вердикта: предложенная починка стоит в сборщике — файлы-заходы теперь рождаются с секцией «## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ» (закрывающий заход сам читал её в своём файле), то есть блок переживает обрезанный вывод. Осталось доставить закон в дом; дома вне зоны закрывающего захода — доставка отдельным ходом.
ДОМ: zahody
ПРЕДЛАГАЕМАЯ ПОЧИНКА (владельцу на просуживание, не решение): `bootstrap_zahod.py` кладёт блок НЕ ТОЛЬКО в stdout, но и в сам файл-заход отдельной секцией `## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ`; `check_zahod.py` получает клаузу «секция есть и непуста». Тогда блок переживает обрезанный вывод, перечитывается в любой момент и впервые получает то, что может покраснеть. Приём не новый — тот же, которым в этом же скилле закрыли молчаливый дефолт канала (`--kanal` сделан обязательным флагом после того, как дефолт дважды выбрал канал за владельца, урок 45).

### 2. `bootstrap_zahod.py` вшил в заход строку «невлитых веток 0» — их четыре, и в каждой лежит работа
ЦЕНА: нашёл исполнитель захода `yadro-blokov`, доставлено приёмкой 13.08 — та же команда `git branch --no-merged` даёт **4** — `zahod/porozhdenie-kartochki`, `zahod/tipologia-slajdov`, `zahod/zakony-v-gejt`, `zahod/zamer-blokov-l2`, и это не пустые хвосты, а по коммиту реальной работы (3 · 2 · 17 · 3 файла). Проверено в трёх местах — в рабочей папке, в основной и относительно `arka/mat-kostyak` — везде 4. Исполнителю `yadro-blokov` это стоило шести команд перепроверки и не более, потому что его зоны ветки не касались. Дорого встанет следующему: **обе заявки, лежащие в очереди прямо сейчас, просят править `gejt_kartochki.py` и `tipy_slajdov.py` — а свежие версии обоих лежат ровно в этих невлитых ветках** (`zakony-v-gejt` несёт +298 строк в `gejt_kartochki.py`). Исполнитель, поверивший строке, правил бы версию на 298 строк беднее и не заметил бы: гейты молчат, конфликт всплывает при слиянии, то есть после того, как работа сделана дважды. 🔴 Класс — «ПОВТОРИТСЯ»: строка вшивается сборщиком в КАЖДЫЙ следующий заход автоматически ⇒ по правилу RUKOVODSTVO это заход ДО следующего прогона, а не пункт очереди.
ВЕРДИКТ: вписан → doma/zahody/POKRYTIE.md — закон: число, вшитое сборщиком в файл-заход, есть СНИМОК момента сборки, а не факт на момент прогона; вшитое число законно только в паре с командой перепроверки и меткой «ПРОВЕРЬ ПЕРВЫМ ХОДОМ», доверие ему без живого прогона — ошибка исполнителя по построению сборщика. Починка формы уже видна: этот самый файл-закрытия несёт метку «🔴 снимок при сборке …, ПРОВЕРЬ ПЕРВЫМ ХОДОМ» у обоих вшитых чисел. Класс подтвердился тем же днём: критерий закрытия этой арки строился на состоянии папки ДО коммита `d8e359c` (20.08, снос шести пробных заходов) и к прогону устарел — снова вшитый снимок. Доставка закона в дом — отдельный ход.
ДОМ: zahody

### 3. Критерий готовности вида `grep -c … → 0` краснеет на комментарии о самом себе
ЦЕНА: нашёл исполнитель `yadro-blokov` — клауза 1 грепала `_BOLD_STMT\s*=`; исполнитель написал в коде комментарий, объясняющий, почему НЕ надо заводить локальный алиас, и привёл образец запрещённой строки — комментарий тут же провалил клаузу. Один круг «прогнал → красное → нашёл, что красное на собственном тексте → переформулировал → прогнал снова». Мелочь на одном заходе, но класс повторяемый: греп не отличает объявление от упоминания и тем более от предостережения, а красное по виду неотличимо от настоящей находки. Чинится калечением текста (нельзя процитировать то, что запрещаешь), а не кода. 🔴 Та же форма критерия стоит в `PLAN.md §Ф1` и воспроизводится из захода в заход ⇒ для гейтов, которые останутся жить, судить надо объявления (якорь на начало строки / AST), а не подстроку в файле.
ВЕРДИКТ: вписан → doma/zahody/POKRYTIE.md — закон: критерий вида «подстрока не встречается» (`grep … | wc -l → 0`) не отличает объявление от упоминания и от предостережения; живой гейт судит ОБЪЯВЛЕНИЕ — якорь начала строки, а лучше AST, — иначе он краснеет на документации о самом себе, а чинится это калечением текста (нельзя процитать то, что запрещаешь). Вторая половина урока («гейты, которые останутся жить») при доставке едет копией в дом `priyomka` — его гейты переживают заход. Доставка — отдельный ход.
ДОМ: zahody

### 4. Класс задачи сузился молча: «термин» в задании прочиталось как «существительное», и половина класса выпала
ЦЕНА: заход Ф2 (Opus, восемь текстов) искал «определение невосстановимо». **Все семь находок класса — существительные и конструкции**; ни одного прилагательного, ни одного глагола. Проверено пробой на живом тексте: свойство «финитно аппроксимируемая» (`obzory/sofichnost-priblizhenie-grupp-konechnymi/src/obzor.md:102`) работает с первой строки раздела, употребляется семь раз, несёт два утверждения — определения не имеет нигде, в инвентарь не попало. Цена — полный прогон Opus, покрывший класс примерно наполовину, и узнал об этом владелец только по готовому отчёту. 🔴 **Вина не исполнителя:** задание не сказало, он не додумал, и это правильное поведение. Сбой в ПОСТАНОВКЕ, а не в исполнении, и потому невидим до конца прогона — заход зелёный по всем гейтам, потому что гейты судят форму, а не полноту предмета. Владелец назвал лечение: интервью перед сборкой захода. Доставлено в дверь `disciplina/zhurnal/VHOD-2026-08-13_disciplina-zahodov.md` находкой Н6.
ВЕРДИКТ: вписан → doma/zahody/POKRYTIE.md — закон: границу класса предмета задание называет ЯВНО; всё, что исполнитель обязан додумать об объёме класса, он НЕ додумает — и это правильное поведение, вина в постановке; прогон при этом остаётся зелёным по всем гейтам, потому что гейты судят форму задания, а не полноту накрытия предмета. Лечение — интервью перед сборкой, где объём класса фиксируется финализированным пунктом; механизм уже частично стоит: сборщик требует `--intervyu` и секцию «ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ» (этот файл её несёт). Доставка закона в дом — отдельный ход.
ДОМ: zahody

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*
