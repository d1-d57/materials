# Канал исполнителя — yadro-blokov (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — механика НЕ расписана: границу «что уходит в общее ядро, а что остаётся движку» проводит исполнитель по живому коду, и она главный предмет захода.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-13_skill-mat-teksta» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. В репозитории живут ДВА генератора, собирающие «ленту», и ТРИ дома одного и того же
словаря блоков: `build_doc.py` (1791 стр., лента для чтения, свой словарь — кириллица, 8 родов,
разметка `**Определение N (имя).**`), `sborka/lenta.py` (205 стр., лента лекции в три вкладки,
берёт словарь из `bloki.py`) и сам `sborka/bloki.py` (147 стр., латиница, 7 родов, разметка
`### [rod] мысль`). `build_doc.py` НЕ импортирует `bloki.py` — связи между ними ноль.
Прошлый этап: замер владельца 13.08 — расхождение подтверждено грепом, зафиксировано в `PLAN.md`
арки, §«Несущее ограничение».
ЦЕЛЬ: свести словарь и разбор блока в ОДНО ядро, на котором сидят оба движка, НЕ трогая список
родов и НЕ сливая рендеры.
Приёмка — по ОТЧЁТУ, без построчной сверки. Если стоп до цели: получишь замер границы ядра и,
возможно, сам модуль — но НЕ переведённый на него `build_doc.py`; тогда так и напиши, зона
остаётся рабочей.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. СПИСОК РОДОВ НЕ МЕНЯЕТСЯ. Меняется только число его домов: 3 -> 1. Слайдовая система блоков сейчас перестраивается владельцем, брать из неё список запрещено до его сигнала.
2. В общее ядро идёт словарь родов, синтаксис разметки, парсер и модель блока. РЕНДЕР НЕ СЛИВАЕТСЯ — у документа своя тема и свободная высота, у дека фикс-канвас 1440x810; это разные артефакты и у каждого свой движок.
3. Кириллические имена (Определение, Теорема) сохраняются В ВИДЕ как отображение на печать, а не как второй словарь. Ни один собранный obzory/*/src не должен измениться внешне.
4. Требование 'каждый абзац принадлежит блоку' на этом заходе НЕ внедряется в движок как ошибка сборки — только считается и печатается отчётом. Внедрение — после контракта Ф4.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ — ОТДЕЛЬНАЯ РАБОЧАЯ ПАПКА (worktree), не основная.** 🔴 Первым ходом:
  ```
  cd ~/Documents/GitHub/materials
  python3 _generator/tools/git_zona.py worktree add yadro-blokov
  cd ../materials-wt/yadro-blokov      # точный путь печатает команда выше — бери ЕЁ вывод, не этот
  git branch --show-current            # обязано быть zahod/yadro-blokov
  ```
  🔴 **`git checkout` в основной папке НЕ делать никогда:** на 13.08 живых рабочих папок 13, рядом идут чужие заходы, и checkout МОЛЧА перепишет дерево под всеми (цена 27→28.07: файл откатился ночью, поймал владелец вручную; следа в git не остаётся). Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `_generator/sborka/bloki.py` `_generator/build_doc.py` `_generator/sborka/lenta.py` `_generator/tools/` `_studio/zhurnal/2026-08-13_skill-mat-teksta/` `_studio/zhurnal/2026-08-13_skill-mat-teksta/kod_yadro-blokov.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

🔴 **ЗАБРАТЬ ОБЯЗАТЕЛЬНО — правки аналитика, которые он сам закоммитить не может** (он в песочнице, в `.git` не пишет):
- `_studio/zhurnal/2026-08-13_skill-mat-teksta/` целиком — арка заведена 13.08: `TZ.md`, `PLAN.md`, `NAVIGATOR.md`, `SESSIYA.md`, `UROKI-FABRIKE.md` и этот файл-заход;
- 🔴 **`_studio/docs/KARTA.md` — ОБЯЗАТЕЛЬНО В ТОТ ЖЕ КОММИТ.** `bootstrap_arka.py` и `bootstrap_zahod.py` вписали в её §6 шесть строк регистрации. Ворота 5 читают §6 **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, а все шесть документов уедут сиротами и строки умрут при первом `checkout`.

Точный список снимать командой `git_zona.py check`, а не по перечню отсюда.

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

🔴 **Гасить НЕЧЕГО, и это решение, а не пропуск.** Своя ветка `zahod/yadro-blokov` и своя рабочая папка `yadro-blokov` закрываются приёмкой, не тобой.
🔴 **НЕПРИКОСНОВЕННО: чужие рабочие папки.** На 13.08 их 13 и рядом идут чужие заходы — папка, погашенная под чужим исполнителем, исчезает под ним молча. Ни `worktree remove`, ни `zakryt-vetku` по чужому имени не звать ни при каких условиях.

- Ветка проверяется ПОСЛЕ захода в свою рабочую папку (см. КОНТРАКТ ЗОНЫ): `git branch --show-current` → `zahod/yadro-blokov`. Не она — СТОП, `git checkout` НЕ делай.
- Точка отката: `git add `_generator/sborka/bloki.py` `_generator/build_doc.py` `_generator/sborka/lenta.py` `_generator/tools/` `_studio/zhurnal/2026-08-13_skill-mat-teksta/` `_studio/zhurnal/2026-08-13_skill-mat-teksta/kod_yadro-blokov.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти якоря, в этом порядке. Проект не изучай, других файлов не открывай:
  1. `_generator/sborka/bloki.py` (147 стр.) — целиком;
  2. `_generator/build_doc.py` — ТОЛЬКО окрестности `_STMT_NUM` / `_BOLD_STMT` / `_STMT_CLS` / `_PROOF_HEAD` / `_typed_block` (≈ строки 170–345) и точку вызова `_typed_block` в `render_stream` (≈ 566–580). Все 1791 строку читать НЕ надо;
  3. `_generator/sborka/lenta.py` (205 стр.) — целиком;
  4. `_generator/DVIZHKI.md` — раздел «источник → вид» (контракт: завёл новое преобразование — той же рукой строку в таблицу);
  5. `_studio/zhurnal/2026-08-13_skill-mat-teksta/PLAN.md` — §«Несущее ограничение» и §Ф1.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Свести словарь и разбор блока в ОДНО ядро, на котором сидят оба движка.

**Ш1. Замер границы — ДО всякой правки.** Выписать в отчёт таблицей: что про блок знает
`bloki.py`, что знает `build_doc.py`, и по каждой позиции — совпадает / расходится / есть только
у одного. Позиции: список родов · имена родов · синтаксис разметки в источнике · модель блока
(какие поля) · что считается границей блока · нумерация · статус подробности. Это вход для Ш2 и
он же — материал, по которому владелец будет судить границу.

**Ш2. Провести границу ядра и назвать её вслух в `## ПЛАН` ДО работы.** Ориентир владельца,
финализирован (§ЧТО ФИНАЛИЗИРОВАНО п.2): в ядро идут словарь родов, синтаксис разметки, парсер и
модель блока; рендер НЕ сливается. Считаешь границу кривой — скажи в `## ПЛАН`, не молча сдвигай.

**Ш3. Ядро — один модуль.** `bloki.py` становится единственным домом списка родов, разметки и
парсера. Он уже почти им является: у него есть `TIPY_BLOKOV`, `BLOCK_RE`, `Block`,
`check_composition`. Достроить недостающее из `build_doc.py` (кириллические имена как ОТОБРАЖЕНИЕ
на печать, нумерация утверждений `_STMT_NUM`, признак кат-доказательства).

**Ш4. Перевести `build_doc.py` на ядро.** Он импортирует `bloki`, свои `_STMT_CLS`/`_BOLD_STMT` как
ИСТОЧНИК ПРАВДЫ теряет. Рендер и CSS-классы (`stmt defn`/`thm`/`task`/`rem`) остаются у него —
это отображение рода на вид, и оно его собственное.

**Ш5. Счётчик бесхозных абзацев** — `_generator/tools/schet_blokov.py`: на вход папка-лента, на
выход печать «блоков N, абзацев вне блоков M, по родам …». 🔴 **Только печать, НЕ гейт и НЕ ошибка
сборки** (§ЧТО ФИНАЛИЗИРОВАНО п.4). Прогнать по всем `obzory/*/src` и вывод целиком — в отчёт: это
вход фазы Ф2 арки.

**Ш6. Регрессия вида.** Пересобрать все `obzory/*/src` и сверить с тем, что было ДО правки.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — четыре клаузы, все счётные:**

1. **Домов списка ровно один:**
   `grep -rn "TIPY_BLOKOV\s*=\|_STMT_CLS\s*=\|_BOLD_STMT\s*=" _generator/ | grep -v "sborka/bloki.py" | wc -l` → **0**.
2. **Оба движка сидят на ядре:** `grep -c "import bloki\|from bloki" _generator/build_doc.py` → **≥1**, и то же уже верно для `sborka/lenta.py`.
3. **Вид НЕ изменился ни в одном живом тексте.** До правки снять эталонные копии всех
   `obzory/*/src/view.html`; после — пересобрать и сравнить побайтово. Расхождений — **0**.
   🔴 Расхождение есть и оно, по-твоему, законное (например, изменился только порядок атрибутов) —
   это НЕ повод его засчитать: назови в отчёте поимённо и оставь красным, судит владелец.
4. **Счётчик отработал:** `schet_blokov.py` прогнан по КАЖДОЙ папке `obzory/*/src`, число
   проверенных папок названо и равно числу папок на диске. «Проверено 0» при зелёном коде — красный.

5. **ЖИВОЙ ПРОГОН на реальном объекте репозитория, не на фикстуре.** Взять живую ленту
   `obzory/monady-i-yoneda/src` и живую лекцию `teorkat-vvedenie/L2` (первая идёт через
   `build_doc.py`, вторая через `sborka/lenta.py` — по одному объекту на каждый движок, иначе
   общее ядро проверено только с одной стороны) и прогнать полный цикл:
   ```
   python3 -m dvizhki doc obzory/monady-i-yoneda/src
   python3 -m dvizhki vid obzory/monady-i-yoneda/src
   python3 _generator/sborka/lenta.py teorkat-vvedenie/L2 -o /tmp/lenta-L2.html
   python3 _generator/tools/schet_blokov.py obzory/monady-i-yoneda/src
   ```
   Все четыре команды — `rc=0`, вывод каждой целиком в отчёт. 🔴 Код возврата смотреть ПЕРВЫМ
   (`echo $?`), до содержательного вывода: «отработала» и «упала, а я читаю прошлое состояние»
   выглядят одинаково.

🔴 **Список родов после захода обязан быть ТОТ ЖЕ.** Проверка: `TIPY_BLOKOV` содержит ровно те же
семь имён, что и в HEAD до работы. Изменил — критерий провален, даже если «стало лучше».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
🔴 **НУЖЕН — операция лоссовая.** Ты выносишь знание из движка на 1791 строку, и потеря здесь
тихая: движок останется рабочим, а частный случай, который знал только `build_doc.py`, исчезнет.
Класс уже оплачен в этом репозитории — «скилл выглядит целым, а трети правил в нём нет».

Свежий субагент, ДРУГОЙ метод, чем твой. Ему НЕ давать твой план и твои выводы. Задача:
взять `git show HEAD:_generator/build_doc.py` (версия ДО правки), выписать из окрестностей
`_BOLD_STMT`/`_STMT_CLS`/`_PROOF_HEAD`/`_typed_block` **каждое** поведение, включая частные случаи
из комментариев, и проверить по живому коду ПОСЛЕ правки, что каждое на месте. Вердикт по каждому
пункту отдельно, не общий.

Особо проверить три места, где комментарии в коде прямо называют оплаченную цену и потому
регрессия здесь дороже всего: (а) зачин доказательства ТОЛЬКО курсивом — жирный откачен тремя
замерами; (б) втягивание следующего блока внутрь `<details>` доказательства; (в) `SHOW_NUM` —
свойство ФАЙЛА, а не сборки, ставится в `load_streams` перед рендером каждого потока.

**Отрицательный вердикт несёт ОХВАТ:** не «потерь не найдено», а «потерь не найдено, проверено
X поведений из Y выписанных».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/sborka/bloki.py _generator/build_doc.py _generator/sborka/lenta.py _generator/tools/ _studio/zhurnal/2026-08-13_skill-mat-teksta/ _studio/zhurnal/2026-08-13_skill-mat-teksta/kod_yadro-blokov.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/sborka/bloki.py _generator/build_doc.py _generator/sborka/lenta.py _generator/tools/ _studio/zhurnal/2026-08-13_skill-mat-teksta/ _studio/zhurnal/2026-08-13_skill-mat-teksta/kod_yadro-blokov.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/sborka/bloki.py` `_generator/build_doc.py` `_generator/sborka/lenta.py` `_generator/tools/` `_studio/zhurnal/2026-08-13_skill-mat-teksta/` `_studio/zhurnal/2026-08-13_skill-mat-teksta/kod_yadro-blokov.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/sborka/bloki.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/build_doc.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/sborka/lenta.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-13_skill-mat-teksta/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/2026-08-13_skill-mat-teksta/kod_yadro-blokov.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Строка «вливать нечего, невлитых веток 0» в собранном заходе оказалась неверной — их четыре, и в каждой лежит невлитая работа

Заход (§0.1, слот 1) утверждает: *«вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0»*. Та же команда 13.08 даёт **4**: `zahod/porozhdenie-kartochki`, `zahod/tipologia-slajdov`, `zahod/zakony-v-gejt`, `zahod/zamer-blokov-l2` — и не «пустые хвосты», а по одному коммиту реальной работы каждая (3 · 2 · 17 · 3 файла). Проверено и в рабочей папке, и в основной, и относительно `arka/mat-kostyak` (база пункта Г3) — везде 4.

ЦЕНА: у меня — шесть команд перепроверки, и это дёшево, потому что моей зоны эти ветки не касаются (проверено `show --name-only`: ни `bloki.py`, ни `build_doc.py`, ни `lenta.py`). Дорого это встанет следующему: **обе заявки, лежащие сейчас в очереди, просят править `gejt_kartochki.py` и `tipy_slajdov.py` — а свежие версии обоих файлов лежат ровно в этих невлитых ветках** (`zakony-v-gejt` несёт +298 строк в `gejt_kartochki.py`, `tipologia-slajdov` — сам `tipy_slajdov.py`). Исполнитель, поверивший строке «вливать нечего», взял бы заявку и правил бы версию файла на 298 строк беднее, ничего при этом не заметив: гейты бы молчали, а конфликт всплыл бы при слиянии — то есть после того, как работа сделана дважды.

### Критерий готовности, устроенный как `grep -c` по тексту, краснеет на комментарии о самом себе

Клауза 1 («домов списка ровно один») грепает `_BOLD_STMT\s*=`. Я написал в `build_doc.py` комментарий, объясняющий, почему НЕ надо заводить локальный алиас, и привёл в нём образец запрещённой строки — комментарий тут же провалил клаузу: греп не отличает объявление от упоминания, а тем более от предостережения.

ЦЕНА: один круг «прогнал критерий → красное → нашёл, что красное на собственном комментарии → переформулировал → прогнал снова». Мелочь на моём заходе, но класс общий и повторяемый: любой критерий вида «`grep … | wc -l` → 0» даёт красное, неотличимое по виду от настоящей находки, на документации о самом правиле — и чинится это калечением текста (нельзя процитировать то, что запрещаешь), а не кода. Для гейтов, которые останутся жить, форма должна судить объявления (AST/якорь на начало строки), а не подстроку в тексте файла.

## ПЛАН — (заполняет исполнитель)

### Ш1 · ЗАМЕР ГРАНИЦЫ — сделан ДО правки, по живому коду (это вход Ш2)

Дата данных: 2026-08-13, версия `HEAD` = `8f3397d` в рабочей папке `materials-wt/yadro-blokov`.

| позиция | `bloki.py` (147 стр.) | `build_doc.py` (1791 стр.) | вердикт |
|---|---|---|---|
| список родов | `TIPY_BLOKOV` — 7: `narrativ` `opredelenie` `primer` `utverzhdenie` `dokazatelstvo` `itog` `uprazhnenie` | внутри `_BOLD_STMT` (:178) — 8: Определение Теорема Лемма Предложение Утверждение Пример Задача Замечание | **расходятся и по числу, и по составу** |
| имена родов | латиница, машинные | кириллица, они же печатаются | расходятся |
| синтаксис разметки | `### [rod] мысль` (`BLOCK_RE`, :47), плюс разделы `## …` (`SECTION_RE`) | `**Род N (имя).**` (`_BOLD_STMT`) + курсивный зачин `*Доказательство…*` (`_PROOF_HEAD`, :202) | расходятся; у документа **два** синтаксиса вместо одного |
| модель блока | `Block(tip, mysl, telo)` — namedtuple (:49) | модели нет: `_typed_block` (:290) разбирает и печатает одним ходом, поля живут в локальных `kw/num/name/body` | **есть только у `bloki.py`** |
| граница блока | от `### ` до следующего `### ` внутри раздела (:94–96) | пустая строка: `re.split(r"\n\s*\n", body)` в `render_stream` (:429) | расходятся по устройству |
| нумерация | нет вовсе | `_STMT_NUM` (:177) — префикс линии + цифры + суффикс + штрих; печать под опт-ином `SHOW_NUM`, буквенный номер печатается всегда (:315) | **есть только у `build_doc.py`** |
| статус подробности | нет | `Статус: …` срезается из метки (:306); `_PROOF_HEAD` → свёрнутый `<details>`; втягивание следующего блока в кат (:568) | **есть только у `build_doc.py`** |
| «абзац вне блока» | считается: `orphan_matematika` / `orphan_tekst` (:81–85) | не считается никак — ветка 7 `render_stream` (:580) печатает `<p>` молча | есть только у `bloki.py` |
| связь | — | `import bloki` отсутствует (проверено грепом) | **ноль связей, подтверждено** |

🔴 **Главное, что показал замер, и чего в заходе нет.** Два списка — **не два дома одного словаря, а два разных словаря о разном**. `TIPY_BLOKOV` — риторические роли блока в карточке слайда (зачем этот кусок сказан: нарратив, итог, упражнение). Восемь кириллических имён в `_BOLD_STMT` — математические жанры врезки в потоке документа (что это за утверждение: теорема, лемма, замечание). Точных пар всего **три из восьми**: Определение↔`opredelenie`, Пример↔`primer`, Утверждение↔`utverzhdenie`. Без пары остаются: `narrativ`, `itog`, `dokazatelstvo` (в документе доказательство вообще не жанр врезки — оно живёт отдельным механизмом `_PROOF_HEAD`), `uprazhnenie` (Задача — близко, но не то же), и Теорема/Лемма/Предложение/Замечание, которых в карточке нет.

### Ш2 · ГРАНИЦА ЯДРА — провожу и называю вслух

Ориентир владельца (§ЧТО ФИНАЛИЗИРОВАНО п.2) принимаю: словарь + синтаксис + парсер + модель → ядро, рендер не сливается. Уточняю его по живому коду:

**`bloki.py` становится единственным домом ДВУХ словарей, а не одного.** Свести их в один список нельзя, не меняя `TIPY_BLOKOV`, — а это заходом прямо запрещено (и запрещено правильно: список слайдов сейчас перестраивается владельцем). Поэтому ядро объявляет:
- `TIPY_BLOKOV` — как есть, семь имён, **ни одного символа не трогаю**;
- `ZHANRY_VREZKI` — восемь кириллических жанров, физически переехавших из `_BOLD_STMT`;
- `STMT_NUM`, `BOLD_STMT_RE`, `PROOF_HEAD_RE` — синтаксис разметки документа;
- `Utverzhdenie(zhanr, nomer, imya, status, telo)` — модель блока документа, которой сегодня нет ни у кого, плюс разборщик `razobrat_zachin()` / `razobrat_zachin_vyvoda()`.

**Что НЕ идёт в ядро и остаётся у `build_doc.py`:** весь рендер, CSS-классы (`stmt defn/thm/task/rem`), якоря `id`, `SHOW_NUM` и решение «печатать ли номер», `_body_html`, HTML ката. Отображение жанра на вид — собственное дело движка.

🔴 **Оспариваю формулировку Ш3, до работы.** Ш3 просит внести кириллические имена «как ОТОБРАЖЕНИЕ на печать». Это ложная предпосылка: отображения нет, потому что нет биекции — восемь кириллических имён не являются печатной формой семи латинских (см. три пары из восьми выше). Написать `{"opredelenie": "Определение", …}` значило бы **выдумать** пять пар, которых в коде нет, и закрепить их как контракт. Делаю вместо этого честное: два соседних словаря в одном доме и печатная таблица соответствий, где пар ровно три, в отчёте. Настоящее отображение — предмет Ф4 (контракт блока), который заходом отложен до сигнала владельца; раньше него его не изобрести.

🔴 **Оспариваю клаузу 1 критерия готовности — она проходима переименованием.** Греп `_STMT_CLS\s*=` ловит имя, а не сущность: любой дубль списка под другим именем даст зелёное. Поэтому дополнительно завожу то, что покраснеет по существу (`KONSTITUCIYA §11`): в `build_doc.py` при импорте проверяется, что все ключи его таблицы вида ⊆ `bloki.ZHANRY_VREZKI`, и падает с внятным текстом иначе. Клаузу 1 исполняю дословно, эту проверку добавляю сверх неё.

### Ш3–Ш6 · порядок ходов

1. **Эталон вида — пересборкой на HEAD, а не с диска.** Лежащие `view.html` могли быть собраны прошлыми версиями; сравнивать с ними значило бы поймать чужой дрейф вместо своей регрессии. Ход: на нетронутом дереве прогнать `dvizhki doc` по всем 8 папкам, копии `view.html` отложить в `/tmp/etalon-do/`. Только после этого — первая правка.
2. `bloki.py` дополняется ядром (список, синтаксис, модель, парсер). Существующее в нём не трогаю.
3. `build_doc.py` переводится на ядро: `import bloki`, свои `_STMT_NUM`/`_BOLD_STMT`/`_STMT_CLS`/`_PROOF_HEAD` как источник правды теряет. Рендер остаётся дословно тем же.
4. `lenta.py` — уже на ядре (`import bloki`, :31); правок не требует, клауза 2 на ней проверяется, а не достигается.
5. `_generator/tools/schet_blokov.py` — счётчик, **только печать, rc=0 всегда**. Требования Г4 закрываю тем же ходом: `--help` без трейсбека, маркер охвата кривого входа, живая точка вызова (иначе `check_tool_contract` красный по кластеру F).
6. Регрессия вида: пересборка 8 папок, побайтовое сравнение с `/tmp/etalon-do/`. Расхождение — красным, без самосуда.
7. Верификатор (§3) — свежий субагент, другим методом, без моего плана и выводов.

**Риск, который называю заранее:** операция лоссовая, `build_doc.py` — 1791 строка, и частный случай, который знал только он, исчезает тихо. Поэтому правка идёт переносом дословных литералов и регэкспов, без «заодно почищу», а сверка — побайтовая, а не «на глаз».

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

### Пункты очереди

```
1. `_generator/DVIZHKI.md` §«СПИСОК ПРЕОБРАЗОВАНИЙ ИСТОЧНИК → ВИД»: колонка «где в коде»
   называет `_PROOF_HEAD` и `_STMT_CLS`, а этих имён в `build_doc.py` больше нет — заходом
   `yadro-blokov` они переехали в ядро (`bloki.PROOF_HEAD_RE`; `_STMT_CLS` разделён на
   словарь `bloki.ZHANRY_VREZKI` и таблицу вида `build_doc.VID_ZHANRA`). Сами три
   преобразования НЕ изменились — вид побайтово тот же, новой строки в таблицу заход не
   добавляет; устарели только адреса. Файл вне моей зоны, поэтому не тронут.
   ДОМ: _generator/DVIZHKI.md
   ДОСТАВЛЕНО: нет

2. Та же таблица, строка про `Статус:`: в колонке «где в коде» стоит `_STATUS_IN_HEAD`, но
   это имя живёт в `_generator/tools/check_view.py` (гейт), а не в движке — в самом движке
   срезание было безымянным `re.sub` и теперь лежит в `bloki.razobrat_zachin`. Расхождение
   существовало и до этого захода; называю, потому что читатель таблицы ищет имя в движке
   и не находит.
   ДОМ: _generator/DVIZHKI.md
   ДОСТАВЛЕНО: нет

3. Заявка `2026-08-12T0311-2-gejt-kartochki-py-narrativ-2026` (новый закон фазы 2 в
   `gejt_kartochki.py`) — помечена ЗАСТРЯЛА, остаётся открытой: файл вне зоны захода, и
   §ЧТО ФИНАЛИЗИРОВАНО п.1 прямо запрещает трогать слайдовую систему блоков до сигнала
   владельца. Заявка не является git-операцией и командами `git_zona.py` не исполняется.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. Заявка `2026-08-12T0359-tipy-slajdov-py-2-primer-3` (слоты `[primer]`/`[opredelenie]` в
   раскладках Т2/Т3) — помечена ЗАСТРЯЛА по той же причине.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. Настоящее отображение лента→слайд (`TIPY_BLOKOV` ↔ кириллические жанры) этим заходом НЕ
   заведено и завестись не могло: точных пар три из восьми, остальные пять пришлось бы
   выдумать. Это предмет Ф4. Вопрос владельцу, который придётся решить там: `dokazatelstvo`
   в карточке слайда — РОД блока, а в документе доказательство вообще не жанр врезки, у него
   отдельный механизм (курсивный зачин + свёрнутый кат). Пока эти два устройства не сведены
   решением, «по ленте строим слайды» упирается именно сюда, а не в число домов.
   ДОМ: _studio/zhurnal/2026-08-13_skill-mat-teksta/PLAN.md
   ДОСТАВЛЕНО: нет

6. 🔴 **Третий дом зачинов вывода — `build_doc._DRILL_PROOF_RE` (:599), и в нём ТРИ из
   четырёх: «Решение» отсутствует** (`Доказательство|Логика|Идея`). Нашёл верификатор, я
   его не видел. Заход им не двигал намеренно: подключение к ядру расширило бы множество
   распознаваемых зачинов, а вид требовалось сохранить. Сегодня вид и не изменился бы —
   в единственном живом drill-документе `teorkat-vvedenie/MAT-KOSTYAK.md` вхождений
   `*Решение` ровно 0 (там 63 `*Доказательство` и 8 `*Идея`). Дефект скрытый и ждёт своего
   текста: автор пишет `*Решение.*` — в обычном потоке получает свёрнутый кат, в
   drill-потоке не получает ничего, и никакой гейт этого не скажет (клауза 1 критерия
   грепает три имени, этого среди них нет). Решать владельцу: подключить к
   `bloki.ZACHINY_VYVODA` (тогда «Решение» появится в drill) или объявить разницу законной
   и записать её.
   ДОМ: _generator/build_doc.py
   ДОСТАВЛЕНО: нет

7. Два урока фабрике записаны в секцию `## УРОКИ ФАБРИКЕ` этого файла жёстким форматом
   (`### …` / `ЦЕНА: …`), потому что по нему краснеет `check_uroki.py`. Оба подлежат
   переносу в дом арки: (а) строка «невлитых веток 0» в собранном заходе неверна — их
   четыре, и заявки очереди просят править файлы, чьи свежие версии лежат именно в них;
   (б) критерий вида `grep -c` краснеет на комментарии о самом себе.
   ДОМ: _studio/zhurnal/2026-08-13_skill-mat-teksta/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет
```

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/private/tmp/lenta-L2.html` — открыть в браузере (лента лекции L2, 23 слайда, три вкладки; собрана движком `sborka/lenta.py` уже НА ЯДРЕ)
**АРТЕФАКТ-2:** `/Users/ivanyakovlev/Documents/GitHub/materials-wt/yadro-blokov/obzory/monady-i-yoneda/src/view.html` — открыть в браузере (документ-вид, собран `build_doc.py` уже НА ЯДРЕ). 🔴 Смотреть его надо с ясным ожиданием: он обязан выглядеть **точно как раньше, до символа**. Успех этого захода в том, что артефакт НЕ изменился, — сменился движок под ним, а не картинка.

### Что сделано и зачем

**Цель достигнута полностью: домов словаря и разбора блока стало один вместо трёх, оба движка сидят на нём, вид не сдвинулся ни в одном из восьми живых текстов.**

1. **`_generator/sborka/bloki.py` — ядро.** Достроен словарём, синтаксисом, моделью и парсером документ-вида: `ZHANRY_VREZKI` (восемь кириллических жанров), `STMT_NUM`, `BOLD_STMT_RE`, `ZACHINY_VYVODA`, `PROOF_HEAD_RE`, модели `Utverzhdenie`/`Vyvod` и разборщики `razobrat_zachin()` / `razobrat_zachin_vyvoda()`. Правка — **чистое добавление: 0 удалённых строк** (`git diff HEAD | grep -c "^-[^-]"` → 0), поэтому карточная половина ядра, на которой сидит лента лекции, не могла измениться по устройству, а не по моему обещанию.
2. **`_generator/build_doc.py` — переведён на ядро.** `import bloki`; собственные `_STMT_NUM`/`_BOLD_STMT`/`_PROOF_HEAD` как источник правды потеряны, `_STMT_CLS` разделён по границе: список жанров ушёл в ядро, отображение жанра на вид осталось здесь под именем `VID_ZHANRA` (CSS-класс и якорный ключ — решение о картинке, а не о словаре). Рендер, `SHOW_NUM`, якоря, печать номера — не тронуты.
3. **`_generator/sborka/lenta.py` — не правился ни строкой.** Он сидел на `bloki` и до захода; клауза 2 на нём **проверена, а не достигнута**, и это честнее, чем править файл ради галочки.
4. **`_generator/tools/schet_blokov.py`** — счётчик блоков и бесхозных абзацев. Печать, не гейт: находки никогда не меняют код возврата (0), ненулевой код значит ровно «инструмент не отработал». Сегменты делятся той же границей, что у движка, и разбираются ЯДРОМ, а не собственной копией разметки.
5. **`_generator/tools/fixtures/schet_blokov/PROGNAT.sh`** — спутник-фикстура (Г4 без неё красный по кластеру «кривой вход»). Главная её проверка не про разбор флагов: она сторожит, что находки НЕ роняют код возврата — день, когда это сломается, станет днём, когда сборка встанет у всего репозитория, а выглядеть будет как поломка гейта.

### Граница ядра — как проведена и почему именно так

Ориентир владельца (словарь + синтаксис + парсер + модель → ядро; рендер не сливается) исполнен дословно. Уточнение, которое пришлось внести по живому коду: **в ядре теперь ДВА соседних словаря, а не один сведённый.** `TIPY_BLOKOV` — риторические роли блока карточки (зачем кусок сказан), кириллические жанры — математические жанры врезки документа (что это за утверждение). Точных пар **три из восьми**: Определение↔`opredelenie`, Пример↔`primer`, Утверждение↔`utverzhdenie`; без пары остаются `narrativ`, `itog`, `uprazhnenie`, `dokazatelstvo` (в документе доказательство вообще не жанр врезки — у него отдельный механизм) и Теорема/Лемма/Предложение/Замечание. Свести их в один список значило бы **выдумать пять соответствий, которых в коде нет**, и закрепить их как контракт — при том что список родов заходом менять запрещено. Настоящее отображение лента→слайд — предмет Ф4; до сигнала владельца его не изобрести. Замер целиком — таблицей в `## ПЛАН` выше, там же оспорена формулировка Ш3 («кириллические имена как отображение на печать» — отображения нет, потому что нет биекции).

### Критерий готовности — по клаузам

| клауза | команда | результат |
|---|---|---|
| 1. домов списка ровно один | `grep -rn "TIPY_BLOKOV\s*=\|_STMT_CLS\s*=\|_BOLD_STMT\s*=" _generator/ \| grep -v "sborka/bloki.py" \| wc -l` | **0** ✅ |
| 2. оба движка на ядре | `grep -c "import bloki\|from bloki"` | `build_doc.py` → **1** ✅ · `sborka/lenta.py` → **1** ✅ |
| 3. вид не изменился | пересборка 8 папок + `diff -rq` с эталоном ДО | **расхождений 0 из 8 файлов** ✅ (независимо подтверждено `git status --porcelain obzory/` → 0 строк) |
| 4. счётчик отработал | `schet_blokov.py obzory/*/src` | **проверено 8 папок из 8 на диске** ✅ (`ls -d obzory/*/src \| wc -l` → 8) |
| 5. живой прогон | четыре команды | все **rc=0** ✅, вывод целиком ниже |
| список родов ТОТ ЖЕ | `git diff HEAD -- bloki.py` | `TIPY_BLOKOV` в дифф не попал вовсе: 0 удалённых строк ✅ |

🔴 **Про эталон клаузы 3 — существенная оговорка о методе.** Лежавшие на диске `view.html` я эталоном НЕ взял: они могли быть собраны прошлыми версиями движка, и сверка с ними поймала бы чужой дрейф вместо моей регрессии. Эталон снят **пересборкой на нетронутом дереве** до первой правки. Побочно это дало проверку самого репозитория: свежая пересборка на HEAD совпала с лежащим в git **побайтово во всех восьми папках** — то есть собранные виды в репозитории не протухли.

### Живой прогон (критерий 5) — вывод целиком, код возврата первым

```
$ python3 -m dvizhki doc obzory/monady-i-yoneda/src            → rc=0
── ЛИНТЕР (doc-движок) ──
  ✓ структура и мат-долги в норме (0 мягк. предупрежд.)
  потоков(вкладок): 1 · открытых ⚑ всего: 0 · obzor.md→«Монады и Йонеда»
── СБОРКА ──
  ✓ obzory/monady-i-yoneda/src/view.html (49 КБ, вкладок: 1)

$ python3 -m dvizhki vid obzory/monady-i-yoneda/src            → rc=0
── ГЕЙТ ВЫХОДА (check_view) ──
  ✓ ЗЕЛЁНЫЙ: текст не съеден, опасных формул нет

$ python3 _generator/sborka/lenta.py teorkat-vvedenie/L2 -o /tmp/lenta-L2.html   → rc=0
собрана лента: 23 слайдов → /tmp/lenta-L2.html

$ python3 _generator/tools/schet_blokov.py obzory/monady-i-yoneda/src            → rc=0
obzory/monady-i-yoneda/src
  файлов .md: 1
  блоков: 1 · абзацев вне блоков: 15
  по родам: Определение 1
  прочее (не проза и не блок): заголовки 8 · поля/цитаты 5 · сырой HTML 1
```

### Счётчик по всем восьми папкам (Ш5) — вход фазы Ф2, вывод целиком

```
obzory/dika-v-vysshey-matematike/src
  файлов .md: 2
  блоков: 35 · абзацев вне блоков: 52
  по родам: Определение 8 · Теорема 8 · Пример 6 · Логика 6 · Утверждение 4 · Замечание 1 · Предложение 1 · Задача 1
  прочее (не проза и не блок): заголовки 23 · списки 6 · поля/цитаты 3 · формулы 3 · иллюстрации 1 · сырой HTML 4 · курсивные пометки 25
obzory/goodstein-ordinaly-gidra/src
  файлов .md: 1
  блоков: 48 · абзацев вне блоков: 37
  по родам: Определение 11 · Теорема 10 · Доказательство 10 · Замечание 8 · Утверждение 5 · Пример 4
  прочее (не проза и не блок): заголовки 21 · списки 1 · поля/цитаты 15 · сырой HTML 1 · курсивные пометки 34
obzory/karri-hovard-lambek/src
  файлов .md: 1
  блоков: 0 · абзацев вне блоков: 18
  по родам: (нет)
  прочее (не проза и не блок): заголовки 8 · списки 2 · поля/цитаты 3 · сырой HTML 1
obzory/kombinatorika-okruzhnosti/src
  файлов .md: 1
  блоков: 41 · абзацев вне блоков: 22
  по родам: Определение 12 · Теорема 11 · Доказательство 11 · Пример 4 · Утверждение 2 · Замечание 1
  прочее (не проза и не блок): заголовки 16 · списки 1 · таблицы 2 · поля/цитаты 9 · курсивные пометки 18
obzory/monady-i-yoneda/src
  файлов .md: 1
  блоков: 1 · абзацев вне блоков: 15
  по родам: Определение 1
  прочее (не проза и не блок): заголовки 8 · поля/цитаты 5 · сырой HTML 1
obzory/puti-ogranichennoy-vysoty/src
  файлов .md: 1
  блоков: 38 · абзацев вне блоков: 26
  по родам: Доказательство 12 · Теорема 8 · Определение 6 · Утверждение 5 · Замечание 4 · Пример 3
  прочее (не проза и не блок): заголовки 11 · списки 1 · поля/цитаты 7 · сырой HTML 2 · курсивные пометки 15
obzory/sluchajnye-bluzhdaniya-na-gruppah/src
  файлов .md: 1
  блоков: 39 · абзацев вне блоков: 61
  по родам: Логика 8 · Утверждение 7 · Доказательство 7 · Определение 4 · Замечание 4 · Пример 4 · Теорема 3 · Лемма 2
  прочее (не проза и не блок): заголовки 16 · списки 2 · поля/цитаты 9 · формулы 1 · сырой HTML 5
obzory/sofichnost-priblizhenie-grupp-konechnymi/src
  файлов .md: 1
  блоков: 39 · абзацев вне блоков: 47
  по родам: Определение 9 · Идея 9 · Утверждение 6 · Теорема 6 · Замечание 5 · Логика 3 · Пример 1
  прочее (не проза и не блок): заголовки 25 · списки 1 · поля/цитаты 23 · формулы 4 · сырой HTML 1 · курсивные пометки 1

ИТОГО по 8 папкам: блоков 241 · абзацев вне блоков 278
  по родам: Определение 51 · Теорема 46 · Доказательство 40 · Утверждение 29 · Замечание 23 · Пример 22 · Логика 17 · Идея 9 · Лемма 2 · Предложение 1 · Задача 1
  прочее: заголовки 128 · списки 14 · таблицы 2 · поля/цитаты 74 · формулы 8 · иллюстрации 1 · сырой HTML 15 · курсивные пометки 93
```

🔴 **Что это число значит и чего НЕ значит.** «Абзацев вне блоков 278 при 241 блоке» — это НЕ вердикт «корпус плохой». Требование «каждый абзац принадлежит блоку» пришло из мира карточек слайда, где всё тело размечено `### [rod]`; документ-вид так никогда не писался, и связная проза между врезками там — законный жанр, а не дефект. Читать эту таблицу как инвентарь Ф2 можно, как приговор — нельзя. Две позиции в ней стоит посмотреть глазами отдельно: `karri-hovard-lambek` — **0 блоков при 18 абзацах** (текст вообще не размечен врезками), и `monady-i-yoneda` — 1 блок на 15 абзацев. Остальные шесть лежат в диапазоне 35–48 блоков.

### Что НЕ трогал

- **Список родов** — ни символа: `TIPY_BLOKOV` в дифф не попал.
- **`sborka/lenta.py`** — правок не требовал, и я их не выдумывал.
- **Рендер и CSS обоих движков**, `SHOW_NUM`, якоря, `_body_html`, HTML ката — остались у движка, в ядро не поехали.
- **Слайдовая система блоков** (`gejt_kartochki.py`, `tipy_slajdov.py`, `PRAVILA-FAZY-2.md`) — под прямым запретом §ЧТО ФИНАЛИЗИРОВАНО п.1; из-за него же обе заявки очереди помечены ЗАСТРЯЛА, а не исполнены.
- **`_generator/DVIZHKI.md`** — вне зоны. В его таблице «источник → вид» устарели адреса имён; новой строки заход не требует (преобразований я не заводил, вид тот же). Пункты очереди 1–2.
- **Чужие рабочие папки и ветки** — не гасил ни одной.

### Гит-контур (§0.1)

- **Слот 0, очередь.** Заявок было 2, обе вне зоны и под прямым запретом п.1 → `zayavka-zakryt --zastryala` с причиной, обе остаются открытыми, приёмка на них покраснеет законно. Id — в пунктах очереди 3–4.
- **Слот 1, влить.** Строка захода «невлитых веток 0» **не подтвердилась**: их 4, в каждой невлитый коммит с работой. Проверил, задевают ли они мою зону — `git show --name-only` по каждой: **ни `bloki.py`, ни `build_doc.py`, ни `lenta.py` не тронуты ни одной**, поэтому не вливал ничего и работал на своей базе. Урок фабрике записан — цена там не моя, а следующего исполнителя.
- **Слот 2, забрать.** Арка `2026-08-13_skill-mat-teksta` (6 файлов) забрана из основной папки. 🔴 `_studio/docs/KARTA.md` **скопирована НЕ была, и это осознанный отказ**: на диске основной папки в ней 10 новых строк, из которых 4 — чужие, на документы арки `2026-08-10`, которых в моей ветке нет ни в git, ни на диске. Копирование файла целиком унесло бы чужую работу в мой коммит и завело бы 4 битые ссылки. Вместо этого — законная дверь: шесть вызовов `register_doc.py` в рабочей папке. Дифф `KARTA.md` в моём коммите — ровно 6 строк, все мои (проверено `git diff HEAD -- _studio/docs/KARTA.md`).
- **Слот 3, закрыть.** Гасить нечего: своя ветка и своя папка закрываются приёмкой. Чужих 13 — не тронуты.

### Гигиена Г1–Г6

- **Г1.** `git_zona.py check --zone` по всем шести путям зоны — ниже, вместе с коммитом.
- **Г2.** Неприменимо, и это проверено, а не предположено: все пути зоны лежат внутри репозитория `materials`, за его пределы зона не выходила.
- **Г3.** `git branch --no-merged arka/mat-kostyak` → **4 на входе, 6 на выходе — прибавилось две, обе законны и названы поимённо:**
  - `zahod/yadro-blokov` (+3 коммита) — **моя собственная**, ею и делается заход; гасится приёмкой, не мной;
  - `zahod/mikrotipografika-melochi` — **чужая, появилась за время прогона**: рядом идёт чужой заход (её имя совпадает с одним из документов, зарегистрированных в KARTA.md основной папки аркой `2026-08-10`). Не трогал ни её, ни её рабочую папку.

  Само число 4 на входе расходится со строкой захода «невлитых веток 0» — см. урок фабрике.
- **Г4.** Новых `.py` один — `_generator/tools/schet_blokov.py`. `check_tool_contract.py` на нём и на фикстуре → **✅ проверено 2 из 2, rc=0**. Первый прогон был КРАСНЫЙ («нет спутник-фикстуры»); чинил, а не обходил — завёл фикстуру.
- **Г5.** Новых `.md` не заводил ни одного (шесть файлов арки — правки аналитика, я их только забрал в git). `register_doc.py` звал для всех шести; строки лежат на диске: `grep -c` по каждому имени в `_studio/docs/KARTA.md` → **1, 1, 1, 1, 1, 1**.
- **Г6.** `git show --stat` — ниже, вместе с коммитом.

### 🔴 НЕОБРАТИМОЕ

1. **Удалена мёртвая переменная `_KW_THM`** (`build_doc.py`, было :169; кортеж «Теорема, Лемма, Предложение, Утверждение») · где: `_generator/build_doc.py` · чем восстанавливается: `git show 8f3397d:_generator/build_doc.py`. Почему удалена, а не оставлена: это четвёртый дом части словаря, объявленный и **нигде не использованный** (проверено грепом по всему `_generator/` — единственное вхождение было само объявление). Клауза 1 его не грепает, то есть он пережил бы заход незамеченным.
2. **Перезаписаны 8 файлов `obzory/*/src/view.html`** пересборкой (эталон + финальная сверка) · где: рабочая папка `materials-wt/yadro-blokov` · чем восстанавливается: содержимое **побайтово идентично прежнему**, `git status --porcelain obzory/` → пусто; фактической потери нет, называю как факт перезаписи.
3. **Создан `/tmp/lenta-L2.html`** (артефакт критерия 5) · чем восстанавливается: пересборкой той же командой.
4. **Две заявки очереди помечены ЗАСТРЯЛА** · где: `_studio/zhurnal/_INFRA-git/zayavki/` (namеренно вне git) · чем восстанавливается: сами заявки НЕ удалены и остаются открытыми — помечен только результат попытки.

Основную папку репозитория не трогал вовсе, кроме этого файла-захода (план/вопросы/отчёт — как предписано) и двух файлов заявок. `git checkout` не делал ни разу, ни в основной папке, ни в рабочей.

### ПОВТОРЯЕМОСТЬ находок

- 🔴 **ПОВТОРИТСЯ на следующем же заходе — значит это заход ДО следующего прогона, а не пункт очереди:** строка «вливать нечего, невлитых веток N» вшивается в файл-заход сборщиком (`bootstrap_zahod.py`), то есть неверное число поедет в каждый следующий заход автоматически. Уже сейчас цена конкретна: две открытые заявки просят править файлы, чьи свежие версии лежат в невлитых ветках. Чинить надо до следующего прогона сборщика.
- 🔴 **ПОВТОРИТСЯ:** критерий готовности вида `grep … | wc -l → 0` краснеет на комментарии о самом себе. Форма критерия воспроизводится из захода в заход (та же форма стоит в `PLAN.md` §Ф1), значит и ложное красное воспроизведётся.
- **НЕ повторится** (законно уходит пунктами очереди): устаревшие адреса имён в таблице `DVIZHKI.md` — разовое следствие именно этого переезда; обе застрявшие заявки — разовые, снимаются сигналом владельца о готовности слайдовой системы.

### Открытое «возвращаться»

- **Ф4 без сигнала владельца не сдвинется.** Заход снял число домов (3→1), но не свёл сами словари — и это не остаток работы, а названный предел: пять из восьми соответствий пришлось бы выдумать. «По ленте строим слайды» упирается именно в это, а не в число домов.
- **Требование «каждый абзац принадлежит блоку» в движок не внедрено** (§ЧТО ФИНАЛИЗИРОВАНО п.4) — счётчик только печатает. Внедрять после контракта Ф4; фикстура уже сторожит, что до тех пор находки не роняют сборку.

**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** на канале `app` неприменимо, снимать неоткуда — лог прогона существует только на канале `terminal`, где его кладёт `tee`.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА

Свежий субагент, другим методом, без моего плана и выводов; вход — `git show HEAD:_generator/build_doc.py`, то есть версия ДО правки.

**Вердикт с охватом: «потерь поведения не найдено, проверено 35 поведений из 35 выписанных»** (плюс одно удаление имени без поведения — мёртвая `_KW_THM`, п.1 его списка; он независимо подтвердил 0 использований).

Три места, за которые заход велел браться особо, проверены **исполнением**, а не чтением:
- **(а) зачин доказательства только курсивом.** На живом `teorkat-vvedenie/DOKAZATELSTVA-L1`: 13 жирных `**Доказательство` в источнике → 13 плоских абзацев, катов в выходе **0**. Чужой жанр `**Решение: не нести.**` тоже остался абзацем. Откат тремя замерами не потерян.
- **(б) втягивание следующего блока внутрь `<details>`.** Кат стоит ВНУТРИ того же `<div class="stmt thm">`, следующий абзац — снаружи; втягивается ровно ОДИН блок (на двух подряд второй кат оказывается снаружи, как и было); работает и когда вывод — последний блок потока. В корпусе 450 катов.
- **(в) `SHOW_NUM` — свойство ФАЙЛА.** Собрал папку из двух файлов, `a.md` с `nomera: da` и `b.md` без: в ОДНОЙ сборке `a` дал «Теорема 7.» с номером, `b` — «Теорема 8.» без. Буквенный номер печатается всегда: `**Утверждение A1.**` при `SHOW_NUM=False` даёт метку с номером и якорь `s0-t-A1`; в корпусе 15 буквенных якорей (`A1`, `A1′`, `C1a`…) — все побайтово те же.

Чем проверял сверх чтения (его собственные числа): пересборка **37 doc-источников репозитория** старым и новым движком → **37/37 побайтово идентичны**; дифф-фаззинг **25 788 уникальных строк** на `*`/`**` из всех `*.md` репозитория через `_typed_block`/`proof_details` обеих версий при `SHOW_NUM` False и True → **0 расхождений**; 58 краевых кейсов × 4 комбинации; побайтовое сравнение самих регэкспов (`pattern` и `flags`) и словарей (`_STMT_CLS == VID_ZHANRA` → True).

🔴 **Его находка, которой у меня не было, — и я её принял:** третий дом зачинов вывода `_DRILL_PROOF_RE`. Мой докстринг в ядре объявлял модуль «единственным домом парсера для обоих движков» — заявление шире фактического. **Исправил формулировку**, сузив охват до «основного потока» и назвав исключение поимённо с ценой; сам `_DRILL_PROOF_RE` не тронул (подключение изменило бы поведение, а вид требовалось сохранить). Отдельный пункт очереди 6. Вторая его находка — устаревшие адреса в `DVIZHKI.md` — совпала с тем, что я уже вынес пунктами 1–2.

**КОММИТ:** `79f180c` — «yadro-blokov: словарь, разметка, парсер и модель блока сведены в одно ядро sborka/bloki.py; build_doc.py переведён на него (вид не изменился ни в одном из 8 obzory, сверено побайтово); счётчик бесхозных абзацев schet_blokov.py + спутник-фикстура» · **и** `4b5d513` — «arka 2026-08-13_skill-mat-teksta: заход yadro-blokov (план, вопросы, отчёт, уроки фабрике) + регистрация шести документов арки в KARTA.md §6» · `git_zona.py check --zone` по **всем шести путям зоны** → **✅ ✅ ✅ ✅ ✅ ✅**

Коммита два, а не один, и это вынужденно: `git_zona.py commit -m` принимает РОВНО ОДНУ зону (`--zone` — один префикс пути), а зона захода лежит в двух корнях, `_generator/` и `_studio/`. Разделение оказалось к лучшему — см. долг ниже. `git show --stat` по обоим: чужих путей нет (4 файла и 7 файлов, все мои).

🔴 **ДОЛГ — ОБХОД `pre-commit` НА КОММИТЕ АРКИ `4b5d513`.** Ворота 3 краснели: `_studio/zhurnal/2026-08-13_skill-mat-teksta/TZ.md:19` ссылается на карточку `reserch-cikl`, которой нет. Долг ЧУЖОЙ и структурный: строка описывает скилл СЛЕДУЮЩЕЙ арки, и в самой же строке таблицы стоит «❌ нет, следующая арка» — то есть карточки не должно существовать по замыслу, а ворота читают любое такое имя как ссылку. Файл писал аналитик; чинить значило бы править смысл чужого ТЗ. Обход сделан инструментом с причиной (она уехала в `_INFRA-git/INCIDENTY.md`), не голым `--no-verify`.
**Коммит кода `79f180c` прошёл БЕЗ обхода, под полноценным хуком** — обход применён точечно к чужому долгу и ни одной строки моего кода не покрывает. Это и было смыслом разделения на два коммита.

**Не закоммичено намеренно:** `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — автолог, куда инструмент записал причину обхода. Он вне зоны захода, поэтому не тронут; запись на диске есть.
**Не выкладывал на origin:** `--push` не звал — ветка `zahod/yadro-blokov` не отслеживает origin, работа лежит на этой машине. Вывоз — решение владельца, не моё.
