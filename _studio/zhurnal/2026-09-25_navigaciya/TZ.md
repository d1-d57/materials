# ТЗ арки «2026-09-25_navigaciya»

> Короткое ТЗ арки — форма из `../../docs/spravka/PROFILI-CIFERBLATY.md §TZ`. Заполняется на Ф0 (до go); фиксирует, ЧТО и ЗАЧЕМ, чтобы заход исполнителю опирался на него.

- **Цель** — что арка производит и зачем сейчас: Навигация по репозиториям: карточка-анкета в корне каждого репозитория, функция-таблица со статистикой, система папок и префиксов, метки GitHub = теги Finder, три состояния (в работе / в архиве / на паузе). Первое звено цепочки «станция и память».
- **Аудитория / скоуп** — <кому/на что; для лекции-курса — уровень>
- **Deliverable** — Одна команда выводит таблицу всех репозиториев со статистикой; у каждого репозитория в корне карточка из генератора с одним из трёх состояний; имена и папки — по согласованной с владельцем системе; метки GitHub = теги Finder, расхождение краснеет само; в анкете заложены поля под будущие связи (люди, темы, похожие проекты)
- **Границы / anti-scope** — <что НЕ входит>
- **Ворота go/no-go** — GO: работа на несколько сессий, трогает структуру и имена всех репозиториев, меняет правила для каждой будущей сессии; открытой арки о навигации нет (арка 2026-09-24_arhitektura-repozitoriev её только спланировала)
- **Открытые вопросы** — <развилки владельцу; рабочий дефолт, не блок>

<!-- Курс-масштабная арка? добавь явно (PROFILI §TZ): профиль + циферблаты · формат (число лекций/дней) · throughline (одна ось) -->

## ИНТЕРВЬЮ ПРИ ЗАВЕДЕНИИ

> Секцию вшил `bootstrap_arka.py` из флагов `--intervyu da` и `--finalizirovano`; краснеет на пустоте `check_arka.py` (клауза **А1**). Ответы стоят ДОСЛОВНО — пересказ запрещён ровно ради того, ради чего интервью и заводили.

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-09-25) — флаг `--intervyu da` при заведении. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Зачем сейчас: владелец переходит на GitHub в облаке как основной инструмент — GitHub привести в порядок целиком; за 4,5 мес. работы с Claude накопилось много проектов, опыт систематизировать. Настоящая боль — «станция и память» (связанная память проектов, людей, тем) — это цепочка дальше; Навигация — первое звено
2. Карточка: файл в корне каждого репозитория, создаётся генератором по единой дисциплине как анкета (не проза); машинные поля (дата создания и т. п.) вписывает генератор, смысловые — исполнитель-Claude; вместе с генератором — функция, выдающая живую таблицу и статистику по всем карточкам
3. Таблица — не разовый снимок, а вызов функции; ценность — в правильных названиях (префиксы ↔ папки на компьютере), глубоком тегировании и трёх состояниях: в работе / доделано и в архиве / начато, брошено, может быть вернёмся. Состояние — меткой, не в имени
4. Метки GitHub (topics) и теги Finder совпадают один к одному; синхронизация желательна, но не критична; расхождение — системная ошибка, проверять всегда при действиях с репозиториями
5. Система папок/префиксов НЕ решена: владелец решает только на живом содержании, вместе с аналитиком; наметка — в одной папке репозитории, близкие по инструментам (сайт: код, вёрстка, дизайн — против математического ресёрча: книги, программы курсов). Порядок: сначала простые репозитории и сводная таблица, materials и disciplina последними
6. Вне звена (цепочка дальше): люди и коллаборации, классификатор типов сессий и интервью-маршрутизатор в начале сессии, регистрация сессий/арок/заходов, ретроспектива 4 лет, сайт, игротека, отчёт за год; разведение трёх «спецмат 179» — решить после сводной таблицы. Исключение: что можно подготовить под будущее на этом шаге (поля анкеты под связи) — делать сейчас

## ИНТЕРВЬЮ ПРИ ЗАКРЫТИИ

> Заполняется при консолидации (`disciplina-arka §7`), тем же ходом, что и баннер `> 🔒 АРКА ЗАКРЫТА` в `PLAN.md`. Краснеет `check_arka.py` (клауза **А2**) — но только на закрытой арке: на открытой пустота законна, арку ещё не закрывают.

ИНТЕРВЬЮ ПРОВЕДЕНО: <да | нет> (<дата>)

<сюда — что владелец сказал при закрытии, нумерованным списком, минимум две строки>
