# VOPROSY — svedenie

Form: `<object> — <what it is> — PROPOSED: <default> — because <reason>`

1. `disciplina/--help/` (6 files, 2026-09-21) — test-fixture output of a tool that was called with `--help` as its target path (`--help/_generator/tools/gejt_proby.py`, `--help/skills/proba/RESHENIYA.md`, lessons named "proba"); the hook refuses it and it is not work — PROPOSED: move it to `~/.Trash/` (or `git_zona.py purge --yes`, which exists for names with a leading `-`), not commit — because committing a test artifact into the trunk is not conservation. Left in place; it is the only remainder in `disciplina` (`status --porcelain` → 1 line).
2. 39 local branches fully in their trunk (e.g. materials `zahod/chistka-hvostov`, disciplina `zahod/zhivost-porog-i-posledovatelno`, `zahod/sudya-ne-perepisyvaet-cheloveka`, spetsmat-bot `zahod/B2-v10-ochered`) — `zakryt-vetku` refuses: a tombstone `mogila/<same name>` already exists on an older tip — PROPOSED: `git branch -d <b>` (safe delete; the tip is an ancestor of the pushed trunk, zero loss), or teach `zakryt-vetku` a numbered tombstone — because the pass may delete local branches only through `zakryt-vetku`, so I left them. List: rows `skip … VOPROSY #2` in `actions.tsv`.
3. disciplina `zahod/git-bez-zamkov` — one of the 22; merged before this pass (beea8ce5f), local branch already gone; only `origin/zahod/git-bez-zamkov` and `origin/park/…/wt-git-bez-zamkov` remain — PROPOSED: delete them on GitHub in the next pass — because nothing is lost.
4. arhiv-london-avgust-2026 `pages` — the Pages branch, unrelated history to `main` — PROPOSED: keep until the repository is split — because it is a different thing, not an edit.
5. disciplina `zahod/byudzhety-i-rashod` (C14) — 3 commits, 2 files; the last commit says the position was stopped by the head on the owner's word (F2) midway; code conflict in `bootstrap_mandate.py` 3531 commits later — PROPOSED: keep until the repository is split (or close without merging) — because it is unfinished by the owner's decision, not a finished small edit.
6. disciplina `zahod/roli-i-dver-brifa` (C23) — 4 commits, 7 files; stopped by the head on the owner's word (F2); 3 code conflicts — PROPOSED: keep until the repository is split (or close without merging) — because unfinished by decision.
7. disciplina `zahod/dobor-s-mesta` (M-2) — 5 commits, 5 files; its own diary records a merge conflict against an already-merged duplicate `dobor-s-mesta` + M-1; 5 code conflicts in `dirizher.py` and its fixtures — PROPOSED: keep until the repository is split — because resolving it means choosing between two versions of the dirizher resume logic, which is not a conflict understood from the diff.
8. disciplina `zahod/git-zona-zona-shire-kontrakta` — 2 commits by a free model that replaced its own brief `kod_git-zona-zona-shire-kontrakta.md` with a 5-line stub (+2 −658) — PROPOSED: close without merging — because merging would erase the brief from the trunk.
9. disciplina `zahod/snimok-nazyvaet-avtora` (A-4) — writer in the snapshot header + an «inherited» mark at position start — PROPOSED: close without merging — because the header is already in main in its accepted form (9d91fb216, re-issued as `-2`) and main rejected the mark on purpose (trap SV4: «nothing to fix in the snapshot itself»).
10. disciplina `zahod/generator-rychaga` — merged up to 33ebd512e (merge 374e825b0, rychag fixture 28/28 green); the tip commit cf5e91692 «аварийное сохранение» adds only 3 rescue backups (`PROGNAT.sh.rej`, `*.iz-osnovnogo-20260920-191037`) — PROPOSED: close without merging the tip — because the backups are copies of files now in trunk.
11. matemdigest-map `rabota` and `zahod/stil-noch` — unrelated history to `main`; `rabota` is the checked-out working branch (125 commits ahead) — PROPOSED: keep until the repository is split; decide which of `main`/`rabota` is the trunk — because they are separate lines, not edits.
12. sayt-sistemy-konstantinova `tagging` — checked out, 219 commits and 1124 files ahead of `main`, clean merge-tree — PROPOSED: keep until the repository is split, or make `tagging` the trunk — because it is a long divergent line, not a small edit.
13. spetsmat-bot `zahod/B1-v14-vid` — unfinished autosave of the V14 school-row view — PROPOSED: close without merging — because main rolled this view back on purpose (0be9ed8) and redid it via `B2-v14-raskladka` (accepted d5e9ffa).
14. `git_zona.py vlit-v-osnovnuyu` in `materials` takes `main` (the published site) as the trunk, not `arka/mat-kostyak`; it refused only because the zone check failed — PROPOSED: make the tool read the trunk of `materials` as `arka/mat-kostyak` — because a call without `--zone` could publish to the site. I merged in `materials` with `git_zona.py merge` into the current branch instead.
