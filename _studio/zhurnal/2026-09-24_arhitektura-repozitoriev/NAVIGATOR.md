# НАВИГАТОР арки «2026-09-24_arhitektura-repozitoriev»

> Читается ПЕРВЫМ на входе в чат внутри арки (ARKA §3). Стабилен, правится редко.

## Концепция — что строим, какую функцию (1 абзац)
Развести опубликованный Hugo-сайт materials/main и рабочий контент курсов по репозиториям, закрыть просроченную arka/mat-kostyak, дать materials/GitHub понятную навигируемую структуру

## Границы — что НЕ входит (anti-scope)
- Не сливать `arka/mat-kostyak` в `main` целиком и не пытаться слить его вообще в
  `main` каким-либо способом — `main` structurally это Hugo-сайт (`content/`,
  `layouts/`, `static/`, `hugo.yaml`), а не контент-ствол; слияние утащило бы в
  витрину весь монорепо. Проверено 24.09: merge с `--zone kurs-puti-i-volny/`
  отказал сам, назвав 115 затронутых зон.
- Не трогать чужие незакоммиченные зоны без их же заявок: `ucheniki/` (материалы
  занятий с учеником Мишей) и `diskmat-57/2026-08-18_logika-goda-7` (кружок-логика)
  — у обеих есть открытые заявки в `_studio/zhurnal/_INFRA-git/zayavki/`
  (`2026-09-20T1840-2-20-09-ucheniki-15-11.md`,
  `2026-09-23T1445-diskmat-57-kruzhok-logika-hvost.md`), их коммитит тот, кто
  писал заявку, по её описи, не эта арка вслепую.
- Не выносить `kurs-puti-i-volny/` (или любой другой курс) в отдельный
  репозиторий БЕЗ отдельного разговора с владельцем — 24.09 владелец явно
  отклонил самостоятельный вынос, попросив сначала системно разобраться.
- Не вводить git submodules — предварительная позиция 24.09: усложняют ровно
  там, где нужно упростить; предпочтение — плоские репозитории-соседи под
  `~/Documents/GitHub/`, как уже сделано с `moskva`.
- Не редактировать содержимое курсов/карточек под новый канон попутно —
  архитектура репозиториев, не ревизия контента.

## Указатели в долгую — места проекта, которые трогаем (выжимка; файл→раздел→когда)
- `disciplina/KAK-USTROEN-GITHUB.md` → карта всей `~/Documents/GitHub/`, данные на
  30.08 — освежать целиком; уже не хватает `moskva/`, вероятно `spetsmat-bot/`,
  `spetsmat-179/`. Команды пересчёта — прямо в файле.
- `materials/README.md` §0/§2/§3 → карта внутри `materials`; §3 «Карта проектов»
  сверена 06.08, протухла молча (сам файл это предсказывает). Команда сверки —
  `ls -d */` из корня `materials`.
- `disciplina/skills/disciplina-git/GIT-disciplina.md` §4в «ПУБЛИКАЦИЯ САЙТА» →
  устройство `main`-как-сайта, `opublikovat`, почему слияние с `main` запрещено.
- `_studio/zhurnal/_INFRA-git/zayavki/*.md` → живые заявки на коммит от других
  сессий; ЧИТАТЬ перед тем, как трогать любую чужую незакоммиченную зону.
- `git_zona.py doctor` (из `materials`, `GIT_ZONA_REPO=.../materials`) → 185 путей
  вне git на 24.09, дедлок разбирали (снят), заявок открыто было 12.

## Внешние источники — ссылки
Нет — источники все внутренние (репозитории владельца), см. «Указатели в долгую».

## Скиллы арки — какие поднимать
`disciplina-git` (слияние с `--zone`, `worktree`, коммиты) · `disciplina-arka` (§7
закрытие `arka/mat-kostyak` как отдельной, более узкой процедуры внутри этой
арки) · `disciplina-istochnik-pravdy` (куда девать новые доки при освежении карт).

## Процедуры
вход → `../../docs/kak-delat/ARKA.md` §3 · план → §4 · хэндофф → §6 · закрытие → §7 · жизненный цикл + гид → §10 · заход исполнителю → `RUKOVODSTVO-zahodami.md`.
