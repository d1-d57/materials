# УРОКИ ФАБРИКЕ — арка «2026-09-24_arhitektura-repozitoriev»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

### 1. 2026-09-24 — «публичные персональные данные» поданы как находка первой очереди

Аналитик облачной сессии, приняв инвентаризацию, вынес владельцу как «🔴 срочное» то, что в публичном
`materials` лежат профили учеников, — поверх находок по существу (ветки, вес истории, код).
Владелец: это не критично, и это ПОВТОРЯЮЩИЙСЯ сбой — исполнители на сайте класса 179 раз за разом
«находят» фамилии, хотя им это объясняли.
ЦЕНА: один круг разговора владельца ушёл на опровержение; повторяемость — «раз за разом, каждую сессию» (слова владельца, счёта нет).
Рычаг: `SPISOK-DEL.md` п.34 — строка в `CLAUDE.md` каждого репозитория и в скиллах захода/приёмки. Это надежда, не гейт: машинного носителя нет.
ВЕРДИКТ: вписан → `SPISOK-DEL.md` п.34 и п.169 (очередь арки Д). В канон Дисциплины НЕ вписан: правка скиллов заморожена решением владельца 25.09 09:57 (`RESHENIYA.md`) — это отступление от трёх форм §7, названное вслух.
ЗАКРЫТО: содержание уже в доме руками аналитика — `SPISOK-DEL.md` п.34, п.169(е); дословная доставка в таблицу не нужна

### 2. 2026-09-25 — переписывая заход, аналитик вернул уже исправленную ошибку

Строка `xargs -0 -I{} sh -c 'test $(stat …)'` в §2.3 захода `kod_uborka-materials` была заменена Python-ом (пути
с пробелами и кириллицей), но при полной переписи задания текст собирался из черновика, сделанного ДО замены.
Исполнитель на Mac упёрся в папку с именем `--help/`, заметил поломку и перепроверил Python-ом.
ЦЕНА: одна проверка размеров прошла вслепую на части парковок; повторная проверка исполнителя — «ни один файл >5 МБ
не попал». Рычаг: переписывать заход из ТЕКУЩЕГО файла, а не из черновика в scratchpad. Сейчас это надежда, машинного носителя нет.
ВЕРДИКТ: вписан → `SPISOK-DEL.md` п.169 (очередь арки Д; канон Дисциплины заморожен 25.09 09:57).
ЗАКРЫТО: содержание уже в доме — `SPISOK-DEL.md` п.169(е)


### 3. 2026-09-25 — аналитик называл владельцу числа со слуха, не сверив с переписью
Книги — «18 ГБ» (перепись `perepis/folders.tsv`: 1707 МБ, владелец: 1,58 ГБ); «бо́льшая часть папки GitHub — disciplina» (перепись `perepis/repos.tsv`: крупнейший — materials).
ЦЕНА: два исправления владельцем (п.162, п.163); на ложных 18 ГБ строился вопрос «книги на GitHub?» с ресёрчем лимитов LFS — ход ушёл на задачу не того размера.
ВЕРДИКТ: отбракован — правило уже есть и достаточно (`CLAUDE.md` корня, правило 4 «предпосылку проверяй по живым файлам»; `KONSTITUCIYA §10` «пиши команду, а не число»). Нарушено исполнением, а не пробелом в каноне.
ЗАКРЫТО: отбракован, дом не нужен — правило есть (см. ВЕРДИКТ)

### 4. 2026-09-25 — аналитик пропустил `--sudit-sborku` у принятого захода и собирал следующий
ЦЕНА: генератор отказал в сборке нового захода, пока старый стоял «ЖДЁТ»; один лишний ход с клапаном `--krasnaya-sborka-prichina`.
ВЕРДИКТ: отбракован — гейт сработал сам (генератор отказал); правило и рычаг уже есть.
ЗАКРЫТО: отбракован, дом не нужен — гейт сработал сам

### 5. 2026-09-25 — гейт инцидентов при закрытии сессии судит не ту репу
`zakryt_sessiyu.py` запускает `check_incidenty.py` с `cwd` = корень disciplina, а не `GIT_ZONA_REPO`: из облака materials гейт показывает 22 неразобранных класса старых инцидентов disciplina.
ЦЕНА: сессия закрыта с красным гейтом осознанно (вердикты не выдумывались); у гейта пропадает сигнал — красный по чужому долгу неотличим от красного по своему.
ВЕРДИКТ: вписан → `SPISOK-DEL.md` п.167 (дефект инструмента, арка Д).
ЗАКРЫТО: содержание уже в доме — `SPISOK-DEL.md` п.167

---

## Пункты очереди заходов, доставленные `dostavit_urok.py`

> Дословные находки из секций `## ВОПРОСЫ` файлов-заходов — голос исполнителя, который об это споткнулся, не редактора. Разбирает их человек: находка описывает, что мешало, но задачей сама не становится.
> Строка `ДОСТАВЛЕНО: <заход>#<N>` внизу каждой записи — та же метка, что стоит в источнике; по ней Г7 `priyomka.py` проверяет, что доставка не объявлена ложно.

#### perepis-diska#1

1. The §0.1 self-check `git --no-optional-locks branch --no-merged claude/bold-faraday-wq09ql | grep -c 'zahod/'` can never be red in a worktree made by the starter message. The base exists there only as `origin/claude/bold-faraday-wq09ql`, so `git` fails with `fatal: malformed object name`, and `grep -c` still prints `0`. The generator should emit the `origin/…` form, or check that the ref exists. The same false green hits Г3 and step 4 of the WARNING block.
   ДОМ: _studio/zhurnal/2026-09-24_arhitektura-repozitoriev/UROKI-FABRIKE.md
ДОСТАВЛЕНО: perepis-diska#1 · 2026-09-25

#### perepis-diska#2

2. `schet_nezakrytogo.py _studio/zhurnal/2026-09-24_arhitektura-repozitoriev` (the brief's "check first" command) refuses with rc=1 ("under the area not a single file"). The arc folder exists only on the brief's branch, not where the tool reads. As a result, the header's 0/0/0/0 for this pass could not be re-verified.
   ДОМ: _studio/zhurnal/2026-09-24_arhitektura-repozitoriev/UROKI-FABRIKE.md
ДОСТАВЛЕНО: perepis-diska#2 · 2026-09-25
