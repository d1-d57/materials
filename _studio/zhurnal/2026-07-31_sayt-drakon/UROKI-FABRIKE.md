# УРОКИ ФАБРИКЕ — арка «2026-07-31_sayt-drakon»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c '^### '      UROKI-FABRIKE.md   # уроков
grep -c '^ЦЕНА: '    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -c '^ВЕРДИКТ: ' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---

### 1. Стартовая команда генератора многострочная, а её отдавали владельцу текстом в чат
ЦЕНА: фильтр показа потока — python-скрипт в одинарных кавычках с настоящими переводами строк, команда занимает ~14 физических строк. Вставленная в чат обрезается по первому переводу: владелец копирует `… | python3 -u -c 'import sys,json`, кавычка не закрыта, оболочка молча ждёт — внешне неотличимо от зависшего прогона. Поймал владелец 31.07, сравнив этот прогон с пятнадцатью прошлыми, где он видел только финальный ответ. Починено тем же ходом: генератор теперь печатает предупреждение «отдавать ТОЛЬКО ФАЙЛОМ, проверка `sh -n`».
ВЕРДИКТ: <пусто до закрытия арки>

### 2. Заход обещал исполнителю вызвать скиллы, которых в Claude Code нет
ЦЕНА: `popsci-narrative`, `russian-editor`, `visual-design-review`, `site-cross-device-audit` названы в заходе как инструменты. В Claude Code они не подключены — исполнитель читал их как файлы с диска и применял руками. Заявлено им в отчётах ДВАЖДЫ (прогоны 1 и 2), то есть ошибка пережила одну итерацию. Прямой цены в переделке нет, но гейт ТЗ «прогнать через russian-editor» был невыполним по построению, и тексты вышли невычитанными — владелец забраковал их целиком («ужас, кошмар, всё переделывать»).
ВЕРДИКТ: <пусто до закрытия арки>

### 3. Аналитик записал в заход противоположное тому, что сказал владелец
ЦЕНА: владелец: «микшеры вынести направо». В заход ушло «все ползунки — в левую колонку». Исполнитель выполнил дословно и правильно. Один полный прогон органов управления сделан не туда, прогон 4 переносил обратно. Поймано только на просмотре собранного сайта владельцем.
ВЕРДИКТ: <пусто до закрытия арки>

### 4. Cowork правил и пересобирал артефакт, не имея возможности его увидеть
ЦЕНА: в песочнице нет браузера (playwright не ставится), поэтому пересборка сайта из Cowork прошла без вшивания шрифта — системные шрифты владельца песочнице не видны, `build_sayt.py` тихо ушёл на фолбэк. Регрессия внесена тем самым агентом, который весь день требовал от исполнителя «смотреть глазами». Стоила одной лишней пересборки у владельца; поймана самопроверкой сразу после сборки, до выдачи.
ВЕРДИКТ: <пусто до закрытия арки>

### 5. Cowork запустил генератор «на пробу» и породил документ-сироту с регистрацией
ЦЕНА: проверка правки в `bootstrap_zahod.py` сделана боевым запуском — родился `kod_proba.md` и строка в `KARTA.md §6`. Строку сняли, файл из песочницы удалить нельзя. Владелец потратил два тупиковых хода: `purge` его не берёт (снимает только неотслеживаемое), потребовался `git rm -f`. Файл дополнительно блокировал слияние ветки.
ВЕРДИКТ: <пусто до закрытия арки>

### 6. Два предложения упростить доказательство оказались ложными — и это поймала машина, а не автор
ЦЕНА: нулевая, и в этом суть. Гипотезы «уголок всегда лежит в клетке одного цвета шахматной доски» и «новая вершина посещается один раз» звучали убедительно и сократили бы доказательство на шаг. Обе проверены перебором ДО того, как попали владельцу: первая ложна уже при ранге 2, вторая — 815 контрпримеров на ранге 12. Без проверки в лекцию уехало бы неверное рассуждение.
ВЕРДИКТ: <пусто до закрытия арки>

### 7. Правка файла, в который параллельно пишет живой прогон
ЦЕНА: предотвращена, но чуть не случилась. Владелец попросил дописать правки в заход, пока прогон 4 писал туда же отчёт. `Edit` читает файл целиком и переписывает — чужая запись между чтением и записью теряется молча. Обошли, положив правки отдельным файлом `PRAVKI-5.md`. Та же причина сделала невозможной параллельную работу субагентов по сценам: все сцены живут в одном `scenes.js`.
ВЕРДИКТ: <пусто до закрытия арки>

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*
