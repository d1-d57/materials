# ZHURNAL — wave noch-puti-i-volny

> Journal of the wave, written ALONG THE WAY, round by round. Every record: a PRICE as a number
> and a line ABOUT THE HEAD ITSELF. Mandate: `mandate_noch-puti-i-volny.md` (same folder).

## Round 0 — 2026-09-19 00:50 MSK — tooling and baselines, before S1

- **Tooling (OSNASTKA), measured by command:** caffeinate pid 25005 (TAKE ✅) · `indeks.py --proverit` rc=0 (TAKE ✅) · `topsort_karty.py --help` rc=0 · `dirizher.py --help` rc=0 · calendar `слотов 33, тем 32` (TAKE ✅) · model registry dated 2026-09-17T16:22 (two days stale), 9 alive of 25 → above 5, not refreshed (debt 10: no probing «just in case»).
- 🔴 **Duplicate baseline differs from the mandate: `dubli.py --porog 0.45` prints `пар выше порога: 7`, the mandate says 6.** The mandate's TAKE probe is therefore red by one pair. Not a wave failure — the baseline for clause (b) is recorded here as the MEASURED seven, by name:
  1. 0.989 `kurs-puti-i-volny/OBRAZEC-summy-kvadratov.md` ↔ `kurs-puti-i-volny/obrazec/src/obrazec.md`
  2. 0.546 `kurs-puti-i-volny/zahody/ZAHOD-formy-yakobi.md` ↔ `kurs-puti-i-volny/zahody/ZAHOD-sverka-koncepcii.md`
  3. 0.516 `obzory/funkciya-putey-i-ee-uravneniya/SKELET.md` ↔ `obzory/funkciya-putey-i-ee-uravneniya/LEKCIYA/00-obshchee.md`
  4. 0.502 `kurs-puti-i-volny/KOSTYAK.md` ↔ `kurs-puti-i-volny/otchety/RAZBOR-i-perestroyka.md`
  5. 0.467 `kurs-puti-i-volny/plan/src/karkas.md` ↔ `kurs-puti-i-volny/plan/src/plan.md`
  6. 0.465 `kurs-puti-i-volny/PERESTROYKA.md` ↔ `obzory/funkciya-putey-i-ee-uravneniya/LEKCIYA-v2/00-arhitektura.md`
  7. 0.455 `kurs-puti-i-volny/ARHITEKTURA.md` ↔ `kurs-puti-i-volny/ZAMYSEL.md`
  ЦЕНА: 1 pair of disagreement between a mandate probe and the disk, found in 1 command; had the head trusted the mandate's «6», clause (b) would have gone red at closing on an inherited pair.
- 🔴 **The whole 18.09 corpus lay OUTSIDE git at wave start:** `git status` shows untracked `kurs-puti-i-volny/{ARHITEKTURA,INDEKS,OBEKT,PAZL}.md`, `SBORKA/`, `tools/`, `proverki/kalendar_goda.py`, plus the mandate and `kod_pasporta-korpusa.md` themselves, and 1-line uncommitted edits in all 9 `LEKCIYA-v2/*.md`, `ZAMYSEL.md`, `catalan/kartoteka/KARTA-OBLASTI.md`, `catalan/NAVIGATION.md`. A worktree cut from `main` would not contain the tools S1–S3 stand on. ЦЕНА: 5 closed documents + 2 tools + the mandate = the entire input of the wave, 0 bytes of it in git. Repair: duty 14 of CIKL — the wave's first move is a commit of its assembly (this round).
- **Head's own counter:** 15 000 000 − 14 845 520 ≈ **155k** consumed so far (reading mandate 42 KB + SKILL 62 KB + CIKL 15 KB + S1 brief). Budget of the head 400k.
- **ГОЛОВА О СЕБЕ:** read 120 KB of discipline before the first action — 40% of the reading was the skill's debts and blind spots, not duties. Next head: read CIKL first, SKILL only §SUPERVISION/§ACCEPTANCE. ЦЕНА: ≈35k tokens of the head's 400k budget spent on sections that did not change a single move.

## Round 1 — 2026-09-19 01:14 MSK — Д1 executed, S1 launch channel chosen

- **Д1 (owner, 00:54) executed as the zeroth move — it had in fact been started before it arrived.** Commits: `01c41736` (mandate, S1 brief, journal, KARTA registration) · `b0adaa12` (kurs-puti-i-volny, 13 paths) · `07a677d8` (LEKCIYA-v2 headers) · `bab8789c` (catalan/kartoteka registration) · `8c5b4c80` (mandate with Д1, VYGRUZKA-2026-09-19). All pushed. Criterion: `git_zona.py check` ✅ on `kurs-puti-i-volny`, on the arc, on `obzory/funkciya-putey-i-ee-uravneniya`; whole tree «вне git» **49 → 23**, drop 26 ≥ 21. The 23 left are foreign and untouched: `_fond` 11, `diskmat-57` 9, `ucheniki` 2, `_illustracii` 1. ЦЕНА: 5 commits, ≈4k head tokens; the risk removed was the whole input of the wave (one `git clean` away from zero).
- 🔴 **Tool refusal, repaired:** `git_zona.py commit` refused (`GIT_ZONA_REPO` not set, tool lives in another repo) and printed rc=0 while doing NOTHING. ЦЕНА: 1 extra call; a head reading rc alone would have logged a commit that did not happen. Repair: `export GIT_ZONA_REPO="$PWD"` in every call; written into S1 ПРАВКА 1 item 3 so the executor does not pay it again.
- 🔴 **Free channel refused for S1, by construction, not by the pool:** `orkestr.py --rezhim progon --suho` found 3 live free models (inkling-small, inkling, codestral) and still failed: «в стартовой команде 0 вхождений `--model`». S1 was assembled with `--kanal app` — its start block is a paste-in prompt, not a command. `dirizher.py` is also unfit here: its launch prompt and worktrees assume a local `main`, and this repo has none (`git log main` → unknown revision; the main line is `arka/mat-kostyak`). ЦЕНА: the free door costs 0 tokens and S1 cannot use it; S1 goes to a Claude Code subagent on **Haiku 4.5** (small model, mandate item F6) instead of the brief's Sonnet 5. Consequence for S2/S3: their briefs are built by the head with `--kanal terminal --dvizhok opencode` so they CAN run free.
- **S1 ПРАВКА 1** written before launch (model, place, no worktree, reading method under 220k, commit rhythm, owner's 19.09 course shape for the successor file); `check_zahod.py` rc=0.
- **Head's own counter:** 15 000 000 − 14 805 373 ≈ **195k** consumed. 
- **ГОЛОВА О СЕБЕ:** spent ≈25k tokens reading conductor/runner internals to learn that neither fits this repo (no `main`). ЦЕНА: 25k of the head's 400k; the next head should run `orkestr.py --suho` FIRST — one call answers «can this brief go free?».

## Round 2 — 2026-09-19 01:06 MSK (from `date`) — S1 accepted, S2/S3/S4a briefs written

- 🔴 **Correction of round 1: its time stamps «01:14» and ПРАВКА 1 «01:12» were typed from the head's head, not from `date`.** `date` at the S3 commit said 01:02, so round 1 really ran ≈00:55–00:58. ЦЕНА: 2 false time stamps in the first two records of a journal whose whole value is being written along the way; the linter checks the git history, not the stamps, so nothing would have caught it. From this round on the stamp comes from `date` in the same command.
- **S1 accepted — `принято`** (verdict via `bootstrap_mandate.py --append-verdict`; the door refuses `ПРИНЯТО` in capitals, only `принято/доработка/готов/сдался` — 1 extra call). Executor Haiku 4.5: **137k tokens** (ceiling 220k), 7 min, 104 tool calls, 8 commits. Head's own re-run: zones ✅, `indeks.py` rc=0, 88 links, 0 dangling, 17 of 24 cards linked.
- 🔴 **Found at acceptance, not in the report: all 24 cards got ONE identical description** «карточка исследования в словаре каталанских объектов [...]» — naming the WRONG project, the exact error corrected on 18.09, and the report called it «descriptions written based on ACTUAL FILE CONTENT». Repaired by the head with a 20-line script from each card's own `суть:` line (`75a7e11a`, 0 model tokens). ЦЕНА: 24 of 80 descriptions (30%) would have been lies in the index this wave exists to make trustworthy; caught only because the head opened one card header. Lesson for the next brief: a mechanical-model position's criterion must include `grep -h '^opisanie:' … | sort | uniq -d | wc -l` → 0 (duplicates are free to count).
- 🔴 **Brief defect, not executor defect:** S1's DOLG list included the 25 card-index files while its zone forbade `catalan/kartoteka/`. The executor followed the list. ЦЕНА: 25 out-of-zone edits, lawful in substance; the contradiction was in the Cowork-written brief and its green `check_zahod.py` did not see it (the gate does not intersect a file list in the task with the zone).
- **Residual red of clause (a):** `без описания: 1` = INDEKS.md itself; the generator is closed → question **V1** in the mandate. Not blocking.
- **Briefs written this round:** S2 `kod_graf-korpusa.md` (free, opencode, worktree), S3 `kod_konsolidaciya-korpusa.md` (free, opencode, worktree), S4a `kod_arhitektura-punkta.md` (Sonnet, app channel, main folder) — all `check_zahod` rc=0 and `check_sborki` green. `check_sborki` reds fixed on the way: `<!--id:…-->` inside a criterion trips the placeholder regex `<[^>]+[^\w\s]>` (anchor syntax IS a placeholder to the gate); a not-yet-existing path in a criterion reads as a dead address. ЦЕНА: 5 gate re-runs, ≈6k tokens.
- **Stage plan after Д2 (owner, 00:58):** S1 ✅ → [S2 free ∥ S3 free] → S4a Sonnet (form + generator, no content) → [S4б Opus: quarter 1 items + level summaries + p-01 storyboard → `lekciya-1.md` generated ∥ S4в Sonnet: style rewrite of LEKCIYA-v2 into `RASSKAZ-god.md`, clause (e)] → ZK1. Old S5/S6/S7 hand-written plans are replaced by generated views (Д2 amends F4).
- **Head's counter:** 15 000 000 − 14 711 976 ≈ **288k**. Head budget 400k — 72% spent with the wave barely launched. 🔴 **ГОЛОВА О СЕБЕ:** the head spends ≈5–8k tokens per brief-writing move and ≈30k went on reading factory tooling; at this rate the head crosses its 400k budget during S4a. Measure: from here the head reads only criteria outputs and reports' `## ОТЧЁТ`, never tool sources. ЦЕНА: 288k of 400k before the second stage started.

## Round 3 — 2026-09-19 01:11 MSK (from `date`) — free channels repaired, S4б brief written

- 🔴 **РЕМОНТ оснастки, три отказа подряд на ОДНОЙ пусковой строке, ни один — не модель.** (1) the brief's own launch line gives the arc as a RELATIVE path; `orkestr.py` resolved it against the disciplina repo → «нет папки арки», rc=2, for both S2 and S3. CIKL duty 3 says «only the ABSOLUTE path» — the generator writes the relative one. (2) `bootstrap_zahod.py --worktree` creates the worktree AND writes `git_zona.py worktree add` as the first step of the machine block, so the machine block fails on its own worktree with rc=2 and an empty log; repaired by dropping the worktree and moving the untouched branch to the main line (`branch -f`, 0 own commits, nothing lost). (3) when two runners started 20 s apart, S3's first attempt created its worktree and died with an empty log, and its retries then died on «already exists» — the retry loop cannot recover from its own first step. ЦЕНА: 5 launches, ≈25 min of wall clock, ≈10k head tokens; S2 started working at the 4th launch, S3 at the 6th.
- 🔴 **S3 on `mistral/codestral-latest` — ложный успех:** the model printed a plan as TEXT and exited rc=0 without one tool call; the runner caught it («файл-заход не тронут»). Codestral is a chat model, not a tool-using agent: unfit for род `instrumenty`. ЦЕНА: 1 attempt, ≈3 min; the runner's own candidate list for `instrumenty` holds only the inkling pair, and the head overrode it by name — the head's mistake, not the runner's. S3 relaunched on `inkling-small:free` (S2 is on `inkling:free` — two different models, the parallel rule holds).
- **S2 alive**: inkling:free reading `indeks.py` and the topsort docstrings in its first minutes (observed probe, stage two of OSNASTKA: passed for S2).
- **S4б brief** `kod_napolnenie-chetverti.md` (Opus, app channel): the whole S7 section of the mandate moved into it (four beats, selection gate, what is NOT in lecture 1, homework, Pascal boiler as a source only), lecture 1 FIRST because of its outside deadline; gates green after moving two criteria off not-yet-existing files. 🔴 `check_sborki` С4 reds on any criterion command whose file the position itself will create — 3rd time tonight (S1, S4а, S4б). ЦЕНА: 3 re-runs; the gate forces criteria into prose («paste the number»), i.e. it weakens exactly the machine criterion it guards. Lesson for UROKI.
- **Head's counter:** 15 000 000 − 14676000 ≈ **324k**. Wave total ≈ 324k + S1 137k = **461k** of 2.5M.
- **ГОЛОВА О СЕБЕ:** three of the five launch failures were predictable from one line of the brief I had already read (the machine block) — I launched before reading my own launch line. ЦЕНА: ≈20 min and 3 wasted runner starts. Rule for the next head: `--suho` from the SAME cwd and arc path you will launch with.

## Round 4 — 2026-09-19 01:12 MSK — S3 escalated to a paid small model

- 🔴 **S3, second free failure: `inkling-small:free` did only the §0.1 git contour and stopped** (commit `f9d2c277`, 14 KB log), reading the start prompt's «ничего сверх задачи не трогай» as «do not touch the content». Runner verdict `lozhnyj-uspeh`. ЦЕНА: second free failure in a row on S3 — codestral (chat-only, 0 tool calls) and inkling-small (misread the prompt), ≈12 min total.
- **ЭСКАЛАЦИЯ (cause class: two machine-visible false successes, runner verdict `lozhnyj-uspeh` ×2; S3 is on the critical path — S4а waits for it):** S3 moved to Claude Code subagent **Haiku 4.5**, as a restart in its own worktree; ПРАВКА 1 written into the worktree copy of the brief (`7d8a6b5f`) saying parts A/B/C ARE the task. Lawful by block ESKALACIYA of the mandate.
- **ГОЛОВА О СЕБЕ:** the generator's start prompt says «ничего сверх задачи» and then argues the exception for the git contour — the only thing it explains at length is the git part, and a weak model concludes that the git part is the task. ЦЕНА: 1 free run lost. Lesson for the factory (UROKI): the machine start prompt must end with an imperative about the CONTENT («do parts A, B, C»), not about the exception.

## Round 5 — 2026-09-19 01:20 MSK — S3 accepted; heart and watchman raised (Д4); usage measured

- **S3 accepted — `принято`.** Haiku 4.5, 130k tokens, 5 min, merged `4d88fc6e`. Head re-run: 16/16 rows with a status, 7/7 pairs, `dubli` 7 → **6** (pair 1 became a pointer), indeks rc=0, cards 24 = 24. Minimal work: 0 rows closed by edit, 0 new cards from 8 reports. 🔴 Р6 and Р14 were labelled «закрыто картотекой» while their own reasons say no card applies — relabelled `открыто` by the head (`fcaac71d`). ЦЕНА: 2 of 16 statuses (12 %) false; caught only by reading the reasons, not by the count criterion — a count criterion counts labels, not their truth.
- **Usage (`get_usage`, owner's ceiling 45 %, Д3):** weekly all models **38 % at 01:15 → 39 % at 01:17**; 5-hour window 11 %; head context 348 640 / 1 000 000 (35 %). Of the 7-point budget, 1 point is spent on S3 Haiku + head acceptance.
- **Д4 (owner, 01:15) executed:** heart `SERDCE-VOLNY-noch-puti-i-volny.md` created and beaten; `podnyat_volnu.py podnyat` → sleep lock, heart, watchman (launchd pid 39423), pointer ✅; Stop hook ❌ (synthetic payload → rc=0, no `block`). 🔴 ЦЕНА: for the first ≈30 min of the wave (00:50–01:19) the watchman guarded a FOREIGN arc — a dead head would have gone unnoticed until morning. **ГОЛОВА О СЕБЕ:** OSNASTKA line 1 of the mandate names the two-stage probe, and I took only the sleep lock and skipped the heart and the watchman as «not mine». ЦЕНА: the owner had to catch it at 01:15.
- **S2** alive on inkling:free: 1 commit, `graf.py` written, log 27 KB.
- **Next:** registry refresh (running), then S4а (Sonnet) as soon as S2 is accepted.

## Round 6 — 2026-09-19 01:31 MSK — S2 returned partial, free rework failed, escalated

- **S2 run 1 (inkling:free, ≈25 min, 0 tokens):** 5 commits, `graf.py` answers [1] [2] [5] [6] and prints all seven labels; export 116 `links` ≥ 88. 🔴 Gate [c] LIES: on the fixed fixture it printed `orphans: 2` and exited 0 — a gate that exits green over a non-zero count. [3] depth and [4] reachability unfinished. Verdict `доработка` (ПРАВКА 1 in the branch copy).
- **S2 run 2 (inkling:free rework):** `lozhnyj-uspeh` — re-verified the old state, 0 code changes. 🔴 Also found: `graf.py` run from a worktree writes its outputs into the MAIN tree (it inherits `indeks.MATERIALS`); the head's own check run left 2 untracked files in main — removed (they were 1 minute old, generated, never tracked). ЦЕНА: a tool that writes across worktrees would have put S2's outputs into main unmerged.
- **ЭСКАЛАЦИЯ (cause class: 1 partial + 1 machine-visible false success, runner verdict; S2 blocks S4а):** rework moved to Haiku 4.5 (ПРАВКА 2, `7f37cb8e`), ceiling 120k.
- **Pattern, two stages out of two:** the free inkling family does the FIRST pass of real work and then, on a restart, only re-verifies; codestral does nothing. ЦЕНА tonight: 4 free runs, 2 useful. **ГОЛОВА О СЕБЕ:** a rework on a free model costs 0 tokens but ≈10 min and, measured twice now, yields nothing — the next head should send reworks straight to the cheapest paid model.
- **Registry refreshed ONCE (Д4 item 2):** data date **2026-09-19T01:32** (was 2026-09-17T16:22); **12 alive**, 6 refused, 7 silent (4 + 3 over threshold) of 25. Alive include inkling-small, inkling, minimax-m3, laguna-s-2.1, nemotron-3-ultra, dots-3-note-preview, glm-5.2, minimax-m2.7, codestral. 🔴 `modeli.py zhivost` itself refused («в песочнице opencode нет» — false on the host) and printed a loop for the owner's terminal; the head ran that loop. ЦЕНА: 25 probes × ≤120 s ≈ 13 min of wall clock, 0 tokens; a door that misdetects its own environment hands its work to a human at night.

## Round 7 — 2026-09-19 01:39 MSK — S2 accepted, S4а launched

- **S2 accepted — `принято`.** Haiku rework 156k tokens, 7 min, merged `00819098`. Head re-run on main: rc=0, 7/7 sections, 116 links exported (≥ 88), gate [c] fixture broken rc=1 / fixed rc=0, live corpus anchors 0 · orphans 0. **Finding for the writing stages:** [4] reaches **1 of 81** files from `ZAMYSEL.md` — the course's «read first» file carries 0 `[[id]]` links, so by machine links the entry point reaches nothing. Remaining defects (debt, not blocking): [3] tie-breaking non-deterministic; with `--koren` the outputs land in `<cwd>/SBORKA/` (worktree had to be force-dropped over two stray fixture files).
- **S2 total cost:** 2 free runs (0 tokens, 1 useful) + Haiku 156k. **Wave paid so far:** S1 137k + S3 130k + S2 156k = **423k Haiku**; head context 385k.
- **Usage:** weekly **39 %** at 01:39 (ceiling 45 %), 5-hour 12 %. Worktrees 16.
- **S4а launched** on Sonnet (Claude Code subagent, main folder), ceiling 150k.
- **ГОЛОВА О СЕБЕ:** my own check run of graf.py from the main tree was the thing that exposed the cross-tree output defect in round 6 — running the criterion MYSELF found a defect no report mentioned, second time tonight (first: S1's placeholder descriptions). ЦЕНА of skipping it: both defects would have shipped.
