# Канал исполнителя — nositel-start (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet 5** — механика расписана до строк файла, план рождаться по ходу не должен.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.
> *(Вписан руками 13.08: генератор его пока только печатает в stdout — ровно та дыра, которую этот заход и закрывает. Первый ход владельца: запустить рабочую папку и отдать сообщение ниже.)*

```
Модель: Sonnet 5 — механика расписана до строк файла, план рождаться по ходу не должен.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/materials-wt/nositel-start.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-08-13_nositeli-zahoda/kod_nositel-start.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Первым ходом: python3 _generator/tools/git_zona.py worktree add nositel-start --branch zahod/nositel-start, затем чтение захода, затем секция
ПЛАН внутри него — до всяких действий.
```

⚠ Одна строка блока здесь изменена намеренно: «секция **## ПЛАН**» → «секция **ПЛАН**». Причина — ловушка, найденная при сборке этого захода и описанная в §2 шаг 1: заголовок второго уровня внутри секции-носителя обрезает «задание» для клауз З10/З12 и удваивает счёт З9. Это ровно то, что заход обязан починить по-настоящему.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-13_nositeli-zahoda» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/festive-sweet-clarke/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. `bootstrap_zahod.py` печатает стартовое сообщение владельцу (то, чем он запускает исполнителя) **только в stdout**. Канон `disciplina-zahod §Два канала запуска` при этом говорит: «Заход считается сданным владельцу только вместе с этим блоком», и восстанавливать блок по памяти ЗАПРЕЩЕНО тем же правилом. Прошлый этап: 13.08 блок пропал на живом заходе `kod_yadro-blokov.md` — аналитик гнал генератор из песочницы и обрезал вывод, блок стоял выше хвоста. Перечитать было неоткуда: на диске он не остаётся нигде, а законного способа восстановить нет вовсе.

ЦЕЛЬ: дать блоку носитель на диске — генератор кладёт его секцией в НАЧАЛО файла-захода, а линтер входа краснеет, если секции нет или она пуста. Приём не новый: тем же закрыли молчаливый дефолт канала (`--kanal` сделан обязательным).

Приёмка — по ОТЧЁТУ, без построчной сверки.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

1. секция кладётся в НАЧАЛО файла-захода (решение владельца 13.08: он открывает файл, копирует блок и отправляет; в конце искать неудобно)
2. канал сборки этого захода — app (Claude Code в десктопе)
3. З13 — четвёртое объявленное исключение линтера наравне с З10/З11/З12: на заходах, собранных ДО этой правки, оно краснеет законно

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/nositel-start`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/nositel-start`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add nositel-start --branch zahod/nositel-start
  cd ../materials-wt/nositel-start
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/bootstrap_zahod.py` `_generator/tools/check_zahod.py` `_generator/tools/fixtures/zahod/PROGNAT.sh`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**0. ВЫЧИТАТЬ ОЧЕРЕДЬ.** Что просил сделать Cowork — он в `.git` не пишет и отдаёт это тебе:
    python3 _generator/tools/git_zona.py zayavki
Исполняй каждую заявку командами `git_zona.py`, потом закрывай:
    python3 _generator/tools/git_zona.py zayavka-zakryt <id> --rezultat "<что вышло>"
Упёрся (конфликт, чужой лок, грязный путь) — `merge --abort`, затем
`zayavka-zakryt <id> --zastryala "<что помешало>"` и РАБОТАЙ ДАЛЬШЕ по своей задаче: заявка
останется открытой, приёмка на ней покраснеет. Исполнение заявки — единственное законное
действие за пределами твоей зоны (`GIT-disciplina §4г`); называй каждую в `## ОТЧЁТ` с её id.

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

Забирать нечего: правок аналитика в этой зоне нет — вся работа по этому заходу твоя.

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

Гасить нечего. 🔴 **Чужие ветки и рабочие папки НЕ трогай вовсе:** рядом идут другие заходы (при сборке этого насчитано 15 живых рабочих папок), и погашенная чужая папка исчезнет у исполнителя под ногами.

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/nositel-start`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/bootstrap_zahod.py` `_generator/tools/check_zahod.py` `_generator/tools/fixtures/zahod/PROGNAT.sh`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти три файла, целиком, и ничего больше: `_generator/tools/bootstrap_zahod.py` · `_generator/tools/check_zahod.py` · `_generator/tools/fixtures/zahod/PROGNAT.sh`. Проект не изучай. Шаблон захода (`_studio/zhurnal/_TEMPLATE-zahod.md`) читать НЕ надо и править его НЕЛЬЗЯ — он вне зоны, и секция в него не добавляется: её пишет генератор (см. §2, почему именно так).
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Три шага, строго в этом порядке: сначала генератор кладёт секцию, потом линтер её сторожит, потом фикстура сторожит линтер.

### Шаг 1 — `bootstrap_zahod.py` кладёт стартовое сообщение В ФАЙЛ, а не только в stdout

**Куда:** секцией `## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ` в **НАЧАЛО** файла-захода — сразу после блока-шапки (после строк `>` и до `── СЧЁТ НЕЗАКРЫТОГО ──`). Начало, а не конец, — решение владельца 13.08 дословно: *«лучше ставить секции в начало файла… я захожу, копирую и отправляю»*. В конце ему пришлось бы искать.

🔴 **Секция начинается строкой-адресатом, потому что читать её первым будет ИСПОЛНИТЕЛЬ, а адресована она владельцу.** Первой строкой секции — ровно это: `> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.` Без такой строки исполнитель получает в самом начале файла инструкцию, обращённую не к нему, и обязан гадать, чья она.

**Что именно кладётся:** тот же текст, что уходит в stdout, **байт в байт, из одной переменной**. 🔴 Не «собрать похожий блок ещё раз» — это дало бы два дома у одного решения, и они разъедутся при первой правке. Значит:

- собери блок в переменную ДО записи файла (сейчас он собирается в самом хвосте `main()`, после `dst.write_text`, — эту часть надо поднять выше);
- вставь переменную в `text`, потом прогоняй линтер, потом `dst.write_text(text)`;
- печатай в stdout **ту же переменную**.

**Оба канала.** У `--kanal app` блок — стартовое сообщение (`Модель: … / Ты исполнитель в репозитории … / Твой единственный вход …`). У `--kanal terminal` блок — стартовая КОМАНДА (`cd … && claude -p …`, плюс первая строка `worktree add`, когда она есть, плюс команда снятия токенов). В файл кладётся то, что соответствует выбранному каналу; в кодовом блоке ` ``` `, чтобы владелец копировал одним движением. **Прочее, что генератор печатает вокруг** (пояснения про `stream-json`, предупреждение о гонке checkout, «путь от места запуска») в файл НЕ едет: это разговор с аналитиком в момент сборки, а не то, что отдают владельцу.

**Порядок в коде — не косметика.** Структурный линтер (`check_zahod.judge(text, checks=STRUCTURAL_CHECKS)`) стоит ДО `dst.write_text` и судит собранный `text`. Секция обязана быть в `text` к этому моменту — иначе новая клауза (шаг 2), попав в `STRUCTURAL_CHECKS`, уронит генератор на его собственном свежем файле.

🔴 **ГЛАВНАЯ ЛОВУШКА, НАЙДЕННАЯ ЖИВЫМ ПРОГОНОМ ПРИ СБОРКЕ ЭТОГО ЗАХОДА — ПРОЧТИ ДО ПЛАНА.** Секция с блоком вписана в начало этого файла руками, и `check_zahod.py` тут же дал **три красных**: `З9` («секция „план" встречается 2 раза»), `З10` («нет блока §0.1 ГИТ-КОНТУР»), `З12` («нет строки „0. ВЫЧИТАТЬ ОЧЕРЕДЬ"»). Механизм не в секции как таковой:

- блок канала `app` содержит строку `## ПЛАН внутри него — до всяких действий`;
- `З10` и `З12` судят **только текст ДО `## ПЛАН`** (`ZADANIE_KONEC_RE`) — а этот `## ПЛАН` теперь стоит в НАЧАЛЕ файла, и «задание» схлопывается до нескольких строк, где блока §0.1 нет по построению;
- `З9` считает заголовки регуляркой `^##\s+ПЛАН\b` и получает два.

⇒ **Значит правка не сводится к «положить секцию».** Секция-носитель есть ЦИТАТА, а не текст задания, и все клаузы обязаны её игнорировать. Дешёвое лечение — одно и в одном месте: **вырезать секцию `## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ` из текста ОДИН раз, в `judge()`, до раздачи клаузам** — а не чинить З9, З10 и З12 по отдельности (три места разъедутся, четвёртая клауза про это не узнает). Сама З13 при этом должна смотреть на ПОЛНЫЙ текст: ей нужна как раз вырезаемая секция.

В этом файле я обошёл ловушку иначе — снял `##` в одной строке блока (см. пометку под ним). **Это заплатка на одном файле, а не решение; твоя работа — сделать так, чтобы заплатка стала не нужна.** После твоей правки строку в этом файле можно вернуть дословной, и линтер обязан молчать — включи этот случай в пункт 5 критерия.

**Живые ловушки помельче, проверь их явно:**
- в блоке канала `app` есть плейсхолдер в угловых скобках `<одна фраза почему; см. шапку захода>`. Проверка незаполненного в генераторе ищет `{{…}}`, не `<…>`, — то есть НЕ сломается; но убедись сам, а не поверь этой строке;
- `check_sborki.py` зовётся генератором на свежесобранном файле. Новая секция не должна ронять ни одну из его проверок. Прогон обязателен, вывод — в отчёт;
- клауза З9 требует, чтобы секции, которые скелет заводит один раз, встречались РОВНО один раз. Имя секции уникально — проверь грепом, что оно не совпало ни с одним существующим заголовком.

### Шаг 2 — клауза **З13** в `check_zahod.py`: секция есть и непуста

Форма — как у соседних клауз, тем же стилем, с комментарием-шапкой `# ── З13: …`. Красит поимённо (`("З13", "…")`).

🔴 **З13 — ЧЕТВЁРТОЕ ОБЪЯВЛЕННОЕ ИСКЛЮЧЕНИЕ, наравне с З10/З11/З12.** На каждом заходе, собранном ДО этой правки, секции нет и З13 краснеет — красное истинное, а не ложное. Допиши З13 в тот же список объявленных исключений в докстринге `check_zahod.py` (он сейчас говорит «три объявленных исключения — З10, З11 и З12») и в фикстуру положительного контроля, которая требует молчания по всем проверкам КРОМЕ них. **Не расширяй исключение до «молчать, если файл старый»:** дата файла — не свойство текста, и такой обход отключит клаузу навсегда.

Реши и назови в `## ПЛАН`: входит ли З13 в `STRUCTURAL_CHECKS` (набор, которым генератор судит себя при сборке). Довод «за»: секцию пишет сам генератор, значит ответить за неё он может сразу — как за зону и секции-заготовки. Довод «против»: если порядок из шага 1 сделан неверно, генератор перестанет собирать заходы вообще. Выбери одно, обоснуй одной фразой.

### Шаг 3 — фикстуры `PROGNAT.sh`: минимум две новые ловушки

- **отрицательная:** заход БЕЗ секции (или с пустой) → З13 краснеет **поимённо**;
- **положительная:** свежесобранный генератором заход → З13 молчит. Гони её на ОБОИХ каналах (`--kanal app` и `--kanal terminal`): у них разное содержимое блока, и клауза, случайно завязанная на слово из одного канала, на другом даст ложное красное.
- Число ловушек в шапке `PROGNAT.sh` («ЛОВУШЕК ТРИДЦАТЬ ПЯТЬ») **обнови**, и обнови охватную строку `ОХВАТ:` — она сейчас говорит «девять проверок (З1-З9)» и отстала уже на четыре.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ — четыре пункта, все машинные, гнать из корня репозитория):**

1. **Секция доехала в файл и совпала со stdout.** Собери пробный заход в этой же арке (`тема` = `probnyj-nositel`, `--branch zahod/probnyj-nositel`, зона — любой один файл, `--kanal app`), перенаправив stdout в файл. Тогда:
   ```
   grep -n '^## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ' <путь к kod_probnyj-nositel.md>   # ровно 1 совпадение, номер строки < 15
   ```
   и **блок из файла совпадает с блоком из stdout — сверь `diff`ом, не глазами.** Команду сверки придумай сам и **напечатай её вместе с выводом** в отчёт: «сверил» без команды в отчёте не принимается.
2. **Тот же пункт 1 на `--kanal terminal`** (тема `probnyj-nositel-t`) — секция есть, непуста, и внутри неё лежит строка `claude -p`.
3. **Линтер судит правильно в обе стороны:** `python3 _generator/tools/check_zahod.py <свежий пробный заход>` → rc=0 и ни одного З13; тот же файл с вырезанной секцией → rc=1 и в выводе **именно `З13`**, а не что-то другое.
4. **Фикстуры зелёные целиком:** `sh _generator/tools/fixtures/zahod/PROGNAT.sh` → **rc=0** (сначала `echo $?`, потом выводы). Число ловушек до и после — командой:
   ```
   grep -c '^#  *[0-9]*\.' _generator/tools/fixtures/zahod/PROGNAT.sh
   ```
   В отчёт: два числа и разница. Разница 0 — критерий провален, сколько бы ни было сказано словами.

5. **ЖИВОЙ ПРОГОН на реальных объектах репозитория, а не только на фикстурах** — это отдельный пункт, потому что фикстура сторожит поломку, а он сторожит излишнюю строгость, от которой линтер отключат целиком. Возьми пять последних живых `kod_*.md` (`ls -t` по журналу) и прогони на каждом `check_zahod.py`. **Ожидание: красным светит ТОЛЬКО объявленное — З10, З11, З12, З13 — и ничего сверх.** Если новая клауза уронила что-то ещё или изменила поведение соседней, это регрессия. В отчёт — список из пяти файлов с вердиктом по каждому.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «всё зелено», а «зелено, проверено X пунктов из 5, каналов 2 из 2, живых файлов 5 из 5». Без охвата вердикт не принимается.

🔴 **Считаешь критерий кривым — скажи в `## ПЛАН` ДО работы и предложи поправку.** Опровержение — полноценный успех: если носитель в файле окажется хуже носителя в отдельном файле, это надо сказать, а не сделать плохо и отчитаться зелёным.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). **Не нужен: операция не лоссовая** — ничего не двигается, не сжимается и не удаляется, а критерий §2 весь машинный и проверяется прогоном. Единственное, что здесь может пропасть, — пробные заходы `kod_probnyj-nositel*.md`: они регистрируются в `KARTA.md` при сборке, поэтому НЕ удаляй их, а назови в отчёте отдельной строкой, чтобы приёмка решила их судьбу дверью.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/bootstrap_zahod.py _generator/tools/check_zahod.py _generator/tools/fixtures/zahod/PROGNAT.sh                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/bootstrap_zahod.py _generator/tools/check_zahod.py _generator/tools/fixtures/zahod/PROGNAT.sh   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/bootstrap_zahod.py` `_generator/tools/check_zahod.py` `_generator/tools/fixtures/zahod/PROGNAT.sh`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/bootstrap_zahod.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/check_zahod.py` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/fixtures/zahod/PROGNAT.sh` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*
