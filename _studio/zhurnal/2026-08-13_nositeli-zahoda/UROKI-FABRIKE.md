# УРОКИ ФАБРИКЕ — арка «2026-08-13_nositeli-zahoda»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*

---

## Пункты очереди заходов, доставленные `dostavit_urok.py`

> Дословные находки из секций `## ВОПРОСЫ` файлов-заходов — голос исполнителя, который об это споткнулся, не редактора. Разбирает их человек: находка описывает, что мешало, но задачей сама не становится.
> Строка `ДОСТАВЛЕНО: <заход>#<N>` внизу каждой записи — та же метка, что стоит в источнике; по ней Г7 `priyomka.py` проверяет, что доставка не объявлена ложно.

#### nositel#4

4. Урок фабрике: **жёсткий структурный гейт (`STRUCTURAL_CHECKS`) на артефакте, чей якорь сборки МЯГКИЙ (терпит отсутствие ради синтетических/урезанных шаблонов фикстур), роняет генератор на КАЖДОМ таком шаблоне** — обнаружено живым прогоном чужой фикстуры `fixtures/dvizhok/PROGNAT.sh`, а не самопроверкой; цена — коммит встал бы (или, хуже, прошёл БЫ молча без хука) на состоянии, где `bootstrap_zahod.py` перестаёт собирать заходы из любого урезанного шаблона. Правило: прежде чем класть НОВУЮ клаузу в `STRUCTURAL_CHECKS`, проверить livewire-ом, что её якорь ТАКЖЕ жёсткий (не входит в список «мягких» анкоров с count>1-проверкой) — иначе клауза туда не идёт, только в полный `HARD_CHECKS`.
   ДОМ: _studio/zhurnal/2026-08-13_nositeli-zahoda/UROKI-FABRIKE.md
ДОСТАВЛЕНО: nositel#4 · 2026-08-13

### Фикстура копирует куратируемый список инструментов и отстаёт от импортов живого кода
ЦЕНА: ловушка 22 `fixtures/git_zona/PROGNAT.sh` (стр. 769–770) собирает синтетический репозиторий из перечня файлов, в котором нет `modeli.py`, — а `bootstrap_zahod.py:61` импортирует `modeli` давно. Итог: ловушка краснеет на КАЖДОЙ правке `bootstrap_zahod.py` любым будущим заходом, `ModuleNotFoundError`. Класс НЕМЕДЛЕННОЕ. Приёмкой подтверждено независимо: соседняя ловушка 17 (стр. 514) тот же `modeli.py` копирует — то есть список не единый, а размноженный по ловушкам, и расходится он молча.
ДОМ: disciplina/doma/meta/POKRYTIE.md
ВЕРДИКТ:

### Число ловушек в шапке фикстуры отстаёт от тела и никем не сверяется
ЦЕНА: `fixtures/sborka/PROGNAT.sh` называет в шапке «ЛОВУШЕК ДВАДЦАТЬ ЧЕТЫРЕ», а тело доходит до 42. Заход `nositel` обновил число командой у себя (25→26) и наткнулся на унаследованную недостачу — то есть шапка врала уже до него, и это не видно ничем: `grep -c` знает правду, но его никто не зовёт. Тот же класс, что «число вписано, а не посчитано» (`KONSTITUCIYA §10`), только внутри инструмента, который сам сторожит других.
ДОМ: disciplina/doma/meta/POKRYTIE.md
ВЕРДИКТ:

### Жёсткий гейт сборки на МЯГКОМ якоре роняет генератор на каждом урезанном шаблоне
ЦЕНА: заход `nositel` собирался положить З13 в `STRUCTURAL_CHECKS` (так стояло в его `## ПЛАН` п.5, и заход-задание довод «за» тоже называл). Живой прогон ЧУЖОЙ фикстуры `fixtures/dvizhok/PROGNAT.sh` уронил её целиком: та кормит `bootstrap_zahod.py` синтетическим мини-шаблоном без якоря `\nКОНТЕКСТ.` — того самого, на который садится секция-носитель. Якорь мягкий (как ВОПРОСЫ/УРОКИ/ГИГИЕНА — ни один не в `STRUCTURAL_CHECKS`), значит на урезанном шаблоне секция не вставляется, а жёсткий гейт сразу после вставки роняет генератор при КАЖДОМ таком вызове. Найдено не самопроверкой, а прогоном чужой применимой фикстуры. Правило-кандидат: новая клауза идёт в `STRUCTURAL_CHECKS` только если её якорь тоже жёсткий.
ДОМ: disciplina/doma/meta/POKRYTIE.md
ВЕРДИКТ:
*(тот же факт лежит выше находкой исполнителя `#### nositel#4` — здесь он оформлен уроком, потому что счётчик уроков читает только `### ` и `ЦЕНА:`.)*

### Доставленная находка ложится в слой «находок», где счётчик уроков её не видит
ЦЕНА: `dostavit_urok.py --dostavit 4` перенёс пункт очереди, который исполнитель сам озаглавил «Урок фабрике», заголовком `#### nositel#4` в секцию дословных находок — без строки `ЦЕНА:`. Гейт закрывающей сессии считает `^### ` и `^ЦЕНА:`, то есть этот урок не вошёл бы в разбор ни строкой, а выглядело бы это как «доставлено» (Г7 зелёный, метка на месте). Поймано приёмкой вручную; ровно тот класс, чья цена уже оплачена однажды: «файл выглядел 17 уроков / 18 цен / 1 вердикт, неся 17 уроков, 17 цен и 0 вердиктов».
ДОМ: disciplina/doma/meta/POKRYTIE.md
ВЕРДИКТ:
