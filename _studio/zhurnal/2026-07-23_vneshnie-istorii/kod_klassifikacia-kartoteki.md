# Канал исполнителя — klassifikacia-kartoteki (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — суждение: классификация и уточнение тезисов рождаются по ходу, механических якорей нет; двусторонняя работа «система ↔ классификация» — тяжёлое суждение, не расписываемая механика.

КОНТЕКСТ. Проект — курс «Введение в теорию категорий» (`teorkat-vvedenie`), **внешний трек**: сюжеты из чужих полей математики, ради которых курс слушают. Прошлый этап: собрана картотека внешних сюжетов (`KARTOTEKA-SYUZHETOV.md`) и **открытый список из 14 тезисов «зачем категории»** в 4 группах (`MANIFEST-zachem-kategorii.md`). ЦЕЛЬ: прогнать картотеку под тезисами и расклассифицировать — **двусторонне**: разметить сюжеты по тезисам И уточнить саму классификацию (склейки / удаления / новые тезисы — как ПРЕДЛОЖЕНИЯ), плюс собрать карту пробелов для будущего поиска.
Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **СТОП ДО ЦЕЛИ:** получишь классификацию + карту пробелов + предложения по тезисам — но **НЕ** запуск нового поиска Fable и **НЕ** применение изменений в манифест. И то и другое — отдельные ходы ПОСЛЕ приёмки владельцем. Твоё дело — измерить и предложить, не решить.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке репозитория — `git checkout arka/mat-kostyak` первым ходом. Ветку НЕ переключай дальше, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/zhurnal/2026-07-23_vneshnie-istorii/RAZBOR-klassifikacii.md _studio/zhurnal/2026-07-23_vneshnie-istorii/KARTA-PROBELOV.md _studio/zhurnal/2026-07-23_vneshnie-istorii/kod_klassifikacia-kartoteki.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout arka/mat-kostyak`.
- Точка отката: `git add _studio/zhurnal/2026-07-23_vneshnie-istorii/RAZBOR-klassifikacii.md _studio/zhurnal/2026-07-23_vneshnie-istorii/KARTA-PROBELOV.md _studio/zhurnal/2026-07-23_vneshnie-istorii/kod_klassifikacia-kartoteki.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти якоря (репо-относительные пути от корня):
  1. `_studio/zhurnal/2026-07-23_vneshnie-istorii/MANIFEST-zachem-kategorii.md` — 14 тезисов в 4 группах (I находить · II отождествлять/переносить · III компоновать процессы · IV регистр Quanta). **Это классификация, под которую разносишь.**
  2. `_studio/zhurnal/2026-07-23_vneshnie-istorii/KARTOTEKA-SYUZHETOV.md` — **корпус, который классифицируешь**: сюжеты A1–A8, B1–B9, банк F2, полный проход. Это ЕДИНСТВЕННЫЙ корпус задачи (сырые 242 из `PERECHOT` НЕ трогаешь — см. границы).
  3. `teorkat-vvedenie/SPEKA.md` — рама (читать ПЕРВОЙ, истина проекта): §Главный вопрос, §Зал (комбинаторика/теория чисел/линал/анализ/вероятность — доступные залу разделы), throughline «языка мало, приложений много».
  4. `teorkat-vvedenie/DUH.md` — оси гейта, которыми уже размечена картотека: §2 каналы, §3 прок, §5 мотивируемое↔мотивация, §9 эстетика.
  5. `teorkat-vvedenie/MAT-KOSTYAK.md` — **только шапку «Соглашения документа»** как референс осей разметки (род / вердикт / уровень / узел). Каркас берёшь, полностью документ не читаешь.
- Проект дальше не изучай — эти пять файлов самоценны для задачи.
- ПЛАН — в `## ПЛАН` перед действиями.

**ЯКОРНЫЕ ФАКТЫ (из штаба, дословно — чтобы не ходить в другие файлы):**
- 🔴 **Критерий рода — занять у арки A: род сюжета определяется тем, О ЧЁМ он, а не какими словами звучит.** Пример: утверждение про λ-исчисление и логику осталось *внутренним*, потому что утверждается свойство произвольной ДЗК; а «$V\cong V^{**}$» — *внешнее-пример*, потому что предмет конкретен (конечномерные пространства). Тот же нож режет «реальная внешняя помощь vs формализация естественности в переодевании».
- ⚠ **Форма источника ≠ существо предмета.** Сюжет, поданный языком одной области, может быть про другой вопрос — суди по предмету, не по подаче.
- **Опора-число:** во внутреннем списке из 67 утверждений содержательных нетривиальных безусловных — 6. Значит внешний приз для курса **экзистенциален** (без него курсу не на чем стоять), а не украшение. Вопрос «есть ли реальные примеры под тезис» — не академический.
- **Наводки, которые стоит увидеть при классификации** (не обязательны, но если всплывут — не потеряй): У60 как внешний приз Йонеды; walking-I; liminf/limsup категорно (Riehl, печ. 127–128) — доступный залу анализ; пределы/копределы мотивируются ИМЕННО снаружи (где залу пришлось собрать (ко)предел и это дало результат).

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.**

🔴 **НЕ ПОДЫГРЫВАЙ (заход проверяет ГИПОТЕЗУ аналитика).** 14 тезисов — это гипотеза аналитика, что классификация хорошая. Тебе с пустым контекстом не видно, что аналитику приятно её подтверждение. Поэтому:
- **Опровержение — полноценный успех.** Тезис пуст (нет ни одного сюжета), два тезиса дублируют друг друга, сюжет никуда не лезет — это НАХОДКА, ради которой заход и запущен, а не провал. Говори прямо.
- **Брак — только выдуманный вердикт.** Впихнуть сюжет в тезис силой ради «покрытия», выдумать пример, назвать пустой тезис населённым — вот это брак. Пустой тезис честнее натянутого.
- Правило владельца: **классификацию НЕ подгоняем под сюжеты и сюжеты НЕ подгоняем под классификацию силой** — проверяем, есть ли реальное соответствие. Нет — так и пиши.

**СУБАГЕНТЫ (аккуратный режим, назови в ПЛАНЕ сколько и на что):** до 5 читателей параллельно — разбить картотеку на батчи (например: A-программы · B-сюжеты · банк F2 · полный проход) и каждый батч разметить по 14 тезисам; ведущий (ты) сводит, решает спорные и формулирует предложения по классификации. Плюс ОДИН свежий субагент-верификатор (§3). Рейт-лимит = отступить + доложить, не слепой ретрай. Время меряй `date`, не суммой `sleep`.

## 2. ЗАДАЧА

Двусторонняя классификация. Работаешь только в двух новых файлах зоны (`RAZBOR-klassifikacii.md`, `KARTA-PROBELOV.md`); картотека и манифест — READ-ONLY (правки в них — только как ПРЕДЛОЖЕНИЯ в тексте `RAZBOR`, не редактируешь).

**Шаг 1 — исход по каждому сюжету.** Для КАЖДОГО сюжета картотеки — ровно один из трёх исходов, судя по предмету (критерий рода, §0):
- **(лёг)** — к какому тезису(ам) из 14 и какой группе относится; разметить осями по образцу MAT-KOSTYAK: род (внутреннее / внешнее-пример — по A/B-критерию) · уровень зала (школьно / требует-приручения / вне-школы) · статус из картотеки. Сюжет может лечь в 2 тезиса — так и пометь.
- **(заявка)** — не лёг ни в один, но силён → предложить новый или уточнённый тезис, с формулировкой (открытой, без примера — как в манифесте).
- **(на удаление/склейку)** — пуст, дублирует другой или бессмыслен как внешняя мотивация → предложить убрать или склеить, с обоснованием.

**Шаг 2 — взгляд со стороны классификации.** Для КАЖДОГО из 14 тезисов: сколько сюжетов легло (в т.ч. **0 — явно**); какие тезисы пусты, какие дублируют друг друга, какие просят переформулировки. Это и есть «двусторонность»: система и классификация правятся навстречу.

**Шаг 3 — артефакт `RAZBOR-klassifikacii.md`:** таблица «сюжет × исход × тезис × оси» + раздел **«Предложения по классификации»** (склейки / удаления / новые тезисы — каждое с обоснованием и пометкой «предложение, владельцу решать»).

**Шаг 4 — артефакт `KARTA-PROBELOV.md`** (вход для будущего Fable-поиска), две оси:
- **(а) разделы математики** — из каких зал-доступных областей (комбинаторика, теория чисел, линал, анализ, вероятность — §Зал SPEKA) сюжеты уже есть, какие тонки, какие пусты;
- **(б) роды применений** — какие тезисы/группы населены, какие пусты, и какие курсу НУЖНЫ (с учётом: внешний приз экзистенциален, §0).

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. Оба файла (`RAZBOR-klassifikacii.md`, `KARTA-PROBELOV.md`) на местах и непусты; **каждый сюжет картотеки имеет ровно один исход**, нераспределённых = 0. Число сюжетов взято **командой** (`grep` по картотеке), не на глаз; в `RAZBOR` есть строка «**распределено X из Y, X = Y**».
2. **Отрицательный вердикт несёт охват В СЕБЕ:** для каждого из 14 тезисов строка «примеров N» (N = 0 писать явно); в `KARTA-PROBELOV` — «проверено сюжетов X из Y». «Тезис пуст» без числа не принимается.
3. **Чекер честно печатает слепую зону:** явно назвать, что классифицирован корпус `KARTOTEKA-SYUZHETOV.md`, а сырые 242 из `PERECHOT` в охват НЕ входили.

## 3. ВЕРИФИКАТОР (обязателен — операция лоссовая: синтез сворачивает сюжеты в классификацию)
Свежий субагент, ДРУГОЙ метод: **независимо** читает список сюжетов из `KARTOTEKA-SYUZHETOV.md` (не из твоего `RAZBOR`) и проверяет как ГЕЙТ:
- **список потерь ПУСТ** — ни один сюжет не выброшен молча (сверка id сюжетов картотеки против id в `RAZBOR`, программно; «распределено X из Y» = правда);
- **ни один сюжет не впихнут силой** — на выборке спорных проверяет, что разметка честна по предмету (тезис реально про этот сюжет), а не натянута ради покрытия. Нашёл натяжку → назвать сюжет и тезис.
Результат верификатора — отдельной строкой в `## ОТЧЁТ` (список потерь, число натяжек).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/2026-07-23_vneshnie-istorii/RAZBOR-klassifikacii.md _studio/zhurnal/2026-07-23_vneshnie-istorii/KARTA-PROBELOV.md _studio/zhurnal/2026-07-23_vneshnie-istorii/kod_klassifikacia-kartoteki.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "внешние истории: классификация картотеки под 14 тезисов + карта пробелов (аналитический заход, предложения по классификации внутри)" -- _studio/zhurnal/2026-07-23_vneshnie-istorii/RAZBOR-klassifikacii.md _studio/zhurnal/2026-07-23_vneshnie-istorii/KARTA-PROBELOV.md _studio/zhurnal/2026-07-23_vneshnie-istorii/kod_klassifikacia-kartoteki.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` («УРОК БЕЗ ЦЕНЫ») судит staged-пути.** Коммитишь свою зону — там только твоё, `--no-verify` не нужен. Хук покраснел — значит в коммит уехало чужое: **не обходи, пересобери коммит.**
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Старт 2026-07-23 17:37. Прочитаны все 5 якорей целиком** (SPEKA первой; MAT-KOSTYAK — только «Соглашения документа»).

**Названные предпосылки/развилки (решаю сам, вслух):**
1. **Единица корпуса = позиция с вердиктом** в `KARTOTEKA-SYUZHETOV.md`: строка сводных таблиц A/B, позиция банка F2, строка таблицы полного прохода, вердиктное вхождение в прозе «Остаточных кластеров», списковая строка «Дополнений». Кластер якорей под одним вердиктом (напр. Брауэр-кластер) = 1 позиция — так решила сама картотека. «Слияния-пометки» (§495, «отдельный вердикт не нужен») и «Исключения-понятия» (5 узлов → зона A) — НЕ позиции: исключены самой картотекой. A3/A4 в «Пересмотре подвешенных» = те же строки сводной таблицы (не двою).
2. **Счёт корпуса командой:** 8 (A) + 9 (B) + 4 (F2) + 214 (таблицы прохода) + 22 (проза) + 8 (дополнения) = **Y = 265**. Команды — в ОТЧЁТЕ.
3. **Слитые строки** («слито с X» в примечании) получают исход узла-хозяина с пометкой «=X»; в «распределено X из Y» входят, в счёт «примеров N» по тезисам — НЕ входят (иначе двоение веса дублей исказит карту пробелов). Оба счёта — командой.
4. **⛔/❌-позиции тоже классифицируются по предмету**: тезисы манифеста — про «зачем категории» вообще, не только в зоне курса; статус картотеки — отдельная ось и сохраняется. Служебные ❌ («мимо», 3 шт.) — кандидаты в исход «удаление/склейку» как не-сюжеты.
5. Критерий готовности считаю здоровым, поправок не предлагаю.

**Субагенты — 5 читателей (Opus, параллельно) + 1 верификатор (Opus, свежий):**
- R1: сводные A1–A8 + B1–B9 + F2 + детальные разборы B и пересмотр A3/A4 (21 позиция, глубокие);
- R2: Ступень 1 (196–234) + Ступень 5 (309–316) + Мимо (501–503) = 50;
- R3: Ступень 2 (242–270) + Ступень 3 (278–297) = 49;
- R4: УС (324–380) = 57;
- R5: Пределы + ЭК + Сопряжения + За-границей + КХЛ + проза + дополнения (388–494) = 88 (много механики: готовые вердикты/темы границы).
Каждый получает: 14 тезисов дословно, критерий рода (якорь §0), шкалу уровня (MAT-KOSTYAK), опись зала (SPEKA), свои диапазоны строк, правило «не подыгрывай» (пустой тезис честнее натянутого, «не лёг» — законный исход), наводки §0. Спорное помечают — свожу и пересуживаю сам.

**Ход:** счёт корпуса ✓ → читатели → свод + пересуд спорных → `RAZBOR-klassifikacii.md` (таблица позиций + счёт по 14 тезисам + предложения по классификации) → `KARTA-PROBELOV.md` (оси: зал-разделы × тезисы; наводки штаба liminf/limsup, walking-I, У60; «внешний приз экзистенциален») → верификатор-гейт (потери=∅, натяжки на выборке) → правки → отчёт → коммит зоны (полная форма §4).

## ВОПРОСЫ — (заполняет исполнитель)

1. **Регистрация новых документов — нужен ход аналитика.** `RAZBOR-klassifikacii.md` и `KARTA-PROBELOV.md` — новые .md; по `KONSTITUCIYA §9` их надо зарегистрировать в `_studio/docs/KARTA.md §6`, но KARTA.md вне моей зоны (READ-ONLY). Без регистрации — документы-сироты.
2. **Каталожная мелочь вне зоны:** якорь [perrone 102] фигурирует в картотеке дважды с разными предметами (кластер пересечений, стр. 403, и проза «частные⇏совместное», стр. 477) — вероятно, две находки одной страницы источника, но стоит проверить при следующем туре картотеки. Не трогал.
3. Развилки по существу классификации (принять/отклонить тезис-Й, Z1, С, уточнения 3/5/7, судьба 12) — не вопросы захода, а **предложения владельцу** в `RAZBOR §Предложения` (П1–П10), каждое с пометкой «владельцу решать».

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Двусторонняя классификация корпуса `KARTOTEKA-SYUZHETOV.md` (265 позиций) под 14 тезисами `MANIFEST-zachem-kategorii.md` — чтобы измерить, держит ли классификация реальный корпус, и собрать вход для Фазы 2. Выходы: `RAZBOR-klassifikacii.md` (таблица позиция×исход×тезисы×оси, население тезисов, пересуды, П1–П10) и `KARTA-PROBELOV.md` (оси: зал-разделы × роды применений + целевые запросы Фазы 2).

**Главные результаты (сжатие для приёмки).**
- **Распределено 265 из 265, 265 = 265; нераспределённых 0.** Исходы: лёг 217 (12 дублей-наследников) · заявка 17 · удал-склейка 31. Все числа — командами (в `RAZBOR §Корпус`), не на глаз.
- **Нулевых тезисов нет, но три «пусты в зоне»:** 12 (N=2, оба носителя вне зоны — П8), 10 (N=6, все ⛔-моноидальность — П9), 14 (N=9, в зоне 2 слабых — П9). Перегружен 1 (N=65, шов двух мотивов — П7). Дублей среди 14 не найдено (П10).
- **Главная находка — тезис-Й (представимость/Йонеда) в манифесте отсутствует**, а корпус его вынуждает: 9 позиций-заявок (вся ступень 5 + F2-3/F2-4 + функтор точек), к пробелу независимо пришли три читателя из пяти; сходится с наводкой штаба «У60 как внешний приз Йонеды». Предложение П1 с черновиком формулировки.
- Прочие заявки: Z1 «зависимость как структура» (5 позиций, все носители за границей — П2 с оговоркой), У «относительность к универсуму» (2, как уточнение тезиса 5 — П4), С «согласование структур» (1, на вырост — П3). Уточнения формулировок: 7 (обратный ход инвариантов — П5), 3 (несуществование структур — П6).
- **Карта пробелов:** по зал-областям — линал/комбинаторика/теорчисла богаты, **анализ — эпизоды без пласта (дыра №1, наводка liminf/limsup в корпусе отсутствует — проверено grep)**, **вероятность — беднейшая в зоне (4 строки, ✅ одна; дыра №2)**. Целевые запросы Фазы 2 сведены с Кандидатами картотеки и рулём владельца 23.07 (геометрию не нацеливать).
- **Род:** 45 позиций из 265 — `внутреннее` (включая A1, A3, A7, B9): приз чужой при внутреннем роде утверждения — законно по критерию арки A, отмечено в RAZBOR.

**Как проверил.** (1) Счёт корпуса и распределения — командами (grep/awk, воспроизводимы из файлов); Y=265 посчитан ДО классификации, X=265 — после, X=Y. (2) Пять читателей-Opus параллельно (батчи: A/B/F2 · ступени 1+5+мимо · ступени 2+3 · УС · хвост прохода), спорные пересужены ведущим (7 пересудов, зафиксированы в RAZBOR). (3) **Верификатор** (свежий Opus, независимый метод: header-scoped awk + сверка множеств comm, не мои диапазоны строк) — результат ниже. (4) Два прозаических числа, пойманных верификатором, исправлены до коммита.

**Результат верификатора (гейт §3): ПРОЙДЕН.** Список потерь **ПУСТ** (бэктик-id: корпус 124 = разбор 124, множества тождественны; посекционная сверка 265=265 совпала полностью; фантомов 0 — полная обратная сверка). Натяжек **0** на 30 проверенных позициях (все 7 пересудов ведущего = 17 строк + 8 выборочных + 5 заявок контрпроверкой; «отказы заявок контрпроверку выдержали»). Найдено и исправлено: два прозаических числа в RAZBOR («26»→31 удал-склеек, «~55»→65 носителей т.1) — на распределение не влияли.

**Что НЕ трогал.** Картотеку и манифест (READ-ONLY — все правки только как предложения в RAZBOR); `_studio/docs/KARTA.md` (вне зоны — вопрос 1); сырые 242 PERECHOT (слепая зона по заходу, названа в обоих артефактах); Fable-поиск НЕ запускал, изменения в манифест НЕ применял (СТОП-граница захода).

**Открытое «возвращаться».** (а) Судьба П1–П10 — гейт владельца; (б) регистрация двух .md (вопрос 1); (в) [perrone 102]-дубль (вопрос 2); (г) флаг картотеки «`sobstvennoe-est-uravnitel` не сверено с источником» — остаётся, сверка при написании микроэссе.

**Время прогона:** 17:37–18:35, ~1 ч. **Токены:** субагенты (точно, из телеметрии): читатели 56.6k+59.4k+57.2k+57.5k+60.4k ≈ 291k, верификатор 189.5k; итого субагентских ≈ 481k; ведущий контекст — оценочно ~180k. Модель Opus везде (по заходу и калибровке: классификация = суждение).

**КОММИТ:** `035315a` — «внешние истории: классификация картотеки под 14 тезисов + карта пробелов (аналитический заход, предложения по классификации внутри)» (артефакты RAZBOR + KARTA-PROBELOV, 2 файла, 569 строк) · заход с отчётом — следующим коммитом тем же ходом · точка отката до исполнения — `30b40c6`.
**ГЕЙТ:** `git_zona.py check --zone` по `RAZBOR-klassifikacii.md` → ✅ и по `KARTA-PROBELOV.md` → ✅ (прогнаны, rc=0, «работа доехала в git, вне git ничего нет»); по `kod_klassifikacia-kartoteki.md` — прогон сразу после его коммита, при провале — перекоммит с поправкой этой строки.
