# Канал исполнителя — polnyj-prohod-vneshnie (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — суждение рождается по ходу: каждый узел судишь заново под новой рамой, готовых якорей нет.

КОНТЕКСТ. Курс «Введение в теорию категорий» (2–3 лекции, наша часть до сопряжений). Идёт **внешний трек** — сюжеты из чужих полей математики, которыми наполняется несущая линия «лестница естественности». Прошлый этап: аналитик свёл в картотеку выход Fable (8 программ + раздел B = B1–B9) — это лишь часть корпуса. ЦЕЛЬ: **достроить картотеку до ПОЛНОГО прохода** — прогнать весь уже собранный внешний материал (ночная карта областей + каталог находок), переосмыслив его под новую раму (лестница · канва универсальности · граница с Василием), и свести в один отборный вид с полной разметкой.

Приёмка — по ОТЧЁТУ, без построчной сверки.
🔴 **СТОП ДО ЦЕЛИ — заявляю громко:** ты производишь **систематизированный отбор** (входы №2 и №3 в сыром виде). Ты **НЕ** запускаешь новый поиск сюжетов (это Фаза 2, ждёт достройки лестницы аркой A) и **НЕ** пишешь микроэссе (это позже). Получишь полную размеченную картотеку — но НЕ новые сюжеты и НЕ тексты.
🔴 **Ты проверяешь работу аналитика — НЕ подыгрывай.** Аналитик уже разметил Fable A/B и заинтересован, чтобы его разметка подтвердилась. «Этот узел никуда не вписывается», «в этой области сильных сюжетов нет», «разметка аналитика по узлу N неверна» — **полноценные результаты**, а не брак. Брак — только выдуманный вердикт. Форма источника ≠ существо: **ночная карта размечала статус под СТАРУЮ раму (потолок CCC, финал КХЛ) — её статусы читай как гипотезу на пересмотр, не переноси.**

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** 🔴 **worktree-папка** арки B (ветка `arka/vneshnie-istorii`), НЕ основная папка. Основная стоит на `arka/mat-kostyak` (там параллельно идёт арка A) — `git checkout` в ней перепишет дерево соседу. Владелец завёл worktree командой `git_zona.py worktree add polnyj-prohod-vneshnie --branch arka/vneshnie-istorii`; путь напечатан в стартовом сообщении. Первый ход — `cd` в этот путь, НЕ `git checkout`. Ветку НЕ переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** _studio/zhurnal/2026-07-23_vneshnie-istorii/. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `cd` в worktree-папку (путь — в стартовом сообщении). Убедись `git branch --show-current` = `arka/vneshnie-istorii`.
- Точка отката: `git add _studio/zhurnal/2026-07-23_vneshnie-istorii/` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- **Прочитать ТОЛЬКО эти якоря (проект не изучай):**
  1. `teorkat-vvedenie/DUH.md` — критерий отбора: §1 приз чужой · §2 три канала · §3 лестница прока · §9 эстетика · §5 два слота мотивации. **Это гейт.**
  2. `teorkat-vvedenie/KOTYOL.md` §Несущая линия (лестница естественности, 5 ступеней + дыры УС/ЭК) · §Запрос · §Банк.
  3. `teorkat-vvedenie/SPEKA.md` §Охват — потолок и граница с Василием.
  4. Эта папка: `KARTOTEKA-SYUZHETOV.md` (уже размеченные Fable A1–A8, B1–B9 + §Решения владельца: канва, «не режем», «примеры сопряжений можно, слово — нет») · `NAVIGATOR.md` §Разведение (зона A vs B).
  5. Корпус на проход: `../2026-07-20_vvedenie-sborka/nochnaya-sborka/KARTA-OBLASTEJ.md` (229 узлов по 16 областям, разметка по узлу) · `../2026-07-20_teorkat-motivacia/PERECHOT-kataloga.md` (242 находки) · `../2026-07-20_teorkat-motivacia/fable_generacia-sjuzhetov.md` (оригинал Fable, разделы A–E) · `../2026-07-20_vvedenie-sborka/RAZBOR-vosmi-programm.md` (разбор A1–A8).
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА — полный проход по внешнему корпусу → размеченная картотека сюжетов

Достроить `KARTOTEKA-SYUZHETOV.md` (эта папка) до **полного отборного вида**. Тип захода — **аналитический с синтезом** (читаешь корпус, судишь, сводишь; оригиналы корпуса read-only).

**Шаг 1. Определи внешний корпус (числом, не на глаз).** Из `KARTA-OBLASTEJ.md` возьми узлы ВСЕХ областей, КРОМЕ `теория-категорий` и `методика-курса` (это зона арки A — понятия и процесс, не внешние сюжеты). Число внешних узлов посчитай командой (grep по полю `область`), впиши в отчёт «внешних узлов Y». Плюс 242 находки `PERECHOT` и раздел B Fable (B1–B9). Fable A1–A8 и B1–B9 уже в картотеке — их не переразбирай, а **встрой в общий вид** и при необходимости поправь разметку (это законно, ты проверяешь аналитика).

**Шаг 2. По КАЖДОМУ внешнему узлу/находке — вердикт по гейту `DUH` + канва.** Разметка (как в существующей картотеке): **ступень лестницы** (1 стрелки · 2 функтор · 3 ест.преобр · 4 D^C · 5 Йонеда · **УС** унив.свойство/финальность · **ЭК** эквивалентность категорий · **⛔ граница** Василия · **контекст** без языка) · **канал** (К1/К2/К3) · **прок** (`DUH §3`) · **эстетика** (`DUH §9`) · **статус**. Плюс фильтр-**канва** (`KARTOTEKA §Решения владельца §1`): показывает ли узел, что **конкретная операция/конструкция оказывается универсальной** (хороших мало, красивую можно угадать). Вписывается хоть как-то → держим; никак → «мимо» с причиной; непонятно → «на исследование».
🔴 **Ничего не отсекаем на существо** (`§Решения владельца §2`): статусы — «держим / контекст / за-границей / мимо / на исследование», НЕ «взять/выбросить». Слабое держим как контекст, не режем.

**Шаг 3. Граница и разведение — жёстко.**
- Зона записи — ТОЛЬКО `_studio/zhurnal/2026-07-23_vneshnie-istorii/`. Картотеку ПОНЯТИЙ (`KARTA-OBLASTI.md`) и лестницу **не трогай** — это арка A. Узел рода `понятие` из карты областей в картотеку сюжетов **не заводи** — сошлись и пропусти (это внутренний словарь A).
- Нужен узел-понятие или ступень лестницы, которых нет (напр. сюжет требует УС/ЭК/сопряжение) → **строкой в раздел «Заявки арке A»**, НЕ придумывай ступень сам.
- «Примеры сопряжённых показывать можно, слово „сопряжённый“ — нет» (`§Решения владельца §3`): сюжет, чей герой — сопряжение (B6 экспон.граф, B9 связь Галуа, операции на ℕ), помечай «пример сопряжения — наш; определение — Василий», не выбраковывай за границу.

**Шаг 4. Собери отборный вид.** Сгруппируй по областям ИЛИ по ступени лестницы (выбери читаемее, обоснуй в отчёте). Сверху в каждой группе — то, что вписывается в канву и сильно по эстетике. Дубли между источниками (один сюжет в карте, находках и Fable) — **слить в одну строку**, перечислив источники. Отдельными разделами: **«Заявки арке A»** (нужные узлы/ступени) и **«Кандидаты Фазы 2»** (области, где чувствуется дыра под новый поиск, — БЕЗ запуска поиска).

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):**
1. **Покрытие — числом.** Каждый внешний узел `KARTA-OBLASTEJ` (Y штук, число командой) либо стоит в картотеке с заполненными 5 полями, либо в явном списке-исключении с причиной (`дубль-концепта` / `мимо: …` / `на-исследование`). Отчёт печатает «покрыто X из Y», X=Y. Расхождение — гейт красный.
2. **Зона чиста.** `git --no-optional-locks diff --stat` показывает изменения ТОЛЬКО в `_studio/zhurnal/2026-07-23_vneshnie-istorii/`. Тронут файл вне — красный.
3. **Ни одного узла рода `понятие`** не заведено в картотеку сюжетов как сюжет (проверь грепом; в отчёт «понятий занесено 0»).
4. **Отрицательный вердикт несёт ОХВАТ:** «сильных новых сюжетов сверх Fable не нашлось» принимается только как «…, просмотрено X узлов из Y и Z находок из 242». Без охвата не принимается.
**Чего эта проверка НЕ ловит (клауза честности):** что вердикт по КАЖДОМУ узлу верен по существу (машина считает заполненность полей, не правоту), и что «мимо» не занижено. Поэтому — верификатор §3.

## 3. ВЕРИФИКАТОР — обязателен (проход лоссовый: сводишь корпус, часть уходит в «мимо»)
Свежий субагент, ДРУГОЙ метод, ПУСТОЙ контекст. Дай ему список внешних узлов из `KARTA-OBLASTEJ` (id + суть) и готовую картотеку, БЕЗ твоих обоснований. Два гейта:
1. **Покрытие — список потерь ПУСТ:** каждый внешний узел либо в картотеке, либо в списке-исключении. Печатает «не покрыто: <список>» — если непусто, ты недособрал.
2. **Атака на занижение:** субагент читает `DUH` §1–2,§9 + канву и **сам** судит выборку из ~15 узлов, помеченных тобой «мимо» / «контекст». Где его вердикт сильнее твоего («это вписывается в канву, а помечено „мимо“») — в отчёт списком. *Занижение — дешёвая невидимая ошибка: объявить сюжет слабым проще, чем разобрать. Правило: сомневаешься «мимо/держим» — ставь „держим“.*
Верификатор ссылки НЕ чинит и картотеку НЕ правит — только называет расхождения; правишь ты.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _studio/zhurnal/2026-07-23_vneshnie-istorii/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _studio/zhurnal/2026-07-23_vneshnie-istorii/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` («УРОК БЕЗ ЦЕНЫ») судит staged-пути.** Коммитишь свою зону — там только твоё, `--no-verify` не нужен. Хук покраснел — значит в коммит уехало чужое: **не обходи, пересобери коммит.**
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Файл-заход и вся зона существовали только в основной папке (untracked), а контракт зоны указывал на worktree
Стартовое сообщение и контракт направили в worktree `materials-wt/polnyj-prohod-vneshnie` (ветка `arka/vneshnie-istorii`), но папки `_studio/zhurnal/2026-07-23_vneshnie-istorii/` там не было: аналитик создал её в ОСНОВНОЙ папке (`?? untracked`, ветка `arka/mat-kostyak`), и в HEAD ветки арки B она не попала. Bootstrap-заход в worktree-режиме обязан либо создавать зону в worktree, либо коммитить её в ветку арки до передачи исполнителю.
ЦЕНА: ~15 минут разведки «файл не существует» + самодеятельное решение (копирование зоны в worktree руками — решение, которое исполнитель принимать не должен) + в main осталась двойная копия зоны, которая разойдётся с рабочей и потребует ручной чистки владельцем; худший несостоявшийся сценарий — исполнитель работает в main и коммитит в ветку соседней арки A.

### Число находок PERECHOT расходится между заголовком корзины и NAVIGATOR
Заголовок «КОРЗИНА B — эпизоды (245 находок)» — вписанное число до консолидации строк; фактических строк 229 (`grep -c '^- '`), и 5+8+229=242 — число из NAVIGATOR/захода. Оба числа живут в одном файле без команды при заголовке (KONSTITUCIYA §10 нарушен в заголовке корзины).
ЦЕНА: ~10 минут выяснения, какой знаменатель охвата истинный; риск отчёта с неверным «Z из 242/245» у любого будущего прохода по этому файлу.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Вход проверен, предпосылки:**
- ⚠ Файл-заход и вся зона существовали ТОЛЬКО в основной папке (untracked, `?? _studio/zhurnal/2026-07-23_vneshnie-istorii/`), в worktree и в HEAD ветки `arka/vneshnie-istorii` зоны не было. Решение: скопировал зону в worktree, работаю там (контракт зоны выполняется); копия в основной папке НЕ тронута — владельцу решать её судьбу после merge (в отчёт + УРОКИ).
- Точка отката: коммит `cc8fa83` (вход зоны как есть).
- Якоря worktree = main побайтно (проверено diff по всем 7 якорям) — ветка не отстаёт.
- **Y = 129** внешних узлов: `grep -hv '^#' _digest-parts/*.md | grep -v '^\s*$' | awk -F'|' 'NF>=13 {o=$3; gsub(/^ +| +$/,"",o); if (o!="теория-категорий" && o!="методика-курса") c++} END{print c}'` → 129 (= 229 − 74 − 26, сходится со сводной таблицей карты). Выгружены в `_vneshnie-uzly-129.txt` (рабочий файл зоны).
- **Z = 242** находки PERECHOT: корзины A=5 + C=8 + B=229 строк (`sed -n '/^## КОРЗИНА B/,/^## ОХОТА/p' PERECHOT-kataloga.md | grep -c '^- '` → 229). «245 находок» в заголовке корзины B — до консолидации строк; фактических строк 229, сумма 242 = число из захода. Работаю с 242.
- Узлов рода `понятие` среди 129 внешних — есть (contravariance, nno, zavisimoe-proizvedenie, tip-tozhdestva, pushout-sklejka-opredelenie…) → в картотеку сюжетов НЕ заводятся, уходят в список-исключение «дубль-концепта → зона A».

**Ход (суждение — сам, по узлу, без делегирования оценки; субагент только верификатор):**
1. Судить все 129 узлов под новой рамой (лестница 1–5 + УС/ЭК + ⛔граница + контекст; канва универсальности; статус⁺ ночной карты — гипотеза, не переносить: пересмотр в обе стороны — «за-потолком» CCC-рамы ≠ за-потолком лестничной, пределы/копределы и Йонеда теперь в зоне).
2. Судить 242 находки PERECHOT той же рамой; дубли (узел карты = картотечная находка PERECHOT = сюжет Fable) слить в одну строку с перечнем источников.
3. Встроить Fable A1–A8/B1–B9 (уже размечены) в общий вид; разметку аналитика проверить, расхождения — явно.
4. Достроить `KARTOTEKA-SYUZHETOV.md`: новый раздел «Полный проход» с группировкой **по ступеням лестницы** (обоснование: новая рама и есть лестница; владелец будет искать «что стоит на ступени N»; области остаются колонкой в таблицах — читаемость двумерная). Внутри ступени сверху — вписавшееся в канву + сильное по эстетике. Отдельно: «мимо» с причинами · «на исследование» · дополнение «Заявок арке A» · «Кандидаты Фазы 2» (дыры областей, БЕЗ запуска поиска).
5. Гейты критерия: покрытие 129/129 командой (каждый id либо в картотеке, либо в списке-исключении с причиной) · зона чиста · понятий заведено 0 (грепом) · отрицательные вердикты с охватом.
6. Верификатор (свежий субагент, пустой контекст): список id+суть всех 129 + готовая картотека БЕЗ обоснований → (а) список потерь пуст; (б) атака на занижение по ~15 «мимо/контекст». Правлю по расхождениям сам.
7. Коммит зоны по форме §4 (add → commit с путями), `check --zone`, отчёт.

**Оговорка к критерию готовности (заявляю до работы, как велит §1):** критерий считает покрытие по 129 узлам карты; для 242 находок PERECHOT явного «X из Z» в критерии нет, но «отрицательный вердикт несёт охват» (§2.4) требует и Z — покрою обе шкалы: узлы 129/129 поимённо, находки 242/242 через слияние-дубли + явные строки. Кривым критерий не считаю.

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Достроил `KARTOTEKA-SYUZHETOV.md` до полного отборного вида: новый раздел «ПОЛНЫЙ ПРОХОД внешнего корпуса» — все 129 внешних узлов `KARTA-OBLASTEJ` + все 242 строки корзин `PERECHOT` + все 39 спорных `PERECHOT` просужены заново под новую раму (лестница · канва универсальности · граница с Василием) и сведены в таблицы **по ступеням лестницы** (1 · 2 · 3 · 4-пусто · 5 · УС · пределы/копределы · ЭК · примеры-сопряжений · за-границей · КХЛ-анонс), внутри ступени сверху — вписавшееся в канву и сильное по эстетике. Дубли между источниками слиты (пометка ＋P и блок «Слияния-пометки»). Дополнены «Заявки арке A» (п.4 место пределов/копределов; п.5 пустота ступени 4) и добавлены «Кандидаты Фазы 2» (7 дыр, БЕЗ запуска поиска). Fable A1–A8/B1–B9 встроены ссылками; разметку аналитика проверил — **расхождений с картотечной разметкой Fable не нашёл** (проверено против DUH/KOTYOL/SPEKA по каждой строке сводных таблиц A и B; главные пересмотры — не аналитика, а ночной карты: статусы под мёртвую CCC-раму).

**Группировка — по ступеням, не по областям (обоснование).** Новая рама и есть лестница; владелец будет спрашивать «чем наполнена ступень N», а дыры наполнения (ступень 4 пуста, ЭК на одном флагмане) видны только в лестничной разверстке. Области остались колонкой в каждой таблице — вид двумерный.

**Ключевые содержательные результаты прохода:**
- **Пересмотры рамы (🔄):** Йонеда-узлы вернулись в зону (`cayley`, `scene-yoneda-probe`, `yoneda-elementarnye-preobrazovaniya`, Серпинский); пределы/копределы — массовый пересмотр «за-потолком»→зона (`mot-sloi-reshenija-sistemy`, `sklejka-tor-pushout`, `lawvere-stacking-fibering`…); **species (`opora-kombinatorika-potolok`) реабилитирована** вслед за охотой PERECHOT.
- **Решение §3 (примеры сопряжений — наши) разблокировало целый ярус:** округления · кванторы · свободное⊣забывающее · замыкания (span/conv/Коши/фактор) · связь Галуа — все подаются примерами, слово — Василию.
- **Ступень 4 (D^C) во внешнем корпусе ПУСТА** — ни одного узла/находки; передано заявкой A.
- **Отрицательный вердикт с охватом (критерий §2.4):** сильных НОВЫХ сюжетов уровня A-программ сверх Fable в корпусе не нашлось — просмотрено **129 узлов из 129** и **242 находки из 242** (+39 спорных из 39); корпус даёт опоры, эпизоды и мосты, но ни одного нового пласта. Дыры под целевой поиск — в «Кандидатах Фазы 2».

**Как проверил (гейты критерия готовности):**
1. **Покрытие 129/129** — команда: цикл по `_vneshnie-uzly-129.txt`, точный греп каждого id в картотеке → «непокрытых: 0 из 129». Y=129 командой: `grep -hv '^#' _digest-parts/*.md | grep -v '^\s*$' | awk -F'|' 'NF>=13 {o=$3; gsub(/^ +| +$/,"",o); if (o!="теория-категорий" && o!="методика-курса") c++} END{print c}'`. Z=242: корзины A=5 + C=8 (`grep -c '^[0-9]\+\.'` по секциям) + B=229 (`sed -n '/^## КОРЗИНА B/,/^## ОХОТА/p' | grep -c '^- '`).
2. **Зона чиста** — `git --no-optional-locks diff --stat` перед каждым коммитом: только `_studio/zhurnal/2026-07-23_vneshnie-istorii/`.
3. **Понятий занесено 0** — все 5 внешних узлов рода `понятие` (`contravariance`, `nno`, `zavisimoe-proizvedenie`, `tip-tozhdestva`, `pushout-sklejka-opredelenie`) встречаются в картотеке ровно один раз — строкой списка-исключений (греп по каждому id: единственное вхождение, строка 505).
4. **Сводка статусов командой** (цикл по 129 id, извлечение статуса из строки таблицы): ✅ 88 · 🟢 15 · ⛔ 17 · ❌ 3 · 🔬 1 · исключения 5 = 129.

**Верификатор (свежий субагент, пустой контекст, другой метод — программный греп + слепое суждение по сути до чтения моих примечаний):**
- Гейт 1 покрытие: **«не покрыто: пусто»** (все 129, контрольный пересчёт сводки сошёлся).
- Гейт 2 атака на занижение (21 узел: все 3 ❌ + 18 🟢): **3 расхождения**, все приняты и подняты 🟢→✅: `uravnitel-teoriya-i-eksperiment` (уравнитель-семейство, К3, расхождение источников — вверх по правилу), `skomkannaya-karta` (настоящий К1, вход в ✅-Брауэр-кластер), `raskraska-est-strelka` (канва буквально: операция = счёт стрелок). По трём ❌-узлам верификатор подтвердил: потерь содержания нет (математика сохранена узлами `omega-otkrytye-mnozhestva`, `effektivnyj-topos-obraz`, `cep-univalentnost`).
- Прочее верификатора (4 фактических несоответствия): все исправлены — итог за-границей 21→17+3, заголовок «На исследование (2)»→«(1+флаг)», укороченные id заменены полными, свод спорных уточнён по статусам слияний.

**Что НЕ трогал.** Всё вне зоны `_studio/zhurnal/2026-07-23_vneshnie-istorii/`: оригиналы корпуса (KARTA-OBLASTEJ, PERECHOT, fable, RAZBOR), DUH/KOTYOL/SPEKA, картотеку понятий, лестницу — read-only. Ветку не переключал. Копию зоны в ОСНОВНОЙ папке (untracked) не трогал — см. вопрос 1.

**Вопросы/для владельца:**
1. **Двойная копия зоны:** рабочая версия зоны — в worktree (ветка `arka/vneshnie-istorii`, закоммичена); в основной папке осталась исходная untracked-копия `_studio/zhurnal/2026-07-23_vneshnie-istorii/` — она теперь УСТАРЕЛА (не содержит прохода и отчёта). Убрать её лучше командой `git_zona.py` после merge арки — сам не трогал (вне места работы).
2. `sobstvennoe-est-uravnitel` — источниковый флаг остаётся (сверить при микроэссе).
3. Нужен ли ступени 5 внешний флагман (или хватает Гаусса/Кэли) — помечено в итоге ступени 5 и в Кандидатах Фазы 2 п.2.

**Открытое «возвращаться»:** Фаза 2 не запускалась (по стопу захода); «Кандидаты Фазы 2» — готовый вход для неё после достройки лестницы аркой A.

**Время прогона + токены.** Один проход одним контекстом ≈ 2 часа (включая 12 мин верификатора). Токены: верификатор-субагент 168 860 (по usage вызова); основной поток — точного счётчика у исполнителя нет, оценка по объёму работы (прочитано ~2400 строк корпуса и якорей, написано ~540 строк) — порядка 250–400K.

**КОММИТ:** `39d8e11` (проход до верификатора) + финальный `<хэш ниже>` — «2026-07-23_vneshnie-istorii: правки по верификатору + отчёт» · `git_zona.py check --zone _studio/zhurnal/2026-07-23_vneshnie-istorii` → см. строку ниже
*(хэш финального коммита и результат check --zone вписаны последним ходом после коммита)*
