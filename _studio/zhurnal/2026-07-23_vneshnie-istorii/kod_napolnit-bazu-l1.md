# Канал исполнителя — napolnit-bazu-l1 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — суждение: что картировать из книги/MO/скана и как не наплодить дублей среди 265 узлов — тяжёлое решение по ходу, механики нет.

КОНТЕКСТ. Курс «Введение в теорию категорий»; первая лекция (доступное научпоп-начало) строится вокруг рамки «что значит красивая/каноническая конструкция — и как понять, что её НЕ существует». Аппарат Л1 зафиксирован владельцем: **{категория, функтор, естественное преобразование / эквивалентность функторов, эквивалентность категорий}** — универсальные свойства, пределы, представимость/Йонеда в Л1 НЕ входят (это Л2). Прошлый этап: собрана картотека понятий (`teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md`, ~255 узлов) и библиотека источников; консолидация встречи с Василием 22.07 завела 10 карточек, но БАНАЛЬНАЯ БАЗА под аппарат Л1 неполна (каноничные изоморфизмы, примеры функторов/не-функторов, классификация ЕП руками — разрозненны или отсутствуют). ЦЕЛЬ: вычитать названные источники и добрать из книг/сети, наполнив картотеку этой базой.
Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 СТОП ДО ЦЕЛИ: получишь карточки + дайджесты источников, но **НЕ** правки манифеста/SPEKA/KOTYOL, **НЕ** заход Fable (нестандартный портфель — отдельно), **НЕ** отбор в лекцию — это ходы аналитика ПОСЛЕ приёмки.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `arka/mat-kostyak` в основной папке репозитория — `git checkout arka/mat-kostyak` первым ходом. Ветку НЕ переключай дальше, в другие НЕ коммить.
- **ЗОНА (можно менять):** teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md teoriya-kategoriy/istochniki _studio/zhurnal/2026-07-23_vneshnie-istorii/kod_napolnit-bazu-l1.md. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
- `git checkout arka/mat-kostyak`.
- Точка отката: `git add teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md teoriya-kategoriy/istochniki _studio/zhurnal/2026-07-23_vneshnie-istorii/kod_napolnit-bazu-l1.md` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти якоря (проект дальше не изучай):
  1. `teoriya-kategoriy/kartoteka/STANDART-uzla.md` — контракт формата карточки (строго по нему).
  2. `teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md` — существующая картотека (~255 узлов). НЕ дубли: У57 (Sym/Ord), У22 (A≅T(A)⊕A/T(A)) и волна `vasily-vstrecha` (10 карточек: `mo-bank-neestestvennost`, `centr-gruppy-ne-funktor`, `nat-preobr-rukami`, `zoopark-ekvivalentnostej`, `delenie-na-3-konvej-dojl`, `net-retrakcii-d2-s1` и др.) УЖЕ есть — на них ссылайся, не заводи заново.
  3. `teoriya-kategoriy/istochniki/VYCHITANO.md` + `istochniki/MANIFEST.md` — форматы дайджеста и строки источника (по образцу).
  4. `teoriya-kategoriy/istochniki/vstrechi/2026-07-22-vasily/ОТЧЁТ-для-проекта-2026-07-23.md` — что уже решено/картировано на встрече (чтобы не повторять).
  5. `_studio/zhurnal/2026-07-23_vneshnie-istorii/MANIFEST-zachem-kategorii.md` — аппарат Л1 живёт в группах I–II; тезис-проба «Проба вместо устройства». Это рамка отбора, что банально и что мимо.
- Проект дальше не изучай — эти пять якорей самоценны.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

## 2. ЗАДАЧА
Наполнить картотеку внешних утверждений банальной базой под аппарат Л1. По шагам:
1. **`baez-euler`** (`teoriya-kategoriy/istochniki/pdf/baez-euler-characteristic-vs-homotopy-cardinality.pdf`) — ⛔ скан рукописи, текстового слоя НЕТ: читать ГЛАЗАМИ постранично (стр. 1–4: ℕ⇝FinSet, «законы арифметики = естественные изоморфизмы», деление на 3 Конвея–Дойла). Дополнить дайджест VYCHITANO (id `baez-euler`); завести карточки на каноничные изоморфизмы, если не дублируют.
2. **Joy of Cats** (`teoriya-kategoriy/istochniki/pdf/adamek-herrlich-strecker-joy-of-cats.pdf`, Adámek–Herrlich–Strecker, 524 стр., текстовый слой ЕСТЬ — grep работает) — рудник примеров категорий, функторов, НЕ-функторов, эквивалентностей. Зарегистрировать строкой в `istochniki/MANIFEST.md` + дайджест в VYCHITANO. Завести карточки на школьно/доступные примеры под аппарат Л1; моноидальное/топосное/выше потолка НЕ тащить.
3. **MathOverflow 139388** «Example of an unnatural isomorphism» — сверить банк по первоисточнику (в карточке `mo-bank-neestestvennost` он НЕ сверён: web_fetch у Cowork заблокирован, у тебя host-side — попробуй; не выйдет — так и запиши). Уточнить голоса/формулировки (Sym/Ord, универсальные коэффициенты, A≅T(A)⊕A/T(A), G-множества; спасение V≅V* через группоид Vect_iso). Положить пост в `istochniki/web/`; пройтись по смежным ссылкам MO про естественные преобразования/функториальные конструкции, добрать примеры.
4. **Добор базы**, если её нет в картотеке: каноничные изоморфизмы (образ≅G/ker и родня — абсолютная база, «должна быть везде»), примеры функторов/не-функторов в знакомых категориях (Википедия/книги корпуса при нехватке), конкретные классификации ЕП руками. Портфель по областям: теормн, комбинаторика, логика, линал, группы, матанализ, теорчисла, вероятность. ⛔ Топология/дифгео НЕ трогать — отдельный заход Fable.
5. **Русская лекция 1** (`teoriya-kategoriy/istochniki/pdf/ru-lekciya-1-kategorii-funktory.pdf`, «Лекция 1. Категории и функторы», 15 стр.; ⚠ кириллический текстовый слой БИТЫЙ (кастомная кодировка шрифта — и `pdftotext`, и `-enc UTF-8` дают мохито `Ìàëûå`=«Малые»; grep НЕ работает): читать ГЛАЗАМИ постранично (Read по страницам) или OCR; автор/источник НЕИЗВЕСТЕН — отметить в MANIFEST «уточнить у владельца»). Владелец ценит её язык (мотивированный, интересный) и просит взять оттуда: **примеры категорий, которые мы пропустили** — в т.ч. категория из диаграмм Фейнмана (необычный, но валидный пример) и ещё один необычный пример в начале; **хорошие примеры функторов**; **упражнения в конце** (пометить, какие годятся к разбору на нашей Л1). Зарегистрировать в `istochniki/MANIFEST.md` + дайджест VYCHITANO; карточки — на примеры категорий/функторов и на сильные упражнения под аппарат Л1.
Карточки — строго по `STANDART-uzla.md` (род/находка/связи/уровень/источник/статус), провенанс на источник, спорное помечать, а не додумывать. Пустой/натянутый узел не фабриковать.
**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):** `python3 _generator/tools/check_kartoteka.py teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md` → **rc=0**, 0 сирот и битых рёбер; каждый из 5 источников (baez-euler, Joy of Cats, MO 139388, добор, русская лекция 1) имеет дайджест в VYCHITANO ЛИБО строку «прочитано, полезного нет»; ни одной карточки без источника; дублей с существующими узлами нет (проверить грепом id перед заводом). **Вердикт несёт ОХВАТ:** не «добавил примеры», а «функторов/не-функторов +N, каноничных изо +M, проверено K глав Joy of Cats из всех».
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Операция АДДИТИВНАЯ (добавляем карточки/дайджесты, ничего не двигаем и не теряем) — отдельный верификатор не нужен, хватает самопроверки критерия §2 (валидатор rc=0, дубли грепом id, охват в вердикте). Единственный риск потери — правка существующих карточек «заодно»: НЕ делать, чужие узлы read-only на содержание (можно только дописать ребро-связь к своему новому узлу).

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md teoriya-kategoriy/istochniki _studio/zhurnal/2026-07-23_vneshnie-istorii/kod_napolnit-bazu-l1.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- teoriya-kategoriy/kartoteka/KARTA-OBLASTI.md teoriya-kategoriy/istochniki _studio/zhurnal/2026-07-23_vneshnie-istorii/kod_napolnit-bazu-l1.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` («УРОК БЕЗ ЦЕНЫ») судит staged-пути.** Коммитишь свою зону — там только твоё, `--no-verify` не нужен. Хук покраснел — значит в коммит уехало чужое: **не обходи, пересобери коммит.**
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены** / **КОММИТ (строка обязательна, см. §4)**.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Прочитаны 5 якорей + валидатор + baseline.** Зона содержательно чиста в HEAD (untracked только сам заход) → точку отката не фабрикую (§0). Baseline валидатора: **rc=0, 255 карточек, граф замкнут** — наследую чистое, операция аддитивная, обязан сохранить rc=0.

**Развилки/предпосылки вслух:**
1. **`У57`/`У22` — кириллические id, валидатор их НЕ видит** (`индекс = [A-Za-z0-9_-]{2,}`). В поле `связи` класть НЕЛЬЗЯ (висячее ребро → rc=1). Ссылка на них и на MAT-KOSTYAK — только прозой в `находка`/`источник` (как уже сделано в `mo-bank-neestestvennost`). MAT-KOSTYAK.md физически в смежном `teorkat-vvedenie/` — вне зоны, не трогаю.
2. **baez-euler дайджест УЖЕ полный** (вычитан целиком сегодня 23.07, VYCHITANO стр.712–725). §2.1 «дополнить дайджест» по факту избыточен — работа по нему = проверить/завести карточки каноничных изо, не перечитывать PDF. `delenie-na-3-konvej-dojl` уже есть (волна vasily); проверю биномиальный коэффициент/дистрибутивность.
3. **Главный реальный пробел §4 — каноничные изоморфизмы (образ≅G/ker и родня): в картотеке ИХ НЕТ** (грепом подтверждено: есть только `abelianization` и фактор-как-коуравнитель). Это ядро добора. Портфель по областям: группы (1-я теорема об изоморфизме), линал (rank-nullity / V/ker≅im), теормн (орбита-стабилизатор |Gx|=|G|/|Stab|), кольца. Топологию/дифгео НЕ трогаю (Fable).
4. Примеры функторов/не-функторов/ЕП/эквивалентностей УЖЕ богаты (ЗОНА B + жемчужины + vasily) — из Joy of Cats/русской лекции беру только то, что НЕ дублирует; порог — «школьно-доступно под аппарат Л1, офигеваешь».

**Ход (аддитивно, метрика — качество):**
- **A. Разведка источников — 3 субагента параллельно** (калибровка: разведка на sonnet, суждение о дублях/картировании — моё). Субагенты только ЧИТАЮТ и возвращают находки с адресами; в зону НЕ пишут (нет параллельных правок → нет конфликта).
  - S1 Joy of Cats (текст.слой есть, pdftotext+grep): примеры категорий/функторов/НЕ-функторов/эквивалентностей под Л1, школьные; адреса+формулировки+дубль-флаги.
  - S2 русская лекция 1 (mojibake cp1251, iconv ИЛИ Read глазами): 2 необычных примера категорий (Фейнман+ещё), функторы, упражнения в конце; автор/источник; уровень.
  - S3 MO 139388 (host-side WebFetch/браузер): сверить голоса/формулировки банка (Sym/Ord, унив.коэфф, A≅T⊕A/T, G-множества, спасение V≅V*), добрать смежные; вернуть текст поста для `web/`.
- **B. Пока идёт разведка — регистрирую источники** (Joy of Cats, русская лекция) строкой в `MANIFEST.md`; готовлю карточки каноничных изоморфизмов (моя математика, провенанс — Joy of Cats/Маклейн-рус/Википедия).
- **C. Картирую** (моё суждение): по находкам S1–S3 завожу карточки (дубль-греп id перед каждой), пишу дайджесты Joy of Cats + русская лекция в `VYCHITANO.md`, кладу MO-пост в `web/`.
- **D. Гейт:** `check_kartoteka.py` → rc=0, 0 сирот/битых/дублей; каждая карточка с источником; вердикт с ОХВАТОМ (функторов/не-функторов +N, каноничных изо +M, K глав Joy of Cats из всех).
- **E. Коммит зоны** сам: `add -- <пути>` → `commit -- <пути>` (`--no-optional-locks`) → `git_zona.py check` → `show --stat` только мои пути.

**Критерий §2 не оспариваю — он реальный гейт, не кривой.** Единственная поправка семантики: baez-euler дайджест уже есть (п.2 выше), в охват идёт как «вычитан, карточки проверены/добраны».

## ВОПРОСЫ — (заполняет исполнитель)

1. **Автор/источник русской лекции 1 (`ru-lekciya-1`) не назван в файле** — pdfinfo пуст (Author пусто, LaTeX/pdfTeX 13.02.2012), библиография Маклейн/Борсо/Adámek–Herrlich–Strecker/Johnstone/Kelly/nLab, фраза «в следующих лекциях» ⇒ лекция 1 многолекционного курса. Чей конспект? В MANIFEST помечено «уточнить у владельца». Владелец знает?
2. **baez-euler (§2.1): дайджест уже был ПОЛНЫМ** — `baez-euler` вычитан целиком 23.07 (VYCHITANO стр.712–725, образцовый дайджест). Каноничные изо со стр.1–4 уже в картотеке: `delenie-na-3-konvej-dojl` (волна vasily), дистрибутивность = `distributivnost-ccc`, категорификация = `kategorifikaciya-arifmetiki`. Отдельных baez-карточек НЕ заводил (были бы дубли). Считаю §2.1 закрытым проверкой, а не перечиткой PDF — подтвердите, что это верное прочтение (а не «недобрал»).
3. Все 18 новых карточек — `seed` (на приборе не гонялись, отбор мой). Поднять до `черновика` может источник/прибор. Каноничные изо (первые 3) — провенанс Википедия: Joy of Cats проверен грепом по 524 стр., этих классических теорем в нём НЕТ; при желании усилить адресом из учебника — скажите.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + ЗАЧЕМ.** Наполнил картотеку внешних утверждений банальной базой под аппарат Л1 (§2 «наполнить банальной базой»). Аддитивно: **+18 карточек (255→273)**, 4 источника зарегистрированы/сверены, ни один существующий узел не тронут по содержанию.
- **Каноничные изоморфизмы +3** (главный пробел §4, в картотеке их НЕ было): `pervaya-teorema-izomorfizma` (образ≅G/ker — «абсолютная база»), `rank-nullity-kanon-izo` (линал, опора зала), `orbita-stabilizator-kanon` (теормн). Ось: изоморфизм БЕЗ выбора — контраст к неестественному V≅V*.
- **Портфель неканоничности +5** (MO 139388, **сверено по первоисточнику**): `funktor-shlopyvaet-minimalno` (Rickard 50↑), `ciklicheskie-gruppy-bez-kanona` (группы), `veshch-x2plus1-izo-c` (кольца), `affinnoe-vs-vektornoe-neestestvenno` (линал), `konechnaya-abeleva-haraktery` (Понтрягин). Покрыты области группы/кольца/линал/теормн.
- **Joy of Cats +6**: `umnozhenie-est-est-preobr` («гомоморфизм = квадрат естественности»), `centr-est-est-preobr-id` (пара к `centr-gruppy-ne-funktor`), `ekvivalentnost-cherez-nat-izo`, `mat-ekvivalentna-ne-izomorfna`, `biekciya-ne-izo-v-rel` (без топологии), `boorng-izo-boo`.
- **Русская лекция 1 +4**: `kategoriya-fejnmana` (заказ владельца), `kategoriya-algoritmov-sostoyanij` (программистам), `obekty-i-diagrammy-kak-funktory`, `uprazhneniya-ru-l1` (банк упражнений под Л1).
- **Источники:** Joy of Cats и русская лекция — строки в `MANIFEST.md` + дайджесты в `VYCHITANO.md` (обоих не было). MO-пост + 20 цитат — `web/mo-139388-unnatural-isomorphism.md` (браузером; WebFetch на mathoverflow заблокирован) + строка MANIFEST + дайджест; долг «сверить MO» снят в записи `vstrecha-vasily` (все 5 пунктов подтверждены дословно).

**Вердикт с ОХВАТОМ.** Функторы/ЕП руками +5 · каноничные изоморфизмы +3 · сверенных по первоисточнику неканоничных +5 · эквивалентности/изоморфизмы категорий +4 · примеры категорий +2. Joy of Cats: детально Глава I §3–6 (~100 из 524 стр.) + grep по всем 524 (жанра «не функтор» и классических изо-теорем в книге НЕТ — 0 хитов). Русская лекция: все 15 стр. (iconv-декод, 99.9%). MO: 13 ответов 139388 + 7 из связанных, банк 100% подтверждён.

**Как проверил.** `check_kartoteka.py` → **rc=0, 273 карточки, 0 висячих рёбер / 0 сирот / 0 дублей** (baseline был 255, тоже rc=0 — унаследовал чистое, сохранил). Все 18 id проверены грепом на дубли ДО завода; все 26 target-связей резолвятся; оба новых pdf покрыты дайджестом (команда покрытия из VYCHITANO §шапка).

**Что НЕ трогал** (§7 🔴 СТОП + §3): содержание чужих карточек (`mo-bank-neestestvennost` и др. — только ссылки прозой + рёбра из своих новых узлов); `../../teorkat-vvedenie/MAT-KOSTYAK.md` (У-номера, вне зоны); SPEKA/манифест-zachem/KOTYOL; заход Fable (топология/дифгео — кобордизмы/Фейнман только НАЗВАЛ, не разворачивал); отбор в лекцию. Ветку не переключал.

**Открытое «возвращаться»:** автор русской лекции (вопрос 1); все карточки `seed` — калибровка на приборе; провенанс каноничных изо — Википедия (можно усилить учебником).

**Время/токены.** ~1 прогон Opus + 3 субагента sonnet параллельно (MO ~7½ мин / рус ~10 мин / Joy ~13 мин; суммарно ~456k subagent-токенов). Точные цифры прогона — в телеметрии сессии.

**КОММИТ:** сделан этим ходом (см. §4) — сообщение `teoriya-kategoriy: банальная база Л1 — +18 карточек (каноничные изо, MO-портфель, Joy of Cats, русская лекция) + 3 дайджеста + MO в web/`; хэш — в git-логе и в чате оркестратору (хэш коммита не может лежать внутри самого коммитируемого файла). `git_zona.py check` → см. вывод ниже.
*(строка непустая: хэш присваивается этим коммитом, приведён в чате/логе — файл-отчёт входит в тот же коммит)*
