# Канал исполнителя — zapret (один заход до конца)
<!-- собран bootstrap_zahod.py -->
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — один новый чекер по готовому образцу, одна строка в хуке, калибровка названа поимённо шестью файлами; развилок нет.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-11_dveri-discipliny» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/modest-pensive-bardeen/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Репозиторий `materials` — фабрика курсов. Аналитик (Cowork) пишет заходы и хэндоффы; исполнитель — Claude Code. За одну сессию трижды нарушилась одна дисциплина: документ создавался РУКАМИ мимо генератора. Предыдущий заход `kod_dveri` (влить его первым ходом, §0.1) сделал половину лечения: генераторы теперь требуют интервью и отсечку дневника, а `bootstrap_zahod.py` ставит в собранный файл маркер `<!-- собран bootstrap_zahod.py -->` — такой же, какой `sdelat_handoff.py` ставит с самого начала. **Маркер есть, но на его отсутствие пока не краснеет ничто.**

ЦЕЛЬ: заставить `pre-commit` краснеть на файле-заходе или хэндоффе, который создан мимо генератора. После этого обойти генератор можно будет только напечатав `--no-verify` руками — то есть громко, а не молча.

🔴 **ЧТО ЗАПРЕЩАЕТСЯ, а что нет — не перепутай, это главная развилка задания.** Запрещается **создать файл-заход в обход генератора**. НЕ запрещается писать в него содержание руками — содержание всегда пишет аналитик, и это норма: генератор даёт КАРКАС (контракт зоны, блок §0.1 ГИТ-КОНТУР, раздел гигиены, секции ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ, маркер), а текст задачи, критерий и что читать вписываются потом руками в оставленные `<...>`. Маркер отличает «каркас родился из генератора» от «файл сочинён с нуля», и ничего больше. Чекер, который начнёт судить содержание, — неверен.

Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 **СТОП ДО ЦЕЛИ:** чекер судит только НОВЫЕ файлы коммита (`--diff-filter=A`). Двести сорок пять уже существующих `kod_*.md` маркера не несут и краснеть не будут — иначе гейт даст сотни ❌ на унаследованном долге, его обойдут `--no-verify`, и пропадёт вся защита. Это осознанная граница, а не недоделка; объяви её в докстринге чекера.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ (2026-08-10, владелец подтвердил)

*Секция вписана Cowork руками: генератор научится вшивать её только после влития З1, которое ты сделаешь первым ходом.*

1. **Заход руками запрещаем полностью, лазейку не проектируем.** Владелец: «зачем нам делать лазейку? Во всех ситуациях можно сделать универсальные заходы. Находя проблемы с заходами, систематически будем через генератор править». Громкий обход уже есть и его достаточно — `--no-verify`, который надо напечатать руками.
2. **Запрет — на СОЗДАНИЕ файла мимо генератора, не на ручное содержание.** Владелец проговорил это отдельно: «всё содержание будет вставлено, просто оно будет через генератор проводиться». Каркас — из генератора, текст — руками, это норма.
3. **Перенос ветки предыдущего захода делает ЭТОТ заход, не владелец.** Владелец: «я никогда сам не переношу, мы всегда делаем это в следующем заходе; в начале каждого захода мы подчищаем хвосты». Поэтому влитие стоит в §0.1 слотом `--vlit`, а не строкой в чат.
4. **После влития проверяется не только факт коммита, но и что инструменты встали на место** — тремя грепами в §0, по требованию владельца.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/zapret`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/zapret-rukopisnyh`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add zapret --branch zahod/zapret-rukopisnyh
  cd ../materials-wt/zapret
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/` `.githooks/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

```
python3 _generator/tools/git_zona.py merge zahod/dveri-discipliny --zone _generator/tools/ --zone .githooks/
```

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/dveri-discipliny`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- новый чекер в _generator/tools/, его фикстуру и строку вызова в .githooks/pre-commit

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- ветку zahod/dveri-discipliny после влития; свою — после приёмки

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/zapret-rukopisnyh`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/` `.githooks/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, в этом порядке. Проект не изучай:
  1. `.githooks/pre-commit` — весь. Это точка встройки; смотри, как устроены соседи: у каждого блока названа поломка, по которой он заведён, и каждый судит только тронутые коммитом пути.
  2. `_generator/tools/check_uroki.py` — образец простого чекера, которого зовёт хук: молчит, когда чисто; `exit 1` на красном.
  3. `_generator/tools/check_kartoteka.py` — образец двух вещей сразу: режима `--staged` (судит только пути коммита) и **честной печати того, чего чекер НЕ проверяет**. Твой докстринг обязан иметь такой же блок.
  4. `_generator/tools/sdelat_handoff.py`, строка ~125 — как выглядит константа `MARKER` и зачем она заведена (`--zamenit` берёт только свои файлы, рукописные не трогает ни при каких флагах).
  5. `_generator/tools/bootstrap_zahod.py` — **после влития З1** найди грепом, где вписывается маркер: `grep -n "собран bootstrap_zahod" _generator/tools/bootstrap_zahod.py`. Ноль совпадений = ветка не влита, СТОП.
  6. `_generator/tools/fixtures/uroki/PROGNAT.sh` — образец фикстуры чекера: своя пиши той же идиомой.
- 🔴 **После влития — проверить, что инструменты ВСТАЛИ НА МЕСТО, а не только что коммит виден.** Три грепа, все должны дать ≥1: `grep -c 'собран bootstrap_zahod' _generator/tools/bootstrap_zahod.py` · `grep -c 'finalizirovano' _generator/tools/sdelat_handoff.py` · `grep -c 'З11' _generator/tools/check_zahod.py`. Любой ноль — влитие не доехало, СТОП в `## ВОПРОСЫ`.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
Три шага.

### Ш1. Новый чекер `_generator/tools/check_marker.py`

**Кого судит.** Только `.md`, добавленные коммитом (`--diff-filter=A`), и только те, которые **объявляют себя заходом или хэндоффом**. Признак — ЛЮБОЙ из двух, и оба нужны, потому что по отдельности каждый дыряв:
- **по имени:** `kod_*.md` · `HANDOFF-*.md` · `PEREDACHA-*.md` · `ZAHOD-*.md`;
- **по содержимому первых ~30 строк:** заголовок `# Канал исполнителя —` · строка `## КОНТРАКТ ЗОНЫ` · заголовок `# ХЭНДОФФ`.

*Почему два признака, а не один: имя обходится переименованием (`PEREDACHA-…` вместо `HANDOFF-…` — ровно так и вышел один из трёх сегодняшних промахов), содержимое обходится тем, что рукописный файл может не копировать шапку. Вместе они закрывают обе дыры, и ложное срабатывание тут дешевле пропуска — на чужой `.md` чекер просто не посмотрит.*

**Что требует.** В первых **пяти** строках файла — ровно один из маркеров: `<!-- собран bootstrap_zahod.py -->` или `<!-- собран sdelat_handoff.py -->`. Нет → ❌, `exit 1`. Текст красного называет: путь файла, какой маркер ждали, и **чем чинить** — `bootstrap_zahod.py` / `sdelat_handoff.py`, с напоминанием, что содержание после генерации дописывается руками и это норма.

**Режим и молчание.** Флаг `--staged` (как у `check_kartoteka.py`). Чисто → молчит полностью, `rc=0`. *Гейт, который шумит зря, отключат — `KONSTITUCIYA` Р31.*

**Докстринг обязан честно назвать слепые зоны** (клауза 4, образец — `check_kartoteka.py`). Минимум три, они реальны:
1. судятся только НОВЫЕ файлы — 245 существующих `kod_*.md` без маркера пройдут молча всегда;
2. маркер подделывается печатью одной строки — чекер ловит забывчивость, а не злой умысел, и не должен читаться как защита от второго;
3. чекер не судит СОДЕРЖАНИЕ: заход с маркером и пустой задачей для него зелёный (содержание судят `check_zahod.py` и `check_sborki.py`).

### Ш2. Точка вызова в `.githooks/pre-commit`

Отдельным блоком, идиомой соседей: комментарий с поломкой, по которой блок заведён, затем вызов. Ставить **рядом с гейтом уроков** (в начале, до тяжёлых фикстур) — он копеечный.
```
python3 _generator/tools/check_marker.py --staged || exit 1
```
*Инструмент без живой точки вызова зелен ровно потому, что его никто не звал — это Г4 твоей гигиены и повод, по которому весь заход `instrument-podklyuchen` существовал.*

### Ш3. Фикстура `_generator/tools/fixtures/marker/PROGNAT.sh`

С шапкой `# TOOL-CONTRACT-COVERS: check_marker.py`. Ловушки — на калибровочном материале ниже плюс на «чисто → молчит» и на «чужой `.md` не судится».

---

**🔴 КАЛИБРОВКА — материал живой и уже оплаченный, не выдумывай свой.** Гейт, который не краснеет на первых трёх и не зеленеет на вторых трёх, — не гейт.

**Обязан ПОКРАСНЕТЬ (написаны руками мимо генератора, 2026-08-10):**
- `_studio/zhurnal/PEREDACHA-2026-08-10-vecher-blochnaya-model.md`
- `_studio/zhurnal/ZAHOD-2026-08-11-katalog-instrumentov.md`
- `_studio/zhurnal/ZAHOD-2026-08-11-edinyj-istochnik-metrik.md`

**Обязан ПРОМОЛЧАТЬ (собраны генератором, маркер несут):**
- `_studio/zhurnal/2026-08-10_dizajn-i-metriki/HANDOFF-2026-08-11.md`
- `_studio/zhurnal/2026-08-11_dveri-discipliny/kod_dveri.md`
- `_studio/zhurnal/2026-08-11_dveri-discipliny/kod_zapret.md` (этот файл)

⚠ **Про два последних — знай правду, иначе решишь, что нашёл подделку.** Оба РОДИЛИСЬ из `bootstrap_zahod.py`, но ДО того, как заход `dveri` научил генератор ставить маркер; строку `<!-- собран bootstrap_zahod.py -->` вписал в них аналитик руками 2026-08-10, разовой миграцией, и говорит об этом здесь вслух. Признак подлинности проверяем независимо от маркера: оба несут блок `### 0.1 🔴 ГИТ-КОНТУР` и раздел `## 4.1 🔴 ГИГИЕНА`, которые пишет только генератор. **Больше таких файлов нет и не будет** — всё, что собрано после влития З1, получает маркер само.

**Замеры «задача уже сделана?» — сняты командами 2026-08-10, дата данных та же:**
```
ls _generator/tools/check_marker.py            → нет такого файла
grep -c 'check_marker' .githooks/pre-commit    → 0
ls _generator/tools/fixtures/marker/           → нет такого каталога
```

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — шесть проверок, каждая командой; вывод каждой в `## ОТЧЁТ`:**

1. `python3 _generator/tools/check_marker.py <три красных файла по путям выше>` → `rc=1`, и в выводе названы **все три** поимённо. Названы не все — проверка дырява, чини её, а не список.
2. `python3 _generator/tools/check_marker.py <три зелёных файла по путям выше>` → `rc=0`, вывод **пустой**. Хоть одна строка вывода на здоровом файле — ложное красное, а его обходят вместе со всеми остальными гейтами.
3. Живой прогон хука на настоящем коммите: застейджить один рукописный файл из списка красных и позвать хук — коммит обязан **остановиться** с внятным текстом. Затем то же с файлом из зелёных — **пройти**. Стейдж после опыта откатить (`git restore --staged`), ничего не коммитя этим опытом.
4. `sh _generator/tools/fixtures/marker/PROGNAT.sh` → `rc=0`.
5. `python3 _generator/tools/check_tool_contract.py _generator/tools/check_marker.py` → `rc=0` (у нового инструмента есть живая точка вызова и покрывающая фикстура).
6. **Охват — числом внутри вердикта.** Прогнать чекер по всему живому корпусу заход-подобных файлов и напечатать распределение:
   ```
   python3 _generator/tools/check_marker.py _studio/zhurnal/*/kod_*.md _studio/zhurnal/*.md
   ```
   В отчёт: «судимо X, без маркера Y, с маркером Z», X = Y + Z. **Ожидание, названное заранее:** без маркера — подавляющее большинство (245 старых `kod_*.md` плюс три рукописных), с маркером — единицы (девять хэндоффов генератора плюс два файла миграции). **Расходится с ожиданием — скажи, не подгоняй.** Это прогон вне `--staged`, он ничего не роняет; красное здесь — печать, а не приговор коммиту.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Операция не лоссовая — ПОСЛЕ-верификатор не нужен.

🔴 **Нужен ДО-верификатор, ПЕРВЫМ ходом, до строчки кода.** Один свежий субагент, которому дают ТОЛЬКО текст правила Ш1 («заход-подобный файл обязан нести маркер в первых пяти строках») и живое дерево. Задача — **написать файл, который правило обманывает**: пройдёт молча, будучи рукописным, или покраснеет, будучи законным. Что искать он знает по образцу: так уже находили дыры в З8 `check_zahod.py` (см. её докстринг — весь список слепых зон получен этим способом). Находки — в `## ПЛАН` до кода, и они правят Ш1, а не остаются наблюдением.

*Почему ДО, а не после: после написания чекера ты — автор, и у автора слепые пятна ровно там, где он уверен. Тот же закон, по которому вердикт по уроку выносит не тот, кто урок записал.*

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/ .githooks/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/ .githooks/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/` `.githooks/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅; `python3 _generator/tools/git_zona.py check --zone .githooks/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Задание указало «маркер в первых пяти строках», а `sdelat_handoff.py` кладёт его на строку 6
ЦЕНА: буквальная реализация уронила бы в ❌ собственный обязан-промолчать калибровочный файл (`HANDOFF-2026-08-11.md`) при первом же прогоне критерия готовности — гейт лгал бы на своей же калибровке уже в день рождения. Поймано ДО кода: чтением `sdelat_handoff.py` по указанию §0.1 п.4 (маркер — элемент списка `L`, вставляется ПОСЛЕ пятистрочного YAML-фронтматтера) плюс независимым ДО-верификатором (§3, отдельный субагент, тот же вывод на восьми живых файлах). Стоило одной проверки `head -8` на живом коммите и одного прогона субагента — не переделки чекера после первого красного прогона калибровки.

### Команда критерия готовности 6 не достаёт до файлов, которые сама же должна найти
ЦЕНА: заданная команда (`_studio/zhurnal/*/kod_*.md _studio/zhurnal/*.md`) ловит только `kod_*.md` внутри арок и плоские файлы прямо в `zhurnal/`, а все маркерные `HANDOFF-*.md` лежат на уровне `<арка>/HANDOFF-*.md` — вне обоих паттернов. Прогон дал «с маркером: 0» вместо ожидаемых «единиц», хотя задание само предупредило: «расходится с ожиданием — скажи, не подгоняй». Полный рекурсивный прогон (`find _studio/zhurnal -name "*.md"`) дал 7 из 279 — ожидание по существу верно, неверна была ФОРМА команды для его проверки. Цена — один лишний прогон и необходимость сообщить расхождение строкой вместо тихой сдачи зелёного вердикта по заданной команде.

## ПЛАН — (заполняет исполнитель)

**§0.1 пройден.** Влито `zahod/dveri-discipliny` → `449f894`, виден в `git log --oneline`. Три грепа втройне ≥1 (bootstrap_zahod маркер=1, sdelat_handoff finalizirovano=7, check_zahod З11=7). Зона `_generator/tools/` `.githooks/` чиста в HEAD рабочей папки — точка отката не фабриковалась (правило «не фабрикуй, если чиста»). Прочитаны все шесть названных файлов по порядку.

**🔴 Предпосылка «шесть калибровочных файлов лежат в рабочей папке» — ложна, доказательство:** все шесть калибровочных `.md` (три красных, три зелёных, включая сам этот файл-заход) — **незакоммиченные (`??`) файлы основного репозитория `materials`**, их нет в истории git и поэтому нет в worktree `../materials-wt/zapret` (worktree видит только то, что закоммичено). Проверено `git status --porcelain` + `git log -1 --` на каждом из шести путей в основном репозитории — везде `??`, нигде коммита. Решение: для критериев готовности 1/2/4/6 (явные пути) буду вызывать чекер, указывая эти файлы по путям — при необходимости временно скопирую их в рабочую папку СТРОГО для локального прогона (не коммичу, не трогаю зону аналитика, удалю после прогона). Для критерия 3 (живой хук на настоящем коммите) это обязательно: `git add` требует, чтобы файл физически лежал в рабочем дереве этого репозитория, — скопирую по одному файлу из красного и из зелёного списков во временные пути внутри моей рабочей папки на время опыта, застейджу, прогоню хук, `git restore --staged`, затем удалю копии (они не мои и не часть зоны, оставлять их как есть — засорять чужую территорию).

**🔴 Предпосылка «маркер — в первых ПЯТИ строках файла» (Ш1) — ложна, доказательство и поправка:**
`sdelat_handoff.py` (прочитан по указанию §0.1 п.4) кладёт `MARKER` в список `L` ПОСЛЕ пятистрочного YAML-фронтматтера (`---`/`tab:`/`status:`/`poryadok: 0`/`---`) — то есть маркер стоит **строкой 6**, не в пределах первых пяти. Проверено `head -8` на живом закоммиченном `_studio/zhurnal/2026-08-05_faza-lenty/HANDOFF-2026-08-07.md` — маркер физически на строке 6. То же самое подтвердил независимый ДО-верификатор (см. ниже) на восьми живых `HANDOFF-*.md`, включая сам калибровочный `HANDOFF-2026-08-11.md` — то есть буквальная реализация «пяти строк» уронила бы **собственный обязан-промолчать калибровочный файл** в ❌ при первом же прогоне. `bootstrap_zahod.py`-маркер стоит строкой 3 (внутри и пяти, и шести). **Поправка: окно расширено до ПЕРВЫХ ШЕСТИ строк** — минимальное исправление, покрывающее обе реально наблюдаемые позиции (3 и 6), без домысливания дальнейшего запаса. Названо в докстринге чекера как факт с ценой, не молчком.

**ДО-верификатор (§3, отдельный свежий субагент, дан ТОЛЬКО текст правила Ш1 + живое дерево) — находки, которые правят Ш1:**
1. Подтвердил находку про строку 6 (см. выше) — независимо, на тех же восьми файлах.
2. `HANDOFF.md` без дефикса (`catalan/biblioteka/process/HANDOFF.md`, реальный живой файл) не ловится ни по имени (глоб требует `HANDOFF-*`), ни по содержимому (латинское «HANDOFF», не кириллическое «# ХЭНДОФФ»).
3. Строчный/суффиксный `peredacha-*.md` (`kurs leto 2026/zhurnal/2026-07-02_pitch/peredacha-START.md`, `ucheniki/danya/arhiv-oblako/sostoyanie-peredacha.md`) — реальные живые файлы, не ловятся ни по имени (регистр/порядок), ни по содержимому.
4. Тире-подмена: рукописная копия заголовка `# Канал исполнителя - fake` (обычный дефис вместо «—», U+2014) не совпадёт при точном строковом сравнении.
5. Неоднозначность реализации: «строка `## КОНТРАКТ ЗОНЫ`» — точное совпадение всей строки или совпадение по префиксу? Живые файлы несут `## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)`, не голую строку — при точном сравнении этот content-признак никогда бы не сработал ни на одном реальном файле.

**Как правки 2–5 входят в Ш1:**
- Находка 5 (моя, в моей власти) — решаю явно: `## КОНТРАКТ ЗОНЫ` матчится ПРЕФИКСОМ (`startswith`), не точным равенством строки. Это устраняет найденную неоднозначность до того, как она стала багом.
- Находки 2–4 — это дыры в СПИСКЕ ИМЁН/content-эвристике, который **явно назвал сам заход** (`kod_*.md · HANDOFF-*.md · PEREDACHA-*.md · ZAHOD-*.md` + три конкретных content-префикса). Расширять этот список в одиночку — не моё решение и не минимум («минимум — ровно то, что решает задачу», список — часть задачи, данная явно). Вместо тихого патча — называю их ЧЕТВЁРТЫМ пунктом честных слепых зон докстринга (сверх обязательных трёх из задания): признак эвристический по фиксированному списку, нестандартные имена/тире-варианты новых рукописных файлов могут пройти молча. Решение владельца/аналитика — расширять список или нет; это в `## ВОПРОСЫ`.

**Дизайн `check_marker.py` (Ш1):** явные пути — приоритет; без путей + `--staged` — автовыбор `git diff --cached --diff-filter=A --name-only -z`, фильтр `.md`. Признак «заход/хэндофф»: имя (`fnmatch`, 4 глоба) ИЛИ содержимое первых 30 строк (3 префикса, включая префиксный, не точный, матч КОНТРАКТ ЗОНЫ). Маркер: подстрокой в первых 6 строках. Молчание при чистом результате — **всегда**, независимо от `--staged` (совпадает и с требованием «--staged: чисто → молчит», и с калибровкой 2, где явные пути без `--staged` тоже обязаны дать пустой вывод на здоровых файлах) — идиома `check_uroki.py`. При нарушениях — печатает КАЖДЫЙ путь и сводку «Судимо X, без маркера Y, с маркером Z» (тем самым закрывает и критерий 1 (поимённо), и критерий 6 (числом), одним и тем же кодом, без двух режимов вывода).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. Список имён/content-признаков Ш1 (`kod_*.md · HANDOFF-*.md · PEREDACHA-*.md · ZAHOD-*.md` + три content-префикса) не ловит реально существующие в дереве варианты: `HANDOFF.md` без дефикса (`catalan/biblioteka/process/HANDOFF.md`), строчный/суффиксный `peredacha-*.md` (два живых файла), заголовок с обычным дефисом вместо «—». Расширять список — не моё решение (не мой минимум); честно назван четвёртым пунктом слепых зон докстринга `check_marker.py`. Владелец/аналитик решает: оставить как есть (осознанная граница, как с 245 старыми `kod_*.md`) или расширить глоб отдельным заходом.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. Ветка `zahod/dveri-discipliny` (влита в мою зону, §0.1 п.1) НЕ закрыта, как требовал §0.1 п.3 («ЗАКРЫТЬ ПОСЛЕ»): `git_zona.py zakryt-vetku --branch zahod/dveri-discipliny` отказал (`rc=1`) — в соседней рабочей папке `../materials-wt/dveri` (чужой параллельный заход) лежит незакоммиченный `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, и закрытие ветки снесло бы единственную живую копию. Не трогал чужую папку — это не моя зона. Ветка осталась незакрытой, как и предписывает инструмент при отказе («rc=1 — не закрывать»).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал + ЗАЧЕМ.** Три шага задания: (Ш1) новый чекер `_generator/tools/check_marker.py` — судит только НОВЫЕ (`--diff-filter=A`) заход-подобные/хэндофф-подобные `.md` (по имени ИЛИ по content-шапке), требует один из двух генератор-маркеров в первых **шести** строках (было «пяти» в задании — поправка с доказательством, см. `## ПЛАН`); (Ш2) точка вызова `python3 _generator/tools/check_marker.py --staged || exit 1` в `.githooks/pre-commit`, рядом с гейтом уроков; (Ш3) фикстура `_generator/tools/fixtures/marker/PROGNAT.sh` + восемь синтетических файлов-ловушек. Цель — заставить pre-commit краснеть на файле-заходе/хэндоффе, созданном мимо генератора; до этого маркер генератора существовал, но потребителя не имел.

**Как проверил.** ДО-верификатор (§3, отдельный субагент, дан ТОЛЬКО текст правила Ш1 + живое дерево) — нашёл 5 находок, две из них правят реализацию (окно 5→6 строк; префиксный, не точный матч «## КОНТРАКТ ЗОНЫ»), три остальные честно названы 4-м пунктом слепых зон докстринга (см. `## ВОПРОСЫ` п.1). Критерий готовности — все шесть проверок:
1. Три красных калибровочных файла разом → `rc=1`, все три названы поимённо. ✅
2. Три зелёных калибровочных файла разом → `rc=0`, вывод пустой (`[]`). ✅
3. Живой стейдж на настоящем коммите в рабочей папке: красный файл застейджен → хук остановился с внятным текстом (гейт маркера сработал первым). Зелёный файл застейджен → полный хук в итоге упал, но НЕ на гейте маркера (тот прошёл молча, хук продолжил дальше к `check_ves`/`check_kartoteka`) — упал на ЧУЖИХ, несвязанных воротах картотеки (файл не зарегистрирован в `KARTA.md §6`, вне моей зоны и вне этого задания). Изолированная проверка ТОЙ ЖЕ строки хука (`check_marker.py --staged`) на том же живом стейдже дала `rc=0`, вывод пуст — гейт маркера подтверждённо зелёный. Стейдж откатил `git restore --staged` оба раза, ничего не закоммитил этим опытом. ✅ (с честной оговоркой про чужие ворота)
4. `sh _generator/tools/fixtures/marker/PROGNAT.sh` → `rc=0`, «ФИКСТУРЫ ЗЕЛЁНЫЕ». ✅
5. `check_tool_contract.py _generator/tools/check_marker.py` → `rc=0`, «проверено 1 из 1». ✅
6. Охват буквально заданной командой дал «судимо 249, без маркера 249, **с маркером 0**» — разошлось с заявленным ожиданием «единицы». Причина: сама команда критерия (`_studio/zhurnal/*/kod_*.md _studio/zhurnal/*.md`) не достаёт до `<арка>/HANDOFF-*.md`, где физически лежат все маркерные файлы (см. `## УРОКИ ФАБРИКЕ`). Полный рекурсивный прогон (`find _studio/zhurnal -name "*.md"`) дал «судимо 279, без маркера 272, с маркером 7» — ожидание в целом подтвердилось (единицы, не ноль), не подгонял вывод под цифру из задания. ⚠ названо честно, не скрыто.

**Что НЕ трогал.** Ничего за пределами зоны `_generator/tools/` `.githooks/`. Шесть калибровочных `.md`-файлов основного репозитория (незакоммиченные, `??`) — не редактировал; временно копировал их в рабочую папку строго для прогона критериев 1–3 и удалил копии сразу после (оригиналы в основном репозитории нетронуты). Соседний заход `../materials-wt/dveri` и его незакоммиченный `INCIDENTY.md` — не трогал (см. `## ВОПРОСЫ` п.2). `_studio/docs/KARTA.md` — не правил: новых `.md`, требующих регистрации, не заводил (`korni.регистрация_обязательна()` → `False` на всех новых путях, проверено командой).

**Результат ДО-верификатора.** См. выше и `## ПЛАН` — 5 находок, 2 вошли в реализацию, 3 честно задокументированы как слепая зона (не расширял список имён в одиночку).

**Открытое «возвращаться».** Ветка `zahod/dveri-discipliny` не закрыта (см. `## ВОПРОСЫ` п.2) — вернуться, когда сосед `dveri` закоммитит свой `INCIDENTY.md` или заход `dveri` закроется штатно. Список имён Ш1 (`## ВОПРОСЫ` п.1) — решение владельца, не блокер.

**Время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет).

**ПОВТОРЯЕМОСТЬ находок.** Не повторится: обе находки (окно 5→6, узкий глоб критерия 6) — свойства именно ЭТОГО задания, найдены и закрыты внутри него же (исправление в коде + честная оговорка в отчёте), а не системная закономерность, которая ударит по следующей независимой единице работы. Урок для будущих заходов того же КЛАССА (пишущих новый генератор-маркер или калибровочную glob-команду) зафиксирован в `## УРОКИ ФАБРИКЕ` этого файла — следующий такой заход унаследует его через канон, а не наступит молча.

**НЕОБРАТИМОЕ:** необратимого нет. Единственные удаления за сессию — это удаление МОИХ ЖЕ временных копий шести калибровочных файлов (скопированы мной из основного репозитория для прогона критериев 1–3, удалены мной же сразу после; оригиналы в `/Users/ivanyakovlev/Documents/GitHub/materials` нетронуты, `git status` там по-прежнему `??` как и было). Попытка закрыть ветку `zahod/dveri-discipliny` НЕ состоялась (инструмент отказал, `rc=1`) — веток не удалено.

**АРТЕФАКТ:** артефакта нет: инфраструктурный заход (новый чекер + фикстура + точка вызова хука, не документ/колода/картинка).

**КОММИТ:** `a91a3b2` — «_generator/tools .githooks: гейт маркера — заход/хэндофф без следа генератора не коммитится (заход zapret-rukopisnyh)» · `git_zona.py check --zone _generator/tools/` → ✅ · `git_zona.py check --zone .githooks/` → ✅ · `git show --stat` — только свои 11 путей (Г6).
