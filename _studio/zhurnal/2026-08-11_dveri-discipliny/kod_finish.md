# Канал исполнителя — finish (один заход до конца)
<!-- собран bootstrap_zahod.py -->
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — перенос разделов по именам, влитие и закрытие веток готовыми командами; суждения нет.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-11_dveri-discipliny» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Два репозитория-соседа в `~/Documents/GitHub/`: `materials` (фабрика, каноны в `_meta/disciplina/`) и `disciplina` (плагин Claude, скиллы — распечатки канонов). Арка `2026-08-11_dveri-discipliny` закрыла обход генераторов четырьмя заходами: обязательное интервью и отсечка дневника как предусловие · запрет заходов мимо генератора в `pre-commit` · охват имён · разделение скиллов на девять с разведёнными триггерами. Всё принято, всё лежит в ветках.

ЦЕЛЬ — последний заход арки, три вещи: (а) сделать `disciplina-git` тоньше выносом двух блоков в `references/`; (б) довезти работу до места, где она работает: влить ветки; (в) подчистить хвосты предыдущих заходов.

🔴 **ЭТОТ ЗАХОД КРОСС-РЕПОЗИТОРНЫЙ.** Часть работы в `materials`, часть в `~/Documents/GitHub/disciplina`. Каждый шаг ниже НАЗЫВАЕТ, в каком репозитории он делается. Перепутаешь — коммит уедет не туда.

Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 **СТОП ДО ЦЕЛИ:** девятый скилл появится у владельца в интерфейсе только после `claude plugin marketplace update disciplina` — **это его кнопка, не твоя**. Ты доводишь до состояния «на `origin/main` лежат девять скиллов»; проверка в интерфейсе — его ход.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ (2026-08-11, владелец подтвердил)

1. **`slajdy` и `reserch-cikl` НЕ трогаем.** Владелец: «слайды точно не будем, это отдельная задача». Они большие (1365 и 520 строк) и режутся отдельным разговором.
2. **`disciplina-git` — решение принято по структуре, а не обсуждением.** Владелец: «если с этим несложно разобраться, то можно попробовать; если сложный дополнительный разговор — не надо, мы экономим токены». Разбор оказался дешёвым: из 490 строк 104 (21 %) читаются почти никогда, но грузятся всегда. Их выносим в `references/`.
3. **Резать `disciplina-git` на скиллы по ролям НЕЛЬЗЯ** — роль читателя неизвестна до вызова. Поэтому `references/`, а не новые скиллы.
4. **После этого захода арка закрывается.**

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **ДВА МЕСТА РАБОТЫ — сверься с таблицей ПЕРЕД каждым шагом, это главный источник ошибки в этом заходе:**

  | шаг | репозиторий | где стоишь |
  |---|---|---|
  | §0.1 влитие `ohvat-imyon`, Ш3 хвосты, Ш4 закрытие веток materials | `materials` | рабочая папка `../materials-wt/finish` |
  | Ш1 влитие `razdelenie-skillov` + вывоз, Ш2 `references/`, Ш4 закрытие ветки disciplina | `disciplina` | `~/Documents/GitHub/disciplina`, ветка `main` |

  🔴 **В `disciplina` свою ветку НЕ заводи** — там ты только вливаешь чужую в `main` и правишь `disciplina-git`; коммитишь через `python3 tools/git_zona.py` **из корня `disciplina`**. Ветка `zahod/git-tonkij` существует только в `materials`.

- **МЕСТО РАБОТЫ (в `materials`):** **рабочая папка `../materials-wt/finish`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/git-tonkij`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add finish --branch zahod/git-tonkij
  cd ../materials-wt/finish
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `../disciplina/skills/disciplina-git/` `_studio/zhurnal/_INFRA-git/` `_generator/tools/` `.githooks/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

```
python3 _generator/tools/git_zona.py merge zahod/ohvat-imyon --zone _generator/tools/ --zone .githooks/ --zone _studio/zhurnal/_INFRA-git/
```

Проверка фактом на диске, а не словом инструмента — для каждой ветки:
```
git log --oneline
# обязан быть виден коммит ветки `zahod/ohvat-imyon`
```
Не сошлось хоть для одной — СТОП, в `## ВОПРОСЫ`, дальше не иди. Ветки НЕ удаляй.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- references у disciplina-git; INCIDENTY.md в materials

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- ветки zahod/dveri-discipliny и zahod/ohvat-imyon в materials; zahod/razdelenie-skillov в disciplina

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/git-tonkij`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `../disciplina/skills/disciplina-git/` `_studio/zhurnal/_INFRA-git/` `_generator/tools/` `.githooks/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО: `~/Documents/GitHub/disciplina/skills/disciplina-git/SKILL.md` целиком (490 строк) и `~/Documents/GitHub/disciplina/KAK-ZAVESTI-SKILL.md` §1 (форма каталога, где описан `references/`). Больше ничего.
🔴 **ДВА КРАСНЫХ ГЕЙТА СБОРКИ НА ЭТОМ ЗАХОДЕ — ЛОЖНЫЕ. Доказательство здесь; не «чини» их и не подстраивай под них содержание.**
- **С1: «ветки `zahod/razdelenie-skillov` не существует».** Он ищет её в `materials`, а она в `disciplina`. Проверено при сборке: `cd ~/Documents/GitHub/disciplina && git branch --list zahod/razdelenie-skillov` — ветка есть, на ней коммит `c3befd3`.
- **С3: «путь `../disciplina/skills/disciplina-git/SKILL.md` не существует».** У тебя существует: `materials` и `disciplina` — соседние папки в `~/Documents/GitHub/`, и `../disciplina/…` резолвится от корня любого из двух. Гейт гонялся из песочницы Cowork, где у `materials` другой родитель.

⚠ **Все пути вида `../disciplina/…` выбраны нарочно** — верны и когда ты в `materials`, и когда в `disciplina`. Не переписывай их в абсолютные.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
Четыре шага, по порядку. Порядок не декоративный: Ш1 обязан пройти ДО Ш4, иначе закроешь ветку с неперенесённой работой.

### Ш1 [репозиторий `disciplina`]. Влить ветку скиллов в `main` и вывезти

```
cd ~/Documents/GitHub/disciplina
python3 tools/git_zona.py merge zahod/razdelenie-skillov --zone skills/ --zone KOLLIZII-opisanij.md
```
Проверка фактом на диске, а не словом инструмента: `git log --oneline main -1` показывает коммит `c3befd3`, и `ls -1 skills/ | wc -l` на `main` даёт **9**.
Затем вывезти на origin — плагин читает GitHub, а не твой диск: `python3 tools/git_zona.py vyvezti --yes`.
🔴 **Пока это не сделано, все остальные шаги бессмысленны:** девять скиллов существуют только в ветке.

### Ш2 [репозиторий `disciplina`]. `disciplina-git` — вынести два блока в `references/`

Из `../disciplina/skills/disciplina-git/SKILL.md` переезжают ДВА куска, **дословно, без переписывания**:
- **`references/instrument.md`** ← раздел `## 5. ИНСТРУМЕНТ И ЕГО ЗАЩИТА ОТ РЕГРЕССИИ` (~70 строк). Читает только тот, кто правит сам `git_zona.py`.
- **`references/pochemu-tak.md`** ← разделы `## 6. ПОЧЕМУ НЕТ РАЗРЕЗА МОНОРЕПО…`, `## 7. РАЗВЕНЧАННЫЙ ФАНТОМ…`, `## 8. ЧТО ЕЩЁ НЕ ПРОВЕРЕНО БОЕМ` (~34 строки). Это справка, не правило.

На их месте в самом скилле — по ОДНОЙ строке-указателю с названием файла и одной фразой, что там лежит.
🔴 **Ничего не переписывай и не сокращай при переносе.** Задача — снять вес с всегда-загружаемого тела, а не отредактировать содержание. Перенос дословный; правки содержания — не этот заход.
🔴 **Форму `references/` бери из `KAK-ZAVESTI-SKILL.md` §1**, не изобретай: справочные `.md` ровно на один уровень.
Закоммитить в `disciplina` и вывезти тем же способом, что в Ш1.

### Ш3 [репозиторий `materials`]. Хвосты

1. `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — правлен и не закоммичен (строка автозаписи от 2026-08-10 19:43, оставлена аналитиком из песочницы, которая коммитить не может). Закоммитить.
2. Влить `zahod/ohvat-imyon` — там лежат все три захода арки стопкой (`449f894` → `a91a3b2` → `14bad00`).

### Ш4 [оба репозитория]. Закрыть отработавшие ветки — только через метрику

```
python3 _generator/tools/git_zona.py poteri --branch zahod/dveri-discipliny
python3 _generator/tools/git_zona.py poteri --branch zahod/ohvat-imyon
cd ~/Documents/GitHub/disciplina && python3 tools/git_zona.py poteri --branch zahod/razdelenie-skillov
```
`rc=1` — **ветку НЕ закрывать**, назвать строкой в отчёте, что потерялось бы. `rc=0` → `zakryt-vetku`. Никогда `git branch -d`.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — шесть проверок, каждая командой, вывод в `## ОТЧЁТ`:**

1. **Девять скиллов на `main` в `disciplina`:** `cd ~/Documents/GitHub/disciplina && git --no-optional-locks show main:skills --name-only | wc -l` и `ls -1 ../disciplina/skills/ | wc -l` → **9**, среди них `disciplina-zahod`, `disciplina-priyomka`, `disciplina-handoff`; папки `disciplina-zahody` нет.
2. **Работа вывезена:** `git --no-optional-locks rev-list --count origin/main..main` → **0**. Не ноль — плагин у владельца не обновится, и весь заход впустую.
3. **`disciplina-git` похудел:** `wc -l ../disciplina/skills/disciplina-git/SKILL.md` — до работы **490** (замер 2026-08-11), после обязан быть **меньше 400**. И `ls -1 ../disciplina/skills/disciplina-git/references/ | wc -l` → **2** (каталога сейчас нет — его создаёт этот заход).
4. **🔴 ПРИ ПЕРЕНОСЕ НИЧЕГО НЕ ПОТЕРЯНО — числом.** `wc -l` по `../disciplina/skills/disciplina-git/SKILL.md` плюс обоим новым `references/*.md` в сумме **не меньше 485** (490 минус четыре строки-указателя плюс заголовки новых файлов; расхождение больше пяти строк в минус — потеря, назови какая). Плюс греп на месте: `grep -rc 'ЗАЩИТА ОТ РЕГРЕССИИ' ../disciplina/skills/disciplina-git/` и `grep -rc 'РАЗВЕНЧАННЫЙ ФАНТОМ' ../disciplina/skills/disciplina-git/` — обе дают непустой результат и ДО работы, и ПОСЛЕ; меняется только файл, в котором нашлось. Рекурсивно нарочно: так проверяется, что правило не исчезло из скилла ВООБЩЕ, а не что оно лежит в конкретном файле.
5. **Хвосты `materials` закрыты:** `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/_INFRA-git/` → ✅ **из основной папки репозитория, не из своей рабочей** — это разные ответы, и приёмка спросит основную.
6. **🔴 ЖИВОЙ ПРОГОН, а не файлы на диске.** По каждой из трёх веток — вывод `poteri` дословно и результат `zakryt-vetku`. Охват числом: «веток судимо 3, закрыто X, оставлено Y с названной причиной; X + Y = 3».

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). <Если операция не лоссовая — не нужен, хватает самопроверки критерия §2.>

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- ../disciplina/skills/disciplina-git/ _studio/zhurnal/_INFRA-git/ _generator/tools/ .githooks/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- ../disciplina/skills/disciplina-git/ _studio/zhurnal/_INFRA-git/ _generator/tools/ .githooks/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `../disciplina/skills/disciplina-git/` `_studio/zhurnal/_INFRA-git/` `_generator/tools/` `.githooks/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone ../disciplina/skills/disciplina-git/` → ✅; `python3 _generator/tools/git_zona.py check --zone _studio/zhurnal/_INFRA-git/` → ✅; `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅; `python3 _generator/tools/git_zona.py check --zone .githooks/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)
1. `materials`: `worktree add finish --branch zahod/git-tonkij`, `cd ../materials-wt/finish`, проверить HEAD.
2. В worktree: `merge zahod/ohvat-imyon` с тремя зонами (§0.1) — тем же ходом закрывает и Ш3.2 (та же ветка).
3. Прочитать `~/Documents/GitHub/disciplina/skills/disciplina-git/SKILL.md` и `KAK-ZAVESTI-SKILL.md` §1.
4. `disciplina`: Ш1 — `merge zahod/razdelenie-skillov`, проверка (9 скиллов, коммит `c3befd3`), `vyvezti --yes`.
5. `disciplina`: Ш2 — вынести два блока в `skills/disciplina-git/references/{instrument.md,pochemu-tak.md}` дословно, заменить на строки-указатели, закоммитить и вывезти.
6. `materials` (worktree): Ш3.1 — закоммитить `INCIDENTY.md` (правку аналитика).
7. Ш4 — `poteri` для трёх веток (`zahod/dveri-discipliny`, `zahod/ohvat-imyon` в materials; `zahod/razdelenie-skillov` в disciplina), закрыть при `rc=0`.
8. Финальный коммит зоны `materials` (§4): `_studio/zhurnal/_INFRA-git/`, `_generator/tools/`, `.githooks/`.
   🔴 **Расхождение с §4/ЗОНОЙ, оспариваю здесь по правилу «оспорить ложную предпосылку»:** путь `../disciplina/skills/disciplina-git/` в списке ЗОНА и в шаблоне §4 — не может быть закоммичен инструментом `materials`-репозитория (`git add`/`commit` не берут путь вне корня репозитория; сам разработчик это ставит явным правилом в таблице «ДВА МЕСТА РАБОТЫ» — Ш2 коммитится через `disciplina`-свой `git_zona.py` из корня `disciplina`, а не отсюда). Читаю это как унаследованный шаблонный артефакт `bootstrap_zahod.py`, не размеченный под кросс-репо. Действую по таблице: `../disciplina/skills/disciplina-git/` коммичу ИЗ `disciplina` (шаг 5 выше), в финальный `materials`-коммит (шаг 8) эту зону не включаю. Опишу вывод в `## ОТЧЁТ`.
9. Прогнать шесть проверок КРИТЕРИЯ ГОТОВНОСТИ + Г1–Г6, записать вывод в `## ОТЧЁТ`.

## ВОПРОСЫ — (заполняет исполнитель)

1. `~/Documents/GitHub/disciplina/tools/git_zona.py` — не универсальный инструмент, а бесподкомандный (только `vyvezti`, проверено `--help`): команды слияния/`poteri`/`zakryt-vetku` в нём НЕТ. Буквальная команда Ш1 из этого же захода (`cd ~/Documents/GitHub/disciplina && python3 tools/git_zona.py merge zahod/razdelenie-skillov ...`) падает `invalid choice: 'merge'`. Рабочая форма — вызывать инструмент `materials` с `GIT_ZONA_REPO`, она уже документирована в `disciplina-git/SKILL.md §3`, но заходы, порождаемые `bootstrap_zahod.py`, её не подставляют: `GIT_ZONA_REPO=$PWD python3 ~/Documents/GitHub/materials/_generator/tools/git_zona.py merge <ветка> --zone <путь>`. Сработало (проверено прогоном, коммит `c3befd3` влит).
   ДОМ: 2026-08-11_dveri-discipliny/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

2. Контракт зоны и шаблон §4 этого захода перечисляют путь `../disciplina/skills/disciplina-git/` в общей зоне `materials` — но `materials`-репозиторий не может закоммитить путь вне своего корня (git этого не умеет структурно), а таблица «ДВА МЕСТА РАБОТЫ» этого же захода правильно требует коммитить эти правки ИЗ `disciplina`. Разошлись сам шаблон §4/ЗОНА (унаследован из однорепозиторного `bootstrap_zahod.py`) и явная инструкция кросс-репо-захода. Разрешил по таблице — Ш2 закоммичен в `disciplina`, в финальный `materials`-коммит путь `../disciplina/...` не включён.
   ДОМ: 2026-08-11_dveri-discipliny/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ОТЧЁТ — (заполняет исполнитель)

### Что сделал и зачем
**(а) `disciplina-git` тоньше.** Из `~/Documents/GitHub/disciplina/skills/disciplina-git/SKILL.md` (490 строк) дословно вынесены `## 5. ИНСТРУМЕНТ И ЕГО ЗАЩИТА ОТ РЕГРЕССИИ` → `references/instrument.md` (69 строк) и `## 6–8` (разрез монорепо / развенчанный фантом / что не проверено боем) → `references/pochemu-tak.md` (30 строк); на их месте — по одной строке-указателю под тем же заголовком. Тело `SKILL.md` — 399 строк (< 400). Форма `references/` — по `KAK-ZAVESTI-SKILL.md §1`.
**(б) Работа довезена: влито и вывезено.**
   - `disciplina`: `zahod/razdelenie-skillov` → `main` (`c3befd3`, 9 скиллов) → `origin/main`; Ш2 (`80cfc30`) → `origin/main`.
   - `materials` (worktree `zahod/git-tonkij`): `zahod/ohvat-imyon` влита (`694e548`, все три коммита арки: `449f894`→`a91a3b2`→`14bad00`) — по правилу «влитие делает приёмка» саму `zahod/git-tonkij` в `arka/mat-kostyak`/`main` НЕ вливал.
**(в) Хвосты подчищены.** `INCIDENTY.md` (правка аналитика, записи 17:06–03:38) закоммичена и вывезена (`8d781e6`, из основной папки — см. `## ВОПРОСЫ`, там же лежала правка). Ветки: закрыты `zahod/ohvat-imyon` (materials, надгробие `mogila/zahod/ohvat-imyon`) и `zahod/razdelenie-skillov` (disciplina, `mogila/zahod/razdelenie-skillov`); `zahod/dveri-discipliny` НЕ закрыта — `poteri` дал `rc=1`: на ней своя, нигде больше не лежащая версия `INCIDENTY.md` (правлено с обеих сторон) и живой worktree `materials-wt/dveri` — решение не моё.

### Как проверил (КРИТЕРИЙ ГОТОВНОСТИ, все шесть)
1. **9 скиллов на `main`:** `ls -1 ../disciplina/skills/ | wc -l` → **9** (disciplina-arka, disciplina-git, disciplina-handoff, disciplina-istochnik-pravdy, disciplina-priyomka, disciplina-zahod, illustracii, reserch-cikl, slajdy) — есть все три названных, `disciplina-zahody` нет. ⚠ `git show main:skills --name-only | wc -l` даёт **11**, не 9 — команда критерия считает 2 служебные строки заголовка (`tree main:skills` + пустая); авторитетный счёт — `ls -1`, оба согласны на 9 сущностей.
2. **Вывезено:** `git rev-list --count origin/main..main` (disciplina) → **0**.
3. **Похудел:** `wc -l SKILL.md` → **399** (было 490, порог < 400 — прошло с запасом в 1 строку); `ls -1 references/ | wc -l` → **2**.
4. **Ничего не потеряно числом:** сумма `wc -l` SKILL.md + оба `references/*.md` → **498** (порог ≥ 485). `grep -rc 'ЗАЩИТА ОТ РЕГРЕССИИ' skills/disciplina-git/` и `grep -rc 'РАЗВЕНЧАННЫЙ ФАНТОМ' skills/disciplina-git/` — оба непустые ДО и ПОСЛЕ; после — переехали в `references/instrument.md` и `references/pochemu-tak.md` соответственно (в `SKILL.md` первый термин остался в заголовке-указателе, второй — нет: ожидаемо, критерий требует только непустоты в папке, не конкретного файла).
5. **Хвосты `materials` закрыты:** `git_zona.py check --zone _studio/zhurnal/_INFRA-git/` из ОСНОВНОЙ папки → **❌** — но красное НЕ моё: единственный путь вне git — чужой `_studio/zhurnal/_INFRA-git/kod_git-uborka.md` (другой, параллельный заход, файл создан 04:08, не мной; я его не трогал согласно контракту зоны — «нашёл вне зоны → в отчёт, не трогай»). Моя правка (`INCIDENTY.md`) в этой же зоне закоммичена и вывезена — см. коммит `8d781e6`.
6. **Живой прогон, охват 3 из 3:** `poteri --branch zahod/dveri-discipliny` → `rc=1`, «пропадёт файлов: 1 (изменено с обеих сторон) + рабочая папка `materials-wt/dveri` может вести живой заход» → НЕ закрыл. `poteri --branch zahod/ohvat-imyon` → `rc=0` → `zakryt-vetku` → ✅ (потребовался `--vsyo-ravno` — единственный игнорируемый путь в рабочей папке был `__pycache__/*.pyc`, названо причиной). `poteri --branch zahod/razdelenie-skillov` (disciplina) → `rc=0` → `zakryt-vetku` → ✅. **Итог: судимо 3, закрыто 2, оставлено 1 с названной причиной; 2 + 1 = 3.**

### Гигиена (Г1–Г6)
Г1 — ✅ по всем четырём зонам, кроме `_studio/zhurnal/_INFRA-git/` (❌ по чужому файлу, см. критерий 5 выше). Г2 — применимо (зона расширилась в `disciplina`): `git status --porcelain` там пуст ✅. Г3 — невлитых веток (`--no-merged arka/mat-kostyak`) в `materials`: 2 — обе законны: `zahod/git-tonkij` (моя, вливать не мне) и `zahod/dveri-discipliny` (сознательно не закрыта, см. критерий 6). Г4 — неприменимо, новых `.py` не заводил. Г5 — неприменимо, новых `.md`, требующих регистрации, не заводил (в `disciplina` `register_doc.py`/`KARTA.md` не существует вовсе — не тот репозиторий). Г6 — все три содержательных коммита (`80cfc30`, `8d781e6`) и слитый коммит (`694e548`) по `--stat` содержат только пути своей зоны.

### Что НЕ трогал
`slajdy` и `reserch-cikl` (по решению интервью) · `_studio/zhurnal/_INFRA-git/kod_git-uborka.md` (чужой файл-заход) · содержание §5–8 при переносе (дословно, без правок) · ветку `zahod/dveri-discipliny` и её worktree · ветку `zahod/git-tonkij` не сливал (не моё решение).

### Верификатор
Не требовался (§3 захода): операция лоссовая только по форме, не по содержанию — самопроверка критерия §2 (сумма строк + рекурсивный grep до/после, п.4 выше) её покрывает.

### Необратимое
Необратимого нет: перенос текста — копирование + замена на указатель, оригинал цел в истории git (`disciplina`, коммит перед `80cfc30`, и глубже); закрытые ветки — с надгробиями, воскрешаются одной командой.

### Повторяемость находок
Обе находки `## ВОПРОСЫ` (нет `merge` в `disciplina/tools/git_zona.py`; кросс-репо-путь в шаблоне §4/ЗОНА) — про шаблон `bootstrap_zahod.py`, не про эту задачу; в ЭТОЙ арке следующего прогона не будет (арка закрывается этим заходом, п.4 интервью), поэтому не «заход НЕМЕДЛЕННОЕ», а законно уходят в очередь.

### Время прогона + токены
На канале `app` неприменимо, снимать неоткуда.

**АРТЕФАКТ:** артефакта нет: заход про слияние веток и организацию кода (git + перенос текста скилла), не про сборку документа/деки/картинки.
**КОММИТ (три, кросс-репо):**
- `disciplina` `c3befd3` — merge `zahod/razdelenie-skillov` → `main` (Ш1) · вывезен · `git_zona.py vyvezti` → ✅
- `disciplina` `80cfc30` — вынос §5/§6-8 `disciplina-git` в `references/` (Ш2) · вывезен · `check --zone skills/disciplina-git/` → ✅
- `materials` `8d781e6` (ветка `arka/mat-kostyak`, основная папка) — `INCIDENTY.md` хвост (Ш3.1) · вывезен · `check --zone _studio/zhurnal/_INFRA-git/` → ❌ (чужой файл, см. критерий 5)
- `materials` `694e548` (ветка `zahod/git-tonkij`, worktree `finish`) — merge `zahod/ohvat-imyon` (§0.1) · НЕ вывезен намеренно (ветку в конце не вливать/не выводить — это делает приёмка) · `check --zone _generator/tools/` → ✅ · `check --zone .githooks/` → ✅
