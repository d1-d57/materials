# Канал исполнителя — skilly (один заход до конца)
<!-- собран bootstrap_zahod.py -->
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus** — продукт захода это СУЖДЕНИЕ (где режется граница смысла, какая формулировка триггера ловит живую речь), план рождается по ходу, готовых якорей нет.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-11_dveri-discipliny» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /Users/ivanyakovlev/Documents/GitHub/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. У владельца два репозитория: `materials` — фабрика курсов (там живут КАНОНЫ дисциплин, `_meta/disciplina/*.md`), и `disciplina` — плагин Claude, куда эти каноны РАСПЕЧАТАНЫ скиллами (`skills/<имя>/SKILL.md`, у каждого в шапке `istochnik:`). Связь односторонняя: правится канон в `materials`, потом перепечатывается в скилл.

Прошлый этап, и он объясняет, зачем этот заход: выяснилось, что плагин `disciplina` **не был установлен вовсе**, а вместо него подхватывалось старое поколение скиллов из `~/.claude/skills/` — те, копии которых сам владелец отправил в `disciplina/arhiv-skillov/` как конфликтующие с фабрикой. Дисциплина заходов поэтому не срабатывала ни разу: актуальный скилл не был установлен, а установленный был предшественником. Плагин установлен 2026-08-11, три устаревших скилла отключены.

ЦЕЛЬ: (а) развести «написание захода», «приёмку отчёта» и «хэндофф» по трём разным скиллам — три разных момента работы с разными триггерами, а сейчас они размазаны по двум; (б) сократить их, не потеряв правил; (в) свести описания ВСЕХ скиллов плагина так, чтобы ни один не пропускал свой момент и ни один не конкурировал за чужой.

Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 **СТОП ДО ЦЕЛИ:** заход правит ТОЛЬКО репозиторий `disciplina`. Каноны в `materials/_meta/disciplina/` он **не трогает** — там источник, и правка источника это отдельное решение владельца. Расхождение, если найдёшь, — строкой в отчёт.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ (2026-08-11, владелец подтвердил)

1. **Режем на ТРИ, а не на два** (решение уточнено 2026-08-11 после замера цены). Станет **девять** скиллов:
   - `disciplina-zahod` — написание захода (было частью `disciplina-zahody`);
   - `disciplina-priyomka` — **приёмка отчёта** (было там же). Владелец: *«приёмка — это сложная штука, наверное более сложная, чем написание захода: смотреть, как влиты ветки, что не влито, проверить, что новый инструмент подключился»*;
   - `disciplina-handoff` — закрытие сессии и вход в следующую (было частью `disciplina-arka`);
   - `disciplina-arka` — жизнь арки без хэндоффа; остальные пять без изменений.
   Имени `disciplina-zahody` после захода быть не должно — проверяется командой `ls -d ../disciplina/skills/disciplina-zahody` (ожидается ошибка «No such file or directory»; сейчас, до работы, папка есть).
2. 🔴 **Цена — тоже требование, и она замерена.** `claude plugin details` 2026-08-11: `disciplina-zahody` **~19.3k токенов на вызов**, тяжелее всех семи (`disciplina-git` ~12.5k, `slajdy` ~9.9k, остальные 3–7k). На 425 строк это ~45 токенов на строку: скилл не длинный от объёма функционала, он **многословный**. Платится это каждой сессией, которая тронула тему, — даже если нужна была одна треть.
3. **Проза с ценами инцидентов в скилле не нужна.** Её дом — канон `materials/_meta/disciplina/`, который читают когда нужно; скилл грузится каждую сессию. В скилле остаётся правило и то, что краснеет; развёрнутая история — ссылкой на канон.
4. **Курс владельца на будущее, за пределами этого захода:** дробить дальше, пока делится; логику по возможности уносить в python-инструменты и верификаторы, а тексты держать короткими и жёсткими. Здесь это делается только для трёх новых скиллов; карта дальнейшего дробления — отдельный аудит.
5. **Триггеры всех скиллов плагина сводятся ТЕМ ЖЕ заходом.** Материал один — поле `description`; деление всё равно заставляет переписать соседние описания, чтобы не столкнуться.
6. **Задача владельца дословно:** «максимально чётко и хорошо настроить теги, чтобы ни в коем случае не пропускать их запуск, когда нужно. И чтобы ни с кем они не конфликтовали». Обе половины обязательны, и они тянут в разные стороны — см. Ш3.
7. **Отключённые старые скиллы обратно не включаются.** `agentic-coding-session-brief`, `content-studio`, `session-handoff` сняты сознательно.
8. **Плагин установлен, проверено 2026-08-11:** `claude plugin details disciplina@disciplina` → `Skills (7)`, scope `user`.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- 🔴 **МЕСТО РАБОТЫ — ДРУГОЙ РЕПОЗИТОРИЙ. Перечитай этот пункт целиком, он переопределяет то, что напечатал генератор.**
  Заход **кросс-репозиторный**: файл-заход лежит в `materials`, а работа идёт в `~/Documents/GitHub/disciplina` — это отдельный git-репозиторий со своим origin на GitHub (репозиторий d1-d57/disciplina) и своим `tools/git_zona.py`.
  ```
  cd ~/Documents/GitHub/disciplina
  git --no-optional-locks checkout -b zahod/razdelenie-skillov
  ```
  🔴 **Рабочую папку-worktree в `materials` НЕ ЗАВОДИ — этой рабочей папки в заходе нет.** Флаг `--worktree` при сборке был нужен только генератору (он предполагает один репозиторий и без флага краснеет на несуществующей ветке). В `disciplina` рядом никто не работает, `checkout -b` там безопасен. *Названо явно, потому что канон запрещает ставить исполнителя между двумя обязательными указаниями: печать генератора и этот пункт расходятся, и главный — этот.*
  🔴 **Сам файл-заход (этот `.md`) остаётся в `materials`** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка.
  🔴 **В `materials` не коммить НИЧЕГО.** Все твои правки — в `disciplina`, и коммитишь ты её инструментом: `python3 tools/git_zona.py …` **из корня `disciplina`**, не из `materials`.
- **ЗОНА (можно менять):** `../disciplina/skills/` `../disciplina/KOLLIZII-opisanij.md` `../disciplina/zhurnal/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- правки в репозитории disciplina: skills/, KOLLIZII-opisanij.md, zhurnal/

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- свою ветку в disciplina после приёмки

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/razdelenie-skillov`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `../disciplina/skills/` `../disciplina/KOLLIZII-opisanij.md` `../disciplina/zhurnal/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
🔴 **ДВА КРАСНЫХ ГЕЙТА СБОРКИ НА ЭТОМ ЗАХОДЕ — ЛОЖНЫЕ, И ВОТ ДОКАЗАТЕЛЬСТВО. Не чини их и не подстраивай под них содержание.**
`check_sborki.py` при сборке дал два ❌, оба — слепая зона кросс-репозиторного захода, а не дефект задания:
- **С1: «ветки `zahod/razdelenie-skillov` не существует».** Верно — её и не должно быть в `materials`. Ты заводишь её в `disciplina`, командой из блока «МЕСТО РАБОТЫ». Гейт умеет спрашивать только один репозиторий.
- **С3: «путь `../disciplina/KAK-ZAVESTI-SKILL.md` не существует».** У тебя он существует: `materials` и `disciplina` — соседние папки в `~/Documents/GitHub/`, и `../disciplina/…` резолвится от корня ЛЮБОГО из двух. Гейт гонялся из песочницы Cowork, где `materials` смонтирован с другим родителем, — там соседа нет. Проверено обоими способами при сборке.

⚠ **Все пути вида `../disciplina/…` в этом заходе выбраны нарочно:** они одинаково верны и когда ты стоишь в `materials`, и когда стоишь в `disciplina`. Не переписывай их в абсолютные.

- **Гейт входа: плагин установлен?** `claude plugin details disciplina@disciplina` → ожидается секция `Skills (7)` со списком `disciplina-arka, disciplina-git, disciplina-istochnik-pravdy, disciplina-zahody, illustracii, reserch-cikl, slajdy`. Не семь или команда не отвечает — **СТОП**, строкой в `## ВОПРОСЫ`: без установленного плагина проверить результат нечем.
- Прочитать ТОЛЬКО эти файлы (все пути — от корня `~/Documents/GitHub/disciplina`). Ни `materials`, ни архив не изучай:
  1. `skills/disciplina-zahod/SKILL.md` — целиком, 425 строк. Это главный материал.
  2. `skills/disciplina-arka/SKILL.md` — целиком, 223 строки. Отсюда режется хэндофф.
  3. `../disciplina/KOLLIZII-opisanij.md` — целиком. **Готовый вход, не переоткрывай его своим анализом:** там уже посчитаны 4 коллизии внутри плагина и 9 против внешних, с методом и цитатами. Вердиктов в нём нет сознательно — их выносишь ты.
  4. `../disciplina/KAK-ZAVESTI-SKILL.md` §1–§2 — форма каталога и что реально стоит во фронтматтере пяти работающих скиллов. **Форма скиллов задаётся здесь, а не твоим представлением о том, как надо.**
  5. Шапки (первые 25 строк, поле `description`) остальных пяти: `skills/disciplina-git/`, `skills/disciplina-istochnik-pravdy/`, `skills/illustracii/`, `skills/reserch-cikl/`, `skills/slajdy/`.
  6. `README.md` §«Установка» — как проверяется, что плагин подхватился.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
Четыре шага. Все пути — от корня `disciplina`.

### Ш1. Разрезать на три новых скилла

**Ш1.1. `disciplina-zahody` (425 строк) → `disciplina-zahod` + `disciplina-priyomka`.**
- **`disciplina-zahod`** — всё до запуска: самодостаточность захода, семь клауз критерия готовности, гейт входа из семи вопросов, контракт зоны, режим субагентов, выбор модели, два канала запуска, скелет.
- **`disciplina-priyomka`** — всё после того, как исполнитель вернулся: пять шагов ритуала, гейт 0 «работа доехала в git», перегон числовых утверждений отчёта командой, хенд-офф-долг и его пять домов, закрытие ветки через метрику, хук «→ фабрика», верификаторы результата.
- 🔴 **Проверка влития и подключения инструмента — в `disciplina-priyomka`, и это по прямому слову владельца.** Как влиты ветки, что не влито, согласуется ли новое со старым, **встал ли новый инструмент на место** (не «коммит виден», а греп по живому файлу) — это работа приёмки, а не написания.
- 🔴 **Связка «критерий в заходе → гейт на приёмке» рвётся при разрезе — не дай ей порваться молча.** В каждом из двух скиллов поставь одну явную перекрёстную строку: «клаузы критерия формулируются в `disciplina-zahod`, проверяются в `disciplina-priyomka`» и обратную. Одна строка, не пересказ.

**Ш1.2. `disciplina-arka` (223 строки) → `disciplina-arka` + `disciplina-handoff`.**
В хэндофф переезжает закрытие сессии и вход в следующую: порядок закрытия, оба интервью, что несёт хэндофф, где его запускать. В арке остаётся жизнь арки: завести · нужна ли она вообще · вести · закрыть.

**Граница везде режется по МОМЕНТУ, не по слову.** «завожу арку», «это на несколько сессий» — арка; «закроем сессию», «контекст кончается», «сделай хэндофф», «что дальше делаем» — хэндофф; «собери заход», «отправь в код» — заход; «отчёт вернулся», «прими отчёт», «заход вернулся» — приёмка. «Закрываем арку» остаётся у арки, и в хэндоффе на неё стоит явное «не я».

Фронтматтер новых скиллов — по форме из `../disciplina/KAK-ZAVESTI-SKILL.md` §2, с `istochnik:` на соответствующий канон.

🔴 **Оба разреза лоссовые — верификатор обязателен (§3).**

### Ш1½. Сократить, не потеряв правил

Цель — **правило и то, что краснеет, остаются; развёрнутая проза с ценами инцидентов уходит**. У каждого правила в каноне `materials/_meta/disciplina/` есть дом; в скилле вместо абзаца-истории ставится короткая ссылка на канон.

Что режется в первую очередь: курсивные абзацы «*Цена 29.07: …*» на 5–15 строк · повторы одного правила в разных секциях · пересказ того, что уже сказано соседней строкой.
Что НЕ режется ни при каких условиях: сама формулировка правила · то, чем оно краснеет (команда, гейт, артефакт) · числа, которые нужны для исполнения · перекрёстные ссылки Ш1.1.

⚠ **Сокращение — тоже лоссовая операция, и верификатор §3 покрывает её наравне с разрезом.** «Стало короче» без «правил не потеряно» — не результат.

### Ш2. Догнать оба скилла до ЖИВОГО состояния инструментов

Скиллы описывают ритуал, который **вчера изменился в `materials`**, и без этого шага они будут учить устаревшему. Три захода 2026-08-10/11 сделали следующее, и это обязано быть отражено:

**В `disciplina-handoff`:**
- `sdelat_handoff.py` теперь **отказывается собирать файл** без `--finalizirovano` (≥3) и `--vopros` (≥3);
- он же отказывается, если в `<арка>/SESSIYA.md` нет отсечки `<!-- РАЗНЕСЕНО ДО СЮДА: <сегодня> -->`;
- **четыре шаблонных вопроса удалены из кода.** Вопросы пишет аналитик под контекст. Отдельно: вопрос «что-то изменилось со вчера» удалён по прямому слову владельца — «бессмысленно, я всегда отвечаю „не изменилось“»;
- продукт интервью — **финализация** («что именно решили»), а не список заданных вопросов.

**В `disciplina-zahody`:**
- `bootstrap_zahod.py` требует `--finalizirovano` (≥2) и вшивает секцию `## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ`;
- он же ставит маркер `<!-- собран bootstrap_zahod.py -->`;
- `check_zahod.py` получил проверку **З11** (секция финализации непуста);
- `pre-commit` зовёт `check_marker.py`: заход или хэндофф без маркера генератора **не коммитится**. Распознавание регистронезависимое, разделитель `-`/`_`/ничего;
- **писать заход руками запрещено полностью.** Лазейки нет; громкий обход один — `--no-verify` руками. Запрещено СОЗДАНИЕ файла мимо генератора, **не** написание содержания: каркас из генератора, текст в оставленные `<...>` — норма.

**В `disciplina-git`:** дописать подкоманду `vyvezti [--zahody] [--yes]` (вывоз локальных коммитов на origin; без `--yes` — предпросмотр). В каноне `materials` она уже описана 2026-08-11, в скилле нет.

### Ш3. Свести описания ВСЕХ девяти скиллов

🔴 **Два требования владельца тянут в РАЗНЫЕ стороны, и это надо решить, а не выбрать одно.** «Ни в коем случае не пропускать запуск, когда нужно» толкает расширять списки триггеров; «чтобы ни с кем не конфликтовали» толкает их сужать. Разрешается это **не длиной списка, а разграничением по МОМЕНТУ работы**: каждый скилл называет момент, в который он единственный уместен, и моменты не пересекаются. Тема пересекаться может, момент — нет.

Каждое из девяти описаний обязано нести три вещи:
1. **момент** — что пользователь делает прямо сейчас;
2. **живые формулировки владельца**, а не канцелярские (см. лист ниже);
3. **явное «НЕ для …» с именем скилла-адресата** — куда уходит соседний случай. Без адресата это не разграничение, а отказ.

Разрешить все четыре внутренние коллизии из `KOLLIZII-opisanij.md` поимённо. Две названы там прямо: `disciplina-git` × `disciplina-istochnik-pravdy` делят **дословную** фразу «проверь перед коммитом»; `disciplina-git` × `disciplina-zahody` делят слово «заход».

**Лист живой речи владельца — каждая фраза обязана иметь РОВНО ОДИН адрес:**
«собери заход» · «отправь в код» · «напиши промпт для Claude Code» · «прими отчёт» · «заход вернулся» · «сделай хэндофф» · «закроем сессию» · «передача» · «что дальше делаем» · «запиши в дневник» · «выведи документ» · «по ритуалу» · «контекст кончается» · «завожу арку» · «проверь перед коммитом» · «закоммить» · «куда это записать».

### Ш4. Пересчитать `KOLLIZII-opisanij.md`

Дописать раздел с датой данных: было → стало по каждой из четырёх внутренних. Файл остаётся показом, но теперь показывает и разрешение.

---

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — семь проверок, каждая командой, вывод в `## ОТЧЁТ`:**

1. **Девять скиллов, имя папки = поле `name`.** `ls -1 ../disciplina/skills/ | wc -l` → `9`; папки `disciplina-zahody` больше НЕТ; и для каждого `grep -m1 '^name:' ../disciplina/skills/<имя>/SKILL.md` совпадает с именем папки — напечатать таблицу из девяти строк, как это уже сделано в `../disciplina/KAK-ZAVESTI-SKILL.md` §1.
2. **🔴 ПОТЕРЬ ПРИ РАЗРЕЗЕ И СОКРАЩЕНИИ НЕТ — верификатором (§3), охват числом, ПО ОБОИМ разрезам отдельно.** Каждое ПРАВИЛО исходных `disciplina-zahody` и `disciplina-arka` имеет дом в одном из новых скиллов ИЛИ сознательно снято с названной причиной («ушло в канон, ссылка стоит» — законная причина; «показалось лишним» — нет). Форма вердикта: «заголовков в исходнике X, нашли дом Y, снято сознательно Z, **потеряно 0**; X = Y + Z». Отрицательный вердикт без охвата не принимается.
3. **Каждое описание несёт разграничение.** `grep -lE 'НЕ для|НЕ активируется|не я —|не этот скилл' ../disciplina/skills/*/SKILL.md | wc -l` → `9`. Меньше девяти — назови, у каких нет и почему.
4. **Лист живой речи разведён.** Таблица «фраза → скилл» на все 17 фраз в `## ОТЧЁТ`. **Ни одной без адреса и ни одной с двумя.** Фраза, которая честно принадлежит двум, — это дефект границы: назови её и предложи, как режется.
5. **Скиллы описывают ЖИВЫЕ инструменты.** Все шесть грепов дают ≥1:
   `grep -c finalizirovano ../disciplina/skills/disciplina-handoff/SKILL.md` · `grep -c 'РАЗНЕСЕНО' ../disciplina/skills/disciplina-handoff/SKILL.md` · `grep -c finalizirovano ../disciplina/skills/disciplina-zahod/SKILL.md` · `grep -c 'собран bootstrap_zahod' ../disciplina/skills/disciplina-zahod/SKILL.md` · `grep -c 'З11' ../disciplina/skills/disciplina-zahod/SKILL.md` · `grep -c 'vyvezti' ../disciplina/skills/disciplina-git/SKILL.md`.
   И обратный греп: `grep -c 'изменилось со вчера' ../disciplina/skills/disciplina-handoff/SKILL.md` — ДО работы такой строки нет вовсе, ПОСЛЕ обязан быть ноль (устаревший ритуал не должен приехать в новый скилл).
6. **`KOLLIZII-opisanij.md` пересчитан:** раздел «было → стало» покрывает **все четыре** внутренние коллизии, у каждой назван итог. Жёстких внутренних осталось `0` — или названо, какая осталась и почему это законно.
7. **🔴 ОБЪЁМ УПАЛ, И ЭТО ЧИСЛО, А НЕ ОЩУЩЕНИЕ.** Замер до работы (снят при сборке захода, дата данных 2026-08-11): `wc -l ../disciplina/skills/disciplina-zahody/SKILL.md` → **425**, `disciplina-arka` → **223**, вместе **648**.
   После работы напечатать `wc -l` по всем четырём затронутым (`disciplina-zahod`, `disciplina-priyomka`, `disciplina-handoff`, `disciplina-arka`) и их сумму. Две планки, обе могут провалиться:
   - **сумма четырёх < 648** — иначе сокращения не было вовсе;
   - **ни один из четырёх не длиннее 250 строк** — иначе разрез прошёл не там, где тяжесть.
   ⚠ **Планка по объёму НЕ перебивает пункт 2.** Потеря правила ради строки — брак, а не экономия; упереться в 250 и не суметь урезать дальше без потерь — законный отчёт, скажи это строкой и назови, что мешает.
8. **🔴 ЖИВОЙ ПРОГОН, а не только файлы на диске.** `claude plugin details disciplina@disciplina` — напечатать вывод дословно. **Ожидание, названное заранее: там будет по-прежнему `Skills (7)`**, потому что плагин читает `main` на GitHub, а твоя работа лежит в ветке. Это НЕ провал — это стоп до цели: восьмой скилл появится после влития и `claude plugin marketplace update disciplina`, и делает это владелец. Увидел `Skills (8)` до влития — вот это расхождение, назови его.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
🔴 **Ш1 и Ш1½ лоссовые — разрез надвое и сокращение теряют МОЛЧА.** ПОСЛЕ-верификатор обязателен: **свежий субагент, ДРУГОЙ метод, читает ОРИГИНАЛЫ из git**, а не твою память о них:
```
cd ~/Documents/GitHub/disciplina
git --no-optional-locks show HEAD:skills/disciplina-zahody/SKILL.md
git --no-optional-locks show HEAD:skills/disciplina-arka/SKILL.md
```
Две таблицы, по одной на разрез: «**правило** оригинала → дом (`zahod` | `priyomka` | `arka` | `handoff` | снято сознательно, причина)», сплошь, с охватом числом (критерий п.2).

🔴 **Единица сверки — ПРАВИЛО, а не заголовок.** Заголовки после сокращения сольются и переименуются, и сверка по ним даст зелёное на потерянном содержании. Верификатор идёт по правилам: что предписано, чем краснеет.
🔴 **Корпус большой (648 строк) — задай ДОЛЮ ВЫБОРКИ явно:** сплошная не менее трети каждого оригинала, и какая именно треть — назвать. Иначе верификатор законно отступит от буквы гейта, а вердикт «потерь нет» будет означать «нет в непонятно какой части».

Не пересказывай ему, что перенёс: он сверяет с оригиналом, а не с твоим отчётом — у автора правок слепые пятна ровно там, где он уверен.

🔴 **Ш3 требует ДО-верификатора, и он идёт ПЕРВЫМ ходом.** Свежий субагент получает ТОЛЬКО лист из 17 живых фраз владельца и девять описаний **в их нынешнем виде** — и отвечает, какая фраза сегодня попадает в какой скилл (или ни в какой). После твоей правки этот замер неисполним: описания изменятся, и «стало лучше» станет нечем доказать. Ответ — в `## ПЛАН` до работы, и он же служит колонкой «было» в отчёте.

⚠ **Мандат чтения верификатора шире твоего** — ему можно открыть и `arhiv-skillov/`, и `KOLLIZII-opisanij.md`, и старые версии из `git log`. Тебе — только названное в §0.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- ../disciplina/skills/ ../disciplina/KOLLIZII-opisanij.md ../disciplina/zhurnal/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- ../disciplina/skills/ ../disciplina/KOLLIZII-opisanij.md ../disciplina/zhurnal/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `../disciplina/skills/` `../disciplina/KOLLIZII-opisanij.md` `../disciplina/zhurnal/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone ../disciplina/skills/` → ✅; `python3 _generator/tools/git_zona.py check --zone ../disciplina/KOLLIZII-opisanij.md` → ✅; `python3 _generator/tools/git_zona.py check --zone ../disciplina/zhurnal/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Гейт Г1 на кросс-репозиторной зоне даёт ЛОЖНЫЙ ЗЕЛЁНЫЙ — доказано мутацией, а не заподозрено
ЦЕНА: `git_zona.py check --zone ../disciplina/skills/`, запущенный из `materials` как велит §4.1, печатает `✅ работа доехала в git` и `rc=0` — при untracked-файле, лежащем в этой самой зоне. Проверено мутацией: завёл `skills/__PROBA-Г1.md`, гейт остался зелёным, а живой `git status` самой `disciplina` файл видит (`?? skills/__PROBA-Г1.md`). Причина в первой же строке вывода: «🔎 Репозиторий: /Users/…/materials · ветка: arka/mat-kostyak» — инструмент спрашивает git ОДНОГО репозитория о папке, которую тот не отслеживает, и «вне git ничего нет» становится истиной по пустоте. **Приёмка гоняет Г1 первым ходом и приняла бы незакоммиченную зону как доехавшую.** Это ровно класс клаузы 4½ («чекер не нашёл, что проверять, и молча вернул успех»), написанной в том же скилле, который этот заход и правил. Стоило одного хода на мутационную пробу; в общем случае стоит целого захода, объявленного доехавшим.

### «Невлитых веток 0» в §0.1 измерено не в том репозитории, а вся работа Ш2 лежала именно в невлитых
ЦЕНА: слот 1 ГИТ-КОНТУРА утверждал «вливать нечего, проверено `git branch --no-merged` при сборке: невлитых веток 0». На живом `materials` их две (`zahod/dveri-discipliny`, `zahod/ohvat-imyon`), и все пять фактов Ш2 лежат в них: на текущей ветке `arka/mat-kostyak` нет ни `check_marker.py`, ни `--finalizirovano`, ни З11 — греп даёт нули. То есть заход требовал описать инструменты, которых по его же измерению «нет». Стоило хода на проверку по коду веток; **если бы я поверил диску, а не заходу, я объявил бы Ш2 ложной предпосылкой и не сделал бы его вовсе** — и три скилла учили бы устаревшему ритуалу, а отчёт выглядел бы честным.

### Контракт зоны назвал инструментом коммита дверь, у которой нет подкоманды `commit`
ЦЕНА: блок «МЕСТО РАБОТЫ» предписывает «коммитишь ты её инструментом: `python3 tools/git_zona.py …` **из корня `disciplina`**», а §4 того же файла даёт голый двухходовый `git add` / `git commit -- <пути>`. Живой `disciplina/tools/git_zona.py --help` печатает ровно одну подкоманду — `vyvezti` (main → origin/main); ни `commit`, ни `check` там нет и по докстроке не предполагалось («это НЕ порт materials-версии»). Исполнитель оказался между двумя обязательными указаниями без названного приоритета — тот самый класс, который канон запрещает заводить. Стоило одной проверки `--help`; цена в общем случае — либо коммит не тем инструментом, либо остановка захода на ровном месте.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ветка заведена:** `zahod/razdelenie-skillov` в `~/Documents/GitHub/disciplina` (`rev-parse --abbrev-ref HEAD` → `zahod/razdelenie-skillov`), зона в HEAD чиста (`git status --porcelain` пуст) — точку отката не фабрикую.
**Гейт входа пройден:** `claude plugin details disciplina@disciplina` → `Skills (7)`, список совпал дословно.

### П0. ДО-верификатор Ш3 отработал ПЕРВЫМ ходом — колонка «БЫЛО»

Свежий субагент получил только 17 фраз и семь нынешних `description` (тела не читал — модель на старте тоже видит только шапки). Результат: **ровно один адрес 13 из 17 · коллизий 2 · дыр 2**.

| # | фраза | БЫЛО (сегодня) | вердикт |
|---|---|---|---|
| 1 | «собери заход» | disciplina-zahody | ОДИН |
| 2 | «отправь в код» | disciplina-zahody (слабо — держится на слове «код») | ОДИН |
| 3 | «напиши промпт для Claude Code» | disciplina-zahody | ОДИН |
| 4 | «прими отчёт» | disciplina-zahody | ОДИН |
| 5 | «заход вернулся» | disciplina-zahody | ОДИН |
| 6 | «сделай хэндофф» | disciplina-arka | ОДИН |
| 7 | «закроем сессию» | disciplina-arka | ОДИН |
| 8 | «передача» | disciplina-arka | ОДИН |
| 9 | «что дальше делаем» | disciplina-arka | ОДИН |
| 10 | «запиши в дневник» | — | **ДЫРА** |
| 11 | «выведи документ» | — | **ДЫРА** |
| 12 | «по ритуалу» | zahody · arka · istochnik-pravdy | **КОЛЛИЗИЯ** |
| 13 | «контекст кончается» | disciplina-arka | ОДИН |
| 14 | «завожу арку» | disciplina-arka | ОДИН |
| 15 | «проверь перед коммитом» | istochnik-pravdy · git | **КОЛЛИЗИЯ** |
| 16 | «закоммить» | disciplina-git | ОДИН |
| 17 | «куда это записать» | disciplina-istochnik-pravdy | ОДИН |

🔴 **Замер показывает не то, чего боялись, и это меняет работу.** Формально «один адрес» у 13 фраз — но пять из них (1–5) ведут в ОДИН скилл `disciplina-zahody`, и пять (6–9, 13) — в ОДИН `disciplina-arka`. Адрес один, потому что альтернативы не существует, а не потому что граница проведена. Ровно это и режет заход: после разреза у каждой из этих десяти фраз адрес обязан стать РАЗНЫМ и по-прежнему единственным.

### П1. Предпосылки Ш2 проверены по живым файлам — одна ложная, остальные верны

Проверял грепом по коду, а не по прозе захода.

- ✅ `sdelat_handoff.py`: `--finalizirovano` и `--vopros` обязательны **минимум по 3 раза** (`len(...) < 3` → `sys.exit`); отсечка проверяется регуляркой `OTSECHKA_RE` на СЕГОДНЯШНЮЮ дату; строка «изменилось со вчера» из кода ушла.
- ✅ `bootstrap_zahod.py`: `--finalizirovano` **минимум 2 раза**, вшивает `## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ`, маркер `<!-- собран bootstrap_zahod.py -->`.
- ✅ `check_zahod.py`: З11 есть (`FINALIZIROVANO_RE`, красное на отсутствующей и на пустой секции).
- ✅ `check_marker.py`: есть.
- ✅ `git_zona.py vyvezti`: есть (`cmd_vyvezti`).
- 🔴 **Ложная предпосылка §0.1 слот 1: «невлитых веток 0, проверено `git branch --no-merged` при сборке».** На живом `materials` их **две** — `zahod/dveri-discipliny` и `zahod/ohvat-imyon`, и **весь Ш2 лежит именно в них**: на текущей ветке `arka/mat-kostyak` ни одного из пяти фактов нет (`check_marker.py` вообще отсутствует, `finalizirovano` = 0 вхождений, З11 нет). В `disciplina` невлитых тоже две — `zahod/fazy-1-2-dovodka`, `zahod/format-kartochki-faza-1`, обе трогают только `skills/slajdy/**`, моих файлов не касаются.
  **На работу это не влияет** (я этими инструментами не пользуюсь, а описываю их) — но Ш2 я пишу по коду веток, а не по памяти, и говорю об этом вслух: заход утверждал «влито», факт — «не влито».

### П2. Как режу — граница по МОМЕНТУ

- **`disciplina-zahod`** — момент «задание ещё не выдано»: самодостаточность, семь клауз критерия, гейт входа из семи вопросов, контракт зоны, субагенты, модель, два канала, гигиена сессии исполнителя, встроенный верификатор (оба типа, доля выборки), тип захода, скелет, «греп не доказывает отсутствия», рамку ставит владелец, дыра интервью.
- **`disciplina-priyomka`** — момент «исполнитель вернулся»: `adopt` пофайлово, `priyomka.py` (Г0–Г7), пять шагов, гейт 0, перегон чисел командой, охват у положительного вердикта, артефакт карточкой, хенд-офф-долг и пять домов, закрытие ветки через `poteri`, хук «→ фабрика» целиком, **и новое по прямому слову владельца — проверка влития и того, что новый инструмент реально встал** (греп по живому файлу, а не «коммит виден»).
- **`disciplina-handoff`** — момент «сессия кончается»: порядок закрытия из пяти шагов, оба интервью, что несёт хэндофф, где его запускать, гейт «работа доехала в git» и почему он поздний, гигиена документа передачи (§6в), триггер B.
- **`disciplina-arka`** — момент «единица работы»: завести (Ф0 + ворота + переоценка), нужна ли вообще, вход в чат, четыре дока памяти, размер арки, закрыть, шаблоны, триггеры A и C.

**Допущение (называю до работы, потому что Ш1.2 читается двояко):** «оба интервью» в хэндоффе я читаю как (1) интервью закрытия «что делаем дальше» вопросами да/нет и (2) финализацию «что именно решили» — то, что инструмент требует как `--finalizirovano` ×3 и `--vopros` ×3. Ф0-интервью арки («нужна ли арка») остаётся в `disciplina-arka`, и в хэндоффе на него стоит явное «не я».

### П3. Разрешение коллизий и двух дыр

- **А1 «проверь перед коммитом»** → `disciplina-istochnik-pravdy` (он отвечает «что проверить»); `disciplina-git` фразу отдаёт и получает строку «НЕ для „что проверить перед коммитом"».
- **А2 «заход»** → слово делится по моменту: написание — `disciplina-zahod`, параллельность/worktree — `disciplina-git`, каждый называет соседа.
- **А3 арка × заход** → снимается самим разрезом: хэндофф, приёмка и написание получают свои имена.
- **А4 «завёл новый .md»** → остаётся у `istochnik-pravdy`; `arka` называет его адресатом явно.
- **Дыра «запиши в дневник»** → `disciplina-arka` (дневник `SESSIYA.md` — её док памяти).
- **Дыра «выведи документ»** → `disciplina-priyomka`. Читаю как «покажи мне собранный файл»: правило «артефакт выводится владельцу карточкой с абсолютным путём, и он не старше источника» живёт именно там. Если владелец имел в виду сборку документа генератором `build_doc.py` — такой дисциплины в плагине нет вовсе, и это отдельная дыра, а не адрес; вынесу пунктом очереди.
- **«по ритуалу»** — голое слово получает ОДИН адрес `disciplina-priyomka` (там ритуал — собственное имя процедуры: «ЖЁСТКИЙ РИТУАЛ»); у соседей слово употребляется только с определением («арк-ритуал», «чек-лист»), поэтому голая форма больше ни к кому не липнет.

### П4. Порядок ходов

1. `git mv` каркаса: `disciplina-zahody` → `disciplina-zahod` (наследует `DOLG.md`, `NEPOKRYTOE.md`, `evals/`), новые папки `disciplina-priyomka`, `disciplina-handoff`.
2. Написать четыре тела с сокращением: правило и то, чем оно краснеет, остаются; курсивные «*Цена …*» уходят ссылкой на канон `materials/_meta/disciplina/*.md`.
3. Ш2 — вписать живые инструменты по коду веток.
4. Ш3 — переписать все девять `description` (≤1024 знака, третье лицо, момент + живая речь + «НЕ для … → имя скилла»).
5. Ш4 — пересчитать `KOLLIZII-opisanij.md`.
6. ПОСЛЕ-верификатор: свежий субагент, читает оригиналы `git show HEAD:…`, сверяет ПРАВИЛАМИ, сплошная выборка ≥⅓ каждого оригинала с названием, какая именно треть.
7. Критерий (8 проверок командами) → коммит зоны → отчёт.

### П5. Что НЕ трогаю, хотя оно сломается — зона запрещает

- `../disciplina/README.md §6` несёт регистрацию `skills/disciplina-zahody/NEPOKRYTOE.md`; после переименования папки строка станет мёртвой. README вне зоны, а запрет зоны 🔴 сильнее добровольной регистрации (гейта `check_kartoteka --gates` для `disciplina` нет вовсе — README §6 говорит это прямо). → пункт очереди.
- `../disciplina/tools/polnota_perenosa.py` несёт пару `("disciplina-zahody", ...)` в константе `PARY`. Вне зоны. Плюс сам смысл инструмента после Ш1½ меняется — см. `## ВОПРОСЫ`.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `README.md §6` репозитория `disciplina` несёт регистрацию `skills/disciplina-zahody/NEPOKRYTOE.md` — после переименования папки строка указывает в никуда (файл жив по адресу `skills/disciplina-zahod/NEPOKRYTOE.md`). README вне зоны, а запрет зоны 🔴 сильнее добровольной регистрации, и гейта `check_kartoteka --gates` для `disciplina` нет вовсе. Одна строка правки.
   ДОМ: ../disciplina/README.md
   ДОСТАВЛЕНО: нет

2. `README.md §Установка` в двух местах говорит «ожидается: 7 скиллов» и перечисляет старый состав с `disciplina-zahody`. После влития станет **девять**. Заодно: п.8 самого захода ожидает «восьмой скилл появится после влития» — скиллов прибавилось два, а не один, так что ожидание надо читать как `Skills (9)`.
   ДОМ: ../disciplina/README.md
   ДОСТАВЛЕНО: нет

3. `tools/polnota_perenosa.py` несёт в константе `PARY` пару `("disciplina-zahody", "_meta/disciplina/RUKOVODSTVO-zahodami.md")` — имя мёртвое. Но чинится не переименованием: **Ш1½ сломал сам смысл инструмента.** Он судит «включение источника в скилл» построчно и на побайтовой копии давал `потеряно 0`; теперь четыре скилла сознательно короче источника, и по построению он даст красное на здоровом — то есть станет ровно тем гейтом, который обходят. Нужно решение: либо снять эти пары из `PARY`, либо научить его классу «снято сознательно, ссылка на канон стоит».
   ДОМ: ../disciplina/tools/polnota_perenosa.py
   ДОСТАВЛЕНО: нет

4. Оценок (`evals/evals.json`) у `disciplina-priyomka` и `disciplina-handoff` **нет**, у `disciplina-arka` и `disciplina-zahod` осталось по три — но часть из шести существующих теперь проверяет ЧУЖОЙ момент: `zahod` eval №3 («записывать всё, консолидировать, потом хэндофф») и оба граничных eval арки («а хэндофф-то сделал?», «начать новый чат») судят поведение `disciplina-handoff`. Чек-лист `zhurnal/STANDART-SKILLA.md` C6 требует ≥3 оценок на скилл и прямо говорит, что группу C **исполнитель не чинит** — «написать оценку значит решить, что считается успехом». Поэтому только называю, не переписываю.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. Домов инцидентов пять (`illustracii · lenta · meta · reserch · slajdy`), скиллов теперь девять, и в доме `meta` сидят уже ШЕСТЬ скиллов `disciplina-*` вместо четырёх. Правило «урок едет в дом своего скилла; несколько скиллов → несколько домов» на них по-прежнему не разделяет, и расхождение выросло разрезом. Таблицу «скилл → дом» делает владелец, а не гейт (это записано в `disciplina-priyomka` как открытый вопрос).
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

6. Фраза «выведи документ» отдана `disciplina-priyomka` (там живёт правило «артефакт выводится карточкой, путь от корня, не старше источника»). Холодный ПОСЛЕ-замер согласился с адресом, но пометил его **натянутым**: вне контекста «исполнитель вернулся» фраза читается как «оформи/зарегистрируй документ», и второй претендент — `disciplina-istochnik-pravdy`. Как режется, если владелец имел в виду второе: перенести фразу туда, а `priyomka` оставить «покажи артефакт». Третье чтение — «собери документ генератором `build_doc.py`» — не покрыто НИ ОДНИМ из девяти скиллов, и это не адрес, а дыра.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

7. Фраза «проверь перед коммитом» формально имеет один адрес (`disciplina-istochnik-pravdy`), но держится он на отводе в описании `disciplina-git`, который содержит ту же формулировку («НЕ для вопроса „что проверить перед коммитом"»). При чисто лексическом сопоставлении `git` поднимется рядом. Механически хрупко; лечится либо отводом без повтора фразы, либо машинной проверкой уникальности триггеров.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina/KOLLIZII-opisanij.md` — открыть в редакторе markdown; читать **раздел 4** (разрешение, дата данных 2026-08-11), разделы 1–3 оставлены как показ 06.08.
Четыре файла, ради которых заход и был:
`/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-zahod/SKILL.md`
`/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-priyomka/SKILL.md`
`/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-handoff/SKILL.md`
`/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/disciplina-arka/SKILL.md`
⚠ Всё это лежит на ветке `zahod/razdelenie-skillov` репозитория `disciplina`, не на `main`.

**КОММИТ:** `c3befd3` — «skills: disciplina-zahody -> zahod + priyomka, arka -> arka + handoff; девять описаний сведены по МОМЕНТУ; живые инструменты; KOLLIZII пересчитаны» · 15 файлов, +693/−600 · рабочее дерево `disciplina` после коммита пусто (`git status --porcelain` → пусто).
🔴 **Коммит сделан голым `git --no-optional-locks add` + `commit -- <пути>` (§4), а НЕ инструментом**, как велит блок «МЕСТО РАБОТЫ»: у `disciplina/tools/git_zona.py` единственная подкоманда — `vyvezti`, `commit` там не существует. Развилка названа уроком фабрике, не обойдена молча.

### Что сделал и ЗАЧЕМ

**Ш1 — разрез на три новых скилла.** `disciplina-zahody` (425) → `disciplina-zahod` (написание, всё до запуска) + `disciplina-priyomka` (всё после возвращения). `disciplina-arka` (223) → `disciplina-arka` (жизнь арки) + `disciplina-handoff` (конец сессии и вход в следующую). Граница везде режется по МОМЕНТУ, не по слову. Вместе со скиллом переехал его вспомогательный файл: `references/gigiena-peredachi.md` из `arka` в `handoff` — он про документ передачи, а ссылки скилл→скилл в этом репозитории заведомо не резолвятся (`KAK-ZAVESTI-SKILL §9`).
Перекрёстная связка, которая рвётся при разрезе, поставлена явно в обе стороны: «клаузы критерия формулируются в `disciplina-zahod`, проверяются в `disciplina-priyomka`» и обратная. По прямому слову владельца в `disciplina-priyomka` заведён **шаг 0½** — влито/не влито, согласуется ли новое со старым и **встал ли новый инструмент на место грепом по живому файлу, а не «коммит виден»**.

**Ш1½ — сокращение.** Резались курсивные абзацы «*Цена …*» и повторы; формулировки правил и то, чем они краснеют, не резались. В шапке каждого из четырёх файлов стоит адрес канона, куда ушла развёрнутая история.

**Ш2 — догнал до живых инструментов.** Проверял не по прозе захода, а по коду (см. `## ПЛАН` П1): пороги `--finalizirovano ≥3` и `--vopros ≥3`, регулярка отсечки на СЕГОДНЯШНЮЮ дату, `--finalizirovano ≥2` у `bootstrap_zahod.py`, `FINALIZIROVANO_RE` у З11, `check_marker.py`, `cmd_vyvezti`. Для `vyvezti` завёл строку в таблице дверей `disciplina-git` и **пересчитал число живых подкоманд командой** (`--help` → 16, было заявлено 15), а не рукой.

**Ш3 — свёл все девять описаний.** Каждое несёт момент · живые формулировки владельца · «НЕ для … — это `<имя>`». Замер сделан дважды свежими субагентами, вслепую (только `name` + `description`, тела не читались).

**Ш4 — пересчитал `KOLLIZII-opisanij.md`:** раздел 4 «было → стало» по всем четырём внутренним коллизиям, плюс четыре НОВЫХ стыка, которых до разреза не существовало, плюс статус внешних.

### Критерий готовности — восемь проверок, вывод команд

**1. Девять скиллов, имя папки = поле `name`** — ✅
```
$ ls -1 skills/ | wc -l        → 9
$ ls -d skills/disciplina-zahody → ls: skills/disciplina-zahody: No such file or directory   (rc=1, как и ожидалось)
disciplina-arka             | disciplina-arka             | СОВПАДАЕТ
disciplina-git              | disciplina-git              | СОВПАДАЕТ
disciplina-handoff          | disciplina-handoff          | СОВПАДАЕТ
disciplina-istochnik-pravdy | disciplina-istochnik-pravdy | СОВПАДАЕТ
disciplina-priyomka         | disciplina-priyomka         | СОВПАДАЕТ
disciplina-zahod            | disciplina-zahod            | СОВПАДАЕТ
illustracii                 | illustracii                 | СОВПАДАЕТ
reserch-cikl                | reserch-cikl                | СОВПАДАЕТ
slajdy                      | slajdy                      | СОВПАДАЕТ
```

**2. 🔴 ПОТЕРЬ ПРИ РАЗРЕЗЕ И СОКРАЩЕНИИ НЕТ — двумя верификаторами, по разрезу каждый.** Два свежих субагента, читали ОРИГИНАЛЫ из `git show HEAD:…`, единица сверки — ПРАВИЛО, а не заголовок; про то, что я перенёс, им не сообщалось.

| разрез | охват | вердикт верификатора (ДО починки) |
|---|---|---|
| `zahody` → `zahod` + `priyomka` | **сплошь 1–425 из 425** (три окна) | правил 165, нашли дом 162, снято сознательно 0, **потеряно 3**; 165 = 162 + 0 + 3 |
| `arka` → `arka` + `handoff` | **сплошь 1–223 из 223** | правил 88, нашли дом 84, снято сознательно 3, **потеряно 1**; 88 = 84 + 3 + 1 |

Обе выборки сплошные и целые — гейт «не менее трети» перекрыт втрое.
**Все четыре потери возвращены тем же ходом, ни одна не признана несущественной:**
- «Метрика — КАЧЕСТВО. Часы — норма» (шапка скелета захода) → `disciplina-zahod`, `grep -c 'часы — норма'` → 1;
- «для курс-проекта рамка `SPEKA.md` читается ПЕРВОЙ» → там же, `grep -c 'SPEKA.md'` → 1 (уцелевала только приёмочная половина «рамка соблюдена» — то есть краснеть было чему, а предписания уже не было);
- «Сохранять по умолчанию» (§1 дисциплины) → там же, `grep -c 'сохранять по умолчанию'` → 1;
- `bootstrap_arka.py` копирует `_TEMPLATE-arka/`, и им же заводится первая арка-пример → `disciplina-arka`, `grep -c '_TEMPLATE-arka'` → 1.
Плюс верификатор нашёл разорванную в одну сторону перекрёстную ссылку (арка объявляет `priyomka` домом правила о вердиктах, а `priyomka` арку не называла) — дописана, `grep -c 'disciplina-arka' skills/disciplina-priyomka/SKILL.md` → 1.
**Состояние после починки: потеряно 0 по обоим разрезам** — по четырём адресным грепам выше, а не по повторному прогону верификаторов; это названо честно, полного второго прохода я не гонял.

**3. Каждое описание несёт разграничение** — ✅
```
$ grep -lE 'НЕ для|НЕ активируется|не я —|не этот скилл' skills/*/SKILL.md | wc -l   → 9
```

**4. Лист живой речи разведён — 17 фраз, замер ДО и ПОСЛЕ свежими субагентами вслепую**

| # | фраза | БЫЛО (7 описаний) | СТАЛО (9 описаний) |
|---|---|---|---|
| 1 | «собери заход» | disciplina-zahody | **disciplina-zahod** |
| 2 | «отправь в код» | disciplina-zahody | **disciplina-zahod** |
| 3 | «напиши промпт для Claude Code» | disciplina-zahody | **disciplina-zahod** |
| 4 | «прими отчёт» | disciplina-zahody | **disciplina-priyomka** |
| 5 | «заход вернулся» | disciplina-zahody | **disciplina-priyomka** |
| 6 | «сделай хэндофф» | disciplina-arka | **disciplina-handoff** |
| 7 | «закроем сессию» | disciplina-arka | **disciplina-handoff** |
| 8 | «передача» | disciplina-arka | **disciplina-handoff** |
| 9 | «что дальше делаем» | disciplina-arka | **disciplina-handoff** |
| 10 | «запиши в дневник» | 🔴 ДЫРА | **disciplina-arka** |
| 11 | «выведи документ» | 🔴 ДЫРА | **disciplina-priyomka** (натянуто — см. `## ВОПРОСЫ` п.6) |
| 12 | «по ритуалу» | 🔴 КОЛЛИЗИЯ ×3 | **disciplina-priyomka** |
| 13 | «контекст кончается» | disciplina-arka | **disciplina-handoff** |
| 14 | «завожу арку» | disciplina-arka | **disciplina-arka** |
| 15 | «проверь перед коммитом» | 🔴 КОЛЛИЗИЯ ×2 | **disciplina-istochnik-pravdy** (хрупко — п.7) |
| 16 | «закоммить» | disciplina-git | **disciplina-git** |
| 17 | «куда это записать» | disciplina-istochnik-pravdy | **disciplina-istochnik-pravdy** |

**Счёт: было — один адрес 13 из 17, коллизий 2, дыр 2. Стало — один адрес 17 из 17, коллизий 0, дыр 0.**
🔴 **И «13 из 17» на входе льстило.** Пять из тринадцати «однозначных» вели в ОДИН `disciplina-zahody`, ещё пять — в ОДИН `disciplina-arka`: адрес был один не потому, что граница проведена, а потому что альтернативы не было. Сегодня те же десять фраз ведут в четыре разных скилла и по-прежнему в один каждая — это и есть содержание работы, а не число 17.
⚠ **Промежуточный ПОСЛЕ-замер дал 16 из 17 и одну коллизию:** «что дальше делаем» ловилось и `disciplina-arka` через английское "what's next". Починено снятием "what's next" и «что дальше по плану» у арки (у неё остались «где мы сейчас», «на какой мы стадии», "where are we now"); таблица выше — состояние после починки, проверенное грепом на уникальность каждой фразы по девяти файлам.

**5. Скиллы описывают ЖИВЫЕ инструменты — все шесть грепов ≥1, обратный = 0** — ✅
```
grep -c finalizirovano   disciplina-handoff → 4        grep -c 'РАЗНЕСЕНО'  disciplina-handoff → 2
grep -c finalizirovano   disciplina-zahod   → 2        grep -c 'собран bootstrap_zahod' → 1
grep -c 'З11'            disciplina-zahod   → 1        grep -c 'vyvezti'    disciplina-git    → 2
grep -c 'изменилось со вчера' disciplina-handoff → 0   ← обратный греп, обязан быть 0
```
*(правило про снятый вопрос-пустышку в скилле есть, но сформулировано без устаревшей строки — иначе гейт краснел бы на её упоминании.)*

**6. `KOLLIZII-opisanij.md` пересчитан** — раздел 4 покрывает все четыре внутренние (А1–А4), у каждой назван итог и то, чем он закреплён. **Жёстких внутренних осталось 0.**
🔴 **Поправка к самому показу, найденная при разрешении:** А1 была помечена «жёсткая (буквальное совпадение)» — буквального совпадения не было ни на момент разбора. `git show HEAD:skills/disciplina-git/SKILL.md | sed -n '1,16p' | grep -c 'проверь перед коммитом'` → **0**, у `istochnik-pravdy` → 1. То есть **жёстких внутренних коллизий не существовало уже 06.08**, все четыре были смысловыми. На работу это не влияет, но метку типа в разделе 1 читать как измеренную нельзя — оговорка вписана в файл.

**7. 🔴 ОБЪЁМ УПАЛ — числом** — ✅ обе планки
```
$ wc -l  (после)                          было: zahody 425 + arka 223 = 648
     239 skills/disciplina-zahod/SKILL.md
     144 skills/disciplina-priyomka/SKILL.md
     112 skills/disciplina-handoff/SKILL.md
     147 skills/disciplina-arka/SKILL.md
     642 total
```
- сумма четырёх **642 < 648** ✅ — но запас всего **6 строк**, и он честно мал: четыре шапки вместо двух дают +40 строк накладных сами по себе, то есть тело сократилось примерно на 46 строк при неизменном числе правил;
- ни один из четырёх не длиннее 250: максимум **239** (`disciplina-zahod`) ✅.
⚠ **Куда упереться дальше и что мешает.** `disciplina-zahod` держит 239 строк потому, что несёт две самые плотные машины захода — семь клауз критерия (с четырьмя дробными) и семь вопросов гейта входа. Резать их без потери правила нечем: каждая клауза несёт собственное «чем краснеет». Дальнейшее сокращение — это уже следующее ДРОБЛЕНИЕ (курс владельца, п.4 финализации), а не редактура: кандидаты — «критерий готовности» и «гейт входа» отдельными скиллами либо `references/`.

**8. 🔴 ЖИВОЙ ПРОГОН** — вывод дословно:
```
$ claude plugin details disciplina@disciplina
Component inventory
  Skills (7)  disciplina-arka, disciplina-git, disciplina-istochnik-pravdy, disciplina-zahody, illustracii, reserch-cikl, slajdy
  Always-on:   ~2,934 tok   added to every session
```
**`Skills (7)` со старым составом — это ожидание, названное заранее, а не провал:** плагин читает `main` на GitHub, работа лежит в ветке. Расхождения с предсказанием нет. После влития и `claude plugin marketplace update disciplina` там должно стать **`Skills (9)`** — не 8, как сказано в п.8 финализации: скиллов прибавилось два (`zahody` → `zahod` + `priyomka`, `arka` → `arka` + `handoff`).

🔴 **Разночтение по ЦЕНЕ, ради которой заход и делался.** Живой `claude plugin details` 2026-08-11 печатает on-invoke не те числа, что стоят в п.2 финализации: `disciplina-zahody` **~37.2k** (в заходе 19.3k), `disciplina-git` ~24.1k (12.5k), `slajdy` ~19.2k (9.9k) — все примерно вдвое выше, отношение между скиллами то же. Решение это не меняет (`zahody` был и остался самым тяжёлым), но планка «~45 токенов на строку» на живом замере равна ~87. Замер после влития покажет настоящую экономию — сейчас его снять неоткуда.

### Как проверил
Каждое утверждение выше — вывод команды, а не пересказ. Отдельно: предпосылки Ш2 сверены с ИСХОДНЫМ КОДОМ инструментов (`sys.exit` при `len(...) < 3`, `OTSECHKA_RE`, `FINALIZIROVANO_RE`, `cmd_vyvezti`), а не с прозой захода; число подкоманд `git_zona.py` взято из `--help`; ложный зелёный гейта Г1 доказан МУТАЦИЕЙ, а не рассуждением.

### Гигиена (Г1–Г6)
- **Г1. Зона доехала в git — ✅, но гейт в форме из §4.1 НЕ ГОДИТСЯ, и это доказано.** `git_zona.py check --zone ../disciplina/skills/` из `materials` печатает ✅ rc=0 даже при untracked-файле в зоне (мутационная проба). Настоящая проверка — в самой `disciplina`: `git --no-optional-locks status --porcelain` → **пусто** по всем трём путям зоны. Подробности и цена — в `## УРОКИ ФАБРИКЕ`.
- **Г2. Второй репозиторий — ПРИМЕНИМ, вопреки печати генератора.** Заход объявил пункт неприменимым «потому что все пути зоны лежат внутри `materials`» — это неверно, вся зона лежит в `disciplina`. Команда прогнана: `cd ~/Documents/GitHub/disciplina && git --no-optional-locks status --porcelain` → пусто.
- **Г3. Невлитых веток не прибавилось… кроме моей собственной, и это законно.** В `disciplina` было 2 (`zahod/fazy-1-2-dovodka`, `zahod/format-kartochki-faza-1` — обе трогают только `skills/slajdy/**`, моих файлов не касаются), стало 3: добавилась `zahod/razdelenie-skillov`, которую вливает приёмка. В `materials` — 2, не менялось (я там не коммитил вовсе).
- **Г4. Новый инструмент — неприменимо: ни одного `.py` заход не завёл.** Правка `evals.json` — данные, не инструмент.
- **Г5. Новый `.md` зарегистрирован — неприменимо в объявленной форме, и это названо, а не умолчано.** Заведены три новых `SKILL.md`; регистрации они не требуют — семь живых `SKILL.md` в `README.md §6` не зарегистрированы ни один, там стоят только `NEPOKRYTOE.md` и корневые доки. `register_doc.py` я не звал: единственный файл, чья регистрация ПОСТРАДАЛА (`NEPOKRYTOE.md` сменил папку вместе со скиллом), лежит в `README.md` — **вне зоны**, и запрет зоны сильнее добровольной регистрации. Пунктом очереди, `## ВОПРОСЫ` п.1.
- **Г6. В коммите нет чужих путей — ✅.** `git show --stat` даёт ровно 15 файлов: `KOLLIZII-opisanij.md` и `skills/**`. Ничего вне зоны.

### Что НЕ трогал
`materials` — **ни одного коммита, ни одной правки** кроме этого файла-захода (его коммитит приёмка). Каноны `_meta/disciplina/**` не тронуты. Вне зоны `disciplina` не тронуты: `README.md`, `tools/`, `doma/`, `arhiv-skillov/`, `zagotovki/`, `.claude-plugin/`, `zhurnal/` (правок не потребовалось). Ветку не вливал. Скиллы `slajdy` и `reserch-cikl` — только описание, тела не трогал.

### НЕОБРАТИМОЕ — списком
- **Папка `skills/disciplina-zahody/` перестала существовать** (`git mv` → `disciplina-zahod`, вместе с `DOLG.md`, `NEPOKRYTOE.md`, `evals/`) · восстанавливается `git checkout c3befd3^ -- skills/disciplina-zahody`.
- **`skills/disciplina-zahody/SKILL.md` (425 строк) переписан** в `disciplina-zahod/SKILL.md` (239) · оригинал — `git show c3befd3^:skills/disciplina-zahody/SKILL.md`.
- **`skills/disciplina-arka/SKILL.md` перезаписан** (223 → 147) · оригинал — `git show c3befd3^:skills/disciplina-arka/SKILL.md`.
- **`references/gigiena-peredachi.md` перемещён** из `disciplina-arka/` в `disciplina-handoff/` (папка `disciplina-arka/references/` удалена как пустая) · `git checkout c3befd3^ -- skills/disciplina-arka/references`.
- **Описания пяти соседних скиллов перезаписаны** (`git`, `istochnik-pravdy`, `illustracii`, `reserch-cikl`, `slajdy`) · оригиналы там же, в `c3befd3^`.
- **Поле `skill_name` в `disciplina-zahod/evals/evals.json`** изменено `disciplina-zahody` → `disciplina-zahod`.
- **Временный файл `skills/__PROBA-Г1.md`** создан для мутационной пробы гейта и **удалён тем же ходом**; в git не входил, рабочее дерево после снятия чисто.
Всё перечисленное лежит внутри объявленной зоны и откатывается одним коммитом-родителем `c3befd3^`.

### ПОВТОРЯЕМОСТЬ находок
**Повторятся на следующей же единице работы — это заходы ДО следующего прогона, а не пункты очереди:**
- **ложный зелёный Г1** сработает на КАЖДОМ кросс-репозиторном заходе, а их теперь класс, а не случай (`materials` ↔ `disciplina`); цена каждого раза — принятая приёмкой незакоммиченная зона;
- **«невлитых веток 0», измеренное не в том репозитории** — тот же класс и тот же генератор; следующий кросс-репо заход получит то же ложное утверждение в §0.1;
- **контракт зоны, называющий несуществующую подкоманду инструмента**, — повторится в любом заходе в `disciplina`, пока генератор печатает `tools/git_zona.py` как дверь коммита.
**Не повторятся — законно уходят пунктами очереди в `## ВОПРОСЫ`:** мёртвая строка `README §6`, «7 скиллов» в README, `PARY` в `polnota_perenosa.py`, оценки новых скиллов, таблица «скилл → дом», два хрупких адреса фраз.

### Открытое «возвращаться»
- **Влитие и обновление плагина делает владелец** — заход ветку не вливал сознательно (так велит контракт): `git_zona.py merge` в `disciplina` + `claude plugin marketplace update disciplina`, ожидание `Skills (9)`.
- **Замер экономии токенов снимается только после влития** — сейчас плагин читает `main`, где лежит старый состав.
- **`disciplina-zahod` на 239 строках — следующая цель дробления,** и кандидаты названы выше (критерий готовности · гейт входа).

**Время прогона + токены — НЕПРИМЕНИМО:** канал `app`, лога прогона не существует (`tee` есть только на канале `terminal`).
