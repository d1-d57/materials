# Канал исполнителя — dveri (один заход до конца)
<!-- собран bootstrap_zahod.py -->
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Sonnet** — механика расписана до флагов и мест правки, развилок нет; abort и фикстуры ловят сбой.

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
Область: «_studio/zhurnal/2026-08-11_dveri-discipliny» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/modest-pensive-bardeen/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Репозиторий `materials` — фабрика курсов; аналитик работает в Cowork (песочница, в `.git` не пишет), исполнитель — Claude Code. Прошлый этап: за одну сессию трижды нарушилась одна и та же дисциплина — хэндофф написан прозой мимо `sdelat_handoff.py`, два захода написаны руками мимо `bootstrap_zahod.py`, заход выдан без стартового блока. Диагноз владельца шире: **любой шаг дисциплины, который живёт ВНУТРИ сессии Cowork, пропускается систематически** — интервью перед заходом, интервью перед хэндоффом, отсечка дневника, прогон захода перед отправкой. Причина структурная: всё, что краснеет в этом репозитории, живёт в коммите, а аналитик не коммитит.

ЦЕЛЬ: превратить три пропускаемых шага из ПРАВИЛА в ДВЕРЬ — сделать их обязательными параметрами генераторов, без которых артефакт не собирается. Правило можно не вспомнить; отсутствующий обязательный флаг вспоминать не надо — скрипт откажется работать.
Приёмка — по ОТЧЁТУ, без построчной сверки. 🔴 **СТОП ДО ЦЕЛИ, называю громко:** этот заход закрывает только ДВЕРИ (обязательные параметры генераторов). Он **НЕ** трогает `.githooks/pre-commit` и **НЕ** строит запрет на заход, написанный руками, — это отдельный второй заход, который опирается на маркер, который ты здесь поставишь. То есть после тебя рукописный заход по-прежнему пройдёт молча; закроется это следующим заходом.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ (2026-08-10, владелец подтвердил)

*Секция вписана Cowork РУКАМИ — флага `--finalizirovano` ещё нет, его заводит этот самый заход. После Ш2.1 её будет вшивать генератор, и рукописной она больше не будет.*

1. **Диагноз шире брифа.** Пропускается не «генератор», а любой шаг дисциплины внутри сессии Cowork: интервью до захода, интервью до хэндоффа, интервью в начале сессии, запись в дневник, проверка дневника, прогон захода перед отправкой.
2. **Механизм — дверь, не гейт.** Гейт «аналитик обязан прогнать скрипт» — то же правило-надежда, что и «аналитик обязан позвать генератор»: скрипт можно не позвать, регресс бесконечный. Поэтому шаг становится обязательным ПАРАМЕТРОМ генератора: артефакт без него не собирается.
3. **Лазейки не проектируем.** Владелец: «зачем нам делать лазейку? Во всех ситуациях можно сделать универсальные заходы. Находя проблемы с заходами, систематически будем через генератор править». Громкий обход уже существует и достаточен — `--no-verify`, который надо напечатать руками.
4. **Интервью не стандартизуется.** Вопросы осмысленные, из контекста; шаблонные удаляются. Продукт интервью — финализация («что именно решили»), а не список заданных вопросов.
5. **Объём — два захода подряд.** Этот — двери; следующий — запрет рукописного захода в `pre-commit`, он опирается на маркер отсюда.

🔴 **Объявленная слепая зона (не прячу):** дверь не срабатывает, если аналитик до неё не дошёл. Сессия, в которой не собрали ни захода, ни хэндоффа, не охвачена ничем. Лечения у этого в коде нет.

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** **рабочая папка `../materials-wt/dveri`** (рядом с репозиторием) — ТОЛЬКО ДЛЯ КОДА, worktree захода на ветке `zahod/dveri-discipliny`. 🔴 **Заводишь её САМ, первым ходом, из корня репозитория:**
  ```
  python3 _generator/tools/git_zona.py worktree add dveri --branch zahod/dveri-discipliny
  cd ../materials-wt/dveri
  ```
  Папка уже есть — не пересоздавай, просто перейди: команда идемпотентна, «уже существует» не ошибка и не повод останавливаться. 🔴 `git checkout` в основной папке ЗАПРЕЩЁН: рядом идут другие заходы, переключение подменит файлы у них под ногами. 🔴 **Сам файл-заход (этот `.md`) остаётся в ОСНОВНОЙ папке репозитория** — один экземпляр, не копия: ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ пишешь в него по абсолютному пути из стартовой строки, а сам файл НЕ коммитишь — это делает аналитик при приёмке. 🔴 **Ветку в конце НЕ вливай** — влитие делает приёмка, у неё свои гейты.
- **ЗОНА (можно менять):** `_generator/tools/`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО (три слота, пустых быть не должно)

**1. ВЛИТЬ ПЕРЕД РАБОТОЙ.** Без этого инструмента, которым заход работает, на диске может не быть вовсе — а выглядит это как «файла нет», не как «ветка не влита».

вливать нечего, проверено командой `git branch --no-merged` при сборке: невлитых веток 0.

**2. ЗАКОММИТИТЬ ПО ХОДУ.** Что заход обязан забрать в git — включая правки аналитика: он в песочнице в `.git` не пишет и закоммитить их сам не может (`GIT-disciplina.md §2`). Точный список снимать КОМАНДОЙ (`git_zona.py check`), а не по перечню отсюда: он мог устареть.

- правки _generator/tools/*.py и фикстур своей зоны

**3. ЗАКРЫТЬ ПОСЛЕ.** Какие ветки и рабочие папки гасятся (`git_zona.py poteri --branch <в>` → `zakryt-vetku`, `rc=1` — не закрывать) и какие трогать НЕЛЬЗЯ: рядом может идти чужой заход, и его папка под ним исчезнет.

- ветку zahod/dveri-discipliny после приёмки

- 🔴 **Заведи рабочую папку и перейди в неё** (команды в блоке «МЕСТО РАБОТЫ» выше). Уже есть — просто `cd`, это не ошибка.
- Проверить, что на месте: `git rev-parse --abbrev-ref HEAD` → должно быть `zahod/dveri-discipliny`. Не оно — ты не в своей папке.
- ПЛАН/ВОПРОСЫ/ОТЧЁТ/УРОКИ ФАБРИКЕ пиши в ЭТОТ файл — он в основной папке, не копируй его в рабочую.
- Точка отката: `git add `_generator/tools/`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, в этом порядке. Проект не изучай, по сторонам не смотри:
  1. `_generator/tools/sdelat_handoff.py` — весь (≈250 строк). Докстринг называет, ЗАЧЕМ файл существует, словами владельца; в теле — `main()` с `add_argument` и сборка списка `L`, куда вшиты четыре шаблонных вопроса интервью.
  2. `_generator/tools/bootstrap_zahod.py` — `main()` и функция, собирающая шапку файла-захода. Целиком читать не обязательно (файл большой), но **найди грепом**, где формируются первые строки результата: `grep -n "Канал исполнителя" _generator/tools/bootstrap_zahod.py`.
  3. `_generator/tools/check_zahod.py` — докстринг целиком (два закона + объявленное исключение З10) и список проверок. **Закон 2 — калибровка — определяет, как тебе добавлять новую проверку;** прочитай его прежде, чем писать код.
  4. `_generator/tools/fixtures/sessiya/PROGNAT.sh` — фикстура, объявившая охват над `sdelat_handoff.py`. Смотри, как устроены её ловушки: свои добавляешь той же идиомой.
  5. `_generator/tools/fixtures/zahod/PROGNAT.sh` — то же для `check_zahod.py`; в ней живёт ловушка 1 (эталоны молчат) и ловушка 24 (З10 на них краснеет истинно). **Твоя З11 — третий житель этой же развилки.**
  6. `_generator/tools/check_tool_contract.py` — только докстринг: он объясняет, почему у каждого правленого инструмента обязана быть зелёная фикстура.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
Четыре шага. Все — в `_generator/tools/`; `.githooks/` и `_studio/` НЕ трогать.

---

### Ш1. `sdelat_handoff.py` — интервью и отсечка дневника становятся предусловием

**Ш1.1. Новый ОБЯЗАТЕЛЬНЫЙ флаг `--finalizirovano`** (`action="append"`, минимум **3** позиции; меньше — `sys.exit` с внятным текстом). Смысл — не «вопросы, которые аналитик задал», а **что владелец подтвердил**. Печатается в хэндофф отдельной секцией `## 🔴 ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ` сразу ПОСЛЕ блока стартовой строки и ДО секции «ПЕРВЫЙ ХОД».

*Зачем именно так, словами владельца (не пересказывай — это причина, из которой следует форма):* «у нас уже всё это сбилось, очень долгое обсуждение, часто извилистое, и в итоге нет места, где мы финализируем, что именно хотим сделать. И оно куда-то съезжает». Интервью — это **предохранитель-финализация**, а не опросник.

**Ш1.2. Новый ОБЯЗАТЕЛЬНЫЙ флаг `--vopros`** (`action="append"`, минимум **3**) — вопросы для интервью в НАЧАЛЕ следующей сессии. Пишет их аналитик под конкретный контекст.

**Ш1.3. УДАЛИТЬ четыре вшитых шаблонных вопроса** из сборки списка `L` (сейчас это строки вида `f"1. Задача сессии — **{args.chto_delaem}**. Верно?"` … `"4. Что-то изменилось со вчера, чего я не знаю?"`). На их место — нумерованный список из `--vopros`. Требование «формат да/нет» и цитата владельца про да/нет **остаются** в тексте секции.
*Почему удаляем, а не оставляем как дефолт:* владелец про четвёртый вопрос дословно — «бессмысленно, я всегда отвечаю „не изменилось“». Шаблонный вопрос, на который всегда один ответ, — это ритуал, изображающий интервью, и он вытесняет настоящее. Дефолт был бы использован всегда, потому что он бесплатен.

**Ш1.4. ПРЕДУСЛОВИЕ «дневник разнесён».** Перед сборкой файла скрипт читает `<арка>/SESSIYA.md` и ищет отсечку `<!-- РАЗНЕСЕНО ДО СЮДА: <ГГГГ-ММ-ДД> -->` с датой **сегодняшней**. Нет отсечки или дата старее сегодняшней → **файл не собирается**, `sys.exit(1)`, текст отказа называет: какой файл, что искалось, и команду порядка закрытия сессии (`zakryt_sessiyu.py` → разнести → отсечка → интервью → `sdelat_handoff.py`). Файла `SESSIYA.md` нет вовсе → тоже отказ, с этим же текстом.
⚠ **Развилка, реши сам и назови решение в `## ПЛАН`:** нужен ли аварийный флаг вида `--bez-otsechki "<причина>"`. Мой совет — **не делать**: универсальный громкий обход уже есть (`--no-verify` на коммите), а дешёвый флаг с причиной будет использоваться всегда, потому что ничего не стоит. Считаешь иначе — скажи ДО работы, с аргументом.

### Ш2. `bootstrap_zahod.py` — интервью и маркер сборки

**Ш2.1. Новый ОБЯЗАТЕЛЬНЫЙ флаг `--finalizirovano`** (`action="append"`, минимум **2**). Вшивается в собираемый файл секцией `## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ` — сразу под абзацем `КОНТЕКСТ.` и НАД `## КОНТРАКТ ЗОНЫ`.

**Ш2.2. Маркер сборки** `<!-- собран bootstrap_zahod.py -->` — вписывается **третьей строкой файла** (по образцу `sdelat_handoff.py:125`, где такой маркер уже живёт и уже работает: он отличает «мой файл» от рукописного при `--zamenit`).

🔴 **Маркер вписывает КОД, а не шаблон.** В `_studio/zhurnal/_TEMPLATE-zahod.md` его добавлять НЕЛЬЗЯ, и это не вкусовщина: маркер в шаблоне копируется вместе с шаблоном, то есть рукописный заход получит его даром — и весь смысл маркера (отличить сборку от руки) исчезнет молча. Инъекция кодом — та же идиома, которой уже вписывается блок §0.1 ГИТ-КОНТУР. Шаблон вне твоей зоны и трогать его не надо.

### Ш3. `check_zahod.py` — проверка З11

Новая проверка **З11**: в тексте захода есть секция `## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ` и она непуста (не плейсхолдер `<...>`, не пустая строка). Жёсткая (красит `exit 1`), не печать-предупреждение.

🔴 **Тут ты упрёшься в закон 2 докстринга (калибровка) — прочти его ДО кода.** Два эталонных захода (`kod_tirazh-ocheredi.md`, `kod_ochered-domov.md`) написаны до появления секции, значит З11 на них покраснеет. Это красное **истинное**, ровно как у З10, и лечится тем же способом, каким З10 уже вылечен: **объявленным исключением в докстринге + ловушкой фикстуры**, которая требует от эталонов молчания по всем проверкам КРОМЕ З10 и З11, и использует их же положительным контролем на З11. **Не смягчай З11 и не переписывай эталоны** — эталон правят только по решению владельца, а смягчённая проверка не увидит того, ради чего заведена.

### Ш4. Фикстуры — на каждый правленый инструмент

Охват объявлен в `# TOOL-CONTRACT-COVERS:`; правленые тобой файлы покрывают: `fixtures/sessiya/` (→ `sdelat_handoff.py`), `fixtures/zahod/` (→ `check_zahod.py`), `fixtures/gigiena/`, `fixtures/register_doc/`, `fixtures/korni/`, `fixtures/git_zona/`, `fixtures/dvizhok/` (→ `bootstrap_zahod.py`). Допиши ловушки на новое поведение (отказ без флага · отказ без отсечки · маркер на месте · З11) и прогони их все до зелёного.

---

**ЗАМЕРЫ «ЗАДАЧА УЖЕ СДЕЛАНА?» — сняты командами на живых файлах, дата данных 2026-08-10.** Если у тебя выйдет иначе — значит между сборкой захода и запуском кто-то работал; скажи в `## ПЛАН` и не гонись за фантомом.

```
grep -rc 'finalizirovano' _generator/tools/*.py | grep -v ':0' | wc -l   → 0
grep -c -- '--vopros' _generator/tools/sdelat_handoff.py                 → 0
grep -c 'изменилось со вчера' _generator/tools/sdelat_handoff.py         → 1
grep -c 'собран bootstrap_zahod' _generator/tools/bootstrap_zahod.py     → 0
```

🔴 **Испытывай на временном материале, НЕ на живых арках.** `bootstrap_zahod.py` и `bootstrap_arka.py` пишут строку регистрации в `_studio/docs/KARTA.md`, а `sdelat_handoff.py` создаёт файл в папке арки — прогон «на живом» оставит мусор вне твоей зоны, который ты же не имеешь права убрать. Идиома подмены (`ZHURNAL_DIR`, временный каталог) уже есть в фикстурах — бери её.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ) — восемь проверок, каждая командой; вывод каждой в `## ОТЧЁТ`:**

1. `python3 _generator/tools/sdelat_handoff.py <врем-арка> --chto-delaem A --gde B --pervyj-hod C --gotovo D` **без** `--finalizirovano` → `rc≠0`, файл НЕ создан (`ls` подтверждает отсутствие).
2. То же **с** `--finalizirovano`×3 и `--vopros`×3, но при `SESSIYA.md` **без сегодняшней отсечки** → `rc≠0`, файл НЕ создан.
3. То же при отсечке `<!-- РАЗНЕСЕНО ДО СЮДА: <сегодня> -->` в `SESSIYA.md` → `rc=0`, файл создан, и `grep -c 'ЧТО ФИНАЛИЗИРОВАНО' <файл>` → `1`.
4. `grep -c 'изменилось со вчера' _generator/tools/sdelat_handoff.py` — ДО работы печатает **1** (замер выше), ПОСЛЕ обязан печатать ноль.
   *Цель написана словом, а число рядом — текущий замер, и это НЕ уловка, а обход названной поломки: `check_sborki.py` С4 сверяет вывод команды с ближайшим числом, предполагая, что критерий описывает МИР СЕЙЧАС. На заходе, который этот мир меняет, предположение ложно, и С4 краснеет на здоровом критерии. Класс записан уроком в `UROKI-FABRIKE.md` этой арки; чинить С4 — не твоя зона и не твоя задача.*
5. `python3 _generator/tools/bootstrap_zahod.py …` **без** `--finalizirovano` → `rc≠0`, `kod_*.md` НЕ создан.
6. Он же **с** флагом → `rc=0`, и на свежесобранном файле: `head -5 <файл> | grep -c 'собран bootstrap_zahod.py'` → **1**, `grep -c 'ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ' <файл>` → **1**.
7. `python3 _generator/tools/check_zahod.py <оба эталона>` → печатает **только** З10 и З11, ни одной другой проверки; на свежесобранном заходе из п.6 — **молчит** (`rc=0`, пустой вывод).
8. Семь фикстур из Ш4 — `sh _generator/tools/fixtures/<имя>/PROGNAT.sh` для каждой → `rc=0`. **Охват называй числом внутри вердикта:** «фикстур прогнано X из 7», а не «фикстуры зелёные».
9. **🔴 ЖИВОЙ ПРОГОН НА РЕАЛЬНОМ РЕПОЗИТОРИИ, не на фикстуре.** Прогнать новый `check_zahod.py` по ВСЕМ живым заходам репозитория, read-only:
   ```
   python3 _generator/tools/check_zahod.py _studio/zhurnal/*/kod_*.md
   ```
   В отчёт — **распределение**, а не вердикт: сколько файлов судимо, на скольких сработала З11, и совпало ли это с ожиданием (ожидание: З11 краснеет на ВСЕХ существующих заходах, потому что секция введена сегодня — это истинное красное, как у З10). **Красное З11 хоть на одном заходе, собранном ПОСЛЕ твоей правки, — ложное, и тогда проверка неверна по построению.** *Фикстура зелёная — этого одного мало: требование владельца «всегда гонять на конкретной практике»; гейт `check_zahod.py` З2 краснеет ровно на критерии без этого пункта.*
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Операция не лоссовая (ничего не переносится и не удаляется, кроме четырёх строк шаблонных вопросов в Ш1.3) — ПОСЛЕ-верификатор не нужен, хватает восьми проверок критерия §2.

🔴 **Но нужен ДО-верификатор, и он идёт ПЕРВЫМ ходом, до первой правки кода.** Один свежий субагент, читающий ТОЛЬКО `_generator/tools/check_zahod.py` (докстринг + список проверок) и оба эталонных захода. Вопрос ему один: **покраснеет ли З11 в формулировке из Ш3 хоть на одном заходе, который признан ХОРОШИМ, кроме двух эталонов?** Ищет он в `_studio/zhurnal/*/kod_*.md`. После работы этот вопрос задать уже нельзя — все новые заходы будут рождаться с секцией, и ложное красное станет невидимым. Ответ — в `## ПЛАН` до кода.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- _generator/tools/                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- _generator/tools/   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `_generator/tools/`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone _generator/tools/` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `materials` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged arka/mat-kostyak` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Ложная предпосылка найдена (§0.1 п.2 «найди грепом»).** `grep -n "Канал исполнителя" _generator/tools/bootstrap_zahod.py` даёт 0 совпадений — не потому что файл большой, а потому что первые строки захода НЕ собираются Python-строками внутри `bootstrap_zahod.py` (в отличие от `sdelat_handoff.py`, который строит вывод списком `L`). `bootstrap_zahod.py` читает внешний шаблон `TEMPLATE = korni.шаблон("_TEMPLATE-zahod.md")` и делает `.replace()` по якорям (`ЯКОРЬ_SCHET`, `ЯКОРЬ_ГИГИЕНА`, `{{ЗОНА}}` и т.д.) — сам текст «# Канал исполнителя…» физически живёт в `_TEMPLATE-zahod.md`, не в `.py`. Чтобы вписать Ш2.1/Ш2.2 тем же приёмом (якорь-замена), пришлось минимально прочитать первые ~40 строк этого шаблона (файл не в списке §0.1, но необходим, чтобы не гадать с текстом якоря — тот же риск, от которого уже отказывает сам генератор строкой «ОШИБКА: якорь … встречается N раз»). Правки самого шаблона НЕ будет — только чтение, контракт это разрешает («Шаблон вне твоей зоны и трогать его не надо» — про правку, не про чтение).

Из прочитанного подтверждено: заголовок `## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)` — литеральный, не подставляется параметром → безопасный якорь для Ш2.1 (секция ЧТО ФИНАЛИЗИРОВАНО вставляется ПЕРЕД ним, что и есть «под КОНТЕКСТ., над КОНТРАКТ ЗОНЫ», так как между ними в шаблоне только пустая строка). Для Ш2.2 (маркер — третья строка файла) якорь — первые две строки шаблона («# Канал исполнителя — {{ТЕМА}}…» + первая строка цитаты «Твой единственный файл-заход…»); маркер вставляется между ними и оставшимися строками цитаты. Это буквально режет один блок-цитату надвое (HTML-комментарий не в цитате) — визуально минорный артефакт, но исполняет инструкцию дословно; альтернативы (маркер после 4-й строки, после заголовка) не совпадают со словом «третьей», а гадать не буду — если владелец имел в виду другое место, поправит на приёмке.

**Открытый риск (флагом, не блокером) по КРИТЕРИЮ п.7.** Докстринг `bootstrap_zahod.py` (у вызова `check_zahod.judge`) сам утверждает: «полный `judge()` красит З2 на КАЖДОМ свежесобранном файле» (плейсхолдер критерия не несёт слова «жив*»/«реальн*»). КРИТЕРИЙ ГОТОВНОСТИ п.7 этого захода ожидает, что полный `check_zahod.py` на свежесобранном файле «молчит». Это может оказаться конфликтом с уже задокументированным поведением генератора. Не гоняюсь за фантомом заранее — проверю эмпирически на живом прогоне (п.5-7) и если разойдётся, назову это в `## ОТЧЁТ` дословно с доказательством, а не подгоню один из двух текстов под другой.

**ДО-верификатор (§3) отработал ДО первой правки кода.** Вопрос: краснеет ли новая З11 (секция `## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ` непуста) хоть на одном заходе, признанном ХОРОШИМ, кроме двух эталонов? Свежий субагент прогнал `grep` заголовка по всем 244 файлам `_studio/zhurnal/*/kod_*.md` — совпадений 0 везде, включая явно принятые/закрытые заходы (`kod_priyomka-mehanizm.md`, `kod_zakryt-arku.md` и др.). Вывод: третьего, «особого», случая нет — все старые заходы одинаково не несут секции, введённой сегодня, и это тот же класс истинного красного, что уже объявлен для З10 (закон калибровки не нарушается, дополнительных исключений сверх двух эталонов не требуется). Субагент не нашёл сам `kod_dveri.md` (искал по неверному месту/некоммиченный файл) — не проблема: это текущий рабочий файл, у него секция ЧТО ФИНАЛИЗИРОВАНО уже вписана вручную (см. выше), он не «старый заход без секции».

**Развилка Ш1.4 (`--bez-otsechki`).** Соглашаюсь с советом захода — флаг НЕ завожу. Громкий обход (`--no-verify` на коммите) уже существует и достаточен; дешёвый флаг с причиной использовался бы всегда, потому что бесплатен (тот же аргумент, каким в тексте объяснена причина удаления четвёртого шаблонного вопроса Ш1.3).

**Порядок работы:** Ш1 (`sdelat_handoff.py`: `--finalizirovano`≥3, `--vopros`≥3, удаление 4 шаблонных вопросов, предусловие отсечки дневника) → Ш2 (`bootstrap_zahod.py`: `--finalizirovano`≥2 якорем перед КОНТРАКТ ЗОНЫ, маркер третьей строкой якорем после первой строки цитаты) → Ш3 (`check_zahod.py`: З11 в HARD_CHECKS, докстринг дополняется объявленным исключением по образцу З10) → Ш4 (ловушки в `fixtures/sessiya`, `fixtures/zahod`; регрессионный прогон `fixtures/gigiena`, `fixtures/register_doc`, `fixtures/korni`, `fixtures/git_zona`, `fixtures/dvizhok` — читаю их непосредственно перед правкой, по мере необходимости, тем же приёмом, что уже виден в двух прочитанных фикстурах) → 9 пунктов КРИТЕРИЯ на временном материале → живой прогон п.9 → коммит зоны (§4).

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `fixtures/git_zona/PROGNAT.sh` красит красным «В КАНОНЕ НЕТ ПОДКОМАНД: vyvezti vyvezti» — подкоманда `git_zona.py vyvezti` (коммит `7b0109d`) не описана в `GIT-disciplina.md`, а сама проверка к тому же дублирует имя (парсит `--help` дважды одним и тем же регэкспом). Проверено на чистом `main` ДО моих правок командой `sh _generator/tools/fixtures/git_zona/PROGNAT.sh` — красное существовало независимо от этого захода; я его не трогал (вне зоны Ш1-Ш4, чужой инструмент).
   ДОМ: `_meta/disciplina/GIT-disciplina.md` (дописать подкоманду) и `_generator/tools/fixtures/git_zona/PROGNAT.sh` (снять двойной подсчёт при извлечении списка подкоманд)
   ДОСТАВЛЕНО: kod_dveri#1 — строка про `vyvezti` вписана в `_meta/disciplina/GIT-disciplina.md` (таблица подкоманд + блок вызовов) при приёмке 2026-08-10; фикстура перегнана ПОСЛЕ правки → `✅ все подкоманды описаны`, `ФИКСТУРЫ ЗЕЛЁНЫЕ`, rc=0. Двойной подсчёт имени в самой проверке НЕ чинён: это правка кода в `_generator/tools/`, уходит пунктом в следующий заход.

## ОТЧЁТ — (заполняет исполнитель)

**Что сделал и зачем.** Три двери вместо трёх правил-надежд (§ЦЕЛЬ):
- Ш1 `sdelat_handoff.py` — `--finalizirovano`/`--vopros` обязательны (мин. 3 каждый, `sys.exit` без них), секция `## 🔴 ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ` печатается между стартовой строкой и «ПЕРВЫЙ ХОД», четыре шаблонных вопроса заменены нумерованным списком `--vopros`, предусловие `proverit_otsechku()` требует в `<арка>/SESSIYA.md` отсечку `<!-- РАЗНЕСЕНО ДО СЮДА: <сегодня> -->` (нет файла / нет отсечки / отсечка старее сегодня — все три `sys.exit(1)` с полным порядком закрытия сессии в тексте отказа). Аварийный флаг `--bez-otsechki` НЕ заведён — решение по совету захода, названо в `## ПЛАН`.
- Ш2 `bootstrap_zahod.py` — `--finalizirovano` обязателен (мин. 2), вставляется якорем `## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)` (мягкий якорь той же породы, что ВОПРОСЫ/УРОКИ/ВЛИТИЕ — `fixtures/dvizhok` кормит генератор мини-шаблоном без этой строки, и это законно). Маркер `<!-- собран bootstrap_zahod.py -->` вписывается третьей строкой файла якорем по первым двум строкам шаблона.
- Ш3 `check_zahod.py` — новая жёсткая проверка З11 (секция ЧТО ФИНАЛИЗИРОВАНО непуста и не плейсхолдер), в `HARD_CHECKS`; докстринг расширен вторым объявленным исключением закона калибровки (были эталоны истинно-красные по З10, теперь и по З11).
- Ш4 — ловушки дописаны в `fixtures/sessiya` (+6: без `--finalizirovano`, `--finalizirovano`<3, без `--vopros`, без отсечки, без файла дневника, отсечка вчерашним числом; плюс проверки содержимого положительного контроля — секция на месте, текст `--vopros` попал, «изменилось со вчера» пропало) и `fixtures/zahod` (+2: ловушка 28 — синтетический заход без секции краснеет по З11 поимённо; ловушка 29 — оба живых эталона краснеют по З11 поимённо, симметрично ловушке 25 для З10). Пять фикстур, где сам `bootstrap_zahod.py` вызывается как инструмент (`dvizhok`, `gigiena`, `register_doc`, `korni`, `git_zona`), донесены до зелёного добавлением `--finalizirovano` в существующие вызовы (9+7+3+1+2 места) — без этого они стали бы красными от МОЕЙ правки, не тестируя то, что заявляют.

**Как проверил.** Восемь пунктов КРИТЕРИЯ §2 — на временном материале (одноразовый git-репозиторий и временная арка, живые заходы не трогал):
1. `sdelat_handoff.py` без `--finalizirovano`/`--vopros` → `rc=1`, файл не создан — ✅.
2. То же с флагами, но `SESSIYA.md` без сегодняшней отсечки → `rc=1`, файл не создан — ✅.
3. То же с отсечкой на сегодня → `rc=0`, `grep -c 'ЧТО ФИНАЛИЗИРОВАНО' <файл>` → `1` — ✅.
4. `grep -c 'изменилось со вчера' _generator/tools/sdelat_handoff.py` → `0` (было `1`) — ✅.
5. `bootstrap_zahod.py` без `--finalizirovano` → `rc=1`, `kod_*.md` не создан — ✅.
6. С флагом → `rc=0`; `head -5 <файл> | grep -c 'собран bootstrap_zahod.py'` → `1`; `grep -c 'ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ' <файл>` → `1` — ✅.
7. **Частично не подтвердилось — говорю прямо, не подгоняю.** `check_zahod.py <оба эталона>` → печатает только `З10`/`З11` поимённо — ✅ подтверждено. Но «на свежесобранном заходе — молчит» НЕ подтвердилось: полный прогон (все `HARD_CHECKS`) красит `З2` («в критерии нет живого прогона») — ожидаемо, это ДОКУМЕНТИРОВАННОЕ, НЕ связанное с моей правкой поведение: докстринг самого `bootstrap_zahod.py` (строки у вызова `check_zahod.judge`) прямо говорит «полный `judge()` красит З2 на КАЖДОМ свежесобранном файле», поэтому генератор и гейтит при сборке только `STRUCTURAL_CHECKS` (З3/З5/З7), не полный набор. Изолированный вызов `check_zahod.check_z11_finalizirovano(text)` на том же свежесобранном файле подтверждает: `[]` (пусто, молчит) — моя правка исправна, ложной была только ожидание в тексте КРИТЕРИЯ. Названо ДО кода в `## ПЛАН`, проверено эмпирически, не гонялся за фантомом.
8. Семь фикстур Ш4 — **6 из 7 зелёных** (`sessiya`, `zahod`, `dvizhok`, `gigiena`, `register_doc`, `korni`); `git_zona` красная по НЕ связанной с заходом, предсуществующей причине (см. `## ВОПРОСЫ` п.1, воспроизведено на чистом `main`).
9. Живой прогон на реальном репозитории: `check_zahod.py _studio/zhurnal/*/kod_*.md` → судимо **244 из 244**, З11 сработала на **244 из 244** (100%) — совпадает с ожиданием («истинное красное, как у З10», секция введена сегодня); ни на одном заходе, собранном ПОСЛЕ правки, красного нет (таких заходов в корпусе пока не появилось — `kod_dveri.md` не в этом дереве, он вне git до приёмки).

**Что НЕ трогал.** `.githooks/pre-commit` — не тронут ни байтом (запрет на рукописный заход — следующий заход, СТОП ДО ЦЕЛИ соблюдён). `_studio/` целиком, `_TEMPLATE-zahod.md` — не тронут ни байтом (только читал, для якорей). `git_zona.py`, `GIT-disciplina.md` — не тронуты (найденный чужой долг ушёл в `## ВОПРОСЫ`, не в правку).

**ДО-верификатор (§3).** Отработал ПЕРВЫМ ходом, до правки кода (см. `## ПЛАН`): ложного красного З11 на «хорошем» заходе, кроме двух эталонов, не нашлось — 244 файла корпуса проверены грепом заголовка, третьего особого случая нет.

**ПОВТОРЯЕМОСТЬ находок.** Ни одна находка не повторится на следующей единице работы без вмешательства: находка `## ВОПРОСЫ`#1 (`git_zona` канон) — разовый долг чужого инструмента, не паттерн, который проявится снова именно на следующем заходе этой серии; находка КРИТЕРИЙ п.7 — свойство ЭТОГО конкретного текста критерия, не фабричный паттерн (уже задокументировано в самом `bootstrap_zahod.py`, ему не нужен отдельный заход). Обе законно уходят в очередь, не в «немедленное».

**НЕОБРАТИМОЕ.** Необратимого нет. Один `--no-verify` на коммите — с причиной, легально по контракту §4, автоматически ушёл строкой в `INCIDENTY.md` самим `git_zona.py commit`; называю его долгом отдельно ниже.

**ДОЛГ (обход хука).** Коммит `449f894` сделан с `--no-verify` — причина: чужой долг `git_zona` (см. `## ВОПРОСЫ` п.1). Обход записан автоматически в `INCIDENTY.md` инструментом `git_zona.py commit`.

**АРТЕФАКТ:** артефакта нет — заход правит инструменты (`.py`/`.sh`), не собирает документ/деку/картинку.

**КОММИТ:** `449f894d08e249d9c135bc00a620d267752767e5` — «_generator/tools: двери дисциплины — finalizirovano, отсечка дневника, маркер сборки, З11» · `git_zona.py check --zone _generator/tools/` → ✅ (проверено после коммита, показано выше).

**Гигиена (§4.1):**
- Г1: `git_zona.py check --zone _generator/tools/` → ✅.
- Г2: неприменимо — все пути зоны внутри `materials`.
- Г3: `git --no-optional-locks branch --no-merged arka/mat-kostyak` → 1 строка (своя же `zahod/dveri-discipliny`, законно — ветка этого захода).
- Г4: новых `.py` не заводил — неприменимо.
- Г5: новых `.md` не заводил — неприменимо, `register_doc.py` не звал.
- Г6: `git show --stat` — только пути `_generator/tools/`, чужих нет (проверено грепом, 0 совпадений вне зоны).
