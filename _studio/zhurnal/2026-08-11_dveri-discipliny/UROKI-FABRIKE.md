# УРОКИ ФАБРИКЕ — арка «2026-08-11_dveri-discipliny»

> **Это не дневник и не свалка. Это ВХОД для отдельной сессии, которая при закрытии арки правит канон.**
> Самоценен: сессия читает ТОЛЬКО его и канон — `SESSIYA.md` открывать не должна.
> Механизм и почему он такой — `../../docs/kak-delat/RUKOVODSTVO-zahodami.md §Хук «→ фабрика»`.

## Как сюда писать (после каждого отчёта/хода — минута, не больше)

Одна секция на урок. **Факт и ЦЕНА — не формулировка закона.** Закон сформулирует тот, кто не совершал этой ошибки: автор урока — лицо заинтересованное, красивая формулировка от него подозрительна by design.

```
### N. <что произошло — одной фразой, без морали>
ЦЕНА: <что сломалось и сколько стоило: сколько ходов/заходов/лекций, кто поймал>
ВЕРДИКТ: <пусто до закрытия арки>
```

**ЦЕНА пишется сразу или не пишется вовсе.** Задним числом она не восстанавливается, а урок без цены — наблюдение, ему в канон нельзя.

## Как отсюда читать (закрытие арки — отдельная сессия, пустой контекст)

Вердикт на КАЖДУЮ секцию, одна из трёх форм:

- `ВЕРДИКТ: вписан → <файл>#<место>` — правило внесено, **консолидируя**: если оно уже есть в каноне слабой формулировкой — переписано, а не положено рядом.
- `ВЕРДИКТ: слит с N` — тот же закон в другой одежде.
- `ВЕРДИКТ: отбракован — <почему>` — случай, повторившийся дважды по совпадению; цена не названа; правило есть и достаточно. **Это результат сессии, а не её провал.**

## Гейты — грепом, оба могут провалиться

```
grep -c  '^### '            UROKI-FABRIKE.md   # уроков
grep -cE '^\*{0,2}ЦЕНА:'    UROKI-FABRIKE.md   # разошлось ⇒ урок без цены: он не урок
grep -cE '^\*{0,2}ВЕРДИКТ:' UROKI-FABRIKE.md   # разошлось ⇒ арка НЕ закрыта
```

*Цена жирным (`**ЦЕНА:**`) — законная форма, не «наблюдение»: строгий `^ЦЕНА: ` её не видел, и на арке `2026-07-25_lekcia-1` это дало ложный зелёный на 13 из 19 уроков (закрыто заходом `kod_gejty-kotorye-vrut.md`, 06.08). Отступ перед `ЦЕНА:`/`ВЕРДИКТ:` — НЕ то же самое: строка с отступом внутри списка законна как запись, но в шапке САМОГО этого файла (пример выше) индент — приём, которым запись НАМЕРЕННО прячут от этих же грепов, поэтому гейт индент не подхватывает. Строка в бэктиках (цитата формы, а не запись) — тоже не считается: гейт судит начало строки, а не упоминание слова где-то внутри неё.*

*Чего эти греп-гейты НЕ проверяют (честно, по клаузе 3 гейта): что цена настоящая, а вердикт честен. Считаются строки, не правда. Это ловит только владелец или свежая сессия.*

## Уроки

### 1. `check_sborki.py` С4 краснеет на здоровом критерии захода, который МЕНЯЕТ проверяемый файл
ЦЕНА: С4 сверяет вывод команды с ближайшим числом, предполагая, что критерий описывает мир СЕЙЧАС. На заходе `kod_dveri` критерий «`grep -c 'изменилось со вчера' …` → 0» — это ЦЕЛЬ после работы, а живой замер даёт 1, и гейт покраснел. Стоило: один круг переписывания критерия и обходную формулировку («ноль» словом, число рядом — текущий замер), то есть гейт вынудил писать критерий менее прямо, чем следовало. Класс общий: любой заход, правящий файл, чей греп стоит в критерии, наткнётся на то же. Поймал сам гейт при сборке захода.
ВЕРДИКТ:

### 2. Бриф сессии утверждал, что `check_zahod.py` живёт в pre-commit — в живом хуке его нет вовсе
ЦЕНА: диагноз брифа звучал как «краснеет у исполнителя, а не у аналитика», из чего следовало бы лечение «перенести гейт раньше». Живой `grep -n check_zahod .githooks/pre-commit` = 0: линтер вызывается ИЗ `bootstrap_zahod.py` (строка 1073), то есть уже стоит в нужный момент, но внутри генератора — обход генератора отключает его НАВСЕГДА, а не «до коммита». Приняв предпосылку на веру, сессия строила бы перенос уже стоящего на месте гейта. Поймано первым же грепом; стоило минуты, могло стоить сессии.
ВЕРДИКТ:

### 3. Маркер сборки в ШАБЛОНЕ бесполезен по построению — только инъекция кодом
ЦЕНА: предотвращена на этапе проектирования. Очевидное место для `<!-- собран bootstrap_zahod.py -->` — `_studio/zhurnal/_TEMPLATE-zahod.md`; оттуда маркер копируется вместе с шаблоном, и рукописный заход получает его даром, а вся затея «отличить сборку от руки» умирает МОЛЧА — гейт остаётся зелёным и на подделке. У `sdelat_handoff.py` маркер с самого начала вписан кодом (строка 125), и это не случайность, а тот же вывод, сделанный раньше.
ВЕРДИКТ:

### 4. Отчёт объявил охват «244 из 244», а живых файлов 245 — знаменатель занижен в свою пользу
ЦЕНА: исполнитель исключил из счёта `kod_dveri.md`, решив, что он «вне git до приёмки», — а глоб `_studio/zhurnal/*/kod_*.md` берёт файлы с ДИСКА, и файл там был. Вреда не вышло по случайности: пропущенный файл — единственный, который З11 проходит, то есть единственный положительный контроль во всём корпусе, и он оказался НЕ назван. Отчёт говорил «сработала на всех», хотя сильная правда была другая: «сработала на 244 из 245, а молчит ровно на том одном, где секция есть». Класс — знаменатель, вычисленный из представления о мире, а не командой по источнику (клауза 2 гейта, «Y — ЧИСЛО, посчитанное по источнику ДО работы»). Поймала приёмка, перегнав прогон сама; ход не стоил ничего, но при обратном знаке ошибки стоил бы доверия к З11 целиком.
ВЕРДИКТ:

### 5. Аналитик выдал владельцу команду `adopt` в чат — а перенос ветки принадлежит §0.1 СЛЕДУЮЩЕГО захода
ЦЕНА: канон называет «перенести код к себе» первой строкой приёмки, аналитик не смог (`adopt` из песочницы отдаёт `rc=3`) и сделал ровно то, что запрещено правилом владельца «никаких shell-строк в чат»: выдал команду человеку. Владелец поправил дословно — «я никогда сам не переношу, мы всегда делаем это в следующем заходе; в начале каждого захода мы подчищаем хвосты». Механизм для этого УЖЕ существует и уже вшит генератором: блок §0.1 ГИТ-КОНТУР с тремя слотами (`--vlit` / `--kommitit` / `--zakryt`) плюс проверка после влития, что инструменты встали на места. То есть аналитик выдал ручную команду вместо флага генератора — тот же класс, ради которого затеяна вся арка, совершённый ВНУТРИ неё, на приёмке первого же захода. Стоило: одна лишняя реплика владельца; систематически — возврат к перекладыванию файлов руками, от которого он прямо отказался 03.08.
ВЕРДИКТ:

### 6. Канон приёмки называет первым шагом действие, которое аналитику недоступно, и не называет его настоящего исполнителя
ЦЕНА: `RUKOVODSTVO §Приёмка` пишет «ПЕРВАЯ СТРОКА ПРИЁМКИ — ПЕРЕНЕСТИ КОД К СЕБЕ» и даёт команду `adopt`, не оговаривая, что из Cowork она отказывает по построению (FUSE) и что перенос штатно уезжает в слот `--vlit` следующего захода. Читатель канона — аналитик — исполнить предписание не может и вынужден изобретать обход; сегодня изобрёл неверный. Это тот же дефект, что урок 5, но у него другой адрес: там ошибся аналитик, здесь врёт канон.
ВЕРДИКТ:

### 7. `git_zona.py check --zone` даёт РАЗНЫЕ ответы в worktree исполнителя и в основной папке — и оба верны
ЦЕНА: исполнитель захода `ohvat` честно отчитался «`check --zone _studio/zhurnal/_INFRA-git/` → ✅», прогнав команду у себя в `materials-wt/ohvat`, где зона чиста. Приёмка гоняет тот же гейт 0 в ОСНОВНОЙ папке — там он красный: висит строка автозаписи от 2026-08-10 19:43, оставленная аналитиком ДО начала сессии и не закоммиченная, потому что из песочницы Cowork коммитить нельзя. Канон запрещает читать отчёт при красном гейте 0 («что бы в нём ни было написано»), так что приёмка встала на ровном месте и ход ушёл на выяснение, чья это грязь. Никто не соврал: у команды просто два разных ответа в зависимости от того, где её запустить, и ни канон, ни текст гейта об этом не предупреждают.
ВЕРДИКТ:

### 8. Долг аналитика в зоне захода читается приёмкой как недоработка исполнителя
ЦЕНА: та же строка 19:43 лежит в `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, который заход `ohvat` объявил своей зоной для другой цели (перенести копию из папки `dveri`). Исполнитель эту грязь не создавал, из своего worktree не видел и по контракту зоны трогать чужое не обязан — но гейт приёмки судит зону целиком и указывает на него. Систематическое следствие: каждый заход, чья зона пересекается с файлами, которые правит Cowork, будет приходить на приёмку с ложно-красным гейтом 0, и приёмка либо начнёт его игнорировать, либо каждый раз тратить ход на разбор. Первое опаснее.
ВЕРДИКТ:

### 10. Плагин со всеми актуальными дисциплинами не был установлен — работал его предшественник из архива
ЦЕНА: бриф сессии искал причину «почему скилл заходов не срабатывает» в формулировке триггеров и планировал переписывать описание. Живая причина другая и грубее: плагин `disciplina` (7 скиллов, среди них `disciplina-zahody` на 425 строк — распечатка действующего канона) **не был установлен вовсе**, а подхватывался `agentic-coding-session-brief` на 310 строк из `~/.claude/skills/` — тот самый, копию которого владелец сам положил в `arhiv-skillov/` с пометкой «при включении начал бы конфликтовать с фабрикой». То есть актуальная дисциплина не могла сработать по построению, а срабатывала отменённая. Найдено только потому, что перед делением скилла аналитик вызвал `list_plugins` вместо того, чтобы поверить брифу. Стоило: ноль переделки (деление ещё не начиналось), но владелец полсессии считал, что мы правим не тот файл. Обратная цена, если бы не проверили: разделили бы архивную ветку, и расхождение стало бы тройным.
ВЕРДИКТ:

### 11. `check_sborki.py` структурно слеп к кросс-репозиторному заходу и даёт ложное красное
ЦЕНА: на заходе `skilly` (работа в репозитории `disciplina`, файл-заход в `materials`) гейт дал 11 ❌, из которых все до одного — про то, что пути и ветка не находятся в `materials`. Девять снялись переписыванием путей в форму `../disciplina/…` (она верна от корня обоих репозиториев — они соседи), два остались ложными: С1 спрашивает про ветку не тот репозиторий, С3 гонялся из песочницы, где у `materials` другой родитель и соседа нет. Опасность не в двух строках вывода, а в соблазне «починить» их содержанием: подстроив заход под гейт, получаешь задание, которое врёт исполнителю про место работы. Пришлось вписать в заход отдельный блок с доказательством ложности — то есть предупреждать исполнителя о собственном гейте.
ВЕРДИКТ:

### 12. Указание «рабочую папку заводишь САМ, первым ходом» адресовано тому, кто физически не может его выполнить
ЦЕНА: один сорванный запуск захода `modeli` — владелец вставил стартовую команду и получил `cd: /Users/…/materials-wt/modeli: No such file or directory`. Противоречие живёт внутри одного генератора: контракт зоны (блок «МЕСТО РАБОТЫ») велит исполнителю завести worktree первым ходом, а стартовая команда того же `bootstrap_zahod.py` НАЧИНАЕТСЯ с `cd` в эту папку — то есть `cd` падает раньше, чем Claude вообще стартует, и первого хода не случается никогда. Канон `disciplina-zahod` повторяет ту же формулировку («WORKTREE ЗАВОДИТ САМ ЗАХОД ПЕРВЫМ ХОДОМ, не владелец руками»), так что ошибка не в тексте захода, а в правиле. Аналитик из песочницы завести папку тоже не может — `git_zona.py worktree add` отказывает намеренно и правильно. Значит единственный исполнитель этого указания — владелец, а адресовано оно исполнителю. Чинится либо тем, что генератор печатает `worktree add` ПЕРЕД стартовой командой в том же блоке (одна строка, владелец вставляет обе), либо тем, что стартовая команда делает `cd` в корень репозитория, а не в рабочую папку. ПОВТОРЯЕТСЯ на каждом параллельном заходе с `--worktree`, то есть на большинстве.
ВЕРДИКТ:

### 13. Запись об обходе хука рождается в рабочей папке и до аналитика не доезжает
ЦЕНА: пока мала — заход `modeli` назвал свой законный `--no-verify` прямо в отчёте, и приёмка узнала о нём оттуда. Но независимая проверка приёмки не сошлась: `INCIDENTY.md` в ОСНОВНОЙ папке обрывается на 05:00 и записи 15:55 про этот обход не содержит — она лежит в копии журнала внутри `materials-wt/modeli` и приедет только слиянием ветки. То есть механизм «инструмент сам пишет причину обхода в журнал» защищает ровно от того случая, которого он не покрывает: заход, который обошёл хук и НЕ написал об этом в отчёт, оставит след в файле, которого аналитик при приёмке не видит. Проверять пришлось руками, зная, куда лезть.
ВЕРДИКТ:

### 14. Стенд фикстуры `dvizhok` отстал от хука и краснеет на каждой чужой правке (доставлено: modeli#1)
ЦЕНА: один законный обход `--no-verify` на коммите `7e55389` и время исполнителя на диагностику чужого красного. `fixtures/dvizhok/PROGNAT.sh` (ловушка 5) собирает одноразовый тестовый репозиторий и копирует в него `check_uroki.py` и `check_ves.py`, но не `check_marker.py` — а `.githooks/pre-commit` требует его с коммита `a91a3b2` (заход `zapret-rukopisnyh`), который позже последней правки самой фикстуры (`449f894`). Приёмка проверила независимо: `sh _generator/tools/fixtures/dvizhok/PROGNAT.sh` на ЧИСТОМ дереве основной папки, без правок захода, даёт `rc=1` и `can't open file '…/check_marker.py'`. Значит красное чужое и обход законен. ПОВТОРЯЕТСЯ на каждой правке любого из пяти файлов охвата этой фикстуры (`engine.js base.css build_deck.py render.py bootstrap_zahod.py`) — то есть на каждом следующем заходе по генератору. Чинится одной строкой `cp "$TOOLS/check_marker.py" "$R7/_generator/tools/check_marker.py"` рядом со строками 688-689 фикстуры.
ВЕРДИКТ:

---
*Локальная память арки, ephemeral. При закрытии уходит в канон `../../docs/kak-delat/` и умирает вместе с аркой. Дневник — `SESSIYA.md`; план — `PLAN.md`.*
