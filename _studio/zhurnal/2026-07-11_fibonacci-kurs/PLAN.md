# ПЛАН арки «2026-07-11_fibonacci-kurs»

## ⭐ СЕЙЧАС — приёмка владельцем (ночной прогон ВЫПОЛНЕН 2026-07-11)
**Автономный ночной прогон завершён** (Claude Code, ~3 ч, 7 коммитов). Что смотреть утром:
1. **`run-log.md`** — подробный дневник (4 части). Главное событие: adversarial-рецензент поймал 2 РЕАЛЬНЫЕ матошибки (формула Каталана 11г, доказательство задачи 4) — исправлены. Плюс находки по фабрике (тройная нестыковка формата карточки, sim-init в скрытом слайде, профиль-зависимость G2).
2. **Дек** — открыть `fibonacci/src/dist/index.html` (или `python3 -m http.server` над `dist/`): 12 слайдов, интерактив `fib-strip` (клик по полоске → образ-биекция живьём: 0/1-строка / подмножество / перестановка / рекуррента / сумма / Кассини). Верифицирован в браузере.
3. **Математика** — `fibonacci/kotly/matematika.md` (22 утв.+ 4 опр., все задачи листка) + `fibonacci/verify_skelet.py` (сверка на n≤8, зелёный).
4. **Вопросы владельцу** — `kod_kurs-avtonom.md §ВОПРОСЫ` (H_n даёт f_{n−1}, дефолты на развилках).

**Гейт-статус:** G1–G6, G8–G11 PASS; G7 — эвристический FAIL (cover-слайды без content/, валидно). Браузерные слои audit.py/render.py — прогнать у владельца (Playwright).

**Стоп на чистой границе:** TIER-1 (полный сценарий + дневник) — ЗАКРЫТ. TIER-2 — построен связный СРЕЗ 12 слайдов (PoC всей дуги + механизм на 3 спицах), НЕ весь курс.

### Что дальше (хэндофф, если продолжать)
- Достроить слайды лекций 2–3 из `raskadrovka/PLAN.md` (задачи 4,6,9,12; Люка 13–15; Цекендорф 5) тем же механизмом `fib-strip` (+ режимы cut/lucas).
- Прогнать браузерный `audit.py`/`render.py` у владельца (overflow/scene-diff/глаз).
- Решить вопросы из §ВОПРОСЫ (особенно H_n) + развести две подачи (сильная/слабая группа).
- Приёмка → закрыть арку (ARKA §10 C) ЛИБО открыть Фазу II-волну на достройку.

<details><summary>Исходное описание прогона (архив)</summary>

**Был готов АВТОНОМНЫЙ НОЧНОЙ прогон — `kod_kurs-avtonom.md`** (opusplan): §0 файловая настройка + источники + мусор; дальше автономно гонит фабрику end-to-end (тонкий ресёрч → skelet со всеми биекциями → rasskaz → раскадровка ≤30 слайдов → интерактив-биекции canvas) + ведёт `run-log.md`. Ниже — что заводит:

**Первый ход (детали настройки, делает заход).** Завести рабочую папку курса в `../../../fibonacci/` по канону `PAPKA-LEKCII` (`kartoteka/ istochniki/ kotly/ chernovik/ raskadrovka/ src/`, `brief.md`: слайды=да, профиль ОТ-КНИГИ). ⚠ `bootstrap_lekcia.py` на НЕПУСТУЮ папку не встанет (там уже книга+листок) → скаффолдить вручную/дозаписью, источники (2 PDF) → в `istochniki/` (MANIFEST). Затем — шаг ресёрча (тонкий, ОТ-КНИГИ): вычитать листок + релевантные главы книги → карточки объектов/биекций.

## Шаги
- [x] 0. Рабочая папка курса `fibonacci/{kartoteka,istochniki,kotly,chernovik,raskadrovka,src}` + `brief.md`; 3 PDF → `istochniki/pdf/` (MANIFEST+дайджесты)
- [x] 1–2. Тонкий ресёрч ОТ-КНИГИ → карточный граф (29 карточек, `kartoteka/KARTA-OBLASTI`) + `VYCHITANO`/`TERMINY`; G2 PASS
- [x] 3. `skelet` (`kotly/matematika.md`, 22 утв.) + `verify_skelet.py`; G3 PASS; adversarial-ревью (2 ошибки исправлены)
- [x] 4. Нить → `rasskaz` (5 актов, 14 🖼); G4 PASS
- [x] 5. Раскадровка (`raskadrovka/PLAN.md` + `src/brief.md` register/budget/slide_order); G5 PASS
- [x] 6–10. Дек: `src/` (shablon, 12 слайдов, content, 6 SVG, `sims/lab.core.js`) → `build_deck.py` → `dist/`; линтер PASS; верифицирован в браузере
- [~] Достройка лекций 2–3 (полный курс ~30 слайдов) — СРЕЗ 12 построен, остальное по `raskadrovka/PLAN.md`
- [ ] Две подачи (сильная/слабая группа) — одна база, вариант темпа (позже)
- [ ] Браузерный QA (audit.py/render.py, Playwright) + приёмка владельца

<!-- при закрытии арки — баннер «✅ АРКА ЗАКРЫТА (<дата>)» сверху (ARKA §7 п.4 / §10 C) -->
</details>
