# ПЛАН арки «2026-07-11_fibonacci-kurs»

## ⭐ СЕЙЧАС — первый конкретный шаг следующего хода
**Готов АВТОНОМНЫЙ НОЧНОЙ прогон — `kod_kurs-avtonom.md`** (Claude Code, opusplan): §0 делает файловую настройку + раскладывает источники + удаляет мусор; дальше автономно гонит фабрику end-to-end (тонкий ресёрч → skelet со всеми биекциями → rasskaz → раскадровка ≤30 слайдов → интерактив-биекции canvas) + ведёт подробный `run-log.md`. TIER-1 (полный сценарий + дневник) гарантирован; TIER-2 (интерактивный дек) — насколько хватит контекста. Утром — приёмка + разбор дневника. Ниже — что именно он заводит:

**Первый ход (детали настройки, делает заход).** Завести рабочую папку курса в `../../../fibonacci/` по канону `PAPKA-LEKCII` (`kartoteka/ istochniki/ kotly/ chernovik/ raskadrovka/ src/`, `brief.md`: слайды=да, профиль ОТ-КНИГИ). ⚠ `bootstrap_lekcia.py` на НЕПУСТУЮ папку не встанет (там уже книга+листок) → скаффолдить вручную/дозаписью, источники (2 PDF) → в `istochniki/` (MANIFEST). Затем — шаг ресёрча (тонкий, ОТ-КНИГИ): вычитать листок + релевантные главы книги → карточки объектов/биекций.

## Шаги
- [ ] 0. Рабочая папка курса `fibonacci/{kartoteka,istochniki,kotly,chernovik,raskadrovka,src}` + `brief.md` (слайды=да, ОТ-КНИГИ, тонкая строгость); 2 PDF → `istochniki/`
- [ ] 1–2. Тонкий ресёрч ОТ-КНИГИ: листок + главы книги → карточки объектов/биекций (`kartoteka/`) + дайджест (`istochniki/VYCHITANO`)
- [ ] 3. Тонкий `skelet`: биекции-доказательства из карточек (`kotly/matematika`)
- [ ] 4. Нить → `rasskaz` (последовательный разбор 15 задач)
- [ ] 5. Раскадровка → ~50 интерактивных слайдов
- [ ] 6–10. Текст → вёрстка → сцены → **интерактив-биекции (canvas + `lab.js`, шаг 9)** → сборка
- [ ] Две подачи (сильная/слабая группа) — одна база, вариант темпа (позже)

<!-- при закрытии арки — баннер «✅ АРКА ЗАКРЫТА (<дата>)» сверху (ARKA §7 п.4 / §10 C) -->
