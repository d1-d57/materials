# ПЛАН арки «2026-07-11_fibonacci-kurs»

## ⭐ СЕЙЧАС — Л1 СОБРАНА (SPEKA + канон + движок + дек, все машинные гейты зелёные) → ГЛАЗ ВЛАДЕЛЬЦА

**Хэндофф 2026-07-12. Одобренный план исполнен целиком. Ниже под чертой — история хода.**

**Сделано этой сессией:**
1. **SPEKA введена** — `fibonacci/SPEKA.md` (единый источник истины: базовое правило «все утверждения листка по порядку», метод двойного счёта, все 8 объектов A–H × 4 сцены, формат-слово, роутер = слой 2) + шаблон `_studio/docs/spravka/SPEKA-TEMPLATE.md`; вписана в `PAPKA-LEKCII` / `KARTA` / `RUKOVODSTVO §0+приёмка` / `START-HERE`.
2. **Канон формата → «цветное слово»:** Р23 + `konvejer/07-verstka` + `raskadrovka/STANDART-canvas` (Задача `--brick`+номер · Определение `--cgreen` без номера · **Утв/Теорема общий `--steel`, не различаются**; каждая модель в своём языке, полоска-A только в сцене биекции; ритм = 4 сцены).
3. **Движок** `src/sims/lab.core.js`: модели **D** (`compGE2`), **E** (`compOdd`), **G** (`zigzag`) + сцена-биекция у A/F/H; `verify_models.py` расширен → **PASS 8/8** (счёт=fₙ; карты замощение→объект — точные биекции A↔X; split верен). Сверено и python-верификатором, и независимым node-харнессом.
4. **Тексты** `raskadrovka/TEKSTY-L1.md`: пересобран по ролям (8 моделей алфавитом), дописаны **D/E/G** (рекурренты сверены численно). Заход `kod_lekcia1-dobiv.md`.
5. **Дек:** **16 слайдов** ровно по SPEKA (обложка · Задача 1 · A · обзор(2 колонки) · B · C · D · E · F · G · H · пуанта · 3а · 3б · 4 · 5); `s03-strip` + звезда `s05-masks` → `_zadel`.

**Гейты (Cowork перегнал сам):** линтер `build_deck` зелёный (16 слайдов, dist собран, 0 сирот/дублей/нерезолвленных); `verify_models` 8/8 · `verify_skelet` · `verify_H` зелёные; `kicker-claim`/`kicker-thm`=`--steel`; у всех 8 моделей `data-scenes=4` + `empty examples recur bijection`; проза-грепы пусты.

**ОСТАЛОСЬ — ОДНО: глаз владельца на живом деке** `fibonacci/src/dist/index.html` (визуальный ревью — роль владельца; headless-браузера в песочнице нет, машинно проверить вид нельзя). Места под глаз: сцена «биекция» у **D/G/H упрощённа** (полоска сверху ↔ объект по центру снизу, бессловесно — НЕ по-клеточно; owner финализирует); свёртка 2×10 (Задача 1); обзор-колонки; формат-слово на задачах. **Коммит:** из песочницы НЕ коммичено (`RUKOVODSTVO §17`) — дельту забирает host.

**Развилки закрыты владельцем:** обзор-колонки — оставлены; Утв/Теорема — не различаются (`--steel`); добив — Cowork сам (исполнено).

**Дальше:** владелец смотрит дек → либо точечные правки (в `TEKSTY-L1`/слайды), либо **закрыть арку** (ARKA §10 C: durable по домам — Р23 «цветное слово» уже в каноне; базовое правило «идём по листку, все утверждения» и «SPEKA как дом охвата» — занести в `RESHENIYA`/`KONSTITUCIYA`; беклог #14). Достройка Л2–Л3 — отдельная арка.

---
*Ниже — история хода до хэндоффа 2026-07-11.*

## ⭐ (архив) Лекция 1: серия МОДЕЛЕЙ (v1 ПРИНЯТА; заход v2 формат C + тексты готовы → глаз владельца на тексты → запуск)
**Приёмка ночного прогона проведена (Cowork, 2026-07-11).** Гейты перепрогнаны на живых файлах: `verify_skelet.py` зелёный ВКЛЮЧАЯ исправленные конструкции (11г Каталан, задача 4 «первый квадрат», Кассини-сдвиг); `sostoyanie.py` G1–G6/G8–G11 PASS, G7 — эвристический FAIL (cover без content, валидно). **Математика ПРИНЯТА.** Дек (`fibonacci/src/dist/index.html`) — формат **ЗАБРАКОВАН** владельцем: много эссе-текста, декоративные иллюстрации к каждой фразе, односторонний интерактив.

**Решение владельца:** перевёрстка в ПЕРВУЮ ЛЕКЦИЮ (задачи 1–5 по порядку) по стандарту **«двузонный canvas»** — условие→доказательство, минимум текста, зоны разделены фоном, соответствие выравниванием, редактируемый низ, ограничение-во-вводе; **дешёвые переиспользуемые примитивы** (конфиг на слайд, не bespoke-код). НЕ переделка с нуля — довести существующее; задел лекций 2–3 НЕ удалять. `H_n` — ошибка индекса в определении (даёт f_{n−1}), исправить до f_n.

**v1 перевёрстки исполнена и ПРИНЯТА по гейтам (Cowork, 2026-07-11).** `kod_lekcia1-pereverstka.md` отработан (HEAD `d934302`, 6 коммитов): линтер зелёный (11 слайдов), `verify_H`/`verify_skelet` PASS, зона коммитов чистая (ничего вне `fibonacci/`), задел в `src/_zadel/`. Гейты §5 пройдены; audit.py не прогнан (нет Playwright — глаз владельца достаточно). Ветка: решено вынести в `fibonacci-l1`.

**Разворот владельца (после глаза на дек):** Л1 = **серия МОДЕЛЕЙ Фибоначчи** (A→C→B→F→H), каждая = ОДИН канвас со СЦЕНАМИ (пусто→примеры малых n→рекуррента-по-первому-элементу), верх = определение объекта ДОСЛОВНО из листка, «Задача N» по центру, минимум текста (убрать инструктивные подписи), звезда-хаб s05 — в задел, H — свой слайд. Кросс-биекции (2в) — лёгкий бонус. = ровно задача 2 листка (2а примеры, 2б рекуррента, 2в бонус).

**Готово к запуску:** заход **`kod_lekcia1-modeli.md`** v2 (формат опорных точек **C** «номер на поле»; серия моделей A→C→B→F→H; +задача 3а «одно условие на языках»; методы задачи 5, биекция `a↦Σ(1−aₙ)fₙ`; две колонки вместо звезды; баги v1) + тексты **`fibonacci/raskadrovka/TEKSTY-L1.md`** (голос владельца, снят с референс-презентации; формулировки из листка). **Следующий шаг:** глаз владельца на тексты (регистр) → запуск исполнителя (`opusplan`, ветка `fibonacci-l1`, строка в шапке захода) → приёмка §5.

**Стоп на чистой границе:** TIER-1 (полный сценарий + дневник) — ЗАКРЫТ. TIER-2 — построен связный СРЕЗ 12 слайдов (PoC всей дуги + механизм на 3 спицах), НЕ весь курс.

### Что дальше (хэндофф, если продолжать)
- Достроить слайды лекций 2–3 из `raskadrovka/PLAN.md` (задачи 4,6,9,12; Люка 13–15; Цекендорф 5) тем же механизмом `fib-strip` (+ режимы cut/lucas).
- Прогнать браузерный `audit.py`/`render.py` у владельца (overflow/scene-diff/глаз).
- Решить вопросы из §ВОПРОСЫ (особенно H_n) + развести две подачи (сильная/слабая группа).
- Приёмка → закрыть арку (ARKA §10 C) ЛИБО открыть Фазу II-волну на достройку.

<details><summary>Исходное описание прогона (архив)</summary>

**Был готов АВТОНОМНЫЙ НОЧНОЙ прогон — `kod_kurs-avtonom.md`** (opusplan): §0 файловая настройка + источники + мусор; дальше автономно гонит фабрику end-to-end (тонкий ресёрч → skelet со всеми биекциями → rasskaz → раскадровка ≤30 слайдов → интерактив-биекции canvas) + ведёт `run-log.md`. Ниже — что заводит:

**Первый ход (детали настройки, делает заход).** Завести рабочую папку курса в `../../../fibonacci/` по канону `PAPKA-LEKCII` (`kartoteka/ istochniki/ kotly/ chernovik/ raskadrovka/ src/`, `brief.md`: слайды=да, профиль ОТ-КНИГИ). ⚠ `bootstrap_lekcia.py` на НЕПУСТУЮ папку не встанет (там уже книга+листок) → скаффолдить вручную/дозаписью, источники (2 PDF) → в `istochniki/` (MANIFEST). Затем — шаг ресёрча (тонкий, ОТ-КНИГИ): вычитать листок + релевантные главы книги → карточки объектов/биекций.

## Шаги
- [x] 0. Рабочая папка курса `fibonacci/{kartoteka,istochniki,kotly,chernovik,raskadrovka,src}` + `brief.md`; 3 PDF → `istochniki/pdf/` (MANIFEST+дайджесты)
- [x] 1–2. Тонкий ресёрч ОТ-КНИГИ → карточный граф (29 карточек, `kartoteka/KARTA-OBLASTI`) + `VYCHITANO`/`TERMINY`; G2 PASS
- [x] 3. `skelet` (`kotly/matematika.md`, 22 утв.) + `verify_skelet.py`; G3 PASS; adversarial-ревью (2 ошибки исправлены)
- [x] 4. Нить → `rasskaz` (5 актов, 14 🖼); G4 PASS
- [x] 5. Раскадровка (`raskadrovka/PLAN.md` + `src/brief.md` register/budget/slide_order); G5 PASS
- [x] 6–10. Дек: `src/` (shablon, 12 слайдов, content, 6 SVG, `sims/lab.core.js`) → `build_deck.py` → `dist/`; линтер PASS; верифицирован в браузере
- [~] Достройка лекций 2–3 (полный курс ~30 слайдов) — СРЕЗ 12 построен, остальное по `raskadrovka/PLAN.md`
- [ ] Две подачи (сильная/слабая группа) — одна база, вариант темпа (позже)
- [ ] Браузерный QA (audit.py/render.py, Playwright) + приёмка владельца

<!-- при закрытии арки — баннер «✅ АРКА ЗАКРЫТА (<дата>)» сверху (ARKA §7 п.4 / §10 C) -->
</details>
