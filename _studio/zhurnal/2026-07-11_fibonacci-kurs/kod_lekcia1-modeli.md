# Канал исполнителя — Лекция 1 «Числа Фибоначчи»: серия МОДЕЛЕЙ (сцены-в-канвасе) — v2

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу этого файла. Метрика — КАЧЕСТВО (вкус владельца), часы — норма.

**Модель:** `opusplan` — расширение движка (сцены-в-канвасе + рекуррента-по-первому-элементу для КАЖДОЙ модели) рождается по ходу на Opus; механическое размножение слайдов-конфигов — Sonnet.

**Текст запуска (владелец → Claude Code):**
```
Модель: opusplan — дизайн расширения движка на Opus, механический HTML на Sonnet.
Claude Code, твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-11_fibonacci-kurs/kod_lekcia1-modeli.md. Прочитай ТОЛЬКО его и то, что он называет; проект не изучай. Точка отката: git-коммит своей зоны сразу (см. §0). План/вопросы/отчёт — в этот же файл вниз (## ПЛАН/ВОПРОСЫ/ОТЧЁТ). Ничего сверх задачи не трогай.
```

КОНТЕКСТ. Курс «Числа Фибоначчи» (профиль ОТ-КНИГИ), папка `fibonacci/`. Л1 v1 (11 слайдов, двузонный canvas, линтер зелёный) — принята по гейтам, но владелец посмотрел дек и **развернул спайн**: Л1 = **серия МОДЕЛЕЙ Фибоначчи** (замощения, коды, подмножества, перестановки, H) — каждая со своим лицом (определение + примеры + рекуррента), а не хаб с кросс-биекциями. **ЦЕЛЬ (v2):** перевести Л1 в серию моделей по контракту §3, с точечными багами v1 из §5 — доводя существующее, НЕ переписывая с нуля.

⚠ **Стоп до цели (громко):** ты соберёшь ЛЕКЦИЮ 1 (задачи 1–5 листка), НЕ весь курс. Лекции 2–3 — НЕ здесь. Задел (`src/_zadel/`) НЕ трогать.

⚠ **ТЕКСТЫ слайдов даёт Cowork** (файл `fibonacci/raskadrovka/TEKSTY-L1.md`, в голосе владельца) — ты ставишь их **дословно**, своей прозы НЕ пишешь. Если файла ещё нет на старте — доложи в `## ВОПРОСЫ` и не выдумывай тексты. Формулировки задач — из листка дословно.

Приёмка — по ОТЧЁТУ + линтер `build_deck.py` + глаз владельца.

## 0. ПЕРВЫЙ ХОД
Рабочая папка — `fibonacci/` (корень репо `/Users/ivanyakovlev/Documents/GitHub/materials`).
1. **Ветка + точка отката:**
   ```
   rm -f .git/index.lock
   git switch -c fibonacci-l1 || git switch fibonacci-l1
   git add fibonacci _studio/zhurnal/2026-07-11_fibonacci-kurs/kod_lekcia1-modeli.md _studio/zhurnal/2026-07-11_fibonacci-kurs/PLAN.md _studio/zhurnal/2026-07-11_fibonacci-kurs/SESSIYA.md
   git commit -m "Л1 модели-серия v2: точка отката"
   ```
   НЕ `git add -A`.
2. **Скиллы:** `html-slides-studio`, `design-mode`, `microinteractions`, `karpathy-guidelines`.
3. **Прочитай ТОЛЬКО:** этот заход; `fibonacci/istochniki/pdf/Числа_Фибоначчи.pdf` (ЛИСТОК — текст условий/определений дословно); `fibonacci/raskadrovka/TEKSTY-L1.md` (тексты слайдов от Cowork — дословно); `fibonacci/kotly/matematika.md` (истина по рекуррентам — НЕ трогать); `fibonacci/raskadrovka/STANDART-canvas.md` (движок `fib-strip`, сцены — БАЗА); `fibonacci/src/sims/lab.core.js`; `shablon.html`+`tokens.css`+`base.css`; текущие `slides/*.html`+`content/*.md`; `verify_skelet.py`+`verify_H.py`; `_generator/build_deck.py`.
4. **ПЛАН** — в `## ПЛАН`. Фаза A (движок: секвенсор сцен + рекуррента-по-первому-элементу для каждой модели + формат опорных точек), Фаза B (модели-конфиги + постановка текстов).

## 1. ДИСЦИПЛИНА (Карпатов)
Развилки — в `## ВОПРОСЫ`. **Минимум:** расширяй `lab.core.js`, НЕ переписывай. **Хирургия:** каждая правка — к §3/§5. Своей прозы не пишешь (тексты — из `TEKSTY-L1.md`/листка). Субагенты ≤5, механика — Sonnet, не коммитят.

## 2. ГРАНИЦЫ + ЗОНА
**ЗОНА:** `fibonacci/**` + этот заход. Вне — READ-ONLY (фабрика, чужие проекты). `matematika.md`/`verify_skelet.py`/задел `src/_zadel/` — не трогать. **git:** только зона, НИКОГДА `-A`; субагенты не коммитят.

## 3. ЗАДАЧА — Л1 как серия МОДЕЛЕЙ

### Контракт слайда-МОДЕЛИ (жёстко)
Каждая модель = **ОДИН слайд, ОДИН канвас** (не три). Устройство:
- **ВЕРХ (светлая зона) постоянен = ОПРЕДЕЛЕНИЕ объекта** (дословно из листка/`TEKSTY-L1.md`) + как устроен элемент. НЕ про биекцию. Между сценами верх не меняется (no reflow).
- **НИЗ проходит СЦЕНЫ** (≥3, можно больше), меняется только образ снизу:
  1. **пусто** (только определение);
  2. **маленькие примеры** — выписаны ВСЕ элементы для n=1,2,3;
  3. **пример побольше** — n=4 (счёт 1,1,2,3,5…);
  4. **рекуррента-биекция** — элемент делится **по первому элементу** на `Y→X_{n−1}` и `Z→X_{n−2}` (`matematika.md` Утв.2–3, листок з.2б).
- **Минимум текста.** Никаких инструктивных подписей («меняй ленту…») и подписей к иллюстрациям-доказательствам («квадрат + A₆») — УБРАТЬ.

### Формат ОПОРНЫХ ТОЧЕК (директива владельца — вариант C «номер на поле»)
Опорные точки — «**Задача** / **Условие** / **Утверждение** / **Теорема**» — единым форматом (раньше в лекциях их не было явно, разрабатываем сейчас):
- **Крупный НОМЕР на ПОЛЕ, не по центру:** слева на поле крупная Forum-цифра-номер (по листку) как буквица; рядом мелкий Forum-caps надзаголовок («Задача»); тонкая линейка `--rule` под блоком; формулировка — Noto в мерной колонке справа от номера. Семья «Определение/Утверждение/Теорема» — тем же языком (крупный акцент + надзаголовок; номер там, где уместен). Референс — виджет Cowork, вариант **C**.
- **Картинка ПОСЛЕ формулировки:** сначала «Задача»+условие (сцена), потом доказательство-канвас (следующая сцена). Порядок всегда ЧТО→как.
- Финальный контракт формата занеси в `STANDART-canvas.md`.

### Роутер моделей Л1 (порядок; тексты — дословно листок/TEKSTY)
1. **s01 Обложка** — только «Числа Фибоначчи», **без подписи** (убрать `.sub`).
2. **Задача 1** (2×10). Поток: показать замощение доски 2×10 → «разбиение восстанавливается по ВЕРХНЕЙ строке» (НЕ «снять лишние измерения») → переход к полоске 1×n квадрат/доминошка. ⚠ формулу «2×10» НЕ разрывать переносом (зона во всю ширину: либо влезает целиком, либо переносится слово «доминошки»).
3. **Модель A** — упорядоченные разбиения n в сумму 1 и 2 (замощения 1×n). Сцены: определение → выписать ВСЕ малые примеры (1,1,2,3,5) → **гипотеза: это Фибоначчи** → рекуррента по первой плитке → доказательство. *(расширить `s04-recur` сценами)*
4. **Две колонки — «ещё семь объектов»** (замена звезды-хаба s05): СЛЕВА список определений объектов (B,C,D,E,F,G,H — из листка), СПРАВА узкая голубая колонка с примером КАЖДОГО при n=5. Без картинки-звезды. Отсюда — «дальше разбираем по одной модели».
5. **Модель C** — 0/1 длины n без «00», кончаются на 1: определение → примеры → рекуррента по первому символу. *(repoint `s07-code`)*
6. **Модель B** — подмножества {1..n−1} без соседних. *(repoint `s08-subset`)*
7. **Модель F** — перестановки {1..n}, |π(i)−i|≤1. *(repoint `s09-perm`)*
8. **Модель H** — цифры 1–4, длина n, старт 1, соседние позиции — соседние цифры. **Свой слайд.** ⚠ длина n даёт f_{n−1}; бери ИСПРАВЛЕННУЮ длину n+1 (f_n, сверено `verify_H`); отметь сдвиг честно.
9. **Пуанта** — у всех одна рекуррента `f_n=f_{n−1}+f_{n−2}` и база `|X_0|=|X_1|=1` ⇒ все f_n. Кросс-биекция (задача 2в, X_n↔A_n) — **лёгкий бонус** (одна сцена), не основное.
10. **Задача 3а** (была пропущена) — ОДНО условие переговорённое на РАЗНЫХ языках: «элементы Aₙ: сначала k единиц, потом двойка» → что это в Bₙ, Cₙ, …, Hₙ. **Один слайд** (payoff серии). Текст — листок з.3а.
11. **Задача 3б** — `f_0+…+f_n = f_{n+2}−1`, по первой доминошке (Утв.9). ⚠ картинка сначала БЕЗ красной доминошки → потом добавить красную = ответ.
12. **Задача 4** — `f_1+f_3+…=f_{2n}−1`, `f_0+f_2+…=f_{2n+1}`, по первому квадрату (Утв.10, режим `firstsq`).
13. **Задача 5 (Цекендорф) — финал.** 5а существование — индукция (единственное место листка, где можно); 5б единственность — «сюръекция между множествами равной мощности есть биекция»; 5в явная биекция `C_n→{1..f_n}`: последовательность `a ↦ Σ_n (1−a_n)·f_n` (⚠ сверить численно отдельным скриптом — как `verify_H`).

D, E, G — в Л1 НЕ разворачиваем (листок сам помечает «курсу не нужны»); в двух колонках (п.4) присутствуют определением+примером, детально — задел.

### Точечные баги v1→v2 (трасса к глазу владельца)
- **s03 свёртка:** ЛЕВАЯ картинка — доминошки РАЗНОГО размера (горизонтальная vs вертикальная); должны быть ОДИНАКОВЫ, повёрнуты на 90°. Правая уже верна — привести левую к ней.
- Убрать ВСЕ поясняющие подписи к иллюстрациям-доказательствам.
- `s05-masks` (звезда) → `src/_zadel/`, вне `slide_order`.

### Движок (Фаза A, Opus) — расширить `fib-strip`, НЕ переписывать
- **Секвенсор сцен:** одна модель = один `<canvas>`, N сцен (`data-scenes`), верх-определение вне канваса и постоянен; листание сцен низа (пусто→примеры→рекуррента), геометрия заморожена.
- **Сцена «примеры»:** генератор выписывает ВСЕ X_n для малых n (сверить счёт с f_n).
- **Сцена «рекуррента»:** разбиение по первому элементу для КАЖДОЙ модели (не только A).
- Слайд = КОНФИГ. Обнови `STANDART-canvas.md` (сцены + формат опорных точек A + «без поясняющих подписей»). Численные сверки моделей — допиши `verify_*.py`, не ломая существующие.

## КРИТЕРИЙ ГОТОВНОСТИ (может провалиться)
- `python3 _generator/build_deck.py fibonacci/src` — линтер ЗЕЛЁНЫЙ, dist собран, slide_order = точное покрытие (0 сирот/дублей).
- Каждая модель A,C,B,F,H = ОДИН слайд/ОДИН канвас; верх = определение из листка дословно; ≥3 сцены (пусто→примеры→рекуррента), верх без reflow.
- Рекуррента-по-первому-элементу верна численно для КАЖДОЙ модели (`|X_n|=f_n`, разбиение корректно).
- Задача 3а (одно условие на языках всех объектов) — есть; задача 5в биекция `a↦Σ(1−a_n)f_n` сверена численно.
- Формат опорных точек C (номер на поле) реализован; картинка ПОСЛЕ формулировки; сумма з3б — reveal красной доминошки.
- Тексты — ДОСЛОВНО из `TEKSTY-L1.md`/листка (своей прозы 0; grep «меняй ленту\|строка меняется» = пусто).
- Баги v1 закрыты: обложка без подписи; «2×10» без переноса; левая картинка s03 — плитки одного размера (поворот); подписи к илл. убраны; звезда s05 в заделе; H свой слайд.
- Браузер: скриншоты сцен (пусто/примеры/рекуррента) у 2–3 моделей + формат «Задача N» + свёртка 2×n.

## 4. ОТЧЁТ → секция `## ОТЧЁТ` внизу
что сделал+ЗАЧЕМ / как проверил (линтер+браузер+скрипт-сверки) / что НЕ трогал (математика, задел, фабрика, тексты сверх TEKSTY) / развилки / **время+токены**. Подтверди стоп-до-цели (Л1, задачи 1–5).

## ПЛАН — (исполнитель)

> Статус: **точка отката поставлена** (ветка `fibonacci-l1`, коммит `2190d82` — только зона `fibonacci/**` + журнал, чужие зоны нетронуты, без `-A`). Якоря прочитаны все. **Развилки закрыты владельцем** (см. ВОПРОСЫ): Задача 1 = ОДИН слайд (свёртка — сцена 3, 13 слайдов); рекуррента — СОВСЕМ без подписей (цвет первой плитки + скобка). Иду в Фазу A; wireframe формата C покажу до размножения.

### Итог разведки (проверенные факты, не догадки)
1. **Секвенсор сцен УЖЕ в `engine.js`**: `data-scenes="N"` + классы `.scene-k` на слайде + `[data-scene-from]/[data-scene-until]` (реализовано в `base.css`, до сцены 5); листается кликером (`next/prev`), есть мост `postMessage(navSub)` и экспорт `?only=N&scene=K`. → на уровне дека сцены готовы, **engine.js не трогаю**.
2. **Настоящий пробел** (это и есть Фаза A): `sims/lab.core.js` рисует ОДНУ случайную ленту (`randParts`) в своём `data-mode` и **сцену не читает**. Нет ни «пусто/примеры/рекуррента», ни перечисления всех Xₙ. Режимы `recur/sum/firstsq` уже делят по первому элементу — но только для ленты A; для C/B/F/H картинки-разбиения нет.
3. **Тексты Cowork** (`raskadrovka/TEKSTY-L1.md`) на месте — 13 слайдов s01–s13, дословные определения из листка + связки в голосе владельца. Ставлю дословно, своей прозы 0.
4. **Слайд = КОНФИГ**: слайды тривиальны (`.zone.copy` с `{{MD:id}}` + `.board>.panel>canvas|data-ill`). Порядок рендера = список `{{SLIDE:id}}` в `shablon.html`; линтер сверяет `slide_order` из `src/brief.md` с файлами `slides/` (0 сирот/дублей). Добавить/убрать слайд ⇒ синхронно: файл + `{{SLIDE}}` в shablon + `slide_order` в brief.
5. **Баги §5 локализованы**: свёртка-картинка — SVG `illustrations/strip-transform.svg` (не канвас); «Меняй ленту — строка меняется сама» — `content/s07-code.md` стр.2; звезда — `illustrations/star.svg` + слайд `s05-masks`; обложка `.sub` — `slides/s01-title.html` стр.4; «2×10»-перенос — текст `content/s02-hook.md`.
6. **Премиса Цекендорфа (§3.13) — ПРОВЕРЕНА численно** (scratchpad `zeck_check.py`): `|C_n|=f_n` ✓ (n≤7); формула `a↦Σ(1−aₚ)·fₚ` даёт биекцию `C_n→{1..f_n}` **только с уточнением**: вес позиции p = fₚ (различные Фибоначчи 1,2,3,5,8…, курсовая нумерация) **плюс смещение +1**. Без «+1» выходит {0..f_n−1} — «f_n чисел», но не «от 1 до f_n». Зашью соглашение в verify и в канвас s13.

### Фаза A — движок (Opus, я сам; расширяю `lab.core.js`, НЕ переписываю)
- **A1. Канвас ← сцена (связь без правки engine.js).** В `syncAndDraw`/`pump` читаю активную сцену из класса `.scene-k` на `canvas.closest('.slide')` (движок его уже ставит) + вешаю `MutationObserver` на `class` слайда → мгновенная перерисовка на клик. Конфиг слайда: `data-stages="empty examples recur"` (индекс = сцена−1). Нет `data-stages` ⇒ прежнее поведение (обратная совместимость, эталон B/subset не ломается).
- **A2. Перечислители (чистые функции на модель).** Все Xₙ для малых n: ленты A (композиции из 1,2) → отображения в C (`code`), B (`subset`), F (`perm`); H — блуждания по P4 длины n+1 (как в `verify_H`). Счёт сверяется с fₙ (в движке — assert в консоль; жёстко — в verify-скрипте).
- **A3. Рендер сцены «примеры»** (новый): колонки n=1..4, в каждой вертикально ВСЕ элементы мини-образами, под колонкой — счёт fₙ (1,1,2,3,5). Конфиг `data-examples="1-4"`. Геометрия заморожена, верх-определение вне канваса.
- **A4. Рендер сцены «рекуррента» — разбиение по первому элементу для КАЖДОЙ модели.** Единый приём (модель-носитель = лента): первая плитка квадрат→остаток Xₙ₋₁ / доминошка→Xₙ₋₂, образ модели нарисован на остатке, два случая. **Без прозы-подписи** («квадрат + A₆» убираю, §3/§5) — только скобка над остатком + метка-множество |Xₙ₋₁| (вид утверждаю на wireframe, ВОПРОС 2).
- **A5. Формат опорных точек C (номер на поле).** CSS + разметка-обёртка слайда: слева на поле крупная Forum-цифра-буквица (номер листка) + мелкий Forum-caps надзаголовок («Задача/Определение/Утверждение/Теорема») + тонкая линейка `--rule` + формулировка Noto в мерной колонке справа. Реализую классами (`.zone.copy.anchored` + `.anchor-num/.anchor-kicker`) в `shablon.html`-CSS; хост — обёртка в HTML слайда (не расширяю MD-диалект). **«Картинка ПОСЛЕ формулировки»** ⇒ на опорных слайдах +1 ведущая сцена: сцена 1 = только формулировка (канвас `empty`), сцена 2+ = канвас проявляется.
- **A6. Численные сверки — НОВЫЙ `verify_models.py`** (не трогаю `verify_skelet.py`/`verify_H.py`): для каждой модели |Xₙ|=fₙ и корректность split (Y≅Xₙ₋₁ ⊔ Z≅Xₙ₋₂); биекция Цекендорфа `a↦1+Σ(1−aₚ)fₚ` = {1..f_n} на C_n. Зелёный — гейт.
- **A7. Обновляю `STANDART-canvas.md`**: сцены (пусто→примеры→рекуррента), формат опорных точек C, «без поясняющих подписей». Финальный контракт формата — туда.

### Фаза B — слайды-конфиги + постановка текстов (механика → субагенты Sonnet ≤5, не коммитят)
Роутер v2 (порядок рендера в `shablon.html`; тексты — ДОСЛОВНО из TEKSTY, определения — листок):

| # | TEKSTY | файл (действие) | канвас/илл · сцены |
|---|--------|-----------------|--------------------|
| 1 | s01 Обложка | `s01-title` (убрать `.sub`) | title-art · 1 |
| 2 | s02 Задача 1 | `s02-hook` (формат C; «2×10» без переноса; **свёртка = сцена 3**; `s03-strip`→`_zadel/`) | hook-2xn + strip-transform(фикс) · условие→замощение→свёртка «по верхней строке» |
| 3 | s03 Модель A | `s04-recur` (+сцены) | `recur` · опр→примеры→счёт→рекуррента |
| 4 | s04 Ещё 7 объектов | **`s05-cols` НОВЫЙ** (замена звезды; s05-masks→`_zadel/`) | 2 колонки: слева определения B–H, справа узкая голубая — пример каждого при n=5 |
| 5 | s05 Модель C | `s07-code` (+сцены; убрать «Меняй ленту…») | `code` · примеры→рекуррента |
| 6 | s06 Модель B | `s08-subset` (+сцены) | `subset` · примеры→рекуррента |
| 7 | s07 Модель F | `s09-perm` (+сцены) | `perm` · примеры→рекуррента |
| 8 | s08 Модель H | **`s13-hwalk` НОВЫЙ** (свой слайд; длина n+1, сдвиг честно) | нов. режим `hwalk` · примеры→рекуррента |
| 9 | s09 Пуанта | **`s14-puanta` НОВЫЙ** | одна рекуррента+база; бонус-сцена Xₙ↔Aₙ (тихо) |
| 10 | s10 Задача 3а | **`s15-langs` НОВЫЙ** (payoff серии) | нов. режим `langs`: один элемент Aₙ на языках B,C,…,H |
| 11 | s11 Задача 3б | `s10-sum` (reveal красной доминошки = ответ) | `sum` · условие→без красной→красная |
| 12 | s12 Задача 4 | `s11-square` | `firstsq` |
| 13 | s13 Задача 5 | `s12-zeck` (+сцена биекции; verify) | `zeck` · жадный→биекция C_n→{1..f_n} |

Итого **13 слайдов** (Задача 1 = 1 слайд, свёртка сценой 3 — реш. владельца). Ретайр: `s03-strip`+`s05-masks`→`_zadel/`, вне `slide_order`. На каждый слайд: `slide_order` (brief) + `{{SLIDE}}` (shablon) + файл — синхронно. Рекуррента-сцена — **без подписей** (реш. владельца): цвет первой плитки + скобка над остатком, метки множества нет.

### Правки владельца по wireframe v1 (2026-07-11) — ОБЯЗАТЕЛЬНЫ до размножения
**A. Формат опорной точки (я ошибся с буквицей).**
- Буквы-буквицы «C» НЕТ. Опора = цветное СЛОВО-надзаголовок красивым шрифтом. «Задача» — КРАСНЫМ (brick) + крупный НОМЕР задачи (на месте, где я ставил буквицу). «Определение» — ДРУГОЙ красивый цвет (не красный), номера/линейки/ничего под словом НЕТ. Утверждение/Теорема — свои цвета.
- Текст-формулировка — НИЖЕ слова (заметно ниже), начинается у ЛЕВОГО поля (там, где начиналось слово), на ВСЮ ширину слайда (не узкая мерная колонка).

**B. Пунктуация:** точки в конце абзацев НЕ ставим (в т.ч. «…оканчивающиеся на 1» — без точки).

**C. Логика сцен (была «ужасной» — переделать):**
- Каждая модель — в СВОём языке: C = строки 0/1, B = подмножества, F = перестановки, H = цифры, A = замощения. Доминошек на слайде C НЕТ — биекция к полоске НЕ установлена на этих сценах (её строим ПОЗЖЕ / в пуанте).
- Порядок: (1) выписываем сами объекты; (2) по счёту 1,1,2,3,5 — гипотеза «это Фибоначчи»; (3) рекуррента по первому элементу; биекция — позже.
- Рекуррента — НЕ фиксированное нарисованное разбиение. Показать МЕХАНИЗМ, в идеале ИНТЕРАКТИВНО: смотрим на первый символ; 1 → дальше любая последовательность (кусок отваливается, n−1); 0 → за ним обязана 1, дальше на 2 короче (n−2). Реализовать во всех моделях (в C просто, есть сложнее — придумать).

**D. Сцена «примеры» — раскладка:**
- Счёт-ЗНАЧЕНИЕ не писать (видно и так).
- «n = k» — ЖИРНО и КРУПНО, слева; объекты длины k — на ТОЙ ЖЕ строке справа.
- Включить n = 0 (пустая последовательность — она одна, это важно).
- НЕ столбцы (много пустого места). Разбить доску на БЛОКИ-прямоугольники разного размера по n (n=0,1,2 мелкие; n=3 средний; n=4 крупный), читать слева-направо/сверху-вниз, максимизировать шрифт.
- «0» горчичным не видно → чёрным (или иначе, но контрастно).

*Статус v3 (после 2-го круга правок владельца): «Определение» → зелёный (steel сливался с доской; документированного цвета в дизайн-системе НЕ нашёл — только формат опоры; жду hex владельца); «n=k» → курсивная засечка (TeX-вид); рекуррента → случайная валидная C-строка, клик по ЛЮБОМУ символу редактирует с сохранением валидности (нет «00», конец=1) — это интерактивный инструмент, не картинка. Артефакт-ревью пересобран с ЖИВЫМ кликабельным канвасом сцены 3 (владелец не видел интерактив в статике). Примеры — ряды с крупными объектами (владелец: блоки желательны, ряды допустимы). verify_models/линтер зелёные, консоль чиста.
Прежний статус v2: движок+формат переработаны под A–D, re-wireframe Модели C сделан и показан (v2). Рекуррента-текст — устно (лектор), не на слайде (реш. владельца). Что реализовано в v2: формат = цветное слово (steel «Определение», без буквицы) + текст на всю ширину без точки; примеры = строки 0/1 блоками по n (n=0..4), «n=k» крупно слева, счёт не пишем, цифры чёрные; рекуррента = ИНТЕРАКТИВ (клик по первому символу: 1→остаток C_{n−1}; 0→форс 1→C_{n−2}), бессловесно; legacy-режимы целы, verify_models/линтер зелёные. Открыто: пустоты в коротких рядах примеров (ряды vs блоки-прямоугольники), цвет слова «Определение».*

### Порядок работ и гейты
1. Фаза A целиком (A1–A7), верификатор `verify_models.py` зелёный, `verify_H`/`verify_skelet` не сломаны.
2. **Wireframe-проверка глазом владельца**: формат C + сцена «рекуррента» на 1–2 моделях (ВОПРОС 2,3) ДО размножения.
3. Фаза B субагентами (тексты дословно, баги §5).
4. Гейты приёмки (§ Критерий): `python3 _generator/build_deck.py fibonacci/src` зелёный + dist собран; verify зелёный; браузер — скриншоты сцен (пусто/примеры/рекуррента) у 2–3 моделей + формат «Задача N» + свёртка 2×n; grep прозы = пусто.

## ВОПРОСЫ — (исполнитель)

Три развилки — вкус владельца. **1 и 2 закрыты владельцем** (AskUserQuestion, 2026-07-11):

1. **Задача 1 — два слайда или один?** → ✅ **ОДИН слайд**, свёртка = сцена 3 (`s02-hook`, 3 сцены; `s03-strip`→`_zadel/`, его свёртка-SVG фикшу и кладу сценой 3). Итого 13 слайдов.
2. **Насколько бессловесна сцена «рекуррента»?** → ✅ **СОВСЕМ без подписей**: цвет первой плитки + скобка над остатком, ни метки, ни прозы. Связь с fₙ₋₁ проговаривает лектор.
3. **Формат C — утверждаю на wireframe** (открыт, не блокер Фазы A). Кегль номера-буквицы, позиция надзаголовка, ширина мерной колонки — покажу на 1 слайде до размножения. **Дефолт: буквица ≈ высоте 2 строк Noto, надзаголовок над номером.**

Побочно (не вопрос, зафиксировано): формула Цекендорфа требует «+1» и весов = различных Фибоначчи (проверено); H даю длиной n+1 (сдвиг помечу тихой скобкой, как в TEKSTY s08).

### Ход размножения (Фаза B)
**Пять моделей ГОТОВЫ** (закоммичено `e5fd9db`, зона fibonacci, точка отката `2190d82` в предках):
- C (строки 0/1), B (множества), F (однострочная π), A (замощения-полоски), H (цифры-обходы) — каждая в СВОём языке; примеры-таблицей (n │ список), счёт всюду 1,1,2,3,5.
- Рекуррента mode-aware, интерактивная «по первому элементу»: C (клик по символу), B (клик по числу вкл/выкл, крайний элемент), A (клик по плитке квадрат⇄доминошка), F (клик — соседняя транспозиция, π(1)), H (клик — другой пример; первый шаг 1→2). Валидность сохраняется, фидбек на запрет.
- Формат опоры: цветное слово (Определение — зелёный по одобрению; Задача — красный + номер, todo).
- Слайды: s04(A)/s07(C)/s08(B)/s09(F) переверстаны, s13-hwalk(H) создан, вписан в shablon+slide_order. verify_models/линтер зелёные. Артефакт-ревью: все 5 моделей (C,B живые), тот же URL.

**⚠ РАЗВОРОТ владельца (2026-07-11, после 5 моделей):** «для каждой модели нужно ПОЛНОЕ объяснение: примеры, рекуррента, БИЕКЦИЯ с A_n». Меняет §3-контракт:
- **Все 8 моделей** (A,B,C,D,E,F,G,H) — полные, не 5. Добавить **D** (разбиения n+2 на слагаемые >1), **E** (разбиения n+1 на нечётные), **G** (0/1-строки a₁≤a₂≥a₃≤…). Заход §3 их парковал («курсу не нужны») — владелец отменил.
- **4-я сцена у каждой модели — «биекция с A_n»**: объект ↔ замощение выровнены (легаси two-zone strip↔образ, интерактив). Полоска-A появляется тут, ПОСЛЕ рекуррент (биекция позже — как договаривались).
- Контракт модели: пусто → примеры → рекуррента → **биекция с A**. Обновить STANDART-canvas.
- Движок: биекция C/B/F — легаси `drawImage` (strip+образ, есть); A — тождество/пропуск; H/D/E/G — их strip-отображение (добавить). D/E/G — новые перечислители+рендер+рекуррента+биекция.

**Осталось (пересмотрено):** сцена «биекция» во все 8 моделей; модели D, E, G (полные); две колонки-роутер (нужны ли, если все 8 полные? — уточнить); Задача 1 (свёртка); Пуанта; задачи 3а/3б/4/5. Заголовки задач — формат C «Задача N» красным. Ретайр s03-strip/s05-masks. Финальный slide_order. STANDART-canvas.md.

## ОТЧЁТ — (исполнитель, ПРОМЕЖУТОЧНЫЙ — Л1 не завершена)

**Точка отката:** `2190d82` (в предках). Коммиты в зоне `fibonacci/**`: `e5fd9db` (движок+5 моделей), `cbbdec2` (биекция-сцена), `93996a5` (убрал звезду, алфавит). Ветка `fibonacci-l1` общая — чужие зоны не трогал, ни разу `-A`.

**Сделано (по гейтам):**
- **Движок Фазы A** (`lab.core.js`, расширен, не переписан): секвенсор сцен читает `.scene-k` (engine.js не тронут) + MutationObserver; нативный рендер объектов в языке модели; интерактивные рекурренты «по первому элементу» (клик редактирует, валидность сохраняется, фидбек); сцена «биекция с A» = переиспользованный легаси two-zone канвас (strip↔образ, двусторонний). Формат опоры — цветное слово (Определение зелёный, Задача красный+номер — todo).
- **5 моделей** (A,B,C,F,H) в СВОём языке: примеры-таблицей (n │ список, счёт 1,1,2,3,5), рекуррента, у C/B — сцена биекции. Слайды переверстаны, s13-hwalk создан.
- **Убрана лишняя звезда** s05-masks (+star.svg) → задел; модели по алфавиту.
- **`verify_models.py`** зелёный (счёт=fₙ, образы-биекции, split, Цекендорф); линтер зелёный (11 слайдов); консоль чиста. Живой дек-артефакт для владельца (кликабельный).

**НЕ тронул:** `matematika.md`, `verify_skelet.py`, `verify_H.py`, задел, фабрику, чужие зоны/проекты, тексты сверх TEKSTY.

**Осталось (директивы владельца, после 5 моделей):**
1. **8 моделей** (добавить D — разбиения n+2 на слагаемые >1; E — n+1 на нечётные; G — 0/1 a₁≤a₂≥a₃…), порядок алфавитный A,B,C,D,E,F,G,H.
2. **Сцена «биекция с A» во ВСЕ 8** (сейчас только C/B; A/F/H/D/E/G — добавить, у H/D/E/G нужно их strip-отображение).
3. **Две колонки** «ещё объекты» (слева список моделей, справа примеры при n=5) — вместо звезды.
4. **Задачи** 1(свёртка, фикс SVG), 3а(langs), 3б(sum reveal), 4(firstsq), 5(zeck+биекция) — формат C «Задача N» красным; переверстать старые s02/s03/s10/s11/s12.
5. Пуанта. Финальный slide_order. `STANDART-canvas.md`. Финальный QA-проход по всем слайдам.

**Стоп-до-цели подтверждаю:** только Л1 (задачи 1–5 листка), задел не трогал, Л2–3 не начинал.
