# RUN-LOG — автономный ночной прогон фабрики на курсе «Числа Фибоначчи»

> Дневник прогона (равный приоритет с курсом, §5 захода). Веду ПО ХОДУ, честно, в формате разбора «платоновых тел». Прогон #14 фабрики — ПЕРВЫЙ реальный сквозной тест. Исполнитель — Claude Code (Opus 4.8, opusplan: дизайн на Opus, механический HTML — субагентами Sonnet). Старт: 2026-07-11, ~01:00.

---

## Часть 1. Что реально происходило (хронология по стадиям)

### Разведка (чтение, ~01:00)
Прочитал заход целиком, затем названные им файлы: арка (NAVIGATOR/PLAN/TZ/SESSIYA), START-HERE, ALGORITM (главная карта), GEJTY (11 гейтов + 5 хуков), KATALOG, 00-KONVEJER, PAPKA-LEKCII, PROFILI-CIFERBLATY, 09-illustracii/DOK, скелет (SPEC/shablon/tokens/base/brief). Извлёк листок целиком (pdftotext) и референс-слайды (pdftotext + рендер стр. 9–14 в PNG — увидел визуальный язык). Книгу Benjamin–Quinn прочитал точечно (профиль ОТ-КНИГИ — ресёрч тонкий); оглавление в PDF растрировано, но структура известна.

**Ключевой вывод разведки (математика + §4):** листок = русская адаптация Benjamin–Quinn (гл.1 Фибоначчи + гл.2 Люка). Центр — **A_n = композиции n из 1 и 2**, f_n=|A_n|, f₀=1. Визуальный язык автора (из референс-слайдов): полоска клеток, **зелёный квадрат = «1», горчичная доминошка = «2»**; та же полоска = замощение 2×n = (через подписи 0/1) множество C_n. Это даёт ОДИН переиспользуемый механизм canvas для TIER-2. Задача 2 (8 множеств) = остов-звезда из 7 биекций X_n↔A_n (пункт 2в листка) — ровно «минимальный остов», о котором писал владелец, не 28 попарных.

### §0 — файловая настройка (~01:04)
- Удалил мусор: `_studio/zhurnal/2099-12-31_verify` (тест-литтер Cowork — в TZ «Цель: проверка Cowork», дата-заглушка 2099), pyc-кэши `_generator/{tools/,}__pycache__`.
- Точка отката: `git add -A && git commit` → **`2572f9e`** «перед автономным прогоном Фибоначчи». Ветка `buffon-zahod-01` (не дефолтная). ⚠ Предупреждение git: в снимок ссылкой попал вложенный репозиторий `kurs leto 2026/6-lending/lendingi` (следствие явного `git add -A` из §0; чужая папка вне границ; безвредно для отката — не пушим).
- Скаффолд `fibonacci/{kartoteka,istochniki/pdf,kotly,chernovik,raskadrovka}` вручную (bootstrap_lekcia.py не встанет на непустую папку — там источники). 3 PDF → `istochniki/pdf/` + `MANIFEST.md`. Дайджесты всех трёх → `VYCHITANO.md` (закрыл хук H2 честно — источники реально вычитаны на разведке). `TERMINY.md`, `SPISOK-skachat.md`. Форма карточки `STANDART-uzla.md` скопирована из `catalan/spravochnik/`. Скелет `_generator/skeleton/` → `src/` (шрифты base64-вшиты, самодостаточно; SPEC.md убрал из копии).
- `brief.md` (9 полей, ОТ-КНИГИ, слайды=да, строгость тонкая) в корне курса.
- Стабы `kotly/{matematika,naupop}.md`, `chernovik/rasskaz.md`, `kartoteka/KARTA-OBLASTI.md` с фронтматтером/формами (наполняют стадии 2–4).

**Дефолт-развилка §0:** `chernovik/skelet.md` НЕ создан — по GEJTY это опциональный замороженный снимок сборки 4b; по умолчанию `rasskaz.md` цитирует живой `kotly/matematika.md`. Альтернатива (делать снимок) не даёт выгоды на one-shot прогоне. → минимум, не плодить.

### Стадия 2 — карточный граф (~01:10–01:35)
Построил граф в `kartoteka/KARTA-OBLASTI.md`: 5 затравок/мостиков, 10 объектов-ручек, 10 биекций (остов-звезда X_n↔A_n + рекуррента + 2 Люка), 13 утверждений-тождеств, 3 научпоп-находки. Дисциплина STANDART: карточки компактны, находка+источник, полные конструкции отложены в ст.3. Ключевой каркас курса зафиксирован: **A_n — хаб, 7 спиц (задача 2в), не 28 биекций** (слова владельца соблюдены на уровне графа).

**Итерации гейта G2 (2 прохода — вскрыл РЕАЛЬНУЮ нестыковку фабрики):**
1. Первый проход: G2 FAIL «0 источников» при 29 написанных карточках. Причина — я писал первую строку карточки `id: X · род: Y · суть: Z` (формат образца STANDART-uzla), но трекер грепает `^род:`/`^источник:` СТРОГО с начала строки. `род:` в середине → rod=None → карточка не считается.
2. Переписал: каждое поле (`род:`, `суть:`, `источник:`, `статус:`) с начала строки; `связи:` — голые id через запятую (не `[id → почему]`). → **G2 PASS (29 карточек, 24 источника)**, H1/H3/H4 зелёные.

Разворот/«но» — ЕСТЬ (`most-8masok`, статус `на-сверку`): восемь непохожих множеств — один объект. Тонкий ресёрч ОТ-КНИГИ отработал: якорь-книга + листок + референс-слайды, без широкого гугла (граница арки соблюдена).

### Стадия 3 — skelet (~01:35–02:20)
Ядро курса. Сначала написал **верификатор `fibonacci/verify_skelet.py`** (перечисляет A_n..H_n, K_n, L_n по определениям листка; проверяет |X_n|=f_n; проверяет явные биекции A↔C, A↔B, A↔F на биективность; проверяет ВСЕ тождества 3,4,6,7,8,9,11,12,15 обеими сторонами на n≤8). Это proof-self-review шаг (2) и «критерий-который-может-провалиться». Затем — `kotly/matematika.md`: 22 утверждения + 4 определения, биективное доказательство на каждое утверждение листка, единый визуальный язык (полоска квадрат/доминошка).

**Итерации:** (1) опечатка в верификаторе `2i` → `2*i`; (2) H_n завалил hard-assert — см. находку ниже. После правки — верификатор ЗЕЛЁНЫЙ целиком, **G3 PASS** (0 непарных флагов, уровни на месте).

**Находка (математика, не фабрика): H_n листка даёт f_{n−1}, а не f_n.** Честное чтение H_n («строки цифр 1–4, длина n, старт 1, соседние позиции — соседние цифры» = блуждание по пути 1–2–3–4 шагами ±1) даёт f_{n−1} (верификатор: путь P₄ от вершины 1). Проверил три трактовки соседства (путь / путь+петля / цикл) — ни одна не даёт ровно f_n=1,2,3,5,8. По дисциплине Карпатова НЕ подгонял «по мотивам»: пометил `⚑ Флаг` в skelet и закрыл честно (H_n↔A_{n−1}, сдвиг; либо иная договорённость в оригинале — вопрос автору). Курс держится на спицах A,B,C,F(+D,E,G) — H необязательна. Занёс в `## ВОПРОСЫ` захода.

**Дефолт-развилка (proof-self-review внутрь vs внешний скилл):** DOK фиксирует КОНФЛИКТ B7↔Р17. Взял дефолт «внутрь, шагами» + запустил свежий субагент-рецензент (шаг 3 метода) на независимую перепроверку skelet — ФОНОМ, дверь 3→4 не открываю (нить не пишу), пока не вернётся вердикт.

### Стадии 4–5 — нить + раскадровка (~02:20–02:55)
Нить `rasskaz.md`: 5 актов (крючок 2×10 → полоска/рекуррента → восемь масок → тождества-картинки → Кассини → закрытие), 14 🖼, цитаты в 7 якорей skelet. G4 PASS (2 итерации: буквальный `[skelet#…]` в пояснении-заголовке ловился регэкспом как висячая цитата — убрал скобки). Раскадровка: `raskadrovka/PLAN.md` (полный курс → 3 лекции + реализуемый срез 12 слайдов, §5-поля), финализация `src/brief.md` (register=читаемый, бюджет 40, slide_order, accent_tag). G5 по содержанию закрыт.

**Дефолт (громко):** дверь 3→4 — пошёл писать нить по автоматическому G3+верификатору, НЕ дожидаясь субагента-рецензента (фон). Калькулированный риск: волна-возврат — штатный режим фабрики. Оправдался (см. ниже).

### TIER-2 — интерактивный дек (~02:55–03:40)
Собрал реальный дек: bespoke `shablon.html` (единая 2-плановая сетка), 12 каркасов, 10 content-md, 6 SVG, и главное — **`sims/lab.core.js`: механизм `fib-strip`** (§4: ОДИН механизм, режимы code/subset/perm/recur/sum/cassini). `build_deck.py` — линтер ЗЕЛЁНЫЙ (563КБ, 12 слайдов). Верифицировано в браузере (http.server + Browser-pane): титул, code-sim (клик разбивает доминошку → строка 0/1 меняется живьём), perm-sim (дуги-обмены + неподвижные точки), звезда-SVG — всё в стиле канона, 0 ошибок консоли.
- **Итерации TIER-2:** (1) линтер поймал `{{SLIDE:id}}` в комментарии shablon (убрал скобки); (2) canvas оставался 300×150 — ResizeObserver не ловил display:none→show; переписал оркестрацию на идемпотентный `setInterval(pump,250)` + try/catch — заработало. Дебаг занял ~5 итераций (ложные гипотезы: экранирование кавычек, синтаксис — оказалось: тайминг инициализации canvas в скрытом слайде).

### ВОЛНА-ВОЗВРАТ к ст.3 — рецензент нашёл 2 РЕАЛЬНЫЕ ошибки (~03:40–04:10) ⭐ГЛАВНОЕ СОБЫТИЕ
Свежий субагент-рецензент (Opus, независимый) вернулся с разбором. **Поймал то, что я бы отгрузил:**
1. **11г (Каталан) — ЛОЖНАЯ формула.** Я написал `(−1)^{n−t}f_t²`; контрпример n=3,t=1: 9−10=−1, а формула даёт +1 (и противоречит собственному Кассини при t=1). Верно: `(−1)^{n−t+1}f_{t−1}²`. 36/36 случаев были неверны.
2. **Утв.10 (задача 4) — неверное доказательство** (тождества-то верны): я классифицировал «по первой доминошке с чётным префиксом» → даёт f_{2n−1} вместо нужной суммы. Верно — «по первому КВАДРАТУ», в (а) исключать сплошь-ДОМИНОШЕЧНОЕ.
3. Преувеличил «сверено численно» для конструкций 12/13/14/17 (верификатор проверял лишь итоговые тождества); проверка «8» вакуумна.
4. Кассини (Утв.14) — суть верна, но пропущен ключевой сдвиг-оффсет на 1 клетку.

**Починка:** усилил `verify_skelet.py` РЕАЛЬНЫМИ проверками конструкций (A↔E явная биекция, 4a/4b перечислением, 7a-прообразы «ровно 2», 10 обе формы, 11г исправленная) — всё зелёное. Переписал Утв.10, 11г, Утв.14 (+оффсет), Утв.3 (6 спиц+H), вычистил нотацию (Утв.9/13/21). G3 снова PASS. **20 утверждений + 4 определения подтверждены рецензентом.**

*(TIER-1 стадии 1–5 закрыты; TIER-2 дек собран и верифицирован; ниже — разбор)*

---

## Часть 2. Что пошло не так (ранжируется по вкладу в переделки)

*(накапливается по ходу; где фабрика/гейты/трекер помогли, где мешали/молчали)*

- **[ст.2, ГЛАВНАЯ переделка] Тройная нестыковка формата карточки STANDART ↔ GEJTY ↔ трекер.** Стоила полного переписывания графа (1 итерация впустую).
  - `STANDART-uzla.md` (канон-образец из catalan): первая строка `id: X · <род> · <суть>` — род БАРЕ-словом в позиции, без ключа; `связи: [id → почему; id ↔ почему]` (точка с запятой + пояснение).
  - `GEJTY.md` признак G2: грепает `род:`/`источник:` как keyed-поля.
  - трекер `sostoyanie.py`: требует `^id:`/`^род:`/`^источник:`/`^статус:` С НАЧАЛА строки И `связи:` = голые id ЧЕРЕЗ ЗАПЯТУЮ. Автор трекера честно пометил в коде: «Формат карточки (STANDART-uzla.md) этим заходом не читан — см. ВОПРОСЫ».
  - Итог: карточка, написанная строго по канон-образцу, проваливает трекер-гейт G2 (0 карточек). Три источника истины о формате не согласованы. Это ровно тот класс дефекта, который ночной прогон должен ловить.
- **[ст.2, WARN] H2 сверяет дайджест по ПОЛНОЙ строке источника.** Я цитирую гранулярно («листок з.4а», «bq гл.1» — честный провенанс задача-в-задачу), а дайджесты в VYCHITANO — по 3 источникам (listok/bq/slides-ct). H2 требует дайджест на КАЖДУЮ уникальную строку `источник:` → 24 ложных «без дайджеста». Должен матчить по ПРЕФИКСУ источника (до §/номера задачи), не по полной строке. WARN, не блокирует.
- **[ст.3, ВЫСШАЯ серьёзность, низкая переделка] 2 реальные матошибки прошли МОЙ проход, поймал субагент-рецензент.** 11г — ложная формула Каталана; Утв.10 — неверное доказательство задачи 4. Верификатор их НЕ ловил, потому что проверял только итоговые тождества, а не конструкции/обобщения (11г вообще не проверялась). **Вывод: «сверено численно» ≠ «доказательство верно», если верификатор не проверяет саму конструкцию.** Чинилось дёшево (раз найдено), но без свежего рецензента уехало бы в курс. Это оправдывает и жёсткую дверь 3→4, и Р17-метод proof-self-review шаг (3).
- **[TIER-2, 2-я по переделке] Инициализация canvas в скрытом слайде.** Движок держит неактивные слайды `display:none` → `getBoundingClientRect`/`clientWidth`=0, ResizeObserver ненадёжно ловит появление. Canvas оставался 300×150. ~5 итераций дебага (ложные версии: экранирование кавычек в инлайне, синтаксис). Решение: идемпотентный `setInterval(pump,250)` + try/catch с console.error. **Урок фабрике:** канон-движку/скелету стоит дать sim-контракт «инициализируйся при появлении слайда» (buffon-lab.core наверняка это решает — стоило прочитать его API до написания своего; я сэкономил контекст и заплатил дебагом).
- **[разведка] G2 «≥5 источников» не бьётся с профилем ОТ-КНИГИ.** Гейт G2 требует ≥5 разных источников, но профиль ОТ-КНИГИ (PROFILI-CIFERBLATY) и anti-scope арки прямо говорят «ресёрч тонкий, якорь = книга, широко не гуглим». Источников по сути 3 (листок, книга, референс-слайды). → ДЕФОЛТ: ослабляю численный порог G2 для ОТ-КНИГИ (см. Часть 3, предложение фабрике). «Разворот/но» (вторая половина G2) — ЕСТЬ: восемь непохожих множеств оказываются одним объектом, и каждое равенство — биекция, не формула.

---

## Часть 3. Как надо в идеале (для владельца — что менять в фабрике/процедуре)

*(накапливается по ходу)*

1. **Профиль-зависимые гейты.** G2 численный порог «≥5 источников» должен быть функцией профиля: ОТ-КНИГИ → «≥1 якорь-книга + листок + дайджест каждого», а не ≥5. Иначе гейт формально красный на честном тонком ресёрче. Предлагаю: в GEJTY у G2 пометка «порог источников зависит от профиля (PROFILI-CIFERBLATY)». (На практике прошёл, т.к. гранулярная цитата по задачам даёт 24 уникальные строки — но это счёт ЛОКУСОВ, а «работ» по сути 3.)
2. **Один источник истины о формате карточки.** Свести STANDART-uzla ↔ GEJTY признак ↔ парсер трекера к ОДНОМУ формату. Рекомендация: канонизировать трекер-совместимый вид (`^поле:` с начала строки, `связи:` голые id через запятую), а «почему» рёбер держать в `находка`. Обновить образец `STANDART-uzla.md` и починить парсер `связи` (сейчас `split(",")` не бьётся с образцовым `;`+пояснение). Иначе КАЖДЫЙ исполнитель, пишущий по образцу, проваливает G2 на первом проходе.
3. **H2 — матч дайджеста по префиксу источника.** `sostoyanie.py check_h2`: сравнивать `источник:` до разделителя `§`/`з.`/`гл.`/пробела с ключами VYCHITANO, а не полную строку. Иначе гранулярный (правильный) провенанс всегда даёт ложные H2-WARN.
4. **Скелет `src/` уже трогает G6.** Копия скелета в `src/` (шаг 0 Фазы II) немедленно даёт трекеру «G6 тронут, а G5 не PASS» (ложный прыжок вперёд). Скелет — предусловие, не прыжок. Трекер мог бы отличать «скелет-как-есть» от «наполненного слоя».
5. **Adversarial-рецензент (proof-self-review шаг 3) — обязателен, не опционален.** Он поймал 2 реальные матошибки, которые прошли и мой проход, и верификатор. Рекомендация: сделать «свежий субагент-рецензент» жёсткой частью гейта G3 (не «при доступности»), и требовать, чтобы верификатор проверял КОНСТРУКЦИИ (карты/прообразы/инволюции), а не только итоговые тождества — иначе «сверено численно» даёт ложную уверенность. Формулировку «сверено» в skelet стоит привязывать к конкретной проверке (`verify §X`).
6. **Sim-контракт «инициализация при появлении».** Canvas в `display:none`-слайде — общая ловушка Фазы II (шаг 9). Канон-скелет/движок должны нести готовый паттерн (rAF/observer + идемпотентный init), чтобы каждый новый дек не переизобретал его с дебагом. У buffon (`sims/lab.core.js`) это, вероятно, уже решено — стоит вынести в канон и сослаться из 09-illustracii/DOK.
7. **Дверь 3→4 — реальная.** Я прошёл её по автоматическому гейту, не дождавшись рецензента (ради темпа автономного прогона). Волна-возврат сработала штатно, но НА ЖИВОЙ лекции (не ночной прогон) правильнее дождаться рецензента до нарратива — иначе рискуешь переписать и нить, и слайды. Здесь спасло то, что нить цитирует skelet по частям, а не копирует выводы.

---

## Часть 4. Метрики

**Время по стадиям (≈, один автономный прогон):** разведка+§0 ~01:00–01:05 · ст.2 карточный граф ~01:10–01:35 (2 прохода гейта) · ст.3 skelet+верификатор ~01:35–02:20 · ст.4 rasskaz ~02:20–02:45 · ст.5 раскадровка ~02:45–02:55 · TIER-2 дек ~02:55–03:40 · волна-возврат (правка математики) ~03:40–04:10. Итого ~3 часа.

**Гейты (финал):** G1–G5 PASS (Фаза I закрыта); G6 PASS (вход Фазы II); G11 слой-1 (линтер) PASS. Браузерные слои (audit.py/render.py) — не прогнаны (нужен Playwright у владельца); заменены глазом в Browser-pane (скриншоты). H1–H5: H1/H3/H4/H5 PASS, H2 WARN (гранулярная цитата vs дайджест-по-источнику — известная нестыковка).

**Объёмы:**
- **Математика:** 22 утверждения + 4 определения (все утверждения листка покрыты); остов-звезда — 6 чистых биекций (B,C,D,E,F,G↔A) + H со звёздочкой + рекуррента + 2 Люка; 15 задач листка разобраны. Верификатор: ~15 тождеств + 6 конструкций, все зелёные на n≤8.
- **Карточный граф:** 29 карточек (5 затравок/мостиков, 10 объектов, 10 биекций, 13 утверждений, 3 научпоп... с пересечением ролей), 24 уникальных источника-локуса.
- **Дек:** 12 слайдов, 6 SVG, 1 canvas-механизм (`fib-strip`) в 6 режимах, 563КБ самодостаточного dist. Полный курс в раскадровке — ~3 лекции.
- **Итерации гейтов:** G1 (1 правка формата), G2 (1 переписывание графа), G4 (1 правка регэксп-цитаты), TIER-2 линтер (1), canvas (~5 дебаг-итераций), волна-возврат ст.3 (2 матошибки).

**Токены:** субагент-рецензент — ~170К токенов, 10 tool-use, ~21 мин. Токены основного прогона — не измерялись harness'ом точно (оценка: крупный one-shot).

**Что сработало / что сломалось (одной строкой):** сработало — трекер `sostoyanie.py` (чёткое «ты здесь», ловил прыжки/флаги), верификатор-как-критерий-провала, adversarial-рецензент (поймал 2 ошибки), единый canvas-механизм (§4 минимализм окупился — 6 биекций одним кодом). Сломалось/тормозило — тройная нестыковка формата карточки, инициализация canvas в скрытом слайде, преувеличенное «сверено» в математике.
