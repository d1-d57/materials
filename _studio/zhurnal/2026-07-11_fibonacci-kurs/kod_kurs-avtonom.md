# Канал исполнителя — АВТОНОМНЫЙ НОЧНОЙ ПРОГОН: курс «Числа Фибоначчи» end-to-end (первый реальный тест фабрики)

> Твой единственный файл-заход. Автономный ночной прогон ВСЕЙ фабрики на курсе Фибоначчи. Работай ДИСК-ПЕРВЫМ (каждая стадия пишется в папку курса ДО перехода дальше), веди ПОДРОБНЫЙ ДНЕВНИК по ходу, НЕ жди владельца: где нужен его вкус — ставь разумный дефолт и ГРОМКО пиши в дневник. Метрика — качество курса + честный дневник.

**Модель / усилие:** Effort = **High (Smarter)** — решает качество дизайна биекций, не скорость. Модель — **Opus на весь прогон** (дизайн-тяжёлый one-shot). Допустимо `opusplan` (Opus планирует, Sonnet делает механический HTML) ради экономии токенов. **Цель — собрать весь интерактивный дек за ОДИН прогон** (с минимализмом §4 это реалистично); TIER-1 — гарантированный минимум, если не хватит.

**Текст запуска (владелец → Claude Code):**
```
Модель: opusplan — дизайн Фазы I на Opus, механический HTML на Sonnet.
Claude Code, твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-11_fibonacci-kurs/kod_kurs-avtonom.md. Прочитай ТОЛЬКО его и то, что он называет. АВТОНОМНЫЙ НОЧНОЙ ПРОГОН: не жди меня, ставь разумные дефолты, веди подробный дневник. План/вопросы/отчёт — в этот же файл внизу.
```

КОНТЕКСТ. Проект — фабрика научпоп-презентаций `_studio/` (обёртка идея→слайды: `bootstrap` → трекер `sostoyanie.py` → единый `ALGORITM`+`GEJTY` → `KATALOG`; профили — `PROFILI-CIFERBLATY`). Это **ПЕРВЫЙ реальный прогон фабрики (#14)** и её сквозной стресс-тест. Курс «Числа Фибоначчи», профиль **ОТ-КНИГИ**: содержание уже в одном листке + одной книге → ресёрч тонкий, якорь = книга → сразу разбор.

**Что это за прогон.** НЕ обычный по-стадийный заход. Фабрика спроектирована под по-стадийные сессии с гейтами АВТОРА; здесь мы намеренно гоним ВСЁ автономно за одну ночь. К утру нужно: (1) **полный сценарий** — математика со всеми биекциями + нарратив + раскадровка (~25–30 слайдов); (2) по возможности — первый **интерактивный дек**; (3) **РАВНЫМ приоритетом — подробный дневник прогона**, по которому владелец увидит, что сработало и что сломалось. Дисциплина «диск-первым + громкий дневник + разумные дефолты» — критична (без неё длинный автономный прогон дрейфует — это урок разбора «платоновых тел»).

## 0. ПЕРВЫЙ ХОД — файловая настройка + точка отката
Рабочая папка — корень репо `/Users/ivanyakovlev/Documents/GitHub/materials`.
1. **Убрать мусор** (тест-литтера Cowork + pyc):
```
rm -rf _studio/zhurnal/2099-12-31_verify _generator/tools/__pycache__ _generator/__pycache__ 2>/dev/null
```
2. **Точка отката:**
```
rm -f .git/index.lock 2>/dev/null; git add -A && git commit -m "перед автономным прогоном Фибоначчи (точка отката)"
```
3. **Завести рабочую папку курса** в `materials/fibonacci/` по канону `_studio/docs/spravka/PAPKA-LEKCII.md`. ⚠ `bootstrap_lekcia.py` НЕ встанет (папка непустая — там источники) → создай структуру вручную/дозаписью, повторяя ровно тот layout: подпапки `kartoteka/ istochniki/ kotly/ chernovik/ raskadrovka/ src/`; `brief.md` (профиль ОТ-КНИГИ · слайды=да · строгость тонкая · аудитория 179 школа 8→9 кл, 2 группы · throughline «каждое тождество Фибоначчи — биекция, не формула»); все источники из корня `fibonacci/` (PDF-книга, PDF-листок, слайды «комбинаторные тождества») перенеси в `istochniki/pdf/` и зарегистрируй в `istochniki/MANIFEST.md`; скопируй канон-скелет `_generator/skeleton/` в `src/`. Стабы (`kartoteka/{STANDART-uzla,KARTA-OBLASTI}.md`, `kotly/{matematika,naupop}.md`, `chernovik/{rasskaz,skelet}.md`, `istochniki/{VYCHITANO,TERMINY}.md`) — по формам `PAPKA-LEKCII` / образцам `../catalan/spravochnik/`.
4. **Прочитать процедуру фабрики** (только это): `fibonacci/START-HERE.md` · арка `_studio/zhurnal/2026-07-11_fibonacci-kurs/{NAVIGATOR,PLAN,TZ}.md` · `_studio/konvejer/ALGORITM.md` (драйвер обеих фаз — твоя главная карта) · `konvejer/GEJTY.md` (гейты — самопроверка) · `docs/spravka/PAPKA-LEKCII.md` · `konvejer/00-KONVEJER.md` + пакеты `01–10/DOK.md` (КАК исполнять каждую стадию) · `konvejer/KATALOG.md` (инструмент-под-задачу) · `docs/spravka/PROFILI-CIFERBLATY.md` (ОТ-КНИГИ) · `konvejer/09-illustracii/DOK.md` + `ls ../buffon/src/sims/` (образец canvas-sim для интерактива биекций).
5. **Прочитать ИСТОЧНИКИ курса** (pdftotext, НЕ листать картинками): листок `Числа_Фибоначчи.pdf` (15 задач / ~20 пунктов — каркас курса), книга `Benjamin, Quinn — Proofs that Really Count.pdf` (главы про Фибоначчи — откуда биекции и указания), слайды «комбинаторные тождества» (стиль показа + частичный контент, можно переиспользовать кусочки).
ПЛАН — в `## ПЛАН` перед действиями (разбей на стадии, отметь TIER-1/TIER-2).

## 1. АВТОНОМИЯ + ДИСЦИПЛИНА
- **Не жди владельца.** Где фабрика говорит «чекпоинт/развилка владельцу» — поставь разумный ДЕФОЛТ, ГРОМКО запиши в дневник (что выбрал, почему, какая была альтернатива), продолжай.
- **Диск-первым.** Каждая стадия пишет результат в `fibonacci/` ДО перехода дальше (состояние переживёт дрейф контекста). После каждой стадии — `python3 _generator/tools/sostoyanie.py fibonacci` и «ты здесь»/гейты в дневник.
- **Карпатов:** предпосылки вслух; биекции — НАСТОЯЩИЕ доказательства (не «по мотивам»); критерий-который-может-провалиться на каждой стадии.
- **Чистый стоп.** Заверши TIER-1 ПОЛНОСТЬЮ и на диске, прежде чем начинать TIER-2. Кончается контекст/силы — остановись на ЧИСТОЙ границе стадии, зафиксируй в дневнике «где стоп / что дальше».

## 2. ГРАНИЦЫ
- НЕ трогать фабрику (`_studio/konvejer`, `docs`, `_generator/tools`, ядро `_generator`) — только читать/использовать. Пишешь ТОЛЬКО в `fibonacci/` + дневник арки.
- Бюджет слайдов **≈ число задач/пунктов** (доказательства без текста → слайдов заметно меньше; ориентир ≤30, ~10/лекция; меньше — лучше). Сцен на слайд — ограниченно.
- Ресёрч ТОНКИЙ (ОТ-КНИГИ): не гуглить широко; источник = листок + книга + слайды. Биекции элементарны (дух биекций / указания Бенджамина-Куина; «следующие задачи аналогичны предыдущим»).

## 3. ЗАДАЧА — прогнать фабрику end-to-end по `ALGORITM`, профиль ОТ-КНИГИ

**TIER 1 — ПОЛНЫЙ СЦЕНАРИЙ (обязательно, приоритет; гарантированный выход):**
- **Ресёрч = карточный граф (тонкий).** Из листка+книги: объекты (множества, считаемые Фибоначчи), биекции, тождества → карточки `kartoteka/` (`STANDART-uzla`) + `KARTA-OBLASTI`; дайджест источников `istochniki/VYCHITANO`. Гейт G2: ни одной находки без карточки.
- **skelet (тонкий, но ПОЛНЫЙ по биекциям)** → `kotly/matematika.md`. Каждое утверждение листка → биективное доказательство (Определение/Утверждение/Доказательство-биекция/Источник/Уровень). ⚠ **Дисциплина минимальности (важно, слова владельца):** задача, где дано ~8 определений множеств, считаемых Фибоначчи, и просят установить биекции — это НЕ 28 попарных биекций, а **минимальный остов ~7 биекций** (связать все через одно ядро — `X_n`/`A_n` — как дерево на графе). Не тупить, не тратить лишнюю энергию. Гейт G3: 0 открытых `⚑ Флаг`, биекции настоящие.
- **rasskaz (нить)** → `chernovik/rasskaz.md`. Последовательный разбор задач, process-first (каждый объект — из конкретного вопроса), биекция-throughline, без формул/индукции.
- **Раскадровка** → `raskadrovka/` + каркасы в `src/`. Слайд-модель §4: условие+доказательство (обычно на ОДНОМ слайде со сценами), слайдов ≈ число задач/пунктов; **доказательства — canvas БЕЗ текста**; бюджет+регистр в `brief.md`. Гейт G5.

**TIER 2 — ИНТЕРАКТИВНЫЙ ДЕК (если хватит контекста/сил, ТОЛЬКО после чистого TIER-1):**
- Текст (06) → вёрстка (07) → сцены (08) → **интерактив-биекции canvas/`lab.js` (09)** → сборка (10, `python3 _generator/build_deck.py fibonacci/src`). Хотя бы первые 3–5 слайдов как proof-of-concept, если не весь дек. Гейт: линтер `build_deck.py` зелёный.

## 4. ИНТЕРАКТИВ И МИНИМАЛИЗМ — ядро эксперимента (слова владельца, ВАЖНО)
**Большинство доказательств — БЕЗ ТЕКСТА ВООБЩЕ.** Биекция/соответствие на уровне картинки — это просто картинка: экран делится на ДВЕ части (вертикальная линия: слева/справа; ИЛИ горизонтальная: сверху/снизу), один объект ↔ другой. Ни комментариев, ни символов (исключение — `0/1` в двоичных объектах). Это сильно упрощает и текст-стадию (06), и вёрстку: **условия задач несут текст, доказательства — чистая графика.**

**Механика canvas** (образец — `../buffon/src/sims/lab.core.js` + `sims/ext/*`): одна сторона редактируема (заполняешь полоску доминошками / двигаешь путь / меняешь подмножество), другая = её образ под соответствием, обновляется живьём. Придумай ОДИН механизм и переиспользуй.

**Модель слайда (цель — супер-минимализм):** условие (текст) + доказательство (canvas, без текста). Лучше — оба на ОДНОМ слайде через сцены:
- сцена 1 — условие задачи;
- сцена 2 — объекты слева/справа (сверху/снизу) появляются, соответствие ещё НЕ установлено (наводим на идею);
- сцена 3 — соответствие показано на примере;
- сцена 4 — интерактив: меняешь одну сторону → меняется другая.
Тогда и постановка, и решение — на одном слайде, слайдов ≈ **число задач/пунктов**.

**Где интерактив сложен — не насилуй:** статичная картинка соответствия или один финальный canvas. Демо-минимум приемлем: слайд-условие + слайд-canvas-доказательство, без единого символа.

**Визуальный стиль** — по референс-слайдам автора в `fibonacci/` («комбинаторные тождества» / приложенные слайды): тот же чистый минималистичный дух, но HTML + canvas. Список задач на слайдах — как в листке. Развилки визуала — дефолт + громко в дневник.

## 5. ДНЕВНИК ПРОГОНА — РАВНЫЙ приоритет с курсом
Файл `_studio/zhurnal/2026-07-11_fibonacci-kurs/run-log.md`, ведёшь ПО ХОДУ (не в конце), честно и подробно, в формате разбора «платоновых тел»:
- **Часть 1. Что реально происходило** — хронология по стадиям: что делал, сколько итераций, где буксовал, какие дефолты ставил на развилках и почему.
- **Часть 2. Что пошло не так** — проблемы, ранжированные по вкладу в переделки; где фабрика/гейты/трекер ПОМОГЛИ, где мешали или молчали.
- **Часть 3. Как надо в идеале** — что изменить в фабрике/процедуре по итогам (для владельца).
- **Часть 4. Метрики** — время по стадиям, число итераций, токены (если доступны), сколько вышло слайдов/биекций/сцен.
Плюс после каждой стадии — строка в `SESSIYA.md` арки + прогон `sostoyanie.py` (гейт-статус в дневник).

## 6. ФИНАЛ
- Обнови `PLAN.md` арки (что закрыто / где стоп / что дальше). Курс готов целиком → предложи закрыть арку (по `ARKA §10 C`: durable + следующая арка). Стоп посередине → чёткий хэндофф в `PLAN`/`run-log`.
- `git add -A && git commit -m "автономный прогон Фибоначчи: <докуда дошёл>"`.

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- `fibonacci/` заполнена по `PAPKA-LEKCII`; источники в `istochniki/`, мусор (`2099-12-31_verify`, pyc) удалён.
- `kotly/matematika.md` несёт биективное доказательство на КАЖДОЕ утверждение листка; 0 открытых `⚑ Флаг`; задача-на-8-определений решена минимальным остовом (~7 биекций через `X_n`/`A_n`), не 28.
- `chernovik/rasskaz.md` — связный разбор, биекция-throughline.
- `raskadrovka/`+`src/` — ≤30 слайдов с дизайном сцен биекций; `brief.md` несёт бюджет+регистр.
- **`run-log.md` — подробный, честный, по формату §5 (ОБЯЗАТЕЛЬНО, даже если курс не доделан до конца).**
- `sostoyanie.py fibonacci` прогнан, гейт-статус в дневнике; всё закоммичено.

## ПЛАН — (заполняет исполнитель)

> Составлен после чтения: заход · арка (NAVIGATOR/PLAN/TZ/SESSIYA) · START-HERE · ALGORITM · GEJTY · KATALOG · 00-KONVEJER · PAPKA-LEKCII · PROFILI-CIFERBLATY · 09-illustracii/DOK · скелет (SPEC/shablon/tokens/base) · листок (15 задач, извлечён целиком) · референс-слайды (стиль показа, отрендерены) · catalan/STANDART-uzla. Книгу читаю точечно (профиль ОТ-КНИГИ — ресёрч тонкий).

**Метрика прогона:** качество курса + честный дневник (равный приоритет). Дисциплина: диск-первым, громкий дневник, разумные дефолты на развилках.

### Ключевое математическое решение (ядро §4 + слова владельца)
Листок = русская адаптация Benjamin–Quinn «Proofs That Really Count», гл. 1 (Фибоначчи) + гл. 2 (Люка). Центр всего — **A_n = композиции n из 1 и 2**, f_n = |A_n|, f₀=1. Визуальный язык автора (из референс-слайдов): **полоска клеток, зелёный квадрат = «1», горчичная доминошка = «2»**; та же полоска ЕСТЬ и замощение 2×n, и (через подписи 0/1) множество C_n. Доказательства — БЕЗ ТЕКСТА, чистая перекладка полоски.
- **Задача 2 (8 множеств) = НЕ 28 биекций, а остов-звезда из 7**: каждое X_n ↔ A_n (это ровно пункт 2в листка). A_n — ядро-хаб.
- Тождества 3,4,12 (суммы) · 6,10 (свёртки/разрезы) · 7 (кратные прообразы) · 11 Кассини (инволюция переклейки хвостов) · 5 Цекендорф · 8,9 (через 2^n и сочетания) · 13–15 (Люка, круговые замощения) — все биективны/двойным подсчётом.

### Стадии (диск-первым, гейт после каждой, строка в дневник)
- [x] **§0 Настройка.** Мусор удалён, точка отката (`2572f9e`). Скаффолд `fibonacci/{kartoteka,istochniki,kotly,chernovik,raskadrovka,src}` + `brief.md`; 3 PDF → `istochniki/pdf/` + MANIFEST; скелет → `src/`.
- [ ] **Стадия 1 — brief.** 8 полей, ОТ-КНИГИ, слайды=да, тонкая строгость. Гейт G1.
- [ ] **Стадия 2 — карточный граф (тонкий).** Объекты A–H (+K,L Люка), 7 биекций остова, ~14 тождеств → карточки + KARTA-OBLASTI + VYCHITANO/TERMINY. Гейт G2 (⚠ дефолт: «≥5 источников» ослаблен для ОТ-КНИГИ — см. дневник).
- [ ] **Стадия 3 — skelet (`kotly/matematika.md`).** Биективное доказательство на КАЖДОЕ утверждение листка; остов ~7 биекций через A_n. proof-self-review + численная сверка малых n. 0 открытых `⚑ Флаг`. Гейт G3.
- [ ] **Стадия 4 — rasskaz (`chernovik/rasskaz.md`).** Нить: последовательный разбор, process-first, биекция-throughline; 🖼-описания по тактам. Гейт G4.
- [ ] **Стадия 5 — раскадровка + `src/`.** Слайд-модель §4 (условие-текст + доказательство-canvas-без-текста, сцены). ≤30 слайдов, ≈ число задач; slide_order+регистр+бюджет в `brief.md`. Гейт G5.
- [ ] **TIER-2 — интерактивный дек.** ОДИН механизм canvas (Фиб-полоска: редактируешь квадраты/доминошки слева → образ справа). 6→10, сборка `build_deck.py`. Минимум 3–5 слайдов PoC; цель — весь дек. Гейт: линтер зелёный.

### TIER-1 (гарантированный) = стадии 1–5 + дневник. TIER-2 (если хватит) = дек. Чистый стоп на границе стадии.

## ВОПРОСЫ — (заполняет исполнитель)

> Автономный прогон: не жду ответов, ставлю дефолты и пишу их здесь + громко в дневник. Владельцу на приёмку.

1. **H_n листка (задача 2) даёт f_{n−1}, а не f_n.** Честное чтение «строки цифр 1–4, длина n, старт 1, соседние позиции — соседние цифры» = блуждание по пути 1–2–3–4 шагами ±1 → ровно f_{n−1} (проверено `verify_skelet.py`; ни путь, ни путь+петля, ни цикл на {1,2,3,4} не дают f_n). НЕ подгонял. Вопрос: какая договорённость в оригинале делает |H_n|=f_n? (счёт шагов вместо клеток? петля на конце? другой алфавит?) Пока H дана как «маска со сдвигом» / со звёздочкой; остальные 7 спиц несут курс.
2. **Дефолты фабрики, взятые молча (все в дневнике):** (а) `chernovik/skelet.md` не создаю — цитирую живой `kotly/matematika.md` (GEJTY: снимок опционален); (б) proof-self-review — ВНУТРЬ+субагент (конфликт B7↔Р17, взял свежесть Р17); (в) карточки-тег над одним графом (не два набора); (г) нарратив — под-стадия 4a без renumber; (д) сшивка цитатой-сноской (не `<details>`); (е) структура слайда — атрибуты на каркасе (вариант 1 арки 5); (ж) G2 «≥5 источников» ослаблен для ОТ-КНИГИ.
3. **Формат карточки** — тройная нестыковка STANDART↔GEJTY↔трекер (см. дневник Часть 2/3): нужен один источник истины. Не правил фабрику (граница §2), только доложил.

*(остальное — по ходу; полный разбор в `run-log.md`)*

## ОТЧЁТ — (заполняет исполнитель)

*(заполняется в конце прогона)*
