# Канал исполнителя — Лекция 1 «Числа Фибоначчи»: перевёрстка по стандарту «двузонный canvas» (один заход)

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу этого файла. Метрика — КАЧЕСТВО (вкус владельца), часы — норма.

**Модель:** `opusplan` — дизайн переиспользуемого canvas-движка и эталонной сцены рождается ПО ХОДУ (Opus); механическое размножение по остальным слайдам — Sonnet.

**Текст запуска (владелец → Claude Code):**
```
Модель: opusplan — дизайн движка/эталона на Opus, механический HTML на Sonnet.
Claude Code, твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/_studio/zhurnal/2026-07-11_fibonacci-kurs/kod_lekcia1-pereverstka.md. Прочитай ТОЛЬКО его и то, что он называет; проект не изучай. Точка отката: git-коммит своей зоны сразу. План/вопросы/отчёт — в этот же файл вниз (## ПЛАН/ВОПРОСЫ/ОТЧЁТ). Ничего сверх задачи не трогай.
```

КОНТЕКСТ. Проект — курс «Числа Фибоначчи» (профиль ОТ-КНИГИ), папка `fibonacci/`. Прошлый этап: ночной прогон #14 собрал СРЕЗ-хайлайт 12 слайдов через всю дугу (`fibonacci/src/` → `dist/index.html`); математика верна и сверена (`fibonacci/verify_skelet.py` зелёный, гейты G1–G6/G8–G11 PASS). Владелец ПРИНЯЛ математику, но забраковал ФОРМАТ дека: много эссе-текста, декоративные иллюстрации к каждой фразе, односторонний интерактив, непоследовательная подача. **ЦЕЛЬ:** перевёрстка в ПЕРВУЮ ЛЕКЦИЮ (задачи 1–5 листка по порядку) по стандарту «двузонный canvas» (§3) — НЕ переделывая с нуля, а доводя существующее до идеала.

⚠ **Стоп до цели (громко):** ты соберёшь ЛЕКЦИЮ 1 (задачи 1–5), НЕ весь курс. Лекции 2–3 (суммы-продолжение, Кассини, Каталан, Люка, хвост Цекендорфа) — НЕ в этом заходе. Их слайды-задел НЕ удалять — вывести из `slide_order` Л1 и оставить на потом.

Приёмка — по ОТЧЁТУ + линтер `build_deck.py` + глаз владельца (вкус, не автоматизируется).

## 0. ПЕРВЫЙ ХОД
Рабочая папка — `fibonacci/` (корень репо `/Users/ivanyakovlev/Documents/GitHub/materials`).
1. **Точка отката:** `git add fibonacci _studio/zhurnal/2026-07-11_fibonacci-kurs/kod_lekcia1-pereverstka.md && git commit -m "Л1 перевёрстка: точка отката"`. НЕ `git add -A` (в одном репо чужие проекты — инцидент 2026-07-11).
2. **Подними канон-скиллы:** `html-slides-studio` (канон дека: 4-блочный файл-контракт, сцены, fill, экспорт), `design-mode` (визуальная фаза: анти-slop + illustration-aware архитектура «источник→генератор→самодостаточный HTML» ради ДЕШЁВЫХ правок), `microinteractions` (контракт виджета: триггер/правило/обратная связь/состояния), `karpathy-guidelines` (дисциплина).
3. **Прочитай ТОЛЬКО:** этот заход; `fibonacci/src/sims/lab.core.js` (существующий движок — это БАЗА, расширяем, не выбрасываем); `fibonacci/src/shablon.html` + `tokens.css` (сетка/токены; цвета `--cgreen`/`--mustard` уже есть); `fibonacci/src/content/*.md` + `slides/*.html` (текущие 12 слайдов); `fibonacci/kotly/matematika.md` (ИСТОЧНИК ИСТИНЫ по условиям/доказательствам задач 1–5 — уже исправлена и сверена); `fibonacci/raskadrovka/PLAN.md`; `_studio/konvejer/09-illustracii/DOK.md` (контракт canvas-иллюстраций) + `ls ../buffon/src/sims/` (образец canvas-движка); **референс визуального языка владельца** — `fibonacci/istochniki/pdf/Комбинаторные тождества.pdf`, стр. 8–9 (свёртка 2×n→1×n; зоны фоном; код под клетками).
4. **ПЛАН** — в `## ПЛАН` перед действиями. Раздели: Фаза A (эталон + переиспользуемый движок), Фаза B (размножение по слайдам Л1).

## 1. ДИСЦИПЛИНА (Карпатов)
Предпосылки/развилки — назвать в `## ВОПРОСЫ`, не выбирать молча. **Минимум:** расширяй `lab.core.js`, НЕ переписывай рабочее ради красоты. **Хирургия:** каждая правка трассируется к пункту §3; чужой стиль/имена сохраняй. **Критерий, который может провалиться** — см. §3. Ложную предпосылку оспорь по живым файлам. **Субагенты** (аккуратный режим): ≤5 параллельно, механическое размножение слайдов — Sonnet; рейт-лимит = отступить + доложить; субагенты НЕ коммитят.

## 2. ГРАНИЦЫ + ЗОНА
**ЗОНА (можно менять):** `fibonacci/**` + ЭТОТ файл-заход (секции `## ПЛАН/ВОПРОСЫ/ОТЧЁТ`). Всё вне — **READ-ONLY:** фабрика (`_studio/**`, `_generator/**`), другие проекты в репо. `fibonacci/kotly/matematika.md` и `verify_skelet.py` — НЕ трогать (уже верны/сверены), из них БЕРЁШЬ условия/доказательства. Задел (слайды лекций 2–3) НЕ удалять. **git:** коммить ТОЛЬКО зону (`git add fibonacci <файл-заход>`), НИКОГДА `-A`.

## 3. ЗАДАЧА — Лекция 1 (задачи 1–5) по стандарту «двузонный canvas»

**Формат-контракт (жёстко):** каждая задача = **УСЛОВИЕ** (короткий текст-формулировка из листка/`matematika.md`) → **ДОКАЗАТЕЛЬСТВО** (иллюстрация/интерактив, почти без слов). Текст — минимум; никаких эссе-подводок и навязанного «стиля». Иллюстрация — ТОЛЬКО к доказательству, не декоративная к каждой фразе. Порядок сцен всегда: сперва ЧТО (вопрос / какую биекцию строим) → потом ответ/построение.

**Стандарт «двузонный canvas»** (эталон — подмножество↔полоска; выведен из референса владельца, стр. 8–9):
1. **Две зоны разделены ФОНОМ, без подписей.** Светлое поле сверху — короткое условие/комментарий «что делаем» (буквы `A_n`/`B_n` — только при формулировке задачи из листка). Цветная полоса снизу — сам canvas, без слов.
2. **Соответствие — ВЫРАВНИВАНИЕМ:** образ строго под своей клеткой (код-цифра под своей плиткой, как на стр. 9 референса) + подсветка пары там, где естественно (число снизу ↔ разрез/склейка сверху). Не насильно везде.
3. **Обе стороны РЕДАКТИРУЕМЫ**, где осмысленно. Сейчас `lab.core.js` правит только верх-полоску (клик по плитке), низ read-only — **достроить редактируемый низ**.
4. **Ограничение — во ВВОДЕ, не в тексте:** подмножество — соседние кнопки/лунки гаснут (нельзя два подряд); строки 0/1 — после «0» доступна только «1» (нет «00»). Правило показывает интерактив.
5. **Один canvas — весь путь объекта** (определение → примеры → рекуррента → биекция); меняется только что показано.
6. **Клик по полоске оставляем** (владелец подтвердил): клик по плитке — доминошка распадается на два квадрата / два квадрата сливаются; достроить, чтобы добираться до ЛЮБОГО замощения.

**Ритм каждого объекта (B, C, D, E, F, H) — больше сцен, плавно:** (1) определение сверху; (2) примеры при малых n, руками через генератор объекта; (3) **рекуррента как canvas** (верх фикс. длины n ↔ низ: «квадрат + объект n−1» / «доминошка + объект n−2»); (4) биекция X↔A, ВЫВЕДЕННАЯ из рекурренты. **Рекуррента и суммы — ТОТ ЖЕ формат canvas** (одна сторона фикс. длины, другая пробегает), не исключение.

**Дешёвый переиспользуемый метод (ГЛАВНАЯ архитектурная директива владельца):** НЕ bespoke-код на каждый слайд. Выдели МАЛЫЙ набор переиспользуемых примитивов (в `lab.core.js`/рядом) и внедри на ВСЕ канвасы: (а) двузонный layout (фон-зоны + разделитель); (б) рендер полоски с выравниванием образа под клетки; (в) редактируемый ввод-с-ограничением (полоска-клик; кнопки 0/1; выбор-без-соседних); (г) живая двусторонняя связка. Каждый слайд = КОНФИГ над примитивами, не новый код. Это критерий масштабируемости.

**Скоуп слайдов Л1 (задачи 1–5, ≈по порядку):**
- **Задача 1 (2×10):** условие → свёртка 2×n→1×n. ⚠ Текущая картинка `s02` неверна: плитки одного размера (сделать квадрат 1×1 vs доминошка 1×2); переход нарисован несогласованно (слева вертикаль/горизонт, справа наоборот) → консистентно: вертикальная доминошка↔квадрат, пара горизонтальных↔доминошка; цвет — зелёный квадрат / горчичная доминошка.
- **Задача 2 (ядро):** 2а — первые значения f₀..f₄ (по сценам, с замощениями); 2б — рекуррента (canvas; вопрос «чем закрыта первая клетка?» → ответ на след. сцене; перенос строки после вопроса); 2в — восемь масок → A_n, спицы **A↔C** (код 0/1), **A↔B** (подмножества — ЭТАЛОН стандарта), **A↔F** (перестановки). **H_n** — см. ниже.
- **Задача 3:** условие `f₀+…+fₙ = fₙ₊₂−1` → canvas «по первой доминошке».
- **Задача 4:** условие → доказательство **«по первому КВАДРАТУ»** (в `matematika.md` Утв.10 уже исправлено — бери оттуда, НЕ переизобретай).
- **Задача 5 (Цекендорф):** условие → жадный алгоритм + биекция `Cₙ→{1..fₙ}`.
- **Убрать декоративные иллюстрации `s05`/`s06`** (там задача не формулируется) — переработать в контент задачи 2. Слайды `s10` (суммы-прод.), `s11` (Кассини), `s12` («2×10=89») — **вывести из `slide_order` Л1** (задел, НЕ удалять).

**H_n (решено владельцем):** в определении H_n листка — ошибка ИНДЕКСА (написано `n`, нужно `n±1`), из-за чего выходит f_{n−1}. Исправь ОПРЕДЕЛЕНИЕ так, чтобы `|Hₙ|=fₙ` (подбери длину/индекс); проверь численно на малых n ОТДЕЛЬНЫМ скриптом (`verify_skelet.py` без нужды не правь; правишь — только дополняя). H остаётся полноценной спицей.

**Durable:** сохрани дистиллят стандарта (пункты 1–6 + список примитивов) в `fibonacci/raskadrovka/STANDART-canvas.md` — чтобы был переиспользуем на лекциях 2–3.

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- `python3 _generator/build_deck.py fibonacci/src` — линтер ЗЕЛЁНЫЙ, `dist` собран; `slide_order` = точное покрытие `slides/` Л1 (0 сирот/дублей).
- Каждый слайд Л1 несёт УСЛОВИЕ + доказательство-иллюстрацию; 0 эссе-абзацев; 0 декоративных иллюстраций без доказательства (само-чеклист по §3).
- Двузонный layout (фон-зоны), выравнивание образа под клетки, редактируемый низ, ограничение-во-вводе — реализованы на ЭТАЛОНЕ (подмножество↔полоска) и ПЕРЕИСПОЛЬЗОВАНЫ (один набор примитивов, не копипаст на слайд).
- Картинка 2×n (задача 1) консистентна, плитки разных пропорций.
- H_n: отдельная численная сверка показывает `|Hₙ|=fₙ`.
- Задел (`s10/s11/s12`) на диске, вне `slide_order` Л1.
- Проверено в браузере (`python3 -m http.server` над `dist/` + скриншоты ключевых сцен: эталон-подмножество с редактируемым низом; код 0/1 с ограничением; свёртка 2×n).

## 4. ОТЧЁТ → секция `## ОТЧЁТ` внизу
что сделал + ЗАЧЕМ / как проверил (линтер + браузер, скриншоты) / что НЕ трогал (математика, задел, фабрика) / развилки и дефолты / **время прогона + токены**. Подтверди, что стоп-до-цели (Л1, не весь курс) соблюдён и заявлен.

## ПЛАН — (исполнитель)

**Точка отката:** коммит `c7a1425` «Л1 перевёрстка: точка отката» (ветка `buffon-zahod-01`, см. ВОПРОСЫ-3). Линтер на нём ЗЕЛЁНЫЙ (12 слайдов, 0 предупр.) — базлайн зафиксирован, регрессии будут мои.

### Разведка (прочитано ровно то, что назвал заход)
`lab.core.js` (движок-БАЗА), `shablon.html`+`tokens.css`+`base.css`+`engine.js` (4-блочный контракт, сетка, сцены), `brief.md` (slide_order), все `content/*.md`+`slides/*.html` (12 слайдов), `kotly/matematika.md` (истина по задачам 1–5), `raskadrovka/PLAN.md` (раскадровка), `_generator/build_deck.py` (линтер-контракт), `09-illustracii/DOK.md` (контракт илл.), референс `Комбинаторные тождества.pdf` стр. 8–9 (ДНК стандарта). Канон-скиллы подняты: html-slides-studio, design-mode, microinteractions, karpathy.

### Диагноз (что есть → что нужно)
- **Двузонный layout УЖЕ есть на уровне сетки слайда** (`.copy` = бумага-условие сверху, `.board` = синяя доска снизу — ровно как стр. 8–9 референса). Разрыв — ВНУТРИ canvas.
- **Движок `lab.core.js`**: редактируется только ВЕРХ-полоска (клик: доминошка↔2 квадрата); низ-образ `drawImage` — read-only и НЕ выровнен под клетки (код рисуется центрированной строкой, а не цифра-под-плиткой как на стр. 9). Нет ограничения-во-вводе. Нет режимов «первый квадрат» (з.4) и Цекендорфа (з.5).
- **Скоуп L1 сместился**: старая раскадровка = L1 это s01–s09; ЗАХОД = L1 это **задачи 1–5** → з.3 (сумма, s10), з.4 (чёт/нечёт суммы — НОВЫЙ слайд), з.5 (Цекендорф — НОВЫЙ слайд) втянуты в L1. Кассини (s11) уходит в задел.
- **H_n:** буквальная длина n даёт f_{n−1} (подтвердил перечислением); длина **n+1** даёт ровно f_n (подтвердил). Правлю ОПРЕДЕЛЕНИЕ в контенте деки (не в matematika.md — она read-only и честно флагует), проверяю отдельным скриптом.

### ФАЗА A — Opus (движок + ЭТАЛОН). Ядро; делаю сам.
Расширяю `lab.core.js` (НЕ переписываю), выделяю МАЛЫЙ набор переиспользуемых примитивов (§3 «дешёвый метод»):
- **(a) две зоны в canvas** — полоска-band сверху, образ-band снизу, разделены геометрией/фоном (доска синяя уже даёт фон-разрыв; добавлю мягкий разделитель при нужде).
- **(b) образ ВЫРОВНЕН под клетки** — рефактор `drawImage`: код-цифра строго под своей плиткой (1 под квадратом, 0·1 под доминошкой — стр. 9); подмножество — лунка на границе i под левым краем доминошки; перестановка — дуги между центрами соседних клеток.
- **(c) редактируемый НИЗ (двусторонняя связка)** — клик по образу (лунка подмножества / цифра кода) правит ту же полоску; верх и низ — два входа в одно состояние.
- **(d) ограничение ВО ВВОДЕ + обратная связь** (microinteractions: триггер/правило/фидбек/состояния) — подмножество: соседние лунки к выбранной гаснут (нельзя два подряд); код: «0» тянет за собой «1» (нет «00») структурно.
- **(e) любое замощение достижимо** кликом (проверю перечислением).
- **(f) новые режимы:** `firstsq` (з.4, первый КВАДРАТ, чётность хвоста) по образцу `sum`; Цекендорф (з.5) — отдельный компактный интерактив (жадный разбор N + нумерация C_n→{1..f_n}), решу форму в A.
- **ЭТАЛОН = A↔B (подмножество↔полоска)** собираю ПЕРВЫМ и целиком (§3), на нём отлаживаю примитивы; дальше слайды = КОНФИГ над примитивами (`data-mode/-n/-editimg/...`), не новый код.
- **Durable:** `raskadrovka/STANDART-canvas.md` (пункты 1–6 + список примитивов) для L2–L3.
- **`verify_H.py`** (отдельный скрипт): |Hₙ|=fₙ на малых n.

### ФАЗА B — Sonnet-субагенты (размножение по слайдам-конфигам, ≤5 параллельно, не коммитят)
Механическая перевёрстка задача-за-задачей по формату УСЛОВИЕ→ДОКАЗАТЕЛЬСТВО, ритм 4 сцен (опр.→примеры→рекуррента→биекция). Каждый субагент — 1–2 слайда, трасса к §3, чужой стиль сохраняет.

### Предлагаемый roster L1 (задачи 1–5 по порядку)
| # | id | задача | что |
|---|----|--------|-----|
| 1 | s01-title | — | обложка (минимум) |
| 2 | s02-hook | **З1** | условие 2×n + доска замощений; **ЧИНЮ картинку** (верт.доминошка↔зелёный квадрат, пара гориз.↔горчичная доминошка; разные пропорции) |
| 3 | s03-strip | **З1** | свёртка 2×n→1×n (двузонный стандарт) |
| 4 | s04-recur | **З2а,б** | объект A_n + f₀..f₄ примерами → рекуррента (canvas recur, редакт.+выровнен); дроблю если переполнено |
| 5 | s05-masks | **З2в** | маски→одно лицо→звезда-хаб A_n (свёртка декоративных s05+s06 в ОДИН содержательный; §3) |
| 6 | s08-subset | **З2в** | **A↔B — ЭТАЛОН** (редакт. низ, ограничение-во-вводе) |
| 7 | s07-code | **З2в** | A↔C (0/1, без «00»), цифра-под-клеткой |
| 8 | s09-perm | **З2в** | A↔F (дуги-перестановка) |
| 9 | (сцена/компакт) | **З2в** | H_n исправленный (длина n+1), спица + скрипт-сверка |
| 10 | s10-sum | **З3** | Σfₖ=f_{n+2}−1 «по первой доминошке» (keep, к стандарту) |
| 11 | s11-square (нов.) | **З4** | f₁+f₃+…=f_{2n}−1 и f₀+f₂+…=f_{2n+1} «по первому КВАДРАТУ» (Утв.10) |
| 12 | s12-zeck (нов.) | **З5** | Цекендорф: жадный + биекция C_n→{1..fₙ} |
| — | финал | — | компактный клоузер (см. ВОПРОСЫ-2) |

**Задел (НЕ удалять, вынести из slides/ в `slides-zadel/` + из slide_order + из shablon):** `s11-cassini`, `s12-close`. Механизм вынужденный: линтер (rule 5) требует slide_order = точное покрытие `slides/`; файл в `slides/` вне order = ошибка. Поэтому задел физически переезжает в сестринскую папку (остаётся «на диске»), а не просто исключается из order.

### Верификация (критерий, который может провалиться)
`build_deck.py` зелёный + `audit.py` + браузер (`?only=N&scene=K` скриншоты: эталон-подмножество с редакт. низом; код 0/1 с ограничением; свёртка 2×n) + `verify_H.py` зелёный. Каждый слайд: УСЛОВИЕ+доказательство, 0 эссе, 0 декоративных илл.

## ВОПРОСЫ — (исполнитель; дефолты выбраны, все обратимы)

1. **s10 = задача 3, оставляю в L1** (заход в §3 пометил s10 «суммы-прод.→задел», но содержимое `s10-sum.md` — ровно `f₀+…+fₙ=f_{n+2}−1` = задача 3, которую тот же §3 требует в L1). Развилка живого файла vs пометки; иду по файлу. **Дефолт: keep s10 как З3.**
2. **Финал L1.** Заход велит вынести s12 «2×10=89» в задел. Но это ОТВЕТ на задачу 1 (f₁₀=89) — естественная арка-закладка. **Дефолт:** кульминация — задача 5 (Цекендорф) + компактный клоузер, в который вношу закладку «2×10 = 89 = f₁₀». Если владелец хочет буквально убрать любой «2×10=89» — уберу.
3. **Ветка.** Репозиторий был на `buffon-zahod-01` (остаток чужого захода). Точку отката закоммитил туда (заход дал явную команду без ветвления). Если L1 нужна отдельная ветка — скажите, перенесу.
4. **Глубина H_n в L1.** §3 называет несущими спицами C/B/F; H — «полноценная спица», но лёгкая. **Дефолт:** H как компактная спица/сцена + `verify_H.py`, без полного отдельного тяжёлого слайда (минимум по Карпатову).

## ОТЧЁТ — (заполняется в конце)
