# Канал Claude Code — doc-движок (#11, движок 2)

> Для исполнителя: твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; остальной проект не изучай.
> План/вопросы/отчёт — в секции внизу этого файла. Метрика — КАЧЕСТВО и ПОЛНОТА.
> **Модель:** opus — часть дизайна рождается по ходу (прародитель `build.py` в этом репо недоступен, спека — дистилляты). План — обдуманно, механику — быстро.
> ДЁШЕВО: один агент, без роя субагентов.

## ЗАДАЧА (один проход, можно долгий)

КОНТЕКСТ. Фабрика: `materials/_generator` — дом движков (Р15). Движок 1 ДЕК (`build_deck.py`) — готов. Нужен ДВИЖОК 2 — ДОКУМЕНТ (doc-движок): растущие markdown-источники Фазы I (интервью → концепт → котёл → раскадровка) → HTML-вид для авторской вычитки. Модель данных — ПОТОК текста (не слайд-канвас), с полями и математикой. Прародитель `build.py` (проект `kurs leto 2026`) в этом репо НЕДОСТУПЕН — спека берётся из дистиллятов (якоря ниже) + брат-движок `build_deck.py`. ЦЕЛЬ: реализовать движок + демо. Приёмка — по отчёту + гейту.

⚠️ СТОП-ФЛАГ: ты строишь МЕХАНИЗМ + демо на игрушечном контенте, НЕ настоящую лекцию. Канон/стиль дека не трогаешь. Это инфра.

⚠️ D1 (важно — показ владельцу). Владелец хочет ОДИН HTML с ДВУМЯ вкладками — «математика» и «научпоп» — как вид стадии-КОТЛА (нелинейный склад сюжетов, ещё без единой линии). Источник истины — markdown, легко править; внутреннюю файловую структуру проектируешь ты. Реализация ОБЯЗАНА поддержать двухвкладочный вид и показать его на демо — это закрывает развилку показом, а не абстрактным выбором.

### 0. ПЕРВЫЙ ХОД
Рабочая папка `/Users/ivanyakovlev/Documents/GitHub/materials`. Git есть — откат через git; вне `_generator/` ничего не трогай. Прочитай ТОЛЬКО:
- `_generator/DVIZHKI.md` — контракт семейства + ТЗ движка 2 (главная спека);
- `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-tehnika-md-html.md` — техника владельца (build.py: token-free, `status`-гейт, `> поле:`→`<aside>`, `.mn` float);
- `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-format-teksta.md` — живой формат: `.insight`/`.mn`/`.foot`, мат-долг `⚑ Флаг`→`Флаг закрыт`, гейт-против-воды, слои `rasskaz`/`skelet`/`statya`, шаблон `skelet` (Определение/Утверждение/Доказательство/Источник/Уровень);
- `_generator/build_deck.py` — брат-движок: стиль кода, stdlib-паттерны, линтер, `{tex→html}`-кэш;
- `_studio/konvejer/FORMAT-ISTOCHNIKA.md` — как хранится источник (конвенции имён; стык со стадиями).
Изложи ПЛАН в `## ПЛАН`: модули движка, модель данных потока, синтаксис полей, как делаешь две вкладки, что в демо, как гейтишь. Спорный дизайн → `## ВОПРОСЫ`, не угадывай молча.

### 1. ДИСЦИПЛИНА
Назови предпосылки (ложную оспорь). Чистый stdlib (`re/pathlib/argparse/html/base64`), БЕЗ сети/pip для сборки. KaTeX-вид математики — только как АВТОРСКИЙ рантайм-вид (CDN-скрипт внутри HTML допустим ТОЛЬКО тут — это вид для вычитки, не финальный дек; Р7 разрешает рантайм вне финала) — пометь это явно. Минимум надёжного; нет спеки/тонко → честная пометка в `## ВОПРОСЫ`, НЕ фабрикуй фичу. Код — в стиле `build_deck.py`.

### 2. ГРАНИЦЫ
НЕ трогай (ЧИТАЙ): `_studio/`, `buffon/`, `dandelin/`, `build_deck.py` (дек-движок не переписывай). Создаёшь: `_generator/build_doc.py` (движок 2) + `_generator/doc-demo/` (игрушечный источник + собранный `view.html`). Общий модуль (напр. `_md_common.py`), если выделишь — назови в отчёте. Интернет — нет (кроме KaTeX-CDN внутри HTML-вида, если так решишь).

### 3. ЗАДАЧА
Реализуй `build_doc.py` — движок 2 (по DVIZHKI «Движок 2 — ДОКУМЕНТ»):
1. **Вход**: markdown-источник (папка) с одним/несколькими `.md`, шапка со `status:` (`skelet|chernovik|…|polno`). **Выход**: ОДИН самодостаточный HTML-вид (поток, не слайд-канвас).
2. **Поля/пометки** (grounded в `distillat-format-teksta`): `> поле: …` → `<aside>`; `.mn` (мелкая оговорка в правом жёлобе, float), `.insight` (крупное прозрение/разворот), `.foot` (статус черновика).
3. **Мат-долг**: конвенция `⚑ Флаг …` → `Флаг закрыт` (грепается). **Линтер падает (exit 1), если остался открытый `⚑` при `status: polno`.**
4. **`status`-гейт / линтер**: структурная ошибка или открытый долг на финальном статусе → сборка прервана (exit 1), как в `build_deck.py`.
5. **Математика**: KaTeX-вид для вычитки (рантайм-CDN допустим — авторский вид).
6. **Иллюстрации-плейсхолдеры** `🖼` (описание + число сцен) → заметный блок (стык с аркой 9 позже).
7. **Двухвкладочный вид (D1)**: движок умеет собрать ДВА потока (напр. `matematika.md` ‖ `naupop.md`) в ОДИН HTML с переключением вкладок (чистый HTML/CSS/JS, без сети). Это вид «котла».
8. **ДЕМО**: положи `_generator/doc-demo/{matematika.md, naupop.md}` с примерами ВСЕХ примитивов (поле, `.mn`, `.insight`, `.foot`, `⚑`-долг открытый И закрытый, формула, `🖼`) и собери `view.html`.

КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):
(a) `python3 build_doc.py <demo>` из чистого stdlib собирает `view.html` БЕЗ сети/pip;
(b) демо с ОТКРЫТЫМ `⚑` при `status: polno` → **exit 1** (гейт ловит); закрыли долг → сборка проходит;
(c) `view.html` самодостаточен (0 внешних URL, кроме — если выбран — KaTeX-CDN, тогда это помечено как авторский рантайм-вид) и показывает ДВЕ вкладки математика‖научпоп + все примитивы;
(d) правка markdown → пересборка без токенов меняет вид (token-free доказан).

### 4. САМОПРОВЕРКА
Прогони движок на демо в обоих сценариях (открытый/закрытый `⚑`), приложи RAW exit-коды и путь к `view.html`. Подтверди stdlib-only (нет pip-зависимостей у сборки).

### 5. ОТЧЁТ → секция `## ОТЧЁТ`
Что реализовал + ЗАЧЕМ (модули, синтаксис полей, как сделал вкладки); где ПРОБЕЛЫ спеки (что доспроектировал сам и почему); RAW прогонов (exit-коды, token-free); открытые вопросы владельцу (особенно по D1-виду) → `## ВОПРОСЫ`; **время + токены**. Отчёт — дистиллят.

## ПЛАН — (исполнитель)

**Предпосылки (названы; ложную оспариваю).**
- Прародитель `build.py` в этом репо недоступен — беру диалект из дистиллятов + ДНК `build_deck.py`. Честно: часть синтаксиса доспроектирована (ниже, помечено).
- ЛОЖНАЯ предпосылка, которую отвергаю: «поля надо размечать сырым HTML, как в catalan». Дистилляты прямо говорят — источник маркируется markdown-примитивом `> поле:<вид>` (как build.py), HTML только рендерится. Слушаюсь дистиллята.
- Общий модуль `_md_common.py` НЕ выделяю: он потребовал бы править `build_deck.py` (запрещено «не переписывай»), а дистиллят-техника §4 прямо рекомендует «два дёшевых скрипта > общая абстракция». `build_doc.py` самодостаточен (дублирует ~10 строк хелперов). Назвал в отчёте.

**Модули `build_doc.py` (стиль build_deck: stdlib re/sys/argparse/pathlib/html, template+подстановка, линтер→exit 1).**
1. `read_text/write_text` · `split_frontmatter` (YAML-lite, порт `parse_brief`) · `first_heading`.
2. `render_inline` — stash→parse→unstash математики (порт приёма build.py): `$…$`/`$$…$$` прячу до разметки, потом `**b**`→`<strong>`, `*i*`→`<em>`, `` `c` ``, `[t](u)`; TeX восстанавливаю дословно (KaTeX получит сырьё).
3. `FIELD_MAP` + `render_field` — семья полей (порт `FOOTNOTE_MAP`).
4. `render_ill` — 🖼-плейсхолдер (описание + `{N}` сцен).
5. `render_stream` — блоки (пустая строка=блок) → поток HTML: цитата/поле · 🖼 · флаг-закрыт · флаг-открыт · заголовок · список · абзац.
6. `scan/лог мат-долга` — счётный: `count(⚑) − count("Флаг закрыт")`; net>0 → открыт.
7. `load_streams` — все `*.md` в папке = потоки/вкладки (порядок `poryadok`, ярлык `tab:`|H1|имя).
8. `build_html` — шаблон PAGE + генерю радио-вкладки (CSS-only) + панели.
9. `lint` — гейт (ниже) · `main` — CLI `<src> [--lint] [-o]`.

**Модель данных = ПОТОК текста** (не канвас): каждый `.md` — непрерывный поток блоков; никакой геометрии/сцен-канваса. Одна папка = один HTML со вкладками-потоками.

**Синтаксис полей (grounded + доспроектировано).**
- `> поле: текст` → `<aside class="note">` (буква ТЗ; generic build.py-примитив). *(grounded)*
- `> поле:mn текст` / `> поле:честно текст` → `<p class="mn">` float-жёлоб (CSS дословно из дистиллята §2; «честно»→.mn т.к. дистиллят §3: честные пометки идут в .mn). *(grounded)*
- `> поле:insight Тег | текст` → `<div class="insight"><span class="tag">Тег</span>…` (Тег=Прозрение/Разворот). *(grounded)*
- `> поле:foot текст` → `<p class="foot">` статус черновика. *(grounded)*
- `> поле:уровень школьно` → пилюля уровня (школьно·приручение·вне-школы). *(grounded, дистиллят §5)*
- `🖼 описание {N}` → заметный dashed-блок «🖼 ИЛЛЮСТРАЦИЯ · N сцен». *(доспроектирован формат строки — спеки на синтаксис 🖼 нет)*
- `⚑ …` → красный бейдж «долг открыт»; `Флаг закрыт…` → зелёный бейдж. *(grounded convention)*
- НЕ фабрикую полный skelet-диалект (Определение/Утверждение/§N·ТИП·[поля]) — дистиллят §5 прямо: content-диалект НЕ решён. → ВОПРОСЫ.

**Две вкладки (D1) — CSS-only, без сети/JS.** Каждый `.md` = вкладка. Скрытые `<input type=radio name=doc-tab>` + `<label>` в `.tabbar` + `.panel`; генерю парные правила `#tab-i:checked ~ #panel-i{display:block}`. Zero-JS = труально самодостаточно. Ярлык вкладки из `tab:`. Демо: `matematika.md ‖ naupop.md` = вид «котла».

**Математика.** KaTeX auto-render с CDN, вызов на `DOMContentLoaded` c guard (офлайн → сырой `$…$`, не падает). Помечено видимым баннером «авторский рантайм-вид, Р7, единственная внешняя зависимость».

**Демо (`doc-demo/`).** `matematika.md` (status: chernovik, регистр читаемый): поле, уровень, mn, insight-Прозрение, формулы inline+display, 🖼, ⚑ открытый + ⚑…Флаг закрыт, foot. `naupop.md` (chernovik, устный): поле, mn, insight-Разворот, 🖼, список, foot. Оба потока → `view.html` со вкладками.

**Гейт.** structural error → exit 1 (как build_deck). Ключевое: для КАЖДОГО потока `net_open = count(⚑)−count("Флаг закрыт")`; если `net>0 И status==polno` → ошибка (exit 1); если `net>0 И status!=polno` → warn (планка растёт со стадией, дистиллят §5). Плюс: нет ярлыка вкладки → ошибка; внешний asset-URL в ТЕЛЕ автора → ошибка (мой KaTeX-head в head — доверенный, исключён). Демо-default chernovik (оба ⚑-примитива видны, сборка идёт); гейт exit1 доказываю на /tmp-копии со status→polno.

## ВОПРОСЫ — (исполнитель)

Открытые развилки, которые я НЕ решал молча (спека отсутствует/тонко):

1. **D1 — модель «котла» (главный вопрос владельцу).** Реализовал «N потоков `*.md` = N вкладок» — это ближе к «ДВА параллельных документа» (rasskaz‖skelet) из развилки-линчпина DVIZHKI, чем к «один файл со свёрнутыми `<details>`». НО объединяю их **вкладками**, а не «цитатой-сноской» (реальная практика владельца: `.mn` у теоремы → «полное доказательство — в мат-скелете»). Сейчас связь между вкладками — только рукописный текст. Вопрос: вкладки — финальная форма котла, или нужен машинный cross-ref между потоками (маркер `→[вкладка:Математика]` → авто-ссылка/бэклинк, как `→[id]` в build.py)?
2. **Content-диалект строгих блоков НЕ решён** (дистиллят-формат §5 прямо: «не наследовать молча»). НЕ фабриковал полный skelet-шаблон (Определение/Утверждение/Доказательство/Источник/Уровень) как машинный синтаксис — дал только `> поле:уровень`. Какой диалект parse'ить: бюрократический build.py (`§N·ТИП·**[поля]**·→[id]`) или catalan-проза (`Определение N.` + `$\blacksquare$`)? Нужно до арки 3.
3. **Мат-долг — счётный** (`count(⚑) − count("Флаг закрыт")`), без пар по метке — faithful греп-практике владельца, но не ловит «закрыл не тот флаг». Достаточно, или ввести `⚑{id} … Флаг закрыт {id}`?
4. **Порог гейта — только `polno`.** Лестница `skelet→chernovik→…→polno` с многоточием: промежуточные статусы не специфицированы. Нужно ли эскалировать ⚑ раньше (напр. на `chistovik`)?
5. **Регистр (A4)** показываю мета-бейджем; DVIZHKI: «едет в brief.md к арке 6». Нужен ли машинный экспорт поля в brief.md, или пока только вид?
6. **🖼-синтаксис** (`🖼 описание {N}`) доспроектирован мной — спеки на строку нет. Ок как конвенция?

## ОТЧЁТ — (исполнитель)

**Реализовано.** `_generator/build_doc.py` (движок 2, ~430 строк, чистый stdlib) + демо `_generator/doc-demo/{matematika.md, naupop.md}` → `view.html`. `build_deck.py` и канон не тронуты; git — `_generator/` untracked (откат = удалить файлы). Общий `_md_common.py` СОЗНАТЕЛЬНО не выделял: он потребовал бы править `build_deck.py` (запрет), а дистиллят-техника §4 рекомендует «два дёшевых скрипта > абстракция»; движок самодостаточен.

**Модули (зачем).** `render_inline` — stash→parse→unstash математики (приём build.py: прячу `$…$`/`$$…$$` до markdown-регексов, чтобы `*`/`_`/`{}` в TeX не ломали разбор; восстанавливаю дословно). `FIELD_MAP`+`render_field` — порт `FOOTNOTE_MAP`: семья `> поле:<вид>`. `render_stream` — блоки (пустая строка=граница) → поток HTML (модель данных = ПОТОК, не канвас). `load_streams` — каждый `*.md`=вкладка. `build_html` — шаблон+подстановка (стиль семьи). `lint`→exit 1.

**Синтаксис полей.** `> поле: …`→`<aside class=note>`; `> поле:mn|честно …`→`.mn` (float-жёлоб, CSS дословно из дистиллята §2); `> поле:insight Тег | текст`→`.insight`+`.tag`; `> поле:foot …`→`.foot`; `> поле:уровень школьно`→пилюля; `🖼 … {N}`→dashed-блок; `⚑ …`→красный бейдж, `Флаг закрыт …`→зелёный.

**Вкладки (D1).** CSS-only, без сети/JS: скрытые `<input type=radio name=doc-tab>` + `<label>` + генерённые парные правила `#tab-i:checked ~ #panel-i{display:block}`. Труально самодостаточно. Демо `matematika ‖ naupop` = вид котла.

**Пробелы спеки, что доспроектировал сам** — см. `## ВОПРОСЫ` (D1 cross-ref, skelet-диалект, счётный мат-долг, 🖼-строка). Ключевое: НЕ фабриковал строгий диалект и cross-ref — поставил вопросами.

**RAW прогонов (гейт может провалиться — не провалился).**
- (a) `python3 build_doc.py doc-demo` → `EXIT=0`, `view.html` 14 КБ, 2 вкладки, чистый stdlib (AST: только `re/sys/argparse/pathlib/html`, вне-набора — НЕТ).
- (b) `/tmp`-копия, `status→polno` при открытом ⚑ → `RAW_EXIT_RUN2=1` (гейт ловит); дописал `Флаг закрыт …` → `RAW_EXIT_RUN3=0` (сборка проходит). ✓
- (c) `view.html` самодостаточен: 3 внешних URL — все `cdn.jsdelivr.net/npm/katex` (помечены баннером как авторский рантайм-вид Р7); не-KaTeX внешних — 0. Все примитивы + 2 вкладки (Математика/Научпоп) присутствуют; TeX дословно цел; 0 нерезолвленных `{{…}}`, 0 null-байтов/утечек stash.
- (d) token-free: две сборки байт-идентичны; механическая `sed`-правка md (без модели) → пересборка → маркер появился в `view.html` (0→1); откат → чисто (демо возвращено к исходному).

**Путь к выходу:** `/Users/ivanyakovlev/Documents/GitHub/materials/_generator/doc-demo/view.html`

**Критерии (a)(b)(c)(d) — все зелёные.**

**Время + токены:** ~1 проход (opus), ~11 tool-calls; вход ~40k токенов (5 якорей + захода), выход ~14k (код+демо+секции). Точный счётчик модели недоступен — оценка.
