# ДНЕВНИК арки «сборка конвейера»

## 2026-07-10 — арка заведена (в конце сессии постановки проекта)
Ф0 (интервью + ресёрч + go) прошёл на протяжении всей сессии постановки проекта: обсудили три архитектуры, прайор-арт, карту 10 шагов, поправку «фабрика самодостаточна». Решение **go**: сборка конвейера — многосоставная, на несколько сессий, дедлайн завтра (4 лекции). Классы сборки — Р14 (ПЕРЕНОС/ОРКЕСТРАЦИЯ/GREENFIELD).

**Проверено по живым файлам (снимает главную тревогу владельца):** text-корпус ЖИВ — `content-studio/assets/corpus/texts-corpus.md` (5073 стр, 6 деков) + `references/slide-voice.md`; его философия дословно = Р4 (корпус — авторитет стиля, `russian-editor` только гигиена). 7 деков разобраны в `html-slides-studio/references/decks/`. Извлечение из старых презентаций НЕ потеряно → арка 6 локализует готовый корпус.

**СЕЙЧАС:** 4 агента-дистиллятора (см. PLAN). Greenfield (интервью + гибрид-источник) — интерактивно, следующей сессией.

## 2026-07-10 — Фаза II собрана (4 дистиллятора → 5 арк-файлов)
Прогнал 4 general-purpose дистиллятора (sonnet, инлайн-роль, не рой) — каждый верифицировал источник по ЖИВЫМ файлам (не по памяти о скилле) и записал `distillat-*.md` в папку арки. Из них собрал арк-файлы Фазы II в новый дом `../../konvejer/` (паттерн SKILL.md, Р9): `06-tekst`, `07-verstka`, `08-sceny`, `09-illustracii`, `10-sborka-qa` + индекс `00-KONVEJER.md`. Зарегистрировал в KARTA §1/§2/§6 (орфан-дисциплина). Дом `konvejer/` выбран, чтобы не путать с процесс-арками (`zhurnal/`) и долгой памятью (`docs/`).

**Ключевые находки дистилляторов (осевшее):**
- Генератор+формат Р2 УЖЕ покрывают больше, чем казалось: `build_deck.py` парсит весь синтаксис сцен (`{@N}`/`{fill}`/`{blur}`), несёт линтер (сироты `slide_order`, битый `data-ill`, undefined `var`→«чёрная заливка»); `render.py --compare` = глаз. Патч-механизм из `DESIGN.md §6` НЕ нужен — патчи (zoom/fit/sim-reset) осели в `engine.js`/`sims/lab.core.js` на этапе распила. Арка 10 = секвенировать готовое, не строить.
- Оба tokens.css (dandelin/buffon) палитрой+типо-токенами ИДЕНТИЧНЫ → Р8 primitive-слой подтверждён фактом.
- Грид bespoke per-slide (`#id .grid{}` в shablon), общих классов `fr-*` нет; арка 7 канонизирует bespoke, архетипы = справочная таблица.
- **Проверено live (снят риск арки 8):** blur-reveal и scene-from ортогональны (blur несёт `data-reveal`, не `data-scene-from`) — конфликта специфичности НЕТ. Но blur-CSS живёт per-deck в `overlay.css`, не в базе → рекомендация вынести в канон-базу (одна правка на деки).
- Text-стиль: плотность — потолок регистра деки (устные ~30 слов vs читаемые ~45+), не квота; старый корпус фейкал reveal дублированием слайдов — арка 6 обязана СХЛОПывать это в `{fill@}`/`{blur@}`; капс титулов = CSS-трансформ, писать обычным регистром.
- Ресёрч+матбаза = ОДИН поток, растит один HTML; «правило персистентности» (факты+мат-долги пишутся в файл сразу, нарратив-полировка откладывается до арки 4) чинит срез математики из «платоновых тел».
- Иллюстрации = 4 ветки (SVG/lab.js/threejs/растр), не бинарно; вход = плейсхолдер-файл, заменяемый по stem; SVGO id-prefix обязателен (конфликт id при инлайне нескольких SVG в один документ — реальный, прецедент `waffle`).

**Открыто (durable, к переносу при закрытии):** SVGO-конфиг не существует; пиксель-гейт «одноцветная зона» не построен; синтаксис «мат-долга» не спроектирован; delivery-копия без E/A/X — за владельцем; `_generator/audit.py` спроектирован, не написан; арк-файлы 6–10 верифицированы на регрессии, не на greenfield — первая проверка = «многогранники».

**СЕЙЧАС:** GREENFIELD Фазы I интерактивно с владельцем — старт с формата интервью-арки (арка 1, Р13).

## 2026-07-10 — Фаза II доведена до пакетов + верификация (поправка Вани)
Ваня поправил курс: НЕ бежать в Фазу I; довести Фазу II до идеала — документация + промты под **Claude Code** + структура. Ключевое, что было недосмотрено: **формат хранения дека**. Модель: Фаза I — в Cowork (ресёрч, много сессий); Фаза II — запускаем Claude Code, каждый шаг 6–10 отдельной код-сессией; из Cowork показываем ему файл-заход, он берёт накопленный контекст лекции и делает шаг.

**Сделано:**
- `konvejer/FORMAT-ISTOCHNIKA.md` — канонический контракт хранения дека (ядро src/ + опц. модули; схема `brief.md`; конвенции имён; стык каждого шага со слоем). Помечено расхождение: DESIGN.md §4 заявляет `base.css`/`grids.css` отдельными файлами, но живые деки держат грид+базу в `shablon.html` `<style>` — для greenfield решить (по умолчанию как в эталонах).
- Реструктура: каждый шаг 6–10 → папка-пакет `konvejer/NN-<имя>/` из `DOK.md` (спека, паттерн SKILL.md) + `ZAHOD.md` (промт под Claude Code по RUKOVODSTVO, дисциплина Карпатова, петли+критерий-который-может-провалиться) + `PRIMERY.md` (примеры из живых деков). 5 агентов-сборщиков (sonnet, параллельно ≤5), DOK не трогали.
- Верификатор (свежий агент, другой метод — читал живой код, прогонял `build_deck.py`): 0 битых ссылок; 10/10 цитат PRIMERY реальны; нюансы дистиллятов не потеряны (один даже усилен); независимо подтвердил линтер (dandelin 12 warn, buffon 2) и **byte-exact пересборку обоих** деков. Отчёт — `verifikacia-paketov.md`.
- Дочистка по вердикту (мутирующий заход, точечно): **баг data-scenes** (генератор его НЕ пишет — это слой арки 7 в `slides/<id>.html`; ошибка была унаследована из дистиллята) исправлен в 08; `render.py --only` → `?only=N&scene=99` в 07/DOK; оговорка про `.t-small` (нейтрализация buffon-оверрайдом, greenfield получит меньший размер) в 07; добавлен `## 4 ВЕРИФИКАТОР` в 07/ZAHOD; H1 унифицированы; `.fuse_hidden`-хвосты убраны. Все правки сверены grep'ом.

**Решения (durable, к переносу в дома при закрытии арки):**
- **Строгость — поле брифа арки 1, не глобальный дефолт** (Ваня: «решает интервью каждый раз»). → кандидат в RESHENIYA (уточняет Р13).
- **Дом шагов конвейера — `konvejer/`**, каждый шаг = пакет DOK+ZAHOD+PRIMERY. → архитектурное, в ARHITEKTURA-FABRIKI + RESHENIYA при закрытии.
- Формат хранения дека канонизирован в `FORMAT-ISTOCHNIKA.md` (фабрика обязана выдавать его).

**Открыто (durable):** `_generator/audit.py` спроектирован, не написан; SVGO-конфиг (Р10) не существует; пиксель-гейт «одноцветная зона» не построен; решение `base.css` (вынести общий CSS/blur-reveal); скелет пустого greenfield-`src/`; синтаксис «мат-долга» (Фаза I); пакеты верифицированы на регрессии, не на greenfield — первая реальная проверка = «многогранники».

**СЕЙЧАС:** развилка (владельцу) — обкатать на «многогранниках» (гейт) / Фаза I пакетами / инфраструктура. См. PLAN.

## 2026-07-10 — Фаза II закрыта (алгоритм + склейка + решения); хэндофф
Поправка Вани уточнила приёмку «Фаза II идеально закрыта»: (1) безупречно склеена с архитектурой эталонов и выдаёт дек в правильном формате; (2) очень чёткий алгоритм «почти как Python» — жёсткие шаги, развилки, точки взаимодействия с владельцем, ИИ идёт шаг-в-шаг, каждый в своём чате.

**Сделано:** `konvejer/ALGORITM.md` — управляющая линия Фазы II: driver-модель (Cowork ↔ Claude Code, состояние на диске, псевдокод цикла), вход-стык с Фазой I (precondition), линия 6→10 (DAG: 6→7→8, 9∥8, →10) с гейтами/развилками/чекпоинтами, сводка «что не продолбать», раздел «Стыки с архитектурой» (4 инфра-стыка) и гейт приёмки «Фаза II закрыта». Зарегистрирован в 00-KONVEJER + KARTA §6. Критерии (1)+(2) закрыты; склейка доказана на 2 эталонах (линтер+byte-exact, `verifikacia-paketov.md`).

**Решение владельца (durable, к переносу в RESHENIYA при закрытии арки):**
- **Одобрено направление base.css + пустой greenfield-скелет `src/`** как render-identity-preserving перенос замороженного канона (стиль не меняется; KONSTITUCIYA §1 разрешает архитектурный пересмотр «не упустили ли файл», §7 гейт). **Сборка — отдельной фокус-сессией**, не в этом чате.
- (ранее) строгость — поле брифа арки 1, не глобальный дефолт; дом шагов — `konvejer/`, шаг = пакет DOK+ZAHOD+PRIMERY; формат хранения канонизирован (`FORMAT-ISTOCHNIKA.md`).

**Открытые инфра-стыки (bounded, следующая сессия; см. `ALGORITM.md` §Стыки):** §1 скелет `src/` (жёсткий блокер старта greenfield-дека) · §2 `base.css` (одобрено) · §3 `_generator/audit.py` (спроектирован, контракт в `10-sborka-qa/DOK.md`) · §4 SVGO-конфиг (Р10). Плюс math-арк (Р7), tokens-эмиттер (Р8).

**ХЭНДОФФ.** Фаза I — в НОВОМ чате (контекст этой сессии забит). Инфра-сборка — отдельной фокус-сессией. Гейт «многогранники» (Фаза I для них уже решена, контент в отчёте владельца) — когда будет скелет. Вход в следующую сессию — по арк-ритуалу (NAVIGATOR→PLAN→хвост SESSIYA).

## 2026-07-11 — ночные прогоны приняты (Фаза I пакеты + инфра)
Два ночных захода под Claude Code (opusplan) отработали и приняты аналитиком по гейтам (скилл §5, санити по живым файлам).

**Инфра (`kod_infra.md`) — ПРИНЯТ.** A: `_generator/svgo.config.mjs` (svgo@3.3.2 + prefixIds по stem, проверен на cone-title). B: `_generator/audit.py` (контракт слоя 3, py_compile ✓, НЕ прогнан — нужен браузер, за владельцем). C: `_generator/skeleton/` + `base.css` — byte-exact на обоих эталонах (`verifikacia-infra.md`; я перепроверил независимо: dandelin/buffon пересобираются в точный размер оригинала). **Исправлена ложная предпосылка ALGORITM §Стыки:** общий base на ВСЕ деки байт-безопасно НЕВОЗМОЖЕН (эталоны расходятся по существу канона: `.slide` фон, transition, стейдж) → base только ПЕР-ДЕКОВЫЙ, скелет несёт dandelin-base дефолтом. Реальные эталоны НЕ мигрированы (канон цел, dist == baseline). `build_deck.py` +1 строка (`{{BASE_CSS}}` опционален). 3 развилки владельцу (единый/семейный base; миграция эталонов; preserveAspectRatio).

**Фаза I (`kod_faza1.md`) — ПРИНЯТ.** Пакеты `01-brief … 05-raskadrovka` (15 файлов) по образцу 06–10; дистилляты арок 1/4/5 (2–3 из готового семени, не передистиллировано). Все 7 критериев PASS (`verifikacia-faza1.md`); исполнитель дисциплинированно НЕ распараллелил запись пакетов (связность контрактов). Вердикт верификатора «точечная дочистка» (2 mojibake) — исполнитель сам исправил; я подтвердил: CJK 0, ссылки 0 битых. Контракт 5→6 несёт поле `register`+`word_budget_per_slide` (то, чего не хватало арке 6). 14 вкусовых развилок вынесены владельцу, ничего не решено молча (группа A — 6 нужных для старта арок; группа B — 8 архитектурных).

**Консолидировано:** KARTA §6 (пакеты 01–05 + инфра `_generator/{skeleton,svgo.config.mjs,audit.py}`); 00-KONVEJER (статусы арок 1–5); ALGORITM §Стыки (A/B/C ✅ + правка предпосылки base.css).

**Критичные развилки владельцу (гейтят первый реальный дек):** мат-долг синтаксис (реком. Б `<details class="math-debt">`); форма поля регистр+бюджет; формат интервью (реком. гибрид); + инфра: единый/семейный base.css. Полный список — `kod_faza1.md` §ВОПРОСЫ (14) + `kod_infra.md` §ВОПРОСЫ (3).

**Разблокировано:** скелет = «шаг 0» → гейт готовности «многогранники» теперь можно прогнать end-to-end. **Арка не закрыта:** остаются вкусовые развилки + гейт многогранников; консолидация решений в RESHENIYA — при закрытии.

## 2026-07-11 — владелец комментирует развилки (переосмысления + решения)
Разбор `RAZVILKI.md` с владельцем. Несколько ответов — не «да/нет», а переосмысления; часть ждёт вложений (НЕ дошли — uploads пуста).

**Сквозной принцип (важно, кандидат в RESHENIYA):** источник истины = **markdown** (быстро/дёшево править содержание), **HTML = вид**, пересборка markdown→HTML — **Python-скриптом, без токенов**. ОДИН источник истины на ВСЕХ стадиях (интервью → растущий источник → раскадровка → финал). Распространяется на всё «текст→визуал». Согласуется с Р1, но владелец хочет это явно и раньше (с Фазы I), и token-free пересборку. У владельца есть готовая техника — обещал прикрепить папку (НЕ дошла).

**Переосмысления (ждут вложений/подтверждения):**
- **A1:** интервью не статичный 5-строчный бриф, а СТАРТ растущего документа (концепт курса → концепт лекции), который наполняется по фазам (математикой и т.д.) и собирается в HTML по требованию. Это тот же документ, что становится гибрид-источником (арка 4).
- **A3 (мат-долг):** механизм = **пометки на полях** (margin notes) в документе — существующая техника владельца. Заменяет мою рек. `<details class="math-debt">`. Владелец пришлёт 2 черновика лекций с полями (НЕ дошли).
- **A4:** устный регистр — текст рассказа МОЖЕТ быть большим/неограниченным (лектор дораскажет голосом); бюджет слов — ТОЛЬКО для ПИСЬМЕННОГО (что на слайде, влезает в объём слайда). Ревизия поля: ограничение — на slide-text, не на устную часть.
- **A5/A6:** раскадровка = самый дешёвый минимальный HTML-вид, чтобы глазами проверить, как сидит текст/влезает; markdown-источник + Python-пересборка; один источник demo→текст→финал. Дефолт формата — на моё усмотрение, лишь бы бесшовно.

**Решено (лочим):**
- **A2:** строгость — спрашивать явно, зависит от аудитории (спектр: строгий спецкурс ↔ научпоп). Подтверждено.
- **I1: база канона = BUFFON** (не dandelin). Buffon — стандарт (совпадает с Canva-шаблоном и остальными референсами); dandelin был первым пробным, «разъехался». Довести/слить с dandelin — отдельным заходом дизайн-системы позже.
- **I2:** эталоны НЕ мигрировать. **I3:** SVGO preserveAspectRatio — оставить.
- **B8:** no-go «тема не тянет» — чекпоинт владельцу (на этой стадии диалог). **B7, B9–B14:** принять мои рекомендации (владелец не разобрал формулировки, доверяет).

**Ждём от владельца:** переприкрепить (1) 2 черновика лекций с пометками на полях, (2) папку с техникой markdown→HTML. До этого A1/A3/A4/A5 и base→buffon не финализируем (иначе переделка). Пакеты 01–05 пока НЕ правлены.

## 2026-07-11 — решение по структуре движков + дизайн doc-движка
Владелец приложил рабочие папки (`kurs leto 2026`, `catalan`); разведка (`distillat-tehnika-md-html.md`) вскрыла его технику: markdown→HTML делает `build.py` (kurs leto, токен-фри, `status`-гейт, `> поле:`→`<aside>`); наш `build_deck.py` — его потомок; пометки на полях = класс `.mn` (float в жёлоб).

**Решение владельца (вар. 1, → RESHENIYA Р15):** `_generator/` = ДОМ ДВУХ ДВИЖКОВ одного семейства (дек `build_deck.py` ГОТОВ + документ doc-движок СПРОЕКТИРОВАН, портировать из `build.py`); `_studio` = мозг, пользуется ими. Код НЕ сливать (разные модели данных: слайд-канвас vs поток+поля). Дизайн — `_generator/DVIZHKI.md` (зарегистрирован в KARTA §1).

**Как это закрывает развилки Фазы I:** A1 (интервью = старт растущего markdown-документа со `status`), A3 (мат-долг = пометка на поле `> поле:`/`.mn`, видимая+грепаемая), A4 (регистр: устный = устный текст не ограничен, лимит только на slide-text; читаемый = слайд несёт довод), A5/A6 (раскадровка = дешёвый doc-движковый вид, один markdown-источник demo→тексты→финал).

**Ещё решения (в RESHENIYA Р16 / к энкодингу в пакеты):** база канона = **buffon** (стандарт; dandelin — первый пробный, разъехался; переключить дефолт скелета). Строгость (A2) — спрашивать явно, зависит от аудитории. Требование владельца: «видеть, как выглядит, пока правишь» — цикл md→Python-пересборка→смотрю HTML; на будущее — doc-движок как общий скилл + in-place правка (à la Claude Design).

**Следующие ходы (follow-on, разные заходы):** (1) реализовать doc-движок (портировать `build.py` + `> поле:`/`.mn`/`status`/KaTeX-вид); (2) довести пакеты Фазы I `01/03/04/05` под этот механизм (сейчас черновики по старому «HTML с этапа 2»); (3) переключить базу скелета dandelin→buffon; (4) обкатать «многогранники» end-to-end.

## 2026-07-11 — грунтовка формата Фазы I по живым текстам владельца
Владелец: НЕ изобретать формат — взять из его лекционных текстов (`catalan`, `kurs leto`). Прочитал `mir1-rasskaz.md` сам + разведчик (`distillat-format-teksta.md`).

**Взято (grounded):** шаблон обзора — шапка-`>` (статус+throughline+указатель на skelet) → вопрос-крючок §0 → цепочка актов «конкретная задача → прозрение вслух → стержень-идея, выживающая обобщение → выход в новую область» → закрытие с кольцеванием и честным «зазором». Слои: `rasskaz` (нарратив) / `skelet` (полные доказательства, шаблон: Определение/Утверждение/Доказательство/Источник/Уровень) / `statya.html` (сборка). Пометки: `.insight`/`.mn`/`.foot`; **мат-долг = конвенция `⚑ Флаг`→`Флаг закрыт`** (грепается). Гейт-против-воды: приём-заглушка обязан нести рядом предложение-механизм (реальный провал — `mir2-rasskaz §6`).

**⚠ Развилка-линчпин (владельцу):** гибрид-источник = ОДИН файл со свёрнутыми `<details>` (блюпринт Р5) vs ДВА параллельных документа (`rasskaz`+`skelet`), сшитых цитатой-сноской (реальная практика владельца). `<details>` у него = оглавление, не доказательства. Сверить перед доводкой арки 4. DVIZHKI обновлён под grounded-формат.

## 2026-07-11 — брейншторм Фазы I + большие решения; переход в новый чат
Долгий разговор с владельцем по структуре Фазы I (скилл `brainstorming`). Осело:
- **Р17 — автономность:** скиллы ВНУТРЬ, не в прод (кроме код+3D). popsci-* / the-interviewer / math-russian-terminology — разобрать на куски, разложить шагами. Термины → per-project документ-список + шаг сверки (не скилл-триггер, это был баг на Каталане). Ужесточает Р3.
- **Р18 — лекция = курс, одна фабрика:** курс = запуск многократно (концепт курса → пер-лекция), костяк-текст может быть для лекции ИЛИ курса (как catalan); мосты между лекциями фиксируем; фабрика делает и контент, и презентацию/сайт поверх. Разворачивается в фабрику курсов.
- **Редизайн Фазы I** (`FAZA-1-REDESIGN.md`, заменяет ранние стадии блюпринта): интервью=разведка; ресёрч и нарратив = РАЗНЫЕ сессии; ресёрч = ДВА документа-котла (математика ‖ научпоп-контекст, критерии: математика переизобретаема вопросами / каждый факт — законченная мини-история); нарратив отдельной сессией тащит линию из котла. `distillat-reserch-matbaza` частично устарел.
- Формат текста grounded в живых текстах владельца (`distillat-format-teksta.md`): слои rasskaz/skelet/statya, пометки `.insight`/`.mn`/`.foot`, мат-долг `⚑ Флаг`, гейт-против-воды. DVIZHKI обновлён.

Консолидировано: RESHENIYA Р15–Р18; FAZA-1-REDESIGN.md + KARTA §1/§6; DVIZHKI.md; PLAN «СЕЙЧАС» = задача следующей сессии.

**ПЕРЕХОД:** чат исчерпан. Следующая сессия — НОВЫЙ чат, вход по арк-ритуалу (+ прочитать FAZA-1-REDESIGN, DVIZHKI, Р15–Р18): сделать план и раздать параллельные заходы (Claude Code, ~1ч) на (1) доводку фабрики (doc-движок #11, пакеты Фазы I под редизайн #12, база→buffon #13, скиллы-внутрь Р17) и (2) реальный прогон как ФАБРИКА КУРСА — концепт ≈8 лекций, потом слайды первых 4 с мостами. Арка НЕ закрыта.

## 2026-07-11 — поправка хэндоффа: kurs-fabrika + цель след. сессии
Владелец: цель следующей сессии — НЕ реальный прогон, а **довести фабрику до «почти полностью готовой»**: полностью разработать верхнюю часть Фазы I (по редизайну), внести скиллы внутрь (Р17), поправить мелочи Фазы II. Реальный прогон (#14) — уже ПОЗЖЕ, отдельной сессией.
**+ Новая задача (#15): абсорбировать `kurs-fabrika`** — заброшенный старт-кит «фабрика курсов» (`~/Documents/GitHub/kurs-fabrika/`, v1 из «Каталана»): PLAYBOOK (методология фаз/профилей/**циферблатов масштаба лекция↔курс**), CHECKLIST Ф0–Ф8, shablon/, заход «программа курса». Взять курс-уровневую методологию в `_studio` под Р18, потом папку удалить (лоссово → бэкап+верификатор, удаление гейтится). Наши процесс-доки эволюционнее — брать ДЕЛЬТУ про курсы, не дубли. PLAN «СЕЙЧАС» обновлён под эту цель.

## 2026-07-10 — ДОВОДКА до «почти готово»: волны A/B/C (Cowork ведёт заходами)
Сессия из PLAN «ЦЕЛЬ». Cowork держал стратегию, всю конкретику — заходами (general-purpose исполнители с инлайн-ролью, приёмка по гейтам скилла §4). Вход по ритуалу + FAZA-1-REDESIGN/DVIZHKI/Р15–18. **D1 (структура источника Фазы I)** владелец делегировал Cowork → закрыт дизайном+демо (не абстрактным выбором): источник = markdown, вид = один HTML с двумя вкладками математика‖научпоп (стадия-котёл), внутри — карточный граф Каталана.

**Волна A (инфра) — принята.** З-1 `base→buffon` (#13): скелет-дефолт dandelin→buffon; buffon пересобирается byte-exact, dandelin без регрессии (независимый верификатор). З-2 `doc-движок` (#11): `../../../_generator/build_doc.py` — поток+поля (`.mn`/`.insight`/`.foot`), `status`-гейт, мат-долг `⚑`→exit 1, token-free + ДЕМО двухвкладочного котла (`../../../_generator/doc-demo/view.html`).

**Волна B (ядро Фазы I) — принята.** `kod_faza1-redesign.md` (opus): пакеты `konvejer/01–05` переписаны. Грунтовка — `distillat-catalan-kartochki.md` (карточный граф; нюанс: у Каталана `rasskaz‖skelet` = пост-нарративный разрез, ≠ пред-нарративные котлы математика‖научпоп → синтез: теги над одним графом). Скиллы внутрь (Р17). 4 развилки ratified владельцем. Свёрка live: контракт 5→6 полон, 0 битых ссылок, CJK 0, старое наследие вычищено.

**Волна C (абсорбция kurs-fabrika) — принята + удаление.** `kod_absorb-kursfabrika.md`→`delta-kursfabrika.md` (дельта: профили/циферблаты/Ф7-Ф8/программа-курса тащить; процесс-каркас — дубль). `kod_absorb-prizemlenie.md`→ `PROFILI-CIFERBLATY.md` + `PROGRAMMA-KURSA.md` + таблица модель/запуск в `RUKOVODSTVO-zahodami.md`. Свежий верификатор: список потерь ПУСТ, GREEN. **`kurs-fabrika` УДАЛЁН** (бэкап `backup_kurs-fabrika_20260710.tar.gz` в арке; удаление гейтилось разрешением).

**Осело:** RESHENIYA Р19 (редизайн Фазы I + 4 развилки), Р20 (скиллы внутрь + kurs-fabrika); Р5 пересмотрено. **Фабрика — «почти готова».** Остаток: реальный прогон #14 (многогранники end-to-end) — СЛЕДУЮЩАЯ сессия; мелочь — 06/09 внешние термины (Фаза II, беклог).
