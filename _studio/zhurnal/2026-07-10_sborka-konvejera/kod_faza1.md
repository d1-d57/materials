# Канал исполнителя — ФАЗА I: пакеты шагов 1–5 (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу этого файла. Метрика — КАЧЕСТВО. Часы — норма.
> Все пути — от корня репозитория `materials/` (ты стартуешь в нём).

КОНТЕКСТ. Проект `_studio` — фабрика научпоп-презентаций: конвейер «идея → готовый дек» из 10 шагов поверх генератора `_generator/build_deck.py` и замороженного канона (эталоны `dandelin`, `buffon`). Каждый шаг живёт как **папка-пакет** `_studio/konvejer/NN-<имя>/` из трёх файлов: `DOK.md` (подробная документация, паттерн SKILL.md), `ZAHOD.md` (промт под Claude Code по `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md`), `PRIMERY.md` (примеры). Прошлый этап: **Фаза II (шаги 6–10) собрана пакетами и верифицирована** (см. `_studio/konvejer/`). ЦЕЛЬ этого захода: собрать пакеты **шагов 1–5 (Фаза I «индивидуальность»)** в ТОМ ЖЕ формате и стиле, что 6–10.

Приёмка — по ОТЧЁТУ, без построчной сверки.
**СТОП ДО ЦЕЛИ (громко):** ты произведёшь ЧЕРНОВЫЕ пакеты 1–5 + отчёт верификатора + список ВКУСОВЫХ РАЗВИЛОК владельцу. Ты НЕ финализируешь greenfield-решения, требующие вкуса владельца (формат интервью, синтаксис «мат-долга», структура раскадровки) — их выносишь в `## ВОПРОСЫ` пакета, не «решаешь молча». Ты НЕ трогаешь общий индекс (`KARTA.md`, дневник арки `SESSIYA.md`/`PLAN.md`, `RESHENIYA.md`) — консолидацию сделает аналитик утром.

## 0. ПЕРВЫЙ ХОД
Рабочая папка — корень репо `materials/`. **Точка отката:** сделай zip-бэкап `_studio/konvejer/` СРАЗУ (единственный откат; в main не коммить). Прочитать ТОЛЬКО (ничего сверх):
- **Формат пакета и стиль (эталон для копирования):** `_studio/konvejer/00-KONVEJER.md`, `_studio/konvejer/ALGORITM.md`, `_studio/konvejer/FORMAT-ISTOCHNIKA.md`; готовый пакет как ОБРАЗЕЦ структуры — `_studio/konvejer/06-tekst/{DOK.md,ZAHOD.md,PRIMERY.md}` и `_studio/konvejer/10-sborka-qa/{DOK.md,ZAHOD.md}`.
- **Дизайн Фазы I (ядро задачи):** `_studio/docs/spravka/FAZA-1-BLUEPRINT.md` (этапы 0–4 с выходами и гейтами) + `_studio/docs/pochemu-i-videnie/RESHENIYA.md` (Р5 линчпин-гибрид-источник, Р6 фазы, Р13 интервью под матлекцию — НЕ общий скилл).
- **Методология ZAHOD:** `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md` (скелет захода — по нему пишешь `ZAHOD.md` каждого пакета).
- **УЖЕ ГОТОВОЕ семя арок 2–3 (НЕ переделывать):** `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-reserch-matbaza.md` — дистиллят «ресёрч + матбаза как один поток». Это твой источник для пакетов 2 и 3; заново не дистиллировать.
Затем напиши `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
Назови предпосылки и развилки в `## ПЛАН` до правок; развилку не выбирай молча. Минимум без спекуляций — пакеты 1–5 строго по образцу 6–10, без новых сущностей «на будущее». Хирургия: только создаёшь `_studio/konvejer/01-brief/ … 05-raskadrovka/`; чужого не трогаешь. Критерий — проверяемый (см. §3), петля до зелёного. **Оспорь ложную предпосылку:** если FAZA-1-BLUEPRINT или этот заход расходятся с живыми файлами — проверь по файлам, скажи в `## ВОПРОСЫ`, не гоняйся за фантомом.
**СУБАГЕНТЫ (аккуратный режим):** для дистилляции семян арок **1, 4, 5** подними ≤3 субагента-дистиллятора параллельно (арки 2–3 НЕ дистиллировать — семя готово). Каждый читает названное, пишет `distillat-<арка>.md` в папку арки `_studio/zhurnal/2026-07-10_sborka-konvejera/`, возвращает 3 строки. Рейт-лимит = отступить и доложить в `## ОТЧЁТ`, НЕ слепой ретрай. Сборку 5 пакетов можно тоже раздать субагентам (≤5), но каждый пакет — свой; DOK не переписывать после написания без причины.

## 2. ГРАНИЦЫ
НЕ трогать: пакеты `06…10` (готовы, верифицированы), `00-KONVEJER.md`/`ALGORITM.md`/`FORMAT-ISTOCHNIKA.md` (кроме если явно надо дописать ссылку — тогда в `## ВОПРОСЫ`), генератор `_generator/*`, эталоны `dandelin`/`buffon`, замороженный канон (KONSTITUCIYA), общий индекс `_studio/docs/KARTA.md` и дневник `_studio/zhurnal/.../{SESSIYA,PLAN}.md`, `RESHENIYA.md`. Ты только СОЗДАЁШЬ 5 новых папок-пакетов. Регистрацию в KARTA и консолидацию оставляешь аналитику (перечисли созданное в `## ОТЧЁТ`).

## 3. ЗАДАЧА
Собрать 5 пакетов Фазы I по образцу 6–10. Шаги и их суть (из `FAZA-1-BLUEPRINT.md`, этапы 0–4):
- **`01-brief/`** — Бриф/интервью ПОД МАТЛЕКЦИЮ (Р13: НЕ общий `the-interviewer`/`brainstorming`; у матлекции заранее понятная арка). Выход шага: бриф ~5–6 строк (тема · аудитория · длительность→бюджет слайдов · throughline-кандидат · что резать · референс · **уровень строгости** — решение владельца: это ПОЛЕ брифа, не глобальный дефолт). Дистиллятор арки 1 читает: `FAZA-1-BLUEPRINT.md` §0, Р13, и КРИТИЧЕСКИ смотрит скиллы `the-interviewer`/`brainstorming` — что перенять, но переосмыслить под матлекцию (не звать generic).
- **`02-reserch/`** и **`03-matbaza/`** — из ГОТОВОГО `distillat-reserch-matbaza.md` (ресёрч + строгая матбаза как ОДИН локальный поток; правило персистентности; гейт «математика верна» между 3 и 4). Семя не переделывать — собрать пакеты из него.
- **`04-gibrid-istochnik/`** — ЛИНЧПИН (Р5): ОДИН растущий HTML (живая история + математика финального объёма в тексте + свёрнутые `<details>` + 🖼-описания илл.). Режима нет ни в одном скилле — чистый greenfield из `FAZA-1-BLUEPRINT.md` §3. Спроектировать синтаксис «мат-долга» (маркер «здесь нужен вывод X», физически видимый, нельзя молча пропустить) — но как ПРЕДЛОЖЕНИЕ в `## ВОПРОСЫ`, развилка владельцу.
- **`05-raskadrovka/`** — из источника: список слайдов + бюджет; на каждый archetype + дистилляция + сцены + описание илл. (СОДЕРЖАНИЕ, не финальный текст). Выход — кликабельное HTML-демо (плейсхолдеры илл. + сцены + рамочные слайды), см. `FAZA-1-BLUEPRINT.md` §4. **Обязательно:** контракт 5→6 несёт структурное поле «**регистр + бюджет слов деки**» (устный/читаемый) — этого не хватало арке 6 (см. её DOK «Открытые вопросы»). Дистиллятор арки 5 смотрит `content-studio` (mockup-format, archetypes) + blueprint.

Каждый пакет = `DOK.md` (когда · класс Р14=GREENFIELD · контракт вход→выход · гейт · алгоритм · ловушки · якоря · открытые вопросы) + `ZAHOD.md` (промт под Claude Code по RUKOVODSTVO: ## 0 первый ход + ## 1 дисциплина Карпатова + ## 2 границы + ## 3 задача+КРИТЕРИЙ-который-может-провалиться + ## 4 верификатор если уместно + ## 5 отчёт + ## ПЛАН/ВОПРОСЫ/ОТЧЁТ; H1 = `# Канал исполнителя — <ШАГ> (один заход до конца)`) + `PRIMERY.md` (примеры; для greenfield-шагов примеры тоньше — опирайся на `FAZA-1-BLUEPRINT.md`, структуру `brief.md` эталонов `dandelin/src/brief.md`,`buffon/src/brief.md`, и распил источника; если живого примера нет — честно помечай «иллюстративный по блюпринту», не выдавай выдумку за цитату).

Связки: вход Фазы II (арка 6 ждёт регистр+бюджет от арки 5; арка 9 ждёт 🖼-описания от арки 4) — сверь с `ALGORITM.md` §«Вход Фазы II» и не сломай контракт. Строгость — поле брифа арки 1 (решение владельца 2026-07-10).

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):** (1) существуют 5 папок `_studio/konvejer/01-brief/ … 05-raskadrovka/`, в каждой ровно `DOK.md`+`ZAHOD.md`+`PRIMERY.md`; (2) каждый `ZAHOD.md` несёт скелет RUKOVODSTVO с КРИТЕРИЕМ, который может провалиться (не «я закончу»); (3) 0 битых относительных ссылок (проверить программно `test -e` из папки пакета); (4) арки 2–3 собраны из существующего дистиллята, не передистиллированы; (5) контракт 5→6 явно несёт «регистр+бюджет слов»; (6) все вкусовые развилки (формат интервью, синтаксис мат-долга, детали раскадровки) вынесены в `## Открытые вопросы` DOK / `## ВОПРОСЫ`, а не решены молча; (7) стиль/структура совпадают с образцом 06-tekst/10-sborka-qa (H1, разделы, плотность). Не сошлось хоть одно — стоп, чини, потом отчёт.

## 4. ВЕРИФИКАТОР (обязателен — это лоссово-синтетический заход)
В конце подними ОДИН свежий субагент-верификатор, ДРУГИМ методом: он читает живые файлы заново и проверяет ПРОГРАММНО — (а) все относительные ссылки в 5 пакетах резолвятся (`test -e`, битых 0); (б) каждый `ZAHOD.md` имеет секции ## 0/1/2/3/5 + ## ПЛАН/ВОПРОСЫ/ОТЧЁТ и КРИТЕРИЙ-который-может-провалиться; (в) пакеты 2–3 действительно опираются на `distillat-reserch-matbaza.md` (ключевые нюансы: правило персистентности, гейт «математика верна», мат-долг — на месте); (г) контракт 5→6 несёт регистр+бюджет; (д) стиль совпадает с 06/10. Отчёт верификатора — в файл `_studio/zhurnal/2026-07-10_sborka-konvejera/verifikacia-faza1.md`, СПИСОК ПОТЕРЬ + вердикт (принять / точечная дочистка). Не сошлось — дочисти точечно и перепроверь.

## 5. ОТЧЁТ → секция ## ОТЧЁТ внизу
Что сделал и ЗАЧЕМ (по каждому пакету) · как проверил (критерий §3 по пунктам) · что НЕ трогал (границы) · вкусовые развилки владельцу (сводный список — САМОЕ ВАЖНОЕ для утреннего ревью) · результат верификатора (путь + вердикт + список потерь) · **время прогона + потраченные токены** · что осталось для аналитика (регистрация в KARTA §6, консолидация).

## ПЛАН — (исполнитель)

**Предпосылки (названы до правок, Карпатов):**
- П1. Формат пакета = ровно `06-tekst`/`10-sborka-qa`: H1 `# Арка N — <имя> · <класс Р14>` в DOK и `# Канал исполнителя — <ШАГ> (один заход до конца)` в ZAHOD; DOK-блоки (когда·вход·выход·гейт·next·семя → Контракт → правила/алгоритм → ловушки → якоря → Открытые вопросы); ZAHOD-скелет RUKOVODSTVO (## 0/1/2/3[+4]/5 + ## ПЛАН/ВОПРОСЫ/ОТЧЁТ) с КРИТЕРИЕМ-который-может-провалиться. Проверено чтением обоих образцов — совпадает.
- П2. Все 5 арок — класс **GREENFIELD** (по `00-KONVEJER.md` карте шагов). Значит примеры тоньше, PRIMERY опирается на blueprint/эталоны, «иллюстративный по блюпринту» помечается честно.
- П3. Арки 2–3 собираются из ГОТОВОГО `distillat-reserch-matbaza.md` — не передистиллировать (проверил: дистиллят несёт полные контракты 2 и 3, правило персистентности, гейт «математика верна», список next-дыр включая мат-долг).
- П4. Цепочка Фазы I: 1(бриф)→2(ресёрч)→3(матбаза)→4(гибрид-источник, линчпин)→5(раскадровка)→[вход Фазы II]. Растёт ОДИН HTML с этапа 2/3 (формат-инвариант HTML+MathJax+SVG). Гибрид-источник (арка 4) кормит: арку 6 ($tex$+смысл), арку 9 (🖼-описания). Раскадровка (арка 5) кладёт в `src/` рамочные слайды + плейсхолдеры + `brief.md` c **регистром+бюджетом слов** — сверено с `ALGORITM.md` §«Вход Фазы II».

**Развилки → в ## ВОПРОСЫ владельцу (не решаю молча):**
- Р-и1. Формат микро-интервью арки 1 (набор вопросов под матлекцию; как «уровень строгости» становится ПОЛЕМ брифа).
- Р-и2. Синтаксис «мат-долга» арки 3/4 (физически видимый маркер «здесь нужен вывод X», нельзя пропустить молча) — предложу 2–3 кандидата.
- Р-и3. Детали раскадровки арки 5 (структура записи слайда; форма кликабельного HTML-демо).
- Р-и4. `proof-self-review` для гейта «математика верна?» (из next дистиллята) — за владельцем.
- Р-и5. no-go «тема не тянет» из гейта арки 2 — чекпоинт владельцу vs тихий сдвиг.

**Ход:**
1. Точка отката — zip-бэкап `_studio/konvejer/` — СДЕЛАН (`BACKUP_konvejer_20260710_042634.zip`, 129 КБ).
2. 3 субагента-дистиллятора параллельно (sonnet, аккуратный режим): **арка 1** читает `the-interviewer`+`brainstorming` SKILL.md + Р13 + blueprint §0; **арка 4** читает blueprint §3 + Р5 и проектирует кандидатов мат-долга; **арка 5** читает `content-studio` SKILL.md + blueprint §4 + арка-6 DOK «Открытые вопросы». Каждый пишет `distillat-arka{1,4,5}.md` в папку арки, возвращает 3 строки. Арки 2–3 НЕ дистиллирую (П3).
3. Пока дистилляторы работают — создаю 5 папок; пишу пакеты **02-reserch** и **03-matbaza** из готового дистиллята (зависимости от субагентов нет).
4. По готовности дистиллятов — пишу пакеты **01-brief**, **04-gibrid-istochnik**, **05-raskadrovka**. Пакеты пишу САМ в одном контексте (не раздаю сборку субагентам) — доминирующий риск здесь связность кросс-контрактов (5→6 регистр+бюджет, 4→9 🖼, гейт 3→4) и точное совпадение хаус-стайла; один сильный контекст это контролирует лучше, чем 5 параллельных писателей. Это дисциплинированный выбор, отражён в отчёте.
5. Гейт §3 по пунктам (программно: `test -e` ссылок, наличие секций, «регистр+бюджет» в 5→6).
6. Свежий субагент-верификатор ДРУГИМ методом → `verifikacia-faza1.md`, список потерь + вердикт. Точечная дочистка при находках.
7. Заполняю ## ВОПРОСЫ (сводка развилок) и ## ОТЧЁТ.

**Границы (не трогаю):** `06…10`, `00-KONVEJER.md`/`ALGORITM.md`/`FORMAT-ISTOCHNIKA.md`, генератор, эталоны, канон, `KARTA.md`/`SESSIYA.md`/`PLAN.md`/`RESHENIYA.md`. Только создаю `01-brief … 05-raskadrovka`. Регистрацию в KARTA и консолидацию — аналитику.

## ВОПРОСЫ — (исполнитель)

Сводный список ВКУСОВЫХ РАЗВИЛОК владельцу (ничего не решено молча — всё вынесено в `## Открытые вопросы` соответствующих DOK; здесь — свод для утреннего ревью). Помечена рекомендация/дефолт, но финал за владельцем.

**Группа A — нужны для старта арок (решают, как выглядит шаг):**
1. **Формат микро-интервью арки 1** (`01-brief/DOK.md`). К1 «одна пачка+1 уточнение» / К2 «3 хода one-at-a-time» / К3 «дефолт-первый, правки на расхождение». *Реком.: гибрид — дефолт-блок логистики (К3) + явный открытый вопрос по throughline (К2).* Дефолт захода в ZAHOD — гибрид.
2. **Как «уровень строгости» становится полем брифа** (`01-brief/DOK.md`). Опции: интуитивный / выкладки в `<details>` / полностью строгий. Спрашивать всегда MC или предлагать дефолт по теме? *Реком.: явная MC-опция каждый раз (Р13 требует поле на деку).*
3. **Синтаксис «мат-долга» арок 2–4** (`04-gibrid-istochnik/DOK.md`). А — HTML-коммент `<!-- MATH-DEBT -->` (грепается, но невидим в рендере); Б — `<details class="math-debt" data-debt>` (виден в коде и рендере, эволюция заглушка→вывод, CSS-QA); В — видимый `<div class="debt">` (максимально виден, ломает поток). *Реком.: Б (баланс), возможен гибрид Б+А.* Это ключевая механика — без выбора «правило персистентности» остаётся декларацией.
4. **Форма поля «регистр + бюджет слов деки» в `brief.md`** (`05-raskadrovka/DOK.md`). Предложено `register: устный|читаемый` + `word_budget_per_slide: 30-35|45-57`. Утвердить синтаксис (это контракт 5→6, критичный вход арки 6).
5. **Структура записи слайда в раскадровке** (`05-raskadrovka/DOK.md`). 1 — атрибуты на каркасе; 2 — таблица в `brief.md`; 3 — гибрид (таблица-канон + `data-*`-производная). *Реком.: гибрид (дефолт захода).*
6. **Форма кликабельного HTML-демо арки 5** (`05-raskadrovka/DOK.md`). 1 — один плоский HTML; 2 — отдельные `slides/*.html` + `index.html` prev/next; 3 — двухступенчато (вайрфрейм→материализация). *Реком.: 2 (ближе к Фазе II, дефолт захода).*

**Группа B — архитектурные, можно закрыть при первом реальном деке:**
7. **`proof-self-review` для гейта «математика верна?»** (арки 2/3) — звать или нет. По духу Р3 подходит (инструментальный), но не в списке допущенных внешних скиллов. За владельцем.
8. **no-go «тема не тянет»** из гейта арки 2 (`02-reserch/DOK.md`) — чекпоинт владельцу vs тихий сдвиг акцента. Блюпринт зовёт «гейты автора» (Р6) → вероятно чекпоинт, но не проговорено.
9. **Одна сессия без обнуления 2→3** — как гарантировать в харнессе (кастомных `subagent_type` нет): один заход/субагент на обе стадии, либо два арк-файла подряд без compact/clear. Пакеты 02/03 написаны как ОДИН поток, но механику запуска надо зафиксировать.
10. **Граница гейта «источник закрыт?»** (арка 4) — автор-чекпоинт vs автопроверка (греп `.math-debt`, падение при находке).
11. **Кто снимает метку мат-долга при закрытии** — арка 3 (закрывает и снимает) vs сверочный шаг в арке 4. Пакеты предполагают: арка 3 обязана закрыть до гейта 3→4.
12. **Формат 🖼-описания** (`data-ill`/`data-tact`, арка 4) — сверить с реальным ожиданием арки 9 (`distillat-illustracii.md` не читан по границам захода).
13. **Named стиль-шаг арки 4** — тот же локальный корпус, что арка 6 (Р4), или отдельный узкий словарь (сценовая проза vs финальный текст).
14. **Возврат throughline в арку 1**, если при ресёрче (арка 2) или раскадровке (арка 5) он не подтверждается/не режется чисто — контракт возврата явно не проговорён.

## ОТЧЁТ — (исполнитель)

**СТОП ДО ЦЕЛИ (как заявлено):** произведены ЧЕРНОВЫЕ пакеты 1–5 + отчёт верификатора + сводный список вкусовых развилок (`## ВОПРОСЫ`). Greenfield-решения, требующие вкуса владельца, НЕ финализированы — вынесены в `## Открытые вопросы` каждого DOK и сведены в `## ВОПРОСЫ`. Общий индекс (`KARTA.md`, `SESSIYA.md`/`PLAN.md`, `RESHENIYA.md`) НЕ тронут — консолидация за аналитиком.

### Что сделал и ЗАЧЕМ (по пакетам)
Создано 5 папок `_studio/konvejer/01-brief … 05-raskadrovka/`, в каждой ровно `DOK.md`+`ZAHOD.md`+`PRIMERY.md`, по образцу 06-tekst/10-sborka-qa.
- **01-brief** (GREENFIELD). Микро-интервью ПОД МАТЛЕКЦИЮ (Р13): 7-полевой бриф с «уровнем строгости» как полем. Из the-interviewer/brainstorming перенят метод (one-question+дефолт, Phase 0), НЕ вызов скилла. Зачем: у матлекции заранее понятная арка — generic-интервью уводит не туда (диаграмма провала «платоновых тел»).
- **02-reserch** + **03-matbaza** (GREENFIELD, из ГОТОВОГО `distillat-reserch-matbaza.md` — НЕ передистиллировано). Два пакета одного потока 2→3: ресёрч растит факты+мат-долги+скелет, матбаза закрывает каждый мат-долг строгим выводом; правило персистентности, гейт «математика верна?» как жёсткая дверь 3→4, одна сессия без обнуления. Зачем: чинит срез математики — она обязательна и с гейтом, а не «черновик для себя».
- **04-gibrid-istochnik** (GREENFIELD, линчпин). ОДИН растущий HTML, три слоя (видимый / `<details>` / 🖼). Нарративная полировка ВОКРУГ запертой математики, вывод не переписывается; спроектированы 3 кандидата синтаксиса мат-долга (реком. Б). Зачем: единственный документ, кормящий слайды → математику не срезают (Р5).
- **05-raskadrovka** (GREENFIELD, конец Фазы I). Резка источника на слайды (archetype+дистилляция+сцены+илл.), выход = кликабельное демо + наполнение `src/`. **Контракт 5→6 несёт поле «регистр+бюджет слов деки»** (`register` + `word_budget_per_slide`) — то, чего не хватало арке 6. Из content-studio перенят язык описания и дисциплина вайрфрейма, НЕ финальность текста и не файловая монолитность.

### Как проверил (КРИТЕРИЙ §3 по пунктам)
1. **5 папок, в каждой ровно 3 файла** — ✅ (`ls` дерева: 15/15).
2. **Каждый ZAHOD несёт скелет RUKOVODSTVO + КРИТЕРИЙ, который может провалиться** — ✅ (программно: секции `## 0/1/2/3/5` + `## ПЛАН/ВОПРОСЫ/ОТЧЁТ` + строка «КРИТЕРИЙ ГОТОВНОСТИ» во всех 5; H1 = `# Канал исполнителя — …`).
3. **0 битых относительных ссылок** — ✅ (программно `os.path.exists` из папки каждого файла: 58 ссылок, 0 битых; одно скобочное сокращение `{dandelin,buffon}` заменено на явные пути).
4. **Арки 2–3 собраны из существующего дистиллята, не передистиллированы** — ✅ (субагентов на 2–3 не поднимал; DOK/ZAHOD воспроизводят правило персистентности, гейт 3→4, синтаксис мат-долга из семени; подтверждено верификатором, проверка «в»).
5. **Контракт 5→6 явно несёт «регистр+бюджет слов»** — ✅ (греп `register`/`word_budget_per_slide` в DOK+ZAHOD+PRIMERY арки 5).
6. **Все вкусовые развилки в `## Открытые вопросы`/`## ВОПРОСЫ`, не решены молча** — ✅ (формат интервью, уровень строгости, синтаксис мат-долга, структура/форма раскадровки, форма поля 5→6 — все как развилки с рекомендацией, не как решённое).
7. **Стиль/структура совпадают с 06/10** — ✅ (верификатор, проверка «д»: H1-формат, блок-цитата DOK, Контракт/Ловушки/Якоря/Открытые вопросы, плотность — PASS).

### Субагенты (аккуратный режим, названы явно)
- 3 дистиллятора-разведчика параллельно (sonnet, фон): арка 1 (the-interviewer+brainstorming), арка 4 (blueprint §3+Р5, кандидаты мат-долга), арка 5 (content-studio+blueprint §4). Каждый прочитал названное, записал `distillat-arka{1,4,5}.md`, вернул 3 строки. Арки 2–3 не дистиллировал (семя готово).
- 1 верификатор (sonnet, фон, ДРУГОЙ метод — читал живые файлы заново, проверял программно).
- **Дисциплинированный выбор:** сборку 5 пакетов НЕ раздал субагентам (заход это допускал) — писал сам в одном контексте ради связности кросс-контрактов (5→6 регистр+бюджет, 4→9 🖼, гейт 3→4) и точного хаус-стайла; это доминирующий риск качества, один сильный контекст его контролирует лучше 5 параллельных писателей. Рейт-лимитов не было.

### Что НЕ трогал (границы §2)
Пакеты `06…10`, `00-KONVEJER.md`/`ALGORITM.md`/`FORMAT-ISTOCHNIKA.md`, генератор `_generator/*`, эталоны `dandelin`/`buffon` (только читал `src/brief.md` для формы, как разрешено §33), канон, `KARTA.md`, `SESSIYA.md`/`PLAN.md`, `RESHENIYA.md`. Готовый `distillat-reserch-matbaza.md` не переписывал. Стыки Фазы II сверил с `ALGORITM.md`, контракт не менял.

### Вкусовые развилки владельцу (САМОЕ ВАЖНОЕ для утреннего ревью)
Полный свод — в `## ВОПРОСЫ` выше (14 пунктов; группа A — 6 развилок, нужных для старта арок; группа B — 8 архитектурных, можно закрыть при первом деке). Критичные: №3 синтаксис мат-долга (реком. Б `<details class="math-debt">`), №4 форма поля регистр+бюджет, №1 формат интервью (реком. гибрид).

### Результат верификатора
Путь: `verifikacia-faza1.md`. Вердикт: **точечная дочистка** (→ выполнена). Список потерь: 2, обе — mojibake-порча текста (не структурные): «быть局альной»→«быть локальной» (`02-reserch/PRIMERY.md`), «на双угольники»→«на двуугольники» (`03-matbaza/PRIMERY.md`). Обе исправлены; пере-проверка: CJK-порча 0, ссылки 0 битых. Проверки а–д (ссылки, скелет ZAHOD, опора на семя, контракт 5→6, стиль vs эталон) — все PASS.

### Время прогона + токены
Прогон ~30–35 мин настенных. Субагенты (точно): дистилляторы 66.0k + 60.4k + 67.7k = 194.1k; верификатор 151.5k; итого субагентов ≈ 345.6k токенов. Главный контекст (оценка) ≈ 120–150k. Суммарно ориентировочно ≈ 0.47–0.50M токенов. Бэкап: `BACKUP_konvejer_20260710_042634.zip` (129 КБ).

### Что осталось аналитику
- Регистрация 15 новых файлов (5 пакетов) + 4 новых дистиллята (`distillat-arka{1,4,5}.md`, `verifikacia-faza1.md`) в `KARTA.md` §6.
- Консолидация: обновить `00-KONVEJER.md` (статус арок 1–5: черновики собраны/верифицированы), дневник арки (`SESSIYA.md`/`PLAN.md`), при желании — осадить развилки группы A в `RESHENIYA.md` после решения владельца.
- Решить развилки `## ВОПРОСЫ` (особенно №1,3,4) — до первого реального greenfield-дека («многогранники»).
