# Канал исполнителя — ИНФРАСТРУКТУРА Фазы II (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу этого файла. Метрика — КАЧЕСТВО. Часы — норма.
> Все пути — от корня репозитория `materials/` (ты стартуешь в нём).

КОНТЕКСТ. Проект `_studio` — фабрика научпоп-презентаций: источник-папка `<лекция>/src/` собирается генератором `_generator/build_deck.py` (чистый stdlib) в один самодостаточный `dist/index.html` канона. Визуальный канон ЗАМОРОЖЕН (эталоны `dandelin`, `buffon`; KONSTITUCIYA §1). Управляющая линия Фазы II и список инфра-стыков — `_studio/konvejer/ALGORITM.md` §«Стыки с архитектурой». ЦЕЛЬ: закрыть 4 инфра-стыка, чтобы фабрика могла стартовать НОВЫЙ дек и проходить полный QA.

Приёмка — по ОТЧЁТУ, без построчной сверки.
**СТОП ДО ЦЕЛИ (громко, простыми словами):**
- **audit.py** ты НАПИШЕШЬ по контракту, но НЕ прогонишь — ему нужен браузер (Playwright), которого в этой среде нет; прогонит владелец на своей машине. Не гейти на его запуске.
- **Скелет + base.css** трогают ХРАНЕНИЕ канона. Делаешь их ТОЛЬКО под гейтом byte-exact (ниже). Если byte-exact не достигается разумными усилиями — НЕ ломай канон: откати, оставь вместо этого детальную СПЕКУ скелета/base.css и СТОП с докладом. Канон дороже фичи.
- Ты НЕ трогаешь общий индекс (`_studio/docs/KARTA.md`, дневник `SESSIYA.md`/`PLAN.md`, `RESHENIYA.md`) — консолидацию сделает аналитик утром.

## 0. ПЕРВЫЙ ХОД
Рабочая папка — корень репо `materials/`. **Точка отката:** zip-бэкап `_generator/` и `dandelin/src/dist/index.html` + `buffon/src/dist/index.html` СРАЗУ (эталон истины для byte-exact). В main не коммить. Прочитать ТОЛЬКО:
- `_studio/konvejer/ALGORITM.md` (§Стыки — 4 инфра-пункта, одобрено владельцем направление скелет/base.css) + `_studio/konvejer/FORMAT-ISTOCHNIKA.md` (контракт хранения; расхождение DESIGN.md `base.css`/`grids.css` vs живой формат — ключ к задаче).
- Контракты: `_studio/konvejer/09-illustracii/DOK.md` (SVGO/Р10: pinned + prefixIds по stem) + `_studio/konvejer/10-sborka-qa/DOK.md` (§гейт слой 3 = контракт `audit.py`).
- Генератор: `_generator/DESIGN.md` (§4 контракт источника, §5 плейсхолдеры вкл. `{{BASE_CSS}}`/`{{GRID_CSS}}`, §9 линтер), `_generator/build_deck.py` (модифицируешь ТОЛЬКО ради base.css, под гейтом), `_generator/render.py`, `_generator/SLIDE-FORMAT.md`.
- Канон под перенос: `dandelin/src/{shablon.html,tokens.css,brief.md}`, `buffon/src/{shablon.html,tokens.css,overlay.css,brief.md}`.
- Рамки: `_studio/docs/KONSTITUCIYA.md` (§1 канон заморожен, §7 render-identity), `_studio/docs/pochemu-i-videnie/RESHENIYA.md` (Р8 токены, Р10 SVG), `_studio/docs/kak-delat/RUKOVODSTVO-zahodami.md`.
Затем `## ПЛАН` перед действиями. **Порядок задач — от безопасного к деликатному (§3): SVGO → audit.py → скелет/base.css.** Так частичный успех полезен.

## 1. ДИСЦИПЛИНА (Карпатов)
Назови предпосылки/развилки в `## ПЛАН`. Минимум: ровно 4 стыка, ничего сверх (не «улучшать» генератор, не рефакторить рабочее ради красоты). Хирургия: каждая правка `build_deck.py` трассируется к задаче base.css; чужой стиль сохраняй. **Оспорь ложную предпосылку по живым файлам:** проверь, поддерживает ли `build_deck.py` уже `{{BASE_CSS}}`/`{{GRID_CSS}}` (DESIGN.md §5 их называет, но живые деки могут не использовать — см. FORMAT-ISTOCHNIKA); если base CSS уже отделяем — не изобретай. Критерий — который может провалиться (§3), петля до зелёного. **Один риск за раз:** для скелета/base.css сначала read-only АУДИТ (что в `shablon.html` — общий канон-base vs пер-слайдовый грид; где граница), потом МУТАЦИЯ по плану, потом СВЕРКА (byte-exact). **СУБАГЕНТЫ:** ≤3 (напр. верификатор byte-exact отдельным свежим субагентом, §4); рейт-лимит = отступить+доложить.

## 2. ГРАНИЦЫ
НЕ менять визуальный СТИЛЬ канона (KONSTITUCIYA §1) — только ПЕРЕНОС хранения байт-в-байт (§7). НЕ трогать: пакеты `_studio/konvejer/06…10` (готовы), эталонные `src/*` кроме как для доказательства byte-exact (работай на КОПИИ эталона, не на оригинале; оригинальный `dist/index.html` не удалять — он эталон истины), `engine.js` (движок дословно), `KARTA.md`/`SESSIYA.md`/`PLAN.md`/`RESHENIYA.md` (аналитик). Модификация `_generator/build_deck.py` разрешена ТОЛЬКО ради `{{BASE_CSS}}` и ТОЛЬКО если byte-exact обоих эталонов сохраняется; иначе откат.

## 3. ЗАДАЧА (4 стыка, порядок как ниже)
**A. SVGO-конфиг (безопасно, тестируемо).** Создай минимальный `_generator/svgo.config.mjs` (или в скелете): pinned версия SVGO + `prefixIds` с префиксом по stem файла (против конфликта id при инлайне нескольких SVG в один документ, Р10/09-DOK). Проверь через `npx svgo` на реальном `dandelin/src/illustrations/cone-title.svg` (у него внутренний `clipPath id="waffle"`) → в выходе id должен получить per-file префикс. Критерий: svgo прогоняется и префиксует id. Если `npx`/node недоступны — оставь конфиг + документируй ручной процесс, отметь в отчёте.

**B. `_generator/audit.py` (пиши по контракту, НЕ прогоняешь).** Реализуй по `10-sborka-qa/DOK.md` §гейт слой 3: Playwright headless, точка входа `<src>/dist/index.html`, проверки — overflow `.zone`==0; shadow/radius/gradient==0; фон-плашки только из `:root` СОБРАННОГО `tokens.css` (парсить, НЕ хардкод); весь `.fit`/`.t-body` текст `#333333`; `.t-body`≥35px (`FLOOR_PX=35`); 0 `<figcaption>`; `--scene-diff` (последовательные сцены отличаются только добавленными пикселями). PASS/FAIL/WARN построчно, exit 1 при FAIL. Паттерн 1:1 с `html-slides-studio/scripts/audit.py`, но под наш `dist/index.html`. Критерий: файл существует, синтаксически валиден (`python3 -m py_compile _generator/audit.py` проходит), структура проверок соответствует контракту. Прогон — за владельцем (нет браузера).

**C. Скелет greenfield-`src/` + base.css (ДЕЛИКАТНО, под гейтом).** Одобрено владельцем как render-identity-preserving перенос замороженного канона. Шаги: (1) АУДИТ — раздели в `shablon.html` эталона общий канон-base CSS (структура `#stage/.slide/.zone/.fit`, scene-каскад `[data-scene-from]`, `.blur-reveal`, `transition-delay:0s`) от ПЕР-СЛАЙДОВОГО грида (`#sNN .grid{}` — bespoke, остаётся в деке). (2) МУТАЦИЯ на КОПИИ эталона — вынеси общий base в `base.css`, добавь `{{BASE_CSS}}` в `shablon.html` и поддержку в `build_deck.py`; собери `_generator/skeleton/` = стартовый `src/`: `shablon.html` (каркас + плейсхолдеры, пер-слайдовый грид ПУСТ) · `base.css` (общий канон) · `engine.js` (дословно из эталона) · `tokens.css` (канон-палитра) · `fonts/` · шаблон `brief.md` (пустой `slide_order`, `accent_tag`) · пустые `content/ slides/ illustrations/`. (3) СВЕРКА (гейт, §4). **КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):** пересборка ОБОИХ эталонов `build_deck.py <эталон>/src --no-banner` после перехода на `base.css` даёт `dist/index.html`, **нормализованно идентичный** забэкапленному оригиналу (снять HTML-комментарии, схлопнуть межтеговые пробелы; sha256 совпадает ИЛИ `cmp` exit 0). Не byte-exact хоть у одного эталона → откати всё C, оставь детальную СПЕКУ скелета/base.css в `_generator/skeleton/SPEC.md` и СТОП по C с докладом (A и B при этом сохраняются).

## 4. ВЕРИФИКАТОР (обязателен для C — это перенос/лоссовая правка)
Свежий субагент, ДРУГОЙ метод: читает ОРИГИНАЛЬНЫЙ `dist/index.html` обоих эталонов из zip-бэкапа (не из рабочей копии), пересобирает из модифицированного `src/` через `build_deck.py`, нормализует и сверяет ПРОГРАММНО (sha256/`cmp`). Отчёт: «список расхождений ПУСТ» = гейт зелёный. Любой байт-дифф → C не принят (откат). Верификатор пишет итог в `_studio/zhurnal/2026-07-10_sborka-konvejera/verifikacia-infra.md`.

## 5. ОТЧЁТ → секция ## ОТЧЁТ внизу
По каждой задаче A/B/C: что сделал + ЗАЧЕМ · как проверил (критерий по пунктам) · статус (готово / спека+стоп) · что НЕ трогал (границы, канон-стиль) · результат верификатора C (путь + байт-дифф пуст?) · **время прогона + потраченные токены** · что осталось аналитику/владельцу (регистрация в KARTA, прогон audit.py на машине с браузером). Флаг стоп-до-цели — явно: что готово, что требует владельца.

## ПЛАН — (заполняет исполнитель)

### Точка отката
`_backup_infra_20260710-043240.zip` (в этой же папке): `_generator/` + оба эталонных `dist/index.html`.
Эталон истины для byte-exact сверки — не удаляю, в main не коммичу.

**Baseline byte-exact (REFERENCE, зафиксирован ДО правок):** пересборка обоих эталонов
`build_deck.py <эталон>/src --no-banner` сейчас даёт (sha256):
`dandelin=8a62487f…`, `buffon=6adb7df0…`. Это детерминированный выход генератора =
забэкапленный `dist` минус баннер-комментарий. Гейт C = TEST-пересборка после base.css
должна дать эти же байты (`cmp` exit 0), сильнее нормализованной сверки.

### Проверенные предпосылки (по живым файлам — до кода)
1. **DESIGN.md §5 называет `{{BASE_CSS}}`/`{{GRID_CSS}}`, но живой `build_deck.py` их НЕ знает.**
   Реальные токены: `{{FONTS_CSS}} {{TOKENS_CSS}} {{ENGINE_JS}} {{OVERLAY_CSS}} {{SLIDE:*}}
   {{MD:*}} {{ILL:*}} {{CHAPTER:*}} {{DRIVER:*}} {{SIM:*}} {{THREE_LIB}}`. `base.css`/`grids.css`
   как файлов источника не существует ни у одного дека — база+грид лежат дословно в `<style>`
   `shablon.html` (подтверждает FORMAT-ISTOCHNIKA «расхождение»). Значит `{{BASE_CSS}}` надо
   ДОБАВИТЬ в `build_deck.py` — но как ОПЦИОНАЛЬНУЮ загрузку (как уже сделано для `overlay.css`
   через `load_if_present`), чтобы эталонов без `base.css` не задеть.
2. **⚠ ЛОЖНАЯ ПРЕДПОСЫЛКА (ALGORITM §2): «общий base определяется ОДИН раз, наследуется ВСЕМИ
   деками».** По живым `shablon.html` базовые CSS двух эталонов **НЕ байт-идентичны** — расходятся
   не пробелами, а по существу канона:
   - `.slide{background:#e6e5e1;color:#333}` (dandelin) ↔ `background:var(--paper);color:var(--ink)` (buffon);
   - `[data-scene-from]{transition:opacity .35s}` (dandelin) ↔ `.24s ease` (buffon);
   - разный словарь классов: `.copy/.cer/.tlist/.ph/.pad/.ill-box/.bcenter` (dandelin) ↔
     `.t-display/.t-math/.t-small/.panel/.lab-row/.sim-controls` (buffon);
   - `html,body{background:#111}` ↔ `#2b3038`; у dandelin лишняя строка `#hint{display:none}`.
   **Вывод:** ОДИН общий byte-shared `base.css` для обоих эталонов невозможен без переверстки
   одного из них = рестайл замороженного канона (KONSTITUCIYA §1) — ЗАПРЕЩЕНО. Байт-безопасен
   только **пер-дековый** `base.css` (каждый дек выносит СВОЙ base в свой `base.css`). Обещанная
   ALGORITM выгода «одна правка на все деки» пер-дековым выносом НЕ достигается — фиксирую честно.

### Порядок (безопасное → деликатное), ровно 4 стыка, ничего сверх
**A. SVGO-конфиг** (`_generator/svgo.config.mjs`): pinned SVGO + `prefixIds` по stem файла.
   Тест: `npx svgo` на `dandelin/src/illustrations/cone-title.svg` (внутр. `clipPath id="waffle"`)
   → id получает пер-файловый префикс. node v24/npx 11 в среде есть. Крит.: svgo прогоняется и
   префиксует id.

**B. `_generator/audit.py`** по контракту `10-sborka-qa/DOK.md §гейт слой 3`, паттерн 1:1 с
   `html-slides-studio/scripts/audit.py` (прочитан как якорь), НО: (1) точка входа — наш
   `dist/index.html`; (2) `ALLOWED_BG` = ПАРС `:root` из СОБРАННОГО дека (regex hex из первого
   `:root{}`), не хардкод-сет; проверки overflow `.zone`=0, shadow/radius/gradient=0, весь
   `.fit`/`.t-body` текст `#333333`, `.t-body`≥`FLOOR_PX=35`, 0 `<figcaption>`, `--scene-diff`,
   fit-группы, PASS/FAIL/WARN построчно, exit 1 при FAIL. НЕ прогоняю (нет браузера).
   Крит.: `python3 -m py_compile _generator/audit.py` проходит; py3.9-совместимо.

**C. Скелет + base.css** (деликатно, под гейтом byte-exact). Один риск за раз:
   1. **Правка `build_deck.py`** — единственная строка, трассируется к base.css: добавить
      `load_if_present("{{BASE_CSS}}", src/"base.css")` рядом с `overlay.css`. Опциональна →
      эталонов без `base.css` не меняет. ПРОВЕРКА: пересборка РЕАЛЬНЫХ `dandelin/src`,`buffon/src`
      `--no-banner` = REFERENCE-байты (гейт: правка генератора безопасна).
   2. **Пер-дековый вынос на КОПИЯХ эталонов** (scratchpad, оригинальные `src/`+`dist/` не трогаю):
      для каждого дека вырезать из `shablon.html` общий канон-base (от после `{{TOKENS_CSS}}` до
      первого пер-слайдового `#s…`-правила) в `base.css`, на его место — `{{BASE_CSS}}`. Пересборка
      копии `--no-banner` → `cmp` с REFERENCE == 0 (byte-exact, гарантирован дословным вырезом).
   3. **Скелет `_generator/skeleton/`** = стартовый `src/`: `shablon.html` (каркас канона +
      `{{BASE_CSS}}`, пер-слайдовый грид ПУСТ) · `base.css` (= канон-base dandelin дословно —
      первичный эталон §KONSTITUCIYA1) · `engine.js` (дословно) · `tokens.css` (канон-палитра) ·
      `fonts/faces.css` (дословно) · шаблон `brief.md` (пустой `slide_order`, `accent_tag`) ·
      пустые `content/ slides/ illustrations/` · `SPEC.md`. Санити: `build_deck.py skeleton --lint`
      зелёный (пустой дек валиден).
   **Гейт готовности C (может провалиться):** оба `cmp` == 0. Не byte-exact хоть у одного →
   откат всего C, детальная `_generator/skeleton/SPEC.md`, СТОП по C (A и B сохраняются).

**Верификатор C (§4):** свежий субагент, ДРУГОЙ метод — читает ОРИГИНАЛ обоих `dist` из
   zip-бэкапа, нормализует (снять комментарии, схлопнуть межтеговые пробелы), пересобирает из
   модифицированного (base.css) `src/`-копии, сверяет ПРОГРАММНО (sha256/cmp). Итог →
   `verifikacia-infra.md`. Любой байт-дифф → C не принят.

**Субагенты:** 1 (верификатор C). Рейт-лимит = отступить+доложить.

**СТОП ДО ЦЕЛИ (громко):** `audit.py` НАПИШУ по контракту, но НЕ прогоню (нет браузера) —
   прогон за владельцем. Реальные эталонов `src/` на base.css НЕ мигрирую (канон дороже фичи;
   решение — за владельцем, см. ВОПРОСЫ); `{{BASE_CSS}}` опционален, эталонов работают как есть.
   Индекс (`KARTA.md`/`SESSIYA.md`/`PLAN.md`/`RESHENIYA.md`) не трогаю — за аналитиком.

## ВОПРОСЫ — (заполняет исполнитель)

1. **Единый vs семейный `base.css` (решение владельца).** Доказано: byte-shared общий base на
   ОБА эталона невозможен — их base-CSS расходится по существу канона (рестайл запрещён §1).
   Скелет несёт `base.css` = канон-base **dandelin** (первичный эталон). Развилка:
   (а) **dandelin-base — единственный дефолт** [текущее], деки в стиле buffon (sim/math) правят
   base под себя; (б) **два семейных скелета** (dandelin-base + buffon-base); (в) **curated
   минимальное ядро** (только байт-идентичное в обоих: `*{box-sizing}`, `#stage`, `.zone`, `.fit`,
   `.fill`, `.scene-off`, каскад сцен, `deck-in`) + семейные оверлеи. Вкус — за владельцем.
2. **Мигрировать ли РЕАЛЬНЫЕ эталонов `src/` на `base.css`?** Byte-exact доказан на обоих (гейт+
   верификатор зелёные), НО это трогает замороженное канон-ХРАНИЛИЩЕ. Сейчас НЕ мигрированы
   (канон дороже фичи; `{{BASE_CSS}}` опционален — эталонов работают как есть). Регресс-выгода
   (одна правка base у эталона вместо правки внутри `shablon`) vs риск — за владельцем.
3. **SVGO снимает `preserveAspectRatio="xMidYMid meet"`** как равный дефолту SVG (render-neutral;
   `viewBox` + `width/height:100%` сохранены — оверфлоу-страховка Р10 цела). Оставить так [дефолт]
   или форсить сохранение атрибута? Мелочь; поведение идентично.

## ОТЧЁТ — (заполняет исполнитель)

**Итог: все 4 стыка закрыты. A, B, C — готово; C прошёл byte-exact гейт + независимый
верификатор (зелёный). audit.py НЕ прогонялся (нет браузера — за владельцем, заявлено громко).**

### A · SVGO-конфиг — ГОТОВО
- **Что+зачем:** `_generator/svgo.config.mjs` — pinned `svgo@3.3.2` + `preset-default`
  (`removeViewBox:false`, `cleanupIds:false`) + `prefixIds` с префиксом по **stem входного файла**
  (`foo.svg` → id `x` → `foo__x`). Зачем: несколько инлайн-SVG в одном `dist` делят namespace;
  два `id="waffle"` тихо ломают друг друга (Р10 / 09-DOK). Пер-файловый префикс — единственное
  лекарство.
- **Как проверил (критерий):** `npx svgo@3.3.2 --config … -i dandelin/src/illustrations/cone-title.svg`
  → в выходе `id="waffle"`→`id="cone-title__waffle"` И ссылка `clip-path="url(#waffle)"`→
  `url(#cone-title__waffle)` (обе обновлены согласованно); `viewBox` сохранён. Критерий
  «svgo прогоняется и префиксует id» — выполнен. node v24/npx 11 в среде есть.
- **NB:** SVGO снимает `preserveAspectRatio` = дефолт (см. ВОПРОС 3, render-neutral).

### B · `_generator/audit.py` — ГОТОВО (написан, НЕ прогнан)
- **Что+зачем:** канон-аудит слоя 3 (10-DOK), паттерн 1:1 с `html-slides-studio/scripts/audit.py`,
  адаптирован: точка входа — наш `dist/index.html`; `ALLOWED_BG` **парсится из `:root`
  СОБРАННОГО дека** (не хардкод — Р8 пер-дек оверрайд). Проверки: overflow `.zone`=0;
  shadow/radius/gradient=0; фон-плашки ⊆ палитры `:root`; весь `.fit`/`.t-body` текст `#333333`;
  `.t-body`≥`FLOOR_PX=35`; 0 `<figcaption>`; fit-группы консистентны; `--scene-diff` (аддитивность
  сцен + непустой клик). PASS/FAIL/WARN построчно, exit 1 при FAIL, WARN не валит.
- **Как проверил (критерий):** `python3 -m py_compile _generator/audit.py` — проходит (py3.9-совм.);
  функция `parse_allowed_bg` без браузера прогнана на реальных `dist` → извлекла канон-палитру
  (dandelin 19 цветов вкл. `--accent`, buffon 17). Структура проверок 1:1 контракту. **Прогон с
  браузером — за владельцем** (в песочнице генератора Playwright-браузера нет).

### C · Скелет + `base.css` — ГОТОВО, byte-exact (гейт зелёный)
- **Что+зачем:** (1) `build_deck.py` +**1 строка**: `load_if_present("{{BASE_CSS}}", src/"base.css")`
  рядом с `overlay.css` — опциональная загрузка, трассируется только к base.css. (2) Пер-дековый
  вынос общего канон-base из `<style>` `shablon.html` в `base.css` (граница = первое пер-слайдовое
  `#<id> .grid`). (3) `_generator/skeleton/` = стартовый greenfield-`src/`: `shablon.html` (каркас
  + `{{BASE_CSS}}`, пер-слайдовый грид пуст) · `base.css` (канон-base dandelin) · `engine.js` ·
  `tokens.css` · `fonts/faces.css` (всё дословно) · `brief.md`-шаблон · `content/` · `SPEC.md`.
- **Оспорена ложная предпосылка (главное):** ALGORITM §2 «общий base — ОДИН, наследуется ВСЕМИ
  деками» **ложна по живым файлам**: base-CSS двух эталонов расходится по существу канона
  (`.slide` фон #e6e5e1 vs var(--paper); transition .35s vs .24s; разный словарь классов; стейдж
  #111 vs #2b3038). Byte-shared общий base = рестайл §1 = запрещён → байт-безопасен только
  ПЕР-ДЕКОВЫЙ base. Выгода «одна правка на все деки» пер-дековым выносом НЕ достигается — зафиксировано в SPEC/ВОПРОСАХ.
- **Как проверил (критерий, который мог провалиться):**
  · C1-гейт: пересборка РЕАЛЬНЫХ `dandelin/src`,`buffon/src` `--no-banner` после правки генератора
    == REFERENCE-байты (`cmp` exit 0) — правка безопасна.
  · C2-гейт: пересборка КОПИЙ с вынесенным `base.css` `--no-banner` == REFERENCE (`cmp` exit 0
    обоих) — вынос лосслесс.
  · Скелет: `build_deck.py skeleton` — линтер зелёный (0 слайдов валидно), 0 нерезрешённых `{{…}}`,
    `base.css` подставлен (`.bcenter` есть, пер-слайдовый грид отсутствует).
- **Верификатор C (§4, свежий субагент, ДРУГОЙ метод):** истина = закоммиченный `dist` из
  ZIP-бэкапа (не пересборка); нормализация (снять комментарии+баннер, схлопнуть межтеговые пробелы);
  сверка нормализ. sha256 + `cmp`. **Оба дека совпали** (даже raw byte-for-byte до нормализации).
  Отчёт: `verifikacia-infra.md`. **Вердикт: зелёный, C принят.**

### Границы — что НЕ трогал
- Визуальный СТИЛЬ канона (KONSTITUCIYA §1) — только перенос ХРАНЕНИЯ байт-в-байт (§7).
- Реальные эталонов `src/` НЕ мигрировал (мутации — на scratchpad-копиях); реальные `dist/index.html`
  не тронуты (sha == baseline: dandelin `46cf00b3…`, buffon `9ba456a8…`); `engine.js`/`tokens.css`/
  шрифты — дословные копии. Пакеты `06…10`, `KARTA/SESSIYA/PLAN/RESHENIYA` — не трогал.
- `build_deck.py` — ровно 1 добавленная строка (`{{BASE_CSS}}`); `load_dir`/линтер не менял.

### Что осталось владельцу / аналитику
- **Владельцу:** прогнать `audit.py` на машине с браузером (`python3 _generator/audit.py <дек>/dist/index.html`).
  Ответить на 3 ВОПРОСА (единый vs семейный base; миграция эталонов; preserveAspectRatio).
- **Аналитику:** регистрация новых файлов в `KARTA.md` — `_generator/{svgo.config.mjs,audit.py,skeleton/}`;
  консолидация индекса/дневника. Скелет готов к «шагу 0» Фазы II (гейт «многогранники»).
- **Точка отката:** `_backup_infra_20260710-043240.zip` (не удалять — эталон истины). В main не коммитил.

### Время + токены
- Прогон: один заход, ~20 мин (старт бэкапа 04:32). Субагент-верификатор: **51 517 токенов** (замерено),
  ~73 с. Токены основного прохода инструментом точно не замеряются в этой среде; оценка — крупная
  доля ушла на чтение якорей (два `shablon.html` ~880 строк, генератор, контракты). Метрика захода —
  КАЧЕСТВО; все гейты зелёные.
