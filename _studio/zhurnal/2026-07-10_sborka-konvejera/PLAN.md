# ПЛАН арки «сборка конвейера»

> 🔒 АРКА ЗАКРЫТА. Работа в папке не идёт с **2026-07-10** (последний коммит в неё; дата данных 2026-07-28 — проверяется командой `git log -1 --date=short -- <папка>`).
> Durable уехал по домам при закрытии (`../../docs/kak-delat/ARKA.md §7`); остальное здесь — СЛЕД того, как шли, а не документ, который ищут в индексе.
> ⇒ внутренности папки **не судятся воротами 5** `check_kartoteka.py` (сама папка обязана стоять в `../../docs/KARTA.md §6`).
> Работа в арке возобновилась — снять эту отметку тем же ходом.

## ⭐ СЕЙЧАС
**✅ ДОВОДКА ВЫПОЛНЕНА (2026-07-10, эта сессия) — фабрика «ПОЧТИ ГОТОВА».** Волны A/B/C закрыты и приняты по гейтам: (A) инфра — `base→buffon` byte-exact + `doc-движок` `build_doc.py`+двухвкладочное демо (D1 закрыт показом); (B) редизайн Фазы I `konvejer/01–05` — карточный граф (теги котлов) → skelet → нить→rasskaz → сборка → раскадровка, скиллы внутрь (Р17), 4 развилки ratified; (C) `kurs-fabrika` абсорбирована (→ `PROFILI-CIFERBLATY.md`, `PROGRAMMA-KURSA.md`) и УДАЛЕНА (бэкап в арке, верификатор GREEN). Решения — Р19/Р20; Р5 пересмотрено. **Осталось до полной готовности:** реальный прогон #14 (многогранники end-to-end) — СЛЕДУЮЩАЯ сессия; мелочь — 06/09 внешние термины (Фаза II). История сессии — хвост `SESSIYA.md`.

---

**Фаза II закрыта на уровне алгоритма + склейки + решений.** Что есть в `../../konvejer/`: 5 пакетов 6–10 (`DOK.md`+`ZAHOD.md`+`PRIMERY.md`), `FORMAT-ISTOCHNIKA.md` (контракт хранения — выход фабрики), `ALGORITM.md` (управляющая линия: driver-модель, вход-стык с Фазой I, линия 6→10 с гейтами/развилками/чекпоинтами, стыки с архитектурой), `00-KONVEJER.md` (карта). Верификация (`verifikacia-paketov.md`): 0 битых ссылок, 10/10 цитат реальны, нюансы не потеряны, независимо прогнан линтер + byte-exact пересборка; точечные баги (data-scenes, render.py --only, .t-small, ## 4) дочищены. Критерии приёмки владельца — (1) склейка+правильный формат и (2) чёткий алгоритм — закрыты; доказано на 2 эталонах.

**Одобрено владельцем (2026-07-10):** направление `base.css` + пустой greenfield-скелет `src/` как render-identity-preserving перенос замороженного канона (стиль не меняется, KONSTITUCIYA §1/§7). **Сборка 4 инфра-стыков — отдельной фокус-сессией** (не в этом чате — контекст забит).

**Ночные прогоны ПРИНЯТЫ (2026-07-11):** инфра готова (`../../../_generator/{skeleton,base.css,audit.py,svgo.config.mjs}`, byte-exact зелёный); пакеты Фазы I 01–05 — черновики приняты. Решения осели: **Р15** (семейство движков), **Р16** (база канона = buffon), **Р17** (автономность — скиллы ВНУТРЬ, не в прод), **Р18** (лекция = курс, одна фабрика). Движки — `../../../_generator/DVIZHKI.md`; формат текста grounded в `catalan`/`kurs leto` — `distillat-format-teksta.md`.

**⭐ РЕДИЗАЙН ФАЗЫ I** (брейншторм 2026-07-11, `../../docs/spravka/FAZA-1-REDESIGN.md`): ресёрч и нарратив = РАЗНЫЕ сессии; интервью = лёгкая разведка (не решение «тянет ли математика»); ресёрч = ДВА документа-котла (математика ‖ научпоп-контекст, каждый со своим критерием); нарратив = отдельная сессия (тащим ЛИНИЮ из котла); лекция=курс. Пакеты 01–05 переделать под это; `distillat-reserch-matbaza` частично устарел (ранний нарратив убрать).

**➡️ ЦЕЛЬ СЛЕДУЮЩЕЙ СЕССИИ (НОВЫЙ чат) — довести фабрику до «ПОЧТИ полностью готовой». Это НЕ реальный прогон (он — ПОЗЖЕ, отдельной сессией).** Большая работа, вероятно серия заходов Claude Code.
Войти по арк-ритуалу + прочитать `FAZA-1-REDESIGN.md`, `DVIZHKI.md`, Р15–Р18. Сделать ПЛАН, затем заходы на:
1. **Полностью разработать верхнюю часть Фазы I** (интервью-разведка → ресёрч-котёл (два документа) → нарратив-линия) по `FAZA-1-REDESIGN.md`; переделать пакеты 01–05 (#12). Это ядро.
2. **Внести скиллы ВНУТРЬ (Р17):** `popsci-research`/`popsci-narrative`/`the-interviewer`/`math-russian-terminology` → разобрать на куски, разложить конкретными шагами; термины → per-project документ-список.
3. **АБСОРБИРОВАТЬ `kurs-fabrika` (#15)** — заброшенный старт-кит «фабрика курсов» (`../../../../kurs-fabrika/`: PLAYBOOK = методология фаз/профилей/**циферблатов масштаба**, CHECKLIST Ф0–Ф8, shablon, заход «программа курса»). Взять курс-уровневую методологию в `_studio` (Р18: лекция=курс, циферблат масштаба), потом папку УДАЛИТЬ (лоссово → бэкап+верификатор, удаление гейтится). Наши процесс-доки (ARKA/RUKOVODSTVO/COWORK) уже эволюционнее — брать ДЕЛЬТУ про курсы, не дубли.
4. **Мелочи Фазы II:** doc-движок (#11), база скелета → buffon (#13).
Когда фабрика «почти готова» — сессия заканчивается. Реальный прогон (концепт курса ≈8 лекций → слайды первых 4, #14) — уже СЛЕДУЮЩАЯ за ней сессия.

## 🔧 ПЛАН ДОВОДКИ — ратифицирован 2026-07-10 (эта сессия; Cowork ведёт заходами, не хардкодит руками)

**D1 (линчпин) — снят.** Владелец делегировал структуру мне. Источник истины = markdown, легко править (Р15); doc-движок рендерит ОДИН HTML с ДВУМЯ вкладками (математика ‖ научпоп) как вид стадии-КОТЛА (нелинейный склад сюжетов); нарратив-стадия добавляет `rasskaz`. Файловую структуру проектирую я, показываю ДЕМО в #11 (закрытие развилки показом, а не абстрактным выбором). Р5 «один гибрид-файл» → superseded, переписать при закрытии.

**Область (владелец, 2026-07-10):** Cowork держит стратегию/документацию и НЕ делает конкретику руками — всё через заходы (Claude Code / general-purpose субагенты с инлайн-ролью), отчёты — дистилляты, контекст Cowork чистый. Цель сессии — закрыть обсуждённые вопросы, получить рабочую новую фабрику, избавиться от `kurs-fabrika`, закрыть Фазу I. Best-effort («если получится — идеально»).

**Волна A · инфра (параллельно, независимо от развилок):**
- З-1 `kod_base-buffon.md` (#13, Sonnet, ПЕРЕНОС) — дефолт скелета dandelin→buffon. Гейт: скелет-base воспроизводит buffon byte-exact; dandelin-эталон не сломан.
- З-2 `kod_doc-dvizhok.md` (#11, opus, ПЕРЕНОС из build.py по дистилляту) — движок 2 (поток+поля, `status`-гейт, `.mn`/`.insight`/`.foot`, мат-долг `⚑`, KaTeX-вид, token-free) + ДЕМО двухвкладочного котла (закрывает D1). Гейт: открытый `⚑` при `status: polno` → exit 1; закрыли → самодостаточный двухвкладочный HTML.

**Волна B · ядро — редизайн верхней Фазы I (#12) + скиллы внутрь (Р17), после #11:**
- З-3 01-brief (интервью=разведка; лекция/курс Р18; поля register+word_budget; строгость явной MC; контракт-возврат throughline; `the-interviewer` внутрь).
- З-4 02-reserch+03-matbaza (ДВА котла математика‖научпоп со своими критериями; `⚑`-долг; `proof-self-review` допущен; no-go=чекпоинт; один сеанс 2→3; `popsci-research` + термины→per-project список внутрь).
- З-5 нарратив+04+05-раскадровка (линия из котла→`rasskaz`; 04=сборка по D1; 05=дешёвый doc-движковый вид A5/A6; `popsci-narrative` внутрь).

**Волна C · абсорбция kurs-fabrika (#15) — лоссово, удаление гейтится:**
- З-6 аналитик-субагент → delta-report курс-методологии (профили, циферблаты масштаба лекция↔курс, CHECKLIST Ф0–Ф8, «программа курса»), которой нет у нас.
- З-7 (Cowork) приземлить дельту в `_studio` (операционализирует Р18) + KARTA.
- З-8 верификатор (свежий, другой метод) — список потерь ПУСТ.
- З-9 бэкап + удалить репо (по явному go владельца).

**Закрытие:** RAZVILKI→RESHENIYA (Р19…), правка Р5; обновить устаревший борд `OTKRYTYE-ZADACHI`, KARTA §6, строка в `SVODKI`, PLAN «СЕЙЧАС».

## Шаги
- [x] 4 дистиллята → на диск (`distillat-{verstka-kanon-qa,text-stil,reserch-matbaza,illustracii}.md`)
- [x] арк-файлы Фазы II (6–10) как DOK → `../../konvejer/` + `00-KONVEJER.md`; KARTA §1/§2/§6
- [x] `FORMAT-ISTOCHNIKA.md` — контракт хранения дека (расхождение DESIGN.md base.css/grids.css помечено)
- [x] пакеты 6–10: `ZAHOD.md` (промт под Claude Code) + `PRIMERY.md` (живые примеры) на каждый шаг
- [x] верификация пакетов (свежий агент, живой код) → `verifikacia-paketov.md`; точечная дочистка применена
- [ ] обкатать конвейер end-to-end на «многогранниках» (гейт готовности)
- [ ] GREENFIELD Фаза I (1–5) пакетами
- [ ] инфраструктура: `_generator/audit.py`, SVGO-конфиг, math-арк (Р7), tokens (Р8), `base.css`-решение

## Гейт готовности фабрики
Собрать НОВЫЙ дек из готового содержания одной из завтрашних лекций (напр. многогранники) end-to-end и получить самодостаточный HTML канона.
