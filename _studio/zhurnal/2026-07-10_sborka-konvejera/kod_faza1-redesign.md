# Канал Claude Code — редизайн верхней Фазы I (#12) + скиллы внутрь (Р17)

> Единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО и ПОЛНОТА.
> **Модель:** opus — связный редизайн 5 пакетов с вкусовыми развилками.
> ОДИН агент, БЕЗ распараллеливания записи пакетов (контракты 01→05 связны — пишет один исполнитель подряд, как в ночном `kod_faza1`).

## ЗАДАЧА (один длинный проход)

КОНТЕКСТ. Фабрика курсов `materials/_studio`. Пакеты Фазы I `konvejer/01-brief … 05-raskadrovka` — ЧЕРНОВИКИ по СТАРОМУ блюпринту («HTML с этапа 2, один гибрид-файл, ранний нарратив»). Редизайн (`FAZA-1-REDESIGN.md` + решения владельца ниже) их отменяет. ЦЕЛЬ: переписать 01–05 под новую модель + внести содержимое скиллов ВНУТРЬ (Р17). Приёмка — по отчёту + гейту связности контрактов.

⚠️ СТОП-ФЛАГ: это СПЕКИ процесса (пакеты `DOK`/`ZAHOD`/`PRIMERY`), НЕ прогон реальной лекции. Вкусовую развилку, не закрытую явно в этом заходе, — ФЛАГ владельцу в `## ВОПРОСЫ` + пометка в самом пакете, НЕ решай молча. Реальный вкус проверится на «многогранниках» (следующая сессия).

### 0. ПЕРВЫЙ ХОД
Рабочая папка `/Users/ivanyakovlev/Documents/GitHub/materials`. Пути: Read/Grep/Glob — host `…/materials/…`; bash — mount `/sessions/practical-exciting-ritchie/mnt/materials/…`.
**Git НЕ покрывает `_studio/` (untracked) → СРАЗУ tar-снапшот** папки `_studio/konvejer/` в `_studio/zhurnal/2026-07-10_sborka-konvejera/snap_faza1_pre.tar` (не zip). Прочитай ТОЛЬКО:
- ЦЕЛЬ-МОДЕЛЬ: `_studio/docs/spravka/FAZA-1-REDESIGN.md`;
- ГРУНТОВКА КАРТОЧЕК: `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-catalan-kartochki.md` (карточный граф Каталана: узел/карта/нить/дисциплина; ВАЖНЫЙ нюанс — `rasskaz‖skelet` у Каталана = пост-нарративный разрез, ≠ пред-нарративный «математика‖научпоп»);
- ФОРМАТ ТЕКСТА: `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-format-teksta.md` (слои `rasskaz`/`skelet`/`statya`, `.insight`/`.mn`/`.foot`, мат-долг `⚑`, гейт-против-воды);
- УСТАРЕВШЕЕ (сверить, что выкинуть): `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-reserch-matbaza.md` (ранний нарратив — убрать);
- ДВИЖОК ВИДА (готов): `_generator/build_doc.py` + `_generator/doc-demo/` (движок 2: поток+поля+`status`+`⚑`+двухвкладочный вид — пакеты Фазы I опираются на него для авторского вида);
- РЕШЕНИЯ: `_studio/docs/pochemu-i-videnie/RESHENIYA.md` (Р13, Р15, Р17, Р18);
- ОТВЕТЫ ПО РАЗВИЛКАМ (энкодить): `_studio/zhurnal/2026-07-10_sborka-konvejera/RAZVILKI.md`;
- ТЕКУЩИЕ ЧЕРНОВИКИ: `_studio/konvejer/{01-brief,02-reserch,03-matbaza,04-gibrid-istochnik,05-raskadrovka}/{DOK,ZAHOD,PRIMERY}.md`;
- СТЫК ВНИЗ: `_studio/konvejer/FORMAT-ISTOCHNIKA.md` + `_studio/konvejer/06-tekst/DOK.md` (контракт 5→6: `register`+`word_budget`+`accent_tag`+`slide_order`);
- ИНДЕКС для регистрации: `_studio/konvejer/00-KONVEJER.md` + `_studio/docs/KARTA.md`.
Изложи ПЛАН в `## ПЛАН` перед правкой (по пакетам: что меняешь, контракты, какие развилки флагаешь).

### 1. ДИСЦИПЛИНА
Grounded, НЕ изобретай (формат — из Каталана/дистиллятов). Р17-метод внесения скилла: перенести содержимое многословно → выкинуть лишнее/чужую цель → свести к коротким конкретным шагам процедуры. Развилку не закрывай молча — флаг в `## ВОПРОСЫ` + пометка в пакете. Хирургия: рабочий текст пакетов ради красоты не переписывай. CJK/mojibake 0.

### 2. ГРАНИЦЫ
Правишь ТОЛЬКО `_studio/konvejer/01-brief … 05-raskadrovka` (+ при нужде НОВЫЙ пакет нарратива — см. задачу) + строки в `00-KONVEJER.md`/`KARTA.md §6`. НЕ трогай: `06–10` (готовы), `_generator/` (движки готовы), канон, эталоны. Интернет — нет.

### 3. ЗАДАЧА — переписать 01–05 под новую модель

**Сквозная модель Фазы I (энкодь во все пакеты):**
Интервью-разведка (01) → РЕСЁРЧ = **карточный граф** (02): узлы-стикеры (формат Каталана `id·род·находка·связи·уровень·источник·статус`, 5–10 строк, дисциплина «находка без карточки = потеряна») + карта связей + нити; карточки **тегируются котлом `математика|научпоп`** — это ОДИН граф, а «два котла» = два тег-фильтра, показанные двухвкладочным doc-движковым видом (D1). Критерии котлов: математика — «лекцию можно заменить набором вопросов, слушатель сам переизобретёт»; научпоп — «каждый факт = законченная мини-история». → Строгая матбаза (03) = слой `skelet` (Определение/Утверждение/Доказательство/Источник/Уровень), растит доказательства; мат-долг `⚑ Флаг`→`Флаг закрыт` (A3), закрывает и снимает арка 3 (B11). → НАРРАТИВ (отдельная стадия) = вытащить НИТЬ из графа → `rasskaz` (5 актов, process-first), добор ресёрча где провисает (`popsci-narrative` внутрь). → 04 = сборка вида источника (`rasskaz`+`skelet`, сшивка цитатой-сноской; НЕ переусложнять — владелец: котёл черновой, всё переписывается); гейт «источник закрыт» = `grep ⚑` + автор-чекпоинт (B10). → 05-раскадровка = дешёвый doc-движковый вид (A5 гибрид-таблица+`data-*`, A6 `slides/*.html`+`index.html`), несёт контракт 5→6.

**Пер-пакет:**
- **01-brief:** интервью = ЛЁГКАЯ РАЗВЕДКА (тема·аудитория·формат · есть ли тексты/читал ли кто такие лекции · **лекция ИЛИ курс** Р18), НЕ решение «тянет ли математика» (это ресёрч). Формат интервью — гибрид (A1: логистика блоком с дефолтами; throughline и «что резать» — по одному). Строгость — явная MC каждый раз (A2: интуитивный / выкладки в `<details>` / полностью строгий; спектр спецкурс↔научпоп). Поля `register`+`word_budget` (A4: `устный` — текст рассказа НЕ ограничен, лимит только на slide-text; `читаемый` — слайд несёт весь довод). Контракт-возврат throughline (B14). Содержимое `the-interviewer` — внутрь (Р17). Курс-режим (Р18): если курс — сперва концепт курса (лекции/сюжеты/порядок/мосты), потом пер-лекция того же большого документа.
- **02-reserch:** ресёрч широко, БЕЗ навязанного сюжета → карточный граф (грунтовка Каталана — перенеси формат узла/карты/нити/дисциплину). Два котла `математика‖научпоп` = теги над одним графом. Источники/якорь-книги (biblioteka-модель) → дайджест → карточка. `popsci-research` содержимое внутрь. Термины (Р17): per-project `TERMINY.md`-список (образец `catalan/spravochnik/TERMINY-russkie.md`) + шаг «сверься», НЕ скилл-триггер. no-go «тема не тянет» = чекпоинт владельцу (B8). Один сеанс 2→3 без обнуления (B9 — в `ZAHOD` явной инструкцией запуска).
- **03-matbaza:** слой `skelet`, полные доказательства, шаблон Каталана. Гейт «математика верна» — метод `proof-self-review` ВНУТРЬ (Р17 ужесточает Р3: внешние только код+3D). ⚠ РАЗВИЛКА (флаг владельцу): внести adversarial-переreview как ШАГИ внутрь vs разрешить внешний `proof-self-review` как исключение (B7 говорил «разрешить», Р17 позже сузил — конфликт). Рекомендация: метод внутрь, шагами. Мат-долг закрывает и снимает арка 3 (B11).
- **04 (источник/сборка):** `rasskaz`+`skelet` сшиты сноской-цитатой; НЕ переусложнять. 🖼-слой свести с `09-illustracii/DOK.md` (`<div class="ph">`+число сцен, B12). Гейт «источник закрыт» = `grep ⚑`+автор-чекпоинт (B10). Стиль источника — журнальная проза, отдельный регистр от плакатной арки 6 (B13). ⚠ РАЗВИЛКА (флаг): нарратив как ОТДЕЛЬНЫЙ пакет (напр. `04-narrativ/`, тогда renumber) vs подраздел 04 — реши и обоснуй.
- **05-raskadrovka:** дешёвый doc-движковый вид; A5 гибрид (таблица-канон + производные `data-*`); A6 (отдельные `slides/*.html` + `index.html` prev/next — совпадает с форматом Фазы II, бесшовно). Несёт `register`+`word_budget` в контракт 5→6.

КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):
(a) все 5 пакетов переписаны, контракты цепочки согласованы (01→02→03→04→05→06: выход = вход следующего; 5→6 несёт `register`+`word_budget`+`accent_tag`+`slide_order`);
(b) карточный граф (02) grounded в `distillat-catalan-kartochki` (узел/карта/нить/дисциплина), НЕ изобретён; двухвкладочный котёл опирается на `build_doc.py`;
(c) наследие «ранний нарратив / один поток» из `distillat-reserch-matbaza` УБРАНО (нарратив — отдельная стадия);
(d) скиллы внесены по Р17 (popsci-research→02, popsci-narrative→нарратив, the-interviewer→01, термины→per-project список+шаг); НЕ осталось «позови скилл X» внешним триггером (кроме флагнутого `proof-self-review`);
(e) КАЖДАЯ вкусовая развилка (proof-self-review внутрь-vs-внешний; тег-карточки-vs-два-набора; нарратив-пакет-vs-подраздел) — ФЛАГ в `## ВОПРОСЫ` + пометка в пакете, НЕ решена молча;
(f) 0 битых ссылок, CJK/mojibake 0, новое зарегистрировано в `00-KONVEJER` + `KARTA §6`.

### 4. САМОПРОВЕРКА
`grep`/линки: контракты цепляются, ссылки резолвятся, устаревшее убрано. Список флагнутых развилок — в отчёт.

### 5. ОТЧЁТ → секция `## ОТЧЁТ`
Что переписал по пакетам + ЗАЧЕМ; как лёг карточный граф; что внесено из скиллов и как ужато (Р17); список ФЛАГНУТЫХ развилок владельцу; что НЕ трогал; пробелы; **время+токены**. Дистиллят, не вываливай пакеты целиком.

## ПЛАН
**Снапшот:** `snap_faza1_pre.tar` снят ПЕРВЫМ ходом (512 КБ, `zhurnal/2026-07-10_sborka-konvejera/`). ✓

**Сквозной сдвиг формата (grounded в build_doc.py + doc-demo + Р15):** верхняя Фаза I больше НЕ «растущий HTML+MathJax с шага 3» (это часть отменённого one-stream-дизайна `distillat-reserch-matbaza`). Теперь — **растущие markdown-потоки doc-движка** (`build_doc.py`, вид `view.html`): фронтматтер `tab/status/registr/poryadok`, `> поле:уровень|mn|insight|foot`, `⚑ Флаг`→`Флаг закрыт`, `🖼 …{N}`. KaTeX-CDN — авторский рантайм-ВИД (Р7, допустим только в doc-движке, не в финал-деке). Два котла = `matematika.md ‖ naupop.md` (демо `_generator/doc-demo/`), CSS-вкладки D1.

**Пер-пакет (цепочка 01→05, один проход, без распараллеливания):**
- **01-brief:** интервью = ЛЁГКАЯ РАЗВЕДКА. Добавляю поля «есть ли тексты/читал ли кто такие лекции» и «формат события (устный доклад/текст для чтения)» → сигнал регистра (A4-семантика: устный=лимит только на slide-text; читаемый=слайд несёт весь довод), но числовой `register`+`word_budget` финализирует 05 (контракт 5→6). Явно снимаю «решение тянет-ли-математика» (это ресёрч). Р18 курс-режим (концепт курса→пер-лекция). A1 гибрид, A2 явная MC. the-interviewer уже внутри — оставляю, чищу.
- **02-reserch:** ПЕРЕПИСЬ. Ресёрч широко без сюжета → **карточный граф Каталана** (STANDART-uzla: `id·род·находка·связи·уровень·источник·статус`, 5–10 строк; лёгкие «объекты-ручки`; KARTA-OBLASTI резолвит id; нить=упорядоченный маршрут; дисциплина «находка без карточки=потеряна», авто-хук, id стабилен, понижать-не-удалять, честность-развилки). Котлы `математика‖научпоп` = ТЕГ над одним графом, два-вкладочный doc-вид. Критерии котлов внесены. biblioteka→дайджест→карточка. popsci-research внутрь. Термины=per-project `TERMINY.md` (образец `catalan/spravochnik/TERMINY-russkie.md`)+шаг «сверься» (Р17, НЕ скилл). no-go=чекпоинт (B8). Сеанс 2→3 без обнуления (B9). УБИРАЮ ранний нарратив (throughline+акты-скелет в арке 2) и «один HTML». Флаг: тег-vs-два-набора.
- **03-matbaza:** слой `skelet` (Определение/Утверждение/Доказательство/Источник/Уровень, шаблон Каталана) — математика-котёл, растит доказательства; критерий «лекцию можно заменить набором вопросов». Мат-долг = `⚑ Флаг`→`Флаг закрыт` (A3 в markdown-виде), закрывает и снимает арка 3 (B11). proof-self-review — метод ВНУТРЬ шагами (Р17); термины=TERMINY-список (НЕ внешний math-russian-terminology — Р17 убрал его из допущенных). Флаг: proof-self-review внутрь-vs-внешний.
- **04-gibrid-istochnik:** две под-стадии. **4a НАРРАТИВ** (отдельная стадия/сессия): вытащить НИТЬ из графа → `rasskaz` 5 актов process-first, добор ресёрча где провисает (popsci-narrative внутрь). **4b СБОРКА**: `rasskaz`(видимый)+`skelet` сшиты цитатой-сноской (модель Каталана/формата §5: два параллельных документа, сшитых цитатой — НЕ один файл с `<details>`; не переусложнять). 🖼-слой сведён с `09-illustracii` (`<div class="ph">`+N сцен, B12). Гейт «источник закрыт»=grep ⚑+автор-чекпоинт (B10). Регистр источника=журнальная проза (B13). Флаг: нарратив отдельный-пакет-vs-подраздел (дефолт: под-стадия 4a, без renumber — 06–10 заперты); мелкий флаг: сшивка-цитатой-vs-`<details>`.
- **05-raskadrovka:** дешёвый doc-вид; A5 гибрид (таблица-канон+`data-*`), A6 (`slides/*.html`+`index.html` prev/next). Контракт 5→6 несёт `register`+`word_budget`+`accent_tag`+`slide_order`. Вход = собранный источник rasskaz+skelet (не «три слоя HTML»). Наименее тронут.

**Индексы:** 00-KONVEJER §таблица Фазы I + note редизайна + строка внешних скиллов (Р17); KARTA §6 note что Фаза I пересобрана под редизайн. Renumber НЕ делаю.

**Сиды репойнчу:** 02/03→`distillat-catalan-kartochki`+`distillat-format-teksta`+`FAZA-1-REDESIGN`; 04→`FAZA-1-REDESIGN`+`distillat-format-teksta` (arka4-сид помечу superseded для `<details>`-ветки); 05→`distillat-arka5`(валиден для архетипов)+`FAZA-1-REDESIGN`. `distillat-reserch-matbaza` помечаю УСТАРЕВШИМ, снимаю как первичный сид 02/03.

## ВОПРОСЫ
Флагнутые вкусовые развилки владельцу (НЕ решены молча; дефолт взят, помечен в самом пакете):

1. **proof-self-review внутрь-vs-внешний (03-matbaza).** КОНФЛИКТ: B7 рекомендовал «разрешить как допущенный внешний»; Р17 ПОЗЖЕ сузил допущенные внешние до код+3D (термины и всё прочее — внутрь). **Дефолт (взят):** метод adversarial-переreview ВНУТРЬ, короткими шагами (холодная пере-читка рецензентом + свежий субагент-рецензент другим проходом), внешний `proof-self-review` НЕ звать. Помечено в `03/DOK.md §Открытые вопросы` и `03/ZAHOD.md`. Owner: подтвердить внутрь ИЛИ вернуть B7-исключение (тогда добавить в Р3/Р17 список).
2. **Карточки: тег-над-одним-графом vs два-набора (02-reserch).** Из `distillat-catalan-kartochki §6`: либо (а) один граф + поле-тег `котёл` на карточке + два тег-фильтра, либо (б) два параллельных набора карточек с одной картой-индексом. **Дефолт (взят):** (а) один граф, `котёл: математика|научпоп` как поле карточки, две вкладки = два фильтра (совпадает с doc-демо `matematika.md‖naupop.md`). Помечено в `02/DOK.md`. Owner: подтвердить (а) ИЛИ выбрать (б).
3. **Нарратив: отдельный пакет vs под-раздел 04.** Отдельный пакет `04-narrativ/` требует renumber 04→05→…, каскад в ЗАПЕРТЫЕ 06–10 — дорого и вне границ захода. **Дефолт (взят):** нарратив — отдельная СТАДИЯ/сессия/гейт, но housed как под-стадия **4a** внутри `04-gibrid-istochnik/` (без renumber, 06–10 целы). Помечено в `04/DOK.md`. Owner: оставить под-стадией ИЛИ санкционировать отдельный пакет с полным renumber отдельным заходом.
4. **(мелкий) Сборка источника: сшивка-цитатой-сноской vs один файл с `<details>` (04).** `distillat-format-teksta §5` фиксирует как развилку: эталон Каталана = два параллельных документа, сшитых цитатой (skelet никогда не рендерится, видимый слой цитирует сноской); старый `distillat-arka4` предлагал `<details>` внутри одного HTML. **Дефолт (взят):** сшивка цитатой-сноской (grounded, «не переусложнять»). `<details>`-ветка помечена как альтернатива. Owner: подтвердить.

Прочие (не вкусовые, наследие — закрыты дефолтом по RAZVILKI, отмечаю для полноты): A1 гибрид-интервью · A2 явная MC строгости · A3 мат-долг=`⚑ Флаг` (markdown-native, эволюция A3-Б) · A4 регистр+бюджет · A5 гибрид-запись · A6 slides+index · B8 no-go чекпоинт · B9 один сеанс 2→3 · B10 grep+чекпоинт · B11 арка3 снимает долг · B12 свод 4→9 · B13 отдельный регистр источника · B14 контракт возврата throughline.

**Вне границ (не тронул, gap для след. захода):** `06-tekst/DOK.md` и `09-illustracii/DOK.md` всё ещё зовут `math-russian-terminology` как ВНЕШНИЙ скилл — противоречит Р17 (термины→per-project список). Пакеты 06–10 заперты этим заходом; пометил в `00-KONVEJER`. Требует отдельной правки Фазы II.

## ОТЧЁТ
**Снапшот:** `snap_faza1_pre.tar` (512 КБ) снят ПЕРВЫМ ходом до правок. ✓
**Гейт готовности:** все критерии (a)–(f) зелёные (проверено грепом/резолвом ссылок, ниже).

**Что переписано по пакетам (+ зачем):**
- **01-brief** (DOK/ZAHOD/PRIMERY): интервью = ЛЁГКАЯ РАЗВЕДКА. Расширил 7→**8 полей** (добавил «материал: есть ли тексты/читал ли кто такие лекции» и «формат события+лекция/курс»). Явно снял «решение тянет-ли-математика» (→ ресёрч). throughline теперь КАНДИДАТ-гипотеза (B14). Р18 курс-режим (концепт курса→пер-лекция). Регистр (A4): формат события — сигнал здесь, число финализирует 05. the-interviewer/brainstorming — внутрь (уже были, вычистил). Зачем: старый 01 решал слишком много и не нёс разведочных полей редизайна.
- **02-reserch** (полная перепись): ресёрч широко без сюжета → **карточный граф Каталана**. Внёс STANDART-узел (`id·род·котёл·находка·связи·уровень·источник·статус`), объекты-ручки, KARTA-OBLASTI, нить, дисциплину («находка без карточки=потеряна», авто-хук, id-стабилен, понижать-не-удалять, честность-развилки). Котлы `математика‖научпоп` = ТЕГ над одним графом, критерии внесены, двухвкладочный doc-вид (build_doc.py, demo). biblioteka→VYCHITANO→карточка. popsci-research внутрь. Термины=per-project `TERMINY.md`. no-go=чекпоинт (B8), сеанс 2→3 (B9). **Убрал** ранний throughline+акты-скелет и «один HTML» (наследие).
- **03-matbaza** (перепись): слой **`skelet`** (Определение/Утверждение/Доказательство/`>поле:источник`/`>поле:уровень`, шаблон Каталана) = математика-котёл, растит доказательства из логичных вопросов (критерий «слушатель переизобретёт»). Мат-долг → `⚑ Флаг`→`Флаг закрыт` (markdown-native, эволюция A3-Б), снимает арка 3 (B11). proof-self-review — метод ВНУТРЬ шагами (Р17). Термины=`TERMINY.md`, внешний `math-russian-terminology` УБРАН (Р17). Формат — markdown doc-движок, не «HTML+MathJax-only».
- **04-gibrid-istochnik** (перепись, две под-стадии): **4a НАРРАТИВ** (отдельная стадия) — вытащить НИТЬ графа → `rasskaz` 5 актов process-first, добор ресёрча, popsci-narrative внутрь. **4b СБОРКА** — `rasskaz`(видимый)‖`skelet`(строгий) сшиты **цитатой-сноской** (модель Каталана §5, НЕ `<details>`; не переусложнять). Нюанс разреза: `rasskaz‖skelet`=ПОСТ-нарративный ≠ котлы арки 2 (grounded, не путать). 🖼 сведён с 09 (`род·смысл·{N}`→`<div class="ph">`, B12). Гейт=grep`⚑`+автор-чекпоинт (B10). Журнальный регистр (B13).
- **05-raskadrovka** (хирургия): вход обновлён на `rasskaz‖skelet`+нить (не «три слоя HTML»), бриф 8 полей, doc-вид. A5/A6/контракт **5→6 (register+word_budget+accent_tag+slide_order)** уже были верны — сохранены.

**Как лёг карточный граф:** узел-STANDART + объекты-ручки + KARTA-OBLASTI (резолвит id) + нить (маршрут, тянет арка 4a) — перенос 1:1 из `distillat-catalan-kartochki`, НЕ изобретено. Разрез на котлы решён тегом `котёл` на карточке (дефолт (а)), два тег-фильтра = две doc-вкладки (совпало с `doc-demo/matematika.md‖naupop.md`). Нарезку нитей не форсирую (каталог-без-нитей нормален). Grounded нюанс: пред-нарративные котлы (арка 2) отделены от пост-нарративного rasskaz‖skelet (арка 4).

**Внесено из скиллов (Р17) и как ужато:** the-interviewer→01 (one-question+дефолт, Phase0, assumption-check; выкинут Step2 full-spec/калибровка/диспетчер). popsci-research→02 (5–15 заходов, ≥5 источников, находка-со-ссылкой; выкинута опциональность «черновик-для-себя»). popsci-narrative→04a (throughline-тест, Story Spine, «из-за этого», small-big-small, imperfect-puzzle, закольцовка; как чек-лист, не цепочка вызовов). proof-self-review→03 (пере-читка рецензентом+численная сверка+субагент-рецензент, шагами). math-russian-terminology→per-project `TERMINY.md`+шаг «сверься» (образец `catalan/…/TERMINY-russkie.md`). Метод везде: перенести многословно→выкинуть чужую цель→короткие шаги. 0 живых внешних вызовов осталось (кроме флагнутого proof-self-review).

**СПИСОК флагнутых развилок владельцу (4, в `## ВОПРОСЫ`; дефолт взят + помечен в пакете, НЕ решено молча):**
1. proof-self-review внутрь-vs-внешний (03) — КОНФЛИКТ B7↔Р17; дефолт: внутрь.
2. карточки тег-над-графом vs два-набора (02) — дефолт: (а) тег.
3. нарратив отдельный-пакет vs под-стадия 4a (04) — дефолт: 4a (без renumber, 06–10 целы).
4. (мелкая) сборка сшивка-цитатой vs `<details>` (04) — дефолт: цитата-сноска.

**Самопроверка (грепом):** 0 битых ссылок (71 уникальный relative-токен, 17 файлов); CJK/mojibake **0**; цепочка контрактов сцеплена (01→2→3→4→5→Фаза II, next-поля совпадают); 5→6 несёт все 4 поля; build_doc.py grounded в 02–05; 0 живых скилл-вызовов. Индексы: 00-KONVEJER (таблица+note+строка Р17) и KARTA §6 обновлены; renumber НЕ делал; новых konvejer-файлов не создавал (TERMINY — per-lecture, не konvejer-док).

**Что НЕ трогал:** 06–10, `_generator/*`, канон, эталоны, ALGORITM, FORMAT-ISTOCHNIKA, интернет.
**Пробелы (вне границ):** `06-tekst/DOK.md` и `09-illustracii/DOK.md` всё ещё зовут внешний `math-russian-terminology` (противоречит Р17) — пакеты заперты; пометил gap в `00-KONVEJER`. Требует отдельной правки Фазы II.
**Время+токены:** ~1 длинный проход, ≈2ч эквивалент; ~205k токенов контекста (≈20 якорей прочитано, 13 файлов переписано + 4 отредактировано). Оценка приблизительная.
