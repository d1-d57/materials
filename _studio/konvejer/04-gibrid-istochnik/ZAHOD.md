# Канал исполнителя — НАРРАТИВ + ГИБРИД-ИСТОЧНИК (шаг 4, линчпин) (один заход до конца)

> 🗄 **АРХИВ — конвейер до 27.07.2026.** Файл описывает СТАРЫЙ конвейер: 10 стадий `01-brief`…`10-sborka-qa`,
> реестр `GEJTY.md`, дерево `src/`+`slides/`. **Живой конвейер — фазы Ф1…Ф7**, его
> карта: `../KARTA-ZHIVOGO-KONVEJERA.md` (там же машиночитаемый реестр инструментов §5 и гейт
> согласованности `_generator/tools/check_karty.py`, который краснеет, когда реестр
> и диск разъезжаются). 🔴 **Отсюда НИЧЕГО не удалено:** история решений — единственное,
> чем доказывается «почему так», и стадия может оказаться живой в другом проекте.
> Помечено заходом `karta-kak-reestr` 2026-08-09; гейт согласованности архивные файлы
> не читает вовсе.

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные ниже якоря — проект не изучай.
> План/вопросы/отчёт пиши в секции внизу ЭТОГО же файла. Метрика — КАЧЕСТВО источника: математика в
> финальном объёме + история держит. Часы — норма. Это линчпин Фазы I, не срезай. НЕ переусложняй
> (котёл черновой, всё переписывается вниз).

**Лекция:** `<LECTURE>` — подставь имя папки лекции ПЕРЕД выдачей файла. Пути — от корня `materials/`.

КОНТЕКСТ. Ты на шаге 4 (нарратив + гибрид-источник) Фазы I конвейера `materials/_studio` — **это
ЛИНЧПИН**: источник, который дальше кормит ВСЕ слайды. Поток 2→3 оставил карточный граф: котлы
`математика‖научпоп` (арка 2) и выросший слой `skelet` с якорями, прошедший гейт «математика верна?»
(арка 3). ЦЕЛЬ шага 4 — ДВЕ под-стадии: **4a** вытащить ЛИНИЮ (нить графа) в `rasskaz` (5 актов,
process-first); **4b** собрать закрытый источник — `rasskaz` (видимая журнальная проза) ‖ `skelet`
(строгий слой), сшитые ЦИТАТОЙ-СНОСКОЙ; 🖼-описания по тактам; закрыть гейт «источник закрыт?».
Приёмка — по ОТЧЁТУ.

**Флаг «стоп до цели» (ГРОМКО):** ты сдаёшь ЗАКРЫТЫЙ ГИБРИД-ИСТОЧНИК (`rasskaz`‖`skelet`, сшивка
цитатой) — НЕ слайды и НЕ финальный текст слайдов (арка 6), НЕ раскадровку (арка 5), НЕ иллюстрации
(арка 9). Ты пишешь ОПИСАНИЯ илл. (`🖼`), а не рисуешь. **Жёсткое предусловие:** гейт 3→4 «математика
верна?» (0 открытых `⚑ Флаг`) ДОЛЖЕН быть зелёным ДО старта — иначе НЕ начинаешь (см. `## 1`).

## 0. ПЕРВЫЙ ХОД
- Рабочая папка: `<LECTURE>/` — тот же карточный граф от арок 2–3 (путь подставит Cowork). Ты ДОБАВЛЯЕШЬ
  слой `rasskaz` и сшивку, не переписываешь математику.
- Точка отката: `materials/` под git — закоммить состояние ПОСЛЕ арки 3 / ДО арки 4; иначе `tar`. Заход
  двигающий (правит/добавляет прозу), но НЕ лоссовый по математике — вывод не трогается; откат обязателен
  (полировка может «поехать»).
- Прочитать ТОЛЬКО:
  1. `_studio/konvejer/04-gibrid-istochnik/DOK.md` — контракт, 4a нарратив / 4b сборка, сшивка цитатой,
     нюанс разреза rasskaz‖skelet vs котлы, 🖼-слой (B12), гейт, ловушки, развилки. Читаешь, не редактируешь.
  2. `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-format-teksta.md` — §1 слои, §2 шаблон rasskaz
     (5 актов, process-first, «пометки для сборки»), §5 сшивка цитатой.
  3. `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-catalan-kartochki.md` — нить (маршрут по карте)
     + нюанс: rasskaz‖skelet = ПОСТ-нарративный разрез, не котлы арки 2.
  4. `_studio/docs/spravka/FAZA-1-REDESIGN.md` — стадии 3–4 (нарратив отдельной сессией).
  5. Карточный граф лекции `<LECTURE>` (итог арок 2–3) — котлы, нити, слой skelet, якоря. Читаешь целиком.
- ПЛАН — в `## ПЛАН` ДО первой правки: (а) греп `⚑ Флаг` — сколько открытых (если >0 — СТОП, см. `## 1`);
  (б) какую НИТЬ графа тянешь в линию (маршрут узлов затравка→…→выход); (в) на каждый акт — сцена/связка,
  что цитируешь-сноской из skelet, где ставишь `🖼`.

## 1. ДИСЦИПЛИНА (Карпатов)
- **Развилки, не закрытые владельцем (назови в ПЛАНЕ, возьми дефолт, не молчи):** (а) нарратив отдельный
  пакет vs под-стадия 4a — дефолт **под-стадия 4a** (без renumber, 06–10 заперты); (б) сборка сшивка-
  цитатой vs `<details>` — дефолт **цитата-сноска** (модель Каталана, не переусложнять).
- **Предпосылка, которую ОБЯЗАН проверить первой:** гейт 3→4 закрыт — 0 открытых `⚑ Флаг`. Греп по потоку.
  **Найдены открытые — СТОП:** математика не заперта, нарратив воспроизведёт провал «платоновых тел».
  Доложи в `## ВОПРОСЫ`, верни в арку 3, не начинай.
- **Математику НЕ переписываешь и НЕ сокращаешь** — обрамляешь сценой; ядро вывода в видимом слое, цитата-
  сноска ведёт к ПОЛНОМУ skelet. Не путай разрезы: `rasskaz‖skelet` (пост-нарратив) ≠ котлы арки 2.
- **process-first:** каждый объект rasskaz — из конкретной задачи; связки «из-за этого», не «и ещё»;
  прозрение вслух; закольцовка §5↔§0; честный зазор напоследок.
- **Добор ресёрча:** нить обнажила дыру — вернись в граф (арка 2), допиши карточку; НЕ выдумывай факт.
- **Хирургия:** добавляешь `rasskaz`, цитаты-сноски на СУЩЕСТВУЮЩИЕ якоря skelet, `🖼`-описания. Не трогаешь
  якоря/id, не меняешь нотацию. Named стиль-шаг — точечно, только по видимой прозе (не по математике).
- **Оспорить ложную предпосылку:** throughline из арки 1/2 не держит поверх реальной математики (акт
  «поехал») — не подгоняй прозу, подними как находку (B14).
- **Критерий, который может провалиться:** см. `## 3 → КРИТЕРИЙ ГОТОВНОСТИ`.
- Субагенты: ≤5 (если полируешь акты параллельно), рейт-лимит = отступить и доложить.

## 2. ГРАНИЦЫ
- НЕ переписывать/сокращать математику (слой skelet) · НЕ трогать карточки/карту/якоря (кроме обрамления
  прозой) · НЕ переименовывать id.
- НЕ резать на слайды (арка 5) · НЕ писать финальный текст слайдов (арка 6) · НЕ рисовать SVG (арка 9) —
  только `🖼`-ОПИСАНИЯ.
- НЕ звать `popsci-narrative`/`humanizer`/`russian-editor` как ЖИВЫЕ СКИЛЛЫ (Р3, Р17) — приёмы в DOK.
- НЕ начинать при открытых `⚑ Флаг` (см. `## 1`).

## 3. ЗАДАЧА
(4a) Вытащить нить графа в `rasskaz`: 5 актов process-first, прозрения вслух, связки «из-за этого»,
закольцовка, честный зазор, «пометки для сборки»; добор ресёрча где провисает. (4b) Собрать источник:
`rasskaz` (видимая журнальная проза, математика в финальном объёме) ‖ `skelet`, сшитые ЦИТАТОЙ-СНОСКОЙ
по якорям арки 3; `🖼 <род·смысл> {N}` на КАЖДЫЙ такт (сырьё — пометки арки 3); точечный стиль-шаг.

**КРИТЕРИЙ ГОТОВНОСТИ** (проверяемый, способен провалиться):
1. **0 открытых `⚑ Флаг`** (греп не находит). Проверяемо грепом.
2. Математика в ВИДИМОМ слое — в финальном объёме: ни один вывод не сокращён ради нарратива (сверить со
   skelet: ядро каждого доказательства на месте, не «см. сноску»).
3. throughline держит от начала до конца; связки — «из-за этого», а не «и ещё» (прочитать подряд, каждый стык).
4. Каждый такт имеет `🖼`-описание (род·смысл·N сцен) — 0 тактов без описания. Проверяемо сверкой числа тактов.
5. Все цитаты-сноски резолвятся: каждая ссылка на skelet-якорь → реальный блок, 0 сирот. Проверяемо грепом якорей.
6. Нотация и термины НЕ изменились относительно арки 3 (стиль-шаг не проехался по математике).
7. Разрез не спутан: `rasskaz‖skelet` (пост-нарратив), не котлы арки 2.
8. Ничего из зон арок 5/6/9 не сделано (нет слайдов, нет финального текста, нет нарисованных SVG).

## 4. ВЕРИФИКАТОР
Заход двигающий — встрой сверку ДРУГИМ проходом: холодная пере-читка как читатель, не автор. (а) Греп
`⚑ Флаг` = 0. (б) Греп якорей цитат-сносок против skelet = 0 сирот. (в) Число `🖼` = число тактов из
«пометок для сборки». (г) Diff математического текста до/после = только обрамление, нотация не тронута
(`git diff`, если под git). (д) Прочитать видимый слой подряд вслух — throughline держит, связки «из-за
этого». При доступности параллельных заходов — свежий субагент-рецензент на пункт (д) ДРУГИМ взглядом.

## 5. ОТЧЁТ
В `## ОТЧЁТ` внизу: (4a) какую нить вытянул, throughline, акты; (4b) что сшито цитатой, что 🖼 · как
проверил (критерий + верификатор а–д) · какие развилки взял дефолтом (упаковка нарратива; сшивка) · что
НЕ трогал (математика/якоря/границы) · вопросы (открытые флаги, если нашлись; поехавший throughline) ·
время прогона + токены. Затем — арка 5 (раскадровка).

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
