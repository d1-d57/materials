# КАРТА ЖИВОГО КОНВЕЙЕРА — что реально везёт лекцию сегодня

> **Когда читать:** прежде чем чинить/расширять фабрику презентаций — понять, какой кусок вообще
> живой, а какой описан, но отключён. · **Что это:** сверка реестра гейтов старого конвейера
> (`GEJTY.md`, 11 папок `_studio/konvejer/*/`) с живым конвейером Ф1…Ф7 (`SKILL.md`,
> `~/Documents/GitHub/disciplina/skills/slajdy/`) — по факту на диске, не по названию. · **Куда
> дальше:** раздел 4 — с чего начать следующему заходу. НЕ проектирует новый конвейер и не
> переставляет фазы — только фиксирует то, что есть, честно помечая то, чего нет.
>
> **Метод:** каждая метка ниже подтверждена командой, прогнанной на живой лекции
> `teorkat-vvedenie/L2` (единственный живой экземпляр дерева Ф1…Ф7 на момент сверки, 2026-08-08/09).
> Пустая клетка — не позор, а находка: содержимое не выдумано.

## 1. Живой конвейер Ф1…Ф7 — одной таблицей

Источник формы — `SKILL.md` (`~/Documents/GitHub/disciplina/skills/slajdy/`, 1473 строки, переписан
2026-08-07/08). Колонка «чем доказано, что гейт кто-то зовёт» — прогон на `teorkat-vvedenie/L2`, а не
чтение документации: там, где лекция гейт ещё не прошла, это сказано прямо, без выдумки.

| Фаза | Решение фазы | Артефакт | Дом на диске | Гейт | Верификатор | Чем доказано, что гейт зовут |
|---|---|---|---|---|---|---|
| **1 · Интервью** | карточка лекции (жанр·бюджет·сквозная линия·`tochno_ne_pokazyvaem`·`uzhe_vvedeno_ranee`) + разбиение на слайды/блоки + `vvodit`/`opiraetsya_na` | `brief.md` (шапка) · `slajdy/<имя>/slaid.md` (шапка+разметка блоков) · `INTERVYU.md` | `<лекция>/{brief.md,INTERVYU.md,slajdy/<имя>/slaid.md}` | Хук 1: `grep -c "^ПРИНЯТО фаза-1:" brief.md` = 1 · **нет .py-гейта:** проверка фазы — однострочный grep по `brief.md`, отдельного инструмента у неё нет и не задумано | свежий агент судит план целиком, не видит истории обсуждения | **НЕ закрыт формально на L2**: `grep -c '^ПРИНЯТО фаза-1:' teorkat-vvedenie/L2/brief.md` → `0`. Карточка лекции при этом заполнена (жанр/бюджет/сквозная линия/`tochno_ne_pokazyvaem` — 5 пунктов/`uzhe_vvedeno_ranee` — 3 пункта, все непустые, `brief.md` строки 20–36) |
| **2 · Раскадровка** | перенос куска ленты в блок (`matematika_iz`) · вырезки · `tip_verstki`/`liniya` · заказ иллюстраций (`zakaz.md`, 5+1 полей) · уточнённый бюджет слов | `slajdy/<имя>/slaid.md` (`## Математика — развёрнуто`, шапка дописана) · `VYREZANO.md` · `illustracii/<имя>/zakaz.md` | `<лекция>/{VYREZANO.md,illustracii/<имя>/zakaz.md}` | `gejt_kartochki.py` (частично применим, Д38) · сверка покрытия ленты (3 числа) · `gejt_illyustracij.py` · Хук 2 | содержание блока отвечает мысли плана; `tip_verstki` из реестра, не изобретён | `python3 _generator/tools/gejt_illyustracij.py teorkat-vvedenie/L2` → **ЗЕЛЁНЫЙ**, 11 ссылок/11 заказов. Пример заказа (`illustracii/centr-i-ego-obraz/zakaz.md`) несёт все 6 полей заполненными. **`VYREZANO.md` отсутствует вовсе** (обязателен даже при 0 вырезках — SKILL.md ВЫХОД фазы 2), `ПРИНЯТО фаза-2:` = `0` |
| **3 · Текст слайдов** | регистр по типу блока · сжатие до плаката · порядок при переборе бюджета | `slajdy/<имя>/slaid.md`, `## Текст слайда — сжато` | тот же файл | `gejt_kartochki.py` (применим ЦЕЛИКОМ — единственная фаза) exit 0 · `check_view.py` · Хук 3 | правило первого употребления термина, форма фразы по типу, плакатный стиль | `python3 _generator/sborka/gejt_kartochki.py teorkat-vvedenie/L2` → **«ЗЕЛЁНЫЙ (полная проверка): проверено 15 из 15 карточек, замечаний 0»**. Карточка `centr-gruppy` несёт запись правки «2026-08-09 · фаза 3: текст слайдов написан». `ПРИНЯТО фаза-3:` = `0` — формально не закрыт, хотя контент готов |
| **4 · Вёрстка** | вмещается ли текст в `tip_verstki` · кегль · порядок починки переполнения · SPLIT | `slajdy/<имя>/dist/index.html`(+png) | `<лекция>/slajdy/<имя>/dist/` | компиляция без отказа + `audit.py` [браузерный] + `gejt_vmeshcheniya.py` (**СУЩЕСТВУЕТ и подключён**, ФАЗА 3.9) | канон формы, блочный ритм (two-plane core) | 🔴 **ИСПРАВЛЕНО 2026-08-09 на слитом дереве:** прежняя запись «ГЕЙТ ВМЕЩЕНИЯ (не существует — долг)» была НЕВЕРНА — карта прочитала документацию вместо диска, ровно тот грех, ради разоблачения которого писалась. Факт: `ls -la _generator/sborka/gejt_vmeshcheniya.py` → файл на 5217 байт, написан давно; `grep -c gejt_vmeshcheniya _generator/sborka/bootstrap_lekcii.py` → `1` (строка «ФАЗА 3.9», закрытие фазы 3, до ФАЗЫ 4). Прежний замер верен для НЕСЛИТОГО дерева, где файла не было. Сборка: `deck.py --bez-podbora` → см. фазу 6 |
| **5 · Иллюстрации** | делегирование скиллу `illustracii` · стык по имени папки пула · первая генерация одним агентом | `illustracii/<имя>/risunok.svg` | `<лекция>/illustracii/<имя>/` | `gejt_kartochki.py` (резолв имён) + `gejt_illyustracij.py` | иллюстрация отвечает `zakaz.md` по всем полям (сосед-скилл `illustracii`) | `gejt_illyustracij.py` → ЗЕЛЁНЫЙ (см. фазу 2). `ls teorkat-vvedenie/L2/illustracii/*/risunok.svg \| wc -l` → `11` из 11 заказов |
| **6 · Сборка и QA** | что входит в дек (`status: rezerv`) · происхождение дека · working-копия, не delivery | `<лекция>/dist/index.html` · `dist/lenta.html` | `<лекция>/dist/` | 5 слоёв: `gejt_kartochki.py` → `deck.py`/`lenta.py` → render-identity (N/A на первой сборке) → `audit.py` → глаз | гейт прогнан на эталоне прежде работы | 🔴 **ИСПРАВЛЕНО 2026-08-09 на слитом дереве: 44 `⟦MISSING-MATH⟧` больше нет, и изменилось не только число, а САМ КЛАСС ОТКАЗА.** (а) Живой дек на диске ЕСТЬ: `grep -c MISSING-MATH teorkat-vvedenie/L2/dist/index.html` → **`0`** (прежняя запись «живого `dist/` на диске сегодня нет» относилась к неслитому дереву). (б) Тихо собрать дек с дырами вместо формул сборка БОЛЬШЕ НЕ МОЖЕТ: она отказывается. Замер той же командой, что дала 44: `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 --bez-podbora -o /tmp/x.html` → **rc=1**, `ОШИБКА: слайд centr-gruppy: 3 формул(ы) нет в кэше math/katex.json`. То есть «44 дыры в деке» сменились на «сборка встала» — заплатка `sborka-l2-fazy-4-7` (ФАЗА 3.5, `kesh_formul.js`). (в) 🔴 ЖИВОЙ ОСТАТОК, не закрытый этим заходом: кэш `math/katex.json` отстал от карточки `centr-gruppy` на 3 формулы — пересборка дека сегодня встанет. Дом — `teorkat-vvedenie/**`, вне зоны захода `gigiena-i-svedenie` |
| **7 · Сцены** | где ставить паузу (`{@N}`) · пропуск внутри блока (`{fill@N}`/`{blur@N}`) · накопление vs замена | теги сцен внутри тел блоков (тот же `slaid.md`) | `<лекция>/slajdy/<имя>/slaid.md` | `audit.py` `--scene-diff` [браузерный] + попиксельная проверка видимости | каждый клик что-то меняет; порождённое `data-scenes` отвечает замыслу | На L2 сцен **0**: `grep -rl '{@\|{blur@\|{fill@' teorkat-vvedenie/L2/slajdy/*/slaid.md` → пусто. Легально — фаза идёт последней, дек до неё ещё не дошёл. `{blur@N}` сегодня **не работает физически** ни на одном дереве (SKILL.md: `.blur-reveal`/`data-reveal` отсутствуют в `base.css`/`engine.js`) |

## 1а. Класс «ИНСТРУМЕНТ ЕСТЬ, ВЫЗОВА НЕТ» — самый частый способ, которым фабрика ломается тихо

🔴 **Карта обязана показывать этот класс отдельно, иначе она показывает не то.** Инструмент,
которого никто не зовёт, зелен ровно потому, что его никто не звал: он не отличим от работающего ни
одним признаком, кроме прямого замера. Так уже жили `gejt_vmeshcheniya.py` (написан давно, подключён
08.08) и `gejt_verstki.py` (написан 08.08, вызова не имел вовсе — см. ниже).

Замер — командой, не глазами (дата данных 2026-08-09, слитое дерево):

```
python3 _generator/tools/check_tool_contract.py $(ls _generator/tools/*.py _generator/sborka/*.py)
```

| было до захода `gigiena-i-svedenie` | стало | чем |
|---|---|---|
| красных **7** | красных **3** | −1 подключением, −3 починкой самой проверки |

- 🔴 **Три из семи красных были ЛОЖНЫМИ, и это дефект проверки, а не инструментов.**
  `bloki.py`, `korpus.py`, `podgonka.py` числились «нигде не вызываются», хотя импортируются
  соседями (`bloki` — из `formaty`/`bootstrap_lekcii`/`lenta`/`gejt_kartochki`/`smeta`, `korpus` и
  `podgonka` — из `vmeshchenie`). Причина: охват `check_tool_contract.has_live_trigger()` обходил
  `_generator/` через `iterdir()` с `p.is_file()`, то есть **подпапка `_generator/sborka/`
  отсеивалась** — а боевой конвейер сборки живёт именно там. Половина ложных срабатываний на
  сплошном прогоне; гейт с такой долей обходят, а не чинят по нему. Охват расширен.
- 🔴 **ПОПРАВКА 2026-08-09 (заход `karta-kak-reestr`), замер вместо повтора: `podgonka` НЕ
  импортируется ниоткуда.** Утверждение выше верно для `bloki` и `korpus`, но не для `podgonka`:
  `grep -rnE '^\s*(import podgonka|from podgonka)' --include='*.py'` по всему репозиторию → **пусто**.
  `vmeshchenie.py` называет `podgonka.kegl_dlya_znakov` в КОММЕНТАРИИ (строка 31), и это единственная
  связь; `_podgonka` в `zamer_smety.py` — локальная функция, к модулю отношения не имеющая. Тот же
  замер опровергает и докстринг самого `check_tool_contract.check_live_trigger` (строки 468–470),
  который эту связь утверждает. Строка реестра §5 у `podgonka.py` заполнена по замеру, а не по этому
  абзацу. *Как поймано: независимый субагент-верификатор, сверявший пять случайных строк реестра
  своими командами.*
- **`gejt_verstki.py`** — был красным по-настоящему: вызова не имел нигде. Подключён маркером
  `# TOOL-CONTRACT: called-by-hand` с НАЗВАННОЙ фазой (ФАЗА 2). 🔴 **Остаток долга:** строки фазы в
  `bootstrap_lekcii.LIFECYCLE_TMPL` по-прежнему нет — она в чужой зоне; поставят её, маркер можно
  снять.
- **Остались красными 3** — `lenta.py`, `slaid.py`, `vmeshchenie.py`, все по классу «кривой вход»:
  нет спутник-фикстуры, доказывающей, что кривой вход инструмент отвергает. Не «забыты»: честно
  закрыть их значит написать фикстуры кривого входа на три инструмента, это отдельный заход. Дом —
  `_generator/sborka/**`, вне зоны захода `gigiena-i-svedenie`.

⚠ **Чего этот замер НЕ ловит** (объявлено, чтобы зелёный не читали шире, чем он есть): «живая точка
вызова» — текстовая эвристика (упомянут ли basename вне своего файла). Инструмент, упомянутый в
мёртвой ветке кода или в закомментированной строке, она считает подключённым.

## 2. Таблица сверки К1 — старый гейт/стадия → метка → доказательство

Число гейтов — `grep -c '^### G' _studio/konvejer/GEJTY.md` → **17** (`G1–G4, G16, G16.9, G5, G7, G6,
G8–G15`). Число хуков — `grep -c '^### H' _studio/konvejer/GEJTY.md` → **6**. Число стадий —
`ls -d _studio/konvejer/*/ | wc -l` → **11**. Размечено ниже: 17 из 17 гейтов, 6 из 6 хуков, 11 из 11
стадий (34 записи из 34).

🔴 **Итог по меткам, и это находка, не недосмотр: `grep -c '\| ЖИВ' KARTA-ZHIVOGO-KONVEJERA.md` → 0.**
Ни одна из 34 записей не размечена ЖИВ в строгом смысле определения захода («делает то же и работает
на живом дереве») — 30 ПЕРЕЕХАЛ + 4 АРХИВ (`grep -cE '\| ПЕРЕЕХАЛ'` → 30, `grep -cE '\| АРХИВ'` → 4).
Это ожидаемо и согласуется с самим диагнозом захода: старые признаки-на-диске категорически не читают
новое дерево `slajdy/`/`illustracii/` — если бы среди них нашёлся хоть один ЖИВ, диагноз был бы
неполным. Следствие для §3 верификатора ниже: раз ЖИВ нет, «три случайные метки ЖИВ» заменены на три
случайные метки ПЕРЕЕХАЛ — тот же риск (утверждение «живой механизм есть и работает»), только не
привязанное к устаревшему id.

### 2.1 Стадии (11 из 11)

| Стадия | Метка | Доказательство |
|---|---|---|
| `01-brief` | ПЕРЕЕХАЛ | → скилл `reserch-cikl` (ЧЕРНОВИК, `disable-model-invocation: true`) + slajdy Фаза 1 решение 1. `head ~/.claude/plugins/marketplaces/disciplina/skills/reserch-cikl/SKILL.md` → `istochnik: "...{01-brief,...}"`. `brief.md` L2 несёт карточку лекции YAML-полями, не 8 пронумерованными |
| `02-reserch` | ПЕРЕЕХАЛ | → `reserch-cikl` (ЧЕРНОВИК). `istochnik` называет `02-reserch`. `find teorkat-vvedenie -iname kartoteka` → пусто — артефакта на дереве L2 не осталось (см. §3) |
| `03-matbaza` | ПЕРЕЕХАЛ | → `reserch-cikl` (ЧЕРНОВИК). `istochnik` называет `03-matbaza`. `find teorkat-vvedenie -iname kotly` → пусто |
| `04-gibrid-istochnik` | ПЕРЕЕХАЛ | → `reserch-cikl` (ЧЕРНОВИК), живой выходной артефакт есть: `wc -l teorkat-vvedenie/LENTA-L2.md` → `865` |
| `04.5-intervyu` | ПЕРЕЕХАЛ | → slajdy Фаза 1, подфазы 5.1–5.4 (Хук 1). Id `G16` сохранён, признак другой — см. §2.2. Старый артефакт `teorkat-vvedenie/KONTRAKT-L2.md` остался сиротой (`status: utverzhden` по старой модели, новая Фаза 1 файл не читает вовсе) |
| `05-raskadrovka` | ПЕРЕЕХАЛ | → Фаза 1 решение 2 + Фаза 2 целиком. `slide_order:` в `brief.md` — 15 записей, 1:1 с папками `slajdy/*/` (сверено питоном: пересечение пусто в обе стороны) |
| `06-tekst` | ПЕРЕЕХАЛ | → Фаза 3. `gejt_kartochki.py` ЗЕЛЁНЫЙ 15/15 (см. §1) |
| `07-verstka` | ПЕРЕЕХАЛ | → Фаза 4. `deck.py --bez-podbora` собирает 15/15; штатный авто-подбор кегля падает (см. §1, §4 п.2) |
| `08-sceny` | ПЕРЕЕХАЛ | → Фаза 7, переставлена в КОНЕЦ (решение владельца 2026-08-08, не сразу после вёрстки). На L2 не начата (сцен 0) — легально |
| `09-illustracii` | ПЕРЕЕХАЛ | → Фаза 5 (тонкая: заказ/приёмка) + скилл `illustracii` (рисование, делегировано целиком, Н4 SKILL.md). `gejt_illyustracij.py` ЗЕЛЁНЫЙ |
| `10-sborka-qa` | ПЕРЕЕХАЛ | → Фаза 6. `deck.py`/`lenta.py` реально собирают. 🔴 ИСПРАВЛЕНО 09.08: «44 `MISSING-MATH`» — число с НЕСЛИТОГО дерева; на слитом в `teorkat-vvedenie/L2/dist/index.html` их `0`, а тихо собрать дек с дырами сборка больше не может — отказывается (см. §1, фаза 6) |

### 2.2 Гейты (17 из 17)

| Гейт | Метка | Доказательство |
|---|---|---|
| **G1** бриф закрыт | ПЕРЕЕХАЛ | → Фаза 1 решение 1 (карточка лекции, Хук 1). `grep -c '^ПРИНЯТО фаза-1:' teorkat-vvedenie/L2/brief.md` → `0` (не закрыт), карточка сама заполнена (см. §1) |
| **G2** ресёрч закрыт | ПЕРЕЕХАЛ | → `reserch-cikl`, вход Фазы 1 (SKILL.md ВХОД п.2). Артефакта на дереве L2 нет |
| **G3** математика верна | ПЕРЕЕХАЛ | → `reserch-cikl`, вход Фазы 1 (ВХОД п.3). Отдельного слоя `skelet` со флагами на дереве L2 нет — математика живёт внутри карточек `## Математика — развёрнуто` |
| **G4** источник закрыт | ПЕРЕЕХАЛ | → `reserch-cikl`, вход Фазы 1 (ВХОД п.4), живой артефакт: `teorkat-vvedenie/LENTA-L2.md`, 865 строк |
| **G16** контракт интервью утверждён | ПЕРЕЕХАЛ | → Фаза 1, Хук 1. Id сохранён (SKILL.md «Сводная карта гейтов»: «G16 \| контракт, шаг 4.5 \| гейт выхода фазы 1 (Хук 1) \| да»), признак заменён целиком — больше не файл `KONTRAKT-*.md`, а строка `ПРИНЯТО фаза-1:` |
| **G16.9** илл. в KONTRAKT несёт ТЗ | ПЕРЕЕХАЛ | → Фаза 2 решение 4 (`zakaz.md`, ПЯТЬ полей ANKETA G2 + `источник:`). `gejt_illyustracij.py` ЗЕЛЁНЫЙ; пример заказа несёт все 6 полей (§1) |
| **G5** раскадровка закрыта | ПЕРЕЕХАЛ | → Фаза 1 решение 2 + Фаза 2 целиком (см. §2.1 `05-raskadrovka`) |
| **G6** вход Фазы II | АРХИВ | Смысл (precondition между раскадровкой и вёрсткой) растворился: у каждой фазы Ф1–Ф7 свой явный ВХОД, читающий гейт предыдущей — отдельного узла-precondition не осталось. SKILL.md сама сводная карта: «между фазой 3 и фазой 4 \| сформулирован? нет» |
| **G7** предвёрсточный документ | АРХИВ | Формат `raskadrovka/teksty/*.md`+`view.html` на дереве L2 не существует (`ls teorkat-vvedenie/L2/raskadrovka` → нет такой папки). Функция «владелец читает целиком, принимает» ПЕРЕЕХАЛА в Хук 2 (`lenta.py` + `ПРИНЯТО фаза-2:`) и Хук 3 |
| **G8** вёрстка (7→8) | ПЕРЕЕХАЛ (частично, долг) | → Фаза 4 ГЕЙТ ВЫХОДА (`audit.py` + «ГЕЙТ ВМЕЩЕНИЯ»). SKILL.md прямо: «ГЕЙТ ВМЕЩЕНИЯ — того, чего нет, и это самый дорогой пробел фазы»; решается параллельным заходом `solver-vmeshcheniya` |
| **G9** сцены без дед-кликов | ПЕРЕЕХАЛ | → Фаза 7 ГЕЙТ ВЫХОДА (`--scene-diff`, SKILL.md сводная карта). На L2 неприменим пока (сцен 0) |
| **G10** иллюстрации (9→10) | ПЕРЕЕХАЛ (частично, дыра) | Резолв имени в пул жив (`slaid.py:load_illustrations` отказывает на ненайденном файле). `var(--x)`-проверка и запрет русского текста в `<text>` — НИЧЕМ не проверяются на новом дереве, названо дырой в самом SKILL.md (Фаза 5 ГЕЙТ ВЫХОДА) |
| **G11** сборка + QA | ПЕРЕЕХАЛ | → Фаза 6 ГЕЙТ ВЫХОДА, тот же принцип 5 слоёв дёшево→дорого. `deck.py`/`lenta.py` реально собирают (§1) |
| **G12** обложка/визитка/финал | ПЕРЕЕХАЛ (частично, дыра) | → Фаза 4 решение (не перегружены). Механизм-генератор служебных слайдов из старого `{{SLIDES}}` в новом `deck.py`/`slaid.py` НЕ существует (`slaid.py` явно документирует: «без `{{SLIDES}}`-конвейера `build_deck.py`, тот НЕ трогается»); признак-на-диске неприменим. На L2 служебных карточек нет вовсе: `ls teorkat-vvedenie/L2/slajdy \| grep -i 'title\|vizitka\|thanks'` → пусто |
| **G13** илл.-ответ не раньше сцены | ПЕРЕЕХАЛ | → Фаза 7 (та же разметка `{@N}` на иллюстрации). На L2 неприменим пока (сцен 0) |
| **G14** `data-scenes` согласован | АРХИВ как гейт-сверка / ЖИВ как принцип | Величина порождается компилятором (`slaid.py` `max_scenes`), не сверяется руками — SKILL.md Фаза 7 п.2: «`data-scenes` НЕ проставляется руками — его вычисляет компилятор». `grep -c 'data-scenes' teorkat-vvedenie/L2/slajdy/*/slaid.md` → `0` (нечего сверять, атрибута в исходнике нет) |
| **G15** каскады сцен полны | АРХИВ как гейт-сверка / ЖИВ как принцип | Каскад порождается компилятором (`scene_cascade_css`), то же место SKILL.md. `grep -c '\.scene-' teorkat-vvedenie/L2/slajdy/*/slaid.md` → `0` |

### 2.3 Дисциплина-хуки (6 из 6)

| Хук | Метка | Доказательство |
|---|---|---|
| **H1** карточка без источника | ПЕРЕЕХАЛ | → `reserch-cikl` (ЧЕРНОВИК). На дереве L2 не покрыт — `kartoteka/` нет |
| **H2** источник без VYCHITANO | ПЕРЕЕХАЛ | → `reserch-cikl` (ЧЕРНОВИК). Не покрыт на дереве L2 |
| **H3** висячее ребро KARTA-OBLASTI | ПЕРЕЕХАЛ | → `reserch-cikl` (ЧЕРНОВИК). Не покрыт на дереве L2 |
| **H4** открытый `⚑ Флаг` | ПЕРЕЕХАЛ | → `reserch-cikl` (ЧЕРНОВИК). Не покрыт на дереве L2 |
| **H5** `slide_order`-сироты/дубли | ПЕРЕЕХАЛ | Старый признак — `src/brief.md` vs `slides/*.html`; новый механизм — `slide_order` YAML в `brief.md` vs папки `slajdy/*/` (тот же принцип, другой конкретный чек). Сверено питоном на L2: 15=15, пересечение расхождений пусто в обе стороны |
| **H6** величина выписана руками в двух местах | ПЕРЕЕХАЛ | Старый хук — грепаемый WARN-скрипт; новый механизм — компилятор структурно не даёт писать `data-scenes`/`.scene-N` в исходнике (цель хука достигнута архитектурой, не отдельной проверкой): `grep -c 'data-scenes\|\.scene-' teorkat-vvedenie/L2/slajdy/*/slaid.md` → `0` |

## 3. Что заведомо НЕ покрыто ничем

- ~~**Формулы не рендерятся вовсе** (44 `⟦MISSING-MATH⟧`).~~ 🔴 **ЗАКРЫТО, дата данных 2026-08-09,
  слитое дерево.** `grep -c MISSING-MATH teorkat-vvedenie/L2/dist/index.html` → `0`. Заплатка
  `sborka-l2-fazy-4-7` (ФАЗА 3.5, `kesh_formul.js` → `math/katex.json`) заменила тихую дыру на
  ОТКАЗ сборки. **Остаток:** кэш отстал от карточки `centr-gruppy` на 3 формулы, поэтому пересборка
  сегодня встаёт с `rc=1`; дом — `teorkat-vvedenie/**`.
- **Авто-подбор типографики падает** на живом деке L2 (`RuntimeError: зона .zone.t-body не найдена
  на слайде`) при штатном вызове `deck.py` без `--bez-podbora`.
- **`var(--x)` в SVG и запрет русского текста в `<text>`** — обе клаузы жили в линтере
  `build_deck.py`, который читает старое дерево; на новое дерево (`slajdy/`, `illustracii/`) он не
  наводится. Названо дырой самим SKILL.md (Фаза 5), не нашей находкой.
- **`SCENE_CAP`, ≤4 текстовых блока на сцену, задвоение id главы** — держались тем же старым
  линтером; на новом пути ничем не заменены (SKILL.md Фаза 6 ГЕЙТ ВЫХОДА, поимённый список).
- **`{blur@N}` физически не работает** ни на одном дереве нового поколения: ни `.blur-reveal`, ни
  `data-reveal` не встречаются в `_generator/skeleton/base.css`/`engine.js`. Употреблять запрещено до
  починки (SKILL.md Фаза 7 п.4).
- **Кто заводит служебные карточки (обложка/визитка/финал) в новом дереве — не решено ни одним
  документом.** У L2 таких карточек нет вовсе.
- **`reserch-cikl` — сам статус ЧЕРНОВИК** (`disable-model-invocation: true`, версия 0.0.1) и не
  оставляет в дереве лекции ни одного артефакта старого формата (`kartoteka/`, `kotly/`, `istochniki/`
  на дереве L2 нет нигде) — куда делись карточки ресёрча самой L2, на диске не нашлось.
- **Три хука `ПРИНЯТО фаза-N:` (N=1,2,3) не закрыты формально на L2** ни разу, хотя содержание фактом
  ушло дальше третьей фазы (текст написан, иллюстрации получены, `gejt_kartochki.py`/
  `gejt_illyustracij.py` зелёные).
- **`VYREZANO.md` — обязателен по SKILL.md даже при нуле вырезок — на L2 отсутствует вовсе.**
- **Браузерный слой целиком** (`audit.py`, `render.py`, `--scene-diff`, `svgOverflow`) — физически не
  прогнать в этой песочнице: `python3 -c "import playwright"` проходит, но у Playwright нет
  бинарника Chromium (`BrowserType.launch: Executable doesn't exist`). Тот же честный предел, что был
  у старого трекера, унаследован без изменений — не новая дыра.
- **Верификаторы-субагенты фаз 1–7** существуют только как текстовое описание в SKILL.md — ни
  одного отдельного скрипта-обёртки или сохранённого журнала прогонов на диске не найдено.
- **`teorkat-vvedenie/KONTRAKT-L2.md`** — файл-сирота старой модели (`status: utverzhden`), новая
  Фаза 1 его не читает вовсе; ни архивирован явно, ни удалён.

## 4. С чего начать в следующий раз — по убыванию цены

1. **Формулы не рендерятся** — самое дорогое: ставить дек владельцу нельзя, пока `⟦MISSING-MATH⟧` не
   уйдёт. `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 --bez-podbora -o /tmp/x.html && grep -c MISSING-MATH /tmp/x.html`
2. **Авто-подбор кегля падает на живом деке** — блокирует штатную сборку без обходного флага.
   `python3 _generator/sborka/deck.py teorkat-vvedenie/L2 -o /tmp/x.html` (без `--bez-podbora`,
   смотреть трейсбек)
3. **Три хука `ПРИНЯТО фаза-N:` не закрыты ни разу**, хотя контент фазы 3 уже готов — либо
   дозакрыть формально, либо решить, нужен ли хук вообще при последовательной работе одного агента.
   `grep -c '^ПРИНЯТО фаза-[123]:' teorkat-vvedenie/L2/brief.md`
4. **`reserch-cikl` не оставляет в дереве лекции ни одного артефакта** — на диске негде найти,
   откуда взялась математика L2 задним числом. `find teorkat-vvedenie -iname kartoteka -o -iname kotly`
5. **`KONTRAKT-L2.md` осиротел** — файл старой модели, новая Фаза 1 его не читает; решить: архивировать
   явно или удалить. `cat teorkat-vvedenie/KONTRAKT-L2.md | head -5`
6. **Служебные слайды (обложка/визитка/финал) не заведены для L2**, и механизма их порождения в новом
   дереве нет вовсе. `ls teorkat-vvedenie/L2/slajdy | grep -i 'title\|vizitka\|thanks'` (пусто)
7. **Браузерный слой не прогнать в песочнице** — первая реальная проверка вёрстки и сцен ждёт машину
   владельца. На машине владельца: `playwright install && python3 _generator/audit.py teorkat-vvedenie/L2/dist/index.html`

## 5. РЕЕСТР ИНСТРУМЕНТОВ — машиночитаемый, сторожится гейтом

> **Зачем раздел существует.** Текстовая карта протухла дважды, и второй раз — на самой себе
> (§1, фаза 4: «ГЕЙТ ВМЕЩЕНИЯ не существует» при живом файле рядом). Проза не краснеет.
> Ниже — не проза, а таблица, которую читает программой `_generator/tools/check_karty.py`:
> она сверяет реестр с диском и краснеет на расхождении, называя виновника поимённо.

### 5.1 Как читать и как менять

**Строка реестра — шесть клеток, порядок фиксирован:**

```
| `<путь от корня репо>` | <фаза / роль> | <кто зовёт> | <чем проверен> | ЖИВ|АРХИВ | <УРОВЕНЬ · понятие> |
```

- **файл** — путь от корня репозитория, в обратных кавычках. Гейт проверяет, что он есть на диске.
- **фаза / роль** — какой фазе `Ф1…Ф7` живого конвейера служит инструмент, либо
  `инфраструктура`, либо имя другого проекта. Решает человек. **Что делает каждая фаза —
  таблица §1 этого же файла** (Ф1 интервью · Ф2 раскадровка · Ф3 текст слайдов · Ф4 вёрстка ·
  Ф5 иллюстрации · Ф6 сборка и QA · Ф7 сцены). Запись `Ф4/Ф6` читается как «служит ОБЕИМ
  фазам», а не «одной из по контексту». *Обе оговорки дописаны по замечанию свежего читателя,
  которому дали ТОЛЬКО этот раздел: без них он мог перечислить инструменты фазы, но не сказать,
  что эта фаза делает, а косую черту трактовал сам.*
- **кто зовёт** — `хук` · другой инструмент · шаг сборки · `по руке`. Замер — прогоном
  `check_tool_contract.py`, не чтением докстринга.
- **чем проверен** — какая `PROGNAT.sh` покрывает инструмент. Ответ даёт сам инструмент:
  `python3 _generator/tools/check_tool_contract.py --fixtures-for <путь>`.
- **статус** — `ЖИВ` либо `АРХИВ`, ровно одно слово. Решает человек.
- **УРОВЕНЬ** — `МЕТА|КОНТЕНТ|СЛАЙДЫ|СПОРНО` + одно понятие через ` · ` (заход
  `uroven-instrumentov`, §5.5 объясняет ось). Понятие — то, при потере которого инструмент
  становится бессмысленным; уровень следует из него механически.

🔴 **Шестая клетка — известный, объявленный красный `check_karty.py` (не регрессия, см. §5.5
и отчёт захода `uroven-instrumentov`, критерий 4).** `chitat_reestr()` (§`kletki()`/`STROKA_RE`)
жёстко требует РОВНО пять клеток и `continue`-ит строку при любом ином числе — колонка УРОВЕНЬ
добавлена ЭТИМ заходом по прямому ТЗ, а расширение самого чекера до шести клеток лежит вне его
зоны (`_generator/tools/` — READ-ONLY). Пока чекер не обновлён, `check_karty.py` красен на ВСЕХ
52 строках реестра каскадом (52 «клеток 6» → 52 «И1: нет строки» → 13 «И4: гейт не найден» = 117);
причина одна, не сто семнадцать разных. Следующий заход обязан поднять эту сноску, а не открывать
её заново.

🔴 **ЗАКОННАЯ ПУСТОТА ОТЛИЧАЕТСЯ ОТ ДЫРЫ ФОРМОЙ, А НЕ НАМЕРЕНИЕМ.** У инструмента может не
быть фикстуры, у фазы — верификатора: это находка, а не ошибка. Но записывается она
**явно**: клетка либо несёт содержание, либо ровно `нет: <причина>`. Клетка пустая или с
прочерком — **красное**. Пустая с причиной — зелёное. Иначе «мы про это знаем» неотличимо
от «мы это забыли», а именно эта неразличимость и стоила карте двух протуханий.

### 5.2 Реестр

| файл | фаза / роль | кто зовёт | чем проверен | статус | УРОВЕНЬ |
|---|---|---|---|---|---|
| `_generator/tools/bootstrap_arka.py` | инфраструктура · процесс-память арки | по руке (аналитик заводит арку); прямым запуском гоняют фикстуры `korni`, `register_doc` | `fixtures/git_zona/PROGNAT.sh` · `fixtures/korni/PROGNAT.sh` · `fixtures/register_doc/PROGNAT.sh` | ЖИВ | МЕТА · арка |
| `_generator/tools/bootstrap_lekcia.py` | Ф1 СТАРОГО дерева (`src/`+`slides/`, `PAPKA-LEKCII.md`); на живом дереве заменён `_generator/sborka/bootstrap_lekcii.py` | нет: настоящей точки вызова нет ни одной — инструмент старого дерева | `fixtures/git_zona/PROGNAT.sh` (ловушка 14 сторожит `bootstrap_*`) | АРХИВ | КОНТЕНТ · лекция |
| `_generator/tools/bootstrap_zahod.py` | инфраструктура · генератор заходов | по руке (аналитик собирает заход); прямым запуском гоняют фикстуры `gigiena`, `git_zona`, `korni`, `register_doc`, `zahod` | `fixtures/gigiena/PROGNAT.sh` · `fixtures/git_zona/PROGNAT.sh` · `fixtures/korni/PROGNAT.sh` · `fixtures/register_doc/PROGNAT.sh` · `fixtures/dvizhok/PROGNAT.sh` | ЖИВ | МЕТА · заход |
| `_generator/tools/check_incidenty.py` | инфраструктура · разбор классов инцидентов | инстр.: `import` — `git_zona.py`, `schet_nezakrytogo.py`; запуск — `zakryt_sessiyu.py` | `fixtures/incidenty/PROGNAT.sh` · `fixtures/korni/PROGNAT.sh` | ЖИВ | МЕТА · инцидент |
| `_generator/tools/check_kartoteka.py` | инфраструктура · целостность картотеки и корней | хук `.githooks/pre-commit` · инстр.: `import` — `register_doc.py` | `fixtures/korni/PROGNAT.sh` · `fixtures/register_doc/PROGNAT.sh` | ЖИВ | КОНТЕНТ · картотека |
| `_generator/tools/check_karty.py` | инфраструктура · согласованность ЭТОГО реестра с диском | по руке, маркер `called-by-hand`: строка для `.githooks/pre-commit` готова и названа пунктом очереди захода `karta-kak-reestr` (хук вне его зоны); прямым запуском гоняет `fixtures/karty/PROGNAT.sh` | `fixtures/karty/PROGNAT.sh` | ЖИВ | СПОРНО · реестр-инструментов |
| `_generator/tools/check_kurs.py` | другой проект: курс «Теория категорий», не фаза дека | по руке; настоящей точки вызова в дереве нет — единственное упоминание имени лежит в докстринге `check_tool_contract.py` как пример | нет: фикстуры нет — долг, назван в `## ВОПРОСЫ` захода `karta-kak-reestr` | ЖИВ | КОНТЕНТ · курс |
| `_generator/tools/check_lenta.py` | Ф2 · форма ленты | хук `.githooks/pre-commit` | `fixtures/lenta/PROGNAT.sh` | ЖИВ | КОНТЕНТ · лента |
| `_generator/tools/check_sborki.py` | инфраструктура · гейт сборки захода (С1–С8) | инстр.: `import` — `bootstrap_zahod.py` (линтует собранный заход до записи на диск) | `fixtures/gigiena/PROGNAT.sh` · `fixtures/sborka/PROGNAT.sh` | ЖИВ | МЕТА · заход |
| `_generator/tools/check_termin.py` | Ф3 · термин не раньше определения | по руке (канон `STANDART-teksta.md` правило 11); прямым запуском гоняет `fixtures/termin/PROGNAT.sh` | `fixtures/termin/PROGNAT.sh` | ЖИВ | КОНТЕНТ · термин |
| `_generator/tools/check_tool_contract.py` | инфраструктура · контракт инструмента | хук `.githooks/pre-commit` · инстр.: запуск — `bootstrap_zahod.py`, `check_karty.py` (инвариант И3 спрашивает ответ у него) | `fixtures/tool_contract/PROGNAT.sh` | ЖИВ | МЕТА · git |
| `_generator/tools/check_uroki.py` | инфраструктура · у урока обязана быть ЦЕНА | хук `.githooks/pre-commit` · инстр.: `import` — `dostavit_urok.py`, `schet_nezakrytogo.py` | `fixtures/uroki/PROGNAT.sh` | ЖИВ | МЕТА · урок |
| `_generator/tools/check_ves.py` | инфраструктура · тяжёлый файл вне git не заводится молча | хук `.githooks/pre-commit` | `fixtures/ves/PROGNAT.sh` | ЖИВ | МЕТА · git |
| `_generator/tools/check_view.py` | Ф3/Ф6 · текст не съеден при отрисовке | инстр.: запуск — `dvizhki.py` · сценарий приёмки `_studio/zhurnal/2026-07-21_mat-kostyak/PRIEMKA.sh` | нет: фикстуры нет — долг, назван в `## ВОПРОСЫ` захода `karta-kak-reestr` | ЖИВ | КОНТЕНТ · документ |
| `_generator/tools/check_zahod.py` | инфраструктура · линтер входа (З1–З10) | инстр.: `import` — `bootstrap_zahod.py` (`STRUCTURAL_CHECKS` на собранном тексте) | `fixtures/zahod/PROGNAT.sh` | ЖИВ | МЕТА · заход |
| `_generator/tools/dnevnik.py` | инфраструктура · полнота дневника сессии | инстр.: `import` — `priyomka.py`, `zakryt_sessiyu.py` | `fixtures/sessiya/PROGNAT.sh` | ЖИВ | МЕТА · арка |
| `_generator/tools/dostavit_urok.py` | инфраструктура · транспорт наблюдения в его дом | инстр.: `import` — `priyomka.py`, `schet_nezakrytogo.py`, `zakryt_sessiyu.py` | `fixtures/nablyudenia/PROGNAT.sh` · `fixtures/urok/PROGNAT.sh` | ЖИВ | МЕТА · урок |
| `_generator/tools/gejt_illyustracij.py` | Ф2 (заказ) и Ф5 (приёмка) | хук `.githooks/pre-commit` — единственный настоящий вызов; `sostoyanie.py` эту команду ПЕЧАТАЕТ советом, а не запускает | `fixtures/illyustracii/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · иллюстрация |
| `_generator/tools/gejt_verstki.py` | Ф2 · вёрстка слайда решена и обоснована | по руке, маркер `called-by-hand` с названной ФАЗОЙ 2; настоящей точки вызова нет — строка в `bootstrap_lekcii.LIFECYCLE_TMPL` остаётся ДОЛГОМ захода `gigiena-i-svedenie`, её там пока НЕТ | `fixtures/verstki/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · вёрстка |
| `_generator/tools/git_zona.py` | инфраструктура · единственная дверь к git | инстр.: запуск — `bootstrap_zahod.py`, `priyomka.py`, `sdelat_handoff.py`, `zakryt_sessiyu.py`; прямым запуском гоняют фикстуры `git_zona`, `korni`, `dvizhok`. `check_kartoteka.py` эту команду ПЕЧАТАЕТ советом, а не запускает; фикстуры `sborka` и `tool_contract` держат её только строкой в шаблоне | `fixtures/git_zona/PROGNAT.sh` · `fixtures/korni/PROGNAT.sh` | ЖИВ | МЕТА · git |
| `_generator/tools/graf_zavisimostej.py` | Ф2/Ф3 · граф зависимостей лекции, циклы и порядок | хук `.githooks/pre-commit` | `fixtures/graf/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · карточка |
| `_generator/tools/korni.py` | инфраструктура · реестр корней | инстр.: `import` — `bootstrap_arka.py`, `bootstrap_zahod.py`, `check_incidenty.py`, `check_kartoteka.py`, `git_zona.py`, `register_doc.py` | `fixtures/korni/PROGNAT.sh` | ЖИВ | МЕТА · корень |
| `_generator/tools/priyomka.py` | инфраструктура · приёмка отчёта (Г0–Г8) | по руке (аналитик гоняет на отчёте); вызова из инструментов нет — связь ОБРАТНАЯ: `priyomka.py` сама импортирует `dnevnik.py` и `dostavit_urok.py`; прямым запуском гоняют фикстуры `priyomka`, `git_zona` | `fixtures/priyomka/PROGNAT.sh` | ЖИВ | МЕТА · заход |
| `_generator/tools/register_doc.py` | инфраструктура · дверь регистрации `.md` в `KARTA.md §6` | инстр.: `import` — `bootstrap_arka.py`, `bootstrap_zahod.py`; запуск — `sdelat_handoff.py`, `zakryt_sessiyu.py` | `fixtures/korni/PROGNAT.sh` · `fixtures/register_doc/PROGNAT.sh` | ЖИВ | МЕТА · реестр-документов |
| `_generator/tools/reviziya_dolgov.py` | инфраструктура · статусы `DOLG.md` скилла `slajdy` | по руке, маркер `called-by-hand`; настоящей точки вызова нет | `fixtures/reviziya_dolgov/PROGNAT.sh` | ЖИВ | МЕТА · долг |
| `_generator/tools/schet_nezakrytogo.py` | инфраструктура · четыре числа незакрытого | инстр.: `import` — `bootstrap_zahod.py`, `zakryt_sessiyu.py` | `fixtures/nezakrytogo/PROGNAT.sh` | ЖИВ | МЕТА · долг |
| `_generator/tools/sdelat_handoff.py` | инфраструктура · вход в следующую сессию | по руке, маркер `called-by-hand`; настоящей точки вызова нет | `fixtures/sessiya/PROGNAT.sh` | ЖИВ | МЕТА · арка |
| `_generator/tools/sostoyanie.py` | трекер СТАРОГО конвейера: читает `GEJTY.md` и дерево `src/`+`slides/`; на дереве `slajdy/` не наводится | нет: настоящей точки вызова нет — упоминания в `check_lenta.py` и `check_zahod.py` это проза о гейте G7; прямым запуском гоняет только своя фикстура | `fixtures/sostoyanie/PROGNAT.sh` | АРХИВ | СЛАЙДЫ · гейт-конвейера |
| `_generator/tools/sostoyanie_lekcii.py` | другой проект: представление над markdown-базой курса теоркат | по руке; настоящей точки вызова нет — `check_kurs.py` называет его в прозе | нет: фикстуры нет — долг, назван в `## ВОПРОСЫ` захода `karta-kak-reestr` | ЖИВ | КОНТЕНТ · курс |
| `_generator/tools/zakryt_sessiyu.py` | инфраструктура · выгрузка знания из транскрипта | инстр.: `import` — `dnevnik.py`; запуск — `sdelat_handoff.py` | `fixtures/sessiya/PROGNAT.sh` | ЖИВ | МЕТА · арка |
| `_generator/sborka/bloki.py` | Ф1/Ф3 · блоки, сцены и разделы тела карточки | инстр.: `import` — `formaty.py`, `gejt_kartochki.py`, `lenta.py` | нет: своей фикстуры нет — из трёх вызывающих `fixtures/sborka/PROGNAT.sh` гоняет только `gejt_kartochki.py` | ЖИВ | СЛАЙДЫ · карточка |
| `_generator/sborka/bootstrap_lekcii.py` | Ф1 · порождение дерева лекции (`slajdy/`, карточки, `LIFECYCLE`) | по руке (порождение дерева лекции); прямым запуском гоняет `fixtures/sborka/PROGNAT.sh` | `fixtures/sborka/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · карточка |
| `_generator/sborka/deck.py` | Ф6 · сборка целого дека | инстр.: запуск — `bootstrap_lekcii.py`; прямым запуском гоняет `fixtures/sborka/PROGNAT.sh` | `fixtures/sborka/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · дек |
| `_generator/sborka/formaty.py` | Ф1/Ф2 · формат карточки слайда и markdown её тела | инстр.: `import` — `bootstrap_lekcii.py`, `deck.py`, `gejt_kartochki.py`, `lenta.py`, `slaid.py`, `smeta.py`, `graf_zavisimostej.py` | нет: своей фикстуры нет — покрыт косвенно через `fixtures/sborka/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · карточка |
| `_generator/sborka/gejt_kartochki.py` | Ф3 целиком; Ф2, Ф5, Ф6 частично | инстр.: запуск — `bootstrap_lekcii.py`; прямым запуском гоняет `fixtures/sborka/PROGNAT.sh` | `fixtures/sborka/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · карточка |
| `_generator/sborka/gejt_vmeshcheniya.py` | Ф4 · гейт вмещения (ФАЗА 3.9, закрытие Ф3 до Ф4) | инстр.: запуск — `bootstrap_lekcii.py` (строка «ФАЗА 3.9» в `LIFECYCLE_TMPL`) · плюс маркер `called-by-hand` | `fixtures/sborka/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · вмещение |
| `_generator/sborka/korpus.py` | Ф4 · диапазоны кегля из корпуса живых деков | инстр.: `import korpus` — `vmeshchenie.py` (по имени МОДУЛЯ, без `.py`: эвристика `check_tool_contract` этого не видит, §5.4) | нет: своей фикстуры нет — покрыт КОСВЕННО и это проверено: `fixtures/sborka/PROGNAT.sh` гоняет `gejt_vmeshcheniya.py`, а тот через `vmeshchenie.py` грузит `korpus` (замер: модуль оказывается в `sys.modules`) | ЖИВ | СЛАЙДЫ · кегль |
| `_generator/sborka/lenta.py` | Ф2/Ф6 · лента лекции одним HTML | по руке (представление ленты); вызова из инструментов нет — `sostoyanie.py` команду печатает советом | нет: спутник-фикстуры кривого входа нет — КРАСНЫЙ контракта, отдельный заход (`gigiena-i-svedenie`, `## ВОПРОСЫ` п.4) | ЖИВ | СЛАЙДЫ · карточка |
| `_generator/sborka/podgonka.py` | Ф4 · подгонка кегля под знаки | нет: ни импорта, ни запуска, ни `по руке` — у модуля нет ни `__main__`, ни `argparse`, запускать его нечем; `vmeshchenie.py` называет `podgonka.kegl_dlya_znakov` в комментарии, импорта нет | нет: покрытия нет ВОВСЕ — ни своей фикстуры, ни косвенного: модуль никем не импортируется, ни одна `PROGNAT.sh` не исполняет ни строки | ЖИВ | СЛАЙДЫ · кегль |
| `_generator/sborka/slaid.py` | Ф4/Ф5 · компилятор одного слайда, резолв иллюстраций | инстр.: `import` — `deck.py`, `lenta.py`, `smeta.py`, `zamer_smety.py`; запуск — `bootstrap_lekcii.py` | нет: спутник-фикстуры кривого входа нет — КРАСНЫЙ контракта, отдельный заход | ЖИВ | СЛАЙДЫ · слайд |
| `_generator/sborka/smeta.py` | Ф2 · смета вмещения без браузера | инстр.: запуск — `bootstrap_lekcii.py` · плюс маркер `called-by-hand` | `fixtures/sborka/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · вмещение |
| `_generator/sborka/tipy.py` | Ф2/Ф4 · реестр типов вёрстки | инстр.: `import` — `build_deck.py`, `deck.py`, `slaid.py` | нет: своей фикстуры нет — косвенно через `fixtures/sborka/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · вёрстка |
| `_generator/sborka/vmeshchenie.py` | Ф4 · измеритель вмещения и солвер кегля | инстр.: `import` — `deck.py`, `gejt_vmeshcheniya.py`, `smeta.py`, `zamer_smety.py` | нет: спутник-фикстуры кривого входа нет — КРАСНЫЙ контракта, отдельный заход | ЖИВ | СЛАЙДЫ · вмещение |
| `_generator/sborka/zamer_smety.py` | Ф2 · замер исходных чисел сметы | инстр.: запуск — `smeta.py` · плюс маркер `called-by-hand` | `fixtures/sborka/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · кегль |
| `_generator/audit.py` | Ф4/Ф6/Ф7 · канон-аудит собранного дека, `--scene-diff` [браузерный] | по руке [браузерный]; настоящего вызова нет — `sostoyanie.py` и `build_deck.py` называют команду в прозе | нет: фикстуры нет, и прогнать её негде — браузерный слой в песочнице не запускается (§3); `check_tool_contract` на нём КРАСНЫЙ («кривой вход») | ЖИВ | СЛАЙДЫ · дек |
| `_generator/build_deck.py` | Ф4/Ф6 · движок дека СТАРОГО дерева (`src/`+`slides/`, `{{SLIDES}}`) — им собраны все ранее выпущенные деки проектов | инстр.: `import` — `bootstrap_lekcii.py`, `deck.py`, `formaty.py`, `lenta.py`, `slaid.py`; запуск — `dvizhki.py` | `fixtures/dvizhok/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · дек |
| `_generator/build_doc.py` | другой движок: документ-вид (лекции-статьи, листки-развороты), не фаза дека | инстр.: запуск — `dvizhki.py`, `check_lenta.py`, `check_view.py` · сценарий приёмки `_studio/zhurnal/2026-07-21_mat-kostyak/PRIEMKA.sh` | нет: фикстуры нет; `check_tool_contract` на нём КРАСНЫЙ («кривой вход») | ЖИВ | КОНТЕНТ · документ |
| `../disciplina/_generator/build_listok.py` (вынесен из `_generator/` 25.08 заходом vynos-dvizhkov-2) | другой проект: листок задач из банка (спецмат), не фаза дека | нет: настоящей точки вызова нет ни одной — КРАСНЫЙ контракта «нигде не вызывается», дом вне зоны захода `karta-kak-reestr` | нет: фикстуры нет; и тот же КРАСНЫЙ контракта по «кривому входу» | ЖИВ | КОНТЕНТ · листок |
| `_generator/dvizhki.py` | инфраструктура · единственная дверь к `_generator` снаружи | проекты: `python3 -m dvizhki` — `krivaya-drakona/lekciya/assemble.py`, `krivaya-drakona/razbor/assemble.py`, `teorkat-vvedenie/src/tools/podognat.py`; `import dvizhki` — `obzory/sluchajnye-bluzhdaniya-na-gruppah/zahod/render.py`; консольная точка входа — `_generator/setup.cfg` | нет: фикстуры нет; `check_tool_contract` на нём КРАСНЫЙ («кривой вход») | ЖИВ | СПОРНО · движок |
| `_generator/harvest_katex.py` | Ф6 · сборщик кэша формул из готовой колоды | по руке; настоящего вызова нет — `build_deck.py` и `kesh_formul.js` называют его в прозе | нет: фикстуры нет; `check_tool_contract` на нём КРАСНЫЙ («кривой вход») | ЖИВ | СЛАЙДЫ · колода |
| `_generator/render.py` | Ф4/Ф6/Ф7 · локальный рендер и сверка колод [браузерный] | инстр.: `import` — `slaid.py` | `fixtures/dvizhok/PROGNAT.sh` | ЖИВ | СЛАЙДЫ · слайд |
| `_generator/setup.py` | инфраструктура · шим сборки пакета `dvizhki` (`pip` без PEP 660) | сборка пакета: `pip install -e _generator` по `_generator/setup.cfg`; фабрикой не зовётся | нет: фикстуры нет и не нужна — две строки без разбора входа | ЖИВ | СПОРНО · пакет |
### 5.3 Числа — командами, не глазами

```
ls _generator/tools/*.py _generator/sborka/*.py _generator/*.py | wc -l          # инструментов под реестром
grep -cE '^\| `_generator/[^`]+` \|' _studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md  # строк реестра
grep -cE '\| АРХИВ \|$' _studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md               # из них АРХИВ
python3 _generator/tools/check_karty.py                                          # гейт согласованности
```

### 5.4 🔴 Чего этот реестр НЕ доказывает — объявлено, чтобы зелёный не читали шире

- 🔴 **КОЛОНКА «КТО ЗОВЁТ» И ГЕЙТ ОТВЕЧАЮТ НА РАЗНЫЕ ВОПРОСЫ, И ЭТО НАДО ЗНАТЬ ОБОИМ.**
  Колонка заполнена НАСТОЯЩИМИ точками вызова: `import <модуль>`, запуск процессом
  (`python3 … x.py`, `sys.executable`), `python3 -m <модуль>`, строка хука. Упоминание
  имени в комментарии или докстринге вызовом НЕ считается. Инвариант И3 гейта спрашивает
  ответ у `check_tool_contract.has_live_trigger()`, а та — текстовая эвристика: упомянут
  ли basename вне своего файла. **Эвристика слепа в обе стороны, и обе измерены на живом
  дереве:** ложный ЗЕЛЁНЫЙ — `priyomka.py`, `podgonka.py`, `korpus.py`, `harvest_katex.py`
  (имя названо только прозой соседа либо докстрингом самого гейта); ложное КРАСНОЕ по
  смыслу — `dvizhki.py`, у которого девять живых вызовов вида `python3 -m dvizhki` в трёх
  проектах, а эвристика видит лишь `README.md`. Первая независимая сверка пяти случайных
  строк дала **1 правдивую из 5** именно потому, что первая редакция колонки была
  заполнена этой же эвристикой; колонка переписана по замеру, эвристика — нет.
  **Практический вывод: зелёный И3 означает «basename где-то упомянут», а не «инструмент
  реально зовут». Правду о вызове несёт КОЛОНКА, а не код возврата.**
- **Статус `ЖИВ` не означает «прогнан сегодня»**: он означает решение человека, что
  инструмент служит живому конвейеру. Работоспособность доказывают `PROGNAT.sh` и колонка
  «чем проверен», а не статус.
- **`.py` внутри `_generator/**/fixtures/`** в реестр не входят: это нагрузка одной фикстуры,
  а не инструмент фабрики (то же исключение, что у `check_tool_contract.check_live_trigger`).
  Разница чисел: `find _generator -name '*.py' | wc -l` минус охват реестра.
- **Колонка «фаза / роль» машинно не проверяется ничем.** Гейт сверяет существование,
  вызов и покрытие; сказать, что инструмент отнесён не к той фазе, он не может.

### 5.5 УРОВНИ — что переносится за пределы слайдов

> Разметка захода `uroven-instrumentov`. Ось — не «насколько инструмент универсален», а
> ЧТО ИМЕННО ОН ЗНАЕТ: потерял заход/арку/дом/урок/долг/git/реестр документов → МЕТА (переносится
> во ВСЕ проекты владельца без изменений); потерял ленту/лекцию/термин/курс → КОНТЕНТ (переносится
> в проекты, где производится содержание, но не туда, где его нет); потерял карточку/вёрстку/
> кегль/дек → СЛАЙДЫ (никуда, прибит к слайдам). Понятие рядом с уровнем в таблице §5.2 — ровно
> то, при потере которого инструмент становится бессмысленным.

Четыре числа — командой, не глазами:

🔴 Якорь `^\| \`_generator/...\` \|` обязателен — без него грep ловит и ЭТУ строку примера
команды (та тоже несёт текст `| МЕТА · `), что даёт 19 вместо 18. Замерено живьём при сборке.

```
grep -cE '^\| `_generator/[^`]+` \|.*\| МЕТА · ' _studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md      # 18
grep -cE '^\| `_generator/[^`]+` \|.*\| КОНТЕНТ · ' _studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md   # 9
grep -cE '^\| `_generator/[^`]+` \|.*\| СЛАЙДЫ · ' _studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md    # 22
grep -cE '^\| `_generator/[^`]+` \|.*\| СПОРНО · ' _studio/konvejer/KARTA-ZHIVOGO-KONVEJERA.md    # 3
```

18 + 9 + 22 + 3 = 52 = N из шага 1 (§5.3). Сходится.

#### МЕТА (18) — главный выход захода: уходит на метаскилл, в каждом новом скилле больше не строится заново

🔴 Список — НЕ таблица §5.2 нарочно: строка вида `| \`путь\` | ... |` в начале — та же форма,
которую `check_karty.py` (`STROKA_RE`) читает как строку реестра ГДЕ УГОДНО в разделе «## 5.», не
только в §5.2; таблицей это уже поймано на живую (см. критерий 4 отчёта) и исправлено на список.

- `_generator/tools/bootstrap_arka.py` — арка
- `_generator/tools/bootstrap_zahod.py` — заход
- `_generator/tools/check_incidenty.py` — инцидент
- `_generator/tools/check_sborki.py` — заход
- `_generator/tools/check_tool_contract.py` — git
- `_generator/tools/check_uroki.py` — урок
- `_generator/tools/check_ves.py` — git
- `_generator/tools/check_zahod.py` — заход
- `_generator/tools/dnevnik.py` — арка
- `_generator/tools/dostavit_urok.py` — урок
- `_generator/tools/git_zona.py` — git
- `_generator/tools/korni.py` — корень
- `_generator/tools/priyomka.py` — заход
- `_generator/tools/register_doc.py` — реестр документов
- `_generator/tools/reviziya_dolgov.py` — долг
- `_generator/tools/schet_nezakrytogo.py` — долг
- `_generator/tools/sdelat_handoff.py` — арка
- `_generator/tools/zakryt_sessiyu.py` — арка

🔴 **Верификатор (см. `## ОТЧЁТ` захода `uroven-instrumentov`, §3 «ВЕРИФИКАТОР»)** прогнал ПЯТЬ
из них обратным ходом — «что переписать для проекта без лекции/ленты/слайдов» — и нашёл, что
`_generator/tools/schet_nezakrytogo.py` строкой 62 хардкодит `_studio` в коде (`ZHURNAL = REPO_ROOT
/ "_studio" / "zhurnal"`), а не через `korni.py`, как остальные четыре (`git_zona.py`,
`bootstrap_zahod.py`, `register_doc.py`, `korni.py` сам). Понятие (долг) верно, метка МЕТА остаётся,
но перенос в проект с журналом не под `_studio/` потребует одной строки правки — деталь в отчёте.

#### Живёт на двух уровнях сразу (3) — то, что мешает разрезать скиллы

- `_generator/dvizhki.py` — единственная дверь к `_generator` СНАРУЖИ, маршрутизирует РОВНО между
  `build_doc.py` (КОНТЕНТ) и `build_deck.py` (СЛАЙДЫ) как двумя равноправными движками — по
  конструкции, не по случайности.
- `_generator/sborka/lenta.py` — понятие в докстроке буквально «лента» (слово из списка КОНТЕНТ),
  но код жёстко читает формат карточки слайда (`slajdy/*/slaid.md`, поле `status: v_deke`, раздел
  «Текст слайда — сжато»); без карточки слайда читать нечего — верю докстроке про формат, не про
  голое слово «лента» (правило захода: «разошлись греп и докстрока — верь докстроке»).
- `_generator/tools/graf_zavisimostej.py` — концепция («граф зависимостей терминов лекции»,
  `vvodit`/`opiraetsya_na`) content-уровня, но источник данных зашит на путь
  `<лекция>/**/slajdy/<imya>/slaid.md` — в лекции без слайдов инструменту нечего читать.

#### СПОРНО (3) — по коду не решается, законное значение

- `_generator/dvizhki.py` — см. выше: живёт на двух уровнях сразу, единый ярлык МЕТА/КОНТЕНТ/СЛАЙДЫ
  исказил бы находку.
- `_generator/setup.py` — двухстрочный `pip`-шим для editable-install пакета `dvizhki`
  (`from setuptools import setup; setup()`); ось «что теряет смысл» к нему неприменима буквально —
  это не про ведение работы, не про контент и не про слайды, а про механику упаковки Python.
- `_generator/tools/check_karty.py` — сверяет реестр §5 (именно ЭТОТ файл) с диском; механизм в
  принципе общий (таблица markdown против файлов на диске), но это НЕ «реестр документов» из
  списка МЕТА (`KARTA.md §6`, дверь `register_doc.py`) — это отдельный реестр ИНСТРУМЕНТОВ
  конвейера слайдов, и понятие не совпадает буквально ни с одним из трёх столбцов.

---
*Сверка — заход `karta-konvejera` (`../zhurnal/2026-08-07_arhitektura-slajdov/kod_karta-zhivogo-konvejera.md`).
Реестр §5 и гейт `check_karty.py` — заход `karta-kak-reestr` (`../zhurnal/2026-08-07_arhitektura-slajdov/kod_karta-kak-reestr.md`).
Живой источник фаз — `~/Documents/GitHub/disciplina/skills/slajdy/SKILL.md`. Старый реестр — `GEJTY.md`,
`ALGORITM.md`, `KATALOG.md`, `00-KONVEJER.md` в этой же папке.*
