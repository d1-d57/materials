# Канал исполнителя — РАСКАДРОВКА (шаг 5, конец Фазы I) (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные ниже якоря — проект не изучай.
> План/вопросы/отчёт пиши в секции внизу ЭТОГО же файла. Метрика — КАЧЕСТВО раскадровки: на каждом
> слайде ясно ЧТО и СКОЛЬКО. Часы — норма. Это стык с автоматом Фазы II — не сломай контракт.

**Лекция:** `<LECTURE>` — подставь имя папки лекции ПЕРЕД выдачей файла. Пути — от корня `materials/`.

КОНТЕКСТ. Ты на шаге 5 (раскадровка) — ПОСЛЕДНЕМ шаге Фазы I «индивидуальность» конвейера
`materials/_studio` и СТЫКЕ с автоматом Фазы II. Источник (арка 4) закрыт: `rasskaz` (видимая
журнальная проза, математика в финальном объёме) ‖ `skelet` (строгий слой), сшитые цитатой-сноской, с
якорями и 🖼-описаниями по тактам; режется по актам нити. ЦЕЛЬ шага 5 — разрезать источник на слайды
(на каждый: archetype + дистилляция смысла + сцены + описание илл.), посчитать бюджет, **зафиксировать
регистр+бюджет слов деки** (контракт 5→6), и выдать вход Фазы II: кликабельное HTML-демо + наполнение
`<LECTURE>/src/`. Приёмка — по ОТЧЁТУ. Дальше — СТОП-точка автора (Фаза II автоматична).

**Флаг «стоп до цели» (ГРОМКО):** ты сдаёшь СОДЕРЖАНИЕ слайдов (дистилляция смысла), НЕ финальный текст
слайдов — его пишет арка 6. Ты НЕ верстаешь (арка 7), НЕ рисуешь иллюстрации (арка 9) — только каркасы
и плейсхолдеры-описания. Если тянет написать готовую фразу слайда — это работа арки 6, стоп.

## 0. ПЕРВЫЙ ХОД
- Рабочая папка: `<LECTURE>/` (вход — закрытый гибрид-источник; ты создаёшь кликабельное демо +
  наполняешь `<LECTURE>/src/`). Точный путь источника подставит Cowork.
- Точка отката: `materials/` под git — закоммить состояние ПОСЛЕ арки 4 / ДО арки 5; иначе `zip`
  затрагиваемого. Заход создаёт новые файлы в `src/` — риск в основном в правильности `slide_order`.
- Прочитать ТОЛЬКО:
  1. `_studio/konvejer/05-raskadrovka/DOK.md` — контракт, таблица архетипов, **контракт 5→6
     (регистр+бюджет)**, ловушки. Читаешь, не редактируешь.
  2. `_studio/zhurnal/2026-07-10_sborka-konvejera/distillat-arka5.md` — семя (разбор content-studio,
     форма поля 5→6, развилки структуры записи и формы демо).
  3. `_studio/konvejer/FORMAT-ISTOCHNIKA.md` — формат `src/` и схема `brief.md` (что и как называть,
     `slide_order`, `accent_tag`). Читаешь, не редактируешь.
  4. `_studio/konvejer/ALGORITM.md` §«Вход Фазы II» — что раскадровка ОБЯЗАНА оставить в `src/`
     (precondition Фазы II). Сверяешь свой выход с ним.
  5. `_studio/konvejer/06-tekst/DOK.md` — потребитель твоего выхода: как арка 6 использует archetype +
     дистилляцию + сцены + **регистр+бюджет**. Читаешь, чтобы не сломать её вход.
  6. Закрытый источник лекции `<LECTURE>` (арка 4) — `rasskaz`‖`skelet`, сшивка цитатой, якоря, 🖼,
     нить. Предмет резки. Читаешь целиком.
  7. Бриф лекции `<LECTURE>` (арка 1, 8 полей) — длительность→бюджет, throughline, что резать, и
     **формат события** (устный/читаемый) для выбора регистра. `brief.md` эталонов `dandelin`/`buffon` —
     как форма (структурно), не копировать.
- ПЛАН — в `## ПЛАН` ДО первого файла: список слайдов-кандидатов (id + archetype + одна фраза
  дистилляции + число сцен), посчитанный бюджет (слайдов × 2 мин vs минуты брифа), и ВЫБРАННЫЙ
  регистр+бюджет слов с обоснованием из брифа.

## 1. ДИСЦИПЛИНА (Карпатов)
- **Развилки, не закрытые владельцем (назови в ПЛАНЕ, возьми дефолт, не молчи):** (а) структура записи
  слайда — 3 кандидата в DOK/семени; дефолт захода — **гибрид** (таблица в `brief.md` как канон +
  `data-*` в `slides/<id>.html` как производная для демо/арки 7). (б) форма демо — 3 кандидата; дефолт
  — **отдельные `slides/<id>.html` + лёгкий `index.html` prev/next** (ближе к Фазе II, не переразбирать
  потом). Если владелец предпочтёт иное — переключишься; сейчас не молчи о выборе.
- **Регистр+бюджет слов деки — ОБЯЗАТЕЛЬНО в `brief.md`** (контракт 5→6). Даже если «очевидно устный» —
  запиши поле явно, не оставляй арке 6 догадываться. Это главный смысл шага для стыка.
- **Дистилляция смысла, НЕ финальный текст.** На слайд — сжатый тезис (что понять), не готовая фраза.
  Финал — арка 6.
- **Минимум без спекуляций:** не добавляй фактов сверх источника; не переноси прозу источника дословно в
  сцены/описания (режь на структуру).
- **Оспорить ложную предпосылку:** если слайд не влезает даже как структура (текст переполз за край
  каркаса 1440×810) — сигнал «делить слайд», не сжимать (фит-движка тут нет); зафиксируй.
- **Хирургия:** создаёшь `slides/*.html` каркасы, `illustrations/*` плейсхолдеры, `brief.md`; копируешь
  канон-скелет. Не пишешь текст слайдов, не верстаешь грид, не рисуешь.
- **Критерий, который может провалиться:** см. `## 3 → КРИТЕРИЙ ГОТОВНОСТИ`.
- Субагенты: ≤5 (если раскадровываешь группы слайдов параллельно), рейт-лимит = отступить и доложить.

## 2. ГРАНИЦЫ
- НЕ писать финальный текст слайдов (арка 6) · НЕ верстать грид/зоны-в-пикселях (арка 7) · НЕ рисовать
  SVG/иллюстрации (арка 9) — только плейсхолдеры `<div class="ph">` с описанием+числом сцен.
- НЕ трогать закрытый гибрид-источник (только читаешь и режешь) · НЕ переименовывать якоря источника.
- НЕ звать `content-studio` как ЖИВОЙ СКИЛЛ (Р3) — его приёмы уже в DOK; архетипы/конвенции перенесены.
- Архетипы — ТОЛЬКО из канонного списка (совпадают с аркой 6/7), не изобретать свои.
- `data-desc` — идентификаторы и смысл, НЕ фразы-подписи.

## 3. ЗАДАЧА
Разрезать гибрид-источник `<LECTURE>` на слайды и подготовить вход Фазы II: (1) на каждый слайд —
archetype + дистилляция смысла + ≤3 сцены + описание илл. (род+смысл+феас); (2) посчитать бюджет слайдов
и сверить с минутами брифа; (3) **зафиксировать регистр+бюджет слов деки в `brief.md`**; (4) выдать
кликабельное HTML-демо + наполнить `src/` (каркасы `slides/*.html`, плейсхолдеры `illustrations/*`,
`brief.md` со `slide_order`+`accent_tag`+регистр+бюджет; скопировать канон-скелет).

**КРИТЕРИЙ ГОТОВНОСТИ** (проверяемый, способен провалиться):
1. `slide_order` в `brief.md` ТОЧНО покрывает `slides/*.html` — без сирот и дублей (как требует
   `FORMAT-ISTOCHNIKA.md`; проверяемо сверкой списка и файлов).
2. На КАЖДЫЙ слайд заполнены archetype + дистилляция смысла (тезис, не проза) + список сцен (≤3) +
   описание илл. (род+смысл+феас). Ни одного пустого поля.
3. **`brief.md` несёт поле «регистр + бюджет слов деки»** явно (`register` + `word_budget_per_slide`) —
   проверяемо грепом. Без него задача НЕ готова, даже если всё остальное идеально.
4. Бюджет слайдов посчитан и сверен с длительностью брифа (число слайдов × ~2 мин ≈ минуты); расхождение
   объяснено.
5. Вход Фазы II по `ALGORITM.md` §«Вход Фазы II» присутствует: канон-скелет скопирован, на каждый
   прозаический слайд есть каркас, плейсхолдеры илл. с описанием+числом сцен, `brief.md` полон.
6. Дистилляция — это ТЕЗИС, а не финальная фраза слайда (нет готового плакатного текста; проверяемо
   глазами: нет `{@N}`-шорткатов, нет отполированных предложений).
7. Слайды, не влезающие как структура, помечены «делить», не ужаты; архетипы — только канонные.
8. Ничего из зон арок 6/7/9 не сделано (нет текста, нет пиксельного грида, нет нарисованных SVG).

## 4. ВЕРИФИКАТОР
Заход создаёт вход автомата Фазы II — цена ошибки контракта высока. Сверка ДРУГИМ проходом перед
отчётом: (а) `slide_order` vs `ls slides/*.html` — 0 сирот/дублей; (б) греп `brief.md` на `register` и
`word_budget_per_slide` — поле есть; (в) на каждый слайд — 4 поля заполнены (пробежать демо); (г) число
слайдов × 2 ≈ минуты брифа; (д) открыть кликабельное демо — ничего не переполнено за край каркаса
1440×810 (иначе «делить»). Если доступен инструмент параллельных заходов — свежий субагент сверяет вход
Фазы II против `ALGORITM.md` ДРУГИМ взглядом (что ни один core-файл не забыт).

## 5. ОТЧЁТ
В `## ОТЧЁТ` внизу: таблица слайдов (id · archetype · дистилляция · сцены · илл.) · выбранный
регистр+бюджет и почему (из брифа) · бюджет слайдов vs минуты · какие развилки взял дефолтом (структура
записи, форма демо) · что НЕ трогал (текст/вёрстка/илл.) · слайды с сигналом «делить» · вопросы (форма
поля 5→6; развилки на утверждение владельцу) · результат сверки (а–д) · время прогона + токены. Дальше —
СТОП-точка автора, вход Фазы II готов.

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
