# Канал исполнителя — ВЁРСТКА (шаг 7) (один заход до конца)

> 🗄 **АРХИВ — конвейер до 27.07.2026.** Файл описывает СТАРЫЙ конвейер: 10 стадий `01-brief`…`10-sborka-qa`,
> реестр `GEJTY.md`, дерево `src/`+`slides/`. **Живой конвейер — фазы Ф1…Ф7**, его
> карта: `../KARTA-ZHIVOGO-KONVEJERA.md` (там же машиночитаемый реестр инструментов §5 и гейт
> согласованности `_generator/tools/check_karty.py`, который краснеет, когда реестр
> и диск разъезжаются). 🔴 **Отсюда НИЧЕГО не удалено:** история решений — единственное,
> чем доказывается «почему так», и стадия может оказаться живой в другом проекте.
> Помечено заходом `karta-kak-reestr` 2026-08-09; гейт согласованности архивные файлы
> не читает вовсе.


> Твой единственный файл-заход + 2 названных документа (`DOK.md`, `PRIMERY.md`, оба рядом
> с этим файлом) + `FORMAT-ISTOCHNIKA.md`. Читай ТОЛЬКО их и накопленный контекст лекции в
> разделе `## ВХОД` ниже; проект шире названного не изучай.
> План/вопросы/отчёт — в секциях внизу файла. Метрика — КАЧЕСТВО (0 overflow, глаз чист,
> канон не тронут за пределами своего слоя). Часы — норма.

КОНТЕКСТ. Фабрика научпоп-презентаций, конвейер из 10 шагов; состояние живёт на диске, не
в памяти чата. Прошлый этап (арка 6) написал плакатный текст каждого слайда лекции
`<лекция>` в `content/<id>.md`; арка 5/9 подготовила описания иллюстраций. У этой лекции уже
есть замороженный канон-скелет: `shablon.html` (движок+токены+гриды уже сделанных слайдов),
`tokens.css`, `engine.js`, `fonts/faces.css`, `brief.md`. ЦЕЛЬ этого захода: для каждого id
из таблицы `## ВХОД` разложить готовый текст и описания иллюстраций в **bespoke CSS-грид**
внутри `shablon.html` — `slides/<id>.html` с зонами, слотом `{{MD:<id>}}`, пустыми боксами
под иллюстрации, без переполнения и без правок канон-слоёв.

Приёмка — по `## ОТЧЁТ`, без построчной сверки. **Стоп до цели** (заявляю громко заранее):
получишь вёрстку слайдов этой лекции (`slides/*.html` + гриды в `shablon.html`), готовую для
арки 8 (разметка сцен `{@N}` в `content/*.md`) / арки 9 (сама графика в `data-ill`/`data-iframe`/
`data-sim`) / арки 10 (финальная сборка+QA-гейты). Ты НЕ размечаешь сцены, НЕ рисуешь
иллюстрации, НЕ выпускаешь `dist/index.html` как поставку (хотя пересоберёшь его локально
как побочный эффект гейта на каждом слайде — не удалять, но это не финальный продукт).

---

## ВХОД (Cowork заполняет ДО отправки; всё остальное в файле — неизменно для любой лекции)

**Лекция:** `<лекция>` — рабочая папка `<src-путь>` (обычно `<лекция>/src/`, путь — от корня
репозитория, там же, где лежат `dandelin/`, `buffon/`, `_studio/`).

**Уже на месте у этой лекции (арки 1–6, заморожено, не создавать заново):**
`<src-путь>/shablon.html`, `<src-путь>/tokens.css` (один `:root{}` — ПОЛНАЯ палитра+типографика
именно этой лекции: роли `--paper/--board/--card/--ink/--accent/--marker`,
`--t-lead/--t-body/--t-dense/--t-caption` — значения могут отличаться от dandelin/buffon,
роли те же), `<src-путь>/engine.js`, `<src-путь>/fonts/faces.css`, `<src-путь>/brief.md`
(id/title/canvas/`slide_order`), `<src-путь>/content/<id>.md` для каждого id ниже (текст
арки 6, ДОСЛОВНО — не переписывать при вёрстке).

**Таблица слайдов этой сессии.** Архетип-подсказка — ориентир поиска по таблице `DOK.md`
(сайдбар/рейка/плашка/доска-полоса), НЕ процент для копипаста — см. `PRIMERY.md` §4.
Иллюстрации: тип по умолчанию `data-ill` (растр/SVG, наполнит арка 9); `data-iframe` только
если явно указана 3D-глава; `<canvas data-sim>` только если явно указана симуляция.

| id | content-файл | архетип-подсказка | иллюстрации (тип · имя · что рисует) | заметки |
|---|---|---|---|---|
| `<id1>` | `content/<id1>.md` | `<напр. сайдбар-рейка ~25/74%W>` | `<data-ill · NAME · описание>` | `<напр. текста много — присмотреться к SPLIT>` |
| `<id2>` | `content/<id2>.md` | `<напр. плашка+board ~24/76%H>` | `<data-iframe · tpl-NAME · описание>` | |
| … | … | … | … | … |

**`slide_order` на момент старта** (из `<src-путь>/brief.md`, для проверки покрытия
до/после): `<вставить текущий список id через запятую>`.

---

## 0. ПЕРВЫЙ ХОД
Рабочая папка: `<src-путь>`. Прочитать ТОЛЬКО:
1. Этот файл целиком.
2. `_studio/konvejer/07-verstka/DOK.md` — спека шага 7 (контракт, гейт, ловушки, решение
   «bespoke-грид, не общие классы»).
3. `_studio/konvejer/FORMAT-ISTOCHNIKA.md` — контракт папки `src/` (что где лежит, конвенции
   имён, кто в какой слой пишет).
4. `_studio/konvejer/07-verstka/PRIMERY.md` — живые фрагменты-образцы с разбором (цитаты из
   dandelin/buffon, уже проаннотировано что канон, что нет).
5. Раздел `## ВХОД` выше + сами файлы `<src-путь>/{brief.md,shablon.html,tokens.css}` (только
   файлы СВОЕЙ лекции).

НЕ читать: скилл `html-slides-studio`, `decks/*.md`, исходники `_generator/{build_deck.py,
render.py}` целиком (нужен только CLI — он в §3 ниже, уже выверен), код других лекций
(dandelin/buffon уже процитирован в `PRIMERY.md` — не открывать их файлы повторно). Не
изучай проект шире названного.

Точка отката ДО первой правки `shablon.html` / первого файла в `slides/`: если `<лекция>/`
(или весь репозиторий) — git-репозиторий → `git add -A && git commit -m "чекпойнт перед
вёрсткой <лекция>"`; если не уверен, есть ли git — `cp -r <src-путь> <src-путь>.bak-$(date +%s)`.
Сделать ОДНО из двух, не пропускать.

ПЛАН — заполнить `## ПЛАН` (id из таблицы `## ВХОД` → архетип-решение на каждый) ДО первой
правки файлов.

## 1. ДИСЦИПЛИНА (Карпатов)
Минимум без спекуляций: текст слайда уже написан арка 6 — не переписывать формулировки
«чтобы влезло», это чужой слой (можно ТОЛЬКО перераспределить существующий текст между двумя
слайдами при SPLIT, см. §3.8 — без добавления новых предложений). Хирургия: правишь
`slides/<id>.html` + свой блок `#<id> .grid{...}` (и `#<id> .<zone>{...}`) в `shablon.html` —
ничего вокруг них.

Bespoke-грид — уже РЕШЕНО в `DOK.md` (раздел «Решение: bespoke грид, не общие классы»), не
пересматривать в этом заходе: CSS грида — point-wise на `#<id>`, НЕ вводить общие
архетип-классы (`.fr-sidebar`/`.fr-board` и т.п.), даже если кажется DRY-евее — ни dandelin,
ни buffon так не делают (проверено), и решение принято раньше тебя.

Критерий готовности (§3) должен быть способен провалиться — прогони его сам ПЕРЕД отчётом,
не после. Якорные замены: если правишь СУЩЕСТВУЮЩИЙ слайд (не создаёшь новый) — посчитай
вхождения того, что меняешь, ДО правки; результат ≠ ожидаемому → остановись, спроси в
`## ВОПРОСЫ`, не гадай. Сохранять по умолчанию: не удаляй существующие `#<other-id>` блоки,
не переименовывай чужие id.

Оспорь ложную предпосылку: если таблица `## ВХОД` расходится с тем, что реально лежит в
`<src-путь>` (заявленный `content/<id>.md` не существует; `slide_order` уже содержит id с
другим текстом; иллюстрация в таблице просит тип, который не бьётся с описанием) — не
подгоняй, останови и опиши расхождение в `## ВОПРОСЫ`, не изобретай трактовку сам.

Субагенты (если решишь дробить работу по слайдам): ≤5 параллельно, крупную пачку — в
очередь. Рейт-лимит/ошибка инструмента = отступить и доложить, не слепой ретрай.

## 2. ГРАНИЦЫ
Канон заморожен. НЕ трогать:
- `engine.js` — блок `[4]` целиком (движок, verbatim, патчи уже вшиты при сборке скелета
  этой лекции — не «улучшать», не переписывать `fitText`/`fitAll`/сцены);
- `fonts/faces.css`, `lib/three.min.js` (если есть у лекции) — verbatim;
- `tokens.css` — только ЧИТАТЬ значения (`var(--paper)` и т.п.); не добавлять новые
  переменные, не менять существующие значения;
- `shablon.html` ВНЕ своего блока `#<id> .grid{...}`: не трогать общий слой `[1] DECK
  STYLES` (`.zone`, `.fit`, `.copy`, `.pad`, `[data-scene-from]` и т.п.), не трогать `[2]
  SLIDES` разметку чужих секций, `[3] ASSETS`, `[4] ENGINE`, не трогать чужие `#<other-id>
  .grid{...}` блоки;
- `brief.md` — можно ТОЛЬКО дописать (append) новые id в `slide_order`, и только как часть
  SPLIT (§3.8); поля `id`/`title`/`canvas`/`provenance`/`provenance_sha256`/`milestone` не
  трогать;
- `content/<id>.md` — текст не переписывать (дословно из арки 6); можно физически создать
  файл или перенести часть существующего текста на два файла ТОЛЬКО в рамках SPLIT (§3.8),
  без добавления новых формулировок;
- `overlay.css`, `sims/`, `illustrations/`, `chapters/`, `drivers/` (если есть у лекции) —
  чужой слой (арка 9); заводить под них только ПУСТЫЕ ссылки (`data-ill="<NAME>"` /
  `data-iframe="tpl-<NAME>"` / `<canvas data-sim="<KIND>">`), не файлы с содержимым;
- `dist/index.html` — никогда не редактировать руками (сгенерированный файл, баннер это
  фиксирует); пересобирать только через `build_deck.py`.

Не вызывать скиллы/режимы `html-slides-studio`, `frontend-design`, `web-typography`,
дизайн-режим — это свободный режим ВНЕ этого конвейера (`DOK.md`, раздел «Внешнее»); канон
уже задан `shablon.html`/`tokens.css` этой лекции, задача — раскладка В НЁМ, не
переизобретение стиля.

## 3. ЗАДАЧА
Для каждого id из таблицы `## ВХОД` (в порядке `slide_order`): разложить готовый текст и
описания иллюстраций в bespoke CSS-грид — выдать `slides/<id>.html` (если слайда ещё нет)
или поправить его (если есть — тогда доп. гейт Р1, см. ниже) + добавить/поправить
`#<id> .grid{...}` в `shablon.html`.

**Цикл на каждый слайд:**
1. Взять текст `<src-путь>/content/<id>.md` как есть (обычно уже существует — арка 6 его
   создала; если нет — создать файл с текстом ИЗ таблицы `## ВХОД`, тоже дословно) + архетип-
   подсказку + описания иллюстраций оттуда же.
2. Выбрать раскладку по семье архетипа (см. `PRIMERY.md` §4 — сайдбар/рейка, плашка+board,
   доска-полоса и т.п.), но CSS писать **point-wise на `#<id>`** (§1) — таблица DOK лишь
   ориентир поиска, реальные проценты/px мерятся под объём ИМЕННО этого текста/иллюстрации
   (`PRIMERY.md` §4 показывает разброс ±2–3 п.п. между слайдами одной семьи — это норма).
3. Роли: display-элементы (Forum caps, крупные цифры, рубрики-заголовки) → `.fit` +
   `data-max` на самом элементе, БЕЗ паддинга на зоне, где он сидит (`PRIMERY.md` §2–3);
   основной текст → `.zone` с фиксированным `--t-body` слайда (паддинг `.pad` можно, если в
   этой зоне НЕТ `.fit`); слот текста — `{{MD:<id>}}` внутри `.copy`/аналогичного контейнера.
   Иллюстрации — пустые боксы: `data-ill="<NAME>"` по умолчанию / `data-iframe="tpl-<NAME>"`
   только если во входе явно 3D-глава / `<canvas data-sim="<KIND>">` только если во входе
   явно симуляция; имя `<NAME>`/`<KIND>` — из таблицы `## ВХОД`, содержимое НЕ создавать (это
   арка 9) — бокс остаётся пустым.
4. Линтер (дёшево, без браузера): `python3 ../disciplina/_generator/build_deck.py <src-путь> --lint` → 0
   нерезолвленных `{{...}}`; каждый `data-ill`/`data-iframe` резолвится в существующий файл
   ИЛИ явно варнинг «ждёт арку 9» (не фейл на этой стадии); `slide_order` без сирот/дублей.
5. Полная сборка: `python3 ../disciplina/_generator/build_deck.py <src-путь>` → пишет
   `<src-путь>/dist/index.html`.
6. Глаз: `python3 _generator/render.py <src-путь>/dist/index.html _render/<лекция>` — снимает
   ВСЕ слайды PNG (`_render/<лекция>/NN.png`, `NN` — позиция id в `slide_order`, счёт с нуля);
   открыть PNG нужного id. Быстрее для одного слайда без пересъёмки всех — открыть в Chrome
   напрямую `file://<абсолютный путь к src-путь>/dist/index.html?only=<NN>&scene=99` (тот же
   0-индекс позиции, `scene=99` = показать слайд в финальной сцене). **Важно:** у `render.py`
   нет CLI-флага `--only` — `?only=` это URL-параметр самого скрипта при навигации, не
   аргумент командной строки; не вызывать `render.py ... --only N` (упадёт: скрипт примет
   `--only` за путь к деку). Если в этой среде нет Chrome/Playwright (`pip install playwright
   && playwright install chrome` не стоит) — это СТОП ДО ЦЕЛИ: написать явно в `## ОТЧЁТ`,
   что глаз не прогонялся и почему, не пропускать шаг молча.
7. Если видно переполнение (или есть подозрение по объёму текста) — чинить СТРОГО по
   порядку, останавливаясь на первом шаге, который решает проблему:
   a. блок-гэп/отступ между абзацами (свой `#<id> .copy p+p{margin-top:…}` override, не
      трогая общий слой);
   b. отступ формулы/конкретного блочного элемента (`margin-top`);
   c. перераспределение `grid-template-rows`/`columns` (доли/px/`fr`; на ВЛОЖЕННОМ гриде
      внутри зоны — `minmax(0,1fr)` или явный px, НЕ голый `%` рядом с `auto`-рядом — иначе
      % молча схлопывается в размер контента, см. ловушку и живой аналог `min-height:0` в
      `PRIMERY.md` §4; на верхнеуровневом `#<id> .grid{position:absolute;inset:0}` голый `%`
      безопасен, т.к. высота слайда 810px уже определена, но по умолчанию предпочитать
      px/fr);
   d. ТОЛЬКО в конце, и только если элемент несёт `.fit` — довериться его бинарному поиску
      (`data-max`/опц. `data-min` на самом элементе); это не «уменьшение руками», а штатная
      работа фиттера.
   **Никогда:** не трогать `--t-body` слайда как приём почини overflow (можно было выбрать
   ДРУГОЕ значение `--t-body` для этого слайда ДО того, как текст лёг, — это первичный выбор
   кегля, не починка постфактум; реальные деки живут с разбросом `--t-body` 32–41px по
   слайдам — это диапазон осознанного выбора, не дрейф, см. `PRIMERY.md` §1). Не заводить
   новую уменьшенную типографическую роль (аналог `.t-small`) как обход overflow именно у
   `.copy` этого слайда — см. `PRIMERY.md` §5: это найдено как отдельная роль (сноска), не
   как приём починки, и путать одно с другим — запрещённый паттерн.
8. Не влезло даже после (d) → SPLIT: разбить слайд на `<id>` + `<id>b` (буквенный суффикс —
   уже живая конвенция дандел ина: `s05`→`s05a`/`s05b`/`s05c`, `s07`→`s07b`, `s09`→`s09b`/
   `s09p`, `s11`→`s11b`, см. `slide_order` в `dandelin/src/brief.md`), перенести ЧАСТЬ
   существующего текста ДОСЛОВНО (без перефраза, без новых предложений) во второй
   `content/<id>b.md`, добавить `<id>b` в `slide_order` СРАЗУ ПОСЛЕ `<id>` (append, остальной
   список не трогать), пересобрать и прогнать шаги 1–7 для ОБОИХ получившихся слайдов.
   Залогировать SPLIT в `## ОТЧЁТ` явно: было `<id>` → стало `<id>`+`<id>b`, что именно ушло
   во второй файл.
9. Следующий id из таблицы.

**Правка СУЩЕСТВУЮЩЕГО слайда** (не создание нового) — доп. гейт Р1 (render-identity): перед
правкой сохранить нормализованный текст текущего `dist/index.html` (снять HTML-комментарии,
схлопнуть пробелы) или снимок PNG (`render.py`); после правки убедиться, что отличается
ТОЛЬКО ожидаемый id — сверить `render.py --compare <до>.html <после>.html
_render/<лекция>-diff` (попиксельный дифф, если есть Pillow). Гейт: 0 неожиданных изменений
на других id.

**КРИТЕРИЙ ГОТОВНОСТИ** (проверяемый, способен провалиться — прогнать на каждый id из
таблицы `## ВХОД`, не только на последнем добавленном):
- на каждый id есть `<src-путь>/slides/<id>.html` с `<section class="slide" id="<id>"
  data-scenes="N">`, хотя бы одной `.zone`, слотом `{{MD:<id>}}` внутри неё, и пустыми
  `data-ill`/`data-iframe`/`data-sim` боксами на каждую заявленную иллюстрацию (боксы пустые
  — это ожидаемо, содержимое даёт арка 9);
- на каждый такой id есть свой блок `#<id> .grid{...}` (+ сопутствующие `#<id> .<zone>{...}`)
  в `<src-путь>/shablon.html`, bespoke, без переиспользования общих архетип-классов;
- `python3 ../disciplina/_generator/build_deck.py <src-путь> --lint` — 0 фейлов: нерезолвленных `{{...}}` =
  0, `slide_order` точно покрывает `slides/`+`content/` без сирот/дублей;
- 0 overflow `.zone` — проверено глазом на PNG/скриншоте (`render.py`), для КАЖДОГО
  затронутого id, не «на вид похоже без проверки»;
- `--t-body` слайда НЕ уменьшался в процессе починки overflow ни разу (см. §3.7 — выбор ДО
  вёрстки допустим, правка ПОСЛЕ — нет);
- фон-заливки — только `var(--paper)`/`var(--board)`/`var(--card)` (или другие `:root`-
  переменные ИМЕННО `<src-путь>/tokens.css` этой лекции), 0 произвольных hex/rgb/градиентов/
  теней/radius;
- ровно один `.fit` на зону (если зона вообще использует `.fit`) — не два в одной зоне;
- новые/изменённые `grid-template-rows` на вложенных гридах — без голого `%` рядом с
  `auto`-рядом (§3.7c);
- если были SPLIT — каждый залогирован в `## ОТЧЁТ` с исходным/новыми id и что перенеслось;
- если правился существующий слайд — гейт Р1 выше зелёный.

Не выполнено хотя бы одно — заход не закрыт: писать в `## ОТЧЁТ`, что именно и почему, а не
молчать/дотягивать вручную до вида «похоже готово».

## 4. ВЕРИФИКАТОР
Заход мутирующий (правишь `slides/<id>.html`+блок в `shablon.html`), но отдельный субагент-верификатор (шаблон RUKOVODSTVO §4) избыточен: гейт render-identity (Р1, см. §3) уже закрывает эту роль при правке СУЩЕСТВУЮЩЕГО слайда — посимвольно/попиксельно сверяет с оригиналом и ловит любое неожиданное изменение на чужих id. Для НОВОГО слайда сверять не с чем (оригинала нет, терять нечего) — там верификация исчерпывается линтером + глазом из критерия §3, тот же принцип, каким это обосновано в `08-sceny/ZAHOD.md`/`10-sborka-qa/ZAHOD.md`.

## 5. ОТЧЁТ
В секцию `## ОТЧЁТ` внизу: что сделал (список id → что получилось, зачем такой архетип-
выбор, если неочевидно из таблицы `## ВХОД`) / как проверил КАЖДЫЙ пункт критерия §3 —
командами, не словом «проверил» / что НЕ трогал (границы §2 соблюдены — явно) / SPLIT, если
были, с деталями / вопросы без ответа → `## ВОПРОСЫ` / результат гейта Р1, если правил
существующий слайд / **время прогона** (часы — норма, не повод срезать проверки) и
количество вызовов инструментов (токены не видны самому себе — отметь хотя бы это).

---

## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
