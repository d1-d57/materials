# GEJTY — реестр гейтов конвейера (данные для трекера)

> 🗄 **АРХИВ — конвейер до 27.07.2026.** Файл описывает СТАРЫЙ конвейер: 10 стадий `01-brief`…`10-sborka-qa`,
> реестр `GEJTY.md`, дерево `src/`+`slides/`. **Живой конвейер — фазы Ф1…Ф7**, его
> карта: `KARTA-ZHIVOGO-KONVEJERA.md` (там же машиночитаемый реестр инструментов §5 и гейт
> согласованности `_generator/tools/check_karty.py`, который краснеет, когда реестр
> и диск разъезжаются). 🔴 **Отсюда НИЧЕГО не удалено:** история решений — единственное,
> чем доказывается «почему так», и стадия может оказаться живой в другом проекте.
> Помечено заходом `karta-kak-reestr` 2026-08-09; гейт согласованности архивные файлы
> не читает вовсе.


> **Когда читать:** строишь трекер-скрипт (заход ③) или каталог (заход ④); нужен полный список дверей фабрики и способ проверить каждую по диску. · **Что это:** структурная таблица — КАЖДЫЙ гейт как запись `id · стадия/граница · что проверяет · признак-на-диске · тип · возврат-разрешён`, которую скрипт-трекер проверяет автоматически (или помечает браузерным для ручного прогона). НЕ алгоритм-проза (та — `ALGORITM.md`) и НЕ реализация трекера (та — заход ③, вне этого файла). · **Куда дальше:** драйвер, который эти гейты вызывает — `ALGORITM.md`; формат Фазы II — `FORMAT-ISTOCHNIKA.md`.

## Как читать эту таблицу
- **тип** — `жёсткий-стоп-вперёд` (блокирует переход на следующий шаг, пока не зелёный) · `автор-чекпоинт` (требует вкуса/взгляда владельца, механически не решается) · `дисциплина-хук=WARN` (предупреждает по ходу работы ДО гейта-границы, не блокирует). Гейт может нести оба типа сразу (авто-часть + чекпоинт-часть) — тогда указаны через `+`.
- Пометка **[браузерный]** — трекер (заход ③) не проверяет автоматически в CI/скрипте без браузера; выносит отдельным пунктом «прогнать локально у владельца» (Playwright/`render.py`/`audit.py`).
- **возврат-разрешён** — да/нет по смыслу «гейты не рельсы» (`ALGORITM.md §Модель исполнения`): да означает, что провал этого гейта — штатный повод для волны назад по конвейеру, а не тупик.
- ✅ **Размещение Фазы I РЕШЕНО** (Cowork, консолидация Волны 1): канон — `../docs/spravka/PAPKA-LEKCII.md` (`brief.md` в корне · `kartoteka/KARTA-OBLASTI.md` · `istochniki/{MANIFEST,VYCHITANO,TERMINY}.md` · `kotly/matematika.md` · `kotly/naupop.md` · `chernovik/rasskaz.md`). Признаки-на-диске ниже приведены к нему. Слой `skelet` физически = **`kotly/matematika.md`** (математика-котёл, растущий в строгий слой, Р19); `chernovik/skelet.md` — опциональный замороженный снимок сборки 4b, по умолчанию цитируется `kotly/matematika.md`.

> ✅ **ДОЛГ ЗАКРЫТ 28.07.2026** (заход `../zhurnal/2026-07-27_paskal-lekcia-sborka/kod_gejty-shaga-6.md`). `G7` переписан в трекере под ленту, `G12`–`G15` и `H6` реализованы; у каждой проверки есть фикстура, на которой она ПАДАЕТ — `../../_generator/tools/fixtures/sostoyanie/` + `PROGNAT.sh` (гонять после любой правки `sostoyanie.py`). Статус проверок не выписывать руками, а смотреть прогоном: `python3 _generator/tools/sostoyanie.py <лекция>`.
>
> ⚠ **Четыре места, где реализация отступает от буквы этого файла, и почему.** Отступления помечены `⚠ реализация` прямо у своих записей ниже. Общая причина одна: трекер судит коммиты троих писателей, и гейт, краснеющий на здоровом деке, обходят через `--no-verify` — тогда пропадает ВСЯ защита, а не одна проверка. Каждое отступление измерено прогоном по всем живым декам репозитория, число ложных ❌ названо. Буквальные формулировки НЕ вычеркнуты: если решение окажется неверным, спор идёт с текстом, а не с памятью.

> 🔴 **КАК ПИСАТЬ «признак-на-диске» (закон, оплачен 28.07.2026).** Признак пишется НЕ по памяти об инциденте, а **грепом по всем живым декам ТЕМ ЖЕ ходом**, и число совпадений вписывается в запись. Формулировка, верная как описание прошлой поломки, почти всегда неверна как условие на диск: она называет то, чего в репозитории нет, или то, что есть у здоровых. *Цена: четыре из шести признаков, записанных 27.07 «по инциденту», при буквальной реализации дали бы **24 ложных ❌ по G14, ❌ по G15 на всех четырёх живых деках, 2 по G13 и 12 разделов из 14 по G7** — при нуле настоящих долгов среди них. `G13` требовал атрибут `data-scene`, которого в репозитории ноль вхождений; `G15` требовал `.scene-1`, которого не бывает по устройству движка; `G7` требовал ≤1 акцента, а `06-tekst/DOK.md` тем же файлом велит набирать вопрос-заголовок жирным целиком. Гейт, краснеющий на здоровом деке, обходят `--no-verify` — и пропадает вся защита, а не одна проверка.*

> **Пределы реализации, названные честно (28.07.2026, заход `kod_gejty-shaga-6`).** Не «дыр не найдено», а «найдено три, вот они»:
> 1. **`G7` = N/A не влияет на код возврата** `sostoyanie.py`. Дек, проскочивший шаг 6, печатает `⚠`, но даёт `exit 0` — хук, судящий по коду возврата, его пропустит. Менять семантику кода возврата — решение уровня конвейера, не одной проверки.
> 2. **`G15` не видит каскад вне `src/*.css` и `shablon.html`.** Если каскад живёт в `<style>` внутри `slides/*.html` или порождается в `dist/`, проверка уходит в N/A и честно печатает, что полноту НЕ подтверждает.
> 3. **`H6` не отличает сгенерированный каскад от рукописного** — маркера порождённого блока в источнике не существует. Значит после перехода на генерацию хук продолжит WARN-ить, и объявленная им цель «ноль совпадений» недостижима, пока `build_deck.py` не начнёт помечать порождённый блок. Долг генератора.

## Гейты-границы Фазы I (G1–G5 + G7 + G16)

> ⚠ **G7 переехал в Фазу I** (решение 27.07.2026, `00-KONVEJER.md`): шаг 6 — последний авторский чекпоинт Фазы I, а не первый автоматический шаг Фазы II. **Id гейта НЕ переномерован** — на `G7` ссылаются снаружи; переехал только раздел и смысл. Порядок прохождения: G1 → … → G4 → **G16** (фаза 4.5, блокирующая) → G5 → **G7** → G6 (вход Фазы II) → G8 → …

### G1 · бриф закрыт (граница 1→2)
- **что проверяет:** бриф полон (8 полей: тема · аудитория · длительность→бюджет слайдов · throughline-кандидат · что резать · материал/референс · уровень строгости · формат события+лекция/курс); throughline назван ОДНОЙ фразой; бюджет слайдов посчитан арифметически (не угадан).
- **признак-на-диске:** `<лекция>/brief.md` существует; содержит 8 непустых полей (точные ключи фиксирует `01-brief/ZAHOD.md`, напр. `тема:`,`аудитория:`,`длительность:`,`бюджет_слайдов:`,`throughline:`,`что_резать:`,`материал:`,`строгость:`,`формат_события:` — свериться при первом деке); поле `throughline:` — однострочное значение; поле `бюджет_слайдов:` — целое число, кратное делению длительности на 2.
- **тип:** жёсткий-стоп-вперёд
- **возврат-разрешён:** да (throughline не подтверждается ресёрчем/раскадровкой → правка брифа, контракт B14)

### G2 · ресёрч закрыт (граница 2→3)
- **что проверяет:** ≥5 разных источников; ни одной находки без карточки; в материале реально есть «но» (разворот, не пересказ).
- **признак-на-диске:** в `<лекция>/kartoteka/KARTA-OBLASTI.md` каждый блок-карточка рода `находка|утверждение|биекция|мостик` содержит непустое поле `источник:` (0 карточек без источника при грепе блоков); ≥5 уникальных значений `источник:` (по префиксу файла/книги/URL); поле `статус: на-сверку` ИЛИ явная текстовая пометка разворота присутствует хотя бы у одной карточки.
- **тип:** жёсткий-стоп-вперёд (источники+карточки — авто) + автор-чекпоинт (честность разворота «но» — вкус, не грепается надёжно)
- **возврат-разрешён:** да (разворота нет → тема нечестно не тянет, чекпоинт владельцу B8)

### G3 · математика верна (граница 3→4)
- **что проверяет:** 0 открытых `⚑ Флаг`; уровень-тег на каждом мат-блоке; нотация единообразна по всему потоку; термины сверены с `TERMINY.md`; adversarial ре-ревью пройдено.
- **признак-на-диске:** grep `⚑ Флаг` по `<лекция>/kotly/matematika.md` → каждое совпадение имеет ниже по файлу парный `Флаг закрыт:` (0 непарных); grep `Утверждение|Определение|Лемма|Следствие|Теорема` → в пределах следующих ~5 строк каждого совпадения есть `> поле:уровень`.
- **тип:** жёсткий-стоп-вперёд (флаги+уровень-тег — авто) + автор-чекпоинт (adversarial ре-ревью, нотация единообразна — вкус/внимательное чтение)
- **возврат-разрешён:** да (флаг не закрыт/нотация плывёт → доработка внутри арки 3, не выход из неё)

### G4 · источник закрыт (граница 4→5)
- **что проверяет:** 0 открытых `⚑ Флаг`; математика в видимом слое в финальном объёме; throughline держит от начала до конца; каждый такт имеет 🖼-описание; цитаты-сноски резолвятся в реальные `skelet`-якоря (0 сирот).
- **признак-на-диске:** grep `⚑ Флаг` по `<лекция>/chernovik/rasskaz.md` → 0 непарных; grep `🖼` по `chernovik/rasskaz.md` → число вхождений ≥ число тактов нити (сверить со списком тактов в карте); каждая цитата-сноска в `chernovik/rasskaz.md` (напр. `[skelet#<якорь>]`) резолвится в существующий якорь слоя skelet `kotly/matematika.md` (0 висячих ссылок).
- **тип:** жёсткий-стоп-вперёд (греп-часть) + автор-чекпоинт (полнота/нарратив — B10, механический греп и человек ловят разное)
- **возврат-разрешён:** да (throughline не держит / дыра в ресёрче → назад в арку 2/4a, добор, не выдумка)

### G16 · контракт интервью утверждён владельцем (граница 4→5, фаза `04.5-intervyu/`)
- **что проверяет:** восемь пунктов гейта арки 4.5 (`04.5-intervyu/DOK.md §Контракт`): (1) файл `KONTRAKT-*.md` существует и его `status:` не `na-utverzhdenii`; (2) в части 2 число карточек единиц выхода **больше нуля**, и у каждой заполнены `ВОПРОС` и `ОТЛИЧИЕ`; (3) **названия единиц выхода предъявлены владельцу строкой в чате**, не ссылкой на файл; (4) габарит в части 0 подкреплён **командой замера**, а не числом из головы; (5) каждый открытый пункт части 3 несёт дату; (6) в журнале арки есть `INTERVYU.md` с записью не менее одного круга; (7) **каждый круг в `INTERVYU.md` содержит не менее одной дословной реплики владельца**; (8) **`ANKETA.md` выросла** между заведением `INTERVYU.md` и закрытием G16.
- **признак-на-диске:** существует `<проект>/KONTRAKT-*.md`; его фронтматтер содержит `status:` со значением **не** `na-utverzhdenii` (grep `^status:` по файлу); в файле есть раздел части 2 и в нём ≥1 карточка, у каждой — непустые `ВОПРОС` и `ОТЛИЧИЕ` (grep `ВОПРОС` и `ОТЛИЧИЕ`, число совпадений каждого == числу карточек); в части 0 строка габарита несёт исполнимую команду замера (grep блока с габаритом на наличие ``` `…` ``` с `python3`/`grep`/`find`/`wc`); каждый пункт части 3 содержит дату вида `\d\d\.\d\d\.\d{4}` (0 пунктов без даты); существует `<арка>/INTERVYU.md` непустой; **(7)** число кругов = БОЛЬШЕЕ из `grep -c '^## Круг ' <INTERVYU.md>` и `grep -cE '^\| *[0-9]+ *\|' <INTERVYU.md>` (секции ИЛИ строки таблицы кругов — оба живых формата протокола); число цитат = БОЛЬШЕЕ из `grep -c '^> «' <INTERVYU.md>` и `grep -o '«[^»]*»' <INTERVYU.md> | wc -l` (цитата отдельной строкой ИЛИ инлайн в прозе); проверка: **кругов > 0** И **цитат ≥ кругов** (кругов = 0 — это FAIL, не вырожденный PASS: раньше `0 ≥ 0` зеленело вхолостую, см. заметку 2026-08-05 ниже); **(8)** `grep -c '^| [A-J][0-9]' ANKETA.md` замерен при заведении `INTERVYU.md` и повторно при закрытии G16, второе число **строго больше** первого (равенство = ❌).
- **тип:** **автор-чекпоинт, БЛОКИРУЮЩИЙ** (главное — содержание контракта, а его механически «хороший от плохого» не отличить: гейт закрывается СЛОВОМ владельца) + жёсткий-стоп-вперёд (наличие файла, `status:`, полнота карточек, даты открытых пунктов, наличие `INTERVYU.md`, **дословные реплики по кругам (7), рост анкеты (8)** — авто). Пока G16 красный, **арки 5–10 не стартуют**. Пункты 7 и 8 — **авто, в отличие от пункта 3** (названия в чате), который механически не проверяется вовсе.
- ⚠ **пункт 3 механически не проверяется вовсе.** «Названия предъявлены владельцу в чате» на диске следа не оставляет: чат — не файл. Держится на честности интервьюера и на том, что владелец сам заметит непоказанные названия (что и произошло 05.08 — ценой шести актов репетиции с одним похваленным заголовком). Заводить признак «по памяти об инциденте» здесь запрещено законом шапки: признака нет — так и записано, а не выдуман.
- 🔴 **Основание пункта 7 (дословная реплика в каждом круге).** На пункте анкеты **H2** («решение владельца записывается дословной цитатой») висит **6 уроков из пяти арок** — самый нагруженный рычаг фазы (`04.5-intervyu/POKRYTIE.md`), и держится он только на записи в анкете. По собственному закону репозитория запись в канон не лечит: пересказ вместо цитаты уже стоил полного прогона не туда («микшеры вынести направо» → «все ползунки в левую колонку»). Пункт 7 переносит рычаг с анкеты на гейт — единственный доступный механический след цитаты — строка `> «…»` в круге.
- 🔴 **Основание пункта 8 (анкета выросла).** Правило дополнения анкеты — из `04.5-intervyu/DOK.md §Анкета`: интервью, не давшее ни одного нового вопроса, скорее всего было поверхностным, а не исчерпало анкету. ⚠ **Оговорка честности: гейт проверяет ФАКТ дописывания строки, а не качество вопроса.** Дописанный ради зелёного пустой пункт пройдёт проверку; ни один греп не отличит вопрос, найденный в живом разговоре, от вопроса, выдуманного под гейт. Признака на качество нет — так и записано, а не выдумано.
- 🔴 **Починка вакуума 2026-08-05, заход `kod_dovodka-fazy-intervyu.md`.** Буквальный признак пункта 7
  (`grep -c '^> «'` и `grep -c '^## Круг '`) прогнан на единственном живом `INTERVYU.md`
  (`_studio/zhurnal/2026-07-30_dovodka-fabriki/INTERVYU.md`) и дал **`0 ≥ 0` — вакуумно зелёный**:
  протокол записан таблицей кругов с цитатами инлайн в прозе, а не секциями `## Круг N` с цитатой
  строкой `> «…»` (обнаружено заходом `kod_kartoteka-intervyu.md`, 2026-08-05). `INTERVYU.md` — НЕ в
  зоне этого захода (только `04.5-intervyu/` и этот файл), переписывать его нельзя; правится признак.
  Новый признак п.7 (см. выше) распознаёт ОБА формата и требует `кругов > 0`, что убивает вырожденный
  `0 ≥ 0`. **Прогнано ДВАЖДЫ, оба результата — реальные exit-коды bash, не догадка:** на живом файле
  кругов=6 (по строкам таблицы), цитат=16 (по инлайн-«») → `[ 6 -gt 0 ] && [ 16 -ge 6 ]` → **rc=0,
  PASS**; на синтетической фикстуре о трёх круглах и нуле цитат (не в репозитории, только для проверки
  признака) → `[ 3 -gt 0 ] && [ 0 -ge 3 ]` → **rc=1, FAIL** — признак действительно способен покраснеть.
  ⚠ **Честная граница нового признака:** `«…»` считает ЛЮБОЙ гильеметный фрагмент текста, не только
  реплику, помеченную словом «Владелец:» — на живом файле 16 фрагментов «»́, из них явно подписанных
  «Владелец: *«…»*» — один (строка 64), остальные — имена гипотез/механизмов/решений в кавычках без
  явной атрибуции. Признак НЕ отличает цитату речи от закавыченного термина; это тот же класс
  оговорки, что уже стоит у пункта 8 ниже («факт дописывания, не качество»). Точнее без разбора чата
  не сделать, а разбор чата — не грепается.
  **Пункт 8 НЕ был вакуумным** — предпосылка захода здесь неточна, проверено git-историей, а не
  памятью: `ANKETA.md` при коммите `75667fd` (заведение `INTERVYU.md`, 2026-08-05 00:56) несла **59**
  строк `^| [A-J][0-9]`, на `HEAD` — **63** (`git show 75667fd:…/ANKETA.md \| grep -c` и то же на
  `HEAD`) — рост реальный, не `0 ≥ 0`. Прогнано тем же методом дважды: реальные числа `59 < 63` →
  `[ 63 -gt 59 ]` → **rc=0, PASS**; синтетический кейс «до == после» (тот же файл сам с собой) →
  `[ N -gt N ]` → **rc=1, FAIL**. Признак п.8 не изменён — он и так не вырожден; уточнение внесено,
  чтобы не унести неверную посылку дальше по цепочке заходов. ⚠ Рост анкеты в этом случае пришёл не из
  ЖИВОГО разговора `INTERVYU.md`, а из параллельного пересчёта корпуса (`J17/J18/H9/H10` из
  `POKRYTIE.md`) — признак судит ФАКТ роста файла, не его причину, и это та же оговорка честности,
  что стоит строкой ниже.
- ⚠ **`KONTRAKT-*.md` не существует вовсе → N/A «дек шёл по схеме до 05.08.2026», НЕ FAIL** — по образцу N/A у G7. Фаза заведена 2026-08-05, старые деки её не проходили, и гейт, краснеющий на здоровом деке, обходят `--no-verify` (тогда пропадает вся защита, а не одна проверка). Файл есть, а части 2 в нём нет / карточек ноль → FAIL: контракт заведён и не наполнен. Чтобы N/A не прятал пропущенную фазу, при начатой арке 5 (существует `<проект>/src/slides/`) печатается `⚠ дек в раскадровке без утверждённого контракта интервью`.
- 🔴 **Замер 05.08.2026, тем же ходом (закон «признак пишется грепом»):** `find <дек> -name 'KONTRAKT-*.md'` — `teorkat-vvedenie` **1** (`teorkat-vvedenie/KONTRAKT-L2.md`), `buffon` **0**, `dandelin` **0**, `fibonacci` **0**; по всему репозиторию **1**. `find . -name 'INTERVYU.md'` — **0** по всему репозиторию (журнал интервью пока не заведён ни в одной арке). То есть на трёх старых деках гейт даёт **N/A, 0 ложных ❌**, а на единственном живом контракте он **красный по существу и это верно**: `KONTRAKT-L2.md` несёт `status: na-utverzhdenii` (пункт 1) и парного `INTERVYU.md` у него нет (пункт 6) — контракт действительно ещё не утверждён.
- **возврат-разрешён:** да, и это **нормальный режим**: кругов интервью может быть сколько угодно (на живой сессии 05.08 — шесть, и шестой ещё давал новое). Признак последнего круга — «не знаю, как ответить» или повтор владельца; пока их нет, следующий круг обязателен. Провал G16 — повод вернуться в арку 2/4 за добором, а не тупик.

### G16.9 · иллюстрация в KONTRAKT несёт ТЗ, а не просьбу (черновик, граница 4→5, фаза `04.5-intervyu/`)
> 🔴 **Перенесено 2026-08-05** (заход `kod_dovodka-fazy-intervyu.md`) из черновика, оставленного заходом
> `kod_kartoteka-intervyu.md` в `~/Documents/GitHub/disciplina/korpus/2026-07-30_dovodka-fabriki/KARTOTEKA-problem.md` («Часть
> D-примечание»). Рычаг для урока-без-рычага `АНКЕТА G2` (6 уроков подкласса «иллюстрации без ТЗ»,
> `04.5-intervyu/POKRYTIE.md`): «иллюстрация заказывается просьбой, а не описанием» — 34 фигуры → 11,
> девять слайдов из пятнадцати без иллюстраций, включая все четыре заказанные.
- **что проверяет:** если карточка единицы выхода (часть 2 контракта `KONTRAKT-*.md`) отмечена как
  нуждающаяся в иллюстрации, у неё заполнено ТЗ — что изображено · сколько объектов и как подписаны ·
  что происходит · чего рисовать НЕ надо · размер (пять полей `АНКЕТА G2`, дословно).
- **признак-на-диске (черновик — поле в карточке ещё не заведено, см. замер ниже):** в карточке части 2
  поле `ИЛЛЮСТРАЦИЯ: да` ⇒ в тех же ~10 строках непустое поле `ТЗ:`, и внутри него ≥3 из пяти
  маркеров-подпунктов (что/сколько/не-рисовать/размер/подписи).
- **тип:** жёсткий-стоп-вперёд (черновик; полей `ИЛЛЮСТРАЦИЯ:`/`ТЗ:` в формате карточки `DOK.md §Контракт
  часть 2` сегодня нет вовсе — гейт нечем проверять, пока поле не заведено)
- ⚠ **Не внедрён — честный N/A на всех живых деках, не выдуманный признак.** `DOK.md §Контракт часть 2`
  сегодня несёт только `ВОПРОС · ОТЛИЧИЕ · ОПИРАЕТСЯ · ВВОДИТ · ОТКУДА · ВРЕМЯ` — поля `ИЛЛЮСТРАЦИЯ:`/
  `ТЗ:` нет в формате карточки нигде. Первый шаг — не гейт, а поле в `DOK.md` (правка вне зоны захода
  `kod_dovodka-fazy-intervyu.md`, предложена аналитику через `## ВОПРОСЫ` того захода).
- 🔴 **Замер 2026-08-05, тем же ходом (закон «признак пишется грепом»):** `grep -rc
  '^ИЛЛЮСТРАЦИЯ:\|^ТЗ:' teorkat-vvedenie buffon dandelin fibonacci` → **0 совпадений во всех четырёх**
  живых декaх (пересчитано этим ходом, совпадает с замером захода `kartoteka-intervyu`) — поле нигде не
  существует, значит гейт был бы **N/A на всех живых деках, 0 ложных ❌**, но и толку ноль, пока поле не
  заведено в формате карточки.
- **возврат-разрешён:** да (ТЗ неполно → доработка карточки внутри интервью, не тупик)

### G5 · раскадровка закрыта (граница 5 → 6, внутри Фазы I)
- **что проверяет:** `slide_order` покрывает `slides/` без пропусков/сирот; на каждый слайд заполнены archetype+дистилляция+сцены+илл.-описание; регистр+`word_budget_per_slide` зафиксированы явно в `brief.md`.
- **признак-на-диске:** `<лекция>/src/brief.md` фронтматтер содержит непустой список `slide_order:`; для каждого `<id>` из списка существует файл `<лекция>/src/slides/<id>.html`; множество id из `slide_order` == множеству basename-файлов `slides/*.html` (0 сирот/дублей — тот же чек, что линтер арки 10 §1); `brief.md` содержит непустые `register:` и `word_budget_per_slide:`.
- **тип:** жёсткий-стоп-вперёд
- **возврат-разрешён:** да (throughline не режется на слайды чисто → назад в арку 1/4, эхо B14)

### G7 · предвёрсточный документ принят (граница 6 → вход Фазы II, **конец Фазы I**)
- **что проверяет:** 10 пунктов гейта арки 6 — объём в бюджете регистра (сверен по живому слайду, не по знакам в вакууме); **каждая формулировка полна, доказательство может отсутствовать** (проверка по СТАТУСУ куска, не «читается без лектора» — та формулировка отменена 27.07.2026); архетипный паттерн; акцент экономен (0–1 `**acc**`); без подписи-описания илл. в теле текста; сцены шорткатами, не дублированием; формулы перенесены, не передоказаны; не влезло после честного сжатия → SPLIT или канвас, не обрывок; раскладка проставлена каждому слайду и не противоречит роду иллюстрации; **лента собрана и прочитана целиком** — нарратив не провисает между слайдами.
- **признак-на-диске:** существует непустой `<лекция>/raskadrovka/teksty/*.md` (≥1 файл с фронтматтером `tab:`); число разделов `^## ` по всем файлам ленты == длине `slide_order` в `src/brief.md`; существует `<лекция>/raskadrovka/teksty/view.html` и он НЕ старше самого свежего `*.md` рядом (`find teksty -name '*.md' -newer teksty/view.html` — пусто, иначе лента не пересобрана); в каждом разделе, **кроме первого (раздел-обложка)**, есть строка `поле:mn **Раскладка.**` — у обложки устройство фиксировано и G12 требует от неё голоты, раскладке там нечего решать (освобождено 28.07.2026 по вопросу исполнителя: иначе вечное ложное ❌ на каждой будущей ленте); grep `**` — не более одного акцент-блока на раздел (эвристика); подсчёт слов раздела укладывается в `word_budget_per_slide`±допуск из `brief.md`.
- **тип:** **автор-чекпоинт** (главное — вкус и чтение ленты целиком; механически не решается) + жёсткий-стоп-вперёд (бюджет слов, свежесть `view.html`, покрытие `slide_order`, наличие раскладки — авто)
- ⚠ **реализация (28.07.2026):** жёстко проверяются ровно четыре пункта, перечисленные в поле «тип» выше; «≤1 акцент-блок» вынесен в `⚠`-строку, не красит. Причина: сам признак помечает его «(эвристика)», в списке жёстких его нет, а на эталонной ленте Паскаля 12 разделов из 14 несут 2–3 `**…**` — потому что жирным набран вопрос-заголовок целиком, чего `06-tekst/DOK.md` прямо требует. Жёсткая версия дала бы 12 ложных ❌ из 14 на единственном существующем эталоне.
- ⚠ **ленты нет вовсе** (`raskadrovka/teksty/` не заведена) → **N/A** «шаг 6 шёл по старой схеме», НЕ FAIL: `bootstrap_lekcia.py` создаёт `raskadrovka/` с `.gitkeep`, но `teksty/` не создаёт, значит её появление — намеренный акт шага 6 по новой схеме. Старые деки (`buffon`, `dandelin`, `fibonacci`) уходят в N/A. Папка есть, а `*.md` с `tab:` в ней нет → FAIL: лента заведена и не наполнена. Чтобы N/A не прятал пропущенный шаг, при начатой Фазе II печатается `⚠ дек в Фазе II без предвёрсточного документа`.
- **возврат-разрешён:** да, и это **нормальный режим**: шаг 6 интерактивен, кругов правки может быть много (на живом прогоне — семь). Не влезло после честного сжатия → SPLIT наверх в раскадровку.

## Вход Фазы II (1)

### G6 · вход Фазы II готов (precondition)
- **что проверяет:** канон-скелет скопирован; `brief.md` несёт `id`/`title`/`canvas`/`accent_tag`/`slide_order`/регистр+бюджет; на каждый слайд есть каркас; `illustrations/*`-плейсхолдеры на месте.
- **признак-на-диске:** `<лекция>/src/{shablon.html,engine.js,tokens.css,fonts/}` существуют; `brief.md` фронтматтер содержит непустые `id`,`title`,`canvas: 1440x810`,`accent_tag`,`slide_order`; для каждого id из `slide_order` — файл `slides/<id>.html` содержит `<section class="slide"`; для каждого `data-ill="<name>"`, встречающегося в `slides/*.html` (grep `data-ill="([^"]+)"`), существует файл `illustrations/<name>.*`.
- **дополнительно (с 27.07.2026):** G7 зелёный — предвёрсточный документ принят владельцем; `src/content/<id>.md` **порождены** из ленты (для каждого id из `slide_order` файл существует, непуст и не содержит html-тегов — regex `<[a-zA-Z]` не совпадает; `<figure>`/`<figcaption>` живут только в ленте, в дек не идут).
- **тип:** жёсткий-стоп-вперёд
- **возврат-разрешён:** да (не готово → назад в Фазу I, арка 5 или 6)

## Гейты Фазы II (G8–G11 + G12–G15)

### G8 · вёрстка (граница 7→8)
- **что проверяет:** 0 overflow `.zone`; `--t-body` не уменьшен; палитра только из `:root`; fit-группы консистентны.
- **признак-на-диске:** [браузерный] `_generator/audit.py <src>/dist/index.html` → overflow `.zone`==0; статически — grep hex-цветов (`#[0-9a-fA-F]{3,6}`, вне `tokens.css`) в `slides/*.html` == 0 (палитра только токенами); grep значения `--t-body` в `slides/*.html`/`shablon.html` не встречает переопределения меньшим числом, чем в `tokens.css`.
- **тип:** жёсткий-стоп-вперёд [часть браузерная] + автор-чекпоинт (глаз — `render.py`)
- **возврат-разрешён:** да (переполнение → починка по чек-листу порядка; не влезло → SPLIT в раскадровку)

### G9 · сцены без дед-кликов (граница 8→9/10)
- **что проверяет:** каждый клик что-то меняет (нет дед-кликов); связка вне блюра; геометрия заморожена между сценами.
- **признак-на-диске:** [браузерный] `--scene-diff` (`_generator/audit.py`) → последовательные сцены отличаются только добавленными пикселями; статически — grep `{blur@` в `content/*.md`: связка-символ (`≈`,`=`,`⇒` и т.п.) НЕ является первым child внутри того же `{blur@…|…}` (регулярка «связка вне фигурных скобок blur»).
- **тип:** жёсткий-стоп-вперёд [браузерный — локально у владельца] + дисциплина-хук (связка вне блюра — статически грепаемо)
- **возврат-разрешён:** да (дед-клик найден → назад в арку 8, разметка шорткатов)

### G10 · иллюстрации (граница 9→10)
- **что проверяет:** каждый `var(--x)` внутри иллюстрации определён в `tokens.css` (иначе чёрная заливка без ошибки в консоли); SVG не переполняет бокс; id внутри SVG несут per-file префикс (0 конфликтов при инлайне нескольких).
- **признак-на-диске:** статический lint `build_deck.py.lint()` — regex `var\(--[\w-]+\)` из каждого `illustrations/*.svg` сверяется с `:root{}` собранного `tokens.css` (0 неопределённых имён, без браузера); [браузерный] `audit.py` `svgOverflow`==0; grep `id="` по всем `illustrations/*.svg` одной колоды — 0 совпадающих значений id без stem-префикса.
- **тип:** жёсткий-стоп-вперёд (lint-часть — авто без браузера) + [браузерный — локально у владельца] (`svgOverflow`)
- **возврат-разрешён:** да (неопределённый var/переполнение/id-конфликт → починка внутри арки 9)

### G11 · сборка + QA, 5 слоёв (финальный, дёшево→дорого)
- **что проверяет:** линтер → render-identity (только правка, greenfield N/A) → аудит → scene-diff → глаз; FAIL на любом слое → назад на виновный шаг конвейера.
- **признак-на-диске:** exit-код `python3 _generator/build_deck.py <лекция>/src` == 0 (0 нерезолвленных `{{...}}`, 0 `⟦MISSING-MATH⟧`, `data-ill`/`data-iframe` резолвятся, `var(--x)` определён, 0 внешних src/href, `slide_order` = точное покрытие `slides/`); для правки существующего дека — [браузерный] `render.py --compare orig rebuilt` даёт `cmp` exit 0 (нормализованный посимвольный дифф); [браузерный] `audit.py` PASS (WARN не валит FAIL); [браузерный] `--scene-diff` PASS; финально — глаз владельца на `render.py`-PNG (не автоматизируется).
- **тип:** жёсткий-стоп-вперёд (слой 1 — авто без браузера; слои 2–4 — [браузерный]) + автор-чекпоинт (слой 5, глаз)
- **возврат-разрешён:** да (FAIL на любом слое → назад на виновный шаг, не только внутри арки 10). Greenfield-нюанс: слой 2 (render-identity) N/A без `provenance_sha256` — гейт = слои 1+3+4+5.

### G12 · обложка, визитка и финал (внутри арки 7)
- **что проверяет:** три служебных слайда — обложка, визитка «Про меня» и финал — **порождены генератором, а не написаны руками**. Обложка — название + опц. одна иллюстрация; дата, место, подзаголовок — только объявленным полем `brief.md`. Визитка — всегда вторым слайдом, один канон на все деки. Финал — «Спасибо за внимание» + опц. одна иллюстрация, тела текста нет. Мотив: владелец, 27.07.2026 — *«прямо всё время почему-то начало и конец неправильно рисует»*; чинилось руками и возвращалось следующей большой правкой, поэтому с 28.07.2026 гейт судит не аккуратность, а **происхождение**: писать отсебятину должно быть негде.
- 🔴 **признак-на-диске — РАЗВИЛКА по одному грепу.** `grep -l '{{SLIDES}}' <лекция>/src/shablon.html`:
  - **есть совпадение (дек на порождении)** → две проверки сразу, обе нужны:
    (1) **происхождение** — файл в `src/slides/`, чей basename **без расширения** равен `sl-title`/`sl-vizitka`/`sl-thanks`, = **FAIL**: он перебивает порождение. Сверять именно basename, а не `<id>.html`: генератор ключует слайды по `p.stem` и глобит `*`, поэтому `sl-title.txt` и даже `sl-title.html~` (бэкап редактора, появляется без злого умысла) перебивают канон ровно так же. Визитка не судится, если объявлено `vizitka: net`, — генератор её и не порождал, перебивать нечего.
    (2) **объём объявленного** — `title` + `cover_sub` + `cover_date` + `cover_place` вместе ≤ 25 слов. Счёт слов не отменяется, а **перенацеливается** со слайда на поля: «писать лишнее негде» неверно, канал есть и он же санкционированный. *Цена, оплаченная верификатором 28.07.2026: без этой ветки обложка на **52 слова**, собранная через `cover_sub`/`cover_date`/`cover_place`, давала G12 = PASS — то есть гейт снимался ровно тем действием, которое канон предписывает.*
    Число `data-ill` не проверяется: полей ровно два (`cover_ill`, `final_ill`), больше одной иллюстрации на слайд объявить нечем.
- ⚠ **пределы, названные честно (28.07.2026, верификатор захода `kod_sluzhebnye-slajdy.md`, охват 31 вход):**
  1. **Служебный слайд под ЧУЖИМ именем не ловится.** Рукописный `sl-oblozhka` первым в `slide_order` даёт деку с двумя обложками, и G12 молчит: тройка id фиксирована. Держится на том, что дубль виден глазом на первом же прогоне.
  2. **Рукописная обложка при порождённых визитке и финале — FAIL, и это намеренно.** Сам генератор такой режим поддерживает (приоритет рукописного), гейт же требует, чтобы миграция была атомарной: три слайда разом или ни одного. Цена названа: промежуточное состояние миграции красное по построению, а красное на здоровом провоцирует `--no-verify`. Пересматривать — владельцу явно.
  3. **`{{SLIDES}}`, названный в КОММЕНТАРИИ шаблона, включает порождение** (подстановка комментариев не различает, весь поток слайдов уезжает внутрь `<!-- -->`). Гейт честно зеркалит механику; чинить надо в `build_deck.py`, а не в признаке. Побочный случай `{{ SLIDES }}` с пробелами закрыт линтером: «в собранном деке ноль слайдов» = ошибка сборки.
  - **совпадений нет (немигрированный дек)** → прежняя ветка счётом слов: для `slide_order[0]` и `slide_order[-1]` раздел ленты / `content/<id>.md` ≤ 25 слов; `data-ill="` в `slides/<id>.html` ≤1 у каждого; ни в одном нет `{@`/`{blur@`/`{fill@`.
  - *Замер 28.07.2026 по всем живым декам, тем же ходом (закон «признак пишется грепом»):* `{{SLIDES}}` — **0 совпадений** до миграции Паскаля, **1** после (`paskal`); рукописные `slides/{sl-title,sl-vizitka,sl-thanks}.html` — buffon **3**, dandelin **0**, fibonacci **0**, paskal **1** до миграции и **0** после. То есть структурная ветка не судит немигрированные деки вовсе: **0 ложных ❌**, три буффоновских рукописных слайда остаются законными.
- **тип:** жёсткий-стоп-вперёд (происхождение — авто; счёт слов у немигрированных — авто) + автор-чекпоинт (образ финала — вкус)
- ⚠ **счёт слов НЕ отменён, а сужен** до немигрированных деков. Он был единственной проверкой до 28.07.2026 и на живом замере пропускал ровно ту поломку, ради которой заведён: обложка Паскаля с лишним подзаголовком давала G12 = PASS, потому что подзаголовок короткий. Отменить его совсем нельзя — `buffon`, `dandelin`, `fibonacci` в этом заходе не мигрированы, и другой проверки у них нет.
- ⚠ **решено 28.07.2026: «финал-задача» — не третий случай, а долг.** `fibonacci/s12-zeck` кончает деку задачей на 29 слов, финала-образа нет вовсе, и G12 на нём красный — верно. Правило пришло от владельца дословно («финальный слайд слабый — нужен один образ и „спасибо за внимание“, без текста»), и формат `fibonacci` он же забраковал. Порог 25 слов не смягчается. Пересмотреть — только владельцу и явно, а не молчаливым ослаблением проверки.
- **возврат-разрешён:** да (перегружено → починка внутри арки 7)

### G13 · иллюстрация-ответ не раньше своей сцены (границы 8→9, 9→10)
- **что проверяет:** если рисунок отвечает на заданный на слайде вопрос, он появляется **вместе с ответом**, а не с началом слайда. Иначе картинка спойлерит и убивает отгадывание залом. На живом прогоне нарушали четыре рисунка.
- **признак-на-диске:** для каждого `data-ill` в `slides/<id>.html`, у которого задан `data-scene="N"`, — в `content/<id>.md` существует маркер `{@N}` или `{blur@N` с тем же N (илл. привязана к существующей сцене, не «сцена 0 по умолчанию»); обратно — если раздел содержит `{blur@` (то есть на слайде есть спрятанный ответ), а `data-ill` того же слайда не несёт `data-scene`, → FAIL (рисунок показан раньше ответа).
- **тип:** жёсткий-стоп-вперёд (привязка — авто) + автор-чекпоинт (является ли рисунок ответом — вкус)
- ⚠ **реализация (28.07.2026), три поправки.** (1) Атрибута `data-scene="N"` в репозитории нет ни одного: живой словарь — `data-scene-from` / `data-scene-until`, и именно его порождает `build_deck.py` из `{@N}`. Привязкой считается любой из трёх. (2) Жёсткая часть — «привязка ведёт к СУЩЕСТВУЮЩЕЙ сцене»: `2 ≤ N ≤ data-scenes`. Требовать вдобавок маркер `{@N}` в тексте нельзя — иллюстрация вправе занимать собственную сцену, где текстовой дельты нет; это 2 ложных ❌ (`fibonacci/s02-hook`, `paskal/sl-pairs`). (3) Обратная ветка (есть `{blur@`, а у `data-ill` привязки нет) реализована как **WARN**, не FAIL: она держится на суждении «рисунок и есть ответ», которое сам этот файл относит к автор-чекпоинту; на живых деках даёт 2 срабатывания, где рисунок ответа не выдаёт.
- **возврат-разрешён:** да (спойлер → назад в арку 8, разметка сцен)

### G14 · `data-scenes` согласован с числом сцен в тексте (внутри арки 8)
- **что проверяет:** атрибут `data-scenes="N"` слайда равен фактическому числу сцен в его тексте. Разошлись — блюр не раскрывается, кликер уходит на следующий слайд (урок 7 арки 27.07).
- **признак-на-диске:** для каждого `<id>`: `N` из `data-scenes="N"` в `slides/<id>.html` == `max` по всем маркерам `{@k}`, `{blur@k`, `{fill@k` в `content/<id>.md`. **Правильная починка — не сверка, а генерация:** `data-scenes` должен вычисляться сборкой из текста, а не выписываться руками (см. H6).
- **тип:** жёсткий-стоп-вперёд (авто, без браузера)
- ⚠ **реализация (28.07.2026):** источниками сцен считаются ВСЕ статические, не только маркеры текста — `{@k}`/`{blur@k}`/`{fill@k}` в `content` плюс `data-scene-from|until="k"` в слайде плюс число стадий в `data-stages="…"` у `<canvas>`. Жёстко краснеет только случай `need > N` (текст требует сцену, которой слайд не объявил, — это и есть урок 7). Случай `need < N` при живом `data-sim`/`data-iframe` не судится вовсе: стадии добавляет JS. Буквальная версия «max по маркерам в тексте» давала **24 ❌ на четырёх живых деках, настоящих среди них 0** — слайд с одной сценой маркеров не имеет по устройству, а sim-слайды держат стадии в канвасе (`fibonacci/s08-subset`: `data-scenes="4"` при `data-stages="empty examples recur bijection"` — всё верно).
- **возврат-разрешён:** да

### G15 · каскады сцен полны (границы 8→9, 10)
- **что проверяет:** CSS-каскад раскрытия сцен покрывает все сцены слайда. Выписанный руками до `.scene-5` каскад обрывается на шестой сцене — последняя сцена начинает повторять первую (урок 8).
- **признак-на-диске:** для каждого `<id>` с `data-scenes="N"`: в собранном CSS существуют правила для всех `.scene-1 … .scene-N` (grep `\.scene-(\d+)` → множество покрывает `1..N` без дыр). **Как и G14 — правильная починка через генерацию каскада, не через ручную дописку.**
- ⚠ **реализация (28.07.2026), две поправки.** (1) Покрытие требуется с **2**, не с 1: `.scene-1` в каскаде не бывает по устройству движка — сцена 1 есть базовое состояние, раскрывать нечего. Буквальное «покрывает `1..N`» дало бы ❌ на всех четырёх живых деках. (2) Каскад судится **по семействам отдельно**, а не объединением grep `\.scene-\d+`: семейство раскладки (`{@k}`/`{fill@k}`/`data-scene-from|until` ↔ селекторы `.scene-k [data-scene-from…`) и семейство блюра (`{blur@k}` ↔ селекторы `.scene-k …blur-reveal|data-reveal`). Объединение проспало бы ровно урок 8 — обрыв одного семейства при полном другом (у `buffon` каскад раскладки идёт до `.scene-5`, а каскад блюра только до `.scene-4`). По семействам на живых деках 0 ❌.
- **тип:** жёсткий-стоп-вперёд (авто, без браузера)
- **возврат-разрешён:** да

## 🔴 Три гейта, которых НЕ ХВАТАЕТ (открыто 01.08, каждый оплачен)

| что не краснеет | чем чинится | цена, которой оплачено |
|---|---|---|
| **Повторный ключ в dict-литерале** `sverstat.PRAVKI` | `assert len(PRAVKI) == len(ключей в исходнике)` — одна строка, краснеет сама | В файле жили ДВА ключа `"s05"` в 90 строках друг от друга; третий, добавленный «рядом с соседями по смыслу», не сработал — побеждает нижний, Python не предупреждает ничем. Прогон зелёный, промер не сдвинулся ни на пиксель, **правка написана и не существует**. Круг диагностики на пустом месте; дубль жил с 30.07 и был заряжен на любого следующего |
| **Линтер «порция текста на клик» не видит сцену пункта списка** | считать блок пришедшим на сцену N, если сценовый спан внутри него — единственный потомок. Одна строка в `build_deck` | Два круга. Сначала исполнитель отказался от списков и переписал три слайда абзацами, тихо отказав владельцу в том, чего тот не просил менять; потом владелец потребовал булеты поимённо в пяти местах, и пришлось заводить **обходной носитель** — теперь в колоде два механизма списка вместо одного |
| **Маркер пункта гаснет вместе со своим пунктом — селектор был шире правила** | `:has(> [data-scene-from]:only-child)` вместо `:has(> [data-scene-from])` | Правило гасило маркер у ЛЮБОГО пункта, внутри которого есть сценовый спан. На «Трёх биекциях» левая часть равенства стоит с первой сцены, а по клику приходит ответ — на слайде стояли три формулы и **один булет из трёх**. CSS верен, HTML верен, промер 0, линтер зелёный; поймано глазом на кадре |

**Общее у всех трёх: зелёный прогон при неверном результате.** Первые два — задачи в `_generator/`, третий уже починен в `sobrat_overlay.py`.

## Дисциплина-хуки (WARN — continuous, не граница)

### H1 · карточка без источника
- **стадия:** арка 2 (ресёрч), continuous по ходу сессии, до гейта G2.
- **что проверяет:** карточка рода `находка|утверждение|биекция|мостик` заведена без поля `источник:`.
- **признак-на-диске:** grep блоков карточек в `<лекция>/kartoteka/KARTA-OBLASTI.md` — блок между `id:` и следующим `id:` не содержит строки `источник:`.
- **тип:** дисциплина-хук=WARN
- **возврат-разрешён:** н/п (WARN не блокирует; чинится тем же ходом сессии; эскалирует в G2, если не починено к гейту)

### H2 · источник без дайджеста VYCHITANO
- **стадия:** арка 2, continuous, до первой карточки, ссылающейся на источник.
- **что проверяет:** источник упомянут (заведён в списке источников/карточках), но нет предшествующего дайджеста `VYCHITANO`.
- **признак-на-диске:** для каждого уникального значения `источник:` в `<лекция>/kartoteka/KARTA-OBLASTI.md` — существует запись-дайджест для `<источник>` в `<лекция>/istochniki/VYCHITANO.md`.
- **тип:** дисциплина-хук=WARN
- **возврат-разрешён:** н/п

### H3 · висячее ребро в KARTA-OBLASTI
- **стадия:** арка 2, continuous.
- **что проверяет:** `id`, упомянутый в чьих-то `связи:`, отсутствует как заведённый узел графа.
- **признак-на-диске:** множество всех `id:` в `kartoteka/KARTA-OBLASTI.md` минус множество всех id, встречающихся в любом `связи: [...]` любой карточки — разность в сторону «упомянут, но не заведён» должна быть пустой.
- **тип:** дисциплина-хук=WARN
- **возврат-разрешён:** н/п

### H4 · открытый `⚑ Флаг` без парного `Флаг закрыт` (раннее предупреждение)
- **стадия:** арка 3 (и остаточно арка 4 — только сверка), continuous ДО гейта G3/G4.
- **что проверяет:** маркер долга висит незакрытым дольше текущего шага сессии (мягче G3 — предупреждает по ходу, не только на выходе).
- **признак-на-диске:** grep `⚑ Флаг` по `<лекция>/kotly/matematika.md` в произвольный момент сессии (не только на гейте) → количество непарных >0 → WARN.
- **тип:** дисциплина-хук=WARN
- **возврат-разрешён:** н/п (эскалирует в жёсткий G3, если не закрыт к границе 3→4)

### H5 · `slide_order`-сироты/дубли (раннее предупреждение)
- **стадия:** арка 5/10, continuous при любой правке `brief.md`/`slides/`.
- **что проверяет:** `slide_order` расходится с фактическим множеством файлов `slides/` (пропуск, файл-сирота без записи, дублирующая запись).
- **признак-на-диске:** `set(slide_order)` == `set(basename(slides/*.html) без .html)`; любое расхождение → WARN до сборки (на слое 1 гейта G11 то же расхождение эскалирует в жёсткий FAIL).
- **тип:** дисциплина-хук=WARN (эскалирует в жёсткий-стоп-вперёд на слое 1 гейта G11)
- **возврат-разрешён:** н/п

### H6 · величина выписана руками в двух местах (общее правило)
- **стадия:** любая, continuous. Особенно арки 7/8/10.
- **что проверяет:** **если значение обязано совпадать с другим значением — оно должно порождаться, а не сверяться.** Три бага живого прогона (`data-scenes` ≠ число сцен · каскад `.scene-N` оборван · `slide_order` ≠ файлы `slides/`) — одной природы: величина выписана руками в двух местах и разъехалась. Гейт, который их ловит, лечит симптом; лечение — генерация.
- **признак-на-диске:** в `slides/*.html` и `shablon.html` не должно быть руками выписанных величин, вычислимых из `content/*.md` или из скелета: grep `data-scenes="\d+"` → каждое совпадение = WARN «должно вычисляться сборкой»; grep `\.scene-\d+` в рукописном CSS (вне сгенерированного блока) → WARN. Ноль совпадений = правило соблюдено и гейты G14/G15 становятся бессодержательными (это и есть цель).
- ✅ **первое живое применение хука к слою вёрстки — 28.07.2026** (заход `../zhurnal/2026-07-27_paskal-lekcia-sborka/kod_sluzhebnye-slajdy.md`). Три служебных слайда и их CSS больше не выписываются в каждом деке: их строит `build_deck.py` из полей `brief.md` и канона `_generator/skeleton/sluzhebnye/` (гейт — G12 выше). Тем же ходом перестал дублироваться **порядок слайдов**: у дека на порождении он живёт только в `brief.slide_order`, а не второй раз списком `{{SLIDE:<id>}}` в шаблоне. Порождённые слайды `data-scenes` не несут вовсе (движок читает `dataset.scenes || '1'`), то есть в названном выше пределе «`H6` не отличает сгенерированный каскад от рукописного» закрыта та часть, что касается служебных слайдов: у них WARN-у не на что срабатывать. Каскад `.scene-N` пер-слайдовой вёрстки по-прежнему рукописный — долг генератора остаётся.
- **тип:** дисциплина-хук=WARN (эскалирует в G14/G15 на границе)
- **возврат-разрешён:** н/п

## Сводка (для критерия готовности)
Счёт записей **не выписывается руками** (`KONSTITUCIYA §10`) — считается по файлу:

```sh
grep -c '^### G' _studio/konvejer/GEJTY.md   # гейты
grep -c '^### H' _studio/konvejer/GEJTY.md   # дисциплина-хуки
```

Границы Фазы I — G1–G5 + G7 (G7 переехал 27.07.2026, id сохранён) **+ G16** (фаза `04.5-intervyu/`, граница 4→5, заведена 05.08.2026 — id взят следующим свободным, гейты между G5 и G7 НЕ переномерованы); вход Фазы II — G6; Фаза II — G8–G11 + G12–G15. Все несут «признак-на-диске» конкретным грепом/файл-условием (ни одной абстрактной «проверить качество») и явный «тип».

## Якоря
- Драйвер, вызывающий эти гейты: `ALGORITM.md`. Формат Фазы II: `FORMAT-ISTOCHNIKA.md`. Карта шагов: `00-KONVEJER.md`.
- Источник контрактов по гейтам: `01-brief/DOK.md` … `10-sborka-qa/DOK.md` (каждый гейт дословно берёт формулировку из своего пакета, не изобретает новую).

---

## Пункты очереди заходов, доставленные `dostavit_urok.py`

> Дословные находки из секций `## ВОПРОСЫ` файлов-заходов — голос исполнителя, который об это споткнулся, не редактора. Разбирает их человек: находка описывает, что мешало, но задачей сама не становится.
> Строка `ДОСТАВЛЕНО: <заход>#<N>` внизу каждой записи — та же метка, что стоит в источнике; по ней Г7 `priyomka.py` проверяет, что доставка не объявлена ложно.

#### rasshivka-kornej#5

5. **Браузерная половина G10 не запускается ничем автоматически.** `check_g10` (`sostoyanie.py:582`) считает две из трёх объявленных проверок, а `svgOverflow` только ПЕЧАТАЕТ совет прогнать `audit.py` руками. Гейт при этом даёт `PASS` — то есть зелёный цвет накрывает и непроверенную треть. Слепая зона объявлена в тексте гейта, но не в его вердикте.
   ДОМ: `_studio/konvejer/GEJTY.md`
ДОСТАВЛЕНО: rasshivka-kornej#5 · 2026-08-07
