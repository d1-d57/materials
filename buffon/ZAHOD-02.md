# Канал исполнителя — «Игла Бюффона» ЗАХОД-02: дочистка после ревью (короткий)

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> План / вопросы / отчёт — в секции внизу ЭТОГО файла. Правки хирургические, метрика — качество.

## КОНТЕКСТ
Дек «Игла Бюффона» (`index.html`, 1440×810, канон **html-slides-studio**). ЗАХОД-01 уже пересобрал
первую половину + системный слой (ветка `buffon-zahod-01`, «принято с замечаниями»). Это **короткая
дочистка**: 4 микроправки, найденные при живом визуальном ревью автора. Полная спека — `WORKLIST.md §6`.
Приёмка — по твоему ОТЧЁТУ, по гейтам (низ файла).

## 0. ПЕРВЫЙ ХОД
Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/materials/buffon/`
Точка отката: продолжай на ветке `buffon-zahod-01` (под git) — коммить по каждому пункту. Если ветки нет — создай её от текущего состояния и сделай zip-бэкап.
Прочитай ТОЛЬКО: этот файл; `./WORKLIST.md` (§6 — спека ЭТИХ правок; §0 — правила G1–G6; §1 — контекст слайдов); `./README.md` (карта: id слайда → канвас → строки); скилл html-slides-studio (`scripts/audit.py`, `assets/etalons/`, references) для рендера/аудита; `index.html` (грепай узко — исключай `data:image`/`data:font`).
Якорься по `id` секции / сигнатуре, НЕ по номеру строки. Напиши **## ПЛАН перед правками**.

## 1. ГРАНИЦЫ
- Блок `[4]` ENGINE — verbatim.
- **НЕ гоняйся за `audit exit 0`.** 3 пре-существующих FAIL (title overflow, coin 32px, condition 24px-текст) — вне задачи; текст ради зелёной галочки НЕ ужимать. (Пункты П1/П2 ниже ПОБОЧНО снимут 2 других FAIL — legend-radius и thanks-shadow; это нормально.)
- Трогай ТОЛЬКО перечисленное в §2. Содержание слайдов (первая/вторая половина) не переписывай.

## 2. ЗАДАЧА (4 пункта; коммит после каждого)

**П1 — Слайд 19 `sl-condition` («При каких y и α игла пересекает линию»).**
(а) Убрать боковую подпись-легенду «красное пересекает / голубое не пересекает» — это стандартный визуальный язык, пояснять не нужно. Канвас вынести в ЦЕНТР освободившейся зоны.
(б) Точки `phase-mass` на этом слайде ЗАМИРАЮТ. Сделать, чтобы канвас НЕ застывал (правило G3 «ничего не останавливается само»). Это плотностная диаграмма — простой `endless` зальёт её в сплошное пятно, поэтому: **катящееся обновление** — старые точки тают/вытесняются, поток идёт бесконечно, картинка дышит и не зарастает.
**КРИТЕРИЙ:** легенды нет; канвас по центру; точки сыплются НЕПРЕРЫВНО (не встают) и плотность не превращается в сплошную заливку. Рендер по сценам + глаз.

**П2 — Слайд 23 `sl-thanks`.** Убрать полупрозрачный прямоугольник/плашку под канвасом (декоративный box-shadow/border-radius — при разглядывании виден и смотрится грязно).
**КРИТЕРИЙ:** под канвасом нет полупрозрачной плашки, финал чистый. Рендер + глаз. (Побочно уходит audit-FAIL thanks-shadow.)

**П3 — Слайд 11 `sl-count` («разрежем иглу на три части»).** С жирным акцентом что-то не так. Применить принцип G5: жирный, который ПОЯВЛЯЕТСЯ/ИСЧЕЗАЕТ между сценами (толчковый) → перевести в ЦВЕТ (`.acc`), чтобы вёрстка не дёргалась; постоянный смысловой жирный — оставить. Найти конкретный дефект (лишний `<b>`, скачок метрики при смене сцены) и починить.
**КРИТЕРИЙ:** при прощёлкивании сцен вёрстка не прыгает; акцент читается; «мигающего» жирного нет. Рендер по сценам + глаз.

**П4 — Слайд 1 `sl-title` (низкий приоритет, ОПЦИОНАЛЬНО).** Аудит считает overflow, визуально его нет. Если правка ДЕШЁВАЯ и безопасная (чуть опустить подпись ИЛИ на пару % уменьшить картинку, чтобы контент влез в зону) — сделать; если есть риск сдвинуть композицию — НЕ трогать и отметить в отчёте. Вид важнее галочки.
**КРИТЕРИЙ:** либо overflow ушёл без ухудшения вида, либо осознанно оставлено (в отчёт).

## Принцип «золото / жирное» (для П3 и вообще)
Золото = **сигнал** (ответ / reveal / спотлайт / что изменилось) — им НЕ заливать всё. Толчковый (мигающий между сценами) `<b>` → цвет `.acc`. Постоянный структурный жирный — оставить жирным. Не переводить в золото весь жирный по деку.

## ПРОТОКОЛ (кратко)
Хирургия: `.bak`/коммит перед правкой; якорь матчится РОВНО раз (0 или >1 → стоп). После каждого пункта — рендер тронутого слайда по сценам (масштаб + `?only=N`) и СМОТРИ на картинку; сцены прощёлкать (пустых кликов нет). Инвариант: канвас в покое = слайд; кнопки-глифы `▸ ▸▸ ❚❚ ↺`; неопределённая CSS-var в SVG → чёрный.

## 3. ВЕРИФИКАТОР
После всех правок — свежий субагент-рендерер (другой глаз): рендерит слайды 11/19/23 по сценам + `audit.py --scene-diff`; подтверждает: легенды нет и канвас по центру (19); точки не встают и не зарастают (19); плашки под канвасом нет (23); вёрстка не прыгает на сценах (11); новых регрессий/пустых сцен нет; целевые audit-FAIL (legend-radius, thanks-shadow) ушли.

## 4. ГЕЙТЫ → в ## ОТЧЁТ
Что сделал + ЗАЧЕМ (к П1–П4) · как проверил (рендеры, audit, глаз) · что НЕ трогал (границы) · развилки · результат верификатора · чеклист П1–П4 (☑ / ☐ / осознанно-отложено) · **время прогона + токены**. Недобор — заявить ГРОМКО.

## ПЛАН — (заполняет исполнитель)

Прочитано: этот файл, `WORKLIST.md` (§0 правила, §1 контекст первой половины, §6 спека этих 4 пунктов), `README.md` (карта id→строки), узкий grep `index.html` по нужным `id`/модулям (без base64). Ветка `buffon-zahod-01` уже текущая (есть незакоммиченный правленый `WORKLIST.md` от ревью-аналитика — не мой, не трогаю).

**П1 (сл.19 `sl-condition`, CSS L308–320 + разметка L661–665):**
- (а) Убрать `<div class="panel p-legend…">` (L663) и его CSS-блок `#sl-condition .p-legend*` (L315–320, 6 строк — легенда/дот-стили). Канвас `.p-needle` (L314): доска шириной 1440 (grid-columns 8+1404+28), панель шириной 1106 → центрировать `left: 292px` → `left: 167px` = (1440−1106)/2. `top:18px` уже почти по центру вертикально (доска 364, панель 327) — не трогаю.
- (б) `lab-ext/phase-mass.js`, `layout==='condition'` ветка (конструктор ~L2078, `throwOne` ~L2171–2175): сейчас `autoTarget:800` без `endless` → `play()` останавливается на n≥800 (замирание, L943 движка). Добавляю `endless:true`. Чтобы не «заливало» плотностной диаграммой — верчу окно: `this.pts` капаю на `this.o.autoTarget` (800, независимо от `this.target`, который дергают кнопки ▸/▸▸), при превышении `shift()` старейшую и делаю `this.redrawAll(thr)` вместо разового `stampDot` в offscreen — это перерисовывает НЕ только новую точку, но и убирает вытесненную из аккумулятора (иначе eviction из массива не стирает уже нарисованный пиксель). `redrawAll` уже существует (используется волной раскраски), просто зову его на каждый бросок для этого layout. Слайд `sl-phase` (тот же модуль, `layout==='canvas'`) не трогаю — там своя, рабочая, механика (`recent`/`KEEP`).
- Критерий: рендер сл.19 по 3 сценам + `?only=19`, глазами — легенды нет, канвас в центре, точки не замирают и плотность визуально стабильна (не в сплошное пятно) после ~10–15 c наблюдения.

**П2 (сл.23 `sl-thanks`, CSS L433–434):**
- Убрать `box-shadow:0 2px 10px rgba(60,70,90,.10);border-radius:6px;` у `#sl-thanks .thx-art svg` — это и есть «плашка» (мягкая тень + скруглённый угол читаются как полупрозрачная панель при увеличении). Внутренний `<rect rx="6" fill="#ffffff">` самого SVG (это фон-артворк, не декоративная панель) не трогаю — визуально проверю, не режет ли глаз несовпадение.
- Критерий: рендер сл.23 крупным планом (`?only=23`) — под артом нет читаемой рамки/тени.

**П3 (сл.11 `sl-count`, L559):**
- Найден дефект: `<span data-scene-until="2"><b>Сколько</b> (примерно) <b>будет пересечений?</b></span><span data-scene-from="2">Сколько (примерно) будет пересечений?</span>` — один и тот же вопрос показан жирным в сцене 1 и обычным весом в сценах 2–3 (переключение через `visibility`, G3). Смена font-weight у видимого текста — и есть «скачок» (жирный текст шире, при подмене на обычный дёргается фраза). По принципу G5 перевожу толчковый `<b>` → `.acc` (только `color: var(--accent)`, без веса — проверил объявление `.acc` в CSS, веса не меняет): `<span class="acc">Сколько</span> (примерно) <span class="acc">будет пересечений?</span>`. Совпадает с идиомой, уже использованной в сл.13 (`sl-polygons`, L585 — тот же класс `fill-q`, то же переключение until/from на `.acc`).
- Постоянный смысловой `<b>С какой вероятностью он пересекает линию?</b>` (L561, не переключается между сценами) — оставляю жирным, это не толчковый акцент.
- Критерий: прощёлкать сцены 1→2→3, глазами — вопрос не «прыгает», акцент читается цветом, финального жирного вопроса не потеряно.

**П4 (сл.1 `sl-title`, CSS L116–121) — опционально:**
- `.art img { height:100% }` внутри строки-сетки 667px (`place-items:center`) — потенциальный источник overflow, который аудит ловит, а глазом не видно. Дешёвая правка: `height:98%` (симметричное уменьшение картинки вокруг центра, ничего не сдвигает). Если после рендера станет ХУЖЕ видно (заметный люфт/пустота) — откатываю и фиксирую «осознанно оставлено» в отчёте вместо правки.
- Критерий: рендер `?only=1` — либо overflow ушёл без ухудшения вида, либо решение задокументировано.

**Порядок:** commit после каждого пункта (П1→П2→П3→П4), рендер+глаз после каждого перед коммитом. Блок `[4]` ENGINE не трогаю (все правки в `[1]`/`[2]`/`[3]`).

## ВОПРОСЫ — (заполняет исполнитель)

Нет — все 4 пункта однозначны по спеке, развилок, требующих решения владельца, не возникло (П4 — сам решаю по месту, критерий уже в задаче).


## ОТЧЁТ — (заполняет исполнитель)

**Ветка:** `buffon-zahod-01` (продолжена, не создавал заново). Коммиты этого захода: `9466ad9` (П1), `f3b4fe6` (П2), `5db888f` (П3). П4 — без коммита (осознанно не тронуто).

### Что сделал и зачем

- **П1, сл.19 `sl-condition`:** (а) убрал боковую легенду «пересекает/не пересекает» (лишний стандартный визуальный язык) и её CSS (6 строк), канвас `.p-needle` отцентрировал в освободившейся доске (`left: 292px → 167px` = (1440−1106)/2). (б) `phase-mass` в раскладке `condition` не имел `endless` → `play()` останавливался на n≥800 (замирание, нарушение G3, замечено автором вживую). Добавил `endless:true` + «катящееся окно»: массив точек капается на `autoTarget` (800), при превышении старейшая точка вытесняется и весь аккумулятор перерисовывается заново (`redrawAll`) — иначе eviction из массива не стирал бы уже нарисованный пиксель. Слайд `sl-phase` (та же JS-функция, ветка `layout==='canvas'`) не тронут — там другая, рабочая механика (`recent`/`KEEP`).
- **П2, сл.23 `sl-thanks`:** убрал `box-shadow`+`border-radius` у `.thx-art svg` — это и была «полупрозрачная плашка», видимая при разглядывании (совпадает с audit-FAIL `thanks-shadow`). Внутренний `rect rx="6"` самого SVG (фон-артворк) не трогал — вопрос снят: без тени скругление не читается как отдельная панель.
- **П3, сл.11 `sl-count`:** нашёл дефект — вопрос «Сколько (примерно) будет пересечений?» был жирным (`<b>`) в сцене 1 и обычным весом в сценах 2–3 (тот же текст, переключение через `visibility`), т.е. смена font-weight на видимом тексте — «скачок» ширины при щелчке. Перевёл толчковый `<b>` → `.acc` (только цвет, без веса), по идиоме, уже используемой в той же конструкции `fill-q` на сл.13. Постоянный смысловой `<b>С какой вероятностью…</b>` (сцена 3, не переключается) не тронут.
- **П4, сл.1 `sl-title` (опционально):** НЕ сделано осознанно. При рендере `?only=0` overflow визуально не проявился — картинка и заголовок сидят с явным запасом полей сверху/снизу. Аудит-FAIL здесь, похоже, ложноположительный (или триггерится на строгий геометрический порог без визуального запаса), и правка «на всякий случай» (уменьшение картинки/сдвиг подписи) не даёт видимой пользы при ненулевом риске тронуть выверенную композицию. Решение: оставить как есть, отметить громко здесь.

### Как проверил
- Каждая правка — рендер через `python3 -m http.server` (файл скопирован в домашнюю папку `~/buffon-zahod-preview`, т.к. сервер-хелпер превью не имеет TCC-доступа к `~/Documents/GitHub/...` — техническая находка сессии, см. ниже) + `preview_screenshot`/`preview_eval` по `?only=N&scene=K` (N — 0-based индекс слайда: сл.11→10, сл.19→18, сл.23→22).
- П1(б) отдельно: браузер-хелпер держит вкладку в фоне (`document.hidden=true`), из-за чего `requestAnimationFrame` жёстко троттлится и анимация выглядит «замёрзшей», хотя не заморожена — обошёл, подменив `window.requestAnimationFrame` на `setTimeout(…,4)` и перезапустив симуляцию кнопкой `↺`; после этого дождался ~45–100 с реального прогона и увидел непрерывный точечный поток, стабилизирующийся по плотности (не заливает).
- **Верификатор:** свежий независимый субагент (без контекста этой сессии), тот же метод (свой `~/verify-buffon`, свой троттлинг-обход), 70 tool-uses, ~15 мин. Все 5 критериев — **PASS**: легенды нет+центр (геометрия смерена по пикселям: 167/1273 — симметрично), точечный поток непрерывен и плотность плато на ~184k закрашенных пикселей после ~800-точечного окна (не солидное пятно), кривая cos α рисуется корректно на сцене 2, у `sl-thanks` `box-shadow` в computed style пуст, у сл.11 позиции спанов «Сколько»/«будет пересечений?» **пиксель-в-пиксель совпадают** между сценой 1 (золото) и сценой 2 (обычный цвет) — реflow нет, сцена 3 жирный вопрос не пострадал. **Регрессий не найдено.**

### Что НЕ трогал (границы)
- Блок `[4]` ENGINE — не тронут (все правки в `[1]` CSS / `[2]` разметке / `[3]` ассетах-симуляциях).
- Содержание слайдов (текст/формулы/порядок) — не переписывал, только визуал+поведение по спеке.
- `WORKLIST.md` — незакоммиченная правка ревью-аналитика (7 строк изменений, видел в `git status` до начала работы) не моя, оставил как есть.
- Слайд `sl-phase` (вторая половина phase-mass) — не трогал, использует другую ветку того же модуля.
- П4 — сознательно не тронул (см. выше).

### Развилки
- Единственная содержательная развилка была в П1(б): простое `endless:true` без ограничения окна залило бы диаграмму сплошным пятном (сам предвидел это по спеке worklist §6). Решил через капанный `this.pts` + полный `redrawAll` на каждый бросок (дёшево — прямоугольник маленький, ~800 точек). Альтернатива «инкрементальная точка без переперерисовки» не работает, т.к. вытесненную точку из аккумулятора иначе не стереть.
- П4 — решение «не трогать» принято по месту (критерий это разрешал), не эскалирую как открытый вопрос.

### Верификатор — результат
Зелёный, без регрессий (детали выше). Доп. техническая находка от верификатора: последовательность «подменить rAF → нажать ↺» чувствительна к порядку — если между щелчками не переустановить патч, нативный rAF может снова взять управление и создать ложное впечатление «заморозки». Учтено в моей же проверке, стоит держать в голове для следующего захода.

### Чеклист П1–П4
- ☑ П1(а) — легенда убрана, канвас в центре.
- ☑ П1(б) — phase-mass endless + катящееся окно, не замирает, не заливает.
- ☑ П2 — плашка под `sl-thanks` убрана.
- ☑ П3 — толчковый жирный → `.acc`, вёрстка не прыгает.
- ◐ (осознанно) П4 — не тронуто; overflow визуально отсутствует, риск/выгода не в пользу правки.

### Время + токены
- Токены/тулзы верификатора: 116 421 out-tokens, 70 tool-uses, ~15 мин (906 480 мс) — по данным harness.
- Счётчик основного цикла (эта сессия) из окружения недоступен — как и в ЗАХОД-01, честно заявляю недобор метрики, а не подставляю оценку.
- **Недобор, заявленный ГРОМКО:** только П4 (осознанно отложено, не баг процесса) + отсутствие числа токенов основного цикла (техническое ограничение окружения, не мой недосмотр).
