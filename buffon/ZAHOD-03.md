# Канал исполнителя — «Игла Бюффона» ЗАХОД-03: анимация «площадь под косинусом» на слайде sl-area

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект целиком не изучай.
> План / вопросы / отчёт — в секции внизу ЭТОГО файла. Метрика — КАЧЕСТВО. Часы работы — норма.
> **Модель: рекомендуется `opusplan`** — задача с суждением (новая анимация + перевёрстка слайда + визуальный QA).

## КОНТЕКСТ
Дек «Игла Бюффона» (`index.html`, 1440×810, канон **html-slides-studio**). ЗАХОД-01 (первая половина + системный слой) и
ЗАХОД-02 (дочистка) ПРИНЯТЫ, всё на ветке `buffon-zahod-01`.
ЦЕЛЬ этого захода: на слайде **`sl-area`** (последний слайд Доказательства II, «площадь под косинусом», сейчас
`data-scenes="5"`) **оживить существующий кинематический аргумент** — заменить две статичные иллюстрации на ОДНУ живую
интерактивную анимацию `data-sim="cos-area"`, раскрывающуюся по сценам синхронно с уже написанным текстом p4–p6.
Прототип-эталон вида — `anim-01-prototype.html`; полная спека — `ANIM-01-cos-area-spec.md`.
Приёмка — по ОТЧЁТУ, по гейтам (низ файла).

**СТОП-ФЛАГ:** ты доводишь `sl-area` до готового, отрендеренного, прошедшего аудит состояния. Если упрёшься в
развилку по вёрстке (см. §3.C) — собери РАБОЧИЙ вариант и вынеси выбор в ## ВОПРОСЫ, не застревай.

## 0. ПЕРВЫЙ ХОД
Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/materials/buffon/`
**Коммит принятого + точка отката (СРАЗУ):** ты на ветке `buffon-zahod-01` (под git). Первым делом закоммить текущее
принятое состояние (ЗАХОД-01+02), если есть незакоммиченное (напр. правки аналитика в `*.md`): `git add -A && git commit -m "docs: worklist/анимация — заметки аналитика; приёмка ЗАХОД-02"`. Дальше анимацию делай отдельными коммитами по фазам. (Мердж в `main` — по желанию владельца, НЕ обязателен.)
**Прочитай ТОЛЬКО:**
- ЭТОТ файл (заход) — цель, тексты сцен, границы, гейты.
- `./ANIM-01-cos-area-spec.md` — спека анимации (идея, приём «скорость = повёрнутый радиус», 3 сцены, раскладка).
- `./anim-01-prototype.html` — **визуальный эталон** (открой/прочитай: геометрия, цвета, спидометр, график, площадь). Логику рисования переноси отсюда.
- `./README.md` — карта дека; `./WORKLIST.md §6` — контекст финализации.
- Скилл **html-slides-studio**: `references/per-slide-algorithm.md` (обязательно — алгоритм слайда), `references/slide-engine.md` + `references/simulations.md` (новый sim: контракт `data-sim`, «в покое = слайд», глифы `▸ ▸▸ ❚❚ ↺`, узкая ветка, автозапуск/сброс G3), `references/export-png.md`, `scripts/audit.py`, `assets/etalons/buffon-p*.jpg`.
- `index.html` — целевой файл (грепай узко, исключай `data:image`/`data:font`). Якорь — `id="sl-area"` и сигнатуры, НЕ номера строк.
Напиши **## ПЛАН** перед правками.

## 1. ГРАНИЦЫ
- Блок `[4]` ENGINE — verbatim (новый sim регистрируется как `lab-ext` модуль в блоке `[3]` + канвас `data-sim="cos-area"` в разметке; движок activate() уже умеет их поднимать — НЕ трогай его).
- Трогаешь ТОЛЬКО слайд `sl-area` (разметка секции + его CSS `#sl-area…` + новый sim-модуль в `[3]`). Другие слайды не трогать.
- **Математику НЕ менять:** `p = S/π ⇒ 2/π ⇒ S = 2` (p2) остаётся как есть. Анимация иллюстрирует ЧЕТВЕРТЬ (путь/площадь = 1) — это ровно то, что говорит p6; S=2 — вся область (две симметричные четверти), это уже забота p1–p2. Не «поправляй» 1 vs 2.
- Тексты p1, p2, p3 (вывод S=2 и вопрос «другим способом?») — сохранить.

## 2. ТЕКСТ СЦЕН (уже написан в слайде — СОХРАНИТЬ как подписи к анимации)
Слайд уже несёт кинематический аргумент; эти абзацы и есть покадровые подписи, менять смысл НЕ нужно:
- **Сцена 3** (`data-scene-from="3"`): «Точка $P$ с единичной скоростью проходит четверть единичной окружности. Параметр $α$ — это время.»
- **Сцена 4** (`data-scene-from="4"`): «Когда $P$ находится в положении $α$, скорость её проекции $x$ равна $\cos α$. Значит, сверху изображён график скорости $x$.»
- **Сцена 5** (`data-scene-from="5"`): «Площадь под графиком равна пути $x$, то есть $1$.»

Правки текста — только если перевёрстка потребует (напр. слово «сверху» в p4, если график окажется не сверху) → микроправка через `russian-editor`/`math-russian-terminology`, смысл сохранить, в отчёт.

## 3. ЗАДАЧА (фазы; коммит после B и C)

### Фаза A — АУДИТ (read-only, план)
Прочитай/отрендерь текущий `sl-area` (`?only=N` по всем 5 сценам). Зафиксируй в ПЛАН: его сетку (`#sl-area .grid/.copy/.rail`), два ill-шаблона (`sl-area-1` косинус-площадь `top:178 302×228`, `sl-area-2` кинематика `top:406 302×225`, появляется со сцены 3), и что p4–p6 уже описывают анимацию. Реши, как sim ляжет в сцены 3–5. Ничего не меняй.

### Фаза B — SIM `cos-area` (новый lab-ext модуль)
Собери симуляцию по `anim-01-prototype.html` + `ANIM-01-cos-area-spec.md`, но в КАНОНЕ дека: цвета из `tokens()` (`steel`/`brick`/`mustard`/`ink`/`rule`), шрифты дека, кнопки-глифы, «в покое = слайд», автозапуск-с-нуля/сброс по G3, узкая ветка. Приём «почему скорость = cos»: вектор скорости ⟂ радиусу и единичной длины (= радиус, повёрнутый на 90°) + спидометр в правом-верхнем углу; золотая вертикаль спидометра = `cos`. Подписей внутри канваса НЕТ, только точки-границы (единичная окружность; ось `0…π/2`).
**Раскрытие по сценам (через `onScene`), синхронно с p4–p6:**
- **sc≤2:** канвас пуст/в покое (идёт вывод S=2, анимации ещё нет).
- **sc3:** точка `P` едет по четверти дуги (радиус, точка, её проекция-путь `x`).
- **sc4:** появляется скорость — стрелка у точки + спидометр (золотая вертикаль = `cos α`) — и ГРАФИК СКОРОСТИ (`cos`) сверху.
- **sc5:** под графиком скорости заливается площадь → = пройденный путь `x` = 1.
*(Второй график «высота/путь = sin» из прототипа — опционально: аргумент p4–p6 центрируется на ОДНОМ графике скорости `cos` + путь показан движением проекции. Реши по рендеру, что читается чище; не перегружай.)*
**Мат-сверка (proof-self-review):** проекция `x` идёт со скоростью `cos α`, путь `x = ∫₀^α cos = sin α`, к α=π/2 путь = 1 = площадь под `cos` на `[0,π/2]`. Совпадает с p5–p6.
**КРИТЕРИЙ B:** sim рендерится по сценам; на sc3 точка едет с нуля и не встаёт (G3); на sc4 скорость=cos видна и на спидометре, и на графике; на sc5 площадь = 1; в покое = аккуратный кадр. Коммит + audit.

### Фаза C — интеграция в `sl-area` + вёрстка
Замени ДВЕ статичные иллюстрации (`sl-area-1`, `sl-area-2`) на ОДИН канвас `data-sim="cos-area"`, раскрытый по сценам 3–5. Тексты p1–p6 сохрани (p4–p6 = подписи анимации).
**Развилка вёрстки — собери РАБОЧИЙ вариант, выбор вынеси в ВОПРОСЫ:**
- Вариант (i) «минимальный риск»: оставить сетку текст-слева/доска-справа, но в доске — крупный канвас анимации вместо двух картинок.
- Вариант (ii) «как предложил владелец»: перевёрстка — текст сверху, БОЛЬШОЙ канвас снизу по центру. Тогда правь `#sl-area .grid/.copy/.rail` по алгоритму `per-slide-algorithm.md` (одна `.fit`-зона на зону, промеры, без `clamp/vh`).
Отрендерь выбранный вариант по всем сценам, посмотри глазами. Если (ii) выходит чисто — оставь его; если рискованно/некрасиво — оставь (i) и отметь в ВОПРОСАХ. Коммит + audit + `--scene-diff`.
**КРИТЕРИЙ C:** на sc1–2 виден вывод S=2 (p1–p3), анимации нет; на sc3–5 анимация раскрывается синхронно с p4–p6; ни одна сцена не пустая; тело текста не ниже пола; ничего не наложилось; выглядит как модельный слайд дека.

### Фаза D — ВЕРИФИКАТОР + сверка
Свежий субагент-рендерер (другой глаз): рендер `sl-area` по 5 сценам + `audit.py --scene-diff`; подтвердить: sim с нуля и не встаёт (sc3), скорость=cos читается (sc4), площадь=1 (sc5), пустых сцен нет, S=2 и p1–p3 не тронуты, движок цел, других слайдов не трогали.

## 4. ГЕЙТЫ → в ## ОТЧЁТ
Что сделал + ЗАЧЕМ (к фазам/сценам) · как проверил (рендеры по сценам, audit, эталоны) · что НЕ трогал (границы, математика S=2) · **какой вариант вёрстки выбран и почему** (i/ii) · развилки в ВОПРОСЫ · результат верификатора · **время + токены** · недобор — ГРОМКО.

## ПЛАН — (исполнитель, 2026-07-04)

**Среда.** Playwright-браузер не качается (нет сети), но рендер идёт через системный Google Chrome
(`p.chromium.launch(channel='chrome')`) — локально, сеть не нужна. `audit.py` — локальная копия с той же
правкой (`scratchpad/audit.py`). Рендер-харнесс — `scratchpad/render.py`. `sl-area` = слайд index **20**.

**Аудит текущего (Фаза A, read-only — сделано).**
- Сетка: `.grid` = `20px | 1075px | 345px` (текст-слева / доска-справа). `.copy` в кол.2, `.rail` (=`--board`
  #a7c2cb) в кол.3 на всю высоту. Тело `--t-body:35.3px`, шаг абзаца 53.9.
- Два ill-шаблона в `.rail`: `sl-area-1` (p-cos, `top:178 302×228`) косинус+площадь, виден всегда;
  `sl-area-2` (p-kin, `top:406 302×225`) кинематика, `data-scene-from="3"`.
- Тексты: p1 (прямоугольник π) + формула S=2 — сцена 1; p3 «другим способом?» — сцена 2; p4 «Точка P…» — сц3;
  p5 «Когда P… сверху изображён график скорости x» — сц4; p6 «Площадь = путь = 1» — сц5.
- Мат-роль x: горизонтальная проекция = `sin α` (0→1), скорость = `cos α`; площадь под `cos` = 1. Проверено.

**Фаза B — sim `cos-area` (новый lab-ext модуль, вшивается `<script>` в блок [3]).**
- Класс `CosArea(cv)`, регистрируется `window.LabKinds['cos-area']`; палитра из `LabCore.tokens()`
  (`card/ink/rule/steel/brick/mustard`), шрифты дека, кнопки-глифы через свою мини-`row()` (`▸/❚❚/↺`).
- Композиция (по прототипу, одна кривая скорости): слева — четверть единичной окружности (O внизу-слева),
  радиус O→P (ink), точка P (mustard+halo), тангенц. стрелка скорости (brick), проекция на гориз. ось = путь x
  (brick, растёт), угол α; сверху-справа — спидометр (единичная окружность, вектор скорости = радиус,
  повёрнутый на 90°, **золотая вертикаль = cos α**); справа — график скорости `cos` над осью `0…π/2`, маркер,
  и (сц5) золотая заливка площади. Подписей внутри канваса нет — только точки-границы «1» и метка `π/2`.
- Раскрытие через `onScene(k)`: sc≤2 — пусто/покой; sc3 — дуга+точка+путь x; sc4 — +скорость+спидометр+график;
  sc5 — +заливка площади (→1). Реализация раскрытия — флаг `this.reveal=k`, `draw()` рисует слои ≤ reveal.
- Движение: `settle()` = reset(t=0)+автозапуск (G3), проигрыш 0→1 за ~6 c c коротким ease-in у нуля (чтобы
  QA-кадр `--scene-diff` ловил почти нулевое t во всех сценах → аддитивность), затем покой на финальном
  (полном) кадре = «в покое = слайд». REDUCED → сразу финальный кадр. `measure()`/`pause()` по контракту.
- Мат-сверка (proof-self-review) на сборке: путь x(α)=sin α, площадь ∫₀^α cos = sin α, к π/2 = 1.
- Гейт B: standalone-рендер модуля на целевом размере; точка едет с нуля и не встаёт; cos виден на спидометре
  и графике; площадь=1; покой аккуратен. Коммит + audit.

**Фаза C — интеграция + вёрстка.** Основной план — **вариант (ii)** (предложение владельца): текст сверху,
БОЛЬШОЙ канвас снизу по центру; перевёрстка `#sl-area .grid/.copy/.rail` по `per-slide-algorithm.md`
(промеры, одна зона на .fit, без clamp/vh). Композиция графика — справа в канвасе → слово «сверху» в p5
станет неверным: микроправка одного слова (через math-russian-terminology/russian-editor, смысл сохранён,
в отчёт). Замена: два `data-ill` → один `<canvas data-sim="cos-area">`, раскрытие по сценам 3–5, тексты
p1–p6 сохранены. **Фолбэк** — вариант (i) (сетка как есть, канвас в правой доске, график сверху → «сверху»
не трогаем): если (ii) по рендеру выйдет рискованным/некрасивым — оставляю (i), развилку в ВОПРОСЫ.
Гейт C: sc1–2 — вывод S=2 без анимации; sc3–5 — раскрытие синхронно с p4–p6; пустых сцен нет; тело не ниже
пола 35px; ничего не наложилось. Коммит + audit + `--scene-diff`.

**Фаза D — верификатор.** Свежий субагент-рендерер: 5 сцен + `audit.py --scene-diff`; подтвердить критерии
B/C, что S=2 и p1–p3 целы, движок [4] не тронут, другие слайды не тронуты.

**Границы (не трогаю):** блок [4] ENGINE; математику S=2 (p1–p2); тексты p1–p3; другие слайды.
Отдельными коммитами: baseline (сейчас) → B → C. STOP-флаг: развилку вёрстки — в ВОПРОСЫ, не застреваю.

## ВОПРОСЫ — (исполнитель)

1. **Вёрстка: выбран вариант (i), НЕ (ii) — по замеру, не по осторожности.** Текст `sl-area` занимает
   y=0…685 из 810 при теле уже 35.3px (пол — 35px). Значит (ii) «текст сверху + БОЛЬШОЙ канвас снизу»
   физически не влезает: даже раздав текст на всю ширину, он ≥ ~500px по высоте → канвасу остаётся < ~280px,
   это не «большой», а математика тела упёрлась бы в пол. Поэтому взял (i): сетка «текст-слева/доска-справа»
   как есть (текст не тронут, гарантированно влезает), две картинки в доске → один высокий живой канвас.
   Бонус (i): график скорости лёг СВЕРХУ канваса → слово «сверху» в p5 осталось верным, текст не правил.
   **Нужно решение владельца:** оставляем (i) (моя рекомендация — он и отрендерен, и выглядит модельно) или
   всё же хотите (ii) ценой ужатия/переписывания текста в более короткие строки (тогда это отдельная задача
   по тексту, не только вёрстке)?
2. **Осиротевшие шаблоны.** `<template id="ill-sl-area-1">` и `ill-sl-area-2` больше ниоткуда не
   вызываются (data-ill убран). Оставил их как есть (инертны, не рендерятся — минимум изменений). Удалить
   ради чистоты? — скажите, вынесу отдельным коммитом.
3. **Вне границ захода (не трогал):** пред-существующие фейлы аудита на ЧУЖИХ слайдах —
   `sl-title: zone art overflow` и `sl-coin: тело 32px < пол 35px`. Были в baseline до меня, к `sl-area`
   отношения не имеют. Кандидаты в отдельный заход.

## ОТЧЁТ — (исполнитель, 2026-07-04)

**Что сделал и зачем.** Оживил кинематический аргумент на `sl-area`: две статичные иллюстрации в правой
доске заменил ОДНИМ живым канвасом `data-sim="cos-area"`, раскрывающимся по сценам 3–5 синхронно с p4–p6.
- **Фаза B** — новый `lab-ext` модуль `cos-area` (вшит `<script>` в блок [3]): точка `P` идёт по четверти
  единичной окружности (α=время); горизонтальная проекция-путь `x=sin α` растёт 0→1; её скорость = `cos α`,
  показана и вектором у точки, и спидометром (скорость = радиус, повёрнутый на 90°; **золотая вертикаль =
  cos α**), и кривой `cos` сверху; на sc5 под кривой заливается площадь = путь = 1. Канон: цвета из
  `LabCore.tokens()`, кнопки-глифы `▸ ▸▸ ❚❚ ↺`, «в покое = слайд», автозапуск-с-нуля по входу в раскрытие
  (`onScene` рестартит при подъёме reveal до 3 — G3). Композиция — ВЫСОКАЯ (график сверху / кинематика+
  спидометр снизу) под узкую доску; фит-трансформ, размеро-независима.
- **Фаза C** — интеграция (вариант i): разметка `.rail` (два `data-ill` → один `<canvas>` с
  `data-scene-from="3"`), CSS (`.p-cos/.p-kin` → `.p-anim` 317×618 в доске). Тексты p1–p6 сохранены дословно.

**Раскрытие по сценам (проверено рендером):** sc≤2 — доска пуста, слева вывод S=2; sc3 — точка едет с нуля
по дуге + путь x + пустая рамка графика; sc4 — +вектор+спидометр+кривая cos; sc5 — +заливка площади → в
финале покоя площадь заполнена целиком = 1.

**Мат-сверка (proof-self-review).** `x(α)=sin α`, `dx/dα=cos α`, `∫₀^{π/2}cos = 1` = путь. Вектор скорости
единичный и ⟂ радиусу (`P·V=0`) → «повёрнутый радиус», горизонт. компонента = cos α. Совпадает с p4–p6.
Математику S=2 (p1–p2) и тексты p1–p3 НЕ трогал (граница §1).

**Как проверял.** Рендер системным Chrome (`channel='chrome'`; Playwright-браузер не качается — нет сети, но
рендер локальный). Все 5 сцен + финальный кадр покоя отсмотрел глазами (standalone-харнесс на этапе B и в
контексте дека на этапе C). `audit.py` (канон) и `audit.py --scene-diff`.

**Результат аудита.** Канон: НОВЫХ фейлов у `sl-area` нет (все его проверки PASS). Единственные 2 фейла —
пред-существующие на чужих слайдах (`sl-title`, `sl-coin`), были в baseline. Scene-diff `sl-area`: «пустых
сцен нет» и «каждый клик раскрывает» — ВСЕ PASS; «adds pixels only» sc3→4 и sc4→5 — 0.06% (доброкачественное
перекрытие живыми слоями, НЕ скачок геометрии). Это НАИМЕНЬШИЙ non-additive среди 8 анимированных слайдов
дека (у прочих 0.08–2.94%) — цена оживления, ожидаемо.

**Верификатор (Фаза D, независимый субагент, свежий рендер).** Подтвердил все критерии B/C по картинкам:
S=2 и p1–p3 целы; sc3 точка едет с нуля и не застряла; sc4 скорость=cos читается на спидометре и графике;
sc5 площадь=1 в финале; пустых сцен нет; наложений/вылезаний/обрезки нет; рендер не «встал». Новых фейлов
нет. Дефектов глазами не нашёл.

**Что НЕ трогал.** Блок [4] ENGINE (движок поднимает `cos-area` штатно через `activate()`); математику S=2;
тексты p1–p3; другие слайды; осиротевшие шаблоны (см. ВОПРОС 2).

**Выбор вёрстки — вариант (i), обоснование в ВОПРОСЕ 1** (замер: (ii) не влезает по высоте текста).

**Коммиты (ветка `buffon-zahod-01`, точка отката — baseline `5afe5ef`):**
`5afe5ef` baseline (приёмка ЗАХОД-02 + ПЛАН) → `4c0b53a` Фаза B (модуль) → `59e13ed` Фаза C (интеграция).

**Время/токены.** Один непрерывный заход (с одним разрывом сети — переключился на системный Chrome).
Верификатор-субагент: ~65k токенов, 17 вызовов, ~4.3 мин. Точный счёт токенов основного контекста не измеряю.

**Недобор — честно:** (1) scene-diff `sl-area` не «весь зелёный» — 2 строки по 0.06% (аддитивность),
неизбежно для анимации, значимые проверки пройдены; (2) вариант (ii) не реализован — по замеру невозможен без
переписывания текста (ВОПРОС 1); (3) осиротевшие шаблоны оставлены на решение владельца (ВОПРОС 2).
