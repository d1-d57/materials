# Канал исполнителя — ZAHOD-04 · Канвас-подсистема + мелкие правки: A1–A6 одним заходом

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные ниже якоря в `index.html` — **грепом по сигнатурам, не целиком** (файл 2.8 МБ, вшитый base64: `Read` не тянет, широкий grep поймает шрифты). Проект не изучай.
> План/вопросы/отчёт пиши в секции внизу ЭТОГО файла (`## ПЛАН` / `## ВОПРОСЫ` / `## ОТЧЁТ`). Метрика — КАЧЕСТВО рендера, не скорость. Часы — норма.

**Как автор запускает (не часть работы исполнителя):**
```
Модель: opusplan — план рождается по ходу (A3/A5 — диагностика+починка без готовых якорей); механику фаз D–G гони быстрой.
Твой заход — файл /Users/ivanyakovlev/Documents/GitHub/materials/buffon/ZAHOD-04.md.
Прочитай ТОЛЬКО его и то, что он называет; проект не изучай.
Точка отката: git-коммиты по блокам на ветке buffon-zahod-01 (репо на уровне materials/).
План/вопросы/отчёт — в секции внизу этого же файла. Ничего сверх задачи не трогай.
```

---

## КОНТЕКСТ

Дек «Игла Бюффона» — один `index.html` (холст 1440×810, 23 слайда, канвас-симуляции на ядре `Lab`), канон скилла **html-slides-studio**. Ты на ветке `buffon-zahod-01` (в `main` не влита). Приняты ЗАХОД-01/02/03 (пересобрана первая половина, вылечен системный слой G1–G6, вживлена анимация `cos-area` на `sl-area`).

**ЦЕЛЬ:** закрыть шесть правок из живого ревью автора (после лекции) — **A1–A6 за один заход**. Три канвас-системные (A3/A5/A6) + три мелкие пер-слайдовые (A1/A2/A4). Все — в один `index.html`.

**Скорость через безопасную параллельность (важно, прочти):**
- **Диагностику/рендер (read-only) — ПАРАЛЛЕЛЬНО** субагентами (Фаза A): рендер каждого канваса, замеры, локализация якорей. Это медленная часть — её и распараллеливаем.
- **Записи в `index.html` — СТРОГО ПОСЛЕДОВАТЕЛЬНО, один писатель.** НЕ запускай параллельных агентов на правку файла: регионы пересекаются (`yellow` — в A3 и A5; `cos-area` — в A4 и A5; общий CSS `.lab-row`), два писателя затрут друг друга / дадут git-конфликт. Каждая правка — свой коммит.

Приёмка — по твоему `## ОТЧЁТ`, без построчной перепроверки. **Твоя приёмка НЕ финальная:** аналитик/автор смотрит глазами на своей машине. Поэтому единственное, что делает отчёт принимаемым, — **PNG-рендеры затронутых слайдов в папку проекта** (не /tmp) + честные замеры (Фаза H). Без картинок отчёт не принять.

> **Ложная предпосылка, которую ОСПОРИТЬ (A3).** ЗАХОД-01 заявил: все боковые сведены к `spacing:158`, окружность и многоугольники «одинаковы ~4.1 периода», претензия «у окружности мельче» опровергнута. Автор глазами видит: у окружности шаг очень мелкий. **Живой код подтверждает автора:** окружность строится с `spacing:158`, но тут же переопределяет шаг — `this.T = this.H / 12`. Значит «всё 158» ложно на рендере. Проверь по коду и рендеру, но исходи из того, что прав автор.

---

## 0. ПЕРВЫЙ ХОД

- Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/materials/buffon/`. Правишь `index.html`.
- **Откат:** ты под git (ветка `buffon-zahod-01`, репо на уровне `materials/`). Коммить по блокам (каждая фаза B–G — свой коммит). Если git недоступен — zip-бэкап `index.html` ПЕРВЫМ действием.
- **Читать ТОЛЬКО:** этот файл + указанные ниже сигнатуры в `index.html` (грепом). При нужде в машинном аудите/скрин-diff — скрипты скилла **html-slides-studio** (`scripts/audit.py`, `references/`). Прочие доки (`WORKLIST`, `README`, прошлые ZAHOD) НЕ читай — контекст здесь.
- **Рендер:** системный Chrome через playwright `channel="chrome"` (скачиваемый браузер в лимит песочницы не встаёт). Слайд — 1440×810.
- ПЛАН в `## ПЛАН` ПЕРЕД правками.

---

## 1. ДИСЦИПЛИНА (Карпатов)

1. **Думать до правки.** Предпосылки/развилки — в `## ПЛАН`. Ложную предпосылку (в т.ч. из захода/от автора) оспорь по живым файлам с доказательством.
2. **Минимум.** Ровно то, что чинит A1–A6. Никаких «заодно», абстракций «на будущее», переписывания рабочего кода ради красоты.
3. **Хирургия.** Каждая изменённая строка трассируется к конкретной A-правке. Чужой стиль (кавычки, отступы, имена) сохраняй. Удаляй только осиротевшее ТВОЕЙ правкой.
4. **Цель — проверяемое условие.** Критерии фаз могут ПРОВАЛИТЬСЯ; итерируй до зелёного по РЕНДЕРУ. «Выглядит правильно» без картинки — не критерий.
- **Якорные замены — по сигнатуре, не по номеру строки** (строки сместились за 3 захода; номера ниже — ориентир). grep по сигнатуре даёт ≠1 совпадение → СТОП, не угадывай, спроси в `## ВОПРОСЫ`.
- **Субагенты — аккуратный режим.** Фаза A: до **6 параллельных read-only** субагентов (по канвасу/правке — рендер+замер+локализация якорей), собери дистилляты. Фаза H: ОДИН свежий верификатор (другой глаз). Мутации (B–G) НЕ делегировать параллельно — один писатель. Рейт-лимит → отступить и доложить, не слепой ретрай. Тяжёлое суждение (какой шаг унифицировать, что дохлое) — на основной сильной модели.

---

## 2. ГРАНИЦЫ (что НЕ трогать)

- **Блок `[4]` ENGINE — verbatim.** Начинается с маркера `/* ===== [4] ENGINE — канонический из deck-skeleton.html ===== */` (~L2592). Всё ДО него (ядро `Lab`, sim'ы, `CosArea` L2396–2586) — редактируемо; движок — нет. `activate/measure/settle/MutationObserver` — в ядре, редактируемы.
- **Математику не трогать:** `S=2`, `level: 2/π` (result/needles), `level: 1/π` (circle), геометрию доказательств. A3 — про ШАГ РАЗЛИНОВКИ и размер фигуры, не про матсодержание.
- **A3 — только семейство «боковых»:** `yellow, polygons, circle, result`. Прочие sim по разлиновке НЕ унифицировать (у них свой смысл шага, напр. `phase-mass`: `this.T = 92 // = длина иглы`).
- **Не переоткрывать принятое** (палитра G2, золото G4, дробление сцен, содержание слайдов). `phase-mass` намеренно НЕ endless — если Фаза A найдёт его «замирание», это ИЗВЕСТНО, НЕ форсить endless; трогать только если это и есть цель A5 — тогда через `## ВОПРОСЫ`.
- **Перекрытие с прошлым:** `sl-result` «старт не с нуля» — у `result` уже `endless:true`, проверь зелёным на рендере, не открывай вслепую. `cos-area` — принят в ЗАХОД-03; A4 меняет ТОЛЬКО порядок раскрытия сцен, не математику анимации.
- Слайды `sl-title/vizitka/sim/divider*` и вторую половину (17–22) руками не трогать.

---

## 3. ЗАДАЧА — фазы (аудит → последовательные мутации → сверка)

### Фаза A — АУДИТ + ЛОКАЛИЗАЦИЯ (read-only, ПАРАЛЛЕЛЬНЫЕ субагенты, БЕЗ правок)
Разложи на ≤6 read-only субагентов, собери в `## ОТЧЁТ`:

1. **[A3] Таблица разлиновки** (yellow, polygons, circle, result): эффективный `this.T` (CSS-px после всех переопределений), `clientHeight` панели, число видимых линий, и **замер шага на РЕНДЕРЕ в CSS-px по скриншоту** (не «периоды» из кода); визуальный размер фигуры. Корень-якорь: у `circle` (сигнатура ctor `spacing: 158, seed: 158, autoTarget: 100 ... level: 1 / Math.PI`) сразу `this.T = this.H / 12;`. База: `this.T = this.o.spacing || 90`.
2. **[A5] Таблица G3** (все 12 sim: `needles, coin, convergence, yellow, prob, polygons, circle, result, phase-mass, coords-needles, coords-phase, cos-area`): `endless?` · старт-с-нуля при входе? · сброс на по-вторном входе (enter→leave→re-enter)? · сам стопается на target? · верный размер при первом показе? Якоря: `function activate(slide, justShown)` → `if (exp.measure) exp.measure(); if (exp.settle) exp.settle(); else {reset();play();}`; `MutationObserver`; само-стоп `if (self.n >= self.target && !self.o.endless)`. `endless` уже у coin/prob/polygons/circle/result. Прайм-подозреваемые: `yellow` (init-гонка), `convergence` (преролл/по-вторный вход), `cos-area` (сброс на входе — сам стоп на `t≥1` это норма).
3. **[A6]** Рендер последней сцены `sl-result`: где `.lab-row` вылезает, относительно какого предка позиционируется.
4. **[A1]** `sl-interval` (`<section id="sl-interval" data-scenes="3">`): найти правую иллюстрацию-градусник и спаны `blur-reveal[data-reveal]`; текущий вид вопроса `<p data-scene-from="2"><b>Чему равна вероятность, если диаметр d?</b>`.
5. **[A2]** `sl-prob` (`<section id="sl-prob" data-scenes="2">`): фраза `<p>Треугольник либо не пересекает линию, либо пересекает её дважды</p>` — куда лечь маленькой илл. (на этом слайде или предыдущем).
6. **[A4]** `cos-area`: отрендерить сцены слайда `sl-area` 3/4/5, зафиксировать что рисуется при каждом `reveal`. Якоря: `CosArea.prototype.onScene` (`rise = k>=3 && reveal<3`), `.draw`, `._stage`, `._speedo`, `._graphFrame`, `._graphCurve`.

**ГЕЙТ Фазы A:** таблицы + карта якорей по ВСЕМ A1–A6; числа разлиновки — замеры с рендера; A5-цель названа с доказательством-скрином. Если A5 внезапно >2 sim или упирается в границу — стоп, в `## ВОПРОСЫ`. Правок нет — коммита нет.

### Фаза B — A3: честная унификация разлиновки + крупнее окружность. (коммит)
Свести `this.T` четырёх боковых к одному (реком. 158 — текущее polygons/result; подтверди по таблице A); убрать/заменить `this.T = this.H / 12` у `circle`; окружность увеличить (радиус — по рендеру под крупный шаг).
**КРИТЕРИЙ:** на рендере расстояние между линиями у yellow/polygons/circle/result одинаково (замер CSS-px ±5%); в коде их `this.T` равны; диаметр окружности заметно больше прежнего; фигуры не вылезают/не наезжают некрасиво; прочие sim по разлиновке не изменены.

### Фаза C — A5: оживить оставшийся дохлый G3. (коммит)
Починить КАЖДЫЙ помеченный в таблице G3 sim **тем же механизмом ядра** (`measure()` от init-гонки / `reset()` на входе / `endless` для experiment-канваса) — без пер-sim костылей. Не форсить `endless` на `phase-mass`.
**КРИТЕРИЙ:** цикл вход→уход→по-вторный вход: помеченный канвас стартует с нуля, анимируется, сбрасывается на по-вторном входе; повторная таблица G3 — 0 фейлов (кроме намеренного `phase-mass`, если не был целью).

### Фаза D — A6: кнопки `.lab-row` в границах на sl-result. (коммит)
Якоря: база `.lab-row {position:absolute; right:12px; bottom:8px}`; общий блок `#sl-polygons .lab-row, #sl-circle .lab-row, #sl-result .lab-row {right:50%; transform:translateX(50%); bottom:6px; gap:2px}`; образцы сдвигов `#sl-condition/#sl-convergence/#sl-phase .lab-row {...}`; разметка `sl-result` → `.rail` → `.panel.p-contours` (`left:20px; top:81px; width:263px; height:648px`) → `<canvas data-sim="result">`. Почини минимально: сдвиг `#sl-result .lab-row` или DOM-якорь панели. `sl-polygons/sl-circle` тот же блок и НЕ сломаны — не трогать.
**КРИТЕРИЙ:** на рендере последней сцены `sl-result` обе кнопки целиком внутри `.p-contours` (0 px за краем); на hover видны; polygons/circle без изменений.

### Фаза E — A1: sl-interval — убрать спойлер, оживить. (коммит)
- Значение вероятности при диаметре `d` — НЕ под блюром: вынести отдельной фразой внизу.
- На правой илл.-градуснике подписать **«¼»** (аккуратно, вверху у красного — допустимое исключение из «без подписей»).
- При переключении на `d` линия раздвигается/сужается — видно, что становится = `d` (лёгкая анимация; илл. справа уже есть).
**КРИТЕРИЙ:** на рендере сцен sl-interval значение не под блюром; «¼» подписано на градуснике; при сцене про `d` линия визуально меняет ширину к `d`; вёрстка не поехала.

### Фаза F — A2: sl-prob — илл. двух точек пересечения. (коммит)
Рядом с фразой «…либо пересекает её дважды» — маленькая иллюстрация: прямая с ДВУМЯ точками пересечения (на sl-prob или предыдущем слайде). Стиль — как соседние илл.
**КРИТЕРИЙ:** на рендере видна маленькая корректная илл. «прямая + 2 точки» у нужной фразы; не ломает раскладку/сцены слайда.

### Фаза G — A4: cos-area — переиграть порядок раскрытия. (коммит)
Сейчас порядок неинтуитивен (нижняя картинка раньше, верх пуст). Надо: на первой сцене раскрытия (slide sc3) — **только точка** едет с единичной скоростью, проекцию НЕ рисовать; со второй (sc4) — появляются движущаяся проекция И график её скорости. Факт «площадь = путь» комментировать НЕ надо (автор: «тут всё нормально»). Правка — гейтинг по `this.reveal` в `draw`/`_stage`/`_speedo`/`_graph*` (проекция+спидометр+график только при `reveal ≥ 4`; при `reveal == 3` — точка на дуге). Математику анимации не трогать.
**КРИТЕРИЙ:** рендер sc3 — только едущая точка (нет проекции/графика скорости); sc4 — проекция + график скорости появляются вместе; sc5 — как было (площадь=1); анимация считает то же.

### Фаза H — ВЕРИФИКАТОР (read-only, свежий субагент). См. §4.

---

## 4. ВЕРИФИКАТОР (гейт захода)

Свежим субагентом (не тем, что правил), другим методом:
- Заново отрендери ВСЕ затронутые слайды с нуля: `sl-yellow, sl-polygons, sl-circle, sl-result, sl-interval, sl-prob, sl-area` (+ помеченные в C).
- Проверь КАЖДЫЙ критерий фаз B–G по картинке; замер разлиновки — программно/по скрину.
- Регрессии: `audit.py --scene-diff` (если доступен) — пустых сцен 0, non-additive не вырос; блок `[4]` байт-идентичен (`git diff` по диапазону движка пуст); математика (`S=2`, `level`) цела; слайды вне A1–A6 не изменены (`git diff --stat`).
- **PNG ключевых слайдов — в папку проекта** (напр. `buffon/_render-zahod04/`), НЕ в /tmp.
- Верни: критерий → ✓/✗ + скрин; любые регрессии.

---

## 5. ОТЧЁТ → пиши в `## ОТЧЁТ` внизу

Что сделал и ЗАЧЕМ (трассировка к A1–A6) · как проверил (рендеры, замеры) · что НЕ трогал (границы §2) · таблицы Фазы A · развилки/оспоренные предпосылки · результат верификатора (Фаза H) + путь к папке PNG · **время прогона + потраченные токены** · ГРОМКО: что осталось незакрытым, если осталось (тихий недобор = провал гейта).

---

## ПЛАН

**Инфраструктура.** git ветка `buffon-zahod-01` ✓ (репо на уровне `materials/`, откат — коммитами). Рендер-харнесс `_render-zahod04/shot.py` (playwright channel=chrome, 1440×810, dpr2): режим `static` через движковый `?only=idx&scene=k` (activate срабатывает 1 раз на load) и режим `--nav "showSingle(i,k)"` (реальная навигация → MutationObserver → activate с justShown для теста A5). Смоук-тест sl-circle прошёл.

**Карта симов (0-based idx / section / класс-ctor / эфф. this.T).**
| idx | section | data-sim | класс (стр) | ctor spacing | эфф. this.T |
|----|---------|----------|-------------|--------------|-------------|
| 6 | sl-interval | — | — | — | — (A1) |
| 9 | sl-yellow | yellow | Yellow L1024 | 158 | **158** |
| 11 | sl-prob | prob | Prob L1727 | 96 | 96 (A2 — тут илл., не разлиновка) |
| 12 | sl-polygons | polygons | Polygons L1783 | 158 | **158** |
| 13 | sl-circle | circle | Circle L1874 | 158 | **H/12 ≈ 13** ← выброс |
| 14 | sl-result | result | (Object.create) L1927 | 158 | **158** |
| 20 | sl-area | cos-area | CosArea L2396 | — | — (A4) |
Прочие: needles(90), coin(200,endless), convergence, phase-mass(92=длина иглы), coords-*.

**A3 — оспоренная предпосылка (ДОКАЗАНО по коду).** ЗАХОД-01 «всё 158, ~4.1 периода» — ЛОЖНО для окружности. Circle L1877 `this.T = this.H / 12;` переопределяет шаг на H/12 (12 периодов в панели вместо ~4). Автор прав: у окружности шаг ~в 3× мельче. База `Lab` L821 `this.T = this.o.spacing || 90` — CSS-px напрямую (ctx масштабирован dpr, this.H=clientHeight), значит шаг разлиновки в CSS-px = this.T. → yellow/polygons/result = 158 px, circle = H/12 px.
**Фикс B (минимальный):** удалить L1877 `this.T = this.H / 12;`. Тогда (а) T=158 как у остальных — шаг унифицирован; (б) `this.r = this.T/(2π)` (L1878) автоматически даёт r≈25px вместо ~2px — окружность крупнее в ~12×, при этом отношение r/T=1/2π сохранено → математика (hit ⇔ dist<r, доля→1/π) цела. Проверить рендером: замер шага у 4 боковых (±5%), окружность не вылезает.

**A1 (sl-interval, idx6, 3 сцены).** Ответ «вероятность = d» сейчас под блюром: `<p data-scene-from="2">…если диаметр d?: <span class="fill"><span class="blur-reveal" data-reveal="3"> d </span>`. Илл.-градусник — `data-ill="sl-interval-1"` (шаблон `#ill-sl-interval-1`). Правки E: (1) вынести значение d вниз без блюра; (2) «¼» на градуснике; (3) анимация линии → ширина d. [DOM-детали шаблона — субагент SubC]

**A5/A4/A6/A2 — уточняется субагентами** (SubA: re-entry-свип 12 симов; SubB: CosArea sc3/4/5 + гейтинг; SubC: DOM sl-interval/sl-prob/sl-result). См. таблицы в ОТЧЁТ.

**Порядок мутаций (один писатель, коммит на фазу):** B[A3]→C[A5]→D[A6]→E[A1]→F[A2]→G[A4]→H[верификатор]. Пересечения регионов (yellow в A3/A5; cos-area в A4/A5) — потому строго последовательно.

## ВОПРОСЫ

**1. A3 — ложная предпосылка ЗАХОД-01 подтверждена как ложная (сделано по правому варианту — автора).** Замер на рендере ДО правки: yellow/polygons/result = 157.5 CSS-px между линиями (по 4 линии), circle = **53.8 CSS-px (12 линий)** — шаг у окружности втрое мельче. Причина в коде: `Circle` L1877 `this.T = this.H / 12`. Исправлено (Фаза B). Тонкость, которую сообщаю: circle раньше брал именно H/12, чтобы в панель влезало ЦЕЛОЕ число периодов (равномерная выборка cy честна без смещения). Но yellow/polygons/result и так берут дробное число периодов (H/158 ≈ 3.5–4.1) и это принято — так что унификация circle к 158 не хуже соседей (крохотное смещение статистики того же порядка, что у них). Если для окружности важна именно несмещённость — скажи, верну целопериодную выборку при шаге 158 (правка в `throwOne`, чуть больше минимума).

**2. A5 — ГРОМКО: «оставшийся дохлый G3» НЕ воспроизвёлся. Фаза C — no-op (кода не менял).** Свип всех 12 симуляций (счётчик `.n`/`.t` через реальную навигацию enter→leave→re-enter + визуальная проверка скринов) показал 0 сломанных. Прайм-подозреваемые чисты: **yellow** — на первом показе рисует треугольники+иглы+живой график, без init-гонки/пустоты/кривого размера (скрин `a5_yellow_1enter.png`); **convergence** — преролл + сброс на входе работают (n: 1394→562→2866); **cos-area** — сам стоп на t≥1 (норма, ты это и отмечал), на повторном входе t→0 и переигрывает (скрин `a5_cos-area_2reenter.png`). `phase-mass@sl-phase` намеренно не-endless (известно). Вывод: системный слой G1–G6 уже вылечен прошлыми заходами; A5 закрыт как «проверено-здорово». **Развилка:** если на твоей машине конкретная симуляция всё же «замирает» — назови её и шаги навигации (какими клавишами, с какого слайда), это ловится только конкретным сценарием. Инвертировать/чинить наугад здоровый код не стал (принцип минимума).

## ОТЧЁТ

**Итог: A1, A2, A3, A4, A6 закрыты и проверены рендером (свежий верификатор — ALL PASS). A5 — no-op (ничего не сломано; см. ВОПРОСЫ №2, флаг громкий).** Границы §2 соблюдены: движок `[4]` байт-идентичен (в diff нет ни строки ENGINE/showSingle/applyScene/…), математика цела (`S=2`, `level 2/π`, `1/π`, `const W=1440,H=810` на месте), изменён только `buffon/index.html`, слайды вне A1–A6 не тронуты. 6 коммитов по фазам на `buffon-zahod-01`.

### Что сделал и зачем (трассировка)
| Правка | Фаза/коммит | Суть | Файл-якорь |
|---|---|---|---|
| **A3** унификация разлиновки + крупнее окружность | B `47074e2` | Удалён `Circle` `this.T = this.H/12` → T=158 как yellow/polygons/result; `r=T/2π` авто-вырос ×3 (математика 1/π цела) | `function Circle` L~1888 |
| **A6** кнопки в границах панели | D `1709504` | `#sl-result .lab-row { transform:none }` гасит остаточный `translateX(50%)`, выносивший кнопки на 48px за край | CSS L~327 |
| **A4** порядок раскрытия cos-area | G `fe22352` | `_graphFrame`→ветку `reveal>=4`; блок проекции (L2526–2530) обёрнут в `if(reveal>=4)`. sc3 — только точка на дуге | `CosArea.draw`/`_stage` L~2509/2535 |
| **A1** де-спойлер + живой градусник | E `7efa6f1` | Ответ «вероятность = d» вынесен в отд. фразу без блюра (сцена 3); градусник: подпись ¼→d, крест-зоны анимированно сужаются к d (scaleY на `.scene-3`), стальной стержень во всю высоту | HTML L~517, CSS L~189, шаблон L~728 |
| **A2** илл. двух точек | F `3cbbdd9` | Новый шаблон `ill-prob-2p` (прямая + треугольник, 2 красные точки), `ill-box` в правом-верхнем углу текст-зоны sl-prob | HTML L~581, CSS L~250, шаблон L~728 |

### Фаза A — аудит (read-only, 3 параллельных субагента + мой замер)
- **A3-таблица (замер на РЕНДЕРЕ, CSS-px).** ДО: yellow 157.5 (4 линии) · polygons 157.5 (4) · result 157.5 (4) · **circle 53.8 (12 линий, r=8.6)**. ПОСЛЕ: все четыре 157.5 (4 линии), circle r=25.1 (диаметр ×~3). Предпосылка ЗАХОД-01 «всё 158» — опровергнута (ВОПРОСЫ №1).
- **A5-таблица (G3, 12 симуляций).** Все живые: стартуют с нуля, анимируются, сбрасываются на повторном входе; не-endless сами стопаются на target. 0 сломанных (ВОПРОСЫ №2).
- **A4:** `onScene` = 1:1 scene→reveal; корень — весь `_stage` (вкл. проекцию) + пустой `_graphFrame` рисовались уже при reveal 3.
- **A6:** lab-row overflow +48px вправо — инлайн `right:8px` (из JS) перебивал `right:50%`, но `translateX(50%)` оставался.
- **A1:** ответ d под `blur-reveal data-reveal=3`; градусник — inline-SVG (viewBox 88×649, полоса 0..1 = y29.3→603.7, ¼ = 143.6px).
- **A2:** фраза на sl-prob (row2), места справа от 1-й строки хватает; соседние илл. — inline-SVG `ill-box`/`data-ill`.

### Как проверил
- Каждая фаза — рендер затронутых сцен системным Chrome (playwright channel=chrome, 1440×810, dpr2) до зелёного по КАРТИНКЕ; A3/A6 — ещё и объективный замер (`window.LAB[id].T`, `getBoundingClientRect`).
- **Фаза H — свежий верификатор** (не тот, что правил): заново отрендерил sl-yellow/polygons/circle/result/interval/prob/area, проверил каждый критерий B–G глазами. **ALL PASS.** Объективно: T===158 у всех четырёх боковых; lab-row.right 1396.96 ≤ panel 1404.94 (внутри). Симы не пустые (регрессий нет).
- **PNG ключевых слайдов — в папке проекта `buffon/_render-zahod04/`** (не /tmp): `verify_a1_interval_sc2/sc3`, `verify_a2_prob_sc1/sc2`, `verify_a3_{yellow,polygons,circle,result}`, `verify_a4_area_sc3/sc4/sc5` + до/после каждой фазы (`a3_*_before/after`, `a6_result_after`, `e_interval_*`, `f_prob_*`, `g_area_*`). Скрипты аудита/замера там же.

### Границы (§2) — что НЕ трогал
Движок `[4]` (verbatim), математика (`S=2`, `level`, геометрия), палитра/золото/дробление сцен (принято), `phase-mass` не-endless (намеренно), sl-polygons/sl-circle lab-row (общий блок, не сломан), слайды title/vizitka/sim/divider/вторая половина.

### Развилки / оспоренные предпосылки
1. **A3:** предпосылка ЗАХОД-01 ложна — исправлял по варианту автора, с замером-доказательством (ВОПРОСЫ №1; тонкость про целопериодную выборку — там же).
2. **A5:** предпосылка захода «есть дохлый G3» не воспроизвелась — Фаза C сделана как no-op, наугад здоровый код не трогал (ВОПРОСЫ №2, флаг громкий).

### Незакрытое (громко)
- **A5 — кода не менял** (нечего чинить по двум независимым сигналам + скринам). Если у автора на его машине конкретный sim всё же замирает — нужен сценарий навигации, тогда доловлю. Это ЕДИНСТВЕННЫЙ пункт, где результат — «проверено-здорово», а не «поправлено».

### Время / токены
Прогон ~1 час (аудит + 5 мутаций + верификация; коммиты 20:16→20:40). Токены: 4 субагента = **277 274** (A5-свип 65 918 · A4 55 958 · A1/A2/A6 DOM 73 384 · верификатор 82 002) + основной цикл (чтения 2.8 МБ-файла узкими окнами, ~30 рендеров) — оценочно суммарно порядка 0.5–0.7 млн.
