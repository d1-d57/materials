# Канал исполнителя — «Игла Бюффона»: первая половина + системный слой (один заход до конца)

> Твой единственный файл-заход. Читай ТОЛЬКО его и названные ниже якоря; проект целиком не изучай.
> План / вопросы / отчёт пиши в секции внизу ЭТОГО файла (## ПЛАН / ## ВОПРОСЫ / ## ОТЧЁТ).
> Метрика — КАЧЕСТВО, не скорость. Часы работы — норма. Ничего сверх задачи не трогай.

## КОНТЕКСТ

Проект — самодостаточная HTML-презентация «Игла Бюффона»: один файл `index.html` (холст 1440×810,
23 слайда, канвас-симуляции), собранный по канону скилла **html-slides-studio**.

Прошлый этап (аналитик): разобран разбор автора, собраны `README.md` (карта) и `WORKLIST.md`
(пер-слайдовая спека + системные правила G1–G6 + диагностика кода). Правок в `index.html` ещё НЕ вносили.

ЦЕЛЬ: пересобрать **первую половину (слайды 1–15)** по `WORKLIST.md` и починить **системный слой
(G1–G6)** так, чтобы он покрыл и вторую половину; во второй половине (17–22) — только внедрить общие
правила, не переписывая её.

Приёмка — по твоему **ОТЧЁТУ** (без построчной перепроверки), но по ГЕЙТАМ (§5).

**СТОП-ФЛАГ (громко, простыми словами):** ты доводишь `index.html` до ГОТОВОГО, отрендеренного и
прошедшего аудит состояния. Отдельного «ночного прогона» ПОСЛЕ тебя нет — результат твоей работы и
есть финал к утру. Не останавливайся, пока все гейты не зелёные; если всё же остановился до цели —
заяви это ГРОМКО в отчёте (тихий недобор = провал гейта).

**Рекомендация по модели (окончательно выбирает владелец при запуске):** задача смешанная —
механические правки (системные fix, текст, цвет, золото, endless) + суждение (разбивка на сцены,
геометрия иллюстраций, визуальный QA). Лучший результат-на-цену — `opusplan` (план Opus, механика
Sonnet в одном прогоне). Чистый Sonnet тоже допустим — заход расписан максимально механически, — но
на геометрии/сценах жди риск и возможный второй заход-дочистку.

---

## 0. ПЕРВЫЙ ХОД

Рабочая папка: `/Users/ivanyakovlev/Documents/GitHub/materials/buffon/`

**Точка отката — СРАЗУ, первым действием:** проверь `git rev-parse --is-inside-work-tree`.
- Под git → коммить по блокам (после каждой фазы A/B/C/D), сообщение — фаза + что сделал.
- НЕ под git → сделай `zip`-бэкап папки (index.html + README.md + WORKLIST.md) — это единственный откат.

**Прочитай ТОЛЬКО это (твой канон и спека, шире не лезь):**
- ЭТОТ файл (заход) — порядок, границы, гейты.
- `./WORKLIST.md` — ГЛАВНЫЙ источник ЧТО менять: §0 правила G1–G6, §1 пер-слайдовые пункты 1–15,
  §2 открытые вопросы (закрыты), §5 диагностика кода с якорями.
- `./README.md` — карта: 4 блока `index.html`, индекс слайдов (id → строки → сцены → канвас → эталон),
  инварианты «не ломать», таблица радиуса правки.
- Скилл **html-slides-studio** (установлен) — канон КАК делать: `SKILL.md`,
  `references/per-slide-algorithm.md`, `references/slide-engine.md`, `references/simulations.md`,
  `references/export-png.md`, `scripts/audit.py`, `assets/etalons/buffon-p*.jpg` (исходные Canva-кадры).
  Если скилл недоступен — ключевые правила инлайнены ниже в § ПРОТОКОЛ; работай по ним.
- `index.html` — целевой файл. **Грепай узко:** в нём вшиты шрифты и base64 — исключай `data:image`
  и `data:font` из вывода, не тащи блобы в контекст.

Напиши **## ПЛАН перед правками**.

---

## 1. ДИСЦИПЛИНА (Карпатов) — обязательна

1. **Думать до правки.** Назови предпосылки, на которых действуешь; развилку — не молча, а в ## ВОПРОСЫ.
2. **Оспорь ложную предпосылку — даже из worklist/захода.** Диагностика `WORKLIST §5` сделана по коду,
   но номера строк в ней — ориентир на прошлую версию и МОГЛИ сместиться. Якорись по `id`/сигнатуре
   функции (`Lab.prototype.settle`, `function activate`, `function tokens`, `data-sim="…"`), НЕ по номеру
   строки. Не сходится с реальностью — скажи с доказательством в ## ВОПРОСЫ, не гоняйся за фантомом.
3. **Минимум.** Ровно то, что решает пункт. Никаких абстракций «на будущее», не переписывай рабочий
   код ради красоты.
4. **Хирургия.** Трогаешь только заказанное; каждая изменённая строка трассируется к пункту worklist.
   Чужой стиль сохраняй (кавычки, отступы, имена, русские комментарии-промеры). Якорная замена обязана
   матчиться **РОВНО один раз** — при 0 или >1 совпадений ОСТАНОВИСЬ и разберись, не применяй вслепую.
5. **Цель — проверяемое условие** (см. КРИТЕРИЙ в каждой фазе). «Выглядит правильно» — не критерий; рендер и audit — критерий.
6. **Субагенты (аккуратный режим).** Для визуальной верификации после блока можешь поднять свежий
   субагент-рендерер (другой глаз): рендерит тронутые слайды + `audit.py` + сверка с эталоном → список
   дефектов. Ориентир ≤5 параллельно; **рейт-лимит = отступить и в отчёт**, не слепой ретрай.

---

## 2. ГРАНИЦЫ (что НЕ трогать)

- **Блок `[4]` ENGINE** (навигация/сцены/фуллскрин) — verbatim. Единственное исключение — если
  системный fix G3 прямо про lifecycle-хук (`activate`/`settle`): тогда точечно, с обоснованием в отчёте.
- **Вторая половина (17–22)** — НЕ переписывать по содержанию/вёрстке (там хорошо). Только: (а) системные
  G1–G5 приходят из ядра/CSS сами; (б) G6 — аккуратно раздробить сцены, где 2–3 абзаца стоят одной. Тексты не трогать.
- **Слайды 1, 2, 4, 8** (первая половина) — без претензий, содержание не трогать (только системное покрытие).
- **Число на слайде 15** (`2/π ≈ 0.637`) — ВЕРНО, не менять. (Из него — только сделать формулы `2L/π`, `L/π` золотыми.)
- Тексты, НЕ помеченные в worklist, — не переписывать и не «улучшать».

---

## 3. ЗАДАЧА — фазами по РИСКУ (один риск за раз); коммит + аудит после каждой фазы

### Фаза A — АУДИТ (read-only, план)
Сверь диагностику `WORKLIST §5` на живом `index.html` по сигнатурам. Подтверди ИЛИ оспорь каждый корень
(мёртвый `settle`; само-стоп на `target`; reset-на-возврате через MutationObserver; init-гонка размеров;
`tokens()` без `--cgreen/--corange`; разнобой `spacing`). Выпиши в ## ПЛАН точные якоря для каждого
системного fix и для каждого пункта 1–15. Ничего не меняй.
**КРИТЕРИЙ:** у каждого пункта worklist §0–§1 назван конкретный якорь; расхождения — в ## ВОПРОСЫ.

### Фаза B — СИСТЕМНЫЙ СЛОЙ (G1–G6), чинить в ОДНОМ месте
- **G3 lifecycle:** оживить единый хук покоя — `activate(justShown)` должен вызывать `settle()` (sim
  переопределяют его), ЛИБО перенести пер-sim преролл (`Conv.settle` `throwN`) в `reset`/`onScene`, чтобы
  он РЕАЛЬНО срабатывал при входе. Мёртвый код убрать. *Проверяемо:* на слайде 5 график стартует с ~50, не с 0.
- **G3 endless:** experiment-канвасам (coin, prob, polygons, circle, result и одноимённым во 2-й половине)
  включить бесконечный режим, чтобы `play()` не вставал на `target` (стоп — только рукой). *Проверяемо:* канвас не замирает сам.
- **G3 reset-на-возврате:** подтвердить, что уход/возврат сбрасывает канвас в ноль и запускает заново.
  *Проверяемо:* назад-вперёд → канвас с нуля.
- **G3 init-гонка:** канвас мерит размеры при ПОКАЗЕ (в `activate`), не только в конструкторе. *Проверяемо:*
  yellow-модель включается с первого входа.
- **G2 палитра:** добавить `--cgreen`/`--corange` в `tokens()` (в объект `C`) — сейчас их там нет.
- **G1 масштаб:** единый принцип шага разлиновки на боковых канвасах (сейчас `spacing` 90/112/150/200/96/158/
  `H12`/150/92 вразнобой). Свести к общему правилу (напр. одинаковое число периодов на высоту панели). Визуально шаг един.
- **G4 золото:** blur-reveal ответ раскрывается акцентным золотом (не чёрным) — проверить класс/CSS.
- **G5 подсветка:** класс-акцент ЦВЕТОМ (жёлтый) вместо `<b>` для важного вопроса/изменившегося символа;
  не меняет метрику/вёрстку между сценами.
- (G6 — мета-правило дробления, применяется пер-слайдово в фазах C/D.)

Коммит/бэкап-веха + АУДИТ (рендер нескольких канвас-слайдов + `audit.py`).
**КРИТЕРИЙ:** audit зелёный; график-5 с ~50; канвасы бесконечны и стартуют с нуля; зел/оранж доступны в `C`; шаг разлиновки визуально един.

### Фаза C — ПЕР-СЛАЙД, первая половина (1–15)
По `WORKLIST §1`, слайд за слайдом, батчами (**1–7**, затем **8–15**); коммит + рендер + аудит после
каждого батча. Для КАЖДОГО слайда — цикл html-slides-studio: правка → рендер обоих видов (`?only=N`
масштаб + экспорт-кадр) → ПОСМОТРЕТЬ на картинку → сверка с эталоном `assets/etalons/buffon-p*.jpg` →
`audit.py` (+ `--scene-diff` для многосценовых).

Краткий индекс (ПОЛНАЯ спека — `WORKLIST §1`, это лишь напоминание):
- **6 coin:** сцена 1 — круги + 2 линии БЕЗ центров; сцена 2 — +центры всех кругов +полоски; убрать «200/по кнопке» (с нуля, endless); только красн/син (никакого жёлтого).
- **7 interval:** 2 сцены; нижние строки в едином стиле «…если … = ?» + блюр «½».
- **9 plan:** подпись «синий/красный» → «зелёный/оранжевый» (в тексте, якорь по фразе, не по строке); ОБЕ илл. — палочка длины 1 (= шаг) + МАЛЕНЬКИЙ треугольник, сторона ⅓ (периметр 1); шаг линий = 1.
- **10 yellow:** стороны треуг. жёлт/оранж/зел (сейчас 1 жёлт + 2 синих; красн/син на этом канвасе НЕ применять); сцены; шаги 1/⅓/1; чинить init.
- **11 count:** абзацы по одному на сцену (сейчас сразу два); правый треугольник — геометрия.
- **12 prob:** ответы золотыми.
- **13 polygons:** 8-угольник периметр 1; жирный вопрос → цветом (G5); 5 сцен (см. §1); переход 8→k — подсветить изменившийся символ.
- **14 circle:** шаг унифицировать (G1); +1 сцена (не два абзаца сразу); старт с нуля/endless.
- **15 result:** `2L/π` и `L/π` — золотыми; фразу про `L/π` упростить в две строки (нормальный русский, без «можно доказать, что» и «так же, как для окружности»); канвас выровнять (кнопки на место); с нуля.

**КРИТЕРИЙ:** каждый пункт §1 закрыт и ОТМЕЧЕН; каждый тронутый слайд отрендерен и «выглядит как
модельный слайд дека»; `audit.py` зелёный; канвасы бегут; сцены не пустые (`--scene-diff`).

### Фаза D — вторая половина (17–22): только G1–G6
Системные fix уже покрыли канвасы/цвета. Пройди 17–22 рендером и: примени **G6** (раздроби абзацы-стопки
на сцены, где стоят одной), подтверди G1/G2/G3/G4/G5. Тексты/вёрстку НЕ переписывать. Консервативно:
сомнение → ## ВОПРОСЫ, не ломать.
**КРИТЕРИЙ:** 17–22 отрендерены; где было 2–3 абзаца одной сценой — раздроблено; системные правила видны; регрессий нет.

### Фаза E — ФИНАЛЬНАЯ СВЕРКА (read-only)
Полный прогон: рендер всех 23 слайдов по сценам + `audit.py --scene-diff` по всему деку + сверка с
эталонами. Пройди список `WORKLIST §0–§1` **попунктно, буквально** (не «по духу») и отметь каждый.
**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):** `audit.py` exit 0; пустых сцен нет; все пункты worklist
§0–§1 + G6·(17–22) отмечены; канвасы (autostart-с-нуля, endless, reset-на-возврате, init с первого раза)
работают; слайды 1/2/4/8 и вся 2-я половина по содержанию не изменены; блок `[4]` verbatim; ни один
тронутый слайд не сломал соседний канвас.

---

## ПРОТОКОЛ html-slides-studio (инлайн-выжимка — на случай, если скилла нет)
- **#0 РЕНДЕРь и СМОТРИ.** После каждой правки геометрии/контента — рендер в двух видах (масштаб `?only=N`
  + экспорт-кадр) и суди по КАРТИНКЕ, не по коду. Критерий каждого слайда: «выглядит как чистый модельный
  слайд дека?». Overlap / пустая полоса / крошечный канвас / симуляция не бежит / не та сцена / переполнение — дефект: чинить и пересматривать.
- **#1 СЦЕНЫ.** Прощёлкать каждую; пустой клик (ничего не появилось) = дефект (или удалить сцену);
  ничего не появляется раньше своей сцены; `audit.py --scene-diff` FAIL-ит пустую сцену.
- **#2 КАНВАС.** Рендер + интеракция; симуляция реально бежит; кликер ведёт задуманные шаги; проверять
  функционально (состояние/хэш), не чтением кода.
- **Хирургия.** `.bak`/коммит перед батчем; якорь матчится РОВНО раз; узкий греп (исключай `data:image`/`data:font`/`three`).
- **Инварианты.** Канон-палитра (проверит `audit.py`); тело текста не ниже 4.3%H — не влезает → ДРОБИ
  слайд, не ужимай; неопределённая CSS-переменная в SVG-заливке → рендерится ЧЁРНЫМ (определяй все).
- **Симуляции.** Свежий seed/сброс на входе, «в покое = слайд»; кнопки — глифы `▸ ▸▸ ❚❚ ↺`, не эмодзи; узкая (compact) ветка док-панели обязательна.
- **Экспорт-кадр** — screenshot `?only=N`, НЕ печать браузера (fitted-текст ломается при печати).

---

## 4. ВЕРИФИКАТОР (системная правка = высокий риск регрессии канвасов)
После фаз B / C / D — свежий субагент-верификатор ДРУГИМ методом: рендерит тронутые слайды из ТЕКУЩЕГО
`index.html`, прогоняет `audit.py --scene-diff`, сверяет с эталонами и со списком пунктов worklist; отдаёт
«дефектов N / пусто». Автор правок себя не проверяет — у него слепые пятна.

## 5. ГЕЙТЫ ПРИЁМКИ → в ## ОТЧЁТ внизу
Что сделал + ЗАЧЕМ (трассировка к пунктам worklist) · как проверил (рендеры, `audit.py`, эталоны) · что
НЕ трогал (границы соблюдены) · вопросы и развилки · результат верификатора · **попунктный чеклист
worklist §0–§1 + G6·(17–22)** (☑/☐ с причиной) · **время прогона + потраченные токены**. Остановка до
цели — заявить ГРОМКО.

---

## ПЛАН — (заполняет исполнитель)

**Точка отката:** `index.html` закоммичен в git (`git ls-files` → tracked, рабочее дерево чистое; последний коммит `730c176 buffon: заменить на правильную версию`). Откат = `git checkout -- buffon/index.html`. Коммичу после каждой фазы. Плюс `.bak` перед каждым батчем правок.

**Харнес рендера:** `scratchpad/shot.py` (python-playwright через системный Chrome, кэш ms-playwright рассинхронен по версии — обойдено `channel="chrome"`). Рендер `?only=N&scene=K` в 1440×810. `audit.py` из скилла запускается тем же python-playwright. Скилл найден: `…/skills/html-slides-studio/` (есть audit.py, эталоны buffon-p*.jpg, references, lab.js).

### Фаза A — АУДИТ: диагностика §5 ПОДТВЕРЖДЕНА по сигнатурам (не по номерам строк)
- **G3 settle мёртв** — ✔ подтверждено: `grep '\.settle('` пусто; `activate()` (L1266) в ветке `justShown` (L1278) делает `pause()+reset()+play()` в обход `settle()`. Значит `Conv.settle` (L1352, преролл `throwN(100)`) НЕ запускается. `activate` — в блоке `[3]` (713–2337), НЕ в `[4]` ENGINE (2339+) → правка `activate` санкционирована исключением G3 захода.
- **G3 self-stop** — ✔ `Lab.prototype.play` L940: `if (self.n >= self.target && !self.o.endless)` стоп. Ни у одного experiment-sim нет `endless`.
- **G3 init-гонка** — ✔ `Lab()` L818–820: `box` берётся как ссылка на метод без вызова и не используется; W/H = `clientWidth/Height` меряются РАЗ в конструкторе. Первый показ при неготовой вёрстке → fallback 800/500 (баг yellow «не включился с первого раза»).
- **G3 reset-на-возврате** — ✔ `watch()`/MutationObserver L1285 ловит показ и зовёт `activate(s, justShown)`; механизм рабочий, «с середины» — следствие self-stop.
- **G2 tokens** — ✔ `tokens()` L797 не отдаёт `--cgreen/--corange`; сами переменные в CSS ЕСТЬ (L29: `#7ea474`/`#d98e4a`). Блокер для yellow (L1071 рисует «другие стороны» `C.steel`).
- **G1 spacing вразнобой** — ✔ needles 90 (L997), yellow 112 (L1020), coin 200 (L1528), prob 96 (L1697), polygons 158 (L1754), circle 158 (L1843), result 150 (L1898), coords 150 (L1106), phase-mass default 90 (L2036-37).
- **G4 gold** — ✔ `.blur-reveal` раскрывается в `color:inherit` (L387) = чернила; надо `var(--accent)` (охра-золото).
- **G5** — механизм `.acc` (L379, только цвет `--accent`, без веса → без сдвига метрики) заменяет `<b>` в помеченных местах.

### Фаза B — СИСТЕМНЫЙ СЛОЙ (правки, все в блоке [1]/[3], НЕ в [4])
1. **B-tokens (G2):** в `tokens()` (L797) добавить `cgreen: t('--cgreen','#7ea474'), corange: t('--corange','#d98e4a')`.
2. **B-lifecycle (G3 settle):** снять guard `if(this.n) return` в `Lab.prototype.settle` (L988) и `Conv.settle` (L1353); в `activate` (L1278) заменить `pause()+reset()+play()` на `pause()` → `settle()` (fallback `reset()+play()`). `Conv.settle` преролл `throwN(100)`→`throwN(50)` (Q3: автор хочет 50). Мёртвый код не остаётся — settle оживает.
3. **B-endless (G3):** добавить `endless:true` в opts: coin(L1528), prob(L1697), polygons(L1754), circle(L1843), result(L1898), phase-mass(L2036). needles/convergence — не трогаю (у conv свой play-override; needles ☑).
4. **B-init (G3 init-гонка):** добавить `Lab.prototype.measure()` (пере-замер `clientWidth/Height` + ресайз буферов offscreen/ctx), звать из `activate` justShown ДО settle, только если `clientWidth>0`.
5. **B-spacing (G1):** единый принцип — `spacing = H / k` (одинаковое число периодов k на высоту панели), k подобрать рендером под эталон; свести боковые к общему k. Точные значения — после замера рендером (в ВОПРОСАХ фиксирую k).
6. **B-gold (G4):** L387 `color:inherit` → `color:var(--accent)`.
Коммит + аудит (рендер conv/coin/yellow/polygons/circle/result + audit.py).

### Фаза C — ПЕР-СЛАЙД 1–15 (батчи 1–7, 8–15), якоря по id/фразе
- **3:** `[G1]` шаг разлиновки правого рисунка (SVG `ill-sl-buffon-*`).
- **6 coin:** сцена1 круги+2 линии без центров; сцена2 +центры+полоски; endless+с нуля (B); только brick/steel (проверить «жёлтый круг» — по §5 вероятно артефакт).
- **7 interval:** 2 сцены; нижние строки «…если …=?» + блюр «½» (SVG-термометр).
- **9 plan:** текст «синий/жёлтый/красный»→«зелёный/жёлтый/оранжевый» (якорь по фразе); ОБЕ илл. — палочка длины 1 + МАЛЕНЬКИЙ треуг. сторона ⅓, шаг=1.
- **10 yellow:** стороны mustard/corange/cgreen (L1071/1075); init (B); шаги 1/⅓/1 (G1).
- **11 count:** абзацы по одному на сцену; правый треуг. геометрия.
- **12 prob:** ответы золотыми (G4 покрывает; проверить).
- **13 polygons:** 8-уг периметр 1; жирный вопрос→`.acc` (G5); 5 сцен; 8→k подсветить `.acc`.
- **14 circle:** шаг (G1); +1 сцена; endless+с нуля (B).
- **15 result:** `2L/π` и `L/π` золотыми (G4); текст упростить в 2 строки; канвас выровнять; с нуля (B).
Коммит + рендер + audit после каждого батча. Верификатор-субагент после C.

### Фаза D — 17–22: только G6 (дробление сцен-стопок) + подтвердить G1–G5. Тексты/вёрстку не трогать.
### Фаза E — полный рендер 23 слайдов + `audit.py --scene-diff` + сверка эталонов + попунктный чеклист.

**Стратегия модели (opusplan):** правки одного большого файла делаю сам (сериально, хирургия); верификацию (свежий глаз: рендер+audit+сверка) отдаю Sonnet-субагентам после фаз B/C/D.

## ВОПРОСЫ — (заполняет исполнитель)

Развилок, требующих решения владельца, на входе НЕТ — заход закрыл Q1–Q6, остальное решается рендером (моё суждение). Фиксирую решения, принятые по ходу:
- **G1 k (число периодов):** значение подберу рендером под эталоны; занесу сюда фактические spacing.
- **Yellow «сотрём кроме жёлтой» (сцена 2):** по §1 два варианта; выберу по рендеру (склоняюсь к «всё жёлтым» на сцене, где активна только левая половина).

## ОТЧЁТ — (заполняет исполнитель)

**Модель:** opusplan (план Opus, механика в одном прогоне). Дата прогона: 2026-07-04.
**Ветка:** `buffon-zahod-01` (создана от `main`; main чист; НЕ пушил). Точка отката — коммиты фаз.

### Коммиты по фазам (чекпоинты отката)
- `5afe8c9` Фаза B — системный слой (settle/endless/tokens/measure/gold)
- `1c7103f` Фаза C батч 1–7 (coin, interval)
- `22e366c` Фаза C батч 8–15 ч.1 (yellow цвет, plan текст, result золото+текст)
- `9a9ebb9` Фаза C батч 8–15 ч.2 (count/prob/polygons-G5/circle сцены)
- `841c2e6` G1 канвасы (result/yellow → 158)
- `a20cf8a` ⅓-треугольник (ill-rail-triangle)
- (в `a20cf8a`+1) 8→k золото — коммит «слайд 13 8→k»
- `806c542` Фаза D (17–22 G6)

### Что сделал + ЗАЧЕМ (трассировка к worklist)
**Системный слой (чинил в ОДНОМ месте — ядро/CSS):**
- **G3 — главный корень.** `settle()` был МЁРТВ (никем не вызывался; `activate` шёл в обход через `pause+reset+play`), из-за чего преролл графика-5 не работал. Оживил: `activate(justShown)` → `pause()→measure()→settle()`; sim переопределяют `settle`. Убрал guard `if(this.n)return`, чтобы сброс срабатывал на КАЖДОМ входе. → график-5 стартует с ~50 (не с 0). [G3 lifecycle]
- **G3 endless.** `endless:true` для coin/prob/polygons/circle/result — `play()` больше не встаёт на `target` (стоп только рукой). [G3 endless]
- **G3 init-гонка.** `Lab.prototype.measure()` — пере-замер `clientWidth/Height` + ресайз буферов при ПОКАЗЕ (yellow «не включался с первого раза»). [G3 init]
- **G2.** `tokens()` теперь отдаёт `--cgreen/--corange` (были в CSS, не в объекте C — блокер). [G2]
- **G4.** `.blur-reveal` раскрывается `var(--accent)` (золото), а не `inherit` (чернила) — один рычаг на весь дек. [G4]

**Слайды (пер-слайдово):**
- **5** convergence: преролл `throwN(100)→50` + оживление settle → график с ~50. 
- **6** coin: сцены (onScene+rebake+all[]) — сц.1 круги+2 линии без центров/полос, сц.2 +центры+полосы; убрал мустард-вспышку (жёлтых кругов нет); autostart+endless.
- **7** interval: 2→3 сцены (прогрессивный reveal, ответ d золотом, стиль «…?: [блюр]»).
- **9** plan: текст «синий/красный»→«зелёный/оранжевый»; ⅓-треугольник (см. геом).
- **10** yellow: 2 «другие» стороны → corange+cgreen (было 2 синих); шаг 158 (G1); init через measure.
- **11** count: «Правильный треугольник» перенесён на сцену 3 (абзацы по одному).
- **12** prob: оба ответа золотом (через G4).
- **13** polygons: G5 вопрос → .acc цвет (не `<b>`); 8→k золото (scoped CSS); периметр 8-уг = spacing честно (проверено в коде: side=T/k).
- **14** circle: 3→4 сцены (окружность/d=1/π/формула по одному); шаг 158.
- **15** result: `2L/π` и `L/π` → `.acc` (золото); текст упрощён в 2 строки (убрано «можно доказать, что», «Так же как для окружности»); канвас с нуля.
- **G1 канвасы:** result 150→158, yellow 112→158 (единый шаг 158 на боковых). ⅓-треугольник rail: сторона ~115px→55.3=⅓·166.
- **Фаза D (17–22):** G6 — слайд 21 (4 абзаца на одной сцене → сцены 2–5), слайд 19 (формализация→сц.3). 17/18/20/22 не требуют. G3/G4/G5 в гл.2 подтверждены рендером.

### Как проверил
- **Рендер-харнес** (python-playwright через системный Chrome, кэш ms-playwright рассинхронен — обошёл `channel="chrome"`): каждый тронутый слайд отрендерен по сценам в 1440×810, `reduced-motion` для детерминизма, СМОТРЕЛ на картинки.
- **audit.py** (скопирован из скилла, пропатчен на `channel="chrome"`): после каждой фазы. Итог — **5 FAIL, ВСЕ пре-существующие** (git HEAD даёт ровно те же 5): sl-title overflow, sl-coin 32px, sl-condition 24px + border-radius точек-легенды, sl-thanks box-shadow/radius. **0 регрессий.**
- **audit.py --scene-diff** по всему деку: **пустых сцен 0**; «non-additive» прыжки 13(baseline)→**12** (МЕНЬШЕ — новых прыжков геометрии не внёс; остаток присущ живым канвасам/цвет-реквилам).
- **Верификатор** (свежий субагент Sonnet, другой метод, 19 рендеров): все 10 контрольных пунктов ✓, **дефектов не найдено**.

### Что НЕ трогал (границы соблюдены)
- Блок `[4]` ENGINE — verbatim (правил только `activate` в блоке `[3]`, по санкции исключения G3).
- Слайды 1, 2, 4, 8 — не трогал. Число `0.637` на сл.15 — не менял.
- Гл.2 (17–22) по содержанию/вёрстке не переписывал — только G6 (scene-gating атрибуты) + подтверждение G1–G5.
- Тексты, не помеченные в worklist, — не трогал.

### Вопросы / развилки / оспоренные предпосылки
1. **G1 «у окружности вдвое мельче 8-уг» (worklist §5) — ОПРОВЕРГНУТО замером:** polygons и circle оба 4.1 периода (spacing 158, панель ~648px) — уже идентичны. Реальный разнобой был у yellow(112)/result(150), их и свёл к 158.
2. **Слайд 3 G1 — НЕ сделано (ограничение):** правый рисунок `ill-needles` — это base64-PNG, шаг разлиновки запечён в растр; правкой разметки не меняется. Нужна регенерация изображения (вне модели редактирования дека). Worklist помечал «иначе норм».
3. **Слайд 7 ½ не блюрил:** worklist предлагал «½ затемнить», но ½ уже ВЫВЕДЕНО аргументом про четверти (p2) — блюрить уже доказанное неверно педагогически. Сделал прогрессивный reveal вопроса про d вместо этого.
4. **phase-mass НЕ endless:** это плотностная диаграмма (заполняет прямоугольник точками); endless сделал бы её сплошной. Обоснованное отклонение от общего правила endless.

### ⚠️ ГРОМКО: что осталось НЕ доведено до конца
Не тихий недобор — заявляю прямо:
- **Слайд 13 «5 сцен»** (worklist): оставил 4 сцены. G5-вопрос, 8→k-золото, честный периметр — сделаны; более дробная 5-сценная раскадровка (полировка темпа) НЕ реализована — риск сломать рабочий слайд ради тонкой полировки счёл неоправданным.
- **Слайд 3 G1** — не сделан (PNG, см. вопрос 2).
- **Пре-существующие 5 audit-FAIL** — НЕ устранял: они в слайдах вне задачи (1 title, 23 finale) или упираются в текст-объём гл.2/coin, который границы запрещают переписывать. `audit.py exit 0` НЕДОСТИЖИМ без нарушения границ — это ограничение канон-примера, не моя регрессия (baseline даёт те же 5).

### Гейты приёмки
- audit зелёный? — **Нет абсолютно** (5 пре-существующих FAIL вне границ), но **0 регрессий** и все системные фиксы работают. `--scene-diff`: пустых сцен нет.
- график-5 с ~50 ✓ · канвасы endless+с нуля ✓ · зел/оранж в C ✓ · шаг разлиновки един (158) ✓ · золото ответов ✓ · верификатор зелёный ✓ · блок [4] verbatim ✓ · слайды 1/2/4/8 и гл.2-содержание не тронуты ✓.

### Попунктный чеклист worklist §0–§1 + G6·(17–22)
**§0:** G1 ☑(канвасы+⅓-треуг; сл.3 PNG=ограничение) · G2 ☑ · G3 ☑ · G4 ☑ · G5 ☑ · G6 ☑
**§1:** 1☑(н/т) 2☑(н/т) 3⚠(PNG) 4☑(н/т) 5☑ 6☑ 7☑ 8☑(н/т) 9☑ 10☑ 11☑ 12☑ 13◐(G5+8→k+периметр ☑; 5-сцен ☐) 14☑ 15☑
**G6·(17–22):** 21☑ 19☑ 17/18/20/22 ☑(не требуют)

### Время + токены
Прогон — один длинный сеанс (Фазы A→E последовательно, ~8 коммитов, десятки рендеров). Точный счётчик токенов основного цикла мне из окружения недоступен; субагент-верификатор — 74 276 out-токенов / 19 tool-uses. Оценка масштаба основного прогона — существенно больше (полное чтение ядра, ~40 правок, циклы рендер-смотри). Если нужен точный биллинг — по логам сессии.
