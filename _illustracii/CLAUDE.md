# CLAUDE.md — вход исполнителя в фабрику иллюстраций

> **КОГДА читать:** первым, если тебе дали заход по иллюстрациям или ты собираешься рисовать. · **КУДА дальше:** `DISCIPLINA.md` — до первой линии.

## Одно правило перед всеми остальными

**Рисуешь — сначала открываешь `DISCIPLINA.md`.** Не изобретай примитив, который уже есть: иначе получится 92 разных рисунка вместо одного языка, и ни один не будет починен, потому что разбирать каждый отдельно дороже, чем терпеть.

Взял примитив из словаря, и вышло плохо → **обязан починить словарь**, а не только свой рисунок. Молча обойти — оставить грабли следующему; правка в словаре чинит все будущие рисунки разом.

## Что здесь лежит

| Файл | Что в нём |
|---|---|
| `DISCIPLINA.md` | **ядро.** Словарь примитивов, главное правило «внутри рисунка только МЕТКА, проза — в подпись», пропорции, фон-доска, ловушки, дисциплина |
| `POKRYTIE.md` | инцидент → рычаг, с ценой: что уже ломалось и чем это ловится (или не ловится ничем) |
| `UROKI.md` | жалобы владельца и находки исполнителей — сырьё для правил |
| `fazy/DOK.md` · `fazy/ZAHOD.md` · `fazy/PRIMERY.md` | шаг 9 конвейера презентаций: материализация плейсхолдеров в деке |
| `docs/KARTA.md` | индекс документов фабрики |

## Дисциплина

- **Завёл новый `.md` — регистрируешь ТЕМ ЖЕ ходом, одной командой:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). Индекс — `docs/KARTA.md §6`, свой, не студийный. Руками в §6 не писать: разъехавшиеся дверь и ворота 5 не оставляют легального хода, кроме `--no-verify`.
- **Git — только через `_generator/tools/git_zona.py`**, голым `git` не трогать. Канон — `../_studio/docs/kak-delat/GIT-disciplina.md`.
- **Проверяется глазом то, что глазом и живёт.** Машинного гейта на «красиво» нет и быть не может; что реально краснеет — перечислено в `POKRYTIE.md`.
- **Цвет — только классом или токеном.** Литеральный hex и `stroke="black"` означают, что рисунок не сядет в палитру и не перекрасится темой.

## Граница фабрики

Здесь — **«иллюстрация сама по себе здорова»**. Не здесь — **«дек её правильно зовёт»**: `build_deck.py.lint()` (резолвится ли `data-ill`, не осиротела ли картинка) и `audit.py svgOverflow` (SVG против бокса панели слайда) остаются при слайдах, потому что считают по собранному деку. Подробнее — `README.md §Чего фабрика НЕ делает`.

---
*Короткий вход. Ядро — `DISCIPLINA.md`; что фабрика вообще есть — `README.md`.*
