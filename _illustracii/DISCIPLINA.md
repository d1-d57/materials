# СЛОВАРЬ ПРИМИТИВОВ — из чего рисуются все наши иллюстрации

> **КОГДА читать:** ПЕРЕД тем как рисовать любую иллюстрацию — и для слайда (дек), и для объяснительного текста (doc-вид). Один словарь на оба. · **ЧТО дописывать сюда:** новый примитив — сразу, как только понадобился (см. §Дисциплина). · **КУДА дальше:** оркестрация арки 9 — `fazy/DOK.md`; вид текста — `../_studio/docs/kak-delat/STANDART-oformlenia.md §6`; канон рисунка для дека — `html-slides-studio/references/images.md`.

## Зачем словарь

Иллюстрации рисуются **руками** (ветка 1 арки 9 — дефолт ~90% нагрузки), и каждый раз с нуля. Отсюда две беды, обе живые:

- **Съезжает и не правится.** В `catalan/` — **92 фигуры в 8 файлах**; часть съехала, и владелец их не правил: разбирать каждую отдельно дороже, чем терпеть. Со словарём правка делается **один раз в словаре** и дальше применяется ко всем.
- **Проза внутри картинки.** Из **952** подписей `<text>` в каталане **134 — пояснения**, а не метки: «стек-сортировка π = 4123 (Опр. 1.1): читаем слева направо», «если бы a∈L, b∈R и a больше b —». Владелец: *«некрасиво выглядят и непонятно обычно»*. Это же ловит гейт `svgProse` (`audit.py`, WARN) и канон `images.md` («БЕЗ текста-комментария, только идентификаторы `F₁`, `P`»).

## Главное правило (оно же — самое нарушаемое)

> **Внутри рисунка живёт только МЕТКА. Пояснение живёт в подписи под рисунком.**

- ✅ **метка** — имя объекта или координата: `C₁ = 1`, `v`, `t`, `0`, `−1`, `F₁`, `π`.
- ❌ **проза** — фраза, объясняющая смысл. Уходит в `<figcaption>` (doc-вид) или в текст слайда (дек).

**Смысл несёт графика, а не текст.** Как показывать смысл без слов:

| Хочешь сказать | Показывай |
|---|---|
| «вот этот объект, о нём речь» | залитый узел `.s-node-r` среди белых `.s-node` |
| «вот что изменилось» | акцентный цвет `.s-accent` |
| «это построение / ось / вспомогательное» | пунктир `.s-dash`, тонкая `.s-thin` |
| «это одно переходит в то» | стрелка `.s-ar-m` |
| «эта область — та самая» | заливка `.s-fillsh` |

*Образец:* в рисунке действия `e_i` (лекция про `TL`) **нигде не написано, на какие точки оно действует** — пара `i, i+1` просто залита. Ни одного слова, и всё понятно.

## Словарь

Форма примитива одна; привязка к цвету зависит от носителя — **doc-вид** красится классами `.s-*` (они в `_generator/build_doc.py`), **дек** — токенами `var(--…)` из `tokens.css`.

| # | Примитив | Как рисуется | Класс (doc-вид) | Семантика |
|---|---|---|---|---|
| 1 | **узел** | `<circle r="3.4">` (в деке крупнее) | `.s-node` — обычный · `.s-node-r` — тот, о ком речь · `.s-node-a` — акцентный | точка, вершина, элемент |
| 1б | **цветной узел** (ТОЛЬКО дек) | `<circle class="s-node s-node-c1">` — **всегда парой классов**, см. ниже | `.s-node-c1` · `.s-node-c2` · `.s-node-c3` (`--p1`/`--p2`/`--p3`) | цвет несёт ТОЖДЕСТВО элемента: «этот элемент здесь и этот же элемент там». Не состояние и не роль — для них узел #1 |
| 2 | **ребро / отрезок** | `<line>` | `.s-line` (2px) · `.s-thin` (1.2px) · **`.s-thin-a`** (1.4px акцентом) | связь; тонкая — вспомогательная; **акцентная тонкая — стрелка ДЕЙСТВИЯ** (переименование, перестановка), когда её много и `.s-accent` 2.4px слишком жирна |
| 3 | **дуга** | `<path d="M x,y C x,h x2,h x2,y">`, подъём `14 + (пролёт−1)·11` | `.s-line` · `.s-accent` | соединение пары точек одного ряда (паросочетание, скобки) |
| 4 | **путь-ломаная** | `<polyline points="…">`, шаг 36 по x, 26 по y | `.s-line` · `.s-accent` | путь Дика, траектория |
| 5 | **дефект / свободный конец** | `<line>` вниз на 17px от узла | `.s-line` · `.s-accent` | несоединённая точка |
| 6 | **многоугольник** | `<polygon points="…">` | `.s-line` + `.s-fillsh` (заливка) | многоугольник, триангуляция, область |
| 7 | **клетка** | `<rect>` | `.s-line` · `.s-fillsh` · `.s-fillw` | клетка решётки, плитка, ячейка таблицы |
| 8 | **ось / уровень** | `<line stroke-dasharray="5 4">` | `.s-thin` | координата, горизонталь отражения |
| 9 | **построение / разрез** | `<line>`/`<path>` пунктиром | `.s-dash` (пунктир акцентом) | вспомогательная линия, диагональ |
| 10 | **стрелка между ОБЪЕКТАМИ** (`→`) | `<line>` + наконечник, **вычисленный из направления линии** (формула — §СК1 ниже). Константа `l9,4 -9,4` — только для строго горизонтальной вправо | `.s-thin` + `.s-ar-m` · `.s-ar-a` | отображение объекта в объект: множества, пространства, группы |
| 10б | **стрелка между ЭЛЕМЕНТАМИ** (`↦`) | то же, что #10, **плюс палочка у хвоста**: отрезок перпендикулярно линии через её начало (формула — §СК2 ниже) | те же классы, палочка — классом самой линии | элемент переходит в элемент: `x+iy ↦ (x,y)`, многочлен ↦ набор коэффициентов |
| 11 | **метка** | `<text>` | `.s-txt` (13px) · `.s-txt-m` (12px, приглушённая) · **`.s-txt-a`** (12px акцентом — имя стрелки действия) · `.s-txt-w` (белая, на заливке) | ТОЛЬКО имя/координата (см. правило) |
| 12 | **путь со скруглёнными углами** | `<path>`: в каждой вершине срезать `c = min(r, полребра)` по обеим сторонам и заменить угол дугой `A c,c 0 0,sweep` (`sweep = 1`, если поворот по часовой В ЭКРАННЫХ координатах: `cross = ax·by − ay·bx > 0`) | `.s-line` · `.s-accent` | ломаная, у которой углы читаются как самопересечения: кривая дракона, фрактальные пути, плотно уложенные траектории |

**Про #12 — почему заведён (2026-07-16, арка `krivaya-drakona`, дисциплина §3).** Примитив #4 (ломаная) на кривой дракона **вышел плохо**: при ранге ≥ 4 ломаная подходит к одной вершине решётки дважды, и на остром угле при толщине штриха 3px касание читается как **самопересечение** — картинка выглядит грязной и врёт про предмет. Владелец: *«гораздо лучше они будут выглядеть, если ты их сгладишь, как рисуют кривую дракона обычно: угол заменишь на дугу — тогда оно перестанет себя пересекать»*. При `c = полребра` дуги соседних углов смыкаются в серединах рёбер: путь становится гладким (касательная непрерывна), прямых участков не остаётся — это и есть канонический вид кривой дракона. **Скругление — вид, а не предмет:** касания и самопересечения считаются по ЛОМАНОЙ (целочисленные вершины), рисуются — по скруглённому пути. Считать по скруглённому — ошибка.

**Про #1б — почему заведён и почему ПАРОЙ КЛАССОВ (2026-07-31, арка `dovodka-l1`).** Владелец заказал на «Примерах» шесть биекций трёхэлементных множеств *«там даже можно буквы не ставить — просто три цвета, слева три элемента и справа такие же, и показывать, как мы переставляем объекты»*. Буквы сняты, значит тождество элемента больше нести нечему, кроме цвета: `.s-node-r`/`.s-node-a` тут не годятся — они означают роль («о нём речь», «акцент»), а не «который именно». Три цвета берутся токенами `--p1/--p2/--p3`, своих не заводится.

> 🔴 **Писать `class="s-node s-node-c1"`, а не `class="s-node-c1"`.** Класс определён в носителе ДЕКА (`<лекция>/src/tools/sobrat_overlay.py` → `overlay.css`) и НЕ определён в носителе doc-вида (`_generator/build_doc.py`). Одиночный `.s-node-c1` в doc-виде дал бы `<circle>` без `fill` — то есть ровно тихую черноту из ловушки ниже, за которую уже заплачено девятью ненарисованными стрелками. В паре базовый `.s-node` красит узел в обоих носителях, а в деке `.s-node-cN` перебивает заливку позже по каскаду при равной специфичности. **Это общий приём для любого примитива, живущего только в одном движке:** базовый класс первым, специализация вторым.

**Сетка (doc-вид).** Шаг по x — 32–36px, по y — 26px; узел `r ≈ 3.4`; строки «до → после» — `<g transform="translate(0,N)">`, шаг 68px; поля от края `viewBox` — не меньше 18px.

## 🔴 СТРЕЛКА — ДВА ПРАВИЛА, ЗАКРЫТЫЕ РАЗ И НАВСЕГДА (решение владельца 2026-08-09)

Источник дословно: `../_studio/zhurnal/2026-08-07_arhitektura-slajdov/PRAVKI-SBORKI-L2.md §8.1`.
Владелец, глядя на собранную деку Л2: *«процентов 60 претензий связано с тем, что стрелочки плохо
нарисованы… это надо просто формализовать, и уже будет лучше»*. И про дом правила: *«это вопрос,
который должен раз и навсегда один раз в скилле иллюстраций решиться»*.

### СК1. Наконечник строится ИЗ НАПРАВЛЕНИЯ ЛИНИИ, а не ставится константой

Дословно: *«конец стрелочки — это маленький треугольничек, равнобедренный. И он должен строиться
так: берём ось — саму линию, которой мы его присоединяем, — и рисуем треугольничек так, чтобы его
высота, которая у него ещё и медиана, и биссектриса, была продолжением этого луча. Он как бы
нанизывается»*.

**Формула — вычисляется, всегда, для каждой стрелки.** Линия идёт `P1 → P2`:

```
u = (P2 − P1) / |P2 − P1|          единичное направление
n = (−u_y, u_x)                     нормаль
T = P2                              вершина треугольника — на конце луча
B = T − 9·u                         середина основания, в 9 px позади вершины
d = "M {B+4n} L {T} L {B−4n} Z"     равнобедренный: высота 9, основание 8
```

**Размеры взяты замером эталонов, а не назначены:** и `retrakt-i-obraz`, и живая картинка слайда 12
`vlozhenie-i-vozvrat` (владелец: *«тут правильно, красивая, кстати, стрелочка нарисована»*) дают
ровно высоту 9 и основание 8.

**Две законные привязки, обе «нанизаны на луч»** — важно, потому что в деке живут обе:
- **вершина на конце линии** (`T = P2`, основание внутри линии) — так в `retrakt-i-obraz`;
- **основание на конце линии** (`T = P2 + 9u`, наконечник надставлен снаружи) — так в
  `vlozhenie-i-vozvrat` и `disk-i-okruzhnost`.
Выбор — за автором рисунка; **не выбор — ось и место**: ось строго вдоль `u`, основание не дальше
9 px от конца линии.

**Для кривой (`<path>`, дуга, спираль) `u` — касательная В КОНЦЕВОЙ ТОЧКЕ.** Для кубики
`C P1 P2 P3` это `P3 − P2`; для дуги `A` — касательная в конце дуги.
🔴 **И отдельно, глазом:** касательная должна быть ВИДНА. Если кривая приходит в конец по участку
длиной в пару пикселей, формально соосный наконечник читается как отдельный клин, висящий сбоку.
Лечится формой кривой (прямой участок + скругление `A r,r`, как в `vlozhenie-i-vozvrat`), а не
доворотом наконечника. *Живой случай: дуги `id`/`≠ id` на `centr-i-ego-obraz` — Безье приходила
вертикально в конец, наконечник был соосен и всё равно выглядел неправильно; владелец назвал обе.*

✅ **Пример (горизонталь вправо, `P1=(46,29) P2=(132,29)`):**
`<line class="s-thin" x1="46" y1="29" x2="132" y2="29"/><path class="s-ar-m" d="M123,25 l9,4 -9,4 z"/>`
— это и есть формула при `u=(1,0)`; константная запись здесь законна ровно потому, что совпала.

❌ **Контрпример (наклонная, живой брак из `vektory-na-ploskosti` до правки):**
`<line class="s-line" x1="42" y1="180" x2="156" y2="143"/><path class="s-ar-m" d="M164,140 L154,150 L151,139 z"/>`
Проверка краснеет на нём словами *«треугольник не равнобедренный (боковые расходятся на 8 %)»* —
боковые стороны 14,1 и 13,0 вместо равных, высота 12,4 вместо 9, основание 11,4 вместо 8. В полной
картинке к этому добавляется «основание в 10,5 px от конца линии» и отклонение оси: перекос такой,
что даже вершина треугольника читается неоднозначно.
**Так выглядит константа `l9,4 -9,4`, подправленная на глаз.**

🔴 **Почему НЕ `<marker orient="auto">`, хотя браузер повернул бы сам** — три причины, все из ловушек
этого же файла: (1) `<marker>` живёт в `<defs>` по `id`, а дек инлайнит все слайды в ОДИН html —
это прямиком ловушка «конфликт `id` при инлайне», оплаченная 19 маркерами каталана; (2) `.s-ar-*` —
это `fill` наконечника, и класс определён в двух разных носителях, внутрь маркера тот каскад не
доезжает; (3) `cairosvg` не применяет CSS-классы и на растровой проверке соврёт. Плюс вычисленный
`path` статически проверяем — см. ниже; маркер проверять нечем.

### СК2. Палочка `↦`: между элементами — не то же, что между объектами

Дословно: *«когда ты рисуешь стрелку, которая изображает не отображение множеств, а изображает, что
элемент слева переходит в элемент справа, мы ставим вертикальную палочку перед этой стрелкой. Без
палочки — это отображение множеств»*.

⇒ Это **два разных примитива** (#10 и #10б), а не оттенок одного. Палочка:

```
n = (−u_y, u_x)
<line class="<класс линии>" x1={P1+5n} … x2={P1−5n}/>      полудлина 5, через начало линии
```

✅ **Пример:** `x+iy ↦ (x,y)` и «многочлен ↦ набор коэффициентов» на слайде 5 Л2 — стрелки между
ЭЛЕМЕНТАМИ, палочка обязательна.
❌ **Контрпример:** `f: V → W`, `s: ℤ₂ → S₃`, `π₁`, `η_V` — стрелки между ОБЪЕКТАМИ, палочки нет.
Поставить её здесь — такая же ошибка, как не поставить там.

⚠ **Правило уже жило в половине конвейера и не доезжало во вторую.** В ТЕКСТЕ слайда 5 различение
было сделано верно (`f\colon V\to W` без палочки, `x+iy\mapsto(x,y)` с палочкой), а на картинке того
же слайда — нет. Владелец: *«эти палочки должны быть на голубой зоне тоже нарисованы»*. Отсюда общий
вывод: **`→`/`↦` в формуле и `→`/`↦` на рисунке — одно решение, принятое один раз, а не два.**

### Способ проверки

```
python3 _illustracii/proverka_pomarok.py <файл.svg> [ещё файлы…]
```
Проверка **СК1** механическая и краснеет сама: для каждого `<path class="s-ar-*">` ищется линия (или
конец кривой), к концу которой он привязан, и проверяются три вещи — треугольник равнобедренный,
ось отклонена от направления линии не больше чем на 5°, основание не дальше 9,5 px от конца.
Фикстуры: `fixtures/narushitel-sk1.svg` — краснеет, `fixtures/chistaya.svg` — молчит.

⚠ **Чего проверка НЕ ловит и что остаётся глазу** (называется прямо, чтобы зелёный не читался как
«стрелки в порядке»): (1) **СК2 целиком** — машина не знает, элементы у тебя на картинке или
объекты, палочку проверить нечем; (2) **невидимую касательную** из СК1 — формально соосный
наконечник на кривой, приходящей «из ниоткуда»; (3) наконечник, привязанный не к той линии, если
линии сходятся в одной точке ближе 10 px. **Задача визуального верификатора — пройти по каждому
элементу рисунка и сверить его с этим словарём**, а не оценить «красиво ли»; планка владельца:
*«это должны быть иллюстрации, которые могли бы быть опубликованы в учебнике — по геометрии
абсолютно стандартные»*.

**ЦЕНА, из-за которой правила записаны.** Примитив #10 задавал наконечник константным смещением
`l9,4 -9,4` — треугольником, смотрящим вправо. Рисующий агент делал ровно то, что написано в
словаре, поэтому **любая негоризонтальная стрелка съезжала по построению**: на живой деке Л2 из 46
линий 9 наклонных, и все наконечники на них оказались браком. Владелец: *«с этим любой Claude
понимает, как должно быть нарисовано; то, что он это пропускает — какая-то систематическая вещь,
которую я бы решал более структурно»*. Виноват был словарь, не исполнение.

## Пропорции решают, где рисунок будет стоять (2026-07-24, doc-вид)

В документ-виде есть правое поле шириной почти как текстовая колонка, и рисунок едет туда или
остаётся в потоке **по жёсткому критерию, который движок считает сам из `viewBox`:**

> **высота / ширина ≥ 0,6 → на поле** (`<figure class="mn">`) · **иначе — во всю строку потока.**

Широкая полоса в жёлобе сжимается до нечитаемости; высокая в потоке оставляет по бокам пустоту.

**Решение принимается ДО рисования, и рисунок делается под своё место.** В поток — **горизонтальный**
(длинная траектория, лента шагов, ряд панелей бок о бок): тянем в ширину. На поле — **узкий,
вытянутый вверх** (столбик, квадрат, компактная фигура): ширина в жёлобе фиксирована, лишняя ширина
превращается в мелкий нечитаемый шрифт.

**Рисунок «почти проходит» — чинить РИСУНОК, а не правило.** Живые прецеденты одной сессии:
- диагонали Паскаля 0,61 → 0,95: выброшено слово-заголовок «суммы» **внутри графики** и колонка
  значений (они уже есть в подписи), строки обрезаны многоточием — для диагоналей нужна вся
  диагональ, а не вся строка;
- колокол 0,51 → 0,89: убрана длинная ось, сужен шаг столбиков;
- звезда Давида 0,49 → 0,66: сжат горизонтальный шаг гексагона.

**Служебные подписи — к краю, предмет рисунка — по центру.** Прецедент: подписи `n = 0…5` слева
были включены в центрирование, и треугольник Паскаля выглядел съехавшим вправо. Центрировать надо
то, ради чего рисунок сделан.

**Ставить фигуру сразу за тем блоком, который она иллюстрирует:** на поле она встаёт на его уровень.

## 🔴 ФОН РИСУНКА — ГОЛУБАЯ ДОСКА, НЕ БЕЛЫЙ ПРЯМОУГОЛЬНИК (решение владельца 2026-07-31)

**Белых подложек под иллюстрацию не бывает. Ни в деке, ни в doc-виде.** Рисунок ложится прямо на доску `--board`.

> Владелец, увидев колоду Л1: *«У нас такая структура: голубая полоса, на ней ещё голубой прямоугольник, сверху белый, сверху картинка. Лучше было бы, чтобы картинка была сразу в белой зоне… раз и навсегда никаких белых прямоугольников не вставлять. Сразу сделать более стильным и даст больше места — возможность налево и направо поместить какие-то вспомогательные вещи».*

**Почему это не вкусовщина.** Экран делится на ДВЕ зоны — бумага `--paper` и доска `--board`. Белая панель заводит третью, ничего не означающую, и платит за неё шириной: на живом слайде Л1 панель отжимала рисунку около трети полосы. Убрали панель — картинка стала шире примерно в полтора раза и встала по центру, а «лишние вертикальные разделители», которые владелец требовал снять на соседнем слайде, оказались не нужны: слагаемые разносятся расстоянием, которого раньше не было.

**Две правки палитры, без которых на доске плохо** — обе проверены глазом на настоящей фигуре:

| Примитив | Было (для белого) | Стало (для доски) | Почему |
|---|---|---|---|
| `.s-node` — полый узел | `fill: var(--card)` — чистый белый | `var(--hollow)` — очень светлый голубоватый | чистый белый на доске читается как наклейка сверху, то есть возвращает снятый слой размером с точку |
| `.s-thin`, `.s-ar-m` — служебная линия и стрелка | `var(--steel)` | `var(--steel-d)` — заметно темнее стали | сталь на доске почти растворяется; на белом её хватало |

✅ **ИСПОЛНЕНО 31.07 в `teorkat-vvedenie`** (дата данных 2026-07-31): токены `--hollow: #e7f0f4` и `--steel-d: #566b86` заведены в `src/tokens.css`, привязка — в `src/tools/sobrat_overlay.py` (не в порождённом `overlay.css`). Сам `--steel` НЕ тронут: им крашены `.s-txt-m` и приглушённые подписи, а они стоят не на доске. **Заливка панели снята там же, где обещано:** `src/base.css`, `.panel{background:none}`.

**Где физически сидела панель** (чинить там, а не в фигурах): `<лекция>/src/base.css`, правило `.panel { background: var(--card) }`.

**ЦЕНА, из-за которой правило записано:** три слоя вместо двух прожили всю арку Л1 незамеченными — ни один гейт не краснел, потому что структура была технически исправна. Поймал владелец глазами при второй читке колоды и сказал прямо: *«не понимаю, почему это до сих пор у нас в фабрике не прописано»*. До записи здесь правило существовало только как вкус того, кто рисует.

⚠ **Проверяется только глазом.** Машинного гейта на «красиво на доске» нет и быть не может; ближайшее, что краснеет, — греп `background:\s*var\(--card\)` по `src/base.css` лекции, и (с 2026-08-09) `proverka_pomarok.py` ниже, если белая заливка стоит ВНУТРИ самой иллюстрации. Ни то ни другое не ловит плохо подобранный цвет узла.

## 🔴 ПОМАРКИ ВЛАДЕЛЬЦА — П9, П10/П11 (2026-08-09, разбор `centr-gruppy`)

Источник дословно: `_studio/zhurnal/2026-08-07_arhitektura-slajdov/POMARKI-2026-08-09.md §3`.
Проверка — `python3 _illustracii/proverka_pomarok.py <file>` (вызывается из `fazy/ZAHOD.md §4`,
шаг создания иллюстрации, ДО того как она уедет на слайд); фикстуры-нарушители — `fixtures/`.

**П9. 🔴 Вопросительные знаки на иллюстрации — ЗАПРЕЩЕНЫ.** Дословно: *«лишних подписей вот этих
вопросительных знаков вообще не нужно, это запрещено просто»*.

**П10. Подпись поверх стрелки — не нужна.** Равенство, написанное поверх центральной вертикальной
стрелки, убрать. Три раза написать `Z` — можно.

**П11. 🔴 Композиция рисуется ДУГОЙ, а не подписью.**
Владелец: *«если ты хочешь выразить, что сквозная стрелка тождественная, то нужно нарисовать
сквозную стрелку сверху диаграммы — такой полукруг выпуклый — и она тождественная. А снизу тогда
`Z(s)`, `Z(r)` и композиция, симметрично дугой вниз, и подписать `Z(id) = id`.»* Дуга — примитив
#3 словаря выше; подпись `Z(id) = id` под ней — законная МЕТКА (не проза), запрещено только
уравнение композиции (`∘` вместе с `=`) ВНУТРИ `<text>` как замена дуге.

⚠ Признак нарушения механический, но приближённый: `<text>`, несущий одновременно `∘` и `=`, —
кандидат «равенство композиции написано текстом вместо дуги». Не ловит случай, когда дуга нарисована
неверно (не дугой, а прямой) — это по-прежнему проверяется глазом.

## Обязательное на каждом рисунке

1. **`viewBox`** — всегда. Плюс `width` в атрибуте (doc-вид) или `preserveAspectRatio="xMidYMid meet"` + `width:100%;height:100%;display:block` (дек — иначе обрежет при масштабе, ловит `svgOverflow`).
2. **`role="img"` + `aria-label`** — человеческим языком, что изображено.
3. **Цвет — только классом/токеном.** Никаких `stroke="black"` и литерального hex: рисунок не сядет в палитру и не перекрасится темой.
4. **Единый размер фигур** (прецедент #14): повёрнутая фигура — тот же размер, **поворот, а не масштаб**.
5. **Уместность** (прецедент #14): рисунок служит доказательству или условию. Декоративный «к каждой фразе» — дефект, выпиливается.

## Ловушки (все — живые прецеденты)

- **Проза внутри `<svg><text>`** — 134 случая в каталане. См. главное правило.
- **Конфликт `id` при инлайне.** Несколько SVG в ОДНОМ документе делят пространство имён: два независимых `id="clip1"`/`marker` тихо ломают друг друга. В каталане `defs`+`marker` — 19 штук. Лечение: per-file префикс (`prefixIds` по stem).
- **Undefined `var(--x)` → чёрная заливка**, БЕЗ ошибки в консоли. Ловит статический var-lint (`build_deck.py`).
- 🔴 **Класс выдуман НА МЕСТЕ — его нет ни в словаре, ни в движке, и все гейты зелёные.** Прецедент `.s-node-a` ниже учит «взял примитив — проверь, что класс есть в движке»; он НЕ срабатывает, когда класса нет и в словаре, — проверять нечего. *Цена (2026-07-31, записки Л1): аналитик завёл `.s-thin-a` и `.s-txt-a` для цветового кодирования роли стрелки, вписал фигуру в ленту, и в разделе «Почему это красиво» не нарисовалась ни одна из 9 стрелок σ и обе горизонтали коммутативного квадрата — `<line>` без своего `stroke` по SVG не рисуется ВОВСЕ, даже не чернотой. `build_doc.py`, `check_view.py`, `check_termin.py` — все rc=0. Поймано только растеризацией, и не автором.* **Механизм, которого не хватает:** греп всех `class="s-*"` по ленте против списка классов, определённых в движке. Одна команда, краснеет сама.
- **Примитив есть в словаре, но не в движке** — та же тихая чернота. Живой прецедент: `.s-node-a` стоял в строке «узел» с самого начала, а в `build_doc.py` его не было; `<circle>` без `fill` падает в чёрный молча. Заведён 2026-07-16 (арка `krivaya-drakona`). **Берёшь примитив — проверь, что класс реально определён в движке-носителе** (`build_doc.py` — doc-вид, `tokens.css` — дек); нет → доводи движок, это и есть дисциплина §3.
- **`<img>`/base64-изоляция:** внутри растра `var()`/`currentColor` не работают. Растр — только для фото; схема всегда `<svg>`.
- **`cairosvg` не применяет CSS-классы** — при проверке в песочнице зальёт всё чёрным и соврёт. Рецепт разворачивания классов в атрибуты — `../_studio/docs/kak-delat/STANDART-oformlenia.md §6`.
- **`cairosvg` масштабирует по атрибуту `width` и игнорирует пропорцию `viewBox`** — PNG выходит растянутым, автор видит перекос, которого в браузере нет, и «чинит» верный рисунок. Снимай `width` перед растеризацией: `svg = re.sub(r'\swidth="\d+"', '', svg, count=1)`. *Цена (2026-07-27): рисунок 372×122 с `width="640"` растянулся в 1,7 раза; поймано на четырёх параллельных исполнителях сразу.*
- **Карта разворачивания классов неполна ⇒ гейт падает `KeyError`, и каждый чинит её у себя.** *Цена (2026-07-27): `.s-ar-m` / `.s-ar-a` отсутствовали, два исполнителя дописали их независимо в свои копии — общий рецепт остался сломанным. Завёл новый класс `.s-*` в движке — той же рукой добавь строку в карту `M` (`STANDART-oformlenia.md §6`).*
- 🔴 **`font-size` АТРИБУТОМ на `<text class="s-txt">` не работает — размер задаёт класс, и он бьёт
  атрибут.** В CSS-каскаде презентационный атрибут SVG стоит НИЖЕ любого правила таблицы стилей,
  а `.s-txt{font-size:13px}` и `.s-txt-m{font-size:12px}` определены жёстко
  (`_generator/skeleton/base.css`, у деки — инлайном в собранном html). Поэтому `font-size="44"`
  рисуется тринадцатью пикселями, и рисунок молча выходит с метками вдвое-втрое мельче замысла.
  **Крупная метка задаётся ИНЛАЙНОВЫМ стилем:** `<text class="s-txt" style="font-size:40px">` —
  инлайн-стиль каскад выигрывает. Атрибут можно оставить рядом, он документирует намерение.
  *Цена (2026-08-09, доводка Л2): владелец заказал на слайде 15 буквы `S¹`/`D²` «в 2–3 раза крупнее
  нынешнего шрифта, символы должны быть соразмерны стрелкам». Им было выставлено `font-size="44"`,
  файл прошёл `proverka_pomarok.py`, гейты вёрстки и иллюстраций, сборку деки и мой собственный
  осмотр кадра — а на экране буквы стояли 13 px против 12 px у подписей, то есть заказ не выполнен
  вовсе при всех зелёных. Поймал визуальный верификатор, замерив `getComputedStyle` вместо того,
  чтобы поверить атрибуту. Проверять так: `getComputedStyle(t).fontSize`, а не значение в файле.*
- **Стрелка `.s-ar-*` — это ЗАЛИВКА наконечника, а не обводка.** Наконечник рисуется `<path d="M x,y l9,4 -9,4 z">`, и в движке класс задаёт `fill`. Развернёшь его как обводку — треугольник выйдет полым.

## Дисциплина (решение владельца 2026-07-16)

1. **Рисуешь иллюстрацию → СНАЧАЛА смотришь этот словарь.** Не изобретай примитив, который уже есть: иначе получится 92 разных рисунка вместо одного языка.
2. **Нужного примитива нет → добавь его сюда**, тем же ходом, что рисуешь. Строка в таблицу + семантика.
3. **Взял примитив из словаря, и вышло плохо → ОБЯЗАН починить словарь**, а не только свой рисунок. Молча обойти — значит оставить грабли следующему; правка в словаре чинит все будущие рисунки разом.
4. Это же — ответ на «съехало и не правим»: правим **в словаре, раз и навсегда**, и потом прогоняем по нему старые фигуры.

---

*Общий словарь для арки 9 (слайды) и doc-вида (объяснительные тексты). Оркестрация — `fazy/DOK.md`. Канон рисунка дека — `html-slides-studio/references/images.md`. Вид текста — `../_studio/docs/kak-delat/STANDART-oformlenia.md`.*
